Here is the updated AI thread summary:

Feature and bugfix series for `imap-send` now in v5, completing all technical requirements while adding user-facing conveniences. The series resolves: (1) critical configuration parser bug where `cfg->folder` was incorrectly NULLed out (regression from 6d1f198f34), (2) OAuth2.0 support via curl's native API (OAUTHBEARER/XOAUTH2) with OpenSSL fallback paths, (3) new PLAIN authentication method for OpenSSL users (RFC 4616 compliant), (4) comprehensive CRAM-MD5 memory leak fixes including all error paths (now complete with final socket write failure case addressed in this patch), (5) new `--folder` CLI option providing runtime override of `imap.folder` configuration, (6) `imap.preferredbackend` configuration for curl/OpenSSL selection, and (7) new `imap.usecurl` configuration for persistent backend preference.

Key technical details:
- Configuration parsing handles all IMAP settings (`imap.folder`, `imap.user`, `imap.pass`, `imap.tunnel`, `imap.authmethod`, `imap.preferredbackend`, new `imap.usecurl`) with proper cleanup
- Fixes regression from 6d1f198f34 where `FREE_AND_NULL(cfg->folder)` incorrectly cleared folder field when processing other settings
- OAuth2.0 maintains RFC 5801/7628 compliance via curl's `CURLOPT_XOAUTH2_BEARER` with OpenSSL fallback (`oauthbearer_base64`/`xoauth2_base64` helpers)
- PLAIN authentication (`auth_plain()`) implements RFC 4616 via `plain_base64()` helper, completing authentication parity between curl and OpenSSL backends
- CRAM-MD5 fixes now fully comprehensive including socket write failure paths (final fix in this patch ensures `response` buffer freed when `socket_write()` fails)
- New `--folder` (`-f`) option overrides `imap.folder` config with proper memory management (frees old string before assignment)
- New `imap.preferredbackend` and `imap.usecurl` configurations select curl/OpenSSL with fallback logic and persistent preference
- Documentation covers all authentication methods (including new PLAIN support) and new options with clear precedence rules (CLI over config)
- New `AUTH_PLAIN` capability flag added to IMAP protocol handling
- Final patch adds `imap.usecurl` config to complement `--[no]-curl` CLI flags, maintaining build-time constraint handling (`USE_CURL_FOR_IMAP_SEND`/`NO_OPENSSL`)

Review history shows thorough iteration: Jeff King diagnosed configuration bug, Eric Sunshine identified memory issues, Brian Carlson drove architectural pivot to curl-native OAuth2.0. Junio Hamano reviewed config fix. Ben Knoble identified final use-after-free in CRAM-MD5 (fixed in v4) and confirmed completeness. V5 adds user conveniences while maintaining all prior fixes. All technical issues resolved with Knoble's validation of CRAM-MD5 memory management, now including this final socket write path cleanup.

Participants: Aditya Garg (author) incorporated feedback from Sunshine, King, Hamano, Carlson, and Knoble. The v5 additions make the feature more practical while preserving architectural improvements. The series satisfies legal constraints (curl-native OAuth2.0) and technical requirements, supporting all authentication methods (now including RFC 4616 PLAIN for OpenSSL) with complete memory management. Configuration fix remains critical for usability. With comprehensive authentication support and new user options (including runtime folder override via `--folder` and persistent backend selection via `imap.usecurl`), the series is merge-ready.

The OAuth2.0 implementation (part 2/6 of original series) provides both OAUTHBEARER (RFC 5801/7628) and XOAUTH2 (Google) mechanisms, with OpenSSL fallback via `oauthbearer_base64()`/`xoauth2_base64()` when curl unavailable. Documentation includes provider-specific guidance for Gmail (app passwords/OAuth2.0 tokens) and Outlook (XOAUTH2). Curl integration uses `CURLOPT_XOAUTH2_BEARER` for automatic mechanism selection. Error handling covers OpenSSL encoding and socket write failures. New capabilities `AUTH_OAUTHBEARER` and `AUTH_XOAUTH2` added to IMAP protocol handling while maintaining backward compatibility with existing `imap.authMethod` configuration. PLAIN authentication completes the method coverage with RFC 4616 implementation for OpenSSL users. CRAM-MD5 memory management is now fully robust across all error paths. The new `--folder` option follows established CLI patterns in `imap-send.c` and provides direct user control over destination selection. Final `imap.usecurl` configuration provides persistent backend preference to complement CLI flags.