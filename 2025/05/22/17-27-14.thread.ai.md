Here is the updated AI thread summary:

Feature and bugfix series for `imap-send` now in v8, introducing new OAuth2.0 authentication support alongside previously mentioned improvements. The series provides comprehensive IMAP functionality with both technical fixes and user-facing enhancements. Key resolved issues include:

1. Critical configuration parser bug where fields were incorrectly NULLed out (regression from 6d1f198f34) - fully fixed with proper field-specific cleanup
2. New OAuth2.0 support via both OAUTHBEARER (RFC 5801/7628) and XOAUTH2 protocols, implemented for both OpenSSL and libcurl backends:
   - OpenSSL path implements base64 encoding directly
   - Libcurl leverages native OAuth2.0 support via `CURLOPT_XOAUTH2_BEARER`
   - Comprehensive documentation covering Gmail and Outlook use cases
3. RFC 4616-compliant PLAIN authentication method for OpenSSL users
4. Comprehensive CRAM-MD5 memory fixes including all error paths
5. `--folder` CLI option for runtime override of `imap.folder` config
6. New `--list` option showing available IMAP folders (replacing v7's `imap.usecurl` config)

The OAuth2.0 implementation adds new authentication handlers (`auth_oauthbearer` and `auth_xoauth2`) with:
- New `AUTH_OAUTHBEARER` and `AUTH_XOAUTH2` capability flags
- Base64 encoding utilities for both protocols
- Conditional password handling in curl setup to avoid double-authentication
- Updated config documentation showing new authmethod options
- Detailed examples for Gmail and Outlook configurations

Server folder discovery capability works across both OpenSSL and libcurl backends:
- OpenSSL implements RFC6154 LIST command
- Libcurl follows curl documentation examples
- Output displays server-specific folder hierarchies and flags
- Helps users navigate different server conventions

User experience improvements remain from v7:
- Standardized message formatting (22 strings touched)
- Credential helper shows host:port (matching `git send-email`)
- Progress output displays destination folder name
- Documentation covers all authentication methods and options

Technical details:
- Configuration parsing correctly handles all IMAP settings with proper cleanup
- OAuth2.0 maintains RFC compliance via curl with OpenSSL fallback
- PLAIN authentication implements RFC 4616 with proper encoding
- CRAM-MD5 fixes now fully comprehensive
- New `list_folders` static variable controls `--list` behavior
- Both backends implement folder listing with protocol-specific approaches
- 431 lines changed across 3 files in v8 (plus 4-line config fix)

Review history shows thorough iteration with feedback from Jeff King, Eric Sunshine, Brian Carlson, Junio Hamano, and Ben Knoble. Phillip Wood's concerns about dual-backend maintenance remain open, with v8 changes shifting focus from configuration (`imap.usecurl`) to practical utility (`--list` and OAuth2.0 support). The series now addresses both technical correctness (including the critical config fix) and user experience while maintaining both OpenSSL and libcurl support, with new features providing immediate value for users dealing with different IMAP server conventions and modern authentication requirements.