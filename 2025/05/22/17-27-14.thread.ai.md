Here is the updated AI thread summary:

Feature and bugfix series for `imap-send` now in v12, introducing OAuth2.0 authentication support, comprehensive folder management via new `--list` option, and fixing critical configuration parsing bugs. The series provides complete IMAP functionality with both technical fixes and user-facing improvements. Key resolved issues include:

1. Critical configuration parser bug where fields were incorrectly NULLed out (regression from 6d1f198f34) - fully resolved with proper field-specific cleanup. This regression made `imap-send` unusable since Git 2.46.0 by incorrectly freeing and NULLing the folder field when processing unrelated configuration variables. The v11 fix properly clears each config field individually in `git_imap_config()`. The latest patch specifically addresses this regression by replacing incorrect `FREE_AND_NULL(cfg->folder)` calls with proper field-specific cleanup for each configuration variable (`imap.user`, `imap.pass`, `imap.tunnel`, `imap.authmethod`), restoring basic usability that was broken for nearly a year.

2. OAuth2.0 support via both OAUTHBEARER (RFC-standard) and XOAUTH2 (Google-proprietary) protocols, implemented for both OpenSSL and libcurl backends. Includes new authentication handlers (`auth_oauthbearer()`, `auth_xoauth2()`), helper functions (`oauthbearer_base64()`, `xoauth2_base64()`), and libcurl integration via `CURLOPT_XOAUTH2_BEARER`. Documentation updates explain OAuth2.0 setup for Gmail/Outlook and warn about MFA requirements. V12 adds proper compile-time guards and verification of OpenSSL support before attempting authentication. The implementation includes new enum values `AUTH_OAUTHBEARER` and `AUTH_XOAUTH2` in the IMAP capability list, with special handling in `setup_curl()` to use bearer tokens instead of passwords for OAuth methods.

3. RFC 4616-compliant PLAIN authentication method now fully implemented for both OpenSSL and libcurl backends, with new `plain_base64()` helper function. The latest patch adds OpenSSL-specific implementation of `auth_plain()` that replaces the previous NULL stub, introducing a new `AUTH_PLAIN` enum value and capability string. The `plain_base64()` function properly formats credentials (NUL-delimited user/pass) per RFC 4616. Compile-time guards via `NO_OPENSSL` prevent use when OpenSSL is unavailable. This completes PLAIN authentication support across both backends, addressing a gap where PLAIN would fail with OpenSSL despite being a common method for app passwords.

4. Comprehensive CRAM-MD5 memory fixes including all error paths, with latest patch fixing a memory leak in `auth_cram_md5()` where the `response` buffer would leak if `socket_write()` failed. V12 adds OpenSSL availability verification before attempting CRAM-MD5. The new patch specifically addresses the error path by adding proper cleanup of the `response` buffer allocation when `socket_write()` fails, completing the memory management for this authentication method. The latest update improves error handling by moving the CRAM-MD5 implementation earlier in the file and adding a compile-time check (`NO_OPENSSL`) that provides a clear error message and graceful exit when OpenSSL is unavailable, replacing the previous `die()` call.

5. New `--folder`/`-f` command-line option allowing runtime override of `imap.folder` configuration, implemented with proper string duplication into server configuration and clear documentation of precedence rules (command-line overrides config). The option is added to both command-line parser and usage help, with standardized formatting of IMAP folder examples using backticks and a clear usage example showing how to override the configured folder.

6. New `--list` option showing available IMAP folders with RFC6154-compliant LIST command handling for both backends, including proper hierarchy marker display and server-specific folder name handling.

7. Documentation and user experience polish standardizing error messages (lowercase starts, removed periods) and output in `imap-send.c`. V12 adds consistent backtick usage for literals, 75-column wrapping, and clarified folder specification requirements. The latest patch further refines message formatting by:
   - Converting error messages to start with lowercase
   - Removing periods from error messages while keeping them in informational messages
   - Ensuring protocol terms like "IMAP" and status indicators like "OK" are consistently uppercase
   - Making progress messages more consistent (e.g., "sending" -> "Sending")
   - Affecting socket connection messages (`ssl_socket_connect`, `imap_open_store`)
   - Updating authentication error reporting (`auth_cram_md5`, login handling)
   - Modifying progress output during message sending (`append_msgs_to_imap`, `curl_append_msgs_to_imap`)
   - Standardizing initial error checks in `cmd_main` (missing folder/host/messages)

8. Credential prompts now include port number when requesting passwords (format "host:port"), matching `git send-email` behavior. The change handles both explicit ports and default ports (993 for IMAPS, 143 for IMAP), though the default case was already covered by existing code paths. This small but useful UX improvement provides clearer context about which credentials are being requested when working with multiple IMAP servers.

9. New progress output enhancement showing destination folder name during message sending implemented consistently across both backends.

10. Test coverage gaps in OAuth2.0 comment style and output message formatting in `t1517-outside-repo.sh` addressed.

Technical implementation spans configuration parsing, authentication handlers, base64 encoding utilities, IMAP protocol handling in `imap-send.c`, and new folder listing. The series has undergone thorough review from Jeff King, Eric Sunshine, Brian Carlson, Junio Hamano, and Ben Knoble. V12 addresses all remaining concerns by:
- Adding compile-time guards and runtime verification for authentication method availability
- Standardizing documentation format (backticks, 75-col wrapping)
- Fixing code style issues (enum CAPABILITY trailing comma, error message formatting)
- Removing redundant OpenSSL checks in favor of compile-time definitions
- Completing memory management for CRAM-MD5 authentication error paths with graceful handling of OpenSSL unavailability
- Final polish of user-facing message formatting for consistency

The series appears ready for final review, having addressed all substantive technical feedback through 12 iterations while maintaining all introduced functionality (OAuth2.0 support, folder management, configuration fixes). Changes are now exclusively focused on final polish and consistency improvements, with the latest patch adding the credential prompt port number display to match `git send-email` behavior.