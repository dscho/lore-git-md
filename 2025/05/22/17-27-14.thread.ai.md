Here is the updated AI thread summary:

Feature and bugfix series for `imap-send` now at v18, rebased on master. The series introduces OAuth2.0 authentication support, comprehensive folder management, and critical configuration parsing fixes. Key elements:

1. Critical configuration parser regression (since Git 2.46.0) where fields were incorrectly NULLed out (6d1f198f34) - fixed with proper field-specific cleanup in `git_imap_config()`. The bug made `git-imap-send` unusable when processing unrelated configuration options (`imap.user`, `imap.pass`, `imap.tunnel`, `imap.authmethod`) which incorrectly NULLed out `cfg->folder`. The fix properly NULLs out the correct struct fields for each configuration option rather than incorrectly clearing `cfg->folder` in all cases.

2. OAuth2.0 support via OAUTHBEARER (RFC-standard) and XOAUTH2 (Google-proprietary) protocols, implemented for both OpenSSL and libcurl backends with helper functions `oauthbearer_base64()` and `xoauth2_base64()`. Includes RFC5801/RFC7628 compliance for OAUTHBEARER and proper Google XOAUTH2 formatting. Patch 4/10 adds `AUTH_OAUTHBEARER` and `AUTH_XOAUTH2` capability flags, auth method dispatch in `imap_open_store()`, and credential handling in `setup_curl()`. The implementation handles both backends, with libcurl using `CURLOPT_XOAUTH2_BEARER` for both mechanisms while maintaining backward compatibility.

3. RFC 4616-compliant PLAIN authentication now fully implemented for both OpenSSL and libcurl backends via new `plain_base64()` helper, implementing proper NUL-delimited "user\0pass" format before base64 encoding. Adds OpenSSL backend support with new `AUTH_PLAIN` capability flag. The implementation includes careful memory management with `xmallocz` allocations and proper cleanup paths, completing parity between backends for services that provide app passwords using PLAIN auth.

4. CRAM-MD5 improvements including:
   - Complete error paths with additional leak fix when socket write fails
   - New `try_auth_method()` helper for centralized authentication failure handling
   - Runtime OpenSSL availability check (replacing compile-time `die()`)
   - Fixed error message to reference `srvc->auth_method` instead of `srvc->host`
   - Plugged memory leak in `auth_cram_md5()` where `response` buffer would not be freed if `socket_write()` failed

5. New `--list` option implementing RFC6154-standard IMAP LIST command for discovering available folders and their server-specific naming conventions. Handles both backends:
   - OpenSSL: Direct IMAP protocol handling via `list_imap_folders()` issuing `LIST "" "*"`
   - libcurl: Uses `curl_easy_perform` patterns in `curl_list_imap_folders()`
   Shows folder hierarchies and attributes (e.g., `\Drafts`, `\Trash` flags) while reusing existing connection/auth infrastructure. Adds `list_folders` static variable and modifies `buffer_gets()` to show LIST responses in list mode.

6. New `--folder` command-line override with proper memory management (freeing previous value via `free(server.folder)` before `xstrdup` of new value) and documented precedence rules (CLI overrides config but config remains fallback). The implementation adds an `opt_folder` static variable, extends the option parsing table, and applies the override before connecting to the server. Documentation updates clarify the precedence rules and fix minor formatting inconsistencies (switching quotes to backticks for command/field names).

7. User experience improvements including:
   - Destination folder display during send progress
   - Port numbers in credential requests (host:port format via `xstrfmt` in `server_fill_credential()`), now refined to only show non-default ports (993 for IMAPS, 143 for IMAP)
   - Standardized error message formatting throughout `imap-send.c`

The v18 rebase incorporates final fixes from Junio Hamano's review:
- Removes unnecessary variable initializations
- Fixes CRAM-MD5 error message to show auth method rather than host
- Cleans up port display logic in credential prompts
- Adds missing `free(response)` in CRAM-MD5 error path when socket write fails
- Moves OpenSSL availability check for CRAM-MD5 from compile-time to runtime

Technical implementation spans configuration parsing, authentication handlers, base64 utilities, IMAP protocol handling, and folder management. The series has undergone thorough review from Jeff King, Eric Sunshine, Brian Carlson, Junio Hamano, and Ben Knoble. All technical issues are resolved including the configuration parser regression that made `git-imap-send` unusable since Git 2.46.0 and the newly fixed CRAM-MD5 memory leak. The series is now ready for integration with all review feedback addressed, including the comprehensive OAuth2.0 support that handles both RFC-standard OAUTHBEARER and Google-proprietary XOAUTH2 mechanisms across both OpenSSL and libcurl backends, plus the newly completed PLAIN authentication support for OpenSSL and the folder listing capability.