Here is the updated AI thread summary:

Feature and bugfix series for `imap-send` now in v6, completing all technical requirements while adding user-facing conveniences. The series resolves: (1) critical configuration parser bug where `cfg->folder` was incorrectly NULLed out (regression from 6d1f198f34) - now fully fixed with proper field-specific cleanup in all affected locations, (2) OAuth2.0 support via curl's native API (OAUTHBEARER/XOAUTH2) with OpenSSL fallback paths, (3) new RFC 4616-compliant PLAIN authentication method for OpenSSL users (now matching curl implementation with new `plain_base64()` helper and `AUTH_PLAIN` capability flag, completing authentication parity), (4) comprehensive CRAM-MD5 memory leak fixes including all error paths (now complete with final socket write failure case addressed in v5 patch), (5) new `--folder` CLI option providing runtime override of `imap.folder` configuration (with v6 documentation fixes for argument syntax), (6) `imap.preferredbackend` configuration for curl/OpenSSL selection, and (7) new `imap.usecurl` configuration for persistent backend preference.

Key technical details:
- Configuration parsing handles all IMAP settings (`imap.folder`, `imap.user`, `imap.pass`, `imap.tunnel`, `imap.authmethod`, `imap.preferredbackend`, `imap.usecurl`) with proper cleanup
- Fixes regression from 6d1f198f34 where `FREE_AND_NULL(cfg->folder)` incorrectly cleared folder field when processing other settings - now uses field-specific cleanup (`cfg->user`, `cfg->pass`, `cfg->tunnel`, `cfg->auth_method`) in all affected locations
- OAuth2.0 maintains RFC 5801/7628 compliance via curl's `CURLOPT_XOAUTH2_BEARER` with OpenSSL fallback (`oauthbearer_base64`/`xoauth2_base64` helpers), adding new capability flags `AUTH_OAUTHBEARER` and `AUTH_XOAUTH2` to IMAP protocol handling
- PLAIN authentication (`auth_plain()`) implements RFC 4616 via new `plain_base64()` helper, completing authentication parity between curl and OpenSSL backends with new `AUTH_PLAIN` capability flag
- CRAM-MD5 fixes now fully comprehensive including socket write failure paths (final fix in v5 ensures `response` buffer freed when `socket_write()` fails)
- New `--folder` (`-f`) option overrides `imap.folder` config with proper memory management (frees old string before assignment), now documented with correct `--folder=<folder>` syntax in v6
- New `imap.preferredbackend` and `imap.usecurl` configurations select curl/OpenSSL with fallback logic and persistent preference
- Documentation covers all authentication methods (including new PLAIN and OAuth2.0 support) and new options with clear precedence rules (CLI over config)
- Final implementation maintains build-time constraint handling (`USE_CURL_FOR_IMAP_SEND`/`NO_OPENSSL`)

Review history shows thorough iteration: Jeff King diagnosed configuration bug (now fully fixed with field-specific cleanup in all locations), Eric Sunshine identified memory issues, Brian Carlson drove architectural pivot to curl-native OAuth2.0. Junio Hamano reviewed config fix. Ben Knoble identified final use-after-free in CRAM-MD5 (fixed in v4) and confirmed completeness. V5 added user conveniences while maintaining all prior fixes. V6 addresses final man page formatting for `--folder` option syntax and completes authentication parity with PLAIN support. Eric Sunshine's suggestion to include `git range-diff` output remains the only pending process item before merge.

Participants: Aditya Garg (author) incorporated feedback from Sunshine, King, Hamano, Carlson, and Knoble. The series satisfies legal constraints (curl-native OAuth2.0) and technical requirements, supporting all authentication methods (now including RFC 4616 PLAIN for OpenSSL and comprehensive OAuth2.0 support) with complete memory management. Configuration fix remains critical for usability (now fully resolved with proper field-specific cleanup). With comprehensive authentication support and new user options (including runtime folder override via `--folder` and persistent backend selection via `imap.usecurl`), the series is merge-ready pending final presentation polish with range-diff.

The OAuth2.0 implementation provides both OAUTHBEARER (RFC 5801/7628) and XOAUTH2 (Google) mechanisms, with OpenSSL fallback when curl unavailable. Documentation includes provider-specific guidance for Gmail (app passwords/OAuth2.0 tokens) and Outlook (XOAUTH2). Curl integration uses `CURLOPT_XOAUTH2_BEARER` for automatic mechanism selection. Error handling covers OpenSSL encoding and socket write failures. New capabilities `AUTH_OAUTHBEARER`, `AUTH_XOAUTH2`, and now `AUTH_PLAIN` added to IMAP protocol handling while maintaining backward compatibility. PLAIN authentication completes the method coverage with RFC 4616 implementation for OpenSSL users matching curl's existing support. CRAM-MD5 memory management is fully robust across all error paths. The `--folder` option follows established CLI patterns with v6 documentation fixes for argument syntax (`--folder=<folder>`). Final `imap.usecurl` configuration provides persistent backend preference to complement CLI flags.