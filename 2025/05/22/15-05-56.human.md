This is a test patch in a series addressing memory leaks in pack-bitmap.c, specifically focused on validating the fixes for corruption handling. The patch adds a test that intentionally corrupts a bitmap's XOR offset field to verify the code handles malformed data without leaking memory.

Lidong Yan responds to Taylor Blau's concerns about the test's necessity, explaining that the primary goal isn't to test fallback behavior (which is already covered elsewhere) but to validate that the leak fixes work under ASAN. The test would catch memory leaks when run with SANITIZE_LEAK enabled, which the existing tests don't explicitly cover for this corruption path.

The discussion reveals two perspectives: Taylor questions whether the test adds value beyond the suite's existing leak checking, while Lidong maintains it specifically targets the leak scenarios fixed in earlier patches. Lidong also shares an alternative implementation approach using a test helper in pack-bitmap.c that would more directly tied to the actual bitmap loading code, though this appears more invasive.

The thread shows careful consideration of test design tradeoffs - the current test's fragility in parsing bitmap files versus the value of targeted leak validation. The core leak fixes (patches 1-2) remain uncontested, with this final test patch being the only point still under discussion. Lidong indicates willingness to drop the patch if reviewers deem it unnecessary.