This is a style nit in the ongoing stash import/export feature series. Junio Hamano points out unnecessary braces around a single-statement `return -1` in the newly introduced `parse_stash_revision` helper function in `builtin/stash.c`. The braces were likely added by the author (brian m. carlson) out of caution but aren't needed for such a simple conditional block.

The comment is purely about code style and doesn't affect functionality - the patch is otherwise ready as part of the v6 series implementing bidirectional stash transfer via `refs/stash-export/`. Junio also asks about an apparent typo ("pwodd") in the email headers, though this appears unrelated to the technical content.

This is a minor cleanup suggestion on an otherwise mature patch series that has already incorporated extensive review feedback. The series is in its final stages, with only Phillip Wood's sign-off pending after resolution of a separate `get_oid` error handling discussion. The braces removal would be a trivial follow-up change to the existing patch.