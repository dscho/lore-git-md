This is a follow-up in the ongoing discussion about a Perl compatibility fix for git-cvsserver. Jeff King (Peff) confirms Junio's suspicion that the `escapeRefName` function being patched appears to be dead code - adding a `die` statement to it doesn't cause any test failures, suggesting it's never called. 

The key insight is that the original issue (Perl 5.41.4+ warnings causing test failures) stems from Perl parsing the file, not from actually executing the problematic code. This reinforces Junio's earlier question about whether we should simply remove this unused function rather than fixing it.

While the technical fix using the `!~` operator is sound and ready to go, this email shifts focus to the larger maintenance question: git-cvsserver may contain unused code that's only being noticed now because of new Perl warnings. The thread is now considering whether this is an opportunity to clean up potentially dead code rather than just fixing the immediate warning.

For readers following this thread, the immediate Perl compatibility fix is settled, but there's now an open question about the broader maintenance status of git-cvsserver and whether more substantial cleanup might be warranted.