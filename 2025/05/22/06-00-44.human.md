Follow-up discussion about potential `strmap` API improvements in the batched reference updates series.

Jeff King responds to Karthik Nayak's suggestion about modifying the `strmap` API to handle `const void*` pointers, arguing against the change despite its appeal for eliminating typecasts. He explains the type safety tradeoffs in both directions - either approach would require some form of casting and could mask important const-correctness issues. 

King proposes an alternative design involving runtime flag checks and separate const/non-const accessor functions, but acknowledges it would be cumbersome compared to compile-time solutions available in other languages. This is marked as a non-blocking discussion point (#leftoverbits) that doesn't affect the already-approved performance optimization series.

The exchange shows careful consideration of API design constraints in C while maintaining the thread's focus on the core batched reference updates functionality. No changes to the patches are needed - this is purely a forward-looking discussion about potential future improvements to Git's internal APIs.