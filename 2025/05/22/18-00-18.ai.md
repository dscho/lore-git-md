Bugfix for `imap-send` configuration parsing, with additional leak analysis. This is reviewing Aditya Garg's patch that fixes incorrect NULL assignments in `git_imap_config()` where four IMAP configuration options (`imap.user`, `imap.pass`, `imap.tunnel`, `imap.authmethod`) were mistakenly clearing `cfg->folder` instead of their intended target fields. The review confirms the fix's correctness but notes two additional potential memory leak issues not addressed by this patch.

Eric Sunshine suggests the commit message should reference commit 6d1f198f34 ("imap-send: fix leaking memory in `imap_server_conf`") as the source of these copy/paste bugs. The review also flags a possible missed leak of `cfg->host` in the same function and raises questions about leaks in `server_fill_credential()`, though explicitly states these should be handled separately from the current bugfix.

The exchange shows careful review of both the immediate fix and related code quality issues, with clear boundaries drawn about what belongs in this patch versus future work. The original bug made `imap-send` unusable when certain configurations were set, and this fix directly addresses that while leaving room for follow-up leak investigations.