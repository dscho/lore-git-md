This is version 4 of a bugfix series addressing memory leaks in pack-bitmap.c's bitmap loading paths. The series has evolved through previous versions with review feedback incorporated, and this iteration appears to be primarily focused on commit message refinements rather than technical changes.

The series contains two patches:
1. Taylor Blau's fix for a memory leak when `load_bitmap()` fails, originally fixing `load_bitmap_entries_v1` but now generalized in the commit message to cover the broader `load_bitmap()` case. The patch removes redundant cleanup code by relying on callers' `free_bitmap_index()` for uniform cleanup.

2. Lidong Yan's test case that uses clang`'s leak sanitizer to verify proper handling when loading corrupt bitmaps. This builds on the test infrastructure added in v3 that allows targeted corruption of specific bitmap fields.

The range-diff shows minimal changes from v3 to v4 - just commit message updates to better reflect the scope of the first patch. The technical approach remains consistent with previous versions that received positive reviews from Jeff King and Junio C Hamano, focusing on centralized cleanup via `free_bitmap_index()` and robust test coverage.

Key technical details for future reference:
- The fix centers around removing error-path-specific cleanup in favor of relying on `free_bitmap_index()`
- Test infrastructure includes `test-tool bitmap` helper for structured corruption testing
- Leak detection uses clang sanitizer in t5310 test
- Changes affect pack-bitmap.c's bitmap loading paths and associated test files

The series appears ready for inclusion, having addressed all previous feedback and maintained the technical improvements from earlier versions while refining the commit messages. No new technical concerns are evident in this version.