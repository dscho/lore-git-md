Documentation patch converting the `git-merge` manpage to new synopsis style format, part of the ongoing documentation standardization series. This is patch 2/9 in the series, following the same mechanical conversion pattern applied to `git-checkout.adoc` in patch 1/9.

The changes focus on formatting consistency without altering content:
- Converts SYNOPSIS section to AsciiDoc synopsis block for automatic styling
- Standardizes placeholder notation to `_<placeholder>_` format
- Uses backticks` for command/option references throughout
- Special handling for conflict markers (`<<<<<<<`, `>>>>>>>`) using `+` signs to prevent formatting issues
- Updates configuration section to match new style (`branch.<name>.mergeOptions`)

Key technical details:
- Modifies only `git-merge.adoc` (25 insertions, 26 deletions)
- Preserves all existing content while updating markup
- Follows same pattern as previous documentation conversions
- Handles edge cases like conflict markers that could break formatting
- Updates references to use consistent backtick notation (e.g. `ort` strategy, `merge.conflictStyle` values)

The patch maintains the series' mechanical nature, applying established documentation conventions without introducing functional changes. It continues the systematic update of command documentation to the new standard format.