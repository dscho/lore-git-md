This is a refactoring patch targeting the pack-bitmap code. The change removes a conditional check before freeing a bitmap in `find_boundary_objects()`, making the cleanup unconditional. The author suggests this could help static analysis tools and potentially offer performance benefits, though no specific measurements are provided.

The patch modifies the bitmap cascade logic in pack-bitmap.c, where `roots_bitmap` is currently only freed if `cascade_pseudo_merges_1()` fails. The new version always frees the bitmap after the cascade operation. The commit message indicates this change might improve static analysis, implying the previous conditional free could mask potential issues.

The change is small (4 lines modified) and focused on a single function. While the commit mentions potential performance benefits, the primary motivation appears to be code hygiene and tooling support. The patch doesn't include tests, but the change is straightforward enough that it likely doesn't require new test coverage. The modification touches core bitmap handling code, so reviewers will want to verify it doesn't introduce memory leaks or other resource management issues.