This thread discusses a small but potentially impactful change to the pack-bitmap code. The patch refactors `find_boundary_objects()` to make bitmap cleanup unconditional rather than conditional on the success of `cascade_pseudo_merges_1()`. The author suggests this could help static analysis tools and might offer performance benefits, though concrete measurements aren't provided.

The change is focused and touches only four lines in pack-bitmap.c, modifying how the `roots_bitmap` is freed after the cascade operation. While the patch doesn't include new tests, the modification appears straightforward and unlikely to require additional coverage. Reviewers will want to verify this doesn't introduce any memory management issues in the bitmap handling code, which is performance-sensitive infrastructure. The discussion may focus on whether the unconditional free is truly safe and whether the potential benefits justify the change.