Lidong Yan's series fixing memory leaks in pack-bitmap.c's boundary traversal has now been fully merged after several rounds of refinement. The core issue involved `roots_bitmap` cleanup in `find_boundary_objects()`, where the bitmap could leak if `cascade_pseudo_merges_1()` succeeded. The solution established a pattern of unconditional `bitmap_free()` calls, validated by ASAN-enabled tests in t5333 that demonstrate both failure and success path coverage.

The final follow-up addressed one last edge case where the bitmap could still leak on success paths, adding a specific test scenario with two commits and pseudo-merge traversal. Junio Hamano merged the series after Lidong incorporated feedback from Patrick Steinhardt on the implementation and Eric Sunshine on test portability, with Taylor Blau contributing test refinements. The commit messages received careful wording polish, particularly around ownership semantics of the bitmap parameter. This concludes the series, leaving no known leaks in the boundary traversal code while establishing a reusable approach for ASAN-based memory validation in Git's test suite.