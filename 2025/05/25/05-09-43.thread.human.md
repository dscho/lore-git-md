Lidong Yan's series fixing memory leaks in pack-bitmap.c's boundary traversal logic has now been fully resolved with this final edge case fix. The original implementation in commit 11d45a6e addressed the core issue of `roots_bitmap` cleanup but only handled failure paths, leaving a leak when `cascade_pseudo_merges_1()` succeeded. This follow-up makes the `bitmap_free(roots_bitmap)` call unconditional, closing the last remaining gap.

The series benefited from thorough review by Patrick Steinhardt and Taylor Blau, with Eric Sunshine ensuring shell portability in the tests. Junio Hamano merged the main series and provided final wording improvements to the commit message. The discussion revealed how the original test coverage in t5333 missed this boundary traversal scenario, allowing the leak to persist even after similar cases were fixed. With this last patch applied, the memory management is now robust across all execution paths, and the series serves as a model for ASAN-based leak detection in future tests.