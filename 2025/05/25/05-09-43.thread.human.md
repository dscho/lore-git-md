This thread addresses a memory leak in Git's pack-bitmap subsystem, specifically in the boundary traversal logic for pseudo-merges. The original patch series (v2) from Lidong Yan fixed an issue in `find_boundary_objects()` where `roots_bitmap` could leak memory in certain error cases. The first patch made the cleanup unconditional, while the second patch (just received) adds test coverage to verify the fix.

Patrick Steinhardt provided key review feedback that shaped the series, particularly emphasizing the need for both the fix and test coverage. The test case carefully sets up a repository with two commits and configures pseudo-merge settings to exercise the boundary traversal path where the leak would have occurred. While it doesn't directly detect memory leaks (which would require instrumentation), it serves as an effective regression test by verifying the bitmap count operation works correctly on the previously problematic code path.

The series is now complete, with both the technical fix and verification in place. The changes are minimal (just 4 lines modified in the core fix) but important for correctness in bitmap operations. This continues ongoing optimizations in the pack-bitmap subsystem, demonstrating good responsiveness to review and a thorough approach to fixing memory management issues.