Documentation patch continuing the manpage conversion series to new synopsis format. This is 5/9 in the series, converting `merge-strategies.adoc` to use consistent AsciiDoc markup for command/option names and placeholders.

The patch standardizes formatting across all merge strategy documentation, including:
- Converting strategy names (`ort`, `recursive`, `octopus`, etc.) and their options (`ours`, `theirs`, `find-renames`, etc.) to backtick-wrapped monospace
- Using `_<placeholder>_` syntax for variables in option descriptions
- Maintaining the same technical content while improving presentation
- Preserving all existing warnings about strategy differences (e.g., distinguishing `-Xours` from `ours` strategy)

Changes are mechanical but thorough, touching every strategy and option in the file (58 lines modified). The patch follows the same pattern as previous conversions in the series, applying the new documentation style guide consistently. No functional changes are made - this purely affects rendering.

Key technical details:
- File modified: `Documentation/merge-strategies.adoc`
- All merge strategies updated: `ort`, `recursive`, `resolve`, `octopus`, `ours`, `subtree`
- All strategy options updated including whitespace handling, rename detection, and diff algorithm controls
- Special notes preserved about deprecated options (`recursive` as `ort` synonym, `rename-threshold` vs `find-renames`)
- Warnings maintained about confusing cases (difference between `ours` strategy and `-Xours` option)

This continues the systematic documentation conversion work, with the remaining patches in the series expected to cover `merge.adoc`, `mergetool.adoc`, checkout config references, and other related files.