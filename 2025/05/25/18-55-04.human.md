This is the final patch in a feature series adding configuration and authentication improvements to `imap-send`. It introduces a new `imap.usecurl` config option to let users set their preferred backend (libcurl vs OpenSSL) as a default, rather than only via command-line flags. The option respects the same build constraints as the existing `--curl`/`--no-curl` flags, with CLI arguments taking precedence over the config.

The patch is straightforward, adding the config parsing logic alongside similar IMAP settings and updating documentation. It completes the series' goal of providing comprehensive configuration options for IMAP operations, following earlier patches that fixed memory leaks, added OAuth2 support, and introduced other user-facing improvements. The change maintains consistency with existing code by using the same `use_curl` global variable that the command-line flags modify.

As the sixth patch in v5 of this well-reviewed series, it represents a polished addition that rounds out the feature set. The implementation is clean and matches the established patterns in the codebase, with proper documentation updates. This appears ready for inclusion, capping off a series that has addressed all identified technical and usability concerns.