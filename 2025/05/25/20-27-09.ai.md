Documentation patch completing a 9-part series standardizing manpage formatting. This final patch converts `git-switch.adoc` to the new synopsis style, bringing it in line with the other converted merge-related documentation from earlier in the series.

The changes follow the established pattern seen in previous patches: replacing verse blocks with synopsis blocks, standardizing placeholder markup as `_<placeholder>_`, wrapping commands and options in backticks, and maintaining consistent casing for special references like `HEAD`. The patch makes no functional changes to the documentation - it purely updates formatting while preserving all technical content about `git switch`'s behavior, options, and configuration.

Key formatting updates include converting all option descriptions to use backtick-wrapped names, standardizing the presentation of command syntax variants (create vs force-create branches, detached HEAD mode), and applying consistent markup to revision specifications like `@{-<N>}` and `<rev-a>...<rev-b>`. The patch also preserves all existing examples and cross-references to related commands like `git checkout` and `git branch`.

This completes the series' systematic conversion of merge-related documentation, having now covered command manpages (`git-checkout`, `git-merge`, `git-switch`, `git-mergetool`), option documentation, strategy explanations, and configuration references. The changes are mechanical but thorough, with the diff showing 114 lines changed (57 additions and 57 deletions) as the formatting is updated while keeping the technical content identical.