Documentation patch continuing the standardization of mergetool configuration docs. This is patch 8/9 in Jean-Noël Avila's series converting Git documentation to the new synopsis style format.

The patch focuses on mergetool configuration options, applying the same mechanical changes seen in earlier patches: converting `<placeholder>` to `_<placeholder>_` syntax and wrapping commands/variables in backticks. It touches both the main mergetool config documentation and the vimdiff-specific helper file, maintaining consistency with the series' approach.

Changes are purely formatting - no technical content is altered. The patch continues the systematic conversion of merge-related documentation that began in earlier patches, now covering the configuration side of `git mergetool`. Like previous patches, it standardizes environment variable references (BASE/LOCAL/REMOTE/MERGED) and option syntax while preserving all existing functionality descriptions.

The diff shows typical line-for-line replacements of old markup with the standardized format, with some minor wording clarifications (like changing "Git's" to "Git`s" to avoid syntax issues). The vimdiff section gets particular attention to normalize variant naming and layout examples. This patch appears consistent with the quality and approach of the series so far.