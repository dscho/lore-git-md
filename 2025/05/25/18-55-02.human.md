This is a feature patch adding a command-line option to `git imap-send`. It allows users to specify the target IMAP folder directly via a new `--folder` (or `-f`) option, overriding any `imap.folder` configuration setting. The change is part of a larger v5 series improving `imap-send` with authentication methods and configuration fixes.

The implementation is straightforward: a new `opt_folder` variable captures the command-line argument, which then replaces the configured folder during initialization. Documentation updates clearly explain the precedence (command line over config) and provide usage examples. The patch maintains consistency with existing style and error handling patterns.

This appears to be a well-considered user convenience feature, completing the series' goal of making `imap-send` more flexible. The change is small, focused, and properly documented, following the established pattern of other command-line options in the command. As the final patch in the technical portion of the series (before documentation), it rounds out the improvements with a practical workflow enhancement.