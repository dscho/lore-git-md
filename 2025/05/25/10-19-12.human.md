Bugfix for memory leak in sequencer's interactive rebase handling, now taking Phillip Wood's suggested approach. This is the second version of Lidong Yan's patch, switching from adding cleanup code to using a `BUG()` assertion for invalid command types in `update_squash_messages()`.

The patch changes how the sequencer handles invalid command types during squash message updates. Instead of gracefully returning an error (which could lead to memory leaks), it now triggers a fatal `BUG()` when receiving anything other than the expected TODO_SQUASH or TODO_FIXUP commands. This aligns with Phillip Wood's suggestion that such cases represent programming errors rather than runtime conditions that need graceful handling.

Yan has provided static analysis showing this makes the previously leaky path unreachable, addressing Junio Hamano's earlier concern about whether the assertion would be verifiable. The change is minimal - just one line modifying the error handling - but reflects an important design decision about how Git should handle internal consistency violations.

The discussion has narrowed to choosing between two valid approaches: Yan's original cleanup path versus this assertion-based solution. This version adopts the latter approach, which may be more maintainable long-term by treating invalid states as bugs rather than recoverable conditions. The patch appears well-justified and follows Git's conventions for similar cases elsewhere in the codebase.