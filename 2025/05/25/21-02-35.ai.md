Feature patch adding R language support to Git's diff highlighting.

This is the first of two patches adding userdiff patterns for the R programming language. The change modifies `userdiff.c` to recognize R function definitions (those using the `<- function` syntax) for improved diff highlighting. The pattern matches R's assignment operator syntax for functions while also accounting for whitespace variations.

The implementation follows the established pattern format used for other languages in this file, placing the new R definition between Python and Ruby patterns. The regex appears straightforward and correctly handles basic R function declaration syntax. The commit message is brief but clear about the purpose - this will help users who version R code with Git.

As the first in a two-patch series, we can expect a follow-up with additional R language patterns or related functionality. The change is small and focused, touching only the expected file with minimal additions. The patch appears ready for review, though reviewers may want to verify the regex handles edge cases in R's function declaration syntax.