This is a refactoring patch series focused on cleaning up the oidmap implementation. The series consists of three small changes:

1. Renames `oidmap_free()` to `oidmap_clear()` to better reflect its behavior (it clears but doesn't free the map itself)
2. Adds a new `oidmap_size()` function to query the number of entries
3. Removes a redundant pointer to `replace_map` in `raw_object_store`

The changes are mechanical and touch several files that use the oidmap functionality, including commit-graph handling, object filtering, and the sequencer. The patch series appears to be straightforward cleanup with no behavior changes - the commit messages don't mention any bugs being fixed or performance improvements. The changes are small and focused, with each patch doing one clear thing and updating all callers consistently. The series looks like routine maintenance work to improve code clarity and provide better introspection capabilities through the new size function.