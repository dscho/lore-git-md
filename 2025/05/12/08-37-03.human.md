This is a refactoring patch in the ongoing packed-refs optimization series, now addressing filesystem safety concerns raised by Patrick Steinhardt. The patch extracts buffer allocation logic from `load_contents()` into a new helper function `allocate_snapshot_buffer()` to enable reuse while maintaining platform-specific handling.

Patrick Steinhardt responds to the second patch in the series, noting he doesn't recall suggesting this specific change (despite being listed in Suggested-by) but agrees the refactoring looks sensible. The change moves 54 lines of buffer allocation logic while preserving Windows compatibility and error handling.

This appears to be a straightforward technical improvement that enables the subsequent mmap-based fsck verification in patch 3. The core optimization work remains uncontroversial, though the thread's main focus has shifted to safety concerns about empty file handling raised in patch 1. This middle patch seems likely to proceed as-is while the first patch undergoes further review.