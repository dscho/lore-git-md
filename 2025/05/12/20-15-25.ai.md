This is a follow-up discussion in the ongoing FQDN detection thread, specifically addressing platform compatibility for the `hostname` command flags. 

Junio Hamano points out that while Eric Sunshine correctly identified the broader BSD-lineage issue with `--fqdn` versus `-f` flags, the current patch implementation already limits its platform scope to just Linux and macOS (`darwin`), thus avoiding the BSD case entirely. His comment carries a lighthearted tone ("for unfathomable reason, so BSD's are safe already ;-)") while making the technical observation that the current patch's platform restrictions coincidentally prevent the BSD flag issue from being a concern.

The exchange demonstrates the maintainer's attention to platform-specific details and serves as a subtle reminder that the patch's platform limitations should either be expanded (to properly handle BSD systems with `-f`) or at least documented as intentional. This comes after earlier consensus had been reached about supporting BSD systems, making Junio's observation particularly relevant for ensuring the implementation aligns with the agreed approach.