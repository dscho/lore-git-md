This is a follow-up in the gitk SHA-256 support thread, addressing remaining code review items. Takashi Iwai responds to Johannes Sixt's observation that several procedures still contain hardcoded 40-character SHA-1 lengths that need conversion to use the new `hashlength` variable.

The email confirms five specific locations needing attention: the `longid` comment, and regex patterns in `shortids`, `setlink`, `mkpatch`, and `mktag` procedures. Iwai acknowledges these as legitimate issues to address, though notes they weren't initially caught because they relate to less commonly used gitk features.

This represents incremental progress on the SHA-256 compatibility work, with Iwai taking responsibility for these final conversions. The tone suggests these will be straightforward changes, though their discovery highlights the challenge of comprehensively updating all hash-length assumptions in the Tcl codebase. No new technical concerns or design decisions are raised here - it's purely about completing the mechanical conversion work identified in review.