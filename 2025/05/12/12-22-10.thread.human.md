This thread addresses memory leaks in pack-bitmap.c's bitmap loading paths, originally reported by Lidong Yan. The series has progressed through three versions, with the latest adding comprehensive test coverage for the fixes. The first patch (Lidong Yan) reordered validation checks to prevent leaks on corrupt bitmap entries, while the second (Taylor Blau) simplified error handling by removing redundant cleanup code in favor of centralized cleanup via `free_bitmap_index()`. Both fixes received positive reviews from Jeff King and Junio C Hamano, with Junio noting the rare case where a fix actually reduces code size.

The third and final patch, also from Lidong Yan, completes the series by adding targeted test cases that verify the fixes work as intended. The test deliberately corrupts bitmap files in specific ways - particularly the XOR offset field - to trigger both validation failures and partial-load scenarios while ensuring proper cleanup. The test implementation shows careful attention to the bitmap file format, using a helper function to navigate the structure and inject corruption at precise locations. This provides robust regression protection for the memory leak fixes while demonstrating their effectiveness under controlled failure conditions. With all key reviewers having approved the technical approach in previous iterations and the test coverage now complete, this series appears ready for merging.