This thread addresses memory leaks in pack-bitmap.c's bitmap loading paths, originally reported by Lidong Yan. The series has gone through four versions, with the latest refinements focusing on test infrastructure improvements. The first patch (Taylor Blau) simplifies error handling in `load_bitmap()` by removing redundant cleanup code, while the second patch (Lidong Yan) adds comprehensive test coverage using a new test helper for targeted corruption injection.

In the latest exchange, Lidong responds to Taylor's review feedback on the test infrastructure. The discussion centers around simplifying the position tracking mechanism - Lidong agrees to Taylor's suggestion to add a `map_pos` field directly to the `stored_bitmap` struct rather than using a separate conditional structure. Lidong also commits to making several test robustness improvements: avoiding pipes in output capture, using full object lists for verification, and sorting outputs for reliable comparison. The test helper will be renamed from `list-commits-offset` to `list-commit-offsets` for clarity.

Jeff King has reviewed both fixes across multiple iterations, confirming they properly address the validation-order leak and partial-load leak scenarios. Junio C Hamano has noted the rare case where a fix removes code rather than adding it, approving the reduction in redundant error handling. While technical consensus exists on the approach, Taylor has indicated the test infrastructure implementation still needs refinement before merging, particularly around position tracking simplification and test output verification. With Lidong now incorporating these changes, the series appears to be nearing readiness for inclusion.