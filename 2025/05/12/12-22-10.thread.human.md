This thread addresses memory leaks in Git's pack-bitmap handling, specifically when loading corrupt bitmaps. The series has gone through four iterations, with the current version refining commit messages while maintaining the technical approach established in v3.

Taylor Blau's first patch simplifies error handling in `load_bitmap()` by removing redundant cleanup code and relying on callers' `free_bitmap_index()` for uniform cleanup. The change eliminates ~17 lines of code while fixing a leak scenario where partial bitmap loading could orphan entries. Jeff King confirmed the safety of this approach, and Junio C Hamano noted the rare case where a fix removes code rather than adding it.

Lidong Yan's second patch adds comprehensive test coverage using a new `test-tool bitmap` helper that enables targeted corruption injection for leak detection. The test infrastructure includes position tracking and uses clang's leak sanitizer to verify proper handling of corrupt bitmaps.

Both patches have been thoroughly reviewed by Jeff King and Junio C Hamano across multiple iterations, with v4 addressing all feedback. The technical consensus favors centralized cleanup via `free_bitmap_index()` over per-error-path handling. The series appears complete with no outstanding objections, combining effective fixes with maintainable test coverage.