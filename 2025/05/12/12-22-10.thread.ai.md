Here is the dense, loss-free AI thread summary:

Bugfix for a memory leak in pack-bitmap.c's `load_bitmap_entries_v1()` when handling corrupt bitmaps. The issue occurs when validation checks (XOR offset and commit index bounds) fail after `read_bitmap_1()` allocation. Patch reorders operations to validate before allocating, preventing the error path leak. Change is minimal - moves 4 lines in pack-bitmap.c to perform all checks prior to bitmap reading. No new allocations or logic - just reordering existing validation. Fix targets error handling for corrupt bitmap cases, not normal operation. The commit-graph and bitmap subsystems are critical for performance in large repos, making even small leaks worth fixing. Patch appears ready with clear commit message and focused changeset. No review feedback yet - being a straightforward fix with limited scope, it may proceed directly to maint/master.