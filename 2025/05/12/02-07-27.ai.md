This is a bugfix patch addressing a memory leak in the `git am` command's StGit series handling. The patch fixes a case where `series_dir_buf` allocated by `xstrdup` would leak if `fopen` failed when processing a StGit series file. The fix is straightforward - adding a `free(series_dir_buf)` call in the error path before returning.

The change is minimal (3 lines inserted, 1 line modified) and clearly correct - it properly cleans up allocated memory in an error case that was previously missed. The commit message explains the issue concisely and the diff shows exactly where the fix is applied in `builtin/am.c`. This appears to be a well-scoped, uncontroversial fix for a real issue.

For future thread reference: The patch touches the StGit series handling code in `builtin/am.c`, specifically the `split_mail_stgit_series` function. The memory leak occurs when opening the series file fails, and the `series_dir_buf` string duplication isn't freed. The fix adds the missing cleanup while maintaining the same error message behavior. No configuration options or user-visible changes are involved - this is purely an internal memory management fix.