This is a refactoring patch in the ongoing packed-refs optimization series. Jeff King (peff) is reviewing the second patch from shejialuo's v3 series, which extracts buffer allocation logic from `load_contents()` into a new helper function `allocate_snapshot_buffer()`.

The change enables cleaner code reuse for fsck verification while maintaining all the existing mmap/read thresholds and Windows compatibility. Peff gives the patch a positive review, noting only a minor style nit about the `ret` variable that isn't worth a re-roll. The core refactoring approach has already been confirmed as technically sound by Patrick Steinhardt in earlier discussion.

This patch appears to be in good shape technically, with clear motivation (avoiding direct file access during fsck) and careful preservation of existing behavior. The review conversation is now focusing on small implementation details rather than fundamental design questions.