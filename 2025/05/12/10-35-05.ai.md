This is a refactoring patch in the ongoing packed-refs memory optimization series. The patch extracts buffer allocation logic from `load_contents()` into a new helper function `allocate_snapshot_buffer()` to enable reuse in fsck verification while maintaining all existing mmap/read thresholds and Windows compatibility.

The change addresses a specific need in the verification path where opening the packed-refs file directly isn't desired, while avoiding code duplication. Patrick Steinhardt confirms the technical soundness of the refactoring despite not recalling suggesting it specifically. The author credits both Jeff King and Steinhardt for their general guidance on how GitHub/GitLab handle similar cases.

Key technical details:
- 54 lines moved to new helper function
- Preserves MMAP_TEMPORARY handling for Windows
- Maintains all existing mmap vs read thresholds
- Enables clean mmap/verify/unmap pattern in fsck
- Part of broader packed-backend.c optimizations

This patch appears well-constructed and uncontroversial, having received positive feedback from a key reviewer (Steinhardt) while addressing a clear technical need in the verification path. It represents a stable intermediate step in the larger optimization effort.