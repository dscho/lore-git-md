Performance optimization in the packed-refs backend, specifically addressing memory usage during fsck operations.

Jeff King (Peff) reviews the third patch in a series that refactors packed-refs handling to use mmap for large files during fsck. The patch builds on previous work that extracted buffer allocation into a helper function (`allocate_snapshot_buffer()`), now applying this to the fsck path. The key change replaces direct file reading with mmap-based verification, maintaining the same buffer allocation thresholds and Windows compatibility established in the series.

Peff notes that while a simpler `xmmap()` approach could have worked without the refactoring in patch 2, he accepts the current direction as it may enable future code sharing between normal and fsck operations. This confirms the technical approach while acknowledging alternative implementations could exist. The review indicates this patch is functionally sound and ready for inclusion, completing the series' optimization goals for packed-refs handling.

The thread context shows this is the final piece of a reviewed optimization effort, with the buffer allocation refactoring (patch 2) already approved and only minor open questions remaining about empty file handling safety. This patch maintains all established thresholds and platform-specific behaviors while providing cleaner mmap/verify/unmap patterns for fsck operations.