This is a maintainer ack for a bugfix series addressing memory leaks in the reftable writer. Patrick Steinhardt approves Lidong Yan's v3 patches which fix two distinct error-path memory leaks in `reftable/writer.c` - one in `padded_write()` and another in `writer_index_hash()`. 

The series has gone through three iterations, with v3 splitting the fixes into atomic commits as requested by the maintainer. The first patch (addressing the `zeroed` buffer leak) remains unchanged from the previously approved v2 version, while the second patch now properly handles the `key` buffer leak in the index hash operation. 

Steinhardt's brief approval suggests the technical implementation meets maintainer standards. The fixes are minimal (6 lines added, 2 removed total) and maintain existing error handling flows while properly cleaning up resources. While the thread summary notes the lack of specific test coverage for these error cases, the maintainer appears satisfied with the current state for merging. 

This concludes the review cycle for these fixes, which are now likely to be queued for inclusion in an upcoming release. The series represents incremental improvements to the reftable backend's robustness during error conditions.