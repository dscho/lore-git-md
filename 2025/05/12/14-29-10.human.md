SHA-256 support follow-up in gitk. Takashi Iwai responds to Johannes Sixt's review of the first patch adding SHA-256 repository support to gitk. The discussion focuses on several technical points:

1. Defends keeping the regex range check as-is (4-63 chars) rather than using `$hashlength`, noting this was the originally suggested approach. 

2. Acknowledges the whitespace (space vs tab) formatting issue.

3. Clarifies the `$objformat` variable usage - it's needed for the error message and shouldn't be blindly uppercased, while the separate `$hashalgorithm` variable (uppercased) is used elsewhere in the UI.

The exchange shows careful attention to the review feedback while maintaining some original design choices. The technical discussion remains focused on implementation details rather than the broader SHA-256 support goal, suggesting the core approach is sound but needs polish. This continues the thread's pattern of methodically addressing gitk's SHA-256 compatibility while preserving existing behavior where possible.