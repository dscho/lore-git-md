This is a documentation and test modernization patch in the ongoing `git whatchanged` deprecation effort. Elijah Newren provides a brief review of Junio Hamano's patch that updates fast-import tests to use `git log --raw` instead of the deprecated `git whatchanged`.

The patch itself (which was sent earlier in the thread) updates several fast-import test scripts to replace `whatchanged` calls with their `log --raw` equivalents. Newren's review is positive, suggesting only minor wording improvements to the commit message ("invoked" instead of "placed") and noting the already-corrected "developped" typo.

This is part of the larger `whatchanged` deprecation track that has been progressing through the mailing list. The test changes are preparatory work for when `whatchanged` is eventually removed, ensuring the test suite won't break. Newren's review indicates the changes are technically sound and the approach is correct, with only minor editorial suggestions.