This is a platform compatibility patch in the ongoing `git send-email` FQDN detection thread. Eric Sunshine provides additional context about BSD-derived systems' `hostname` command behavior, noting that the `-f` flag (rather than `--fqdn`) is the more widely supported option across BSD-lineage systems beyond just macOS. This reinforces the earlier decision to use platform-specific flags (`--fqdn` for Linux, `-f` for BSD/macOS) while acknowledging the Alpine Linux limitation.

The email confirms the technical basis for the hybrid approach already under discussion, where the validation safety net from `valid_fqdn()` provides protection against malformed output from platform-specific `hostname` commands. Eric's observation about BSD systems broadens the justification beyond just macOS, suggesting the platform-specific solution will cover more real-world cases than initially considered.

This appears to be the final piece of technical confirmation needed before finalizing the FQDN detection implementation, as it addresses the maintainer's earlier concern about `-f` flag reliability by showing its broader standardization across BSD-derived systems. The thread has effectively converged on the v4 approach with platform-specific commands and validation fallbacks.