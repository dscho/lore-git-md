Bugfix for `git stash` branch display in submodule contexts. This is a follow-up to the earlier discussion about incorrect branch names appearing in stash messages when working with submodules.

K Jayatheerth confirms the proposed fix works as intended by providing a detailed test case showing correct behavior. The reproduction steps demonstrate that after applying the patch, stashing changes in the superproject's `bugfix_bar` branch properly displays that branch name in the stash message, rather than incorrectly showing the submodule's `feature_sdk_foo` branch.

The email serves as verification that the technical solution (introducing `xstrdup()` to preserve the branch name) resolves the original issue without introducing new problems. While the patch still needs formal review and merging, this confirmation from another developer helps validate the fix's effectiveness.

The test case follows the same structure as the original bug report but shows the corrected output, providing concrete evidence that the buffer reuse problem in `builtin/stash.c` has been properly addressed. This moves the thread closer to resolution, with the next steps likely being formal code review and integration into mainline Git.