This is a refactoring patch that's part of Git's deprecation infrastructure work, specifically focusing on the `pack-redundant` command. It extracts the deprecation warning logic into a reusable helper function called `you_still_use_that()`.

The patch takes the existing deprecation warning from `pack-redundant` - which requires users to pass `--i-still-use-this` to run the command - and moves it to a shared location in `usage.c`. This makes the warning message consistent and easier to reuse for other commands being deprecated. The change includes adding the new function declaration to `git-compat-util.h` and updating `pack-redundant.c` to use it.

A notable addition is a new test case that verifies the deprecation enforcement works as expected for `pack-redundant`. The patch maintains the same warning text and behavior while making the implementation more maintainable.

This appears to be a clean, focused change that follows the deprecation approach established in the thread - keeping the same user experience while improving the code structure. It's part of a larger effort to systematically deprecate commands like `pack-redundant` and `whatchanged`, with this particular change helping to standardize the warning mechanism.