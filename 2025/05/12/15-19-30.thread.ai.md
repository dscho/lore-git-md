Bug report and fix for `git stash list` branch display corruption in submodule contexts. Stuart MacDonald initially reported that stashing changes in a superproject's `bugfix_bar` branch incorrectly shows the submodule's `feature_sdk_foo` branch in stash entries' "On <branch>" display. K Jayatheerth confirmed the issue with a minimal test case and provided the fix - modifying `builtin/stash.c` to use `xstrdup()` for preserving the superproject branch name through submodule operations, preventing buffer reuse corruption.

The solution introduces a `branch_name_buf` variable to maintain the correct branch name while keeping the existing "(no branch)" fallback behavior. K Jayatheerth verified the patch works correctly in their test environment, showing stash entries properly reflect the superproject branch (`bugfix_bar`) rather than the submodule branch (`feature_sdk_foo`). Stuart MacDonald independently confirmed the fix resolves the issue in real-world usage, though noted deployment considerations for packaged distributions like Git for Windows.

Key technical details:
- Localized change to `do_create_stash()` message generation path
- Heap allocation via `xstrdup()` preserves branch name through submodule ops
- Maintains existing buffer reuse pattern for other ref resolutions
- No functional changes to core stash behavior - purely metadata correction
- Verified working in multiple independent test environments

The thread is technically resolved with a validated fix, though the historical context of when this behavior was introduced remains an open question. The test case serves as both verification and documentation of the corrected behavior. While some users may need to wait for official distribution updates, the core solution appears sound and ready for upstream consideration. No outstanding technical concerns remain regarding the fix itself.