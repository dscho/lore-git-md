This is a follow-up discussion on the SHA-256 support patches for gitk, specifically addressing review feedback on the first patch. Takashi Iwai responds to Johannes Sixt's code review points about the initial SHA-256 implementation.

The key technical exchanges focus on:
1. Defending the regex range check in `gotocommit` that doesn't use `$hashlength`, explaining it was intentionally kept as a range match per earlier review feedback
2. Clarifying the error message formatting for unsupported hash algorithms, showing `$objformat` is properly interpolated
3. Explaining the rationale for using both `objformat` and `hashalgorithm` variables - the former preserves the raw git-rev-parse output for error messages while the latter provides consistently formatted display strings

The discussion reveals some implementation details that weren't clear in the original patch:
- The error message for unsupported algorithms does properly include the algorithm name
- The variable split between `objformat` and `hashalgorithm` serves specific purposes (error reporting vs UI display)
- The regex range check was deliberately designed to work with both SHA-1 and SHA-256 without needing dynamic length

This response doesn't introduce new code changes but provides important context about design decisions that will inform the ongoing review. The thread shows active engagement with reviewer feedback while maintaining certain original design choices with clear justification. The exchange helps clarify the scope of remaining work (like the hardcoded length conversions noted in the thread summary) versus intentional design decisions.