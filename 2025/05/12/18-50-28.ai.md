This is a refactoring patch that renames `oidmap_free()` to `oidmap_clear()` to better reflect its actual behavior. The function clears entries from an oidmap without freeing the map structure itself, making the new name more accurate according to Git's CodingGuidelines.

The patch updates all callers across several subsystems including commit-graph handling (`builtin/rev-list.c`), object filtering (`list-objects-filter.c`), the object store (`object-store.c`), and the sequencer (`sequencer.c`). It also updates the unit tests and header documentation to match. The changes are mechanical and straightforward, simply renaming the function while maintaining all existing behavior.

The commit message clearly explains the motivation for the change, noting that the original name was misleading since it didn't actually free the map structure. This is the first patch in a 3-part series focused on improving the oidmap implementation, with subsequent patches adding new functionality and removing redundant code. The changes appear well-scoped and carefully implemented, with all call sites updated consistently.