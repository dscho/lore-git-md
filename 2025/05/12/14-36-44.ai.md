This is the second patch in a series adding SHA-256 support to gitk, specifically addressing hash length preferences. The email continues the discussion with Johannes Sixt about whether length preferences should be algorithm-specific (current implementation) or repository-agnostic (Sixt's suggestion).

The key points in this response:
1. Takashi Iwai defends the algorithm-specific approach, noting that without it, SHA-256 repositories would default to showing only 40 chars (SHA-1 length) which provides insufficient context for the longer hashes.
2. He provides a real-world example of an openSUSE SHA-256 repository (kernel-firmware-all) that motivated this work.
3. The exchange reveals a fundamental design question about whether length preferences should follow the user (Sixt's position) or the repository (Iwai's position).
4. Iwai requests specific wording suggestions for improving the commit message's user-facing description.

The thread context shows this is one of several outstanding issues in the SHA-256 gitk implementation, with five other procedures still needing length conversion updates. The discussion here focuses specifically on the user experience implications of the auto-select length preference implementation, with Iwai maintaining that algorithm-specific defaults provide better usability for SHA-256 repositories despite Sixt's preference for consistency across repositories.