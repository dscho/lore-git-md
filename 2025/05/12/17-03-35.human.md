This is a technical discussion about Change-ID implementation approaches, specifically addressing predecessor tracking in commit evolution models.

Junio C Hamano responds to Martin von Zweigbergk's proposal about Change-IDs by offering two refinements to the technical approach. First, he suggests that predecessor pointers shouldn't be limited to just 0 or 1, noting that a commit could naturally have multiple predecessors when combining changes. Second, he proposes that predecessor pointers could be treated like optional metadata in object transfers, similar to how tree pointers work in filtered clones, only being fetched when explicitly requested.

The email continues the thread's exploration of how to track commit evolution while balancing local workflow benefits against distributed system challenges. Junio's suggestions aim to address some of the bandwidth and security concerns raised earlier, particularly around transferring intermediate commit states. His proposal for optional metadata transfer could help mitigate the "wasteful sharing" problem Martin identified, though it would still require careful handling to prevent graph cycles.

This response moves the discussion forward by offering concrete implementation ideas while acknowledging the fundamental tension between local utility and distributed system requirements that has characterized the thread. The suggestions appear technically sound but don't yet resolve the larger questions about whether and how to standardize Change-IDs in Git.