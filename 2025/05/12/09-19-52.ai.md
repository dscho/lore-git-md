This is a cleanup patch in Patrick Steinhardt's systematic `contrib/` directory series. It removes the long-obsolete `contrib/examples/README` file that had been kept as a historical note after the actual example scripts were removed in 2018. The patch follows the series' established criteria by removing documentation for functionality that has been gone for over 7 years, with clear alternatives suggested (using `GIT_TRACE` and examining helper code history).

The removal is straightforward - just deleting a 20-line README that pointed to historical examples of scripted builtins that have since been rewritten in C. The commit message notes these examples had already drifted from actual implementations when they were removed, making their documentation of limited current value. This change aligns with the series' goal of removing unmaintained `contrib/` components while the cover letter's policy framework provides the broader context for such removals.

Key technical details for future reference: removes `contrib/examples/README` which contained historical notes about scripted builtin examples removed in commit 49eb8d39c78. The file suggested alternatives like using `GIT_TRACE=1` and examining `*--helper.c` history, but these pointers are available elsewhere. This is a non-controversial removal of documentation for long-gone functionality, consistent with the series' established criteria.