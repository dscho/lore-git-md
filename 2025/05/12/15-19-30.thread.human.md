Stuart MacDonald and K Jayatheerth have confirmed the fix for a `git stash list` display issue where stashes made in a superproject's branch (like `bugfix_bar`) incorrectly showed the submodule's branch name (like `feature_sdk_foo`) in the output. The root cause was a buffer reuse problem in `builtin/stash.c` where submodule operations overwrote the branch name before the stash message was finalized.

The solution, which introduces a new `branch_name_buf` variable to properly preserve the superproject's branch name via `xstrdup()`, has been independently verified to work correctly. Both reporters confirm it maintains all expected behaviors, including the "(no branch)" fallback when applicable, with no regressions in basic stash functionality. While the technical fix is sound and ready for upstream consideration, Stuart notes that users of packaged distributions like Git for Windows may need to wait for official updates to benefit from the correction.

This concludes the thread's technical discussion, with the bug properly diagnosed and fixed through collaborative effort between the reporters. The patch appears well-scoped and effective, addressing the metadata display issue without introducing new problems.