This thread introduces a series of refactoring changes to improve Git's oidmap implementation. The initial patch renames `oidmap_free()` to `oidmap_clear()` to better reflect its actual behavior - the function clears entries without freeing the map structure itself. The change updates all callers across multiple subsystems including commit-graph handling, object filtering, and the sequencer. 

The series is focused on internal API improvements rather than new features or bug fixes. Subsequent patches will add an `oidmap_size()` function for better introspection and remove a redundant pointer from `struct raw_object_store`. The changes are mechanical but improve code accuracy and clarity while maintaining existing behavior. The implementation appears careful and thorough, with all call sites updated consistently and documentation kept in sync.