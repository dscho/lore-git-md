Bugfix review response in the reftable memory leak series. Patrick Steinhardt reviews v2 of Lidong Yan's patch fixing two memory leaks in the reftable writer's error paths. He agrees with both proposed fixes but suggests splitting them into separate commits since they address distinct code areas (padded writes vs index hash operations). 

The first fix adds cleanup for a `zeroed` buffer when `padded_write()` fails, while the second ensures proper freeing of a `key` buffer when `reftable_buf_add()` fails in `writer_index_hash()`. Steinhardt explicitly approves both changes, noting the second case's logic where the key should be freed if it can't be added to the object index tree. 

This moves the thread forward by providing maintainer feedback on the technical approach while keeping the scope focused on the original memory leak fixes. The review doesn't address the open questions from the thread summary about test coverage or other potential leaks, suggesting those may be follow-up discussion points.