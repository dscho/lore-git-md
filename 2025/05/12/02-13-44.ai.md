Bugfix follow-up in the `git apply --intent-to-add` series. This email reports an unexpected interaction between the patch series' index handling changes and `git checkout -p`. The author notes that while the logical change from `state->check_index` to `state->update_index` should only affect intent-to-add cases, it somehow breaks checkout's patch mode. 

The key technical detail is the discovery that using `(state->check_index || state->ita_only)` as a temporary workaround avoids the breakage, though the author acknowledges this doesn't make immediate sense given that `update_index` is set nearby. The email serves as both a heads-up about needing a reroll and a request for general review of the series while the specific checkout interaction is investigated.

This follows the comprehensive fix series addressing index corruption in `--intent-to-add` mode, where proper index loading was a core part of the solution. The current hiccup suggests there may be subtle interactions between the apply machinery's various modes that weren't fully accounted for in the test coverage. The author's approach of both working on a fix and soliciting broader review suggests this is likely to be resolved carefully rather than rushed.