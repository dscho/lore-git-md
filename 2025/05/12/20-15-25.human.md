This is a follow-up discussion in the ongoing `git send-email` FQDN detection improvement thread. Junio Hamano responds to Eric Sunshine's observation about BSD systems also needing the `-f` flag for hostname resolution, rather than the Linux-specific `--fqdn` flag.

Junio notes with humor that while Eric is correct about BSD systems generally needing `-f`, the current patch already limits its platform-specific approach to only Linux and macOS (via $^O checks), so BSD systems would fall back to the existing behavior anyway. This appears to be a lighthearted acknowledgement that the patch's current scope doesn't yet address the broader BSD compatibility concern Eric raised.

The exchange shows the maintainer's awareness of the platform-specific nuances in hostname resolution, while indicating the current implementation remains focused on just Linux and macOS support. This suggests BSD support might be a natural next step for the patch series, though the thread has already established consensus on the core approach.