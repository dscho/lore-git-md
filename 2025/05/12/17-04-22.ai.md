This is a brief response in the ongoing thread about adding a `--mergeability-only` option to `git merge-tree`. Junio C Hamano suggests that the proposed flag might be more conventionally named `--dry-run`, implying this terminology would be more familiar to users. The comment is brief and doesn't reject the functionality itself, just raises a naming question. This appears to be the first feedback on the thread since the initial patch submission.

The thread context shows this is part of a 2-patch series that adds internal merge-ort plumbing (patch 1) and the user-facing `--mergeability-only` flag (patch 2) to optimize merge checks in Git forges. The implementation is complete with thorough test coverage and documentation. The naming question raised here is the first point of potential discussion in what has otherwise been a straightforward proposal.