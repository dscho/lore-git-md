Follow-up review comment on the stash branch name fix. Eric Sunshine notes that while Junio C Hamano has approved the core fix (using `xstrdup()` to preserve superproject branch names in submodule contexts), the patch is missing a test case to verify the corrected behavior. This is a standard expectation for bugfix patches - especially for issues that were previously untested.

The technical context remains unchanged from the thread summary: the fix involves heap-allocating branch names via `xstrdup()` in `do_create_stash()` to prevent buffer reuse corruption when resolving submodule references. The two outstanding issues are now:
1. Code quality fixes (indentation and memory leak) noted by Junio
2. Missing test coverage noted by Eric

No new technical concerns are raised - this is purely about ensuring the fix is properly validated. The thread appears to be in final polishing stages before merging, with these being the last required improvements. The historical context of when the bug was introduced remains unaddressed but doesn't block the fix.