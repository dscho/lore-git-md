Bugfix follow-up in the `git apply --intent-to-add` series. Raymond Pasco has discovered an unexpected interaction between the core fix (changing `check_index` to `update_index` for index reading) and `git checkout -p`, causing breakage that isn't immediately explainable. 

The author notes that while the logical flow suggests this change should only affect `--intent-to-add` cases (since `update_index` is set just above), the observed behavior contradicts this. A temporary workaround using `(state->check_index || state->ita_only)` appears to function, but the author wants to investigate further rather than applying an unexplained fix.

This email serves as both a heads-up about a coming reroll and a request for general review of the series while the specific `checkout -p` interaction is being debugged. The thread context shows this is part of a comprehensive fix for index corruption issues in `--intent-to-add` mode, with previous patches already handling error checking, index loading, and write conditions.

The author's approach here demonstrates careful debugging practices - noting an unexpected failure, proposing a temporary solution, but prioritizing understanding the root cause over quick fixes. This suggests the series is being developed with attention to edge cases and broader command interactions.