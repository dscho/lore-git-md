This is a follow-up in the merge-tree optimization thread, where Elijah Newren accepts Junio Hamano's naming recommendation for the new mergeability check flag. The email resolves the naming debate by agreeing to use `--dry-run` instead of the originally proposed `--mergeability-only`, citing `git fetch --dry-run` as precedent for commands that perform internal object writes while still being considered "dry run" operations from the user's perspective.

Elijah acknowledges Junio's argument about Git's established dry-run semantics, where the key criterion is avoiding user-observable changes rather than prohibiting all object creation. He notes the documentation will need to explicitly mention the edge cases where objects are still written (merge base computation and rename/rename conflicts), as some users may care about this implementation detail despite it not affecting the dry-run guarantee.

The email also references Junio's dismissal of using `--check` (from `git apply`) as an alternative name, agreeing with the maintainer's position that consistency with Git's broader dry-run conventions is more precise. This appears to be the final resolution of the naming discussion, with no outstanding technical objections to the implementation itself. The patch series now awaits finalization with the `--dry-run` flag name and corresponding documentation updates about object creation behavior.