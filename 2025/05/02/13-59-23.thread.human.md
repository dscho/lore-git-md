This thread began with a bug report from Arnoud BERENDSEN about `git svn clone` failures on Windows, where cryptic Perl warnings and branch initialization errors made the tool difficult to use. Johannes Schindelin (Dscho) acknowledged the Windows-specific maintenance challenges and suggested using WSL with Linux Git as a workaround, while also noting an existing discussion about potentially deprecating Windows support for `git svn`.

The conversation then broadened when Patrick Steinhardt raised concerns about Git's `contrib/` directory, using `git svn` as an example of how unmaintained scripts can create false expectations of support. In the latest email, Dscho clarifies that `git svn` is actually part of core Git (alongside other legacy import tools like `git-cvsserver`), not the `contrib/` directory. He agrees that `contrib/` needs cleanup but notes it contains a mix of actively maintained scripts (like Bash completion) and truly unmaintained code. The `git svn` tool and similar import commands, he suggests, are "dust collectors" from Git's early days when such transitional tools were more necessary.

The thread now has two clear tracks: the original technical issues with `git svn` on Windows (still unresolved), and the broader discussion about how Git should handle legacy code—both in `contrib/` and in core commands. Dscho's latest contribution helps separate these concerns, noting that while `contrib/` cleanup is worthwhile, the status of `git svn` and similar import tools is a distinct question that may need separate consideration.