Here's a human-readable summary of the thread:

The discussion began with Arnoud BERENDSEN reporting two issues when using `git svn clone` on Windows with Git for Windows 2.49.0. The problems involve path stripping failures (particularly with `.gitignore` files) and Perl warnings in Git.pm that lead to fatal errors during branch initialization. The error messages were criticized for being unhelpful, showing internal Perl details rather than actionable guidance.

Johannes Schindelin (Git for Windows maintainer) has now responded, acknowledging the long-standing maintenance challenges with `git svn` on Windows. He suggests using WSL with Linux Git as a workaround and links to an existing discussion about potentially deprecating Windows support for this feature. The response indicates there's currently no developer bandwidth to fix these Windows-specific issues, as the feature is in a precarious maintenance state in Git for Windows. While the technical details of the specific Perl warnings (lines 1655 and 1681 in Git.pm) and branch initialization errors remain relevant, the focus has shifted to the broader question of whether to continue supporting this feature on Windows at all.

The thread highlights an ongoing tension between users needing SVN-to-Git conversion on Windows and the maintenance burden of keeping this Perl-based feature working reliably across platforms. For now, Windows users experiencing these issues are effectively being directed to alternative solutions like WSL rather than expecting fixes in Git for Windows.