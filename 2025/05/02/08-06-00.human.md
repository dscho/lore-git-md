This is a follow-up discussion in the `git mv` bugfix thread, focusing on the broader implications of converting `assert()` to `BUG()` calls in the codebase.

Patrick Steinhardt responds to Junio Hamano's suggestion that Johannes Schindelin's Windows/Meson assert-suppression patch may no longer be needed after their `git-mv` changes. The discussion has shifted from the specific bugfix to a project-wide policy question about assertion handling in CI.

Patrick proposes potentially banning `assert()` entirely in favor of `BUG()`, noting there are over 600 existing `assert()` calls that would need conversion. He suggests a transitional macro (`BUG_UNLESS`) to ease the migration. The email confirms Patrick's agreement with Junio's inclination to drop the assert-suppression patch when merging to 'next'.

The exchange highlights an ongoing tension between test reliability (where stuck tests from assertions can block CI) and bug detection (where suppressing assertions might hide real issues). While the core `git-mv` solution remains approved, this thread branch now opened a larger conversation about Git's assertion handling philosophy.