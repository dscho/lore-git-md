Here's the updated human-readable thread summary:

The thread addresses platform compatibility issues with BSD variants' errno behavior when using O_NOFOLLOW during packed-refs fsck operations. The core solution - using lstat() for portable symlink detection in packed-backend.c - has been agreed upon, with Jeff King's approach resolving the POSIX-compliance versus BSD errno divergence. Performance considerations for large packed-refs files emerged as a related concern, with GitLab.com data showing most files are under 50MB (99% under 5MB) but confirming rare 2GB cases that might benefit from mmap() optimization.

Collin Funk has been handling the BSD-specific errno cases, with a follow-up addressing FreeBSD's EMLINK still pending. In the latest email, Patrick Steinhardt reviewed Collin's patch correcting BSD errno values (EFTYPE on NetBSD, EMLINK on FreeBSD) to match POSIX-standard ELOOP behavior. While agreeing with the technical approach, Patrick noted some style guide violations in preprocessor directive formatting that need adjustment - specifically around indentation of nested directives. The underlying solution remains uncontested, with the discussion now focusing on final polish before merging.

Key participants include Collin Funk (BSD errno handling), Jeff King (lstat() solution and mmap() optimization suggestion), and Patrick Steinhardt (providing GitLab.com data and code review). Junio C Hamano has confirmed the incremental approach for both BSD cases and performance considerations. The thread appears close to resolution, pending Collin's style fixes and FreeBSD follow-up patch.