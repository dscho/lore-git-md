Here's a concise human-readable summary of the thread's current state:

The thread began as an effort to handle platform differences in how BSD variants report errors when detecting symlinks during packed-refs operations. After several iterations, Collin Funk's patch to translate BSD-specific errno values (EFTYPE on NetBSD, EMLINK on FreeBSD) to the standard ELOOP has been finalized and reviewed, with Patrick Steinhardt confirming it meets Git's style requirements. This resolves the core compatibility issue.

Meanwhile, shejialuo has been working in parallel on optimizing packed-refs reading using mmap. Their latest email clarifies that while the current implementation performs redundant symlink checks (using both `open_nofollow()` and `stat`), they won't change this logic now that the BSD errno solution is settled. Instead, they'll focus on the mmap optimization work, which addresses performance side of the effort.

The discussion has revealed some tradeoffs between atomicity guarantees and implementation complexity - while `open_nofollow()` provides better atomicity on some platforms, it introduces compatibility challenges. The group has converged on using `lstat()` for portable symlink detection, accepting that perfect atomicity may not be practical across all platforms. With the BSD errno patch ready for merge, attention now shifts to shejialuo's upcoming mmap optimization work.