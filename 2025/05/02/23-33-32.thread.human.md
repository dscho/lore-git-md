Here's the updated human-readable summary:

The thread began as a platform compatibility discussion about how different BSD variants handle errno values when encountering symlinks during packed-refs operations, specifically NetBSD returning EFTYPE and FreeBSD returning EMLINK where POSIX specifies ELOOP. The core solution settled on using lstat() for portable symlink detection in packed-backend.c, with Collin Funk working on style fixes for the BSD errno translation patches.

The conversation has since expanded into two related areas: performance optimization of packed-refs reading (with shejialuo preparing an mmap-based implementation) and deeper questions about atomic operation guarantees in Git's platform abstraction layer. Jeff King notes that while the immediate BSD errno fixes are straightforward, there are broader design questions about when operations truly need atomic guarantees versus portable error handling. He points out that open_nofollow()'s header comment already warns about race conditions in its fallback implementation, though this might not be sufficiently visible to developers.

Key open questions remain about whether to maintain both reading strategies or fully transition to mmap, how to document atomicity requirements, and whether open_nofollow() should be restricted to contexts where atomicity is truly needed. Junio C Hamano continues to guide the discussion toward these architectural implications while allowing the immediate BSD errno fixes to proceed. The performance optimization work is informed by GitLab.com metrics showing most packed-refs files are under 5MB, with a few outliers up to 2GB.