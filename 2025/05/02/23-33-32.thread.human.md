Here's the updated human-readable thread summary:

This thread addresses platform compatibility issues around BSD variants returning different errno values (EMLINK on FreeBSD and EFTYPE on NetBSD) when detecting symlinks during packed-refs operations. The core solution - using lstat() for portable symlink detection - remains unchanged and has consensus. Collin Funk has been documenting these BSD errno behaviors in Gnulib for broader ecosystem awareness.

The latest discussion clarifies why these BSD-specific errno values can be reliably used as secondary checks: Git's codebase doesn't trigger the other cases where these errnos might appear (like NetBSD's O_REGULAR flag usage). Jeff King had raised concerns about over-reliance on errno checking, but the analysis shows these particular BSD cases are safe to handle. 

Work remains to finalize the FreeBSD EMLINK handling in a follow-up patch, while the NetBSD changes are in progress. Performance optimizations for very large packed-refs files (potentially using mmap) remain an outstanding but low-priority item. The thread demonstrates Git's careful approach to platform compatibility - solving issues through core logic improvements while still accounting for system-specific behaviors where appropriate.