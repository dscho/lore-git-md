This is a continuation of the ongoing discussion about assertion handling in Git, specifically regarding Windows/Meson builds. Johannes Schindelin responds to Junio's suggestion to drop his assert-suppression patch, arguing that it remains necessary for proper Visual Studio compatibility.

Key technical points:
- Maintains that Windows CI has historically built in release mode to avoid modal dialogs from `assert()` in both Git and its dependencies
- Points to specific code in `compat/mingw.c` that shows how MSVC handles assertions differently in debug vs release modes
- Argues the Meson build mistakenly defaulted to debug mode, making his patch a bug fix rather than a policy choice
- Offers two alternatives: either accept his patch to properly set release mode, or completely drop Windows/Meson support

The email reveals deeper platform-specific considerations in the assertion handling debate:
- Windows behavior differs fundamentally from other platforms due to MSVC's modal dialog behavior
- The issue affects not just Git's own code but also dependencies
- There's tension between CI catching all assertions (Junio's position) and providing a usable Windows development environment

This keeps open the thread's two parallel tracks: the already-solved `git mv` bugfix and the broader assertion policy discussion that now includes Windows-specific build considerations. Johannes provides concrete technical rationale for maintaining his approach rather than adopting Junio's suggested CI behavior for Windows builds.