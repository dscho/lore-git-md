This is a follow-up discussion in the reftable test modernization series, specifically about build system cleanup after converting tests to the Clar framework. The email addresses two technical points about removing references to the old test infrastructure:

1. The meson.build file still contains a glob pattern for old-style unit tests (`t-*.c`) that now matches nothing, which Patrick notes should ideally be removed in the next commit but can't be due to the empty glob causing errors.

2. Patrick points out that the Makefile's "check-meson" target may also need updating to remove references to the old unit tests, suggesting this was overlooked in the conversion.

The discussion is focused on final cleanup details after the main technical work of converting the reftable stack tests to Clar in patch 9/10. This is part of the broader effort to standardize all reftable tests on the Clar framework, with the series now in its third iteration. The thread shows attention to build system consistency across both meson and Makefile configurations as the old test infrastructure is phased out.