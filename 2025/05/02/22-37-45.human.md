This is a follow-up discussion in the resolved Meson shell path detection thread, where Eli Schwartz responds to Junio Hamano's question about user-specified command paths in Meson builds. The thread had previously reached consensus on the technical implementation, but Junio raised a philosophical question about the interface difference between Makefile's direct `SHELL_PATH` setting and Meson's configuration file approach.

Eli acknowledges that while it would be possible to add direct command-line support for specifying paths in Meson (similar to Makefile's `SHELL_PATH`), he doesn't personally object to the current solution that requires using Meson's `paths.ini` configuration file. He notes that such a feature might be implemented at the Meson level rather than in Git's build system, mentioning a past proposal for Meson to support command-line machine description settings.

The email serves as a final confirmation that while the interface differs from Makefile's approach, the functionality is equivalent. Eli's response indicates no remaining technical objections, though he suggests the possibility of future Meson-level improvements that could make the interface more similar to Makefile's direct settings. The thread remains fully resolved with this exchange, as all participants have accepted the current Meson implementation as functionally adequate despite its different configuration mechanism.