This is a refactoring patch that's part of Patrick Steinhardt's series to decompose `git gc` functionality into standalone maintenance tasks. The patch moves the rerere garbage collection code from `cmd_gc()` into a new `maintenance_task_rerere_gc()` function, following the same pattern used earlier in the series for worktree pruning.

The change is straightforward - it extracts the existing rerere GC implementation (which simply spawns a child process to run `git rerere gc`) into a standalone function with the same interface as other maintenance tasks. This prepares for the next patch in the series which will expose this as a configurable maintenance task.

The patch maintains identical behavior to the current `git gc` implementation, including the child process spawning approach. This is part of the series' careful strategy to first achieve functional parity before optimizing implementation details. The diff shows clean extraction with no behavior changes, just moving code into the new function and updating the call site in `cmd_gc()`.