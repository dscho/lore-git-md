This is a discussion continuation about unifying diff context handling between interactive patch commands and regular diff output. Phillip Wood weighs in on Leon Michalak's proposal to make interactive commands (`git add -p` etc.) respect `diff.context` and `diff.interhunkcontext` configurations.

Phillip agrees with Jeff King's earlier position that inheriting these configs by default would be a useful improvement, suggesting the implementation could follow the existing pattern for `diff.algorithm`. He expresses skepticism about the value of supporting `-U<n>` command-line overrides for these interactive commands, noting that dynamic context changes during a session would require significant implementation work to regenerate diffs on the fly.

The email reinforces the emerging consensus from the thread that config inheritance is the right approach, though Phillip's comments about implementation complexity hint at potential future discussion points around dynamic adjustment capabilities. The technical constraints around hunk tracking remain a key consideration, with the current focus being on establishing baseline config inheritance before considering more advanced features.