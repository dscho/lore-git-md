This is a technical follow-up in the ongoing effort to unify diff context handling between interactive and non-interactive Git commands. Junio Hamano responds to Leon Michalak's implementation questions about inheriting `diff.context` settings in `git add -p`.

The exchange focuses on a specific implementation detail: how to represent unset context values internally. Junio notes that since negative context values don't make logical sense (the diff.c code uses `int` but expects non-negative values), suggests using -1 as a sentinel value to indicate when context hasn't been explicitly set via config or command line.

This is a small but important technical point in the larger effort to make interactive patch commands respect the same diff context settings as regular diff output. The thread shows careful attention to implementation details while maintaining the previously agreed architectural approach. Junio's response indicates approval of the general direction while offering concrete advice on the internal representation question.