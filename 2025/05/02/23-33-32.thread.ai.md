Updated AI summary:

Thread identity: Platform compatibility fix for BSD variants' errno behavior with O_NOFOLLOW, addressing test failures in t0602-reffiles-fsck.sh where NetBSD returns EFTYPE and FreeBSD returns EMLINK for symlink operations instead of POSIX-standard ELOOP. Originated from Collin Funk's patch after commit cfea2f2da8 added stricter filetype checking for packed-refs.

Core problem: POSIX specifies ELOOP should be returned when O_NOFOLLOW encounters a symlink, but BSD variants diverge - NetBSD returns EFTYPE and FreeBSD returns EMLINK. This causes test failures in refs/files-backend.c's validation logic.

Current status: Collin Funk has completed the BSD errno translation work with a v2 patch that extends the initial NetBSD-specific solution (translating EFTYPE to ELOOP) to also handle FreeBSD's EMLINK case. Both patches use the same pattern in wrapper.c's open_nofollow() function, employing conditional compilation with SYMLINK_ERRNO macros for each BSD variant. The solution maintains POSIX-expected behavior without additional lstat() calls and fixes all known test failures in t0602-reffiles-fsck.sh across BSD platforms. OpenBSD remains unaffected but could be similarly addressed if needed.

Key technical details: The implementation handles both NetBSD's EFTYPE and FreeBSD's EMLINK errno values through platform-specific translations in wrapper.c. The approach remains narrowly scoped to symlink cases where these errno values are known to occur, avoiding broader errno handling that might mask legitimate errors. The solution uses conditional compilation to define SYMLINK_ERRNO differently for each BSD variant while maintaining a single code path for POSIX-compliant systems. The changes preserve the original behavior where ELOOP is expected while accommodating BSD deviations.

Open questions: None remaining - the v2 patch completes the BSD errno translation work started earlier. brian m. carlson and Junio C Hamano's earlier concerns about broader BSD errno divergence remain as a systemic issue but are recognized as beyond the scope of this specific fix.

Participant positions: Collin Funk authored both the initial NetBSD fix and this FreeBSD extension, demonstrating consistent implementation across BSD variants. brian m. carlson had previously reviewed and approved the technical approach despite not being a BSD user. Junio C Hamano approved the NetBSD patch and this follow-up completes the work he had anticipated.

Related work: The issue surfaced after commit cfea2f2da8 (refs: verify packed-refs using filetype in dirent) added stricter validation. The thread demonstrates Git's ongoing need to handle platform-specific errno variations in filesystem operations, particularly around symlink handling in the refs backend. The BSD errno translation pattern established here - using conditional compilation for platform-specific errno handling - may inform future compatibility fixes for similar platform divergence cases.