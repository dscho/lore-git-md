This is a technical clarification in the ongoing discussion about diff context handling in interactive patch commands. Phillip Wood raises a side question about the plumbing command `git diff-index`'s behavior with `GIT_DIFF_OPTS`, noting that while this environment variable was historically the only way to adjust diff context, modern Git provides direct `-U` flags. He questions whether it's still desirable for plumbing commands to respect `GIT_DIFF_OPTS`, as this could lead to unexpected behavior when scripts explicitly specify context via `-U` while the environment contains conflicting settings.

The email doesn't directly advance the main thread about interactive patch commands, but surfaces an implementation detail that could affect related diff machinery. Phillip's question suggests there may be an opportunity to clean up legacy behavior in how diff options are processed across Git's plumbing commands, though he leaves this open for others to weigh in on. This is a lower-level technical consideration compared to the user-facing config inheritance discussion that dominates the main thread.