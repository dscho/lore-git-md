This is a bugfix discussion regarding `git apply --intent-to-add` behavior. Ryan Hodges responds to Junio Hamano's concerns about the proposed solution (making `--intent-to-add` imply `--index`) by demonstrating a concrete case where the current implementation causes serious problems - applying a patch with `--intent-to-add` incorrectly marks unrelated tracked files as deleted in the index. 

The key technical details are: 1) The current behavior completely replaces the index contents with just the intent-to-add files, which is clearly wrong, 2) Ryan provides a clear reproduction case showing this destructive behavior, and 3) He explains the intended workflow where `--intent-to-add` should help maintain consistency between apply/diff/commit operations for new files.

Ryan's email advances the thread by moving from theoretical concerns about solution approaches to demonstrating actual broken behavior that needs fixing, regardless of the implementation path. The core conflict remains between the documented behavior (where `--intent-to-add` is ignored unless `--index` is not specified) and the real-world impact (index corruption when using the option as intended). The email suggests the original patch may have been abandoned due to maintainer concerns rather than being irrelevant.