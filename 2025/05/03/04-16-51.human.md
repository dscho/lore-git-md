Platform compatibility fix extending the BSD errno translation pattern to FreeBSD. This is a follow-up to Collin Funk's earlier patch that addressed NetBSD's non-POSIX errno behavior for symlink operations with O_NOFOLLOW. 

The patch modifies wrapper.c's open_nofollow() to handle FreeBSD's EMLINK return value (analogous to NetBSD's EFTYPE) by translating it to the POSIX-standard ELOOP. This fixes test failures in t0602-reffiles-fsck.sh where FreeBSD's behavior was causing incorrect error messages when checking packed-refs filetypes.

The change follows the same established pattern as the NetBSD fix that was recently approved, using compile-time detection to apply the errno translation only on affected platforms. The implementation avoids extra lstat() calls while maintaining correct POSIX semantics. 

This appears to be a straightforward extension of the BSD compatibility work, with the technical approach already vetted in the previous discussion. The patch is narrowly focused and well-explained, showing clear test case impact and proper attribution to the original issue (cfea2f2da8). 

With the NetBSD portion already approved, this FreeBSD follow-up seems likely to proceed smoothly through review, completing the BSD errno handling for this particular edge case. The patch demonstrates good attention to cross-platform compatibility details that matter for Git's filesystem operations.