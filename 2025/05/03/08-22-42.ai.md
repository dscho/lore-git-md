Bugfix for `git apply --intent-to-add` index corruption issue, with additional edge case discovered.

Raymond Pasco continues analysis of the `--intent-to-add` (`-N`) bug in `git apply`, building on the thread's established understanding that the option incorrectly clobbers the index contents. While confirming that Johannes Altmanninger's original patch code was correct despite its confusing commit message, Pasco now identifies a new edge case: even with the fix, the command incorrectly attempts to apply the entire diff (including non-new-file changes) when `ita_only` is enabled.

This discovery means the fix requires more work than previously thought - not just documentation updates about the mutual exclusivity with `--index` and `--cached`, but also logic adjustments to properly handle partial diffs. The technical challenge involves ensuring `--intent-to-add` only processes the new-file portions of patches while maintaining index safety.

The thread now has both a reproduction case and increasingly precise technical understanding, but needs either Pasco or another contributor to submit an updated patch addressing both the original index corruption and this newly found partial-diff issue. The solution will need to carefully balance the original intent of `--intent-to-add` with the various apply modes while preventing any index corruption scenarios.