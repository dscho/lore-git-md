This is a platform compatibility discussion regarding errno handling in symlink checks, with Jeff King proposing an alternative approach to the BSD errno translation fix.

Jeff King suggests that rather than adding platform-specific errno translation logic in `open_nofollow()`, we could simplify the packed-refs validation in `packed-backend.c` by using `lstat()` first. His proposed change would check the file type directly via `lstat()` before attempting to open the file, which would work consistently across all platforms without needing errno translation. The approach would also make the error message more generic ("not a regular file" rather than specifically mentioning symlinks).

The discussion reveals this is part of a larger consideration about how strictly it should interpret errno values, with Jeff noting we've generally avoided being too particular about errno values in the recent `cfea2f2da8` change. He also points out an unrelated performance concern about reading large packed-refs files into memory rather than mmap'ing them.

This email represents a thoughtful counterproposal to the BSD errno translation patches, suggesting a simpler cross-platform solution while raising broader questions about error handling philosophy in Git's filesystem operations. The suggestion appears technically sound and maintains the same safety guarantees while being more portable.