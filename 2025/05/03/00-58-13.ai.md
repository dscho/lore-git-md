This is part of the ongoing `git whatchanged` deprecation series, specifically implementing the final removal step when built with `WITH_BREAKING_CHANGES`. The patch conditionally removes the command from the codebase while maintaining all the preparatory work done in previous patches.

The changes systematically remove `whatchanged` from:
- Documentation (5 files updated to conditionally exclude references)
- The command table in git.c
- The implementation in builtin/log.c (wrapped in `#ifndef WITH_BREAKING_CHANGES`)
- Test suites (t4013 and t4202 updated to skip tests when built without the command)

Key technical aspects:
- Maintains all previous deprecation infrastructure (opt-in flag, warnings)
- Uses `WITH_BREAKING_CHANGES` compile-time flag to control removal
- Updates tests to handle both compilation modes via `WITHOUT_BREAKING_CHANGES` prerequisite
- Keeps the door open for slower rollout by making removal optional

The patch follows the established deprecation pattern while being cautious about final removal timing. It references the parallel `branches/` and `remotes/` removal as a model but chooses to keep documentation updates separate for now. The implementation is thorough, updating all relevant documentation and tests while maintaining backward compatibility through the compilation flag.