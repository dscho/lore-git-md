This is the final documentation patch in the `git whatchanged` deprecation series, formally announcing the command's planned removal in the BreakingChanges document. The patch adds a clear warning to both the command's man page and the BreakingChanges file, noting that `git whatchanged` has been obsolete for over a decade (with `git log --raw` serving as a better alternative) and that no users have complained during the deprecation period's opt-in requirement phase.

The patch could technically be squashed with the previous code removal patch, but Junio Hamano keeps it separate to allow maintainers more time to gauge real-world impact before finalizing the deprecation. The documentation changes follow the same pattern used for `git pack-redundant`, maintaining consistency in how Git documents breaking changes. The warning text is straightforward, explaining both the rationale for removal (redundancy with `git log --raw`) and the deprecation timeline.

This completes the full deprecation lifecycle for `git whatchanged`, from initial warnings through opt-in requirement to final documentation of its removal, while still preserving backward compatibility path via the `WITH_BREAKING_CHANGES` compilation option.