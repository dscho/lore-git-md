This is a platform compatibility discussion regarding errno handling in the packed-refs fsck operation, continuing the thread about BSD variants' behavior with O_NOFOLLOW. The email from shejialuo responds to Jeff King's proposal to replace open_nofollow() with lstat() in packed-backend.c to avoid platform-specific errno translation.

The key technical point is a tradeoff between atomicity and platform compatibility. While shejialuo initially preferred open_nofollow() for its race-avoidance properties, they now agree with Jeff King that the lstat()-based approach is preferable given that open_nofollow() is already best-effort against races. This would eliminate the need for the BSD-specific errno translation patches that were previously discussed.

The email also raises a separate performance consideration about strbuf vs mmap for reading large packed-refs files during fsck. shejialuo questions whether mmap optimization is necessary given that fsck operations typically don't share packed-refs content between processes, and asks for more context about GitHub's use case where packed-refs files can grow very large.

This moves the thread forward by:
1) Accepting Jeff King's proposed lstat() solution as preferable to BSD errno translation
2) Opening a new discussion point about performance optimization for large packed-refs files
3) Requesting real-world usage data to inform the mmap vs strbuf decision

The thread now has two separable technical questions: the settled lstat() approach for symlink detection, and the newly-raised performance consideration for large packed-refs handling during fsck.