This is a bugfix follow-up in the `git apply --intent-to-add` thread. Raymond Pasco identifies an additional issue with the proposed fix - the current implementation incorrectly tries to apply the entire diff (including non-new-file changes) when `--intent-to-add` is active, rather than only handling new file additions as intended.

The email builds on the thread's technical analysis which established that `--intent-to-add` should be mutually exclusive with `--index` and `--cached` options, and that the original 2021 patch had correct code despite unclear documentation. Pasco had previously offered to refresh the patch, but now notes it will require more work to properly handle the ita_only case.

This appears to be a serious but well-understood bug that affects real workflows when maintaining consistency between apply/diff/commit operations. The discussion has clear technical direction and an engaged contributor willing to implement fixes, suggesting this will likely be resolved soon with proper documentation about the option's limitations.