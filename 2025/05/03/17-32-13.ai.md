This is a platform compatibility follow-up in the BSD errno handling thread. Junio C Hamano confirms Collin Funk's willingness to extend the NetBSD errno fix to cover FreeBSD's similar EMLINK behavior, though emphasizes this can be done as a separate patch. The exchange shows the thread moving from problem identification (NetBSD's EFTYPE) to recognizing a broader BSD pattern (FreeBSD's EMLINK) while maintaining the agreed lstat()-based solution approach.

The email serves primarily as coordination between maintainer and contributor, with Junio both acknowledging Collin's offer to handle FreeBSD and making clear this doesn't need to block the current NetBSD fix. This maintains the thread's trajectory toward comprehensive BSD compatibility while allowing incremental implementation. The technical context remains the packed-refs fsck operations where BSD variants return different errno values (EFTYPE/EMLINK) than POSIX's ELOOP for symlink operations, though the actual solution path has already settled on using lstat() rather than errno translation.