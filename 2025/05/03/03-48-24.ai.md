This is a follow-up in the platform compatibility thread addressing BSD errno behavior with O_NOFOLLOW. Collin Funk confirms brian m. carlson's suspicion that FreeBSD exhibits similar non-POSIX behavior by returning EMLINK instead of ELOOP when O_NOFOLLOW encounters a symlink, matching the earlier-identified NetBSD EFTYPE issue.

The email indicates Collin will prepare a separate patch to handle FreeBSD's EMLINK case, following the same pattern as the already-approved NetBSD fix that translates platform-specific errnos to POSIX-standard ELOOP in wrapper.c's open_nofollow(). Both parties express frustration with BSD variants not conforming to POSIX errno specifications even when _POSIX_SOURCE is defined, though they acknowledge this as a systemic issue beyond Git's control.

Key technical details carried forward: The solution approach involves errno translation in wrapper.c without additional lstat() calls, maintaining POSIX-expected behavior while accommodating platform quirks. The FreeBSD case will require the same careful handling as NetBSD's, where the translation is safe because these errnos only occur for symlinks in this specific context.

This moves the thread forward by confirming the need for parallel FreeBSD handling while the NetBSD fix proceeds independently. The discussion surfaces ongoing challenges in maintaining POSIX compliance across BSD variants, particularly for filesystem operations in the refs backend that rely on strict symlink handling.