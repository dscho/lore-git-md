This is a documentation patch in the `whatchanged` deprecation series, specifically preparing documentation references to be future-proof when `git whatchanged` is removed. 

The patch updates two documentation files to remove examples and references to `git whatchanged` in favor of more stable alternatives. In `MyFirstObjectWalk.adoc`, it changes the example placement of a hypothetical `walken` command from being near `whatchanged` to being near `version`, and updates the corresponding `builtin.h` reference. In `user-manual.adoc`, it replaces `whatchanged` with `show` as an example of commands sharing implementation in `builtin/log.c`.

These changes are purely preparatory - they don't affect functionality but make the documentation more resilient to the eventual removal of `whatchanged`. The patch follows the series' pattern of systematically eliminating `whatchanged` references while maintaining accuracy. The modifications are straightforward substitutions that don't require any complex reasoning about alternatives, keeping with the series' measured approach to deprecation.

Key technical details include updates to the tutorial documentation's example code placement and the user manual's description of command implementation sharing. The changes touch the documentation build system but don't affect runtime behavior. This patch represents the documentation-focused portion of the multi-phase `whatchanged` deprecation effort.