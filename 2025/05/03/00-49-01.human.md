This is a bugfix follow-up in a thread about SSH cloning permissions. Brian Carlson suggests trying Git 2.48 or newer on the server side, which includes a patch addressing untrusted repository cloning scenarios. The original issue involved SSH clones failing on bare repositories with different ownership, where Git's suggested `safe.directory` workaround didn't actually resolve the access problem.

The response notes that local clones would require `--no-local` with the new version, but this restriction doesn't apply to network operations. This appears to be a known solution to the reported issue, though it's unclear from this email alone whether it fully resolves the original multi-user access scenario described in the thread. The suggestion implies the fix is already available in recent Git releases rather than requiring new development.