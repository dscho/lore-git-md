This is a platform compatibility discussion regarding symlink handling in Git's refs fsck. The thread has evolved from Collin Funk's BSD errno translation patches to Jeff King proposing a more fundamental change in how packed-refs validation checks file types.

Jeff King suggests replacing the current `open_nofollow()` approach in `packed-backend.c` with direct `lstat()` checks, which would make the platform-specific errno translations unnecessary. His proposed change would simplify the code by handling all file type checks (including symlinks) in one operation, though it trades off some atomicity that isn't critical for fsck operations.

Shejialuo responds that while they originally used `open_nofollow()` to minimize race conditions, they acknowledge Jeff's point about the remaining races and agree the simpler `lstat()` approach may be better. They also discuss an unrelated observation about using `strbuf` versus `mmap` for large packed-refs files, with Shejialuo seeking more context about GitHub's use cases to determine if optimization is needed.

The discussion presents two viable solutions to the BSD errno divergence issue: the already-implemented errno translation approach versus Jeff's proposed architectural change that would eliminate the platform-specific code entirely. The thread remains open with no clear resolution yet between these approaches.