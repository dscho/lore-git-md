This is a test patch in the v2 series addressing memory leaks in pack-bitmap.c's boundary traversal, now responding to shell portability feedback. The patch adds test coverage for the bitmap cleanup fix from the first patch in the series, but was flagged for using non-portable `export VAR=value` syntax in the test setup.

Eric Sunshine precisely identified the issue - Git's test suite convention requires separate variable assignment and export (`VAR=value && export VAR`) for portability. The test-lint target catches this specific syntax violation. Lidong Yan acknowledges the feedback and will update the test accordingly.

The core test logic remains sound - it verifies the memory leak fix when pseudo-merges are used in boundary traversal. This is purely a test-suite implementation detail that needs alignment with Git's shell scripting standards. The fix will maintain consistency with how environment variables are handled elsewhere in the test suite while preserving the test's ability to verify the bitmap cleanup behavior.

This follows Junio Hamano's earlier feedback about test formatting, completing the polish needed for this series. The memory leak fix itself is technically sound and well-tested, with this being the final piece of test infrastructure cleanup before the series can be considered ready for integration in the next cycle.