Platform compatibility follow-up for BSD CPU detection. Brad Smith pings the thread about his previously submitted patch to fix CPU count reporting on OpenBSD/NetBSD systems when SMT is disabled. The original change modifies `online_cpus()` in `thread-utils.c` to properly handle the BSD-specific `HW_NCPUONLINE` sysctl parameter, ensuring accurate reporting of active CPUs rather than physical cores.

The thread context shows this was already reviewed and approved by Patrick Steinhardt, who confirmed it aligns with OpenBSD documentation and correctly implements the intended behavior. The ping suggests the patch may be awaiting final merge - it's a straightforward, low-risk change that only BSD systems will encounter, following the established fallback chain of HW_AVAILCPU -> HW_NCPUONLINE -> HW_NCPU.

Key technical details remain unchanged from the original submission: the modification to the sysctl parameter selection logic in thread-utils.c, with the BSD-specific handling properly nested within the existing HW_AVAILCPU conditional. The patch maintains identical behavior on non-BSD platforms while fixing the core count reporting issue on affected BSD systems.