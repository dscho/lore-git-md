This is a documentation patch fixing formatting in the `git-column` man page. 

The patch corrects an issue introduced in commit 227c4f33a03 where list continuation markers (`+`) were incorrectly used at the top level of example blocks in the AsciiDoc documentation, causing literal `+` characters to appear in rendered output. The fix simply removes these markers while maintaining the blank lines around the example blocks.

The change was part of a broader effort to standardize block delimiters in documentation, but this particular case required different handling. The patch is straightforward and correctly applies the fix to three example blocks in the man page.

This appears to be a clean, well-scoped documentation improvement that follows the project's style guidelines. The commit message clearly explains both the problem and solution, referencing the earlier commit that introduced the issue. The change is unlikely to be controversial as it purely affects documentation formatting.