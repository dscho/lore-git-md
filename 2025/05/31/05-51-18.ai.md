This is a follow-up in the ongoing discussion about modifying `git add -p` to handle merge conflicts. Jarrad Whitaker proposes a thought experiment about having `git add -p` automatically resolve conflicts by discarding the stage 1-3 versions (base/ours/theirs) when first invoked on a conflicted file. 

The key technical question raised is whether this automatic conflict resolution would be too dangerous/unexpected for users, as it would immediately mark the file as no longer conflicted. This suggests a possible implementation path that avoids the architectural challenges of maintaining both conflict state and partial resolution simultaneously, though at the cost of potentially surprising behavior.

The email doesn't propose a full solution but rather explores a boundary case of the problem space - showing the tradeoff between implementation complexity (maintaining parallel conflict state) and user experience (automatic resolution that might be unexpected). This keeps the discussion moving forward while awaiting input from add-p expert Phillip Wood.

The proposal touches core index handling and would impact commands expecting atomic conflict resolution via `git add`. The thread remains in exploratory phase, with no clear consensus yet on whether to pursue index modifications or consider alternative approaches like this more aggressive resolution strategy.