This is a discussion about the dual-backend maintenance question in the `imap-send` series. Junio Hamano responds to Phillip Wood's earlier question about the rationale for keeping both OpenSSL and libcurl backends, noting Wood's suggestion to simplify by removing the OpenSSL code entirely.

The thread context shows this is part of a larger v8 series adding OAuth2.0 support and folder listing capabilities to `imap-send`. While most technical issues have been resolved through iteration, this architectural question about maintaining two backends remains open. Junio explicitly asks if there are downsides to dropping OpenSSL support, echoing Wood's concerns about code complexity and security correctness.

Key technical points from the thread context that inform this discussion:
- The series already implements all new features (OAuth2.0, folder listing) for both backends
- OpenSSL-specific bugs were recently fixed (configuration parsing, CRAM-MD5 memory handling)
- The codebase currently stands at 500+ lines changed across both implementations

This email doesn't propose a solution but signals the maintainer's openness to simplifying the architecture by removing the OpenSSL backend, pending identification of any blocking concerns. The question touches on broader maintenance tradeoffs between implementation flexibility and codebase complexity.