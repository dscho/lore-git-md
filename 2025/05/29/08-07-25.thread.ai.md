Thread summary:

Build system modernization for git-gui, introducing Meson support while maintaining Makefile compatibility. The series refactors the existing Makefile-based build into modular components, extracting build steps into standalone scripts (git-gui.sh, git-gui-macos.sh) and replacing GIT-GUI-VARS with GIT-GUI-BUILD-OPTIONS for better configuration handling. Key changes include modifications to GIT-VERSION-GEN for out-of-tree builds and new meson.build files for the main project, library, and translations. The work touches core build infrastructure (Makefile, build scripts) without affecting runtime behavior, following Git's broader build system modernization trend. The series appears carefully structured with each commit focusing on a specific refactoring step leading to Meson integration.