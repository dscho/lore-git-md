This is the second patch in a bugfix series addressing memory leaks in pack-bitmap.c. The patch adds test infrastructure and a test case for corrupt bitmap handling, specifically:

The patch introduces a new test helper `test-tool bitmap list-commits-offset` that can output the exact file positions of bitmap entry fields (commit OID, xor_offset, flags, and EWAH bitmap data). This enables targeted corruption of specific bitmap fields for testing cleanup behavior. The test case uses this to corrupt the xor_offset field of the first bitmap entry and verifies proper handling.

Junio C Hamano notes the commit message could better explain the motivation for adding test code directly in pack-bitmap.c rather than keeping it separate. The patch appears technically sound but the justification could be clearer. This complements the first patch's leak fixes by providing regression tests, though reviewers haven't raised any technical concerns about the implementation itself.

The test infrastructure looks thorough, using clang's leak sanitizer and precise file manipulation to verify proper cleanup of corrupted bitmaps. This should help catch similar leak issues in the future. The patch series as a whole appears ready for merging, with this test addition providing important validation of the fixes.