This is a discussion thread about the `imap-send` backend implementation, specifically whether to maintain both OpenSSL and libcurl backends or consolidate to just libcurl.

Junio Hamano is responding to Phillip Wood's question about why users would need to switch between OpenSSL and libcurl backends at runtime. Phillip had suggested that maintaining only the libcurl backend would simplify the code and avoid potential OpenSSL implementation concerns. Junio agrees with this perspective and asks if there are any downsides to dropping the OpenSSL backend entirely.

The conversation occurs in the context of a larger v8 patch series that has already added significant new functionality to `imap-send`, including OAuth2.0 support and folder listing capabilities. The thread history shows this has been thoroughly reviewed, with the dual-backend maintenance being one of the few remaining open questions.

This exchange suggests the maintainers are leaning toward simplifying the codebase by removing the OpenSSL backend, but are checking for any compelling reasons to keep it before making that decision. The discussion doesn't present any technical arguments for keeping OpenSSL - it appears the main consideration is whether any users would be negatively impacted by its removal.