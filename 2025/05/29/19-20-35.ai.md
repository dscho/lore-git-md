Documentation patch updating the MyFirstObjectWalk tutorial as part of the the_repository removal effort. This is part 1 of a 2-part series modernizing the tutorial.

The patch updates the walken.c code example to use a passed `struct repository*` parameter instead of the global `the_repository`, following the pattern established by commit 9b1cb50. Key changes include:
- Adding the `repo` parameter to `cmd_walken()` function signature
- Replacing `git_config()` calls with `repo_config()`
- Updating `repo_init_revisions()` call to use the passed repository parameter
- Maintaining all surrounding tutorial text to match the code changes

The changes are straightforward mechanical updates to align with current Git practices, though there appears to be a minor inconsistency in the `repo_init_revisions()` call where both `repo` and `the_repository` are passed (likely a copy-paste error that should be caught in review). The patch maintains the tutorial's explanatory flow while updating the technical content.

This is a focused change that directly supports the series' goal of modernizing the tutorial's repository handling. The patch appears technically sound but will need verification that the updated examples compile and run correctly with both build systems (a point noted in the thread's open questions).