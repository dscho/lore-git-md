Junio C Hamano raises important questions about a proposed optimization to the promisor-remote subsystem. The original change aimed to remove redundant promisor object checks during failed fetches, claiming callers already filter out local objects. However, Junio points out this assumption may not hold - callers appear to pass any missing objects, not just promisor ones. 

The discussion reveals two key concerns: First, the behavior change could be significant - the code will now die on any failed fetch, not just for promisor objects. Second, if `is_promisor_object()` is indeed slow, perhaps the deeper issue is its implementation rather than its usage here. Junio suggests caching promisor object information like reverse indexes.

This appears to be a thoughtful critique of the optimization's assumptions and potential side effects. The thread now awaits input from the CC'd reviewers (Christian and Jonathan Tan) about whether the behavior change is acceptable and if there might be better ways to address the performance issue. The patch may need revision based on this feedback.