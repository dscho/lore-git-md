Bugfix patch in the ongoing `imap-send` authentication series, specifically addressing a memory leak in CRAM-MD5 authentication failure paths. This is patch 4/9 in v8 of the series, which as noted in the thread context has been through extensive review iterations and includes both feature additions (OAuth2.0 support) and various fixes.

The patch corrects a memory leak in `auth_cram_md5()` where the allocated `response` buffer wasn't freed if the socket write operation failed. The fix adds the missing `free(response)` call in the error path before returning. This completes the comprehensive CRAM-MD5 memory fixes mentioned in the thread summary, ensuring all error paths properly clean up allocated resources.

The change is minimal (3 lines added, 1 removed) and focused, directly addressing one of the technical correctness issues in the larger series. The thread context shows this is part of broader work to improve IMAP authentication reliability, with this particular fix being a straightforward improvement that reviewers are likely to accept given the series' maturity. The patch maintains consistency with the series' dual-backend approach (OpenSSL and libcurl) by fixing a shared authentication path.