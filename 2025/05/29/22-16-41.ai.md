Feature addition for R language support in Git's diff highlighting. This v2 patch from Rodrigo Carvalho implements R programming language function recognition in `userdiff.c`, addressing all feedback from the initial submission. The patch now handles both `<- function` and `= function` assignment syntax variants, uses the default word-diff pattern `[^ \t]+` as suggested by maintainers, and includes comprehensive test coverage.

The implementation inserts the R pattern between Python and Ruby patterns in `userdiff.c`, with a function header regex that matches both assignment styles. Three test files demonstrate the functionality: `r-noindent` shows basic function definition, `r-indent` adds proper indentation, and `r-indent-nested` verifies nested function handling. The tests include `ChangeMe()` markers likely for verifying hunk header behavior.

This appears ready for inclusion, having addressed all technical concerns raised in the initial review. The patch follows Git's established pattern for language support additions, with clear test coverage of edge cases. The only remaining question is whether the function header regex might need future refinement based on real-world usage, but the current implementation provides a solid baseline for R language support in Git diffs.