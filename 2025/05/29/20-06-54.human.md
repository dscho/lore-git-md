Review feedback on MyFirstObjectWalk tutorial updates. This is a documentation patch series (2 parts) that modernizes the tutorial to use `struct repository*` parameters instead of `the_repository` global and adds meson build system support.

Karthik Nayak provides a positive review of Lucas Seiki Oshiro's initial submission, noting the updates are valuable since these tutorial documents often get overlooked by regular contributors. The review confirms the correct approach to parameterizing `cmd_walken()` with `struct repository*` and the dual build system support.

A few minor issues were identified: a parameter inconsistency in `repo_init_revisions()` where both `repo` and `the_repository` are passed, commit reference formatting that needs adjustment, and a suggestion to better explain the `git_config` to `repo_config` change by referencing commit 036876a106 more clearly. There's also an optional wording suggestion about build system naming in the tutorial text, marked as a "super nit" by the reviewer.

The review maintains a constructive tone, with Karthik noting the patches look good overall while providing specific actionable feedback. This continues the thread's focus on keeping the new contributor tutorial up-to-date with current Git practices around repository parameterization and build system support.