Build system modernization for git-gui, adding Meson support alongside the existing Makefile-based build. This is a significant infrastructure change that:

1. Introduces a new Meson build system configuration while maintaining compatibility with the existing Makefile
2. Extracts several build steps into standalone scripts (generating the git-gui executable, tclIndex, macOS wrapper/app)
3. Replaces GIT-GUI-VARS with GIT-GUI-BUILD-OPTIONS for better build-time configuration
4. Modifies GIT-VERSION-GEN to work better with out-of-tree builds
5. Adds Meson build files for the main project, library, and translations

The changes appear to be a careful modernization effort, splitting monolithic Makefile logic into more modular components while adding Meson support. The patch series touches core build infrastructure but doesn't modify runtime behavior. Key files affected include the Makefile (heavily refactored), new *.sh scripts for build steps, and new meson.build files throughout the project structure. The work seems well-structured with each commit focusing on a specific refactoring step, culminating in the Meson integration. This aligns with Git's broader build system modernization efforts.