Feature patch in the ongoing `imap-send` series (v8 8/9), adding user-facing output about the destination mailbox during message sending. This builds on the comprehensive IMAP improvements already in the thread, which include OAuth2 support, authentication fixes, and new folder management features.

The change modifies the progress output during `imap-send` operations to include the destination folder name, making it clearer where messages are being delivered. Previously, users would only see "Sending N message(s)", while now it shows "Sending N message(s) to FOLDER folder...". The modification applies to both the OpenSSL and libcurl backends, with identical output formatting.

Technical implementation is straightforward - two fprintf() calls are updated to include the server->folder parameter. The change touches only the user interface layer, with no impact on the underlying IMAP protocol handling or authentication flows. This appears to be a pure quality-of-life improvement following the series' theme of enhancing user feedback, joining earlier changes like port-inclusive credential prompts and standardized error messages.

The patch is small (6 lines changed) and focused, with no apparent technical concerns. It logically fits with the series' progression from core functionality (OAuth2, authentication fixes) to user experience polish. Since this is the second-to-last patch in v8, it suggests the series is nearing final form after extensive review iteration.