Platform compatibility fix for reftable's UNUSED macro, following up on NonStop build failure.

Junio C Hamano acknowledges Carlo Marcelo Arenas Belón's patch to make `REFTABLE_UNUSED` C99-compatible by conditionally applying the GNU `__attribute__((__unused__))` only when `__GNUC__` is defined. The patch directly addresses Randall S. Becker's report of build failures on NonStop platforms where the GNU attribute syntax isn't supported.

Hamano notes the solution follows Git's historical approach to such compatibility issues, though he points out the main Git codebase uses a more complex version of this macro in `compat/posix.h` that includes deprecation warnings. He suggests the simpler approach may be sufficient given modern compiler requirements, and indicates he will queue the patch.

Key technical details:
- Original issue introduced in f93b2a0424 (reftable/basics: introduce `REFTABLE_UNUSED`)
- Macro location: `reftable/basics.h`
- Alternative implementation in main codebase at `compat/posix.h` with deprecation attributes
- Patch uses minimal conditional compilation (`#ifdef __GNUC__`)

This appears to be a straightforward, uncontroversial fix that maintains compatibility while not introducing new warnings or complexity. The thread shows consensus on the approach, with Hamano's approval suggesting this will be merged without further discussion needed.