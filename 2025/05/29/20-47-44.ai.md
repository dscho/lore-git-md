This is a correctness patch in the ongoing MIDX refactoring series, addressing potential array access issues in multi-pack-index handling. Taylor Blau introduces a new helper function `nth_midxed_pack_name()` to safely access pack names across MIDX chains, replacing direct array accesses that could be problematic with incremental MIDXs.

The key technical change is the new helper function that properly handles pack ID translation between global and local indices when MIDX files are chained. While presented as a code cleanup, Junio Hamano's review questions suggest it may actually fix real bugs - the original direct array accesses in pack-bitmap.c could be incorrect when `i` exceeds `num_packs` in a chained MIDX scenario. The patch updates four call sites, with the bitmap-related changes being particularly important for correctness.

The implementation maintains the existing behavior where safe (like in test-read-midx.c) while preventing potential out-of-bounds accesses in bitmap handling code. This continues the thread's pattern of incrementally improving MIDX safety through better encapsulation, building on earlier work around pack ID handling and negative lookup caching. The change is well-contained but touches sensitive areas, so careful review of the bitmap cases is warranted given the potential for subtle bugs in the original code.