This is a test infrastructure discussion about path resolution in Git's test suite, specifically addressing platform-specific behaviors around symlinks and canonical paths.

Mark Mentovai responds to Junio Hamano's proposal to modify `t/test-lib.sh` to use `cd -P` for canonical path resolution during test setup. While acknowledging this would fix the immediate macOS test failure (where `pwd` and Git's `strbuf_realpath()` disagree about symlink-containing paths), Mark raises concerns about the broader implications.

He argues that forcing all tests to run with canonicalized working directories might hide bugs that only appear with non-canonical paths - including potential issues in Git's own path canonicalization logic. This suggests a preference for more targeted fixes (like his original `pwd -P` change) over sweeping infrastructure modifications.

The exchange shows the ongoing tension between quick fixes and architectural solutions in the thread. While Junio's approach would comprehensively address the immediate issue, Mark cautions about unintended consequences that could affect test coverage. The discussion remains technical and constructive, with both contributors focused on maintaining test reliability across platforms while preserving the ability to catch path-related bugs.