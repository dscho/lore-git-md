K Jayatheerth's 2-patch series improving submodule configuration management is nearing completion after extensive review. The first patch prevents moved submodule config overwrites when adding new submodules at the same paths, while the second eliminates redundant `submodule.<name>.active` entries when paths match existing patterns. Both patches have thorough test coverage in t7400 and t7413.

Junio C Hamano has already approved patch 1/2 with only documentation suggestions remaining. Patch 2/2 has evolved through eight iterations to implement a wildcard-based approach that maintains backward compatibility while cleaning up redundant configurations. Recent discussion has focused on test hygiene, with Junio agreeing with D. Ben Knoble that using `test_must_fail` rather than bare `!` operators would make the tests more robust by catching abnormal failures.

The series appears technically complete, with all substantive feedback addressed. The remaining items are minor documentation and test improvements that don't affect the core functionality. The thread is now in its final polishing phase, awaiting either final approval from maintainers or any last-minute adjustments before potential merging.