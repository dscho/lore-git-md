This thread discusses K Jayatheerth's 2-patch series improving submodule configuration management. The first patch, already approved by Junio, prevents moved submodule config overwrites when adding new submodules at the same paths. The second patch, which removes redundant `submodule.<name>.active` entries, has been the focus of recent discussion.

The author has provided a detailed technical response to Junio's review, explaining why they chose a more focused implementation for the second patch rather than using the general `is_submodule_active()` helper as Junio suggested. They identified a specific test case where Junio's approach would fail (when global active config is absent) and demonstrated how their solution better handles this edge case while still meeting the goals of keeping config clean and ensuring new submodules are active by default.

While this creates some logic duplication, the author has made a strong case for their approach being more appropriate for this specific use case in `git submodule add`. The discussion remains technical and constructive, with both parties clearly understanding the tradeoffs involved. The series appears ready for merging once Junio reviews this latest explanation and confirms acceptance of the implementation approach.