This thread concerns K Jayatheerth's series improving submodule configuration management. The first patch, already approved by Junio, prevents `.gitmodules` overwrites when adding new submodules at paths that previously hosted other submodules. D. Ben Knoble recently provided user experience feedback on this patch, suggesting the `--force` behavior (which auto-increments submodule names during conflicts) be better documented in the manual pages, though they raised no technical concerns about the implementation.

The second patch, which eliminates redundant `submodule.<name>.active` entries when paths match existing patterns, has seen more extensive review. The current version implements a wildcard-based approach with thorough test coverage. Knoble earlier suggested some documentation and test improvements for this patch but confirmed the core functionality works as intended without disrupting simple configurations. 

Junio is awaiting final review from submodule experts before merging the series. The first patch remains approved, while the second appears technically sound but awaits confirmation that its wildcard-matching approach aligns with project preferences. All test coverage is in place, with only minor documentation suggestions outstanding.