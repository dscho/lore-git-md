Bugfix follow-up in the `imap-send` series. Ben Knoble points out a potential memory safety issue in Eric Sunshine's CRAM-MD5 memory leak fixes - the code calls `strlen(response)` after already freeing the `response` buffer. This would lead to undefined behavior if the socket write fails.

The observation comes as a quick but important catch in the final stages of a comprehensive `imap-send` overhaul that has already gone through multiple rounds of review. While the main series has addressed all major authentication and configuration issues (including other memory management problems), this appears to be one last edge case that slipped through.

Given the thoroughness of the prior reviews and the straightforward nature of this bug (clearly an ordering mistake rather than a deeper architectural issue), this will likely be a simple fix to swap the `free()` and `strlen()` calls. The thread context shows the series is otherwise in good shape, with all major technical concerns already addressed across the 4 iterations.