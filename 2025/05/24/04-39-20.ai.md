This is a test infrastructure discussion about path resolution in Git's test suite, specifically addressing the ongoing thread about macOS and Windows maintenance tests in t7900-maintenance.sh. Mark Mentovai responds to Junio Hamano's proposal to canonicalize paths earlier in test setup by using `cd -P` in `t/test-lib.sh`.

Mark acknowledges that Junio's approach would work as an alternative fix for the original issue (symlink path resolution in macOS tests), but raises concerns about its broader implications. He argues that forcing all tests to run from canonicalized paths might hide bugs that only manifest with non-canonical working directories, including potential issues in Git's own path canonicalization logic (`strbuf_realpath`).

The email continues the technical debate about the tradeoffs between:
1) Targeted fixes (like Mark's original `pwd -P` change for specific tests)
2) Broader infrastructure changes (Junio's `cd -P` proposal)
3) The risk of masking edge cases in path handling

Mark's position suggests a preference for more surgical fixes that preserve the ability to test path resolution edge cases, while Junio's approach favors consistent test behavior across all platforms. The discussion remains technical and collegial, with Mark carefully evaluating the architectural implications of the proposed change.