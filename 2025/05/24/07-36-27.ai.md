This is a bugfix patch addressing submodule configuration management, part of a 2-patch series (1/2) from K Jayatheerth. The patch prevents silent overwriting of .gitmodules entries when reusing paths from moved submodules.

The change modifies `module_add()` in `builtin/submodule--helper.c` to detect when a new submodule's path-derived name conflicts with an existing submodule's configuration. When such a conflict is found, it either errors out (default) or auto-increments the name (with --force). The implementation checks via `submodule_from_name()` and handles conflicts by either dying or generating a new unique name like `child2`.

Test coverage in `t/t7400-submodule-basic.sh` demonstrates the problem scenario: moving a submodule then attempting to add a new one at the original path. The test verifies the new protection works while preserving the moved submodule's config. The change appears focused and well-contained, with clear motivation about preventing configuration loss. Reviewers will likely examine the force-mode behavior and whether the error messaging could be more helpful, but the core logic seems sound. The patch builds on the thread's established context about submodule configuration safety.