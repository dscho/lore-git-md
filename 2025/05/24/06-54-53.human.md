This is a follow-up to the submodule active flag optimization series, now in its 6th iteration. The patch focuses on avoiding redundant `submodule.<name>.active=true` config entries when a submodule's path already matches an existing `submodule.active` pattern.

The author, Jayatheerth K, reports having added tests for the active flag optimization behavior (test 9 in t7413) and introduced an additional test case (test 10). This addresses maintainer feedback requesting clearer test coverage for the optimization behavior. The implementation now uses a dedicated `submodule_active_matches_path()` helper function to check path matches against active patterns.

The changes appear to complete the technical implementation of this optimization, which was previously separated from path safety checks for conceptual clarity. With the test coverage now in place and the maintainer's requests satisfied, this patch seems ready for final review. The only remaining item noted in the thread is documentation of the optimization rationale, though that may be handled separately.

This represents the final piece of a well-reviewed series that improves submodule configuration efficiency by eliminating unnecessary config entries when patterns already cover the desired behavior.