This is a technical follow-up in the xdiff buffer underflow bugfix discussion. René Scharfe provides additional analysis of Alex Guo's proposed fix for potential buffer underflows in `xdl_build_script()`, but now concludes the original issue may not be as severe as initially thought.

Scharfe retracts his earlier concern about counters going below -1, realizing the loop indices should reach zero simultaneously when walking backwards through the diff records. He notes that while the code style made the issue appear more dangerous (missing braces in his test code led to false positives), proper testing with AddressSanitizer doesn't actually trigger any out-of-bounds access.

The email suggests the core question may instead be about the loop's logical correctness - whether it should use `||` (continue until both sides are exhausted) or `&&` (stop when either side finishes). This shifts the discussion from a safety fix to potentially improving the algorithm's robustness. Scharfe maintains his nit about inconsistent indentation in the patch but no longer sees an urgent buffer safety issue needing fixing.

The thread shows how careful code review can surface deeper questions about algorithm design, even when initial concerns prove unfounded. The patch may need reworking to address the more fundamental loop logic question rather than just adding bounds checks.