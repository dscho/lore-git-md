This is a final revision (v7) of a two-patch series addressing submodule configuration safety and optimization, building on extensive prior discussion. The series combines two key improvements to `builtin/submodule--helper.c`:

1. **Path reuse protection**: Fixes a bug where moving a submodule and reusing its original path could silently overwrite the moved submodule when adding a new one. The solution uses `submodule_from_name()` to detect name/path mismatches, with `--force` enabling auto-incremented names (foo, foo2, foo3) as a recovery path.

2. **Active flag optimization**: Avoids writing redundant `submodule.<name>.active=true` entries when the path already matches `submodule.active` patterns, using a new `submodule_active_matches_path()` helper that leverages `wildmatch()` for pattern checking.

The implementation shows careful attention to maintainer feedback with:
- Expanded test coverage in t7400-submodule-basic.sh and t7413
- Strict memory management using `strbuf` and `xstrfmt`
- Clear separation of concerns between path safety and active state logic
- Documentation updates in RelNotes

Notable technical details for future reference:
- The path safety check occurs in `module_add()`
- Pattern matching uses Git's standard `wildmatch()` semantics
- Tests verify both conflict detection and `--force` fallback behavior
- The active state optimization specifically avoids config bloat

The v7 change is purely cosmetic (converting test indentation from spaces to tabs), indicating the technical content is now considered final. With all maintainer requests addressed and comprehensive test coverage, this appears ready for merging pending final documentation of the optimization rationale. The thread demonstrates Git's iterative review process where parallel concerns (safety vs optimization) were addressed in coordinated patch series.