Bugfix for submodule configuration management, part 2/2 in a series. This patch addresses redundant `submodule.<name>.active` entries being created when the submodule's path is already covered by existing `submodule.active` patterns.

The change introduces a new helper function `submodule_active_matches_path()` that checks if a given path matches any existing active patterns using wildmatch. This replaces the previous logic in `configure_added_submodule()` that would always write an active entry unless the submodule was explicitly inactive. Now it only writes the entry when no existing pattern covers the path.

Key technical details:
- New helper function in `builtin/submodule--helper.c` that performs the pattern matching
- Modified `configure_added_submodule()` to use this helper before writing active entries
- Added test case in `t/t7413-submodule-is-active.sh` verifying that submodules added under matching patterns don't get redundant active entries
- Maintains backward compatibility while reducing config file clutter

This completes the series' goal of making submodule configuration management more robust and less prone to unnecessary entries. The implementation appears clean and well-tested, with clear commit message explaining the motivation and changes. The thread shows careful consideration of edge cases and pattern matching behavior.