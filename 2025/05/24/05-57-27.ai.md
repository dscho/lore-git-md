Bugfix follow-up analyzing xdiff buffer underflow issue. René Scharfe responds to Alex Guo's initial patch fixing buffer underflows in `xdl_build_script()` by providing deeper technical context about the xdiff internals.

Key technical clarifications: The rchg arrays actually have extra allocated entries at both ends, making -1 accesses technically in-bounds. However, the counters can go below -1 when files have different numbers of records (nrec), which is the real danger. Scharfe notes AddressSanitizer doesn't catch this by default but provides a manual check that can trigger the overflow detection.

Proposed direction: Suggests the loop condition using `||` might be incorrect - it should probably use `&&` since we can stop when either file's records are exhausted. Also notes a minor indentation issue in the patch.

The response provides important context about xdiff's memory layout that wasn't in the original bug report, while raising questions about the fundamental loop logic. This moves the discussion from "does this fix the symptom?" to "is this the right way to structure the loop?" The technical depth suggests the reviewer is familiar with xdiff's internals.