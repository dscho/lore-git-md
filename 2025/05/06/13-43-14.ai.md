This is a discussion about platform compatibility and atomicity in the context of the packed-refs fsck operations, continuing the thread about BSD errno handling.

The email from shejialuo responds to Jeff King's points about the tradeoffs in using `open_nofollow()` for symlink detection in packed-refs operations. Shejialuo explains their initial approach of checking file type before opening, noting the potential race condition if the file becomes a symlink between those operations. They acknowledge that while `open_nofollow` provides some atomicity benefits on certain platforms, it introduces compatibility issues with BSD errno values.

The discussion reveals that the current implementation requires both `open_nofollow` and a subsequent `stat` call, creating some redundancy. However, with Collin Funk's BSD errno translation solution now ready for merge, shejialuo confirms they won't change this logic and will instead focus on their parallel work optimizing packed-refs reading using `mmap`.

Key technical points:
- The thread has consensus on using `lstat()` for portable symlink detection
- `open_nofollow()` provides limited atomicity but has platform-specific errno behavior
- Current implementation requires both symlink checks (via `open_nofollow`) and file type verification (via `stat`)
- Collin's BSD errno translation patch resolves the compatibility issues
- shejialuo is now shifting focus to mmap-based optimization work

The email confirms the BSD errno handling is considered resolved, with attention turning to the performance optimization track of this effort. The discussion also surfaces ongoing questions about the ideal balance between atomicity guarantees and implementation complexity in filesystem operations.