This thread follows Patrick Steinhardt's ongoing 17-patch series refactoring Git's object database (ODB) subsystem to eliminate reliance on the `the_repository` global variable. The latest patches focus on methodically renaming functions to use consistent `odb_` prefixes and explicit ODB parameters.

Patch 13/17 renamed `oid_object_info()` to `odb_read_object_info()`, updating 48 call sites while maintaining identical behavior through compatibility wrappers. Now in patch 14/17, Steinhardt continues this pattern by renaming `repo_read_object_file()` to `odb_read_object()`, affecting 47 files across the codebase. The function's signature changes to take a `struct object_database*` parameter instead of `struct repository*`, better reflecting its ODB-focused operation while preserving behavior through another compatibility wrapper.

The series has now converted all major subsystems handling object database operations, with over 250 files modified. The infrastructure consistently uses explicit repository parameters and standardized naming, though documentation for the new `odb_*` functions remains outstanding. Performance validation across various workflows (alternate lookups, submodule operations, clone/fsck/repack) is still pending before the ODB abstraction can be considered complete. The changes are mechanical but thorough, systematically eliminating implicit global state dependencies while maintaining stability through careful compatibility measures.