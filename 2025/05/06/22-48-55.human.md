This is a test fix addressing flakiness in t6011-rev-list-with-bad-commit.sh, part of an ongoing discussion about test reliability and portability. Junio C Hamano responds to Torsten Bögershausen's earlier report by fixing a test that was converted from Perl to sed in a previous refactoring (commit cdbdc6bf). The immediate issue is that the test needs to make packfiles writable before modifying them, which the patch addresses by adding a `chmod +w`.

The email reveals deeper concerns about the test's approach: the original Perl version could modify read-only files directly, while the sed version requires explicit permission changes. Junio notes this is one of two issues with the test conversion - the other being sed's undefined behavior with binary files (packfiles in this case). While this patch takes the conservative path of making the test work as-is, Junio hints that a more thorough solution might involve reverting some of the Perl-to-sed conversions or standardizing on `mv -f` instead of `chmod`.

The patch itself is straightforward and clearly correct for its limited scope, but the surrounding discussion suggests this may not be the final word on how Git's tests should handle file modifications. The thread remains open on whether sed is the right tool for binary file manipulation in tests.