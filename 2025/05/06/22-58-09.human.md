This is a platform compatibility discussion regarding errno handling in Git's filesystem operations. The thread has largely settled on a solution for BSD variants' errno behavior with O_NOFOLLOW in packed-refs fsck operations, with Collin Funk's patches handling NetBSD's EFTYPE and FreeBSD's EMLINK returns for symlink operations.

In this specific email, Junio Hamano and Jeff King discuss the broader implications of errno consistency across platforms and the use of `open_nofollow()`. Jeff notes that while the current implementation has a comment warning about potential race conditions in the fallback code, the immediate issue is about making errno responses more portable. Both agree that while maintaining errno consistency across platforms is somewhat messy, it's likely unavoidable in practice.

The discussion also touches on different callers of `open_nofollow()` (in attr.c, dir.c, and mailmap.c) and their varying needs around atomicity versus errno checking. The thread appears to be winding down, with consensus that the BSD errno translation patch is ready to merge while acknowledging the inherent challenges in perfect platform compatibility.