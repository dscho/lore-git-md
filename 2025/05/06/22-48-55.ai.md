This is a test fix addressing flakiness in t6011-rev-list-with-bad-commit.sh, part of an ongoing discussion about test reliability and portability. Junio Hamano responds to Torsten Bögershausen's earlier fix by pointing out a deeper issue with the test's conversion from Perl to sed for binary file manipulation.

The patch makes the test more robust by adding `chmod +w` before editing packfiles, matching the pattern used elsewhere in the test suite. This addresses the immediate filesystem permission issue that was causing flakiness. However, Hamano notes this is only a partial solution - the test still relies on undefined behavior by using sed to edit binary packfiles, a practice introduced in commit cdbdc6bf when Perl was replaced with sed.

The email reveals two distinct concerns: the now-fixed permission handling (via `chmod +w`), and the lingering question of whether sed is appropriate for binary file manipulation. While current sed implementations (GNU/BSD) appear to handle it, the behavior is technically unspecified. Hamano suggests the broader Perl-to-sed conversion may need revisiting, but treats that as a separate issue from this immediate fix.

Key technical details: the patch modifies packfile corruption simulation in t6011 by ensuring write permissions before sed/mv operations. The test's core purpose (verifying `git rev-list` with corrupted commits) remains unchanged. The discussion references prior work (cdbdc6bf) and notes portability considerations around sed's binary file handling.