This is a code review discussion about a helper function in the ongoing worktree pruning feature series. Eric Sunshine responds to Christian Couder's suggested refactoring of `get_worktree_names()` to reduce `goto` usage, agreeing with the general direction but questioning whether the function should exist at all.

The discussion focuses on the implementation details of a helper function that retrieves worktree names from the repository's worktrees directory. Christian proposed eliminating multiple `goto` statements by restructuring the control flow, while Eric had previously made a similar suggestion and also raised a more fundamental question about whether this helper function is necessary in the first place.

Key technical points mentioned include:
- Current implementation uses `goto` for error handling and cleanup
- Alternative control flow proposed by Christian that avoids `goto`
- Eric's prior suggestion for a more concise version
- Underlying question about whether this helper function should exist at all in the public API

This email doesn't introduce new functionality but rather discusses code organization and API design questions that emerged during review. The discussion is part of the broader effort to finalize the worktree pruning maintenance task implementation, with reviewers now focusing on fine-grained implementation details after the core functionality was approved.