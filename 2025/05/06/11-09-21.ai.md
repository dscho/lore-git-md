Refactoring patch in Patrick Steinhardt's ongoing series to remove `the_repository` dependencies from the object database (ODB) subsystem. This is patch 8/17, focusing on alternate object database handling.

The patch converts alternate-related functions to take an explicit `struct object_database*` parameter instead of implicitly using `the_repository`. Key changes include:
- Renaming `add_to_alternates_file()` to `odb_add_to_alternates_file()`
- Renaming `add_to_alternates_memory()` to `odb_add_to_alternates_memory()`
- Renaming `prepare_alt_odb()` to `odb_prepare_alternates()`
- Renaming `has_alt_odb()` to `odb_has_alternates()`
- Updating all callers across 14 files including clone, fsck, repack, and submodule operations

The changes maintain existing behavior while removing global state dependencies. The author notes the current implementation still relies on the repository's object path, which will be addressed in future work. The patch follows the established pattern of the series, with mechanical updates to function signatures and callers while preserving the underlying logic.

Affected areas include alternate handling during clone operations, fsck checks, repack bitmap creation, and submodule ODB registration. The changes appear straightforward but reviewers should verify alternate handling remains correct in edge cases, particularly around nested alternates and error paths.