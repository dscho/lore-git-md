Performance optimization patch for xdiff's `--minimal` mode, now concluded with maintainer approval. This final email from Junio C Hamano confirms Phillip Wood's review approval of Niels Glodny's v3 patch, which disables the `cleanup_records` heuristic when `--minimal` is explicitly requested via `XDF_NEED_MINIMAL` flag.

The technical implementation remains unchanged from previous versions: deriving `need_min` from `cf->flags`, modifying marking logic when `need_min` is true, and using the new test script `t4071-diff-minimal.sh`. Benchmark results continue to show the 1.3% diff size reduction with no statistically significant runtime impact (`2.363s ± 0.023s` vs `2.362s ± 0.035s`).

With Phillip Wood's final approval and Junio's acknowledgment, this thread is now fully concluded. The patch has addressed all technical feedback across three iterations, demonstrated stable performance characteristics, and received all necessary approvals for potential merging. The change remains narrowly scoped to only affect `--minimal` output where shortest-possible diffs are the expected behavior.