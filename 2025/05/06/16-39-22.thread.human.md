This thread documents the optimization work for memory usage in Git's packed-refs backend, particularly enabling mmap for large packed-refs files during fsck operations. The work originated from Jeff King and Patrick Steinhardt's suggestions, with refinements guided by Junio Hamano through multiple iterations (v1-v3).

The latest patch refactors the buffer allocation logic by extracting it into a new helper function `allocate_snapshot_buffer()`, enabling reuse in fsck operations while maintaining all existing behavior. This structural change (54 lines moved, net +10/-0) preserves the current mmap/read selection based on file size and platform considerations, including Windows compatibility. The refactoring enables the upcoming fsck optimization to reuse this logic cleanly.

The series is now complete with all technical considerations addressed, including:
1. Fixing fsck behavior for empty packed-refs files
2. This refactoring to share buffer allocation logic
3. Implementing mmap-based fsck verification with proper cleanup

The changes reduce memory pressure during packed-refs verification while preserving all platform-specific handling. Jeff King and Junio Hamano have reviewed and approved the implementation, which is now poised for integration. Future work may unify more of the packed-refs parsing logic, but the current series achieves its goals with all edge cases properly handled.