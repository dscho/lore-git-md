This is part 13/17 of Patrick Steinhardt's series refactoring the object database (ODB) subsystem as part of the `the_repository` removal effort. The patch renames `oid_object_info()` to `odb_read_object_info()` and its extended variant to `odb_read_object_info_extended()`, bringing them in line with modern coding guidelines and the ODB naming scheme. 

The change is mechanical but widespread, touching 48 files across the codebase. The patch introduces compatibility wrappers to avoid breaking in-flight topics, showing consideration for ongoing work. The new names better reflect that these functions operate on the object database rather than being generic object utilities.

This continues the series' pattern of methodically updating ODB-related functions to use explicit parameters rather than relying on the `the_repository` global. The changes appear straightforward and well-contained, with no behavioral modifications - just renaming and updating callers. Reviewers should focus on ensuring all call sites were properly converted, particularly in performance-sensitive paths where the function name change might affect readability.

The patch maintains the series' high quality with thorough mechanical updates and clear commit message. It's another incremental but important step toward the larger goal of removing `the_repository` dependencies from the ODB subsystem.