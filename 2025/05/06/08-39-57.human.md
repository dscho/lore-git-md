This is a refactoring patch in Patrick Steinhardt's v4 series to decompose `git gc` functionality into standalone maintenance tasks. The patch moves rerere garbage collection code into a separate function in preparation for making it a configurable maintenance task.

Christian Couder provides a minor but thoughtful suggestion to improve the commit message structure. Rather than repeating the subject line ("Move...") as the first paragraph, he proposes framing it as: first state the future goal (adding the new maintenance task), then explain the current change (refactoring) as preparation for that goal. This follows Git's conventional "problem/solution" commit message structure more cleanly.

The change itself is straightforward - just moving code with no behavior changes - and part of the larger effort to fully replace `git gc` with granular maintenance tasks. The series is nearly ready, with only a few minor issues (like a memory leak in another patch) remaining before merge. Christian's suggestion is about commit message polish rather than technical concerns.