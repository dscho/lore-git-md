This is a refactoring patch in a completed series optimizing memory usage in the packed-refs backend. The patch extracts buffer allocation logic from `load_contents()` into a new helper function `allocate_snapshot_buffer()`, making the code more reusable and maintainable. 

The change is straightforward - it moves 54 lines of code with no functional changes, just better organization. The commit message clearly explains the motivation: to avoid repetition and enable reuse of the allocation logic during consistency checks where file opening isn't desired. 

Junio Hamano's review confirms the patch is technically sound ("trivially correct") and well-presented ("cleanly done, nicely described"). This was one of four patches that together optimize memory usage during packed-refs verification, particularly for large files. The series has already incorporated feedback from multiple reviewers and appears ready for integration.