This is a refactoring patch in Patrick Steinhardt's v4 series to decompose `git gc` functionality into standalone maintenance tasks. The patch moves rerere garbage collection code into a separate function in preparation for making it a configurable maintenance task.

Christian Couder provides minor commit message feedback, suggesting the message could better follow the "problem first, solution second" convention rather than repeating the subject line. He offers an alternative wording that first states the motivation (adding a new maintenance task) before describing the refactoring. This continues the thread's discussion about commit message structure that arose in earlier reviews.

The patch itself is straightforward - it extracts existing rerere GC logic from `gc.c` into a new function without changing behavior. This follows the same preparatory refactoring pattern seen in other patches of the series, where functionality is first isolated before being exposed as a maintenance task. The change is purely structural and maintains all existing behavior, including the child process spawning compatibility layer that the series preserves.

No technical concerns are raised about the implementation itself - the feedback is solely about commit message clarity. This suggests the patch is otherwise in good shape as part of the technically complete v4 series, which has already addressed Junio Hamano's earlier concerns about the rerere GC auto-condition heuristic.