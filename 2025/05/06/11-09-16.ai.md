This is part 3/17 in Patrick Steinhardt's series refactoring the object database (ODB) subsystem as part of the `the_repository` removal effort. Following the previous patches that renamed `struct raw_object_store` to `struct object_database` and `struct object_directory` to `struct odb_backend`, this patch renames the implementation files from "object-store.{c,h}" to "odb.{c,h}" to match the new naming scheme.

The change is purely mechanical, updating include directives in 126 files across the codebase. The patch maintains consistency with the broader refactoring direction and prepares for making the ODB more self-contained by removing `the_repository` dependencies. The rename aligns with the goal of establishing proper terminology and removing the misleading "raw_" prefix from the codebase.

This continues the pattern seen in the ref store refactoring, where implementation details are being standardized with clearer naming. The change is straightforward but low-risk since it only affects file names and includes, not actual logic. Reviewers should verify the mechanical updates across the codebase, particularly in areas dealing with alternate/submodule ODB cases.