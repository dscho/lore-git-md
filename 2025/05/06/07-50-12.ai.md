This is a refactoring patch in Patrick Steinhardt's v4 series to decompose `git gc` functionality into standalone maintenance tasks. The patch moves worktree pruning code into a separate function as preparation for adding it as a new maintenance task.

Christian Couder provides minor commit message feedback, suggesting the message should follow the conventional "problem first, solution second" structure rather than repeating the subject line. He proposes rewording to first state the goal (adding a new maintenance task) before explaining the preparatory refactoring. This is a stylistic suggestion aimed at improving clarity, not a technical concern about the code changes themselves.

The patch itself is part of the larger effort to fully replace `git gc` by implementing its remaining functionality (worktree pruning and rerere GC) as configurable maintenance tasks. This particular change extracts the worktree pruning logic from `gc.c` into a standalone function, following the pattern used for other decomposed tasks. The refactoring is straightforward and uncontroversial, with the main discussion point being commit message phrasing rather than implementation.

The thread context shows this series is technically complete aside from a known memory leak in the worktree pruning path that needs to be fixed before merging. This patch precedes the actual maintenance task implementation but establishes the necessary code structure for it.