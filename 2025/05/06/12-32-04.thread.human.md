This thread began with Torsten Bögershausen identifying a flaky test in t6011-rev-list-with-bad-commit.sh, where the test's packfile corruption step would fail intermittently due to filesystem permission issues. His solution to use `mv -f` instead of plain `mv` reliably fixed the immediate problem, making the test's intentional corruption step work consistently when verifying `git rev-list` behavior with bad commits.

Junio C Hamano has now weighed in, agreeing with the `mv -f` fix but raising a more fundamental concern. He points out that the test originally used Perl for binary file editing (via `perl -i.bak -pe`), which properly handles both permissions and the binary nature of packfiles. The switch to `sed` in commit cdbdc6bf may be problematic since `sed` behavior on binary files is technically undefined. While the immediate permission issue is resolved, the discussion has now uncovered a potentially more significant problem with the test's approach to binary file manipulation.

The thread has evolved from a straightforward permission fix to questioning whether the test needs a more thorough solution - either reverting to Perl for binary safety or finding another reliable way to corrupt packfiles. The core goal of testing `rev-list` behavior with corrupted commits remains valid, but the implementation method now appears suspect. No clear resolution has been proposed yet, but Junio's intervention suggests this may require more careful consideration than the initial quick fix.