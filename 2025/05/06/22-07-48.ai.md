This is a technical discussion about domain name validation in `git send-email`, specifically addressing RFC compliance for the `valid_fqdn` function. The exchange focuses on refining the regex pattern to properly enforce domain name syntax rules across all label positions.

Junio Hamano points out that the current implementation may incorrectly handle dash placement in domain labels. He references RFC 5321's syntax rules showing that the same constraints (no leading/trailing dashes) should apply uniformly to all subdomains, not just the first one. The example "someone.-example.com" demonstrates an invalid case where a dash appears in the wrong position in a non-initial label.

The discussion reveals a subtle but important distinction in the RFC's requirements versus the current implementation - the validation rules need to be applied consistently across all domain components rather than just the first label. This builds on earlier thread context about balancing RFC compliance with real-world email server behavior, particularly regarding edge cases like local hostnames and trailing periods.

Key technical points for future reference:
- The regex needs to enforce `Let-dig [Ldh-str]` pattern uniformly
- Current implementation may incorrectly validate dashes in non-initial labels
- RFC 5321's `sub-domain` syntax applies equally to all domain components
- Related to earlier points about `Net::Domain` limitations and server validation differences

This continues the thread's progression toward a more robust validation approach that properly handles all RFC-specified edge cases while maintaining practical compatibility with real email systems.