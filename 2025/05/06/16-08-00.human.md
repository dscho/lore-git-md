This is a code style discussion in the ongoing worktree maintenance task series. Eric Sunshine responds to Christian Couder's suggestion about reducing `goto` usage in the `get_worktree_names()` helper function, agreeing with the general direction while questioning whether the function should exist at all. 

The exchange focuses on control flow improvements for a helper function that retrieves worktree names from the repository's worktrees directory. Christian proposed eliminating multiple `goto` statements by restructuring the function to use simple conditional blocks. Eric indicates he had made a similar suggestion earlier in the thread, but raises a more fundamental question about whether this helper function is actually needed in the public API.

The discussion represents a minor stylistic point in the larger patch series that's otherwise technically complete and approved. While the control flow improvement is uncontroversial, Eric's comment about the function's necessity hints at a deeper design question about API boundaries that may need resolution before the series can be merged. This comes alongside the known memory leak issue that remains the primary blocking concern.