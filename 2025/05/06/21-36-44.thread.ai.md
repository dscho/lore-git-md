Here is the updated dense AI thread summary:

Bug report and investigation of `git pull --rebase` unexpectedly dropping commits after changing a repository's remote URL. Original report from Bill demonstrated reproducible case: base repo has A-B-C, clone_1 adds F-G, clone_2 is made from clone_1. After base adds D-E and clone_2's remote is changed to point to base, `git pull --rebase` in clone_2 drops F-G entirely instead of rebasing them. D. Ben Knoble's bisect investigation shows this behavior has existed since Git 2.0 (commit d717282532), persisting through multiple major versions (confirmed on 2.35.8 and 2.46.2).

Tuomas Ahola discovered related `git merge-base --fork-point` anomalies when changing remote URLs, showing inconsistent results between identically-configured remotes (origin vs upstream pointing to same URL). The fork-point calculation appears incorrectly tied to remote name rather than commit history, returning empty for one remote while finding commits for another. This is significant because `merge-base --fork-point` is used internally by `git pull --rebase`, potentially explaining the original commit-dropping behavior.

D. Ben Knoble's follow-up seeks to connect these findings, questioning whether the fork-point anomalies directly explain the original rebase behavior or represent a separate but related issue in remote tracking logic. The thread now has two confirmed technical manifestations of remote URL change issues: the original `pull --rebase` commit dropping and the new fork-point inconsistency.

Key technical details: manifests when changing remotes with `git remote set-url`, specific to `git pull --rebase` operation, reproducible with simple linear commits. The bisect used a modified version of the original reproduction script (available on GitHub Gist). The fork-point investigation demonstrates the underlying issue may involve how Git tracks commit relationships after remote URL changes.

Current status: confirmed as long-standing behavior (not a recent regression) with new evidence pointing to fork-point calculation issues. Open questions remain about whether these are two symptoms of the same root cause in remote tracking logic or separate bugs. Both original reporter (Bill) and new contributor (Tuomas Ahola) remain available for follow-up testing. D. Ben Knoble continues coordinating the investigation while noting his fork's branches may not be fully up-to-date for testing purposes.

[Note: The tangential build system discussion regarding macOS compilation issues with Git master (specifically around `_false_but_the_compiler_does_not_know_it` in commit 1ee85f0e21) has been resolved. Torsten Bögershausen's fix in branch `tb/macos-false-but-the-compiler-does-not-know-it-fix` works correctly, per D. Ben Knoble's confirmation. This build system issue was unrelated to the core rebase/fork-point investigation.]