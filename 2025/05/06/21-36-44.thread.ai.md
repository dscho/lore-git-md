Here is the updated dense AI thread summary:

Bug report and investigation of `git pull --rebase` unexpectedly dropping commits after changing a repository's remote URL. Original report from Bill demonstrated reproducible case: base repo has A-B-C, clone_1 adds F-G, clone_2 is made from clone_1. After base adds D-E and clone_2's remote is changed to point to base, `git pull --rebase` in clone_2 drops F-G entirely instead of rebasing them. D. Ben Knoble's bisect investigation shows this behavior has existed since Git 2.0 (commit d717282532), persisting through multiple major versions (confirmed on 2.35.8 and 2.46.2).

New findings from Tuomas Ahola reveal related `git merge-base --fork-point` anomaly when changing remote URLs, showing inconsistent results between identically-configured remotes (origin vs upstream pointing to same URL). The fork-point calculation appears incorrectly tied to remote name rather than commit history, returning empty for one remote while finding commits for another. This is significant because `merge-base --fork-point` is used internally by `git pull --rebase`, potentially explaining the original commit-dropping behavior.

Key technical details: manifests when changing remotes with `git remote set-url`, specific to `git pull --rebase` operation, reproducible with simple linear commits. The bisect used a modified version of the original reproduction script (available on GitHub Gist). The fork-point investigation demonstrates the underlying issue may involve how Git tracks commit relationships after remote URL changes.

Current status: confirmed as long-standing behavior (not a recent regression) with new evidence pointing to fork-point calculation issues. Open questions remain about root cause in remote tracking logic and whether this is a bug or undocumented behavior. The thread now has two related manifestations of the same underlying problem spanning Git's entire 2.x series. Both original reporter (Bill) and new contributor (Tuomas Ahola) remain available for follow-up testing. Unrelated debugging notes mention `GIT_DIR` environment variable behavior during `git bisect run` and a separate build issue with Git master (linker problem involving `_false_but_the_compiler_does_not_know_it`).