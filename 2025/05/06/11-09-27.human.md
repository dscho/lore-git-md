Refactoring patch in the `the_repository` removal effort. This is patch 14/17 in Patrick Steinhardt's series to eliminate global repository dependencies in the object database (ODB) subsystem.

The patch renames `repo_read_object_file()` to `odb_read_object()` to align with the naming conventions established in earlier patches of the series. The change is mechanical but widespread, touching 47 files across the codebase. A compatibility wrapper is provided to avoid breaking in-flight topics.

This continues the pattern seen in previous patches of standardizing ODB-related function names to use the `odb_` prefix and accept explicit ODB parameters rather than relying on `the_repository`. The change appears straightforward with no behavior modifications - it's purely a renaming operation to improve consistency as part of the larger architectural effort to remove global state.

The patch maintains the same functionality while making the interface naming more consistent with the rest of the ODB subsystem changes in this series. Reviewers should verify the mechanical updates are correct, though the risk appears low given the pattern has been well-established in prior patches.