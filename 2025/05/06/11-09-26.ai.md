This is part 13/17 of Patrick Steinhardt's series refactoring the object database (ODB) subsystem as part of the `the_repository` removal effort. The patch focuses on renaming the `oid_object_info()` function to `odb_read_object_info()` and its extended variant to `odb_read_object_info_extended()`, bringing them in line with modern coding guidelines and the ODB subsystem's conventions.

The changes are mechanical but widespread, touching 48 files across the codebase. The patch:
1. Renames the core functions and updates all callers
2. Adds compatibility wrappers to avoid breaking in-flight topics
3. Updates related documentation comments to reflect the new names
4. Maintains all existing functionality while making the ODB interface more consistent

Key technical details:
- The renamed functions now take an explicit `struct object_database*` parameter instead of implicitly using `the_repository`
- All call sites are updated to pass `repo->objects` where needed
- The changes affect object info lookup paths across commands like `git cat-file`, `git fsck`, `git grep`, and many others
- The patch maintains backward compatibility through wrapper functions

This continues the series' pattern of methodically converting ODB-related functions to work with explicit repository parameters rather than globals, bringing us closer to completely removing `the_repository` dependencies from the ODB subsystem. The changes appear straightforward but reviewers should verify the mechanical updates didn't miss any edge cases in object info lookup paths.