Patrick Steinhardt's v5 series (17 patches) completes the refactoring of Git's object database (ODB) subsystem to remove `the_repository` global state and enable pluggable ODB backends. The final patch renames `read_object_with_reference()` to `odb_read_object_peeled()`, converting the last major ODB function to use explicit `object_database` parameters and the `odb_` prefix convention. This affects 8 files with 37 insertions and 45 deletions, updating callers in `builtin/` commands and core tree-walking code to pass `the_repository->objects`.

The series systematically converted all ODB functions through mechanical changes:
1. Structural foundation:
   - Renamed `struct raw_object_store` to `struct object_database` (patch 1/17)
   - Renamed `struct object_directory` to `struct odb_source` (patch 2/17)
   - Updated includes from `object-store.{c,h}` to `odb.{c,h}` (126 files in patch 3/17)
   - Added repository tracking via parent pointers (patch 4/17)
   - Established `odb_` prefix convention for API names

2. Key conversions:
   - `find_odb()` -> `odb_find_source()` with explicit ODB parameter (patch 5/17)
   - `assert_oid_type()` -> `odb_assert_oid_type()` (patch 6/17)
   - `odb_mkstemp()` updated to take explicit ODB (patch 7/17)
   - Alternates handling converted (14 files in patch 8/17)
   - Submodule ODB tracking moved to repository storage (patch 11/17)
   - `oid_object_info()` -> `odb_read_object_info()` (48 call sites in patch 13/17)
   - `repo_read_object_file()` -> `odb_read_object()` (47 call sites in patch 14/17)
   - `has_object()` -> `odb_has_object()` (30 call sites in patch 15/17)
   - `pretend_object_file()` -> `odb_pretend_object()` (3 files in patch 16/17)
   - `read_object_with_reference()` -> `odb_read_object_peeled()` (8 files in patch 17/17)

3. Technical impact:
   - Eliminated `the_repository` from odb.c internals (patch 12/17)
   - Established clean API boundaries for future pluggable backends
   - Improved code organization through consistent naming
   - Reduced global state via explicit ODB parameters
   - Maintained backward compatibility throughout transition

Review status:
- Junio Hamano reviewing v5 based on current main branch
- Derrick Stolee provided Reviewed-by on foundational patches
- Series evolved through v1-v5 with naming refinements
- No outstanding concerns on implementation approach
- Touches 150+ files total for mechanical conversions

The work represents a significant architectural evolution, methodically preparing the codebase for pluggable ODB backends while improving maintainability through reduced global state and consistent naming. With all planned conversions now complete, the series awaits Junio's consideration for merging into the main branch.