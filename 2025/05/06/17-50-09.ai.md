This is a follow-up discussion about the `ps/maintenance-missing-tasks` series, which adds new maintenance tasks for `rerere-gc` and `worktree-prune` functionality. Eric Sunshine responds to Christian Couder's note about a potential memory leak in the series by confirming the leak issue (previously identified in [1]) and raising additional concerns about the "worktree: expose function to retrieve worktree names" patch (detailed in [2]). The thread context shows this series is currently marked for merging to 'next' but remains pending resolution of these outstanding issues. Eric's email serves to highlight two specific technical concerns that need addressing before final integration - a memory leak in one part of the code and design reservations about the worktree name retrieval functionality in another. The references point to previous detailed reviews where these issues were originally raised.