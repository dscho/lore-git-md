This is a review comment on a refactoring patch that's part of Patrick Steinhardt's series to decompose `git gc` functionality into standalone maintenance tasks. The patch in question (v4 2/7) removes unnecessary global variables used for assembling command line arguments in `git-gc.c`.

Christian Couder points out a minor issue with the commit message's grammar, suggesting two alternative formulations that would make it clearer: either adding "is" ("where it's trivial to do") or simplifying to "where trivial to do". This is a small polish suggestion for the commit message wording rather than a technical concern about the change itself.

The patch being reviewed is one of the preparatory refactoring patches in the thread, cleaning up `gc.c` by removing three global `strvec` variables that were only used in single locations. The thread context shows this is part of a larger effort to decompose `git gc` functionality, with the series being technically complete except for a memory leak fix in the worktree pruning path that Christian had previously identified and confirms still exists in v4. The review feedback here is focused on documentation quality rather than implementation, suggesting the kind of minor wording polish that often comes up in final review rounds.