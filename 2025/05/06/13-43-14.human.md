This is a follow-up discussion in the platform compatibility thread about BSD errno handling and packed-refs file operations. The email from shejialuo responds to Jeff King's points about the tradeoffs between using `open_nofollow()` versus direct filetype checking in packed-refs operations.

The discussion clarifies that while `open_nofollow()` provides some atomicity benefits on certain platforms (avoiding a race where a file could become a symlink between checking and opening), it introduces portability issues with errno values across BSD variants. The thread has already converged on Collin Funk's solution for standardizing the errno behavior, so shejialuo confirms they won't change the core logic.

Instead, they'll focus on their parallel work to optimize packed-refs reading using `mmap`, which remains the outstanding work in this series. The email serves as confirmation that the BSD errno handling patch is considered settled, allowing attention to shift to the performance optimization effort. The technical discussion reveals some subtle tradeoffs in filesystem race conditions versus portability that informed the final design.