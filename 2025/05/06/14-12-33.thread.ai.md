Thread identity: Patrick Steinhardt's ongoing series to clean up Git's `contrib/` directory, now expanded to include both `git-new-workdir` (superseded by `git worktree`) and `git-contacts` (documented but rarely used). The thread has evolved into a policy discussion about the nature and maintenance expectations of the `contrib/` directory, with historical context emerging about past decisions.

Core problem/goal: Systematic rationalization of `contrib/` through removal of broken/unmaintained/superseded components, now addressing both `git-new-workdir` and `git-contacts` while establishing formal policy for future `contrib/` maintenance. The discussion balances technical debt removal against user expectations and documentation references, with Patrick advocating for treating `contrib/` as explicitly experimental.

Current status: Consensus reached on removing `git-new-workdir` (superseded by `git worktree`), with Junio Hamano noting personal use of existing workdirs while agreeing not to create new ones. For `git-contacts`, Patrick considers moving it to `tools/` rather than removal, as it serves Git development purposes despite limited adoption. Eric Sunshine and Todd Zullinger provide historical context about the script's documentation mentions and alternative implementations.

Key technical details: `git-new-workdir` removal follows the established pattern of components superseded by core functionality. `git-contacts` presents a more nuanced case - documented but with minimal usage, and a history including Eric Sunshine's Perl port of the more functional `git-related` Ruby script that was rejected due to language constraints.

Open questions: Whether to formalize new `contrib/` policy in `contrib/README` declaring it as explicitly experimental, and how to handle components like `git-contacts` that serve development purposes but have unclear user value. The discussion reveals differing historical perspectives on what belongs in `contrib/`, with references to Felipe Contreras' similar cleanup attempt 11 years ago.

Participant positions: Patrick Steinhardt advocates for systematic cleanup with policy change to treat `contrib/` as experimental, now considering `tools/` as an alternative destination for development-focused scripts. Junio Hamano acknowledges technical cases while revealing usage patterns. Eric Sunshine and Todd Zullinger provide historical context about `git-contacts`' documentation presence and alternative implementations that inform current decisions.

Related work: Builds on prior `contrib/` cleanup of twelve components, extending the systematic approach while addressing broader policy implications. The `git-new-workdir` case parallels prior removals while `git-contacts` discussion reveals historical context about past cleanup attempts (Felipe Contreras' series) and implementation decisions (Eric Sunshine's Perl port). The thread may establish clearer guidelines for handling documented but rarely used components versus clearly superseded ones.