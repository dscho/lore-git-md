Thread identity: Patrick Steinhardt's completed series to clean up Git's `contrib/` directory by removing broken/unmaintained/obsolete components, now totaling ten removals: the obsolete `remotes2config.sh` script, Mercurial/Bazaar remote helper stubs, Thunderbird patch integration scripts, example hooks from `contrib/hooks/`, the MediaWiki integration tools (`mw-to-git/`), the persistent-https remote helper (confirmed as bitrotted and non-compiling with modern Go), the `git-resurrect.sh` script, the non-functional `emacs/` directory, the `git-new-workdir` script, and now the `git-resurrect.sh` script.

Core problem/goal: Systematic rationalization of `contrib/` directory by removing technical debt. The removals target components that are either broken (persistent-https helper fails to compile with modern Go), unmaintained (git-resurrect.sh last updated 2009, MediaWiki tools last security fixes 2020), or have been superseded by core Git functionality or external projects. The MediaWiki tools removal follows the pattern of externalizing specialized integrations, with the functionality now maintained in the Git-Mediawiki/Git-Mediawiki repository.

Current status: Series complete with all ten components marked for deletion. Junio Hamano has given final approval for the removal strategy, distinguishing between components that can be removed immediately (MediaWiki tools, persistent-https helper) versus those needing Git 3.0 coordination (`remotes2config.sh`). The `git-new-workdir` removal may still require following the `WITH_BREAKING_CHANGES` transition pattern due to test dependencies. Junio supports the Linux kernel staging drivers model as future policy for `contrib/` and suggests splitting development tools into a new `tools/` directory while moving documentation examples out of `contrib/`.

Key technical details: The MediaWiki tools (`mw-to-git/`) are effectively unmaintained in Git with a bitrotted test suite, while the external project shows limited but more recent activity. Like the persistent-https helper (bitrotted Go code) and git-resurrect.sh (functionally replaced by core Git), this removal has no test dependencies. The persistent-https helper's historical version remains in Git's history for reference, as will the MediaWiki tools. The `remotes2config.sh` script removal is deferred to Git 3.0 due to repository layout changes.

Open questions: Only timing considerations remain for two specific removals: `remotes2config.sh` (awaiting Git 3.0) and `git-new-workdir` (potential `WITH_BREAKING_CHANGES` transition). All other technical and policy questions are resolved.

Participant positions: Patrick Steinhardt maintains consistent position that all removed components meet criteria. Junio Hamano confirms final approval of the series, distinguishing between immediate and deferred removals. Junio endorses the Linux kernel staging drivers model for future `contrib/` policy and suggests further reorganization into `tools/` and documentation examples directories.

Related work: Completes the `contrib/` cleanup effort. The MediaWiki tools removal follows the pattern of externalizing specialized integrations, similar to the persistent-https helper's bitrot demonstrating risks of unmaintained components. References Linux kernel's staging drivers model for future policy. Series ties into broader Git evolution of consolidating core features, with `remotes2config.sh` removal aligning with Git 3.0 repository layout changes. Junio's approval includes forward-looking suggestions for `contrib/` reorganization beyond the current removals.