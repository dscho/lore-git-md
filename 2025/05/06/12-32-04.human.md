This is a test fix addressing a minor issue in the t6011 test script. Torsten Bögershausen points out that the test's use of `mv` to overwrite a packfile needs the `-f` flag to handle read-only files properly. The change is straightforward - just adding `-f` to force overwrite when modifying test packfiles. While a small change, it makes the test more robust by ensuring the file operations succeed regardless of file permissions. The patch appears correct and well-motivated, though it's a very minor maintenance tweak rather than anything affecting core Git behavior.