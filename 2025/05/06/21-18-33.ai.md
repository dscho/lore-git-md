This is a test fix discussion that builds on the earlier thread about making t6011-rev-list-with-bad-commit.sh more reliable. Junio C Hamano agrees with Torsten Bögershausen's observation that the test needs `mv -f` to handle read-only files during packfile corruption simulation, but raises a more fundamental concern about using `sed` for binary file manipulation.

The thread had previously established that the test's packfile corruption step was flaky due to filesystem permission issues, and Torsten's `mv -f` solution addressed that. However, Junio points out that the test was originally using Perl for binary file editing (via `perl -i.bak -pe`), which properly handles both permissions and binary nature of packfiles. The switch to `sed` in commit cdbdc6bf may be problematic since `sed` behavior on binary files is technically undefined.

This suggests the test may need a more thorough fix - either reverting to Perl for binary safety or finding another reliable way to corrupt packfiles. The discussion shifts from a simple permission fix to questioning the test's fundamental approach to binary file manipulation. The test's purpose remains verifying `git rev-list` behavior with intentionally corrupted commits, but the implementation method now appears suspect.