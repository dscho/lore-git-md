The thread discusses a proposed optimization to `git merge-tree` that would add a mergeability check without performing a full merge. The initial patch series introduced a `--mergeability-only` flag (with Junio suggesting `--dry-run` as an alternative name) and included thorough implementation and tests. 

Elijah Newren, the series author, has now responded to the naming discussion with important technical context. While the flag avoids writing most merge result objects, it still creates objects in two specific cases: when computing merge bases and when handling certain rename conflicts. This subtle behavior means `--dry-run` might mislead users expecting no object creation at all, while `--mergeability-only` more accurately reflects the partial work being done. 

The technical merits of the implementation itself remain uncontested, but the naming discussion has become the focal point. The thread now faces a design question: whether to use the more familiar `--dry-run` (with documentation about its caveats) or find a name that better captures the nuanced behavior. Junio and Elijah are the key participants so far, with no other reviewers having weighed in yet. The next steps will likely involve either settling on a name or exploring alternative naming options that balance clarity with technical precision.