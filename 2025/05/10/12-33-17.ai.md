This is a documentation patch in Jean-Noël Avila's ongoing series standardizing Git man page formatting. It specifically addresses environment variable notation in `git-var.adoc`, completing the documentation standardization work for this command.

The patch makes a clear distinction between referring to environment variable *names* (now consistently backquoted as `` `VAR` ``) versus their *values* (written as `$VAR`). This aligns with the thread's established documentation principles where backquotes indicate literal names while dollar signs indicate runtime values. The changes are mechanical but important for consistency - for example, converting `GIT_EDITOR` to `` `GIT_EDITOR` `` when discussing the variable name while keeping `$GIT_EDITOR` when showing its value in usage examples.

This final patch in the series demonstrates the complete application of the documentation standards to `git-var.adoc`, following earlier work on SYNOPSIS formatting and option presentation. The changes are purely presentational with no behavior impact, maintaining the thread's focus on consistent markup rather than content changes. The patch shows careful attention to detail, with 40 lines modified to implement the new convention across all environment variables documented for `git var`.