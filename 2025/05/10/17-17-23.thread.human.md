Here's a human-readable summary of the thread:

The thread addresses a compatibility issue in gitk, Git's graphical repository browser, where a recent change inadvertently broke support for older Tcl/Tk versions. The problem stemmed from using modern `ttk::combobox` widgets in gitk's preferences page for text wrapping options, which requires Tk 8.5+ with themed widgets enabled. 

The fix, proposed by the author, replaces these comboboxes with gitk's own `makedroplist` implementation that works with basic Tk widgets. This maintains the same functionality (keeping the dropdown values and read-only behavior) while restoring compatibility with older Tcl/Tk installations. The change is minimal (just 2 lines) and well-explained in the commit message, which notes the regression's origin in commit 904b36b815.

While this specific issue is now resolved, it's worth noting that gitk still contains other uses of `ttk::` widgets that could potentially cause similar compatibility problems down the line. The fix appears straightforward and uncontroversial, addressing a clear regression without introducing new behavior changes.