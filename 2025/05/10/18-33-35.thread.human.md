This thread proposes changing `git stash`'s default behavior to restore the index by default when breaking changes are enabled (GIT_BREAKING_CHANGES), fixing what the author D. Ben Knoble considers a historical inconsistency that causes users to lose staged changes. The 9-patch series includes implementation changes, documentation updates, and comprehensive test coverage showing unexpected benefits for submodule handling.

The discussion has reached an impasse between two perspectives: the author argues this change prevents real-world workflow accidents, while maintainer Junio C Hamano expresses reservations based on his experience that the new default (`--no-index` being longer to type than `--index`) may create more friction than it solves. The author's latest email gently prods for more feedback without directly addressing Junio's ergonomic concerns, while acknowledging a potential test improvement (using `test_has_prereq` instead of split tests) that remains unimplemented.

Key open questions include whether the safety benefits outweigh the typing inconvenience, if the test strategy should be revised, and how this aligns with Git 3.0's breaking changes policy. The submodule improvements - which fix known failures and enable new operations - remain an intriguing but underexplored aspect of the change.