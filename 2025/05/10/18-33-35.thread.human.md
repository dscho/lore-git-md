Here's the updated human-readable thread summary:

This thread introduces a new `stash.index` configuration option that lets users control whether `git stash apply/pop` restores the index by default, addressing a long-standing inconsistency where staged changes were saved but not automatically restored. D. Ben Knoble's patch series implements this while maintaining backward compatibility - the default remains false, and `--no-index` still works as an override.

The implementation is now complete in v2, with all apply/pop paths respecting the new config. Phillip Wood has reviewed the tests, suggesting they focus on config/flag interactions rather than duplicating all scenarios, and advocating for using modern test style conventions in new tests even if surrounding tests haven't been updated yet. There's agreement on the core functionality, with discussion now centered on test organization and documentation clarity as the feature prepares for inclusion in Git 3.0. The change remains a straightforward, backward-compatible addition that gives users more control over stash behavior.