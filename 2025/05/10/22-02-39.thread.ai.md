Optimized AI thread summary:

This thread tracks Elijah Newren's 2-patch series adding a `--dry-run` option to `git merge-tree` for efficient mergeability checking, primarily targeting Git forges. The implementation involves:

1. Core merge-ort plumbing changes introducing a `mergeability_only` flag that:
   - Skips writing merged blobs/trees in outer merges except for rename/rename+modify cases
   - Enables early conflict exit while preserving object writes for inner merges
   - Documents edge cases where object writes still occur

2. User-facing interface implementing:
   - `--dry-run` flag (per Junio Hamano's naming guidance)
   - Safety checks preventing combination with incompatible options
   - Comprehensive tests verifying all behaviors

Technical scope covers merge-ort.h changes, modified blob/tree writing logic, early exit paths, and full test coverage. Key participants are Elijah Newren (author), Junio Hamano (naming guidance and review), and Kristoffer Haugsbakk (documentation review).

The series is now fully resolved with:
- Technical implementation complete and tested
- Documentation updated to consistently use `--dry-run` terminology (fixing final inconsistency caught by Kristoffer Haugsbakk and confirmed by Junio Hamano)
- Consensus on both technical approach and interface design
- Final cleanup applied for last remaining references to old flag name

The optimization provides performance benefits for hosting platforms needing mergeability checks without full merge computation, with all edge cases documented and tested. The implementation is ready for finalization with no outstanding issues.