Optimized AI thread summary:

This thread tracks Elijah Newren's patch series adding mergeability checking to `git merge-tree`, with interface refinements progressing through multiple versions. The core implementation consists of:

1. Merge-ort plumbing changes introducing a `mergeability_only` flag that:
   - Skips writing merged blobs/trees in outer merges (except rename/rename+modify cases)
   - Preserves object writes for inner merges while enabling early conflict exit
   - Documents edge cases where object writes still occur

2. User-facing interface originally implementing `--dry-run` (per Junio Hamano's naming guidance), now being renamed to `--quiet` after Phillip Wood's observation about output behavior:
   - Original `--dry-run` suppressed all output (like `git diff --quiet`)
   - Most Git commands use `--dry-run` to produce diagnostic messages (e.g. `git push`)
   - Final version will use `--quiet` to better match silent-operation expectation
   - Retains all safety checks against incompatible options (`--messages`, `--stdin`)
   - Maintains comprehensive t4301 tests verifying empty stdout, no object writes, and correct exit codes

Technical scope covers merge-ort.h, merge-ort.c blob/tree writing logic, builtin/merge-tree.c validation, and test coverage. Key participants are Elijah Newren (author), Junio Hamano (initial naming approval), Phillip Wood (interface consistency feedback), and Kristoffer Haugsbakk (documentation).

The series is approved for merging with this final naming adjustment. The optimization provides hosting platforms with efficient mergeability checks without full merge computation. All edge cases are documented and tested, with the `--quiet` rename addressing the last minor inconsistency in interface conventions. The core functionality (skipping object writes and early conflict detection) remains unchanged from the approved version.