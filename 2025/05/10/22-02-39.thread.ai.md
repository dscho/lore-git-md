Optimized AI thread summary:

This thread tracks Elijah Newren's patch series adding a `--dry-run` option to `git merge-tree` for efficient mergeability checking, primarily targeting Git forges. The implementation has progressed through multiple versions with key refinements:

1. Core merge-ort plumbing changes (patch 1/2) introducing a `mergeability_only` flag that:
   - Skips writing merged blobs/trees in outer merges except for rename/rename+modify cases
   - Enables early conflict exit while preserving object writes for inner merges
   - Documents edge cases where object writes still occur

2. User-facing interface (patch 2/2) implementing:
   - `--dry-run` flag (per Junio Hamano's naming guidance, converted from original `--mergeability-only`)
   - Safety checks preventing combination with incompatible options (`--messages`, `--stdin`)
   - Early exit via new `cleanup` label when conflicts detected
   - Comprehensive tests in t4301 verifying:
     * No output produced (empty stdout)
     * No objects written to object store
     * Correct exit codes (0 for success, 1 for conflict)

Technical scope covers merge-ort.h changes, modified blob/tree writing logic in merge-ort.c, option validation in builtin/merge-tree.c, and test coverage. Key participants are Elijah Newren (author), Junio Hamano (naming guidance and review), and Kristoffer Haugsbakk (documentation review).

The series is now complete with:
- Technical implementation finalized across both patches
- Documentation fully updated to use `--dry-run` terminology
- All instances of the old `--mergeability-only` flag name removed from both man page and code
- Option validation logic preserved with the new flag name
- Kristoffer Haugsbakk's final approval confirming all feedback addressed
- Consensus on approach and interface design established through multiple review rounds

The optimization provides performance benefits for hosting platforms needing mergeability checks without full merge computation, with all edge cases documented and tested. With all outstanding feedback incorporated and final approval given, the series is now ready for merging.