This thread introduces a 2-patch series adding a mergeability check optimization to `git merge-tree`. The series consists of:

1. Internal merge-ort plumbing changes to support early termination on conflicts while avoiding object creation, via a new `mergeability_only` flag in `struct merge_options` that carefully modifies conflict handling and directory processing.

2. A user-facing `--mergeability-only` flag (with Junio C Hamano now suggesting `--dry-run` as a more conventional alternative name) that provides fast mergeability checking without full merge computation. The implementation includes:
   - Incompatibility with output-affecting flags
   - Comprehensive tests for exit codes, object creation, and empty output
   - Clear documentation

The changes target Git forge optimization use cases where only mergeability status is needed. The implementation appears complete with thorough test coverage and documentation. Junio's naming suggestion is the first feedback point raised in the thread, introducing a potential naming discussion while not objecting to the functionality itself. No other reviews or loose ends are currently present beyond this naming consideration. The series remains in its initial submission state with this being the first response.