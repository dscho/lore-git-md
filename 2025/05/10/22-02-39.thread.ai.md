Optimized AI thread summary:

This thread tracks Elijah Newren's 2-patch series adding a `--dry-run` option to `git merge-tree` for efficient mergeability checking, primarily targeting Git forges. The implementation involves:

1. Core merge-ort plumbing changes (patch 1) introducing a `mergeability_only` flag that:
   - Skips writing merged blobs/trees in outer merges (call_depth == 0) except for rename/rename+modify cases
   - Enables early conflict exit from `process_entries()`
   - Preserves object writes for inner merges (call_depth > 0) needed for merge base computation
   - Documents edge cases where object writes still occur (rename conflicts)

2. User-facing interface (patch 2) implementing:
   - `--dry-run` flag (per Junio Hamano's naming guidance) that sets `mergeability_only`
   - Safety checks preventing combination with incompatible options (`--messages`, `--name-only`, `--stdin`, `-z`)
   - Comprehensive tests verifying:
     * No output/objects on clean merges
     * Correct exit code (1) without objects on conflicts
     * Edge case handling (rename conflicts still write objects)
     * Option interaction validation

Technical scope covers:
- merge-ort.h flag addition
- Modified blob/tree writing in `handle_content_merge()` and `write_completed_directory()`
- Early exit path in `process_entries()`
- Command-line interface constraints
- Test coverage for all behavioral aspects

Key participants:
- Elijah Newren (author) implementing the optimization
- Junio Hamano providing decisive naming guidance to use `--dry-run` instead of initial `--mergeability-only` proposal
- Kristoffer Haugsbakk catching a remaining documentation reference to the old `--mergeability-only` flag name that needs updating to match the final `--dry-run` naming

The discussion is fully resolved with consensus on both technical approach and interface design. The series provides a performance optimization for hosting platforms needing mergeability checks without full merge computation, with all edge cases documented and tested. The implementation is complete and ready for finalization, pending the minor documentation correction noted by Kristoffer Haugsbakk.