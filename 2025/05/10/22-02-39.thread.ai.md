Updated AI thread summary:

This thread introduces a 2-patch series adding a mergeability check optimization to `git merge-tree`, authored by Elijah Newren. The series consists of:

1. Internal merge-ort plumbing changes introducing a flag in `struct merge_options` that enables early termination on conflicts while carefully avoiding most object creation, with modifications to conflict handling and directory processing.

2. A user-facing flag for fast mergeability checking without full merge computation. The naming of this flag is the primary discussion point, with Junio Hamano now strongly advocating for `--dry-run` despite the command still performing some object writes in edge cases (merge base computation and rename/rename conflicts). Junio provides two key precedents where Git already uses `--dry-run` for operations that write internal objects (`git fetch --dry-run` and the conceptually similar `git apply --check`), arguing these establish that "dry run" in Git means no user-observable changes rather than absolute prohibition of object creation.

Elijah Newren had initially proposed `--mergeability-only` to accurately reflect the partial object creation behavior, but Junio counters that the Git convention treats object writes as implementation detail when they don't affect user-visible state. Junio explicitly rejects using `--check` (despite its use in `git apply`) to maintain consistency with existing dry-run semantics.

The technical implementation remains uncontested, with all discussion focused on naming. The thread now presents a clear maintainer preference for `--dry-run` based on established Git conventions, with Junio's position carrying significant weight. The two key technical caveats (object creation during merge base computation and rename/rename conflicts) would need clear documentation if `--dry-run` is adopted. No other loose ends remain beyond this naming decision, which appears close to resolution given Junio's authoritative interpretation of Git's dry-run semantics.