This is a feature patch series improving context line customization in interactive patch commands (`add`, `commit`, `checkout`, `reset`, `restore`, `stash` with `--patch`). The series builds on prior work to make these commands respect `diff.context` and `diff.interHunkContext` configurations.

The v2 series makes several key changes from v1: it drops the interactive subcommand idea (deferring it for future work), improves test hygiene by using modern test helpers, adds negative value validation, and makes the implementation more robust. The core changes remain:
1. Making patch commands respect diff context configs (patch 3)
2. Adding command-line overrides (`-U`/`--unified` and `--inter-hunk-context`) that take precedence over configs (patch 4)

The implementation looks thorough, with comprehensive tests covering both config and command-line behavior across all affected commands. The documentation updates clearly explain the new options and their interaction with config settings. Based on the thread context, this aligns with maintainer feedback about option phrasing ("only effective with" rather than "implies") and the technical approach.

This is a well-motivated quality-of-life improvement that makes Git's interactive patch interface more consistent with other diff-generating commands. The series appears ready for review, with the most complex interactive UI aspects deferred to keep the scope focused.