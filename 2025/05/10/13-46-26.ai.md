This is a test refactoring patch, the first in a series introducing command-line controls for diff context settings in interactive patch commands. The patch modernizes test scripts by replacing direct `grep` calls with the Git-specific `test_grep` helper in preparation for subsequent patches in the series.

The changes affect two test files (`t/t3701-add-interactive.sh` and `t/t4055-diff-context.sh`), converting 38 instances of plain `grep` to `test_grep`. The helper provides better debugging output when tests fail. The patch maintains all existing test assertions while making them more robust - negative matches are converted to `test_grep !` patterns, and color/control character tests are preserved.

This is purely mechanical test infrastructure work with no behavior changes. The commit message clearly states this is prerequisite work for the series, which will add command-line overrides for diff context settings in interactive commands. The changes follow established patterns for test modernization in the Git codebase.