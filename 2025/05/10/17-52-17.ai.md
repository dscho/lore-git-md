This is a follow-up discussion about fixing gitk's Tcl/Tk compatibility regression. Johannes Sixt responds to Mark Levedahl's initial patch by pointing out that simply replacing `ttk::combobox` with `makedroplist` isn't sufficient - the two widgets have different calling conventions. He references an alternative solution from YOKOTA Hiroshi that properly handles both the parameter passing differences and maintains compatibility across Tk versions.

The key technical details are:
1. The original patch tried a direct substitution that wouldn't work due to parameter differences
2. The referenced solution properly uses `makedroplist`'s interface which internally handles both themed (`ttk::combobox`) and legacy (`tk_optionMenu`) cases
3. The fix still targets the same two UI elements (text wrapping preferences) but with correct implementation
4. The diff shows the proper way to call `makedroplist` with variable name first followed by values

This appears to be a more complete solution to the compatibility issue, though it hasn't yet been proposed to the list directly. The discussion highlights the subtle differences in widget APIs that need consideration when maintaining backward compatibility.