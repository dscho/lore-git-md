[PATCH 0/4] git stash: add stash.index config to restore index by default

Thread identity: Completed patch series adding configurable index restoration to `git stash`. Originating author D. Ben Knoble. Thread type is feature addition targeting Git 3.0, now maintainer-acknowledged with all feedback addressed.

Core problem: Resolved inconsistency where `stash push` saves staged changes but `apply/pop` doesn't restore them by default, implemented through backward-compatible config rather than breaking changes.

Current status: Series approved by maintainer with all test refinements complete, now including test isolation improvements in v3 and preparatory refactoring. Final patch implements core functionality to honor `stash.index` config in `apply` and `pop` modes:
- `stash.index` config (default false) implemented and tested
- `apply_stash()` and `pop_stash()` respect config as default
- `--no-index` opt-out preserved and verified
- Test strategy finalized per Phillip Wood's feedback
  - Added test isolation fixes for t3903-stash.sh
  - Now creates subdir on demand with `mkdir -p`
  - Minor test cleanup in t3905-stash-include-untracked.sh (removed legacy blank line)
- Documentation in config/stash.adoc complete
- Preparatory refactoring:
  - Consolidated config-related globals (`show_stat`, `show_patch`, `show_include_untracked`) at file scope
  - Purely organizational change with no behavior impact
  - Sets up for future config additions

Key technical details:
- `use_index` static variable reads from config
- All apply/pop paths use config-driven defaults
- Code organization improvements:
  - Config-related globals now grouped at file scope
  - Maintains same style as surrounding code
- Comprehensive test coverage:
  - Core config/flag interaction tests (t3903)
    - Now fully isolated with on-demand subdir creation
  - Config/CLI precedence verification
  - Edge case coverage (t3904-t3905)
    - Includes minor formatting cleanup in t3905
  - Interactive mode testing
- Pure runtime behavior, no conditional compilation
- Maintains all existing safety checks

Participant positions:
- D. Ben Knoble (author):
  - Implemented complete functionality
  - Adopted optimized test strategy
  - Added test isolation improvements
  - Performed final test cleanup
  - Added preparatory refactoring
  - Confirmed all feedback addressed
  - Ready for merging
- Phillip Wood:
  - Confirmed test strategy resolves concerns
  - No objections to functionality
- Junio C Hamano (maintainer):
  - Approved series for integration
  - No technical concerns remain
  - Suggested default may evolve based on user feedback

Related work: Standalone feature addition with no direct dependencies. Represents a backward-compatible improvement to stash behavior while maintaining all existing functionality. Final version incorporates all review feedback including test isolation fixes, minor test cleanup, and preparatory refactoring, and is ready for merging.