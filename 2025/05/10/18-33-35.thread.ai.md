[PATCH 0/9] git stash: make --index the default under breaking changes

Thread identity: Subject proposes changing `git stash` behavior to default to `--index` when breaking changes are enabled (GIT_BREAKING_CHANGES). Originating author not yet named in this summary. Thread type is feature change proposal targeting Git 3.0.

Core problem: Current `git stash apply/pop` defaults to not restoring the index, creating a "footgun" where carefully staged changes are lost during stash application. The series rectifies this historical decision when breaking changes are enabled, making `stash apply` properly reverse `stash push` by default.

Current status: Series now includes 9 patches with comprehensive changes:
- Infrastructure improvements making stash tests more independent
- Documentation updates in git-stash.adoc and BreakingChanges.adoc
- Implementation changes in builtin/stash.c that:
  - Flip default behavior when WITH_BREAKING_CHANGES is defined
  - Replace `--index` flag with `--no-index` in help/parsing
  - Update error messages to reference new opt-out flag
- Test updates across multiple test files showing:
  - Basic operations (t3903) with comprehensive but duplicated tests
  - Interactive mode (t3904) now split into old/new behavior versions
  - Untracked files (t3905) with new adjustments for index behavior
  - Submodules (t3906) demonstrating unexpected improvements
  - Documentation consistency (t0450)

Key technical details:
- Behavior change gated behind GIT_BREAKING_CHANGES flag
- New default makes `stash apply` restore index by default, with `--no-index` opt-out
- Modifies builtin/stash.c core functionality with conditional compilation
- Updates git-stash.adoc with dual behavior documentation
- Test strategy explicitly splits old/new behavior tests
- Submodule tests (t3906) reveal additional benefits:
  - Fixes known failures when replacing files with submodules
  - Enables non-fast-forward merges that previously failed
  - Improves submodule removal operations
- Pathspec filtering behavior with stashed index remains an open question

Open questions:
- Whether the change warrants being a breaking change
- If test strategy (explicit splits vs conditionals) is optimal
- Potential migration impact on existing workflows
- Timing relative to Git 3.0 release
- Whether pathspec filtering should apply to stashed index
- Mechanism behind submodule improvements needs investigation
- Significant new concern from Junio C Hamano about default behavior ergonomics:
  - Personal experience shows `--index` default caused workflow friction
  - Asymmetry in typing effort (`--no-index` being longer) may frustrate users
  - Suggests current behavior may be more ergonomic for common cases

Participant positions:
- Junio C Hamano expresses strong reservations based on personal workflow experience
  - Doesn't reject outright but signals significant hesitation
  - Frames change as potentially creating more frustration than it solves
  - Counters "footgun" rationale with ergonomic considerations

Related work: References original stash implementation decisions that considered this behavior but didn't implement it, with links to historical mailing list discussions. No other related in-flight topics mentioned yet.