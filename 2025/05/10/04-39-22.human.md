This is a performance test addition for the sparse index optimization series. Elijah Newren is responding to Derrick Stolee's patch adding a performance test for `git add -p` with sparse index support. The test demonstrates dramatic improvements - a 96.7% reduction in runtime (from 2.09s to 0.07s) when using a sparse index compared to the full index case.

The test is added to the existing `p2000-sparse-operations.sh` performance test suite, exercising the interactive add operation with a simple edit scenario. Elijah's response is positive ("Very nice!"), indicating approval of both the performance results and the test addition. This suggests the series is in good shape for merging, following his earlier detailed reviews of test cases in patches 1 and 2.

The test validates the sparse index optimizations for `git add -p` that were implemented in the series, showing they achieve their intended performance goals without regressing functionality. Since `git add -i` uses the same underlying code, a separate test isn't needed. This appears to be the final piece of review feedback on the series before it can be merged.