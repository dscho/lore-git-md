Thread identity: Bugfix series for memory leaks in reftable writer during error conditions, covering two distinct cases (padded writes and index hash operations), now with maintainer feedback.

Core problem: The reftable writer in `reftable/writer.c` leaks allocated buffers in error paths:
1. `zeroed` buffer leak during failed padded writes
2. `key` buffer leak in `writer_index_hash()` when `reftable_buf_add()` fails

Current status: Patch v2 under review, expanding scope from v1 (which only addressed the padded write case). Patrick Steinhardt has reviewed and approved both fixes but recommends splitting them into separate commits since they address distinct code areas.

Key technical details:
- Both fixes follow same pattern: adding `reftable_free()` calls in error paths
- First case: cleanup for `zeroed` buffer in `padded_write()`
- Second case: freeing `key` buffer when `reftable_buf_add()` fails in `writer_index_hash()`
- Modifications limited to `reftable/writer.c`
- Total changes: 6 lines added, 2 removed
- Maintains existing error handling flow
- No behavioral changes to successful code paths
- Still lacks specific test coverage for these error cases

Version changes (v1->v2):
- Added second fix for `writer_index_hash()` leak
- Updated commit message to cover both cases
- Kept original padded_write() fix unchanged

Open questions:
- Should regression tests be added for these error paths?
- Are there other similar error-path resource leaks in reftable writer?
- Will author split patches per Steinhardt's suggestion?

Participant positions:
- Initial submission by Lidong Yan in v1
- Patrick Steinhardt reviewed v2:
  - Approved both fixes technically
  - Suggested splitting into separate commits
  - Explicitly noted correctness of second case's logic
- No other reviews or acks yet

Related work:
- Part of ongoing reftable backend development
- Builds on work by primary reftable contributors
- No other directly related patches mentioned in thread