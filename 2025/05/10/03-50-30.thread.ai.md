Thread identity: Bugfix series for memory leaks in reftable writer during error conditions, split into two atomic patches (padded writes and index hash operations) as requested by maintainer.

Core problem: The reftable writer in `reftable/writer.c` leaked allocated buffers in error paths:
1. `zeroed` buffer leak during failed padded writes (first patch)
2. `key` buffer leak in `writer_index_hash()` when `reftable_buf_add()` fails (second patch)

Current status: v3 series now has maintainer ack from Patrick Steinhardt after being split into two atomic commits. First patch (padded_write leak) unchanged from v2's approved version. Second patch (index_hash leak) follows same error-path cleanup pattern. Both fixes are minimal (6 lines added, 2 deleted total) and maintain existing successful execution flows.

Key technical details:
- First patch adds `reftable_free(zeroed)` in `padded_write()` error path
- Second patch adds `reftable_free(key)` when `reftable_buf_add()` fails
- Changes limited to error-path resource cleanup
- No behavioral changes to successful paths
- Still lacks specific test coverage for these error cases

Version history:
- v1: initial fix for padded_write() leak only
- v2: added writer_index_hash() fix
- v3: split into two atomic commits per maintainer request
- No functional changes from v2 to v3

Open questions:
- Should regression tests be added for these error paths?
- Are there other similar error-path resource leaks in reftable writer?

Participant positions:
- Lidong Yan (author):
  - Implemented both fixes
  - Split patches in v3 per maintainer request
  - Resent first patch with no changes
  - Completed series with atomic second fix
- Patrick Steinhardt (maintainer):
  - Approved technical correctness in v2
  - Requested patch splitting for better commit granularity
  - Now acked v3 with "these look good to me"
  - Implicitly confirms readiness for merge

Related work:
- Part of ongoing reftable backend development
- Builds on work by primary reftable contributors (Patrick Steinhardt, Karthik Nayak)
- No other directly related patches mentioned in thread