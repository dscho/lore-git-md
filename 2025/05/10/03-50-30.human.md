This is a bugfix patch addressing a memory leak in the reftable backend. When `padded_write()` fails during a reftable operation, it was leaking memory allocated for zero-padding. The patch adds proper cleanup by freeing the allocated memory before returning the error.

The change is straightforward - wrapping the error return case with a call to `reftable_free()`. The commit message clearly explains the scenario where the leak could occur (during write errors) and the simple solution. The fix is small (4 lines changed) and focused, touching only the error handling path in `reftable/writer.c`.

This appears to be a well-contained fix for a real issue, though the impact is limited to error cases in the reftable code. The patch follows Git's coding style and includes the necessary context in the commit message.