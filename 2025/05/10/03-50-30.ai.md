Bugfix for a memory leak in the reftable writer when padded writes fail. The patch addresses a case in `reftable/writer.c` where allocated memory (`zeroed` buffer) would leak if the underlying writer (`w->writer`) failed during a padded write operation. This could occur with `reftable_write_data` in `reftable/stack.c` during write errors.

The fix is straightforward - adding a `reftable_free(zeroed)` call in the error path before returning. The change is minimal (3 lines added, 1 removed) and clearly targets a specific leak scenario. The commit message explains the failure condition and solution concisely, though it could benefit from mentioning whether this was found via code inspection or actual observed leaks.

This appears to be a solid, focused fix for a real resource management issue in the reftable code. The patch follows standard style and includes the necessary free call while maintaining the existing error handling flow. No tests are included, but the nature of the fix (pure leak plugging) may not require them.