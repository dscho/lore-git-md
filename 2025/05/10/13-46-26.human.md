Test refactoring patch in a series adding diff context controls. This is the first of four patches that will introduce command-line overrides for diff context settings in interactive patch commands.

The patch modernizes test scripts by replacing direct `grep` calls with the Git-specific `test_grep` helper in two test files (`t/t3701-add-interactive.sh` and `t/t4055-diff-context.sh`). This change provides better debugging output when tests fail. The changes are mechanical substitutions with no behavior changes - each `grep` is replaced with `test_grep` while preserving all existing patterns and negations (using `!`).

This appears to be solid groundwork for the subsequent patches in the series. The test changes are straightforward and focused, with the commit message clearly stating this is prerequisite work. The patch follows the established pattern of test modernization seen elsewhere in the Git codebase.