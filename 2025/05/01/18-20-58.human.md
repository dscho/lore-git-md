This is a design discussion follow-up in the Scalar `--no-maintenance` feature series. Junio Hamano reiterates his architectural preference for keeping maintenance control at the command level (`cmd_register()`/`cmd_clone()`) rather than embedding it in the shared `register_dir()` helper. His key argument is the Unix philosophy of single-responsibility functions - `register_dir()` should focus solely on registration, while maintenance toggling belongs at the higher command level where the decision is made.

The email notes an implementation detail that could easily have been overlooked - the symmetrical support for both `--maintenance` and `--no-maintenance` flags - praising this as good API design. This suggests the technical implementation is sound, even as the architectural debate continues. The tone remains constructive, with Hamano acknowledging he might have missed this detail himself while focused on the new functionality.

The discussion remains focused on the core tension between centralized configuration (Stolee's position) versus explicit command-level control (Hamano's preference), with implications for how future customizations might be added. While no new technical arguments are introduced here, the philosophical framing reinforces that this is about long-term maintainability rather than immediate functionality. The patch series appears to be in good shape for a v2 that addresses these architectural concerns while preserving the already-working `--no-maintenance` behavior.