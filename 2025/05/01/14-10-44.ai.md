Bugfix addressing a heap-use-after-free in `git rebase --rebase-merges` reflog handling. Phillip Wood confirms Jeff King's diagnosis that the root cause is improper string ownership management in the sequencer's reflog message handling, where `ctx->reflog_message` can point to freed memory after strbuf reallocation.

The issue manifests when `do_reset()` calls `reflog_message()` during merge operations, potentially reallocating the static strbuf while `ctx->reflog_message` still holds a pointer to the old buffer. Phillip notes this explains the intermittent nature of the corruption Kristoffer Haugsbakk observed, as it depends on strbuf reallocation timing.

This email serves as technical validation of Jeff's proposed fix (changing `reflog_message` from `const char*` to `char*` with proper ownership management). Phillip's confirmation that `do_reset()` is a likely caller that could trigger the strbuf reallocation strengthens the case for the fix by identifying a concrete reproduction path.

The discussion remains focused on the sequencer.c implementation details, particularly the interaction between `reflog_message()`, `do_reset()`, and `do_merge()`. No new test cases or code changes are proposed here - this is an expert review confirming the technical analysis behind Jeff's pending fix. The thread appears to be converging on a solution, with Phillip's input providing additional confidence in the diagnosis.