This is the final patch in Junio C Hamano's documentation standardization series, applying the same formatting improvements to `git-worktree`'s man page that were previously applied to other commands. The changes are purely presentational, following the pattern established in earlier patches: converting SYNOPSIS from [verse] to [synopsis] format, backquoting all option names (like `--force` and `--expire <time>`), and splitting negatable options (such as `--[no-]checkout`) into separate `--checkout`/`--no-checkout` entries for better grepability.

The patch makes extensive but mechanical changes throughout `git-worktree.adoc`, touching nearly every option description while maintaining the exact same content. This continues the project-wide effort to standardize documentation formatting that began with Jean-Noël Avila's earlier work. The changes are clearly non-controversial, building on established consensus about documentation conventions.

Junio has marked this as a WIP (work-in-progress) patch, indicating it may need minor adjustments before final submission, though the diff shows a complete and consistent application of the new formatting rules. This concludes the 4-patch series that has systematically updated verification commands, `git var`/`write-tree`, `git-daemon`, and now `git-worktree` documentation.