Test improvement patch from Junio Hamano that makes test cases in t7500 more independent by adding `--allow-empty` to commit commands. This is part 1 of a 3-patch series introducing a general mechanism for optional file specifications (seen in the thread context), though this particular patch focuses solely on test reliability.

The change modifies several test cases in t7500 that verify commit template behavior. Previously, these tests could fail for the wrong reason (due to having nothing to commit) if an earlier test case didn't fail as expected. By adding `--allow-empty`, each test now properly fails only for its intended reason (template validation) regardless of the index state. The diff shows straightforward additions of the flag to 5 test cases while maintaining their existing assertions.

This appears to be preparatory work for the larger series, ensuring the test infrastructure is robust before introducing new functionality. The patch is small and focused, with a clear explanation of the commit message. As the first in a series, it doesn't yet touch the core functionality being proposed (optional file handling), but establishes reliable test conditions for subsequent patches to build upon.