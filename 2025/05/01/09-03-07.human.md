This is a bugfix follow-up email regarding the `git apply --intent-to-add` behavior. The original issue, reported in 2021, involved the command incorrectly marking tracked files as deleted when used with `--intent-to-add`. Johannes Altmanninger had submitted a v3 patch to fix this by making `--intent-to-add` imply `--index` behavior.

Jason Cho is now checking on the status of this fix - specifically whether it was merged and in which Git version it appears. The email doesn't contain technical details about the fix itself, but rather serves as a status inquiry about a known issue that had a proposed solution. The original patch appeared to address a clear behavioral bug in a non-controversial way, though we'd need to see the actual patch to assess its quality more thoroughly.