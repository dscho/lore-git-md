This is a follow-up inquiry about a bugfix patch regarding `git apply --intent-to-add` behavior. The email references a 2021 bug report where `--intent-to-add` was incorrectly marking tracked files as deleted in the index. Johannes Altmanninger's v3 patch from that time proposed making `--intent-to-add` imply `--index` to fix this. The current sender (Jason Cho) is asking whether this fix was ever merged and in which Git version it appears.

Key technical details for future reference:
- The original issue involved `builtin/apply.c` behavior with the `--intent-to-add` flag
- The proposed solution was to automatically set the `--index` flag when `--intent-to-add` is used
- The thread context suggests this was a known bug with a clear reproduction case

The email doesn't contain new technical content - it's a status check on an old patch. Future replies will likely either:
1) Confirm the merge happened (with version info)
2) Explain why it wasn't merged
3) Restart discussion if the fix was overlooked