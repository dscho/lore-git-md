Here's the updated AI thread summary incorporating the documentation patch:

This thread addresses a serious bug in `git apply --intent-to-add` (`-N`) where the command incorrectly clobbers the index contents, marking tracked files as deleted. Raymond Pasco has submitted a comprehensive fix series that resolves both the technical implementation and documentation aspects.

The complete five-patch series now includes:
1. Error handling for invalid usage outside a repository
2. Core fix for index corruption by ensuring proper loading before intent-to-add entries
3. Tightened index write conditions in `create_file()` for new files only
4. Expanded test coverage for complex scenarios
5. Documentation improvements clarifying behavior and flag interactions

Key technical changes in `apply.c`:
- Explicit `is_not_gitdir` check in `check_apply_state()`
- `update_index` used instead of `check_index` for reading
- Precise index write conditions in `create_file()`
- Maintained existing behavior while fixing edge cases

Documentation updates in `Documentation/git-apply.txt`:
- Clarified mutual exclusivity with `--index`/`--cached`
- Removed confusing double negative phrasing
- Maintained note about `--index` being implied by `--3way`
- Aligned docs with fixed implementation

Test coverage in `t/t4140-apply-ita.sh` verifies:
- Basic `--intent-to-add` functionality
- Index integrity with existing index
- Mixed modification/creation cases
- Behavior via `ls-files` and `diff` assertions

The solution addresses both the original 2021 report and recent demonstrations of breakage. Technical details:
- Prevents index clobbering through proper initialization
- Restricts writes to new files via combined conditions
- Documents flag interactions clearly
- Maintains `ita_only` and `conflicted_threeway` paths
- Comprehensive test coverage

Participants:
- Altmanninger: Original reporter
- Hamano: Maintainer feedback
- Cho/Hodges: Recent breakage reports
- Pasco: Author of complete fix series

The series now provides a complete solution with both implementation fixes and clear documentation, maintaining `--intent-to-add`'s purpose as a worktree-only operation while eliminating the dangerous index corruption behavior. The documentation patch finalizes the solution by ensuring users understand the command's behavior and limitations.