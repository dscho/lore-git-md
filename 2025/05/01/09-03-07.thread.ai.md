Here's the updated AI thread summary incorporating the new email:

This thread addresses a serious bug in `git apply --intent-to-add` (`-N`) where the command incorrectly clobbers the index contents, marking tracked files as deleted. Raymond Pasco has submitted a comprehensive fix series that resolves both the technical implementation and documentation aspects, with ongoing investigation of an unexpected interaction with `git checkout -p`.

The complete five-patch series includes:
1. Error handling for invalid usage outside a repository
2. Core fix for index corruption by ensuring proper loading before intent-to-add entries
3. Tightened index write conditions in `create_file()` for new files only
4. Expanded test coverage for complex scenarios
5. Documentation improvements clarifying behavior and flag interactions

Key technical changes in `apply.c`:
- Explicit `is_not_gitdir` check in `check_apply_state()`
- `update_index` used instead of `check_index` for reading (now under review for checkout interaction)
- Precise index write conditions in `create_file()`
- Maintained existing behavior while fixing edge cases
- Current investigation of `(state->check_index || state->ita_only)` workaround for checkout breakage

Documentation updates in `Documentation/git-apply.txt`:
- Clarified mutual exclusivity with `--index`/`--cached`
- Removed confusing double negative phrasing
- Maintained note about `--index` being implied by `--3way`
- Aligned docs with fixed implementation

Test coverage in `t/t4140-apply-ita.sh` verifies:
- Basic `--intent-to-add` functionality
- Index integrity with existing index
- Mixed modification/creation cases
- Behavior via `ls-files` and `diff` assertions
- (Pending: checkout -p interaction tests)

The solution addresses both the original 2021 report and recent demonstrations of breakage, with one remaining open issue regarding checkout interaction. Technical details:
- Prevents index clobbering through proper initialization
- Restricts writes to new files via combined conditions
- Documents flag interactions clearly
- Maintains `ita_only` and `conflicted_threeway` paths
- Comprehensive test coverage (with one gap being addressed)

Participants:
- Altmanninger: Original reporter
- Hamano: Maintainer feedback
- Cho/Hodges: Recent breakage reports
- Pasco: Author of complete fix series, now investigating checkout interaction

The series provides a nearly complete solution with implementation fixes and clear documentation, maintaining `--intent-to-add`'s purpose as a worktree-only operation while eliminating the dangerous index corruption behavior. The documentation patch finalizes the intended behavior while the checkout interaction remains under investigation, with the author taking a careful approach to resolving this final edge case.