Here is the updated dense AI thread summary incorporating the new discussion:

This thread introduces a 2-patch series establishing standardized deprecation infrastructure for Git commands, demonstrated by deprecating `git whatchanged`. The work generalizes the `pack-redundant` deprecation pattern into reusable core components.

The first patch refactors deprecation logic into a `you_still_use_that()` helper in usage.c with warning/die functionality moved from pack-redundant.c, test verification in t5323, and clean separation of infrastructure. The second patch applies this to `git whatchanged` with updates to builtin/log.c, documentation, and multiple test files (t4013, t4202, t9300, t9301).

Junio Hamano has now clarified both the deprecation strategy and implementation details in response to Patrick Steinhardt's questions:
1. The current phase is usage measurement via forced opt-in (`--i-still-use-this`)
2. BreakingChanges.adoc updates should wait until formal deprecation
3. The `i_still_use_this` flag must persist in `struct log_config` due to `cmd_log_init_finish()` parsing flow
4. Explicit replacement suggestions (`git log --raw`) are intentionally omitted at this stage

Key technical details:
- Core infrastructure in usage.c/git-compat-util.h
- Command plumbing in pack-redundant.c and builtin/log.c
- Documentation updates with warning sections
- Test verification across multiple suites
- Persistent flag in `struct log_config` for command flow
- Warning insertion in `cmd_whatchanged()`

The series establishes a complete deprecation template where:
1. Commands show warnings requiring explicit opt-in
2. Documentation marks deprecation status
3. Tests verify warning behavior
4. Users are surveyed via forced opt-in
5. Infrastructure changes precede specific deprecations

Philosophical difference remains on messaging:
- Steinhardt favors proactive guidance with replacement suggestions
- Hamano prefers measuring actual usage patterns first

The approach suggests this may become Git's standard deprecation path, with `git whatchanged` kept for transitional users. While implementation details are now clarified, the discussion reveals ongoing tension between proactive guidance and usage measurement in the deprecation process.