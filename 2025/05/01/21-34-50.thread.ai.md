Here is the updated dense AI thread summary incorporating the new discussion:

This thread introduces a 2-patch series establishing standardized deprecation infrastructure for Git commands, demonstrated by deprecating `git whatchanged`. The work generalizes the `pack-redundant` deprecation pattern into reusable core components.

The first patch refactors deprecation logic into a `you_still_use_that()` helper in usage.c with:
- Warning/die logic moved from pack-redundant.c to usage.c
- Function signature: `you_still_use_that(const char *command_name)` with NORETURN
- Declaration in git-compat-util.h for accessibility
- Test verification in t5323-pack-redundant.sh using test_grep
- Maintains original warning message and behavior
- Clean separation of infrastructure from specific deprecations

The second patch applies this to `git whatchanged` with:
- Warning and flag handling in builtin/log.c using the shared helper
- Updated git-whatchanged.adoc with WARNING section and `[synopsis]` markup
- Test adjustments in t4013-diff-various.sh (verifying warning behavior)
- Additional test updates in t4202-log.sh, t9300-fast-import.sh, t9301-fast-import-notes.sh
- Explicit note excluding this from Git 3.0 breaking changes

Patrick Steinhardt has raised two key points:
1. A process question about whether deprecation warnings should be accompanied by immediate breaking change notices in BreakingChanges.adoc
2. A technical suggestion to enhance the deprecation warning by:
   - Explaining the placement of `i_still_use_this` flag in `struct log_config` (due to parsing in `cmd_log_init_finish()`)
   - Extending `you_still_use_that()` to accept an explanation parameter showing the replacement command (`git log --raw`)

Technical scope spans:
- Core infrastructure (usage.c, git-compat-util.h)
- Command plumbing (pack-redundant.c, builtin/log.c)
- Documentation (man pages with warning sections)
- Tests (t5323, t4013, t4202, t9300, t9301)

The series establishes a complete deprecation template where:
1. Commands show warnings requiring `--i-still-use-this`
2. Documentation marks deprecation status
3. Tests verify warning behavior and migrate to alternatives
4. Users are surveyed via mailing list notifications
5. Infrastructure changes precede specific deprecations
6. Test suites are updated to use flag or alternatives
7. (Proposed) Warnings include explicit migration paths

The work remains framed as maintenance preparation rather than immediate removal, with `git whatchanged` kept historically for users not yet transitioned to `git log --raw`. The approach suggests this may become Git's standard deprecation path. No reviewers have yet commented on either technical implementation or the process question about breaking change documentation timing, though Steinhardt's technical refinement proposal would make the warnings more actionable by explicitly stating alternatives.