Here's a human-readable summary of the thread's current state:

This thread has documented Git's systematic approach to command deprecation through two case studies: the removal of `git whatchanged` (now complete) and the ongoing deprecation of `git pack-redundant`. The work has established reusable infrastructure for managing deprecation lifecycles while allowing different commands to progress through the process at their own pace.

The `whatchanged` removal has now reached its final stage with two key developments: First, the code now conditionally removes the command when built with `WITH_BREAKING_CHANGES`, maintaining backward compatibility through compilation flags. Second, a new documentation patch formally lists the deprecation in BreakingChanges.adoc and adds a warning to the command's man page, noting its obsolescence (it's been functionally replaced by `git log --raw` for over a decade) and scheduled removal.

Meanwhile, `pack-redundant` remains in the warning phase, with its deprecation infrastructure sharing core components with the `whatchanged` work but following its own timeline. Junio Hamano has maintained consistent principles throughout both implementations, emphasizing usage measurement through forced opt-in, careful documentation updates, and preservation of warning text.

The thread demonstrates Git's methodical approach to removing obsolete functionality - first introducing warnings, then requiring explicit opt-in, documenting the change, and finally implementing removal while maintaining compilation-time backward compatibility. With this final documentation update, the `whatchanged` deprecation lifecycle is now fully documented in both code and project documentation.