Here's the updated human-readable summary:

The thread continues to develop Git's deprecation infrastructure through parallel work on `git whatchanged` and `git pack-redundant`. The latest contribution is a documentation patch in the `whatchanged` series that prepares for the command's eventual removal by updating references in two files: `MyFirstObjectWalk.adoc` and `user-manual.adoc`. The changes replace `whatchanged` examples with more stable alternatives (`show` and `version`) while maintaining accuracy.

The effort now spans two tracks with distinct approaches:
1. For `whatchanged`, a comprehensive 6-patch series establishes deprecation infrastructure including opt-in measurement (`--i-still-use-this`), build-time controls, and systematic documentation removal. The latest patch continues this by future-proofing tutorial and manual references.
2. For `pack-redundant`, a single patch retrofits an existing warning to use the new shared infrastructure while preserving behavior.

Junio Hamano's principles guide both implementations - emphasizing empirical measurement through forced opt-in rather than proactive guidance about alternatives. The work demonstrates how the deprecation helper can serve both new warnings (whatchanged) and existing ones (pack-redundant), with shared infrastructure in usage.c while maintaining command-specific timelines. The documentation updates show the careful, phase-by-phase approach being taken to remove whatchanged references across the codebase.