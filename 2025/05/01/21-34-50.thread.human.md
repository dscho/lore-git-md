Here's the human-readable thread summary:

This thread introduces a new standardized approach for deprecating Git commands, using `git whatchanged` as the initial test case. The proposal establishes core infrastructure in Git's codebase to handle deprecation warnings and opt-in requirements, following the successful pattern previously used for `pack-redundant`. The work consists of two main parts: first creating reusable deprecation utilities, then applying them to `git whatchanged`.

Junio Hamano has clarified the deprecation strategy - currently in a measurement phase where users must explicitly opt-in with `--i-still-use-this` to continue using the command. Formal documentation updates will come later, once actual usage patterns are understood. The implementation touches multiple areas: core warning infrastructure in usage.c, command-specific changes in builtin/log.c, and updates to several test files.

A recent discussion between Hamano and Patrick Steinhardt revealed some philosophical differences about deprecation messaging. Steinhardt suggested including replacement guidance (noting that `git log --raw` provides similar functionality), while Hamano prefers focusing first on measuring actual usage before adding such explanations. They also discussed technical details of where to place the opt-in flag in the code, with Hamano explaining why it needs to persist in the log configuration structure rather than being command-local.

The thread shows Git establishing a more systematic approach to command deprecation, with clear phases for measurement, warning, and eventual removal. While the core approach is settled, some implementation details around messaging and flag placement remain open for discussion. The work positions `git whatchanged` as the first beneficiary of this new deprecation framework, which may become the standard path for future command retirements.