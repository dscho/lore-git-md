Here's a human-readable summary of the thread so far:

This thread introduces a new standardized way to deprecate Git commands, using `git whatchanged` as the first example. The approach builds on how `pack-redundant` was previously deprecated, but generalizes it into reusable infrastructure. 

The first patch refactors the deprecation warning system into a shared helper function that shows warnings and requires users to explicitly opt-in with `--i-still-use-this`. The second patch then applies this to `git whatchanged`, updating its code, documentation, and tests accordingly. The goal isn't immediate removal - instead, the warnings serve both to nudge users toward the preferred alternative (`git log --raw`) and to survey who's still actively using the command via mailing list notifications.

The changes span core code, command implementations, documentation, and tests. The author notes a couple additional test files that need updating but haven't been touched yet. While this sets up the command for potential future removal, the author clarifies it's not currently slated for Git 3.0. The thread is still early with no review feedback yet, but the approach suggests this may become Git's standard deprecation pattern going forward.