This thread documents Git's systematic approach to command deprecation through two case studies: `git whatchanged` (now fully removed with Meson build integration) and `git pack-redundant` (currently in warning phase). The latest patch refactors the deprecation warning infrastructure, extracting shared logic into a reusable `you_still_use_that()` helper function in `usage.c` while maintaining identical behavior for `pack-redundant`.

The `whatchanged` track has completed its deprecation lifecycle across both Makefile and Meson build systems, while `pack-redundant` demonstrates the same infrastructure in its warning phase. Elijah Newren provided a minor suggestion to clarify the commit message wording, distinguishing between the pre-existing `--i-still-use-this` requirement and the new refactoring work. The patch maintains the project's established deprecation methodology - usage measurement via forced opt-in, consistent warning text, and test coverage - now with reusable components that future deprecations can leverage.

Junio Hamano and Patrick Steinhardt have shaped the approach throughout, ensuring consistent implementation across build systems and careful documentation. The parallel implementations show how the infrastructure supports different deprecation timelines while maintaining a unified methodology, with all technical components now in place including complete Meson integration.