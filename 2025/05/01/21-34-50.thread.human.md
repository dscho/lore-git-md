The thread documents Git's deprecation infrastructure development, focusing on removing `git whatchanged` and warning about `git pack-redundant`. The 11-patch `whatchanged` series (with a recent typo fix) has established reusable deprecation components including an opt-in mechanism (`--i-still-use-this`), documentation updates, and compilation controls (`WITH_BREAKING_CHANGES`). Patrick Steinhardt and Junio Hamano have aligned on the deprecation approach, with recent discussion refining commit message precision.

A new follow-up from Patrick addresses a gap in the Meson build system integration, ensuring `git-whatchanged.adoc` is properly excluded from main documentation when built with breaking changes. This mirrors the handling of `pack-redundant` and completes the technical implementation. The change is narrowly scoped to build configuration, maintaining consistency established by the core series. While untested, the patch follows the pattern already validated for other deprecated commands.

The thread shows a complete deprecation lifecycle for `whatchanged`, from initial warning through to build-system integration, while `pack-redundant` remains in its warning phase. Both share infrastructure but follow independent timelines, demonstrating the flexibility of Git's new deprecation framework.