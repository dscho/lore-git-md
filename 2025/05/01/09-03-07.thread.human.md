Here's the updated human-readable thread summary:

This thread addresses a serious bug in `git apply --intent-to-add` (`-N`) where the command was incorrectly corrupting the index by marking tracked files as deleted. Raymond Pasco has submitted a comprehensive five-patch series that fixes both the implementation and documentation. The solution ensures proper index loading before handling intent-to-add entries and tightens write conditions to only affect new files, while maintaining existing behavior for other cases.

The series has now hit a snag - an unexpected interaction with `git checkout -p` was reported after the index handling changes. While the logical switch from `check_index` to `update_index` should only affect intent-to-add cases, it somehow breaks checkout's patch mode. The author has identified a temporary workaround using `(state->check_index || state->ita_only)` but acknowledges this isn't fully understood yet. This suggests there may be subtle interactions between apply's various modes that weren't caught in the extensive test coverage.

The thread has seen participation from the original reporter (Altmanninger), recent breakage reporters (Cho/Hodges), and maintainer feedback from Junio Hamano. The current focus is on carefully resolving this last issue while maintaining the series' overall quality - the author is both working on a fix and soliciting broader review to ensure no other edge cases were missed. The comprehensive solution already addresses the core index corruption problem while clearly documenting the command's behavior and limitations.