Documentation patch updating the `cmd_psuh` example in Git's new contributor tutorial to reflect current coding practices. This is patch 2/3 in a series modernizing the MyFirstContribution.adoc guide.

The patch makes two key changes to the tutorial's sample command implementation: First, it updates the `cmd_psuh` function signature to include the now-required `struct repository *repo` parameter, bringing it in line with Git's current subcommand API. Second, it adds detailed explanation of the `UNUSED` macro pattern used to silence compiler warnings about intentionally unused parameters, with clear commentary about why this is necessary even for parameters that aren't yet used.

The diff shows the new signature with all parameters marked UNUSED initially, then demonstrates removing the UNUSED markers when parameters are actually used later in the tutorial. The patch maintains the tutorial's pedagogical approach by explaining each change rather than just making silent updates - it notes that the `prefix` and `repo` parameters' purposes will be explained later, and keeps the alphabetical ordering of declarations in builtin.h.

This is a straightforward documentation update that keeps the tutorial current with Git's evolving codebase practices. The changes are well-motivated (preventing build failures and compiler warnings) and clearly explained for new contributors and maintainers alike. The patch fits cleanly into the series' goal of modernizing the tutorial while maintaining its educational value.