Here's a human-readable summary of the thread based on this new email:

A bug report has been filed regarding Git operations failing with 'bus error' when working with repositories on NFS-mounted directories. Evaldas Svidras reports experiencing this issue specifically during `git commit` operations. The error appears to be related to how Git interacts with NFS filesystems.

The thread seems to involve discussion between Evaldas and another participant (addressed as "Efka" in the email, possibly dpantel@gmail.com based on the email headers), though the full context of their exchange isn't visible in this message. The bus error suggests there may be memory access or filesystem synchronization issues when Git performs operations over NFS.

At this point in the thread, the key open questions appear to be:
- Whether this is a known issue with existing workarounds
- If the problem reproduces with current Git versions
- What specific conditions trigger the bus error

The severity would depend on how common this NFS configuration is among Git users, but bus errors typically indicate serious low-level problems that would need investigation.