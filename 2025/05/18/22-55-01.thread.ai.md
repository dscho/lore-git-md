Thread identity: Proposal to add `--index` flag to `git diff` as alternative to `--cached`, with eventual deprecation goal. Originating author not yet named in this initial email.

Core problem/goal: Make `git diff` terminology more intuitive by providing `--index` flag that directly references the staging area, replacing the less clear "cached" terminology. The change follows Git's pattern of adding more intuitive aliases for historical terms.

Current status: Initial proposal with working implementation. No reviews or discussion yet. Patch adds `--index` while maintaining full backward compatibility with `--cached`.

Key technical details: Modifies diff.c to add `--index` option to diff_options struct, updates Documentation/diff-options.txt, adjusts builtin/diff.c logic to handle both flags equivalently. Includes test updates in t/t4013-diff-various.sh. Change is ~20 lines of code.

Open questions: Whether the change is worth potential churn given widespread `--cached` usage in existing docs/scripts. Impact on tutorials and scripts not addressed in initial proposal.

Participant positions: None yet - this is the thread root.

Related work: Follows pattern of other terminology improvements like `git restore` replacing some `git checkout` uses.