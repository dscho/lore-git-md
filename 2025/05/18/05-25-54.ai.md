This is a documentation clarification regarding the `send-email` configuration changes. Aditya Garg points out that one commit in the `ag/doc-send-email` series (merged to 'next') needs to be reverted, while the other two documentation improvements are ready to graduate to 'master'. The problematic commit added OAuth credential helper documentation for Outlook and Gmail, which appears to have issues requiring reversion. The remaining changes include general `send-email` documentation updates and improved FQDN validation checks in the send-mail component.

The thread context shows this is part of broader documentation and `send-email` improvements that were previously noted in Junio's "What's cooking" report. The email serves as a status update on which parts of the series are ready versus needing adjustment, with a link to the revert patch for the OAuth helper documentation. The technical details mention `valid_fqdn` checks and OAuth2.0 examples as the key documentation changes involved.