This thread discusses adding a new `--index` flag to `git diff` as a more intuitive alternative to the existing `--cached` flag, which shows differences between the index and HEAD. The proposal comes from a contributor who argues that "index" better describes what the flag actually does (showing staged changes) compared to the historical "cached" terminology.

The initial patch implements the change by adding the new flag while keeping `--cached` for backward compatibility. The implementation touches the diff options handling code and documentation, with corresponding test updates. While the change is technically straightforward, it may generate discussion about whether introducing a new flag is justified given the widespread use of `--cached` in existing scripts and documentation. The thread will likely explore whether this change aligns with Git's history of gradually replacing less intuitive commands (like the `git restore`/`git checkout` transition) or if it risks causing unnecessary churn for minimal benefit.