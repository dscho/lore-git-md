This is a refactoring patch in the string-list modernization series, specifically addressing an unused parameter in the implementation.

The patch removes the unused `insert_at` parameter from `add_entry()` in string-list.c, which was always passed as -1 in practice (via `string_list_insert()`). The parameter was originally designed to allow inserting at specific positions, but this functionality was never used. The change simplifies the interface while maintaining all existing behavior.

Key technical details:
- Removes the `insert_at` parameter from `add_entry()`
- Updates the single caller (`string_list_insert()`) to no longer pass -1
- Maintains the same insertion logic via `get_entry_index()`
- Keeps the same return value convention (-1-index for existing entries)

This change follows naturally from the thread's goal of cleaning up the string-list implementation. It removes dead code and simplifies the interface without affecting functionality. The patch is straightforward and well-contained, with a clear commit message explaining the rationale. This continues the series' pattern of incremental improvements to the string-list implementation while maintaining test coverage.