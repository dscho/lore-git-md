This is a refactoring patch in shejialuo's ongoing string-list modernization series (patch 2/8). It removes an unused parameter from the string-list implementation that was effectively dead code.

The patch eliminates the `insert_at` parameter from `add_entry()`, which was always passed as -1 in practice (via `string_list_insert()`). The parameter was originally meant to allow inserting at specific positions, but this functionality was never used. The change simplifies the code while maintaining identical behavior, as all callers already relied on the auto-positioning behavior.

The commit message clearly explains the change and its motivation - the parameter wasn't being used for its intended purpose and could potentially cause problems if misused. The diff shows straightforward mechanical changes to both the function signature and its single caller. This appears to be a clean, well-justified simplification that aligns with the series' goal of improving the string-list implementation.