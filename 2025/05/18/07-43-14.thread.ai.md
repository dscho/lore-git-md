[PATCH v4 0/3] Update MyFirstContribution tutorial for current practices

Documentation patch series modernizing Git's new contributor tutorial (MyFirstContribution.adoc) to reflect current practices. Key changes across all patches:

1. Removes reference to defunct gitmentoring@googlegroups.com list (patch 1), replacing with current guidance to use git@vger.kernel.org. The commit explains this change as removing obsolete mentoring list reference.

2. Updates `cmd_psuh` example implementation (patch 2) to reflect current coding practices: 
   - Adds `struct repository *repo` parameter to match current subcommand API
   - Documents `UNUSED` macro pattern for intentionally unused parameters
   - Maintains tutorial's pedagogical approach by explaining changes rather than silently updating
   - Shows transition from UNUSED parameters to actual usage later in tutorial

3. Updates configuration API usage (patch 3), completing the series by:
   - Adding `repository.h` include
   - Replacing `git_config()` calls with `repo_config()`
   - Updating `git_config_get_string_tmp()` to `repo_config_get_string_tmp()`
   - Modifying `wt_status_prepare()` call to use passed `repo` parameter
   - Updating surrounding documentation text to reference the new API

Series touches only Documentation/MyFirstContribution.adoc with net +12 lines (43 added, 31 removed). All patches are focused documentation updates keeping the tutorial current with Git's evolving practices, particularly the ongoing `the_repository` removal effort. Patch 1 removes an 8-line section about obsolete git-mentoring list. Patch 2 updates example code while maintaining educational flow. Patch 3 modernizes config API usage to match current best practices. No technical controversy - straightforward updates with clear rationales in commit messages. The series appears ready for merging as it achieves its goal of keeping the new contributor guide aligned with current Git development practices.