Here is the updated dense thread summary:

This thread addresses a bugfix for the histogram diff algorithm (XDF_HISTOGRAM_DIFF) in xdiff, focusing on an edge case where shifted change groups during compaction produce redundant diff output containing matching lines from both files. The issue is specific to histogram diff since Myer's and patience algorithms either naturally produce minimal diffs or use anchors preventing this problem.

The technical solution adds logic in xdl_change_compact() to detect when shifting creates new matching groups, then re-diffs those sections using Myer's algorithm via xdl_fall_back_diff() while preserving all original diff flags (including --ignore-all-space handling). Test coverage includes t4073-diff-shifted-matched-group.sh with three test cases verifying the fix across scenarios.

Phillip Wood raised several technical concerns that have been addressed: confirmed both file groups must be checked during compaction, implemented safer memory handling, and established that full re-diff is necessary for correctness. He clarified that completely disabling group shifting (as Junio C Hamano initially wondered) would be worse - preventing useful coalescing of changed lines and interfering with the indent heuristic. Phillip also noted histogram diff sometimes produces harder-to-read diffs than patience diff as an inherent characteristic.

Junio C Hamano suggested extracting the new logic into a helper function for clarity, which has been implemented. The thread has reached consensus on the approach, with all substantive technical concerns resolved. The original commit message provided clear motivation with abstract/concrete examples, noting this affects <1% of diffs but fixes clearly undesirable output when it occurs. The patch is now in final review with only minor implementation refinements remaining before merge.