Here is the updated dense thread summary:

This thread addresses a bugfix for the histogram diff algorithm (XDF_HISTOGRAM_DIFF) in xdiff, focusing on an edge case where shifted change groups during compaction produce redundant diff output containing matching lines from both files. The issue is specific to histogram diff since Myer's and patience algorithms either naturally produce minimal diffs or use anchors preventing this problem.

The technical solution adds logic in xdl_change_compact() to detect when shifting creates new matching groups, then re-diffs those sections using Myer's algorithm via xdl_fall_back_diff() while preserving all original diff flags (including --ignore-all-space handling). The implementation has been refined to check only one group rather than both (as Phillip Wood confirmed sufficient), uses safer memory handling patterns, and moves the new logic into a helper function for clarity as Junio C Hamano suggested.

Test coverage includes t4073-diff-shifted-matched-group.sh with three test cases verifying the fix across scenarios. Phillip Wood raised and resolved several technical concerns: confirmed group shifting shouldn't be completely disabled (as it enables useful coalescing of changed lines and works with the indent heuristic), implemented safer memory handling, and noted histogram diff's inherent tendency to sometimes produce harder-to-read diffs than patience diff (though this patch doesn't address that broader issue).

The commit message provides clear motivation with abstract/concrete examples, noting this affects <1% of diffs but fixes clearly undesirable output when it occurs. The thread has reached consensus on the approach with all substantive concerns resolved. The patch appears ready for merging with only minor implementation refinements remaining.