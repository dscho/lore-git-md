Refactoring patch in a multi-pack index (MIDX) series. This is the first of 17 patches from Taylor Blau, focusing on making the MIDX code more precise in its const-correctness.

The patch marks the `get_midx_checksum()` function's parameter as `const`, making it clear the function doesn't modify the passed `multi_pack_index` struct. This is a straightforward change affecting both the declaration in midx.h and implementation in midx.c. The commit message notes this is preparatory work for a follow-up commit that will rename the function to better reflect its behavior (returning raw checksum bytes rather than hex encoding).

Key technical details for future reference:
- Function: `get_midx_checksum()` in midx.{c,h}
- Changes: Adds `const` qualifier to parameter type
- Context: Part of larger MIDX work, with next patch renaming this function
- Impact: No behavior change, just improved type safety
- Files touched: midx.c and midx.h with simple signature updates

The change appears well-justified and mechanically sound, following good practice for const-correctness in C. Being the first in a series, it sets up for more substantive MIDX changes to come.