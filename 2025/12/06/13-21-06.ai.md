Security-focused series finalizing the elimination of insecure `mktemp(3)` usage in Git. This v2 series builds on the previous discussion by adding a fifth patch to remove the now-trivialized `gitmkdtemp()` compatibility wrapper, completing the cleanup path discussed earlier in the thread.

The series introduces `git_mkdtemp()` as a secure alternative (patch 1), migrates the Windows compatibility layer to use it (patch 2), removes the obsolete `mingw_mktemp()` implementation (patch 3), bans `mktemp(3)` via `banned.h` (patch 4), and now adds the discussed removal of `gitmkdtemp()` (new patch 5). The v2 changes include clarifying comments about return values from `git_mkdstemps_mode()` and the additional cleanup patch.

The implementation achieves a net reduction of 33 lines across 10 files while maintaining cross-platform compatibility. The security rationale remains focused on proactively removing insecure functions before exploits emerge, with all callers now migrated to secure alternatives. The series has received review input from Jeff King and Junio Hamano, with consensus around the direction of removing redundant compatibility layers once the secure infrastructure is in place.

Key technical details for future reference: The new `git_mkdtemp()` builds on existing `git_mkstemps_mode()` infrastructure but handles directory creation with secure permissions (0700). The removal of `gitmkdtemp()` completes the simplification path discussed earlier in the thread, though build system considerations (particularly CMake on macOS) remain a factor for any potential future removal of the NO_MKDTEMP knob. The series leaves open the longer-term question of whether to similarly ban `mkdtemp(3` system calls, which would require consideration of third-party code dependencies.