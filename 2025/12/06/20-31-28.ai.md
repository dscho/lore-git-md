Preparation patch for MIDX compaction series introducing overflow-safe 32-bit addition.

This patch adds a new `u32_add()` helper function to git-compat-util.h, following the pattern of the existing `u64_mult()` function. The function performs overflow-checked addition of two uint32_t values, dying with an error message if overflow would occur. The implementation uses the existing `unsigned_add_overflows()` macro to detect overflow conditions.

The commit message indicates this is needed by a future commit in the series, though it doesn't specify which one. Given the context of the MIDX compaction work, this is likely related to safely calculating object counts or offsets when combining MIDX layers. The change is straightforward and well-contained, adding only the new function with its documentation.

This appears to be a preparatory infrastructure change that enables safer arithmetic operations in the subsequent compaction logic. The patch follows established patterns for overflow checking in Git's codebase and maintains consistency with similar existing functions.