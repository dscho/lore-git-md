Here's the updated dense AI thread summary:

Performance optimization for promisor pack handling, focusing on `add_promisor_object()` and `parse_object_with_flags()`. Current implementation unnecessarily decompresses and hashes all objects during promisor set initialization. Aaron Plattner's v2 patch modifies these functions to properly handle `OBJ_NONE` objects (from `mark_uninteresting()`) and uses `PARSE_OBJECT_SKIP_HASH_CHECK` for blobs. Jeff King (Peff) reviewed the patch, agreeing with the technical approach but suggesting: (1) splitting the `OBJ_NONE` handling fix into a separate bugfix patch, and (2) explicitly documenting in the commit message why skipping hash checks for all objects is safe (referencing commit c868d8e91f).

In follow-up discussion, Aaron Plattner questions whether skipping hash verification for all object types (not just blobs) might be unsafe for detecting on-disk corruption, since trees/commits are actually used for their contents. He proposes three options: (1) splitting `PARSE_OBJECT_SKIP_HASH_CHECK` into blob-specific and general flags, (2) reverting to the v1 approach, or (3) keeping current version if risk is acceptable. Aaron confirms he'll extract the `OBJ_NONE` handling fix as a separate patch regardless. The discussion now focuses on safety tradeoffs of hash verification skipping, with Jeff having previously approved the technical implementation but requested clearer justification. Benchmark shows dramatic improvement (76 minutes -> 2 minutes for 176GB promisor packs). Open questions remain about the safest approach for hash verification and whether to modify the flag system, with references to prior work in c868d8e91f. Core optimization is sound with consensus pending on these safety considerations. Related to prior promisor pack and object parsing optimizations.