This is a follow-up to the third patch in Patrick Steinhardt's bugfix series addressing object database issues, specifically responding to Eric Sunshine's review about a documentation inconsistency in the commit message.

The original patch fixed a memory leak in submodule handling during git-grep operations by correcting the cleanup sequence in `odb_free()`. Eric pointed out that the commit message's phrasing "In the next commit" didn't make sense for the final patch in a series. Patrick acknowledges this was a copy-paste artifact from when the fix was part of a larger series, and shares the queued documentation fix that removes the forward-looking language while keeping the technical explanation of the leak scenario.

The core technical content remains unchanged - this still fixes the 192-byte leak when loading packfiles/multi-pack-indexes from submodules by properly ordering the `odb_close()` and `odb_free_sources()` calls. The thread context shows this was one of three independent odb fixes split from larger work, with the other patches addressing geometric repacking with promisors and commit-graph writing issues.

Key technical details for future reference:
- Fix location: `odb.c` cleanup sequence in `odb_free()`
- Leak scenario: submodule packfile loading during git-grep
- Leak size: 192 bytes per occurrence
- Root cause: incorrect free/close ordering
- Related to: quarantine directory handling (ac65c70663)
- No new tests added despite detailed leak analysis

The exchange is purely about commit message clarity - the technical solution remains sound and uncontroversial, with Patrick opting not to respin the series for this documentation tweak.