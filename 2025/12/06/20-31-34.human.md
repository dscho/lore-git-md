This is part 12 of Taylor Blau's 17-patch series implementing MIDX layer compaction, which aims to improve incremental repacking in Git's multi-pack index system. The series builds on prior refactoring work to enable more efficient handling of packfiles.

This patch extracts a helper function `fill_pack_from_midx()` from the existing `fill_packs_from_midx()` function. The new helper centralizes the logic for loading pack information from a MIDX file, which will be needed for the upcoming MIDX compaction functionality. The change is purely a refactoring - no behavior is modified, just reorganized to avoid future code duplication when compaction support is added.

The patch follows the series' pattern of careful, well-explained incremental changes. Each commit message clearly states both what is being done and why, making the overall series easier to review. This particular change is straightforward and unlikely to be controversial, serving as preparation for more substantial compaction logic in later patches.

The diff shows the mechanical nature of the change - moving existing code into a new function while maintaining all the same error handling and pack information population. The series continues to demonstrate high-quality, focused changes that build toward the larger goal of MIDX compaction.