Here's the human-readable thread summary:

This thread focuses on optimizing how Git handles promisor packs, specifically addressing a performance bottleneck in `add_promisor_object()`. The original patch from Aaron Plattner showed dramatic improvements - reducing processing time for a 176GB promisor pack from 76 minutes to under 2 minutes by avoiding unnecessary decompression and hash checks for blobs.

The key technical change modifies just 4 lines across object.c and packfile.c, but makes a significant difference for repositories using promisor packs. Version 2 of the patch improved handling of `OBJ_NONE` objects that arise during `mark_uninteresting()` operations.

Jeff King (Peff) has reviewed the patch and agrees with the technical approach, but suggests two refinements: splitting the `OBJ_NONE` fix into a separate commit for clarity, and expanding the commit message to better explain why skipping hash checks is safe for all objects (not just blobs). He references prior work in commit c868d8e91f as relevant context.

The thread shows consensus on the optimization's effectiveness, with discussion now focused on how to best structure and document the changes before merging. The core performance improvement remains validated, with the benchmark results standing as strong evidence of the patch's value for large promisor pack scenarios.