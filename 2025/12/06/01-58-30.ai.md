Performance optimization follow-up for promisor object handling, addressing edge cases and environmental factors.

Jeff King provides additional analysis of the `OBJ_NONE` edge case in promisor pack processing, explaining how these objects originate from `mark_uninteresting()` during revision walks. The backtrace shows these dummy objects are created via `create_object()` when preparing the revision walk, which explains why they appear in the promisor object set without type information.

The email resolves the mystery of differing performance results between test cases - the timing variations depend on whether `is_promisor_object()` checks occur before or after the uninteresting` flagging in the revision walk process. King notes that his initial test case checked promisor status early (during tip loading), while Plattner's scenario hit the slower path where objects are first flagged uninteresting.

The discussion also touches on environmental factors affecting performance, particularly commit-graph availability in Scalar clone worktrees. King suggests checking for commit-graph files and manually generating one if missing, as this infrastructure significantly impacts the optimization's effectiveness. The pathological cases Plattner observed in `git pull` may correlate with missing commit-graph data.

This email doesn't introduce new code changes but provides important context about:
1) The lifecycle of `OBJ_NONE` objects in promisor processing
2) How revision walk ordering affects performance measurements
3) Real-world deployment considerations (Scalar worktrees and commit-graph interaction)

The analysis confirms the optimization approach remains sound while highlighting environmental factors that maintainers should consider when evaluating performance claims. The thread appears ready for Plattner to submit the v2 patch incorporating both the type-check fixes and King's flag-based optimization.