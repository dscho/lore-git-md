This is a technical discussion about Windows symlink handling in patch 3/10 of the test suite preparation series. The exchange clarifies the intended behavior specification for the `open()` emulation on Windows.

The discussion focuses on whether the commit message should describe the behavior as matching Linux specifically or POSIX more generally. Johannes Schindelin originally framed the change as making Windows behave "like Linux" when handling `O_CREAT | O_EXCL` with symlinks. Junio Hamano suggests this should reference POSIX standards instead, since Git's requirements align with POSIX specifications rather than Linux-specific behavior.

Key technical points:
- The patch modifies Windows' `_wopen()` to fail when given `O_CREAT | O_EXCL` on a symlink, rather than following the symlink
- The debate centers on documentation clarity for future maintainers
- Junio argues the commit message should reference POSIX standard behavior rather than Linux specifically, to avoid implying Linux-specific quirks are required

This is a documentation refinement discussion for an already-technically-settled patch. The underlying code change (making Windows fail appropriately on symlink+O_CREAT|O_EXCL) remains uncontested, but the maintainer wants the rationale documented in terms of standards compliance rather than platform emulation. The exchange shows the maintainer attention to making commit messages maximally useful for future developers.