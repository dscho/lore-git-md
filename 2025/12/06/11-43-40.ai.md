This is a follow-up discussion about the third patch in Patrick Steinhardt's bugfix series addressing object database issues. The patch fixes a memory leak in submodule handling by correcting the cleanup sequence in `odb_free()`.

Eric Sunshine points out lingering documentation issues in the commit message that reference non-existent "next commits" - a leftover from when this fix was part of a larger series. Patrick had already prepared a local fix removing the forward-looking language while preserving the technical explanation of the leak scenario (192-byte leak during git-grep operations when loading packfiles/multi-pack-indexes from submodules).

The core technical content remains unchanged - this remains a straightforward fix swapping `odb_close()` and `odb_free_sources()` calls in odb.c to properly clean up resources. The discussion is purely about commit message clarity, with Eric suggesting further removal of the "In the next commit" phrasing that no longer makes sense for this standalone patch. No code changes or series respin appear necessary beyond the documentation tweak Patrick has already queued.