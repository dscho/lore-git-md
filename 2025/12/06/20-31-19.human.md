This is a technical refactoring patch in Taylor Blau's MIDX compaction series, addressing a subtle indexing issue in the bitmap position assignment code. The change prepares for upcoming compaction work by removing a dependency on the `pack_perm` array when setting bitmap positions for packs.

The patch explains that while the current use of `pack_perm` is safe (because expired packs don't affect bitmap writing), an upcoming compaction change would make this approach problematic. The new code simplifies the logic by directly indexing into the pack info array rather than going through the permutation array, making the code more robust for future changes.

This appears to be a well-justified preparatory change - the commit message clearly explains both the current safe behavior and why the change is needed for future work. The actual code change is minimal (a one-line modification) but addresses a potentially tricky edge case that could arise during compaction. As part of a larger performance-focused series, this patch helps maintain correctness while enabling more efficient MIDX handling.