Performance optimization for promisor pack handling, continuing the thread about skipping unnecessary blob processing in `add_promisor_object()`. Jeff King (Peff) reviews Aaron Plattner's v2 patch that improves handling of `OBJ_NONE` objects in `parse_object_with_flags()` while optimizing promisor pack initialization.

Peff agrees with the technical approach but suggests two refinements: (1) considering whether the `OBJ_NONE` handling fix should be split into a separate bugfix patch, and (2) explicitly addressing in the commit message why skipping hash checks for all objects (not just blobs) is safe, referencing prior discussion in commit c868d8e91f. The review confirms the patch's effectiveness while raising these minor structural/documentation considerations.

The changes remain minimal (4 lines across object.c and packfile.c) but impactful, with the original benchmark showing a dramatic speedup from 76 minutes to under 2 minutes for large promisor packs. The thread shows consensus on the technical solution while discussing how to best structure and document the changes.