Security series finalizing the removal of insecure `mktemp(3)` usage. This v2 patch series from René Scharfe completes the work to ban this insecure function, building on previous discussion and reviews from Jeff King and Junio Hamano.

The series introduces a secure `git_mkdtemp()` wrapper (patch 1), migrates the Windows compatibility layer to use it (patch 2), removes the now-unused `mingw_mktemp()` implementation (patch 3), and adds `mktemp(3)` to `banned.h` (patch 4). The v2 revision adds a fifth patch to remove the trivialized `gitmkdtemp()` wrapper as well, following up on earlier discussion about simplifying the compatibility layer.

Key technical changes include clarifying the return value documentation for the new `git_mkdstemps_mode()` helper function and consolidating temporary directory creation logic. The series shows careful attention to cross-platform compatibility while eliminating insecure code paths, resulting in a 33-line net reduction across the codebase.

This appears to be the final iteration of this security hardening effort, incorporating all the feedback from the previous round. The changes are well-motivated (POSIX standard no longer specifies `mktemp(3)` due to security concerns) and the implementation looks thorough, with proper attention to Windows compatibility and build system impacts. The series has already received positive feedback from key reviewers earlier in the thread.