This is a refactoring patch that's part of a larger series (1/17) from Taylor Blau. The change marks the `get_midx_checksum()` function's argument as `const` to make it clear the function doesn't modify its `multi_pack_index` parameter. This is a straightforward documentation-through-code change that helps readers understand the function's behavior at a glance.

The commit message notes this is preparatory work for a follow-up commit that will rename the function to better reflect that it returns raw checksum bytes rather than a hex-encoded string. The patch touches both the midx.c implementation and midx.h header file with simple one-line changes.

This appears to be a well-justified, mechanical change that improves code clarity without altering behavior. As the first in a series, it sets up for more substantive MIDX-related changes to come. The commit message clearly explains both the immediate change and how it fits into the larger context.