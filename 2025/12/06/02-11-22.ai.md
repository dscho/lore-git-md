Security follow-up discussion about mkdtemp() wrapper consolidation.

Jeff King and René Scharfe continue their discussion about the secure temp file infrastructure changes, focusing on the NO_MKDTEMP compatibility wrapper. While King initially proposed simplifying the wrapper to directly use the new `git_mkdtemp()` function, Scharfe's concerns about build system complexity (particularly CMake issues on macOS and limited testing coverage) have convinced him this deserves a separate patch.

The discussion now turns to the longer-term question of whether to completely ban `mkdtemp(3)` system calls (matching the approach taken with `mktemp(3)` in this series) or maintain the redirection through the compatibility layer. King notes this decision should align with whatever approach is taken for `mkstemp()`, and raises a practical concern about third-party code like the clar test framework which currently calls `mkdtemp()` directly - maintaining the redirection would avoid needing to patch imported sources.

This exchange shows the security-focused thread moving into implementation refinement, with both participants demonstrating careful consideration of real-world constraints (build system fragility, third-party dependencies) while maintaining the security goals. The discussion remains technical and collaborative, with King acknowledging Scharfe's valid points while still exploring the design space. The thread appears to conclude this particular sub-discussion, leaving the NO_MKDTEMP wrapper cleanup and potential `mkdtemp(3) ban as future work to be addressed separately.