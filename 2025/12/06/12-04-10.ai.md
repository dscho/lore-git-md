This is a documentation clarification for the third patch in Patrick Steinhardt's bugfix series addressing object database (odb) subsystem issues. The thread has already established the technical merit of the patch, which fixes a 192-byte memory leak during git-grep operations involving submodules by correcting the cleanup sequence in `odb_free()`.

The current discussion focuses solely on improving the patch's commit message. Eric Sunshine pointed out that the original message contained forward-looking language ("In the next commit...") that was a holdover from when this fix was part of a larger series. Patrick acknowledges this and had already prepared a local fix removing the problematic phrasing while preserving the technical explanation of the leak scenario. Eric further suggests removing the entire "In the next commit" clause, which Patrick agrees with.

The patch itself remains unchanged - this is purely about documentation clarity. The technical content (swapping `odb_close()` and `odb_free_sources()` calls in odb.c) and its motivation (fixing leaks when loading packfiles/multi-pack-indexes from submodules) are well-established in the thread. With this final documentation tweak, the series appears ready for merging with no outstanding issues.