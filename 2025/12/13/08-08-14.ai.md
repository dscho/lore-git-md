This is part 8/10 of the submodule gitdir path encoding series, implementing URL encoding for submodule paths to prevent filesystem collisions when `extensions.submodulePathConfig` is enabled. The patch focuses on handling nested submodule gitdirs by applying RFC3986 URL encoding to problematic path components.

The key technical changes include:
- Adding URL encoding as a fallback strategy in `create_default_gitdir_config()` when plain paths would collide
- Introducing `validate_submodule_encoded_git_dir()` for validating encoded paths
- Splitting validation logic between legacy and encoded modes via `validate_submodule_legacy_git_dir()`
- Comprehensive test coverage for nested submodule scenarios including parallel cloning
- Enforcement that encoded paths cannot contain forward slashes

The implementation maintains backward compatibility while solving the nested gitdir problem through URL encoding of path components. Tests verify both the encoding behavior and proper interaction with parallel submodule cloning operations. The validation logic is now split cleanly between legacy and encoded modes based on the `extensions.submodulePathConfig` setting.

This builds on the series' established architecture while addressing the specific challenge of nested submodule paths. The solution follows the design consensus reached earlier in the thread, using URL encoding only when necessary for new submodules while maintaining existing behavior for legacy cases.