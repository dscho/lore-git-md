This is a refactoring patch series that continues the MEMZERO_ARRAY() conversion work started by Toon Claes. The series consists of two patches aimed at improving code consistency and passing Coccinelle checks.

The first patch modifies the coccicheck target in the Makefile to emit the contents of the Cocci patch, which appears to be a debugging aid for the Coccinelle transformation process. The second patch replaces several manual memset() calls with the MEMZERO_ARRAY() macro in diffcore-delta.c, linear-assignment.c, and shallow.c. This follows the pattern established in previous work to standardize array zeroing operations.

The changes are mechanical replacements that don't alter functionality, focusing on code style consistency. The series builds on existing work to make the codebase pass Coccinelle checks at the tip of the 'seen' branch. The patches touch core components (diff and object storage) but the changes are straightforward and low-risk, being simple API substitutions with identical behavior.

Key technical details for future reference:
- Files modified: Makefile, diffcore-delta.c, linear-assignment.c, shallow.c
- Macro being standardized: MEMZERO_ARRAY()
- Original functions being replaced: memset()
- Build system impact: coccicheck output format change
- Branch context: part of ongoing work to pass Coccinelle checks on 'seen' branch

The series appears to be a routine maintenance update with no expected controversy, continuing established patterns in the codebase.