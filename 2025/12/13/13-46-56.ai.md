This is the first patch in a v2 documentation series clarifying `git replay` behavior. It explicitly documents that the command produces no stderr output when conflicts occur, unlike some other Git commands. The patch adds this clarification in the man page with a cross-reference to the EXIT STATUS section, which already notes the command exits with status 1 on conflicts.

The change is straightforward and uncontroversial, implementing a documentation improvement that was discussed in the v1 review. The patch maintains consistency with the series' goal of precisely documenting `git replay`'s behavior, particularly around its silent conflict resolution approach. The addition of the `[[exit-status]]` anchor (while not strictly necessary for this change) helps future cross-references in the document.

Key technical details:
- Modifies `Documentation/git-replay.adoc` to document silent conflict behavior
- Adds anchor for EXIT STATUS section
- No functional changes to the command itself
- Follows up on v1 discussion about clarifying this behavior

This patch appears ready for inclusion as part of the broader documentation improvements for `git replay`. The series as a whole has addressed previous feedback and seems to be converging toward a final version.