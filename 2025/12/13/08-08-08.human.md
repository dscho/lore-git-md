This is part of the submodule gitdir path encoding series (v6), specifically focusing on validation improvements. The patch centralizes submodule gitdir validation checks inside `submodule_name_to_gitdir()` rather than having them scattered across callers. 

The change makes the API more robust by ensuring validation always happens when getting a submodule's path, preventing callers from accidentally skipping checks. It removes redundant validation from three locations while keeping two critical checks in `clone_submodule()` to handle parallel clone races. The commit message clearly explains the benefits: reduced code duplication, eliminated risk of missed validations, and preparation for upcoming config-based gitdir paths.

The implementation looks clean and well-motivated, removing 8 lines while adding 4, with the validation logic now consolidated in one place. This appears to be a straightforward improvement to the series' infrastructure that doesn't affect the higher-level path encoding functionality being developed. The patch follows the series' architectural direction while simplifying the codebase.