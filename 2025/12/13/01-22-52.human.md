This is a follow-up in the MEMZERO_ARRAY/Coccinelle series, focusing on build system integration issues. Junio Hamano reports being unable to reproduce the `builtin/last-modified.c` case that motivated the include path changes in patch 2/2, despite the Coccinelle rules working for similar cases elsewhere. He shares the exact rule modifications he tested with (based on René Scharfe's earlier suggestions), showing the expanded pattern matching for both pointer and array syntax with multiple sizeof() variants.

The email reveals an inconsistency in Coccinelle's behavior - while it handles `eword_t` cases in other files, it mysteriously misses the `last-modified.c` case. This keeps open the question of whether explicit include paths are truly needed, as Patrick Steinhardt had questioned earlier. Junio's reproduction attempt suggests the issue might be more subtle than originally thought, though he notes he didn't expect his minimal test case to produce changes.

The thread remains at an impasse regarding the build system changes, with the original motivation now in doubt but no clear alternative solution yet proposed for whatever underlying issue exists with type resolution. The core MEMZERO_ARRAY implementation appears uncontested, but the Coccinelle integration path needs resolution.