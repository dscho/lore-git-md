This is the final patch in a documentation series improving the `git replay` man page. It standardizes the Asciidoc markup by converting a plain text reference to the OUTPUT section into a proper cross-reference link (using `<<output,OUTPUT>>` syntax) and adds the corresponding anchor (`[[output]]`).

The change is straightforward and uncontroversial, completing the series' goal of modernizing the documentation's formatting. The patch follows through on the standardization work started in earlier patches, ensuring consistent linking style throughout the manual page. 

This concludes the v2 series which has already addressed previous review feedback, including clarifying the `--contained` option's behavior and documenting silent conflict resolution. With this final markup cleanup, the documentation improvements appear ready for merging.