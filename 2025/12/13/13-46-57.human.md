This is the second patch in a documentation series improving the `git replay` man page, focusing on clarifying the `--contained` option. The patch addresses feedback from the v1 discussion by making two key improvements: first, it replaces the vague "contained in" phrasing with a more precise description that explains `--contained` updates branches pointing to commits in the revision range. Second, it standardizes on using "update" rather than "advance" as the verb, which reviewers agreed is more conventional in Git's terminology.

The patch also adds an important clarification that `--contained` requires the `--onto` option, which wasn't previously documented. The changes are made in both the man page and the corresponding help text in the source code to keep them consistent. The commit shows good attention to detail by maintaining parallel wording between documentation and implementation, and it carries forward the consensus from v1 review while adding this new clarification.

This continues the series' trend of making the `git replay` documentation more precise and complete, particularly around edge cases and option interactions. The changes are straightforward and uncontroversial, building on previous reviewer feedback.