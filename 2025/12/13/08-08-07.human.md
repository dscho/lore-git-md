This is a refactoring patch in the submodule gitdir path encoding series, which standardizes path handling in the submodule--helper code.

The patch updates `add_submodule()` to use the centralized `submodule_name_to_gitdir()` helper function instead of hardcoding the path construction. This change aligns with the series' goal of having consistent path handling across all submodule operations. The modification fixes test failures that occurred when testing the new path encoding scheme.

The change is straightforward - it replaces a direct path construction (`".git/modules/%s"`) with a call to the helper function, while also cleaning up some variable scoping. This is part of the foundational work (phase 1) in the series to establish core extension infrastructure before implementing the actual path encoding functionality.

The patch appears well-contained and follows the established pattern seen elsewhere in the codebase. Since this is part of a larger, well-reviewed series that has reached architectural consensus, this particular change seems uncontroversial and focused on technical consistency.