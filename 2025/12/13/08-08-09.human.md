This is part of the submodule gitdir path encoding series (v6), specifically patch 3/10 implementing core infrastructure. The patch adds a new `gitdir` subcommand to `submodule--helper` that exposes the internal `submodule_name_to_gitdir()` path computation logic.

The new command provides a way for users and tests to query the computed gitdir path for a given submodule name, which will be useful for both debugging and for the upcoming gitdir configuration feature. The implementation is straightforward - it takes a submodule name as input and outputs the corresponding gitdir path, leveraging the centralized path resolution logic that was refactored in earlier patches.

This appears to be a well-contained plumbing change that serves as groundwork for the rest of the series. The patch follows the established pattern of the submodule helper commands and includes proper error handling. Since this is part of a larger, well-discussed series that has reached architectural consensus, this particular piece seems uncontroversial and focused on its specific task.