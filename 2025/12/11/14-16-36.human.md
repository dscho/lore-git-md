This is a review/acknowledgement of Patrick Steinhardt's bugfix series addressing memory management issues in Git's commit-graph writing functionality.

Toon Claes confirms the correctness of Patrick's fix in `builtin/gc.c` that replaces `commit_list_append()` with `commit_list_insert()` to prevent memory leaks during depth-first search operations. The reviewer notes that since the list is only used for counting (not ordering), this change is appropriate. The fix addresses both memory management and correctness issues in commit counting, particularly for merge commits.

The email also indicates approval for the entire series, which includes this commit-graph fix and another related to submodule ODB leaks. This suggests the fixes have been thoroughly reviewed and are ready for merging. The changes were previously discussed with Justin Tobler regarding test coverage improvements, which have now been implemented.

The patch appears well-vetted at this point, with clear technical explanations in the original commit message and now explicit approval from another contributor. The changes are focused and maintainable, touching only the necessary parts of the code while adding appropriate test coverage.