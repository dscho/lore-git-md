This is patch 6/8 in Patrick Steinhardt's v3 series refactoring Git's object database alternates handling. The patch focuses on code cleanup following the architectural changes made in previous patches.

The key change here is removing a forward declaration of `read_info_alternates()` that is no longer needed after patch 5/8 eliminated the mutual recursion between this function and `link_alt_odb_entries()`. The patch reorganizes the code in `odb.c` to group related functions together now that the forward declaration isn't required, moving `read_info_alternates()` and its helper functions up in the file.

Notably, the patch removes outdated documentation for `read_info_alternates()` that no longer accurately described the function's behavior. The commit message explains this was unclear and didn't match the modern implementation. The actual functionality remains unchanged - this is purely a code organization improvement.

The diff shows significant line movement (125 lines total, with 54 insertions and 71 deletions) as functions are reordered, but no functional changes. This continues the series' pattern of cleaning up the alternates handling code while maintaining all existing behavior. The change is straightforward and builds on the more substantial architectural work done in earlier patches of the series.