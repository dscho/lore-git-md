This is a clarification request in the ongoing discussion about `git pull --rebase` fork-point behavior with multiple push URLs. K Jayatheerth seeks confirmation about the technical underpinnings of the fork-point calculation before proposing solutions.

The email focuses on understanding two key functions (`get_rebase_fork_point()` and `get_rebase_newbase_and_upstream()`) that implement the fork-point heuristic. The author wants to verify their understanding that this heuristic examines the upstream branch's reflog to detect prior rebase/reset operations, which affects how Git selects the merge base during rebase operations.

This comes at a point where the thread has already established that the fork-point calculation occurs before fetching (contrary to earlier assumptions) and that push-originated reflog entries are causing problems when push/pull URLs diverge. The email doesn't propose new solutions but rather aims to build foundational understanding before contributing to the solution space.

Key technical elements mentioned are the fork-point calculation functions and their reliance on reflog examination to adjust merge-base selection. The question implies the author is considering approaches that might modify this behavior while needing to confirm the current implementation details first. This reflects the thread's broader challenge of balancing fork-point heuristics with URL divergence scenarios.