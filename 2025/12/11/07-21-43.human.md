This is a bugfix in Patrick Steinhardt's ongoing ODB alternates refactoring series (v2 4/8). SZEDER Gábor points out a compilation error in the patch - it references `odb_add_source()` which doesn't exist yet in the codebase. The patch intended to replace `link_alt_odb_entries()` with a more direct `odb_add_source()` call to better match the single-directory use case, but the function name appears to be incorrect or premature.

The issue highlights a practical problem with the series' incremental development - later patches reference functions that haven't been introduced yet. This is the kind of issue that typically gets caught in CI testing, but the reporter helpfully flags it early. The patch's conceptual goal (simplifying alternates handling) still makes sense, but it will need to be adjusted to use the correct function name or reordered in the series.

This is a good example of why multi-patch series often need careful review of both the logical progression and buildability at each step. The thread context shows the series is otherwise complete and reviewed, so this should be a straightforward fix to align the function names.