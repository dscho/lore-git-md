This is a follow-up discussion about a bugfix in the commit-graph writing logic, part of Patrick Steinhardt's series addressing object database issues (though the first patch was withdrawn).

The exchange clarifies a test improvement suggestion from Justin Tobler regarding the commit-graph test case. Justin had proposed keeping the feature branch in the test scenario to better validate that commits aren't double-counted when calculating whether to write a new commit-graph. Patrick confirms this would be a useful enhancement now that the underlying bugs (a memory leak and SEEN flag miscounting) have been fixed.

The original patch (now patch 2/3 in the revised series) fixed two issues in `builtin/gc.c`: incorrect memory management when building commit lists, and reference tip miscounting that affected the auto-write decision logic. The test case being discussed validates the latter fix by checking when maintenance triggers commit-graph writes.

This appears to be a productive refinement of the test coverage, showing the core fixes are working as intended. The discussion remains focused and technical, with Patrick demonstrating good responsiveness to review feedback. The change would make the test more thorough by ensuring merge commits and their parents are properly handled in the commit-graph write calculations.