This is a cover letter for a bugfix series addressing object database issues, with two remaining patches after withdrawing one problematic change from v1. The series now focuses on:

1. Fixing commit-graph writing conditions in `builtin/gc.c` by correcting SEEN flag handling and reference tip counting, with improved test coverage in `t/t7900-maintenance.sh` that now better exercises merge scenarios (retaining feature branches rather than deleting them).

2. Fixing a memory leak in `odb.c` when reading submodule data via `git-grep`, caused by incorrect cleanup sequence between `odb_close()` and `odb_free_sources()`. This appears to be a straightforward fix with existing test coverage.

The withdrawn patch regarding geometric repacking with promisor remotes revealed deeper architectural issues that will need separate work. The current two patches are clean fixes with Patrick having addressed Justin Tobler's test coverage suggestions in v2. The changes are well-contained and appear ready for merging, with the commit-graph fix now having more robust test validation of its SEEN flag handling in merge scenarios.