This is a technical clarification in the ongoing discussion about `git pull --rebase` behavior with multiple push URLs. Phillip Wood responds to K Jayatheerth's question about the fork-point calculation implementation details, confirming that `get_rebase_fork_point()` and `get_rebase_newbase_and_upstream()` are indeed the key functions involved.

The explanation clarifies that the fork-point heuristic examines the upstream branch's reflog to find the most recent commit that is a descendant of the local branch, using this to limit the rebase range when the upstream has been reset or rewritten. Phillip points to the official Git documentation which contains a helpful diagram illustrating this behavior.

This email doesn't propose new solutions but serves to ensure all participants share a correct understanding of the fork-point mechanism's current implementation before considering modifications. The technical precision here will be important for evaluating whether proposed fixes (like disabling fork-point for pushurl cases or adding advisory messages) would interact correctly with these underlying functions. The reference to the documentation suggests this is an established behavior that any solution would need to either work within or explicitly override.