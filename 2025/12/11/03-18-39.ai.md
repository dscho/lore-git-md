This is a review comment on the Coccinelle semantic patch from the MEMZERO_ARRAY series, specifically addressing pattern matching completeness.

Junio Hamano points out that the current transformation rule in array.cocci only catches memset() calls with literal 0 or 0x0 as the fill value, but suggests it should also handle the common pattern using '\0' (null character) notation. This would make the semantic patch more comprehensive in converting existing code to the new MEMZERO_ARRAY() macro.

The comment focuses narrowly on the pattern matching logic in the Coccinelle rule, implying the author may want to expand the matching criteria before the patch is finalized. This is a technical refinement suggestion rather than a fundamental objection to the approach.

Key technical details:
- Current rule transforms memset(ptr, 0/0x0, n*sizeof(T)) to MEMZERO_ARRAY(ptr, n)
- Proposed addition would also catch memset(array, '\0', sizeof(array[0])*ARRAY_SIZE(array))
- Does not affect the macro implementation itself, only the Coccinelle conversion rules
- Suggests author may want to make the pattern matching more thorough before finalizing

This moves the thread forward by identifying a potential gap in the automated conversion coverage while the series is still in its initial review phase. The comment maintains the thread's focus on code quality improvements through consistent memory initialization patterns.