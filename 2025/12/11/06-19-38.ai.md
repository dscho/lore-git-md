This is a follow-up discussion in the Coccinelle build system patch thread, focusing on the include path changes proposed in patch 2/2. Patrick Steinhardt questions the necessity of explicitly passing include paths to spatch, noting that Git's codebase already includes headers relative to the root directory and that the compiler doesn't require these paths.

Key technical discussion points:
- Patrick challenges the premise of the fix, pointing out that `ewah/ewok.h` is already properly included in `builtin/last-modified.c` without needing special include paths
- He observes that Git's compilation doesn't require these include paths, making their addition to Coccinelle analysis seem inconsistent
- The core question is whether Coccinelle truly needs these paths to resolve types, or if there's another underlying issue with the rule matching

This email represents the first substantive pushback on the technical approach of the series, specifically targeting the build system changes meant to support the new MEMZERO_ARRAY conversions. The discussion now centers on whether the include path solution is addressing the right problem, with Patrick suggesting the issue might lie elsewhere in how Coccinelle processes the types. This could lead to either a revision of the approach or a more detailed explanation from the original author about why these paths are necessary for spatch but not for compilation.