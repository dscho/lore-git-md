This is a follow-up discussion in the ongoing thread about enhancing Git's `repo structure` command with object size reporting. The specific focus here is on human-readable formatting of count values.

Junio Hamano responds to Justin Tobler's patch that introduced static unit strings ("k", "M", "G") for humanizing count values. He suggests reusing existing Git infrastructure like `strbuf_humanise_bytes()` from progress.c rather than implementing a custom solution. This comes after Patrick Steinhardt had previously pointed out that the unit strings should be translatable (which Justin acknowledged would be addressed in the next version).

Key technical points:
- The discussion centers around the implementation in `builtin/repo.c` where count values are being formatted
- Current patch uses hardcoded unit strings (unit_k, unit_M, unit_G)
- Junio points to `strbuf_humanise_bytes()` as a potential existing solution
- Follows up on Patrick's earlier feedback about internationalization

This is part of the larger effort to improve the `repo structure` command's output formatting, specifically around how it presents object counts in human-readable form. The thread shows the maintainer guiding the contributor toward better code reuse while building on previous feedback about internationalization.