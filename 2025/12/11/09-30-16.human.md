This is patch 7/8 in Patrick Steinhardt's series refactoring Git's object database alternates handling as part of the broader ODB abstraction effort. The patch transitions alternates reading to use the new source-based interface introduced earlier in the series.

The key change replaces direct filesystem access with a `odb_source`-based approach for reading alternates. The patch renames `read_info_alternates()` to `odb_source_read_alternates()` and modifies it to operate on an `odb_source` object rather than taking a raw path. This maintains the same functionality while better fitting the series' architectural goal of abstracting alternates handling.

The changes are straightforward mechanical conversions of the alternates reading code to use the new interface. Three call sites are updated to use the renamed function, with the core logic remaining similar but now operating through the source abstraction. The patch continues the series' pattern of incremental, focused changes that build toward the final goal of pluggable ODB backends.

This appears to be a well-executed step in the series, with clean diffs and clear commit message. The change follows naturally from the preparatory work in earlier patches and doesn't introduce new complexity. Reviewers will likely focus on verifying the source-based approach works correctly for all alternates cases, though the series has already seen multiple iterations addressing earlier feedback.