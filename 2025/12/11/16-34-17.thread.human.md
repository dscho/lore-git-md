This thread began with Kristoffer Haugsbakk reporting a segfault in `git replay` when using a non-existent `--onto` target, which René Scharfe promptly fixed by moving a NULL check before pointer dereferencing. The fix, reviewed and approved by Phillip Wood, addressed the immediate crash while noting a preexisting issue where invalid and missing arguments were treated identically.

Kristoffer has now followed up with a proposal to improve error handling further. His work-in-progress patch would make `git replay` fail explicitly with descriptive messages when given invalid commit arguments, rather than silently returning NULL. This would help distinguish between missing arguments (which currently show a "root commit not supported" message) and invalid ones. The change focuses on `peel_committish()` in `builtin/replay.c` but still needs testing and refinement, particularly around memory management during error conditions.

While this builds on Phillip's earlier observation about argument handling, it doesn't yet address the remaining limitation around root commit support (marked as FIXME in the code). The discussion shows steady progress from fixing a crash to improving user feedback, with clear separation between the immediate fix and longer-term enhancements.