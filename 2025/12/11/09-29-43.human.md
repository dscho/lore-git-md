Bugfix follow-up in the ODB alternates refactoring series.

Patrick Steinhardt responds to SZEDER Gábor's report of a build failure in patch 4/8 of the object database alternates refactoring series. The issue stems from calling `odb_add_source()` before it's actually implemented in the series. 

The original patch aimed to replace `link_alt_odb_entries()` with `odb_add_source()` in `odb_add_to_alternates_file()` to better match the single-directory use case, but this created a forward reference since the new function isn't introduced until later in the series. SZEDER Gábor also notes that some commit messages reference this non-existent function from earlier versions.

Patrick acknowledges both issues and indicates he'll fix them, likely by either reordering patches or adjusting the implementation sequence. This is a straightforward technical correction needed before the otherwise well-reviewed series can proceed. The exchange shows good collaborative review catching an important build issue before merging.