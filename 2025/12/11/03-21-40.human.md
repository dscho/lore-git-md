This is a discussion about fixing data loss in `git pull --rebase` when repositories have multiple push URLs. The thread has revealed that the core issue stems from push operations creating misleading reflog entries when pushing to different servers than those being pulled from, causing `--fork-point` to incorrectly calculate which commits to preserve during rebase.

Junio Hamano responds to Phillip Wood's observation that fork-point calculation already happens before fetching (contrary to earlier assumptions) and that the problematic behavior occurs when push and pull URLs diverge. Wood suggests potentially disabling fork-point entirely when `pushurl` is configured, which Hamano acknowledges as tempting but also wonders if better user education might suffice.

The discussion shows the problem is more nuanced than initially thought, with solutions needing to account for the pre-fetch calculation behavior and the divergence between push/pull destinations. While a workaround exists (`git fetch && git rebase --no-fork-point`), the thread is exploring whether to implement Wood's configuration-based solution or pursue alternative approaches like better diagnostics. The technical complexity has grown as participants uncover more edge cases in URL handling and remote-tracking behavior.