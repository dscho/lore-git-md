This is a bugfix for the `git replay` command, addressing a segfault when using a non-existent `--onto` target. The issue was introduced in commit 15cd4ef1 ("replay: make atomic ref updates the default behavior") from November 2025.

The patch moves a NULL check for the `onto` pointer before its first use, preventing a segmentation fault that would occur when users specify a non-existent commit with `--onto`. The fix is straightforward - just reordering two code blocks - but importantly prevents a crash in core functionality of the relatively new `git replay` command.

The patch maintains the existing behavior of rejecting attempts to replay down to a root commit (with the same "not supported yet" message), just doing so before rather than after the pointer is dereferenced. The change is minimal (6 lines modified) and comes with a clear explanation linking it to the reported regression.

This appears to be a well-scoped, necessary fix for a clear crash bug, and the patch follows Git project conventions with proper sign-off and attribution to the original reporter. The change should be uncontroversial and likely to be merged quickly.