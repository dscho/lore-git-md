This is a bugfix in the ODB alternates refactoring series, addressing a compilation error in patch 4/8. SZEDER Gábor points out that the patch introduces a call to `odb_add_source()` which doesn't exist in the codebase yet, causing build failures. The function appears to have been referenced in multiple commit messages from previous rounds but was never actually implemented.

The issue stems from Patrick Steinhardt's attempt to replace `link_alt_odb_entries()` with a more direct `odb_add_source()` call in `odb_add_to_alternates_file()`. While the conceptual change makes sense (avoiding the surprising multi-entry handling of `link_alt_odb_entries` when only single entries are ever passed), the implementation is incomplete without the promised `odb_add_source()` function.

This appears to be an oversight in the series' incremental development, where documentation and commit messages referenced a function that hadn't been introduced yet. The error suggests the series may need to either implement `odb_add_source()` earlier or adjust the patch ordering to maintain buildability. The thread context shows this is part of a larger, nearly-complete ODB abstraction effort that has otherwise received positive review.