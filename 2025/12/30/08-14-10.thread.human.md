Here's the human-readable summary of the thread's current state:

The thread began by addressing an interaction issue between Git's filesystem monitoring (fsmonitor) and the split-index feature when index.skipHash is enabled. The core fsmonitor implementation itself is stable, but the discussion revealed a deeper design question about how Git should handle feature combinations that may be fundamentally incompatible.

Paul Tarjan initially proposed a technical fix that would temporarily enable hashing during shared index writes while preserving skipHash functionality elsewhere. While this worked, Johannes Schindelin raised concerns about the long-term implications, arguing that split-index and skipHash might be inherently incompatible since one needs object IDs while the other tries to avoid generating them.

The discussion has now reached a pragmatic resolution. Paul has implemented a simpler solution that avoids the problematic combination in the test cases where it surfaces, rather than modifying core index behavior. By unsetting GIT_TEST_SPLIT_INDEX in specific scalar clone tests (matching an existing workaround), all tests now pass while maintaining current behavior. This approach sidesteps the deeper architectural questions about feature interaction design, focusing instead on keeping the tests green.

The thread shows how technical discussions in Git often evolve from specific bug fixes to broader design philosophy debates, then sometimes circle back to practical solutions that defer the larger questions. The fsmonitor core remains unchanged, and the immediate issue is resolved, though the underlying tension between feature combination and design purity remains an open topic.