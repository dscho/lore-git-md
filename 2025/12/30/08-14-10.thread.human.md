This thread has evolved into a deep technical discussion about Git's filesystem monitoring (fsmonitor) implementation and its interaction with split-index functionality. The fsmonitor work, now in its 12th iteration, has achieved cross-platform parity and production stability, with Paul Tarjan leading the implementation. The latest discussion centers on an edge case where index.skipHash interacts poorly with split-index, potentially causing index corruption.

Johannes Schindelin (Dscho) has identified that the issue isn't actually with fsmonitor itself, but rather stems from how Scalar sets index.skipHash=true, which can leave the shared index OID null. This creates a situation where later index reads skip loading the shared index while the fsmonitor_dirty bitmap still references entries from that unloaded index. Schindelin argues that Paul's current approach of silently skipping bitmap entries could mask real filesystem changes, and instead suggests three alternative solutions that would address the root cause rather than the symptom.

The discussion has now shifted from being purely about fsmonitor to encompassing broader index handling concerns. While the fsmonitor implementation itself remains technically sound and production-validated, this newly uncovered interaction suggests the series might need to drop or modify one patch to properly handle this edge case. The core team is now weighing whether to:
1) modify write_shared_index() to generate real OIDs even with skipHash,
2) fix the index reload behavior in hn/git-checkout-m-with-stash, or
3) maintain the current test workaround of disabling split-index.

This represents one of the final technical hurdles before this long-running fsmonitor work can be merged, with the main implementation already having addressed all other substantive feedback from reviewers including Patrick Steinhardt. The discussion remains productive, with participants focused on finding the most robust solution rather than just the quickest fix.