Here's the updated human-readable thread summary:

The thread focuses on Linux filesystem monitoring (fsmonitor) implementation and its interaction with split-index and index.skipHash features. The core fsmonitor functionality - including process isolation, Windows compatibility, and Linux inotify support - remains production-validated and technically sound.

Paul Tarjan identified and fixed a root cause issue where index.skipHash=true caused shared index OID generation to fail while still maintaining fsmonitor_dirty bitmap references. His solution temporarily forces hashing during shared index writes while preserving the original skipHash setting, which testing confirmed resolves all test failures.

Johannes Schindelin has now raised deeper concern about whether this is the right long-term approach, arguing that split-index (which requires OIDs) and skipHash (which avoids them) may be fundamentally incompatible features. He suggests alternatives like faking OIDs or simply avoiding the combination in tests, preferring clear feature boundaries over special-case fixes.

The discussion has evolved from implementation details to design philosophy - whether to work around the incompatibility or acknowledge it explicitly. While the immediate fsmonitor functionality is unaffected, this surfaces important questions about how Git should handle feature interactions in its index handling layer. Both Paul and Johannes agree the technical behavior but differ on the preferred resolution path.