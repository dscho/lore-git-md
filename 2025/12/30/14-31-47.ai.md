This is a follow-up in the `git replay` error handling thread, specifically addressing dead code removal in preparation for v2 of the patch series. Kristoffer Haugsbakk acknowledges Elijah Newren's explanation about historical context for an unreachable `else` clause in `determine_replay_mode()` in `builtin/replay.c`. The dead code originated from when `--onto` and `--advance` weren't mandatory options in Newren's private branch, before Christian Couder prepared the patches for upstream inclusion.

The email confirms this dead code has been removed in v2 of the patch series, which aligns with the thread's consensus on architectural changes. The removal is part of the broader effort to improve error handling by moving validation to call sites in `determine_replay_mode()`, as suggested by Junio Hamano and Phillip Wood. This cleanup helps streamline the codebase while maintaining the stricter validation requirements for the `--advance` option that performs separate `repo_dwim_ref()` checks.

The exchange shows the patch series progressing toward v2 with clearer error handling architecture and removal of historical artifacts, building on earlier discussions about test coverage and validation flow restructuring.