This is a review comment on patch 4/7 of Patrick Steinhardt's object info refactoring series. Kristoffer Haugsbakk points out a typo in the commit message ("inconsistecny" should be "inconsistency"). The patch itself is part of the series' effort to standardize how packfile metadata is handled, specifically ensuring pack-specific fields are always populated when reading object info from packs.

The change affects `packfile.c`'s `packed_object_info()` function, making its behavior consistent with `packfile_store_read_object_info()`. This is one of several interface cleanups in the series that eliminate inconsistencies in how object metadata is reported from packfile operations. The typo fix is trivial but shows careful review attention to detail. The underlying patch appears to be a straightforward improvement that helps achieve the series' goal of more reliable pack metadata handling.