This is a technical explanation from Junio C Hamano about Git's revision walking internals, specifically addressing how `--reverse` works in `git log`.

The response clarifies that `--reverse` operates in two phases: first collecting commits in forward order (newest-to-oldest) using the standard traversal logic, then outputting them in reverse from an internal queue. This confirms Jon Forrest's suspicion about commit lists being involved, though the key insight is that the reversal happens as a post-processing step after normal traversal completes.

The explanation is characteristically terse but authoritative, coming from Git's maintainer. It provides enough technical detail to understand the basic mechanism while leaving room for follow-up questions about implementation specifics. The response suggests the code should be "fairly obvious" once you know this two-phase approach, though Jon had noted the lack of comments made this non-obvious during initial code reading.

This moves the thread forward by answering the core question while potentially opening new avenues about the queueing mechanism or performance implications of this approach. The explanation is likely to satisfy the immediate query while giving other contributors enough context to chime in with more detailed explanations if desired.