This is a test patch in the ongoing series improving error handling in `git replay`. The email addresses a technical issue in one of the test cases pointed out by Phillip Wood, where piping through `head` after `test_must_fail` would lose the command's exit status. Kristoffer Haugsbakk acknowledges the issue and plans to modify the test to check the full output rather than just the first line.

The test in question verifies that `git replay` requires either `--onto` or `--advance` options, checking the error message when neither is provided. The fix will make the test more robust by properly capturing the failure status while still validating the error message content. This is a minor but important correction to the test coverage being added for `git replay`'s error conditions.

The thread context shows this is part of a broader effort to improve error reporting in `git replay`, with consensus already reached on moving error handling to callers and maintaining `--advance`'s stricter validation. This particular patch focuses on ensuring the test suite properly validates these error conditions. The fix demonstrates responsiveness to code review while maintaining the test's original purpose of verifying mandatory option enforcement.