This is the third patch in a 5-part bugfix and test coverage series for `git replay` error handling, focusing on invalid commit-ish inputs. The patch improves error messaging when users provide invalid commit-ish arguments to `--onto` or accidentally pass malformed input due to option parsing ambiguity.

The key change modifies `peel_committish()` to immediately die with a descriptive error when given an invalid commit-ish, rather than returning NULL and triggering a confusing "root commit not supported" message later. The function now takes a `mode` parameter ("--onto" or "--advance") to provide context in error messages. This addresses cases where users might unintentionally pass invalid input due to the option parser accepting both `--onto=<name>` and `--onto name` forms.

The patch removes now-unreachable NULL checks for `onto` and updates the test suite with a new case verifying the improved error message for invalid `--onto` inputs. It also moves the root commit check directly next to the existing check for merge commits, maintaining the FIXME comment about future support.

Technical details show careful handling of the series evolution: the commit message explains why an earlier `--advance` test was removed (made redundant by previous patches), and notes design decisions around error message placement (keeping validation in `peel_committish()` with context rather than moving to callers). The changes align with the v2 series' architectural goals of clearer error handling and dead code removal while expanding test coverage.

This patch represents a focused improvement to user-facing error reporting that builds on the foundational changes in earlier patches while setting up for subsequent test expansion. The implementation appears clean and well-justified, with clear connections to both prior feedback and the series' overall direction.