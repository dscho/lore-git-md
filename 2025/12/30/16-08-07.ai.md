Test improvement patch for the `git status` divergence display feature, part 2/3 in v10 series. This follows the major design pivot from using `status.goalBranch` configuration to leveraging existing branch tracking mechanisms.

The patch significantly refactors the test suite (249 lines changed) to:
- Simplify test output capture by removing redundant redirections
- Standardize on using `${SQ}` for quoted strings in test expectations
- Consolidate test setup and assertions
- Maintain the same comprehensive coverage of 12 scenarios from previous versions

Key test scenarios preserved include:
- Various branch states (ahead, behind, diverged)
- Remote configuration cases (origin vs upstream)
- Edge cases (unset/malformed branches)
- Output format verification
- Integration with tracking info

Notable changes from prior versions:
- Removed `--long -b` flags from test invocations where not needed
- Simplified test setup by removing unnecessary `>/dev/null` redirects
- Kept all test cases despite configuration mechanism change
- Maintained same verification rigor through `test_cmp` and `test_grep`

The tests continue to validate the core functionality showing divergence from target branch in status output, now using `branch.<name>.pushRemote` when available and falling back to tracking branch if unset. The test improvements appear focused on maintainability while preserving the ability to catch regressions in the feature's behavior.