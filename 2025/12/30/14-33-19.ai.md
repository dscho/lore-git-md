This is a follow-up in the ongoing "git replay" test improvements thread (kh/replay-invalid-onto-advance). The patch series aims to improve error handling and test coverage for the `git replay` command.

The current email responds to Junio Hamano's "What's cooking" report, clarifying that the main change is improving the error message when an invalid commit-ish is provided to `--onto`. The author acknowledges that test coverage for simple error conditions was previously lacking, and mentions they were about to make a similar mistake with `--advance` error messages before catching it. New tests for argument validation will help prevent such regressions.

The email indicates a second version of the patch series is forthcoming, suggesting the first version needed rework (as noted in Junio's original report). The discussion references a specific previous message where Junio pointed out an issue with the error messaging approach.

Key technical details for future reference:
- Focuses on `git replay` argument validation (`--onto` and `--advance`)
- Adds regression tests in t3650
- Improves error messages for invalid commit-ish inputs
- Previous version needed rework on the "die descriptively" patch
- Thread includes discussion about maintaining consistent message quality across similar error cases