This thread focuses on improving error handling in `git replay`, particularly around the `--onto` and `--advance` options. Elijah Newren initiated the discussion by addressing confusing error messages when invalid commit references are provided. His patches improved the error reporting from `peel_committish()` and added test coverage for various failure cases.

Junio Hamano and Phillip Wood have been key participants in the technical discussion. Junio suggested moving error handling to the callers of `peel_committish()` to provide more specific error messages tied to each option (`--onto` or `--advance`). Phillip noted that since there are only two callers, this approach is feasible. He also identified a test case that needed adjustment to properly capture failure status.

In the latest email, Junio builds on these observations, proposing to restructure the validation flow for `--advance` to perform reference checking before commit resolution. This would create more parallel error handling paths between the two options while maintaining `--advance`'s stricter requirements. The discussion remains focused on refining the implementation details rather than adding new functionality.

The thread has progressed from initial error message improvements to considering architectural changes in error handling and validating test coverage. Key open questions include where to place error handling (in `peel_committish()` or its callers) and how to best structure the validation sequence for `--advance`. The test coverage now includes cases for mandatory options, root commits, and option conflicts, with one test case needing correction.