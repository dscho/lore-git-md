This thread focuses on improving error handling in `git replay` when invalid commit references are provided. The discussion started with Elijah Newren addressing confusing error messages for the `--onto` and `--advance` options, proposing to make failures more descriptive. 

The conversation has evolved into considering where error handling should occur - currently centralized in `peel_committish()` but potentially better placed at each of its two call sites (`--onto` and `--advance` paths in `determine_replay_mode()`). Junio Hamano and Phillip Wood noted that `--advance` already performs its own reference validation through `repo_dwim_ref()`, making some error checking redundant. Kristoffer Haugsbakk, who contributed test cases, now confirms they're incorporating these suggestions in v2 of their patch, moving error handling to callers while preserving `--advance`'s stricter requirements.

The thread has uncovered some historical context - dead code remaining from when `git replay` could auto-detect its mode, which Elijah confirms could now be removed. Test coverage has been expanded to include invalid references, root commits, and option conflicts, with one test case needing adjustment to properly capture failure status. The discussion remains focused on refining the validation architecture while maintaining backward compatibility.