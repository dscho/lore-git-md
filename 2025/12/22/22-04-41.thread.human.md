This thread focuses on improving error handling in `git replay`, particularly around invalid commit specifications. Elijah Newren's initial patches addressed confusing error messages when invalid commit-ish arguments are given to `--onto` and `--advance` options, replacing misleading "root commit" messages with more descriptive errors. The series also added comprehensive test coverage in `t/t3650-replay-basics.sh`.

Junio Hamano reviewed the changes, suggesting the error handling might be better placed in the calling functions rather than the shared `peel_committish()` helper, since there are only two callers and `--advance` already does its own reference validation. Phillip Wood later confirmed this architectural approach could simplify the code.

Most recently, Phillip reviewed additional test cases from Kristoffer Haugsbakk, noting a technical issue with one test that could mask failures due to piping through `head`, while confirming the other tests (covering root commit errors and conflicting options) are sound. The discussion has now shifted from core error message improvements to validating the test coverage, with the architectural question about error reporting placement still open for implementation.