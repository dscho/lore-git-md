This is a clarification response regarding a reported bug in `git restore` error messaging. Brian M. Carlson explains that what initially appeared to be a typo in the error message ("could not resolve ource") is actually correct behavior when the command is invoked incorrectly with `-source` (single dash) instead of `--source` (double dash). 

The response notes that with single-dash syntax, Git interprets `-source` as `-s ource`, passing "ource" as the revision to resolve, which naturally fails. The error message correctly reflects this by showing the actual invalid revision it tried to resolve. The code path in `builtin/checkout.c` is working as designed, printing the unresolvable revision name after the fixed string "could not resolve".

This effectively closes the bug report by explaining it's not a Git bug but rather a command-line syntax misunderstanding. The response provides technical details about how Git processes single-dash arguments and points to the relevant code location that generates the message. No patch is needed as the behavior is correct, though the explanation may help users understand proper command syntax.