This is a technical response in an ongoing discussion about improving merge conflict metadata during rebase operations. D. Ben Knoble replies to Esteban Küber's questions about identifying rebase state and origins more efficiently.

The email provides three key technical responses: 1) suggests using `@{1}` instead of `ORIG_HEAD` to identify the rebase-onto branch, 2) proposes checking `REBASE_HEAD` vs `MERGE_HEAD` to distinguish between rebase and pull-initiated operations, and 3) notes that `git status` already surfaces some of this rebase state information. The reply is concise and focused on the specific technical questions raised, without expanding the scope of the original proposal.

For future reference in this thread: The discussion centers around accessing rebase state metadata (branches involved, operation origin) without expensive operations like `--points-at`. Key terms include `ORIG_HEAD`, `@{1}`, `REBASE_HEAD`, `MERGE_HEAD`, and the suggestion to examine `git status` output. The reply maintains the thread's focus on tooling support for conflict resolution while suggesting existing Git features that may address some of the original concerns.