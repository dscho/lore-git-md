Here's the human-readable thread summary:

This thread began with a user report about confusing error output from `git restore` when accidentally using `-source` (single dash) instead of the correct `--source` syntax. Initially, the message "could not resolve ource" appeared to contain a typo. Brian M. Carlson clarified that Git was correctly parsing this as `-s ource`, with the error showing the failed revision resolution.

Junio C Hamano then noted the error message formatting didn't follow Git's style guidelines, which recommend quoting unresolved names (like "could not resolve 'ource'") for clarity. Brian has now submitted a one-line patch to fix this, wrapping the revision name in quotes in the error message. While the original behavior was technically correct, this small improvement makes the output clearer when users make this common syntax mistake. The patch maintains the conclusion that this was user error, just with better messaging, and credits the original reporter.