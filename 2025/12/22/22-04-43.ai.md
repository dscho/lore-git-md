This is a test patch adding more regression tests for `git replay` failure conditions, building on Elijah Newren's earlier bugfix series for error handling.

The patch adds three new test cases to `t/t3650-replay-basics.sh` that verify error conditions in `git replay`:
1. Confirms the mandatory nature of either `--onto` or `--advance` options by testing the error message when neither is provided
2. Tests the error when attempting to replay down to a root commit (which is not yet supported)
3. Verifies the conflict between mutually exclusive options `--advance` and `--contained`

The tests follow the same pattern as the existing in the file, using `test_expect_success` with explicit expected error messages. The patch is narrowly focused on test coverage expansion without modifying any implementation code. It complements the earlier error message improvements by ensuring these specific failure modes remain properly caught and reported.

Key technical details for future reference:
- Tests verify exact error message text including formatting (single-quoted option names via `${SQ}`)
- Uses standard test helpers like `test_must_fail` and `test_cmp`
- Covers both option validation (`--onto`/`--advance` requirement) and semantic constraints (no root replay, option conflicts)
- Maintains consistency with the test file's existing style and organization

The patch appears well-formed with clear commit message and appropriate sign-off, though it doesn't reference the earlier series it builds upon. The tests look thorough for the specific cases they target.