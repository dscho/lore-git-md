Here is the updated dense AI thread summary incorporating the new email:

Bugfix and test coverage series for `git replay` error handling when invalid commit-ish is given, now in v2. Original author Elijah Newren addressed confusing error messages for `--onto` and `--advance` options. Kristoffer Haugsbakk leads the series incorporating feedback from Junio Hamano and Phillip Wood. The 5-patch v2 series structure:

1. Dead code removal and function reorganization
2. `--advance` validation restructuring (pre-peeling)
3. Caller-specific error message improvements
4. `parse_object_or_die()` adoption (current patch)
5. Expanded test coverage

Key architectural changes in v2:
- Dead code removal from when options were optional
- Restructured error handling moved to callers
- `--advance` validation now occurs before commit peeling via `repo_dwim_ref()`
- `peel_committish()` now dies immediately on invalid commit-ish with mode-specific errors
- Root commit check moved adjacent to merge commit check
- `parse_object_or_die()` replaces `parse_object()` for robust object handling

Technical improvements:
- Uses `parse_object_or_die()` for immediate failure on invalid objects
- `peel_committish()` takes `mode` parameter for contextual errors
- Fixes test cases to properly capture failure status
- Removes redundant NULL checks after making validation fatal
- Adds regression test for OID-vs-reference case in `t/t3650-replay-basics.sh`

Implementation details:
- `builtin/replay.c` changes to `peel_committish()` call paths
- Parallel validation flows for `--onto` (basic commit-ish) vs `--advance` (reference + commit-ish)
- Comprehensive test coverage in `t/t3650-replay-basics.sh` including:
  - New reference validation test
  - Invalid `--onto` input test
  - Option parsing ambiguity cases
- Documentation polish (series subject line refined per Junio's suggestion)

Review history shows progression from initial error message fixes to consensus on architectural changes. Junio Hamano suggested both moving error handling to callers for specificity and using `*_or_die` patterns for robustness. Phillip Wood identified technical test issues and validation redundancy. Kristoffer Haugsbakk implemented these in v2 while adding historical context about the removed dead code. The current patch (4/5) implements Junio's `*_or_die` suggestion, removing NULL checks while maintaining mode-specific errors through the existing infrastructure. All prior feedback appears incorporated in v2, with test coverage expanded to validate the new error handling paths and object parsing behavior, including the new reference-before-peeling validation order for `--advance`. The series maintains clear architectural direction toward robust, context-aware error handling.