Here is the updated dense AI thread summary incorporating the final patch:

Bugfix and test coverage series for `git replay` error handling when invalid commit-ish is given, now complete in v2. Original author Elijah Newren addressed confusing error messages for `--onto` and `--advance` options. Kristoffer Haugsbakk leads the series incorporating feedback from Junio Hamano and Phillip Wood. The 5-patch v2 series structure:

1. Dead code removal and function reorganization
2. `--advance` validation restructuring (pre-peeling)
3. Caller-specific error message improvements
4. `parse_object_or_die()` adoption
5. Expanded test coverage (final patch)

Key architectural improvements:
- Dead code removal from when options were optional
- Restructured error handling moved to callers
- `--advance` validation now occurs before commit peeling via `repo_dwim_ref()`
- `peel_committish()` dies immediately on invalid commit-ish with mode-specific errors
- Root commit check moved adjacent to merge commit check
- `parse_object_or_die()` replaces `parse_object()` for robust object handling
- Comprehensive test coverage added for all error paths

Final patch (5/5) technical additions:
- New regression tests for:
  - Mandatory `--onto` or `--advance` requirement
  - Root commit replay prohibition
  - `--advance` and `--contained` incompatibility
  - Multiple source ordering ambiguity
  - Merge commit limitation
- Test infrastructure improvements:
  - `test_must_fail` for proper error checking
  - Expected error messages in `expect` files
  - Merge commit test setup
  - Option parsing and semantic validation coverage

Implementation details:
- `builtin/replay.c` changes to `peel_committish()` call paths
- Parallel validation flows for `--onto` (basic commit-ish) vs `--advance` (reference + commit-ish)
- Test coverage in `t/t3650-replay-basics.sh` now includes:
  - Reference validation
  - Invalid `--onto` input
  - Option parsing ambiguity
  - New edge cases from final patch
- Documentation polish (series subject line refined per Junio's suggestion)

Review history shows progression from initial error message fixes to consensus on architectural changes. Junio Hamano suggested moving error handling to callers and using `*_or_die` patterns. Phillip Wood identified technical test issues and validation redundancy. Kristoffer Haugsbakk implemented these in v2 while adding historical context about removed dead code. The final patch completes the test coverage goal, validating all new error paths and adding edge cases while maintaining the series' architectural direction toward robust, context-aware error handling. All feedback appears incorporated with tests designed for future maintainability.