Here is the updated dense AI thread summary:

Bugfix and test coverage series for `git replay` error handling. Author Elijah Newren initially addressed confusing error messaging when invalid commit-ish arguments are provided to `--onto` or `--advance` options. The first patch modified `peel_committish()` in `builtin/replay.c` to fail immediately with descriptive errors on commit resolution failure, replacing misleading "root commit" messages. Handled both stuck (`--onto=<name>`) and unstuck (`--onto name`) option forms. The second patch added regression tests in `t/t3650-replay-basics.sh` covering invalid reference cases.

A new test patch expands coverage with three additional test cases in the same file: 1) verifies mandatory nature of `--onto` or `--advance` options by testing error when neither is provided, 2) confirms unsupported root commit case, and 3) checks conflict between mutually exclusive `--advance` and `--contained` options. Tests use standard helpers (`test_must_fail`, `test_cmp`) and verify exact error message text including formatting (single-quoted option names via `${SQ}`). Maintains consistency with existing test file style while covering additional validation cases.

Technical scope remains focused on error reporting without behavioral changes. The series noted potentially dead code in `determine_replay_mode()` due to mandatory nature of `--onto`/`--advance` but left it unchanged. New tests complement earlier error message improvements by ensuring specific failure modes remain properly caught. Both patches demonstrate improved user experience through concrete examples while maintaining narrow focus on commit resolution and option validation error cases.