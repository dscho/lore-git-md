Here is the updated dense AI thread summary:

Bugfix and test coverage series for `git replay` error handling when invalid commit-ish is given. Author Elijah Newren initially addressed confusing error messaging for `--onto` and `--advance` options. The first patch modified `peel_committish()` in `builtin/replay.c` to fail immediately with descriptive errors on commit resolution failure, replacing misleading "root commit" messages. Handled both stuck (`--onto=<name>`) and unstuck (`--onto name`) option forms. The second patch added regression tests in `t/t3650-replay-basics.sh` covering invalid reference cases. A third test patch expanded coverage with three test cases.

Junio C Hamano reviewed the changes, agreeing the new error message improves the repo_get_oid() failure case but noting the function can still return NULL when parse_object() fails. He suggested moving error handling to callers where more specific messages can reference the problematic option (`--onto`/`--advance`). Phillip Wood observed there are only two callers to `peel_committish()`, making caller-specific error handling feasible, and noted `--advance` already has its own reference validation ("must be a reference"), implying potential redundancy.

Phillip Wood provided feedback on Kristoffer Haugsbakk's test cases, identifying a technical issue where piping through `head` after `test_must_fail` would lose the command's exit status in one test case. The other test cases (root commit errors and conflicting `--advance`/`--contained` options) were confirmed as technically sound.

Junio Hamano now builds on Phillip's observations, focusing on the two call paths in `determine_replay_mode()` where `peel_committish()` is called. He suggests moving error handling to these call sites for more specific error reporting. Junio notes the existing asymmetry where `--advance` performs additional reference validation through `repo_dwim_ref()`, enforcing a stricter "must be a reference" requirement. He proposes restructuring `--advance` handling to perform reference validation first via `repo_dwim_ref()` before calling `peel_committish()`, creating parallel validation paths while maintaining `--advance`'s stricter requirements.

Elijah Newren provides historical context about dead code in `determine_replay_mode()`, explaining the unreachable `else` clause is a remnant from when `--onto` and `--advance` weren't mandatory options. The code originated in Newren's private branch where `git replay` could auto-detect mode, but this feature was removed when Christian Couder prepared the patches for upstream. Newren confirms this is dead code that could be removed, noting future auto-detection would be better implemented fresh.

Kristoffer Haugsbakk acknowledges Phillip Wood and Junio Hamano's feedback about moving error handling to callers, confirming this approach is being implemented in v2 of the patch. They recognize the redundancy in `--advance` validation and agree caller-specific messages would be better. The architectural direction is now clear: error handling will move to `determine_replay_mode()` call sites while maintaining `--advance`'s separate `repo_dwim_ref()` validation.

Technical scope remains focused on error reporting architecture and test coverage:
- `peel_committish()` in `builtin/replay.c` has two call paths (`--onto` and `--advance`)
- Error handling will move to callers in v2 per Junio's suggestion
- `--advance` maintains separate `repo_dwim_ref()` validation
- Test coverage includes mandatory options, root commits, and option conflicts
- One test case needs adjustment to properly capture failure status
- Historical dead code identified for potential removal
- v2 patch will implement caller-specific error messages

The thread shows progression from initial error message improvements to consensus on architectural changes (validation flow restructuring) and test coverage validation. Recent discussion confirms the implementation direction for v2, addressing both high-level design and specific validation requirements for the `--advance` option.