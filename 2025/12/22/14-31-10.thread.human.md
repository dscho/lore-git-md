Here's a human-readable summary of the thread so far:

A Rust compiler developer has proposed improvements to how Git exposes merge conflict metadata, particularly during rebase operations. Drawing from their experience implementing enhanced conflict marker diagnostics in rustc, they highlight current limitations in accessing Git's internal state when conflicts occur.

The proposal focuses on making it easier for tools to provide better user guidance during conflicts by exposing more contextual information. Currently, their rustc implementation works around limitations by parsing Git's internal files and using potentially expensive commands like `git branch --points-at`. They identify several pain points: performance issues in large repositories, missing metadata about rebase-onto branches, and difficulty distinguishing between rebase operations initiated by different commands (like `git pull`).

Key questions raised include whether Git could provide more direct access to rebase metadata and whether conflict markers could be enhanced to include more context by default. The discussion stems from observed user confusion during various conflict scenarios (rebase, pull, merge, stash) and seeks to balance richer information with maintaining Git's traditional output format.