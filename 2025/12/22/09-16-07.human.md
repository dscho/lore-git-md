Bugfix for a typo in `git restore` error message. 

Zhelyo Zhelev reports a simple but noticeable typo in the error message from `git restore` when given an invalid `-source` argument. Instead of printing "could not resolve source", it currently prints "could not resolve ource" (missing the initial 's'). 

The bug is easily reproducible on both Windows and Linux with Git 2.52.0. The reporter provides clear reproduction steps: running `git restore -source my_base_branch` with an invalid branch name triggers the malformed error message. 

This appears to be a straightforward string literal fix in the source code. While minor, such typos in error messages can be confusing for users and make the software appear less polished. The fix should be simple to implement since it only requires correcting the string constant. 

No additional context or discussion is needed for this fix - it's a clear-cut case of a typo that should be corrected in the next release. The bug report is well-formatted and includes all necessary details including version information and reproduction steps.