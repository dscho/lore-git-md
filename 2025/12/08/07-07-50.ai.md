This is a code refactoring discussion in the context of the ongoing `git replay --revert` feature development. Patrick Steinhardt responds to Siddharth Asthana's explanation about why `refer_to_commit()` wasn't included in the shared revert message formatting function (`sequencer_format_revert_header()`). 

The key technical points revolve around the architectural boundary between interactive (sequencer) and non-interactive (replay) contexts. Siddharth explains that `refer_to_commit()` depends on sequencer-specific `replay_opts` for its formatting choices, while replay.c uses full OIDs exclusively. Patrick suggests an alternative approach using a flags parameter to control the formatting behavior, which could potentially unify the implementations without exposing sequencer internals.

This discussion represents a refinement of the v2 implementation's architecture, specifically around how commit references should be formatted across different contexts. The exchange maintains the established design principle of keeping clean boundaries between interactive and non-interactive components while exploring potential simplifications. The proposed flags-based approach would need to be evaluated against the current separation of concerns in the codebase.