This is a follow-up discussion about the `git-last-modified` bugfix, now focusing on the proposed `MEMZERO_ARRAY` macro solution. Toon Claes responds to Jeff King's earlier suggestion about potential architectural improvements by submitting a v2 patch that introduces a new `MEMZERO_ARRAY` macro in `git-compat-util.h`. The macro would handle array zeroing with proper size calculations using `st_mult()`, following the pattern of existing array helpers like `ALLOC_ARRAY` and `CALLOC_ARRAY`.

The discussion has evolved from the initial bugfix (incorrect `memset` size calculation) to considering more maintainable solutions. King had previously suggested two approaches: a specialized `CLEAR_ARRAY` macro and a `bitmap_clear()` function, while acknowledging both might be over-engineering. Claes now proposes a middle ground with `MEMZERO_ARRAY` that specifically handles zero-initialization cases, which covers the original use case while being more general than a one-off fix.

Key technical points for future reference:
- The macro would be used in `process_parent()` to clear `lm->scratch->words`
- It follows Git's existing array helper patterns
-ARRAY` convention
- Uses `st_mult()` for safe size multiplication
- Specifically designed for zero-initialization cases (unlike a more general `MEMSET_ARRAY`)
- Addresses the type-safety concerns raised earlier in the thread

The thread shows consensus on the technical solution (proper size calculation) but explores different implementation approaches. The maintainer has already approved backporting the fix, and this email seeks feedback on whether the macro-based solution is preferable to the direct fix. The discussion remains focused on code quality and maintainability rather than the core bugfix, which is already settled.