Refactoring patch in Patrick Steinhardt's ODB alternates handling series (7/8). This continues the effort to abstract alternates handling through object database sources rather than direct filesystem operations.

The patch renames `read_info_alternates()` to `odb_source_read_alternates()` and modifies it to operate on an `odb_source` struct rather than taking a raw path string. The key changes are:
- Function now takes `struct odb_source *` instead of `const char *relative_base`
- Path construction uses `source->path` instead of direct parameter
- All call sites updated to pass the source object rather than path strings
- Maintains same parsing behavior through existing `parse_alternates()` helper

This is a straightforward mechanical change that completes the transition to source-based alternates reading that was prepared in earlier patches. The diff shows 6 lines changed across 4 locations, all in odb.c, with no behavior modifications - just moving the alternates reading interface to work through the source abstraction layer.

The change positions the codebase for future work where alternates handling could be implemented differently by pluggable ODB backends, though that capability isn't exposed yet. This appears to be a clean, focused step in the series with no obvious technical concerns.