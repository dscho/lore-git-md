This is a minor bugfix in the MIDX compaction series, addressing a memory leak in test infrastructure.

Patrick Steinhardt provides review feedback on patch 15/17 of Taylor Blau's MIDX compaction series. The patch fixes a memory leak in the test helper `test-read-midx.c` when selecting MIDX layers by checksum. The original code would return directly on error without freeing resources, while the new version properly cleans up via a `goto out` pattern.

Patrick notes the change also modifies the error return code from 1 to -1 (using `error()`) but considers this an unimportant detail. He briefly considers whether the initial MIDX setup failure case should also use `goto out` for consistency, but doesn't insist on the change. The review is positive overall, with Patrick indicating he'll review the remaining two patches in the series separately.

This is a straightforward cleanup patch in test infrastructure, with no major technical implications for the core MIDX compaction functionality. The review confirms the change is correct while noting some minor stylistic considerations that don't affect the patch's correctness.