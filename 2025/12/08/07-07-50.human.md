This is a code refactoring discussion in the ongoing `git replay --revert` feature series. Patrick Steinhardt responds to Siddharth Asthana's explanation about why the `refer_to_commit()` function wasn't included in the shared revert message formatting code.

Patrick suggests a potential simplification - converting the `struct replay_opts` parameter into a simple flags field to control whether to use full OIDs or abbreviated commit info. This would allow sharing more code between the sequencer and replay components while maintaining clean boundaries.

The discussion shows careful consideration of architectural boundaries between interactive (sequencer) and non-interactive (replay) contexts. Siddharth had previously explained the rationale for keeping them separate due to different requirements around commit reference formatting. Patrick's suggestion offers a middle ground that could reduce duplication while preserving the conceptual separation.

This is a technical refinement to the already well-advanced `--revert` feature implementation, showing the careful attention to detail being given to the code structure as the series approaches its v3 submission.