This is a follow-up discussion in the documentation patch series for `git replay`, specifically about the wording of the `--contained` option description.

Junio Hamano confirms the series looks sensible overall but notes the `--contained` documentation (copied from the original implementation commit) might be unclear. He provides a concrete example showing potential ambiguity: when a branch spans commits both inside and outside a revision range, it's not obvious whether `--contained` would consider it "contained" based on just the branch tip or all its commits.

The discussion suggests the current phrasing may be kept as-is since it matches the feature author's original wording, but the example highlights a real documentation gap. This appears to be a minor refinement point that won't block the series' acceptance, but could be addressed in future documentation updates. The thread shows maintainer approval for the overall series while leaving room for clarifying the edge case behavior later.