This is the sixth patch in Patrick Steinhardt's 8-part refactoring series for Git's object database alternates handling. The patch follows up on the previous change that eliminated mutual recursion between `link_alt_odb_entries()` and `odb_add_source()`.

The key change here is removing the forward declaration of `read_info_alternates()` which is no longer needed after the recursion was eliminated in patch 5. The patch reorganizes the code by moving `read_info_alternates()` and its dependent functions (`odb_source_new()` and `odb_add_source()`) up in the file to eliminate the forward declaration. 

The commit notes that it removes outdated function documentation for `read_info_alternates()` that no longer accurately described the function's behavior after the refactoring. The diff shows a significant code reorganization (125 lines moved) but no functional changes - this is purely a cleanup patch building on the structural improvements made earlier in the series.

This continues the series' pattern of improving code organization and encapsulation in preparation for future pluggable ODB backends, with each patch making incremental improvements to the alternates handling code. The changes are focused and mechanical, following logically from the previous refactoring steps.