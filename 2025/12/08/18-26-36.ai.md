This is a review comment on patch 08/17 in Taylor Blau's MIDX compaction series, focusing on the introduction of `struct write_midx_opts`. Patrick Steinhardt raises two technical points about the implementation.

First, he suggests that mandatory parameters like `struct odb_source` might be better kept as direct function arguments rather than struct members, since they aren't truly optional. This would make the interface more self-documenting. Second, he notes that the patch uses C99 compound literal syntax (`(struct foo){...}`) in several places, which is currently only allowed as a test balloon in Git's codebase per the CodingGuidelines, with wider adoption planned for mid-2026.

The review is generally positive, calling the change "a nice improvement" aside from these two points. The discussion touches on code style and forward compatibility rather than the core MIDX functionality, suggesting this is a relatively minor refinement to the larger series. The thread context shows this is part of Taylor Blau's comprehensive MIDX compaction work, with Patrick having previously reviewed other patches in the series.