This is a performance optimization discussion about promisor pack handling, continuing the thread about skipping hash verification for objects. Jeff King (Peff) responds to Aaron Plattner's concerns about the safety of skipping hash checks for non-blob objects in `add_promisor_object()`.

Peff argues that skipping hash verification is acceptable in this context since the primary goal is speed rather than thorough database consistency checking. He distinguishes between two types of object parsing callers: those verifying database integrity (like `fsck`) and those prioritizing performance (like promisor object traversal). He suggests the commit message should simply clarify that this optimization targets the performance-oriented case.

The email also provides guidance on structuring the patch series: first fixing the `OBJ_NONE` handling issue (which Aaron has already agreed to split out), then applying the `SKIP_HASH` optimization. Peff notes that demonstrating the impact of the `OBJ_NONE` fix would be difficult but not crucial, since the second patch's performance improvements will effectively showcase the benefits.

Key technical points include:
- Reaffirmation that `PARSE_OBJECT_SKIP_HASH_CHECK` is safe for this use case
- The distinction between verification-focused and performance-focused object parsing
- The planned two-patch structure for the changes
- Reference to prior discussion about tradeoffs between safety and performance

The discussion continues to focus on the safety implications of skipping hash verification while maintaining consensus on the core optimization approach. Peff's response provides clear rationale for proceeding with the current direction while addressing Aaron's concerns about object integrity checking.