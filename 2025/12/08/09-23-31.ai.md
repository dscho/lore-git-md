This is a follow-up discussion in the submodule gitdir path encoding series, specifically about build-time configuration options that were previously proposed. The email confirms the final decision to reject build-time configuration entirely in favor of pure runtime configuration via `/etc/gitconfig` and other standard Git config mechanisms.

Adrian Ratiu acknowledges Patrick Steinhardt's suggestion about potentially making the runtime config default depend on build configuration (like `WITH_BREAKING_CHANGES`), but indicates this will be reconsidered only after implementing the v6 atomic enablement command. The discussion reinforces Junio Hamano's earlier position that runtime configuration is sufficient, especially since managed environments can use system-wide gitconfig.

Key technical points from this exchange:
- Build-time influence through `WITH_BREAKING_CHANGES` remains definitively rejected
- Runtime configuration via `/etc/gitconfig` is confirmed as the only supported path
- Any remaining consideration of build-time defaults is deferred until after the atomic enablement command is implemented in v6
- The thread shows consensus solidifying around the pure runtime approach

This email effectively closes the discussion about build-time configuration options, with all participants aligned on the runtime-only approach. The focus now shifts to Adrian's implementation of the atomic enablement command in v6 as the primary transition mechanism.