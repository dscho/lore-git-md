This is a small technical discussion about a helper function in Taylor Blau's MIDX compaction series. Patrick Steinhardt questions why the new `u32_add()` function in git-compat-util.h uses `PRIuMAX` for its overflow error message rather than the more specific `PRIu32` that's already used elsewhere in the codebase. The function itself is a safe addition helper for 32-bit unsigned integers that will die() on overflow.

The exchange is part of the larger review of patch 10/17 in the series, which introduces this utility function to support the MIDX compaction work. While the question is narrowly focused on formatting consistency, it reflects Patrick's ongoing attention to detail in reviewing the series. The thread context shows this is one of several small but thoughtful technical points he's raised across the patches.