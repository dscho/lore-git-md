This is a follow-up discussion in the documentation patch series for `git replay`. The thread has already seen the complete series reviewed and approved by maintainer Junio C Hamano, with minor observations about the phrasing of the `--contained` option documentation.

The current email shows Kristoffer Haugsbakk responding to feedback from Toon Claes about the silent conflict behavior documentation. Toon agrees the current behavior is unusual and mentions considering future patches to change it, but supports the current documentation changes as a first step. Kristoffer notes the `--contained` option's behavior appears similar to `git rebase --update-refs` and suggests this comparison could be added to the documentation.

.

The discussion confirms the series is ready to proceed as-is, while leaving open possibilities for future improvements: either behavioral changes to make conflict handling more consistent with other Git commands, or refinements to the `--contained` option documentation. The reference to `git rebase --update-refs` suggests a potential direction for clarifying the `--contained` documentation in future revisions.

.

Key technical points mentioned include the silent conflict resolution behavior being documented (unlike other Git commands), and the `--contained` option's implementation originating from commit c4611130. The comparison to `git rebase --update-refs` is a new suggestion that could inform future documentation updates.

.

The thread shows consensus on the current documentation changes while acknowledging room for future refinement, particularly around making the `--contained` option's behavior more clear to users familiar with similar Git commands. No immediate changes are proposed to the already-approved series.