This is a technical discussion about Git's index extension handling, specifically around the `index.recordOffsetTable` behavior and compatibility warnings.

The email continues a thread about Scalar's configuration cleanup, but focuses on a deeper technical question about Git's index extension philosophy. Patrick Steinhardt and Junio Hamano are discussing whether Git should still suppress warnings about unknown optional index extensions (like the offset table) for compatibility reasons.

Junio clarifies that Git will always complain about mandatory extensions it doesn't understand, and will note (but not fail on) optional ones. He argues these "ignoring" messages serve a useful purpose when users downgrade Git versions, even if such cases are rare. Patrick acknowledges this perspective but wonders if there should be a way to disable these warnings via the advice system.

The discussion reveals an interesting tension in Git's design - between keeping behavior clean/simple for users and providing enough information when unusual situations occur. While this doesn't block the Scalar config series (which remains ready for integration), it provides important context about how Git handles index compatibility that could influence future decisions.