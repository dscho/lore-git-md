This is a technical discussion about fixing the data loss bug in `git pull --rebase` with multiple push URLs. Junio Hamano is responding to Phillip Wood's analysis of the issue, agreeing that the current behavior where `git push` updates remote-tracking branches makes this tricky to fix.

Junio suggests a more comprehensive solution than Phillip's workaround (`git fetch && git rebase --no-fork-point`): performing the fork-point computation *before* fetching rather than after. This would involve calculating the rebase range prior to updating remote-tracking refs, then executing the fetch, then performing the rebase with explicit range parameters. He notes this would be a significant architectural change compared to the current implementation.

The discussion continues to explore solutions for this edge case where partial push success/failure across multiple push URLs creates inconsistent remote-tracking states. Junio's proposal would fundamentally change when and how fork-point calculations occur during the pull-rebase workflow, potentially providing a more robust solution than configuration-based special casing. The complexity of the fix reflects the subtle interaction between push URL handling, remote-tracking branch updates, and rebase's fork-point logic.