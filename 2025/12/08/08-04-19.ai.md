Refactoring patch in Patrick Steinhardt's ODB alternates handling series (2/8). This change moves relative path resolution for alternates from the linking phase into the parsing phase, bringing the code closer to being fully contained within the ODB source abstraction.

The patch relocates path normalization logic from `link_alt_odb_entry()` (renamed to `odb_add_source()`) into `parse_alternates()`. Key changes include:
- Path resolution now happens immediately during parsing rather than during linking
- The renamed `odb_add_source()` becomes simpler, focusing only on adding validated paths
- Relative paths are resolved against `relative_base` during parsing
- Error handling for path resolution remains unchanged (failed resolutions are silently ignored)
- The diff shows 64 lines changed with a net zero line count difference

This continues the series' goal of making alternates handling more self-contained in preparation for pluggable ODB backends. The change maintains existing behavior while improving separation of concerns - parsing now fully processes paths before they reach the linking stage. The patch follows the established pattern of incremental refactoring seen in the first patch of the series.