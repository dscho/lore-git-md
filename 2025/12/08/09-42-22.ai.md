This is a follow-up discussion in the submodule gitdir path encoding series, specifically addressing build-time configuration options which have now been conclusively rejected in favor of a pure runtime approach. The email from Adrian Ratiu responds to Junio Hamano's skepticism about build-time behavior changes, explaining the rationale behind considering them and outlining the planned v6 implementation.

Adrian references Josh Steadmon's earlier feedback about needing to force-enable `extensions.submoduleEncoding` regardless of local config, which initially motivated the build-time option. However, he now agrees with Patrick Steinhardt's suggestion to use a runtime config variable with a build-time default instead. This represents a shift from the v5 approach and aligns with the thread's consensus against build-time configuration influence.

The key technical detail is the planned v6 implementation strategy: using a config variable (likely in `/etc/gitconfig`) with build-time defaults, coupled with an automatic migration command. This maintains the runtime-only principle while still addressing the original use case of managed environments needing to enable the extension universally. The solution avoids the problematic aspects of build-time configuration while preserving the ability to transition repositories automatically.

This email marks the final resolution of the build-time configuration discussion, with Adrian confirming the architectural direction that will be implemented in v6. The thread has now reached clear consensus on all major design points, with only implementation details remaining for the atomic enablement command and related migration handling.