Existing AI thread summary:

Character encoding conversion issue with iconv(3) on macOS affecting t3900 tests. Initial report from René Scharfe identifies incomplete ISO-2022-JP to UTF-8 conversion in `reencode_string_iconv()` when the macOS iconv loses state between buffer growth operations. The problem manifests when the function needs to make multiple iconv(3) calls for stateful encodings. Current analysis suggests this is a macOS-specific implementation limitation rather than a Git bug. The thread is exploring workarounds that maintain correctness while accommodating platform quirk.