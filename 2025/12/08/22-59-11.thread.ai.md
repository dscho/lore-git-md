Updated AI thread summary:

Character encoding conversion issue with iconv(3) on macOS affecting t3900 tests, originally reported by René Scharfe. The problem involves incomplete ISO-2022-JP to UTF-8 conversion in `reencode_string_iconv()` where macOS's iconv implementation loses state between buffer growth operations for stateful encodings. Yee Cheng Chin confirmed this is a regression in macOS 15 (not present in 14.8.2), despite Apple's broader iconv implementation switch from GNU to custom in macOS 14. Collin Funk provided historical context about Apple's custom iconv being based on a buggy FreeBSD version.

Three solution approaches remain under discussion:
1. René Scharfe's original code-based workaround using `ICONV_BREAKS` compile-time flag with explicit state reset during buffer growth
2. Torsten Bögershausen's alternative code-based approach using `__APPLE__` platform detection but lacking state reset (shown incomplete by test failures)
3. René's build-system approach detecting and preferring Homebrew's libiconv when available, now with additional architecture-awareness considerations

René Scharfe's latest response provides technical refinements to the first approach:
- Defends `ICONV_BREAKS` over `__APPLE__` detection since the issue is macOS 15+ specific
- Details pointer arithmetic handling with `const char*` vs `iconv_ibp` casting
- Explains tradeoffs between storing original `insz` vs recalculating
- Proposes keeping buffer growth and state reset logic combined for simplicity
- Maintains preference for build-system solution when Homebrew libiconv is available

Technical consensus has emerged on:
- Necessity of explicit state reset (`iconv(conv, NULL, NULL, NULL, NULL)`)
- macOS 15+ regression being the specific target (not all Apple platforms)
- Homebrew libiconv path selection working correctly across architectures (ARM64 native and x86-64 under Rosetta 2)

Open implementation questions:
- Final platform detection method (`ICONV_BREAKS` vs `__APPLE__`)
- Whether to combine approaches (Homebrew preference + fallback workaround)
- Solution for non-Homebrew macOS 15+ systems
- Verification across all stateful encodings

The discussion has progressed to fine-grained implementation details while maintaining all three solution paths. Key technical references remain:
- Yee Cheng Chin's macOS version regression confirmation
- Torsten's testing showing state reset necessity
- René's architecture-aware Homebrew path handling
- Collin Funk's historical context about FreeBSD iconv origins

The thread preserves complete technical context while focusing current discussion on pointer arithmetic details and platform detection methodology in the code-based workaround. All solution approaches remain viable with tradeoffs between complexity and coverage.