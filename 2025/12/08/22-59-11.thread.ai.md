Updated AI thread summary:

Character encoding conversion issue with iconv(3) on macOS affecting t3900 tests, originally reported by René Scharfe. The problem involves incomplete ISO-2022-JP to UTF-8 conversion in `reencode_string_iconv()` where macOS's iconv implementation loses state between buffer growth operations for stateful encodings. Yee Cheng Chin confirmed this is specifically a regression in macOS 15 (not present in 14.8.2), despite Apple's broader iconv implementation switch from GNU to custom in macOS 14. Collin Funk provided historical context, noting Apple's custom iconv is based on a buggy FreeBSD version with Apple-specific patches, as previously criticized by GNU iconv maintainers. Two validated workarounds remain: 1) René's ICONV_BREAKS compile-time flag forcing full-string reconversion during buffer growth, and 2) runtime use of Homebrew's libiconv via ICONVDIR. The thread maintains consensus that this is a platform regression (not a Git bug) in macOS 15's iconv, with technical focus on `reencode_string_iconv()`'s state handling during buffer expansion in convert.c. No unresolved technical questions remain, though the preferred solution path (compile-time workaround vs. recommending Homebrew libiconv) may still need discussion. The new information precisely isolates the regression to macOS 15 while providing broader context about Apple's problematic iconv history, reinforcing that the current issues are part of a longer-standing pattern rather than an entirely new development.