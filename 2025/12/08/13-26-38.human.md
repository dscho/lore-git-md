Bugfix follow-up with new helper macro. Junio Hamano notes that the original fix for the `git-last-modified` bitmap initialization issue is already in 'next', making this v2 patch potentially redundant. Toon Claes's updated version introduces a new `MEMZERO_ARRAY` macro in `git-compat-util.h` that properly calculates byte counts for array zeroing using `st_mult()`, following the pattern of existing array helpers like `ALLOC_ARRAY`.

The patch replaces the direct `memset()` fix in `builtin/last-modified.c` with this new macro, which was one of the architectural improvements suggested earlier in the thread. While the core bug (proper size calculation for bitmap clearing) was already addressed, this version offers a more maintainable solution by encapsulating the logic in a reusable macro. The changes also include minor commit message improvements from earlier feedback.

The discussion appears settled on the technical solution, with this version representing a code quality improvement over the already-accepted fix. Hamano's comment suggests this may be more of a "nice-to-have" follow-up rather than a necessary replacement for the original patch.