This is a documentation patch fixing an AsciiDoc formatting issue in the `git-send-email` man page. The patch removes an errant blank line that was breaking the list continuation syntax in the documentation for the `--use-imap-only` option.

The change is minimal - deleting a single blank line between a list item and its continuation paragraph. The commit message cites AsciiDoc's specification that list continuations must be "immediately adjacent" to the preceding list item. The patch correctly formats the documentation to show the IMAP configuration instructions as a continuation of the `--use-imap-only` option description rather than as a separate paragraph.

The fix is straightforward and correctly motivated by the AsciiDoc syntax rules. The patch targets the `git-send-email.adoc` file and maintains the existing content while fixing the formatting. No behavioral changes or user-facing impacts are introduced - this is purely a documentation syntax correction.