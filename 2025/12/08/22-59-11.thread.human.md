Here's a human-readable summary of the thread's current state:

The thread addresses a macOS-specific character encoding issue affecting several tests, where Apple's custom iconv implementation causes problems with ISO-2022-JP to UTF-8 conversion. After extensive discussion, the solution has coalesced around three approaches: a code workaround, platform-specific detection, and better integration with macOS package managers like Homebrew.

René Scharfe has led the implementation with a v3 patch series that adds comprehensive Homebrew support to the build system. The first patch introduces architecture-aware paths and fine-grained control over Homebrew components through individual USE_HOMEBREW_* flags. The second patch specifically handles libiconv integration to address the test failures.

In the latest development, René is reconsidering whether the fine-grained component flags (like USE_HOMEBREW_GETTEXT) add unnecessary complexity compared to a simpler NO_HOMEBREW toggle. This reflects an ongoing tension between making the build system highly configurable versus keeping it maintainable. The question is particularly relevant since the core issue stems from libiconv handling, covered in patch 2/2), while the gettext-related flags might be overkill.

The thread has seen input from several key contributors, with Junio Hamano guiding the overall direction and others like Torsten Bögershausen and Carlo Arenas Belón weighing in on macOS-specific considerations. The discussion has expanded to include broader questions about how much package manager support should be baked into Git's build system, with Homebrew being the primary focus but MacPorts and Fink also in the picture.

As the solution nears completion, attention is turning to documentation and potential simplifications, with René now questioning whether some of the configurability he initially proposed might be streamlined without losing essential functionality.