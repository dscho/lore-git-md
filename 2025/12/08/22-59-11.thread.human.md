Here's the updated human-readable summary:

This thread addresses a macOS-specific character encoding problem where recent versions (15+) break ISO-2022-JP to UTF-8 conversion in Git's test suite, specifically in t3900 tests. The issue stems from Apple's modified iconv implementation losing state during buffer growth operations. René Scharfe originally identified the regression, which Yee Cheng Chin confirmed appears in macOS 15 but not 14.8.2.

Three solution paths have emerged: René's compile-time workaround with state resets, Torsten Bögershausen's platform-specific approach, and René's newer proposal to optionally use Homebrew's libiconv. The discussion has evolved to focus on build system integration details, particularly around multi-architecture support and package manager flag organization (with NO_HOMEBREW being considered for consistency with existing patterns like NO_DARWIN_PORTS).

Brian M. Carlson's input adds an important strategic perspective - by making the Homebrew solution opt-in, Apple would have direct incentive to fix their implementation since their own Git builds would continue failing tests. This reinforces the practical value of the Homebrew approach while keeping other solutions available as fallbacks. The thread remains technically focused on finalizing implementation details, with consensus that some form of state reset is necessary for macOS 15+ compatibility.