Refactoring patch in Patrick Steinhardt's series modernizing Git's alternates handling.

This is the sixth patch in an 8-part series restructuring how Git handles object database alternates. The change is straightforward: after the previous patch eliminated mutual recursion between functions, this removes a now-unnecessary forward declaration of `read_info_alternates()`. The patch also relocates some function definitions to maintain logical ordering and removes outdated documentation that no longer matched the function's behavior.

The change is purely organizational - no behavior is altered. This continues the series' pattern of cleaning up the alternates handling code in preparation for future work on pluggable object database backends. The diff shows significant line movement (125 lines changed) but the actual modifications are minimal, just rearranging existing code.

This appears to be a well-justified cleanup following the more substantial changes in previous patches. The commit message clearly explains the change and notes the documentation removal as intentional. The patch maintains the series' focus on improving code structure without changing functionality.