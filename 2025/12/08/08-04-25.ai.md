This is the final patch in Patrick Steinhardt's 8-part refactoring series for Git's object database alternates handling, continuing the effort to abstract alternates system for future pluggable ODB backends.

The patch restructures how alternates are written to use the new source-based abstraction, mirroring the earlier changes made for reading alternates. It introduces `odb_source_write_alternate()` which handles the actual file operations, replacing direct filesystem access in `odb_add_to_alternates_file()`. The new function takes a source object and alternate path, performing the lockfile creation, duplicate checking, and file writing operations. The existing public interface remains unchanged but now delegates to this new implementation.

Key technical changes include moving the alternates file path construction into the new function (using `source->path`), converting error handling from `die()` to return codes, and maintaining the same duplicate-checking behavior. The patch maintains backward compatibility while preparing for future backend-specific implementations. The change touches 51 lines in odb.c, completing the series' transition to source-based alternates handling.

This concludes the initial refactoring phase of the series, having systematically replaced direct filesystem access with source-based operations for both reading and writing alternates. The changes remain behavior-preserving while establishing the foundation for future pluggable backend support.