This is patch 13/18 in a major platform compatibility series introducing comprehensive symbolic link support for Windows. The patch implements the POSIX `readlink()` function for Windows by leveraging the NTFS reparse point functionality introduced in earlier patches.

The implementation reads symlink and directory junction targets using the previously added `read_reparse_point()` helper, which handles the Windows-specific reparse point data structures. The function includes careful handling of buffer sizes and UTF-8 conversion, with special attention to the POSIX `readlink()` API's unusual behavior where truncation isn't considered an error. When symlinks are disabled (via `core.symlinks=false`), it fails with ENOSYS to maintain consistency with the rest of the system.

This is a key building block in the larger effort to provide full symlink support on Windows, following the pattern of replacing CRT functions with direct Win32 API calls. The patch appears well-constructed, with clear handling of edge cases and proper integration with the existing MinGW compatibility layer. It builds on several prior patches in the series that laid the groundwork for reparse point handling and symlink operations.