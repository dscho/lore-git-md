This is a refactoring patch in Justin Tobler's series adding object size metrics to `git repo structure`. It splits out the byte-formatting logic from `strbuf_humanise()` into a new `humanise_bytes()` function that returns the numeric value and unit string separately.

The change enables proper column formatting for the upcoming size metrics in `git repo structure` output, where values and units need independent handling for alignment. The patch maintains all existing behavior while adding the new API, including preserving the human-readable formatting and translation support. A minor translation conflict is resolved in the test helper code.

The implementation looks clean and focused, with thorough handling of both regular byte counts and rate-based units (like "MiB/s"). The new `humanise_bytes()` function will be used in the following patches to format the object size metrics while keeping table columns properly aligned. This is a well-executed preparatory change that fits logically into the series' progression.