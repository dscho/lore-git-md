This is a follow-up discussion in the `git replay` empty commit handling thread, specifically addressing a test case detail. Phillip Wood responds to Junio C Hamano's question about using `--only` when creating an empty test commit. 

Phillip explains his habitual use of `--only` with `--allow-empty` to ensure empty commits remain truly empty even if there are staged changes, contrasting this with the default behavior where `--allow-empty` would commit any staged changes. He offers to remove the flag if preferred, while sharing his personal preference for explicit empty commit creation.

The exchange shows the attention to detail in test case construction, though this appears to be a minor stylistic point rather than a substantive issue affecting the patch's functionality. The core behavior changes discussed in the thread remain technically resolved and ready for inclusion.