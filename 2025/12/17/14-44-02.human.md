This is a follow-up discussion on the first patch in the Windows symlink preparation series, focusing on the `mingw_getcwd()` implementation. Patrick Steinhardt notes that the revised version now attempts `GetFileAttributesW()` as a fallback when `CreateFileW()` fails, which he considers an improvement since it allows returning the unresolved working directory path when full resolution isn't possible. The change maintains the core functionality while adding more graceful fallback behavior.

The discussion confirms the patch's approach to handling symlink resolution in Windows' current directory paths, with Patrick's comment suggesting the modification is a net positive. This appears to be a minor refinement discussion rather than raising any substantive concerns about the patch itself, which had already been signed off by Johannes Schindelin as part of the series. The exchange shows the careful attention being paid to Windows-specific path handling details in preparation for broader symlink support.