This is a documentation patch that follows up on the previous version by adding the missing Signed-off-by line, as promised in the initial submission. The content remains unchanged from the already-reviewed version that improved clarity around the `core.attributesFile` configuration option.

The patch makes the documentation more self-contained by directly specifying that the value is a pathname, removing the indirect reference to `core.excludesFile` for path expansion behavior. This matches the same pattern used in commit dca83abde2 which improved the `core.excludesFile` documentation. The change maintains all existing functionality while making the behavior more immediately obvious to readers.

Key technical details for future reference: the patch modifies Documentation/config/core.adoc, specifically the section describing `core.attributesFile`. It replaces the indirect path expansion reference with a direct "pathname" type specification, following the precedent set for `core.excludesFile`. The default value behavior (using XDG_CONFIG_HOME or falling back to $HOME/.config) remains unchanged.

This appears to be a straightforward, uncontroversial documentation improvement that has already been reviewed and accepted in content, now properly formatted with the required contributor attestation.