This is part 5/7 of Justin Tobler's series adding object size reporting to `git repo structure`. The patch focuses on adding inflated object size metrics to the table output format while maintaining the existing keyvalue and nul formats unchanged.

The changes introduce human-readable formatting for inflated sizes in the table view, showing values like "16.03 MiB" with appropriate unit scaling. A new `HUMANISE_COMPACT` flag is added to control whether to use short unit symbols ("B") or full words ("bytes"). The implementation builds on the existing `object_stats` infrastructure and adds new `stats_table_size_addf()` helper functions.

The patch includes thorough test updates showing both empty repository and populated repository cases, demonstrating the new size reporting alongside the existing count metrics. The changes appear well-integrated with the series' existing architecture and maintain backward compatibility with non-table output formats.

This continues the series' methodical approach, now completing the size reporting functionality across all object types (commits, trees, blobs, tags) with proper human-readable formatting. The implementation looks clean and maintains the established patterns from earlier patches in the series.