This is part 3 of Justin Tobler's series adding object size metrics to `git repo structure`, focusing on human-readable formatting of large numbers. The patch introduces a new `humanise_count()` function that converts large counts into scaled values with SI prefixes (like "1.02 k" instead of "1024"). The table output now displays these formatted values with proper alignment between numbers and their units, while preserving the exact counts in machine-readable formats.

The changes include:
1. A new `humanise_count()` utility in strbuf.c that handles number scaling
2. Updates to the table formatting code to accommodate separate number/unit columns
3. Test updates showing the new formatted output (including a test case with 1005 commits showing as "1.01 k")

This is a well-executed refactoring that builds on the series' earlier work, maintaining all existing behavior while adding the promised human-friendly formatting. The patch shows careful attention to detail with proper i18n support and clean table alignment. As part of the v5 series that's already received positive review, this appears ready for integration.