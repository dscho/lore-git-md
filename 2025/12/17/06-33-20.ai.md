This is a workflow question about rebasing after automatic code reformatting. Simon Richter describes their current manual process: creating an empty commit at the branch point, then using `filter-branch` to apply the same formatting to all commits in their branch before rebasing, which avoids merge conflicts by ensuring consistent formatting throughout the history.

The core issue is that automatic reformatting (like `clang-format` or similar tools) creates widespread whitespace and formatting changes that would normally cause numerous merge conflicts during rebase. Simon's current solution works around this by making the formatting changes "disappear" from Git's perspective by applying them uniformly across all commits before the rebase.

The email asks if there's a better way to automate this process, particularly when dealing with multiple branches that need the same treatment. This suggests the current approach may be tedious or error-prone when scaled. No specific Git commands or scripts are proposed - the email is purely seeking advice on improving this workflow.

Key points for future reference:
- The problem involves rebasing after mechanical formatting changes
- Current solution uses `filter-branch` to normalize formatting
- Looking for alternatives that might be more maintainable or scalable
- Particularly interested in solutions that work across multiple branches

This appears to be a genuine workflow pain point that might benefit from either existing Git features the author isn't aware of, or possibly new tooling suggestions from the community. The question is well-framed and specific enough that experienced Git contributors may have concrete suggestions.