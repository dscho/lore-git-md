This thread has completed a major effort to bring comprehensive symbolic link support to Git on Windows, building on years of work in Git for Windows. The core series (now at v5) has been approved for integration after addressing all technical concerns, including performance optimizations, architectural cleanups, error handling improvements, and most recently a fix for correct symlink size calculation.

Karsten Blees and Johannes Schindelin (Dscho) have been the primary drivers, with Johannes Sixt and Ben Knoble contributing key reviews and fixes. The series touches several Windows-specific compatibility layers, implementing POSIX-like symlink operations while handling Windows' reparse points and other platform quirks.

The latest patch refactors retry logic for file operations that might be temporarily locked (like when another process has a file open) into a shared helper function. This cleanup consolidates duplicate code from `mingw_unlink()`, `mingw_rmdir()`, and renaming functions, implementing consistent retry behavior with progressive delays. Both Karsten and Dscho have signed off on this refinement, which follows the series' pattern of incremental quality improvements after the core functionality was solidified.

With all technical issues resolved and maintainer approval secured, this Windows symlink support work is now complete and ready for inclusion in upcoming releases. The final patches represent polishing touches to an already-solid implementation that will significantly improve Git's behavior on Windows.