Thread identity: Major platform compatibility patch series introducing symbolic link support for Windows (18 patches, now at v4 with contributions from Bill Zissimopoulos, Johannes Schindelin (Dscho), and Karsten Blees). Builds on Git for Windows' symlink implementation that has been maturing since 2015.

Core problem/goal: Upstream comprehensive Windows symlink support by implementing POSIX-like symlink operations in the MinGW compatibility layer, including creation, reading, stat'ing, directory handling, and now renaming. Key technical components include proper `stat()`/`lstat()` implementation, `readlink()` functionality, directory symlink handling in `unlink()`/`chdir()`, symlink-specific error codes, and robust rename operations. Now includes default behavior configuration with `core.symlinks=false` on Windows and complete symlink creation support for both files and directories, plus non-elevated creation on modern Windows.

Current status: Sixteen patches submitted (v4). New patch 16 adds non-elevated symlink creation support for Windows 10+ with Developer Mode enabled. Previous patches established file/directory symlink creation (patches 14-15), symlink detection infrastructure, error handling, rename behavior, and `readlink()` support. Series builds on long-running Git for Windows development. Not yet in any integration branch.

Key technical details: Touches primarily `compat/mingw*` files. New patch 16 implements Windows 10+ non-elevated symlink creation using `SYMBOLIC_LINK_FLAG_ALLOW_UNPRIVILEGED_CREATE` with careful version checking (build >= 14972). Adds `symlink_file_flags`/`symlink_directory_flags` globals and `adjust_symlink_flags()` initialization. Integrates with existing `process_phantom_symlink()` and `symlink()` paths while maintaining backward compatibility. Builds on previous infrastructure: Patch 1's redundant call elimination, Patch 2's symlink resolution, Patch 6's `read_reparse_point()`, Patch 9's error code translations, and Patches 14-15's symlink creation. Maintains consistent path handling and error reporting across all versions.

Open questions/loose ends: Final two patches remain to complete series. Edge cases around target type changes after creation and cross-process directory creation acknowledged but not fully handled. Test coverage for new non-elevated symlink functionality across Windows versions needed. MAX_PATH limitations in symlink resolution may require follow-up. Series approaches completion with only minor loose ends remaining.

Participant positions: Karsten Blees remains primary author with Dscho's signed-off-by on all patches. No external reviews yet recorded. Consistent contributor engagement suggests series is following established Git for Windows approach.

Related work: Continues `js/prep-symlink-windows` foundation. Represents major progress in Windows-POSIX compatibility, now covering creation (including non-elevated), reading, and management of both file and directory symlinks. Each patch demonstrates iterative refinement of Windows filesystem behavior while maintaining POSIX compatibility. The series shows careful attention to edge cases while methodically completing previously stubbed POSIX APIs.