This is part 6/7 of Justin Tobler's series adding comprehensive object size reporting to `git repo structure`. The patch extends the keyvalue and nul output formats to include disk size metrics for objects, complementing the existing inflated size reporting.

The change adds a new `disk_sizes` field to the `object_stats` struct and populates it during object counting by using `oi.disk_sizep` from `odb_read_object_info_extended()`. The disk sizes are then included in the keyvalue/nul format output alongside the other metrics. Test coverage is updated to verify the new disk size reporting works correctly.

This completes the size reporting functionality for the machine-readable output formats, following the same pattern established earlier in the series for inflated sizes. The implementation maintains consistency with the existing code structure and adds the expected test coverage. With this change, all three output formats (table, keyvalue, and nul) now support both count and size metrics (inflated and disk) for objects.

The patch appears well-crafted, following the established patterns from earlier in the series, and completes an important piece of the overall feature. It's part of a methodical series that has already received positive technical feedback from reviewers.