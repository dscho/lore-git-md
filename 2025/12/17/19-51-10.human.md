This is a documentation patch improving the description of `core.attributesFile`. Matthew Hughes points out that the current wording requires readers to jump through multiple documentation sections to understand path expansion behavior. The patch simplifies this by directly linking to the 'pathname' type documentation, following the same pattern used for `core.excludesFile` in a previous commit.

The change is straightforward - it removes the indirect reference to `core.excludesFile` and makes the pathname behavior more immediately clear. This appears to be a well-motivated documentation improvement that makes the config option's behavior more discoverable without changing any functionality. The patch follows the established pattern from prior documentation updates.