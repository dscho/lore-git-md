This is a clarification response in the ongoing `git replay` empty commit handling thread. Phillip Wood explains his rationale for using `--only` with `--allow-empty` when creating test commits, noting it's his personal practice to ensure empty commits remain truly empty even if there are staged changes. He offers to remove the flag if preferred, though expresses his general dislike for `--allow-empty` without `--only` since he always wants explicit control over empty commit creation.

The email doesn't introduce new technical changes to the v3 patch but provides context for the test case implementation detail that Junio C Hamano questioned. Phillip's explanation reveals this as a personal workflow preference rather than a test-specific requirement, showing willingness to adjust if maintainers disagree. The thread remains technically resolved with this being the last minor point of discussion before finalization.