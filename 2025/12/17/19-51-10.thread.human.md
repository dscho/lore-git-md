This thread begins with a documentation patch improving the clarity of the `core.attributesFile` configuration option's description. The change makes the behavior immediately understandable by directly stating it's a pathname, rather than requiring readers to cross-reference the similar `core.excludesFile` documentation. The patch follows the same pattern as a previous improvement to `core.excludesFile` (commit dca83abde2), maintaining consistency across related configuration options while making each one self-contained. The change is cleanly focused, touching only the relevant section of Documentation/config/core.adoc, and doesn't modify any actual behavior - it simply makes the existing functionality clearer to users reading the documentation.