This thread documents a straightforward improvement to the `core.attributesFile` configuration option's documentation. The original patch, submitted by Matthew Hughes, made the behavior more self-contained by directly stating the value is a pathname rather than indirectly referencing `core.excludesFile` for path expansion behavior - following the same pattern used in a prior change to `core.excludesFile` documentation (commit dca83abde2).

The initial submission lacked the required Signed-off-by line, which Matthew promptly corrected in this follow-up version while keeping the documentation content unchanged. The change maintains all existing functionality while making the behavior more immediately obvious to readers. The patch content had already been reviewed and accepted as a clean, focused improvement before the contributor attestation was added.

This appears to be a minor but useful documentation clarification that follows established patterns and should be uncontroversial to merge. The thread is now complete with all required elements in place.