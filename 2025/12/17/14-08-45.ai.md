This is part 8 of the major Windows symlink support series, focusing on default behavior configuration. The patch changes the default value of `core.symlinks` to false on Windows, requiring explicit opt-in for symlink functionality. This reflects Windows' more restrictive symlink environment where creation often needs admin privileges and behavior differs from Unix systems.

The change affects `compat/mingw.c` by modifying the Windows environment setup to disable symlinks by default unless the MSYS2 environment explicitly enables them via the `MSYS=winsymlinks:nativestrict` variable. This allows the test suite to run with symlink support when desired while keeping the safer default for normal usage. The implementation includes special handling for test environments while maintaining the series' focus on proper POSIX compatibility.

Key technical details include the direct modification of the `has_symlinks` global variable during environment setup and the conditional check for MSYS2's symlink support flag. The change builds on the series' goal of comprehensive Windows symlink handling while acknowledging platform limitations through conservative defaults. The signed-off-by from both Karsten Blees and Johannes Schindelin indicates this continues their collaborative work on Windows compatibility.

This patch represents a policy decision in the larger technical implementation, showing the series' attention to real-world usability concerns. The test suite accommodation demonstrates forward-thinking about development workflows while the default change reflects production environment realities. The implementation is straightforward but important for the series' overall Windows compatibility story.