This is the foundational refactoring patch in Justin Tobler's series adding comprehensive object size reporting to `git repo structure`. It restructures the code to prepare for adding size metrics alongside existing object counts.

The patch introduces a new `object_values` struct to group the per-type object counts (tags, commits, trees, blobs) that were previously direct fields in `object_stats`. This creates a clear pattern for adding parallel size measurements in subsequent patches. The change includes:
- Moving the count fields into the new nested struct
- Renaming `get_total_object_count()` to `get_total_object_values()` to reflect its broader role
- Updating all references to the moved fields through the new `type_counts` member
- Maintaining identical output formatting in both table and keyvalue modes

The refactoring is mechanical but carefully done, with all call sites updated consistently. This establishes the architectural pattern the rest of the series will follow when adding inflated and disk size measurements. The patch shows good attention to detail, with proper progress reporting preservation and BUG() handling for invalid types carried forward unchanged.

Key technical points for future reference:
- New `object_values` struct contains the core count fields
- `object_stats` now contains `type_counts` as a member
- All display formatting (`stats_table_*` and `structure_keyvalue_print`) updated to use new access path
- `count_objects()` progression counting maintains same behavior via new struct
- Total count calculation function renamed but keeps same signature/behavior

This is a clean preparatory change that enables the series' subsequent additions while maintaining existing functionality. The thorough updates across all output formats and helper functions demonstrate the change was properly validated.