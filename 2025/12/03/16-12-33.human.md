Security hardening follow-up to the `mktemp(3)` removal series.

Jeff King (Peff) expresses approval for René Scharfe's final patch in the series that bans the insecure `mktemp(3)` function from Git's codebase. This comes after the team successfully migrated all callers to the secure `git_mkdtemp()` wrapper introduced earlier in the series.

The exchange is brief but significant - it marks the successful completion of a security-focused effort to eliminate a known-vulnerable POSIX function that was deprecated due to TOCTOU race conditions. The series achieved both security goals (removing insecure code paths) and codebase simplification (net reduction of platform-specific code).

This appears to be the final ack in the thread, suggesting the series is ready for merging after the earlier discussion about potential follow-up simplifications to the Windows compatibility layer. The patch quality looks solid, with all security considerations properly addressed in the earlier messages.