This is a refactoring patch in Patrick Steinhardt's `git-history` series, specifically focused on the interactive add/patch subsystem. The patch removes the dependency of `add-patch.c` on the broader `add-interactive` infrastructure by directly managing its own configuration state.

The change follows from the previous patch in the series which split out interactive configuration handling. Now instead of initializing through `init_add_i_state()`, the code maintains its own `struct interactive_config` directly within `add_p_state`. This makes the relationship between the patch-specific state and its configuration more direct and self-contained.

The diff shows mechanical changes to update all references from the old `s.s.cfg` path to the new direct `s.cfg` access. The patch appears to be a straightforward structural improvement that doesn't change behavior, just organization. It's part of the larger effort to make the interactive add/patch code more modular and reusable, which is important for the history editing commands being introduced in this series.

The changes are consistent with the series' goal of refactoring infrastructure to support the new `git-history` command while improving code organization. Since this is part of an established series that's already received significant review, and the changes are focused and mechanical, it seems likely to be uncontroversial.