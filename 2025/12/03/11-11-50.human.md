Classification: Bugfix for `git last-modified` behavior with sparse checkouts.

This email from Toon Claes weighs in on the ongoing discussion about fixing `git last-modified`'s handling of paths outside sparse checkouts. The thread began with Johannes Schindelin's patch to properly support the `--` separator for such cases, which Derrick Stolee had questioned regarding behavior boundaries.

Claes provides concrete test demonstrations showing the current behavior on master (where both forms fail) versus the patched version (where `--` works as expected). They argue against overcomplicating the solution, noting that pathspecs after `--` should simply return nothing when no paths match, which aligns with Git's general behavior patterns.

The email serves as a clear endorsement of Schindelin's original patch, showing it produces the expected behavior while maintaining consistency with Git's existing conventions. Claes explicitly states the new behavior is better and gives their support for the fix, moving the discussion toward resolution. The technical details remain focused on the sparse checkout case with path 'a' when only 'b' is checked out, keeping the scope narrow to the original bug report.