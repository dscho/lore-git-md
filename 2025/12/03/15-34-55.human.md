This is a documentation patch fixing a minor typo in the `git-pull` man page. Julia Evans corrects an incorrect command syntax from `git --rebase abort` to the proper `git rebase --abort`. The error was introduced in commit e9d221b0b.

The change is straightforward and clearly correct - the original syntax would not work as written, while the corrected version matches Git's actual command structure. The patch touches only the documentation file with a simple two-line fix, and includes a proper commit message identifying both the problem and its origin. This appears to be a clean, uncontroversial improvement to the documentation.