This is a refactoring patch in Patrick Steinhardt's v7 series introducing the `git-history` command. It splits the `add-patch` declarations out of `add-interactive.h` into a new `add-patch.h` header file.

The change is straightforward - it moves declarations related to the patch application functionality (like `struct add_p_opt` and `enum add_p_mode`) into their own header while keeping the interactive add-specific declarations in `add-interactive.h`. This creates clearer boundaries between the two subsystems.

The patch is part of the larger effort to enable in-memory index operations for the `git-history` command's `split` subcommand, which builds on the interactive add/patch infrastructure. While this particular change is purely organizational, it helps maintain separation of concerns as the codebase grows to support these new history editing capabilities.

The implementation looks clean and focused, with appropriate includes added and declarations moved verbatim. This appears to be a preparatory refactoring that doesn't change any behavior, just improves code organization in service of the larger feature work.