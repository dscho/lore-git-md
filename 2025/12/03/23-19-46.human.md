Bugfix/feature discussion around lockfile debugging mechanisms. Taylor Blau responds to Jeff King's concerns about the `.lock.pid` approach for tracking stale locks, proposing alternative naming schemes and configuration models.

The email explores two main alternatives to avoid refname collisions: (1) changing the suffix pattern to `-pid.lock` instead of `.lock.pid`, which would leverage existing `.lock` suffix protections, and (2) introducing new refname constraints for `.lock.pid` (which Taylor opposes as too restrictive, especially with reftable adoption). The discussion then shifts to configuration models, suggesting a more flexible approach inspired by `core.fsync` settings that would allow per-subsystem control (e.g., enabling PID tracking for index operations but not ref updates).

Taylor's response thoughtfully engages with the technical tradeoffs while maintaining a clear preference against adding new refname constraints. The proposal for a structured configuration format (similar to Git's fsync settings) shows careful consideration of both immediate needs and long-term interface design. This continues the thread's progression toward practical solutions while acknowledging the complex filesystem coordination challenges involved.