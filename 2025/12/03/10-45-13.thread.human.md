This thread has successfully completed a security-focused effort to eliminate insecure `mktemp(3)` usage throughout Git's codebase, culminating in adding it to `banned.h`. The series introduced a secure cross-platform `git_mkdtemp()` wrapper and removed now-unneeded compatibility code, resulting in a net reduction of 30 lines while maintaining Windows support.

Key participants Jeff King (Peff) and René Scharfe have been discussing follow-up cleanups to further simplify the temporary file infrastructure. While there was initial agreement to have the NO_MKDTEMP compatibility wrapper directly use the new `git_mkdtemp()` function, Scharfe raised valid concerns about build system complexities (particularly CMake issues on macOS) that warrant addressing this in a separate patch. The discussion has now expanded to consider whether to completely ban `mkdtemp(3)` system calls, though this raises questions about third-party code dependencies like the clar test framework that currently uses it directly.

Junio Hamano has reviewed and approved the technical approach, particularly the shared `git_mkdstemps_mode()` helper that handles both file and directory creation paths. While some implementation refinements remain for future work, the core security improvements are now complete with all callers migrated to secure alternatives. The thread demonstrates Git's careful approach to security hardening - proactively eliminating unsafe functions even without known exploits - while thoughtfully considering real-world constraints like build system compatibility and third-party code dependencies.