Build system patch continuing the Meson cross-compilation improvements. This is a follow-up to the previous 2-patch series, addressing Patrick Steinhardt's observation about standardizing on `meson.can_run_host_binaries()` for execution tests.

The change replaces `not meson.is_cross_build()` with `meson.can_run_host_binaries()` in a compiler test for `sysctl` detection, making the build system properly respect execution wrappers like QEMU during cross-compilation. The patch notes there's one remaining case where `is_cross_build()` is still appropriate (guarding `fs.exists()`), showing careful consideration of when each check should be used.

This is a straightforward technical improvement that aligns with the series' goal of better cross-compilation support. The change is minimal (1 line modified) and well-explained, with clear justification for both the change and the remaining exception. The patch appears ready for inclusion, completing the standardization effort Patrick suggested earlier in the thread.