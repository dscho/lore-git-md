This thread began as a documentation correction from Julia Evans, who noticed and fixed an incorrect command syntax example in the git-pull manpage where `git --rebase abort` was mistakenly written instead of `git rebase --abort`. The change was minimal but important for accuracy.

Kristoffer Haugsbakk then pointed out that commit references in documentation should use Git's standard `git show -s --pretty=reference` format rather than abbreviated hashes, adding another documentation convention to the discussion. 

The conversation has since shifted slightly to address contributor guidelines, with Haugsbakk explaining to Stefan Rieche that the Git project requires plain text emails (not HTML/multipart) for list participation, noting this is why Rieche's message didn't appear in the archives. While no further action is needed on the original documentation patch, this serves as useful guidance about the project's communication standards for new contributors. The thread remains focused on documentation quality - first correcting command syntax, then discussing commit reference formatting, and finally covering email submission practices.