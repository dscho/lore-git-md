This is a documentation correction follow-up. Kristoffer Haugsbakk points out that Julia Evans' patch fixing the `git --rebase abort` typo in git-pull.adoc should reference the commit using the standard `git show -s --pretty=reference` format rather than just the abbreviated hash. 

The original patch (already submitted via GitGitGadget) was a straightforward fix for an incorrect command syntax example, changing `git --rebase abort` to the proper `git rebase --abort` form. This follow-up doesn't challenge the technical content of the fix but rather provides a style suggestion about commit reference formatting, pointing to the SubmittingPatches documentation.

The exchange shows good attention to detail from both contributors - first catching the documentation error, then ensuring the fix follows project conventions. The thread remains focused on this minor but correct documentation improvement.