Feature patch series introducing `git-history` command with `reword` and `split` subcommands, building on Git's replay infrastructure. This v7 iteration refactors shared code between subcommands, improves test coverage (now 432 lines for split, 237 for reword), and addresses technical feedback from previous reviews. Key changes include:

- Shared validation logic and message handling between `reword` and `split` subcommands
- `split` now prompts for both commit messages and disallows hunk editing to avoid conflicts
- Uses `--ancestry-path` and `--reverse` for commit enumeration
- Improved documentation of experimental status and limitations
- The implementation now uses in-memory index operations via refactored add-patch infrastructure
- Current focus remains on non-conflicting operations (reword and split) while laying groundwork for future subcommands like `drop` and `reorder`

Technical details show careful integration with existing Git infrastructure:
- Leverages replay machinery while avoiding `the_repository` global
- Adds wt-status tree-to-tree diff capabilities
- Extends cache-tree to support in-memory index writing
- Refactors add-patch to support in-memory operations

The series appears ready for experimental merging pending final documentation updates, with Junio Hamano advocating for wider testing in 'next'. Real-world use cases from D. Ben Knoble demonstrate value of the broader functionality, though some previously implemented subcommands were removed from this series due to technical challenges in the replay-based approach.