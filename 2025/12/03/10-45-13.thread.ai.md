Security-focused series eliminating insecure `mktemp(3)` usage throughout Git codebase, now expanded to 6/5 patches with v2 adding removal of `gitmkdtemp()` compatibility wrapper (-33 LOC total). Series introduced cross-platform `git_mkdtemp()` wrapper (wrapper.c/h) in patch 1/6, building on `git_mkstemps_mode()` infrastructure for secure directory creation with 0700 permissions. The new patch 1/6 introduces the foundational `git_mkdstemps_mode()` function as shared backend for both file and directory creation, with careful documentation of return value behavior (-1 for error, 0 for directory creation, file descriptor for regular files). Subsequent patches will migrate Windows-compatibility layer (2/6), remove `mingw_mktemp()` (3/6, -15 LOC), ban `mktemp(3)` in `banned.h` (4/6), and remove `gitmkdtemp()` wrapper (5/6) as discussed earlier in thread.

Jeff King (Peff) and René Scharfe provided key review input, with Peff proposing the wrapper removal path and Scharfe noting build system considerations (CMake macOS issues, NO_MKDTEMP testing coverage). Junio Hamano concurs with implementation approach. V2 changes include clarified comments about `git_mkdstemps_mode()` return values and the additional cleanup patch completing the simplification path.

Open questions remain regarding future potential banning of `mkdtemp(3)` system calls (parallel to `mktemp(3)` ban), which would require evaluating third-party code dependencies like clar test framework. Build system integration for NO_MKDTEMP knob removal also remains for future consideration, though current series achieves consensus on removing redundant layers where safely possible.

Security rationale emphasizes proactive hardening by banning unsafe functions before exploits emerge, with all callers migrated to secure alternatives. Implementation maintains cross-platform compatibility while eliminating insecure code paths. Key participants: Jeff King (security review), René Scharfe (build system considerations), Junio Hamano (maintainability review). Series shows completed migration path from insecure functions to unified secure infrastructure, with clear direction for any future cleanups. The expanded patch 1/6 provides the necessary foundation for both file and directory creation under a single secure implementation.