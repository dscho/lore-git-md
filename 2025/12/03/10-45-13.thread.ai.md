Security-focused series eliminating insecure `mktemp(3)` usage throughout Git codebase, now completed with 4/4 patch banning the function in `banned.h` (+3 LOC). Series introduced cross-platform `git_mkdtemp()` wrapper (wrapper.c/h) in patch 1/4, building on `git_mkstemps_mode()` infrastructure for secure directory creation with 0700 permissions. Subsequent patches migrated Windows-compatibility layer's `gitmkdtemp()` (2/4), removed now-unused `mingw_mktemp()` implementation (3/4, -15 LOC), and received final Reviewed-by from Jeff King (Peff) for the `banned.h` addition (4/4).

Jeff King and René Scharfe discussed technical refinements earlier in the thread, with Peff proposing two potential follow-ups: removing the redundant `gitmkdtemp()` wrapper while keeping NO_MKDTEMP knob (immediate cleanup), then eliminating the knob entirely since `git_mkdtemp()` provides portable implementation (future step). These changes would remove compat/mkdtemp.c entirely (-15 LOC) and update build systems (Makefile, meson, CMake, configure.ac). In the latest exchange, Scharfe agrees with the direction but notes practical complications across build systems: CMake being broken on macOS, limited testing coverage for NO_MKDTEMP configurations, and the need for careful coordination across Makefile, meson, and CMake. He suggests these cleanups warrant a separate patch due to their complexity, leaning toward the comprehensive solution of eliminating NO_MKDTEMP entirely by always using `git_mkdtemp()`, which would require updates to configure.ac and other build files.

Chris Torek noted a minor code clarity issue in the `git_mkdtemp()` implementation where the shared `fd` variable usage between file and directory creation paths could mislead future maintainers. While functionally correct, the observation suggests adding a clarifying comment to prevent misunderstanding of the return value semantics. This represents the careful attention to detail typical of Git's security-related changes.

Implementation achieves net -30 LOC across series when including proposed follow-ups, preserving Windows compatibility through mingw layer while eliminating insecure code paths. Security rationale emphasizes proactive hardening by banning unsafe functions even without known exploits, following precedent of `gets()`/`strtok()` bans. All callers migrated to secure alternatives before final ban, with careful attention to maintaining identical permission behavior. The thread shows consensus on both the security improvement and potential future cleanups, with remaining work focused on build system integration details for the NO_MKDTEMP removal. Key technical considerations include maintaining the function_checks dictionary entry during transition, parallel CMakeLists.txt updates, and the eventual possibility of banning mkdtemp(3) entirely rather than just redirecting to the secure wrapper.