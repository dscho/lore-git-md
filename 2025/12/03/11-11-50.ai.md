Bugfix follow-up discussion for `git last-modified` sparse checkout handling.

Toon Claes responds to Derrick Stolee's questions about the sparse checkout behavior of `git last-modified` with the `--` separator. The discussion centers on whether paths outside the sparse checkout should work without `--`, and what the command should do when no paths match.

Clae demonstrates the current behavior on master versus the patched version, showing that with the patch, `git last-modified -- a` (where 'a' is outside sparse checkout) now succeeds with empty output rather than erroring. This matches the behavior of other Git commands where non-matching pathspecs after `--` produce empty output rather than errors.

The key technical points are:
- Confirmation that paths outside sparse checkout should only work with `--` separator
- Empty output (exit code 0) is the correct behavior for non-matching pathspecs
- The patch maintains consistency with other Git commands' handling of pathspecs

Clae approves the patch as-is, suggesting no need to complicate it further by trying to handle paths without `--`. This moves the discussion toward resolution, with Stolee's earlier questions about behavior boundaries being addressed by showing the current behavior is both correct and consistent with Git's patterns.