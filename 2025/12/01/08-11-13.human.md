Follow-up to documentation fix for `git config unset` flags. This is a simple ack from Patrick Steinhardt, agreeing with Junio Hamano's earlier approval of René Scharfe's patch that corrected misleading help text for the `--all` and `--value` flags in `git config unset`. The original patch (which modified two lines in `builtin/config.c` to clarify these flags' purposes) has already been reviewed and queued for inclusion. This email adds no new technical content - it's just a confirmation from another contributor that the fix is correct and uncontroversial. The thread appears concluded with this being the second positive review.