This is a follow-up in the `git replay` signature handling bugfix thread, where Patrick Steinhardt acknowledges the resolution and plans for integration with his ongoing work.

Patrick concurs with Phillip Wood's earlier observation about the duplicated signature exclusion logic across three files (`builtin/replay.c`, `builtin/commit.c`, and `sequencer.c`), noting the inconsistency with `commit.c`'s `gpg_sig_headers` variable. While recognizing this as a maintainability issue (#leftoverbits), he defers it for future work, agreeing the immediate 1-line fix for `gpgsig-sha256` handling takes precedence.

Responding to Junio's prioritization of bugfixes over development, Patrick confirms he'll rebase his `ps/history` branch (which had conflicted with this change due to code movement) to incorporate the fix. The exchange concludes the technical discussion, with all parties aligned on the path forward: the bugfix will land first, followed by potential architectural cleanup of the signature exclusion mechanism later. Key files involved remain `builtin/replay.c` (immediate fix) and the three files with duplicated logic (future refactoring target).