This is a build system follow-up confirming meson's stripping behavior.

Patrick Steinhardt confirms Johannes Schindelen's earlier observation about meson's native stripping capability, noting that meson automatically handles stripping all binaries when the `--strip` option is used during setup. This means no explicit changes are needed in the meson build configuration to support stripping the `scalar` binary, unlike the Makefile case that started this thread.

The email serves as a technical confirmation that meson's approach is indeed superior for this use case, reinforcing the earlier discussion about build system modernization. It also implicitly supports the thread's resolution to accept the minimal Makefile fix while recognizing meson's more comprehensive solution.

Key technical details include meson's `--strip` setup option behavior and the contrast with Makefile's explicit per-binary stripping. The message doesn't introduce new changes but provides important confirmation about meson's capabilities that could inform future build system decisions.