This is a discussion about clar unit test framework improvements, following up on Jeff King's experience report about difficulties encountered while writing tests for the new parsing utilities.

Phillip Wood responds to several specific pain points Jeff raised about the clar framework. He suggests potential solutions for some issues while acknowledging others as real problems. For the uninformative error messages, Phillip notes the old test framework's approach of passing file/line numbers via macros could be adapted to clar, and mentions `cl_failf()` as an alternative to assertions for adding context. He confirms the `cl_assert_equal_i_fmt()` UB bug is known and points to an open PR (#117) that would add type-agnostic assertion macros.

Phillip provides a concrete fix for the TAP output parsing issue (changing `---` to `...` in YAML terminator lines) that makes `prove` work correctly, though it still doesn't show error messages. He's less opinionated about Jeff's suggestion of wrapping unit tests in a shell script to handle output filtering, noting the parallel execution rationale no longer applies since clar tests run serially.

The exchange highlights ongoing friction between clar's current capabilities and Git's testing needs, particularly around test debugging, output formatting, and assertion ergonomics. Phillip's responses indicate some issues may be addressed by existing PRs or workarounds, while others represent deeper framework limitations that may require clar-side changes. The discussion remains constructive but surfaces real productivity impacts from these testing friction points.