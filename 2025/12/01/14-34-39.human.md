This is a follow-up discussion in the thread about adding `-z` support to `git repo structure`. Lucas Seiki Oshiro responds to Patrick Steinhardt's defense of maintaining consistency with `git repo info`'s option parsing behavior.

The email confirms agreement with Patrick's position that the `-z` flag should behave the same way in both commands, where combining `--format` and `-z` follows Git's standard "last one wins" behavior. Lucas notes that while alternative documentation formats like `[--format ... | -z]` would be acceptable, keeping the synopsis identical between the two related commands makes more sense.

This moves the thread closer to resolution, as it appears all parties now agree on:
1. Maintaining behavioral consistency with `git repo info`
2. Using the same documentation format for both commands
3. The test case should directly check `-z` rather than `--format=nul`

The only remaining open question from earlier in the thread - whether to add an explicit test for combined option behavior - is not addressed in this response. The patch appears to be in good shape technically, with the discussion now focused on minor documentation and testing polish.