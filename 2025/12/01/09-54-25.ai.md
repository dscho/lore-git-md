This is a status update on two ongoing refactoring/feature topics from Patrick Steinhardt, responding to Junio Hamano's "What's cooking" report.

The first topic (`ps/object-read-stream`) is a major refactoring of Git's object streaming interface to better support pluggable object databases. It's a 20-commit series that reorganizes the streaming code to be object-database-centric, removes `the_repository` usage, and introduces new structures for different stream types. Patrick indicates this is ready for merging to 'next' after addressing previous review comments through message improvements and a function rename, with no new feedback on the latest version.

The second topic (`ps/history`) introduces a new `git history` command for history rewriting, currently at 12 commits. This appears to be a more complex feature involving multiple subsystems (cache-tree, add-patch, replay) and still requires a reroll to address feedback. Patrick notes he'll handle this later in the week. The branch is also being used by another topic (`pw/replay-drop-empty`), indicating some dependency relationships in the history rewriting work.

Both topics represent significant architectural changes - the first cleaning up object streaming internals, the second introducing new user-facing functionality. The streaming refactor seems closer to completion while the history command is still in active development. The email provides useful context about the state of these contributions in the broader integration process.