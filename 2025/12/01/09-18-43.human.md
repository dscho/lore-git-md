Bugfix follow-up for `git replay` signature handling. Patrick Steinhardt acknowledges the recently approved patch that fixes incomplete signature header exclusion in `git replay` (which was missing the `gpgsig-sha256` header). He agrees with Phillip Wood's earlier observation about the duplicated exclusion logic across three files (`builtin/commit.c`, `builtin/replay.c`, and `sequencer.c`), noting this as technical debt that could be addressed separately.

The email confirms Steinhardt will rebase his `ps/history` branch (which had conflicted with this fix) on top of the now-approved change, respecting Junio Hamano's decision to prioritize bugfixes over development work. While the immediate signature handling issue is resolved, the architectural question of centralizing this logic remains open as future work. The exchange shows smooth coordination between the maintainer and active developers to land an important correctness fix.