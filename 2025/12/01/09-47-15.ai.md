This is a technical follow-up in the Windows symlink test compatibility series, specifically addressing patch 7/10 which modifies t1006-cat-file.sh. Patrick Steinhardt is seeking clarification on the Windows-specific test changes for `git cat-file --follow-symlinks` behavior.

The key technical question revolves around whether the MSYS2 symlink normalization behavior (which causes the test to be skipped on MINGW) represents a bug in Git's symlink handling that should eventually be fixed, or if it's an unavoidable platform difference. Patrick suggests that if this is a fixable limitation, the test should include an `else` branch documenting the current failure mode to serve as a regression test for future fixes.

The patch being discussed makes two Windows-specific adjustments to t1006:
1. Modifies the expected output format for out-of-repo symlinks (changing length from 3 to 2 bytes and removing trailing slash) when both MINGW and SYMLINKS prerequisites are met
2. Skips the internal `..` symlink tests entirely on MINGW due to MSYS2's path normalization differing from Git's expectations

Patrick's query indicates potential uncertainty about whether the skipped tests represent:
- A temporary workaround for MSYS2's behavior that should eventually be resolved in Git's Windows symlink handling
- A permanent platform difference that requires ongoing special casing

The thread context shows this is part of a fully-approved series, but Patrick's question suggests there may be lingering uncertainty about whether this particular test adjustment represents a permanent solution or a temporary measure awaiting core symlink handling improvements on Windows. The response from the original author (not shown here) would need to clarify whether the MSYS2 symlink normalization behavior is considered a bug in Git or an unavoidable platform difference.