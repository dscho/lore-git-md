This is a status update on two ongoing refactoring efforts from Patrick Steinhardt, responding to Junio Hamano's monthly "What's cooking" summary.

The first topic (`ps/object-read-stream`) is a 20-commit series refactoring Git's object streaming interface to better support pluggable object databases. It's now ready for merging into 'next' after addressing previous review comments with minor commit message improvements and a function rename. The changes eliminate `the_repository` usage and restructure the streaming backend to be more object-database-centric.

The second topic (`ps/history`) is a 12-commit series introducing a new `git history` command for history rewriting, which is still awaiting a reroll. Patrick notes he'll address feedback on this later in the week. This work builds on previous replay functionality while also removing `the_repository` dependencies.

Both patches represent significant internal refactoring work - the first appears polished and ready for integration, while the second remains in development. The email provides useful context about their status in the broader development cycle.