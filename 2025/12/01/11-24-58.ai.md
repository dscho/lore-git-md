This is a refactoring patch that's part of a series (1/2) to support alternate reference directories. The patch extracts the core ref_store creation logic from `get_main_ref_store()` into a new helper function `get_ref_store_for_dir()`. This prepares for upcoming changes where the refs subsystem will need to create stores for reference directories outside the main repository.

The new function takes a repository, directory path, and storage format as parameters, making it more flexible than the original single-purpose function. The actual behavior remains unchanged - this is purely structural preparation. The patch is cleanly implemented with minimal changes (10 lines added, 3 removed) and maintains the existing debug wrapper functionality.

Key technical details for future reference:
- Introduces `get_ref_store_for_dir()` with same initialization logic as original
- Keeps `maybe_debug_wrap_ref_store()` wrapping
- Maintains same `REF_STORE_ALL_CAPS` flag usage
- Part of work to support arbitrary reference directories (implied by commit message)
- Touches only refs.c with straightforward changes
- Follows existing patterns for ref_store initialization

This appears to be a well-justified preparatory change that doesn't introduce new behavior but enables future work with alternate ref directories. The commit message clearly explains the motivation and the changes are minimal and focused.