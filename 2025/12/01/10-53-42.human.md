This is a status update on two of Karthik Nayak's patch series in response to Junio Hamano's "What's cooking" summary.

For the `kn/ref-location` series (introducing `GIT_REF_URI` for specifying reference backends and directories), Karthik indicates a new version will be sent today. This appears to be a feature targeting refs migration scenarios, though the current status is unclear - it's marked as needing comments in Junio's summary.

The `kn/fix-fetch-backfill-tag-with-batched-ref-updates` series (fixing tag fetching issues with batched ref updates) is reported as ready for merging to 'next' after multiple iterations. This bugfix addresses cases where conflicting tag updates would prevent other tags from being fetched properly. Karthik notes it has undergone thorough review across several versions, suggesting it's likely well-vetted.

Both patches represent active work in the refs subsystem - one adding new functionality and one fixing edge cases in existing behavior. The fetch fix seems closer to completion while the ref backend work is still evolving.