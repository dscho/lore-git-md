This is a follow-up discussion on the documentation patch for `git-last-modified`'s depth handling behavior. Patrick Steinhardt questions whether the command's current root-level-only default behavior could be improved to automatically handle nested paths more intuitively.

The original patch (3/3 in the series) documented that `git-last-modified` by default only shows information about root-level paths, requiring explicit `--recursive` or `--max-depth` flags for nested paths. Patrick suggests this might be unnecessarily restrictive, asking whether the command could instead automatically detect when users want information about specific subdirectory files (like "sub/file") without requiring special flags.

This raises an interesting usability question about the command's design philosophy. The thread context shows this is part of a broader documentation effort where the command's behavior is being formally specified, suggesting this may be the right time to reconsider such design choices if warranted. No technical limitations preventing such a change are mentioned in the thread so far.