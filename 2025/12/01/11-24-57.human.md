Feature implementation for reference backend selection via URI syntax, now in its third iteration. This series introduces a `GIT_REF_URI` environment variable to specify reference storage locations, addressing GitLab's zero-downtime migration needs between files and reftable backends.

The core functionality remains unchanged from v2: the URI format `<backend>://<path>` allows directing ref operations to alternate locations, with validation for proper syntax. The main changes in v3 are cleanup-oriented - removing stale code, localizing error messages, and eliminating redundant checks. The implementation builds on the thread's consensus around using environment variables for temporary overrides, following Git's established patterns.

The series consists of two commits: one refactoring ref_store creation to support arbitrary directories, and another implementing the URI parsing logic. Test coverage verifies both successful operations and error cases. While the architectural approach is now settled, the thread's remaining open questions about URI format syntax (`://` vs `:`) and filesystem path handling don't affect this particular implementation.

This appears to be a well-considered solution to a real production need, with thorough consideration of edge cases and clear documentation of the migration workflow. The changes are focused and the test coverage appears comprehensive. The series seems ready for final review pending resolution of the broader URI format discussion in the thread.