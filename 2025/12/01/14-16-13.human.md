Test framework discussion: Phillip Wood responds to Jeff King's clar test framework experience report with technical suggestions and workarounds.

This email continues the discussion about the clar unit test framework's limitations that emerged during Jeff King's work on parsing utilities. Phillip offers several concrete suggestions to address the pain points Jeff identified:

1. For better error context, he suggests using `cl_failf()` or the `cl_assert_*_` variants that allow adding context, while acknowledging the framework's diagnostic messages have regressed from the previous test system.

2. He confirms the boilerplate issue for parameterized tests and notes the old framework's `TEST()` macro handled this more elegantly.

3. Regarding the `cl_assert_equal_i_fmt()` UB bug, Phillip points to an existing PR (#117) that would add type-agnostic assertions, though it's stalled awaiting review.

4. For the TAP output parsing issues, Phillip provides a patch to fix YAML formatting (changing `---` to `...`) that makes `prove` work correctly, though it still doesn't show error messages. He plans to submit this to clar.

5. On the `--verbose-log` question, Phillip is neutral but notes the parallel execution rationale no longer applies since clar tests run serially.

The tone is constructive and solution-oriented, with Phillip both validating Jeff's frustrations and offering specific technical remedies where possible. The exchange highlights real-world friction points in Git's unit testing infrastructure that may influence future test framework choices.