Follow-up in the `-z` flag discussion for `git-repo-structure`. Justin Tobler responds to Lucas Seiki Oshiro's patch adding `-z` as an alias for `--format=nul`, noting two minor issues:

1. The documentation formatting could be improved by adding a '+' separator line before the new `-z` description to match the style used in the `git repo info` section above it.

2. The test case meant to verify `-z` functionality accidentally still uses `--format=nul` instead of the intended `-z` flag.

The response confirms the technical approach of reusing the `parse_format_cb()` from `git-repo-info` is correct and maintains consistency. While the core functionality is uncontested (as established in prior thread discussion), this follow-up identifies two small polish items that should be addressed before finalizing the patch. The thread remains focused on implementation details rather than fundamental design, with consensus already reached about maintaining parity with `git-repo-info`'s behavior.