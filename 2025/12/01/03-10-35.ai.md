This is a test fix addressing a platform compatibility issue. The patch marks `gui--askyesno` as an expected failure when running tests with GIT_TEST_INSTALLED, following up on a similar fix from August 2025. The issue stems from `git-gui--askyesno` being installed by `make install` after commit e749c87e75, which breaks the test environment in nixpkgs builds.

The change is minimal - just adding `gui--askyesno` to the list of commands that are expected to fail in t/t1517-outside-repo.sh. The author notes this is likely a temporary fix and suggests a more fundamental solution might be needed to prevent recurring issues, but offers this patch as an immediate workaround to unblock nixpkgs builds for Git v2.52.0.

Key technical details for future reference:
- Tests affected: t1517-outside-repo.sh
- Commands now marked as expected failures: `gui--askyesno` joins existing exceptions like `gui` and `gui--askpass`
- Related commits: e749c87e75 (triggering the issue), c4cf8caadd (previous similar fix)
- Build system impact: nixpkgs compatibility
- Potential follow-up: more robust test modification to prevent similar issues