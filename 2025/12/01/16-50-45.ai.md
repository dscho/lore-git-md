This is a maintenance patch removing stale configuration values from Scalar's recommended settings, part of a series improving Scalar's config documentation and handling. The patch removes five config options that are either now Git defaults, Windows-specific, or obsolete.

The removed configs are: `core.preloadIndex` (now true by default), `core.fscache` (Windows-specific), `core.multiPackIndex` (now default), `credential.validate` (obsolete credential manager), and `pack.useSparse` (now default). Each removal comes with detailed justification in the commit message explaining why the setting is no longer needed. The patch updates tests to use `gui.gcwarning=false` instead of the removed `core.preloadIndex` checks.

The changes align with the series' goal of auditing and documenting Scalar's configuration approach while maintaining backward compatibility where needed. The patch follows earlier discussion about these removals and incorporates feedback from reviewers. Test adjustments demonstrate the config changes work as intended while preserving the verification functionality. This continues the thread's progression toward cleaner, better-documented config handling in Scalar.