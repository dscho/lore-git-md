Feature patch discussion follow-up regarding `-z` flag consistency for `git-repo-structure`. Justin Tobler responds to Lucas Seiki Oshiro's patch adding `-z` as an alias for `--format=nul`, noting two minor documentation and testing issues while generally approving the approach.

The response highlights that the documentation formatting should better match the prior section by adding a '+' separator line before the new `-z` description. It also points out that the test case appears to incorrectly check `--format=nul` again rather than actually testing the new `-z` flag as intended. Justin confirms the implementation approach of reusing the `parse_format_cb()` callback is correct and consistent with `git-repo-info`.

This email moves the thread forward by identifying small but important corrections needed before the patch can be finalized, while the core concept of adding `-z` support remains uncontested. The discussion remains focused on polishing the implementation details rather than debating the feature itself.