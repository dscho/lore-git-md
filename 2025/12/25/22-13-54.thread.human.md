Here's the human-readable summary:

This thread investigates severe performance issues with `git pack-refs --all` on large repositories (3M+ refs) stored on NFS, where Git takes 5+ minutes compared to JGit's 20 seconds. Martin Fick's flame graph analysis confirmed the bottleneck lies in mmap/NFS interactions, with 42% of time spent on page faults and 30% on object header parsing. While Patrick Steinhardt maintains that adopting the reftable backend is the proper long-term solution, the discussion has been at an impasse regarding short-term fixes.

Jeff King (Peff) has now identified a specific interoperability issue: JGit writes packed-refs files without marking them as sorted, forcing Git to repeatedly verify and potentially re-sort them. This creates significant overhead compared to JGit's simpler approach. Peff suggests testing whether manually marking these files as sorted would improve performance, and recommends JGit should properly implement these markers to enable Git's optimized binary search.

The thread now has two distinct but related performance angles: the fundamental mmap/NFS overhead (which would require deeper changes to address) and this newly identified packed-refs marker issue (which offers a more immediate optimization opportunity). While Patrick continues advocating for reftable adoption, Peff's analysis provides a concrete path to potentially improve the current situation without architectural changes.