Here is the updated dense AI thread summary:

Performance investigation of `git pack-refs --all` slowness on large repositories with high ref counts (~3M refs). Originating report from Martin Fick shows 5+ minute runtime vs JGit's 20s on same Gerrit server environment (~90M objects, 50GB data, NFS storage, Git 2.45.2). Key technical details:

1. Core issue confirmed through Martin Fick's latest test: Performance bottleneck is mmap'd packfile access rather than packed-refs handling, as shown by pre-reading pack file reducing runtime from 5+ minutes to under 20 seconds while pre-reading packed-refs has no effect. This validates Jeff King's earlier hypothesis about mmap/NFS interaction being the primary issue. Martin's tests maintain same environment characteristics (Gerrit server, NFS storage) while isolating variables.

2. Implementation behavior clarified: Git uses sequential access (not binary search) for packed-refs during write-out, with delays occurring between write operations rather than during writes themselves. Lock contention remains problematic as Git holds `packed-refs.lock` during slow packing operations, but root cause now clearly identified as mmap performance rather than ref handling algorithms.

3. NFS constraints and architectural implications: While JGit's better performance suggests mmap is the culprit, Martin Fick reiterates NFS remains non-negotiable for Gerrit's mirroring requirements. Discussion shifts focus from general optimization to specifically addressing mmap/NFS interaction, with Martin expressing interest in Jeff King's NO_MMAP build suggestion as potential runtime configuration rather than compile-time option.

4. Technical findings and pending work: Latest results eliminate packed-refs handling as bottleneck, confirming mmap'd packfile access as sole significant slowdown factor. Jeff King's NO_MMAP build test and Patrick Steinhardt's perf(1) flame graph approach remain pending but would provide definitive confirmation. Martin's production-validated tests provide strongest evidence yet of mmap/NFS interaction as root cause.

5. Open questions and next steps: Can mmap behavior be improved or worked around without compromising NFS requirements? Would runtime NO_MMAP configuration provide sufficient flexibility for Gerrit deployments? Discussion now focused exclusively on mmap/NFS interaction rather than broader ref handling, with Martin Fick's latest tests providing clear direction for further investigation. Thread status remains technically focused on diagnosis, with solution exploration pending definitive bottleneck confirmation through NO_MMAP testing or flame graphs.

Repository characteristics and constraints unchanged: high-refcount Gerrit deployments require NFS and experience lock contention during slow packing. Martin Fick's systematic testing methodology, combined with Jeff King's technical hypotheses and Patrick Steinhardt's diagnostic approaches, have progressively narrowed investigation to mmap/NFS interaction as the definitive performance bottleneck.