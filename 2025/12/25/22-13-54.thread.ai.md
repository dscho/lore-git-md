Here is the updated dense AI thread summary:

Performance investigation of `git pack-refs --all` slowness on large repositories with high ref counts (~3M refs) on NFS storage. Originating report from Martin Fick shows 5+ minute runtime vs JGit's 20s on Gerrit server environment (~90M objects, 50GB data, Git 2.45.2). Key technical details:

1. Core issue expanded with definitive findings:
   - Verification overhead from missing packed-refs markers confirmed as primary bottleneck (not mmap/NFS as initially suspected)
   - Martin Fick's test shows adding "fully-peeled" marker reduces runtime from 5+ minutes to under 4 seconds
   - JGit properly sorts refs but fails to write either "sorted" or "fully-peeled" markers

2. Current technical landscape updated:
   - Marker verification overhead dwarfs mmap/NFS issues in this specific case
   - "Fully-peeled" marker (not "sorted") proves to be the critical factor
   - Mmap/NFS interaction remains a secondary consideration

3. Diagnostic status:
   - Jeff King's hypothesis about marker verification confirmed
   - Patrick Steinhardt's NFS buffer size suggestion (1MB rsize/wsize) now lower priority
   - NO_MMAP build test suggestion no longer critical path

4. Implementation path clarified:
   - Clear JGit-side fix: add proper packed-refs markers ("fully-peeled" at minimum)
   - Git-side optimization opportunity remains for handling missing markers
   - Reftable transition (Patrick's recommendation) remains separate long-term path

5. Open questions refined:
   - Should Git implement fallback behavior when markers are missing?
   - What is the verification cost breakdown between "sorted" vs "fully-peeled" checks?
   - Does this finding change the priority of reftable adoption?
   - Should JGit's packed-refs writer be modified to include both "sorted" and "fully-peeled" markers?

Repository characteristics unchanged. The thread has shifted focus from mmap/NFS issues to packed-refs marker verification as the dominant performance factor. Martin Fick's empirical test provides a clear optimization path for JGit while leaving open whether Git should handle missing markers more gracefully. The performance picture is now complete with marker verification identified as the primary culprit, though the relationship between this finding and the previously identified mmap/NFS issues remains to be fully characterized.