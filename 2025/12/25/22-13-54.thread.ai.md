Here is the updated dense AI thread summary:

Performance investigation of `git pack-refs --all` slowness on large repositories with high ref counts (~3M refs). Originating report from Martin Fick shows 5+ minute runtime vs JGit's 20s on same Gerrit server environment (~90M objects, 50GB data, NFS storage, Git 2.45.2). Key findings from analysis:

1. Root cause identified as `should_pack_ref` check that verifies each ref points to valid object via `ref_resolves_to_object`, explaining strace-observed object directory accesses (200+ opens, 26K writes). Implementation detail of actually opening object files rather than just checking OIDs remains unclear.

2. NFS exacerbates performance impact - checking 37 pack files and indices over network creates significant overhead. Repacking repository first (reducing pack file count) dramatically improves ref-packing time, suggesting pack file count is major factor. Suggested mitigation: set `receive.unpackLimit=1` to minimize loose objects.

3. Core discrepancy with JGit's approach remains unresolved - JGit achieves order-of-magnitude faster performance without apparent object access. Open questions: can Git optimize object verification to avoid file opens? Is full object verification necessary for ref packing? Would alternate ref storage (reftable) avoid this issue?

Repository characteristics and impact remain relevant: high-refcount deployments like Gerrit servers experience lock contention during slow packing operations. Diagnostic clues from original report confirmed (repack-first speedup, JGit comparison). Thread status: root cause identified but fundamental optimization potential vs JGit still unexplored.