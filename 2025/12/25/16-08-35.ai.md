This is a feature inquiry about the `git branch` command's behavior. Sergey Organov questions why the `-v` (verbose) flag doesn't work with `--show-current`, noting that it would be useful to see verbose descriptions of the current branch similar to how `--list` behaves with `--show-current` currently only outputs the branch name without additional commit information.

The email raises a straightforward usability question about command behavior consistency but doesn't propose any specific changes or patches. It appears to be probing whether there's an intentional design reason behind this limitation or if it's an oversight that could be addressed. The inquiry seems well-motivated by practical use cases where users might want to quickly see both the current branch name and its associated commit information in one command.

Key technical details for future reference:
- Command in question: `git branch --show-current -v`
- Current behavior: silently ignores `-v` flag
- Desired behavior: show commit hash/subject like `--list -v` does
- No implementation details or patches proposed yet

This seems likely to prompt discussion about whether this is a bug (missing feature) or intentional design choice in the command's behavior. The thread may explore whether adding verbose output would create any backward compatibility issues or UI consistency problems with other `git branch` modes.