Here's a human-readable thread summary:

This thread began with Sergey Organov questioning why `git branch --show-current -v` doesn't display verbose commit information, unlike the `--list -v` combination which shows both branch names and their associated commits. The current behavior means users can't easily see the commit hash and subject for their current branch in one command, which seems like an inconsistency in the command's interface.

The discussion appears to be in early stages, with no responses yet addressing whether this is an intentional design choice or an oversight. The core question is whether adding verbose output to `--show-current` would improve usability without introducing backward compatibility issues or UI inconsistencies. This seems like a straightforward feature inquiry that might lead to either a documentation clarification or a small behavioral change in the command.