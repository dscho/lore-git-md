This is a question about Git's branch command behavior. Sergey Organov asks why `git branch --show-current` doesn't honor the `-v` (verbose) flag to show additional branch information, unlike `git branch --list` which does support this combination. The email suggests this would be a useful and natural extension of the command's functionality.

The question appears well-motivated and points to what seems like an inconsistency in the command's interface. There's no obvious technical reason given in the email why these flags couldn't work together, making this a reasonable thing to ask about. The tone is constructive, simply seeking to understand if there's a deliberate design decision behind the current behavior.

This could potentially lead to a discussion about making the flags compatible or documenting the rationale for keeping them separate. The question is just opening the conversation, so we'll need to see follow-ups from maintainers or other contributors to understand if there are hidden complexities or if this might be a candidate for improvement.