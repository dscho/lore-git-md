Classification: Feature enhancement for `git status` branch comparison

Harald Nordgren responds to maintainer feedback in this 7th iteration of the branch comparison feature for `git status`. The series appears to be nearing completion, with Harald agreeing to squash the commits since the total diff remains small. This follows a partial reversion from v6's unified `status.compareBranch` approach back to v5's `status.goalBranch` configuration while preserving v6's test coverage and performance optimizations.

The feature allows users to configure a goal branch (like mainline) to track divergence from, even when working on a different tracking branch. The implementation shows careful attention to performance with a single ref resolution point and consolidated memory management. Test coverage remains thorough with 12 scenarios in t6040-tracking-info.sh verifying various edge cases and output formats.

While the technical implementation seems solid, the email doesn't explain the rationale for reverting the configuration key name from v6's `status.compareBranch` back to v5's `status.goalBranch`. This appears to address maintainer concerns from earlier in the thread, though the specific reasoning isn't stated here. The squashing of commits suggests this may be the final form before potential inclusion.