This is a discussion response in the ongoing thread about adding a `--no-indicators` option to `git diff`. Phillip Wood raises several technical points about the proposed feature:

1. Notes that the patch still leaves leading spaces when removing the +/- indicators, questioning if those should be stripped as well for better copy-paste use cases. Also raises the question of how to handle the "\ No newline at end-of-file" marker.

2. Points out that existing `--output-indicator-*` options can already achieve similar results through an alias, though they can't completely remove indicators (only replace them with spaces). This suggests the functionality might be achievable without a new option.

3. The response maintains a constructive tone while challenging the necessity of the new option, continuing the thread's theme of debating whether this feature justifies core implementation versus being handled through existing options or external tools.

The discussion remains focused on design trade-offs rather than implementation details, with no new code proposed. Phillip's comments suggest the original justification for the option may need strengthening to demonstrate why existing alternatives are insufficient. The thread continues to explore whether this change provides enough unique value to warrant inclusion in core Git.