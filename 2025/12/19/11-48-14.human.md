This is a follow-up in the discussion about adding a `--no-indicators` option to `git diff`. Harald Nordgren responds to Collin Funk's earlier suggestion that `sed` could handle indicator removal, arguing that a built-in option would be more convenient than either the `sed` workaround or the existing verbose `--output-indicator-*` syntax.

The email provides more context about the author's personal use case - frequently copy-pasting code from diffs and wanting to manually remove the +/- indicators. While acknowledging it's not a major pain point, Harald positions this as a quality-of-life improvement for CLI-heavy workflows.

The response doesn't address Phillip Wood's more technical concerns from earlier in the thread about space handling and newline markers, focusing instead on the basic justification for the feature. The discussion remains at the conceptual level, with no new code changes proposed yet. The core question of whether this warrants a new core option versus using existing mechanisms remains open.