This thread has reached a clear conclusion with a documentation patch that standardizes terminology and clarifies behavior for `git worktree` commands. The v3 patch, now squashed into a single commit, finalizes the distinction between "prune" and "expire" operations, explicitly documenting that `--expire` only affects missing worktrees (those whose working directories no longer exist). The changes improve the `prune` subcommand's description to better explain its use cases.

The patch touches both the man page and in-code help text, bringing consistency to previously mixed terminology like "unused" vs "missing" worktrees. Eric Sunshine confirms this version addresses all prior feedback, and Junio Hamano's lack of further change requests indicates maintainer approval. The discussion built on earlier iterations where Junio suggested expanding subcommand descriptions and reviewers collaborated on wording. With no open questions remaining, this low-risk documentation improvement is ready for merging to `next`. The changes clarify user-facing behavior without modifying functionality, completing a clean cycle from problem identification through consensus to implementation.