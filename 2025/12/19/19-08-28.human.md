This is a follow-up in the ongoing discussion about adding a `--no-indicators` option to `git diff` and `git show`. Harald Nordgren responds to maintainer Junio Hamano's skepticism by reiterating their use case of copying React/TypeScript code blocks from diffs, where auto-formatting handles leading spaces in their workflow. They acknowledge the technical point about indicator removal needing extra work since existing `output-indicator-*` options don't support empty strings.

The email attempts to address Hamano's terminal emulator suggestion by noting they use iTerm2 on macOS and aren't aware of column selection functionality. While maintaining that manual indicator removal is possible, they frame this as a missed opportunity for Git to handle natively. The response ends with a request for clarification about Hamano's reference to GNU/BSD tools.

This continues the thread's pattern where the author keeps returning to their original workflow justification while not fully engaging with the technical alternatives (like `git restore -p` or `git show` with temporary files) that multiple contributors have demonstrated. The core tension remains between the author's desire for built-in functionality versus the community's view that existing tools adequately solve the problem.