This is a technical follow-up in the `git-history` feature series, specifically addressing implementation details of the `split` subcommand. Patrick Steinhardt responds to Phillip Wood's question about why the patch uses a forked `read-tree` process rather than direct `unpack_trees()` calls or writing the index to disk in-process.

The exchange reveals that the current implementation inherits a subprocess approach from other parts of the codebase that interface with `run_add_p()`, which requires an on-disk index. Steinhardt acknowledges this was likely an oversight and commits to changing it to a more direct approach.

Key technical points:
- The discussion focuses on `builtin/history.c`'s split implementation
- Current code uses `repo_git_path_replace()` to create a temporary index file
- Phillip Wood had questioned the subprocess overhead in v6 review
- Change will affect how the parent tree is processed during commit splitting

This is a minor implementation refinement in the larger context of the series, showing responsiveness to code review feedback while not altering the core functionality or user-facing behavior of the `split` command. The thread's main branch-handling compromise proposal remains unaffected by this technical adjustment.