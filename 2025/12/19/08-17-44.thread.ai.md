Here is the dense AI thread summary for this new patch:

New thread: [PATCH] diff: add --no-indicators option to suppress +/-/ indicators. Author proposes adding a `--no-indicators` flag to `git diff` and related commands that replaces the '+', '-', and ' ' indicators in the left margin with spaces while preserving color output. Primary use case is cleaner output when copy-pasting diffs. Implementation adds `diff_opt_no_indicators` callback to modify `output_indicators` array in diff options struct. Touches `diff.c` for option parsing and `t/t4000-diff-format.sh` for test coverage. Tests verify both indicator suppression and color preservation. Patch appears well-contained with clear motivation and implementation. No reviews or discussion yet as this is the initial submission.