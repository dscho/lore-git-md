This is a discussion thread about a proposed `--no-indicators` feature for `git diff`/`git show`. Chris Torek responds to Harald Nordgren's dismissal of his earlier workflow suggestion by providing a more concrete script implementation for comparing file versions.

Torek demonstrates how to extract both old and new versions of a file from a given revision into temporary files using basic shell scripting with `git show`. His approach uses `${rev}^:$path` to get the parent version and `${rev}:$path` for the current version, showing this requires only slightly more typing than the direct `git show` command Harald was using.

The response reinforces the ongoing theme in this thread that existing Git functionality (in this case `git show` with revision syntax) can solve the use case without new features. Torek positions this as a simple alternative to Harald's proposed `--no-indicators` flag, though he notes he doesn't script it since he rarely needs the functionality.

Key technical details include the `${rev}^` parent revision syntax and the straightforward file output redirection. This continues the thread's pattern of contributors suggesting alternative workflows while the original proposer maintains the need for their specific use case. The exchange highlights the tension between adding specialized features versus composing existing Git primitives.