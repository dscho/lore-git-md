This is a patch in the `git replay` behavior discussion, now at v4 with all technical feedback addressed. The patch modifies the experimental `git replay` command to drop commits that become empty during replay (when their changes already exist in the target branch), matching the behavior of `git rebase --reapply-cherry-picks --empty=drop` and `git cherry-pick --empty=drop`. 

The key technical aspects remain unchanged from previous versions: tree OID comparison in `replay.c` to detect empty commits, proper handling of branch pointer updates when tip commits are dropped, and thorough test coverage in t3650. The v4 changes are minor - removing the `--only` flag from test case commit creation (a personal workflow preference that wasn't essential to the test) and maintaining the `--ancestry-path --branches` test approach for broader coverage.

Junio C Hamano indicates he will apply the patch but raises an open question about the base branch (`ps/history` vs `master`) given ongoing discussions about whether this behavior change is too fundamental for the experimental phase. The patch author Phillip Wood has stated this should be the default behavior, with room to add an option later if users need to retain empty commits. All technical feedback has been incorporated, including punctuation improvements from Junio and test simplification from Elijah Newren.

The thread shows consensus on the implementation approach, with the only remaining uncertainty being about the broader context of when/how to merge these changes given `git replay`'s experimental status. The patch itself is technically complete and ready for inclusion once the base branch question is resolved.