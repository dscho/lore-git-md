This is a review response to the documentation patch standardizing "missing worktree" terminology for the `--expire` option in `git worktree` commands. Eric Sunshine agrees with the core change but suggests further refinements to the wording.

The reviewer endorses the patch's goal of consistently using "missing" rather than "unused" to describe worktrees whose working directories no longer exist. However, he proposes two specific wording improvements: (1) changing "expire" to "prune" in the man page documentation to better match the actual command behavior, and (2) making the same verb change in the `prune` command's help text. The suggested phrasing would be "prune missing worktrees older than <time>" in both cases.

The reviewer explicitly approves the unchanged wording for the `list` command's help text modification, which adds "missing" to clarify which worktrees get the 'prunable' annotation. This feedback maintains the patch's core documentation improvement while suggesting more precise action verbs that better reflect what the commands actually do. The review appears constructive and focused, with all suggestions directly related to the clarity of the documentation changes.