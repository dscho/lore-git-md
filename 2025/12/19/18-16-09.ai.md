This is the second patch in a documentation series clarifying terminology around `git worktree`'s `--expire` option. It implements the specific wording change suggested by Eric Sunshine in the v1 review, replacing "expire" with "prune" when describing the action taken on missing worktrees.

The patch makes two straightforward changes: updating the man page description and the help text in `builtin/worktree.c` to consistently use "prune" when referring to the removal of expired worktrees. This aligns with the command name (`git worktree prune`) and makes the behavior clearer to users. The changes are minimal - just two lines modified - and maintain all the existing functionality while improving consistency.

This follows directly from the v1 discussion where Eric Sunshine noted that "prune" better describes the actual operation being performed. The first patch in the series (already reviewed) standardized on "missing" terminology, while this one completes the "expire"->"prune" refinement. The changes remain tightly focused on documentation clarity without altering any behavior.

Key technical details for future reference:
- Modified files: Documentation/git-worktree.adoc and builtin/worktree.c
- Changes the verb from "expire" to "prune" in both the man page and command help text
- Maintains the same time-based qualification ("older than <time>")
- Keeps the distinction between `prune` and `list` subcommand behaviors
- Follows directly from Eric Sunshine's v1 review suggestion

The patch appears ready for final review, having incorporated the feedback from the previous round while maintaining the already-approved changes from patch 1/2.