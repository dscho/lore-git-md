This is a technical discussion response in the ongoing thread about the proposed `--snapshot` feature for `git commit`. Chris Torek expands on Phillip Wood's earlier critique, providing deeper technical analysis of the proposal's conceptual and implementation issues.

The email makes several key technical points about Git's internals that challenge the proposal's design: (1) commits already are efficient snapshots via tree objects, making file copies redundant, (2) the current implementation incorrectly assumes only changed files are staged, (3) the `cp`-based implementation wastes space compared to Git's object model, and (4) alternative approaches like `git commit-tree` or stash-like commits would better leverage Git's native capabilities if snapshots are truly needed.

Chris suggests the feature might be conceptually misaligned with Git's model, echoing Phillip's earlier concerns, and proposes that creating temporary branches could satisfy whatever underlying need motivated the snapshot proposal. The response maintains a fundamental question about the feature's purpose while offering concrete technical alternatives that would better integrate with Git's architecture if the use case were clarified.

Notable technical terms referenced include: `git commit-tree`, tree hash ID, `refs/snaps/`, garbage collection, and comparisons to `git stash`'s commit model. The discussion continues to lack any clear use case justification from the original proposer.