This is a discussion about a proposed `--no-indicators` feature for `git diff` and `git show`, where Chris Torek offers an alternative workflow solution.

Chris responds to Harald Nordgren's dismissal of his earlier suggestion by providing a more detailed script-based approach. He demonstrates how to use `git show` to extract both the old and new versions of a file from a commit into temporary files (`$path.old` and `$path.new`), which can then be compared directly. This avoids the need for diff indicators entirely while still allowing for version comparison.

The exchange highlights the ongoing tension in the thread between adding a new diff feature versus using existing Git capabilities. Chris's solution is presented as a practical alternative that doesn't require modifying Git's core diff functionality. His approach is straightforward and scriptable, though he acknowledges he doesn't use it frequently enough to bother scripting it himself.

This continues the thread's pattern where multiple contributors are suggesting alternative workflows that might make the proposed feature unnecessary, while the original author maintains their position without addressing the technical concerns raised. The discussion remains at an impasse regarding whether the new option provides sufficient unique value to justify its addition.