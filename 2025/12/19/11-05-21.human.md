This is a discussion response in the ongoing thread about the proposed `--snapshot` feature for `git commit`. Chris Torek weighs in with technical clarifications about Git's snapshot model and alternative approaches.

Chris explains that Git commits already serve as complete snapshots (via tree objects) rather than just tracking changed files, contrary to what the original patch's implementation might suggest. He notes the current implementation makes inefficient full file copies rather than leveraging Git's object storage. 

The response suggests several better approaches: using `git commit-tree` to create lightweight tree objects, storing them in special refs (like `refs/snapshots/`), or creating full commits off-branch similar to `git stash`. However, Chris agrees with Phillip Wood's earlier concern that the fundamental motivation for this feature remains unclear, suggesting that creating temporary branches might serve most use cases better.

The discussion continues to center around whether this feature conceptually belongs in Git at all, given existing mechanisms for snapshotting work. No new implementation details are proposed - the focus remains on questioning the feature's core premise and suggesting alternative approaches using Git's existing capabilities.