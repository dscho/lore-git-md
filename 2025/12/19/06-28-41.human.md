Bugfix follow-up for refs tracing debug output. Patrick Steinhardt reviews AZero13's one-character fix to properly dereference the `optimize_required` boolean pointer in `refs/debug.c`'s tracing output.

The patch corrects debug output that was previously showing "yes" regardless of the actual value by adding the missing pointer dereference. Steinhardt confirms the change is safe after verifying that all implementations of this callback already dereference the pointer unconditionally. This is a minor but clear improvement to debugging accuracy.

The review is straightforward with no objections raised - Steinhardt's analysis suggests the patch is ready to proceed. This continues the thread started by Greg Funni's original fix that Junio C Hamano forwarded, with the change now receiving positive feedback from another contributor familiar with the refs backend code.