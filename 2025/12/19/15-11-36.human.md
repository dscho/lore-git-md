This is a documentation refinement in response to Sam Bostock's patch about `git worktree` terminology. Eric Sunshine agrees with the core change to standardize on "missing worktrees" rather than mixing "unused" and "missing" in the `--expire` option documentation, but suggests two wording improvements to make the behavior even clearer.

The key suggestion is to replace "expire" with "prune" in the documentation strings when describing what happens to missing worktrees, since "prune" is the actual action being performed. The change to the `list` command's help text (which marks worktrees as prunable rather than actually pruning them) is considered correct as proposed.

The feedback is constructive and focused - Eric identifies specific lines where the wording could be tightened while agreeing with the overall direction of the patch. This appears to be early-stage review feedback on a straightforward documentation patch that hasn't yet seen broader discussion. The changes suggested would make the documentation more precise about the `--expire` option's actual effect on missing worktree paths.