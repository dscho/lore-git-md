Documentation patch fixing AsciiDoc formatting issues across multiple man pages. This is the first of two commits in a series that has already been merged, addressing technical markup problems while preserving git-blame usefulness by separating them from content changes.

The changes include: correcting backtick usage in `git-checkout.adoc` (replacing them with underscores for placeholder text), fixing option syntax in `git-worktree.adoc` (properly escaping `-f/--force`), and standardizing list formatting in `git-send-email.adoc` (converting tabs to spaces and adding `<path>` parameter notation for `--smtp-ssl-cert-path`). The SMTP validation steps are reformatted for consistency with other documentation.

This patch demonstrates Git's attention to documentation quality, with changes that improve both human readability and machine processing (benefiting translation workflows). The modifications are non-controversial technical fixes that follow established patterns, having already been reviewed and merged into the `next` branch. The commit organization reflects the project's preference for separating markup changes from content edits to maintain clear attribution history.