This is a documentation patch series (v2) clarifying the `--expire` option behavior in `git worktree` commands. The series builds on the v1 discussion where the core concept of standardizing on "missing" terminology was approved, with Eric Sunshine suggesting refinements to use "prune" rather than "expire" in certain contexts.

The v2 series consists of two patches: the first maintains the v1 changes (clarifying that `--expire` applies to missing worktrees), while the second implements the suggested wording change - replacing "expire" with "prune" in the help text to better match the actual behavior. The changes affect both the man page and builtin command help text.

Key technical changes:
- Documentation/git-worktree.adoc gets a one-word change from "expire" to "prune"
- builtin/worktree.c sees two help text modifications making the same terminology shift

The patch appears to fully address the review feedback from v1 while maintaining the original goal of clarifying when `--expire` applies. The changes are minimal and focused, touching only the documentation strings as intended. The series shows good responsiveness to review feedback while keeping the scope narrow.