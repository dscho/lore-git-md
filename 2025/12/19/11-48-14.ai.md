This is a follow-up in the discussion about adding a `--no-indicators` option to `git diff`. Harald Nordgren responds to Collin Funk's earlier suggestion that `sed` could handle indicator removal, arguing that a native Git solution would be preferable to external filtering.

The key points in this response are:
1. Acknowledges the existing `--output-indicator-*` options but argues they are "clunky" compared to a dedicated `--no-indicators` flag
2. Provides personal use case context - frequently copy-pasting from CLI diffs and wanting to manually remove indicators
3. Positions this as a quality-of-life improvement rather than a critical feature

The email doesn't introduce new technical details but reinforces the original motivation from a user experience perspective. It continues the thread's central debate about whether this convenience warrants a core Git feature versus being handled by existing options or external tools. No new code changes are proposed in this message.