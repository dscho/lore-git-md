Here's the updated human-readable thread summary:

This thread discusses how to best document Git's hash algorithm defaults when compiled with `WITH_BREAKING_CHANGES` (where SHA-256 becomes the default instead of SHA-1) ahead of Git 3.0. The initial patches took a straightforward approach, updating individual man pages with conditional blocks to show either SHA-1 or SHA-256 as the default depending on build configuration.

Junio Hamano then suggested a more maintainable approach using Asciidoc attributes to centralize the default hash algorithm documentation, avoiding duplication across files. Patrick Steinhardt expanded on this with a concrete proposal using a `default-hash-algo` attribute. Now, brian m. carlson has entered the discussion with a third potential approach - setting the default through build system variables (Makefile/Meson) rather than Asciidoc configuration.

The core goal remains unchanged: ensuring documentation accurately reflects the active hash algorithm while maintaining compatibility with both regular and breaking-changes builds. The discussion has evolved from implementation details to considering broader architectural choices about where this configuration should live. Brian indicates he may prototype the build-system approach, suggesting we'll see concrete alternatives to compare soon. All participants agree on the importance of getting this right for the SHA-256 transition, but differ on the most maintainable technical solution.