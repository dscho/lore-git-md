This is a documentation clarification in the ongoing discussion about the `-z` flag for `git-repo-structure`. Junio Hamano responds to Patrick Steinhardt's defense of the current documentation format by revisiting the thread's history about option grouping syntax.

The email clarifies that the core issue isn't about `-z` specifically being incompatible with `--format=nul`, but rather about the general principle that Git allows repeated format options where the last one wins. Junio references prior discussion (linking to a specific message) that established the correct documentation syntax for mutually exclusive options, showing the proper way to group format-related options with spacing around the `|` separator.

Key points:
- Reiterates Git's standard behavior for conflicting options (last one wins)
- References established documentation conventions from CodingGuidelines
- Confirms the thread's resolution about proper option grouping syntax
- The discussion remains focused on documentation presentation rather than implementation

This appears to be the final clarification needed before the patch can be finalized, as it confirms the proper way to document the option syntax while maintaining consistency with Git's existing conventions. The technical implementation remains unchanged from prior agreement in the thread.