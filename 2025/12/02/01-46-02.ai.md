Documentation patch updating the main `git` man page to reflect SHA-256 as the default hash algorithm when compiled with WITH_BREAKING_CHANGES. This is part 1 of a 4-patch series documenting hash algorithm defaults across multiple commands.

The patch conditionally updates the description of `GIT_DEFAULT_HASH` in git.adoc, showing "sha1" as default in normal builds and "sha256" when breaking changes are enabled. The change follows good documentation practices by keeping conditional text as complete sentences to aid localization. The modification is purely presentational - it doesn't alter any actual hash algorithm behavior, just ensures the documentation matches what the code already does when built with the breaking changes flag.

The patch follows the series' established pattern of mechanical documentation updates to prepare for Git 3.0's SHA-256 transition. The change is straightforward and unlikely to be controversial, though reviewers may want to verify the exact phrasing of the conditional blocks. The patch maintains backward compatibility by preserving the SHA-1 documentation path for normal builds while adding the SHA-256 variant.