Documentation patch updating `git-init` man page for SHA-256 default when compiled with breaking changes. This is the third in a 4-part series aligning documentation with the actual behavior when Git is built with `WITH_BREAKING_CHANGES`, where SHA-256 becomes the default hash algorithm instead of SHA-1.

The patch follows the same pattern as previous ones in the series, updating the `--object-format` option documentation in `git-init.adoc` to show the conditional default. It maintains the existing SHA-1 documentation path for normal builds while adding a parallel section showing SHA-256 as default when breaking changes are enabled. The change preserves full sentences for localization purposes, matching the approach taken in earlier patches for `git` and `git-index-pack` documentation.

The modification is purely presentational, ensuring the manual page accurately reflects the code's behavior without altering any actual functionality. The patch maintains backward compatibility by keeping the SHA-1 documentation intact for standard builds. Like the rest of the series, this appears to be a straightforward documentation update with no expected controversy, though reviewers may want to verify the exact phrasing of the conditional text blocks.

Key technical details include the conditional asciidoc directives (`ifndef::with-breaking-changes[]` and `ifdef::with-breaking-changes[]`) that control which default is shown, and the preservation of the `object-format-disclaimer.adoc` inclusion. The change affects only the `git-init` command's documentation of the `--object-format` option.