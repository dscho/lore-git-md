This is a technical discussion email in the ongoing `git-history` feature series, specifically addressing implementation details of the `reword` subcommand. The email responds to Elijah Newren's review feedback on commit collection logic and branch handling behavior.

Patrick Steinhardt addresses several technical points from the review:
1. Revises the commit collection logic to use `--ancestry-path` for clearer ancestry tracking and removes a manual commit reversal in favor of `rev.reverse`
2. Explains the current branch-only modification behavior is intentional (only checking reachability from HEAD) but leaves open the possibility of adding multi-branch support via a future flag
3. Acknowledges the need to preserve commit headers (like encoding) by switching from `commit_tree()` to `commit_tree_extended()`
4. Defends the current implementation approach that reimplements some replay logic rather than shelling out, citing future plans for commit reordering features

The discussion reveals key implementation details:
- The `collect_commits()` function's evolution
- Current limitations in branch handling (`verify_reachable_from_head()`)
- Pending technical debt around replay infrastructure integration
- The experimental command's design philosophy favoring simplicity over completeness initially

This email advances the thread by resolving some technical review points while establishing clear positions on others (particularly the branch handling behavior). It leaves the door open for future enhancements while maintaining the current branch-focused approach as a deliberate design choice rather than an oversight. The exchange highlights the tension between immediate usability and comprehensive functionality in this experimental feature.