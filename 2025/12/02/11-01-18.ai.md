This is a discussion about the default recursive behavior of the experimental `git-last-modified` command, continuing the documentation-focused patch series. The author Toon Claes responds to Patrick Steinhardt's question about whether the command should automatically handle nested paths like "sub/file"" by showing their last modification time.

Key technical points in the reply:
- References GitHub's internal `git-blame-tree` implementation which defaults to recursive behavior
- Notes the current upstream `git-last-modified` implementation doesn't default to recursive
- Explains the original reason was that `--max-depth` wasn't available when first implemented
- Suggests that with `--max-depth` now available (via a separate patch series), always-on recursion might be reasonable
- Proposes making recursive behavior default since the command is still marked EXPERIMENTAL

The email reveals an important implementation detail - the command's current behavior stems from its incremental development path (first implemented without depth controls, then had them added later). This raises the documentation-vs-implementation tension noted in the thread summary: should patch 3 document the current behavior or prompt redesign?

The discussion is now squarely focused on user experience design questions rather than pure documentation, with the author appearing open to changing the default behavior if reviewers agree. This may require revisiting the implementation before finalizing the documentation patches.