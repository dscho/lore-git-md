This is a refactoring patch in the ongoing `git-history` series, specifically addressing the add-patch/add-interactive separation work. The email is a response to Elijah Newren's question about code duplication between the new `run_add_p_index()` function and the existing `run_add_p()`.

The key technical points are:
- The new `run_add_p_index()` function enables in-memory index patching, a requirement for the `git-history` command's split functionality
- Only about 20 of the 90 lines are actually duplicated from `run_add_p()`, with the rest being new logic
- Patrick argues the duplication is justified because:
  - The shared portion is relatively small (~20 lines)
  - The overall logic is complex enough that deduplication wouldn't provide significant benefits
  - The functions serve sufficiently different purposes

The discussion reveals an architectural tension in the series - while the team is working towards clean separation between add-patch and add-interactive subsystems, some pragmatic duplication is being accepted where the abstraction cost outweigh the benefits. This patch represents an incremental step in the larger refactoring effort to support the history editing features while maintaining reasonable code organization.

The exchange highlights the careful balance being struck in this series between ideal architectural purity and practical implementation concerns, with Patrick opting for limited, justified duplication in this case rather than forcing an abstraction that might complicate the code.