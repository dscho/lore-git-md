This is a design discussion about the experimental `git-last-modified` command's default behavior regarding recursion and depth handling. Patrick Steinhardt responds to Toon Claes' proposal to make recursive behavior always-on while the command remains experimental.

Patrick agrees that recursive-by-default would make common use cases more intuitive, but suggests an alternative approach: defaulting to infinite max-depth for recursive mode and max-depth 0 for non-recursive mode. This would provide intuitive behavior in both cases - showing all entries recursively by default, while still allowing users to get file-specific results with `--no-recursive`.

The discussion reveals some subtle tradeoffs in the design space. Patrick considers but remains uncertain about whether max-depth of 1 might be better than 0 for non-recursive mode, as it would show directory listings without descending further. The thread continues to explore how to balance intuitive defaults with flexible control over traversal depth.

This email moves the discussion forward by proposing concrete implementation strategies for the recursive behavior, while acknowledging there are still open questions about the optimal depth defaults. The experimental status of the command provides flexibility to iterate on these design decisions.