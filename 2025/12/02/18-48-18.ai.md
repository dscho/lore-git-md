This is a documentation clarification in the ongoing `git-history` command series. Patrick Steinhardt responds to Elijah Newren's observation about the commit message's wording regarding which subcommands are implemented in the series. 

Patrick confirms that while earlier versions of the series included more subcommands, the current v6 series only implements two (`reword` and `split`), and the commit message should have been more precise about this. This exchange highlights the importance of accurate documentation even in commit messages, especially for a major new feature like `git-history`.

The thread context shows this is part of broader documentation accuracy concerns that have emerged as the implementation shifted from rebase to replay infrastructure. While this particular clarification is minor, it reflects the careful attention to detail needed when introducing new commands that will become part of Git's core interface. The response doesn't introduce new technical content but helps maintain the series' precision as it moves toward potential experimental merging.