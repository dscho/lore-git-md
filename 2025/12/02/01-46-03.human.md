This is a documentation patch in a series updating man pages to reflect SHA-256 as the default hash algorithm when Git is compiled with WITH_BREAKING_CHANGES enabled. It updates the second of four man pages, this time focusing on `git-index-pack`.

The patch modifies the `--object-format` documentation in git-index-pack.adoc to show SHA-256 as the default hash algorithm when breaking changes are enabled, while preserving the existing SHA-1 documentation path for normal builds. The change follows the same pattern as the first patch in the series, using conditional blocks to maintain both documentation paths while ensuring each remains a complete sentence for localization purposes.

The change is purely presentational - it doesn't alter any actual behavior of `git-index-pack`, just ensures the documentation matches what the code already does when compiled with breaking changes. The patch appears straightforward and well-scoped, continuing the series' consistent approach to documenting the SHA-256 transition for Git 3.0.