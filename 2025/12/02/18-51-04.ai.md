Feature implementation for the `git-history split` subcommand, part of the ongoing v6 series introducing history editing operations. This patch implements the core functionality for splitting commits, building on previous infrastructure work and addressing feedback from Phillip Wood.

The implementation now prompts for both commit messages when splitting (addressing earlier workflow concerns) and shares more code with the `reword` subcommand. Key technical details include:
- Uses `read-tree` with a temporary index file due to `run_add_p()` interface constraints
- Retains original authorship information when creating new commits
- Performs reachability checks to ensure the commit being split is in the current branch
- Handles pathspec filtering for partial splits
- Shares validation logic with the reword subcommand via refactored helpers

Phillip Wood's review identified several opportunities for code sharing between subcommands, particularly around commit validation and message handling. Patrick Steinhardt has addressed these by:
- Refactoring common commit validation into shared helpers
- Unifying commit message prompting behavior between split and reword
- Planning to extend these shared components for future subcommands like `drop` and `amend`

The implementation maintains the series' experimental approach while improving consistency across subcommands. Remaining considerations include:
- Potential future support for splitting into more than two commits
- Testing workflows for split operations
- Further code deduplication opportunities as more subcommands are added

This patch represents the final implementation piece of the core split functionality in the v6 series, with 432 lines of test coverage demonstrating its behavior. The response indicates Patrick has incorporated Phillip's technical feedback while maintaining the command's experimental nature.