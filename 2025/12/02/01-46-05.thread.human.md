This thread documents a series (4 patches) updating Git's documentation to reflect behavior changes when compiled with WITH_BREAKING_CHANGES, particularly around hash algorithm defaults. The final patch focuses on `git-show-index`, clarifying that SHA-256 becomes the default hash algorithm in this mode rather than SHA-1.

The changes are carefully implemented with AsciiDoc conditionals (`ifdef::with-breaking-changes[]`) to maintain separate documentation paths for different build configurations. The patch pays particular attention to keeping complete sentences within conditional blocks to support localization. The main change affects the `--object-format` option description, where the default value switches from SHA-1 to SHA-256 when breaking changes are enabled, while preserving the fallback behavior to repository configuration.

As a straightforward documentation update that aligns with the broader series, this patch appears uncontroversial and correctly implemented. Reviewers may want to verify consistency with the other documentation changes in the series, but no substantive technical concerns are evident.