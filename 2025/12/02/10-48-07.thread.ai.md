This 3-patch series by Toon Claes (with initial work by D. Ben Knoble) improves Git's Meson build system for cross-compilation scenarios, particularly targeting s390x but applicable broadly. The changes address specific cross-compilation issues through focused modifications to build configuration.

Technical changes:
- Added `subprojects/.wraplock` to `.gitignore` to exclude Meson wrapper lock files (Meson issue #14948)
- Modified `meson.build` to skip ICONV_OMITS_BOM detection when cross-compiling via `meson.can_run_host_binaries()` check
- Standardized on `can_run_host_binaries()` for `sysctl` detection test per Patrick Steinhardt's suggestion
- Maintained distinction between execution tests (`can_run_host_binaries()`) and filesystem operations (remaining `is_cross_build()` check for `fs.exists()`)
- Total changes: 4 lines modified across 2 files (1 line in first patch, 3 lines across second and third patches)

Build system context:
- All patches focus on Meson build configuration
- First patch handles version control hygiene for build artifacts
- Second and third patches fix test program execution during cross-compilation
- Changes remain minimal and focused on build system edge cases

Current status:
- Series complete with all cross-compilation issues addressed
- Patrick Steinhardt has reviewed and approved the final standardization
- No open questions or objections remain
- Changes are small in scope and clearly motivated by cross-compilation needs
- All patches ready for inclusion

The series represents a focused effort to make the Meson build system more robust in cross-compilation scenarios, with no runtime impact on native builds. The changes have been validated through discussion and implementation of Patrick's feedback, resulting in consistent handling of execution tests while maintaining appropriate distinctions for filesystem operations. The thread concludes with all patches reviewed and approved.