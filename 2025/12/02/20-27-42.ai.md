This is a follow-up discussion on the Meson build system improvements for cross-compilation. Patrick Steinhardt responds to Toon Claes's patch that fixed ICONV_OMITS_BOM detection during cross-compilation by using `meson.can_run_host_binaries()`.

Patrick notes there's another similar case in the codebase that currently uses `not meson.is_cross_build()` to guard a `compiler.run()` call. He suggests this should also be updated to use `can_run_host_binaries()` since it's the more robust approach (allowing for execution wrappers like QEMU) and is available in the minimum supported Meson version (0.61.0). The comment implies this would be a good candidate for a follow-up patch.

The discussion remains technical and focused on build system improvements, with Patrick validating the approach taken in the original patch while suggesting a related cleanup opportunity. No objections are raised to the current changes, and the tone remains collaborative. The thread continues the series' theme of making Meson builds more robust for cross-compilation scenarios.