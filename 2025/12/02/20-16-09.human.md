This is a feature addition to the `git replay` command, introducing a `--revert` mode to reverse commits on bare repositories. The patch series builds on previous discussion and implements the revert functionality as a distinct operation mode (alongside `--onto` and `--advance`), following the architectural pattern suggested by maintainers.

The implementation mirrors Git's existing revert behavior by swapping merge arguments to effectively reverse the diff direction. Revert commits follow standard conventions with "Revert" prefixes and original commit SHAs in messages, automatically handling "Reapply" cases when reverting already-reverted commits. The series includes thorough test coverage for various scenarios including conflicts, reflog messages, and edge cases.

Key technical points:
- Revert is implemented as a standalone mode (not a flag) for consistency with other operations
- Uses the same underlying merge machinery as cherry-pick but with swapped parent/commit arguments
- Shares revert message generation logic with sequencer.c via a new helper function
- Maintains atomic transaction support and conflict handling from existing replay infrastructure
- Includes comprehensive tests covering basic reverts, reapply behavior, conflicts, and error cases

The patch series appears well-constructed with clear commit messages, thorough documentation updates, and extensive test coverage. It addresses GitLab's need for server-side revert operations while fitting cleanly into Git's existing architecture. The implementation follows the consensus reached in earlier discussion threads regarding mode separation and behavior.