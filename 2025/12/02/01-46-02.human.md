Documentation patch updating the main Git manual page to reflect SHA-256 as the default hash algorithm when compiled with WITH_BREAKING_CHANGES. This is part 1 of a 4-patch series documenting hash algorithm defaults across several commands.

The patch updates the description of GIT_DEFAULT_HASH in git.adoc to conditionally show either "sha1" (normal builds) or "sha256" (breaking changes builds) as the default. The change follows good documentation practices by keeping the conditional text as complete sentences to aid localization. The modification is straightforward and matches the actual behavior when Git is compiled with the breaking changes flag enabled.

This is purely a documentation update with no code changes. The patch appears well-formed and properly scoped to its documentation purpose. As noted in the thread context, this series prepares for version 3.0 where SHA-256 will become the standard default, though this particular patch only deals with the transitional breaking changes build mode.