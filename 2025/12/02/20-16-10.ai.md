Refactoring patch in the `git replay --revert` series, extracting revert message formatting logic into a shared helper function. This is the first of two patches in v2 of the series, focusing on code reorganization to support the new `--revert` functionality while maintaining consistency with existing sequencer behavior.

The patch moves the revert message generation logic from `do_pick_commit()` into a new `sequencer_format_revert_header()` function in sequencer.c, making it available for reuse by the upcoming `git replay --revert` implementation. The function handles both standard reverts (prepending "Revert") and revert-of-revert cases (using "Reapply"), while preserving the special handling for `commit_use_reference` cases. The implementation maintains all existing behavior including the guard against nested reverts ("Revert \"Revert\"") and the standard "This reverts commit" trailer.

Key technical details include:
- New public function in sequencer.h with clear documentation
- Preserved logic for commit_use_reference special case
- Identical output format to existing implementation
- No functional changes to message generation
- Prepares for shared use by builtin/replay.c in next patch

The change is purely structural - splitting the code to eliminate duplication while maintaining all existing behavior. The patch follows the thread's architectural direction by keeping revert message handling consistent across commands. Test coverage is implied through existing sequencer tests rather than new cases, as this is a refactoring without behavior change. The implementation appears clean and well-contained, with clear documentation of the new API.