Documentation patch updating `git-index-pack` man page for SHA-256 default in breaking changes mode.

This is the second patch in a series updating documentation to reflect that SHA-256 becomes the default hash algorithm when Git is compiled with WITH_BREAKING_CHANGES. The patch modifies the `git-index-pack` man page to show the conditional default - SHA-1 for normal builds, SHA-256 when breaking changes are enabled. 

The change follows the same pattern as the first patch in the series, updating the description of the `--object-format` option to include conditional text blocks. The documentation maintains both paths (with and without breaking changes) while keeping complete sentences for localization purposes. The actual behavior being documented is not new - this just ensures the docs match what the code already does when compiled with breaking changes.

The patch is mechanical and well-scoped, only touching the documentation file. It follows the same careful approach as the rest of the series in handling the conditional documentation, ensuring backward compatibility while preparing for Git 3.0's SHA-256 transition. Reviewers should verify the exact phrasing matches the intended behavior, though the change appears straightforward.