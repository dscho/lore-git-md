Documentation patch updating `git-init` man page for SHA-256 default when compiled with breaking changes. This is the third in a 4-part series aligning documentation with the actual behavior when Git is built with `WITH_BREAKING_CHANGES`, where SHA-256 becomes the default hash algorithm instead of SHA-1.

The patch follows the same pattern as previous ones, updating the `--object-format` option description in `git-init.adoc` to conditionally show SHA-256 as the default when breaking changes are enabled. The change maintains backward compatibility by keeping the SHA-1 documentation path for normal builds, and carefully structures the conditional text in complete sentences to aid localization.

Like the rest of the series, this is a mechanical documentation update that doesn't change any runtime behavior - it just ensures the man page matches what the code already does when compiled with breaking changes. The patch appears straightforward and uncontroversial, following the established pattern from the earlier patches in the series. One remaining patch will complete the documentation updates for this behavior change.