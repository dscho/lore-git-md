This is a cover letter for a 2-patch series addressing cross-compilation issues in Git's Meson build system. The changes focus on improving the experience when cross-compiling for s390x architecture.

The first patch adds `subprojects/.wraplock` to `.gitignore` to prevent Meson's wrapper lock files from being tracked. The second patch modifies the ICONV_OMITS_BOM detection logic to only run when possible during cross-compilation, avoiding build failures when the test program can't execute on the build host.

The changes are narrowly focused on Meson build system quirks encountered during s390x cross-compilation. The patches are small (2 lines changed total) and appear to be straightforward technical fixes rather than controversial changes. The cover letter provides clear context about the motivation (cross-compiling for s390x) but doesn't go into deep technical justification for the changes.

Key technical details for future reference:
- Files touched: `meson.build` and `subprojects/.gitignore`
- Specific change in `meson.build`: modifies `ICONV_OMITS_BOM` detection logic
- New ignore pattern: `subprojects/.wraplock`
- Build system context: affects Meson configuration during cross-compilation
- Architecture context: specifically encountered on s390x but likely affects other cross-compile scenarios