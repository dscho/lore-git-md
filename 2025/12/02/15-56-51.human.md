Documentation modernization patch updating advice messages to prefer `git help` over `man`. This is version 2 of a change that replaces `man git check-ref-format` references with `git help check-ref-format` in branch-related error messages, following up on feedback from Junio Hamano.

The patch makes three improvements: (1) switches to `git help` for better cross-platform compatibility and to stay within Git's ecosystem, (2) uses single quotes for command references as per convention, and (3) updates test syntax to use `{SQ}` for better readability. The commit message has been expanded in v2 to better explain the platform compatibility rationale and note that `git help` respects the user's `help.format` configuration.

This is a straightforward change that aligns with Git's documentation modernization efforts. The implementation is minimal (5-line diff across 3 files) and non-disruptive, just updating advice text and test expectations. The thread shows consensus on both the approach and rationale, with no outstanding questions. The patch appears ready for inclusion.