D. Ben Knoble started this thread with a 2-patch series improving Git's Meson build system for cross-compilation, particularly on s390x architecture. The first patch excluded Meson wrapper lock files from version control, while the second addressed ICONV_OMITS_BOM detection during cross-compilation. 

Toon Claes has now picked up the second patch, maintaining its technical focus on cross-compilation scenarios. The change prevents the build system from attempting to execute cross-compiled test binaries on the build host when detecting iconv behavior, wrapping the test with `meson.can_run_host_binaries()`. This is a minimal but important fix for a specific build system edge case that doesn't affect runtime behavior.

The series remains focused and uncontroversial, with both patches addressing clear technical needs in the Meson build configuration. The changes are small in scope - just 3 lines modified across 2 files - and specifically target cross-compilation scenarios while maintaining existing behavior for native builds. No substantive discussion or objections have been raised about either patch.