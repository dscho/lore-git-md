Documentation patch updating `git-show-index` manpage to reflect hash algorithm changes in WITH_BREAKING_CHANGES mode.

This is the final patch in a series (4/4) that updates documentation to account for behavior changes when Git is compiled with WITH_BREAKING_CHANGES. The patch modifies the `git-show-index` manpage to correctly state that SHA-256 becomes the default hash algorithm in this mode, rather than SHA-1. The changes are properly conditionalized using AsciiDoc directives (`ifdef::with-breaking-changes[]`) to maintain separate documentation paths for the two build configurations.

The patch carefully structures the documentation changes to keep complete sentences within each conditional block, which the commit message notes is important for localization purposes. The modification affects the description of the `--object-format` option, where the default value changes from SHA-1 to SHA-256 when breaking changes are enabled. The patch maintains all other aspects of the option's behavior, including fallback to repository configuration via `extensions.objectFormat`.

The change appears straightforward and correctly implemented, with no obvious technical issues. As a documentation update for a specific build configuration, it's unlikely to be controversial, though reviewers might want to verify it aligns with other similar documentation changes in the series.