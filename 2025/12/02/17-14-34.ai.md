This is a discussion about behavior design for the experimental `git-last-modified` command, specifically around its depth handling and recursive behavior. The email continues the thread's shift from documentation to fundamental UX decisions.

Patrick Steinhardt responds to Toon Claes' proposal to make recursive behavior the default while the command remains EXPERIMENTAL. Patrick suggests two potential approaches: either defaulting to infinite max-depth with recursive behavior, or combining recursive-by-default with a max-depth of 0 for non-recursive cases. He also considers but remains uncertain about using max-depth=1 for non-recursive mode.

Key technical points raised:
- The current non-recursive default stems from historical implementation constraints (lack of `--max-depth`)
- GitHub's `git-blame-tree` implementation defaults to recursive behavior
- The discussion now centers on whether to document current behavior or redesign first
- Patrick proposes specific depth control combinations that could make common cases more intuitive

The email highlights the tension between documenting existing behavior versus redesigning it while the command is still EXPERIMENTAL. Patrick appears to favor making recursive behavior the default but offers alternative approaches for consideration. This continues the thread's exploration of how to balance technical constraints with intuitive UX for path handling in the command.