This is a follow-up in the thread about adding `-z` flag support to `git repo structure`. Lucas Seiki Oshiro responds to Justin Tobler's review comments, confirming two key points: first, that the patch was based on an unmerged branch (lo/repo-info-all) which has since landed in master, and second, acknowledging a test case needs updating to use `-z` instead of `--format=nul`.

The exchange shows the patch is in its final polishing stages, with the technical approach already agreed upon and now just needing minor test/doc fixes. Lucas confirms the implementation's consistency with `git-repo-info` and the straightforward nature of sharing the format parser between commands. The tone suggests this will likely be ready for inclusion after these last tweaks.