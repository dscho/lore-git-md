This is a build system patch continuing the discussion about cross-compilation fixes in Git's Meson setup. Patrick Steinhardt responds to Toon Claes's patch that guards an iconv BOM detection test with `meson.can_run_host_binaries()` when cross-compiling.

Patrick agrees with the approach but suggests expanding it - he notes there's another similar check elsewhere in the build system that currently uses `meson.is_cross_build()` instead. He points out that `can_run_host_binaries()` is the more robust solution since it accounts for cases where the host might use emulation (like QEMU) to run target binaries. He confirms the function has been available since Meson 0.55, well below Git's minimum requirement of 0.61.0.

The exchange shows technical alignment on the cross-compilation fix while identifying an opportunity to make the solution more comprehensive. Patrick's tone is constructive, treating this as a natural extension of the current patch series rather than a criticism. The discussion remains focused on build system technicalities without introducing any controversy.