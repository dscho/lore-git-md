This is a follow-up discussion in the macOS Homebrew configuration thread, specifically questioning the design approach of patch 1/2 in René Scharfe's v3 series. The email focuses on whether the fine-grained `USE_HOMEBREW_*` flags (for gettext and msgfmt) are necessary in the proposed build system changes.

The thread context shows this is part of a larger effort to address iconv(3) conversion issues on macOS 15+ by improving package manager integration, with Homebrew being the primary focus. The v3 series already implemented explicit Homebrew support with architecture-aware paths and component-specific flags.

René Scharfe is now questioning his own design, specifically whether the individual `USE_HOMEBREW_GETTEXT` and `USE_HOMEBREW_MSGFMT` flags add unnecessary complexity compared to a simpler `NO_HOMEBREW` toggle. This reflects ongoing tension in the build system between configurability and maintainability.

The technical context from the thread shows these flags were added to give precise control over Homebrew components while maintaining backward compatibility. However, the question suggests they may be over-engineering the solution, especially since the primary motivation was fixing t3900 test failures related to libiconv (handled in patch 2/2).

This email doesn't propose concrete changes but signals potential design reconsideration that could affect how Homebrew integration is structured in the final solution. The thread's open questions about package manager support scope and build system understandability become directly relevant to this specific implementation choice.