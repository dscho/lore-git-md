This is the final patch in Justin Tobler's series adding object size reporting to `git repo structure`, completing the table format display with disk size metrics. The patch mirrors the structure of the inflated size display, adding parallel columns for disk usage by object type (commits, trees, blobs, tags) under a new "Disk size" section.

The implementation extends the existing `stats_table_setup_structure()` function to calculate and display disk sizes using the same `stats_table_size_addf()` helper. The test suite is updated to verify both empty and populated repository cases, with special handling for tag sizes due to `--disk-usage=human` formatting quirks. The test helper `object_type_disk_usage()` is enhanced to support both raw and human-readable output modes.

Key technical points:
- Completes the metrics display by adding disk sizes alongside existing count and inflated size columns
- Uses the same `disk_sizes` struct fields introduced earlier in the series
- Maintains consistent formatting with the existing table structure
- Handles edge cases in tag size display where human-readable` output differs from other types
- Extends test coverage to verify disk size reporting matches `git rev-list --disk-usage` output

This concludes the core functionality of the series, having now exposed all collected metrics (counts, inflated sizes, disk sizes) through all output formats (table, keyvalue, nul). The implementation follows the established pattern of cleanly separating data collection from presentation while maintaining rigorous test coverage.