This is a refactoring patch in Justin Tobler's v4 series adding object size reporting to `git repo structure`. The patch splits out byte-formatting logic from `strbuf_humanise()` into a new `humanise_bytes()` function to support upcoming table output formatting needs.

The key change extracts the byte scaling and unit selection logic into a standalone function that returns the numeric value and unit string separately, rather than formatting them together. This enables callers to handle the components independently, which will be needed for proper column alignment in the repo structure output. The patch maintains all existing behavior while adding flexibility for the new use case.

Technical details include:
- New `humanise_bytes()` function in strbuf.[ch] that returns value/unit pairs
- Introduction of `HUMANISE_RATE` flag enum for rate vs size unit variants
- Modified `strbuf_humanise()` now acts as a wrapper around the new function
- Test helper adjusted to avoid translation conflict with new plural forms
- 74-line change across strbuf.c (majority being moved code with minor adjustments)

The change is well-contained and clearly motivated by the series' needs, with careful attention to maintaining existing behavior and translation support. It follows the pattern seen elsewhere in the series of preparatory refactoring before adding new functionality. The patch appears ready for inclusion, having evolved through multiple rounds of review.