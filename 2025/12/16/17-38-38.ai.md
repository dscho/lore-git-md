This is a refactoring patch in Justin Tobler's v4 series adding object size reporting to `git repo structure`. It introduces human-readable formatting for count values in the table output format.

The patch extracts count formatting logic into a new `humanise_count()` function in strbuf.c, similar to the existing `humanise_bytes()` but for simple counts rather than byte sizes. The function produces scaled values with SI prefixes (k, M, G) for large numbers, keeping raw counts for small values. The table output format is updated to properly align these formatted values with their unit indicators.

Key technical changes:
- New `humanise_count()` API in strbuf.[ch] that returns value and unit strings separately
- Added `unit_col_width` to `stats_table` struct to handle unit display
- Updated table formatting logic to properly align values and units
- Test updates to verify the new human-readable output format
- Maintains existing behavior for keyvalue and nul formats

The changes are well-contained (91 lines added/modified across 4 files) and maintain backward compatibility while improving readability. The patch follows the series' pattern of preparatory refactoring before adding the actual size measurement functionality in later patches. Test coverage appears thorough, with updated expectations for both small and large count scenarios.

This refactoring cleanly separates concerns between counting logic and display formatting, setting up for the subsequent size reporting features while improving the existing count display. The implementation handles translation concerns (with gettext markers for unit prefixes) and maintains consistent alignment in the table output.