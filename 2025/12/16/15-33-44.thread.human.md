This thread covers a series of preparatory changes for Windows symlink support, co-authored by Johannes Scharfe and Karsten Blees with Johannes Schindelin (Git for Windows maintainer) contributing fixes. The five-patch series addresses technical prerequisites across mingw compatibility, config parsing, strbuf utilities, and path handling, resulting in a net reduction of 22 lines across 6 files.

The first patch improved `git rev-parse --is-inside-work-tree` behavior in symlink-containing paths by enhancing `mingw_getcwd()` with better symlink resolution and fallback behavior. Subsequent patches addressed initialization ordering (ensuring `core.symlinks` is parsed early on Windows), optimized `strbuf_readlink()`, removed PATH_MAX limitations, and improved cross-platform path separator handling in `trim_last_path_component()`.

In a recent follow-up discussion, Patrick Steinhardt confirmed the technical soundness of the second patch's approach to early config parsing during `git init`. The change from `platform_core_config` to `git_default_core_config` ensures core settings (particularly `core.symlinks` on Windows) are available before template copying, while maintaining all existing functionality through Git's config parsing hierarchy. This validation confirms the patch solves the initialization ordering problem without introducing regressions.

The series has now addressed all identified prerequisites for upcoming Windows symlink support, with each change being a focused, incremental improvement to core functionality. Johannes Schindelin's sign-offs throughout confirm the Windows-specific aspects are properly handled.