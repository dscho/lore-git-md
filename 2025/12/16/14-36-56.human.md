This is a follow-up discussion in the documentation thread about `git rev-list --filter` behavior. Justin Tobler asks Patrick Steinhardt about potential use cases for the current default behavior where explicitly provided objects bypass filters unless `--filter-provided-objects` is specified.

The exchange reveals this was originally a backward compatibility compromise rather than an intentional design choice. Justin questions whether there are any legitimate use cases for this behavior, noting it seems more like a "sharp edge" than a useful feature. The tone suggests this might be exploring whether the behavior could be changed in the future, though the thread has already established the documentation will accurately reflect the current implementation.

The discussion remains exploratory rather than proposing concrete changes, with Justin seeking to understand the historical context behind what all agree is a suboptimal design. The core documentation patch itself remains uncontroversial and queued for merging.