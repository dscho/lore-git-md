This is a refactoring patch in the submodule gitdir path encoding series, specifically addressing validation logic consolidation.

Patrick Steinhardt is reviewing Adrian Ratiu's patch that centralizes submodule gitdir validation within `submodule_name_to_gitdir()`. The change moves validation checks that were previously scattered across the codebase into this central function, ensuring all paths (including future config-provided ones) get validated. The patch maintains two external validation calls where they're still needed to prevent parallel clone races.

The refactoring simplifies the API by making validation mandatory whenever getting a gitdir path, reducing the chance of callers forgetting to validate. This is part of the larger effort to handle submodule gitdir paths more robustly, particularly for the new `extensions.submodulePathConfig` feature. The review confirms the changes are correct and properly handle the validation call sites.

Key technical points for future reference:
- `submodule_name_to_gitdir()` now always validates paths
- Two remaining external `validate_submodule_git_dir()` calls kept for race prevention
- Prepares for config-provided gitdir paths coming in later patches
- Maintains same validation safety while reducing redundancy

The change appears straightforward and well-justified, with the reviewer confirming the correctness of the call site removals. This is a clean architectural improvement that tightens up the validation model for the larger submodule path encoding work.