This is a refactoring patch in Justin Tobler's series adding object size reporting to `git repo structure`. The patch splits out byte-formatting logic from `strbuf_humanise()` into a new `humanise_bytes()` function that returns the numeric value and unit prefix as separate strings.

The change prepares for upcoming patches that will display human-readable byte sizes in aligned columns in the repo structure output. The new function provides more flexibility by separating the numeric value from its unit, while maintaining all the existing translation support for byte units (including proper plural forms and rate/size variants).

The patch touches core string formatting code in strbuf.c, adds a new public function declaration in strbuf.h, and makes a minor adjustment to a translation marker in the test helper code. The implementation looks thorough, preserving all existing behavior while making the logic more reusable for the column-aligned output needs of the repo structure command.

This is a clean internal refactoring that doesn't change user-visible behavior but enables the next steps in the series. The careful handling of translation strings suggests good attention to i18n requirements that were discussed earlier in the thread.