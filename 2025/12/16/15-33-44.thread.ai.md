This thread concerns a 5-patch series preparing for Windows symlink support, co-authored by Johannes Scharfe and Karsten Blees with Johannes Schindelin (Git for Windows maintainer) contributing fixes. The series addresses technical prerequisites across mingw compatibility, config parsing, strbuf utilities, and path handling, with a net reduction of 22 lines across 6 files.

The first patch fixed `git rev-parse --is-inside-work-tree` failures in symlink-containing paths by modifying `mingw_getcwd()` to resolve symlinks via `GetFinalPathNameByHandleW`. The second ensured early parsing of `core.*` config settings before template copying during `git init`. The third optimized `strbuf_readlink()` to avoid redundant system calls. The fourth and fifth patches focused on path handling improvements, with the key technical discussion centering on removing PATH_MAX limitations in `strbuf_readlink()`.

Patrick Steinhardt initially raised concerns about potential undefined behavior with unbounded growth in patch 4/5, questioning whether Windows imposes natural size limits that could provide safe bounds. The latest patch in this subthread has now addressed these concerns, with sign-offs from both Karsten Blees and Johannes Schindelin confirming the approach is sound for Windows. Junio Hamano reviewed the unbounded growth implementation and confirmed its safety, noting the ERANGE retry mechanism prevents infinite loops on broken platforms. This change follows Git's historical approach to PATH_MAX issues while maintaining robust error handling.

The series has now completed its technical discussion with all patches reviewed and signed off by relevant maintainers. The preparatory work for Windows symlink support is ready, having addressed mingw compatibility, config timing, path utilities, and the final PATH_MAX question through focused improvements to core functionality. Johannes Schindelin's participation throughout ensures Windows-specific considerations were properly handled.