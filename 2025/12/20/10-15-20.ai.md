This is part of the submodule gitdir path encoding series, specifically patch 3/11 implementing a helper command for the new path resolution system. The patch adds a new `git submodule--helper gitdir` command that exposes the internal `submodule_name_to_gitdir()` path computation logic to users and tests.

The new command takes a submodule name and prints its computed gitdir path, serving as both a debugging tool and a building block for the upcoming configuration system. This follows the series' architectural approach of centralizing path resolution logic in `submodule_name_to_gitdir()`, which now includes validation checks from previous patches. The implementation is straightforward - it wraps the existing function in a command-line interface that outputs the resolved path.

Key technical details for future reference:
- New subcommand `gitdir` added to `builtin/submodule--helper.c`
- Uses the centralized `submodule_name_to_gitdir()` function
- Outputs the computed path for a given submodule name
- Prepares for the next patch's gitdir configuration system
- Based on earlier work by Brandon Williams
- Files touched: `builtin/submodule--helper.c`
- Functions modified: adds new `module_gitdir()`
- Related to config key `submodule.<name>.gitdir` (coming in next patch)

The patch appears well-integrated with the series' goals of making submodule gitdir paths more flexible and robust. It's a small but important plumbing piece that enables both user inspection and the upcoming configuration features.