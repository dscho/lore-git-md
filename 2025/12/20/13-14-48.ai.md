This is a feature proposal discussion about adding `--no-indicators` to `git diff` and `git show`. Johannes Sixt (Hannes) enters the thread with a new alternative suggestion, proposing `git difftool` as a solution to Harald Nordgren's use case of copying React/TypeScript code blocks from diffs. 

Hannes notes that graphical diff tools like meld (on Linux) and WinMerge (on Windows) make it easy to transfer individual lines or hunks between versions, which directly addresses Harald's stated workflow of recovering discarded code blocks. This suggestion builds on previous alternatives proposed in the thread (`git restore -p`, `git show` with temporary files) but introduces a GUI-based approach that avoids the indicator character issue entirely.

The email doesn't engage with the specific technical challenges of the `--no-indicators` proposal (newline markers, space handling) but instead offers a completely different solution path. This continues the thread's pattern where multiple established Git features are presented as alternatives to the proposed new flag, while the original proposal's technical details remain unresolved.

Key technical points for future summarization: Hannes suggests `git difftool` configured with meld/WinMerge as a GUI alternative to the proposed text-based solution. This maintains the first/last version comparison capability while providing visual line/hunk selection. The suggestion doesn't require any Git changes and works with existing configurations, contrasting with the proposed `--no-indicators` flag that would need new core functionality. The thread now has four distinct alternative workflows proposed (terminal column selection, `git restore -p`, `git show` with temp files, and now `git difftool`), all claiming to solve the same use case without new features.