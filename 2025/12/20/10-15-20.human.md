This is part 3 of an 11-patch series implementing submodule gitdir path encoding via a new extension. The series aims to prevent filesystem conflicts in submodule gitdirs and enable custom layouts through runtime configuration.

This patch adds a new `git submodule--helper gitdir` command that exposes the internal `submodule_name_to_gitdir()` function. The helper computes and prints the gitdir path for a given submodule name, which will be useful for both users and tests. The next patch in the series will add gitdir configuration support, and this helper will also be used to validate user-configured paths.

The implementation is straightforward - it adds a new subcommand that takes a submodule name as input, computes the gitdir path using the existing `submodule_name_to_gitdir()` function, and prints the result. The patch is well-contained and follows the series' architectural approach of centralizing path resolution logic.

This appears to be a solid technical building block for the larger feature. The commit message clearly explains the motivation and how this fits into the broader series. The implementation is clean and minimal, adding just what's needed at this stage.