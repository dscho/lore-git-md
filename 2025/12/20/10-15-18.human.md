This is the first patch in the v7 series implementing submodule gitdir path encoding, focusing on a refactoring change in `submodule--helper`.

The patch updates `add_submodule()` to use the centralized `submodule_name_to_gitdir()` helper function instead of hardcoding the modules path. This change aligns with the series' goal of consistent path handling and fixes test failures that occurred with the old hardcoded approach. The modification also includes a scope cleanup by moving the `submod_gitdir_path` variable declaration inside the relevant code blocks.

As part of the larger effort to enable path encoding for submodule gitdirs, this foundational change ensures all submodule operations will properly handle encoded paths through the unified helper function. The patch appears straightforward and well-contained, with no visible downsides to the refactoring. It's a typical preparatory change we often see in multi-patch series - cleaning up call sites before introducing new functionality.