This is a refactoring patch in the submodule gitdir path encoding series, specifically addressing path resolution in `submodule--helper`. 

The patch modifies `add_submodule()` in `builtin/submodule--helper.c` to use the centralized `submodule_name_to_gitdir()` helper function instead of hardcoding the modules path. This aligns with the series' goal of consistent path handling and fixes test failures that occurred with the old approach. The change involves:
1. Replacing direct path construction (`".git/modules/%s"`) with the helper call
2. Switching from heap-allocated strings to `strbuf` for path storage
3. Narrowing variable scope where possible

The modification maintains identical functionality while preparing for the new path encoding system. This is a straightforward technical cleanup that follows the architectural direction established in the series' cover letter, where `submodule_name_to_gitdir()` serves as the single source of truth for path resolution. The patch demonstrates good hygiene by properly releasing resources via `strbuf_release()` and keeps the diff focused (13 lines changed total).

This change is part of the foundational work for patch 1-6 in the series, establishing consistent path handling before the later patches introduce the new encoding behavior. The commit message clearly explains both the immediate fix (test failures) and the architectural alignment purpose.