This is a response in the ongoing discussion about adding a `--no-indicators` flag to `git diff` and `git show`. Johannes Sixt suggests an alternative approach to Harald Nordgren's use case of extracting code snippets from diffs.

The email proposes using `git difftool` with visual merge tools like meld or WinMerge as a more effective solution for moving code between versions, rather than modifying the diff output format. This continues the thread's pattern of contributors suggesting existing Git functionality that could address the original need without adding new features.

The response is brief and practical, focusing on the concrete workflow aspect rather than engaging with the broader technical debate about indicator suppression. It represents another voice in the growing list of alternatives being proposed (joining `git restore -p`, `git show` with temporary files, and custom scripts) that challenge the necessity of the proposed feature.

Notably, this suggestion introduces GUI tooling into the discussion, which hadn't been mentioned previously, though it doesn't directly address Harald's stated preference for command-line workflows. The email maintains the thread's overall dynamic where multiple established solutions are being presented while the feature proposal continues to face skepticism about its added value.