This is a refactoring patch in the submodule gitdir path encoding series, specifically addressing validation of submodule gitdir paths. The patch consolidates validation checks that were previously scattered across multiple call sites into the `submodule_name_to_gitdir()` function itself.

The key changes are:
1. Moves gitdir validation from three separate locations (in `submodule--helper.c` and `submodule.c`) into `submodule_name_to_gitdir()`, which now always validates before returning a path
2. Maintains two remaining validation calls where they're needed to prevent parallel clone races
3. Simplifies the API by making validation mandatory rather than leaving it to callers
4. Prepares for future work where users can specify gitdir paths via config

The patch removes 12 lines of redundant validation code while adding 4 lines to make validation intrinsic to the path resolution function. This is part of the larger effort to implement safe path encoding for submodule gitdirs, with this change specifically ensuring all paths are consistently validated regardless of how they're obtained.

The implementation touches core submodule operations like clone`, `move-head`, and directory relocation. The validation helps prevent submodule gitdirs from being created in invalid locations or conflicting with other submodules' paths. This is particularly important safeguard as the series introduces more flexible path configuration options.

The change appears well-contained and aligns with the series' architectural goal of centralizing path resolution logic. It addresses a clear code organization issue while maintaining all necessary safety checks. The commit message clearly explains the motivation and benefits of the consolidation.