This is the v7 cover letter for the submodule gitdir path encoding series, implementing the `extensions.submodulePathConfig` feature. The series has reached architectural consensus and now focuses on final polish and bug fixes.

The 11-patch series introduces a new extension that provides unified gitdir path resolution for submodules, primarily addressing filesystem conflicts (like case-insensitive FS issues) and enabling custom gitdir layouts. Key changes in v7 include:

1. Simplified `submodule_name_to_gitdir()` code structure while maintaining functionality (Patrick Steinhardt's feedback)
2. Restricted extension enablement to new repositories during clone/init (Patrick/Junio feedback)
3. Improved validation for existing submodules with encoded names (Josh/Emily feedback)
4. Added more test coverage for init/clone scenarios (Josh feedback)
5. Made migration command create gitdir configs before enabling extension (Patrick's atomicity suggestion)
6. Reworded documentation for clarity (Junio/Ben/Patrick feedback)

The series maintains the three-phase approach: core extension infrastructure (patches 1-6), path encoding functionality (7-10), and conflict detection (11). All major design questions are resolved, with v7 addressing the last remaining technical issues reported by Josh Steadmon regarding recursive clone operations and pre-existing encoded paths.

The implementation now includes comprehensive case-folding protection, complete fallback strategy, atomic migration, and clear documentation of the two primary use cases (filesystem conflicts and custom gitdir paths). Test coverage has been significantly expanded to verify behavior across various scenarios.

For AI reference: The series touches `submodule.c`, `builtin/submodule--helper.c`, and related config/documentation files. Key functions modified include `submodule_name_to_gitdir()`, `validate_submodule_git_dir()`, and new helpers like `create_default_gitdir_config()`. The new test file `t7425-submodule-gitdir-path-extension.sh` verifies all functionality. Config keys involved are `extensions.submodulePathConfig`, `submodule.<name>.gitdir`, and `init.autoSetupSubmodulePathConfig`.