This is a follow-up question in the ongoing discussion about enhancing `git status` to show comparisons with default branches. The author, Harald Nordgren, is seeking clarification about remote configuration behavior in triangular workflows (where push and pull remotes differ).

Nordgren asks whether Git's push/pull remote configuration can be set up to automatically push to a fork while pulling from upstream without requiring command-line flags, and whether such a configuration would work with the proposed `git status` enhancement that shows comparisons against the default branch.

The question demonstrates practical workflow considerations that are central to the thread's current focus on configuration flexibility. It touches on key discussion points about:
- Triangular workflow support
- Remote naming conventions
- The interaction between push/pull configuration and status output
- The need for intuitive default behaviors

This isn't proposing new functionality but rather exploring how existing remote configuration mechanisms might address some of the workflow concerns raised in the thread. The answer could help inform decisions about how flexible the `git status` enhancement needs to be to support common development scenarios.