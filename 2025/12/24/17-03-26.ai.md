Refactoring patch converting commit-graph.c to use the new `commit_stack` API, part 13/14 in the standardization series. This continues the pattern of replacing custom commit array implementations with the shared infrastructure, now applied to the commit-graph writing subsystem.

The patch replaces the `packed_commit_list` structure and its manual memory management with `commit_stack` throughout commit-graph.c. Key changes include:
- Removing the custom `packed_commit_list` struct definition
- Converting all `list`/`nr`/`alloc` references to use `commit_stack`'s `items`/`nr` members
- Replacing `ALLOC_GROW` calls with `commit_stack_push` and `commit_stack_grow`
- Using `commit_stack_clear` for cleanup
- Maintaining identical behavior while reducing code by 8 lines (47 deletions vs 39 additions)

The conversion touches core commit-graph writing logic including chunk generation, bloom filter computation, and generation number handling. The changes are mechanical but thorough, updating all commit array operations to use the new API while preserving the existing algorithms. This demonstrates `commit_stack`'s applicability to performance-critical subsystems that manage large commit collections.

This patch follows the established series pattern of subsystem conversions, building on the shallow.c changes from patch 11 and the API refinements in patch 12. It further validates the `commit_stack` design by handling commit-graph.c's complex commit processing needs without requiring test modifications. The clean diff suggests the API provides good coverage for commit array use cases.