Final configuration change in a feature series adding branch comparison to `git status`. This patch renames the configuration key from `repo.settings.statusGoalBranch` to the more standard `status.compareBranch` format, completing the v6 series' interface polish.

The change is straightforward - updating the key name in both the implementation (remote.c) and all test cases (t6040-tracking-info.sh). The functionality remains identical, just using Git's conventional dotted configuration syntax. This follows through on the series' evolution from earlier versions that used different naming schemes.

The tests confirm the feature continues working as before, showing branch divergence information when configured and falling back gracefully when unset. This appears to be the last piece in finalizing the user interface for this feature, which has undergone several rounds of review and optimization throughout its v1-v6 progression. The patch is small and focused, with no behavioral changes beyond the configuration key update.