Refactoring patch converting `builtin/log.c` to use the new `commit_stack` infrastructure for managing commit arrays. This is part of a larger series standardizing dynamic commit pointer handling across Git's codebase.

The patch replaces manual array management in `cmd_format_patch()` with the shared `commit_stack` API, eliminating direct `REALLOC_ARRAY` calls and explicit index calculations. Key changes include using `commit_stack_push()` to add commits and `commit_stack_pop()` to process them in LIFO order, which simplifies the control flow while maintaining identical functionality.

The conversion demonstrates the series' benefits clearly - the diff shows 16 lines removed versus 15 added, with the remaining changes being straightforward mechanical replacements of array operations with their `commit_stack` equivalents. The patch maintains all existing behavior including progress reporting and patch generation logic.

As with the series' other conversions, this appears to be a non-controversial improvement that reduces code duplication while keeping the same performance characteristics. The change affects core log/patch generation functionality but doesn't modify any user-visible behavior.