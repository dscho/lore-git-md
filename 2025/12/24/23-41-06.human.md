Feature enhancement finalizing the configurable branch comparison in `git status`. This v6 series from Harald Nordgren concludes the work to show divergence from a configured "goal" branch, now renamed to use `status.compareBranch` as the final config key. The series maintains all functionality from v5 while responding to feedback about the configuration name.

The key change in v6 is the renaming of the configuration key from `statusGoalBranch` to `status.compareBranch` for better consistency with Git's existing configuration naming patterns. This appears to be the only change from v5, as indicated by the range-diff showing all previous patches unchanged except for the addition of patch 6.

The implementation remains robust, with comprehensive test coverage in t6040-tracking-info.sh and optimized internals that reuse Git's existing branch tracking infrastructure. The feature allows users to monitor their branch's divergence from an important reference branch (like mainline) even when tracking a different branch, addressing workflow needs for keeping tabs on upstream changes.

With this final naming adjustment, the series appears ready for consideration, having addressed all previous feedback through its iterations while maintaining the core functionality that shows: "Your branch is ahead of 'main' by X commits" alongside the regular tracking information.