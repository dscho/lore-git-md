This is a discussion about configuration approach for the proposed `git status` default branch comparison feature. Yee Cheng Chin responds to Harald Nordgren's suggestion about making the setting repo-specific rather than just per-branch.

The email acknowledges the need for flexibility in configuration, particularly for special cases like 'gh-pages' branches where the default branch comparison might not be useful. While not taking a strong stance on the per-repo vs per-branch debate, Chin suggests a potential two-layered configuration system that could accommodate both approaches, though they express some concern about complexity.

This continues the thread's evolution from implementation details to broader design questions about how to make the feature configurable enough to handle diverse workflows. The discussion remains focused on finding the right balance between flexibility and simplicity in the configuration approach.