This is the final patch in a refactoring series that standardizes Git's handling of commit pointer arrays using the new `commit_stack` infrastructure. The patch converts commit-reach.c to use the shared API, replacing manual array management with `commit_stack` operations.

The change follows the established pattern seen throughout the series, replacing direct `ALLOC_GROW` calls and manual array management with the standardized `commit_stack_push`, `commit_stack_grow`, and `commit_stack_clear` functions. The conversion maintains all existing behavior while reducing code duplication - this particular change shows a net reduction of 1 line (13 deletions vs 12 additions).

As with previous conversions in the series, no test modifications were needed, demonstrating the API's behavioral compatibility. The commit-reach.c changes complete the series' goal of eliminating custom commit array implementations across Git's codebase. The patch builds on earlier work that refined the API and demonstrated its suitability in performance-critical subsystems like commit-graph writing.

This marks the conclusion of a significant standardization effort that has touched 23 files, resulting in a net reduction of over 100 lines of code. The series has shown the `commit_stack` API to be robust enough for both simple cases and complex commit processing needs across Git's codebase.