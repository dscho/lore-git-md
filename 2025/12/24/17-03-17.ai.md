Refactoring patch converting `name-rev` to use the new `commit_stack` infrastructure.

This is the fourth patch in a 14-part series standardizing Git's handling of dynamic commit pointer arrays. The patch converts `builtin/name-rev.c` to use the shared `commit_stack` API, replacing manual array management with the standardized operations. The changes follow the same pattern seen in previous conversions: replacing `ALLOC_GROW` with `commit_stack_push`, manual array indexing with `commit_stack_pop`, and direct freeing with `commit_stack_clear`.

The conversion touches the core traversal logic in `name_rev()`, where commit parents are processed. The patch demonstrates the API's applicability to name-rev's parent processing workflow, showing how it simplifies the allocation and management of temporary commit storage. The diff shows a net reduction of 5 lines while maintaining identical behavior.

This continues the series' trend of mechanical conversions across different subsystems, now covering the name-rev command. The changes are straightforward and follow the established pattern from prior patches, suggesting this is a low-risk conversion. The patch maintains the series' focus on eliminating duplicate array management code while not introducing any functional changes.