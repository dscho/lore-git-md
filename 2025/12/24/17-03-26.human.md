This is patch 13/14 in a refactoring series that standardizes Git's handling of commit pointer arrays using the new `commit_stack` infrastructure. The patch converts the commit-graph writing code to use `commit_stack` instead of its custom `packed_commit_list` implementation.

The changes are mechanical but widespread, touching nearly all parts of the commit-graph writing logic that deal with commit lists. The conversion:
- Replaces `packed_commit_list` with `commit_stack` in the context structure
- Updates all references from `list` to `items` (the standard field name in `commit_stack`)
- Swaps manual array growth using `ALLOC_GROW` for `commit_stack_push()`
- Uses `commit_stack_grow()` for pre-allocation when merging graphs
- Cleans up with `commit_stack_clear()` instead of direct `free()`

The patch follows the established pattern seen in previous conversions in this series, demonstrating the `commit_stack` API's versatility across different subsystems. As with earlier patches, this maintains identical behavior while reducing code duplication. The diff shows a net reduction of 8 lines, continuing the series' trend of removing more code than it adds.

This conversion is particularly notable because the commit-graph machinery is performance-sensitive, so its adoption of the shared infrastructure helps validate the API's efficiency. The patch appears well-executed, with thorough updates to all relevant code paths and no behavioral changes.