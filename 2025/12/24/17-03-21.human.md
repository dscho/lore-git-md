This is the eighth patch in a refactoring series standardizing Git's commit pointer array handling with the new `commit_stack` API. The patch converts the test-reach helper to use the shared infrastructure, replacing its custom array management code.

The test-reach helper is a test utility that exercises Git's commit reachability algorithms. This change replaces manual array growth operations (`ALLOC_GROW`) and separate counter variables with the standardized `commit_stack` API, making the code more concise and consistent with other parts of the codebase. The patch shows a typical reduction pattern (14 lines removed, +8 added) while maintaining identical functionality.

This conversion follows the same successful pattern seen in previous patches, now covering the test infrastructure. The change is straightforward and mechanical, with no special edge cases - test-reach was using the same basic array management pattern that the `commit_stack` API was designed to replace. The patch further demonstrates the API's versatility by showing it works well in test code, not just core functionality.

As with previous patches in the series, this appears to be a clean improvement with no visible downsides. The conversion maintains all existing test coverage while reducing maintenance burden. The series continues to show strong momentum with each new conversion strengthening the case for the API's adoption.