Refactoring patch converting another commit array to the new `commit_stack` infrastructure, now targeting `remote.c`'s tag synchronization code. This is patch 7/14 in the series standardizing Git's handling of growable commit pointer arrays.

The change replaces a manually managed commit array in `add_missing_tags()` with the shared `commit_stack` API, following the same pattern seen in previous conversions. The patch shows the typical benefits: simpler allocation patterns (replacing `ALLOC_GROW` with `commit_stack_push`) and automatic cleanup via `commit_stack_clear`. The conversion maintains all existing functionality including reachability calculations and flag clearing.

This continues the series' accelerating code reduction trend, though the 7-line net change here is more modest than some earlier patches. The conversion demonstrates the API works well in networking-related code paths, further validating its general applicability. With no substantive objections raised in the thread so far, the series appears to be progressing smoothly toward inclusion.