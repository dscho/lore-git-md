This is patch 12/14 in the `commit_stack` refactoring series, introducing a new helper function for the growing API. The patch adds `commit_stack_grow()` to allow callers to pre-allocate capacity when they know how many commits they'll need to store, reducing reallocations. 

The change is straightforward - it extracts the growth logic from `commit_stack_push()` into its own function and updates the push operation to use it. The implementation still uses Git's standard `ALLOC_GROW` macro internally. This follows the series' pattern of providing more granular control over the stack operations while maintaining the same underlying behavior.

As with previous patches in the series, this demonstrates the API's thoughtful evolution - the new function wasn't needed for initial conversions but emerges as a natural optimization point now that the infrastructure is being adopted more widely. The change touches only the core stack implementation files (commit.c/h) and maintains the series' clean diffstat (7 lines added, 1 changed).

The patch continues the series' strong technical quality with clear commit message, minimal scope, and obvious mechanical correctness. It positions the API for more efficient bulk operations while keeping all existing functionality intact. With only two patches remaining after this one, the series appears to be approaching completion of its goal to standardize commit array handling across Git's codebase.