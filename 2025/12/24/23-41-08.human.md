Performance optimization patch in the `git status` branch comparison series. This is the second patch in v6 of a series that adds configurable branch comparison to `git status` output.

The patch simplifies the logic for comparing branches by reducing redundant ref resolution calls and memory allocations. Key changes include:
- Moves default branch ref resolution to a single point in `format_tracking_info()` rather than doing it multiple times
- Eliminates the separate `is_default_remote_branch()` function by incorporating its logic directly
- Consolidates memory management by handling all allocations/frees in the main function
- Maintains the same functionality while being more efficient

The changes follow through on performance optimizations discussed earlier in the thread, building on Junio C Hamano's guidance about using existing branch configuration. The patch appears well-contained and focused improvements to the v5 implementation, with the core branch comparison behavior remaining the same but now more efficiently implemented.