This is a refactoring patch in the ongoing commit_stack standardization series (now at 9/14). It introduces `commit_stack_init()` as a new public API function for cases where static initialization with `COMMIT_STACK_INIT` isn't practical. The implementation follows the same zero-initialization pattern used elsewhere in Git's dynamic array handling.

The patch makes two notable changes: First, it adds the new initialization function to commit.h and implements it in commit.c with the standard NULL/0 initialization pattern. Second, it modifies `commit_stack_clear()` to reuse this new function rather than directly manipulating the struct fields, demonstrating the DRY principle the series aims to promote.

This continues the series' pattern of small, focused changes that incrementally improve code consistency. The change to `commit_stack_clear()` shows how the new API enables cleaner internal implementations while maintaining identical external behavior. With this addition, the commit_stack API now provides both static and dynamic initialization options, covering all common use cases.

The patch follows the series' established style with minimal churn (9 lines added, 2 changed) and maintains the thread's momentum toward eliminating duplicate commit array management code. No new test changes are needed since this is purely an internal API addition that doesn't affect behavior.