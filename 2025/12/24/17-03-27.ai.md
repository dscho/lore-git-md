Final patch in the commit_stack refactoring series, targeting commit-reach.c.

This patch converts the commit array handling in commit-reach.c to use the new `commit_stack` API, completing the series' goal of standardizing commit pointer management across Git's codebase. The changes follow the established pattern seen in previous patches: replacing manual `ALLOC_GROW` operations with `commit_stack_push` and converting array management to use the stack's `items` and `nr` fields. The patch touches the `remove_redundant_with_gen` function which handles commit reachability calculations with generation numbers.

Key technical changes include replacing the `walk_start` array and its separate allocation counter with a `commit_stack`, converting all array operations to use the stack API, and maintaining the same STALE flag manipulation logic but through the stack's accessors. The diff shows a net reduction of 13 lines removed versus 12 added, continuing the series' trend of code simplification.

This conversion demonstrates the `commit_stack` API's applicability to core algorithms like commit reachability, which involves complex commit graph traversal and generation number comparisons. The patch maintains all existing behavior while eliminating custom memory management code. As with previous conversions in the series, no test modifications were needed, indicating the changes are purely internal refactoring.

The commit-reach.c conversion represents the final major subsystem adoption in this series, following successful conversions of revision.c, networking code, test infrastructure, tag handling, bitmap generation, shallow clone operations, and commit-graph writing. This completes the standardization effort that began with extracting the `commit_stack` concept from revision.c and promoting it to a public API.