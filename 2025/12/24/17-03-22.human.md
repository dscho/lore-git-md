This is a refactoring patch in a series standardizing Git's handling of commit pointer arrays. It's the 9th of 14 patches introducing a shared `commit_stack` infrastructure to eliminate code duplication across multiple subsystems.

The patch adds a new `commit_stack_init()` function for cases where static initialization (via `COMMIT_STACK_INIT`) isn't practical. The implementation is straightforward - it zeroes the stack's items pointer and counters. The patch also tweaks `commit_stack_clear()` to reuse this new function rather than duplicating the initialization logic.

This continues the series' pattern of small, focused changes that incrementally improve codebase consistency. The change is mechanical but meaningful - it reduces duplication in the stack management code itself, demonstrating the API's internal consistency. With this addition, the `commit_stack` interface now has complete symmetry between static and dynamic initialization paths.

The patch follows the series' established quality standards: minimal changes (9 lines added, 2 removed), clear commit message, and no behavior changes. It builds on the thread's momentum where previous conversions have already demonstrated the API's value across both production code and test infrastructure.