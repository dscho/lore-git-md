Refactoring patch converting `remote.c`'s custom commit pointer array to the new `commit_stack` infrastructure, part of the ongoing series to standardize dynamic commit array handling.

This sixth patch in the 14-part series replaces the ad-hoc `struct tips` implementation in `remote.c` with the centralized `commit_stack` API. The changes are mechanical but demonstrate the API's flexibility by applying it to the tag synchronization logic in `add_missing_tags()`. Key changes include replacing manual array growth with `commit_stack_push()`, using `COMMIT_STACK_INIT` instead of memset, and calling `commit_stack_clear()` for cleanup. The patch maintains all existing functionality including the TMP_MARK flag handling and reachability calculations.

The conversion follows the established pattern of the series, reducing code by 13 lines while making the memory management more robust. This particular change affects the tag synchronization logic used during push operations, showing the API works well in the networking code path. The patch continues the series' trend of demonstrating the `commit_stack` API's versatility across different subsystems, having now been applied to log, midx, name-rev, and remote functionality.

Technical details: The `sent_tips` array tracking pushed commits is now a `commit_stack`, with all direct array manipulation replaced by API calls. The reachability check in `get_reachable_subset()` now uses the stack's `items` array directly. This maintains the exact same behavior while eliminating custom allocation code. The patch implicitly confirms the API works for temporary marking patterns (TMP_MARK) and complex operations like reachability calculations.