This is a feature enhancement for `git status` output, part of the ongoing effort to show comparisons with the default branch (like `upstream/master`) alongside the usual tracking branch info. The author Harald Nordgren shares a real-world example of using their own patched Git version, demonstrating how the new output shows both the divergence from the tracking branch (`origin/ahead_of_main_status`) and the position relative to the default branch (`upstream/master`). The example shows a clean 2-commit lead over the default branch while also noting a 1-commit divergence from the tracking branch.

This personal testimonial comes after the technical discussion in the thread about implementation details and configuration options. It serves as a practical validation of the feature's utility in daily workflow - the author finds it genuinely useful enough to use their custom build. The output format follows the established `git status` style while cleanly adding the new information.

The thread has evolved from initial implementation to broader design questions about configuration, but this email brings it back to concrete user experience. It shows the feature helps visualize both local branch state and relationship to the main development line, which was the original motivation for the change. The positive real-world feedback suggests the feature delivers value despite the remaining open questions about edge cases and configuration.