Refactoring patch in the `commit_stack` standardization series, now at patch 10/14. This converts the pack-bitmap-write subsystem to use the new shared `commit_stack` API for managing its commit pointer array, replacing direct ALLOC_GROW usage and manual memory management.

The changes follow the established pattern seen in previous patches: the `bitmap_builder` struct replaces its individual commit tracking fields (`commits`, `commits_nr`, `commits_alloc`) with a `commit_stack` member. Initialization now uses `commit_stack_init()`, push operations use `commit_stack_push()`, and cleanup uses `commit_stack_clear()`. The diff shows a net reduction of 4 lines while maintaining identical functionality.

This continues the series' trend of mechanical conversions that demonstrate the API's versatility - here applied to bitmap generation code that walks commit graphs. The changes are straightforward with no behavioral impact, just like prior patches in the series. The commit message is minimal but sufficient given the established context of this multi-patch effort.

Key technical points for future reference:
- Touches `pack-bitmap-write.c`'s bitmap building infrastructure
- Replaces manual array growth with `commit_stack_push()`
- Updates iteration to use `items[i-1`] instead of direct array access
- Maintains all existing bitmap generation behavior
- Follows same pattern as previous conversions in series
- No new test changes needed (behavior preserved)