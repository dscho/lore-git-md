This is a follow-up question in the ongoing discussion about configurable branch comparison in `git status` output. The author Harald Nordgren seeks clarification about remote configuration behavior in triangular workflows (where push and pull remotes differ), specifically asking whether `git status` would show comparisons against the upstream remote when push/pull remotes are properly configured.

The email poses a concrete scenario with two remotes ('upstream' and 'fork') and asks whether Git's existing push/pull remote configuration can support the common triangular workflow pattern where developers want to:
- Push to their personal fork by default (`git push`)
- Pull from the canonical repository (`git pull`)
- See status comparisons against the canonical repository in `git status`

This question directly relates to the thread's exploration of how the proposed default branch comparison feature should interact with Git's existing remote configuration mechanisms. The answer would help determine whether the current design needs to account for these workflow patterns or if they're already handled by core Git functionality.

The inquiry suggests the author is trying to understand the boundaries between what should be solved by the new feature versus what belongs to Git's existing remote configuration system. This is particularly relevant given previous discussion about hardcoded remote preferences ("upstream"/"origin") and the need for more flexible configuration in multi-remote scenarios.