Thread identity: Test fix for flaky fsmonitor event tests in t7527-builtin-fsmonitor.sh.

Core problem: Race conditions in fsmonitor tests where grep checks for filesystem events could fail because the fsmonitor daemon hadn't finished writing events to the trace file yet.

Solution introduced: New `retry_grep()` helper function that repeatedly attempts a grep with 5-second timeout, waiting for expected events to appear. Replaces previous approach of using `test-tool fsmonitor-client query` as a delay mechanism.

Technical details: Changes affect t7527-builtin-fsmonitor.sh test file. Removes 24 lines (query calls) and adds 27 (new helper and calls). Applied consistently across all event-checking tests (edit, create, delete, rename, directory operations). Implementation follows pattern similar to `retry_until_success` from lib-git-p4.sh.

Motivation: Fix prompted by actual CI failure, not just theoretical flakiness.