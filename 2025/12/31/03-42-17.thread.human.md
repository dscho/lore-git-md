Here's a human-readable summary of the thread so far:

The thread discusses an RFC proposal to improve how `git format-patch` handles merge commits. Currently, the command silently skips merge commits (those with multiple parents), which can surprise users of merge-heavy workflows like Jujutsu (jj). The initial patch proposes adding warnings when merges are skipped, making the behavior more visible by showing the commit OIDs.

Junio Hamano has now responded with an important consideration: while warnings would be helpful when no commits are formatted at all (indicating the user might have expected output), they could become annoying noise in common workflows where users intentionally include merge-heavy ranges (like `trunk..mytopic`) and expect merges to be skipped silently. This suggests the warning behavior needs to be more nuanced - perhaps only showing warnings when no patches are generated at all, or making the warnings configurable.

The discussion now centers on balancing two goals: making the skipping behavior more transparent for users who need that visibility, while not disrupting established workflows where the current silent behavior is actually desired. The next iteration of the patch will likely need to address this distinction through either smarter warning conditions or configuration options.