This is a performance discussion about `git pack-refs` behavior on NFS with high ref counts, continuing the investigation into why Git is significantly slower than JGit in this scenario.

Martin Fick pushes back on the NFS latency explanation, noting that JGit handles the same NFS environment with 37 pack files much faster (20s vs Git's 5+ minutes). He argues that even with NFS overhead, 200 object lookups shouldn't take 3 minutes unless each lookup took about 1 second - which seems implausible. While acknowledging NFS can be slow, he suspects there's something more fundamental about Git's implementation causing the slowness, though he can't pinpoint what exactly.

The email maintains focus on the core performance discrepancy between Git and JGit that started the thread, while challenging the NFS explanation as insufficient to account for the magnitude of difference. This keeps the discussion centered on potential inefficiencies in Git's ref packing implementation rather than environmental factors. The technical context from earlier in the thread (verification requirements, reftable backend differences) remains relevant background for this ongoing analysis.