This is a follow-up discussion in the ongoing thread about enhancing Git's `repo structure` command with object size reporting. The specific focus here is on human-readable formatting of count values in the output table.

Justin Tobler responds to Junio Hamano's suggestion about reusing `strbuf_humanise_bytes()` from progress.c for count formatting. Justin acknowledges that while he initially considered this approach, he chose a different implementation to achieve proper alignment of values and unit prefixes in the table output. However, he indicates plans to refactor in the next version to reuse some of the underlying logic while maintaining the desired formatting.

Key technical points:
- The discussion centers around the formatting of human-readable numbers in the `repo structure` output
- Current implementation uses static strings for units ("k", "M", "G") which Patrick Steinhardt previously noted should be translatable
- Junio suggests reusing existing `strbuf_humanise_bytes()` helper
- Justin explains his alignment requirements for table output prevented direct reuse
- Plans to refactor to share logic while preserving formatting needs

This exchange shows the careful consideration being given to both code reuse and output presentation quality in the feature implementation. The thread continues to refine the implementation based on reviewer feedback while maintaining specific formatting requirements.