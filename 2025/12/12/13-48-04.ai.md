This is a follow-up in the ongoing macOS iconv conversion issue discussion, specifically addressing build system paths for package managers. 

René Scharfe points out that the Makefile's handling of Fink package manager paths is outdated, referencing `/sw` when Fink moved to `/opt/sw` six years ago. This comes in response to Junio Hamano's speculation about possible symlinks between the old and new paths. The exchange highlights how Git's build system needs to stay current with package manager conventions, even for less commonly used options like Fink.

The note serves as a minor correction in the broader context of refining macOS package manager path detection, where the main focus remains on Homebrew paths but peripheral package managers like Fink still need accurate representation. This doesn't change the core solution approach (which centers on Homebrew's architecture-specific paths) but does surface an opportunity to clean up historical path references that may no longer be valid.

The thread continues to balance comprehensive package manager support against practical maintenance concerns, with this particular observation suggesting the Makefile could use updating for Fink's current standard path while not requiring any changes to the primary Homebrew-focused solution path.