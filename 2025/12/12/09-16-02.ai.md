This is a build system patch addressing macOS package manager paths for iconv support, continuing the thread about character encoding issues on macOS.

René Scharfe clarifies the different installation paths used by various macOS package managers in response to Junio Hamano's questions about path coverage. The explanation confirms Homebrew uses `/opt/homebrew` for Apple Silicon and `/usr/local` for Intel Macs, while MacPorts (formerly DarwinPorts) uses `/opt/local` and Fink uses `/opt/sw`. The current patch specifically targets Homebrew installations by checking both possible Homebrew locations, with priority given to the Apple Silicon path.

The email provides important context about why the patch focuses on Homebrew paths rather than attempting to cover all package managers. This helps justify the current approach while leaving the door open for potential future MacPorts/Fink support if needed. The response maintains the thread's technical precision while addressing the maintainer's concerns about path coverage documentation.

Key technical details include the specific paths for each package manager and the ARM/Intel architecture distinction in Homebrew installations. The email doesn't propose any code changes but solidifies the rationale behind the existing patch's design decisions. This information will be important for future discussions about macOS build system support and any potential expansion to other package managers.