Here's a human-readable summary of the thread:

The discussion centers on `git replay`'s behavior when given raw commit hashes instead of branch references. While the command processes these commits correctly, creating new objects in the database, it currently provides no output or reference updates in this mode - it only works when given branch-decorated commits. This creates a potentially confusing situation where the command appears to do nothing.

The thread explores whether this is an intentional limitation (in which case the command should error out to be clearer) or an oversight that could be improved. The author suggests valid use cases where raw hash input would be useful, particularly for experimentation and scripting scenarios where branch names aren't needed or desired. The discussion identifies the `DECORATION_REF_LOCAL` check as the current limiting factor.

Two potential solutions emerge: either modify the command to output the resulting commit IDs when given raw hashes, or make it fail explicitly to avoid silent non-operation. The examples provided show concrete cases where the current behavior could trip up users (`git replay --onto master master..45ab67bc`). This appears to be a design question about whether to expand the command's capabilities or make its limitations more obvious.