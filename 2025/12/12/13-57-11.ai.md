This is a follow-up discussion in the Scalar configuration documentation series, specifically about removing stale config values while preserving implicit dependencies. The email focuses on the interaction between `index.threads` and `index.recordOffsetTable` settings.

Derrick Stolee responds to Matthew Hughes' observation about the implicit relationship where `index.recordOffsetTable` defaults to true when `index.threads` is explicitly set (a behavior dating back to 2018 for compatibility). Stolee acknowledges this subtle dependency and commits to documenting it explicitly in the configuration documentation, ensuring future maintainers understand this relationship before considering any changes to the behavior.

The exchange highlights an important consideration when removing explicit config settings - some may have undocumented side effects on other configuration values. This particular case involves index performance optimizations where thread configuration affects offset table behavior. The thread context shows this is part of a broader effort to clean up Scalar's configuration while maintaining backward compatibility where needed.

Key technical details include:
- `index.threads` explicitly set affects `index.recordOffsetTable` default (true when threads set, false otherwise)
- This behavior originates from commit 2a9dedef2e (2018) for backwards compatibility
- Documentation will now explicitly call out this relationship
- Future changes to this behavior would need to consider both config values

The discussion continues the thread's theme of carefully auditing configuration interactions while modernizing Scalar's settings. This particular point illustrates how some configuration decisions have subtle but important ripple effects that need to be preserved or deliberately changed.