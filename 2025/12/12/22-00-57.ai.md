This is a workflow discussion email in the ongoing `git-history` command thread, where D. Ben Knoble shares practical experience with a Jujutsu-inspired "mega merge" workflow in Git. The email provides concrete examples of attempting similar multi-branch editing workflows using existing Git tools like `rebase --autosquash --rebase-merges` and `commit --fixup`, while acknowledging the friction points.

Knoble describes creating temporary integration branches with independent changes, making fixes across them, and manually propagating changes back to their original branches - a workflow that aligns with the capabilities being discussed for the new `git-history` command. The email confirms the practical need for better tooling in this space while noting that current Git workflows can approximate (but not elegantly replicate) Jujutsu's capabilities.

Key technical details mentioned include:
- Use of octopus merges for temporary integration
- `commit --fixup` with target branch specification
- `rebase --autosquash --rebase-merges` for final cleanup
- Challenges with commit shuffling across branches
- Workflow friction points that could inform `git-history` design

The email doesn't propose specific changes to the `git-history` implementation but provides valuable real-world context about the types of workflows users are attempting with current tools, which could help inform the ongoing design discussions around multi-branch handling and safety mechanisms. The experience shared suggests that while possible, these workflows are currently cumbersome enough in Git to discourage regular use.