This is a technical discussion about Git's C99 flexible array member (FAM) syntax requirements, continuing the thread about whether this change warrants Git 3.0 documentation. Junio Hamano responds to Patrick Steinhardt's query about the Git 3.0 classification by proposing a more aggressive approach - completely removing the legacy FLEX_ARRAY compatibility layer rather than just documenting it as a breaking change.

The new patch eliminates all the platform-specific workarounds in git-compat-util.h that previously supported pre-C99 FAM syntax (including the [0] GNU extension and [1] fallback), unconditionally requiring C99's [] syntax. The change is framed as a "weather balloon" test to see if any platforms still need the legacy support, with the understanding it could be easily reverted if problems emerge. The technical impact is minimal (31 lines removed, 2 added) but the philosophical approach differs from the initial proposal - this tests adoption by forcing the change rather than documenting it as a breaking change.

Junio mentions this is a step toward adopting Linux kernel-style fortified memcpy features that require C99 FAM support. The discussion remains focused on process (how to phase in this requirement) rather than technical implementation, with Junio now favoring immediate testing of full C99 adoption over the original Git 3.0 documentation path. Key files affected are git-compat-util.h and any code using FLEX_ARRAY macros, though the actual behavioral impact should be zero on compliant compilers.