This is a follow-up discussion in the merged series about object size reporting in `git repo structure`. Justin Tobler responds to Patrick Steinhardt's suggestion about using `git rev-list --disk-usage` for verifying disk sizes in tests.

The discussion reveals a technical nuance: `git rev-list --filter` unexpectedly includes user-specified objects (like HEAD) regardless of the type filter, making the suggested verification approach unreliable for type-specific size calculations. Justin identifies this as potentially intentional behavior based on the implementation in list-objects-filter.c.

For testing, Justin proposes an alternative using `git cat-file --batch-check` with type filters to sum disk sizes, though this doesn't help verify human-readable output. The email maintains the thread's focus on robust test validation while surfacing an edge case in `rev-list`'s filtering behavior that may warrant separate discussion.

Key technical points:
- Confirmed limitation in `git rev-list --filter` behavior regarding user-specified objects
- Proposed workaround using `git cat-file --batch-check` for raw size verification
- Acknowledged gap in human-readable output validation
- Implicit question about whether `rev-list`'s filtering behavior is intended or needs documentation

This exchange shows the series maintainers carefully considering test methodology after merge, with Justin demonstrating thorough understanding of both the object filtering internals and practical testing constraints. The discussion may prompt future consideration of `rev-list`'s filtering semantics.