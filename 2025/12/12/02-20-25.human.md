This is a build system discussion about macOS package manager paths for libiconv, continuing the ongoing thread about character encoding issues on macOS.

Junio Hamano follows up on Carlo Arenas Belón's earlier patch that defaulted ICONVDIR to Homebrew paths (/opt/homebrew and /usr/local). Junio notes that the Makefile historically mentions DarwinPorts (now MacPorts) using /opt/local, and questions whether the patch should account for this alternative package manager path as well. He suggests updating the commit message to clarify the rationale for including both paths if they're needed for different Homebrew installations.

Carlo responds that since the original patch focused specifically on Homebrew, it might not need to mention other package managers. He seeks clarification on whether both paths are actually needed for Homebrew itself (covering different installation variants) or if this was meant to support multiple package managers.

The exchange reveals some uncertainty about macOS package manager conventions, with Junio (who monitors macOS CI but doesn't use it directly) trying to understand the landscape. The discussion remains focused on build system details rather than reopening the broader iconv technical solution that was settled earlier in the thread.