This is a technical follow-up in the MEMZERO_ARRAY macro discussion, focusing on Coccinelle rule improvements. René Scharfe weighs in on Junio Hamano's suggestion to extend the pattern matching for array zeroing.

René agrees with Junio's proposal to catch `'\0'` patterns (noting their presence in compat/regex/) and suggests also matching `sizeof(ptr[...])` patterns for completeness, though no current code uses this form. He offers two optimizations: dropping the explicit `0x0` match since `0` already covers it, and making the array size parameter `n` optional in parentheses for cleaner output.

The email demonstrates deep Coccinelle expertise, with specific version testing notes ("spatch version 1.3-dirty") and file references (ewah/bitmap.c) where the changes would improve results. This moves the thread forward by validating and refining the technical approach while keeping the focus on code quality improvements. The exchange remains productive, with no new objections raised to the core concept.