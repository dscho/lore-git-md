Thread identity: Bug report about inconsistent rename detection behavior between `git diff` and `git merge` with same similarity threshold (50%).

Core problem: When moving+modifying a file (test case shows 71% similarity), `git diff` correctly detects the rename while `git merge` fails, creating a modify/delete conflict instead. Both use same config (`merge.renames=true`) and threshold, suggesting an implementation inconsistency.

Key technical details:
- Affects rename detection in diff vs merge machinery
- Test case involves file moved to subdirectory with content modifications
- `git diff` shows 71% similarity (above 50% threshold)
- `git merge` fails to detect rename at same threshold
- Both use `merge.renames=true` config

Open questions:
- Is this discrepancy intentional (undocumented) or a bug?
- Should diff and merge rename detection behave identically given same inputs?
- Needs investigation into merge machinery's rename detection implementation

Participant positions:
- Luca Balsanelli reported with clear reproduction recipe
- No maintainer response yet

Related work: Rename detection thresholds and consistency across Git commands has been discussed before (e.g. `diff.renames` vs `merge.renames` configs).