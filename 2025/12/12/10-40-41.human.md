This is a technical clarification in the ongoing macOS iconv(3) thread. René Scharfe corrects his earlier statement about being able to compile Git with NO_ICONV on macOS, explaining that this isn't actually possible due to `compat/precompose_utf8.c`'s dependency on `reencode_string_iconv()`. 

The note serves as an important implementation detail for the broader discussion about macOS character encoding issues, confirming that the NO_ICONV workaround isn't viable on macOS systems. This reinforces the need for the other proposed solutions (state reset workaround or package manager path detection) being discussed in the thread.

The message is brief but significant for maintainers working on the build system changes, as it eliminates one potential avenue for solving the macOS test failures. It also highlights the complexity of macOS-specific Unicode handling in Git's codebase.