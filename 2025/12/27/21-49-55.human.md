This is a technical discussion about Git's handling of directory permissions, specifically the SGID bit and ACL alternatives. Gabor Gombas responds to Hadmut Danisch's assertion that ACLs don't solve the problem of group inheritance in container workflows.

Gabor suggests that default ACLs might actually work because Git doesn't interfere with them (unlike SGID bits which Git actively clears). He acknowledges ACL management has its own challenges, but frames the core issue as Git's overzealous permission management rather than a lack of features. The email notes that while `core.filemode` exists to ignore executable bits, there's no inverse option to preserve all other permission bits.

The response maintains the thread's technical focus while introducing a pragmatic perspective - that Git's "clever" permission handling may be causing more problems than it solves. Gabor hints that a patch preserving permission bits (except executables) could be a solution, though he doesn't propose specific code changes himself. The email keeps the discussion alive while awaiting either maintainer input or concrete technical proposals.