This thread addresses a security-sensitive bug in `receive-pack` where symbolic refs pointing beyond their namespace could cause crashes. Troels Thomsen's original patch fixed a NULL dereference by properly handling fully-qualified ref names in `check_aliased_update_internal()`, with a test case in `t5509-fetch-push-namespaces.sh` verifying the fix.

While Junio Hamano acknowledges the crash fix, he raises deeper questions about namespace isolation policy. The discussion has revealed two perspectives: Troels argues the permissive behavior is reasonable since symrefs require explicit server-side configuration, suggesting hooks could enforce stricter policies if needed. Junio counters that cross-namespace symrefs could create security holes and proposes making `receive-pack` explicitly reject such updates by default.

The technical context involves commits ded8393610 (namespace handling) and da3efdb17b (symref checks). Currently at an impasse, the thread remains open with core questions about Git's intended namespace semantics for symrefs - whether to maintain flexibility for intentional cross-namespace links or enforce strict boundaries by default. No implementation changes have been proposed yet, but the discussion continues to weigh security considerations against configuration flexibility.