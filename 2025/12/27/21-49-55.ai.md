This is a technical discussion about Git's handling of directory permissions, specifically the SGID bit and ACL alternatives. Gabor Gombas responds to Hadmut Danisch's assertion that ACLs don't solve the SGID propagation problem in container workflows.

Gombas suggests that Git's lack of ACL awareness might actually be beneficial in preserving permissions, since Git wouldn't try to "manage" them like it does with permission bits. He acknowledges ACL management challenges but points out the core issue is Git's overzealous permission bit handling. The email notes that while `core.filemode` exists to ignore executable bits, there's no inverse option to preserve all bits except executable ones.

The response reinforces the thread's technical focus while introducing a new perspective: that Git's ignorance of ACLs could be leveraged as a workaround. Gombas also hints at a potential solution path by suggesting Danisch could propose a patch to modify Git's permission handling behavior. The email maintains the thread's established distinction between SGID's automatic inheritance and ACL capabilities while adding the observation that Git's current "clever" behavior with permission bits may be causing more problems than it solves.