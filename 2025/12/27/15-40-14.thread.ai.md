Bugfix for receive-pack crash when handling symbolic refs pointing beyond their namespace, now centered on security policy debate. Original patch from Troels Thomsen modified `check_aliased_update_internal()` in `builtin/receive-pack.c` to handle fully-qualified ref names (including namespaces), preventing NULL dereferences during symref updates. Test case in `t/t5509-fetch-push-namespaces.sh` verifies push to symref with out-of-namespace target succeeds without crashing.

Junio Hamano acknowledges the crash fix but advocates stricter policy: `receive-pack` should reject cross-namespace symref updates as security violations. Troels Thomsen counters that symrefs require explicit server-side creation, implying intentional cross-namespace links, and suggests documenting current behavior in gitnamespaces(7). Junio firmly rejects documentation-only approach, insisting security boundaries should be strict by default with explicit opt-in for exceptions. 

The debate crystallizes opposing views of Git's namespace model: Troels sees symrefs as intentionally configurable links between namespaces, while Junio treats namespaces as strict security boundaries requiring isolation. Technical context remains the original NULL dereference fix, which Junio now wants to repurpose for policy enforcement. No implementation exists yet for Junio's rejection approach. Open questions include: test case validity under potential behavior changes, whether hooks should enforce policy (as both agree they could), and whether this represents a security vulnerability or legitimate use case. The thread has shifted from technical fix to fundamental namespace design philosophy, with no resolution between security-first and backward-compatibility priorities.