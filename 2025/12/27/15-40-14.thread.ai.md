Bugfix for receive-pack crash when handling symbolic refs pointing beyond their namespace, with ongoing security policy debate. Original patch from Troels Thomsen modified `check_aliased_update_internal()` in `builtin/receive-pack.c` to properly handle fully-qualified ref names (including namespaces), preventing NULL dereferences during symref updates. Test case in `t/t5509-fetch-push-namespaces.sh` verifies push to symref with out-of-namespace target succeeds without crashing. Builds on prior namespace (ded8393610) and symref (da3efdb17b) handling commits.

Junio Hamano acknowledges the crash fix but proposes stricter policy: `receive-pack` should reject cross-namespace symref updates with an error, treating them as security violations rather than valid configurations. This contrasts with Troels Thomsen's position that symrefs require explicit server-side creation, implying intentional cross-namespace links, and that hooks can enforce stricter policies if needed. Junio emphasizes preventing accidental security holes through strict namespace boundaries, while Troels argues for preserving flexibility for intentional configurations.

The discussion crystallizes a design tension in Git's namespace model - whether symrefs should be allowed to bridge namespaces (Troels' permissive view based on explicit configuration) or whether namespace boundaries should be strictly enforced (Junio's security-conscious position). No implementation proposals yet for Junio's suggested rejection behavior, but the thread remains open for further design discussion. The test case's validity under potential behavior changes remains an open question. Both sides acknowledge hooks could implement either policy, differing on what the default should be.