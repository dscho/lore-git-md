This is a follow-up discussion about patch 2/6 in the series enhancing Git's `repo structure` command with human-readable output formatting. The patch focuses on adding human-friendly scaling (k/M/G units) for count values in the command's table output.

Justin Tobler responds to Patrick Steinhardt's review feedback, agreeing to make the unit strings translatable via gettext in the next version. There's discussion about whether to use decimal (1000-based) or binary (1024-based) units - Justin explains the current approach uses SI units (decimal) for counts but will use IEC units (binary) for disk sizes in later patches. 

The exchange also touches on test output formatting, where Justin acknowledges a minor spacing change in the test expectations that could potentially be made conditional. The tone is collaborative, with Justin receptive to the feedback while explaining the rationale behind certain design choices.

This is part of a larger effort that has since been merged, adding comprehensive object size reporting to `repo structure`. The discussion here helps refine the implementation details around unit formatting and internationalization.