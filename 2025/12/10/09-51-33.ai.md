This is a technical follow-up email in the `git-history` feature series, specifically addressing implementation details of the `split` subcommand. Phillip Wood provides focused code review feedback on Patrick Steinhardt's v6 patch 11/11, continuing their earlier discussion about the split implementation.

The email centers on three specific technical points from the implementation:

1. Questions the need for forking a `read-tree` subprocess rather than using `unpack_trees()` directly, following up on Patrick's explanation about in-memory index limitations. Phillip probes deeper into why an on-disk operation is necessary at all in this context.

2. Notes code duplication in commit author handling between the `split` and `reword` subcommands, suggesting a shared helper function. Patrick had already acknowledged this point in the previous exchange and indicated he would refactor it.

3. Observes that the initial validation logic in `cmd_history_split()` closely mirrors that of `cmd_history_reword()`, predicting this pattern will repeat for future subcommands like `drop` and `amend`. This builds on Patrick's earlier response where he mentioned having already addressed some of this duplication.

The exchange shows active refinement of the implementation details, with Phillip providing focused technical review and Patrick responsive to the feedback. This represents the polishing phase of the feature development, with attention now on code organization and eliminating duplication as the series nears completion. The discussion remains narrowly technical, avoiding the broader design philosophy debate about multi-branch handling that dominates other parts of the thread.