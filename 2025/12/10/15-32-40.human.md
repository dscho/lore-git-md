This is the seventh patch in Patrick Steinhardt's v2 series refactoring Git's object database alternates handling, part of the broader ODB abstraction effort. The patch continues the transition from direct filesystem operations to working through the new "source" abstraction layer.

The key change here is renaming `read_info_alternates()` to `odb_source_read_alternates()` and modifying it to operate on an `odb_source` object rather than taking a raw path. This better aligns with the series' goal of abstracting alternates handling to support pluggable backends. The patch updates all callers to use the new interface while maintaining identical functionality.

The changes are straightforward mechanical updates following the architectural direction established earlier in the series. The patch received positive review in v1 from Justin Tobler, who had one question about future interface design that Patrick addressed by clarifying plans for URI-based source type handling. The v2 version incorporates naming feedback from earlier reviews while keeping the core technical approach.

This patch represents another incremental step in the series' progression toward fully abstracted alternates handling, building on the recursion elimination and cleanup work from previous patches. The changes are focused and well-contained, touching only 12 lines across 4 locations in odb.c.