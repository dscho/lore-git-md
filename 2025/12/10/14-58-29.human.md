This is a merge commit in the ongoing `repo structure` command enhancement series, specifically addressing test conflicts between two concurrent topics. Junio Hamano has merged Justin Tobler's patch series (which adds disk and inflated size reporting to the `repo structure` command) with Lucas Seiki Oshiro's work making `-z` a synonym for `--format=nul`.

The merge resolves test conflicts in t1901-repo-structure.sh by:
1. Keeping Justin's approach of stripping platform-variant disk sizes from test output
2. Adapting the NUL-format testing to work with Lucas's `-z` flag
3. Changing the test strategy to convert NUL output back to text format for comparison rather than converting expected text to NUL format

The merge appears to maintain all functionality from both branches while cleaning up the test approach. Junio notes the changes and asks the original authors to verify the merged result, particularly the handling of the `-z` flag and the test output normalization strategy. This is a routine integration step in the patch series' progression toward mainline inclusion.