This is a discussion thread continuation about the `git-history` command's multi-branch rewrite behavior, specifically addressing user expectations around explicit control versus automatic branch handling.

Phillip Wood responds to Matthias Beyer's preference for conservative single-branch operations by default in the `git-history` command. Beyer had expressed that as a user, he expects rewriting operations to only affect the current branch unless explicitly told otherwise, citing a principle of least surprise and preference for explicit control. Wood acknowledges this perspective as valuable input, noting that his initial assumption that rewriting all descendant branches would be the natural default behavior isn't universally shared.

The exchange highlights a key design tension in the `git-history` feature between:
1) The convenience of automatically keeping dependent branches in sync when rewriting shared history (Wood's initial position)
2) The safety and explicitness of only modifying the working branch unless specifically instructed otherwise (Beyer's position)

This follows earlier technical discussions about implementation approaches for multi-branch handling, where Phillip Wood had previously proposed a compromise solution involving a `--single-branch` flag to let users choose between the behaviors. The current email shows Wood incorporating user experience feedback into the design considerations, moving beyond pure technical implementation concerns to consider how users will actually interact with the command.

The thread continues to explore the balance between Git's traditional conservative defaults and the desire to improve upon `git-rebase`'s limitations, with this particular exchange focusing on workflow expectations rather than technical implementation details. Wood's response indicates openness to adjusting the default behavior based on user feedback while maintaining the capability for more extensive rewrites when explicitly requested.