Refactoring patch in Patrick Steinhardt's ODB alternates handling series (v2 6/8). This patch follows the elimination of mutual recursion in the previous commit and removes the now-unnecessary forward declaration of `read_info_alternates()`. The function and its dependencies are moved up in the file, and outdated documentation for `read_info_alternates()` is removed since it no longer accurately describes the function's behavior.

The changes are purely organizational with no functional impact, continuing the series' goal of restructuring alternates handling through a source abstraction. The diff shows significant code movement (125 lines changed with net -17 lines) as functions are reordered, but no logic changes. This patch builds on the architectural improvements from earlier in the series, particularly the recursion elimination in v2 5/8, and maintains the clean separation between parsing and linking phases.

Key technical details for future reference:
- Removed forward declaration of `read_info_alternates()`
- Moved `read_info_alternates()` definition before its callers (`odb_source_new()` and `odb_add_alternate_recursively()`)
- Deleted outdated documentation block that described legacy behavior
- Maintained all existing function signatures and behaviors
- Part of broader effort to replace direct filesystem operations with source abstraction for ODB alternates

The patch appears straightforward and well-contained, following the established pattern of the series. It addresses a clean-up opportunity created by the previous recursion elimination without introducing new complexity.