This is a discussion follow-up in the ODB alternates refactoring series, specifically addressing architectural concerns about alternates representation in `struct object_database`.

Patrick Steinhardt responds to Justin Tobler's observation about the current implementation's fragility for write operations, where ODB transactions reorder the source list to control write targets. While agreeing with Justin's assessment that the current approach is suboptimal, Patrick suggests this issue should be addressed separately from the current alternates refactoring series.

The exchange highlights two key architectural pain points: (1) the single-list representation of primary and alternate sources creates fragility during transactions, and (2) write operations only use the first source while treating alternates as second-class citizens. Patrick indicates this warrants future work to either restructure the source representation or modify transaction behavior, though he considers it out of scope for the current series focused on source-based alternates handling.

This discussion doesn't introduce new code changes but surfaces an important architectural consideration that may influence future ODB abstraction work, particularly around transaction handling and source representation. The thread remains technically focused, with both participants aligned on the problem's validity while deferring its solution.