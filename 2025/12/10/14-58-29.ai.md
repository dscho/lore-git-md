This is a merge commit message from Junio Hamano that resolves a conflict between two concurrent patch series: Justin Tobler's "repo-struct-more-objinfo" (adding disk size reporting to `repo structure`) and Lucas Seiki Oshiro's "-z is a synonym for --format=nul" topic.

The key changes in this merge:
- Resolves test conflicts in t1901-repo-structure.sh by:
  - Keeping Justin's disk size reporting tests but modifying the output filtering approach
  - Adapting the NUL-format testing to work with Lucas's -z synonym while maintaining the disk size filtering
- Introduces a new test approach that:
  - Uses grep to filter platform-variant disk sizes from raw output
  - Tests both --format=keyvalue and -z/--format=nul formats
  - Verifies the "last one wins" behavior of format options

Technical details:
- The merge combines commits 09bd4419e7 and b8cacabfa5
- Test changes involve:
  - Output filtering via `grep -v "objects\..*\.disk="`
  - NUL format handling via `tr "\0\n" "\n="` conversion
  - Verification of both keyvalue and nul format outputs
- Maintains SHA1-specific test patterns (as seen in test_expect_success SHA1)

The merge appears to be a straightforward resolution of the format testing conflicts while preserving the functionality of both patch series. The approach of filtering platform-variant disk sizes from test comparisons remains consistent with the series' strategy for handling platform differences in disk size reporting.