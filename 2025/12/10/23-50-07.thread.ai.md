Thread identity: Bugfix for `git add -p` handling of binary file deletions, single patch addressing an edge case in interactive staging.

Core problem: `git add -p` inconsistently handles binary files - while it properly skips modified binary files (as they can't be interactively edited), it incorrectly attempts to process deletions of binary files due to parsing logic that fails to detect the "Binary files" header when the file is marked as deleted in the diff output.

Current status: Patch proposes a fix that makes binary file handling consistent by properly detecting and skipping all binary files (both modified and deleted) during interactive add. The change is small (6 insertions, 4 deletions) and comes with a test case demonstrating the problem and solution.

Key technical details:
- Fixes parsing logic in `add-patch.c`'s `parse_diff()` function to properly detect "Binary files" header even for deleted files
- Updates binary file skipping logic to ignore all binary files regardless of hunk count
- Maintains correct interaction between `file_diff->binary`, `file_diff->deleted`, and `file_diff->hunk_nr` state tracking
- Does not affect behavior for non-binary files
- Includes test case verifying the fix
- Closes Debian bug report #1122502

Open questions: None raised yet - this appears to be a straightforward bugfix with clear reproduction case and solution. The patch maintains existing behavior while fixing the edge case.