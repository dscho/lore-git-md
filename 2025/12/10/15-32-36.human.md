This is the third patch in Patrick Steinhardt's v2 series refactoring Git's object database alternates handling. The patch moves the computation of the normalized object directory path into the `alt_odb_usable()` function (renamed to `odb_is_source_usable()`), eliminating redundant parameter passing.

The change simplifies the calling code by having the function compute its own normalized path rather than requiring callers to provide it. The patch maintains all existing functionality while modernizing the terminology to match the series' broader "source" abstraction approach. The diff shows straightforward mechanical changes - moving path normalization into the function and updating callers accordingly.

This continues the series' pattern of cleaning up the alternates handling code in preparation for supporting different object database backends. The change appears well-contained and builds on the positive feedback received for earlier patches in the series.