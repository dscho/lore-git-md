This is a test improvement suggestion for the final patch in the 6-part series enhancing Git's `repo structure` command with object size reporting. Patrick Steinhardt follows up on his earlier review comments regarding test validation of disk sizes.

The email focuses on a specific test case in t1901-repo-structure.sh that currently strips disk size information due to platform variance. Patrick reiterates his previous suggestion (from patch 5/6 review) that the test could instead use `git rev-list --disk-usage` to compute and validate expected disk sizes, rather than simply skipping them.

This maintains the thread's technical focus on accurate size reporting while addressing test reliability concerns. The suggestion aligns with Patrick's earlier feedback about strengthening test validation for disk size calculations. The change would make the test more robust by verifying the actual disk size computations rather than just their presence in output.

The email doesn't introduce new technical elements but reinforces a testing approach that could be applied to improve validation of the series' disk size reporting functionality. It represents a potential follow-up action rather than requiring immediate changes to the current patch series.