Here's a human-readable summary of the thread:

This thread addresses a bug in `git add -p` where it incorrectly attempts to process deletions of binary files, despite already skipping modifications to binary files (which it can't meaningfully interact with). The issue stemmed from the diff output parser failing to properly detect the "Binary files" header when processing file deletions.

The proposed fix modifies the parsing logic in `add-patch.c` to consistently skip all binary files - whether modified or deleted - during interactive adds. The change is small and focused (6 lines added, 4 removed), with an accompanying test case that demonstrates both the problem and solution. This resolves a Debian bug report (#1122502) while maintaining the expected behavior where `git add -p` simply reports "Only binary files changed" in such cases.

The patch touches the interaction between several state-tracking variables in the file_diff structure (`binary`, `deleted`, and `hunk_nr`), but leaves the behavior for non-binary files unchanged. This appears to be a straightforward fix for an edge case in the interactive add machinery.