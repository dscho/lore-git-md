This is a status update on two of Karthik Nayak's patch series, following up on Junio Hamano's "What's cooking" report. The first series (`kn/ref-location`) introduces GIT_REF_URI for specifying reference backends and directories, which is useful for ref migration. Karthik notes he's currently on leave but plans to address Patrick Steinhardt's review comments in a future version, though this will be delayed. 

The second series (`kn/fix-fetch-backfill-tag-with-batched-ref-updates`) fixes issues with batched ref updates during `git fetch` where conflicting tags could prevent other tags from being fetched. Karthik indicates the only outstanding issue is a typo in test comments, which he considers minor enough that a re-roll may not be needed, though he's willing to provide one if preferred. 

This email continues the thread tracking these two series - one delayed for more substantial revisions, the other nearly ready pending a decision about whether to fix a minor typo. The context shows these are among several active ref-related efforts being monitored in the "What's cooking" thread.