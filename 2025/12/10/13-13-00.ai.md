This is a two-patch series introducing a new utility macro and updating Coccinelle rules, stemming from a bug discussion about uninitialized memory in git-last-modified.

The series addresses two distinct but related to code quality improvements. The first patch introduces a new `MEMZERO_ARRAY()` macro to standardize zeroing dynamically allocated arrays, replacing various ad-hoc `memset()` calls across the codebase. The naming choice (`MEMZERO_ARRAY` over `CLEAR_ARRAY`) is explicitly justified to avoid potential confusion about array resizing. The patch includes conversions in several files (last-modified, simple-ipc, diff-delta, ewah, hashmap, pack-revindex) while noting one exception in add-patch.c where pointer arithmetic makes the macro unsuitable.

The second patch modifies Coccinelle infrastructure to properly pass include paths to spatch, fixing an edge case encountered during development. This appears to be a build system adjustment needed for the new array.cocci rules to work correctly. The series maintains compatibility with both Makefile and meson builds.

The changes appear well-scoped and motivated by a concrete bug report, with clear explanations for design decisions. The patches touch multiple subsystems but follow established patterns, and the exception case in add-patch.c shows thoughtful consideration of edge cases. The coccinelle adjustment suggests the author tested the changes end-to-end.