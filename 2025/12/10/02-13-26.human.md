This is a discussion about Git's flag terminology for index operations, specifically whether to standardize on `--staged` or `--cached` across commands. Jacob Keller responds to Junio Hamano's suggestion that perhaps `--cached` should be kept as the primary term (removing its "obsolete" label) rather than expanding `--staged` usage.

Keller takes the opposite position from Hamano, arguing it would be better to add `--staged` as a synonym to commands that currently only accept `--cached` (like `git rm` and `git grep`). This continues the thread's debate between two approaches: expanding the newer `--staged` terminology versus reinforcing the established `--cached` flag.

The email doesn't introduce new technical arguments but rather positions itself in the ongoing discussion. Keller's stance aligns with the original proposal's direction (making `--staged` consistently available) rather than Hamano's alternative suggestion. The exchange highlights how interface design decisions in Git often balance between consistency with newer conventions and stability with existing usage.