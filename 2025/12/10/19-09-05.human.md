This is a follow-up discussion about test conflicts between two merged patch series - Justin Tobler's object size reporting work and Lucas Seiki Oshiro's `-z` format handling. 

Lucas points out that his change to test `-z` as a synonym for `--format=nul` can be cleanly moved alongside Justin's relocated `--format=nul` test in the test file. The change is minimal - just adding a test case verifying that `-z` works equivalently to `--format=nul` and follows Git's usual "last one wins" rule for conflicting options.

The discussion shows good attention to test organization and conflict resolution between independently developed features. Both authors are working to maintain test coverage while avoiding redundancy. Since both series have already been merged by Junio Hamano, this appears to be a post-merge confirmation that the test changes can coexist cleanly.