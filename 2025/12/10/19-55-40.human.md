This is a discussion about safety mechanisms for Git's proposed `git-history` command, specifically around the handling of immutable branches during history rewriting.

Elijah Newren clarifies his position in response to Martin von Zweigbergk's proposal for configurable immutable ref patterns. While initially confused about how immutability would interact with single-branch operations, Newren now agrees that preventing rewrites of protected branches (when marked immutable) is indeed the desired behavior. This aligns with the safety goals of the immutability proposal, which would block operations on any branch configured as immutable, regardless of whether the user intended to modify just one branch.

The exchange highlights an important clarification: immutability rules would intentionally prevent certain single-branch operations when those branches are protected, creating a stronger safety net than flag-based approaches. This represents a philosophical shift from opt-in safety (via flags) to prevention-based safety (via immutable patterns), with both participants now on the same page about the implications would work in practice.

The discussion continues to explore the tension between workflow flexibility and safety guarantees, with this particular thread resolving a key misunderstanding about how immutability would function in single-branch scenarios.