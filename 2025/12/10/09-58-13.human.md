This is a discussion about the multi-branch handling behavior in the proposed `git-history` command, specifically regarding the `reword` and `split` subcommands. Phillip Wood is engaging with Matthias Beyer's user perspective on how history editing should handle commits that exist in multiple branches.

Matthias expressed that by default, rewriting a commit like B` (which appears in multiple branches) should be denied unless explicitly requested via flags, as it would affect branches beyond the current one. Phillip probes deeper, asking whether this caution stems from concerns about rewriting commits that have been merged upstream, or if there are other reasons. He suggests that if multi-branch rewriting is implemented, the tool should include checks to avoid rewriting commits that have been merged upstream.

The exchange highlights the ongoing debate in the thread about whether `git-history` should default to single-branch operations (Patrick Steinhardt's current approach) or handle multi-branch cases more aggressively (as advocated by Elijah Newren and others). Phillip seems open to multi-branch rewriting if proper safeguards are in place, indicating this is still an active design consideration rather than a settled matter. The discussion remains focused on user expectations and safety mechanisms rather than implementation details.