Refactoring patch in Patrick Steinhardt's ODB alternates handling series (v2 4/8). This patch simplifies `odb_add_to_alternates_file()` by having it call the more precise `odb_add_source()` instead of `link_alt_odb_entries()`. 

The change addresses a subtle mismatch where the function was processing multiple alternates (via newline separation) despite all callers only ever passing single directories. The new implementation better matches the actual usage pattern while maintaining the same core functionality of immediately making new alternates' objects available when alternates are already loaded.

The diff shows a straightforward replacement of one function call with another, keeping the same error handling and lockfile management. This continues the series' theme of separating concerns between parsing and linking alternates, with `odb_add_source()` now handling the single-source case more cleanly. The patch maintains compatibility with existing callers while removing undocumented multi-source processing behavior that wasn't actually being used.