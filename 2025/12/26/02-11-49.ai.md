This is a discussion about potential behavior changes to `git branch --show-current` regarding its handling of the `-v` flag. Junio Hamano clarifies that while the current silent ignoring of `-v` with `--show-current` was intentional (as the feature was originally designed as a simple alternative to `git symbolic-ref HEAD`), he now considers this silent behavior to be buggy since commands should generally error on unrecognized flags. 

The email raises two key technical points: (1) the silent dropping of `-v` should likely be fixed to either properly implement verbose output or error out, and (2) any verbose output implementation for `--show-current` might need a different format than `--list -v` since the two-column indentation used in list mode wouldn't make sense for single-branch output. 

This moves the discussion forward by identifying the silent flag dropping as a clear issue while keeping open the question of what form verbose output should take if implemented. The thread remains in exploratory phase with no concrete implementation proposed yet, but now with clearer direction that some change is needed to either properly support or reject the `-v` flag.