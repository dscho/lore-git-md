This is a discussion about potential feature enhancement to `git branch --show-current` behavior. Junio Hamano responds to Sergey Organov's inquiry about why `-v` is ignored when combined with `--show-current`, explaining the historical context and design intent behind the current implementation.

The response clarifies that `--show-current` was originally created as a simpler alternative to `git symbolic-ref HEAD`, not as a variant of `--list` output. Hamano notes that while adding `-v` support wouldn't face strong objections, the verbose output format might need to differ from `--list -v` since the two-column indentation used in list mode wouldn't make sense for a single branch output.

Key technical points for future reference: the discussion centers on output formatting considerations for a potential `--show-current -v` combination, specifically whether it should maintain parity with `--list -v` or adopt a different format. The thread suggests this would be a new feature rather than fixing an oversight, with backward compatibility not being a major concern since the current behavior simply ignores `-v` rather than erroring out.