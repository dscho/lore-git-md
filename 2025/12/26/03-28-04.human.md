This is a bugfix patch addressing the `git branch --show-current -v behavior` discussion. K Jayatheerth proposes making the command error out when `--show-current` and `-v` are used together, rather than silently ignoring the verbose flag as it currently does.

The patch implements Junio Hamano's suggestion from the thread that the current behavior should be considered buggy and fixed. It adds an explicit error message when both flags are used together, along with a test case to prevent regression. The change is straightforward - just a conditional check before printing the current branch name.

This appears to be a clean, minimal solution to the identified issue. The patch follows the thread's technical direction while avoiding the more complex alternative of implementing verbose output support. The test coverage is appropriate for the change being made. This seems likely to be accepted as it cleanly resolves the bug while leaving open the possibility of implementing verbose support later if desired.