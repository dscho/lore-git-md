[PATCH] builtin/show-branch: replace commit_list with prio_queue

René Scharfe's optimization patch replaces the O(n²) `commit_list_insert_by_date` in `show-branch` with an O(n log n) `prio_queue` (min-heap), significantly improving performance for complex commit graphs. The change modifies `builtin/show-branch.c` to use `prio_queue_peek()` and `prio_queue_replace()` for efficient commit traversal while maintaining identical output behavior. Key technical details:

- Performance tests in `t/perf/p6010-merge-base.sh` show 98.6% speedup in pathological cases with minimal (1%) impact on normal operation
- The new `get_pending` boolean controls whether to replace or add queue entries
- Commit date ordering preserved via `compare_commits_by_commit_date`
- Test changes replace tags with branches to properly exercise `show-branch`

Derrick Stolee validated the approach with monorepo benchmarks showing 23x speedup and confirmed the implementation strategy, particularly the use of `prio_queue_replace()` for first parents to avoid double-sift operations. Stolee explicitly approved with "This patch LGTM". The thread shows consensus forming around this as an uncontroversial performance win, with no open questions or objections raised. Changes remain focused (34 lines implementation, 8 lines test updates) following René's characteristic style of clean data structure improvements.