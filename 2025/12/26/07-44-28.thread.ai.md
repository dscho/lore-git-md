[PATCH] builtin/show-branch: replace commit_list with prio_queue

This patch from René Scharfe optimizes the commit sorting in `show-branch` by replacing the O(n²) `commit_list_insert_by_date` with an O(n log n) `prio_queue` (min-heap). The change primarily affects `builtin/show-branch.c`, modifying the commit storage and traversal logic while maintaining identical output behavior. Key technical details:

- The priority queue implementation uses `prio_queue_peek()` and `prio_queue_replace()` to minimize heap rebalancing operations
- A new performance test in `t/perf/p6010-merge-base.sh` demonstrates the improvement: 98.6% faster in pathological cases with negligible (1%) impact on normal operation
- The patch preserves all existing functionality while improving worst-case performance
- Changes are limited to 34 lines in the main implementation and 8 lines in test infrastructure

The optimization is particularly valuable for repositories with complex commit graphs where the previous quadratic behavior could become noticeable. René's implementation follows his characteristic style of focused, clean improvements to core data structures. No open questions or objections are raised in the initial submission - this appears to be a straightforward performance win.