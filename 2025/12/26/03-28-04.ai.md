This is a bugfix patch addressing the `git branch --show-current -v` behavior discussed in the ongoing thread. The patch takes the first of the two potential resolution paths identified in the discussion - making the combination of flags error out rather than silently ignoring `-v`.

The change is straightforward: it adds an explicit error when `--show-current` and `-v` are used together, with a clear error message explaining the conflict. The patch includes a corresponding test case to verify the new behavior. This implements the "make it error out" approach that was one of the options under consideration in the thread.

Key technical details:
- Modifies `builtin/branch.c` to add the error check before `print_current_branch_name()`
- Uses `die()` with a user-friendly message showing the conflicting options
- Adds test case `git branch --show-current rejects -v` in t/t3200-branch.sh
- Maintains the existing behavior when `--show-current` is used without `-v`

The patch aligns with Junio Hamano's revised position that the current silent behavior is buggy and needs fixing. It chooses the simpler resolution path (erroring out) rather than implementing verbose output support, which would require more design decisions about formatting. The implementation looks correct and well-contained, with appropriate test coverage.