This is a technical discussion about performance optimization in `git pack-refs` for repositories with high ref counts, specifically examining the tradeoffs around object verification and peeling.

Brian m. carlson responds to Jeff King's analysis of why `pack-refs` performs expensive object verification, particularly the need to peel tags by reading object headers. They push back against the suggestion to skip verification for `refs/heads/` refs, noting that while branches should point to commits, manual ref updates could create invalid states that the code should still handle safely.

The key new consideration is whether the reftable backend could avoid this overhead entirely by maintaining pre-peeled references. This suggests reftable might offer inherent performance advantages for high-refcount scenarios like Gerrit servers, though the thread hasn't yet explored implementation specifics or whether this would fully replace the current verification needs. The discussion remains focused on fundamental design constraints rather than proposing concrete code changes.