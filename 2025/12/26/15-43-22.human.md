This is a follow-up discussion in the thread about `git branch --show-current -v` behavior. Sergey Organov responds to Junio Hamano's earlier comments about potentially implementing verbose output for the current branch display.

The email focuses on clarifying the use case: Sergey wants a concise way to see upstream branch information for the current branch, finding `git branch --show-current -vv` more natural than existing alternatives. He demonstrates the current workarounds (`git br -lvv master` or nesting commands) are either imprecise or overly verbose.

An interesting tangent emerges about Git's lack of a shorthand symbol for the current branch (like `@@` or `@HEAD`), which Sergey suggests would make such commands more elegant. This isn't directly related to the original `-v` discussion but shows the conversation expanding into broader ergonomic considerations for branch operations.

The thread appears to be winding down after the earlier decision to explicitly reject `-v` with `--show-current` rather than implement verbose output. This email doesn't propose new technical changes but provides useful context about real-world usage patterns that could inform future improvements to branch command ergonomics.