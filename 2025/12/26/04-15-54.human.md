This is a bugfix patch for `git branch --show-current` behavior, following up on a thread about how it silently ignores the `-v` verbose flag. The patch makes the command explicitly error out when `--show-current` and `-v` are used together, rather than silently dropping the verbose flag.

K Jayatheerth's implementation adds a check in `builtin/branch.c` that dies with a clear error message when both flags are present, along with a test case in t/t3200-branch.sh. Junio Hamano acknowledges this as an improvement over the current behavior, though notes that a more thorough solution would also catch cases where verbose mode might be disabled through other means (like `-q` quiet flag).

The patch appears well-contained and correct, addressing the core issue while maintaining backward compatibility. Junio indicates he will queue it despite the theoretical edge case, suggesting this likely concludes the thread's discussion unless new arguments emerge. The solution aligns with the thread's consensus that silent flag ignoring was buggy behavior.