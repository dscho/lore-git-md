This is a follow-up discussion in the `git branch --show-current -v` behavior thread, where the initial proposal to support verbose output has been resolved with an error check instead. Sergey Organov responds to Junio Hamano's technical considerations about verbose output format differences between `--show-current` and `--list`.

The email explores alternative workflows for viewing current branch status, noting that while the rejected `--show-current -v` approach would have been convenient, users can currently chain commands like `git br -lvv $(git br --show-current)`. Organov then raises a new tangential question about why Git lacks a shorthand symbol (suggesting `@HEAD` or `@@`) to reference the current branch, comparing it to Perl's `$_` idiom. This shifts focus from the original flag behavior discussion to potential syntax sugar for branch references, though no concrete proposal is made. The thread's core resolution remains unchanged - error checking for `-v` with `--show-current` will be implemented as previously agreed.