This is a performance optimization follow-up discussing René Scharfe's patch to replace `commit_list` with `prio_queue` in `show-branch`. Derrick Stolee provides validation and additional benchmarking data, confirming the significant performance improvements (23x faster in his monorepo tests) while analyzing the implementation details.

Key technical points from Stolee's review:
- Confirms the optimization strategy of using `prio_queue_replace()` for the first parent to avoid double-sift operations
- Notes the new `get_pending` boolean controls whether to replace or add queue entries
- Verifies the queue maintains commit date ordering via `compare_commits_by_commit_date`
- Observes the test changes replace tags with branches to properly exercise `show-branch`

The review provides strong validation of the patch's approach and results, with Stolee explicitly stating "This patch LGTM" (Looks Good To Me). The discussion focuses on implementation mechanics rather than raising any concerns, suggesting this optimization is likely to proceed uncontroversially. The thread shows consensus forming around this as a clear performance win for repositories with complex commit graphs.