This is a discussion about error handling in the `git replay` command, specifically around the `--onto` and `--advance` options. Phillip Wood is responding to Junio C Hamano's suggestion about improving error messages when invalid commit references are provided.

Phillip agrees with Junio's idea to move error handling to the callers of `peel_committish()` rather than handling it within the function itself, since there are only two callers (for `--onto` and `--advance`). He notes that `--advance` already has its own specific error message ("argument to --advance must be a reference"), suggesting the focus should be on improving just the `--onto` error handling.

The exchange shows the developers working through the tradeoffs between centralized vs. caller-specific error handling, with Phillip providing concrete observations about the current behavior. This continues the thread's technical discussion about how to best communicate errors to users while maintaining clean code structure.