This is a discussion about error handling in the `git replay` command, specifically around the `peel_committish()` function that processes `--onto` and `--advance` arguments. Phillip Wood responds to Junio Hamano's suggestion about improving error messages by noting there are only two callers to the function, making caller-specific error handling feasible. He observes that `--advance` already has its own validation ("must be a reference"), implying the error handling could be simplified to focus just on `--onto` validation.

The exchange highlights an implementation detail that wasn't fully addressed in the original patches - while the initial fix improved error messages for commit resolution failures, there's now discussion about whether the validation responsibility should shift from the helper function to its callers where more specific context is available. Phillip's observation about `--advance`'s existing validation suggests the current implementation might have some redundancy.

Key technical points for future reference:
- The debate centers on `peel_committish()` in `builtin/replay.c`
- Two call paths exist (for `--onto` and `--advance`)
- `--advance` already enforces reference validation separately
- Junio's original suggestion was to move error reporting to callers for more context
- This could lead to simplifying the helper function's role

The discussion shows the thread moving from initial error message improvements to considering architectural changes in error handling responsibility.