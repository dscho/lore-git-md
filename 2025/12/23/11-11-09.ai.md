This is a documentation patch in the LOP (Large Object Promisors) series, specifically documenting the `--filter` option for `git fetch` to match its existing documentation in `git clone`.

The patch adds clear documentation for the `--filter=<filter-spec>` option in `fetch-options.adoc`, explaining how to use partial clone functionality with `git fetch`. It describes the feature's purpose (requesting a subset of reachable objects) and provides concrete examples of filter specifications (`blob:none` and `blob:limit=<size>`), with a reference to `git-rev-list` for more details. The documentation follows the same style and format as the existing `clone` documentation, maintaining consistency across commands.

This is a straightforward documentation addition that fills a gap in the existing docs, ensuring users understand they can apply the same filtering capabilities to `fetch` as they can to `clone`. The change is minimal (10 lines added) and well-contained, with no implementation changes or behavioral impact. As part of the larger LOP series, it helps document one of the key features being enhanced (filter specifications in promisor remote operations).