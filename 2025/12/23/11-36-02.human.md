This is a follow-up discussion in the ongoing feature enhancement thread about showing default branch comparisons in `git status`. 

Harald Nordgren responds to Junio C Hamano's earlier technical suggestion about reusing existing branch tracking infrastructure. The key technical point here is that when a new branch is created and then pushed with `--set-upstream`, the branch's tracking configuration (`branch.<name>.merge` and `branch.<name>.remote`) gets set to the push target rather than maintaining any reference to the default branch (upstream/HEAD). 

This demonstrates a real limitation in trying to reuse the existing tracking logic for the default branch comparison feature - the tracking information gets overwritten when users follow Git's own suggested workflow for setting up new branches. Harald's message implies that new logic may indeed be necessary to properly implement the default branch comparison feature, as the current tracking infrastructure doesn't preserve the needed reference points.

The exchange shows the discussion moving into deeper technical implementation details, with the author providing concrete examples of how current Git behavior would interfere with the proposed feature. This suggests the solution will need to account for these edge cases in branch setup workflows. The thread continues to refine the technical approach while maintaining focus on the original goal of better visibility into mainline drift.