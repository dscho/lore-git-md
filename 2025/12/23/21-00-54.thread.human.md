This thread addresses a user's confusion between `git remote --add` (which doesn't exist) and the correct `git remote set-url --add` command. Andrew Chitester clarifies that the documentation is accurate - the user misread the help output, attempting to use `--add` as a direct option to `remote` rather than as part of the `set-url` subcommand. The response confirms there's no actual documentation bug, just a straightforward case of user misunderstanding, and politely redirects to the properly documented usage. No code or documentation changes are needed.