This is the fourth patch in a 9-part series enhancing Git's promisor remote functionality. It continues the refactoring work started in earlier patches to prepare for new promisor features.

The patch converts the `filter_options` variable in `builtin/fetch.c` from a static global to a local variable within `cmd_fetch()`, passing it explicitly through the call chain. This affects several helper functions including `prepare_transport()`, `do_fetch()`, `backfill_tags()`, `fetch_one_setup_partial()`, and `fetch_one()`. The change improves code organization and makes ownership clearer, though it requires modifying many function signatures.

This is a straightforward refactoring with no behavior changes, maintaining the existing filter handling while making the code more maintainable for the upcoming patches that will build on this infrastructure. The diff shows careful attention to updating all call sites and proper cleanup with `list_objects_filter_release()`. Like previous patches in the series, this appears to be a well-executed preparatory change.