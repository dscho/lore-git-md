Here is the updated dense thread summary incorporating the new documentation patch:

This thread contains two related patch series enhancing Git's promisor remote functionality, both now technically complete and ready for merging:

1. The 9-patch LOP (Large Object Promisors) series (v4) that:
   - Implements `promisor.storeFields` config for secure storage of remote fields
   - Fully implements `--filter=auto` mode for dynamic filter combination
   - Has received final approval from Patrick Steinhardt after addressing Jeff King's NULL pointer fix
   - Is rebased and ready for merging into master

2. Christian Couder's URL-based auto-configuration series (v3) that:
   - Adds `promisor.acceptFromServerUrl` config for pattern-based remote configuration
   - Supports glob patterns (`*`) with security constraints (exact scheme/port, host boundary rules)
   - Auto-generates remote names with `promisor-auto-` prefix and collision handling
   - Stores original advertised names in `remote.<name>.advertisedAs`
   - Includes comprehensive URL matching tests in t5710-promisor-remote-capability.sh
   - Now includes the final documentation patch (8/8) that:
     - Clarifies `acceptFromServer` doesn't create remotes (vs `acceptFromServerUrl`)
     - Reformats text into clear bullet points
     - Makes default "none" option more visible
     - Fixes "client" vs "server" terminology
     - Links to new `acceptFromServerUrl` option
     - Improves readability while maintaining technical accuracy
   - Incorporates all prior technical refinements including:
     - New `url_normalize_pattern()` helper
     - Removed WM_PATHNAME flag
     - Explicit security documentation
     - Simplified wildmatch() handling
     - Test modernization
     - Boolean type refactoring
     - New helper functions for remote name generation
     - Auto-configuration logic
     - Core infrastructure additions
     - URL matching implementation

Key technical details:
- Core files modified: `promisor-remote.[ch]`, `urlmatch.[ch]`
- Documentation: promisor.adoc updated with clearer formatting and cross-references
- Test file t5710 updated with modernized patterns and new test scenarios

Review status:
- Both series have completed all technical review cycles with:
  - Patrick Steinhardt's approval
  - Junio's positive feedback
  - Toon Claes' detailed verification
- The documentation patch finalizes the series without requiring further review

Open items:
- Future GSoC 2026 project ("Implement promisor remote fetch ordering") remains planned
- No outstanding technical or documentation issues

The combined work represents significant progress in promisor remote functionality, with both series now fully complete including all technical implementations and final documentation improvements. The v3 URL-based auto-configuration series is ready for integration alongside the previously approved LOP series.