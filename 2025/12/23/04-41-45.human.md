This is a documentation patch fixing outdated comments in the xdiff patience diff implementation. The patch updates comments that still referred to an old `ha` member variable that was split into `line_hash` and `minimal_perfect_hash` in a previous refactoring (commit 6a26019c81f).

The change is straightforward - it updates the explanatory comment to correctly reference `minimal_perfect_hash` instead of the obsolete `ha` variable. The comment explains how this value is used in the patience diff algorithm, noting that it's a linearized version of the hash that starts at 0 and increments.

This appears to be a clean, focused documentation fix that maintains accuracy after an earlier code change. The patch is small (13 lines changed) and targets a specific subsystem (xdiff). While not earth-shattering, it's the kind of maintenance work that helps keep the codebase's documentation in sync with implementation.