This is a technical discussion about error handling refinement in `git replay`, specifically around the `--onto` and `--advance` options. Junio Hamano builds on Phillip Wood's observation that `peel_committish()` has only two callers, making caller-specific error handling feasible. 

The email focuses on two code paths in `determine_replay_mode()` where `peel_committish()` is called - for `--onto` and `--advance` options. Junio suggests moving error handling to these call sites for more specific error messages. He notes the existing asymmetry where `--advance` already performs additional reference validation through `repo_dwim_ref()`, which enforces a stricter "must be a reference" requirement beyond just commit resolution.

Junio proposes restructuring the `--advance` handling to perform the reference validation first (via `repo_dwim_ref`) before calling `peel_committish()`, which would make the error handling flow more logical. This would create parallel validation paths for both options while maintaining `--advance`'s stricter requirements. The discussion remains focused on implementation details rather than introducing new functionality, continuing the thread's theme of improving error reporting architecture in `git replay`.