This thread covers an 8-patch series (v2) enhancing Git's promisor remote functionality, focusing on secure storage of remote fields and dynamic filter combination. The series has seen extensive review from Patrick Steinhardt across most patches, with Jean-Noël Avila providing documentation feedback and Christian Couder contributing to both security implementation and auto-filter infrastructure.

The latest addition is a straightforward documentation patch that fills a gap by adding `--filter` option documentation to `git fetch`, matching the existing text for `git clone`. While the wording could potentially be improved, the patch maintains consistency between related commands for now. This follows Jean-Noël Avila's earlier suggestions about documentation formatting in the series.

Key technical changes in the series include new `promisor.storeFields` config for secure remote field storage, `--filter=auto` mode for dynamic filter combination, and various refactorings to improve code clarity and resource management. The patches touch multiple areas including promisor-remote handling, list objects filtering, and the clone/fetch commands. The series appears to be converging, with most patches having addressed reviewer feedback in the v2 iteration.