Refactoring patch in the LOP (Large Object Promisors) series. This is the first of 9 patches enhancing Git's promisor remote functionality. The patch extracts common initialization code from `fields_sent()` and `fields_checked()` in `promisor-remote.c` into a new `initialize_fields_list()` helper function, preparing for additional similar functions to be added later.

The change is straightforward - it eliminates duplicate initialization code that sets up string lists for promisor remote field handling. The refactored version maintains identical behavior while making the code additions in subsequent patches cleaner. The patch touches only `promisor-remote.c`, modifying how field lists are initialized while keeping all existing functionality.

This appears to be a clean preparatory refactor with no behavior changes, setting up for the more substantial promisor remote enhancements that follow in the series. The commit message clearly explains the motivation (avoiding duplication for upcoming additions) and the change is well-contained to its stated purpose.