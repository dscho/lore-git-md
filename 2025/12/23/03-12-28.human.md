This is a review comment on a bugfix patch for `git replay`'s error handling. Junio C Hamano is responding to Kristoffer Haugsbakk's patch that improved error messages when invalid commit-ish arguments are provided to `--onto` or `--advance` options.

The review notes that while the patch's error message for failed `repo_get_oid()` is reasonable, there's still a case where `peel_committish()` can return NULL (when `parse_object()` fails) that isn't being handled with a similar descriptive error. Hamano suggests it might be better to move the error reporting to the callers where there's more context about which option the invalid argument was provided to.

The comment maintains the thread's focus on improving error reporting quality while raising a valid technical point about where error handling should live. This appears to be a thoughtful review that could lead to either adjusting the current patch or adding follow-up improvements to cover the remaining NULL case. The exchange shows the careful attention being paid to making `git replay`'s error messages as helpful as possible.