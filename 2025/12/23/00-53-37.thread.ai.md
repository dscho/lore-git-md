Harald Nordgren's configurable branch comparison feature for `git status` (status.compareBranches) has been merged to 'next' after completing all technical review. The 2-patch series (v31) enables configurable @{upstream}/@{push} comparisons while maintaining default @{upstream} behavior, concluding a multi-year contribution effort.

Implementation details:
- First patch (v31 1/2) refactors branch comparison formatting from `format_tracking_info()` into new `format_branch_comparison()` helper while preserving existing behavior
- Second patch (v31 2/2) adds `status.compareBranches` config option with space-separated @{upstream}/@{push} support
- Core changes in remote.c with new `resolve_compare_branch()` helper
- String list processing with strset deduplication
- Context-aware advice messages (pull advice only for upstream, push advice only for push branches)
- 354 test lines in t6040-tracking-info.sh covering:
  * Ahead/behind/diverged states
  * Triangular workflows
  * --no-ahead-behind output
  * Single-branch configurations
  * Duplicate branch handling
  * Remapped refspec cases
- Documentation complete on config order preservation and duplicate handling

Final changes addressed all review feedback:
- Fixed inconsistent `git -C` vs subshell usage in tests
- Renamed and expanded deduplication test case
- Added explicit tests for single-branch cases
- Documented deduplication behavior
- Style fixes and test organization improvements

Junio Hamano has now contributed follow-up documentation and test improvements to clarify behavior that was implemented but not fully documented:
- Explicitly documents that branch comparisons are shown in config order
- Documents duplicate entries resolving to same ref are suppressed
- Documents when `@{upstream}` and `@{push}` resolve to same branch, only one comparison shows
- Test updates demonstrate this behavior more clearly by:
  * Adding explicit tests for single-branch configurations
  * Renaming test branches to avoid confusion
  * Expanding the deduplication test case
  * Using `git -C` consistently instead of subshells
  * Verifying order preservation in the output

Participant positions:
- Junio Hamano: Approved merge after final test review, contributed final documentation polish
- Jeff King: Brokered final technical approach
- Harald Nordgren: Implemented through 31 iterations over two months (plus 2016-2018 efforts)
- Kristoffer Haugsbakk: Clarified administrative comments

The feature is now fully documented and tested, handling various workflow scenarios while maintaining backward compatibility. It serves as a case study for persistent contribution through the mailing list workflow, with all social protocols followed and technical concerns resolved before merging. The implementation includes robust advice message context control via ENABLE_ADVICE_PULL/PUSH/DIVERGENCE flags and comprehensive test coverage of edge cases including remapped refspecs.