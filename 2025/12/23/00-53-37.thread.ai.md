Harald Nordgren's 2-patch series (v25) enhancing `git status` to show divergence from both upstream and push tracking branches was merged into Git. The implementation introduced:

1. Core refactoring with `format_branch_comparison()` helper centralizing formatting logic while maintaining existing behavior including quick mode (AHEAD_BEHIND_QUICK) and all output formats.

2. Push tracking implementation with `get_remote_push_branch()` helper and extended `format_tracking_info()` handling both upstream/push, including 285-line test coverage in t6040-tracking-info.sh. The feature shows divergence info for both upstream and push when they differ, with output on new lines and preserved quick mode behavior.

Post-merge discussion continues regarding workflow ergonomics, particularly around triangular workflows. Jeff King (Peff) detailed his specific workflow where he uses a GitHub mirror (`github` remote) for backup and CI purposes, with branches tracking `origin/master` as upstream while pushing `jk/*` branches to the mirror once daily via script. In this case, the push-tracking status provides noise rather than useful information.

The exchange reveals differing workflow perspectives - Harald Nordgren had suggested configuring more frequently used push destinations, while Peff's established mirror-based workflow serves his needs well. This may inform future configuration options to suppress push status in such cases while preserving upstream tracking information.

Key technical details remain unchanged from the merged implementation. The discussion surfaces considerations for potential follow-up work:
- Configuration options for selective push status suppression
- Workflow guidance around push destinations in triangular setups
- No concrete changes proposed yet, but real-world usage patterns informing future directions

Key participants: Harald Nordgren (implementor), Jeff King (workflow concerns), Phillip Wood (earlier push.default feedback), Junio C Hamano (merger). The thread demonstrates how merged features continue evolving based on usage patterns and workflow preferences, with Peff's detailed workflow example providing concrete input for potential refinements.