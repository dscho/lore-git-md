Harald Nordgren's 2-patch series (v25) enhancing `git status` to show divergence from both upstream and push tracking branches has been approved by Junio C Hamano for merging into `next`. The implementation consists of:

1. Core refactoring (patch 1/2):
 * `format_branch_comparison()` helper centralizes formatting logic
 * Extracted from `format_tracking_info()` for unified upstream/push handling
 * Maintains existing behavior including quick mode (AHEAD_BEHIND_QUICK)
 * Final variable naming: `cmp_fetch`/`push_cmp_fetch` per Junio's direction
 * Clean separation of concerns between caller and helper
 * Preserves all output formats and advice messages
 * Includes final pluralization fix kept in series

2. Push tracking implementation (patch 2/2):
 * `get_remote_push_branch()` helper for push reference resolution (simplified by removing redundant `full_ref_out` parameter)
 * Extended `format_tracking_info()` handling both upstream/push
 * Renamed flags to ENABLE_ADVICE_PULL/PUSH/DIVERGENCE for clarity
 * 285-line test coverage in t6040-tracking-info.sh (expanded from v24)
 * Handles remapped push refspecs
 * Shows divergence info for both upstream and push when they differ
 * Maintains same output format on new line for push tracking
 * Preserves quick mode (`--no-ahead-behind`) behavior
 * Divergence advice now shows for both upstream and push branches when enabled
 * Final resolution of advice display discussion with Phillip Wood
 * Maintains all edge case handling from v23

Key changes in v25:
 * Added test case for upstream-ahead/push-up-to-date scenario
 * Renumbered test branches to avoid potential conflicts
 * Confirmed advice display works correctly in all divergence scenarios

Implementation details:
 * Push branch comparison appears on new line using same format as upstream status
 * Example: shows both relationships when tracking `origin/main` but pushing to `origin/feature`
 * Test coverage now includes:
   - Diverged upstream vs ahead push
   - Upstream ahead but push up-to-date
   - Remapped push refspecs
   - Multiple remote configurations
   - `--no-ahead-behind` behavior
   - Push default interactions
   - Advice display when upstream diverges but push branch matches

Open considerations:
 * None remaining - all feedback addressed

Key participants:
 * Harald Nordgren: Implementation and responding to feedback
 * Phillip Wood: Provided detailed feedback on push.default and advice display
 * Junio C Hamano: Approved for merging
 * Ben Knoble: Earlier workflow advice
 * Patrick Steinhardt: Flag conventions input

Status:
 * Both patches finalized and merge-ready in `next`
 * Core functionality approved by Junio
 * All major technical requirements met
 * Documentation note added regarding versioning conventions
 * Final advice-display cleanup implemented
 * Phillip's feedback on `push.default` handling fully addressed
 * Implementation now fully respects all `push.default` configurations
 * Expanded test coverage confirms robust behavior in all scenarios

The implementation cleanly extends `git status` output while maintaining existing behavior after 25 iterations of review. The series follows project conventions and represents a complete feature ready for wider testing in `next`, with all edge cases now properly covered through expanded test scenarios in v25. The discussion shows the project's attention to detail even at the final stages of patch review.