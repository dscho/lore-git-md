Harald Nordgren's 2-patch series (v25) enhancing `git status` to show divergence from both upstream and push tracking branches was merged into Git. The implementation introduced:

1. Core refactoring with `format_branch_comparison()` helper centralizing formatting logic while maintaining existing behavior including quick mode (AHEAD_BEHIND_QUICK) and all output formats.

2. Push tracking implementation with `get_remote_push_branch()` helper and extended `format_tracking_info()` handling both upstream/push, including 285-line test coverage in t6040-tracking-info.sh. The feature shows divergence info for both upstream and push when they differ, with output on new lines and preserved quick mode behavior.

Post-merge discussion continues exploring workflow ergonomics, particularly triangular workflows. Jeff King (Peff) detailed his specific workflow using a GitHub mirror (`github` remote) for backup/CI with branches tracking `origin/master` while pushing `jk/*` branches to the mirror via script - here push-tracking status provides noise rather than useful info. Harald Nordgren responded by suggesting `push.default=upstream` combined with explicit push destinations in scripts as an alternative approach, while sharing his own pre-feature workflow of manual upstream setting and origin pushing (the gap his feature addressed).

The exchange reveals differing workflow philosophies - Harald's feature assumes more frequently used push destinations while Peff's established mirror-based workflow serves different needs. This informs potential future configuration refinements:
- Options for selective push status suppression
- Workflow guidance for triangular setups
- No immediate code changes proposed, but real-world usage patterns being gathered

Key technical details remain unchanged from merged implementation. The discussion surfaces considerations around:
- `push.default=upstream` configuration
- `git branch --set-upstream-to` manual setup
- Scripted push patterns
- Original feature motivation (missing status in manual push workflows)

Key participants: Harald Nordgren (implementor, offering workflow alternatives), Jeff King (detailing mirror-based workflow), Phillip Wood (earlier push.default feedback), Junio C Hamano (merger). The thread demonstrates how merged features evolve through real-world usage feedback, with Peff's workflow example and Harald's response providing concrete input for potential future refinements while maintaining the feature's core value proposition.