This is the fourth patch in a 9-part series enhancing Git's promisor remote functionality. It's a refactoring change that makes the `filter_options` variable in `builtin/fetch.c` local to `cmd_fetch()` rather than being a static global.

The patch moves the `list_objects_filter_options` struct from file scope into `cmd_fetch()` and passes it through several helper functions (`prepare_transport()`, `do_fetch()`, etc.) that need access to the filter settings. This 48-line change (27 additions, 21 deletions) improves code organization by making the data flow more explicit, which will be helpful for the upcoming patches that build on this functionality.

The change is mechanical and maintains existing behavior - it just makes the ownership of the filter options clearer. This continues the series' pattern of preparatory refactors before introducing new promisor remote features in later patches. The implementation looks straightforward and well-contained, with all necessary function signatures updated to pass the filter options through the call chain.