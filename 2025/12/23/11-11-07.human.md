This is a refactoring patch in a series enhancing Git's promisor remote functionality. It's the third of nine patches in the initial submission.

The patch makes a technical improvement to `builtin/clone.c` by converting a static global `filter_options` variable into a local variable within `cmd_clone()`. This clarifies ownership of the variable's lifecycle and makes the code easier to follow, especially as subsequent patches will make more use of this variable. The change requires passing the filter options as a parameter to the `checkout()` function.

The change is small (16 lines modified) and mechanical in nature, focused on code organization rather than behavior changes. It's part of the preparatory work for later patches in the series that will implement new promisor remote capabilities. The commit message clearly explains the motivation and notes that this is a "small quite cheap cleanup" - an accurate assessment given the straightforward nature of the changes.

As with the earlier patches in the series, this appears to be a well-considered change with a clear purpose in the context of the larger effort. The patch maintains existing functionality while making the codebase more maintainable for the features to come.