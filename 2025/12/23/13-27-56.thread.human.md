The thread introduces a new whitespace rule called `tab-between-non-ws` to catch documentation typos where tabs appear between non-whitespace characters in AsciiDoc files, as suggested by Junio Hamano. The initial implementation in `ws.c` with comprehensive test coverage correctly detects these cases while avoiding false positives for tabs at tab stops.

Junio has now reviewed the series and identified two remaining gaps: the colored diff output doesn't properly highlight these violations, and the whitespace fixing functionality doesn't yet handle converting the flagged tabs to spaces. He also clarified edge case behavior, confirming that end-of-line tabs should remain the domain of the existing trailing-whitespace check. The implementation appears otherwise solid, with the final changes needed being relatively contained before the feature can be considered complete.