This is a test patch review in the ongoing `git replay` error handling series. Phillip Wood is reviewing Kristoffer Haugsbakk's additional test cases for failure conditions in `git replay`. 

The review notes a technical issue with the first test case - using a pipe after `test_must_fail` means the test won't properly detect if the command succeeds, since the pipe swallows the exit code. Phillip confirms the other test cases look good, which cover:
1. The requirement for either `--onto` or `--advance` options
2. Error when attempting to replay down to root commit
3. Incompatibility between `--advance` and `--contained` options

This continues the thread's focus on improving test coverage for `git replay`'s error conditions, following earlier patches that fixed confusing error messages. The review is constructive, catching a subtle testing pitfall while approving the overall approach. The test additions appear well-motivated to catch regression in the command's input validation.