Harald Nordgren's merged series enhancing `git status` to show divergence from both upstream and push tracking branches continues to be refined through post-merge discussion. The core functionality is stable, but configuration decisions remain open.

Jeff King has clarified how `@{push}` behaves in triangular workflows (where push and fetch remotes differ), showing it works with `push.default=current/matching` but not `simple`. This technical detail informs the ongoing debate about whether the comparison should be enabled by default. D. Ben Knoble argues for default-on to support triangular workflow users, while Jeff maintains his conservative default-off position based on UI cleanliness concerns.

Junio Hamano's original configuration proposal (`status.compareBranches`) remains pending as participants work through the implications. The discussion has surfaced important edge cases around remote configuration that will need documentation, particularly around when `@{push`} resolution succeeds or falls back to `@{upstream}`. While the implementation is merged, these final design questions about defaults and discoverability continue to be hashed out between the key participants.