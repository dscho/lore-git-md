This thread explores the real-world implications of a recently merged enhancement to `git status` that shows divergence from both upstream and push tracking branches. The original implementation by Harald Nordgren (v25) introduced centralized formatting logic while maintaining existing behavior, with thorough test coverage. While the core functionality works as intended, the discussion has revealed several distinct workflow patterns that interact with it in different ways.

Jeff King (Peff) first described his mirror-based CI workflow where branches track `origin/master` but push elsewhere, noting potential conflicts with `push.default=upstream`. Harald Nordgren then responded with configuration alternatives (`simple` or `nothing`) that could prevent unwanted push destinations while reducing status noise. Now, Harald shares his own interactive development setup using `remote.pushDefault=origin` and `push.default=current`, which makes bare pushes convenient while still benefiting from the new status information. This exchange has documented three distinct real-world patterns: Harald's manual push setup, Peff's scripted mirror workflow, and D. Ben Knoble's similar mirror workflow with implicit push destinations.

The thread continues to explore configuration ergonomics - particularly around `push.default` settings and their interaction with different workflows (scripted vs interactive) - without suggesting changes to the stable core implementation. Participants including Harald Nordgren, Jeff King, and D. Ben Knoble are contributing practical insights that may inform future refinements to how Git handles these triangular workflow patterns.