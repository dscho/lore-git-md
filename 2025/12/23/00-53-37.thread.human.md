This thread continues the discussion about configuring `git status` output to show divergence from both upstream and push tracking branches, building on Harald Nordgren's merged series. The core implementation in `format_branch_comparison()` is stable, with push tracking handled via `get_remote_push_branch()`, tested in t6040-tracking-info.sh.

Recent discussion between Jeff King (Peff) and Phillip Wood has focused on workflow preferences, particularly for triangular workflows where fetch and push remotes differ. Both find the exact ahead/behind counts less useful than simple binary states - knowing if the local branch is behind upstream (signaling a potentially difficult rebase) or differs from the push branch (indicating a needed push). Jeff notes the counts are mainly useful for detecting misconfigured tracking branches. 

The conversation suggests designing the configuration system to eventually support both detailed and simplified output modes, though this isn't urgent. Participants confirm that `push.default=current` is a common setting for triangular workflow users, and the discussion continues to balance immediate needs with long-term configurability. Key open questions remain around the final configuration schema, default settings, and documentation needs - particularly for `@{push}` resolution in triangular workflows. The thread maintains its focus on refining the user experience while keeping the stable core implementation intact.