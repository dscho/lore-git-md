Here's the human-readable summary of the thread's current state:

This thread began with Harald Nordgren's enhancement to `git status` that shows divergence from both upstream and push tracking branches, which has now been merged. The core technical work is complete, with discussion shifting to how users should control this feature.

Jeff King (Peff), who originally implemented the `@{push}` syntax, has been a key voice in shaping the conversation. While he agrees with the technical merits of the feature, he strongly advocates for making it opt-in rather than enabled by default, citing limited user demand and the importance of maintaining clean output for most workflows. Junio Hamano has proposed a flexible `status.compareBranches` configuration to control the display.

The current discussion focuses on finding the right balance between discoverability and user experience. Peff suggests following Git's traditional approach of introducing new features as opt-in first, allowing real-world usage to determine if they should become defaults. There's consensus on the configurability design but some skepticism about needing comparisons beyond the basic `@{upstream}` and `@{push}` cases.

With the implementation merged and the configuration design largely agreed upon, the thread is waiting for Junio to implement the configuration mechanism. The remaining open questions center on the exact syntax and whether to support more complex comparison scenarios in the future.