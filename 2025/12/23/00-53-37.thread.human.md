This thread documents the completion of Harald Nordgren's multi-year effort to make branch comparison in `git status` configurable. The feature, now in its 26th iteration, allows users to specify which branches to compare (upstream, push, or both) via the new `status.compareBranches` config option. The implementation includes triangular workflow support and context-aware advice messages, with Jeff King having resolved all memory management concerns in the final version.

Junio Hamano has queued v26 of the series, marking technical completion. Jeff King's latest review confirms the memory leak fixes are satisfactory and that the default behavior (showing both upstream and push branch comparisons) remains unchanged from previous versions. He notes one minor pre-existing quirk in advice message handling that doesn't affect this feature's readiness. 

All implementation issues are now resolved, with only documentation updates remaining as a follow-up task. The thread has reached consensus that the feature is ready for release, concluding a significant enhancement to `git status` that maintains backward compatibility while providing new configuration flexibility for branch comparison behavior.