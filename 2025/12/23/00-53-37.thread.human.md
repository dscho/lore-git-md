This thread centers on enhancing `git status` to show divergence from both upstream and push tracking branches, implemented in a 2-patch series by Harald Nordgren. The feature evolved from initial custom configuration to leveraging Git's existing branch tracking (`branch.<name>.pushRemote`) after feedback from Junio Hamano and Ben Knoble.

The first patch refactors the branch comparison logic, extracting it into a new `format_branch_comparison()` function while carefully preserving all edge case behaviors. After 17 iterations of review, Phillip Wood confirmed the refactoring maintains correctness while improving code organization. Key changes include parameter renaming for clarity (`sti` → `up_to_date`), type safety improvements, and proper handling of quick mode cases where branch state must be checked separately from commit counts.

The second patch implements the push tracking display, adding comparison against the push branch when it differs from upstream. Phillip Wood's latest review suggests potential refinements to reference resolution and advice display, but confirms the core functionality is sound.

All technical requirements are satisfied, with comprehensive test coverage in place. The series is now merge-ready, with one identified pre-existing quick mode edge case to be addressed separately. Junio Hamano provided crucial technical direction on control flow, while Phillip Wood's thorough reviews ensured code quality throughout the extensive refinement process. The changes maintain Git's output conventions while adding the requested tracking visibility.