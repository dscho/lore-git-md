This thread centers on Harald Nordgren's implementation of configurable branch comparison in `git status`, allowing users to specify which branches to compare (like `@{upstream}` and `@{push}`) via a new `status.compareBranches` setting. The feature is structurally complete with thorough test coverage, but Jeff King recently identified and is addressing a critical memory management issue in the legacy push-tracking code that affects the implementation.

While the technical foundations are being solidified, the discussion has expanded to include usability considerations. Chris Torek has now weighed in, supporting Ben Knoble's earlier suggestion to default to including both `@{upstream}` and `@{push}` comparisons. Torek notes this would particularly benefit triangular workflows common on GitHub forks, where tracking both the original upstream and the user's fork is valuable. He acknowledges the added complexity when additional remotes are involved but sees merit in having the capability available by default.

The thread shows steady progress on two fronts: Jeff King is handling the low-level memory management fixes, while high-level design questions about default configuration and workflow integration continue to be refined. With the memory issues being actively addressed, the feature appears to be nearing readiness for merging, pending resolution of these final configuration decisions.