This thread documents the diverse real-world workflows enabled by Harald Nordgren's merged enhancement to `git status` that shows divergence from both upstream and push tracking branches. The discussion has evolved into a knowledge-sharing session about configuration patterns, with recent contributions from Jeff King and Junio Hamano.

Jeff described his mirror-based CI setup where he occasionally pushes to a bare repository, highlighting how a bare `git push` would fail if it defaulted to his upstream remote (where he lacks write access). Junio agreed this illustrates how different workflows require different configurations. Jeff also shared a practical use of `@{push}` with `git range-diff` to compare branch versions, which Junio appreciated as a useful workflow tip.

The thread has surfaced four distinct workflow patterns: Harald's interactive setup using `remote.pushDefault`, Jeff's scripted mirror workflow, D. Ben Knoble's predefined mirror destinations, and traditional manual push configurations. While no code changes are proposed (the feature is already merged), the discussion serves as valuable documentation of how these configurations interact with Git's push tracking capabilities, potentially informing future ergonomic improvements. Key participants include Harald (the implementer), Jeff (sharing detailed workflow examples), and Junio (facilitating the discussion post-merge).