Updated AI thread summary:

This thread introduces and implements a new whitespace rule `tab-between-non-ws` to detect tabs between non-whitespace characters in AsciiDoc documentation files, addressing a suggestion from Junio Hamano to catch documentation typos where tabs are incorrectly used in running text. The implementation adds a `WS_TAB_BETWEEN_NON_WS` flag to `ws.c` with careful logic to only flag tabs that expand to a single space (avoiding false positives at tab stops).

The feature includes:
- Core detection in `ws.c` with new flag and tab expansion logic
- `.gitattributes` configuration applying the rule to `*.adoc` files
- Comprehensive test coverage in `t/t4015-diff-whitespace.sh` for edge cases
- Confirmed handling of tabs at end-of-line via existing trailing-whitespace check

Adrian Ratiu has addressed Junio's review comments by implementing:
1. Proper BG_RED highlighting for violations in colored diff output
2. Whitespace fixing support in `ws_fix_copy()`

Key technical details:
- New helper function for colored output of violations
- Updates to `diff_colors[]` for highlighting
- Whitespace fixing converts flagged tabs to single spaces
- Commit message improvements explaining false positive avoidance

The implementation is now complete with all requested functionality implemented and tested. The thread shows consensus on the approach and no remaining open questions. The feature is ready for merging pending submission of the final v2 patch series incorporating the coloring and whitespace-fix components.