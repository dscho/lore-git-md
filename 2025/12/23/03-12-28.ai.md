This is a review comment on a bugfix patch in the `git replay` error handling series. Junio C Hamano examines the proposed change to `peel_committish()` that makes it die with a descriptive error when an invalid commit-ish is provided. He agrees the new error message is reasonable for the repo_get_oid() failure case, but points out the function can still return NULL in other cases (when parse_object() fails), meaning callers must still handle NULL returns.

The key technical observation is that while the patch improves one error path, the function's contract remains partially unchanged - it can still silently return NULL in some cases. Junio suggests it might be better to move error handling to the callers where more context is available to craft specific messages (like mentioning which option the invalid argument was provided to). This implies the current patch may not fully address the error reporting goals of the series.

The comment maintains focus on the technical details of error propagation while questioning the architectural choice of where to place the error handling. It doesn't reject the patch outright but prompts reconsideration of the error reporting strategy's completeness. The thread context shows this builds on earlier work to improve `git replay`'s error messages for invalid commit-ish arguments.