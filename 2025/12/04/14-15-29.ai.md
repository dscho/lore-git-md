Hook subsystem refactoring (part 5/11): pre-push hook conversion to structured API.

This patch converts the pre-push hook implementation in transport.c to use the new hook API introduced earlier in the series. The key technical changes involve replacing the direct `child_process` usage with the structured `run_hooks_opt` approach, including a new `pre_push_hook_feed_stdin` callback function that handles the ref status streaming to the hook's stdin.

The conversion maintains all existing functionality while simplifying the implementation by:
1. Moving ref filtering logic into the new `pre_push_hook_feed_stdin` callback
2. Using the standard `strbuf` pattern for ref data formatting
3. Automating EPIPE/SIGPIPE handling through the hook API infrastructure
4. Preserving the same ref status filtering (skipping non-pushed refs)
5. Keeping the same transport name/URL argument passing

The patch demonstrates the series' goal of standardizing hook execution while maintaining backward compatibility. It builds on parts 2-4's stdin handling infrastructure, showing how the `feed_pipe` callback mechanism can be applied to a real-world hook that processes incremental input. The implementation follows the established pattern seen in the post-rewrite hook conversion (part 4), with similar memory safety practices around strbuf management.

Review context shows this conversion was previously reviewed and signed off by Ævar Arnfjörð Bjarmason, confirming the stdin callback approach aligns with the series' technical direction. The v4 iteration appears polished, with no outstanding technical concerns noted in the thread.