This is a follow-up in the ongoing discussion about making `git replay` drop commits that become empty during replay. The thread has reached technical consensus on the core behavior change, with maintainer approval and all implementation details resolved.

Phillip Wood checks in with Junio C Hamano about whether to base the next version of the patch series on `master` rather than the in-progress `ps/history` branch. This appears to be the final administrative question before the RFC can be rerolled as a final submission.

The exchange shows the patch is nearing completion, with only this minor workflow question remaining before it can progress from RFC status. Junio had previously indicated the current base was acceptable for the RFC stage, but Phillip is offering to adjust if preferred.