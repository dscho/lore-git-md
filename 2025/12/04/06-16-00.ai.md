This is a build system patch that concludes the cross-compilation improvements for Git's Meson build system. Patrick Steinhardt is reviewing and approving the final patch in Toon Claes' series.

The patch standardizes on using `meson.can_run_host_binaries()` for execution tests during cross-compilation, replacing an earlier use of `meson.is_cross_build()` that was too restrictive. The change ensures the build system properly handles cases where an exe_wrapper is configured to run cross-compiled binaries. One remaining `is_cross_build()` check is intentionally left in place for filesystem operations, as confirmed in earlier discussion.

The approval from Patrick (who had previously suggested this standardization) indicates the series has addressed all cross-compilation issues identified, with clear technical rationale for the remaining filesystem check. The changes are minimal (just 3 lines modified across two patches) but make the build system more robust for cross-compilation scenarios while maintaining native build behavior.

This concludes the thread with all patches reviewed and approved, ready for potential inclusion. The series demonstrates careful attention to build system edge cases, particularly around the distinction between execution tests and filesystem operations in cross-compilation contexts.