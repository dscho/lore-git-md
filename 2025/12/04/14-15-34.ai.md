This is part 10 of the hook subsystem refactoring series, converting receive-pack's update and post-update hooks to the new structured API. The patch focuses on integrating the sideband output handling capability introduced in part 9, while maintaining the existing sequential execution behavior.

The key changes replace direct `child_process` management with the new hook API, specifically:
- Replaces manual hook path lookup and process setup with `run_hooks_opt()`
- Implements `hook_output_to_sideband()` callback to handle output redirection when sideband is enabled
- Simplifies both `run_update_hook()` and `run_update_post_hook()` by removing explicit process management code
- Maintains identical functionality while using the new structured approach

The conversion demonstrates the API's ability to handle both per-ref update hooks and batched post-update hooks while preserving the sideband output capability. The changes are straightforward mechanical conversions that leverage the infrastructure built up in previous patches, particularly the output capture functionality from part 9.

This patch appears to be a clean application of the established patterns from earlier in the series, with no new technical challenges introduced. It completes the receive-pack hook conversions while maintaining all existing behavior, including sideband support. The changes are well-contained to receive-pack.c and follow the API usage patterns established in previous conversions.