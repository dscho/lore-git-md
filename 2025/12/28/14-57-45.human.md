This is a bugfix for a receive-pack crash when handling symbolic refs that point outside their namespace, with an open question about the security implications of allowing such updates.

Junio C Hamano acknowledges Troels Thomsen's fix for the NULL dereference crash in `check_aliased_update_internal()`, but raises a deeper concern about namespace integrity. The current patch makes receive-pack handle these cross-namespace symref updates without crashing, but Junio questions whether this is actually desirable behavior from a security perspective.

The core issue is that namespaces are meant to segregate references, and allowing updates through symrefs that cross namespace boundaries could undermine that isolation. Junio suggests an alternative approach where receive-pack would explicitly reject such updates with an error rather than permitting them, while still avoiding the crash.

This moves the discussion from a straightforward crash fix to considering the broader design implications of namespace enforcement. The thread now awaits input on whether the current behavior (allowing the updates) is intentional or if the security boundary should be strengthened by rejecting them.