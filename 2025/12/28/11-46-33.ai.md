This is a final polish pass on the test suite for the now-stable `status.goalBranch` feature, addressing shell scripting style issues in the test cases. Junio C Hamano provides detailed feedback on test formatting, specifically:

1. Recommends redirecting `git status` output directly to the actual file rather than capturing the entire subshell output, which makes the test intent clearer.

2. Explains proper use of `<<-\EOF` here-documents with tab stripping, showing how to indent the heredoc content for better readability while maintaining correct shell behavior.

3. Suggests using the `$SQ` variable for single quotes in test expectations, which would allow keeping the heredoc delimiter as `<<-EOF` (without backslash) for variable interpolation.

The feedback focuses exclusively on test presentation quality, not touching the already-stable implementation logic. Junio stops reviewing after establishing the pattern, trusting the author to apply similar fixes throughout. This marks the final round of review for this feature, with all technical aspects settled and only minor test formatting improvements remaining before merge.