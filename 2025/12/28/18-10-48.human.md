This is part 1 of 4 in René Scharfe's series continuing the `the_repository` removal effort, focusing on tag-related code. The patch makes `parse_tag_buffer()` properly repository-aware by eliminating its implicit dependency on the global `the_hash_algo`. Instead, it now uses the hash algorithm from the passed-in repository parameter.

The change is small but meaningful - it updates the buffer size check and OID parsing to use `r->hash_algo` rather than the global variable. This affects core tag parsing logic that's used by various commands. The diff shows careful attention to detail, with the author converting both explicit and implicit uses of the global (the latter via `parse_oid_hex()`).

As part of a systematic cleanup from a trusted contributor, this patch appears straightforward and low-risk. It follows the established pattern for `the_repository` removal work, though reviewers will likely want to verify the change doesn't affect any callers that might pass NULL for the repository parameter. The commit message could be more explicit about why this particular case needs fixing, but the technical change itself looks solid.