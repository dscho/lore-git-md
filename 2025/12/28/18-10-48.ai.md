This is part 1 of 4 in René Scharfe's series continuing the `the_repository` removal effort, specifically targeting tag-related code. The patch focuses on making `parse_tag_buffer()` properly repository-aware by eliminating its implicit dependency on the global `the_hash_algo`.

The change replaces two instances where the function was using the global hash algorithm - once directly for size calculation and once indirectly through `parse_oid_hex()`. The new version uses the repository's `hash_algo` member instead, switching to `parse_oid_hex_algop()` which takes the hash algorithm as a parameter. The diff is small (4 insertions, 2 deletions) but touches a security-sensitive area of tag parsing.

This appears to be a straightforward technical cleanup that follows the established pattern for `the_repository` removal - pushing repository awareness up the call chain. The change affects core tag parsing logic but maintains identical behavior when called with the main repository. No test changes are included, which is somewhat notable given the security implications of tag parsing, though the changes are mechanical enough that existing tests likely provide sufficient coverage.

For future reference in this thread: the key technical points are the switch from `the_hash_algo` to `r->hash_algo` and the use of `parse_oid_hex_algop()` instead of `parse_oid_hex()`. The changes propagate through to any callers of `parse_tag_buffer()`, which includes various tag-related operations across the codebase.