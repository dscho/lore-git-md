This is part 3 of René Scharfe's series removing `the_repository` dependencies from tag handling code. The patch focuses on making `parse_tag()` repository-aware by adding a `struct repository` parameter and updating all callers to pass either `the_repository` or their own repository pointer.

The changes are mechanical but widespread, touching 8 files including core tag operations in `tag.c`, plus callers in describe, pack-objects, fsck, ref-filter, and other commands. The key technical change is modifying `parse_tag()` to take a repository parameter and use it for both object reading (via `odb_read_object()`) and tag parsing (via `parse_tag_buffer()`). One caller (`object-name.c`) already has its own repository context from a previous refactor and passes that instead of `the_repository`.

The patch follows the established pattern from earlier series parts - maintaining identical behavior when called with `the_repository` while allowing proper repository isolation. No test changes are included as this is a straightforward refactoring that shouldn't affect observable behavior. The diffstat (13 insertions vs 13 deletions across 8 files) shows the focused nature of the changes.

This continues the systematic elimination of global state from tag handling, with the remaining work (removing final `the_repository` dependencies) presumably coming in patch 4/4. The changes are low-risk but touch security-sensitive tag parsing paths, though the mechanical nature and lack of test changes suggest confidence in existing coverage.