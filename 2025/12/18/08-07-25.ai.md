This is a technical discussion about the PID file naming scheme for lockfile debugging, part of the ongoing effort to improve stale lock debugging. Patrick Steinhardt raises concerns about the proposed `foo-pid.lock` naming convention, pointing out it creates a new restriction where references named "foo" and "foo-pid" cannot be modified in the same transaction. While rare, this could affect some users.

Patrick suggests an alternative using forbidden refname characters (like `foo:pid.lock`) which would avoid the collision issue, though notes this relies on implementations properly rejecting invalid refnames. He weighs tradeoffs: the performance impact of writing two files per ref update in the files backend versus the debugging benefits, and the increased chance of stale files after crashes. His key points are that this should be opt-in rather than opt-out, and that the current proposal may not be the final solution.

The discussion continues to explore edge cases and implementation details, with Patrick's email focusing on the files backend implications and naming constraints. This builds on previous technical analysis while introducing new considerations about refname validity rules as a solution path. The thread remains focused on production hardening with particular attention to edge cases and backward compatibility.