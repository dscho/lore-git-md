This is a bugfix patch that's part of an 8-part series improving object info reading in Git's object database layer. The patch addresses an inconsistency in the `odb_source_loose_read_object_info()` function where early return paths weren't properly setting the `whence` field to `OI_LOOSE` in the object_info struct.

The change ensures the `whence` field is always set when successfully reading loose object info, even in quick-check cases where the full object isn't opened. While the commit notes this isn't currently causing issues, it prevents potential future bugs by maintaining consistent behavior across all code paths. The fix touches the object-file.c file and modifies the early return logic to properly set the OI_LOOSE flag before returning.

This appears to be a straightforward and well-justified change that improves code consistency in the object database handling code. The patch is cleanly implemented with clear testable behavior, though it doesn't include new tests since it's fixing a latent issue rather than an active bug. As the first patch in a larger series, it sets up proper infrastructure for subsequent changes that may depend on consistent object info reporting.