Bugfix follow-up confirming and endorsing the performance regression fix. Aaron Plattner, who originally reported the severe performance degradation (where `git commit` times ballooned from 0.38s to 1508s), verifies that Junio C Hamano's patch successfully restores the original performance.

The fix addresses a regression introduced by the recently merged ps/object-read-stream topic, where static "blank" object_info structs were incorrectly assumed to be shared between functions. The patch removes these problematic static variables and adds explicit NULL checks throughout the object handling code in object-file.c, odb.c, and packfile.c.

Plattner suggests adding a "Fixes:" line referencing commit 385e18810f10") and provides both Tested-by and Reviewed-by tags, indicating thorough validation of the fix's effectiveness. The email serves as confirmation that this was indeed the right approach to solve the performance issue, with no new concerns raised about the solution.