This is a technical review comment on patch 2/18 of the Windows symlink support series, focusing on the `stat()` implementation. Johannes Sixt points out that the commit message's mention of `ELOOP` handling appears disconnected from the actual code changes, suggesting it might be a leftover from earlier development when symlinks were optional. The patch itself implements proper symlink resolution in `mingw_stat()` by using `CreateFileW` and `GetFileInformationByHandle` to follow symlinks, replacing the previous behavior that treated `stat()` like `lstat()`. 

The reviewer notes the patch looks good technically, but flags a minor inconsistency in the commit message documentation. This is a small clarification request rather than a substantive issue, as the series has maintainer approval and is otherwise ready for merging. The comment helps ensure the historical record accurately reflects the implementation.