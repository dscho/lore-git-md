This is a follow-up discussion about the performance regression fix in object handling code, specifically addressing Junio Hamano's proposed minimal fix.

Patrick Steinhardt reviews Junio's patch which removes the problematic use of static "blank" object_info structs as NULL substitutes. The patch modifies object-file.c, odb.c, and packfile.c to handle NULL object_info pointers directly rather than using blank structs. Patrick confirms the changes look correct and will properly fix the severe performance regression (1508s -> 0.38s for `git commit`).

Notably, Patrick mentions he will rebase his own more comprehensive patch series on top of Junio's fix, suggesting the two approaches are compatible. His series (referenced in the footnotes) appears to include additional optimizations beyond just fixing the NULL handling issue.

The email indicates consensus is forming around Junio's minimal fix as the immediate solution, with Patrick's additional improvements potentially coming later. The technical review suggests this is a solid fix that properly addresses the core issue while maintaining clean code structure.