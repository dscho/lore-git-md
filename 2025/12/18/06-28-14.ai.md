This is patch 4/8 in a series improving object info reading, focusing on ensuring pack-specific information is consistently populated. The patch addresses two cases where pack info was missing: when calling `packed_object_info()` directly rather than through `packfile_store_read_object_info()`, and when handling empty requests (though the latter case is noted as currently unused, to be fixed in the next patch).

The changes move pack info population earlier in `packed_object_info()` and ensure the blank request case still sets basic pack metadata (marking the delta type as "unknown"). This maintains consistency for callers expecting the `u.packed` struct to be populated regardless of request type. The diff shows careful restructuring to avoid code duplication while preserving all existing behavior.

This continues the series' pattern of tightening object info handling invariants in preparation for future work. The changes are localized to packfile.c and maintain backward compatibility while making the interface more predictable. The patch appears well-scoped and technically sound, following the series' incremental improvement approach.