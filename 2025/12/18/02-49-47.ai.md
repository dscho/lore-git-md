This is a minor correction to patch 10/18 in the Windows symlink support series. Ben Knoble points out a typo in Karsten Blees's commit message where "would" was misspelled as "woutl". The original patch (part of the now-complete v5 series) deals with handling directory symlinks in the Windows-specific `mingw_unlink()` implementation.

The correction doesn't affect the technical content of the patch, which properly handles the Windows API behavior where `_wunlink()` and `DeleteFileW()` return ERROR_ACCESS_DENIED when attempting to delete symlinks to directories. The series as a whole has already been approved for merging, with this being a purely editorial fix for the commit message.

Key technical context remains unchanged from the thread summary: the patch is part of comprehensive symlink support for Windows, specifically addressing directory symlink handling in unlink operations. The series touches `compat/mingw.c` extensively and has undergone thorough review, with this being a late-spotted typo in documentation rather than any functional issue.