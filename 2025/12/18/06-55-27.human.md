Refactoring patch in Patrick Steinhardt's series to move packfile storage to object database sources. This is patch 8/10 in the ongoing effort to enable pluggable ODBs by properly tracking packfile ownership.

The patch inlines the `find_kept_pack_entry()` function into its only caller `has_object_kept_pack()`, eliminating an unnecessary layer of indirection. It also reorders the code to remove the function declaration from packfile.h, making it file-local to packfile.c. The changes are straightforward and maintain the same behavior while simplifying the code structure.

This continues the series' pattern of cleaning up transitional functions after the architectural shift in patch 5/10. The changes are mechanical and focused - removing now-redundant code paths as the packfile storage model becomes more direct. The patch follows the established pattern of the series and appears to be a logical next step in the refactoring.