This is a refactoring patch in Patrick Steinhardt's ongoing series to move packfile handling to the ODB source level. The patch inlines the `find_kept_pack_entry()` function into its sole caller `has_object_kept_pack()`, then makes the combined function file-local by reordering code to eliminate the need for a declaration in packfile.h. 

Justin Tobler reviews this as part of his ongoing review of the 10-patch series, noting a minor typo ("allow" -> "allows") but otherwise approving the changes. The modification is straightforward - combining two simple functions and cleaning up visibility - and fits with the series' broader goal of simplifying packfile-related code as it transitions to the new ODB architecture.

Key technical details: The patch removes `find_kept_pack_entry()` from packfile.h and merges its logic directly into `has_object_kept_pack()`, which was previously just a thin wrapper. This reduces indirection in the kept-pack lookup path while maintaining identical functionality. The change is purely internal - no behavior or interfaces visible outside packfile.c are affected.

The review confirms this patch continues the series' pattern of mechanical, well-contained refactoring steps that incrementally improve the code structure without introducing new complexity. With Justin's approval, this appears ready to proceed as part of the larger series.