Bugfix for refs debug output. This patch fixes a logical error in `refs/debug.c` where the debug output for `optimize_required` was incorrectly checking the pointer address rather than dereferencing the boolean value it points to. The fix changes `required ? "yes" : "no"` to `*required ? "yes" : "no"` to properly reflect the intended boolean value.

The issue caused the debug trace to always print "yes" regardless of the actual optimization requirement, making the output misleading for debugging purposes. The change is small and focused, directly addressing the core problem without introducing additional complexity.

While the patch appears correct and well-scoped, the commit message could be more detailed about the practical impact - it doesn't explain where/how this debug_optimize_required function is used or who might be relying on its output. The change seems straightforward enough that it's unlikely to be controversial.