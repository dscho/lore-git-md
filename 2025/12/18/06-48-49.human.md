This is a refactoring patch in Patrick Steinhardt's ongoing series to move packfile handling to the ODB source level. It's patch 8/10 in the series, which Justin Tobler has already approved with a typo correction.

The change inlines `find_kept_pack_entry()` into its only caller `has_object_kept_pack()`, eliminating an unnecessary layer of indirection in the kept-pack lookup path. The combined function is then made file-local by removing its declaration from packfile.h, further simplifying the interface. Justin confirmed the change is correct and noted only a minor typo in the commit message (now fixed).

This is a straightforward cleanup that follows the series' architectural direction of simplifying packfile operations. The patch has already been reviewed positively and appears ready to proceed as part of the larger refactoring effort.