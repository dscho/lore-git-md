This is a refactoring patch in the Windows symlink support series, specifically addressing code cleanup in the MinGW compatibility layer. Johannes Sixt confirms the correctness of Karsten Blees' patch that simplifies the `mingw_lstat()` implementation by removing the now-unnecessary `do_lstat()` helper function.

The change eliminates redundant code paths by recognizing that `do_lstat()` was only ever called with `follow=0` after earlier refactoring. The patch removes 20 lines of conditional logic that previously handled both symlink-following (`stat()`) and non-following (`lstat()`) cases, since the symlink-following behavior is now fully handled by `mingw_stat()`. The remaining implementation directly sets the symlink attributes when detecting a reparse point.

Key technical details: The patch touches only `compat/mingw.c`, removing the dual-purpose `do_lstat()` function and its associated comment block, while preserving the core symlink detection logic (checking for `FILE_ATTRIBUTE_REPARSE_POINT` and `IO_REPARSE_TAG_SYMLINK`). The simplified `mingw_lstat()` now directly sets `S_IFLNK` mode without the previous conditional path for following symlinks. Johannes Sixt confirms this is "obviously correct" and withdraws his earlier comment about documentation cleanup from patch 1/18 since the relevant comment is being removed here.

This represents a straightforward cleanup after architectural changes in earlier patches of the series, with no functional changes to symlink handling behavior. The removal of dead code and simplification of the control flow improves maintainability of the Windows-specific stat implementation.