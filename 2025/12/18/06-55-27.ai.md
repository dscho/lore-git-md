This is patch 8/10 in Patrick Steinhardt's series refactoring packfile storage to track packs per object database source. The patch inlines the `find_kept_pack_entry()` function into its sole caller `has_object_kept_pack()`, eliminating an unnecessary layer of indirection. 

The changes move the code flow directly into `has_object_kept_pack()` and reorder the functions to remove the now-unneeded declaration from packfile.h. This continues the series' pattern of simplifying packfile access by reducing abstraction layers and making ownership more explicit. The patch touches packfile.c and packfile.h, removing 24 lines while maintaining identical functionality.

Key technical details:
- Eliminates `find_kept_pack_entry()` by moving its logic into `has_object_kept_pack()`
- Reorders functions to allow removing the declaration from packfile.h
- Keeps the same pack entry lookup behavior through ODB sources
- Follows the series' architectural direction of simplifying packfile operations

This is a straightforward refactoring that builds on the architectural changes from earlier patches, particularly the packfile store relocation in patch 5/10. The change is mechanical and low-risk, focusing on code organization rather than behavior modification. It continues the series' progression toward explicit source-level packfile management.