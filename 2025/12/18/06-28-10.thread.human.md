This thread continues Patrick Steinhardt's work to improve object database handling in Git, now focusing on standardizing how packfile metadata is populated. The v2 series (7 patches) refactors object info reading to be more consistent and performant, while laying groundwork for future `odb_for_each_object()` functionality.

The latest patch (4/7) ensures `packed_object_info()` consistently fills all pack-related metadata - including offset, pack pointer, and delta type - eliminating previous inconsistencies where this data was only set by some callers. This change simplifies the code by removing duplicate logic while maintaining the new 4-state delta tracking (introduced in patch 3/7) that distinguishes between unknown, full, ref-delta and ofs-delta objects.

The series has seen careful review and iteration, with patch 2/7 simplifying the interface by removing the `OI_DBCACHED` distinction after maintainer confirmation. Performance improvements are already showing ~9% speedups for size-only requests. The work coordinates with Patrick's parallel packfile-store-in-odb-source effort, with attention to maintaining backward compatibility during interface changes.

Key files affected include odb.h, object-file.c and packfile.c, with changes that will benefit several subsystems including repacking and promisor pack handling. The thread demonstrates Git's incremental approach to refactoring - each patch moves toward cleaner interfaces while preserving stability, with clear attention to how the changes fit into the broader odb abstraction effort.