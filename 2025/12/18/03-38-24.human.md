This is a follow-up question in the ongoing discussion about PID files for lock debugging. Ben Knoble raises an important technical consideration about the proposed `.pid.lock` files: whether the lock cleanup behavior during abnormal termination matches the PID file cleanup behavior.

The core observation is that if locks aren't automatically released during crashes (while PID files are), the debugging information would be lost precisely when most needed. This touches on the thread's central purpose of improving stale lock debugging, suggesting a potential need to verify or align the cleanup behaviors.

The question doesn't propose any modifications but highlights an implementation detail that could affect the utility of the feature. It comes late in a well-developed thread where version safety and tempfile handling have already been discussed, now focusing on operational edge cases. The response to this could influence final testing or documentation of the feature's limitations.