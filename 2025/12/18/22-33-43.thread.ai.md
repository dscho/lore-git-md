Here is the dense AI thread summary for this bundle-uri bugfix patch:

This thread addresses a bugfix for the bundle-uri subsystem where malformed bundle entries could cause a segfault. The patch adds validation to catch two error cases: missing URI fields and "url" vs "uri" typos. Core changes include config parsing validation in `bundle_uri_parse_config_format()` using `hashmap_for_each_entry` and NULL checks in `fetch_bundle_uri_internal()`. Test coverage via `test-tool bundle-uri parse-config` verifies both error conditions, with error messages identifying problematic bundles by ID or "<unknown>". The changes are narrowly focused on preventing NULL dereferences while maintaining clear error reporting. No open questions or outstanding review comments are present in this initial submission. The patch appears ready for review with thorough test coverage for the specific edge cases addressed.