This is a minor review comment on patch 1/18 of the Windows symlink support series. Johannes Sixt (Hannes) points out that while the patch removes redundant `GetFileAttributes()` calls in `mingw_lstat()`, it doesn't update the associated comment that describes the now-obsolete double-call behavior. The comment currently explains the workaround for trailing slashes that the patch just eliminated.

The observation doesn't affect the technical correctness of the patch (which has already been approved for merging) but notes a documentation oversight. This is a small cleanup opportunity in code that handles Windows path normalization and stat operations, part of the foundational work for the symlink series. The thread context shows this was the first infrastructure patches in the 18-part series that has since been fully reviewed.