Thread identity: Bugfix patch (v2) addressing memory leaks in macOS fsmonitor implementation (CoreFoundation paths not being freed), with corrected author name from initial submission.

Core problem: The Darwin-specific fsmonitor listener code fails to properly release CoreFoundation paths (`cfar_paths_to_watch`, `cfsr_gitdir_path`, `cfsr_worktree_path`) in both error and normal shutdown paths, causing memory leaks. Version 2 corrects only the author name from initial submission.

Key technical details:
- Adds `CFRelease()` calls in `fsm_listen__ctor` error path and `fsm_listen__dtor`
- Adds missing `CFTypeRef` type definition in Darwin GCC compatibility header
- Changes limited to `compat/fsmonitor/fsm-listen-darwin.c` and `compat/macos/gcc-compat.h`
- Follows CoreFoundation memory management conventions
- No functional changes from v1 to v2 (only author name correction)

Current status:
- Patch at version 2 with trivial author correction
- Still lacks detail about impact/discovery method in commit message

Open questions:
- Reviewers may still request more context about impact or discovery method
- Brief commit message could be expanded with more technical rationale

Participant positions:
- Patch author identified the leak and provided fix (name corrected in v2)
- No reviews or acks yet (second submission)

Related work:
- Part of ongoing fsmonitor maintenance (cross-platform watchman integration)
- Follows pattern of Darwin-specific memory management fixes
- Builds on previous discussion about CoreFoundation path handling