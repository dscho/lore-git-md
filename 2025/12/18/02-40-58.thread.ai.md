Thread identity: Bugfix patch addressing a memory leak in macOS fsmonitor implementation (CoreFoundation paths not being freed).

Core problem: The Darwin-specific fsmonitor listener code fails to properly release CoreFoundation paths (`cfar_paths_to_watch`, `cfsr_gitdir_path`, `cfsr_worktree_path`) in both error and normal shutdown paths, causing memory leaks.

Key technical details:
- Adds CFRelease() calls in `fsm_listen__ctor` error path and `fsm_listen__dtor`
- Adds missing `CFTypeRef` type definition in Darwin GCC compatibility header
- Changes limited to `compat/fsmonitor/fsm-listen-darwin.c` and `compat/macos/gcc-compat.h`
- Follows CoreFoundation memory management conventions

Open questions:
- No details provided about impact or discovery method (reviewers may request more context)
- Brief commit message could be expanded with more technical rationale

Participant positions:
- Patch author identified the leak and provided fix
- No reviews or acks yet (first submission)

Related work:
- Part of ongoing fsmonitor maintenance (cross-platform watchman integration)
- Follows pattern of Darwin-specific memory management fixes