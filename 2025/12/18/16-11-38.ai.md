Bugfix addressing memory leaks in macOS fsmonitor CoreFoundation path handling. This is version 2 of the patch, with the only change being the author's name correction from the initial submission.

The patch adds proper cleanup of CoreFoundation paths in both error and shutdown paths for the Darwin fsmonitor listener. Key technical changes include:
- Adding `CFRelease()` calls for `cfar_paths_to_watch`, `cfsr_gitdir_path`, and `cfsr_worktree_path` in `fsm_listen__ctor`'s error path and `fsm_listen__dtor`
- Adding missing `CFTypeRef` type definition in the Darwin GCC compatibility header
- Modifying `compat/fsmonitor/fsm-listen-darwin.c` and `compat/macos/gcc-compat.h`

The commit message remains quite terse, simply stating the paths "are not freed even though they should be" without additional context about impact or discovery method. The changes appear straightforward and follow CoreFoundation memory management conventions, though reviewers may request more detailed rationale about how the leaks were discovered and their practical impact.

This continues the thread's focus on fixing memory management in the Darwin-specific fsmonitor implementation, building on previous discussion about CoreFoundation path handling. The patch is narrowly scoped to the identified leaks without introducing broader changes to the fsmonitor subsystem.