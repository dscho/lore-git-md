This is a documentation patch fixing a stale comment in the test infrastructure. Johannes Schindelin corrects a comment in `t/test-lib-functions.sh` that was accidentally left unchanged when the `test_detect_hash` function was copy-edited to create `test_detect_ref_format` in commit 58aaf59133b. The comment incorrectly still referred to detecting hash algorithms rather than ref formats.

The change is straightforward - just updating a single line to accurately describe what the function does. The patch was noticed by Matthew John Cheetham and carries his Noticed-by tag. This appears to be a simple but useful correction to keep the test code's comments accurate.