This is a refactoring patch that's part of a larger series standardizing Git's hook subsystem. It introduces the first of several planned helper functions to make parallel process state checks more readable in `run-command.c`.

The patch extracts the anonymous child process state structure into a named `parallel_child` struct and adds a `child_is_working()` helper function to check if a child process is in the `GIT_CP_WORKING` state. This replaces several direct state comparisons throughout the file with the more descriptive helper function. The change is purely structural - there's no behavior modification.

As the first patch in an 11-part series that's now on its fifth iteration, this establishes groundwork for subsequent changes that will add more state helpers and hook-related functionality. The thread context shows this series has undergone extensive review and is nearing completion, with maintainers having signed off on key design decisions. This particular patch appears straightforward and uncontroversial, focusing on code organization improvements.