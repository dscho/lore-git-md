Patch review in the ODB refactoring series. This is patch 8/10 in Patrick Steinhardt's ongoing work to move packfile handling from object databases to their owning sources, part of the larger effort to enable pluggable ODBs.

Justin Tobler reviews a straightforward cleanup patch that inlines `find_kept_pack_entry()` into its only caller `has_object_kept_pack()`, then makes the combined function file-local by reordering code. The change removes an unnecessary layer of indirection in the kept-pack lookup path while maintaining the same functionality. Justin notes a minor typo ("allow" vs "allows") but otherwise approves the change.

This continues the series' pattern of mechanical refactoring to simplify the packfile storage architecture. The patch appears uncontroversial - it's a clean, focused change that removes dead weight from the codebase without introducing new complexity. With Justin's approval, this seems ready to proceed alongside the rest of the series.