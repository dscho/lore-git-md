This thread documents a straightforward documentation fix for Git's test infrastructure. The patch corrects a stale comment in `t/test-lib-functions.sh` that was overlooked when the `test_detect_hash` function was adapted to create `test_detect_ref_format` in an earlier commit. Johannes Schindelin submitted the patch after Matthew John Cheetham noticed the discrepancy. The change simply updates the comment to refer to "ref format" instead of the outdated "hash algorithm" wording.

The fix is uncontroversial and clearly correct, addressing a minor but legitimate documentation oversight in the test helper functions. No substantive discussion or review was needed for this small cleanup.