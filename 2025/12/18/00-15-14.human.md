This is a documentation patch that has already been reviewed and approved for inclusion. Junio Hamano confirms the change aligns with prior documentation improvements made to `core.excludesFile` in commit dca83abde2, noting the patch should have been made at the same time as that earlier change.

The patch simplifies the description of `core.attributesFile` by directly stating it takes a pathname value, removing the indirect reference to `core.excludesFile` for path expansion behavior. The actual functionality remains unchanged - it still looks for attributes in the specified file (defaulting to XDG_CONFIG_HOME or $HOME/.config locations) in addition to the standard .gitattributes locations.

This is a straightforward, uncontroversial improvement that makes the documentation more self-contained and consistent with similar config options. The patch is now properly signed off and ready to be merged as part of Git's ongoing documentation maintenance.