Here's the human-readable thread summary:

This thread documents an edge case in `git rev-list --filter` behavior regarding how explicitly provided objects interact with filters. Justin Tobler's patch clarifies in the man page that objects given directly to `git rev-list` bypass filter restrictions by default, only applying filters when `--filter-provided-objects` is specified. This documents existing but previously implied behavior.

Junio Hamano, the maintainer, has reviewed and accepted the patch, confirming it accurately reflects the current implementation. While he queues the change, he notes some dissatisfaction with the design requiring a separate option to filter provided objects. The documentation update is minimal (3 lines added, 1 removed) and non-controversial, targeting the `object:type` filter section specifically though likely applying to other filter types as well. The clarification appears to have been prompted by prior discussion about this behavior.