Here's the human-readable summary of the thread:

This thread documents and discusses the behavior of `git rev-list --filter` when objects are explicitly provided. Justin Tobler's patch clarifies in the documentation that such objects bypass filter restrictions by default, only applying filters when `--filter-provided-objects` is specified. While this behavior was previously implied but not explicitly documented, the patch makes it clear.

Junio Hamano confirmed the documentation accurately reflects current behavior and queued the patch, though he expressed some dissatisfaction with the design. Patrick Steinhardt, who originally implemented `--filter-provided-objects`, explained this was a backward compatibility compromise rather than an intentional design choice. While acknowledging the behavior is surprising, he was uncertain about changing it without breaking existing users.

In a follow-up, Justin asked whether there are any known use cases for the current default behavior beyond backward compatibility. This continues the thread's exploration of whether this surprising behavior has ongoing utility worth preserving. While all agree the documentation should reflect the status quo, the discussion leaves open the possibility of reconsidering the default in future. The patch remains queued as it accurately documents existing behavior without proposing implementation changes.