This is a refactoring patch in Patrick Steinhardt's ongoing series to move packfile handling from object database (ODB) to ODB source level. The patch simplifies the code by inlining `find_kept_pack_entry()` into its only caller `has_object_kept_pack()`, eliminating a layer of indirection. 

The changes are straightforward: the now-inlined function checks for objects in kept packs by iterating through ODB sources and their pack caches, similar to before but with slightly cleaner control flow. The patch also removes the now-unneeded declaration from packfile.h, reducing the public API surface. 

This appears to be a mechanical cleanup as part of the larger architectural effort, with no behavior changes. The diff shows 24 lines removed versus 10 added, continuing the series' trend of simplifying the codebase while maintaining functionality. The change is well-contained and builds on previous patches in the series that refactored the kept-pack cache handling.