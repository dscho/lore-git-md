Refactoring patch in the ODB abstraction series, moving `find_pack_entry()` to operate on packfile stores rather than repositories. This continues the architectural shift from repository-level to source-level packfile handling.

The patch refactors `find_pack_entry()` to take a `packfile_store` parameter instead of a `repository`, aligning with the series' goal of moving packfile operations to the ODB source level. The function now checks a single store's MIDX and packs rather than iterating through all repository sources. This creates a cleaner separation where callers`packfile_store_freshen_object()` and `packfile_store_read_object_info()` now work directly with stores as intended, while the remaining repository-level caller `has_object_pack()` is updated to explicitly iterate sources when needed.

Key technical changes:
- Removes repository iteration from `find_pack_entry()` core logic
- Updates all callers to match new interface
- Maintains identical lookup behavior through explicit source iteration in `has_object_pack()`
- Touches packfile.c (23 insertions, 20 deletions)
- Follows series pattern of moving functionality to proper architectural level

The change is mechanical and well-contained, continuing the series' systematic refactoring toward source-level packfile operations. No behavior changes are introduced - the patch simply moves iteration responsibility to callers that already handle it (`has_object_pack()`) or should conceptually own it (the store-level functions). This prepares for future work where stores will be managed entirely at the source level.