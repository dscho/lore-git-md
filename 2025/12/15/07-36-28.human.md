This is part 9/10 of Patrick Steinhardt's ongoing refactoring series to move packfile handling from the object database (ODB) level to the ODB source level, a key step in enabling pluggable ODB backends. The patch focuses on `find_pack_entry()`, which previously operated at the repository level but is now refactored to work with individual packfile stores.

The change makes the function's scope match its callers' expectations - most callers (`packfile_store_freshen_object()` and `packfile_store_read_object_info()`) already work with individual stores. The one exception, `has_object_pack()`, is updated to explicitly iterate through sources when needed. The diff shows a clean mechanical transformation where the repository-level loops are moved out of `find_pack_entry()` and into the caller that needs them.

This continues the series' pattern of moving functionality to the proper architectural level while maintaining behavior. The changes are well-contained (43 lines modified across packfile.c) and follow logically from the previous patches. As with the rest of the series, this appears to be a careful, incremental change that advances the ODB abstraction work without introducing new complexity.