Here is the updated dense AI thread summary incorporating the new email:

Documentation patch clarifying `git rev-list --filter` behavior when objects are explicitly provided. Justin Tobler's patch updates `Documentation/rev-list-options.adoc` to explicitly state that objects given directly to `git rev-list` bypass filter restrictions by default, only respecting filters when `--filter-provided-objects` is specified. This documents existing but previously implied behavior in the filtering system. The discussion reveals this behavior stems from Patrick Steinhardt's conservative implementation approach for `--filter-provided-objects`, prioritizing backward compatibility over design purity due to concerns about potential impact on partial clone users, despite no known positive use cases for the current default behavior.

Junio Hamano as maintainer confirms the documentation accurately reflects current behavior and queues the patch, while expressing mild dissatisfaction with the design. Steinhardt provides historical context - the behavior was a compatibility compromise rather than intentional design, acknowledging its surprising nature but emphasizing the need for thorough research before considering changes that might affect partial clone users. Justin Tobler probes for affirmative use cases beyond compatibility, finding none but recognizing the complexity of assessing impact on partial clone workflows.

Key technical details:
- Current behavior: explicitly provided objects bypass filters unless `--filter-provided-objects` specified
- Affects all filter types (`object:type`, `blob:none`, etc)
- Documentation change is minimal (3 line additions, 1 deletion) in `rev-list-options.adoc`

Open questions:
- Whether to eventually change default behavior given lack of positive use cases
- How to safely assess impact on partial clone users before changing defaults
- Potential deprecation path if behavior is changed

Participant positions:
- Justin Tobler: documents current behavior, questions its utility
- Patrick Steinhardt: confirms compatibility rationale, cautious about changes
- Junio Hamano: accepts documentation patch while noting design suboptimal

The thread establishes that:
1) The documentation patch correctly describes the current implementation
2) The behavior exists solely for backward compatibility, not by design
3) No known use cases justify the current default beyond compatibility
4) Changing defaults would require careful research on partial clone impact
5) The surprising behavior may warrant future reconsideration if safety can be established

All agree the documentation should reflect the status quo while leaving open the possibility of future design changes. The patch remains queued for merge as it accurately documents existing behavior without proposing implementation changes. The exchange provides important context about the behavior's origins and preserves the option to revisit the design, with Steinhardt's latest response highlighting the research needed before considering any behavior modification.