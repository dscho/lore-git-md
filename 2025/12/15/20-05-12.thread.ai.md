Here is the updated dense AI thread summary incorporating the new email:

Documentation patch clarifying `git rev-list --filter` behavior when objects are explicitly provided. Justin Tobler's patch updates `Documentation/rev-list-options.adoc` to explicitly state that objects given directly to `git rev-list` bypass filter restrictions by default, only respecting filters when `--filter-provided-objects` is specified. This documents existing but previously implied behavior in the filtering system. The change is minimal (3 line additions, 1 deletion) and non-controversial, targeting the `object:type` filter section while likely applying to other filter types as well.

Junio Hamano as maintainer confirms the documentation accurately reflects current behavior and queues the patch, while expressing mild dissatisfaction with the design requiring a separate option to filter provided objects. Patrick Steinhardt follows up revealing he was the original implementer of `--filter-provided-objects`, explaining the current behavior was a backward compatibility compromise rather than an intentional design choice. He acknowledges the behavior is surprising but expresses uncertainty about changing it without breaking existing users.

Justin Tobler's follow-up probes whether there are any known use cases for the current default behavior beyond backward compatibility, suggesting the behavior may lack clear utility. This continues the thread's exploration of whether the surprising behavior is worth preserving long-term, though no immediate changes are proposed.

The thread establishes that:
1) The documentation patch correctly describes the current implementation
2) The current behavior stems from compatibility concerns during initial implementation
3) There are no known positive use cases for the current default beyond maintaining compatibility
4) The historical context may inform future discussions about modifying the default behavior
5) While the design is acknowledged as suboptimal, changing it would require assessing potential breakage

All participants agree the documentation should reflect the status quo while the design discussion remains open-ended. The patch remains queued for merge as it accurately documents existing behavior without proposing implementation changes. The exchange provides important context about why the surprising behavior exists and preserves the option to revisit the design in future, with Justin's question highlighting the lack of affirmative justification for the current default.