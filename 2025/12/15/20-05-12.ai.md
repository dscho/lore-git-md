This is a documentation patch clarifying the behavior of `git rev-list --filter` when objects are explicitly provided. 

The patch updates the man page to explicitly state that objects given directly to `git rev-list` bypass any filter restrictions by default, and will only respect filters if `--filter-provided-objects` is also specified. This behavior was previously implied but not clearly documented, as noted in prior discussion (referenced by the author but not included in this email).

The change is minimal - just a few lines added to `Documentation/rev-list-options.adoc` to document this edge case in the filtering behavior. The patch appears well-formed with a proper commit message referencing the discussion that prompted it, though we don't see the full context of that discussion here. 

This is a straightforward documentation improvement that helps users understand an important nuance of the filtering system, particularly affects cases where users might be passing explicit object IDs while also trying to filter the output. The change is non-controversial and aligns with existing behavior rather than modifying it.