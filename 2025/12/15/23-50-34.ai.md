This is a maintainer review response in the ongoing `git replay` empty commit handling thread. Junio C Hamano provides stylistic and technical feedback on Phillip Wood's patch to drop commits that become empty during replay.

The review confirms the core implementation approach while suggesting minor wording improvements to the commit message and documentation. Junio notes the straightforwardness of the tree comparison logic in `replay.c` that detects empty commits, and approves the branch pointer update behavior when commits are dropped. He questions the use of `--only` in one test case but doesn't block on it.

Key technical points noted:
- Confirms the tree OID comparison approach using `replayed_base_tree` temporary variable
- Verifies branch pointer update behavior is correct and well-documented
- Questions test case use of `--only` flag with empty commit
- Suggests minor commit message phrasing improvements ("onto then" -> "onto," and adding a pause after "breaking change")

The review maintains alignment with the thread's consensus that dropping empty commits is the right default behavior for the experimental `git replay` command. Junio's feedback is entirely editorial - no technical changes are requested, indicating the implementation details are sound. The thread remains in RFC stage but with all substantive technical questions resolved.