Here is the dense AI thread summary for this new thread:

Thread subject: [PATCH 1/3] utf8: handle iconv state reset on buffer expansion for macOS. Thread type: platform compatibility fix (patch series). Originating author: (not specified in input).

Core problem: macOS 14/15 (Sonoma/Sequoia) system iconv implementation fails to properly handle ISO-2022-JP conversions during piecemeal processing when output buffers need resizing, leaving escape sequences in `git show` output for commit messages.

Technical solution: Adds `ICONV_RESTART_RESET` mode to `reencode_string_iconv()` in utf8.c that fully resets conversion state (via `iconv(conv, NULL, NULL, NULL, NULL)`) and restarts from scratch when expanding output buffers. Change is opt-in via preprocessor flag to maintain backward compatibility. Follow-up patches will add build system integration.

Key details:
- Modifies utf8.c core conversion logic
- Preserves original buffer positions during retry
- Inspired by similar issues in other projects
- Suggested approach from René Scharfe
- Part 1 of 3-part series (build integration and tests coming)

Open questions:
- Build system integration not yet implemented
- Testing approach for other platforms not specified
- No decision yet on whether to enable by default for macOS

Related work:
- References similar iconv issues in other projects
- Builds on existing third-party iconv support (Fink/MacPorts)
- Follows pattern of platform-specific workarounds in compat/

Participant positions:
- René Scharfe suggested the technical approach
- No other reviewers noted yet (thread just started)