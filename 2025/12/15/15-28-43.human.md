Build system fix for Rust compilation on macOS. This is a follow-up to the discussion about GNU sed's `-s` flag causing build failures on macOS, where Patrick Steinhardt asks if either Eric Sunshine or D. Ben Knoble would submit the agreed-upon fix.

The issue stems from a GNU-specific flag (`-s`) being used in the build system to parse `cargo -vV` output, which fails on macOS's BSD sed. The thread has already established that replacing this with portable `-n` and `p` flags would solve the problem without changing functionality. The fix appears straightforward and would unblock Rust-enabled builds on macOS.

Patrick's email serves as a gentle nudge to implement the solution that was technically settled in the earlier discussion. No new technical ground is covered here - it's primarily about moving from diagnosis to implementation. The patch would be a simple one-liner affecting the Rust build infrastructure, specifically the library naming convention detection.

This appears to be a clear-cut bugfix with consensus already established, so the main question is simply who will submit the patch. The change is low-risk and would help maintain cross-platform compatibility for Rust builds.