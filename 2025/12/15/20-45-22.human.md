Build system refactoring for Darwin/macOS support. This is part 2 of a 3-patch series (no cover letter seen yet) that relocates all Darwin-specific build configuration from the main Makefile to config.mak.uname. The changes are purely organizational - the same build flags and conditions are preserved, just moved to what appears to be their more logical home. The commit message indicates this move is preparatory work needed for the next patch in the series.

The patch handles several macOS-specific build considerations: Fink package manager paths, Darwin Ports paths, Apple's Common Crypto framework usage (which replaces OpenSSL), and pthread library handling. The changes appear mechanical and well-contained, touching only build system files with no functional impact on the resulting binaries.

This looks like straightforward build system maintenance - the changes are clearly motivated (preparation for future work) and the implementation appears correct, moving the Darwin logic verbatim to its new location. The patch follows Git's established patterns for platform-specific configuration.