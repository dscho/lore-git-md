This is a refactoring patch in Patrick Steinhardt's ongoing series to restructure packfile storage around object database sources rather than repositories. The patch cleans up misleading logic in `unuse_one_window()`, which handles packfile window unmapping when memory limits are exceeded.

The function previously took a `packed_git` parameter that was always non-NULL in practice, despite historical code that suggested it could be NULL. The patch simplifies this by having it take the object database directly, removing the redundant NULL check and making the control flow clearer. The change reveals that the function was already effectively operating at the ODB level, as it scans all packfiles in the database regardless of the input parameter.

This is a mechanical change that aligns with the series' goal of moving packfile handling to the ODB source level. The patch touches core packfile memory management but maintains identical behavior, just with clearer intent. It updates one caller in `use_pack()` to pass the ODB rather than a packfile, and modifies the window scanning logic to operate purely at the ODB level.

The commit message provides thorough historical context, tracing the function's evolution from its introduction through several refactorings that made the NULL case impossible. This attention to detail suggests the change is well-researched and unlikely to introduce regressions. As with previous patches in the series, this appears to be a straightforward step toward the larger architectural goal of source-level packfile management.