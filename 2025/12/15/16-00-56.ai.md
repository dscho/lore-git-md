This is a feature proposal for Git core integration of the "rebaser-flow" concept, moving from shell scripts to built-in commands. Milos Markovic responds to Kristoffer Haugsbakk's Bash compatibility critique by proposing direct Git command integration rather than shell script workarounds.

The key proposed addition is a `--follow-branch` option that would automatically rebase specified branches during pull operations, implementing the workflow's core premise of maintaining branch hierarchies (production > QA > dev). This would replace the Bash-dependent scripts currently shown in the reference implementation.

The email doesn't address other technical concerns raised in the thread, focusing solely on the portability issue by suggesting native Git command integration. No implementation details or prototype are provided - this remains a high-level proposal. The discussion continues to lack concrete metrics or use cases demonstrating advantages over existing workflows.

Technical considerations for future thread tracking: The proposal introduces `--follow-branch` as a potential new Git option, targeting pull/rebase operations. The workflow maintains its production-first hierarchy but now seeks core integration rather than script-based implementation. Shell compatibility concerns would become moot if implemented natively, though the fundamental workflow philosophy remains unvalidated.