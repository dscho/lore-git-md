This is a reply in the ongoing refactoring thread about moving packfile storage from object database (ODB) to ODB source level. Justin Tobler provides feedback on Patrick Steinhardt's first patch in the 10-part series.

The reply confirms understanding of the architectural change where packfile stores will now reference ODB sources rather than ODBs directly. Justin notes the transitional "weird in-between state" where stores are still ODB-owned but initialized via sources, and points out an apparent awkwardness where alternate sources are added to the same packfile store during this transition phase.

Key technical observations made:
- The packfile store struct now stores a `source` pointer instead of `odb`
- The ODB can still be accessed via `source->odb` when needed
- `packfile_store_new()` now takes a source parameter
- The diff shows updates to `packfile_store_load_pack()` and `packfile_store_prepare()` to use the source chain

Justin's feedback is generally positive ("Makes sense", "Looks good") while noting the temporary awkwardness of the transition state. No objections or suggested changes are raised - this appears to be a confirmation of understanding rather than substantive critique. The reply stays focused on the technical changes in this specific patch without digressing into broader series discussion.