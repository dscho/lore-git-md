Documentation patch clarifying `git-rev-list` filter behavior.

Justin Tobler proposes a small but useful documentation update to clarify how `--filter` interacts with explicitly provided objects in `git-rev-list`. The patch makes clear that objects given directly to the command bypass filters by default, unless `--filter-provided-objects` is also specified. This behavior was previously implied but not explicitly stated in the docs.

The change is straightforward, adding just two lines of explanatory text to the `rev-list-options.adoc` file. The patch appears to be in response to prior discussion (referenced via a message ID), suggesting this clarification addresses a real point of confusion. The wording is clear and matches Git's existing documentation style.

This is the kind of small but meaningful documentation improvement that helps users understand subtle edge cases without having to dig through implementation details or experiment with the command. The patch looks ready to apply as-is.