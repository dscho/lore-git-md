Proposal to standardize terminology for index operations across Git commands, originally focused on expanding `--staged` flag usage but now considering whether to instead remove deprecation status from `--cached`. Thread root noted inconsistency where older commands require `--cached` while `git diff` accepts both since 2008 (2baf1850ce). 

Junio C Hamano introduced a pivot, questioning expansion of `--staged` versus accepting `--cached` as stable terminology. Jacob Keller's response reinforces the original proposal, advocating for consistency by adding `--staged` to commands currently using only `--cached`. Technical constraint remains maintaining distinction between index-only (`--cached`/`--staged`) and index+working tree (`--index`) operations.

Two clear positions have emerged: 
1. Keller's consistency-through-expansion (make `--staged` available everywhere)
2. Hamano's stability-through-canonicalization (standardize on `--cached` by removing its "obsolete" label)

Open questions include which approach better serves long-term interface stability versus terminology consistency, technical implications for existing scripts, and interaction with `git diff`'s dual-support. The discussion remains focused on interface design principles, with no new technical constraints introduced in Keller's response. Key participants are Hamano (maintainer perspective favoring stability) and Keller (advocating for expanded consistency). The thread has shifted from implementation details to fundamental questions about Git's interface evolution.