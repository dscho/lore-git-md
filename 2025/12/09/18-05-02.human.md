This is a follow-up discussion in the `git replay` documentation patch series, focusing on clarifying the behavior of the `--contained` option. Kristoffer Haugsbakk responds to Junio C Hamano's questions about edge cases in branch containment detection during replay operations.

The email provides concrete test results showing how `--contained` behaves with merged branches and duplicate patch-ids, confirming that branches are only updated if their head commits fall within the specified revision range. Kristoffer demonstrates that when branches span range boundaries (as in Junio's example with commits split between master and next), they are not considered contained.

The discussion reveals two key points about the current implementation: first, that `--contained` requires `--onto` (though this isn't documented yet), and second, that the command fails with a misleading error when given a non-existent target branch. Kristoffer considers adding an example to clarify the behavior but questions whether the source code's phrasing should also be updated to match any documentation changes.

This exchange shows the series is nearing completion, with the remaining work being minor wording refinements rather than substantive changes. The author appears thorough in testing edge cases and open to suggestions while maintaining that the original phrasing is understandable for typical use cases.