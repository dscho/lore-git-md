This thread contains a 2-patch series from Samo Pogačnik addressing issues in shallow repository fetching. Patch 1/2 fixed a memory leak in `get_shallow_commits()` by adding `object_array_clear(&stack)` in shallow.c, serving as a preparatory cleanup. Patch 2/2 (now detailed in the new email) implements the core fix for relative-depth fetching (`--deepen`) in repos with merged branches by introducing `get_shallows_depth()` to calculate equivalent depths for existing shallow commits, unifying the absolute and relative deepening paths.

The series modifies shallow.c (both patches) and upload-pack.c (patch 2), with test cases in t5500-fetch-pack.sh. Patch 2's key changes include: removing the redundant `get_reachable_list()`, implementing depth calculation via new `get_shallows_depth()` (using a `commit_depth` slab to track minimum depths during parent walks), and carefully handling graft points in shallow history. The solution demonstrates correct behavior through a new test case showing proper handling of merged branches during relative-depth fetches.

Technical considerations include: the memory leak fix in patch 1 was low-risk cleanup, while patch 2's restructuring of upload-pack.c's deepening logic requires careful review of edge cases in depth calculation, particularly around graft points and multiple parent handling. The implementation eliminates duplicate code paths while preserving the shallow/non-shallow boundary calculations. The thread remains in early stages with no reviews yet; reviewers should focus on verifying the depth calculation logic in `get_shallows_depth()` and the test coverage of merged branch scenarios.