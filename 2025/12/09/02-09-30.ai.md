This is a review response in the ongoing refactoring series for Git's object database alternates handling. Justin Tobler is providing feedback on Patrick Steinhardt's second patch, which moves relative path resolution from the linking phase into the parsing phase of alternates handling.

The key technical point under discussion is the function renaming from `link_alt_odb_entry()` to `odb_add_source()`. Justin questions whether the new name is too generic since the function specifically handles alternate sources, suggesting `odb_add_alternate_source()` might be more precise. He confirms the technical approach of moving path resolution into `parse_alternates()` is sound.

The exchange shows active collaboration on naming conventions as part of the broader ODB abstraction effort. Justin's review is constructive, raising a naming consideration while affirming the technical direction. This follows his positive review of the first patch in the series, maintaining engagement with Patrick's systematic refactoring work.

The thread context shows this is part of a carefully planned transformation of alternates handling to use source abstractions rather than direct filesystem operations, preparing for pluggable backends while preserving current behavior. The discussion remains focused on implementation details rather than fundamental design.