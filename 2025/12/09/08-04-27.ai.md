This is a brief clarification in the ongoing refactoring series for Git's object database alternates handling. Patrick Steinhardt responds to Justin Tobler's suggestion about a potential documentation typo in the third patch of the series, which moves normalized path computation into `odb_is_source_usable()`.

Patrick clarifies that "iff" (meaning "if and only if") in the function's documentation comment is intentional mathematical notation, not a typo that should be changed to "if". This maintains the precise logical meaning in the function's contract. The exchange is minor and doesn't affect the technical changes in the patch, which Justin had already reviewed positively regarding the relocation of normalized path computation.

The thread context shows this is part of a systematic refactoring to handle object database alternates through a source abstraction rather than direct filesystem operations. The series has seen constructive review engagement, with this being the only remaining minor documentation point under discussion.