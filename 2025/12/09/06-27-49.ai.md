This is a naming convention discussion in Taylor Blau's MIDX compaction series, specifically about patch 02/17 which splits checksum handling functions. The thread has already seen agreement on standardizing function names to follow modern Git conventions with `midx_` prefix, but now focuses on the suffix choice between `_hex` and `_raw` variants.

Patrick Steinhardt advocates for `midx_get_checksum()` and `midx_get_checksum_hex()` to maintain consistency with existing hex.h interfaces, despite Taylor's concern about verbosity. The discussion reveals the functions differ only in output format (binary vs hex-encoded) not in hash type, making the `_hex` suffix more accurate than Taylor's alternative `_raw` suggestion. This refinement continues the series' pattern of careful API design attention, though it doesn't affect the core MIDX compaction functionality. The exchange demonstrates the ongoing attention to detail in the review process, with naming consistency being prioritized even for helper functions. This patch is one of the final ones under review in the 17-part series.