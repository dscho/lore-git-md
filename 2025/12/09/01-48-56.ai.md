This is the first patch in a performance optimization series targeting promisor object handling. It extends the `PARSE_OBJECT_SKIP_HASH_CHECK` optimization to work with `OBJ_NONE` objects (unknown types) in addition to the existing support for `OBJ_BLOB` and `OBJ_TREE`. The change modifies `parse_object_with_flags()` in `object.c` to skip full parsing when the object type is unknown but later determined to be a blob or tree via `odb_read_object_info()`.

The patch fixes a gap where promisor objects marked as uninteresting (with `OBJ_NONE` type) weren't benefiting from the skip-hash optimization, causing unnecessary parsing. The change is small (4 lines modified) but impacts a critical path in partial clone workflows where promisor objects are frequently accessed. The optimization maintains all existing behavior while improving performance in the common case where object contents aren't needed.

Key technical details include the interaction between `PARSE_OBJECT_SKIP_HASH_CHECK` flag, `obj->type` states (particularly `OBJ_NONE`), and the `odb_read_object_info()` fallback path. The change parallels existing optimizations for blobs and trees, applying the same logic to unknown-type objects that turn out to be blobs or trees. This builds on commit 8db2dad7a045 which first introduced blob-specific optimizations.

The patch appears well-constructed with a clear commit message explaining both the current behavior and the optimization opportunity. It's part of a v3 series, suggesting prior iterations have addressed feedback on this approach. The change is focused and low-risk, touching only the object parsing optimization path while maintaining all existing behavior guarantees.