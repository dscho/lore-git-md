Feature addition for `git repo info` command, implementing the `--keys` flag as suggested by Junio Hamano. This is the second patch in a v2 series (though the thread context shows this was later updated to v3 with format handling improvements).

The patch adds a `--keys` option to list available repository information keys without their values, addressing the need for programmatic discovery of available fields. The implementation includes:
- New `print_keys()` function handling both newline and NUL-terminated output formats
- Strict validation preventing `--keys` from being used with `--all` or specific key requests
- Documentation updates explaining the new option and its format support
- Comprehensive test coverage including format verification and error cases

Key technical details for future reference:
- Output format handling uses existing `FORMAT_DEFAULT` and `FORMAT_NUL_TERMINATED` enums
- Error paths enforce valid option combinations (`--keys` cannot mix with `--all` or key arguments)
- Tests verify behavior with command substitution and format handling
- Documentation clarifies the `default` and `nul` format options for key listing

The implementation appears thorough with good test coverage and clear documentation. The patch builds on prior work (bdc5341ff6) and follows the project's direction for improving programmatic interfaces. The thread context indicates this approach was later expanded in v3 to fully integrate with the output format system.