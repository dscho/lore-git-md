This is a technical discussion about backward compatibility in the MIDX compaction series, specifically patch 09/17 which relaxes lexicographic pack ordering requirements.

The email addresses Patrick Steinhardt's concerns about compatibility with other Git implementations (notably libgit2) that assume strict pack ordering in MIDX files. Taylor Blau initially thought existing Git clients would gracefully handle out-of-order packs without a version bump, but acknowledges libgit2's behavior as a strong argument for incrementing the MIDX version number to avoid compatibility issues.

Key technical points:
- Current libgit2 implementation explicitly checks for sorted packs and fails if they're out of order
- Taylor had wanted to avoid version bumps for minor changes but now agrees it's necessary
- Confirms MIDX packs array cannot be modified after sorting (no dynamic updates)

This discussion will likely lead to a version bump in the MIDX format for the relaxed ordering change, ensuring compatibility across Git implementations while maintaining the compaction functionality. The exchange shows careful consideration of ecosystem impact beyond just the core Git implementation.