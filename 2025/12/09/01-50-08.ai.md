This is a follow-up discussion in Taylor Blau's MIDX compaction series, specifically addressing naming conventions for checksum-related functions introduced in patch 02/17. The thread context shows this is part of a larger refactoring effort to improve MIDX/bitmap handling.

Taylor is responding to Patrick Steinhardt's earlier suggestion to rename the checksum accessor functions to `midx_get_checksum()` and `midx_get_checksum_hex()` for better clarity and consistency with modern Git naming patterns. While initially agreeing, Taylor now raises a new consideration about the naming symmetry between the raw and hex-encoded variants.

The key points in this email are:
1. Taylor acknowledges Patrick's naming suggestions are reasonable but questions whether "_hex" is the clearest suffix given that the hex variant will be more frequently used
2. Proposes an alternative naming scheme using "_raw" suffix instead of "_hex" for the binary form
3. Notes the tradeoff between clarity and verbosity in API naming

This continues the thread's pattern of careful API design discussion, with particular attention to:
- Consistency with Git's evolving naming conventions (prefixing with "midx_")
- Clarity about return types (raw bytes vs hex strings)
- Practical considerations about frequently-used APIs

The discussion remains focused on implementation details rather than core functionality, as the series' technical approach was already established in earlier exchanges. This email represents the kind of fine-tuning that happens during thorough code review of complex subsystems.