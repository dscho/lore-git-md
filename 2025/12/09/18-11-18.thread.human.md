Samo Pogačnik's two-patch series addresses edge cases in shallow repository handling, with the first patch fixing a memory leak in `get_shallow_commits()` and the second implementing proper relative-depth fetching (`--deepen`). The series has gone through careful review by Patrick Steinhardt, who confirmed the technical approach while requesting documentation and test improvements.

The first patch adds a single-line fix (`object_array_clear(&stack)`) to properly clean up memory in `get_shallow_commits()`, with the fix validated by test cases in the second patch. The second patch introduces a new `get_shallows_depth()` function to handle cases where deepening fetches go past merged branches, preventing unreachable shallow commits from being left behind. It removes the now-redundant `get_reachable_list()` and adds comprehensive coverage in t5500-fetch-pack.sh.

Samo has incorporated all of Patrick's feedback, including improved commit messages with ASCII graph examples, removal of debug output, test hygiene improvements, and better documentation of the memory management. The changes are well-contained to shallow.c and upload-pack.c, with the series demonstrating effective collaboration and steady progress. With all review points addressed and the technical approach validated, the patches appear ready for final consideration.