Samo Pogačnik's patch series addressing shallow repository issues has reached its fourth iteration, with both patches now in polished form after thorough review. The series fixes a memory leak in `get_shallow_commits()` and implements correct relative-depth fetching behavior when using `--deepen`.

The implementation introduces a new `get_shallows_depth()` function that properly handles edge cases when deepening beyond merged branches, ensuring we no longer end up with unreachable shallow commits. Key technical improvements include tracking minimum depth of existing shallow commits, removing redundant code, and properly handling graft points. The changes are well-tested with comprehensive cases in t5500-fetch-pack.sh.

The patches have benefited from multiple rounds of review from Patrick Steinhardt (verifying technical correctness), Junio C Hamano (reviewing documentation and style), and Kristoffer Haugsbakk (catching typos). All feedback has been addressed across the four iterations, with the v4 changes being minor refinements to commit messages and documentation. The series appears ready for integration, solving both the memory leak and broader shallow repository edge cases while maintaining backward compatibility.