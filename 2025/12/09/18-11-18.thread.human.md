Samo Pogačnik's patch series addressing shallow repository fetching issues has progressed through several iterations, with v4 now ready for integration. The two-patch set fixes a memory leak in `get_shallow_commits()` and implements correct relative-depth fetching (`--deepen`) through a new `get_shallows_depth()` function.

The series has undergone thorough review from Patrick Steinhardt (technical correctness) and Junio C Hamano (documentation hygiene), with Kristoffer Haugsbakk catching a final typo that was fixed in v4. The implementation handles edge cases when deepening beyond merged branches where previous versions could leave unreachable shallow commits, while maintaining backward compatibility. 

Changes in v4 were minimal - just the typo fix ("allways" -> "always") and some commit message clarifications about avoiding logic duplication. The technical solution remains robust, with comprehensive test coverage in t5500-fetch-pack.sh and validation through leak-checking CI tests. The collaboration has been effective, with all reviewer feedback addressed across the iterations. The patches appear ready for merging, solving real issues in shallow repository handling while keeping the changes focused and well-documented.