This is a follow-up review on the memory leak fix in the HTTP 429 retry series. Taylor Blau suggests an alternative approach to handling the memory cleanup in `show_http_message()` that would avoid copying the message buffer.

Rather than introducing a new buffer copy as in the original patch, Taylor proposes making the function explicitly fatal by renaming it to `show_http_message_fatal()` and having it handle all cleanup before calling `die()`. The suggested implementation would release all buffers (type, charset, and msg) and then invoke the die routine, eliminating the need for callers to manage cleanup.

The review notes this would be more efficient than the current patch's buffer-copying approach while still solving the memory leak issue. The change would affect all HTTP error paths (not just the new rate-limiting case) and would require updating all call sites to use the new fatal variant.

This appears to be a refinement suggestion rather than a blocking concern - the original patch correctly fixes the leak, but Taylor's approach might be more maintainable long-term by centralizing the resource cleanup. The discussion continues the thread's focus on implementation details while maintaining consensus on the core functionality.