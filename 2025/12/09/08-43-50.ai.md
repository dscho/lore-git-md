This is a brief follow-up in the `git-last-modified` bitmap initialization thread, now discussing the architectural implications of the fix.

Toon Claes responds to Junio Hamano's observation that the original fix has already been merged to 'next', agreeing to abandon the v2 patch that proposed a more general `MEMZERO_ARRAY` macro solution. This effectively concludes the discussion about standardizing array zeroing patterns, leaving the immediate bugfix (already merged) as the only change.

The thread had previously explored two architectural approaches (`CLEAR_ARRAY` and `MEMZERO_ARRAY` macros) with Jeff King identifying 5 potential use sites, but no consensus emerged on whether the pattern was common enough to warrant standardization. With this email, the maintainer's implicit preference for the minimal fix is accepted, closing the architectural discussion without further action. The key technical detail remains the original `sizeof(eword_t)` fix for bitmap initialization in `builtin/last-modified.c`.