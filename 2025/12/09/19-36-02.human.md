This is a preparatory refactoring patch for the `git repo info --keys` feature series. It introduces a `FORMAT_DEFAULT` enum value to cleanly handle output format selection in the first of a two-patch set.

The change adds a new `FORMAT_DEFAULT` state to the output format enum and makes it the initial value in `cmd_repo_info()`, while maintaining the existing `FORMAT_KEYVALUE` behavior as the fallback. This allows future patches to explicitly handle "default" format requests while keeping backward compatibility. The implementation is straightforward - just adding the enum value and mapping it in the format parser.

As part of the larger series that adds `--keys` functionality, this change helps pave the way for consistent format handling across all output modes. The patch appears well-contained and properly scoped for its role in the series, with clear commit message explaining the change's purpose. Since this is version 2 of the patch, it has presumably already incorporated any initial feedback from reviewers.