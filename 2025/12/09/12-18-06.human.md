This is a thread wrap-up message regarding the `git-last-modified` bitmap initialization fix and the broader discussion about standardizing array zeroing patterns in the codebase.

Junio Hamano acknowledges Toon Claes' agreement to abandon the v2 patch proposing a `MEMZERO_ARRAY` macro, as the original one-line fix has already been merged to 'next'. However, Hamano notes that Patrick Steinhardt's suggestion to standardize on a `CLEAR_ARRAY()` pattern throughout the codebase (not just for this specific case) might still be worth considering as a future cleanup effort.

The message playfully suggests this could be an opportunity to practice Coccinelle (the semantic patching tool used for large-scale code transformations), indicating this might be a good candidate for a future refactoring project, though not something that needs to block the current bugfix. The thread is effectively concluded with the original fix being the only change merged.