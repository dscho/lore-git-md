This is a review response in the ongoing refactoring series for Git's object database alternates handling. Justin Tobler is engaging with Patrick Steinhardt's seventh patch, which renames `read_info_alternates()` to `odb_source_read_alternates()` as part of moving alternates handling to a source-based abstraction.

Justin confirms the patch looks good but raises an architectural question about how future ODB sources will handle alternates differently. He notes that while the current interface expects parsed alternates in a strvec, different source types may need different handling in `odb_add_source()`, possibly differentiated by URI schemes. This suggests the abstraction may need to evolve further as pluggable ODB backends are implemented.

The exchange shows productive technical discussion between two key contributors to Git's object storage modernization. Justin's question doesn't indicate any problems with the current patch, but rather looks ahead to implications for future work in this area. The thread appears to be moving toward completion, with this being one of the final patches in the series.