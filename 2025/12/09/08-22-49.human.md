This is a follow-up in the HTTP authentication thread, where Ashlesh Gawande is revisiting the 403 handling approach after earlier discussions. The original proposal to treat 403 responses like 401s was abandoned due to protocol concerns raised by brian m. carlson, but the test infrastructure components remain valuable.

Gawande now suggests a modified approach that would still prompt for credentials on 403 responses, but only when no credentials are already configured. This would prevent the credential-erasure problem while potentially improving the UX for cases where users have no credentials set up. The key change would be adding a conditional check to avoid overriding existing credentials on 403 responses while still allowing prompts when no credentials exist.

The email shows the proposed code modification to `http.c` that would implement this behavior. This appears to be an attempt to find a middle ground between the original proposal (full 403 prompting) and the current behavior (no prompting on 403), though it's unclear yet whether this will satisfy the protocol concerns previously raised.

The discussion continues to focus on improving the authentication experience while respecting HTTP semantics, with the test infrastructure work now being separated as an independent improvement. This email represents an attempt to salvage some UX benefit from the original idea while avoiding the problematic credential erasure scenario.