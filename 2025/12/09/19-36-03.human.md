Feature addition to `git repo info` command, part of a series implementing `--keys` flag functionality.

This patch introduces a `--keys` flag to `git repo info` that lists available keys without their values, addressing the need for programmatic discovery of supported keys. The implementation includes proper format handling (supporting both newline and NUL-terminated output via `--format`), strict validation to prevent invalid combinations with `--all` or specific key requests, and thorough test coverage.

The change builds on prior work in the series (as noted in the thread context) and follows Junio Hamano's original suggestion. The implementation looks solid, with careful attention to edge cases and clean integration with existing format handling. Tests verify the new functionality while also modernizing the test suite to use the dynamic key listing rather than hardcoded values.

This appears ready for inclusion, providing a useful feature for scripting while reducing maintenance burden in the test suite. The patch is well-structured with clear documentation updates and follows Git's established patterns for similar functionality.