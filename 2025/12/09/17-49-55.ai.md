This is a review response in the ongoing ODB alternates refactoring series. Justin Tobler provides feedback on patch 7/8, which renamed `read_info_alternates()` to `odb_source_read_alternates()` as part of moving alternates handling to work through object database sources.

Justin confirms understanding of the patch's purpose - to enable different ODB source types to define their own alternates parsing methods, since not all sources will use filesystem-based alternates. He raises a technical question about the interface design: whether `odb_add_source()` will need to handle different source types (potentially identified by URI) since alternates parsing happens before sources are added to the ODB list.

The review is positive overall ("The patch itself here looks good") while probing the future direction of the abstraction. This continues the productive technical dialogue seen throughout the thread, with Justin engaging on implementation details while Patrick has been responsive to previous questions (like the "iff" documentation clarification mentioned in the thread summary). The exchange suggests the series is nearing completion with no major outstanding issues.