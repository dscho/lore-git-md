This is a performance optimization series targeting promisor object handling, specifically improving the `--exclude-promisor-objects` feature. The first patch extends the `PARSE_OBJECT_SKIP_HASH_CHECK` optimization to work with objects marked as `OBJ_NONE` (unknown blobs), which wasn't previously the case. The second patch leverages this change in `add_promisor_object()` to skip hash verification when processing promisor objects, which should provide significant speedups for partial clone operations.

The changes are relatively small (4 lines net) but touch core object handling in `object.c` and promisor object processing in `packfile.c`. The optimization appears well-motivated for partial clone workflows where promisor objects are frequently accessed but their contents aren't always needed. The commit messages are technical but clear about the performance benefits being targeted.

Key technical details for future reference: the optimization centers around `parse_object_with_flags()`'s handling of `PARSE_OBJECT_SKIP_HASH_CHECK` and its interaction with `OBJ_NONE` objects, then applies this to `add_promisor_object()`. The changes are conservative - they maintain all existing behavior while just skipping unnecessary hash checks in specific cases. The series is marked v3, suggesting previous iterations have likely addressed initial feedback.