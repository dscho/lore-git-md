This is a memory leak fix in the shallow clone functionality. The patch addresses a minor but clear oversight where the `object_array` named 'stack' in `get_shallow_commits()` wasn't being freed before function return, leaving allocated elements unreachable. The fix is straightforward - adding a single `object_array_clear(&stack)` call before returning.

The change is part of a 2-patch series (positioned as 1/2) that ultimately aims to unify absolute and relative depth calculations in shallow fetching. This first patch serves as preparatory cleanup, fixing a resource leak in the shallow commit processing code. The leak itself appears to be relatively harmless in practice since it only occurs during fetch operations, but properly freeing the allocations is clearly the right thing to do.

The patch touches only shallow.c, modifying the `get_shallow_commits()` function which is core to shallow repository operations. The change is minimal and focused, adding exactly one line to free the previously leaked allocations. While simple, this cleanup helps pave the way for the more substantial changes in patch 2/2 of the series which will modify the depth calculation logic.

For future thread reference: this preparatory patch introduces no new concepts or interfaces - it simply fixes a memory leak in existing shallow commit processing code. The 'stack' object_array is a temporary working structure used during shallow commit processing, and this change ensures it's properly cleaned up. The core functionality remains unchanged.