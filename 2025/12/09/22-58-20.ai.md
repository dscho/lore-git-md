This is the final patch in a 6-part series extending Git's `repo structure` command with object size reporting. The patch completes the human-readable table output by adding disk size metrics alongside the previously added inflated size metrics.

The implementation mirrors the structure used for inflated sizes, adding disk size totals and per-type breakdowns (commits, trees, blobs, tags) to the table output. The test suite is updated to verify the new output format while acknowledging platform variance in disk sizes - tests check for presence of the fields but skip exact value validation. A new `strip_object_disk_usage` helper filters out disk size lines for tests where exact values can't be asserted.

Key technical points:
- Completes the table output format with disk size metrics
- Uses same `stats_table_size_addf()` infrastructure as inflated sizes
- Maintains consistent formatting with KiB/MiB/GiB scaling
- Tests verify output structure while handling platform variance
- Follows same pattern as previous patches for SHA1-specific test cases

The patch concludes the series by delivering the promised disk size reporting in human-readable format, matching the machine-readable output added in patch 5. The implementation remains focused on the `repo structure` subcommand without modifying core object storage behavior.