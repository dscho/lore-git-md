Performance optimization series (v3) targeting promisor object handling in partial clone workflows, specifically improving `--exclude-promisor-objects`. Core changes extend `PARSE_OBJECT_SKIP_HASH_CHECK` optimization to work with `OBJ_NONE` objects (unknown blobs) via `parse_object_with_flags()` in `object.c`, then apply same optimization to promisor object initialization in `add_promisor_object()` (`packfile.c`). Key insight: promisor processing often only needs object presence, not content validation. First patch enables skip-hash for `OBJ_NONE` when later determined to be blob/tree via `odb_read_object_info()`; second patch applies this to promisor set initialization, showing dramatic speedup (76min->2min for 176GB pack). Changes are minimal (4+3 lines net) but impactful, maintaining all behavior guarantees while optimizing common case where promisor objects are accessed but contents unneeded. Technical focus remains on `parse_object_with_flags()` behavior with `PARSE_OBJECT_SKIP_HASH_CHECK` and `OBJ_NONE`, now applied across two key promisor processing paths. Builds on commit 8db2dad7a045 (blob-specific optimizations). Series appears well-constructed with clear commit messages explaining optimizations and safety, showing real-world benchmarks. Both patches form coherent optimization of promisor object pipeline - first enabling the optimization for more object types, then applying it to additional processing phase.