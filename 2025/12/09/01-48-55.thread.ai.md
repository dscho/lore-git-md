Performance optimization series (v3) targeting promisor object handling in partial clone workflows, specifically improving `--exclude-promisor-objects`. Core change extends `PARSE_OBJECT_SKIP_HASH_CHECK` optimization to work with `OBJ_NONE` objects (unknown blobs), then leverages this in `add_promisor_object()` to skip hash verification when processing promisor objects. Small but impactful changes (4 lines net) touching `object.c` (core object handling) and `packfile.c` (promisor processing). Maintains all existing behavior while optimizing the common case where promisor objects are accessed but their contents aren't needed. Technical focus on `parse_object_with_flags()` behavior with `OBJ_NONE` and `PARSE_OBJECT_SKIP_HASH_CHECK`, applied to promisor object processing path. Series is at v3, indicating prior iterations addressed feedback.