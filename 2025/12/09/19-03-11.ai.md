This is a proposal to standardize the `--staged` flag across Git commands that operate on the index, addressing a naming inconsistency between older and newer commands.

The email notes that while `--cached` is considered obsolete terminology (documented as such since 2005), it remains the flag used by commands like `git rm` and `git grep` to operate on the index. Newer commands like `git restore` and `git stash` use `--staged` instead, while `git diff` accepts both as synonyms since 2008. The author reports frequently typing `--staged` out of muscle memory when using older commands, suggesting this inconsistency creates a minor usability friction.

The proposal doesn't include specific implementation details but references commit 2baf1850ce which added `--staged` as a synonym in `git diff`. The change would affect commands where `--cached` is currently the only way to specify index operations, potentially including `git rm`, `git grep`, and others. The email frames this as a consistency improvement rather than a bugfix, noting the existing behavior works correctly but suffers from outdated terminology.

Key technical details include the historical context (2005 glossary change marking "cache" as obsolete, 2008 `git diff` change adding `--staged`), and the current split between command families. The proposal is likely to be uncontroversial from a technical standpoint but may prompt discussion about backward compatibility and whether all index-related commands should be updated at once.