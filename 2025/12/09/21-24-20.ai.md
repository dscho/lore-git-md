This is a technical discussion about the macOS iconv(3) workaround implementation, continuing the thread about stateful encoding conversion issues. Torsten Bögershausen responds to René Scharfe's critique of his earlier approach, acknowledging the need for state reset in the conversion process and suggesting refinements to René's proposed solution.

The email focuses on two key implementation details: First, Torsten confirms that state reset (`iconv(conv, NULL, NULL, NULL, NULL)`) is indeed necessary, having verified this through testing. Second, he proposes moving some buffer position calculations into the `else` clause of the conditional block for better code organization.

Torsten raises several open questions about the broader approach: whether to use `__APPLE__` or `ICONV_BREAKS` for platform detection, whether the workaround should apply to all macOS versions (not just the affected ones), and whether a Makefile configuration knob is needed. He also notes some code-level concerns about pointer arithmetic and variable naming in René's implementation, particularly around the handling of the input size (`insz`).

The discussion remains focused on the technical implementation rather than revisiting the fundamental problem (already established in the thread) or the high-level solution approaches. This email represents an iteration toward a final implementation, with Torsten providing specific code review feedback while accepting the core technical requirements René identified.