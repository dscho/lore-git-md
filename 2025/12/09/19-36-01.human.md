Feature addition to `git repo info` with NUL-terminated format support for key listing.

This is the second version of a patch series adding a `--keys` flag to `git repo info`, allowing users to list available keys without values. The main change from v1 is the addition of format support (both default line-based and NUL-terminated output) for the key listing functionality.

The implementation now properly handles invalid combinations (like using `--keys` with `--all` or specific key arguments) and supports both default and NUL-terminated output formats. The documentation has been updated to reflect these capabilities, and new tests verify the behavior.

The patch appears well-considered, addressing previous review feedback while maintaining the core functionality. The commit messages are clear and the test coverage looks thorough. This seems like a useful addition for programmatic use cases where only key names are needed, and the implementation choices (like rejecting invalid combinations) show good attention to edge cases.

The series builds on the recently merged `lo/repo-struct-z` work and consists of two patches: one adding a default output format enum, and the main patch implementing the `--keys` functionality. The changes are focused and the diff shows careful attention to the review feedback from the first submission.