This is a technical discussion in the ongoing refactoring series about object database alternates handling. Justin Tobler follows up on Patrick Steinhardt's explanation about moving alternate path resolution into the source abstraction layer.

Justin acknowledges Patrick's clarification about the goal of making alternates handling work generically across different ODB source types, not just filesystem-based ones. He agrees with renaming the function to `odb_add_alternate_source()` to better reflect its specific purpose.

The email raises an interesting architectural consideration about how alternates are represented in `struct object_database`. Justin notes that while the current single-list approach works for reading objects, it creates some fragility for write operations (like ODB transactions) that need to treat the primary source specially. He suggests either making alternates more explicitly separate from the primary source, or handling write destinations through a different mechanism.

This appears to be a forward-looking design discussion rather than a critique of the current patch series. The exchange shows constructive technical dialogue between the two developers as they refine the ODB abstraction work.