Here's a human-readable thread summary:

This thread discusses the potential deprecation of `git whatchanged`, a command that shows commit history with file change information. While technically redundant with `git log --raw`, some users still find it valuable for its simplicity and memorable name.

A user has shared their perspective that `whatchanged` remains useful in their workflow, particularly for those who find `git log`'s extensive options harder to remember. They don't oppose deprecation but suggest improving the transition by making the warning message more actionable - specifically recommending users create an alias like `git config --global alias.whatchanged "log --raw"` as a migration path.

The discussion highlights the tension between maintaining clean code and preserving user-friendly interfaces, with this particular case focusing on how to best guide users when functionality moves to more general-purpose commands. The suggestion aims to make any eventual removal less disruptive while acknowledging the maintenance rationale behind deprecation.