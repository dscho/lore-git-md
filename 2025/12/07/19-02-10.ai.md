Feature addition to `git-repo-info` command.

This patch introduces a new `--keys` flag to `git repo info` that lists available keys without their values, addressing a suggestion from Junio. The change is straightforward: it adds documentation, implements the flag in `builtin/repo.c` with a new `print_keys()` function that iterates through the `repo_info_fields` array, and updates tests to verify the new functionality. The implementation replaces a hardcoded list of keys in the test suite with a dynamic check that compares `--all` output against using `--keys` to fetch the same keys.

The patch appears well-crafted with clear documentation and test coverage. It follows the existing code style and integrates smoothly with the command's existing `--format` and `--all` options. The change is small and focused, touching only the necessary files (documentation, implementation, and tests). The test modification is particularly thoughtful, eliminating a maintenance burden by removing the hardcoded key list in favor of runtime verification.

Key technical details for future reference: the new `--keys` flag is implemented alongside existing flags in `cmd_repo_info()`, the `print_keys()` helper outputs one key per line by iterating `repo_info_fields`, and the test now uses command substitution to verify `--keys` output matches `--all` behavior. The patch builds on commit bdc5341ff6 with `lo/repo-struct-z` merged.