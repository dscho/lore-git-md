This is a discussion about the potential deprecation of `git whatchanged`, focusing on improving the migration path for users. Kristoffer Haugsbakk responds to Lee Smith's practical use case for keeping the command, acknowledging that while `git log --raw --no-merges` provides equivalent functionality, some users find the dedicated command easier to remember.

The email suggests that the current `--raw` output might be too verbose for interactive use, proposing alternatives like `--stat` or `--name-only` as potentially more user-friendly options. However, it maintains a neutral stance on workflow preferences, recognizing that different users have valid approaches.

Most helpfully, the reply provides a concrete alias suggestion that users could adopt if the command is eventually removed: `git config set --global alias.whatchanged 'log --raw --no-merges'`. This addresses Lee's suggestion to improve the deprecation messaging by giving users a ready-made migration path rather than just a warning.

The exchange shows the tension between codebase maintenance (removing redundant commands) and user experience (preserving familiar interfaces), with this particular response focusing on making any transition as smooth as possible for existing users.