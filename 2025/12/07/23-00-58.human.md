This is a technical discussion about refactoring in the `git replay --revert` series. Siddharth Asthana responds to Patrick Steinhardt's question about why the newly extracted `sequencer_format_revert_header()` function doesn't also handle the `refer_to_commit()` logic.

The key point is that `refer_to_commit()` depends on sequencer-specific options (particularly `commit_use_reference`) that aren't relevant to the non-interactive `replay` context. Siddharth explains that while the revert message formatting was worth extracting as shared code, the commit reference formatting is intentionally kept separate due to these different use cases between the sequencer and replay operations.

The response shows careful consideration of architectural boundaries - avoiding leaking sequencer internals into the shared infrastructure while keeping the door open for alternative approaches if Patrick sees a cleaner solution. This level of thoughtful design discussion is typical of the high-quality technical exchanges happening in this thread.