Thread summary:

Documentation patch clarifying Git bash completion behavior for options and arguments, with two iterations. The thread focuses on git-completion.bash's header comment and commit message. The initial patch updated the script's header to explicitly document that it completes --long-options but not single-letter options, while supporting arguments for both option types (e.g. -c` with config vars). Junio Hamano's follow-up enriches the commit message with developer rationale - explaining the design decision to exclude short option completion (limited value compared to long options) while keeping this reasoning in commit history rather than script comments. The changes maintain a clear separation between end-user documentation (script comments) and developer context (commit message). No functional changes to the completion behavior are proposed - the thread remains focused on accurately documenting the existing implementation's capabilities. Both patches are small, focused documentation updates targeting different audiences (users vs. maintainers) while preserving the script's actual behavior.