Thread summary:

Documentation patch clarifying Git bash completion behavior for short options and arguments. The patch updates the header comment in git-completion.bash to explicitly state that the script completes common --long-options but not single-letter options, while noting that arguments to both long and short options are supported (e.g. -c` followed by config vars). The change makes the script's actual behavior more transparent in its documentation, correcting the original 2007 text which only mentioned long options without clarifying the exclusion of short ones. The patch provides a clear, focused update to the comment block to match the implementation's capabilities.