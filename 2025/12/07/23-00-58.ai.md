Refactoring patch in the `git replay --revert` series, addressing code organization question from prior discussion.

This email responds to Patrick Steinhardt's review of the v2 patch that extracted revert message formatting into `sequencer_format_revert_header()`. The key technical point here is explaining why `refer_to_commit()` wasn't included in the shared function - it depends on sequencer-specific `replay_opts` and the `commit_use_reference` flag that aren't relevant to the non-interactive `replay.c` context. 

The author (Siddharth Asthana) provides rationale for keeping the commit reference formatting separate:
- `replay.c` always uses full OIDs via `oid_to_hex()`
- Sharing would require exposing sequencer internals or adding complexity
- Current separation maintains clean boundaries between interactive (sequencer) and non-interactive (replay) use cases

This advances the thread by resolving one of the open questions from the v2 review - whether to consolidate more message formatting logic. The explanation suggests the current pragmatic approach while leaving the door for reconsideration if reviewers propose a cleaner solution. The response maintains the series' focus on keeping the revert functionality consistent between components while respecting their architectural differences.