This is the third and final patch in a documentation series improving the `git replay` man page. It focuses on proper Asciidoc markup for internal section links.

The patch makes two simple but important changes to `Documentation/git-replay.adoc`:
1. Replaces a plain text reference to the "OUTPUT section" with proper Asciidoc link syntax (`<<output,OUTPUT>>`)
2. Adds the missing `[[output]]` anchor that the link references

These changes complete the technical cleanup of the man page's formatting. While minor, they ensure consistent linking behavior and make the documentation more maintainable. The patch follows the same careful approach as the previous two in the series, which documented silent conflict resolution and the `--contained` option.

As the final piece of this focused documentation improvement, this patch appears ready for straightforward inclusion. The series as a whole addresses real gaps in the `git replay` documentation without introducing any behavior changes.