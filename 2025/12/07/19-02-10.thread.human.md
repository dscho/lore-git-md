Junio Hamano has reviewed the initial patch adding a `--keys` flag to `git repo info`, which implements his earlier suggestion to make key discovery more programmatic. While he approves of the feature's concept and the clear commit message, he points out several technical shortcomings in the current implementation. 

The main issues involve option handling: the `--keys` flag currently ignores `--all` and key arguments without warning, and fails to respect the `-z` format flag for NUL-terminated output. Junio recommends making these behaviors explicit by erroring out on invalid combinations and properly supporting NUL termination in the key listing. 

This feedback keeps the feature on track while addressing important consistency and robustness concerns. The author will need to send a revised version incorporating these changes, but the review suggests the feature itself is sound and likely to be accepted once these implementation details are fixed. The thread remains focused and uncontentious, with clear direction for the next steps.