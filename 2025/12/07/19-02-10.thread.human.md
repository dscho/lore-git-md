This thread introduces a new `--keys` flag for the `git repo info` command, allowing users to list available keys without their values. The change was prompted by a suggestion from Junio, the Git maintainer.

The patch is well-structured, adding documentation, implementation in `builtin/repo.c` with a new `print_keys()` function, and updated tests. The test improvements are particularly notable - they replace a hardcoded list of keys with a dynamic check that compares output between the new `--keys` flag and existing `--all` option. This eliminates a maintenance burden while thoroughly verifying the new functionality.

The change is small and focused, touching only the necessary files while maintaining consistency with existing `--format` and `--all` options. The implementation builds on commit bdc5341ff6 and appears ready for merging, with no outstanding questions or concerns raised in the discussion.