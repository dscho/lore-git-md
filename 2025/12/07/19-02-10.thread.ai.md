Thread identity: `git repo info --keys` feature addition, patch series by Lucas Seiki Oshiro, reviewed by Junio Hamano and Patrick Steinhardt.

Core problem or goal: Add a `--keys` flag to `git repo info` to list available keys without values, implementing Junio's prior suggestion. The change enables programmatic key discovery and removes test maintenance burden by replacing hardcoded key lists with dynamic verification.

Current status: Initial patch version implementing the feature needs revision per Junio's review. Builds on commit bdc5341ff6 (`lo/repo-struct-z` merged). Key issues to address:
- Currently silently ignores `--all` and key arguments when `--keys` is given
- Doesn't respect `-z` format flag for NUL-terminated output
- Needs explicit error handling for invalid option combinations
- Commit message wording fix needed ("ask to all" -> "ask for all")

Key technical details: Implementation in `builtin/repo.c` via `print_keys()` function iterating `repo_info_fields` array. Tests modified to use command substitution for dynamic key verification. Changes needed:
- Add error handling for `--keys` + `--all`/key arguments combinations
- Pass format flag to `print_keys()` for NUL-terminated output support
- Fix commit message wording per Patrick's suggestion
- Maintain existing documentation and test updates

Open questions: None remaining after addressing Junio's review comments - implementation refinements are clearly specified.

Participant positions: 
- Junio Hamano: Suggested feature originally, approved current approach with specific implementation fixes needed
- Lucas Seiki Oshiro: Implemented initial version that needs revision per review
- Patrick Steinhardt: Noted minor commit message wording improvement needed

Related work: Builds on `lo/repo-struct-z` (bdc5341ff6). Review confirms feature aligns with project direction.