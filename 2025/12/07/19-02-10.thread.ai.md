Thread identity: `git repo info --keys` feature addition, patch series by original author (name not yet specified in this email).

Core problem or goal: Add a `--keys` flag to `git repo info` to list available keys without values, implementing a suggestion from Junio Hamano. The change makes key discovery more programmatic and eliminates a maintenance burden in tests.

Current status: Initial patch version implementing the feature, with documentation and test updates. Builds on commit bdc5341ff6 (`lo/repo-struct-z` merged).

Key technical details: New `--keys` flag implemented in `builtin/repo.c` via `print_keys()` function that iterates `repo_info_fields` array. Tests modified to dynamically verify keys by comparing `--all` output with `--keys` results rather than maintaining hardcoded key list. Changes limited to:
- Documentation update for new flag
- `cmd_repo_info()` flag handling
- New `print_keys()` helper
- Test suite updates using command substitution

Open questions: None raised yet - appears ready for review. Junio's prior input suggests likely acceptance if implementation matches his suggestion.

Participant positions: Junio Hamano suggested this functionality in prior discussion (implied but not quoted in this patch). Original author (unnamed) implemented the suggestion.

Related work: Builds on `lo/repo-struct-z` (bdc5341ff6).