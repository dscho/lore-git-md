Thread identity: `git repo info --keys` feature addition, patch series by Lucas Seiki Oshiro, reviewed by Junio Hamano and Patrick Steinhardt.

Core problem or goal: Add a `--keys` flag to `git repo info` to list available keys without values, implementing Junio's prior suggestion. The change enables programmatic key discovery and removes test maintenance burden by replacing hardcoded key lists with dynamic verification.

Current status: Second patch version addresses Junio's review comments from initial submission. Builds on commit bdc5341ff6 (`lo/repo-struct-z` merged). Key changes in v2:
- Explicit error handling invalid combinations (`--keys` + `--all`/key arguments)
- Considering but deferring `-z` format support for NUL-terminated output
- Maintaining core functionality of `print_keys()` iterating `repo_info_fields`
- Outstanding decision: whether to reject format options with `--keys` or implement format support

Key technical details: Implementation in `builtin/repo.c` with changes:
- Added error paths for invalid option combinations
- Kept simple key listing via `print_keys()` without format handling
- Tests updated to use command substitution for dynamic verification
- Commit message wording fixed per Patrick's suggestion

Open questions: Final decision needed on format option handling - whether to:
1. Reject format-related options with `--keys` (simpler)
2. Implement full format support including `-`z (more complex)

Participant positions:
- Junio Hamano: Originally suggested feature, reviewed initial implementation
- Lucas Seiki Oshiro: Implemented v2 addressing review comments, leaning toward simpler option rejection
- Patrick Steinhardt: Provided commit message wording suggestion in initial review

Related work: Builds on `lo/repo-struct-z` (bdc5341ff6). Feature aligns with project direction for programmatic interfaces.