Feature addition to `git replay` implementing `--revert` mode, addressing review feedback on the v2 patch series. This email responds to Patrick Steinhardt's technical suggestions for improving the implementation quality of the revert functionality.

The author agrees with several proposed improvements:
1. Adding defensive `BUG()` checks for unexpected action values in `create_commit()`
2. Using `FREE_AND_NULL()` for safer memory management in merge operation cleanup
3. Consolidating option validation with `die_for_incompatible_opt3()` for the mutually exclusive --revert/--advance/--onto modes
4. Merging duplicate ref update logic between revert and advance operations into a single conditional block

These changes address implementation details while maintaining the core functionality that:
- Swaps base/pickme trees to reverse diffs during revert operations
- Uses standard Git revert message formatting
- Preserves the existing transaction-based ref update behavior
- The response indicates these changes will be incorporated in a forthcoming v3 of the patch series.

The discussion remains focused on code quality improvements rather than architectural changes, as the core design decisions were settled in earlier thread discussion. The implementation appears to be maturing with these refinements to error handling, memory management, and code organization.