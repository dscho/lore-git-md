Feature refinement for `git replay`'s `--revert` mode, addressing review feedback. This email from Siddharth Asthana responds to Patrick Steinhardt's code review of the second patch in the `--revert` series, agreeing to implement several suggested improvements:

1. Adding defensive `BUG()` checks for unexpected action values in `create_commit()`
2. Using `FREE_AND_NULL()` for safer memory management in merge operation cleanup
3. Simplifying option validation with `die_for_incompatible_opt3()` for the mutually exclusive `--onto`/`--advance`/`--revert` modes
4. Consolidating ref update logic between `--advance` and `--revert` modes into a single conditional block

The changes address implementation quality concerns while maintaining the core functionality that allows `git replay` to reverse commits by swapping base/pickme trees during merges. The response indicates all suggestions will be incorporated in v3 of the patch series.

This continues the collaborative refinement of the feature, with the implementation now addressing edge cases and adopting Git's standard patterns more thoroughly. The changes don't modify the fundamental behavior but improve code safety and maintainability.