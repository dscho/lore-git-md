This is the second patch in a documentation series for `git replay`, focusing on adding documentation for the previously undocumented `--contained` option.

The patch adds a simple but important piece of documentation by copying the option description directly from the source code in `builtin/replay.c`. The `--contained` option causes `git replay` to advance all branches that are contained within the specified revision range, which differs from the default behavior of only updating explicitly named branches.

The change is straightforward - it inserts a new documentation block for the option in the man page, placed logically among other option descriptions. The description matches the implementation exactly, suggesting this is documenting existing behavior rather than proposing new functionality.

This patch follows the series' pattern of filling in documentation gaps for `git replay`, specifically targeting options that were previously only described in the implementation. The change is minimal and focused, with no apparent controversy or complexity. Reviewers might want to verify the wording matches the actual behavior, but since it's copied from the source, this seems likely to be accurate.