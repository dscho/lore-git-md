This is a feature patch adding a new `--keys` flag to `git repo info`. The new option provides a way to list just the available keys without their values, addressing a usability gap where users previously had to either check documentation or use `--all` to see all key-value pairs.

The patch follows up on a suggestion from Junio Hamano and implements the feature cleanly. It adds documentation, implements the new flag in the command's logic, and updates tests to verify the behavior. The implementation is straightforward - it simply iterates through the existing field definitions and prints each key.

The changes look well-contained and properly tested. The test suite is updated to verify that `--keys` produces the same set of keys that were previously hardcoded in the test file, then confirms that using those keys with the command produces the same output as `--all`. This provides good coverage of both the new functionality and backward compatibility.

This appears to be a small but useful quality-of-life improvement for users of `git repo info`, particularly for scripting or discovery purposes. The patch seems ready for review with no obvious issues.