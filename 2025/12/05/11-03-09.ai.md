This is a technical discussion about debugging stale Git locks, specifically addressing concerns around PID file implementation for the files reference backend.

Patrick Steinhardt responds to Taylor Blau's earlier message about PID file naming conventions and namespace collisions. He agrees the current `.lock.pid` approach isn't feasible for the files backend due to compatibility concerns with alternative Git implementations and older versions. While acknowledging the debugging utility, Patrick suggests the benefit may not justify introducing a repository extension to handle this.

The email explores two alternative approaches: 1) A toggle mechanism to selectively disable PID file creation (though this might reduce the feature's usefulness since ref operations are the main debugging target), and 2) An unconventional solution of appending PID information directly to loose reference files (despite triggering fsck warnings). Patrick leaves open whether either approach is desirable, highlighting the tension between debugging needs and implementation constraints.

Key technical points include: the files backend's special handling of `.lock` suffixes, cross-version compatibility concerns, and the fsck implications of modifying loose ref file formats. The discussion continues to weigh the debugging utility against implementation complexity, with references remaining the primary use case motivating the feature.