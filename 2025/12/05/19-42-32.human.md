This is part of the ongoing submodule gitdir path encoding series, specifically discussing build-time configuration options for the new `extensions.submodulePathConfig` feature.

The patch adds a build-time option to enable-submodule-encoding` (for autoconf) and `submodule-encoding` (for meson) to enable the submodule path encoding extension by default, avoiding the need for users to manually configure it. Patrick Steinhardt questions whether a runtime configuration would be more appropriate, but Adrian Ratiu explains this was specifically designed to minimize user intervention based on feedback from Junio Hamano and others.

The exchange reveals an architectural tension between strict config requirements (Patrick's preference) and practical migration needs (Junio's guidance for automation). Adrian takes a neutral stance, open to adding runtime configuration while maintaining the build-time option as requested by others. The discussion shows the series is nearing finalization, with this being one of the last open questions before potential merging.

The patch appears well-considered, with Adrian carefully balancing different stakeholders' needs while maintaining the series' goal of automated transition to encoded submodule paths. The technical implementation seems sound, though the final decision on build-time vs runtime configuration remains open.