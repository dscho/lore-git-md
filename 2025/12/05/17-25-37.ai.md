This is a technical refinement in the submodule gitdir path encoding series, specifically addressing code organization for URL encoding utilities.

The patch moves the `is_rfc3986_unreserved()` helper function from credential-store.c to url.[ch] to make it available for submodule path encoding. Patrick Steinhardt points out two minor issues in the commit message that need fixing in v6: an outdated statement about the function's future use in submodule.c (which has already happened), and the placement of function documentation which should be in the header file rather than the .c file.

The change itself is straightforward - it relocates a utility function that checks for URL-safe characters to a more central location where it can be shared between credential storage and submodule path encoding. The function's implementation remains identical, just moved to url.c with its documentation. This is part of the broader effort to consolidate URL encoding utilities as the submodule path encoding feature requires the same RFC3986 character validation that was previously only needed for credentials.

The discussion represents final polish on code organization details as the series approaches completion, with Patrick Steinhardt providing last-minute documentation placement feedback before the next version. The change doesn't affect functionality but improves maintainability by properly locating shared utilities and their explanations.