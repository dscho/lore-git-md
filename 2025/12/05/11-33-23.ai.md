Refactoring patch in the `git replay --revert` series, focusing on message formatting extraction. Patrick Steinhardt questions why the newly introduced `sequencer_format_revert_header()` helper function doesn't also handle the `refer_to_commit()` operation, which follows the revert message header and includes the commit SHA being reverted.

This is a preparatory patch in the v2 series that factors out common revert message formatting logic from `do_pick_commit()` into a shared helper. The current implementation leaves `refer_to_commit()` as a separate call after using the helper, which Patrick flags as potentially incomplete factoring. The question touches on code organization principles - whether related operations should be colocated in helpers or kept separate for flexibility.

The thread context shows this is part of a larger effort to support reverts in `git replay`, with the message formatting being shared between `sequencer.c` and the new replay functionality. The discussion around this patch may influence how future shared functionality is factored between these components, particularly as the series progresses toward handling more complex revert scenarios and disconnected commit cases.