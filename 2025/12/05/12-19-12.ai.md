This is a technical discussion about the submodule path encoding series, specifically focusing on patch 4/7 which introduces the `extensions.submoduleEncoding` config. Patrick Steinhardt raises several substantive points about the design and implementation.

The key critique is that the current naming and documentation of the extension over-emphasizes the encoding aspect when the more fundamental change is shifting the source of truth for submodule gitdir paths from dynamic computation to persistent configuration. He suggests renaming the extension to something like "submodulePathConfig" to better reflect this architectural shift.

Patrick identifies two main technical concerns in the implementation. First, he questions the fallback behavior when the gitdir config is missing but the extension is enabled, arguing it should error out rather than trying legacy paths. Second, he observes the function conflates two distinct use cases (reading existing paths vs computing new ones) and suggests separating these concerns for clearer semantics.

The discussion reveals important architectural considerations about how strictly the new configuration-based approach should be enforced. Patrick's comments indicate this is a final review pass focusing on edge cases and semantic clarity rather than fundamental objections to the approach, which has already received architectural approval from multiple maintainers.

The thread context shows this is part of a mature series that has addressed previous rounds of feedback, with Patrick now scrutinizing subtle correctness details in the transition to centralized path resolution. His suggestions would make the behavior more strict and explicit about the configuration-as-source-of-truth model.