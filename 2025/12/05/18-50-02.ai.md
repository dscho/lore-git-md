This is a technical follow-up in the performance optimization thread for promisor object handling, where Aaron Plattner responds to Jeff King's proposed flag-based approach using `PARSE_OBJECT_SKIP_HASH_CHECK`.

Aaron confirms Jeff's approach works in principle but reports it doesn't improve performance in his testing due to an edge case with `OBJ_NONE`-typed objects. He identifies that `parse_object_with_flags()` skips the blob optimization path when encountering unparsed objects (those with `obj->type == OBJ_NONE`), causing full inflation to still occur. 

Aaron suggests modifying the type check in `parse_object_with_flags()` to include `OBJ_NONE` alongside `OBJ_BLOB`, which when combined with Jeff's flag approach reduces his test time from 2 minutes to 1:58 (though still not matching Jeff's 49s result). The discrepancy suggests either environmental differences in their test setups (like commit-graph availability) or reveals a subtle behavior in the object parsing pipeline that needs reconciliation.

This email surfaces an important implementation detail about how unparsed objects interact with the optimization path, potentially requiring a tweak to the type-checking logic in `parse_object_with_flags()`. The discussion remains focused on achieving maximum performance while maintaining correctness, with both contributors building on each other's findings through empirical testing.