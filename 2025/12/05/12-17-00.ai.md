This is a technical review question about the submodule gitdir path encoding series, specifically patch 3/7 which removes validation checks in favor of centralized path resolution.

Patrick Steinhardt raises two concerns about the removal of `validate_submodule_git_dir()` calls in this patch. First, in the clone path, he questions why the TOCTOU race protection is no longer needed when using the unified `submodule_name_to_gitdir()` API, since the validation was specifically guarding against concurrent modifications between initial checks and final operations. Second, in the move-head path, he notes that one branch retrieves paths directly rather than through `submodule_name_to_gitdir()`, potentially bypassing the new centralized validation.

The thread context shows this is part of a mature series that has already incorporated significant review feedback, with architectural agreement on using `submodule_name_to_gitdir()` as the single source of truth for path resolution. The validation being removed was part of the older distributed checking approach. Patrick's questions appear to be ensuring the transition to the new model doesn't accidentally reintroduce race conditions or validation gaps, particularly in edge cases like concurrent operations and direct path access.

This email doesn't propose changes but seeks clarification on design decisions, indicating the series is in final review stages with attention to subtle correctness details. The response will likely need to explain how the new architecture handles these cases either through different mechanisms or by demonstrating they're no longer applicable.