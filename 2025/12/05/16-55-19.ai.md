Performance optimization follow-up in the promisor pack handling thread. Aaron Plattner responds to Patrick Steinhardt's memory efficiency suggestions for the previously proposed blob-parsing optimization in `add_promisor_object()`.

Key technical points in this response:
- Questions whether the `data->repo` context in Patrick's patch is redundant with existing `pack->repo` access
- Proposes a simpler alternative diff that just adds `free_commit_buffer()` call using `pack->repo->parsed_objects`
- Provides benchmark data showing minimal memory impact from commit buffer freeing (8.1GB -> 7.8GB max RSS)
- Concludes the memory savings aren't significant enough to warrant the change at this time

The exchange shows the original performance optimization (skipping blob parsing) remains the primary win, while the follow-up memory improvements yield diminishing returns. The thread maintains focus on promisor pack handling without expanding scope. No new technical concerns are raised about either approach, just a practical assessment of the tradeoffs.