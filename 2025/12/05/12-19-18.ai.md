This is a technical refinement discussion in the submodule path encoding series, specifically addressing the build-time enablement option added in patch 7/7. Patrick Steinhardt questions whether the build-time configuration approach (added via autoconf and meson) is the right design choice, suggesting instead that a runtime configuration key in gitconfig would be more appropriate. This represents a late-stage design critique of how the feature's deployment flexibility is implemented.

The thread context shows this is part of a mature series that has already received architectural approval, with Patrick previously contributing oversight and now focusing on semantic refinements. The build-time option was added to address deployment concerns from Josh Steadmon, allowing distributions to enable the feature by default. Patrick's question probes whether this build-time knob is necessary given the existing repo-level config ( extensions.submoduleEncoding) and whether runtime configuration would provide equivalent flexibility without complicating the build system.

Key technical points at stake:
- Current implementation offers three enablement methods: repo config, command-line config, and now build-time flag
- Patrick suggests consolidating to runtime configuration (either repo or global gitconfig)
- The question implies potential redundancy between build-time and runtime configuration layers
- Build-time option was specifically added to ease deployment for distribution packagers

This email doesn't propose concrete changes but signals potential design reconsideration late in the review cycle. The thread context shows Patrick has been deeply involved in architectural decisions throughout, making this a substantive critique from a key reviewer rather than a new concern. The series remains technically complete but may see last-minute design adjustments based on this feedback.