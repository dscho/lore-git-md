Performance optimization for promisor object handling, continuing the thread about reducing overhead in partial clone scenarios.

Jeff King responds to Aaron Plattner's patch that avoided full parsing of blobs in promisor packs, confirming the significant performance improvement (76min -> 2min for large packs) but suggesting a simpler approach. He notes that the test case needs adjustment to properly trigger promisor checks, proposing a synthetic OID method instead.

The key technical contribution is that `parse_object_with_flags()` with `PARSE_OBJECT_SKIP_HASH_CHECK` can achieve similar blob-skipping behavior while also leveraging commit-graph optimizations, reducing the test case from 2 minutes to 49 seconds. This builds on Aaron's core insight while integrating with existing optimization paths.

Jeff's approach maintains the explicit type-checking while potentially being more maintainable by using existing parse_object infrastructure. He notes this requires a pre-generated commit-graph to see full benefits, which is common but not guaranteed in all workflows. The discussion remains focused on the performance optimization path, with Jeff's suggestion representing an incremental refinement rather than a fundamental disagreement with Aaron's approach.