This is a continuation of the ongoing discussion about test infrastructure improvements, specifically around integrating the clar unit test framework with Git's test suite.

Jeff King (Peff) responds to Patrick Steinhardt's updates about clar fixes, acknowledging the merged TAP output fix (changing `---` to `...` to avoid confusing TAP parsers) but raising deeper concerns about test output handling in CI. The core issue is that clar's output isn't properly captured in CI runs, unlike the main test suite which uses `--verbose-log`.

Peff explores two potential solutions:
1. Modifying the clar wrapper to handle output redirection, which would require understanding `$TEST_OUTPUT_DIRECTORY` and duplicating output streams
2. Running clar tests through the main test suite infrastructure, which already handles these concerns

The second approach shows promise but reveals complications around cleanup handling when using `exec` to bypass test-lib.sh's TAP expectations. Peff notes the removed `test_external` mechanism might have been useful here and questions whether its removal was premature given this use case.

Key technical details:
- Current clar wrapper lacks proper output capture in CI
- `exec` approach skips test-lib.sh cleanup handlers
- Alternative `test_expect_success` wrapper loses granular TAP output
- Historical context: test_external was removed in 5beca49a0b

This continues the thread's theme of improving test infrastructure while maintaining compatibility with Git's existing systems. The discussion remains focused on practical implementation tradeoffs rather than fundamental disagreements.