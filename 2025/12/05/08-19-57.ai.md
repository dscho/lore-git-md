This is a cover letter for a series of three bugfix patches related to the object database (odb) subsystem. The series addresses various small issues Patrick Steinhardt encountered while working on other patches, split out to avoid creating unnecessary dependencies between patch series.

The first patch fixes geometric repacking when promisor remotes are involved, adding test coverage to prevent regression. The second corrects a condition in `git gc` that determines whether to write commit graphs. The third ensures proper cleanup of odb sources by closing them before freeing resources.

The changes touch core functionality in `builtin/repack.c`, `builtin/gc.c`, and `odb.c`, with corresponding test updates in `t/t7703-repack-geometric.sh` and `t/t7900-maintenance.sh`. The series builds on recent work around quarantine directory handling (commit ac65c70663).

The patches appear focused and well-scoped, each addressing a distinct issue with accompanying tests. The cover letter clearly explains the motivation for splitting these fixes into their own series rather than bundling them with other ongoing efforts. The technical changes seem straightforward with clear test coverage added for the repack and gc fixes.