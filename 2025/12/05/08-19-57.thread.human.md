Patrick Steinhardt's bugfix series addressing two memory management issues has now received final review approval and is ready for merging. The series fixes a commit-graph writing memory leak in `builtin/gc.c` and a submodule ODB leak in `odb.c`, both with improved test coverage. Justin Tobler has confirmed the v2 changes look good, noting the geometric repack patch was correctly dropped as it requires more extensive work on promisor pack handling - an issue that will be addressed separately.

The fixes have undergone thorough review from multiple contributors, including Toon Claes who verified the commit-graph changes, and now have robust test coverage. The commit messages were cleaned up to remove references to the withdrawn patch. With all technical concerns addressed and multiple approvals, the series is in good shape to be picked up by the maintainer. The only remaining work is the deferred geometric repack/promisor interaction issue, which doesn't block these contained fixes.