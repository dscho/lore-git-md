Performance optimization follow-up in promisor pack handling. Jeff King responds to Aaron Plattner's discovery of an edge case where `OBJ_NONE`-typed objects bypass the optimization path in `parse_object_with_flags()`. 

Jeff agrees this appears to be a bug in the logic that checks for blob objects, since `OBJ_NONE` should also be considered a potential blob candidate. He's curious about how these `OBJ_NONE` objects are created in the first place, suggesting it might indicate an unusual object creation pattern, though this doesn't affect the immediate fix.

The discussion reveals some performance measurement discrepancies - while Aaron sees a modest improvement (2 minutes to 1:58) with the combined fixes, Jeff had seen more dramatic gains in his testing. They speculate this could be due to differences in repository structure (blob-heavy vs commit-heavy) or commit-graph availability.

Jeff maintains his `PARSE_OBJECT_SKIP_HASH_CHECK` approach as the preferred solution, now with the additional `OBJ_NONE` handling, though he acknowledges the optimization's impact may vary across repositories. The exchange shows the careful iteration typical of performance work, where edge cases and environmental factors must be considered even after the core optimization is validated.