This is a discussion about build-time vs runtime configuration for the submodule path encoding feature. Patrick Steinhardt questions Adrian Ratiu's approach of adding a build-time option (`--enable-submodule-encoding`) to enable the submodule encoding extension by default. 

Patrick suggests that making this a runtime configuration (via `gitconfig`) would be more sensible than baking it into the build system. This appears to be a final design consideration in the otherwise technically complete series, touching on how users will ultimately enable this feature in practice.

The thread context shows this is part of an extensive effort to handle submodule path encoding conflicts, with broad architectural approval but ongoing refinement of the user-facing details. Patrick's question probes whether the build-time default aligns with Git's typical configuration patterns, suggesting the team may want to keep all configuration at runtime rather than mixing build-time and runtime knobs.