This is a technical follow-up in the `git replay --revert` feature series, specifically addressing a refactoring patch that extracts revert message formatting logic into a shared helper function.

Patrick Steinhardt questions why the new `sequencer_format_revert_header()` helper doesn't also handle the `refer_to_commit()` functionality that follows it in the original code. This appears to be a focused code review comment rather than a new proposal - he's pointing out what seems like a logical extension of the refactoring work that was just introduced.

The context shows this is part of a well-developed v2 implementation that has already seen significant design discussion. The core functionality appears solid, with this being a relatively minor implementation detail about code organization. The thread indicates the overall approach has consensus, with ongoing refinement of edge cases and implementation polish.

This email doesn't introduce new functionality or design changes, but rather suggests a possible small improvement to the code structure. It's the kind of incremental review feedback that often appears late in a patch series' lifecycle when reviewers focus on implementation details after broader architectural questions are settled.