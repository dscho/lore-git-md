This is a review comment on the third patch in Patrick Steinhardt's object database bugfix series. Eric Sunshine points out a confusing reference in the commit message - the phrase "In the next commit" doesn't make sense for the final patch in a 3-patch series. The message appears to have been copied from an earlier version where this change wasn't the last in the sequence.

The patch itself (as noted in the thread summary) fixes a memory leak in submodule handling during git-grep operations by correcting the cleanup sequence in `odb_free()`. The technical content remains valid - it swaps `odb_close()` and `odb_free_sources()` calls to prevent resource leaks when loading packfiles/multi-pack-indexes from submodules. 

This appears to be a minor documentation issue rather than a problem with the code change. The thread context shows this is part of a focused series addressing ODB subsystem issues, with the other patches fixing geometric repacking behavior and commit graph memory leaks. The series builds on recent quarantine directory work and has straightforward technical fixes, though this particular patch lacks new test coverage despite including a detailed leak scenario description.