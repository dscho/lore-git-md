This is a clarification question from Junio Hamano about patch 8/13 in Patrick Steinhardt's object store refactoring series. The patch changes `git show-ref` to avoid fetching promisor objects when checking reference validity, arguing references should never point to missing objects in the first place.

Junio questions whether this assumption holds given the series' earlier changes - specifically whether the new `has_object()` API (without promisor-fetch flags) would correctly report "no" for promisor objects that haven't been fetched yet. He's probing whether there's another mechanism ensuring these objects get fetched before references are created to them, or if this might represent a gap in the design.

The exchange highlights an important architectural consideration in the transition from the old `repo_has_object_file()` API to the new flag-based system. Junio isn't objecting to the change, but seeks to understand the complete safety story around reference validation in partial clone scenarios. This ties back to his earlier questions about promisor object handling that arose in patch 7/13's collision check changes.