Here is the dense AI thread summary:

Bug report from Pavel Pavlov demonstrating unexpected behavior in shallow submodule checkouts with GitHub repositories. The submodule checkout fetches full history (34MB) instead of performing a shallow clone when the submodule reference points to a non-default GitHub branch, while working as expected (2.9MB) for default branch references. Two test repositories (submodule-test-ok and submodule-test-bad) show identical configurations except for their submodule branch references, clearly demonstrating the issue. The problem appears specific to GitHub's handling of non-default branches during shallow clones. Impact is significant for bandwidth-constrained environments where shallow clones are used to save resources. No proposed fix yet, but provides clear reproduction cases that could help diagnose whether this is a Git core issue or GitHub-specific behavior. The test repositories serve as concrete examples to investigate the branch reference handling in submodule shallow clone operations.