Here's a human-readable summary of the thread:

The thread discusses a bug in `pack-objects` where the `should_include_obj()` function is incorrectly being called on non-existent objects during tree processing. Jonathan Tan, who originally wrote this code, acknowledges the issue but can't recall why he assumed the function would only see existing objects. The problem stems from `process_tree()` in `list-objects.c` where missing objects are handled after `should_include_obj()` is called.

The proposed solution is to modify `should_include_obj()` to explicitly return 0 for absent objects. However, Tan notes the lack of a reproducible test case and has asked Nik (who reported the bug) to contribute a regression test similar to the existing 'after fetching descendants of non-promisor commits' test in t5616. This appears related to earlier work on promisor remotes, though the specific object existence assumption wasn't previously discussed.

The issue touches core packing functionality and partial clone behavior, particularly around how missing objects are handled during tree traversal. The discussion is currently waiting on a test case to verify the fix before proceeding.