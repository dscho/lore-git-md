Here's a human-readable summary of the thread:

The thread reports a concerning issue with shallow submodule checkouts from GitHub repositories. Pavel Pavlov demonstrates that when checking out a submodule that points to a non-default GitHub branch, Git unexpectedly fetches the full history (34MB) instead of performing a shallow clone. In contrast, submodules pointing to default branches work correctly (2.9MB checkout). Pavel provided two test repositories that clearly show this behavior difference - identical except for their submodule references.

This appears to be either a Git bug or GitHub-specific behavior affecting shallow clone operations. The impact is particularly significant for users in bandwidth-constrained environments who rely on shallow clones to save time and data. While no fix is proposed yet, the report includes clear, reproducible test cases that should help diagnose whether the issue stems from Git's submodule handling or GitHub's branch reference implementation.