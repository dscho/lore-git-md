**Follow-up confirmation in resolved thread**

Junio Luan Pereira provides additional diagnostic information confirming that switching Git for Windows' CURL option from WinSSL to OpenSSL does not resolve the certificate verification issue, as the underlying problem stems from Norton Antivirus's Safe Web feature interfering with Let's Encrypt certificate access. The email includes a full dump of their Git configuration from `install-options.txt` and curl version output showing Schannel remains present despite the OpenSSL selection.

This serves as final confirmation that the workarounds previously identified (disabling Norton Safe Web or using `--ssl-revoke-best-effort`) remain the only viable solutions, as the issue persists across SSL backends. The thread appears fully resolved with no further action needed from the Git side, as the root cause lies in third-party security software behavior rather than Git's implementation.