This is a discussion follow-up in Patrick Steinhardt's object store refactoring series, specifically addressing patch 10/13 which converts the HTTP walker to use the new `has_object()` API. Junio Hamano confirms that the rationale for avoiding promisor fetches in this context makes sense, contrasting it with the more questionable case in patch 7/13 regarding collision checks.

The HTTP walker patch changes the object existence check to use `has_object()` without the `HAS_OBJECT_FETCH_PROMISOR` flag, preventing redundant fetches when the object is already being fetched via the current HTTP connection. Junio agrees this is a clear improvement over the previous behavior where `repo_has_object_file()` would potentially trigger a separate promisor fetch.

This marks resolution of one of Junio's three outstanding questions about promisor handling in the series, specifically validating the approach taken in the HTTP transport layer. The discussion highlights the context-dependent nature of promisor object handling - what was questionable for collision checks is appropriate here. The exchange helps establish boundaries for when promisor fetching should be avoided versus when it might still be necessary for validation purposes.