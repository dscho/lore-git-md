This is a follow-up discussion in the ongoing build system patch thread about installing shell completion scripts. The thread has already established consensus on installing Bash completions while deferring Zsh support due to platform-specific quirks.

Patrick Steinhardt confirms he'll implement Junio Hamano's suggestion to mirror the Makefile approach by adding a `bash_completion_dir` variable in Meson, while leaving out Zsh support for now. The discussion shows clear alignment on the technical approach, with the path resolution for Bash being straightforward (`/usr/share/bash-completion/completions`) while Zsh remains problematic due to inconsistent platform behaviors.

The patch appears to be in its final refinement stage, with all major technical questions settled. The only remaining open item is the deferred Zsh support, which the participants agree should be addressed separately if/when a clear solution emerges. This email doesn't introduce new technical content but rather confirms the implementation direction based on prior discussion.