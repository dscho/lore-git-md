This is a follow-up email concluding a thread about configuration precedence between `pull.ff=only` and `branch.<name>.rebase`. D. Ben Knoble confirms that existing Git configuration already provides the desired behavior without needing new knobs, closing out earlier discussions about potential extensions.

The key realization is that combining `pull.rebase=true` with per-branch exceptions via `branch.<name>.rebase=false` achieves the workflow Knoble originally wanted: fast-forward-only behavior for main branches while allowing rebasing for topic branches. This was verified by examining existing test cases in `t/t5520-pull.sh`.

The thread had previously explored various solutions including new configuration options, but ultimately concludes that the current configuration system is sufficient. The main remaining need is better documentation to help users understand these existing capabilities. The discussion appears settled with consensus that no code changes are required.