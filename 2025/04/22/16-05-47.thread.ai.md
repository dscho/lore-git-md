Thread identity: Bugfix patch addressing wildcard pathspec handling in Git commands, reported by Piotr Siupa and mentored by Jeff King. Focuses on `git add` and `git commit` behavior with wildcard pathspecs.

Core problem: When a literal file named like a wildcard pattern exists (e.g. `f*`), commands like `git add f*` incorrectly skip matching files (e.g. `foo`). The patch ensures proper handling of escaped wildcards (e.g. `f\*` for literal matching).

Technical changes: Modifies `do_match_pathspec` in `dir.c` to correctly handle the `nowildcard_len` case. Introduces comprehensive test coverage in new file `t6137-pathspec-wildcards-literal.sh` that verifies wildcard patterns (`*`, `?`, `**`) and their escaped variants. Tests follow Git's standard structure with `test_cmp` verification.

Current implementation status: Initial version of patch submitted with thorough test coverage but needs refinement in commit message structure and test formatting. Skips Windows-specific tests due to filename limitations. Core logic in `dir.c` confirmed correct by maintainer.

Key improvements needed: Commit message should better explain current behavior and problem before solution. Test file needs proper Git conventions (tab indentation, HERE-DOC formatting). Tests should be more independent and avoid unnecessary cleanup. "Mentored-by" tag may need removal unless formal mentorship. Test setup should be more robust during directory changes.

Participant positions: Jeff King mentored the patch. Junio C Hamano reviewed and provided detailed feedback on presentation and test structure while confirming core logic is sound. No fundamental objections to approach, only requests for improved documentation and test hygiene.

Related work: Builds on existing pathspec handling infrastructure in Git. Windows limitations noted but not addressed in this patch. Junio's feedback provides clear direction for next version.