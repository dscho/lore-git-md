This is a CI/build system discussion continuing the thread about implementing header checking in Meson builds, specifically focusing on package manager detection for Git installation in CI workflows.

Junio C Hamano responds to Phillip Wood's suggestion to use `command -v` for cross-distro package manager detection in the CI workflow. He agrees with the approach, noting it's more portable than previous solutions that used `type` and appreciates the simplicity of having one command per package manager rather than per distro. He references a past issue with `awk` package naming inconsistencies as a cautionary note about potential pitfalls.

The email confirms two technical points from Phillip's proposal: 1) the omission of package cache updates is acceptable since they're handled elsewhere in install-dependencies.sh, and 2) using apt-get matches existing practices in the dependency installation script. Junio also notes the curious absence of an apt-get availability check, hinting at potential future discussion points about apt vs apt-get usage.

This exchange represents final refinements to the CI workflow portion of the larger header checking implementation, with Junio providing his maintainer perspective on the proposed cross-distro package manager detection strategy. The thread shows consensus forming around Phillip Wood's technical solution while leaving room for minor follow-up discussions about apt/apt-get handling.