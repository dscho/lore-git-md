This is a CI/build system discussion about repository detection strategies in the Meson build system implementation. The email continues the thread about Karthik Nayak's series implementing Makefile's 'hdr-check' functionality in Meson, specifically addressing CI workflow issues.

The key technical points in this exchange:
1. Junio confirms the CI issue where GitHub Actions fetches sources via tarball before installing Git, making `.git` unavailable even when the `git` executable is present
2. Karthik explains the chicken-egg problem where dependencies (including Git) can't be installed until sources are available
3. Junio notes the CI's current tarball-based approach doesn't match real user scenarios (either proper `.git` repositories or official release tarballs)
4. Karthik points out this creates inconsistency between GitHub and GitLab CI behaviors
5. Both agree the immediate fix should ensure CI builds from a proper Git repository

The discussion reveals deeper considerations about testing fidelity - while fixing the immediate issue is straightforward (install Git first), Junio raises the broader question of whether CI should also test tarball builds that match release processes. Karthik acknowledges this but suggests handling it as a separate CI job rather than blocking the current fixes. The thread shows the project balancing immediate technical needs with longer-term testing strategy.