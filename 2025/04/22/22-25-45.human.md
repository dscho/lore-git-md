Refactoring patch removing unused field from revision.h. This is the second version of a simple cleanup patch that deletes the unused `log_reencode` field from `struct rev_info`. The field was introduced in 2006's commit 52883fbd ("Teach log family --encoding") but was never actually used.

Junio C Hamano is responding to Lucas Seiki Oshiro's v2 submission, noting he'll tweak the commit message to properly reference the old commit with its full hash and date before applying. The patch itself remains unchanged from v1 - it's still just a single line deletion from revision.h. 

This is a straightforward, low-risk cleanup that appears ready to be merged. The patch is part of Lucas's GSoC project work, with Patrick Steinhardt credited as a helper. The only change between versions was addressing a nit about commit message formatting from the previous review.