This is a test failure report concerning the performance test suite. Philippe Blain has identified that the test `p5332-multi-pack-reuse.sh` (added in commit ba47d88795) fails early during the "setup bitmaps for 1-pack scenario" step. The failure occurs consistently on both macOS and Linux, including in Git v2.44.0 where the test was first introduced.

The report notes an interesting side issue with GitHub CI: the Linux test environments don't have Git installed by default, causing the performance test infrastructure to fail before reaching the problematic test. Philippe suggests installing Git in the CI containers as a potential solution, though this would require handling different package managers across distributions.

The email serves as a heads-up to Taylor (the test's author) about a reproducible test failure, while also highlighting a separate CI infrastructure quirk that prevents the failure from being observed in GitHub's Linux test runs. The issue appears to be a genuine test problem rather than an environment-specific flake, given its consistency across platforms and versions.