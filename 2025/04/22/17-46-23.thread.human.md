Here's a human-readable summary of the thread's current state:

The `git last-modified` series, which tracks file modifications at the tree level (rather than line-by-line like `git blame`), is nearing completion after several iterations. The command now includes Bloom filter optimizations (yielding 36-50% speedups), comprehensive tests, and multiple output formats. The core functionality is solid, with all major technical decisions resolved except one: how to implement the `--extended` output format that includes full commit messages.

Junio Hamano has identified fundamental issues with the current approach of extending Git's pretty-printing machinery, noting it performs unwanted transformations of commit messages. Toon Claes, the primary implementer, now agrees and suggests either reworking the format to bypass pretty-printing entirely or possibly dropping the extended format from this series, as their immediate needs are met by the simpler output. Recent discussion has focused on clarifying edge cases in the documentation around message termination and NUL handling.

The series has seen broad review from key contributors: Patrick Steinhardt on architecture, Taylor Blau on Bloom filters, and Justin Tobler on documentation. With all other aspects finalized, the team is now weighing whether to ship without the problematic format or delay to solve the output representation more cleanly. Given the depth of review and near-complete state, this likely represents the final design discussion before potential merging.