This is a follow-up discussion in the ongoing build system refactoring series, specifically addressing Windows-specific handling of networking function checks in the Meson build.

Eli Schwartz responds to Patrick Steinhardt's feedback on patch 6/6, which proposed skipping networking symbol checks on Windows entirely. The discussion centers around two key technical points: First, whether to maintain platform-specific handling for Windows (skipping checks) versus consistent verification across all platforms. Second, clarifying the correct handling of hstrerror() compatibility implementations, where Eli points out that the compat/hstrerror.c file does exist despite Patrick's initial assertion it didn't.

The exchange reveals an implementation detail about how Windows builds should handle networking functions differently from other platforms, particularly regarding when to include compatibility implementations. Eli argues that skipping checks on Windows would simplify configuration by avoiding incorrect compat implementations being added, while Patrick prefers maintaining uniform verification across platforms. The thread also corrects a misunderstanding about the availability of hstrerror.c in the compat directory.

This discussion represents the final open question in the build system standardization effort - how to best handle platform-specific cases while maintaining overall consistency. The technical specifics involve the treatment of inet_ntop, inet_pton, and hstrerror functions, their compatibility implementations, and the NO_* macro definitions that control their usage. The outcome will determine whether Windows gets special-cased handling or follows the same verification path as other platforms.