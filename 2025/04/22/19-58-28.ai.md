This is a follow-up in the ongoing discussion about configuration precedence between `pull.ff=only` and branch-specific rebase settings. D. Ben Knoble revisits their earlier proposal after Junio C Hamano expressed concerns about safety mechanisms being bypassed.

The email explores whether there's an existing configuration combination that would enforce fast-forward-only behavior for merges while still allowing rebases when explicitly requested (either via branch config or command line). Knoble considers `pull.merge=ff` as a potential solution but ultimately discovers through test examination that the desired behavior is already achievable using `pull.rebase=true` combined with `branch.<name>.rebase=false`.

Key technical points:
- The discussion remains focused on `builtin/pull.c` behavior
- Existing test cases in `t/t5520-pull.sh` demonstrate the configuration combination that satisfies Knoble's use case
- The thread appears to be moving toward resolution as the original submitter finds their needs are already met by current functionality

This represents a potential conclusion point for the thread, with Knoble indicating their original concern is addressed by existing configuration options rather than requiring new knobs. The safety concerns raised by Junio about rebase operations bypassing fast-forward checks remain relevant but may not require code changes if the documented configuration combinations suffice.