Bugfix discussion follow-up confirming existing configuration solves the original use case. D. Ben Knoble verifies that combining `pull.rebase=true` with `branch.<name>.rebase=false` achieves the desired behavior of enforcing fast-forward-only for main branches while allowing rebasing for topic branches, as demonstrated in test cases from `t/t5520-pull.sh`. This confirms the thread's conclusion that no new configuration knobs are needed.

The email references commit 6b37dff17f which introduced the `pull.rebase` option, showing this capability has existed since 2011. Knoble's original goal - different pull behaviors for main vs topic branches through configuration alone - is satisfied by these existing mechanisms. The discussion highlights how proper configuration combinations can enforce both global safety (`pull.ff=only`) and per-branch exceptions (`branch.<name>.rebase=false`).

This wraps up the thread by demonstrating concrete verification of the solution through Git's test suite, eliminating the need for the originally proposed patch or additional configuration variables. The remaining work would be documentation improvements to make these configuration combinations more discoverable to users.