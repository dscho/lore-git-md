This is a follow-up discussion in the Windows/ARM64 compatibility patch series, specifically addressing patch 6/6 which adjusts the maximum tree depth for clang-based ARM64 builds on Windows. The exchange focuses on two aspects: a minor code style improvement and broader architectural concerns.

Johannes Schindelin acknowledges Patrick Steinhardt's suggestion to use `#elif` instead of nested `#if` directives in the stack depth configuration code, agreeing it's a clearer style choice. The more substantive discussion revolves around the recursive tree traversal approach that necessitated this stack depth adjustment in the first place.

Both contributors recognize the current solution as a temporary workaround rather than an ideal fix, with Patrick noting the deeper issue lies in Git's use of recursion for potentially unbounded tree structures. Johannes confirms this limitation while also highlighting the frustrating behavior where stack overflows manifest as misleading exit code 127 on Windows/ARM64 systems.

The tone remains constructive, with agreement that while the immediate patch serves its purpose for platform compatibility, the recursive traversal pattern warrants future architectural attention. This exchange doesn't introduce new technical changes beyond the style tweak, but reinforces that the series' maintainers are thinking about both immediate needs and long-term code health.