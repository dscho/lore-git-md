This is a follow-up in the ongoing discussion about configuration precedence between `pull.ff=only` and branch-specific rebase settings. D. Ben Knoble revisits the thread after realizing that existing test cases show the desired behavior (fast-forward safety with rebase flexibility) can already be achieved through a combination of `pull.rebase=true` and `branch.<name>.rebase=false`. 

The email acknowledges Junio C Hamano's safety concerns about rebase operations bypassing fast-forward protections, but demonstrates that the current configuration system can support both safety and flexibility when properly configured. This appears to resolve Knoble's original use case without requiring new configuration options, though the broader thread continues to debate whether more comprehensive branch control mechanisms would be beneficial.

The exchange shows how careful examination of existing test cases can sometimes reveal that the desired functionality is already available through creative use of current configuration options, potentially avoiding the need for new features.