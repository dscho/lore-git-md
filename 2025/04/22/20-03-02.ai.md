This is a follow-up in the ongoing discussion about configuration precedence between `pull.ff=only` and `branch.<name>.rebase` settings. D. Ben Knoble responds to Alex Henrie's suggestion of implementing per-branch fast-forward controls (`branch.<name>.ff` and `branch.autoSetupFfOnly`), explaining why they're hesitant to pursue that direction.

The key points are:
1. Knoble acknowledges Henrie's proposal for more comprehensive per-branch controls but expresses concern about increasing configuration complexity
2. They describe their own attempt to document the interaction between existing settings, finding the decision matrix already complex enough without new knobs
3. The email suggests the current configuration system's precedence rules are sufficiently powerful but poorly documented
4. Knoble indicates they'll leave further configuration expansion to others, preferring to document existing behavior externally (via blog post) rather than modify Git's configuration system

This represents a practical resolution to the thread, confirming that while Henrie's more flexible branch controls could be useful, the existing configuration combinations (particularly `pull.rebase=true` + `branch.<name>.rebase=false`) already provide the needed functionality without introducing new complexity. The discussion highlights the need for better documentation of current behavior rather than new features.