This is a follow-up discussion in the Windows/ARM64 platform compatibility thread, specifically addressing patch 6/6 which adjusts max_tree_depth for clangarm64 builds. Patrick Steinhardt raises two points about the implementation:

First, he notes a minor code style issue, suggesting the conditional should use `#elif` rather than `#else` followed by another `#if` for better readability. This is a straightforward suggestion that aligns with common coding practices.

More substantively, Patrick expresses concern about the deeper architectural issue of using recursion for tree traversal with potentially unbounded depth. While acknowledging the current patch's necessity as a stopgap measure (since the stack overflow issue needs immediate addressing for Windows/ARM64 compatibility), he suggests the real solution would be to eliminate recursion entirely from the tree traversal logic. This echoes similar concerns about recursive algorithms in Git's codebase that have come up in other contexts.

The discussion maintains focus on the immediate platform compatibility goal while surfacing a longer-term technical debt item that may warrant separate attention. Patrick's comments don't challenge the validity of the current patch for its stated purpose, but rather use it as an example to highlight a broader design consideration.