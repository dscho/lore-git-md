This is a technical discussion about networking library dependencies in Git's Meson build system, part of the ongoing refactoring series.

Patrick Steinhardt responds to Eli Schwartz's patch that adds `-lnsl` and `-lsocket` to the networking dependencies alongside the existing `-lresolv` check. The change mirrors what's already present in `config.mak.uname` and `configure.ac`, particularly for SunOS systems. The patch simplifies the dependency handling by using an array for all three libraries rather than individual checks.

Steinhardt acknowledges the change as reasonable but questions whether further verification of which specific library combinations are actually needed would be worth the implementation effort. This continues the thread's theme of balancing thoroughness with maintainability in the build system transition.

The discussion remains focused on technical implementation details rather than introducing new concerns, with both participants maintaining a constructive tone. This email doesn't raise any new issues for the series but provides context about why certain networking libraries are included in the dependency list.