This is the third patch in a 5-part series modernizing string-list tests by converting them from shell scripts to C unit tests. It focuses on moving the "split_in_place" test functionality from t0063-string-list.sh to u-string-list.c.

The patch removes 73 lines of shell script and test helper code while adding 39 lines of C test code. It ports all test cases for string_list_split_in_place(), including edge cases around empty strings, maxsplit values, and delimiter handling. The new implementation uses the clar test framework's cl_assert* macros and maintains the same test coverage as the original shell version.

Key technical details for future reference:
- Removes write_list() helper from test-string-list.c since it's no longer needed
- Adds t_string_list_split_in_place() helper that duplicates the test string and verifies split results
- Tests identical scenarios: empty fields, maxsplit limits (0, 1, 2, -1), and trailing delimiters
- Uses existing t_create_string_list_dup() helper for expected value setup
- Follows same pattern as previous patch for test_string_list__split()

The change is mechanical and maintains behavior while improving test speed and readability. It continues the series' pattern of removing shell script in favor of C unit tests, with no API or functional changes to the string-list implementation itself.