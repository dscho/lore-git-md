This is a Cygwin-specific build system patch that narrows the conditions under which Git will use its compatibility regex implementation rather than the system one. The change makes the build system behavior consistent across Makefile, autoconf, and meson builds.

The patch restricts the `NO_REGEX` setting to only apply to Cygwin 1.7 systems, as newer Cygwin versions (2.0+) have a FreeBSD-derived regex implementation that passes Git's test suite. The change aligns with how autoconf and meson already handle regex detection. 

A test in t7815-grep-binary.sh that expects regex matching to fail on NUL characters is also modified to skip on Cygwin, since the platform's regex implementation now passes this test case. The commit message provides historical context about when Cygwin's regex behavior changed and notes some uncertainty about why this particular test was originally marked as an expected failure.

This appears to be a well-justified cleanup that removes special cases for older Cygwin versions while maintaining compatibility. The patch is part of a larger series modernizing build system behavior across platforms.