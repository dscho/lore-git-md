This is the final patch in a 4-part series modernizing the MyFirstContribution tutorial, focusing on updating config access patterns. The series aims to bring the contributor guide in line with current Git practices, particularly around repository handling.

The patch replaces global `git_config()` calls with repository-specific `repo_config()` variants in the tutorial's example code. It demonstrates proper config lookup using `repo_config_get_string_tmp()` for the "user.name" setting, explaining that this respects repository-specific overrides unlike the global version. The changes also update related examples showing `wt_status` preparation to use the passed repository parameter rather than `the_repository`.

The patch appears well-motivated, completing the series' goal of showing modern repository interaction patterns. The changes are purely documentation-focused, updating only the tutorial text and example code in MyFirstContribution.adoc. The commit message clearly explains why repository-specific config access is preferred, making this both a practical change and a teaching moment for new contributors.

This concludes the series that has systematically updated outdated patterns in the tutorial, from removing defunct mailing list references to demonstrating proper repository parameter usage and now correct config access. The changes collectively provide a more accurate onboarding experience for new Git contributors.