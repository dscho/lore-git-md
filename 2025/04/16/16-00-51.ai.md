This is a technical discussion about the ongoing investigation into wildcard pathspec handling inconsistencies across Git commands. Junio C Hamano responds to Lucas Seiki Oshiro's findings about command-specific behavior differences, offering historical context about `git add`'s special case handling of paths not yet in the index.

The email suggests the current behavior might stem from a deliberate early design choice in `git add` to prioritize exact matches when dealing with paths containing glob characters, potentially creating divergence from other commands' handling. Hamano speculates this could have led to multiple code paths with inconsistent implementations - some updated to handle wildcards correctly while others retained the original exact-match behavior.

Key technical points raised:
- Confirms `git add` has unique requirements around handling paths not yet in the index
- Recalls an intentional early design decision to help users adding files with literal glob characters in their names
- Suggests the current inconsistency might result from partial updates to some code paths while leaving others unchanged

This moves the discussion toward architectural considerations, as the behavior differences appear rooted in historical design choices rather than simple bugs. The response implies any fix would need to carefully consider backwards compatibility with the existing exact-match behavior that some users may rely on, particularly for filenames containing glob characters.

The email doesn't propose specific solutions but provides important context for understanding why different commands might have evolved different pathspec handling strategies, setting the stage for further discussion about whether and how to unify this behavior.