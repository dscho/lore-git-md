This is a build system patch in the ongoing Makefile/meson alignment series. It focuses on cleaning up conditional compilation flags related to `RUNTIME_PREFIX`.

Ramsay Jones restructures the Makefile to only set certain `BASIC_CFLAGS` when `RUNTIME_PREFIX` is defined, since these flags (like `HAVE_BSD_KERN_PROC_SYSCTL` and `PROCFS_EXECUTABLE_PATH`) only have meaning in that context. The change groups five previously scattered conditionals under a single `RUNTIME_PREFIX` check, making the build logic more organized and intentional.

The patch is straightforward - it touches only the build system and doesn't affect runtime behavior. It's part of a larger effort to modernize and rationalize Git's build infrastructure, particularly around platform-specific cases. The change appears well-considered, with a clear commit message explaining the motivation and a clean diff that just reorganizes existing logic rather than introducing new complexity.