Refactoring patch continuing the removal of unused `log_reencode` field from `struct rev_info`.

Patrick Steinhardt reviews Lucas Seiki Oshiro's simple cleanup patch that removes the unused `log_reencode` field from `struct rev_info` in revision.h. The field has been unused since its introduction in 2006 (commit 52883fb). Steinhardt confirms the patch is correct and provides a minor suggestion to include the historical context in the commit message to help future readers understand why the field is being removed, though he considers this not worth a reroll of the patch.

The change is purely a code hygiene improvement with no functional impact, touching only the struct definition in revision.h. Steinhardt explicitly states the patch "looks obviously good" and thanks the contributor, indicating this is a straightforward, uncontroversial cleanup that aligns with Git's ongoing codebase maintenance efforts. No test modifications are needed as this doesn't affect behavior. The review adds no new technical concerns beyond the original patch's scope.