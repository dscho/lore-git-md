This is a documentation refinement suggestion in the concluded thread about disabling Git hooks. Lucas Seiki Oshiro proposes making the newly added text about `core.hooksPath=/dev/null` more accessible to non-expert users.

The suggestion adds two improvements to the documentation: first, it makes the disabling functionality more prominent by moving it to the start of the sentence rather than being an "also" case. Second, it provides concrete guidance for command-specific use via `git -c`, which is particularly relevant since the thread established that global hook disabling is discouraged in favor of targeted use.

The email represents a polish to the documentation solution that was agreed upon after the rejection of the original `--no-hooks` proposal. It doesn't reopen the feature debate but focuses on making the workaround more user-friendly, particularly for less technical users who might need to temporarily bypass hooks. The suggestion aligns with the thread's resolution while attempting to improve its practical application.