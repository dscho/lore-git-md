This is a CI infrastructure patch that follows up on the Fedora-specific test failure discussion. Johannes Schindelin builds on Todd Zullinger's analysis of the awk availability issue in Fedora 42 container images, implementing a more robust solution than Junio Hamano's initial workaround.

The key technical details are:
- The patch explicitly adds `gawk` to the package list in `ci/install-dependencies.sh` for both Fedora and AlmaLinux systems
- It removes the distro-specific case statement approach in favor of directly installing the known provider package
- The commit message documents the package management nuances between Fedora (where `awk` resolves to gawk) and AlmaLinux (where explicit `gawk` is needed)
- While `*bin/awk` would also work, the patch opts for the more specific `gawk` to avoid potential future ambiguities

The change demonstrates good CI hygiene by:
- Addressing the immediate Fedora 42 container image change
- Future-proofing against similar changes in AlmaLinux
- Choosing the most explicit solution over clever pattern matching
- Thoroughly documenting the rationale in the commit message

This appears to be a well-considered resolution to the CI breakage, building on the thread's earlier analysis while improving the implementation approach. The patch has already been verified to fix the failing builds in Git for Windows' CI pipeline.