This is a build system alignment patch in the ongoing series modernizing Cygwin support. It adds `HAVE_CLOCK_GETTIME` and `HAVE_CLOCK_MONOTONIC` definitions to Cygwin's section in `config.mak.uname`, bringing the Makefile build in line with what autoconf and meson already detect automatically. 

The change is straightforward - Cygwin has supported these timing functions for years, and this simply makes the Makefile build aware of that fact. The patch follows the same pattern as other recent Cygwin-related build improvements in the series, where functionality already working in autoconf/meson builds gets explicitly enabled in the Makefile configuration.

This is part of the broader effort to standardize build behavior across all three build systems (Makefile, autoconf, meson) while cleaning up legacy assumptions. The patch appears well-justified and uncontroversial, matching established behavior from other build systems. It's a small but logical step in the series' goal of consistent Cygwin support.