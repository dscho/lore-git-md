This is a documentation patch following up on the rejected `--no-hooks` proposal. Derrick Stolee now documents the existing `core.hooksPath=/dev/null` workaround for disabling hooks, which was previously only visible in test code (t1350-config-hooks-path.sh).

The patch adds a new paragraph to Documentation/config/core.adoc explaining that setting `core.hooksPath` to `/dev/null` will disable all hooks, with a note that this is primarily for expert users and suggests using it per-command via `-c` rather than globally. The change is minimal - just 5 lines added to the core.hooksPath documentation section.

This represents the documentation-only resolution of the earlier thread where the maintainer rejected adding a new `--no-hooks` option in favor of better documenting existing functionality. The patch acknowledges this context in its commit message, noting the v1 code change was dropped in favor of this documentation approach.

Key technical details for future reference:
- Documents behavior tested since commit c8f6478
- Specifically mentions `/dev/null` as the path value
- Recommends command-specific use via `git -c` rather than global config
- Added to core.hooksPath documentation alongside existing use cases

The patch appears straightforward and uncontroversial, cleanly implementing the documentation improvement path that emerged from the thread's conclusion. It provides users with official documentation for a previously test-only feature while respecting the maintainer's architectural preferences against new options.