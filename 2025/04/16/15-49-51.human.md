This is a bugfix discussion about wildcard pathspec handling inconsistencies across Git commands. Lucas Seiki Oshiro reports that while `git add` exhibits the known wildcard matching issue, `git ls-files` and `git grep` appear to handle the same pathspecs correctly. This suggests the problem may be command-specific rather than a universal pathspec issue.

The email highlights an important discovery: the test suite expansion (currently covering `add`, `commit`, `rm`, and `ls-files`) may need to account for varying behavior across commands. Lucas demonstrates that `ls-files -o 'f*'` correctly lists all matching files including literal 'f*' and 'f**' alongside expanded matches, unlike `git add` which prioritizes the literal interpretation.

This observation complicates the initial assumption that all pathspec-handling commands share the same behavior. Lucas suggests investigating other commands (`checkout`, `log`, `show`, `stash`, `status`) to map out which exhibit the bug and which work correctly, potentially revealing implementation differences in their pathspec handling.

The thread continues to focus on test coverage, but this email shifts attention to understanding the underlying cause of the symptom. The core 3-line fix in dir.c may need reevaluation if the problem turns out to be more nuanced than initially thought.