Documentation patch following up on the rejected `--no-hooks` proposal. This is the resolution of the earlier debate about how to disable Git hooks, now focusing on documenting the existing `core.hooksPath=/dev/null` workaround rather than adding new options.

The patch adds clear documentation for using `/dev/null` as a hooks path to disable all hooks, noting this is primarily for expert users and suggesting the `-c` flag for temporary use. The commit message references the previous discussion and explicitly states the code change from v1 (which proposed a `--no-hooks` option) has been dropped in favor of this documentation-only approach.

This appears to be a straightforward and uncontroversial documentation improvement that implements the resolution agreed upon in the thread. The change is minimal (5 lines added to core.adoc) and well-explained, with the commit message properly acknowledging the prior discussion. The patch follows Git's documentation style and includes appropriate warnings about when this technique should be used.