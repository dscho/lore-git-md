This is a minor refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction. The patch moves the declaration of `revs` later in the function to reduce its scope, which is a reasonable cleanup. However, Junio Hamano points out that the diff introduces an unrelated whitespace change that makes the code slightly worse by moving the blank line after the `oi` declaration.

The change is part of a larger 9-patch series about optimizing pack maintenance, but this particular patch is just a small code organization improvement. It doesn't affect functionality - just moves a variable declaration to where it's actually used and (unintentionally) tweaks some whitespace. Junio's comment suggests the whitespace change should be dropped to keep the patch focused.