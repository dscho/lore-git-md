Here's the human-readable summary of the thread so far:

The thread started with a patch to make `blame.ignoreRevsFile` more forgiving when configured files don't exist - currently Git fails completely if any specified ignore file is missing, which causes problems for global configurations where some repositories might not have the ignore file. The initial solution proposed modifying blame's internals to treat config-specified files differently from command-line ones.

Junio C Hamano has responded with concerns about this approach, arguing that the problem isn't specific to blame and deserves a more general solution. He points out that the current patch creates inconsistencies in how files are handled and removes existing error checking without an opt-in mechanism. Instead, he suggests implementing a unified `:(optional)` prefix syntax that could work across all commands and config options that accept file paths, maintaining strict checking by default while allowing explicit opt-in to lenient behavior when needed.

The discussion has now shifted from a blame-specific fix to considering a broader Git infrastructure change that would handle optional files consistently throughout the codebase. This would affect not just blame but any command or configuration that deals with file paths, providing a more systematic solution to the underlying problem.