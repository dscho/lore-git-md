Performance test infrastructure fix with follow-up considerations.

Jeff King confirms Johannes Schindelin's patch correctly addresses a regression where `GIT_PERF_*` environment variables were being incorrectly overridden by empty values from `GIT-BUILD-OPTIONS`. He verifies the solution works for both `GIT_PERF_LARGE_REPO` and other variables like `GIT_PERF_REPEAT_COUNT`, praising the implementation's careful handling of quoting via sed.

However, King identifies two additional issues: the `run` script also sources `GIT-BUILD-OPTIONS` and needs similar treatment, and there's a problem with default values not being properly applied when variables are unset. He suggests this might require either modifying the `run` script's fallback logic or changing how `GIT-BUILD-OPTIONS` writes unset variables.

While supporting the current patch as an incremental improvement, King notes this is part of a larger issue affecting other variables like `GIT_TEST_*`, with more work needed for a complete solution. The technical discussion remains focused on environment variable handling in test infrastructure, with careful attention to edge cases in shell variable evaluation.