Refactoring patch in Patrick Steinhardt's object storage cleanup series (v3 07/10). This change removes the `index_blob_stream()` wrapper function that had become redundant since 2011 when its implementation was moved into `index_blob_bulk_checkin()`. The patch preserves the function's documentation by moving it to the bulk-checkin header file where the actual implementation lives.

This is a straightforward cleanup that eliminates an unnecessary layer of indirection in the object-file code path. The change is minimal - just removing the wrapper function and updating its single caller in `index_fd()` to call `index_blob_bulk_checkin()` directly. The patch maintains all existing behavior while making the code more direct and easier to follow.

This continues the series' theme of cleaning up and rationalizing the object-file implementation, though it's a smaller change compared to some of the more architectural refactorings earlier in the series. The change appears well-justified and low-risk, with the commit message clearly explaining the historical context for the removal.