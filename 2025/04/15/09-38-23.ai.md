This is the final patch in Patrick Steinhardt's object store refactoring series (v3 10/10), completing the consolidation of object store headers. The patch merges "object-store-ll.h" into "object-store.h" now that the last remaining piece (`odb_path_map`) has been moved to the implementation file.

The change:
- Deletes "object-store-ll.h" entirely
- Moves all its contents into "object-store.h"
- Updates 124 files to include "object-store.h" instead of "object-store-ll.h"
- Adds the khash initialization for `odb_path_map` to object-store.c

This completes the architectural goal of the series to properly separate interface from implementation for the object store subsystem. The change is mechanical with no functional impact - it simply eliminates the transitional split-header approach now that the refactoring work is done. The patch shows the thoroughness of the series, updating every file that previously included the low-level header.

Key technical points:
- Maintains all existing functionality
- Finalizes header consolidation after earlier refactoring steps
- Updates build systems and all callers
- Preserves the exact same API surface
- Completes the object store modularization effort

The series has now addressed all architectural feedback and appears ready for integration, having systematically improved the code organization while maintaining behavior. This final patch cleans up the temporary scaffolding used during the refactoring process.