This is part 3/4 of the Perl removal series, focusing on documentation build system changes. The patch converts the `fix-texi.perl` script to a shell equivalent (`fix-texi.sh`), removing Perl as a requirement for building the user manual.

The `fix-texi` script performs two simple transformations on the output of `docbook2x-texi`: changing the output filename to "git.info" and inserting a standardized directory entry for the manual. The conversion from Perl to shell is straightforward, using `awk` to handle the same logic with slightly different syntax. The new shell version maintains identical functionality while eliminating the Perl runtime dependency.

Key technical details:
- The Makefile is updated to call the shell script instead of the Perl version
- The new `fix-texi.sh` uses `awk` for text processing, which is already a Git build requirement
- The script handles the same two transformations: `@setfilename` and `@direntry` sections
- 21 lines added, 17 removed across 3 files (Makefile, old Perl script, new shell script)

This change aligns with the series' goal of removing Perl dependencies, though it's worth noting this particular script was only used during build time rather than runtime. The conversion appears clean and maintains the same output behavior.