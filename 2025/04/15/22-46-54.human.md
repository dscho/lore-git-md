This is a refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction. It's patch 3/9 in the v3 series.

The patch extracts the handling of `--stdin-packs` from the main `cmd_pack_objects()` function into a new `read_stdin_packs()` helper function. This is purely a code organization change with no behavior modification - the same logic is just moved into a dedicated function. The commit message explains this prepares for future expansion of the stdin-packs handling logic in subsequent patches.

The change is straightforward and mechanical, moving 5 lines of inline code into a new function while maintaining all the same functionality. This continues the series' trend of improving code structure for better maintainability, following similar refactoring in earlier patches. The patch touches only `builtin/pack-objects.c` and the diff shows the clean extraction with no surprises.

This appears to be a well-justified preparatory change that makes the codebase more maintainable while waiting for the more substantive changes to `--stdin-packs` behavior that will come later in the series. The commit message clearly explains the motivation and scope of the change.