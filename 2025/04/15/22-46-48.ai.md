This is a refactoring patch that's part of Taylor Blau's performance optimization series for MIDX and cruft pack interaction during repacking. As the first patch in the v3 series, it standardizes how `pack-objects` handles incompatible command-line options by replacing custom error checks with the shared `die_for_incompatible_opt2()` function.

The change affects several option pairs in `builtin/pack-objects.c` including `--exclude-promisor-objects` vs `--exclude-promisor-objects-best-effort`, `--keep-unreachable` vs `--unpack-unreachable`, and `--stdin-packs` vs `--filter`. The refactoring ensures consistent error message formatting across all these cases. One test in t5331-pack-objects-stdin.sh needed updating to match the new standardized error message format.

This patch doesn't introduce new functionality but cleans up existing code in preparation for later patches in the series that will add more option interactions (particularly around `--stdin-packs=follow` mode). The changes are mechanical and focused on error handling consistency, with minimal test impact - just one test assertion needed updating to expect the new standardized error message format. The patch demonstrates good attention to detail by maintaining all existing functionality while improving code organization.