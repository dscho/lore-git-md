This is a refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction. Elijah Newren responds to patch 3/8, which makes two small code quality improvements in `add_object_entry_from_pack()` in `builtin/pack-objects.c`:

1. Moves the `revs` declaration later in the function where it's actually needed
2. Changes the `struct object_info` initialization to use a designated initializer

Elijah agrees with the first change but expresses a preference for keeping the original `OBJECT_INFO_INIT` initialization pattern rather than switching to a designated initializer. His concern is about future-proofing - if `OBJECT_INFO_INIT` were ever changed from an all-zero initializer, the designated initializer approach could break. This ties into the broader thread discussion about initialization macro consistency that was noted in the thread summary.

The changes are minor and don't affect functionality, but the discussion surfaces an important consideration about initialization patterns that connects to the larger documentation consistency questions being debated in the thread. The patch itself is straightforward and unlikely to be controversial beyond this style discussion.