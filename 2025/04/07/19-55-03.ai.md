Test modernization patch updating `t5601-clone.sh` to use modern test helpers.

This is a test infrastructure patch that replaces manual file checking with Git's standard `test_path_is_file` helper in the clone test script. The change follows Git's modern testing conventions where specialized test assertions are preferred over raw comparisons. The patch appears straightforward - it swaps out a `test_cmp` check against an expected output file for the more direct `test_path_is_file` assertion.

The commit message is minimal but sufficient for this simple change, noting the modernization goal and verification that tests pass. The diff shows a complete file rewrite (with 948 lines added), which seems unusual for what should be a small change - this may indicate the patch was generated or formatted incorrectly, though the actual test change appears sound in principle. The modernization aligns with Git's ongoing efforts to standardize on more robust test helpers.