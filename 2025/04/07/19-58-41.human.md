This is a refactoring patch that's part of the ongoing `the_repository` removal effort. It removes a now-unnecessary NULL check before calling `repo_config()` in `builtin/update-server-info.c`, since a previous change (f29f1990b5) made `repo_config()` safely handle NULL repository parameters.

The patch makes a straightforward 4-line modification to remove the conditional check while maintaining the same behavior. It also adds a new test case in `t1517-outside-repo.sh` to verify that `git update-server-info -h` works both inside and outside a repository, following earlier discussion about test organization.

The change builds on prior work to improve repository handling infrastructure and has already incorporated review feedback about commit message clarity. The patch appears well-tested and low-risk, with all technical concerns resolved in the thread. This is a clean, focused improvement that aligns with the project's architectural goals.