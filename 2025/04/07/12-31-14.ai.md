This is a naming refinement in Patrick Steinhardt's reftable API refactoring series (patch 14/16). The patch introduces a new table block iterator interface (`struct reftable_table_iterator`) for fine-grained block iteration, which will be needed for future reftable consistency checks.

Justin Tobler suggests renaming the initialization function from `reftable_table_init_table_iterator` to `reftable_table_iterator_init` to better match existing naming conventions. Patrick agrees with this suggestion and notes they should also adjust the parameter order accordingly. This is a minor API naming polish in an otherwise technically sound series that has already received positive reviews for its core implementation.

The change doesn't affect functionality - it improves consistency with the established pattern seen elsewhere in the codebase. This follows the series' broader theme of API clarity and standardization, particularly important as these interfaces may be used by external projects like libgit2. The thread shows the typical back-and-forth of code review where naming and style details are being refined while the technical approach remains approved.