Refactoring patch removing redundant NULL check in `update-server-info` as part of the `the_repository` removal effort. This is the final version of a previously discussed change that has already incorporated all review feedback.

The patch removes an unnecessary NULL check before calling `repo_config()` in `builtin/update-server-info.c`, since the function was modified in commit f29f1990b5 to safely handle NULL repository parameters. The change is straightforward - just removing the conditional wrapper around the `repo_config()` call.

A new test case is added to `t1517-outside-repo.sh` to verify `git update-server-info -h` works both inside and outside a repository, following Patrick Steinhardt's suggestion about test organization. The test checks both the exit code and usage output pattern.

The commit message has been updated per Eric Sunshine's suggestion to reference the specific implementation commit (f29f1990b5) rather than a merge commit, making the change's lineage clearer. The message properly credits Christian Couder as mentor.

This appears to be the final version of a well-reviewed patch that has addressed all feedback. The implementation is clean (4 lines modified in main code plus test addition), test coverage is appropriate, and the change fits well with the ongoing repository API work. No outstanding issues remain - this is ready for merging as part of the architectural improvements to repository handling infrastructure.