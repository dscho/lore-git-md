This is a naming clarification in Patrick Steinhardt's reftable API refactoring series (patch 6/16). The patch renames `struct reftable_block` to `struct reftable_block_data` to better reflect that this structure only holds block data without access logic. The change is purely about API clarity and affects 8 files with mechanical updates.

Justin Tobler suggests further renaming the field from `block` to `data` to avoid `block->block` chaining when `block_reader` becomes `reftable_block` in the next patch. Patrick agrees to rename it to `block_data` instead, maintaining consistency with the struct name while addressing the chaining concern. This continues the series' systematic API naming improvements, with all changes being mechanical renames that don't affect behavior. The discussion shows careful attention to naming details that will affect external API users.