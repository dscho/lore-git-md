Test improvement patch addressing pipe usage in test assertions, with reviewer feedback. This is responding to Anthony Wang's patch that modified t/t9811-git-p4-label-import.sh to avoid pipes in test assertions by redirecting output to temporary files first. Patrick Steinhardt notes two additional improvements that could be made: (1) standardizing the grep usage (the first grep lacks -q while others have it, though all could use -q since output isn't needed), and (2) switching to test_grep() which provides better debugging output on failures. The reviewer confirms the core approach of avoiding pipes is sound, suggesting these as follow-up refinements rather than blocking issues. The discussion focuses on test robustness best practices rather than functional changes to the Perforce import tests themselves.