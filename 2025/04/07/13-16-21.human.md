This is part of Patrick Steinhardt's reftable API refactoring series, specifically introducing a public interface for block-level access. The patch moves block-related declarations from internal headers to a new public `reftable-block.h` header, exposing them for future verification work in Git.

The change enables low-level block inspection needed for upcoming reftable consistency checks, though the interface is still incomplete (lacking record-level reading functionality that will come later). The patch also requires exposing the `reftable_buf` structure used by the first-key decoding function.

The implementation is clean - it relocates existing declarations with minimal changes beyond documentation improvements. This continues the series' pattern of making internal structures available for verification while maintaining good separation of concerns. The patch fits naturally with the earlier block source consolidation work and sets up for subsequent iterator optimizations in the series.

This appears to be a well-considered step in the larger reftable refactoring effort, with the public interface carefully scoped to support the stated verification use case while keeping the API surface minimal. The changes are mechanical but meaningful for the library's evolution.