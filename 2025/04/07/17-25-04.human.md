Test improvement patch completing a series to make Perforce integration tests more robust. This is the third and final patch in Anthony Wang's v3 series, converting remaining `grep` calls to `test_grep` in t9811-git-p4-label-import.sh for better test failure debugging.

The change is straightforward - three `grep` calls checking tag output are replaced with `test_grep`, which provides more helpful output when tests fail by showing the non-matching file contents. This matches the pattern established in patch 3 of the v2 series and maintains the same test conditions while improving debuggability.

The series has addressed all previous feedback, with this version adding Signed-off-by lines and improving commit messages as requested. While some higher-level questions about test verification methodology remain open (Junio Hamano's show-ref suggestion), this particular patch appears ready to go as it cleanly implements an uncontroversial test improvement.

The changes affect two test cases involving Perforce label import/export functionality, specifically verifying that multiple tags on the same changelist are properly handled. The patch completes the technical improvements to the test suite while preserving all existing test coverage.