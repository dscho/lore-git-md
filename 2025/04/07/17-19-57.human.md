Test improvement patch in the Perforce integration test suite. This is part of Anthony Wang's v2 series addressing exit code handling in test assertions, following up on earlier review feedback.

Junio Hamano responds to the first patch in the series, which replaces pipe-to-grep patterns with temporary file redirection to properly capture Git command exit codes. While acknowledging the technical correctness of the change, Junio raises deeper questions about the test's intent. He suggests the current grep-based assertions may be too loose, potentially masking the true desired state (verifying specific tags exist while others don't).

The response proposes using `git show-ref --verify` for more precise tag existence checking, which would both properly test the intended conditions and maintain exit code fidelity. This goes beyond the original mechanical pipe-to-file conversion, suggesting a semantic improvement to the test's verification strategy.

Junio's feedback indicates the patch may need further refinement to properly express the test's requirements, though the core approach of avoiding pipes remains sound. The exchange demonstrates Git's careful attention to test quality, where even mechanical improvements prompt re-examination of the underlying test design.