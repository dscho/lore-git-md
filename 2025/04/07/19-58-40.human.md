This is a refactoring patch that's part of the ongoing `the_repository` removal effort. Usman Akinyemi submits a single-patch series to clean up a NULL check in `builtin/update-server-info.c` that's no longer needed after previous changes to make `repo_config()` handle NULL repository parameters safely.

The patch removes a redundant `if (repo)` guard around a `repo_config()` call, following up on earlier work that modified this API. It also adds a test case to `t1517-outside-repo.sh` to verify `git update-server-info -h` works both inside and outside a repository, following Patrick Steinhardt's suggestion to consolidate such tests in one place. The author indicates they'll move similar tests from other builtins to this file if this approach is accepted.

The v2 revision incorporates Eric Sunshine's documentation suggestion to reference the specific implementation commit (f29f1990b5) rather than a merge commit in the commit message. This appears to be a straightforward, well-justified cleanup with appropriate test coverage. The thread shows consensus on both the technical approach and documentation style, with all review feedback addressed. The patch seems ready for integration into the ongoing repository API improvements.