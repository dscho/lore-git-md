Test improvement patch in a Perforce integration test series, converting plain `grep` to `test_grep` for better debugging. This is the third patch in Anthony Wang's v2 series addressing test reliability in t/t9811-git-p4-label-import.sh.

The change replaces three direct `grep` calls with `test_grep` in the 'two labels on same changelist' test case. This follows Git's test suite conventions where `test_grep` is preferred over raw `grep` because it provides more helpful output when tests fail - it will show the contents of the non-matching file rather than just silently failing. The modification is mechanical and maintains the exact same matching behavior while improving debuggability.

This continues the series' theme of making Perforce integration tests more robust by following Git's best practices for test assertions. The change is small (3 lines modified) and focused, similar to the previous patches in the series. It builds on the earlier work that addressed exit code handling and grep usage patterns in these tests.

The patch appears well-scoped and follows the established pattern of the series - making targeted improvements to test reliability without changing the underlying test logic. It addresses a clear need (better failure output) in a way that's consistent with Git's test suite conventions. No new test logic is introduced, just improved assertion behavior.