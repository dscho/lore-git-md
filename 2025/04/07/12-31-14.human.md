This is a naming clarification in Patrick Steinhardt's reftable API refactoring series (patch 14/16). The patch introduces a new table block iterator interface for future reftable consistency checks.

Justin Tobler suggests renaming the initialization function from `reftable_table_init_table_iterator` to `reftable_table_iterator_init` to better match existing naming conventions. Patrick agrees with the suggestion and notes they should also adjust the parameter order accordingly.

This is a minor but sensible naming improvement in an otherwise mature patch series that has already received positive reviews. The change doesn't affect functionality but improves API consistency. The thread shows careful attention to detail from both reviewer and maintainer, with Patrick quickly accepting the constructive feedback.