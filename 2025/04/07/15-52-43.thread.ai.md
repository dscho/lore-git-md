Here is the dense AI thread summary incorporating this new email:

RFC patch from Phillip Wood proposing partial support for preserving extra commit headers (excluding GPG signatures) during conflict-free `git rebase -m` operations. The current implementation works when the sequencer uses `commit_tree_extended()` (non-conflict cases) but headers are still lost during merge conflicts or cherry-picks. Changes modify `sequencer.c` to pass through extra headers when creating commits, with test coverage added in `t3404` to verify preservation across pick/fixup/reword/edit/squash operations. The author acknowledges limitations but argues this provides value for server-side workflows where conflicts are rejected. Positioned as an incremental improvement until `git replay` (which fully preserves headers) sees broader adoption. Technical implementation involves propagating headers through the sequencer's commit creation path while explicitly excluding signature headers. Test demonstrates header preservation through various rebase operations while verifying GPG headers are not copied.