This is a test improvement patch in a series about making Perforce integration tests more robust. It's the first of three patches in v2 series that addresses an issue where pipe-to-grep patterns in tests were hiding command exit codes.

The patch modifies two test cases in t/t9811-git-p4-label-import.sh to write `git tag` output to temporary files before grepping, rather than piping directly to grep. This ensures test failures will properly report if the `git tag` command itself fails, not just if the grep pattern doesn't match. The changes are mechanical but important for test reliability - they follow up on review feedback from Patrick Steinhardt about making test failures more debuggable.

The modifications are straightforward and focused - just 6 lines added and 4 removed across two test cases. This continues the thread's theme of improving Perforce test reliability through better assertion patterns, without changing any actual test functionality. The patch appears well-executed and addresses the specific technical concern raised in the v1 discussion.