This is a follow-up discussion in Patrick Steinhardt's series to eliminate Perl dependencies from Git's test suite, which has reached final approval status. The email specifically addresses Johannes Schindelin's suggestion about removing Perl usage from `git filter-branch`.

Patrick acknowledges the sentiment to remove `git filter-branch` entirely (referencing Elijah Newren's famous "debug it" quip about removing problematic code), but pragmatically notes we should first have a proper replacement - likely `git-filter-repo` integrated into Git proper. As an interim step, Patrick mentions he's already prepared a follow-up series that removes Perl dependencies from both `git filter-branch` and `git request-pull`.

The exchange shows the careful balance being struck in this Perl removal effort - while the ultimate goal is complete elimination of Perl dependencies, the maintainers are being methodical about ensuring functionality isn't lost in the process. The discussion also hints at the broader ecosystem impact, with `filter-branch` being a particularly sensitive component given its historical importance for repository rewriting.