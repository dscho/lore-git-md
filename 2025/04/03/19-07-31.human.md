Performance optimization follow-up in the bundle ref deduplication series. Toon Claes reviews Karthik Nayak's patch that replaces the O(N^2) `object_array_remove_duplicates()` with an O(1) `strset`-based solution in `write_bundle_refs()`. The reviewer confirms the approach makes sense, particularly noting the handling of edge cases like exclusion patterns (`master^!`) and race conditions during bundle creation. They suggest using `STRMAP_INIT` for initialization but otherwise approve the implementation, having tested additional edge cases not covered by the existing tests. The review highlights the patch's thoroughness in addressing both the performance issue and related test fixes, with no outstanding concerns raised.