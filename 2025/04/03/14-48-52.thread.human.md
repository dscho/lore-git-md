Here's a human-readable summary of the thread so far:

The thread addresses a test reliability issue in t/t7422-submodule-output.sh where a printf call in a loop had an extraneous argument that could cause POSIX-compliance problems. Subhaditya Nath initially proposed removing the unused "$i" argument to avoid undefined behavior with extra printf arguments.

Eric Sunshine agreed with the fix but suggested an alternative approach - instead of just removing the argument, formatting it properly with "%d" to match another similar loop in the same test file. This would maintain consistency with the test's other printf usage and likely better reflect the original author's intent when the test was added in commit 65f586132b.

The discussion remains focused on test code quality, specifically around submodule status output testing and SIGPIPE handling. Both approaches would solve the POSIX compliance issue, with Sunshine's suggestion offering slightly better stylistic consistency between the test's two similar loops. The change has no impact on production code behavior.