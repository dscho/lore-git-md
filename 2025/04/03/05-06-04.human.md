This is patch 13/20 in Patrick Steinhardt's series to eliminate Perl as a mandatory test dependency. It refactors tests that previously relied on Perl for textconv scripts, replacing them with shell utilities.

The patch converts three test files (t4030, t4031, t7815) to use simpler shell tools like `tr` and `test-tool hexdump` instead of Perl scripts. The author notes one case where they deliberately chose a minimal conversion (using `tr` for specific byte translations) rather than a full hexdump to avoid cascading changes across other tests.

This continues the series' pattern of methodical, file-by-file Perl removal while maintaining test functionality. The changes are straightforward - removing Perl prerequisites and replacing Perl scripts with equivalent shell commands. The patch maintains backward compatibility that's been a hallmark of the series, and follows the established approach of making the minimal viable change needed to eliminate Perl while keeping tests working.

The patch appears well-considered, with clear explanations for the design choices. It builds on previous work in the series and doesn't introduce any new patterns or complications. Given the series' steady progress and positive reception so far, this patch seems likely to be uncontroversial.