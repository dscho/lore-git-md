This is a discussion about change ID semantics in Git operations, specifically addressing how cherry-pick and rebase should handle them. Nico Williams responds to Elijah Newren's concerns about change ID uniqueness and usability.

Nico agrees that using change IDs directly in commands like `git rebase` and `git switch` would be problematic due to potential duplicates, suggesting they're better suited as documentation to track commit evolution rather than as direct references. He explores some hypothetical solutions (like branch-specific lookups or multiple change IDs per commit) but acknowledges the practical challenges.

The email reinforces that change IDs should be preserved during cherry-pick operations since they're fundamentally about authorship (which cherry-pick already preserves), and since rebase is built on cherry-pick. The discussion continues the thread's theme of balancing ideal change ID behavior with real-world constraints, though Nico doesn't propose concrete implementation changes - it's more about clarifying the conceptual model.