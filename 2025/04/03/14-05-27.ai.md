This is the final version of a 2-patch series adding SHA implementation reporting to `git version --build-options`, now incorporating all agreed-upon documentation refinements. The series is ready for merging with all technical and presentation aspects settled.

The patches enhance version reporting to show which SHA-1 and SHA-256 implementations Git was built with, including special handling for non-collision-detecting SHA-1 variants. The implementation uses preprocessor symbols (`SHA1_BACKEND`, `SHA1_UNSAFE_BACKEND`, `SHA256_BACKEND`) defined in hash.h to provide compile-time safety and avoid duplicate detection logic. The output format shows raw symbol names and includes security warnings about non-collision-detecting SHA-1 variants.

Key changes in v3 include final documentation updates to better explain the security implications of non-collision-detecting SHA-1 implementations, using the finalized "non-collision-detecting-SHA-1" terminology. The implementation now omits the non-crypto SHA-1 line entirely when no unsafe backend is selected, addressing the last minor discussion point about output format clarity. The patches touch hash.h (symbol definitions), help.c (version output), and git-version.adoc (documentation).

The thread has reached full consensus with Patrick Steinhardt acknowledging Justin Tobler's final documentation refinements. All architectural decisions were approved in earlier versions, and this v3 represents the conclusion of the discussion with all participants (including Christian Couder and Junio Hamano) satisfied with the outcome. The series is now complete and ready for merging in its current form.