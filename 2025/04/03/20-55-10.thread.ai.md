Thread summary:

Feature proposal for configurable default partial clone filters. Sylvain Fardeau suggests adding a configuration option to make `--filter=blob:none` the default for clone operations, citing real-world Stack Overflow use cases where developers want this behavior consistently. The proposal focuses on workflows involving binary files where users currently must manually specify the filter flag each time. Brian m. carlson responds with initial feedback, acknowledging potential utility while noting no such option currently exists in Git. He suggests aliases as a current workaround and explains the project's typical process requiring concrete patches for feature evaluation. The response raises technical concerns needing addressing: accidental partial clones causing offline problems, compatibility issues with scripts expecting full clones, and parallels to shallow clone concerns. While maintaining a neutral stance, the feedback indicates this would require Junio Hamano's approval and careful implementation consideration. Key technical aspects would involve config parsing logic (likely adding a `clone.defaultFilter` or similar option) and modifying clone command behavior, with specific attention to offline operation challenges and backward compatibility. The discussion remains at the conceptual level with no specific configuration syntax proposed yet, and no patches submitted. Open questions include how to prevent accidental partial clones and ensure script compatibility, with no participant positions beyond the initial proposal and response.