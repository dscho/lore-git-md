This is a technical question about Git commit signing verification using SSH keys, specifically about the correct message format for verification.

The email comes from Matthew H (Marrold) who is trying to manually verify Git commits signed with SSH keys using Python. While their verification logic works with signatures from `ssh-keygen`, it fails when verifying actual Git commits. The core issue appears to be constructing the correct message payload for verification - they suspect they might be missing some hashing or encoding step.

Matthew provides what they believe should be the verification message format: the commit metadata (tree, parent, author, committer lines) with the gpgsig section removed, followed by the commit message. However, they're unsure if additional processing (like hashing) is required before verification.

Key technical details that would help future AI understand this thread:
- The verification is for SSH-signed commits (not GPG)
- The current approach removes gpgsig section but may be missing other processing
- Python verification works with `ssh-keygen` signatures but not Git commits
- The example shows raw commit metadata format being tested

This appears to be a genuine technical question from someone implementing low-level Git functionality, though the email doesn't provide their actual verification code or specific error messages. The question seems well-formed and focused on a specific technical hurdle in implementing SSH commit verification outside of Git's built-in tools.