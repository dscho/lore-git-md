Matthew H (Marrold) is working on implementing Git commit verification using SSH keys in Python. While their code successfully verifies signatures produced directly by `ssh-keygen`, it fails when verifying actual Git commits signed with SSH keys. The core issue appears to be in constructing the correct message payload for verification - they suspect they might be missing some hashing or encoding step in processing the commit data.

Matthew describes their current understanding of the verification message format: it should include the commit metadata (tree, parent, author, and committer lines) with the gpgsig section removed, followed by the commit message. However, they're unsure if additional processing is required before verification. The question is specifically about SSH-signed commits, not GPG signatures.

This is a technical deep-dive into Git's low-level signing implementation, focused on the exact message construction needed for SSH verification. The question suggests Matthew has already done substantial work to get partial verification working and is now stuck on the final piece of making it work with real Git commits.