This is a cross-compilation support patch in Patrick Steinhardt's build system fixes series. The patch distinguishes between build-host and target-host binaries in Meson configuration, specifically marking which programs should be found on the build machine (native: true) versus those that will run on the target system (native: false). Key changes include introducing target_shell, target_perl, and target_python variables while updating documentation build tools, gitweb configuration, and test environment setup to use the appropriate binary types.

The patch affects multiple meson.build files and introduces new cross-compilation documentation explaining how to specify target binaries via machine files. It handles edge cases like version detection during build (which still uses native shell) while ensuring runtime tools like Perl and Python paths get embedded for the target system. The changes are thorough and well-explained, with clear rationale for each modification.

Technical details include:
- New target_* variables for shell/perl/python
- Updated find_program() calls with native: true/false parameters
- Documentation of cross-compilation machine file format
- Special handling for version detection during build
- Consistent updates across documentation, gitweb, and test infrastructure

The patch appears technically sound and addresses a real cross-compilation need, though as noted in the thread context, it still needs review from Windows/Meson experts. The changes are focused and maintain backward compatibility when not cross-compiling.