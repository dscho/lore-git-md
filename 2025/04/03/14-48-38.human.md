This appears to be a bug report regarding inconsistent submodule behavior. The reporter describes a situation where cloning a repository with `--recursive` results in a different submodule commit hash than when cloning the submodule directly. 

In the recursive clone, the submodule ends up at commit `7bb809da9...` (on the `release/1.0.0b` branch), while a direct clone of the same submodule shows commit `a092be3bc...` (on the `staging` branch). The reporter expected both methods to yield the same commit hash.

The issue seems to involve Git's submodule handling when using the `--recursive` flag, though the exact cause isn't clear from the report. The reporter is using Git 2.49.0 on macOS (ARM64) and provides detailed reproduction steps showing the discrepancy between the two cloning methods. 

This could potentially affect anyone using recursive submodule clones where precise commit pinning is important. The report is well-documented with clear reproduction steps and version information, making it easier to investigate.