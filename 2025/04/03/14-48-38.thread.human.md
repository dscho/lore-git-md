Here's a human-readable summary of the thread so far:

A user has reported unexpected behavior with Git's submodule handling during recursive clones. When cloning a repository containing a submodule recursively via SSH, the submodule ends up checked out at commit `7bb809d`, while directly cloning the same submodule URL results in commit `a092be3` (on the 'staging' branch) being checked out. 

The reporter, Bill Torpey, suspects this discrepancy may indicate a bug in Git's submodule machinery, though the report doesn't include the superproject's .gitmodules configuration or whether the submodule reference points to a specific commit. The issue was observed using Git 2.49.0 on Darwin/arm64. 

At this point, the thread needs more information to determine whether this is a genuine bug or a configuration issue - specifically, whether the superproject intentionally pins the submodule to a particular commit or branch. The behavior could be expected if the superproject specifies a particular commit for the submodule, but without seeing the .gitmodules file or the exact clone commands used, it's hard to say definitively.