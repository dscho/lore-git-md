This thread discusses technical implementation details of verifying Git commits signed with SSH keys outside of Git's built-in tooling. The original question comes from Matthew H (Marrold) who is implementing manual verification in Python.

Core technical problem: Constructing the correct message payload format for SSH-signed Git commit verification. The current approach that works with `ssh-keygen` signatures fails when verifying actual Git commits, suggesting missing preprocessing steps.

Current understanding of required message format:
1. Full commit metadata (tree, parent, author, committer lines)
2. With the gpgsig section removed
3. Followed by the commit message
But this may be incomplete - potential missing steps could include:
- Additional hashing of the payload
- Specific encoding requirements
- Header normalization

Key technical context:
- Focuses specifically on SSH signing (not GPG)
- Involves low-level cryptographic verification
- Requires precise byte-for-byte message reconstruction
- Python implementation is working for test cases but not real commits

Open questions:
- What exact preprocessing Git applies before verification?
- Are there hidden normalization steps in the payload construction?
- How does the verification message differ between `ssh-keygen` and Git's actual signed commits?

The thread begins with this specific technical question about message format construction for SSH commit verification. Future emails may provide implementation details, corrections to the assumed format, or examples of working verification code.