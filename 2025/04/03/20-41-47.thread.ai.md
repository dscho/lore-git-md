Updated AI thread summary:

This thread focuses on the technical implementation details for verifying Git commits signed with SSH keys, specifically addressing Python-based verification outside Git's built-in tooling. The discussion originated with Matthew H (Marrold) encountering verification failures when moving from test cases to real Git commits.

Core technical problem has been resolved: The exact message format for SSH-signed commit verification is now clearly established as the complete commit object with the entire signature header block (either `gpgsig` or `gpgsig-sha256`, including all space-prefixed continuation lines) removed. Brian M. Carlson provided authoritative confirmation that no additional hashing or encoding is applied beyond this simple removal.

Key technical specifications now include:
- Verification uses raw commit object minus complete signature header block
- Must handle both `gpgsig` and `gpgsig-sha256` header variants
- Must preserve all other headers regardless of order
- No hidden normalization or preprocessing steps
- Potential `ssh-keygen` interface quirks may still cause verification failures

Current status:
- Message format question is conclusively answered
- Implementation should now work if following the specified format
- Debugging focus shifts to potential `ssh-keygen` invocation issues

Open questions remaining:
- Why Matthew H's implementation still fails despite correct format
- Whether specific `ssh-keygen` versions/options affect verification
- How to construct ideal test cases to isolate the failure point

Participant positions:
- Brian M. Carlson (authoritative): Provided definitive format specification
- Matthew H (original asker): Needs to provide more specific test cases

The thread has progressed from initial uncertainty about the verification format to a clear technical specification, with remaining issues likely related to tool invocation rather than fundamental understanding of the signing protocol. Future emails may include:
- Debugging information from Matthew H's failing cases
- Additional verification edge cases
- Potential improvements to documentation of SSH signing format