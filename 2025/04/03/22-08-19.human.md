Technical clarification on SSH-signed Git commit verification format.

Brian M. Carlson provides a clear answer to Matthew H's question about the correct message format for verifying SSH-signed Git commits. The key points are:

1. The verification message should be the exact commit object with only the `gpgsig` or `gpgsig-sha256` header and its continuation lines (those starting with space) removed - no additional hashing or encoding is applied.

2. The example shows a real commit with signature stripped to demonstrate the exact payload format that should be verified against. This includes all headers except the signature block, followed by the commit message.

3. Important caveats: Commit headers can appear in any order and may include additional headers beyond the standard ones (tree, parent, author, committer), so implementations shouldn't make assumptions about header ordering or presence.

4. Carlson requests more specific information to help diagnose the verification failure - a concrete commit example, key, and command line invocation that's failing, noting that `ssh-keygen` can be tricky to use correctly.

This response provides the missing technical detail needed to correctly implement commit verification while highlighting potential pitfalls in the process. The explanation is thorough and includes both conceptual clarification and practical examples.