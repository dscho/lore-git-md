This is the second and final patch in a series enhancing `git version --build-options` to report SHA implementations, now ready for merging. The patch specifically focuses on adding reporting for unsafe SHA-1 implementations that don't provide collision detection.

The patch adds a new "non-collision-detecting-SHA-1" line to the version output when such implementations are used, with clear documentation about their security implications. It defines compile-time symbols for three unsafe SHA-1 backends (Apple, OpenSSL, and block-sha1) and updates both the implementation code and documentation to reflect this.

The changes are straightforward and well-contained, touching hash.h (for symbol definitions), help.c (for version output), and git-version.adoc (for documentation). The implementation follows the zero-runtime-overhead design principle established in the series, using preprocessor symbols for compile-time safety.

This completes the series' goal of providing transparent reporting of all SHA implementations used in a Git build, with special attention to clearly marking less secure variants. The thread shows consensus on both the technical approach and documentation wording, with all outstanding questions resolved in previous iterations.