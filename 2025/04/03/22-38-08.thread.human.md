Here's a human-readable summary of the thread so far:

Derrick Stolee has proposed adding a new `--no-hooks` global option to Git that would allow users to temporarily disable hook execution. The patch follows the same pattern as existing options like `--no-advice`, implementing the feature by setting a `GIT_HOOKS` environment variable that the hook execution code checks.

The motivation comes from real-world cases where users currently work around slow hooks through either manual (deleting hooks) or unsafe (pointing `core.hooksPath` to invalid locations) methods. This new option would provide a supported way toggling hook execution without requiring filesystem modifications.

The implementation touches documentation, core command-line processing, and the hook execution logic itself, with comprehensive test coverage verifying both the command-line option and environment variable behavior. The change appears well-scoped and follows established patterns in the codebase for similar global options.