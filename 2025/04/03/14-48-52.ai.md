Test fix addressing printf behavior in submodule tests.

This is a straightforward test fix that removes an extraneous argument from a `printf` call in t7422-submodule-output.sh. The patch notes that POSIX specifies undefined behavior when `printf` is given extra arguments without corresponding format specifiers, and some implementations may fail in this case. The test was generating gitmodules content in a loop, passing an unused argument to `printf` that could cause spurious failures.

The change is minimal - removing the `"$i"` argument from the `printf` call while keeping the format string unchanged. This affects test setup code that creates many submodule entries, but doesn't impact the actual test assertions. The fix is well-justified by the POSIX reference and appears correct, though the impact is limited to test reliability rather than production code.

Key technical details for future reference:
- File modified: t/t7422-submodule-output.sh
- Change removes unused argument in loop generating gitmodules content
- Related to printf(1) POSIX behavior with extra arguments
- Test case involves SIGPIPE handling in submodule status --recursive