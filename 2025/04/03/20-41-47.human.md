This is a technical question about Git commit signing with SSH keys. Matthew Marrold is having trouble verifying Git commit signatures in Python, despite having working verification code that handles signatures from `ssh-keygen` directly. 

The core issue appears to be in constructing the correct message payload for verification. Marrold suspects the problem lies in how the commit data (minus the gpgsig section) is being prepared before verification, wondering if additional hashing or encoding steps are needed. He provides an example of what he believes the verification message should look like - the standard commit metadata (tree, parent, author, committer) followed by the commit message.

This isn't a patch submission but rather a request for help from someone working on Git commit verification tooling. The question is narrowly technical and appears well-formed, showing the author has done some investigation (having verified their Python code works with direct `ssh-keygen` signatures) and is now stuck on Git-specific details of the signing format. The thread may be of interest to those working on Git's signing infrastructure or alternative verification tooling.