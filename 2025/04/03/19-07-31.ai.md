Performance optimization follow-up in the bundle ref deduplication series. Toon Claes responds to Karthik Nayak's patch that replaced the O(N^2) `object_array_remove_duplicates()` with an O(1) `strset`-based solution in `write_bundle_refs()`. The response confirms the technical approach, noting that the new implementation correctly handles both same-refname cases (regardless of object identity) and exclusion patterns (where refnames may be duplicated but point to different INTERESTING/UNINTERESTING objects). Claes points out a minor style question about using `STRMAP_INIT` macro rather than explicit `strset_init()`, but overall approves the implementation after testing additional edge cases. The response also provides historical context about the long-standing O(N^2) warning in the code and links to a related race condition fix in bundle creation. The interaction confirms the optimization's correctness while maintaining all necessary functionality from the original implementation, with test changes showing the fixed behavior for previously marked edge cases.