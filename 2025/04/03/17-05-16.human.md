Test fix addressing printf behavior in submodule tests, with suggested improvement.

Eric Sunshine responds to Subhaditya Nath's patch that removes an extraneous printf argument in t7422-submodule-output.sh. While agreeing the change is correct (fixing potential POSIX undefined behavior), Sunshine suggests an alternative approach that would better match the original author's intent - keeping the argument but using a `%d` format specifier instead, similar to another printf call in the same test file.

The discussion centers around test reliability rather than production code behavior, specifically around how submodule status tests handle SIGPIPE. The proposed change would make the test's printf usage more consistent while still avoiding the POSIX undefined behavior issue. This appears to be a minor but thoughtful refinement to an already correct patch.