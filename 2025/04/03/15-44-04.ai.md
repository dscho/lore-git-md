Technical refinement for git-refs subcommand implementation. Zheng Yiting presents two potential approaches for implementing the `list` functionality in the proposed `git-refs` command, analyzing existing `git-for-each-ref` and `git-show-ref` options to determine which features should be included. The email focuses on how to handle filtering and formatting capabilities, which are currently split across these two commands.

The first approach would combine both filtering and formatting in a single `list` subcommand, while the second would split them into separate `filter` and `show` subcommands. Zheng identifies potential complexity and performance concerns with the unified approach, while noting the split approach might benefit from shared formatting code similar to how `git-add` and `git-restore` share patch functionality. The email specifically excludes `--verify`, `--quiet` and `--exist` options from consideration as they're planned for separate subcommands.

This discussion builds on Patrick Steinhardt's earlier technical review of the GSoC proposal, particularly the constraints around maintaining backward compatibility and working through `refs.c`. The email shows Zheng working through implementation tradeoffs while adhering to the project's technical constraints, though no final decision is made between the approaches. The next steps depend on mentor feedback about which architectural direction to pursue for the ref management unification effort.