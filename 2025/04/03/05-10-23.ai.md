This is a documentation refinement discussion in the SHA-1 backend reporting series, now in its final stages. Patrick Steinhardt responds to Justin Tobler's latest proposal about how to handle reporting of non-cryptographic SHA-1 backends in `git version --build-options` output.

The key discussion point is whether to always show the non-crypto SHA-1 line (with "none" when unset) or omit it entirely when no unsafe backend is selected. Patrick acknowledges Justin's concern that the proposed "SHA-1 without collision detection: none" format could be misleading, but suggests it would improve discoverability. He ultimately defers to Justin's judgment while noting either approach would be an improvement over the current state.

This email represents one of the final minor points being resolved in the series, with Patrick indicating he won't block merging based on this detail. The thread has otherwise reached consensus on all major technical and documentation aspects, including the terminology ("non-crypto-SHA-1" vs "SHA-1 without collision detection") and output format. The implementation already meets the core requirements of reporting both cryptographic and non-cryptographic SHA-1 backends while maintaining compile-time safety.