This is a follow-up discussion in Patrick Steinhardt's series to eliminate Perl dependencies from Git's test suite, specifically regarding the `PERL_TEST_HELPERS` prerequisite introduced in patch 6/20.

The email responds to Johannes Schindelin's suggestion about completely eliminating Perl usage in Git tools rather than just the test suite. Patrick acknowledges the idea of removing `git filter-branch` but notes the need for a proper replacement first, suggesting `git-filter-repo` as a potential candidate. He then reveals he's already prepared a follow-up series that removes Perl dependencies from both `git filter-branch` and `git request-pull`.

Key technical details mentioned:
- The discussion references `git filter-branch` and `--state-branch` functionality
- `git-filter-repo` is proposed as a potential replacement
- A new patch series exists addressing Perl removal in `git-filter-branch` and `git-request-pull`

This exchange shows the Perl removal effort expanding beyond just the test suite to core Git tools, while maintaining consideration for backward compatibility and replacement functionality. The tone suggests technical alignment between the contributors, with Patrick proactively addressing the broader Perl removal goal through additional work.