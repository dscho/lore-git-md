Here is the dense AI thread summary for this new email:

Derrick Stolee introduces a new `--no-hooks` global option to disable hook execution, modeled after `--no-advice`. The patch adds a `GIT_HOOKS` environment variable checked in `run_hooks_opt()` to skip hooks, providing a supported alternative to current workarounds like deleting hooks or setting invalid `core.hooksPath`. Implementation touches git.adoc (documentation), environment.h (variable declaration), git.c (flag handling), hook.c (execution check), and includes tests. The change addresses performance-sensitive cases where users need to temporarily bypass hooks. The patch follows existing patterns for global options and appears well-constructed with thorough docs and tests. No reviews or responses yet as this is the initial submission.