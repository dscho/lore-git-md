This is a follow-up discussion in the rebase status reporting improvements thread, focusing on authorship preservation during conflicted rebase operations.

Johannes Schindelin seeks clarification about why authorship information is lost when using `git commit` to resolve conflicts during a rebase (`pick` or `merge` operations, while it is preserved in standalone `git cherry-pick` scenarios. This builds on Phillip Wood's earlier confirmation that authorship loss occurs consistently across all rebase operations when using `git commit` rather than `git rebase --continue`.

The email highlights an inconsistency between rebase's behavior and cherry-pick's handling of authorship preservation. Schindelin specifically questions why the technique used in `git cherry-pick` (where authorship is preserved when committing after conflict resolution) cannot be applied to rebase operations. This suggests a potential area for future improvement in rebase's handling of authorship metadata during conflict resolution.

Key technical context includes:
- The current behavior where `git commit` during rebase loses authorship for both `pick` and `merge` operations
- The contrasting behavior where `git cherry-pick` preserves authorship when committing after conflicts
- The open question about why rebase can't adopt cherry-pick's authorship preservation approach

This discussion doesn't directly impact the current patch series (which focuses on merge commit handling and status reporting), but surfaces a broader architectural consideration about consistent authorship handling across Git's various history-rewriting operations. The thread remains focused on implementation details with no fundamental objections to the series, but this email opens a new line of inquiry about potential future improvements to rebase's behavior.