This is a follow-up discussion on the second patch of Karthik Nayak's bundle performance optimization series, addressing Toon Claes' review comments. The patch replaces the O(N^2) `object_array_remove_duplicates()` with an O(1) `strset`-based solution in `write_bundle_refs()`, yielding significant speed improvements (6x faster at 100k refs).

Key technical points from the discussion:
- Karthik confirms the implementation correctly handles both same-refname cases (regardless of object identity) and exclusion patterns, with test coverage catching edge cases
- Toon verified additional edge cases beyond the test suite and approved the implementation
- Karthik agrees to change from explicit `strset_init()` to `STRMAP_INIT` macro in the next version
- The conversation references Toon's prior work on bundle race conditions (commit b2a6d1c686) as context
- Both parties confirm the optimization maintains correct behavior while dramatically improving performance

The discussion shows thorough review of the technical approach, with only a minor style improvement remaining before the patch is ready for merging. The thread demonstrates consensus on the solution's correctness and readiness.