Build system patch enabling `sysinfo()` support on Cygwin. This is part of a larger series (v2 11/13) aligning build configurations between Makefile and meson systems, with particular focus on platform-specific functionality.

The patch makes three key changes:
1. Moves the `HAVE_SYSINFO` definition from a direct `-D` flag in `BASIC_CFLAGS` to a proper build variable in `config.mak.uname` for Linux
2. Adds explicit `HAVE_SYSINFO` support for Cygwin, which provides a working implementation despite being Linux-specific function
3. Introduces an autoconf check for `sysinfo()` availability

The changes are cleanly implemented with clear commit message explaining the Cygwin compatibility rationale. This appears to be a straightforward technical improvement that maintains consistency with the series' goal of build system alignment while properly handling platform quirks. The patch follows the pattern seen elsewhere in the series for handling similar platform-specific functionality.