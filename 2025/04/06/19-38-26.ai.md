Build system alignment series (part 1) focusing on Makefile/meson consistency and Cygwin-specific fixes. This 13-patch v2 series refines the initial submission with several changes: splitting patch #3 into two clearer commits (#3 and #4), updating test handling for Cygwin regex behavior in patch #7, fixing a typo in patch #12, and adopting 'getrandom' as the default CSPRNG method on Linux in patch #13 per Brian's recommendation.

Key technical changes include:
- Removal of unnecessary compiler flags like `-DCURL_DISABLE_TYPECHECK`
- Conditional setting of `BASIC_CFLAGS` based on `RUNTIME_PREFIX`
- Multiple Cygwin-specific fixes (regex, `getdelim`, `clock_gettime`, `sysinfo` support)
- Corrected RAM calculation in `builtin/gc.c` when using `sysinfo`
- `arc4random` configuration for Cygwin
- Documentation notes about `NO_STRLCPY` and `CSPRNG_METHOD` for Linux

The series demonstrates significant reduction in symbol differences between Makefile and meson builds, with detailed analysis showing remaining discrepancies primarily around stdio symbols under `-fPIC`. Test suite passes on Linux (excluding some tests), with Cygwin showing full compatibility after these changes. Patches #5 and #12 (comment-only changes) are noted as potentially droppable.

This represents part 1 of planned build system changes, with more to come in a future series. The author has successfully tested against current Git branches (master, next, seen) and provides extensive build comparison data showing the `syms.pl` script output before/after changes.