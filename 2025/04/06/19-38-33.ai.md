Platform compatibility patch in the build system alignment series (7/13), focusing on Cygwin regex behavior. This narrows the `NO_REGEX` setting in `config.mak.uname` to only apply to Cygwin 1.7 (pre-2015), as newer versions now pass Git's regex tests after importing FreeBSD's implementation. The change aligns make builds with autoconf/meson behavior where the platform regex library is used by default.

The patch also modifies test t7815.12 to skip on Cygwin, removing an expected-failure case where the platform regex matches NUL characters differently than Git expects. The commit message provides historical context about the test's evolution, noting it was originally marked as expect-failure in 2010 but never properly explained, with one related test later being promoted to success.

This appears to be a straightforward technical correction based on actual Cygwin behavior changes, though the test modification rationale could be clearer about why the NUL-matching behavior remains problematic on modern Cygwin. The change helps unify build system behavior while maintaining compatibility with older Cygwin versions through version detection.