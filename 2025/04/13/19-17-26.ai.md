This is a user question about Git's email workflow functionality, specifically regarding GPG signing of patches sent via `git send-email`.

The email comes from a user struggling to find documentation on how to GPG-sign patches when using Git's email-based contribution workflow. Klaus Frank reports having tried signing commits directly but couldn't determine how to make `git send-email` transmit signed patches, noting that neither its documentation nor `git am`'s documentation mentions GPG signing. The user expresses concern about authenticity and integrity of patches, and mentions having considered but rejected alternative approaches like MTA-level signing due to uncertainty about compatibility with Git's patch application workflow.

The question appears to stem from a gap in documentation rather than a technical limitation, as Git does support signing patches via `git send-email` through its `--signoff` and `--smtp-encryption` options, though these aren't exactly what the user is asking for. The core issue seems to be that the user wants end-to-end signed patches (likely at the email level) rather than just signed commits, and is unsure how to achieve this within Git's tooling while maintaining compatibility with standard patch application workflows.

This could prompt either documentation clarification from maintainers or potentially a feature request for more integrated GPG signing support in `git send-email`. The email doesn't propose any specific solution but rather seeks guidance, indicating the user may be new to the mailing list contribution workflow.