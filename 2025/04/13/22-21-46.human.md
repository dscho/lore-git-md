This is a technical response about GPG signing in Git's email workflow, addressing the initial question from Klaus Frank about signed patches.

Matt Hunter explains why signed commits don't translate well to the patch email workflow. The core issue is that when patches are applied by recipients, they become new commits with the recipient as committer, which would invalidate any original signatures. This explains why tools like `git format-patch` and `git am` don't preserve signatures - they would become meaningless during the patch application process.

The response draws parallels to how `git rebase` and `git cherry-pick` also drop signatures when replaying commits, since the commit data changes when placed in a new history context. This suggests the behavior is consistent across Git's tools when dealing with transformed commits.

While not providing a direct answer to Klaus's original question about email-level signing, the response offers important context about why commit signatures aren't preserved in patch workflows. This helps explain why the documentation might not cover this use case - because the signatures would be invalidated by the normal patch application process anyway.

The email maintains a helpful, explanatory tone while pointing out fundamental limitations in how Git's patch workflow interacts with cryptographic signing. It doesn't propose any solutions but provides valuable technical background that could inform future discussion about potential approaches to signed patches.