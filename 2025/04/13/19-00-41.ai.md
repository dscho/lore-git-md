Performance test suite fix addressing edge case in prerequisite handling for grep performance tests.

This email is a follow-up discussion about the first patch in Philippe Blain's series fixing test_perf invocations in p7821-grep-engines-fixed.sh. The patch had already been reviewed and accepted, but Patrick Steinhardt raised a concern about edge case behavior with empty `$prereq` variables when testing different regex engines (basic/extended/PCRE).

Philippe confirms that the implementation works correctly in all cases:
1. When built with PCRE, `$prereq` is empty but the test still functions properly since `test_skip` treats empty prerequisites as "no prereq needed"
2. The threaded test case (which could show "PTHREADS," with empty prereq) is only reached when GIT_PERF_GREP_THREADS is set, which automatically sets the PERF_GREP_ENGINES_THREADS prerequisite

The discussion reveals subtle details about the test's behavior:
- The test has different code paths for threaded vs non-threaded grep operations
- Prerequisite checking depends on both build configuration (PCRE support) and runtime environment (GIT_PERF_GREP_THREADS)
- The empty prerequisite case was already handled correctly by the test infrastructure

This confirms the mechanical changes to prerequisite specification are sound, addressing the edge case Patrick identified while maintaining the test's intended behavior across all configurations. The exchange serves as final verification that the patch is ready to proceed as-is.