This is a philosophical response in the ongoing discussion about commit header propagation during rebase operations. Junio C Hamano strongly opposes Phillip Wood's suggestion of a configurable approach for selecting which extra commit headers get preserved during conflict-free rebases.

The core argument is a principled stance against configuration-driven header propagation: if a header has well-defined semantics that are universally agreed upon (as opposed to project-specific trailers), then its propagation behavior should be hardcoded rather than configurable. Junio asserts that allowing configuration would undermine the semantic integrity of these standardized headers during history rewriting operations.

This email marks a clear rejection of the configurable approach Phillip had floated in response to earlier concerns about metadata validation. Junio's position elevates the discussion from practical implementation questions to a more fundamental design principle about commit object integrity. The response implies that any solution to the header propagation question must maintain consistent semantics across all Git projects rather than offering per-project customization.

The technical context remains the same - the patch proposes passing through non-signature headers during `git rebase -m`, with test coverage in t3404. But this email shifts the debate from "how to implement" to "what principles should guide the implementation". Junio's veto on configuration options suggests the solution space is narrowing toward either full propagation (as in `git replay`) or maintaining the status quo, with the decision hinging on whether the Git project accepts header propagation as a universal invariant.