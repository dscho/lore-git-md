Refactoring follow-up regarding mmap utility relocation in the object-file subsystem cleanup.

Elijah Newren responds to Patrick Steinhardt's patch that moved the `xmmap()` family of functions from object-file.c to wrapper.c as part of the larger object-file subsystem refactoring series. The email contains two main points:

1. A minor grammatical correction suggestion, noting that "got" in the commit message should be "has" ("its functionality got nothing to do" → "has nothing to do").

2. The bulk of the email is a verbatim copy of the original patch (Patch 3/9) showing the complete movement of mmap-related code:
   - `mmap_limit_check()`
   - `xmmap_gently()`
   - `mmap_os_err()`
   - `xmmap()`
   - All functions were moved unchanged from object-file.c to wrapper.c (48 lines removed, 48 lines added)
   - The move was justified by these being general utilities rather than object-file-specific code
   The patch was part of establishing clearer subsystem boundaries in preparation for pluggable object storage backends.

This appears to be a reference email rather than raising new substantive issues, likely for context in the ongoing discussion about utility function organization that began with Elijah's question about directory utilities in Patch 1/9. The mmap relocation itself is uncontroversial and was already merged as part of the completed series.