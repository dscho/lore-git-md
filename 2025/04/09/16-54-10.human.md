This is a technical proposal in the ongoing Change-ID standardization discussion, where Nico Williams responds to Theodore Ts'o's concerns about metadata semantics. Nico attempts to define concrete rules for how Change-IDs should behave in Git operations, addressing several key aspects:

Nico proposes specific semantics for Change-ID headers: they should be preserved during cherry-picks and rebases, allow manual editing (though discouraged), and support both free-form and URI formats. The proposal distinguishes between patch series IDs (non-unique, shared across commits) and commit-specific IDs (unique per branch but not repository-wide). It also suggests multi-valued headers to handle squashes/merges and optional ref-based indexing of Change-IDs.

The email directly engages with Theodore's concerns about heuristic-based approaches, agreeing they could assist user decisions while maintaining that Change-IDs need well-defined behavior. Nico also references the file rename tracking analogy from earlier in the thread, acknowledging its complexity while using it to argue for clearer Change-ID rules rather than heuristic solutions.

This response moves the discussion toward concrete technical specifications while still needing to address Junio Hamano's philosophical concerns about metadata that could be derived from content. The proposal is detailed but will likely face scrutiny about whether these semantics provide enough unique value to justify inclusion in Git's commit headers.