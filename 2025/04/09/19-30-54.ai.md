This is a bugfix follow-up in an ongoing git-gui thread. Eric Sunshine identifies the reported issue (git-gui rejecting commit messages containing '#' characters when used for task tracking) as potentially matching a recently fixed bug. He references a March 2025 patch by Oswald Buddenhagen that addressed similar behavior, suggesting this might be a duplicate report or related issue.

Key technical details for future reference: The problem manifests when using '#' for task IDs in commit messages (rather than as comment markers), causing git-gui to incorrectly reject valid messages. The reporter's workflow involves format "<#task-id message>". The referenced fix [1] modified git-gui's commit message validation to properly handle such cases.

The thread now has two actionable paths: either confirm this is a regression of the previously fixed issue (indicating the fix may need backporting or wasn't comprehensive), or determine if this represents a distinct but similar edge case. The original bugreport attachment mentioned in the thread starter would help verify environment specifics and exact error messages.