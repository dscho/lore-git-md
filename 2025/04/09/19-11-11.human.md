This is a test fix implementing the final agreed-upon solution for POSIX-compliant printf formatting in submodule tests. The patch follows through on the thread's decision to use "%d" formatting with argument reuse, resolving a potential portability issue where some printf implementations would fail when given extra arguments.

The change simplifies two test setup loops in t/t7422-submodule-output.sh by leveraging printf's built-in ability to reuse format strings for multiple arguments. The first loop now uses `printf` with `test_seq | sed p` to generate submodule entries, while the second uses a straightforward `printf` with `test_seq`. This maintains the test's purpose of verifying recursive submodule status behavior while being more robust across different platforms.

The implementation matches the consensus reached in the thread, where Eric Sunshine recommended this approach and Junio Hamano approved it despite noting the minor awkwardness of passing arguments twice in some cases. The patch cleanly addresses the original POSIX compliance concern while keeping the test's structure intact. With all technical questions resolved in the thread, this appears ready for integration.