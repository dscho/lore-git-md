Final patch in the completed platform-specific Meson build system series, focusing on Windows networking function handling. This addresses the last remaining issue in the thread by correcting how networking symbol checks are handled on Windows.

The patch makes two key changes to the Meson build configuration:
1. Skips checking for networking functions (`inet_ntop`, `inet_pton`, `hstrerror`) entirely on Windows, since they don't exist there (matching CMake behavior)
2. Adds proper compat implementations for these functions when they're missing on non-Windows systems

The author notes this both simplifies the Windows build (avoiding unnecessary compiler checks) and properly handles cases where these functions are missing on other platforms. This completes the series' goal of standardizing platform-specific function availability checks in the Meson build system, with all technical concerns now addressed.

The implementation matches the established pattern from earlier patches - using the centralized function checking system while properly isolating Windows-specific cases. This represents the final piece of the platform detection work that has been under discussion, with all style and technical feedback from reviewers incorporated.