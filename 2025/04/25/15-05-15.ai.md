CI / build system patch addressing a Windows-specific test hang in the Meson-based GitHub Actions workflow. The issue occurs in the `win+Meson` CI job where test t7001-mv's assertion failure triggers a modal dialog in Debug mode, causing 6-hour timeouts. The fix matches the existing `win+VS` job behavior by building in Release mode (`-Dbuildtype=release`) where assertions don't show dialogs. The change is minimal - a single flag addition in the Meson setup command. The commit notes this isn't fixing the underlying assertion bug (described as a known-but-unfixed issue in t7001-mv's "nonsense mv triggers assertion failure" test), but rather working around the problematic CI behavior. The patch appears straightforward with clear motivation and follows established patterns from other Windows CI configurations.