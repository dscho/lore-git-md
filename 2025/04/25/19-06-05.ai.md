Performance optimization for bundle-uri reference negotiation, part 2/2 of v5 series. This patch adds test coverage for tag handling in bundle-uri clones, complementing the earlier reference expansion work.

The test creates a repository with branches and tags, bundles it with `--all`, then verifies the cloned repository properly includes the tags under the `refs/bundles/` hierarchy. The verification uses the same `for-each-ref` with `lstrip` approach that was previously reviewed and approved for branch testing, now extended to tags. The 30-line test addition follows the established pattern from earlier tests in `t5558-clone-bundle-uri.sh`, creating a test repo with multiple branches and tags, then verifying the bundle clone preserves the references correctly.

This completes the test coverage for the bundle-uri reference expansion feature, which has demonstrated significant performance improvements (32% to 1% object downloads) in earlier testing. The patch maintains consistency with the series' approach while addressing the remaining test case for tags. With this addition, the test suite now covers all reference types (branches and tags) for both path-based and HTTP bundle scenarios.

The series remains technically complete with all substantive feedback incorporated, now just awaiting final documentation updates per Phillip Wood's earlier review. Junio Hamano has already queued the technically equivalent v4 in `seen`, indicating this is ready for integration pending those doc updates.