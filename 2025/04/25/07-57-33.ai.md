This is a follow-up discussion about the Meson benchmark integration series, specifically addressing documentation clarity around benchmark execution behavior.

Christian Couder points out a potential inconsistency in the documentation added by Patrick Steinhardt's patch. While the commit message clearly states benchmarks don't run in parallel (one of three special treatments listed), the meson.build comment suggests benchmark execution follows the same parallelization rules as tests by including the parallelization note immediately after benchmark examples without qualification.

The issue is subtle but important - the current documentation could mislead readers into thinking benchmarks inherit test parallelization behavior when they actually have different execution characteristics. This doesn't affect the technical correctness of the implementation (which properly uses Meson's benchmark() function with its non-parallel semantics), but could cause confusion for users reading the build file comments.

The thread context shows this is part of the final v4 series implementing all core functionality for Meson benchmark support, with documentation updates being one of the few remaining items. This specific observation about execution semantics documentation would need to be addressed either through comment restructuring or additional clarification text.