This is patch 8/13 in Patrick Steinhardt's series refactoring the object store subsystem, part of the ongoing `the_repository` removal effort. The patch modifies `git show-ref` to avoid unnecessarily fetching promisor objects when verifying refs.

The change replaces `repo_has_object_file()` with `has_object()` in `builtin/show-ref.c`, explicitly using the `HAS_OBJECT_RECHECK_PACKED` flag to maintain the existing behavior of checking packed objects while skipping promisor object fetches. The rationale is sound - reference validation shouldn't trigger background fetches since refs should never point to missing objects in the first place.

This continues the series' pattern of methodically converting object existence checks to the new API while carefully considering each case's requirements. The change is small (just 2 lines modified) but demonstrates the series' attention to detail in handling edge cases. The patch builds on earlier work that established the flag-based API in patch 5 and began systematic conversions in patch 6.

As with previous patches in the series, this appears to be a well-considered change with clear motivation and minimal impact. It follows the established pattern while addressing a specific subsystem (`show-ref`) that shouldn't interact with promisor objects. The series continues to show strong technical coherence and careful attention to maintaining correct behavior across Git's various components.