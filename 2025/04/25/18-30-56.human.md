This is a bugfix discussion regarding `pack-objects` functionality. Elijah Newren responds to Nik Garza's bug report about `should_include_obj()` being incorrectly called on non-existent objects, which causes crashes during pack operations. Newren confirms the issue stems from a commit introduced in v2.48.0 (c08589efdc5) that added promisor pack functionality, and he CCs the original author for input. The bug manifests when fetching tags, causing `pack-objects` to die with signal 6.

The response is helpful in tracing the regression to a specific version change, though Newren notes that bug reports should include details inline rather than as attachments. This appears to be a legitimate regression introduced by recent changes to pack handling, and the discussion is just beginning with the original author now looped in. The impact seems significant enough to warrant attention from packfile experts.