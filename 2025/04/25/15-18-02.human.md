CI configuration patch addressing Windows test hangs in Meson builds.

Junio C Hamano replies to Johannes Schindelin's patch that fixes a 6-hour CI timeout in Windows Meson builds. The timeout occurs when test t7001-mv triggers an assertion that opens a modal dialog in Debug mode. The patch matches the existing Visual Studio CI behavior by building in Release mode (`-Dbuildtype=release`) where assertions don't show dialogs.

Junio confirms the problem is real ("I've been annoyed for a long time") and thanks Johannes for the fix. He asks a clarifying question about whether converting the assert to a BUG()` would also work, but makes clear he's not opposing the current solution. The reply indicates this straightforward CI configuration change is likely to be accepted, though it leaves the underlying test issue unaddressed.

The exchange shows maintainer approval for this pragmatic workaround while hinting at a deeper test issue that may need attention later. The one-line change to the workflow file follows established patterns from the VS-based configuration.