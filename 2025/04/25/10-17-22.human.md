This is a brief follow-up in the `git send-email` OAuth2/SMTP thread. Erik Hulsmann acknowledges Aditya Garg's pull request to perl-authen-sasl (PR #19) which contains the now-upstreamed OAuth2 implementation that was previously part of the `git send-email` patch series. 

Erik notes they've reviewed the PR and provided some improvements and questions, expressing general support for moving XOAUTH2 authentication forward. This appears to be a positive technical review of the now-separated OAuth2 work, which was split from the main Outlook Message-ID handling patch that has already been finalized in v6 of that series.

The email doesn't introduce any substantive new discussion points about the core `git send-email` functionality, but shows the OAuth2 component continuing to progress through the perl-authen-sasl upstream process with active review from other contributors.