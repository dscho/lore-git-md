This is the final version of a 6-patch series addressing platform-specific build system detection in Git's Meson configuration. The series standardizes function availability checks and fixes several cross-platform compatibility issues, particularly for Solaris and Windows.

The key technical changes include:
- Refactoring function checks into a structured dictionary format that explicitly lists compatibility implementations
- Adding proper detection for `getpagesize()` and `sysinfo()`
- Fixing networking function checks (hstrerror, inet_ntop/pton) with platform-specific handling
- Maintaining compatibility implementations where needed

The series responds to Patrick Steinhardt's earlier feedback by:
- Replacing the "magical" fs.exists() usage with explicit source file lists
- Improving code organization and style
- Keeping Windows-specific handling properly isolated

This completes the platform detection work that was technically approved but needed style polish. The changes maintain backward compatibility while making the build system more robust across platforms. All functional issues from earlier versions have been resolved, and the implementation now uses the preferred style for Meson configuration.

The series works in parallel with ongoing efforts to introduce array-based function verification, but that remains a distinct future improvement. This submission appears ready for final review and merge, having incorporated all requested changes from previous iterations.