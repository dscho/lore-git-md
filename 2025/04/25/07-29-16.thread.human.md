This thread introduces a patch series that advances the ongoing effort to replace `git gc` with more granular maintenance tasks. The series, consisting of 7 patches, implements the final missing pieces needed to fully cover `git gc`'s functionality - specifically adding standalone "worktree-prune" and "rerere-gc" maintenance tasks.

The author begins by refactoring `builtin/gc.c` to extract worktree pruning and rerere garbage collection into separate functions, then implements these as new maintenance tasks. The changes include exposing a new `get_worktrees()` helper, creating `prune_worktrees()` and `gc_rerere()` functions, and updating documentation. Test coverage is added for the new tasks.

While this series doesn't yet switch over to using these tasks exclusively, it represents an important step toward the long-term goal of deprecating `git gc` in favor of more controlled maintenance operations. The patches appear well-structured with clear commit messages explaining each change. The work touches several subsystems including worktree management and rerere functionality, while maintaining backward compatibility.