This is a technical clarification in the ongoing Meson build system thread about shell path detection, specifically addressing edge cases where the system shell may not meet Git's requirements.

Brian m. carlson provides important context about shell compatibility beyond POSIX requirements, noting that AT&T ksh (including ksh93/ksh88) lacks `local` support which Git needs. They highlight NonStop systems where the default `sh` is AT&T ksh and thus unusable, requiring bash instead. The email also mentions Debian's historical zsh-as-sh implementation which had pipeline subshell behavior issues (now fixed by the author's patch) that could break Git. 

The response reinforces Junio's earlier point about needing to specify non-standard shell paths, extending it to cases where the binary name might be `sh` but the implementation is incompatible. While POSIX only requires the name "sh", Git has stricter functional requirements that some compliant shells don't meet. The examples given show real-world systems where users would need to specify alternate shell paths via `-Dsane_tool_path=`.

This doesn't contradict the current Meson implementation but provides important context about why the ability to override shell paths is necessary - not just for path location but for shell implementation differences. The email serves as a valuable supplement to the thread's technical discussion, documenting edge cases the implementation needs to handle while confirming the existing configuration mechanism (`-Dsane_tool_path=`) is sufficient to address them.