This is a follow-up discussion about the Meson benchmark integration series. Christian Couder points out a potential documentation inconsistency in Patrick Steinhardt's patch that wires up benchmarks in Meson. The commit message states benchmarks don't run in parallel, but the example in the meson.build file could be misinterpreted to suggest they follow the same parallel execution behavior as tests.

The thread context shows this is part of a completed v4 series that fully integrates Git's performance benchmarks with Meson, including repository format preservation, Perl script portability, and out-of-tree build support. The series already received maintainer approval and implements all technical components, with only minor documentation clarification needed before finalization.

This is a small but important nit - the example should probably be clarified to avoid confusion about benchmark execution behavior, since parallel execution is explicitly disabled for benchmarks (unlike regular tests). The observation doesn't affect the core functionality but helps ensure accurate documentation of the new Meson benchmark support.