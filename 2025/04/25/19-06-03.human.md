Performance optimization for bundle-uri reference handling, now in its fifth iteration. This series from Scott Chacon expands bundle-uri's reference negotiation to handle all refs (not just branches) while preserving hierarchy, showing significant performance improvements (reducing object downloads from 32% to 1%). The v5 update is minimal - just cleaning up some colorful language in the commit message while keeping all technical changes intact.

The two-patch series:
1. Modifies `bundle-uri.c` to handle all reference types through a simple `skip_prefix` change
2. Adds comprehensive test coverage in `t/t5558-clone-bundle-uri.sh` for tag handling (both lightweight and annotated)

The implementation has already been queued in Junio Hamano's 'seen' branch after addressing all substantive feedback. The only remaining item is documentation updates requested by Phillip Wood, which aren't blocking further progress. The tests now use Taylor Blau's recommended `for-each-ref` verification approach and cover edge cases including HTTP bundles, failure modes, and non-default hash algorithms.

This appears ready to merge, withstanding the minor documentation polish still needed. The performance benefits are clear and the test coverage thorough.