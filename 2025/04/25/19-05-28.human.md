This is a follow-up in the `git send-email` Outlook Message-ID handling thread, focusing on the OAuth2 authentication component. Erik Huelsmann confirms the Authen-SASL 2.1800 release containing XOAUTH2 support is now official on CPAN, but notes uncertainty about when major Linux distributions will package it (estimating potentially 2 years for Debian). He suggests a backward-compatible approach where Git could probe for XOAUTH2 support at runtime rather than requiring the new version, allowing the feature to work where available without blocking users on older distro packages.

The exchange shows the final pieces falling into place for this long-running effort - the core Message-ID handling is queued, OAuth2 support is now officially released upstream, and there's discussion about graceful handling of the inevitable distribution lag. While not introducing new technical content, this email provides important context about real-world deployment considerations that will affect when users can actually benefit from these improvements.