This is a minor review comment on the test infrastructure part of the bugfix series for `git index-pack`'s REF_DELTA handling.

Patrick Steinhardt questions why the new `test-tool pack-deltas` helper doesn't use Git's standard `parse_options()` interface instead of directly parsing arguments with `atoi()`. He points out that the current implementation silently accepts non-integer arguments, which could lead to confusing behavior. This is a small but valid code quality observation about the test helper that was introduced earlier in the thread.

The comment doesn't affect the core bugfix itself, which has already been through extensive review regarding its functionality and security implications. This is more about making the supporting test infrastructure more robust and maintainable. The suggestion aligns with Git's coding conventions where `parse_options()` is the preferred way to handle command-line arguments.

The thread context shows this test helper has already undergone several rounds of refinement (including memory leak fixes and test layout improvements), so this would be one more potential tweak to consider before finalizing the series.