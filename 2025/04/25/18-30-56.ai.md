Bugfix response in the `pack-objects` thread. Elijah Newren follows up on Nikolas Garza's bug report about `should_include_obj()` being called on non-existent objects. Newren confirms the bug's existence by tracing the error message to commit c08589efdc5 ("index-pack: repack local links into promisor packs"), which was introduced in v2.48.0. He notes this matches Garza's report of the issue appearing after upgrading from v2.47 to v2.49. The response includes technical details showing how he verified the bug's origin via `git grep` and `git log -S` commands. Newren also provides Git mailing list norms feedback by requesting future bug reports include details inline rather than as attachments. The core technical issue remains the same as originally reported - a BUG() assertion firing in `builtin/pack-objects.c` line 4310 when `should_include_obj()` encounters non-existent objects during pack operations. The thread now includes the original commit author via CC for further investigation.