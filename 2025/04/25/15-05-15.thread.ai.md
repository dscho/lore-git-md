Thread identity: Windows CI build system patch addressing test hang in Meson-based GitHub Actions workflow.

Core problem: The `win+Meson` CI job hangs for 6 hours when test t7001-mv's assertion failure triggers a modal dialog in Debug mode. The immediate solution is to match the existing `win+VS` job behavior by building in Release mode (`-Dbuildtype=release`) where assertions don't show dialogs, while the underlying test issue remains known but unfixed.

Current status: Patch proposes minimal change (single flag addition in Meson setup command) which Junio C Hamano has indicated is appropriate. He raises a technical question about potentially replacing asserts with BUG() calls as an alternative approach, but this appears to be a separate consideration rather than a blocker for the current CI fix.

Key technical details: Modifies `.github/workflows/main.yml` to add `-Dbuildtype=release` flag for Meson builds, following same approach used in VS-based Windows CI configuration. Does not modify the problematic t7001-mv test case that triggers assertion dialogs in Debug mode. The change is purely a CI configuration adjustment to prevent hangs.

Open questions: Junio's note about assert/BUG alternatives could spawn follow-up work, but does not affect the current patch's viability. The long-standing test behavior that triggers modal dialogs remains an open but separate issue.

Participant positions: Original author proposed the straightforward configuration change. Junio confirms the solution is appropriate while raising a tangential technical question about assertion handling. No objections or alternative proposals have been made, indicating consensus on proceeding with the Release mode adjustment.

Related work: Builds on existing Windows CI configuration patterns from the `win+VS` job. The underlying test issue in t7001-mv has been known but not addressed by this change.