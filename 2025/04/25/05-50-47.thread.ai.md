Thread identity: Windows-specific case-sensitivity bug report involving file casing conflicts (`button`/`Button`, `tabs`/`Tabs`) in a Svelte project, preventing branch switching despite clean working tree status.

Core problem: Git's case-sensitivity handling on Windows (`core.ignorecase`-related) causes working tree/index desynchronization. Files appear modified when they shouldn't be, with `git status` showing clean but checkouts failing due to phantom untracked files. Specific files affected: `src/lib/components/UI/Tabs.svelte` and related components. The issue persists across Git operations (`git checkout -- .`, `git rm --cached`, `git clean -fdx`, index reset) and system restarts, creating an unrecoverable state without manual filesystem intervention.

Current status: Reporter confirmed the issue's severity through multiple failed resolution attempts. Only manual file deletion in Windows Explorer (bypassing Git) allowed branch switching, though it left the repository in an inconsistent state (file shown as both deleted and untracked). Problem reproduces when switching from `design-system` to `dev` branch with Git 2.36.1.windows.1.

Key technical details:
- Windows filesystem case sensitivity interactions with `core.ignorecase` toggling
- Working tree and index desynchronization regarding file casing
- Git reports clean working tree while preventing branch switches
- Svelte component files (`Tabs.svelte`/`tabs.svelte`) exhibit the issue
- Problem persists across different Git operations and persists after system restarts
- Creating new temporary branches doesn't resolve the branch switching problem

Open questions:
- Is this a new edge case in Git's Windows case-sensitivity logic?
- Why do standard Git recovery operations fail to resolve the state?
- Would a minimal test case help Windows maintainers reproduce the issue?
- How does manual filesystem deletion bypass Git's internal state tracking?

Participant positions: Single reporter providing detailed follow-up confirming original bug's severity and persistence. Still awaiting input from Windows maintainers (Johannes Schindelin, Johannes Sixt) or others familiar with case-sensitivity edge cases.

Related work: Any prior discussions about `core.ignorecase` behavior on Windows or case-sensitivity edge cases would be relevant, particularly those involving unrecoverable states or filesystem/Git state desynchronization.