Thread identity: Bug report about Windows-specific case-sensitivity issues in Git, involving file casing conflicts (`button`/`Button`, `tabs`/`Tabs`) in a Svelte project.

Core problem: Git's handling of case changes on Windows filesystems (`core.ignorecase`-related) causes working tree and index to get out of sync. Files appear modified when they shouldn't be, with `git status` showing clean but checkouts failing due to phantom untracked files. Specific files affected: `src/lib/components/UI/Tabs.svelte` and related components.

Current status: Reporter encountered the issue and temporarily resolved it through manual file deletion in Windows Explorer, but the underlying problem remains unaddressed. Standard workarounds (`git rm --cached`, `git clean -fdx`, index reset) failed to resolve the case-sensitivity conflict.

Key technical details:
- Windows filesystem case sensitivity with `core.ignorecase` toggling
- Inconsistent behavior between working tree and index regarding file casing
- Reproduction shows Git claiming clean working tree while preventing branch switches
- Problem manifests with Svelte component files having case variations

Open questions:
- Is this a bug in Git's case-sensitivity logic on Windows?
- What specific edge case in case-change operations triggers this?
- Would a minimal test case demonstrate the issue more clearly?

Participant positions: Single reporter so far (detailed bug report with reproduction steps but no minimal test case). Likely relevant to Windows maintainers (Johannes Schindelin, Johannes Sixt).

Related work: Any prior discussions about `core.ignorecase` behavior on Windows or case-sensitivity edge cases would be relevant to investigate.