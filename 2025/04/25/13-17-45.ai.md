This is the v4 version of Scott Chacon's performance optimization series for bundle-uri reference negotiation during fetches, now incorporating Taylor Blau's test improvements. The series remains focused on expanding reference handling from just `refs/heads/` to all `refs/` namespaces while preserving hierarchy, with demonstrated performance benefits.

The key change in this version is the test suite update in patch 2, which now implements Taylor Blau's suggested approach for verifying references. The test now uses direct comparison of source and destination references via `for-each-ref` with `lstrip` formatting, replacing the previous grep-based approach. This provides more robust verification while simplifying the test structure. The commit message shows some frustration ("ok, now it's right. christ.") suggesting the test refinement process has been challenging.

The implementation in bundle-uri.c remains unchanged from v3 (just a one-line modification to handle all reference types). The test changes are substantial (202 lines modified in t/t5558-clone-bundle-uri.sh) but primarily reorganize existing test cases to use the new verification approach rather than adding new test coverage.

This version appears to address all outstanding test-related feedback from reviewers (Blau, Claes, Hamano) while maintaining the core functionality that showed significant performance improvements (32% to 1% object downloads). The series seems ready for merging pending documentation updates previously identified by Phillip Wood, which were not part of this test-focused revision.