This is patch 11/13 in Patrick Steinhardt's object store refactoring series, focusing on list-objects filtering behavior. The patch clarifies and improves the logic for excluding promisor objects when `--exclude-promisor-objects` is used.

The key change replaces `repo_has_object_file()` with the new `has_object()` API in `process_blob()`, specifically using only the `HAS_OBJECT_RECHECK_PACKED` flag to avoid promisor fetches. The commit message explains that while the current implementation appears broken (since `repo_has_object_file()` would normally fetch promisor objects), it actually works because `fetch_if_missing` gets unset when the exclude option is enabled. The patch makes this behavior more explicit and consistent with the series' goal of preventing unwanted promisor fetches.

The change is minimal (2-line diff) but important for understanding the interaction between promisor object exclusion and fetching behavior. This continues the pattern established in patches 8-10 of using `has_object()` with explicit flags rather than the more opaque `repo_has_object_file()` variants. The modification maintains all existing functionality while making the code's behavior more transparent and aligned with the series' architectural direction.

This patch addresses one of the remaining open questions from the thread about list-objects filtering behavior. It demonstrates how the new `has_object()` API provides better control over promisor object handling compared to the older interfaces being phased out.