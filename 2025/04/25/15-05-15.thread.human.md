This thread addresses a CI timeout issue in Windows builds using Meson, where test t7001-mv's assertion failure triggers a modal dialog that hangs the GitHub Actions workflow for 6 hours. The original proposal from the author suggests matching the existing Visual Studio build configuration by switching to Release mode (`-Dbuildtype=release`), which avoids the dialog issue. The change is minimal, affecting only the Meson setup command in the CI workflow file.

Junio C Hamano has responded, confirming the problem and solution while raising a tangential question about whether replacing `assert()` with `BUG()` calls might be a more comprehensive fix. However, he accepts the current patch as-is, noting it resolves the immediate CI timeout annoyance. The discussion remains focused on the CI configuration change rather than the underlying test issue, which remains known but unaddressed here.

With no objections raised and Junio's implicit approval, this straightforward change appears ready to proceed. The alternative approach Junio mentioned (using `BUG()`) could be explored separately but doesn't block this CI fix.