This is a platform compatibility patch in a completed 6-part series modernizing Git's Meson build system. It adds proper detection and linking for networking libraries (-lnsl and -lsocket) on SunOS/Solaris systems, matching behavior that was present in the old autoconf-based build system.

The change is straightforward - rather than just checking for -lresolv, it now checks for three common networking libraries in a single list operation. The patch follows the series' pattern of centralizing platform-specific dependencies, though in this case the change is focused specifically on Solaris compatibility.

This appears to be a well-justified change that completes the build system's handling of networking dependencies across platforms. The commit message clearly explains the change by referencing the existing config.mak.uname and configure.ac behavior. The diff is clean and focused, just modifying the networking dependencies section of meson.build.

As part of the now-completed platform compatibility series, this patch represents final polish rather than introducing new concepts - the technical approach was already reviewed and approved earlier in the thread.