This is a technical clarification in the ongoing object store refactoring series, specifically addressing patch 07/13 which modifies index-pack's collision check behavior. The email responds to Karthik Nayak's review questions about the promisor object handling logic.

Key technical points clarified:
- The collision check verifies content identity (not just OID) between incoming packfile objects and existing repository objects
- Current behavior unnecessarily fetches promisor objects during this check
- Patrick explains this creates a circular verification where we fetch an object only to compare it against the same object coming in via the packfile
- Maintains the series' goal of removing implicit promisor fetching where not needed

The exchange confirms the patch's approach of using `OBJECT_INFO_SKIP_FETCH_OBJECT` to avoid this promisor fetch is correct, though the underlying collision check semantics remain unchanged. This keeps the focus on the series' API cleanup while ensuring behavior matches intent.

The thread context shows this is part of a well-reviewed series where Karthik has already approved the general approach to promisor handling (via the `HAS_OBJECT_FETCH_PROMISOR` flag system). This email resolves the last minor points of confusion before the series moves forward.