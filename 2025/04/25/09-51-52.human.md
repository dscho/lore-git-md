This is a follow-up discussion about test infrastructure for the `index-pack` bugfix series. Johannes Schindelin responds to Patrick Steinhardt's suggestion about using `parse_options()` in the new `test-tool pack-deltas` helper.

The exchange clarifies that while `parse_options()` would be ideal (especially with its new integer support), Johannes notes it's working with an older Git version (v2.49.0) where this support isn't available. They agree on an alternative approach that improves argument validation without requiring the newer `parse_options()` functionality.

This is a minor refinement to the test helper that was already substantially reviewed and improved earlier in the thread. The discussion shows the contributors converging on a practical solution while maintaining compatibility constraints. The core bugfix series and its test infrastructure remain unchanged by this exchange.