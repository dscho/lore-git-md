Performance optimization for bundle-uri reference negotiation, now in v5 with minor cleanup. This is Scott Chacon's follow-up to the v4 series that was already queued in `seen` by Junio Hamano. The changes are minimal - the v5 revision removes an expletive from the commit message of patch 2/2 while keeping all technical content identical to v4.

The series remains focused on expanding bundle-uri's reference handling from just `refs/heads/` to all `refs/` namespaces while preserving hierarchy, with demonstrated performance benefits (reducing object downloads from 32% to 1%). The core change is still the one-line `skip_prefix` modification in `bundle-uri.c`, now accompanied by comprehensive test coverage in `t/t5558-clone-bundle-uri.sh` (203 lines) that verifies handling of both lightweight and annotated tags.

Key technical points remain unchanged from v4:
- Part 1/2 implements the reference expansion with the `skip_prefix` check
- Part 2/2 provides test coverage for tag handling in bundle-uri clones
- Test suite uses `for-each-ref` verification per Taylor Blau's suggestion
- Maintains coverage for path/HTTP bundles, failure modes, and non-default hashes

The only delta is the cleaned-up commit message in patch 2/2, removing the informal language while keeping all technical content. The series remains in good standing with all substantive review comments addressed from v4, and Phillip Wood's documentation feedback still pending but not blocking progress. The implementation and test coverage are complete, with the maintainer having already queued the technically equivalent v4 version.