This is a discussion about improving `git blame`'s handling of missing `.git-blame-ignore-revs` files, continuing the thread started by Michael Grosser. Eric Sunshine chimes in with references to relevant past discussions on similar topics.

The thread explores two potential solutions to the current limitation where setting a global `blame.ignoreRevsFile` fails fatally when the file doesn't exist in a repository. Eric points to prior work that might inform the solution, linking to two previous discussions about related `git blame` functionality.

The first reference is to a pull request that added the ignore-revs feature originally, while the second points to a recent patch series from Junio Hamano that touched on similar configuration handling. These links provide important context for anyone considering implementing the requested changes, showing how the feature evolved and what design decisions were made previously.

The discussion remains at the proposal stage, with no concrete implementation yet. The references to past work suggest this isn't the first time this usability issue has come up, which may indicate the problem is worth solving. The thread appears to be moving toward consensus that some solution is needed, with the main question being which approach (automatic file checking vs. explicit ignore-missing option) would be most appropriate.