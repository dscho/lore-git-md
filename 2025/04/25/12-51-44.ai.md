This is a minor follow-up email in the thread about removing the unused `log_reencode` field from `struct rev_info`. The email doesn't contain any new technical content about the patch itself, but rather provides a helpful tip about Git's commit reference formatting.

D. Ben Knoble points out that Junio C Hamano can use `git show -s --format=reference` to properly format commit references, which was mentioned as needed for the commit message tweak. This is presented as general knowledge sharing rather than a required change, since Junio had already indicated he would handle the formatting adjustment before applying the patch.

The email maintains the thread's positive momentum toward applying this simple refactoring patch, which removes dead code with no behavior impact. No further action appears needed from the patch author Lucas Seiki Oshiro or helper Patrick Steinhardt, as the maintainer will make the final minor adjustment.