CI/build system follow-up discussing failure handling in external dependency downloads for Git's CI.

Patrick Steinhardt responds to Junio Hamano's patch that makes Perforce, Git-LFS, and JGit downloads optional in CI scripts. While agreeing with the overall approach to prevent CI failures from network issues, Patrick suggests tightening the error handling to only allow failures for network operations (wget) while keeping strict checks (like chmod) that should never fail if the download succeeded. This builds on the thread's established direction of making external dependencies optional while maintaining CI reliability.

Key technical points in the response:
- Focuses on the error handling pattern in ci/install-dependencies.sh
- Suggests distinguishing between network-dependent operations (which should be allowed to fail) and local operations (which should remain strict)
- References the same code section Junio modified for Perforce downloads
- Implies the same logic should apply to Git-LFS and JGit handling

The discussion continues the thread's technical focus on CI robustness while introducing a refinement to the error handling strategy. No new participants have joined the conversation yet. The exchange remains narrowly focused on implementation details of the CI script changes, with both parties agreeing on the core approach but discussing execution nuances.