This thread began with Nikolas Garza reporting a bug in `pack-objects` where `should_include_obj()` (line 4310 in `builtin/pack-objects.c`) was being incorrectly called on non-existent objects, potentially causing crashes. The issue was observed after upgrading from Git v2.47 to v2.49.

Elijah Newren has now confirmed the bug's existence, tracing it to commit c08589efdc5 ("index-pack: repack local links into promisor packs") introduced in v2.48.0. His analysis using `git grep` and `git log -S` corroborates Garza's report. Newren also took the opportunity to provide guidance on mailing list norms, suggesting future bug reports include details inline rather than as files. The thread now includes the original commit author via CC for further investigation. The core issue remains unchanged - a BUG() assertion firing when `should_include_obj()` encounters non-existent objects during pack operations.