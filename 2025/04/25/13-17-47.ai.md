Test patch for bundle-uri tag handling in clones, part of the performance optimization series for bundle-uri reference negotiation.

This second patch in the v4 series adds test coverage for tag handling in bundle-uri clones, completing the test suite expansion for the reference namespace changes. The test creates a repository with branches and tags, bundles it with `--all`, then verifies the cloned repository properly includes the tags under `refs/bundles/`. The implementation uses `for-each-ref` with `lstrip` formatting for verification, following Taylor Blau's suggested approach from earlier in the thread. The test structure matches the existing bundle-uri test patterns while specifically exercising tag propagation.

Key technical details:
- Tests both lightweight tags and branch structure
- Verifies tag inclusion in `refs/bundles/` hierarchy
- Uses the established `for-each-ref` verification pattern
- Maintains consistency with existing test file structure
- Complements the earlier performance-focused tests

This completes the test coverage requirements identified in the thread, addressing all reviewer feedback on verification methods while maintaining the series' focus on comprehensive testing for the expanded reference handling. The patch appears ready for inclusion alongside the core implementation changes from part 1.