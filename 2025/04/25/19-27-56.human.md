This is a documentation update for the bundle-uri performance optimization series. Scott Chacon's v6 patch series addresses Phillip Wood's outstanding documentation request from earlier reviews, now that the technical implementation has been approved and queued in the `seen` branch.

The key change in this version is updating the bundle-uri documentation to correctly reflect that the refspec used for bundle references is fixed as `+refs/*:refs/bundles/*`, removing the incorrect claim that it can be configured. The documentation now matches the actual behavior implemented in the earlier patches, which expanded reference handling from just branches to all ref namespaces (including tags) while preserving hierarchy under `refs/bundles/`.

The series remains technically identical to the v5 version that Junio Hamano has already queued, with only this documentation clarification added. The second patch maintains the comprehensive test coverage added in the series, including verification of tag handling. With this documentation update, the series appears ready for final merging once the maintainer is satisfied with the wording.