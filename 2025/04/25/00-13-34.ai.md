This is a bugfix patch in the completed platform-specific Meson build system work, specifically addressing Solaris networking library handling. The patch corrects a typo in the build system's function availability checks where `strerror` was incorrectly listed instead of the networking function `hstrerror`. 

The change is straightforward - it replaces one function name with another in the list of networking functions being verified during the build. The commit message notes that while `strerror` isn't actually used or checked anywhere else in the codebase, `hstrerror` is a legitimate networking function that should have been included in these checks. The patch maintains the existing pattern where unavailable functions trigger corresponding `NO_` macro definitions.

This appears to be the final patch in the 6-part series addressing Meson build system platform compatibility issues, with previous patches having handled getpagesize(), sysinfo(), and other networking library dependencies. The fix is small but important for ensuring proper function availability detection on platforms where these networking functions may not be present. The patch follows the established pattern from earlier in the series and doesn't introduce any new technical approaches.