Performance optimization for bundle-uri reference negotiation, now in v7 with whitespace fixes. This is the final iteration of Scott Chacon's series to expand bundle reference handling from just branches to all ref namespaces (tags, notes, etc.), demonstrating significant performance improvements in fetch operations.

The v7 version contains only minor documentation whitespace fixes from v6, with no functional changes. The core implementation remains:
- Modified `bundle-uri.c` to handle all references via updated `skip_prefix` check
- Comprehensive test coverage in `t/t5558-clone-bundle-uri.sh` (263 lines) for both path-based and HTTP bundles
- References now placed under `refs/bundles/` hierarchy using fixed refspec `+refs/*:refs/bundles/*`
- Documentation updates in `bundle-uri.adoc` to clarify refspec behavior

The thread context shows this series is complete with all reviewer feedback addressed, including:
- Taylor Blau's test verification approach
- Phillip Wood's documentation suggestions
- Maintainer Junio Hamano has already queued the technically equivalent v4 in `seen`

Key technical details for future reference:
- Performance impact: reduces object downloads from 32% to 1% in tested scenarios
- Test coverage includes:
  * Path/HTTP bundle handling
  * Branch and tag verification
  * Failure modes
  * Non-default hashes
  * Negotiation cases
- Uses `for-each-ref` + `lstrip` verification pattern
- Documentation now accurately describes the fixed refspec behavior

The v7 iteration is purely a cosmetic fix for whitespace issues caught in testing, maintaining all the substantive improvements from previous versions. The series appears ready for final merge with no outstanding technical concerns.