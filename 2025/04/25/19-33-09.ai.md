This is the second patch in a performance optimization series for bundle-uri reference negotiation, focusing on test coverage for tag handling. The patch adds 30 lines of test code to ensure tags are properly handled when cloning from bundles, completing the test suite for the expanded reference support.

The test creates a repository with both branches and tags, generates a bundle containing all references, then verifies the cloned repository correctly receives the tags through the bundle-uri mechanism. The verification uses `for-each-ref` with `lstrip` to compare references between source and destination, following the approach suggested by Taylor Blau in earlier feedback.

Key technical details:
- New test file `clone-from-tags` sets up a repository with branches and tag `tag-A`
- Bundle creation uses `--all` to include all references (branches and tags)
- Clone verification compares `refs/bundles/` hierarchy against source repository
- Maintains consistency with existing test patterns in `t5558-clone-bundle-uri.sh`

This completes the test coverage for the bundle-uri reference expansion work, which now handles all reference types (branches and tags) while demonstrating significant performance improvements in object negotiation. The patch follows the same verification methodology used for branch testing in previous iterations, ensuring consistent behavior across reference types.

With this addition, the test suite now covers all aspects of the reference handling changes, from path-based to HTTP bundles, with comprehensive verification of both branch and tag scenarios. The series has already been approved by the maintainer and is queued for integration.