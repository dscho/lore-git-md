Test patch completing bundle-uri tag support verification. This is the second and final patch in Scott Chacon's v6 series optimizing bundle-uri reference negotiation, now queued in `seen` as v4 with maintainer approval.

The patch adds 30 lines of test coverage in `t/t5558-clone-bundle-uri.sh` to verify tag handling in bundle-uri clones, complementing the earlier branch testing. It creates a test repository with both branches and tags (`tag-A`), generates an `ALL.bundle` containing all refs, then clones using the bundle and verifies the tags are properly transferred to `refs/bundles/tags/` hierarchy. The verification uses `for-each-ref` with `lstrip` as previously suggested by Taylor Blau, comparing source refs against the cloned repository's bundle refs.

This completes the test matrix for the bundle-uri reference expansion feature, which now handles all ref namespaces (not just branches) while maintaining hierarchy under `refs/bundles/`. The test follows the same pattern as existing branch verification but adds the tag-specific case, ensuring the refspec `+refs/*:refs/bundles/*` works uniformly across reference types. With this addition, the series now has comprehensive test coverage for both path-based and HTTP bundle scenarios across all reference types.