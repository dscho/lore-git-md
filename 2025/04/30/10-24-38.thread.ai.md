Scalar feature patch series introducing `--no-maintenance` flag for both `scalar register` and `scalar clone` commands, completing the two-part series addressing interference issues in automated environments. The changes allow skipping background maintenance setup when Scalar configures repositories for later use (e.g. disk image creation), preventing maintenance.lock conflicts with subsequent automation steps.

First patch modified `scalar register` to accept the new `--no-maintenance` flag via updates to `register_dir()` function signature and `cmd_register()` implementation, documented in scalar.adoc with test coverage in t9210-scalar.sh. Second patch now extends identical functionality to `scalar clone`, adding flag handling in `cmd_clone()` while reusing the same `register_dir()` integration. Documentation updates explain the consistent opt-out behavior across both commands, with new verification in t9211-scalar-clone.sh showing proper handling when maintenance scheduling would normally fail.

Technical approach maintains backward compatibility (defaulting to enabled maintenance) while providing the opt-out mechanism needed for expert automation scenarios. Test coverage demonstrates attention to edge cases in both commands, particularly around maintenance scheduler interactions. The series remains narrowly scoped to each command's implementation while sharing the common flag behavior, with no outstanding objections or review feedback requiring changes. Changes appear ready for integration, providing complete support for maintenance-free repository setup when needed.