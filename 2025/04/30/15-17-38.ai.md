Bugfix investigation with memory corruption confirmation in `git rebase --rebase-merges`. Kristoffer Haugsbakk follows up on Jeff King's suggestion to use address and undefined behavior sanitizers, which successfully reproduces and diagnoses the intermittent reflog corruption issue during complex merge rebases.

The sanitizer output shows a clear heap-use-after-free in `run_git_commit()` at sequencer.c:1148, where `ctx->reflog_message` points to freed memory. The corruption occurs when a strbuf holding the reflog message is reallocated during `reflog_message()` calls in the sequencer's reset path, while `ctx->reflog_message` retains a stale pointer. This confirms Jeff King's hypothesis about the use-after-free scenario.

The backtrace shows the problematic sequence: `sequencer_continue` -> `pick_commits` -> `do_reset` (which reallocates the strbuf via `reflog_message`) -> later `do_merge` -> `run_git_commit` (which uses the freed pointer). The issue manifests after processing multiple merge conflicts (19 in this case), explaining the intermittent reproduction.

Key technical details remain consistent with prior thread discussion: the problem centers around `sequencer.c`'s handling of reflog messages during merge operations, specifically the interaction between `reflog_message()`, `do_reset()`, and `run_git_commit()`. The sanitizer output provides concrete smoking gun for the memory management issue that was previously only suspected.

This email moves the investigation from hypothesis to confirmed root cause, showing the value of sanitizers in diagnosing tricky memory issues. The next step would be to fix the strbuf pointer management in the sequencer's reflog handling code paths.