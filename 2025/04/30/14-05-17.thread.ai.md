Here is the dense AI thread summary:

Bug report regarding incorrect branch name in `git stash` messages when using `-m` option with submodules. On Git for Windows 2.49.0, `git stash push -m` prepends "On master:" to the message regardless of current branch, while basic `git stash` correctly shows "WIP on <current branch>". Issue only occurs when submodules are present and configured to track "master", suggesting submodule configuration interferes with main repository's stash message generation. Behavior difference between message-handling paths in stash command needs investigation. Reporter notes this causes confusing output in `git stash list` since branch information is incorrect. Bug appears reproducible but would benefit from more details about submodule configurations and testing on newer Git versions.