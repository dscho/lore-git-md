Feature patch completing the `--no-maintenance` series for Scalar commands. This second patch extends the new flag to `scalar clone`, matching the functionality already added to `scalar register` in the first patch. The change allows automation scenarios (like VM image preparation) to skip background maintenance setup that could interfere with subsequent steps.

The implementation follows the same pattern as the register command: a new boolean flag defaults to true for normal user scenarios, with `--no-maintenance` disabling the maintenance scheduler setup. Documentation updates clearly explain the option, and a new test case verifies the flag prevents maintenance-related warnings when the scheduler is unavailable. The changes are minimal and focused - just adding the flag handling while reusing the existing `register_dir()` infrastructure.

This appears to be a well-executed conclusion to the two-part series, maintaining consistency between related commands while addressing the automation use case. The patch preserves backward compatibility and follows Scalar project conventions, with thorough documentation and testing matching the quality seen in part 1.