This is a follow-up review in the `git mv` bugfix thread. Junio Hamano notes two small improvements that were missed in the initial review of Patrick Steinhardt's patches fixing assertion failures during parent-child directory moves.

The review highlights two test improvements: suggesting that the tests should verify the index state remains unchanged after the failed move operation, and noting that the test cases should cover the `git mv a a/b` scenario (though this case is likely already handled by existing checks).

Junio approves the core changes (the `MOVE_VIA_PARENT_DIR` flag usage and error message) with "OK" markers, and praises the more descriptive test titles. The feedback is minor and constructive, indicating the series is nearly ready with just these small test enhancements needed. This continues the positive trajectory of the thread where both patches have already received conceptual approval.