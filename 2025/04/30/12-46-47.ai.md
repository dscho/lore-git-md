This is a refinement of Patrick Steinhardt's ongoing patch series to fix parent/child directory move conflicts in `git mv`, addressing specific edge cases identified in Junio Hamano's review.

The email continues the technical discussion about handling directory moves when both a directory and its child are specified as arguments to `git mv`. Patrick acknowledges two key problems: the missing `strbuf_reset()` Junio spotted, and the need for a new `MOVE_VIA_PARENT_DIR` flag to properly handle cases where moving a directory implicitly moves its children. He also notes the asymmetric behavior when argument order is reversed (`a/ a/b` vs `a/b a/`) and questions whether the `-k` flag should bypass parent/child validation at all.

The discussion reveals deeper issues with `git mv`'s error handling semantics, particularly around the `-k` flag's current behavior of continuing after nonsensical operations. Patrick suggests this may warrant tightening, though he maintains the current patch's behavior for compatibility. The email concludes with Patrick announcing he's sent a polished version of the patch series incorporating these fixes.

Technical details remain focused on the hashmap-based solution for tracking moved directories, now enhanced to handle submodule paths and directory move edge cases. The thread shows productive collaboration between Patrick and Junio, with the implementation nearing readiness but still working through some semantic questions about error handling behavior.