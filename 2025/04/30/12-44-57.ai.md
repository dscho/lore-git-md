This is a bugfix patch addressing an assertion failure in `git mv` when moving both a parent and child simultaneously. The patch is part of a 2-commit series from Patrick Steinhardt that replaces an assertion with proper error handling for invalid parent-child moves.

The change introduces a new `MOVE_VIA_PARENT_DIR` flag and a hashmap` to track moved directories, then explicitly checks for and rejects attempts to move both a path and its parent directory in the same command. The error message clearly indicates the conflicting paths. The patch converts a previously failing test into two positive test cases that check both file and directory cases, verifying the new error handling works as expected.

Key technical details for future reference:
- Adds `pathmap_entry` struct and `pathmap_cmp` function for tracking moved directories
- Introduces `MOVE_VIA_PARENT_DIR` flag (bit 5) in `enum update_mode`
- Uses `hashmap` to detect parent-child move conflicts
- Error message format: "cannot move both 'X' and its parent directory 'Y'"
- Tests cover both file-in-directory and nested-directory cases
- Changes affect `builtin/mv.c` (61 lines) and `t/t7001-mv.sh` (24 lines)

The patch appears well-contained with thorough test coverage, cleanly addressing a specific edge case that previously caused CI failures on Windows. The solution is straightforward and unlikely to introduce regressions.