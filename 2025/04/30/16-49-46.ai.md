Documentation patch discussing style consistency for option negation in `git-clone` help text.

Kristoffer Haugsbakk responds to Lino Haupt's proposal to split the `--[no-]single-branch` documentation into separate paragraphs. While acknowledging the discoverability benefits, Haugsbakk raises a concern about consistency with Git's established documentation style, pointing out that many other options use the `--[no-]` prefix pattern. 

The response cites `gitcli(7)` which explicitly documents this negation convention as part of Git's "Enhanced Option Parser" system. This suggests the current combined `--[no-]` form may be intentional rather than an oversight. The email includes a `git grep` command showing widespread use of this pattern across Git's documentation.

This introduces a new consideration to the thread: whether improving discoverability for this specific option is worth deviating from the project's established documentation conventions. The response doesn't outright reject the original proposal but provides important context about Git's documentation norms that wasn't present in the initial patch.

Key technical details:
- References `gitcli(7)` documentation on option negation
- Provides grep command showing prevalence of `--[no-]` pattern
- Mentions consistency with other commands like `git branch --[no-]track`

The thread now has two clear positions: Lino's UX-focused argument for better discoverability versus Kristoffer's consistency-focused perspective on documentation style. This likely requires input from documentation maintainers to determine which principle should take precedence.