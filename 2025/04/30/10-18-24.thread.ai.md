Git thread summary (AI mode):

Subject: Behavior of `git commit <file>` including unstaged changes
Thread type: User question about staging behavior
Core issue: User observes that `git commit file.txt` includes unstaged changes for the file, unlike `git commit` which only commits staged changes. Asks if this can be changed to respect staging area.

Current understanding: This is documented behavior where `git commit <path>` implies `-a` for specified paths (per Git man pages). Not a bug but potentially confusing UX issue.

Technical details:
- `git commit` (no args): only commits staged changes
- `git commit -a`: commits all tracked file changes (staged + unstaged)
- `git commit <path>`: currently behaves like `-a` for specified paths

Open questions:
- Should this behavior be changed to respect staging area?
- If keeping current behavior, should documentation be improved?
- Are there workarounds to achieve staging-respecting behavior with pathspecs?

Related commands: `git add`, `git commit -p`, `git stash -k` for partial commits

Note: This is a user question thread, not a patch submission. The discussion may lead to documentation updates or potential behavior changes if consensus emerges that current behavior is unintuitive.