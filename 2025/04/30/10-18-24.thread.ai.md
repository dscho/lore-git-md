Git thread summary (AI mode):

Subject:** Behavior of `git commit <file>` including unstaged changes
Thread type:** User question about staging behavior evolving into potential feature discussion
Core issue:** Discrepancy between user expectations (that `git commit <file>` should respect staging area like no-args `git commit`) and current behavior (which commits both staged and unstaged changes for specified paths via implied `--only` mode).

Current understanding:** 
- Current behavior is documented but potentially unintuitive
- `--only` mode (current default for pathspec commits) was introduced historically as safer than original `--include` behavior
- No existing way to make path-limited commits respect staging area only

Technical details:
- Behavior matrix:
  - `git commit`: only staged changes
  - `git commit -a`: all tracked changes (staged + unstaged)
  - `git commit <path>`: acts like `-a` for specified paths (via `--only`)
- Junio proposes hypothetical `--cached` mode analog to `git diff --cached` that would:
  - Only commit staged changes for specified paths
  - Require new implementation rather than behavior change

Open questions:
- Should a new `--cached` mode be implemented for staging-respecting path commits?
- If implemented, should it become the new default or remain opt-in?
- Would documentation improvements alone suffice given backward compatibility concerns?
- Are there existing workarounds (`git commit -p`, `git stash -k`) that make this unnecessary?

Participant positions:
- Junio C Hamano: confirms current behavior is intentional but open to new mode
- Original user: finds current behavior surprising compared to no-args commit

Related commands/context:
- `git diff --cached` as model for proposed behavior
- Historical `--include` vs `--only` modes
- Partial commit workflows (`commit -p`, `stash -k`)

Potential outcomes:
- Documentation patch to clarify current behavior
- Feature proposal for `--cached` mode
- Status quo maintained with workarounds