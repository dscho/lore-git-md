Here's the updated human-readable thread summary:

This thread explores the behavior of `git commit <file>` which currently includes unstaged changes for the specified file, unlike the no-argument `git commit` that only commits staged changes. The original poster found this behavior unintuitive. 

Junio Hamano confirmed this is the documented `--only` mode behavior, which was introduced historically as a safer alternative to the original `--include` approach. He explained there's currently no way to make path-limited commits respect only staged changes, but suggested a hypothetical `--cached` mode (similar to `git diff --cached`) could be implemented to provide this functionality. 

The discussion reveals a user experience gap where path-specific commits don't match the staging-respecting behavior users might anticipate. While the current behavior won't change for compatibility reasons, the thread leaves open the possibility of either improved documentation to clarify the behavior or a future feature addition to support staging-aware path-limited commits. No immediate changes are planned, but the conversation has surfaced a potential area for enhancement.