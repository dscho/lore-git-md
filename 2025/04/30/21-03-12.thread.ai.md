Here is the updated dense AI thread summary incorporating the new response:

Thread identity: Bug report about unexpected behavior in `git restore -m` resurrecting committed merge conflict markers, originating from an unnamed reporter. The thread type is a bug report with reproduction steps and maintainer response.

Core problem: When merge conflicts (including conflict markers) are committed to the repository, running `git restore -m .` can resurrect those conflict markers in the working tree despite previous resolution. This contradicts user expectations that `restore -m` should only affect uncommitted changes.

Current status: Junio Hamano has responded that this is intentional behavior dating back to the original 2009 conflict resolution tracking implementation (commit cfc5789a). The design preserves conflict state information post-commit to allow reverting broken resolutions. No code changes are proposed - users should use `git reset --hard` to clear state or avoid `restore -m` if undesired.

Key technical details: The behavior stems from Git's persistent tracking of conflict states across commits. While `git merge`/`reset` clear conflict state, the `restore -m` command can resurrect it from the index. This provides recovery capability but may surprise users. The interaction between committed conflicts and restore's merge option was an explicit design choice rather than a bug.

Open questions: None remaining - the maintainer has clarified this is working as designed. The thread serves as documentation of this edge case behavior for future reference.

Participant positions:
- Original reporter provided clear reproduction case (Git 2.34.1 on Linux)
- Junio Hamano confirmed intentional behavior with historical context and suggested workarounds (`reset --hard` or avoiding `restore -m`)

Related work: Connects to Git's core conflict resolution tracking system from cfc5789a (2009). The behavior is consistent with but not explicitly documented in the `git-restore` man page.

The thread is now complete with the maintainer's explanation closing the question. The summary preserves all technical details and rationale while noting the resolution.