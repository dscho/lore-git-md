This thread began with Patrick Steinhardt's fix for `git mv` assertion failures when moving parent and child directories, which has been merged to `next`. The solution introduced hashmap-based tracking and proper error handling, with thorough test coverage. Post-merge discussion identified potential improvements to verify index state preservation in failed moves, though timing makes this impractical to add now.

The conversation then broadened into a project-wide discussion about assertion handling, with Patrick proposing to systematically replace `assert()` calls with a new `BUG_UNLESS()` macro. Junio Hamano maintains that CI should surface assertion failures rather than suppress them, while Johannes Schindelin has now weighed in with Windows-specific considerations. He argues his assert-suppression patch remains necessary for Visual Studio compatibility, as Windows builds traditionally use release mode to avoid modal dialogs from assertions - a behavior that affects both Git and its dependencies. Johannes presents this as a choice between accepting his patch to properly configure release mode or dropping Windows/Meson support entirely.

The thread now has two active tracks: the resolved `git mv` bugfix (with lessons for future test writing), and the ongoing assertion policy discussion that must reconcile platform differences. Junio is soliciting additional input before making final decisions that will affect both CI behavior and Windows development workflows.