This is a feature proposal for interactive patch commands (`git add -p` and related) that builds on prior discussion about diff context handling. Junio Hamano responds positively to Leon Michalak's proposal to have these commands inherit `diff.context` and `diff.interhunkcontext` configurations by default while still allowing command-line overrides.

Hamano notes the different usage patterns between regular diffs and interactive patch sessions, where users might want wider context during hunk selection to maintain orientation. He agrees the proposed approach - inheriting configs but allowing per-command overrides - would satisfy these needs.

The email reinforces the emerging consensus from the thread that this hybrid model balances porcelain consistency (through config inheritance) with flexibility (via command-line flags). Hamano's response suggests the technical direction is now settled, with remaining work focused on implementation details. The change would affect all interactive patch commands (`add -p`, `checkout -p`, `reset -p`, `stash -p`) through their shared infrastructure.