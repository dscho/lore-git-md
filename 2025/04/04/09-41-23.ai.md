This is a technical discussion about implementing change IDs in Git, focusing on behavior during history rewrites and revision specification.

Patrick Steinhardt clarifies his earlier point about change IDs helping with history rewrites, explaining that the main benefit is avoiding the need to track changing object IDs during rebases. He notes that with change IDs, users would only need to remember one constant identifier per commit rather than tracking new OIDs after each rewrite.

The email continues the discussion of which Git commands would need change ID support, with Elijah Newren adding `git-commit --amend`, `git-fast-export`, and `git-fast-import` to the list. Patrick confirms some tools already handle change ID preservation correctly through existing `read_commit_extra_headers()` infrastructure, though notes potential bugs in the sequencer code.

The conversation then turns to revision specification syntax, where Patrick suggests something similar to the existing `^{/<text>}` syntax but specifically for change IDs. He expresses preference for Jujutsu's reverse hex format while acknowledging the need for disambiguation from refnames, likely requiring some prefix.

Key technical points include:
- Change ID persistence mechanisms during rewrites
- Existing infrastructure that already handles extra headers
- Potential sequencer bug in header handling
- Revision syntax design considerations
- Namespace collision avoidance with refnames

The discussion remains focused on concrete implementation details while maintaining awareness of cross-tool compatibility concerns raised earlier in the thread. Patrick's responses help clarify the practical workflow benefits while moving toward specific syntax and behavior proposals.