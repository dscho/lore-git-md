Final review approval for `git blame` porcelain output fix. This is the concluding email in a long thread (27 iterations) that implemented proper marking of ignored/unblamable lines in porcelain output while maintaining backward compatibility.

The patch has undergone extensive review and testing, with multiple rounds of:
- Output formatting fixes (newline handling)
- Test improvements (portability, robustness)
- Documentation updates
- Code organization refinements

Key technical points in this final version:
- Uses `emit_porcelain_per_line_details()` to add "ignored"/"unblamable" metadata lines
- Maintains strict SHA-1 line format compatibility
- Fully POSIX-compliant test scripts using heredoc `sed` patterns
- Proper newline handling via `fputs()` instead of `puts()`
- Comprehensive test coverage comparing full output via `test_cmp`

Phillip Wood gives final approval, noting the test portability improvements using heredoc`sed` scripts instead of embedded newlines. The implementation addresses all previously identified issues around:
- Output formatting
- Backward compatibility
- Test reliability
- Cross-platform portability

This concludes the thread with the feature now merged, providing proper ignored/unblamable line marking in porcelain output while maintaining strict compatibility with existing parsers.