This is a technical refinement of Zheng Yuting's GSoC proposal for consolidating ref-related functionality into `git-refs`, now focusing on implementation details after mentor Patrick Steinhardt's architectural guidance favored the unified approach.

The email presents a concrete implementation plan for the `git-refs list` subcommand, adopting the unified model that combines filtering, sorting, and formatting capabilities. Key decisions include using `git-show-ref`'s lightweight `<oid> <ref>` format as default (avoiding ODB access) and categorizing options into three clear groups: filtering (inheriting from both `git-for-each-ref` and `git-show-ref`), sorting (from `git-for-each-ref`), and formatting (from both commands). The implementation strategy involves first building a `git-refs show` subcommand to replace `git-show-ref --verify`, then layering the more complex `list` functionality on top.

Open questions remain about legacy format options (`--tcl`, `--shell`, `--perl`) - whether to retain them or deprecate in favor of modern alternatives. The proposal shows careful consideration of Patrick's performance concerns and Karthik's earlier analysis of format tradeoffs, with the unified approach now clearly established as the path forward. The email invites feedback on option structure and legacy feature handling, indicating the next phase will focus on refining these details while maintaining the constraint-conscious approach (backward compatibility, `refs.c`-only changes) emphasized throughout the thread.