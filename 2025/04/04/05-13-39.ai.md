This is a brief follow-up in the bugfix thread addressing `--left-only`/`--right-only` behavior with unrelated histories. Matt Hunter confirms understanding of Phillip Wood's explanation about test repository state management in Git's test suite - specifically that tests within a single file share repository state and should rely on setup tags rather than raw commit counts. 

The exchange doesn't introduce new technical changes but solidifies the testing approach for the patch. Matt indicates he'll maintain the v2 test implementation which uses tags to establish known states, having now understood why direct commit counting was problematic. This concludes the educational sub-thread about test suite conventions that emerged during the review process.

The core fix (setting `revs->limited = 1` in revision.c) remains unchanged from previous iterations, with the test evolution now fully accounting for both cross-platform portability concerns and proper test repository state management. The thread shows the patch reaching final consensus on all aspects of implementation and verification.