This is a continuation of the discussion about symlink handling in Git, particularly focusing on Windows-specific considerations. The thread explores whether Git should automatically handle symlink conversions when `core.symlinks` configuration changes.

brian m. carlson responds to Andrej Zhilenkov's points about Windows symlink usage, agreeing that symlinks are disabled by default on Windows due to privilege requirements but suggesting Developer Mode is preferable for Git users. They push back on the idea of per-repo symlink configuration, arguing that symlink capability is typically an all-or-nothing system-level feature rather than something that should vary by repository.

The response provides additional context about symlink edge cases on Unix systems, including FAT filesystem limitations and a Linux distro's failed security attempt, while maintaining that there's rarely a good reason to globally disable symlinks on Unix. The exchange continues to explore the practical realities of symlink usage while maintaining the core position that automatic conversion between symlink and file representations isn't part of Git's design.

This technical discussion remains relevant for Windows users and maintainers considering symlink behavior, though it doesn't propose any immediate code changes. The thread appears to be moving toward either documentation improvements or acceptance of the current behavior with better user guidance.