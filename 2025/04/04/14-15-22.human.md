This is a response in the ongoing discussion about adding a `--no-hooks` global option to disable Git hooks. Phillip Wood raises several concerns about the proposal:

Wood questions the need for this feature, noting that `git commit --no-verify` already exists for bypassing pre-commit hooks specifically. He suggests that slow hooks should be fixed rather than bypassed, mentioning parallel processing as a potential solution. He also challenges the claim that setting `core.hooksPath=/dev/null` is unsafe, calling it a standard practice.

The response maintains a skeptical tone about the proposal's value, arguing that performance issues should be addressed at the hook level rather than providing a global bypass mechanism. Wood also expresses doubts about the implementation's viability despite acknowledging its technical soundness.

This email continues the thread's exploration of whether the feature is necessary given existing alternatives, and whether it might encourage poor practices by making it too easy to bypass important hooks. The discussion remains focused on balancing expert workflow needs against potential misuse and security implications.