This is a response in the ongoing discussion about adding a `--no-hooks` global option. Phillip Wood raises several concerns about the proposal while engaging with the technical details.

Wood questions the need for a new global option when `git commit --no-verify` already exists for bypassing pre-commit hooks, suggesting slow hooks should be fixed through parallel processing rather than making them easier to skip. He also challenges Stolee's characterization of `core.hooksPath=/dev/null` as unsafe, noting it's a common practice.

The response expresses skepticism about the feature's value proposition, arguing that performance issues in hooks should be addressed directly rather than worked around. Wood acknowledges the technical soundness of the implementation (particularly the static int optimization in `run_hooks_opt()`) but remains unconvinced the feature is beneficial overall.

Key technical details referenced:
- Existing `--no-verify` option for commit hooks
- Current practice of using `core.hooksPath=/dev/null` to disable hooks
- Implementation approach using `GIT_HOOKS` environment variable and static int caching
- Pre-commit hooks as the primary use case discussed

This moves the discussion forward by:
1. Challenging the problem statement (are slow hooks better fixed than bypassed?)
2. Questioning the safety claims about current workarounds
3. Maintaining technical engagement while expressing philosophical disagreement
4. Keeping focus on the pre-commit hook case that started the discussion

The thread now has clear opposing viewpoints on whether this feature solves a real problem in the right way, with Wood advocating for fixing hook performance rather than adding bypass mechanisms.