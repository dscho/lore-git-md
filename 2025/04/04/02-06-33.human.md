This is a discussion about change ID semantics in Git workflows, specifically around backporting and branch management. Elijah Newren responds to Martin von Zweigbergk's assertion that duplicate change IDs are uncommon, sharing his experience from Gerrit-based projects where multiple commits with the same change ID across different branches is actually quite common pattern for LTS release maintenance.

Newren provides concrete examples from real-world workflows where important fixes are propagated across multiple long-term support branches, with teams verifying backports by checking for the same change ID on different branches. He questions the practicality of commands like `git switch <change-id>` in such environments, since the same change ID may exist on multiple branches simultaneously.

The email highlights a key workflow divergence in the ongoing change ID standardization discussion: Martin's view assumes change IDs are unique within a repository's active branches, while Newren's experience shows many projects intentionally maintain identical change IDs across branches for backport tracking. This raises important questions about whether standardization should accommodate both models or choose one as canonical.

The response is well-reasoned and grounded in practical experience, adding weight to the discussion by showing how real projects actually use change IDs in maintenance workflows. It suggests the need for the standardization effort to consider more complex version control scenarios beyond simple linear development.