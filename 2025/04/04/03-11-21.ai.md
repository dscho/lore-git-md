This is a discussion about change ID semantics in version control workflows, specifically addressing the tension between unique identifiers and branch-aware tracking.

Nico Williams responds to Martin von Zweigbergk's concerns about duplicate change IDs by presenting concrete use cases where multiple commits with the same change ID are not just possible but desirable. The key points are:

1) **Backport workflows**: Nico emphasizes that change IDs are particularly valuable for identifying bug fixes across multiple branches, with security fix backports in projects like OpenSSL and PostgreSQL given as real-world examples. This directly contradicts Martin's assumption that duplicate change IDs would be rare.

2) **Alternative implementation branches**: Nico describes a development pattern where multiple experimental branches might contain commits with the same change ID while exploring different approaches to implementing a feature.

3) **OS release maintenance**: At $WORK, Nico maintains Debian-style packages with release-specific branches where the same change ID appears across different OS version branches, both for patched upstream packages and locally developed software.

The email makes a strong technical argument against enforcing change ID uniqueness, highlighting that:
- `git switch $change_id` would be impractical due to potential ambiguity
- Git lacks Fossil's indexing capability for efficient cross-branch change ID searches
- Cherry-pick operations inherently create scenarios where duplicate change IDs are useful

Nico's position represents the "change IDs as lineage markers" perspective in the ongoing debate, providing concrete workflow examples that challenge the "change IDs as unique identifiers" model. The response adds weight to the Gerrit-style branch-aware tracking approach by demonstrating its practical utility in maintenance scenarios that Martin's initial position didn't account for.