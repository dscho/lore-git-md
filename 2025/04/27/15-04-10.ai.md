Performance optimization follow-up discussing Niels Glodny's patch to modify xdiff`--minimal` behavior. Phillip Wood agrees with the premise that `--minimal` should produce truly minimal diffs, even if it sometimes makes them less readable, but suggests several code improvements:

1. The test script could use existing test helpers like `test_write_lines` and `test_grep` for better robustness and debugging output.
2. The `need_min` parameter could be derived from `cf->flags` rather than being passed through multiple functions.
3. Confirms the technical approach of forcing `dis1[i]` to 1 when minimal diffs are requested, preventing premature marking of lines as changed.

The discussion remains focused on implementation details rather than the fundamental approach, with Phillip providing constructive feedback to simplify the changes while maintaining the optimization's goal of producing shorter diffs in ~1.3% of cases when `--minimal` is explicitly requested. No performance numbers are yet provided for the change's impact.