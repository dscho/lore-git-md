This is part 7 of a 9-patch series converting reftable tests to the Clar framework, specifically targeting the readwrite test file. The patch follows the established pattern seen in previous conversions in this series, mechanically updating the test infrastructure while preserving test coverage.

The conversion replaces the old `t-reftable-readwrite.c` with a new `u-reftable-readwrite.c` that uses Clar assertions (`cl_assert*`) instead of the previous custom test framework. The patch shows the typical changes: updating Makefile and meson.build to point to the new file, replacing test macros with Clar equivalents, and restructuring the test cases into Clar's format. The diff shows a net reduction of 115 lines (985 -> 870) while maintaining all the same test coverage.

Key technical details include:
- Tests reftable's read/write operations for both ref and log records
- Verifies buffer handling, compression, and error cases
- Covers edge cases like empty tables, key ordering, and corruption scenarios
- Maintains all existing test patterns (setup -> operations -> verification)
- Updates build system references in both Makefile and meson.build

The patch continues the series' consistent approach to test modernization, with no functional changes to the reftable implementation itself. Like previous patches, this appears to be a straightforward mechanical conversion that follows the established pattern precisely.