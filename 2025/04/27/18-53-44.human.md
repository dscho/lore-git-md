This is the second patch in a 9-part series converting reftable tests to the Clar framework. It focuses on the foundational changes from patch 1 by migrating the reftable basics test file.

The patch converts `t-reftable-basics.c` to `u-reftable-basics.c`, maintaining all existing test coverage while updating the Clar-style assertions and test organization. Key changes include replacing custom `check_*` macros with Clar's `cl_assert*` equivalents and breaking the single test function into multiple focused test cases (like `test_reftable_basics__binsearch`). The diff shows 195 lines added and 219 removed, with the changes being mostly mechanical transformations rather than substantive test modifications.

This appears to be a straightforward continuation of the test conversion effort, following the same pattern established in prior Clar migrations. The patch maintains all the original test cases covering reftable fundamentals like binary search, string array handling, buffer operations, and endian conversions. Build system updates (Makefile and meson.build) ensure the new test file is properly integrated.