This is a workflow question about Git's bisect functionality. Victor Engmark wants to bisect using a test script that lives in a different branch than the one being bisected, while keeping certain test files unchanged throughout the bisect process. His current solution involves manually extracting files from the test branch during each bisect step, which he finds cumbersome.

The key technical elements are:
- The test script (`test.bash`) needs to access files from `my-test-branch` while bisecting another branch
- Current approach uses `git show` to extract files and `git checkout .` to clean up
- Main pain point is maintaining file consistency during bisection

This appears to be a legitimate workflow challenge that could affect developers trying to bisect with custom test suites. The question is well-posed with a concrete example, though it might benefit from more context about why the test files need to remain static (e.g., are they test infrastructure or expected outputs?).

Potential discussion points likely to come up in replies:
- Alternative approaches using `git worktree` or temporary branches
- Whether Git should provide built-in support for this use case
- Possible edge cases around file paths and permissions
- The relationship between the test files and the code being bisected