This is a documentation patch reporting a potential typographic error in Git's tutorial documentation. 

The contributor, N.Antonetsis, points out what appears to be an incorrect shell command in gittutorial-2. The current version shows `$ echo 'hello world!' >file.txt` when the context suggests the text should be appended (using `>>`) rather than overwriting the file. 

The report is concise and appears technically correct - a single `>` does indeed replace file contents while `>>` appends, and the tutorial's surrounding text implies the latter behavior is intended. This would be a straightforward fix for the documentation maintainers to verify and address if confirmed. 

No patch is provided - this is simply an issue report about the online documentation. The observation seems valid at first glance, though someone would need to check the tutorial's flow to confirm the intended behavior matches the suggested correction.