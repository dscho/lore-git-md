Performance optimization for xdiff's `--minimal` mode. This is a reply in an ongoing thread about modifying the `cleanup_records` heuristic to produce shorter diffs when `--minimal` is explicitly requested.

Phillip Wood agrees with the premise of Niels Glodny's original patch, which aims to make `--minimal` diffs truly minimal by skipping the heuristic that marks lines as changed when surrounded by many changes. Phillip notes that while this might make some diffs less readable, it fulfills the promise of `--minimal` to produce the shortest possible diff.

The reply includes several constructive suggestions to simplify the implementation: using existing flags in `xdlclassifier_t` rather than adding a new parameter, and improving the new test case with Git's test helper functions. Phillip also requests performance numbers to quantify the impact of the change, though they acknowledge the original claim of negligible performance impact.

The technical discussion remains focused on the narrow scope of the change - only affecting `--minimal` output where shortest-possible diffs are already the goal. The patch appears well-reasoned and the feedback suggests it's moving in the right direction with some minor implementation refinements needed.