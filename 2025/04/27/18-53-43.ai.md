Test infrastructure modernization: First patch in a 9-part series converting reftable tests to Clar framework.

This initial patch adapts the reftable test helper functions in `lib-reftable.{c,h}` to work with the Clar testing framework. The changes are mechanical but foundational for the subsequent test file conversions. Key modifications include:

1. Renaming helper functions from `t_reftable_*` to `cl_reftable_*` to match Clar conventions
2. Replacing homegrown test assertions (`check()`, `check_int()`) with Clar equivalents (`cl_assert()`, `cl_assert_equal_i()`)
3. Adjusting build system files (Makefile and meson.build) to properly include the reftable helpers in Clar test builds
4. Adding explicit type casts where needed (notably for block size comparisons)

The patch maintains all existing test functionality while updating the implementation to Clar standards. Build system changes show the helpers being moved from the old unit test infrastructure to the new Clar-based setup. The changes are consistent with prior Clar conversions in Git's test suite.

This is a straightforward infrastructure change that enables the actual test file conversions in subsequent patches. The patch follows established patterns from previous Clar migrations and appears to be correctly implemented based on the diff.