This thread reports a potential documentation error in Git's tutorial. N.Antonetsis noticed that `gittutorial-2` shows a shell redirection using `>` (which overwrites) when the surrounding explanation suggests the operation should append to the file (requiring `>>`). The discrepancy appears in the first code example where `file.txt` is modified. 

The issue seems clear-cut - the shell behavior doesn't match the tutorial's description. Unless there's a pedagogical reason for showing the overwrite case (which seems unlikely given the context), this would be a simple but meaningful fix to make the example match its explanation. The thread is straightforward with just this single report so far; it would benefit from confirmation from documentation regulars like Jean-Noël Avila or Eric Sunshine.