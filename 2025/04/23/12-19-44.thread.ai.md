Thread identity: `git send-email` patch series (v6) focusing exclusively on Outlook Message-ID handling, authored by Aditya Garg. Originally part of a larger OAuth2/Outlook compatibility effort, now reduced to single-patch scope.

Core problem/goal: Fix thread tracking when using `git send-email` with Outlook servers by properly handling their nonstandard Message-ID behavior. Prevents thread breaks caused by Outlook rewriting Message-IDs with its own format while maintaining backward compatibility with standard SMTP servers.

Current status: Sixth iteration, now a single-patch series after consensus to:
1. Use upstreamed Authen::SASL OAuth2 implementation (perl-authen-sasl PR #19)
2. Rely on Git's credential helper system instead of `smtp-passeval`
Patch implements Outlook Message-ID retrieval from SMTP response (21-line addition to `git-send-email.perl`).

Key technical details:
- Detects Outlook servers via hostname (`smtp.office365.com`/`smtp-mail.outlook.com`)
- Parses SMTP "250 2.0.0 OK" response for Outlook-assigned Message-ID
- Updates local message with server-provided ID
- Maintains backward compatibility
- Builds on v5 improvements to commit message/documentation
- OAuth2 implementation now properly upstreamed to perl-authen-sasl (PR #19) with:
  - Correct SASL protocol sequence (base64 in `client_start`, dummy `client_step`)
  - XOAUTH2 string format: "user=$username\001auth=Bearer $token\001\001"
  - TLS enforcement
  - Julian Swagemakers' critical fixes included with proper copyright attribution

Open questions/loose ends:
- Enterprise Outlook detection needs configurable approach (Greg Kroah-Hartman's suggestion)
- Documentation improvements for credential helper OAuth2 usage (per Junio Hamano)
- Awaiting perl-authen-sasl maintainer feedback on PR #19)
- Final confirmation of Julian Swagemakers' copyright attribution in upstream submission

Participant positions: Aditya Garg driving focused Message-ID solution. Julian Swagemakers' technical contributions properly acknowledged in perl-authen-sasl PR. Greg Kroah-Hartman's enterprise detection feedback pending. Junio Hamano maintains preference for credential helper approach. Brian M. Carlson's earlier concerns addressed. M Hickford confirms Outlook functionality.

Related work: Connects to Git's email infrastructure and credential helper system. Builds on prior OAuth2 work now upstreamed to perl-authen-sasl. Enterprise Outlook deployments remain consideration. Documentation may reference msmtp's approach. Historical context includes git-credential-manager limitations.