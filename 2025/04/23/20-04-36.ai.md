This is a CI/build system update in the final stages of the Meson header checking implementation series. Junio C Hamano reports test failures when running the CI pipeline with the new changes that require full Git checkouts rather than tarball extraction. The failures occur during dependency installation (exit code 8 from ci/install-dependencies.sh), suggesting the workflow changes may need adjustments for different CI environments.

The email indicates this is likely due to the series' switch from tarball-based to full Git checkouts in CI, which Junio notes could expose previously hidden test failures but considers this a positive long-term improvement. The immediate impact is visible in GitHub Actions runs, where multiple jobs are failing at the dependency installation step.

This appears to be the final verification step before merging the series having already received approvals from both Phillip Wood and Junio. The failures suggest some last-minute CI adjustments may be needed, though Junio frames this as expected fallout from a meaningful testing improvement rather than a flaw in the series itself. The thread context shows the implementation is otherwise complete with all substantive feedback incorporated.