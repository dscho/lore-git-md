This is a discussion about build system modernization, specifically the handling of qsort() verification in the transition to Meson. The email continues the debate between Patrick Steinhardt and Eli Schwartz about whether to maintain platform-specific knowledge about qsort() performance characteristics or rely on generic detection.

Patrick argues for a wait-and-see approach regarding qsort() performance issues, questioning whether historical concerns about O(n^2) behavior are still relevant today. He suggests treating this as a "canary" to identify outdated infrastructure that may no longer be needed, with the goal of eventually simplifying the build system if no problems surface. This contrasts with Eli's earlier position favoring explicit platform-specific handling based on historical config.mak.uname entries.

The discussion touches on core build system philosophy - whether to encode platform knowledge (Makefile approach) or prefer generic detection (Meson approach). Patrick acknowledges they could add platform exceptions later if needed, but prefers starting with simpler verification. This debate is part of the larger 6-patch series standardizing function availability checks in the Meson build system.

Key technical points mentioned include the difficulty of detecting qsort() performance characteristics at build time and the question of whether Git's internal qsort() fallback is still needed on modern platforms. The thread shows tension between maintaining historical compatibility and modernizing the build system, with Patrick advocating for periodically questioning and potentially removing legacy infrastructure.