This is a build system patch series (v5) that completes the 'hdr-check' functionality in Meson, matching the Makefile's capability. Phillip Wood gives final approval with minor comments, indicating the series is ready for merging.

The series introduces header checking to Meson builds while also adding a better-named 'check-headers' alias that will eventually replace 'hdr-check'. Key improvements in v5 include fixing CI issues by ensuring Git is installed before repository checkout, and properly handling header checks in tarball distributions by verifying the .git directory exists.

The implementation is thorough, with careful attention to backward compatibility and cross-platform support. The thread history shows extensive review from multiple maintainers, with all technical concerns now addressed. The changes touch CI workflows, Makefile aliases, and Meson configuration files, demonstrating good test coverage across build systems.

This represents the culmination of several iterations to bring Meson's static analysis capabilities to parity with the Makefile build, with the maintainers now satisfied with the implementation.