This is patch 09/13 in Patrick Steinhardt's object store refactoring series, focusing on promisor object handling in the refs subsystem. The patch modifies `ref_resolves_to_object()` to stop fetching promisor objects when checking reference validity, instead treating missing objects as broken references by definition.

The change replaces `repo_has_object_file()` with `has_object()` using only the `HAS_OBJECT_RECHECK_PACKED` flag, explicitly excluding promisor object fetching (the `HAS_OBJECT_FETCH_PROMISOR` flag is omitted). This aligns with the series' broader goal of making promisor behavior explicit and reducing automatic fetches. The one-line change in refs.c maintains error reporting for invalid objects while removing the implicit fetch behavior.

Karthik Nayak's review note points out a typo in the commit message ("an" -> "and") but doesn't raise substantive concerns about the change itself. This continues the pattern seen in earlier patches where reviewers are accepting the promisor behavior changes while focusing on minor wording improvements. The patch appears consistent with the series' established direction and technical approach to promisor handling.