Build system patch series implementing header checking in Meson builds, now with CI workflow refinements. This is the fifth iteration of Karthik Nayak's work to bring Makefile's 'hdr-check' functionality to Meson builds, introducing several key improvements:

The series adds 'hdr-check' to Meson while also introducing 'check-headers' as a better-named alias (with plans to eventually deprecate 'hdr-check'). The implementation handles header verification through Meson custom targets with proper dependency chaining, including special handling for generated files and platform exclusions.

Key technical changes in v5:
1. Adds explicit Git installation in GitHub CI before repository checkout to prevent tarball extraction issues
2. Modifies header checking logic to expose 'headers_to_check' variable even without Git, allowing dependency checking based on array length
3. Adds verification for '.git' directory existence before populating headers_to_check
4. Maintains all previous refinements from v4 including variable renaming (third_party_sources -> third_party_excludes), typo fixes, and exclusion list cleanup

The series builds on the thread's technical consensus around:
- Using `command -v` for package manager detection (endorsed by Junio with historical POSIX context from Eli Schwartz)
- Skipping checks when Git isn't available rather than implementing `find` fallback (Patrick Steinhardt's position)
- Retaining package cache updates for reliable apt-get operation (Karthik's refinement)
- Centralized header definitions in root meson.build
- Files modified:
  - .github/workflows/main.yml (Git installation addition)
  - Makefile (alias addition)
  - ci/run-static-analysis.sh
  - contrib/coccinelle/meson.build (variable reorganization)
  - meson.build (core implementation)

The implementation now appears technically complete, having addressed all substantive feedback from the thread while maintaining focus on the core header checking functionality. The CI refinements demonstrate careful attention to real-world build scenarios, particularly the chicken-egg problem of needing Git installed before source checkout in CI environments.