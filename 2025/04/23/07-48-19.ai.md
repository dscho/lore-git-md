This is the ninth patch in Patrick Steinhardt's 13-part refactoring series cleaning up the object store subsystem, part of the ongoing `the_repository` removal effort. The patch modifies `ref_resolves_to_object()` in refs.c to stop fetching promisor objects when validating references, continuing the series' theme of removing unwanted object fetches. 

The change replaces `repo_has_object_file()` with `has_object()` using the `HAS_OBJECT_RECHECK_PACKED` flag, maintaining the same behavior of checking packed objects but avoiding promisor fetches. The rationale is that references pointing to missing objects are inherently broken and should be reported as such, rather than triggering a fetch attempt. This aligns with the series' broader goal of making object existence checks more precise and reducing automatic fetches.

The patch is a straightforward one-line change with minimal impact, building on the infrastructure changes from earlier in the series. It follows the same pattern seen in patch 8 (show-ref.c conversion) and demonstrates the systematic approach being taken across the codebase. The modification maintains error reporting for invalid references while removing the promisor fetch side effect.

This continues the thread's technical focus on header reorganization, API cleanup, and promisor object handling improvements. The change affects reference validation behavior in partial clone scenarios but doesn't introduce new functionality - it makes existing behavior more explicit by removing implicit fetches. The patch appears well-contained and follows the established pattern from previous conversions in the series.

Key technical details for future reference:
- Modified function: `ref_resolves_to_object()` in refs.c
- Flag used: `HAS_OBJECT_RECHECK_PACKED`
- Behavior change: Stops promisor fetches during reference validation
- Error reporting: Maintains existing "invalid object" message
- Related to: Partial clone functionality, reference validation
- Follows pattern from: Patch 8's show-ref.c conversion