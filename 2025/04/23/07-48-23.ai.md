Final patch in the object-store cleanup series removing deprecated functions. This is part of the ongoing refactoring effort to modernize Git's object storage subsystem and prepare for `the_repository` removal.

The patch removes `repo_has_object_file()` and `repo_has_object_file_with_flags()` after converting all their callers to use `has_object()` instead in previous patches. The commit message notes this completes the migration work started earlier in the series. The diff shows the straightforward removal of both function implementations from object-store.c and their declarations from object-store.h, along with the associated documentation comments that had marked them as deprecated.

This change follows the pattern established throughout the series of removing thin wrapper functions in favor of more direct API usage. The removal of these functions represents the culmination of work that touched 22 files across multiple subsystems including HTTP transport, ref handling, bulk-checkin, and list-objects filtering. The series has systematically modernized object existence checking while maintaining security properties around promisor objects and hash collision handling.

With this patch, the object-store.h header sees a net reduction of 31 lines across the series, continuing the header cleanup effort. The change is low-risk since all callers were converted first, though reviewers may want to verify no unconverted uses remain in external code or pending patch series. The removal of these deprecated functions marks an important milestone in the object storage modernization effort.