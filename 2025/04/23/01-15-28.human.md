This is a technical discussion about the semantics of Change-IDs in Git commit messages, specifically addressing concerns about mixing unrelated metadata into these identifiers. The email is part of an ongoing thread examining how Change-IDs differ from Git's SHA-based model and their role in patch tracking.

Remo Senekowitsch responds to Nico Williams' suggestion that ticket IDs could serve as Change-IDs, arguing forcefully against conflating these identifiers. The key point is that while free-form metadata in commit messages is harmless, putting unrelated information in structured headers like Change-IDs could break functionality. Senekowitsch uses the analogy of putting parent names in Git's parent header to illustrate how this violates the purpose of structured fields.

The exchange highlights a fundamental question in the thread: whether Change-IDs should remain pure tracking identifiers or could absorb other metadata roles. Senekowitsch's position is clear - metadata belongs in dedicated fields, not repurposed Change-IDs. This contributes to the thread's broader examination of how Change-IDs fit (or don't fit) with Git's existing object model and tracking needs.

The discussion remains technical but is moving toward clearer positions on identifier semantics, with Senekowitsch advocating strongly for keeping Change-IDs focused on their original tracking purpose rather than expanding them to carry other metadata.