This is a bugfix discussion in the ongoing thread about `git index-pack`'s handling of REF_DELTA chains. Junio Hamano is reviewing Derrick Stolee's test case that demonstrates a false cycle detection issue in `index-pack`.

The test shows a scenario where a thin pack contains a REF_DELTA chain A->B->C, with B and C already present in the client repository. While this is a valid packfile configuration, `index-pack` incorrectly flags it as a cycle when it revisits A while processing. Junio confirms the test's validity but suggests a minor readability improvement by moving the pack-deltas input directly after the heredoc marker rather than using a temporary file.

The discussion builds on the thread's earlier technical context about proper REF_DELTA handling, with Junio's response indicating the test case successfully demonstrates the edge case that needs fixing. This email doesn't introduce new technical solutions but confirms the test's value in exposing the bug, while offering a small stylistic suggestion for the test implementation.