This is a naming discussion in the ongoing `git-blame-tree` feature thread. Marc Branchaud argues that the proposed command name "blame-tree" is problematic, suggesting it either be folded into `git blame` as an option (like `--latest`) or given a completely new name that avoids the "blame" terminology. He worries that adding another blame-like command would contribute to Git's reputation for obscurity.

The email references Peff's original justification for the name (which drew parallels between the tree-level modification tracking and file-level blame), but Marc finds this unconvincing as a reason to keep the name separate. The discussion appears to be reopening a settled question from earlier in the RFC process, as the thread context shows the command's implementation and performance optimizations are otherwise quite mature.

This is primarily a UX/design discussion rather than a technical one, and while brief, it raises valid concerns about command discoverability and Git's overall interface consistency. The thread context suggests the naming question was previously considered closed, so this may spark renewed debate about whether to proceed with the standalone command or explore integration with `git blame`.