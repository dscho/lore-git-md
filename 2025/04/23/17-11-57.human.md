This is patch 09/13 in Patrick Steinhardt's series refactoring the object store subsystem, part of the ongoing `the_repository` removal effort. The patch modifies `ref_resolves_to_object()` to stop fetching promisor objects when checking reference validity, continuing the series' theme of making promisor object handling more explicit.

Karthik Nayak provides a quick review, catching a typo in the commit message ("an" should be "and"). The change itself is small but meaningful - it replaces the deprecated `repo_has_object_file()` wrapper with direct `has_object()` call while maintaining the same packed object recheck behavior but explicitly opting out of promisor fetching. This aligns with the series' goal of making object existence checks more precise about their requirements.

The patch follows logically from earlier changes in the series that established the pattern of using `has_object()` with explicit flags. It's a straightforward conversion that appears well-contained, touching only one line in refs.c while maintaining the same error reporting behavior for invalid references. The change makes sense given the series' broader context of cleaning up object store interactions.