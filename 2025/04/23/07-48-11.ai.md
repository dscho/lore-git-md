This is the first patch in a 13-part refactoring series cleaning up the object store subsystem, part of the larger `the_repository` removal effort. The patch moves the `struct packed_git` definition and related `pack_map_entry_cmp()` function from the generic "object-store.h" header to the more specific "packfile.h" header.

The change is motivated by better logical organization - since `struct packed_git` contains packfile-specific information, it belongs with other packfile-related declarations. The patch performs a straightforward relocation of code with no functional changes, just header adjustments. The diff shows the complete movement of the struct definition and comparison function between files, with corresponding updates to include directives in "pack-objects.h".

This appears to be a well-scoped, mechanical change that establishes better header organization for subsequent patches in the series. The commit message clearly explains the rationale and the change is easy to verify since it's purely a code movement. As the first patch in a larger series, it sets up the structural changes needed for more substantive refactoring to follow.