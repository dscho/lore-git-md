This is a follow-up in the Meson build system refactoring thread, specifically addressing Windows networking function verification. Patrick Steinhardt corrects his earlier mistaken claim about missing `hstrerror()` compatibility implementation, acknowledging that the file `compat/hstrerror.c` does exist with the expected `githstrerror()` function that gets remapped via `compat/posix.h`.

The exchange clarifies a technical point in the ongoing discussion about how to handle platform-specific networking function checks in the Meson build system. This particular sub-thread was focused on whether certain functions (`inet_ntop`, `inet_pton`, `hstrerror`) needed compatibility implementations on Windows, with Patrick initially believing `hstrerror` lacked one but now confirming its existence.

This email doesn't introduce new arguments but serves to correct a factual error in the previous technical discussion, removing one potential objection to the proposed approach for Windows networking functions. The thread's core debates about qsort() detection strategy and macro naming remain ongoing, with this being a minor clarification in the Windows networking verification sub-discussion.