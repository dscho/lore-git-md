This is a CI/build system discussion about caching external dependencies in Git's CI pipeline, continuing the thread about making dependency downloads more resilient.

The email is a brief response from Jialuo to Junio Hamano's earlier comment about the inefficiency of repeatedly downloading third-party software in CI jobs. Junio had noted it would be better to download dependencies once per platform type and cache the state for reuse across jobs on that platform.

Jialuo acknowledges this point and commits to investigating implementation approaches for such a caching mechanism. This follows previous work in the thread that made dependency downloads optional with proper error handling, including recent refinements to warning presentation and updating JGit's download source.

Key technical context for future reference:
- The discussion builds on prior changes to `ci/install-dependencies.sh`
- Current focus is optimizing the CI workflow by avoiding redundant downloads
- Potential approaches include GitHub Actions caching or platform-specific state dumps
- Implementation would need to integrate with the existing atomic download-cleanup pattern
- The thread has established consensus that caching is desirable
- Junio Hamano has expressed support for a once-per-platform model
- Jialuo is taking lead on investigating practical implementation

This represents progress on one of the thread's open questions (caching approach) while other aspects like download error handling have already been addressed in earlier patches. The discussion remains focused on CI infrastructure improvements separate from broader test modernization efforts.