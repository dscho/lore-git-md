Test patch preparing for bundle performance optimization. This is the first of two patches in a series addressing O(N^2) performance in bundle creation when handling duplicate refnames. Karthik Nayak adds comprehensive test coverage for the existing duplicate refname detection logic in `t/t6020-bundle-misc.sh`, including edge cases like combining `--all` with manual refs, exclusion syntax (`main^!`), and short/long refname forms. 

The tests reveal two current bugs: duplicate detection fails when using both `--all` and manual refs, and when mixing short and long refname forms. These will be fixed in the subsequent optimization patch. The test design is thorough, covering both correct behavior and known failure cases, which will help verify the coming optimization doesn't introduce regressions.

This appears to be a well-structured preparatory patch - the tests are clearly explained, cover relevant edge cases, and properly document current limitations that the series will address. The commit message effectively ties these tests to the performance work coming next.