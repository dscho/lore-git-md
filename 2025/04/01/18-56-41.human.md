This is a minor correction in Patrick Steinhardt's series to remove Perl dependencies from Git's test suite. Johannes Schindelin points out that the commit message for patch 14/20 is now slightly inaccurate - while the patch originally proposed using awk to replace Perl in the `sanitize_pgp()` helper, during review they settled on using sed instead. 

The change itself is straightforward: the PGP signature sanitization helper in t/lib-gpg.sh is rewritten from Perl to sed, and several tests in t6300-for-each-ref.sh that still need Perl functionality have PERL_TEST_HELPERS explicitly added to their prerequisites. This maintains the series' goal of reducing Perl dependencies while preserving test functionality.

The exchange shows the careful attention to detail in this well-reviewed series, where even small discrepancies in commit messages are being caught and corrected. The patch appears ready for inclusion, with Johannes' note serving more as an FYI about the commit message than requesting any code changes.