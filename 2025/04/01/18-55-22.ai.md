This is a patch in Patrick Steinhardt's series eliminating Perl dependencies from Git's test suite, specifically addressing textconv-related tests. Johannes Schindelin provides review feedback on patch 13/20, which converts Perl-based textconv scripts to shell utilities.

The patch modifies three test files (t4030, t4031, t7815) to replace Perl textconv scripts with shell equivalents, removing PERL_TEST_HELPERS prerequisites. Schindelin notes the t4030 conversion uses `tr` for NUL/SOH replacement rather than the more obvious `test-tool hexdump`, though he doesn't request changes. He observes the t4031 conversion actually strengthens test assertions by checking specific hex values rather than just line counts. The t7815 changes appear uncontroversial, using simple `tr` for NUL-to-Q conversion.

Key technical details: The patch introduces shell script replacements for three Perl textconv operations - binary-to-text conversion (using both `tr` and `test-tool hexdump`), NUL character replacement, and binary diff analysis. Test expectations are adjusted where needed, with some cases becoming more precise in their verification. The changes maintain test coverage while removing Perl dependencies, though Schindelin's comments suggest some implementation choices could be revisited for consistency with other patches in the series.

This feedback comes late in the review cycle, with the overall series already receiving positive reviews from multiple contributors. Schindelin's observations are minor and don't block the patch, focusing more on understanding design choices than requesting changes. The feedback pattern continues the series' trend of moving from substantive review to minor optimization suggestions as it nears completion.