This is a style suggestion in the ongoing bitmap acceleration series for `git cat-file` filtering. Toon Claes responds to Patrick Steinhardt's patch that introduces payload passing to the `show_reachable_fn` callback, proposing a naming tweak to improve readability.

The patch itself (from an earlier version of the series) adds a payload parameter to the callback interface used for bitmap object iteration, which is needed for the cat-file filtering functionality. Toon's comment is narrowly focused on the naming of a parameter in one of the functions - suggesting adding `_fn` to `show_reach` to make it clearer that it's a callback function being passed around.

This is a minor stylistic point in the context of the larger series, which has already demonstrated significant performance improvements (41x speedup in benchmarks). The thread context shows the series is nearly ready for merging, with just a few final technical points being reviewed - this being one of the more straightforward ones.