This is a review comment on the final patch of the HTTP TCP keepalive configuration series. Jeff King points out a naming inconsistency between the documented environment variables (GIT_HTTP_KEEPALIVE_*) and the actual implementation (GIT_TCP_KEEP*). 

The reviewer notes that while he personally hasn't needed these keepalive settings, he has no objection to their inclusion. His main technical observation is that the HTTP-prefixed names would be more consistent with both the configuration options and curl's HTTP-specific usage context. 

This appears to be a minor but important naming cleanup needed before the otherwise-ready series can be merged. The comment suggests the implementation should align with the documented HTTP-specific naming scheme rather than the more generic TCP prefix currently in the code. The feedback comes from a trusted reviewer and seems likely to require a quick v4 with this naming fix.