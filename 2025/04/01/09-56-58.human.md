This is a final refinement to a bugfix thread addressing `--left-only`/`--right-only` behavior with unrelated commit histories. The core fix (setting `revs->limited = 1` in revision.c) was already been established as correct, and the discussion has moved to test implementation details.

Johannes Sixt points out that the proposed test uses bash-specific process substitution (`<(sort head)` syntax) which won't work on Windows or some POSIX shells. This continues the thread's pattern of improving test portability - earlier iterations had already moved from process substitution to temporary files for Windows compatibility. The comment serves as a gentle reminder to maintain cross-platform compatibility in the test suite.

The thread shows the Git project's characteristic attention to both algorithmic correctness and cross-platform robustness, with multiple contributors refining the solution. No new technical concerns are raised here - just a final polish to ensure the test works everywhere Git runs.