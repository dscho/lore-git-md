This is a review comment on the HTTP TCP keepalive configuration series, specifically addressing naming consistency between environment variables. Jeff King notes a discrepancy between the documented environment variable names (GIT_HTTP_KEEPALIVE_*) and the actual implementation (GIT_TCP_KEEP*). 

The technical context shows this is the final patch in a v3 that has already undergone significant review and refinement, with previous feedback from Patrick Steinhardt and Elijah Newren addressed. The series introduces config options http.keepaliveidle/interval/count with environment variable overrides, now with simplified curl handle configuration after maintainer-approved refactoring.

Jeff points out that while the documentation correctly describes HTTP-specific variable names matching the config options, the code implementation uses more generic TCP-prefixed names. He suggests the HTTP variants make more sense since the feature is curl-specific. This appears to be a final naming polish before the otherwise merge-ready series can proceed. The comment maintains the thread's constructive tone while catching a subtle inconsistency that could confuse users.