This is a technical review comment in the ongoing "cat-file batch mode filtering" series. Toon Claes is examining a preparatory refactoring patch from Patrick Steinhardt that deduplicates object iteration logic in `builtin/cat-file.c`.

The key observation is about type safety - Toon questions why the `_payload` parameter in the new `batch_each_object()` helper function is typeless (void pointer) when it's only ever used with `struct object_cb_data`. He suggests maintaining type information where possible for better code clarity and safety.

This is a focused technical point in an otherwise well-reviewed series that has already demonstrated significant performance improvements (41x speedup in benchmarks). The comment doesn't challenge the overall approach but seeks to refine the implementation detail before final merging. The series is in its final review stages, with this being one of the last open questions alongside the previously noted pointer comparison discussion.