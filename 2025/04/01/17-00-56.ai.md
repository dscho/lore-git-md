Test patch for bundle refname deduplication, part of a performance optimization series. This first patch by Karthik Nayak adds test coverage for the existing duplicate refname handling in bundle creation, which currently has O(N^2) performance characteristics. The tests verify current behavior (including known bugs) for various duplicate scenarios: direct duplicate refs, duplicates when combined with `--all`, exclusion patterns (`main^!`), and short/long refname forms. Two tests explicitly mark current deficiencies where short/long name variants aren't properly deduplicated - these will be addressed by the optimization in the series' second patch.

The test additions in t6020-bundle-misc.sh cover both correct deduplication cases and intentionally document current limitations, providing a baseline for the performance improvements to come. The commit message clearly links to historical context (commit b2a6d1c686) and explains the test strategy. This appears to be a well-structured foundation for the optimization work, with comprehensive test coverage that will help verify both correctness and performance of the subsequent changes.