This is a feature patch series (v2) that enhances `git version --build-options` to report which SHA implementations Git was built with. The series has progressed through maintainer feedback and now focuses on finalizing the output format and warning system.

The key changes in this version include:
- Using backend-defined symbols (`SHA1_BACKEND`, `SHA1_UNSAFE_BACKEND`, `SHA256_BACKEND`) as the source of truth
- Displaying the raw symbol names rather than human-readable alternatives
- Adding explicit "(No collision detection)" warnings for non-crypto SHA-1 variants
- Renaming the unsafe variant from "unsafe-SHA-1" to "non-crypto-SHA-1"

The implementation touches hash.h (for symbol definitions), help.c (for output formatting), and updates the git-version documentation. The thread shows consensus forming around this approach, with maintainer approval of the core technical design. The remaining discussion points appear to be minor presentation details rather than architectural concerns.

This looks like a well-considered change that provides useful diagnostic information while maintaining technical accuracy. The commit messages clearly explain the rationale and the series has been responsive to feedback from the mailing list. The changes are narrowly scoped to version reporting without wider impact on the codebase.