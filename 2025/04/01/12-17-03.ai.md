Feature enhancement with type safety consideration in the Git cat-file bitmap filtering series.

This email from Toon Claes provides a small but potentially important naming suggestion for Patrick Steinhardt's ongoing bitmap acceleration work. The discussion focuses on a callback function signature change introduced in patch 6/10 of the series, where a payload` parameter was added to the `show_reachable_fn` callback interface.

Claes suggests improving readability by appending `_fn` to the parameter name `show_reach` when it's passed to `show_objects_for_type()`, making it clearer that this is a function pointer rather than a direct function. This comes in the context of earlier discussions about type safety in callback handling within this series.

The suggestion appears minor but touches on the broader theme of code clarity and maintainability that has been a recurring topic in this thread. While not addressing the more substantial open questions about pointer comparison methodology or payload type safety, this recommendation aligns with the careful attention to detail that has characterized the review process for this performance-enhancing feature series.