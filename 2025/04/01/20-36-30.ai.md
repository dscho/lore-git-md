This is the second and final patch in a series enhancing `git version --build-options` to report SHA implementations, focusing on the "unsafe" (non-cryptographic) SHA-1 variants.

The patch completes the implementation by adding reporting for non-cryptographic SHA-1 backends, following the symbol-based architecture approved in the thread. It defines `SHA1_UNSAFE_BACKEND` symbols in hash.h for each implementation (Apple, OpenSSL, and block-sha1), then exposes them in the version output under a "non-crypto-SHA-1" label. The documentation is updated to explain this new output line.

Key technical details:
- Maintains compile-time safety by using preprocessor symbols
- Follows the established pattern from the first patch's SHA1/SHA256 reporting
- Output format matches the thread's consensus to use raw symbol names (e.g. "SHA1_OPENSSL_UNSAFE")
- Documents the non-crypto variants alongside the existing SHA1 collision warning
- No runtime overhead - all processing happens at compile time

The patch appears to be in final form, implementing all the technical requirements from the thread discussion. It addresses the remaining presentation details from the first patch, completing the series' goal of comprehensive SHA implementation reporting in version output. With maintainer approval already secured for the approach, this should be ready for final review and inclusion.