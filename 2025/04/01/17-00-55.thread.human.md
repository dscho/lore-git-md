This thread centers on Karthik Nayak's two-patch series optimizing bundle creation performance by eliminating an O(N^2) bottleneck in duplicate refname checks. The key improvement replaces the quadratic `object_array_remove_duplicates()` with an O(1) `strset`-based solution in `write_bundle_refs()`, yielding dramatic speedups (6x faster at 100k refs) while maintaining correct handling of edge cases like identical refnames pointing to different objects and exclusion patterns.

Toon Claes provided thorough review, verifying the implementation's correctness through both test cases and additional manual verification. The only outstanding change is a minor style tweak to use the `STRMAP_INIT` macro rather than explicit initialization, which Karthik has agreed to incorporate in the next version. The discussion references Toon's prior work on bundle race conditions (commit b2a6d1c686) as relevant context, showing how this optimization builds on earlier improvements.

With comprehensive test coverage and benchmarking demonstrating the solution's effectiveness, the series appears ready for merging once the final style adjustment is made. The technical approach has clear consensus, addressing a real performance pain point for repositories with large numbers of references.