Here's a human-readable summary of the thread so far:

The thread began with a detailed report about a gap in Git's meson build system where tcsh shell completions are built but not installed, even when explicitly enabled via `-Dcontrib=completion`. The reporter provided clear reproduction steps showing that while other contrib items like git-subtree install correctly, the shell completions only appear in the build directory.

Patrick Steinhardt responded by clarifying that this behavior actually matches the traditional Makefile-based build (which also doesn't auto-install completions), though he acknowledged this isn't ideal. The core issue appears to be a lack of standardization across Linux distributions for where shell completion scripts should be installed - different distros use different paths for bash, tcsh, and zsh completions.

The discussion has now shifted from being a potential meson build bug to a broader question about whether there's enough consensus on standard installation paths to justify implementing automatic completion installation. The thread is currently awaiting input from package maintainers or others familiar with cross-distribution conventions for shell completion file locations.