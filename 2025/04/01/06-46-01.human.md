This is a build system question about the meson transition, specifically regarding shell completion installation.

Akshay Hegde reports that while the meson build system correctly builds and installs the `git-subtree` contrib component when requested via `-Dcontrib=subtree`, it doesn't automatically install shell completion scripts even when `completion` is similarly specified in the contrib options. The completion scripts are built and placed in the build directory, but not copied to the final installation location.

The email is phrased as a question to confirm whether this is expected behavior or if there's an undocumented build option needed to trigger completion script installation. The reporter notes they've already checked the main meson configuration files and found no obvious setting for this.

This appears to be either a documentation gap (if manual installation is expected) or a minor build system bug (if automatic installation was intended). The tone is constructive - the reporter is clearly trying to help validate the new build system and notes they've otherwise had a smooth experience with the meson transition. The issue seems straightforward to reproduce given the provided build command.