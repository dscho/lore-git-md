This is a review comment on a patch in Patrick Steinhardt's series enhancing `git cat-file` batch mode filtering. Karthik Nayak questions a specific implementation detail in the "blob:none" filter support.

The patch adds blob filtering capability to `git cat-file`, but Karthik identifies what appears to be redundant type information handling. He notes that `data->info.typep` is already set for content batch mode in `batch_objects()`, making the additional check for `LOFC_BLOB_NONE` in `batch_object_write()` potentially unnecessary. The concern focuses on code clarity rather than functionality, as the current implementation works but may contain superfluous logic.

Karthik suggests adding explanatory comments if there's a subtle reason for the current approach that isn't immediately obvious. This is a focused technical review point in an otherwise mature series that has already addressed broader architectural concerns and demonstrated significant performance improvements. The question represents the kind of final polish often seen in late-stage patch review, where reviewers scrutinize edge cases and code organization after the main functionality is validated.