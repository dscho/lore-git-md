This is a technical discussion about test hygiene in the context of the rebase status reporting improvements series. Johannes Schindelin responds to Eric Sunshine's feedback about environment variable handling in test scripts, specifically regarding `FAKE_LINES` usage in t3418-rebase-continue.sh.

Schindelin acknowledges the potential pollution issue with the current `FAKE_LINES` approach (where variables remain set across tests) and suggests a fundamentally different mechanism would have been better - writing the pick lines to a file in the repository's git directory that the fake editor script would read and then remove. This would avoid environment variable leakage entirely.

The discussion occurs within the context of patch 1/3 from Philippe Blain's series, which fixes merge commit creation after empty conflict resolution in interactive rebase. While the core patch functionality isn't debated here, this exchange reveals deeper architectural considerations about Git's test infrastructure, particularly around the sequencer's test interface.

Key technical details mentioned:
- Current problematic pattern: `FAKE_LINES=... && export FAKE_LINES`
- Suggested alternative: `$(git rev-parse --git-path sequencer.pick-lines)` file-based approach
- Impacted test: t3418-rebase-continue.sh
- Related component: fake-editor.sh test helper

This suggests potential future work to refactor test infrastructure, though the immediate focus remains on Philippe's series which uses the existing `FAKE_LINES` mechanism (albeit now with Eric's suggested `env` prefix for isolation).