Thread summary (AI mode):

Build system thread addressing shell completion script installation in meson, originating from a report that tcsh completions built with `-Dcontrib=completion` aren't automatically installed. Patrick Steinhardt initially confirmed this matches Makefile behavior but identified the issue as lack of standardized completion paths across distributions. Akshay Hegde proposed extending meson_options.txt for per-shell completion directory specification, enabling automatic installation when paths are provided.

Todd Zullinger's Fedora-specific contribution adds critical distribution perspective: Fedora currently packages only bash (standard path) and tcsh (non-standard `/usr/share/git-core/contrib/completion/` path unchanged since 2013), while omitting zsh completions entirely due to upstream zsh coverage. This reveals significant inter-shell variation even within one distribution, with fish using `/usr/share/fish/vendor_completions.d` and zsh (for other packages) using `/usr/share/zsh/site-functions`.

The discussion has evolved from a build system issue to a packaging policy question with concrete technical proposals. Hegde's meson_options.txt extension remains the most viable path forward, now with real-world evidence supporting the need for per-shell flexibility. Key open questions include:
- Whether tcsh's manual configuration path should be preserved given its decade-long stability
- How to handle zsh given existing upstream coverage
- Whether to establish different default behaviors per shell based on distribution practices

The meson build system context remains central, with ninja backend handling file generation but deployment requiring path configuration. No consensus yet on standardization, but Zullinger's data suggests some shells may not need automatic installation as urgently as others. Distribution maintainers' input remains valuable for establishing viable defaults.