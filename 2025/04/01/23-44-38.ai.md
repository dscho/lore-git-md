This is a technical response in the ongoing discussion about optimizing rebase workflows to avoid unnecessary rebuilds. Brian m. carlson suggests an alternative to Ramkumar Ramachandra's proposed `git rebase-checkout` command, pointing to the existing experimental `git replay` command as potentially solving the same problem.

Key technical details:
- `git replay` is presented as an existing solution that performs non-interactive rebase without touching working tree/index
- Unlike standard rebase, it doesn't update references by default (requires piping to `git update-ref`)
- Currently marked experimental in Git's codebase
- Would address the core timestamp preservation issue while using existing plumbing

The email advances the discussion by:
- Offering a potentially simpler path than introducing a new command
- Noting the experimental status as a current limitation
- Implicitly questioning whether a new command is needed given existing functionality

This suggests the thread may pivot to evaluating whether `git replay` can satisfy the use case, or if the proposed `rebase-checkout` still offers distinct advantages. The experimental nature of `replay` and its reference update behavior appear as key discussion points moving forward.