Bundle performance optimization series addressing O(N^2) scaling in bundle creation due to inefficient duplicate refname checks. Authored by Karthik Nayak, the two-patch series first established comprehensive test coverage (t6020-bundle-misc.sh) documenting current limitations, then implemented the optimization. The second patch replaces the quadratic `object_array_remove_duplicates()` with an O(1) `strset`-based solution in `write_bundle_refs()`, yielding 6x speedup (14.6s → 2.4s at 100k refs) and linear scaling where the original showed 2200x slowdown. The optimization handles two key cases: identical refnames pointing to same/different objects (including short/long variants and exclusion patterns), focusing only on INTERESTING objects for bundle creation. Implementation removes the now-unneeded `object_array_remove_duplicates()` entirely. 

Follow-up discussion between Karthik Nayak and Toon Claes confirms the implementation correctly handles both same-refname cases (regardless of object identity) and exclusion patterns, with test coverage catching edge cases. Toon verified additional edge cases beyond the test suite and approved the implementation. Karthik agrees to change from explicit `strset_init()` to `STRMAP_INIT` macro in the next version, addressing the only remaining style point. The conversation references Toon's prior work on bundle race conditions (commit b2a6d1c686) as context. Both parties confirm the optimization maintains correct behavior while dramatically improving performance, with comprehensive benchmarking showing the solution scales linearly where the original implementation was quadratic. Changes span bundle.c, object.c, object.h with explicit test coverage of edge cases before optimization. No outstanding technical objections remain beyond the agreed style change. The thread demonstrates consensus on the solution's correctness and readiness for merging, with potential merge conflict with ps/object-wo-the-repository remaining easily resolvable.