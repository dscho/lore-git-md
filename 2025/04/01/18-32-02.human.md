This is a review comment on patch 11/20 in Patrick Steinhardt's series eliminating Perl dependencies from Git's test suite. Johannes Schindelin notes that while the patch's commit message focuses on replacing Perl's substitution operator with `sed`, one of the changes actually uses `tr` instead to convert newlines to null bytes. 

Schindelin observes that this particular change might have fit better in the previous patch (which presumably dealt with `tr` conversions), but doesn't object to its current location. The comment serves as a minor clarification rather than requesting any changes, indicating the patch is otherwise acceptable.

This exchange shows the series is in its final polishing stages, with reviewers making only minor observations about organization rather than substantive feedback. The conversion from Perl to POSIX tools appears to be proceeding smoothly with careful attention to detail.