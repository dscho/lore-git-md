Here's the updated AI thread summary:

Feature/bug report regarding `git clone --bundle-uri` progress feedback and filter interaction. Xavier Morel identifies two UX issues: (1) complete lack of progress feedback during bundle processing (even with --verbose/--progress flags), particularly problematic for large repositories (~5M objects, ~10GB fresh clone size), and (2) potentially misleading behavior when combined with --filter, where filters only apply to the post-bundle reconciliation fetch, making the combination both slower and less space-efficient than expected. Technical specifics note this affects local file bundle URIs specifically.

Toon Claes confirms both issues, providing historical context about attempted fixes. For progress feedback, multiple past attempts exist (including v2 patch series and alternative approaches) but remain unimplemented due to technical challenges. The filter interaction is a broader design limitation - clients cannot verify if advertised objects match the filter, and filtering only applies to reconciliation. This matches Xavier's findings of degraded performance with the combination.

Key technical details: Progress reporting is particularly challenging for local file bundles. The filter limitation is fundamental to current bundle-uri design and may require protocol changes. Several past discussions and patch attempts ([1][2][3][4]) exist but no active maintainer is currently working on solutions. The issues represent known UX gaps in large repository workflows, with progress feedback being a particularly notable omission. No current workarounds exist beyond avoiding the problematic --filter combination.