This is a security-related follow-up in the resolved `safe.directory` behavior discussion. The email provides additional technical clarification about why the `--no-local` workaround (introduced in Git v2.48.0) behaves differently from regular clone operations regarding `safe.directory` wildcard matching.

The key technical point explains that `--no-local` triggers Git's secure `git-upload-pack` mechanism (used for untrusted repositories) while regular clones attempt hardlinking which has different security semantics. This confirms the thread's earlier conclusion that the behavior difference is intentional security design rather than a bug.

The email also proposes a potential future enhancement - automatically detecting cross-ownership repositories to use `--no-local` - but marks this as low-priority cleanup work ("#leftoverbits"). This doesn't reopen the resolved discussion but suggests a possible quality-of-life improvement that maintains the same security guarantees.

The response reinforces the thread's core security model where different repository operations (single-process vs multi-process, local vs non-local) deliberately handle configuration propagation differently based on their threat model. No new workarounds or issues are introduced - this serves as additional implementation detail confirming the already-accepted solutions.