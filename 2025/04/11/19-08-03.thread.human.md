Here's the human-readable thread summary:

The thread investigates unexpected behavior when using wildcards with `git add`, where patterns like `f*` fail to match both a literal file named `f*` and files matching the pattern (like `foo`) on the first run. Initial analysis suspected an optimization flaw in the matching logic, but deeper investigation revealed this is actually the expected behavior - Git treats unadorned wildcards as literal matches unless globbing is explicitly enabled via magic prefixes like `:(glob)` or environment variables.

Jeff King (Peff) has now weighed in with technical details about how the pathspec system works, explaining that the `nowildcard_len` field determines where glob characters begin in the pattern. He proposes a focused fix to `do_match_pathspec()` that would make wildcard patterns work more intuitively while maintaining backward compatibility. The change would ensure pathspecs with wildcards are always fully evaluated, rather than sometimes being treated as exact matches.

The discussion has evolved from initially thinking this was a bug to understanding it as a design choice with room for improvement. Peff's proposed solution offers a middle path - keeping the opt-in globbing behavior but making it work more predictably for the wildcard case. The thread now awaits feedback from other contributors and maintainers on whether this approach is preferable to alternatives like changing the default behavior or just improving documentation.