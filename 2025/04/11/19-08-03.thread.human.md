Here's a human-readable summary of the thread so far:

Piotr Siupa has reported an unexpected behavior in `git add` when dealing with wildcard pathspecs in repositories containing both literal and pattern-matching filenames. The key issue occurs when a repository has both a file literally named `f*` and another file matching the pattern `f*` (like `foo`). In this case, the first `git add 'f*'` command only stages the literal `f*` file, while subsequent runs correctly stage both `f*` and `foo`.

The bug appears in both the current stable release (Git 2.43.2) and development version (2.49.0.805.g082f7c87e0), indicating this is a longstanding behavior rather than a recent regression. The minimal reproduction case - a fresh repository containing just `foo` and `f*` files - clearly demonstrates the issue. While there's no workaround mentioned, the fact that subsequent `git add` commands work suggests the problem may lie in how Git caches or handles state during pathspec matching.

The report is well-documented with a clear test case, and the described behavior does appear to violate typical user expectations for wildcard matching. The issue seems to stem from how Git handles the interpretation of literal versus wildcard pathspecs in this specific edge case.