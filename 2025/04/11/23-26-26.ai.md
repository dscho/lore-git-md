This is a refactoring patch in Taylor Blau's series on cruft packs and MIDX optimization during repacking. It's the third of eight parts in the RFC series.

The patch extracts the inline handling of `--stdin-packs` in `cmd_pack_objects()` into a separate `read_stdin_packs()` function. This is a straightforward code organization change that moves the existing logic (which was introduced in commit 339bce27f4) into its own function to prepare for future expansion. The change is mechanical and maintains identical behavior - it just relocates the code that sets `ignore_packed_keep_in_core`, calls `read_packs_list_from_stdin()`, and conditionally calls `add_unreachable_loose_objects()`.

The modification touches only `builtin/pack-objects.c`, moving 6 lines of code into a new function and replacing the original inline code with a call to that function. This continues the series' pattern of preparatory refactoring before introducing new functionality in later patches. Based on the thread context, we can expect this extracted function to be modified in subsequent patches to support the series' goal of optimizing how cruft packs interact with MIDX during repacking.