Here's a human-readable thread summary:

The thread discusses UX issues with `git clone --bundle-uri`, particularly for large repositories. Xavier Morel reports that the command provides no progress feedback during bundle processing, even with `--verbose` flags, which is problematic since bundle-uri is meant for large clones where users expect visibility into long-running operations. 

A secondary issue involves potentially confusing behavior when combining `--bundle-uri` with `--filter`. Experiments show filters only apply to the post-bundle reconciliation fetch, making this combination both slower and less space-efficient than users might expect. The suggestion is to either document this limitation clearly or modify the behavior to be more intuitive.

The discussion highlights a gap in the bundle-uri feature's UX, especially for its primary large-repository use case. The progress reporting omission seems like an oversight that should be addressed, while the filter interaction may require either documentation updates or code changes to align with user expectations.