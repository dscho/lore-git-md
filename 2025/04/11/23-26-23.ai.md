This is a refactoring patch in Taylor Blau's series on cruft packs and MIDX optimization. It's the second of eight patches, focusing on code cleanup in `builtin/pack-objects.c` to support later functionality.

The patch makes two localized improvements to `add_object_entry_from_pack()`:
1. Moves the `revs` declaration inside the conditional where it's actually needed (when handling commit objects)
2. Uses a designated initializer for the `object_info` struct since only the `typep` field is used

These changes don't modify behavior but tighten the code's scope and initialization patterns. The function is part of the pack-objects machinery that will be used later in the series for `--stdin-packs=follow` mode. The changeset is small (4 lines removed, 2 added) and focused, following the series' pattern of starting with preparatory cleanups before introducing new features.

The patch maintains all existing functionality while making the code slightly more maintainable. It continues the series' approach of careful, incremental changes to the pack-objects infrastructure.