Bug report about `git stash -k` behavior with staged changes, where editing a file, staging it, then editing the same line again and running `git stash -k` incorrectly creates a stash diff between HEAD and unstaged changes rather than between staged and unstaged changes. This causes unnecessary merge conflicts when popping the stash later, affecting Git 2.39.5 on macOS (arm64) but likely platform-independent. D. Ben Knoble references a 2023 discussion (message ID ZikMqXeDnOqK_wlq@thechases.com) that appears to address the same underlying issue, suggesting this may be a known limitation or bug in stash's index handling where it fails to properly preserve staged/unstaged relationships. The prior discussion's technical details about stash implementation and index/working tree state handling could help determine if this is expected behavior or a genuine bug needing resolution, though it remains unclear if that thread produced any fixes or workarounds. The connection to past analysis helps frame whether this represents a regression versus long-standing behavior.