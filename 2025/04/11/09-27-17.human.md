This is a follow-up clarification in the now-merged refactoring series that reorganized Git's object-file subsystem. The thread is effectively closed, with all architectural decisions finalized.

Patrick Steinhardt responds to Elijah Newren's question about the relationship between `safe_create_leading_directories()` and `mkdir_in_gitdir()` in the first patch of the series. While the original commit message described them as "relatives," Patrick clarifies they are more like siblings in functionality rather than having a caller/callee relationship. He maintains that grouping them together in dir.c still makes sense because they provide generic directory utilities rather than object-specific operations.

This exchange serves as a post-merge clarification of the design rationale, confirming that the directory-related functions were intentionally grouped by domain (utility functions) rather than call hierarchy. The series has already achieved its primary goal of establishing clearer subsystem boundaries in Git's codebase, with this being a minor documentation point about one of the many relocated functions.