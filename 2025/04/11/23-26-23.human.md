Refactoring patch in a series optimizing cruft pack/MIDX interactions. This is the second of eight patches in Taylor Blau's RFC series, following an initial mechanical refactoring of option validation.

The patch makes two small cleanups to `add_object_entry_from_pack()` in `builtin/pack-objects.c`:
1. Moves the `revs` variable declaration inside the conditional where it's actually used
2. Uses a designated initializer for the `object_info` struct since only the type field is needed

These are straightforward code hygiene improvements with no behavior changes, continuing the series' pattern of preparatory refactoring before introducing new functionality. The changes are minimal (4 lines removed, 2 added) and focused on a single function that will be important for the later `--stdin-packs=follow` mode implementation.

The patch is clearly part of a well-planned series, with each change building toward the larger goal of optimizing how cruft packs interact with MIDX files during repacking. The commit message is concise but explains the rationale for each change.