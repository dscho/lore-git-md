Build system refinement for meson header checking. This is a follow-up to Karthik Nayak's patch series implementing header checking in meson build system (matching Makefile's 'hdr-check' capability). The thread has already reached consensus on major design points including target naming ("check-headers") and dynamic header collection approach.

Patrick Steinhardt points out a potential issue with the current implementation where the `headers` variable is initialized as an empty list unconditionally. He suggests making its declaration conditional on `git.found()` to prevent accidental usage when Git isn't properly configured. This appears to be a minor but sensible refinement to the build system logic, addressing an edge case that could lead to incorrect behavior in certain build configurations.

The comment is narrowly focused on this one technical detail, suggesting the overall patch series is in good shape with only minor polishing needed. The discussion shows careful attention to build system robustness, even for corner cases.