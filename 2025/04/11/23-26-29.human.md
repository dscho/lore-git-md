This is the fourth patch in Taylor Blau's series optimizing cruft pack and MIDX interactions during repacking. It's a refactoring that moves the initialization and execution of a reachability traversal out of `read_packs_list_from_stdin()` and into its caller `read_stdin_packs()`. 

The change prepares for future work where this traversal will be reused for packing objects reachable from commits in input packs but stored elsewhere. The commit message clearly explains this is groundwork for sharing the traversal logic with the `--unpacked` case, referencing the similar `add_unreachable_loose_objects()` function.

The patch is mechanically straightforward - it moves 35 lines of `rev_info` setup and traversal code while maintaining all existing functionality. This continues the series' pattern of careful preparatory refactoring before introducing new behavior. The changes are limited to `builtin/pack-objects.c` and maintain all existing test coverage.

As with previous patches in the series, this appears to be a low-risk change that improves code organization in service of the larger goal. The commit message provides clear motivation and connects the change to the series' broader objectives around pack optimization.