This is a bugfix for the revision walker's handling of `--left-only` and `--right-only` options with unrelated commit histories, now being marked for inclusion in the 'next' branch by Junio C Hamano. The patch follows the established pattern from commit 023756f4eb ("revision walker: --cherry-pick is a limited operation") by setting `revs->limited = 1` when these options are used, ensuring proper behavior when comparing histories without a common merge base.

The technical solution is straightforward - two lines added to revision.c to mirror the existing `--cherry-pick` handling. The test evolution shows significant refinement from v1 to v2, now using sorted temporary files and `test_line_count` assertions rather than Bash process substitution, addressing portability concerns raised in the thread. The test validates that the symmetric difference between two unrelated histories produces disjoint commit sets when using these options.

The thread context shows this patch has undergone thorough review, including test methodology improvements from Phillip Wood, Windows compatibility fixes from Johannes Sixt, and semantic validation refinements. All outstanding technical questions have been resolved, with consensus on both the core fix and test implementation. The patch demonstrates good attention to both correctness and maintainability, with the test now properly using tags to manage repository state rather than raw commit counting.