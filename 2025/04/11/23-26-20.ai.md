Refactoring patch in a series about cruft packs and MIDX optimization. This is the first of 8 patches in Taylor Blau's RFC series, serving as preliminary cleanup before introducing new functionality.

The patch standardizes how `pack-objects` handles incompatible command-line options by replacing custom error checks with calls to `die_for_incompatible_opt2()`. This affects several option pairs including `--stdin-packs`/`--filter` and `--keep-unreachable`/`--unpack-unreachable`. The change makes the error messages consistent with Git's standard format while maintaining the same validation logic.

Key technical details:
- Modifies `builtin/pack-objects.c` to use the standard option incompatibility functions
- Updates one test in `t/t5331-pack-objects-stdin.sh` to expect the new message format
- Maintains all existing option validation behavior while changing only the error reporting mechanism
- Part of groundwork for later patches that will introduce new `--stdin-packs=follow` functionality

The patch is well-contained with minimal test fallout, suggesting it's low-risk preparatory work. The changes are mechanical but important for consistency, especially as the series will later add more complex option interactions around `--stdin-packs`.