Here is the dense AI-mode summary capturing all key information from this "What's Cooking" report:

Junio C Hamano's April 2025 "What's Cooking" report provides status updates on in-flight Git development topics across integration branches. New topics include performance optimizations for bundle creation (fixing O(n²) scaling with many refs), meson build system additions (hdr-check target), and a 10-commit object-file subsystem cleanup series. Cooking topics span test modernization (removing Perl dependencies), performance (path-based delta compression in pack-objects), UX improvements (blame output enhancements, fetch behavior fixes), code quality (removing recursive merge backend, zlib fixes), and new features (reflog expiration in maintenance, non-transactional batch ref updates). Several topics are marked ready for 'next' or 'master' including the bundle optimization and meson hdr-check target. Others await review or rerolls, with discussion still needed on the object-file cleanup and path-based delta compression work. The report provides source links for each topic and tracks their current branch placement (maint, master, next, seen). Key technical areas touched include ref handling, object storage, merge strategies, and build system components. No major design objections or blocking issues are noted for the ready-to-merge topics, while some larger refactors remain under active discussion.