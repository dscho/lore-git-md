This is a build system patch series (3 parts) that adds meson support for header checking, matching the existing Makefile`hdr-check` target. The series is part of the ongoing effort to bring meson build system feature parity with the traditional Makefile.

The first two patches are preparatory cleanups: one renames coccinelle-related variables for clarity, and another moves header definitions to a more logical location in the build system. The third patch introduces the actual `headers-check` target for meson, completing the functionality.

The changes are focused and well-organized, with clean commit messages explaining each step. The series builds on recent work (referencing the merged 'es/meson-build-skip-coccinelle' branch) and appears to be a straightforward quality-of-life improvement for developers using the meson build system. The patch touches only build configuration files, with no runtime implications.