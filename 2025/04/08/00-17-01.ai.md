This is a technical discussion in the ongoing Perforce test improvement series, specifically about verification methodology for tag existence checks. Junio Hamano responds to Anthony Wang's concern about modifying test intent by advocating for more robust verification using `git show-ref --verify` rather than grep patterns.

Junio makes several key points about test design philosophy. He argues that grep-based tag verification is fundamentally flawed as it can produce false positives (e.g., matching "TAG_F1_1" when "T_TAG_F1_1" exists). He emphasizes that proper testing should explicitly verify both presence of expected tags and absence of unexpected ones using `show-ref --verify`, which would catch cases where TAG_F1_ONLY exists when it shouldn't.

The email reveals a difference in perspective between maintaining original test intent versus improving test robustness. While Anthony worries about changing test semantics, Junio views the original approach as "sloppy programming" that should be corrected rather than preserved. This suggests the v3 series will likely need to adopt the show-ref verification approach, representing a more significant change than the original exit-code fixes.

Technical details to note for future summarization: the specific tags involved (TAG_F1_1, TAG_F1_2, TAG_F1_ONLY), the proposed verification pattern using `show-ref --verify` with `test_must_fail`, and the fundamental disagreement about whether grep-based verification should be considered acceptable even in historical tests. The discussion has moved beyond the original pipe/exit-code issues to deeper questions about test methodology.