This is a discussion about the safety and semantics of propagating extra commit headers during rebase operations, continuing the thread about Phillip Wood's RFC patch for partial header preservation.

Junio C Hamano raises important concerns about the fundamental nature of commit header propagation during rebase. He notes that rebase already makes selective decisions about which standard headers to preserve (like author) versus which to update (like parent) or drop (like signatures). The core question is how to handle truly "extra" headers whose semantics are unknown to Git - the machinery cannot determine which should propagate versus which might become invalid or misleading after rewriting.

The response highlights several key philosophical issues: some headers inherently lose meaning during rewrite operations (like parent hashes and signatures), while others may need case-by-case evaluation. Junio expresses particular concern about propagating headers with unknown semantics, as their validity after rewriting cannot be automatically verified. This builds on brian m. carlson's earlier concerns about propagating unvalidated data, but frames it in terms of semantic meaning rather than just data corruption.

The discussion continues to balance Phillip's goal of supporting server-side use cases (where header preservation is valuable and conflicts are rejected) against the need to maintain data integrity. Junio's intervention shifts focus from implementation details of the current patch to deeper questions about how Git should handle metadata semantics during history rewriting operations. This positions the thread at a crossroads between practical functionality and long-term repository health considerations.