Series completion notice for SHA implementation reporting in `git version --build-options`.

Junio Hamano confirms this 2-series is ready for merging after receiving final approvals from both Patrick Steinhardt and Christian Couder. The patches enhance `git version --build-options` to report which SHA implementations are compiled in, with special handling for non-collision-detecting SHA-1 variants (only shown when actually used). The implementation maintains zero runtime overhead while providing clear security warnings about unsafe SHA-1 variants.

All technical and documentation aspects were settled through three iterations, with the final version addressing:
- Standardized output format omitting unused unsafe SHA-1 lines
- Clear "non-collision-detecting-SHA-1" terminology
- Compile-time symbol-based detection
- Explicit security warnings

The thread has reached full consensus with no outstanding issues, and the series will now proceed to the 'next' branch. This represents the completion of a well-executed enhancement that improves transparency about Git's cryptographic foundations.