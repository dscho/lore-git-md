Refactoring patch in a series splitting object-file.c functionality. This second patch moves the platform-agnostic `git_open_cloexec()` function from object-file.c to compat/open.c, where it more logically belongs as a general file-handling utility rather than object-specific code. The change makes the function available unconditionally while keeping the EINTR-related retry logic conditional. 

The patch is mechanical but thorough, updating includes in 11 files (mostly removing object-file.h where it was only needed for this function) and adjusting build systems. This continues the architectural cleanup started in patch 1, with the series aiming to make object-file.c focus solely on loose object I/O while moving other functionality to more appropriate homes. 

The change appears straightforward and well-scoped, with no behavior modifications - just code relocation. It follows the pattern established in the first patch of the series, moving clearly non-object-specific utilities out of object-file.c. Reviewers should verify the build system changes (Makefile and meson.build) handle the unconditional compilation correctly across platforms.