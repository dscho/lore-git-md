Bugfix and refactoring for git-p4 encoding handling. Nikolay Shustov follows up on Phillip Wood's review of his patch series addressing git-p4 crashes when processing non-UTF-8 Perforce error messages. The core of the fix introduces a `MetadataTranscoder` class to handle encoding conversions consistently.

Shustov confirms he'll update the commit message with more context about the specific crash scenario (line 901 failures during `git p4 clone`). The main discussion point now is whether the refactoring (creating the transcoder class) should be split from the bugfix commit. Shustov argues the combined diff shows the changes clearly, while Wood's feedback suggests separating them would better follow Git project conventions.

This is part of an ongoing effort to improve git-p4's handling of encoding issues, particularly for non-Unicode Perforce servers. The technical solution appears sound, with reviewers focusing on commit organization rather than the implementation itself. The series awaits real-world validation from Fahad Al-Rashed's testing environment, which remains outstanding despite multiple follow-ups.