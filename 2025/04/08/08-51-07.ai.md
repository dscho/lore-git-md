This is a refactoring patch in Karthik Nayak's batched reference updates series (v6 3/8), focusing on the files backend. The change removes redundant duplicate reference name verification that was being performed twice - once during the transaction's prepare phase and again during finish. Since the refnames list doesn't change between these phases, the second check is unnecessary.

The patch makes `ref_update_reject_duplicates()` static since it's now only used within refs.c, and removes the duplicate check from files-backend.c while preserving the function's documentation. This cleanup follows the series' earlier move of duplicate checking to the generic refs layer (v6 2/8), continuing the effort to centralize and streamline reference transaction handling.

The changes are straightforward:
1. Function declaration removed from refs-internal.h
2. Implementation in refs.c marked static and kept with its documentation
3. Duplicate check removed from files-backend.c's finish phase
4. No behavioral changes - safety is maintained by the prepare-phase check

This is a logical optimization in the series' finalizes the duplicate checking consolidation, removing now-redundant code while maintaining all safety checks. The patch aligns with the overall goal of making reference updates more efficient while preserving atomicity guarantees.