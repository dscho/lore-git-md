Test improvement patch finalizing the Perforce integration test robustness changes. This v4 submission from Anthony Wang represents the culmination of a multi-iteration review process, now consolidated into a single commit that replaces fragile `grep`-based tag verification with explicit `git show-ref --verify` checks.

The patch makes two key changes to t/t9811-git-p4-label-import.sh:
1. Replaces the pipe-and-grep pattern for tag verification with direct `show-ref` checks for both positive cases (TAG_F1_1, TAG_F1_2) and negative cases (TAG_F1_ONLY)
2. Applies the same verification improvement to a second test case that previously used grep

Technical implementation details the future summarizer should note:
- All tag verification now uses `git show-ref --verify` with explicit ref paths
- Negative verification uses `test_must_fail` wrapper
- Removes all pipe operations that could mask exit codes
- Maintains same test structure but with more robust assertions
- Changes affect two test blocks: 'two labels on the same changelist' and 'use git config to enable import/export of tags'

The patch has evolved through four versions incorporating feedback from reviewers (Junio Hamano, Patrick Steinhardt, Eric Sunshine) and now represents consensus on the optimal approach. It demonstrates the thread's progression from initial exit code fixes to a more fundamental improvement in verification methodology. The final form addresses all previously identified issues while maintaining test clarity.