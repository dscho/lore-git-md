This is part 4 of a 6-part series refactoring reflog expiration to remove global state, specifically focusing on making reflog expiration functions publicly available.

The patch moves three key functions from `builtin/reflog.c` to `reflog.[ch]` to make them reusable elsewhere in the codebase:
1. `find_cfg_ent()` - finds or creates a reflog expiration entry for a given pattern
2. `reflog_expire_config()` - handles configuration parsing for reflog expiration
3. `reflog_expire_options_set_refname()` - applies expiration settings to a specific refname

The changes are mechanical - the functions are moved verbatim with only minor adjustments (like renaming the `EXPIRE_*` constants to `REFLOG_EXPIRE_*` for clarity). This is part of the larger effort to make reflog expiration functionality available to the new maintenance task system while eliminating global state.

The patch appears well-executed, maintaining all existing behavior while improving code organization. It follows the incremental refactoring approach discussed earlier in the thread, where Patrick Steinhardt is carefully moving pieces into place for the eventual maintenance task integration. The commit message is clear about the purpose and ties this change to future work in the series.