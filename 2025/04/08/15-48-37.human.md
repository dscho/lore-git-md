This is part of Elijah Newren's ongoing series to fully replace the legacy merge-recursive implementation with merge-ort. The patch focuses on diff algorithm handling during merges.

The change makes merge-ort's diff algorithm selection behavior match merge-recursive's by removing merge-ort's hardcoded histogram diff and allowing other algorithms to be specified. While this technically changes merge-recursive's default algorithm too, the commit notes this is irrelevant since merge-recursive is being removed entirely in subsequent patches.

The documentation updates clarify that both merge strategies now accept the same diff algorithm options, removing some merge-recursive-specific notes that are no longer needed. The implementation is straightforward - removing a hardcoded setting in merge-ort.c while adding a default to merge-recursive.c for consistency during the transition period.

This appears to be a well-considered step in the larger merge-ort migration, maintaining functional parity during the transition while cleaning up the documentation. The change is low-risk since it doesn't modify actual merge behavior, just makes the configuration options more flexible.