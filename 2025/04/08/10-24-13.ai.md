This is part 5/9 of Patrick Steinhardt's refactoring series splitting object-file.c into logical components. The patch focuses on cleaning up flag handling across three related object-file functions: `hash_object_file()`, `write_object_file()`, and `index_fd()`.

The key change separates the previously shared `HASH_*` flags into distinct sets for each function family, eliminating confusion about which flags apply where. The patch:
- Introduces new `INDEX_*` flags for index-related operations
- Creates `WRITE_OBJECT_FILE_*` flags for object writing
- Adds `HASH_OBJECT_*` flags specific to `hash-object` functionality
- Updates all callers across 9 modified files to use the appropriate new flags

The changes are mechanical but extensive, touching core object creation paths. The separation improves code clarity and maintainability by making flag usage more type-safe. No behavior changes are intended - this is purely an internal API cleanup that maintains all existing functionality while better organizing the concerns.

The patch continues the series' pattern of careful, incremental refactoring. Reviewers should verify the flag translations preserve all subtle behaviors, particularly around error handling paths and platform-specific cases. The changes lay groundwork for future object storage backend work by clarifying interfaces between components.