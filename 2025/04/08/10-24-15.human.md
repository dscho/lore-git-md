This is part 7/9 of Patrick Steinhardt's ongoing refactoring series to split Git's object-file subsystem into logically organized components. The patch continues the architectural cleanup by moving object store management functions from object.c to object-store.c.

The changes relocate core object store lifecycle functions including `raw_object_store_new()`, `free_object_directory()`, and `raw_object_store_clear()`. These handle initialization, cleanup, and memory management for the object store and its components like packfiles, commit graphs, and alternate object directories. The patch maintains all existing behavior while better organizing the code by concern.

This follows the series' pattern of systematically separating generic object utilities from storage-specific functionality. The changes are mechanical but important for the larger goal of enabling modular object storage backends. Reviewers should verify the relocated functions maintain all platform-specific behaviors and error handling, particularly around packfile and loose object management.

The patch touches core object storage paths but appears well-contained, moving 67 lines out of object.c while adding 66 to object-store.c with no functional changes. It builds on previous patches that established the object-store.c home for storage-related code.