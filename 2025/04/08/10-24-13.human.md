This is part 5/9 in Patrick Steinhardt's ongoing refactoring series to split object-file.c into more modular components. The patch focuses on cleaning up flag handling for object-file operations.

The change separates the previously shared `HASH_*` flags into distinct sets for different function families: `hash_object_file()`, `write_object_file()`, and `index_fd()`. The author notes the current shared flags cause confusion since each function only supports a subset, and the entanglement makes the code harder to understand and maintain.

The patch introduces new flag namespaces:
- `INDEX_*` flags for index-related operations
- `WRITE_OBJECT_FILE_*` flags for writing objects
- A new enum for `hash-object` specific flags

The changes are mechanical but widespread, touching 9 files with 56 insertions and 28 deletions. The patch appears carefully implemented with thorough updates to all call sites. This cleanup should make the code more maintainable as the object-file refactoring continues, though reviewers will want to verify no subtle flag behavior changes were introduced across the various conversion paths.