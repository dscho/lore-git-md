This is part 2 of a 6-patch series refactoring reflog expiration to remove global state, as part of the broader maintenance system improvements. The patch moves default reflog expiry dates from global variables into the `reflog_expire_options` struct, adding new fields `default_expire_total` and `default_expire_unreachable` with 30/90 day defaults.

The change improves encapsulation by keeping related expiry configuration together in one place, making the code more maintainable and reusable. The patch follows a clean incremental approach - it handles the default expiry dates first, with per-reflog expiry dates to follow in the next commit. The implementation includes a new `REFLOG_EXPIRE_OPTIONS_INIT` macro to initialize the defaults.

This continues the thread's theme of carefully untangling global state while maintaining functionality. The changes are mechanical but meaningful, touching both the reflog implementation and its configuration handling. The patch appears well-constructed with clear commit message explaining the motivation and approach.