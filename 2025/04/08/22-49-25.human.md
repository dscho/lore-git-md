This is a procedural discussion about the `ib/diff-S-G-with-longhand` patch series, which adds long-form options (`--patch-grep` and `--patch-modifies`) as alternatives to the existing `-G` and `-S` diff options.

The thread shows D. Ben Knoble expressing interest in helping move the series forward by splitting cleanup commits from the new feature additions, but raising concerns about modifying commits without original author sign-offs. Junio Hamano confirms that changes without proper DCO sign-offs cannot be accepted, effectively stalling the series until the licensing/certification issues are resolved.

The technical content of the original patch series (which adds the long-form options with documentation and test updates) had previously been reviewed as sound, but the current discussion focuses entirely on contribution protocol rather than the technical merits. This leaves the series in limbo until either the original author provides sign-offs or another contributor finds a way to proceed within Git's contribution guidelines.