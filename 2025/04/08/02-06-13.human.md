This is a discussion about alternative approaches to managing temporary work-in-progress changes, sparked by the earlier thread about enhancing `git stash list` output. Lucas Seiki Oshiro shares a personal workflow for handling long-lived local changes that shouldn't be committed to the main repository.

The email describes an alternative to using `git stash` for persistent local changes: creating a detached HEAD commit with the experimental changes and tagging it, then using `git merge --squash` to apply the changes when needed. This approach keeps the stash stack free for transient work while still maintaining access to the local-only changes.

The context shows this is a response to Junio Hamano's earlier suggestion that stash entries are meant to be temporary, with Lucas providing a concrete example of when someone might want more permanent (but still local) storage of work-in-progress. The solution presented leverages Git's commit and tag system rather than modifying stash behavior.

This appears to be a useful side discussion about workflow alternatives rather than a continuation of the technical debate about stash metadata. The tone is conversational and shares practical experience rather than proposing code changes.