This is the first patch in a series removing the legacy merge-recursive implementation in favor of merge-ort, representing a major architectural transition in Git's merge machinery.

The patch switches `builtin/checkout.c` from using `merge_trees()` (from merge-recursive) to `merge_ort_nonrecursive()`. The change is straightforward - updating the include and function call while maintaining the same parameters and error handling. The diff shows a simple 5-line change with no behavior modification, just a direct substitution of the merge backend.

As part of the larger series, this initial patch handles one of the remaining callers that needed conversion before merge-recursive could be fully deleted. The commit message is brief since the context is well-established in the cover letter and previous discussion. The technical implementation appears sound, with the simple function swap being a mechanical change that shouldn't introduce new issues.

Key technical details for future reference:
- Updates checkout's merge operation to use merge-ort
- Changes include from "merge-recursive.h" to "merge-ort-wrappers.h"
- Swaps `merge_trees()` for `merge_ort_nonrecursive()` with identical parameters
- Maintains same error handling (exit 128 on failure)
- Part of systematic conversion before final merge-recursive removal

This patch represents a low-risk transition point since checkout's merge behavior has already been verified to work correctly with merge-ort through previous testing. The change is properly contained to the checkout command's merge functionality.