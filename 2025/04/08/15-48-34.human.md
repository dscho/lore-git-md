This is the third version of a major refactoring series that completes the multi-year effort to replace the legacy merge-recursive implementation with merge-ort. The series now stands at 8 patches, with only minor documentation clarifications since v2.

The cover letter humorously frames the removal of merge-recursive as "debugging" (deleting) problematic code, while seriously noting that merge-ort has been the default since Git 2.33.0 and offers better correctness, performance, and extensibility. The series systematically converts all remaining callers to merge-ort, moves shared code between the implementations, then removes merge-recursive entirely along with its test infrastructure.

Key technical aspects:
- Patches 1-5 prepare for removal by switching remaining callers (in checkout, merge, and sequencer) to merge-ort equivalents
- Patch 6 is the main event - deleting merge-recursive.[ch] after moving shared code to merge-ort
- Patches 7-8 clean up now-unneeded test scaffolding (GIT_TEST_MERGE_ALGORITHM)

While the diffstat appears large (+538/-5241 lines), most changes are straightforward deletions and test updates rather than complex logic changes. The maintainer has indicated the technical implementation is sound, with discussion now on how best to document this significant architectural shift. This version addresses prior feedback by clarifying documentation to better capture both the technical motivations (bug fixes, performance) and historical context of the transition.

This represents the culmination of a major multi-year effort to modernize Git's merge machinery, with merge-ort now standing as the sole implementation. The changes are well-vetted through prior incremental adoption, making this final removal low-risk despite its significance.