Refactoring patch in the reflog-expire maintenance task series. This is patch 5/6 in Patrick Steinhardt's series to add reflog expiration as a maintenance task.

The patch extracts the reflog expiration logic from `cmd_gc()` into a new `maintenance_task_reflog_expire()` function, making it reusable by both `git gc` and the upcoming maintenance task. The change is straightforward - it moves the child process setup and execution into a dedicated function while maintaining identical behavior. 

This continues the series' pattern of restructuring reflog expiration code for better sharing between components. The implementation remains simple, just running `git reflog expire --all` as before, but now in a more modular way. The patch removes the now-unused `reflog` strvec while keeping the other command strvecs (repack, prune) as they're still used directly in `cmd_gc()`.

This appears to be a clean, focused change that advances the series' goal of making reflog expiration available as a maintenance task while reducing code. It follows naturally from the earlier refactoring work in patches 1-4.