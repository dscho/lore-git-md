This is a consolidated test improvement patch for Git's Perforce integration tests, now using `git show-ref --verify` for more robust tag verification. Anthony Wang has refined this patch through four iterations based on mailing list feedback, with the latest version combining three previous commits into one focused change.

The patch modifies t/t9811-git-p4-label-import.sh to replace grep-based tag checks with direct reference verification using `git show-ref --verify`, which prevents false positives and makes the test more reliable. It also adds explicit checking that an unexpected tag (TAG_F1_ONLY) does not exist, using `test_must_fail`. The changes follow Junio Hamano's suggestion to fundamentally improve the verification approach.

The evolution of this patch shows the Git community's collaborative refinement process - starting with exit code handling improvements, then incorporating feedback about test debuggability and grep usage, before arriving at the current show-ref methodology. Anthony has been responsive to feedback throughout, with v4 consolidating the changes into a single commit now that the approach has stabilized.

This appears to be a well-considered improvement to test robustness, with clear commit messages documenting both the technical changes and the rationale behind them. The patch addresses a real testing weakness (potential false positives in tag verification) while maintaining the test's original purpose of validating Perforce label import behavior.