This is a refactoring patch in Karthik Nayak's batched reference updates series, now in its sixth iteration. The change removes redundant duplicate-checking logic in the files backend's reference transaction preparation code.

The patch eliminates an unnecessary second check for duplicate refnames in `split_symref_update()`, since the first check via `string_list_has_string()` already guarantees uniqueness. This leads to simplifications in three functions (`split_head_update()`, `split_symref_update()`, and `files_transaction_prepare()`) where the redundant `item->util` assignments and checks are removed.

The change is straightforward and well-justified, with the commit message clearly explaining the reasoning behind removing what was effectively dead code. As part of a mature series that has undergone extensive review, this cleanup aligns with the overall goal of the reference transaction infrastructure while maintaining all existing functionality. The patch touches only the files backend implementation, reducing code by 17 lines while adding 3.