This is a refactoring patch that's part of a series to add header-check functionality to meson builds. The patch focuses on variable naming improvements in the Coccinelle build configuration.

The change renames two generic variables (`sources` and `headers`) in `contrib/coccinelle/meson.build` to be more specific (`coccinelle_sources` and `coccinelle_headers`). This prevents potential naming collisions since meson makes subdirectory variables available at the top level. The patch also standardizes empty list notation to match other parts of the codebase.

The changes are mechanical - updating variable names throughout the file and adjusting the empty list syntax. This appears to be a straightforward cleanup with no behavior changes, preparing for subsequent patches in the series that will implement the header-check target. The patch follows established meson build patterns and maintains consistency with the existing code structure.

Key technical details for future reference: variables renamed from `sources/headers` to `coccinelle_sources/headers`, changes limited to `contrib/coccinelle/meson.build`, uses meson's project_source_root() and custom_target() infrastructure. This is a low-risk change that shouldn't affect build behavior, just variable scoping.