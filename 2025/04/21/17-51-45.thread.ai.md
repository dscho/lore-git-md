Thread identity: Refactoring of Git's Meson build system to standardize function availability checks, part 2 of a 6-patch series.

Core problem: The meson.build file currently handles function availability checks in an ad-hoc manner. This series consolidates these checks into a maintainable array-based approach, with this patch specifically addressing `getpagesize()`.

Current status: Second patch in series adds `getpagesize()` to the centralized checking system. No reviews or acks yet. Series version still not indicated (no cover letter seen).

Key technical details:
- `checkfuncs` array now includes: strcasestr, memmem, strlcpy, strtoull, setenv, mkdtemp, initgroups, mmap, unsetenv, getpagesize
- `getpagesize` marked as having no compatibility implementation (like initgroups)
- Handles POSIX 2001 deprecation of `getpagesize()`
- References existing QNX-specific handling in config.mak.uname
- Preserves special cases: sync_file_range (HAVE_ prefix), Windows MMAP handling
- Still excludes some functions (strdup, strtoumax, qsort, getdelim, clock_gettime) due to unique requirements

Open questions:
- Why certain functions remain outside the system (commit mentions "varying macro naming conventions" but doesn't detail)
- Whether remaining individual checks will be addressed in later patches
- Why `getpagesize` wasn't included in initial array (possibly newly surfaced portability concern)
- Performance impact of new approach (likely negligible)

Participant positions: No reviews or feedback yet (second patch).

Related work: Build system modernization effort (Meson/CMake alongside traditional Makefile).