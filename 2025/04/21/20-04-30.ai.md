This is a follow-up discussion in the Meson build system refactoring thread, specifically addressing the first patch in the series that standardized function availability checks. The email raises several technical questions about implementation details and consistency in the build system.

Eli Schwartz follows up on their own patch with additional observations about inconsistencies in how function availability is handled across different platforms. The key points raised are:

1. Questions why `qsort()` checks remain platform-specific rather than being standardized like other functions, noting that:
   - The Makefile has special cases for AIX and MinGW due to performance issues
   - CMake unconditionally defines `INTERNAL_QSORT`
   - Suggests encoding known slow platforms rather than checking

2. Points out inconsistencies between `HAVE_XXX` and `NO_XXX` macro patterns, asking for clarification on when each is used

3. Notes that some functions (like `getdelim` and `clock_gettime`) use `HAVE_` macros while others use `NO_` macros, questioning the rationale

The email doesn't propose specific changes but seeks clarification to better understand the build system's design decisions. These questions could inform future refinements to the standardization effort, though they don't directly impact the current patch series which focuses on consolidating the checking mechanism rather than changing the underlying platform-specific behaviors.

This follow-up highlights some of the historical quirks in Git's build system that the Meson port is inheriting, and may prompt discussion about whether to maintain these inconsistencies or address them in future work. The thread currently has no responses to any of the patches in the series.