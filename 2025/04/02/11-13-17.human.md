This is a technical discussion about pointer comparison in the pack-bitmap code, part of Patrick Steinhardt's ongoing series to enhance Git's cat-file batch mode filtering.

The exchange focuses on whether comparing packfile pointers directly (rather than by some other identifier) is safe. Toon Claes raised a concern about this practice, particularly wondering if it might cause issues in a future where Git supports multiple `struct repository` instances pointing to the same underlying repository. 

Patrick responds that this shouldn't be problematic in practice, explaining that (1) Git currently never creates multiple repository instances for the same repo, and (2) even if it did, the pointer comparison would still work correctly across different repositories since a packfile from one repository shouldn't be indexed by another.

This discussion represents the last remaining technical hurdle in the series, as noted in the thread summary. The pointer comparison question appears to be the final point needing resolution before the patches can proceed. Patrick's response suggests the current implementation is sound, though the thread leaves open whether Toon will require further clarification or if this satisfies the concern.