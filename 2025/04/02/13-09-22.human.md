This is a follow-up discussion in the rebase status reporting improvements series, specifically about patch 3/3 regarding merge instruction messaging.

Phillip Wood clarifies Johannes Schindelin's question about authorship preservation during rebase operations. While Johannes had wondered if `git commit` retains authorship for interrupted `pick` operations (implying merges might do the same), Phillip confirms that `git commit` actually always loses authorship information when used during rebase, whether for picks or merges. 

The exchange surfaces an underlying inconsistency in Git's behavior - there's no special case where authorship is preserved when using `git commit` directly versus `git rebase --continue`. This doesn't affect the correctness of Philippe Blain's original patch (which improves the rebase status messages to suggest `rebase --continue` rather than `commit`), but highlights a broader design question about authorship preservation that might warrant future attention.

The discussion remains technical and collegial, with Phillip providing a definitive answer to Johannes' query while implicitly confirming that the current patch's approach remains the best available solution. This appears to be a minor clarification point rather than a blocker for the series, which Phillip Wood had already approved in his previous review.