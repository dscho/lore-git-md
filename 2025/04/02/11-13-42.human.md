This is a refactoring patch in the Git cat-file batch mode filtering enhancement series (v3 7/11). It modifies the `show_reachable_fn` callback interface in the bitmap code to accept a payload parameter, which will be needed for upcoming filter support.

The patch makes a straightforward change to the callback signature used throughout the bitmap codebase, adding a `void *payload` parameter. All existing callers are updated to pass NULL for now, maintaining current behavior. The changes touch several key files including `pack-bitmap.c`, `builtin/pack-objects.c`, and `builtin/rev-list.c`.

This appears to be preparatory work for the filter functionality that will be built in subsequent patches. The implementation is clean and mechanical, with each callsite consistently updated. The patch follows the series' established pattern of incremental changes, though it doesn't yet demonstrate the payload's eventual use - that presumably comes later in the series.

The change is well-contained and unlikely to be controversial, serving as infrastructure for the more substantive filter work that follows. It maintains compatibility with existing code while enabling future functionality.