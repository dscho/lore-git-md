Here's the human-readable summary incorporating the maintainer's response:

The thread began with Andrej Zhilenkov asking about Git's behavior when toggling the `core.symlinks` configuration. He observed that changing this setting doesn't automatically convert existing symlinks to regular files (or vice versa) during checkout or reset operations, requiring manual workarounds like checking out a pre-symlink commit or removing and re-checking out files.

Brian m. carlson, responding as a maintainer, clarified that this is intentional behavior. He explained that `core.symlinks` - like `core.filemode` and `core.ignorecase` - is designed as a one-time hint about filesystem capabilities during repository creation, not as a runtime-switchable setting. The lack of automatic conversion between symlink and regular file representations when the setting changes is by design.

While this answers the immediate question about expected behavior, the thread remains open for further discussion. Brian's response invites elaboration on the underlying use case, suggesting potential avenues like better documenting this behavior or reconsidering the design if compelling real-world needs emerge. The original workarounds remain the practical solution for now, though their appropriateness wasn't explicitly confirmed or rejected in the response.