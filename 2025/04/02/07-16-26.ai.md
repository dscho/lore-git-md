This is a minor correction in Patrick Steinhardt's ongoing series to remove Perl dependencies from Git's test suite (now at 20 patches). The exchange clarifies a small discrepancy in patch 14/20's commit message regarding the tool used to refactor the `sanitize_pgp()` test helper.

The original patch converted the PGP signature sanitization from Perl to awk, but during review Johannes Schindelin suggested using `sed` instead - an improvement Patrick acknowledges as making the logic clearer. The current email simply notes this correction in the commit message, confirming the final implementation uses `sed` rather than awk as originally stated.

This is a trivial documentation fix in an otherwise technically sound patch that has already received positive feedback. The change doesn't affect the series' progress or the broader goal of eliminating Perl from test helpers, but ensures accurate records of the implementation approach. The thread shows the typical collaborative refinement process where small optimizations emerge during review.