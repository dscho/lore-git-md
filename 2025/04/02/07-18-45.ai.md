This is a question about Git behavior regarding symlink handling when `core.symlinks` configuration changes. The user Andrej Zhilenkov is asking whether there's a direct way to convert between symlinks and regular files when toggling the `core.symlinks` setting, having found that standard commands like `checkout` and `reset` don't automatically update the filesystem representation.

The email demonstrates through a test case that:
1. After creating a symlink in a repo with `core.symlinks=true`, toggling to `false` doesn't automatically convert the symlink to a regular file
2. The only working approaches are either (a) checking out a commit before the symlink was introduced and then checking out again, or (b) manually removing and re-checking out the symlink
3. The test script shows the filesystem state at each step, confirming that `checkout` and `reset` preserve the existing symlink state regardless of `core.symlinks`

Key technical details:
- The test case creates a simple repo with one regular file and one symlink
- Uses `ls -l` to verify filesystem representation
- Demonstrates the behavior with `core.symlinks=false` specifically
- Shows the workaround of manual removal+checkout does convert the symlink to a regular file

This appears to be either a documentation gap (if this is expected behavior) or potentially a bug/limitation in Git's handling of symlink state changes. The question is framed as seeking confirmation of whether the manual workaround is indeed the correct approach or if there's a better method.