This is a follow-up discussion in the bugfix thread addressing `--left-only`/`--right-only` behavior with unrelated histories. Phillip Wood provides test infrastructure guidance to Matt Hunter, explaining why counting commit IDs directly in tests can be unreliable due to Git's test harness design.

The email clarifies that tests in a file share the same repository state for performance reasons, making absolute commit counts unpredictable across tests. Phillip explains the standard pattern where an initial setup test creates tagged commits that subsequent tests reference, allowing tests to establish known states without worrying about the total commit count.

This continues the thread's theme of improving test robustness, though it's more about general test methodology than the specific bug being fixed. The exchange shows how experienced contributors help newcomers navigate Git's testing conventions while maintaining the project's high standards for reliable verification.