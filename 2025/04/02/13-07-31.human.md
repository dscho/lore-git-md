This is the final test portability fix in the `git blame` porcelain output series. Phillip Wood identifies and resolves a macOS compatibility issue with the `sed` commands used in the test scripts. The problem stems from how different `sed` implementations handle newline insertion when prepending "unblamable"/"ignored" markers before specific lines in the test output.

The discussion reveals that while Phillip initially suggested using `sed`'s `i` command, this approach fails on macOS due to stricter parsing requirements. Karthik Nayak then proposes using a heredoc to provide the `sed` script, which proves to be portable across platforms. Phillip agrees this is the best solution, noting it allows proper indentation of the inserted lines while maintaining compatibility.

This marks the last remaining issue in the long-running thread about properly marking ignored/unblamable lines in `git blame`'s porcelain output. With this fix, the series now has fully portable test scripts using literal tabs and POSIX-compliant `sed` commands, addressing all identified platform-specific concerns. The solution maintains the careful balance between test accuracy and cross-platform compatibility that the Git project requires.