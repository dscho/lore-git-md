The thread has evolved into a nuanced philosophical debate about Git's design principles regarding metadata recording. Junio Hamano framed the discussion by arguing that Git intentionally avoids recording metadata (like file renames or Change-IDs) not just for simplicity, but for correctness - preferring dynamic heuristics that can improve over time rather than baking potentially incorrect assumptions into history. He used file rename detection as a concrete example where recorded metadata could be wrong while heuristics remain flexible.

Nico Williams has now responded by sharpening the philosophical counterpoint. He distinguishes Change-IDs from the file rename analogy, arguing that when users can explicitly record their intent (as with Change-IDs), this human-provided metadata is inherently more valuable than heuristics - even if imperfect. Nico suggests Git's original avoidance of metadata might have been more pragmatic than philosophical, opening the door to reconsidering such cases where user intent can be captured.

The core tension remains unresolved: Junio maintains that avoiding recorded metadata preserves Git's ability to correctly derive relationships over time, while Nico argues user-provided metadata like Change-IDs represent valuable intent that heuristics can't replicate. The discussion has reached a point where proponents would need to either demonstrate why Change-IDs differ fundamentally from other metadata Git avoids, or propose safeguards that address the correctness concerns while preserving the workflow benefits.