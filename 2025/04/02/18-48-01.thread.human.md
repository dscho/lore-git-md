The thread explores how Git could natively track patch series relationships, with two competing proposals now on the table: the Change-ID approach and Theodore Ts'o's newer Patch Set ID system. Ts'o's proposal introduces a `--series` flag for `git commit` that establishes explicit parent-child relationships between patches, with IDs preserved across rebases and cherry-picks while maintaining email workflow compatibility.

Junio Hamano has responded favorably to the technical merits of Ts'o's approach, noting how it cleanly handles both single-commit and multi-commit series through the same mechanism. The proposal satisfies several key requirements previously raised, particularly around native command integration and clear relationship semantics. However, Hamano identifies one remaining gap: while the system can track commit lineages, it lacks robust metadata to determine the temporal ordering of independent rewrites or distinguish between parallel development branches sharing the same Patch Set ID. This history-tracing limitation may need addressing before final adoption.

The discussion has progressed from initial problem analysis through to evaluating concrete technical solutions. With both Change-IDs and Patch Set IDs now proposed, the thread is entering a phase of comparative evaluation - weighing explicit relationship marking against automatic footer generation, considering migration paths for Gerrit users, and examining how each approach meets the maintainer's architectural constraints. The Patch Set ID proposal appears to be gaining traction as a potentially cleaner technical solution, though its history-tracing limitations and interaction with existing systems remain open questions.