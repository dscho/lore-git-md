Here's the human-readable summary of the thread's current state:

The discussion about standardizing change IDs in Git has deepened to examine how these identifiers should behave across branches and during history-rewriting operations. The core tension revolves around whether change IDs should uniquely identify a single commit repository-wide (which breaks when commits are cherry-picked or rebased) or whether multiple commits can share an ID when they represent the "same" logical change in different contexts.

Martin von Zweigbergk's latest contribution clarifies that while duplicate change IDs can technically exist in a repository (especially after cherry-picking), in practice most workflows only have one visible commit per ID at any time since rewritten commits become unreachable. He notes Git's current behavior of not automatically updating refs during rebases limits change IDs' usefulness for history-rewriting operations, though they remain valuable for read-only commands.

The thread has identified several key challenges that need resolution: how change IDs should propagate during cherry-picks (preserved vs regenerated), whether branch context should limit ID resolution, and how to handle the semantic tension between change IDs as unique identifiers versus markers of logical change groups. These questions are particularly relevant for workflows involving frequent history rewriting or parallel development across multiple branches.

Participants are converging on the understanding that any standardization will need to explicitly define behavior for edge cases like rebases and cherry-picks, possibly through metadata headers or secondary ID schemes. The discussion remains productive, with clear recognition that different tools (Gerrit, Jujutsu) and workflows will need to be accommodated in whatever solution emerges.