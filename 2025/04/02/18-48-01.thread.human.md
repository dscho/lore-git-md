Here's a human-readable summary of the thread's current state:

The discussion about standardizing Change-IDs continues, now with security considerations coming to the forefront. The thread began by examining Jujutsu's implementation, which uses randomly generated 16-32 byte values as Change-IDs, intentionally breaking content-based matching when commit messages are modified. Martin von Zweigbergk had explained how this approach supports Jujutsu's workflow commands like `jj describe` while proving reliable in production use.

Brian m. carlson has now raised significant security concerns about the random generation approach. They argue that Git's deterministic nature is important for features like stash operations (referencing their own upcoming work), and that random Change-IDs could create security side channels that might leak sensitive data. They propose alternatives: either a SHA-256 hash of commit data or HMAC-SHA-256 with a fixed key, plus a configuration option to disable Change-ID generation entirely for privacy-conscious users.

This shifts the discussion's focus from workflow compatibility to security implications, adding new constraints to consider. While Jujutsu's random approach has proven practical in their system, the security arguments suggest it may not translate well to Git's broader ecosystem. The thread now balances three competing concerns: Jujutsu's working implementation, cross-tool compatibility, and these new security considerations. The path forward will need to address all three dimensions while maintaining the functionality that makes IDs valuable for distributed workflows.