This thread continues to explore the standardization of Change-IDs in Git commit messages, with the discussion now focusing on how different forges implement version tracking. The core tension remains between explicit Change-ID headers (proposed as a standard) and existing server-side tracking mechanisms like GitLab's internal reflog system.

Martin von Zweigbergk previously highlighted implementation differences between Gerrit's strict (change id, branch) tuple requirement and more flexible PR-style forges. Now Nico Williams analyzes GitLab's approach, which uses similarity detection rather than Change-IDs to track commit versions. This reveals two competing models: one requiring user-supplied metadata (Change-IDs) versus automated server-side tracking. Williams argues the latter provides better UX by eliminating manual steps while achieving similar goals.

Junio Hamano's earlier semantic concerns about Change-ID reuse now appear in a new light, as GitLab's system demonstrates that version tracking can work without explicit IDs. The thread has evolved from theoretical discussions to examining real-world implementations, with GitLab's production system showing one viable alternative. Key open questions remain about whether standardization should proceed given these existing solutions, and whether Change-ID proposal offers enough additional value to justify the user burden. The community appears to be converging on waiting for more real-world data, particularly from Jujutsu's upcoming default-enabled status and GitButler's existing usage patterns, before finalizing any standards.