This thread continues the technical exploration of implementing change IDs in Git, with Patrick Steinhardt and Elijah Newren driving the discussion toward concrete implementation details. The core idea is to provide stable identifiers that survive history rewrites, making workflows like rebase and cherry-pick more manageable by avoiding the need to track changing commit hashes.

Patrick clarifies that change IDs would primarily help by providing constant references during operations that rewrite history. Rather than tracking new object IDs after each rebase, users could rely on persistent change IDs. The discussion has expanded to consider which Git commands would need change ID support, with `commit --amend`, `fast-export`, and `fast-import` now added to the list. Patrick notes that some infrastructure for handling extra commit headers already exists and could be leveraged, though there may be bugs in the sequencer code that would need fixing.

The conversation has turned to the syntax for specifying change IDs in revisions, with Patrick suggesting an extension of Git's existing `^{/<text>}` syntax, possibly with a prefix to avoid collisions with refnames. While he prefers Jujutsu's reverse hex style for change IDs, the discussion remains grounded in Git's existing conventions and technical constraints.

The thread has moved from philosophical debates about change ID semantics to practical questions of implementation: which commands need support, how to handle persistence during rewrites, and how users will reference change IDs. The participants are working through these technical details while maintaining awareness of the broader cross-tool compatibility goals that initiated the discussion.