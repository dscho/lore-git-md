API refinement for reftable block iteration. This is part of Patrick Steinhardt's ongoing reftable refactoring series (patch 14/16), which aims to improve the API for external consumers and enable future consistency checks.

Justin Tobler reviews the new table block iterator interface, suggesting a naming tweak to align with existing conventions. The iterator provides lower-level access to reftable blocks, which will be used both for debugging output and upcoming consistency verification features. The implementation adds 172 lines across header, implementation, and test files.

The review is positive overall, focusing on a minor naming consistency point - Justin questions why the initialization function is named `reftable_table_init_table_iterator` rather than following the more common `reftable_table_iterator_init` pattern seen elsewhere in the codebase. This appears to be the only substantive feedback on this patch in the thread so far.

The patch continues the series' pattern of careful API improvements that maintain functionality while making the reftable implementation more maintainable and usable by external projects. The thorough test coverage (109 lines of new unit tests) suggests this is a well-considered change.