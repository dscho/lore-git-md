This is a follow-up in the ongoing cross-tool standardization effort for commit-level change IDs, specifically addressing the technical implementation details. Martin von Zweigbergk corrects a subject line typo (changing "footer" to "header") to properly reflect that the proposed `change-id` would live in commit headers rather than footers.

The email itself is very short, serving primarily as a correction to the previous discussion's framing. The key technical point reinforced here is that the proposed standard would store change IDs in commit headers, distinguishing it from b4's approach which uses email footers for series-level tracking. This header placement aligns with Jujutsu's existing implementation and the Gerrit design doc's approach.

For future reference in this thread, the header-vs-footer distinction becomes an additional technical consideration alongside the previously established dimensions of granularity (commit vs series), format (structured encoding vs arbitrary strings), and persistence (commit objects vs email metadata). The correction implies the standard is leaning toward deeper integration with Git's object model rather than email-based workflows.

No new technical proposals or alternatives are introduced in this message - it simply clarifies an important implementation detail that may affect how tools would need to adapt their existing change-id handling mechanisms. The core questions about backward compatibility and migration paths for tools like b4 remain open.