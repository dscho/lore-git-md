This is a question about Git's handling of symlinks when `core.symlinks` configuration changes. Andrej Zhilenkov asks whether there's a straightforward way to convert between symlinks and regular files after toggling the `core.symlinks` setting, noting that standard commands like `checkout` and `reset` don't automatically update the symlink state.

The email demonstrates through a test script that:
1. Git maintains symlinks as-is after `core.symlinks` changes unless explicitly removed
2. The only current solutions are either impractical (knowing the exact commit history) or manual (removing and re-checking specific files)

The question appears well-formed with clear reproduction steps, suggesting a genuine workflow challenge rather than a misunderstanding. This seems like a legitimate gap in Git's functionality that might warrant either documentation clarification or a new feature to handle symlink conversion. The thread is just starting, so we may see responses suggesting workarounds or discussion about potential solutions.