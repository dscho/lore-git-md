This is a style discussion in the ongoing "Git cat-file batch mode filtering" series.

Patrick Steinhardt responds to Toon Claes's suggestion about naming conventions in the bitmap code. Claes had proposed adding a `_fn` suffix to the `show_reach` parameter for better readability, since it's a function pointer being passed to `show_objects_for_type()`.

Steinhardt declines the suggestion, noting that the codebase doesn't use this suffix elsewhere when accepting `show_reachable_fn` parameters, and prefers to maintain consistency with existing patterns. This is a minor stylistic point that doesn't affect the core functionality of the patch series, which remains focused on performance improvements to object filtering.

The exchange shows the careful attention to detail in the review process, even for non-functional aspects of the code. This particular discussion appears resolved, with Steinhardt opting to keep the current naming convention. The more substantial technical questions about pointer comparison and type safety mentioned in the thread remain open.