This is a review comment on a refactoring patch from the performance optimization series for MIDX and cruft pack interaction.

Junio Hamano is reviewing Taylor Blau's first patch in the v2 series, which standardizes how `pack-objects` handles incompatible command-line options. The patch replaces custom error checks with the standard `die_for_incompatible_opt2()` function, bringing consistency to error messages while maintaining the same functionality.

Key technical details:
- Changes affect `builtin/pack-objects.c` error handling
- Updates test expectations in `t/t5331-pack-objects-stdin.sh` to match new standardized error message format
- Preserves option ordering in error messages for consistency
- No behavior change beyond error message formatting

The review is positive, noting the reversed check order doesn't affect correctness while maintaining message consistency. This is a straightforward preparatory change that's part of the larger series, with no direct connection yet to the cruft pack/MIDX optimizations that come in later patches. The change appears uncontroversial and well-executed, with proper test updates.