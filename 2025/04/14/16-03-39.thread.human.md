This thread discusses a 3-patch series (currently at v2) that enhances Git's promisor-remote protocol to support configurable validation of remote attributes during clone and fetch operations. The changes allow servers to advertise specific fields (currently limited to "partialCloneFilter" and "token") while clients can verify these fields, all while maintaining backward compatibility.

The series has addressed key concerns from Junio C Hamano and Patrick Steinhardt about protocol design and security, including refactoring the internal representation to use string_list with strict field validation. The implementation now properly handles case sensitivity, memory management, and cleanup logic while maintaining comprehensive test coverage.

In the latest discussion, Christian Couder and Patrick Steinhardt debated how to handle unknown fields for backward compatibility. They agreed that clients should silently ignore unknown fields rather than failing, though this approach may limit future protocol evolution. This conservative strategy ensures existing clients continue working while allowing servers to support both old and new clients simultaneously.

The series appears technically complete with all major feedback incorporated. It now awaits Junio's final review before potential merging. The changes represent a constrained, security-conscious evolution of the promisor-remote protocol that solves immediate needs while leaving the door open for future extensions through documented processes.