Here's the updated human-readable thread summary:

This thread covers a 3-patch series (now at v2) that enhances the promisor-remote protocol to support configurable validation of remote attributes during clone and fetch operations. The changes allow servers to advertise specific fields (currently limited to "partialCloneFilter" and "token") while maintaining backward compatibility. The protocol design has been carefully constrained to address security concerns, with clients silently ignoring unknown fields.

The series has evolved through review feedback from Junio Hamano and Patrick Steinhardt, particularly around protocol design and field handling. Key technical decisions include using a unified `struct promisor_info`, case-insensitive field name comparisons (while keeping values case-sensitive), and maintaining strict backward compatibility. The implementation includes both server-side configuration (`promisor.sendFields`) and client-side validation (`promisor.checkFields`), with comprehensive test coverage.

Christian Couder recently clarified that a previously discussed case sensitivity issue has been fully resolved in v2, though the cover letter hadn't been updated to reflect this. The series now awaits Junio's final review before potential merging. While the current solution is deliberately limited to two fields, the design leaves room for future protocol evolution if needed.