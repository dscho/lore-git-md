This appears to be a bug report regarding Git's handling of UTF-8-BOM encoding on Windows. The reporter (messester) observes that when cloning a specific repository (infinilore.cs), Git shows all files as modified, with the encoding changing from UTF-8-BOM to plain UTF-8. The issue occurs with Git for Windows v2.49.0.windows.1 and appears related to the Windows-specific configuration (core.autocrlf=true, core.symlinks=false).

Key technical details for future reference:
- The problem manifests as spurious modifications showing in `git status` after clone
- The encoding change is visible in text editors that show BOM markers
- The reporter's config shows Windows-specific settings that may interact with text processing
- The test case repository is https://github.com/NinekoTheCat/infinilore.cs.git
- No patch is proposed - this is purely a bug report seeking explanation/solution

The issue seems related to Git's text processing pipeline on Windows, potentially involving the interaction between BOM handling and line ending conversion (core.autocrlf). The reporter provides their full Git config which includes several Windows-specific settings that could be relevant to text processing behavior. This appears to be a genuine bug report worth investigating, though the exact mechanism isn't clear from the report alone.