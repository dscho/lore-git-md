This is a code style discussion about initialization patterns for structs, specifically regarding the `OBJECT_INFO_INIT` macro in `object-store-ll.h`. The email is Junio Hamano's review response to Taylor Blau's patch that added documentation about designated initializers.

Junio questions the usefulness of the current documentation wording, which warns against adding non-zero default fields because it might break existing designated initializers. He suggests two alternative approaches: either recommending developers always use the `FOO_INIT` macro followed by explicit member assignment, or providing clearer guidance about the zero-initialization contract if designated initializers are to be used.

The discussion revolves around maintainability tradeoffs - whether to enforce a specific initialization pattern through documentation (Junio's preference) versus leaving the door open for designated initializers while warning about the constraints (Taylor's current approach). Junio's proposed wording would make the guidance more prescriptive and actionable for future developers.

This is part of Taylor Blau's larger performance optimization series (patch 2/8), but stands as an independent code documentation discussion. The thread context shows this series has already received positive review on its first patch, with this being an early review comment on implementation details. The discussion doesn't block the overall series but refines a documentation point that affects long-term code maintainability.