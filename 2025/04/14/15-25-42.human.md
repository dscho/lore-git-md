This is a documentation suggestion in the ongoing `--no-hooks` discussion. Lucas Seiki Oshiro observes that since `core.hooksPath=/dev/null` is already a working method to disable hooks (with test coverage since commit c8f6478), perhaps the solution would be to document this approach rather than adding new `--no-hooks` functionality.

The email directly responds to Phillip Wood's earlier questioning of the need for a global `--no-hooks` option, suggesting that existing functionality may already meet the need if properly documented. This aligns with Wood's position that new bypass mechanisms may not be necessary, though Oshiro takes a more constructive approach by proposing specific documentation improvements rather than opposing the feature.

The suggestion introduces a new angle to the thread's technical discussion - that some of the debate might be avoided by better surfacing existing capabilities. However, it doesn't address the npm/husky workflow pain points raised earlier in the thread, which may require more than just documentation to solve.