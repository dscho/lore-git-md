This is a documentation discussion in the ongoing thread about hook disabling mechanisms. Brian M. Carlson responds to Lucas Seiki Oshiro's suggestion to document the existing `/dev/null` workaround for disabling hooks.

Carlson takes a pragmatic stance, suggesting that if Stolee's `--no-hooks` patch series proceeds, its documentation should remain part of that series since it provides a clearer interface. However, if the series is rejected, he agrees that documenting the `/dev/null` workaround (which already has test coverage) would be valuable as a standalone documentation patch.

The email doesn't introduce new technical arguments but helps frame the documentation question as dependent on the core decision about whether to implement the new `--no-hooks` feature. It shows the thread is now considering both code and documentation solutions in parallel, with participants weighing in on how best to guide users regardless of the implementation path.