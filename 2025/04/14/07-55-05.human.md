Platform compatibility patch in the build system alignment series, addressing Cygwin timing functions.

This is a small but precise follow-up to Ramsay Jones's earlier patch adding `clock_gettime()` support for Cygwin builds. Patrick Steinhardt points out a minor formatting nit - suggesting adding spaces around the assignment operators (`=`) for consistency with surrounding code. The change itself (adding `HAVE_CLOCK_GETTIME` and `HAVE_CLOCK_MONOTONIC` for Cygwin) was already accepted in the series, so this is purely about code style alignment.

The patch fits into the broader series' goal of standardizing platform-specific build configurations, particularly for Cygwin. While trivial, it demonstrates the attention to detail being applied throughout this build system cleanup effort. No functional changes are proposed here - just a formatting tweak to match project conventions.