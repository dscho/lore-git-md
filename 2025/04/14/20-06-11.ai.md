This is a refactoring patch that's part of Taylor Blau's v2 series optimizing MIDX and cruft pack interactions during repacking. As the first patch in the series, it focuses on code cleanup rather than the core optimization work.

The patch standardizes how `pack-objects` handles incompatible command-line options by replacing custom error checks with calls to `die_for_incompatible_opt2()`. This change affects several option pairs including `--exclude-promisor-objects`/`--exclude-promisor-objects-best-effort`, `--keep-unreachable`/`--unpack-unreachable`, and `--filter`/`--stdin-packs`. The standardization ensures consistent error message formatting across all incompatible option combinations.

Key technical details include:
- Updates to `builtin/pack-objects.c` replacing manual checks with the standard helper function
- One test adjustment in `t/t5331-pack-objects-stdin.sh` to match the new error message format
- Maintains all existing functionality while improving code consistency
- Part of the preparatory work for later patches in the series that will use these option checks

The change is straightforward and low-risk, serving as groundwork for the more substantial MIDX/cruft pack optimizations that follow in the series. The patch demonstrates good attention to detail by including the necessary test updates and following Git's established patterns for option validation.