This is the third patch in a series implementing header checking functionality in Meson builds, matching the Makefile's 'hdr-check' capability. The patch adds the core implementation for verifying that individual header files can be compiled independently in the Meson build system.

The implementation handles several technical challenges: it properly excludes generated headers and platform-specific files, adds necessary include paths for out-of-tree builds, and conditionally skips crypto backend headers that aren't in use. The patch introduces a `generated_headers` list (renamed from `headers_generated` per earlier feedback) and uses Meson's `custom_target()` to create temporary compilation units for each header.

Notable technical details include the two-stage compilation process (creating .cc files then compiling them), proper handling of exclusions (now using directory patterns as suggested in reviews), and the use of `fs.replace_suffix()` for clean filename handling. The patch maintains compatibility by providing both 'hdr-check' (as an alias) and the newly agreed-upon 'check-headers' target name.

This appears to be a well-considered implementation that addresses all the feedback from the extensive review discussion. The patch shows careful attention to build system hygiene and follows Git's conventions while adapting to Meson's requirements. With all major architectural questions resolved in previous iterations, this version focuses on the core implementation details and appears ready for merging.