This is a follow-up in the Meson benchmark infrastructure thread, specifically regarding patch 3/5 about out-of-tree builds. Toon Claes initially reports a regression where cross-version benchmarking fails when trying to aggregate test results, with an error about missing `test-results/p0005-status.subtests`. However, they later retract the report, noting they can no longer reproduce the issue.

The original problem appeared when running the traditional `t/perf/run` script against both current and older Git versions, suggesting it might have been related to the build directory handling changes in patch 3. The error occurred during the result aggregation phase, specifically in `aggregate.perl`. While the report is now withdrawn, it highlights the fragility of cross-version benchmarking - a known limitation of the Meson integration that was previously discussed in the thread.

Key technical details:
- The error referenced `aggregate.perl line 159` failing to open test result files
- The test in question was `p0005-status.sh`
- The build process completed successfully before the aggregation failure
- The issue manifested in the traditional Make-based workflow, not the new Meson integration

This email doesn't introduce new work items since the report was retracted, but serves as a reminder that cross-version benchmarking remains a sensitive area where changes to build infrastructure could have subtle side effects. The thread's open issue about adapting `t/perf/run` for Meson builds may help prevent similar problems in the future.