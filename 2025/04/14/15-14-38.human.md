Refactoring patch removing a long-unused field from Git's revision machinery.

This is a straightforward cleanup patch from Lucas Seiki Oshiro that removes the `log_reencode` field from `struct rev_info`. The field was introduced in 2006 but has never been used in the codebase. The author noticed this while working on a previous patch series.

The change is minimal - just a single line deletion from `revision.h`. The patch appears to be a safe removal of dead code, though it might be worth checking if there was any original intent for this field that was never implemented. The commit message could be slightly more detailed by mentioning whether any git commands or features were expected to use this field historically.

This seems like a good candidate for inclusion as it removes unused code without any behavior changes. The patch is clean and focused, though it might benefit from a quick check by someone familiar with the revision walking code to confirm there are no subtle implications.