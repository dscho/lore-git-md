Refactoring patch as part of a series to enhance promisor-remote capabilities. This first patch moves and renames a config validation helper function to make it reusable across the codebase.

The patch relocates `iskeychar()` from config.c to config.h, renaming it to the more descriptive `is_config_key_char()`. This inline function checks whether a character is valid for config section or variable names (alphanumeric or hyphen). The change enables future patches in the series to validate remote configuration field names using this same logic.

This is straightforward code reorganization with no behavior changes. The commit message clearly explains the move is preparation for upcoming work, specifically mentioning the promisor-remote series' need to validate config keys outside config.c. All call sites are updated to use the new name, and the function remains static inline to preserve performance characteristics.

As the first of four patches in this series, it sets up infrastructure that will be used by subsequent commits implementing the promisor-remote enhancements described in the thread context. The change appears clean and well-contained, with all tests passing (implied by the lack of test changes needed).