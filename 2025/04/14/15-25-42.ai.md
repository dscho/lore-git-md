This is a documentation suggestion in the ongoing `--no-hooks` discussion thread. Lucas Seiki Oshiro observes that since `core.hooksPath=/dev/null` is already a known workaround for disabling hooks (with test coverage since commit c8f6478), perhaps the solution is to document this existing method rather than adding new `--no-hooks` functionality.

The email directly responds to Phillip Wood's position in the thread by noting the undocumented but tested nature of the `/dev/null` workaround. This suggests an alternative path forward that could satisfy some of the thread's use cases without introducing new code paths. The proposal would involve updating Git's documentation to officially recognize this existing mechanism, though it doesn't address all the thread's concerns (like npm/husky workflow issues or performance optimization needs).

Key technical details for future reference:
- References existing test case in c8f6478 validating `/dev/null` hook bypass
- Would require documentation updates if adopted
- Doesn't resolve the thread's deeper debate about bypass mechanisms vs optimization
- Maintains current behavior where hook bypass is explicit (unlike proposed silent `--no-hooks`)

This moves the discussion toward considering documentation solutions alongside code changes, though it doesn't fundamentally alter the core debate between Wood's optimization-first approach and Stolee's bypass-mechanism proposal. The npm/husky use case remains unaddressed by this documentation-only suggestion.