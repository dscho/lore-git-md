This is a test coverage discussion in the ongoing bugfix thread for `git add` wildcard pathspec handling. Lucas Seiki Oshiro provides feedback on the newly added test file t3706-add-wildcard-literal.sh, suggesting simplifications and identifying Windows compatibility issues.

The email questions whether the test setup needs to be split across multiple `test_expect_success` blocks and proposes a more minimal reproduction case using just `touch` and `git add`. It also suggests testing the `?` wildcard character to check for similar bugs. The key technical issue raised is Windows test failures due to path handling differences, particularly with backslash escaping (the tests currently use Unix-style escaping with backslashes).

The discussion builds on the established solution where the core bug was fixed by modifying `do_match_pathspec()` in dir.c to properly handle literal vs. wildcard matches. While the technical approach has consensus, this email focuses on refining the validation approach. The Windows compatibility problem represents the main outstanding issue before the patch can proceed, requiring adjustments to the test file's path handling to work across platforms.