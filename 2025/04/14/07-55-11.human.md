Build system alignment for Cygwin's sysinfo() support. This is a follow-up in the ongoing series to align Makefile and meson build systems while improving platform compatibility.

Patrick Steinhardt confirms Ramsay Jones's patch adding sysinfo() configuration for Cygwin in the Makefile build system. The change makes sense since Cygwin provides a functional implementation of this Linux-originated function. The meson build system already handles this case correctly, so no changes are required there.

This is a straightforward technical adjustment that brings the Makefile configuration in line with both Cygwin's capabilities and the meson build system's behavior. The patch appears well-justified and non-controversial, continuing the series' theme of build system alignment and platform support expansion.

The email doesn't introduce any new questions or concerns - it's a simple acknowledgment that the change is correct and complete as proposed. This suggests the sysinfo() configuration aspect of the build system alignment is now settled.