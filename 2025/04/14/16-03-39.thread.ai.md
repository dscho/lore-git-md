Thread identity: Feature patch series extending promisor-remote capability to support additional remote configuration fields, allowing servers to advertise extra remote attributes (like partial clone filters) and clients to validate them against local configuration.

Core problem/goal: Enhance promisor-remote protocol to support configurable validation of remote attributes during clone/fetch operations, while maintaining backward compatibility. The implementation needs to handle dynamic field advertisement from servers and configurable validation on clients.

Current status: Initial version of patch series introducing two new config options - `promisor.sendExtraFields` (server) and `promisor.checkExtraFields` (client). Replaces previous `strvec` usage with `string_list` structures per review feedback. Known issue with case-sensitive field name comparison acknowledged for future iteration.

Key technical details:
- New helper `is_config_key_char()` in config.h
- Server-side capability advertisement of specified remote fields
- Client-side validation against local config
- Tests in t5710-promisor-remote-capability.sh
- Documentation updates in config/promisor.adoc and protocol-v2.adoc
- Maintains backward compatibility - extra fields only used for validation

Open questions/loose ends:
- Case sensitivity in field name comparison needs fixing
- Additional test coverage planned for next iteration
- No Reviewed-by/Acked-by trailers yet

Participant positions: Only author's contribution so far (no other reviewers commented yet). Author acknowledges technical debt (case sensitivity) to address in next version.

Related work: Builds on existing promisor-remote infrastructure. Follows pattern of configurable capability advertisement seen in other protocol-v2 features.