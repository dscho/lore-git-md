This is a refactoring patch in Taylor Blau's performance optimization series for MIDX and cruft pack interaction during repacking (v2, part 4/8). The patch focuses on cleaning up the pack-objects code in preparation for upcoming changes.

The change extracts the inline handling of `--stdin-packs` into a separate function `read_stdin_packs()`. This moves the logic that reads pack lists from stdin and optionally adds loose objects into its own function, making the main `cmd_pack_objects()` flow cleaner. The refactoring is straightforward - the extracted function maintains the same behavior while being more modular.

This is part of the preparatory work for the series' larger goal of optimizing pack maintenance. The patch demonstrates good practice by keeping the change minimal and focused, with a clear commit message explaining both the current state and the motivation for the refactoring. The change touches `builtin/pack-objects.c` and while small, it follows the series' pattern of incremental improvements before introducing the main optimization in later patches.

Key technical details for future reference:
- New function `read_stdin_packs()` encapsulates stdin pack handling
- Maintains existing behavior with `ignore_packed_keep_in_core` flag
- Preserves the conditional loose object addition via `rev_list_unpacked` parameter
- Original functionality dates back to commit 339bce27f4
- Part of broader work on pack-objects and repack optimization