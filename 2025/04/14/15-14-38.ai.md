This is a refactoring patch removing an unused field from `struct rev_info`. 

The patch removes the `log_reencode` field from the revision.h header file, which appears to have been unused since its introduction in 2006 (commit 52883fb). Lucas Seiki Oshiro noticed this while working on a previous patchset and is now cleaning it up.

The change is straightforward - a single line deletion from the struct definition. The author notes this is a simple cleanup with no functional impact, as the field wasn't being used anywhere in the codebase. The patch touches only revision.h and doesn't require any test changes since it's no behavioral change.

This appears to be a legitimate cleanup of dead code. The patch is small, focused, and well-explained with references to both the original introduction of the field and the context where the author discovered it was unused. The change aligns with Git's ongoing code hygiene efforts.