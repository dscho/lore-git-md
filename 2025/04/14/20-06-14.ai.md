This is a documentation patch that's part of Taylor Blau's larger performance optimization series for MIDX and cruft pack interaction (v2 2/8). While the main series focuses on repacking optimizations, this particular patch adds an important documentation note about the initialization behavior of `struct object_info`.

The patch adds a critical comment near the `OBJECT_INFO_INIT` macro in `object-store-ll.h`, documenting that callers rely on all fields being zero-initialized when using designated initializers. This is important because several existing call sites use patterns like `{ .sizep = &sz }` which implicitly zero out other fields. The comment serves as a warning to future developers that adding fields with non-zero default values could break this assumption.

The change is small (8 lines added) but important for maintaining code safety as the `object_info` struct evolves. It's a defensive documentation addition that prevents subtle bugs from creeping in when the struct is modified. The patch doesn't change any functionality - it documents an existing implicit contract that was previously unwritten.

This appears to be a preparatory patch in the larger series, ensuring safe initialization patterns are documented before subsequent changes that will work with `object_info` structs. The commit message clearly explains the motivation and the potential risk being mitigated.