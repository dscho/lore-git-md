This is a documentation discussion in the ongoing `--no-hooks` feature thread. Brian M. Carlson responds to Lucas Seiki Oshiro's suggestion to document the existing `/dev/null` workaround for disabling hooks rather than implementing a new `--no-hooks` option.

Carlson takes a pragmatic stance, agreeing that documentation should be added regardless of whether the new feature is accepted. If the `--no-hooks` series proceeds, the documentation should be included there as it provides a clearer method. If the series is rejected, a separate documentation patch should be created to officially document the `/dev/null` workaround that's already tested in the codebase (since c8f6478).

The email doesn't take a position on whether the feature itself should be accepted, but reinforces that documentation improvements are needed either way. This keeps the discussion moving forward while the broader debate about the feature's merits continues in parallel. The response acknowledges Oshiro's point while maintaining Carlson's previously expressed concerns about silent hook disabling from earlier in the thread.