Refactoring patch in a performance optimization effort for pack-objects. This is patch 5/8 in Taylor Blau's v2 series improving MIDX and cruft pack interactions during repacking.

The patch moves the initialization and execution of a reachability traversal out of `read_packs_list_from_stdin()` and into its caller `read_stdin_packs()`. This prepares for future changes where the same traversal logic will be reused for the `--unpacked` case. The commit message explains this is groundwork for upcoming modifications that will use the reachability traversal to find objects packed in unknown packs (neither included nor excluded).

The changes are mechanical - relocating code blocks while preserving all existing functionality. The diff shows the `rev_info` struct initialization and traversal execution being hoisted up one level in the call stack, with the traversal now being passed down into `read_packs_list_from_stdin()` as a parameter.

This continues the series' pattern of preparatory refactoring before the core optimization changes. The patch appears well-constructed with a focused scope, maintaining all existing behavior while making the code more flexible for future needs.