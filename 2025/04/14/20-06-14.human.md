Refactoring with documentation note about struct initialization. This is patch 2/8 in Taylor Blau's series optimizing MIDX and cruft pack interactions during repacking, though it stands as an independent preparatory change.

The patch adds a documentation comment to `OBJECT_INFO_INIT` in object-store-ll.h, noting that callers rely on zero-initialization behavior when using designated initializers with `struct object_info`. This comes as Taylor prepares to use such an initializer in a subsequent patch, and serves as a safeguard against future changes that might introduce non-zero default values for new fields.

The change is straightforward and well-justified, catching a subtle invariant that could cause problems if violated. While not directly related to the performance optimization work that follows in the series, it's a sensible bit of defensive programming that improves maintainability of the codebase. The patch follows good form with a clear commit message explaining both the immediate need and the broader implications.