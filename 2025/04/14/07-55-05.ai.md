Platform compatibility update in the build system alignment series. This is a follow-up to Ramsay Jones's patch adding `clock_gettime()` support for Cygwin builds, where Patrick Steinhardt suggests a minor formatting improvement to maintain consistency.

The original patch (part of the larger build system standardization effort) added two Cygwin-specific timing function flags to `config.mak.uname`: `HAVE_CLOCK_GETTIME` and `HAVE_CLOCK_MONOTONIC`. Patrick's response focuses purely on code formatting, noting the lack of spaces around the assignment operator (`=`) in the added lines. This aligns with the series' broader goal of build system consistency, though it's a much smaller stylistic point compared to the major platform compatibility work elsewhere in the series.

Key technical context from the thread includes Cygwin-specific enhancements like arc4random support, sysinfo() fixes, and now clock_gettime() availability. The discussion remains focused on implementation details rather than design questions, with this particular exchange being a straightforward formatting suggestion that doesn't appear controversial. The change would affect the Cygwin build configuration but doesn't modify any runtime behavior.