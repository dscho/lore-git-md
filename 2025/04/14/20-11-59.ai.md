This is a follow-up discussion in the build system alignment series, specifically addressing the Cygwin RAM calculation fix from patch 10/13. The technical focus is on proper handling of the `mem_unit` field in `sysinfo()` for accurate memory reporting in `git gc`.

Ramsay Jones responds to Patrick Steinhardt's review of the RAM calculation patch, maintaining a cautious approach to handling the `mem_unit` multiplier despite Patrick's observation that all systems likely have proper `mem_unit` values. The original patch modified `builtin/gc.c` to multiply `totalram` by `mem_unit` when greater than 1, specifically addressing Cygwin's behavior of reporting memory in 4K pages (with `mem_unit=4096`) versus Linux's byte reporting (`mem_unit=1`).

Key technical details include:
- The `total_ram()` function now properly scales memory values for Cygwin while maintaining Linux compatibility
- Ramsay's conservative approach keeps the `mem_unit > 1` guard clause despite Patrick's suggestion it may be unnecessary
- The change affects `git gc`'s auto-detection logic for memory thresholds
- Discussion confirms Cygwin's 4K page reporting behavior through test results

This exchange represents a minor but important platform-specific adjustment in the larger build system alignment effort, showing careful consideration of edge cases in cross-platform compatibility. The thread context indicates this is part of broader work standardizing system information handling across platforms, with Cygwin receiving particular attention in this series.