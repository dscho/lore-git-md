Refactoring patch in Patrick Steinhardt's object-store cleanup series (v3 2/7). This continues the effort to simplify the object storage API by removing redundant wrapper functions.

The patch eliminates `loose_object_path()`, which was a thin wrapper around `odb_loose_path()` that always operated on the primary object database. The change inlines the function at its four call sites in http-walker.c, http.c, and object-file.c, directly using `odb_loose_path()` with `the_repository->objects->odb`. The documentation comment is moved to object-file.h where the underlying function is declared.

This is a straightforward mechanical change that removes unnecessary indirection while maintaining identical behavior. The patch is part of a larger series that's already received positive feedback on its technical approach, with this particular change being a clear cleanup that doesn't introduce any new complexity. The diff shows a net reduction of 18 lines while improving code clarity by removing an abstraction that didn't provide meaningful value.