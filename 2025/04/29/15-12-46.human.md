This is a follow-up discussion in the promisor-remote protocol enhancement series, specifically addressing how clients should handle unknown fields advertised by servers. The thread has already established a constrained approach where only "partialCloneFilter" and "token" fields are supported and documented in the protocol.

Christian Couder confirms the implementation now strictly accepts only these two fields, addressing Patrick Steinhardt's earlier concerns about undefined field semantics. The key discussion point here revolves around backward compatibility - whether clients should ignore unknown fields (current approach) or bail out. Both agree that ignoring unknown fields is preferable as it maintains compatibility while allowing future extensions.

The exchange highlights an important protocol design principle being adopted: servers can advertise additional fields for newer clients while still supporting older ones, without requiring explicit capability negotiation. This keeps the protocol flexible for future evolution while ensuring current implementations remain stable. The discussion appears to be wrapping up technical details before final maintainer review.