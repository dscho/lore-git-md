Refactoring patch for promisor-remote protocol extension (part 1/3 of v2 series). This is the foundational change that replaces the current `strvec`-based storage of promisor remote information with a new `promisor_info` linked list containing `string_list` fields. The change enables the protocol to handle additional fields beyond just name and URL, which will be used in subsequent patches to support configurable validation of remote attributes.

The patch introduces a well-documented `promisor_info` struct that stores field/value pairs in a `string_list`, with explicit handling for the mandatory "name" and "url" fields. The implementation includes proper memory management and maintains case-insensitive comparison of field names. While this is primarily internal plumbing change, it's carefully done with clear documentation and maintains all existing functionality while setting up for the protocol extensions to come in patches 2 and 3.

The changes appear thorough and maintain the same behavior while improving the code's ability to handle additional fields. The commit message clearly explains the motivation and the relationship to the broader series. This looks like a solid foundation for the protocol extension work that follows.