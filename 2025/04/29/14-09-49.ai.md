Performance optimization patch for xdiff's `--minimal` mode, now in its third iteration. This version finalizes the change to disable the `cleanup_records` heuristic when `--minimal` is explicitly requested via the `XDF_NEED_MINIMAL` flag. The patch remains narrowly focused on ensuring minimal diffs are truly minimal by preventing the heuristic from marking unchanged lines surrounded by many changes as changed.

Key technical details: The implementation now derives `need_min` from `cf->flags` (as suggested in v1 discussion) and modifies the marking logic to force `dis1[i]` to 1 when minimal diffs are requested. The new test script `t4071-diff-minimal.sh` verifies the behavior using test helpers like `test_write_lines` and `test_grep`, specifically checking that unchanged lines surrounded by changes don't appear in minimal diffs.

Benchmark results show no statistically significant runtime impact (2.363s ± 0.023s vs 2.362s ± 0.035s for `git log -p -3000 --minimal`) while maintaining the 1.3% diff size reduction observed in Git's history. The thread has reached consensus with all technical feedback addressed, and this version appears ready for merging with only minor commit message polishing remaining. The change affects only the `--minimal` output path where shortest-possible diffs are the expected behavior.