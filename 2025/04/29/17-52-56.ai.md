This is patch 4/10 in a series modernizing reftable tests by converting them to the Clar framework. The patch focuses on migrating the merged table tests (t-reftable-merged.c -> u-reftable-merged.c).

The conversion maintains all the original test coverage while updating the test infrastructure. Key aspects include:

- Replaces custom test assertions with Clar's cl_assert() framework
- Preserves all test cases from the original file:
  * Single record retrieval
  * Multiple ref updates
  * Repeated seeking behavior
  * Log record handling
  * Default write options validation
- Updates build system files (Makefile, meson.build) to track the new test file location
- Uses existing test helpers from earlier patches in the series (cl_reftable_write_to_buf, cl_reftable_strbuf_writer)

The tests verify core merged table functionality including:
- Correct ordering of refs/logs across multiple tables
- Proper handling of deletions and updates
- Seeking behavior at different positions
- Hash ID and update index tracking
- Error cases around hash algorithms

This appears to be a straightforward mechanical conversion that maintains the original test coverage while adopting the standard Clar framework. The patch follows the same pattern as previous conversions in the series and should be ready for review.