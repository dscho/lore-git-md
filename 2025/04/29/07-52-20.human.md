This is patch 6/7 in Patrick Steinhardt's refactoring series to clean up object file handling, part of the ongoing effort to remove the `the_repository` global. The patch systematically replaces all calls to the deprecated `repo_has_object_file()` and its `_with_flags()` variant with the newer `has_object()` API across 23 files.

The conversion maintains functional equivalence while providing clearer semantics: `has_object()` makes its defaults explicit (no promisor fetching or packfile rechecking by default) and has a simpler name. The patch provides a clear mapping between the old and new flag combinations, showing how each existing call pattern translates to the new API.

This is a mechanical but wide-reaching change that touches core functionality (object existence checks) across many subsystems including fetch, push, refs, and the object store. The patch appears well-constructed with thorough test coverage implied by the breadth of converted call sites. It follows the series' conservative approach of preserving existing behavior while cleaning up the API surface.

The changes are straightforward replacements with no behavioral modifications, continuing the series' incremental progress toward removing deprecated functions. This patch completes the bulk of the mechanical work to sunset `repo_has_object_file()` after the preparatory changes in earlier patches.