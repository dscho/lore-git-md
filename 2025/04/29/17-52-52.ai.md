Test modernization cover letter completing the migration of reftable unit tests to the Clar framework. This is the second version of a 10-patch series that converts all remaining reftable test files (basics, block, merged, pq, reader, readwrite, record, and stack) from custom test harnesses to Clar-based tests. The series begins by moving reftable helper functions into `unit-test.{c,h}` (new in v2 based on review feedback), then systematically converts each test file while maintaining the same test coverage. The changes are mechanical - deleting old `t-reftable-*.c` files and creating corresponding `u-reftable-*.c` implementations that use Clar's assertion macros and test organization. Build system changes update the Makefile and meson.build to track the new test locations. The final patch migrates the helper functions back to `lib-reftable.{c,h}` now that they're Clar-compatible.

Key technical details:
- Each conversion follows the pattern: delete old test, create new Clar-based version
- Test helpers include hash generation (`cl_reftable_set_hash`), buffer writing (`cl_reftable_strbuf_writer`), and record writing utilities (`cl_reftable_write_to_buf`)
- Build system updates remove old test references and add new Clar test suites
- The changes are purely about test infrastructure - no reftable behavior is modified
- Completes a project to standardize all unit tests on Clar framework

The series appears well-structured with each commit focused on a single test file conversion. The cover letter clearly explains the scope and v2 changes. This is the culmination of an ongoing effort to modernize Git's test infrastructure.