This is the first patch in a 3-part v2 series refactoring the promisor-remote protocol extension to support configurable validation of remote attributes. The patch focuses on internal restructuring to prepare for handling additional fields beyond just name and URL.

The key change replaces the existing `strvec`-based storage of promisor remote information with a new `promisor_info` linked list containing a `string_list` of field/value pairs. This provides more flexible foundation for the protocol extension while maintaining backward compatibility. The implementation:

- Introduces `struct promisor_info` with `string_list fields` to store name/url pairs and future fields
- Adds case-insensitive comparison for field names
- Preserves existing behavior while restructuring internal representation
- Includes BUG() checks to enforce field ordering expectations
- Follows string_list conventions for memory management

The patch appears well-constructed with thorough attention to memory management and backward compatibility. It lays necessary groundwork for the protocol extension while keeping the changes focused and contained. The commit message clearly explains the motivation and technical approach.

This refactoring enables the subsequent patches to add support for additional fields (partialCloneFilter and token) in a maintainable way while addressing protocol design concerns raised in v1 review. The implementation aligns with Git's string_list patterns and maintains the existing promisor-remote functionality.