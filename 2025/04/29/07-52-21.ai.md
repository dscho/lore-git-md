This is the final patch in Patrick Steinhardt's refactoring series to clean up object store APIs, specifically completing the removal of deprecated functions as part of the `the_repository` removal effort.

The patch removes `repo_has_object_file()` and `repo_has_object_file_with_flags()` after all callers have been converted to use the newer `has_object()` API in previous patches. The deletion is straightforward - 31 lines removed across object-store.c and object-store.h - with no behavior changes since the transition was already completed mechanically in patch 6/7. The commit message notes this is the last step after all users were converted.

Key technical points for future reference:
- Removes two deprecated object existence check functions that were wrappers around `oid_object_info_extended()`
- Follows through on the deprecation notice that was previously added to these functions' documentation
- Maintains all existing behavior since callers were already updated to use equivalent flag combinations with `has_object()`
- Part of the larger object-file-cleanup effort to remove `the_repository` usage and improve API boundaries
- Builds on earlier patches that established the new `has_object()` API with proper flag handling (including promisor object fetching via `HAS_OBJECT_FETCH_PROMISOR`)

The change is purely mechanical at this point, representing the completion of an uncontroversial API transition that was pre-approved in earlier discussion. No open questions remain about this specific removal, though the broader object store cleanup effort continues with other components.