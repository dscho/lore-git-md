Refactoring patch moving `struct packed_git` definition as part of the object-file-cleanup series.

This is the first patch in Patrick Steinhardt's v3 series (1/7) that continues the object-file API cleanup work. It relocates the `struct packed_git` definition from the generic "object-store.h" header to the more specific "packfile.h" header, along with its associated `pack_map_entry_cmp()` function. The change is purely organizational - no behavior is modified.

The patch makes a straightforward header reorganization, moving 59 lines from object-store.h to packfile.h while maintaining all existing functionality. The commit message argues this is a better location since `struct packed_git` contains packfile-specific details. The diff shows the mechanical movement of code with corresponding updates to include directives in pack-objects.h.

This appears to be a well-scoped preparatory change for the larger series that will remove `repo_has_object_file()`. The patch follows good practices by keeping the diff focused, maintaining all related functionality together, and not introducing any behavioral changes. As the first in the series, it sets up subsequent patches by cleaning up header organization before more substantive API changes.