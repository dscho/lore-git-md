This is part 7/10 of the reftable test modernization series, converting the readwrite test to use the Clar framework. The patch follows the established pattern of the series, mechanically translating the existing test cases while maintaining the same coverage.

Key technical details:
- Converts `t-reftable-readwrite.c` to `u-reftable-readwrite.c` using Clar assertions (`cl_assert`) and test method naming (`test_reftable_readwrite__*`)
- Tests various reftable read/write scenarios including buffer operations, log compression, seek behavior, index handling, and error cases
- Verifies proper handling of edge cases like empty tables, corrupted data, and invalid key ordering
- Maintains all existing test coverage while standardizing on the Clar framework
- Updates build files (Makefile and meson.build) to track the new test location

The patch appears to be a straightforward conversion following the same approach as previous patches in the series. It maintains the same test coverage while adopting the standard test framework conventions. The changes are mechanical in nature, replacing the old test harness assertions with Clar equivalents while preserving the test logic.