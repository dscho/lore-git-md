## AI thread summary

Leon Michalak proposes improving `--patch` interactive commands (`git add -p`, `git checkout -p`, `git reset -p`, `git stash -p`) to respect context line configuration, currently requiring `GIT_DIFF_OPTS=-u<number>` workaround. Three solutions considered: 1) Add `-U/--unified` flags, 2) Make `diff.context` apply universally, 3) Introduce new `interactive.context` setting. 

Junio C Hamano provides implementation guidance favoring the flag approach, detailing steps: 1) Create `add_p_opt` struct for context length, 2) Modify `run_add_p()` to accept this, 3) Add `-U` support to all relevant builtins, 4) Add interactive menu option for context length. Notes technical limitation: dynamic context changes mid-session would require major architectural changes due to hunk tracking constraints. 

Open questions remain about preferred approach (flag vs config) and backward compatibility implications if changing `diff.context` behavior. Implementation would affect all commands using `run_add_p()` infrastructure. Junio's response suggests the flag-based solution is most feasible within current architecture while setting realistic expectations about dynamic adjustment limitations.