## AI thread summary

Leon Michalak proposes improving `--patch` interactive commands (`git add -p`, `git checkout -p`, `git reset -p`, `git stash -p`) to respect diff context line configuration, currently requiring `GIT_DIFF_OPTS=-u<number>` workaround. Three solutions considered: 1) Add `-U/--unified` flags, 2) Make `diff.context` apply universally, 3) Introduce new `interactive.context` setting.

Junio C Hamano provides implementation guidance favoring the flag approach, detailing steps: 1) Create `add_p_opt` struct for context length, 2) Modify `run_add_p()` to accept this, 3) Add `-U` support to all relevant builtins, 4) Add interactive menu option for context length. Notes technical limitation: dynamic context changes mid-session would require major architectural changes due to hunk tracking constraints.

Jeff King (peff) weighs in favoring the config-based approach, arguing interactive commands should honor `diff.context` by default since they are user-facing (not scripted) and Git already respects other porcelain-level configurations like coloring. Notes current limitation stems from interactive code being built around plumbing commands that avoid config options. Suggests starting with just `diff.context` before considering other diff-related configs like `diff.interhunkcontext` or `diff.colormoved`.

Open questions remain about preferred approach (flag vs config) with Junio favoring flags for implementation feasibility and Jeff arguing for config inheritance. Backward compatibility implications need consideration if changing `diff.context` behavior. Implementation would affect all commands using `run_add_p()` infrastructure, with technical constraints around dynamic context adjustment during hunk tracking.