## AI thread summary

Leon Michalak proposes improving `--patch` interactive commands (`git add -p`, etc.) to respect context line configuration. Currently these commands ignore `diff.context` and require awkward `GIT_DIFF_OPTS=-u<number>` workaround. Three solutions under consideration: 1) Add `-U/--unified` flags to patch commands, 2) Make `diff.context` apply universally (potential behavior change), or 3) Introduce new `interactive.context` setting. Change would affect all builtins using `run_add_p()`. Open questions: which approach is preferred, whether the feature is wanted, and how to handle backward compatibility if changing `diff.context` behavior. Discussion will likely weigh convenience against potential behavior change surprises.