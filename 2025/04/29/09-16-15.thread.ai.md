## AI thread summary

Leon Michalak's proposal to improve diff context handling in interactive patch commands (`git add -p` and related) has evolved through discussion with Junio C Hamano and Jeff King. The current hybrid approach would:

1. Inherit `diff.context` and `diff.interhunkcontext` configurations by default for all interactive commands (`add -p`, `checkout -p`, `reset -p`, `stash -p`)
2. Allow per-invocation override via existing `-U<n>` and `--inter-hunk-context=<n>` flags
3. Explicitly maintain the current technical constraint that context cannot change mid-session due to hunk tracking requirements

Junio initially favored a flag-based approach for implementation feasibility, while Jeff advocated for config inheritance from `diff.*` for porcelain consistency. Michalak's synthesis addresses both perspectives while deferring consideration of other diff-related configs (like `diff.colormoved`).

Implementation would modify the shared `run_add_p()` infrastructure affecting all interactive patch commands. The proposal maintains backward compatibility through config inheritance and avoids dynamic context changes during hunk selection. Technical constraints prevent mid-session adjustments without major architectural changes.

Key unresolved questions center on implementation details rather than approach, with consensus emerging around the hybrid model. The thread shows awareness of both user experience (consistent porcelain behavior) and technical realities (plumbing command limitations). Future work could extend this to other diff UI configurations once core context handling is established.