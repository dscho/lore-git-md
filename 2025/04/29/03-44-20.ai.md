Feature patch finalizing the Outlook Message-ID handling in `git send-email`, implementing Junio Hamano's suggested boolean interface while maintaining the original tri-state functionality under the hood.

Aditya Garg responds to Junio's feedback about the `--smtp-outlook-id-tweak` option's interface, agreeing that a boolean would be clearer but noting implementation challenges with the longer parameter name (`--[no-]outlook-message-id-fix`). The email shows Junio's detailed counter-proposal for documentation phrasing that would make the automatic hostname detection (`smtp.office365.com`/`smtp-mail.outlook.com`) the default behavior, with explicit opt-in/opt-out via the boolean flags.

Key technical points in this exchange: the boolean interface would make the hostname-based auto-detection the implicit default, with `--outlook-message-id-fix` forcing the behavior for all hosts and `--no-outlook-message-id-fix` disabling it completely. Junio explicitly states he's queuing the current patch as-is despite his preference, signaling this is a stylistic rather than blocking concern. The discussion reveals the tension between interface clarity (favoring Junio's boolean design) and implementation ergonomics (Aditya's concern about command-line length and sub-usage impact).

This email marks the final design decision point in the thread, with Junio's approval of the queued v7 patch despite his alternative suggestion. The solution now handles both standard Outlook SMTP hosts via hostname detection and enterprise configurations through explicit control, with documentation warnings about misuse of the 'always' mode. All outstanding concerns from earlier in the thread appear resolved, with only minor interface preferences remaining as noted-but-accepted differences.