Bugfix addressing a CodeQL static analysis false positive in `diff.c`. This is a follow-up to the previously approved `read-cache.c` changes that established patterns for handling static analysis findings.

The patch reorders a condition in `fill_es_indent_data()` to check array bounds before accessing the element, which satisfies CodeQL's `cpp/offset-use-before-range-check` rule. The original code was technically safe since it operated on NUL-terminated strings, making this change primarily about tool compatibility rather than fixing an actual bug. Johannes Schindelin notes this is a rare false positive worth accommodating to maintain the usefulness of CodeQL analysis.

The change is minimal (a single line modification) and follows the precedent set by the `read-cache.c` discussion - making defensive programming adjustments where they don't harm readability or performance. Unlike the earlier approved patch, this one doesn't add redundant checks but simply reorders existing ones to satisfy the static analyzer.

This appears ready for inclusion as a low-risk maintainability improvement, though it's worth noting the thread shows this particular change was less critical than the `read-cache.c` case it follows.