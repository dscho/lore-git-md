This is a follow-up discussion in the reftable test conversion series, specifically addressing build failures when applying the first patch in isolation. The author Seyi Chamber confirms to Junio C Hamano that the build errors are expected behavior since unconverted test files still reference the old `t_`-prefixed helper functions while the patch renames them to `cl_` prefix.

The email reveals an implementation detail about the conversion process - the author temporarily moved helper functions into `unit-test.{c,h}` during conversion before moving them back to `lib-reftable.{c,h}`. Seyi asks whether it would be better to keep them in `unit-test.{c,h}` throughout the conversion process and only migrate them to `lib-reftable` in a final patch.

This discussion highlights the transitional challenges of large-scale test framework migrations, where intermediate states necessarily break functionality until the full series is complete. The exchange confirms the series is following the expected conversion pattern while exploring potential workflow improvements for future similar migrations. No new technical issues are raised beyond the already-expected build failures.