Performance optimization follow-up confirming real-world validation of bundle ref handling improvements. This email from Karthik Nayak responds to Toon Claes' confirmation that the bundle ref deduplication changes perform as expected in repositories with fewer refs, showing no regression at smaller scales while maintaining the 6x speedup seen in large repositories (100k refs). The exchange serves as final validation before merging, with both author and reviewer now satisfied with the solution's behavior across repository sizes. The thread context shows this was the last outstanding question after earlier technical review of the O(N^2) to O(1) conversion using `strset` in `write_bundle_refs()`. With benchmarking complete and edge cases addressed (including handling of same-refname variants and exclusions), the series appears ready pending resolution of a simple merge conflict with ps/object-wo-the-repository. The implementation removes `object_array_remove_duplicates()` entirely while adding comprehensive test coverage in t6020-bundle-misc.sh.