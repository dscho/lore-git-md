Thread summary:

Feature inquiry about silent working directory status checking, with response from maintainer. Ross Goldberg's original question sought (1) the simplest existing command for silent dirtiness detection via exit codes (clean=0, dirty≠0) and (2) whether there were feature requests for a dedicated configurable dirtiness-check command. Junio C Hamano's response provides concrete technical guidance while expressing reservations about a unified command. The reply enumerates existing commands for specific dirtiness types: `git diff --cached --quiet` (index), `git diff --quiet` (worktree vs index), `git diff --quiet HEAD` (worktree vs HEAD), and notes `git describe --always --dirty` (lacking untracked file support). The position is that combining these checks would introduce unwarranted complexity given the availability of discrete commands. Open question remains whether the scripting use case justifies a simpler unified command despite the maintainer's expressed skepticism. No other participants have weighed in yet. The thread now contains both the original inquiry and the maintainer's substantive response outlining the current technical landscape and design philosophy regarding dirtiness detection.