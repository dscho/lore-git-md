This is a follow-up in the thread about `safe.directory` wildcard behavior inconsistencies between Git operations. Jeff King confirms brian m. carlson's observation that `git clone --no-local` (introduced in v2.48.0) provides a third workaround for the security-related behavior where local clones enforce stricter ownership checks than other operations.

The email serves as ackowledgment that the `--no-local` approach works as expected, though King notes initial puzzlement about why this flag makes the wildcard work when regular local clones don't. This aligns with the thread's established understanding that the behavior stems from security-conscious process isolation design, where `--no-local` triggers the same untrusted-repository semantics as remote operations.

The exchange appears to be closing out the discussion, confirming all three known workarounds (global config, explicit config passing, and `--no-local`) as valid solutions depending on use case and Git version. No new technical questions or concerns are raised - this seems to be final confirmation that the thread has reached consensus on both the security rationale and practical solutions.