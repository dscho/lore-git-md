This thread has thoroughly examined the security behavior differences in Git's `safe.directory` wildcard handling between single-process operations (like `git status`) and multi-process operations (like `git clone`). The key insight is that this is not a bug but an intentional security feature - when Git spawns subprocesses during clone operations, it deliberately clears environment variables (including `GIT_CONFIG_*`), while single-process operations maintain the config context.

Jeff King provided authoritative technical explanation of this behavior, detailing how process isolation explains the divergence in wildcard matching. brian m. carlson identified the `--no-local` clone option (available in Git v2.48.0+) as a workaround that activates the same security semantics as remote transports. The thread has established three valid solutions: using a `~/.gitconfig` setting, explicitly passing config to upload-pack, or using version-gated `--no-local` clones.

Nikolaus Rath's latest email confirms this understanding through real-world test case, verifying that environment-based configs behave as expected across Git's security boundaries. The discussion is now fully resolved with comprehensive technical explanation and multiple workarounds documented. While future documentation might note these edge cases, no changes to the security model are needed as the behavior is working as designed to prevent accidental config propagation across security boundaries.