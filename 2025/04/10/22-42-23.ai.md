Security policy follow-up confirming `--no-local` workaround details.

Jeff King confirms brian m. carlson's observation that `git clone --no-local` provides a third workaround for the `safe.directory` wildcard behavior discrepancy in Git v2.48.0+. This builds on the established thread understanding that the different behavior between local operations (like `git status`) and clone operations stems from security-conscious process isolation design.

Key technical points reinforced:
- The `--no-local` workaround works by triggering Git's untrusted-repository semantics
- Version dependency noted (v2.48.0+ required)
- Behavior remains consistent with the security model where local/non-local process boundaries affect config propagation

The email serves as a brief confirmation of technical details rather than introducing new arguments, effectively closing the remaining minor question about version compatibility for the `--no-local` approach. This aligns with the thread's earlier conclusion that the behavior is intentional security design with multiple documented solutions available.