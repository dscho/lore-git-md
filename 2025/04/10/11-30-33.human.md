This is part of Karthik Nayak's series to port Git's header checking functionality from Makefile to meson builds. Patch 3/4 implements the core 'hdr-check' target in meson, which verifies each header file can compile independently after including git-compat-util.h.

The implementation closely matches the Makefile version, with the same exclusions for generated headers, platform-specific files, and test/unit-test headers. It conditionally excludes crypto backend headers based on the active configuration. The check uses meson's custom_target() to create temporary compilation units for each header and verify they compile cleanly.

As noted in the thread context, this version maintains backward compatibility by supporting both 'hdr-check' (legacy) and 'headers-check' (new) target names. The patch shows careful attention to detail, with proper handling of generated headers and platform exclusions. The implementation is now at feature parity with the original Makefile version and has addressed all maintainer feedback from earlier iterations.

This represents a significant step in Git's meson build system maturation, bringing an important developer tool to the new build system while maintaining consistency with existing workflows. The series appears ready for merging with no outstanding technical concerns.