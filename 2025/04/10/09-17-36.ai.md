This appears to be a bug report about `git send-email` truncating SMTP passwords when they contain special characters. The reporter, Jérôme Pouiller, describes a regression where their previously-working SMTP password (containing characters like # and $) is being truncated when read from config, though it works correctly when passed via `--smtp-pass` and shows properly in `git config --list`.

Key technical details:
- The password is under 20 characters and uses only ASCII printable characters
- The issue appears to be specific to how `git send-email` reads quoted passwords from config
- System info shows Git 2.39.5 running on Debian Linux with Perl
- No recent changes to Git or Perl packages were made, suggesting this might be an environmental or configuration issue

The report lacks specific reproduction steps or a minimal example, but provides enough context about the failure mode (truncation of quoted passwords containing special chars) for others to investigate. The mention that this worked 5 weeks ago suggests either a subtle regression or environmental change affecting config parsing.