Follow-up to the safe.directory wildcard behavior discussion, offering an additional workaround. Brian Carlson notes that `git clone --no-local` can also bypass the ownership checks by treating the local repository as untrusted, triggering the same security semantics as remote clones. This provides a third option alongside the previously mentioned solutions of setting the config globally or passing it explicitly to upload-pack.

The email confirms the thread's conclusion that the behavior is intentional (not a bug) while expanding the practical solutions available to users. The suggestion appears technically sound and aligns with Git's security model, though it's worth noting this approach would force full remote protocol overhead for local clones. The thread appears effectively resolved with this addition to the workaround options.