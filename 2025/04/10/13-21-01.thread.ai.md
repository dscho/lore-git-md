Git thread summary (AI mode):

Security policy analysis of `safe.directory` wildcard behavior discrepancy between repository operations, now fully resolved with three confirmed workarounds including version-specific `--no-local` approach. Thread establishes this as intentional security design feature rather than bug, with process isolation explaining behavioral differences.

Core technical explanation:
- Fundamental dichotomy between single-process operations (`git status`) and multi-process operations (`git clone`)
- Clone operations spawn upload-pack with cleared environment (including `GIT_CONFIG_*` vars)
- Status operations run in single process where config persists
- `--no-local` clones (v2.48.0+ workaround) trigger untrusted-repository semantics
- Design deliberately matches security model of non-local transports

Current status:
- Behavior confirmed as security feature across all discussion
- Three complete workarounds documented:
  1. Global `safe.directory = *` in `~/.gitconfig`
  2. Explicit config passing to upload-pack
  3. Version-gated `git clone --no-local` solution
- Security rationale unanimously accepted

Key technical specifics:
- Process boundary behavior fully explains config propagation differences
- Wildcard matching works as designed in single-process contexts
- `--no-local` solution ties to untrusted-repository semantics
- Version requirement (v2.48.0+) for `--no-local` approach now confirmed

Participant contributions:
- Jeff King provided authoritative implementation details throughout
- brian m. carlson identified and verified `--no-local` workaround
- Final confirmation of version requirements provided by Jeff King

Security implications reinforced:
- Current behavior prevents accidental config propagation
- Maintains transport-agnostic security posture
- Complete solution set now available for all use cases

Resolution status:
- Thread fully resolved with comprehensive technical explanation
- All identified questions answered
- No outstanding technical issues
- Documentation could note these edge cases for future reference

Related context:
- Builds on safe.directory's security-sensitive design history
- Connects to Git's broader process isolation patterns
- References environment handling implementation details
- Serves as reference for similar config propagation questions