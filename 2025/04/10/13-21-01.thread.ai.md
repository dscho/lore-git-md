Git thread summary (AI mode):

Security policy inconsistency in `safe.directory` wildcard behavior between repository operations. Thread examines why `git status` honors `safe.directory = *` while `git clone` from local paths enforces ownership checks, with Jeff King providing implementation details.

Core technical explanation:
- Clone operations spawn child processes (upload-pack) with cleared environment variables (including `GIT_CONFIG_*`)
- This prevents wildcard setting from propagating to the child process
- Status operations run in single process context where config persists
- Design is intentional security measure matching behavior of non-local transports

Current status:
- Behavior confirmed as implementation detail rather than bug
- Two workarounds identified:
  1. Set `safe.directory = *` globally in `~/.gitconfig`
  2. Explicitly pass config to upload-pack via clone command
- No plans to change current implementation

Key technical specifics:
- Affects local repository operations only
- Rooted in Git's environment sanitization during process separation
- Connects to historical security decisions about config propagation
- Wildcard matching works as designed in single-process contexts

Open questions resolved:
- Inconsistency explained by process boundary behavior
- Clone exclusion is intentional security measure
- Documentation could clarify this edge case

Participant updates:
- Jeff King provided authoritative implementation details
- Confirmed reporter's observations while explaining rationale

Security implications:
- Current behavior prevents accidental config propagation
- Maintains consistency with non-local transport security model
- Tradeoff between usability and strict environment isolation

Related context:
- Builds on safe.directory's security-sensitive design
- Connects to broader Git process isolation patterns
- References historical environment handling debates

No further action expected unless documentation clarification is proposed. Thread likely resolved with current explanation.