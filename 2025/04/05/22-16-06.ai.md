This is a v2 cover letter for a major refactoring series that completes the removal of the legacy merge-recursive implementation in favor of merge-ort. The series represents the final step in a multi-year effort to modernize Git's merge machinery.

The v2 iteration includes minor typo corrections in commit messages but maintains the same technical approach as v1 series. The 8-patch series systematically removes all remaining merge-recursive infrastructure after ensuring merge-ort can handle all use cases, including the recently added diff-algorithm selection capability. Key technical changes include moving shared functions from merge-recursive to merge-ort, deleting merge-recursive.[ch] entirely, and removing test infrastructure that supported comparing the two backends.

The diffstat shows significant deletions (over 5000 lines removed) primarily from deleting merge-recursive.[ch] and test scaffolding, though the author notes the actual functional changes are relatively small. The series affects multiple builtin commands (merge, rebase, revert, checkout) and updates numerous test files to remove merge-recursive-specific logic.

With merge-ort having been Git's default merge strategy since 2.33.0 and now handling all functionality previously provided by merge-recursive, this removal represents the culmination of a major architectural cleanup. The series appears technically sound and ready for merging, with the v2 changes being purely editorial improvements to commit messages.