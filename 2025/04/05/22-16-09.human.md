This is part of the major refactoring series to fully replace merge-recursive with merge-ort. Patch 3/8 specifically enables diff-algorithm selection for merge-ort, bringing it to parity with merge-recursive's capabilities.

The patch removes merge-ort's hardcoded use of the histogram diff algorithm, making it configurable like merge-recursive was. The change moves the default setting to init_merge_options() where it can be overridden. While this technically affects merge-recursive's default too, the commit notes this is irrelevant since merge-recursive is being removed entirely in subsequent patches.

Documentation is updated to reflect that both strategies now support the same diff-algorithm options, removing the previous distinction where recursive supported additional algorithms. The changes are straightforward - removing one hardcoded line in merge-ort.c, adding a default in merge-recursive.c, and updating the strategy docs to show unified behavior.

This appears to be a well-justified change that completes merge-ort's feature parity with merge-recursive before the latter's removal. The patch maintains clean separation of concerns and includes thorough documentation updates. As part of the larger series that has already seen positive feedback, this seems like a solid incremental step toward the final goal.