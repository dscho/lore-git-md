This is a follow-up email in the git-p4 error message encoding fix series. Nikolay Shustov is checking in with Fahad Al-Rashed about testing progress on the patch that addresses `git p4` failures when processing non-UTF-8 Perforce error messages. The thread context shows this is part of a series that introduces a `MetadataTranscoder` class to handle encoding issues, specifically fixing crashes during `git p4 clone` operations when encountering high-bit characters.

The email contains no new technical content - it's simply a polite inquiry about whether Al-Rashed has been able to test the changes on their Perforce system yet. Previous discussion established that testing requires a non-Unicode Perforce server and Python 3 environment, with Shustov having validated the changes on Kubuntu but lacking Windows testing. Al-Rashed had previously indicated they would test the patch in their work environment, which would provide valuable real-world validation beyond the existing test coverage in `t9837-git-p4-error-encoding.sh`.

Key points for future thread context:
- The technical solution (MetadataTranscoder class) remains uncontested
- Testing validation from Al-Rashed's environment is pending
- Phillip Wood's earlier feedback about commit organization remains unaddressed
- The test script `t9837-git-p4-error-encoding.sh` conditionally runs only on Python 3 with non-Unicode Perforce setups