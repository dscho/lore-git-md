Documentation patch fixing a typo in a code comment, with a review response.

Martin Ågren responds to Christian Fredrik Johnsen's patch that fixed a duplicated word ("checking checking" → "checking") in a comment in refs.c. The comment explains an optimization in refname verification where common prefixes like "refs/heads/" are checked only once.

Martin confirms the fix is correct ("Well-spotted") but notes a minor formatting issue - there should be an empty line between the commit message body and the Signed-off-by trailer. This is a standard Git commit message convention that wasn't followed in the original submission.

The exchange is straightforward - the patch itself is purely cosmetic with no functional impact, and Martin's review is positive while pointing out a simple formatting improvement. The thread appears to be moving toward acceptance of this trivial documentation fix after the commit message formatting is corrected.