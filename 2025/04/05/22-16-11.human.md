This is part of the major refactoring series to fully remove the legacy merge-recursive implementation in favor of merge-ort. Patch 5/8 focuses on converting the remaining recursive merge calls in `builtin/merge.c` and `sequencer.c` to use the new `merge_ort_recursive()` function instead of `merge_recursive()`.

The changes are straightforward mechanical conversions, removing conditional backend selection logic since merge-ort is now the only option. The patch also removes a now-unnecessary test setting (`GIT_TEST_MERGE_ALGORITHM=recursive`) from t7615 since we're fully committed to merge-ort. The sequencer changes preserve an existing TODO comment about potential optimizations for working tree updates.

This patch represents a key step in the series, completing the transition of all recursive merge operations to the new backend. The changes are low-risk since merge-ort has been the default since Git 2.33.0 and has been thoroughly tested. The patch maintains all existing behavior while removing dead code paths.