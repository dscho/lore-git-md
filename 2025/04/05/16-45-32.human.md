This is a contributor housekeeping patch removing unused global variables from environment.h. 

The patch removes `packed_git_window_size` and `packed_git_limit` which were made obsolete by commit d284713bae in December 2024, when these variables were moved into `struct repo_settings`. The author confirms through code search that there are no remaining references to these globals elsewhere in the codebase.

The change is straightforward - just deleting two lines from a header file - and appears to be a clean-up of technical debt after a previous refactoring. There's no behavior change, just removal of dead code. The patch follows the standard format and includes a clear commit message linking to the commit that made these variables obsolete.

This seems like a good candidate for inclusion - it's a simple cleanup that reduces clutter in the global namespace. The patch is properly motivated and appears to be correctly targeted.