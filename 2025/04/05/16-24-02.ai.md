This is a response in the ongoing discussion about enhancing `git stash list` output. Lucas Seiki Oshiro suggests using existing `git log` formatting options as an alternative to the proposed metadata enhancements for WIP stashes. The email points out that `--stat` and `--pretty=reference` can already provide some of the requested information (change statistics and reference-style output) through the existing interface.

The response doesn't directly engage with the proposal's core idea of automatically surfacing diff stats and age information, instead offering workarounds using current capabilities. This suggests the discussion may be shifting toward whether new functionality is needed versus better documentation of existing features. The thread's original questions about performance impact and configuration options remain unaddressed in this exchange.

Key technical elements mentioned include the `--stat` flag (which shows diff statistics) and `--pretty=reference` format (a compact output style), both standard `git log` options that `git stash list` inherits. The response implies these might satisfy some of the original motivation without code changes, though it doesn't address the convenience factor of having metadata always visible by default.