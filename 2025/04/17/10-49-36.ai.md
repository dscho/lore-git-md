Bugfix for integer parsing in `git_parse_signed()`. This is part of a larger series (v4, patch 1/7) though the cover letter context isn't provided here.

The patch fixes an off-by-one error in the minimum value check when parsing signed integers. The current code incorrectly calculates the lower bound as `-max` when it should be `-max - 1` to properly handle the full range of signed integers (where the minimum is `-2^(N-1)` and maximum is `2^(N-1)-1`). The change affects the validation logic in `parse.c` that prevents integer overflow when parsing configuration values and options.

The fix is straightforward and appears correct - it properly accounts for the asymmetry in signed integer ranges. The patch is minimal (just one line changed) and comes with a clear explanation of both the problem and solution. This looks like a solid fix for a subtle edge case in integer validation that could affect any code path using `git_parse_signed()` with large negative values.