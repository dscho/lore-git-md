This is a follow-up discussion about Taylor Blau's patch series to enable bitmap lookup tables by default. Junio Hamano raises a technical question about maintaining consistency between two separate code paths that control bitmap writing behavior.

The core concern is whether the changes to `builtin/multi-pack-index.c` and `builtin/pack-objects.c` need to stay synchronized, and if so, what mechanism should ensure this. Hamano notes these use different flag systems (`MIDX_WRITE_BITMAP_*` vs `BITMAP_OPT_*`) and wonders if there might be other places that would need updating when changing defaults in the future.

This appears to be a thoughtful technical review point rather than opposition to the change itself. The question touches on maintainability concerns - how to prevent future drift between these related but independently-defined configuration points. The response will likely need to address whether these are indeed the only two places requiring updates, and whether some shared constant or conversion function might be warranted.

The discussion is still in early stages, with no other participants yet. This is the sort of implementation detail that often gets worked out in review before a patch series is finalized.