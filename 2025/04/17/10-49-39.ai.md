This is patch 4/7 in Patrick Steinhardt's integer parsing hardening series, focusing on API naming consistency in the parse-options infrastructure. The patch renames `OPT_MAGNITUDE()` to `OPT_UNSIGNED()` to better reflect its purpose, since the key distinction from `OPT_INTEGER()` is now signed vs unsigned handling rather than unit factor support (which both now have). The change updates all callers across builtins (gc, multi-pack-index, pack-objects, repack), documentation, and test code. 

The rename is straightforward mechanical replacement with no behavior changes. The patch demonstrates good attention to detail by updating both the implementation and all test cases that reference the old name. This continues the series' pattern of careful, comprehensive updates to the integer parsing infrastructure while maintaining backward compatibility. The change appears uncontroversial and aligns with the thread's established consensus on API design.