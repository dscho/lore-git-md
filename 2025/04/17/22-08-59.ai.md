This is a test cleanup patch in the ongoing bitmap lookup tables series. Junio C Hamano responds to Taylor Blau's patch that removes the duplicate p5312 performance test script, pointing out two minor wording issues in the commit message ("removed" should be "remove" and "added that" needs clarification). 

Junio suggests an alternative approach - rather than simply removing the duplicate test, it might be better to modify one of the tests to explicitly disable reverse indexes to maintain distinct test coverage. This would preserve the ability to test both configurations (with and without reverse indexes) rather than just eliminating the duplicate case.

The technical context shows this is part of Taylor's effort to clean up test scripts after making bitmap lookup tables the default. The original p5312 test became redundant when reverse indexes were enabled by default in commit a8dd7e05b1, making it identical to p5310. While Taylor's approach is to simply remove the duplicate, Junio's comment opens the possibility of keeping both tests with modified configurations to maintain broader coverage.

This exchange highlights the maintainer's attention to both commit message quality and test coverage philosophy, though it doesn't fundamentally challenge the series' direction. The thread continues to focus on test suite maintenance related to the bitmap lookup table changes.