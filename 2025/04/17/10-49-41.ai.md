This is patch 6/7 in Patrick Steinhardt's series hardening Git's integer parsing infrastructure, focusing on unsigned integer precision handling. The patch mirrors the precision handling added for signed integers in the previous patch, now applying it to `OPTION_UNSIGNED`.

Key technical changes:
- Adds precision-aware parsing for unsigned integers (8/16/32/64-bit) via new `git_parse_unsigned()` helper
- Exposes `git_parse_unsigned()` in parse.h (previously static)
- Implements range validation with type-specific bounds (e.g. 0-65535 for uint16_t)
- Updates test infrastructure:
  - Adds `--u16` test option to demonstrate 16-bit unsigned parsing
  - Includes new test cases verifying uint16_t range limits
- Maintains consistent error messaging with signed integer counterpart

The changes touch parse-options internals (48 lines modified), test helpers, and test scripts (18 new test lines). The implementation follows the same pattern as the signed integer precision handling from patch 5/7, completing the type safety work for both signed and unsigned variants.

This patch represents the final major technical component of the series' precision handling work, leaving only documentation updates (presumably in patch 7/7) before the series is complete. All core design questions appear resolved based on prior discussion, with the implementation now providing comprehensive integer parsing safety for both signed and unsigned values across all standard integer sizes.