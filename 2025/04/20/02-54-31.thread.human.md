This thread covers a straightforward refactoring series removing redundant NULL checks before `repo_config()` calls in builtin commands, following Patrick Steinhardt's suggestion after commit f29f1990b5 made `repo_config()` NULL-safe internally. The series consists of two mechanical patches applying the same change pattern to different commands.

The first patch modified `builtin/add.c`, removing a NULL check around a `repo_config()` call in `cmd_add()`. The just-arrived second patch does the same for `builtin/difftool.c`, eliminating a similar NULL check in `cmd_difftool()`. Both changes are behavior-preserving since `repo_config()` now handles NULL repositories internally, making the explicit checks redundant.

These are simple, low-risk cleanups that don't require test modifications. The patches follow the same format - removing the conditional wrapper while keeping the `repo_config()` call - demonstrating a systematic approach to eliminating now-unnecessary defensive programming across the codebase. The work appears complete with these two changes, though similar cleanups may follow in other commands if more redundant checks are found.