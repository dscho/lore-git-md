This is a refactoring patch series that removes redundant NULL checks before calling `repo_config()`. The series builds on a recent change (f29f1990b5) that made `repo_config()` handle NULL repository parameters internally, making these pre-checks unnecessary.

The two patches target different builtin commands - `git add` and `git difftool` - each removing an if statement that checks for NULL before calling `repo_config()`. The changes are mechanical and straightforward deletions of now-redundant conditionals, with identical 3-line changes in both files (removing the if statement while keeping the `repo_config()` call).

The motivation comes from Patrick Steinhardt's suggestion, noting that since `repo_config()` was modified to handle NULL repos, these checks are no longer needed. The changes appear safe as they don't introduce new behavior - they just remove code that has become redundant due to the earlier `repo_config()` modification.

This appears to be part of ongoing codebase cleanup rather than fixing any specific bug. The patches are small and focused, though they don't include tests since they're removing rather than adding functionality. The changes align with the recent effort to make repository-related functions more flexible in their NULL handling.