This is a refactoring patch in Karthik Nayak's series to implement header checking in Meson builds. The patch renames the `third_party_sources` variable to `third_party_excludes` to better reflect its actual purpose - it's used to exclude third-party sources from header checking rather than include them. The change makes the code's intent clearer.

The patch also removes a duplicate entry for `t/unit-tests/clar` that was accidentally included twice in the exclusion list. This is a straightforward cleanup that improves maintainability without changing functionality.

This is part of a larger effort to bring the Meson build system's header checking capabilities in line with the existing Makefile implementation. The series has gone through several rounds of review, with this particular change addressing feedback about variable naming clarity. The patch appears well-tested and ready for inclusion.