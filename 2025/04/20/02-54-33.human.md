Refactoring patch removing redundant NULL checks in `difftool`. This is the second of two mechanical patches following Patrick Steinhardt's suggestion to clean up now-unnecessary NULL checks after commit f29f1990b5 made `repo_config()` NULL-safe. The change removes an if statement in `cmd_difftool()` that previously guarded a `repo_config()` call, since the function now handles NULL repositories internally.

The patch follows the same simple pattern as the first in the series (which modified `builtin/add.c`): deleting the conditional wrapper while keeping the `repo_config()` call intact. The change is straightforward and low-risk, as it doesn't modify behavior - it just removes code made redundant by the earlier NULL-safety improvement. 

The commit message properly credits both the original suggestion and mentoring help. Like the first patch, this appears to be a clean, focused change that contributes to the ongoing effort to simplify the codebase by removing now-unnecessary repository checks. No test changes are needed since this is purely a code cleanup with no functional impact.