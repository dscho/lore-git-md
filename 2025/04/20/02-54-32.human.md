Refactoring patch removing redundant NULL checks in `builtin/add.c`. This is the first of two mechanical patches following Patrick Steinhardt's suggestion to clean up now-unnecessary NULL checks after commit f29f1990b5 made `repo_config()` handle NULL repositories internally. The change simply removes an if statement that was previously guarding the `repo_config()` call in `cmd_add()`. 

The patch is straightforward - a 3-line deletion that removes the conditional while keeping the `repo_config()` call. As noted in the thread context, this is part of ongoing cleanup to remove redundant checks after making repository-related functions NULL-tolerant. The change is safe since it doesn't modify behavior, just removes code made obsolete by the earlier change.

Usman Akinyemi provides proper attribution to both the original suggestion (Steinhardt) and mentoring (Couder). The patch follows the standard Git project conventions and appears to be a clean, well-motivated change. While small, it contributes to the broader effort to simplify the codebase by removing now-unnecessary safety checks.