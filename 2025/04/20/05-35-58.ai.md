This is a discussion about implementing optional file handling in Git, specifically regarding the `blame.ignoreRevsFile` configuration. The email provides historical context about a previous attempt to implement a `:(optional)` prefix mechanism for file paths, which was proposed by Junio C Hamano as a more general solution than the blame-specific approach in the original patch.

Eric Sunshine notes that an initial implementation of the `:(optional)` syntax already existed in Junio's "seen" branch but was dropped due to lack of review rather than technical issues. This suggests the concept is viable and could be revived as part of the current discussion about improving file handling behavior.

The thread has evolved from a specific blame configuration issue to considering a broader Git-wide mechanism for optional file handling. The key technical elements under discussion include:
- The `:(optional)` path prefix syntax
- Integration with parse-options API for command-line options
- Potential modifications to git_config_pathname() for config values
- Maintaining strict checking by default while allowing explicit opt-in

This email provides important context that the proposed solution isn't purely theoretical - a working implementation already existed, which could help accelerate the current discussion about adopting this approach. The lack of previous review attention (rather than technical problems) as the reason for its previous abandonment suggests this might be worth revisiting now that there's active interest in solving this class of problems.