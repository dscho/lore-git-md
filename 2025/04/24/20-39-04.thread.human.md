This thread has evolved into a technical deep dive on improving signature handling in Git's `fast-export`/`fast-import` machinery, now focusing on properly representing different hash algorithms and signature formats. The discussion began with concerns about signature stripping behavior and has progressed through multiple iterations of implementation and documentation refinements.

The latest patch from Christian Couder introduces significant improvements to the experimental signature support, adding the ability to handle both SHA-1 and SHA-256 signatures from the same commit while clearly documenting the new format. The changes include new helper functions in the GPG interface code and expanded test coverage, though testing for the dual-signature case remains outstanding. The implementation maintains backward compatibility while adding warnings for multiple signatures of the same hash type.

Junio Hamano has maintained a pragmatic stance throughout, marking the feature as experimental and considering the current documentation sufficient. brian m. carlson provided key technical input about properly distinguishing between object hash algorithms and signature protocol algorithms. While the core technical issues around test failures and integration work remain, the thread shows clear progress on the signature format representation front, with the latest patch addressing several of the previously noted limitations. The experimental status of the feature remains, but the implementation is becoming more robust with each iteration.