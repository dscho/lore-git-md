This thread has been discussing improvements to signature handling in Git's `fast-export` and `fast-import` commands, particularly around how to handle signed commits during the export/import process. The discussion has evolved from technical implementation details to broader questions about when and how to change default behaviors in Git.

Junio Hamano, Git's maintainer, recently made several key decisions to mark the new signature handling features as experimental and change their default behavior from 'abort' (reject signed commits) back to 'strip' (remove signatures silently). He justified this as a pre-release correction rather than a deprecation, since the feature hasn't shipped in a stable release yet. Documentation has been updated accordingly, and an environment variable was removed as it became unnecessary.

Elijah Newren has now raised an important question about this decision, pointing to Junio's earlier objections to silently stripping signatures. He notes that Junio previously considered stripping signatures to be "the wrong thing" and aborting as potentially a bugfix. Elijah presents three possible interpretations of Junio's current position and indicates his own preference for keeping the default behavior stable until the feature design is more complete.

The thread exemplifies Git's careful approach to changes that affect user expectations and backward compatibility. While technical improvements to signature handling continue in parallel, this discussion has highlighted the project's thoughtful process for evaluating when and how to modify default behaviors, especially for features that aren't yet fully baked. The exchange between Elijah and Junio provides valuable window into Git's decision-making around balancing technical progress with stability.