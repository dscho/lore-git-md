Here's a human-readable summary of the thread's current state:

This thread has been discussing improvements to signature handling in Git's `fast-export` and `fast-import` commands, focusing on how to properly represent cryptographic signatures when moving commits between repositories. After several rounds of technical discussion and policy debate, the community has settled on marking the feature as experimental for now, with the default behavior reverted to the pre-feature state (stripping signatures).

The latest exchange provides an important technical clarification from brian m. carlson, who emphasizes that any future implementation must properly distinguish between the hash algorithm used in the commit object itself (SHA-1 or SHA-256) and the algorithm used within the signature protocol (like OpenPGP or X.509). This matters because a single commit could contain multiple signatures using different algorithms. While this doesn't change the current experimental status, it sharpens the technical requirements for any future implementation.

Key participants have aligned around Junio Hamano's position that the experimental marking and current documentation sufficiently communicate the feature's immaturity. The discussion has progressed from initial implementation through policy debates about defaults, to now focusing on precise technical requirements for when the feature matures. While the immediate path forward is clear (keeping the feature experimental), there remains underlying technical work needed around test integration and proper hash algorithm handling before the feature can be considered production-ready.