This is a discussion about Git's handling of symlinks in `.git` internals, particularly regarding the `objects/` directory. Junio C Hamano responds to Brian Carlson's query about whether symlinking `.git/objects` should be supported, noting that while the `contrib/workdir/git-new-workdir` script creates symlinks for sharing repository data, symlinking the entire `.git` directory itself is not an intended use case. 

The exchange highlights that symlinking specific subdirectories like `hooks/` is somewhat supported, but broader symlink usage (like the whole `.git` directory) is not officially endorsed. Both agree that if support is to be added, tests should be written, and if not, warnings or advice should discourage the practice. 

This continues the thread's exploration of whether Git should formally support, discourage, or maintain the status quo regarding symlinks in `.git/` internals, with implications for tools like Git LFS and Android's `repo` tool. The discussion remains open-ended, with no clear resolution yet on how to proceed.