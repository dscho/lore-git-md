This is a technical discussion about signature handling in `git fast-export`/`fast-import`, specifically addressing how to represent different signature algorithms and hash types. Brian m. carlson responds to Christian Couder's proposed documentation of valid signature algorithm names, arguing that the current approach conflates two distinct concepts: the signature protocol (OpenPGP, X.509, SSH) and the hash algorithm (SHA-1, SHA-256).

The key critique is that the proposed documentation doesn't properly account for cases where different hash algorithms might be used with the same signature protocol, or where multiple signature types might be applied to the same commit with different hashes. Brian suggests either separating these into distinct fields or relying on parsing the signature content itself to determine the protocol.

This email advances the thread's technical discussion by identifying a limitation in the current signature handling design. It doesn't propose a specific solution but raises important considerations about how Git should represent these combinations, particularly for edge cases that aren't currently supported but are technically valid. The response maintains the thread's focus on precise signature handling while introducing new technical constraints to consider.