This is a follow-up in the ongoing Meson build system integration for Git's benchmark infrastructure. Patrick Steinhardt clarifies a technical point about the `time` command requirement for benchmarks.

The discussion has already settled on using Meson feature options (`auto`/`enabled`/`disabled`) for handling the GNU `time` dependency, with maintainer approval. Patrick now confirms that shell built-in `time` commands don't satisfy the benchmark requirements - the infrastructure specifically needs GNU `time` for its extended metrics. He notes this matches the `USR_BIN_TIME` prerequisite in the existing test infrastructure, though he briefly considers (but dismisses) refactoring this single-use prerequisite.

The email serves as a technical clarification rather than proposing new changes, indicating the series is in its final stages with all major decisions made. The only remaining loose end is Patrick's passing thought about the `USR_BIN_TIME` refactor, which he doesn't seem inclined to pursue for this series.