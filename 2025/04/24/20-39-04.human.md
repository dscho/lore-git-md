This is a bugfix and documentation improvement for Git's fast-import/export signature handling. Christian Couder follows up on recent work that added signed commit support to these commands, addressing an issue where all signatures were being labeled as "sha1" regardless of their actual algorithm.

The patch makes several improvements:
1. It properly identifies signature types (OpenPGP, X.509, SSH) using existing gpg-interface code rather than defaulting to "sha1"
2. Adds "unknown" as a fallback for unrecognized signatures
3. Updates documentation to clearly explain the signature algorithm naming
4. Adds tests verifying the new behavior with different signature types

The changes are straightforward and build on existing infrastructure. The commit message clearly explains the change and its motivation (better tool interoperability). The patch includes thorough documentation updates and new test cases covering X.509 and SSH signatures, suggesting it was carefully validated.

This appears to be a focused, well-executed follow-up to recently merged functionality, addressing a clear limitation in the initial implementation.