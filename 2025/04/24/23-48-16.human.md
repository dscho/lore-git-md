This is a brief follow-up in the ongoing Meson build system refactoring thread, specifically about checking for the deprecated `getpagesize()` function. The patch adds `getpagesize()` to Meson's function availability checks, noting it was removed in POSIX 2001 but still used in Git's codebase. 

Junio Hamano asks whether this relates to a recent build failure in Git's CI system. The context suggests this may be addressing an actual compatibility issue that surfaced, though the connection isn't fully explained in this email. The patch itself is straightforward - just adding the function to Meson's check list - but it touches on the broader theme of modernizing build system checks while maintaining compatibility with older systems.

The thread shows this is part of careful work to standardize function detection across platforms, with attention to historical quirks (like QNX's special handling in config.mak.uname). The discussion remains technical and focused, with maintainers actively verifying real-world impact of such changes.