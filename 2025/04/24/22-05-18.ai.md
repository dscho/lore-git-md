This is a technical discussion about signature algorithm handling in `git fast-export`/`fast-import`, specifically addressing how to represent different signature types and hash algorithms. Brian m. carlson responds to Christian Couder's proposed documentation of valid signature algorithm names, pointing out that the current approach conflates two distinct concepts: the signature protocol (OpenPGP/X.509/SSH) and the hash algorithm (SHA-1/SHA-256).

The key technical issue raised is that the current documentation doesn't properly account for the orthogonal nature of signature protocols and hash algorithms. Brian notes that a commit could theoretically use different combinations (e.g., SHA-256 content signed with SSH, or different signatures using different protocol/hash combinations for the same content). They suggest either separating these into distinct fields or keeping just the hash field while deriving the protocol from signature content.

This email advances the thread by introducing new technical considerations about signature representation that weren't fully addressed in the initial implementation. The discussion now needs to reconcile the desire for clear documentation with the more complex reality of possible signature/hash combinations. The solution may require either expanding the format to track both dimensions separately or finding a way to reliably infer one from the other.

The email doesn't propose specific code changes but rather identifies a conceptual gap in the current approach that needs resolution before finalizing the signature handling improvements. This input will likely require updates to both the implementation and documentation to properly handle all valid signature scenarios.