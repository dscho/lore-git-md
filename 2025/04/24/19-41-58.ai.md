This is a minor fixup patch in the ongoing `index-pack` REF_DELTA chain bugfix series. Junio Hamano contributes a one-line memory leak fix for the `test-tool pack-deltas` helper that was introduced earlier in the series. The change adds a missing `string_list_clear()` call to clean up allocated memory in the test helper's command processing.

The patch addresses a leak that was caught when running the test suite with `SANITIZE=leak` enabled. The fix is straightforward - properly freeing the `items` string list that was used to parse input for delta test cases. This maintains the test helper's robustness while keeping the focus on its primary purpose of constructing packfiles with specific delta structures for testing.

The change is minimal (just 1 line added) and clearly correct, cleaning up resources that were previously being leaked. It follows naturally from the earlier discussion about the test helper's implementation, where Junio had previously reviewed the code and suggested naming improvements. This fixup demonstrates the iterative refinement of the test infrastructure that supports the main bugfix for `index-pack`'s delta chain handling.

Key technical details for future reference:
- Fixes a leak in `t/helper/test-pack-deltas.c`
- Adds `string_list_clear(&items, 0)` after processing each test case
- Maintains the test tool's ability to construct REF_DELTA chains for testing
- Follows up on earlier test helper implementation that was reviewed in this thread

The patch is clearly a quality improvement to the test infrastructure, though it doesn't affect the core bugfix logic. It shows the kind of attention to detail expected in Git's test suite, particularly for new test helpers that may be reused in future testing scenarios.