This is a discussion probe about supporting symlinks in `.git` internals, prompted by Git LFS compatibility issues with Android's `repo` tool. The email raises a question about whether Git should officially support having symlinks for internal `.git` directories like `objects/`, noting that while symlinking `hooks/` is somewhat supported, other directories currently cause problems (particularly with path canonicalization in Git LFS). The author cites two Git LFS bug reports showing real-world impact from Android's `repo` tool creating such symlinks.

Key technical points for future reference: the issue manifests when operations run from symlinked `.git/objects` cause path resolution failures in Git LFS's `.git` directory detection. The author explicitly mentions this might not be the only problem. They list several alternative Git implementations (JGit, Game of Trees, etc.) whose behavior with such symlinks is unknown.

The core question is framed as a policy decision: should Git support this usage pattern (requiring test additions) or explicitly discourage it (perhaps via warnings)? The thread seems likely to involve discussion of historical behavior, filesystem edge cases, and compatibility tradeoffs, with potential implications for both core Git and tools like Git LFS that interact with repository internals.