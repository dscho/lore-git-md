This thread introduces a build system change to align the meson build configuration with the existing Makefile behavior for Perl library paths. The patch adds a `perllibdir` meson option that allows customization of where Perl libraries are installed, defaulting to `<datadir>/perl5` but supporting overrides needed by distributions like Fedora. The change updates paths across 10 Perl-related meson build files to use this new variable instead of hardcoded paths, affecting both core Git Perl files and CPAN modules.

The patch is well-scoped and maintains compatibility with the Makefile build system while following meson's configuration style. It appears to be a straightforward improvement that addresses a specific packaging need without introducing new dependencies or complexity. The changes are consistently applied across all affected files.