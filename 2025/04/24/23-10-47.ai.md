CI/build system patch continuing the immediate fix for JGit download failures in Git's Linux CI jobs. This expands the earlier targeted fix to handle all external dependency downloads (Perforce, Git-LFS, and JGit) in a consistent way.

The patch modifies `ci/install-dependencies.sh` to make all three dependency downloads optional, with proper cleanup when downloads fail. Each download operation now includes error handling that removes partially downloaded files and emits a warning, while allowing the script to continue. This matches the test framework's existing ability to skip tests when dependencies are missing.

Key technical details:
- Changes the wget/chmod sequences for P4, Git-LFS, and JGit to use `&&` chains with `||` error handling
- Adds cleanup logic to remove partially downloaded files on failure
- Maintains the same warning pattern ("download (optional) failed") for all three cases
- Preserves the existing behavior where tests will later detect and handle missing dependencies

The change appears well-scoped and consistent, addressing the immediate CI outage while following the pattern already established in the test framework. The patch builds on the earlier discussion about JGit specifically, now applying the same principle to all external dependencies in the script.