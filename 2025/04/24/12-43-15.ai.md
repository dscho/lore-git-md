This is a technical correction in the ongoing `git send-email` OAuth2 patch series discussion. Junio Hamano points out a documentation formatting issue in Aditya Garg's proposed `--smtp-passeval` option syntax. The current patch incorrectly marks the command argument as optional with square brackets (`[=<command>]`), when it should be mandatory like other SMTP options (`=<command>`). 

Junio provides a clear example comparing it to the existing `--smtp-user` option syntax, which takes a mandatory argument. This is a small but important documentation fix for consistency with Git's option syntax conventions. The exchange shows active review of the credential handling approach in the series, though this particular point is purely about documentation formatting rather than the underlying OAuth2 functionality.

The thread context shows this is part of broader discussions about authentication mechanisms in the OAuth2 support series, where security and interface design are being carefully reviewed. This email doesn't introduce new technical content but ensures the documentation accurately reflects the mandatory nature of the command parameter.