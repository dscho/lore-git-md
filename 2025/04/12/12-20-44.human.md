Documentation modernization converting `git-reset` man page to AsciiDoc format. This is the first patch in a 7-part series standardizing documentation for `git-reset`, `git-rm`, and `git-mv`.

The patch updates the `git-reset` documentation with several structural and formatting improvements:
- Replaces the verse-style synopsis with a proper synopsis block that automatically formats placeholders and keywords
- Standardizes on `_<placeholder>_` notation for variables and backticks for commands/options
- Maintains all existing content while improving consistency with other modern Git docs
- The changes are mechanical but thorough, touching nearly every line of the file
- preserving the exact same information while making it more readable and toolchain-friendly
- Part of an established documentation standardization effort following patterns used in prior conversions

The patch appears well-crafted and follows the project's documentation conventions. As the first in the series, it sets the pattern for the subsequent `git-rm` and `git-mv` conversions. The only outstanding issue noted in the thread is a TROFF rendering quirk for special characters, which doesn't affect this specific content.