Documentation toolchain patch addressing triple-dot notation handling in the AsciiDoc conversion series. This sixth patch in the v2 series focuses on fixing edge cases where triple-dots in command synopses (like `git-mv <source>... <dest>`) could be misinterpreted as ellipses during processing.

The changes modify both the AsciiDoc and Asciidoctor toolchain components to better distinguish between literal triple-dots (command syntax) and ellipses (punctuation). Key technical adjustments include:
- Updated regex patterns in `asciidoc.conf.in` to properly handle command syntax with trailing commas
- Enhanced processing in `asciidoctor-extensions.rb.in` with new patterns for standalone triple-dots and improved context handling
- Added special case handling for triple-dots at line boundaries
- Maintained consistency between DocBook/XML (`<literal>`) and HTML (`<code>`) output formats
- Preserved all existing special character handling for backslashes and asterisks from previous patches

This builds on the earlier toolchain improvements in the series while specifically addressing the `git-mv` command's use of triple-dot notation. The changes demonstrate careful attention to edge cases while maintaining the series' goal of consistent documentation rendering across output formats. The patch appears technically sound, with regex patterns that precisely target the problematic cases without disrupting other syntax elements.