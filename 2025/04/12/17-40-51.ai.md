Test coverage addition for the `git add` wildcard pathspec bugfix. This patch adds a new test file t3706-add-wildcard-literal.sh that verifies the correct handling of pathspecs when literal filenames match wildcard patterns. The test creates files with names like 'f*' and 'f**', then checks that `git add` properly distinguishes between escaped literal matches (`f\*\*`) and unescaped wildcard patterns (`f*`).

The test suite creates three files: 'f*', 'f**', and 'foo' in a test directory. It then verifies two key scenarios: (1) that `git add "f\*\*"` correctly adds only the literal file 'f**', and (2) that `git add 'f*'` (with shell quoting) properly treats the pattern as a wildcard, adding all three matching files. The test follows Git's standard test structure with setup, cleanup, and verification steps.

This builds directly on the earlier bugfix for dir.c's pathspec handling, providing concrete validation of the fix's behavior. The test cases specifically address the edge case that motivated the original patch - where literal filenames containing wildcard characters could interfere with intended wildcard expansion. The test file is added to the meson.build configuration to ensure it runs as part of the test suite.

Key technical details: The test uses shell escaping and quoting carefully to ensure the patterns are passed to Git exactly as intended, avoiding shell expansion. It verifies the actual behavior against expected results using `git ls-files` output comparison. The test follows Git's standard pattern of creating a clean state before each verification step.