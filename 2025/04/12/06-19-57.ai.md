Test improvement patch finalizing the Perforce label import verification changes. This v6 patch concludes a six-iteration series that improves the reliability of tag verification in `t/t9811-git-p4-label-import.sh` by replacing fragile `grep` checks with direct `git show-ref --verify` calls.

The key changes in this final version are:
1. Replaces all remaining `git tag | grep` patterns with explicit ref verification
2. Adds a negative test case checking for absence of TAG_F1_ONLY
3. Fixes the original issue where pipe exit codes were being lost
4. Removes legacy `git tag` commands as confirmed by Junio

The patch demonstrates thorough attention to edge cases, now verifying both presence of expected tags (TAG_F1_1, TAG_F1_2) and absence of unintended tags (TAG_F1_ONLY). The changes follow Git's preferred verification patterns and eliminate text-matching fragility. All feedback from previous rounds has been addressed, including Junio's suggestions about commit message structure and scope boundaries for test improvements.

Technical details:
- Uses `git show-ref --verify` exclusively for all tag checks
- Adds `test_must_fail` verification for non-existence case
- Maintains test coverage while making verification more robust
- File modified: t/t9811-git-p4-label-import.sh
- Builds on historical context from commit e71f6a53e2

The series is now ready for inclusion, having addressed all technical and stylistic feedback through six iterations, with consensus from multiple reviewers including Junio Hamano and Eric Sunshine. The changes exemplify Git's commitment to robust test infrastructure through direct object verification rather than text matching.