Bugfix for `git add` wildcard pathspec handling when literal filenames match the pattern. This patch addresses an edge case where pathspecs like 'f*' would incorrectly prioritize a literal file named 'f*' over wildcard expansion, causing inconsistent behavior between first and subsequent invocations.

The fix modifies the pathspec matching logic in dir.c to prevent premature short-circuiting when exact matches exist, while maintaining backward compatibility. Users who want to explicitly target a literal file matching a wildcard pattern can still do so by escaping the asterisk (e.g., 'f\*').

The change is minimal (3 lines in dir.c) but corrects a real inconsistency in path handling. The patch appears well-motivated and targeted, with a clear explanation of both the bug and solution. It includes proper attribution with Reported-by and Mentored-by tags, suggesting it went through some review before submission.

This fixes a subtle but potentially confusing behavior that could trip up scripts or users relying on wildcard expansion. The fix maintains the existing escape mechanism for literal matches while making wildcard behavior more consistent.