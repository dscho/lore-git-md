This is a documentation patch in Jean-Noël Avila's series modernizing command man pages. It's part 4 of 7 in version 2 of the series, focusing specifically on `git-mv`.

The patch standardizes the `git-mv` documentation by moving the command variants from the DESCRIPTION section into the SYNOPSIS section where they belong, matching the pattern established for other commands in this series. The change is mirrored in the built-in command's help text to maintain consistency between documentation and runtime output.

The update follows the project's new documentation standards, using proper formatting for options (`-v`, `-f`) and clearly separating the two invocation forms (single rename vs. multiple sources with directory destination). This continues the series' thorough work of bringing all command documentation to a consistent standard while preserving all existing functionality and semantics.

The patch appears well-executed, with corresponding changes to both the man page source and the C implementation, showing attention to detail in keeping these synchronized. It builds on the earlier patches' improvements to special character handling in the documentation toolchain.