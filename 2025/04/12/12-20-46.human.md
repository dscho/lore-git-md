Documentation modernization converting `git-rm` to AsciiDoc format. This is patch 3/7 in version 2 of a series standardizing documentation for `git-reset`, `git-rm`, and `git-mv`.

The patch updates the `git-rm` man page to match the new documentation standards, with several key changes: converting the synopsis to a dedicated synopsis block for better formatting, using `_<placeholder>_` notation consistently, and wrapping options in backticks. The content remains the same while improving presentation.

Notable technical updates include proper formatting of path specifications, submodule documentation (now using `submodule.<name>` notation), and consistent rendering of special options like `--pathspec-from-file`. The changes align with feedback from version 1 of the series, maintaining the same careful attention to detail seen in the earlier `git-reset` conversion.

This continues the project's documentation standardization effort, with all substantive content preserved while refining the presentation. The patch appears well-executed, following established patterns from prior documentation conversions.