Bugfix for `git add` pathspec matching optimization flaw.

This email provides a detailed technical analysis of the wildcard matching bug reported earlier, identifying the root cause in the pathspec handling logic. The issue occurs in `do_match_pathspec` when a pathspec like `f*` both exactly matches a literal filename (`f*`) and should also match other files via wildcard (`foo`). The current optimization in `do_match_pathspec` incorrectly skips wildcard matching after finding an exact match, causing the pathspec to only match the literal file on the first run.

Key technical points:
- The bug manifests in `prune_directory` -> `do_match_pathspec` -> `match_pathspec_item` chain
- The `seen` array optimization marks pathspecs as `MATCHED_EXACTLY` too aggressively
- Subsequent files are skipped from wildcard matching due to this premature optimization
- Behavior is consistent across Git versions, indicating a long-standing logic flaw

The analysis suggests two potential fix approaches:
1. Modify `match_pathspec_item` to not return `MATCHED_EXACTLY` when globbing is involved
2. Adjust the `seen` array check in `do_match_pathspec` to handle the glob/exact match ambiguity

The email provides a thorough walkthrough of the code path and clearly explains why the current behavior violates expected wildcard matching semantics. This sets up useful context for potential fixes by precisely identifying where the matching logic goes wrong.