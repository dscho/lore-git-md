Build system fix for meson configuration after performance test cleanup.

This is a follow-up to the earlier patch that removed a performance test script but missed updating the meson build configuration. Junio Hamano notes that the fix has already been applied as an "evil-merge semantic conflict fix-up" to address the build breakage, rather than waiting for the original branch to be rebased.

The technical change is straightforward - removing a single line from t/meson.build that referenced the now-deleted performance test script. This is purely a build system fix with no impact on Git's runtime behavior. The patch author had suggested squashing this into the original commit if the branch needed re-rolling, but Junio explains why that approach wouldn't work cleanly given the current branch topology.

The thread shows this was a simple oversight in the original change (commit 8b0b5c7046) that removed the performance test script. The fix is uncontroversial and has already been merged to resolve the immediate build breakage. No further discussion appears needed on this specific issue.