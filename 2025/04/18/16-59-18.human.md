This is a build system fix following up on a recent performance test cleanup. Ramsay Jones points out that commit 8b0b5c7046 removed a performance test script but forgot to remove its reference from the Meson build configuration, causing build failures. The patch simply removes the now-missing script from the build file.

The change is straightforward and clearly correct - it's just keeping the build configuration in sync with the test directory contents. Ramsay notes this should ideally be squashed into the original commit that removed the test script, and addresses the maintainers handling that branch directly. This is a minor but necessary cleanup to fix broken builds.