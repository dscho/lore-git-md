This is a follow-up discussion about test coverage in Taylor Blau's bitmap lookup tables patch series. Junio Hamano raises concerns about completely removing performance tests for the non-default (lookup-table-disabled) configuration, even as the series makes lookup tables the default.

Junio suggests the removal may be premature since Git still supports both configurations, drawing a parallel to the recursive/ORT merge strategy transition where inferior options were only removed after proving the new approach was universally better. He frames the lookup-table-disabled mode as a valuable "escape hatch" that should remain tested until lookup tables prove completely superior in all aspects.

The comment implies Junio would prefer keeping some performance testing of the non-default path for now, though he acknowledges performance test removal is less critical than correctness test removal. This continues the thread's ongoing tension between test suite maintenance (removing redundancy) versus ensuring coverage of all supported code paths.

Key technical points:
- References the bitmap lookup table series (MIDX_WRITE_BITMAP_LOOKUP_TABLE/BITMAP_OPT_LOOKUP_TABLE flags)
- Draws analogy to recursive/ORT merge strategy transition
- Introduces "escape hatch" metaphor for non-default configuration
- Suggests test removal should wait until lookup tables prove universally superior

This email doesn't propose concrete changes but signals Junio's preference for more conservative test coverage until lookup tables are proven beyond needing a fallback path. The discussion may influence whether future versions of the series retain some non-default configuration testing.