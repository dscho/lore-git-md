This is a build system fix following up on a performance test cleanup. The patch removes a reference to a deleted performance test script from the meson build configuration.

The commit 8b0b5c7046 had removed the 't/perf/p5312-pack-bitmaps-revs.sh' script but missed updating 't/meson.build', causing build failures. This follow-up patch cleans up that oversight by removing the now-invalid reference from the meson configuration file.

The change is straightforward - a single line deletion from the benchmarks list in t/meson.build. The author requests that this be squashed into the original commit if the 'tb/pack-bitmap-lookup-tables' branch needs to be re-rolled, as the current state breaks meson builds.

This appears to be a simple but necessary follow-up to maintain build consistency after the test reorganization. The patch is correctly targeted and well-explained, with a clear request for how it should be integrated with the prior work.