This is a build system fix addressing a meson configuration issue that arose from a performance test cleanup. 

The patch removes a reference to a deleted performance test script from `t/meson.build`, fixing a broken build. The original test script was removed in commit 8b0b5c7046 as part of the 'tb/pack-bitmap-lookup-tables' branch work, but the meson build configuration wasn't updated to match.

Junio C Hamano notes this needs to be handled as an "evil-merge semantic conflict fix-up" rather than being squashed into the original commit, since the relevant meson build changes aren't yet in the 'next' branch. He's already applied the fix.

This is a straightforward cleanup with no functional impact - it simply keeps the build system in sync with the test directory changes. The patch is well-justified and clearly explained, though the discussion reveals some non-trivial integration considerations due to branch timing.