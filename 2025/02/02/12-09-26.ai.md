This is a test fix addressing a subtle issue in the merge-rename-directories test script. The patch corrects three test cases in t6423 that were suppressing Git's exit code by piping `git ls-files` output directly to `uniq`. This masking could cause test failures to go unnoticed if the Git command failed.

The fix modifies each affected test to first capture Git's output to a file before processing it with `uniq`, ensuring any failure in `git ls-files` would be properly detected. The changes are mechanical but important for test reliability, touching three similar test cases (12i, 12j, and 12k) that verify directory rename scenarios during merges.

Key technical details for future reference:
- Tests modified: 12i, 12j, 12k in t6423-merge-rename-directories.sh
- Pattern changed: `git ls-files | uniq` → `git ls-files >actual && uniq <actual`
- Tests verify tracked file counts after directory-rename merges
- No behavioral change to Git itself - purely a testing improvement

The patch appears straightforward and correctly addresses the identified issue. The commit message clearly explains the problem and solution, though it might benefit from mentioning that this affects multiple test cases sharing the same pattern.