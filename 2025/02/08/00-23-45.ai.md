This is a technical clarification in the ongoing discussion about Git's renormalization behavior during history rewriting operations. Elijah Newren responds to Josef Wolf's question about the performance/correctness tradeoff by:

1. Correcting potential misunderstandings about using `-X renormalize` alone for history rewriting, explaining it doesn't ensure normalized content in new trees
2. Analyzing Phillip Wood's suggested workflow combining `git add --renormalize` with rebase, noting it would work but requires manual conflict resolution for line endings
3. Proposing a combined approach using both `-X renormalize` and `git add --renormalize` that should handle linear history cases
4. Clarifying the fundamental limitation that merge's renormalize option only affects new blobs during three-way merges, not pre-existing content
5. Reiterating that while the current behavior is technically correct per the documented merge machinery, it may not match user expectations for comprehensive repository normalization

Key technical details:
- The distinction between `-X renormalize` (merge strategy option) and `git add --renormalize` (working tree operation)
- Why combining both approaches might work for linear history cases
- Fundamental architectural constraint that merge renormalization doesn't rewrite existing blobs
- Reference to merge machinery documentation about virtual check-out/check-in behavior

This message provides important implementation details about Git's merge machinery while acknowledging the gap between current capabilities and user expectations for comprehensive repository normalization. It doesn't introduce new solutions but clarifies the technical boundaries of existing ones.