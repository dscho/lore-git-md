Bugfix for graph/diff state management: Emily Klassen follows up on the thread about `git log` crashes when mixing `--graph` and `--no-graph` flags, confirming the issue persists on `next` but identifying the specific cause. The problem stems from `revs->diffopt.output_prefix_data` not being properly cleared when `--no-graph` is processed in `revision.c`. This matches the earlier discussion where Đoàn Trần Công Danh observed memory-related failures from the same flag combination, though his case was resolved by the already-merged `jk/combine-diff-cleanup` series. Emily indicates she's preparing a patch to explicitly null this field during `--no-graph` handling, suggesting this may address both the segmentation fault and memory error manifestations of what appears to be a related underlying issue. The thread now awaits her proposed fix to see if it cleanly resolves both reported failure modes.