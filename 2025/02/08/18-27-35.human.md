Refactoring patch addressing compiler warnings in the refs subsystem. This is a straightforward technical cleanup from a new contributor, removing the `DISABLE_SIGN_COMPARE_WARNINGS` macro from refs.c and fixing several integer comparison issues that trigger `-Wsign-compare` warnings when compiling with developer flags.

The changes are minimal and focused - converting loop counters from `int` to `size_t` where appropriate and adding a cast in one comparison. The patch touches five locations across refs.c, all related to signed/unsigned integer comparisons. The author reports testing with `DEVELOPER=1` builds and the test suite, though the commit message doesn't elaborate on any specific test results.

While the changes appear technically correct, the patch would benefit from more context about why these warnings were originally suppressed and confirmation that the type changes don't affect behavior at runtime. As part of ongoing code quality improvements, this is a small but useful step toward cleaner compiler output, though more substantial refactoring might be needed to fully address signed/unsigned comparison patterns in the codebase.