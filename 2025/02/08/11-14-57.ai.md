This is a technical refinement in the ongoing discussion about Git's renormalization behavior with clean/smudge filters during history rewriting operations. Phillip Wood builds on Elijah Newren's analysis by testing a combined approach that uses both `-X renormalize` and `git add --renormalize` during rebase, with a key modification to handle `.gitattributes` lookup for pre-filter commits.

The email provides a concrete test case demonstrating the workflow works for linear histories when adding `--attr-source=$(git rev-parse HEAD)` to ensure Git can read the correct `.gitattributes` even for commits made before the filter was configured. Phillip's shell script creates a test repository with progressively added lines, then applies the space-normalizing filter and rebases the entire history.

Notably, Phillip confirms Elijah's observation that this approach requires manual conflict resolution for line-ending differences, but the combination of strategies appears functional for the linear case. The email also surfaces an open question about whether this renormalization workflow should be documented in Git's FAQ, referencing Elijah's technical footnote about the limitations of merge machinery's renormalization behavior.

This represents a practical validation of the theoretical approach discussed earlier in the thread, though the solution remains constrained by Git's fundamental architecture where renormalization only affects new blobs during three-way merges. The test case serves as a useful reference for others facing similar filtering challenges during history rewriting.