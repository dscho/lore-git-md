This is a technical follow-up in the ongoing discussion about Git's renormalization behavior with smudge/clean filters, specifically addressing edge cases in history rewriting workflows. Elijah Newren responds to Phillip Wood's practical test case demonstrating a working solution for linear history rebasing with filters.

The key technical advancement is Phillip's addition of `--attr-source=$(git rev-parse HEAD)` to ensure `.gitattributes` is available when processing historical commits. Elijah confirms this works by tracing how the attribute source propagates via the `GIT_ATTR_SOURCE_ENVIRONMENT` variable to subprocesses. The discussion highlights two important workflow components: combining `-X renormalize` with `git add --renormalize`, and handling the environment variable inheritance for attribute lookup.

The email also revisits the fundamental limitation that merge renormalization only affects new blobs during three-way merges, not existing repository content. Both participants acknowledge this solution's assumption of linear history and discuss potential documentation improvements, suggesting this workflow might belong in Git's FAQ despite its specialized nature. The exchange demonstrates practical validation of earlier theoretical discussions while surfacing implementation details about environment variable handling in Git's plumbing.