Bugfix follow-up for graph state management issue. Đoàn Trần Công Danh responds to Emily Klassen's report of a `git log` segfault when mixing `--graph` and `--no-graph` flags, noting that the original test case now produces an out-of-memory error rather than a crash on their system. They suggest the issue may already be fixed in `master` by the `jk/combine-diff-cleanup` series and ask Emily can verify this.

This is a helpful data point in diagnosing the graph state bug, though it's unclear whether the out-of-memory error represents the same root cause as Emily's segmentation fault or a different manifestation. The response is appropriately concise and moves the investigation forward by pointing to a potential existing fix while still leaving room for further debugging if needed.