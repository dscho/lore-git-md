This is a follow-up in the ongoing discussion about Git's renormalization behavior with clean/smudge filters, specifically addressing the `--attr-source` parameter suggested in Phillip Wood's workflow.

Josef Wolf questions why `--attr-source=$(git rev-parse HEAD)` is necessary rather than being the default behavior, expressing surprise that filter settings might change during a rebase operation. He argues that filters should consistently apply to all commits across all branches, suggesting the current behavior may be counterintuitive.

The email highlights a key philosophical question about Git's filter application model that wasn't fully addressed in previous technical discussions. While earlier messages focused on practical workarounds and implementation constraints, Wolf raises a design principle issue about whether filter behavior should be commit-specific or universally consistent.

This appears to be probing deeper into the architectural decisions behind Git's attribute handling system, potentially setting up discussion about whether the current behavior is intentional (for flexibility) or an oversight that could be reconsidered. The question about use cases for changing filters during operation invites either justification of the status quo or acknowledgment of a design gap.