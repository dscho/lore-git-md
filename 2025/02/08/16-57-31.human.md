This is a test modernization patch from a GSoC contributor. Lucas Oshiro updates the t7603-merge-reduce-heads.sh test script to use the Git-specific `test_path_is_file` helper instead of the basic `test -f` shell command. The change improves test output when assertions fail, making it easier to diagnose test failures. The patch is straightforward, mechanically replacing 12 instances of `test -f` with the preferred helper function while maintaining all existing test logic. This appears to be a clean, focused improvement to test infrastructure with no behavior changes beyond better failure reporting.