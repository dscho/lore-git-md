Git log graph state management bug: Thread reports segmentation faults and memory errors in `git log` when mixing `--graph` and `--no-graph` flags, particularly with formatting options like `--name-status` or `--reverse`. Emily Klassen identified the issue on ARM64 macOS with Git 2.48.1 from `next` branch, where `--graph` sets internal state that isn't properly cleared by subsequent `--no-graph`. Đoàn Trần Công Danh reported the same flag combination causes an "Out of memory" error, suggesting memory management issues in the diff machinery. The problem appears related to `revs->diffopt.output_prefix_data` not being properly cleared when `--no-graph` is processed in `revision.c`. While the already-merged `jk/combine-diff-cleanup` series resolved Danh's memory error case, Emily confirms the segmentation fault persists on `next` and is preparing a patch to explicitly null this field during `--no-graph` handling. The thread now awaits her proposed fix to determine if it resolves both failure modes (segmentation fault and memory error) of what appears to be a related underlying issue in graph/diff state management. Key technical details involve the interaction between `revision.c`'s graph state handling and the diff machinery's output prefix data, with the bug manifesting specifically when `--graph` precedes `--no-graph` in command-line arguments.