This is the third patch in a series refactoring Git's reference transaction handling to support partial updates, primarily targeting the reftable backend. The patch removes redundant duplicate reference checks in the files backend, where the same validation was being performed twice - once during transaction preparation and again during finishing. The change makes the duplicate checking logic static to refs.c since it's now only used there, removing 16 lines of code while maintaining the same safety guarantees.

This is a straightforward cleanup that eliminates unnecessary work in the reference update path. The commit message clearly explains the change and its rationale (the check was redundant since refnames don't change between prepare and finish phases). The diff shows the removal of the duplicate check from files-backend.c while preserving the documentation of the checking function in refs.c.

As part of the larger series working toward partial reference updates, this patch helps simplify the transaction flow by removing an unnecessary verification step. The change appears well-contained and low-risk, focusing only on the files backend while the series' main goal is to benefit the reftable backend.