This is part 16/18 of Patrick Steinhardt's series to fully decouple the reftable library from Git's core infrastructure. The patch focuses on platform compatibility by reorganizing Windows-specific headers.

The change splits Git's `git-compat-util.h` header into two parts: a new `compat/posix.h` that contains only POSIX-emulation functionality (541 lines moved), while leaving Git-specific utilities in the original header. This separation is the final architectural step needed to make reftable truly standalone, as it allows external projects to use the POSIX compatibility layer without pulling in Git-specific code.

The patch is mechanically straightforward - it moves code with minimal changes (only adjusting includes and maintaining identical functionality). This completes the platform-specific header reorganization that was identified as the last major organizational improvement needed for clean separation of POSIX functionality from Git utilities. With all major technical concerns from previous reviews already addressed in v3-v4, this appears to be the final piece of the reftable decoupling effort.