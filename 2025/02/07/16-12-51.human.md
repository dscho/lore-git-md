This is a review response in the ongoing refactoring series to support partial reference transactions. Patrick Steinhardt provides feedback on patch 5/6, which implements the core infrastructure for partial updates.

Patrick raises several technical points about the implementation details. He suggests renaming `ref_transaction_add_rejection()` to better reflect its purpose, proposes adding bounds checking for update indices, and questions whether rejection errors need to be strings rather than a more structured enum. He also flags potential safety concerns about which types of failures should be allowed in partial transactions, noting this applies across all backends.

The review maintains the thread's technical depth while focusing on implementation quality. Patrick's comments suggest the patch is conceptually sound but could benefit from more precise error handling and clearer naming. His feedback indicates this is a key infrastructure change that warrants careful review of edge cases, particularly around what constitutes an "acceptable" failure in partial transactions.