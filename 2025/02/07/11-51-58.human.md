This is part of Patrick Steinhardt's v5 series to fully separate the reftable library from Git's core infrastructure. The patch removes the last remaining dependency on Git's `xmmap()` function in the reftable code.

The change replaces `xmmap()` with direct `mmap()` calls and proper error handling, addressing two key issues: the original function would die on failure (which isn't appropriate for a library), and it tied the code to Git's implementation. The patch maintains functionality while properly bubbling up errors through the reftable error system.

This is a well-contained change that follows the series' architectural goals. The diff shows careful attention to error handling, with proper cleanup on failure paths. As part of the broader effort that has already addressed most review feedback, this patch appears ready for inclusion.