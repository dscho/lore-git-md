This is a refactoring patch in Patrick Steinhardt's ongoing reftable decoupling series (v5 12/18). It replaces the Git-specific `sleep_millisec()` function with a direct call to `poll()` in the reftable stack implementation. The change is straightforward - when retrying operations, the code now uses POSIX's `poll()` for millisecond delays instead of Git's wrapper function.

The patch continues the series' systematic removal of Git-specific utilities from the reftable codebase. Here it's part of making the reftable library more self-contained by eliminating another dependency on Git's utility functions. The commit message notes that while `poll()` isn't the most direct sleep mechanism, it's the POSIX-standard way to achieve millisecond precision.

This appears to be a clean, mechanical change that follows the established pattern of the series. The diff is minimal (just one line changed) and the reasoning is sound - this is exactly the kind of utility replacement the series has been methodically working through. No new tests are needed since this is just an implementation detail change with identical behavior.