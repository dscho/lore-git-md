This is a feature patch adding a new flag to the refs migration functionality. The patch introduces a `--skip-reflog` option for `git refs migrate` to allow server-side repositories to skip reflog migration when changing reference backends.

The change builds on previous work (commit 246cebe320) that made reflog migration the default behavior. The new flag provides a performance optimization for server repositories where reflogs are typically not needed. The implementation adds the flag to the command-line interface, updates the underlying migration logic to respect the flag, and includes tests verifying the new behavior.

Key technical details:
- Adds `REPO_MIGRATE_REF_STORAGE_FORMAT_SKIP_REFLOG` flag bit
- Modifies `repo_migrate_ref_storage_format()` to conditionally skip reflog migration
- Updates test helper `test_migration()` to accept additional migration options
- Adds new test case verifying reflogs are properly skipped when requested

The patch appears well-structured with clear commit message, proper documentation updates, and thorough test coverage. The change is focused and addresses a specific performance optimization for server use cases while maintaining the default behavior for client repositories.