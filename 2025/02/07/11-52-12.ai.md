This is part of the reftable library decoupling effort, specifically addressing the final separation from Git's compatibility utilities. The patch completes the architectural work by removing the last direct dependency on `git-compat-util.h` from the reftable library, replacing it with the newly created `compat/posix.h` header that was split out in earlier patches.

The change is straightforward but significant - it switches the reftable system headers to use the standalone POSIX compatibility layer rather than Git's broader utility header. The only remaining Git-specific dependency is now explicitly handled through `system.h` and `system.c`, which external projects like libgit2 will need to provide their own implementations for. The diff shows the mechanical changes to the include directives in both the header and implementation files.

This represents the penultimate step in the series (17/18), with all major technical feedback from previous rounds already addressed. The patch maintains identical functionality while achieving the architectural goal of making reftable usable as a standalone library. The changes are minimal and focused, consisting only of include directive updates now that the underlying POSIX compatibility work was completed in earlier patches.