Thread summary:

Bug report and discussion concerning `git clean -Xdf` behavior in subdirectories of ignored parent directories. Original report by Jan Berges demonstrated that when `dir1` is ignored, running `git clean -Xdn` in `dir1/sub1` incorrectly attempts to clean sibling directory `dir1/sub2`, contrary to documented behavior stating cleaning should start from current directory. The thread now includes Junio Hamano's response providing philosophical context - he explains Git's working tree operations (like `status` and `diff`) typically treat the entire working tree as a single unit, with `git grep` being a notable exception. Hamano acknowledges the documentation/implementation discrepancy but frames it as a design choice rather than a bug, suggesting that making `git clean` behave like `git grep` would create inconsistency. He offers practical advice to use `git clean -n` for preview and pathspecs to limit scope.

Key technical details: The discussion centers on `git clean`'s `-X` flag behavior with ignored directory hierarchies, Git's working-tree-as-unit principle, and the exceptional case of `git grep`. The core discrepancy remains between documented behavior (current-directory-scoped cleaning) and actual behavior (working-tree-wide cleaning when parent is ignored). Junio's position suggests this may require either documentation updates to match implementation or a design decision about changing behavior. No patches or concrete next steps proposed yet, but the thread has shifted from pure bug report to design philosophy discussion. The working tree unity principle emerges as a key consideration for any resolution.