Refactoring patch removing redundant checks in the files backend's reference update handling. This is the first of six patches in a series introducing partial reference transaction support.

Karthik Nayak cleans up duplicate validation in `split_symref_update()` where reference names were being checked twice during transaction preparation. The patch simplifies the code by removing a redundant BUG check and eliminating unused `item->util` field assignments that were only serving as presence markers. These changes prepare the codebase for subsequent modifications to support partial reference updates.

The changes are straightforward and focused - removing about 17 lines of code while maintaining the same safety guarantees through the remaining validation. This appears to be a solid preparatory cleanup with no behavior changes, setting the stage for the more substantial refactoring and feature work to follow in the series.