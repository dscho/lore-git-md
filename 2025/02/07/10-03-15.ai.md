This is a technical discussion about compiler attribute usage in the reftable separation series. Toon Claes questions why Patrick Steinhardt introduced a new `REFTABLE_UNUSED` macro in patch 14/18 rather than using the existing `MAYBE_UNUSED` attribute, noting they appear functionally equivalent. The thread context shows this is part of the final stages of reftable's architectural separation from Git core.

Claes raises two specific points: first, that `MAYBE_UNUSED` (which uses `__attribute__((__unused__))`) seems compiler-independent and equivalent to the new implementation, and second, whether the choice of a separate-line macro was intentional for visibility when parameters later become used. The question comes after the series has already addressed major technical concerns through rigorous review, with only minor implementation detail remaining open.

The discussion touches on a subtle design decision in the reftable independence effort - whether to reuse Git's existing attribute infrastructure or maintain complete separation. The thread summary indicates all major architectural concerns are resolved, making this a fine-grained optimization question rather than a blocking issue. The exchange exemplifies the careful attention to detail in the final stages of this multi-patch architectural change.