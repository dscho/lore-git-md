This is a follow-up discussion in Patrick Steinhardt's Meson build system refactoring series, specifically addressing feedback on patch 9/13 regarding the `sane_tool_path` option changes. The patch converts the option from a single path to an array of paths for more robust handling, but Toon Claes raised concerns about backward compatibility when reconfiguring existing build directories.

Patrick responds that since Meson support is still marked as experimental, they don't need to explicitly document this as a breaking change. He suggests being more careful about backward compatibility once Meson matures (targeting Git v2.49 or v2.50), but considers iteration acceptable during this experimental phase. This maintains the series' approach of modernizing the build system while acknowledging practical migration impacts.

Key technical details:
- The change affects how `sane_tool_path` handles PATH overrides for build processes
- Conversion from string to array type improves correctness but breaks existing configurations
- Discussion now centers on whether experimental status justifies breaking changes
- Patrick's position is that iteration is expected during experimental phase
- Future versions (v2.49+) would need stricter compatibility guarantees

The thread continues to balance build system improvements against real-world usage impacts, with this exchange clarifying the project's stance on breaking changes during Meson's experimental period.