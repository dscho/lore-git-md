This is a documentation thread discussing the conversion of Git's AsciiDoc files from `.txt` to `.adoc` extensions, with specific focus on build system behavior for developer-facing documents.

The email clarifies that `SubmittingPatches` is indeed processed through the AsciiDoc toolchain despite lacking a `.txt` extension, as evidenced by its HTML output being generated and included in the installation. This confirms it should be included in the initial conversion phase. Meanwhile, `CodingGuidelines` appears not to be processed, raising questions about whether it should be converted at this stage.

Junio notes the inconsistency with `ReviewingGuidelines.txt` (which does have an extension and is processed) and suggests standardizing how developer documents are handled. The discussion has moved from general principles to concrete implementation details, with build system behavior now better understood through Knoble's investigation.

Key technical points for future reference:
- `SubmittingPatches` is processed via a special Makefile target creating both `.html` and `.txt` outputs
- `ReviewingGuidelines.txt` is confirmed as processed
- `CodingGuidelines` appears unprocessed but may need consideration for consistency
- Build system handles extensionless files through explicit rules
- Developer documents may need special treatment in the conversion

The thread shows consensus forming around Junio's phased approach, with this email providing important technical validation about which files actually go through the AsciiDoc pipeline. The next steps will involve finalizing the exact set of files for initial phase conversion.