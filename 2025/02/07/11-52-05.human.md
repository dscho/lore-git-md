This is a refactoring patch in Patrick Steinhardt's ongoing reftable decoupling series (v5 10/18). The change removes one of the last Git-specific macros (`ARRAY_SIZE()`) from the reftable reader code, replacing it with a direct sizeof calculation. This continues the series' systematic elimination of Git dependencies as the reftable library moves toward being fully self-contained.

The patch is straightforward - it modifies a single loop in `reftable_reader_print_blocks()` to use standard C array size calculation instead of the Git macro. This is a mechanical change that maintains identical behavior while reducing coupling to Git internals. The change follows the established pattern of the series, which has already addressed all major technical feedback in previous iterations.

As with other patches in this technically complete series, this appears to be a well-justified change that advances the goal of making reftable usable as a standalone library. The modification is minimal and focused, with no apparent impact on functionality or performance.