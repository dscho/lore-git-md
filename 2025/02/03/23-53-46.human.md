This is a brief follow-up in the bugfix series for `git repack --keep-unreachable`. Junio Hamano acknowledges Jeff King's observation that some of the test cases in Patrick Steinhardt's patch duplicate existing coverage in t7701-repack.sh, though they use the long-form `--keep-unreachable` flag rather than the short `-k` form. 

The exchange confirms that two of the three new test scenarios (packed unreachable objects and mixed packed/loose objects) were already covered in the test suite, though the edge case with no existing packs was genuinely new. The discussion doesn't raise any new concerns about the series, which remains technically sound and appears ready for merging. Junio's response suggests the test coverage overlap isn't problematic, just something worth noting for future contributors examining the test file.