This is a review response in the ongoing `git clone --revision` feature series, specifically addressing patch 6/6 which implements the core functionality. Patrick Steinhardt provides focused technical feedback on three specific code changes in the patch:

1. **Commit lookup safety**: Points out that `lookup_commit_or_die()` could replace the manual commit lookup + error handling in `update_head()`, which would simplify the error case handling for the new `--revision` option.

2. **Debug artifact**: Notes what appears to be a leftover debug comment (`//mapped_refs->name[0] = 0;`) that should likely be removed before final submission.

3. **Utility function placement**: Suggests that the new `die_for_incompatible_opt2()` helper in parse-options.h might belong in a separate preparatory commit, and recommends including an example callsite. He notes there are existing uses of similar patterns that could benefit from this helper.

The feedback is narrowly focused on code quality improvements rather than challenging the overall approach. These appear to be straightforward fixes that align with Git's code quality standards - the first two being simple cleanups, and the third being a small but meaningful refactoring opportunity. The review maintains the patch's momentum while ensuring final polish before integration.