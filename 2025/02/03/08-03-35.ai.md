This is part of Patrick Steinhardt's ongoing effort to decouple the reftable library from Git core infrastructure (v3 04/18). The patch removes another Git-specific dependency by replacing `COPY_ARRAY()` with an open-coded implementation in `reftable/record.c`.

The change does two main things: first, it replaces the Git utility `COPY_ARRAY()` with direct `memcpy()` calls, continuing the series' pattern of removing Git dependencies. Second, it adds a safety guard around the array copying logic to avoid potential NULL pointer issues when dealing with zero-sized arrays. The new implementation includes explicit overflow checking (`SIZE_MAX` comparison) before allocation, which wasn't present in the original version.

This change relates to Justin Tobler's earlier memory safety concerns about removing `COPY_ARRAY()`. The patch addresses this by maintaining the same memory safety guarantees while removing the Git-specific helper. The implementation carefully preserves the existing error handling behavior (returning `REFTABLE_OUT_OF_MEMORY_ERROR` on allocation failures) while adding the new overflow check.

The diff shows the change is localized to `reftable_obj_record_copy_from()` in record.c, affecting how object record offsets are copied between structures. The new version is more verbose but achieves the same functionality without Git dependencies. This is one of the final steps in removing Git-specific utilities from the reftable codebase.