This is a bugfix patch series addressing an edge case in `git repack --keep-unreachable`. The issue occurs when there are no preexisting packfiles in the repository, causing the command to fail. The series consists of two patches: first adding test coverage for this scenario, then fixing the actual bug in the repack logic.

The problem specifically affects the `--keep-unreachable` option, which is meant to preserve unreachable objects during repacking. The current implementation assumes there will always be existing packfiles to process, but crashes when that assumption fails. The fix modifies the repack logic to handle the no-pack case gracefully.

The series includes thorough test coverage with 73 lines of new test code in t7700-repack.sh, demonstrating both the failure case and the fixed behavior. The changes are minimal (5 lines modified in builtin/repack.c) but important for this edge case. The commit messages are clear and to-the-point, though they don't elaborate on real-world impact beyond stating the technical issue.

Key technical details for future reference: the bug manifests in builtin/repack.c's handling of the `--keep-unreachable` flag when no packs exist, and the fix involves adding a conditional check. The test cases verify behavior with and without existing packs, and with both reachable and unreachable objects. This appears to be a straightforward fix for a clear edge case bug.