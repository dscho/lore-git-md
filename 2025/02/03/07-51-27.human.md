This is a review comment on patch 4/6 in the `--revision` clone series, focusing on the preparatory refactoring of tag handling. Patrick Steinhardt notes that the patch moves tag refspec addition earlier in the clone process, which simplifies later code. He points out two issues:

1. The logic to add tags when `option_branch` is set (not just `option_tags`) is surprising, since the branch option could point to either a branch or tag. While correct, this behavior isn't immediately obvious from the code.

2. The patch would benefit from a comment explaining why adding tags for both `option_tags` and `option_branch` cases is the right approach, to help future maintainers understand the rationale.

The feedback is constructive and focused on code clarity rather than functionality - the underlying change is sound, but could be better documented. This continues the pattern of thorough review for this series, where each preparatory refactoring is being vetted before the final `--revision` feature lands.