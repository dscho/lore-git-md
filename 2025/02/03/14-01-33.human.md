This is a continuation of the discussion about repository reinitialization with different formats, specifically addressing how environment variables should interact with existing repository configurations.

Junio Hamano responds to Patrick Steinhardt's position that environment variables like `GIT_DEFAULT_REF_FORMAT` should behave as pure defaults (similar to config options) rather than forcing changes to existing repositories. Junio pushes back on this view, arguing that environment variables often serve as temporary overrides rather than just defaults in Git's existing behavior. He cites common patterns like using `GIT_EDITOR` or `GIT_AUTHOR_*` variables as one-time overrides, suggesting environment variables should be treated more like command-line options (which do error out when incompatible with existing configurations).

The exchange highlights a philosophical difference in how environment variables should function in Git. While Patrick advocates for silent safety (preserving existing configs without warning), Junio favors making users explicitly aware when their environment variables are being ignored, seeing this as more consistent with Git's existing behavior patterns. This remains the key unresolved point in an otherwise technically sound patch series.