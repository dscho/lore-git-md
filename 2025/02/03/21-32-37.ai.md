This is a CI/build system patch that concludes a minor thread about a regression in the Coverity job setup. The issue stemmed from Patrick Steinhardt's earlier CI modernization series (ps/ci-misc-updates) which changed how distro-specific dependencies are handled, switching from `$distro` to `CI_JOB_IMAGE` variables. Jeff King noticed and fixed a missed instance in the Coverity job configuration that was still using the old syntax, causing curl installation to fail. The one-line change in `.github/workflows/coverity.yml` ensures consistent dependency installation across all CI jobs. Patrick acknowledged the oversight ("Doh, indeed") and Junio has now queued the fix. This wraps up the thread with all instances of the old `distro:` syntax now properly updated, maintaining the containerization improvements from the original series. The fix is straightforward and uncontroversial, addressing a clear regression from otherwise successful CI modernization work.