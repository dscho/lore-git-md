Implementation refinement for packed-refs validation series (v2 7/8), focusing on type consistency in the sortedness verification code. Patrick Steinhardt provides specific feedback on shejialuo's patch that adds sortedness checking to packed-refs validation.

The key technical points address type consistency in the fsck_packed_ref_entry implementation:
1. Line numbers should use `size_t` rather than `int` in the fsck_packed_ref_entry struct
2. Array iteration counters (both in free_fsck_packed_ref_entries and packed_fsck_ref_sorted) should use `size_t` for consistency
3. The feedback maintains the series' focus on robust validation while improving type safety

This continues the thread's pattern of incremental refinement, with Patrick providing focused technical corrections to implementation details. The changes don't affect the core validation logic but ensure proper type usage throughout the codebase. The feedback aligns with Junio's earlier suggestion to use `size_t` for line numbers, now extended to array handling as well.

The patch remains part of the larger effort to validate packed-refs files, with this particular iteration focusing on verifying sortedness when explicitly declared in the header. Patrick's comments represent another round of quality improvements to the implementation details, following his earlier architectural feedback on the series.