This is a status update on two ongoing patch series from Derrick Stolee, following up on Junio's "What's Cooking" report.

For the `ds/backfill` series (6 patches), Stolee notes it has been rebased onto master now that the prerequisite `ds/path-walk-1` is merged, with a v3 reroll coming soon. This series introduces a new `git backfill` command to optimize blob downloads in sparse checkouts by doing bulk prefetching rather than one-at-a-time lazy loading. The implementation includes `--sparse` and `--min-batch-size` options.

Regarding `ds/name-hash-tweaks` (7 patches), Stolee confirms the v4 version appears stable with Taylor Blau's review and suggests it's ready to graduate to 'next'. This series adds configurable name-hash algorithms to `pack-objects` and `repack` via new `--name-hash-version` options, aiming to improve delta compression by considering deeper history. Stolee notes this needs to land before he can rebase his separate `--path-walk` work due to textual conflicts.

The email serves as both a progress report and coordination signal - indicating which branches are ready for next steps while acknowledging deeper design discussions still needed for the path-walk functionality. Both series represent performance-oriented work touching core object storage and packing mechanisms.