This is a build system patch that concludes Patrick Steinhardt's 18-part series decoupling the reftable library from Git's core infrastructure. The change excludes reftable sources from Coccinelle semantic patching, as the library no longer uses Git-specific helper functions that Coccinelle rules typically target.

The patch modifies the Makefile to filter out reftable/ paths from COCCI_SOURCES, preventing false positives that could occur when Coccinelle attempts to apply Git-specific transformations to reftable code. This change follows the series' architectural goal of making reftable a standalone library, now that it has replaced all Git dependencies with its own implementations (as detailed in the thread summary).

The modification is straightforward - a single-line change to the COCCI_SOURCES definition that adds reftable/ to the existing filter-out pattern that already excluded third-party sources. This maintains build system consistency while acknowledging reftable's architectural independence. The patch appears well-justified given the series' context and represents a logical final step after the library's complete decoupling from git-compat-util.h and other core dependencies.