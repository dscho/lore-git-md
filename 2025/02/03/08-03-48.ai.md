This is part of Patrick Steinhardt's ongoing series to decouple the reftable library from Git core infrastructure, specifically focusing on POSIX header separation. The patch completes the architectural separation by removing the final dependency on git-compat-util.h in the reftable codebase.

The key change replaces git-compat-util.h with a new compat/posix.h header that provides only the POSIX-like functionality needed by reftable, without pulling in Git-specific helpers. The system.h and system.c files now remain as the only Git-specific components that external projects would need to implement themselves. The diff shows the straightforward header swap in system.h and adds git-compat-util.h back in system.c where Git-specific functionality is still needed.

This represents the penultimate step in the reftable decoupling effort, following previous work to eliminate Git-specific macros, utilities, and platform abstractions. The changes maintain all existing behavior while structurally separating the code. The patch follows the established pattern of the series by making surgical changes focused on dependency reduction rather than functional modifications.

The thread context shows this work has undergone extensive review, with all major technical concerns addressed except for final verification of I/O implementations. This particular change appears uncontroversial as it completes a planned architectural separation that was previewed and discussed in earlier patches.