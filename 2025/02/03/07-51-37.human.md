This is a review response in the ongoing thread about adding a `--revision` option to `git clone`. Patrick Steinhardt provides several technical suggestions for the final implementation patch (6/6 in the series).

The review focuses on three specific code changes:
1. Suggests using `lookup_commit_or_die()` instead of manual commit lookup and error handling in `update_head()`
2. Points out what appears to be a leftover debug comment (`//mapped_refs->name[0] = 0;`)
3. Recommends splitting out the new `die_for_incompatible_opt2()` helper function into a separate commit with example usage, noting there are existing call sites that could benefit from it

These are relatively minor cleanups to an otherwise well-developed patch series that has undergone multiple rounds of review. The suggestions align with Git's code quality standards - preferring existing utility functions, removing debug artifacts, and properly factoring helper functions. The feedback doesn't controversial and seems straightforward to address as part of finalizing the series.