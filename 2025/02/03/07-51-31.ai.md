This is a review response in the ongoing `git clone --revision` series, focusing on the refactoring work to introduce `struct clone_opts`. Patrick Steinhardt provides several small but important code review comments on patch 5/6.

The review notes several formatting and style issues in the patch:
1. An `else if` that should be on the same line as the closing brace
2. Excessive empty lines in the `cmd_clone()` function without clear purpose
3. Suggests adding a comment to document the newly exposed `tail_link_ref()` function

Patrick also validates the logic change around `opts.wants_head`, agreeing with the approach of not fetching remote HEAD when a specific branch is requested with `--single-branch`. This is part of the series' broader effort to clean up and consolidate clone's option handling into a structured form.

The feedback is relatively minor but maintains the high standard for code quality in the project. These are the kinds of polish items that typically get addressed in a v5 submission, especially since they don't affect the core functionality being introduced. The review doesn't challenge the overall approach but helps ensure the code meets Git's documentation and style standards.