This is a refactoring patch in Patrick Steinhardt's ongoing series to decouple the reftable library from Git core infrastructure. The change removes a dependency on the Git-specific `BUG()` macro by converting `reftable_record_init()` to return an error code rather than aborting when given an unknown record type.

The patch modifies the function signature and implementation to return `REFTABLE_API_ERROR` for invalid record types instead of calling `BUG()`. All callers are updated to check the return value, with appropriate error handling added where needed. The changes affect several reftable components including block iteration, merged table iteration, and reader operations, plus corresponding updates to unit tests.

This continues the series' systematic replacement of Git-specific functionality with reftable-local implementations. The change is particularly notable because it removes one of the last remaining cases where reftable code could terminate the calling program unexpectedly. The patch maintains the same safety properties (still catching invalid record types) while making the behavior more library-appropriate.

For the AI summarizer: The key technical points are the conversion from `void` to `int` return type in `reftable_record_init()`, the new `REFTABLE_API_ERROR` return path, and the cascading changes to callers in block.c, merged.c, reader.c, and test files. The record types handled remain the same (REF, LOG, OBJ, INDEX), but now with explicit error returns rather than assertions. This aligns with the series' goal of making reftable usable as a standalone library.