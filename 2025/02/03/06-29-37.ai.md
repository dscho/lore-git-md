This is a test patch in the atomic push exit code propagation series, adding new test cases to verify proper error reporting when `git-receive-pack` fails during atomic pushes.

The patch introduces two new scenarios in t5543-atomic-push.sh that were previously untested edge cases. First, it verifies that atomic pushes properly report when `git-receive-pack` exits with a failure code even when ref updates succeed. Second, it checks that this error reporting works correctly with the `--porcelain` output format. Both tests use a wrapper script to force `git-receive-pack` to exit with status 1 after successful ref updates.

The test cases are currently marked as `test_expect_failure`, indicating they're documenting known issues that the series aims to fix. The test infrastructure follows modern patterns seen throughout the series, using heredocs for expected output and proper shell quoting. The test setup builds on the existing atomic push test framework in t5543-atomic-push.sh.

Key technical details:
- Tests both regular and porcelain output modes
- Verifies error message content matches expectations
- Uses `write_script` helper for portable wrapper creation
- Maintains consistent test isolation patterns seen in earlier series patches
- Focuses on the specific edge case where ref updates succeed but receive-pack fails

This patch completes the test coverage for the atomic push error propagation behavior, complementing the protocol and transport layer changes in earlier parts of the series. The tests will help ensure the fixes work as intended across different output formats and edge cases.