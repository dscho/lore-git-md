This is a minor review comment on the test infrastructure from the `--keep-unreachable` bugfix series. Junio Hamano suggests two potential improvements to the test helper functions introduced in the first patch of the series.

For `expect_object_count()`, he questions whether the name should include "loose" since it specifically counts loose objects (excluding packed ones via the `-prune`). The current implementation uses `find` to traverse `.git/objects` while skipping the `pack` and `info` directories.

For `expect_object_in_idx()`, he proposes a potentially more efficient alternative implementation using `git show-index` rather than `git verify-pack -v`. He notes the current name suggests using `.idx` files (which is correct) but the function actually accepts either `.idx` or `.pack`. The suggested alternative would parse the index directly rather than verifying the full pack contents.

These are minor optimization and naming suggestions for test helpers that already work correctly. The core bugfix and test coverage remain unaffected. The thread shows the maintainer engaging with the test implementation details while the main technical content of the series appears settled.