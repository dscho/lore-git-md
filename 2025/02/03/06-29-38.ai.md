Final patch in the atomic push exit code propagation series, addressing connection cleanup. This completes the fix for cases where git-receive-pack errors during atomic pushes weren't properly reported back to the client. The key change ensures graceful connection closure by sending a flush packet when atomic push fails, allowing receive-pack to exit cleanly rather than hanging. This reverts a previous workaround in transport.c that ignored finish_connect() errors during atomic pushes, now that the root cause is properly fixed in send-pack.c.

The patch converts two test_expect_failure cases to test_expect_success, confirming the fix works for both regular and porcelain output modes when receive-pack fails after successful ref updates. The changes are minimal but critical - just adding the flush packet in send-pack.c and simplifying transport.c's error handling now that the underlying issue is resolved.

This builds on the series' earlier protocol changes (introducing ERROR_SEND_PACK_BAD_REF_STATUS) and test infrastructure improvements. The solution maintains backward compatibility while properly propagating errors through the entire push pipeline, including the edge case where receive-pack fails after ref updates succeed. The test suite now provides comprehensive coverage of atomic push error scenarios.