This is the final patch in the v3 series introducing the `git backfill` command, focusing on sparse-checkout integration. The patch makes `--sparse` mode automatic when sparse-checkout is enabled, while still allowing explicit `--no-sparse` override. This addresses a potential usability issue where users with sparse-checkout might unintentionally download all historical objects.

Key technical details:
- Adds logic in `builtin/backfill.c` to check `core_apply_sparse_checkout` when sparse mode isn't explicitly set
- Updates documentation to clarify the default behavior
- Includes two new test cases: one verifying error when `--sparse` is used without sparse-checkout, and another confirming `--no-sparse` can override the default
- Uses `USE_THE_REPOSITORY_VARIABLE` macro to access repository state
- Maintains the existing sparse-checkout integration from previous patches while making it the default behavior

The change is well-motivated by user experience considerations and maintains all previously established functionality while adding sensible defaults. The test coverage is thorough, verifying both the new default behavior and edge cases. This appears to be the final refinement of the series' sparse-checkout integration, with all prior review feedback addressed.