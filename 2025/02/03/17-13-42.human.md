This is a minor review comment on the test infrastructure from the `git repack --keep-unreachable` bugfix series. Junio Hamano suggests two potential improvements to the test helper functions introduced in the first patch of the series.

For the `expect_object_count` function that counts loose objects, he questions whether the name should include "loose" to be more precise about what it counts. For the `expect_object_in_idx` function that verifies object presence in a pack, he proposes an alternative implementation using `git show-index` that might be more efficient than the current `git verify-pack` approach.

These are optimization suggestions rather than blocking issues - the test helpers work as written, and the original patch's test coverage remains valid. The feedback is constructive but doesn't affect the core bugfix or its test validity. The thread shows the maintainer engaging with the technical details of the test implementation while the core fix itself appears uncontroversial.