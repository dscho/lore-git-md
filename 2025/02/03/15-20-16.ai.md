This is a follow-up discussion about test infrastructure in the context of the already-merged reftable series. The email addresses a specific technical concern about undefined behavior in unit tests, but does not propose any changes to the reftable implementation itself.

Jeff King points out that the test case `t_reftable_invalid_limit_updates()` contains potential undefined behavior if `reftable_new_stack()` fails, since the test continues execution despite the error and may dereference NULL. He demonstrates this with a segmentation fault example and discusses the tradeoffs between fixing this (which would require more complex cleanup code) versus living with it until the planned migration to the `clar` test framework.

Karthik Nayak acknowledges the issue but suggests keeping the current pattern for consistency with other tests in the file, noting that the problem will be resolved when moving to `clar` as previously discussed in the thread. The email includes a demonstration patch showing how to trigger the failure case, but this appears to be for illustration rather than a proposed change.

This discussion is now effectively closed, as it reiterates points already resolved in the broader test framework debate (where consensus was reached to accept the current behavior until the `clar` migration). The reftable implementation itself remains unchanged and merged.