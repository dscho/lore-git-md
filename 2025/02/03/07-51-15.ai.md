This is a refactoring patch in the ongoing `git clone --revision` series, specifically addressing the `wanted_peer_refs()` function. The patch simplifies this complex function that maps server refs to local clone refs, reducing it from 39 to 24 lines while maintaining behavior.

Patrick Steinhardt provides review feedback noting two issues: (1) the commit message could better guide readers through the significant changes, and (2) the loop counter should use `size_t` rather than `int` to match `refspec->nr`'s type. The feedback is constructive but doesn't challenge the overall refactoring approach.

This patch is part of the preparatory work for the new `--revision` feature, having already gone through several iterations. The changes here are focused on code quality and maintainability rather than functionality, continuing the series' pattern of significant refactoring before introducing new features. The review suggests minor but important improvements to type safety and documentation that align with Git's coding standards.