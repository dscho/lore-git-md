This is a review response in the ongoing `git clone --revision` series, focusing on patch 5/6 which introduces a `struct clone_opts` to better organize clone options. Patrick Steinhardt provides several small but thoughtful code review suggestions for Toon Claes's patch.

The main technical point is about the new `wants_head` field in `struct clone_opts`, which tracks whether to fetch remote HEAD. Patrick agrees with the logic that skips fetching HEAD when both `--single-branch` and `--branch` are specified, since the branch is already explicitly given.

The review also includes several style and documentation nits:
- Formatting suggestion to put `else if` on the same line as the closing brace
- Note about excessive empty lines in one section
- Request to add a comment explaining the purpose of the newly exposed `tail_link_ref()` function

These are all minor polish items that don't challenge the core approach of the patch. The review maintains the constructive tone seen throughout this series, with Patrick building on his previous feedback about code organization and clarity. The patch appears to be in good shape overall, just needing these small tweaks before finalization.