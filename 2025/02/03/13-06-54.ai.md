This is the first patch in a two-part bugfix series addressing `git repack --keep-unreachable` behavior when no preexisting packfiles exist. The patch adds comprehensive test coverage (73 new lines) for three scenarios involving unreachable objects: when all objects are packed, when some are packed and some loose, and when no packfiles exist (which currently fails). The test infrastructure includes helper functions to verify object counts and check for specific objects in packfiles.

The tests demonstrate the current behavior where `--keep-unreachable` correctly preserves unreachable packed objects when other packs exist, but fails when there are no existing packs (marked as test_expect_failure). The test cases carefully set up repositories with controlled object states, using `test_commit` and `git repack` to create specific conditions before exercising the `--keep-unreachable` option.

This test-only patch lays the groundwork for the subsequent fix by clearly documenting the expected behavior and current failure case. The test design shows attention to edge cases and includes cleanup via `test_when_finished` blocks. The commit message is straightforward, noting the failing test will be addressed in the next commit.