Test modernization patch completing a 4-part series converting oid-related tests to the Clar framework. This final patch converts the oidtree test suite, following the same pattern established in previous conversions of oid-array and oidmap tests.

The patch:
- Renames t-oidtree.c → u-oidtree.c
- Restructures tests into discrete functions (test_oidtree__contains and test_oidtree__each)
- Uses Clar assertions (cl_assert, cl_failf) consistently
- Maintains the shared lib-oid.c helper functions (cl_parse_any_oid)
- Adds initialization/cleanup handlers (test_oidtree__initialize/cleanup)
- Updates build system integration (Makefile and meson.build)

Notable technical details:
- Uses both cl_assert() and cl_failf() assertions, continuing the mixed style from the oidmap patch
- Preserves all test cases while simplifying control flow
- Maintains test coverage of oidtree operations (insertion, contains checks, iteration)
- Shares the global oidtree state across tests via static struct ot
- Builds on the series' established patterns:
  - File renaming convention
  - Test function naming
  - Shared helper functions
  - Build system integration

This completes the series' technical work, with the only remaining open question being the assertion style debate (cl_assert vs cl_failf) which appears to be resolving toward a mixed approach. The patch demonstrates good technical quality and consistency with the rest of the series.