This is a minor code cleanup suggestion in the ongoing `git clone --revision` series. Patrick Steinhardt points out that the explicit `if` condition in Toon Claes's patch is redundant when using the new `die_for_incompatible_opt2()` helper function. The helper already handles the conditional logic internally, so the surrounding `if` can be removed for cleaner code.

The observation comes as part of the final review phase for this series, where most of the substantive work has already been addressed across five versions. This particular note is about tightening up the implementation of a helper function introduced earlier in the series to standardize option validation. It doesn't affect the core `--revision` functionality but helps maintain code quality.

The series as a whole is nearly ready for integration, with this being one of the last minor polish items before the final safety verification on another part of the code. Patrick's comment shows the careful attention to detail in the review process, even for small implementation details.