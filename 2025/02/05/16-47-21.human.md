Refactoring patch in the `--revision` clone option effort. This is the sixth patch in a seven-part series that's already gone through extensive review and is awaiting merge.

The patch introduces `die_for_incompatible_opt2()`, a helper function for handling mutually exclusive command-line options, completing the set of similar functions that already existed for 3 and 4 options. The implementation is straightforward - it just calls the existing 4-option version with empty slots for the extra parameters. The change is demonstrated by converting the `--onto`/`--advance` option check in `builtin/replay.c` to use the new helper.

This appears to be a clean, mechanical change that improves consistency in the codebase's option parsing. The patch is small (13 lines net change) and focused, with no behavioral changes beyond the error message formatting. As part of a mature patch series that's already addressed all technical feedback, this looks like final polish before merging.