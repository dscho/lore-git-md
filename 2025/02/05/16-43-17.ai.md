This is a final technical clarification in the approved `--revision` clone feature series. Toon Claes responds to Patrick Steinhardt's query about a safety improvement in the `update_head()` function, explaining why the change from `lookup_commit_reference()` to `lookup_commit_or_die()` is necessary but can't be split into a separate commit.

The change hardens the clone operation against invalid inputs when using `--revision` with a tree hash, though it's tightly coupled with the new test case that exercises this path. The discussion confirms this is not a standalone bugfix but rather an integral part of ensuring robust error handling for the new feature. This final exchange shows the maintainers performing last-mile verification of edge case handling before the series gets merged.