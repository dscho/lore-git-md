Here's a human-readable summary of the thread's current state:

This thread addresses a subtle conflict in Git's pull configuration precedence, where branch-specific rebase settings (`branch.<name>.rebase=true`) were being incorrectly overridden by the global `pull.ff=only` setting. The original patch proposed fixing this by separating the rebase configuration checks in the pull code, allowing branch-specific settings to properly override the fast-forward requirement.

Junio Hamano has now weighed in with a contrasting view, arguing that `pull.ff=only` should be treated as a strict safeguard that shouldn't be bypassed by rebase settings. He sees this as protecting users who explicitly set `pull.ff=only` to prevent potentially problematic merges. This introduces a philosophical question about configuration hierarchy - whether safety constraints should override workflow preferences.

The technical implementation itself isn't in dispute, but the underlying behavior is now being reconsidered. The discussion has shifted from a bugfix to a deeper conversation about user intent when combining these settings. This may require either adjusting the patch's behavior or more clearly documenting the interaction between these configuration options.