This is a refactoring patch that introduces a new helper function for option parsing, part of the `--revision` clone option series that's nearing completion.

The patch adds `die_for_incompatible_opt2()` to complement existing functions that handle 3-4 incompatible options. The new helper provides a consistent way to error out when two options conflict, replacing an ad-hoc check in `builtin/replay.c` for the incompatible `--onto` and `--advance` flags. The implementation reuses the existing `die_for_incompatible_opt4()` infrastructure while providing a simpler interface for the two-option case.

The change appears straightforward and well-contained, with minimal impact - just 3 lines changed in `replay.c` to use the new helper, plus the 9-line function definition in `parse-options.h`. This is clearly a final polish item in the series, improving code consistency rather than adding new functionality. The patch maintains all existing behavior while making the option conflict handling more uniform across the codebase.