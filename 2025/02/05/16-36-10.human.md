This is a follow-up discussion in an ongoing thread about configuration precedence between `pull.ff=only` and branch-specific rebase settings. D. Ben Knoble responds to Junio C Hamano's concerns about whether `pull.ff=only` should strictly enforce fast-forward behavior regardless of rebase settings.

The email provides a detailed analysis of the documentation, showing that while the current implementation aligns with Junio's interpretation, the docs don't explicitly state that `pull.ff=only` should override branch-specific rebase settings. Ben argues that `branch.<name>.rebase=true` represents an explicit user opt-in that should take precedence, allowing rebases even when `pull.ff=only` is set globally.

Ben suggests this combination could be useful for workflows where users want fast-forward-only behavior for some branches (like main) but automatic rebasing for others (like topic branches). They acknowledge Junio's safety concerns but propose this as a reasonable middle ground between strict safety and workflow flexibility.

The discussion highlights a philosophical difference in interpreting configuration precedence, with no clear resolution yet. Ben remains open to alternative approaches but maintains that their proposed behavior would be more intuitive for certain workflows. The email keeps the technical discussion grounded in actual documentation while exploring the practical implications of each interpretation.