This is a final review comment on the `--revision` feature for `git clone`, specifically addressing a potential safety issue in the implementation. Patrick Steinhardt questions whether a change in patch 7/7 fixes a possible segfault when handling non-branch references (like tags) during clone operations. 

The change in question replaces `lookup_commit_reference()` with `lookup_commit_or_die()`, which would crash rather than silently proceed with a NULL commit. Patrick suggests that if this is fixing a real issue, it should be split into a separate commit with a test case. If not, the change may be unnecessary.

This comes as the final piece of feedback on a well-reviewed series that has already addressed all major technical concerns. The core `--revision` functionality itself appears solid, with extensive test coverage and documentation refinements. The thread shows clear technical agreement on the overall approach, with this being one of the last minor points to resolve before potential integration.