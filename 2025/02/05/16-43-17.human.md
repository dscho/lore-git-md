This is a final refinement to the `--revision` clone option series, addressing a potential segfault edge case. Toon Claes responds to Patrick Steinhardt's query about a safety improvement in the `update_head()` function, explaining that switching from `lookup_commit_reference()` to `lookup_commit_or_die()` is necessary to handle a specific test case where `--revision` points to a tree hash rather than a commit. 

The change prevents a possible NULL dereference when dealing with non-commit objects, though Toon notes this can't be split into a separate commit since it's directly tied to the new test case. This appears to be the last remaining discussion point before the series (already approved by Junio) gets merged, showing the maintainers' attention to safety even in final refinements.