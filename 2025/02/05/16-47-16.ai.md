Refactoring patch in the `--revision` clone feature series. This is the first of seven patches that prepares `builtin/clone.c` for the new functionality by eliminating global variables.

The patch moves the `struct option` array and all related parsing variables from file scope into `cmd_clone()`, reducing global state. Key changes include:
- Converts 195 lines of global declarations into local variables
- Maintains all existing functionality while improving encapsulation
- Initializes variables at point of declaration with sensible defaults
- Adds proper cleanup for string lists before function exit
- Changes `option_config` from NODUP to DUP to match usage patterns

This preparatory work was essential for the series' later introduction of the `--revision` option, as noted in the thread summary. The change follows Git's incremental refactoring approach - improving code structure before adding features. The patch shows careful attention to detail with proper initialization and cleanup of all moved variables.

The thread context shows this refactoring has already been through multiple review cycles and is now in its final form, with all feedback incorporated. It serves as the foundation for the subsequent patches that implement the actual `--revision` functionality.