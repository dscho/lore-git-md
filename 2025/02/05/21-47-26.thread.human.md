Here's a human-readable summary of the thread's current state:

This thread explores the complex interaction between Git's clean/smudge filters and history rewriting operations, particularly focusing on why `git rebase --root --strategy renormalize` fails in some scenarios. What began as a user's specific problem with XML file processing has evolved into a deep technical discussion about Git's merge machinery and filtering architecture.

The core issue stems from how Git applies filters during history rewriting. While `-X renormalize` was thought to handle all filtering automatically, contributors have discovered it only affects files requiring three-way merging. Elijah Newren's latest clarification explains that files resolvable by simple hash comparison bypass filtering entirely, which explains why conflicts still occur. This behavior, while correct, wasn't clearly documented.

Several solutions have emerged:
1. A combined approach using both `-X renormalize` and `git add --renormalize`
2. Phillip Wood's technique of setting `--attr-source=$(git rev-parse HEAD)` to properly handle pre-filter commits
3. Brian Carlson's recommendation of `git filter-repo` for more robust cases

The discussion has revealed fundamental limitations in Git's architecture - particularly that merge renormalization only affects new blobs during three-way merges, not existing content. While practical solutions exist for linear history cases (validated through test repositories), broader questions remain about whether Git's filter application model should be commit-specific or universally consistent.

Key participants include Elijah Newren (merge machinery expert), Phillip Wood (who provided working examples), Brian Carlson (who clarified proper syntax), and Junio Hamano (who engaged on potential plumbing improvements). The thread has reached practical resolution for the original use case while opening deeper questions about Git's design principles around filtering.

Documentation improvements are planned to better explain these behaviors, particularly around the "virtual check-out/check-in" description that proved misleading. The exchange highlights how Git's performance optimizations (like avoiding unnecessary file content examination) can create surprising edge cases when combined with filtering operations.