Here's the updated human-readable thread summary:

This thread explores the complex interaction between Git's clean/smudge filters and history rewriting operations, sparked by a user's experience where filters worked for new commits but failed during `git rebase --root --strategy renormalize`. The discussion has evolved through multiple technical layers with contributions from core Git developers and experienced users.

The technical foundation was established by Brian M. Carlson, who corrected the invalid `--strategy renormalize` syntax and documented the correct workflow using `git add --renormalize` during rebase. He strongly recommended `git filter-repo` as the most robust solution. Josef Wolf then expanded on practical edge cases, particularly around cherry-pick operations which bypass clean filters, and shared a personal perspective on Git's reliability advantages despite these quirks.

Elijah Newren provided crucial architectural insights, explaining that renormalization only applies during three-way merges due to performance constraints, and that cherry-pick's behavior is a fundamental limitation rather than a bug. His latest contribution further clarifies that while combining `-X renormalize` with `git add --renormalize` can handle linear history cases, merge's renormalize option doesn't rewrite existing blobs - a key limitation users should understand.

Multiple alternative workflows have been proposed, including:
- Chris Torek's suggestion to avoid cherry-pick entirely in favor of direct file restoration
- Phillip Wood's refined command sequence that avoids empty commits
- Torsten Bögershausen's experimental merge-based approach

Junio Hamano, the Git maintainer, provided historical context about why these gaps exist (many commands predate widespread filter usage) and suggested that community prioritization could drive improvements. He outlined the technical requirements for better renormalization support but noted it would need motivated developers to implement.

The thread now stands as a comprehensive resource covering:
1. Correct workflows for filter usage during history rewriting
2. Architectural constraints and their performance rationale
3. Multiple practical workarounds for different scenarios
4. The historical evolution that led to current behavior
5. Clear documentation of the gap between user expectations and technical reality

While no silver bullet solution exists, participants have mapped out the problem space thoroughly, providing users with both understanding of the limitations and practical paths forward. The latest clarification from Elijah Newren reinforces that while current solutions can work for linear histories, comprehensive normalization remains architecturally challenging due to Git's performance-oriented design.