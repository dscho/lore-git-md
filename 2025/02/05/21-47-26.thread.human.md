This thread has thoroughly explored the complex interaction between Git's clean/smudge filters and history rewriting operations like rebase` and `cherry-pick`. What began as a user's specific issue with `git rebase --root --strategy renormalize` has grown into a comprehensive examination of Git's filtering behavior, with contributions from several domain experts.

The core issue stems from Git's efficiency optimizations - during operations like cherry-pick, Git intentionally skips the clean filter to avoid unnecessary work, which can lead to unexpected behavior when rewriting history. Brian M. Carlson provided the foundational technical understanding, correcting the invalid syntax and documenting the proper renormalization workflow using `git add --renormalize`. Josef Wolf identified several edge cases, particularly around cherry-pick operations which bypass filters in ways that surprise users. Torsten Bögershausen added valuable historical context, explaining that renormalization was a later addition to Git's feature set.

Multiple workflow solutions have emerged: Chris Torek proposed a simpler alternative avoiding cherry-pick entirely, while Phillip Wood refined the command sequence to handle edge cases more cleanly. Elijah Newren provided architectural perspective, explaining fundamental limitations in Git's design. Most recently, Chris Torek completed the technical picture by pointing to documentation that explains why explicit renormalization is needed to bypass Git's efficiency optimizations.

The discussion has reached a point where the original question is thoroughly answered, though it has surfaced several areas where Git's behavior could be improved. Junio Hamano has engaged on potential for plumbing improvements, though any changes would require significant architectural work. For now, users have multiple documented approaches to work around the limitations, with clear understanding of why these workarounds are necessary.