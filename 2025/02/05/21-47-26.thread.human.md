Here's the updated human-readable thread summary:

This thread began as a user seeking help with Git's clean/smudge filters during history rewriting operations, particularly when using `git rebase --root`. It has grown into a comprehensive technical discussion with contributions from several Git experts. The core issue involves how Git applies content filters during operations that rewrite history.

Brian M. Carlson provided authoritative guidance early on, correcting misconceptions about the `--strategy renormalize` syntax and documenting the proper workflow using `git add --renormalize` during rebase. He also identified `git filter-repo` as a more robust alternative for many cases. Josef Wolf then surfaced practical edge cases, particularly around automation challenges and commits that don't modify filtered files.

Elijah Newren, the filter-repo maintainer, added crucial architectural context. He explained why filter-repo intentionally avoids working tree operations and what plumbing changes would be needed for better renormalization support. Phillip Wood contributed workflow refinements, suggesting a more robust command sequence that avoids empty commits when no files need renormalization.

The latest message from Josef reveals a persistent misunderstanding about when Git applies clean filters - expecting them to run automatically during cherry-pick operations, when in reality they only run during specific operations like checkout and add. This highlights a documentation gap about Git's filtering behavior that wasn't fully covered in earlier technical explanations.

While the thread has established clear technical solutions for proper renormalization workflows, this latest exchange shows there's still room to improve user education around Git's filtering model. The core technical questions have been answered, but the discussion continues to surface areas where Git's behavior might surprise users.