Here's a human-readable summary of the thread's current state:

This thread began with a technical question about why Git's clean/smudge filters worked for new commits but failed during a root rebase with renormalization. It has evolved into a deep exploration of Git's attribute handling during history rewriting, with contributions from several domain experts.

The core technical solution has been established: combining `-X renormalize` with `git add --renormalize` and using `--attr-source=$(git rev-parse HEAD)` allows linear history rewriting with consistent filtering. Elijah Newren provided key architectural insights, clarifying that renormalization only applies to files needing three-way merges and explaining the environment variable propagation mechanism. Phillip Wood validated this approach with practical testing.

Josef Wolf's latest contribution shifts the discussion to real-world constraints, describing a case with proprietary XML files where historical `.gitattributes` versions are unusable. He challenges Git's design philosophy, arguing the newest attributes should apply universally rather than requiring explicit specification. This raises fundamental questions about whether Git's current behavior represents a principled design choice or an oversight.

The thread now balances between concrete technical solutions (the validated workflow) and broader design considerations (how attributes should apply across history). While the immediate technical question is resolved, the discussion continues regarding documentation improvements and whether Git's attribute handling could be made more intuitive for cases like Josef's. Junio Hamano has engaged with the architectural implications, leaving the door open for potential future enhancements.