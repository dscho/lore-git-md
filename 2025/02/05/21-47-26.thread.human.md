Here's the updated human-readable thread summary:

This thread began as a user asking about Git's clean/smudge filters during history rewriting operations like `git rebase --root`. The discussion quickly turned into a deep technical exploration of content filtering, with Brian M. Carlson providing authoritative guidance on proper renormalization techniques and limitations. Key takeaways include the correct syntax for renormalization during rebase (`--strategy ort -X renormalize`), the limited scope of `merge.renormalize` config, and why `git filter-repo` remains the most robust solution for complex cases.

The thread has taken an important turn with input from Elijah Newren, maintainer of `filter-repo`. He explains why the suggested `git add --renormalize` approach fundamentally conflicts with `filter-repo`'s design - the tool intentionally operates without a working tree or index. Newren highlights a core limitation in Git itself: there's currently no way to preview how renormalization would modify content without a full checkout. This suggests solving the problem properly would require changes to Git's plumbing to expose renormalization as a queryable operation.

What started as a workflow question has evolved into a discussion about architectural constraints. While practical workarounds exist (like using `filter-repo` with its current limitations), the most complete solution may require deeper changes to Git's internals. The thread now serves as both a guide to current best practices and a roadmap for potential future improvements to Git's filtering capabilities.