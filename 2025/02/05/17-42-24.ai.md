This is a philosophical discussion about the safety implications of `pull.ff=only` versus branch-specific rebase settings in Git's pull behavior. Junio C Hamano responds to D. Ben Knoble's patch that would allow branch-specific rebase settings to override `pull.ff=only`, expressing concern that this undermines the safety mechanism `pull.ff=only` is meant to provide.

Hamano frames `pull.ff=only` as a protective measure for users who want to ensure they don't accidentally create merge commits when pulling into branches with local changes. He worries that allowing rebase operations to override this safety check would leave rebase-oriented workflows without similar protections. The discussion highlights a tension between two valid perspectives: Knoble's view that branch-specific settings represent explicit user intent that should take precedence, versus Hamano's view that safety mechanisms should be preserved across all workflows.

The email doesn't propose technical changes but rather explores the conceptual implications of the patch's behavior. Hamano notes that while command-line `--rebase` flags currently work in these scenarios, he's concerned about losing the safety net for rebase users if branch-specific configurations can bypass `pull.ff=only`. This suggests the discussion has moved beyond implementation details to fundamental questions about Git's configuration philosophy and user expectations.

Key points for future discussion include whether `pull.ff=only` should maintain its current strict behavior as a safety feature, or whether branch-specific configurations should be able to override it as proposed. The thread shows no clear resolution yet on how to balance these competing concerns between workflow flexibility and safety guarantees.