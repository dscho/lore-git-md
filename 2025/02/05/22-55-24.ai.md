This is a user support response clarifying Git's history renormalization behavior with clean/smudge filters. Brian M. Carlson corrects several misconceptions in the original query, explaining that `--strategy renormalize` is invalid syntax (Git looks for a non-existent `git merge-renormalize` binary) and that `merge.renormalize` is not a real configuration option. The key technical clarification is that renormalization should be done via `git add --renormalize .` during a rebase operation, with Carlson suggesting a concrete command sequence using `git rebase --root -x` to automate the process. The response also mentions `git filter-repo` as a potential alternative approach and CCs its maintainer for further input. This email provides authoritative guidance on a common point of confusion where Git's content filtering intersects with history rewriting operations, though it does not propose any code changes to Git itself. The explanation focuses on correct usage patterns rather than suggesting modifications to Git's behavior or documentation.