This is a follow-up in the ongoing discussion about configuration precedence between `pull.ff=only` and branch-specific rebase settings. D. Ben Knoble responds to Junio C Hamano's safety concerns by proposing an alternative configuration approach that would maintain safety for non-rebase cases while still allowing rebase workflows to function as expected.

The email suggests introducing a new configuration option `pull.merge=ff` (or `ff-only`) that would enforce fast-forward-only behavior for merge-based pulls while permitting rebase operations when explicitly configured (either via branch-specific settings or command-line flags). This attempts to address Hamano's concern about losing safety protections in rebase workflows while preserving the flexibility of branch-specific configurations.

Knoble notes that while `pull.rebase=true` provides similar behavior, it doesn't fully meet their use case of wanting warnings about potential non-fast-forward situations for main branches. The proposal implies a more nuanced configuration system where the safety mechanism (`ff-only`) would be context-aware, applying only to merge operations while allowing rebase operations to proceed when specifically requested.

This response moves the discussion forward by offering a potential compromise solution that might satisfy both the safety requirements and workflow flexibility concerns. It suggests the current binary choice between strict safety and workflow preferences might be too limiting, pointing toward a more sophisticated configuration system that could distinguish between merge and rebase contexts.