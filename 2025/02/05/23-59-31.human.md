This is a follow-up in a user support thread about Git's content filtering during history rewriting. Josef Wolf confirms Brian M. Carlson's earlier guidance about the correct way to use renormalization with rebase, but encounters some practical issues with the suggested approach.

Josef verifies that using `--strategy ort -X renormalize` works better than the invalid `--strategy renormalize` syntax, though it still requires manual intervention when filtered files are involved. He successfully uses `git add --renormalize` followed by commit amendment to progress through the rebase, though this isn't fully automated.

The thread reveals two remaining practical concerns: (1) Carlson's suggested `-x` automation runs the renormalization on every commit, producing warnings for commits that don't touch filtered files, and (2) Josef questions whether there's a way to only run the command when rebase actually halts due to merge conflicts. The exchange remains constructive, with Josef clearly documenting his steps and results while seeking to refine the workflow. No new bugs are identified - this remains a usage optimization question building on the thread's earlier resolution.