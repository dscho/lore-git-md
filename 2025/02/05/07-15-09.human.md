This is a brief review comment on a patch proposing new long option names for `git log -S` and `git log -G`. Johannes Sixt notices that the patch accidentally includes changes to gitk (a separate Git GUI tool) mixed in with the core diff functionality changes. He requests that the gitk changes be split into a separate patch submission, following the convention that gitk patches should have a subject line prefixed with "gitk:".

The comment doesn't engage with the substantive discussion about the proposed `--patch-modifies` and `--patch-grep` names, focusing instead on this submission hygiene issue. It suggests the author needs to resubmit with the gitk changes separated out. This appears to be a straightforward procedural correction rather than a technical critique of the patch's main content.