This appears to be a user question about Git's clean/smudge filters and history rewriting, rather than a patch submission. Josef Wolf is encountering issues when trying to use `git rebase --root --strategy renormalize` to normalize file history after setting up clean/smudge filters.

The key points:
- They've successfully set up clean/smudge filters that work for new commits
- They're trying to normalize existing history but getting confusing errors
- The error message references a non-existent "merge-renormalize" command
- They've tried adding `-c merge.renormalize=true` without success

This seems to be either a misunderstanding of Git's rebase/renormalize functionality or possibly a bug in how Git handles the renormalize strategy. The email is seeking help from the community on the proper way to normalize file history when using clean/smudge filters.

The question appears genuine and well-formed, though it might be better suited for a user support forum than the development mailing list. The issue touches on some advanced Git functionality (clean/smudge filters and history rewriting) that could benefit from expert input.