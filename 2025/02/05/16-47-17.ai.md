This is a refactoring patch in the `git clone --revision` series, specifically handling tag cloning behavior. The patch inverts the internal flag controlling tag cloning from `--no-tags` to `--tags` as the default, while maintaining the same functionality. 

The change simplifies the code by making the default case (cloning tags) the positive case rather than having to negate `--no-tags` throughout the code. The documentation is updated to clarify that `--tags` is now an explicit option, though it remains a no-op in the default case. The patch includes thorough updates to both the man page and code to reflect this inversion, with careful attention to all locations where the tag cloning behavior is controlled.

This is a preparatory change that makes the code more straightforward for the subsequent introduction of the `--revision` feature. The patch maintains all existing behavior - future fetches still won't follow tags when `--no-tags` is specified, and explicit tag fetches continue to work as before. The change was reviewed and verified as part of the larger series, with no need for additional tests since it doesn't alter functionality.