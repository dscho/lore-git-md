This is a feature patch proposing long option names for `git log -S` and `git log -G` functionality, part of an ongoing discussion about improving command-line interface consistency.

The patch introduces `--patch-modifies` as an alias for `-S` and `--patch-grep` as an alias for `-G`, addressing previous feedback about naming clarity. The author argues these names better reflect the user's mental model of the operations (searching patch content) rather than the internal implementation details (which don't always generate full patches). The changes touch documentation, core diff functionality, and related tests across multiple files.

The thread context reveals this is version 2 of the patch, with previous discussion debating alternative names like `--grep-in-patch` and `--pickaxe-grep`. Key points from earlier feedback include concerns about consistency with existing `--pickaxe-*` options and whether the names accurately describe the underlying behavior. The current proposal attempts to balance these concerns by using a common `--patch-` prefix while maintaining the existing short options.

The implementation appears thorough, updating all relevant documentation and test cases to reference both old and new option names. The changes are focused and don't appear to introduce any behavior modifications beyond the naming additions. The patch seems ready for review, though the naming choices may still warrant discussion given the previous back-and-forth about terminology.