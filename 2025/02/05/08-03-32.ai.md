This is a minor code review comment in the `git clone --revision` series, specifically about the helper function `die_for_incompatible_opt2()` introduced in patch 6/7. Patrick Steinhardt points out that the explicit condition check before calling the helper function is redundant, as the helper already handles the condition internally. The comment is directed at Toon Claes regarding a usage example in `builtin/replay.c` that shows the helper being called with a pre-checked condition.

The observation is technically correct but appears to be about an unrelated file (`builtin/replay.c`) rather than the clone series itself. This suggests the comment may have been misdirected or is discussing a separate application of the helper function. The core functionality of the clone series remains unaffected, with the only outstanding issue being the safety verification in `update_head()` noted in the thread summary.