This is a brief response in the ongoing thread about adding long option names for `git log -S` and `git log -G` functionality. Johannes Sixt requests that Illia Bobyr split out gitk-related changes from the current patch submission into a separate follow-up patch with an appropriate "gitk:" prefix in the subject line. 

The request is procedural - it doesn't comment on the substantive naming debate around `--patch-modifies`/`--patch-grep` versus other alternatives that has been the main focus of the thread. The gitk changes appear to be tangentially related to the main patch but should be submitted separately according to project conventions for maintaining clean patch boundaries. This maintains the separation between core Git changes and gitk-specific modifications.

The thread context shows this is version 2 of a patch that adds long option names for pickaxe functionality, with previous discussion focused on naming clarity and consistency. The current exchange doesn't advance that discussion but rather ensures proper patch organization before further review. The gitk changes would presumably need to be updated to handle the new long options once they're finalized.