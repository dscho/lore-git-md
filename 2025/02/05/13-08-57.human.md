Bugfix discussion regarding configuration precedence in `git pull`. Junio Hamano expresses reservations about the proposed change that would allow branch-specific rebase settings (`branch.<name>.rebase=true`) to override a global `pull.ff=only` setting.

Hamano argues that when users set `pull.ff=only`, they likely want to enforce fast-forward behavior as a strict safety measure, regardless of whether the pull would otherwise rebase. He suggests this configuration indicates users want to prevent any non-fast-forward merges, viewing them as potentially problematic situations that require manual review.

The response casts doubt on the original patch's assumption that users would want rebase behavior to bypass fast-forward restrictions. This introduces a potential philosophical disagreement about configuration precedence - whether branch-specific preferences should override what might be intentional global safety settings.

The discussion appears to be heading toward deeper examination of user expectations around these configuration interactions, suggesting the original behavior might have been intentional rather than a bug. This could lead to either rejecting the patch or potentially documenting the current behavior more clearly.