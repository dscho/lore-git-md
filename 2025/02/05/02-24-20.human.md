Feature addition introducing long option names for `-S` and `-G` pickaxe options.

This patch (v2) adds `--patch-modifies` as the long option for `-S` and `--patch-grep` for `-G`, continuing the thread's discussion about naming clarity. The changes are comprehensive, updating documentation, error messages, and tests across multiple files where these options are referenced. The author argues these names better reflect the user-facing behavior (searching patch content) rather than implementation details.

The implementation appears thorough, with all relevant documentation and tests updated to include both short and long forms. Tests now use the long options where possible for readability, while maintaining backward compatibility. The patch maintains existing behavior while adding the new interface options.

This follows previous discussion (v1) where alternative names like `--grep-in-patch` and `--pickaxe-grep` were debated. The current naming attempts to balance concerns about consistency with existing `--pickaxe-*` options while accurately describing the behavior. While technically sound, the naming choice may still benefit from further discussion as noted in the thread.