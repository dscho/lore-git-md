This is a technical clarification in the ongoing discussion about Git's renormalization behavior with smudge/clean filters. Josef Wolf follows up on Elijah Newren's explanation about how `--attr-source` parameters propagate during rebase operations.

The key points:
- Confirms understanding that `--attr-source` settings propagate via environment variables to subprocesses (including those run with `-x` during rebase)
- Raises a new technical nuance: since each `-x` command runs at a different point in the rebase (with different HEADs), directly specifying `--attr-source` in those commands would be incorrect
- The discussion continues to refine the precise mechanics of attribute handling during complex history rewriting operations

This email represents a subtle but important clarification in the thread's exploration of Git's internals, particularly around how environment variables and attribute sources interact during rebase operations. It doesn't introduce new solutions but helps ensure the previously proposed workflows are implemented without subtle errors.