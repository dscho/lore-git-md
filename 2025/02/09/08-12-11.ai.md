Platform compatibility patch addressing `-Wsign-comparison` warnings in the apply subsystem, part of a v2 series from Zejun Zhao's GSOC work. This specific patch focuses on type changes in `struct apply_state` and related variables to eliminate signed/unsigned comparison warnings while maintaining existing behavior.

The changes convert `.max_change` to `unsigned` (as it's used as a counter) and `.max_len` to `size_t` (as it represents a length), with corresponding updates to variables in `show_stats()`. The patch includes a necessary but safe cast to `int` for `printf()` formatting of `max_len`, which is bounded to 50 characters. The modifications touch 9 lines across `apply.c` and `apply.h`, primarily changing type declarations and adding one explicit cast.

This continues the thread's systematic approach to warning elimination while respecting the project's established patterns. The changes appear well-considered, with the author noting the safety of the printf cast due to the existing length bound. The patch fits within the thread's broader context of balancing type safety with practical constraints, though it doesn't address the still-open questions about stats output casting documentation.