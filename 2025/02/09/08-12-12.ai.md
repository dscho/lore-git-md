Bugfix and platform compatibility patch addressing `-Wsign-comparison` warnings in `apply.c`, part of a v2 series from Zejun Zhao's GSOC work. This second patch in the 6-part series focuses on converting specific variables from `int` to `size_t` or `unsigned` where appropriate, following the thread's established patterns.

The changes target three categories of variables: those returned from system functions (like `strlen()`), array indexing variables, and length/size/distance variables. Key changes include converting `len` in gitdiff_index parsing, `preimage_limit` in patch matching logic, and position tracking in hunk application. The patch also updates statistics tracking in `patch_stats()` to use `size_t` for filename lengths and `unsigned` for line counts.

The modifications maintain existing behavior while eliminating type mismatch warnings, with careful attention to pointer arithmetic safety and interface consistency. The changes align with the thread's technical resolutions around type safety decisions, particularly the distinction between using `size_t` for lengths and `unsigned` for counters. The patch demonstrates good attention to detail in selecting which variables to convert while avoiding unnecessary churn in the codebase.