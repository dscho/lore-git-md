This is a technical follow-up in the ongoing discussion about Git's attribute handling and line ending conversions, specifically addressing pattern matching in `.gitattributes` files.

Josef Wolf responds to Torsten Bögershausen's suggestions about simplifying `.gitattributes` patterns for CRLF conversion. The key points are:

1. Josef explains that the `*/*` pattern is intentionally matching files one level below the `.gitattributes` directory, citing Git's pattern matching rules. However, he confirms that changing to a simple `*` pattern produces the same behavior.

2. He observes that committing `.gitattributes` triggers a warning about LF->CRLF conversion, but the actual conversion doesn't occur until after removing and resetting the files with `git reset --hard`. This shows the conversion isn't happening during normal operations as expected.

3. Josef clarifies that the `*/B` and `*/P-*` patterns refer to specific XML files needing CRLF conversion, not directories, correcting Torsten's assumption.

The email provides concrete evidence that Git's attribute processing isn't behaving as documented, particularly around when line ending conversions are applied. This adds another data point to the thread's exploration of edge cases in Git's text conversion pipeline, specifically around the interaction between attribute patterns and file operations. The discussion remains focused on diagnosing why specified CRLF conversions aren't being applied as expected during normal Git operations.