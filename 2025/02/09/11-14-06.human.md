This is a platform compatibility discussion about Git's attribute handling, specifically around line endings and filters. Torsten Bögershausen responds to Josef Wolf's report of `.gitattributes` CRLF settings being ignored for certain files when combined with smudge/clean filters.

Josef describes a complex setup involving XML files from a proprietary application that randomly reorders elements, requiring canonical sorting in clean filters, alongside CRLF line endings. His `.gitattributes` configuration specifies `text=auto eol=crlf` with filter rules, but `git ls-files --eol` shows LF line endings instead of the requested CRLF.

Torsten suggests simplifying the attribute patterns from `*/*` to `*` and questions whether `B` is a directory in the path specifications. He also recommends removing the leading path component from binary file patterns (like `*.dll` instead of `*/*.dll`). The response doesn't solve the core issue but offers potential configuration tweaks to investigate.

This continues the thread's exploration of Git's complex interaction between filters and text conversion attributes, now with a concrete example where explicit CRLF settings appear to be ignored despite proper configuration. The discussion remains technical and focused on attribute pattern matching rather than addressing the philosophical questions raised earlier in the thread.