This is a philosophical discussion about Git's attribute handling during history rewriting, continuing the long-running thread about clean/smudge filters and renormalization.

Elijah Newren responds to Josef Wolf's concerns about the `--attr-source` parameter behavior during rebase operations. He clarifies that the primary rebase call does need explicit attribute source specification because HEAD changes during rebasing, and would otherwise use older `.gitattributes` versions by default. He confirms that environment variables propagate to `- x` subprocesses, explaining why Phillip Wood's solution with `--attr-source=$(git rev-parse HEAD)` works as intended.

The exchange highlights an ongoing tension between Git's architectural principle of respecting historical repository state and real-world use cases where users need to apply current filters to old commits. Newren empathizes with the challenges of fixing past mistakes while defending Git's design choices, drawing from his experience creating `git-filter-repo` to handle such scenarios.

This email doesn't introduce new technical solutions but provides important clarification about environment variable propagation and rebase's HEAD behavior. It maintains the thread's constructive tone while reinforcing why the current behavior makes sense architecturally, even if it sometimes surprises users. The discussion remains relevant for anyone dealing with complex history rewriting involving filters.