Platform compatibility patch (5/6 in series) converting loop counters from `int` to `size_t` in `apply.c` to resolve `-Wsign-comparison` warnings. This continues Zejun Zhao's GSOC work on type safety in the patch application code.

The patch systematically changes 14 loop counter declarations from `int` to `size_t` across various functions in `apply.c`, primarily where counters are compared against `ARRAY_SIZE()` or string list counts. Key changes include loop counters in `parse_git_diff_header()`, `use_patch()`, `parse_chunk()`, and several patch matching/image manipulation functions. The diff shows straightforward mechanical conversions with no logic changes.

This follows the series' established pattern of using `size_t` for memory/length operations while maintaining the code's architectural decisions. The changes are minimal (32-line diff) and focused, continuing the project's principle of targeted fixes for type safety warnings. The patch demonstrates consistent application of the type hierarchy principles documented in earlier series patches, now extending them to loop counters that interface with size-based operations.