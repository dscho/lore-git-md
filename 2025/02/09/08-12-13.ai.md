This is the third patch in a 6-part series addressing `-Wsign-comparison` warnings in `apply.c`, focusing on type safety in patch application logic. The patch specifically handles a comparison between `git_hdr_len` (an `int` that can be negative) and `len` (a `size_t`), which triggers compiler warnings.

The change is minimal but important - it adds a `size_t` cast to `git_hdr_len` after confirming it's non-negative, maintaining the type safety patterns established earlier in the series. The patch follows the thread's established conventions where negative values are checked first before safe type conversions.

This continues the series' consistent approach to:
1. Preserve existing behavior while eliminating warnings
2. Maintain clear type hierarchy (`size_t` for lengths, `int` where needed)
3. Keep changes surgical and well-justified

The modification affects just one comparison in the patch header parsing logic, showing the author's careful attention to minimizing churn while addressing the warning systematically. The change appears technically sound and aligns with the architectural decisions made in previous iterations of the series.