This is a follow-up in the ongoing investigation of Git's filter and attribute processing issues, specifically addressing binary file corruption during checkout operations.

Josef Wolf identifies an ordering problem in `.gitattributes` as the likely cause of the previously reported binary file corruption. The issue stems from a later pattern (`*/P-* filter=etsfile`) overriding an earlier one (`*.store -filter -text`), which explains why the `.store` file wasn't being treated as binary as intended. 

The author retracts their previous email's findings, clarifying this was not a fundamental binary file handling bug but rather a configuration error where attribute patterns were being unintentionally overridden. This narrows the scope of the binary file corruption issue while leaving the broader questions about CRLF conversion timing and attribute processing still open for discussion.

The note is brief but important for thread participants to understand that one aspect of the investigation (the binary file corruption) appears to have a simpler explanation than initially thought. The core technical discussion about Git's filter pipeline behavior continues, but now with this particular edge case resolved.