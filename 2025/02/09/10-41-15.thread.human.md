Here's a human-readable summary of the thread:

René Scharfe has proposed a small but clever optimization to Git's commit marking code that restructures how parent commits are processed during traversal. The change reduces peak memory usage by clearing parent lists earlier, moving from O(n) to O(1) memory complexity while maintaining the same depth-first behavior. In tests, this cuts the maximum parent list length from 12 to 4 entries.

The technical merits are well-established - the implementation is correct and provides measurable (though modest) memory improvements. Patrick Steinhardt initially raised questions about whether the optimization would be noticeable in real-world scenarios, particularly in complex merge histories. René acknowledges the gains won't be dramatic in typical cases but maintains there are no downsides to adopting the cleaner approach.

The discussion has now reached a decision point: whether small-but-clean optimizations like this should be accepted on their own merits, or if they need to demonstrate more substantial real-world impact. With no outstanding technical concerns and agreement on the implementation's correctness, the patch appears ready for maintainer consideration. The 4-line change to commit.c cleanly demonstrates how thoughtful restructuring can yield memory improvements without compromising behavior.