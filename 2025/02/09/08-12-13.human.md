This is the third patch in a 6-part series addressing `-Wsign-comparison` warnings in `apply.c`, part of a GSoC project focused on platform compatibility. The series has established clear type conversion patterns, with `size_t` used for length quantities and `unsigned` for counters.

The current patch specifically handles a warning in the `find_header()` function where a signed `git_hdr_len` (which can be negative) was being compared against an unsigned `len`. The solution is straightforward - after verifying the value is non-negative, it's safely cast to `size_t` for the comparison. This maintains the existing behavior while silencing the compiler warning.

The change is minimal (just one line modified) and follows the established patterns from earlier patches in the series. The author has clearly internalized the thread's technical decisions about type safety and is applying them consistently. This continues the series' trend of careful, focused changes that improve code quality without introducing new risks.