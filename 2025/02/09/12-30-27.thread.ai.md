Here is the updated dense AI thread summary incorporating Peter Seiderer's latest email:

Cross-compilation issue in Git's build systems where host's shell path (/usr/bin/sh) leaks into target builds, causing git fetch failures when target expects /bin/sh. Originally reported by Peter Seiderer while converting Buildroot package from autoconf to Meson for Git 2.48.1. Problem affects both Meson and make builds but manifests differently - Meson's `find_program('sh')` calls don't distinguish build-time vs target-time executables, while make historically assumed matching host/target paths.

Patrick Steinhardt proposed initial Meson solution using `native:` parameter and `target_*` variables, but Peter Seiderer raised concerns about Meson documentation compliance. Junio Hamano expanded scope to question whether similar fixes should be made to make-based builds given historical lack of cross-compilation support. Peter Seiderer now provides key observation that make users have historically accepted hardcoded defaults (SHELL_PATH=/bin/sh) while only native build users customized paths, suggesting make's cross-compilation issues may be less pressing in practice.

Technical details remain focused on proper path separation for shell (`sh`), Perl, Python across build systems. Meson-specific issues center on lines 186 (shell detection) and 685 (SHELL_PATH definition) in meson.build. Make build considerations involve SHELL_PATH and whether to introduce target-specific variants, though evidence suggests current defaults satisfy most cross-compilation users.

Key open questions:
1. Final Meson implementation approach (Patrick's native: + target_* vs alternatives)
2. Whether to extend fixes to make build system (growing consensus to focus on Meson only)
3. Impact on Patrick's in-flight meson improvements series which must land first

Thread now shows convergence toward asymmetric solution - proper cross-compilation support in Meson while leaving make's behavior unchanged, supported by Peter's observation about make users' historical satisfaction with defaults. Participants include original reporter Peter Seiderer, patch author Patrick Steinhardt, and maintainer Junio Hamano, with Peter's latest input providing practical data supporting the pragmatic Meson-only approach.