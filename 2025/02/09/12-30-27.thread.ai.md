Here is the dense AI thread summary capturing all key information from this initial bug report:

Cross-compilation issue in Meson build system where host's shell path (/usr/bin/sh) leaks into target builds, causing git fetch failures when target expects /bin/sh. Reported by Peter Seiderer while converting Buildroot package from autoconf to Meson for Git 2.48.1. Autoconf correctly uses /bin/sh while Meson hardcodes host path from find_program('sh'). Problematic code in meson.build lines 186 (shell path detection) and 685 (SHELL_PATH definition). Fails with ENOENT when target lacks /usr/bin/sh. Suggested solutions: hardcode /bin/sh or make path configurable via Meson option. Clear reproduction provided showing autoconf vs Meson behavior via strace output. Affects any cross-compiled Meson build where host and target shell paths differ. Requires Meson build system changes for proper cross-compilation handling of shell path detection.