Platform compatibility patch addressing `-Wsign-comparison` warnings in `apply.c`, part of a 6-patch series. This is the fourth iteration (v3) of changes to improve type safety in patch application logic.

The patch focuses on three specific functions where pointer differences (ptrdiff_t) were being compared with size_t values, triggering compiler warnings. The author explains why each cast is safe by detailing the pointer relationships and guarantees in each case:

1. In `date_len()`, the date pointer is guaranteed to be at or after the line start
2. In `git_header_name()`, pointer arithmetic ensures `cp` is within bounds
3. Similarly for name iteration in the same function

The changes are minimal (3 lines modified) but carefully reasoned, with explicit safety justifications provided. This continues the series' pattern of methodical type system improvements while preserving existing behavior. The patch appears technically sound and well-explained, addressing a clear compiler warning without introducing new risks.

As part of the larger series, this contributes to eliminating `-Wsign-comparison` warnings while maintaining the code's architectural integrity. The v3 iteration shows responsiveness to review feedback from earlier versions.