Platform compatibility patch completing the `-Wsign-comparison` warning fixes in `apply.c`. This is the final patch (6/6) in Zejun Zhao's v3 series addressing type safety in the patch application code.

The patch removes the `DISABLE_SIGN_COMPARE_WARNINGS` macro that was previously suppressing compiler warnings about signed/unsigned comparisons. This removal is now safe because the preceding 5 patches in the series have systematically converted all relevant variables and comparisons to use appropriate types (`size_t` or `unsigned int`) while preserving the existing behavior and error handling.

Key technical points:
- The series has already converted all problematic comparisons across 8 core patch application functions
- Maintained negative value handling where needed (e.g., `git_hdr_len` remains `int` for error signaling)
- Standardized type usage for string operations, array indexing, and loop counters
- Verified printf formatting safety for all size_t variables
- Resolved all prior review feedback about type consistency

This final change demonstrates confidence that the type conversions in patches 1-5 have fully addressed the warning conditions, allowing the warning suppression to be removed. The one-line change is effectively a quality gate - if any sign comparison issues remain, they would now surface as compiler warnings.

The series appears technically complete, having addressed all concrete feedback from reviewers while maintaining the core architecture. The remaining open questions about signed vs unsigned type strategy (noted by Junio C Hamano) are more philosophical and don't block this specific series.