Documentation patch for `git merge-tree --stdin` behavior.

This patch improves the documentation for `git merge-tree`'s `--stdin` mode, clarifying two key aspects of its behavior. First, it adds a dedicated section for `--stdin` in the options list, noting that it implies `-z` mode (which uses NUL-terminated output). Second, it corrects the merge status documentation by removing the mention of negative status codes, since the command would die before printing them in error cases.

The changes are straightforward and align with the thread's broader goal of improving `--stdin` mode's reliability and documentation. The patch builds on Elijah Newren's earlier documentation updates in the series while maintaining consistency with Phillip Wood's other contributions to this thread. The modifications are limited to `Documentation/git-merge-tree.txt`, focusing on making the behavior more transparent to users without introducing any functional changes.

Key technical details for future reference:
- Documents that `--stdin` implies `-z` (NUL-terminated output)
- Removes incorrect documentation about negative merge status codes
- Adds cross-reference to INPUT FORMAT
- Maintains consistency with the thread's plumbing/porcelain separation theme

The patch appears well-considered and uncontroversial, completing the documentation improvements started earlier in the series.