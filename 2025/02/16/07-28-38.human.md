This is the first patch in a 6-part series addressing `-Wsign-comparison` warnings in `apply.c`, focusing on type safety improvements. The patch corrects type mismatches in `apply.h` where several fields were declared as `int` but were only used as unsigned values, causing compiler warnings.

The changes include:
- Converting `apply_state::max_change` to `unsigned int` (it's a line counter)
- Changing `apply_state::max_len` to `size_t` (it stores lengths)
- Making `patch::lines_added/deleted` `unsigned int` (they're line counters)
- Updating related variables in `show_stats()` and `patch_stats()` functions

The patch includes necessary printf format adjustments with safe casts, noting that a 50-character upper bound ensures the size_t-to-int cast is safe. This appears to be a straightforward technical improvement that maintains existing behavior while eliminating compiler warnings, though it does raise the broader architectural question (mentioned in the thread context) about Git's strategy for signed vs unsigned types.

As the first in the series, this sets the pattern for subsequent patches by demonstrating the careful type conversions and printf formatting considerations needed for this kind of change. The v3 iteration has already incorporated review feedback from earlier versions.