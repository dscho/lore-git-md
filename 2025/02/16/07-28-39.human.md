This is part 2 of a 6-patch series addressing `-Wsign-comparison` warnings in `apply.c`, focused on type safety improvements. The patch systematically converts several variables from `int` to `size_t` or `unsigned int` where appropriate, including:

- Variables receiving return values from system functions (like `strlen`)
- Array indexing variables
- Length/size/distance variables
- Function parameters like `preimage_limit` in `line_by_line_fuzzy_match()`

The changes are straightforward type conversions that eliminate compiler warnings while maintaining the existing logic. The author has carefully preserved the handling of negative values where needed (like in `find_pos()`) and ensured printf formatting safety through bounded ranges. This continues the series' pattern of addressing specific warning categories while maintaining architectural consistency with the rest of the codebase.

The patch appears technically sound and well-contained, following the established strategy from earlier reviews. It's part of a larger effort that has already incorporated feedback from multiple contributors through three iterations, with this version resolving previous concerns about type consistency between related fields.