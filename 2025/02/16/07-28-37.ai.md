This is a platform compatibility patch series (v3) addressing `-Wsign-comparison` warnings in `apply.c`, part of Zejun Zhao's GSOC project. The series systematically converts signed/unsigned mismatches in the patch application logic while preserving existing behavior.

The key changes in v3:
- Aligns `patch::lines_added/deleted` with other counters by making them `unsigned int`
- Uses `unsigned int` consistently instead of bare `unsigned`
- Separates unrelated changes from the loop counter commit

The series follows three strategies for different warning types:
1. Casting `ptrdiff_t` to `size_t` where safe
2. Converting `int` lengths to `size_t` when provably unsigned
3. Using `size_t` loop counters with unsigned bounds

Technical scope includes:
- Type changes in `apply_state` (max_change, max_len)
- Function parameter updates (preimage_limit)
- Loop counter conversions
- printf format adjustments with necessary casts
- 69 lines modified across apply.c/apply.h

The series now addresses earlier review feedback about field type consistency, particularly aligning `patch_stats()` calculations with the new unsigned types. The author maintains the architectural approach of preserving negative value handling where needed while eliminating warnings through targeted type changes.

This iteration shows responsiveness to review comments while keeping the focused scope of warning elimination. The changes demonstrate careful consideration of type system implications in patch application logic, particularly around line counting and comparison operations. The series appears technically sound with thorough test coverage implied by the maintainer's earlier positive feedback on the approach.