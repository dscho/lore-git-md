This thread has been refining the `git merge-tree --stdin` functionality, with Phillip Wood and Elijah Newren collaborating on both bugfixes and code cleanups. The core issue was a potential deadlock in scripts processing merge-tree output incrementally, which has been resolved with minimal performance impact. The series has evolved through multiple iterations, with v2 now incorporating all feedback.

Key developments include:
- Deadlock fixes via stdout flushes between merges (benchmarked at <0.5% overhead)
- Clear documentation that `--stdin` implies `-z` and always exits 0 (status reported via stdout)
- Removal of redundant error handling in the stdin path
- Precise documentation of exit status behavior

The latest patch from Phillip Wood simplifies the code by removing unused result check in stdin mode, as `real_merge()` either succeeds or dies (never returning negative values in this context). This maintains all existing behavior while making the implementation cleaner. Elijah Newren, the merge machinery expert, has reviewed and confirmed the accuracy of these changes throughout the thread. The series is now well-polished, with all functional improvements tested and documentation thoroughly vetted.