Refactoring patch in the `git merge-tree --stdin` series. This is patch 2/5 in Phillip Wood's follow-up to Elijah Newren's original series addressing a deadlock issue in `merge-tree`'s stdin mode.

The patch simplifies the stdin handling code in `builtin/merge-tree.c` by removing redundant error checking. It observes that `real_merge()` can only return 0 (success) or 1 (failure) since it dies on any negative status, making the `result < 0` check and associated variable unnecessary. The change is straightforward and maintains the same behavior while making the code slightly cleaner.

This appears to be a logical cleanup that builds on the earlier deadlock fix, though it's worth noting the patch comes from a different author (Phillip Wood) than the original series (Elijah Newren). The change is small and focused, touching only the stdin parsing logic without affecting the core merge functionality.