This is a bugfix and documentation cleanup series for `git merge-tree --stdin`. Phillip Wood encountered a deadlock when using the command in a script due to unflushed stdout, which is fixed in the first patch. The remaining patches clean up redundant code, clarify configuration behavior, and improve documentation formatting.

The series addresses a real issue where `merge-tree --stdin` could hang when used in scripts because it wasn't flushing output between merges. The fix is straightforward - adding explicit flushing. The follow-up patches show good attention to detail, removing unused code and improving docs while working in the area. The changes are small and focused, touching only the merge-tree command's implementation and documentation. The patches appear well-crafted and uncontroversial, making this a solid maintenance series that improves both functionality and code quality.