Platform compatibility patch continuing the series to address `-Wsign-comparison` warnings in `apply.c`. This is patch 5/6 in the v3 series, converting various loop counters from `int` to `size_t` where they trigger signed/unsigned comparison warnings.

The changes are mechanical but widespread, touching 30 lines across `apply.c` to update loop counters in functions like `parse_git_diff_header()`, `use_patch()`, `parse_chunk()`, and several others involved in patch application logic. Each change follows the established pattern from earlier patches in the series, maintaining the same behavior while resolving compiler warnings. The author has carefully preserved all existing logic, only changing the counter types.

Key technical points for future reference:
- Loop counters changed to `size_t` in 8 different functions
- Array indexing operations updated to use `size_t` consistently
- Maintains all existing bounds checking and negative value handling where relevant
- Follows the series' established approach of minimal, focused changes
- No behavior changes - purely type system adjustments

This patch continues the series' high quality, with each change clearly motivated by the compiler warning it fixes. The v3 iteration has already addressed previous review feedback, and this patch appears uncontroversial as it follows the same well-established pattern as earlier patches in the series. The remaining architectural questions about signed vs unsigned strategy (raised in earlier discussion) don't directly impact these loop counter changes.