This is a platform compatibility patch in a series addressing `-Wsign-comparison` warnings in `apply.c`. It's the third of six patches in v3 of the series, focusing on type safety in patch application logic.

The patch specifically deals with a `git_hdr_len` variable that can be negative but needs to be safely compared against a `size_t` length. The solution casts `git_hdr_len` to `size_t` after confirming it's non-negative, eliminating the compiler warning while maintaining the existing behavior. The change is minimal (just one line modified) but follows the series' pattern of careful type conversions.

Key technical details for future reference:
- Variable `git_hdr_len` remains `int` to preserve negative value signaling
- Comparison with `size_t len` now uses explicit cast after safety check
- Maintains error return (-128) for negative `git_hdr_len` cases
- Part of broader strategy to handle signed/unsigned comparisons in `apply.c`

This patch demonstrates the series' approach to warning elimination - preserving the existing logic while making type conversions explicit and safe. It follows the established pattern seen in earlier patches of the series, where negative value handling is maintained while satisfying compiler warnings.