Here is the updated dense AI thread summary incorporating the new email:

John Giorshev reports a behavioral issue with `--filter=blob:none` where Git silently falls back to full blob downloads when the server lacks filtering support, contrary to user expectations and causing performance problems in their repo scanning use case. They propose making this a hard error rather than silent fallback, providing an implementation in `fetch-pack.c` that checks filter capability negotiation and fails the clone when requested filters cannot be honored.

Jeff King (Peff) analyzes the proposal, noting an inconsistency in Git's current behavior: while unsupported filters silently fall back, explicitly disallowed filters (like `blob:none` or `sparse:oid`) already fail hard. He considers both backward compatibility concerns and the performance impact of silent fallback being worse than immediate failure. The discussion focuses on protocol negotiation in `fetch-pack.c` and filter capability handling, with Jeff suggesting the change might be reasonable without configuration given the existing behavior for disallowed filters, while leaving open the possibility of making this configurable as a transition path.

Key technical details: modifies filter capability handling in `fetch-pack.c`, touches fetch protocol negotiation, specifically the blob filtering feature. Open questions remain about whether to break compatibility or make strict behavior opt-in, and if acceptable for inclusion. The thread now includes Jeff King's analysis of the behavioral inconsistency and performance considerations, though no firm resolution yet.