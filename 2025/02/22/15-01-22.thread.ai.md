Here is the dense AI thread summary capturing all key information from this new thread:

John Giorshev reports a behavioral issue with `--filter=blob:none` where Git silently falls back to full blob downloads when the server lacks filtering support, contrary to user expectations and causing performance problems in their repo scanning use case. They propose making this a hard error rather than silent fallback, providing an implementation in `fetch-pack.c` that checks filter capability negotiation and fails the clone when requested filters cannot be honored. This would break backward compatibility for workflows potentially relying on the silent fallback, raising the question of whether to make this breaking change or introduce a new opt-in flag/config. The change affects protocol negotiation behavior in fetch/push, particularly around filtering capabilities, impacting users of `--filter` with older servers or proxies. A Stack Overflow link demonstrates real-world impact on the reporter's scanning tool. Key technical details: modifies filter capability handling in `fetch-pack.c`, touches fetch protocol negotiation, specifically the blob filtering feature. Open questions: whether to break compatibility or make strict behavior opt-in, and if acceptable for inclusion. No reviews or other participant positions yet as this is the thread root.