Here is the updated dense AI thread summary incorporating the new email:

John Giorshev's thread about making `--filter=blob:none` behavior stricter when server filtering is unsupported continues, now with a concrete pull request (#1869) implementing the change. The core issue remains the inconsistency in Git's current behavior where some unsupported filters fail hard while others (like blob filtering) silently fall back to full downloads, causing performance problems in repo scanning use cases.

Jeff King (Peff) previously analyzed this behavioral inconsistency, noting that explicitly disallowed filters already fail hard while unsupported ones fall back. The discussion has now moved to evaluating John's implementation, which would make unsupported blob filtering a hard error by modifying filter capability negotiation in `fetch-pack.c`.

Key technical details still center on `fetch-pack.c`'s filter capability negotiation, with the pull request providing a concrete implementation to evaluate. The open questions now focus on the policy decision: whether to prioritize backward compatibility (via warnings/fallback) or fail fast for performance-sensitive cases, given the existing behavior where disallowed filters already fail hard. Jeff's position remains that the change might be reasonable without configuration given this precedent, though the thread has not yet reached consensus on the compatibility tradeoffs.

The thread now includes both the original analysis of the behavioral inconsistency and the concrete pull request implementation, with discussion ongoing about whether this breaking change would be acceptable or needs to be made configurable as a transition path. Participant positions show John advocating for the stricter behavior to prevent performance issues, while Jeff provides context about existing filter behavior patterns but has not yet taken a firm position on the specific proposal.