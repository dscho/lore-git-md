Here's the human-readable thread summary:

John Giorshev reported an issue where Git silently falls back to full downloads when using `--filter=blob:none` with servers that don't support filtering, which causes performance problems for his repository scanning use case. He proposed making this a hard error instead.

Jeff King (Peff) analyzed the current behavior, noting an inconsistency where some unsupported filters fail hard while others silently fall back. He raised concerns about backward compatibility but acknowledged the performance impact of silent fallback might justify the change, especially since explicitly disallowed filters already fail hard.

John has now opened pull request #1869 implementing his proposed change to make unsupported blob filtering a hard error. The discussion continues about whether this breaking change is acceptable or if it should be made configurable. The key question remains whether to prioritize backward compatibility (with warnings and fallback) or fail fast for performance-sensitive cases, with Jeff noting there's already some precedent for hard failures with certain filters.

The technical focus remains on filter capability negotiation in `fetch-pack.c`, but the discussion has shifted from implementation details to evaluating the policy decision and compatibility tradeoffs. No final decision has been reached yet.