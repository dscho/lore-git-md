# Git Mailing List Digest — 2025/02/22

**The day in brief.** A moderately busy Friday with 10 emails across 8 threads, featuring final reviews on major refactoring work, documentation debates, and several bug reports. The standout items are Johannes Sixt's detailed review of shell function recognition improvements and Junio's guidance on documenting submodule merge behavior.

## Notable threads

**Shell function recognition improvements face test failures.** Johannes Sixt provides a thorough technical review of Moumita's v2 patch to enhance Bash function pattern matching in Git's diff machinery. The review identifies several issues with the current implementation that caused test failures, including invalid regex syntax and incorrect claims about supported features. Johannes suggests simplifying the approach by capturing entire function headers rather than precisely matching body introducers, while also recommending improvements to shell variable expansion patterns. The discussion gains historical context when Johannes explains that the word pattern requirement is a framework constraint rather than an intentional design choice, referencing a 2021 thread on the topic.

**Documenting submodule merge behavior sparks structural debate.** The discussion about how to document submodule conflict resolution behavior shared between 'ort' and 'recursive' merge strategies continues, now in its second iteration. Lucas Seiki Oshiro argues against splitting the duplicated text into an include file, citing the transitional nature of the recursive-to-ort migration. Junio Hamano later proposes a compromise: keeping the full warning text in the 'ort' section while adding a brief cross-reference in the 'recursive' section. This middle path addresses maintenance concerns while avoiding the complexity of AsciiDoc attributes suggested earlier in the thread.

**`the_repository` removal series nears completion with minor fix.** Jeff King spots a type mismatch in Patrick Steinhardt's major refactoring series to eliminate the `the_repository` global variable. The issue in `rerere_last_used_at()` involves using `int` instead of `timestamp_t` for storing a timestamp value. This small but important detail surfaces during final review of the 16-part series, which has already received extensive review and is marked ready for integration. The correction doesn't affect the core refactoring work but ensures type safety.

## In brief

Junio clarifies the mathematical details of `git bisect` optimization thresholds, noting that the 10,000-commit cutoff preserves correctness while allowing 1.6% deviation in midpoint selection. A NULL pointer dereference fix for `git check-mailmap` earns Junio's approval after review, addressing a segfault when processing email-only inputs. John Giorshev reports that `--filter=blob:none` silently falls back to full downloads with older servers and seeks guidance on making this a hard error. Markus Gerstel discovers `git gc --auto` failed to clean up a 180MB reflog over six years, suggesting a potential gap in the heuristics. Junio advises Audrey Dutcher on commit message best practices for an OpenBSD build fix involving clang 19's stricter warnings.