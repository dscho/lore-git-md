Refs iterator lifecycle patch with filesystem leak identified.

This is a brief follow-up in the performance optimization series for the refs subsystem, specifically addressing iterator lifecycle management. Patrick Steinhardt acknowledges shejialuo's discovery of an overlooked filesystem leak in the `files_fsck_refs_dir()` function when using directory iterators for ref consistency checks. 

The thread context shows this is part of a larger effort to improve refname availability checks across Git's ref backends, with significant performance gains already demonstrated (1.19x-2.32x speedups). The current patch focuses on properly separating iterator lifecycle management from iteration itself, including renaming `ref_iterator_abort()` to better reflect its now-unconditional cleanup role.

While the core iterator changes have been well-received and naming concerns resolved, this newly identified leak in the files backend needs investigation. The exchange demonstrates effective collaboration, with shejialuo catching an implementation gap and Patrick promptly acknowledging it. The series appears mature otherwise, with this being one of the final issues to address before completion.