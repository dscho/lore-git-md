This is a follow-up discussion about the gitk encoding fix series, specifically addressing Windows compatibility concerns raised about the first patch's approach to forcing UTF-8 system encoding.

Junio C Hamano responds to Konstantin Khomoutov's observation that the `encoding system utf-8` change in patch 1/2 may cause problems on Windows systems where the native encoding differs. The core issue revolves around how to handle filename encoding in gitk when dealing with Japanese characters during merge conflicts, now with platform-specific considerations.

The discussion highlights that while the current solution works for UTF-8-based systems (Linux/macOS), it could break Windows compatibility by overriding Tcl runtime's expected system encoding. Junio probes whether the solution should instead allow specifying repository-specific encodings through either user interface controls (menu items) or git configuration (proposing a hypothetical `gitk.pathencoding` setting).

Key technical points under consideration:
- The forced UTF-8 encoding may conflict with Windows' native code page handling
- Alternative approaches would need to either:
  - Detect platform and apply encoding selectively
  - Provide user-configurable encoding options
- The solution must handle edge cases like repositories with non-standard encoding (e.g., EUC-jp paths on UTF-8 systems)

This moves the discussion beyond the original bugfix to consider more flexible, platform-aware encoding handling in gitk. The thread remains focused on the first patch's approach while acknowledging the second patch's merge conflict decoding improvements remain technically sound but may need platform-specific adjustments.