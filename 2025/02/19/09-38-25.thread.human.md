Here's the updated human-readable thread summary:

This thread investigates unexpected pack file regeneration during Git's automatic garbage collection. Pierre Ossman reported that large repositories experience excessive pack file recreation during `git gc --auto`, contrary to documentation suggesting Git should reuse existing packs. The behavior persists even when using `gc.bigPackThreshold`, leading to unnecessary backup increments.

Han Young has now suggested a potential explanation: the issue might stem from partial clone functionality. He notes that `git-repack` currently repacks promisor packs (used in partial clones) even when they're marked as keep packs, which could explain the duplication Pierre observed. Han references a patch from another thread that would modify this behavior.

The discussion has narrowed to focus on whether partial cloning is involved in Pierre's case and whether the proposed promisor pack handling change would resolve the issue. This remains relevant to packfile experts working on Git's object storage behavior, particularly in scenarios involving partial clones and automatic maintenance operations.