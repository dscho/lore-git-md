This is a build system fix for the netrc credential helper tests, part of Patrick Steinhardt's series adding Meson support to Git's contrib components. The patch addresses test failures that occur when building out-of-tree (a common Meson pattern) by properly locating test scripts and binaries across source and build directories.

The changes introduce two key fixes: using the new `GIT_SOURCE_DIR` variable to find test scripts in the source tree, and adding a `CREDENTIAL_NETRC_PATH` environment variable to locate the built helper binary in the build directory. This maintains compatibility with existing in-tree builds while properly supporting Meson's out-of-tree approach.

As part of the larger effort to unify build systems, this patch demonstrates careful attention to cross-build-system compatibility. The changes are focused and well-explained, though the thread context notes that credential helper tests still have broader issues to resolve beyond just the build system integration.