This is part 10/12 of Usman Akinyemi's series to remove `the_repository` global variable from Git's builtin commands, following the established two-phase pattern seen in previous conversions.

The patch refactors `cmd_for_each_ref()` to prepare for removing its dependency on `the_repository`. The key change moves the `git_config()` call after `usage_with_options()` to avoid needing a NULL `repo` check when "-h" is passed. This maintains the same behavior while setting up for the next patch that will switch from `git_config()` to `repo_config()` and remove the global variable usage.

The change is minimal (just 3 lines modified) and follows the same pattern seen in earlier conversions in this series. The commit message clearly explains the rationale and how this fits into the larger refactoring effort. Christian Couder continues as mentor for this systematic conversion work.

This continues the series' consistent, careful approach to eliminating global state while preserving functionality. The change appears straightforward and low-risk, following the proven pattern established in previous conversions.