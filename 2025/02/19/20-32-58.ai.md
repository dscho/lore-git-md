This is part 10/12 of Usman Akinyemi's systematic `the_repository` removal series, focusing on `builtin/for-each-ref`. The patch follows the established two-phase pattern seen in previous conversions, with this being the initialization restructuring phase (odd-numbered patch) that moves the `git_config()` call after `usage_with_options()` to avoid needing NULL `repo` checks when "-h" is passed. The change is minimal (3 lines modified) but important for enabling the subsequent `the_repository` removal in the next patch.

Key technical details:
- Moves `git_config(git_default_config, NULL)` call after `usage_with_options()`
- This restructuring allows the subsequent patch to safely switch to `repo_config()` without NULL checks
- Preserves the "-h" help behavior that runs outside a repository
- Follows the same pattern demonstrated in previous conversions (pack-refs, ls-files, etc.)

The patch maintains the series' consistent quality - small, focused changes that enable the global variable removal while preserving functionality. The commit message clearly explains the motivation and connects to the larger refactoring effort. This continues the methodical progress through Git's builtins in the `the_repository` removal project.