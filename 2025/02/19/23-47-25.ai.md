This is a design discussion email in the ongoing `git-diff-pairs` plumbing command development thread. Junio Hamano responds to earlier points about responsibility allocation between frontends and the diff-pairs backend, focusing particularly on rename detection and pathspec filtering.

The email explores two architectural approaches for handling rename detection in the pipeline: either having frontends handle it (requiring backends to parse rename pairs) or making it the backend's responsibility (simplifying frontends but potentially limiting their flexibility). Junio frames this as a tradeoff between making frontends simpler versus giving them more control over diff processing.

On pathspec support, the discussion similarly considers whether filtering should happen in frontends (keeping the backend simple) or be optionally handled by the backend (to support simpler frontends). Junio emphasizes that the decision depends on which side of the pipeline the project wants to optimize for - frontend simplicity versus backend predictability.

The email doesn't propose a specific resolution but rather lays out the design space for these pipeline responsibilities, building on earlier decisions about input/output formats and tree object handling. It continues the thread's pattern of carefully considering the command's role in toolchains and maintaining flexibility for future enhancements.