Bugfix discussion for gitk's filename encoding handling, now considering Windows compatibility. Junio C Hamano responds to Konstantin Khomoutov's concerns about the proposed UTF-8 encoding fix, which works on Linux/macOS but may break Windows systems where Tcl expects different default encodings.

The core issue remains displaying Japanese filenames correctly during merge conflicts, but the solution now needs to account for platform differences. Hamano explores whether the encoding should be configurable per-repository (via something like `gitk.pathencoding`) or adjustable by users when dealing with non-standard encodings, rather than forcing UTF-8 system-wide.

This continues the thread about fixing gitk's display of non-ASCII filenames, but now with added complexity around cross-platform support. The patches will likely need revision to handle Windows cases where the system encoding differs from UTF-8 default. The discussion suggests the solution may need to be more flexible than the current approach of hardcoding UTF-8 encoding.