This is a design discussion email in the ongoing `git-diff-pairs` plumbing command development thread. Junio C Hamano responds to Justin Tobler's considerations about input format and behavior for the new command.

The email focuses on several key design decisions for `git-diff-pairs`:
1. Input format debate between NUL-delimited (`-z`) vs traditional raw format, with Junio noting historical reasons for text formats (debuggability) while acknowledging `-z` may be more efficient
2. Object ID handling, where abbreviated OIDs are deemed non-essential for this tool-to-tool interface
3. Pathspec support tradeoffs, discussing where in the pipeline diffcore machinery (like rename detection) should operate
4. Tree object handling behavior, noting current behavior where trees pass through unexpanded
5. Documentation rebase status, confirming the earlier doc/.adoc conversion is now in master

The discussion reveals several implementation constraints being solidified:
- Input will remain strictly `-z` mode with full OIDs
- Pathspec support architecture is preserved but implementation timeline remains open
- Tree expansion strategy needs clearer documentation
- The command maintains its role as an intermediate processor in pipelines

This email represents the maintainer weighing in on architectural decisions while leaving room for further implementation refinement, particularly around pipeline responsibilities and output synchronization. The thread appears to be converging on a design that prioritizes simplicity and explicit contracts for this plumbing command.