This is a response in the ongoing `git-diff-pairs` plumbing command development thread, where Junio C Hamano provides architectural guidance on input format design and pathspec handling.

Junio pushes back on two design decisions from the current implementation. First, he strongly prefers using the conventional `-z` flag for NUL-terminated input rather than a hypothetical future `--input-format` option, suggesting the command should explicitly declare its current NUL-only limitation rather than inventing new syntax. Second, he argues against the current strict rejection of pathspecs, noting that filtering diff output by path is a legitimate use case that should eventually be supported, even if the initial implementation doesn't handle it yet.

The feedback emphasizes maintaining consistency with Git's existing diff architecture, where the proposed `diff-pairs` command effectively implements the backend portion of the diff pipeline. Junio suggests being more permissive with options the command doesn't yet support rather than proactively rejecting them, to leave room for future expansion while keeping the familiar Git diff interface.

This response continues the thread's trend of refining the command's design to align with Git's established patterns while preserving flexibility for future enhancements. The feedback will likely influence the v3 patch series' handling of input formats and option processing.