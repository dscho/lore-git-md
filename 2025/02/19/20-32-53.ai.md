This is part 5/12 of Usman Akinyemi's `the_repository` removal series, focusing on the send-pack builtin. The patch refactors `cmd_send_pack()` to prepare for removing the global repository variable in the next patch.

The key change moves the `git_config()` call after `usage_with_options()` to avoid needing a NULL `repo` check when handling the "-h" help flag. This follows the established pattern seen in earlier patches (verify-tag and verify-commit conversions) where initialization flows are restructured first before replacing `the_repository` with the passed repository parameter. The change is minimal (just 3 lines moved) but enables the subsequent patch to safely switch from `git_config()` to `repo_config()`.

The patch maintains the same careful approach seen throughout the series - preserving behavior while enabling future refactoring, with Christian Couder continuing as mentor. This continues the incremental methodology that has proven effective in previous conversions, now applying it to the send-pack builtin. The change is straightforward and follows the same NULL `repo` handling pattern established for help text in prior patches.