This is a refactoring patch in Jeff King's zlib inflation series, focusing on error handling simplification in `unpack_loose_rest()`. The change eliminates redundant condition checks that were previously the source of a bug (fixed earlier in the series) by restructuring the logic into a cleaner if/else cascade.

The patch modifies the function to first check for error conditions (either incomplete zlib stream or trailing garbage bytes) before falling through to the success case, rather than checking success first and then re-checking the same conditions for reporting. This makes the control flow more straightforward and less prone to inconsistencies between the success and error paths.

The change is small (3 lines modified) but meaningful, continuing the series' theme of hardening and clarifying zlib inflation handling. It builds on earlier fixes in the thread that addressed edge cases in object parsing, though this particular change is purely about code organization rather than fixing specific bugs. The commit message clearly explains the motivation and shows before/after pseudocode to illustrate the improvement.