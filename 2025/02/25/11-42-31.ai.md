This is a minor code style suggestion in Jeff King's zlib inflation series. Patrick Steinhardt points out that the test case for truncated objects with `--allow-unknown-type` would be more idiomatic if it queued the cleanup operation (`test_when_finished`) before performing the potentially destructive `mv` operation. While this doesn't affect functionality, it follows Git's modern convention of setting up cleanup handlers before making changes.

The suggestion relates to patch 04/10 in the series, which fixes infinite loops from broken zlib input by properly handling truncated objects when `OBJECT_INFO_ALLOW_UNKNOWN_TYPE` is used. The test case in question specifically verifies behavior with artificially long type names that exceed the 32-byte header limit, where the object is truncated mid-stream to ensure proper error handling during inflation.

This is a small stylistic improvement to an otherwise technical series focused on hardening zlib handling. The thread's main technical discussion continues around the broader questions of unknown type handling and zlib edge cases, while this email addresses a minor test organization point.