This is a clarification in the ongoing discussion about signed commit support in `git fast-export/import`. 

Elijah Newren expresses confusion about whether the current patch series implements the attestation signatures feature that was previously discussed (where commits would be re-signed during import). Junio Hamano steps in to clarify that the current series is just laying groundwork by handling existing signatures, and that the attestation feature would be a separate follow-up effort.

The exchange confirms that:
1. The v5 series only handles transporting existing signatures (via abort/verbatim/strip modes)
2. The git-filter-repo use case will ultimately need both this functionality and future attestation support
3. The attestation feature remains unimplemented but validated as a useful direction

This is essentially a meta-discussion about the thread's boundaries - making sure everyone agrees what's in scope for the current series versus future work. No new technical changes are proposed here; it's just ensuring alignment between contributors about the roadmap. The core functionality in the v5 series remains production-ready with all issues resolved.