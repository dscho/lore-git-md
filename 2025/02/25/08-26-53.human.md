This is a minor review comment on a refactoring patch in the `git fsck` packed-refs validation series. Patrick Steinhardt suggests a small improvement to the documentation comment for a new `get_worktrees()` variant that avoids reading HEAD information.

The original patch (v5 2/8) introduced a version of `get_worktrees()` that skips reading HEAD, which is useful for ref database consistency checks where HEAD might be corrupted. Patrick proposes slightly rewording the comment to generalize the rationale beyond just refdb checking, mentioning it could also help with functions like `repair_worktrees()`.

The author considers this a nit that doesn't require a reroll of the patch series, which is now complete. This is a small polish suggestion that doesn't affect functionality - just makes the comment more broadly applicable to other potential use cases where avoiding HEAD reads would be beneficial.