Refactoring patch in the git-diff-pairs plumbing command series.

This is the first of three patches in a series that has already undergone significant design discussion and refinement. The patch refactors core diff queue operations to support future needs of the git-diff-pairs command. It splits the existing `diff_addremove()` and `diff_change()` functions into two layers: lower-level queue operations that return the created `diff_filepair` (needed for subsequent patches), and the original functions that maintain backward compatibility.

Key technical changes:
- Introduces new `diff_queue_addremove()` and `diff_queue_change()` functions that return the queued `diff_filepair`
- Preserves existing callback signatures in `diff_addremove()` and `diff_change()` by making them wrappers
- Maintains all existing behavior including submodule handling and prefix filtering
- Adds comprehensive documentation for the new queue functions in diff.h

The changes are mechanical but necessary infrastructure for the git-diff-pairs feature, allowing subsequent patches to access and modify queued file pairs. The patch appears well-constructed with:
- Clear commit message explaining the why behind the refactor
- Preservation of all existing functionality
- New public functions properly documented
- No behavioral changes introduced

This is foundational work that has already incorporated feedback from previous rounds, with the thread summary indicating the overall series is now mature and ready for final review. The changes are limited to diff.c and diff.h, focusing specifically on the queue manipulation infrastructure needed by the broader feature.