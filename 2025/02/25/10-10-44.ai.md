Final patch in a completed test modernization series converting oidtree tests to Clar framework. This concludes the 4-part effort to standardize oid-related test suites (oid-array, oidmap, oidtree) under the unit testing framework.

The patch follows the established pattern from previous conversions in the series:
- Renames test file from t-oidtree.c to u-oidtree.c
- Introduces Clar-style initialization/cleanup handlers (test_oidtree__initialize/cleanup)
- Replaces custom test assertions with Clar variants (cl_assert, cl_failf)
- Uses cl_parse_any_oid() helper for consistent hash algorithm handling
- Splits tests into discrete functions (test_oidtree__contains, test_oidtree__each)
- Maintains all existing test coverage while improving error reporting
- Updates both Makefile and meson.build build systems

Key technical changes specific to oidtree:
- Global oidtree instance (struct oidtree ot) shared across tests
- check_each() now fails fast on first error
- Removes custom test harness in favor of Clar runner
- Preserves all test cases (prefix matching, iteration, containment checks)

The conversion is thorough and consistent with the patterns established in earlier patches of the series. With this final patch, all oid-related test suites have been successfully modernized, marking completion of this standardization effort. The implementation appears technically sound with no outstanding issues.