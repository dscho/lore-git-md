This thread continues to track issues with Git's Meson build system, which started with Windows-specific problems but has expanded to reveal broader fragility in incremental builds. The original issue involved test failures on Windows after merging Patrick Steinhardt's build fixes, where a commit dropped critical Windows linker arguments needed for proper CRLF handling. Jeff King then identified additional problems that occur when switching between branches (like 'next' to 'master') without doing a full rebuild, causing Meson to fail with array comparison errors.

Patrick Steinhardt has now confirmed Jeff's findings about the incremental build issues, tracing them to a backwards-incompatible change in how the `sane_tools_path` option is handled (changed from string to array type). While acknowledging this causes problems during branch switches, Patrick considers it acceptable during Meson's experimental phase, with a commitment to stabilize the build system after Git v2.49's release. The discussion highlights the tension between evolving the build system and maintaining a smooth developer workflow, particularly for bisection work - though ccache helps, it doesn't fully solve the underlying fragility.

The thread now encompasses both the original linker flag issue (still pending verification) and these new build system maturity concerns. Key participants include Patrick Steinhardt (diagnosing and fixing issues), Jeff King (who identified the incremental build problems), and Junio Hamano (who initially bisected the regression). The discussion remains technical but has broadened from Windows-specific focus to consider the Meson build system's general reliability as Git moves toward making it the default build method.