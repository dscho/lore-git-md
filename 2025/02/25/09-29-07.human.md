Refactoring patch in the reftable backend for partial reference updates. This is part 4/7 of Karthik Nayak's series introducing `--allow-partial` support for `git update-ref`.

The patch extracts the core reference update preparation logic from `reftable_be_transaction_prepare()` into a new `prepare_single_update()` function. This centralizes all validation and preparation steps for individual reference updates, including object ID verification, HEAD reference handling, and symref processing. The change is purely organizational at this stage - no behavior is modified - but it sets up the infrastructure needed for the partial transaction support coming in subsequent patches.

The refactoring improves code maintainability by consolidating what was previously scattered validation logic into a single logical block. The commit message clearly explains this is groundwork for the partial transaction functionality, with the actual feature implementation following in later commits. The diff shows significant code movement (237 lines added, 226 removed) with careful preservation of all existing behavior.

This continues the series that has already seen positive review feedback, with the v2 iteration incorporating suggestions around error handling and documentation. The patch appears well-constructed and focused on its stated goal of preparing the codebase for the partial update feature.