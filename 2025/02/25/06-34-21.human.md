This is the 10th patch in Jeff King's 12-part series hardening Git's zlib inflation handling against edge cases. It's a refactoring change focused specifically on improving code clarity in `unpack_loose_rest()`.

The patch simplifies the control flow by making error handling more explicit. Previously, the function had a subtle pattern where successful returns bypassed cleanup code, while errors fell through to shared cleanup block. The new version handles cleanup directly in each error path, making the logic more straightforward at the cost of some minor duplication.

The change doesn't modify behavior - it's purely about making the code easier to understand and maintain. As Jeff notes, this could have been squashed into the previous patch, but keeping it separate helps reviewers follow the incremental improvements. The series continues to demonstrate careful attention to error handling patterns in Git's object reading code.