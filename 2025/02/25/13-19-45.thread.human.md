This thread continues the discussion of Scott Chacon's performance optimization for bundle-uri reference negotiation during fetches. The v3 patch series expands reference handling to all `refs/` namespaces while preserving hierarchy, showing significant performance improvements (reducing object downloads by 32% to 1%). The implementation in `bundle-uri.c` is complete with comprehensive test coverage, though documentation updates remain pending.

The latest exchange focuses on refining test methodology. Junio Hamano has weighed in on Phillip Wood's earlier feedback about the test implementation, agreeing that replacing `for-each-ref | grep` patterns with direct `git show-ref` checks would be more efficient. More importantly, Hamano proposes an additional test scenario to verify the interaction between bundle-uri tags and normal fetch behavior, particularly for annotated tags. This would ensure tags properly propagate from `refs/bundles/tags/` to `refs/tags/` only after being fetched normally, not immediately after cloning.

All substantive implementation concerns have been addressed, and the series appears technically sound. The remaining work consists of: (1) updating documentation to clarify the new reference hierarchy behavior and resolve discrepancies about configurable refspecs, and (2) implementing the test improvements suggested by Phillip Wood and now expanded by Hamano. With these final refinements, the series appears ready for merging.