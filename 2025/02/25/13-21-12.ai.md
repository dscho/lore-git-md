Test modernization patch in the `git fsck` packed-refs validation series.

This is the first patch in v6 series that enhances `git fsck`'s validation of packed-refs files. The patch focuses on improving test hygiene in t0602-reffiles-fsck.sh by wrapping test operations in subshells to ensure proper working directory restoration. The change addresses a potential issue where test failures between `cd repo` and `cd ..` could leave the working directory in an inconsistent state.

The patch makes extensive mechanical change to the test file, converting all test blocks to use subshells for directory changes. This affects nearly every test case in the file, with 967 lines modified (494 insertions, 473 deletions). The changes are straightforward - each test block that previously did direct cd operations is now wrapped in `( )` to create an isolated subshell environment.

Key technical details:
- Maintains all existing test assertions and behaviors
- Preserves error message expectations and test logic
- Handles both single-level and nested directory structures
- Works with worktree-related tests
- Maintains SYMLINKS test prerequisites where applicable

The patch is part of a larger series that has already gone through multiple review iterations (now at v6) with all prior feedback addressed. This particular change was likely suggested during review to improve test reliability, though the commit message doesn't specify the review history for this specific patch.