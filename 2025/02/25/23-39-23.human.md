This is a refactoring patch that's part of the `git-diff-pairs(1)` plumbing command series. It prepares the diff queue infrastructure for upcoming changes by restructuring how file pairs are added to the diff queue.

The patch splits the existing `diff_addremove()` and `diff_change()` functions into two variants each: one that maintains the current void return behavior (for backward compatibility with callback interfaces), and new `diff_queue_addremove()`/`diff_queue_change()` functions that return the queued `diff_filepair`. This change enables future patches to modify file pairs after they've been queued, which will be needed for the batch diff functionality.

The implementation is clean and maintains all existing behavior while adding the new capability. The changes are well-contained to diff.c and diff.h, with comprehensive updates to both function implementations and their documentation. This appears to be a straightforward preparatory change that follows the established pattern of the diff machinery.

As part 1/3 in the series, this sets up the infrastructure needed for the core `git-diff-pairs` command to be introduced in subsequent patches. The thread context shows this series has already gone through several rounds of review and refinement, with this particular patch now addressing all previous feedback.