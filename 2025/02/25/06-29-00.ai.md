Refactoring patch in a zlib inflation improvement series. This is patch 2/10 in a series addressing zlib handling issues in Git's object reading code. The patch simplifies the `next_out` pointer management in `unpack_loose_header()` by moving the zstream buffer reset inside the inflation loop rather than after it.

The change provides three benefits: (1) eliminates duplicate code for resetting the output buffer, (2) makes it obvious the buffer is always fresh on each call (preventing potential Z_BUF_ERROR confusion), and (3) leaves the stream state in a more consistent position after completion. While not strictly necessary for correctness, the author notes this refactoring will make reasoning about subsequent fixes (particularly in patch 4) easier.

The modification touches only `object-file.c`, moving the buffer reset operations inside the inflation loop and removing redundant post-loop resets. This appears to be a preparatory cleanup before more substantive zlib handling fixes later in the series. The change is small (4 lines removed, 3 added) and focused purely on code structure without altering behavior.