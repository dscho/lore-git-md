This is a historical context email in the `show-branch` refactoring thread, providing additional background on the `commit_slab_with_stride` infrastructure being discussed.

Jeff King adds depth to the historical discussion by pointing to his 2014 work that first explored using the slab stride mechanism for bit storage in `paint_down_to_common()`. He references two specific series from June 2014 that demonstrate early implementations of this approach: one for fast `--contains` traversal and another introducing a "bitset" API for arbitrary-sized bit storage. 

The email clarifies that while parts of this early work were merged (like converting to `prio_queue`), the slab stride portions were left incomplete due to unresolved edge cases. King notes that subsequent work by Stolee and others may have made some of the 2014 approaches obsolete, but suggests the bitset API design might still be relevant for the current `show-branch` effort.

This provides important context about why the `_with_stride` infrastructure remained unused despite being designed for this exact use case. The email also surfaces potential prior art (the bitset API) that could inform the current implementation, while acknowledging the evolution of the codebase since 2014 might require fresh approaches. The tone suggests this is background information rather than direct implementation advice.