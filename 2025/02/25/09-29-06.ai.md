This is a refactoring patch in the ongoing partial reference updates series, specifically targeting duplicate reference name checking in the files backend. 

The patch removes redundant duplicate reference checking in the files backend's transaction finish phase, since this verification is already performed during the prepare stage. The refnames list remains unchanged between these phases, making the second check unnecessary. The change involves:

1. Removing the duplicate check from files-backend.c
2. Making the `ref_update_reject_duplicates()` function static since it's now only used within refs.c
3. Moving its documentation comment to the function definition in refs.c

This change follows the series' pattern of centralizing common reference transaction logic in the generic refs layer rather than duplicating it across backends. The patch is a straightforward cleanup that eliminates 6 lines of code while maintaining the same safety guarantees, since the prepare-phase check remains in place. The commit message clearly explains the rationale and the safety considerations.

The change is part of the larger effort to support partial reference updates, but focuses specifically on code organization and removing redundancy. It demonstrates good attention to detail in the refactoring process, ensuring safety while simplifying the codebase. The patch appears well-contained and low-risk, with no behavior changes beyond removing an unnecessary check.