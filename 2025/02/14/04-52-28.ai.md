Bugfix patch adding packed-refs filetype validation to `git refs verify`. This is part 3/8 of a series improving packed-refs validation, now focusing on ensuring the packed-refs file is a regular file rather than a symlink or other special file type.

The patch introduces a check using `open_nofollow()` to verify the packed-refs file is a regular file, reporting an error via the existing `FSCK_MSG_BAD_REF_FILETYPE` message if it's not. The implementation handles edge cases like missing files (treated as valid) and symlinks (explicitly rejected). A new test case verifies the behavior by creating a symlink to simulate the invalid condition.

Key technical details:
- Adds validation in `packed_fsck()` function in `refs/packed-backend.c`
- Uses `open_nofollow()` to safely check file type
- Reuses existing fsck message ID rather than introducing new one
- Includes comprehensive test in `t/t0602-reffiles-fsck.sh`
- Maintains backward compatibility with missing packed-refs files

The change builds on the series' goal of thorough packed-refs validation while keeping the implementation focused and maintainable. The patch appears well-contained with clear error handling and appropriate test coverage.