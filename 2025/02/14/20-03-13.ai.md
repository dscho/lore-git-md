This is a continuation of Josef Wolf's ongoing investigation into Git's renormalization behavior with smudge/clean filters, now exploring git-filter-repo as an alternative approach after previous methods failed.

Josef reports attempting to use git-filter-repo to handle the problematic *.ets5hash files, first by pruning them and then attempting to renormalize the remaining content. While the initial renormalization process appeared successful, further attempts to modify history (specifically moving .gitattributes to the beginning of history) again resulted in line ending conflicts. The conflicts persisted despite using both --attr-source and the renormalize-helper script, leading Josef to conclude that some commits still contain unclean content.

The email reveals growing frustration with the fundamental issue: Git's diff/merge operations appear to bypass clean filters when processing content during history rewriting. Josef proposes (with emphasized frustration) that Git should have an option to always apply clean filters before feeding content to merge or diff operations. This suggests a potential architectural limitation in how Git handles filtered content during complex history rewrites.

Key technical details for future reference:
- The workflow involves git-filter-repo with --prune-empty and --invert-paths
- Uses a renormalize-helper script that combines git add --renormalize with commit amendment
- Attempts to rebase with --root -X renormalize still fail
- The --attr-source parameter doesn't resolve the underlying issue
- Core problem appears to be clean filters not being applied during merge/diff operations
- Specific files involved are *.ets5hash with CRLF requirements

This represents another dead end in the investigation, moving the focus from attribute processing and file collisions back to the fundamental behavior of clean filters during history rewriting operations. The thread continues to expose complex interactions between Git's filtering system and its history manipulation commands.