Maintainer follow-up on documentation build system fix. Junio Hamano responds to Adam Dinwoodie's patch addressing the documentation build regression, agreeing with the functional goal (restoring complete documentation builds by default) but suggesting an implementation refinement. He proposes using an empty double-colon `all::` rule at the file's start - a project convention seen in t/Makefile and the main Makefile - rather than just moving the target definition. This makes the default target explicit and prevents similar regressions. The email includes a concrete diff showing both the new empty rule and the conversion of the existing `all:` to `all::`. 

The response confirms this is the only instance needing correction in the Documentation/Makefile. The technical impact remains focused on local documentation builds, now with improved maintainability through consistent rule syntax. The thread appears ready for Adam to submit a revised patch incorporating this feedback, as there are no outstanding technical disagreements - just a style preference aligning with established project patterns for phony targets in Makefiles.