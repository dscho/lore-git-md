Here's the updated dense AI thread summary for this thread:

RFC patch proposing From header support in `git show --format=email` when format.from/format.forceInBodyFrom are configured, with discussion shifting to fundamental design questions. Original author Aditya Garg questioned whether the expectation was correct given potential redundancy with struct format_config. Junio Hamano has now responded, challenging the premise by suggesting `format.*` configurations were specifically designed for `git format-patch` rather than `git show`/`git log`.

Key technical details:
- Proposed implementation adds `from` member to struct log_config
- Reads format.from/format.forceinbodyfrom configs
- Populates rev.from_ident via split_ident_line()
- Affects builtin/log.c (21 lines added)
- Output affected: email format in `git show`

Current status:
- Junio expresses skepticism about need for change ("nothing that needs fixing")
- Suggests `format.*` configs are intentionally scoped to `format-patch`
- Leaves room for being convinced otherwise ("may be missing something")

Open questions now expanded:
1. Fundamental design: Should `format.from` affect `git show` output at all?
2. Command scope: Are email formatting configs meant to be `format-patch`-specific?
3. If proceeding, is the proposed struct log_config approach correct?
4. Is there redundancy with existing format_config infrastructure?

Participant positions:
- Aditya Garg (original author): Proposed implementation as RFC
- Junio Hamano: Questions fundamental premise, leans against change
- Discussion shifted from implementation to design philosophy
- Next steps depend on whether use cases can justify expanding config scope

Key distinction: This is no longer just about the code change but about whether the change aligns with Git's design for email formatting configuration scoping. The thread awaits either concrete use cases justifying the change or consensus that current behavior is correct.