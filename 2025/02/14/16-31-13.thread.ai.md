Here's the dense AI thread summary for this new thread:

RFC patch proposing to add From header support in `git show --format=email` when format.from and format.forceInBodyFrom are configured. Currently this doesn't work despite existing log-tree.c infrastructure appearing designed for it. Author (Aditya Garg) questions whether the expectation of seeing From fields in this context is correct given potential redundancy with struct format_config. 

Implementation adds `from` member to struct log_config and reads user configuration to populate rev.from_ident, reusing existing force_in_body_from variable. Changes affect builtin/log.c (21 lines added) to handle format.from/format.forceinbodyfrom configs and populate rev.from_ident via split_ident_line(). Marked as RFC seeking feedback on both behavioral expectation and implementation approach. Signed-off-by present but explicitly flagged as exploratory.

Key technical details:
- New struct log_config member: `from`
- Configs processed: format.from, format.forceinbodyfrom
- Fields populated: rev.from_ident, rev.force_in_body_from
- Output affected: email format in `git show`
- Implementation uncertainty: potential redundancy with format_config

Open questions:
- Is From header in `git show --format=email` expected behavior?
- Is this the right implementation approach given existing format_config?
- Should the existing log-tree.c infrastructure be used differently?