Here's the updated dense AI thread summary for this thread:

RFC patch proposing From header support in `git show --format=email` when format.from/format.forceInBodyFrom are configured, with discussion evolving through several phases. Original author Aditya Garg questioned implementation approach given potential redundancy with struct format_config. Junio Hamano challenged the premise, suggesting `format.*` configurations were designed specifically for `git format-patch`. Antonin Godard has now provided concrete evidence of real-world impact and existing precedent.

Key technical details:
- Proposed implementation adds `from` member to struct log_config
- Reads format.from/format.forceinbodyfrom configs
- Populates rev.from_ident via split_ident_line()
- Affects builtin/log.c (21 lines added)
- Output affected: email format in `git show`
- `git_log_config()` already processes other `format.*` variables for email output
- Current behavior breaks b4 tool expectations

Current status:
- Junio initially skeptical ("nothing that needs fixing")
- Antonin demonstrated existing `format.*` usage in `git show`
- Identified concrete b4 integration pain point
- Discussion shifted from theoretical to practical considerations

Open questions now focused:
1. Should `format.from` consistency across commands outweigh original `format-patch` design intent?
2. How to resolve the b4 tool compatibility issue?
3. Is the current partial `format.*` support in `git show` an oversight or intentional?
4. If proceeding, is struct log_config approach still preferred given existing format_config usage?

Participant positions:
- Aditya Garg: Original RFC implementation
- Junio Hamano: Initially opposed, pending justification
- Antonin Godard: Provided use case and code precedent
- Discussion now has concrete stakes with b4 impact

Key developments:
- Proof that `git show` already honors some `format.*` configs
- Identification of b4 as affected tool
- Shift from abstract design debate to practical compatibility concern
- Decision now balances design purity against real-world tool integration

The thread awaits either:
- Consensus that b4's needs justify expanding config scope
- Or a solution that maintains `format-patch` specificity while addressing tool needs
- Potential middle path: document current behavior as intentional limitation

Related work:
- b4 tool's reliance on `git show --format=email`
- Existing partial `format.*` support in log config
- Historical design decisions around email formatting commands