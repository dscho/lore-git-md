This is a technical suggestion in an ongoing thread about CRLF conversion and filter behavior. Brian M. Carlson responds to Josef Wolf's struggles with CRLF enforcement via clean filters by proposing an alternative approach using Git's built-in `eol=crlf` attribute instead of custom filters. The suggestion comes with a concrete example of how to configure this in `.gitattributes` (`*.bat text eol=crlf`) and notes this would maintain CRLF in the working tree across all platforms while storing LF in the repository. Carlson also mentions the `working-tree-encoding` option for cases needing specific encodings, while pragmatically dismissing edge cases where users might override these settings locally.

The email represents a potentially simpler solution to part of the complex filter/attribute issues being discussed in the thread, though it doesn't address the deeper architectural limitations around filter application during merge/diff operations that were recently uncovered. The suggestion focuses specifically on the CRLF conversion aspect rather than the broader filter behavior problems, offering a standard Git feature as an alternative to custom filtering for this particular use case.