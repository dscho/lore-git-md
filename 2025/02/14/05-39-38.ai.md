This is a refactoring patch that improves clarity in the refspec matching code. The patch renames `match_name_with_pattern()` to `match_refname_with_pattern()` to better reflect its purpose of matching refnames against refspec patterns. It also updates parameter names to be more descriptive: `key` becomes `src_pattern`, `name` becomes `refname`, and `value` becomes `dst_pattern`. The documentation comments are improved to clarify that this function handles globbing patterns for refspec matching and optionally computes destination refnames.

The changes affect multiple files (refspec.c, refspec.h, remote.c) where this function is used, updating all call sites to use the new names. The error messages are also updated to use the new terminology ("source pattern" and "destination pattern" instead of "key" and "value"). The patch appears to be a straightforward renaming with no behavior changes, following up on a previous discussion where Junio Hamano suggested this improvement.

Key technical details for future reference:
- Function renamed from `match_name_with_pattern` to `match_refname_with_pattern`
- Parameters renamed to clarify their roles in refspec matching
- Documentation updated to better explain the globbing pattern matching behavior
- Changes propagated through refspec.c, refspec.h, and remote.c
- Error messages updated to use consistent terminology
- References a previous discussion in the lore archive about this change