Git email digest: This is a technical discussion about line ending handling and XML file processing in Git filters, continuing an ongoing thread about CRLF conversion and history rewriting.

Josef Wolf responds to Brian M. Carlson's suggestion to use Git's built-in `eol=crlf` attribute instead of custom clean/smudge filters. He explains that his use case requires more complex processing than simple line ending conversion - his XML files contain mixed CRLF and LF line endings, and the application randomly shuffles XML elements on startup. To prevent conflicts from this shuffling, his clean filter needs to both enforce CRLF line endings and sort XML elements into a canonical order.

The response highlights that while the standard `eol=crlf` approach could handle the line ending aspect, it doesn't address the XML element ordering requirement. This maintains the need for custom filters in this specific case, though the thread now has both approaches (standard attributes vs custom filters) documented for different scenarios. The discussion continues to explore the complex interactions between Git's filter system, line ending handling, and content normalization requirements.