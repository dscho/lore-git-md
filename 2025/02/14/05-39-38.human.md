This is a refactoring patch that improves clarity in the refspec matching code. Meet Soni proposes renaming `match_name_with_pattern()` to `match_refname_with_pattern()` to better reflect its purpose of matching reference names against glob patterns. The patch also updates parameter names to be more descriptive (`key`→`src_pattern`, `name`→`refname`, `value`→`dst_pattern`) and improves the function's documentation.

The changes are straightforward and mechanical, touching 3 files with 24 deletions and 25 additions. The author notes this change was previously discussed in an earlier thread where Junio Hamano suggested waiting to make this update. The patch appears well-scoped and uncontroversial, focusing purely on naming improvements without changing any behavior. The commit message clearly explains the motivation (inconsistent naming making the code harder to understand) and the solution.