This is the final patch in a bugfix series integrating comprehensive packed-refs validation into `git fsck`. The patch adds a new `--[no-]references` option to `git fsck` that controls whether to check reference database consistency via a new `git refs verify` child process.

The implementation creates a new `fsck_refs()` function that runs `git refs verify` as a child process, with progress reporting and proper error handling. The check is enabled by default but can be disabled with `--no-references`. The patch includes thorough tests verifying the option works as expected, including cases with malformed refs containing trailing garbage.

This completes the series' goal of bringing packed-refs validation into `git fsck`, building on previous patches that implemented various validation checks. The approach maintains backward compatibility while providing users with control over the reference checking behavior. The implementation appears clean and well-tested, with clear progress reporting and proper integration into fsck's error reporting system.

The patch follows the series' pattern of careful, incremental changes with good test coverage. It addresses all feedback from previous iterations and represents the final piece of this comprehensive ref validation effort.