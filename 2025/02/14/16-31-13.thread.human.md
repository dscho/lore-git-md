Here's a human-readable summary of this thread:

The thread begins with an RFC patch proposing to add From header support in `git show --format=email` when the format.from and format.forceInBodyFrom configurations are set. The author notes that while there appears to be existing infrastructure in log-tree.c to handle this, it currently doesn't work as expected.

The patch introduces a `from` member to struct log_config and reads user configuration to populate rev.from_ident, reusing the existing force_in_body_from variable. The author explicitly marks this as an RFC because they're uncertain about both the behavioral expectation (whether showing the From field in this context makes sense given potential redundancy with struct format_config) and the implementation approach.

The change affects builtin/log.c, adding configuration handling for these email format options. The patch is signed-off but presented as a request for feedback rather than a final solution, with the author seeking input on both the concept and technical approach before proceeding further.