Here's the human-readable thread summary:

The thread began with an RFC patch from Aditya Garg proposing to add From header support in `git show --format=email` when format.from and format.forceInBodyFrom are configured. The patch noted that while the log-tree.c infrastructure seems designed to support this, it currently doesn't work. The implementation would add a `from` member to struct log_config and read user configuration to populate rev.from_ident.

Junio Hamano has now responded, questioning the fundamental premise of the change. He suggests that the `format.*` configurations were specifically designed for `git format-patch` (used when emailing patches) rather than for `git show` or `git log`. While leaving room to be convinced otherwise ("I may be missing something"), Junio expresses skepticism that there's actually anything to fix here, implying the current behavior may be correct by design.

The discussion has now shifted from implementation details to a more fundamental question about whether email formatting configuration should apply to `git show` output at all. The key open question is whether there are legitimate use cases for having `format.from` affect `git show`, or if this would inappropriately blur the line between patch generation and commit display functionality.