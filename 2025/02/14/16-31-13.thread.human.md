Here's the updated human-readable thread summary:

The thread explores whether `git show --format=email` should respect the `format.from` configuration setting, which affects email headers. What began as a technical RFC has evolved into a deeper discussion about configuration scope in Git's email formatting commands.

Aditya Garg originally proposed adding `format.from` support to `git show`, noting it would help tools like b4 that rely on this command for email output. Junio Hamano questioned the change, suggesting these configurations were intentionally designed only for `git format-patch`. The discussion then shifted from implementation details to fundamental design philosophy.

Antonin Godard's recent response adds important context: the code already applies some `format.*` configurations to `git show` (like subject prefixes), creating an inconsistency. More importantly, he highlights a concrete problem - tools like b4 currently can't properly format emails from `git show` because they expect `format.from` to work consistently across commands. This real-world impact strengthens the case for change.

The core questions now are:
1. Should email formatting configurations apply uniformly to all relevant commands, or remain `format-patch`-specific as Junio suggested?
2. Given that some `format.*` settings already affect `git show`, is there a principled reason to exclude `format.from`?
3. Does the b4 integration use case justify expanding the configuration scope?

Junio remains skeptical but hasn't closed the door entirely. The thread now awaits either a compelling counterargument or additional examples demonstrating why consistent behavior matters for real workflows. The outcome will likely hinge on whether the practical benefits outweigh any philosophical objections to broadening the configuration scope.