Here's the updated human-readable summary:

The thread explores whether `git show --format=email` should honor the same `format.from` configuration settings that `git format-patch` uses. Aditya Garg originally proposed adding this support, noting the inconsistency in how these related commands handle formatting. Junio Hamano initially pushed back, arguing these settings were specifically designed for `format-patch`.

The discussion took a practical turn when Antonin Godard demonstrated that `git show` already uses some `format.*` configurations through shared code paths, and identified the b4 tool as being affected by the current behavior. This shifted the conversation from abstract design principles to concrete tool integration concerns.

In the latest response, Junio provides historical context, citing 2022 commits that introduced these features specifically for `format-patch`. He acknowledges the theoretical merit of consistency across commands but raises new concerns about backwards compatibility if behavior changes. While maintaining his position, his tone suggests openness to solutions that address the b4 tool's needs without breaking existing uses.

The core tension now is between honoring the original design intent (keeping these configurations specific to `format-patch`) versus meeting real-world tooling needs and maintaining consistency where some `format.*` settings already work in `git show`. The path forward likely requires either demonstrating minimal compatibility impact or finding an alternative solution that preserves existing behavior while fixing b4's use case.