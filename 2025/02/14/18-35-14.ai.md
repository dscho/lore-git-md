Thread continuation in the tag fetching regression discussion, with Junio Hamano weighing in on Taylor Blau's proposed fix. This is a protocol v2 fetch behavior issue where tag decoration breaks in shallow fetch scenarios due to interaction between remote/HEAD handling and ref_prefixes logic.

Junio analyzes Taylor's suggested change to unconditionally add "refs/tags/" to ref_prefixes when tags are requested, noting it would alter the fundamental assumption that no prefixes means "get all refs". He identifies the deeper architectural issue - the current incremental strvec_push() approach for prefixes is brittle as more use cases (first tags, now HEAD) get added. The patch might work but could have cascading effects on other code paths that rely on the empty-prefix-means-all behavior.

Key technical observations:
- Current behavior assumes ref_prefixes starts empty unless tags are needed
- Adding HEAD handling broke this assumption
- Taylor's fix would make tags always add a prefix, changing fundamental protocol v2 semantics
- Future additions (beyond tags and HEAD) would likely expose similar fragility

Junio suggests the real solution may require restructuring how ref prefixes are determined upfront rather than incrementally modifying them. The discussion remains open for protocol v2 experts to weigh in on balancing optimization (hash-only fetches) with correct tag decoration behavior.