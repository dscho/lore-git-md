This is a discussion about progress reporting in the bundle-uri feature, specifically around error handling when downloading bundles via HTTPS. Toon Claes is reviving a patch that would enable progress reporting from `git-remote-https` but is struggling with how to handle error messages that would now be visible to users.

The core issue is that enabling stderr for progress reporting also exposes fatal error messages from the child process (previously suppressed). Jeff King had suggested two potential solutions: having the child process send machine-readable progress over stdout, or somehow suppressing non-progress errors. Toon explores these options but finds challenges with each approach.

The discussion is technical and focused on implementation details of inter-process communication between Git and its remote-https helper. Toon is seeking clarification on Jeff's suggestions and proposes some possible message formats for progress reporting. The thread shows active problem-solving around a tricky technical challenge in Git's bundle URI implementation.