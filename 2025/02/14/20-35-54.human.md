This is a follow-up discussion about a proposed feature to add From header support in `git show --format=email`. 

Junio C Hamano questions whether the original patch's premise is correct, pointing out that the `format.*` configuration variables are typically associated with `git format-patch` rather than `git show` or `git log`. He expresses skepticism that there's actually a bug to fix here, though leaves open the possibility he might be missing something.

The exchange highlights a fundamental question about whether email-style output from `git show` should respect the same configuration options as `git format-patch`. Junio's response suggests the original proposal may be based on a misunderstanding of how these configuration variables are meant to be used across different Git commands. The thread appears to be at an early stage where the basic premise of the change is being challenged rather than implementation details being discussed.