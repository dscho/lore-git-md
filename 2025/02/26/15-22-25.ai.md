This is a follow-up discussion in the Git cat-file batch mode filtering enhancement series, specifically about the command-line interface naming for object filters. 

Toon Claes responds to Patrick Steinhardt's patch implementing "blob:none" filter support, pointing out that if the series ultimately adopts `--object-filter` rather than `--filter` as the option name (as discussed earlier in the thread), the documentation would need to be updated accordingly. The comment is brief but highlights an important user-facing consistency issue that needs to align with whatever naming convention the maintainers decide on.

The email doesn't introduce new technical content but keeps the naming discussion active, reminding that documentation must match the final chosen option name. This is part of the ongoing debate between `--objects-filter` (advocated by Toon for consistency with `git rev-list`) versus `--filter=` (used in Patrick's current implementation). The thread context shows this is a secondary consideration while the series remains blocked on bitmap infrastructure questions.