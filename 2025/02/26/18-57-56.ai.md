This is a follow-up in the thread about `git rev-list --no-walk=sorted` commit ordering with identical timestamps. D. Ben Knoble responds to Clement Moyroud's bug report by asking whether `--topo-order` could address the issue. The question suggests exploring an existing flag as a potential solution before considering deeper changes to the sorting behavior. 

The core issue remains unchanged: when commits share timestamps, `--no-walk=sorted` preserves input order rather than applying topological sorting, which breaks automated cherry-picking workflows. Knoble's response focuses the discussion on whether the existing topological sorting capability (via `--topo-order`) could satisfy the use case without modifying the `--no-walk` behavior. 

Key technical points: the thread now explicitly contrasts `--no-walk=sorted` (date-based) with `--topo-order` (topology-based) as potential solutions. The workflow requirement remains stable topological ordering for customer-specific patch builds. No new reproduction steps or system details are added in this response.