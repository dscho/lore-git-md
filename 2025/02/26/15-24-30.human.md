Refactoring patch in a series adding reflog expiration to git-maintenance.

This is the fifth patch in a six-part series that will eventually add a new maintenance task for reflog expiration. The patch extracts the reflog expiration logic from `cmd_gc()` into a new `maintenance_task_reflog_expire()` function, making it reusable for the upcoming maintenance task. The change is straightforward - it moves the existing child process setup and execution into a dedicated function while maintaining the same behavior.

The patch continues the series' clean separation of concerns, with previous patches having refactored the reflog expiration internals to remove global state. This change is a logical next step that prepares for sharing the implementation between `git-gc` and the new maintenance task. The diff shows careful attention to detail, properly handling error cases and maintaining the existing functionality while making it more modular.

This is a well-contained change that builds on the solid foundation established by earlier patches in the series. The commit message clearly explains the motivation and the change itself is minimal and focused.