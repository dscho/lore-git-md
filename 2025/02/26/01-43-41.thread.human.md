Here's a human-readable thread summary:

The thread discusses a bug in `git rev-list` where commits with identical timestamps aren't properly ordered when using `--no-walk=sorted`. The reporter found that when dates collide, the command preserves input order rather than falling back to topological relationships, which breaks their automated cherry-picking workflow for creating customer-specific patch builds.

The issue specifically appears when piping commits through stdin with `--no-walk=sorted` - the current implementation only considers commit dates without any secondary sorting criteria. The reporter provided a clear reproduction case but hasn't proposed a code fix, instead asking whether better sorting approaches might exist and noting the lack of a pure topological sorting option in this mode.

This appears to be a genuine limitation in `git rev-list`'s sorting logic rather than a misunderstanding of expected behavior. The problem affects real-world workflows that depend on stable topological ordering for automated operations. No solutions or workarounds have been suggested yet in the thread.