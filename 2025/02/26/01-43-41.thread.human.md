Here's the updated human-readable thread summary:

This thread examines a bug report about `git rev-list --no-walk=sorted` behavior when processing commits with identical timestamps. Clement Moyroud originally reported that the command preserves input order rather than maintaining topological ordering in these cases, which breaks automated cherry-picking workflows. The discussion initially explored whether existing flags like `--topo-order` could solve the problem, but Clement demonstrated that it processes the full commit graph rather than just the stdin input set, making it unsuitable.

Phillip Wood has now clarified a technical detail in the reproduction case, pointing out that the correct invocation should include `--no-walk` with `--topo-order`. This combination might behave differently by limiting processing to just the input commits while preserving their topology. However, it's still unclear whether this would fully address the timestamp collision issue at the heart of the workflow problem.

The core challenge remains: Git currently lacks a way to topologically sort only a specified subset of commits (particularly those with identical timestamps) without processing the entire commit graph. The thread has effectively ruled out simple solutions using existing flags, suggesting the need for either modified behavior or a new flag. Participants are now focused on precisely understanding the limitations of current functionality before proposing changes.