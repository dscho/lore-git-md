This is a bugfix patch in Jeff King's zlib inflation series, addressing an infinite loop in `unpack_loose_header()` when processing malformed zlib input. The fix changes the loop condition from checking for `Z_STREAM_END` to continuing while the status is `Z_OK`, which properly handles error cases that could previously cause Git to hang.

Junio Hamano notes this follows a familiar pattern in zlib error handling - several similar bugs in Git's history were fixed by checking for `Z_OK` rather than specific completion states. The change is small but important, preventing a potential denial-of-service vector where specially crafted objects could cause Git to enter an infinite loop during object parsing.

The patch is part of a larger effort to harden Git's zlib handling against edge cases, with this particular fix targeting the loose object header parsing path. The thread discussion indicates this is a well-understood class of bug with an established solution pattern, suggesting the change is low-risk and uncontroversial.