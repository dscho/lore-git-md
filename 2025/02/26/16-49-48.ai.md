Junio Hamano continues the discussion on Phillip Wood's patch to change `git add -p` hunk splitting behavior. This email focuses on workflow implications of making split sub-hunks default to "undecided" rather than inheriting their parent's selection state.

Junio suggests an alternative approach to address the interface inconsistency Phillip identified: instead of marking all split hunks as undecided, the interface could skip already-selected sub-hunks and jump directly to the first unselected one during prompting. This would maintain the power-user workflow where splitting a selected hunk keeps sub-hunks selected by default, while avoiding the confusing prompt for already-decided hunks.

The email also surfaces a broader UX concern about `add -p`'s inability to revisit decisions after all hunks are processed, though Junio acknowledges this is orthogonal to the current patch. The core discussion remains focused on balancing:
1. Interface consistency (current behavior prompts for already-selected hunks)
2. Workflow efficiency (current behavior allows quick deselection of parts of split hunks)
3. Discoverability (proposed change makes state transitions more explicit)

The technical context remains in `add-patch.c`'s hunk state handling, with the interaction between splitting operations and selection inheritance. No new code changes are proposed in this email - it's refining the design discussion around possible solutions to the UX inconsistency Phillip identified.