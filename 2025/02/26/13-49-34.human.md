Test modernization patch in a series improving `git fsck` packed-refs validation.

This first patch in the v7 series focuses on improving test reliability by wrapping test operations in subshells to ensure proper working directory cleanup. The change affects `t/t0602-reffiles-fsck.sh`, converting all test blocks that change directories to use subshells, which automatically restore the original directory when the subshell exits. This prevents test pollution if a test fails mid-execution.

The patch is straightforward - it wraps existing test code in `( ... )` subshell blocks wherever there were explicit `cd` operations. This maintains all the same assertions and test logic while making the tests more robust. The change is mechanical but important for test reliability, especially as this series adds more complex validation logic.

As part of a mature series (now at v7) that has incorporated extensive review feedback, this foundational test improvement helps ensure the subsequent validation patches can be properly verified. The patch follows good practices with a clear commit message explaining the motivation and maintains all existing test coverage while making the tests more robust.