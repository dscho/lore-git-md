This is a follow-up in the thread about `git rev-list --no-walk=sorted`'s behavior with commits sharing identical timestamps. D. Ben Knoble responds to Clement Moyroud's bug report by asking whether `--topo-order` might solve the issue, suggesting a possible alternative approach to the problem of maintaining topological ordering when commit dates collide.

The exchange highlights a real limitation in Git's commit sorting capabilities - while `--no-walk=sorted` handles date-based ordering, it lacks a pure topological sort option for stdin input. The original reporter's use case involves automated cherry-picking workflows where correct commit sequence is essential, particularly for customer-specific patch builds.

The thread remains in the discussion phase, with no patch proposed yet. The question about `--topo-order` indicates the community is exploring whether existing options might address the need before considering changes to the sorting implementation. The technical focus remains on how Git handles commit ordering when primary sorting criteria (timestamps) are identical.