This is a discussion response in the thread about adding a reflog-expire maintenance task. Ramsay Jones provides a contrasting perspective to Patrick Steinhardt's motivation for the patch series, noting they personally rely heavily on `git gc` rather than the newer `git maintenance` system. The response serves as a reality check that while the maintenance system is being developed as a more flexible replacement for `git gc`, there remains significant existing usage of the traditional `git gc` approach. The comment doesn't critique the technical implementation of the patches but rather questions the underlying assumption that moving functionality from `gc` to `maintenance` is universally desired. This may prompt discussion about whether the new reflog-expire task should also remain available via `git gc` for users like Ramsay who prefer that workflow.