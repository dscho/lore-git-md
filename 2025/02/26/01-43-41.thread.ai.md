Here's the updated AI thread summary:

Bug report and discussion regarding `git rev-list --no-walk=sorted` commit ordering behavior with identical timestamps. Clement Moyroud originally reported that when commits share timestamps, the current implementation preserves input order rather than maintaining topological ordering, causing issues for automated cherry-picking workflows in customer-specific patch builds. The reproduction case demonstrates the problem when piping commit hashes through stdin` with `git rev-list --reverse HEAD | git rev-list --no-walk=sorted --stdin`.

D. Ben Knoble initially suggested exploring the `--topo-order` flag, but Clement Moyroud clarified this doesn't work for their use case as it processes the full commit graph. Phillip Wood then provided a key technical correction: Clement's test case was missing the `--no-walk` flag when testing `--topo-order`, suggesting the correct invocation should be `git rev-list --stdin --topo-order --no-walk` to potentially limit processing to just stdin commits while maintaining topological order.

The discussion has revealed two important technical constraints: (1) the need to process only input commits (not the full graph) while preserving their topological relationships, and (2) special handling required for timestamp collisions. The thread has systematically evaluated existing flags:
- `--no-walk=sorted` fails with identical timestamps
- `--topo-order` alone processes too much history
- The combination `--topo-order --no-walk` remains to be tested for whether it meets the workflow requirements

No patches have been proposed yet, but the analysis has progressed from problem identification to exploring whether existing flags can solve it before considering API changes. The key open question is whether Phillip's suggested flag combination will satisfy Clement's workflow needs while properly handling timestamp collisions. The thread remains in technical investigation phase, with participants carefully evaluating edge cases in Git's commit sorting behavior.