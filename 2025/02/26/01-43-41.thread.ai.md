Here's the updated AI thread summary:

Bug report and discussion regarding `git rev-list --no-walk=sorted` commit ordering behavior with identical timestamps. Clement Moyroud originally reported that when commits share timestamps, the current implementation preserves input order rather than maintaining topological ordering, causing issues for automated cherry-picking workflows in customer-specific patch builds. The reproduction case demonstrates the problem when piping commit hashes through stdin with `git rev-list --reverse HEAD | git rev-list --no-walk=sorted --stdin`.

D. Ben Knoble has now joined the discussion, suggesting exploration of the existing `--topo-order` flag as a potential solution before considering deeper changes to the `--no-walk=sorted` behavior. This introduces an explicit comparison between date-based sorting (`--no-walk=sorted`) and topology-based sorting (`--topo-order`) approaches.

Key technical details remain: the issue affects stdin input processing with timestamp collisions, and the workflow requires stable topological ordering. The discussion is now focused on whether existing flags can address the use case versus needing changes to the sorting implementation. No patches have been proposed yet - the thread is currently in the problem analysis and solution exploration phase.