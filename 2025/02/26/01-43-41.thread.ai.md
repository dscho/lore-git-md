Here's the updated AI thread summary:

Bug report and discussion regarding `git rev-list --no-walk=sorted` commit ordering behavior with identical timestamps. Clement Moyroud originally reported that when commits share timestamps, the current implementation preserves input order rather than maintaining topological ordering, causing issues for automated cherry-picking workflows in customer-specific patch builds. The reproduction case demonstrates the problem when piping commit hashes through stdin with `git rev-list --reverse HEAD | git rev-list --no-walk=sorted --stdin`.

Initial discussion explored existing flags:
- D. Ben Knoble suggested `--topo-order`, but Clement clarified this processes the full graph
- Phillip Wood proposed testing `--topo-order --no-walk`, but Clement confirmed this doesn't work as `--no-walk` explicitly disables topological ordering per documentation

Key technical findings:
1. Current implementation has no way to both restrict to input commits AND maintain topological relationships when timestamps collide
2. `--no-walk` modes are limited to either input order (`unsorted`) or chronological order (`sorted`)
3. The mutual exclusion between `--no-walk` and topological sorting creates a functional gap

The discussion has progressed from problem identification through solution exploration to recognizing an interface limitation. Clement's use case - automated cherry-picking requiring topological relationships without full graph processing - remains unmet by current flags. The thread now focuses on potential API changes, with the core question being whether to:
1. Modify `--no-walk` to optionally respect topology
2. Introduce a new flag combining both behaviors
3. Find an alternative approach to satisfy the workflow needs

No patches have been proposed yet, but the technical constraints are now clearly defined. The missing functionality is the ability to process only input commits while applying topological sorting when dates match, which current flags cannot provide. Participants are systematically evaluating edge cases in Git's commit sorting behavior before considering implementation approaches.