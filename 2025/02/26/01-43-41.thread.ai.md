Here's the updated AI thread summary:

Bug report regarding `git rev-list --no-walk=sorted` commit ordering behavior when commits share identical timestamps. The current implementation fails to maintain topological ordering when commit dates collide, instead preserving input order. This creates problems for automated cherry-picking workflows that process subsets of a repository's history, particularly when creating customer-specific patch builds where correct commit sequence is essential.

The reporter provides a reproduction case demonstrating the issue: when piping commit hashes through stdin with `git rev-list --reverse HEAD | git rev-list --no-walk=sorted --stdin`), commits with identical timestamps appear in input order rather than topological order. The current sorting logic lacks any secondary criteria when dates match. The reporter notes the absence of a pure topological sorting option in `--no-walk` mode and questions whether alternative sorting approaches might better serve this use case.

Key technical details: the problem affects the interaction between `--no-walk=sorted` and stdin input processing, specifically dealing with timestamp collisions. The reporter's workflow depends on stable topological ordering for automated operations, suggesting this may be a genuine limitation in the command's sorting implementation rather than a documentation or usage misunderstanding. No patch solution is proposed yet - the email serves to surface the issue and discuss potential approaches.