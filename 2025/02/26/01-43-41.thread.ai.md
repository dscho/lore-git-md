Here's the updated AI thread summary:

Bug report and discussion regarding `git rev-list --no-walk=sorted` commit ordering behavior with identical timestamps. Clement Moyroud originally reported that when commits share timestamps, the current implementation preserves input order rather than maintaining topological ordering, causing issues for automated cherry-picking workflows in customer-specific patch builds. The reproduction case demonstrates the problem when piping commit hashes through stdin with `git rev-list --reverse HEAD | git rev-list --no-walk=sorted --stdin`.

D. Ben Knoble suggested exploring the `--topo-order` flag as a potential solution, but Clement Moyroud has now clarified why this doesn't work for their use case. The key limitation is that `--topo-order` operates on the full commit graph rather than just the input commits from stdin, causing it to include hundreds of thousands of unrelated commits in the output. This makes it unsuitable for their workflow which requires topological sorting limited to a specific subset of commits.

The discussion has revealed a gap in Git's commit sorting capabilities: there is currently no way to perform topological sorting constrained to just an input set of commits, particularly when those commits may have identical timestamps. This affects automated patch build workflows where maintaining the original topological order is crucial but timestamp collisions prevent reliable date-based sorting.

Technical requirements are now clearly defined: the solution must process only the input commits (not the full graph) while preserving their topological relationships, with special handling needed for timestamp collisions. The thread remains in the problem analysis phase, with no patches proposed, but the discussion has effectively ruled out existing flags (`--no-walk=sorted` and `--topo-order`) as solutions, pointing toward the need for either a new flag or modified sorting behavior.