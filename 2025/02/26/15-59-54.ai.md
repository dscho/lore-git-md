This is a follow-up in the ongoing discussion about backspace escape sequence behavior in Git's output, specifically addressing the pager configuration aspect.

Junio Hamano responds to Jeff King's suggestion about modifying Git's default `$LESS` environment variable to include the 'U' flag (which makes control characters visible). He agrees with the general direction, noting that while this change would disable overstrike formatting in Git's direct pager output, that's an acceptable tradeoff. He makes an important distinction that this change wouldn't affect man pages, since they use a separate pager invocation.

The email reinforces the technical context established earlier: the 'U' flag would help reveal obscured configuration values by showing raw backspace characters, but earlier discussion identified limitations (like making tabs display as control characters). Hamano's response suggests moving forward with this change despite the formatting tradeoffs, while preserving the existing behavior for man pages.

Key technical details carried forward include:
- Git's pager invocation mechanics (direct vs man page spawning)
- The 'U' flag's effect on control character display
- The distinction between formatting loss in Git output versus man pages

This moves the thread toward a potential resolution on the pager configuration question while leaving other aspects of the backspace handling discussion (like command output consistency) still open.