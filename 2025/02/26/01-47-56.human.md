This is a discussion about potentially deprecating Git's support for unknown object types, part of Jeff King's ongoing zlib inflation hardening series.

Junio Hamano responds to Jeff King's observation that the `OBJECT_INFO_ALLOW_UNKNOWN_TYPE` feature (which permits querying info about objects with unknown types) may not be useful in practice. Hamano confirms that the related `hash-object --literally` option was primarily a debugging tool with limited utility, since such objects can't be packed or transferred. He suggests this feature shouldn't survive into Git 3.0.

The exchange highlights that the original purpose of this capability was testing how Git handles corrupt objects rather than supporting new object types. This discussion emerges from earlier patches that added BUG() assertions to prevent unsafe operations with unknown types, revealing that the feature may be more trouble than it's worth.

The thread context shows this is part of a broader effort to harden zlib inflation handling, with this particular sub-thread now considering whether to deprecate little-used features uncovered during the code review. The tone suggests this may be the beginning of a deprecation discussion rather than a final decision.