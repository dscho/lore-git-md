This is a refactoring patch that's part of the `git-diff-pairs` plumbing command development. Junio Hamano is reviewing Justin Tobler's implementation of the diff queue helper refactoring (patch 1/3 in the series).

The patch splits the diff queue operations into lower-level functions that return `diff_filepair` pointers (`diff_queue_addremove()` and `diff_queue_change()`) while maintaining the original wrapper functions (`diff_addremove()` and `diff_change()`) for compatibility. Junio notes the implementation looks correct but suggests the commit message could be more explicit about how the original functions are preserved as thin wrappers - an implementation detail that confirms the refactoring was done properly without changing behavior.

This is foundational work for the new `git-diff-pairs` command, providing the internal API it needs to process batch diffs. The thread context shows this is the final review round after multiple iterations, with all major design decisions settled and the series nearing completion. Junio's comment appears to be a minor documentation nit rather than requiring code changes.