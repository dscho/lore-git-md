Maintainer follow-up on pager configuration for control character handling.

Junio Hamano concisely agrees with Jeff King's assessment that adding 'U' to Git's default `$LESS` configuration would negatively impact tab display in diffs, despite initially considering it as a solution for backspace character visibility. The exchange confirms this approach is off the table, with both maintainers recognizing the tradeoff between control character visibility and readable diff formatting.

This email effectively closes one avenue of the broader discussion about handling control characters in Git's pager output, while leaving other aspects (like command-specific escape sequence handling) still open. The thread has now reached consensus that pager configuration changes must preserve diff readability as a higher priority than making backspace characters visible.

Key points for future reference:
- Maintainers have rejected modifying `$LESS` defaults to show control characters
- Primary concern is preserving clean tab display in diffs
- Decision maintains status quo for pager behavior
- Other solutions for backspace handling would need to avoid impacting diff output