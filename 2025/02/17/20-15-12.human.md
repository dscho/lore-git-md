This is a code cleanup patch in the ongoing `git merge-tree --stdin` bugfix series. Elijah Newren is reviewing Phillip Wood's simplification of error handling in the stdin processing code.

The patch removes redundant checks for negative return values from `real_merge()`, since that function already dies on errors. Newren confirms the technical correctness but suggests expanding the commit message to clarify how return values are (or aren't) used differently between stdin and non-stdin modes.

The change is straightforward - removing unused variables and dead code paths - and maintains the core functionality while making the code easier to understand. This is part of a larger effort to improve the reliability and maintainability of `merge-tree`'s stdin mode, following earlier discovery of potential deadlock issues.

Newren gives the patch his approval while offering a minor documentation suggestion, indicating this is a non-controversial improvement that aligns with the series' goals. The thread shows good collaboration between the two contributors, with Wood providing the cleanup and Newren (as the original author of much of this code) verifying the changes.