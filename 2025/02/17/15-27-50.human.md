Bugfix patch (4/8 in series) tightening `packed-refs` header validation in `git fsck`. This patch focuses specifically on ensuring the packed-refs file header follows the expected format. The header must start with "# pack-refs with: " (note the trailing space, correcting a previous inconsistency). The patch introduces new fsck error messages for malformed headers and non-newline-terminated entries.

Key changes:
- Adds strict validation for the packed-refs header format
- Introduces `FSCK_MSG_BAD_PACKED_REF_HEADER` and `FSCK_MSG_PACKED_REF_ENTRY_NOT_TERMINATED` error types
- Handles edge cases gracefully (missing headers allowed for backward compatibility, unknown traits not flagged as errors)
- Includes comprehensive tests covering various invalid header scenarios

The patch maintains alignment with runtime behavior while adding stricter validation where appropriate. It's part of a larger series enhancing packed-refs validation in `git fsck`, with previous patches having established the basic framework and subsequent ones adding more checks. The implementation appears thorough, with clear test coverage for the new validation cases.