Here's a human-readable thread summary:

This thread began with an RFC documentation patch aiming to clarify how Git handles submodule conflicts differently from regular file conflicts during merges. The author, drawing from personal debugging experience with GitHub's conflict display, proposes adding a technical note to `git-merge.adoc` explaining two special cases: (1) when one submodule commit is a descendant of another (triggering a fast-forward attempt), and (2) when a single merge commit contains both conflicting submodule commits (where Git suggests it as resolution while keeping the conflict marked).

The 7-line addition stems from the author's examination of merge-ort behavior and addresses a real documentation gap that caused confusion in practice. While the patch is technically sound, the author explicitly marked it as RFC to solicit feedback on whether this explanation belongs in the merge documentation or should instead appear in submodule-specific docs. The change appears well-motivated by observable behavior in both merge-ort and the older merge-recursive strategy.