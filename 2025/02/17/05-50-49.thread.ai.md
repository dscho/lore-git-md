Thread summary:

This thread introduces a refactoring of `builtin/show-branch.c` to replace direct access to commit object flags with commit-slab storage. The change follows Junio Hamano's prior suggestion to use slabs for runtime-computed bitmasks, potentially helping address the `MAX_REVS` limitation. The patch establishes `commit_flags` slab infrastructure with `get_commit_flags()`/`set_commit_flags()` helpers while preserving all existing flag manipulation logic (including `UNINTERESTING`, `REV_SHIFT` and `MAX_REVS` constants). The author seeks guidance on whether the slab should store simple integers or a more complex struct. Implementation details show careful attention to initialization/cleanup and thorough testing via both the test suite and GitHub CI. The changes are self-contained within `show-branch.c` with no interface modifications. Key open question remains the optimal slab storage type (integer vs struct) for the flags.