Documentation patch fixing a typo in `git-send-email` manpage.

This is a straightforward typo fix in the `git-send-email` documentation. The patch corrects the option name from `--no-signed-off-cc` to `--no-signed-off-by-cc` in the manpage text. The author notes there is no `--signed-off-cc` variant (without `-by`) for `git send-email`, making this a clear error.

The change is minimal (single character correction) and uncontroversial, affecting only documentation. The patch follows standard formatting with a proper commit message and Signed-off-by line. No tests or behavioral changes are involved since this purely fixes documentation accuracy.