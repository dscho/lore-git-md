This is part of the performance optimization series for the refs subsystem, specifically introducing infrastructure for skipping redundant object-ID ambiguity checks. The patch adds a new `GET_OID_HASH_SKIP_AMBIGUITY_CHECK` flag to bypass the reference ambiguity verification in `get_oid_basic()`, which will be used in later optimizations.

Patrick Steinhardt extends the object lookup API to support selective skipping of the refname ambiguity check, modifying both the flag definitions in `hash.h` and the core lookup logic in `object-name.c`. The change is minimal but foundational - it doesn't yet implement the optimization itself but provides the mechanism that subsequent patches will use to avoid unnecessary checks during ref operations.

The modification preserves all existing behavior when the new flag isn't used, maintaining backward compatibility. This fits into the series' broader goal of optimizing refname availability checks, particularly for the reftable backend where these checks currently cause performance bottlenecks. The thread context indicates this is part of coordinated work between Steinhardt and Karthik Nayak, with Steinhardt handling the object-lookup infrastructure changes while Nayak focuses on the ref-specific optimizations.