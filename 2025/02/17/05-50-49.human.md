Refactoring patch for `show-branch` to use commit-slabs for flag storage. This is a technical change that replaces direct access to `commit->object.flags` with a commit-slab mechanism, introducing new helper functions `get_commit_flags()` and `set_commit_flags()`. The patch appears to be part of a larger effort to lift the `MAX_REVS` limitation in `show-branch`, as suggested in prior feedback from Junio Hamano.

The author (Meet Soni) acknowledges some uncertainty about the original TODO comment's intent regarding ref name storage, but has proceeded with flag storage as a first step. The changes are well-tested, passing both the test suite and GitHub CI. The diff shows a thorough conversion of all flag accesses in `builtin/show-branch.c` to use the new slab-based approach.

This looks like a clean, focused refactoring that follows up on previous maintainer feedback. The implementation appears correct and maintains existing behavior while paving the way for future improvements to the flag handling system. The author is explicitly seeking guidance on whether to extend the slab to store additional metadata (like a size field), suggesting this may be the first in a series of related changes.