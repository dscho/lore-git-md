Bugfix for pickaxe (-S/-G) argument validation to prevent crashes with empty strings.

This patch implements the solution discussed in the thread, adding input checking to reject empty arguments for `-G` and `-S` options in `git log` and related commands. The fix adds explicit validation in `diff.c` that returns an error message when an empty string is provided, preventing the BUG() assertion that would otherwise trigger in diffcore-pickaxe.c. The patch includes tests verifying the new error messages appear for both `-G ""` and `-S ""` cases.

The changes are minimal and focused - just two conditionals in the option parsing code plus corresponding test cases. The error messages follow Git's standard style and exit with code 129 (usage error). This matches the thread's consensus that empty pickaxe arguments serve no practical purpose and should be rejected rather than treated as no-ops or allowed to crash.

The test additions in t4209-log-pickaxe.sh demonstrate the fix works by checking both the error output and exit code. The tests follow the existing pattern in that file while adding coverage for this edge case. The patch appears complete and properly addresses the reported issue without introducing new problems or side effects.