This is a documentation patch in a series addressing `git merge-tree --stdin` improvements. Elijah Newren is reviewing Phillip Wood's documentation updates from the previous email in the thread.

The patch clarifies two aspects of `--stdin` behavior: it explicitly documents that `--stdin` implies `-z` (NUL-terminated output), and removes incorrect documentation about negative merge status codes (the command would die before outputting such codes). 

Newren agrees with both changes but raises a question about whether the documentation should instead specify the exit code (128) that `die()` would produce in error cases, rather than removing the negative status documentation entirely. This suggests the documentation might still need one final tweak to be technically precise about error handling behavior.

The changes are straightforward and uncontroversial, building on the thread's earlier work to properly document the plumbing behavior of `merge-tree`. The series appears nearly complete, with this minor point about error code documentation being the only remaining question.