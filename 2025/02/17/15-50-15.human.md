This is a refactoring patch that introduces a new function `repo_get_oid_with_flags()` as part of a larger performance optimization series for the refs subsystem. The patch comes from Patrick Steinhardt, though it's worth noting the thread context indicates Karthik Nayak is the primary author of the overall series.

The change adds a flags parameter to the OID lookup functionality, creating a new `repo_get_oid_with_flags()` function while keeping the existing `repo_get_oid()` as a wrapper that passes default flags. The implementation is straightforward - it modifies the object-name.c/h files to expose the flags parameter that was already being used internally by `get_oid_with_context()`.

While the commit message is minimal (stating only that this will be used in a subsequent commit), the thread context shows this is the first step in a 14-part series aimed at optimizing refname availability checks, particularly for the reftable backend. The clean separation suggests this is preparatory work for more substantive changes to come in later patches.

The change appears mechanically sound - it doesn't alter any behavior, just exposes existing functionality in a more flexible way. Reviewers will likely want to see how this gets used in subsequent patches to evaluate whether the abstraction is justified.