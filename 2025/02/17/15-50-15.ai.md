This is a refactoring patch that introduces a new function `repo_get_oid_with_flags()` as part of a larger performance optimization series for the refs subsystem. The patch comes from Patrick Steinhardt, who appears to be collaborating with Karthik Nayak on this ref backend optimization work.

The new function generalizes the existing `repo_get_oid()` by adding a flags parameter that gets passed through to the underlying `get_oid_with_context()`. The implementation is straightforward - it extracts the core logic from `repo_get_oid()` into the new function and makes the original function a simple wrapper that passes flags=0. The header file gets updated with documentation matching the existing style.

This appears to be foundational work for later optimizations in the series, though the commit message doesn't specify exactly which flags will be used or what subsequent changes will build on this. The changes are cleanly separated into logical commits, with this one focusing solely on the API extension without any behavior changes. The diff shows careful attention to maintaining the existing code structure and documentation style.

Key technical details for future reference:
- New function: `repo_get_oid_with_flags()` in object-name.[ch]
- Maintains identical behavior to `repo_get_oid()` when flags=0
- Prepares for future use of GET_OID_* flags in ref operations
- Part of broader ref performance work but touches object-name infrastructure
- No behavior changes or performance impact in isolation