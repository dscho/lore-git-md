This is a documentation patch in the ongoing `git merge-tree --stdin` bugfix series. Elijah Newren responds to Phillip Wood's documentation improvements with a minor clarification request. The patch being discussed adds explicit documentation that `--stdin` implies `-z` (NUL-terminated output) and removes incorrect documentation about negative merge status codes (since the program dies before outputting them).

Newren agrees with the changes but questions whether the documentation should specify the exact exit code (128) when the program dies, rather than just removing the "<0" status documentation entirely. This maintains the thread's focus on precise documentation of `merge-tree`'s behavior, particularly around error conditions and output formatting.

The exchange shows the careful attention being paid to documenting edge cases and failure modes in the `--stdin` implementation. Newren's response indicates the series is nearing completion, with only minor documentation polish remaining. The thread context shows this is part of a coordinated effort involving multiple contributors to fix deadlock issues and improve the codebase's robustness.