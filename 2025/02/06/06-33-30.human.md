This is a refactoring patch in the `git clone --revision` series, specifically addressing tag handling behavior. 

The patch inverts the internal flag controlling tag cloning from `--no-tags` to `--tags`, making the default behavior (cloning tags) explicit rather than implicit. While this doesn't change functionality (since tags are cloned by default anyway), it simplifies the code and prepares for future changes in the series. 

The documentation is updated to properly document both `--tags` and `--no-tags` options, clarifying that `--tags` is the default behavior and explaining how these options interact with future fetches. The changes are straightforward - flipping boolean values and updating help text - with no behavioral impact, so no new tests are needed.

This appears to be a clean, focused change that improves code clarity while maintaining existing behavior. It's part of a well-reviewed series that has gone through multiple iterations, with this particular change helping pave the way for the new `--revision` functionality.