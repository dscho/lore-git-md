Git-p4 Python interpreter path compatibility discussion, examining whether the shebang should use `python` (current) or `python3` for modern Python 3 default. Thread began with reports of `git p4` failures when `python` isn't in PATH on systems like Debian where `python` isn't symlinked to Python 3 (Git 2.30.2). Maintainer brian m. carlson defended the `python` shebang as necessary for Python 2.7 compatibility, recommending build-time PYTHON_PATH configuration or proper package management as solutions. 

Illia Bobyr now counters with Ubuntu 24.10 evidence showing `python` resolves to Python 3.12.7 via `python-is-python3` package, with version-switching packages (`python-is-python2`/`python-is-python3`) allowing explicit version selection. This demonstrates modern Ubuntu defaults to Python 3 for unversioned `python`, challenging the assumption that `python` shebangs primarily serve Python 2.7 compatibility needs. The landscape appears more nuanced, with major distributions diverging in their `python` resolution policies.

Key technical detail remains the `git-p4` shebang line in `git-p4.py`. Open question: whether Git should update to `python3` given evidence that (1) Python 2.7 is EOL, (2) some major platforms now default `python` to 3.x, and (3) version-switching packages exist for legacy needs. Maintainer position still favors `python` for maximum compatibility, but new evidence suggests this may no longer be the dominant case. No patches proposed yet; workarounds remain local modification or system configuration. Thread shows tension between backward compatibility assumptions and modern Python ecosystem reality.