This is the third version of a feature patch adding long option names for Git's pickaxe search functionality, specifically `--patch-modifies` as an alias for `-S` and `--patch-grep` for `-G`. The changes are now focused solely on core Git after separating out gitk-related changes from earlier versions.

The patch updates documentation (diff-options.txt, git-blame.txt, gitdiffcore.txt) to explain both the old and new option forms while maintaining identical functionality. Core diff machinery (diff.c, diff.h) is modified to support the new names alongside existing ones. Test coverage in t4209-log-pickaxe.sh has been expanded to verify behavior parity between old and new option names across various scenarios, with simpler tests duplicated for both forms while more complex tests use the long names for readability.

Key technical details carried forward from the thread:
- `-S`/`--patch-modifies` searches for changes in string occurrence counts
- `-G`/`--patch-grep` finds patches containing matching lines
- Both maintain full backward compatibility
- Error messages now show both short and long forms (e.g., "-G/--patch-grep")
- All existing tests pass with both option forms

The patch appears well-tested and complete, with no outstanding technical issues noted in the thread. The author has addressed prior feedback by separating concerns from gitk, leaving this iteration focused on the core Git changes. Documentation and tests have been updated in parallel with the code modifications.