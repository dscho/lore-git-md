This is the final patch in Usman Akinyemi's series adding OS version capability to Git's protocol, now addressing Junio Hamano's feedback about the privacy control design. The thread has progressed through multiple iterations (v1-v4) with the implementation now extending the existing `agent` capability string with OS information rather than introducing a separate capability.

Key developments in this email:
- Junio questions the need for the `transfer.advertiseOSVersion` config knob, suggesting the existing `GIT_USER_AGENT` mechanism already provides sufficient privacy control by allowing users to override/suppress the entire agent string
- Usman agrees to remove the config option entirely, simplifying the interface
- Junio also suggests renaming `os_info_sanitized()` to just `os_info()` since there's no unsanitized version available, which Usman accepts
- The implementation details remain robust from previous patches:
  - Uses `get_uname_info()` helper with platform-specific handling
  - Includes string sanitization via `redact_non_printables()`
  - Maintains backward compatibility
  - Has comprehensive test coverage in t5701-git-serve.sh

This concludes the architectural discussion, with the series now ready for merging after these final simplifications. The remaining open question from earlier threads (whether to keep the config knob) is resolved here in favor of using `GIT_USER_AGENT` as the sole privacy control mechanism. The implementation maintains all the benefits originally cited (simplicity, backward compatibility, debugging utility) while removing what the maintainer viewed as unnecessary configuration surface area.