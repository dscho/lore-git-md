This is part of Patrick Steinhardt's ongoing effort to fully separate the reftable library from Git core infrastructure (v4 patch 3/18). The patch removes the last remaining Git-specific dependency in the blocksource code by replacing `xmmap()` with direct `mmap()` calls.

The change serves two purposes: it makes the reftable code properly handle mmap failures by returning errors rather than dying, and it eliminates another tie to Git's utility functions. The implementation carefully maintains the same memory safety guarantees while using the more portable mmap interface. Error handling is improved by properly bubbling up failure codes from the mmap operation.

This appears to be a straightforward but important technical cleanup as part of the larger reftable decoupling effort. The patch follows the established pattern seen in earlier parts of the series, replacing Git-specific helpers with direct system calls while maintaining robust error handling. Reviewers had already acked the memory safety aspects in previous iterations, and this change aligns with the architectural direction already established in the thread.