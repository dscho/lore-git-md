Refactoring patch in the `--revision` option series for `git clone`. This is the first of seven patches that prepares the codebase for the new feature by cleaning up global variables in `builtin/clone.c`.

The patch moves numerous global declarations into `cmd_clone()`'s local scope, including the `builtin_clone_options` array and all related option variables. This 195-line change (101 insertions, 94 deletions) is primarily structural - it doesn't change behavior but makes the code more maintainable by reducing global state. The changes include:

- Moving option parsing variables like `option_reject_shallow`, `option_template`, and `option_origin` into function scope
- Relocating the `builtin_clone_options` array definition into `cmd_clone()`
- Adding proper initialization for moved variables
- Cleaning up string lists at function exit

This groundwork enables the series' later patches to more cleanly implement the `--revision` functionality. The patch appears well-scoped and mechanical, with no obvious issues in the diff. As the first step in a well-reviewed series, it provides a solid foundation for the feature work to follow.