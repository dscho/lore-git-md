This is a follow-up in the thread introducing long option names for pickaxe search functionality (`-S`/`--patch-modifies` and `-G`/`--patch-grep`). The patch author Illia Bobyr asks whether to submit gitk changes separately or include them in this series, having already split them per prior feedback.

Junio C Hamano responds that while keeping them separate would be technically cleaner (since gitk depends on core Git), including them in the same series would likely be acceptable in practice. This suggests the core Git changes are nearing final review, with gitk integration being a secondary consideration. The thread shows the patch series is now focused solely on core Git functionality after addressing Johannes Sixt's feedback about separating concerns.

Key technical context remains unchanged from the thread summary: the patch adds long option aliases while maintaining backward compatibility, with thorough test coverage and documentation updates. The gitk changes would presumably mirror this same functionality but in the separate gitk codebase. The discussion now centers on workflow preferences rather than technical concerns about the core implementation.