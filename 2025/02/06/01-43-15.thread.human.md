Here's a concise human-readable summary of the thread's current state:

This thread introduces long option names for Git's pickaxe functionality (`--patch-modifies` for `-S` and `--patch-grep` for `-G`), now at version 5. The technical implementation is complete, with the latest patch adding dual-form support while maintaining identical behavior between short and long options. The changes touch core diff machinery, documentation, tests, and completion scripts.

Junio Hamano has approved the technical implementation but suggests better organizing the series by splitting it into two parts: documentation/test cleanups first, followed by the long-options feature work. The documentation fixes (correcting descriptions of `-S` and `-G` behavior) could land independently, making the series easier to review.

An ongoing discussion remains about whether to use "block-of-text" (reflecting Linus Torvalds' original design for detecting changed blocks) or "string" (matching the current implementation's counting of occurrences) in the documentation. Junio prefers keeping "block-of-text" for historical accuracy but is open to "string" if accompanied by a note about multi-line usage.

The thread shows how even straightforward features like adding long options can surface interesting historical context and documentation nuances in Git's codebase. With the technical work resolved, the remaining decisions are about documentation phrasing and patch series organization before the changes can be merged.