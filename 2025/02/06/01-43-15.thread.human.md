This thread introduces long option names for Git's pickaxe search functionality, adding `--patch-modifies` as an alias for `-S` and `--patch-grep` for `-G` to improve discoverability. The patch series is now at version 3, having been separated from gitk changes per prior feedback.

The changes modify core diff machinery (diff.c, diff.h) to support both the new and existing option names while maintaining backward compatibility. Documentation has been updated across multiple man pages (diff-options, git-blame, gitdiffcore) to explain both forms. Test coverage in t4209 has been expanded to verify identical behavior between the old and new option names, with simpler tests duplicated for both forms and more complex tests using the long names for readability.

The author has addressed all prior feedback, and there are no outstanding technical objections. The patch appears well-tested and complete, with error messages now showing both short and long forms (e.g., "-G/--patch-grep"). The changes are now focused solely on core Git after separating out gitk-related modifications from earlier versions. The thread shows consensus that this is ready for merging.