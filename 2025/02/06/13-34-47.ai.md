Junio Hamano raises a question about the relationship between two refactoring goals in David Aguilar's difftool patch series. This is part of the ongoing `the_repository` removal effort, specifically addressing patch 1/3 which introduced a `struct difftool_options` to encapsulate global variables.

The key observation is that while both eliminating globals and removing `the_repository` usage are worthwhile goals, Junio questions whether they are truly interdependent as implied by the commit message's "in preparation for" phrasing. He suggests these might be orthogonal changes that could be pursued independently.

The technical content remains unchanged from previous versions: the patch moves three global variables (`trust_exit_code`, `has_symlinks`, and `symlinks`) into a new struct, updates configuration handling to use the struct, and modifies all affected functions to take the struct as a parameter. The symlink handling behavior (where `has_symlinks` is checked in `get_symlink()` while most code uses `symlinks`) remains the same as in previous iterations.

This email doesn't introduce new changes to the code but rather seeks clarification about the conceptual relationship between the two refactoring goals. The thread context shows the series is otherwise functionally complete and approved, with this being the last open discussion point before potential integration.