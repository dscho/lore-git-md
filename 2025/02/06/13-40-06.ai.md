This is a follow-up in the ongoing thread about Git's clean/smudge filters and history rewriting operations. Josef Wolf expresses continued confusion about why Git's cherry-pick operation appears to ignore clean filters when storing content in the repository, despite working with a linear history where they expected each operation to automatically apply the clean filter before committing.

The email highlights a fundamental misunderstanding about Git's filtering behavior - the expectation that all content going into the repository should automatically be "cleaned" conflicts with Git's actual design where filters are only applied during specific operations (checkout/add). This contrasts with the thread's earlier technical explanations about proper renormalization workflows.

Elijah Newren's quoted response clarifies that the `merge.renormalize` option does work in both merge backends (recursive and ort), despite merge-ort's index-avoidance design requiring special implementation handling. This confirms the option's availability while hinting at deeper architectural constraints in Git's filtering system.

The email surfaces a key conceptual gap about when and how Git applies content filters during history operations, suggesting the need for clearer documentation about the limited scope of automatic filtering. This represents an unresolved educational aspect of the thread, though the technical solutions for proper renormalization workflows were already been established in prior messages.