This is part 10/18 of Patrick Steinhardt's series to fully decouple the reftable library from Git core infrastructure. The patch continues the established pattern of replacing Git-specific utility functions with reftable-specific implementations, this time targeting the `ARRAY_SIZE()` macro.

The change is straightforward - it replaces a single use of `ARRAY_SIZE()` in `reftable/reader.c` with an open-coded equivalent (`sizeof(sections) / sizeof(*sections)`). This follows the series' architectural goal of eliminating all Git-specific dependencies from the reftable code. The modification appears in a debug printing function (`reftable_reader_print_blocks()`), so it's unlikely to affect production performance.

This patch maintains the series' high quality standards - it's a minimal, focused change with clear motivation stated in the commit message. The change is consistent with previous utility function replacements in the series (like `COPY_ARRAY()`, `BUG()`, and endian conversion functions). Since this is a mechanical replacement in non-critical debug code, it seems unlikely to introduce new technical concerns or require significant review attention.

The patch continues the series' progression toward complete independence from Git infrastructure, with all major technical concerns already addressed in prior reviews. As with previous patches, it maintains behavioral compatibility while removing Git-specific dependencies.