This is a follow-up in the ongoing thread about Git's clean/smudge filters and history rewriting operations, specifically focusing on cherry-pick behavior. Josef Wolf reports problematic interactions between cherry-pick operations and content filtering workflows.

The email identifies two key issues with `git cherry-pick` when clean/smudge filters are involved: First, it appears to bypass the clean filter when committing changes, resulting in smudged content being stored in the repository. Second, even when starting from a normalized commit, cherry-pick attempts to apply the changes without proper cleaning, leading to conflicts. The author suggests these problems can only be worked around by manually renormalizing with `git add --renormalize` and amending the commit.

This continues the thread's exploration of Git's limitations around content filtering during history operations, now specifically examining cherry-pick's behavior. The issues raised align with earlier discussions about Git's architectural constraints regarding in-memory attribute processing and the need for plumbing improvements to properly support filtering during operations like rebase and cherry-pick. The email doesn't propose solutions but rather documents observed behavior that appears inconsistent with the expected filter application model.