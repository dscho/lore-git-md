This is a refactoring patch in the `git clone --revision` series, specifically handling tag behavior inversion. The patch flips the internal representation of tag cloning from a `--no-tags` flag to a positive `--tags` flag (defaulting to true), while maintaining identical behavior. 

The change simplifies the code by using a positive rather than negative flag, which better matches the default behavior where tags are cloned. The documentation is updated to show both `--tags` and `--no-tags` as available options, though `--tags` is effectively a no-op since it's the default. The commit explains this inversion prepares for future commits in the series, though it doesn't specify what those changes will be.

Key technical changes include:
- Renames `option_no_tags` to `option_tags` with default value 1
- Updates all conditionals checking tag behavior to use the positive form
- Documents both `--tags` and `--no-tags` in git-clone.txt
- Maintains all existing functionality including config setting for `tagOpt`

The patch doesn't require new tests since the behavior remains unchanged - it's purely an internal refactoring. This appears to be a straightforward, well-contained change that improves code clarity while preserving functionality.