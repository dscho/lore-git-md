Refactoring patch in the `git clone --revision` series, focusing on simplifying the `wanted_peer_refs()` function. This is part of the larger effort to enable cloning specific revisions without remote-tracking branches.

The patch streamlines the logic for handling refs during clone operations, particularly when `--single-branch` is specified. Key changes:
- Eliminates duplicate code paths between single-branch and regular clone modes
- Restructures the function to have a single main flow with early modifications to `refs` when in single-branch mode
- Improves memory management by introducing a `to_free` pointer for temporary refs
- Maintains all existing functionality while reducing nesting and complexity
- Preserves special handling for tags and mirror cases

The refactoring appears well-considered, with the commit message clearly explaining the motivation (reducing complexity from accumulated changes over time) and approach (consolidating code paths). The diff shows careful attention to maintaining existing behavior while making the logic more straightforward. This is a preparatory change for the larger `--revision` feature, helping ensure the ref handling logic is clean before new functionality is added.