This is a refactoring patch in the `--revision` clone option series, focusing on tag handling. The patch simplifies the code by moving the tags refspec addition earlier in the clone process, combining it with the main fetch refspec processing rather than handling it separately later.

The key change moves the tag refspec (`refs/tags/`) from being processed in `wanted_peer_refs()` to being appended to `remote->fetch` upfront when tags are requested. This eliminates some duplicate processing and makes the control flow more straightforward. The patch also fixes a minor inconsistency where `--mirror` clones were still processing tags separately despite already mirroring all refs.

This appears to be a clean, focused improvement that builds on the series' earlier refactoring work. The change is well-contained and maintains all existing functionality while making the code more maintainable. The commit message clearly explains both the technical change and the rationale behind it.