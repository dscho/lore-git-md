This is a review comment on a refactoring patch that's part of the `the_repository` removal effort. Elijah Newren is responding to David Aguilar's patch that moves `difftool`'s global variables into a `difftool_options` struct.

The main observation is about an apparent inconsistency in how symlink handling works - the code uses `has_symlinks` in `get_symlink()` when it might be more logical to use the `symlinks` parameter. Elijah notes this same question came up in the v1 review from Patrick Steinhardt, suggesting it's a recurring point of confusion.

While acknowledging this doesn't belong in the current patch, Elijah suggests the commit message should address this is intentional (if it is) to prevent future reviewer confusion. He also notes that if `has_symlinks` is only being used as a default value for `symlinks`, it might be removable in a follow-up patch.

The review is positive overall ("Thanks for splitting these out") while constructively pointing out an area that could use clarification. The technical discussion focuses on the symlink handling details while recognizing the patch's primary goal of encapsulating state in preparation for removing global repository access.