This is a final cleanup note for the `git clone --revision` feature series. Patrick Steinhardt points out that the explicit `TEST_PASSES_SANITIZE_LEAK=true` line in the new test file is no longer needed, as leak checking is now mandatory for all tests by default. He considers this a minor enough issue that it doesn't require resubmitting the patch series.

The comment comes as the series is effectively complete - it has already gone through six iterations of review and refinement, with all technical implementation and documentation finalized. The thread summary shows this was a substantial effort involving multiple contributors, with Patrick being one of the key reviewers who helped validate error handling and early tag refspec processing in previous versions.

This appears to be the last bit of polish before the series gets merged, addressing a small documentation detail in the test infrastructure rather than the core functionality. The maintainers have already queued the series for integration, and this observation doesn't change that status.