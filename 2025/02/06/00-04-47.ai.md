This is a coordination email in the ongoing reftable and Windows compatibility work, specifically addressing the reftable Win32 in-use error series that was previously overlooked in Junio Hamano's "What's cooking" report. 

The thread context shows Patrick Steinhardt had pointed out the missing series, which Johannes Schindelin had already approved. Junio now raises a technical concern about the implementation approach - the patch relies on Windows-specific unlink() behavior that would require including <compat/mingw.h>, which conflicts with the parallel ps/reftable-sans-compat-util effort to reduce compat-util dependencies.

This creates a tension between two ongoing efforts: the immediate need for Windows-specific error handling in reftable operations versus the architectural goal of removing compat-util dependencies. The email doesn't propose a resolution but highlights the architectural conflict that needs to be addressed, likely requiring either a rework of the Win32 error handling approach or a temporary exception in the compat-util cleanup effort.

Key technical points for future reference:
- The contested patch series deals with Windows file-in-use errors during reftable operations
- Current implementation uses unlink() from compat/mingw.h
- This conflicts with ps/reftable-sans-compat-util's goal of removing compat-util dependencies
- Johannes Schindelin has already approved the Win32 error handling approach
- Junio is questioning the architectural consistency of mixing these efforts

The email represents a maintainer-level checkpoint where technical tradeoffs between immediate fixes and long-term goals need to be evaluated, with Windows-specific behavior and reftable internals being the key subsystems involved.