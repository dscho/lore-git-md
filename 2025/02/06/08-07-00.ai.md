This is a technical clarification in the ongoing thread about Git's renormalization behavior during history rewriting. Elijah Newren provides authoritative details about how the `merge.renormalize` configuration option works across Git's merge backends.

The email confirms that while the option parsing infrastructure is defined in `merge-recursive.c`, the `merge-ort` backend does properly support the `merge.renormalize` option despite its original design avoiding index operations that renormalization requires. Newren references his earlier implementation work that added this support through "ugly shenanigans" (linking to the original discussion), though he notes this is tangential to the current thread.

Key technical details clarified:
- Both merge backends (`recursive` and `ort`) share the same option parsing infrastructure from `merge-recursive.c`
- The `merge.renormalize` option is fully functional in `merge-ort` despite its index-avoidance design
- Implementation required special handling in `merge-ort` to accommodate renormalization's index requirements

This response definitively settles the open question about whether `merge.renormalize` works with the newer `ort` strategy, confirming it does while providing architectural context about the implementation challenges. The information helps complete the thread's technical picture of renormalization behavior across Git's merge strategies.