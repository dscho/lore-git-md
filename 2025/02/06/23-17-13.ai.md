This is a platform compatibility discussion about the `git-p4` Python interpreter path, continuing the ongoing thread about shebang line handling.

Junio C Hamano clarifies Git's general build-time approach to interpreter paths, explaining that Git scripts (including `git-p4.py`) are modified during build to use a specific Python path rather than relying on `/usr/bin/env`. He emphasizes this is a deliberate choice to avoid PATH-related issues and ensure consistent behavior across installations. The response confirms that builders should explicitly specify their desired Python path (defaulting to `/usr/bin/python` if unspecified), addressing the original complaint about missing `python` in PATH.

Key technical details include: Git's build system rewrites shebang lines based on PYTHON_PATH configuration, the default `/usr/bin/python` path when unspecified, and the distinction between this build-time resolution versus runtime PATH lookup. The email reinforces Git's conservative approach to compatibility while providing a clear mechanism for system-specific customization.

The exchange reveals an underlying tension between Git's build-time configuration philosophy and modern Python distribution practices, though Junio presents the current behavior as an intentional design choice rather than an oversight. No changes to the status quo are proposed, maintaining the position that system integrators should configure PYTHON_PATH appropriately for their environment.