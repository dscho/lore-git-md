Follow-up technical clarification in the renormalization thread. Brian M. Carlson corrects their earlier statement about `merge.renormalize` configuration, noting it does exist but may only affect the legacy `merge-recursive` strategy rather than the newer `merge-ort`. They acknowledge the edge case Josef Wolf identified where the suggested `git add --renormalize` approach fails on commits that don't modify filtered files, proposing two alternative solutions:

1. Using `-X renormalize` with the default merge strategy (which may preserve empty commits but drop merge commits)
2. Recommending `git filter-repo` as a more robust solution for history rewriting with filters

The email surfaces important implementation details about the limited scope of `merge.renormalize` (only working with `merge-recursive`) and practical limitations of the rebase-based approaches. Carlson defers to Elijah Newren (merge-ort author) for definitive answers about current behavior. The technical discussion focuses on preserving history structure during filtered rewrites, particularly around empty commits and merge preservation.