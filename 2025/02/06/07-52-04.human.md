This is part of Patrick Steinhardt's v4 series (2/18) to fully decouple the reftable library from Git's core infrastructure. The patch replaces Git's `write_in_full()` utility with a reftable-specific implementation called `reftable_write_data()`, continuing the architectural separation effort.

The new function maintains the same error handling semantics (including EAGAIN/EINTR retries and proper error returns) while removing the Git dependency. The change is straightforward - it replaces three existing `write_in_full()` calls with the new wrapper while adding the implementation that closely mirrors Git's original behavior.

This appears to be a well-executed mechanical change that aligns with the series' overall goal. The patch is small and focused, with the new function clearly modeled after Git's implementation to maintain behavior parity. As part of the broader reftable decoupling effort that has already undergone significant review, this change seems uncontroversial and ready for integration.