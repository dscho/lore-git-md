Memory optimization follow-up discussion. Patrick Steinhardt responds to René Scharfe's patch that restructured `clear_commit_marks_many()` to reduce peak memory usage during commit graph traversal. While acknowledging the theoretical improvement from O(n) to O(1) memory usage, Steinhardt raises two key points:

1. The change effectively converts the algorithm from depth-first to breadth-first traversal, which could perform worse in certain merge-heavy repository topologies ("branch thickets"). This suggests the optimization might have tradeoffs rather than being universally beneficial.

2. He requests real-world performance measurements to validate the improvement, questioning whether the memory reduction has practical significance in typical repository operations. The discussion now hinges on whether the optimization provides measurable benefits that outweigh any potential performance regressions in edge cases.

The technical focus remains on the commit.c changes and their impact on graph traversal behavior, with the core question shifting from implementation correctness to practical performance implications.