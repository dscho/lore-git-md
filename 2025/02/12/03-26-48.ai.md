This is part of the pickaxe long-option series, specifically updating the short help text in diff.h to include both the short (`-G`) and long (`--pickaxe-grep`) forms of the regex-based pickaxe search option. The patch aligns the help text with the existing `-S/--pickaxe-all` pairing, maintaining consistency in how the options are presented to users.

The change adds two key elements: the short `-G` description that was previously missing from the help text, and the new `--pickaxe-grep` long option that matches the functionality. It also updates the `--pickaxe-all` description to clarify it applies to both `-G` and `-S` searches. The diff shows careful attention to formatting consistency with the existing help text structure.

This patch follows the series' pattern of maintaining backward compatibility while improving discoverability through long options. The implementation is straightforward - just adding the new option descriptions without changing any underlying behavior. The change is minimal but important for user-facing documentation, completing the dual-option presentation for both pickaxe search variants (`-G/--pickaxe-grep` for regex matching and `-S/--pickaxe-all` for occurrence counting).

Technical details to note for future reference:
- Help text now shows both `-G<regex>` and `--pickaxe-grep` forms
- `--pickaxe-all` description updated to reference both `-G` and `-S`
- Formatting maintains existing indentation and line break patterns
- No functional changes to the pickaxe search behavior itself
- Part of broader effort to make pickaxe options more discoverable
- Follows same pattern established for `-S/--pickaxe-all` pairing