This is a refactoring patch in the `git-diff-pairs` series, responding to review feedback about API design. Patrick Steinhardt questions whether the patch should go further in its changes to the diff queue helper functions.

The patch currently introduces new variants of `diff_addremove()` and `diff_change()` that return the queued `diff_filepair`, while keeping the original functions. Patrick suggests two potential improvements: (1) modifying the existing functions rather than maintaining both variants, and (2) making the functions take an explicit `diff_queue` parameter instead of using the global queue, which would aid libification efforts.

This email doesn't propose concrete changes but raises thoughtful questions about API evolution. The suggestions align with Git's ongoing efforts to reduce global state and improve code structure. The thread context shows Justin Tobler had similar concerns about maintaining both function variants, indicating this is a point worth discussing before finalizing the API.