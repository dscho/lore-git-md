This is a test cleanup patch in the pickaxe long-options series. It fixes a minor inconsistency in test naming where a dummy argument for `-G` was called "string" rather than "regex" in line with how the option is documented and used elsewhere. The change is purely cosmetic - it doesn't affect test logic, just makes the test more accurately reflect that `-G` expects a regex pattern rather than a literal string.

The patch modifies a single test in t/t4209-log-pickaxe.sh to use "regex" as the placeholder value when testing the `-G` option's interaction with `--no-pickaxe-regex`. This aligns with the series' broader goal of improving consistency in how Git's pickaxe options are presented and documented.

Key technical details:
- Changes test case to use "regex" instead of "string" as dummy argument for `-G`
- Maintains identical test behavior (still checks error code 128)
- Part of larger effort to standardize terminology around pickaxe options
- Follows existing convention seen elsewhere in the codebase

This appears to be a straightforward improvement that makes the test more self-documenting without changing any functionality. It's the first in a series of 10 patches introducing long option names for pickaxe functionality.