This is a technical discussion in the ongoing thread about partial reference transactions, specifically addressing Phillip Wood's concerns about atomicity and interface design. Karthik Nayak responds to three key points:

1. **Atomicity clarification**: Acknowledges that the files backend isn't truly atomic during multi-ref updates (due to per-file locking), but maintains the reftable backend does provide atomicity through its write-to-new-table-then-commit model. This addresses Phillip's observation about intermediate states being visible.

2. **Transaction purpose**: Agrees transactions should generally be atomic, but frames the partial update support as "exception handling" similar to PostgreSQL's EXCEPTION mechanism, suggesting this is a valid extension of transaction semantics rather than a violation.

3. **Interface design**: While open to Phillip's suggestion of a separate interface for performance batching, expresses concern about code duplication. Requests more detailed suggestions on how such an alternative interface might differ structurally from the current flag-based approach.

The exchange shows the discussion moving from high-level concerns to implementation tradeoffs, with Karthik defending the current design while remaining open to alternatives. Key technical points include the backend-specific atomicity behaviors and the tension between interface purity and implementation sharing. The thread remains unresolved on whether partial updates should be a transaction flag or a separate operation type.