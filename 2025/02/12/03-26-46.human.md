Bugfix in the pickaxe options documentation series.

This is the second patch in a 10-part series introducing long option names for Git's pickaxe search functionality (`-S`/`--patch-modifies` and `-G`/--patch-grep`). The patch fixes a copy-paste error in the help text for the `-G` option, which incorrectly described it as counting regex occurrences (the behavior of `-S`) rather than searching for patches containing matching lines.

The change is minimal - a one-line correction in diff.c to properly describe `-G`'s actual behavior. This appears to be a straightforward documentation fix that aligns the help text with the option's long-standing functionality. The patch is part of a larger effort to improve pickaxe option discoverability and documentation, but stands on its own as a clear bugfix.

No functional changes are introduced - this simply corrects misleading documentation. The patch follows naturally from the series' first patch which cleaned up test terminology, and precedes the core implementation of long option names. Given its focused nature and position in a well-structured series, this change seems uncontroversial and merge-worthy.