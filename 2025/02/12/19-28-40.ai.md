Type-safety refactoring in commit-reach.c, part of a GSOC contributor's learning exercise.

This patch continues Moumita's work to improve type safety in Git's commit graph traversal code, specifically focusing on the `paint_down_to_common()` function in commit-reach.c. The change modifies the `flags` parameter type from `int` to `unsigned int` to better match its actual usage pattern with the PARENT1, PARENT2, and STALE bit flags (defined as `1u << N`). This is a straightforward type correction that makes the code's intent clearer without changing any behavior.

The patch is minimal - just a one-line change to the variable declaration - and follows the same pattern as the contributor's previous type-safety improvements in this area. Since this is purely a type correctness change, no test modifications are needed or included. The change appears technically sound and uncontroversial, aligning with Git's codebase practices for bitwise operations.

As part of a GSOC project, this demonstrates the contributor's growing familiarity with Git's codebase and attention to type-system semantics in low-level operations. The thread currently shows no responses or objections to this change, which appears ready for review and potential inclusion. The patch maintains the series' focus on precise type usage while keeping each change small and reviewable.