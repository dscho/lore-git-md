This is a refactoring patch in the `git-diff-pairs` series, specifically addressing API design questions around the diff queue helpers. Justin Tobler responds to Patrick Steinhardt's review feedback about whether to modify existing functions versus keeping parallel variants, and whether to pass the `diff_queue` explicitly.

The key points are:
1. Justin explains why he kept the original functions (`diff_addremove`/`diff_change`) unchanged - they're used as callbacks in `diff_options` where returning a `diff_filepair` wouldn't make sense (since pruning callbacks don't queue filepairs).
2. He acknowledges Patrick's suggestion about passing the `diff_queue` explicitly for better libification, and agrees to implement this in the next version.
3. The discussion reveals architectural considerations about the diff subsystem's callback mechanisms and the tradeoffs between maintaining backward compatibility versus cleaner API design.

This email moves the thread forward by:
- Clarifying the callback constraints that influenced the initial design
- Accepting the more substantial API modernization (explicit queue parameter)
- Setting up for a v3 that will incorporate both the callback preservation and queue parameter changes

The exchange highlights how this refactoring serves the larger goal of enabling `git-diff-pairs` while also improving the diff subsystem's internal APIs. The next version will need to carefully balance backward compatibility with the desired libification improvements.