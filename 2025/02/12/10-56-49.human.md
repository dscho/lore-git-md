Bugfix patch refining packed-refs sortedness checking in the final stages of a series adding comprehensive validation to `git fsck`.

This is the 7th patch in an 8-part series that's nearing completion, with Patrick Steinhardt and shejialuo working through final implementation details. The patch addresses how to efficiently verify that packed-refs files marked with the "sorted" trait are actually sorted, a key validation step for reference integrity.

The current implementation stores all ref entries in memory for comparison, which Patrick notes could be memory-intensive. shejialuo acknowledges this concern and proposes two alternatives: either keep the current space-inefficient approach that avoids re-parsing, or re-parse the file to compare entries pairwise (saving memory but costing time). After consideration, shejialuo agrees the memory-optimized approach is better and will revise the patch to implement pairwise comparison during the initial parse.

This refinement follows earlier discussion about header validation and error reporting in the series. The change aligns with the series' goal of thorough but efficient reference validation while maintaining clean separation between different validation checks (format vs sortedness). The patch appears well-considered, with the author thoughtfully weighing tradeoffs before settling on the more scalable solution.