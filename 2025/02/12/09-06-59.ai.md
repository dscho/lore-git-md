This is a review comment on the first patch of the `git-diff-pairs` series, which refactors diff queue internals to expose `diff_filepair` access. Justin Tobler suggests an alternative approach to the patch's implementation strategy.

The reviewer proposes replacing the existing `diff_addremove()` and `diff_change()` functions entirely with the new variants that return `diff_filepair` pointers, rather than keeping both sets of functions. They note there are few callers of the original functions, making this a feasible simplification. The suggestion aims to reduce API surface area while maintaining the same functionality.

The review also includes a nit about adding documentation comments for the new functions to clarify their purpose. The overall tone is constructive, acknowledging the patch's correctness while offering an architectural improvement suggestion.

This feedback comes late in the thread's lifecycle (after the series was already marked complete) and focuses on potential API design refinements rather than functional issues. The core functionality of exposing `diff_filepair` access remains uncontested, with the discussion now shifting to interface ergonomics.