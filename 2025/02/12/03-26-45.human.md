This is a minor test cleanup patch in the pickaxe long-options series. It fixes a naming inconsistency in the test script where `-G` (which takes a regex) was being tested with a dummy value of "string" rather than "regex" like other similar tests. The change has no functional impact but improves consistency in test naming.

As the first patch in v5 of the series, this appears to be a straightforward preparatory change before introducing the main feature (long option names for pickaxe search). The author notes this matches existing conventions where `-G` tests elsewhere use "regex" as the placeholder value. 

The patch is small and focused, modifying just one line in the test script. It demonstrates attention to detail in test hygiene, though the impact is purely cosmetic. Given its position early in the series, this seems to be setting up cleaner test infrastructure before the more substantive changes that follow.