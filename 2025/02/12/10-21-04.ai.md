This is the final patch in a bugfix series adding packed-refs validation to `git fsck`. The patch implements the `--[no-]references` option integration, completing the architectural work started in earlier patches.

The email shows shejialuo responding to Patrick Steinhardt's review feedback on the documentation and testing aspects. Key points addressed:
1. Documentation will be updated to explicitly state the default behavior (checking references) for the new `--[no-]references` option
2. Additional test cases will be added to verify the flag's behavior, likely using trailing contents checks

The patch builds on the series' established architecture where `git fsck` spawns a child process running `git refs verify` to check reference consistency. This final piece ties together the validation work with user control while maintaining backward-compatible defaults. The implementation appears thorough, addressing both the core functionality and now the documentation/testing polish requested in review.