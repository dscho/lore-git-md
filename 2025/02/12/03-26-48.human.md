This is part of the ongoing pickaxe long-option series, specifically updating the short help text in diff.h to include both the short (`-G`) and long (`--pickaxe-grep`) forms of the regex-based pickaxe search option. 

The patch makes two straightforward changes to the help text: it adds the `-G` option description that was previously missing, and includes the new `--pickaxe-grep` long option as an alias. It also updates the `--pickaxe-all` description to clarify it works with both `-G` and `-S` options.

This is a small but important piece of the larger effort to make pickaxe options more discoverable through long option names while maintaining backward compatibility. The change is cleanly implemented and consistent with the series' goal of keeping both old and new forms working identically. 

As part of a well-structured series that's now in its fifth iteration, this patch appears ready for inclusion, with the technical aspects already vetted in previous rounds. The help text updates here complement the more substantial documentation fixes in earlier patches of the series.