This is a documentation bugfix in the pickaxe options series, specifically correcting the help text for `-S` in diff.h.

The patch fixes the short help description of `-S` (pickaxe search) to accurately describe its behavior. The old text incorrectly suggested `-S` only finds filepairs where one side completely lacks the string, while the new text correctly states it finds differences in the number of string occurrences. This matches the actual implementation where `-S` detects any change in occurrence count, not just presence/absence.

The change is minimal - a single line in diff.h updating the help string. This is part of a larger series introducing long option names for pickaxe search, with this particular patch addressing an existing inaccuracy that was noticed during the review process. The correction ensures users get accurate information about `-S`'s behavior when viewing the short help output.

Key technical details:
- Updates the `-S` description in diff.h's built-in help text
- Changes from "only one side contains" to "differ in number of occurrences"
- Part of patch 3/10 in the v5 series
- Follows from earlier discussion about accurately documenting pickaxe behavior
- No functional changes to the `-S` option itself
- Maintains consistency with the more detailed documentation in the man pages
- Helps prevent user confusion about the pickaxe search's exact matching behavior

The patch represents a straightforward documentation improvement that was identified and fixed as part of the broader pickaxe options work. It's a small but meaningful correction to ensure the help text properly reflects the actual functionality.