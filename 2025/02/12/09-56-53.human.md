This is a review comment on the final patch of a bugfix series that adds packed-refs validation to `git fsck`. Patrick Steinhardt is providing feedback on the documentation and testing aspects of the new `--[no-]references` option.

The patch being reviewed adds the new option to `git fsck`'s documentation but doesn't specify the default behavior (which is to check references by default). Patrick suggests two improvements: explicitly documenting the default behavior in the man page, and adding tests to verify the flag's functionality.

This is a minor but important polish to the series, addressing documentation completeness and test coverage for the new feature. The feedback comes late in the review cycle (v3 8/8) and focuses on final touches rather than core functionality, suggesting the technical implementation is otherwise sound. The series as a whole appears to be nearing completion, with this being one of the last remaining items to address.