Final patch in a bugfix series adding packed-refs validation to `git fsck`. This patch completes the integration by adding a `--[no-]references` option to control reference verification, with the default being to check references (matching historical behavior). The author, shejialuo, responds to Patrick Steinhardt's review by agreeing to improve the documentation to explicitly state the default behavior and add tests for the new flag's functionality.

This wraps up an 8-part series that has evolved through multiple rounds of review, now providing comprehensive validation of packed-refs files from file access through content structure. The implementation uses a child process running `git refs verify` to perform the actual checks, with proper flag propagation and error reporting back to `git fsck`. The series appears well-tested and addresses all major technical components, with just minor documentation and test improvements remaining based on this final review feedback.