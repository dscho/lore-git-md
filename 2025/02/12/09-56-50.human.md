This is a review response in the ongoing bugfix series for packed-refs validation in `git fsck`. Patrick Steinhardt provides two specific suggestions to improve patch 6/8, which adds content consistency checking for packed-refs entries.

First, he notes that the current implementation unnecessarily computes a descriptive string for every line in the packed-refs file, even though it's only needed for error reporting. He suggests passing the line number directly instead, which would be more efficient since most lines won't trigger errors.

Second, he points out that the test case could be simplified by using HERE documents rather than multiple `printf` statements to construct the test packed-refs file. This would make the test more readable and maintainable while achieving the same test coverage of malformed packed-refs entries.

These are relatively minor optimizations to an otherwise solid patch that's part of a larger effort to validate packed-refs files more thoroughly. The review maintains the positive trajectory of the series while catching some small opportunities for improvement in both performance and code clarity.