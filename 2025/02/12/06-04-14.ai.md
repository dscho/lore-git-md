Refactoring patch in the xdiff warning-handling series, focusing on signed/unsigned comparisons. This second patch in the 6-part series addresses a specific warning case in xdiffi.c, changing a loop counter from `int` to `size_t` to avoid signed/unsigned comparison warnings when checking against `ignore_regex_nr`. The change is straightforward and localized to the `record_matches_regex()` function, removing the now-unneeded `DISABLE_SIGN_COMPARE_WARNINGS` macro from this file (which was moved in patch 1/6). The commit message explains the rationale clearly - the variable is only used for non-negative comparisons with size_t values. This continues the series' theme of cleaning up warning suppression by addressing root causes rather than suppressing warnings globally. The diff shows a simple type change with corresponding macro removal, maintaining the same logic while improving type safety. No behavioral changes are introduced. Key files touched are xdiff/xdiffi.c, continuing the xdiff subsystem focus from patch 1/6.