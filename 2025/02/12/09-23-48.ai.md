This is the third and final patch in a series implementing the new `git-diff-pairs` plumbing command, adding explicit control over when diffs are output. The patch allows triggering a diff queue flush by sending a NUL byte between file pairs in addition to the existing EOF-based behavior, with output batches separated by line terminators.

Patrick Steinhardt provides review feedback focusing on documentation and implementation details. He suggests avoiding internal terminology like "flushing" and "diff queue" in user-facing docs, preferring descriptions of observable behavior. He also notes several technical improvements: using `fputc()` instead of `fprintf` for terminator output, fixing typos ("nul" → "NUL"), and maintaining consistent terminology in tests.

The implementation adds the flush control while preserving all existing functionality. Tests verify both regular and NUL-terminated output modes work correctly with explicit flushes. The changes are contained to the command's asciidoc documentation, core processing logic in builtin/diff-pairs.c, and expanded test coverage in t4070.

This completes the series' goal of enabling batch processing for server-side use cases. The review feedback presents opportunities to improve documentation clarity and code quality before finalization, but the core functionality appears sound with comprehensive test coverage. The thread's remaining open questions center on API design choices and documentation polish rather than fundamental behavior.