This is a documentation patch in the pickaxe long-options series, specifically updating help text to include the new long option names alongside their short forms.

The patch modifies both diff.c and diff.h to consistently show the dual forms `-S/--patch-modifies` and `-G/--patch-grep` in help messages. The changes ensure users see both variants when reading about pickaxe-related options, making the long forms more discoverable while maintaining backward compatibility with the short forms. The help text updates cover three key pickaxe-related options: `--pickaxe-all`, `--pickaxe-regex`, and the core pickaxe functionality itself.

The changes are straightforward textual updates that don't affect behavior, just improving the documentation to reflect the dual-form support added earlier in the series. The patch maintains the existing technical accuracy while adding the new option names - for example, keeping the precise description of how `--pickaxe-regex` modifies `-S/--patch-modifies` behavior.

This appears to be one of the final documentation cleanups in the series, following the earlier structural documentation changes. The patch is small and focused, just updating the help text strings to match the dual-form approach established in the implementation. It's a logical addition to the series that completes the documentation side of the long-option support.