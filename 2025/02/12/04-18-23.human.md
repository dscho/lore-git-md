This is a refactoring patch that's part of a series introducing a new `git-diff-pairs` plumbing command. The patch modifies the diff core to expose the queued `diff_filepair` structs after they're added to the diff queue.

The change splits the existing `diff_addremove()` and `diff_change()` functions into two layers: the original functions now become thin wrappers around new `diff_filepair_addremove()` and `diff_filepair_change()` functions that return pointers to the queued pairs. This prepares for future patches that will modify the queued diffs after they're created.

The implementation is straightforward - it preserves all existing behavior while adding the new return values. The patch touches core diff functionality but doesn't change any observable behavior yet. It's a clean preparatory change with comprehensive updates to both the implementation and header files, maintaining the existing coding style.

This is the first of three patches in the series, setting up infrastructure needed for the new batch diff processing command described in the thread summary. The changes are well-contained and focused on their stated purpose.