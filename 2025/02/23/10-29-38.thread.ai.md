Git thread summary (AI mode):

Performance investigation of `git diff --find-renames` slowdown when path filtering is applied to merge commits containing large modified files. Thread examines a pathological case where rename detection takes 4+ minutes (vs <1s expected) even when filtered paths exclude all possible rename candidates. Originating report from Devste Devste identified the issue occurs with single modified files and no actual renames, suggesting unnecessary rename detection. Elijah Newren's follow-up clarifies that diffcore-rename.c already contains optimizations to skip processing when no additions/deletions exist, implying the problem lies in the interaction between path filtering and rename detection machinery. Key open questions center on precise reproduction conditions: whether "modified files" are relative to one or all merge parents, if changes include any additions/deletions beyond pure modifications, and exact test setup triggering the slowdown. Technical focus areas include the diffcore-rename logic's behavior when path limiting is active and potential optimizations similar to those used for `--follow-renames`. Current understanding suggests the path filtering may be causing rename detection to run on the full diff before applying filters, rather than optimizing based on filtered content. Next steps require more detailed reproduction details from Devste to isolate whether the issue stems from merge commit handling, path filter application timing, or edge cases in the existing skip-detection heuristics. The thread remains active with participants yet to address whether similar optimizations from the `--follow-renames` implementation could be adapted to this case.