Continuing the test modernization series converting oid-related tests to Clar framework, this patch focuses on the oidmap test suite. The conversion follows the same pattern as the previous oid-array patch, restructuring the test into Clar-style assertions and initialization patterns.

Key changes include:
- Renames t-oidmap.c → u-oidmap.c per Clar conventions
- Replaces custom test assertions with `cl_assert` macros
- Splits tests into focused cases (replace, get, remove, iterate)
- Moves test data to global scope with explicit `test_oidmap__initialize()` and `test_oidmap__cleanup()`
- Uses `cl_parse_any_oid()` helper from earlier in the series
- Updates both Makefile and meson.build build systems
- Net change: -49 lines (153 → 104)

The patch demonstrates good test coverage of oidmap functionality including insertion, lookup, replacement, removal, and iteration. The conversion appears thorough, maintaining all the original test cases while adopting the Clar framework's patterns. The changes align with the series' goal of standardizing test infrastructure and reducing code.

This continues the pattern established in patch 2, setting up for the remaining oidtree conversion in subsequent patches. The build system changes show the practical application of the infrastructure introduced earlier in the series.