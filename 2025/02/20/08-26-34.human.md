This appears to be a bug report regarding unexpected behavior with `git clone --shallow-exclude`. The reporter identifies two distinct error cases when using this parameter in Git 2.45.2.

In the first case, specifying a branch name (like `master`) with `--shallow-exclude` results in a "no commits selected" error. The second case shows that using a commit SHA-1 instead produces an "ambiguous deepen-not" error. Both scenarios suggest the shallow exclude functionality isn't working as expected when trying to exclude specific commits from a clone operation.

The report is concise but provides the essential details needed to investigate: exact error messages, Git version (2.45.2), and the operating system (CentOS 7). While it doesn't include reproduction steps beyond the basic command syntax, the core issue is clearly described. This looks like a legitimate bug report that would warrant investigation by someone familiar with Git's shallow clone implementation.