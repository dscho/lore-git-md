This is a bug report about unexpected behavior in `git clone --shallow-exclude`. The reporter identifies two distinct error cases when using this parameter in Git 2.45.2.

The report describes two failure modes: when excluding by branch name ("master"), Git fails with "no commits selected", while when excluding by commit hash, it produces an "ambiguous deepen-not" error. Both cases suggest the shallow-exclude functionality isn't working as expected, though the commit-hash case appears to trigger a different code path with its own distinct error message.

The report is concise but provides the essential technical details needed to investigate - the exact Git version (2.45.2), the complete error messages, and clear reproduction cases. The reporter doesn't speculate about root causes or propose fixes, sticking to observable behavior. This appears to be a legitimate bug report rather than user error, as the commands follow documented usage patterns but produce unexpected errors.

The errors suggest either a regression in shallow clone handling or possibly an edge case that wasn't properly tested. The "ambiguous deepen-not" message in particular hints at potential issues in the protocol negotiation between client and server during shallow clones. The report would benefit from maintainers familiar with the shallow clone internals to determine whether this is a client-side bug, server-side issue, or expected behavior that needs better documentation.