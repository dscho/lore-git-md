This is an implementation discussion about the `git diff-pairs` plumbing command, specifically regarding its handling of tree objects in the input. The email continues the thread's focus on defining clear behavioral contracts for this new command.

Justin Tobler proposes tightening the current behavior by making `diff-pairs` fail explicitly when encountering tree objects in its input, rather than silently passing them through. This would create a clearer contract and prevent future compatibility issues if tree expansion support is added later via a `-r` option. The change would make the current limitation more visible to users while preserving the option to implement tree expansion in a backward-compatible way later.

The discussion builds on previous agreement that the initial implementation should stay simple, with tree expansion being a potential future enhancement. This email suggests an implementation strategy that enforces that simplicity more strictly by failing early rather than handling tree objects in potentially surprising ways. The proposal aligns with the thread's established principle of keeping the backend simple while allowing for future extension points.

Key technical details include:
- Current behavior passes tree objects through unexpanded
- Proposed change would make this an explicit error case
- Future `-r` option remains reserved for tree expansion
- Maintains the command's position as a simple diff generator in the pipeline

This represents a refinement of the implementation approach rather than a major design change, continuing the thread's progression toward a well-defined plumbing command contract.