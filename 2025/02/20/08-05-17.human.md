Performance optimization follow-up in the refs subsystem. This is a brief response from Patrick Steinhardt to Justin Tobler's review of patch 3/16 in the performance optimization series for refname availability checks. 

The exchange confirms that one remaining instance of `repo_get_oid()` in `parse_cmd_symref_update()` was unintentionally left unconverted to the new `repo_get_oid_with_flags()` API with the `GET_OID_SKIP_AMBIGUITY_CHECK` flag. Patrick acknowledges this as an oversight and indicates it will be fixed, completing the conversion work started in this patch.

This is a minor cleanup in the context of the larger series, which aims to optimize refname availability checks across different ref backends. The series has been merged with measurable performance improvements, and this exchange represents post-merge review feedback being addressed. The change is straightforward and uncontroversial, simply bringing one remaining API call in line with the rest of the conversions made in this patch.