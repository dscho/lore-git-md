This thread reports unexpected behavior in `git clone --shallow-exclude` in Git 2.45.2. The reporter found two distinct failure modes: excluding by branch name ("master") fails with "no commits selected", while excluding by commit hash produces an "ambiguous deepen-not" error. Both cases suggest the shallow-exclude functionality isn't working as expected, with the commit-hash case triggering a different code path.

The report provides clear reproduction steps and exact error messages, making this a well-documented bug report rather than user error. The errors could indicate either a regression in shallow clone handling or an untested edge case. The "ambiguous deepen-not" message in particular suggests potential protocol negotiation issues during shallow clones. This will need investigation by maintainers familiar with the shallow clone internals to determine if this is a client-side bug, server-side issue, or simply a case that needs better documentation.