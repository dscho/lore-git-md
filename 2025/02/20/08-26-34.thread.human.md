Here's the updated human-readable summary:

This thread examines limitations in Git's `--shallow-exclude` feature, particularly in GitLab CI environments. The discussion began with two issues: a bug in commit hash exclusion (fixed in Git 2.48) and a workflow limitation where temporary merge commits in GitLab's "Merged Result Pipelines" break shallow exclusion ranges. The core problem is that `master..temp` only includes the merge commit, missing the actual changes needing testing.

Participants have confirmed the technical constraints, with D. Ben Knoble and Philipp Hahn working through the specifics of GitLab's pipeline behavior. While blobless clones (`--filter=blob:none`) emerged earlier as a viable workaround, this latest exchange explores how to structure commit ranges within GitLab's constraints - suggesting using `master..topic` for linting individual commits while optionally including the temporary merge for merge-specific checks.

The thread has largely converged on understanding both the protocol limitations (where commit exclusions create crafted root commits) and practical workarounds. While no new technical solutions are proposed here, the discussion helps clarify how to best use existing features in complex CI scenarios. Documentation updates or protocol improvements may follow, but for now the blobless clone approach appears to be the most practical solution despite fetching more history than strictly necessary.