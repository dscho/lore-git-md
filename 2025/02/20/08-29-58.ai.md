Test modernization converting oidtree tests to Clar framework. This is the fourth patch in a 5-part series standardizing object ID handling tests (oid-array, oidmap, and now oidtree) by migrating them from custom test code to the Clar unit testing framework.

The patch follows the established pattern from previous conversions in the series:
- Renames t-oidtree.c → u-oidtree.c per Clar conventions
- Restructures tests using Clar patterns:
  - Replaces custom assertions with `cl_assert` macros
  - Splits into focused test cases (contains, each)
  - Moves test data to global scope with explicit initialize/cleanup
  - Uses `cl_parse_any_oid()` helper from earlier in series
- Updates both Makefile and meson.build
- Net change: -50 lines (78 → 28) while maintaining all test coverage

Key technical details:
- Introduces `test_oidtree__initialize` and `test_oidtree__cleanup` for setup/teardown
- Maintains same test coverage for oidtree operations (insertion, contains checks, iteration)
- Uses same SHA-1/SHA-256 compatibility approach as previous patches
- Follows same build system integration pattern (Make + Meson)
- Completes the oid-* test conversions after oid-array and oidmap

The conversion appears thorough and consistent with the series' approach. With this patch, the oidtree tests now follow the same patterns established for oid-array and oidmap, completing the standardization of these core data structure tests. The remaining patch in the series will presumably handle any final cleanup or documentation.