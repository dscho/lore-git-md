This is a technical discussion about cross-compilation path handling in Git's Meson build system, specifically addressing how shell and Perl paths are detected and passed between host and target environments.

Patrick Steinhardt responds to Peter Seiderer's concerns about the current Meson behavior where host paths leak into target builds. The core issue revolves around `find_program()` usage in Meson.build, particularly lines 186 (shell detection) and 685 (SHELL_PATH definition). Patrick clarifies the intended behavior of Meson's `native:` parameter in `find_program()`, quoting documentation showing it should properly separate host/target paths when using a cross file.

Two alternative solutions are proposed: introducing build options for path specification, or adding cross-file properties via `meson.get_external_property()`. Patrick also notes a correction to Peter's suggested diff regarding `target_perl.found()` checking. The discussion remains focused on technical implementation details, with Patrick CC'ing Eli for additional input, indicating this is an active design discussion rather than a settled solution.

The email advances the thread by providing concrete technical alternatives while acknowledging the complexity of proper path separation in cross-compilation scenarios. It maintains the thread's focus on build system internals while moving toward potential solutions that could satisfy both Buildroot and Yocto use cases.