This is a documentation patch in Phillip Wood's v2 series (4/5) improving `git merge-tree --stdin` behavior. It adds two key clarifications to the manual page: first, it explicitly documents that `--stdin` implies `-z` mode (which affects how output is parsed), and second, it corrects the status code documentation to note that negative values (indicating errors) cause the command to terminate before producing any merge results. The changes are straightforward and align with the thread's ongoing effort to precisely document `--stdin` behavior, following earlier code fixes for deadlock issues and configuration handling. This patch appears to be the final polish on the documentation aspects of the series, with clean diffs and clear explanations.