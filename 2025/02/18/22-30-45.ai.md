This is a discussion about NFSv4 compatibility issues with Git's packfile handling, continuing the thread about whether Git should adapt to NFSv4's non-POSIX behavior during failover recovery.

Brian M. Carlson responds to Bryan Maloney's point about NFSv4 specifications, suggesting that the NFS server could handle this case better during state recovery rather than requiring Git to change. They propose potential server-side solutions that could maintain POSIX-compliant behavior while working around NFSv4's limitations, mentioning this could be implemented in Linux's in-kernel NFS server or userspace servers with careful permission handling.

The email raises serious concerns about the implications of accepting NFSv4's behavior, particularly around potential repository corruption. Carlson argues that allowing a filesystem to spontaneously produce EBADF errors during operations creates dangerous race conditions where Git might accidentally write to the wrong file descriptor if another thread reuses the number after an unexpected closure. They emphasize that while this specific issue might be worked around, tolerating such behavior could lead to more edge cases and potential data loss.

The response maintains the thread's position that this is fundamentally an NFSv4 standards compliance issue rather than a Git bug, but goes further to argue that the NFSv4 behavior creates unacceptable risks for Git's data integrity. Carlson suggests server-side solutions would be preferable to modifying Git's packfile handling, which relies on POSIX guarantees for atomic file replacement. The technical discussion focuses on file descriptor management and the potential for repository corruption if Git were to accommodate NFSv4's non-POSIX behavior.