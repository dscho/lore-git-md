Junio Hamano's response to the commit-slab refactoring patch for `show-branch.c` provides technical guidance while expressing enthusiasm for the direction. He clarifies that the patch is correctly scoped to just `show-branch.c` (not broader commit traversal changes) and strongly supports lifting the `MAX_REVS` limitation. 

Key technical points raised:
- The slab storage should use a bitfield array rather than a single `int` to properly support >32/64 revs
- Suggests revised API signatures using `unsigned flags[]` arrays
- Recommends adding new tests demonstrating handling of >`MAX_REVS` cases
- Notes historical context about the long-standing `MAX_REVS` limit

The response is constructive but makes clear the current implementation doesn't fully achieve the goal of removing the rev limit. Hamano provides specific suggestions for the slab interface design while maintaining a positive tone about the overall approach.