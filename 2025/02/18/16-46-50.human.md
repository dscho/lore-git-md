This is a review ack for a bugfix and cleanup series targeting `git merge-tree --stdin`. Elijah Newren confirms Phillip Wood's v2 patch series is ready for inclusion after incorporating feedback on the documentation of exit status behavior.

The series addresses a deadlock issue in `merge-tree --stdin` mode where missing stdout flushes could block scripts processing output incrementally. It also includes several cleanups: removing redundant error handling, properly scoping merge configuration to basic plumbing needs, and improving documentation clarity around `--stdin` behavior and exit codes.

Newren's ack indicates the series has been thoroughly reviewed through multiple iterations, with all functional changes and documentation updates now in good shape. The changes maintain a clear separation between plumbing and porcelain behavior while fixing a real-world usability issue for scripted merge operations. The performance impact of the deadlock fix was previously benchmarked at less than 0.5% overhead.

This appears to be the final approval needed before the series can be merged, wrapping up a collaborative effort between Newren (merge expert) and Wood (sequencer expert) to refine both the implementation and documentation.