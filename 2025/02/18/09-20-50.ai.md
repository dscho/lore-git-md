This is patch 14/18 in Patrick Steinhardt's v6 series to decouple the reftable library from Git core dependencies. The patch introduces a reftable-specific `REFTABLE_UNUSED` macro to replace Git's `UNUSED` attribute annotation throughout the reftable codebase.

The change systematically replaces all instances of `UNUSED` with `REFTABLE_UNUSED` across multiple files (blocksource.c, iter.c, record.c, writer.c) while maintaining the same functionality. The macro is defined in basics.h alongside the existing `MAYBE_UNUSED` definition, following the same unconditional approach. This continues the series' pattern of removing Git-specific utilities in favor of reftable-native implementations.

The patch affects function parameters marked as unused across various reftable subsystems, including block source handling, iterator implementations, record encoding/decoding, and writer utilities. Each change is straightforward - a direct substitution of the annotation macro while preserving all existing parameter attributes. The implementation follows the same pattern seen in earlier patches of the series, maintaining consistency in how Git dependencies are being removed.

This is a mechanical change that doesn't introduce new functionality or behavior, but contributes to the series' overarching goal of making the reftable library usable outside of Git by eliminating all Git-specific dependencies. The patch appears well-scoped and follows the established pattern of the series, with no obvious technical concerns.