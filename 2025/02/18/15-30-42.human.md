This is a straightforward build system update that aligns the Meson build's Perl version requirement with Git's main Makefile-based build system. 

The patch updates the minimum Perl version requirement in `meson.build` from 5.8.1 to 5.26.0, matching the requirement that was already established in the Makefile build system back in October 2024 (commit 702d8c1f3b). This is part of a two-patch series that has gone through several review iterations and is now in its final form.

The change is minimal - just a one-line modification to the version string in the Perl version check. The patch maintains all the existing logic around Perl feature detection and the `NO_PERL` configuration, only updating the version requirement to match Git's current standards.

This appears to be a clean, well-justified change that brings consistency between Git's two build systems. The patch has already been reviewed and refined through multiple versions of the series, with all feedback incorporated. As part of the final v3 series, it's likely ready for merging.