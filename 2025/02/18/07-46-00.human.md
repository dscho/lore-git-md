**CI / build system** - Final patch in Patrick Steinhardt's series adding Meson support for Git's contrib components, focusing on credential helper testing.

This patch completes the Meson build system series by ensuring credential helpers are properly exercised in CI runs. It modifies GitHub Actions, GitLab CI, and the Linux/macOS test infrastructure to build and test credential helpers across platforms: enabling wincred on Windows builds, libsecret/netrc on Linux, and osxkeychain on macOS. The changes include adding libsecret as a dependency for Linux builds and adjusting Meson configuration flags in all CI environments.

The patch follows the established pattern seen throughout this series - systematically addressing each component while maintaining cross-platform consistency. As the final piece, it ensures the credential helper infrastructure (previously prepared in earlier patches) is actually validated in automated testing. The changes are straightforward configuration updates that match the series' careful attention to build system parity and test coverage.

This concludes a well-executed effort to bring Meson support to Git's contrib components, with each patch methodically handling a different subsystem while maintaining build consistency across Makefile, CMake and Meson. The series appears ready for integration, having addressed all major contrib areas with appropriate platform-specific considerations.