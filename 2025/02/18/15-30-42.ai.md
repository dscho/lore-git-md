This is a build system patch that finalizes the Perl version requirement alignment between Git's Makefile and Meson build systems, part of Peter Oliver's completed series. The change updates Meson's minimum Perl version from 5.8.1 to 5.26.0, matching the Makefile's requirement established in commit 702d8c1f3b. 

The patch is straightforward, modifying just one line in meson.build to enforce the newer Perl version requirement. The commit message clearly ties this change to prior Makefile standardization work, providing necessary context for the version bump. This change was previously discussed in the thread and represents the final technical implementation after review feedback was incorporated.

Key technical details include the version check remaining conditional on Perl being required (perl_required), maintaining the existing behavior where Perl is optional for core Git functionality. The change affects only the build system's requirements check, not runtime behavior. The patch preserves all other aspects of Perl detection and feature enablement logic while updating the version floor.

This patch is the first of two in the series, with the second patch focusing on documentation. The thread context shows this change has already undergone review and reached consensus, with all technical concerns addressed in previous iterations. The implementation is minimal and focused, touching only the version requirement while leaving the surrounding build infrastructure unchanged.