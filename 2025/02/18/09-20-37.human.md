This is a refactoring patch in Patrick Steinhardt's ongoing series to reduce the reftable library's dependency on Git-specific code (part 1 of 18). The change replaces a single use of `read_in_full()` with direct file reading logic in the reftable stack implementation.

The patch open-codes the file reading functionality to avoid pulling in Git's `read_in_full()` helper. The new implementation is slightly simplified since it doesn't need to handle non-blocking I/O (which the reftable code doesn't use anyway). The change appears straightforward and maintains the same error handling behavior for the cases that matter to reftable.

This is a typical dependency-reduction change - not particularly exciting on its own, but part of a larger effort to make the reftable library more self-contained. The implementation looks correct and the commit message clearly explains the partial porting of functionality. Readers interested in the reftable independence work would want to follow the rest of this series.