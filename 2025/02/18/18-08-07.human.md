This is a discussion about implementing commit summary formatting in `git blame`, building on Aleks Todorov's feature proposal. Junio Hamano responds to concerns about reusing `git log`'s formatting infrastructure, particularly around alignment and sanitization of user input.

Hamano suggests that alignment could be the user's responsibility if they're using format strings with the existing %</%> specifiers from `git log`, though he notes uncertainty about how cleanly those alignment features could be reused. He also raises the possibility that GUI tools might be better served by using `--incremental` output rather than custom formatting.

The exchange highlights key architectural questions in the proposal: whether to fully reuse `git log` formatting (with its alignment capabilities) or implement a custom solution, and how different use cases (CLI vs GUI) might affect the design. Hamano's response indicates these are valid concerns but doesn't take a firm position, instead seeking to better understand the motivation behind the proposed feature.