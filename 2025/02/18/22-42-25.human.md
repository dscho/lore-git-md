This is a bugfix patch for gitk addressing filename encoding issues, specifically for Japanese filenames during merge conflicts. It's the first of a two-part series.

The patch makes a straightforward change to the gitk Tcl/Tk script, explicitly setting the system to use UTF-8 encoding by adding `encoding system utf-8`. This appears to be a foundational fix that ensures proper handling of non-ASCII filenames throughout the interface, not just in merge conflict scenarios.

The change is minimal (just one line added) and focused on the gitk graphical interface rather than core Git functionality. While the commit message is quite brief, the technical approach seems reasonable - forcing UTF-8 encoding in Tcl/Tk should help prevent garbled display of Japanese and other non-ASCII filenames.

This appears to be a targeted fix for a specific pain point that affects users working with non-English filenames. The simplicity of the change suggests it's either a well-understood issue in Tcl/Tk applications or that the author has identified a clear solution to the encoding problems.

As the first in a series, this patch lays groundwork that the second patch (which specifically targets Japanese filenames in merge conflicts) will presumably build upon. The changes are contained entirely within gitk's script, so they shouldn't affect other Git operations or interfaces.