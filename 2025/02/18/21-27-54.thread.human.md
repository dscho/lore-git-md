This thread concerns a broken link on git-scm.com where the v2.48.1 release notes point to a `.txt` file that no longer exists due to the recent migration of documentation to `.adoc` format (commit 1f010d6bdf). Bede Skinner-Vennell originally reported the issue, noting that while the tagged `.txt` version still works, the website should be updated to reference the new `.adoc` file in `master`. 

Christian Couder has since responded, clarifying that git-scm.com is managed separately on GitHub (git/git-scm.com) and suggesting the issue be filed there. He confirms the format transition as the root cause but notes this falls outside the mailing list's scope. While the technical solution is straightforward—updating the website's link generation—the actual fix must be handled through the git-scm.com repository. The thread remains open in the sense that the website still needs updating, but the discussion here has run its course with the redirection to the proper venue.