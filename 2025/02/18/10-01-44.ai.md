This is a refactoring patch in the ongoing `git merge-tree --stdin` series. Phillip Wood removes redundant error handling code in the stdin processing path of `builtin/merge-tree.c`, since `real_merge()` can only return 0 or 1 (with negative values already handled via die() earlier). Elijah Newren suggests expanding the commit message to clarify that while the return value is unused in stdin mode (where conflicts are reported via output), it remains relevant for non-stdin case. The change itself is straightforward - removing an unused variable and dead code path while maintaining identical behavior. This appears to be a final polish in the series after the core deadlock fixes and documentation improvements.