This is a response in the ongoing promisor-remote/LOP series discussion, specifically addressing security concerns around remote name matching. Christian Couder clarifies that the current implementation does not actually let servers control remote names - it only performs matching between client-configured remotes and server-advertised ones based on the `promisor.acceptFromServer` setting. He pushes back against Patrick Steinhardt's concerns about protocol rigidity, arguing the current approach uses existing information and meets many use cases while remaining simple.

The exchange reveals an important security distinction: while the server can't force name assignments, the "KnownName" matching option could still potentially allow malicious servers to exploit name collisions if clients have loosely configured promisor remotes. Christian acknowledges the ID-based alternative proposal but suggests it belongs in a follow-up series, offering to drop the current patch if preferred. This keeps the discussion focused on whether the current security model is sufficient for initial rollout versus needing more fundamental changes.