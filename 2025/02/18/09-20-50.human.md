This is part of Patrick Steinhardt's ongoing series to separate the reftable library from Git core dependencies. Patch 14/18 introduces a reftable-specific `REFTABLE_UNUSED` macro to replace Git's `UNUSED` attribute annotation for marking intentionally unused function parameters. 

The change is straightforward - it defines the new macro in reftable/basics.h and updates all existing uses of `UNUSED` across the reftable codebase to use this reftable-native version instead. The patch follows the series' pattern of systematically replacing Git-specific components with reftable equivalents, though this particular change is more about naming consistency than functional impact.

The patch touches 5 files with simple mechanical replacements, maintaining the same behavior while removing another small Git dependency. Like other patches in the series, this appears to be a well-contained change with no user-facing effects, just internal code hygiene as part of the larger reftable independence effort.