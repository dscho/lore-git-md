This is a follow-up in the ongoing discussion about `format.from` configuration affecting `git show --format=email`. Junio Hamano responds to Antonin Godard's argument that `git show` already uses some `format.*` configs via `git_log_config()`, maintaining his position that these were specifically designed for `git format-patch`.

Key technical points in this response:
- Cites two 2022 commits (34bc1b10, d5fc07df) that introduced the `format.from` and `format.forceInBodyFrom` features specifically for `format-patch`
- Notes that shared `log-config` structure usage doesn't imply all commands use all fields
- Expresses concern about backwards compatibility if behavior changes
- Hypothetically agrees the design might have been different if starting fresh

The email advances the discussion by:
1. Providing concrete historical context for the original implementation
2. Clarifying that config sharing in code doesn't necessarily imply intended behavior sharing
3. Introducing the backwards compatibility concern as a new consideration
4. Offering a thought experiment about alternative designs

Open questions now include:
- Whether historical design intent should override current practical needs
- How to weigh backwards compatibility against b4 tool integration
- Whether the partial `format.*` support in `git show` was intentional

The tone remains technical but becomes more conciliatory, with Junio acknowledging the theoretical merit of Antonin's position while maintaining practical objections. The thread appears to be moving toward needing either:
- A demonstration that the backwards compatibility impact would be minimal
- Or an alternative solution that preserves existing behavior while fixing b4