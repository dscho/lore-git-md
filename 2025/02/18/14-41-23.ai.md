This is a technical discussion about cross-compilation path handling in Git's build systems, specifically around shell path detection in Meson builds. Eli Schwartz weighs in with Gentoo-specific perspective on the `/bin/sh` vs `/usr/bin/sh` issue.

Eli clarifies that `/bin/sh` is the portable POSIX-mandated path that should work across all Unix systems, though notes Solaris has a special case with `/usr/xpg4/bin/sh`. He argues that for shell path detection specifically, defaulting to `/bin/sh` is the right approach since it works across both merged-usr and split-usr systems (relevant for Gentoo binary packaging).

The email provides important real-world context about how this affects non-cross-compilation scenarios in Gentoo, where build-time paths can leak into installed binaries in ways that cause problems on differently-configured target systems. Eli notes they'll need to address this in Gentoo's packaging regardless of the upstream solution.

Key technical points:
- Confirms Patrick's approach of using `native: false` with cross files would work for this specific case
- Suggests build options as an alternative to cross-file properties
- Highlights the `/bin/sh` path as a special case that should be hardcoded by default
- Introduces Gentoo's split-usr vs merged-usr considerations as another dimension to the path handling problem

The discussion continues to refine the technical approach while expanding the scope of real-world use cases that need to be considered (now including Gentoo's packaging requirements alongside Buildroot and Yocto). Eli's input provides valuable perspective on non-cross-compilation scenarios where the same path separation issues manifest.