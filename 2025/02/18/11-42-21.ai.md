This is a follow-up discussion in the promisor-remote/LOP series (v5) focusing on security considerations and implementation details of the promisor remote acceptance mechanism. Christian Couder responds to Junio Hamano's feedback about the security model and URL comparison behavior in the client-side promisor remote acceptance logic.

Key points addressed:
1. Security clarification: Confirms that while the current implementation doesn't allow server-advertised URLs to replace client-configured ones, future mechanisms might introduce this capability, making the "KnownName" option potentially risky in corporate environments where name/URL trust relationships are assumed.

2. Implementation fixes:
   - Corrects documentation typo ("insensively" → "insensitively")
   - Renames `strvec_find_index()` to `remote_nick_find()` per Junio's suggestion
   - Changes URL comparison from case-insensitive (`strcasecmp`) to case-sensitive (`strcmp`)
   - Removes redundant NULL check for config value

3. Test case adjustments: Notes that while case-sensitive URL comparison is now used, the test suite doesn't currently verify case variations in remote names/URLs.

The discussion highlights ongoing security considerations in the promisor remote system, particularly around the trust model between client configuration and server advertisements. Christian demonstrates responsiveness to feedback while maintaining the series' focus on providing flexible but safe remote acceptance controls. The exchange helps solidify the security boundaries before finalizing the implementation.