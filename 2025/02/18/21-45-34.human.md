This is a follow-up in the ongoing discussion about whether `git show --format=email` should honor `format.from` and `format.forceInBodyFrom` configuration settings. Junio Hamano responds to Antonin Godard's argument that these settings are already partially used in `git_log_config()`.

Junio clarifies that the original design (established in 2022) specifically intended these format.* configurations for `git format-patch`, not other log commands, despite some shared implementation. He cautions against retroactively changing behavior that some users may be relying on, while acknowledging that in a hypothetical fresh design, a unified approach might have been preferable.

The exchange reveals the core tension: between maintaining backward compatibility versus providing consistent behavior across related commands. Junio's response shifts the discussion toward assessing the real-world impact of any change, particularly how many users might be affected by altered output versus how many would benefit from the new behavior (like the b4 tool integration mentioned earlier in the thread).

The technical details remain unchanged from previous messages - this is purely a design policy discussion about where to draw configuration boundaries between commands. The thread appears to be moving toward needing either stronger evidence of user impact or a compromise solution that doesn't break existing expectations.