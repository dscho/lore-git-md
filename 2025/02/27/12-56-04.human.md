This is a review comment on the `git-diff-pairs` implementation, which is part of a completed series adding a new plumbing command for batch blob diff generation. Patrick Steinhardt raises several technical points about the implementation details in patch 2/3:

1. Questions about initialization timing - whether `found_follow` should be set earlier in main() rather than in `flush_diff_queue()`, and whether default format should be set before `setup_revisions()`.

2. Style suggestions - recommends avoiding `usage_with_options()` in favor of direct `usage()` calls with clearer error messages, and notes a typo ("effect" vs "affect").

3. Implementation details - asks whether submodules are intentionally unsupported (suggesting checking `S_ISGITLINK()`) and requests a test case if they are excluded.

4. Notes the command's current restrictions (no pathspecs, no revision args) while acknowledging this keeps future options open.

The review is focused and technical, coming after the core implementation has already been through multiple rounds of review. Patrick's comments represent final polish items rather than fundamental concerns, as the thread summary shows the overall design and implementation have already been approved by multiple maintainers. These are the kinds of small but important details that often come up in final reviews before merging.