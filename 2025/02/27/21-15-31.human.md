This is a follow-up discussion about a platform compatibility patch that fixes a const-correctness issue in identity handling code. Junio Hamano is responding to Johannes Schindelin's patch that changes a `char*` to `const char*` in `ident.c`'s GECOS field handling.

The core technical change is straightforward - making a loop variable `const` where it should have been all along, since the code only reads from this buffer. Junio agrees the patch's correctness but questions why this 2011-vintage code needs attention now, wondering if there's some new build environment change or header definition that prompted it. He notes that while POSIX doesn't require the GECOS field to exist, other `struct passwd` string fields are writable, making this particular const-correctness fix seem oddly timed.

Junio makes clear he has no objection to the change itself, which is clearly correct, but pushes back slightly on any implied urgency - he doesn't see why this needs to be treated as a hotfix for the upcoming Git 2.49 release cycle. The discussion remains technical and constructive, with Junio seeking to understand the motivation behind surfacing this change now after over a decade.