This is a response to a refactoring patch that changes how upload-pack handles hidden refs during namespaced reference iteration. Patrick Steinhardt raises an important concern about the original patch from SURA, which removed conditional exclude pattern setup without providing justification.

The key issue is that the original change eliminated a performance optimization (using exclude patterns to skip hidden refs) that's particularly important for large repositories. Patrick notes the patch lacks any explanation of what problem it solves or why removing this optimization is acceptable. The technical change itself is simple (passing NULL instead of exclude patterns to `refs_for_each_namespaced_ref()`), but the implications aren't addressed.

This appears to be a case where a technically correct simplification (removing redundant pattern handling) may have unintended performance consequences. The thread now awaits SURA's response explaining either the rationale for removing the optimization or an alternative approach that maintains performance while achieving whatever goal motivated the original patch. The exchange highlights how even small refactorings in core components like upload-pack require careful consideration of edge cases and performance impacts.