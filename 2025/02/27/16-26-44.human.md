This is a discussion about output formatting in Git's configuration system, specifically around handling control characters like backspace (`\b`). Phillip Wood weighs in on an ongoing thread that started with a security concern about inconsistent handling of escape sequences between `git` commands.

The email supports Brian Carlson's suggestion for adding a human-readable output option for `git config`, while noting that the existing `-z` option already handles some ambiguity cases. Phillip agrees with the general consensus that raw output should remain the default, but sees value in adding more readable alternatives, similar to how `core.quotePath` handles special characters.

The discussion has evolved from a specific bug report into a broader conversation about Git's output formatting philosophy, with participants considering both immediate fixes (like better escape sequence handling) and potential long-term solutions (such as structured output formats). Phillip's brief but thoughtful response helps reinforce the direction of the conversation while keeping it grounded in existing Git conventions.