This is a bugfix and refactoring series (v8, 0/9) that finalizes the `git fsck` packed-refs validation improvements. The key change in this version addresses Junio Hamano's suggestion from the v7 review to improve resource handling patterns in patch 3/9 by initializing file descriptors to -1 and using a unified cleanup path.

The series now implements all planned validation checks for packed-refs files, including:
- Filetype verification (regular file check)
- Header format validation
- NUL character detection in refnames
- Entry consistency checks
- Sorting verification when "sorted" trait is present
- Integration with `git fsck` via new `--[no-]references` option

The v8 changes specifically modify patch 3/9 to:
1. Initialize fd = -1 to enable unified `goto cleanup` control flow
2. Avoid closing uninitialized file descriptors when file access fails
3. Maintain consistent error handling patterns across all validation functions

The author indicates this may be the final version, having incorporated all substantive feedback from previous rounds. The series has received positive reviews from Patrick Steinhardt and addresses Junio's resource handling suggestion, leaving only minor implementation pattern refinements under consideration. The changes maintain all existing validation behavior while improving code robustness through standardized cleanup patterns.