This is a technical discussion thread about the final polish for the `git-diff-pairs` plumbing command implementation, specifically addressing review feedback on patch 2/3 of the series. Justin Tobler responds to Patrick Steinhardt's detailed code review with explanations of current behavior and planned improvements.

Key technical points addressed:
- Explains why `found_follow` is set in `flush_diff_queue()` rather than initialization, noting it gets reset by `diffcore_std()`. Plans to replace this with a new `diff_options` field to avoid the workaround.
- Clarifies the current diff options parsing flow, acknowledging the confusing split between `setup_revisions()` and direct parsing. Will refactor to parse options upfront while maintaining usage message functionality.
- Agrees to replace `usage_with_options()` with direct `usage()` calls for cleaner error output, per modern Git conventions.
- Justifies the conditional `DIFF_FORMAT_PATCH` default due to OPT_BITOP() allowing multiple output formats simultaneously.
- Confirms submodule support is intended and will add test coverage, contrary to Patrick's assumption they might be excluded.

The exchange shows the series is in its final refinement phase, with Justin addressing subtle implementation details and interface polish based on reviewer feedback. All major architectural decisions appear settled, with only minor code organization and testing gaps remaining before merge.