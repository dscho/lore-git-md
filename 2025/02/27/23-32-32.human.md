This is a technical discussion about bitmap iteration approaches in the Git object filtering series, with Junio Hamano weighing in on Taylor Blau's concerns about the callback architecture.

Junio analyzes the current implementation's `ewah_for_type()` function, which provides type-specific bitmap access. He notes that the function made sense when each object type had its own bitmap, but becomes ambiguous with the newer `*_all` bitmaps that combine types. His comment suggests the architectural question isn't just about callbacks versus direct iteration, but also about how type filtering should conceptually interact with the bitmap data structures.

The email shows the maintainer engaging with Taylor's performance concerns while probing deeper into the underlying data model implications. Junio isn't taking sides yet, but is clarifying the technical context that makes this decision non-trivial. This moves the discussion beyond just "callback vs direct" to consider how the filtering abstraction should map to the actual bitmap storage format.

For readers following this series, this email signals that the maintainer is actively considering the bitmap integration questions and that the final solution may need to address both performance and conceptual clarity in the bitmap interface.