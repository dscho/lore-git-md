This is a technical discussion about bitmap filtering optimizations in `git cat-file`, part of Patrick Steinhardt's series to enhance object type filtering. Taylor Blau responds to patch 9/9 with suggestions for expanding the bitmap handling cases.

Taylor identifies an additional filter case that could benefit from bitmap optimization: when filtering by tree depth of 0 (which would only show commits). He then explores the architectural challenge of unifying this with existing bitmap filtering logic in pack-bitmap.c, noting two key mismatches: filter_bitmap() operates on full bitmaps rather than ewah_iterators, and traverse_bitmap_commit_list() requires a completed bitmap walk.

The email suggests a potential solution path - introducing a way to traverse the bitmap as if it were fully populated (all-1s bitmap) - while acknowledging the current architectural gaps. This continues the thread's technical depth around bitmap abstractions, now focusing specifically on how to reconcile the new cat-file filtering needs with existing bitmap infrastructure.

Key technical points for future reference:
- LOFC_TREE_DEPTH with depth=0 as another candidate for bitmap optimization
- Current architectural friction between ewah_iterator handling and bitmap traversal functions
- Potential solution direction involving simulated all-1s bitmap traversal
- Maintains focus on the series' core challenge of efficient type filtering while respecting bitmap subsystem constraints