This is a brief reply in the ongoing discussion about backspace character handling in Git's output. Phillip Wood responds to Brian Carlson's suggestion about adding a human-readable output option for `git config` that would escape control characters like backspaces.

Key points:
- Phillip agrees with the general direction of keeping raw output by default
- Notes that `-z` already exists to handle newline ambiguity in config output
- Supports the idea of adding an optional human-readable output format
- Does not propose specific implementation details, but aligns with the thread's emerging consensus around providing both machine-readable and human-friendly output options

The email reinforces the thread's trajectory toward maintaining backward-compatible raw output while exploring additional formatting options, though it doesn't advance the technical discussion with new proposals. It serves as a lightweight confirmation of the direction rather than introducing new considerations.