This thread addresses a bug in how `transfer.hideRefs` handles ref exclusions differently between packed and loose refs, with the scope later expanded to include the reftable backend. The original issue stems from commit 59c35fac54, where packed refs incorrectly hide all refs starting with a prefix (e.g., hiding all `sura*` branches when configured to hide `refs/heads/sura`), while loose refs correctly require a '/' suffix for prefix matching.

Jeff King (Peff) identified the root cause and proposed aligning all backends to the loose refs' correct behavior. Patrick Steinhardt has now confirmed that the reftable backend shares the same incorrect behavior as packed refs and provided the reftable-specific implementation needed for the fix. His solution mirrors the planned packed refs changes, ensuring all three storage systems (loose, packed, reftable) will consistently require proper directory containment for prefix exclusions.

The thread has progressed from problem identification to concrete solution design, with implementation paths now clear for both the packed and reftable backends. The remaining work involves finalizing the packed refs implementation (building on Jeff King's analysis) and ensuring the changes are reviewed by refs subsystem experts. Once implemented, this will resolve the inconsistency while maintaining the expected exclusion behavior across all ref storage formats.