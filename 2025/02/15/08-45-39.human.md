Refactoring patch improving refspec matching clarity. This is the second version of a change that renames `match_name_with_pattern()` to `match_refname_with_pattern()` and updates its parameter names to be more descriptive. The function handles globbing pattern matching for refspec operations in both push and fetch directions.

The v2 changes incorporate feedback from Junio Hamano, replacing the "src_pattern/dst_pattern" terminology with "pattern/replacement" to better reflect the function's bidirectional nature. The commit message and comments are updated to clarify that this is about matching refnames against globbing patterns, optionally computing mapped refnames.

The patch is part of ongoing work to improve the refspec code's readability. It touches refspec.c, refspec.h, and remote.c, updating all callers to use the new function name and parameter conventions. The changes are purely naming/documentation improvements with no behavior modifications.

The patch appears well-crafted with thorough updates to both implementation and documentation. It addresses a real readability issue in the codebase while being careful to maintain the function's existing behavior. The v2 changes show responsiveness to review feedback about terminology.