This is a performance discussion in the ongoing reftable backend investigation. Brian m. carlson responds to Jeff King's analysis of ambiguous refname warning overhead, agreeing that disabling these warnings would provide meaningful speedups for common cases.

The email focuses on practical scenarios where the warnings are unnecessary - primarily automated operations like pushes and API calls where full object IDs are always used. Brian argues that even interactive users of `git update-ref` (like in scripts or aliases) would prefer performance over these warnings, noting that the command is most often used in contexts where speed matters more than human-readable output.

This reinforces the emerging consensus from the thread that these warnings should be moved or removed, with Brian providing real-world use cases where the current implementation's performance penalty is particularly painful. The response suggests broad agreement with Jeff King's earlier findings while adding perspective from a user who frequently handles large numbers of refs.