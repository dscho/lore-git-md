This is a performance optimization discussion regarding the merge-recursive backend, specifically about potential O(n) improvements to `get_unmerged()`. The email from Elijah Newren provides the final position on this patch in the thread, confirming it should be dropped rather than pursued further.

Key technical points:
- The proposed optimization could theoretically reduce complexity from O(n log n) to O(n) by leveraging the index's sorted nature
- However, the practical impact would be minimal since:
  - The number of unmerged files (n) is typically small
  - More significant O(m²) bottlenecks exist elsewhere in merge-recursive
  - The recursive backend is being phased out in favor of merge-ort
- Newren confirms this optimization wouldn't benefit merge-ort, making it not worth the implementation complexity

Strategic considerations:
- Maintainers agree merge-recursive is legacy code not worth significant optimization
- Efforts should focus on merge-ort rather than improving the old backend
- The discussion serves as closure on whether to pursue further merge-recursive optimizations

This email effectively ends the thread's discussion of the second patch, with Newren's authoritative position as the merge machinery maintainer carrying decisive weight. The only remaining action would be merging the previously-approved first patch (for `process_renames()` optimization).