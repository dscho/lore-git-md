This is a protocol design response in an ongoing discussion about bundle-uri advertisement for filtered clones. Brian Carlson confirms the current protocol limitation where servers must guess client filter preferences when selecting bundles, since the `bundle-uri` command happens before the `fetch` command that specifies filters.

The response notes that while the protocol syntax could theoretically support parameters for the `bundle-uri` command, no such capability currently exists. Carlson suggests this is likely due to limited use of the functionality so far, but indicates adding such support would be feasible if someone wanted to implement it.

This moves the discussion forward by confirming the protocol's current behavior isn't just an oversight but a deliberate (if perhaps underdeveloped) design choice. The response implies that solving the bundle selection timing issue would require protocol extensions, setting the stage for potential future work in this area. The tone suggests this is a reasonable direction to explore, though not an urgent problem given current usage patterns.