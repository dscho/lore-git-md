This is a review discussion in the platform compatibility series addressing `-Wsign-comparison` warnings in `apply.c`. The email focuses on patch 1/6 which changes fields in `apply_state` to unsigned types.

Junio C Hamano responds to Karthik Nayak's observation about type consistency between `max_change` and the `lines_added`/`lines_deleted` fields it depends on. Hamano agrees that either all these fields should be unsigned, or if some internal APIs use negative numbers for error signaling, they should all be signed. He also questions whether using `size_t` (which requires explicit casts in printf calls) might be less readable than sticking with signed integers and adding range checks where needed.

The discussion highlights an important architectural decision point in the series - whether to consistently use unsigned types throughout the related fields and functions, or to maintain some signed types where they serve a purpose (like error signaling). This touches on both technical correctness (avoiding warnings while maintaining functionality) and code quality/readability considerations.

Key technical points mentioned:
- The `max_change` field is calculated from `lines_added` + `lines_deleted`
- Current code shows printf formatting requiring explicit casts when using `size_t`
- Potential need for range checking if sticking with signed integers

This email moves the discussion beyond just fixing warnings to consider the broader implications of type choices in this part of the codebase. It suggests the series may need to expand its scope to include related fields (`lines_added`/`lines_deleted`) to maintain consistency, or alternatively reconsider the signed/unsigned tradeoffs.