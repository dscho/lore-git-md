This is part 6 of a 10 introducing long option names for Git's pickaxe functionality (`--patch-modifies` for `-S` and `--patch-grep` for `-G`). The patch maintains the dual-form implementation where both short and long options work identically, with tests verifying this behavior.

The maintainer notes this patch is technically sound with proper sign-off and clear title, but suggests it might be better to split the series into two parts: first landing the preliminary cleanups (patches 1-5), then the new long options (patches 6-10). This particular patch touches documentation that was cleaned up earlier in the series, making it harder to separate cleanups from new features.

The implementation appears complete with:
- Core diff machinery supporting both option forms
- Documentation showing both forms while preserving technical details
- Tests verifying identical behavior for short and long options
- Completion script updates for both forms

This continues the thread's theme of improving pickaxe usability while maintaining backward compatibility and accurate documentation. The maintainer's feedback focuses on process improvements (better patch organization) rather than technical implementation.