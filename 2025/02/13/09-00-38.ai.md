This is a performance optimization patch series for merge-recursive, following up on previous discussion about quadratic complexity in string list construction. The series now includes two patches addressing different functions in merge-recursive.c.

The first patch (previously discussed in the thread) optimizes `process_renames()` by changing string list construction from O(n²) to O(n log n), implementing the approach Elijah Newren approved while preserving the important documentation from the FIXME comment. The commit message now more precisely scopes the optimization to `process_renames()`.

The second patch is a RFC addition applying similar optimization logic to `get_unmerged()`, suggesting this pattern could be used more widely in the codebase. The author marks this as RFC to solicit feedback on whether this broader application of the optimization is appropriate. The implementation follows the same approach: replacing incremental sorted insertion with bulk append+sort.

Key technical details: Both patches modify string list construction patterns in merge-recursive.c, with the second patch touching the same file but different functions. The changes are minimal (25 total lines modified across both patches) and maintain the same behavioral guarantees while improving asymptotic complexity. The thread context shows this builds on established consensus about the optimization approach from previous review.

The RFC status of the second patch indicates the author is seeking guidance on whether to pursue similar optimizations elsewhere, making this a potential starting point for broader performance work in merge-recursive. The thread shows the optimization is technically sound for the first case, with the main open question being about the scope of application.