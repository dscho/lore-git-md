This is a technical discussion about callback compatibility in the `git-diff-pairs` plumbing command series. Patrick Steinhardt responds to Justin Tobler's explanation about why the patch maintains separate callback variants rather than modifying existing functions. 

The key point is that the original `diff_addremove()` and `diff_change()` functions must retain their current signatures because they're used as callbacks in `diff_options`, while the new use cases need access to the queued `diff_filepair` structs. Justin explains that modifying the existing callbacks would be problematic since some variants (like `file_add_remove()`) don't actually queue filepairs. Patrick agrees this is important context that should be documented in the commit message.

This exchange clarifies an architectural constraint in the series - the need to preserve callback signatures while still enabling new functionality. The discussion helps justify the approach taken in patch 1/3 of the series, where new functions were introduced rather than modifying existing ones. The thread shows careful attention to maintaining backward compatibility while evolving the diff internals.