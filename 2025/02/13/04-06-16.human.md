This is a test cleanup patch in the ongoing series adding long option names for pickaxe search. Junio Hamano responds to a minor fix in test script terminology, pointing out that the change from "string" to "regex" in test descriptions for `-G` is technically correct but should be handled separately from the main feature work.

The maintainer suggests either:
1. Keeping this series focused solely on adding `--patch-grep`/`--patch-modifies` without any ancillary cleanups, or
2. Splitting such preliminary cleanups into their own series to be merged first

He notes the current patch is missing a sign-off and could use a better subject line. While the change itself is correct (using "regex" rather than "string" for `-G` test values), it's not directly related to the long option feature and thus creates distraction in review.

The response indicates the maintainer's preference for keeping feature series tightly focused, with preparatory cleanups handled separately. This suggests the submitter may need to restructure their approach for the next version.