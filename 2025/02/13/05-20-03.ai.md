This is a patch in the ongoing series introducing long option names for Git's pickaxe functionality (`--patch-modifies` for `-S` and `--patch-grep` for `-G`), now at version 5. The patch focuses on implementing the dual-form options while maintaining existing behavior.

The maintainer notes this patch is technically sound but suggests it could be separated from the earlier documentation fixes (patches 01-05) which could land first. The implementation here:
- Keeps existing short-form usage in tests/docs while adding long forms
- Duplicates option parsing tests to verify both forms
- Maintains the corrected option descriptions from earlier patches
- Has proper sign-off and a clear subject line

Key technical aspects carried forward from the thread:
- diff.c handles both short/long forms with consistent error/help text
- Behavior remains identical between short and long forms
- Test coverage verifies this equivalence
- Documentation prefers long forms while preserving technical details

The maintainer's feedback indicates this patch is ready in isolation but suggests better organization by splitting the series into preliminary cleanups (docs/tests) followed by the core feature work. The terminology discussion around "block-of-text" vs "string" remains open but doesn't block this implementation.

This patch represents the technical completion of the long-options feature while the thread continues to refine documentation phrasing and series organization. The maintainer's approval of the implementation suggests it's likely to be accepted once properly sequenced with the preliminary work.