This is a discussion about performance optimization in merge-recursive's string list processing, specifically regarding the `get_unmerged()` function. The email captures Junio Hamano responding to Elijah Newren's technical concerns about the proposed optimization.

The key points from this exchange:
- Newren had suggested that with the specialized input structure, an O(n) solution might be possible for `get_unmerged()`, avoiding the need for sorting and duplicate removal entirely.
- Hamano picks up on this idea, noting that if the input is already sorted, a merge-like approach could be more efficient.
- Both acknowledge that while theoretically appealing, such an optimization would require careful implementation, testing, and measurement to justify the added complexity.
- The discussion touches on the broader context that merge-recursive is slated for eventual removal in favor of merge-ort, raising questions about how much optimization effort is worthwhile for this legacy code.

Technical context for future reference:
- The debate centers around string list processing in `get_unmerged()` within merge-recursive.c
- Current approach uses O(n log n) sorting with potential for O(n²) operations
- Proposed alternatives range from the original append+sort pattern (like the approved rename processing optimization) to a hypothetical O(n) merge of pre-sorted inputs
- Performance measurement is identified as a key missing piece for evaluating the optimization's value
- The discussion implies merge-ort may have already addressed these performance concerns differently

This email doesn't introduce new code but captures important considerations about optimization tradeoffs in legacy code paths, with both technical and strategic dimensions. The thread shows maintainers weighing algorithmic improvements against implementation complexity and the codebase's future direction.