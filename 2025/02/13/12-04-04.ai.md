This is a follow-up in the `git rebase --update-refs=interactive` feature discussion, where implementation details have now reached consensus. Ivan Shapovalov responds to Phillip Wood's proposal about the command-line interface and configuration approach for the interactive ref updating feature.

The key development is Shapovalov agreeing with Wood's suggestion to reserve the `--update-refs` optional argument for future pattern filtering capabilities (like `*-v2` branch matching) rather than using it for the interactive mode. This preserves future extensibility while allowing the interactive behavior to be controlled via config (`rebase.updateRefs=interactive`). Shapovalov confirms he'll send a v2 patch with this change, dropping the CLI modification while keeping the core interactive behavior.

The exchange confirms the architectural decision to handle interactive review through configuration rather than command-line syntax, while leaving room for future branch filtering capabilities. This aligns with the thread's earlier consensus about supporting both simple CLI use cases and more complex config-driven workflows. The discussion also touches on but doesn't dwell on the broader branch maintenance strategies, as those were already addressed in prior messages.