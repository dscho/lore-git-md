This is a protocol design discussion about bundle-uri advertisement in Git, specifically regarding the challenge of optimizing bundle selection for filtered clones. The email comes from Brian Carlson in response to Brian Celenza's earlier question about protocol sequencing limitations. 

Carlson confirms that the current protocol indeed lacks any mechanism for clients to signal their filtering intentions before the bundle-uri command, as Celenza had observed. The protocol's command sequence (capabilities → ls-refs → bundle-uri → fetch) forces servers to select bundles without knowing the client's eventual filter preferences. 

The technical assessment notes that while the bundle-uri command syntax could theoretically support parameters, no such capability currently exists. Carlson suggests this likely reflects limited use of the feature rather than a deliberate design constraint. The response implies this could be addressed through a capability extension if demand warranted it.

This moves the discussion forward by confirming the protocol's current limitations while leaving open the possibility of future extensions. The exchange remains at the design exploration stage, with no concrete proposal yet for how such an extension might work. The technical details center on the bundle-uri command's parameter support potential and the protocol's capability negotiation mechanism.

Key terms that may recur in future discussion include: bundle-uri command, capability extension, protocol sequencing, filtered clones, and parameter support. The thread appears likely to continue exploring whether and how to modify the protocol to better support optimized bundle delivery for filtered clone scenarios.