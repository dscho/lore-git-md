This is a documentation patch in the ongoing series adding long option names for pickaxe search (`-S`/`-G`). The patch changes placeholder text in `gitdiffcore.txt` from verbose terms ("regular-expression", "block-of-text") to the shorter forms ("regex", "string") used elsewhere in Git's docs and code.

Junio Hamano approves the change in principle but raises a subtle concern: the original "block-of-text" phrasing intentionally conveyed that `-S` typically operates on multi-line blocks (like function bodies) rather than short strings. While agreeing the shorter terms are more consistent, he suggests adding clarification about the expected input size if making this change.

The discussion highlights how documentation choices can subtly influence user expectations, even for technical readers. This patch appears well-considered but may need minor adjustments to preserve the semantic nuance about input scale. The thread shows maintainers carefully balancing consistency with precise technical communication.