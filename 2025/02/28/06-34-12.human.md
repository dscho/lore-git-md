This is a build system discussion about fixing the gitweb.js generation issue, specifically debating Makefile implementation details between GNU make and BSD make compatibility.

Thorsten Glaser responds to Simon Richter's suggestion about using order-only dependencies (`|` syntax) in the Makefile, noting it's a GNU make feature that may not be portable. As a BSD make user, they observe that `$^` in non-suffix rules often causes problems, which aligns with the root issue where `$^` incorrectly included the build script in the output.

The email suggests testing Simon's approach if it works, while acknowledging the solution should follow the project's preferred style. Glaser's aside about mksh's build system appears to be an unrelated personal observation about build system quality, not directly relevant to the gitweb.js discussion.

This continues the technical debate about the cleanest way to fix the build artifact contamination, now considering portability concerns between make implementations. The thread remains focused on finding a robust solution that works across different make variants while maintaining clear dependency relationships.