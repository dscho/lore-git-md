This is a discussion about potentially deprecating Git's `--literally` flag and `OBJECT_INFO_ALLOW_UNKNOWN_TYPE` feature, which appears to be part of Jeff King's ongoing zlib inflation series.

Taylor Blau responds to Junio Hamano's observation that the `--literally` flag was primarily a debugging tool for testing corrupt object handling. While agreeing it shouldn't survive into Git 3.0, Taylor notes its usefulness for testing intentionally broken objects like malformed commits or trees. He suggests possibly moving this functionality into a test helper instead of exposing it via the plumbing interface.

The discussion reveals this feature has seen little production use in its 10-year existence, with consensus forming around deprecation. The thread shows the Git community carefully considering how to maintain testing capabilities while cleaning up rarely-used interfaces that complicate the codebase. Taylor's input suggests a pragmatic path forward that could preserve testing needs while removing the public-facing interface.