This is a technical follow-up in the incremental MIDX bitmaps thread, focusing on implementation details of the pack-revindex changes needed to support the feature. Patrick Steinhardt reviews Taylor Blau's patch that prepares the reverse index machinery for MIDX chains.

The key changes involve:
1. Adding `bitmap_non_extended_bits()` to account for objects in base MIDX layers when calculating bitmap sizes
2. Modifying revindex loading to handle both regular and split MIDX files
3. Updating position-to-MIDX mapping to traverse base layers when needed

Steinhardt identifies several technical points needing attention:
- Questions whether `bitmap_num_objects()` should be used in certain remaining call sites (`pseudo_merge_bitmap_for_commit`, `create_bitmap_mapping`)
- Notes a minor formatting issue in the code
- Suggests the commit message could better explain why some functions weren't converted to use the new helper

The review maintains the thread's technical depth while focusing on implementation correctness rather than design questions. It highlights areas where documentation or justification in the commit message could help future readers understand the choices made. The exchange shows careful attention to how the changes interact with MIDX layering while preserving existing behavior where needed.