This is a documentation maintenance patch that's part of a completed series converting Git's documentation from .txt to .adoc format. The patch updates `Documentation/howto/howto-index.sh` to handle the new .adoc extensions, fixing broken links in the generated howto-index.html output while maintaining identical processing logic.

The changes are mechanical substitutions of `txt` to `adoc` throughout the script, with all 7 changes being straightforward filename extension updates. Junio notes the patch looks correct but points out an existing inconsistency in how the script handles filenames with whitespace - one grep call doesn't properly quote the variable while all other references do. He suggests this could be cleaned up later as a follow-up.

This was the first patch in Todd Zullinger's original 3/3 series, which was later supplemented by Patrick Steinhardt's Meson build system updates. The thread shows this was a well-executed documentation conversion with thorough build system coverage, now fully acknowledged by the maintainer. The only minor note is the whitespace handling inconsistency flagged by Junio, which appears to be pre-existing rather than introduced by this change.