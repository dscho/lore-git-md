This is a follow-up discussion about the gitweb.js build system bug, specifically exploring alternative fixes for the Makefile dependency issue. Simon Richter responds to Thorsten Glaser's proposed fix by suggesting an alternative approach using order-only dependencies (marked with `|` in Makefile syntax).

The thread previously identified that the build script was incorrectly included in the JavaScript output due to Makefile using `$^` (all dependencies) rather than explicitly specifying only the output file. Simon's suggestion of an order-only dependency would maintain the build-time requirement while preventing the script from being treated as input content - a potentially cleaner solution than Thorsten's approach.

The exchange shows developers considering different technical solutions to the same underlying problem, with Simon's proposal being more precise about the dependency relationship. This level of discussion suggests the issue is being taken seriously, though both solutions would effectively prevent the build script from polluting the output. The thread may continue with further evaluation of which approach is more maintainable or idiomatic for Git's build system.