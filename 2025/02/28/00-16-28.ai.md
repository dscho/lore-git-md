This is a discussion about potentially deprecating Git's `--literally` flag and `OBJECT_INFO_ALLOW_UNKNOWN_TYPE` feature as part of Jeff King's zlib inflation series. Taylor Blau responds to Junio Hamano's observation that these features were primarily debugging tools with limited practical use.

Taylor acknowledges the debugging utility of `--literally` for creating intentionally broken test objects (like malformed commits or trees), but suggests possibly moving this functionality into a test-only helper rather than keeping it in the public interface. This aligns with the thread's growing consensus that these features should be deprecated in Git 3.0, though Taylor notes their value for testing edge cases.

The discussion continues the series' theme of hardening object handling while cleaning up questionable legacy features. Taylor's input suggests a possible middle path where testing capabilities are preserved internally while removing the public-facing plumbing interface. This email doesn't propose concrete changes but contributes to the ongoing evaluation of whether and how to deprecate these debugging-oriented features.