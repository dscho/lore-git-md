This is the first patch in the v5 series introducing the new `git-diff-pairs(1)` plumbing command, focusing on refactoring the diff queue infrastructure. The patch splits the existing `diff_addremove()` and `diff_change()` functions into two layers: low-level queue operations that return the created `diff_filepair`, and thin wrappers that maintain backward compatibility. 

The change enables future patches in the series to modify queued file pairs after creation, which will be needed for the new batch diff functionality. The patch carefully preserves all existing behavior while adding the new `diff_queue_addremove()` and `diff_queue_change()` functions. It includes thorough documentation in diff.h explaining the new functions.

This is a well-structured refactoring that follows Git's established patterns for exposing low-level operations while maintaining compatibility. The changes are mechanical but necessary groundwork for the new command, and the patch has already incorporated feedback from previous review rounds. As part of a mature series that's nearing completion, this patch appears ready for merging with only minor documentation tweaks remaining.