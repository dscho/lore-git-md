This is a refactoring patch that's part of the `git-diff-pairs` plumbing command development series. It introduces a new `skip_resolving_statuses` option in the diff machinery to prevent automatic status resolution when processing file pairs.

The patch addresses a specific technical need in the diff core - currently, `diffcore_std()` always resolve statuses via `diff_resolve_rename_copy()`, which can overwrite manually-set status information. The new option gives callers control over this behavior, which is particularly useful for the new `git-diff-pairs` command where statuses are already known.

The change is minimal (just 2 files modified) but important for the series' functionality. It replaces a previous workaround using the `found_follow` field with a more explicit and properly documented mechanism. The implementation follows Git's established patterns and maintains backward compatibility.

This appears to be a well-justified change that's been through several rounds of review, with all major architectural decisions already settled in the thread. The patch is now in its final polishing phase before merging.