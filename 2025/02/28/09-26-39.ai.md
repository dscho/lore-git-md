This is patch 2/16 in a performance optimization series for the refs subsystem, introducing a new flag to skip ambiguity checks in object ID lookups. The patch adds `GET_OID_SKIP_AMBIGUITY_CHECK` to the flags passed through `get_oid()` family of functions, modifying `get_oid_basic()` to conditionally skip the reference ambiguity warning check when this flag is set.

The change is a preparatory step for subsequent optimizations where these checks would add unnecessary overhead. The patch touches `hash.h` to define the new flag and `object-name.c` to implement the conditional check. While this doesn't yet change any behavior (the flag isn't used until later patches), it establishes the API extension needed for the optimization series.

The thread context shows this is part of a larger effort to improve refname availability checks across different backends, with measurable performance gains already demonstrated in later patches. The series has undergone multiple revisions with input from several reviewers, and this particular change appears uncontroversial as it's purely additive with no behavior changes until the flag is actually used.