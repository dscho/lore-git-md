Build system fix for gitweb.js generation, continuing the thread discussion of Makefile dependency handling. Patrick Steinhardt responds to Thorsten Glaser's patch that fixed the issue where the build script was incorrectly included in gitweb.js output due to using `$^` (all dependencies) rather than explicit file listing. Patrick suggests improvements to the commit message to better explain the regression introduced in commit 19d8fe7da65 and its fix. He also notes the Signed-off-by should use a plain name rather than alias, and proposes an alternative solution using `$(filter %.js,$^)` to avoid repeating the file list while still excluding non-JS files. The technical discussion continues weighing explicit listing versus filtering approaches, with Patrick's suggestion potentially offering a middle ground between the current patch's explicit listing and Simon Richter's earlier proposed order-only dependency solution. The thread remains focused on finding the most maintainable solution that works across different make implementations.