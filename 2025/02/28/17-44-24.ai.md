This is a naming convention discussion in the ongoing cat-file batch mode filtering enhancement series, specifically addressing the `--filter` vs `--objects-filter` option naming debate.

Junio Hamano weighs in decisively on the side of consistency, arguing that `cat-file` should use the established `--filter=` naming pattern seen across other Git commands (rev-list, pack-objects, repack, clone) rather than introducing a new `--objects-filter` variant. He provides historical context showing that `--filters` in cat-file refers specifically to smudge/clean content filtering, making it sufficiently distinct from the object filtering use case. The email presents a strong case that either all commands should standardize on `--filter=` (current practice) or all should move to a more explicit name like `--object-filter=`, with the former being the more practical path forward given existing usage patterns.

This moves the naming discussion toward resolution by providing authoritative historical context and clear technical rationale for maintaining consistency with other commands. The intervention comes from a position of deep institutional knowledge about Git's command-line interface evolution and naming conventions around object filtering. Junio's position effectively counters Patrick's concern about confusion between `--filter` and `--filters` by clarifying their distinct historical purposes. The email leaves little room for ambiguity about the preferred direction while acknowledging the theoretical appeal of more explicit naming.