This is a technical discussion about memory safety in the MIDX bitmap implementation, specifically around the `load_all_type_bitmaps()` function.

Patrick Steinhardt is reviewing Taylor Blau's patch that tracks type bitmaps (commits, trees, blobs, tags) across MIDX layers. The patch introduces arrays to store these bitmaps for each layer, indexed from the base layer upwards. Steinhardt questions whether the code should include a `BUG()` check when the layer index `i` reaches 0, as the current implementation could potentially underflow the array index when decrementing `i` after processing the last layer.

The discussion continues the thread's focus on implementation correctness and edge case handling in the MIDX bitmap subsystem. This particular exchange highlights the careful attention being paid to memory safety and array bounds checking in the recursive layer traversal logic. The question suggests the need for either an explicit bounds check or documentation explaining why such a check isn't necessary in this context.

Key technical details:
- The function maintains parallel arrays (`commits_all`, `trees_all`, etc.) for each layer's type bitmaps
- Layers are processed in reverse order (base layer first) using a 1-based index that decrements
- The safety question focuses on the final iteration when `i` would become 0 after decrement
- The discussion occurs within the broader context of MIDX bitmap layer management and type filtering optimizations