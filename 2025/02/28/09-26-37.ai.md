Performance optimization cover letter for refs subsystem (v4, 16 patches). This series builds on the v3 iteration that completed the iterator reuse optimization, now addressing final review comments and adding clarifications to the commit messages. The core focus remains optimizing refname availability checks across Git's ref backends (files, packed, reftable), particularly for operations involving many references.

Key technical changes in v4:
- Enhanced commit message for patch 3 (`builtin/update-ref`) with clearer rationale for skipping ambiguity checks, noting users should use fully-qualified refnames in plumbing contexts
- Maintains all performance improvements from v3:
  - 1.23-1.27x speedup in files backend
  - 4.78-7.56x speedup in reftable backend
  - Iterator reseek capability now fully implemented across all backends
  - Batched prefix checks via `strset`

The series remains structured as:
1. Skip unnecessary ambiguity checks in `git-update-ref` (patches 1-4)
2. Batch refname availability verifications (patches 5-9) 
3. Implement iterator reseek infrastructure (patches 10-15)
4. Final optimization: reuse iterators for nested checks (patch 16)

Notable aspects for future discussion:
- The `git-update-ref` behavior clarification (now documented in patch 3) confirms plumbing commands should accept any valid object name format while suppressing ambiguity warnings
- Iterator reseek implementation maintains cross-backend consistency while allowing backend-specific optimizations
- Performance measurements show the reftable backend now outperforms files backend in realistic scenarios (15x faster when updating refs among preexisting refs)

The series appears ready for final review, with all major technical questions from previous iterations addressed. The remaining documentation updates for `git-update-ref` behavior have been acknowledged as a separate follow-up task.