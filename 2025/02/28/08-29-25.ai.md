This is a follow-up review comment on the `git-diff-pairs` series, specifically addressing the new `skip_resolving_statuses` option introduced in v4 patch 2/4. Patrick Steinhardt notes that there's an existing workaround in `tree-diff.c` that abuses the `found_follow` flag to achieve similar status-preservation behavior, and suggests converting that code to use the new dedicated mechanism instead.

The suggestion would require modifying `tree-diff.c` to use the new `skip_resolving_statuses` field rather than misusing `found_follow`, which would clean up the implementation by:
1. Removing the hacky workaround
2. Consolidating status preservation under the new official mechanism
3. Potentially simplifying the `found_follow` flag's responsibilities

This is a relatively straightforward change that would improve code clarity, though it's not strictly necessary for the core functionality of the `git-diff-pairs` command. The comment doesn't indicate whether this would require changes to the current patch series or could be done as a follow-up cleanup.