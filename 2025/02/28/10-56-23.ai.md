This is a review comment on the ongoing refactoring to handle NULL repository pointers in Git's configuration machinery. Phillip Wood builds on the previous discussion about modifying `repo_config()` to accept NULL repos by pointing out a subtle behavioral issue with the current implementation.

The key observation is that `read_very_early_config()` ignores command-line config options (`-c key=value`), which would be an undesirable regression for commands running outside repositories. Phillip suggests calling `config_with_options()` directly instead, which would properly handle all config sources. He also identifies `git for-each-repo` as a command that would need updating to use the new callback approach rather than `repo_config_get_string_multi()`.

This review maintains the thread's technical focus while adding new implementation considerations. The core proposal (NULL repo handling via config fallback) remains conceptually sound, but now with an additional requirement to preserve command-line config processing. The comment doesn't challenge the overall approach but refines the technical details, keeping the change aligned with Git's config handling expectations.

The thread continues its trajectory toward merging, now with two clear implementation tasks: adding test coverage (per Junio's earlier review) and adjusting the config source handling to include command-line options (per Phillip's new feedback). Both points address completeness rather than fundamental design issues.