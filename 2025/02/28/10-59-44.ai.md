This is a technical clarification in the ongoing discussion about git rev-list's commit ordering behavior with identical timestamps. Phillip Wood points out that Clement Moyroud's test case demonstrating why `--topo-order` doesn't work for their use case was missing the `--no-walk` flag. Phillip suggests the correct invocation should be `git rev-list --stdin --topo-order --no-walk`, which would theoretically limit the output to just the input commits while maintaining topological order.

The email doesn't propose a solution but corrects a technical detail in the reproduction case. This is important because the thread has been exploring whether existing flags could solve the problem before considering new functionality. Phillip's observation implies that `--topo-order` combined with `--no-walk` might behave differently than Clement's initial test showed, though it remains to be seen if this combination would meet the workflow requirements of only processing stdin commits while preserving their topological relationships.

Key technical details for future reference: the specific flag combination being discussed (`--topo-order --no-walk`), the stdin-based workflow context, and the ongoing need to handle commits with identical timestamps. The thread continues to explore whether Git's existing flags can address the use case before considering API changes.