This is a technical discussion about a bug in `transfer.hideRefs` behavior between packed and loose refs. Taylor Blau responds to SURA's report, clarifying that the current behavior is intentional rather than a regression from commit 59c35fa. 

The key point is that loose refs are designed to be filtered by the caller (upload-pack) rather than skipped during iteration, while packed refs can be skipped more efficiently. Taylor explains the call chain where upload-pack filters refs using `ref_is_hidden()`, which properly handles both exact and prefix matches. He disputes SURA's claim that 59c35fa caused the issue, noting the upload-pack behavior changes came in a later commit.

The response is thorough but suggests more concrete reproduction steps would help, as Taylor suspects he might be missing part of the problem. The discussion appears to be in early stages, with Taylor providing background but not yet proposing a solution. This looks like a nuanced refs backend issue that may require deeper investigation into the actual observed behavior versus intended design.