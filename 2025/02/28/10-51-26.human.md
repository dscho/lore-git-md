This is a technical discussion about filtering capabilities in `git cat-file`'s batch mode, part of Patrick Steinhardt's ongoing series to enhance object filtering.

Patrick responds to Toon Claes' question about why the patch doesn't support combining multiple filter conditions (LOFC_COMBINE). He explains that the current filtering options are too limited to make combination useful - the only potentially meaningful combination would be filtering blobs by both type and size limit. Since other combinations would produce empty result sets and implementing this would add significant complexity without clear use cases, Patrick chose to omit it for now.

The exchange highlights an interesting design tradeoff in the filtering system. While the thread's broader context shows this is part of a mature technical implementation with bitmap optimizations and naming conventions being discussed in parallel, this particular sub-thread focuses narrowly on the scope of filter combinations. Patrick's response suggests this is a deliberate simplification rather than an oversight.

The discussion remains technical but appears non-contentious, with Patrick providing clear reasoning for the design choice. This doesn't seem to be a blocking issue for the series, but rather a point of clarification about implementation boundaries.