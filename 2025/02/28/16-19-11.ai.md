This is a follow-up discussion in the `git add -p` hunk splitting thread, focusing on whether to change the default behavior of split hunks from "selected" to "undecided". The email captures Phillip Wood and Junio Hamano converging on the technical merits of the change while expressing concerns about backward compatibility.

Phillip indicates he will drop the patch for now, citing the potential disruption to existing workflows. He suggests revisiting the change if a "Git 3.0" feature flag mechanism is implemented, which would allow opt-in behavior changes. Junio agrees this would be a better approach than adding configuration knobs for such small behavioral tweaks.

The discussion also surfaces two related UX issues in `add -p`: the lack of visual state indication when revisiting hunks, and a mismatch between the documented and actual behavior of the `J`/`K` navigation commands. These appear to be recognized as legitimate problems but are being treated as separate from the immediate hunk splitting question.

Key technical points for future reference:
- The proposed change would modify state inheritance in `add-patch.c` when hunks are split
- Current behavior preserves selection state across splits
- Proposed behavior would reset split hunks to "undecided"
- Navigation commands `J`/`K` currently preserve state rather than setting to undecided as documented
- No visual indicators exist for hunk state during review

The thread shows the maintainers balancing improvement against stability, with the likely outcome being deferral of the behavior change until a major version bump or feature flag mechanism is available. The discussion remains focused and technical, with both parties acknowledging the tradeoffs involved.