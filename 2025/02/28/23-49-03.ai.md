Implementation refinement for incremental MIDX bitmaps (part 3/13)

This email continues the technical discussion around incremental MIDX bitmap implementation, specifically addressing two review points from Patrick Steinhardt regarding the `base_nr` field indexing and redundant bitmap filename operations.

The key points addressed are:

1. **`base_nr` indexing clarification**: The author acknowledges the unusual 1-indexing of `base_nr` (used for EWAH iterator initialization) and proposes two solutions:
   - Rename to `layers_nr` to make 1-indexing explicit
   - Keep name but switch to 0-indexing with callers adjusting as needed
   The author expresses preference for the second approach (0-indexed with explicit adjustment) for clarity.

2. **Redundant filename operation removal**: The author confirms and removes an unnecessary double-call to `midx_bitmap_filename()` when opening MIDX bitmaps, agreeing with Steinhardt's observation that the retry logic was redundant.

This exchange demonstrates the careful attention to both implementation details (indexing conventions) and code efficiency (avoiding redundant operations) that characterizes this technical thread. The discussion remains focused on refining the incremental MIDX bitmap implementation while maintaining clear communication about design choices.

The email shows the author (Taylor Blau) responding to specific technical points from Steinhardt's review, making concrete adjustments while maintaining the overall architecture of the bitmap handling for MIDX chains. The changes are relatively minor but important for code clarity and correctness in this complex subsystem.