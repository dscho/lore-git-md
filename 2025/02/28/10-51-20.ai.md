This is a discussion about naming consistency for a new filtering option in `git cat-file`'s batch mode, part of Patrick Steinhardt's series to enhance object filtering capabilities.

The thread context shows this is part of a mature technical discussion about bitmap-based object filtering optimizations, but this particular email focuses on a narrower naming question. Toon Claes argues for using the standard `--filter=` option name (consistent with `git rev-list` and other commands using `OPT_PARSE_LIST_OBJECTS_FILTER`), while Patrick Steinhardt prefers the distinct `--objects-filter` name to avoid confusion with the existing `--filters` option in `git cat-file`.

Patrick indicates he's open to either approach but leans toward his original naming to prevent user mistakes between the very similarly named `--filters` and `--filter=`. He's soliciting additional input to break the tie. The discussion remains cordial and focused on user experience considerations rather than technical implementation.

Key technical details for future reference:
- The debate concerns `git cat-file --batch` mode filtering
- Existing option is `--filters` (no argument)
- Proposed new option would filter objects by type/size
- Standardization question around `--filter=` vs `--objects-filter`
- Related to broader series adding bitmap-based optimizations for object filtering