This is a refactoring patch in a 7-part series introducing a `--revision` option for `git clone`. The patch inverts the internal handling of tag cloning from `--no-tags` to `--tags` (default-enabled) to simplify the code and prepare for future changes in the series.

The change flips the internal boolean flag from `option_no_tags` to `option_tags` (defaulting to true) while maintaining identical behavior, since tags are cloned by default. The documentation is updated to show both `--tags` and `--no-tags` as valid options, though `--tags` has no practical effect beyond the default behavior. 

Key technical changes include updating all conditionals checking the flag, modifying the option parsing to use the new boolean, and adjusting the documentation syntax from `--[no-]-tags` to the correct `--[no-]tags` format. The patch includes no functional changes or new tests since the default behavior remains unchanged - this is purely an internal refactoring to make the code more straightforward for the subsequent patches in the series that will build on this structure.

The change follows up on historical discussion about supporting both `--tags` and `--no-tags` options, though the actual motivation here appears to be code simplification rather than user-facing feature parity. This preparatory work is part of the larger effort to enable cloning specific revisions without creating remote-tracking branches.