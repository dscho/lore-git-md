Refactoring patch moving `valid_remote_name()` from refspec to remote subsystem. This v2 iteration follows up on Patrick Steinhardt's review of the initial version, implementing two requested changes: clarifying the commit message to explicitly mention both header and implementation file moves, and verifying no unnecessary `#include` cleanup opportunities exist in refspec.c.

The patch performs a straightforward relocation of the function definition from refspec.c to remote.c and its declaration from refspec.h to remote.h, maintaining identical behavior. The function continues to validate remote names by constructing a test refspec and checking it with `valid_fetch_refspec()`. 

This change better aligns the function's location with its logical home in the remote subsystem rather than refspec, following Junio Hamano's earlier suggestion about code organization. The thread has reached consensus with all review feedback addressed, and the patch appears ready for integration as a clean code organization improvement with no impact on functionality.