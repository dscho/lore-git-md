This is a feature patch that has gone through multiple review iterations, now ready for potential inclusion in Git's 'next' branch. It adds `--expire-to=<dir>` support to `git gc`, mirroring existing functionality in `git repack` for managing cruft packs.

The patch has evolved through four versions with all substantive feedback addressed, including documentation clarifications and expanded test coverage. Junio Hamano is now asking if there are any final objections before marking it for inclusion. The implementation appears solid, with proper handling of option combinations and clear documentation stating that `--expire-to` requires `--cruft`.

Key technical points:
- Modifies `git gc` to pass through the `--expire-to` parameter to `git repack`
- Adjusts behavior when `--prune=now` is used with cruft packs to store rather than delete unreachable objects
- Includes comprehensive tests verifying various option combinations

The change simplifies repository maintenance workflows by allowing direct cruft pack expiration through `git gc` rather than requiring separate `git repack` and `git prune` commands. All core reviewers have signed off on the technical approach, and the patch appears ready for the next step in the integration process.