This is a feature patch that completes the implementation of `--expire-to` support in `git gc`, mirroring the existing `git repack --expire-to` functionality for cruft pack management. The patch has gone through four review iterations with all substantive feedback addressed, and Junio Hamano is now calling for final review before merging to 'next'.

The change allows specifying a directory for expired cruft packs during garbage collection, which is particularly useful for server-side maintenance workflows. The implementation carefully handles the interaction between `--expire-to`, `--cruft`, and the existing `-a` flag behavior, ensuring backward compatibility while adding the new functionality. The documentation clearly states that `--expire-to` requires `--cruft` to be effective.

Technical details include:
- New `repack_expire_to` field in `gc_config` structure
- Modified `add_repack_all_option()` logic to handle the new parameter while maintaining backward compatibility
- Special handling of `-a` flag skipping when both `--cruft` and `--expire-to` are specified
- Comprehensive test coverage in t6500-gc.sh verifying various option combinations including `--no-cruft` scenarios

The patch appears well-tested and ready for inclusion, having incorporated all reviewer feedback from Jeff King and Junio Hamano regarding documentation clarity and test coverage. It aligns with Taylor Blau's ongoing cruft pack work and provides independent utility for repository maintenance.