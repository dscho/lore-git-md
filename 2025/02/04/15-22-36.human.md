This is a follow-up discussion in a bugfix thread about `git repack --keep-unreachable`. The original series fixed an edge case where loose unreachable objects weren't being packed when no preexisting packfiles existed. 

Jeff King (the original author of the `--keep-unreachable` feature) confirms Patrick Steinhardt's v2 patch is correct, clarifying his own earlier misunderstanding. He explains that both test scenarios (with and without reachable objects) were actually exhibiting the same root bug - the failure to pack loose unreachable objects - just manifesting differently based on repository state. 

The key insight is that the "Nothing new to pack" message in empty repositories wasn't a separate issue as initially suspected, but rather another symptom of the same underlying problem. With the fix in place, both cases now work as intended. The discussion shows thorough analysis from both contributors, with the original feature implementer validating the solution.

This email effectively concludes the thread, confirming the fix addresses all edge cases and that no further changes are needed. The patch appears ready for merging with consensus from all key participants.