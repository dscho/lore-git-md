This is a documentation refinement for the worktree detection bugfix. Olga Pilipenco and Junio Hamano finalize the inline comment explaining the `!worktree->is_current` optimization in the worktree detection logic. The discussion has narrowed to two documentation options, with Junio preferring the more detailed version that explicitly connects the `is_current` flag to main worktree identification and explains the config lookup behavior. 

The chosen documentation will clarify:
1. The need for extra config checks in secondary worktrees
2. Why the check is unnecessary in the main worktree
3. The relationship between `is_current` flag and main worktree status
4. The technical context of `$commondir/config.worktree`

This represents the final polish on a previously resolved technical issue, with all substantive questions about the implementation already addressed in earlier rounds. The thread demonstrates Git's attention to both technical correctness and maintainability through clear documentation of non-obvious implementation details. The patch is now complete with maintainer-approved documentation style and ready for merge.