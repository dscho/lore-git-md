Bugfix patch in the packed-refs validation series, addressing progress reporting and validation control in `git fsck`.

This final patch in the v2 series responds to Patrick Steinhardt's review feedback about the ref validation progress meter and control mechanism. Shejialuo explains their initial approach of a simple status message broke existing tests in t1050-large.sh that expect empty stderr output from `git fsck`. The current implementation uses either verbose mode or progress meter to show the "Checking ref database" message, following existing patterns in fsck.c.

Patrick suggests removing the progress meter since it only shows 0/1 states, and proposes adding a `--[no-]references` option to control whether ref validation runs during fsck. Shejialuo agrees this is a good idea and will implement it in the next version. The discussion highlights the careful balance between user feedback and maintaining backward compatibility with existing test expectations.

The patch touches builtin/fsck.c, integrating the new ref validation as a child process while preserving the existing behavior where tests expect no output. The thread shows the series is nearing completion, with this being one of the final refinements before the comprehensive packed-refs validation can be merged.