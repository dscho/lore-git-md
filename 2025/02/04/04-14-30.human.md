This is a refactoring patch that moves the `valid_remote_name()` function from `refspec.[ch]` to `remote.[ch]`. The change follows up on a previous suggestion from Junio Hamano that this function more naturally belongs with the remote-related code.

The patch is straightforward - it removes the function and its declaration from the refspec files and adds them to the remote files, with no changes to the implementation. The commit message cites the specific discussion where this relocation was suggested.

This appears to be a clean, uncontroversial change that better organizes the code by putting remote-related functionality together. As a GSoC contribution, it shows good attention to code organization and responsiveness to maintainer feedback. The patch applies cleanly and makes logical sense, though it's a fairly minor change in the grand scheme of things.