This is a small refactoring patch in Eric Ju's larger series implementing remote object-info functionality. The change is straightforward: it moves loop counter variable declarations inside their respective `for` loops where they're only used locally, and updates the type from `int` to `size_t` where appropriate for array indexing. The modifications touch two files (`builtin/cat-file.c` and `fetch-pack.c`) with simple mechanical changes that improve code readability and type safety.

The patch is part of the v11 series that has already undergone extensive review, with this particular change being a minor cleanup suggested by Christian Couder. It doesn't affect functionality but follows good coding practices by limiting variable visibility and using more appropriate types. Since this is a well-contained change in an already-reviewed series, it likely won't generate much discussion.