Here is the updated AI thread summary incorporating the new email:

This thread concerns Phillip Wood's two-patch series improving `git add -p`'s hunk splitting behavior in `add-patch.c`, with corresponding test additions in `t/t3701-add-interactive.sh`. The changes address state inheritance during splitting and post-edit status updates, stemming from practical problems encountered during development of the 'pw/add-patch-with-suppress-blank-empty' feature.

The first patch's core change - making split sub-hunks default to "undecided" - has evolved into a nuanced discussion between Phillip Wood and Junio Hamano about workflow tradeoffs. Phillip's latest response to Junio's skip-prompt alternative raises new concerns that skipping selected hunks might create confusion about hunk locations, while acknowledging the corner-case nature of this behavior. The discussion now weighs three dimensions:
1. Interface consistency (current behavior prompts unnecessarily)
2. Workflow efficiency (current behavior enables quick partial deselection)
3. Discoverability (Phillip's original proposal made state transitions explicit)
4. New concern: Visibility of split hunk locations if prompts are skipped

The technical implementation in `add-patch.c` involves hunk state handling during splitting operations, with potential changes to both state inheritance logic and prompt sequencing. The conversation has surfaced two broader UX limitations:
1. Inability to revisit decisions after processing all hunks (Junio's concern)
2. Lack of state visibility when revisiting hunks (Phillip's observation)
3. Navigation commands ('J'/'K') not matching help text behavior

The second patch (with Junio's earlier review) fixes a subtle issue where manual hunk edits could leave `splittable_into` counts out of sync. This patch remains technically sound with Junio's confirmation that the `recount_edited_hunk()` approach aligns with original assumptions, needing only minor test formatting fixes. The comprehensive tests demonstrate:
1. Creating splittable changes
2. Using a fake editor to modify line types
3. Verifying proper splitting into three parts
4. Confirming final staged content

Key participants are Phillip Wood (primary author and `add -p` expert), Junio Hamano (maintainer perspective on workflow implications), and Justin Tobler (technical review). Open questions now include:
1. Whether to:
   - Accept Phillip's original change
   - Implement Junio's skip-prompt alternative
   - Find another compromise
   - Leave behavior unchanged given corner-case nature
2. How each approach affects:
   - New user experience
   - Power-user workflows
   - Code complexity
   - Hunk location visibility
3. Whether to document behavior changes prominently if kept
4. Potential follow-up work on broader UX limitations surfaced

The patches represent focused improvements to `git add -p`'s hunk handling, with 31+ new test lines across both changes. While the second patch awaits only trivial formatting fixes, the first patch's design discussion has deepened with Phillip's response introducing new considerations about hunk location visibility if prompts are skipped. The conversation suggests this may be reaching the point of diminishing returns given the corner-case nature of the behavior in question.