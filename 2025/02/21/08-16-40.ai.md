Packfile maintenance strategy discussion. Patrick Steinhardt responds to the ongoing investigation into excessive pack regeneration, offering both analysis of the current behavior and alternative approaches.

The email makes several key technical points about packfile maintenance:
1. Confirms that frequent repository updates (multiple fetches per day) can legitimately trigger regular repacks under current design, suggesting this may be expected behavior in active repos rather than a bug
2. Proposes switching from `git gc` to `git maintenance` with its "incremental-repack" task as an alternative strategy, using multi-pack-index with controlled batch sizes
3. Shares personal configuration disabling traditional gc in favor of incremental-repack plus other maintenance tasks
4. Identifies `git repack --geometric` as a potentially better future solution for packfile size management

The response provides concrete configuration examples and references to specific Git features (`git-multi-pack-index`, `--batch-size`, `--geometric`) that could help address the reported issues. It distinguishes between current workarounds and ideal future solutions, while acknowledging the complexity of balancing packfile efficiency against repack frequency.

This moves the discussion from problem diagnosis toward solution options, though it doesn't directly address the partial clone case mentioned earlier in the thread. The technical details about maintenance task configuration and geometric repacking provide specific hooks for future discussion about implementation approaches.