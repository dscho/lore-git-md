This is a brief discussion about API design choices in the refs performance optimization series, specifically around ambiguity checks in object name resolution.

Jeff King (peff@peff.net) is responding to Patrick Steinhardt's preference for keeping the new `GET_OID_SKIP_AMBIGUITY_CHECK` flag approach (introduced in patch 2/16) rather than using the existing global `warn_on_object_refname_ambiguity` flag. Patrick had revealed he has local work that builds on this new flag to completely eliminate the global variable.

Jeff acknowledges the value in removing global state but expresses some concern about the complexity of propagating the new flag through the call stack, particularly into `handle_revision()`. While he doesn't oppose the approach, his comment suggests he's weighing the tradeoffs between the current series' complexity and the long-term benefit of state reduction.

This exchange highlights the key architectural decision point in the series - whether to accept some short-term complexity for better long-term design (Patrick's position) or prefer simpler immediate changes (Jeff's initial suggestion). The discussion remains collegial, with both agreeing that eliminating global state is desirable.