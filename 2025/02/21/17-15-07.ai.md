This is a technical discussion about implementing bit array storage in `show-branch.c` using commit-slabs with stride.

Junio Hamano acknowledges Jeff King's pointer to the `_with_stride` variant of commit-slabs from commit a84b794ad0, which was specifically designed for cases needing dynamic scaling of per-commit bit storage. The email reveals that this capability has existed since 2013 but remains unused in practice. Hamano notes the original design appears to have been a collaborative effort addressing the very problem they're now facing - efficiently storing one-bit-per-ref data that needs to scale beyond fixed limits.

The discussion confirms the technical path forward for removing `show-branch.c`'s `MAX_REVS` limitation: using `commit_slab_with_stride` to implement a dynamically-sized bit array storage system. This approach aligns with Hamano's earlier architectural guidance about needing proper bit array manipulation helpers while leveraging existing but dormant infrastructure. The exchange demonstrates alignment between the technical principals (Hamano and King) on both the problem analysis and solution approach, with King providing historical context about the slab implementation that validates the direction.