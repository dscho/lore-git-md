Code style cleanup patch in the remote object-info series.

This is a minor refactoring patch in the larger series implementing remote object-info functionality. It moves variable declarations inside for loops where they are used, improving code locality and readability. The changes also switch from `int` to `size_t` where appropriate for loop counters, which is more correct type for array indexing. The patch affects two files:
- `builtin/cat-file.c`: Modifies several loops in the batch command processing code
- `fetch-pack.c`: Adjusts a loop writing server options

The changes are straightforward and uncontroversial, with Christian Couder providing a Helped-by for the style improvements. This appears to be a final polish before the series is merged, addressing code style consistency across the implementation. The patch doesn't change any functionality but helps maintain Git's coding standards in the new code paths being added by the series.