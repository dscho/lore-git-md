Bugfix targeting branch deletion when bisect is active. This patch addresses a confusing error message that occurs when trying to delete a branch that's being used by an active bisect operation, rather than directly by a worktree as the current message suggests.

The current implementation gives a misleading "used by worktree" error when attempting to delete a branch that's actually been checked out for a bisect operation. The patch proposes changing this to explicitly state when a branch cannot be deleted due to an active bisect, making the error message more accurate and helpful.

The change appears straightforward, modifying the error messaging in the branch deletion logic to detect and report bisect usage separately from worktree usage. This would improve user experience when cleaning up branches after interrupted bisect operations. The patch doesn't include test updates, which would be needed to verify the new behavior.

The problem is clearly described with a real-world scenario, and the proposed solution seems reasonable. However, the patch itself isn't included in this email - it appears to be just raising the issue. A follow-up with actual code changes would be needed to properly evaluate the implementation.