This is a technical follow-up in the `show-branch` refactoring thread about using commit-slabs for flag storage. Jeff King chimes in to point out that the commit-slab infrastructure already has a `_with_stride()` variant designed precisely for handling arbitrary-sized data like the bit arrays Junio Hamano described. He references the original 2013 commit introducing commit-slabs (a84b794ad0) which included this capability, though it has apparently never been used in practice.

The email serves as a helpful implementation pointer rather than proposing new code, confirming that the technical direction suggested by Hamano is feasible with existing infrastructure. King's note about the unused `_with_stride` feature suggests this could be its first real-world application while solving the `MAX_REVS` limitation. The exchange shows the thread progressing toward a concrete implementation approach, with experienced contributors validating the technical path forward.