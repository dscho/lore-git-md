This is a technical discussion about bisection performance optimizations, specifically addressing Christian Couder's feedback on Junio C Hamano's earlier midpoint merge traversal patch.

Junio clarifies several points from Christian's response. First, he notes that first-parent bisection (already implemented since 2020) is orthogonal to the current optimization work, as it uses a different O(N) approach that doesn't need the expensive count_distance() calls. He emphasizes that Uwe's original performance report wasn't about first-parent bisection being slow.

Regarding Christian's suggestion about loosening the "good enough" criteria for midpoint selection, Junio explains the current 0.1% threshold was chosen carefully - the patch already includes (but doesn't enable) a more aggressive 3% variant that could be used when the candidate set is large, though this can't rely on integer division truncation effects.

Junio pushes back on implementing Christian's second suggestion (marking unreachable commits as non-viable) as an optional mode, agreeing it's likely not worth the complexity despite the small code size. He reiterates that the core optimization is focused on improving non-first-parent bisection by reducing count_distance() calls through smarter merge commit selection.

The discussion reveals some disconnect - Christian seems to be suggesting alternative approaches while Junio maintains these are either already implemented (first-parent bisection) or not relevant to the current optimization goal. The thread continues to focus on refining the midpoint merge traversal approach that already demonstrated 30x speedups, rather than exploring entirely new bisection algorithms.