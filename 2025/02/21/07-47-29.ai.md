This is a feature patch adding blob size filtering to `git cat-file`, part of a larger series (4/9) about object type filtering. The patch introduces a new `blob:limit=<n>` filter that excludes blobs larger than the specified size (with support for k/m/g suffixes). 

The implementation adds a new `LOFC_BLOB_LIMIT` filter type to the cat-file machinery, tracking blob sizes during object info queries and skipping output when blobs exceed the limit. The documentation clearly explains the new syntax and unit suffixes. Tests verify the functionality by creating blobs of varying sizes (1KB, 10KB) and checking they're properly filtered at different threshold levels.

The changes are cleanly scoped to cat-file's batch processing mode, with appropriate BUG guards for unsupported filter types. Test coverage is thorough, exercising both the new limit filter and maintaining existing filter behavior. The commit message is concise but complete, though it doesn't explain the feature's motivation (presumably covered in the series' cover letter).

Key technical details for future reference:
- New filter syntax: `blob:limit=<n>[kmg]`
- Internal enum value: `LOFC_BLOB_LIMIT`
- Size comparison happens in `batch_object_write()`
- Tests use generated large blobs (1000B, 10000B) to verify thresholds
- Maintains existing behavior for other filter types and non-batch modes