This is a technical discussion in the ongoing refactoring of `show-branch.c` to replace commit flag storage with a more scalable commit-slab approach. Junio Hamano responds to Jeff King's pointer about the existing but unused `_with_stride` variant of commit-slabs, which was specifically designed for cases like this where bit-per-reference storage needs to scale dynamically.

Hamano expresses appreciation for King's reference to commit a84b794ad0 from 2013, which introduced the `_with_stride` mechanism precisely for handling bit arrays that need to grow with the number of references. He notes this appears to have been a well-considered group design decision from the early days of commit-slabs, though the functionality has remained unused until now.

The exchange confirms the technical path forward for the `show-branch` refactoring: using `commit_slab_with_stride` will properly implement the bit array storage that can scale beyond the current `MAX_REVS` limitation, fulfilling the architectural requirements Hamano outlined earlier in the thread. This moves the effort closer to implementation, with all key technical questions now answered.