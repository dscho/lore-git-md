Phillip Wood's patch series improving `git add -p`'s hunk splitting behavior has completed its review cycle and received final approval from maintainer Junio Hamano. The changes, which modify `add-patch.c` to consistently mark split hunks as requiring explicit user confirmation (`UNDECIDED_HUNK`), address a long-standing UI clarity issue. The solution emerged through multiple iterations, with Junio ultimately favoring an unconditional behavior change over adding configuration options, preferring to revert if needed rather than maintain toggleable complexity.

While clearing the series for merging, Junio noted two remaining UI limitations in the interactive patch workflow: the inability to revisit previously decided hunks without restarting, and the lack of visual feedback about hunk selection states. These were deliberately left as potential future improvements (`#leftoverbits`) since they fall outside the current scope's focus on hunk splitting semantics. The thread concludes with the patches ready for integration, representing a focused improvement to `git add -p`'s behavior while keeping the codebase maintainable.