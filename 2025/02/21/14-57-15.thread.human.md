This thread documents the completion of Phillip Wood's patch series improving `git add -p`'s hunk splitting behavior. The changes make split hunks always appear as undecided, requiring explicit user selection, rather than inheriting their parent hunk's state. The implementation in `add-patch.c` includes thorough test coverage in t3701.

After multiple iterations and review, Junio Hamano approved the changes as a clear improvement to the UI, intentionally avoiding configuration options to keep the code simple. The series is now fully ready for merging, with Phillip checking in one last time about any final commit message tweaks needed before Junio picks it up. Junio confirms the patches are good to go as-is, marking the conclusion of this effort to make hunk splitting behavior more predictable and explicit.