This thread has concluded Phillip Wood's series improving `git add -p`'s hunk splitting behavior. The final patch fixes an edge case where editing a hunk (changing context lines to deletions or vice versa) could leave the hunk's splittability count incorrect. The solution updates the recount logic to track line type transitions during edits, ensuring accurate split points.

The series implements a maintainer-approved approach where split hunks are always marked as requiring explicit user selection, rather than inheriting state from their parent hunk. Junio Hamano rejected version-gating mechanisms, opting for an unconditional behavior change that provides clearer interaction semantics. Phillip has addressed all feedback across multiple versions, with comprehensive test coverage in t/t3701-add-interactive.sh.

With all technical questions resolved and no outstanding objections, the series is now ready for merging. The changes will ship in the next release without configuration options, representing a small but meaningful improvement to the interactive patch workflow. Junio has indicated willingness to revert if users complain, though the cleaner implementation and more predictable behavior suggest this is unlikely to be necessary.