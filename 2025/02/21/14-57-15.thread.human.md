Here's a human-readable summary of the thread:

Phillip Wood has been improving `git add -p`'s hunk splitting behavior in a patch series that's now at version 2. The changes focus on two main areas: how split hunks inherit their state, and fixing a technical issue with hunk editing.

The first patch introduces a new `UNDECIDED_HUNK` state for split hunks when using the `WITH_BREAKING_CHANGES` option, which helps maintain consistency when users split hunks during interactive staging. This change is gated behind the existing breaking changes mechanism to preserve backward compatibility.

The second patch, just submitted, fixes an edge case where editing a hunk (like changing context lines to deletions) could leave the internal `splittable_into` count out of sync. While this situation is rare in practice, the patch ensures proper synchronization by recalculating split points during the recount operation. The fix includes thorough test coverage that verifies behavior under both old and new code paths.

Junio Hamano has been guiding the approach, emphasizing the use of existing version-gating mechanisms and prioritizing technical correctness over cosmetic improvements. The series demonstrates Git's careful approach to changes - breaking behavior is opt-in, fixes are well-tested, and user interface refinements are considered separately from core functionality.

With these two patches, the technical improvements are complete. Some potential UI enhancements were identified (like better hunk state visibility) but are being considered for future work rather than holding up these fixes. The changes are now ready for integration, having addressed all feedback from reviewers.