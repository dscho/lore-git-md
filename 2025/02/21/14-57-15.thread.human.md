Here's the updated human-readable thread summary:

This thread covers a two-patch series from Phillip Wood improving edge cases in `git add -p`'s hunk splitting behavior. The changes address practical issues discovered while developing the 'pw/add-patch-with-suppress-blank-empty' feature.

The first patch fixes counterintuitive behavior where splitting a selected hunk would automatically select all sub-hunks. It now properly marks split sub-hunks as "undecided" to require explicit user selection. The second patch resolves a subtle issue where manual edits to a hunk (changing line types) could leave the split count out of sync with the actual content. Both changes are thoroughly tested with new cases in t3701.

Phillip Wood, as the primary contributor to `git add -p` and sequencer functionality, brings deep expertise to these incremental improvements. The patches maintain the interface's consistency while fixing edge cases - particularly important for the interactive patch workflow. The changes are well-contained in `add-patch.c` with clear commit messages explaining both the technical solutions and practical context. The series appears ready for review, though no feedback has been posted yet.