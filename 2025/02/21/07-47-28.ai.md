This is a feature patch adding blob filtering support to `git cat-file`, part of a larger series (3/9) though no cover letter context is provided. The patch implements the "blob:none" filter option which causes `cat-file` to omit blob objects from its output when used in batch mode.

The implementation adds a new LOFC_BLOB_NONE filter choice to the object filtering system, with corresponding documentation updates and test coverage. The tests are thorough, covering packed objects (both bitmap and non-bitmap cases) as well as loose objects. The patch includes appropriate error handling to ensure the filter is only used in batch mode.

The changes touch three files: the cat-file documentation to describe the new filter, the cat-file implementation to add the filtering logic, and the test script with new test cases. The test infrastructure is well-designed, with a helper function that could be reused for testing future filter types. The implementation appears clean and focused, with no obvious technical issues in the diff.