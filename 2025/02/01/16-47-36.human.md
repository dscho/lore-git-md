This is a documentation review in the context of a larger refactoring series preparing for a new `--revision` clone option.

Jean-Noël AVILA provides two specific documentation suggestions for the patch that inverts clone's tag handling from `--no-tags` to `--tags` as the default. First, they point out a typo in the option syntax (`--[no-]-tags` with an extra hyphen) that needs correction. Second, they recommend maintaining the imperative mood in the documentation text ("Don't clone any tags") rather than starting with "By default" phrasing, suggesting the default behavior explanation should come after the command description.

The review is narrowly focused on documentation style and correctness, not challenging the underlying code changes (which the thread context shows are preparatory work for the `--revision` feature). The author will need to adjust the man page formatting but the core refactoring work appears uncontroversial based on the thread history.