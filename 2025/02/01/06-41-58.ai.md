This is the first patch in a refactoring series moving refspec-related functions from remote.c to refspec.c. The patch focuses on renaming `omit_name_by_refspec()` to the more descriptive `refname_matches_negative_refspec_item()`, which better conveys its purpose of checking if a reference name matches any negative refspec. The change includes updating all callers and removing redundant documentation since the new function name is self-explanatory.

The patch makes several specific changes:
- Renames the function and its parameter from `name` to `refname` for consistency with `refs.h` conventions
- Updates four call sites across remote.c and builtin/remote.c
- Removes the doc comment from remote.h since the name now clearly indicates the function checks for negative refspec matches
- Maintains identical behavior while improving clarity

This is part of a larger effort to centralize refspec logic, with this initial patch focusing on establishing clearer naming before the subsequent moves. The change is well-contained (6 insertions, 10 deletions) and follows through on naming improvements suggested in earlier review rounds. The patch appears technically sound and ready for inclusion, representing the first step in the refactoring series that has already undergone thorough review iterations.