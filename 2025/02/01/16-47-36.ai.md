This is a documentation review in the context of a larger refactoring series preparing for a new `--revision` clone option. Jean-Noël AVILA provides specific feedback on the documentation changes in patch 2/6, which inverts the internal handling of tag cloning from `--no-tags` to `--tags` (default-enabled).

The reviewer notes two documentation issues:
1. A typo in the option syntax where `--[no-]-tags` incorrectly includes an extra hyphen
2. A style suggestion to maintain the imperative mood in the description ("Don't clone any tags") rather than starting with "By default", while still allowing the default behavior explanation to be included later

The patch itself (not shown in full here) is part of significant preparatory work that has already moved 20+ globals to local scope and restructured tag handling to simplify the codebase before implementing the new `--revision` feature. This particular change makes the internal logic default to cloning tags (with `--no-tags` as the opt-out) rather than defaulting to no tags, which the author argues simplifies future changes.

The review is narrowly focused on documentation presentation rather than the underlying code changes, suggesting small but important consistency fixes to match Git's documentation conventions. The thread context shows this is part of a well-advanced series (v4) that has already addressed several rounds of technical feedback on the implementation approach.