Refactoring patch in the ongoing refspec centralization series. This is patch 3/5 from Meet Soni's v3 submission, which renames several refspec-related functions for clarity. The changes introduce more descriptive names that better reflect each function's purpose: `query_refspecs()` becomes `find_refspec_match`, `query_refspecs_multiple()` becomes `find_all_refspec_matches`, and `query_matches_negative_refspec()` becomes `find_negative_refspec_match`.

The patch is part of a well-reviewed series that's systematically moving refspec logic from remote.c to refspec.c while maintaining exact behavior. This particular change focuses purely on naming improvements, following earlier feedback in the thread. The new names make it immediately clear whether a function looks for a single match or multiple matches, and whether it handles negative refspecs.

The changes are mechanical but meaningful, touching three files (remote.c, remote.h, and builtin/push.c) with straightforward string substitutions. The patch maintains all existing functionality while improving code readability. Based on the thread context, this appears to be one of the final polishing changes before the series is ready for merging.