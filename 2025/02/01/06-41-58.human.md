This is the first patch in a refactoring series that centralizes refspec-related functions. The patch renames `omit_name_by_refspec()` to the more descriptive `refname_matches_negative_refspec_item()`, making its purpose clearer - it checks if a reference name matches any negative refspec. The change is purely a naming improvement with no behavior modification, updating all callers and removing redundant documentation since the new function name is self-explanatory.

The patch is part of a well-reviewed series (now at v3) that has evolved through multiple iterations with feedback from Junio C Hamano. This particular change shows good attention to detail - it not only improves the function name but also standardizes parameter naming to match `refs.h` conventions. The series as a whole appears ready for merging, with all major feedback addressed and thorough documentation improvements.