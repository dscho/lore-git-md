This is a small documentation and implementation patch that adds "show" as a valid value for the `help.autocorrect` configuration option. It's part of a well-developed thread that has refined the autocorrect behavior through multiple iterations.

The patch adds "show" as an equivalent to 0/"false"/"off"/"no" in the configuration parsing, making it consistent with the other boolean-style options. The change is minimal - just adding the new string to the parser and documentation, plus a test case. The author notes this is purely for consistency and is willing to drop it if not deemed useful.

This comes after the thread has already settled on the core behavior where:
- False-like values (now including "show") just display the correction
- "never" does nothing
- True-like values execute the correction (either immediately or after a delay)

The patch appears straightforward and uncontroversial, maintaining the careful attention to interface consistency shown throughout this thread. It's a minor polish on top of the already-solid solution.