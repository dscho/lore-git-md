This is the final patch in Meet Soni's refactoring series to centralize refspec logic in refspec.c. The patch moves the remaining two refspec-related functions from remote.c to refspec.c, completing the systematic relocation of all refspec handling code.

The patch relocates `apply_refspecs()` and `apply_negative_refspecs()` with their exact implementations, maintaining all existing functionality. These functions handle core refspec application logic - matching ref names against refspecs and filtering references based on negative refspecs. The movement follows the same pattern as previous patches in the series, with corresponding header updates (adding declarations to refspec.h while removing them from remote.h).

Key technical details:
- 32 lines added to refspec.c implementing both functions
- 31 lines removed from remote.c
- Header declarations moved from remote.h to refspec.h
- Maintains all existing behavior including edge case handling
- Preserves the interaction with `find_refspec_match()` moved in patch 4/5
- Documentation comments for both functions are preserved in the new location

This completes the mechanical relocation portion of the refactoring series, with all refspec-related functions now centralized in refspec.c. The thread history shows this series has undergone thorough review, with all major feedback addressed in previous iterations. The changes appear merge-ready, representing a clean organizational improvement that maintains exact behavior while making the codebase more maintainable.