Documentation maintenance patch from a new contributor. This single-patch series proposes renaming all `.txt` files in Git's Documentation directory to `.adoc` extension to better reflect their AsciiDoc formatting. The author notes this addresses a #leftoverbits issue and appears to be their first contribution to the project.

The change is straightforward - a bulk rename operation with no content modifications. While the commit message is brief, the motivation is clear: standardizing on `.adoc` extensions for AsciiDoc files makes their format immediately obvious to readers and tooling. This appears to be a uncontroversial cleanup, though reviewers may want to verify no build system or documentation tooling relies on the `.txt` extension.

As a new contributor's first patch, this represents a good starter project - small in scope, clearly tagged as cleanup work, and touching only filenames rather than content. The patch would benefit from a more detailed commit message explaining why `.adoc` is preferred over `.txt` for AsciiDoc files in Git's docs.