Refinement to packed-refs sortedness verification in the ongoing fsck validation series.

This email from shejialuo acknowledges Junio Hamano's correction about when sortedness verification should occur in the packed-refs validation checks. The key clarification is that the check should only run when the packed-refs file explicitly declares a "sorted" trait in its header, not unconditionally. This matches the runtime contract where Git only guarantees sorting when explicitly declared.

The email indicates shejialuo will update the patch in the next version to reflect this more precise behavior. This continues the thread's evolution around validation semantics, where earlier discussion had established that the runtime only enforces sorting when the header declares it, and the fsck checks should follow the same contract. The change aligns with Junio's guidance to maintain compatibility with older Git versions and respect the packed-refs format's optional traits.

This adjustment is part of the larger series introducing comprehensive packed-refs validation that will be integrated into `git fsck` via a new `git refs verify` subprocess. The series has seen significant refinement through review cycles, particularly around header validation semantics and now sortedness verification behavior.