Documentation patch standardizing terminology in gitdiffcore.adoc for pickaxe options. This is part of the ongoing series adding long option names (`--patch-grep`/`--patch-modifies`) alongside the existing `-G`/`-S` options.

The patch updates the documentation to consistently use shorter placeholder names (`<regex>` and `<string>`) instead of the more verbose `<regular-expression>` and `<block-of-text>`. The author argues this matches usage elsewhere in Git's docs and codebase, and that the terms are familiar enough to Git users. The changes are purely cosmetic - the actual behavior of the options remains unchanged.

This appears to be a straightforward cleanup that improves consistency. The patch is well-contained, touching only documentation, and follows the series' pattern of making the pickaxe options' documentation more uniform. It builds on earlier patches that fixed option descriptions and added long-form equivalents.