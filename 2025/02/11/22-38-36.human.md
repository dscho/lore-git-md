This is a protocol design question about bundle-uri advertisement in Git clones. Brian Celenza is exploring how servers could provide optimized bundle URLs based on the client's eventual fetch parameters (like filters), but notes the current protocol ordering prevents this since the `bundle-uri` command happens before the `fetch` command reveals the client's intent.

The email clearly identifies a gap: servers must guess the client's needs when advertising bundles, potentially leading to inefficient transfers. Brian asks whether this is simply the current protocol limitation or if there's prior art for clients to signal their intent earlier. The question is well-motivated and touches on real-world optimization concerns for bundle-based workflows.

This appears to be a genuine exploration rather than a bug report or feature proposal. The technical context is solid, and the question could spark productive discussion about protocol evolution or workarounds. No immediate solutions are suggested, making this more of a conversation starter than a concrete patch.