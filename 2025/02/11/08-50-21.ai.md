This is part 9/10 of the pickaxe long-option series, focusing on documentation updates to help text. The patch updates the help messages for `--pickaxe-all` and `--pickaxe-regex` to include references to their new long option names (`--patch-grep` and `--patch-modifies`), making the relationship between short and long forms clearer for users.

The changes modify both the runtime help text (in diff.c) and the static documentation string (in diff.h) to consistently show both forms together. This helps users understand that `-S/--patch-modifies` and `-G/--patch-grep` are equivalent options. The help text now explicitly shows that `--pickaxe-regex` affects both the `-` and `--` variants of the pickaxe options.

The patch follows the series' pattern of maintaining backward compatibility while improving discoverability. It's a straightforward documentation update that completes the integration of long option names into the help system, following earlier patches that implemented the core functionality and test coverage. The changes are minimal and focused, touching only the help text strings while preserving all existing behavior.