This is a documentation patch in a series introducing long option names for pickaxe search functionality. It fixes a minor naming typo in test t4209-log-pickaxe.sh where a dummy argument for `-G` was incorrectly labeled as "string" rather than "regex" to match the option's actual behavior and the naming convention used elsewhere in the test file.

The change is purely cosmetic - it doesn't affect test logic - but improves consistency by using the correct terminology for the `-G` option, which searches using regular expressions rather than simple string matching. The patch demonstrates attention to detail in aligning test documentation with actual functionality, though the impact is minimal since this is just a dummy value in an error case test.

This is the first of 10 patches in v4 of the series, focused on cleaning up documentation and test cases before introducing the new long option names. The change follows the pattern seen elsewhere in the test file where `-G` arguments are consistently labeled as "regex". While trivial, such consistency improvements help maintain code quality and reduce potential confusion for future readers.