Feature patch adding long option names for pickaxe search functionality. This is patch 6/10 in a series introducing `--patch-grep` as an alias for `-G` and `--patch-modifies` for `-S`, making Git's pickaxe options more discoverable and script-friendly.

The patch updates documentation (diff-options.adoc, gitdiffcore.adoc) to show both short and long forms together, modifies error messages in diff.c to display both variants (e.g., "-G/--patch-grep"), and adds corresponding entries in diff.h's help text. Test coverage in t4209-log-pickaxe.sh is expanded with duplicate tests for both short and long forms, verifying they behave identically.

The changes are straightforward and maintain backward compatibility - the new long options are pure aliases with identical functionality to their single-letter counterparts. The implementation looks clean, with consistent updates across documentation, help text, and tests. This continues the series' trend of careful, incremental improvements to Git's pickaxe interface.