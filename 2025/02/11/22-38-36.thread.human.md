This thread explores a protocol design challenge in Git's bundle-uri mechanism, specifically around optimizing bundle selection for filtered clones. Brian Celenza originally raised the question of whether the current protocol sequence creates a limitation, as servers must select bundles (during the bundle-uri phase) before clients specify their filter preferences (during fetch). 

Brian Carlson has now confirmed this is indeed a current protocol limitation - there's no way for clients to signal filtering intentions early. While the bundle-uri command could theoretically support parameters to address this, no such capability exists today. Carlson suggests this likely reflects limited use rather than a deliberate design constraint, leaving the door open for future protocol extensions if needed.

The discussion remains at the exploration stage, with participants examining whether this sequencing issue is fundamental or could be addressed through capability extensions. Key technical considerations include the bundle-uri command's parameter support potential and the protocol's capability negotiation mechanism. The thread appears likely to continue exploring potential solutions to better support optimized bundle delivery for filtered clone scenarios.