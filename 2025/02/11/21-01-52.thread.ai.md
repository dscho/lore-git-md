Thread identity: Documentation patch clarifying merge's renormalize option behavior, specifically when line-ending normalization is applied during merges.

Core problem: The patch addresses confusion about when Git applies line-ending normalization with the `-X renormalize` option and `merge.renormalize` config. It makes clear this only happens for files requiring three-way content merges, not all files in the tree.

Current status: Documentation-only patch (no code changes) updating three files to clarify this behavior. The commit deliberately omits discussion of the more obscure modify/delete conflict case where normalization also applies.

Key technical details: Files modified are `Documentation/config/merge.txt`, `Documentation/gitattributes.txt`, and `Documentation/merge-strategies.txt`. The changes consistently add clarification about three-way merge cases being the primary scenario for renormalization.

Open questions: None raised in this patch, though the commit references a previous discussion thread (linked via lore.kernel.org) that presumably surfaced the original confusion.

Participant positions: Single-author patch focused on documentation clarity. No reviews or objections noted in this initial submission.

Related work: References prior discussion thread about this behavior (exact link not preserved in summary but present in original commit message).