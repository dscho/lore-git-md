Documentation patch in the pickaxe long-options series. This is patch 4/10 in version 4 of the series, focusing on updating the short help text in diff.h to include both the short (`-G`) and long (`--pickaxe-grep`) forms of the pickaxe grep option.

The change adds the `-G<regex>` description that was previously missing from the help text, while also introducing the new `--pickaxe-grep` option as an alias. It also updates the `--pickaxe-all` description to clarify it works with both `-G` and `-S` options. The patch maintains consistency with the existing help text format and aligns with the series' goal of improving discoverability of pickaxe options.

Key technical details:
- Adds missing `-G<regex>` help text explaining it finds differences where patch contains the regex
- Introduces `--pickaxe-grep` as the long option equivalent to `-G`
- Updates `--pickaxe-all` description to reference both `-G` and `-S` options
- Maintains same help text format and style as existing options
- Part of broader effort to document both short and long option forms together

This is a straightforward documentation update that follows the series' pattern of presenting both short and long option forms together. The change is minimal and focused, touching only the help text in diff.h while maintaining backward compatibility.