This is a technical follow-up in the memory management bugfix thread, where Jeff King (peff) provides additional bisection details and methodology improvements.

Jeff points out that the original bisect script from D. Ben Knoble might not reliably catch the use-after-free issue, since undefined behavior can manifest in different ways (segfault, garbage output, or even appearing to work). He recommends using `SANITIZE=address,undefined` builds for more reliable detection of memory errors, which reveals the bug actually dates back to v2.37.0 and commit 087c745833 ("log: add a --no-graph option").

The email also discusses practical bisection techniques, noting that test commands should use stable references (like `origin`) rather than HEAD to avoid non-deterministic results. Jeff shares his own successful command that reliably triggered the ASan detection without needing commit-limiting flags. This adds important context to the investigation while confirming the bug's origin aligns with the feature introduction of `--no-graph`.