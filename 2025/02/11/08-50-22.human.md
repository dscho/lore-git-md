This is the final documentation patch in a series introducing long option names for Git's pickaxe search functionality (`--patch-grep` for `-G` and `--patch-modifies` for `-S`). The patch updates three documentation files to consistently use the new long option names, with only a few exceptions where the short forms are mentioned for completeness.

The changes are primarily mechanical replacements of `-G` with `--patch-grep` and `-S` with `--patch-modifies` across the documentation. The most notable change is in gitdiffcore.adoc, where the author reorganized the text to introduce the short forms in a subsequent paragraph while keeping the mnemonic explanation for `-G`. This improves readability while maintaining all technical accuracy.

The patch maintains all existing examples and explanations, just with the new option names. The author notes that some reformatting was needed to keep lines within width limits, which might make the diff appear more extensive than the actual changes. This completes the documentation updates for the pickaxe long-option series, bringing it in line with the implementation changes from earlier patches.