This is a follow-up discussion in the progress reporting for multi-pack-index verification thread, where Junio C Hamano provides additional feedback on the technical approach. The maintainer emphasizes that the original patch's commit message failed to justify its assumptions about qsort() behavior, which is critical since the C standard imposes no requirements on comparison call order. 

Hamano notes the author's response about the TODO comment motivation is insufficient - while showing progress is desirable, the patch needs to properly document its technical foundations. The key issue remains that tracking positions during qsort() is fundamentally unreliable without implementation-specific guarantees. 

This exchange highlights the architectural challenge at the core of the feature: progress reporting requires predictable comparison ordering that standard qsort() doesn't provide. The thread has moved from implementation details to questioning whether any solution exists within standard C constraints. The maintainer's feedback suggests the patch needs either a platform-specific approach (if viable) or clear documentation of why progress reporting isn't possible, rather than an implementation based on invalid assumptions.