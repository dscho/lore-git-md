This is a continuation of the discussion about configuration precedence between `pull.ff=only` and `branch.<name>.rebase` settings. Alex Henrie responds to D. Ben Knoble's proposal for a new `pull.ff=onlyUnlessOverridden` option by advocating for a more comprehensive solution with per-branch fast-forward controls.

Alex argues against adding another `pull.ff` variant, stating the configuration matrix is already too complex. Instead, he strongly recommends implementing per-branch fast-forward settings (`branch.<name>.ff` and `branch.autoSetupFf`) as a more general solution that would enable various workflow patterns beyond just the current conflict case. He suggests this would be a better long-term investment despite being more work initially, and offers mentoring support for the implementation.

The email highlights the ongoing tension in the thread between targeted fixes (Knoble's proposal) and more systemic solutions (Henrie's preference). Key technical points include the existing implementation patterns that could be followed (`branch.<name>.rebase` as a model) and the potential workflow flexibility that per-branch controls would enable. The discussion remains focused on finding the right balance between configuration complexity and user control over merge behavior.