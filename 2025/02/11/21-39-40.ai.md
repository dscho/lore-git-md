This is a technical discussion about NFSv4 packfile handling and temporary file permissions, continuing the thread about failover behavior in HA environments. Jeff King (peff) responds to Brian Carlson's skepticism about a Git-side fix by proposing a concrete solution: creating tempfiles with 0644 permissions during write operations, then switching to 0444 via fchmod before the final rename into place. He notes this pattern already exists in Git's adjust_shared_perm() infrastructure.

The key technical points are: (1) the proposed solution would maintain Git's security model of read-only packfiles while potentially working around NFSv4's permission evaluation timing, (2) it would add minimal overhead (one extra chmod call) for systems not affected by the NFS issue, and (3) it mirrors existing permission-adjustment patterns in the codebase. The email leaves open whether this approach would actually resolve the failover issues in practice, but presents it as a potentially standards-compliant alternative to the current behavior.

This moves the discussion forward from theoretical concerns to a specific, implementable change that could be tested against the NFSv4 failover scenario while maintaining Git's security invariants. The solution would touch object-file.c's tempfile creation logic and potentially wrapper.c's file handling, building on existing permission-adjustment infrastructure.