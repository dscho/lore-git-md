This is a documentation patch in the pickaxe long-options series, specifically fixing the help text description for `-S`. 

The patch corrects the short help text in diff.h` to more accurately describe what `-S` (and its new alias `--patch-modifies`) actually does. The old text suggested it only finds filepairs where one side contains the string while the other doesn't, which was misleading. The new text properly explains that it detects changes in the *number of occurrences of the string*, which is the true behavior.

This is patch 3/10 in version 4 of the series, following Junio Hamano's feedback about documentation accuracy. The change is minimal - just a single line in diff.h - but important for user understanding. The patch maintains consistency with the thread's goal of improving pickaxe option clarity while keeping the implementation focused.

The correction aligns with the series' attention to detail in documentation, though it's a small enough fix that it likely won't generate much discussion beyond perhaps some stylistic notes about the phrasing. The change is clearly correct and uncontroversial.