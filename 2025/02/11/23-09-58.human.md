This is a follow-up discussion in an ongoing bugfix thread about memory management issues in Git's revision walking code. The thread has established that the bug was introduced in v2.37.0 with the addition of `--no-graph` support (commit 087c745833) and causes segfaults when combining `--graph` and `--no-graph` options with certain output formats.

Junio Hamano confirms Jeff King's findings about the bug's origin and bisection methodology. Jeff discovered that using a stable reference like `origin` rather than `HEAD` makes the bisection more reliable, since the test command fails deterministically with address sanitizer builds on affected versions. The discussion reinforces that the technical solution (nulling out freed pointers) is correct, while also refining the historical understanding of when the bug was introduced.

The exchange shows the maintainer validating the investigation approach while the thread moves toward finalizing the fix with proper test coverage and backport decisions. No new technical concerns are raised - this is primarily about confirming the bug's timeline and sharing useful debugging techniques.