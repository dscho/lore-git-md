This is a bugfix patch addressing promisor pack handling in `git repack`, specifically around the interaction between `--keep-pack`/`.keep` files and `gc.bigPackThreshold`. The patch author Tomáš Trnka responds to Han Young's technical concerns about the implementation approach, providing real-world validation that their solution works as intended in a production scenario with large promisor packs.

The key technical point is that while Han Young correctly identified the theoretical issue with the initial approach (promisor objects being collected before pack-objects sees them), Trnka demonstrates through practical testing that the patch successfully prevents repacking of large promisor packs that should be kept according to `gc.bigPackThreshold`. The patch appears to properly integrate with `want_found_object()`/`add_object_entry()` in pack-objects.c to honor keep-pack rules.

The email highlights significant performance implications - avoiding unnecessary repacking of 80GB promisor packs on btrfs (where this would consume substantial CPU, RAM, and disk space due to snapshot overhead). This provides concrete motivation for the change beyond the theoretical correctness concerns.

The thread remains in technical discussion phase, with the author defending their implementation against concerns while acknowledging they may not fully understand the internals. The core issue around promisor pack handling during object collection versus packing time remains open, but this email provides empirical evidence that the current approach works in practice despite theoretical objections. The formatting issues preventing clean patch application are still unresolved.