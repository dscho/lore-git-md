Bugfix discussion about misleading error messages during `git rebase --continue` when the editor fails.

Björn Lindqvist follows up on a thread about confusing error messages when `git rebase --continue` fails to launch the default editor (vi). The current error suggests using `-m` or `-F` options, which would discard the carefully preserved commit message and authorship information that rebase maintains. 

Phillip Wood had previously noted this comes from `git commit`'s generic editor-failure handling, suggesting it might be better to recommend setting up a working editor instead. Lindqvist argues the current message is particularly problematic for rebase scenarios where preserving the original message is crucial.

The discussion highlights a tension between generic error handling (from `git commit`) and the specific needs of rebase operations. Lindqvist demonstrates the current behavior produces an unhelpful message that could lead users to accidentally discard important metadata if they follow the suggested `-m` workaround.

This appears to be a legitimate usability issue that could confuse users during rebase operations, though the thread hasn't yet reached consensus on the best solution. The discussion remains focused on improving error messaging rather than proposing code changes at this stage.