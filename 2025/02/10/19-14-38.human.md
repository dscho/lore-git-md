Refactoring patch in the `git clean` exclusion patterns series.

This is the second patch in a three-part series enhancing `git clean` with persistent exclusion patterns. It performs a straightforward variable rename from `ignored` to `remove_ignored` in the clean command's implementation, making the variable's purpose clearer. The change is purely internal - no behavior is modified.

The patch touches only `builtin/clean.c`, updating the variable name in five places where it's used to control whether ignored files should be removed (the `-x` flag behavior). The new name better reflects the variable's role in the cleanup operation.

This appears to be a preparatory change ahead of introducing new exclusion-related features in the next patch. The series remains focused on its goal of improving exclusion handling, with this patch improving code clarity before adding more complex functionality. The change is small and mechanical, with no obvious issues.