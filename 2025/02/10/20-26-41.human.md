This is a follow-up in the ongoing discussion about Git's pull configuration precedence, specifically addressing how `branch.<name>.rebase` should interact with `pull.ff=only`. D. Ben Knoble responds to Alex Henrie's proposal for new branch-level `ffOnly` options by suggesting a more targeted solution: introducing a new `pull.ff=onlyUnlessOverridden` value that would respect branch-specific rebase settings while maintaining fast-forward-only behavior elsewhere.

The email highlights the philosophical tension in the thread - whether to prioritize configuration specificity (branch settings overriding global ones) or safety enforcement (global ff-only taking precedence). Knoble's proposal attempts to bridge these by creating an explicit opt-in mechanism for the override behavior, suggesting names like `onlyOr[Branch]Rebase` as alternatives to convey the nuanced behavior.

This continues the thread's evolution from a simple precedence bugfix toward more flexible configuration design, though Knoble expresses hesitance about implementing Henrie's more expansive branch-level controls. The discussion remains focused on finding a balance between workflow flexibility and safety guarantees in Git's pull behavior.