Here's an updated human-readable summary of the thread:

The discussion explores a proposal from Isaac Oscar Gariano to extend `git commit` to directly accept untracked files, rather than requiring a separate `git add` step. Currently in the feedback-gathering phase, the idea would modify commit's behavior to optionally include untracked files, either by expanding the `-a` flag or adding a new option.

Brian m. carlson has weighed in with thoughtful feedback, acknowledging the potential utility (particularly for test scenarios where committing entire directory contents might be useful) while advocating for caution. They suggest introducing this as an explicit `--include-untracked` flag rather than modifying `-a`'s behavior, emphasizing the importance of maintaining backward compatibility and preventing accidental commits. Brian notes that while they personally prefer careful staging, they recognize different workflows might benefit from this change.

The thread now has a concrete suggestion (`--include-untracked`) to consider, though no patches have been submitted yet. Key open questions remain about whether this should be opt-in versus default behavior, and how to balance convenience with Git's traditional emphasis on explicit staging. The discussion remains focused on usability improvements while respecting Git's conservative approach to behavior changes.