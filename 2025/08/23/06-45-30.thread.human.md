Here's a human-readable thread summary:

Isaac Oscar Gariano has proposed making `git commit` more convenient by allowing it to directly commit untracked files without requiring a separate `git add` step first. The suggestion includes extending this behavior to work with the `-a` option as well. This change would streamline workflows where users frequently need to commit newly created files.

The proposal is currently at the idea stage, with the author seeking community feedback on whether this functionality would be welcome before implementing it. Key open points include whether the change should be gated behind a new option for backward compatibility, and what the exact behavior should be when combining untracked files with other commit options. The change would primarily affect the `git commit` command-line behavior and related documentation, without apparent technical implementation challenges.