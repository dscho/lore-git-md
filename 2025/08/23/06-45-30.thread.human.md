Here's the updated human-readable thread summary:

The thread discusses a proposal from Isaac Oscar Gariano to extend `git commit` to accept untracked files directly, rather than requiring a separate `git add` step. Currently in the discussion phase, the main suggestion from Brian m. carlson has been to implement this as an opt-in `--include-untracked` flag to maintain safety and backward compatibility, rather than modifying the existing `-a` behavior.

Michal Suchánek has now introduced a different perspective, suggesting that rather than modifying Git's index-based model, users who find this workflow cumbersome might consider alternative version control tools like jujutsu that don't use an index. This shifts the discussion to more fundamental questions about Git's design principles versus workflow ergonomics, though it doesn't directly engage with the technical specifics of the proposed flag implementation.

The core questions remain open: whether to proceed with the `--include-untracked` approach, how to evaluate the safety/usability tradeoffs, and whether this change aligns with Git's design philosophy. No patches have been submitted yet to prototype the suggested behavior.