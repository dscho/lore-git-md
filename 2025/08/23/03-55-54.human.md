Refactoring patch in the Rust xdiff infrastructure series (v3 13/15). This continues the transition from chastore to ivec by removing the now-redundant `recs` field from `xdfile_t`, which previously duplicated the record array already stored in the `record` ivec. The change touches all xdiff components (diff algorithm, merge handling, patience/histogram variants) to use direct ivec access instead of the intermediate pointer array.

The patch is a straightforward cleanup following the earlier structural changes in v3 11/15 and 12/15. It demonstrates careful attention to detail with updates across all xdiff subsystems and maintains the series' pattern of incremental, behavior-preserving changes. The diff is best viewed with `--color-words` to highlight the systematic replacement of `recs[i]` with `record.ptr[i]` patterns.

This continues Ezekiel Newren's methodical preparation work for Rust integration, now eliminating a vestigial data structure after completing the ivec transition. The changes are mechanical but thorough, covering all xdiff components that previously used the redundant array.