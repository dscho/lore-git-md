This is the second patch in a 15-part series introducing Rust infrastructure for xdiff, focused on build system integration and type definitions. The patch establishes the foundational Rust build setup while deferring actual xdiff functionality to later patches.

The key technical changes include:
- Adding Rust build infrastructure via `build_rust.sh` script and Makefile/meson integration
- Defining Rust primitive types in `git-compat-util.h` (u8, i32, usize etc.) for C/Rust interop
- Setting up a Rust workspace with `xdiff` and `interop` crates (empty for now)
- Modifying build rules to compile Rust before linking Git binaries
- Adding platform-specific handling for Windows/MSVC targets
- Excluding Rust build artifacts from `.gitignore`

The patch maintains the series' zero-copy FFI approach and follows the established policy of using multiple crates for modularity. It implements the build system changes needed for subsequent patches that will actually port xdiff functionality to Rust, while keeping the Rust code itself minimal (empty lib.rs files for now).

Build system changes are comprehensive, touching Makefile, meson.build, and adding a new build_rust.sh script. The patch handles both debug and release builds, with appropriate compiler flags for each. Platform support considerations include Windows/MSVC compatibility and Linux-specific linker flags (-ldl).

This is purely infrastructure work - no behavior changes or optimizations yet. The patch aligns with the series' revised focus on establishing framework first before optimizations, as discussed in earlier thread feedback. Subsequent patches will build on this to implement actual xdiff functionality in Rust.