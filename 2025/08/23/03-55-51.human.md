Refactoring patch in the Rust xdiff infrastructure series (10/15). This patch consolidates memory cleanup in xdiff's file preparation code by moving the `xdl_free_ctx()` function definition before its first use and having `xdl_prepare_ctx()` call it rather than managing cleanup manually. The change eliminates duplicate cleanup code and local variables, making the error handling path simpler and more consistent.

The patch is part of Ezekiel Newren's ongoing series to prepare xdiff for Rust integration by standardizing and simplifying the C codebase. This particular change doesn't introduce new behavior but improves maintainability of the memory management code. The diff shows a net reduction of 36 lines through consolidation of the cleanup logic.

This appears to be a straightforward cleanup with no visible downsides, following the series' incremental approach. The change touches only xdiff internals and maintains all existing behavior while making the code more robust against memory leaks. It builds on prior infrastructure work in the series while keeping platform compatibility unchanged.