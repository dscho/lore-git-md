This is the final patch in a 4-part series introducing the `git refs exists` subcommand as part of a GSoC project. The series aims to consolidate ref-related commands under the `git refs` namespace, with this new command duplicating the functionality of `git show-ref --exists`.

The current patch adds a test script `t1462-refs-exists.sh` that serves as a driver for the shared test library created in the previous patch. By setting `git_show_ref_exists` to use the new command and sourcing the shared test library, it ensures the new implementation is tested against the same comprehensive test cases as `git show-ref --exists`.

The approach demonstrates good test hygiene by reusing existing test infrastructure while maintaining full coverage. The patch is small and focused, just adding the test driver and updating the build configuration. It completes the test refactoring work started earlier in the series, with all requested changes from previous reviews now implemented.

This appears to be the final piece of the series, which has been through several iterations with mentor review. The implementation maintains behavioral parity with the existing command while fitting into the new refs namespace organization.