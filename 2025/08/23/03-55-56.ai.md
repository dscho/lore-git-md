Final patch in the Rust xdiff infrastructure series, implementing `xdl_trim_ends()` in Rust. This completes the type-safe Rust implementation of xdiff's core functionality that was started in earlier patches.

The patch replaces the C implementation of `xdl_trim_ends()` with a Rust version that maintains identical behavior. The function trims matching records from the start and end of diff inputs to reduce the working set for the diff algorithm. Key technical aspects:

- Introduces new Rust modules `xprepare.rs` (for diff preparation logic) and `xtypes.rs` (for FFI-compatible type definitions)
- Defines Rust equivalents of core xdiff structs (`xrecord`, `xdfile`) with proper type alignment (u64 for hashes, usize/isize for indices)
- Uses the `IVec` type (from earlier patches) for safe vector handling across FFI boundary
- Maintains pointer-based access for C-compatible fields (rchg, rindex, ha)
- Preserves all existing behavior while adding Rust's safety checks (null pointer validation, bounds checking)

The implementation is a straightforward port of the C logic to Rust, with the main differences being:
- Explicit bounds checking via Vec/IVec methods
- Type-safe index calculations
- Structured as proper Rust modules rather than C-style loose functions
- Error handling via Option/expect rather than manual null checks

This concludes the mechanical conversion work in the series, having systematically replaced xdiff's core components with type-safe Rust equivalents while maintaining all existing behavior and platform support. The changes are focused on infrastructure rather than algorithm improvements, setting the stage for future Rust-based enhancements.