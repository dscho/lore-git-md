The thread discusses modernizing Git's progress meter signaling, originally using the obsolescent `setitimer()` system call. Carlo Marcelo Arenas Belón's initial two-patch series proposed replacing it with the simpler `alarm()` function, maintaining signal-based updates while removing platform-specific code and adding a robust three-state shutdown protocol. The changes were well-contained in `progress.c` and addressed compatibility concerns.

Johannes Sixt has now proposed a more radical approach: eliminating signal-based updates entirely in favor of direct time checking using `getnanotime()`. This would remove all signal handling infrastructure from the progress meter, instead checking for updates every 1/8th second. While this approach would simplify the code by removing signal complexity, it raises questions about performance impact (particularly on systems with coarse timer resolution) and whether the more frequent time checks might affect operations where progress reporting is most needed.

The thread now presents two distinct directions: the original incremental improvement (signal-based with `alarm()`) versus a more architectural change (timer-based polling). Key considerations for reviewers include performance characteristics, platform compatibility, and whether the additional simplification justifies the more invasive changes. The discussion may also explore whether hybrid approaches could combine benefits of both methods.