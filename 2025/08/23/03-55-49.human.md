This is a refactoring patch in the ongoing Rust infrastructure for xdiff series (now v3/15). It removes three unused fields from xdiff's internal data structures: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. 

The patch demonstrates the series' careful approach - before adding Rust components, it's cleaning up unnecessary C code. The commit message notes these fields were initialized but never actually used, and the diff shows their removal from both the type definitions and all related initialization/cleanup code. The changes are straightforward deletions with no behavior changes.

This continues the series' pattern of methodical groundwork - here simplifying the codebase by removing dead weight before the Rust porting work begins in earnest. The changes are low-risk (removing unused code) but help reduce the surface area that will need to be maintained across both C and Rust implementations.