Feature patch (11/11 in series) implementing the `split` subcommand for the proposed `git-history` tool, focusing on interactive commit splitting functionality.

Jean-Noël Avila provides detailed documentation feedback on the newly introduced `split` subcommand in Patrick Steinhardt's RFC series. The feedback focuses on improving the command's man page formatting and consistency rather than the implementation itself. Key points include:

1. Suggestions to simplify the synopsis section since `drop` and `reorder` currently have no options, and to explicitly mention `--message` for `split` command.

2. Documentation formatting improvements:
   - Adding missing backticks around command elements
   - Ensuring consistent ordering between synopsis and detailed description
   - Using placeholder syntax (`<commit>`) consistently
   - Clarifying when the editor launches for commit messages
   - Properly linking to the pathspec documentation

3. Example section feedback:
   - The provided example demonstrates the basic workflow but could benefit from clearer formatting
   - Suggests using standard Git documentation conventions for example blocks and cross-references

The feedback maintains the technical accuracy of the documentation while pushing for better alignment with Git's established documentation standards. These are relatively minor polish issues that don't affect the core functionality but would improve the user-facing documentation quality. The exchange shows the series is now receiving thorough documentation review alongside the earlier technical discussions.