Refactoring patch in the Rust xdiff infrastructure series (v3 12/15). This follows the previous commit's memory management changes by removing the now-redundant `nrec` field from `xdfile_t`, using the ivec's `record.length` instead. The change touches several xdiff files to update all references to `nrec`, with careful attention to type safety (adding explicit casts where needed).

This is a straightforward cleanup that builds on the foundation laid in patch 11/15, which replaced the chastore memory pool with an ivec. The patch appears mechanically sound, updating all relevant call sites consistently. The diff shows no behavioral changes - it's purely removing duplicate state now that the record count is tracked by the ivec.

As part of the larger Rust infrastructure effort, this continues the incremental approach of simplifying and standardizing xdiff's internals before introducing Rust components. The changes are focused and well-contained, with no impact on platform support or existing functionality.