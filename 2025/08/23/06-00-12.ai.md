Test patch completing the `git refs exists` test infrastructure refactoring. This is the final patch in Meet Soni's v2 series introducing the new subcommand as part of GSoC.

The patch adds `t/t1462-refs-exists.sh` as a minimal driver script that tests the new `git refs exists` command by reusing the shared test library created in the previous patch. The script simply sets `git_show_ref_exists` to the new command and sources the shared test file, ensuring the subcommand gets tested against the full suite of test cases originally written for `git show-ref --exists`.

This completes the test infrastructure reorganization suggested by Patrick Steinhardt in earlier reviews, where the core test logic was extracted into a shared library while maintaining all test coverage. The implementation demonstrates behavioral parity between the old and new commands by running identical test cases against both. The patch follows Git's standard test patterns, including proper setup of `GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME` and use of the test-lib.sh framework.

Key technical details:
- New test file follows naming convention (t1462) for refs-related tests
- Minimal 10-line script delegates to shared test logic
- show-ref-exists-tests.sh
- Maintains all original test coverage from t1422-show-ref-exists.sh
- Added to meson.build integration tests list
- Confirms exit code behavior (0=exists, 2=missing, 1=error) matches show-ref
- Tests special ref cases (HEAD, symrefs, FETCH_HEAD) through shared library

The patch appears ready for review, completing the test-related work for this feature. With this in place, reviewers can verify the command works identically to show-ref --exists while living under the new refs namespace.