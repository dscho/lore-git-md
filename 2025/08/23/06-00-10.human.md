This is a test refactoring patch, part of a series introducing a new `git refs exists` command. It splits the `--exists` tests from `t1403-show-ref.sh` into a new standalone test file `t1422-show-ref-exists.sh` as preparation for sharing test infrastructure between the existing `show-ref` and new `refs exists` commands.

The patch is a straightforward code movement with no behavior changes - it extracts all `--exists` related tests from the original file into the new one, including tests for various edge cases (HEAD, symrefs, bad refnames, special refs like FETCH_HEAD). The test setup and assertions remain identical, just relocated.

This appears to be a cleanly executed refactoring that follows Patrick Steinhardt's suggestion from the v1 review. The test coverage remains comprehensive, checking all the same scenarios as before. The change helps pave the way for the new `refs exists` command by making the test infrastructure more modular.