**Refactoring patch in the Rust xdiff infrastructure series**

This patch (10/15 in v3 series) simplifies memory management in xdiff's preparation code by consolidating cleanup logic. It moves the `xdl_free_ctx()` function definition before its first use in `xdl_prepare_ctx()`, then replaces manual cleanup of individual fields with calls to this unified function. The changes are mechanical and maintain identical behavior:

1. **Memory management unification**:
   - Moves `xdl_free_ctx()` definition up in the file
   - Replaces 5 separate `xdl_free()` calls in error paths with single `xdl_free_ctx()`
   - Eliminates local variables used only for cleanup (`ha`, `rindex`, etc.)

2. **Behavior preservation**:
   - Still handles all error cases identically
   - Maintains same allocation patterns and pointer arithmetic (note `xdf->rchg += 1`)
   - No changes to actual diff algorithm logic

3. **Series progression**:
   - Follows prior type standardization work (patch 9/15)
   - Continues preparing codebase for Rust FFI by simplifying C-side resource management
   - Touches only xprepare.c, leaving other xdiff files unchanged

The change appears well-contained and low-risk, with the commit message accurately describing the simplification. This fits the series' incremental approach to refactoring before Rust integration. No new platform considerations or architectural impacts are introduced - it's purely a code organization improvement within the existing C implementation.