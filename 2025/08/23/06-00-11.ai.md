This is a test refactoring patch (3/4) in the `git refs exists` series by Meet Soni. The patch reorganizes the test infrastructure for the new command by splitting the existing `git show-ref --exists` tests into a shared library that can be reused for testing both implementations.

The key technical changes:
- Moves all test logic from `t1422-show-ref-exists.sh` to new `show-ref-exists-tests.sh`
- Replaces hardcoded `git show-ref --exists` calls with a `$git_show_ref_exists` variable
- Converts the original test file into a minimal driver script that sets the variable and sources the shared tests
- Preserves all existing test cases covering regular refs, HEAD, symrefs, special refs (FETCH_HEAD), and invalid refs
- Maintains the same test-tool ref-store usage for low-level ref manipulation

The refactoring follows established patterns for sharing test logic between implementations and prepares for the upcoming `git refs exists` tests. This addresses Patrick Steinhardt's feedback from v1 review about test organization. The patch appears to be a straightforward mechanical transformation that doesn't modify test behavior, just structure.