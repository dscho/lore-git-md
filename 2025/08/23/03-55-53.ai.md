Refactoring patch in the Rust xdiff infrastructure series (v3 12/15), removing redundant field after memory management changes. This follows the previous patch's replacement of chastore with ivec for record storage.

The patch eliminates the `nrec` field from `xdfile_t` since the record count is now tracked by the `ivec`'s `length` field. It updates all references to use `record.length` instead, with appropriate type casts where needed (mostly `(long)` casts since much of xdiff's code assumes long-based array indexing). The changes span xdiff's core files - xdiffi.c, xemit.c, xmerge.c, xpatience.c, and xprepare.c - with the field removal in xtypes.h.

This is a straightforward cleanup that depends on the infrastructure work in prior patches. The changes are mechanical but widespread, touching diff calculation, merge handling, and patience diff algorithms. The patch maintains all existing behavior while simplifying the dual-storage redundancy that existed before the ivec conversion.

Key technical points for future reference:
- Removes `xdfile_t::nrec` field (was tracking same count as `record.length`)
- Updates all array bounds checks and loops to use `record.length`
- Preserves all existing casts and type assumptions in xdiff algorithms
- Maintains identical diff/merge output behavior
- Follows from v3 11/15's ivec conversion
- Part of systematic preparation for Rust integration

The patch appears well-executed with thorough updates across xdiff's codebase. As with previous patches in this series, it demonstrates careful attention to maintaining existing behavior while cleaning up the code structure.