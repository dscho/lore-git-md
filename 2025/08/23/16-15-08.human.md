This is a review comment on patch 5/11 of Patrick Steinhardt's `git-history` RFC series, focusing on documentation improvements for the new `drop` subcommand. Jean-Noël AVILA suggests several documentation formatting changes:

1. Recommends using `<commit>` or `<commit-ish>` instead of `<revision>` in the synopsis for consistency with Git's existing documentation style.

2. Points out missing backticks around the command syntax in the description section (`drop <revision>` should be `drop <revision>`).

3. Suggests restructuring the examples section by making each example a subsection (using `~~~~~` underlines) for better readability.

The suggestions are focused on aligning the new command's documentation with Git's existing style rather than challenging the functionality itself. These are relatively minor but valid documentation polish items that would improve consistency with the rest of Git's man pages. The review doesn't raise any concerns about the implementation or design of the `drop` subcommand itself.