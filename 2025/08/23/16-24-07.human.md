Documentation feedback on the `git-history reorder` subcommand.

Jean-Noël Avila provides specific suggestions for improving the documentation of the new `reorder` subcommand in Patrick Steinhardt's `git-history` RFC series. He focuses on two main areas: parameter naming consistency and synopsis formatting.

For the synopsis, Avila notes that the current `--(before|after)=<revision>` syntax is unconventional in Git's man pages and suggests unrolling it into separate options. He also recommends using more descriptive placeholder names like `<commit>`, `<following-commit>`, and `<preceding-commit>` to make the documentation clearer.

The feedback is narrowly focused on documentation style rather than the implementation itself, aligning with Git's established conventions. Avila's suggestions appear well-considered and would bring the new command's documentation in line with existing patterns. This follows the thread's recent expansion to include documentation quality considerations alongside the technical discussion.

The changes proposed are purely about presentation and clarity - they don't affect the command's functionality or the ongoing discussion about implementation details like merge commit handling or index preservation. This represents a polishing pass that would likely be straightforward for the author to incorporate in a future revision.