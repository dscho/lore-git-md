Here's the AI-mode summary for this email:

Feature patch series (v2) introducing `git refs exists` subcommand by Meet Soni as part of GSoC. This continues the refs command consolidation effort by adding a new subcommand that duplicates `git show-ref --exists` functionality under the refs namespace.

Key changes in v2:
- Rebased onto master branch now that prerequisite commits are merged
- Split test organization per previous feedback: first separates show-ref tests into new file (t1422-show-ref-exists.sh), then makes setup shareable via show-ref-exists-tests.sh
- Maintains behavioral parity with show-ref while using refs exists command

Technical details:
- New subcommand in builtin/refs.c implementing exists with same exit codes (0=exists, 2=missing, 1=error)
- Uses get_main_ref_store() and refs_read_raw_ref() for ref access
- Test coverage includes:
  - Regular refs, HEAD, symrefs, special refs (FETCH_HEAD)
  - Invalid ref formats
  - Shared test infrastructure between show-ref and refs exists commands
- Documentation added to git-refs.adoc

Implementation notes:
- Deliberately duplicates ~20 lines of show-ref logic rather than abstracting
- Follows established pattern for test infrastructure sharing
- Builds on Git v2.51.0 with `ms/refs-list` merged

The series appears to address all major feedback from v1 review, particularly around test organization. The implementation remains straightforward and maintains compatibility with existing show-ref behavior while fitting into the broader refs command consolidation effort.