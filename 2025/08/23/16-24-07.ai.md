Documentation feedback on the `git-history reorder` subcommand implementation.

Jean-Noël Avila provides specific documentation suggestions for the `reorder` subcommand in Patrick Steinhardt's `git-history` RFC series. The feedback focuses on improving the man page formatting and consistency with Git's documentation standards.

Key points raised:
1. Recommends standardizing parameter naming between `<revision>` and `<commit>`/`<commit-ish>` for consistency with other Git documentation
2. Notes that the `--(before|after)` syntax in the SYNOPSIS is unconventional and suggests unrolling it into separate options as done in the description
3. Proposes clearer parameter naming in the description to distinguish between the commit being moved and its new neighbor
4. Suggests rewording the description to use more precise terms like "preceding-commit" and "following-commit"

The feedback is narrowly focused on documentation presentation rather than the implementation itself, continuing Jean-Noël's pattern of providing detailed documentation review from the previous email in the thread. The suggestions aim to align the new command's documentation with established Git conventions while making the parameter relationships clearer to readers.

This represents incremental refinement of the series' documentation quality without requiring any changes to the underlying implementation. The feedback is constructive and specific, addressing the same kind of documentation polish issues noted in Jean-Noël's previous review of the `drop` subcommand.