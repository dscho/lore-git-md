Test refactoring patch in the `git refs exists` series. This is patch 3/4 in the v3 submission, which reorganizes the test infrastructure to share common test cases between `git show-ref --exists` and the new `git refs exists` command.

The patch moves all test logic from `t1422-show-ref-exists.sh` into a new shared library `show-ref-exists-tests.sh`, replacing hardcoded `git show-ref --exists` calls with a configurable `$git_show_ref_exists` variable. The original test file becomes a minimal driver that sets this variable and sources the shared library. This follows an established pattern for test sharing in Git's test suite.

Key technical details:
- Creates new `show-ref-exists-tests.sh` with all test cases (77 lines)
- Reduces `t1422-show-ref-exists.sh` to just setup and sourcing (4 lines)
- Tests cover various ref scenarios: existing/missing refs, HEAD, symrefs, special refs, bad refs
- Uses `test-tool ref-store` for low-level ref manipulation
- Maintains same test coverage while enabling reuse for new command
- Follows pattern suggested by Patrick Steinhardt in earlier reviews

This is a straightforward refactoring that enables the test cases to be reused for the new `git refs exists` command in the next patch. The approach is consistent with Git's test patterns and all technical feedback from previous versions appears addressed. The patch is part of Meet Soni's GSoC project to consolidate ref-related commands under the `git refs` namespace.