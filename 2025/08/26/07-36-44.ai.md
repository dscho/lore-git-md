This is a test refactoring patch in the `git refs optimize` subcommand series. It prepares existing pack-refs tests to be shared with the new `refs optimize` command by extracting the core test logic into a separate file.

The patch moves all test cases from `t0601-reffiles-pack-refs.sh` into a new `t/pack-refs-tests.sh` file, replacing hardcoded `pack-refs` command references with a `$pack_refs` variable. The original test file becomes a minimal driver that sets this variable and sources the shared test library. This follows the pattern used for other ref-related command tests and will allow the same test cases to verify both `pack-refs` and the new `refs optimize` command.

Key technical details:
- New shared test file contains all 400+ lines of test logic from original
- Tests cover packing behavior, pruning, ref exclusions/inclusions, worktree refs, and edge cases
- Original test file reduced to 4 lines (shebang, GIT_TEST_DEFAULT_REF_FORMAT export, test-lib.sh include, and new test sourcing)
- Uses same approach as `for-each-ref`/`refs list` test sharing
- Prepares for subsequent patch adding `refs optimize` tests

The change is purely structural - no test behavior is modified. The thorough test coverage from the original remains intact, now in a more reusable form. This is patch 4/5 in the series introducing the `git refs optimize` subcommand as part of refs command consolidation.