This is a technical discussion about Rust/C interop design in the ongoing xdiff Rust infrastructure effort (v3). Brian Carlson responds to Ezekiel Newren's request for feedback on the `ivec` type implementation, which provides vector operations that work across both languages.

Carlson shares practical experience from their work porting C to Rust, noting they've used both custom vector types (like `ivec`) and boxed Rust types depending on context. They confirm `ivec` is the right choice for cases where C code needs direct access to the internals, as is common in Git's codebase. For Rust-only components (like a hypothetical Rust port of loose object mapping), they'd prefer using boxed native Rust types (`Box<HashMap<_,_>>`) instead.

The email reinforces Newren's design decisions while providing real-world context about when each approach (custom interop type vs boxed Rust) is appropriate. This comes after extensive technical discussion in the thread about why Rust's native `Vec` can't be used for FFI due to version instability and layout concerns. Carlson's endorsement of `ivec` for C-visible cases and boxed types for Rust-only components aligns with the thread's consensus on maintaining both strategies.