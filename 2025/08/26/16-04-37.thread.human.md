This thread has converged on a solution for parallel builds when Rust components (`libgit-sys` and `libgit-rs`) are enabled via `INCLUDE_LIBGIT_RS`. The original issue involved cargo lock warnings and linker errors during concurrent builds. Kyle Lippincott's patch implements the agreed-upon approach: it establishes proper Makefile dependencies between the Rust components to enforce serial execution while maintaining platform compatibility, particularly addressing Randall Becker's concerns about NonStop support.

The solution has evolved to cover both build and test phases, replacing directory changes with cargo's `--manifest-path` option for cleaner recipes. The implementation now clearly expresses the component relationships - `libgit-rs` depends on `libgit-sys` - and extends this pattern to test execution. Junio Hamano's question about test serialization has been addressed by applying the same dependency logic to the test targets in `t/Makefile`.

With all raised concerns now resolved and the solution demonstrating technical consensus, this appears ready for merging. The changes make the build process more robust while preserving the optional nature of Rust components, providing a template for similar build system challenges in mixed Make/Cargo environments.