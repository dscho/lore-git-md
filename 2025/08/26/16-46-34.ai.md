Build system fix for parallel Rust builds (follow-up)

This is a response to the initial patch that proposed serializing `libgit-sys` and `libgit-rs` builds using Make's `.WAIT` directive. Junio Hamano questions whether a similar change is needed for the test suite (`t/Makefile`), specifically whether `cargo test` exhibits the same parallel build issues as `cargo build`.

The original issue involves race conditions when building Rust components (`libgit-sys` and `libgit-rs`) in parallel with `make -JN` when `INCLUDE_LIBGIT_RS` is enabled. The proposed solution adds `.WAIT` between these targets in the main Makefile, but the follow-up raises the question of whether test builds need equivalent protection.

Key technical points:
- The discussion now expands to consider test builds (`cargo test`) in addition to the original build-time issue
- The core problem remains Make/Cargo interaction during parallel execution
- No new code changes are proposed in this response - it's a question about test suite implications

This moves the thread forward by identifying a related area (test builds) that may need the same parallel build protection, suggesting the original fix might need to be applied more broadly. The response doesn't provide data about test behavior, leaving that as an open question for the original author or other contributors to address.