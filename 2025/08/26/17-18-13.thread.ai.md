Thread identity: Performance and safety patch series for `git range-diff` addressing memory exhaustion issues when comparing very large commit ranges (250,000+ commits), with a configurable memory limit for the cost matrix calculation to prevent integer overflow and excessive memory usage.

Core problem: The n×n cost matrix allocation in `range-diff` can attempt to use hundreds of GB of memory for extremely large commit ranges (e.g. 260GB for 256,783 commits), causing memory exhaustion. The solution introduces memory limits with clear error messaging.

Current status: Version 2 complete with both cleanup and feature patches. Core memory limit functionality remains solid with Acked-by from Johannes Schindelin and minor wording improvements suggested by Elijah Newren. First commit (mechanical reordering) now faces substantive objections from both Junio Hamano and Elijah Newren regarding its impact on logical option groupings.

Key technical details:
- New `max_memory` field in `struct range_diff_options`
- Memory size parsing via `parse_max_memory()` callback accepting human-readable sizes (e.g. "1G", "500M")
- Human-readable error messages using `strbuf_humanise_bytes()`, now to be reworded from "X bytes available" to "X bytes limited to" per Newren's suggestion
- Default limits: 4GB (64-bit) and 2GB (32-bit) via `RANGE_DIFF_MAX_MEMORY_DEFAULT` macro
- Checks in `get_correspondences()` before matrix allocation
- Affects three call sites: builtin/log.c, builtin/range-diff.c, log-tree.c
- First commit's lexicographic option ordering now contested for breaking logical groupings of related options like `--left-only`/`--right-only` and `--diff-merges`/`--remerge-diff`

Changes from v1 to v2:
- Split into cleanup + feature commits per Junio's initial feedback
- First commit purely mechanical option reordering
- Fixed Acked-by trailer formatting
- Corrected author name from "pcasaretto" to "Paulo Casaretto"
- Removed unrelated option reordering from feature commit
- Cleaned up whitespace/indentation issues

Open questions: Whether to retain, modify, or drop the option reordering commit. Junio and Newren now agree that strict alphabetical ordering harms usability for related options, suggesting either:
1. Dropping the reordering entirely
2. Adopting a hybrid approach with section headers for logical groupings (Junio's suggestion) and alphabetization within groups (Newren's suggestion)
The memory limit error message wording still needs updating per Newren's suggestion.

Participant positions:
- Johannes Schindelin: Maintained Acked-by on memory limit implementation
- Junio Hamano: Objects to strict alphabetical ordering, suggests help text section headers for logical groupings
- Elijah Newren: Agrees with Junio on preserving logical groupings, suggests hybrid organization approach while maintaining Acked-by on memory limit implementation
- Paulo Casaretto: Author, needs to respond to maintainer feedback on option organization and implement Newren's wording suggestion

Related work: Continues solution for real-world issue observed in Shopify's monorepo. Core technical approach to memory limits remains unchanged while ancillary cleanup work requires reconsideration of user interface principles, with emerging consensus against pure alphabetical ordering in favor of semantic grouping.