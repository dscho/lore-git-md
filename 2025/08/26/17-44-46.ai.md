This is a follow-up discussion about the build system fix for parallel Rust builds. Kyle Lippincott questions whether the proposed `.WAIT` solution is the most elegant approach, suggesting instead that the dependency could be expressed more naturally by making `libgit-rs` depend directly on `libgit-sys` in the Makefile rules, rather than using the `.WAIT` directive in the `all` target. This would mean the serialization would be handled by Make's normal dependency mechanism rather than an explicit synchronization point.

The email doesn't propose specific code changes but raises an architectural question about whether the solution should be implemented at the target dependency level rather than the build sequence level. This suggests the discussion may shift from "how to serialize these builds" to "where the serialization should be expressed in the Makefile structure."

The thread context shows this is part of a broader consideration of parallel build issues affecting Rust components, now potentially including both the main build and test execution. This particular suggestion hasn't been tested or implemented yet, but represents an alternative approach to solving the same underlying problem of concurrent Cargo invocations.