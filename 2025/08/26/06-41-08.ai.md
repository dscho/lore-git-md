Test refactoring patch in the `git refs exists` series. This is patch 2/4 in the third version of Meet Soni's GSoC project to consolidate ref-related commands under the `git refs` namespace.

The patch moves all `--exists` tests from `t1403-show-ref.sh` into a new standalone test file `t1422-show-ref-exists.sh`. This is a pure code reorganization with no behavior changes, preparing the way for sharing these tests between `git show-ref --exists` and the new `git refs exists` command. The test coverage remains identical, just relocated to a more logical home.

Key technical details:
- 65 lines removed from t1403-show-ref.sh
- 83 lines added to new t1422-show-ref-exists.sh
- Tests cover regular refs, HEAD, symrefs, special refs, and edge cases
- Uses test-tool ref-store for low-level ref manipulation
- Maintains all existing test conditions and assertions
- Part of broader refs command consolidation effort
- Follows pattern established in prerequisite patches
- Builds on Git v2.51.0 with `ms/refs-list` merged

The patch appears complete and well-executed, addressing all prior feedback from Patrick Steinhardt about test organization. This is a clean separation that will allow the test logic to be shared between the old and new implementations of ref existence checking.