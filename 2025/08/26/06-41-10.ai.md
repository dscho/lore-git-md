Test patch completing the `git refs exists` feature series. This is the final patch in a 4-part series that adds a new subcommand to check ref existence under the `git refs` namespace, duplicating `git show-ref --exists` functionality.

The patch adds a minimal test driver script (`t1462-refs-exists.sh`) that reuses the comprehensive test suite from `git show-ref --exists` by sourcing a shared test library. This ensures behavioral parity between the old and new commands. The test script is added to the meson build system and follows Git's standard test patterns.

Key technical details for future reference:
- Test script location: `t/t1462-refs-exists.sh`
- Uses shared test library: `show-ref-exists-tests.sh` (from previous patch)
- Overrides `git_show_ref_exists` variable to test new command
- Added to meson.build test list
- Follows same test infrastructure approach as show-ref tests (t1422)

This completes the implementation work for the feature, with all prior review feedback addressed. The test approach was suggested by Patrick Steinhardt to maximize coverage while minimizing duplication. The patch appears ready for final review and merging.