Feature patch adding `-u` shorthand for `git pull --set-upstream`. This is a follow-up email in the thread proposing to align `git pull`'s option naming with the existing `git push -u` behavior.

D. Ben Knoble points out that the documentation wasn't updated in the original patch. This is a brief but important reminder - the man pages would need to reflect the new `-u` option if the change is accepted. The original patch from Julien Jerphanion already includes the code change and test coverage, showing the short option works identically to the long form.

The change itself is small and focused - just adding the short flag to match `git push`'s behavior. The test demonstrates the functionality works correctly for setting upstream branches. This appears to be a straightforward quality-of-life improvement that maintains consistency across Git commands. The thread is still in review, with this being the first substantive comment pointing out a documentation gap that would need addressing before merging.