This is a build system discussion about parallel builds with Rust components. The thread explores two approaches to fix parallel build issues (`make -JN`) when `INCLUDE_LIBGIT_RS` is enabled, where concurrent Cargo builds of `libgit-sys` and `libgit-rs` cause lock warnings and linker errors.

The original patch proposed using Make's `.WAIT` directive to serialize the two builds. Kyle Lippincott suggested an alternative approach of making `libgit-rs` depend on `libgit-sys` in the Makefile rules. In this response, Randall Becker notes that not all platforms can build `libgit-rs`, so making it a required dependency might not be ideal.

The discussion remains technical and focused on build system mechanics, with no clear consensus yet on which approach is better. The thread also has an open question from Junio Hamano about whether similar parallel build protections are needed for the test suite, though that hasn't been addressed yet. The exchange shows careful consideration of platform compatibility implications while solving the parallel build issue.