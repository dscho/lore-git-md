Refactoring patch as part of the `git refs optimize` subcommand series to consolidate ref-related commands. This first patch extracts the core logic from `git pack-refs` into a reusable helper function to share with the new subcommand.

The patch moves the packing implementation from `cmd_pack_refs()` into a new `pack_refs_core()` function in a new pack-refs.h header. The original command becomes a thin wrapper that provides its specific usage string. The header defines a common options macro `PACK_REFS_OPTS` that will be shared with the upcoming `git refs optimize` command.

Key technical details:
- Creates new pack-refs.h header with public interface
- Extracts option parsing and packing logic into `pack_refs_core()`
- Maintains identical behavior through the refactoring
- Uses a usage string parameter to make the helper generic
- Keeps existing `cmd_pack_refs()` as backward-compatible interface
- Files modified: builtin/pack-refs.c (logic extraction), new pack-refs.h
- Shared options macro: PACK_REFS_OPTS
- Core function signature: pack_refs_core(int, const char**, const char*, struct repository*, const char* const*)

The patch appears well-structured with clear separation of concerns, following Git's established patterns for command refactoring. The header documentation explains the shared components' purpose, and the implementation maintains all existing functionality while enabling future reuse.