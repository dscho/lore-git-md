Build system fix for parallel Rust builds. Junio C Hamano follows up on David Aguilar's patch that addressed parallel build issues with Git's Rust components by adding a `.WAIT` directive between `libgit-sys` and `libgit-rs` targets.

The original patch fixed cargo lock warnings and linker errors that occurred when these components were built in parallel (`make -JN` with `INCLUDE_LIBGIT_RS` enabled). Junio questions whether a similar change might be needed for `t/Makefile` to handle parallel test execution, or if the issue is specific to the build phase.

This appears to be a focused technical discussion about build system synchronization. The original solution was minimal and targeted, and Junio is probing whether the same race condition might exist in test execution. The thread shows maintainer engagement with a new contributor's build system patch, suggesting the fix is likely to be merged with any additional refinements that emerge from this discussion.