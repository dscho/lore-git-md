This is the final test patch in a 4-part series introducing the new `git refs exists` subcommand as part of a GSoC project. The patch adds a minimal test driver script (`t1462-refs-exists.sh`) that reuses the comprehensive test suite originally written for `git show-ref --exists` by simply pointing to the new command via a variable override.

The approach demonstrates good test hygiene - rather than duplicating test cases, it leverages existing test infrastructure through a shared library pattern. The test verifies that `git refs exists` behaves identically to `show-ref --exists` across all edge cases (regular refs, HEAD, symrefs, etc.), which is important since this is meant to be a drop-in replacement.

This appears to be the final piece of a well-executed series that has already addressed all substantive feedback from reviewers. The implementation follows established Git test patterns and completes the project goal of moving ref existence checking under the new `git refs` namespace. With this last test addition, the series seems ready for final review and potential merging.