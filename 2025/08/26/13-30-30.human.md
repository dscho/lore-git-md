This is a technical discussion in the ongoing Rust xdiff infrastructure thread, focusing on the design of the `ivec` interoperability type between C and Rust.

Ben Knoble follows up on earlier questions about why Rust's native `Vec` type couldn't be used for FFI, acknowledging Ezekiel Newren's explanation about `Vec`'s unstable memory layout across Rust versions and lack of `#[repr(C)]` guarantee. The exchange clarifies that the current dual-implementation approach (in both C and Rust) was a deliberate choice to avoid requiring Rust dependency while still enabling Rust interoperability.

Knoble notes that the Rust implementation of `ivec` actually does more work than he initially realized, with Rust-side methods for insert/push operations. He agrees with the pragmatic approach of keeping the type usable from pure C until/unless Rust becomes a hard requirement for Git.

This email represents a resolution of some technical questions in the thread, though it doesn't change the fundamental policy impasse around Rust adoption that the thread AI summary describes. The discussion remains collegial and focused on understanding design tradeoffs rather than reopening debates.