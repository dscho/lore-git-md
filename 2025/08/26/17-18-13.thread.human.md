Here's the updated human-readable thread summary:

This thread addresses a performance and safety issue in `git range-diff` where comparing very large commit ranges (250,000+ commits) could attempt to allocate hundreds of GB of memory for the n×n cost matrix calculation, causing memory exhaustion. The solution introduces configurable memory limits with clear error messaging.

The v2 series has been split into two parts following Junio's feedback. The first patch (already merged) was a purely mechanical cleanup reorganizing options lexicographically in builtin/range-diff.c. This second and final patch implements the core functionality - adding a `--max-memory` option that accepts human-readable sizes (e.g. "1G", "500M") with sensible defaults (4GB for 64-bit systems, 2GB for 32-bit). The implementation includes proper error checking before matrix creation and uses `strbuf_humanise_bytes()` for user-friendly error messages.

Johannes Schindelin has maintained his Acked-by from the initial version, confirming the technical approach remains sound. The patch addresses a real-world issue observed in Shopify's monorepo while avoiding more complex alternatives like pre-counting commits or streaming. With this final patch, the series appears ready for merging, having addressed all feedback from the initial version.