This is a discussion about the planned removal of `git whatchanged` in Git 3.0. Kristoffer Haugsbakk responds to Chris Palmer's concerns about two remaining use cases for the command, providing a direct replacement using `git log` with `--no-merges --raw` flags. The response notes this is functionally equivalent with minor output differences around empty commits.

The email confirms the command is indeed slated for removal in Git 3.0 (though that version's release date remains undetermined) and points to existing documentation about this breaking change from Git 2.51.0. The key technical detail is the specific `git log` invocation that can serve as a drop-in replacement, addressing both of Palmer's cited use cases (examining recent commits and file-specific changes).

This moves the thread forward by providing concrete migration guidance while acknowledging the version timeline remains open. The discussion appears to be converging toward documenting the replacement pattern rather than reconsidering the removal decision itself.