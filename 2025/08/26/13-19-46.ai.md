Feature patch follow-up: Documentation reminder for `git pull -u` shorthand.

D. Ben Knoble replies to Julien Jerphanion's initial patch adding `-u` as a shorthand for `--set-upstream` in `git pull`. The original patch (currently awaiting review) would align `git pull`'s option behavior with the existing `git push -u` convention. Knoble's brief response serves as a reminder that documentation updates would be needed if the change is accepted, though no specific doc changes are proposed in this email.

The technical context remains unchanged from the initial patch: a one-line code change in `builtin/pull.c` to add the short form, plus a test case in `t/t5553-set-upstream.sh` verifying identical behavior between the short and long forms. The reminder about documentation suggests that `Documentation/git-pull.txt` would need updating, though this hasn't been addressed in the patch series yet.

This appears to be a straightforward quality-of-life improvement that maintains consistency with Git's existing conventions. The exchange so far suggests the change is uncontroversial, though proper documentation would be expected before final acceptance. The thread remains in early stages with no substantive technical discussion yet.