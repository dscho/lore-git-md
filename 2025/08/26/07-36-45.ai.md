This is the fifth and final patch in a series introducing the new `git refs optimize` subcommand as a replacement for `git pack-refs`. The patch adds comprehensive test coverage by creating `t/t1463-refs-optimize.sh`, which leverages the shared test library refactored in the previous patch.

The test script is intentionally minimal - just 17 lines - as it inherits all test cases from `t/pack-refs-tests.sh` by setting `pack_refs='refs optimize'` before sourcing the shared library. This ensures the new subcommand is tested against the full suite of existing pack-refs tests, verifying it behaves identically as a drop-in replacement. The patch follows the same pattern used for other ref-related command consolidations, maintaining consistency with the project's testing approach.

Key technical details:
- New test file `t1463-refs-optimize.sh` added to meson.build
- Uses same test infrastructure pattern as previous refs command consolidations
- Verifies identical behavior between `refs optimize` and `pack-refs`
- Maintains all existing test coverage while avoiding duplication
- Follows project conventions for test organization and structure

This completes the series by providing the testing component for the new subcommand, ensuring the functionality is properly validated before potential merging. The approach demonstrates good test hygiene by reusing existing test logic rather than duplicating it.