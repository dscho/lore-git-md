Performance optimization discussion continuing the comparison of khash and commit-slab approaches, now focusing on object flag storage implications.

Junio Hamano responds to Jeff King's corrected benchmarks showing khash's performance characteristics in topological sorting (2-6% slower than commit-slabs) and considers broader implications for object flag storage strategies. The email crystallizes the emerging consensus around context-dependent optimization principles:

1. **Benchmark Context**: Confirms Jeff's revised measurements showing khash's modest but consistent performance penalty (2% on git.git, 6% on linux.git) in topological sorting scenarios, validating commit-slabs' advantage for dense access patterns.

2. **Storage Philosophy**: Articulates a clear decision framework - use commit-slabs when per-object metadata is needed for all processed objects (like in `show-branch`), and khash for selective object tracking (as in `git describe`'s tag candidate handling).

3. **Flag Storage Experiment**: Anticipates Jeff's radical experiment converting all object flags to khash (later shown to cause 25-93% slowdowns), noting the theoretical worst-case scenario where direct struct access would be clearly superior.

Key technical references:
- `finish_depth_computation()` as the khash conversion point
- `--date-order` vs `--author-date-order` performance characteristics
- `show-branch` as archetypal slab use case
- Implicit `objflags` khash table in later experiments

The discussion reinforces that while khash offers flexibility (supporting removals, sparse storage), its overhead makes it unsuitable for high-frequency operations like flag checking during traversals. The email helps formalize the emerging optimization playbook while leaving open questions about potential hybrid approaches and generalized abstractions.