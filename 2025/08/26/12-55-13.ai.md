This is a minor code review comment in the ongoing `git-history` RFC series. D. Ben Knoble points out what appears to be a repeated hunk in the sequencer.c changes from Patrick Steinhardt's v2 patch 03/16. The code in question handles validation of todo commands for different sequencer operations (pick, revert, and the new history-edit mode).

The observation suggests this might be leftover from a refactoring, though the comment doesn't propose any specific changes. The thread context shows this is part of the infrastructure work for the new `git-history` command, which is currently at v3 with 26 patches. The series has seen significant discussion around both the feature design and preparatory refactoring work, including recent approval of related `add-patch` subsystem changes.

This appears to be a minor code organization note rather than a substantive issue with the history editing functionality itself. The thread shows active engagement with the implementation details as the series evolves through multiple revisions.