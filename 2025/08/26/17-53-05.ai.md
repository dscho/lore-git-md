This is a build system discussion about parallel build fixes for Rust components in Git's contrib/ area. The email responds to Kyle Lippincott's suggestion about expressing dependencies between `libgit-sys` and `libgit-rs` through Makefile rules rather than using the `.WAIT` directive proposed in the original patch. The author (rsbecker@nexbridge.com) pushes back on making `libgit-rs` a required component in the build, noting that not all platforms can build it. This maintains the thread's focus on solving parallel build issues while keeping the Rust components optional.

 Key technical points in this response: the platform compatibility constraint for `libgit-rs` builds is highlighted as a reason not to make it a required dependency. The discussion continues weighing `.WAIT` versus target dependencies as synchronization approaches, with this email favoring the original `.WAIT` solution due to platform considerations. The thread's open questions about test suite protection and cargo test parallelization remain unaddressed in this response.

 The exchange shows the tradeoffs in build system design when dealing with optional components that have internal dependencies - the solution needs to handle both the technical synchronization requirements and the practical reality of platform limitations. The response maintains a narrow focus on the build-time issue without expanding scope to the test execution questions raised earlier in the thread.