Build system fix for parallel Rust component builds. This is the final version of a patch that addresses parallel build issues (`make -JN`) when the optional Rust components `libgit-sys` and `libgit-rs` are enabled via `INCLUDE_LIBGIT_RS`. The solution makes `libgit-rs` depend on `libgit-sys` to enforce proper build ordering, while also simplifying the build recipes by using cargo's `--manifest-path` instead of directory changes.

The patch has evolved through discussion to handle both build and test execution scenarios, with the test Makefile receiving similar treatment to ensure serialized test runs. The changes maintain the optional nature of the Rust components while properly expressing their dependencies through Makefile semantics.

David Aguilar's implementation has been reviewed and approved by Kyle Lippincott, indicating technical consensus. The solution appears clean and well-motivated, addressing both the original build issues and follow-up concerns about test parallelism. This concludes the thread with a robust fix for Rust component builds in parallel environments.