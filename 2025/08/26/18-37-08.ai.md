Performance optimization for `git ls-files` sparse index handling. This is a follow-up discussion to Derrick Stolee's patch that conditionally delays sparse index expansion in `ls-files` until encountering a sparse directory that matches the pathspec.

Elijah Newren provides review feedback, noting that the performance benefit comes from `prune_index()` filtering index entries by pathspec prefix before iteration begins, not just for single pathspecs as originally described. He suggests clarifying the commit message to explicitly mention `prune_index()`'s role in reducing the working set before sparse directory checks occur.

The technical discussion confirms the patch's safety - expanding the index mid-iteration is valid because earlier entries remain stable. Newren verified the behavior using the included test case (`ls-files deep/deeper1`) which demonstrates the optimization by showing sparse index isn't expanded unnecessarily when pathspecs match sparse-checkout patterns.

Key implementation details:
- Moves `ensure_full_index()` inside the file iteration loop in `builtin/ls-files.c`
- Only triggers expansion when hitting a sparse directory (`S_ISSPARSEDIR`) without `--sparse` flag
- Maintains existing behavior while avoiding full index expansion in common cases

The exchange highlights this as a well-considered optimization from an experienced contributor, with Newren's review adding clarity about the interaction with `prune_index()`. The patch appears ready for inclusion with minor commit message refinements.