This is a build system discussion about parallel Rust component builds, specifically addressing platform compatibility concerns. Kyle Lippincott clarifies his earlier suggestion about target dependencies in response to Randall Becker's platform compatibility objection.

The email refines the proposed solution to the parallel build problem by suggesting a more precise dependency structure. Lippincott proposes splitting the combined `libgit-sys libgit-rs:` target into separate targets with `libgit-rs` explicitly depending on `libgit-sys`, while maintaining the conditional `INCLUDE_LIBGIT_RS` guard. This would allow the `all` target to depend only on `libgit-rs` (which would transitively pull in `libgit-sys`), rather than using the `.WAIT` directive between both targets.

Key technical points include maintaining the existing platform compatibility by keeping the Rust components optional (via `INCLUDE_LIBGIT_RS`), while potentially improving Makefile semantics through proper target dependencies rather than explicit synchronization. The proposal attempts to address both the parallel build issue and Becker's platform concerns by keeping the conditional compilation behavior unchanged while restructuring the internal dependencies.

This moves the discussion forward by offering a third alternative to the original `.WAIT` approach and Lippincott's earlier dependency suggestion, now more carefully considering the platform constraints. The solution would still need to be tested for actual parallel build behavior and platform compatibility, but represents a more nuanced approach to the problem.