This is the third patch in a 5-part series introducing a new `git refs optimize` subcommand as part of refs command consolidation, specifically replacing `git pack-refs` functionality under the new namespace.

The patch adds the actual `optimize` subcommand implementation to `builtin/refs.c`, which calls the `pack_refs_core()` helper function that was extracted from `git pack-refs` in previous patches. The implementation maintains identical behavior to the original `git pack-refs` command while providing the new interface. Documentation for the subcommand is added to git-refs.adoc, leveraging the shared options file created earlier in the series via an AsciiDoc include directive.

Key technical details:
- New `cmd_refs_optimize()` function in builtin/refs.c that serves as a thin wrapper around `pack_refs_core()`
- Usage string `REFS_OPTIMIZE_USAGE` defined using the shared `PACK_REFS_OPTS` macro
- Subcommand registration via `OPT_SUBCOMMAND` in `cmd_refs()`
- Documentation section added with explanation that this is an alias for `git-pack-refs` with identical functionality
- Options included via `include::pack-refs-options.adoc` to maintain consistency with original command

The patch follows the established pattern from earlier in the series, cleanly implementing the new subcommand while reusing the refactored core functionality. The implementation appears straightforward with no obvious technical issues, and maintains the thorough approach seen in previous patches with proper documentation and integration.