This is a documentation patch in a reviewed and nearly-finalized series improving the `git-add` man page. Junio Hamano provides final feedback on patch 3/4, which aims to make the explanation less dry with concrete examples.

The patch replaces abstract descriptions with specific examples, like showing when to re-add an edited file. Junio prefers the new phrasing for explaining file edits but suggests two improvements: using `--cached` instead of `--staged` in the diff example (to match `git diff -h` output), and expanding the diff discussion to cover checking both staged and unstaged changes.

The feedback is minor - Junio clearly approves the overall approach and the patch appears ready for merging with these small tweaks. This continues the series' pattern of making technical documentation more accessible while preserving accuracy. The discussion remains focused on wording refinements rather than substantive changes, indicating the series is in its final polishing stage.