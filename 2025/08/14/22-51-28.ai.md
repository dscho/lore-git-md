This is a technical discussion email in the SHA-1/SHA-256 interoperability storage format thread, focusing on architectural separation of static and dynamic mapping data.

Junio Hamano responds to brian m. carlson's proposal to handle dynamic metadata (like shallow boundary info) through `git gc` integration. Hamano clarifies his earlier suggestion wasn't about timing of data collection/consumption, but rather about physically separating the storage formats. He advocates for two distinct files - one for static bidirectional mappings (which rarely change) and another for dynamic metadata (like shallow info that changes frequently). This would avoid rewriting the entire mapping file when only dynamic data needs updating.

The exchange highlights a key architectural decision point in the hash interoperability work - whether to co-locate all mapping information or maintain separate storage layers. Hamano's position favors the two-file approach to minimize I/O overhead for frequent updates, while carlson's initial response suggested periodic compaction via `git gc` as an alternative strategy. The discussion remains technical and collaborative, with both participants focused on optimizing the storage format's efficiency.

Key technical details mentioned include:
- Static mappings (bidirectional SHA-1<->SHA-256 conversions)
- Dynamic metadata (shallow boundary markers)
- `git gc` integration proposal
- File format separation concerns
- Write frequency implications

This continues the thread's progression toward concrete implementation decisions while maintaining focus on core requirements like lookup efficiency and storage overhead. The discussion remains unresolved but productive, with clear technical alternatives on the table.