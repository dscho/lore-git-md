This is a documentation patch in the final review stage of a series improving the `git-add` man page. The email contains Junio Hamano's review feedback on patch 3/4, which focuses on clarifying the explanation of how `git add` works with subsequent file edits and adding information about checking staged changes.

Key points from the review:
1. Junio prefers the new phrasing about needing to re-add edited files but suggests keeping some aspects of the original wording that were clearer.
2. He recommends using `--cached` instead of `--staged` in the diff example since it's the primary form shown in `git diff -h`.
3. The review suggests expanding the documentation to also cover using plain `git diff` to check unstaged changes that might have been forgotten, complementing the existing advice about checking staged changes.

The feedback is minor and constructive, indicating the patch is nearly ready for merging. This continues the series' pattern of making the documentation more practical while maintaining technical accuracy, with Junio providing final polish on wording choices. The changes align with the overall goal of making Git's behavior more understandable to users, particularly around the index/staging area workflow.