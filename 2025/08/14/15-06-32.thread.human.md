Here's a human-readable summary of the thread's current state:

The thread began with Michael Schroeder's patch to handle submodule operations when the superproject and submodule use different hash algorithms (SHA-1 vs SHA-256). The initial approach modified submodule helper functions to truncate padded commit hashes when fetching shallow submodule clones. Junio Hamano reviewed this, suggesting improvements to both the implementation and commit message clarity.

The discussion took a significant turn when Brian M. Carlson identified a critical limitation: the proposed solution only works when the superproject uses SHA-256 and the submodule uses SHA-1, while actually creating data corruption risks in the opposite case. Carlson argues that submodules fundamentally should match their superproject's hash algorithm, since tree objects can't be automatically converted between algorithms. This challenges the very premise of trying to handle mixed-algorithm cases through hash length adjustments.

The thread now faces deeper architectural questions about how Git should handle hash algorithm transitions in submodule relationships. What began as a technical bugfix discussion has revealed a more fundamental design issue regarding whether and how submodules should support different hash algorithms from their containing repositories. The core question remains open: should Git support mixed-algorithm submodules (requiring proper conversion machinery) or enforce algorithm matching between superprojects and their submodules?