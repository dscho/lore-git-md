This is part of the ongoing `git repo info` command implementation series, specifically addressing test coverage for the output ordering behavior. 

Lucas Seiki Oshiro responds to Eric Sunshine's request for test coverage of the output ordering by implementing a test case that verifies the new behavior where fields are returned in the order they were requested, rather than lexicographically sorted. This follows the recent consensus to remove both sorting and deduplication from the output.

The test creates a repository and checks that `git repo info` returns fields in exactly the order specified in the command line, with two permutations to verify the ordering is truly input-dependent. This confirms the implementation matches the agreed-upon approach that prioritizes script predictability over sorted output.

The patch appears to be a straightforward implementation of the testing requirements for the newly agreed behavior. It's a focused addition that completes the test coverage for this aspect of the command's functionality, which was one of the final open items in the series. The test follows the project's style and provides clear verification of the key behavior change.