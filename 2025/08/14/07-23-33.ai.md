This is a documentation discussion about command usage consistency in Git's man pages, specifically regarding the `checkout` vs `switch` debate in the context of the recently merged rebase documentation improvements.

Karthik Nayak weighs in on the ongoing discussion about whether to use `git checkout` or `git switch` in documentation examples. He agrees with Junio C Hamano's position that maintaining consistency with existing documentation (using `checkout`) is preferable for now, rather than mixing commands within the same document. The email supports Junio's suggestion that any systematic replacement of `checkout` with `switch`/`restore` should be handled as a separate, comprehensive effort targeting Git 3.0, not as part of the current rebase documentation work.

Key technical points include:
- The recognition that `checkout` handles both branch and detached HEAD cases where `switch` requires `--detach` for non-branch arguments
- The importance of documentation consistency to avoid confusing readers
- The distinction between immediate documentation needs (rebase) and broader documentation modernization efforts

This email represents a consensus-forming moment in the thread, with multiple participants now aligned on keeping `checkout` in the rebase docs while planning a future sweep for command modernization. The discussion remains focused on documentation principles rather than rebase implementation details.