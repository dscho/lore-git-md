This is a documentation refinement discussion in the ongoing git-rebase man page improvement thread. Junio C Hamano provides technical corrections to Julia Evans' proposed description of rebase internals, specifically addressing the conceptual model of how commits are selected during a rebase.

The key technical clarification is that rebase does not actually perform commit enumeration and filtering as two separate steps (as previously described), but rather does this in a single operation through either the `format-patch | am` or `rev-list | cherry-pick` pipeline. Junio suggests either combining the first two steps into one simplified step, or explicitly marking the separation as a conceptual white lie for educational purposes.

The discussion reveals implementation details about rebase's internals:
- The actual execution uses either `run_am()` or `run_sequencer_rebase()`
- Both approaches perform commit enumeration and filtering in one pass
- The current description in v7 already has a simplified version that could serve as the basis

This email continues the thread's pattern of balancing technical accuracy with approachability in documentation, now focusing specifically on the internal commit selection mechanics. The exchange demonstrates how implementation knowledge should inform but not overwhelm documentation, with Junio providing the technical grounding while Julia considers pedagogical effectiveness. The resolution will affect how rebase's performance characteristics are presented to users.