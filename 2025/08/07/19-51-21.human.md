This is the third patch in a 4-part series fixing corruption issues in `git archive` when handling large files. The patch refactors the zlib compression loop in `archive-zip.c` to properly process all input data before reading more.

René Scharfe reviews Toon Claes' change, which adds a nested loop around `git_deflate()` to ensure all input is processed before continuing. The original code could lose input data by not fully compressing each chunk before moving to the next. Scharfe confirms the technical approach makes sense, noting the previous misunderstanding about `deflateBound()` leading to the incorrect single-pass assumption.

The change follows Git's standard zlib usage patterns and maintains all existing error checking. With Scharfe's approval, this patch appears technically sound and moves the series closer to completion. The thread shows strong consensus on the solution, with no outstanding objections to this particular change.