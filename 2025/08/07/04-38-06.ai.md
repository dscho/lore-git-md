Refactoring follow-up to the push error handling series. This is a minor cleanup addressing a final review comment from Patrick Steinhardt on Denton Liu's switch statement conversion patch that concluded the earlier bugfix series.

The original series fixed a BUG() assertion when pushing invalid object IDs to unqualified refs, with this final patch restructuring the error handling code into a switch statement for better readability. Patrick now notes that the `type` variable can be removed since it's only used within the switch statement's value check. This would further clean up the code without changing behavior.

The change is localized to `remote.c`'s `show_push_unqualified_ref_name_error()` function, which handles error advising for push operations. The thread is effectively complete with all technical issues resolved and maintainer approvals given - this is just a small optimization to the already-approved refactoring. The series has been marked ready for inclusion in the next Git release cycle.