Thread summary:

Heap overflow fix for `git diff --no-index` stdin comparison in subdirectories, with architectural follow-up addressing root cause. Original issue reported by Gregoire Geis involved "-" (stdin) path handling in subdirectories causing path normalization overflow in `diff.c`'s `strip_prefix()`. 

Initial fix (v1) added stdin path checks in `diff.c` with new test `t4072-diff-no-index-dash.sh`. Junio Hamano identified deeper architectural issue: `--no-index` incorrectly inherits Git repository working directory assumptions. Final solution:

1. Reverts original working directory change
2. Clears Git startup prefix during `--no-index` operation
3. Uses existing test in `t4053-diff-no-index.sh`

Technical implementation:
- Changes limited to `builtin/diff.c` initialization (14 lines)
- Preserves stdin output format ("a/- b/-")
- Only affects `--no-index` mode
- Validated in reporter's environment

Current status:
- Original reporter Gregoire Geis confirmed solution works in their environment with ASAN verification
- All tests pass in `t4053-diff-no-index.sh` (note: `t4056-diff-order.sh` failure is pre-existing)
- Junio Hamano verified testing approach
- Memory safety issue resolved
- Architectural improvement implemented
- No outstanding technical concerns
- Ready for integration

The thread demonstrates Git's collaborative workflow: initial bug report led to focused solution, which maintainer feedback elevated into more architecturally sound fix. Final confirmation from both reporter and maintainer completes the technical discussion cycle. Solution maintains test coverage while properly separating `--no-index` mode from repository assumptions.