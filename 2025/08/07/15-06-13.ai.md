Bugfix for a heap overflow in `git diff --no-index` when comparing stdin in a subdirectory. The issue occurs when using `-` (stdin) as one of the paths while running from a subdirectory - the path normalization code fails to handle this special case, leading to a buffer overflow that ASAN detects. The fix adds an explicit check for "-" in the path normalization logic to skip processing for stdin paths, similar to how absolute paths are already handled. 

The patch modifies `diff.c` to add the stdin check in the path stripping logic, adds a new test file `t4072-diff-no-index-dash.sh` that verifies the fix works both in the root directory and deep subdirectories, and updates the meson build file to include the new test. The test uses a deep directory structure (`aa/bb/cc`) to ensure the bug is caught even without ASAN instrumentation.

Key technical details:
- The bug manifests in `diff.c`'s `strip_prefix()` function
- The fix adds `strcmp(*namep, "-")` checks alongside existing absolute path checks
- Test verifies correct output format ("a/- b/-") for stdin comparisons
- The issue was found using ASAN and valgrind instrumentation

This appears to be a straightforward and well-justified fix for a clear memory safety issue, with appropriate test coverage added. The change is minimal and focused, only touching the necessary components to fix the specific bug.