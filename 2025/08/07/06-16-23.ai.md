This is a technical follow-up in Patrick Steinhardt's multi-pack index refactoring series, specifically addressing feedback on patch 3/8 about ODB source creation. The discussion focuses on optimizing path handling in `odb_add_to_alternates_memory()`.

Toon Claes had suggested avoiding `real_pathdup()` by using `odb->sources->path` directly with a "/" base parameter. Patrick explains this isn't possible because `alt_odb_usable()` requires normalized paths for comparison. However, he identifies a deeper redundancy - callers are passing both an ODB and its path when the path is already stored in the ODB structure.

The key technical points:
- Maintains the need for path normalization in alternate ODB validation
- Reveals redundant parameter passing pattern in `link_alt_odb_entry()`
- Suggests making the path parameter internal to simplify the interface
- Builds on prior changes to return created sources rather than void

This exchange shows Patrick both addressing review feedback and identifying additional optimization opportunities in the ODB interface. The discussion remains focused on implementation details of the refactoring, with no behavior changes proposed.