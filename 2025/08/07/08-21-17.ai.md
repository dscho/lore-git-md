This is a refactoring patch in Patrick Steinhardt's ongoing ODB source system integration series, specifically patch 3/8. Karthik Nayak provides a review of the changes.

The patch modifies `link_alt_odb_entry()` to return the newly created `odb_source` pointer rather than just a success/failure integer. This prepares for future need to access the source pointer directly. The review notes this change makes sense for the upcoming integration work.

Karthik also observes the function signature change from taking a `strbuf` to a `const char*` for the `entry` parameter, making the function more versatile. He suggests this change should be explicitly mentioned in the commit message.

The changes affect ODB alternates handling, with modifications to `odb.c` and `odb.h`. The refactoring maintains existing behavior while improving the code structure for future use cases. This continues the pattern seen in earlier patches of simplifying interfaces and reducing redundant parameter passing in the ODB source system work.

The review indicates general approval of the changes while suggesting minor documentation improvements. This patch appears to be a straightforward preparatory step in the larger ODB abstraction effort, building on established patterns from previous refactoring work.