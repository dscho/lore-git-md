This is a code review comment on Patch 1 of the `git archive` ZIP corruption fix series. René Scharfe approves the buffer management simplification in `archive-zip.c`'s `write_zip_entry()` function, noting the patch successfully reduces code duplication by moving zlib output buffer setup (`next_out` and `avail_out`) into the main compression loop.

The review confirms the optimization is safe despite theoretically redundant writes when `out_len == 0`, as this condition is both rare and harmless. The change aligns with the series' goal of cleaning up zlib usage patterns while fixing corruption issues with large files. No new technical concerns are raised - this appears to be a straightforward approval of the refactoring approach taken in the first patch.