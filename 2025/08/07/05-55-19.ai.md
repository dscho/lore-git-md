This is a follow-up discussion in the `--max-depth` feature series, focusing on implementation details of the option parsing and validation logic.

Patrick Steinhardt raises two key points about the current implementation:

1. Questions whether `OPT_UNSIGNED()` could be used instead of the current callback approach, suggesting the recursive flag could be set in `diff_setup_done()` like other options. This would simplify the need for the `max_depth_valid` flag.

2. Notes that `git grep --max-depth` handles -1 as "no limit" (matching `-r/--recursive` behavior), which the current diff implementation doesn't support. Suggests either:
   - Initializing with -1` as default to eliminate `max_depth_valid`
   - Keeping current approach if -1 initialization isn't straightforward

The discussion remains technical and constructive, with Patrick offering alternatives while being open to keeping the current implementation if the changes prove difficult. This continues the thread's pattern of refining the feature's edge cases and implementation details after the core functionality was validated in earlier reviews.