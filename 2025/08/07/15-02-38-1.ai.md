This is part of the `git repo info` feature series, specifically adding the `layout.shallow` field to report whether a repository is shallow. The patch moves functionality from `git rev-parse --is-shallow-repository` into the new command hierarchy. It implements the field using `is_repository_shallow()` from shallow.h, following the established pattern of the series by returning "true"/"false" strings. The implementation includes documentation updates and test cases that verify both shallow and non-shallow repository detection, including a test that creates an actual shallow clone to validate the true case.

Key technical details:
- New field: `layout.shallow` returns "true" or "false" via `is_repository_shallow()`
- Test coverage includes:
  - Baseline non-shallow case
  - Actual shallow clone creation/verification
  - Uses the series' standard test_repo_info helper
- Follows the established dual strbuf architecture
- Maintains consistency with other boolean fields like `layout.bare`
- Documentation added alongside other fields in git-repo.adoc

The patch has multiple Helped-by tags indicating thorough review, particularly around the shallow repository testing approach. It's a straightforward addition that fits cleanly into the series' architecture while providing equivalent functionality to the rev-parse command it replaces.