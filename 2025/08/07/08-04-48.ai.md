This is part of the ongoing `the_repository` removal effort in the commit-graph subsystem. Patch 6/10 in Patrick Steinhardt's series refactors how hash algorithm information is stored in the commit-graph data structure.

The change replaces the `hash_len` field in `struct commit_graph` with a `hash_algo` pointer to the full hash algorithm structure. This provides access to the complete algorithm information rather than just the raw size, which will be needed for subsequent changes in the series. The patch updates all code that previously used `hash_len` to instead use `hash_algo->rawsz`, maintaining identical behavior while enabling future work.

The diff shows mechanical updates across the commit-graph implementation - in chunk validation, OID lookups, generation number calculations, and graph verification. The changes are straightforward substitutions with no functional impact, though they do touch sensitive areas like commit data parsing and graph verification. The patch maintains all existing behavior while setting up infrastructure needed for later patches that will fully eliminate `the_repository` dependencies in the commit-graph code.

This continues the series' pattern of incremental, mechanical changes that move toward the architectural goal while maintaining correctness. The patch has no direct user impact but is an important stepping stone in the larger effort to make the commit-graph subsystem repository-agnostic.