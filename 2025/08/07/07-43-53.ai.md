This is a test patch review in the context of the already-merged `git repo info` command series. Karthik Nayak provides a final nitpick on test style for patch 2/8, which introduced the `references.format` field reporting. The comment specifically addresses single-quoting style in test error messages, suggesting the use of `${SQ}foo${SQ}` instead of the literal `'\'foo\''` seen in the test. This matches Git's established test conventions for quoting. The test file being reviewed (t1900-repo.sh) contains the core validation for the ref format reporting functionality, including: correct output for both files/reftable backends, error handling for invalid keys, mixed valid/invalid key behavior, and deduplication of repeated keys. The tests use the keyvalue output format and verify the command's behavior matches expectations through test_cmp comparisons. This late-stage review note indicates the series is in its final polishing phase, with only minor style adjustments being caught after merge to 'seen'. The test architecture shown here became the pattern for subsequent tests in the series, with similar validation approaches used for the layout.bare and layout.shallow fields in later patches.