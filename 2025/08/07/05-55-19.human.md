This is a follow-up discussion in the `--max-depth` feature series for Git's diff machinery. Patrick Steinhardt is responding to Toon Claes about implementation details of the new option.

The conversation focuses on two technical refinements:
1. Whether to use `OPT_UNSIGNED()` for the max-depth parameter and handle the recursive flag in `diff_setup_done()`, which would simplify the code but requires determining how to track whether max-depth was set
2. Aligning with `git grep`'s behavior by supporting `-1` as "no limit" (currently not implemented)

Patrick suggests initializing with a default value of -1` to potentially eliminate the need for the `max_depth_valid` flag, while acknowledging the current implementation may be acceptable if that approach isn't straightforward. The exchange shows careful attention to consistency with existing Git conventions and APIs.

This is a focused technical discussion between collaborators refining an already well-designed feature, with no major disagreements - just exploring potential simplifications to the implementation.