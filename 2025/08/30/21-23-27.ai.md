This is the final patch in a series addressing midx-write error handling and cleanup. It simplifies error case management in `write_midx_internal()` by standardizing the error return pattern.

The patch makes several key changes to error handling in midx-write.c:
1. Initializes `result` to -1 at function start rather than 0, making error the default state
2. Only sets `result = 0` on successful paths (including two new success cases via goto)
3. Removes redundant `result` assignments before cleanup jumps
4. Maintains all existing cleanup behavior while making the control flow more consistent

The changes ensure the function always returns -1 on error rather than the previous mix of 1 and -1 values. The patch builds on earlier work in the series to standardize error handling patterns, with all cleanup now properly managed via the `cleanup:` label. This final patch completes the series' goal of robust and consistent error handling in midx writes.

The technical changes are focused entirely on control flow within `write_midx_internal()`, touching no other subsystems. The patch follows the series' established pattern of pessimistic initialization and comprehensive cleanup. As the last in the series, it represents the culmination of several rounds of review and refinement of the midx-write error handling approach.