This is a response in the ongoing discussion about retaining the `git whatchanged` command. Jeff King (peff@peff.net) engages with the kernel developer's concerns about command removal, proposing a technical solution to ease the transition.

The email suggests allowing aliases to override deprecated commands like `whatchanged`, which would let users maintain their workflow while moving to `git log --raw` equivalents. Peff provides a concrete patch that would modify Git's command dispatch logic to permit this special case for deprecated commands. The change would help during the transition period where some Git versions show deprecation warnings but don't yet support the alias override behavior.

Peff acknowledges this may not fully solve the original poster's Docker versioning problems, particularly if containers lack the alias configuration. The response maintains a constructive tone while exploring technical middle ground - neither rejecting the removal outright nor dismissing the user's workflow concerns. The patch appears to be a well-considered incremental change that could make command deprecations less disruptive in general.