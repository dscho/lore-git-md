This is a technical discussion about the potential deprecation of `git whatchanged` and a proposed mitigation strategy. Jeff King (peff@peff.net) responds to a kernel developer's concerns about removing the command by suggesting `git log --raw` as an alternative and proposing a code change to make the transition smoother.

The key technical proposal is a patch that would allow aliases to override deprecated commands (currently only `whatchanged` is marked this way). This would let users create a `whatchanged` alias that works across Git versions during the deprecation period. The change modifies `git.c` to check for deprecated commands before enforcing the usual "no alias override" rule.

Peff acknowledges limitations - this wouldn't help with containers lacking the alias configuration, and already-released Git versions would still show deprecation warnings. The discussion reveals an interesting tension between deprecation policies and real-world workflows, particularly in constrained environments like Docker containers where version management is difficult.

The thread context shows this is part of a larger discussion about `git whatchanged` removal, with a kernel developer arguing for its retention due to workflow preferences and toolchain constraints. Peff's response attempts to find a middle ground that maintains forward compatibility while still allowing eventual removal of the command.