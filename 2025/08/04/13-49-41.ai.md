Performance optimization follow-up in the xdiff hashing series. Phillip Wood responds to Alexander Monakov's patch optimizing `xdl_hash_record_verbatim()` with a djb2 hash variant, raising several practical considerations before merging.

While acknowledging the 1.5x speedup in the isolated hash function, Phillip notes the end-to-end `git log` improvement is more modest (1.02x over previous patch, 1.11x over master). He points out Phillip Wood's xxhash implementation still outperforms this by 1.03x, suggesting the optimization's real-world impact may be limited. 

Key technical questions raised include: compiler compatibility beyond GCC/Clang (particularly regarding the REASSOC_FENCE macro), potential use of C11's atomic_signal_fence() where available, and whether the changes might regress performance on some platforms. The email maintains the thread's focus on xdiff/xutils.c optimizations while shifting discussion toward practical integration concerns rather than algorithmic improvements. 

This continues the performance-oriented thread but introduces new validation requirements before the change could be merged, despite Junio Hamano's earlier approval of the technical approach. The exchange highlights how microbenchmark improvements don't always translate linearly to real-world gains.