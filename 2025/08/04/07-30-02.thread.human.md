This thread started with a one-line fix to make the `t0450` test work correctly with Meson builds by using `$GIT_SOURCE_DIR` instead of `$GIT_BUILD_DIR` when locating documentation files. Patrick Steinhardt noted that the test wasn't failing as expected because it silently skips checks when documentation is missing, using a prereq mechanism (`BUILTIN_ADOC_$builtin`). He suggested this might hide real issues and proposed investigating whether to replace the prereq with an explicit skip-list approach.

Toon Claes has now joined the discussion, agreeing with Patrick's analysis. He supports moving toward a more explicit skip-list system (similar to the existing `adoc-help-mismatches` file) that would make documentation requirements clearer and help catch missing documentation for new subcommands. The conversation is evolving from the initial build-system fix to focus on improving how the test handles missing documentation cases, with both participants favoring a stricter approach that would make the test failures more actionable. This may lead to a follow-up patch modifying the test's behavior.