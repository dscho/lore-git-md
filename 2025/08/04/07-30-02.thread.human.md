The thread began with a simple build fix for documentation verification in test t0450, where Patrick Steinhardt identified that the test's prereq mechanism was silently skipping builtins without documentation, potentially masking real gaps. This led to a broader discussion about test robustness, with Patrick and Toon Claes advocating for an explicit allowlist approach instead of silent skips.

The resulting two-patch series first addressed the original build issue, then implemented the new verification strategy. The second patch, which just arrived, introduces an allowlist file (`t0450/adoc-missing`) that explicitly tracks which builtins are permitted to lack documentation. The test now actively verifies that undocumented builtins are properly accounted for in the allowlist, and fails if there are any mismatches. This makes missing documentation a deliberate, tracked decision rather than an oversight, while ensuring new subcommands will be properly documented. The changes focus on improving the reliability of the documentation verification tests, with the implementation now enforcing stricter requirements through the allowlist mechanism. The thread shows clear technical progression from initial problem identification to robust solution, with Patrick's analysis and Toon's review helping shape the final approach.