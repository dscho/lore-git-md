This thread addresses a test fix for Meson build compatibility, specifically targeting test t0450 which verifies consistency between command-line help and documentation. The original version incorrectly used `$GIT_BUILD_DIR` to locate documentation sources during out-of-tree builds, which fails with Meson's always-out-of-tree approach. The one-line patch changes this to use `$GIT_SOURCE_DIR` instead, ensuring the test works correctly across all build configurations.

The change is minimal but important for maintaining test reliability as the project expands build system support. While the commit message could be more explicit about targeting Meson builds, the technical solution is straightforward and correct. This appears to be a focused, uncontroversial fix that improves cross-build consistency.