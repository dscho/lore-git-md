This is the core refactoring patch in the `git refs list` series, extracting shared logic from `git for-each-ref` into a reusable helper. The patch introduces a new `for-each-ref.h` header containing the common option documentation macro `COMMON_USAGE_FOR_EACH_REF` and the new `for_each_ref_core()` function that implements the ref listing logic.

The key technical changes move all option parsing, filtering, and formatting from `cmd_for_each_ref()` into the new helper function, which takes the command's usage string as a parameter. The original command becomes a thin wrapper that just defines its specific usage and calls the shared helper. The header file establishes the architectural foundation for sharing this logic with the upcoming `git refs list` command.

The patch maintains all existing behavior while enabling code reuse, following the pattern preferred by Junio Hamano where helper functions receive usage strings as parameters. The implementation handles error cases consistently by passing through the caller's usage string to `usage_with_options()`. The `COMMON_USAGE_FOR_EACH_REF` macro consolidates the shared option documentation that will be used by both commands.

This appears to be a well-executed refactoring with clear motivation stated in the commit message. The changes are focused and maintain existing functionality while enabling the planned `git refs list` feature. The patch follows established Git conventions for command refactoring and has already gone through multiple review iterations, with no new technical concerns raised in this version.