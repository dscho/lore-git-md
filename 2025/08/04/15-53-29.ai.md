This is a refactoring patch that finalizes the compiler warning fix for `builtin/remote.c` discussed in the thread. Jeff King (peff) implements the control flow restructuring approach previously suggested by Junio Hamano, moving the remote name validation earlier in `set_head()` to make initialization paths clearer to both humans and compilers.

The patch enforces that a remote name argument is present before proceeding, eliminating the conditional `remote` struct initialization that triggered GCC's `-Wmaybe-uninitialized` warning under `-Og`. The change duplicates a usage message line but improves code clarity by making the remote name requirement explicit up front. The diff shows the straightforward restructuring - moving the remote name check before option handling while keeping the same core logic.

Key technical points: The `remote` pointer initialization is now unconditional after the early check, addressing the compiler warning. The change touches `builtin/remote.c`'s `set_head()` function, specifically the argument validation logic and remote name handling. The patch maintains the existing behavior while making the control flow more obviously correct.

The email also includes an aside about the parallel `matchlen` initialization in `clar.c`, noting that zero is the most logical default value when `filter` is NULL since it represents "zero matched bytes". Jeff mentions this code originates from libgit2 and CCs Patrick Steinhardt for potential upstream consideration.

This concludes the thread's technical work - the patch implements the agreed-upon solution with no outstanding questions or objections from maintainers. The changes are behavior-neutral refactorings prioritizing code clarity over compiler workarounds.