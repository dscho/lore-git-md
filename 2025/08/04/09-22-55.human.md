This is the final test patch in a series introducing `git refs list` as a thin wrapper around `git for-each-ref`. The patch adds a new test script `t1461-refs-list.sh` that verifies the new command behaves identically to `for-each-ref` by reusing the existing test infrastructure.

The test approach is straightforward but effective: it sets `git_for_each_ref` to "git refs list" and sources the shared test library from the previous commit, ensuring the new command passes all the same tests as the original. This confirms the wrapper maintains full compatibility while keeping test maintenance simple through code reuse.

The patch follows the series' pattern of careful, incremental changes with strong test coverage. It's a clean addition that completes the testing strategy for this feature. With this final test in place, the series appears ready for final review and potential merging.