This is part 4/9 in Patrick Steinhardt's series to remove global state dependencies from the commit-graph subsystem, specifically addressing sign comparison warnings. The patch focuses on type consistency, converting several signed integer variables to unsigned types where they represent counts or positive quantities.

Key technical changes include:
- Converting loop counters and array indices from `int` to `size_t` where they iterate over unsigned counts
- Changing `num_commit_graphs_before` and `num_commit_graphs_after` from `int` to `size_t` in `write_commit_graph_context`
- Making `write_graph_chunk_base_1()` return `size_t` instead of `int` for chunk counting
- Fixing the hash version initialization in bloom settings to use an explicit unsigned cast for the `-1` sentinel value
- Adding casts where necessary for comparisons between signed and unsigned values (e.g., file sizes vs hash lengths)

The changes are mechanical and behavior-preserving, continuing the series' focus on type correctness while removing implicit dependencies on global state. The patch touches core commit-graph writing logic but doesn't modify any algorithms - it just makes the types match their actual usage. The bloom filter hash version change is notable as it clarifies the sentinel value handling while maintaining the same behavior.

This patch follows the established pattern of the series by addressing a specific category of technical debt (sign comparison warnings) while moving toward the larger goal of removing `the_repository` dependencies. The changes are localized to commit-graph.c and don't require test updates, suggesting they maintain existing behavior while improving type safety.