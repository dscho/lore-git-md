Feature patch series (v5) introducing `git refs list` as a consolidated ref listing command, now in final polishing stage. This version splits out a test fix into its own commit (patch 2/6) as requested in v4 reviews, while maintaining all architectural improvements from previous iterations.

The series establishes `git refs list` as a thin wrapper around the newly shared `for_each_ref_core()` helper, providing identical functionality to `git for-each-ref` but under the unified `git refs` namespace. Key technical elements remain unchanged from v4: shared option documentation in `for-each-ref-options.adoc`, the `COMMON_USAGE_FOR_EACH_REF` macro, and test infrastructure consolidation through `t/for-each-ref-tests.sh`.

Patch organization now shows:
1. Documentation refactoring (unchanged from v4)
2. New commit fixing `for-each-ref` usage string alignment with man page
3. Core logic extraction to `for_each_ref_core()`
4. `refs list` implementation
5-6. Test refactoring and new tests

The cover letter reiterates the long-term vision for ref command consolidation while emphasizing this initial step maintains strict backward compatibility. Wildcard behavior remains anchored at hierarchy boundaries (`refs/heads/m*` matches `morning` but not `mid/night`), with cross-hierarchy matching (`**`) explicitly deferred to future work.

With all major architectural decisions settled and only minor polishing remaining, this appears ready for final review. The changes since v4 are minimal - primarily addressing organizational feedback by separating the test fix - suggesting the series is nearing completion. Key participants remain Meet Soni (author), Patrick Steinhardt (mentor), and Junio Hamano (maintainer), with no new technical concerns raised in this iteration.