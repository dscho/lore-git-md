This is a test refactoring patch in the `git refs list` series (v5 5/6). It prepares for adding tests for the new `git refs list` command by restructuring the existing t6300 test suite to make its logic shareable.

The patch moves the core test logic from `t6300-for-each-ref.sh` into a new `for-each-ref-tests.sh` file, replacing hardcoded calls to "git for-each-ref" with a `$git_for_each_ref` variable. The original test file becomes a simple driver that sets the default value of this variable and sources the test library. This follows the pattern used for sharing tests between `git-blame` and `git-annotate`.

Key technical elements:
- Creates new `for-each-ref-tests.sh` with 2141 lines of test logic
- Modifies `t6300-for-each-ref.sh` to be a minimal driver (14 lines)
- Uses `$git_for_each_ref` variable to make tests command-agnostic
- Maintains all existing test coverage while enabling reuse
- Follows established test sharing pattern from other commands

The change is part of the larger effort to introduce `git refs list` as a thin wrapper around `git for-each-ref`. This test refactoring enables the new command to reuse the extensive existing test infrastructure with minimal duplication. The patch appears well-structured and focused, with clear commit message explaining the motivation and approach.