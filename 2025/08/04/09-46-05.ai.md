This is a type safety refactoring patch in Patrick Steinhardt's reflog migration series. It changes the `split_ident_line()` function's length parameter from `int` to `size_t` to better match its actual usage, since callers consistently pass positive values from `strlen()`, `strbuf::len`, or pointer differences.

The change is straightforward - just updating the function signature in both the declaration (ident.h) and implementation (ident.c). The commit message explains the rationale well: the parameter represents a string length that is always non-negative in practice, so using `size_t` is more appropriate than `int`. This aligns with Git's ongoing type safety improvements.

This patch doesn't introduce any behavior changes - it's purely a type system improvement that makes the interface more accurately reflect its real-world usage. As part of the larger reflog migration work, it helps ensure type consistency when processing ident lines from reflog entries. The change is small and focused, with no apparent controversy or open questions - it's the kind of uncontroversial cleanup that typically gets merged quickly.