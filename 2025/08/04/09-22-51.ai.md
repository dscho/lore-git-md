This is a documentation and test fix patch, part of the v5 series introducing `git refs list`. The patch specifically addresses a pre-existing inconsistency between `git for-each-ref`'s usage string and its man page documentation, which had caused test t0450 to be marked as broken.

The changes align the in-code usage string with the documentation by:
1. Moving the full option list into the new `COMMON_USAGE_FOR_EACH_REF` macro (introduced in the series for sharing between commands)
2. Updating the usage string array to use this macro
3. Removing the 'known breakage' marker from t0450 since the mismatch is fixed

The patch demonstrates good attention to detail in catching and fixing this documentation inconsistency while leveraging the series' new shared infrastructure. It cleanly separates this documentation fix from the core functionality changes in other patches of the series, as requested in previous reviews. The fix is straightforward and uncontroversial, simply bringing the code in line with existing documentation standards.