This is the second patch in a series fixing ZIP archive corruption issues, specifically a refactoring change to simplify buffer handling in `archive-zip.c`. The patch removes an unnecessary conditional check around `write_or_die()` since the function can safely process zero-length writes. The change maintains the same behavior while making the code more straightforward by eliminating redundant logic.

The modification affects the compression output path where zlib-processed data (`compressed` buffer) gets written to the final ZIP archive. The patch removes the `if (out_len > 0)` guard while keeping the essential operations - writing the output buffer and tracking compressed size. This aligns with the series' goal of cleaning up zlib stream handling while fixing corruption issues with large files.

Key technical details: the change touches the same compression buffer management as patch 1, specifically around `zstream.next_out` and the `compressed` array. The simplification is safe because `write_or_die()` internally handles zero-length writes correctly, and negative lengths were already impossible due to prior validation. This continues the pattern of removing redundant checks while maintaining the core compression logic.

The patch retains Justin Tobler's co-authorship from the original diagnosis while Toon Claes drives the implementation. This change appears non-controversial as it's a straightforward cleanup that doesn't affect functionality, just code clarity. It builds on the foundation laid in patch 1 and prepares for the more substantial zlib loop corrections coming in subsequent patches.