Follow-up discussion on git mv --after proposal

This is a response in the ongoing discussion about adding a `--after` flag to `git mv`. The original proposer, FMorschel, expresses surprise at Git's design choice to not explicitly track renames in commits, instead relying on heuristics to detect them later. They ask whether this behavior might be configurable on a per-project basis.

The email is reacting to Konstantin Khomoutov's technical explanation that `git mv` is essentially just a convenience wrapper for `rm`+`add` operations, and that Git's rename detection works by comparing tree objects rather than recording rename operations directly. FMorschel seems to be grappling with the fundamental difference between Git's approach and Mercurial's explicit rename tracking.

The exchange highlights a potential disconnect between the original proposal's assumptions and Git's actual rename detection model. While the thread started as a feature proposal, it's now turning into a discussion about Git's core design philosophy regarding rename tracking. No new technical details about implementation are offered in this response - it's primarily seeking clarification about Git's current behavior and whether it might be made configurable.