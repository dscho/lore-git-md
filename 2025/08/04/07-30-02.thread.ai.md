Test fix and hardening for documentation verification in t0450, addressing both Meson build compatibility and test robustness. The thread originated with a one-line fix for out-of-tree builds (using `$GIT_SOURCE_DIR` instead of `$GIT_BUILD_DIR` to locate .adoc files), which Patrick Steinhardt analyzed to reveal a deeper issue: the test's prereq mechanism (`BUILTIN_ADOC_$builtin`) silently skipped missing documentation cases, potentially masking real gaps.

The final two-patch series implements an explicit allowlist approach as proposed by Patrick and endorsed by Toon Claes. Patch 1 retains the original build fix, while patch 2 introduces two allowlist mechanisms: `t/t0450/adoc-help-mismatches` tracks permitted documentation/help inconsistencies, and `t0450/adoc-missing` lists 9 builtins that intentionally lack documentation (e.g., `checkout--worker`, `submodule--helper`). The test now fails if a builtin's documentation status doesn't match its allowlist entries.

Junio C Hamano has confirmed he will squash Patrick Steinhardt's fixup changes into Toon Claes' original [2/2] patch and merge the topic to 'next'. The fixup corrected two key issues in `t/t0450-txt-doc-vs-help.sh`:
1. Reversed the grep condition to properly set test prereqs
2. Added the missing `adoc-missing` allowlist file

Toon Claes explicitly approved the final implementation after acknowledging their initial solution worked locally but failed in CI due to rushed testing. The collaboration between Patrick Steinhardt, Toon Claes, and Junio Hamano progressed from initial build fix to robust test design, with community review catching implementation gaps. The series is now complete with all technical issues resolved and final approvals given.

Key technical elements are the transition from silent prereq skips to explicit allowlisting, with test logic verifying both documentation presence/absence and help-text consistency against their respective allowlists. The changes improve documentation verification rigor while maintaining compatibility with both Makefile and Meson builds. The thread demonstrates Git's effective patch review process where initial implementations are refined through community feedback into robust solutions.