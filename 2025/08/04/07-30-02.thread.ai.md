Test fix and hardening for documentation verification in t0450, addressing both Meson build compatibility and test robustness. The thread originated with a one-line fix for out-of-tree builds (using `$GIT_SOURCE_DIR` instead of `$GIT_BUILD_DIR` to locate .adoc files), which Patrick Steinhardt analyzed revealed a deeper issue: the test's prereq mechanism (`BUILTIN_ADOC_$builtin`) silently skipped missing documentation cases, potentially masking real gaps.

The current two-patch series implements an explicit allowlist approach as proposed by Patrick and endorsed by Toon Claes. Patch 1 retains the original build fix, while patch 2 introduces two allowlist mechanisms: `t/t0450/adoc-help-mismatches` tracks permitted documentation/help inconsistencies, and `t0450/adoc-missing` lists builtins that intentionally lack documentation. The test now fails if a builtin's documentation status doesn't match its allowlist entries - either missing documentation when not allowed, or having documentation when listed as intentionally missing.

Patrick Steinhardt completed the implementation after Junio C Hamano noted an incomplete submission from Toon Claes (missing diff content). The final version reverses the original logic to check the allowlist first rather than checking for file existence, with Patrick correcting the grep condition from Toon's proposal. The `adoc-missing` allowlist contains 9 builtins (e.g., `checkout--worker`, `submodule--helper`) that intentionally lack documentation. The test now explicitly verifies documentation presence/absence against the allowlist using grep checks, replacing the silent-skip behavior entirely.

Key technical elements are the transition from silent prereq skips to explicit allowlisting, with test logic in `t/t0450-txt-doc-vs-help.sh` verifying both documentation presence/absence and help-text consistency against their respective allowlists. The changes focus on improving documentation verification rigor while maintaining compatibility with both Makefile and Meson builds. Patrick is seeking review on whether to squash this with the previous patch or submit separately. The thread shows clear progression from initial build fix to improved test design, with Patrick Steinhardt and Toon Claes driving the discussion toward more robust documentation verification, and Junio ensuring implementation completeness.