Documentation patch fixing a consistency issue in the `git for-each-ref` command. This is part of Meet Soni's v5 series introducing `git refs list` as a wrapper around `for-each-ref`.

The patch aligns the in-code usage string for `git for-each-ref` with its man page documentation, resolving a known test failure in `t0450-txt-doc-vs-help.sh`. The fix consolidates the previously split usage strings into a single string using the new `COMMON_USAGE_FOR_EACH_REF` macro introduced earlier in the series. This change both fixes the documentation test and maintains consistency with the shared infrastructure being built for the new `refs list` command.

The patch is straightforward - it updates the usage string format in `builtin/for-each-ref.c` and removes the corresponding test from the known-breakages list. This appears to be a clean, well-justified change that was specifically requested in reviews of previous versions of the series. The patch maintains all the architectural improvements from earlier iterations while addressing this documentation consistency issue.