This is part 5/9 of Patrick Steinhardt's series to remove global state dependencies from the commit-graph subsystem, continuing the broader `the_repository` removal effort. The patch converts two hash-algorithm-dependent macros (`GRAPH_DATA_WIDTH` and `GRAPH_MIN_SIZE`) into functions that take the hash algorithm as an explicit parameter, eliminating their reliance on the `the_hash_algo` global. 

The changes are mechanical but touch several key parts of commit-graph handling:
- The new `graph_data_width()` function calculates the per-commit data size (hash size + 16 bytes)
- The new `graph_min_size()` function computes the minimum valid commit-graph file size
- All call sites are updated to pass `the_hash_algo` explicitly (temporarily maintaining the same behavior)
- The changes affect core operations like graph loading, parsing, and tree lookups

This follows the established pattern in the series of replacing implicit global access with explicit parameter passing. The diff shows 16 insertions and 9 deletions across commit-graph.c, with changes focused on the graph file format's size calculations. While this patch doesn't yet eliminate `the_hash_algo` entirely (it still passes it explicitly), it removes two more direct dependencies as groundwork for future work. The changes are behavior-preserving and don't require test updates.