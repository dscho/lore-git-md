Test fix follow-up discussing test behavior with Meson builds. Patrick Steinhardt responds to Toon Claes's patch that fixed t0450 to work with out-of-tree builds by using `$GIT_SOURCE_DIR` instead of `$GIT_BUILD_DIR` for finding documentation files. 

While agreeing the change is correct, Patrick raises an interesting observation about the test's prereq behavior: currently the test silently skips checks when documentation files aren't found, rather than failing. He suggests this might mask real problems, and proposes investigating whether the prereq could be removed entirely or replaced with an explicit skip-list for builtins known to lack documentation. The discussion highlights a potential improvement area in test robustness beyond the immediate fix.