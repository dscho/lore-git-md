Test patch completing the `git refs list` series by adding comprehensive test coverage. This is the final patch in the v5 series that introduces a new `git refs list` command as a thin wrapper around `git for-each-ref`.

The patch adds `t/t1461-refs-list.sh` which leverages the shared test infrastructure created in the previous commit (patch 5/6). The test script works by:
- Setting `git_for_each_ref` to "git refs list"
- Sourcing the shared `for-each-ref-tests.sh` library
This ensures the new command is tested against the full `for-each-ref` test suite, verifying it behaves identically.

The implementation is minimal and clean, consisting of just 8 lines of shell script that hook into the existing test framework. The test is added to meson.build to be included in the test suite. This follows the established pattern seen with other command pairs like `git-blame`/`git-annotate`.

As the final patch in a well-reviewed series, this appears ready for inclusion. It completes the testing strategy by ensuring the new `refs list` command maintains behavioral parity with `for-each-ref` through shared test coverage. The approach demonstrates good architectural thinking by reusing rather than duplicating test logic.