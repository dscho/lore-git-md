This is a refactoring patch in Patrick Steinhardt's reflog migration series, focusing on the `REF_HAVE_OLD` flag behavior for log-only updates. 

The patch changes how Git handles the `REF_HAVE_OLD` flag during reflog updates. Previously this flag was always unset for log-only operations, but now it's preserved while still skipping the old OID verification. This maintains the flag as a useful signal that an old OID was provided, which will be needed in a subsequent commit for new reflog writing functionality.

The change affects all ref backends (files and reftable) and includes updates to verification logic to properly handle the new behavior. The commit message clearly explains the motivation - this isn't just a cleanup, but groundwork for upcoming functionality that will use the preserved flag as a signal.

This appears to be a well-considered change that's part of a larger, carefully constructed series. The patch touches multiple files but makes focused, consistent changes throughout. It maintains backward compatibility while enabling future work, and includes updates to both implementation and documentation to keep everything in sync.