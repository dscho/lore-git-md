This is a brief reply in the ongoing "commit-graph: remove global state dependencies" series, specifically addressing patch 5/9 which converts hash-algorithm-dependent macros to functions.

Taylor Blau provides a positive but minimal review of Patrick Steinhardt's patch that eliminates `the_hash_algo` usage in commit-graph size calculations. The patch converts the `GRAPH_DATA_WIDTH` and `GRAPH_MIN_SIZE` macros into functions that take an explicit `hash_algo` parameter, continuing the series' theme of removing global state dependencies. 

The changes are straightforward - 16 lines added and 9 removed in commit-graph.c, with all callers updated to pass the hash algorithm explicitly. Blau's "Very nice" comment suggests he finds this particular change uncontroversial and well-executed, in contrast to the more debated integer type changes discussed earlier in the thread. This patch appears to be one of the less contentious parts of the series, with no technical objections raised in this response.