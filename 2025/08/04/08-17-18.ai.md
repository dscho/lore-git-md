Refactoring patch in the `the_repository` removal series, specifically addressing integer type consistency in the commit-graph bloom filter counters. This is patch 2/9 in Patrick Steinhardt's series to eliminate global state dependencies in the commit-graph subsystem.

The change converts five signed integer counters in `write_commit_graph_context` to `size_t` since they track non-negative quantities (bloom filter statistics) that only increment. The counters track various bloom filter states: computed, not computed, truncated (empty or large), and upgraded filters. Corresponding trace2 logging calls are updated to use the new `trace2_data_uintmax()` function introduced in the series, maintaining the same statistical reporting but with proper unsigned type handling.

This is a straightforward mechanical change that improves type correctness without altering behavior. The patch aligns with the series' broader goal of removing implicit dependencies on global state by ensuring all numeric types properly reflect their domain. No test updates are included, suggesting the change is purely internal and doesn't affect observable behavior. The diff shows 15 lines changed in `commit-graph.c`, all simple type substitutions in struct fields and their corresponding trace2 calls.