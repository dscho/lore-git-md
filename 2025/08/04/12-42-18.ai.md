This is a technical discussion email in the ongoing thread about content-ignoring diff options and metadata outputs. Jeff King (peff) responds to Junio Hamano's evolving position on whether `--raw` format should respect content-ignoring options like `-w` and `-I<regex>`.

The email focuses on user experience considerations for the proposed behavior change. Peff strongly agrees with Junio's shift toward filtering `--raw` output when blobs compare equal under ignore rules, arguing this better matches user expectations when combining metadata and content-ignoring options. He presents two hypothetical counterarguments but finds them unconvincing: (1) that someone might want to see paths with only whitespace changes in `--raw` output, and (2) the computational overhead of checking content when most files will still show changes.

Key technical points raised:
- The inconsistency between `--name-only/--raw` and `-p` output when using `-w` is highlighted as particularly problematic
- Peff suggests the current behavior could be considered a bug when options are combined
- The discussion assumes implementation via the proposed `DIFF_FORMAT_DRYRUN` approach
- Performance impact is acknowledged but deemed acceptable given the UX benefits

The email advances the thread by solidifying consensus around the expanded scope (including `--raw`) while explicitly addressing potential objections. This suggests the technical direction is nearly finalized, with remaining work focused on implementation details rather than design questions.