[PATCH 0/9] commit-graph: remove global state dependencies

Series by Patrick Steinhardt completing commit-graph subsystem's transition from global state as part of `the_repository` removal and ODB abstraction. 9 patches refactor commit-graph.[ch], builtins, and test helpers to eliminate `the_repository` and `the_hash_algo` while enabling future pluggable object storage.

Key technical changes:
- Type consistency: converted signed counters to unsigned (bloom filters, loops, array indices); changed `struct commit_graph_opts` fields from `int` to `size_t`
- Hash algorithm handling: macros to functions with explicit `hash_algo` params (patch 5/9); stored full `hash_algo` pointers (patch 6/9); updated size calculations
- Trace2: added `trace2_data_uintmax()` for unsigned logging (reviewed by Taylor Blau)
- Parameter cleanup: removed redundant `struct repository*` where `odb_source` provided context
- Verification, progress, file ops updated to use repository context consistently
- Patch 8/9 acked by Taylor Blau, eliminating remaining `the_repository` usage from builtins (`commit.c`, `merge.c`) with 42-line net reduction

Review positions:
- Taylor Blau:
  * Positive on hash algorithm changes (5/9-7/9)
  * Questioned API design of `parse_commit_graph()` regarding repository vs settings/hash_algo separation
  * Mixed on integer type changes - supports `size_t` for array counts but questions bloom filter version handling
  * Provided concrete example of -1 validation issue in patch 3/9
- Oswald Buddenhagen: initially questioned `size_t` for bloom filters
- Junio Hamano: 
  * Clarified position on integer types - requires total memory calculations (N*M) to use `size_t` safely but doesn't mandate `size_t` for individual counts (M)
  * Accepts `size_t` for size parameters (N) while maintaining flexibility for count types
  * Prefers platform-native integers unless memory-specific
- Derrick Stolee: unqualified LGTM, commends thoroughness beyond minimum requirements

Open discussions:
1. Integer type philosophy: refined position from Hamano - memory calculations must be safe in `size_t` but individual counts need not be `size_t` unless memory-specific
2. Hash algorithm API boundaries: whether `parse_commit_graph()` should take repository vs separate settings/hash_algo

Behavior unchanged (test suite passes). Achieves two goals: (1) complete `the_repository` removal in commit-graph, (2) foundational improvements for pluggable ODB. Core technical merits uncontested (Blau's ack of patch 8/9), with remaining discussions about future-proofing rather than blocking issues. Stolee's positive review from commit-graph expert suggests series nearing merge readiness. Hamano's clarification provides clearer guidance on integer type selection while maintaining flexibility in implementation.