This is a test fix targeting Meson build compatibility. The patch addresses an issue in test t0450 where it incorrectly used `$GIT_BUILD_DIR` to locate documentation sources during out-of-tree builds. The test now uses `$GIT_SOURCE_DIR` instead, which properly points to the source tree regardless of build configuration.

The change is minimal - a one-line modification to the `builtin_to_adoc` helper function in t/t0450-txt-doc-vs-help.sh. This test appears to verify consistency between command-line help output and documentation files. The fix ensures the test works correctly with Meson builds, which are always out-of-tree.

The patch looks straightforward and well-motivated, addressing a clear compatibility issue. It's a small change but important for maintaining test reliability across different build systems. The commit message could benefit from explicitly mentioning Meson (it's only implied by "out-of-tree builds"), but the technical content appears sound.