This is the implementation patch for the new `git refs list` subcommand, part of the v5 series consolidating ref listing functionality. The patch introduces `cmd_refs_list()` as a thin wrapper around the shared `for_each_ref_core()` helper extracted in previous commits, making it a peer to `git for-each-ref` with identical functionality.

The implementation follows the established pattern from earlier in the series:
- Uses the `COMMON_USAGE_FOR_EACH_REF` macro for consistent option documentation
- Leverages the shared `for-each-ref-options.adoc` include for man page content
- Maintains behavioral parity with `for-each-ref` through the core helper function

Documentation explicitly states this is an alias for `git-for-each-ref` with identical functionality, addressing potential confusion about behavioral differences. The patch adds the subcommand to both the main command dispatch table and the usage string array in line with Git's command-line interface conventions.

This appears to be a straightforward mechanical implementation of the architectural foundation laid in previous patches, with no new technical complexity introduced. The changes are limited to:
1. Adding the subcommand declaration and implementation in builtin/refs.c
2. Extending git-refs.adoc with the new subcommand's documentation
3. Wiring up the shared options documentation via include directive

The patch maintains the series' clean separation of concerns and demonstrates the value of the shared infrastructure created earlier. With the core logic already extracted and tested in prior commits, this implementation focuses solely on the new command's plumbing while inheriting all existing functionality through the helper.