This is a feature patch series (v5) introducing `git refs list` as part of the ongoing ref-related command consolidation effort. The series builds on previous versions that have already resolved major architectural questions, with this iteration focusing on final polish.

The series now splits out a documentation fix into its own commit (patch 2/6) as requested in previous reviews, while maintaining the core approach of sharing logic between `for-each-ref` and the new `refs list` command via a common helper function. The implementation remains conservative, ensuring full backward compatibility by reusing `for-each-ref`'s internals.

Key technical elements carried forward from earlier versions include the shared `for_each_ref_core()` helper, consolidated option documentation in `for-each-ref-options.adoc`, and test infrastructure that verifies identical behavior between the old and new commands. The series appears well-tested with over 2000 lines of test cases now organized into a shareable test library.

The cover letter reiterates the long-term vision for `git refs` as a unified interface, while emphasizing this initial step deliberately avoids behavioral changes. The author invites discussion about potential future refinements to the ref listing interface now that it's being reconsidered as part of the consolidated command structure.

With major architectural questions settled in previous rounds and only minor organizational tweaks remaining, this v5 appears to be in final polishing shape. The changes since v4 are minimal (splitting out one fix into its own commit) suggesting the series may be nearing readiness for inclusion.