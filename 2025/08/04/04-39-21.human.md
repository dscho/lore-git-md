**Bugfix and behavior clarification for content-ignoring diff options**

Junio C Hamano reconsiders his earlier position on how `--raw` diff output should interact with content-ignoring options like `-I<regex>` and `-w`. While previously suggesting `--raw` should remain unaffected by these filters, he now acknowledges that omitting files where blobs compare equal under the ignore criteria might be what users actually want when combining these options.

This represents a subtle but important shift in the technical discussion about diff behavior consistency. The email suggests that all metadata-focused outputs (`--raw`, `--name-only`, `--name-status`, and `--check`) should potentially respect content-ignoring options, even if this creates some conceptual inconsistency in how `--raw` normally works. Junio recognizes this might seem incoherent from an implementation perspective but argues it better serves user expectations.

The message indicates the thread is moving toward a more user-centric resolution where all diff output formats will consistently apply content-ignoring filters, aligning with Jeff King's earlier arguments about the principle of least surprise. This appears to be the final piece of consensus needed before implementing the `DIFF_FORMAT_DRYRUN` solution discussed earlier in the thread.