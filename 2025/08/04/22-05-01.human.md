Technical review of a commit-graph refactoring patch. This is part of Patrick Steinhardt's ongoing series to remove global state dependencies from the commit-graph subsystem, specifically targeting `the_hash_algo` usage here.

The patch converts two hash-algorithm-dependent macros (`GRAPH_DATA_WIDTH` and `GRAPH_MIN_SIZE`) into functions that take an explicit `hash_algo` parameter instead of relying on the `the_hash_algo` global. This is a straightforward mechanical change that aligns with the series' goal of eliminating global state. Taylor Blau's brief reply indicates approval with no substantive feedback, suggesting this particular change is uncontroversial.

The change affects commit-graph.c with 16 lines added and 9 removed, touching size calculations that depend on the hash algorithm. While small, it's an important step in the larger effort to make the commit-graph subsystem work with pluggable object storage backends. The lack of test modifications confirms this is purely an internal refactoring with no behavior change.