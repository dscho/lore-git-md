Synthetic worktree proposal from Jimmy Thrasibule, introducing new commands (`git worktree push`, `git worktree top`, `git worktree pop`) to create a unified view combining files from multiple repositories while maintaining independent version control. Inspired by OverlayFS, with identified challenges around file conflicts, Git tracking across synthetic views, and clean removal of stacked repositories. Use cases include dependency vendoring and Quilt-style patch management.

Antonio Russo responds with a workaround using existing Git features: gitignore-everything patterns, sparse checkout (non-cone mode), and manual GIT_DIR/GIT_WORK_TREE manipulation. This approach requires force-adding files (`git add -f`) and lacks layer prioritization, but demonstrates real-world demand for the functionality. Notes ostree as a potentially related system, though connection remains unexplored.

Key open questions: how to properly implement layer priorities, handle file conflicts between repositories, and maintain tracking without force-adds. The ostree reference suggests prior art worth investigating. The workaround's limitations highlight requirements for any complete solution: clean layer management and conflict resolution beyond current Git capabilities.