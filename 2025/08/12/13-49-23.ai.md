This is the final version of Julia Evans' documentation series improving the git-rebase man page, now addressing Junio Hamano's technical review comments. The series has already been merged, with this v7 posting showing the final refinements made.

The key changes in this version are all in patch 5/5, which reworks the internals section as a clearer 5-step breakdown. The updates include: clarifying that rebase uses `git patch-id` (not SHA) to detect duplicate commits, fixing line wrapping, and correcting a `git checkout` command from `-C` to `-B`. These changes align with the post-merge discussion that confirmed using `git checkout` (rather than `git switch`) for detached HEAD operations.

The series as a whole restructures the rebase documentation to be more approachable, with early examples, consolidated content, and improved information flow. The internals section now explicitly describes rebase's operation as a 5-step process, with clearer technical details about commit comparison and branch updating. The final version maintains consistency with the project's documentation style while incorporating all review feedback.