Refactoring patch in Patrick Steinhardt's reftable/libgit2 compatibility series (1/8). This first patch fixes a type mismatch in the reftable writer interface where callers were passing `size_t` for record counts but the functions accepted `int`. The change makes the interface consistent and more robust by using `size_t` throughout.

The patch is straightforward, touching only the writer interface and implementation files (`reftable-writer.h` and `writer.c`). It updates function signatures and their implementations to use `size_t` for the record count parameter, which is more appropriate for array sizes. The changes include minor cleanups to the loop variable declarations in the implementation.

This appears to be a preparatory change in the larger series that improves type safety and consistency. The modification is small (11 lines changed) and focused, with no apparent behavior changes beyond the type adjustment. The patch follows the series' theme of making the reftable code more robust and libgit2-compatible.