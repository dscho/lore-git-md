This is part 5/8 of Patrick Steinhardt's reftable/libgit2 compatibility series, focusing on adding flags support to the stack addition interface. The patch modifies `reftable_stack_add()` to accept a `flags` parameter that gets passed through to `reftable_stack_init_addition()`. While currently unused in Git, this prepares for libgit2's needs where automatic stack reloading may be required when modifications occur before locking.

The changes affect multiple layers:
- Updates function signatures in `reftable-stack.h` and `stack.c`
- Modifies all callers in `reftable-backend.c` to pass `0` as the flags value
- Updates numerous test cases in `t-reftable-stack.c` to include the new parameter

Key technical points:
- Maintains backward compatibility by defaulting to `0` flags in all existing callers
- The flag will enable `REFTABLE_STACK_NEW_ADDITION_RELOAD` functionality in libgit2
- Touches 4 files with 39 insertions and 36 deletions
- Follows the series' pattern of incremental interface improvements while preserving existing behavior

This is a straightforward plumbing change that extends the API without modifying current functionality, continuing the series' work to make the reftable implementation more robust and libgit2-compatible. The patch appears well-contained with comprehensive test coverage of the modified code paths.