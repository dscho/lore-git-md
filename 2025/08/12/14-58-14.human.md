Bugfix refinement for object name abbreviation. René Scharfe suggests a cleaner implementation for Junio Hamano's patch that fixes object name disambiguation in Git. The original patch addressed a regression where SHA-1 abbreviations were incorrectly limited to 20 characters, but René proposes simplifying the logic by separating the length increment check from the maximum length enforcement.

The key observation is that the current combined condition `if (mad->cur_len <= i && i < mad->max_len)` could be clearer as two separate checks: first incrementing the length if needed for disambiguation, then updating only if we found a longer match. This maintains the same behavior but makes the code's intent more obvious.

This is a minor but thoughtful improvement to an already-reviewed bugfix that had reached consensus on the technical approach. The suggestion doesn't change the patch's core functionality but could improve code readability. Given the thorough prior discussion and validation of the underlying issue, this refinement seems likely to be accepted if Junio agrees with the stylistic preference.