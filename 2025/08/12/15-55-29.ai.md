Maintainer approval for documentation fix. Junio Hamano rewrites the commit message for Knut Ryager's patch correcting nested bracket syntax in `git-clone`'s man page synopsis. The message now explicitly calls out the incorrect `[--filter=<filter>] [--also-filter-submodules]]` formatting (with the stray bracket after --filter) versus the correct `[--filter=<filter> [--also-filter-submodules]]` grouping, explaining that `--also-filter-submodules` depends on `--filter` being specified. 

The message traces the history: correct grouping in f05da2b4 (2022) when the submodule option was introduced, then incorrect reformatting in 76880f05 (2024). The actual patch remains a one-line removal of the stray bracket in Documentation/git-clone.adoc. Junio adds his sign-off, indicating the patch is ready for inclusion. This concludes the thread's discussion, which focused entirely on commit message clarity after the technical fix was agreed upon in v1.