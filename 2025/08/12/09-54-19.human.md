This is part 5/8 of Patrick Steinhardt's reftable/libgit2 compatibility series, focusing on adding flags support to the stack addition interface. The patch modifies `reftable_stack_add()` to accept a `flags` parameter that will be passed through to the underlying stack operations. While Git doesn't currently use these flags, they're needed for libgit2 integration to handle cases where the stack might be modified concurrently.

The changes are straightforward - adding the flags parameter through the call chain from the public API down to internal functions, and updating all callers to pass 0 for now. The patch includes updates to the reftable backend code, stack implementation, and test suite to accommodate the new parameter.

This appears to be a well-contained change that maintains backward compatibility while laying groundwork for future functionality. The patch follows the series' pattern of careful interface evolution, with all existing tests updated to use the new signature. As with previous patches in the series, the changes are clean and focused, with clear documentation updates explaining the new parameter's purpose.