Test infrastructure patch completing a 3-part series modernizing editor-related tests. This final patch focuses on environment isolation, ensuring test modifications don't leak between cases.

The patch systematically replaces direct environment variable manipulation with `test_env` and `test_config` helpers, and wraps test blocks in subshells to contain changes. Key changes include:
- Converting global `TERM` exports to per-test `test_env` usage
- Moving editor variable handling into subshells to prevent cross-test pollution
- Replacing direct `git config` calls with `test_config`
- Consolidating duplicate test loops into a single subshell-wrapped version
- Maintaining identical test coverage while improving isolation

This follows through on the series' goal of modernizing test practices while preserving functionality. The changes align with previous feedback about proper environment handling and complete the transition to modern test infrastructure patterns. No behavior changes are introduced - this is purely about making the tests more robust and maintainable.

The patch appears well-constructed, with careful attention to maintaining test fidelity while improving isolation. It completes the series' technical goals after the earlier patches addressed vi detection modernization and exec-path security. With all controversial elements removed from earlier versions, this should be ready for final review.