This is a follow-up to the IMAP sent-folder archiving feature series, specifically addressing Junio C Hamano's documentation formatting feedback from v4 review. The email confirms that the `--[no-]use-imap-only` flag documentation has been split into two separate entries (`--use-imap-only` and `--no-use-imap-only`) in v5, aligning with Git's documentation standards. 

The change is purely presentational - the functionality remains unchanged from v4 where the pure-IMAP mode was fully implemented. This completes the final documentation polish requested by Junio, marking the last remaining issue resolved in the series. The thread context shows all technical aspects were already addressed in previous iterations, with this being the last stylistic adjustment needed before merging.