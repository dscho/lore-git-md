This is a refactoring patch in Patrick Steinhardt's reftable/libgit2 compatibility series (v3 5/8). It modifies the `reftable_stack_add()` function in reftable-stack.[ch] to accept a new `flags` parameter, though the immediate motivation isn't fully explained in the commit message.

The change appears to be preparatory work for future functionality, particularly for libgit2 integration, where automatic stack reloading will be needed when modifications occur before locking. Justin Tobler notes in the reply that the new flag will actually be used later in this same series, suggesting this is more than just future-proofing.

The patch touches the core stack modification logic but doesn't yet change behavior - it simply passes the flags through the call chain from `reftable_stack_add()` down to `reftable_stack_init_addition()`. This maintains the series' focus on robust state management while keeping the door open for the upcoming concurrent write safety improvements.

Key technical details:
- Adds `flags` parameter to `reftable_stack_add()` interface
- Passes flags through to `reftable_stack_init_addition()`
- Currently unused but will be leveraged for `REFTABLE_STACK_NEW_ADDITION_RELOAD` in later patches
- Part of broader effort to standardize stack modification patterns

The change is small (just parameter additions) but strategically placed in the stack modification workflow, continuing the series' methodical approach to error handling and concurrent access safety.