Test infrastructure patch focusing on exec-path handling in editor tests. This is part 2 of a 3-patch series modernizing editor-related test infrastructure, following feedback that removed a controversial strvec API conversion from earlier versions.

The patch replaces direct `--exec-path` usage with `test_env PATH` manipulation in t7005-editor.sh. This change affects two test cases that verify editor behavior during commit amendments. The modification ensures test editors are found via PATH rather than through Git's exec-path mechanism, which better isolates the test environment and follows modern test practices.

Technical details show the exact change: each `git --exec-path=. commit --amend` becomes `test_env PATH="$PWD:$PATH" git commit --amend`. This maintains the same test coverage while using the preferred test_env helper for environment manipulation. The change aligns with the series' goal of improving test reliability and security by properly handling exec-path scenarios.

The patch appears straightforward and uncontroversial, implementing feedback from previous rounds about proper PATH handling. It preserves all existing functionality while adopting the test_env pattern that was discussed and agreed upon in the thread. No behavior changes are introduced - this is purely a test infrastructure improvement.

This follows patch 1's test modernization and precedes patch 3's environment cleansing work in the series. The change is small but meaningful, addressing one of the key technical points in the thread about proper exec-path handling in tests.