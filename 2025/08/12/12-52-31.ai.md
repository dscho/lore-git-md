This is a test fix and hardening patch that completes the implementation of the allowlist mechanism for builtins with missing documentation, following up on Junio's observation that the original submission lacked the actual diff content.

Patrick Steinhardt steps in while Toon Claes is unavailable, providing the missing implementation that reverses the original logic - now checking the allowlist first rather than checking for file existence. The patch adds two key components: the `adoc-missing` allowlist file containing 9 builtins that intentionally lack documentation, and the test logic that enforces documentation presence/absence based on the allowlist. The test now explicitly verifies that builtins either have documentation (when not in the allowlist) or are properly listed as exceptions (when they lack documentation).

The implementation uses grep to check the allowlist and sets test expectations accordingly, with separate test assertions for both cases. The allowlist includes internal/helper commands like `checkout--worker` and `submodule--helper` that don't need user-facing documentation. This completes the transition from silent skips to explicit verification that was proposed earlier in the thread, ensuring documentation coverage is properly tracked going forward.

The patch maintains the same test file (`t/t0450-txt-doc-vs-help.sh`) and adds the new allowlist file, with Patrick noting he had to reverse the grep condition from Toon's original proposal to make the logic work correctly. The changes are ready for review, with Patrick asking whether they should be squashed with the previous patch or submitted as a separate version.