This is the final version of Julia Evans' documentation series improving the git-rebase man page, now incorporating all review feedback. The series has been merged after addressing Junio Hamano's technical corrections and the post-merge discussion about command usage.

The v7 version makes three final tweaks to the internals section: clarifying that rebase uses patch-id (not direct SHA comparison) to detect duplicate commits, fixing some line wrapping, and correcting a `git checkout` flag from `-C` to `-B`. These changes complete the series' goal of making the rebase documentation more approachable through better examples, reduced duplication, and clearer explanations of internals.

The thread shows consensus on keeping `git checkout` rather than switching to `git switch` in examples, as checkout provides clearer behavior for detached HEAD states during rebase. The series represents a significant improvement in documentation structure while maintaining technical accuracy, with all open questions resolved through collaborative review.