This is a discussion response in the configurable signing formats thread. Brian m. carlson engages with Andrea Pappacoda's proposal to allow separate signing formats (SSH vs OpenPGP) for commits versus tags, while raising practical considerations.

The response acknowledges the current limitation while suggesting a temporary workaround using Git aliases. A key contribution is requesting clarification on the motivating use case, as this appears to be a novel request. The email thoughtfully examines two implementation challenges: backward compatibility (proposing new options should default to existing `gpg.format` values) and verification complexity (noting that verification occurs more frequently than signing and may require configuration awareness from all verifiers).

Technical points raised include the potential need for signature autodetection during verification and the configuration burden this feature might impose on downstream users. The response maintains a neutral stance on whether the feature should be implemented, focusing instead on identifying considerations that would need addressing in any solution. The thread continues to explore the viability of decoupling commit and tag signing formats while maintaining verification reliability.