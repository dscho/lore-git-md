Test fix follow-up completing the allowlist implementation for missing documentation. Patrick Steinhardt steps in to finalize Toon Claes' incomplete patch submission while Toon is unavailable. The change implements the proposed allowlist mechanism for builtins without documentation (`t0450/adoc-missing`), but with a key correction: it reverses the logic to check the allowlist first, then verify the documentation presence/absence matches expectations. 

The patch adds 9 builtins to the new allowlist file (including `submodule--helper` and various merge variants) and restructures the test to explicitly verify two conditions: that listed builtins truly lack documentation, and that unlisted ones have it. This replaces the previous silent-skip behavior that could mask real documentation gaps. The implementation maintains compatibility with both Makefile and Meson builds while making the test more rigorous.

The change appears well-considered, addressing Junio's concern about the incomplete submission while preserving the original intent. Patrick notes the logic reversal was necessary for correctness and offers to either have this squashed or sent as a formal resubmission. The patch completes the transition to explicit allowlisting that the thread has been working toward.