Documentation patch consolidating duplicate conflict resolution instructions in the git-rebase man page. This is part of Julia Evans' series to improve the rebase documentation's structure and clarity.

The patch combines two separate explanations of how to handle merge conflicts during rebase into a single, numbered list early in the document. The new version appears right after the initial rebase example, making this important information more discoverable. The content remains technically identical - it removes 29 lines of redundant explanation while preserving all the key instructions about `--continue`, `--abort`, and `--skip`.

This change follows the series' goal of reducing duplication and improving information flow. The patch is cleanly implemented with no behavioral changes, just documentation reorganization. It builds on the first patch in the series which added an early example comparing rebase vs merge. The thread context shows this series has already been merged after thorough review, with all technical corrections addressed.