This is a technical refinement in Patrick Steinhardt's reftable/libgit2 compatibility series, addressing compiler warning issues around initialization syntax. The patch responds to Carlo Arenas' suggestion to replace the `REFTABLE_ADDITION_INIT` macro with either static zero-initialization or explicit `memset()` calls in stack.c.

The change focuses on two key functions: `reftable_stack_new_addition()` now uses static zero-initialization for its `empty` struct, while `stack_try_add()` switches to explicit `memset()`. This resolves compiler warnings that occurred with both older C compilers and C++ builds, without requiring a broader shim layer solution as previously debated in the thread.

The patch maintains the series' goal of shared reftable code between Git and libgit2 while sidestepping the architectural debate about compatibility boundaries. It represents a pragmatic solution that doesn't commit to either Patrick's minimal localized fixes approach or Junio Hamano's proposed shim layer strategy, instead opting for direct initialization methods that work universally.

Key technical details: the removal of `REFTABLE_ADDITION_INIT` macro (4 lines), addition of `memset()` call (3 lines), and conversion to static initialization (1 line). The changes are contained within stack.c and don't affect the public API or other files in the reftable implementation.

This appears to be a well-considered middle ground that addresses the immediate technical issue while leaving room for future architectural decisions about how Git and libgit2 should handle such compatibility differences in their shared code.