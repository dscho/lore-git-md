This is a final refinement to the resolved object abbreviation bugfix thread. René Scharfe and Junio Hamano are discussing a small but meaningful simplification to the `extend_abbrev_len()` helper function that was introduced in the fix.

The key insight is that the function can be made simpler and more robust by relying solely on the NUL-terminated string property of `mad->hex` rather than tracking maximum lengths. René initially suggested splitting the length checks into two separate conditions, but Junio proposed an even simpler version that just checks for the NUL terminator and current length. 

René's follow-up confirms this approach correctly handles duplicate objects (where the same object might exist loose and packed) by intentionally not extending the abbreviation length when objects match exactly. This maintains the desired behavior while making the code more straightforward.

The discussion represents the final polish on a thread that already reached consensus on the core solution. This refinement doesn't change the behavior but improves code clarity by removing what Junio called "the max_len crap" while preserving all the carefully considered edge case handling from earlier in the thread.