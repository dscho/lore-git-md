Thread identity: Bugfix and cleanup for `git diff --no-index` mode, addressing working directory inconsistency when handling special paths like "-" (stdin/stdout). Patch series from Junio C Hamano with input from Gregoire Geis and Ramsay Jones.

Core problem: `git diff --no-index` incorrectly respects Git's working tree discovery despite being designed to operate outside repository context. The current implementation changes directory to the worktree root before diffing, which breaks special path handling.

Current status: v2 patch confirmed working by original reporter Gregoire Geis through both test suite verification and manual command testing. The follow-up patch simplifies the implementation by taking advantage of guaranteed NULL prefix conditions, removing now-unnecessary complexity while preserving all functionality.

Key technical details:
- Core fix checks `no_index && prefix` condition and uses chdir(prefix) to revert Git's working directory change
- Follow-up patch removes prefix_filename() calls and simplifies "-" path handling via direct string comparison
- Adds BUG() check to enforce NULL prefix invariant in no-index mode
- Touches `builtin/diff.c` and adds test in t/t4053-diff-no-index.sh
- Special case for "-" path handling preserved but implemented more directly
- Reported by Gregoire Geis, with help from Ramsay Jones on diagnosis
- Maintainer Junio C Hamano implemented both the fix and cleanup

Open questions: None - confirmation from original reporter and maintainer indicates the fix and cleanup are complete.

Participant positions:
- Junio C Hamano (maintainer) authored both the fix and cleanup patches
- Gregoire Geis (reporter) confirmed v2 patch resolves the issue through thorough testing
- Ramsay Jones provided helpful input on diagnosis

Related work: This is a standalone bugfix series, but touches behavior that could affect other commands using `--no-index` mode. The changes are well-contained and unlikely to impact other areas.