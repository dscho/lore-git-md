This is a review follow-up for the first in a 3-patch series modernizing editor-related test infrastructure. The patch converts t7005-editor.sh to modern test conventions, replacing direct shell output comparisons with `test_cmp` and cleaning up whitespace.

Phillip Wood provides minor suggestions to further improve the test quality: replacing a direct `if` check with `test_must_fail` for better error handling, and proposes using `test_commit_message` instead of just checking the subject line to verify editor behavior more thoroughly. He confirms the whitespace cleanups look good.

The changes discussed are minor refinements to an already-reviewed patch that modernizes test style in t7005-editor.sh. The core conversion work (using `test_cmp`, removing unnecessary blank lines) remains unchanged and approved. This email represents final polish before the v2 submission of this test modernization patch.