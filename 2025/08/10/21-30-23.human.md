Documentation fix for git-clone option syntax. This is version 2 of Knut Harald Ryager's patch correcting the nested bracket formatting for `--filter` and `--also-filter-submodules` options in the clone command synopsis. The change properly nests `--also-filter-submodules` within `--filter`'s brackets to reflect that submodule filtering requires regular filtering. 

The patch follows up on Junio Hamano's review of version 1, which noted the original fix removed the wrong bracket. The commit message has been updated to be more direct about the change ("Remove the extra and incorrect closing bracket") rather than describing it as a "stray bracket". 

This is a straightforward documentation-only change with no functional impact - it fixes how the option relationship appears in the man page. The patch is cleanly implemented with a single-line change to Documentation/git-clone.adoc. The author has also corrected their name consistency between commit author and sign-off between versions. 

The change maintains the historical context established in the thread - the original introduction of these options in commit f05da2b4 and subsequent reformatting in 76880f05. The patch appears ready for inclusion as a minor but correct documentation improvement.