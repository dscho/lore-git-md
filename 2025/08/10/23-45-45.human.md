This is part of the ongoing `the_repository` removal effort, specifically targeting the `merge_log_config` global variable used in merge operations. The patch localizes this variable within `cmd_fmt_merge_msg()` and `cmd_merge()` functions, passing it via callbacks instead of using the deprecated global.

The change maintains the existing behavior where `merge.summary` takes precedence over `merge.log` for determining shortlog length, while eliminating global state. The implementation follows the pattern of passing configuration through callback parameters rather than relying on global access.

The patch touches several files across the codebase, removing the global declaration and updating all relevant call sites. This appears to be a straightforward refactoring that doesn't introduce new behavior, just reorganizes how the configuration value flows through the system.

As part of a mentored GSoC project, this represents incremental progress in the larger `the_repository` removal effort. The changes look focused and mechanical, though reviewers will want to verify that all edge cases (particularly around config precedence) are properly preserved in the new implementation.