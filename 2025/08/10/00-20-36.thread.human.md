Junio C Hamano's bugfix for `git diff --no-index` mode has progressed from addressing the core working directory inconsistency to a follow-up simplification. The original fix, which corrected how the command handles special paths like "-" (stdin/stdout) outside repository contexts, was confirmed working by reporter Gregoire Geis and incorporated input from Ramsay Jones. 

The maintainer has now sent a second patch that streamlines the implementation, taking advantage of the guaranteed NULL prefix condition in `--no-index` mode. This removes unnecessary memory management and simplifies path comparison logic while preserving all functionality, including the special "-" handling. The changes are focused in diff-no-index.c and include a BUG() check to enforce the NULL prefix invariant. 

With both the original reporter and maintainer satisfied with the fix's effectiveness and this subsequent cleanup, the thread appears resolved with a robust solution that maintains backward compatibility while removing now-redundant code paths. No further discussion points remain open.