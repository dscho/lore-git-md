Bugfix discussion for `git-jump` filename handling with additional technical clarification. Phillip Wood provides a key insight that Git quotes filenames containing control characters (including tabs), which changes the analysis of edge cases in the current solution. This means the previously identified problematic cases (filenames ending with literal tabs) would actually be quoted in the diff output, making them distinguishable from the space-containing filenames that get trailing tabs appended.

The email notes that while `git-jump` likely doesn't currently handle quoted filenames, adding such support would be straightforward using Git.pm's unquoting functionality. This observation strengthens the case for the simpler regex-based solution (stripping trailing tabs) since the truly problematic edge cases would be protected by Git's quoting mechanism.

The discussion continues to weigh the tradeoffs between the current minimal fix (handling spaces with a simple regex) versus more comprehensive approaches like parsing "diff --git" lines or adding quoted filename support. Phillip's technical clarification about Git's quoting behavior provides important new context that makes the simple solution more robust than initially thought, as it effectively handles what were previously considered edge cases through the quoting mechanism.