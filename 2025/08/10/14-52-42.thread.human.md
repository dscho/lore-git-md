Jon Forrest reported a bug where `git cat-file` shows duplicate abbreviated hashes when encountering ambiguous object IDs in a corrupted repository (where two objects share the same prefix but have different full hashes). The initial report demonstrated this with a test case that manually created two blob objects with nearly identical hashes.

Patrick Steinhardt has now weighed in, noting this is an intentional repository corruption scenario. He points out the issue stems from `show_ambiguous_object()` and `repo_find_unique_abbrev()` getting confused when two objects hash to the same content despite appearing distinct. Patrick questions whether this edge case needs fixing since it requires manual repository corruption and would be caught by `git-fsck`, asking for clarification on whether this occurs naturally or if the behavior has changed in recent Git versions.

The discussion has shifted from bug reporting to analyzing whether this edge case warrants a fix, given it requires intentional repository manipulation. The technical details about the object disambiguation code path would be relevant if the discussion continues toward proposing a solution.