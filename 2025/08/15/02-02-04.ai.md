This is a follow-up in the resolved thread about Git's alias behavior with `git stash`. Christoph Anton Mitterer seeks clarification on whether there's a way to override Git's intentional prevention of aliases shadowing built-in commands like `stash`. 

The email references Junio Hamano's earlier explanation that Git ignores aliases that would hide existing commands, as documented in `git help config`. Mitterer questions whether his specific shell function alias (using the `!` prefix syntax) would actually hide other commands, suggesting he may not fully grasp the blanket prohibition on shadowing any built-in command regardless of implementation details.

The thread remains technically closed - the behavior is confirmed as intentional design - but this follow-up shows some lingering user desire for workarounds. The core technical points (alias shadowing being universally blocked for all built-ins, documented in git-config) remain unchanged. No new technical information is introduced in this message beyond the user's request for potential override mechanisms.