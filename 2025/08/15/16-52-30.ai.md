Documentation patch finalizing the technical internals section of git-rebase.

This is the final patch in Julia Evans' v8 series improving the git-rebase documentation. It focuses on clarifying the description of rebase's internal operation while removing redundant explanations. The changes:

1. Replace the previous detailed step-by-step with a clearer 4-step overview of the rebase process, emphasizing it as a multi-stage operation. The new description better matches the conceptual model while still being technically accurate.

2. Consolidate the explanation of how rebase skips equivalent commits into a single bullet point, removing the ASCII diagram that previously illustrated this. The text now treats this as just one of rebase's many complex behaviors rather than giving it special visual treatment.

3. Remove redundant details about `--fork-point` and `--root` that were duplicating content from the OPTIONS section.

4. Gather all `ORIG_HEAD` discussion into the existing NOTE section, making it clearer this is an implementation detail rather than core behavior.

The patch continues the series' trend of reducing line count (-43 lines) while improving clarity. It finalizes the technical internals section that had seen extensive discussion in earlier rounds, particularly around how to describe the commit selection process. The changes implement Junio Hamano's suggestion to combine commit enumeration and cherry-pick detection into a single conceptual step.

This completes the major restructuring work on the rebase documentation, with all key technical points from the thread now addressed. The result is documentation that better separates conceptual models from implementation details while maintaining accuracy.