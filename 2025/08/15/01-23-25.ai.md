This is a technical explanation from Junio C Hamano about Git's alias behavior, specifically addressing why shell function aliases for `git stash` don't work as expected. The response clarifies that this is intentional behavior - Git deliberately ignores aliases that would hide existing Git commands to prevent confusion and script breakage. 

The email provides two key pieces of documentation from `git help config`: first explaining that command-hiding aliases are ignored (which answers the original question about `stash`), and second clarifying that the `!` prefix indicates a shell command should be executed. 

This response settles the core question in the thread by pointing to explicit design decisions in Git's alias system. The explanation confirms this isn't a bug but rather a deliberate safeguard against command shadowing. The documentation references provide authoritative answers to both the specific `stash` case and the more general question about shell function syntax in aliases.

For future reference in this thread, the key takeaways are:
1. Git's config system explicitly ignores aliases that would shadow built-in commands
2. The `!` prefix in aliases indicates shell command processing
3. The `stash` alias behavior is working as designed, not a bug
4. Documentation for these behaviors exists in `git help config` under the alias.* section