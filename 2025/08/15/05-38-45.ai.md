Junio Hamano's response in the documentation terminology thread provides important context about Git's design philosophy regarding the index and snapshots. This is a follow-up to Julia Evans' observation about index snapshot behavior in the `git-add` documentation.

The email makes several key technical clarifications:
1. Explains why Git doesn't store multiple index snapshots (commits serve this purpose better)
2. Notes the exception for conflicted state (via the "resolve undo" feature)
3. Provides rationale for current design choices (commit-based workflow being lightweight)
4. Sets boundaries for further discussion (avoiding endless "why" questions without concrete proposals)

The response emphasizes Git's content-based model where commits are the primary mechanism for preserving states, with the index serving as a transient staging area. It references existing features (`git stash --keep`, conflict resolution undo) that demonstrate this philosophy in practice while acknowledging they're special cases with specific use cases.

This appears to be closing discussion on this particular design point while leaving open the possibility of future concrete proposals. The tone suggests this is maintainer guidance about Git's fundamental architecture rather than open-ended debate. The footnote provides additional technical context about the snapshot model's advantages.