This is a question about Git aliases and shell function behavior, specifically regarding the `git stash` command. Christoph Anton Mitterer reports that while they can create shell function aliases for most Git commands, an alias named "stash" doesn't seem to work as expected. They also ask several technical questions about how shell function aliases are executed in Git.

The email reveals an interesting edge case in Git's alias system - while most commands can be overridden with shell function aliases, `git stash` appears to behave differently. The user provides a working example of a shell function alias (using the `!c(){}; c` syntax) that works for other command names but fails when named "stash". 

Key technical points for future reference:
- The user's alias attempts to make `git stash` with no arguments behave like `git stash list`
- The `!` prefix in aliases indicates a shell command should be executed
- The email raises several implementation questions about shell function execution environment (IFS, function namespace pollution, environment sanitization)

This appears to be either a bug in Git's alias handling for built-in commands or possibly an undocumented restriction. The questions about shell execution environment suggest this could touch on security-sensitive areas of Git's implementation. The thread may need input from Git maintainers about whether this is expected behavior and what constraints exist around alias execution.