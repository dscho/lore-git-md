This patch continues the Rust integration effort by modifying xdiff`'s core data structures to use Rust-compatible types, specifically changing `xrecord_t` fields to use `u8`, `usize`, and `u64` instead of C's `char*`, `long`, and `unsigned long`. The changes align with earlier type definitions added for FFI interop and address a type mismatch in `xdlclass_t::ha` that Johannes Schindelin identified.

The patch introduces numerous casts throughout xdiff to maintain compatibility during the transition, particularly around string operations where `char*` must be cast to/from `u8*`. Key changes include updating the `xrecord_t` struct definition in `xtypes.h` and modifying pointer arithmetic in merge/diff calculations to account for the new types. While this creates temporary casting overhead in C code, the changes enable zero-copy interop with Rust through `std::slice::from_raw_parts()` as discussed in prior thread exchanges.

The modifications touch core xdiff functionality across multiple files (`xdiffi.c`, `xmerge.c`, `xprepare.c` etc.), primarily affecting line comparison and whitespace handling logic. Performance impact appears neutral since the changes preserve existing behavior while preparing for Rust integration. This patch represents a tactical step in the larger strategy of type alignment for FFI, though Phillip Wood has questioned whether this approach is preferable to handling conversions in Rust wrappers.