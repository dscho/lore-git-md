This is part 4/6 of Patrick Steinhardt's series to remove global state from the commit-graph subsystem, specifically targeting the `the_hash_algo` usage. The patch systematically replaces all instances of `the_hash_algo` with repository-specific hash algorithms (`r->hash_algo`), continuing the effort to make the commit-graph code repository-agnostic.

The changes are mechanical but thorough, touching hash calculations, file operations, and data structure handling throughout commit-graph.c. A few key transformations stand out: file size checks now use the repository's hash algorithm, chunk data width calculations are repository-aware, and hash writes use the context's algorithm rather than the global one. The patch also updates function signatures to explicitly pass hash algorithm parameters where needed.

This is a foundational change that builds on the previous patches in the series. While the diff is large, it follows a clear pattern of replacing global references with repository-specific ones. The changes appear well-contained and focused, with no obvious side effects beyond the intended removal of `the_hash_algo` dependencies. The patch maintains compatibility with existing functionality while advancing the larger goal of removing global state from the commit-graph subsystem.