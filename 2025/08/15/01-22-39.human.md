This is a refactoring patch in the Rust integration series, specifically cleaning up unused fields in xdiff's internal data structures. The patch removes three fields that were initialized but never actually used in the code:

1. `xrecord_t.next` (a linked list pointer)
2. `xdfile_t.hbits` (hash bits field)
3. `xdfile_t.rhash` (a hash table pointer)

The changes are straightforward deletions with no behavior changes, simplifying the xdiff internals by removing dead code. This appears to be preparatory work for the Rust integration effort, making the codebase cleaner before introducing new Rust components. The patch follows the series' pattern of incremental, verifiable changes to xdiff internals.

The removal is well-contained, touching only xdiff preparation code and type definitions. It passes all tests, suggesting these fields were truly vestigial. This is the kind of cleanup that's generally non-controversial, though reviewers may want to double-check that these fields were used by any external consumers of xdiff (the commit message doesn't mention checking for that).