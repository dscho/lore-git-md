Thread identity: Question about `git stash` shell function alias behavior, originating from Christoph Anton Mitterer. Thread type is technical inquiry about Git's alias system implementation details.

Core problem: Why shell function aliases (using `!c(){}; c` syntax) work for most Git commands but fail when named "stash", with the specific case of making `git stash` without arguments) behave like `git stash list`.

Current status: Initial report of the issue with a working example for other command names but failure when using "stash" as the alias name. No responses or resolutions yet.

Key technical details:
- Alias system behavior with `!` prefix for shell command execution
- Special case of `git stash` command interaction with aliases
- Shell function execution environment considerations (IFS, namespace pollution, environment sanitization)
- Potential security implications of alias execution

Open questions:
- Is this a bug or intended behavior for built-in commands?
- What constraints exist around alias execution in Git?
- How does Git handle shell function namespace for aliases?
- Are there special cases for commands with default subcommands (like `stash`)?

Participant positions:
- Christoph Anton Mitterer: Reported the issue and provided technical details of the behavior difference

Related work:
- Git's alias system documentation
- Shell function execution in Git commands
- Built-in command handling mechanisms