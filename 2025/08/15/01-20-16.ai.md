This is a discussion about test future-proofing in the `git repo info` series, specifically regarding reference backend formats. 

Junio C Hamano raises a concern about test future-proofing when Git might default to reftable backend in builds with WITH_BREAKING_CHANGES=YesPlease. Eric Sunshine clarifies that Lucas Oshiro had already addressed this by explicitly using `--ref-format=files` in test invocations, which ensures the tests will continue working regardless of future default backend changes. Junio acknowledges this solution, noting that explicitly specifying the files backend (which will always be available) properly future-proofs the tests.

The exchange confirms that the test strategy in patch 3/5 of the `git repo info` series is robust against future changes to default reference backends. This was the last open question about test future-proofing in the series, which has now been resolved with all mentor approvals in place. The discussion reinforces that the tests use both stable keys (`layout.*`) and explicit format specification approaches, with the latter providing the needed future-proofing.