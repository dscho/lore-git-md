This is a test design discussion in the ongoing `git repo info` command implementation series, specifically about patch 5/5 which adds the `--format` flag for output formatting.

Junio C Hamano points out two key observations about the test implementation:

1. Terminology correction: The email notes that "NUL characters" is the conventional spelling in Git's codebase (not "NULL characters"), as evidenced by documentation searches. This is a minor but important terminology fix.

2. Test structure critique: The main technical point questions why the patch creates two separate repositories (`repo_name_keyvalue` and `repo_name_nul`) to test the different output formats, when the format flag should only affect output formatting without requiring different repository states. This suggests the test helper could be simplified by testing both formats against the same repository state.

The email maintains an approving tone ("OK" for the core implementation) while identifying these two areas for potential improvement. This continues the thread's ongoing discussion about test design tradeoffs between thoroughness and simplicity, though now focused specifically on the format testing rather than the earlier broader test helper discussions.

The patch itself implements dual output formats (keyvalue for humans, NUL-terminated for scripts) following `git config`'s precedent, with comprehensive testing that may be more complex than necessary according to Junio's review.