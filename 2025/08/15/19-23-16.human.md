This is a feature patch in the `git repo info` command series, now in its 10th iteration and approved for merging. The patch adds a `--format` flag to control output formatting, supporting both human-readable key-value pairs and machine-readable NUL-terminated output.

Junio C Hamano provides a final review note, pointing out two minor issues: the documentation should consistently use "NUL" (not "NULL") when referring to null characters, and questions the test design that creates separate repositories for each output format when the format flag shouldn't affect repository state. The first is a straightforward documentation fix, while the second suggests the test helper could be simplified to avoid redundant repository setup.

The patch itself implements the dual output formats inspired by `git config`, with comprehensive tests verifying both formats work correctly. While the series is functionally complete and approved, this final feedback suggests potential minor cleanup before merging, though it doesn't block the overall approach. The discussion represents the final polish on a well-architected feature that's part of a larger effort to migrate repository metadata access from `git rev-parse` to a more structured command hierarchy.