CI/build system patch in the Rust integration series (13/17) that ensures Cargo.lock files are preserved as build artifacts in GitHub Actions workflows. This allows auditing of Rust dependency versions used during CI builds, addressing a supply chain security concern raised earlier in the thread.

The patch adds artifact upload steps for Cargo.lock in four workflow jobs: Windows builds (both Makefile and Meson), Linux test runs, and static analysis. Each upload uses a distinct name pattern matching the job context. This is a straightforward mechanical change that implements the thread's earlier consensus about tracking dependencies while keeping Cargo.lock out of the main repository.

As part of the larger Rust integration effort, this patch helps maintain reproducibility and auditability of builds, though it doesn't affect functionality or performance. The change is well-contained and follows the pattern established by existing artifact upload steps in the workflow file.