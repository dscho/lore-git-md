Updated AI thread summary:

Performance optimization for sparse index handling in `git ls-files` (builtin/ls-files.c), now queued for inclusion after thorough review. The patch series builds on sparse index infrastructure (commit 78087097b8) to modify sparse index expansion behavior, triggering only when encountering a sparse directory that matches the pathspec rather than doing full expansion upfront. This avoids unnecessary index operations when pathspecs match directories covered by sparse-checkout patterns.

Key technical changes:
- Moves `ensure_full_index()` inside the file iteration loop in `builtin/ls-files.c`
- Only expands for sparse directories (`S_ISSPARSEDIR`) without `--sparse`
- Preserves index sorting invariants during mid-iteration expansion
- Maintains existing behavior while optimizing common cases

The optimization was initially targeted at `ls-files` but has demonstrated broader applicability, particularly improving real-world `git mergetool` workflows where pathspec-triggered index expansion caused slowdowns. Derrick Stolee originally proposed this optimization and confirmed it addresses concrete performance pain points.

Elijah Newren's review clarified two key aspects:
1. The performance benefit comes from `prune_index()` filtering index entries by pathspec prefix before iteration begins, not just for single pathspecs
2. The approach is safe as earlier index entries remain stable during mid-iteration expansion

Junio Hamano verified three critical index ordering assumptions:
1. Index entries remain sorted throughout
2. Expanding a sparse directory won't cause its contents to sort before their original position
3. No need to restart iteration since only the current entry is processed

Test coverage in t1092 verifies:
- Correctness for pathspecs hitting sparse directories mid-iteration
- Pathspecs filtering to sparse directories
- Preservation of existing behavior while optimizing common cases

The patch is now ready for inclusion with all review feedback addressed, including commit message refinements to better explain `prune_index()`'s role and the index sorting guarantees. The additional `mergetool` use case strengthens the justification for inclusion, showing the change solves real performance issues beyond the original `ls-files` context. The thorough technical review from both domain experts (Stolee, Newren) and the maintainer (Hamano) confirms the optimization is well-considered and safe.