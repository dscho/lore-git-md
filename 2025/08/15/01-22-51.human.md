This is a performance optimization patch in the ongoing Rust xdiff series, specifically targeting the `--ignore-cr-at-eol` whitespace flag case. The patch adds special handling when this is the only whitespace flag set, avoiding the slower general whitespace iterator path.

The change makes two key optimizations:
1. In `WhitespaceIter`, it handles the CR-at-EOL case directly with simple length checks rather than going through the full iterator logic
2. In `line_equal`, it adds a fast path comparison that checks line endings without full whitespace processing

The commit message explains this is a tactical optimization while waiting for broader Rust/C interop improvements that would enable more comprehensive whitespace handling optimizations. The current approach prioritizes hashing performance over equality checking, with plans to revisit other whitespace flags later.

This patch appears well-contained and focused, building on the established Rust xdiff infrastructure from earlier in the series. The optimization seems sensible given the simple nature of CR-at-EOL handling compared to other whitespace rules. The commit message clearly states the tradeoffs and future plans.