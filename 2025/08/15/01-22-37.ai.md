This is the foundational infrastructure patch in the Rust integration series, establishing the build system support and type definitions needed for subsequent xdiff optimizations. The patch introduces:

1. **Build system integration** for Rust via both Makefile and meson, with debug/release configurations and appropriate compiler warnings disabled (`-Aunused_imports -Adead_code`). The Rust code builds as a static library that Git links against.

2. **Type definitions** in `git-compat-util.h` mapping C types to Rust primitives (u8, u32, usize etc.), which will be critical for later FFI interop. This includes both integer types and platform-specific size types (usize/isize).

3. **Rust workspace structure** with two initial crates:
   - `xdiff` (empty in this patch) for the core diff algorithm work
   - `interop` (also empty) for future FFI infrastructure

Key technical decisions visible here that will shape later discussion:
- The choice to build Rust separately via cargo rather than fully integrating with make/meson
- The type alignment strategy (C-side typedefs matching Rust primitives)
- The warning suppressions that Phillip Wood later questions in the thread
- The multi-crate approach for modularity

The patch is purely additive infrastructure with no functional changes yet. It follows the series' established pattern of type alignment for zero-copy interop, though Phillip Wood's later comments will challenge whether this is necessary. The build system changes are comprehensive, covering both make and meson, and handle debug/release configurations appropriately. The empty crate structure suggests this is truly foundational work where the algorithmic changes will come in later patches.