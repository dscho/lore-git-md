Bugfix follow-up to the `git diff --no-index` directory handling fix. This is a simplification patch from Junio C Hamano that takes advantage of the now-guaranteed NULL prefix condition to clean up the code.

The patch removes now-unnecessary path prefixing logic and memory management since the previous fix ensures `diff_no_index()` always operates with a NULL prefix. It simplifies special case handling for "-" (stdin) by doing direct string comparisons rather than pointer comparisons against a constant string. The changes are focused entirely on `diff-no-index.c` and maintain all existing functionality while making the code more straightforward.

This appears to be a straightforward cleanup with no behavior changes, building on the recently confirmed fix for working directory handling in `--no-index` mode. The patch removes about 30 lines while adding 14, primarily by eliminating now-redundant path manipulation code. Since this follows up on a fix that was already tested and confirmed working, it seems low-risk and ready for inclusion.