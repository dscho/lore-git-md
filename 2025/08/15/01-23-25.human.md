This is a clarification about Git's alias behavior, specifically regarding why shell function aliases can't override built-in commands like `git stash`. Junio C Hamano responds to Christoph Anton Mitterer's inquiry by pointing to existing documentation that explains this is intentional behavior.

The response confirms that Git deliberately ignores aliases that would hide existing Git commands, which explains why the `stash` alias in the example doesn't work while similar aliases for non-built-in commands do. Junio also clarifies that the exclamation mark prefix indicates the alias should be treated as a shell command, referencing the relevant documentation sections.

The exchange highlights a documented but perhaps not widely known limitation of Git's alias system - it prevents users from accidentally or intentionally shadowing core Git commands with custom implementations. This maintains consistency in behavior and prevents potential script breakage that could occur if core commands were unexpectedly replaced by aliases.

For users wanting custom `git stash` behavior, they'll need to use a different alias name or find another approach that doesn't involve overriding the built-in command. The response is concise and authoritative, directly quoting from Git's own documentation to explain both the alias shadowing behavior and the shell command syntax.