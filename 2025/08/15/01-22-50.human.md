This is part of the ongoing RFC series proposing Rust as a hard dependency with xdiff optimizations, now at version 15/17. The patch introduces two new Rust functions that leverage the whitespace iterator added in previous patches: `line_hash()` and `line_equal()`. These functions handle line hashing and comparison while respecting whitespace flags, falling back to direct operations when no whitespace flags are set.

The implementation shows careful attention to the existing xdiff interface, with `xdl_line_hash` and `xdl_line_equal` serving as the FFI-compatible entry points. The hashing uses XXH3 (via the `xxhash-rust` crate) for both direct and whitespace-aware modes, maintaining consistency with the performance optimizations discussed earlier in the thread. The equality comparison uses the previously introduced `chunked_iter_equal` utility when whitespace flags are active.

This appears to be a straightforward technical implementation that builds on the foundations established earlier in the series. The patch doesn't introduce new architectural questions but rather implements the logical next step in the whitespace-handling optimization path. Performance impact remains unmeasured, as noted in the thread's open items. The code follows the established pattern of type-safe Rust wrappers around the C-compatible interfaces, continuing the series' focus on zero-copy interop where possible.