Junio Hamano responds to Phillip Wood's proposed `--unstaged` option for `git stash pop`, questioning whether the behavior should be enabled by default rather than requiring a new option. He suggests the original `--keep-index` design assumed users wouldn't modify working tree files while testing staged changes, implying the current behavior may suffice for that use case.

The email continues the thread's exploration of improving stash workflows during interactive rebases, specifically around separating unstaged changes. Hamano's skepticism about needing a new flag contrasts with Wood's script-based approach that demonstrated technical feasibility. His comment hints at potential design philosophy differences - whether to expand stash's capabilities or maintain its original focused purpose.

Key technical elements referenced include the stash commit structure (with `stash_oid^1` and `stash_oid^2` components), the `git merge-tree` three-way merge approach used in Wood's script, and the temporary index file handling (`$GIT_DIR/stash-pop-index`). The discussion remains focused on backward compatibility implications and whether the new behavior should be opt-in or default.