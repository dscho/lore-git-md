This is a feature proposal for adding JSON output to `git log`, now with design feedback from Junio C Hamano. The thread has been actively discussing technical challenges around character encoding and format choices, with recent expert input on handling non-UTF-8 data.

Junio raises important scope questions about the proposal. He asks whether the JSON output should be limited to commit metadata (similar to `git cat-file commit` content) or also include diff-related information like `--stat` or `-p` output. He notes that sticking to commit metadata would avoid complex design questions, while hinting that diff output would introduce additional challenges.

The email specifically calls out several commit header elements that would need special handling in JSON output: "encoding", "gpgsig", and "mergetag". These are important cases that weren't addressed in the initial proposal. Junio's feedback suggests the design needs to either explicitly exclude these cases or define clear handling rules for them.

This continues the thread's trend of refining the technical scope and implementation details. While the core idea of JSON output remains appealing, each round of discussion surfaces new edge cases and design decisions that need resolution before the feature could be implemented. The maintainer's questions suggest the proposal may need to either narrow its scope or provide more comprehensive design documentation.