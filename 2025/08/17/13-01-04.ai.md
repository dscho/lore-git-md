This is a follow-up to the completed 9-patch series on submodule gitdir path encoding. Adrian Ratiu reports test failures in the GitHub CI pipeline on Windows and macOS that will be addressed in v2. The email serves as a status update while leaving v1 available for review feedback.

Key technical context from the series remains unchanged: the implementation uses URL-style encoding (RFC 3986) with case encoding for filesystem compatibility, handling special cases like slashes, percent signs, case conflicts, and Windows reserved names. The complete series includes comprehensive test coverage across 24 files, with path construction following a three-step process (config override -> legacy path check -> new encoded path generation).

The current CI failures (platform-specific but not yet detailed in this email) represent the only known outstanding issues before the series can be considered ready. The author maintains the same technical approach from v1 series while preparing fixes for the test failures. No new technical discussion is introduced in this email - it primarily serves to acknowledge the CI issues and keep the review process moving.