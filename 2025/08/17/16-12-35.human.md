This is a CMake build system patch that got lost in the mail traffic. It's a follow-up to earlier changes that migrated reftable tests from shell scripts to C unit tests. The patch adjusts the CMake configuration to properly handle the new test organization, using wildcard pattern matching for test discovery instead of the old `UNIT_TEST_PROGRAMS` variable.

The patch had been submitted before the v2.51.0-rc0 release but wasn't noticed until Johannes Schindelin followed up about it. Junio Hamano confirms he'll apply it directly to master, indicating this is a straightforward and uncontroversial fix that maintains build system parity between Makefile and CMake configurations.

The change is minimal (just 4 lines) and aligns CMake with the Makefile's approach to test discovery. It's part of the ongoing work to support the reftable backend's test infrastructure, with Johannes Schindelin's involvement suggesting particular importance for Windows builds. No further discussion is needed - this is a simple mechanical fix that was just overlooked in the release cycle.