Junio Hamano responds to Phillip Wood's proposed `git stash --only-unstaged` script with skepticism about whether the new behavior should be the default. He questions if the original `--keep-index` designers anticipated users modifying working tree files while testing staged changes, suggesting the current behavior might be sufficient for that narrower use case.

The email maintains a cautious tone, implying the script's approach - while technically sound - may not warrant changing core stash behavior. Hamano's "Hmph" reaction suggests he sees this as solving a problem outside the original design scope of `--keep-index`. His comment leaves open whether the feature should be optional (via a new flag) or omitted entirely rather than changing defaults.

This continues the thread's central debate about whether to expand stash's capabilities for interactive rebase workflows versus maintaining its current behavior. Hamano's perspective as Git's maintainer carries weight in determining whether this will progress as a built-in feature or remain a userland script solution.