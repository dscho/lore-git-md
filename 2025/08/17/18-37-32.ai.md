This is a documentation discussion thread continuation regarding the `git-add` man page improvements, specifically about how to describe the relationship between `git add` and `git commit`.

Jean-Noël Avila responds to Junio Hamano's technical clarification about `git commit <pathspec>` behavior, agreeing it's an advanced feature but arguing it shouldn't be introduced too early in the `git-add` documentation. The key points are:

1. Jean-Noël acknowledges Junio's explanation of `git commit <pathspec>` and `git commit -i` behaviors but suggests these are advanced features that shouldn't be part of initial documentation for beginners.

2. He argues the current "by default" phrasing about `git commit` behavior in the `git-add` documentation creates confusion without proper context, preferring to keep the initial explanation simple and unambiguous.

3. The discussion reveals a tension between completeness (including all possible workflows) and beginner-friendliness (presenting a clear, simple mental model first).

4. Jean-Noël suggests relegating discussion of advanced commit workflows to later sections or to the section 7 manual pages that cover workflows more broadly.

5. Both agree some mention of `git commit` belongs in the `git-add` documentation since the commands are fundamentally related, but differ on how and when to introduce the more complex interactions.

The exchange shows the careful balancing act required in documentation between technical accuracy, completeness, and pedagogical effectiveness, particularly when dealing with Git's sometimes unintuitive behaviors. The thread appears to be converging toward a solution that introduces core concepts cleanly first, with advanced features covered later.