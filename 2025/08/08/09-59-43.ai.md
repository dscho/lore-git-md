Test hardening for documentation verification in t0450. This is the second patch in a series that transitions from silently skipping builtins without documentation to an explicit allowlist approach. The patch introduces a new file `t0450/adoc-missing` that serves as an allowlist for builtins that intentionally lack documentation. The test now fails if a builtin is missing documentation but not listed in the allowlist, or has documentation but is listed in the allowlist.

The implementation replaces the previous prereq-based skipping mechanism with a more rigorous check that verifies the allowlist's accuracy. When a builtin is listed in `adoc-missing`, the test expects no corresponding `.adoc` file to exist, and vice versa. This change makes documentation gaps a conscious decision rather than an accidental omission, helping catch documentation issues for new subcommands.

Key technical elements include the new allowlist file and the modified test logic in `t/t0450-txt-doc-vs-help.sh` that enforces the documentation presence/absence based on the allowlist. The patch builds on the thread's consensus that silent skips were problematic and follows Patrick Steinhardt's suggestion for an allowlist approach. This represents the culmination of the thread's progression from a simple build fix to improved test design for documentation verification.