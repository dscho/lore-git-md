Documentation patch consolidating merge conflict resolution instructions in `git-rebase` man page. This is part 2 of Julia Evans' 5-part series reorganizing the rebase documentation for better accessibility.

The patch removes duplicate explanations of how to handle merge conflicts during rebase, combining them into a single clear section near the top of the document. The unified version keeps all the essential information (resolving with `git add`, continuing with `--continue`, aborting with `--abort`, or skipping with `--skip`) while eliminating redundancy. The changes are purely organizational - no technical content is added or removed, just reordered for better flow.

This follows the series' pattern of streamlining the documentation by removing duplicated explanations. The patch maintains the same level of detail while making the information easier to find. As with the rest of the series, it's a well-executed documentation improvement that has already been merged after positive feedback from maintainers.