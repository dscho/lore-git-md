Here's the human-readable summary of the thread's current state:

This thread began with Julia Evans' series to make the git-rebase documentation more approachable through structural improvements. The initial work restructured content, removed duplication, and added clearer examples - including ASCII-art graphs to explain --onto. After merging, discussion shifted to deeper questions about command recommendations and documentation style.

A key debate emerged around whether examples should use `git checkout` or the newer `git switch`. Junio Hamano established that `checkout` is technically preferable here because it handles both branch and detached HEAD cases naturally, while `switch` requires explicit `--detach` for commit-ish arguments. This decision reflects Git's principle that documentation should maintain internal command consistency based on technical behavior rather than surface-level preferences.

Phillip Wood recently contributed performance context about `--reapply-cherry-picks` in partial clones, sparking new discussion about how much implementation detail belongs in user-facing docs. Julia agrees some niche options could be removed for consistency but argues that keeping the `git log` explanation helps users understand rebase's performance characteristics. She's seeking confirmation whether her understanding of the two-step process (fast `git log` vs slow `git patch-id`) is accurate.

The thread shows how post-merge discussions can deepen a feature's documentation by balancing technical accuracy with approachability. While consensus remains that `checkout` is correct choice for examples, the team continues refining how much implementation detail to include - particularly around performance-sensitive behaviors that might help users optimize their workflows.