Here's a human-readable summary of the thread:

The thread began with Julia Evans' series improving the git-rebase documentation, which was merged after several rounds of review. The changes made the man page more approachable by front-loading examples, consolidating duplicate content, and reorganizing sections like the --onto explanation. 

After merging, a discussion emerged about whether to use `git checkout` or the newer `git switch` in the examples. Junio Hamano and others explained that while `git switch` was intended to improve upon `git checkout`, it hasn't fully delivered on that promise - particularly for detached HEAD operations, where `git checkout` remains more straightforward. 

D. Ben Knoble recently reinforced this position, noting that since rebase targets can be any commit-ish (not just branch names), `git checkout` handles these cases more cleanly than `git switch` (which requires explicit `--detach` for non-branch arguments). This technical nuance explains why the documentation keeps the older command in these examples, despite the general preference for `git switch` elsewhere.

The discussion serves as useful context for future documentation work, showing how implementation details can override the simple "newer is better" assumption when choosing which commands to recommend. All open questions are now resolved, with the merged documentation and this follow-up discussion providing a complete picture of the decisions made.