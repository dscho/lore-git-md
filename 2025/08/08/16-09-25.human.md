This is the final confirmation in a thread about fixing a heap overflow in `git diff --no-index` when comparing stdin in subdirectories. Junio Hamano follows up on Gregoire Geis's verification that the architectural fix works in practice. 

The original issue involved path normalization problems when using "-" (stdin) in subdirectories, which could lead to buffer overflows. The solution evolved from a direct fix in `diff.c` to a more fundamental correction that properly separates `--no-index` mode from repository assumptions by clearing Git's startup prefix during operation.

Gregoire confirms the patch works in their environment, though Junio clarifies he was specifically asking for this verification since he hadn't tested it himself. With this confirmation, the thread appears resolved - the memory safety issue is fixed while also improving Git's architecture. The solution maintains the expected stdin output format and adds test coverage, with no outstanding technical concerns remaining.