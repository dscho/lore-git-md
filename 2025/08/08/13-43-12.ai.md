This is a documentation patch fixing a formatting issue in the Git manual. The patch corrects a mistake in commit b27be108c89 where the documentation for `log.decorate` config options was incorrectly formatted as a literal block (using `---`) instead of a description list (using `--`). The content remains the same - it still describes the `short`, `full`, and `auto` options for decorating commit refs in log output - but now uses the intended AsciiDoc syntax.

The change is straightforward and clearly motivated by the commit message, which references the earlier conversion effort. This appears to be a simple follow-up fix to properly complete the documentation modernization that was started in July 2025. The patch touches only the relevant config documentation file with minimal changes (2 lines modified).

For future reference in case this thread continues: the patch modifies Documentation/config/log.adoc to change the block delimiters around the `log.decorate` option descriptions from `---` to `--`, maintaining all other content including the descriptions of the `short`, `full`, and `auto` decoration modes and their behaviors. The change aligns with Git's ongoing effort to modernize its documentation format.