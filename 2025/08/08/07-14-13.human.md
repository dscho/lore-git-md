Follow-up to a completed refactoring patch in the push error handling series.

Denton Liu acknowledges a minor oversight in the recently merged patch that refactored an if-else chain into a switch statement for push error handling in remote.c. Patrick Steinhardt pointed out that the `type` variable couldn't be removed yet as originally intended, since it's still needed for a BUG() call that will later be converted to advise(). 

This is a quick acknowledgment of the issue with no technical changes - just a note that another patchset will follow to properly clean up the variable once the BUG() is converted. The thread had already concluded with all patches approved and merged, so this is post-merge housekeeping for a minor implementation detail that doesn't affect functionality.