This is the second patch in Julia Evans' documentation series restructuring the `git-rebase` man page. The focus here is on deduplicating the merge conflict resolution instructions, which were previously explained in two separate sections.

The patch combines the two existing explanations into one clear section placed right after the initial rebase example. The new version keeps all the essential information - how to resolve conflicts with `git add`, how to continue with `--continue`, abort with `--abort`, or skip with `--skip` - but presents it just once in a more logical location. The changes remove 29 lines of redundant text while preserving all the technical content.

This continues the series' goal of making the documentation more accessible by eliminating duplication and improving information flow. The patch appears well-executed, with the merge conflict instructions now appearing where users are most likely to need them (right after seeing the basic rebase example). As with the first patch, this is purely a documentation improvement with no behavior changes.

The series is shaping up as a thoughtful reorganization that maintains technical accuracy while making the content more approachable for beginners. This patch in particular addresses a clear pain point where users might have previously needed to hunt through the document to find conflict resolution instructions.