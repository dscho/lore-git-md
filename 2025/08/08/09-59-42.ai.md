Test fix addressing Meson build compatibility in t0450 documentation verification. This is the first patch in a two-part series that transitions from silent prereq skips to explicit allowlisting for missing documentation cases.

The patch makes a straightforward change to the test script `t/t0450-txt-doc-vs-help.sh`, replacing `$GIT_BUILD_DIR` with `$GIT_SOURCE_DIR` when locating documentation source files. This fixes the test for out-of-tree builds (particularly Meson builds) where build artifacts and source files reside in different directories. The change affects the `builtin_to_adoc` helper function that maps builtin commands to their corresponding documentation files.

The modification is minimal (a single-line change) and focused on build system compatibility, following the thread's established direction. As noted in the thread context, this was the original issue that revealed the deeper problem with the test's prereq mechanism, which the second patch in the series will overhaul. The change maintains the test's core functionality while making it work correctly in more build configurations.