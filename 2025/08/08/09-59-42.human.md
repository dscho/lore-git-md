This is a test fix for out-of-tree builds, specifically addressing Meson compatibility. The patch modifies test t0450-txt-doc-vs-help.sh to correctly locate documentation sources by using `$GIT_SOURCE_DIR` instead of `$GIT_BUILD_DIR`. This change ensures the test works properly when building Git with Meson, where build artifacts are placed in a separate directory from the source.

The fix is straightforward - a one-line change to the `builtin_to_adoc` helper function that points to the correct documentation path. This is part of a two-patch series that originated from identifying a deeper issue with how the test handles missing documentation. While this first patch is simple, it enables the more substantial follow-up change in patch 2 that improves the documentation verification mechanism.

The change appears well-justified and uncontroversial, addressing a clear technical requirement for Meson build compatibility. It maintains the test's original purpose of verifying consistency between command-line help and documentation while fixing the path resolution.