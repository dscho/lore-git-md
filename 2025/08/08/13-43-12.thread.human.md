This thread concerns a straightforward documentation fix for the Git manual. The patch corrects a formatting issue in the `log.decorate` config option documentation, where the description of the `short`, `full`, and `auto` decoration modes was incorrectly marked up as a literal block (`---`) rather than a description list (`--`). The content remains unchanged - it still accurately describes how each decoration mode affects commit ref display in log output.

The change is part of Git's ongoing documentation modernization effort to standardize on AsciiDoc syntax. The patch modifies just two lines in Documentation/config/log.adoc, fixing the markup while preserving all the existing descriptive text about the decoration options. This appears to be a clean follow-up to earlier documentation conversion work from July 2025.