This is a preparatory refactoring patch in Patrick Steinhardt's `git-history` RFC series, focusing on the `add-patch` subsystem. The patch adds support for in-memory index patching, a capability that will be used by subsequent commits in the series.

The patch introduces a new function `run_add_p_index()` that allows applying changes to an arbitrary in-memory index rather than always operating on the repository's main index. This is achieved by adding new `index` and `index_file` fields to `struct add_p_state` and modifying the existing patch application code to use these fields instead of always going through `s->r->index`. The function maintains the same interface and behavior as `run_add_p()` but with the added flexibility of working with different index states.

Key technical details include:
- New `index` and `index_file` fields in `struct add_p_state`
- Modified index handling in `patch_update_file()` to use the specified index
- Careful handling of index refresh operations when working with the main repository index
- New `run_add_p_index()` function with same basic structure as `run_add_p()` but accepting explicit index parameters
- Updated header file with new function declaration

The changes are mechanical and maintain existing functionality while adding the new capability. The patch appears well-structured and follows the incremental refactoring approach seen in previous preparatory work in this series. The commit message clearly states this is infrastructure for future use in the series, though it doesn't specify exactly which upcoming patch will utilize this new capability.