This is a refactoring patch in Patrick Steinhardt's RFC series for the new `git-history` command. It continues the preparatory work by decoupling the "add -p" (interactive patch) subsystem from the "add -i" (interactive add) infrastructure.

The patch removes `add-patch.c`'s dependency on `add-interactive.h` by directly embedding the interactive configuration (`struct interactive_config`) in `add_p_state` rather than inheriting it via `add_i_state`. This follows the previous patch that split out the interactive configuration into its own shared structure.

The changes are mechanical but thorough - all references to `s->s.cfg` are updated to `s->cfg`, and initialization/cleanup is adjusted accordingly. The patch maintains identical behavior while improving code organization, showing careful attention to detail with 68 lines changed (36 insertions, 32 deletions).

This continues the series' pattern of careful infrastructure preparation before introducing new functionality. The change is well-contained and appears to be a straightforward improvement, though as part of an RFC series, it will likely receive scrutiny about whether this is the right long-term direction for the interactive patch code.