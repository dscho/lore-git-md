This is a refactoring patch in Patrick Steinhardt's RFC series introducing the `git-history` command. The patch focuses on cleaning up the interaction between `add-interactive` and `add-patch` subsystems by introducing a new `struct interactive_options` to better organize shared configuration.

The key changes include:
- Splitting out common interactive mode options from `struct add_p_opt` into a new `struct interactive_options`
- Moving color and diff configuration handling into a new `struct interactive_config`
- Updating all callers (across `add`, `checkout`, `commit`, `reset`, and `stash` commands) to use the new structure
- Renaming related functions and variables to reflect the new structure
- Maintaining all existing functionality while improving code organization

The patch is part of a larger effort to build history editing operations inspired by Jujutsu (jj) while using Git's sequencer infrastructure. This particular change doesn't introduce new functionality but cleans up the underlying code to make future development easier. The changes are mechanical but touch many files, showing the wide impact of the interactive patch mode infrastructure.

The patch appears well-structured with clear commit message explaining the motivation and scope. It follows the series' pattern of careful infrastructure improvements while maintaining functionality. The changes are focused on code organization rather than behavior changes, making it a relatively low-risk refactor in the context of the larger RFC series.