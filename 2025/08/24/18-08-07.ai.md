Feature proposal for the `git-history` command's `reword` subcommand, with usability feedback from Kristoffer Haugsbakk. This email focuses on an edge case in the `reword` implementation that surfaces when a user provides an invalid commit reference.

The reporter notes that when mistyping a commit reference (using `./s2` instead of a valid revision), the error message incorrectly references "split" rather than "reword" operations. This suggests the error handling code may be shared between subcommands without proper contextualization. The error message's wording implies implementation details are leaking through the user interface.

This feedback builds on Haugsbakk's earlier positive review of the `split` subcommand's design, now testing the boundaries of the `reword` implementation. The issue highlights the importance of consistent error messaging across subcommands in the `git-history` suite. While minor, such polish items matter for user experience in a tool designed to simplify history editing workflows.

The thread context shows this is part of an extensive RFC series (now at v3) introducing multiple history-editing subcommands (`drop`, `reorder`, `split`, `reword`) with management commands. Previous discussion has focused on balancing simplicity versus flexibility, particularly around interactive workflows and stateful sessions. This new observation about error messaging adds another dimension to the usability considerations under review.

Key details for future reference:
- Error message says "split" when failing in `reword` context
- Invalid commit reference format tested: `./s2`
- Related to shared error handling infrastructure between subcommands
- Connects to ongoing discussion about subcommand consistency

The feedback doesn't challenge the core functionality but identifies a polish issue that could confuse users. It reinforces the thread's theme of refining the user experience as this new command family takes shape.