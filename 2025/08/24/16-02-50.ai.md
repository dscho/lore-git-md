Feature implementation for the `git-history split` subcommand, part of the ongoing RFC series introducing new history editing operations. This email exchange focuses on documentation refinements for the `split` subcommand's synopsis.

Patrick Steinhardt responds to Jean-Noël Avila's review of the `split` subcommand documentation, acknowledging two issues: missing backticks around command elements in the inconsistent ordering of options versus the `<revision>` parameter in the synopsis. While confirming the current ordering is technically valid, Steinhardt agrees it wasn't intentional and commits to fixing it in the next version.

This continues the pattern seen throughout the thread where Avila provides meticulous documentation feedback (previously seen with the `drop` subcommand) and Steinhardt promptly addresses the issues. The exchange demonstrates the ongoing refinement of command syntax presentation, particularly around parameter ordering conventions and markup consistency - key concerns for a new command's user-facing interface.

Technical details relevant for future discussion include: the `--message` option for custom commit messages during splitting, pathspec support for limiting scope, and the relationship between the `<revision>` parameter and option ordering in command synopses. The thread shows maintainers paying close attention to documentation quality even while core functionality is still being implemented.