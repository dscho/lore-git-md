This is a follow-up discussion about the proposed `git-history` command series, focusing on hook integration. Kristoffer Haugsbakk suggests that the new command should trigger the `post-rewrite` hook, particularly for the `split` subcommand, to maintain consistency with Git's existing rewrite operations.

The suggestion specifically addresses how `git-history split` (which creates a one-to-many commit relationship) would complement the existing hook coverage that handles one-to-one rewrites (like rebase) and many-to-one operations (like squash). This would provide a more complete solution for tools that rely on rewrite notifications.

The proposal appears well-reasoned and aligns with Git's existing hook architecture. It's a focused suggestion that builds on the ongoing discussion about the command's behavior and integration points, without challenging the core design decisions that have already been vetted in the thread. The maintainers seem likely to consider this a straightforward and beneficial addition to the series.