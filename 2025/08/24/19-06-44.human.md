This is the final patch in a refactoring series for Git's line-log functionality (`git log -L`). The series has focused on optimizing and cleaning up merge commit handling in the line-log code.

Patch 4 simplifies the condition checking for merge commits in `line-log.c`. The author found that the existing logic for determining whether a commit is a merge (has multiple parents) was written in a "head-scratchingly backwards" way. The patch flips the condition to make it more straightforward, changing from checking "if not a merge" to directly checking "if a merge".

The change is purely about code clarity - there's no functional difference in behavior, just improved readability. This continues the series' theme of maintainability improvements after the performance optimization in patch 1. The diff shows a simple 6-line change that reorganizes the conditional logic while keeping the same behavior.

This completes the series which has now delivered both measurable performance improvements (especially in repos with many merges) and several cleanups to make the code more maintainable. All changes are internal to line-log.c with no user-facing impact.