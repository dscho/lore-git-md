Refactoring patch in the `git-history` RFC series, focusing on the `add-patch` subsystem. This patch removes `add-patch`'s dependency on the `add-interactive` subsystem by directly embedding the interactive configuration it needs rather than inheriting it. The change:

1. Replaces the `add_i_state` struct with direct fields for repository pointer and interactive config in `add_p_state`
2. Updates all references to use the new embedded fields rather than the indirect path through `add_i_state`
3. Switches initialization from `init_add_i_state()` to `interactive_config_init()`
4. Maintains identical functionality while simplifying the code structure

The patch is part of the preparatory work for the `git-history` command series, continuing the pattern of careful infrastructure improvements. It demonstrates good attention to cross-command consistency by updating all interactive mode callers (`add`, `checkout`, `commit`, `reset`, `stash`) in a single coherent change. The diff shows thorough mechanical conversion of all affected code paths with no behavior changes.

Key technical details:
- Introduces direct `r` and `cfg` fields in `add_p_state`
- Updates color handling, diff configuration, and repository operations to use new direct references
- Maintains all existing configuration options (context, interhunkcontext, diff algorithm)
- Preserves test coverage since behavior remains unchanged

This appears to be a well-executed internal refactoring that simplifies the codebase while maintaining functionality, following the series' pattern of careful infrastructure preparation.