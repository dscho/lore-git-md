This thread is evaluating whether to remove the deprecated `git whatchanged` command in favor of using `git log` with appropriate options. The discussion began with Git 2.51.0's deprecation notice and has been gathering feedback about real-world usage and technical migration paths.

Monte Davidoff reported actively using the command, though without specifying their use case, while Kristoffer Haugsbakk initially suggested `git log --no-merges --raw` as the closest equivalent. Junio C Hamano later corrected a technical point in this migration path, clarifying that the key behavioral difference isn't about timestamp handling (as previously thought) but rather how single-parent empty commits are treated between the commands.

The discussion has revealed that while the basic functionality can be replicated with `git log`, there are subtle edge cases that need to be considered before full removal. The thread remains focused on understanding these technical differences and ensuring the migration path is fully understood, with references to prior discussions about implementation details. No clear consensus has emerged yet about whether the command's removal would significantly impact users.