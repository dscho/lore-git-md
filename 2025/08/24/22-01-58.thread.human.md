This thread discusses the potential removal of `git whatchanged`, which was marked as deprecated in Git 2.51.0 in favor of `git log` with specific options. The command provides similar functionality to `git log --raw` but with historical differences in defaults and behavior.

Monte Davidoff reported still using `git whatchanged`, though without specifying their particular use case. Kristoffer Haugsbakk responded by providing the equivalent `git log --no-merges --raw` command that replicates most of `git whatchanged`'s functionality, while noting there may be some subtle differences in timestamp handling from prior discussions.

The discussion remains focused on gathering concrete usage examples and providing migration guidance, rather than debating the deprecation policy itself. At this point, the thread has established that while the command can largely be replaced by `git log` options, some users still actively use it, and there may be minor behavioral differences to consider before finalizing its removal.