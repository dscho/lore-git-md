Thread identity: Discussion about potential deprecation of `git whatchanged` in Git 2.51.0, initiated by deprecation notice.

Core problem or goal: Evaluating whether to fully remove the `git whatchanged` command, which has been marked as deprecated in favor of `git log` with appropriate options. The command provides similar functionality to `git log --raw` but with different default output and some behavioral differences.

Current status: Git 2.51.0 included a deprecation notice for `git whatchanged`. The thread is collecting user feedback about real-world usage while documenting migration paths to `git log`.

Key technical details: `git whatchanged` shows commit history with diff output, similar to `git log -p` but with different defaults. Kristoffer Haugsbakk notes the closest equivalent is `git log --no-merges --raw`, though there may be timestamp-related differences from the original behavior. The command dates back to early Git history and is considered redundant with modern `git log` functionality.

Participant positions:
- Monte Davidoff reports actively using `git whatchanged` (no details on use case)
- Kristoffer Haugsbakk provides technical migration path to `git log` while acknowledging potential minor behavioral differences

Open questions: Whether sufficient users depend on `git whatchanged` to justify keeping it despite redundancy with `git log`, and whether the subtle behavioral differences (like timestamp handling) are significant enough to block removal. Need to gather more usage data points and assess impact of the minor discrepancies.

Related work: References a discussion about timestamp-related differences between the commands, though not detailed in this thread. The deprecation notice in Git 2.51.0 release serves as the starting point for this evaluation.