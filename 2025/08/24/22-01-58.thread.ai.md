Thread identity: Discussion about potential deprecation of `git whatchanged` in Git 2.51.0, initiated by deprecation notice.

Core problem or goal: Evaluating whether to fully remove the `git whatchanged` command, which has been marked as deprecated in favor of `git log` with appropriate options. The command provides similar functionality to `git log --raw` but with different default output.

Current status: Git 2.51.0 included a deprecation notice for `git whatchanged`. The thread is collecting user feedback about real-world usage before deciding on removal.

Key technical details: `git whatchanged` shows commit history with diff output, similar to `git log -p` but with different defaults. The command dates back to early Git history and is considered redundant with modern `git log` functionality.

Participant positions:
- Monte Davidoff reports actively using `git whatchanged` (no details on use case)
- (Previous messages would contain the original deprecation notice and any other feedback)

Open questions: Whether sufficient users depend on `git whatchanged` to justify keeping it despite redundancy with `git log`. Need to gather more usage data points.