This is a reply in the ongoing RFC thread about the new `git-history` command series, focusing on Jujutsu (jj) command comparisons and potential future directions. Martin von Zweigbergk provides detailed comparisons between the proposed Git subcommands (`drop`, `reorder`, `split`) and their Jujutsu counterparts, noting several behavioral differences that aren't yet addressed in the RFC:

1. For `drop`, points out that Jujutsu's `jj-abandon` also automatically rebases descendants and updates branch/HEAD references - asks if Git's implementation will match this behavior.

2. Clarifies that `reorder` more closely resembles `jj rebase -r X -A/-B Y` than `jj-new`.

3. Notes additional `jj split` options (-A/-B/-d) for more flexible splitting behavior beyond the basic parent/child split being implemented.

4. Suggests future considerations from Jujutsu that might influence the CLI design:
   - Multi-commit message editing (`jj describe`)
   - Revset syntax adoption (from Mercurial via Jujutsu) for potential UX improvements

The email doesn't propose specific changes to the current RFC but provides valuable context about Jujutsu's more sophisticated behaviors that could inform future iterations. It maintains the thread's constructive tone while surfacing important UX considerations that weren't fully addressed in the initial proposal, particularly around automatic descendant rebasing and more flexible commit positioning options.