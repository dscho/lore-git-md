Performance optimization and refactoring series for Git's line-log functionality (git log -L), focusing on merge commit processing. Series consists of 4 patches: 1 performance optimization followed by 3 subjective cleanups.

Core change in patch 1 optimizes merge commit handling in line-log.c by restructuring `process_ranges_merge_commit()` to combine two separate passes (diff computation and processing) into one. This avoids computing tree diffs against secondary parents when the first parent shows no changes to tracked paths, yielding significant speedups: 2.25x in git.git (25.8% merges) and 1.32x in linux.git (7.5% merges). The change removes the need for storing diff queues per parent, eliminating the `free_diffqueues()` helper (-15 lines net).

Patch 2 (newly added) refactors the same function to eliminate a dynamically allocated parents array, instead iterating directly through the commit's parent list. While the runtime impact is "unmeasurable", this simplifies memory management by:
- The `parents` array and its allocation/free operations
- Converting array-indexed access to direct linked-list traversal
- Maintaining identical blame assignment while reducing code complexity
Net reduction of 12 lines, continuing the series' cleanup focus.

Remaining planned cleanups (patches 3-4):
- Clean up diff queue initialization in process_ranges_ordinary_commit()
- Simplify merge commit condition checking

All changes are internal to line-log.c with cumulative -42 LOC across 62 modified lines. No user-facing behavior changes or test/documentation updates - purely implementation improvements combining measurable performance gains (patch 1) with code quality refinements (patches 2-4). The series maintains clear separation between performance-critical optimization and subsequent maintainability enhancements, with patch 1's detailed performance measurements establishing the baseline impact in merge-heavy histories.