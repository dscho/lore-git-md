Thread identity: Proposal to add `--index` flag to `git diff` as an alias for `--cached`, with eventual goal of deprecating `--cached` for terminology consistency. Thread type is feature proposal touching core diff functionality.

Core problem: Current inconsistency where `git diff --cached` shows index-vs-HEAD differences while other commands (`git add`, `git rm`) use `--index` for similar index-related operations. Proposal aims to standardize terminology across commands.

Current status: Initial patch proposing the change, which would make `git diff --index` behave identically to existing `--cached`. No version history yet as this is the first submission.

Key technical details:
- Adds `--index` option to builtin/diff.c alongside existing `--cached` handling
- Updates documentation to show both forms
- Adds tests verifying `--index` matches `--cached` behavior
- Does not yet remove `--cached` (would be future change)
- No behavior changes beyond adding the new alias

Open questions:
- Whether to proceed with eventual `--cached` deprecation
- Potential impact on scripts/muscle memory
- Whether other commands should be updated for consistency

Participant positions:
- Original author proposes the change for consistency
- No reviews or other positions yet (thread just started)

Related work:
- Existing `--cached` behavior in `git diff`
- `--index` usage in `git add`/`git rm`
- General Git CLI consistency efforts