Refactoring patch in the `git-history` series, splitting header declarations between `add-interactive` and `add-patch` subsystems.

This patch separates the header declarations for the patch-related functionality from `add-interactive.h` into a new `add-patch.h`. The motivation is to create clearer boundaries between the two subsystems, making it easier to locate relevant code. The changes include:
- Moving `struct add_p_opt` and its initialization macro
- Moving the `enum add_p_mode` and `run_add_p()` function declaration
- Adding necessary forward declarations for `struct pathspec` and `struct repository`
- Updating includes in both `add-interactive.h` and `add-patch.c`

The patch maintains all existing functionality while improving code organization. This appears to be a preparatory change in the larger `git-history` series, though it's not directly related to the history editing commands being developed. The change is straightforward and well-contained, with no behavioral modifications. The patch follows the series' pattern of careful refactoring with clear commit messages.