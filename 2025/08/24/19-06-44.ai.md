This is the final patch in a refactoring series for Git's line-log functionality, specifically addressing code quality in merge commit handling. The patch simplifies the condition check in `process_ranges_arbitrary_commit()` that determines whether a commit is a merge (has multiple parents) or not.

The change flips the logic of the condition from checking "if not a merge" to directly checking "if is a merge", making the code more straightforward. While functionally equivalent, the new version is more immediately clear to readers by putting the merge case first. The patch modifies 3 lines with no behavior change, continuing the series' theme of maintainability improvements after the performance optimization in patch 1.

This completes the series' cleanup of merge commit processing in line-log.c, which has now seen both performance improvements (from patch 1's combined diff computation) and several readability enhancements (patches 2-4). The cumulative effect is a more maintainable implementation that's also significantly more efficient for merge-heavy histories.