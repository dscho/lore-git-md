Feature implementation in the `git repo info` command series.

This patch adds the `layout.bare` field to the new `git repo info` command, migrating functionality from `git rev-parse --is-bare-repository`. The implementation follows the established pattern from previous patches in the series, using the dual strbuf architecture for clean value handling. The field returns "true" for bare repositories and "false" otherwise.

The patch includes comprehensive test coverage verifying:
- Correct reporting for both bare and non-bare repositories
- Proper integration with the command's existing output formats
- Correct behavior when combined with other fields (like in the new two-keys test case)

Implementation details:
- Adds `get_layout_bare()` function using `is_bare_repository()`
- Updates documentation to include the new field
- Extends test suite with new cases for bare repository detection
- Maintains consistency with the series' established patterns for error handling and output formatting

This continues the series' goal of moving repository metadata queries from `git rev-parse` to the more appropriate `git repo info` command. The implementation appears solid, with clear tests and documentation matching the established style of the series. The patch has received multiple "Helped-by" tags indicating thorough review from experienced contributors.