This is the first patch in a 10-part series refactoring the commit-graph subsystem to remove global state dependencies, specifically targeting `the_repository` removal and ODB abstraction.

The patch introduces a new Trace2 function `trace2_data_uintmax()` to complement the existing `trace2_data_intmax()` function, adding support for logging unsigned integers. The implementation follows the same pattern as the signed version, formatting the value and passing it to the string tracing function. The change is straightforward - adding the new function declaration in trace2.h and its implementation in trace2.c.

This foundational change supports the larger series by enabling proper tracing of unsigned values that will be used in subsequent commit-graph refactoring patches. The commit message notes this will be used in a follow-up commit, though it doesn't specify which one. The patch appears clean and focused, with no obvious issues in the implementation.

Key technical details for future reference:
- New function `trace2_data_uintmax()` mirrors `trace2_data_intmax()` but for unsigned values
- Uses `uintmax_t` type and `PRIuMAX` format specifier
- Follows existing Trace2 patterns for string conversion and forwarding
- Will be used later in the series for commit-graph metrics
- Touches both trace2.c (implementation) and trace2.h (declaration)