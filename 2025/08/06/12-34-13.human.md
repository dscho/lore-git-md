This is a minor technical comment on patch 3/10 in Patrick Steinhardt's series removing global state from the commit-graph subsystem. Oswald Buddenhagen suggests that the change from `int` to `size_t` for the `max_commits` option would be better served by introducing a new `OPT_SIZE_T` option type rather than using `OPT_UNSIGNED`. 

The comment is narrowly focused on this one line change in the patch, which is part of a larger mechanical conversion of integer types throughout the commit-graph code. While the suggestion makes technical sense, it doesn't affect the overall goals of the patch series, which has already received broad approval from key reviewers. The thread context shows this series is in its final stages with all major technical discussions resolved.