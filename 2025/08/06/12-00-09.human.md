Type consistency cleanup in the commit-graph subsystem, part of the ongoing `the_repository` removal series.

This patch fixes sign comparison warnings in commit-graph.c by converting several variables to unsigned types where they represent inherently non-negative quantities. Changes include:
- Converting counters like `num_commit_graphs_before/after` from `int` to `uint32_t`
- Making loop variables and chunk counters use `size_t` where appropriate
- Fixing the Bloom filter hash version initialization to use an explicit cast rather than relying on -1
- Adjusting file size comparisons to handle signed/unsigned mismatches

The changes are mechanical but important for type safety, continuing the series' focus on proper type usage. Reviewers have already approved the approach to unsigned types in previous discussion, and this patch applies those principles consistently. The diff is large (54 lines) but straightforward, mostly changing type declarations and adding casts where needed.

This is patch 4/10 in a well-reviewed series that's nearing completion, with all major technical questions already resolved. The changes align with the series' goals of removing global state while improving code quality.