This is part 2/10 of Patrick Steinhardt's series to remove global state from the commit-graph subsystem, specifically focusing on Bloom filter counters. The patch changes several integer counters in `commit-graph.c` from signed to unsigned types since they only track non-negative values (like filter computation statistics). 

The change makes the code's intent clearer by using types that match the variables' actual usage patterns. It also updates the corresponding Trace2 logging calls to use the new `trace2_data_uintmax()` function introduced in patch 1/10. This is a straightforward refactoring that follows the series' broader goal of type consistency improvements.

The change appears well-justified and low-risk, with reviewers having already approved similar type changes in earlier discussions. As part of a larger series that has reached consensus, this patch seems ready to proceed.