This is a reply in the ongoing discussion about adding a `--max-depth` option to Git's diff machinery, part of a 3-patch series originally authored by Jeff King and now being shepherded by Toon Claes.

Toon responds to Patrick Steinhardt's review of the main patch implementing the `--max-depth` feature. The discussion clarifies several edge cases in the depth calculation behavior, particularly around multiple overlapping pathspecs (confirming that max-depth applies to all matching pathspecs) and sibling directories at the same depth (which are included). Toon acknowledges several technical improvements needed: making error messages translatable, using `git_parse_unsigned()` for argument validation, handling `-1` as "no limit" like `git grep` does, and switching from `die()` to `BUG()` for internal checks.

The exchange reveals some design considerations that weren't fully documented in the original patch, particularly around how max-depth interacts with different pathspec scenarios. Toon appears receptive to the feedback and indicates plans to update the implementation, suggesting this is still an active work in progress rather than a final proposal. The discussion remains technical and constructive, with both parties focused on making the feature robust and consistent with Git's existing behavior patterns.