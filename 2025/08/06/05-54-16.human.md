This is a type safety refactoring patch in Patrick Steinhardt's reflog migration series. It changes the length parameter of `split_ident_line()` from `int` to `size_t` to better match how the function is actually used - callers typically pass string lengths from `strlen()` or `strbuf` which are unsigned values. The change is straightforward and maintains the same behavior while making the interface more type-correct.

The patch is part of a larger effort to improve reflog handling, particularly around the reftable backend transition. While small, this change helps prevent potential integer overflow issues and makes the API more consistent with Git's string handling conventions. The modification touches both the declaration in ident.h` and implementation in `ident.c`, with no functional changes beyond the type adjustment.

This appears to be a well-justified cleanup that was likely identified while working on the reflog infrastructure. The commit message clearly explains the rationale, and the change is minimal and focused. It's one of several preparatory improvements in this series that collectively make the codebase more robust.