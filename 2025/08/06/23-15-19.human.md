This is a test fix patch in a series addressing merge-ort's rename detection edge cases. It corrects a minor oversight in test setup for three test cases (12i, 12j, 12k) where a file modification wasn't properly staged before committing. While the tests were still functional, this cleanup makes them more accurate and less confusing by properly representing the intended test state.

The patch fixes copy-paste errors from the original test introduction where `git add` was omitted for the 'baz' file modifications. This change doesn't affect the test outcomes but removes an incidental complexity where merges were running with unstaged changes. The author notes this cleanup will help with exposing and fixing a related bug in merge-ort's rename-to-self handling in a subsequent commit.

This is part of a mature v3 series that has already addressed the core technical issues around directory rename collisions, with this patch representing final polish on the test infrastructure. The changes are straightforward and uncontroversial, just ensuring the test environment matches the intended scenario.