This is a brief follow-up discussion in the commit-graph refactoring series, specifically about patch 3/10 which converts write options to unsigned integers. The exchange focuses on the choice between `size_t` and `unsigned int` for counting commits.

Junio Hamano responds to Oswald Buddenhagen's suggestion about splitting off `OPT_SIZE_T` by arguing that commit counts don't need to use `size_t` (which is typically for memory sizes) and that `unsigned int` would be more appropriate. He characterizes the use of `size_t` for counting commits as an unnecessary "superstition" that should be abandoned.

The discussion is a minor implementation detail in an otherwise settled series, representing a philosophical difference about integer type choices rather than a substantive technical issue. The thread context shows this was previously raised by Oswald but not blocking the series, and Junio's response reinforces the current approach of using unsigned integers throughout the series. This appears to be closing commentary rather than reopening debate, as the series has already incorporated all substantive feedback and gained key approvals.