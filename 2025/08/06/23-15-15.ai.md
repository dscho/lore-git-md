Bugfix series addressing complex edge cases in merge-ort's rename detection, now at version 3 with minor documentation and comment improvements. This builds on the previously reviewed and technically complete v2 series, adding three small but meaningful clarifications.

The key changes in v3 are:
1. Added a new patch (5/7) clarifying string interning behavior in `opt->priv->path` with an expanded comment in `merge_options_internal`
2. Enhanced the comment about skipping rename-to-self cases in `process_renames()` to better explain the string interning behavior and logic
3. Updated test case references in comments to consistently include both test file and case number (e.g., "12n" -> "t6423 12n")

The series continues to address the core issue where directory renames could incorrectly affect unrelated files during merges, particularly when combined with rename/delete operations. The fixes span `merge-ort.c`'s rename handling logic and include extensive coverage in `t/t6423-merge-rename-directories.sh`.

Notable technical details carried forward from v2:
- Directory rename collision handling in `path_in_way()`
- Three specific rename-to-self bug fixes (cases 12i2, 12n2, 12n)
- Test setup corrections for cases 12i, 12j, 12k
- Optimization awareness where renames convert to delete-and-add operations

The v3 changes are purely documentation-focused, responding to Junio Hamano's feedback about test reference formatting and adding internal implementation clarity. The series remains ready for merging with all technical aspects resolved and now with even more precise documentation.