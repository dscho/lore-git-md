Bugfix and test enhancement patch in the merge-ort rename detection series. This is patch 3/7 in v3, focusing on documenting and testing two edge cases involving rename-to-self scenarios during directory renames.

The patch adds detailed documentation and verification for test case 12n, which involves a directory rename that transitively creates a rename-to-self situation. It also introduces a new test case 12n2 that examines the same scenario but with `merge.directoryRenames=true`. Both test cases are currently marked as expecting failure, as they reveal bugs in the handling of these cases.

Key technical details:
- Test 12n checks a case where a file rename (world -> tools/world) conflicts with a directory rename (tools/ -> toplevel) that would make it world -> world
- The test now verifies the expected number of index entries (1 for hello, 2 for world) to catch incorrect handling
- Test 12n2 examines the same scenario with directory renames enabled, where the file should be deleted but is currently retained
- Both tests include full English descriptions of the test setup, matching the style of other tests in t6423
- The bugs involve incorrect handling of rename-to-self cases during directory renames, particularly around conflict resolution

The patch builds on the thread's context by providing additional test coverage for the directory rename collision fixes being discussed. It specifically addresses gaps in test documentation and verification that could have helped catch these bugs earlier. The test cases will be used to verify fixes in subsequent patches in the series.