Refactoring patch in the merge-ort rename detection series. This is the second of seven patches in v3 of the series addressing directory rename edge cases.

The patch simplifies the code in `check_for_directory_rename()` by replacing a verbose check using `strmap_get_entry()` with a direct `strmap_contains()` call. The change removes an unnecessary temporary variable and makes the intent clearer while maintaining the same functionality. The commit message explains this eliminates "weirdly coded" logic in favor of a more natural approach.

This is a straightforward internal cleanup that doesn't affect behavior, building on the more substantial fixes in the rest of the series. The change helps prevent directory rename confusion cases mentioned in test cases 9c and 9d of t6423, though the actual fix for those cases was implemented in earlier patches.