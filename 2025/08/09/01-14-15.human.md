This is a documentation patch (3/5 in a series) that cleans up redundant explanations in the `git-rebase` man page. The patch removes a duplicate description of the `<upstream>` and `<branch>` arguments that was already covered earlier in the documentation.

The change is straightforward - it eliminates 6 lines of redundant text while preserving the essential information about default upstream behavior. The remaining explanation is slightly reworded for clarity ("If you don't specify an `<upstream>`..." instead of the more technical phrasing). This maintains all the technical accuracy while reducing duplication.

As part of a larger series reorganizing the rebase documentation, this patch follows the principle of keeping explanations in one logical place rather than repeating them. The series has already been through several iterations (v4 here) with maintainer feedback incorporated, and this particular patch was merged in its final form.

The patch demonstrates good documentation hygiene - identifying and removing redundancy without losing any important information. It's a small but meaningful improvement to the overall clarity of the rebase documentation.