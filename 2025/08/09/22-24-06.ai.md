Bugfix report for `git subtree split` encountering a fatal error during processing. The reporter describes a case where the command fails with "fatal: no new revisions were found" when processing a merge commit that was originally created by `git subtree add`. The problematic merge commit brings in an external library (Tracy) and appears to be the point where processing stops.

The issue occurs consistently across fresh clones of the public repository (gutter_runner on git.sergeyp.dev) using Git 2.50.1 on Fedora. The reporter suspects the merge commit structure is the root cause but hasn't provided analysis of the underlying code paths. No workarounds or attempted fixes are mentioned beyond basic troubleshooting.

This appears to be a legitimate bug report with clear steps to reproduce, though it lacks deeper technical analysis of why the merge commit causes problems. The report would benefit from bisect results or analysis of the `subtree.c` logic that handles such merge cases. The public repository availability makes this particularly actionable for maintainers to investigate.