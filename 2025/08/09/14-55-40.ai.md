This is a documentation patch discussion in the completed series reorganizing the `git-rebase` man page. D. Ben Knoble suggests adding a cross-reference from the early DESCRIPTION section to the newly created "TRANSPLANTING A TOPIC BRANCH WITH --ONTO" section that was moved later in the document by Julia Evans' patch 4/5.

The key technical point is whether to add a pointer to the `--onto` examples earlier in the document, since while `--onto` is less commonly used than basic rebase operations, it's still important enough that users might benefit from knowing about these examples sooner. The patch had already added a cross-reference from the MODE OPTIONS section, but Knoble wonders if another one in DESCRIPTION would help users discover these examples earlier.

This is a minor refinement suggestion to an already-merged patch in a completed series. The core restructuring work (moving the detailed `--onto` examples to their own section) was well-received and merged, with this being a potential small improvement to the discoverability of those examples. The suggestion maintains the series' goal of preserving all technical content while improving organization.