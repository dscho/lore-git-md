Thread identity: Bug report for `git subtree split` failing with "fatal: no new revisions were found" when processing merge commits created by `git subtree add`.

Core problem: The `git subtree split` command fails when encountering merge commits that were originally created by `git subtree add`, specifically when those merges introduce external libraries (Tracy in this case). The command aborts with "fatal: no new revisions were found" at these merge points.

Current status: Unresolved bug with clear reproduction case in public repository (gutter_runner on git.sergeyp.dev), reproducible with Git 2.50.1 on Fedora. No patches or fixes proposed yet.

Key technical details:
- Fails specifically on merge commits from `git subtree add`
- Involves the `subtree.c` implementation's merge commit handling
- Public test case available for investigation
- Error suggests revision walker loses track of history at these merge points

Open questions:
- Why does the revision walker fail on these specific merge commits?
- Are there workarounds for users needing to split such subtrees?
- Does this affect other subtree operations or just split?

Participant positions:
- Original reporter provided clear reproduction case but no code analysis
- No maintainer responses or analysis yet

Related work:
- `git subtree` is maintained in contrib/subtree/
- Similar merge commit handling issues might exist in other subtree operations