This is a documentation patch (3/5 in a series) that removes redundant explanations in the `git-rebase` man page. The patch eliminates a duplicate description of the `<upstream>` and `<branch>` arguments that was already covered earlier in the document. The change simplifies the text by consolidating the explanation of what happens when `<upstream>` isn't specified, while preserving all technical content.

The modification removes 6 lines while adding 1, making the documentation more concise without losing any information. The patch maintains the existing behavior descriptions about branch switching and upstream configuration, just in a more streamlined way. This change follows the series' goal of improving the documentation's organization and clarity.

The thread context shows this is part of a completed series that has already been merged, with all feedback incorporated. This particular patch was refined through multiple versions (v3 had wording improvements) and was recognized by the maintainer as an example of effective documentation editing. The series as a whole successfully reorganized the `git-rebase` documentation while preserving all technical content.

Key technical details for future reference:
- Removes duplicate explanation of `git rebase <upstream> <branch>` behavior
- Consolidates description of default upstream behavior into single location
- Maintains all technical content about branch switching and configuration
- Changes are purely organizational (no behavior changes)
- Part of broader effort to mirror `git-merge` documentation structure