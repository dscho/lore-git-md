Bugfix follow-up for `git-jump` filename handling with spaces. This email confirms the proposed solution works as intended for the common case (filenames with spaces) while acknowledging it drops support for the edge case of filenames ending with literal tabs. The responder (D. Ben Knoble) validates the behavior through reproduction and suggests two potential alternatives if the edge case becomes problematic: making tab-stripping conditional on space presence, or parsing `--patch-with-raw` output instead. The discussion highlights the historical context of Git's diff.c behavior (from 2006) that necessitates this workaround. The one-line Perl regex change remains the minimal solution for now, with the thread leaving open whether to handle the tab-terminated filename case in the future.