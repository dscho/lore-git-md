René Scharfe noticed and fixed a small documentation inconsistency in Git's merge command. The `--compact-summary` option, which was added to both `git merge` and `git pull` in June 2025, incorrectly suggested in the merge implementation that it takes an argument, when in fact it doesn't. The patch simply removes this misleading argument description from the option definition in `builtin/merge.c`, bringing it in line with both the pull implementation and the actual behavior. 

Junio C Hamano acknowledged the patch with a "Thanks", indicating this straightforward documentation fix is ready to be merged. The change is minimal (just one line modified) and purely cosmetic, with no impact on functionality. This sort of careful attention to detail is characteristic of René's contributions to the project.