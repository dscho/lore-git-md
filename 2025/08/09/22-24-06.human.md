This is a bug report about `git subtree split` failing with a fatal error. The reporter, Sergey Poznyak, encounters a "no new revisions were found" error when trying to split part of their repository containing a subtree that was previously merged from an external library. 

The issue occurs specifically when processing a merge commit that was created by `git subtree add` without squashing. The reporter has provided a fresh clone of their repository for reproduction and notes they're using Git 2.50.1 on Fedora 41. 

The error suggests there may be a problem with how `git subtree split` handles certain merge commits from subtree operations. The reporter suspects the issue lies with the merge commit that incorporated the external library, though the error manifests when processing the preceding commit. 

This appears to be a legitimate bug report with clear reproduction steps and version information provided. The reporter has done basic troubleshooting by trying a fresh clone, and the public repository availability makes this easier to investigate. The problem seems specific to the interaction between `git subtree` operations and merge commits.