This is a technical discussion about in-memory index handling in the `git-history` RFC series, specifically addressing implementation details in the `add-patch` infrastructure.

Patrick Steinhardt responds to D. Ben Knoble's code review of patch 10/11, which adds in-memory index patching support. The exchange focuses on two key points: First, Patrick acknowledges a duplicate `read_index_from()` call that Knoble identified, confirming it was unintentional. Second, they discuss index handling differences between the traditional `run_add_p()` (which discards the index) and the new `run_add_p_index()` (which preserves in-memory index contents for potential multi-revision operations).

The discussion reveals important implementation constraints: the new in-memory index API must maintain index contents across operations, unlike the discard-and-refresh pattern used in the traditional path. This aligns with the series' goal of providing more flexible history manipulation operations while maintaining data integrity. The technical exchange remains focused on implementation correctness without revisiting the broader architectural questions from earlier in the thread.