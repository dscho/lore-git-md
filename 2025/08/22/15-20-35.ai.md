Documentation patch fixing mismatched single quotes in the `git-interpret-trailers` manpage.

This is a straightforward documentation fix addressing mismatched single quotes in the `git-interpret-trailers` manpage. The patch corrects four instances where closing single quotes were missing in the description of the `--if-exists` and `--if-missing` options. The commit includes a demonstration showing the current output (with mismatched quotes) versus the fixed version.

The changes are minimal but important for documentation accuracy - each affected line now properly closes the single quotes around option names (`--if-exists` and `--if-missing`). The patch doesn't change any functionality, just improves the documentation formatting. The commit message clearly shows the before/after state through example output from `html2text`.

This appears to be a clean, focused documentation improvement with no obvious issues. The patch correctly targets the asciidoc source and maintains the existing documentation style. The change is small enough that it likely doesn't require extensive review, though someone might verify the quotes were indeed mismatched in the original.