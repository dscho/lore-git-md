Refactoring patch in Junio Hamano's ongoing string-list API modernization series. This is patch 4/12 in v3, converting interactive clean's choice parsing to use the new string_list API more efficiently.

The change modifies `parse_choice()` in builtin/clean.c to accept a simple character pointer rather than a full strbuf structure, since the function only needs the string content. This follows the series' pattern of simplifying string handling while maintaining functionality. The conversion demonstrates the API's flexibility - the same string_list_split_in_place_f() helper works with both strbuf and plain strings.

This is a straightforward mechanical change that builds on the previous patches' string_list infrastructure. It removes unnecessary structure passing while keeping all existing behavior (range selection, prefix matching) intact. The patch is small (6 lines changed) and focused, continuing the series' trend of making targeted improvements to string processing across Git's codebase.

The change appears well-considered and low-risk, following established patterns from earlier in the series. It contributes to the overall goal of reducing code duplication and unsafe string handling patterns while maintaining backward compatibility.