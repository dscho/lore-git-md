This thread covers minor CMake build system adjustments needed after the reftable test migration from shell scripts to C unit tests. The original patch, submitted by an unnamed contributor, aligned CMake's test discovery mechanism with the Makefile's wildcard pattern matching (`t-*.c`) rather than parsing the obsolete `UNIT_TEST_PROGRAMS` variable. Johannes Schindelin later confirmed this fix was still needed for Git v2.51.0's final release, having originally been submitted before the -rc0 cutoff.

Junio Hamano has now acknowledged the oversight and will apply the straightforward technical fix directly to master. The changes maintain build system parity between Makefile and CMake configurations while preserving linking behavior against test libraries. With Johannes' endorsement as the Windows build system expert and Junio's quick merge approval, this concludes the thread about these uncontroversial follow-up adjustments to the reftable test migration work.