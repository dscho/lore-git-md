This is the 6/7 patch in Junio Hamano's v4 string-list API refactoring series, adding a new STRING_LIST_SPLIT_NONEMPTY flag to omit empty strings from split results. The patch builds on the unified split_string() helper introduced earlier in the series, adding logic in append_one() to skip empty segments when the flag is set. 

The implementation adds the new flag (1<<1) alongside the existing STRING_LIST_SPLIT_TRIM flag, maintaining the series' pattern of bit-shifted enum values. The change affects both string_list_split_f() and string_list_split_in_place_f() through their shared helper. Test coverage includes basic empty-string filtering, edge cases with multiple delimiters, and combination with the trimming functionality.

This continues the series' theme of expanding the string-list API's capabilities while unifying the split variants' behavior. The patch demonstrates good test coverage with 15 new test cases across both split functions, including interaction with the trimming feature. The change is mechanical and focused, following the established pattern of the refactoring while adding a commonly-needed string processing feature.

Key technical details for future reference:
- New STRING_LIST_SPLIT_NONEMPTY flag (1<<1) in string-list.h
- Logic added in append_one() to skip empty segments (end <= p check)
- Tests verify standalone NONEMPTY behavior and its interaction with TRIM
- Maintains compatibility with all 21 existing callers
- Preserves in_place vs copy behavior split from earlier patches
- Follows same testing pattern as previous flags in u-string-list.c