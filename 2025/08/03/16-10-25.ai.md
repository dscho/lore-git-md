Test patch fixing prereqs for `instaweb` in the `-h`/`--help-all` test series.

This is the first of three patches in the final v3 series expanding help option availability across Git commands. It specifically addresses test infrastructure in t1517-outside-repo.sh, adding Perl as a prerequisite for testing `git instaweb -h` outside a repository context. The change ensures the test properly accounts for `instaweb`'s Perl dependency when verifying help output behavior.

The patch modifies the test script to conditionally set the PERL prereq only for the instaweb command case, while maintaining the existing test structure for other commands. This follows the series' goal of comprehensive help option coverage while respecting command-specific requirements. The change is straightforward and aligns with the thread's established direction of making help options work consistently while preserving special cases.

Key technical details:
- Adds case statement to set `prereq=PERL` specifically for `instaweb`
- Maintains existing test expectations and output verification
- Preserves the test's structure of checking help output outside repository context
- Part of finalizing test coverage for the help option expansion work

The patch appears well-contained and necessary for the series' test coverage, addressing a specific gap identified during development. It follows the thread's consensus approach of maintaining special case handling where needed.