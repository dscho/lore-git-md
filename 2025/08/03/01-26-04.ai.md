Feature patch completing the `--help-all` support outside repository contexts, part 3/3 of the series. This final patch extends the repository-less help behavior from `-h` to `--help-all` by modifying several key components:

The changes touch multiple files to ensure consistent behavior:
- `git.c` now treats `--help-all` the same as `-h` for repository setup
- `parse-options.c` gains explicit handling for `--help-all` alongside `-h`
- `merge-recursive.c` gets special case handling since it doesn't use newer APIs
- `usage.c` is updated to recognize `--help-all`
- Tests in `t1517-outside-repo.sh` are expanded to verify the new behavior

The patch maintains the existing special cases for `ls-remote` and `grep` while ensuring all other commands will show their full help text (including hidden/deprecated options) when run with `--help-all` outside a repository. The implementation follows the same pattern established for `-h` in commit 99caeed05d, using gentle repository setup when help is requested.

This completes the series' goal of making help consistently available regardless of repository context, with all controversial aspects (like mid-command `-h` handling) already addressed in previous discussion. The changes are straightforward and maintain backward compatibility while expanding functionality.