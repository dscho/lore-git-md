[AI thread summary]

This thread addresses CMake build system fixes following the reftable test migration from shell scripts to C unit tests (commit 9bbc981c6f2). The initial patch adjusted CMake configuration to handle unit test discovery via wildcard pattern matching (`t-*.c`) instead of parsing the obsolete `UNIT_TEST_PROGRAMS` variable, aligning with the Makefile's approach. A follow-up patch from Johannes Schindelin confirms this fix is still needed for Git v2.51.0 final, having originally been sent before -rc0 was released.

Technical changes include:
- Replacing `parse_makefile_for_scripts()` with `file(GLOB)` for test discovery
- Using `list(TRANSFORM)` operations to adjust paths and remove `.c` suffixes
- Maintaining existing `target_link_libraries()` configuration

Both patches are minimal (4 lines each) and focused on fixing CMake compatibility issues post-migration, preserving build system parity between Makefile and CMake configurations. The changes maintain linking behavior against unit-test-lib and common-main while adapting to the new test organization. Johannes Schindelin's involvement indicates this is a Windows build system consideration. No open questions or outstanding issues remain - these are straightforward, uncontroversial follow-ups to the reftable test migration work.