[AI thread summary]

This thread tracks CMake build system adjustments required after the reftable test migration from shell scripts to C unit tests (commit 9bbc981c6f2). The changes focus on maintaining build system parity between Makefile and CMake configurations following the test reorganization.

Key technical changes implemented:
- Replacement of `parse_makefile_for_scripts()` with `file(GLOB)` for test discovery (`t-*.c` pattern)
- Use of `list(TRANSFORM)` operations for path adjustments and `.c` suffix removal
- Preservation of `target_link_libraries()` configuration for unit-test-lib and common-main

Thread progression:
1. Initial patch submitted pre-v2.51.0-rc0 to align CMake with Makefile changes
2. Johannes Schindelin confirmed ongoing need for the fix in final v2.51.0
3. Junio Hamano acknowledged the oversight and committed to master

Participant roles:
- Johannes Schindelin (Windows build system expert) endorsed the changes
- Junio Hamano (maintainer) approved and applied the fix

Status: Both patches (4 lines each) are now merged to master with no open questions. The changes are minimal, uncontroversial technical follow-ups to the reftable test migration, specifically addressing CMake compatibility. The thread's significance lies in ensuring build system consistency across platforms, particularly for Windows users, after the test infrastructure changes.