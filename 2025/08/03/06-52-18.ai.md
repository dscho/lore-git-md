API refactoring in Junio Hamano's string-list series, part 2/7. This patch aligns `string_list_split()` with its `_in_place()` counterpart by allowing multiple delimiter characters, matching functionality added in 52acddf36c. The change affects 19 files across the codebase where single-character delimiters are replaced with string literals (e.g., `','` becomes `","`). 

Key technical changes:
- Modifies `string_list_split()` signature to take `const char *delim` instead of `int delim`
- Replaces `strchr()` with `strpbrk()` for multi-character delimiter support
- Updates all callers to pass delimiter strings rather than characters
- Maintains identical test coverage in t/unit-tests/u-string-list.c
- Preserves existing behavior for empty strings and edge cases

The patch is mechanically straightforward, converting call sites while keeping the same splitting logic. This prepares for future work to unify behavior between the two functions, though the immediate change is purely internal with no user-facing impact. The series has already undergone extensive review, and this patch continues the established pattern of the v4 changes.