Performance optimization patch series (2/2) for `git describe`, building on the previous conversion to use `prio_queue` by introducing a `lazy_queue` wrapper struct to optimize the common get+put sequence. The series shows cumulative speed improvements: 1.83x faster than Git 2.50.1 (29% from first patch, additional 41% from this patch). Key technical changes include: new `struct lazy_queue` with `get_pending` flag to track state; wrapper functions `lazy_queue_get/put/empty/clear` replacing direct `prio_queue` operations in `describe_commit()` and related functions; updated `all_have_flag()` helper to account for pending state. The implementation maintains identical functionality while reducing overhead, with proper state handling and cleanup. The series appears complete with no open questions or objections raised - both patches demonstrate clear performance wins through careful data structure optimization. Benchmarks verify the improvements on real-world cases (4751 commits between v2.41.0 and v2.47.0).