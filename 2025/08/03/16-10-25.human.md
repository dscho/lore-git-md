Test fix for the `-h`/`--help-all` feature series.

This is the first patch in the final v3 series that expands help option availability across Git commands. It addresses a test infrastructure issue in t1517-outside-repo.sh, specifically for the `instaweb` command which requires Perl to run. The patch adds proper prerequisite handling so the test will skip rather than fail when Perl is unavailable.

The change is straightforward - it adds a case statement to set the `PERL` prerequisite for `instaweb` tests while leaving other commands unaffected. This maintains the test coverage goals while properly accounting for optional dependencies. The patch follows the series' established pattern of test improvements, building on the ua/t1517-short-help-tests branch infrastructure.

As part of the finalized v3 series that has addressed all previous technical concerns, this test fix helps ensure reliable validation of the core feature - making `-h` and `--help-all` work consistently outside repository contexts. The change is small but important for comprehensive testing across different system configurations.