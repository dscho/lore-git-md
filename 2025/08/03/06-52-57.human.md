This is part 5 of Junio Hamano's ongoing string-list API refactoring series, specifically converting the interactive clean command's pattern filtering code. The patch replaces `strbuf_split()` with the newer `string_list_split_in_place_f()` API in `filter_by_patterns_cmd()`, which handles user-input patterns during interactive cleaning.

The change eliminates manual string trimming and buffer management by leveraging the string_list API's built-in trimming capability (via the `STRING_LIST_SPLIT_TRIM` flag). The conversion follows the established pattern seen elsewhere in the series, maintaining all existing functionality while making the code both safer and simpler.

Key improvements include removing the need for temporary strbuf allocations and manual memory management, while keeping the same behavior for pattern matching. The patch is a straightforward application of the new string_list API that reduces code by 2 lines while making it more maintainable.

This continues the series' trend of replacing older string processing code with more robust alternatives, now demonstrating the benefits in an interactive user-facing command. The change is well-contained and builds on the API improvements introduced in earlier patches.