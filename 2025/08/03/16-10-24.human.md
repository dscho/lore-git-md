This is a cover letter for a feature patch series (3 parts) that expands `-h` and `--help-all` option availability across Git commands. The series builds on test infrastructure from the ua/t1517-short-help-tests branch and has gone through several iterations based on feedback.

The key changes in this v3 version include rebasing on an updated test branch, adjusting for Perl dependency in the instaweb test case, and using `#define` constants instead of an enum for internal flags. The controversial fourth part from earlier versions (allowing `-h` mid-command) has been dropped due to technical and security concerns identified in previous discussions.

The series consists of:
1. Test infrastructure fixes for the dependency branch
2. Refactoring of parse-options.c to use clearer flag constants (USAGE_NORMAL/USAGE_FULL)
3. Modifications to git.c to properly handle `--help-all` outside repository contexts while preserving special cases for `ls-remote` and `grep`

The author notes some process questions about managing CC lists for patch submissions, indicating they're still getting familiar with the Git contribution workflow. The patches appear well-considered, having incorporated feedback from previous rounds, and focus on the non-controversial aspects of the original proposal.