Test infrastructure patch fixing minor issues in the help-related test suite. This is part 1 of a 3-part series improving `-h` and `--help-all` behavior across Git commands.

The patch cleans up several test script issues that were identified during development:
- Removes a spurious "Hello" message that was accidentally left in the test output
- Corrects the test expectations for `instaweb` (removing an incorrect `.sh` suffix and marking it properly as not a failure case)
- Fixes a missing newline in the `t5200` test assertion
- Adjusts the list of commands in `t1517` to remove some that actually work with `-h` outside a repository

The changes are straightforward test fixes that don't affect production code. The author notes these will likely be squashed into the upstream branch but submits them separately for easier testing. This continues the work from the `ua/t1517-short-help-tests` branch to improve testing around repository-less help behavior.