This is a test infrastructure patch in the ongoing series to improve `-h` and `--help-all` behavior across Git repositories. Junio C Hamano notes that D. Ben Knoble's fix for the `instaweb` test prerequisites (adding PERL dependency) would ideally be squashed into the base test topic (ua/t1517-short-help-tests) rather than being a separate patch.

The change itself is straightforward - it properly marks the `instaweb` test as requiring PERL support, which was missed in the original test setup. This follows the pattern seen elsewhere in Git's test suite where Perl-dependent commands are properly annotated. The patch maintains the existing test structure while adding the necessary prerequisite check.

As this is a minor test fix in an otherwise complete series, it doesn't introduce new functionality or change behavior - it just ensures the test suite runs on systems without Perl installed. Junio's comment indicates this is likely the final polish needed before the series can proceed.