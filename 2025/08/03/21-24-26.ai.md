This is a CI/build system patch that fixes CMake compatibility with recent unit test changes. The patch addresses build failures that occurred after commit 9bbc981c6f2 migrated reftable-related tests from shell scripts to C unit tests.

The core change replaces the explicit `UNIT_TEST_PROGRAMS` list parsing in CMake with a wildcard pattern that finds all `t-*.c` files in the unit test directory. This matches the Makefile's approach and fixes a CMake error that occurred when trying to parse the now-removed `UNIT_TEST_PROGRAMS` variable.

The patch is straightforward and appears to be a necessary follow-up to the test migration. It touches only the CMake build system file, modifying how unit test executables are discovered and built. The change maintains the same linking behavior (against unit-test-lib and common-main) while adapting to the new test organization.

The commit message clearly explains the context (reftable test migration) and the specific build error being fixed. The patch itself is minimal (4 lines changed) and focused on the immediate problem without introducing additional changes.