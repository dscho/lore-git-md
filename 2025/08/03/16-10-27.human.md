This is the final patch in a 3-part series extending `-h` and `--help-all` support across Git commands. The patch ensures `--help-all` works outside repository contexts, matching the existing behavior of `-h`.

The change modifies several key components: `git.c` now treats `--help-all` the same as `-h` for repository checks, `parse-options.c` adds explicit handling for `--help-all` alongside `-h`, and tests are updated to verify the new behavior. The legacy `merge-recursive` command gets special handling since it doesn't use the standard option parsing APIs.

The patch builds on previous work that refactored the help system to use clearer constants (USAGE_NORMAL/USAGE_FULL) instead of magic numbers. It maintains existing special cases for `ls-remote` and `grep` while expanding test coverage to verify `--help-all` behaves consistently across commands.

This appears to be a well-considered change that completes the series' goal of making help functionality more accessible. The patch follows the established pattern from earlier work while handling edge cases appropriately. With the v3 series now complete and addressing all previous feedback, this looks ready for inclusion.