This is a performance optimization discussion continuing the investigation of data structure choices for Git's internal metadata storage, specifically focusing on `git describe`'s depth computation.

René Scharfe responds to Jeff King's earlier benchmarks comparing khash and commit-slab approaches, with several key technical points:

1. Clarifies that khash's flag storage overhead is minimal (just 2 bits per bucket), contrary to initial assumptions about uint32_t usage.

2. Explores security implications of using oidhash() directly, noting it could enable hash collision attacks (though the practical impact is unclear). Suggests strengthening oidhash() with additional mixing as a mitigation.

3. Validates Jeff's experiment showing equivalent performance when replacing the custom khash implementation with Git's existing oidset structure, despite oidset's larger key size (36-byte OIDs vs 8-byte pointers).

4. Agrees oidset is preferable when performance is comparable, as it eliminates custom code while maintaining the same speed.

5. Briefly discusses potential future directions like a "struct objset" abstraction that could combine the best properties of different approaches, though notes measurement challenges.

The exchange demonstrates convergence toward using oidset for this specific optimization in `git describe`, while leaving open broader questions about optimal data structure selection patterns across Git's codebase. The discussion remains technical and constructive, with both participants carefully analyzing tradeoffs.