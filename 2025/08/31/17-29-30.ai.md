This is a follow-up discussion about a performance optimization patch in the `git describe` command. The thread context shows this is part of Junio Hamano's "What's cooking" status update, where René Scharfe's original patch (rs/describe-with-lazy-queue-and-khash) proposed using khash to optimize the commit queue tracking in `describe`'s depth computation. 

In this email, René requests keeping the patch in the experimental 'seen' branch rather than promoting it to 'next', citing Jeff King's (Peff) alternative suggestion to use an oidset instead. René indicates plans to conduct further performance measurements before deciding between the approaches. The discussion remains technical and collaborative, with no objections raised yet - just a request for more time to evaluate alternatives.

Key technical details for future reference: The optimization targets `finish_depth_computation()` in `describe`, comparing khash vs oidset implementations for tracking queued commits. The original motivation was avoiding object flag bit usage while improving performance over the current scanning approach. This appears to be a low-level performance optimization where implementation choices could significantly impact memory usage and speed.