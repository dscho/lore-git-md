This is a discussion thread about the ongoing strbuf_split*() refactoring series, focusing on API design considerations and behavioral edge cases.

Junio Hamano responds to Jeff King's review of the strbuf_split*() conversion series, addressing several subtle compatibility points. The conversation centers around three key behavioral differences between the old strbuf_split*() and new string_list_split*() approaches: how they handle the "max" parameter (counting pieces vs splits), repeated delimiters (empty elements), and whitespace trimming (bilateral vs right-only).

Junio agrees with Jeff's assessment that the string_list API could be enhanced to better match common split function semantics, particularly around the "max" parameter, but considers this a follow-up task. For whitespace handling, Junio notes the current conversions are slightly more lenient than the original code (accepting leading whitespace that the original didn't), but suggests this likely doesn't matter in practice and proposes dropping the TRIM flag to maintain stricter behavior.

The exchange demonstrates careful consideration of backward compatibility while modernizing string handling code. Both maintainers appear aligned on the overall approach, with Junio signaling readiness to proceed with the series while noting potential follow-up cleanups. The discussion provides valuable documentation of edge cases for future reference.