This is a follow-up discussion in the string-list API refactoring thread, specifically about the multi-delimiter support history. Jeff King confirms Junio Hamano's recollection that the multi-delimiter capability (added in 52acddf36c) was only used in test helpers for whitespace splitting, not in core Git code. 

Jeff humorously notes this reflects well on Git's code quality that `strtok()` usage was contained to test code. The exchange serves as historical context confirming the technical decisions in Junio's series were sound - the multi-delimiter support can remain as a harmless extension since it's already in the API, even if its original motivation (replacing `strtok()` in production code) turned out to be unnecessary.

This doesn't introduce any changes to the patch series but provides additional justification for keeping the multi-delimiter capability that was aligned between `string_list_split()` and `string_list_split_in_place()` in patch 2/7. The thread remains technically complete with all prior feedback addressed.