This is a refactoring discussion in the ongoing string handling modernization series, specifically addressing patch 2/11 which fixes unsafe strbuf passing in `builtin/clean.c`.

Jeff King agrees with Junio Hamano's fix for passing strbuf by value (which could cause double-free issues) but suggests an alternative approach: since the function doesn't need the full strbuf capabilities, it could take a const char* instead. He notes this would require reordering patches 2 and 3 in the series, as the current patch 2 still needs strbuf access for comma-to-space conversion that gets eliminated in patch 3.

The key technical points are:
- Current patch fixes unsafe strbuf passing by using pointer instead of value
- Proposed alternative would use const char* since the function doesn't modify the buffer
- This would require reordering patches to first eliminate the comma conversion
- The change would make the interface more minimal/appropriate for its needs
- Only one caller exists, so flexibility isn't a major concern

This is a refinement discussion about interface design within the larger string handling refactoring effort, showing careful consideration of both safety and API appropriateness. The thread context shows this is part of a well-reviewed series that's nearly ready for merging.