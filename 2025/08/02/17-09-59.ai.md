This is a discussion thread continuation about the strbuf_split*() refactoring series, focusing on API design considerations and behavioral edge cases.

Junio Hamano responds to Jeff King's review of the strbuf_split*() conversion series, addressing several subtle compatibility points raised. The discussion centers on three key behavioral differences between the old and new APIs: the "max" parameter semantics (counting pieces vs splits), handling of repeated delimiters (empty elements), and whitespace trimming behavior (bilateral vs right-only). 

Junio agrees the max parameter counting is a string_list API wart worth fixing later. For delimiter handling, he confirms strbuf_split*() produces empty elements while string_list_split*() can be configured either way via STRING_LIST_SPLIT_NOEMPTY. On whitespace, he notes the original code used rtrim() to compensate for strbuf_split*() leaving delimiters in place, and suggests the bilateral trimming of STRING_LIST_SPLIT_TRIM may be overly lenient compared to the original strict SP-only behavior.

The exchange reveals thoughtful consideration of API design tradeoffs - between strict parsing vs leniency, between immediate safety fixes vs ideal interfaces, and between preserving exact behavior vs accepting reasonable generalizations. Junio appears inclined toward slightly stricter behavior by dropping TRIM to better match original intent, though acknowledges the current behavior is likely acceptable. The discussion demonstrates the series is reaching final refinement stages, with both reviewers aligned on core approach while carefully vetting edge cases.