This is a documentation refinement in the ongoing string-list API refactoring series. Junio Hamano responds to Jeff King's suggestion to clarify the meaning of STRING_LIST_SPLIT_TRIM in the enum documentation. The flag's behavior (trimming whitespace from split pieces) was already correctly in the implementation, but the documentation could be more explicit to prevent any ambiguity with other possible interpretations of "trim".

The exchange confirms that the implementation matches the first interpretation Jeff King suggested (trimming whitespace from each split piece) rather than the alternative (trimming excess delimiters). Junio acknowledges that while "trim" in Git's codebase usually means whitespace removal, there are exceptions, making the documentation clarification worthwhile.

This is a minor but important polish to the otherwise technically complete string-list API changes. The clarification will be added to the enum documentation in string-list.h, ensuring future developers understand the flag's exact behavior without needing to examine the implementation. The change doesn't affect the series' technical merits or require any code modifications, as the documentation will simply make explicit what was already implicit.