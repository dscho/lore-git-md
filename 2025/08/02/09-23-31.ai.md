This is a technical discussion email in the ongoing feature series about expanding `-h` and `--help-all` option availability. Jeff King (peff) responds to D. Ben Knoble's earlier test results about `grep -h` behavior, uncovering deeper implementation details and potential issues with the proposed changes.

The email identifies two key technical problems with patch 4 (enabling `-h` mid-command): First, it notes the patch doesn't modify parse-options.c's special case for "lone -h" handling, potentially creating inconsistency between different command paths. Second, it describes a subtle interaction between RUN_SETUP flags and help handling that could affect repository-requiring commands, where `-h` might trigger help too early before option parsing.

Peff also reports technical difficulties applying the patch series, suggesting it may be based on unpublished work rather than current master. This raises questions about the testability of the proposed changes. The discussion continues to focus on edge cases and implementation details rather than the fundamental security concerns raised earlier in the thread, though those remain unresolved.

Key technical points for future reference:
- parse-options.c's special "lone -h" handling (lines 1-3 shown in email)
- RUN_SETUP flag interaction with help handling
- `grep -h` works because it bypasses modified code paths
- Potential inconsistency between parse-options and if_asked functions
- Patch application issues suggesting unpublished dependencies

The email doesn't resolve the thread's open questions but provides important implementation details that future discussion will need to address, particularly around consistency between different help-handling code paths.