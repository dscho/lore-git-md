This is a feature patch (part 1 of 2) from a GSoC contributor adding a `-z` flag to `git repo info` as shorthand for `--format=nul`. The change follows Git's established convention where commands like `git-config` and `git-status` use `-z` for null-terminated output.

The patch updates documentation, adds the flag handling in `builtin/repo.c`, and includes new tests verifying the behavior and error cases. Karthik Nayak provides a minor nit about formatting in the documentation but otherwise approves the approach. The implementation looks clean and well-tested, with proper handling of the mutually exclusive `--format` and `-z` options.

This appears to be a straightforward quality-of-life improvement that brings `git repo info` in line with other Git commands' conventions. The patch is part of a small series (with part 2 adding object format reporting) and has mentor sign-offs from established contributors.