This is patch 6/16 in Patrick Steinhardt's series refactoring packfile management into `struct packfile_store`. The patch moves the "kept pack cache" from `struct object_database` to the new packfile store structure.

The kept pack cache is used by `git-pack-objects` to handle cruft objects. The patch relocates both the cache pointer and its associated flags field into a new `kept_cache` substructure within `packfile_store`. This completes the migration of all packfile-specific state from the ODB to the dedicated store.

The changes are mechanical but thorough:
1. Removes `kept_pack_cache` from `struct object_database`
2. Adds new `kept_cache` field to `struct packfile_store`
3. Updates all cache access in packfile.c to use `store->kept_cache`
4. Adjusts the ODB comment to reflect no remaining packfile state
5. Maintains identical behavior for `find_kept_pack_entry()` and related functions

This continues the series' pattern of moving packfile-specific functionality out of the generic ODB structure. The patch is well-contained with changes limited to odb.h, packfile.c, and packfile.h. The commit message clearly explains the motivation (better encapsulation) and scope of the change (last packfile-related state moved).

The patch-specific discussion appears minimal - this seems to be a straightforward continuation of the established refactoring pattern. The thread context shows previous discussion focused more on other aspects like MRU list handling and MIDX integration. This patch doesn't introduce new architectural questions beyond what's already been discussed in the series.