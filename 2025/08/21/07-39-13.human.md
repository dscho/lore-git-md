This is part 15/16 of Patrick Steinhardt's series refactoring packfile management into a dedicated `packfile_store` structure as part of the object database abstraction effort. The patch refactors `get_all_packs()` to work directly with a packfile store rather than a repository, continuing the pattern established in previous patches.

The change is mechanical but wide-ranging, updating 19 files to use the new `packfile_store_get_packs()` function instead of the old repository-based `get_all_packs()`. The function's behavior remains identical - it still prepares the packfile store and returns its packfiles, including those referenced by multi-pack indices. The patch follows the series' architectural direction of moving packfile-specific operations out of the generic ODB structure.

This appears to be a straightforward continuation of the established refactoring pattern, with no new functionality or behavior changes. The patch maintains the series' high quality with thorough updates to all affected code paths. Reviewers have already discussed the architectural implications in earlier patches, so this change should be uncontroversial as it simply completes the transition for this particular function.