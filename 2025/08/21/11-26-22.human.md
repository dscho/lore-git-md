This is a refactoring patch in the submodule gitdir path encoding series. Adrian Ratiu responds to Josh Steadmon's review feedback on patch 1/9, agreeing to move the `submod_gitdir_path` variable to be local to its `if` block in the next version.

The change itself is straightforward - it replaces a hardcoded path construction (`.git/modules/$name`) with a call to the shared `submodule_name_to_gitdir()` helper function, which handles both legacy and new encoded paths. This maintains consistency with the rest of the codebase's path handling.

The interaction shows the series is in active review, with the author responsive to feedback about code organization. While this particular change is minor, it's part of a larger effort to standardize submodule gitdir path handling across Git's codebase. The thread context indicates this series is otherwise progressing well, with reviewers focusing on platform-specific edge cases and test coverage.