The thread discusses a compatibility issue where Git 2.51.0's new requirement for `--i-still-use-this` with the deprecated `git whatchanged` command is breaking Jenkins' GitSCM plugin, which still relies on this command internally. The author has reported the issue to the Jenkins team but suggests adding a Git configuration option (proposing names like `core.ignoreDeprecations` or `core.iReallyMeanIt`) to bypass the CLI requirement, since Jenkins provides no way to modify the command being run.

This proposal raises an interesting tension between maintaining backward compatibility and enforcing deprecations. While the suggested config option would solve the immediate Jenkins issue, it could undermine the deprecation warning's purpose by allowing silent continued use of deprecated features. The email doesn't include specific code changes but rather floats the config option idea for discussion, recognizing this may be a controversial approach. The discussion highlights the challenges of deprecating interfaces that are still embedded in third-party tooling.