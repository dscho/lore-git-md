This is the final patch in a series fixing color handling in Git's interactive patch mode, specifically addressing documentation.

The patch updates the `diff-highlight` README to mention the `interactive.diffFilter` configuration option, which allows using `diff-highlight` with interactive commands like `git add -p` and `git checkout -p`. While this feature was previously documented in Git's main documentation, it wasn't mentioned in the `diff-highlight` README itself.

The change is straightforward and well-justified, completing the series' goal of properly documenting all color-related configuration options for interactive mode. The patch follows the same format as existing documentation in the README and includes a clear example configuration block.

This is a minor but useful addition that helps users discover the feature regardless of which documentation they consult first. The patch is cleanly implemented and properly attributed to the person who suggested the improvement.