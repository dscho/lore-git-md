This is a technical response in the ongoing packfile store refactoring series, specifically addressing patch 12 which introduces a new helper function for packfile loading. Patrick Steinhardt confirms two points from Karthik Nayak's review:

1. Clarifies that `packfile_store_load_pack()` does indeed return either an existing or newly loaded packfile, as Karthik assumed. This function centralizes packfile lookup and loading logic, converting .idx paths to .pack paths for hashmap lookups.

2. Responds to Karthik's question about using `strmap` instead of the current hashmap implementation for `packfile_store.map`. Patrick agrees there's no strong reason against using `strmap` but defers this change to a future series, keeping the current implementation for now to maintain focus on the core refactoring work.

The exchange shows steady progress in the review process, with Patrick acknowledging valid technical suggestions while maintaining the series' incremental approach. The thread context indicates this is part of a larger effort to move packfile management into a dedicated `packfile_store` structure, with careful attention being paid to data structure choices and encapsulation boundaries. Patrick's response suggests he's tracking these architectural questions for follow-up work after the current mechanical refactoring is complete.