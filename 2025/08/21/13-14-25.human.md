This is a follow-up in the submodule gitdir path encoding series, specifically addressing platform compatibility issues in patch 6/9. Adrian Ratiu acknowledges two Windows-specific problems identified by Jeff King: 

1) The use of `pathconf()` which isn't available on Windows, requiring a compat wrapper that falls back to `NAME_MAX`. 

2) Format string portability issues with `%z` and `%ld` that need to use `PRIuMAX` instead for Windows' LLP64 model.

Ratiu also notes potential macOS test failures in t7527.62 (submodule absorbgitdirs with daemon start) that may be related to the series. He commits to fixing these issues in v2 of the patch series, using GitHub CI for testing since he lacks direct access to Windows systems. 

The response shows the author taking responsibility for the CI failures in his first Git contribution, demonstrating responsiveness to platform compatibility concerns in this complex refactoring of submodule path handling. The core technical approach remains sound, with the fixes needed being localized to platform-specific implementations.