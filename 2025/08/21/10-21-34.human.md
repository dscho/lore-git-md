Review feedback on refs exists implementation. Patrick Steinhardt provides several technical suggestions for Meet Soni's patch adding the `git refs exists` subcommand. The review focuses on code style and error handling improvements rather than fundamental design, since Patrick agrees with the decision to duplicate the simple existence-checking logic from `git show-ref --exists`.

Key suggestions include:
1. Removing an unnecessary empty newline in the code
2. Combining two argument checks into a single `if (argc != 1)` with a translated error message
3. Considering using `goto` for common error handling to ensure proper cleanup of the `strbuf`

The tone is constructive and leaves room for the author to decide on implementing the `goto` suggestion. The review maintains focus on the current patch while acknowledging it as part of the broader refs command consolidation effort.