This is a bugfix series addressing color handling inconsistencies in Git's interactive patch mode, building on earlier discussion about the regression from the Perl-to-C conversion. Jeff King has identified and fixed three distinct color-related bugs affecting commands like `stash`, `checkout`, and `reset` in interactive mode.

The series starts by fixing `git stash` to properly pass `--no-color` to child processes (patch 1), then addresses the core issue where `add-interactive` wasn't respecting `color.diff` settings (patch 2). Patch 3 implements a fallback to `color.ui` when other color settings aren't found, which was the original workaround discussed in the thread. The final patch updates the `diff-highlight` documentation to mention the `interactive.diffFilter` workaround that was previously confirmed effective.

The changes touch several key areas including the interactive add/patch machinery, stash implementation, and test coverage. The test additions in particular look thorough, covering both the fixed behaviors and edge cases. This appears to be a well-constructed series that directly addresses the known issues while maintaining backward compatibility.

Given the thread history and the comprehensive nature of these fixes, this series seems likely to merge once reviewed. The patches are particularly subtle issues that would affect users with custom color configurations, though the average user might not have noticed the inconsistencies.