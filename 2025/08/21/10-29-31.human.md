This is a minor code style fix for the second patch in a GSoC feature series enhancing `git repo info`. The patch adds a new `objects.format` field to expose the repository's object format (like `git rev-parse --show-object-format`).

Patrick Steinhardt points out a trivial formatting issue - a missing space before a closing curly brace in the field definition. The rest of the patch appears sound, building on earlier review feedback and mentor sign-offs. This follows the first patch in the series which added a `-z` flag for null-terminated output.

The series has received positive feedback overall, with only minor documentation formatting questions remaining open. Both patches are ready for consideration, addressing independent but complementary improvements to the `git repo info` command.