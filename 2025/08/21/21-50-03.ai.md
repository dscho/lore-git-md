This is a documentation clarification in the ongoing `git repo info` feature series. Junio C Hamano provides definitive guidance on the proper syntax for documenting the `-z` flag in the man page, resolving the earlier discussion about whether to use parentheses when showing alternate flag forms.

The email explains that while Git's CodingGuidelines prescribe parentheses for grouping when showing repeatable options (with `...`), they serve no purpose for single-use alternates like `--format=(keyvalue|nul) | -z`. This confirms Lucas's position that the simpler syntax without outer parentheses is correct, as there's no ambiguity to resolve.

This wraps up the documentation syntax question that was one of the final polish items for the `-z` flag patch. The remaining open questions from the thread are now just the naming conventions for the object format field (`object.format` vs `objects.format`) and potential alignment with `extensions.refStorage`. The technical implementation of both features remains merge-ready with mentor approvals.