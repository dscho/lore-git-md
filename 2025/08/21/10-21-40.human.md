This is a review response in the ongoing thread about adding a `git refs exists` subcommand as part of Git's refs command consolidation effort.

Patrick Steinhardt responds to Meet Soni's test infrastructure patch (part 2/2 of the series) with a suggested improvement to the test organization. Rather than just extracting the test cases into a shared helper script while keeping the setup code duplicated, Patrick proposes first splitting the existence-check tests in t1403-show-ref.sh into their own test file, then sharing both the setup and test cases between the old and new commands.

The suggestion aims to reduce code while maintaining test coverage parity between `git show-ref --exists` and the new `git refs exists` command. This would be a more thorough refactoring than the current approach of just sharing the test cases themselves. The change would require restructuring the patch series to do the test file split first, then the helper extraction.

Patrick's tone is constructive, focusing on improving the technical approach rather than rejecting the patch outright. The suggestion shows attention to test suite organization details while keeping the broader goal of behavioral parity between the old and new commands.