Test patch for the new `git refs exists` subcommand, part of Meet Soni's GSoC project to consolidate ref-related commands. This is the second patch in a 2-part series implementing the new subcommand.

The patch focuses on testing infrastructure, extracting the existing `git show-ref --exists` tests into a shared helper script (`show-ref-exists-tests.sh`) that can be used by both the old and new commands. The tests verify various edge cases including:
- Existence checks for regular refs, HEAD, and symrefs
- Error handling for missing refs and invalid ref names
- Special refs like FETCH_HEAD
- Non-commit objects and directory references

The new test file `t1462-refs-exists.sh` sources these shared tests while setting up its own test environment. The approach ensures behavioral parity between the old and new commands while avoiding test duplication. The patch includes thorough test coverage with 12 test cases covering the full range of expected behaviors.

Technical details include:
- New helper script at `t/show-ref-exists-tests.sh` with parameterized command invocation
- Test file `t/t1462-refs-exists.sh` added to meson.build
- Shared tests verify identical exit codes (0=exists, 2=missing, 1=error)
- Uses test-tool ref-store for low-level ref manipulation in some tests
- Maintains existing test patterns from t1403-show-ref.sh

The patch appears well-structured with clear commit message explaining the test strategy. It builds on the implementation from patch 1/2 while keeping the test changes focused and maintainable.