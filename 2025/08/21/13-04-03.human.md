This is a follow-up in the submodule gitdir path encoding series, addressing review feedback on patch 3/9. The patch adds a config override mechanism for submodule gitdir paths as part of a larger effort to prevent filesystem conflicts in submodule paths.

Adrian Ratiu acknowledges Josh Steadmon's suggestion to use Git's test helper functions (`test_config -C` and `test_unconfig -C`) instead of subshells for cleaner test code. The test in question verifies that the new `submodule.child.gitdirpath` config option correctly overrides the default gitdir path location for submodules.

The change is straightforward - replacing the subshell-based config manipulation with the more idiomatic test helper approach. This doesn't affect the core functionality but improves test script quality. The author confirms this will be fixed in the upcoming v2 of the patch series.

This is a minor refinement in response to review feedback, showing the series is in the polishing phase where test script style and documentation are being improved while the core technical approach remains stable.