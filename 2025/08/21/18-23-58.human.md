Documentation nitpick on `git repo info` option syntax. Jean-Noël Avila points out that the patch's documentation for the `-z` flag doesn't follow Git's CodingGuidelines for option formatting. The current patch shows `[--format=(keyvalue|nul)|-z]` but should instead use `[--format=(keyvalue|nul) | -z]` with spaces around the pipe separating distinct options.

This is a minor style correction to the documentation of a feature patch that's otherwise ready for merging. The thread context shows this patch (adding `-z` as shorthand for null-terminated output) has already been reviewed and approved by mentors, with Junio Hamano having confirmed the technical approach. The formatting issue doesn't affect functionality but ensures consistency with Git's documentation standards.

The correction aligns with the CodingGuidelines distinction between pipes separating alternate arguments (no spaces) versus pipes separating distinct options (spaces). Since this is purely a style fix, it shouldn't impact the patch's merge readiness once updated.