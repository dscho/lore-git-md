Thread identity: Feature patch series introducing `git refs exists` subcommand, version 1, by Meet Soni as part of GSoC. Continues effort to consolidate ref-related commands under `git refs` namespace.

Core problem/goal: Add new `git refs exists` command to check ref existence, duplicating `git show-ref --exists` functionality but under the refs namespace. Part of broader ref command reorganization.

Current status: Initial version (v1) of 2-patch series complete. First patch implements command in builtin/refs.c, second adds comprehensive tests by refactoring existing show-ref tests into shared helper script while adding new test file.

Key technical details:
- New subcommand in builtin/refs.c implementing exists command
- Directly calls refs_read_raw_ref() with same error handling as show-ref
- Uses get_main_ref_store() to access ref storage
- Documents behavior in git-refs.adoc including exit codes (0=exists, 2=missing, 1=error)
- Test infrastructure:
  - Shared helper script t/show-ref-exists-tests.sh with parameterized command invocation
  - New test file t/t1462-refs-exists.sh sourcing shared tests
  - Tests cover regular refs, HEAD, symrefs, special refs (FETCH_HEAD), invalid refs
  - Uses test-tool ref-store for low-level ref manipulation
  - Maintains behavioral parity with show-ref tests from t1403-show-ref.sh
  - 12 test cases covering all edge cases and exit codes
- Builds on prerequisite patches listed in cover letter
- Deliberately duplicates show-ref --exists logic (~20 lines) rather than abstracting

Open questions: Whether to maintain duplicate existence-checking logic between show-ref and refs exists commands long-term.

Participant positions: Initial submission by Meet Soni, no reviews yet.

Related work: Part of ongoing refs command consolidation effort, builds on prerequisite patches for refs namespace work. Test approach follows patterns from existing show-ref tests.