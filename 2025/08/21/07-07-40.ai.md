Bugfix series addressing color handling inconsistencies in Git's interactive patch mode, building on the earlier thread about color configuration problems. Jeff King identifies and fixes multiple related issues in the `add-interactive` machinery and `stash` command.

The series contains four patches that collectively address three distinct bugs discovered while investigating the color configuration problems reported earlier. The first patch fixes `stash` to properly handle color when spawning `diff-tree` child processes. The second ensures `add-interactive` respects `color.diff` settings, while the third implements proper fallback to `color.ui` when other color configurations aren't set. The final patch updates the `diff-highlight` documentation to mention the `interactive.diffFilter` workaround that was discussed earlier in the thread.

Key technical changes include modifications to `add-interactive.c` to properly handle color configuration fallbacks, updates to `builtin/stash.c` to pass `--no-color` flags correctly, and new test coverage in `t/t3701-add-interactive.sh` and `t/t3904-stash-patch.sh`. The changes touch core color handling logic that affects multiple interactive commands (`add`, `commit`, `stash`, `checkout`, `reset`, `restore`) when used with `--patch` mode.

This appears to be the technical follow-up to the earlier documentation-focused discussion, addressing the root causes of the color configuration problems rather than just documenting workarounds. The patches show thorough test coverage and appear to maintain backward compatibility requirements while fixing the regression introduced in the Perl-to-C conversion.