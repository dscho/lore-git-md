Feature patch series (2 parts) adding enhancements to `git repo info` from GSoC contributor Lucas Seiki Oshiro, now in final approval stage.

This email resolves the last minor documentation question in the series, confirming that the option syntax in the man page should follow Git's CodingGuidelines by not including spaces around the pipe character in `--format=(keyvalue|nul)|-z`. Junio C Hamano cites the specific guideline and Karthik Nayak acknowledges the correction. This was the final open question before merging.

The series adds two independent improvements to `git repo info`:
1. A `-z` flag shorthand for `--format=null` (following Git's convention for null-terminated output)
2. An `objects.format` field exposing the repository's object format

Both patches have received mentor sign-offs from Karthik Nayak and Patrick Steinhardt, with all technical and documentation issues resolved. The changes build on the `lo/repo-info` topic (commit a81224d1) in the upcoming Git 2.51 release. Implementation includes proper option handling with `die_for_incompatible_opt2()`, tests in t1900-repo.sh, and documentation following Git's style guidelines.

Key technical details for future reference:
- `-z` flag mirrors behavior from git-config/git-status
- `objects.format` reads from `repo->hash_algo->name`
- Changes in builtin/repo-info.c, builtin/repo.c
- Falls back to "keyvalue" format when neither option specified
- Documentation follows strict CodingGuidelines syntax

The series is now fully approved and ready for merging with no outstanding questions.