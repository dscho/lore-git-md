Feature discussion in the ongoing `git-history` RFC thread, focusing on command boundaries between rebase and cherry-pick functionality.

Ben Knoble responds to Sergey Organov's question about whether `git-history` could subsume rebase functionality, particularly addressing the distinction between "rebase here" versus "rebase there" operations. Knoble points out that `git rebase --onto` already provides the "rebase there" capability that Organov describes, and playfully inverts Organov's characterization by suggesting rebase duplicates cherry-pick rather than vice versa.

This exchange highlights an architectural question in the `git-history` design discussion: where to draw boundaries between history editing (rebase-style operations) and commit copying (cherry-pick-style operations). The thread shows differing perspectives on whether certain functionality belongs more naturally in rebase versus cherry-pick, with implications for how the new `git-history` command should integrate with or distinguish itself from these existing commands.

Key technical references include `git rebase --onto` syntax and the conceptual distinction between history rewriting (which discards old history) versus commit copying (which preserves original commits). The discussion remains at a design level without diving into implementation specifics of the proposed `git-history` command.