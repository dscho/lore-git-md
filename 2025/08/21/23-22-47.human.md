This is the second patch in a 4-part series refactoring Git's bulk-checkin subsystem as part of the `the_repository` removal effort. The patch systematically removes global transaction tracking from bulk-checkin operations, moving the state into `struct object_database` instead.

The changes update all bulk-checkin functions to take a `struct odb_transaction` parameter, eliminating the previous global `transaction` variable. Key modifications include:
- Moving transaction state into `struct object_database`
- Updating all callers to pass transaction context
- Maintaining the same behavior while removing global dependencies
- Adding transaction parameter to functions like `index_blob_bulk_checkin()`, `prepare_loose_object_bulk_checkin()`, and `fsync_loose_object_bulk_checkin()`

The patch affects 9 files with 94 insertions and 52 deletions, touching several Git commands (`add`, `update-index`, `unpack-objects`) that use bulk-checkin functionality. The implementation follows the established pattern for `the_repository` removal by first restructuring state management while preserving existing behavior.

This continues the architectural consensus from earlier thread discussion, showing careful attention to maintaining compatibility while advancing the long-term goal of eliminating global state. The changes appear thorough and well-tested, with all affected call sites updated consistently.