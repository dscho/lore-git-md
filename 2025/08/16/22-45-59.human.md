This is the foundational patch for a new feature introducing the `git repo` command hierarchy, part of a GSoC project to migrate repository metadata functionality from `git rev-parse`. 

The patch establishes the basic scaffolding for the new command, including the main `builtin/repo.c` implementation file, documentation, and build system integration. The new command will initially support just one subcommand, `info`, which will eventually handle repository metadata queries currently handled by `rev-parse`. The documentation clearly marks this as experimental, with behavior subject to change.

The implementation shows careful attention to Git's command structure patterns, with proper subcommand handling via `parse_options()`. The patch includes all the standard components needed for a new builtin: Makefile/meson build rules, command registration, and basic documentation. Multiple mentors and contributors have reviewed and helped shape this initial version.

As the first in an 8-part series that's now in its 11th iteration and approved for merging, this patch represents the starting point for a well-vetted feature that will provide a more logical home for repository inspection functionality. The thread context shows the series has undergone extensive review and refinement, particularly around output formatting and documentation standards.