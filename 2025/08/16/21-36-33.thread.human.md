This thread has finalized the design and implementation of path encoding for submodule gitdirs, addressing filesystem conflicts through a comprehensive solution. The series, now at v5, establishes a config-based approach (`extensions.submoduleEncoding` and `submodule.<name>.gitdirpath`) with URL percent-encoding as the standard method, resolving all architectural questions with consensus from Adrian Ratiu, Junio Hamano, and Patrick Steinhardt.

The latest patch completes the solution by handling case-folding collisions, adding detection logic that triggers stricter encoding of uppercase characters when needed. The implementation introduces new helpers in url.[ch] while maintaining the series' principle of minimal encoding - only applying changes when filesystem conflicts are detected. Test coverage in t7425-submodule-encoding.sh verifies the behavior across different scenarios, including the new case-variant handling.

With this final piece in place, the series provides a complete solution for submodule path encoding that: preserves existing repositories by default, handles conflicts through config-based migration, and covers all edge cases while keeping implementation details hidden from users. The design has evolved through five versions with input from key contributors, culminating in a solution that balances compatibility, safety, and maintainability.