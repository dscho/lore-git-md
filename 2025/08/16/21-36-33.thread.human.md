This thread has been refining the design for preventing filesystem conflicts in submodule gitdirs through config-based path resolution. The current approach (v5.7) implements path encoding via `submodulePathConfig` with strict configuration requirements when the extension is enabled, ensuring `.git/modules/` remains conflict-free while maintaining a no-migration principle.

Junio Hamano has now weighed in decisively on the build-time vs runtime configuration debate, rejecting the proposed build-time enablement options (via meson/Makefile) in favor of a purely runtime solution through gitconfig. He argues that system-wide configuration files already provide the necessary deployment control, making compile-time choices unnecessary. This guidance simplifies the design by removing the build-time dimension while preserving the series' core goal of automated transition without manual intervention.

The architectural consensus is now clear: path resolution will be strictly config-driven at runtime, with no fallbacks when the extension is enabled. Adrian Ratiu will incorporate this direction in v6, which should be the final iteration before potential merging. The remaining open questions around case-folding implementation and validation paths appear resolvable within this framework. With Junio's latest input addressing the last major design consideration, the series appears ready for final polishing and review.