This thread has reached consensus on a comprehensive solution for handling path encoding in submodule gitdirs, with all key architectural decisions settled between Adrian Ratiu, Junio Hamano, and Patrick Steinhardt. The series implements a config-based approach using `extensions.submoduleEncoding` and `submodule.<name>.gitdirpath` to safely handle filesystem conflicts through URL percent-encoding when needed.

The latest patch is a mechanical refactoring of `submodule--helper` to use the centralized `submodule_name_to_gitdir()` function rather than hardcoding paths, bringing this component in line with the rest of the codebase's handling of encoded paths. The change replaces direct `.git/modules/` construction with the standard helper, switches to `strbuf` for safer string handling, and maintains all existing functionality including force-overwrite checks. This represents final polish on an already-agreed design, with no new architectural questions raised.

The series is now in its fifth iteration with all major decisions resolved, including the encoding scheme (URL percent-encoding), transition strategy (config-based with `gitdirpath` as authoritative source), and handling of edge cases. The implementation has been refined through review to remove non-essential platform-specific checks while maintaining core functionality, with test coverage ensuring robust behavior across filesystems.