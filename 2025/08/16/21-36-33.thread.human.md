This thread covers a v2 series from Brandon Williams that rewrites how Git handles submodule gitdir paths to prevent filesystem conflicts, using URL-style encoding (RFC 3986) with case encoding for cross-platform compatibility. The 10-patch series has expanded test coverage (202 new test cases) and resolves all platform-specific issues from v1, including Windows/macOS compatibility.

The latest patch (3/10) introduces a config override mechanism via `submodule.<name>.gitdir`, allowing custom paths for interoperability with tools like JGit. This is checked first in the path resolution flow (before legacy or encoded paths), maintaining backward compatibility. The patch adds a `git submodule--helper gitdir` subcommand and comprehensive test coverage via a new helper script.

The series has incorporated feedback from key reviewers: Jeff King provided Windows fixes, Josh Steadmon improved test quality, Ben Knoble fixed commit messages, and Junio C Hamano enhanced test robustness. All identified issues from v1 have been addressed, with the series now considered production-ready. The implementation maintains the dual-path structure (.git/modules/ for backward compatibility and .git/submodules/ for encoded paths) while adding this new config override capability as a flexible escape hatch.