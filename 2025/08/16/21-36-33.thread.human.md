This thread covers the 7th iteration of a series implementing configurable submodule gitdir paths through a new `extensions.submodulePathConfig` extension. The core architecture is now settled, with all build-time options definitively rejected in favor of pure runtime configuration. The 12-patch series introduces a four-tier fallback system for path resolution (plain/encoded/numbered/hashed) with case-folding protection and comprehensive test coverage.

The latest patch (6/11) adds an atomic `git submodule--helper migrate-gitdir-configs` command to convert existing submodules to the new system while carefully handling edge cases. It ensures the repository format version is properly set and skips already-configured submodules. Patrick Steinhardt identified two minor issues - future-proofing the version check and fixing test file redirection operators - which Adrian Ratiu will address in v8.

Junio Hamano has given his ack for merging after clean application and CI testing, marking the architectural discussion complete. Patrick Steinhardt confirmed the runtime-only approach is sound while suggesting final polish items across several patches, including config renaming (`init.defaultSubmodulePathConfig`) and initialization location changes. The series now includes enhanced test coverage for relative path storage verification, representing the final refinements to this long-running effort before merging.