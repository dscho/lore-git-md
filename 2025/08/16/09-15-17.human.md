This is a technical discussion about Git alias behavior in scripting contexts, continuing the resolved thread about `git stash` aliasing.

Matthias Aßhauer responds to Christoph Anton Mitterer's point about aliases being problematic in scripts. The key observation is that Git currently has no way to distinguish script execution from interactive use, and adding an `--ignore-aliases` option now wouldn't help existing scripts that don't use it. This maintains the thread's focus on the practical implications of alias handling while shifting slightly from security concerns to backward compatibility considerations.

The email is brief and doesn't introduce new technical proposals, instead reinforcing the practical constraints around any potential changes to alias behavior. It serves as a reality check on the feasibility of modifying alias handling for scripting contexts, suggesting that any solution would need to account for widespread existing usage patterns.