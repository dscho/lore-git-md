Thread identity: Test fix for t/t1517-outside-repo.sh handling platform-specific commands when running with GIT_TEST_INSTALLED, addressing failures introduced by commit 39fc408562 which automated testing of `git subcmd -h` behavior outside repositories.

Core problem: The test was failing when run against installed Git binaries due to unexpected commands (git-gui and credential helpers) appearing in the command list. These needed to be excluded from the automated help test while maintaining verification of command help behavior outside repositories.

Current status: Adam Dinwoodie's patch series has expanded the exclusion list in two steps: first for git-gui commands (citool, gui, gui--askpass), then for credential helpers (credential-netrc, credential-libsecret, credential-osxkeychain) found in Nixpkgs packaging. The changes follow the existing pattern for special-case commands like http-backend and mergetool.

Key technical details: The fix modifies the case statement in t1517 to exclude both git-gui commands and platform-specific credential helpers from the automated help test. The test continues to verify command help behavior outside repositories by checking `git subcmd -h` output. Changes are minimal (4 lines added, 1 modified) and focused on known failure cases.

Participant positions: Adam Dinwoodie proposed both fixes. Usman Akinyemi validated the git-gui portion works with GIT_TEST_INSTALLED, suggesting it's ready for inclusion while noting more platform testing would be prudent. The credential helper addition appears to be a logical extension of the same approach.

Related work: Builds on commit 39fc408562 which introduced the automated help test behavior. Follows existing exclusion pattern for special-case commands. Addresses issues specific to Nixpkgs packaging environment.