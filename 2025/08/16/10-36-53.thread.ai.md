Thread identity: Test fix for t/t1517-outside-repo.sh handling platform-specific commands when running with GIT_TEST_INSTALLED, addressing failures introduced by commit 39fc408562 which automated testing of `git subcmd -h` behavior outside repositories.

Core problem: The test was failing when run against installed Git binaries due to unexpected commands (git-gui and credential helpers) appearing in the command list. These needed to be excluded from the test while maintaining verification of command help behavior outside repositories. Junio Hamano has raised concerns about the long-term viability of maintaining an exclusion list as installed environments may contain retired commands, third-party additions, or future commands not yet known to the test script.

Current status: Adam Dinwoodie's patch series has expanded the exclusion list in two steps: first for git-gui commands (citool, gui, gui--askpass), then for credential helpers (credential-netrc, credential-libsecret, credential-osxkeychain) found in Nixpkgs packaging. Junio acknowledges these as immediate improvements but questions whether the approach should shift to testing only builtins (--list-cmds=builtins) or reconsider the testing strategy entirely.

Key technical details: The fix modifies the case statement in t1517 to exclude both git-gui commands and platform-specific credential helpers from the automated help test, following the existing pattern for special-case commands like http-backend and mergetool. The test continues to verify command help behavior outside repositories by checking `git subcmd -h` output. Changes remain minimal (5-line diff) but highlight the challenge of maintaining an exhaustive exclusion list across diverse installation environments.

Open questions and loose ends: Fundamental questions about the test's approach remain unresolved - whether to continue expanding exclusions, limit to builtins, or redesign the test entirely. Version skew between test script and installed commands presents an ongoing maintenance challenge. Junio's local setup demonstrates the problem with 20+ unexpected command differences.

Participant positions: Adam Dinwoodie proposed both fixes. Usman Akinyemi validated the git-gui portion works with GIT_TEST_INSTALLED. Junio Hamano queues the current patch as an improvement but raises architectural concerns about the testing strategy's sustainability, suggesting builtin-only testing or more fundamental changes may be needed long-term.

Related work: Builds on commit 39fc408562 which introduced the automated help test behavior. Follows existing exclusion pattern for special-case commands. Addresses issues specific to Nixpkgs packaging environment while revealing broader challenges with GIT_TEST_INSTALLED verification.