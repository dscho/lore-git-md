Submodule gitdir path encoding series (v5) - complete implementation with final case-folding collision handling, now addressing Aaron Schrab's edge case. The series implements path encoding via `extensions.submoduleEncoding` config to prevent filesystem conflicts in submodule gitdirs, with Adrian Ratiu, Junio C Hamano, and Patrick Steinhardt in full agreement on all architectural points. Aaron Schrab identified a remaining edge case where current case-folding collision detection could fail when adding case-variant submodules where the second name contains no uppercase characters to encode.

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Implementation resolves all edge cases except this final scenario:
1. URL percent-encoding (`%41` for 'A') as primary encoding method with case-folding awareness
2. Config-based transition using `submodule.<name>.gitdirpath` as authoritative mapping (Junio's proposal)
3. Truncation+uniquification for long paths via runtime filesystem feedback
4. Case-folding collision detection via `core.ignorecase` and path normalization (now with open edge case)
5. Documentation covers all edge cases while hiding algorithm details

Implementation includes:
- New `is_casefolding_rfc3986_unreserved()` in url.[ch] treating uppercase as reserved
- Two-stage encoding fallback in `submodule_name_to_gitdir()` (needing potential adjustment)
- Case-folding detection via `core.ignorecase` and filesystem checks
- Comprehensive test coverage in t7425-submodule-encoding.sh including case-variant submodules
- Documentation updates for case-folding behavior

Final consensus and refinements:
- Patrick Steinhardt confirms strong support for Junio's authoritative config design
- Adrian Ratiu explicitly confirms agreement in prior email
- Case-folding solution maintains all prior architectural principles except this one edge case
- Aaron Schrab's scenario requires resolution before final merge
- Migration strategy remains unchanged:
  1. Existing gitdirs remain unmigrated unless conflicts exist
  2. `gitdirpath` serves as single source of truth
  3. Physical moves only required for pre-existing path conflicts
  4. Special cases like `.git/reftable/` handled as one-time migrations

The series now has one remaining open question regarding case-folding collision detection in the scenario where a second submodule's name contains no uppercase characters to encode. All other aspects are fully resolved with three-way consensus between Adrian, Junio, and Patrick. The solution will likely involve either adjusting the collision detection logic or documenting this as a known limitation, as the core architecture is otherwise complete.