Submodule gitdir path encoding series (v6) - complete implementation of pure runtime configuration through `extensions.submodulePathConfig` with all build-time options definitively rejected. The 10-patch series implements path encoding via a new extension name (`submodulePathConfig`) to prevent filesystem conflicts in submodule gitdirs, with architectural consensus requiring complete `submodule.<name>.gitdir` configuration when enabled. Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`.

Technical implementation through three logical phases with new runtime configuration support and migration command:
1. Core extension infrastructure (patches 1-6):
   - New `repository_format_submodule_path_cfg` flag in `struct repository`
   - Extension handling in `setup.c` for `extensions.submodulePathConfig`
   - Automatic config creation via `create_default_gitdir_config()`
   - Strict validation in `submodule_name_to_gitdir()` enforcing config existence
   - New test file `t7425-submodule-gitdir-path-extension.sh`
   - Helper function `validate_and_set_submodule_gitdir()`
   - Maintained four-tier fallback system (plain path -> URL encoding -> digit appending -> hashed name)
   - Case-folding protection via RFC3986 encoding variants including new case-folding specific encoding (v6.2)
     * New `is_casefolding_rfc3986_unreserved()` helper in url.[ch] treating uppercase as reserved
     * Case-folding collision detection via `check_casefolding_conflict()`
     * Three-stage fallback in `create_default_gitdir_config()`: standard encoding -> case-folding encoding -> digit-appended variants
     * Comprehensive test cases for mixed-case, encoded, and digit-suffixed paths
   - URL encoding helpers consolidated in url.[ch] including relocated `is_rfc3986_unreserved()` from credential-store.c (patch 7/10)
     * Function checks if character is safe for URLs without encoding
     * Now public and documented in url.h for reuse
     * Enables consistent URL encoding for submodule paths
   - First patch refactors `submodule--helper` to use centralized `submodule_name_to_gitdir()` path resolution
   - New `git submodule--helper gitdir` command (patch 3/10) exposes path computation
   - New runtime configuration support (patch 5/10)
   - Migration command implementation (patch 6/10)

2. Path encoding functionality (patches 7-10):
   - Centralized path resolution via `submodule_name_to_gitdir()`
   - Migration command implementation
   - Removal of automated fallback migration
   - TOCTOU race protection in clone path
   - Validation in move-head path through absorb_git_dir_into_superproject()
   - Comprehensive test coverage in t7425-submodule-gitdir-path-extension.sh
   - New validation improvements (v6.1):
     * Centralizes submodule gitdir validation in `submodule_name_to_gitdir()`
     * Removes redundant validation calls
     * Maintains critical validations to prevent parallel clone races
     * Ensures config-provided paths are validated
     * Dies on invalid paths with consistent error messages
   - Patch 8/10 specific additions:
     * URL encoding as fallback strategy in `create_default_gitdir_config()`
     * New `validate_submodule_encoded_git_dir()` for encoded path validation
     * Split validation logic between legacy/encoded modes via `validate_submodule_legacy_git_dir()`
     * Enforcement against forward slashes in encoded paths
     * Expanded test coverage for nested submodule scenarios including parallel cloning
   - Final patch (10/10) adds hashed fallback path resolution:
     * Uses `the_hash_algo` infrastructure for consistent hashing
     * Follows Git object format (blob header with size + NUL + content)
     * Only activates after exhausting all other resolution strategies
     * Comprehensive test coverage with explicit hash verification
     * Maintains validation flow through `validate_and_set_submodule_gitdir()`
     * Preserves uniqueness guarantees while sacrificing human readability

Current status reflects Junio Hamano's latest guidance with new runtime configuration flexibility and migration command. Implementation follows refined three-phase approach with runtime config integration and migration. Key changes in v6 include extension renaming, logical phase separation, migration command addition, `submodule--helper` refactoring, validation centralization, runtime configuration support, URL encoding helper consolidation in url.[ch], case-folding collision handling, nested submodule path handling through URL encoding, and final hashed fallback strategy.

Participant positions:
- Junio Hamano maintains config requirements while raising practical migration concerns, definitively closes build-time configuration discussion, approves runtime configuration approach
- Patrick Steinhardt concurs with runtime-only approach, proposed separating core extension from migration functionality
- Adrian Ratiu implements atomic enablement command and three-phase approach, addresses Patrick's concerns with new naming/decoupling, now adds validation centralization and helper commands including case-folding protection and final hashed fallback, clarifies practical use cases in response to Ben Knoble's query
- Phillip Wood continuing design review
- Josh Steadmon and Aaron supporting balanced approach
- Ben Knoble raises user-facing documentation question about practical benefits and use cases for `extensions.submodulePathConfig`, seeking clearer explanation of when/why to enable the feature (filesystem conflict prevention, case-sensitivity handling, nested submodule support)

Open questions:
- Whether to augment documentation with clearer user-facing motivation beyond technical implementation details
- Potential need for examples demonstrating practical benefits (case-sensitive filesystem compatibility, nested submodule support, pathname collision avoidance)

Practical use cases clarified in latest response:
1. Filesystem conflicts in submodule paths (particularly on case-insensitive systems)
2. Need to customize submodule gitdir locations beyond standard `.git/modules/<name>` convention

The series has reached final architectural consensus with all major design questions resolved, now including comprehensive case-folding protection and complete fallback strategy. The implementation cleanly separates concerns between core infrastructure and encoding functionality while preserving strict config requirements, with added flexibility through runtime configuration support, practical migration tooling, consolidated URL encoding utilities, specific solutions for both nested submodule cases and case-folding filesystem collisions, and a final hashed path resolution mechanism. The user-facing documentation question remains open for potential follow-up patches, though the practical motivations have now been explicitly stated in thread discussion.