Submodule gitdir path encoding series (v5) - complete implementation with final case-folding collision handling, concluding the comprehensive solution for submodule path encoding. The series implements path encoding via `extensions.submoduleEncoding` config to prevent filesystem conflicts in submodule gitdirs, with Adrian Ratiu, Junio C Hamano, and Patrick Steinhardt in full agreement on all architectural points including this final case-folding addition.

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Final implementation resolves all edge cases:
1. URL percent-encoding (`%41` for 'A') as sole encoding method, now extended with case-folding awareness
2. Config-based transition using `submodule.<name>.gitdirpath` as authoritative mapping (Junio's proposal)
3. Truncation+uniquification for long paths via runtime filesystem feedback
4. Case-folding collision detection via `core.ignorecase` and path normalization
5. Documentation covers all edge cases while hiding algorithm details

Implementation includes:
- New `is_casefolding_rfc3986_unreserved()` in url.[ch] treating uppercase as reserved
- Two-stage encoding fallback in `submodule_name_to_gitdir()`
- Case-folding detection via `core.ignorecase` and filesystem checks
- Comprehensive test coverage in t7425-submodule-encoding.sh including case-variant submodules
- Documentation updates for case-folding behavior

Final consensus and refinements:
- Patrick Steinhardt confirms strong support for Junio's authoritative config design
- Adrian Ratiu explicitly confirms agreement in latest email
- Case-folding solution maintains all prior architectural principles
- Migration strategy remains unchanged:
  1. Existing gitdirs remain unmigrated unless conflicts exist
  2. `gitdirpath` serves as single source of truth
  3. Physical moves only required for pre-existing path conflicts
  4. Special cases like `.git/reftable/` handled as one-time migrations

The series now provides complete handling of all path encoding scenarios while maintaining minimal complexity. Test coverage exercises both happy path and edge cases including path encoding, nesting, and now case-folding collisions. All open questions resolved with three-way consensus between Adrian, Junio, and Patrick, making this a fully-baked solution ready for merging.