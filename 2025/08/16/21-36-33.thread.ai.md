Submodule gitdir path encoding series (v7) - complete implementation of pure runtime configuration through `extensions.submodulePathConfig` with all build-time options definitively rejected. The 12-patch series implements path encoding via a new extension to prevent filesystem conflicts in submodule gitdirs and enable custom layouts, with architectural consensus requiring complete `submodule.<name>.gitdir` configuration when enabled. Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`.

Key v7 features:
1. Four-tier fallback system (plain -> encoded -> numbered -> hashed paths)
2. Case-folding protection via RFC3986 encoding
3. Atomic `git submodule--helper migrate-gitdir-configs` command
4. New gitdir path conflict detection
5. Runtime restriction to new repositories
6. `init.autoSetupSubmodulePathConfig` option (pending rename to `init.defaultSubmodulePathConfig` per Patrick Steinhardt's suggestion)
7. Comprehensive test coverage including nested submodules, case-insensitive filesystems, and relative path verification

Technical details:
- Core extension infrastructure (repository format flag, strict validation)
- Path encoding functionality (centralized resolution, hashed fallback)
- Migration automation (atomic operation, conflict detection)
- Files: `submodule.c`, `builtin/submodule--helper.c`, `setup.c`, `url.[ch]`
- Configs: `extensions.submodulePathConfig`, `submodule.<name>.gitdir`, `init.autoSetupSubmodulePathConfig` (name pending finalization)
- Extension initialization location under discussion (Patrick suggests moving from `init_db()` to `initialize_repository_version()`)
- Tests: `t7425-submodule-gitdir-path-extension.sh` with nested submodule, case-folding, conflict scenarios, and relative path verification (needs redirection operator spacing cleanup)

Final status: Junio Hamano has acked the series for merging after clean application to target branch and CI testing. Patrick Steinhardt's final review confirms architectural soundness while identifying minor polish items across multiple patches:
- Patch 4/11: Minor documentation polish
- Patch 5/11: Config naming and initialization location suggestions
- Patch 6/11: Forward-compatibility fix for repository format version handling and test file redirection operator spacing
- Patch 11/11: Missing blank line between functions in `submodule.c` (style nit)
These are non-blocking suggestions that the author may incorporate or disregard based on technical merit.

Participant positions:
- Junio Hamano: Final ack for merging, noted doc whitespace nit
- Patrick Steinhardt: Validated runtime-only approach, confirmed architectural soundness, provided final polish feedback including repository format version safety, test style consistency, and function spacing
- Adrian Ratiu: Primary implementation, addressing all final issues
- Josh Steadmon/Emily Shaffer: Identified and verified edge cases
- Phillip Wood: Design review
- Ben Knoble: Documentation polish

The series remains ready for merging with all technical and documentation issues resolved, now including consideration of Patrick's latest non-blocking style suggestion about function spacing in `submodule.c`. The architectural discussion is complete and the feature implementation has reached final form after seven iterations, demonstrating Git's attention to detail in post-ack cleanup phases. These final polish items represent the last refinements to a long-running effort to make submodule gitdir paths configurable at runtime.