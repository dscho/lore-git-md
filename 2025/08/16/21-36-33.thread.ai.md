Submodule gitdir path encoding series (v5) - final edge case resolution converging on automatic suffix solution for all-lowercase collisions. The series implements path encoding via `extensions.submoduleEncoding` config to prevent filesystem conflicts in submodule gitdirs, with full architectural agreement from Adrian Ratiu, Junio C Hamano, and Patrick Steinhardt. Aaron Schrab identified and analyzed the last remaining edge case where case-folding collision detection fails when adding an all-lowercase submodule ("foo") that conflicts with an existing case-variant ("Foo").

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Implementation resolves all edge cases except this final scenario, with discussion now finalized on automatic suffix appending ("foo2" or "foo_") using a simple retry algorithm. Adrian Ratiu confirms the extension remains opt-in via `extensions.submoduleEncoding` and automatically handles existing submodule configs, proposing a fallback to hashing if all retries fail. Aaron Schrab's analysis shows the impact extends beyond initial `submodule add` to affect existing clones during updates, making manual configuration impractical.

Technical implementation includes:
- URL percent-encoding (`%41` for 'A') as primary encoding method
- Config-based transition using `submodule.<name>.gitdirpath` as authoritative mapping
- Case-folding detection via `core.ignorecase` and `validate_submodule_git_dir()`
- Comprehensive test coverage in t7425-submodule-encoding.sh
- New `is_casefolding_rfc3986_unreserved()` in url.[ch]
- Finalized automatic suffix resolution for all-lowercase conflicts:
  - Simple retry algorithm (create path -> validate -> try alternative)
  - "foo2" or "foo_" suffix preferred
  - Fallback to name hashing if all alternatives fail

Current status and open questions:
- Architectural decisions finalized with three-way agreement (Adrian/Junio/Patrick)
- Implementation path fully resolved with automatic suffix approach
- Aaron Schrab confirms "foo2" suffix preference but scenario will be rare
- Impact analysis unchanged:
  - Affects new additions and existing clones during updates
  - Automated environments cannot rely on manual config
- Collation order concerns with `C` locale remain but deemed acceptable
- Migration strategy remains:
  - Existing gitdirs stay put unless conflicts exist
  - `gitdirpath` as single source of truth
  - Physical moves only for pre-existing conflicts
  - Special cases like `.git/reftable/` handled as one-time migrations

The series now awaits final implementation of the automatic suffix solution, representing the last code change needed before merging. All architectural questions are resolved, providing comprehensive protection against filesystem conflicts in submodule gitdirs while maintaining backward compatibility through the opt-in config extension.