Submodule gitdir path encoding series (v9) - final polished implementation of runtime configuration through `extensions.submodulePathConfig`, now complete with all review feedback addressed. The 12-patch series implements path encoding via repository extension to prevent filesystem conflicts in submodule gitdirs, maintaining architectural consensus requiring complete `submodule.<name>.gitdir` configuration when enabled. Core path resolution through `submodule_name_to_gitdir()` with unified `.git/modules/` structure.

Key v9 changes:
1. Replaced `git config --global` with `test_config_global` in tests
2. Split `die()` messages to multiple lines for readability
3. Fixed v8 whitespace issues
4. Patch 1/11 refactors path construction to consistently use `submodule_name_to_gitdir()` helper in `builtin/submodule--helper.c`, fixing test failures and narrowing variable scope

Core features preserved from v8:
- Four-tier fallback (plain -> encoded -> numbered -> hashed paths)
- Case-folding protection via RFC3986 encoding (using `is_rfc3986_unreserved()` and new `is_casefolding_rfc3986_unreserved()` from `url.[ch]`)
- Atomic `git submodule--helper migrate-gitdir-configs` with version checking
- Runtime restriction to new repositories
- Comprehensive test coverage including nested submodules, case-folding collisions, and migration scenarios

Technical details:
- Files: `submodule.c`, `builtin/submodule--helper.c` (with new helper usage), `setup.c`, `url.[ch]`
- Configs: `extensions.submodulePathConfig`, `submodule.<name>.gitdir`, `init.defaultSubmodulePathConfig`
- Tests: `t7425-submodule-gitdir-path-extension.sh` with expanded coverage

Final status: Junio Hamano's merge approval from v8 stands, with Patrick Steinhardt's final polish comments now addressed. Josh Steadmon's production validation (Google's internal use) confirms readiness. All technical and documentation issues resolved across 9 iterations, including case-folding protection, hashed fallback, test hygiene improvements, message formatting, test config cleanup, and now final path helper consolidation.

Participant positions:
- Junio Hamano: Maintains merge approval
- Patrick Steinhardt: Final review comments addressed (test config, message formatting, whitespace)
- Adrian Ratiu: Implemented all changes including final polish and path helper refactoring
- Josh Steadmon: Confirmed production readiness
- Phillip Wood: Earlier design review
- Ben Knoble: Earlier documentation polish

The series is now fully polished with all components implemented, tested, and reviewed, including the extended URL encoding helpers, case-folding protection, hashed fallback, comprehensive test coverage, and consistent use of path resolution helpers. Real-world validation complete via Google's downstream testing. Ready for merging with no outstanding issues.