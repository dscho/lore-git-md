Submodule gitdir path encoding series (v8) - final implementation of pure runtime configuration through `extensions.submodulePathConfig` with all build-time options definitively rejected. The 12-patch series implements path encoding via a new extension to prevent filesystem conflicts in submodule gitdirs and enable custom layouts, with architectural consensus requiring complete `submodule.<name>.gitdir` configuration when enabled. Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`.

Key v8 features:
1. Four-tier fallback system (plain -> encoded -> numbered -> hashed paths)
2. Case-folding protection via RFC3986 encoding (using relocated `is_rfc3986_unreserved()` helper from `url.[ch]`), now extended with case-folding collision detection
3. Atomic `git submodule--helper migrate-gitdir-configs` command with version check (now with improved error message formatting per Patrick Steinhardt's review)
4. New gitdir path conflict detection including case-insensitive filesystem handling
5. Runtime restriction to new repositories
6. `init.defaultSubmodulePathConfig` option in `setup.c`
7. Comprehensive test coverage including nested submodules, migration scenarios, case-folding collisions, hashed fallback, and new path conflict validation

Technical details:
- Core extension infrastructure (repository format flag, strict validation)
- Path encoding functionality (centralized resolution using RFC3986 helpers, case-folding detection, hashed fallback)
- Migration automation (atomic operation with version handling, now with improved error message formatting)
- Default config implementation in `initialize_repository_version()`
- New path conflict detection via `submodule_conflicts_with_existing()` in `submodule.c`
- Files: `submodule.c`, `builtin/submodule--helper.c`, `setup.c`, `url.[ch]` (now containing public `is_rfc3986_unreserved()` and new `is_casefolding_rfc3986_unreserved()`)
- Configs: `extensions.submodulePathConfig`, `submodule.<name>.gitdir`, `init.defaultSubmodulePathConfig`
- Tests: `t7425-submodule-gitdir-path-extension.sh` with expanded coverage including case-folding, hashed fallback, and new path conflict scenarios, now with improved test hygiene per Patrick Steinhardt's review (using `test_config` helper and `test_when_finished` for config cleanup)

Final status: Junio Hamano has given final ack for merging, confirming the series is in good shape after 8 iterations. Josh Steadmon's validation that the changes won't break Google's internal users (who sponsored Adrian Ratiu's work) provides production readiness confirmation. All review feedback addressed across v8 iterations, including case-folding protection, hashed fallback, added relative path verification tests, improved error messages (now with better formatting per Patrick's suggestion), finalized config naming, fixed version handling, complete default config implementation, atomic migration command, URL helper extensions, final path conflict detection, and test suite hygiene improvements. Patrick Steinhardt (key reviewer) gives final approval with only two minor outstanding polish comments, indicating the series is ready for merging.

Participant positions:
- Junio Hamano: Final ack for merging, confirms series is in good shape
- Patrick Steinhardt: Validated runtime-only approach, suggested test hygiene and error message formatting improvements, gives final approval with two minor polish comments
- Adrian Ratiu: Primary implementation (including URL helper extensions, hashed fallback, final conflict detection, test improvements, and error message formatting)
- Josh Steadmon: Verified edge cases and confirms successful local testing, readiness to replace Google's downstream implementation (notes company sponsorship of Adrian's work)
- Phillip Wood: Design review
- Ben Knoble: Documentation polish

The series is now complete with all components implemented and tested, including the extended URL encoding helpers, comprehensive case-folding protection, hashed fallback mechanism, final path conflict validation, test suite best practices, and improved error message formatting. Josh Steadmon's confirmation of successful real-world testing and migration path from Google's downstream implementation provides final validation of production readiness. The architectural approach of centralized path resolution through `submodule_name_to_gitdir()` is maintained with all technical and documentation issues resolved, ready for merging pending resolution of Patrick's two minor polish comments.