Submodule gitdir path encoding series (v10) - complete implementation now including hashing fallback for unresolvable path conflicts and final validation of gitdir configuration conflicts, building on the v9 series with all review feedback addressed. The 11-patch series implements path encoding via repository extension to prevent filesystem conflicts in submodule gitdirs, maintaining architectural consensus requiring complete `submodule.<name>.gitdir` configuration when enabled. Core path resolution through `submodule_name_to_gitdir()` with unified `.git/modules/` structure, now enhanced with case-folding awareness, hashing fallback, and configuration conflict detection.

Key v10 changes:
1. Added hashing-based conflict resolution as final fallback in `create_default_gitdir_config()`
2. Uses Git's hash API (`git_hash_ctx`) to generate SHA-1/SHA-256 hashes of submodule names
3. Comprehensive test coverage verifying hashing is only used after exhausting all options
4. Preserved existing error message for the extremely rare case where even hashing fails
5. Test case demonstrating fallback with 22 conflicting submodule name variants
6. New config conflict detection via `find_conflict_by_gitdir_cb` preventing silent failures
7. Handles edge case where encoded name ("nested%2fsub") conflicts with literal name
8. Maintains all v9 improvements (case-folding protection, URL encoding refactoring, etc.)

Technical details:
- Files: `submodule.c`, `builtin/submodule--helper.c`, `setup.c`, `url.[ch]` (shared URL encoding)
- Configs: `extensions.submodulePathConfig`, `submodule.<name>.gitdir`, `init.defaultSubmodulePathConfig`
- Tests: `t7425-submodule-gitdir-path-extension.sh` now includes hashing fallback and config conflict scenarios
- Path resolution sequence: 1) plain name, 2) URL-encoded name, 3) numbered variants, 4) hashed name
- Hashing: Uses Git's object hashing machinery for consistency with rest of system
- Final fallback: Generates hash of submodule name when all other attempts fail
- New validation: Detects gitdir configuration conflicts upfront with clear error messages

Final status: Junio Hamano's merge approval stands, with all review comments addressed including this final configuration conflict detection mechanism. The series is fully polished with all components implemented, tested, and reviewed, now including comprehensive filesystem protection through case-folding awareness, guaranteed-unique hashing fallback, and configuration conflict prevention.

Participant positions:
- Junio Hamano: Maintains merge approval
- Patrick Steinhardt: Final review comments addressed
- Adrian Ratiu: Implemented all changes including URL encoding refactoring, collision resolution, case-folding protection, hashing fallback, and final config conflict detection
- Josh Steadmon: Confirmed production readiness and provided testing feedback leading to final improvement
- Phillip Wood: Earlier design review
- Ben Knoble: Earlier documentation polish

The series is ready for merging with no outstanding issues, now including shared URL encoding functionality in `url.[ch]`, comprehensive collision resolution for submodule path encoding, case-folding filesystem protection, guaranteed-unique hashing fallback, and final validation of gitdir configuration conflicts. The implementation handles all known edge cases for filesystem compatibility while maintaining the existing submodule path encoding architecture.