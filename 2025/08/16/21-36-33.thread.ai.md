Submodule gitdir path encoding series (v5) - final edge case resolution converging on automatic suffix solution for all-lowercase collisions. The series implements path encoding via `extensions.submoduleEncoding` config to prevent filesystem conflicts in submodule gitdirs, with full architectural agreement from Adrian Ratiu, Junio C Hamano, and Patrick Steinhardt. Aaron Schrab identified and analyzed the last remaining edge case where case-folding collision detection fails when adding an all-lowercase submodule ("foo") that conflicts with an existing case-variant ("Foo").

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Implementation resolves all edge cases except this final scenario, with discussion now favoring automatic suffix appending ("foo2" or "foo_") over documenting as a limitation. Aaron Schrab's analysis shows the impact extends beyond initial `submodule add` to affect existing clones during updates, making manual configuration impractical for automated environments.

Technical implementation includes:
- URL percent-encoding (`%41` for 'A') as primary encoding method
- Config-based transition using `submodule.<name>.gitdirpath` as authoritative mapping
- Case-folding detection via `core.ignorecase` and `validate_submodule_git_dir()`
- Comprehensive test coverage in t7425-submodule-encoding.sh
- New `is_casefolding_rfc3986_unreserved()` in url.[ch]
- Pending modification to handle all-lowercase conflicts via automatic suffix

Current consensus and open questions:
- Architectural decisions finalized with three-way agreement (Adrian/Junio/Patrick)
- Resolution path for all-lowercase conflicts now leaning toward automatic suffix:
  - "foo2" or "foo_" suffix preferred over manual configuration
  - Aaron notes "foo2" may be clearer but scenario will be rare
- Impact analysis shows problem affects:
  - New submodule additions
  - Existing clones during updates
  - Automated environments where manual config is impractical
- Collation order concerns with `C` locale remain but deemed acceptable
- Migration strategy unchanged:
  - Existing gitdirs stay put unless conflicts exist
  - `gitdirpath` as single source of truth
  - Physical moves only for pre-existing conflicts
  - Special cases like `.git/reftable/` handled as one-time migrations

The series awaits final implementation of the automatic suffix solution to complete handling of all case-folding collision scenarios. All other aspects - including encoding strategy, config design, and test coverage - remain fully resolved. This will represent the last code change needed before the series can be merged, providing comprehensive protection against filesystem conflicts in submodule gitdirs while maintaining backward compatibility.