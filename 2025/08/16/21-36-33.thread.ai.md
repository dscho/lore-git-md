Submodule gitdir path encoding series (v9) - final polished implementation of runtime configuration through `extensions.submodulePathConfig`, now complete with all review feedback addressed. The 12-patch series implements path encoding via repository extension to prevent filesystem conflicts in submodule gitdirs, maintaining architectural consensus requiring complete `submodule.<name>.gitdir` configuration when enabled. Core path resolution through `submodule_name_to_gitdir()` with unified `.git/modules/` structure, now enhanced to centralize path validation in this helper function.

Key v9 changes:
1. Centralized path validation in `submodule_name_to_gitdir()` (removing redundant checks from 3 call sites while preserving race protection in clone operations)
2. Replaced `git config --global` with `test_config_global` in tests
3. Split `die()` messages to multiple lines for readability
4. Fixed v8 whitespace issues
5. Patch 1/11 refactors path construction to consistently use `submodule_name_to_gitdir()` helper in `builtin/submodule--helper.c`, fixing test failures and narrowing variable scope
6. New patch 3/11 adds `git submodule--helper gitdir` command exposing `submodule_name_to_gitdir()` for testing and debugging
7. Patch 4/11 introduces the `extensions.submodulePathConfig` extension as the core configuration mechanism, with new repository format flag `repository_format_submodule_path_cfg` and automatic config creation during submodule init/add
8. Patch 5/11 adds `init.defaultSubmodulePathConfig` configuration option for enabling the extension by default in new repositories (via `git init` or `git clone`), maintaining backward compatibility with existing repositories
9. Patch 6/11 implements `git submodule--helper migrate-gitdir-configs` command for automated migration of existing submodules to the new scheme, including path validation, repository format updates, and extension enabling, with comprehensive test coverage of migration scenarios and clone behavior

Core features preserved from v8:
- Four-tier fallback (plain -> encoded -> numbered -> hashed paths)
- Case-folding protection via RFC3986 encoding (using `is_rfc3986_unreserved()` and new `is_casefolding_rfc3986_unreserved()` from `url.[ch]`)
- Atomic `git submodule--helper migrate-gitdir-configs` with version checking
- Runtime restriction to new repositories
- Comprehensive test coverage including nested submodules, case-folding collisions, and migration scenarios

Technical details:
- Files: `submodule.c` (with centralized validation), `builtin/submodule--helper.c` (with new helper usage and remaining race checks, plus new `gitdir` and `migrate-gitdir-configs` subcommands), `setup.c` (now handling `init.defaultSubmodulePathConfig`), `url.[ch]`
- Configs: `extensions.submodulePathConfig`, `submodule.<name>.gitdir`, `init.defaultSubmodulePathConfig`
- Tests: `t7425-submodule-gitdir-path-extension.sh` with expanded coverage for mixed extension/non-extension submodules, path validation, cloning behavior, relative/absolute URLs, update/push workflows, init/clone scenarios with global default config, and now migration scenarios
- New helper commands: `git submodule--helper gitdir <name>` for path resolution testing and `git submodule--helper migrate-gitdir-configs` for automated migration

Final status: Junio Hamano's merge approval from v8 stands, with Patrick Steinhardt's final polish comments now addressed including this validation centralization, new helper commands, global default configuration mechanism, and migration tool. Josh Steadmon's production validation (Google's internal use) confirms readiness. All technical and documentation issues resolved across 9 iterations, including case-folding protection, hashed fallback, test hygiene improvements, message formatting, test config cleanup, path helper consolidation, validation logic unification, helper command additions, core extension configuration mechanism, global default configuration option, and now migration tool implementation.

Participant positions:
- Junio Hamano: Maintains merge approval
- Patrick Steinhardt: Final review comments addressed (test config, message formatting, whitespace, validation centralization, global default config)
- Adrian Ratiu: Implemented all changes including final polish, path helper refactoring, validation unification, new helper commands, core extension configuration, global default option, and migration tool
- Josh Steadmon: Confirmed production readiness
- Phillip Wood: Earlier design review
- Ben Knoble: Earlier documentation polish

The series is now fully polished with all components implemented, tested, and reviewed, including the extended URL encoding helpers, case-folding protection, hashed fallback, comprehensive test coverage, consistent use of path resolution helpers, unified validation logic, new helper commands, core extension configuration mechanism, global default option, and migration tool. Real-world validation complete via Google's downstream testing. Ready for merging with no outstanding issues.