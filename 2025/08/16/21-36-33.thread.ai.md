Submodule gitdir path encoding series (v1 with CI issues identified) - complete rewrite of 2018 Brandon Williams work to prevent filesystem conflicts in submodule gitdir paths. Current status:

- Core path encoding mechanism implemented using URL-style encoding (RFC 3986) with case encoding for filesystem compatibility (patches 1-6)
- Patch 7 removed redundant `validate_submodule_git_dir()` function
- Patch 8 reorganized test suite by moving nested gitdir tests
- Patch 9 added comprehensive test coverage for path encoding edge cases
- Path construction flow in `submodule_name_to_gitdir()` applies:
  1. Config override check
  2. Legacy path check
  3. New encoded path generation
- Maintains backward compatibility while transitioning to new paths
- Windows reserved names handled through encoding
- Path length validation against `NAME_MAX` preserved as BUG checks
- Test coverage expanded across t7400, t7406, t7425
- Builds on earlier patches that changed default gitdir location and added config override mechanism

Key technical details:
- URL encoding implementation in submodule.c with shared utilities
- Case encoding designed for cross-platform compatibility
- 24 files updated across code and documentation
- New `git submodule--helper gitdir` subcommand

Current status update:
- Adrian Ratiu reports test failures in GitHub CI pipeline on Windows and macOS
- Junio C Hamano identifies shell syntax issue in test case using `seq` command (fixed by replacing with `test_seq`)
- Jeff King identifies two Windows-specific issues in patch 6/9:
  1. Missing `pathconf()`/`_PC_NAME_MAX` requiring compat wrapper falling back to `NAME_MAX`
  2. Format string portability issues (`%z`/`%ld` vs `PRIuMAX`) in LLP64 model
- Jeff also notes macOS CI failures in t7527.62 (submodule absorbgitdirs with implicit daemon start), possibly related
- Josh Steadmon suggested making `submod_gitdir_path` variable local to its `if` block in patch 1/9 (code organization)
- Josh Steadmon identifies testing gap in patch 3/9's `verify_submodule_gitdir_path()` helper - only verifies legacy path format (`.git/modules/$path`) without checking encoded path (`.git/submodules/$encoded_path`), potentially masking issues in new path generation
- Issues are localized to path length validation and test coverage, not affecting core encoding mechanism
- Author preparing fixes for v2 while keeping v1 available for review
- Technical approach remains unchanged from v1 series
- Series in refinement stage with reviewers examining platform-specific edge cases, test coverage completeness, and code quality after main technical approach was established

Open issues:
- Windows `pathconf()` compatibility wrapper needed
- Windows format string portability fixes
- macOS t7527.62 test failure investigation
- Shell script standards compliance (`seq` -> `test_seq`)
- Variable scoping improvements
- Test helper verification gap for encoded paths
- Need to enhance `verify_submodule_gitdir_path()` to properly validate both legacy and encoded path formats

The path encoding work continues progressing with technical fixes for platform compatibility, test coverage improvements, and code quality refinements being incorporated. The core technical approach remains stable while addressing edge cases and verification completeness.