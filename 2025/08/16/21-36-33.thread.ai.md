Submodule gitdir path encoding series (v1) - complete rewrite of 2018 Brandon Williams work to prevent filesystem conflicts in submodule gitdir paths. Current status through patch 9/9:

- Core path encoding mechanism implemented using URL-style encoding (RFC 3986) with case encoding for filesystem compatibility (patches 1-6)
- Patch 7 removed redundant `validate_submodule_git_dir()` function after path encoding made its conflict checks obsolete
- Patch 8 reorganized test suite by moving nested gitdir tests from t7425-submodule-mixed-gitdir-paths.sh
- Patch 9 adds comprehensive test coverage for path encoding edge cases:
  - Verifies encoding of problematic names: slashes (`foo/bar` -> `foo%2fbar`), existing percent signs (legacy paths preserved), new percent signs (`fooish%bar` -> `fooish%25bar`), mixed case (`FooBar` -> `_foo_bar` on case-insensitive), Windows reserved names (`COM1` -> `_c_o_m1`)
  - Enforces `NAME_MAX` limits by dynamically testing with names that encode to exceed filesystem maximums
  - Uses `verify_submodule_gitdir_path` helper for consistent path verification
- Path construction flow in `submodule_name_to_gitdir()` applies:
  1. Config override check (`submodule.<name>.gitdirpath`)
  2. Legacy path check (`.git/modules/`)
  3. New encoded path generation (`.git/submodules/` with URL + case encoding)
- Maintains backward compatibility while transitioning to new paths
- Windows reserved names handled through encoding
- Path length validation against `NAME_MAX` preserved as BUG checks
- Test coverage expanded across t7400, t7406, t7425 for clone/update/checkout operations
- Test expectations updated for encoded paths (e.g. `dirdir%2fsubsub`)
- Builds on earlier patches that:
  - Changed default gitdir location to `.git/submodules/` (patch 2/9)
  - Added config override mechanism (patch 3/9)
  - Introduced test infrastructure (lib-verify-submodule-gitdir-path.sh)
  - Added mixed-path tests (t7425 now at 308 lines)

Key technical details:
- URL encoding implementation in submodule.c with shared utilities moved to strbuf.c
- Case encoding designed for cross-platform compatibility
- Test scenarios verify correct behavior with:
  - Legacy and new path coexistence
  - Case conflicts
  - Windows reserved names
  - Nested submodule operations
  - Path length limits
  - Special character handling
- 24 files updated across code and documentation
- New `git submodule--helper gitdir` subcommand for path querying

The series is now complete with comprehensive test coverage for all edge cases. The design accommodates alternative implementations (JGit mentioned) via config overrides while providing a robust default encoding scheme that handles filesystem limitations and special cases. The test reorganization and final test additions ensure the implementation is thoroughly validated across platforms.

[Note: Ben Knoble pointed out a style inconsistency in Adrian Ratiu's email regarding commit reference formatting, suggesting use of `git show -s --format=reference` output. This is a documentation convention note unrelated to the technical content of the series.]