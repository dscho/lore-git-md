Submodule gitdir path encoding series (v3) - implements path encoding to prevent filesystem conflicts in submodule gitdirs, gated behind `extensions.submoduleEncoding` config. Current state:

- Parts 1-3/5 implemented:
  - Part 1: Converted `add_submodule` in submodule--helper to use centralized `submodule_name_to_gitdir()` helper
  - Part 2: Introduced config override via `submodule.<name>.gitdirpath` with new `gitdir` command and test helper
  - Part 3: Moved `is_rfc3986_unreserved()` URL encoding helper from credential-store.c to strbuf.c for shared use

Overall architecture:
- Unified directory structure in `.git/modules/` with extension control
- Path resolution order in `submodule_name_to_gitdir()`:
  1. Config override (`submodule.<name>.gitdirpath`)
  2. Legacy path validation
  3. Encoding if extension present (now using shared strbuf URL encoding)
  4. Length validation via `pathconf(_PC_NAME_MAX)`
- Case encoding preserved for cross-platform compatibility
- 22 files modified (20 from parts 1-2 + strbuf changes in part 3) with comprehensive test coverage in `t7425-submodule-encoding.sh`

Reviewer feedback incorporated:
- Phillip Wood/Patrick Steinhardt: single directory architecture
- SZEDER Gábor: unused variable fixes
- Kristoffer Haugsbakk: documentation polish
- Jeff King: path length validation approach
- Junio C Hamano: URL encoding preference

Open considerations:
- Final decision on `pathconf()` vs fixed limit for path length validation
- Edge cases in path length handling
- Documentation updates for extension usage
- Review of config override interaction with encoding scheme

Technical details:
- `builtin/submodule--helper.c` modified in parts 1-2 to use new path resolution
- `strbuf.[ch]` now contains shared URL encoding logic (part 3)
- New config override mechanism provides escape hatch while maintaining core extension approach
- Test coverage expanded with config override cases via new helper
- CI passes except one unrelated Windows+Meson failure

The series continues to provide a robust solution for submodule path handling, now with shared URL encoding infrastructure and added flexibility through config overrides while preserving the path encoding architecture. The implementation maintains clear separation between legacy behavior, config overrides, and new path encoding via repository extensions.