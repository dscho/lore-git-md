Submodule gitdir path encoding series (v5) - complete implementation with final design decisions on path encoding scheme, length handling, and transition strategy. The series implements path encoding via `extensions.submoduleEncoding` config to prevent filesystem conflicts in submodule gitdirs, with Adrian Ratiu, Junio C Hamano, and Patrick Steinhardt in full agreement on all architectural points.

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Key resolved decisions:
1. URL percent-encoding (`%41` for 'A') adopted over custom schemes as the sole encoding method
2. Config-based transition using `submodule.<name>.gitdirpath` as authoritative mapping (Junio's proposal now strongly endorsed by Patrick Steinhardt and explicitly confirmed by Adrian Ratiu)
3. Truncation+uniquification for long paths via runtime filesystem feedback (handling `ENAMETOOLONG` by progressive shortening)
4. Documentation clarifies encoding permanence and edge cases while hiding algorithm details
5. Existing paths with percent-encoded-looking sequences treated literally via `gitdirpath` mapping (Junio's final verification)
6. Encoding applied minimally - only when needed for filesystem safety (case/NFC/NFD conflicts detected via `EEXISTS` from `mkdir()`)
7. No future encoding variants planned while allowing internal improvements

Implementation refinements from latest email:
- Dropped platform-specific path length checking via `pathconf()` and `_PC_NAME_MAX` (per Junio Hamano and Jeff King's suggestion) as it only affected error message formatting
- Maintained architectural principle of minimal complexity while leaving room for future path sharding/trimming enhancements
- Using `repo_config_get_string_tmp()` for cleaner config value lifetime management in `submodule.c`
- Fixed heredoc indentation style in test helper script
- Tight coupling between `submodule.<name>.gitdirpath` config and `extensions.submoduleEncoding` extension (config only honored when extension active)
- Restructured commits in v4 to properly gate config override functionality behind extension flag

Implementation includes:
- `repository_format_submodule_encoding` flag in repository.h
- Config handling in setup.c with `gitdirpath` key using `repo_config_get_string_tmp()`
- Case-aware percent-encoding functions with runtime filesystem conflict detection via error codes (`ENAMETOOLONG`, `EEXISTS`)
- Comprehensive t7425-submodule-encoding.sh test coverage
- Refactored `add_submodule()` using centralized path helpers with adaptive retry logic
- Documentation polish for `is_rfc3986_unreserved()` function in strbuf.h
- `compat/posix.h` changes for `NO_PATHCONF` fallbacks (`pathconf()` and `_PC_NAME_MAX`)

Final consensus on config-based approach:
- Patrick Steinhardt confirms strong support for Junio's authoritative config design
- Adrian Ratiu explicitly confirms agreement in latest email
- Junio Hamano's original proposal now has unanimous support
- Migration strategy finalized:
1. Existing gitdirs remain unmigrated unless conflicts exist
2. `gitdirpath` serves as single source of truth
3. Physical moves only required for pre-existing path conflicts
4. Special cases like `.git/reftable/` handled as one-time migrations
5. Documentation covers edge cases without exposing encoding details

All open questions resolved with three-way consensus between Adrian, Junio, and Patrick. The series represents a fully-baked solution to submodule path encoding with clear consensus among key contributors, now incorporating final implementation refinements for config handling and test formatting, including the removal of non-essential platform-specific path length checking while maintaining the core functionality.