Submodule gitdir path encoding series (v5) - complete implementation with final design decisions now made on both path length handling and encoding scheme. The series implements path encoding to prevent filesystem conflicts in submodule gitdirs via `extensions.submoduleEncoding` config option, with all major components including platform-portable length validation and test coverage.

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Junio C Hamano's review raised key concerns now resolved: 1) settled on URL percent-encoding (`%41` for 'A') instead of custom `_a` scheme, 2) documentation phrasing about encoding permanence, 3) truncation+uniquification for long paths, and 4) migration strategy converting existing gitdirs when enabling the feature.

Adrian Ratiu has implemented all major design decisions in v5, with Junio proposing an enhanced transition strategy where `submodule.<name>.gitdirpath` becomes the authoritative mapping mechanism. The latest discussion focuses on transition mechanics, with Junio countering Adrian's concerns about mapping complexity by arguing the config-based approach actually reduces complexity by serving as single source of truth. Junio analyzes Adrian's alternative hashed prefix proposal (`<hash>_<name>`), noting it doesn't fully address filesystem constraints and collision risks that require proper handling.

Implementation includes:
- `repository_format_submodule_encoding` flag in repository.h
- Config handling in setup.c with `gitdirpath` key
- Case-aware percent-encoding functions
- Comprehensive coverage in t7425-submodule-encoding.sh
- Refactored `add_submodule()` using centralized path helpers

The migration strategy has now been finalized with Junio's config-based approach being adopted over physical directory moves. Key resolution points:
1. Existing gitdirs will not be automatically migrated when enabling encoding
2. `submodule.<name>.gitdirpath` will serve as authoritative mapping
3. Physical moves only required for pre-existing path conflicts (e.g. `.git/modules/A` and `.git/modules/A/B`)
4. Documentation will clarify edge cases around new top-level `.git/` directories (like `.git/reftable/`)

Open questions now focused entirely on implementation details:
1. Final documentation wording for config-based migration behavior
2. Windows edge case verification
3. Gitdir conversion helper implementation for conflict cases
4. Resolution mechanics between configured paths and filesystem layout during transition

The series is functionally complete with all architectural and migration strategy decisions made. The thread has progressed from initial design through multiple iterations to consensus on substantive technical points, with only final implementation polishing remaining. Adrian's v5 implements the settled design with percent-encoding and now incorporates Junio's config-based mapping solution for the transition period.