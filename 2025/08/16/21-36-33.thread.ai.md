Submodule gitdir path encoding series (v6 pending) - finalizing configuration approach with Junio Hamano's guidance rejecting build-time options in favor of pure runtime configuration. The series implements path encoding via `submodulePathConfig` to prevent filesystem conflicts in submodule gitdirs, with architectural consensus on config authority requiring complete `submodule.<name>.gitdir` configuration when extension is enabled. Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`.

Technical implementation includes all previously detailed components with these updates:
- Build-time configuration (`--enable-submodule-encoding`/meson option) removed per Junio's guidance
- Reliance solely on runtime `extensions.submoduleEncoding` config option
- System-wide configuration via `/etc/gitconfig` as preferred enablement path
- Maintained four-tier fallback system (plain path -> URL encoding -> digit appending -> hashed name) for legacy cases
- `SUBMODULE_ENCODING_BY_DEFAULT` preprocessor definition retained for default behavior
- Initialization of `repo_fmt.submodule_encoding` when feature enabled
- Documentation updated to reflect pure runtime configuration approach
- URL encoding helper `is_rfc3986_unreserved()` in url.[ch]
- Centralized path resolution via `submodule_name_to_gitdir()`
- TOCTOU race protection in clone path
- Validation in move-head path through absorb_git_dir_into_superproject()

Test coverage remains comprehensive in t7425-submodule-encoding.sh including edge cases (Foo + foo + %46oo + numbered fallbacks + hashed variants).

Current status reflects Junio Hamano's latest guidance:
- Extension-enabled repositories require complete submodule.gitdir configuration
- New submodules automatically record non-conflicting paths in config
- Third-party tools can rely solely on config without understanding path derivation
- Design ensures config entries exist post-initialization
- Preserves no-migration principle while increasing robustness
- Pure runtime configuration approach mandated

Open considerations:
- Build-time vs runtime configuration question resolved in favor of runtime-only
- Remaining work on path computation case separation
- Final review of case-folding implementation
- Documentation of transition scenarios
- Potential for future config inheritance mechanisms
- Making validation paths more explicit in move-head operations (Adrian's planned v6 change)

Participant positions:
- Junio Hamano vetoed build-time configuration, reinforcing config must be strict when extension enabled
- Adrian Ratiu to incorporate this in v6 while maintaining migration-friendly design
- Patrick Steinhardt's configuration strictness concerns fully addressed
- Phillip Wood continuing design review
- Aaron and Josh supporting balanced approach

The series approaches finalization with all architectural questions now resolved. Junio's guidance has simplified the implementation by removing the build-time dimension while maintaining the core principle of config authority. Adrian's v6 will reflect this direction, positioning the series for potential merging after final documentation and validation refinements.