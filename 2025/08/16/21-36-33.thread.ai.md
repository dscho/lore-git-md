Submodule gitdir path encoding series (v5) - complete implementation with final design decisions now made on both path length handling and encoding scheme. The series implements path encoding to prevent filesystem conflicts in submodule gitdirs via `extensions.submoduleEncoding` config option, with all major components including platform-portable length validation and test coverage.

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Junio C Hamano's review raised key concerns now resolved: 1) settled on URL percent-encoding (`%41` for 'A') instead of custom `_a` scheme, 2) documentation phrasing about encoding permanence, 3) truncation+uniquification for long paths, and 4) migration strategy converting existing gitdirs when enabling the feature.

Adrian Ratiu has accepted all major design decisions from Junio's review:
- Percent-encoding chosen as final scheme (replacing custom `_a` encoding)
- Will modify `is_rfc3986_unreserved()` to implement case handling
- Commits to single encoding scheme without future alternatives
- Migration behavior will convert existing gitdirs to new encoding

Implementation includes:
- `repository_format_submodule_encoding` flag in repository.h
- Config handling in setup.c
- Case-aware encoding via percent-encoding functions
- Comprehensive coverage in t7425-submodule-encoding.sh
- Refactored `add_submodule()` using centralized path helpers

Open questions now limited to:
1. Final documentation wording for migration behavior
2. Remaining Windows edge case verification
3. Implementation details for gitdir conversion helper

The series is functionally complete with all architectural decisions made. Adrian indicates next version (v4) will implement the settled design with percent-encoding and migration behavior, leaving only minor documentation and edge case verification remaining before potential merge. The thread has progressed from initial design discussion through multiple iterations to reach consensus on all substantive technical points.