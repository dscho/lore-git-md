Submodule gitdir path encoding series (v7) - complete implementation of pure runtime configuration through `extensions.submodulePathConfig` with all build-time options definitively rejected. The 12-patch series implements path encoding via a new extension to prevent filesystem conflicts in submodule gitdirs and enable custom layouts, with architectural consensus requiring complete `submodule.<name>.gitdir` configuration when enabled. Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`.

Key v7 features:
1. Four-tier fallback system (plain -> encoded -> numbered -> hashed paths)
2. Case-folding protection via RFC3986 encoding
3. Atomic `git submodule--helper migrate-gitdir-configs` command (now with future-proofed version check and corrected test file operators per Patrick Steinhardt's review)
4. New gitdir path conflict detection
5. Runtime restriction to new repositories
6. `init.defaultSubmodulePathConfig` option (final name)
7. Comprehensive test coverage including nested submodules, case-insensitive filesystems, relative path verification, and repository-relative path storage validation

Technical details:
- Core extension infrastructure (repository format flag, strict validation)
- Path encoding functionality (centralized resolution, hashed fallback)
- Migration automation (atomic operation with future-proof version handling, conflict detection)
- Files: `submodule.c`, `builtin/submodule--helper.c`, `setup.c`, `url.[ch]`
- Configs: `extensions.submodulePathConfig`, `submodule.<name>.gitdir`, `init.defaultSubmodulePathConfig`
- Extension initialization in `initialize_repository_version()`
- Tests: `t7425-submodule-gitdir-path-extension.sh` with nested submodule, case-folding, conflict scenarios, relative path verification, and repository-relative path storage validation

Final status: Junio Hamano has acked the series for merging after clean application to target branch and CI testing. Patrick Steinhardt's final review confirms architectural soundness while identifying minor polish items being addressed in v8:
- Patch 4/11: Documentation polish and test coverage for relative path handling (now complete)
- Patch 5/11: Config naming finalized and initialization location set
- Patch 6/11: Future-proof version check added and test file redirection operator spacing fixed
- Patch 11/11: Style nit (missing blank line between functions) resolved

Participant positions:
- Junio Hamano: Final ack for merging, noted doc whitespace nit
- Patrick Steinhardt: Validated runtime-only approach, confirmed architectural soundness, suggested final config naming and initialization location changes, identified version check and test operator issues in migration command
- Adrian Ratiu: Primary implementation, addressing all final issues including new relative path test verification, config renaming, and migration command fixes
- Josh Steadmon/Emily Shaffer: Identified and verified edge cases
- Phillip Wood: Design review
- Ben Knoble: Documentation polish

The series remains ready for merging with all technical and documentation issues resolved, now including the migration command improvements and final test validations. The architectural discussion is complete and the feature implementation has reached final form after seven iterations, demonstrating Git's attention to detail in post-ack cleanup phases. These final polish items represent the last refinements to a long-running effort to make submodule gitdir paths configurable at runtime.