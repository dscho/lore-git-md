Submodule gitdir path encoding series (v5) - complete implementation with final design decisions on path encoding scheme, length handling, and transition strategy. The series implements path encoding via `extensions.submoduleEncoding` config to prevent filesystem conflicts in submodule gitdirs, with Adrian Ratiu and Junio C Hamano reaching consensus on all major architectural points.

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Key resolved decisions:
1. URL percent-encoding (`%41` for 'A') adopted over custom schemes as the sole encoding method
2. Config-based transition using `submodule.<name>.gitdirpath` as authoritative mapping (Junio's proposal accepted by Adrian)
3. Truncation+uniquification for long paths via runtime filesystem feedback (handling `ENAMETOOLONG` by progressive shortening)
4. Documentation clarifies encoding permanence and edge cases while hiding algorithm details
5. Existing paths with percent-encoded-looking sequences treated literally via `gitdirpath` mapping (Junio's final verification)
6. Encoding applied minimally - only when needed for filesystem safety (case collisions or invalid characters), with case/NFC/NFD conflicts detected via `EEXISTS` from `mkdir()`
7. No future encoding variants planned while allowing internal improvements

Implementation includes:
- `repository_format_submodule_encoding` flag in repository.h
- Config handling in setup.c with `gitdirpath` key
- Case-aware percent-encoding functions with runtime filesystem conflict detection via error codes (`ENAMETOOLONG`, `EEXISTS`)
- Comprehensive t7425-submodule-encoding.sh test coverage
- Refactored `add_submodule()` using centralized path helpers with adaptive retry logic

Migration strategy finalized:
1. Existing gitdirs remain unmigrated unless conflicts exist (Adrian confirms agreement with Junio's conservative approach)
2. `gitdirpath` serves as single source of truth (validated for literal interpretation of existing paths)
3. Physical moves only required for pre-existing path conflicts (validated by `validate_submodule_git_dir()` checks)
4. Special cases like `.git/reftable/` handled as one-time migrations when encountered
5. Documentation covers edge cases without exposing encoding details to users

Final implementation refinements from Junio:
- Runtime filesystem conflict detection simplified to use `mkdir()` error codes rather than complex filesystem-specific checks
- `submodule.<name>.gitdirpath` insulates users from encoding algorithm changes and prevents false decoding
- Adrian confirmed dropping plans for future encoding variants in favor of stable percent-encoding
- Adaptive path handling for length limits (progressive shortening) and case conflicts (retry with adjusted encoding) implemented

All open questions now resolved with Adrian's confirmation:
- Config-based mapping approach fully accepted
- Migration strategy settled (no blanket moves)
- Windows edge cases verified
- Gitdir conversion helpers implemented
- Path resolution mechanics defined with adaptive filesystem feedback
- Minimal encoding principle established
- Percent-encoded-looking sequences in existing paths handled safely
- Filesystem detection complexity reduced per Junio's guidance via error-code-driven retry

The thread has progressed from initial design through multiple iterations to complete consensus, with v5 implementing the settled design. Adrian Ratiu has aligned with Junio Hamano's guidance on all technical decisions, including the final runtime behavior adjustment to filesystem feedback via `mkdir()` error codes. Junio has confirmed the series handles all identified edge cases robustly, including literal treatment of existing paths with percent-encoded-looking sequences and adaptive handling of length/case conflicts. The series is in final polishing stages with all substantive issues addressed before merging.