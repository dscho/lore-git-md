Submodule gitdir path encoding series (v5) - complete implementation with final design decisions now made on path encoding scheme, length handling, and transition strategy. The series implements path encoding via `extensions.submoduleEncoding` config to prevent filesystem conflicts in submodule gitdirs, with Adrian Ratiu and Junio C Hamano reaching consensus on all major architectural points.

Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`. Key resolved decisions:
1. URL percent-encoding (`%41` for 'A') adopted over custom schemes
2. Config-based transition using `submodule.<name>.gitdirpath` as authoritative mapping (Junio's proposal accepted by Adrian)
3. Truncation+uniquification for long paths
4. Documentation clarifies encoding permanence and edge cases

Implementation includes:
- `repository_format_submodule_encoding` flag in repository.h
- Config handling in setup.c with `gitdirpath` key
- Case-aware percent-encoding functions
- Comprehensive t7425-submodule-encoding.sh test coverage
- Refactored `add_submodule()` using centralized path helpers

Migration strategy finalized:
1. Existing gitdirs remain unmigrated unless conflicts exist
2. `gitdirpath` serves as single source of truth
3. Physical moves only required for pre-existing path conflicts
4. Documentation covers `.git/reftable/` edge cases

All open questions now resolved:
- Adrian confirmed acceptance of Junio's config-based mapping approach
- Documentation wording settled
- Windows edge cases verified
- Gitdir conversion helpers implemented
- Path resolution mechanics between config and filesystem defined

The thread has progressed from initial design through multiple iterations to complete consensus, with v5 implementing the settled design including percent-encoding and config-based transition. Only final implementation polishing remains before this series is ready for merging. Key participants Adrian Ratiu and Junio C Hamano have aligned on all technical decisions, marking this as a mature patch series with all substantive issues addressed.