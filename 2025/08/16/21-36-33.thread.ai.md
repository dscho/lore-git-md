Submodule gitdir path encoding series (v8) - final implementation of pure runtime configuration through `extensions.submodulePathConfig` with all build-time options definitively rejected. The 12-patch series implements path encoding via a new extension to prevent filesystem conflicts in submodule gitdirs and enable custom layouts, with architectural consensus requiring complete `submodule.<name>.gitdir` configuration when enabled. Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`.

Key v8 features:
1. Four-tier fallback system (plain -> encoded -> numbered -> hashed paths)
2. Case-folding protection via RFC3986 encoding
3. Atomic `git submodule--helper migrate-gitdir-configs` command with future-proof version check (new in this patch)
4. New gitdir path conflict detection
5. Runtime restriction to new repositories
6. `init.defaultSubmodulePathConfig` option (final name) - now implemented in `setup.c` with initialization/clone handling
7. Comprehensive test coverage including:
   - Nested submodules
   - Case-insensitive filesystems
   - Relative path verification
   - Repository-relative path storage validation
   - Improved error messages and advice
   - New tests for `init`/`clone` behavior with default config
   - Legacy module conversion tests (new in this patch)
   - Clone behavior with/without extension tests (new in this patch)

Technical details:
- Core extension infrastructure (repository format flag, strict validation)
- Path encoding functionality (centralized resolution, hashed fallback)
- Migration automation (atomic operation with version handling, conflict detection) - now fully implemented
- Default config implementation in `initialize_repository_version()`
- Files: `submodule.c`, `builtin/submodule--helper.c`, `setup.c`, `url.[ch]`
- Configs: `extensions.submodulePathConfig`, `submodule.<name>.gitdir`, `init.defaultSubmodulePathConfig`
- Tests: `t7425-submodule-gitdir-path-extension.sh` with expanded coverage for default config and migration scenarios

Final status: Junio Hamano has acked the series for merging. All review feedback has been addressed across v8 iterations:
- Added relative path verification tests
- Improved error messages and advice
- Finalized config naming (`init.defaultSubmodulePathConfig`)
- Fixed repository format version handling
- Corrected test file operators and spacing
- Resolved style nits
- Now includes complete default config implementation
- Added atomic migration command implementation

Participant positions:
- Junio Hamano: Final ack for merging
- Patrick Steinhardt: Validated runtime-only approach, suggested final config naming and initialization changes
- Adrian Ratiu: Primary implementation, addressed all final issues including default config handling and migration command
- Josh Steadmon/Emily Shaffer: Verified edge cases
- Phillip Wood: Design review
- Ben Knoble: Documentation polish

The series remains fully reviewed and ready for merging with both the helper command and default config implementation complete. All technical and documentation issues are resolved, maintaining the architectural approach of centralized path resolution through `submodule_name_to_gitdir()`. This completes the implementation work for the submodule gitdir path encoding feature with consistent validation consolidation and configuration support, including the final migration automation piece.