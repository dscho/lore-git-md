Submodule gitdir path encoding series (v5) - implementation now settled with expanded deployment considerations from Google's experience. The series implements path encoding via `extensions.submoduleEncoding` config to prevent filesystem conflicts in submodule gitdirs, with architectural agreement from Adrian Ratiu, Patrick Steinhardt, and Junio Hamano. Core architecture maintains unified directory structure in `.git/modules/` with path resolution through `submodule_name_to_gitdir()`.

Technical implementation includes:
- Simplified collision detection via directory scan with `strcasecmp()`/`strcmp()`
- URL percent-encoding (`%41`) for case-folding conflicts
- `is_rfc3986_unreserved()` helper moved to `url.[ch]` for reuse
- Config-based transition using `submodule.<name>.gitdirpath` mapping
- Comprehensive test coverage in t7425-submodule-encoding.sh
- No dependency on `core.ignorecase` config
- Build-time configuration option to force-enable the extension
- Improved case-folding conflict detection
- Fallback to hashed names when encoding fails
- Split validation functions between encoded/non-encoded paths
- Expanded test coverage for edge cases
- Standardized gitdir path handling via `submodule_name_to_gitdir()`
- New in v5.1: Centralized validation logic in `submodule_name_to_gitdir()` that was previously scattered across multiple call sites, making the API safer by ensuring consistent validation
- New in v5.1: Removed `validate_submodule_git_dir()` from public API
- New in v5.1: Deleted redundant validation checks from `builtin/submodule--helper.c` and `submodule.c`
- New in v5.1: Maintained same validation logic (preventing gitdir nesting) but applied more consistently
- New in v5.1: Updated error messages to match previous behavior

Deployment challenges and responses:
1. Josh Steadmon identified limitations in automated migration
2. Adrian Ratiu implements build-time configuration option
3. Planned test expansion for backward compatibility
4. Migration tools still needed for existing repos
5. Verification of nested submodule prevention
6. Added CASE_INSENSITIVE_FS prereq tests
7. Config validation edge cases covered

Resolved architecture remains unchanged:
- Filesystem conflict protection through opt-in `extensions.submoduleEncoding`
- Migration strategy for new conflicts
- `gitdirpath` as single source of truth
- Build-time enablement as deployment workaround
- Centralized path resolution via `submodule_name_to_gitdir()`

Open considerations:
- Final review of simplified collision detection
- Edge case test coverage (Foo + foo + %46oo)
- Documentation of transition scenarios
- Potential for future config inheritance mechanisms
- Participant positions:
  - Adrian Ratiu driving implementation with build-time configuration addition
  - Josh Steadmon providing real-world deployment feedback from Google
  - Patrick Steinhardt and Junio Hamano maintaining architectural oversight

The series continues incremental improvements with v5.1's refactoring to centralize validation logic in `submodule_name_to_gitdir()`, making the implementation more robust while maintaining the core technical design. The change demonstrates careful architectural consideration by reducing duplicate code and preventing potential race conditions, building on the series' thorough test coverage and addressing all major feedback.