This is the final patch in the `git repo info` series, implementing the `--format` flag for output customization. The patch adds two output formats: `keyvalue` (default, human-readable) and `nul` (machine-parsable), following the pattern established by `git config`. 

The implementation introduces an `output_format` enum in `builtin/repo.c` and modifies `print_fields()` to handle both formats. The keyvalue format maintains the existing behavior with `=` delimiters and quoted values, while the nul format uses newline delimiters and NUL terminators without quoting. The patch includes comprehensive tests verifying both formats work correctly with various field types (`references.format`, `layout.bare`, `layout.shallow`), including binary comparison for nul format validation.

Documentation updates detail the new flag and formats, with examples showing both human and machine-oriented usage. Error handling rejects invalid format specifiers. The implementation maintains consistency with the series' established patterns and completes the command's output flexibility requirements as outlined in earlier discussions.

Key technical points for future reference:
- New `output_format` enum with `FORMAT_KEYVALUE` and `FORMAT_NUL_TERMINATED`
- Modified `print_fields()` with format-specific output generation
- `--format` option parsing via `parse_options()`
- Strict binary comparison of nul output in tests (`test_cmp_bin`)
- Documentation follows established definition list style
- Maintains test patterns from earlier patches while adding format-specific cases