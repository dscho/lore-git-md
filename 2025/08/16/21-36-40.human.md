This is part 7/9 of the submodule gitdir path encoding series, focusing on cleanup after the core encoding work. The patch removes the now-redundant `validate_submodule_git_dir()` function, whose conflict-checking role has been superseded by the new path encoding scheme.

The function previously guarded against submodule gitdir nesting conflicts (like `hippo/` and `hippo/hooks/` colliding), but the new encoding makes such conflicts impossible by design. The patch preserves only the basic invariant checks (path suffix matching, length validation) and moves them into `submodule_name_to_gitdir()` as BUG assertions. 

The changes touch 3 files with 93 lines removed, mostly eliminating validation checks that are now unnecessary. The removal is straightforward, with no test changes needed since the encoding scheme's tests already cover these scenarios. This is a logical cleanup step in the series, removing legacy validation that would now just add noise to the codebase.