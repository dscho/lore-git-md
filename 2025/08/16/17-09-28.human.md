This is a CI/build system patch that updates the CMake configuration to match recent changes in the unit test organization. The change follows the migration of reftable tests from shell scripts to C unit tests (commit 9bbc981c6f2), which removed the explicit `UNIT_TEST_PROGRAMS` list in favor of wildcard pattern matching.

The patch replaces CMake's parsing of `UNIT_TEST_PROGRAMS` with direct wildcard matching of `t-*.c` files in the unit-tests directory, mirroring the Makefile's approach. This fixes build errors that occurred after the test migration. The change is minimal (4 lines) and Johannes Schindelin notes it was submitted before the v2.51.0-rc0 release but needs merging for the final release.

The patch appears straightforward and uncontroversial, being a necessary follow-up to prior work. It maintains existing linking behavior while adapting to the new test organization. The author is resubmitting it for inclusion in the upcoming release, indicating it's considered an important fix for CMake users.