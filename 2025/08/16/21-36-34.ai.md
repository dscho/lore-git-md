Refactoring patch in a series (1/9) to standardize submodule gitdir path handling.

This patch modifies `add_submodule()` in `builtin/submodule--helper.c` to use the existing `submodule_name_to_gitdir()` helper function instead of hardcoding the path construction. The change aligns with commit ce125d431a which introduced this helper for consistent submodule gitdir path handling across the codebase.

The key technical change replaces manual path construction (`xstrfmt(".git/modules/%s")`) with the helper function call, switching from a raw char* to a strbuf for path storage. This affects several error handling paths where the gitdir path is checked for existence and used in messages. The patch maintains all existing functionality while using the more robust path construction method.

This appears to be a straightforward modernization that should improve consistency and potentially handle edge cases better, though the commit message doesn't specify what test failures prompted the change. The change is part of a larger series (9 patches) focused on submodule path handling, though no cover letter is provided for context about the series' overall goals.