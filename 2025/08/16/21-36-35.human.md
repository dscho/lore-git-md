Submodule path encoding series (part 2/9) - this patch prepares for URL-style encoding of submodule gitdir paths by moving them to a new `.git/submodules/` directory. 

The change separates legacy submodules (remaining in `.git/modules/`) from new encoded ones, preventing potential issues where an existing module's name might contain encoding escape characters. This is part of a larger effort to handle submodule path conflicts on case-insensitive filesystems and with reserved filenames on Windows.

The patch updates documentation and test infrastructure to reference the new location, while maintaining backward compatibility. It touches 24 files with mostly mechanical path updates, plus logic in submodule.c to check both locations. The changes are straightforward and well-contained, focused on the stage before the actual encoding logic is introduced in later patches.

This foundational change appears carefully considered, with thorough test coverage updates showing the team is systematically addressing a long-standing submodule edge case. The patch keeps the series moving toward its goal while avoiding disruption to existing setups.