This is a technical follow-up in the ongoing discussion about making content-ignoring diff options (`-I`, `-w`) work consistently across output formats. Lidong Yan responds to Junio's suggestion of a `DIFF_FORMAT_DRYRUN` mode with a more detailed proposal for optimizing diff behavior.

Lidong suggests adding a `diff_optimize` field to `struct diff_options` with three modes: `DIFF_OPT_NONE` (current behavior), `DIFF_OPT_DRY_RUN` (for early termination with metadata outputs), and `DIFF_OPT_BUFFER` (which would cache diff results). While the dry-run mode aligns with the thread's direction, the buffering proposal is new and raises concerns about memory usage.

The email continues the thread's technical refinement but introduces a potential complication with the buffering idea. The core consensus around dry-run optimization remains, but Lidong's additional buffering suggestion may spark further discussion about performance tradeoffs. The implementation appears to be converging on a solution that will make `-I` and `-w` work with `--name-status` and `--raw` outputs when blobs compare equal under ignore rules.