Documentation patch (part 5/6) in the man page linting series, focusing on synopsis formatting consistency for `git-for-each-ref`. This patch makes extensive changes to the `git-for-each-ref.adoc` documentation, with 132 lines added and removed, primarily standardizing formatting patterns:

1. **Synopsis standardization**: Replaces angle brackets with underscores for placeholders (e.g., `<pattern>` -> `_<pattern>_`), adds consistent backtick quoting for command options and parameters, and fixes parenthetical grouping in the command synopsis (`[ --stdin | <pattern>... ]` -> `[ --stdin | (<pattern>...)]`).

2. **Option documentation cleanup**: Makes option descriptions more concise and consistent with other man pages, such as changing "If given..." to direct statements ("The list of patterns is read...") and standardizing parameter references (e.g., `_<count>_` instead of bare `<count>`).

3. **Formatting atoms**: Applies consistent backtick quoting to all format atom names (`refname`, `objecttype`, etc.) and their modifiers (`:short`, `:lstrip`), while converting some bullet lists to definition-list style (particularly for `signature:grade` values).

4. **Language consistency**: Replaces informal terms like "we" with passive voice ("the string is evaluated"), fixes minor typos, and aligns wording with documentation conventions (e.g., "commit-ish" instead of "committish").

This patch serves as preparation for the final linter in the series that will enforce these synopsis style rules. The changes are mechanical but thorough, touching nearly every section of the man page to align with the project's documentation standards. The patch maintains all existing functionality while making the documentation more consistent and machine-verifiable.