This is a documentation and test fix as part of the `git refs list` series. The patch aligns the usage string in `builtin/for-each-ref.c` with the command's man page documentation, resolving a known test failure in `t0450-txt-doc-vs-help.sh`. 

The change introduces a new `COMMON_USAGE_FOR_EACH_REF` macro that consolidates all the command's options into a single string format, including the recently added `--start-after` option. This macro will be shared with the new `git refs list` command in later patches. The patch removes the corresponding "known breakage" marker from the test file since the documentation mismatch is now fixed.

This is a straightforward cleanup that improves consistency between the code and documentation while laying groundwork for the shared usage pattern between `for-each-ref` and the upcoming `refs list` command. The change is well-contained and clearly motivated by fixing both a test failure and improving maintainability through shared definitions.