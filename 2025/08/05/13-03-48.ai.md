This is the first patch in a 6-part documentation series introducing automated linting for Git's man pages. The patch adds a new Perl script `lint-gitlink.perl` that checks for well-formed `linkgit:` macros in documentation files, ensuring they are properly formatted and spelled correctly. It also includes a fix to `gitweb.conf.adoc` where a bare command reference was converted to use the proper `linkgit:` macro syntax.

The linter detects when a Git command reference appears without the required `linkgit:` prefix (like `git-daemon[1]` in the example) and reports it as an error. The script checks for the macro prefix in a 8-character window before any `[section]` reference, allowing for some surrounding context like conditionals. This appears to be part of a broader effort to standardize documentation formatting through automated checks rather than manual review.

The patch touches two files: adding the new linter logic and fixing one instance of the issue in gitweb documentation. Based on the thread context, this is the first of several linters being introduced to enforce consistent documentation style, with subsequent patches covering section delimiters, description lists, parameter formatting, and synopsis structure. The changes conflict with two in-flight topics but appear well-contained otherwise.