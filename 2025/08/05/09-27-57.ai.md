This is a test refactoring patch (5/6) in the `git refs list` series, which prepares test infrastructure for the new `git refs list` command by making existing test logic shareable between `for-each-ref` and the upcoming command.

The patch moves the core test logic from `t6300-for-each-ref.sh` into a new `for-each-ref-tests.sh` library file, replacing hardcoded `git for-each-ref` calls with a `$git_for_each_ref` variable. The original test file becomes a simple driver that sets this variable and sources the shared library. This follows the pattern used for sharing tests between `git-blame` and `git-annotate`.

Key technical details:
- Creates new `t/for-each-ref-tests.sh` (2141 lines) containing all test logic
- Modifies `t/t6300-for-each-ref.sh` to be a minimal driver (14 lines)
- Uses `$git_for_each_ref` variable to make tests command-agnostic
- Preserves all existing test coverage while enabling reuse
- Follows established test sharing pattern from other commands

The change is part of a larger effort to introduce `git refs list` as a thin wrapper around `for-each-ref` functionality. This test refactoring ensures the new command will automatically inherit the comprehensive test coverage of `for-each-ref` without duplication. The series has already addressed all technical and design issues across six versions and appears ready for final review.