Feature implementation completing the `git refs list` subcommand series, now rebased onto Git 2.51.0-rc0 to properly incorporate the `--start-after` documentation. This v6 series from Meet Soni finalizes the architectural foundation for shared ref listing functionality between `git for-each-ref` and the new `git refs list` command.

The key changes in this version are:
1. Documentation rebase to include Karthik Nayak's recently merged `--start-after` option in the shared `for-each-ref-options.adoc` (88 lines)
2. Usage string alignment updates to consistently represent all options including `--start-after` in both commands
3. Preservation of all previously established architectural components:
   - Core logic in `for_each_ref_core()` helper (26-line header)
   - Shared test infrastructure (`t/for-each-ref-tests.sh` at 2141 lines)
   - Minimal `cmd_refs_list()` implementation (14 lines)
   - Documentation synchronization via Asciidoc includes

The series maintains its 6-patch structure from v5, with each commit now properly accounting for the `--start-after` integration. Test coverage remains comprehensive through the shared test library and new `t1461-refs-list.sh` script. The implementation achieves its goal of providing a `git refs list` command that behaves identically to `git for-each-ref` while sharing all core logic and documentation.

This appears ready for final review, having addressed the maintainer's request to rebase onto 2.51.0-rc0 for proper `--start-after` documentation integration. The series shows careful attention to detail in maintaining consistency between the two commands while minimizing code duplication.