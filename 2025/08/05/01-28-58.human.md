This is a follow-up discussion in the feature patch series about expanding `-h` and `--help-all` option availability. The thread has already established that the series will not support argument-tolerant `-h` behavior (like `git add -h foo` showing help while ignoring `foo`), which was a point of earlier discussion.

D. Ben Knoble confirms they mistakenly left mention of argument-tolerant behavior in the cover letter, and Junio C Hamano reiterates why that behavior doesn't make sense - showing help while ignoring other arguments would be confusing. This appears to be a minor clarification rather than reopening the debate, as both agree the feature is properly excluded from the series.

The core series remains focused on making `--help-all` work outside repository contexts while preserving special cases for `ls-remote` and `grep`. The maintainer had already approved the final v3 patches, so this exchange seems to be tying up a loose end in the cover letter text rather than changing the technical direction. The patch series appears ready to proceed as-is.