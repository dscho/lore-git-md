This is the final test patch in the `git refs list` series, completing the test coverage for the new subcommand. The patch adds `t1461-refs-list.sh`, a minimal test driver that leverages the shared test infrastructure created in the previous patch.

The test script works by simply setting `git_for_each_ref` to "git refs list" and then sourcing the comprehensive shared test library (`for-each-ref-tests.sh`). This ensures the new `git refs list` command is tested against the full suite of `git for-each-ref` tests, verifying it behaves as a true drop-in replacement. The approach follows established patterns used for similar command pairs like `git-blame`/`git-annotate`.

This patch represents the culmination of a well-structured series that has evolved through six versions with mentor guidance. The testing strategy is particularly elegant - rather than duplicating tests, it inherits all existing coverage through the shared infrastructure while adding the new command to Meson's test list. The series appears ready for final review, having addressed all technical and documentation requirements.