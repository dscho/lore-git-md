This is a documentation formatting fixup patch in Patrick Steinhardt's reflog migration series. Junio Hamano applies a follow-up change to address Jean-Noël Avila's concern about future doc lint compatibility.

The patch converts option descriptions in `git-reflog.adoc` from `--option::` to `` `--option`:: `` format, ensuring consistency with pending documentation lint rules that will require backtick-wrapped synopses. This affects all option listings for the `drop` and `expire` subcommands, including common flags like `--all`, `--dry-run`, and `--verbose`.

While this change is purely cosmetic, it future-proofs the documentation against stricter formatting checks. The patch maintains the exact same content while adjusting the markup style. This follows the series' first patch which converted the main synopsis format, showing attention to documentation details even in a primarily technical series.

The change is straightforward and uncontroversial, though it does highlight how documentation standards continue to evolve in parallel with code changes. The patch is ready for integration alongside the rest of the reflog series.