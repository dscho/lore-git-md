Test patch completing the `git refs list` series by adding comprehensive test coverage through shared infrastructure.

This final patch in the v6 series introduces `t1461-refs-list.sh`, a minimal test driver that verifies the new `git refs list` command behaves identically to `git for-each-ref` by leveraging the shared test library created in patch 5/6. The test script simply sets `git_for_each_ref` to use the new command before sourcing the existing test library, ensuring all 2141 lines of test cases from `for-each-ref-tests.sh` are run against the new interface.

Key technical points:
- Adds the new test script to meson.build
- 8-line driver script that inherits all existing test coverage
- Maintains identical behavior verification through variable substitution
- Follows the established pattern from other command aliases like `git-blame`/`git-annotate`

The patch completes the test strategy for this feature by ensuring the new command inherits the full test coverage of its predecessor without duplication, while keeping the implementation minimal. This approach was validated through earlier discussion and matches the maintainer's preference for shared test coverage between related commands.