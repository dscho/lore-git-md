Feature patch series for `git refs list` subcommand, now rebased and ready for final review.

This is the sixth version of Meet Soni's series introducing `git refs list` as a thin wrapper around `git for-each-ref`, now rebased onto Git 2.51.0-rc0 to properly incorporate the recently merged `--start-after` option documentation. The series has already addressed all technical and design feedback from previous rounds, with comprehensive test coverage and maintainer approval of the wildcard matching behavior.

The key changes in v6 are purely mechanical - updating the documentation to include the new `--start-after` option in the shared options file and aligning the command usage strings. The series maintains its clean split between the shared ref listing core logic (now in `for_each_ref_core() helper) and the thin command implementations, with extensive test coverage through a shared test library.

With all outstanding issues resolved and the documentation now complete, this appears ready for final review and merging. The implementation provides a well-architected foundation for future ref listing commands while maintaining strict compatibility with existing `for-each-ref` behavior.