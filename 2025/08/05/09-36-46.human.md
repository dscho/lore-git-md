This is a terminological note in a now-closed thread about adding a `--after` flag to `git mv`. The original proposal sought to track file renames made outside Git (e.g., by an IDE), but maintainers rejected it as incompatible with Git's design where renames are detected heuristics rather than explicitly tracked.

Konstantin Khomoutov points FMorschel to Linus Torvalds' 2005 email explaining Git's philosophy, emphasizing that Git fundamentally works with content rather than file operations. The note suggests focusing on two key points from that email: how patches were originally submitted as text diffs, and an example of content unification across files. This serves as a gentle reminder that Git's model differs from file-centric version control systems.

The thread is definitively closed, with this email reinforcing the educational aspect rather than reopening discussion. It provides historical context for why Git handles renames the way it does, which may help the original poster (and other readers) understand the maintainers' rejection of the feature proposal.