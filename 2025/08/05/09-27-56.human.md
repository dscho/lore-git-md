This is patch 4/6 in a series introducing a new `git refs list` command as part of Git's reference management consolidation effort. The patch implements the actual `list` subcommand as a thin wrapper around the shared `for_each_ref_core()` helper that was extracted in the previous patch.

The implementation is straightforward - just 14 lines of code that sets up the command's usage string (reusing the shared `COMMON_USAGE_FOR_EACH_REF` macro) and passes through to the core logic. The documentation is similarly minimal, declaring this as an alias for `git for-each-ref` and including the shared options file that was set up earlier in the series.

This appears to be a well-executed piece of the larger refactoring effort. The patch maintains architectural cleanliness by leveraging the shared infrastructure established in previous patches, and the documentation clearly communicates the command's purpose and relationship to `for-each-ref`. The implementation looks solid and properly integrated into the existing `git refs` command structure.

As part of a GSoC project with multiple experienced mentors involved, this patch series has gone through several iterations and appears ready for final review. The current version addresses all previous feedback and maintains consistency with Git's existing behavior and documentation standards.