This is the final patch in the `git last-modified` series, implementing Bloom filter optimization for the new command. The patch completes the performance optimization work by leveraging changed-path Bloom filters to avoid unnecessary diff computations during the revision walk.

The key technical addition is a new `maybe_changed_path()` function that checks Bloom filters before computing diffs. When filters indicate no possible path changes, the commit can be skipped entirely. The implementation handles edge cases like missing commit-graph data and infinite generation numbers. Memory management follows the project's clarified `_release()`/`_clear()` conventions, properly cleaning up Bloom filter keys.

Performance tests show significant improvements: 50% faster for top-level queries and 36% for subdirectory queries. The recursive case sees minimal impact as expected, since it must examine all commits regardless of path changes. The patch builds on Taylor Blau's earlier Bloom filter work while integrating cleanly with the existing `last-modified` infrastructure.

This completes all planned functionality for the feature, with the series now addressing all edge cases and performance considerations raised during review. The implementation matches the final documented behavior where Bloom filter usage is automatic when available, without changing the command's user-visible semantics.