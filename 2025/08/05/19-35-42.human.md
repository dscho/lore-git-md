Refactoring patch in the merge-ort rename detection series. This is the second of six patches addressing complex edge cases in merge-ort's rename detection logic.

The patch simplifies the directory rename checking code in merge-ort.c by replacing an indirect lookup pattern (using a temporary variable and strmap_get_entry()) with a direct strmap_contains() call. The change makes the code more straightforward while maintaining the same functionality - checking whether a directory rename should be excluded due to conflicts with other renames.

The commit message explains this was a "weirdly coded check" that's now made more natural. The diff shows the removal of the unnecessary temporary variable and the consolidation of the check into a single function call. The comment is also updated to remove reference to the now-removed variable.

This appears to be a straightforward cleanup with no behavior change, part of Elijah Newren's ongoing work to improve merge-ort's robustness. The patch follows the first documentation update in the series and precedes more substantive fixes for edge cases in directory rename handling.