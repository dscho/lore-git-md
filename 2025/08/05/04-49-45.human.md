This is a discussion about platform compatibility approaches in the reftable library, continuing from the v2 patch series that improved reftable/libgit2 compatibility. Patrick Steinhardt responds to Junio Hamano's suggestion of using a shim layer to handle compiler differences, explaining why he prefers targeted fixes over a broader architectural solution.

Patrick acknowledges the existence of a shim layer in libgit2 but argues it should be reserved for fundamental differences like platform-specific system calls, not stylistic variations like struct initialization syntax. He maintains that small, localized changes (like using `{{0}}` instead of `{0}` or replacing `QSORT` with `qsort`) are preferable because they keep the code consistent across implementations while avoiding unnecessary complexity.

The exchange highlights an ongoing tension in the project between immediate compatibility needs (Patrick's focus on keeping libgit2 working with older compilers) and long-term maintainability (Junio's push for systematic solutions). Patrick raises valid points about the risks of divergence between implementations if too much behavior is hidden behind shim layers, though the discussion remains productive and technical rather than contentious.

This email doesn't introduce new code changes but provides important context about the design philosophy behind the reftable compatibility work, which may influence future decisions about how Git handles cross-platform and cross-project code sharing.