This is a discussion about integer type choices in the ongoing commit-graph refactoring series, specifically addressing patch 2/9 which converts bloom filter counts from signed to unsigned integers.

Junio Hamano clarifies his position on the integer type debate, correcting a previous statement about memory allocation calculations. He emphasizes that while the total memory calculation (N*M) must fit in `size_t` and be computed safely, there's no inherent requirement for the count of items (M) itself to be stored as `size_t`. He explicitly states he's fine with the size parameter (N) using `size_t`, but maintains that the count (M) doesn't need to follow suit.

This email doesn't introduce new technical changes but provides important clarification from the project maintainer on the philosophical debate about integer typing in the series. The discussion remains focused on future-proofing and API design rather than blocking issues, with Hamano's position suggesting more flexibility than initially indicated regarding which variables should use `size_t`. The exchange helps refine the project's approach to integer type selection in memory-sensitive contexts.