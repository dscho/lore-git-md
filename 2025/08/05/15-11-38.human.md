This is a refactoring patch in Patrick Steinhardt's reflog migration infrastructure series, specifically addressing flag handling for reflog updates. The patch changes how the `REF_HAVE_OLD` flag is handled for log-only updates (those that only write to reflogs without modifying references). Previously, this flag was explicitly unset for reflog updates, but the patch preserves it while still skipping old-OID verification for log-only cases. 

The change enables future use of `REF_HAVE_OLD` as a signal that the caller provided an old object ID, which will be needed in a subsequent commit. The implementation carefully maintains the existing behavior where log-only updates don't verify old object IDs against the current ref state, while preserving the flag itself. The patch touches multiple backend implementations (files and reftable) to ensure the behavior is consistent across storage formats.

This appears to be a well-considered change that's part of the larger reflog infrastructure work, with clear motivation and thorough implementation across all affected components. The changes are focused and maintain backward compatibility while enabling future functionality.