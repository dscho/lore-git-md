This is the implementation patch (4/6) in the `git refs list` series, introducing the new `list` subcommand as a thin wrapper around the shared `for_each_ref_core()` helper. The patch adds the command implementation and documentation while maintaining consistency with `git for-each-ref`.

The implementation is minimal (14 lines in `builtin/refs.c`) since it leverages the shared core logic extracted in patch 3/6. The new `cmd_refs_list()` function simply calls `for_each_ref_core()` with the appropriate usage string. Documentation is added to `git-refs.adoc` with an Asciidoc include of the shared options file created earlier, ensuring all filtering/sorting/formatting options match `for-each-ref` exactly.

Key technical points:
- Uses the `COMMON_USAGE_FOR_EACH_REF` macro for consistent help text
- Documents the command as an alias for `git-for-each-ref` with identical functionality
- Maintains the same wildcard matching behavior established earlier in the series
- Follows the pattern of other `git refs` subcommands in both implementation and docs

The patch appears clean and well-integrated with the architectural foundation laid by previous patches in the series. It completes the core functionality portion of the `git refs list` feature while leaving test coverage to the subsequent patches. The documentation properly indicates this is not new functionality but rather a consolidation under the `git refs` namespace.