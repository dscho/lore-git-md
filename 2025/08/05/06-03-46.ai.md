This is a brief follow-up in the now-closed `git mv --after` feature proposal thread. Johannes Sixt expresses personal sympathy for the idea but notes that in Git's terminology, such a flag would more naturally be called `--cached` rather than `--after`. The email serves as a minor terminological correction rather than reopening the discussion - the maintainer's earlier rejection of the fundamental concept remains unchanged.

Key technical detail is the suggestion to use `--cached` as the flag name, which aligns with Git's existing terminology for operations that work with the index rather than the working tree. This reinforces the thread's established understanding that `git mv` is fundamentally an index operation (`rm` + `add`) rather than a true rename-tracker.

The email doesn't introduce new arguments or technical considerations, but does indicate that the original use case (handling IDE-initiated moves) resonates with at least one experienced contributor, even if Git's design precludes the proposed solution. No code changes or follow-up actions are suggested - this appears to be a conversational coda to the thread.