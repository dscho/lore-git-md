This is a discussion about integer types in the commit-graph bloom filter refactoring, part of Patrick Steinhardt's series to remove global state dependencies.

Junio Hamano clarifies his position on using `size_t` for counting bloom filters in response to Patrick's patch. While agreeing that memory allocations must fit in `size_t`, he notes there's no inherent need to count the number of bloom filters (M) using `size_t` - the memory size (N) of each filter should use `size_t`, but the count can use a regular integer type. This refines his earlier feedback where he questioned using `size_t` for all counters.

The exchange highlights an ongoing philosophical discussion in the series about when to use platform-native integers versus memory-specific types. Hamano's stance appears pragmatic - use the right type for the specific purpose rather than enforcing blanket consistency. This follows earlier reviews where Taylor Blau also questioned some of the integer type changes while generally supporting the hash algorithm improvements.

The thread shows careful technical review of the series' implementation choices, even as the core architectural changes (removing `the_repository` usage) are uncontested. The discussion remains focused on future-proofing rather than blocking issues.