Test isolation patch in the editor test suite, part of a finalized 2/2 series addressing exec-path security concerns through preparatory cleanups. This patch specifically focuses on preventing test pollution in t7005-editor.sh by properly cleaning up environment variables and config settings between tests.

The changes ensure that tests manipulating EDITOR/VISUAL/GIT_EDITOR environment variables or core.editor config don't leak those changes to subsequent tests. The patch replaces direct git config calls with test_config (which automatically cleans up) and adds explicit unset calls for environment variables. The modifications preserve the test's ability to verify editor override behavior while preventing side effects.

Key technical details:
- Adds `unset EDITOR VISUAL GIT_EDITOR` after a test block that may modify them
- Replaces `git config core.editor` with `test_config core.editor` for automatic cleanup
- Maintains all existing test assertions about editor behavior
- Touches only t/t7005-editor.sh
- Follows test_when_finished pattern suggested in thread discussion

This is a straightforward improvement to test hygiene that was uncontroversial in the thread discussion. It builds on the consensus that environment handling improvements should focus on internal cleanup rather than modifying PATH behavior. The patch aligns with the series' narrowed scope to address only well-agreed infrastructure improvements.