Performance test addition for the completed `git last-modified` feature.

This patch adds a performance test script for the new `git last-modified` command, which has been through extensive development and is now feature-complete. The test script `p8020-last-modified.sh` measures three scenarios: basic top-level operation, recursive mode, and operation within a subdirectory. These complement the existing correctness tests in the regular test suite by focusing specifically on performance monitoring.

The test cases are straightforward but cover the key usage patterns: simple invocation (`git last-modified HEAD`), recursive traversal (`-r` flag), and path-specific operation (extracting a subdirectory path via `git ls-tree`). The script follows the standard perf test structure and will help detect any performance regressions in future versions.

This is part 2 of 3 in the final v7 series that prepares the `last-modified` feature for merging. The thread context shows the command has already undergone thorough technical review, with all edge cases handled and documentation finalized. The performance tests represent one of the last remaining pieces before the feature can be considered complete.