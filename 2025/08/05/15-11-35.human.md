This is a type-safety refactoring patch in Patrick Steinhardt's reflog migration infrastructure series. It changes the length parameter of `split_ident_line()` from `int` to `size_t` to better match its actual usage, as callers typically pass string lengths from `strlen()` or `strbuf` operations which return `size_t`. The change is straightforward - just updating the function signature in both the declaration and implementation files.

The patch is part of a larger effort to improve reflog handling (particularly for the upcoming reftable backend), though this specific change is more about code hygiene than direct functionality. It's a small but sensible improvement that makes the API more type-correct, reducing potential confusion about negative length values that could never legitimately occur in practice.

Since this is patch 5/9 in the series, it builds on earlier groundwork while setting up for later patches that will use this function. The change appears uncontroversial and well-justified, with no observable behavior change beyond making the code's intent clearer.