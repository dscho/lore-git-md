This is a type safety refactoring patch in Patrick Steinhardt's reflog migration infrastructure series (v5 5/9). The change modifies the `split_ident_line()` function to use `size_t` for its length parameter instead of `int`, since callers consistently pass positive values from `strlen()`, `strbuf::len, or pointer differences. The patch updates both the declaration in ident.h and implementation in ident.c with this more appropriate type.

The change is straightforward and mechanical, with no behavior modifications - it simply aligns the function signature with its actual usage patterns. This appears to be a preparatory change for the larger reflog work in the series, ensuring type safety when handling identity parsing during reflog operations. The patch maintains the same return value (int) for error signaling while making the length parameter's type match its semantic meaning.

This fits into the series' broader focus on robust reflog handling, though it stands alone as a clean-up of an existing API. The change is small (2 lines) and well-justified, with no apparent downsides or compatibility concerns given the function's documented expectations around its parameters.