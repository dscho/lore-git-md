Test isolation patch in the editor test suite. This is part of a now-reduced 2-patch series addressing environment handling in Git's editor-related code.

The patch fixes test pollution in t7005-editor.sh where environment variables and config settings from earlier tests could affect later ones. It properly cleans up EDITOR/VISUAL/GIT_EDITOR variables and core.editor config after the override tests complete. The changes use test_config (which automatically cleans up) and explicit unset operations.

This appears to be a straightforward test hygiene improvement that follows Patrick Steinhardt's suggestion from earlier discussion. The changes are minimal and focused - just adding cleanup while preserving the test's ability to verify override behavior. As part of the uncontroversial portion of the original series, this should be ready for inclusion.