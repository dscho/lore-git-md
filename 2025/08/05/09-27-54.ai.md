This is a documentation synchronization patch in the `git refs list` series, specifically addressing alignment between the `git for-each-ref` usage string and its man page. The patch fixes a known test breakage in `t0450-txt-doc-vs-help.sh` by updating the command's usage string to match the documentation.

The key change moves the usage string into a shared `COMMON_USAGE_FOR_EACH_REF` macro that will be reused by both `for-each-ref` and the new `refs list` command. The macro includes all documented options in a single string format, including the recently added `--start-after` option. This change allows removal of the 'known breakage' marker from the test file since the usage string now properly matches the documentation.

The patch demonstrates good attention to detail in maintaining consistency between code and documentation. It's part of a larger effort to share infrastructure between `for-each-ref` and the new `refs list` command, with this change establishing the pattern for shared option documentation that will be used throughout the series. The fix is straightforward and well-contained, with clear test impact (removing one line from the known-mismatches file).