This is a follow-up in the ongoing discussion about improving `git stash --keep-index` behavior to better handle unstaged changes during interactive rebases.

Phillip Wood responds to Junio Hamano's question about whether the proposed `--unstaged` behavior should be enabled by default rather than requiring a new option. Wood agrees this could make sense when the stash was created with `--keep-index`, but notes there's currently no way to detect this after creation. He suggests adding a trailer to the stash commit message to record when `--keep-index` was used, which would allow automatically applying only unstaged changes in those cases.

The email continues the thread's exploration of how to safely restore unstaged changes without conflicts, with Wood maintaining his position that the functionality is valuable while acknowledging Hamano's concerns about changing default behavior. The technical discussion remains focused on backward compatibility and how to properly mark stashes created with `--keep-index` to enable smarter restoration behavior.

This exchange shows the discussion moving from technical feasibility (demonstrated by Wood's earlier script) toward design decisions about how to integrate the functionality into Git's existing stash system while respecting backward compatibility concerns raised by the maintainer.