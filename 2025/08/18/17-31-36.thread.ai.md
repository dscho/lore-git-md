[AI thread summary]

Junio C Hamano's "What's cooking" report provides a snapshot of Git development progress across multiple integration branches (maint, master, next, seen). The report organizes work into new topics, cooking topics, and those needing rerolls.

Documentation improvements include `git-clone` synopsis updates, `git-log` markup fixes, and man page linting rules. New user-facing features: `git refs list` as a `for-each-ref` frontend, `git diff-tree --max-depth`, and experimental `git last-modified` for file history tracking. Performance work includes commit-graph de-globalization, Bloom filter wildcard pathspec support, and `git describe` optimization with priority queues.

Merge strategy fixes address `ort` rename handling bugs. Infrastructure changes introduce `git repo` subcommand for repository queries and sparse-checkout `clean` action for unused file pruning. `git send-email` gains IMAP folder support.

Topics needing rerolls: promisor-remote capability, sparse checkout variables, MIDX API improvements. Ongoing discussions cover deprecating `core.commentChar=auto` and xdiff hash function improvements. The report coordinates development across documentation, performance, new features, and infrastructure work.