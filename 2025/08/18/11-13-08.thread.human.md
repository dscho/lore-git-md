The thread addresses a series of bugfixes from SZEDER Gábor for Git's line-log functionality (`git log -L`), triggered by Evgeni Chasnovski's report of assertion failures when using multiple `-L` range specifications. The initial patch fixed three related issues: an assertion failure in `range_set_append()`, an infinite loop in range calculations, and incorrect output showing unmodified line ranges. The second patch, just submitted, fixes another long-standing output correctness issue where changes affecting multiple line ranges would only be shown for the first range.

The fixes involve careful adjustments to boundary condition handling in `line-log.c`, with the second patch specifically correcting `dump_diff_hacky_one()` to properly handle cases where a single diff range touches multiple disjoint line ranges. Interestingly, the bug had been present since line-log's 2013 introduction, and two existing test cases had incorrect expected output that the patch now corrects. The changes are relatively small (6 and 9 lines respectively) but significantly improve correctness, with comprehensive test updates covering both SHA-1 and SHA-256 repositories. The series appears complete, addressing all known issues with multiple `-L` specifications.