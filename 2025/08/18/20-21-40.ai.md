This is a continuation of the `builtin/describe.c` thread, focusing on edge case behavior decisions for blob-describing functionality. Jeff King responds to René Scharfe's patch and analysis, clarifying several key points about the feature's behavior.

The discussion centers on four specific edge cases in blob description behavior, with Jeff agreeing with René's approach for most but noting some nuances. For case 1 (blob not found in traversal), they agree error reporting is better than the current blank line output. Case 2 (unborn branches) should match case 1's behavior - either both error or both return empty. Case 3 (no commit found) is best handled by early termination as René proposed.

Jeff questions whether `--always` should be supported for blobs, noting it might be better to keep the feature contained rather than expand its option set. He also critiques the current error messaging from `describe_commit()` as poorly suited for blob cases. The historical confusion around the feature's original merge status (referencing commits 644eb60bd0 and 15af58c1ad) is noted but deemed separate from the current fixes.

The email concludes with Jeff indicating he'll send patches addressing these cases, suggesting the discussion has moved from theoretical concerns to concrete implementation decisions. The thread remains focused on whether to minimally fix the feature or consider its deprecation, with Jeff appearing to favor the former path while acknowledging the feature's questionable utility.