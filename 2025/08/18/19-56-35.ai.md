This is a technical discussion about edge cases in Git's blob-describing functionality, building on the ongoing thread about `builtin/describe.c`. René Scharfe responds to Jeff King's enumeration of four edge cases in the blob-describing feature, proposing a concrete patch to address two of them while questioning the feature's continued existence.

The email focuses on edge cases (1) and (3) from Jeff King's list: handling blob traversal failures and detecting missing commits. René suggests two alternative approaches - either maintaining the current output (blank line) or returning an error - and provides a patch implementing the error case. The patch modifies `process_object()` to check for NULL commits and `describe_blob()` to detect when no description was produced.

Key technical details in the patch:
- Adds NULL commit check in `process_object()`
- Introduces `orig_len` tracking in `describe_blob()`
- Changes error handling to die() when blob can't be described
- Maintains existing path formatting when successful

René raises historical concerns about the feature's origins, noting confusion between commits 644eb60bd0 and 15af58c1ad that suggest blob-describing may have been intentionally merged. The response maintains skepticism about the feature's utility while pragmatically addressing immediate technical issues.

The discussion continues to balance between:
- Fixing immediate safety/consistency issues
- Questioning the feature's long-term viability
- Maintaining backward compatibility concerns
- Weighing documentation versus implementation changes

This moves the thread forward by providing concrete code while keeping open the larger question of whether to invest in fixing all edge cases or deprecate the functionality entirely. The patch serves as a minimal solution that could be extended if the community decides to keep the feature.