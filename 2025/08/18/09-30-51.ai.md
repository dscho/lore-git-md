This is a discussion response in the ongoing thread about submodule handling with mixed hash algorithms (SHA-1 vs SHA-256). Michael Schroeder pushes back against Brian M. Carlson's assertion that mixed-hash submodules should not work at all, arguing that the current behavior (with padding/truncation) is already functional in practice for non-shallow clones.

Key technical points in this response:
1. Documents current behavior: SHA-1 main repo + SHA-256 submodule truncates the commit hash in gitlinks, while SHA-256 main + SHA-1 submodule pads with zeros
2. Cites commit 52fca06db2 as enabling the padded-zero case to work via prefix matching
3. Maintains that the shallow clone case is the only real problem needing fixing
4. Expresses skepticism about Brian's proposed solution using compatObjectFormat, suggesting it wouldn't solve the shallow clone issue

The email highlights the tension between the pragmatic approach (fixing what users are already doing) versus the architecturally pure approach (enforcing hash algorithm consistency). Michael's position suggests the current patch series (focused on shallow clone fixes) should proceed independently of the larger architectural questions about mixed-hash viability.

This response doesn't introduce new code changes but provides important context about real-world usage patterns that weren't previously discussed in the thread. The exchange surfaces deeper questions about whether Git should formally support mixed-hash submodules or treat them as unsupported edge cases.