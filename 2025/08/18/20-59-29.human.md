This is a refactoring patch that's part of a larger series (1/5) addressing edge cases in `git describe`'s blob handling. The patch makes a straightforward change to pass `struct object_id` by const pointer rather than by value in `describe_blob()` and related functions, bringing it in line with Git's typical conventions.

The change is purely mechanical - it doesn't affect behavior but improves consistency with the codebase's style. The author notes this isn't directly related to the main series' goals but was noticed while working in the area. This appears to be a clean, uncontroversial preparatory change before the more substantive fixes in subsequent patches.

The patch is small (4-line change) and focused, with no test changes needed since it's just an internal refactor. It follows the pattern seen elsewhere in Git where object IDs are passed as const pointers rather than by value. This sets up cleaner interfaces for the more complex changes coming later in the series.