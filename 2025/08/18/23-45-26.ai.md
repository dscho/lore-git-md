This is a notification about ongoing usage of the deprecated `git whatchanged` command in the Jenkins Git client plugin. 

Mark Waite reports that despite Git 2.51.0's deprecation of `git whatchanged`, the Jenkins Git client plugin still relies on this command for generating changelogs. The email serves both as a notification of continued usage (as requested by Git's deprecation warning) and as a heads-up that a replacement implementation is being worked on, though still in early stages.

The message includes the standard deprecation warning text that appears when running `git whatchanged` without the required `--i-still-use-this` flag. This suggests the Jenkins plugin is encountering this error, though the email doesn't specify whether the plugin is currently using the flag as a workaround.

This appears to be a straightforward compliance notification rather than a technical discussion - the author is acknowledging the deprecation and indicating plans to update the Jenkins plugin, but isn't seeking input or raising any particular issues about the deprecation itself. The message serves primarily to document continued usage of the deprecated command from a major CI system.