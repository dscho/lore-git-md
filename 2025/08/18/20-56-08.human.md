This is a follow-up discussion in the ongoing thread about edge case handling in `git describe`, specifically focusing on blob description behavior.

Jeff King identifies another edge scenario where `git describe` fails to describe a blob when no tags exist in the repository. While initially appearing buggy, he realizes this is consistent with the command's current design of describing the containing commit rather than the blob itself. 

The email highlights an inconsistency between the command's behavior and documentation - the `--always` flag works in this case despite not being documented to support blob arguments. This observation connects back to the earlier discussion about whether to expand `git describe`'s interface to properly support blobs.

The note maintains the thread's technical depth while raising practical questions about documentation accuracy and interface design. Jeff's analysis suggests the current behavior might be useful despite the documentation mismatch, leaving open whether to fix the docs or restrict the behavior. This builds on the thread's established consensus about most edge cases while keeping the `--always` flag question active.