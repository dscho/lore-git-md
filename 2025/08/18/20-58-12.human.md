This is a bugfix patch series addressing edge cases in `git describe` for blob objects, building on previous safety improvements to `builtin/describe.c`. The series comes from Jeff King and appears to implement the consensus reached in the ongoing thread about handling various error conditions when describing blobs.

The 5-part series:
1. Makes technical improvements by passing OID structs as const pointers
2. Adds explicit error handling when a blob isn't found
3. Catches the unborn branch case in blob description
4. Handles blob traversal when no commits exist
5. Adjusts commit description to take a commit parameter directly

The changes touch both the core describe logic and add new test coverage. Based on the thread context, this implements the agreed-upon solutions for several edge cases that were previously discussed, including blob traversal failures and unborn branch handling. The series appears to be the implementation follow-through to the theoretical discussion, with Jeff King now providing concrete patches that match the consensus positions from earlier in the thread.

The changes look focused and well-motivated by the prior discussion. The test additions suggest good validation of the new behaviors. This seems like a solid technical contribution that moves the thread toward resolution of the identified issues.