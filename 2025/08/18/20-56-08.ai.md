This is a technical follow-up in the ongoing `builtin/describe.c` edge case discussion, focusing on the `--always` flag behavior for blobs.

Jeff King examines another edge case where `git describe` fails on a blob when no tags exist in the repository. While initially appearing buggy, he realizes the behavior is correct since the command attempts to describe the blob's commit (which requires tags). He notes the `--always` flag, though undocumented for blob use, actually provides useful output in this case - showing the blob's hash when description fails.

The email reinforces the thread's central tension around `--always` support for blobs, demonstrating a concrete case where the flag's undocumented behavior proves useful. This adds weight to the open question about whether to officially support this interface expansion or maintain strict documentation alignment. The example also implicitly highlights the need for clearer error messaging about the commit traversal behavior when describing blobs.

Key technical details carried forward:
- Blob description still depends on commit traversal (via HEAD)
- `--always` works practically for blobs despite docs saying otherwise
- Error case when no tags exist differs from other edge cases
- Demonstrates real-world scenario where current behavior is confusing but correct