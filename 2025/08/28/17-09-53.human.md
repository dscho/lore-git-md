This is a documentation discussion thread about clarifying the "garbage" field in `git count-objects -v`. The thread has already established that the current implementation correctly counts only filesystem artifacts (like temporary files and lock files) rather than Git's internal garbage objects, but the documentation doesn't make this distinction clear.

Daniele Sassoli now raises the point that the term "garbage" itself may be misleading, as multiple users (including those at the Git Mini Summit and on community platforms) naturally associate it with garbage-collectable objects. They suggest either improving the documentation or possibly renaming the field to better reflect its actual purpose.

The technical consensus from earlier in the thread remains unchanged - the implementation behavior is correct and intentionally lightweight compared to `git fsck` or `git gc`. The discussion has now shifted to user experience concerns about terminology clarity. Daniele appears to be preparing to submit a documentation improvement patch but wants to confirm the approach first.