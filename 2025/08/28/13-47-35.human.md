This is a follow-up discussion about edge cases in the recently merged test reorganization for Git command help output testing. D. Ben Knoble responds to Phillip Wood's post-merge observations about two issues in the automated testing approach in t1517-outside-repo.sh.

The first issue involves the test script examining all `git-*` commands in $PATH, including non-core scripts, which causes test failures in some developer setups. Phillip suggests restricting to $GIT_EXEC_PATH only. Knoble notes this should only happen with GIT_TEST_INSTALLED or exotic setups, and can't reproduce the issue with his distro-installed Git.

The second issue concerns the `-i` flag not stopping at the first failure in test loops as expected. Knoble reports being unable to reproduce this behavior, though Phillip observed it in his environment. Both issues appear to be environment-specific edge cases that weren't caught during development.

The exchange shows careful post-merge review catching subtle interaction cases, though the exact conditions triggering the problems remain unclear. The thread suggests these may need follow-up fixes to make the tests more robust across different environments.