This is a follow-up discussion in the `git range-diff` memory limit patch series, focusing on the error message wording and configuration approach. Junio Hamano responds to Elijah Newren's suggestion about the memory limit error message while raising a broader design question about the feature's implementation.

The thread has already established the core memory limit functionality as technically sound (Acked-by from Johannes Schindelin), but now addresses two new aspects:

1. **Error message clarity**: Newren and Hamano agree the current "X bytes available" phrasing could mislead users into thinking it reports system memory rather than the configured limit. They propose rewording to "limited to X bytes" to make the constraint's origin clearer.

2. **Configuration strategy**: Hamano questions whether the feature should start as a command-line-only option before adding config support, suggesting a phased approach: first prove utility with CLI options, then add config if users demonstrate repeated need. This contrasts with the current patch's direct introduction of both mechanisms.

The discussion occurs while the series' first patch (option reordering) faces objections, but this email focuses solely on the memory limit implementation in patch 2/2. The core technical solution remains uncontested, but the user-facing details are being refined through maintainer feedback.

Key technical references:
- Memory limit check in `get_correspondences()`
- Human-readable byte formatting via `strbuf_humanise_bytes()`
- Proposed error message change from "available" to "limited to"
- Question about `rangeDiff.maxMemory` config vs. `--max-memory` CLI option introduction strategy

This represents typical late-stage patch refinement where core functionality is agreed upon but interface details receive scrutiny from maintainers. The author will need to address both the wording change (a straightforward update) and the more substantive question about configuration strategy in their next revision.