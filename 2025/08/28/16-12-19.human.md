This is a bugfix patch for submodule configuration handling. Haihua Yang addresses an issue where submodule core.worktree configuration updates weren't properly respecting the config.worktree file when it exists. 

The patch modifies `builtin/submodule--helper.c` to first check for a config.worktree file in the submodule's git directory before falling back to the regular config file. This matches Git's standard behavior for configuration precedence where worktree-specific configs should take priority.

The change is small (6 lines added, 1 removed) and focused, with a clear explanation of the intended behavior. The patch appears technically sound, though it might benefit from a mention of any specific scenarios where the current behavior causes problems (the commit message doesn't provide concrete examples of breakage).

This seems like a straightforward improvement to submodule configuration handling that aligns with Git's existing configuration file hierarchy rules. The change is unlikely to be controversial but could use more thorough testing documentation to demonstrate the fix in action.