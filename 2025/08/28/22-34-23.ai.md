This is a documentation patch in Julia Evans' ongoing series to clarify `git-checkout.adoc`. The email addresses a specific technical point about how Git interprets arguments to `git checkout`, correcting a suggested wording that would have implied Git treats arguments as tree-ish by default when in fact it requires commit-ish for branch switching.

Julia notes that `git checkout <tree>` is invalid (producing a "Cannot switch branch to a non-commit" error), which contradicts the proposed language about default tree-ish interpretation. This builds on Junio Hamano's earlier clarification about tree-ish vs commit-ish distinctions in the thread. The exchange shows careful attention to technical precision in the documentation, with Julia catching an edge case where the suggested wording didn't match actual Git behavior.

The thread context shows this is part of final polishing touches on the documentation series, with most major technical questions already resolved. This particular correction helps ensure the docs accurately reflect implementation behavior around argument parsing, maintaining the series' focus on technical accuracy while improving clarity.