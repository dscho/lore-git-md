This is a minor technical question about the implementation details in patch that's part of the Git for Windows GUI credential helpers series. Johannes Sixt points out an inconsistency in how paths are constructed between the SSH_ASKPASS and GIT_ASKPASS environment variable assignments in git-gui.sh.

The patch uses `[gitexec git-gui--askpass]` for GIT_ASKPASS while the surrounding code uses `[file join $argv0dir git-gui--askpass]`. Sixt asks whether this difference is intentional (which would require updating the commit message to explain it) or if it should be made consistent with the surrounding code.

This appears to be a straightforward implementation detail in an otherwise uncontroversial patch series that's already been reviewed and signed off by multiple maintainers. The question doesn't challenge the overall approach but seeks clarification on a minor coding style point. The original author (Johannes Schindelin) can likely resolve this quickly with either an explanation of the rationale or a simple adjustment to match the surrounding style.