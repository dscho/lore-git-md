This is a bugfix patch in a series addressing issues with multi-pack-index (midx) writing. It's part of a 5-patch set that started with a critical segfault fix in `git multi-pack-index repack` operations, with subsequent patches cleaning up related code.

The patch restores an error handling behavior that was accidentally removed in a previous commit (fcb2205b77). The change ensures that when `fill_packs_from_midx()` fails, the function properly sets the result to 1 before jumping to cleanup. This maintains consistent error reporting behavior.

The fix is straightforward - just a single line addition - and appears to be a minor oversight in the original implementation. As part of a series that's already addressed the more serious crash issue, this seems like a logical follow-up cleanup. The patch comes from Derrick Stolee, who is deeply familiar with this area of the codebase.