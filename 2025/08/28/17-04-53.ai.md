This is a performance and safety patch for `git range-diff`, part of the ongoing series addressing memory exhaustion issues. Elijah Newren provides a focused review of the error messaging in the memory limit implementation, suggesting clearer wording to avoid user confusion.

The key suggestion is to rephrase the error message about memory limits to avoid implying system-wide memory availability. The current wording says "X bytes available" which could mislead users into thinking it reports their system's free memory rather than the configured limit. Newren proposes changing "available" to "limited to" for clarity.

The technical context remains unchanged from the thread summary: this implements configurable memory limits for the n×n cost matrix calculation in `range-diff` to prevent excessive memory usage when comparing large commit ranges. The patch adds size calculations and human-readable error messages using `strbuf_humanise_bytes()`.

Newren explicitly states the rest of the patch looks good ) (using parentheses to indicate this is his assessment, not the AI's), suggesting the core technical approach remains sound. This review focuses narrowly on user-facing messaging while implicitly accepting the underlying memory limit mechanism that Johannes Schindelin previously acked.

The thread's main unresolved question about option ordering in the first patch is unaffected by this review, which deals exclusively with the second patch's implementation details. Newren's feedback appears easily addressable with a wording tweak in the error message.