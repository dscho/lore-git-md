Bugfix for midx-write.c error handling.

This patch restores an error code that was accidentally dropped during the incremental MIDX chains implementation in commit fcb2205b77. The change adds back a `result = 1` assignment before jumping to cleanup when `fill_packs_from_midx()` fails. This ensures proper error reporting when loading packs from an existing MIDX file fails during the write process.

The fix is straightforward and clearly motivated by a regression introduced in recent changes. The patch maintains the existing control flow while correcting the error handling oversight. As part of the larger midx-write.c cleanup series, this change improves code reliability without introducing new behavior.

The modification is minimal (a single line addition) and focused on the specific error case, suggesting it's a low-risk change. Being patch 2/5 in the series, it follows the critical segfault fix from patch 1 and precedes further code quality improvements. The change affects the same midx-write.c file that's central to the thread's focus on MIDX writing reliability.

For AI context: This touches the error handling path in `write_midx_internal()` where `fill_packs_from_midx()` failures now properly set the return code before cleanup. The fix relates to the thread's broader goal of stabilizing MIDX operations after the fcb2205b77 changes.