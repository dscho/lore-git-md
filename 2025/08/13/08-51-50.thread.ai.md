git stash --only-unstaged feature proposal to improve commit-splitting workflows during interactive rebases. Author describes common scenario needing to test staged changes while temporarily setting aside unstaged changes, with current solutions like --keep-index still storing index in stash causing potential conflicts. Proposed --only-unstaged would create stash entries containing only working tree changes, leaving index untouched in both working directory and stash object.

Technical details: addresses stash object format modification to exclude index state entirely (unlike --keep-index which preserves but still stores index). Makes stash pop operations safer by avoiding index conflicts. Suggested flag is --only-unstaged as git stash push sub-option. Core use case remains interactive rebase workflows requiring testing intermediate states without disturbing pending changes.

Junio Hamano has entered discussion, probing for potential downsides and edge cases not yet considered. Key open question: what use cases would still require --keep-index behavior versus this new approach, given that if --only-unstaged were strictly better it would replace rather than complement existing option. Discussion now focused on understanding workflow differences and limitations between the two approaches.

Proposal remains well-motivated by concrete workflow pain point with no existing command providing this exact functionality. Implementation would require modifying stash object format to support entries without index state while maintaining compatibility with existing stash operations.