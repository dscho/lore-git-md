git stash --only-unstaged feature proposal to improve commit-splitting workflows during interactive rebases, with implementation discussion now covering both technical mechanics and documentation alignment. Original motivation centered on testing staged changes while temporarily setting aside unstaged changes, addressing current --keep-index limitations where pop operations cause conflicts due to working tree state in both stash commits.

Phillip Wood demonstrated technical feasibility via shell script using git merge-tree with stash's index state as merge base, creating temporary index file ($GIT_DIR/stash-pop-index) to preserve real index state during conflict resolution. The current proposal involves adding a trailer to stash commit messages when created with --keep-index (likely format `Stash-created-with: --keep-index`), enabling automatic unstaged-change-only behavior during pop operations while maintaining backward compatibility.

Wood responds to Ben Knoble's concerns about the current --keep-index example in documentation, confirming that conflicts will occur when popping the stash if any staged changes were edited during testing, though conflicts may be avoided for lines that matched across index and worktree at stash creation. Two paths forward emerge: update documentation to explicitly mention expected conflicts (simpler), or implement Wood's script-based approach to avoid conflicts entirely (better UX but more complex implementation). Wood prefers the latter if user feedback confirms need, awaiting input from original requester J. Dettweiler.

Key technical details include: merge-tree mechanics for conflict resolution, conditions triggering conflicts (editing staged-change lines), and staged/unmodified line handling distinctions. Open questions remain about: edge cases in merge conflict resolution with trailer present, whether to modify documentation or behavior to align with examples, and potential future replacement of --keep-index entirely. The thread shows technical consensus forming around the trailer mechanism, with Wood (sequencer/rebase expert) driving implementation, Junio Hamano (maintainer) having provided high-level design feedback, and Knoble (tooling) verifying documentation consistency. Discussion has progressed from design validation to concrete implementation choices with parallel documentation concerns.