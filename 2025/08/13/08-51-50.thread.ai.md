git stash --only-unstaged feature proposal to improve commit-splitting workflows during interactive rebases, with ongoing discussion about whether to modify --keep-index behavior or introduce a new option. Original motivation centered on testing staged changes while temporarily setting aside unstaged changes, with current --keep-index causing conflicts during pop operations due to working tree state in both stash commits.

Phillip Wood demonstrates technical feasibility via shell script using git merge-tree with stash's index state as merge base, creating temporary index file ($GIT_DIR/stash-pop-index) to preserve real index state during conflict resolution. Junio Hamano questions design direction, suggesting --keep-index's original behavior might suffice if users avoid modifying working tree files during testing, while Wood counters with practical workflow needs.

New development: Wood proposes adding a trailer to stash commit messages when created with --keep-index, enabling automatic unstaged-change-only behavior during pop operations while maintaining backward compatibility. This addresses Hamano's concern about default behavior by making it opt-in via the original stash creation command. Implementation would still need to handle the three-way merge mechanics from Wood's script, particularly conflict cases where adjacent lines were modified.

Open questions include whether this trailer-based approach satisfies all use cases, potential edge cases in merge conflict resolution, and whether the solution should eventually replace --keep-index entirely. The thread shows convergence toward a solution that preserves existing behavior while adding smarter handling for the interactive rebase testing scenario, with Wood's technical proposals carrying significant weight given his sequencer and rebase expertise. Discussion remains focused on balancing workflow improvements with Git's stability guarantees.