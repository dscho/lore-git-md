git stash --only-unstaged feature proposal to improve commit-splitting workflows during interactive rebases, with ongoing discussion about modifying --keep-index behavior via a backward-compatible trailer-based approach. Original motivation centered on testing staged changes while temporarily setting aside unstaged changes, addressing current --keep-index limitations where pop operations cause conflicts due to working tree state in both stash commits.

Phillip Wood demonstrated technical feasibility via shell script using git merge-tree with stash's index state as merge base, creating temporary index file ($GIT_DIR/stash-pop-index) to preserve real index state during conflict resolution. Junio Hamano initially questioned the design direction, suggesting --keep-index's original behavior might suffice if users avoid modifying working tree files during testing, while Wood countered with practical workflow needs.

Current proposal involves adding a trailer to stash commit messages when created with --keep-index (likely format `Stash-created-with: --keep-index`), enabling automatic unstaged-change-only behavior during pop operations while maintaining backward compatibility. Wood confirms the trailer approach works with stash import/export since commit messages are preserved verbatim, addressing Ben Knoble's cross-boundary compatibility concern. Knoble had raised additional questions about documentation examples producing unnecessary conflicts, which Wood acknowledges while reiterating the trailer solution's advantages.

Implementation would still need to handle three-way merge mechanics from Wood's script, particularly conflict cases where adjacent lines were modified. Open questions include: edge cases in merge conflict resolution when the trailer is present, and whether the solution should eventually replace --keep-index entirely. The thread shows technical consensus forming around the trailer mechanism, with Wood (sequencer/rebase expert) driving implementation details, Hamano (maintainer) providing high-level design feedback, and Knoble (tooling) verifying cross-feature compatibility. Core discussion has shifted from design validation to precise metadata format and edge case handling.