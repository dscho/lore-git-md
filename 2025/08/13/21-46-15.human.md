This is a discussion email in the ongoing `git repo` command hierarchy series, specifically about the output ordering behavior of the new `git repo info` subcommand.

Eric Sunshine weighs in on the debate about whether `git repo info` should preserve input order or sort/deduplicate its output. He clarifies that Phillip Wood's earlier argument was for dropping both deduplication and sorting entirely, not just one or the other. Eric points out that while Lucas's concern about error cases breaking strict ordering is valid, it's not a strong enough reason to keep the current sorting behavior for scripting use cases.

The email highlights two key technical considerations: the performance impact of process creation (especially Windows) when querying keys individually, and the tradeoff between implementation simplicity and script usability. Eric ultimately sides with Phillip's position, finding the argument for input-order preservation compelling for scripting scenarios.

This discussion represents the final design decision point for the series, with consensus forming around dropping the current sorting/deduplication approach in favor of simpler input-order preservation, even if it means allowing duplicate keys and repeated error messages. The series is otherwise ready for merging with this change.