This is a test modernization patch series with performance optimization feedback from Phillip Wood. The thread continues discussion of D. Ben Knoble's v4 series cleaning up editor-related test infrastructure, now focused on test execution efficiency.

Phillip Wood provides a performance optimization note about `test_env` usage in the test suite, pointing out that direct environment variable assignment (`FOO=bar cmd`) is more efficient than wrapping in `test_env` when not calling shell functions. He shows this pattern is widely used elsewhere in the test suite (152 instances vs just 1 for `test_env` with `test_must_fail`). While not requesting a re-roll for this v4 series, the feedback is intended to guide future test writing.

The email also confirms approval of the test cleanup work ("Thanks for cleaning up this test file, it's looking much nicer now"), indicating the technical content of the patches themselves remains uncontroversial. The discussion has now shifted from the initial test modernization and security concerns to performance optimizations in test execution patterns.

Key technical detail for future reference: The performance-critical distinction between `test_env VAR=val cmd` (spawns extra process) versus direct `VAR=val cmd` assignment, with the latter being preferred except when calling shell functions. This affects test suite runtime efficiency but not functional correctness.