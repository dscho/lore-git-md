This is a discussion about Git diff output formatting, specifically the display of "no newline" markers when comparing files that both lack terminating newlines. Junio C Hamano weighs in on the thread, supporting Phillip Wood's earlier position that the current behavior is correct.

The key technical point is that each "\ No newline" marker applies to the preceding line in its respective version (old/new) of the file. Junio explains that in the example case where only the content changes (from ") }}" to "}}") while both versions lack a final newline, showing both markers is necessary to accurately represent that neither version had a terminating newline. He notes this differs from cases where the newline status itself changes between versions.

The discussion remains at the conceptual level about whether this is a UX issue (original reporter's view) or correct diff format implementation (maintainers' view). No code changes have been proposed yet. The thread continues to focus on the same HTML template example where only the closing tag changes while both versions lack final newlines. Junio's response reinforces that the current output properly reflects the diff specification by showing the newline status of each version independently.