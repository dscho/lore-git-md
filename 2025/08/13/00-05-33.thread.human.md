This thread began with Timur Tabi reporting what appeared to be a bug in `git send-email`'s `--suppress-cc` option, questioning whether it should accept comma-separated values. Eric Sunshine quickly clarified that the current implementation - requiring separate flags for each suppression category (author, self, cc, etc.) - is correct and matches the original design, though the documentation didn't make this explicit.

Junio Hamano then stepped in with a proposed documentation patch to clearly state that `--suppress-cc` is a repeatable flag (one per category) and that the corresponding `sendemail.suppressCc` config follows the same pattern. Eric Sunshine has now confirmed this improvement, agreeing the additional clarity will help users understand they need separate flags rather than comma-separated values. The thread has reached consensus that this was purely a documentation gap - the implementation was correct all along - and the updated docs will prevent similar confusion in the future. No code changes are needed.