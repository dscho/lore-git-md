Here's a human-readable summary of the thread so far:

The thread discusses a proposal to add a `--only-unstaged` option to `git stash push` to better support commit-splitting workflows during interactive rebases. Currently, when trying to test staged changes while temporarily setting aside unstaged ones, users must use `--keep-index`, which still stores the index state in the stash - this can cause conflicts when popping the stash later. The new option would create stash entries containing only working tree changes, leaving the index completely untouched.

Junio Hamano has joined the discussion, probing for potential edge cases and workflow differences between the proposed `--only-unstaged` and existing `--keep-index` behavior. He notes that if the new option were strictly superior, it would replace rather than complement the existing one, suggesting there may be use cases where preserving (but still storing) the index state is actually desirable. The conversation is now exploring what those cases might be, as the technical implementation would involve modifying how stash objects are formatted to exclude index state entirely.