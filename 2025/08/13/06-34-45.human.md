This is a clarification about `git send-email`'s `--suppress-cc` behavior, responding to a bug report. Eric Sunshine points out that the reported issue with comma-separated keywords isn't actually a bug - the documentation never claimed to support that syntax, and the current behavior of requiring separate `--suppress-cc` flags is intentional.

The email serves to correct the misunderstanding from the original bug, noting that while the documentation could be clearer about the multiple-flag approach, the core functionality is working as designed. This effectively closes the bug report unless someone wants to propose adding comma-separated support as a new feature.

The response is technically precise, referencing both the documentation and source code to confirm the intended behavior. It maintains a helpful tone while correcting the initial assumption, and leaves the door open for potential improvements to the man page's clarity.