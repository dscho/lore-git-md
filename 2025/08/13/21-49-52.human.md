Thoughtful response to the `.gitignore` include proposal, highlighting practical concerns.

Brian m. carlson provides a measured counterpoint to Ryan Johnson's proposal for `.gitignore` includes, acknowledging potential usefulness while explaining why Git hasn't implemented this feature. The response focuses on three main concerns: backward compatibility (the proposed syntax could conflict with legitimate filenames), ecosystem impact (many tools parse `.gitignore` and would need updating), and questioning how broadly needed the feature would be (many projects use minimal ignore patterns).

The reply notes that while large language-specific templates exist (like GitHub's Python.gitignore), most projects actually need far fewer patterns, especially when build outputs are well-organized. Examples from Git (256 lines) and libgit2 (8 lines) demonstrate how projects typically use `.gitignore`. The response concludes by doubting the "easy" characterization of implementation while remaining open to some variant of the idea.

This email doesn't reject the proposal outright but provides important context about real-world constraints that would need addressing in any serious implementation attempt. The tone is constructive while clearly setting expectations about the non-trivial challenges involved.