This is a feature proposal discussion about adding `--only-unstaged` to `git stash push`. Junio Hamano is probing for potential downsides and edge cases that might not have been considered in the initial proposal.

The thread centers on improving commit-splitting workflows during interactive rebases by allowing users to stash only unstaged changes while leaving the index untouched. Hamano's response focuses on understanding the tradeoffs between the proposed `--only-unstaged` and the existing `--keep-index` option. He suggests that if the new option were strictly better, it would replace rather than complement `--keep-index`, implying there must be some workflow difference or limitation that needs explicit consideration.

Key technical points for future reference: the proposal involves modifying stash object format to exclude index state entirely (unlike `--keep-index` which preserves but still stores the index), and makes `stash pop` operations safer by avoiding index conflicts. The discussion now turns to understanding what use cases might still require `--keep-index` behavior versus this new approach.