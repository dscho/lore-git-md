This is a discussion email in the ongoing `git repo` command hierarchy series, specifically addressing the output ordering behavior of the new `git repo info` subcommand. Eric Sunshine weighs in on the debate between maintaining input order versus lexicographical sorting with deduplication for the command's output.

The email reinforces Phillip Wood's position that output should preserve input order and drop deduplication, countering Lucas's concerns about error handling and script complexity. Eric makes several key technical arguments: process creation overhead (especially on Windows) makes multiple command invocations undesirable, and the primary use case is for already-debugged scripts where output order predictability matters more than deduplication.

Eric clarifies that Phillip's suggestion wasn't to implement complex deduplication logic but rather to simplify by removing both sorting and deduplication entirely. The email notes this aligns with Eric's earlier neutral position but now finds Phillip's reasoning compelling. The discussion centers around implementation tradeoffs in builtin/repo.c's output handling, with implications for scripting use cases.

This moves the thread closer to resolution on the output ordering question, with Junio having already expressed support for input order preservation. The email serves to consolidate technical rationale behind this approach while addressing Lucas's remaining concerns about error case handling and implementation complexity.