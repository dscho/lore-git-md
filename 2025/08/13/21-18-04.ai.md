This is a discussion about the output ordering behavior in the `git repo info` command implementation, specifically regarding patch 2/5 of the series which adds the `references.format` field. The email is a response to Phillip Wood's concerns about the command's current lexicographical sorting of output keys.

Lucas defends the current deduplication approach (which requires sorting) as simpler than alternatives like hash tables, while acknowledging Phillip's point about script parsing difficulties. He notes that error cases (invalid keys) already break strict output ordering anyway. Lucas suggests that if input order preservation is preferred, it might be cleaner to allow duplicate keys and repeated error messages rather than adding deduplication complexity.

The discussion reveals two competing priorities: Phillip advocates for predictable output ordering to simplify scripting, while Lucas prioritizes implementation simplicity. This email captures the tradeoffs between these approaches, with Lucas leaning toward either keeping the current sorted/deduplicated output or going to unsorted/duplicated output rather than implementing more complex deduplication logic. The thread context shows Junio has since weighed in supporting Phillip's position on input order preservation.