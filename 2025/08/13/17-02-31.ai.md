Feature proposal continuation for `git stash --only-unstaged`, now with concrete reproduction and analysis of current behavior. D. Ben Knoble provides a detailed test case demonstrating how `--keep-index` still causes conflicts during stash pop operations, contrary to what the documentation might suggest. The analysis shows that even though the stash object contains both working tree (W) and index (I) commits, the conflicts arise from the working tree state rather than the index state as previously assumed.

Key technical findings: examining the stash tree objects reveals the working tree commit (stash@{0}^{tree}) contains changes that conflict with subsequent modifications, while the index commit (stash@{0}^2^{tree}) mirrors these same changes. This explains why conflicts appear even when trying to avoid index restoration. The email suggests `git restore` workarounds but notes they are impractical for complex cases and not documented as required solutions.

The discussion advances the thread by providing empirical evidence supporting the need for a true unstaged-only stash operation, where the working tree changes could be stashed without any index state being recorded or restored. This reinforces the original proposal's motivation while clarifying the exact nature of the conflicts that occur with current approaches. The technical depth suggests this is a well-considered contribution from an experienced Git user familiar with stash internals.