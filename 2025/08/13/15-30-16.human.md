Junio Hamano raises an important design consideration about the proposed `git stash --only-unstaged` feature. This is a thoughtful response to the original proposal that identified a concrete workflow improvement for commit splitting during rebases.

The core question focuses on why this needs to be a new flag rather than modifying the existing `--keep-index` behavior. Hamano points out that if the new behavior were strictly better, the proposal would suggest enhancing `--keep-index` directly. The implication is there must be some trade-off or difference in behavior that isn't yet clearly articulated in the proposal.

This moves the discussion forward by asking the proposer to clarify the design space: what specific limitations or changes in behavior would users encounter when switching from `--keep-index` to `--only-unstaged`? The response suggests the proposal needs to more explicitly compare the two approaches and explain why a new flag is justified rather than modifying existing behavior.

The tone is constructive - this isn't a rejection but rather a request for more complete design rationale that would help evaluate whether the new flag is truly needed. It's the kind of probing question maintainers often ask to ensure new features are well-considered additions to Git's interface.