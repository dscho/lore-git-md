This is a technical discussion about Git's revision range notation and symmetric difference behavior, continuing the thread about rebase documentation internals.

D. Ben Knoble follows up on Phillip Wood's earlier correction about using `git log --cherry-pick --right-only <upstream>...HEAD` in rebase documentation by raising a related question about symmetric difference behavior. He compares two commands that count commits using different range notations: `git rev-list --count --left-only @{u}...` versus `git rev-list --count ..@{u}`.

The core question is whether these commands always produce the same result when not using `--cherry-pick`, and whether there are performance differences between the two forms. Knoble hypothesizes that with `--left-only`, the symmetric difference (three-dot) notation might collapse to being equivalent to the two-dot range notation, since both would count commits on upstream but not in HEAD relative to the merge base.

This discussion builds on the thread's ongoing examination of rebase internals and documentation accuracy, now focusing specifically on the behavior of revision range notations in Git. The question touches on both correctness (whether the commands are semantically equivalent) and performance (which form might be more efficient), which could inform future documentation or implementation decisions. The thread has established that such technical details about Git's behavior are relevant to documenting rebase accurately, though the current email doesn't propose any documentation changes - it's seeking clarification about underlying behavior.