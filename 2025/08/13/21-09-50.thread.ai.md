Git thread summary: Ryan Johnson proposes adding an `include` directive to `.gitignore` files to allow including patterns from other ignore files, motivated by managing complex projects with language-specific templates. The suggested syntax would be `include .gitignore.python` to inline patterns from specified files during evaluation, helping separate template content (e.g. from github/gitignore) from project-specific rules. The change would require modifying pattern parsing in `dir.c` but implementation details, edge cases (path handling, cycles), and security implications remain unexplored. The proposal references GitHub's language-specific templates as evidence of the problem space, noting this would be particularly useful for teams maintaining clean separation between template updates and custom patterns. No patch implementation is provided yet.