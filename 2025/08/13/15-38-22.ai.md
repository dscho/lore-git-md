Documentation clarification for `git send-email`'s `--suppress-cc` option behavior. This is a follow-up in the thread about making the repeatable nature of the flag more explicit in the documentation.

Eric Sunshine confirms Junio Hamano's proposed documentation improvement, agreeing that adding text to explicitly state that `--suppress-cc` can be given multiple times (once per category) would be helpful. The clarification will also note that the corresponding `sendemail.suppressCc` config follows the same pattern of accepting multiple entries rather than comma-separated values.

The exchange confirms there was no actual bug in the implementation - the behavior was correct but under-documented. The thread has reached consensus that the documentation should be updated to prevent future confusion about how to suppress multiple CC categories (author, self, cc, etc.). This is purely a documentation improvement with no code changes needed.

Key technical points for future reference: The option requires separate flags for each category (e.g., `--suppress-cc=author --suppress-cc=self`), not comma-separated values. The config variable `sendemail.suppressCc` behaves the same way. The documentation will now explicitly that both can be specified multiple times.