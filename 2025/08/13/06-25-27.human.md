This is a refactoring patch in Patrick Steinhardt's reftable/libgit2 compatibility series (v4, patch 3/8). The change reorganizes code in `reftable/stack.c` to eliminate forward declarations by moving function implementations to more logical positions in the file.

The patch removes five forward declarations and rearranges the corresponding function implementations to appear before they are called. Key functions affected include `reftable_new_stack()`, `stack_try_add()`, and `stack_compact_locked()`. The changes are purely organizational - no behavior is modified, and the code structure becomes slightly cleaner by removing the need for forward references.

This is a straightforward improvement that follows good coding practices by keeping related code together. The patch fits naturally into the larger series which is focused on making the reftable backend more robust and portable for libgit2 integration. Since it's a simple refactoring with no functional impact, it likely requires minimal review attention.