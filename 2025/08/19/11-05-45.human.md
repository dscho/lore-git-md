This is a feature proposal for a new `git history` command to simplify commit manipulation workflows. Patrick Steinhardt responds to Bernd Petrovitsch's demonstration of using `git rebase -i` to squash fixup commits, acknowledging that while the existing tools work, they require expertise and aren't intuitive for many users. 

The email serves as a pointer to Patrick's newly published patch series implementing the proposed `git history` command (linked in the footer). The series aims to provide a more straightforward interface for common history editing tasks like reordering and squashing commits, addressing the workflow pain points described earlier in the thread by R. Diez.

The discussion highlights the tension between power users comfortable with `rebase -i` and the need for more accessible tools. Patrick's implementation appears to be a concrete step toward bridging this gap, though the email doesn't detail the specific subcommands or functionality included in the patch series. The thread now has both theoretical discussion and actual code to evaluate.