This is a documentation patch addressing a compatibility issue between different AsciiDoc processors in the Git man pages conversion effort. 

Jean-Noël Avila fixes a formatting discrepancy between asciidoc.py and Asciidoctor in the `pretty-formats.adoc` documentation. The issue involves how the two processors handle '+' characters in verbatim syntax - Asciidoctor requires double '+' for proper rendering. 

The patch updates the default values for ref name decorations in the `%(decorate)` format specifier, changing single '+' to double '++' in three places: the prefix, suffix, and pointer defaults. This ensures consistent rendering across both documentation processors while maintaining the same visual output.

This appears to be a straightforward technical fix to the Asciidoctor rendering issues noted earlier in the thread by SZEDER Gábor. The change is minimal (just 4 lines modified) and focused specifically on the compatibility problem without introducing any new formatting concepts. 

Since this patch comes after Junio Hamano already approved the v3 series for merging, it likely represents a final polish before the documentation standardization effort is complete. The fix demonstrates attention to detail in ensuring the documentation renders correctly across different toolchains.