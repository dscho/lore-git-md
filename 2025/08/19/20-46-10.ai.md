This is the third and final patch in Julia Evans' v3 documentation improvement series for `git-add`, focusing on simplifying the explanation of ignored files behavior.

The patch refines the ignored files section of `git-add`'s man page by making several clarity-focused changes. It moves the `--force` option mention earlier in the explanation, which helps readers understand the escape hatch for adding ignored files right away. The patch removes the potentially confusing distinction between shell globbing and Git's internal globbing system, noting that this is better covered in the EXAMPLES section. The new text is more direct about the two cases: explicitly named ignored files will trigger an error (listing the ignored files), while globbed/recursive matches will be silently ignored unless `--force` is used.

This change follows through on the series' goal of making the manual more accessible to beginners while maintaining technical accuracy. The patch builds on previous feedback that identified the shell globbing explanation as a point of confusion. The diff shows careful editing to preserve all key information while removing potentially distracting implementation details. As the final patch in a well-reviewed series that has incorporated all maintainer feedback, this appears ready for inclusion.