Patrick Steinhardt's series to abstract packfile management into `struct packfile_store` continues with patch 15, which refactors `get_all_packs()` to work directly on the packfile store. The key discussion revolves around whether removing this convenience wrapper is premature. Taylor Blau expresses concern that forcing callers to directly access `the_repository->objects->packfiles` exposes implementation details too early in the refactoring process. He suggests keeping the wrapper for now to maintain cleaner calling code.

Patrick acknowledges the verbosity this creates but argues it's a necessary step toward the long-term architectural goal of fully decoupling the ODB from packfile storage. He notes that many of the affected callers (like `builtin/cat-file.c`) are inherently tied to on-disk layout details anyway. As a compromise, Patrick mentions plans to introduce a `packfile_store_for_each_pack()` helper in a future series to improve ergonomics while still maintaining the architectural separation.

This discussion highlights the ongoing tension in the series between immediate code cleanliness and the long-term vision for the packfile store abstraction. The patch represents an intermediate step where some verbosity is temporarily introduced to enable the eventual cleaner architecture. Other recent patches in the series have addressed similar tradeoffs around naming, locking, and memory safety, with most of those concerns now resolved. The remaining open questions center on finding the right balance between abstraction and practical usability during this transitional phase.