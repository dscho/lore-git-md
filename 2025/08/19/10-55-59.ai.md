Refactoring patch in the `git-history` RFC series, modifying cache-tree functionality to support arbitrary in-memory indexes.

This patch extends `write_in_core_index_as_tree()` to accept an arbitrary `struct index_state` parameter rather than always using the repository's index. The change is straightforward - the function signature is updated to take the index as a parameter, and all callers (currently just `builtin/checkout.c`) are updated to pass the repository's index explicitly. The implementation remains largely the same, just operating on the passed index instead of assuming `repo->index`.

The commit message indicates this is preparatory work for a subsequent change in the series, likely related to the history editing operations that will manipulate in-memory indexes. The patch touches core index-handling code in cache-tree.c and its header, plus the checkout builtin that uses this functionality. Changes are minimal and focused - just 6 lines added/removed across 3 files.

This appears to be a well-contained infrastructure change that enables more flexible index handling needed for the upcoming history editing commands. The modification maintains existing behavior when called with the repository's index, while allowing other callers to pass different index states. No test changes are included since this is internal refactoring with no observable behavior change yet.