This is a feature discussion about enhancing Git's history manipulation capabilities, building on Patrick Steinhardt's proposed `git history` command. R. Diez responds with a concrete workflow pain point involving iterative fixes during long build processes, where the commit history becomes messy with interleaved fixes (A-B-fixA1-C-fixA2-D-E) that they'd prefer to squash into a cleaner sequence (A+fixes-B-C-D-E).

The email introduces two key technical suggestions: first, adding a `git history squash` subcommand to complement the proposed reorder/drop/split operations, and second, proposing a "safe mode" workflow where history rewrites would operate on a duplicate branch that can be discarded if the operation fails. The latter is framed as a safety net for users who find Git's history rewriting intimidating, particularly when dealing with complex repository states.

The discussion remains focused on CLI-based solutions despite originating from a GUI feature request, with R. Diez acknowledging the tension between ideal Git practices and real-world usability. The email provides valuable context about why some users might prefer linear history over branching even when working alone, and highlights how current tools may fail users who occasionally need to recover from suboptimal commit sequences. No patch is attached - this is purely a design discussion building on the earlier `git history` command proposal.