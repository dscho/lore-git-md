Feature request discussion: CLI vs GUI for commit history manipulation

Bernd Petrovitsch responds to R. Diez's workflow pain points with a simple `git rebase -i` solution, demonstrating how to squash fixup commits (fixA1, fixA2) into their parent commit (A) using the existing interactive rebase interface. This contrasts with R. Diez's earlier request for either a dedicated GUI or new `git history` subcommands to handle such operations.

The email provides a counterpoint to the thread's exploration of new interfaces by showing the existing rebase functionality can already solve the described problem through manual commit reordering and fixup commands in the editor. Bernd's response implicitly questions whether new tooling is needed for this specific use case, though doesn't address R. Diez's broader concerns about GUI usability or the desire for "safe mode" operations.

Key technical elements mentioned are the `HEAD~11` revision range syntax and the interactive rebase's "fixup" command, which automatically folds changes while discarding the intermediate commit messages. The exchange highlights a tension between power users comfortable with rebase workflows and those seeking more guided/visual interfaces for history manipulation.