This is a refactoring patch that alphabetizes the `fsck_msg_type` enum in `fsck.h` and fixes a duplicate entry in the documentation. It's the first patch in a 5-part series introducing reftable fsck validation.

The changes strictly reorganize existing content without adding new functionality. The main work involves:
1. Reordering the `FOREACH_FSCK_MSG_ID` macro entries in `fsck.h` to maintain consistent alphabetical ordering within each severity category (fatal errors, errors, warnings, etc.)
2. Fixing alignment of escaped newlines in the macro definition
3. Removing a duplicate `gitmodulesLarge` entry from `Documentation/fsck-msgids.adoc`

The patch appears mechanically sound - it doesn't modify any logic, just reorganizes and cleans up existing definitions. The changes are straightforward and unlikely to be controversial, serving as groundwork for the subsequent reftable validation patches in the series. The commit message clearly explains the scope of changes and the motivation (maintaining alphabetical order).