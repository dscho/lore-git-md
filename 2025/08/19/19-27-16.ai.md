Test modernization patch in the dangling symref/followRemoteHEAD series. This is patch 3/4 in Jeff King's series fixing incorrect behavior with dangling symrefs during fetch operations. The patch continues the test cleanup work from earlier patches, this time converting subshell-based test operations to use `git -C` for better isolation and consistency.

The changes focus on tests for `followRemoteHEAD` behavior in t5510-fetch.sh, replacing subshells containing `(cd two && git ...)` with direct `git -C two ...` invocations. This allows using `test_config -C` for configuration management rather than manual cleanup via `test_when_finished`. The patch touches 202 lines (83 insertions, 119 deletions) while maintaining all test assertions.

Key technical details:
- Eliminates subshells in favor of `git -C` for all `followRemoteHEAD` test cases
- Uses `test_config -C` for configuration management
- Removes manual cleanup commands that were previously needed with subshells
- Preserves all test expectations and assertions
- Maintains Windows compatibility (important for follow-up patches)

The author notes the change may be considered less readable due to frequent `-C` usage, and is open to feedback on whether to keep this approach or revert to subshells for the final patch in the series. The changes are preparatory for patch 4/4 which will implement the core symref behavior fix.