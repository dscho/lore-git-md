Thread identity: RFC proposal for `git-history` command with subcommands including `drop`, `reorder`, `split`, `reword`, and management commands (`abort`/`continue`/`quit`), inspired by Jujutsu (jj). Authored by Patrick Steinhardt. Series now at v3 with eighteen patches - preparatory work, command scaffolding, and subcommands (`reorder`, `drop`, `split`, `reword`). Patrick confirms this will be the last RFC version before splitting the series, stopping after `reorder` implementation. Junio C Hamano notes practical merge delay due to potential conflicts with Jeff King's add-i color work in 'seen' branch, which Patrick acknowledges and will address before proceeding.

Core problem/goal: Provide flexible history editing operations building on Git's sequencer while taking inspiration from Jujutsu, with ongoing discussion about whether these should be separate commands or integrated into existing infrastructure like `git-replay`. Current subcommands include both editing operations (`drop`, `reorder`, `split`, `reword`) and management commands (`abort`, `continue`, `quit`). Recent technical discussion has refined the implementation of the `skip_commit_summary` flag in the sequencer infrastructure and reconsidered code reuse for the `reword` subcommand, plus new discussion about HEAD management unification between rebase and cherry-pick operations. Patrick Steinhardt now clarifies the relationship between `git-history` and `git-replay`, proposing `git-replay` as plumbing-level tool for scripts/server-side use while `git-history` becomes the user-facing home for history editing operations.

Current status: Eighteen-patch RFC v3 series with active design discussion. Patrick Steinhardt confirms plan to:
1. Resolve conflicts with Jeff King's add-i color work in 'seen' branch
2. Send one final RFC version
3. Then split the series after `reorder` implementation
4. Drop the `reword` implementation from current series to reduce scope
The `split` subcommand implementation now incorporates Phillip Wood's feedback to allow message editing for both resulting commits (not just the first one) and considers sequencer integration. The `skip_commit_summary` flag implementation has been simplified per Phillip Wood's suggestion. New discussion continues about potential unification of HEAD management between rebase and cherry-pick operations via the new `restore_head_target` option, with Patrick opting for conservative approach to avoid rebase regressions. Patrick establishes clear boundaries for `git-history` scope - limited to operations that modify existing commit history, excluding commit creation, import/export, or working tree operations.

Key technical details:
- Development path confirmed with conflict resolution step added
- `skip_commit_summary` flag implementation simplified per Phillip Wood's suggestion
  - Rebase now explicitly sets flag rather than using `is_rebase_i()` check
  - Eliminates dual-path logic in sequencer
  - Maintains same behavior with cleaner implementation
- `split` subcommand enhanced per Phillip Wood's feedback:
  - Now allows message editing for both resulting commits
  - Sequencer integration being considered for future
  - Current behavior still uses file lists rather than full diffs in editor template
  - Second commit preserves original tree and message by default
- New `restore_head_target` option introduced for cherry-pick-like operations
  - Needed for proper HEAD rewinding in `git-history`
  - Phillip Wood suggests potential unification with rebase HEAD management
  - Patrick cautious about modifying existing rebase conditionals
- `reword` subcommand implementation to be dropped from current series
  - Phillip Wood identified sequencer already provides this functionality
  - Will revisit sequencer-based approach in future series
- Command architecture clarified:
  - `git-replay` as plumbing for scripts/server-side
  - `git-history` as user-facing history editing tool
  - Subcommand approach defended for discoverability
  - Aliases could provide separate-command experience
  - Explicit scope boundaries established:
    - Only history editing operations
    - Excludes commit creation (`commit`)
    - Excludes import/export operations
    - Excludes working tree commands (`reset`)
  - `git-histedit` name considered but not committed to
- Phillip Wood's architectural suggestions remain relevant:
  1. Sequencer integration approach
  2. Hook behavior rules
  3. Future feature possibility
  4. Branch update mechanism
  5. Message header handling

Open questions:
- `split` subcommand UI expectations (diff output vs modified files list) - Need for documentation clarification
- Potential future enhancement to show diffs
- Series organization and scope (planned after `reorder`)
- Command architecture philosophy
- Future subcommand implementations
- Sequencer mode interactions
- Command naming and documentation (`git-history` vs `git-histedit`)
- Relationship to `git-replay` (now clarified)
- Phillip Wood's five refinement suggestions
- Architectural integration points
- Hook execution rules
- Branch update strategy
- Commit message header handling
- Exact approach for sequencer-based `reword` implementation
- Potential HEAD management unification between rebase and cherry-pick operations
- Safety of modifying existing rebase conditionals

Participant positions:
- Patrick Steinhardt driving implementation:
  - Accepts Phillip Wood's code simplification suggestion for `skip_commit_summary`
  - Implements `split` improvements allowing message editing for both commits
  - Confirms development path with added conflict resolution step
  - Agrees to drop current `reword` implementation in favor of future sequencer-based approach
  - Cautious about unifying HEAD management between rebase and cherry-pick
  - Clarifies `git-history`/`git-replay` relationship in response to Elijah Newren
  - Defends subcommand approach while acknowledging aliases option
  - Establishes clear scope boundaries for `git-history`
- Phillip Wood provided key implementation refinements:
  - `skip_commit_summary` simplification
  - Identified sequencer reuse opportunity for `reword`
  - Suggested HEAD management unification
  - Proposed `split` subcommand improvements
- Elijah Newren's concerns about command proliferation addressed with scope clarification
- Junio C Hamano notes merge delay due to Jeff King's add-i color work
- D. Ben Knoble's `split` UI expectations mismatch remains relevant
- Kristoffer Haugsbakk's documentation corrections

Related work:
- Git's sequencer infrastructure
- `git rebase` management commands
- Jujutsu's interactive history editing
- Ongoing `the_repository` removal effort
- Git's add-patch implementation
- `git-replay` command evolution
- Phillip Wood's personal `git-rewrite` wrapper
- Prior rebase discussion about commit message header handling
- Jeff King's add-i color work in 'seen' branch

The thread continues with Patrick confirming both the development path forward and architectural refinements based on Phillip Wood's feedback, while maintaining a conservative approach to modifying existing rebase behavior. The series scope is being tightened by dropping the current `reword` implementation in favor of a future sequencer-based approach, while maintaining all other planned technical improvements. The `split` subcommand implementation now incorporates message editing for both resulting commits, addressing one of Phillip Wood's key suggestions. New discussion continues about potential sequencer infrastructure unification opportunities, with Patrick prioritizing stability over architectural purity at this stage. Patrick's response to Elijah Newren clarifies the intended relationship between `git-history` and `git-replay`, establishing clear boundaries for what belongs in the user-facing tool versus the plumbing layer. The exchange demonstrates responsive design evolution while keeping project constraints (conflict resolution) and architectural considerations (sequencer integration) clearly in view as the series approaches its planned reorganization.