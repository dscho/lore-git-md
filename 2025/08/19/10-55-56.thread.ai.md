Thread identity: RFC proposal for `git-history` command with twelve-patch v6 series (with `reword` and `split` subcommands), authored by Patrick Steinhardt. Series builds on Git's replay machinery while drawing inspiration from Jujutsu's history editing capabilities, focused on non-conflicting operations after Git Contributor's Summit discussions.

Core problem/goal: Implement flexible history editing operations (`reword` and `split` subcommands) with supporting infrastructure refactoring (interactive add/patch, cache-tree modifications, and wt-status tree-to-tree diff capabilities) to enable in-memory index operations. Current implementation avoids `drop` and management commands based on community feedback.

Current status: While maintainer Junio Hamano previously indicated the series was nearing completion, substantive concerns from Elijah Newren's review have prompted discussion about potentially limiting initial scope to single-branch cases for earlier integration. Key infrastructure refactoring patches have stabilized but implementation details around `run_add_p_index()` and branch handling design remain active discussion points, with Junio now advocating for an incremental approach. Elijah's latest response challenges the assumption that single-branch limitation simplifies the implementation and raises new concerns about index preservation semantics and commit metadata handling.

Key technical details:
- Foundational infrastructure (completed/updated):
  - New add-patch/add-interactive separation completed via `struct interactive_config` integration
  - `add-patch.h` header with decoupled configuration
  - `run_add_p_index()` for in-memory operations (current discussion point)
  - Generalized `write_in_core_index_as_tree()`
  - `replay.[ch]` infrastructure extraction
  - Tree-to-tree diff in wt-status
  - Final `the_repository` removal in `replay_create_commit()`
- `reword` subcommand:
  - Current implementation uses manual ancestry checking
  - May not properly restrict revision walk to descendants
  - Uses `commit_tree()` instead of `commit_tree_extended()`
- `split` subcommand:
  - Binary split implementation (creates exactly two commits)
  - Interactive hunk selection via `run_add_p_index()`
  - Preserves authorship and reuses original message (currently associates message with second commit)
  - Includes extensive test coverage
  - Index preservation behavior may already correctly handle split sums
- Documentation follows experimental command style
- Build system updates for new components

Open questions:
- Infrastructure refactoring:
  - Current discussion on `run_add_p_index()` implementation approach
  - Potential for accidental use of `s->r->index` instead of intended `s->index`
- Branch handling design:
  - Junio suggests limiting initial scope to single-branch cases
  - Elijah argues this may not simplify implementation and could introduce edge cases
  - Need to verify single-branch assumption holds
- Index preservation:
  - Edge case where splitting commits while editing patches could lead to unexpected state
  - Current implementation may correctly preserve changes as sum of splits
  - Needs explicit documentation of behavior
- Split subcommand design:
  - Binary vs N-way splits discussion ongoing
  - Test/hook execution discrepancy identification
  - Commit message association (first vs second commit in split)
- Commit metadata handling:
  - Lack of extended header preservation in edited commits
  - Potential need for `commit_tree_extended()` usage
- Existing concerns from prior reviews:
  - Revision walking correctness (`--ancestry-path` needed)
  - Unnecessary HEAD reachability requirement
  - Reword subcommand merge commit support

Participant positions:
- Patrick Steinhardt driving implementation, preparing final reroll
- Junio Hamano:
  - Now advocates incremental approach with limited initial scope
  - Suggests deferring multi-branch handling to future iterations
  - Maintains technical direction has consensus
- Elijah Newren:
  - Challenges single-branch limitation as insufficient simplification
  - Identifies index preservation behavior needing documentation
  - Raises commit metadata handling questions
  - Continues to flag substantive concerns about patches 5 and 9
  - Maintains question about binary vs N-way split design
- Phillip Wood:
  - Acknowledges infrastructure refactoring progress
  - Notes remaining `run_add_p_index()` consolidation opportunity
- D. Ben Knoble analyzed design tradeoffs
- Karthik Nayak raised pointer alignment style question

Related work:
  - Git's replay infrastructure
  - `git-replay` command
  - Jujutsu's history editing
  - `the_repository` removal effort
  - wt-status subsystem extensions
  - Add-patch shared code
  - `git-revise` third-party tool
  - Cache-tree functionality

The series remains in an advanced state but now faces additional technical questions from Elijah Newren regarding branch handling assumptions, index preservation semantics, and commit metadata decisions. While Junio Hamano advocates for incremental integration with limited scope, Elijah's concerns suggest the design may require more substantial consideration of edge cases and documentation of subtle behaviors. Patrick Steinhardt's next revision will need to either address these concerns comprehensively or provide justification for the initial limited scope approach while ensuring proper safeguards against edge cases.