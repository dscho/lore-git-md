Thread identity: RFC proposal for `git-history` command with subcommands including `drop`, `reorder`, `split`, `reword`, and management commands (`abort`/`continue`/`quit`), inspired by Jujutsu (jj). Authored by Patrick Steinhardt. Series now at v3 with eighteen patches - preparatory work, command scaffolding, and subcommands (`reorder`, `drop`, `split`, `reword`). Patrick confirms this will be the last RFC version before splitting the series, stopping after `reorder` implementation. Junio C Hamano notes practical merge delay due to potential conflicts with Jeff King's add-i color work in 'seen' branch, which Patrick acknowledges and will address before proceeding.

Core problem/goal: Provide flexible history editing operations building on Git's sequencer while taking inspiration from Jujutsu, with ongoing discussion about whether these should be separate commands or integrated into existing infrastructure like `git-replay`. Current subcommands include both editing operations (`drop`, `reorder`, `split`, `reword`) and management commands (`abort`, `continue`, `quit`). Recent technical refinements include the `skip_commit_summary` flag implementation in the sequencer infrastructure and reconsidered code reuse for the `reword` subcommand, plus new discussion about HEAD management unification between rebase and cherry-pick operations. Patrick Steinhardt clarifies the relationship between `git-history` and `git-replay`, proposing `git-replay` as plumbing-level tool while `git-history` becomes the user-facing home for history editing operations.

Current status: Eighteen-patch RFC v3 series with active design discussion. Patrick Steinhardt confirms plan to:
1. Resolve conflicts with Jeff King's add-i color work in 'seen' branch
2. Send one final RFC version
3. Then split the series after `reorder` implementation
4. Drop the `reword` implementation from current series to reduce scope
The `split` subcommand implementation now incorporates Phillip Wood's feedback to allow message editing for both resulting commits (not just the first one) and considers sequencer integration. The `skip_commit_summary` flag implementation has been simplified per Phillip Wood's suggestion. New discussion continues about potential unification of HEAD management between rebase and cherry-pick operations via the new `restore_head_target` option, with Patrick opting for conservative approach to avoid rebase regressions. Phillip Wood now suggests adopting rebase's detached HEAD approach for `git-history` operations to maintain reflog cleanliness and enable easier result verification via `git range-diff $branch@{1}...HEAD`. This builds on their earlier exchange about HEAD management unification. Phillip Wood further refines the `reword` subcommand approach, proposing to reuse the sequencer's existing reword capability (as used in interactive rebase) by passing a todo-list with "reword $oid", which would provide equivalent functionality with less new code while potentially requiring minor hook execution behavior adjustments. Patrick acknowledges this approach but notes it would put the operation in interactive-rebase mode rather than history-editing mode, expressing concerns about the complexity of introducing a new history-editing mode that aliases interactive-rebase behavior. He suggests a middle path of sharing core operations between sequencer and git-history while avoiding full mode unification, though stops short of implementing this due to complexity.

Key technical details:
- Development path confirmed with conflict resolution step added
- `skip_commit_summary` flag implementation simplified per Phillip Wood's suggestion
  - Rebase now explicitly sets flag rather than using `is_rebase_i()` check
  - Eliminates dual-path logic in sequencer
  - Maintains same behavior with cleaner implementation
- `split` subcommand enhanced per Phillip Wood's feedback:
  - Now allows message editing for both resulting commits
  - Sequencer integration being considered for future
  - Current behavior shows modified files list rather than full diffs in editor template
  - Second commit preserves original tree and message by default
  - D. Ben Knoble notes some users may expect diff output (like `commit.verbose`)
  - Patrick proposes keeping current UI for initial landing, with diff display as future enhancement
- HEAD management discussion expanded:
  - New `restore_head_target` option introduced for cherry-pick-like operations
  - Phillip Wood suggests adopting rebase's detached HEAD approach for:
    - Cleaner reflogs (avoids intermediate states)
    - Easier verification via `range-diff $branch@{1}...HEAD`
  - Patrick previously cautious about modifying existing rebase conditionals
  - Current implementation uses same reflog message as rebase
- `reword` subcommand implementation refined:
  - Current implementation to be dropped from series
  - Phillip Wood identifies sequencer already provides this functionality via todo-list "reword" command
  - Would require minor hook execution behavior adjustments
  - Maintains same user experience with less new code
  - Patrick acknowledges potential but notes mode mismatch between interactive-rebase and history-editing
  - Suggests middle path of shared core operations without full mode aliasing
  - Future implementation will revisit this architectural question
  - Phillip Wood now proposes new sequencer entry point accepting todo-list directly
    - Would allow leveraging existing sequencer operations (rewording, ref updates)
    - Challenges Patrick's concern about "interactive-rebase mode" by noting cherry-pick and rebase already share todo-list processing
    - Acknowledges arguments for avoiding sequencer entirely (to prevent unnecessary worktree updates)
    - Questions current hybrid approach using sequencer for cherry-picking but implementing other operations separately
- Command architecture clarified:
  - `git-replay` as plumbing for scripts/server-side
  - `git-history` as user-facing history editing tool
  - Subcommand approach defended for discoverability
  - Aliases could provide separate-command experience
  - Explicit scope boundaries established:
    - Only history editing operations
    - Excludes commit creation (`commit`)
    - Excludes import/export operations
    - Excludes working tree commands (`reset`)
  - `git-histedit` name considered but not committed to

Open questions:
- `split` subcommand UI expectations (diff output vs modified files list) - Need for documentation clarification
- Potential future enhancement to show diffs
- Series organization and scope (planned after `reorder`)
- Command architecture philosophy
- Future subcommand implementations
- Sequencer mode interactions
  - Tension between code reuse and mode separation
  - Potential middle path for shared core operations
  - Phillip Wood's proposal for new sequencer entry point accepting todo-list
  - Whether to avoid sequencer entirely vs current hybrid approach
- Command naming and documentation (`git-history` vs `git-histedit`)
- Relationship to `git-replay` (now clarified)
- Phillip Wood's five refinement suggestions
- Architectural integration points
- Hook execution rules for sequencer-based `reword`
- Branch update strategy
- Commit message header handling
- Exact approach for sequencer-based `reword` implementation
- Whether to accept mode mismatch for implementation simplicity
- HEAD management strategy (detached vs attached)
  - Benefits of rebase-style detached HEAD for verification
  - Impact on reflog pollution
  - Safety of modifying existing rebase conditionals
- Potential HEAD management unification between rebase and cherry-pick operations
- Future consideration for `git status` to identify `git history` operations distinctly from cherry-pick

Participant positions:
- Patrick Steinhardt driving implementation:
  - Accepts Phillip Wood's code simplification suggestion for `skip_commit_summary`
  - Implements `split` improvements allowing message editing for both commits
  - Confirms development path with added conflict resolution step
  - Agrees to drop current `reword` implementation in favor of future sequencer-based approach
  - Acknowledges sequencer reuse potential but notes mode mismatch concerns
  - Proposes middle path for shared core operations without full mode unification
  - Cautious about unifying HEAD management between rebase and cherry-pick
  - Clarifies `git-history`/`git-replay` relationship in response to Elijah Newren
  - Defends subcommand approach while acknowledging aliases option
  - Establishes clear scope boundaries for `git-history`
  - Responds to D. Ben Knoble's UI feedback with plan for current behavior + future enhancement
- Phillip Wood provides ongoing implementation refinements:
  - `skip_commit_summary` simplification
  - Identified sequencer reuse opportunity for `reword`
  - Suggested HEAD management unification
  - Proposed `split` subcommand improvements
  - Now advocates for rebase-style detached HEAD approach in `git-history`
    - Cites reflog cleanliness and verification benefits
  - Details sequencer-based `reword` implementation approach
  - Challenges Patrick's mode mismatch concern by noting cherry-pick/rebase todo-list sharing
  - Proposes new sequencer entry point accepting todo-list directly
  - Questions current hybrid approach to sequencer usage
- D. Ben Knoble clarifies UI expectations for `split` subcommand
  - Notes potential mismatch with `commit.verbose` diff display behavior
  - Accepts Patrick's phased approach
- Elijah Newren's concerns about command proliferation addressed with scope clarification
- Junio C Hamano notes merge delay due to Jeff King's add-i color work
- Kristoffer Haugsbakk's documentation corrections

Related work:
- Git's sequencer infrastructure
- `git rebase` management commands
- Jujutsu's interactive history editing
- Ongoing `the_repository` removal effort
- Git's add-patch implementation
- `git-replay` command evolution
- Phillip Wood's personal `git-rewrite` wrapper
- Prior rebase discussion about commit message header handling
- Jeff King's add-i color work in 'seen' branch
- Git's `commit.verbose` mode behavior
- `git range-diff` verification workflow
- `git status` operation identification

The thread continues with Patrick confirming both the development path forward and architectural refinements while maintaining a conservative approach to modifying existing behavior. Phillip Wood's latest contributions refine the `reword` subcommand approach to leverage existing sequencer capabilities, proposing a new sequencer entry point accepting todo-lists directly and challenging Patrick's concerns about mode separation by noting cherry-pick and rebase already share todo-list processing logic. This deepens the discussion about sequencer integration strategy, with Phillip questioning the current hybrid approach and advocating for more extensive reuse of sequencer infrastructure. The architectural tension between clean code reuse and operational mode separation remains central, with Phillip now proposing concrete technical solutions to bridge the gap while Patrick maintains caution about potential mode confusion. The exchange demonstrates responsive handling of feedback while keeping focus on incremental progress toward initial acceptance, with both participants contributing substantive technical analysis to resolve the sequencer integration questions.