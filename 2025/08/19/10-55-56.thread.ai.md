Updated AI thread summary:

Thread identity: RFC proposal for new `git-history` command with subcommands `drop`, `reorder`, and now `split`, inspired by Jujutsu (jj) functionality. Authored by Patrick Steinhardt. Series now complete at v1 with thirteen patches - three preparatory patches modifying sequencer and cache-tree internals, command scaffolding, three implemented subcommands (`drop`, `reorder`, `split`), and supporting refactoring work.

Core problem/goal: Provide more flexible history editing operations than raw rebase -i, taking inspiration from Jujutsu's workflow while building on Git's existing sequencer infrastructure. The command now offers three subcommands: `drop` for commit removal, `reorder` for commit rearrangement, and `split` for interactive commit division.

Current status: Thirteen-patch RFC series with:
1-3) Preparatory changes to sequencer and cache-tree
4) Command scaffolding
5) `drop` subcommand implementation
6) `reorder` subcommand implementation
7) Header refactoring (new `add-patch.h` split from `add-interactive.h`)
8) Interactive configuration refactoring (new `struct interactive_options`)
9) `add-patch` subsystem dependency cleanup
10) New in-memory index patching capability for `add-patch` subsystem
11) New `split` subcommand implementation
[12-13 not yet summarized]
Not yet in any integration branch. Implementation has known limitations around merge commits and conflict handling.

Key technical details (expanded from previous):
- New `split` subcommand (patch 11):
  - Interactive hunk selection via `add-patch` subsystem
  - Pathspec support (`--pathspec-from-file` and CLI args)
  - Empty commit prevention logic
  - Commit message handling (editor or `-m`/`-F` options)
  - New `split_commit()` helper integrating with sequencer
  - Uses series' existing `collect_commits()`/`apply_commits()`
  - 300+ lines of test coverage in t3452-history-split.sh
- All subcommands share:
  - Sequencer integration via `sequencer_pick_revisions()`
  - Branch updates via `reset_head()`
  - Common commit collection/application logic
- Header refactoring remains relevant:
  - `add-patch.h` separation complete
  - `struct interactive_options` in use
  - `add-patch` cleanup verified through new `split` usage
- In-memory index patching:
  - Now exercised by `split` command
  - Confirmed working with temporary index operations

Open questions/loose ends (updated):
- Merge commit support still missing (affects all subcommands)
- Conflict handling remains basic (cherry-pick --continue fallback)
- Performance impact unmeasured (particularly for repeated splits)
- Cache-tree changes still need broader review
- Interactive config split incomplete (commit notes outstanding)
- Pathspec edge cases in `split` may need more testing
- Editor vs. message flag behavior in `split` may need polish

Participant positions: [Still awaiting first reviews - tracking as thread develops]

Related work (expanded):
- Builds on existing sequencer infrastructure
- Inspired by Jujutsu (jj) commands
- May interact with Phillip Wood's sequencer work
- Uses `reset_head()` infrastructure
- Cache-tree changes could affect other index operations
- Header refactoring relates to ongoing code organization efforts
- Interactive config changes touch multiple builtin commands
- `add-patch` cleanup affects add/checkout/commit/reset/stash workflows
- In-memory index patching now proven useful beyond initial scope
- `split` implementation shares concepts with `git add -p` but with commit-level focus