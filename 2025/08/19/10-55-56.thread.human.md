Here's a human-readable summary of the thread's current state:

Patrick Steinhardt's proposed `git-history` command continues to evolve, with the latest patch completing the `split` subcommand functionality in the v6 series. The command aims to provide flexible history editing operations (currently `reword` and `split`) using Git's replay machinery while drawing inspiration from Jujutsu's capabilities.

The implementation has addressed several technical concerns raised in reviews. Phillip Wood's feedback led to better code sharing between subcommands, particularly around commit validation and message handling. The `split` operation now prompts for both commit messages when dividing a change and shares infrastructure with the `reword` subcommand. It maintains the experimental approach of only verifying commits are in the current branch (not checking other branches) for performance reasons.

While Junio Hamano advocates merging the core functionality to 'next' for wider testing, some open questions remain. Elijah Newren continues to raise concerns about multi-branch commit handling, while SZEDER Gábor questions the usability of the in-memory `reword` operation that doesn't allow examining file contents. Documentation still needs updates to clearly explain the command's experimental nature, current limitations, and the tradeoffs of its in-memory operations.

The series now has extensive test coverage (432 lines for `split`, 237 for `reword`) and appears technically sound, but some workflow concerns persist. Patrick maintains that the current branch-only behavior and in-memory operations are intentional design choices that provide speed benefits, though he acknowledges they may not suit all use cases. The experimental status leaves room for future adjustments as more users try the feature.