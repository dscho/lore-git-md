Here's the updated human-readable thread summary:

The thread discusses Patrick Steinhardt's RFC proposal for a new `git-history` command that would provide flexible history editing operations, inspired by Jujutsu (jj). The current v3 series includes twenty-six patches implementing subcommands like `drop`, `reorder`, `split`, and `reword`, along with management commands.

A key focus of recent discussion has been the `split` subcommand's behavior. While Patrick confirmed it should show diffs between old and new trees during interactive use, D. Ben Knoble reports this isn't working as expected in practice. He provided a detailed reproduction case showing the issue persists even when manually editing hunks. Patrick acknowledges there may be bugs in the wt-status interface preventing proper diff display, though he couldn't reproduce the problem himself.

This follows earlier feedback about the command's usability, including incorrect error messages (referencing an unimplemented `-F` option) and technical constraints that prevent retrying after empty commit messages. The series has seen several improvements, including documentation fixes, error message corrections, and infrastructure refactoring (particularly around `add-patch` and cache-tree changes) that Junio has approved.

The discussion continues to balance design aspirations with technical constraints, particularly around in-memory index handling. While the core functionality is taking shape, this latest exchange highlights an important usability gap in the `split` subcommand's feedback mechanism that will need addressing before the feature can be considered complete.