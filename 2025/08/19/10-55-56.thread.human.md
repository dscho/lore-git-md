Patrick Steinhardt has proposed a major new `git-history` command that would provide Jujutsu-inspired history editing capabilities, now in its third iteration as a 31-patch series. The command would introduce subcommands for editing operations (`drop`, `reorder`, `split`, `reword`) and management (`abort`, `continue`, `quit`), building on extensive preparatory work that touches sequencer infrastructure, hook execution, and the add-patch subsystem.

Kristoffer Haugsbakk recently pointed out a minor documentation discrepancy in the cover letter, which incorrectly listed the `reword` subcommand as future work when it had actually been implemented in v2. Patrick acknowledged the correction. The `reword` implementation is already complete, handling commit message editing while preserving other metadata and including comprehensive test coverage for edge cases like merge commits and dirty working trees.

The thread remains active with discussions about implementation details and potential future subcommands, though this particular exchange was focused on ensuring accurate documentation of the existing feature set. Reviewers should note that all four main editing subcommands are now implemented, with `reword` having been available since v2 despite the cover letter's implication otherwise.