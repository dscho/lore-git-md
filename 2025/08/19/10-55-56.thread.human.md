Here's a human-readable summary of the thread:

Patrick Steinhardt has proposed a new `git history` command through a 12-patch series (now at v6) that introduces `reword` and `split` subcommands for flexible history editing. The implementation builds on Git's replay machinery while drawing inspiration from Jujutsu's capabilities. The series includes significant refactoring of the add-patch/add-interactive code to support in-memory index operations.

The discussion has centered around several key issues. Junio Hamano advocates merging a minimal version to 'next' for early testing, while reviewers have raised substantive concerns. Elijah Newren and Phillip Wood have provided detailed technical feedback, particularly about branch handling strategies and the need for automatic multi-branch rewriting. SZEDER Gábor has highlighted usability concerns with the `reword` subcommand's in-memory-only operation and lack of interactive selection.

The latest patch (v6 12/12) focuses on the `split` subcommand, implementing a binary split that preserves authorship and tree structure while using interactive hunk selection. It addresses review feedback by preserving extended commit headers and adding extensive test coverage (432 lines). However, some restrictions remain, like requiring splits to be reachable from HEAD and disallowing hunk editing for now.

The thread shows tension between architectural purity and practical implementation needs, particularly around code organization of the add-patch refactoring and global state management. While core functionality is now complete, open questions remain about future enhancements like multi-way splits and integration with replay's revision walking. Documentation also needs updates to accurately reflect the shift from rebase to replay infrastructure.