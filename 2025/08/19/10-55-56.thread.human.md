Here's a human-readable summary of the thread's current state:

Patrick Steinhardt's RFC for a new `git-history` command continues to evolve, now at version 3 with eighteen patches (down from thirty-one). The proposal introduces history editing subcommands like `drop`, `reorder`, `split`, and `reword`, inspired by Jujutsu's functionality. Junio Hamano notes the series may face a brief merge delay due to conflicts with Jeff King's parallel work on `add-i` color changes, but this is purely logistical - there are no technical objections to the proposal itself.

The most recent discussion focuses on the `split` subcommand's user interface. D. Ben Knoble raised concerns about the diff output during splitting operations, expecting to see full diffs rather than just modified file listings. After some back-and-forth, it became clear this was a documentation and expectation issue rather than a bug - the current behavior shows modified files (via `commit.verbose=true`) but not full diffs. This exchange highlights an area where the command's behavior could be made clearer to users.

Key architectural questions from Phillip Wood about sequencer integration and command design philosophy are still under consideration. Patrick plans to split the series after implementing the `reorder` subcommand, suggesting a more focused initial scope. The thread remains active with productive technical discussions, particularly around UI expectations and documentation needs for these new history editing operations.