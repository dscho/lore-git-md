Test cleanup patch in the dangling symref fetch configuration series.

This is the second patch in a 4-part series fixing incorrect behavior with dangling symrefs and `followRemoteHEAD=create` in `git fetch`. The patch modernizes test patterns in t5510-fetch.sh by eliminating top-level working directory changes, moving test operations into subshells where appropriate. This improves test isolation and follows Git's preferred test style.

The changes replace bare `cd` commands that affected subsequent tests with subshells or `git -C` invocations. This allows removing the `$D` variable that was previously used to track the working directory. The patch maintains all test functionality while making the tests more robust - for example, ensuring Windows path handling works correctly in the "fetch --prune prints the remotes url" test by using `$(pwd)` explicitly.

The cleanup is thorough, touching 356 lines with 161 insertions and 195 deletions. It focuses on test infrastructure rather than the core bugfix, preparing the ground for the later patches in the series. The changes are mechanical but important for maintaining test hygiene, and the commit message clearly explains the rationale and verifies no intentional side effects were broken.

Key technical details for future reference:
- Removes all uses of `$D` working directory variable
- Converts tests to use subshells with `(cd dir && ...)`
- Uses `git -C` where appropriate for single commands
- Maintains special Windows path handling in one test
- Preserves all test assertions and behaviors
- Part of larger series fixing symref/fetch interaction