Housekeeping patch in a reftable fsck validation series. This first patch in Karthik Nayak's 5-part series alphabetizes the `fsck_msg_type` enum and fixes some minor formatting issues. The changes are purely organizational - no behavior is modified. The patch:
1. Reorders fsck message IDs to maintain strict alphabetical ordering within each severity category
2. Fixes alignment of escaped newlines in the macro definition
3. Removes a duplicate `gitmodulesLarge` entry from the fsck message documentation

This appears to be preparatory work for the subsequent patches that will add reftable-specific fsck messages. The changes are straightforward and uncontroversial, though the diff is large due to the reordering. The patch follows Git's style conventions and includes documentation updates to match the code changes.