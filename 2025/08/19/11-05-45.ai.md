This is a feature proposal for a new `git history` command as a CLI backend for history manipulation operations, following up on the ongoing discussion about improving commit reorganization workflows.

Patrick Steinhardt has now published his implementation of the proposed `git history` command, referencing the thread where Bernd Petrovitsch demonstrated how to achieve similar results with existing `git rebase -i` functionality. The patch series appears at [1], though the email doesn't detail its specific contents beyond positioning it as a solution to make common history manipulation workflows "more obvious and easy" compared to the expert-level `rebase -i` approach.

The context shows this is exploring the middle ground between R. Diez's GUI-focused usability concerns and Bernd's demonstration of existing CLI capabilities. Patrick's approach seems to be creating a dedicated CLI interface that could serve both direct users and potential GUI frontends, though the current email doesn't specify whether the implementation includes R. Diez's requested "safe mode" or squash subcommand features mentioned earlier in the thread.

Key technical points for future reference: the implementation is structured as a patch series (v1 with 9 parts), uses the `builtin/` directory (indicating a new first-class command), and aims to simplify operations like those demonstrated via `rebase -i HEAD~11` and fixup commands. The thread has established this as one of three competing approaches to the history manipulation problem space.