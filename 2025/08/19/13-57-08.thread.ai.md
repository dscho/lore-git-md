Thread identity: Discussion about planned removal of git grafts functionality in Git 3.0, focusing on real-world usage impacts for the Linux kernel history repository (history.git) and shallow clone operations.

Core problem: The grafts mechanism is scheduled for removal, but key use cases like history.git and shallow clone operations demonstrably depend on it. The replacement mechanism (git replace) needs verification for these cases before grafts removal.

Current status: Grafts remain available but marked for removal in Git 3.0. No demonstrated migration path exists for history.git or shallow clone operations, with new evidence showing shallow clones currently use grafts internally (via "grafted" markers in `git log` output).

Key technical details:
- history.git uses grafts to stitch together Linux kernel history segments
- git replace is the intended replacement mechanism
- Shallow clone operations (--depth=1, --shallow-since, --unshallow) show "grafted" markers in log output (verified on Git 2.47.2)
- Functional requirement: shallow clones must continue working after grafts removal
- No technical details yet on why shallow cloning relies on grafts or how git replace would replace this functionality

Open questions:
- Migration path for history.git from grafts to replace without breaking references
- Whether and how shallow clone functionality internally depends on grafts
- Initial clone
- History deepening operations
- Unshallowing operations
- Required documentation/migration instructions for Git 3.0 release notes

Participant positions:
- End user (Askar Safin) strongly objects to removal until both history.git migration and shallow clone functionality are proven to work with git replace
- Bagas Sanjaya requests explicit confirmation about shallow clone compatibility with git replace
- Both positions indicate need for concrete verification before supporting deprecation

Related work:
- git replace functionality as grafts replacement
- history.git repository maintenance
- Git 3.0 deprecation planning
- Shallow clone implementation details