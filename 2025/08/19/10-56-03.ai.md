This is a refactoring patch that splits header declarations from the interactive add functionality as part of Patrick Steinhardt's RFC series for the new `git-history` command. The patch moves declarations related to `add-patch.c` out of `add-interactive.h` into a new `add-patch.h` header file. This creates clearer boundaries between the two components, making the codebase more maintainable by reducing header file bloat and improving logical separation.

The changes include:
- Creating new `add-patch.h` with declarations for `struct add_p_opt`, `enum add_p_mode`, and `run_add_p()` function
- Moving these declarations out of `add-interactive.h`
- Adding proper includes between the files to maintain functionality
- Keeping `add-i` specific declarations (like `add_i_state`) in the original header

This appears to be a preparatory change for the larger `git-history` series, likely needed to support the interactive components of the new history editing commands. The patch is straightforward and mechanical, with no behavior changes - just code reorganization. It follows good header hygiene practices by properly separating concerns between the patch and interactive subsystems.

The patch is part of a larger series (position 7/11) but this particular change seems self-contained and low-risk, focused purely on code structure rather than functionality. It maintains all existing functionality while making the codebase more navigable.