This thread began with Chris Judkins-Fisher raising two topics: the legacy status of `git whatchanged` and mailing list submission issues regarding HTML formatting. The technical discussion has focused on potential migration paths from `git whatchanged` (which is equivalent to `git log -p --raw --no-abbrev`) to modern alternatives.

Randall Becker proposed a user-side workaround using Git aliases (`git config --global alias.whatchanged 'log'`), which Kristoffer Haugsbakk clarified only works in Git 3.0+ due to core command aliasing limitations. In the latest message, Randall acknowledges this version constraint but maintains that the alias approach remains viable for modern Git installations. The exchange highlights that while the alias solution works for current versions, it leaves users of older Git installations without a simple migration path.

The discussion remains technical and focused on user-side solutions rather than proposing changes to Git itself. The parallel thread about mailing list submission issues hasn't seen further development since the initial report. At this point, the thread appears to be converging toward accepting that `git whatchanged` will remain as legacy functionality, with the alias workaround being the recommended path forward for those using Git 3.0 or later.