Here's the updated human-readable thread summary:

The thread began with Chris Judkins-Fisher raising two issues: the legacy `git whatchanged` command and HTML formatting problems when emailing patches. The technical discussion has focused on `whatchanged`, which is functionally equivalent to `git log -p --raw --no-abbrev`. Randall Becker initially suggested using aliases to migrate users away from the legacy command, but Junio Hamano clarified that core commands like `whatchanged` always take precedence over aliases, making that approach unworkable.

Following this technical clarification, Chris has shared a practical workaround - creating a `whatchange` (without the 'd') alias for `git log --raw`. This cleverly preserves the convenience of shell tab completion while working around the command precedence issue. The discussion now appears to be shifting toward practical adaptation strategies rather than technical solutions, with Junio having subtly suggested users simply adapt to using `git log --raw` directly.

The mailing list submission issue regarding HTML formatting remains unaddressed since the initial report. The technical discussion about `whatchanged` seems to be winding down, with participants converging on the understanding that while the command remains available for backward compatibility, users should transition to using `git log --raw` with whatever workflow adaptations work best for them.