This thread introduces a documentation patch that clarifies the remote repository options available to Git contributors when submitting pull requests. The patch updates `MyFirstContribution.adoc` to explicitly state that both `gitgitgadget/git` and `git/git` can be used as remotes, though they require different workflows. The original text only mentioned `gitgitgadget/git`, which could cause confusion for contributors who cloned from the main `git/git` repository.

The changes include clear instructions for both remote options, with a note about the need to open PRs from forks when using `git/git` as the base. The patch maintains the existing documentation structure while adding this helpful clarification for new contributors. The commit message provides a clear rationale for the change - avoiding potential confusion - and describes the straightforward solution of documenting both approaches. This appears to be a well-considered documentation improvement that should make the contribution process smoother for newcomers.