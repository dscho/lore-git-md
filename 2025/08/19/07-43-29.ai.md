This is a test fix addressing platform-specific failures in the automated help test outside repositories. Adam Dinwoodie's patch expands the exclusion list in t1517 to handle additional commands that appear when testing against installed Git binaries via GIT_TEST_INSTALLED.

The patch builds on the previous fix that excluded git-gui commands (citool, gui, gui--askpass) by now also excluding several credential helpers (credential-netrc, credential-libsecret, credential-osxkeychain) that appear in Nixpkgs packaging. The changes follow the existing pattern of special-casing commands that shouldn't be tested for help output outside repositories.

The test modification maintains the core verification of command help behavior while accounting for platform-specific command availability. The case statement now covers both the original git-gui commands and these additional credential helpers, all of which would otherwise cause test failures when present in an installed Git build.

This appears to be a straightforward extension of the previous fix, maintaining consistency with the test's approach of excluding non-standard commands. The patch is focused on making the test robust across different installation scenarios without changing the fundamental test behavior. The changes are minimal (4 lines added, 1 modified) and targeted specifically at the known failure cases.