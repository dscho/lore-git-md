This is a major refactoring series targeting the packfile subsystem, part of the ongoing effort to enable pluggable object databases. The series introduces a new `struct packfile_store` to centralize packfile management, which is currently spread across `struct packed_git` and `struct object_database`. 

The key technical changes involve moving various packfile-related fields and functions into the new structure, including the packfile chain, MRU list, initialization state, packfile map, and kept pack cache. The series systematically refactors core packfile operations like `prepare_packed_git()`, `install_packed_git()`, and `get_all_packs()` to work with the new store. Several helper functions are introduced to load and manage packfiles through this centralized interface.

The changes are wide-ranging, touching 31 files with 354 insertions and 271 deletions. The series appears well-structured with each commit focusing on moving specific functionality. The cover letter clearly explains the motivation - to enable future work where packfile management can be pushed down to individual object database implementations rather than being handled at the global level.

This is foundational work that doesn't change user-visible behavior but enables significant future architectural improvements. The changes are primarily in the packfile.c/h and odb.c/h files, with widespread but mechanical updates to callers. The series builds on commit c44beea485 and appears to be the first step in a larger effort to make packfiles work with pluggable object databases.