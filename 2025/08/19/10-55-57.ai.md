This is a preparatory patch for the `git-history` command series, modifying sequencer internals to support the new functionality. The patch introduces a `skip_commit_summary` option in the sequencer to suppress the default commit summary output during operations like cherry-pick. This will be used by the upcoming `git-history` commands where such output would be unnecessary noise.

The change adds the new flag to `struct replay_opts` and implements persistence through the sequencer state file. The implementation handles both runtime behavior and configuration storage, with the setting surviving across process restarts (important for conflict resolution scenarios). The patch touches core sequencer files (`sequencer.c` and `sequencer.h`) and maintains backward compatibility by keeping summary printing as the default behavior.

Key technical details:
- New `skip_commit_summary` boolean in `struct replay_opts`
- Modified `do_commit()` to check the flag before printing summaries
- Updated `populate_opts_cb()` and `save_opts()` to handle serialization
- Maintains existing behavior when flag is false (default)
- Part of larger series introducing Jujutsu-inspired history editing commands

This appears to be a well-contained infrastructure change that cleanly extends the sequencer's capabilities without disrupting existing functionality. The commit message clearly explains the motivation (preparing for `git-history` commands) and the implementation looks straightforward.