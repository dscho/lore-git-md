Refactoring patch in Junio Hamano's string-list API series. This is patch 2/7 in the v3 series that aims to unify and enhance Git's string-list splitting functionality.

The patch makes `string_list_split()` accept multiple delimiter characters (like its `_in_place` counterpart already does), rather than just a single character. This is a preparatory change before adding more features to both functions in later patches. The change is mechanical, updating 19 callers to pass delimiter strings in quotes rather than as single characters.

The patch demonstrates careful attention to backward compatibility and internal consistency, with thorough updates to both implementation and documentation. All test cases are updated to match the new behavior, and the change touches several important subsystems including diff, fetch, and ref-filter. This is a straightforward internal cleanup that makes the API more consistent without changing user-visible behavior.