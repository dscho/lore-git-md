This is a feature patch series (v4) introducing `git refs list` as a thin wrapper around `git for-each-ref`, now in final review stages with only minor nits remaining. The series has evolved through multiple rounds of maintainer feedback, with v4 representing a mature architectural approach that consolidates ref listing functionality under a shared core implementation.

The key technical elements remain unchanged from the thread summary - the series establishes `for_each_ref_core()` as the shared helper function, implements proper usage string synchronization via header macros, and follows Junio Hamano's preferred wrapper pattern. The test infrastructure has been refactored to share logic between `for-each-ref` and the new `refs list` command.

Patrick Steinhardt's latest review indicates the series is nearly ready for merging, with only minor documentation and code hygiene issues to address:
- Alphabetical include ordering in `builtin/for-each-ref.c`
- Proper documentation of the t0450 test change where `for-each-ref` entry was removed
- Suggestion to separate the documentation fix into its own commit

The architectural foundation is now solid, with all major technical concerns addressed across the four iterations. The remaining work appears to be limited to final polishing of commit organization and documentation details before potential merge into mainline Git.