Security test fix for t7450-bad-git-dotfiles.sh, verifying behavior from commit 05e9cd64 that prevents malicious repositories from executing post-checkout hooks during recursive submodule clones.

Justin Tobler confirms Jianhu Chen's v2 patch correctly updates the test to check the proper path where a vulnerable Git version would create files via post-checkout hook. The test now verifies the absence of "$PWD/bad-clone/sub/foo" rather than "$PWD/foo", matching the actual directory structure while still maintaining the test marker file "bad-clone/sub\r/post-checkout" to demonstrate the original vulnerability.

The patch follows earlier improvements to the commit message and presentation standards, with Justin noting the technical correctness of the path adjustment. This appears to be the final review before integration, with all prior feedback from Junio Hamano and Kristoffer Haugsbakk having been addressed in previous iterations.

Key technical details for future reference: the test verifies two security properties - prevention of post-checkout hook execution in submodules (first assertion) and blocking checkouts into symlinked directories (second assertion). The path correction ensures accurate verification of the first property while maintaining the test's dual verification approach.