This is a naming refinement discussion for the `git last-modified` command's recursive tree handling option.

Junio C Hamano suggests improving the clarity of the `--tree-in-recursive` option name in response to Toon Claes's documentation update. The maintainer proposes `--show-trees-in-recursive` as a more descriptive alternative that follows Git's established naming patterns (notably similar to `git ls-tree -t`). This continues the ongoing discussion about option naming clarity that emerged during post-merge review.

The suggestion focuses on adding an explicit verb ("show") to better communicate the action being performed on trees during recursive traversal. This refinement would affect both the implementation and documentation, requiring updates to the man page and any related help text if adopted. The change represents Git's characteristic attention to precise, self-documenting command-line interfaces.

This email moves the naming discussion forward from the previous proposals (`--tree-in-recursive` and `--recursive-with-trees`) while maintaining the core functionality unchanged. The thread shows maintainer guidance being applied to finalize interface details after technical implementation has been approved.