This is a technical discussion about implementation details in the `git last-modified` series, specifically around the data types used for boolean flags in the `last_modified` struct.

Christian Couder responds to Taylor Blau's suggestion about the `recursive` and `tree_in_recursive` flags in the struct, proposing to use the C99 `bool` type from `<stdbool.h>` instead of separate `int` declarations or a bitfield. This follows Git's coding guidelines which now permit using `bool`.

The exchange is part of the final polishing of the already-approved series, showing contributors working through minor implementation details before merging. The thread context indicates this is about optimizing the internal representation of flags in a performance-sensitive command, though the change would be purely stylistic rather than functional.