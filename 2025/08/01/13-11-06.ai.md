This is the foundational patch in a series introducing a new `git repo` command hierarchy, specifically focusing on repository metadata retrieval. It establishes the basic scaffolding for what will become a replacement for certain `git rev-parse` functionality that has grown beyond its original scope.

The patch creates:
1. A new `git-repo` command with initial `info` subcommand
2. Documentation stubs in `git-repo.adoc` marking it as experimental
3. Build system integration across Makefile, meson, and command-list
4. A minimal implementation in `builtin/repo.c` with parse-options setup

Key technical details:
- The command is marked EXPERIMENTAL in documentation
- Currently implements only a no-op `info` subcommand (actual functionality comes in later patches)
- Follows standard Git command scaffolding patterns
- Multiple mentors and helpers are credited, indicating careful review

This is patch 1/8 in v7 series that has undergone extensive review. The empty implementation here is intentional - subsequent patches will add the actual metadata retrieval capabilities that currently live in `git rev-parse`. The thread context shows this is part of a GSoC project with strong mentorship and thorough technical review through seven iterations.