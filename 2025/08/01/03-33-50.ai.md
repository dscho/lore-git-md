This is a technical refinement to patch 6/7 in Junio Hamano's string-list API refactoring series. Shejialuo suggests two implementation tweaks to the NONEMPTY flag handling:

1. Moving the empty-string check from append_one() back to split_string(), which would centralize more of the splitting logic in one place. This aligns with the ongoing discussion about optimal placement of trimming/splitting logic between these functions.

2. Questioning whether the empty-string check should use exact equality (end == p) rather than less-than-or-equal (end <= p), though the practical difference between these conditions in this context isn't immediately clear from the diff shown.

The suggestion appears consistent with the series' established direction of consolidating string splitting behavior while maintaining the same functionality. This continues the thread's technical focus on implementation details rather than design questions, with no objections raised to the overall approach. The change would affect string-list.c's internal implementation but not its external API.