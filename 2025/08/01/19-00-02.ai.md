This is a bug report and feature discussion regarding `git clean` behavior in nested repositories, continuing Alon Bar-Lev's earlier proposal. The email provides new technical details showing that `git clean -dxff` fails to remove untracked files (not just ignored files) from submodules, contrary to both the documented behavior and the author's expectations. The reproduction case demonstrates that `module1/junk.txt` remains after running the command when it should be removed.

Key technical points: The issue appears in the interaction between `-d` (directories), `-x` (ignored files), and `-ff` (force in nested repos) flags. The author initially thought this was specific to ignored files but now shows it affects all untracked files in submodules. The `dir.h` implementation is noted as complex, suggesting the fix may not be trivial.

The thread context reveals this builds on an earlier proposal about recursive ignored-file cleaning, but this email shifts focus to a more fundamental issue with untracked file handling. The discrepancy between documented behavior ("will refuse to modify untracked nested git repositories unless a second -f is given") and actual behavior suggests either a regression or longstanding bug.

This appears to be a legitimate issue worth investigating, though the complexity noted in `dir.h` may require careful handling. The email provides a clear reproduction case that could help diagnose whether this is a documentation bug, implementation bug, or intentional (but poorly documented) limitation.