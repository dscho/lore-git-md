Refactoring patch in the string-list API series, focusing on naming convention and flag implementation details.

Patrick Steinhardt provides final polish feedback on patch 4/7 of Junio Hamano's string-list refactoring series. The patch introduces flag-based variants of string splitting functions with a `_f` suffix and defines the TRIM flag. Steinhardt suggests two minor improvements: using `_ext` instead of `_f` for the function suffix to avoid confusion with formatting strings, and switching from a `#define` to an enum for the flags with explicit `(1 << 0)` bit shifting for better readability and grouping.

These suggestions are purely about coding style preferences, not affecting functionality. The feedback comes late in the review cycle where all architectural decisions are settled, representing final tweaks before merging. The thread context shows this is one of the last open points in a series that has already addressed all substantive feedback and is nearing completion. The changes would affect string-list.c/h internals only, maintaining backward compatibility while improving the API's consistency with Git's existing conventions.