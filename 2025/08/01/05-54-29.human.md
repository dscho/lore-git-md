This is a review comment on patch 2/5 of the RFC series introducing `git refs list`. Patrick Steinhardt points out two small issues in Meet Soni's patch that extracts core logic from `for-each-ref` into a shared helper.

First, he notes the new `#include` statement in `for-each-ref.c` should follow alphabetical ordering with the other includes. This is a minor style issue but important for maintainability.

More significantly, he questions the removal of `for-each-ref` from the `adoc-help-mismatches` test file. While the change itself (aligning the usage string with the man page) is good, Patrick suggests it should be in a separate commit since this patch is meant to be a pure refactoring without behavior changes. This follows the Git project's preference for keeping refactoring and behavior changes distinct.

The comment shows careful review of the patch's scope and adherence to project conventions. Patrick's tone is constructive guidance rather than rejection, acknowledging the underlying improvement while suggesting better organization. This continues the thread's pattern of iterative refinement based on maintainer feedback.