This is a review comment on patch 2/5 of the `git refs list` RFC series, focusing on code organization and commit hygiene. Patrick Steinhardt points out two specific issues in Meet Soni's implementation of the core logic extraction for ref listing.

First, he notes the includes in `builtin/for-each-ref.c` should remain alphabetically sorted, with the new `for-each-ref.h` needing proper placement. This is a standard coding style requirement in Git's codebase.

Second, he observes an undocumented change in `t/t0450/adoc-help-mismatches` where the `for-each-ref` entry was removed. While this aligns the documentation with actual usage (a positive change), Patrick suggests this behavioral change deserves its own commit with proper explanation, rather than being silently included in a refactoring patch.

The feedback maintains the series' technical direction while emphasizing commit discipline - keeping refactoring and behavior changes separate, and documenting all intentional modifications. This follows Git's development norms where even correct changes need proper commit separation and messaging. The core architectural approach (extracting `for_each_ref_core()`) remains uncontested.