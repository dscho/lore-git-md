This is part 2/5 of a feature series introducing the new `git repo info` command, specifically implementing the `references.format` field to report whether a repository uses files or reftable for reference storage.

The patch migrates functionality from `git rev-parse --show-ref-format` into the new command hierarchy. It implements the field lookup system with proper error handling (returning valid fields even when invalid keys are requested) and includes thorough tests verifying:
- Correct output for both files and reftable formats
- Error behavior for invalid keys
- Proper handling of duplicate key requests
- Output consistency with the existing rev-parse command

The implementation uses efficient string handling with `strbuf` and maintains lexicographical ordering of output fields. The documentation is updated to explain the new field alongside the command's existing capabilities. This appears to be a well-constructed patch with clear motivation, complete test coverage, and attention to edge cases.