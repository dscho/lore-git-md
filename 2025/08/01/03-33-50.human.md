This is a technical refinement suggestion for Junio Hamano's string-list API refactoring series. Shejialuo proposes moving the NONEMPTY string filtering logic from the append_one() helper function (where it currently lives in patch 6) up to the split_string() function for better architectural clarity. 

The suggestion comes with two specific points: first, that the empty-string check would be more at home in the splitting logic itself rather than during appending, and second, a question about whether the boundary condition check (end <= p) should be tightened to (end == p). 

This continues the thread's pattern of focused implementation discussions about where to place functionality in the string-list refactoring. The change appears compatible with the series' goals and doesn't challenge the overall design, but seeks to improve the internal organization. The suggestion is brief and to the point, typical of the technical back-and-forth common in later stages of patch review.