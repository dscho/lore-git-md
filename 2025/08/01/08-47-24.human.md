Refactoring patch in the string-list API series, focusing on naming conventions and flag implementation details.

This email from Patrick Steinhardt provides final polish feedback on patch 4/7 of Junio Hamano's string-list refactoring series. The patch introduces flag-based control over string trimming behavior in string splitting functions. Patrick suggests two minor improvements: using `_ext` rather than `_f` as the suffix for the new flag-taking variants (to avoid confusion with formatting strings), and preferring enum declarations with explicit bit shifts (`1 << 0`) over macro definitions for the new flags.

These suggestions represent purely stylistic preferences in the final stages of review, as the thread's core technical decisions about the string-list API changes were already settled in earlier discussions. The feedback doesn't challenge the functionality or architecture of the changes, but rather focuses on making the code more consistent with Git's current conventions and more readable for future maintainers.

The series as a whole remains on track for merging, with this exchange representing the kind of final polish discussion that typically occurs just before a maintainer applies the patches. The changes are internal-only refactoring that won't affect end users.