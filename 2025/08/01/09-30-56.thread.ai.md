Here is the updated AI thread summary incorporating the new email:

Subject: Fetch failure in blobless clones when adding new remote. Thread type: bug report. Originating author: Justin Su.

Core problem: Fetch operations fail with "fatal: did not receive expected object" and "invalid index-pack output" in blobless clones (`--filter=blob:none`) when `transfer.fsckObjects` or `fetch.fsckObjects` is enabled. The failure occurs during object verification when fsck encounters references to intentionally missing blobs in a filtered clone, incorrectly treating them as corruption rather than legitimate omissions.

Current status: Jeff King has provided both a concrete reproduction case and a working fix that marks promisor objects as "checked" when verification fails due to their intentional absence. The reproduction script demonstrates the issue by creating a server with two file versions, cloning with blob filtering, then fetching from a fork referencing the older blob. The fix prevents fsck from treating missing promisor objects as corruption while maintaining verification for non-promisor objects.

Key technical details: The bug manifests in `builtin/index-pack.c` during fsck verification of promisor objects in filtered clones. The reproduction isolates the sequence where a filtered clone (lacking an old blob but having a newer version) fails when fetching a reference to the old blob. Object 0020d54b979cc8cf59a13406f98bfe515b190559 was initially identified in failures. The solution involves modifying fsck's handling of promisor objects rather than addressing server-side delta generation.

Open questions: Performance impact of additional `is_promisor_object()` checks remains the primary outstanding concern. Whether similar verification issues affect other filter types (tree:none, sparse-checkout) could be investigated using the provided reproduction framework. Jonathan Tan's review of the solution is still sought given his authorship of related code.

Participant positions: Justin Su provided initial report. Jeff King confirmed reproduction, diagnosed root cause, proposed and validated the solution while flagging performance considerations. Thread consensus solidifies around this being a bug in fsck/partial-clone interaction rather than expected behavior.

Related work: This exposes edge cases in partial clone behavior interacting with object verification. Connects to pack generation optimizations and delta compression strategies, particularly server handling of missing bases. The fsck component introduces verification expectations needing protocol-level consideration. The reproduction case provides a template for testing similar scenarios with other filter types.