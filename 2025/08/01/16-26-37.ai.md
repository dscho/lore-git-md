This is a technical discussion about naming conventions in the string-list API refactoring series. Junio Hamano responds to Patrick Steinhardt's feedback on patch 4/7 regarding function suffixes and flag definitions.

The exchange focuses on two specific style points from Steinhardt's review:
1. Preference for `_ext` suffix over `_f` for flag-taking variants, citing potential confusion with "formatting string" interpretation
2. Recommendation to use enum with explicit bit shifts (`1 << 0`) rather than `#define` for the STRING_LIST_SPLIT_TRIM flag

Hamano acknowledges the feedback while providing context about existing naming patterns in the codebase, noting that `_ext` is already used in midx.c for different purposes. He compares the situation to `oid_object_info_extended()` but expresses general dissatisfaction with verbose suffix options. On the flag definition style, Hamano accepts the enum suggestion without reservation.

The discussion represents final polish for the already-technically-complete series, with these being the last open questions before merging. Both points are purely about code style preferences rather than functionality, and Hamano appears ready to implement the enum change while considering the suffix naming more carefully. This email confirms the architectural decisions from earlier in the thread remain settled.