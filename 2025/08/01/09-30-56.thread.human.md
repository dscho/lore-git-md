Here's a human-readable thread summary:

This thread reports a bug in Git's handling of blobless clones (`--filter=blob:none`). The reporter demonstrates that when fetching from a new remote into a blobless clone, Git fails with "fatal: did not receive expected object" followed by "invalid index-pack output", despite the remote properly enumerating and transferring objects. The issue appears specific to repositories created with partial object storage.

The report includes a clear reproduction case showing the failure when adding an upstream remote and attempting to fetch, along with complete system details (Git 2.50.1 on macOS) and the exact object hash that failed to transfer. While no fix is proposed yet, the detailed error output and reproduction steps make this a well-documented bug report that should be straightforward to investigate. The behavior suggests a problem in how Git verifies object expectations during fetches into filtered clones.