This is a naming refinement for the `git last-modified` command's recursive tree handling option, part of the post-merge polishing phase.

Junio C Hamano suggests improving the clarity of the `--tree-in-recursive` option name by adding an explicit verb, proposing `--show-trees-in-recursive` instead. This follows the naming convention used in `git ls-tree -t` and makes the option's purpose more immediately obvious to users. The suggestion comes in response to Toon Claes's earlier documentation update that aligned the short and long option forms (-t` and `--tree-in-recursive`).

This continues the thread's ongoing discussion about command interface clarity, where multiple naming variants (`--recursive-with-trees` was also proposed) are being considered. The change would be purely cosmetic at this point, as the underlying functionality is already merged and working. The patch series has otherwise addressed all major technical concerns, with this being one of the final naming details to resolve.