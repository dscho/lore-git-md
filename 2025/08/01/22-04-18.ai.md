Refactoring patch in Junio Hamano's string-list API series, aligning string_list_split() with string_list_split_in_place() by allowing multiple characters as delimiters. This change makes the two functions more consistent before adding new features in subsequent patches.

The patch modifies string_list_split() to accept a string of delimiter characters rather than a single char, using strpbrk() instead of strchr() for the splitting logic. This affects 19 files across the codebase where string_list_split() is called, updating callers to pass delimiter strings (like "," or " ") instead of single characters. The string-list.h documentation is updated to reflect the new behavior, showing examples with string delimiters.

Test coverage in t/unit-tests/u-string-list.c is updated to match, though the test cases remain identical in substance - only the delimiter parameter changes from char to string. The change is purely internal with no user-facing impact, maintaining backward compatibility while preparing for future API extensions.

This aligns with the series' goal of unifying string-list behavior while cleaning up duplicate code. The mechanical nature of the changes and thorough test updates suggest this is a low-risk refactoring that maintains the established API contract.