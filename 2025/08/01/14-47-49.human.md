This is a platform compatibility patch in Patrick Steinhardt's series improving reftable support for libgit2. It fixes a compiler warning that appears on older toolchains when initializing nested structs. The change is purely syntactic - replacing `{0}` with `{{0}}` in a macro definition to satisfy stricter brace requirements while maintaining identical functionality. 

The patch is straightforward and uncontroversial, addressing a real-world issue encountered when building with libgit2's toolchain. It's part of the broader effort to make reftable code more portable across different environments. The change is minimal (just one line modified) and focused on eliminating a specific warning without introducing any behavior changes. 

This continues the series' pattern of small, targeted fixes to improve compatibility between Git's reftable implementation and libgit2's requirements. The patch appears well-justified and properly scoped for its purpose.