This is a post-merge discussion about naming and type usage in the `git last-modified` command series, specifically regarding boolean flags in the implementation.

Junio Hamano responds to Christian Couder's suggestion about using `bool` from stdbool.h for the recursive flags in the `last_modified` struct. While noting that the coding guidelines merely "allow" rather than "encourage" this, Hamano agrees it would be an improvement over the current `int` flags. He also suggests renaming `tree_in_recursive` to the more descriptive `show_trees_in_recursive` to better match the functionality (similar to `git ls-tree -t`) and eliminate ambiguity in the flag's purpose.

This continues the thread's theme of post-merge refinement, focusing on code clarity and consistency. The discussion touches on two key aspects: type system improvements (using proper boolean types) and naming conventions (making flag purposes self-documenting). Hamano's comments align with Git's emphasis on readable, maintainable code even after initial implementation approval. The suggestion builds upon earlier naming discussions in the thread while introducing the additional consideration of type system usage.