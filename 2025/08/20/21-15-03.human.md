This is a refactoring patch in Patrick Steinhardt's `git-history` RFC series, specifically focusing on the `add-patch` infrastructure. The patch introduces a new `run_add_p_index()` function that allows applying changes to an in-memory index rather than always operating on the repository's main index.

D. Ben Knoble provides a focused code review, pointing out two observations: first, a potentially duplicated call to `read_index_from()` in the patch, and second, noting that while `run_add_p()` calls `discard_index()` before parsing diffs, this may not be necessary for in-memory indices. The review is constructive and technical, suggesting the author may want to clarify or adjust these implementation details.

The change appears to be preparatory work for the upcoming `split` subcommand implementation in the series. The review engagement shows active discussion around the technical details of the patch, though no major concerns are raised about the overall approach. The patch seems to be progressing the series' goal of building flexible history editing operations while maintaining clean code organization.