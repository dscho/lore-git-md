This is a follow-up in the ongoing discussion about `git whatchanged` usage and potential migration paths. Chris Judkins-Fisher responds to Junio Hamano's technical clarification about command aliasing by sharing their personal workflow - they use tab-completion (`git wh<tab>`) which makes `whatchanged` more convenient than typing out `git log --raw`. As a practical workaround, they've created a shorter alias `whatchange` (note the missing 'd') that maps to `log --raw`.

The email reinforces two key points from the thread: (1) the original suggestion to alias `whatchanged` directly doesn't work due to Git's command precedence rules (as Junio demonstrated), and (2) users have legitimate workflow reasons (like tab-completion) for preferring certain command forms. Chris's solution of creating a similarly-named alias shows one way users might adapt while waiting for potential future changes to `whatchanged`'s status. The thread remains a discussion rather than a code proposal, with no immediate changes to Git's behavior.