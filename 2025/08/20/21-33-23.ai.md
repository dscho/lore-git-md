This is a response in an ongoing thread about spurious "lock file exists" errors during fetch operations in bare repositories. Jeff King suggests two potential explanations for the issue reported by Joe Drew where Git 2.51.0 fails with lock file errors for non-existent branches on macOS ARM64.

First, Jeff hypothesizes this might be due to case-sensitivity issues on macOS's case-insensitive filesystem, where colliding branch names (like "ptv-2164" vs "PTV-2164") could cause lock file conflicts that are cleaned up before being visible. He suggests testing with the reftable backend as a workaround since it doesn't use filesystem-based locking.

Second, Jeff notes the version discrepancy (working in Apple's Git 2.39.5 but failing in Homebrew's 2.51.0) is unexpected for a simple case collision scenario. He recommends building Git from source to bisect when the regression was introduced, offering to provide guidance on that process if needed.

Key technical details include the reftable backend suggestion (`--ref-format=reftable`), the specific versions in question (2.39.5 vs 2.51.0), and the macOS ARM64 environment. The response leaves open whether this is a case-sensitivity issue, regression, or platform-specific bug, while providing concrete next steps for investigation.