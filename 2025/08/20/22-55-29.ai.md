This is the first patch in a refactoring series targeting the bulk-checkin subsystem as part of the `the_repository` removal effort. It introduces a new `struct odb_transaction` to consolidate three previously separate global variables (`odb_transaction_nesting`, `bulk_fsync_objdir`, and `bulk_checkin_packfile`) into a single structure. 

The changes are purely structural, moving the transaction state management into a more organized form while maintaining identical behavior. The patch touches only `bulk-checkin.c`, modifying 46 lines (24 insertions, 22 deletions) to replace direct global variable access with references through the new transaction structure. Key modifications include moving the packfile handling into the transaction struct and updating all related functions to use the new structure.

This initial step lays groundwork for subsequent patches that will wire the repository parameter through the bulk-checkin interfaces. The commit message clearly states this is preparatory work for eliminating global state, following Git's architectural modernization patterns. The changes appear mechanical and well-contained, with no behavioral changes expected.