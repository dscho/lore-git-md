Refactoring patch for git-gui's PATH handling, simplifying the de-duplication logic.

The patch removes redundant data structures in git-gui's PATH processing code. The current implementation uses both a list (`_search_path`) and a dictionary (`_path_seen`) to maintain PATH elements in order while avoiding duplicates, but this is unnecessary since Tcl dictionaries already preserve insertion order. The change replaces this with a simpler approach that just uses the dictionary's ordered keys.

The modification affects how git-gui processes the PATH environment variable, particularly on Windows where path elements are colon-separated. The core behavior remains identical - it still normalizes paths, filters non-absolute paths, and removes duplicates - but does so with less code. The diff shows a net reduction of 6 lines while maintaining the same functionality.

This appears to be a straightforward cleanup with no visible downsides. The commit message clearly explains the change and references the prior commit (8fe7861c51) that introduced the current implementation. The change is limited to git-gui.sh and doesn't affect other parts of the system. Since it's purely an internal refactoring with no behavior change, it's likely to be uncontroversial.