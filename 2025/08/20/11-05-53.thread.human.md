This thread documents and fixes a series of color handling inconsistencies in Git's interactive patch mode (`--patch`) that emerged after the 2021 Perl-to-C conversion. Jeff King has led the effort, identifying and addressing four key issues across commands like `stash`, `checkout`, `reset`. The technical fixes ensure proper handling of `color.diff` and `color.ui` settings, with careful attention to edge cases like the `GIT_PAGER_IN_USE` environment variable.

The series is now complete with a final documentation patch from Isaac Oscar Gariano, which adds mention of the `interactive.diffFilter` workaround to diff-highlight's README. This ensures users who discover the highlighting tool first will also learn about its integration with Git's interactive commands. The change is small but important for discoverability, complementing the deeper technical fixes that address the root causes of the color handling regressions. With this last piece in place, users now have both correct behavior and clear documentation across all aspects of colorized interactive patching.