Thread identity: Refactoring of git-gui's `nice` command handling for long-running Git operations, now at version 2.

Core problem or goal: Simplify how git-gui runs commands with `nice(1)` by replacing dynamic availability checks during each command execution with a single check at startup, while maintaining identical functionality.

Current status: Second version incorporating feedback, with path handling fixes and documentation improvements. The technical approach remains uncontested and appears ready for inclusion.

Key technical details:
- Removes `_lappend_nice` helper function that previously checked `nice` availability dynamically
- Introduces global `_nice` variable (now properly initialized as a list) set once at startup
- Uses Tcl's `{*}` expansion operator for proper command construction with spaces in paths
- Modifies `git_read_nice` to use the pre-checked `_nice` value
- Reduces code from 23 lines to 7 while maintaining identical functionality
- Touches git-gui's command execution infrastructure
- Documentation improvements: uses "nice(1)" notation and fixes "lets" -> "let's" in commit message

Open questions and loose ends: None remaining - all feedback from v1 has been addressed and no new concerns raised.

Participant positions:
- Mark Levedahl: original author proposing the refactoring, implemented v2 changes
- Kristoffer Haugsbakk: suggested minor documentation improvements in v1 which were incorporated

Related work: Self-contained refactoring within git-gui's existing command execution infrastructure. The follow-up addressed presentation aspects and edge case handling (paths with spaces) while keeping the core approach unchanged.