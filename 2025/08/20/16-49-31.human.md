This is a discussion about the legacy `git whatchanged` command and its potential replacement via aliases. Junio C Hamano clarifies a technical point about command aliasing behavior, noting that aliases with the same name as built-in commands are silently ignored (rather than failing) in pre-3.0 Git versions. He demonstrates this with a test case showing `git whatchanged` continuing to work despite an alias definition.

The thread has evolved from initial usage reports to technical details about migration paths. While Randall Becker had suggested using `git log` aliases as a replacement, Kristoffer Haugsbakk pointed out version compatibility limitations. Junio now adds that the alias approach will work in newer Git versions once the built-in command is removed, but suggests users might be better off just typing `git log --raw` directly since it's shorter.

The discussion remains focused on user-side solutions rather than proposing any changes to Git itself. No new action items emerge from this email - it primarily serves to correct a technical misunderstanding about how command aliasing interacts with built-ins. The thread appears to be winding down with no strong push to either keeping or removing `git whatchanged` from Git's codebase.