This is a follow-up patch in the submodule gitdir path encoding series, addressing a test failure found during CI testing. Josh Steadmon points out that while Adrian Ratiu's patch correctly converts the submodule path handling to use the new `submodule_name_to_gitdir()` function, there's an opportunity to further clean up the code by making the `submod_gitdir_path` variable local to the block where it's actually used.

The change is straightforward - it moves from using a manually constructed path (`.git/modules/%s`) to the centralized path encoding function, which is part of the larger effort to make submodule paths filesystem-safe across platforms. The patch maintains all existing functionality while aligning with the series' goal of consistent path handling.

This appears to be a minor but sensible improvement to an already-reviewed patch, addressing both the immediate test failure and a small code organization issue. The discussion seems focused on implementation details rather than any fundamental disagreement about the approach.