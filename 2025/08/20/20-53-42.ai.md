Maintainer response in `git whatchanged` deprecation thread.

Junio Hamano (Git maintainer) responds to Dan Allen's query about replacing `git whatchanged --pretty=%at` in a Python script that synchronizes filesystem mtimes with Git modification times. The maintainer suggests `git log --raw --pretty=%at` as a direct alternative, implicitly confirming this is the intended migration path.

Key technical points:
- Maintainer's proposed replacement drops `--no-merges` from Kristoffer's earlier suggestion, potentially indicating merges aren't relevant to the timestamp extraction use case
- The response is phrased as a question, leaving room for Dan to confirm whether this meets his needs or identify any discrepancies
- No mention of `whatchanged` removal timeline, focusing instead on the technical solution

This moves the thread forward by:
- Providing authoritative confirmation that `git log` can directly replace `whatchanged` for this format
- Shifting focus from documentation references to practical verification
- Leaving the deprecation timeline question unaddressed (likely intentionally)

The next expected steps would be:
1. Dan Allen testing the suggested command
2. Potential follow-up on any output format differences
3. Possible clarification from maintainers about deprecation plans if still needed