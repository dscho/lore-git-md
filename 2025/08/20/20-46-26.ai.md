This is a follow-up in the thread about potential `git whatchanged` removal, providing migration guidance for timestamp extraction use cases.

Kristoffer Haugsbakk responds to Dan Allen's query about replacing `git whatchanged --pretty=%at` with a direct answer from the documentation. The response notes that the documented replacement is `git log --raw --no-merges` with the same formatting options, implying this should work for the timestamp extraction use case. The signature "semantic drift" suggests this might be a known documentation-to-reality gap that needs verification.

Key technical details for future reference:
- The exact documented replacement command is provided: `git log --raw --no-merges`
- The original timestamp format specifier `%at` would presumably work the same way in this replacement
- No actual testing or verification of the solution is shown
- The response comes from documentation rather than practical experience

The email advances the thread by providing a concrete migration path from the docs, though leaves open whether this actually works equivalently in practice for the timestamp extraction use case. This positions the thread for either confirmation from someone who has tested this approach, or for further discussion if the documented replacement proves insufficient.