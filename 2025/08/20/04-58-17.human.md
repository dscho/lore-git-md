Refactoring patch in the packfile subsystem abstraction series. This is a follow-up to Karthik Nayak's style nit about comment alignment in the first patch of Patrick Steinhardt's 16-part series introducing `struct packfile_store`.

Patrick acknowledges Karthik's observation that the comment placement in odb.h could be misinterpreted as applying only to the new `packfiles` field rather than also covering the existing `packed_git` struct. While noting that subsequent patches will eventually remove the ambiguity by moving all packfile-related state to the new store structure, Patrick agrees the comment could be clearer even in this transitional state.

This is a minor documentation clarification in the ongoing effort to centralize packfile management into a dedicated `packfile_store` structure, separating it from the generic object database` abstraction. The exchange shows careful attention to code readability during this large-scale refactoring.