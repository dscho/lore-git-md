This is a style cleanup suggestion in Patrick Steinhardt's ongoing packfile refactoring series. Karthik Nayak points out several places where curly braces can be removed from single-statement loops in files modified by the series (`builtin/gc.c`, `http-backend.c`, and `http.c`). The changes are purely stylistic and don't affect functionality, but they do help maintain consistency with Git's coding guidelines.

The email shows the specific diffs where `for` loops with single statements can drop their braces. This comes as a follow-up to the broader discussion about the architectural changes in patch 15/16, which refactors `get_all_packs()` to work directly with the new `packfile_store` structure rather than going through the repository object.

These are minor style nits that could easily be incorporated into a future revision of the series. The changes are straightforward and uncontroversial, though they do touch multiple files that were modified as part of the larger packfile store refactoring effort.