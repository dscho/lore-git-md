Thread identity: Feature patch series (2 parts) adding enhancements to `git repo info` from a GSoC contributor.

Core problem/goal: Two independent improvements to `git repo info`:
1. Add `-z` flag as shorthand for `--format=null` (following Git's convention for null-terminated output)
2. Add `objects.format` field exposing repository's object format (equivalent to `git rev-parse --show-object-format`)

Current status: Both patches now posted in initial version (v1) of series complete:
- First patch adds `-z` flag with documentation updates in builtin/repo-info.c and new tests in t1900-repo.sh
- Second patch adds `objects.format` field with handler in builtin/repo.c, documentation updates, and tests for sha1/sha256 cases
- Both have mentor sign-offs (Karthik Nayak and Patrick Steinhardt) as part of GSoC project

Key technical details:
- `-z` flag mirrors null-terminated output behavior from git-config/git-status
- `objects.format` reads from `repo->hash_algo->name` to expose hash algorithm
- Changes in builtin/repo-info.c and builtin/repo.c
- New tests verify both features work and maintain expected behavior
- Documentation updates in both command help and man pages

Open questions:
- Should these unrelated changes be in separate series despite merge conflict concerns?
- Any feedback on implementation approach for either patch?

Participant positions: No substantive review yet, but both patches have mentor sign-offs from established contributors (Karthik Nayak and Patrick Steinhardt) as part of GSoC project.

Related work: Builds on existing `git repo info` command, follows conventions from git-config and git-status for null-terminated output, aligns with Git's object format handling via `rev-parse`.