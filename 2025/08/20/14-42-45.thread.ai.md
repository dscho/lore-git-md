Thread identity: Feature patch series (2 parts) adding enhancements to `git repo info` from a GSoC contributor.

Core problem/goal: Two independent improvements to `git repo info`:
1. Add `-z` flag as shorthand for `--format=null` (following Git's convention for null-terminated output)
2. Add `objects.format` field exposing repository's object format (equivalent to `git rev-parse --show-object-format`)

Current status: Initial version (v1) of series posted. First patch adds `-z` flag with:
- Documentation updates in builtin/repo-info.c
- New tests in t1900-repo.sh
- Follows established patterns from other Git commands
- Clean implementation with mentor sign-offs

Key technical details:
- Changes in builtin/repo-info.c (option parsing and output handling)
- New tests verify `-z` works and conflicts properly with `--format`
- Mirrors null-terminated output behavior from git-config/git-status
- Second patch (not yet discussed) will add `objects.format` field

Open questions:
- Should these unrelated changes be in separate series despite merge conflict concerns?
- Any feedback on implementation approach for either patch?

Participant positions: None yet (initial posting) but has mentor sign-offs from established contributors as part of GSoC project.

Related work: Builds on existing `git repo info` command, follows conventions from git-config and git-status for null-terminated output.