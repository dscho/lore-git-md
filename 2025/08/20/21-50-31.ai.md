This is a refinement suggestion for the submodule gitdir path encoding series. Josh Steadmon points out that patch 3/9, which introduces a config override mechanism for submodule gitdir paths, should document the new config option in `Documentation/config/submodule.adoc`. The patch currently adds the functionality and tests but lacks the corresponding documentation.

The thread context shows this is part of a larger effort to handle submodule gitdir path encoding, with the series currently in refinement stage addressing platform-specific issues and test coverage gaps. The core technical approach remains stable, with reviewers focusing on completeness and quality improvements like this documentation oversight.

Key technical details from the thread that relate to this patch:
- The config override mechanism is part of the path construction flow in `submodule_name_to_gitdir()`
- The series maintains backward compatibility while transitioning to new encoded paths
- Test coverage is being expanded across multiple test scripts (t7400, t7406, t7425)

This feedback doesn't indicate any technical problems with the implementation, just a documentation gap that should be addressed in the next revision. The suggestion aligns with the series' current focus on refinement and completeness before finalizing the changes.