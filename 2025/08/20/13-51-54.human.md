This is a minor style suggestion in Patrick Steinhardt's ongoing refactoring series to move packfile management into a dedicated `packfile_store` structure. The series is part of the larger object database abstraction effort.

Karthik Nayak points out a small code style nit in patch 14/16, which removes the `get_packed_git()` function in favor of `get_all_packs()`. In `builtin/gc.c`, he notes that the curly braces around a simple `for` loop could be removed since the loop body is just a single conditional statement. This is purely a cosmetic suggestion and doesn't affect functionality.

The suggestion comes as part of the ongoing review of this mechanical refactoring series, where reviewers are looking at both the architectural changes and smaller code quality issues. The series continues to progress steadily with these kinds of minor refinements being discussed.