This is a minor style suggestion on a git-gui refactoring patch. Kristoffer Haugsbakk responds to Mark Levedahl's patch that simplifies how git-gui handles the `nice` command for long-running operations.

The original patch (which we haven't seen directly) apparently refactors git-gui's use of `nice(1)` by removing dynamic availability checks in favor of a single startup check. Kristoffer suggests two small wording improvements to the commit message: clarifying that "nice" refers to the Unix utility by adding "(1)" to its first mention, and fixing a grammatical issue ("lets" -> "let's").

The suggestions appear constructive and non-controversial. Since this is just a style nit on the commit message rather than substantive feedback on the code changes, it's unlikely to generate much discussion unless the original author disagrees with the proposed edits. The thread seems to be in its early stages with just these two participants so far.