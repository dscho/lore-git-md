This is a thread response providing a migration path for a `git whatchanged` use case. Kristoffer Haugsbakk responds to Dan Allen's query about replacing `git whatchanged --pretty=%at` in a Python script that synchronizes filesystem mtimes with Git commit timestamps.

The response notes that the git-whatchanged manpage already documents a direct replacement: `git log --raw --no-merges` can be used with the same `--pretty` format specifiers. This suggests the timestamp extraction functionality Dan Allen needs is preserved in the recommended alternative.

The reply is concise and technical, focusing solely on the command substitution without additional commentary about the deprecation itself or broader implications. It appears to be a straightforward solution to the specific use case raised in the original query, though it doesn't address whether there might be subtle differences in output format or edge cases between the two commands.

This moves the thread forward by providing a clear migration path while leaving open whether this solution fully covers all of Dan Allen's needs or if there might be other considerations in the timestamp extraction workflow.