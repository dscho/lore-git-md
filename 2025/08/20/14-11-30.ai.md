This is a follow-up in the ongoing discussion about grafts removal, specifically questioning the compatibility of shallow clones with the planned replacement mechanism (git replace).

The email from Bagas Sanjaya seeks clarification about whether shallow clone operations (including initial shallow clones, deepening with `--shallow-since`, and `--unshallow`) will internally use git replace functionality once grafts are removed. This directly addresses one of the open questions in the thread about whether critical functionality like shallow cloning depends on grafts.

The question builds on Askar Safin's earlier concern that `git clone --depth=1` might rely on grafts, though no technical details are provided about why this might be the case or what specific aspects of shallow cloning could be affected. The email appears to assume that git replace should transparently handle these cases but seeks explicit confirmation of this assumption.

This continues the thread's pattern of users requesting concrete verification that grafts removal won't break existing workflows before supporting the deprecation. The question about shallow clone internals suggests this may be another area needing documentation in the Git 3.0 release notes if grafts are indeed removed.