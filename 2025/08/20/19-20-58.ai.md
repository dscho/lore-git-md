This is a user query about the potential removal of `git whatchanged` in Git 2.51. The author relies on this command in a Python script that sets file modification times to match their last commit dates, using `git whatchanged --pretty=%at` to get the timestamps. They're seeking alternative methods to achieve the same functionality if `git whatchanged` is indeed removed.

Key technical details:
- Current usage involves parsing UNIX timestamps (`%at`) from `whatchanged` output
- Use case involves filesystem metadata manipulation (mtime updates)
- The script appears to be for local repository browsing purposes
- No specific alternatives are proposed in this initial message

The email doesn't reference any existing discussion about `whatchanged` deprecation, suggesting this may be a new thread started in response to the removal warning. The request is practical rather than argumentative - the author appears willing to migrate to an alternative if one exists.