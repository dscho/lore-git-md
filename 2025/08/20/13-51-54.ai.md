This is a minor code style follow-up in Patrick Steinhardt's ongoing packfile refactoring series. Karthik Nayak points out a nit in patch 14/16 where curly braces could be removed in `builtin/gc.c` after the change from `get_packed_git()` to `get_all_packs()`. The loop in question checks packfiles for incremental repacking conditions.

The thread context shows this is part of a larger effort to move packfile management into a dedicated `packfile_store` structure, with patch 14 specifically removing the `get_packed_git()` function in favor of `get_all_packs()`. Karthik had previously raised more significant questions about MIDX edge cases and data structure choices, but this particular email focuses only on a small style improvement opportunity in the gc.c changes.

The suggestion doesn't affect functionality but would make the code slightly cleaner by removing unnecessary braces around a simple loop body. This is the kind of minor polish that often comes up in later review rounds after the main technical discussion has settled. The change would be consistent with Git's coding style guidelines which generally omit braces for single-statement blocks.