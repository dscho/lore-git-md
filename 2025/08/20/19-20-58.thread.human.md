Here's a human-readable summary of the thread so far:

Dan Allen is using `git whatchanged --pretty=%at` in a Python script to synchronize filesystem modification times with Git commit timestamps. With `git whatchanged` potentially being removed in Git 2.51, he asked for alternatives. Kristoffer Haugsbakk initially suggested using `git log --raw --no-merges --pretty=%at` based on documentation, noting this was the recommended replacement path.

Git maintainer Junio Hamano has now weighed in, confirming the general approach but simplifying the command to just `git log --raw --pretty=%at` (omitting `--no-merges`). This suggests the merge filtering may not be necessary for the timestamp extraction use case. While Junio didn't address the deprecation timeline directly, his response provides authoritative confirmation that `git log` can serve as a drop-in replacement for this specific formatting need.

The thread now awaits practical verification from Dan Allen on whether Junio's suggested command produces equivalent output for his script's purposes. Any differences in behavior or edge cases (particularly around file renames or deletions) would be the next points to explore. The question of when exactly `git whatchanged` will be removed remains open, but the technical migration path now has maintainer approval.