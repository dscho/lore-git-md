Thread identity: User query about potential removal of `git whatchanged` in Git 2.51, seeking alternatives for timestamp extraction.

Core problem: Author's Python script uses `git whatchanged --pretty=%at` to get UNIX timestamps of last modification for files, then sets filesystem mtimes accordingly. Needs migration path if command is removed.

Current status: No deprecation timeline or alternatives discussed yet in thread. Initial query only.

Key technical details:
- Current method extracts UNIX timestamps via `%at` format from `whatchanged`
- Script purpose: Local repository browsing via filesystem metadata
- Affected workflow: Filesystem mtime synchronization with Git context:
- `whatchanged` is a legacy command largely superseded by `git log`
- Similar timestamp data available via `git log` formats
- `--pretty=` formats are common to both commands

Open questions:
- Whether `whatchanged` removal is confirmed for 2.51
- What exact `git log` incantation could replace this specific use case
- Whether other edge cases exist in timestamp extraction

Participant positions: Only original author so far, with practical migration question. No maintainer or regular contributors have responded yet.

Related work: General `git log` format capabilities should cover this use case, but exact replacement syntax needs specification. No prior threads about `whatchanged` deprecation referenced.