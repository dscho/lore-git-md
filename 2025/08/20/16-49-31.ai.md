This is a technical clarification in the ongoing discussion about `git whatchanged` usage and aliasing. Junio C Hamano corrects Kristoffer Haugsbakk's assertion about command aliasing behavior, demonstrating that core commands cannot be overridden by aliases - the original command takes precedence when it exists, with the alias only becoming active if the command is removed. The example shows `git whatchanged` continuing to work with its original behavior despite an alias definition, proving that the alias is silently ignored when the command exists.

The email also notes that since `git log --raw` is actually shorter to type than `git whatchanged`, users might be better served by simply adapting to the modern equivalent rather than maintaining aliases. This reinforces the thread's technical understanding while subtly suggesting that the alias workaround may not be the most productive path forward.

Key technical details for future reference: core command aliasing behavior (silent fallback to original command when both exist), the specific example command demonstrating this (`git -c alias.whatchanged='!echo bar' whatchanged`), and the continued functional equivalence between `git whatchanged` and `git log -p --raw --no-abbrev`. The email maintains the project's position that `git whatchanged` remains as legacy functionality with no current plans for removal.