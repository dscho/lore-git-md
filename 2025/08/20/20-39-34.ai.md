This is a code review comment on the "drop" subcommand implementation in Patrick Steinhardt's `git-history` RFC series. Ben Knoble raises two specific technical concerns about the commit collection logic in `builtin/history.c`:

1. **Variable naming clarity**: Questions whether "old commit" is the clearest name for the parameter in `collect_commits()`, suggesting it might need more descriptive naming.

2. **Array reversal logic**: Points out a potential bug in the commit ordering reversal code - the comment claims the first element (index 0) is preserved as equivalent to `argv[0]`, but the actual swapping loop appears to modify it. This could lead to incorrect commit ordering if the comment's assumptions about the array structure are accurate.

The review is focused and technical, examining implementation details rather than the high-level design. Knoble notes the rest of the code looks reasonable but qualifies this by stating limited familiarity with the sequencer APIs. This suggests the feedback is from a careful reader catching edge cases rather than a fundamental objection to the approach.

The exchange continues the thread's pattern of detailed technical review, with Knoble building on earlier discussions about the command's behavior while focusing here on specific correctness concerns in the implementation.