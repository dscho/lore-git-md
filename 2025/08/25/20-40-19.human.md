This is a technical discussion in the ongoing Rust infrastructure thread, specifically addressing the design of the `ivec` type for C/Rust interop. Ezekiel Newren responds to Ben Knoble's questions about why Rust's native `Vec` isn't suitable and why the implementation leans toward C.

Newren explains that Rust's `Vec` has incompatible memory layout between versions (showing structural differences between Rust 1.61 and 1.89) and lacks `#[repr(C)]` guarantees needed for stable FFI. He defends the custom `ivec` approach but remains open to alternatives. Regarding implementation language choice, Newren notes the initial design prioritized C compatibility to avoid an immediate Rust dependency requirement.

The exchange highlights key technical constraints in the Rust integration effort, particularly around memory management across language boundaries. While not resolving the thread's broader policy deadlock, this clarifies design decisions for one of the core interoperability components. The tone remains constructive, with both participants exploring technical merits rather than rehashing the larger debate.