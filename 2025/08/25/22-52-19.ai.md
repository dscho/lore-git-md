Code clarity improvement in progress meter signal handling.

This email from Junio Hamano acknowledges Johannes Sixt's rationale for using `int` rather than `bool` for the `update` variable in the progress meter code. The choice was made to maintain compatibility with older maintenance branches that might not have the project's current policy allowing `bool` usage. Hamano confirms this is the right approach for backporting considerations, noting he'll requeue the patch with this understanding.

The discussion represents the final polish on the long-running progress meter modernization effort, where the technical solution (moving from setitimer() to a signal-free approach) was already settled. This exchange specifically addresses the variable typing question that arose during code review, with both parties agreeing that while `bool` would be semantically clearer in new code, using `int` is the pragmatic choice for backportability.

Key technical points:
- Variable in question: `update` flag tracking progress state
- Original type: `sig_atomic_t` (unnecessarily, since not signal-accessed)
- New type: `int` (chosen over `bool` for backport compatibility)
- Affected branches: maint-2.50 and older, where `bool` usage policy differs
- File: progress.c
- Context: Part of signal-free progress meter implementation

The email confirms the thread is now effectively resolved, with all technical and stylistic considerations addressed. The only remaining action is for Hamano to requeue the patch with this understanding.