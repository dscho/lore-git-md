This is a brief response in the ongoing discussion about allowing `git commit` to handle untracked files directly. Michal Suchánek suggests an alternative approach rather than modifying Git's behavior - using a version control system like Jujutsu that doesn't use Git's index model. 

The email doesn't engage with the technical proposal of adding an `--include-untracked` flag that was previously discussed, but instead offers a philosophical counterpoint about Git's design. It implies that Git's index-based workflow is intentional and that users wanting different behavior might be better served by alternative tools rather than changing Git itself.

This perspective adds to the discussion by questioning whether the proposed change aligns with Git's design principles, though it doesn't address the specific implementation concerns raised earlier in the thread. The suggestion to use Jujutsu indicates some users may prefer systems with different workflow models entirely.