This is a user concern about grafts removal impacting shallow clone functionality. Askar Safin responds to Bagas Sanjaya's question about shallow clones using git-replace, clarifying they are not a developer but expressing strong practical concerns about maintaining `git clone --depth=1` functionality post-grafts removal.

Key technical observations:
- Safin confirms current shallow clones (`--depth=1`) show "grafted" in `git log` output (verified on Git 2.47.2)
- Expresses no knowledge about current or future implementation details
- Makes clear functional requirement: shallow clones must continue working after grafts removal

The email highlights a concrete user dependency on grafts that needs resolution before removal - shallow clone operations currently appear to use grafts internally (as evidenced by "grafted" markers in log output). This adds to the thread's core concern about undocumented/untested migration paths for grafts-dependent functionality.

The response maintains Safin's position from earlier in the thread: opposing grafts removal until critical functionality like shallow cloning is proven to work with the replacement mechanism. The specific shallow clone case now has additional empirical evidence (the "grafted" marker observation) supporting the need for implementation verification.