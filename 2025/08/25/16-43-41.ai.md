This is a refactoring patch in Patrick Steinhardt's `git-history` RFC series, specifically addressing the `add-patch` subsystem's dependencies. The patch follows up on an earlier header split that separated `add-patch` declarations from `add-interactive` functionality.

The key change removes `add-patch`'s dependency on initializing the full `add-i-state` by instead storing and initializing just the needed interactive configuration directly in `add_p_state`. This completes a two-step cleanup that Junio Hamano explicitly approves, noting the resulting code organization is much improved.

Technical details show substantial churn (68 lines modified) in `add-patch.c`, with the changes focused on initialization paths rather than core logic. The patch maintains all existing functionality while better isolating the patch-specific code from general interactive add machinery. Junio's positive reaction suggests this refactoring successfully addresses his earlier observation about incomplete header cleanup.

This change is part of the broader preparatory work for the `git-history` command, demonstrating how infrastructure improvements can have clean separation benefits beyond their immediate use case. The patch appears well-executed, with Junio's explicit approval indicating it meets core Git maintainer standards for such refactoring work.