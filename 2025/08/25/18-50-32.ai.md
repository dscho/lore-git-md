This is a code clarity improvement in the ongoing progress meter signal handling modernization effort. The discussion has moved from technical implementation to variable typing semantics, specifically around the `update` flag used in the progress display logic.

Junio Hamano responds to Carlo Marcelo Arenas Belón's suggestion to use `bool` for the `update` variable, which tracks whether progress should be displayed. While agreeing that any non-`sig_atomic_t` type would be appropriate (since the variable isn't signal-accessed), Hamano notes that the `bool` vs `int` discussion is somewhat tangential to the core issue of avoiding misleading type usage.

The exchange shows the project's attention to code readability and semantic intent, even for small implementation details. Hamano maintains that the move to `bool` in new code is a separate stylistic consideration from the immediate goal of fixing the misleading `sig_atomic_t` usage. This continues the thread's pattern of polishing implementation details after the core technical approach (signal-free progress meter) was settled.