This is a brief follow-up in the ongoing progress meter signaling modernization thread. Junio Hamano acknowledges Johannes Sixt's rationale for using `int` rather than `bool` in the progress meter implementation, as it allows for easier backporting to older maintenance branches that may not have adopted Git's `bool` type policy. 

The exchange shows the maintainer's attention to practical considerations when applying fixes across different Git versions. While the technical solution itself remains settled (the signal-free approach using `getnanotime()`), this note confirms the patch will be adjusted for broader compatibility. 

This is a minor but thoughtful polish to the already well-discussed progress meter improvements, demonstrating the project balances technical progress with real-world deployment concerns. The thread continues to show consensus around the core changes while working through these final implementation details.