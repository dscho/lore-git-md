This is a continuation of the discussion about deprecating `git whatchanged`. Junio C Hamano corrects a technical point in Kristoffer Haugsbakk's suggested migration path from `git whatchanged` to `git log --no-merges --raw`. The key clarification is that the behavioral difference isn't about timestamp handling (as previously mentioned in the thread) but rather about how single-parent empty commits are treated between the two commands.

The email references an earlier discussion (linked via lore.kernel.org) that presumably contains more details about these implementation differences. This suggests there may be subtle edge cases where the commands behave differently that need to be considered when evaluating removal. The thread continues to focus on understanding the technical migration path and identifying any non-obvious behavioral differences that could impact users.

This exchange highlights that while the basic functionality can be replicated with `git log`, there are implementation details that may need to be addressed before full removal, particularly around edge cases like empty commits. The discussion remains technical and focused on ensuring the migration path is fully understood.