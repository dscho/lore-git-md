This is a discussion about the potential deprecation of `git whatchanged`, continuing the thread collecting user experiences with the command. Kristoffer Haugsbakk responds to Monte Davidoff's report of still using `git whatchanged` by providing the equivalent `git log` command that replicates its functionality (`git log --no-merges --raw`), though notes there may be some timestamp-related differences (referencing a previous discussion).

The exchange highlights the key technical detail that `git whatchanged` is essentially replaced by `git log` with specific options, though subtle behavioral differences may exist. This follows the thread's pattern of gathering concrete use cases while also providing migration paths for users who still depend on the deprecated command. The response is brief and technical, focusing on the command-line equivalence rather than engaging with the broader deprecation policy discussion.