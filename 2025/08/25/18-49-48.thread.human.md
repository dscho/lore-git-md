This thread proposes adding `-u` as a shorthand for `--set-upstream` in `git pull`, matching the existing behavior in `git push`. Julien Jerphanion's initial patch makes a simple one-line change to implement this, along with test cases verifying the behavior matches between the short and long forms. 

D. Ben Knoble has chimed in to note that documentation updates would be needed if the change is accepted, though no specific doc changes have been proposed yet. The change appears uncontroversial so far, as it follows Git's existing conventions and provides consistency with `git push`. The thread is still in early stages, awaiting broader review and any potential feedback on the change's desirability before moving forward with documentation updates.