This is a refactoring patch in Patrick Steinhardt's `git-history` RFC series, specifically addressing the `add-patch` subsystem's dependencies. The patch follows up on an earlier header split between `add-interactive` and `add-patch` functionality, now removing the remaining initialization dependency between them.

The change stops `add-patch` from calling `init_add_i_state()` and instead initializes the interactive configuration directly within its own `struct add_p_state`. The diff shows significant code movement (68 lines modified with 36 additions and 32 deletions) as the initialization logic is relocated.

Junio Hamano approves of the change, noting that the two-step refactoring (this patch plus the previous header split) has improved the code organization to his satisfaction. The modification appears to be a clean architectural improvement that better isolates the `add-patch` subsystem while maintaining its functionality.

This is part of the larger preparatory work for the `git-history` command, showing attention to code structure before introducing new features. The patch seems well-executed and receives positive feedback from the Git maintainer, suggesting it's likely to be accepted.