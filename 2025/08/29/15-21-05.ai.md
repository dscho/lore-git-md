This is the third patch in a v2 series implementing a standardized deprecation process for `git whatchanged`. The patch focuses on documentation cleanup, removing an unnecessary comparison about command length from the deprecation warning in git-whatchanged.adoc.

The change simplifies the warning message by dropping the subjective "not even shorter to type" clause, leaving just the factual statement about differing defaults. The patch also fixes a grammatical issue by changing "default" to "defaults" to properly reflect that multiple default behaviors differ between the commands. The commit notes that `git log --raw --no-merges` is the closest equivalent to `git whatchanged`.

This is a straightforward documentation tweak that follows through on the series' goal of providing clear, factual guidance about the deprecation. The change appears uncontroversial and aligns with the maintainer-approved approach established earlier in the thread. The patch modifies only Documentation/git-whatchanged.adoc, making a minimal textual change to improve clarity while maintaining all the key deprecation information.

The patch builds on the series' established three-step deprecation pattern (notice/alternatives/contact) by refining the alternatives presentation. It maintains consistency with the broader effort to improve breaking change communication while removing what was likely seen as unnecessary editorializing about command length.