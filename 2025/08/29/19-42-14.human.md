Refactoring patch in the Rust xdiff integration series. This is patch 10/15 in Ezekiel Newren's series to prepare xdiff for Rust interoperability.

The patch simplifies memory management in xdiff's preparation code by consolidating cleanup logic. It moves the `xdl_free_ctx()` function definition before its first use in `xdl_prepare_ctx()`, then replaces the manual cleanup of individual fields with calls to this unified function. The change removes 36 lines of repetitive cleanup code while maintaining identical behavior.

This is a mechanical refactoring that improves code organization and reduces duplication, following the series' pattern of cleaning up xdiff internals before Rust integration. The change touches only xprepare.c, focusing on the memory management of xdfile_t structures. As with previous patches in the series, this appears to be a well-contained change with no expected functional impact.