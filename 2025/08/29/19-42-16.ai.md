This is patch 12/15 in the Rust xdiff integration series, continuing the refactoring work to prepare the xdiff subsystem for Rust interoperability. The patch removes the redundant `nrec` field from `xdfile_t` since the record count is already tracked by the `ivec_xrecord` type's `length` field introduced in previous patches.

The change touches 6 files with 37 insertions and 40 deletions, primarily replacing `xdf->nrec` comparisons with `(long)xdf->record.length` casts throughout xdiff's core algorithms (diff generation, merging, patience diff, etc.). The removal simplifies the data structure by eliminating duplicate state while maintaining identical behavior, as the `ivec` type now serves as the single source of truth for record counts.

This continues the series' pattern of incremental C-side cleanup before introducing Rust components, with each patch making the codebase more FFI-friendly by reducing redundant state and standardizing on interoperable types. The change is mechanical but thorough, updating all xdiff subsystems to use the new access pattern consistently. No functional changes are introduced - this is purely a refactoring step to reduce technical debt before the Rust integration.