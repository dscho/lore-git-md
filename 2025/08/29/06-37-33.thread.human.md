Here's the updated human-readable thread summary:

The thread began with Ron Yorston reporting that a recent Git change broke `git-restore-mtime` from the git-tools collection, which parses `git whatchanged` output to restore file modification times. The breakage appeared suddenly without following Git's standard deprecation process.

Kristoffer Haugsbakk has now responded, providing important context that while `git whatchanged` was never formally deprecated, its use has been discouraged since 2013 (commit 52f425e1a9a). He pointed to an existing issue (#82) and pull request (#83) in the git-tools repository that address the breakage, showing the project is already working on a solution.

The discussion suggests this likely won't be treated as a regression in Git itself, given the long-standing documentation about `git whatchanged` being a legacy command. The focus is shifting toward adaptation in git-tools rather than expecting Git to maintain backward compatibility for this output format. While the exact nature of the format change remains unspecified, the thread now has concrete references to both the problem and an in-progress solution.