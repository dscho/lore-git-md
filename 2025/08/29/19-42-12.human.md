This is a refactoring patch in the Rust integration series, cleaning up unused fields in xdiff's data structures before the Rust port.

Patch 8/15 removes three fields from xdiff subsystem's core data structures that were initialized but never actually used: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. The changes are straightforward deletions with no behavior impact, as confirmed by the test suite. The patch simplifies the code by removing dead fields and the associated allocation/initialization logic, making the structures leaner for the upcoming Rust port.

The changes touch xdiff's core preparation logic, removing hash-related fields that appear to be vestigial from an earlier implementation. The patch follows the series' incremental approach by doing cleanup before introducing Rust code, and maintains the careful attention to detail seen in previous patches - each removal is accompanied by the corresponding cleanup of related initialization and memory management code.

This continues the pattern of preparatory refactoring seen in earlier patches, making the codebase simpler and more focused before the Rust integration work begins in earnest. The changes are mechanical but meaningful, reducing the surface area that will need to be maintained across both C and Rust implementations.