This is a bug report about compatibility breakage with the `git whatchanged` command and related tools. Ron Yorston reports that the latest Git release broke `git-restore-mtime` from the git-tools collection, which depends on `git whatchanged`'s output format. The email expresses frustration that the change was made without following Git's usual deprecation process.

The report doesn't specify exactly what changed in Git's output format that broke the tool, but implies the breakage was sudden rather than following a deprecation warning period. The git-tools project mentioned is a third-party collection of Git utilities, with `git-restore-mtime` being a tool that restores original modification times to files based on Git history.

This appears to be a case where Git's internal changes have affected external tools that parse its output, a known pain point in maintaining compatibility. The tone suggests the change may have been unintentional rather than a deliberate deprecation, but without specifics about what exactly broke, it's hard to assess whether this was an unavoidable change or something that could have been handled more gracefully.

Key points for future discussion would be identifying exactly which Git commit caused the breakage and determining whether the output format change was intentional. If unintentional, this would be a regression worth fixing; if intentional, the discussion might focus on whether the change merits reverting or whether git-tools should adapt.