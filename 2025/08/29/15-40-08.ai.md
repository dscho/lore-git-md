This is a performance and safety patch for `git range-diff` that adds configurable memory limits for the cost matrix calculation, now in its final form pending maintainer approval.

The patch addresses memory exhaustion issues when comparing very large commit ranges (250,000+ commits) by introducing a `--max-memory` option with human-readable size specifications (defaulting to 4GB/2GB). The implementation has received Acked-by tags from key reviewers Johannes Schindelin and Elijah Newren, with all prior feedback incorporated into this v3 version.

Junio Hamano's response indicates the patch is ready for inclusion, though he notes the option placement in the help text could be slightly improved (between diff-merges and remerge-diff options). He suggests this could be addressed later if users find it problematic. The maintainer also speculates about potential future optimizations like matrix size reduction, but explicitly states these are out of scope for this already-solid patch.

The technical implementation remains unchanged from v3, featuring:
- Memory parsing via `parse_max_memory()`
- Pre-allocation validation in `get_correspondences()`
- Clear error messages showing both requested and allowed memory
- Platform-specific defaults (4GB/2GB)

This concludes the thread with the patch effectively ready for merging, pending only the final wording tweak requested by Elijah Newren in his v3 follow-up. The solution demonstrates Git's CLI-first approach while solving a real-world performance problem for large monorepos.