This is a discussion thread continuation about the `git stash --only-unstaged` feature proposal, focusing on documentation clarity and implementation details. Ben Knoble follows up on Phillip Wood's earlier response, zeroing in on the current documentation's implicit assumptions about `--keep-index` behavior.

The key points raised:
1. The existing documentation example for `--keep-index` doesn't mention potential conflicts that arise when editing working tree files during testing, which Knoble argues is a natural interpretation of the "edit/build/test" instruction shown. He suggests either updating the docs to clarify expectations or modifying the implementation to match the example's implied behavior.

2. Knoble reinforces Wood's earlier proposal to use a commit message trailer (`Stash-created-with: --keep-index`) to enable automatic unstaged-change-only behavior during pop operations, noting this approach appears compatible with stash import/export functionality.

3. The exchange highlights a disconnect between the original `--keep-index` design (which assumed working tree files wouldn't be modified during testing) and real-world workflows where such modifications occur. Knoble positions this as either a communication problem (fixable via documentation) or an implementation gap (requiring code changes to support the documented example).

The discussion remains constructive, with Knoble validating the technical direction while pushing for clearer user-facing behavior - either through documentation updates or deeper implementation changes. This moves the thread toward concrete next steps regarding either doc improvements or the trailer-based implementation path.