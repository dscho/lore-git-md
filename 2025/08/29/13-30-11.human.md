This is a discussion about improving the documentation and behavior of `git stash --keep-index` to better match user expectations. Ben Knoble raises concerns that the current documentation example implies a conflict-free workflow when editing files during testing, which doesn't match the actual behavior where conflicts often occur during `stash pop`. 

The email suggests two paths forward: either update the documentation to clarify the expected workflow (and warn about potential conflicts), or modify the implementation to better support the documented example's implied behavior. The latter approach could leverage Phillip Wood's earlier proposal to use commit message trailers to track stashes created with `--keep-index`, enabling smarter conflict resolution.

The discussion shows alignment on the technical approach (using trailers) but highlights a disconnect between the documented example and actual behavior. Knoble's email serves to clarify this documentation issue while supporting Wood's technical solution. The thread continues to refine both the user-facing documentation and the underlying implementation strategy for this stash workflow improvement.