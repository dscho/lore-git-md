Documentation patch in the final v2 series for better deprecation guidance of `git whatchanged`. This patch specifically adds concrete `git log --raw --no-merges` translation for `whatchanged` users in both the command output and documentation, addressing user confusion post-2.51.0 release. The implementation extends the `you_still_use_that()` helper to accept an optional hint parameter, maintaining consistency with the series' three-step deprecation pattern (notice/alternatives/contact).

Key technical elements:
- Modified `you_still_use_that()` signature in git-compat-util.h to accept hint strings
- Added specific command translation in builtin/log.c's error path
- Documented the equivalent in git-whatchanged.adoc with code block formatting
- Maintained backward compatibility by allowing NULL hints (used in pack-redundant.c)
- Incorporated Peff's technical note about empty commit behavior differences via expanded footnote

The patch follows the maintainer-approved structure from earlier discussion, with v2 refinements including whitespace fixes and clearer footnote references. This completes the implementation portion of the series' goal to provide actionable alternatives while preserving the deprecation warning system's consistency across commands.