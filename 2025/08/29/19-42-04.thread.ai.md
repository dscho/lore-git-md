Here is the updated dense AI thread summary incorporating the new CI artifact collection change:

This thread introduces Rust support to Git's xdiff subsystem through a 15-patch series by Ezekiel Newren (13 patches), Johannes Schindelin (1 patch), and brian m. carlson (1 patch). The series follows an incremental strategy: infrastructure and policy first, then build system integration, cleanup, and finally translation. Patch 1 establishes the foundational Rust adoption policy in Documentation/technical/rust-support.adoc (renamed from Rust-usage.md), which becomes required reading for platform support. The policy document makes a comprehensive case for Rust adoption citing security benefits (70% reduction in memory safety vulnerabilities) and outlines key principles: static library linking, avoidance of C-compatibility crates, version support tied to Debian stable releases (approximately 3-year window), and conservative dependency management (no committed Cargo.lock files). It explicitly rejects parallel C implementations and warns about platform-specific type pitfalls (c_long inconsistencies).

Patch 2 implements the build system integration, adding Rust compilation support through a new build_rust.sh script that handles debug/release builds and platform-specific library naming. It updates both Makefile and meson.build to integrate Rust compilation, linking the static libraries produced by Cargo. The patch introduces basic Rust type definitions in git-compat-util.h for C/Rust interop and sets up a Rust workspace with two initial crates: xdiff (the focus of this series) and interop (for future work). Key technical details include new files (build_rust.sh, rust/{Cargo.toml,interop/,xdiff/}), modified files (.gitignore, Makefile, git-compat-util.h, meson.build), and Rust types exposed to C (u8/u16/u32/u64, i8/i16/i32/i64, f32/f64, usize/isize). The build integration points (compile_rust target, RUST_BUILD_MODE, EXTLIBS) and platform handling (different library naming for Windows vs Unix) are carefully implemented to follow the policy from patch 1.

Patch 3 adds Rust toolchain setup to Git's GitHub Actions CI, with special handling for Windows, musl, and 32-bit Linux environments. It introduces two new scripts (install-rust-toolchain.sh and install-rustup.sh) that handle version-specific installations, with minimum version requirements: 1.61.0 baseline, 1.78.0 for Windows (due to bcrypt library resolution), and 1.72.0 for musl (for libc compatibility). The Windows builds require different target triples for make (x86_64-pc-windows-gnu) versus meson (x86_64-pc-windows-msvc) builds. The 32-bit Linux container case needs explicit target triple override (i686-unknown-linux-gnu) since rustup misdetects the host architecture. The implementation shows careful attention to platform-specific quirks and maintains the series' policy of conservative version requirements. It consolidates Rust toolchain definitions in main.yaml while allowing per-job overrides. Johannes Schindelin contributed to the initial library name selection logic which was reworked into this commit.

Patch 4 addresses Windows platform compatibility when using Meson, specifically fixing a linking issue where the Rust-built xdiff library requires the `userenv` system library (providing `GetUserProfileDirectoryW` for Rust's home directory detection). The changes: (1) add `-luserenv` to `EXTLIBS` in `config.mak.uname` for Makefile builds, (2) include `userenv` in `libgit_dependencies` for Meson builds, and (3) add a GitHub Actions step to ensure `libuserenv.a` is present in the Windows SDK environment (downloading it if missing). This patch acknowledges the new dependency isn't yet in the minimal Git for Windows SDK but follows established patterns for Makefile builds while extending support to Meson. The temporary CI workaround demonstrates practical handling of transitional dependency states while maintaining cross-platform consistency.

Patch 5 (new) modifies `.github/workflows/main.yml` to upload `rust/Cargo.lock` files as build artifacts in key CI jobs (Windows Make/Meson, Linux tests, static analysis) while maintaining the policy against committing them to version control. Each artifact is named contextually (e.g. `cargo-lock-windows-meson`) to enable dependency auditing across platforms. This provides traceability for the series' dependency resolution while respecting the conservative dependency management stance from patch 1. The implementation uses standard GitHub Actions artifact upload patterns and will help diagnose platform-specific dependency issues during the Rust integration work.

Subsequent patches (6-15) will implement the technical foundation: the interop vector type (ivec) in interop/ivec.{c,h,rs} enabling C/Rust memory sharing, xdiff preparation removing redundant xrecord_t/xdfile_t members and replacing chastore with ivec, and finally demonstrating the approach by translating xdl_trim_ends() to Rust while maintaining C callability. The series shows thorough test coverage for ivec and careful attention to build system impacts (including Windows/Meson), focusing on practical interoperability patterns before wider adoption. The incremental strategy targets xdiff as a non-critical path to minimize disruption while proving the Rustification approach. No reviews or objections are noted yet in this appears to be the first submission of the non-RFC version. The series represents a significant step in the ongoing Rustification effort, with the policy document and build system changes setting governance and infrastructure for all future Rust work.