Documentation patch in Julia Evans' ongoing series to improve `git-checkout.adoc` clarity. This v2 patch specifically addresses terminology by removing the potentially confusing "reset" language in favor of more descriptive explanations.

The changes focus on two sections describing branch creation/modification behavior with `-B`:
1. Replaces "otherwise, it is reset" with a concrete description: "otherwise `<new-branch>` is forced to point at the commit"
2. Simplifies the transactional behavior explanation from "the branch is not reset/created unless..." to "the branch will not be created or modified unless..."

The patch maintains technical accuracy while improving accessibility, continuing the series' trend of balancing precision with clearer language. This follows Junio Hamano's earlier confirmation of implementation details and aligns with the thread's established goal of making the documentation more approachable without sacrificing correctness. The changes are surgical, touching only the specific "reset" terminology while preserving the surrounding context and examples.

Key technical details remain intact:
- The transactional nature of the operation (all-or-nothing behavior)
- The branch modification semantics when using `-B`
- The equivalent `git branch -f` behavior
- The failure cases (like when branch is in use elsewhere)

This patch appears to be a straightforward improvement that addresses a known documentation pain point (confusion around "reset" terminology) while requiring no further implementation clarification from the thread's technical reviewers.