This is a follow-up discussion about test issues in the recently merged help output testing reorganization. Phillip Wood reports two problems he initially encountered with the automated testing approach in t1517-outside-repo.sh, but now clarifies they were false alarms.

First, he thought the test was failing due to examining all `git-*` commands in $PATH (including non-core scripts), but realizes this was caused by stray files in his repository root rather than a real issue with the test. Second, he initially observed that tests weren't stopping at first failure when using `-i` flag in loops, but now can't reproduce this behavior either.

D. Ben Knoble had previously responded that these issues seemed environment-specific, likely related to GIT_TEST_INSTALLED or distro setups. The exchange confirms these were indeed local configuration artifacts rather than problems with the test design itself.

The thread shows the new test infrastructure working as intended, with initial concerns resolved through debugging. This appears to be a case of post-merge verification catching what turned out to be non-issues, demonstrating the value of real-world testing after theoretical approval.