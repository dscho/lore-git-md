This is a final polish review for the `git repo info --format` implementation, part of a GSoC series that's already approved for merging. Eric Sunshine points out two style nits in the test file for the format flag implementation:

1. A minor whitespace issue after a redirection operator that's not worth a reroll
2. An inconsistency in using `${SQ}` for quoting test output (used in one place but not another)

The feedback is purely about test file polish - the core functionality is solid and already has comprehensive test coverage. The series implements a new `--format` flag for `git repo info` with keyvalue and nul output modes, migrating repository metadata queries from `git rev-parse` to a more structured command hierarchy.

This is the final patch in an 8-part series that's been through multiple rounds of review and is now in the 'next' branch, with mentors Karthik Nayak and Patrick Steinhardt having given their approval. The feedback here is non-blocking and focuses on minor consistency improvements to the test suite.