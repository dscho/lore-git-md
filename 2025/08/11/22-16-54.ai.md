This is the third patch in a 4-part series modernizing editor-related test infrastructure, focusing on test isolation and environment sanitation. The patch systematically cleans up t7005-editor.sh to prevent test pollution by using subshells, test_config, and single-command environment overrides. Key changes include:

1. Replaces global environment variable exports with scoped test-specific settings (TERM, EDITOR, VISUAL)
2. Wraps editor test loops in subshells to contain environment modifications
3. Converts git config operations to test_config for automatic cleanup
4. Consolidates duplicate test loops into a single "Using editors with overrides" test case
5. Maintains identical test coverage while improving isolation

The changes affect how editor preferences (vi, EDITOR, VISUAL, core.editor, GIT_EDITOR) are tested, ensuring each test case starts with a clean slate. The patch builds on previous work in the series by incorporating the PATH="$PWD:$PATH" pattern (replacing --exec-path) and using the test_commit_message helper. 

This represents a significant test modernization that should prevent subtle test pollution issues while making the test file more maintainable. The changes are focused on t7005-editor.sh with straightforward modifications that follow established test patterns. This patch appears technically sound and well-integrated with the series' goals of test reliability and modernization.