This is a follow-up discussion in the editor modernization series, specifically addressing the strvec API conversion in patch 4/4. Junio C Hamano weighs in on Eric Sunshine's earlier critique of the API design change.

The core issue revolves around whether `launch_editor()` should require callers to provide a `strvec` rather than accepting the more general `const char*` array. Junio agrees with Eric's assessment that the change unnecessarily constrains the API without clear benefit, noting that the `const` qualifier means the function couldn't even take advantage of `strvec`'s mutability features.

This email effectively closes the open question about the strvec API design from the thread summary, with the project maintainer siding against the proposed change. The discussion confirms the API should maintain its original signature using `const char* const* for maximum generality. This leaves the series with one less open question, though the commit message motivation point remains unaddressed.

The exchange demonstrates careful API design review in action, with clear technical reasoning about interface constraints. Junio's "Let's not do this" suggests the strvec conversion in this form won't proceed, though the author could potentially revise the approach while keeping the series' other improvements.