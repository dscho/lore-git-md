This is patch 8/10 in Patrick Steinhardt's ODB source system integration series, focusing on MIDX (multi-pack index) writing changes. The patch refactors MIDX writing functions to use `struct odb_source*` instead of separate repository/object_dir parameters, continuing the series' pattern of making MIDX handling source-aware.

Key technical changes include:
- Updates `write_midx_file()`, `write_midx_file_only()`, `expire_midx_packs()`, and `midx_repack()` to take an `odb_source` parameter
- Modifies internal helper functions like `write_midx_internal()` and `clear_midx_files()` to work with sources
- Adjusts callers in `builtin/multi-pack-index.c` and `builtin/repack.c` to pass the source
- Maintains all existing functionality while cleaning up the interface
- Touches midx-write.c, midx.h, and command implementations

The changeset is substantial (67 insertions/49 deletions) but follows the established pattern from previous reviews. Taylor Blau and Karthik Nayak have already approved the technical approach for this part of the series. The patch appears well-structured, with careful attention to maintaining behavior while updating the interface.

This completes the MIDX writing side of the ODB source integration, complementing the earlier loading changes in patch 7/10. The remaining patches (9-10/10) will implement wrapper functions and locality tracking to finish the series.