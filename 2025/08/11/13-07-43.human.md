This is a technical clarification in the ongoing discussion about the rebase documentation improvements.

Phillip Wood weighs in on the debate about whether to update the rebase example to use `git switch` instead of `git checkout`. He notes that Junio Hamano has previously expressed a preference for keeping `checkout` in this context, for two technical reasons: (1) the rebase target can be any commit-ish (not just a branch name), and `git switch` requires `--detach` for non-branch arguments, while `checkout` handles this case; and (2) rebase doesn't perform the same safety checks as `switch` when changing branches.

This is supporting the current documentation's use of `checkout` in the rebase example, providing technical justification for maintaining the status quo. The comment comes in response to Karthik Nayak's suggestion to modernize the example by using `git switch`, showing how even small documentation changes can surface deeper technical considerations about Git's command semantics.

The exchange demonstrates the careful attention to detail in Git's documentation process, where even seemingly simple wording choices are evaluated against technical correctness and historical context.