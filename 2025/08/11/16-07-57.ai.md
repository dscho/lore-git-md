Documentation fix follow-up. This is a reply in the ongoing thread about fixing nested bracket syntax in the git-clone man page's `--filter` option documentation.

Junio Hamano responds to Kristoffer Haugsbakk's suggested commit message revision, agreeing with restoring the descriptive subject line ("docs: remove stray bracket from git-clone synopsis") but requesting additional explanation in the message body. The key point is that while `[--filter=<filter>] [--also-filter-submodules]` might appear plausible, it's incorrect because `--also-filter-submodules` requires `--filter` to be specified - hence the proper nesting `[--filter=<filter> [--also-filter-submodules]]`.

The thread has already established technical consensus on the doc fix itself (correcting the syntax to show the dependency between these options). Current discussion focuses on commit message clarity for future readers. Junio suggests explicitly stating why the alternative formatting would be wrong, helping prevent similar confusion. This maintains the patch's straightforward nature while adding educational value to the commit message.

The change remains documentation-only, affecting only Documentation/git-clone.adoc, with no code impact. All participants agree on the technical content, with this being a polish pass on the explanatory text surrounding it. The fix targets historical documentation that was incorrect since its introduction in f05da2b4 and subsequent reformatting in 76880f05.