Here's a human-readable summary of the thread so far:

A bug report highlights an inconsistency in how `-c` configuration options are handled during submodule cloning. When using `git clone -c key=value --recurse-submodules`, the configuration isn't passed to the submodule clones, though the alternative form `git -c key=value clone --recurse-submodules` works correctly. This particularly affects authentication-related configurations like URL rewrites.

The issue appears to stem from argument parsing during submodule initialization, where the `-c` options aren't properly forwarded in the recursive case. While the underlying mechanism for passing configurations exists (as evidenced by the working form), the bug prevents it from being used when `-c` appears after the `clone` command. The reporter provided clear reproduction steps but hasn't yet proposed a fix. This seems like a straightforward bug in the submodule cloning logic's argument processing.