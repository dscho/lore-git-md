Here's the updated human-readable thread summary:

This thread examines whether `git clone -c key=value --recurse-submodules` failing to propagate configurations to submodules is a bug or expected behavior. The original report showed that while `git -c key=value clone` (with `-c` before the command) works correctly, placing `-c` after `clone` doesn't forward configurations to submodules - particularly problematic for authentication settings like URL rewrites.

D. Ben Knoble has clarified that this behavior may be intentional, reflecting Git's distinction between command-level configurations (passed via `git -c`) and repository-level configurations (set via `git clone -c`). While acknowledging the confusion this causes, especially for authentication workflows, they suggest the current implementation matches the documented behavior. The discussion has now shifted to whether the documentation should be enhanced to explicitly call out this limitation for URL rewrites, or if the behavior itself should be changed to better match user expectations.

The key question remains open: should Git maintain the current technically-correct-but-surprising behavior, or modify either the implementation or documentation to reduce confusion in submodule cloning scenarios? No patches have been proposed yet, but the thread has clarified the underlying technical distinction driving the observed behavior.