This is the final patch (10/10) in Patrick Steinhardt's ODB source system integration series, implementing wrapper functions to complete the MIDX path computation refactoring. The patch follows through on the structural changes made in previous patches by updating all MIDX path-related functions to use the object database source directly rather than passing separate hash algorithm and object directory parameters.

The changes touch midx-write.c, midx.c, and related headers, standardizing on the new `get_midx_filename()` and similar functions that take only a `struct odb_source*` parameter. This eliminates redundant parameter passing and ensures consistent path construction throughout the MIDX subsystem. The patch also received positive reviews from Taylor Blau and Karthik Nayak earlier in the thread.

With this change, the series achieves its goal of making MIDX handling fully source-aware while removing redundant storage and parameter passing. The wrapper implementation here represents the final piece needed before the ODB abstraction work can be merged. The changes maintain behavior while cleaning up the interface, completing a significant step toward pluggable object database backends.