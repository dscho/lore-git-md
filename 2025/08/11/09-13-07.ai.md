Documentation patch with post-merge feedback on already-merged changes. Karthik Nayak suggests updating the rebase example to use `git switch` instead of `git checkout` for consistency with Git's current recommendations.

This is a minor suggestion for patch 1 of Julia Evans' already-merged series reorganizing the `git-rebase` documentation. The series successfully restructured the man page to be more beginner-friendly by front-loading examples. Karthik points out that since `git switch` is no longer experimental, the example should be updated to use it rather than `git checkout` when referring to branch switching.

The suggestion is narrowly focused on one line of the documentation example, maintaining the series' goal of clarity while aligning with Git's current command recommendations. This would be a straightforward change to make if accepted, though it's worth noting that the original phrasing using `checkout` remains technically correct. The feedback continues the thread's pattern of post-merge refinement, with contributors offering small improvements to the already-landed documentation changes.

Key technical detail: The suggestion would change the example's command sequence from `git checkout topic && git rebase master` to `git switch topic && git rebase master`. This doesn't affect the rebase operation itself, just the branch-switching command used in the example. The thread context shows this patch was previously merged after several rounds of review, with Patrick Steinhardt having already suggested other wording improvements to this same section.