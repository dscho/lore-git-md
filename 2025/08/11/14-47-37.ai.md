Bugfix targeting submodule cloning behavior. This email reports that `git clone -c key=value --recurse-submodules` fails to propagate the `-c` configuration to submodule clones, though placing `-c` before `clone` (as `git -c key=value clone`) works correctly. The reporter specifically notes this affects authentication-related configurations like `url.new.insteadOf=old`.

The issue appears to be a straightforward bug in command-line argument handling during submodule initialization. The workaround suggests the underlying mechanism for passing configurations exists, but the argument parsing for the recursive case isn't properly forwarding the `-c` options. This would affect any use case requiring per-clone configuration, particularly authentication rewrites and other URL manipulations.

The report is concise but provides enough technical detail to reproduce the issue, including the exact command forms that do and don't work. No patch is included in this initial report - it reads as a bug report rather than a proposed fix. The behavior discrepancy between the two command forms suggests this could be a relatively simple fix in the submodule cloning logic's argument processing.