This is a feature patch in the `git repo info` series, adding the `layout.bare` field to report whether a repository is bare. The patch moves functionality from `git rev-parse --is-bare-repository` into the new command hierarchy.

Eric Sunshine suggests adding a test to verify the documented behavior that output lines are sorted lexicographically, regardless of input order. He provides two concrete suggestions for implementing this test - either as a separate test case or by modifying the existing test with reversed arguments and an explanatory comment.

The patch appears to be in good shape otherwise, as part of a series that has already received mentor approvals and been promoted to the 'next' branch. This is a minor polish suggestion for test coverage of an already documented behavior. The implementation itself seems straightforward, querying the same underlying repository state that `git rev-parse --is-bare-repository` currently accesses.