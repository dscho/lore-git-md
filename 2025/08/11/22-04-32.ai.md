Documentation patch fixing nested bracket syntax in `git-clone` man page. This is version 3 of a straightforward correction to the command synopsis, adjusting the formatting of `--filter` and `--also-filter-submodules` options to properly show their dependency relationship.

The patch now includes the restored descriptive subject line ("docs: remove stray bracket from git-clone synopsis") that was accidentally dropped in v2, along with an expanded commit message explaining the historical context. The correction shows that `--also-filter-submodules` must appear within `--filter`'s brackets since submodule filtering depends on regular filtering being enabled. The incorrect formatting was introduced in commit 76880f05 after the original correct grouping in f05da2b4.

All technical aspects were agreed upon in previous rounds, with Junio Hamano and Kristoffer Haugsbakk providing review feedback that has now been incorporated. The diff itself remains unchanged from v1 - a simple one-line fix to Documentation/git-clone.adoc removing a stray bracket. The discussion has shifted to optimizing the commit message for clarity, with this version adding explicit notes about the commit history and dependency relationship between the options.

This is a clean, non-controversial documentation fix that properly represents the CLI syntax without any functional impact. The patch is ready for inclusion with all reviewer feedback addressed and consensus established.