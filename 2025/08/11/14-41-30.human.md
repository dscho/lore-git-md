This is a discussion thread about the `git repo info` command implementation, specifically regarding the output sorting behavior of the `references.format` field. Phillip Wood raises a concern about the design decision to lexicographically sort output keys, which was introduced in patch 2/5 of the series.

Phillip argues that maintaining input order would be more useful for scripting purposes, as it would let scripts rely on positional output rather than requiring string comparisons to identify values. He notes that while JSON output (which is no longer part of this implementation) might have needed deduplication, the current key-value format could handle duplicates without sorting by using simpler approaches like a hash table or seen-keys array.

The email includes the relevant code showing the current implementation uses `QSORT()` to sort keys before output, along with test cases that verify the lexicographical sorting behavior. The thread context indicates this is part of a larger GSoC project to create a new `git repo` command hierarchy, with this particular patch already approved by mentors but receiving final polish feedback.

This appears to be a late-stage design discussion about a non-functional but potentially user-facing aspect of the new command's behavior. The sorting behavior was presumably chosen for consistency, but Phillip's feedback suggests it may create unnecessary work for script authors. The thread summary shows the series is otherwise merge-ready, so this discussion may lead to a last-minute behavior tweak before final inclusion.