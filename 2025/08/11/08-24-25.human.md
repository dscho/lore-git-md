This is a follow-up discussion in the ongoing series to remove global state from the commit-graph subsystem, specifically addressing integer type choices in patch 3/10. Patrick Steinhardt responds to Junio Hamano's feedback about using unsigned integers for commit-graph write options.

The key points in this response:
1. Patrick explains why using `uint32_t` could cause regressions by potentially rejecting previously valid large numbers that users might be relying on as "unlimited" values.
2. He defends the choice of `unsigned` over `uint32_t` as it maintains backward compatibility while still addressing the sign comparison warnings.
3. For the `size_multiple` option, Patrick acknowledges Junio's point that smaller types might suffice, but expresses concern about retroactively restricting valid ranges that users might depend on.
4. He shares that he considered but abandoned implementing stricter value restrictions in parse-options due to potential backward compatibility issues.

The discussion reveals a tension between precise type selection and maintaining backward compatibility, with Patrick favoring the more conservative approach that preserves existing behavior. This email doesn't introduce new code changes but provides important rationale for the design decisions in the series. The thread shows healthy technical debate while maintaining progress toward the series' goal of removing global state dependencies.