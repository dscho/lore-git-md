This is a follow-up in Patrick Steinhardt's ODB source system integration series, addressing Taylor Blau's suggestion for improving error handling in object directory lookups.

The email confirms Patrick will implement Taylor's suggestion to add an `odb_find_source_or_die()` wrapper function to avoid repeating the same error handling code at multiple call sites. This builds on the previous patch that made `odb_find_source()` return NULL on failure rather than dying immediately.

The change is part of the ongoing effort to make ODB (object database) behaviors more explicit and improve error handling. Taylor had previously approved most of the series but suggested this small API improvement to reduce code duplication. Patrick's quick acceptance indicates smooth progress on this technical refinement.

This appears to be a straightforward quality-of-life improvement to the API that doesn't affect the core functionality but makes the code more maintainable. The series continues to move forward with positive review feedback and minor iterative improvements.