This is a test modernization patch, part of a 4-patch series updating editor-related test infrastructure. It focuses on t7005-editor.sh, converting old-style tests to use modern Git test helpers and improving reliability.

The patch makes several systematic improvements to the test file: replacing manual error checking with `test_must_fail`, using `write_script` for test editor scripts, consolidating test setup into the setup block, and replacing manual commit message verification with the `test_commit_message` helper. It also removes unnecessary blank lines and subshells while maintaining all test functionality.

Key technical changes include:
- Moves vi detection and editor script creation into the setup test
- Replaces manual commit message verification (`git show | sed | test_cmp`) with `test_commit_message`
- Uses `write_script` instead of manual `cat >` and `chmod`
- Replaces manual error checking with `test_must_fail`
- Maintains all test cases (EDITOR/VISUAL precedence, space handling, etc.) while modernizing their implementation

The changes are focused on test reliability and maintainability without altering the actual test coverage. This appears to be a well-executed modernization that follows established patterns seen in other Git test conversions. The patch builds on the series' goals of improving test isolation and using modern practices, though it doesn't yet address the newer --exec-path concerns that appear in later patches of the series.