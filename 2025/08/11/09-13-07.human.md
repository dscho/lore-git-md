This is a post-merge review comment on a documentation patch in the `git-rebase` man page restructuring series. Karthik Nayak provides a small but important technical suggestion for the already-merged patch 1/5, which introduced introductory examples for `git-rebase`.

The key point is a recommendation to update the example text to use `git switch` instead of `git checkout`, since `git switch` is now the preferred command for branch operations. This aligns with Git's ongoing effort to steer users toward more specialized commands. The change would affect the shortcut syntax example that shows `git rebase master topic` as equivalent to `git checkout topic && git rebase master`.

While the patch itself has already been merged, this suggestion represents the kind of incremental improvement that often follows documentation updates. The comment is constructive and narrowly focused, addressing just this one technical detail without reopening broader discussions about the patch's structure or content. It demonstrates how the community continues to refine documentation even after initial acceptance.