This is a minor documentation fix for the IMAP sent-folder archiving feature in `git send-email`. Junio Hamano points out a formatting issue in Aditya Garg's patch where the `--use-imap-only` and `--no-use-imap-only` options should be written on separate lines to pass Git's documentation validation checks.

The comment comes as a follow-up to the v4 patch series that introduced pure-IMAP mode for `git send-email`, which allows archiving messages to an IMAP folder without SMTP transmission. This is a straightforward technical correction that doesn't affect functionality - just ensuring the documentation meets Git's standard formatting requirements.

The thread context shows this feature has already gone through several rounds of review and appears technically complete, with this being a final polish before potential merging. Junio's tone suggests this is a minor cleanup item rather than a substantive issue.