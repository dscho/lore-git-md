Here's a human-readable summary of the thread so far:

Rune Philosof has proposed adding commit comments as a new Git feature, allowing users to attach supplemental notes to existing commits. The idea is inspired by GitHub's comment system but would be built directly into Git's core, storing comments as special commit objects linked to their target commits. This would be visible in `git show` output and could potentially integrate with platform-specific comment systems.

The proposal is currently at the conceptual stage, with no implementation details or patches provided. Key considerations that would need to be addressed include the object storage format, performance implications (particularly around indexing), and how such a feature might interact with external platforms. The suggestion touches on fundamental aspects of Git's object model and would represent a significant new capability if implemented.