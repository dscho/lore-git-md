This is a discussion about refactoring in Patrick Steinhardt's ongoing packfile store abstraction series, specifically concerning patch 15 which renames `get_all_packs()` to `packfile_store_get_packs()`. Taylor Blau raises concerns about the tradeoffs of this change.

Taylor acknowledges the value in clarifying the function's scope but questions whether removing the convenience wrapper `get_all_packs()` is premature. He argues that callers shouldn't need to know about the packfile store implementation details yet, especially since we don't yet have non-packfile-based object stores. The current change forces all callers to explicitly reference `the_repository->objects->packfiles`, which makes the code more verbose.

Taylor suggests that if this direction is taken, it would be helpful to introduce a local variable to store the packfile store pointer to avoid overly long lines (as shown in the diff where the line exceeds 100 characters). This maintains the technical improvement while addressing readability concerns.

The discussion continues the thread's theme of balancing abstraction boundaries with practical API design. While the series aims to properly encapsulate packfile operations, this particular change may be pushing the abstraction too far ahead of actual need. The exchange highlights an important tension in large refactoring efforts - when to introduce architectural purity versus maintaining convenience for existing callers.