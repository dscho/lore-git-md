This is a documentation patch follow-up in the thread about documenting rev-list options for `git cherry-pick` and `git revert`. Andrew Jeffery (note corrected spelling) provides a minor correction to the attribution in Emily Shaffer's v2 patch, pointing out that his surname was misspelled as "Jeffrey" in the Reported-by line. 

The core technical content of the patch remains unchanged - it still properly documents the long-standing but previously undocumented rev-list options (`--no-walk`, `--left-right`, etc.) that both commands have accepted since 2010. The patch series has already been through one revision (v1 only covered `git cherry-pick`, v2 expanded to include `git revert`) and appears ready for inclusion.

This email doesn't introduce any new technical discussion - it's purely an author attribution correction that could be handled during patch application rather than requiring a respin. The thread shows consensus that the documentation improvement is worthwhile, following established patterns used by other Git commands that accept rev-list options. The historical context (commit f873a273d1) and cross-command consistency aspects remain relevant points for future reference in this thread.