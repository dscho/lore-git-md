Test modernization patch in the ongoing default branch name transition series. This is patch 5/6 in Phillip Wood's series to change Git's default initial branch name from "master" to "main" when WITH_BREAKING_CHANGES is enabled.

The patch modifies t1403-show-ref.sh to remove its dependency on GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME by hardcoding "main" in test assertions. The changes are straightforward mechanical updates to three test cases that previously used the environment variable to check ref existence and symref behavior. Since the test file already sets GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME="main" at startup, these changes maintain identical test coverage while preparing for the variable's eventual deprecation.

This follows the series' established pattern of updating tests before implementing the core behavior change. The modifications are minimal (3 line changes) and focused on test stability during the transition. The patch maintains all existing test assertions while making them more deterministic by removing the indirection through the environment variable. This continues the series' careful approach to ensure test reliability as the default branch naming convention changes.