This is a documentation patch that expands on the previous version to cover both `git cherry-pick` and `git revert` commands. The patch adds formal documentation for rev-list options (like `--no-walk` and `--left-right`) that have been accepted by these commands since 2010 but were only mentioned in examples rather than properly documented.

Emily Shaffer's v2 patch follows up on Andrew Jeffrey's report of this documentation gap. The change is straightforward - it adds the standard `include::rev-list-options.adoc` directive to both command's man pages, matching how other Git commands document these shared options. The patch maintains consistency with Git's documentation practices while fixing a long-standing oversight.

The diff shows this is a minimal change - just 3 lines added across two files. The commit message explains the historical context (commit f873a273d1 from 2010) and rationale for including both commands in this fix. This appears to be a well-justified documentation improvement that follows established patterns.