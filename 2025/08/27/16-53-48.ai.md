Feature proposal discussion regarding commit comments, with Junio C Hamano weighing in on the `git-notes` alternative.

Junio concisely reinforces Kristoffer Haugsbakk's earlier point about `git-notes` being the existing solution for attaching supplemental information to commits, adding the important detail that `git log --notes` can display these notes alongside commit messages. His brief reply suggests the core functionality being proposed may already be adequately covered by the notes system.

This moves the discussion toward a potential resolution, as the project maintainer is now signaling that extending `git-notes` would likely be preferred over creating a new comment mechanism. The technical direction appears to be solidifying around leveraging existing notes capabilities rather than introducing new object types or storage formats.

Key implications from this exchange:
- Maintainer validation that `git-notes` is the canonical way to attach supplemental commit information
- No immediate need for new object types when notes can reference commits
- Existing `--notes` flag already provides the requested display functionality
- Suggests the proposal may need to pivot toward notes integration if it proceeds

The thread now has clear maintainer guidance that any implementation should build upon rather than duplicate the notes infrastructure, though the original proposer hasn't yet responded to this direction.