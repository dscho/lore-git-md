This is Junio Hamano weighing in on the ongoing discussion about documenting rev-list options in `git cherry-pick` and `git revert` man pages. He raises an important concern about the current approach of blindly including all rev-list options, pointing out that some options (like `--graph` or `--bisect`) make no sense in the context of these sequencer operations.

Junio notes that the documentation system already has a mechanism (via `ifdef` directives) to selectively exclude irrelevant options when including `rev-list-options.adoc`. He suggests the author should review which options are actually meaningful for cherry-pick/revert and modify the inclusion accordingly, rather than exposing users to potentially confusing or nonsensical options.

This moves the discussion forward by providing concrete technical guidance - the documentation system's existing conditional inclusion system could be leveraged to create more targeted documentation. The ball is now back in the author's court to either implement this more selective approach or justify why full inclusion is preferable despite the potential for user confusion.