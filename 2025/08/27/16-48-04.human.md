Maintainer feedback on whitespace fix in the git whatchanged deprecation series.

Junio Hamano notes that patch 2/4 in Kristoffer Haugsbakk's series actually fixes an existing whitespace inconsistency rather than introducing one, as he initially thought when reviewing the diff. The patch changes indentation in `builtin/log.c` to properly align the `you_still_use_that()` call while adding a new parameter to provide the `git log` equivalent command.

This is a minor but appreciated cleanup to the code surrounding the main change, which adds specific guidance about how to replace the deprecated `git whatchanged` command. The maintainer's positive tone suggests the series remains on track pending this whitespace correction, consistent with his earlier praise for the documentation improvements in patches 3-4.

The exchange confirms the series is progressing well through review, with only this small formatting issue needing attention before potential inclusion.