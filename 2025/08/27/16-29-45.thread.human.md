This thread has finalized the implementation of Git's command deprecation framework with alias shadowing support. The v4 series, now technically complete with maintainer approval from Junio Hamano, introduces a system to mark commands as deprecated (like `whatchanged` and `pack-redundant`) while properly handling all alias recursion cases. Jeff King provided key architectural input by suggesting moving the recursion detection from `run_argv()` to `handle_alias()`, which has been validated to correctly handle both simple self-referential aliases and complex inter-command cycles.

The latest patch refines the implementation by relocating the command tracking logic as suggested, slightly modifying error output to show one additional alias expansion step before detecting cycles. While this changes the output format, it better prepares the system for deprecated command shadowing. The series includes comprehensive test coverage, optimized listing functionality, and improved user-facing error messages. With all technical aspects addressed and maintainer approval confirmed, this work is now ready for integration into Git's codebase.