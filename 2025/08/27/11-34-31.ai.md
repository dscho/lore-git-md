Documentation patch continuing the discussion about how to include rev-list options in `git cherry-pick` and `git revert` man pages. This is a follow-up to Phillip Wood's feedback on v2 patch, which proposed adding the full `rev-list-options.adoc` to both commands' documentation.

Emily Shaffer acknowledges Phillip's concern about the current approach adding too much potentially irrelevant content (like `--bisect` and `--cherry-mark` options that may not apply to these sequencer commands). She suggests the documentation might benefit from being broken into smaller, more targeted sections rather than including the full rev-list options. However, she notes this would be a more involved change and may be better handled by someone more familiar with the documentation system.

The technical context remains unchanged from the thread summary: these commands have accepted rev-list options since 2010 (commit f873a273d1) but were never properly documented. The discussion now centers on documentation architecture rather than functionality - specifically whether to use the existing `ifdef` system in `rev-list-options.adoc` to selectively include only relevant portions for cherry-pick/revert, or to include the full documentation as other commands do.

Key points for future reference:
- Current patch modifies `git-cherry-pick.adoc` and `git-revert.adoc`
- Phillip Wood (sequencer maintainer) raised concerns about documentation bloat
- Existing `rev-list-options.adoc` already has conditional sections via `ifdef`
- Emily open to more selective approach but notes it would be more work
- No behavior change being proposed - purely documentation discussion

The thread appears to be moving toward needing a decision about documentation strategy before proceeding, likely requiring either:
1. More selective inclusion via new `ifdef` sections in rev-list-options.adoc, or
2. Acceptance that some irrelevant options will be documented for completeness