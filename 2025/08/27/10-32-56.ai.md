This is a response in the ongoing discussion about commit comments as a Git feature. Kristoffer Haugsbakk points out that Git already has a similar mechanism in `git-notes`, which can attach additional information to commits, and notes that GitHub actually discontinued their commit comments feature in 2014 (contrary to the original proposal's implication that platform integration would be valuable).

The response suggests the proposed functionality may be redundant with existing capabilities, though it doesn't address whether `git-notes` meets all the original use cases (like direct integration with `git show` output). This introduces a key technical question for the thread: whether the proposal should build on `git-notes` infrastructure or create a new mechanism, and whether GitHub's abandonment of the feature suggests limited demand.

The exchange highlights two important considerations for the design discussion: 1) potential overlap with existing Git features, and 2) evidence about real-world need for such functionality based on platform adoption patterns. No implementation details are discussed yet, keeping the thread at the conceptual level.