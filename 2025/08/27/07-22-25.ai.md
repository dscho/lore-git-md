Documentation patch expanding the scope of the previous revision to cover both `git revert` and `git cherry-pick`. 

This v2 patch follows up on the initial documentation fix for `git cherry-pick` by applying the same treatment to `git revert`. The change remains straightforward - adding the `include::rev-list-options.adoc` directive to both command's documentation files. The commit message is updated to reflect the broader scope, noting that while the functionality was added to both commands in 2010's f873a273d1, only `cherry-pick` had any examples of the rev-list options in its docs.

The technical change remains minimal - two documentation files each get one new line to include the shared rev-list options documentation. The patch maintains the same approach as other Git commands that accept rev-list options, keeping documentation consistent across the codebase. No behavior changes are involved, just closing a documentation gap that's existed since the feature was introduced 15 years ago.

The range-diff shows the v2 changes are purely about expanding coverage to `git revert` while keeping the same solution approach. The original reporter (Andrew Jeffrey) remains credited, and the patch appears ready for inclusion with no obvious issues. This seems like a straightforward improvement that follows established patterns for documenting shared functionality.