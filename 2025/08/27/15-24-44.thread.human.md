This thread proposes changing Git's default initial branch name from "master" to "main" when users opt into breaking changes (via the WITH_BREAKING_CHANGES flag). The change follows industry practice and builds on a deprecation warning introduced in 2020.

The series takes a methodical approach, first updating test files before changing the implementation. Patch 1/6 updated t0018-advice.sh, and now patch 2/6 mechanically updates t4013-diff-various.sh and 97 associated test expectation files to use "main" instead of "master". These changes are straightforward string replacements that don't affect test logic, making them low-risk despite the large in file count.

The remaining patches will continue updating tests before the final patch implements the actual default branch switch. While the technical changes appear uncontroversial, the broader discussion may center on timing and migration impacts given this would be a breaking change for scripts and tools that assume "master" as the default. The series currently awaits review, with no maintainer feedback yet.