This thread documents an effort to properly include rev-list options in the `git cherry-pick` and `git revert` man pages - functionality that has existed since 2010 but was never fully documented. Emily Shaffer initially proposed simply including the standard rev-list options documentation (as other Git commands do), but Phillip Wood, as the sequencer expert, raised concerns that this would include many options irrelevant to these specific commands (like `--bisect`).

In her latest response, Emily acknowledges Phillip's point and suggests the documentation might need restructuring into more targeted sections, though she notes this would be a more involved change requiring deeper knowledge of Git's documentation system. The discussion has now shifted from whether to document these options (which all agree should happen) to how best to structure that documentation - whether to use the existing conditional inclusion system in `rev-list-options.adoc` to filter for relevant options, or to accept some documentation bloat for consistency with other commands.

The ball appears to be in Phillip's court to either propose specific `ifdef` sections that would properly filter the options, or to suggest another approach that would maintain documentation accuracy while keeping the changes manageable. Given Phillip's expertise with the sequencer commands, his input will likely determine how this documentation improvement proceeds.