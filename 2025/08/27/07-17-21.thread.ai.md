Thread identity: Documentation patch series for `git cherry-pick` and `git revert` man pages, addressing undocumented rev-list options that have been accepted since 2010 (commit f873a273d1).

Core problem: Both commands accept standard rev-list options (like `--no-walk`, `--left-right`) but these were only partially documented in examples without formal inclusion in the man pages. The v2 patch expands coverage from just `git cherry-pick` (v1) to include `git revert` as well. Phillip Wood raises concerns about including the full `rev-list-options.adoc` content, suggesting some sections (like `--bisect`, `--cherry-mark`) may not be relevant to these sequencer commands.

Current status: Patch v2 from Emily Shaffer adds the standard `include::` directive to both commands' documentation files, matching how other Git commands document these shared options. Phillip Wood proposes using `ifdef` mechanisms in `rev-list-options.adoc` for more selective inclusion. The functionality has existed since 2010 but was never properly documented. Andrew Jeffery previously noted a minor attribution fix needed in the Reported-by line.

Key technical details: Modifies both `Documentation/git-cherry-pick.txt` and `Documentation/git-revert.txt` to include `rev-list-options.adoc`. Phillip's feedback suggests revisiting the inclusion approach to better serve these specific commands, potentially using existing conditional documentation mechanisms. No behavior change - purely documentation improvement discussion.

Open questions: Whether to maintain full rev-list options documentation (current v2 approach) or implement a more selective inclusion mechanism as Phillip suggests. The latter would require changes to `rev-list-options.adoc`'s conditional system.

Participant positions: Andrew Jeffery originally reported the oversight and provided name spelling correction. Emily Shaffer authored both versions. Phillip Wood (sequencer expert) provides substantive feedback about documentation weight versus completeness, suggesting a middle ground approach. No other technical feedback yet on v2 beyond Phillip's comments.

Related work: Follows the pattern used by other commands that accept rev-list options. References historical commit f873a273d1 that originally added the functionality. Phillip's suggestion would maintain consistency while better tailoring documentation to these specific commands' needs.