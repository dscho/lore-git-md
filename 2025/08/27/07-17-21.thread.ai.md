Thread identity: Documentation patch series for `git cherry-pick` and `git revert` man pages, addressing undocumented rev-list options that have been accepted since 2010 (commit f873a273d1).

Core problem: Both commands accept standard rev-list options (like `--no-walk`, `--left-right`) but these were only partially documented in examples without formal inclusion in the man pages. The v2 patch expands coverage from just `git cherry-pick` (v1) to include `git revert` as well.

Current status: Patch v2 from Emily Shaffer adds the standard `include::` directive to both commands' documentation files, matching how other Git commands document these shared options. The functionality has existed since 2010 but was never properly documented. Andrew Jeffery (corrected spelling) noted a minor attribution fix needed in the Reported-by line (previously "Jeffrey").

Key technical details: Modifies both `Documentation/git-cherry-pick.txt` and `Documentation/git-revert.txt` to include `rev-list-options.adoc`. No behavior change - purely documentation improvement following established patterns. The range-diff shows the v2 change is minimal and focused, just expanding the same solution to the second command.

Participant positions: Andrew Jeffery originally reported the oversight for `git cherry-pick` and provided the name spelling correction. Emily Shaffer authored both versions, noting in v2 that while the rev-list options documentation is verbose, it's better than having undocumented features. The patch appears ready for inclusion with no outstanding technical issues.

Related work: Follows the pattern used by other commands that accept rev-list options. References historical commit f873a273d1 that originally added the functionality to both commands 15 years ago. Maintains consistency of documentation approach across the codebase. The attribution correction can be handled during patch application without requiring a v3.