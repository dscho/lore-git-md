This is a documentation patch discussion about how to properly include rev-list options in `git cherry-pick` and `git revert` man pages. Junio C Hamano weighs in on Emily Shaffer's v2 patch that added standard `include::rev-list-options.adoc` directives.

Junio raises concerns about including potentially irrelevant rev-list options in these commands' documentation, noting that the current documentation system already has an `ifdef` mechanism to selectively exclude certain options (e.g., `--bisect`). He questions whether all rev-list options make sense for cherry-pick/revert contexts, using `--graph` as an example of an option that would be nonsensical here.

The email suggests the current approach of blindly including all rev-list options could actually harm by exposing users to irrelevant options they wouldn't otherwise consider. Junio implies the documentation should be more selective, building on the existing `ifdef` system that already handles similar cases for other commands.

This moves the discussion forward by:
1. Confirming the technical capability exists for selective inclusion
2. Providing concrete examples of options that don't belong (`--graph`, `--bisect`)
3. Shifting focus from whether to include rev-list options to which specific options should be included

The thread now awaits either:
- A revised patch implementing selective inclusion
- Further discussion about which options are actually relevant to cherry-pick/revert
- Potential documentation system changes to better support this use case