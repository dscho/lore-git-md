This is a bugfix patch addressing a segfault in `git archive --remote=""`. The issue occurs when an empty string is passed to the `--remote` option, which currently triggers a segmentation fault rather than providing a proper error message.

The patch adds a simple check in `builtin/archive.c` to detect when `remote` is non-NULL but points to an empty string (the case when `--remote=""` is used). In such cases, it now calls `usage()` with an appropriate error message instead of proceeding to operations that would cause a crash.

The change is minimal (4 lines added) and focused, directly addressing the immediate issue. The commit message is clear about the problem being fixed, though it doesn't provide background on how the segfault occurs or whether there are other edge cases in remote archive handling that might need similar protection. The fix appears correct for the specific case it targets, but reviewers might want to consider whether there are other related input validation gaps in the archive command's remote handling.