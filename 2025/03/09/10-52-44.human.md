This is a build system refinement for the ongoing `WITH_BREAKING_CHANGES` deprecation work. Phillip Wood follows up on his earlier Meson build system patch, suggesting a more idiomatic way to handle the `WITH_BREAKING_CHANGES` compiler definition.

The change moves from using `add_project_arguments()` to appending the definition to `libgit_c_args`, which better matches Git's existing Meson build patterns. While functionally equivalent, this makes the build configuration more consistent with how other compiler flags are handled in the project.

This is a minor cleanup in what appears to be the final stages of the deprecation work. The thread context shows that all major technical aspects of removing deprecated features under `WITH_BREAKING_CHANGES` have already been addressed across both Makefile and Meson build systems. Phillip's suggestion here is a polish rather than a functional fix, showing attention to detail in making the Meson build match Git's established patterns.