Performance optimization and code simplification in fetch ref-prefix handling, concluding a series addressing protocol v2 fetch behavior.

This final patch in Jeff King's series eliminates the explicit `must_list_refs` flag in favor of checking the ref-prefix list directly, simplifying the logic for when to skip the `ls-refs` server round-trip during fetches. The change builds on earlier architectural improvements that ensured an empty prefix list reliably indicates no refs are needed. The optimization now naturally handles edge cases like negative refspecs with exact OID fetches (`git fetch $remote ^foo $oid`) without requiring special-case logic.

The patch removes 20 lines while adding 7, consolidating the decision-making about ref listing into a single check of the prefix list count. While the negative-refspec case fixed here is esoteric (acknowledged as not worth a dedicated test), the main value comes from cleaner code structure where the prefix list becomes the sole source of truth for this optimization. This follows through on the series' goal of making the protocol v2 fetch behavior more consistent and maintainable.

The change preserves all existing optimizations from e70a3030e7 (skipping `ls-refs` for exact OID fetches) while removing the intermediate flag that had become redundant after the series' earlier refactoring. The patch concludes the technical progression from initial bugfix through architectural improvements to this final simplification, maintaining comprehensive test coverage established in previous patches of the series.