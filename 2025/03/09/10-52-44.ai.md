This is a build system refinement for the ongoing `WITH_BREAKING_CHANGES` deprecation work. Phillip Wood follows up on his earlier meson build system patch with a minor correction to how the breaking changes flag is passed to the compiler.

The change moves the `WITH_BREAKING_CHANGES` definition from a global project argument to being added specifically to the `libgit_c_args` variable. This better matches Git's existing meson build patterns where compiler flags are typically set at the library dependency level rather than globally. The adjustment doesn't change functionality but improves consistency with the rest of the build system.

This comes as a polishing touch after the main technical work on deprecating `git pack-redundant` and related documentation under `WITH_BREAKING_CHANGES` was completed. The thread has already addressed all critical implementation points across both Makefile and Meson build systems, with only one non-blocking documentation issue remaining.

Key technical details for future summarization:
- Build flag moved from `add_project_arguments()` to `libgit_c_args`
- Affects meson.build configuration for `WITH_BREAKING_CHANGES`
- Follows existing pattern where libgit sets compiler flags for dependent targets
- No behavior change - just internal build system consistency improvement
- Part of final cleanup after major deprecation work completed

The email shows attention to detail in ensuring the meson build follows Git's established patterns, even for what appears to be a minor implementation detail. This comes from Phillip Wood who has been a primary contributor to this deprecation effort across both build systems.