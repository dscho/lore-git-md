This is a discussion about deprecating the `core.commentString=auto` feature, continuing the thread's progression toward concrete implementation plans. Junio Hamano responds to Phillip Wood's specific deprecation proposal, agreeing with most elements while questioning one aspect.

The email focuses on implementation mechanics for the deprecation path. Phillip's proposal includes several key components: runtime advice messages (configurable via `advice.autoCommentStr`), documentation in BreakingChanges.adoc, conditional compilation with `WITH_BREAKING_CHANGES`, and potential error behavior when breaking changes are enabled. Junio agrees these form a good transition plan but expresses reservations about making "auto" an error when breaking changes are enabled, arguing that since commentStr accepts arbitrary strings, "auto" could be treated as just another string value.

Technical details to note for future reference include the specific files involved (`builtin/commit.c`, BreakingChanges.adoc), the proposed configuration key (`advice.autoCommentStr`), and the compilation flag (`WITH_BREAKING_CHANGES`). The discussion has moved past whether to deprecate and is now focused on exactly how to implement the deprecation, with this email narrowing the open questions to whether to treat "auto" as an error versus a literal string when breaking changes are enabled.