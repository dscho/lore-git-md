This is a discussion about advice message suppression in Git's clone operation, specifically regarding the default branch name advice.

Phillip Wood responds to Justin Tobler's patch series addressing unexpected advice messages during clone operations. The exchange highlights two distinct but related problems: the immediate regression where advice appears during bundle clones despite suppression flags, and the broader architectural issue where direct `advise()` calls bypass Git's advice suppression mechanisms.

Key technical points in this email:
- Phillip agrees with Justin's assessment that the default branch name advice should never appear in this specific clone scenario, regardless of configuration
- They confirm this matches the original pre-regression behavior
- Phillip supports Justin's plan to add another patch making default branch name advice respect `--no-advice` globally
- The discussion reveals an inconsistency in Git's advice system where some messages ignore suppression flags due to using `advise()` instead of `advise_if_enabled()`

The email shows consensus forming around both fixing the immediate regression and addressing the underlying architectural issue. Phillip's response indicates approval of Justin's approach while highlighting the broader implications for Git's advice system consistency. This moves the thread forward by validating the two-pronged solution approach (immediate fix + architectural improvement) that Justin has proposed.