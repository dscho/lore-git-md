This is a bugfix patch addressing compiler warnings in the bulk-checkin code.

Tuomas Ahola fixes three `-Wsign-compare` warnings in bulk-checkin.c, all related to type mismatches in comparisons. Two warnings stem from loop iterators where an `int` was being compared with a `uint32_t`, which the patch fixes by standardizing on `uint32_t`. The third warning involves a `size_t` comparison that's resolved with an explicit cast.

The patch also removes the `DISABLE_SIGN_COMPARE_WARNINGS` macro that was previously suppressing these warnings for this file, indicating the issues are now properly resolved. The changes are straightforward and mechanical fixes to type safety, with no behavioral impact on Git's functionality.

The diff shows a net reduction of 7 lines (mostly from removing the now-unnecessary `DISABLE_SIGN_COMPARE_WARNINGS` and some variable declarations) while adding 3 lines of proper type declarations. This appears to be a clean, focused fix for compiler hygiene.