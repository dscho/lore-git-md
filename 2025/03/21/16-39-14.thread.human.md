This thread addressed a subtle inconsistency in `git blame`'s porcelain output when using `--ignore-rev` or `--ignore-revs-file`. The original issue was that porcelain mode wasn't properly showing the documented '?' (ignored) and '*' (unblamable) markers for lines affected by ignored revisions.

After initial discussion where Junio Hamano firmly established that the SHA-1 line format must remain unchanged for backward compatibility, Karthik Nayak (with guidance from Patrick Steinhardt) developed a solution using new metadata lines rather than modifying existing output formats. The key insight was to leverage the extensibility of porcelain format by adding dedicated "unblamable" and "ignored" metadata lines, with Toon Claes providing a concrete example demonstrating why this status information must be per-line rather than per-commit.

The implementation went through several iterations, with Eric Sunshine helping polish the test output for better readability. The final design cleanly separates `--porcelain` (commit-deduplicated) from `--line-porcelain` (with per-line details) behaviors while maintaining strict backward compatibility. All tests were updated in `t/t8013-blame-ignore-revs.sh` with improved output checking.

In the latest exchange, Karthik incorporated Patrick's final review feedback by renaming a key function to better reflect its porcelain-specific purpose (`emit_porcelain_per_line_details`) and optimizing some output calls from `printf()` to `puts()`. These were the last remaining polish items on what has become a thoroughly reviewed and tested solution. The thread has reached clear consensus, with Junio's earlier design constraints fully respected and all edge cases properly handled through comprehensive test coverage. The patch appears merge-ready with no outstanding technical questions.