**Thread Summary:**

This thread addresses a bug in `git blame`'s porcelain output where it wasn't properly showing the '?' and '*' markers for ignored and unblamable lines, despite these being documented as supported with the `--ignore-rev` and `--ignore-revs-file` options. The patch fixes this by modifying `builtin/blame.c` to emit these markers in porcelain mode when the relevant config options (`blame.markIgnoredLines` and `blame.markUnblamableLines`) are enabled. The changes specifically target the `emit_porcelain` function to ensure markers appear consistently for both initial and continuation lines.

The fix includes comprehensive test coverage in `t/t8013-blame-ignore-revs.sh`, with new test cases verifying the porcelain output for both unblamable ('*') and ignored ('?') line markers. The patch is well-contained and clearly addresses a documented-but-unimplemented feature, making it a straightforward and justified bugfix. The addition of thorough tests ensures this behavior won't regress in future versions.