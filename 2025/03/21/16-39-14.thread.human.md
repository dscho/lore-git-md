This thread has been working through a bugfix for `git blame`'s porcelain output, specifically addressing how to properly indicate ignored and unblamable lines when using `--ignore-rev` or `--ignore-revs-file`. The discussion began with a proposal to modify SHA-1 line prefixes, but Junio Hamano rejected this approach to maintain backward compatibility. 

The current solution, proposed by Patrick Steinhardt and implemented by Karthik Nayak, introduces new metadata lines to indicate ignored ('?') and unblamable ('*') status while preserving the existing SHA-1 line format. The implementation modifies `emit_one_suspect_detail()` in `builtin/blame.c` to include these new metadata lines.

In the latest exchange, Karthik Nayak and Toon Claes are discussing an edge case about whether these metadata lines need to be emitted for every line when multiple lines blame to the same commit. Karthik questions whether the ignored/unblamable status can actually differ per line when the commit is the same, suggesting these attributes might be commit-level rather than line-level. This is an important final refinement before the solution can be completed.

The thread has reached consensus on the overall approach and is now working through these last implementation details. Once resolved, Karthik will finalize the patch with the metadata line solution, update the tests in `t/t8013-blame-ignore-revs.sh`, and document the new metadata lines in blame-format.txt. All major technical objections have been addressed, and the fix is nearing completion.