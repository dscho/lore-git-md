This thread has reached a successful conclusion for the `git blame` porcelain output fix, which now properly marks lines that are ignored or unblamable in the output. The solution maintains backward compatibility while adding new metadata lines in the porcelain format, with all edge cases and formatting issues addressed through four iterations of the patch series.

Karthik Nayak led the implementation, with key design input from Patrick Steinhardt on using metadata lines and Toon Claes clarifying the per-line requirements. Junio Hamano established the constraints early on, insisting the changes be restricted to `--line-porcelain` output to preserve the stable SHA-1 line format. The series went through rigorous review, with Eric Sunshine improving test readability and Phillip Wood catching a subtle newline duplication bug in the output formatting.

In the final review comment, Patrick Steinhardt suggested a minor optimization to combine two `sed` operations in the test script, which verifies both `--porcelain` and `--line-porcelain` output modes. This is purely a test efficiency improvement and doesn't affect the already-approved functionality. The patch series is now complete with all technical and review feedback addressed, robust test coverage using `test_cmp`, and proper documentation updates. It's ready for merging into the next branch.