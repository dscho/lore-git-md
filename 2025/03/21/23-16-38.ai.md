Bugfix addressing unexpected default branch advice during clone operations, specifically when cloning from bundles. This is part 2/3 in a series fixing a regression where the "hint: Using 'master' as the name for the initial branch" message appears despite suppression flags like GIT_ADVICE=0 or --quiet.

The patch focuses on the clone-from-bundle case where the advice was appearing due to timing changes in default branch name computation. The regression was introduced by commit 199f44cb2ead which modified clone's refdb initialization sequence. The fix modifies `guess_remote_head()` calls in clone.c to use the new `REMOTE_GUESS_HEAD_QUIET` flag, suppressing the advice message.

Key technical details:
- Adds test coverage in t5607-clone-bundle.sh verifying advice suppression
- Uses the flag-based approach established in the series (converting from boolean to bitmask flags)
- Maintains consistency with the broader architectural changes to advice suppression
- Specifically handles the bundle clone case while the series addresses other scenarios

The change is straightforward and well-justified, with clear commit message explaining the regression and solution. The test case provides concrete verification of the fixed behavior. This patch builds on the architectural improvements discussed in the thread while providing an immediate fix for the bundle clone case.