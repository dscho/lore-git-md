Here is the updated dense, loss-free AI mode summary incorporating the new email:

Bugfix for `git blame` porcelain output regarding ignored/unblamable lines, addressing an inconsistency where porcelain mode wasn't showing the documented '?' (ignored) and '*' (unblamable) markers with `--ignore-rev`/`--ignore-revs-file`. The thread has progressed through:

1. Initial proposal modifying SHA-1 line prefixes (rejected by Junio Hamano for backward compatibility)
2. Karthik Nayak's reconsideration accepting interface constraints
3. Patrick Steinhardt's accepted approach using new metadata lines
4. Toon Claes' refinement about metadata emission frequency

Current technical state:
- Core solution uses new "unblamable" and "ignored" metadata lines
- Implementation in `builtin/blame.c` modifies `emit_one_suspect_detail()`
- Preserves SHA-1 line format and porcelain extensibility rules
- Now considering per-line vs per-commit metadata emission in `--porcelain` mode

Key technical details:
- Distinction between `--porcelain` (commit-deduplicated) and `--line-porcelain` modes
- `emit_one_suspect_detail()` output logic
- Per-line ignored/unblamable status may differ even for same commit
- Must maintain consistency with existing porcelain deduplication rules
- Strict SHA-1 line format requirements for backward compatibility

Open questions:
- Final metadata emission strategy for `--porcelain` mode
- Potential test coverage additions in `t/t8013-blame-ignore-revs.sh`
- Documentation updates for new metadata lines

Participant positions:
- Junio Hamano established interface constraints (SHA-1 line format inviolable)
- Karthik Nayak initially proposed SHA-1 prefix change, later accepted constraints
- Patrick Steinhardt designed compliant metadata line solution
- Toon Claes identified edge case needing resolution
- Awaiting final implementation adjustment from Patrick

Related context:
- Builds on existing `blame.markIgnoredLines`/`blame.markUnblamableLines` config
- Follows porcelain format extensibility rules from Documentation/technical/blame-format.txt

The thread has reached consensus on the metadata line solution after initial disagreement about interface changes. The remaining discussion focuses on optimizing metadata emission patterns while maintaining backward compatibility with existing porcelain parsers.