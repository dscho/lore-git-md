Here is the updated dense, loss-free AI mode summary incorporating the test improvement suggestions:

Bugfix for `git blame` porcelain output regarding ignored/unblamable lines, addressing an inconsistency where porcelain mode wasn't showing the documented '?' (ignored) and '*' (unblamable) markers with `--ignore-rev`/`--ignore-revs-file`. The thread has progressed through:

1. Initial proposal modifying SHA-1 line prefixes (rejected by Junio Hamano for backward compatibility)
2. Karthik Nayak's reconsideration accepting interface constraints
3. Patrick Steinhardt's accepted approach using new metadata lines
4. Toon Claes' refinement about metadata emission frequency
5. Karthik Nayak's implementation of metadata line solution
6. Discussion about per-line versus per-commit metadata emission
7. Toon Claes' concrete example demonstrating per-line status requirements
8. Junio Hamano's final approval of design restricting markers to `--line-porcelain`
9. Karthik Nayak's v2 patch implementing the solution with new `emit_per_line_details()`
10. Documentation updates in `blame-options.adoc` and `git-blame.adoc`
11. Extended test coverage in `t/t8013-blame-ignore-revs.sh`
12. Eric Sunshine's test output readability improvements (variable interpolation in titles)

Current technical state:
- Core solution implemented in `builtin/blame.c` via `emit_one_suspect_detail()` and new `emit_per_line_details()`
- New "unblamable" and "ignored" metadata lines added
- Preserves SHA-1 line format and porcelain extensibility rules
- `blame_entry` passed through call chain for per-line status
- Tests updated in `t/t8013-blame-ignore-revs.sh` with improved output readability
- Implementation finalized to restrict status markers to `--line-porcelain` mode
- Documentation updated to clarify line-specific metadata handling
- Design confirmed to properly accommodate future per-line metadata additions

Key technical details:
- Strict maintenance of SHA-1 line format for backward compatibility
- Porcelain extensibility via new metadata rather than format changes
- Clear distinction between:
  - `--porcelain` (commit-deduplicated, no status markers)
  - `--line-porcelain` (per-line details including status markers)
- New `emit_per_line_details()` function handles per-line metadata emission
- Status must be per-line since it depends on original blame history
- Current "ignored" metadata handling confirmed as correct pattern
- Test output improved to clearly show which porcelain mode is being tested
- Design accommodates future per-line metadata additions
- Final test coverage verification including Toon's example case
- Documentation updates completed for new metadata lines in blame-format.txt
- Implementation of `--line-porcelain` restriction now finalized

Participant positions:
- Junio Hamano established interface constraints (SHA-1 line format inviolable) and approved final design
- Karthik Nayak implemented the solution (v2 patch) and favors `--line-porcelain` restriction
- Patrick Steinhardt designed the metadata line approach
- Toon Claes provided critical example showing per-line status requirements
- Eric Sunshine suggested final test output improvements
- Consensus reached and confirmed to restrict markers to `--line-porcelain`

Related context:
- Builds on existing `blame.markIgnoredLines`/`blame.markUnblamableLines` config
- Follows porcelain format extensibility rules from Documentation/technical/blame-format.txt
- Maintains compatibility with existing porcelain output parsers
- Connects to broader blame porcelain output consistency efforts
- Design confirmed to properly handle future per-line metadata additions

The thread has reached final implementation with Karthik Nayak's v2 patch, now incorporating Eric Sunshine's suggested test output improvements. These final polish items enhance test readability while maintaining all existing functionality. Junio Hamano's earlier response validated that the implementation correctly handles the per-line nature of this information while maintaining porcelain format rules. The solution preserves backward compatibility while providing extensibility for future per-line metadata, with all technical concerns addressed and maintainer approval secured. The patch is now in its merge-ready form with comprehensive test coverage and documentation updates.