Performance optimization patch for bundle-uri reference handling, part 1 of a 3-part series. This initial patch expands the reference copying during bundle downloads from just `refs/heads/` to all references under `refs/`, which significantly improves subsequent fetch negotiation efficiency by including more objects in the negotiation phase.

The change is minimal - just removing the `/heads/` restriction in the `skip_prefix` check in `bundle-uri.c`. The commit message explains this helps fetch negotiation by including references beyond just branch heads, though it doesn't provide specific performance metrics (those appear in the thread context). The patch maintains the existing `refs/` → `refs/bundle/` mapping hierarchy that was established in v2 to address naming consistency concerns.

Key technical details for future reference:
- Core change is in `bundle-uri.c` line 403
- Still uses the `refs/bundle/` namespace established in v2
- Preserves the reference hierarchy mapping (just removes the heads restriction)
- Part of broader performance optimization for bundle-uri fetches

This appears to be the foundational change that enables the performance improvements discussed in the thread, with subsequent patches likely adding test coverage and handling edge cases. The implementation is straightforward but impactful, removing an artificial limitation in the reference copying logic.