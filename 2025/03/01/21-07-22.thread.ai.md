Here's the dense AI thread summary incorporating this new email:

New feature introducing `--must-filter` option for `git fetch` and `git clone` to enforce server-side filtering support. When set, Git will error out if the server doesn't support filtering rather than silently continuing without filters. Implementation adds:

- New boolean `--must-filter` flag in `builtin/clone.c` and `builtin/fetch.c`
- `must_filter` field in `struct fetch_pack_args` and `struct git_transport_options`
- Modified `fetch-pack.c` behavior to die() instead of warn() when filtering is unsupported with `must_filter` set
- New test cases in t0410-partial-clone.sh (currently marked expect_failure)

Motivated by prior discussion (Peff reference in commit message). Targets partial clone scenarios where filtering is essential. Technical details to track:

- New option/config: `--must-filter`
- Commands affected: `clone`, `fetch`
- Modified structs: `fetch_pack_args`, `git_transport_options`
- Behavior change: warning→error when server lacks filter support
- Test location: t0410-partial-clone.sh (expect_failure indicates WIP test setup)

The patch appears complete and focused, addressing a clear use case for ensuring filtering actually occurs rather than silently falling back. Test status suggests some finalization work remains on test infrastructure.