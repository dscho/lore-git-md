This is part 2/3 of a performance optimization series for bundle-uri reference negotiation during fetches. The patch updates test cases to reflect the new reference hierarchy where bundle references are now stored under `refs/bundles/heads/` rather than directly in `refs/bundles/`.

The changes are mechanical but extensive, updating 172 lines across the test file to use the new reference path. This follows from the series' earlier decision to preserve reference hierarchy (placing heads under `refs/bundles/heads/`) rather than mixing all references directly in `refs/bundles/`. The test updates verify that the new reference location works correctly across various bundle scenarios including:
- Basic bundle clones
- Failed bundle cases (bad headers/objects)
- HTTP-based bundle operations
- Bundle negotiation behaviors
- Creation token heuristics

The patch appears straightforward and well-scoped, focused solely on test updates to match the new reference layout established in earlier patches. It maintains all existing test coverage while adapting to the new hierarchy structure.