This is the first patch in a 7-part series introducing customizable format specifiers for `git blame` output. The patch adds basic infrastructure for format strings by implementing a new `--format`/`-F` option in `builtin/blame.c`, storing the format string in a new `format` static variable. The implementation is minimal at this stage - just adding the option parsing without yet implementing the format expansion logic that will come in later patches.

The change is straightforward plumbing that follows the established pattern for format options in other Git commands. The patch touches only the blame command's option parsing, adding the new string option alongside existing output control flags like `-s` and `-e`. The implementation appears clean and properly integrated with the existing option handling code.

This initial patch sets up the foundation for the more complex formatting work to come in subsequent patches, which will need to handle the actual format string expansion and introduce blame-specific placeholders. The commit message is brief but adequate for this infrastructure-only change, though it doesn't reference the larger series context (which is covered in the thread summary).