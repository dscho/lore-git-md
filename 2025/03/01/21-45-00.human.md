Feature patch introducing customizable formatting to `git blame`, modeled after `git log`'s format specifiers. This is the first of a 7-part series from first-time contributor Aleks Todorov.

The patch adds basic infrastructure for format strings in `git blame` by introducing a new `--format`/`-F` option that accepts a format string (stored in a new `format` variable). The implementation is minimal at this stage - just adding the option parsing without yet implementing the actual formatting logic. The diff shows the straightforward addition of the option to `builtin/blame.c`'s command-line parsing.

As the thread context notes, this is part of a larger effort to bring `git log`-style formatting capabilities to blame output, with future patches introducing blame-specific placeholders for filename and line number. The approach appears well-considered, reusing Git's existing pretty-print infrastructure while acknowledging some limitations around column width detection.

The patch itself is clean and focused, though reviewers will likely want to see how the subsequent patches implement the actual formatting before evaluating the overall approach. The commit message could be more detailed about the planned format specifiers and how they'll differ from `git log`'s, but this will presumably be covered in later patches.