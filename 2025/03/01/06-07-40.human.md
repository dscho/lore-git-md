Bugfix for a memory leak in reftable test code.

H Z reports a memory leak in the reftable test suite where `rd->name` allocated by `xstrdup()` in `init_reader()` is never freed in `test_corrupt_table_empty()`. The leak occurs when the test_corrupt_table_empty` test function initializes a reader but doesn't clean it up properly.

The report clearly traces the allocation path from `xstrdup()` through `init_reader()` to the test case, showing good technical understanding. While this is test code rather than production code, memory leaks in tests can still be problematic, especially for long-running test suites or when running under memory checkers like Valgrind.

The patch appears to be a straightforward fix - likely just needing to free `rd->name` in the test cleanup. The reporter hasn't provided an actual patch, just the bug report, so this would need implementation by a maintainer or the reporter. The clear description makes this easy to verify and fix.