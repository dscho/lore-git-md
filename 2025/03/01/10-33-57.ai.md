This is part 2/3 of the bundle-uri performance optimization series, focused on updating test cases to match the new reference hierarchy structure. The patch updates the test script `t/t5558-clone-bundle-uri.sh` to reflect that bundle references are now stored under `refs/bundles/heads/` rather than directly in `refs/bundles/`.

The changes are mechanical but extensive - 86 lines changed across 172 test assertions to update reference paths. The tests verify various bundle cloning scenarios including: path-based bundles, HTTP bundles, bundle lists, failure cases, and negotiation behavior. Key test areas affected include bundle header validation, object handling, non-default hash algorithms, and creationToken heuristic behavior.

This follows the v2 change that preserved reference hierarchy by storing heads under `refs/bundles/heads/` rather than flattening the namespace, addressing earlier naming consistency concerns. The test updates maintain coverage while validating the new reference layout works across all bundle scenarios. No functionality changes are introduced - this purely aligns tests with the hierarchy change from the previous patch.

The patch demonstrates thorough test maintenance, updating all relevant test cases to match the new reference structure while preserving their verification purposes. This maintains test coverage for the performance optimization work while ensuring the hierarchy preservation works as intended across all bundle use cases.