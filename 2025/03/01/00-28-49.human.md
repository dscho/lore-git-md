This is a technical discussion about bitmap iteration in the ongoing MIDX bitmap implementation work. Taylor Blau responds to Patrick Steinhardt's question about whether to use different iterator types for MIDX vs non-MIDX cases in the bitmap code.

The exchange clarifies that the current implementation already handles both cases correctly - the `*_all` fields and `base_nr` are properly initialized even for single-pack bitmaps (non-MIDX case) through the `load_bitmap()` function. This means the unified `ewah_or_iterator` approach works consistently across both scenarios without needing conditional logic.

The discussion shows careful attention to maintaining consistent behavior while simplifying the code path, with both developers agreeing the current approach is sound. This patch is part of a larger series (v3 12/13) refining the MIDX bitmap implementation, specifically around how object type iterators are handled.