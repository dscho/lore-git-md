Here is the dense AI thread summary:

Memory leak identified in reftable test infrastructure. Reporter H Z found that `test_corrupt_table_empty()` in `reftable/readwrite_test.c` leaks memory allocated via `xstrdup()` for `rd->name` through `init_reader()` in `reader.c`. The leak occurs in test code rather than production paths, but represents a resource management issue that should be fixed for hygiene. The execution path is clearly traced: allocation in wrapper.c -> storage in reader.c -> missing cleanup in test function. While not critical (affecting only test code), this warrants fixing to maintain proper resource handling patterns and prevent potential issues if test infrastructure is reused. The report provides sufficient technical details (files, functions, allocation path) for verification and correction, though no specific fix is proposed. The issue appears straightforward to address by adding appropriate cleanup in the test function.