Here is the updated dense AI thread summary:

Memory leak investigation in reftable infrastructure covering both test and production code paths. The thread originated with H Z identifying a test-only leak in `test_corrupt_table_empty()` (reftable/readwrite_test.c) through `rd->name` allocation path (wrapper.c -> reader.c -> readwrite_test.c). René Scharfe subsequently discovered a more significant production code leak in `reftable_reader_new()` where the 'name' field allocation would be lost if `block_source_read_block()` or `parse_footer()` failed during initialization.

Current state:
- Test case leak already fixed through prior refactoring
- Production leak addressed via `reftable_free(r->name)` in error paths while maintaining existing allocation timing
- Header documentation inconsistency noted (non-existent `reftable_reader_destroy()` in reftable/reftable-reader.h) remains as potential future work

Technical specifics preserved:
- Leak occurs when reader initialization fails mid-process
- Fix takes minimal approach without restructuring allocation timing
- Both issues involve single field cleanup making verification straightforward
- Thread evolution from test infrastructure to core reftable code memory management

The discussion has converged on a well-contained solution for the production leak while surfacing a documentation discrepancy that could be addressed separately. All context about the original test case, production code path analysis, and solution approach remains intact for future reference.