This is a test patch (5/7) in a series adding customizable format specifiers to `git blame`. It introduces a new test script `t8015-blame-format.sh` to verify the `-F/--format` option and `blame.format` config added in earlier patches.

The test script checks three key aspects of the new formatting functionality: (1) that an empty format string produces clean output without blame information, (2) that both standard and new blame-specific format specifiers (`%F` for filename and `%L` for line number) work as expected, and (3) that the `blame.format` configuration is properly recognized and applied. The tests follow Git's standard test patterns and include setup with realistic commit data.

This appears to be a thorough test suite covering the core functionality introduced in the series so far. The patch is cleanly integrated with the existing test infrastructure (added to `meson.build`) and follows the project's testing conventions. As the fifth in a seven-part series, it builds on the format specifier implementation from earlier patches while leaving documentation updates for later in the series.