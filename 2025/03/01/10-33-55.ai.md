Performance optimization for bundle-uri reference negotiation, v2 series addressing tag handling and test coverage. This is Scott Chacon's follow-up to the discussion about improving fetch performance by copying all bundle references (not just heads) into the refs/bundle/ namespace.

The series now includes three patches (expanded from v1's single patch) that:
1. Maintains the core change copying all refs (not just heads) into refs/bundle/
2. Updates existing bundle clone tests to check the new refspec path (refs/bundle/heads/)
3. Adds new test coverage specifically for tag handling in bundle-uri clones

Key technical details:
- The refspec mapping now preserves hierarchy (refs/heads/foo → refs/bundle/heads/foo)
- Tests verify both branch and tag references are properly handled
- The implementation still uses the single-file modification approach from v1 (bundle-uri.c line change)

This version directly responds to Junio Hamano's concerns about reference naming consistency by preserving the heads/ subdirectory structure, while also addressing Derrick Stolee's tag handling concerns through explicit test coverage. The series appears to favor the fetch-negotiation-side solution rather than modifying bundle creation.

The patch demonstrates progress on the thread's open questions by:
- Providing a concrete implementation of the heads/ subdirectory approach
- Adding test coverage for tag scenarios
- Maintaining the performance benefits while addressing safety concerns

Remaining considerations not yet addressed in this series:
- Multiple bundle reference collision handling
- The peeled tags vs temporary tags question
- Backward compatibility validation

The expanded test coverage and reference hierarchy preservation suggest this is moving toward a viable solution, though the thread's deeper questions about tag philosophy may require further iteration.