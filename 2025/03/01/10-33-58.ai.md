Performance optimization for bundle-uri reference negotiation, part 3/3 focusing on tag handling. This final patch in Scott Chacon's series adds test coverage for tag handling in bundle-uri clones, completing the test suite updates started in part 2.

The patch introduces two new test cases in `t/t5558-clone-bundle-uri.sh`:
1. Creates a test repository with multiple branches and tags
2. Verifies that tags from bundles are properly cloned into the `refs/bundles/tags/` namespace and included in reference negotiation

The test setup creates a bundle containing all refs (including tags) from a sample repository, then clones using that bundle and verifies the tag references appear in the expected location. This addresses one of the open questions from earlier in the thread about tag handling strategy, though the implementation appears to take the straightforward approach of including tags in the same way as branch references.

The test coverage is thorough, checking both the presence of tags in the bundle and their correct placement in the cloned repository's reference hierarchy. This completes the test matrix for the bundle-uri reference negotiation changes, which previously focused only on branch references. The patch maintains consistency with the series' approach of preserving reference hierarchy while expanding coverage to all reference types.

Key technical details:
- New test file: `t/t5558-clone-bundle-uri.sh` (adding 31 lines)
- Reference namespace: `refs/bundles/tags/` now included alongside `refs/bundles/heads/`
- Test verification: Checks both bundle creation and clone results
- Tags handled same as branches in negotiation

This concludes the series with comprehensive test coverage for all reference types in bundle-uri negotiation, though the thread's other open questions (like handling reference collisions) may need separate follow-up work.