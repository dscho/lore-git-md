Documentation patch in a series adding customizable output to `git blame`. This is patch 6/7 in Aleks Todorov's series introducing `--format` options for blame output, similar to `git log` formatting.

The patch moves the comprehensive list of format placeholders from `pretty-formats.adoc` to a new shared file `placeholders.adoc`, which can be included by both the log and blame documentation. This is a straightforward refactoring that enables the blame documentation (coming in later patches) to reference the same placeholder definitions without duplication.

The change is purely mechanical - the content is identical, just relocated. This follows good documentation practices by centralizing shared content that multiple commands need to reference. The patch maintains all existing functionality while setting up for the blame format documentation to be added cleanly in subsequent patches.

As part of a well-structured series, this patch does its job efficiently - it's a simple but necessary step to enable the final documentation of the new blame formatting capabilities. The implementation looks correct and follows Git's documentation conventions.