This is a bugfix for a memory leak in the reftable infrastructure, continuing the thread about memory management issues in the reftable reader code. H Z responds to René Scharfe's analysis of their initial report about a test-only leak, which turned out to be already fixed through prior refactoring. 

The key development is that René identified a more significant production code leak in `reftable_reader_new()` where the 'name' field allocation would be lost if `block_source_read_block()` or `parse_footer()` failed during reader initialization. The proposed fix adds cleanup via `reftable_free(r->name)` in the error path while maintaining the existing allocation timing.

Technical details worth noting for future reference:
- The leak occurs in production code when reader initialization fails mid-process
- The fix takes a minimal approach rather than restructuring when the name is allocated
- Header documentation inconsistency remains (non-existent `reftable_reader_destroy()` mentioned in reftable/reftable-reader.h)
- The thread has evolved from test infrastructure hygiene to addressing a real memory management issue in core reftable code

The patch appears straightforward and well-contained, addressing a clear issue with a minimal change. The discussion also surfaces an interesting documentation discrepancy that could be addressed separately.