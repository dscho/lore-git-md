This thread began with H Z identifying a memory leak in the reftable test infrastructure, which led René Scharfe to discover a more serious production code leak in `reftable_reader_new()`. The issue occurs when initialization fails mid-process, leaving the 'name' field allocated but unreachable. 

The discussion explored several solutions, with Jeff King contributing a careful review that uncovered a potential NULL dereference edge case. The final patch, now in its second version, takes a minimal approach: it adds cleanup for the `name` field in error paths while maintaining the existing allocation timing, and includes Jeff's suggested NULL check to prevent dereferencing a NULL pointer. The fix is concise (just 2 lines) and well-documented, addressing both the original leak and the edge case identified during review.

While the core issue is now resolved, a minor documentation inconsistency regarding `reftable_reader_destroy()` remains as potential future work. The thread demonstrates the project's attention to memory management details, with the initial test case leading to a production fix through collaborative review.