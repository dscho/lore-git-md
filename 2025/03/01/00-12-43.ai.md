Incremental MIDX bitmap implementation discussion (continued)

This email continues the technical exchange between Taylor Blau and Patrick Steinhardt about the incremental MIDX bitmap implementation, specifically addressing concerns around recursive lookup behavior in `bitmap_for_commit()`. The key points:

1. **Recursion depth concern**: Steinhardt raises a valid point about potential stack overflow from deep recursion when traversing MIDX layers, though Blau notes this is unlikely in practice given current layer depth expectations.

2. **Layer compaction status**: Blau confirms that while MIDX layer compaction is not yet implemented, the design accommodates future compaction capabilities (planned for part 3 of this series). This addresses Steinhardt's question about whether layer growth is actively managed.

3. **Implementation details**: The patch modifies `bitmap_for_commit()` to handle NULL `bitmap_git` cases and adds recursive fallback to base layers when commits aren't found in the current MIDX. The changes maintain the hot codepath optimization while adding the new incremental MIDX capability.

4. **Context**: This discussion occurs within the broader MIDX bitmap series where object ordering rules, layer semantics, and performance characteristics are being carefully designed and documented. The current exchange focuses on practical implementation safety rather than architectural changes.

Technical implications for future discussion include:
- Recursion depth safety in MIDX traversal paths
- Interaction between bitmap lookup and future compaction behavior
- Performance characteristics of the recursive fallback path
- Potential need for iteration-based alternatives to recursion
- Layer count monitoring in production use

The conversation remains focused on implementation robustness while maintaining the established design direction.