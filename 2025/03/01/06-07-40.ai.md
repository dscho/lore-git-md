This is a bugfix report identifying a memory leak in the reftable test code. The reporter (H Z) has found that `test_corrupt_table_empty()` in `reftable/readwrite_test.c` calls `init_reader()` which allocates memory for `rd->name` via `xstrdup()`, but never frees it. The leak occurs in a test case rather than production code, but still represents a real resource management issue.

The report clearly traces the execution path: `xstrdup()` allocation in `wrapper.c` -> `init_reader()` in `reader.c` storing to `r->name` -> test function in `readwrite_test.c` failing to clean up. While test code memory leaks are less critical than those in main code paths, this still warrants fixing to maintain good hygiene and prevent potential issues if test infrastructure is reused.

The report is well-structured with clear technical details about the leak's origin and propagation path. The reporter doesn't propose a specific fix, but provides enough information for maintainers to verify and address the issue. This appears to be a straightforward, legitimate bug report that should be simple to verify and fix by adding the missing cleanup in the test function.