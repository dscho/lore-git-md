Feature patch (4/7) implementing the core formatting logic for customizable `git blame` output. This patch builds on the infrastructure from the first three patches in the series, now actually applying the user-specified format string (from either `--format` or `blame.format` config) to generate blame output. The key change is a complete rewrite of the `emit_other()` function to handle both the new format string path and the traditional output path.

The implementation shows careful attention to existing behavior, maintaining all current output options (score, name, number display) while adding the new formatting capability. The example in the commit message demonstrates how the new system can replicate the default blame output format, suggesting this will be useful for debugging workflows where users want to see commit subjects.

The patch appears technically sound, following Git's established patterns for format handling by reusing the `pretty_print_context` infrastructure with the new `is_blame` flag introduced earlier in the series. The diff shows a clean separation between the new format path and legacy output path, suggesting good maintainability.

This represents the first end-to-end working version of the feature, though the series still needs documentation and additional test coverage in the remaining patches. The implementation choice to completely restructure `emit_other()` rather than try to adapt the existing code suggests the author has thought carefully about the cleanest way to integrate the new functionality.