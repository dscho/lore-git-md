This is a bugfix addressing a memory leak in the reftable reader code, following up on an earlier report about test infrastructure leaks. René Scharfe corrects the original report by noting that the specific test case mentioned (test_corrupt_table_empty) no longer exhibits the leak due to recent code changes, but identifies a related leak in the production reftable_reader_new() function when certain error conditions occur.

The patch is straightforward - it adds a missing cleanup of the reader's name field when block_source_read_block() or parse_footer() fail during reader initialization. While not a critical issue (the leak only occurs on error paths), it's good hygiene to fix. The author also notes an oddity in the API documentation mentioning a non-existent reftable_reader_destroy() function, though this isn't addressed in the current patch.

The discussion shows careful attention to the code's evolution, with René tracking how recent changes (like the move to unit tests and refactoring of string duplication) affect the original report. The fix itself is small (just one line added) and well-justified, making it likely to be accepted.