AI thread summary:

This thread concerns a straightforward housekeeping patch to Git's `.editorconfig` file, extending its formatting rules to cover `.bash` files in addition to the existing `.sh` file handling. The change enforces tab-based indentation (per Git's coding guidelines) for Bash scripts like `git-completion.bash` and `check-directional-formatting.bash` when edited in EditorConfig-aware editors.

Original contributor David Mandelberg submitted a one-line modification adding `bash` to the shell script extension pattern, maintaining consistency with Git's C formatting rules (via `.clang-format`). Both affected files already use tabs, making this a mechanical consistency improvement with no functional impact. No tests or documentation changes were required.

Junio C Hamano acknowledged the patch as "obviously correct" in his characteristic approval style, indicating it requires no further discussion and will likely be merged as-is. The thread appears concluded with this maintainer acknowledgment, as no technical concerns were raised about this purely mechanical editor configuration update. The change ensures EditorConfig will enforce the project's standard indentation rules consistently across all shell script variants.