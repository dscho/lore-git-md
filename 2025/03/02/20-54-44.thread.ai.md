Existing AI thread summary:

[No existing summary - this is the thread root]  

AI thread summary:

This thread begins with a contributor housekeeping patch to extend Git's `.editorconfig` file to apply consistent formatting rules to `.bash` files, matching the existing treatment of `.sh` files. The change ensures tab-based indentation (per Git's coding guidelines) will be enforced for Bash scripts like `git-completion.bash` and `check-directional-formatting.bash` when edited in EditorConfig-aware editors.

The one-line modification adds `bash` to the shell script extension pattern in the EditorConfig section that maintains alignment with Git's C formatting rules (via `.clang-format`). The patch notes both affected files already use tabs, making this a straightforward consistency improvement with no functional impact. No tests or documentation changes are required for this mechanical editor configuration update.