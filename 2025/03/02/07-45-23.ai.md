This is the first patch in a series fixing bash completion issues with remote names containing slashes. It introduces a new helper function `__git_escape_fnmatch()` that properly escapes special characters (\, ?, *, [) in strings for use with fnmatch(3) pattern matching. The function will be used in a follow-up patch to handle ref names containing slashes during completion.

The patch adds the helper to git-completion.bash along with a test case in t9902-completion.sh that verifies it correctly escapes all special characters. The test creates an input string with all special characters and compares the output with the expected escaped version. This is preparatory work for the main fixes coming in later patches that will address the slash-handling bugs mentioned in the thread summary.

The implementation appears straightforward and well-tested for its limited scope. The test case covers all the special characters fnmatch cares about, suggesting the author has considered the edge cases. This foundational piece will enable the subsequent patches to properly handle remote names with slashes by ensuring the pattern matching works correctly on escaped strings.