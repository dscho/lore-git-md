Here is the dense AI thread summary capturing all key information from this new thread:

This thread introduces a feature and bugfix patch series for git-p4, the Git-P4 bridge tool, authored by Nikolay Shustov. The changes focus on improving git-p4's workflow with new configuration options and command-line flags while fixing a file handling issue after reverts. 

The series adds several user-configurable features: P4CommandErrorEncoding for non-UTF-8 error handling, changelist creation controls (disable-p4refresh/new-changelist), shelving workflow improvements (keep-p4opened with description templating via p4-description-file/p4-description), and editor control flags (no-edit-p4spec/edit-p4spec). The bugfix properly cleans up writable files after revert operations by using p4 revert -w for added files.

Technical specifics include new config settings (git-p4.P4CommandErrorEncoding, git-p4.disableP4Refresh, git-p4.makeNewChangelist, git-p4.keepShelvedFilesOpened, git-p4.descriptionTemplateFile, git-p4.descriptionTemplate) with corresponding command-line flags. Description templates support {message}, {hash} and \n substitutions. The changes are confined to git-p4's Python implementation and Perforce workspace interaction without modifying core Git functionality. This appears to be a standalone contribution not part of a larger series.