Feature proposal for inline code comments, following up on initial concept. ZheNing Hu continues exploring the idea of Git-native line-specific annotations that would work similarly to GitHub/GitLab comments but in a decentralized way. The follow-up suggests a `git code-note` command interface resembling `git log -L` for viewing comments, while acknowledging uncertainty about the technical feasibility of tracking comments through code changes.

This builds on the original proposal's vision of distributed code annotations that survive file modifications, but shows the author is still in early exploration phase regarding implementation. The email doesn't advance specific technical solutions but keeps the conversation going by framing the feature in terms of existing Git commands (`blame`, `notes`, `log -L`) that contributors would recognize. The proposal remains conceptually interesting but still needs concrete technical exploration around comment storage and line tracking.