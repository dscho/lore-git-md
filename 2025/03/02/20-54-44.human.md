This is a small contributor housekeeping patch that adds `.bash` files to Git's EditorConfig rules. The change ensures that `.bash` files (like the existing `git-completion.bash` and `check-directional-formatting.bash`) will follow the same tab-based indentation style as other shell (`.sh`) files in the project.

The patch is straightforward and uncontroversial - it simply extends an existing pattern to cover another similar file type. The author notes that the two existing `.bash` files in the tree already use tabs, making this change consistent with the current practice. The diff shows the minimal one-line modification to the `.editorconfig` file.

This appears to be a well-justified, low-risk change that maintains consistency in the codebase's formatting standards. The patch follows the project's conventions and includes a proper sign-off.