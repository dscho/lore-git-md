This is a final polish of the `git blame` porcelain output bugfix, addressing Patrick Steinhardt's review comments on the v2 patch. The changes are minor optimizations to an already merge-ready solution for showing ignored/unblamable markers in porcelain mode.

Karthik Nayak accepts two small suggestions from Patrick Steinhardt's review: renaming `emit_per_line_details()` to `emit_porcelain_per_line_details()` to better reflect its purpose, and switching from `printf()` to `puts()` for the simple string outputs. These don't affect the core functionality but improve code clarity and potentially performance.

The thread has already established consensus on the technical approach - using metadata lines in `--line-porcelain` mode rather than modifying SHA-1 prefixes, with comprehensive test coverage and documentation updates. This email confirms the final tweaks before the patch can be merged, showing the discussion has reached its natural conclusion with all major design questions resolved earlier.