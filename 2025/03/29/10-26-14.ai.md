Final polish for the `git blame` porcelain output fix. Karthik Nayak responds to Patrick Steinhardt's review of the v2 patch with two minor implementation tweaks: renaming `emit_per_line_details()` to `emit_porcelain_per_line_details()` to better reflect its purpose, and switching from `printf()` to `puts()` for the simple string outputs. These changes don't affect the core functionality but improve code clarity and potentially performance. The patch remains focused on properly showing 'unblamable' and 'ignored' markers in `--line-porcelain` output while maintaining backward compatibility, with comprehensive test coverage already in place. The thread has reached consensus on all major design points, and this exchange represents the final polishing before potential merge. Key technical details remain unchanged from the v2 implementation: per-line metadata emission through the blame_entry chain, strict separation between `--porcelain` and `--line-porcelain` behaviors, and preservation of SHA-1 line format stability. The changes respond to Patrick's observation about the porcelain-specific nature of the function and a minor output optimization opportunity.