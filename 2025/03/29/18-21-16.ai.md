Bugfix for `git blame` porcelain output, finalizing the implementation of ignored/unblamable line markers in porcelain mode. This v3 patch incorporates the last round of polish from the discussion thread, including:

1. Function rename from `emit_per_line_details()` to `emit_porcelain_per_line_details()` for clarity
2. Optimization of simple string outputs from `printf()` to `puts()`
3. Test fixes for proper variable interpolation in test names
4. Documentation updates clarifying the porcelain output behavior

The implementation now properly distinguishes between `--porcelain` and `--line-porcelain` modes while maintaining backward compatibility. The solution adds new metadata lines ("ignored" and "unblamable") rather than modifying existing SHA-1 line prefixes, addressing the original bug where these markers were missing from porcelain output despite being documented.

Key technical details:
- New `emit_porcelain_per_line_details()` function handles per-line status emission
- Status markers are emitted as separate lines in both porcelain modes
- Test coverage verifies behavior with both `--porcelain` and `--line-porcelain`
- Documentation updated in `blame-options.adoc` and `git-blame.adoc`
- Maintains strict backward compatibility with existing porcelain parsers

The thread discussion has concluded with all feedback addressed, making this a merge-ready solution to the long-standing porcelain output inconsistency. The patch has thorough test coverage and documentation, with the final version incorporating optimizations and naming improvements suggested during review.