This is a follow-up discussion about the SHA-1 implementation reporting feature, specifically addressing the naming of non-cryptographic SHA-1 variants in `git version --build-options` output.

Christian Couder provides a minor documentation fix suggestion for Justin Tobler's patch that adds reporting of unsafe SHA-1 implementations. The original patch introduced the "unsafe-SHA-1" terminology to denote faster but non-cryptographic SHA-1 variants, but there's been some concern about whether this label might unnecessarily alarm users.

The suggested edit is purely editorial - fixing a typo ("implmentation" → "implementation") and slightly rephrasing the documentation sentence for clarity. This doesn't change the technical implementation or the naming decision itself, which remains the open question from the thread. The discussion continues to focus on whether "unsafe-SHA-1" is the best terminology or if something like "fast-SHA-1" would be more appropriate.