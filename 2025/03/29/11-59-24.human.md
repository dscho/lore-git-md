This is a refactoring patch that's part of the ongoing effort to handle NULL repository cases more cleanly in Git's codebase. The patch removes a now-unnecessary NULL check before calling `repo_config()` in `builtin/update-server-info.c`, since a previous change made `repo_config()` itself handle NULL repositories.

The author, Usman Akinyemi, notes this change builds on two prior merged patches: one that originally added the NULL check, and another that made `repo_config()` NULL-safe. The patch also includes a test addition to `t1517-outside-repo.sh` to verify the behavior, following a suggestion from Patrick Steinhardt to consolidate such tests in one place rather than scattering them across individual test files.

The change appears straightforward and well-motivated, with clear references to prior work that established the context for this cleanup. The test addition shows good attention to maintaining coverage while following project conventions for test organization. This seems like a low-risk improvement that aligns with Git's ongoing code quality efforts.