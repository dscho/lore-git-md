This is a follow-up discussion on the SHA-1 implementation reporting patch series, specifically addressing the naming of non-cryptographic SHA-1 variants in the `git version --build-options` output. Christian Couder provides a minor documentation fix to Justin Tobler's patch 2/2, correcting a typo in the git-version.adoc text ("the implmentation used" → "that implementation").

The thread context shows this is part of a 2-patch series that adds SHA implementation reporting to `git version --build-options`, with the second patch specifically handling non-cryptographic SHA-1 variants (currently labeled "unsafe-SHA-1"). The main open question in the thread is whether "unsafe-SHA-1" is the right terminology, with "fast-SHA-1" being proposed as an alternative.

Christian's email doesn't weigh in on the naming debate but focuses on a straightforward documentation improvement. The change is minimal and uncontroversial, just fixing a grammatical error in the man page text that describes the new unsafe-SHA-1 output line. The technical implementation remains unchanged - the patch still reports these variants when detected via their compile-time defines (SHA1_*_UNSAFE) in help.c.