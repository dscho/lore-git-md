Refactoring patch removing a redundant NULL check in `update-server-info` as part of the `the_repository` removal effort. This is a straightforward cleanup that follows prior work making `repo_config()` safely handle NULL repository parameters. The patch removes an unnecessary conditional check before calling `repo_config()` in `builtin/update-server-info.c`.

The change is minimal (4 lines modified) and clearly correct, building on two previously merged patch series that modified repository handling infrastructure. The patch also adds a new test case in `t1517-outside-repo.sh` to verify the command's behavior when run outside a repository, following Patrick Steinhardt's suggestion about consolidating such tests.

Mentored by Christian Couder, this appears to be a clean architectural improvement that aligns with the ongoing work to eliminate the `the_repository` global. The patch looks ready for merging as a simple but meaningful cleanup.