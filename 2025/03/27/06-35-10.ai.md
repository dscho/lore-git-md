This is a technical follow-up in the compiler detection thread, specifically addressing edge cases in sed syntax for parsing compiler version strings. Jeff King acknowledges Eric Sunshine's correction about the importance of properly matching the version string before quitting, rather than unconditionally quitting after the first line (which could miss version strings preceded by copyright notices). 

The discussion confirms Eric's solution using `sed -n '/ version /{p;q;}'` as the correct approach, which:
- Uses pattern matching to find the version string specifically
- Only quits after finding the match (not on first line)
- Works across different sed implementations

Jeff notes his initial misunderstanding about sed's behavior and speculates about possible historical sed implementations that might have handled `/foo/pq` differently, though this appears to be incorrect. The exchange serves as final confirmation that Eric's solution properly handles all edge cases in compiler version detection, including when version strings aren't the first output line.

This email represents the final resolution of the sed syntax issue that was part of the larger compiler detection work in the comma-operator refactoring series. The technical solution has now been thoroughly vetted by multiple contributors (Jeff King, Eric Sunshine) and can be considered settled.