Bugfix patch addressing a theoretical uninitialized variable access in packfile internals. This is the third in a 4-part series fixing similar static analysis warnings found by CodeQL.

The change initializes `revindex_size` to 0 in `load_revindex_from_disk()` to prevent potential access to uninitialized data if the function errors out early. While the uninitialized path would likely indicate deeper problems (as the function would fail before using the value), this makes the code's behavior explicitly correct. The patch follows the series' pattern of defensive initialization for theoretical edge cases.

The change is minimal (1 line modified) and doesn't affect observable behavior, focusing purely on making the code more robust against static analysis warnings. Like others in the series, this appears to be preventative maintenance rather than fixing an observed bug, touching core packfile functionality where correctness is particularly important.