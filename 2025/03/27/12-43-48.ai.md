This is a bugfix patch in a series addressing uninitialized variable warnings from static analysis. It's the third of four patches, all following the same pattern of defensive initialization for variables flagged by CodeQL analysis.

The patch modifies `pack-revindex.c` to initialize `revindex_size` to 0 in the `load_revindex_from_disk()` function. The variable was previously uninitialized when the function errored out, though the author notes this would only happen in error paths. The change makes the code's behavior explicitly correct by ensuring the variable is always initialized before use, even though the uninitialized value would likely have been harmless in practice.

This follows the series' pattern of addressing theoretical edge cases found by static analysis rather than fixing observed bugs. The change touches packfile internals but doesn't alter observable behavior. Like the other patches in the series, it's a minimal one-line change that makes the code more obviously correct without changing its functionality. The patch maintains consistency with the series' approach of defensive programming for static analysis findings.