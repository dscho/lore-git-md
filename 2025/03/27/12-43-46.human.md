Bugfix patch addressing a potential uninitialized variable warning in `cat-file` plumbing. This is the first in a 4-part series fixing similar static analysis warnings found by CodeQL across Git's codebase.

The patch initializes a `size` variable in `cat_one_file()` that could theoretically be used uninitialized if `do_oid_object_info_extended()` failed to populate a type name (though the commit message notes this shouldn't happen in practice). The change makes the code more obviously correct at a glance by removing a subtle control-flow dependency, while functionally equivalent to the original. The fix follows the series' pattern of defensive initialization for variables flagged by static analysis.

The change is minimal (1 line modified) and low-risk, touching core object inspection plumbing but not altering observable behavior. As with the rest of the series, this appears to be preventative maintenance rather than fixing an observed bug. The commit message clearly explains both the theoretical concern and why it's likely not a practical issue.