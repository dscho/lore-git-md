This is a refactoring patch in Patrick Steinhardt's series enhancing Git's object filtering capabilities. It modifies the `show_reachable_fn` callback interface in the pack-bitmap subsystem to accept a payload parameter, laying groundwork for future functionality.

The change is straightforward but touches several key components: the pack-bitmap interface itself plus callers in pack-objects, rev-list, and reachable.c. The patch adds a `void *payload` parameter to the callback implementations, passing NULL for now since this is preparatory work. The diff shows careful attention to maintaining existing behavior while extending the interface.

This appears to be a well-justified change that follows the series' architectural direction. The thread context indicates this is part of implementing object type filtering, where the payload will likely carry filter state. The patch maintains backward compatibility and doesn't introduce any observable behavior changes at this stage.