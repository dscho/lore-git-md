This is part 5/20 of Patrick Steinhardt's series to remove Perl dependencies from Git's test suite. The patch converts the `test_readlink()` helper function from using Perl to a native implementation in `test-tool`.

The change replaces the one-line Perl implementation that reads symlinks with a new C function in `test-path-utils.c` that uses Git's existing `strbuf_readlink()` infrastructure. The shell interface remains identical, just routing through `test-tool` instead of Perl. This allows tests using symlink inspection to work on platforms without Perl or the `readlink(1)` command.

The implementation looks straightforward and maintains the same behavior while removing another Perl dependency. Like previous patches in the series, it follows the established pattern of moving functionality into Git's test infrastructure rather than relying on external tools. The change is small (14 lines added) and focused, with no apparent behavioral changes beyond the implementation switch.

This continues the systematic approach seen in earlier patches, addressing another discrete piece of test infrastructure that previously required Perl. The patch appears well-contained and unlikely to be controversial given the series' established direction.