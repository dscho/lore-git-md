This is a refactoring patch in Patrick Steinhardt's series enhancing Git's cat-file batch mode filtering capabilities. The change modifies the `show_reachable_fn` callback interface in the bitmap code to accept a payload parameter, which will be used by subsequent commits in the series for implementing object filtering.

The patch updates the callback type definition in pack-bitmap.h and adjusts all implementations across five files (pack-bitmap.c, builtin/pack-objects.c, builtin/rev-list.c, and reachable.c) to accept the new payload parameter. While currently unused (marked with UNUSED where applicable), this change lays groundwork for passing filter state through the bitmap traversal code.

The modifications are mechanical but thorough, touching all relevant call sites while maintaining existing functionality. This appears to be a well-scoped preparatory change for the filtering feature, following the established pattern of the series where each commit builds incrementally toward the final implementation. The patch maintains consistency with the series' architectural approach of extending rather than rewriting existing interfaces.