This is a refactoring patch in Karthik Nayak's v5 series on batched reference updates with partial failure support. It removes redundant checks in the files backend's reference update preparation code.

The patch eliminates an unnecessary duplicate check in `split_symref_update()` where the code was verifying something that was already guaranteed by an earlier check. This cleanup also allows simplifying related functions (`split_head_update()` and `files_transaction_prepare()`) by removing now-unneeded assignments to the `util` field of string list items.

The change is straightforward and appears to be a genuine simplification - the removed checks were redundant because the `string_list_has_string()` call earlier in the process already ensures no duplicates exist. The patch maintains all the important validation while making the code more concise.

This is a minor but logical optimization in a larger series that has already undergone several rounds of review. The change aligns with the series' goal of improving reference transaction handling, though it's more about code cleanliness than functionality.