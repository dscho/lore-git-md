This is a refactoring patch in Johannes Schindelin's series eliminating comma operator usage from Git's codebase. The patch focuses on xdiff/xdiffi.c, converting comma-separated statements in the diff algorithm's core into explicit semicolon-separated statements within curly-braced blocks.

The changes modify two similar code blocks in the `xdl_split()` function that handle forward and backward Myers diff algorithm passes. Each replaces a comma operator that previously combined two assignments (adjusting i1 and i2 when boundary conditions are hit) with separate statements wrapped in braces. The transformation follows the series' established pattern of preferring explicit control flow over comma operators, even when the original usage was intentional.

This patch is part of the comprehensive effort that has already addressed comma operator usage across multiple subsystems (remote operations, rebase, pattern matching, and test infrastructure). The xdiff changes complete the diff algorithm updates, with similar transformations already applied to diff-delta.c in earlier patches. The approach here matches the series' consensus style of using explicit blocks rather than macros or other alternatives.

Technical details:
- Modifies boundary condition handling in Myers diff implementation
- Changes affect both forward (fmax/fmin) and backward (bmax/bmin) passes
- Preserves identical algorithm behavior while improving code clarity
- Follows same pattern as kwset.c and diff-delta.c conversions in series
- Completes diff subsystem updates alongside prior diff-delta.c changes

The patch represents a straightforward application of the series' methodology to xdiff's implementation, with no behavioral impact expected. It follows the same style (explicit blocks over comma operators) that has been reviewed and approved in other subsystems throughout the thread.