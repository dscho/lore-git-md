Feature implementation adding type-based object filtering to `git cat-file` batch mode. This patch introduces the `object:type=` filter as part of Patrick Steinhardt's ongoing series to expand Git's object filtering capabilities.

The patch adds a new `LOFC_OBJECT_TYPE` enum to support filtering by object type (blob, commit, tag, or tree). The implementation follows the established pattern from previous filters, checking the object type during batch processing and skipping non-matching objects. Documentation is updated to explain the new filter syntax, and tests are added to verify all four object type cases work correctly.

This builds on the series' earlier work with blob size filtering, demonstrating the filter framework's extensibility. The implementation is cleanly integrated with existing batch processing logic and maintains consistency with the dual-path execution model (both regular and bitmap-optimized paths). Test coverage appears thorough, exercising all supported object types.

Key technical points for future reference:
- New `LOFC_OBJECT_TYPE` filter type
- `object_type` field added to filter options struct
- Type comparison happens in `batch_object_write()`
- Documentation shows `object:type=(tag|commit|tree|blob)` syntax
- Tests cover all four object types
- Maintains compatibility with existing `--batch` mode constraints

This appears to be a well-executed addition to the filtering framework, completing one of the remaining open items from the series' technical questions. The patch follows the established architectural patterns and includes appropriate documentation and testing.