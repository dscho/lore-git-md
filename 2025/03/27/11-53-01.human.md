This is a refactoring patch in a series eliminating comma operator usage in Git's codebase, specifically targeting the regex compatibility code.

The patch makes two changes to the regex implementation. First, it replaces a comma operator in `regex_internal.c` with explicit curly-braced statements for clearer control flow when merging node sets. Second, it marks an intentional comma operator usage in `regexec.c` with a `(void)` cast to indicate the discarded increment is deliberate, preserving the existing loop behavior that handles backreferences.

This is part of a well-reviewed series that has already addressed comma operators across multiple Git subsystems (remote-curl, rebase, diff algorithms, etc.). The changes here follow the established pattern of either replacing comma operators with clearer constructs or explicitly annotating intentional uses, maintaining the exact behavior while improving code clarity. The series appears ready for integration with all technical concerns resolved.