This is part 8/20 of Patrick Steinhardt's series to make Perl optional for Git's test suite, focusing on build system changes. The patch updates Meson's configuration to no longer require Perl just for running tests, reflecting the progress made in previous patches that have reduced test suite dependencies on Perl.

The change is straightforward - it removes the `get_option('tests')` check from Meson's Perl requirement logic, since tests can now run without Perl (except for Gitweb and netrc helper tests). This aligns the build system with the series' goal of making Perl optional for 97% of tests.

The patch follows the series' careful approach - it's a small, focused change that builds on infrastructure established in earlier patches. The commit message clearly explains the change and references the preparatory work done in previous commits. This continues the series' pattern of incremental, well-documented improvements to remove Perl dependencies.