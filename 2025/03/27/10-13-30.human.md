Build system fix for gitweb.js generation regression. This is a follow-up in a thread discussing how to fix a bug where the gitweb.js build script was incorrectly being included in the JavaScript output due to Makefile using `$^` (all dependencies) after a refactoring in commit 19d8fe7da65.

Patrick Steinhardt checks in on Thorsten Glaser's proposed fix, which explicitly lists JavaScript files rather than using `$^`. Patrick had previously suggested improvements to the commit message (now clearly explaining the regression's origin) and formatting (requesting proper Signed-off-by line). He also proposed an alternative solution using `$(filter %.js,$^)` to avoid file list repetition while still excluding non-JS files.

The email serves as a gentle nudge - Patrick asks if Thorsten plans to send an updated patch incorporating these suggestions, or if he should take over to finalize the fix. The discussion remains collegial, with all participants aligned on fixing the core issue but considering different approaches balancing portability and maintainability.