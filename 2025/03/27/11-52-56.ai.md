Refactoring patch in a series eliminating comma operators from Git's codebase. This is patch 3/10 in the final version of Johannes Schindelin's series, focusing on the kwset.c pattern matching implementation.

The patch converts comma operator usage in kwset.c to explicit semicolon-separated statements, following the series' goal of removing potentially confusing comma operators. Key changes include:
- Rewriting tree traversal logic in `kwsincr()` to use braced blocks for if/else branches
- Converting AVL tree rebalancing operations to use semicolons between statements
- Updating the Boyer-Moore string search loop in `bmexec()`
- Splitting a combined declaration in `cwexec()`

The changes are mechanical transformations that maintain the exact same control flow and behavior while improving code clarity. This follows the series' established pattern of using explicit statement separation rather than comma operators, with the specific style (braced blocks vs separate statements) chosen to match each context's existing style.

The patch is part of a well-reviewed series that has already addressed all technical concerns, with this particular file's changes being straightforward replacements that don't affect functionality. The kwset.c modifications complete the pattern matching subsystem updates in the series, alongside previously addressed changes to wildmatch.c and regex_internal.c.