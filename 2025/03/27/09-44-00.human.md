This is part 5/10 of Patrick Steinhardt's series adding filtering capabilities to `git cat-file --batch`, specifically implementing type-based object filtering. The patch adds support for `--filter=object:type=<type>` which will omit objects that don't match the specified type (blob, commit, tag, or tree).

The implementation follows the same pattern established in earlier patches for blob filtering, adding a new `LOFC_OBJECT_TYPE` enum and handling the type comparison in the batch processing logic. The documentation is updated to explain the new filter syntax, and tests are added to verify filtering works for all four object types.

This continues the series' clean architectural approach - the changes are minimal and focused, touching just the necessary parts of the code to add the new filter type while maintaining consistency with the established filtering framework. The test coverage appears thorough, checking all supported object types.

As noted in the thread summary, this addresses one of the remaining open technical items from the series (the `object:type=` filter) while maintaining the prerequisite relationship with Taylor Blau's bitmap work. The implementation looks straightforward and well-contained, following the proven pattern from the blob filtering patches.