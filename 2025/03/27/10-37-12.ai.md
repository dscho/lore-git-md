This is part 14/20 of Patrick Steinhardt's series to eliminate Perl dependencies in Git's test suite, focusing on GPG-related test infrastructure.

The patch converts the `sanitize_pgp()` test helper from using Perl to using sed for stripping PGP signatures from test output. The change removes the PERL_TEST_HELPERS dependency from this helper, though some tests in t6300-for-each-ref.sh still require Perl for other functionality and now explicitly declare that prerequisite rather than having the whole test file skip when Perl isn't available.

Key technical details:
- The new sed` implementation uses a range pattern (`/^-----BEGIN PGP/,/^-----END PGP/`) to match signature blocks
- The sed command prints only the delimiter lines (containing hyphens) while deleting the signature content
- 11 lines removed from t6300-for-each-ref.sh's early Perl prerequisite check
- 4 test cases in t6300 now explicitly declare PERL_TEST_HELPERS where they use Perl-specific features
- The change maintains the same signature-stripping behavior while removing Perl as a dependency

This continues the series' pattern of methodically replacing Perl functionality with POSIX tools while preserving test behavior. The patch is straightforward and maintains test coverage, though reviewers may want to verify the sed pattern handles all PGP signature formats correctly. The change demonstrates progress in the broader effort to eliminate Perl from test infrastructure while keeping the test suite functional.