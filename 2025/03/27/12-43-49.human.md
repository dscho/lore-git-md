Bugfix completing a 4-part series addressing uninitialized variable warnings found by CodeQL static analysis. This final patch initializes `mtimes_size` to 0 in `load_pack_mtimes_file()` to prevent potential access to uninitialized data in error paths, matching the defensive programming approach of the previous patches.

The change is minimal (a single line modifying a variable declaration) and follows the same pattern as the other fixes in the series - ensuring variables are initialized even in error cases where they wouldn't normally be used. Like the other patches, this addresses a theoretical rather than observed issue, as the code would likely work fine in practice (the uninitialized value would only be read in error paths where the function returns early).

The series as a whole demonstrates careful attention to static analysis findings, though reviewers may want to verify whether the flagged paths are actually reachable in real usage. This particular fix touches packfile internals (specifically the mtimes file handling) but like the others, maintains existing behavior while making the code more obviously correct.