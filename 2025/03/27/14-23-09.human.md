Bugfix discussion regarding static analysis warning in pack-revindex initialization.

Taylor Blau responds to Johannes Schindelin's patch that initialized `revindex_size` in `load_revindex_from_disk()` to address a potential uninitialized variable warning. Taylor provides a detailed analysis showing that in practice, the variable is always initialized before use due to the function's control flow - it's only accessed when either already set or when the calling code path wouldn't use the value. 

He questions whether this change was motivated by a specific static analyzer warning (mentioning CodeQL as a possibility given Johannes' work with Microsoft's Git fork) and suggests the patch might be unnecessary unless required to silence such warnings. While not opposed to defensive initialization in general, Taylor indicates he might recommend dropping this particular change if it wasn't flagged by an analyzer.

This continues the thread's theme of addressing theoretical uninitialized variable cases found through static analysis, though Taylor's analysis suggests this particular case may be a false positive where the code's logic already ensures safety. The exchange demonstrates careful consideration of when static analysis findings warrant code changes versus when existing control flow already handles the edge cases.