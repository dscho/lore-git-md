Refactoring patch in a series eliminating comma operators from Git's codebase. This is part 8/10 of Johannes Schindelin's series that has already undergone extensive review, with all technical aspects resolved across multiple subsystems.

The patch focuses on the regex compatibility code, making two changes to address comma operator usage. First, it replaces a comma-separated pair of decrements in `regex_internal.c` with explicit statements wrapped in curly braces, matching the series' preferred style. Second, it marks an intentional comma operator in `regexec.c` with a `(void)` cast to silence compiler warnings while preserving the existing control flow (a necessary exception due to the loop's use of `continue` statements).

This continues the series' pattern of either converting comma operators to clearer constructs or explicitly annotating them when removal would harm readability. The changes maintain exact behavior while improving code clarity and compiler warning hygiene. With all technical questions already resolved in prior reviews, this appears ready for integration as part of the final series.