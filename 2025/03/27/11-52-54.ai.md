Refactoring patch in the comma operator elimination series. This is the first patch in a 10-part series that has already undergone extensive review and is nearing finalization.

The patch modifies `remote-curl.c` to replace comma operators with semicolons in two RPC service initialization calls (`git-upload-pack` and `git-receive-pack`). This is a straightforward change that aligns with the series' goal of removing potentially confusing comma operator usage throughout Git's codebase. The changes are minimal - just two comma-to-semicolon conversions in struct field assignments.

Key technical details:
- Touches the remote operations subsystem
- Modifies `rpc_service` initialization in both fetch and push operations
- Changes are purely syntactic with no behavioral impact
- Part of broader effort to improve code clarity and maintainability

The thread context shows this series has reached consensus, with Junio Hamano already queuing the remote-curl.c changes. This particular patch is a simple but important part of the overall cleanup effort, addressing a code quality issue in Git's HTTP transport layer.