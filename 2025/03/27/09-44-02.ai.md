Performance optimization patch in the Git cat-file batch mode filtering series, adding bitmap iteration with filtering support.

This patch introduces `for_each_bitmapped_object()`, a new function that enables efficient iteration over bitmapped objects while applying filters. The implementation builds on the existing bitmap infrastructure but adds the ability to process objects through a filter before invoking the callback. Key technical aspects include:

1. The function takes a `list_objects_filter_options` parameter and checks filter applicability via `can_filter_bitmap()`
2. It initializes a full bitmap (all bits set) then applies the filter via `filter_bitmap()`
3. The filtered results are processed by type-specific handlers (`show_objects_for_type`) for commits, trees, blobs and tags
4. The existing `show_reachable_fn` callback signature is extended to support a payload parameter
5. Changes span both pack-bitmap.c (implementation) and pack-bitmap.h (interface declaration)

The patch appears well-integrated with the ongoing series, maintaining consistency with previous architectural decisions while adding the promised bitmap iteration capability. Performance considerations are addressed by reusing existing bitmap infrastructure and only allocating temporary bitmaps when needed. The change touches core bitmap handling code but follows established patterns, with the main new logic concentrated in the `for_each_bitmapped_object` function.

This appears to be a key piece of the filtering series, providing the underlying mechanism that will enable efficient filtered access to bitmapped objects. The implementation looks thorough, with proper error handling and resource cleanup. The changes to `show_objects_for_type` to accept an explicit bitmap parameter (rather than using `bitmap_git->result`) suggest careful consideration of the control flow.