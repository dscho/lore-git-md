Refactoring patch in a series eliminating comma operators. This is patch 3/10 in Johannes Schindelin's series to remove potentially confusing comma operator usage throughout Git's codebase. The series has already undergone extensive review and is in its final form, with all technical concerns addressed.

This particular patch focuses on `kwset.c`, which implements string search algorithms. It converts comma-separated statements into explicit semicolon-separated lines, particularly in the tree traversal and balancing logic. The changes are mechanical but improve code clarity by making the control flow more explicit. 

The patch shows careful attention to detail - each comma operator is replaced while maintaining the exact same execution order. The changes affect several key functions in kwset.c including tree rotations (`kwsincr`) and the Boyer-Moore string search implementation (`bmexec`). 

As part of a well-reviewed series that has already addressed all technical concerns, this patch appears ready for integration. It follows the established pattern of the series by making the code more readable without changing any behavior.