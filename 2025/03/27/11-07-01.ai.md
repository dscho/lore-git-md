Bugfix follow-up addressing test output improvements in the `git blame` porcelain mode patch series.

This email from Karthik Nayak responds to Eric Sunshine's review of test cases in the v2 patch for displaying ignored/unblamable markers in `git blame` porcelain output. The thread has already established the technical approach (using metadata lines in `--line-porcelain` mode) and received Junio Hamano's approval. This exchange focuses on final polish of the test suite.

The key changes here are test output improvements:
1. Fixing test titles to properly interpolate the `$opt` variable by switching from single to double quotes
2. Ensuring distinct test names for both porcelain modes (`--porcelain` and `--line-porcelain`) in the ignored lines test case

The patch maintains all the core functionality from v2:
- Still emits "unblamable" and "ignored" metadata lines in porcelain mode
- Preserves the distinction between regular and line-porcelain outputs
- Keeps the same test assertions (checking for exactly 2 matching lines)

This appears to be the final round of minor fixes before the patch can be merged, addressing the last remaining nitpicks about test output clarity. The changes are straightforward and uncontroversial, following directly from Eric Sunshine's suggestions. The thread has effectively reached consensus on both the technical approach and now the presentation details.