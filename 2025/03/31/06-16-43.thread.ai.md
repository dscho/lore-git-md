Updated AI thread summary:

This thread introduces a 5-part patch series adding Meson build system support for Git's performance benchmarks (`t/perf` infrastructure), authored by D. Ben Knoble. The series focuses on integrating benchmark infrastructure with Meson while maintaining compatibility with the existing `t/perf/run` script.

The first patch specifically addresses benchmark compatibility issue with alternate repository formats (SHA256 object format and reftable ref format). It modifies `t/perf/perf-lib.sh` to properly handle repository format preservation when copying repositories for benchmarking, extracting formats via `git rev-parse --show-ref-format` and `--show-object-format` and passing them to `git init`. This maintains the security benefit of not copying arbitrary configuration while supporting format variations (3 lines added, 1 changed).

Technical scope of the full series includes:
- Build configuration modifications (meson.build, meson_options.txt, t/meson.build)
- Benchmark infrastructure changes (t/perf/perf-lib.sh, t/perf/run)
- Key features: alternate repository format support (patch 1), PERL_PATH usage (patch 2), out-of-tree builds (patch 3), Meson build rules (patch 4), benchmark-related build options (patch 5)

The implementation preserves `t/perf/run` as the preferred method for advanced benchmarking while providing simpler Meson integration for basic cases. The changes are focused on build system integration without modifying benchmark functionality itself, maintaining backward compatibility. The patches appear technically sound with clear commit messages, though no reviews have been provided yet. The first patch specifically solves a general benchmark infrastructure issue that would affect users testing with non-default repository formats.