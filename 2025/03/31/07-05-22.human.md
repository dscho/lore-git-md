This is a final review comment on the `git blame` porcelain output fix, specifically addressing test script optimization.

Patrick Steinhardt points out that Karthik Nayak's test script for the unblamable line markers can be simplified by combining two `sed` operations into a single command. The current version writes intermediate files (`raw` and `expect`) multiple times, while Patrick suggests using chained `-e` options in `sed` to perform both replacements at once.

This is a minor optimization to the test script that was added in v4 of the patch series. The core functionality of marking unblamable lines in porcelain output remains unchanged - this is purely about making the test implementation cleaner. The suggestion comes late in a long thread where all major technical issues have already been resolved, with the patch series having gone through multiple rounds of review addressing porcelain format constraints, test coverage, and output formatting.

The change would make the test slightly more efficient and easier to maintain, though the current version is functionally correct. Given that this is the final review comment in a thread that has already reached consensus on the implementation, it's likely to be a quick follow-up fix if Karthik chooses to incorporate it.