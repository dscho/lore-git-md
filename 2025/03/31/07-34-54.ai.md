Final polish for `git blame` porcelain output tests. This email shows Karthik Nayak implementing Patrick Steinhardt's suggestion to optimize the test script by chaining `sed` commands rather than using multiple invocations. The change affects `t/t8013-blame-ignore-revs.sh` where the test verifies unblamable line markers in porcelain output modes.

The optimization combines two separate `sed` operations that modify test output into a single command with chained `-e` expressions. This addresses both portability concern (avoiding `-i` which behaves differently on macOS) and improves efficiency. The test continues to verify the same functionality - that lines marked as unblamable appear correctly in both `--porcelain` and `--line-porcelain` output modes when `blame.markUnblamableLines` is disabled.

This represents the final tweak to the test infrastructure for the feature that has already gone through extensive review, with all previous issues around output formatting, newline handling, and test robustness having been addressed in earlier iterations. The thread is now complete with all technical feedback incorporated.