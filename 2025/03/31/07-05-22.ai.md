This is a final review comment on the `git blame` porcelain output fix patch series, specifically addressing test implementation details in the v4 submission.

Patrick Steinhardt points out a minor optimization in the test script where two separate `sed` invocations could be combined into one. The test verifies the porcelain output format for unblamable lines when using both `--porcelain` and `--line-porcelain` modes. The suggested change would make the test more efficient by chaining `sed` expressions rather than creating intermediate files.

This feedback comes after the extensive thread has already resolved all major technical issues with the implementation, including output formatting, newline handling, and test verification. The patch series is otherwise complete with all previous review feedback addressed, including Junio Hamano's approval and Phillip Wood's bug reports.

Key technical details mentioned:
- The test verifies behavior with both porcelain output variants
- Uses `blame.markUnblamableLines` configuration
- Tests output formatting for ignored revisions
- Currently uses multiple `sed` steps that could be optimized

This represents the final polish on a long-running thread that has already achieved consensus on the core implementation. The suggestion is purely about test script efficiency and doesn't affect the actual functionality being tested.