This is part 6 of Patrick Steinhardt's reftable API refactoring series, focusing on clarifying naming in the block handling subsystem. The patch renames `struct reftable_block` to `struct reftable_block_data` to better reflect its role as a simple container for block contents rather than something that manages access to blocks. 

The change is mechanical but meaningful - it prepares for a follow-up rename where `reftable_block_reader` will become `reftable_block`, making the naming hierarchy clearer (data container vs access manager). The patch updates all related function names consistently (`block_source_read_block` → `block_source_read_data`, `return_block` → `release_data`), touching 8 files with straightforward replacements.

This continues the series' pattern of improving API clarity for both Git internals and external consumers like libgit2. The changes are well-contained and appear to maintain all existing functionality while making the code's intent more obvious. As with earlier patches in the series, this looks like a clean, purposeful improvement to the reftable implementation.