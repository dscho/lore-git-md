This is a follow-up discussion in the ongoing thread about SHA implementation reporting in `git version --build-options`. The email focuses on naming conventions for displaying SHA-1 variants, particularly the collision-detection vs non-crypto cases.

Justin Tobler responds to Christian Couder's suggestions about alternative naming for "unsafe-SHA-1" output. The discussion has shifted from technical implementation (now settled on a symbol-based approach) to user-facing presentation details. Key points:

1. Naming alternatives are proposed for the non-crypto SHA-1 case:
   - Christian suggested "SHA-1 for non-crypto" when unsafe-SHA-1 is used
   - Justin counter-proposes "non-crypto-SHA-1" as slightly less alarming than "unsafe" while still being descriptive

2. Warning display is discussed:
   - Christian suggested adding explicit warnings when non-collision-detecting SHA-1 is used for crypto
   - Justin agrees to implement this with "No collision detection!!!" warnings where appropriate
   - References the thread's earlier decision to use internal symbol names rather than invent new human-readable ones

The email shows the thread moving into final UX polish phase after architectural decisions were settled. The remaining open questions are purely about output formatting and warning messages, with Justin indicating he'll incorporate both the symbol-based approach and the warning suggestions in the next version.