A straightforward test fix in the performance test suite. This is responding to Philippe Blain's patch that addresses an edge case in the scalar clone test when run from a detached HEAD state.

The original issue occurs when the test repository is in detached HEAD - the `scalar clone` command fails to determine the remote's default branch properly. Philippe's solution adds an explicit branch creation step (`git checkout -B test-branch`) during test setup to ensure consistent behavior regardless of the initial repository state.

Patrick Steinhardt's response is positive, calling it an "easy and pragmatic fix." This appears to be the end of the discussion on this particular test fix, which was already uncontested in the thread. The change is small (just 3 lines modified) and focused specifically on making the test more robust.