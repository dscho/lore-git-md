Security configuration follow-up for git-daemon ownership verification. This is a response in the ongoing thread about configuring git-daemon safely after CVE-2022-24765's ownership verification changes.

Konstantin Ryabitsev suggests a simpler wildcard pattern (`*`) for `/etc/gitconfig` as a workaround for the issues MegaBrutal encountered with path matching in git-daemon. The response acknowledges that Git 2.43.0 had limited wildcard support, only accepting `*` rather than path patterns like `/srv/git/*`.

This moves the discussion forward by offering a concrete configuration change that should work with the older Git version in question, though it comes with the tradeoff of being less specific than the path-based approach. The email doesn't address the current directory ('.') issue directly, focusing instead on the wildcard resolution problem. 

The exchange highlights the practical challenges of applying the CVE mitigations in real-world git-daemon deployments, particularly with older Git versions. The suggested solution appears to be a stopgap measure rather than a long-term fix, leaving the underlying path resolution discrepancies between git-daemon and regular Git operations still needing attention.