This is a build system clarification that was part of a series but is now being removed as redundant. 

Patrick Steinhardt acknowledges that this patch (which would have required Perl when building documentation) is functionally identical to a change already merged in commit 7c8cd9c1 from March 16th. The only difference was a typo (`get_options` vs `get_option`) that, when corrected, made the patches the same. Junio Hamano spotted the redundancy and is dropping this patch from the series.

The thread shows good maintenance hygiene - catching duplicate work before it gets merged and keeping the commit history clean. The original issue (ensuring Perl is available when building docs) remains properly fixed, just via the earlier commit rather than this now-abandoned patch.