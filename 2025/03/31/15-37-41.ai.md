This is a follow-up discussion on the first patch in the rebase status reporting improvements series, focusing on merge commit handling during interactive rebase. Phillip Wood provides additional review feedback on Philippe Blain's fix for empty merge resolution in `rebase -r`.

Key technical points raised:
- Suggests expanding cleanup to include `git_path_merge_mode()` in `sequencer.c`, possibly using `remove_merge_branch_state()` instead of individual file deletions
- Questions test placement, proposing t3430 might be more appropriate since it already has failing merge setup
- Recommends adding a test case to verify `git rebase --skip` doesn't create empty merges

The discussion builds on the original patch's changes to `commit_staged_changes()` and `do_merge()` in sequencer.c, maintaining focus on proper merge state handling. Phillip's feedback indicates the patch is generally sound but could benefit from additional test coverage and more comprehensive state cleanup.

This email doesn't introduce new functionality but refines the implementation details of the first patch in the series. The suggestions appear technically sound and aligned with Git's testing practices, particularly around state cleanup completeness and test case organization.