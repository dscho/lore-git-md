This is patch 4/8 in Elijah Newren's series to fully remove the merge-recursive backend in favor of merge-ort. The patch focuses on converting the sequencer's non-recursive merge operations to exclusively use merge-ort.

The change removes the conditional logic in `do_recursive_merge()` that previously allowed selecting either merge-recursive or merge-ort backends. Since merge-ort has been the default for a long time and the series aims to completely eliminate merge-recursive, this patch simplifies the code by removing the now-unused merge-recursive path. The remaining code directly calls `merge_incore_nonrecursive()` from merge-ort.

Key technical details:
- Removes 22 lines of conditional backend selection code while keeping 13 lines of merge-ort-specific logic
- Maintains the same TODO comment about potential optimizations for working tree updates
- Preserves all existing merge-ort behavior including conflict handling and output display
- Affects only `sequencer.c`, specifically the merge operations during rebase/cherry-pick sequences

This is a straightforward mechanical change that follows the series' pattern of removing merge-recursive support from various callers. The patch appears well-contained and low-risk, with no functional changes beyond the backend switch. It builds on the previous patches' work to ensure merge-ort fully replaces merge-recursive's capabilities.