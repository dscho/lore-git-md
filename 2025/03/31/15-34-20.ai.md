Feature proposal for fetch/clone filtering. This appears to be a test email from John Giorshev regarding a proposed `--must-filter` option for `git fetch` and `git clone`, though the actual patch content is missing. The subject suggests this would be a new filtering capability for these commands, but without seeing the implementation details or motivation in the email body, we can't assess the technical approach or use cases. The email seems to be testing formatting rather than presenting the full proposal - likely a follow-up to an earlier thread about fetch filtering that isn't included in this test message. Key things to look for in any subsequent messages would be: what specific filtering behavior this enables, how it differs from existing `--filter` options, and whether there are performance or compatibility implications.