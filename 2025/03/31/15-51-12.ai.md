This is the first patch in Elijah Newren's series to remove the legacy merge-recursive implementation in favor of merge-ort. The patch switches the checkout command's internal merge operation from using merge-recursive's `merge_trees()` to merge-ort's `merge_ort_nonrecursive()`.

The change is straightforward mechanical replacement - updating the include statement and function call while keeping all other parameters the same. The patch affects only `builtin/checkout.c`, modifying the merge logic in `merge_working_tree()`. The commit message notes this is part of the broader effort to eliminate merge-recursive, but doesn't provide additional rationale since the series cover letter explains the overall motivation.

Key technical details for future reference:
- Replaces `#include "merge-recursive.h"` with `#include "merge-ort-wrappers.h"`
- Swaps `merge_trees()` call for `merge_ort_nonrecursive()` with identical parameters
- Maintains same merge options structure (`struct merge_options o`)
- Preserves existing error handling and return value behavior
- Part of systematic migration to remove merge-recursive backend
- No functional changes intended - purely a backend switch
- Sets up for subsequent patches that will remove merge-recursive.[ch]

The patch appears well-scoped and low-risk, being one of the simpler conversions in the series. As the first step, it establishes the pattern for subsequent patches that will update other callers.