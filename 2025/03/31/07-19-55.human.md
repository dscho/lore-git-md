Feature enhancement with implementation concerns. Patrick Steinhardt responds to Junio Hamano's feedback on Justin Tobler's patch to show SHA implementations in `git version --build-options` output.

The thread has established that the feature is desirable but the current approach has maintainability issues. Junio raised concerns about the if/elif cascade structure potentially getting out of sync with other parts of the code and suggested using independent #if blocks instead. Patrick now proposes an alternative approach where each SHA implementation would define its own `SHA1_BACKEND` symbol, which would both enforce implementation consistency and make the code more maintainable.

This moves the discussion forward by offering a concrete technical solution to Junio's architectural concerns. The proposal would require implementations to explicitly declare themselves, making the build configuration more robust. The email doesn't address the ongoing naming discussion around "unsafe" SHA-1 variants, focusing instead on the underlying implementation mechanics.

The patch series now has two distinct but related challenges to resolve: the technical implementation approach (with Patrick's new suggestion adding a third option to consider) and the user-facing naming conventions for SHA variants. Both will need to be addressed in the next revision.