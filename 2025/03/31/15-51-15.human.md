This is patch 4/8 in Elijah Newren's series to fully remove the legacy merge-recursive implementation in favor of merge-ort. The patch focuses on converting the sequencer's non-recursive merge operations to exclusively use merge-ort.

The change removes the `do_recursive_merge()` function's ability to fall back to the old merge-recursive backend, which has been unnecessary since merge-ort became the default long ago. The patch simplifies the code by eliminating the conditional logic that previously supported both backends, leaving only the merge-ort path. This is a straightforward mechanical change that aligns with the series' goal of completely removing merge-recursive dependencies.

The patch maintains an existing TODO comment about potential optimizations for when to update the working tree during rebase operations, indicating this is purely a backend switch without functional changes to the sequencer's behavior. The diff shows a net reduction of 22 lines of code while keeping the core merge logic intact.

This change is part of the broader, well-planned transition that has been building over several years, with merge-ort having proven itself as the superior merge strategy. The patch appears clean and focused, with no obvious issues in its implementation.