This is part 12 of Patrick Steinhardt's ongoing reftable API refactoring series, introducing a generic iterator for reftable records. The patch exposes this iterator through the public interface, building on previous work that standardized block-level operations.

The new iterator allows fine-grained traversal of individual blocks and their records, which will be crucial for implementing future reftable consistency checks. The implementation wraps existing block iteration functionality in a reusable interface (via `reftable_iterator_vtable`) while maintaining the same behavior. Tests demonstrate the iterator works correctly for seeking, next-record iteration, and edge cases like non-existent keys.

This continues the series' pattern of mechanical groundwork that enables future API exposure while keeping clear boundaries between interfaces. The changes are well-contained (56 lines added across 4 files) with thorough test coverage (78 new test lines). As with previous patches in the series, this appears to be a solid technical step toward the larger goal of making reftable internals more accessible for verification and external use.