Security configuration follow-up for git-daemon ownership verification. This is a response in the ongoing thread about configuring git-daemon safely after CVE-2022-24765's ownership verification changes.

Konstantin Ryabitsev suggests trying a simpler wildcard pattern (`directory = *`) for Git 2.43.0, noting that version only supported the basic wildcard syntax. This addresses MegaBrutal's report that path-specific wildcards (`/srv/git/*`) weren't working as expected in the git-daemon context. The response implies the wildcard handling may have been improved in later Git versions, though it doesn't address the separate issue about current-directory ('.') errors that MegaBrutal reported.

The exchange highlights version-specific behavior in Git's security configuration parsing, particularly around wildcard expansion rules. This adds to the thread's growing understanding of differences between direct Git usage and git-daemon-mediated access when processing the `safe.directory` configuration. The suggestion maintains the thread's focus on practical deployment solutions while revealing another implementation detail - that wildcard support evolved after 2.43.0.