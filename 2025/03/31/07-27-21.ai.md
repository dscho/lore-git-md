Test infrastructure fix with follow-up review. This is Patrick Steinhardt's positive review of Philippe Blain's patch fixing the scalar performance test to work in detached HEAD state. The patch had already been discussed in the thread and this email represents its acceptance.

The fix addresses a specific edge case in the p9210-scalar.sh performance test where running from a detached HEAD state would cause failures. The solution creates a local 'test-branch' during setup using `git checkout -B` to ensure consistent behavior regardless of repository state. The change is minimal (3 lines modified) and maintains existing test logic while improving reliability.

Steinhardt explicitly approves the change as "an easy and pragmatic fix", indicating this part of the series is ready to proceed. This contrasts with the ongoing discussion about prerequisite handling in the other patch in the series (p7821-grep-engines-fixed.sh), showing the scalar test fix is uncontroversial and technically sound. The review confirms the solution properly handles the test's requirements without introducing new complexity.