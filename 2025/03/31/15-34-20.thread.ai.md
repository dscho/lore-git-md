Given this is the first email in the thread (thread root), here's the AI mode summary:

Initial proposal for `--must-filter` option in `git fetch`/`git clone` from John Giorshev. The email appears to be a test message without full technical details, but indicates an intent to add new filtering capability to these commands. Core questions to resolve in future messages: specific filtering behavior being proposed, differentiation from existing `--filter` functionality, performance characteristics, and backward compatibility considerations. The thread identity is a feature proposal touching the fetch/clone machinery, with the key technical detail being modifications to object transfer filtering logic. No implementation, reviews, or related work mentioned yet due to the test nature of the initial email.