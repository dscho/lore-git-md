This is a follow-up discussion in the ongoing thread about displaying SHA implementation details in `git version --build-options`. The conversation has narrowed to finalizing the naming and warning messages for SHA-1 variants, particularly when collision detection is disabled.

Justin Tobler responds to Christian Couder's suggestions about naming the "unsafe-SHA-1" variant, proposing "non-crypto-SHA-1" as a less alarming alternative while keeping the base "SHA-1" label unchanged. He also agrees to add explicit warnings when non-collision-detection algorithms are used for cryptographic purposes, following earlier thread consensus to use the internal symbol names rather than inventing new display names.

The exchange shows the thread moving from architectural decisions to final UX polish, with just a few remaining details to settle about warning messages and documentation before the feature can be finalized. The technical approach itself (using backend-defined symbols) appears settled from earlier discussion.