Refactoring of the reftable block reader API, part of Patrick Steinhardt's ongoing reftable polishing series. This patch continues the naming standardization work by renaming `struct block_reader` to `struct reftable_block` to better reflect its role as the primary data structure for accessing reftable block contents. The change follows the established pattern of prefixing public-facing types with `reftable_`.

Key technical changes:
- Renames all `block_reader_*` functions to `reftable_block_*` (e.g. `block_reader_init` → `reftable_block_init`)
- Updates function signatures and call sites across 7 files (block.c, block.h, iter.c, iter.h, table.c, table.h, and unit tests)
- Maintains the same functionality while clarifying the API boundary
- Prepares for future exposure of this structure to external reftable users

The patch is mechanically straightforward, updating names consistently throughout the codebase. It builds on previous patches in the series that freed up the `reftable_block` name and established naming conventions. The changes are well-contained to the block reading subsystem, with no behavioral modifications. Test coverage remains thorough via the existing reftable test suite.

This continues the series' progression from mechanical groundwork toward substantive API improvements, with clear motivation stated for the naming standardization needed for external API usability. The patch appears carefully implemented with no obvious issues in the diff.