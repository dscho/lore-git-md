This thread introduces a 5-part patch series by D. Ben Knoble that integrates Git's performance benchmark infrastructure (`t/perf`) with the Meson build system while maintaining compatibility with the existing `t/perf/run` script. The series addresses several technical challenges including repository format compatibility, Perl path portability, out-of-tree build support, and benchmark configuration wiring.

The final patch completes the series by adding Meson options to control benchmark execution parameters (`benchmark_repo`, `benchmark_large_repo`, `benchmark_repeat_count`), properly handling their conversion to `GIT-BUILD-OPTIONS`. The patch explicitly maintains the separation between Meson-specific configuration and the traditional `t/perf/run` interface, noting that some make-specific variables remain exclusive to the legacy system.

Throughout the series, Knoble demonstrates careful attention to cross-platform compatibility (Windows path handling via `cygpath`, NixOS Perl path resolution) and maintains backward compatibility while introducing the Meson integration. The changes are minimal but impactful, with most patches modifying just 3-9 lines of code while solving specific technical problems. The work aligns with Git's broader Meson adoption while improving the benchmark infrastructure for all build systems. As of this email, the series has not yet received review feedback.