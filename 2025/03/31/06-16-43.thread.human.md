This thread has been tracking Patrick Steinhardt's work to integrate Git's performance benchmark infrastructure (`t/perf`) with the Meson build system. The v4 patch series is now complete, having implemented all the technical components needed for full integration while maintaining compatibility with existing benchmark capabilities.

The implementation handles several key aspects: repository format preservation (supporting SHA256 and reftable), Perl script portability, out-of-tree build support, and now explicit benchmark wiring. The final patch adds user-facing controls for benchmark execution parameters - allowing specification of test repositories (both Git-sized and Linux-sized) and setting the number of test repetitions (defaulting to 3). These configuration options will be written to GIT-BUILD-OPTIONS.

Junio Hamano had previously approved the feature option approach, and the implementation now matches that vision. The series maintains GNU `time` requirements to be consistent with existing test infrastructure, while properly handling Perl dependencies and Windows path conversion where needed. All 47 benchmark tests are now wired up through Meson's benchmark() function.

With this final patch, the technical implementation is complete. The only remaining items are documentation updates for the new options, along with resolution of some CI issues (a Linux Meson job failure and Windows Python asyncio crashes in an upstream Meson PR). The series appears ready for final review and merging once these last items are addressed.