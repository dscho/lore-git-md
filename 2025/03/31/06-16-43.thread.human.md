This thread has been working through the integration of Git's performance benchmark infrastructure (`t/perf`) with the Meson build system. Patrick Steinhardt's v3 series has addressed all major technical concerns, with the implementation now fully functional - supporting all 47 benchmark tests, SHA256/reftable, out-of-tree builds, and maintaining consistency with the existing `test-lib.sh` behavior.

The final discussion point centers on error handling when the `time` command is missing. Christian Couder advocates for explicit error messaging (following the precedent of `msgfmt` handling in internationalization setup), while Patrick Steinhardt prefers maintaining Meson's silent "No tests defined" behavior by default to avoid friction for developers who don't run benchmarks. Patrick has proposed a compromise: using Meson's feature options to make this configurable - silently disabling benchmarks when `time` is missing by default, but requiring it and erroring out when users explicitly enable benchmarks via `-Dbenchmarks=enabled`.

With all other technical aspects resolved (including PERL_PATH handling, path resolution, and CI integration), this UX decision appears to be the last remaining item before the series can be finalized. Multiple reviewers including Toon Claes have approved the approach, suggesting the series is otherwise ready for integration. The discussion now awaits either consensus on the error handling behavior or a maintainer decision to break the tie.