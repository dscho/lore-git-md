This is a final polish to the `git blame` porcelain output fix, addressing a test optimization suggestion in the now-complete thread.

Karthik Nayak responds to Patrick Steinhardt's suggestion about combining `sed` operations in the test script. The original test used two separate `sed` calls to modify the blame output, while Patrick pointed out this could be done more cleanly with a single `sed` command using chained `-e` options. Karthik acknowledges this makes the test cleaner and mentions having initially tried `-i` (in-place editing) which didn't work portably across platforms.

This minor test improvement comes after extensive discussion and refinement of the feature that adds "unblamable"/"ignored" markers to `git blame`'s porcelain output. The thread has already resolved all major technical and implementation issues, with this being one of the final polish items before the change is ready to merge. The test change doesn't affect functionality but makes the test script slightly more efficient and readable.