This is a final test portability fix for the `git blame` porcelain output patch series. Phillip Wood points out that using `\n` in `sed` replacement text isn't portable according to POSIX standards, and suggests a more robust alternative for inserting new lines in the test expectations. The suggested fix uses `sed`'s append command with proper escaping to insert the "unblamable" and "ignored" marker lines in the test output. This addresses the last remaining technical issue in the series, which has already gone through extensive review and iteration to properly mark ignored/unblamable lines in `--porcelain` output while maintaining backward compatibility.

The thread context shows this is the final polish on a well-vetted solution that:
- Adds metadata lines for ignored/unblamable commits in porcelain mode
- Maintains strict SHA-1 line format
- Uses `--line-porcelain` specifically for this feature
- Has comprehensive test coverage
- Now includes fully portable test implementation

Key technical details for future reference:
- Test fixes use POSIX-compliant `sed -e "/pattern/a\\" -e text` syntax
- Maintains same test verification via `test_cmp` of preprocessed output
- Still verifies both `--porcelain` and `--line-porcelain` modes
- Builds on existing `blame.markIgnoredLines`/`blame.markUnblamableLines` config
- Core implementation in `builtin/blame.c` via `emit_one_suspect_detail()`
- Final output formatting uses `fputs()` to avoid newline duplication
- All design constraints and review feedback addressed

The series appears ready for merge with this last portability concern resolved. Phillip Wood's suggestion follows the thread's pattern of thorough attention to edge cases and standards compliance.