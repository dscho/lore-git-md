Bugfix follow-up for `git blame` porcelain output formatting.

This is a small but important correction to the final implementation of the porcelain output format for marking ignored/unblamable lines in `git blame`. Phillip Wood points out that the test script's `sed` pattern needs to use a literal tab character rather than the `\t` escape sequence, despite normal backslash escapes being allowed in `sed` patterns. This follows his earlier identification of portability issues in the test implementation.

The email serves as a final polish to ensure the test cases are fully POSIX-compliant, addressing the last remaining technical detail in this long-running thread. With this correction, all identified issues with the porcelain output format and its test coverage have been resolved, including proper newline handling, output formatting, and now complete portability of the test scripts. The solution maintains backward compatibility while properly marking ignored/unblamable lines in porcelain output.

Key technical details for future reference:
- Test script uses literal tabs in `sed` patterns for portability
- Builds on `emit_porcelain_per_line_details()` in `builtin/blame.c`
- Maintains strict SHA-1 line format requirements
- Finalized via `t/t8013-blame-ignore-revs.sh` test updates
- All design and implementation issues now addressed

This represents the last piece of feedback in the thread, confirming the solution is ready for merge with all optimizations including test script portability. No further changes are expected.