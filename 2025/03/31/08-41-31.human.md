Refactoring patch in Patrick Steinhardt's reftable API series, exposing internal table structure. This is patch 13/16 in a series that's standardizing and cleaning up the reftable API for better external use and future verification features.

The change moves the `reftable_table` struct definition from an internal header to the public `reftable-table.h` interface. This gives callers direct access to the table metadata, which will be needed for upcoming consistency checking functionality. The struct contains various table properties like block sizes, hash types, index ranges, and offset information for different record types (refs, objects, logs).

The patch is a straightforward mechanical relocation of the struct definition with no behavioral changes. It follows the series' pattern of gradually exposing more internals while maintaining clear API separation. The commit message clearly states the motivation - enabling future consistency checks - and the change appears well-contained, touching only two header files.

This continues the series' steady progress toward its dual goals of better external usability and lower-level access for verification. The change seems uncontroversial and well-motivated, though reviewers may want to confirm the exact set of fields being exposed is appropriate for public consumption.