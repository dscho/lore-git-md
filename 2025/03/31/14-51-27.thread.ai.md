Git metadata command proposal (GSOC 2025 project) - Jayatheerth K's evolving proposal for migrating repository query functionality from `git rev-parse` to a new command, now renamed from `git metadata` to `git repo-info` based on mentor feedback. The scope remains focused on relocating the "Options for Files" section from git-rev-list(1), including path queries (`--show-toplevel`), directory queries (`--git-dir`), and state queries (`--is-bare-repository`), while maintaining text output compatibility with rev-parse's current behavior.

The technical implementation plan involves creating `builtin/repo-info.c` to house this functionality, reusing existing logic from rev-parse.c while adding comprehensive tests and documentation. Backward compatibility for scripts is a key consideration, along with proper repository context handling and thorough test coverage across different repository states (bare repos, submodules). JSON output remains a stretch goal but is explicitly secondary to the core refactoring work.

Jayatheerth has structured the 12-week timeline into logical phases, with each group of options to be submitted as separate patch series following Git's incremental contribution workflow. The proposal demonstrates prior engagement through completed microprojects and mailing list participation, with links to previous patches and discussions. Mentor guidance from Patrick Steinhardt has been incorporated throughout, particularly regarding scope constraints and patch submission methodology. Next steps involve community review of this refined proposal and subsequent implementation work.