Patch 1/8 in Elijah Newren's series to remove the legacy merge-recursive implementation. This is a mechanical change switching one of the last remaining callers from merge-recursive to merge-ort.

The patch updates `builtin/checkout.c` to use `merge_ort_nonrecursive()` instead of the old `merge_trees()` function from merge-recursive. The change is straightforward - just swapping out the include and function call while keeping all other parameters and behavior the same. The commit message notes this is part of the broader effort to eliminate merge-recursive entirely now that merge-ort has fully replaced its functionality.

This appears to be a well-executed change - the diff is clean, the commit message is clear about the mechanical nature of the change, and it fits logically as the first step in the series to transition all remaining callers before removing merge-recursive completely. As the first patch in the series, it sets up the pattern we'll likely see in subsequent patches for other callers.