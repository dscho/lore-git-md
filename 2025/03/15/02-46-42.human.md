Build system cleanup patch in a series about runtime prefix handling. This is patch 2/12 from Ramsay Jones that reorganizes Makefile conditional compilation flags related to executable path detection. The change makes several `BASIC_CFLAGS` definitions conditional on `RUNTIME_PREFIX` being set, since they're only relevant in that context (including platform-specific path lookup mechanisms like BSD process sysctls, z/OS executable paths, and Windows path globals).

The patch is straightforward - it moves five existing flag definitions inside a new `RUNTIME_PREFIX` conditional block and slightly refactors the `PROCFS_EXECUTABLE_PATH` handling (renaming the temporary variable for conciseness). This appears to be a logical cleanup rather than a functional change, part of a larger effort to better organize build-time configuration. The commit message clearly explains which flags are affected and why the change makes sense.