Here's a human-readable thread summary:

The thread discusses a bug in Git's path-filtered log behavior when dealing with subtree merges. Oswald Buddenhagen reported that running `git log .` in the git-gui subdirectory shows primarily merge commits rather than the expected history of changes being merged. This makes it difficult to trace the actual development history of subprojects or directories managed via subtree merging.

The issue appears similar to Git's known limitation where `--follow` only works for single files, suggesting there may be underlying constraints in how Git handles path filtering during revision walking. The problem is particularly noticeable in git-gui's history and is exacerbated when viewing the log in gitk.

While no specific solution has been proposed yet, the report highlights a genuine usability pain point for developers working with subtree-merged codebases. The behavior seems to stem from Git's current approach to path filtering during log operations, which may require non-trivial changes to properly support subtree merge scenarios. The discussion is currently focused on documenting and understanding the issue rather than proposing fixes.