Here's a human-readable summary of the thread's current state:

This thread discusses how Git selects cryptographic random number generation methods during builds, particularly on Linux systems. The original patch proposed documenting that Linux users building with `make` could manually set `CSPRNG_METHOD=arc4random` to use glibc's implementation, similar to other version-dependent features. 

A key technical clarification has now emerged: while BSD systems benefit from using `arc4random` (which provides fork-safety through a userspace ChaCha20 implementation), Linux's glibc version is just a thin wrapper around the kernel's `getrandom` system call. This means Linux gains no performance or security advantage from using `arc4random` - it actually adds overhead. The discussion reveals that all supported Linux distributions already have glibc ≥2.25 with `getrandom` available, suggesting Git might want to default to `getrandom` for Linux builds rather than documenting `arc4random` as an optimization.

The conversation has shifted from a straightforward documentation update to a more nuanced technical discussion about implementation differences between platforms and whether the build system should handle Linux's case differently from BSD. This may require revisiting the default configuration rather than just documenting the current behavior.