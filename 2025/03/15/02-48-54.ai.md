Platform compatibility patch for Cygwin, part of a 12-patch series (position 10/12). The patch enables `sysinfo()` functionality on Cygwin by modifying build configuration files.

The change recognizes that while `sysinfo()` is traditionally considered Linux-specific, Cygwin provides a working implementation. The patch makes three coordinated changes: (1) moves the `HAVE_SYSINFO` definition from a direct `BASIC_CFLAGS` setting to a proper build variable in `config.mak.uname`, (2) adds Cygwin to the platforms that set this variable, and (3) introduces an autoconf test for `sysinfo()` availability in `configure.ac`. The Makefile is updated to consistently use the `HAVE_SYSINFO` variable when setting compiler flags.

This appears to be a straightforward technical improvement to better support Cygwin builds. The changes follow Git's established patterns for platform-specific configuration, and the patch includes all necessary coordinated changes across the build system components. The commit message clearly explains the change and its motivation. Since this is part of a larger series, the full impact may depend on how other patches in the series interact with this platform capability.