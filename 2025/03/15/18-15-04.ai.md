Final part (3/3) of the GSOC documentation series modernizing the MyFirstContribution tutorial, focusing on updating config API usage to match current `the_repository` removal effort. This patch completes the code example modernization by replacing `git_config()` calls with `repo_config()` variants throughout the tutorial. The changes demonstrate proper repository-aware configuration handling, including:

1. Adding `repository.h` include for the example
2. Converting `git_config()` to `repo_config()` with explicit `repo` parameter
3. Updating `git_config_get_string_tmp()` to its repo-aware counterpart
4. Modernizing the `wt_status_prepare()` example to use passed `repo` instead of `the_repository`
5. Minor formatting cleanup in the generated files listing

The patch maintains the tutorial's pedagogical flow while bringing it in line with current Git practices. It builds on part 2's introduction of the `repo` parameter and completes the transition away from deprecated global state access patterns. The changes are focused and mechanical, with no functional alterations to the example command's behavior.

Key technical details for future reference:
- Introduces `repo_config()` and `repo_config_get_string_tmp()` API usage
- Shows proper `struct repository *` parameter passing through call chain
- Updates references from `api-config.adoc` to current `git-config.adoc`
- Preserves all existing tutorial explanations while modernizing the code
- Maintains backward compatibility in the example's output behavior

This concludes the series' technical modernization effort, having addressed prerequisites, parameter handling, and now config API usage. The changes appear uncontroversial and well-aligned with the project's direction, though reviewers may want to verify the pedagogical clarity of the new config API explanations.