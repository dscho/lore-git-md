Build system optimization series (12 patches) modifying meson configuration to avoid redundant compiler definitions, currently at patch 3/12. The series restructures meson.build to conditionally include compiler definitions for DEFAULT_EDITOR, DEFAULT_HELP_FORMAT and DEFAULT_PAGER only when meson options differ from built-in defaults ('vi', 'man', 'less'). Patrick Steinhardt raised concerns about the approach increasing hardcoded assumptions without changing runtime behavior, proposing instead to eliminate default values from meson_options.txt entirely and use empty string checks. Ramsay Jones responded with additional context: the changes align with original intent to keep defaults in code while allowing platform-specific overrides (e.g., Windows HTML help format), and highlighted documentation implications where make passes git-default-pager/editor to asciidoc only when values differ from standard. Jones noted current gaps in meson.build's documentation handling and that this patch helps identify build system differences between make and meson, particularly around default handling in documentation generation (referencing specific lines in Documentation/Makefile and config.mak.uname). The discussion reveals this is part of a larger investigation into build system consistency, with more patches planned to address documentation handling. Technical focus remains on DEFAULT_HELP_FORMAT override for Windows, Makefile variable exports (line 2923), and documentation build integration (lines 239-247). The approach is acknowledged as potentially intermediate while exploring meson best practices, with openness to alternative solutions that maintain the series' goal of optimizing compiler definitions while preserving behavior across build systems.