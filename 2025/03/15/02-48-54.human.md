Platform compatibility patch for Cygwin's sysinfo()` support.

This is patch 10/12 in a series (though no cover letter context is provided). It enables Git to use the `sysinfo()` system call on Cygwin, which was previously assumed to be Linux-only. The patch makes three coordinated changes:

1. Moves the `HAVE_SYSINFO` definition from a direct `-D` flag to a proper Makefile variable in `config.mak.uname`, matching how other similar features are handled. This makes the Linux configuration cleaner while allowing the flag to be reused.

2. Adds explicit `sysinfo()` support for Cygwin, which provides a working implementation despite not being Linux. The change is straightforward - just adding the same `HAVE_SYSINFO = YesPlease` line to the Cygwin section.

3. Introduces an autoconf test for `sysinfo()` availability, making the build more robust by properly detecting the function rather than hardcoding assumptions.

The patch appears well-constructed - it follows existing patterns, touches all the right places in the build system, and includes the autoconf test for future portability. While not a user-visible change, it improves Git's build correctness on Cygwin and makes the Linux configuration more maintainable.