Build system alignment series (now 14 patches in v3) focusing on Makefile/meson consistency and platform-specific cleanup, with completed cryptographic random number generation (CSPRNG) standardization. Core technical work spans: conditional `BASIC_CFLAGS`, platform-specific fixes (Cygwin regex, timing functions, arc4random), documentation updates, and meson path handling limitations. Key developments include:

- Expanded Cygwin support: `sysinfo()` implementation with RAM calculation fix in `git gc` (handling `mem_unit` field), arc4random enablement, timing function additions (`clock_gettime`), and `getdelim` support
- Platform standardization: Linux CSPRNG to `getrandom`, meson help format defaults aligned with Makefile (Windows: HTML, others: man)
- Documentation of meson's path handling limitations for DEFAULT_EDITOR/DEFAULT_PAGER (Ramsay Jones' investigation showing quote/backslash escaping issues, GitHub issue #1564)
- Regex compatibility: NO_REGEX conditional on Cygwin 1.7 with CYGWIN-specific skip for test t7815.12 (NUL character matching failure)

New technical specifics in v3:
- Removal of redundant `-DCURL_DISABLE_TYPECHECK` from meson build's `libgit_c_args` (patch 1/13) - flag was originally for sparse builds which meson doesn't support, affecting curl-related files (http-push.c, http.c, http-walker.c, remote-curl.c)
- `BASIC_CFLAGS` adjustments for `RUNTIME_PREFIX` handling (new patch 14/14) making five platform-specific flags conditional on `RUNTIME_PREFIX` definition (`HAVE_BSD_KERN_PROC_SYSCTL`, `PROCFS_EXECUTABLE_PATH`, `HAVE_NS_GET_EXECUTABLE_PATH`, `HAVE_ZOS_GET_EXECUTABLE_PATH`, `HAVE_WPGMPTR`), maintaining exact behavior while improving logical grouping
- Detailed build comparison metrics via custom `syms.pl` script showing Makefile/meson symbol parity progress
- Successful test runs on Linux (Cygwin testing ongoing, 6+ hour runtime)
- Minor whitespace fixes in build variable assignments and commit message refinements

Open platform compatibility considerations:
- Maintainer consensus forming around dropping pre-v2.x Cygwin support, with Junio Hamano agreeing in principle but deferring to Adam Dinwoodie (Cygwin maintainer) for final decision
- Two practical approaches remain: (1) maintain untested support with caveats, or (2) explicitly drop support for older versions
- Ramsay Jones' prepared patch to remove all pre-v2.x Cygwin support still pending
- Project's trend toward aggressive pruning of ancient OS versions continues

Open technical questions:
- Final decision needed on Cygwin version support cutoff (pre-v2.x) for regex implementation
- Meson's quoted path handling limitation remains unresolved (potential upstream meson `.quoted()` method solution)
- Status of 'info'/'pdf' help format targets pending user feedback
- Whether to maintain `mem_unit > 1` guard clause in `total_ram()`
- Resolution of CYGWIN-specific exclusion for t7815.12 NUL character matching test

The series continues systematic build system alignment with particular focus on Cygwin compatibility tradeoffs. Maintainer consensus remains around modernization while respecting ecosystem input, with Junio Hamano, Patrick Steinhardt, and Ramsay Jones aligned on reducing support burden but awaiting Cygwin maintainer guidance. Technical solutions are in place for current Cygwin versions while the community evaluates long-term support boundaries. The new `RUNTIME_PREFIX` flag restructuring cleanly organizes related build configurations without behavioral changes, furthering the series' goal of build system rationalization.