Build system alignment series (now 20 patches in v2) focusing on Makefile/meson consistency and platform-specific cleanup, with completed cryptographic random number generation (CSPRNG) standardization across platforms. Core technical work spans: conditional `BASIC_CFLAGS` based on `RUNTIME_PREFIX`, platform-specific fixes (Cygwin regex, `getdelim`, timing functions, `sysinfo`, arc4random; Linux `NEEDS_LIBRT` removal), documentation updates, meson path handling limitations, and help format defaults.

Key technical developments:
- Expanded `sysinfo()` support now includes Cygwin implementation (patch 20) with proper `config.mak.uname` configuration, aligning with Ramsay Jones' earlier work and confirming meson already handles this case correctly (Patrick Steinhardt review)
- RAM calculation fix for Cygwin's `sysinfo()` (handling `mem_unit` multiplication) in `git gc` auto-detection logic, with Ramsay Jones test results and Patrick Steinhardt review confirming platform behavior differences (Cygwin reports in 4K pages vs Linux bytes)
- arc4random enablement for Cygwin
- Linux CSPRNG method standardization to `getrandom`
- Meson path handling limitation for editor/pager defaults (detailed investigation by Ramsay Jones showing meson's inability to properly escape quotes/backslashes in DEFAULT_EDITOR/DEFAULT_PAGER, with GitHub issue #1564 confirming this as a known meson limitation)
- New help format default alignment (Windows: HTML, others: man)
- Conditional `BASIC_CFLAGS` for `RUNTIME_PREFIX` contexts
- Deliberate omission of 'info' and 'pdf' help format targets from meson builds (Patrick Steinhardt rationale: lack of observed usage in distributions)
- Cygwin timing function support (`clock_gettime`, `clock_monotonic`) with formatting refinement (Patrick Steinhardt suggestion to add spaces around `=` in `config.mak.uname` now acknowledged by Ramsay Jones for potential v3)

Platform compatibility updates:
- Enabled `sysinfo()` for Cygwin through coordinated `config.mak.uname` changes (new patch 20)
- Added Cygwin arc4random support via `CSPRNG_METHOD = arc4random`
- Standardized Linux CSPRNG to `getrandom` in Makefile builds
- Adjusted meson help format defaults to match Makefile behavior
- Made `HAVE_BSD_KERN_PROC_SYSCTL` and `PROCFS_EXECUTABLE_PATH` conditional on `RUNTIME_PREFIX`
- Maintained Makefile 'info'/'pdf' targets while excluding from meson pending user feedback
- Added Cygwin timing functions with consistent formatting (`HAVE_CLOCK_GETTIME = YesPlease` style) - Ramsay Jones confirms intent to fix spacing if series re-rolled
- Fixed Cygwin RAM calculation in `git gc` by properly handling `mem_unit` field (4K pages vs bytes)
- Cygwin regex compatibility discussion: Ramsay Jones indicates readiness to drop support for pre-v2.x Cygwin versions (over 10 years old), noting modern Cygwin (v2.0+) has improved regex that passes tests except for NUL character matching in t7815.12 requiring platform-specific skip. Suggests complete removal of pre-v2.x support would be reasonable given Cygwin's rolling release model, though defers final decision to maintainers.

Technical details:
- Build system differences reduced via `syms.pl` script analysis
- Test suite passes on Linux (excluding some optional tests) with full Cygwin compatibility
- RAM fix maintains backward compatibility (handles both Cygwin's `mem_unit=4096` and Linux's `mem_unit=1`)
- New documentation clarifies `NO_STRLCPY` behavior on Linux systems with glibc ≥2.38
- Platform-specific regex handling now under review (Cygwin 1.7 vs modern)
- Removal of obsolete `NEEDS_LIBRT` variable
- Autoconf additions provide robust detection mechanisms
- CSPRNG changes: Cygwin uses arc4random (userspace ChaCha20), Linux uses getrandom (kernel syscall)
- Meson format defaults: now matches Makefile's platform-specific behavior
- Meson path handling: confirmed limitation in escaping quotes/backslashes in DEFAULT_EDITOR/DEFAULT_PAGER (Ramsay Jones investigation showing failure cases for single/double quotes and backslashes, with meson's automatic quote-wrapping behavior complicating manual escaping attempts)
- Help format divergence: meson intentionally omits 'info'/'pdf' targets based on Patrick Steinhardt's assessment of low usage
- Cygwin timing support: added with minor formatting adjustment to match project style (spacing around `=` operator to be corrected per Patrick Steinhardt's review)
- `git gc` RAM calculation: modified `total_ram()` to multiply by `mem_unit` when >1, addressing Cygwin's 4K page reporting
- `sysinfo()` detection: now properly configured for Cygwin in Makefile while meson already handles it correctly (new patch 20)
- Regex implementation: modern Cygwin (v2.0+) passes tests except t7815.12 NUL character matching requiring platform-specific skip; pre-v2.x support could be dropped per Ramsay Jones' suggestion

Open questions:
- Resolution path for meson's quoted path handling limitation (now documented as GitHub issue #1564) remains unclear, with Ramsay Jones confirming extensive but unsuccessful attempts to replicate Makefile's escaping behavior
- Whether to consider meson path handling a blocker or document as a known limitation (current patch represents improvement despite unresolved issue)
- Potential need for more granular platform detection in meson
- Whether to maintain or deprecate 'info'/'pdf' help format targets based on user feedback
- Decision needed on Cygwin regex compatibility: maintain 1.7 support (current patch makes NO_REGEX conditional) or follow Ramsay Jones' suggestion to drop pre-v2.x support entirely given Cygwin's rolling release model
- Whether to add CYGWIN-specific exclusion for t7815.12 NUL character matching test
- Whether to remove the `mem_unit > 1` guard clause in `total_ram()` as suggested by Patrick Steinhardt (since all systems should have proper `mem_unit` values)

The series continues systematic progress in aligning build system behavior while expanding platform capabilities, with Cygwin compatibility as a focus. The discussion now includes Ramsay Jones' acknowledgment of Patrick Steinhardt's style nitpick regarding timing function configuration formatting, adding to the existing considerations around meson path handling limitations, help format divergence, and Cygwin regex support. The growing set of platform-specific fixes demonstrates careful attention to cross-platform consistency while maintaining the series' architectural focus on build system alignment.