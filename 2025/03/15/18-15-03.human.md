Documentation patch (part 2/3) in a series modernizing Git's contributor tutorial, specifically updating the example command implementation to reflect current best practices.

This patch updates the tutorial's example `cmd_psuh` function to include a `struct repository` parameter and demonstrates proper use of the `UNUSED` macro, bringing the example in line with modern Git code conventions. The changes affect both the function signature shown in the tutorial and the corresponding declaration in `builtin.h`. The patch also adds missing context about registering the command with `RUN_SETUP` and includes more complete code snippets showing function bodies.

The changes are straightforward and appear well-executed, with all modifications focused on the tutorial's example code rather than actual Git implementation. This continues the series' goal of making the contributor documentation more accurate and up-to-date. The patch maintains good attention to detail by showing both the parameter additions and the `UNUSED` macro usage in multiple contexts throughout the tutorial.

As the second in a three-part series, this builds on the prerequisite updates from part 1 while presumably leaving room for additional modernization in part 3. The changes are non-controversial but important for new contributors to see current coding patterns rather than outdated examples.