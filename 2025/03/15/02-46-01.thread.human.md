This thread continues the build system alignment series, now in its third iteration with 20 patches focused on Makefile/meson consistency and platform-specific cleanup. The latest patch completes the cryptographic random number generation (CSPRNG) standardization by setting `CSPRNG_METHOD = getrandom` for Linux builds, bringing the Makefile behavior in line with meson's preference for kernel-based random number generation. 

The series has seen significant progress on Cygwin support, including fixes for RAM calculation via `sysinfo()`, arc4random enablement, and timing function additions. Other notable work includes platform standardization (Linux CSPRNG to getrandom, meson help format alignment), regex compatibility fixes for Cygwin, and removal of obsolete NEEDS_LIBRT support. The latest patches have refined DEFAULT_EDITOR/DEFAULT_PAGER handling and implemented platform-aware help format selection.

Key open questions remain around Cygwin version support cutoff and meson's path handling limitations. The project appears to be moving toward dropping pre-v2.x Cygwin support, pending input from Cygwin maintainers. The series continues to systematically align build systems while removing legacy code, with recent patches focusing on practical optimizations that maintain functionality while reducing complexity.