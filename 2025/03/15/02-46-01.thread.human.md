This build system alignment thread continues to refine platform compatibility, now focusing on Cygwin's `sysinfo()` support. The series, currently at v2 with 15 patches, systematically addresses inconsistencies between Makefile and meson builds while handling platform-specific quirks. The latest patch adds proper Cygwin support for `sysinfo()` by moving the detection into config variables and adding autoconf tests, maintaining Linux behavior while extending compatibility. Key achievements so far include fixing Cygwin's RAM calculation (where memory is reported in 4096-byte units), documenting `NO_STRLCPY` behavior on newer glibc systems, and removing obsolete variables like `NEEDS_LIBRT`. The work shows careful attention to both technical correctness and backward compatibility, with Ramsay Jones and D. Ben Knoble actively reviewing the platform-specific changes. The series represents foundational work that will enable more consistent behavior across Git's build systems and supported platforms.