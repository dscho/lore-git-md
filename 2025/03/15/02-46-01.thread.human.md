This thread covers a 16-patch series (now in its third iteration) focused on aligning Git's build systems (Makefile and meson) while cleaning up platform-specific code. The work standardizes cryptographic random number generation, improves Cygwin support, and removes obsolete compatibility code like NEEDS_LIBRT (no longer needed on modern Linux systems). 

Key developments include expanded Cygwin support with fixes for memory calculation in `git gc`, timing functions, and regex handling. The series also documents meson's path handling limitations and aligns help format defaults between build systems. A new patch in v3 specifically addresses regex compatibility, narrowing the `NO_REGEX` setting to only apply to problematic Cygwin 1.7 versions while noting that test t7815.12 still fails on Cygwin due to NUL character matching issues.

The maintainers (Junio Hamano, Patrick Steinhardt, and Ramsay Jones) are converging on modernizing platform support, with consensus forming around dropping pre-v2.x Cygwin versions. However, they're awaiting input from Cygwin maintainer Adam Dinwoodie before finalizing this decision. The series continues to refine build system behavior while maintaining all functionality, with recent patches optimizing default editor/pager handling and help format selection. Open questions remain about Cygwin version support boundaries and the unresolved NUL character matching test failure.