This is a documentation refinement in the signed commits fast-export/import series. The email contains a discussion about improving the warning text for the `--signed-tags` option's verbatim modes in the git-fast-export documentation.

Elijah Newren suggests strengthening the warning to clarify that signatures may become invalid not just from user transformations, but also from automatic canonicalizations during export/import (like permission normalization, tree sorting, and header handling). Christian Couder agrees with the suggestion and indicates it will be incorporated in the next version of the patch.

The change is part of the v5-v6 series implementing signed commit support, which has already received extensive review and is nearing finalization. This particular patch focuses on making the security implications of signature handling modes more precise in the documentation, specifically warning users that even without explicit changes, the export/import process itself may invalidate signatures in ways that aren't immediately obvious.

The thread context indicates this is one of the final polish items before the series is ready for inclusion, with all technical aspects already resolved and maintainer approvals secured. The documentation refinement aligns with the series' goal of providing clear guidance about signature handling safety.