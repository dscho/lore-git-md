This is a brief response in the ongoing discussion about enabling `-Wunreachable-code` warnings in Git's development builds. 

Junio Hamano responds to Jeff King's suggestion to enable the warning only in the linux-sha256 CI job for evaluation. He confirms this approach would avoid the macOS-specific issue with `sigfillset()` that was causing CI failures, while still allowing detection of the known packed backend issue (the only confirmed real problem found so far). 

The email shows the maintainer's cautious approval for gathering more data through limited CI testing, reflecting the thread's progression from initial implementation concerns to a more measured evaluation strategy. This maintains the technical focus on balancing warning utility against maintenance costs while avoiding immediate platform-specific complications. The response keeps open the key questions about long-term warning policy but moves toward concrete data collection.