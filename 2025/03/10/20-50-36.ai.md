This is a follow-up discussion in the ongoing thread about `--no-optional-locks` for `git describe` and `git diff`. Benjamin Woodruff provides real-world context about encountering lockfile issues when using `git describe --dirty` in a Rust-based build system, explaining their workaround and motivation for the original patch submission.

Key technical details from this email:
- Reveals concrete use case with `vergen-gitcl` crate calling `git describe --dirty` for cache invalidation
- Documents lockfile conflicts occurring in IDE/build environments (rust-analyzer LSP, killed builds)
- Shows implemented workaround using `git status` instead of `describe`
- Questions whether `git-status` documentation about background refresh being performance-only is accurate
- Notes libgit2 divergence in behavior (no background refresh by default)
- Proposes alternative path focusing on documentation improvements rather than new options:
  - Clarify `--no-optional-locks` scope
  - Document `describe --dirty` lock behavior
  - Potentially revise `status` background refresh docs
  - Leave `diff` docs unchanged

The email shifts the discussion toward documentation solutions rather than interface changes, potentially addressing Junio's concerns about permanent option baggage while still solving the discoverability issues that caused problems in Benjamin's use case. It surfaces new information about libgit2's different approach to index updates, which may be relevant to the porcelain/plumbing design discussion.