Maintainer follow-up on remote archive segfault investigation. Junio Hamano concurs with Patrick Steinhardt's assessment that the original patch may be treating symptoms rather than root cause. The discussion shifts from the immediate `--remote=""` validation to deeper architectural questions about `remote_get()` behavior and `run_remote_archiver()` error handling. Key technical points:

1. Confirms `remote_get()` should either handle empty strings gracefully or document its contract clearly
2. Identifies `run_remote_archiver()` as potentially unsafe when consuming `remote_get()` results
3. Suggests refactoring to move `remote_get()` responsibility to `cmd_archive()` and make `run_remote_archiver()` take a `struct remote*`

This moves the thread from surface-level validation to considering proper error propagation in the remote archive plumbing. The original patch's approach now appears insufficient, with maintainers pushing for either fixing `remote_get()`'s contract or restructuring the call chain. No resolution yet on which layer should own the validation.