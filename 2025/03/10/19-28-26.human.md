This is a refactoring patch that's part of Patrick Steinhardt's series to add NUL-delimited output to `git rev-list`. Justin Tobler contributes this preparatory change to clean up the object display code before the NUL-delimited format is implemented.

The patch inlines the single-use `show_object_with_name()` function into `show_object()` and consolidates the common OID printing logic. This simplification makes the code easier to modify in the next patch when adding NUL-delimited output support. The change touches three files but is straightforward - it eliminates a helper function while maintaining identical output behavior.

This appears to be a well-justified cleanup that follows Git's typical refactoring patterns. The commit message clearly explains the motivation and connects it to the larger series goal. The diff shows the change is focused and doesn't introduce any new complexity.