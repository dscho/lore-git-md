This is a small bugfix patch addressing sign comparison warnings in the decoration code.

The patch changes two `int` variables to `unsigned int` in decorate.c to match the unsigned types they're compared against, eliminating compiler warnings about signed/unsigned comparisons. The changes are straightforward: one variable tracks the size of a decoration hash table, and another counts entries in that table. Both are naturally unsigned quantities.

The patch also removes a `#define DISABLE_SIGN_COMPARE_WARNINGS` that was apparently suppressing these warnings, suggesting the author is cleaning up the code to properly handle the type safety rather than just silencing the warnings. The changes are minimal and focused - just two lines plus the warning suppression removal.

This appears to be a clean, focused fix from a GSoC participant (as noted in the subject). While small, it demonstrates good attention to type safety details and proper warning handling. The patch is likely non-controversial and could be merged quickly.