This is part of Patrick Steinhardt's ongoing series to eliminate `the_repository` global variable dependencies across Git's object APIs. Patch 2/12 focuses on converting fundamental object handling functions in object.c to explicitly take a repository parameter rather than implicitly using the global.

The changes modify several key object-related functions to accept a `struct repository*` parameter: `get_max_object_index()`, `get_indexed_object()`, `parse_object_or_die()`, and `clear_object_flags()`. The patch updates all existing callers to pass `the_repository` where no repository context is yet available, while preserving cases where callers already operate within a specific repository context.

The changes touch core object handling across multiple subsystems including fsck, grep, pack operations, revision walking, and shallow clone handling. Each conversion follows the pattern of adding the repository parameter to function signatures and passing it through to internal operations. The patch maintains backward compatibility while systematically removing implicit dependencies on the global repository state.

This represents a foundational change in the object subsystem's architecture, enabling future work toward repository independence. The modifications are mechanical but wide-ranging, affecting 15 files with 48 insertions and 44 deletions. The patch builds on established patterns from earlier parts of the series and maintains consistency with the overall goal of eliminating `the_repository` usage.