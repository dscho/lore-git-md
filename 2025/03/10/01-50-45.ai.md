This is a documentation and test maintenance patch in the path-walk delta compression series. It aligns the usage text in `builtin/pack-objects.c` with the documentation in `git-pack-objects.adoc`, then removes 'pack-objects' from the test exception list in `t0450/adoc-help-mismatches`. 

The changes are straightforward: the usage text in the C file is updated to match the more detailed synopsis from the documentation, including all the same options (with the new `--path-walk` flag added in this series). The test change confirms the alignment by removing pack-objects from the list of commands exempt from documentation-matching checks.

This patch is a minor cleanup that follows logically from the series' addition of the `--path-walk` option, ensuring all documentation remains consistent. The changes are mechanical and unlikely to be controversial, though reviewers may want to verify the exact formatting of the long usage lines in the C source matches their preferences. The patch maintains the series' focus on quality documentation alongside implementation changes.