Test modernization patch in the breaking changes infrastructure series.

This is the second patch in a 4-part series (though the thread context shows it was previously part of a 5-patch series) focused on establishing patterns for handling breaking changes in Git. The patch specifically addresses test hygiene in t6120-describe.sh by fixing instances where git commands were piped, which would hide their exit status. 

The changes split two test cases that previously used pipes (`git rev-list --all | git name-rev...`) into separate commands with intermediate files, ensuring any failures in the first command would be properly caught. The first test verifies `--annotate-stdin` behavior while the second checks the deprecated `--stdin` warning message. Both are related to the series' broader work around the `name-rev` command's evolution.

This appears to be straightforward test cleanup that improves reliability by properly checking command exit codes. The changes are mechanical and focused - just splitting pipes into separate steps while maintaining the same test assertions. As part of the larger breaking changes infrastructure work, this helps ensure the test suite properly validates both old and new behaviors as deprecated functionality is removed.