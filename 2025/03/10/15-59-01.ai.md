This is a follow-up discussion in the signed-commits feature series, addressing a memory leak issue identified in v5. Phillip Wood pointed out that while a spelling correction was made in the comments for `find_commit_multiline_header()`, the comment still incorrectly stating the returned string was statically allocated when it had been changed to require freeing. Christian Couder confirms the fix in v6 updates the comment to correctly indicate the caller's responsibility to free the memory and shows where the `free()` call is added in `handle_commit()`.

The exchange demonstrates the careful attention to memory management details in this series, with Christian providing the specific code locations where the signature memory is allocated and freed. This continues the thread's pattern of thorough technical review and responsive fixes, maintaining the high quality bar established in earlier rounds. The discussion is narrowly focused on this implementation detail rather than introducing new functional changes to the signed commits feature.