This is a test modernization patch in the breaking changes infrastructure series. It focuses on cleaning up test patterns in t6120-describe.sh that check for deprecation warnings of the `--stdin` option in `git name-rev`.

The patch makes three specific improvements to the test checking for the `--stdin` deprecation warning: (1) changes from double-quoted test body to single-quoted for consistency with other tests, (2) replaces the ERE-style grep pattern with a plain string match in double quotes, and (3) replaces direct `grep -E` usage with the preferred `test_grep` helper function. These changes align with established Git test suite conventions and make the test more robust.

This is patch 3/4 in a series that's part of the larger effort to establish patterns for handling breaking changes in Git. The changes here are specifically about test hygiene and don't modify any actual functionality - they just make the test for the deprecated `--stdin` behavior cleaner and more consistent with the rest of the test suite. The patch touches only t/t6120-describe.sh, modifying the test that verifies the deprecation warning output when using the soon-to-be-removed `--stdin` option.