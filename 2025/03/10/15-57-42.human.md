This is a style fix patch in the signed commits fast-export/import series. Christian Couder addresses a minor coding style issue in `builtin/fast-export.c` where several `switch` statements were missing whitespace after the keyword, which violates Git's documented coding guidelines. The changes are purely cosmetic - adding a single space after each `switch` keyword in four locations.

The patch is part of the v6 series that implements signed commit handling in fast-export/import, but this particular change is independent of that functionality. It's a straightforward improvement that aligns with project style standards, similar to other polish changes in this final version of the series. The change is minimal (4 lines modified) and risk-free, just ensuring consistent formatting in the codebase.