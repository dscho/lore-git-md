This is a technical clarification in the ongoing discussion about index lock behavior in Git commands. Junio Hamano responds to Benjamin Woodruff's question about whether `git-status` documentation is incorrect regarding background refreshes being performance-only.

Junio explains the distinction between in-core index updates (required for correctness) and on-disk writes (a performance optimization for other processes). He details how the current implementation works: while the in-memory index must be refreshed for accurate results, writing it to disk is optional and subject to lock contention. The lock coordination mechanism exists to prevent conflicts, but not all processes properly handle waiting for locks.

The email reinforces the technical consensus from earlier in the thread while addressing the documentation question. It confirms that the in-memory refresh is indeed for correctness, while the disk write is the optional performance aspect. This clarification helps frame the ongoing discussion about how to document these behaviors consistently across commands like `status`, `describe`, and `diff`.

Key technical points include:
- The separation between in-core index updates (mandatory) and on-disk writes (optional)
- How `repo_update_index_if_able()` handles the disk write attempt
- The locking requirements for disk writes and coordination challenges
- Implications for processes that don't properly implement lock waiting

This moves the thread forward by providing authoritative technical background that can inform both the documentation improvements being considered and any future implementation decisions. The response maintains Junio's established position while offering concrete technical details to address the specific documentation question raised.