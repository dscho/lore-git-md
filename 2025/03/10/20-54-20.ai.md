This is a refactoring patch in the NUL-delimited `rev-list` output series, specifically addressing early option parsing. Junio Hamano is reviewing Justin Tobler's patch that combines two option-parsing loops in `cmd_rev_list()` into one, while noting a deeper underlying issue.

The patch simplifies the code by merging separate loops for `--exclude-promisor-objects` and `--missing` options into a single pass. However, Junio points out there's an existing `NEEDSWORK` comment indicating more fundamental problems with this parsing approach. He questions whether this refactoring helps or hinders future fixes to the underlying issues, though he doesn't specify what those issues are.

Key technical details:
- Touches `builtin/rev-list.c`
- Combines option parsing for `--exclude-promisor-objects` and `--missing`
- Removes redundant error checking (the die() for incompatible options)
- Still leaves the core parsing issues noted in the `NEEDSWORK` comment unresolved

This appears to be a straightforward cleanup that improves code organization, but Junio's comment suggests the series may need to address deeper architectural issues in option parsing before finalizing. The review doesn't reject the change but raises an important consideration for the series' future direction.