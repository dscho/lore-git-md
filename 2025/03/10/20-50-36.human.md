This is a follow-up discussion in the ongoing thread about `--no-optional-locks` behavior in Git commands. Benjamin Woodruff provides real-world context about why they initially proposed the change, while engaging with Junio and Jeff's technical concerns.

The email explains a concrete use case where `git describe --dirty` caused lockfile conflicts in a Rust build system (Next.js) that uses version identifiers for cache invalidation. Benjamin shares that they've already worked around the issue downstream by reimplementing the dirty-check behavior using `git status`, so the patch is no longer urgent for their needs.

Key new points raised:
- Questions whether Git's documentation about index refresh being "for performance" (in `git-status`) contradicts the correctness argument made earlier in the thread
- Notes that libgit2 diverges from Git's behavior by not doing background refreshes by default
- Proposes scaling back the changes to just documentation updates rather than adding new command-line options, suggesting clearer warnings about lock behavior in `describe --dirty` and `status`

The email shows the discussion shifting from implementation details to interface design philosophy, with Benjamin pragmatically suggesting a documentation-focused compromise that might address the original problem without adding permanent command-line options. This reflects the thread's evolution from technical implementation debate toward finding solutions that balance correctness, usability, and maintenance burden.