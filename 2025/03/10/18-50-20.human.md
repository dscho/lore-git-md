This is a brief follow-up in the ongoing discussion about enabling the `-Wunreachable-code` compiler warning in Git's development builds.

Junio Hamano responds to Jeff King's suggestion to enable the warning in just one CI job (linux-sha256) rather than across all builds. He confirms this approach would avoid the known issue with `sigfillset()` on macOS while still allowing evaluation of the warning's usefulness. Junio gives his approval for this limited experiment.

The exchange shows the maintainer moving from initial skepticism about the warning's maintenance burden to accepting a measured approach for gathering more data. The linux-sha256 CI job will serve as a testbed to evaluate whether the warning catches real issues without causing too many false positives or platform-specific headaches.

This keeps the discussion moving forward while avoiding immediate widespread impact, reflecting Junio's characteristically cautious approach to build system changes. The thread has evolved from technical implementation details to a pragmatic evaluation strategy.