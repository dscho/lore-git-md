This is a refactoring patch that was part of the NUL-delimited output format series for `git rev-list`. The patch inlines the `show_object_with_name()` function into its single caller `show_object()` and consolidates the common OID printing logic. 

The change simplifies the code by removing the helper function and moving its implementation directly into `show_object()`, while also factoring out the shared OID printing that was previously duplicated between the named and unnamed object cases. The patch touches `builtin/rev-list.c`, `revision.c`, and `revision.h`, removing the now-unneeded function declaration and implementation.

Junio Hamano's response indicates he finds the refactoring sensible, which aligns with the thread's earlier positive reception of the technical implementation quality. This was the first patch in the series that laid groundwork for later NUL-delimited output support by cleaning up the object printing infrastructure. The subsequent patches built on this to add the `-z` option functionality discussed in the thread.