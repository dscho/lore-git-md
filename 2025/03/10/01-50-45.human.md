This is a documentation and test maintenance patch in the ongoing path-walk delta compression series. Patch 03/13 aligns the usage text in `builtin/pack-objects.c` with the documentation in `git-pack-objects.adoc`, then removes `pack-objects` from the test exception list that checks for such mismatches.

The changes are straightforward: the command's usage message is updated to match the detailed documentation format, with proper line breaks and indentation. The corresponding test that verifies documentation consistency is updated to no longer exempt `pack-objects` from these checks, indicating the documentation is now properly synchronized.

This is a minor but important housekeeping change in the larger series, ensuring the new `--path-walk` option (introduced in earlier parts of the series) is properly documented and verified. The patch doesn't affect functionality - it's purely about keeping documentation and tests in sync with the implementation.