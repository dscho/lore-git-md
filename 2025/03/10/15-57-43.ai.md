This is a documentation and consistency patch in the signed commit handling series for fast-export/import.

The patch renames the `--signed-tags=warn` option to `--signed-tags=warn-verbatim` to better match the existing pattern where `warn-strip` pairs with `strip`. The change makes the behavior more obvious by explicitly showing that signatures will be exported verbatim (with a warning) rather than stripped. For backward compatibility, the old `warn` name remains supported as a deprecated synonym.

The implementation touches three areas: updating the documentation to show both forms, modifying the enum and parsing logic in fast-export.c, and adding new test cases to verify both the new name and backward compatibility. The changes are straightforward mechanical updates with no behavior changes beyond the naming improvement.

This appears to be a final polish patch in the v6 series, addressing a minor naming inconsistency that was likely caught during review. The patch maintains all existing functionality while improving clarity, and includes thorough test coverage of both the new and legacy option names.