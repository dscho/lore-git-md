Refactoring patch as part of the path-walk delta compression series.

This is the first patch in a 13-part series introducing path-based delta compression. It performs a straightforward refactoring - extracting the delta eligibility check logic from `prepare_pack()` into a new `should_attempt_deltas()` function. The commit message notes this will be reused in future changes, likely for the path-walk delta selection logic.

The change is mechanical and well-contained, moving 24 lines of condition checks into a new function while maintaining all the existing logic. The diff shows the typical pattern of a refactoring patch - code is moved rather than modified, with the surrounding context adjusted to call the new function.

As the first patch of a larger series, this serves as groundwork for the more substantial changes to come. The thread context indicates this series will introduce path-based delta grouping to improve compression, particularly for repositories with many similar filenames. This initial patch doesn't yet implement any of those features - it's purely preparatory work to make the later changes cleaner.