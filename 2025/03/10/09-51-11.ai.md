This is a bugfix patch addressing sign comparison warnings in the decoration code. The patch is part of a Google Summer of Code (GSoC) contribution.

The changes fix two instances where signed integers were being compared with unsigned integers, which could trigger compiler warnings. The first change updates the `size` variable in `insert_decoration()` to match the unsigned type of `n->size`. The second change similarly updates the `nr` counter in `add_decoration()` to be unsigned to match its usage context. 

Notably, the patch also removes the `DISABLE_SIGN_COMPARE_WARNINGS` macro that was previously suppressing these warnings, indicating the issues are now properly fixed rather than just silenced. The changes are straightforward type corrections that maintain the same logical flow while making the code more type-safe.

The patch is small (6 lines changed) and focused, touching only decorate.c. It appears to be a clean, well-motivated fix for a real issue, though the commit message could be slightly more detailed about why these particular sign comparisons matter (beyond just warning suppression). The changes align with Git's code quality standards by properly matching types rather than suppressing warnings.