Updated AI thread summary:

Bugfix addressing NULL pointer dereference in the promisor-remote subsystem when configured without a URL, originally identified by Christian Couder. The issue occurs in `promisor-remote.c` when handling missing URLs, where `git_config_get_string()` returns NULL that was unsafely passed to `strvec_push()`. The thread has evolved through multiple iterations to address both the immediate crash and broader API design questions.

Christian Couder's v2 patch converts NULL URLs to empty strings in `promisor_info_vecs()` while adding explicit warnings for missing URLs in both client and server configurations during 'KnownUrl' checks. The test suite now covers missing client-side URLs in 'KnownName' mode and server-side URL advertisement in 'KnownUrl' mode. The changes focus on `promisor-remote.c` and its tests, maintaining backward compatibility while improving diagnostics.

The discussion has expanded to consider strvec API design implications, with Junio Hamano raising questions about whether strvec_push() should enforce non-NULL values via BUG(). Christian's follow-up clarifies that the promisor-remote behavior is specific to server advertisement validation when 'promisor.acceptfromserver' is set (KnownName/KnownUrl modes), and that the current patch deliberately avoids changing strvec/xstrdup semantics. He explains the logic for remote rejection in each mode and confirms the patch doesn't alter actual fetching behavior - clients still can't use server-provided URLs even when accepted.

Key technical points:
- strvec_push() NULL handling semantics remain unchanged for now
- promisor.acceptfromserver validation modes (KnownName vs KnownUrl) have distinct URL handling
- Advertisement validation is separate from actual URL usage
- Warnings added for missing URL configurations
- Test coverage expanded for edge cases
- Redundant NULL check in URL encoding logic fixed

The thread remains open on whether strvec API changes are warranted, but the immediate promisor-remote fix appears ready for inclusion, maintaining backward compatibility while improving safety and diagnostics. The implementation carefully avoids broader API changes, leaving those discussions for post-2.49.0 consideration.