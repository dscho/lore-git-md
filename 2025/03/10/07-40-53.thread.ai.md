Updated AI thread summary:

Bugfix addressing NULL pointer dereference in the promisor-remote subsystem when configured without a URL, originally identified by Christian Couder. The thread has evolved through three major iterations (v1-v3) to address both the immediate crash and broader API design questions, culminating in behavior that now mirrors standard Git remote conventions.

Christian Couder's v3 patch represents the current consensus solution:
1. Replaces unsafe NULL handling with remote name fallback (matching `git fetch` behavior)
2. Uses `git_config_get_string_tmp()` for safer memory management
3. Adds comprehensive warnings for URL-related edge cases in 'KnownUrl' mode
4. Expands test coverage in `t/t5710-promisor-remote-capability.sh`

Key technical progression:
- v1: Initial fix preventing NULL dereference
- v2: Introduced empty string fallback with URL warnings
- v3: Adopted standard remote behavior (name-as-URL) while maintaining warnings

Implementation details:
- Modified files: `promisor-remote.c` and test script
- Behavior now matches core remote conventions
- Warning system covers:
  - Missing/empty server-advertised URLs
  - Empty client URLs
  - Name/URL mismatches
- Test cases verify:
  - 'KnownName' with missing URL
  - 'KnownUrl' edge cases
  - Environment variable validation (now using `env` per test suite conventions)

Thread consensus points:
1. Immediate NULL dereference fixed via name fallback
2. Standard remote behavior adopted as preferred solution
3. Warnings maintained for configuration issues
4. Test coverage considered comprehensive after test-lint fix

Junio Hamano confirmed the core fix was ready for 2.49.0 and provided test suite hygiene feedback that Christian Couder has acknowledged and will incorporate. The thread demonstrates a clear progression from safety fix to API consistency improvement, with final attention to test suite conventions. The test-lint correction represents the final polishing of this fix before it graduates to `next`.