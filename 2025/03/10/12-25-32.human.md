This is a continuation of the discussion about optional locks in porcelain commands, specifically addressing the technical tradeoffs of index handling.

Junio Hamano responds to Jeff King's explanation of how `git status --no-optional-locks` works, agreeing that the approach of updating the index internally without writing it to disk makes sense for avoiding lock contention. He raises an interesting broader question about whether inspection commands like `diff` and `status` should always opportunistically write the index back by default, even when not explicitly asked to.

The email then references Jeff's earlier analysis of three alternative approaches for scripting scenarios, which all have significant drawbacks: accepting potentially incorrect stat-dirty results, paying the overhead of separate index files, or using in-memory index updates via porcelain commands. This reinforces the technical justification for the proposed `--no-optional-locks` option as a pragmatic solution to real-world lock contention problems.

The exchange highlights the ongoing discussion is moving from initial philosophical concerns about porcelain/plumbing boundaries to more concrete technical evaluation of the proposed solution's merits versus alternative approaches. Junio's response suggests he may be warming to the idea while still maintaining his fundamental position about porcelain command behavior.