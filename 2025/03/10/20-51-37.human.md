This is a refactoring patch that's part of the NUL-delimited output format series for `git rev-list`. The patch inlines a helper function (`show_object_with_name()`) into its only caller (`show_object()`) and consolidates the common OID printing logic. The change is straightforward and improves code organization by eliminating an unnecessary abstraction layer.

The refactoring prepares the codebase for subsequent changes that will add NUL-delimited output support, though this particular patch doesn't yet implement that functionality. The patch appears well-constructed with clean diffs and maintains existing behavior while simplifying the code structure.

As part of a larger series that's already been through review (now at v1), this foundational change seems uncontroversial and technically sound. The thread context shows this is part of a completed series that's now in design discussion about output format unification, with Junio Hamano (the maintainer) giving this particular patch his approval with a simple "Makes sense" response.