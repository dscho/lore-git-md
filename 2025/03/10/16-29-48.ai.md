Junio Hamano's review of the promisor-remote NULL URL fix. This is a technical discussion about the proper handling of missing promisor remote URLs in the strvec implementation.

The review raises several important questions about the patch's approach. First, Hamano questions whether strvec_push() itself should have internal protection against NULL values, suggesting a BUG() might be appropriate. Second, he probes the semantics of empty URLs in the names strvec - whether we should be silently skipping them (as the patch does) versus either erroring out or filtering them earlier. Third, he points out an apparent inconsistency where the patch checks both urls.v[i] existence and content (*urls.v[i]) despite the earlier change ensuring NULLs are replaced with empty strings.

These questions suggest the fix might need refinement in either the strvec API contract or the promisor-remote URL handling logic. The discussion touches on deeper design questions about how invalid configurations should be handled (fail fast versus graceful degradation) and API invariants. The thread appears to be moving from a simple bugfix toward considering broader implications for the strvec interface and configuration validation patterns.