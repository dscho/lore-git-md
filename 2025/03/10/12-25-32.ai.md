This is a continuation of the discussion about optional locks in porcelain commands, specifically addressing the technical tradeoffs of index handling. Junio Hamano responds to Jeff King's explanation of how `git status --no-optional-locks` works, agreeing that the approach of updating the index internally without writing it to disk is "somewhat sensible" for avoiding lock contention while maintaining correctness.

The email raises a deeper architectural question about whether inspection commands like `diff` and `status` should always opportunistically write the index back by default, even when not explicitly asked to do so. This touches on the core tension in the thread between correctness guarantees of porcelain commands and the practical needs of scripting scenarios where lock contention is problematic.

Key technical points referenced:
- The current `--no-optional-locks` implementation updates indexes in-memory but avoids disk writes
- Jeff's outlined alternatives for scripting scenarios (accepting stat-dirty results, using separate indexes, or in-memory updates)
- The fundamental question of whether porcelain commands should default to opportunistic index writes

The discussion continues to explore the balance between Git's porcelain/plumbing design philosophy and real-world use cases, with Junio appearing more open to the technical merits of the approach while still maintaining concerns about porcelain command behavior.