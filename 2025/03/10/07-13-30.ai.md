This is a minor bugfix patch in Patrick Steinhardt's ongoing series to remove `the_repository` usage. It addresses compiler warnings in the newly reorganized hash algorithm code.

The patch fixes "-Wsign-compare" warnings in hash.c by changing loop counters and function parameters from `int` to `size_t` where appropriate. The changes are straightforward:
- Removes the now-unneeded `DISABLE_SIGN_COMPARE_WARNINGS` define
- Updates `hash_algo_by_length()` to take `size_t` parameter (matching its callers)
- Changes loop counters in three hash algorithm lookup functions to `size_t`
- Updates the function prototype in hash.h to match

These are purely technical changes with no behavior impact, cleaning up type safety in the hash algorithm subsystem that was recently moved from object-file.c. The patch is small (5 insertions, 9 deletions) and uncontroversial, appearing as a final polish before the series merges. It maintains the structural improvements made earlier while fixing minor compiler warnings that surfaced during the reorganization.