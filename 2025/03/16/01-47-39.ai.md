Documentation patch correcting an ancestry graph example.

Han Jiang submits a one-line documentation fix for the `--ancestry-path` option in `rev-list-options.adoc`. The patch adds commit C to the example graph showing the `H D..M` range, making it match the actual Git behavior. The change is minimal but corrects what appears to be an oversight in the documentation's example graph.

The patch touches only the documentation file with a straightforward edit to the ASCII graph. It's signed off and includes Elijah Newren and D. Ben Knoble in the CC list, suggesting they may have been involved in prior discussions about this documentation. The change is uncontroversial and clearly corrects the documentation to match expected behavior.

For future reference: This modifies the `--ancestry-path` documentation's example graph in `Documentation/rev-list-options.adoc`, adding commit C between the root and commit G in the example. The change aligns the documentation with how Git actually processes ancestry paths for the given range.