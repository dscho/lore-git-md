Thread summary:

Build system fix addressing Perl detection in Meson configuration. The thread concerns a specific edge case in the Meson build system where documentation generation would fail when Perl bindings were explicitly disabled (`-Dperl=disabled`) but documentation was enabled (`-Ddocs=man`), as documentation tools rely on Perl. The patch adds documentation building to the conditions that make Perl mandatory, alongside existing requirements for tests, gitweb, and credential helpers. The change is a single-line modification to `meson.build` that properly enforces the Perl dependency when documentation is requested. The commit message references a real-world failure case from Gentoo's bug tracker that motivated the fix. The solution is minimal, well-explained, and correctly scoped to address the specific configuration issue without introducing new problems.