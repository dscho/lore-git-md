This is a technical follow-up in the thread about CSPRNG backend configuration on Linux systems. Ramsay Jones responds to Brian Carlson's explanation about why `getrandom` is preferable to `arc4random` on Linux, while examining implementation details across platforms.

The email provides additional implementation analysis, confirming that:
- Cygwin's `arc4random_buf()` uses the OpenBSD-style ChaCha20 approach
- Glibc's implementations of `arc4random_buf()`, `getrandom()`, and `getentropy()` all ultimately wrap the Linux `getrandom` syscall
- The author couldn't identify the "extra function call" overhead Brian mentioned

Key technical points:
- Confirms the meson build system currently selects `arc4random` on Linux inappropriately
- Notes this configuration is correct for Cygwin (which uses OpenBSD-style implementation)
- Suggests the need to modify both meson build configuration and planned autoconf tests

The email effectively concludes the technical discussion by agreeing the patch should be dropped, as all supported Linux distributions have glibc ≥2.25 (providing `getrandom`), making direct `getrandom` usage clearly preferable. This aligns with the thread's established understanding of Linux CSPRNG backend selection.