This is a technical discussion about CSPRNG backend selection on Linux, specifically addressing the preference between `arc4random` and `getrandom` implementations. The email responds to a documentation patch suggesting Linux users could override `CSPRNG_METHOD=arc4random` for make-based builds.

The author argues that `getrandom` should be preferred over `arc4random` on Linux due to implementation differences between BSD and Linux. On BSD systems, `arc4random` uses a userspace ChaCha20 implementation with fork-safety benefits, making it the clear default there. However, Linux's glibc implementation of `arc4random` is just a wrapper around the kernel CSPRNG (via `getrandom`), offering no performance or security advantages while adding an extra function call overhead. The author notes all supported Linux distributions have glibc ≥2.25 (which includes `getrandom`), suggesting Git could default to `getrandom` for Linux builds.

Key technical points include the fork-safety mechanism differences between BSD and Linux implementations, the historical context of the glibc maintainers' decision, and the practical considerations of support windows and performance. The discussion reveals an implementation detail that wasn't apparent in the original documentation patch - that Linux's `arc4random` doesn't provide the same benefits as BSD's, making the documentation suggestion potentially misleading for Linux users. This appears to be a substantive technical correction to the prior documentation-focused discussion.