SMTP authentication error handling refinement (part 2/2 of GSoC series). This patch builds on the unified error capture from part 1 to implement RFC 5321-compliant SMTP status code parsing in `git-send-email.perl`'s authentication flow.

The change introduces three distinct error handling paths based on captured SMTP status codes: temporary errors (4xx codes) allow retries by returning success (1) while warning, permanent errors (5xx codes) fail immediately with a warning, and unrecognized codes default to permanent failure. The implementation uses regex parsing of the error message to extract the 3-digit status code, maintaining the existing credential callback flow when no error occurs.

This completes the series' goal of making SMTP authentication error handling more robust by replacing the TODO placeholder with proper status code interpretation. The 27-line change (+24/-3) focuses narrowly on the `smtp_auth_maybe` function, preserving the v3-approved architecture while adding the promised status code handling. The patch-specific additions include:
- Regex pattern `\b(\d{3})\b` for code extraction
- Distinct warning messages for temporary/permanent/unknown cases
- Return value logic that maintains the original flow for success cases
- RFC 5321 reference in both commit message and code comments

No new configuration or user-facing changes are introduced - this remains an internal robustness improvement building on the v4 series' foundation. The implementation appears thorough, with clear handling for all expected code classes and a fallback path for malformed responses.