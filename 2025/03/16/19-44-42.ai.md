This is a technical discussion about path-filtered log behavior with subtree merges, continuing the thread about directory-level rename tracking limitations in Git's revision walking logic.

Junio Hamano builds on Johannes Schindelin's analysis of `try_to_follow_renames()` in `tree-diff.c`, agreeing that the current single-path queue access (`q->queue[0]`) and pathspec matching would need fundamental changes. He makes several key observations:

1. The current `--follow-renames` implementation's "check-box item" design is fundamentally limited for merge-heavy histories where paths may have different names across branches. The single-path tracking breaks down when paths are renamed differently in parallel histories that later merge.

2. A proper solution would need per-ancestor path tracking during revision walking, maintaining the followed path name specific to each commit's ancestry path when traversing merges.

3. Directory-level rename following would emerge naturally from a more general solution for tracking multiple paths, with directory following being a special case of this more comprehensive approach.

The email reinforces that the current limitations are architectural rather than superficial, requiring changes to core revision walking behavior to properly handle path tracking across merges. Hamano suggests the solution path should first address single-path following correctly in merge scenarios before generalizing to multiple paths, with directory following then becoming a natural extension.

This continues the thread's technical depth while emphasizing the architectural implications of any solution, positioning directory-following as part of a larger path-tracking challenge in Git's revision walking subsystem.