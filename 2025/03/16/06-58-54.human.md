This is a series of small preparatory cleanups as part of the ongoing `merge-recursive` removal effort. Elijah Newren submits five independent patches that each make minor fixes without changing behavior:

1. Removes an unnecessary include of `merge-recursive.h` from `builtin/stash.c`
2. Corrects a reversed commit reference in a test comment
3. Makes test `t7615` more explicit about which diff algorithm it's testing
4. Fixes a typo where `merge-ort` accidentally used `strset` instead of `strintmap`
5. Removes an extraneous word in a `merge-ort` comment

The changes are all straightforward and well-explained, touching only comments, test descriptions, and one include statement. As the cover letter notes, these are small orthogonal cleanups being grouped together for convenience rather than being part of a larger coordinated change. The patches appear to be low-risk maintenance work that helps pave the way for the deprecated `merge-recursive` backend's eventual removal.