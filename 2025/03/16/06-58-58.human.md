This is a refactoring patch in Elijah Newren's series preparing for merge-recursive removal, specifically fixing a minor but technically incorrect data structure usage in merge-ort.

The patch corrects a macro usage mismatch in `merge-ort.c` where `strset_for_each_entry` was incorrectly used to iterate over a `strintmap` structure. While both macros evaluate to the same underlying code, using the type-correct macro makes the intent clearer and prevents potential future issues. The error originated in commit 7bee6c100431 when `relevant_sources` was refactored from a strset to a strintmap.

This is a straightforward cleanup with no behavior change, typical of the series' focus on code hygiene improvements. The change is small (just two lines modified) but demonstrates the careful attention to detail needed when working with these internal data structures. As with the rest of the series, this patch helps maintain code clarity as part of the larger merge-recursive removal effort.