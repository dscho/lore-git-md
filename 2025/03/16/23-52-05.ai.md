This is a technical discussion about Git's cryptographic random number generation (CSPRNG) configuration, specifically addressing platform-specific implementations and build system behavior. The email continues the thread's examination of arc4random versus getrandom usage across different platforms.

Brian Carlson responds to Ramsay Jones' analysis of Cygwin's arc4random implementation (which uses ChaCha20 like OpenBSD) versus Linux's glibc implementation (which just wraps getrandom). They confirm the technical details and discuss the practical implications for Git's build systems. Key points include:

The meson build system currently selects arc4random on Linux by default, which while not ideal (since getrandom is preferable there) isn't catastrophic since the performance impact is negligible for Git's file operations. However, they argue config.mak should hardcode getrandom as the Linux default since all supported environments now have glibc ≥2.25.

The discussion also looks ahead to POSIX standardization of getentropy, suggesting it may become the future cross-platform solution, though that transition isn't complete yet. The email reinforces the thread's established technical consensus while pragmatically addressing the remaining build system configuration issue.