This is a minor documentation cleanup patch in the merge-ort series. It fixes a small grammatical error ("is was" -> "was") in a comment within merge-ort.c that explains how rename similarity scores are handled during merge operations.

The change is purely cosmetic, correcting a redundant verb in the comment block that describes the processing of rename scores from diffcore_rename_extended(). The comment explains that while the similarity score is initially used to detect renames, it becomes irrelevant afterward except for tracking which side of the merge the rename came from.

As the fifth and final patch in Elijah Newren's preparatory refactoring series for merge-recursive removal, this continues the theme of low-risk, behavior-preserving cleanups. The change is trivial but maintains codebase hygiene, and being part of the merge-ort infrastructure (which is replacing merge-recursive), it contributes to the overall code quality of the new implementation.

The patch context shows this touches only merge-ort.c with a simple 3-line comment fix, aligning with the series' focus on isolated improvements. No functional changes or test modifications are involved, just documentation clarity in the rename handling logic.