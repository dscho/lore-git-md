Performance optimization patch introducing batched refname availability checks. This is patch 4/16 in Patrick Steinhardt's series addressing refname checking inefficiencies across Git's ref backends.

The patch introduces a new `refs_verify_refnames_available()` function that checks reference name conflicts for multiple refs at once, replacing the existing single-ref `refs_verify_refname_available()` which is reimplemented as a wrapper. The initial implementation maintains the same logic but processes refs in a loop, setting up for future optimizations in subsequent patches.

This foundational change enables performance improvements by reducing repeated work when checking multiple refs, particularly important for operations like `git-update-ref` that process many refs at once. The diff is large but mostly mechanical - the key change is wrapping the existing logic in a loop over input refnames while preserving all the existing validation rules.

As part of the larger optimization series, this patch shows careful attention to maintaining backward compatibility while laying groundwork for more significant backend-specific optimizations to come. The implementation appears thorough, with the existing test suite presumably covering the unchanged behavior.