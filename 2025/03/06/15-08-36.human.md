Performance optimization patch for the reftable backend, part of a larger series addressing refname availability checks. This is patch 5/16 in v5 of the series.

The patch refactors the reftable backend to batch refname availability checks during transaction preparation, though the actual performance improvements will be implemented in later patches. Currently, it just collects all refnames to check into a list and verifies them together at the end, maintaining the same behavior as the previous per-refname checks.

This is a foundational change that enables subsequent optimizations in the series. The commit message is clear about the current lack of performance impact, setting up expectations for future patches. The change appears well-contained, touching only the reftable backend code with straightforward modifications to use the new batched verification API introduced in patch 4/16.

The patch maintains all existing functionality while preparing for future optimizations, following the series' pattern of incremental improvements. Reviewers have already given positive feedback on the overall approach, with only minor documentation nits noted in earlier versions.