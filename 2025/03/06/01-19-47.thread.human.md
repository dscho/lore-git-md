This thread addresses inconsistencies in how Git handles `--exclude` patterns when iterating references across different backends. The original two-patch series fixed two issues: incorrect handling of empty patterns (which could cause packed refs to be wrongly filtered out) and inconsistent prefix matching behavior between backends. The first patch ensures empty patterns are handled before reaching any backend, while the second normalizes all patterns by adding trailing slashes when absent, creating consistent behavior across files, packed, and reftable backends.

Patrick Steinhardt has now reviewed the series, agreeing with the technical approach but suggesting a more general commit subject since the changes affect all backends, not just files and packed. He confirms the test updates properly verify the fixed behavior, including cases with overlapping excluded regions that previously tested incorrect behavior. While Jeff King provided informal assistance earlier, Patrick's review represents the first formal on-list feedback, indicating technical alignment with the solution while proposing minor documentation improvements. The series appears ready for merging pending any final tweaks to the commit messages.