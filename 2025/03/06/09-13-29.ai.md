This is a files backend cleanup patch in Karthik Nayak's partial reference transaction series. It removes redundant duplicate-refname checking in `split_symref_update()` and cleans up related `util` field assignments that are no longer needed.

The patch eliminates a second verification step that was redundant with an earlier check, simplifying the code while maintaining safety. Junio Hamano initially had minor questions about the commit message phrasing but confirmed the change is correct after examining the full context. The removal of the code is safe because the first check already guarantees no duplicates will be added to the `affected_refnames` list.

This change represents a straightforward simplification that removes unnecessary operations while preserving the intended behavior. It's part of the broader effort to clean up and optimize the reference transaction infrastructure across Git's backends. The patch has Junio's approval and doesn't appear to require further discussion.

Key technical details for future reference:
- Removes duplicate check in `split_symref_update()`
- Cleans up unused `item->util` assignments
- Affects files backend transaction preparation
- Part of larger refactoring to centralize refname checking
- Maintains same safety guarantees with simpler code