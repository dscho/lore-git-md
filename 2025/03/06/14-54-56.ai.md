This is a follow-up discussion in the ongoing `the_repository` removal series, specifically about patch 4/12 which moves `core.bigFileThreshold` to repo settings.

The exchange clarifies a test modification in t1050-large.sh where the test was updated from using `git rev-parse` to `git hash-object` to properly exercise the negative value validation for `core.bigFileThreshold`. Patrick Steinhardt confirms Justin Tobler's understanding that this change was necessary because `rev-parse` no longer triggers the threshold validation code after the configuration was moved to be computed on-demand in the repository settings.

The discussion is brief and technical, focused on ensuring the test properly validates the intended behavior after the architectural change. Patrick agrees to update the commit message to document this test change rationale. This appears to be a minor refinement to an otherwise well-reviewed patch in the larger series eliminating `the_repository` usage.