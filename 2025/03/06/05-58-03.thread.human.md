This thread introduces a new `--no-optional-locks` option for Git commands that perform background index updates, starting with `git describe` and `git diff`. The change addresses lockfile contention issues in scripting scenarios where concurrent Git operations might interfere with each other.

The first patch added support for `--no-optional-locks` to `git describe --dirty`, following the same pattern as `git status`'s existing implementation. It modified `builtin/describe.c`, updated documentation, and added test coverage using Git's established lockfile testing patterns. The second patch now extends this to `git diff`, adding similar index update control with thorough test coverage in a new test file. The implementation verifies behavior with both config settings (`diff.autoRefreshIndex`) and the command-line flag.

The changes are well-scoped and maintain backward compatibility while solving a real pain point in automation environments. The series appears complete with these two patches, bringing consistent behavior to commands that perform background index updates. No reviews or feedback have been received yet, but the implementation follows established patterns and includes comprehensive test coverage.