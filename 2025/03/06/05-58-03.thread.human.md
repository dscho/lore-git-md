This thread explores adding a `--no-optional-locks` option to `git describe` and `git diff` to address lockfile conflicts in scripting scenarios, following the pattern established by `git status`. The discussion has centered on a fundamental tension between Git's porcelain/plumbing design principles and practical scripting needs.

Junio Hamano initially expressed skepticism about making index refreshes optional in porcelain commands, which are designed to provide correct results by default. However, after Jeff King (peff) detailed the technical implementation (where commands update indexes in-memory without disk writes to avoid lock contention), Junio acknowledged this approach as "somewhat sensible" while still questioning whether inspection commands should opportunistically write indexes by default.

The technical implementation is complete (v1 series) with patches modifying `describe.c` and `diff.c`, new test coverage, and documentation updates. The core debate now focuses on architectural questions: whether porcelain commands should maintain their strict correctness guarantees at all times, or whether practical scripting needs justify this opt-in relaxation of behavior. Jeff has provided concrete examples where the porcelain approach solves real problems that plumbing alternatives cannot address as cleanly.

While Junio remains philosophically cautious about altering porcelain command behavior, the discussion has progressed toward recognizing the technical merits of the solution, particularly its balance of correctness (via in-memory updates) and scripting-friendliness (avoiding disk writes). The thread appears to be converging toward acceptance of the approach, pending clear documentation of the tradeoffs.