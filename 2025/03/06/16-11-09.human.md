Junio C Hamano clarifies the motivation for `--no-optional-locks` in this response to Benjamin Woodruff's patch series. He corrects the original reasoning that index updates were for performance reasons, explaining that porcelain commands like `git describe` and `git diff` refresh the index for correctness, while plumbing commands avoid refreshes to give scripts more control.

The key distinction is that porcelain commands must maintain correctness by default, making the proposed `--no-optional-locks` behavior only acceptable as an optional feature when no better alternatives exist (like using plumbing commands in scripts). This suggests the patch series may need to reframe its justification around correctness rather than performance to align with Git's design principles.

The response doesn't reject the feature outright but sets clear expectations about when such behavior would be appropriate, implying the series may need adjustments to its documentation and rationale. This is likely to shape future discussion about the proper scope and justification for the feature.