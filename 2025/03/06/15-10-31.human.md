This is part 7/12 of Patrick Steinhardt's series to remove `the_repository` usage from object APIs, specifically targeting the pack-bitmap writer. The patch converts `pack-bitmap-write.c` to use repository instances passed through the `bitmap_writer` struct rather than relying on the global `the_repository`.

The changes are methodical and consistent with the series' approach: the writer struct now stores a repository* field, which is used throughout for repository-specific operations like hash algorithm access, tree lookups, and progress reporting. The patch removes the `USE_THE_REPOSITORY_VARIABLE` define and updates all affected call sites, including trace2 instrumentation and file permission handling.

This continues the series' pattern of maintaining existing behavior while removing implicit global state. The changes are focused and mechanical, touching only the bitmap writing subsystem. Reviewers will want to verify the repository parameter is properly threaded through all bitmap operations, particularly around the pseudo-merge and lookup table functionality. The patch appears well-contained and follows the established conversion patterns from earlier in the series.