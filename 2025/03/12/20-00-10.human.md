This is a brief technical follow-up in the merge-ort API compatibility thread. Taylor Blau confirms Patrick Steinhardt's observation about missing documentation for the `num_merge_bases == 1` check in the new `merge_ort_generic()` function. The check relates to how merge-recursive handled constructed merge bases, and both reviewers agree this behavior should be explicitly mentioned in the commit message for clarity.

The exchange is a quick +1 on a specific implementation detail from Patrick's review, showing consensus on this minor point in the larger effort to build merge-ort wrappers that match merge-recursive's behavior. No new technical content is introduced here — it's simply an acknowledgment that the documentation gap should be filled.