This is a cover letter for a patch series improving Meson build system handling of breaking changes. The series follows up on previous discussion about how to handle the `-Dbreaking_changes=true` option in the Meson build configuration.

The 3-patch series makes several coordinated changes to better align Meson's behavior with the Makefile build system when breaking changes are enabled. Key changes include properly defining the `WITH_BREAKING_CHANGES` preprocessor flag (matching Makefile behavior), and ensuring `git-pack-redundant` binary and its documentation are excluded from compilation/installation when breaking changes are enabled - consistent with how the Makefile build handles this case.

The changes touch build configuration files in both the main meson.build and Documentation/ subdirectory, with the most substantial changes being in how conditional compilation is handled. The series appears to be a straightforward technical follow-through on prior discussion, with each patch focused on a specific aspect of the breaking changes handling. The changes look well-contained and properly motivated by the existing thread.