Documentation update bringing the "My First Contribution" tutorial in line with current Git practices. This GSOC-contributed patch modernizes several aspects of the new contributor guide:

The changes focus on aligning the tutorial with recent codebase developments, particularly around repository handling. Key updates include adding the now-required `struct repository *repo` parameter to command functions, replacing `git_config()` with `repo_config()`, and introducing the `UNUSED` macro for parameter suppression. The patch also removes outdated references like the deprecated git-mentoring mailing list and standardizes documentation file extensions from `.txt` to `.adoc`.

These are practical, well-considered updates that will help new contributors avoid common pitfalls when working through the tutorial. The changes are focused and clearly explained in the commit message, though some might question whether the `UNUSED` macro introduction deserves its prominence in the tutorial given it's more of a stylistic convention than a core concept. The patch appears to be part of the contributor's GSOC project work, though no series context is provided.