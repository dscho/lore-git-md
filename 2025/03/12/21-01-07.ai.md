Performance-focused refactoring in the path-walk delta compression series.

Taylor Blau reviews Derrick Stolee's patch that extracts delta eligibility checking into a new `should_attempt_deltas()` function in `builtin/pack-objects.c`. This is a preparatory change for the larger path-walk delta compression work. The review notes the patch correctly moves the delta eligibility logic while maintaining the existing behavior, though it observes that a comment about delta reuse was dropped during the extraction. The change is straightforward - moving 24 lines of conditionals into a new function and updating the single call site to use it, with no functional changes. This appears to be a clean refactoring to support the more complex delta selection logic coming in later patches of the series.

Key technical details for future reference: The new `should_attempt_deltas()` function centralizes checks for whether an object should be considered for delta compression, including type validity, size thresholds, and preferred base status. The function returns 1 for eligible objects, 0 otherwise. The patch touches only `builtin/pack-objects.c`, modifying the delta preparation phase while keeping the same control flow and delta counting behavior. This appears uncontroversial as it's purely structural preparation for later functional changes in the path-walk series.