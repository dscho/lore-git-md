This is a follow-up discussion in the performance optimization series for the refs subsystem, specifically addressing patch 6/16 which implements batched refname availability checks in the files backend.

shejialuo questions why the `refnames_to_check` string list isn't sorted in the files backend implementation, noting that this differs from the approach taken in the reftable backend (which was discussed in patch 5/16). The patch author Patrick Steinhardt had included a detailed comment explaining the tradeoffs around locking packed references, but didn't address the sorting question.

The thread context shows this is a non-blocking question about implementation consistency between backends, with shejialuo previously suggesting such optimizations might belong in later commits. The series has already received final approval, with this being a point of clarification rather than a blocking issue. The discussion highlights ongoing considerations about how to best structure these performance optimizations across different ref storage backends.