CI / build system patch (part 2/3 in series) addressing Meson build system alignment with Makefile behavior for breaking changes.

This patch removes compilation of `git-pack-redundant` when the `-Dbreaking_changes=true` option is set in the Meson build, matching the Makefile behavior. The change ensures consistency between build systems when preparing for potential breaking changes by excluding this deprecated command. The current Meson implementation was incorrectly including the pack-redundant builtin source file unconditionally, despite the breaking changes flag being enabled.

The patch modifies meson.build to conditionally add pack-redundant.c to builtin_sources only when breaking changes are disabled. This follows the same pattern used in the Makefile build system and aligns with the series' goal of making Meson behavior match Makefile behavior for breaking change scenarios. The change is straightforward - moving the pack-redundant.c entry into a conditional block based on the breaking_changes option.

The patch builds on previous discussion in the thread about build system parity and specifically addresses an oversight in how the breaking changes option affects builtin compilation. It's a focused technical change with clear motivation from the surrounding thread context about achieving consistency between build systems. The implementation appears clean and maintains the existing build_options_config behavior while properly excluding the deprecated command when requested.