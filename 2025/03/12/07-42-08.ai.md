This is a feature discussion email in the ongoing thread about adding NUL-delimited output to `git rev-list`. The email specifically addresses Jeff King's compatibility question about whether the `-z` option should affect both input and output formats when used with `--stdin`.

Patrick Steinhardt weighs in on the side of consistency, arguing that `-z` should modify both input and output formats, citing `git cat-file` as an example where not doing so led to regrets. This positions him in agreement with Git's general convention for `-z` behavior across commands.

The email is brief but significant as it represents a key design decision point in the implementation. Patrick's position aligns with existing Git conventions and provides a concrete example (`git cat-file`) of why maintaining this behavior is important for long-term usability. The discussion continues the thread's progression from core format design to specific interface questions, with this particular point being one of the remaining open issues alongside the attribute naming convention debate.