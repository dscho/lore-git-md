Feature refinement for rev-list's NUL-delimited output mode.

Justin Tobler responds to Jeff King's compatibility questions about the proposed `-z` option for `git rev-list`. The thread has already established consensus on using single NUL delimiters and full attribute names ("missing=yes") for machine-readable output. 

Tobler confirms two key implementation decisions: First, `- -z` will affect both input and output parsing (aligning with other Git commands' behavior), making stdin parsing NUL-delimited when the option is active. Second, while `rev-list` currently accepts `-z` as a no-op due to its use of `setup_revisions()`, this is considered harmless since the option isn't actually trigger diffs in this context.

The response clarifies technical details about option parsing order - the early `-z` parsing won't interfere with `setup_revisions()`'s handling, and keeping this approach is necessary to properly NUL-delimit stdin arguments. The exchange shows the implementation is thoughtfully considering edge cases while maintaining consistency with Git's existing behavior patterns.