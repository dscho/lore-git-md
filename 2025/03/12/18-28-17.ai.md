This is the final exchange in the cruft pack size limit discussion, where Junio Hamano firmly rejects Taylor Blau's proposal to allow cruft packs to slightly exceed the `--max-pack-size` limit. The thread summary shows this was the last unresolved issue in an otherwise settled technical implementation.

Junio maintains his position that size limits must be strictly enforced, particularly highlighting the edge case where a single large object (e.g., 270MB vs a 200MB limit) could cause significant overshooting. He rejects Taylor's argument that allowing one extra object prevents repack cycles, calling the approach "unacceptable" since it violates the user-specified maximum without bounds on the potential overshoot.

The exchange reveals no movement from either side - Taylor doesn't introduce new technical justification, and Junio doesn't indicate willingness to compromise. This appears to be the thread's conclusion, leaving the final policy decision to the maintainer. The implementation will likely need to enforce strict size limits unless Taylor proposes an approach that avoids both cycles and limit violations.

Key technical context from the thread summary:
- Core implementation in `builtin/pack-objects.c` with object selection helpers
- Consolidated test infrastructure in `t/t7704-repack-cruft.sh`
- Remaining decision affects how `--max-pack-size` interacts with cruft packs
- Alternative solutions would need to address repack cycle avoidance without violating size constraints