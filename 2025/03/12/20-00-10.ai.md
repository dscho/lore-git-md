This is a technical discussion follow-up in the merge-ort API compatibility series. Taylor Blau confirms Patrick Steinhardt's observation about missing documentation for the `num_merge_bases == 1` check in the `merge_ort_generic()` wrapper function. The check relates to how merge-recursive handled constructed merge bases by setting `opt->ancestor` in that case. While this doesn't introduce new code changes, it reinforces the importance of documenting behavioral parity details between merge-ort and merge-recursive during the transition period. The exchange shows ongoing careful review of the series' technical accuracy, particularly around edge case handling that might affect callers expecting merge-recursive's exact behavior. This thread continues the pattern of thorough examination of the merge-ort wrapper layer's implementation details.