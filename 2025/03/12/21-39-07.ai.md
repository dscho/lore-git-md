This is a technical discussion about refactoring option parsing in `git rev-list` as part of the NUL-delimited output format series.

Justin Tobler responds to Junio Hamano's concern about whether combining two option-parsing loops in `cmd_rev_list()` might complicate future fixes to the known broken option validation. The discussion centers around the interaction between early `-z` parsing and the broader issue of how `setup_revisions()` handles incompatible option combinations.

Key technical points:
- The current patch moves `--missing` parsing into the same early loop as `--exclude-promisor-objects`, removing some validation checks
- Justin acknowledges this doesn't fix the underlying issue with `setup_revisions()`'s limited validation capabilities
- Two potential solutions are discussed:
  1. Adding a flag to disable diff options parsing in `setup_revisions()`
  2. More radically, separating argument parsing from `setup_revisions()` entirely
- Justin argues the current approach doesn't make future fixes harder while enabling the immediate `-z` functionality

The exchange reveals deeper architectural questions about revision option parsing that may warrant future refactoring, but suggests proceeding with the current implementation for the NUL-delimited output series. The discussion doesn't introduce new functionality but provides important context about the tradeoffs in the implementation approach.