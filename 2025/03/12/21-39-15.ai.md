This is a technical discussion about implementation details in the ongoing `rev-list -z` feature series, specifically addressing the delimiter handling design.

Justin Tobler responds to Junio Hamano's critique of the current delimiter implementation in patch 3/4. The discussion focuses on whether to use separate variables for NUL-delimited mode versus a unified delimiter approach. Junio had questioned the need for both `nul_delim` and `line_sep` variables, suggesting a single delimiter variable would be clearer.

Justin acknowledges the point and explains the original rationale - different output behaviors (path truncation, c-quoting) that depend on NUL-delimited mode. He agrees to consolidate into a single global delimiter variable in the next version, while noting metadata delimiters (SP in normal mode) may still need separate handling. The response indicates this is a refinement of the implementation rather than a fundamental design change, keeping with the thread's established approach to NUL-delimited output.

Key technical points:
- Current implementation uses `nul_delim` flag plus `line_sep` character
- Junio advocates single `line_delimiter` variable approach
- Justin explains branching behavior needs (path truncation, c-quoting)
- Plans to consolidate while maintaining special handling for metadata delimiters
- Changes will appear in next version of the series

This exchange represents final polishing of the implementation details after the core design (single NUL delimiters, key-value format) was settled earlier in the thread. The discussion remains focused and technical, with clear agreement on the path forward.