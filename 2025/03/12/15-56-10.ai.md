Performance optimization patch introducing batched refname verification as part of a larger series addressing refs subsystem inefficiencies.

This patch (4/16 in v6) introduces `refs_verify_refnames_available()`, a new function that checks reference name availability for multiple refs at once, replacing the existing single-ref `refs_verify_refname_available()`. The implementation currently maintains identical behavior to the original function while adding the batch processing capability, with optimizations to follow in later patches. The diff shows significant refactoring of the verification logic to handle a string list of refnames, with the original function reimplemented as a wrapper that calls the new batch version with a single-item list.

Key technical details for future reference:
- Adds new function signature in refs.h taking `const struct string_list *refnames`
- Restructures verification logic to loop through refnames using `for_each_string_list_item()`
- Maintains all existing checks (directory conflicts, extras/skip lists, initial transaction handling)
- Preserves error message formats and conditions
- Reuses string buffers (`dirname`, `referent`) across checks for efficiency
- Original function becomes a trivial wrapper creating single-item string_list

This foundational change enables subsequent performance optimizations in the series by providing the batch processing interface. The thread context indicates this is part of addressing reftable tombstone inefficiencies, with v6 having received final approval after addressing all review feedback.