Performance optimization series for refs subsystem (patch 6/16). This email addresses a reviewer question about sorting behavior in the files backend implementation of batched refname availability checks.

Patrick Steinhardt responds to shejialuo's query about why the files backend doesn't sort its `refnames_to_check` list like the reftable backend does. He clarifies that the files backend does currently sort the list (via `string_list_insert()`), but acknowledges this sorting is unnecessary. He plans to remove the sorting in both backends by switching to `string_list_append()` in a preceding commit.

This exchange highlights an implementation detail in the performance optimization series, where minor inconsistencies between backend implementations are being identified and addressed. The discussion shows careful attention to maintaining consistent behavior across different ref storage backends while eliminating unnecessary operations. The resolution appears straightforward and uncontroversial, with Patrick accepting the suggestion to remove redundant sorting.

The email doesn't introduce any major new technical concerns, but rather confirms ongoing refinement of the implementation details in this multi-patch optimization series. It demonstrates the typical back-and-forth between contributor and reviewer as they work through edge cases and implementation choices.