Performance optimization series continuation (patch 2/16) introducing a flag to skip refname ambiguity checks in the object lookup path. This email from shejialuo provides final review feedback on the second patch in the performance optimization series for refname availability checks. The patch introduces `GET_OID_SKIP_AMBIGUITY_CHECK` to bypass expensive ambiguity verification when safe, as groundwork for subsequent optimizations in the series.

The reviewer questions whether this change might be better combined with the following patch (3/16) that actually uses the flag in `git-update-ref`, since the motivation isn't fully clear until seeing that application. However, they acknowledge the author's approach of separating the API change from its first consumer as a valid alternative. This represents the last piece of feedback before the series received final approval from Karthik Nayak.

Key technical elements include the new flag in `object-name.h` that controls whether `get_oid_basic()` performs refname ambiguity checking, with the flag being set to maintain current behavior by default. The patch is foundational for the series' performance improvements but was noted by the reviewer as potentially unclear in isolation about its specific optimization benefits until seeing the follow-up application. The thread context shows this concern was ultimately resolved through discussion, with the patch remaining separate in the final version.