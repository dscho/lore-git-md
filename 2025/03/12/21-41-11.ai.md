This is a follow-up discussion in the NUL-delimited output feature series, specifically addressing implementation details of the `-z` option compatibility checks in `rev-list`.

Justin Tobler responds to Patrick Steinhardt's suggestion about adding a clarifying comment to the `-z` option validation code in `builtin/rev-list.c`. The current patch includes a check that prevents `-z` from being used with several other incompatible options (graph output, verbose headers, disk usage, etc.), and Patrick notes this should be documented as an implementation limitation rather than a fundamental restriction.

Justin agrees to add a comment indicating these restrictions could potentially be lifted in future implementations, keeping the door open for extending NUL-delimited output support to more `rev-list` modes. This maintains the series' focus on machine-parseable output while acknowledging possible future directions.

The exchange shows the ongoing refinement of implementation details in this feature series, with attention to both current functionality and future extensibility. The thread has already established consensus on the core design (single NUL delimiters, key-value format), and this discussion represents final polishing of the codebase documentation before merging.