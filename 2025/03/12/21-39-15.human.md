This is a technical discussion about implementation details in the ongoing `rev-list -z` feature series, specifically addressing how to handle delimiters in the output format.

Justin Tobler responds to Junio Hamano's feedback about the current implementation's use of both a `nul_delim` flag and a line separator character. Junio had suggested simplifying this to use a single delimiter variable, arguing that maintaining two separate variables adds unnecessary complexity. Justin acknowledges this is a valid point and explains that the output format actually needs to vary in more ways than just the delimiter (like path truncation and quoting behavior), which initially led to the dual-variable approach.

Justin agrees to refactor this in the next version, using a single global variable to store the configured line delimiter. He also notes they'll need a separate variable for metadata delimiters (which are spaces in normal mode), but both can be set to NUL when `-z` is enabled. This appears to be a straightforward implementation improvement that doesn't affect the overall design consensus reached in the thread. The exchange shows healthy technical discussion about code organization without reopening any major design questions.