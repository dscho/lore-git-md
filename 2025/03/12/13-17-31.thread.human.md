This thread discusses Patrick Steinhardt's 3-patch series that aligns the Meson build system's handling of breaking changes with the existing Makefile behavior, particularly around the `-Dbreaking_changes=true` option. The changes focus on three key areas: proper propagation of the `WITH_BREAKING_CHANGES` preprocessor flag, conditional exclusion of the `git-pack-redundant` binary, and corresponding documentation handling.

The series has received positive feedback from Karthik Nayak, who gave an informal ACK. Phillip Wood has been closely reviewing the implementation details, particularly around the documentation handling in patch 3/3. After initially questioning the need to include `manpages_breaking_changes.keys()` in `configured_manpages` (which Patrick explained is required for Meson's sanity checking), Phillip has identified two new technical concerns: potential duplicate entries in `configured_manpages` when breaking changes are enabled, and possible ordering issues in the comparison between `configured_manpages` and `actual_manpages` due to unspecified dictionary key ordering in Meson.

The discussion remains focused on ensuring robust build system behavior, with Phillip's latest questions highlighting edge cases that might need addressing in the current implementation. The core functionality appears sound, but these final details around documentation validation may require minor adjustments to handle all scenarios properly.