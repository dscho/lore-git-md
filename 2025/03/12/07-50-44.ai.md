This is a follow-up discussion in the ongoing NUL-delimited output format thread, specifically addressing implementation details in the `-z` support for `git rev-list`.

Patrick Steinhardt suggests adding a clarifying comment to the error-checking code that rejects `-z` when used with certain incompatible options. He notes that the current limitations are implementation choices rather than fundamental restrictions, leaving room for future enhancements. The code in question enforces that `-z` cannot be combined with options like graph output, verbose headers, disk usage, timestamps, and others.

The comment would serve to document that these restrictions could potentially be lifted in future versions if someone implements the necessary support. This is a minor implementation detail in the larger context of the NUL-delimited output feature, which has seen significant discussion around attribute naming conventions and `--stdin` behavior. The thread has largely settled on technical approaches but continues to refine edge cases and documentation.