This is the foundational first patch in a 16-part performance optimization series for the refs subsystem, introducing API groundwork for subsequent optimizations.

The patch adds a new `repo_get_oid_with_flags()` function that extends the existing `repo_get_oid()` by accepting flags to pass through to `get_oid_with_context()`. This is a straightforward refactoring that doesn't change behavior when called with flags=0, but provides the plumbing needed for later optimizations. The implementation is clean - it simply wraps the lower-level function while maintaining the same error handling pattern.

The commit message is minimal but sufficient for this preparatory change, noting this will be used in a follow-up commit (which we know from thread context will be patch 2/16 introducing the `GET_OID_SKIP_AMBIGUITY_CHECK` flag). The diff shows the mechanical nature of the change - adding the new function while keeping the original as a wrapper, plus adding the header declaration with appropriate documentation.

Key technical details for future reference:
- New function `repo_get_oid_with_flags()` in object-name.[ch]
- Maintains same signature as `repo_get_oid()` but adds `flags` parameter
- Passes flags through to `get_oid_with_context()`
- Original `repo_get_oid()` becomes a trivial wrapper passing flags=0
- Header comment explains it handles object ID expressions like "xyz^"

This is purely infrastructure work with no behavior change yet, but enables the ambiguity check optimizations coming in patch 2/16. The change is uncontroversial and was approved in the final review round.