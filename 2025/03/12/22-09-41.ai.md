This is a follow-up discussion in the ongoing thread about adding NUL-delimited output to `git rev-list`. Justin Tobler responds to Jeff King's compatibility questions about the `-z` option behavior.

The email confirms two key implementation decisions for the v2 series:
1. `-z` will affect both input and output parsing when reading from stdin, aligning with other Git commands' behavior
2. The early parsing of `-z` won't interfere with diff options processing through `setup_revisions()`, maintaining backward compatibility

Technical details include:
- The current implementation leaves `-z` in place for `setup_revisions()` processing while adding early parsing
- Pathspec and revision parsing from stdin will respect `-z` when enabled
- The interaction between early option parsing and `setup_revisions()` is preserved
- The undocumented historical acceptance of `-z` in rev-list is acknowledged as an implementation artifact

This response resolves the remaining open questions from Jeff King, clearing the way for the v2 implementation. The thread has now addressed all major design and compatibility concerns, with consensus favoring the single-NUL approach and full attribute names. The technical solution maintains consistency with Git's existing NUL-delimited formats while properly handling the command's option parsing architecture.