This is a technical discussion about refactoring option parsing in `git rev-list` as part of the NUL-delimited output feature series.

Justin Tobler responds to Junio Hamano's concern about whether combining two option-parsing loops in `cmd_rev_list()` might complicate future fixes to the known broken option validation. The discussion centers on how `-z` parsing needs to happen early to avoid diff options handling, and whether the current approach makes the underlying issue harder to address later.

Justin explains that while the current patch doesn't fix the fundamental issue with `setup_revisions()` lacking proper option validation, it doesn't make the problem worse either. He suggests two potential longer-term solutions: adding a flag to disable diff options parsing in `setup_revisions()`, or separating out its parsing logic entirely - though both would be non-trivial changes. For now, he advocates keeping the current approach as it enables the immediate NUL-delimited output goals without blocking future improvements.

This is a focused technical exchange about implementation tradeoffs in a series that's otherwise nearing completion, showing the maintainers working through subtle architectural questions even as the main feature design has reached consensus.