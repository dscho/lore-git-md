This is a feature implementation refinement in the ongoing NUL-delimited output format thread for `git rev-list`. Junio C Hamano weighs in on the discussion about whether the `-z` option should affect both input and output formats, agreeing with Patrick Steinhardt's position that it should modify both for consistency with other Git commands.

The email notes that some commands use `-Z` to differentiate input vs. output formatting, but suggests that if this distinction isn't made, then having `-z` affect both sides is the least surprising approach. This represents a key design decision point in the series, as the behavior needs to be settled before finalizing the implementation.

The discussion builds on previous technical decisions in the thread including the move from double-NUL to single-NUL delimiters and the key-value attribute structure. Junio's input carries particular weight as the maintainer and as the original suggester of the NUL-delimited approach, indicating this design direction is likely to be accepted.

The email doesn't introduce new code changes but helps solidify one of the remaining open questions from the thread summary - specifically around `--stdin` behavior when `-z` is specified. This positions the series closer to finalization, with the major technical decisions now either implemented or reaching consensus.