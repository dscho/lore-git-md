This is a technical discussion about API parity in the merge-ort/merge-recursive transition series. Patrick Steinhardt provides a detailed code review of the first patch in Elijah Newren's series, focusing on the new `merge_ort_generic()` function and related changes.

The review notes two key technical differences from merge-recursive's implementation: the use of standard `error()` reporting (an improvement) and the absence of a `num_merge_bases == 1` check (a potential gap needing explanation). Patrick also observes that the `get_ref()` helper is copied verbatim from merge-recursive.c.

The discussion touches on subtle behavioral details around ancestor naming and merge base handling, particularly noting how builtin/am's fake ancestor case is now properly accommodated through a relaxed assertion. These changes appear to maintain backward compatibility while cleaning up the interface.

This email advances the thread by identifying specific implementation choices that may need clarification in the commit message, particularly around the merge base handling logic. The tone is constructive and technical, focusing on ensuring behavioral parity during the transition. The changes discussed affect core merge machinery (merge-ort.c, merge-ort-wrappers.c) and have implications for callers like `git am`.