This is a refactoring patch in Elijah Newren's series to establish API compatibility layers for merge-ort, part of the larger effort to remove merge-recursive. The patch adds a new `merge_ort_generic()` function that handles OID-to-commit conversion and index locking while delegating to `merge_ort_recursive()`.

The discussion focuses on two key differences from merge-recursive's implementation: using standard `error()` reporting instead of a custom `err()` function, and omitting a `num_merge_bases == 1` check that was present in the original. Elijah acknowledges this second point needs better explanation in the commit message and will incorporate details from another commit in the series.

The changes also include relaxing an assertion in `merge_incore_recursive()` to accommodate builtin/am's use of a constructed fake ancestor, showing careful attention to maintaining compatibility with existing callers. While the implementation appears solid, the thread highlights the importance of thorough documentation for subtle behavioral differences from merge-recursive, particularly around merge base handling.