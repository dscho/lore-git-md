This is a refactoring patch that reorganizes commit mark clearing functions in commit.c. The change flips the relationship between `clear_commit_marks_many()` and `clear_commit_marks()`, moving the single-commit handling logic into `clear_commit_marks()` and making `clear_commit_marks_many()` call it in a loop.

The key technical change is that the commit list handling code (previously duplicated in the loop of `clear_commit_marks_many()`) is now consolidated into `clear_commit_marks()`, which gets called for each commit. This reduces code duplication and makes the control flow more straightforward - the many-commit function now clearly delegates to the single-commit function rather than the reverse.

The patch touches core commit graph traversal functionality but doesn't change behavior - it's purely an internal code organization improvement. The diff shows a net reduction of 2 lines (9 removed, 7 added) while making the code more maintainable. The change appears well-contained and low-risk, with no apparent impact on performance or functionality.