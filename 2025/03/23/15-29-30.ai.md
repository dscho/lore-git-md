Security hardening patch for git-shell command override functionality.

This v3 patch from Ayman Bagabas implements the final security model for git-shell built-in command overrides, incorporating the thread's security review conclusions. The key change from v2 is replacing the `F_OK` check with `X_OK` for permission validation, as agreed in the discussion. This ensures the file is both present and executable before attempting to run it.

The patch maintains the core functionality - allowing git-shell commands to be intercepted by scripts in the git-shell-commands directory - while hardening the security checks. The implementation follows the established pattern for git-shell-commands, with the override script receiving the same arguments as the original command would have. The TOCTOU race condition risk with `access()` remains documented but accepted as part of git-shell's existing security model.

Technical details show careful attention to the thread's feedback: the `cd_to_homedir()` timing is correct, error handling matches existing patterns, and the security boundaries maintain git-shell's trust model. The patch appears ready for maintainer consideration, representing a methodical progression from initial proposal through security review to final implementation.