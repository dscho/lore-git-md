Here is the dense AI thread summary for this patch:

This thread begins with a refactoring of commit mark clearing functions in commit.c, authored by [author name]. The patch reorganizes the relationship between `clear_commit_marks_many()` and `clear_commit_marks()`, moving the single-commit handling logic into `clear_commit_marks()` and making `clear_commit_marks_many()` call it in a loop. The change consolidates previously duplicated commit list handling code from `clear_commit_marks_many()` into `clear_commit_marks()`, resulting in a net reduction of 2 lines (9 removed, 7 added) while making the control flow more straightforward. This is a pure internal code organization improvement that touches core behavior but does not change functionality - the commit graph traversal remains identical. The change appears well-contained and low-risk, with no expected impact on performance or external behavior. The patch targets core commit graph functionality but is a maintainability-focused change rather than a behavioral one. No reviewers have commented yet on this initial version.