This thread discusses a refactoring of commit mark clearing functions in commit.c. The patch reorganizes the relationship between `clear_commit_marks_many()` and `clear_commit_marks()`, moving the single-commit handling logic into `clear_commit_marks()` and making the many-commit version call it in a loop. This eliminates code duplication while maintaining identical behavior.

The change is purely an internal code organization improvement, reducing the line count by 2 (9 lines removed, 7 added) while making the control flow more straightforward. The patch touches core commit graph traversal code but doesn't modify any functionality - it simply makes the implementation cleaner and more maintainable. The change appears well-contained and low-risk, with no impact on performance or external behavior.