[GSoC 2025 proposal] Consolidate ref-related functionality into git-refs - technical refinement. Zheng Yuting's proposal now examines implementation approaches for the `list` subcommand, analyzing whether to combine filtering/formatting (following `git-for-each-ref`/`git-show-ref` models) or split them into separate `filter`/`show` subcommands. The unified approach risks complexity but maintains familiar UX patterns, while splitting could leverage shared formatting code (analogous to `git-add`/`git-restore` patch handling). Zheng explicitly excludes `--verify`, `--quiet`, and `--exist` options as they're reserved for future subcommands.

Patrick Steinhardt's prior technical constraints remain foundational: backward compatibility, `refs.c`-only modifications, and no subprocesses. His review established key parameters including `optimize` naming for packing and `rev-parse --verify` semantics for `resolve`. Zheng's current analysis demonstrates deeper engagement with the problem space, weighing performance implications and code sharing opportunities while adhering to these constraints. The discussion connects to Patrick's broader ref backend abstraction vision (GitLab #330), with implementation planning now focusing on core `list`/`update` subcommands before expanding functionality.

Open questions center on architectural direction for filtering/formatting - whether unified or split subcommands better serve long-term maintainability. Next steps await mentor feedback on this design choice. Zheng's technical approach continues to evolve through careful analysis of existing commands (`for-each-ref`, `show-ref`) and their interaction patterns, informed by networking/web development experience and prior Git contributions (send-email optimizations). The project remains in technical refinement phase with implementation details being progressively pinned down while maintaining flexibility for mentor guidance.