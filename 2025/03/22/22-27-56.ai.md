This is a bug report about unexpected behavior in `git diff -w` (whitespace-ignoring mode). The reporter, Eugen Konkov, encountered a merge conflict and tried using `git diff -w` to view changes while ignoring whitespace differences. They found that `-w` not only ignored whitespace changes (as documented) but also hid non-whitespace changes (in this case, a variable name change from `name` to `qualified_name`), which they argue should still be shown.

The email includes concrete examples showing the output with and without `-w`, demonstrating how the non-whitespace changes disappear when `-w` is used. The reporter is working on a SQL Translator project and appears to be in the middle of a rebase involving schema-related changes. The affected files are primarily in the `lib/SQL/Translator/` directory, with changes touching table naming and schema handling.

This appears to be a legitimate bug report about `git diff`'s whitespace-ignoring behavior being too aggressive. The reporter provides clear reproduction steps and example outputs, though they don't propose a specific fix. The issue affects the core functionality of `git diff` when used with the `-w` flag, particularly in merge conflict resolution scenarios.