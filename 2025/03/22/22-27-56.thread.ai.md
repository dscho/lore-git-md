Thread identity: Bug report about `git diff -w` behavior from Eugen Konkov, showing unexpected suppression of non-whitespace changes during merge conflict resolution.

Core problem: `git diff -w` (whitespace-ignoring mode) is hiding non-whitespace changes (specifically a variable name change from `name` to `qualified_name`) in addition to whitespace changes when used during merge conflict resolution. This contradicts the documented behavior where `-w` should only ignore whitespace changes.

Current status: Initial bug report with reproduction case showing problematic behavior in `git diff` core command. No maintainer response or proposed fix yet.

Key technical details: 
- Affects `git diff`'s whitespace handling logic in merge conflict scenarios
- Demonstrated on SQL Translator project files in `lib/SQL/Translator/` directory
- Shows concrete before/after examples with and without `-w` flag
- Involves schema-related changes during rebase operation

Open questions:
- Whether this affects all diff modes or just merge conflict viewing
- Root cause in diff algorithm's whitespace filtering
- Appropriate fix scope (merge-specific or general diff behavior)

Participant positions:
- Eugen Konkov (reporter): Provided clear reproduction case showing unexpected behavior

Related work: None mentioned yet, but potentially connects to broader diff algorithm behaviors around whitespace handling.