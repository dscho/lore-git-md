Here's a human-readable thread summary:

This thread reports an unexpected behavior in `git diff -w` where the whitespace-ignoring mode appears to be too aggressive. Eugen Konkov encountered this while resolving merge conflicts in a SQL Translator project, where `git diff -w` not only ignored whitespace changes (as expected) but also hid a substantive variable name change from `name` to `qualified_name` in files under `lib/SQL/Translator/`. 

The reporter provided clear before-and-after examples showing how non-whitespace changes disappear when using `-w`, which contradicts the documented behavior of only ignoring whitespace differences. This appears to be a genuine bug in Git's diff machinery that could affect users relying on `-w` during merge conflict resolution or code review. The thread is just starting, so there aren't yet any responses or proposed fixes from Git maintainers or contributors.