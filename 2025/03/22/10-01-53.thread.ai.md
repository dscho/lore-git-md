Git shell command override proposal (Ayman Bagabas) - Security hardened implementation ready for maintainer review

The thread has progressed from initial proposal through security review to a finalized implementation allowing git-shell built-in command overrides via executable scripts in git-shell-commands directory. The v3 patch incorporates all feedback, most notably replacing the `F_OK` check with `X_OK` for permission validation as agreed in discussion (executable check implies file existence). Core technical elements remain unchanged from v2: shell.c modifications for command dispatch with `make_cmd()`, permission checks, and `execv()` execution.

Security model now fully specified:
- Strict limitation to git-shell-specific command interception
- Implementation follows existing git-shell-commands patterns
- Elijah Newren's architectural concerns addressed through scope clarification
- TOCTOU race condition with `access()` documented but accepted as reasonable within git-shell's existing security model

Implementation details reflect thread consensus:
1. Permission validation uses single `X_OK` check (Chris Torek confirmed sufficient)
2. Execution flow maintains correct `cd_to_homedir()` timing
3. Error handling matches existing code patterns
4. Security boundaries preserve git-shell's trust model without additional restrictions

The discussion demonstrates methodical security analysis, progressing from design questions through implementation review. With all technical concerns addressed and no outstanding objections, the v3 patch represents the hardened final version ready for maintainer consideration. The solution maintains original functionality while incorporating security best practices as determined through community review.