This is a follow-up in a thread about a `git rebase` issue with worktrees.

Andy Shevchenko reports that recent Git versions have resolved his earlier problem where `git rebase` would fail when a branch was checked out in another worktree. The issue, which required manual workarounds involving `--ignore-other-worktrees`, appears to have been fixed in recent releases (though he doesn't specify exactly which version introduced the fix).

The thread references commit b5cabb4a967 ("rebase: refuse to switch to branch already checked out elsewhere") as potentially related to the original issue. While the fix appears to have resolved Shevchenko's workflow problems, the discussion doesn't provide technical details about what changed or whether this was an intentional fix versus a side effect of other changes. The resolution seems to have happened naturally through Git's development process rather than through direct intervention on this specific issue.