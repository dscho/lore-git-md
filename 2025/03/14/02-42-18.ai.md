This is a technical follow-up in the ongoing discussion about reproducible Git bundles, focusing on implementation details for two verification approaches.

Jeff King provides additional technical guidance for both the bitstream verification and content verification methods discussed earlier. For bitstream verification, he notes that the current `repack` approach may include unwanted objects and suggests creating a minimal repository first via `clone --bare --no-local --single-branch` to isolate only the desired refs. He also demonstrates a lower-level alternative using direct pack-objects manipulation that bypasses `git-bundle` entirely.

For content verification, King explores how to apply the `rev-list`/`cat-file` pipeline to bundle files, though notes it requires creating temporary repositories to properly index and examine the packfile contents. He presents several implementation options with varying levels of Git command involvement, from high-level fetches to manual packfile manipulation.

The email highlights practical tradeoffs between implementation complexity and trust in Git's plumbing commands. King maintains a neutral stance on which approach to prefer, instead focusing on making the technical constraints and requirements clear for each method. The suggestions aim to help Simon Josefsson implement either verification strategy while being aware of edge cases like repository state contamination and bundle format internals.

Key technical details mentioned include: `--no-reuse-delta` pack option, bundle v2 format structure, `index-pack` requirements, and temporary repository creation patterns. The discussion remains grounded in the thread's established context of supporting both bitstream and content verification models.