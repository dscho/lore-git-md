This is a build system improvement discussion about enabling unreachable code warnings. Patrick Steinhardt responds to Junio Hamano's preference for keeping `-Wunreachable-code` enabled everywhere (rather than macOS-specific exemptions) by proposing to add the warning flag to Meson builds as well.

The thread has evolved from initial warnings implementation to platform-specific considerations, with Junio now favoring a short-term solution using `errno` checks while keeping the warning active. Patrick agrees with this direction and extends it to Meson, showing the warning can be consistently applied across both Makefile and Meson build systems.

The patch itself is straightforward - a one-line addition to `meson.build` to mirror the warning flag already in `config.mak.dev`. This maintains build consistency while the team evaluates the warning's long-term usefulness. The discussion shows the Git project's careful approach to compiler warnings - balancing detection of real issues against maintenance overhead, with multiple solutions being evaluated.