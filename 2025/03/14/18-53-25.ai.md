This is a continuation of the build system improvement discussion around `-Wunreachable-code` warnings, specifically addressing the macOS `sigfillset()` issue.

Jeff King responds to Junio Hamano's preference for using `errno` checks as a short-term solution over platform exemptions. He agrees this approach is preferable for now, though acknowledges it may require case-by-case fixes ("wack-a-mole"). Jeff notes Junio's proposed `CAN_BE_TAKEN()` macro would be a more general solution but may be overkill if this is the only case. He invites Junio to submit a patch implementing that approach.

The email reinforces the current consensus to:
1. Keep the warning enabled everywhere
2. Prefer targeted code fixes over platform exemptions
3. Consider more architectural solutions like `CAN_BE_TAKEN` for future cases

Key technical points:
- The specific issue involves `sigfillset()` on macOS being optimized as always-successful
- Current solution uses `errno` checking to make the error path reachable
- Maintainer prefers this over macOS-specific compiler flag exemptions
- Future direction may involve a generic `CAN_BE_TAKEN` macro for similar cases

The discussion remains focused on balancing warning utility against maintenance burden while exploring both immediate fixes and systematic solutions.