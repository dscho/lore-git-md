This is a build system improvement patch in the ongoing discussion about unreachable code detection. Patrick Steinhardt responds to Junio Hamano's preference for keeping `-Wunreachable-code` enabled everywhere with targeted fixes rather than platform exemptions. He agrees with this approach and proposes extending the warning to Meson builds by adding `-Wunreachable-code` to the Meson warning flags.

The patch is minimal (just 1 line added to `meson.build`) but aligns the Meson build system with the Makefile-based build's handling of this warning. This continues the thread's progression from initial warning implementation through platform-specific issues to a consistent cross-build-system solution. The discussion shows consensus forming around keeping the warning active while addressing false positives case-by-case, with Patrick now ensuring Meson users get the same benefits as Makefile users.

Key technical points for future reference:
- The warning helps catch real issues (like the packed backend problem found earlier)
- macOS requires special handling (via errno checks) due to sigfillset() implementation
- The Meson addition mirrors existing Makefile configuration
- Maintainer prefers this over platform exemptions as a general policy
- The thread shows evolving consensus on warning utility vs maintenance cost