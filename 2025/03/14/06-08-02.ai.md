Bugfix for a diffcore-rename issue triggered by `-B --follow` options.

This email is part of an ongoing thread about a BUG() assertion failure in diffcore-rename.c when using `git log` with `-B --follow` options on a specific path. The issue was originally reported by Olaf Hering and bisected by Jeff King to commit 9db2ac5616 ("diffcore-rename: accelerate rename_dst setup", 2020-12-11). 

Elijah Newren responds with a fix (referencing a pull request) for the problem, though notes the response was delayed. The bug appears to involve broken pair tracking in the rename detection logic when using these specific diff options together. The fix is presumably in the linked pull request, though the email doesn't describe the technical details of the solution.

This appears to be a legitimate bug in the rename detection machinery that could affect users combining these diff options. The thread shows proper debugging and bisecting was done to identify the problematic commit, and now a fix is being proposed by the original author of the code in question. The issue seems narrowly scoped to this specific combination of diff options triggering the BUG() assertion.