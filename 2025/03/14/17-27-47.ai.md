This is a follow-up in the ongoing discussion about build system improvements for unreachable code detection, specifically addressing a macOS-specific issue with `sigfillset()` warnings.

Junio Hamano proposes an alternative approach to Jeff King's solution for handling the `sigfillset()` warning on macOS. While acknowledging that Jeff's errno-based solution is technically correct, Junio suggests a more generic mechanism that could be reused for similar compiler-smartness issues. The proposal involves creating a dummy variable that the compiler can't optimize away, which would make the conditional appear reachable. This would avoid needing platform-specific workarounds or verbose comments at each occurrence.

The email shows the discussion evolving from the initial problem (clang warnings on macOS) through multiple solution iterations. Junio's suggestion represents a middle ground between platform-specific exclusions and the current errno-based workaround, aiming for a more maintainable long-term solution. The core technical challenge remains balancing POSIX compliance with compiler warning cleanliness across different platforms.

Key technical details preserved:
- The specific `sigfillset()` issue in run-command.c
- macOS's documented behavior versus POSIX requirements
- Two competing solution approaches (errno check vs dummy variable)
- Potential for a future `CAN_BE_TAKEN` macro abstraction
- Maintainer's preference for generic solutions over one-off fixes

This continues the thread's theme of evaluating warning utility versus maintenance cost, with the maintainer now considering architectural approaches to handle similar cases systematically. The FreeBSD behavior question and long-term warning policy remain open.