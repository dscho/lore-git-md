This is part of Taylor Blau's ongoing series to implement incremental MIDX bitmaps (v4, patch 8/13). The patch makes a straightforward but important change to how disk usage is calculated when working with multi-pack index (MIDX) bitmaps.

The modification updates two locations in pack-bitmap.c to use `nth_midxed_pack()` instead of directly accessing the MIDX's packs array. This aligns with the incremental MIDX architecture where packs may be spread across multiple MIDX layers. The change ensures disk usage calculations work correctly when objects are stored in base MIDX layers rather than just the current one.

The patch is small (just 2 lines changed) but builds on the incremental MIDX infrastructure established in earlier patches. It follows the same pattern seen in previous commits of the series, consistently using the `nth_midxed_pack()` helper for pack lookups. The change appears well-contained and maintains the existing functionality while properly supporting the new MIDX bitmap architecture.

This continues the series' focus on methodically updating all MIDX-related code paths to handle the incremental case, with each patch tackling a specific component. The change is straightforward and unlikely to be controversial given it follows the established pattern from previous patches in the series.