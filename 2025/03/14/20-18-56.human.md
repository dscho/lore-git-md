Performance optimization in Taylor Blau's MIDX bitmap series (12/13). This patch builds on the type-level bitmap tracking arrays introduced in the previous commit, switching from basic `ewah_iterator` to the new `ewah_or_iterator` for traversing object type bitmaps. The change affects several key functions that handle object enumeration and filtering, including `show_objects_for_type()`, `filter_bitmap_exclude_type()`, and disk usage calculations. Each modified function now properly initializes and frees the iterator, maintaining the series' careful memory management patterns. This is a technical plumbing change that enables efficient cross-layer bitmap operations, continuing the incremental implementation of the MIDX bitmap architecture. The patch appears well-contained and follows the established patterns of the series.