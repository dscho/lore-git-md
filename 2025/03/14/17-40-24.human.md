This is a follow-up in the ongoing discussion about enabling `-Wunreachable-code` warnings in Git's development builds. 

Junio Hamano responds to Jeff King's proposed workaround for a macOS-specific compiler warning about `sigfillset()` always succeeding. While initially forgetting to state his preference, he now explicitly favors Jeff's approach of using an `errno` check to handle the warning over simply exempting macOS developers from the warning. 

The email shows the maintainer's evolving stance on handling compiler warnings - preferring targeted fixes that maintain warning coverage across platforms rather than platform-specific exemptions, even if temporary. This continues the thread's theme of balancing warning utility against maintenance burden, with Junio now leaning toward keeping the warning enabled more broadly while addressing individual false positives. 

The discussion remains focused on finding robust solutions that work across compilers and platforms, with macOS's non-standard `sigfillset()` behavior being the immediate case in point. No new technical details are introduced here - it's primarily a clarification of the maintainer's preferred direction from the options previously discussed.