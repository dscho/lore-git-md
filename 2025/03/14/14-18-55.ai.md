This is a follow-up to the Windows reftable backend compatibility bugfix discussion, providing additional context about Git for Windows' handling of the unlink prompt issue. Christian Reich notes that while Patrick Steinhardt's v2 patch (which introduced the `MINGW_DONT_HANDLE_IN_USE_ERROR` flag for reftable) hasn't been merged yet, Git for Windows has independently implemented a solution in commit dba1473162 that prevents multiple prompts by caching the user's response. The email also mentions the alternative of using `GIT_ASK_YESNO` but notes its performance impact due to sleeps.

The thread context shows this builds on the consensus solution where Patrick's patch specifically targets reftable behavior while leaving system-wide Windows behavior for future consideration. Christian's update confirms the problem is being addressed from multiple angles, with Git for Windows taking a different but complementary approach to the same underlying issue. The core technical details remain the reftable-specific flag and modified `mingw_unlink()` behavior from Patrick's patch, while this email adds information about parallel developments in the Git for Windows fork.