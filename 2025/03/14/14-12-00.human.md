This is a bugfix series addressing several issues in Git's promisor-remote capability handling. Christian Couder has split the work into three focused patches based on discussion from previous iterations, showing good responsiveness to feedback.

The series fixes a NULL pointer dereference when promisor remotes lack URLs (patch 1), handles edge cases where no URL is advertised (patch 2), and ensures case-sensitive comparison of remote names (patch 3). The changes touch documentation, core promisor-remote logic, and add substantial test coverage.

The thread history shows this has evolved through multiple rounds of review, with Junio pushing for architectural simplification. The current version appears to have converged on a clean solution that removes special cases while maintaining safety. The test additions (59 new lines) give confidence in the fixes.

This looks ready for inclusion - it addresses a real crash scenario with careful consideration of edge cases, and the split into logical patches makes review easier. The only remaining open question appears to be about config vs runtime case sensitivity, which seems non-blocking for these safety fixes.