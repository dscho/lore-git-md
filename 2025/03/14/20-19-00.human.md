This is the final patch (13/13) in Taylor Blau's v4 series implementing incremental MIDX bitmaps, completing the feature by adding write support. The series enables efficient bitmap operations across multi-pack index layers through a structured bitmap design.

The patch teaches the pack-bitmap-write machinery to create incremental MIDX bitmaps, building on the read support added earlier in the series. Key changes include:
- `find_object_pos()` now handles objects from previous layers by using MIDX parameter to locate their bit positions
- `bitmap_writer_build_type_index()` offsets bits by preceding objects count for proper OR-ing across layers
- Pack order field moves into `write_midx_context` to simplify callers
- New test cases verify bitmap functionality across MIDX layers

The implementation appears thorough, with careful attention to maintaining the series' architectural patterns. Test coverage includes verification of bitmap operations across layers, object access from different packs, and clone behavior with various pack reuse settings. The patch completes the core functionality for this phase of the incremental MIDX bitmap work.