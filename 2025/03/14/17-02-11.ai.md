This is a design discussion about NUL-delimited I/O behavior in `git rev-list` versus `git log`, occurring after the main implementation work is complete. The thread has reached consensus on the current approach where `-z` handles both input and output delimiters in `rev-list`, despite some inconsistency concerns with `git log`'s behavior.

Junio Hamano weighs in with several key points:
1. Notes that while developers might expect consistency between `rev-list` and `log`, end users haven't complained about their current behavioral differences
2. Suggests a more explicit long-form option design (`--nul-delimited-{output,input}`) would be ideal plumbing
3. Proposes that `-z` could start as output-only for consistency, with the design allowing future independent control

The discussion appears to be converging on accepting the current implementation's approach, with Junio providing architectural guidance about how to potentially evolve the interface in the future. This is now more about forward-looking design philosophy than blocking the current series, as Jeff King has already approved the implementation and Justin Tobler has addressed all technical concerns. The thread shows the Git maintainers thinking about long-term interface consistency while pragmatically accepting the current solution.