This is a continuation of the build system improvement discussion around `-Wunreachable-code` warnings, specifically addressing platform-specific behavior of `sigfillset()`.

The email from Junio Hamano provides maintainer feedback on Jeff King's proposed solution for macOS builds failing due to clang detecting unreachable`die_errno()` calls after `sigfillset()`. Hamano expresses a preference for Jeff's approach of using `errno` checks to handle the warning (rather than platform-specific exemptions) as a short-term solution, though he had previously suggested a more architectural dummy-variable approach for similar cases.

Key technical points:
- The specific issue involves `sigfillset()` on macOS always returning success, making error handling code unreachable
- Maintainer prefers keeping the warning enabled everywhere with targeted fixes rather than platform exemptions
- The `errno`-based workaround is seen as acceptable short-term while longer-term solutions are considered
- This maintains the thread's progression from problem identification through multiple solution iterations
- The discussion continues to balance warning utility against maintenance burden

The email reinforces the maintainer's evolving position on handling compiler warnings while preserving the warning's ability to catch real issues like the packed backend problem found earlier. It keeps the focus on practical solutions while larger architectural approaches are considered.