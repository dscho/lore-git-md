This is a follow-up discussion in the NUL-delimited output format thread, specifically addressing command consistency concerns. Jeff King (peff) is responding to Justin Tobler's consideration of input/output behavior differences between `git log` and `git rev-list` when using `-z`.

The email confirms that while there are existing inconsistencies in how Git commands handle `-z` (some using it only for output while others like `grep` use it for both input and output), the current implementation approach has been considered acceptable. Jeff King acknowledges that his earlier concerns about potential inconsistency have been addressed through discussion, and he approves of the current direction where `git rev-list`'s `-z` will handle both input and output delimiters.

Key points for future reference:
- The thread has resolved the question about `-z` behavior consistency between commands
- The implementation in Justin's v2 patches correctly reflects the agreed approach
- No further changes are requested - this appears to be the final word on this particular aspect of the series

This email marks consensus on one of the last open questions in the thread, suggesting the series is now ready for integration with all major concerns addressed. The broader discussion about standardizing `-z` behavior across Git commands remains an open but non-blocking philosophical question.