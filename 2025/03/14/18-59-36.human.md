Bugfix refinement in promisor-remote test suite. Junio Hamano provides final review comments on Christian Couder's v5 patch series, focusing on test case improvements.

The review notes two test ordering issues where `test_when_finished` cleanup commands should appear before the operations they're meant to clean up, particularly important for the clone operation where failure would skip cleanup. While the impact is minimal for config operations (where corruption is unlikely), the suggestion improves robustness.

Junio also observes that two test cases for URL handling (empty vs unset URLs) are nearly identical in their clone operations, differing only in server configuration setup. This duplication is noted as acceptable since it verifies equivalent behavior between empty and unset URL cases.

The review maintains the thread's high standard of attention to detail while confirming the architectural direction established earlier - rejecting invalid configurations rather than handling edge cases. These final tweaks to the test suite represent the last polish before the series can be merged.