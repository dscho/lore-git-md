This is a design discussion follow-up in the ongoing NUL-delimited output format feature series. Justin Tobler responds to Jeff King and Junio Hamano's interface design discussion about how to structure the NUL-delimited I/O options in `git rev-list`.

The core discussion revolves around whether to use a single `-z` flag (currently implemented in the series) or introduce more explicit long options to control NUL-delimited behavior separately for input and output. Justin proposes a compromise approach using a `--nul-delimited` option with sub-parameters (`all`, `input`, `output`) that would allow granular control while maintaining a simple default behavior. This would replace the current `-z` implementation while addressing Junio's preference for explicit control and Jeff's desire to maintain convenience.

Key technical details mentioned include:
- The proposed syntax `--nul-delimited={all,input,output}`
- The default behavior would affect both input and output (matching current `-z` behavior)
- This would require modifying the current patch series to remove `-z` support
- The discussion represents final interface design considerations before merging
- All technical implementation aspects are settled
- The proposal attempts to balance explicit control with usability
- Would require changes to documentation and tests from current implementation

This email shows the series is nearing completion, with only the command-line interface syntax remaining to be finalized. The implementation appears flexible enough to accommodate either approach, with Justin offering to prepare a version using the new option format if desired.