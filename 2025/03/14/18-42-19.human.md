CI infrastructure issue follow-up. Jeff King reports seeing CI failures in the documentation build job across both 'jch' and 'seen' branches, where the build script fails because `meson` is not found in the CI environment. This appears to stem from Patrick Steinhardt's recent commit adding Meson documentation build tests, which assumes Meson is available but the CI setup hasn't been updated to include it. 

The issue is straightforward - the CI environment needs Meson installed to match the new test requirements. Jeff notes this isn't a problem with the documentation build logic itself, just a missing dependency in the test infrastructure. He meant to include Patrick in the discussion but initially forgot to CC him on the email. 

This is a minor but important CI configuration gap that needs to be addressed before the Meson documentation testing can properly work in the automated builds. The fix will likely involve updating the CI setup to install Meson alongside other build dependencies.