This is part 3/12 in Patrick Steinhardt's ongoing series to remove `the_repository` usage from object APIs, specifically targeting pack-writing functionality. The patch converts pack-write.c and its callers to explicitly pass repository parameters instead of relying on globals.

The changes systematically replace `the_hash_algo` and `the_repository` usage in pack-writing operations with repository-aware variants. Key functions modified include:
- `write_idx_file()` and related index writing utilities
- `write_rev_file()` for reverse index generation
- `create_tmp_packfile()` and `stage_tmp_packfiles()` for pack creation
- Various supporting functions for hash writing and file handling

The patch updates 7 files with 55 insertions and 41 deletions, touching core pack operations across fast-import, index-pack, pack-objects, bulk-checkin, and midx-write. Each modified function now takes a `struct repository*` parameter, with callers updated to pass `the_repository` where appropriate (maintaining current behavior while enabling future multi-repo work).

This continues the series' pattern of methodically converting subsystems while preserving existing functionality. The changes are mechanical but thorough, covering all pack-writing paths. The patch demonstrates good test coverage through its impact on multiple pack-related commands and maintains compatibility with the wider codebase by passing through `the_repository` at call sites.