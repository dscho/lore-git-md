Bugfix follow-up addressing `git pack-redundant` deprecation in the meson build system. This is a continuation of the thread finalizing the handling of deprecated features under `WITH_BREAKING_CHANGES`.

Phillip Wood notes that while Junio Hamano's patch fixed the Makefile's handling of `git pack-redundant` exclusion, the meson build system was missed in the original deprecation commit (68f51871df8). The command is still being compiled and documented in meson builds despite being marked for removal. Phillip also suggests adding `#ifndef WITH_BREAKING_CHANGES` guards around the function declaration in builtin.h, though considers this less urgent.

The patch appears straightforward and addresses an oversight in maintaining consistency between build systems. It's a small but important piece of the larger deprecation effort, ensuring the build systems behave identically when configured with breaking changes. The thread has already established consensus on the approach, so this follow-up is likely to be accepted without further discussion.