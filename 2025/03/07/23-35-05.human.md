Continuing the `the_repository` removal effort, this patch converts `builtin/ls-files.c` to stop using the global repository variable. The changes follow the established pattern seen in previous conversions in this series, passing the repository parameter through to helper functions and maintaining NULL repository handling for help text display.

The patch updates two key functions - `expand_objectsize()` and `show_ru_info()` - to accept a repository parameter instead of relying on `the_repository`. It also converts several other repository accesses throughout the command to use the passed-in repository. The changes maintain all existing functionality while removing the global dependency.

Test coverage is added to verify the NULL repository case works correctly when displaying help text, matching the pattern seen in other converted commands. The implementation appears clean and consistent with the architectural approach demonstrated in earlier patches of this series.

This conversion shows the pattern working well for a moderately complex builtin command that handles both repository and non-repository cases, further validating the approach for future `the_repository` removal work.