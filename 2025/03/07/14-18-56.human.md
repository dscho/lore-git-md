Part of the ongoing `the_repository` removal effort, this patch continues Patrick Steinhardt's series to eliminate global repository state from Git's object handling code. Specifically, it removes implicit dependencies on `the_repository` and `the_hash_algo` from pack-writing functionality.

The patch modifies pack-write.c and its callers to explicitly pass repository parameters rather than relying on globals. Key changes include converting functions like `write_idx_file()`, `write_rev_file()`, and `create_tmp_packfile()` to take a `struct repository*` parameter. The changes are mechanical but widespread, touching 7 files with 41 insertions and 41 deletions.

This is patch 3/12 in the v3 series, which has already seen substantial review and iteration. The changes here are consistent with the series' overall goal of making Git's object handling code repository-agnostic, a prerequisite for future work like pluggable backends. The patch appears well-constructed, with thorough updates to all call sites and no obvious omissions.