Bugfix for SMTP authentication error handling in `git send-email`. This patch from Zheng Yuting improves how temporary vs permanent authentication failures are distinguished when sending email via SMTP. The change modifies the `smtp_auth_maybe()` function to better handle network-related issues separately from credential problems.

The key improvement is classifying certain error messages (like timeouts, greylisting, or network issues) as temporary failures that warrant retries, while treating other errors as permanent authentication failures. This prevents false negatives where temporary issues would incorrectly mark valid credentials as invalid. The implementation adds specific error pattern matching and separates the SASL and plain authentication error paths.

As a first contribution from a new contributor, this appears to be a well-scoped change focused on a specific pain point. The 30-line diff (+22/-8) maintains existing behavior for successful cases while adding more robust error handling. Reviewers will likely examine the specific error classification logic and whether the pattern matching covers common SMTP server responses. The patch builds on the existing credential helper infrastructure without introducing new dependencies.