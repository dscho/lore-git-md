Git reflog management enhancement (2 patches) introducing a 'drop' subcommand to `git reflog` for deleting entire reflogs, with '--all' and '--single-worktree' flags. Originating author Karthik Nayak addresses the limitation where users can only expire individual entries but not remove reflogs entirely, particularly useful for repositories with core.logAllRefUpdates=false.

First patch implements the 'drop' subcommand functionality with comprehensive test coverage and documentation. Second patch improves error messaging in `git reflog expire` for invalid references. All substantive review feedback from Patrick Steinhardt has been addressed, including documentation reorganization, error handling improvements, and test coverage.

Final discussion resolves remaining UX concerns about worktree flag behavior between Christian Couder and Junio Hamano. They agree to proceed with current implementation where `--single-worktree` silently does nothing without `--all`, accepting this suboptimal behavior for consistency with the existing `expire` subcommand. Two future improvement paths remain open: 1) maintain current behavior as-is for consistency, or 2) first improve `expire`'s behavior then align `drop`. Christian expresses openness to either approach, noting this is a minor polish that doesn't affect technical correctness.

Follow-up discussion with Toon Claes confirms test coverage decisions regarding edge case behavior: 1) silent no-op when neither `--all` nor refs specified (deferred for consistency with `expire`), 2) HEAD reflog existence verification in worktree tests (useful but not worth re-roll), and 3) use of `test-tool ref-store` versus Git commands in verification (interchangeable). Technical details confirmed include test coverage for `--all` with multiple worktrees, interaction between `--all` and `--single-worktree`, and error handling for incorrectly specified references with `--all`.

Toon Claes previously identified additional edge cases and test verification points, though these don't block the current implementation. The naming of `--single-worktree` (versus Christian's suggested `--current-worktree`) remains an open discussion point for future consideration, but doesn't impact the current series' readiness.

All architectural and implementation concerns have been resolved through reviews from Patrick Steinhardt, Christian Couder, and Toon Claes. Junio's response indicates this is now merge-ready, with potential follow-up work to unify worktree flag behavior across reflog subcommands. The series is positioned for inclusion after feature freeze, with no remaining technical blockers. Comprehensive test coverage and documentation updates are in place, including final verification of edge case behaviors matching `expire`'s implementation for consistency.