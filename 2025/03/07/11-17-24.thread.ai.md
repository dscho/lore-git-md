Git reflog management enhancement (2 patches) introducing a 'drop' subcommand to `git reflog` for deleting entire reflogs, with '--all' and '--single-worktree' flags. Originating author Karthik Nayak addresses the limitation where users can only expire individual entries but not remove reflogs entirely, particularly useful for repositories with core.logAllRefUpdates=false.

First patch implements the 'drop' subcommand functionality with comprehensive test coverage and documentation. Second patch improves error messaging in `git reflog expire` for invalid references. All substantive review feedback from Patrick Steinhardt has been addressed, including documentation reorganization, error handling improvements, and test coverage.

Christian Couder raised naming concerns about the `--single-worktree` flag, suggesting `--current-worktree` might be clearer, while noting the flag's behavior (only effective with `--all`) matches the existing `expire` subcommand. Toon Claes identified additional edge cases and test verification points, though these don't block the current implementation.

Junio Hamano now weighs in on the worktree flag discussion, acknowledging the current behavior (silent no-op without `--all`) is suboptimal but consistent with `expire`. He presents two paths forward: 1) maintain consistency with `expire` as-is, or 2) first improve `expire`'s behavior then align `drop`. The series is otherwise ready for inclusion after feature freeze, with this being a minor UX polish consideration that doesn't affect the core functionality.

Open questions remain about the long-term naming and behavior of worktree-related flags across both `drop` and `expire` subcommands, though these don't impact the current implementation's technical correctness. All architectural and implementation concerns have been resolved, with Patrick Steinhardt, Christian Couder, and Toon Claes having provided thorough review feedback that has been incorporated. Junio's response positions this as a candidate for merging with potential follow-up work to unify the worktree flag behavior across reflog subcommands.