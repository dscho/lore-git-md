This is part of the ongoing `the_repository` removal effort, specifically targeting `builtin/ls-files.c` in patch 6/8 of Usman Akinyemi's v4 series. The patch follows the established pattern seen in previous conversions, removing the global variable in favor of passing repository parameters through function calls.

The changes focus on two main areas: `expand_objectsize()` and `show_ru_info()` functions, which previously relied on `the_repository`. The patch modifies these to take a `repository` parameter instead. It also updates all call sites to pass the repository context through, including the NULL repository case for `-`h` help text display. The conversion maintains the series' pattern of handling NULL repositories for help text while ensuring proper repository context for actual operations.

Test coverage is added to verify the NULL repository case works correctly, matching the approach taken in other converted commands. The changes are straightforward mechanical conversions following the architectural principles established earlier in the series, with Christian Couder continuing in his mentorship role. This patch demonstrates the pattern's applicability to another builtin command while maintaining behavioral parity.