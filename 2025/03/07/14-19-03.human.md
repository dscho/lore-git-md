This is part of Patrick Steinhardt's ongoing series to reduce `the_repository` usage in object APIs, specifically focusing on hash algorithm infrastructure. 

The patch moves all hash algorithm implementation code from `object-file.c` to a new `hash.c` file, keeping the header declarations in `hash.h`. This is a pure code organization change with no functional modifications - the diff shows 283 lines moved verbatim from one file to another, plus the necessary build system updates. The motivation is to better separate concerns, making the hash subsystem implementation more discoverable rather than being buried within object file handling code.

This change fits into the larger series by helping disentangle hash algorithm handling from repository-specific code. While it doesn't directly remove `the_repository` usage itself, it creates cleaner boundaries that will make subsequent refactoring easier. The patch appears mechanically sound - it's a straightforward file split with comprehensive test coverage already existing for the moved functionality.

As the 10th patch in a 12-part series that's now in its third iteration, this represents a mature piece of the broader `the_repository` elimination effort. Reviewers have already vetted the architectural approach in earlier rounds, so this organizational change should be uncontroversial.