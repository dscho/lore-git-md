Build system follow-up addressing `git pack-redundant` deprecation consistency. This email from Phillip Wood responds to Junio Hamano's patch fixing the Makefile handling of `git pack-redundant` under `WITH_BREAKING_CHANGES`. While acknowledging Junio's fix for the `EXCLUDED_PROGRAMS` list, Phillip points out that the meson build system was similarly overlooked in the original deprecation commit (68f51871df8) and still compiles the redundant command. He also notes the function declaration in `builtin.h` should ideally be wrapped in the same conditional, though considers this less urgent.

The discussion continues the thread's theme of ensuring consistent handling of deprecated features across build systems and documentation. Phillip's observations complete the technical review of the `pack-redundant` deprecation, identifying the remaining meson build system gap while implicitly confirming the Makefile solution is correct. The exchange demonstrates the careful cross-build-system validation needed when removing features, with both contributors catching different aspects of the same underlying issue.

Key technical details:
- Meson build still compiles `builtin/pack-redundant.c` despite deprecation
- `cmd_pack-redundant()` declaration in `builtin.h` lacks `WITH_BREAKING_CHANGES` guard
- Original deprecation commit was 68f51871df8
- Follows Junio's Makefile fix adding `git-pack-redundant` to `EXCLUDED_PROGRAMS`

This appears to be the final technical exchange in the thread, with all implementation issues now identified and the remaining work (meson build update and header guard) being straightforward follow-ups.