This is a build system patch addressing the final loose end in the `WITH_BREAKING_CHANGES` implementation for deprecating `git pack-redundant`. The email confirms Phillip Wood's observation that while `GIT-BUILD-OPTIONS` correctly stored the breaking changes setting, it wasn't being properly propagated to C compilation, causing `pack-redundant` to still be built when it shouldn't be.

The patch (not shown in the email but referenced in the thread) completes the deprecation by ensuring `builtin/pack-redundant.c` is excluded from compilation when breaking changes are enabled, matching the behavior already implemented in the Makefile build system. This brings the Meson build system to parity with Make for this deprecation, following the pattern established in commit 68f51871df8.

Junio Hamano acknowledges Phillip Wood's work on this issue while noting the broader Meson build system still needs attention from other contributors. The email suggests the technical work on this particular deprecation path is now finished, with the only remaining gap being the Meson documentation build limitation (which was previously identified as non-blocking).

Key technical details for future reference:
- The fix involves proper propagation of `WITH_BREAKING_CHANGES` to C compilation
- Maintains consistency between Makefile and Meson build systems
- Completes the deprecation path for `git pack-redundant` that began with 68f51871df8
- Build system now properly excludes the command when breaking changes are enabled
- Documentation builds still work as expected despite the compilation exclusion

The thread appears to have reached its conclusion on this specific issue, with all critical implementation points addressed and only non-blocking documentation build issues remaining for Meson.