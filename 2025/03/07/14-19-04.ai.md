This is a minor bugfix patch in the ongoing `the_repository` removal series, addressing compiler warnings in the newly split hash.c file.

The patch fixes several "-Wsign-compare" warnings in hash.c by changing loop counters and function parameters from `int` to `size_t` where appropriate. The changes are straightforward and mechanical:
- Updates the counter type in `hash_algo_by_name()` and `hash_algo_by_id()`
- Changes the parameter type of `hash_algo_by_length()` to match
- Removes the now-unneeded `DISABLE_SIGN_COMPARE_WARNINGS` define
- Maintains all existing functionality while making the type signatures more precise

This is patch 11/12 in the v3 series converting object-related APIs away from `the_repository`. While small, it demonstrates the attention to detail in the series - catching and fixing compiler warnings even in newly created files. The changes are uncontroversial and align with the series' goal of clean, warning-free code.

The patch follows the previous split of hash.c from object-file.c (patch 10/12) and precedes the final patch in the series. It shows the author is maintaining high code quality standards throughout this large refactoring effort.