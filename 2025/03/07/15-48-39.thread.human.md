**Thread Summary:**

This thread follows Elijah Newren's effort to replace Git's `merge-recursive` strategy with the newer `merge-ort` implementation, focusing on converting `git-am` as a concrete demonstration. The series has evolved through multiple iterations, addressing API compatibility, rename detection, directory rename edge cases, and output verbosity. The latest patch marks the completion of this work, switching `git-am` to use `merge-ort` while maintaining behavioral parity with `merge-recursive`.

Key developments include:
- The introduction of `merge_ort_generic()`, a drop-in replacement for `merge_recursive_generic()`, which handles git-am's unique cases like constructed merge bases and configurable ancestor labeling.
- Improved rename detection, including support for `no-renames` and `merge.renames` config, with thorough test coverage verifying edge cases.
- Fixes for directory rename handling when `merge.directoryRenames=false`, ensuring proper behavior without optimization-induced failures.
- Refined output verbosity control in `merge-ort`, suppressing conflict messages when not needed while maintaining backward compatibility.

The final patch makes the actual switch in `builtin/am.c`, requiring minor test adjustments:
- `t4151` now removes an untracked file preemptively, as `merge-ort` correctly detects and aborts on such conflicts (unlike `merge-recursive`).
- `t4255` removes a `KNOWN_FAILURE` marker, as `merge-ort` properly handles submodule cases.
- `t6427` updates expected conflict marker text to reflect the new ancestor labeling.

The series has achieved its goal of validating `merge-ort` as a robust replacement for `merge-recursive` in `git-am`, with all identified issues resolved and test coverage expanded. The work demonstrates `merge-ort`'s stricter conflict detection and improved handling of edge cases, paving the way for broader adoption across Git's merge machinery.