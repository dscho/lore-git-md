Junio C Hamano's response in the ref exclusion pattern matching discussion. This is a follow-up in the bugfix series addressing inconsistencies in `--exclude` behavior across Git's ref backends.

The email confirms Patrick Steinhardt's clarification about the exclusion matching logic - that lower layers should only exclude refs that clearly match patterns, rather than the previous overly loose matching that could incorrectly filter valid refs. Hamano acknowledges this corrects his earlier confusion about false positives/negatives in the exclusion context versus pathspec matching.

A key concern raised is the files backend's optimization depending on packed state, which could lead to untested edge cases since tests might not cover all packing scenarios. Hamano recognizes this as an inherent challenge with backend-specific optimizations, where each storage system (files, packed, reftable) may implement exclusions differently while maintaining correctness at the upper layer.

The discussion highlights the architectural tension between allowing backend-specific optimizations versus maintaining consistent observable behavior. While the current series is technically sound, it surfaces deeper questions about optimization-dependent behavior that may warrant future attention, particularly around testing strategies for backend-specific behaviors.