This is a technical discussion about the final patch in Patrick Steinhardt's series to remove `the_repository` dependencies from object APIs, specifically focusing on the `null_oid()` conversion and its interaction with parallel refactoring work.

The email responds to Junio Hamano's observation about a conflict with Usman Akinyemi's parallel `builtin/ls-files.c` refactoring. Patrick investigates whether submodules can use different hash algorithms than their superprojects, demonstrating through practical examples that while technically possible, the current implementation leads to broken behavior (segfaults, malformed commit hashes). He concludes that submodules should use the superproject's hash algorithm when initializing with `repo_submodule_init()`, since the null OID parameter refers to a superproject object.

Key technical points:
- The `show_submodule()` function in `ls-files.c` creates an indirect `the_repository` dependency through `the_hash_algo`
- Submodule hash format compatibility is theoretically desirable but currently broken in practice
- The null OID parameter for submodule initialization must use the superproject's hash algorithm
- Patrick will update his patch to pass `superproject->hash_algo` directly to resolve the conflict
- The discussion reveals broader issues with mixed-hash submodule support that may need future attention

This exchange highlights the coordination challenges between parallel refactoring efforts in Git's ongoing libification. While the immediate conflict has a clear resolution, it surfaces deeper questions about submodule hash algorithm handling that may require architectural consideration beyond the current series.