Bugfix patch in the reftable error handling series, addressing reviewer feedback on error code propagation. This v2 update focuses on `block_writer_add()` and related functions in the reftable subsystem, replacing generic -1 error returns with specific REFTABLE_*_ERROR codes while ensuring backward compatibility.

Key technical changes:
- Updates `block_writer_add()` in block.c to propagate specific error codes from `reftable_encode_key()` and `reftable_record_encode()` rather than converting all failures to -1
- Modifies record.c to consistently use REFTABLE_ENTRY_TOO_BIG_ERROR for size-related failures in encoding functions
- Simplifies writer.c by removing the manual error code override that previously assumed all failures were size-related
- Updates function documentation in block.h to reflect the new error handling contract

The patch addresses Patrick Steinhardt's feedback about proper error propagation from vtable functions and Junio Hamano's concern about documentation accuracy. It maintains the series' goal of improving debuggability while ensuring existing error handling paths continue working. The commit message notes verification of call sites to confirm they check for nonzero returns rather than specifically -1, addressing backward compatibility concerns raised in reviews.

Notable subsystems/files touched:
- reftable/block.{c,h} (core block writing interface)
- reftable/record.c (record encoding implementations)
- reftable/writer.c (writer integration layer)

The changes appear focused and well-justified, with the v2 updates directly responding to prior review feedback. The patch maintains the thread's technical direction while tightening the implementation details around error handling contracts.