This is a follow-up discussion in the clone advice suppression thread, focusing on the broader architectural implications of Git's advice system behavior. Justin Tobler responds to Phillip Wood's observation about `advise()` calls bypassing `GIT_ADVICE` and `--no-advice` controls.

Justin acknowledges Phillip's point about the inconsistency in advice suppression, noting that while his immediate fix addresses the specific clone scenario (where the advice shouldn't appear regardless of configuration), there appears to be a wider issue where direct `advise()` calls don't respect global suppression mechanisms. He speculates this might be due to historical reasons where `advise_if_enabled()` was introduced later without full conversion of all advice calls.

The email reveals Justin plans to expand his patch series to include a more comprehensive fix that would make the default branch name advice respect `--no-advice` in all cases, not just the clone scenario. This represents a shift from just fixing the immediate regression to addressing the underlying architectural inconsistency Phillip identified.

Key technical details to note for future reference:
- The discussion involves `advise()` vs `advise_if_enabled()` behavior differences
- The default branch name caching behavior (`git_default_branch_name()`) is implicated in the timing issue
- The thread now has two distinct but related fixes in progress: the clone-specific suppression and a broader advice system consistency fix

The exchange shows the clone bug report evolving into a discussion about Git's advice system design, with Justin taking ownership of both the immediate fix and the larger architectural improvement.