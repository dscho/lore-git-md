This is part 18/20 of the Perl removal series, focusing on converting trace2 test scrubbing from Perl to sed. The patch removes Perl dependencies from t0210-trace2-normal.sh by replacing the Perl-based scrub_normal.perl script with an equivalent sed implementation.

The new scrub_normal() function handles several variable fields in trace2 output:
- Replaces elapsed times with _TIME_ placeholder
- Normalizes executable paths to _EXE_ token
- Omits cmd_path messages (RUNTIME_PREFIX-dependent)
- Skips cmd_ancestry (not universally implemented)

The sed implementation uses four patterns to match and transform the trace2 output, maintaining the same scrubbing behavior as the original Perl version. This allows removing the PERL_TEST_HELPERS prerequisite from the test file while preserving all test coverage. The patch follows the series' established pattern of identifying specific Perl usage and replacing it with portable shell/C alternatives.

Key technical details:
- Removes t0210/scrub_normal.perl (54 lines)
- Adds 43 lines of sed-based scrubbing logic to t0210-trace2-normal.sh
- Updates all test cases to use the new scrub_normal() instead of Perl
- Maintains identical expected output patterns in all test assertions

The change is straightforward and maintains test behavior while advancing the series' goal of making Perl optional. The commit message clearly explains the motivation (environment-dependent fields making testing harder) and the solution approach.