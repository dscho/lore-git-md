This is a brief follow-up in the ongoing CSPRNG backend configuration thread, specifically addressing the Meson build system's detection order. Ramsay Jones concurs with Patrick Steinhardt's suggestion to modify the Meson build to prioritize `getrandom()` over `arc4random()` on Linux, aligning with the technical consensus established earlier in the thread.

The email references key technical background from prior messages:
- Glibc's `arc4random` is just a `getrandom()` wrapper with no advantages
- Linux kernel maintainers rejected the original ChaCha20 implementation
- Both build systems (Makefile and Meson) should now consistently prefer `getrandom()`

The message serves as lightweight agreement rather than introducing new technical content, confirming the direction Patrick Steinhardt proposed for the Meson build system changes. This maintains the thread's focus on cross-platform consistency for CSPRNG backend selection.