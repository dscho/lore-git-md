This is a review response in Martin Ågren's pretty-format refactoring series. Patrick Steinhardt is commenting on patch 1/8, which tightens the function signature of `format_commit_one()` in pretty.c by removing an unnecessary `void*` indirection.

The patch removes the need for casting by having callers pass the `struct format_commit_context` directly rather than through a `void*`. Patrick notes this makes sense given the historical context - the function needed the `void*` when introduced in 2009 (commit 9fa708dab1c) because callers only had void contexts at that time, but the calling code has since evolved to provide the proper type directly.

This is a straightforward cleanup that improves type safety without changing behavior, and Patrick agrees with the change. The review confirms this first patch in the series is a good foundation for the subsequent refactorings that will modularize the pretty-format parsing logic.