Here's the updated AI thread summary:

This thread covers a two-patch series adding SHA-256 repository compatibility to gitk, reviving Rostislav Krasny's 4-year-old effort and completing it under current maintainer Takashi Iwai's direction. The work addresses gitk's inability to handle SHA-256 repositories despite Git core's long-standing support, with openSUSE's SHA-256 workflow as a key motivator.

The first patch implements basic SHA-256 support through Tcl modifications: introducing a dynamic `hashlength` variable (40/64 chars) detected via `git rev-parse --show-object-format`, updating regex patterns and UI elements for variable-length hashes while preserving translations. Johannes Sixt's review identified several code quality issues including hardcoded regex lengths that should use `$hashlength`. In follow-up discussion, Takashi Iwai clarified three design choices:
1. The `gotocommit` regex range check intentionally doesn't `$hashlength` as it works for both algorithms
2. Error messages properly interpolate `$objformat` for unsupported algorithms
3. Separate `objformat` (raw git output) and `hashalgorithm` (UI display) variables serve distinct purposes

The second patch implements hash-algorithm-aware length preferences via a new `get_autosellen` proc returning SHA-1 (40) or SHA-256 (64) lengths. Johannes Sixt questioned whether length preferences should be algorithm-specific (current) or repository-agnostic, noting five additional locations with hardcoded SHA-1 lengths needing conversion (`longid`, `shortids`, `setlink`, `mkpatch`, `mktag`).

Key open items:
1. Design decision pending on hash length preferences model (algorithm-specific vs repository-agnostic)
2. First patch needs message/indentation cleanup per Sixt's initial review
3. Five identified procedures require hardcoded length conversions
4. Additional testing needed for hash parsing edge cases

The changes remain confined to gitk's Tcl UI without core impact. This work completes Git's ecosystem SHA-256 support, with gitk being among the last major components to gain compatibility. The series is functionally complete but requires resolution on the preference model and implementation polish (including the newly noted length conversions) before merging. The recent exchange provided important clarification on intentional design decisions versus areas needing revision.