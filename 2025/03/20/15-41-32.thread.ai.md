Updated AI thread summary:

This thread introduces a two-patch series reviving and updating a 4-year-old effort to add SHA-256 repository support to gitk, originally started by Rostislav Krasny and now completed by Takashi Iwai. The work addresses gitk's inability to handle SHA-256 repositories, which Git core has supported for years, particularly motivated by openSUSE's SHA-256 workflow needs.

The first patch implemented basic SHA-256 support through mechanical Tcl changes: introducing a `hashlength` variable (40/64 chars) detected via `git rev-parse --show-object-format`, updating regex patterns and UI elements for variable-length hashes while preserving translations. It left the "Auto-select SHA1" preference for future work.

The second patch (now sent) completes the feature by implementing hash-algorithm-aware length preferences. Key changes include:
- New `get_autosellen` proc returning dynamic lengths (40/64 chars) based on repository settings
- Separate SHA-256 preference storage as `autosellensha256` in gitk's config
- Updated UI handling for commit selection, reference copying and preference dialogs
- Default SHA-256 auto-select length set to 64 characters
- Backward compatibility maintained while properly handling both algorithms

Both patches note only light testing so far, with particular attention needed for hash parsing edge cases. The changes are confined to gitk's Tcl UI without affecting core repository operations. No reviews or feedback have been provided yet on this completed SHA-256 support work, which represents incremental progress in Git's broader SHA-256 ecosystem compatibility. The series appears ready for testing and review.