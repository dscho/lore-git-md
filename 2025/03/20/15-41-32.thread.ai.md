Here's the updated AI thread summary:

This thread introduces a two-patch series reviving and updating a 4-year-old effort to add SHA-256 repository support to gitk, originally started by Rostislav Krasny and now completed by Takashi Iwai. The work addresses gitk's inability to handle SHA-256 repositories, which Git core has supported for years, particularly motivated by openSUSE's SHA-256 workflow needs.

The first patch implemented basic SHA-256 support through mechanical Tcl changes: introducing a `hashlength` variable (40/64 chars) detected via `git rev-parse --show-object-format`, updating regex patterns and UI elements for variable-length hashes while preserving translations. Johannes Sixt reviewed this patch, noting several code quality issues including hardcoded regex lengths that should use `$hashlength`. 

The second patch completes the feature by implementing hash-algorithm-aware length preferences, adding a `get_autosellen` proc that dynamically returns either SHA-1 (40 chars) or SHA-256 (64 chars) lengths based on repository settings. Johannes Sixt questioned the design choice of algorithm-specific length preferences versus repository-agnostic behavior.

Johannes Sixt's follow-up review identified five additional locations with hardcoded SHA-1 length assumptions (value "40") that need conversion to use the dynamic `$hashlength` variable. These affect procedures handling tag/patch generation and display formatting (`longid`, `shortids`, `setlink`, `mkpatch`, `mktag`), representing straightforward mechanical fixes similar to changes already made in the series.

Key open questions and tasks:
1. Design decision needed on hash length preferences: algorithm-specific (current) vs repository-agnostic (Sixt's suggestion)
2. First patch needs code quality improvements per Sixt's feedback (message cleanup, indentation fixes)
3. Additional hardcoded length conversions required in five identified procedures
4. Both patches need more testing, particularly for hash parsing edge cases

The changes remain confined to gitk's Tcl UI without affecting core repository operations. This work continues Git's broader SHA-256 ecosystem compatibility efforts, with gitk being one of the last major components to gain full support. The series appears functionally complete but needs both design reconsideration on the preference model and implementation polish (including the newly identified length conversions) before merging.