Here's the updated AI thread summary:

This thread covers a two-patch series adding SHA-256 repository compatibility to gitk, reviving Rostislav Krasny's 4-year-old effort under Takashi Iwai's direction. The work addresses gitk's inability to handle SHA-256 repositories despite Git core's long-standing support, with openSUSE's SHA-256 workflow as a key motivator.

The first patch implements basic SHA-256 support through Tcl modifications: introducing a dynamic `hashlength` variable (40/64 chars) detected via `git rev-parse --show-object-format`, updating regex patterns and UI elements for variable-length hashes while preserving translations. Johannes Sixt's review identified several code quality issues including hardcoded regex lengths that should use `$hashlength`. Takashi Iwai clarified three design choices regarding the `gotocommit` regex, error messages, and separate `objformat`/`hashalgorithm` variables. Iwai has now acknowledged and committed to fixing five additional procedures (`longid`, `shortids`, `setlink`, `mkpatch`, `mktag`) that still contain hardcoded SHA-1 length assumptions, moving toward resolving one of the key open items.

The second patch implements hash-algorithm-aware length preferences via `get_autosellen` proc, sparking debate between algorithm-specific (Iwai) vs repository-agnostic (Sixt) approaches. Iwai defends algorithm-specific defaults as necessary for SHA-256 usability, citing openSUSE's kernel-firmware-all repository as a real-world case where 40-char display would be insufficient. Sixt prefers consistent user preferences across repositories. The discussion reveals this as a fundamental UX design question beyond just implementation details.

Key remaining items:
1. Resolution needed on hash length preferences model (algorithm-specific vs repository-agnostic) - the core unresolved debate
2. First patch still requires message/indentation cleanup per Sixt's initial review
3. Implementation of length conversions in the five identified procedures (now acknowledged by Iwai as needing work)
4. Iwai requests wording suggestions for commit message improvements
5. Additional testing needed for hash parsing edge cases

The changes remain confined to gitk's Tcl UI without core impact. This work would complete Git's ecosystem SHA-256 support, with gitk being among the last major components to gain compatibility. The series is functionally complete but requires resolution on the preference model debate and implementation polish (including the newly noted length conversions) before merging. Progress continues on technical implementation while the UX design question remains open for discussion.