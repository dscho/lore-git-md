This is part 3/8 of Karthik Nayak's v4 series on batched reference updates with partial failure support, focusing on refactoring the files backend.

The patch removes a redundant duplicate refname check in the files backend's transaction finish phase, since an identical check already occurs during prepare. The refnames list remains unchanged between these phases, making the second check unnecessary. The `ref_update_reject_duplicates()` function is now made static since it's only used within refs.c after this change.

Key technical details:
- Removes 6 lines from files-backend.c by eliminating the duplicate check
- Moves the function documentation to refs.c where the implementation remains
- Updates refs-internal.h to remove the now-internal function declaration
- Maintains all existing behavior since the prepare-phase check remains

This continues the series' trend of centralizing refname handling in the generic refs.c code rather than having duplicate logic in backends. The change is straightforward and builds on Patrick Steinhardt's earlier work on batched refname checks. No behavior changes are introduced - this is purely a code cleanup that removes redundant operations.