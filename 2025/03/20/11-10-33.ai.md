This is a follow-up discussion in the ongoing thread about advice suppression during clone operations, specifically addressing the root cause of the regression.

Phillip Wood raises an important architectural question about Git's advice system, challenging the current behavior where `advise()` calls bypass the `GIT_ADVICE` and `--no-advice` controls. He points out that while the documentation suggests these flags should disable all advice hints, in practice they only affect calls guarded by `advice_enabled()`. This observation goes beyond the immediate clone/bundle fix to question a fundamental design aspect of Git's advice system.

The email references Justin Tobler's explanation about how the deferred `git_default_branch_name()` call timing interacts with advice display, but Phillip argues this is symptomatic of the deeper issue that `advise()` doesn't respect the global advice suppression mechanisms. This suggests the current patch series might be treating a symptom rather than the underlying cause.

This intervention could potentially redirect the thread toward a more comprehensive solution that would modify the advice system's core behavior rather than just patching the specific bundle clone case. Phillip's point implies that similar advice suppression issues might exist elsewhere in the codebase where `advise()` is used directly without `advice_enabled()` checks.

The discussion now has two potential paths forward: continue with the focused clone fix as proposed, or consider a broader refactoring of how advice suppression works across Git's codebase. Phillip's observation about the documentation/implementation mismatch adds weight to considering the latter approach.