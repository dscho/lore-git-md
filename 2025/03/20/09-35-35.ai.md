This is a build system patch in the ongoing series to make Perl optional for Git's test suite. It modifies the Meson build configuration to remove the hard requirement for Perl when tests are enabled, reflecting the previous patches' work to make most tests run without Perl.

The change is straightforward - it removes `get_option('tests')` from the conditions that force Perl to be required. The patch notes this aligns with the series' progress in making tests Perl-optional, though Perl requirement remains for gitweb and the netrc credential helper. The diff shows a simple one-line modification in `meson.build` that loosens this restriction.

This patch follows the series' pattern of incrementally removing Perl dependencies, now addressing the build system's configuration to match the test suite's reduced Perl requirements. The change is minimal (1 line modified) and appears to be a logical next step after the test infrastructure changes in earlier patches. The commit message clearly explains the motivation and connects it to the broader series goal.