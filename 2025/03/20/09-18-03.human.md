This is a review response in Martin Ågren's pretty-format refactoring series, specifically addressing patch 4/8 which fixes edge cases in padding placeholder parsing. Patrick Steinhardt agrees with the technical approach but suggests two minor improvements: making malformed placeholders fatal rather than silently producing wrong output (while acknowledging this might be too disruptive), and renaming the `ans` variable to something more descriptive like `result`.

The patch itself fixes a subtle bug where partially parsed padding directives (like "%<(10,bad)") could incorrectly reuse lingering state from previous valid placeholders. Martin's solution ensures the parsing function only modifies the output struct on success, while also future-proofing with proper initialization. The change is well-tested with a new test case demonstrating the problematic behavior.

Patrick's review is constructive and positive overall, though he raises the interesting question of whether invalid placeholders should trigger a hard error rather than producing potentially confusing output. This seems like a reasonable design consideration that could be addressed in future work, though the current patch represents a clear improvement over the existing behavior.