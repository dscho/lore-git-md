Performance patch in Taylor Blau's incremental MIDX bitmap series (v5 09/14). This small but important change updates the disk-usage calculation code to properly handle incremental multi-pack indexes by using the safer `nth_midxed_pack()` accessor rather than directly accessing the MIDX's packs array. 

The modification ensures consistent behavior when computing object sizes and disk usage across layered MIDX bitmaps, maintaining the series' goal of efficient cross-layer operations. While the change is straightforward (just two line modifications), it's a necessary piece of the larger incremental bitmap infrastructure that enables proper pack lookups when MIDX layers are chained together.

This continues the pattern seen in earlier patches of the series, where direct MIDX pack array accesses are replaced with the more robust `nth_midxed_pack()` helper. The patch is well-contained and follows the established approach, with no apparent controversy or outstanding questions.