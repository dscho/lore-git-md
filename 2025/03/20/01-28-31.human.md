Platform compatibility follow-up in CSPRNG configuration thread. Ramsay Jones concurs with Patrick Steinhardt's suggestion to reorder the Meson build system's CSPRNG backend detection to prefer `getrandom()` over `arc4random()` on Linux. This aligns with the technical consensus established earlier in the thread that Linux's glibc implementation of `arc4random` is merely a wrapper around `getrandom()` with no benefits, while Cygwin's implementation remains the preferred userspace ChaCha20 solution. The email serves as a quick acknowledgment that the Meson build system should mirror the Makefile's existing preference for `getrandom()` on Linux platforms. This appears to be the final loose end being tied up in the comprehensive cross-platform CSPRNG configuration discussion.