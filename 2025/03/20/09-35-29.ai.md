This is the second patch in a 20-part series refactoring Git's test suite to remove Perl as a hard requirement. The patch focuses on environment variable sanitization, which previously used Perl to unset unwanted GIT_* variables while preserving certain whitelisted ones.

The change replaces the Perl script with a `sed` command that achieves the same filtering logic through a series of exclusion patterns followed by variable name extraction. The new implementation maintains identical behavior - unsetting all GIT_* variables except those matching specific patterns like GIT_TRACE, GIT_TEST, GIT_PERF_*, etc. This is a key part of making the test suite runnable without Perl while preserving all functionality.

The technical changes modify `t/test-lib.sh` to use shell-native tools instead of Perl for environment processing. The patch follows the series' established pattern of replacing Perl dependencies with equivalent shell implementations where possible. It builds on prior work by Johannes Schindelin, as noted in the attribution line. The diff shows 14 lines added and 18 removed, with the new `sed`-based approach being more verbose but avoiding Perl entirely.

This continues the systematic approach seen in the series - identifying specific Perl dependencies and replacing them with shell equivalents while maintaining the same test coverage and behavior. The environment sanitization is a foundational piece of test infrastructure, so this change helps establish that core test operations can work without Perl.