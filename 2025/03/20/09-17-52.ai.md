Refactoring patch in the pretty-formatting series. This is a reply to Martin Ågren's patch that simplified color parsing logic in pretty.c by reducing code duplication between "auto" and default cases.

The original patch restructured the color parsing to handle the "always" case separately while combining the identical "auto" and default behaviors. Patrick Steinhardt's review confirms the behavioral equivalence while noting the tradeoff: the simplified version would require more careful handling if new color options are added in the future. The change touches the `parse_color()` function in pretty.c, specifically the logic around `skip_prefix()` calls for "auto," and "always," prefixes.

Key technical details:
- Maintains identical behavior for color formatting options
- Combines "auto" and default cases while keeping "always" separate
- Uses `skip_prefix()` helper consistently
- Preserves the `want_color()` check for non-"always" cases
- Keeps the existing comment about default behavior matching "auto"

The review acknowledges the change makes the code slightly more brittle for future extensions but agrees the current simplification is worthwhile. This aligns with the series' goal of making the pretty-formatting code more maintainable while preserving behavior.