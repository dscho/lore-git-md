This is a follow-up discussion on patch 6/8 in Martin Ågren's series refactoring pretty-format parsing. The patch modularizes the line-wrapping ("%w") logic by extracting it into a new `parse_rewrap()` helper function and introducing a `struct rewrap_args` to hold the parsing results.

In this brief exchange, Patrick Steinhardt questions the necessity of the `memset()` call that zero-initializes the `rewrap` struct, noting that all fields are properly initialized when the function succeeds. Martin agrees and confirms he will remove the unnecessary memset in the next version.

The discussion is straightforward with no outstanding technical disagreements - this appears to be a minor cleanup point caught during review. The change maintains the patch's core purpose of modularizing the line-wrapping parsing logic while removing redundant initialization. This continues the series' pattern of careful code organization improvements in pretty.c with reviewer feedback being promptly incorporated.