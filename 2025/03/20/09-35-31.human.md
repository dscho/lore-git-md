This is part 4 of a 20-part series refactoring Git's test suite to reduce Perl dependencies. The series aims to make Perl optional while maintaining test coverage, with remaining Perl requirements marked via PERL_TEST_HELPERS.

The patch converts the `test_copy_bytes()` helper function from using Perl to using `dd(1)`. This function copies a specified number of bytes from stdin to stdout, and the change is straightforward - replacing a Perl implementation with a simpler shell command. The modification is clean (12 lines removed, 1 added) and maintains the same functionality while removing the Perl dependency.

This change follows the series' established pattern of identifying Perl dependencies in test infrastructure and replacing them with shell equivalents where possible. The commit message clearly explains the motivation (enabling tests to pass without Perl) and the technical approach. As with previous patches in the series, this appears to be a well-considered change that moves toward the overall goal while maintaining test reliability.