Atomic fetch/prune crash bug investigation and fix: A critical regression in Git v2.49.0-rc1 causes core dumps when running `git fetch --prune --atomic` if lock files exist, triggered by commit c92abe71df ("builtin/fetch: fix leaking transaction with `--atomic`"). The bug manifests as a BUG message about operations on closed reference transactions, with two confirmed reproduction cases involving lock files. Justin Tobler's immediate fix modifies `builtin/fetch.c` to explicitly free failed transactions, preventing crashes while maintaining memory safety, with a REFFILES-specific test case verifying graceful failure.

The discussion has expanded to broader reference transaction API design questions. Justin proposed making failed commits not auto-close transactions, requiring explicit abort calls for more consistent error handling. Patrick Steinhardt responded by considering this approach but expressing concern it still leaves callers managing both abort and free operations. He suggested an alternative of making abort operations idempotent, though remains uncertain if this significantly improves the situation. The API design discussion remains exploratory, capturing the tension between simplicity and robust error handling, with no clear consensus yet on the preferred solution path.

Key technical details include the fetch.c error path fix, REFFILES test case, and the two proposed API improvement directions: Justin's explicit abort requirement versus Patrick's idempotent abort suggestion. The thread now documents both the verified crash fix (ready for merging) and active design considerations for reference transactions, with open questions about pursuing API refactoring as follow-up work. Justin remains engaged for potential implementation work once the design direction clarifies.