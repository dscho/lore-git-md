Atomic fetch/prune crash bug investigation and fix: A critical regression in Git v2.49.0-rc1 causes core dumps when running `git fetch --prune --atomic` if lock files exist, triggered by commit c92abe71df ("builtin/fetch: fix leaking transaction with `--atomic`"). The bug manifests as a BUG message from refs.c:2435 about operations on closed reference transactions, with two confirmed reproduction cases: (1) original report with test.lock in refs/, and (2) Justin Tobler's simplified test using a mirrored repository with manual lock files. The root cause involves improper error handling where lock conflicts trigger assertion failures rather than graceful cleanup in atomic reference transactions.

Justin Tobler's fix modifies `builtin/fetch.c`'s error path to explicitly free failed transactions instead of relying on abort, preventing the BUG() trigger while maintaining c92abe71df's memory leak fixes. His accompanying test case verifies atomic fetch now fails gracefully (without crashing) when encountering locked references, marked REFFILES-specific as it depends on filesystem lock behavior. Patrick Steinhardt agrees with the immediate solution but identifies a broader issue with the reference transaction API's automatic cleanup behavior, drawing parallels to past `ref_iterator` problems.

The discussion has now progressed to API design improvements. Justin agrees the current transaction interface is awkward due to inconsistent cleanup behavior - aborted transactions free themselves while committed ones don't. He suggests an alternative approach where failed commits wouldn't automatically close transactions, requiring explicit abort calls. This would make error handling more consistent by allowing unconditional transaction abort during cleanup. Justin indicates willingness to work on a follow-up series to refactor the transaction API if there's interest.

Key technical details include the explicit free+NULL assignment in fetch.c's error path, the REFFILES-specific test case, and the current inconsistency in transaction cleanup behavior. The thread now documents both the specific crash fix and active design considerations for reference transactions, with open questions about pursuing Patrick's suggested API refactoring as follow-up work. Justin's immediate fix stands as the verified solution for the regression while the conversation explores broader improvements to the transaction interface.