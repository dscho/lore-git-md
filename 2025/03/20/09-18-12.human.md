Follow-up review of pretty.c refactoring (patch 7/8). Patrick Steinhardt responds to Martin Ågren's patch that extracts magic character parsing in pretty.c into a new `parse_magic()` function. The change continues the series' pattern of modularizing the pretty-format parsing logic.

Patrick confirms the technical approach is sound but suggests a non-blocking improvement: prefixing the enum values with `MAGIC_` for better namespacing and clarity. He notes this isn't critical since the patch maintains existing names. The reviewer also verifies the refactoring preserves behavior, walking through the control flow to confirm the consumed character counting remains correct.

This is a straightforward review of an organizational change that follows the series' established pattern. Patrick's feedback is constructive but not essential, indicating the patch is likely ready to proceed with or without the enum naming tweak. The interaction shows steady progress on the pretty.c cleanup with no major concerns raised.