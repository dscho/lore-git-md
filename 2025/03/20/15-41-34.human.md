This is the second patch in a series adding SHA-256 support to gitk, completing the work started in the first patch by implementing auto-select length preferences for SHA-256 hashes. The patch adds logic to handle hash length preferences separately for SHA-1 and SHA-256 repositories, with SHA-256 defaulting to 64 characters (the full hash length).

The key changes introduce a new `get_autosellen` helper function that returns the appropriate length based on the repository's hash algorithm, replacing direct `autosellen` variable access throughout the code. The preference UI is updated to show the correct maximum length (40 for SHA-1, 64 for SHA-256) and store the SHA-256 preference separately as `autosellensha256` in gitk's config.

This appears to be a straightforward completion of the SHA-256 support, maintaining consistency with gitk's existing preference system while properly handling the longer hash lengths. The patch is focused and well-contained, touching only the necessary parts of the Tcl code to implement the preference handling. With this change, gitk's SHA-256 support should now be feature-complete, matching the behavior users expect from other Git tools.