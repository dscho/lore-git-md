This is part 13/20 of a series making Perl optional in Git's test suite, focusing on refactoring tests that depend on Perl for textconv scripts. The patch converts three test files (t4030, t4031, and t7815) to use shell utilities instead of Perl for their textconv operations.

The changes replace Perl-based hexdump scripts with simpler shell equivalents using `tr` and a new `test-tool hexdump` helper. While not perfectly matching the original Perl behavior, the simplified versions still verify the core functionality needed by the tests. The patch removes PERL_TEST_HELPERS prerequisites from these tests since they no longer require Perl.

The modifications follow the series' pattern of mechanical substitutions that maintain test coverage while reducing dependencies. The author notes that exact behavioral equivalence isn't necessary - the tests just need to confirm the textconv scripts do something detectable. This pragmatic approach allows for simpler implementations while still serving the tests' purposes.

This continues the steady progress toward making 97% of Git's test suite run without Perl, with each patch tackling specific categories of Perl usage. The changes here are straightforward and unlikely to be controversial, following the established refactoring approach seen in previous patches.