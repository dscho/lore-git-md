This thread documents a 20-part series refactoring Git's test suite to reduce its dependency on Perl while maintaining test coverage. The series systematically replaces Perl functionality with shell and C equivalents, introducing PERL_TEST_HELPERS to mark tests that still require Perl.

The latest patch (14/20) focuses on PGP signature sanitization in test helpers. It converts the `sanitize_pgp()` function from Perl to `sed`, removing another PERL_TEST_HELPERS dependency. The new implementation uses sed range patterns to strip PGP signature blocks while preserving the markers. The patch also updates t6300-for-each-ref.sh to explicitly mark remaining Perl-dependent tests rather than skipping the entire file when Perl is unavailable.

The series has made significant progress, with 97% of tests (30,342/31,358) now running without Perl. Each patch follows a consistent pattern of identifying Perl usage and replacing it with portable alternatives where possible, while carefully preserving test behavior. The remaining Perl dependencies are clearly marked for future handling. The changes are mechanical substitutions that don't introduce new edge cases, maintaining the test suite's reliability throughout the transition.