**Git p4 error message encoding fix** - Patch series addressing `git p4` failures when processing non-UTF-8 Perforce error messages by extending the existing metadata transcoding framework. Original patch from Nikolay Shustov introduces a `MetadataTranscoder` class to consolidate encoding handling, applying the same configurable fallback strategy (`git-p4.metadataDecodingStrategy`/`git-p4.metadataFallbackEncoding`) used for metadata to error messages. Key technical implementation includes: refactoring transcoding logic from `metadata_stream_to_writable_bytes()` into new class, adding error-specific decoding respecting existing settings, Python 3-specific test verification (`t9837-git-p4-error-encoding.sh`), and enhanced verbose diagnostics in `MetadataDecodingException`. Maintains Python 2/3 compatibility through careful byte handling without introducing new configuration. Follow-up ping confirms no outstanding technical objections; patch appears ready for maintainer consideration. Test coverage targets specific failure scenario though limited to Python 3 due to Unicode handling differences. Changes align with established approach to encoding issues in `git p4` bridge, building on prior metadata transcoding work.