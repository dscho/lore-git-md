**Git p4 error message encoding fix** - Patch series by Nikolay Shustov addressing `git p4` failures when processing non-UTF-8 Perforce error messages by extending the existing metadata transcoding framework. Core technical implementation introduces a `MetadataTranscoder` class to consolidate encoding handling, refactoring logic from `metadata_stream_to_writable_bytes()` while maintaining Python 2/3 compatibility through careful byte handling. Applies same configurable fallback strategy (`git-p4.metadataDecodingStrategy`/`git-p4.metadataFallbackEncoding`) used for metadata to error message processing, with enhanced verbose diagnostics in `MetadataDecodingException`. Test coverage via Python 3-specific `t9837-git-p4-error-encoding.sh` verifies the fix by forcing Python 3 execution to validate Unicode handling. Changes build on prior metadata transcoding work in the git-p4 bridge, solving a practical migration pain point for Perforce-to-Git transitions. No outstanding technical objections raised in discussion; patch appears ready for maintainer consideration with test limitations noted (Python 3-only verification due to Unicode handling differences between Python versions). Implementation maintains backward compatibility without introducing new configuration options.