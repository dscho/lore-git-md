This is a brief follow-up in the ongoing CSPRNG backend configuration thread, specifically about the Meson build system's detection order. Patrick Steinhardt confirms alignment with Ramsay Jones' earlier suggestion to prioritize `getrandom()` over `arc4random()` in the Meson build configuration, mirroring the Makefile's approach. The email serves as a handoff request for Ramsay Jones to incorporate this change in the next version of his patch series.

Key technical context from the thread remains unchanged: Linux's glibc `arc4random` is just a `getrandom` wrapper with no advantages, making direct `getrandom` usage preferable. The Meson build system currently checks `arc4random` first, which this exchange confirms should be updated to match the Makefile's `getrandom`-first approach. No new technical arguments are introduced - this is purely about coordinating the implementation of the already-established consensus across both build systems.