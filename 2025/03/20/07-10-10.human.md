CI infrastructure fix for Meson build system dependencies. This is a follow-up to Karthik Nayak's patch that properly sets the `CI_JOB_IMAGE` environment variable in GitHub Actions workflows, addressing the root cause of dependency installation failures.

Patrick Steinhardt confirms the solution is preferable to Junio Hamano's temporary workaround (which manually installed Meson), as it properly initializes the CI environment variables that drive the dependency installation script. The fix covers all non-Windows jobs that need Meson (Documentation, StaticAnalysis, sparse), while Windows jobs continue handling dependencies separately. 

The patch represents the final implementation phase of the Meson CI fix that was previously discussed, with Karthik having already validated it through test runs. Patrick notes this approach is more robust for future dependency needs, as it properly integrates with Git's existing CI infrastructure rather than working around it. The change is now ready for final review and merge.