This is part 19/20 of a series refactoring Git's test suite to reduce Perl dependencies. The patch converts a Perl-based helper function in t5316-pack-delta-depth.sh to use awk instead.

The `max_chain()` helper extracts the maximum chain length from `git index-pack` output, previously using Perl for parsing. The change replaces the Perl one-liner with an awk equivalent that performs the same extraction while being more portable. The patch also drops PERL_TEST_HELPERS prerequisites from four test cases that no longer need Perl.

This follows the series' established pattern of systematically identifying and replacing Perl dependencies. The conversion appears straightforward, maintaining the test's functionality while reducing the test suite's Perl requirements. The commit message clearly explains the change and its motivation, and the diff shows a clean replacement of the parsing logic.