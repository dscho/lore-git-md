This is a follow-up discussion in the bugfix thread addressing edge cases in diffcore-rename when combining break detection (-B) and --follow options.

Jeff King (Peff) responds to Elijah Newren's detailed analysis of the bug's edge cases, confirming understanding of the test case that triggers the BUG() condition. The discussion clarifies that the BUG() only occurs with invalid flag combinations (like using directory/glob pathspecs with --follow), while the more serious uninitialized memory access issue affects broader cases.

Peff agrees the current patch's explanation is sufficient and supports adding a second test case to demonstrate the BUG() trigger scenario. While noting the --follow+glob combination is technically useless (and might be disallowed later), they see value in keeping the test case for now to document the edge case behavior.

The exchange shows consensus on both the technical solution and test coverage approach, with the patch appearing ready for final review. The discussion provides additional context about the bug's manifestation that could inform future similar issues, though the core fix remains uncontested.