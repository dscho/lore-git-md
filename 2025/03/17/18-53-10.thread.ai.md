Updated AI summary:

Performance investigation into `git add .` slowness versus `git status` in large repositories (~27k dirs, ~78k paths) on MacOS with Git 2.49.0. Initial observations showed `git status` completes in ~300ms (with fsmonitor/untracked cache) while `git add .` takes 1.5-2s due to full working tree scans. Brian M. Carlson established the technical distinction: `git add .` must scan for both modified tracked and new untracked files, unlike `git add -u` (similar to `git status`) which only examines tracked files. The untracked cache optimization used by `git status` isn't applicable to `git add .` with general pathspecs due to complexity constraints.

Yissachar Radcliffe's follow-up challenges aspects of this explanation: (1) clarifies `git status` does include untracked files (unlike `git add -u`), (2) suggests `git add .` could be special-cased for optimization despite general pathspec limitations, and (3) argues the performance gap between direct `git add .` and the `git status | git add` workaround is unnecessarily large. The thread now contains competing perspectives on whether the current behavior represents an unavoidable design tradeoff (Carlson) or a potentially addressable optimization opportunity (Radcliffe). Key technical constraints remain the untracked cache's incompatibility with pathspec matching and the fundamental difference in scope between commands that must discover new files versus those that only process known tracked files. No new performance data has been introduced, but the discussion has shifted from explaining the status quo to questioning whether targeted optimizations for the common `git add .` case might be feasible.