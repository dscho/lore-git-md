Performance investigation into `git add .` slowness versus `git status` in large repositories (~27k dirs, ~78k paths) on MacOS with Git 2.49.0. Initial observations showed `git status` completes in ~300ms (with fsmonitor/untracked cache) while `git add .` takes 1.5-2s due to full working tree scans. Brian M. Carlson established the core technical distinction: `git add .` must scan for both modified tracked and new untracked files, unlike `git add -u` (similar to `git status`'s tracked-file focus). The untracked cache optimization used by `git status` isn't applicable to `git add .` with general pathspecs due to complexity constraints.

Yissachar Radcliffe challenged this with three points: (1) corrected that `git status` does include untracked files (unlike `git add -u`), (2) suggested special-casing `git add .` despite general pathspec limitations, and (3) argued the performance gap between direct `git add .` and the `git status | git add` workaround is unnecessarily large. Carlson's latest response acknowledges the `git status` untracked-file behavior but reveals a cultural divide - he considers `git add .` an antipattern in Git developer circles due to frequent accidental staging incidents, explaining why optimizations haven't been prioritized. He identifies `dir.c` as the optimization target but notes no guarantee such work would be accepted, maintaining the technical constraints around pathspec handling and untracked cache limitations.

The discussion now balances technical constraints (untracked cache incompatibility, fundamental scope differences between file discovery commands) against workflow preferences (Radcliffe's view of `git add .` as common versus Carlson's presentation of it as problematic). No new performance data has emerged, but the thread has surfaced that optimization decisions may be influenced as much by perceived workflow safety as by pure technical feasibility. The path forward appears to require either demonstrated performance improvements that don't compromise safety or consensus-building around changing workflow norms.