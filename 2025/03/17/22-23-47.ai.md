This is a tangential discussion in the protocol v2 fetch improvements thread, focusing on test infrastructure rather than the core changes.

The email continues a side conversation about the use of `test_commit` versus raw `git commit` in test scripts. Junio Hamano and Jeff King had previously agreed that `test_commit` has drawbacks - it creates unnecessary working tree cruft and tags by default, with its automatic test_tick advancement being its only real benefit. Taylor Blau acknowledges their points but notes the impracticality of converting the thousands of `test_commit` instances currently in the test suite.

Key technical details for future reference:
- The discussion references `test_commit` test helper behavior
- Specific drawbacks mentioned: creates working tree files, generates tags, increments test_tick
- Jeff King prefers raw `git commit` for simplicity
- 2,570 existing instances of `test_commit` in the codebase make widespread conversion impractical

This is a minor aside in the larger thread about fetch protocol improvements, showing test infrastructure preferences but not affecting the main series' technical direction. The core protocol v2 changes remain approved and ready for integration while this test helper discussion remains an open style consideration.