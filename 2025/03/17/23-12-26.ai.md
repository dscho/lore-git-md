This is a follow-up discussion about the build system improvement series for unreachable code detection, specifically addressing the patch ordering that temporarily caused build breakage on macOS/Clang.

The email from Junio C Hamano responds to Taylor Blau's observation about the patch ordering in the series that enabled `-Wunreachable-code` warnings. Taylor had noted that applying the warning enablement (patch 1) before the macOS/Clang workaround (patch 2) created a temporary build breakage for macOS/Clang users, though this is now moot since the final solution supersedes both.

Junio clarifies that the warning enablement commit (db1d1f5d) is actually not yet in 'next', implying the build breakage window might be smaller than Taylor assumed. The discussion shows maintainer awareness of the potential disruption from patch ordering, while also indicating the progression through the natural solution path (enable warnings → platform workaround → final architectural solution) was preferred over reordering patches to avoid temporary breakage.

Key technical details preserved from the thread context include the specific `sigfillset()` error checking change (10 lines in `run-command.c`), the temporary `errno`-based workaround, and its eventual replacement by the systematic solution with `NOT_A_CONST` macro and `fbtcdnki.c` compilation unit. The exchange also highlights the maintainer's consideration of patch ordering effects on users versus technical solution progression.