Refactoring patch completing the refspec API cleanup series. This is the fourth and final patch in Taylor Blau's series to simplify the refspec API, following the earlier protocol v2 fetch behavior improvements.

The patch replaces the generic `refspec_item_init()` function with fetch/push-specific variants (`refspec_item_init_fetch()` and `refspec_item_init_push()`), mirroring the same pattern applied earlier to `refspec_init()`. This continues the series' goal of making the API more explicit by removing the boolean `fetch` parameter in favor of dedicated functions for each operation type.

Changes are mechanical and consistent with the previous patches in the series, updating callers in `builtin/fetch.c`, `builtin/pull.c`, and internal `refspec.c` code. The patch maintains all existing behavior while making the fetch-vs-push distinction clearer at the API level.

This completes the refspec API simplification that began with converting the `fetch` field from an enum to a boolean. The series has been steadily progressing with patches 1-2 already merged and patch 3 under review. With all protocol v2 behavior questions settled in earlier threads, this final cleanup represents straightforward quality-of-life improvement to the codebase.