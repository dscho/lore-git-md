Here's a human-readable summary of this thread:

The thread discusses an inconsistency between `git switch --force` and `--discard-changes` when dealing with untracked files. David Mandelberg reports that while the documentation suggests these flags are aliases, they behave differently in practice: `--discard-changes` fails when encountering untracked files that would be overwritten (matching normal switch behavior), whereas `--force` proceeds regardless. This was demonstrated using a Makefile example where only `--force` succeeded in switching branches despite the untracked file conflict.

The core issue is whether this represents a documentation bug (if the different behaviors are intentional) or a code bug (if the flags should truly be equivalent). The author notes they couldn't find recent discussion about this discrepancy, suggesting it may have gone unnoticed. The thread appears to be at an early stage, awaiting input from maintainers or other contributors about how to resolve the inconsistency - whether by updating the documentation to clarify the difference or by aligning the flag implementations.