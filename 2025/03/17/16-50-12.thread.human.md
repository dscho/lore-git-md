Here's the updated human-readable thread summary:

This thread examines a discrepancy in `git switch` behavior between the `--force` and `--discard-changes` flags when dealing with untracked files. David Mandelberg originally reported that while the documentation suggests these flags are aliases, in practice `--discard-changes` fails when untracked files would be overwritten, whereas `--force` proceeds. He demonstrated this with a case involving a conflicting `Makefile`.

Junio Hamano traced this divergence to implementation details in two historical commits, noting it was unintentional but now effectively frozen due to potential reliance. While acknowledging arguments for both maintaining the distinction (finer control) and aligning with `checkout -f` simplicity, he leans toward documenting the difference rather than changing behavior.

In a follow-up, Mandelberg clarified his reproduction steps - starting from an empty directory and fetching from git.git - while noting the behavior would likely occur in other scenarios too. He remains neutral on whether to change the behavior, mentioning he can use `git checkout -f` when needed. The discussion continues to weigh whether the current distinction provides enough value to justify the confusion, with no strong push yet for implementation changes. The likely resolution path appears to be documentation clarification if the behavior remains as-is.