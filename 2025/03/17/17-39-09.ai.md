This is a technical clarification in an ongoing discussion about `git switch` flag behavior with untracked files. Junio Hamano provides historical context for the current discrepancy between `--force` and `--discard-changes` behavior, tracing it back to implementation details in commit 3ec37ad1 (which added `--discard-changes`) and subsequent changes in 480d3d6b (which introduced untracked file protection). 

The key technical insight is that the divergence stems from how these commits modified the internal flag handling - 3ec37ad1 added a new `.discard_changes` field while keeping some `.force` checks, and 480d3d6b later made the untracked file protection depend on `.force` rather than the newer field. Hamano notes this could have been avoided with different implementation choices, but considers the current behavior effectively frozen" due to potential user reliance.

The email leans toward documenting the difference rather than changing behavior, though Hamano acknowledges both sides of the debate about whether the finer-grained control is valuable or just confusing. The core issue remains whether to maintain the distinction where `--discard-changes` protects untracked files while `--force` overwrites them, or to align them. The implementation history shows this was likely an accidental divergence rather than intentional design, but one that may now be too established to change.