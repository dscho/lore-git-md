This thread documents the evolution of a series improving cruft pack handling in `git repack`, transitioning from the problematic `--max-cruft-size` behavior to clearer semantics via a new `--combine-cruft-below-size` option. The work has followed a methodical approach, beginning with test infrastructure preparation before introducing behavior changes.

Taylor Blau initiated the series by reorganizing repack-related tests, moving 250 lines from t5329 to t7704 to better separate plumbing tests from user-facing behavior tests, with Elijah Newren confirming this as a straightforward improvement. Subsequent patches refined test terminology and prepared for the behavior change while maintaining coverage.

The core change introduces `--combine-cruft-below-size`, which aggregates small cruft packs while leaving larger ones untouched. The implementation replaces `collapse_small_cruft_packs()` with `combine_small_cruft_packs()` and modifies `write_cruft_pack()` to handle the new size parameter. Junio C Hamano reviewed the final patch, suggesting documentation refinements to clarify size parameter units.

Elijah Newren has now provided additional review feedback, focusing on documentation clarity. He suggests:
- Using "leaves alone" instead of "prohibits" for more precise semantics
- Clarifying that `--max-cruft-size` should be used alongside (not instead of) the new option
- Documenting recommended size ratios between the two options
- Confirming the implementation itself remains sound

The series demonstrates careful attention to both technical correctness and clear communication, with each iteration refining the approach based on reviewer feedback. The changes affect documentation and test cases while maintaining the sound implementation reviewed earlier. This represents the final polish before the feature is ready for integration.