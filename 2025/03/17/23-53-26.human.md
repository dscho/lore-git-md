This is the final cover letter for a 3-patch series that completes the build system improvements for unreachable code detection, now incorporating Junio Hamano's architectural solution. The series has evolved from Jeff King's initial platform-specific workaround to a more general approach.

The series now consists of:
1. Jeff King's initial workaround using `errno` to check `sigfillset()` errors (specific to macOS/Clang)
2. Junio Hamano's new `NOT_CONSTANT` macro that provides a general solution for false positives in conditionals
3. Jeff King's warning enablement patch (now safely applied after the fixes)

Key changes since v2 include reordering the patches (the macro now comes before warning enablement) and improvements to the macro implementation. The solution now works across all build systems (Makefile and Meson) and handles false positives more generally, not just the macOS case. The cover letter notes that while the shortlog doesn't show the patch reordering, this was a significant change to ensure the fixes land before the warning is enabled.

This appears to be the final polished version of the series, having incorporated feedback from multiple reviewers and resolving the earlier concerns about potential build breakage on macOS. The solution is now both technically robust and properly sequenced.