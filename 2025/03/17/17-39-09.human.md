This is a follow-up in the ongoing discussion about `git switch --force` vs `--discard-changes` behavior with untracked files. Junio Hamano provides historical context for the current implementation, tracing how the divergence between the flags' behavior emerged through specific commits (3ec37ad1 and 480d3d6b). He notes the original intention may have been for the flags to be synonyms, but implementation choices led to the current difference where `--force` overwrites untracked files while `--discard-changes` does not.

Hamano acknowledges this could be viewed either as a bug or as an intentional design choice for finer control, but suggests it's too late to change behavior now that users may rely on it. His technical analysis shows the discrepancy stems from which internal flags (`force` vs `discard_changes`) different parts of the codebase check. While he's personally more accustomed to `git checkout -f`'s simpler behavior, he leans toward documenting the current distinction rather than changing it.

The email reinforces the likely resolution path from earlier discussion: updating documentation to clarify the flags' different behaviors rather than altering the implementation. This maintains backward compatibility while addressing the original documentation mismatch that started the thread.