This is a bug report/documentation issue regarding the behavior of `git switch --force` versus `--discard-changes`. The author David Mandelberg points out a discrepancy where the documentation claims these flags are aliases, but they behave differently in practice when dealing with untracked files.

The key technical details are that `--discard-changes` fails when encountering untracked files that would be overwritten (consistent with normal switch behavior), while `--force` proceeds anyway. This affects the `Makefile` example shown, where only `--force` succeeds in switching branches despite the untracked file conflict.

The email suggests this may be either a documentation bug (if the behavior is intentional) or a code bug (if the flags should truly be equivalent). The author notes they checked for recent changes but found no relevant discussion. This appears to be a straightforward behavioral inconsistency that needs resolution, either by updating docs to clarify the difference or aligning the implementations.