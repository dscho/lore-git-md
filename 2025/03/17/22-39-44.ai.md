This is an update on two ongoing performance-related topics in the Git development thread. Taylor Blau provides status updates on his work with cruft packs and incremental MIDX bitmaps.

For the cruft pack fixes (tb/multi-cruft-pack-refresh-fix), Taylor notes that version 5 of the series is now available, focusing specifically on fixing the object freshening bug while deferring the more complex --max-pack-size vs --combine-cruft-below-size issue to a future series. The current version addresses the core problem where certain objects weren't being properly refreshed when multiple cruft packs existed.

Regarding incremental MIDX bitmaps (tb/incremental-midx-part-2), Taylor confirms the series has been rerolled but requests additional review from bitmap experts before merging. While the code changes aren't extensive, the implementation involves subtle bitmap handling logic across multiple layers and types. The series builds on previous work (tb/pseudo-merge-bitmap-fixes and tb/incremental-midx-part-1) to enable incremental updates of multi-pack index files with associated performance optimizations.

Key technical details for future reference: the MIDX bitmap work involves new iterator types (ewah_or_iterator), layer tracking for type bitmaps, and updates to pack-bitmap.c for handling incremental states. The cruft pack changes modify builtin/pack-objects.c's freshening behavior and include test consolidation in t/lib-cruft.sh. Both topics represent significant performance optimizations for large repositories.