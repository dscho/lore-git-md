This is a follow-up discussion about the recently merged build system improvements for unreachable code detection.

Taylor Blau raises a thoughtful point about the patch series ordering, suggesting that the workaround patch (using `errno` to check `sigfillset()` errors) should have come before enabling `-Wunreachable-code` warnings to avoid temporary build breakage for macOS/Clang users with DEVELOPER=1. He acknowledges this is now moot since the series is already merged to 'next', but the observation highlights an interesting consideration for future series sequencing.

The discussion shows good attention to developer experience and historical cleanliness, though it doesn't propose any action since the series is already finalized. This kind of retrospective analysis can be valuable for planning future patch series workflows.