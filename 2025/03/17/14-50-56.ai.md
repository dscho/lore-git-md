Technical discussion follow-up in Meson build system series.

This email continues the technical review of Patrick Steinhardt's patch series aligning Meson's breaking changes behavior with the Makefile build system. Phillip Wood raises two specific points about the implementation details in patch 3/3, which handles conditional installation of the `git-pack-redundant(1)` documentation.

First, Phillip questions whether adding `manpages_breaking_changes.keys()` to `configured_manpages` is redundant when breaking changes are enabled, since the manpage would already be included via `manpages.keys()`. Patrick had previously explained this is necessary for Meson's documentation sanity checking, which requires all potential documentation files to be registered regardless of conditional inclusion. Phillip now better understands this requirement but raises a new concern about potential duplicate entries in `configured_manpages` when breaking changes are enabled.

Second, Phillip identifies a potential issue with the unordered comparison between `configured_manpages` and `actual_manpages` (the sanity check that verifies all documentation files are properly accounted for). He notes the documentation doesn't specify whether dictionary keys are returned in sorted order, and the manual addition of other manpages (`git-bisect-lk2009.adoc` etc.) could create ordering mismatches even when the content is correct. He references Meson's documentation on dictionary key ordering to highlight this potential edge case.

The discussion remains focused on precise implementation details of the build system's documentation handling, specifically around the interaction between conditional inclusion and Meson's configuration validation requirements. Phillip's questions suggest the current implementation might need additional handling for either duplicate entries or sorting to ensure robust comparison behavior.