Refactoring of the refspec API (final patch in series). This is the fourth and concluding patch in Taylor Blau's series to modernize the refspec API, following the earlier protocol v2 fetch behavior improvements. The series has already merged patches 1-3, with this being the last mechanical conversion.

The patch replaces the generic `refspec_item_init()` function with fetch/push-specific variants `refspec_item_init_fetch()` and `refspec_item_init_push()`, mirroring the earlier split of `refspec_init()`. The change:
- Makes the API more explicit by removing the boolean `fetch` parameter
- Updates all existing callers in `builtin/fetch.c`, `builtin/pull.c`, and `refspec.c`
- Maintains the same error handling behavior (die on invalid refspecs)
- Keeps the internal `refspec_item_init()` as a static helper

The changes are straightforward mechanical conversions, building on the architectural decisions already settled in the protocol v2 work. The patch touches key refspec handling code but doesn't introduce new behavior - it's purely an API cleanup. With this change, the refspec API now consistently uses fetch/push-specific variants throughout, completing the simplification started earlier in the series.

The patch follows the same pattern as previous conversions in the series and appears to be a logical conclusion to the refactoring work. All tests continue to pass, and the changes are limited to the intended API modernization scope. The only remaining loose end from the series appears to be the accidental `git-diff-pairs` binary inclusion mentioned in the thread context.