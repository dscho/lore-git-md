This is a follow-up discussion about the build system improvement series for unreachable code detection, specifically addressing a temporary build breakage on macOS with Clang.

The email clarifies that the initial warning-enabling patch (db1d1f5d) is not yet in 'next', contrary to Taylor Blau's assumption that the series was already merged. Junio Hamano notes this means there was no actual period of broken builds for developers, as the fix (using errno to check sigfillset() errors) would have been applied before the warning was enabled.

The exchange shows careful consideration of patch ordering to avoid temporary regressions, though in this case the concern turned out to be moot. The thread demonstrates the project's attention to maintaining build stability even during transitional periods of warning enablement.