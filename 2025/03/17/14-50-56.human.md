This is a follow-up discussion in the Meson build system thread about conditional manpage installation with breaking changes. Phillip Wood questions whether the patch's approach of adding `manpages_breaking_changes.keys()` to `configured_manpages` is redundant or could cause issues with the manpage validation check.

The key points are:
1. Phillip initially thought the addition was unnecessary since `manpages_breaking_changes` gets merged into `manpages` when breaking changes are disabled
2. Patrick explains it's needed when breaking changes are *enabled* to ensure Meson knows about the manpage's existence for validation
3. Phillip raises new concerns about potential duplicate entries in `configured_manpages` and whether the unsorted list comparison with `actual_manpages` might cause problems

This is a technical refinement discussion about edge cases in the build system's manpage validation logic, specifically around how Meson handles conditional documentation files. The conversation shows careful attention to detail in ensuring the solution works correctly in all configurations while maintaining the build system's validation guarantees.