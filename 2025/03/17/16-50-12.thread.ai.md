# Git Thread Summary (AI mode)

Subject: bug report/documentation issue regarding `git switch --force` vs `--discard-changes` behavior with untracked files. Thread originated by David Mandelberg reporting a discrepancy between documented behavior (claiming these flags are aliases) and actual behavior. Key technical observation shows `--discard-changes` fails on untracked files while `--force` overwrites them, demonstrated by a `Makefile` conflict case in both fresh repositories and existing checkouts.

Junio Hamano provided detailed technical analysis tracing the behavioral divergence to implementation details in commits 3ec37ad1 (adding `--discard-changes` with new `.discard_changes` field) and 480d3d6b (introducing untracked file protection tied to `.force` rather than the new field). He notes this was likely an accidental divergence rather than intentional design, but considers the current behavior effectively frozen due to potential user reliance. 

Discussion continues with Mandelberg providing additional reproduction context from an empty directory setup while reinforcing Hamano's position that the current behavior provides finer-grained control (with `--discard-changes` protecting untracked files while `--force` overwrites them), though questioning whether this distinction justifies the confusion. Mandelberg remains neutral on behavioral changes, noting `git checkout -f` remains available as an alternative.

Core considerations remain: (1) whether to preserve the current distinction where `--discard-changes` protects untracked files while `--force` overwrites them, (2) potential to unify behaviors, or (3) documenting the difference as intentional. Implementation history shows the current behavior is internally consistent despite documentation mismatch. No strong push for implementation changes from either party; resolution likely leaning toward documentation clarification if behavior remains unchanged.