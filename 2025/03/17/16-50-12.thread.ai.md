# Git Thread Summary (AI mode)

Subject: bug report/documentation issue regarding `git switch --force` vs `--discard-changes` behavior with untracked files. Thread originated by David Mandelberg reporting a discrepancy between documented behavior (claiming these flags are aliases) and actual behavior. Key technical observation shows `--discard-changes` fails on untracked files while `--force` overwrites them, demonstrated by a `Makefile` conflict case. 

Junio Hamano provided detailed technical analysis tracing the behavioral divergence to implementation details in commits 3ec37ad1 (adding `--discard-changes` with new `.discard_changes` field) and 480d3d6b (introducing untracked file protection tied to `.force` rather than the new field). He notes this was likely an accidental divergence rather than intentional design, but considers the current behavior effectively frozen due to potential user reliance. 

Hamano acknowledges arguments both for maintaining the distinction (finer control) and for aligning with `checkout -f` simplicity, leaning toward documenting the difference rather than changing behavior. The core issue remains whether to preserve the current distinction where `--discard-changes` protects untracked files while `--force` overwrites them, or to unify them. The implementation history shows the current behavior is internally consistent in its treatment of untracked files despite the documentation mismatch. Resolution path appears to favor documentation clarification if the behavior remains unchanged.