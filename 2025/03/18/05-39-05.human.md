This is a follow-up to the recently merged protocol v2 fetch behavior improvements, focusing on refining the `followRemoteHEAD` functionality. Jeff King proposes two changes to how Git handles remote HEAD updates during fetches:

1. The main change limits `followRemoteHEAD` to only take effect when using configured refspecs (not ad-hoc fetches), implementing the simpler "option 1" approach that Junio Hamano favored in earlier discussion. This makes the behavior more predictable and matches what most users would intuitively expect.

2. A secondary optimization skips querying the remote HEAD entirely when `followRemoteHEAD` is explicitly set to "never", avoiding unnecessary network traffic. This is a straightforward improvement that builds on the first change.

The patches include tests verifying both behaviors and touch documentation to clarify the new rules. Jeff notes one remaining edge case (explicit `git fetch origin HEAD` behavior) that isn't addressed here but could be considered separately. The changes are cleanly implemented with significant code reduction, building on the recently merged refactoring work.

This appears to be a well-considered refinement of the protocol v2 fetch behavior, addressing the last piece of the architectural changes discussed in the thread. The patches are small, focused, and come with test coverage, suggesting they're ready for review and likely to be uncontroversial.