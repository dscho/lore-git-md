This is a follow-up clarification in the build system improvement thread regarding the `NOT_CONSTANT` macro implementation. Calvin Wan points out a discrepancy in the object file naming between the patch and what's in the `next` branch (`not-a-constant.o` vs `not-constant.o`), but quickly retracts the observation as being specific to their internal build setup rather than an actual issue in the main Git codebase.

The email doesn't introduce new technical content but serves to prevent potential confusion about what appears to be a naming inconsistency. The core work - adding the `NOT_CONSTANT` macro and its supporting infrastructure to handle unreachable code detection - remains unchanged and was already properly reviewed and merged. This is just a minor clarification that the naming difference some might notice isn't a real problem in the upstream code.