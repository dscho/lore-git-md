This is part 6/9 of the `the_repository` removal series, converting `builtin/ls-files.c` to use explicit repository parameters instead of the global variable. The patch follows the established pattern seen in previous conversions in this series.

The changes focus on passing the repository context through to helper functions `expand_objectsize()` and `show_ru_info()`, which previously relied on `the_repository`. The patch maintains the existing behavior where `-h` help output works both inside and outside a Git repository by properly handling NULL `repo` cases. Test coverage is added to verify this behavior.

The conversion appears mechanical and well-contained, following the same high-quality approach as earlier patches in the series. The changes touch core functionality like object size expansion and resolve-undo display, but the modifications are straightforward parameter additions without behavioral changes. The patch includes appropriate test coverage for the help output behavior in line with the series' pattern.