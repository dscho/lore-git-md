Here's a human-readable summary of this thread:

The thread addresses an issue with Git's date parsing behavior, specifically how "now"/"today" and "yesterday" specifications are handled. The patch series (this being the second part) fixes a subtle bug where these time specifications could produce inconsistent results depending on when the command was run, particularly affecting cases like "noon today". 

The solution modifies the date.c internals to ensure "now"/"today" and "yesterday" always refer to the full calendar day by resetting the day field in the time structure. This makes the behavior more intuitive and consistent - "noon today" will now reliably mean noon on the current calendar day regardless of when the command is executed. The changes include both the core fix and corresponding test updates to verify the corrected behavior.

This appears to be a straightforward improvement to date handling that affects any Git feature using approxidate parsing (like log filtering). The patch seems well-constructed with clear motivation and test coverage, suggesting it should be uncontroversial.