CI infrastructure fix for Meson documentation builds. Junio Hamano follows up on Jeff King's observation that CI jobs were failing with "meson: command not found" during documentation builds. The patch simply adds `meson` to the package list in `ci/install-dependencies.sh` for the Documentation job, which resolves the immediate failure as shown in the linked CI runs.

The change is straightforward but raises architectural questions about how CI dependencies should be managed between distro-specific and job-specific setups. Junio notes the current system's unclear separation between these layers and invites someone more familiar with the CI infrastructure to improve the commit message's explanation. While this fixes the symptom, the thread suggests there may be deeper questions about proper dependency management in the CI system that warrant future attention.