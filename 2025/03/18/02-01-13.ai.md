This is a technical review discussion in Taylor Blau's incremental MIDX bitmap series (v4 11/13), focusing on type bitmap tracking across MIDX layers. Jeff King examines the implementation where each layer's type bitmaps (commits, trees, blobs, tags) are stored in arrays (`*_all` fields) in the top-level `bitmap_index` struct. He notes these arrays are meaningful only at the top level of the MIDX chain, as lower layers wouldn't need their incremental versions.

Key technical points:
- The patch introduces four new arrays in `struct bitmap_index` to track type bitmaps across all layers (`commits_all`, `trees_all`, `blobs_all`, `tags_all`)
- `load_all_type_bitmaps()` populates these arrays by walking the layer chain
- Jeff observes the current implementation always allocates new arrays, even for non-incremental cases where simpler pointer references could suffice
- He characterizes potential optimizations (direct pointer assignment vs allocation) as micro-optimizations that may not be worth the added complexity
- The discussion maintains the thread's pattern of identifying potential refinements while acknowledging the current implementation's correctness

This continues the thread's technical depth around MIDX bitmap layer management, with Jeff's review focusing on memory efficiency patterns rather than core functionality. The observation about array allocation strategy follows previous discussions of similar implementation details in the series.