Completion script testing and portability follow-up in git-branch documentation series.

This email from Jean-Noël Avila responds to Junio C Hamano's concerns about testing coverage for the config list generation in the completion script fix (patch 1/2 of the series). The discussion reveals two key points about the testing infrastructure:

1. The existing test suite (`t0012`) doesn't adequately validate the completeness of generated config lists, as Junio demonstrated by showing broken regex patterns could pass while producing incorrect output. This testing gap affects all config option completions derived from documentation.

2. The regression was actually detected by `t9902` (completion tests) when working with git-branch documentation, specifically when testing completion for `git config get br` which failed to find 'branch.' options. This shows the tests can catch some issues, but only when they manifest in specific completion scenarios.

Jean-Noël suggests committing `config-list.h` as a way to validate the script's output, though this would be a new testing approach. The email confirms the author is proceeding with the promised ERE conversion for portability while acknowledging the broader testing coverage issue that may require additional work beyond the current series.

The discussion highlights that while the documentation standardization portion (patch 2) is approved, the completion script fix now has expanded considerations around both portability (addressed via ERE conversion) and testing validation (which may need follow-up work). The thread shows productive maintainer-author collaboration with clear identification of technical gaps.