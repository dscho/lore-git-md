HTTP TCP keepalive configuration patch series:

This thread introduces new HTTP configuration options for fine-grained control of curl's TCP keepalive behavior through a three-patch series, addressing bandwidth-limited clone failures. The work maintains its original structure: helper function refactoring (patch 1), curl handle preparation cleanup (patch 2), and three new config options implementation (patch 3).

Key technical details remain stable:
- Static variables curl_tcp_keepidle/keepintvl/keepcnt (-1 defaults)
- Environment variables GIT_HTTP_KEEPALIVE_* override config
- Clean application in get_curl_handle()

Patrick Steinhardt's latest review focuses on two aspects of patch 3:
1. Questions whether default values should be adjusted for better out-of-box behavior in bandwidth-limited scenarios, relating back to the original use case
2. Notes redundant type casting in config parsing (int→long promotion happens automatically)

The discussion has evolved from initial architectural approval to implementation refinements:
- Earlier consensus on set_long_from_env() helper remains, with ongoing consideration of error handling improvements
- Core functionality of three config options (keepAliveIdle/Interval/Count) is uncontested
- Documentation coverage remains thorough

Open questions:
- Should default values be adjusted from curl's defaults for Git's common cases?
- Can the config parsing implementation be further cleaned up (type casts, error handling)?

The thread shows converging consensus with attention shifting to polish details rather than fundamental design. Patrick's engagement signals the series is nearing readiness, pending resolution of these final implementation considerations.