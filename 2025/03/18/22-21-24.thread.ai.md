HTTP TCP keepalive configuration patch series:

This thread introduces new HTTP configuration options for fine-grained control of curl's TCP keepalive behavior through a three-patch series, addressing bandwidth-limited clone failures. The work maintains its original structure: helper function refactoring (patch 1), curl handle preparation cleanup (patch 2), and three new config options implementation (patch 3).

Key technical details:
- Static variables curl_tcp_keepidle/keepintvl/keepcnt (-1 defaults)
- Config options: http.keepaliveidle, http.keepaliveinterval, http.keepalivecount
- Environment variables GIT_HTTP_KEEPALIVE_* override config
- Implementation in http.c's config callback and get_curl_handle()

Recent developments:
- Patch 2/3 was updated to remove obsolete set_curl_keepalive() helper function
- Taylor Blau plans to remove unnecessary (long) casts in config parsing per Patrick Steinhardt's feedback
- Default values discussion concluded that curl defaults are sufficient for most users

Open questions resolved:
1. Default values: Consensus reached that curl defaults are appropriate, with customization available for special cases
2. Implementation details: Unnecessary (long) casts will be removed in a follow-up patch

The thread shows converging consensus with all substantive discussions resolved. The series appears ready for finalization with only minor cleanups remaining. Engagement from multiple contributors (Patrick Steinhardt, Elijah Newren, Taylor Blau) has refined the implementation while maintaining the core approach. The three config options for TCP keepalive control remain uncontested with thorough documentation coverage.