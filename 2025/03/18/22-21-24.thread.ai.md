HTTP TCP keepalive configuration patch series:

This thread introduces new HTTP configuration options for fine-grained control of curl's TCP keepalive behavior. The series from author [name TBD] currently has two preparatory patches with the main feature implementation still to come.

The first preparatory patch adds helper function `set_long_from_env()` for environment variable parsing into long integers, replacing manual `strtol()` handling of `GIT_HTTP_LOW_SPEED_LIMIT` and `GIT_HTTP_LOW_SPEED_TIME`. This follows the pattern of the existing `set_from_env()` function while adding numeric conversion capability. The second patch inlines the `set_curl_keepalive()` function as additional preparation.

The core change (not yet sent) will add three new http.* configuration options in http.c:
- http.keepAliveIdle (time before first keepalive probe)
- http.keepAliveInterval (time between probes)
- http.keepAliveCount (number of probes before closing)

These will provide more granular control than the existing binary http.keepalive setting. The implementation will build on the refactored HTTP infrastructure with corresponding documentation updates. The author has noted testing challenges for the TCP-level behavior that the main patch will need to address.

The changes so far are focused on http.c with straightforward refactoring to support the upcoming feature. The preparatory work demonstrates good code hygiene by factoring out common patterns before adding new functionality that will use them. The series remains well-contained within the HTTP subsystem without affecting other areas of the codebase.