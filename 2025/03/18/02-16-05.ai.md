Performance-focused review of MIDX bitmap writing tests.

Jeff King provides a focused review of the test coverage for incremental MIDX bitmap writing in patch 13/13 of Taylor Blau's series. He suggests adding explicit verification that multiple bitmap layers exist in the test suite, though acknowledges this is likely already covered implicitly by the existing test that checks the midx_chain file count. The test in question already validates the migration of MIDX files from packdir to midxdir when creating a new layer, including the expected file presence/absence patterns and the layer count in midx_chain.

Key technical details mentioned:
- The test infrastructure's `write_midx_layer` helper function
- File location validations between packdir and midxdir
- Layer counting via `test_line_count` on midx_chain
- The `GIT_TEST_MIDX_WRITE_REV` test knob usage
- File naming patterns for MIDX components (midx, bitmap, rev)

This continues the pattern of Jeff King's thorough but non-blocking reviews in this series, focusing here on ensuring complete coverage of the bitmap writing functionality. The suggestion is minor and doesn't indicate any architectural concerns with the implementation. The test infrastructure appears robust, with multiple layers being created and verified through both direct file checks and higher-level operations like `git rev-list --test-bitmap`.