Refspec API cleanup in the ongoing modernization series. This is the third patch in a 4-part series that follows the foundational Boolean conversion of the refspec API (already merged). Taylor Blau removes the thin wrapper function `refspec_item_init_or_die()` as part of streamlining the API before introducing fetch/push-specific variants.

The change eliminates one layer of indirection by inlining the error handling at the two call sites (`builtin/pull.c` and `refspec.c`). This prepares for the next patch which will introduce typed initialization functions without proliferating `_or_die` variants. The diff shows straightforward mechanical changes - replacing wrapper calls with direct initialization plus error checking.

This continues the thread's theme of simplifying the refspec API while maintaining behavior. The patch is small and focused, with clear justification in the commit message about avoiding API bloat. It builds on the established design direction from earlier in the thread where the Boolean simplification and typed initialization approach was agreed upon by Taylor Blau, Jeff King, and Junio Hamano.