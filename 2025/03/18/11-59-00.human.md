This is part 8/9 of the `the_repository` removal effort, converting `builtin/checkout-index.c` to use explicit repository parameters instead of the global variable. The patch follows the established pattern seen in previous conversions in this series, with Usman Akinyemi (mentored by Christian Couder) handling the checkout-index command.

The changes systematically replace `the_repository` usage with the passed `repo` parameter and propagate the index state through helper functions. The patch maintains the command's behavior while modernizing its internal API usage. A new test verifies that `-h` help output works correctly both inside and outside a Git repository context, matching the testing approach used in other patches in this series.

The conversion is clean and mechanical, following the same high-quality pattern seen throughout this effort. The diff shows straightforward replacement of global variable access with parameter passing, with the bulk of changes being in the `checkout_file()` and `checkout_all()` helper functions. This continues the series' consistent approach to removing `the_repository` dependencies while maintaining existing functionality.