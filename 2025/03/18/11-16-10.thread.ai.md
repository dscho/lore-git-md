Thread summary:

Bugfix addressing OpenSSL 3+ compatibility regression in hash context initialization, caused by commit 0578f1e66a ("global: adapt callers to use generic hash context helpers"). The change accidentally removed required `->init_fn` calls for temporary hash contexts in packfile operations, leading to "invalid index-pack output" errors during fetches. The patch restores initialization of `tmp_ctx` in both `builtin/index-pack.c` and `builtin/unpack-objects.c` before context cloning. The issue specifically manifests with OpenSSL 3+ SHA1 implementations which enforce proper initialization sequencing. The changes are minimal and targeted, with identical fixes applied to both affected commands that handle packfile operations. No open questions or outstanding issues mentioned; this appears to be a straightforward restoration of accidentally removed functionality.