This is a brief technical clarification request in the ongoing MIDX bitmap implementation series. Jeff King notes a naming inconsistency in the EWAH (Efficient Word-Aligned Hybrid) bitmap iterator implementation - the free function is named `ewah_or_iterator_free()` when previous discussion suggested it should follow the `_release()` pattern used elsewhere in the codebase.

The comment is narrowly focused on this one function name, suggesting Taylor Blau may have missed updating it from an earlier version. This appears to be a minor oversight in an otherwise technically complete patch series, and the fix would be straightforward (just renaming the function). The exchange maintains the thread's pattern of careful code review focused on consistency and maintainability.