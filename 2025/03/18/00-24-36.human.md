This is a final review comment on the refspec API refactoring series, which has already been merged. Jeff King (Peff) responds to Taylor Blau's patch that converted the refspec 'fetch' field from an enum to a boolean value.

The key points are:
- Jeff agrees with the general direction of simplifying the API by using a boolean instead of an enum, though he notes the original enum wasn't necessarily "misleading" as the commit message suggested
- He raises one minor concern about readability when boolean literals (1/0) replace the more descriptive REFSPEC_FETCH/PUSH constants in function calls
- He also humorously points out an accidental binary file (git-diff-pairs) that was included in the diff

This email represents a post-merge review comment rather than a blocking concern, as the series has already been accepted. The discussion shows the Git maintainers' attention to detail even after patches are merged, ensuring the final implementation meets their standards for readability and correctness.