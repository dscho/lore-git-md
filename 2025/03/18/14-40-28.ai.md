Documentation patch fixing formatting in the `git-pack-refs` man page.

This is a straightforward documentation fix adjusting the indentation of the `--exclude` option description in `git-pack-refs.adoc`. The patch changes empty lines between paragraphs to use `+` markers instead, which ensures consistent indentation throughout the entire option description rather than just the first paragraph. The change affects only the man page formatting - there are no behavioral changes to the `--exclude` option itself.

The patch is small (4 lines changed) and focused purely on documentation presentation. Phillip Wood correctly identifies the issue and provides a clean solution using AsciiDoc's `+` continuation marker syntax. The change improves readability of the man page by maintaining consistent indentation for multi-paragraph option descriptions.

Key technical details for future reference:
- Modifies `Documentation/git-pack-refs.adoc`
- Affects the `--exclude` option documentation
- Uses AsciiDoc's `+` syntax for paragraph continuation
- No functional changes to `git-pack-refs` behavior
- Follows existing documentation conventions

The patch appears well-formed and correctly targeted - a simple but useful improvement to the documentation formatting.