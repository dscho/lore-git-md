This is a small behavior change patch affecting `git format-patch`'s notes handling. The patch modifies how commit notes are formatted when using `--notes` with email/mbox output formats (CMIT_FMT_EMAIL and CMIT_FMT_MBOXRD), switching them to use raw formatting rather than the default pretty-printed format.

The change is minimal - just a 3-line modification to log-tree.c that expands the conditions under which notes use raw formatting. The commit message argues that the current default formatting "doesn't make a very good fit" for these output formats, though it doesn't provide specific examples of problems with the current behavior or benefits of the change.

The technical impact appears to be that notes will now be displayed in their raw form (preserving original whitespace and formatting) rather than being reflowed when included in format-patch output. This could affect tools or workflows that parse the patch emails, though the commit doesn't mention any specific downstream consumers.

The patch seems straightforward and low-risk, though the motivation could be more clearly explained. It touches a core component (log-tree.c) but in a very contained way. There's no test coverage included to verify the behavior change, which might be worth requesting if this goes through review.