This is the second patch in a performance optimization series for bundle-uri reference negotiation, specifically focusing on test coverage for tag handling. The patch adds comprehensive tests verifying that tags are properly handled when cloning via bundle-uri, completing the test coverage started in the first patch of the series.

The test suite additions create a repository with multiple branches and tags, then verify that cloning via bundle-uri correctly preserves the tag references under the `refs/bundles/tags/` namespace. The test checks both lightweight tags (A, B) and an annotated tag (tag-A), ensuring all are properly transferred and placed in the expected ref hierarchy. This matches the behavior already tested for branch references in the first patch.

Key technical details include:
- New test file `t/t5558-clone-bundle-uri.sh` additions (31 lines)
- Verifies tag preservation in bundle clones
- Confirms tags appear under `refs/bundles/tags/` namespace
- Tests both lightweight and annotated tags
- Maintains consistency with the branch handling tests from patch 1/2

The patch follows the agreed-upon structure from earlier feedback, keeping tag-specific tests separate from the core functionality tests while maintaining the same rigorous validation approach. This completes the test matrix for the bundle-uri reference expansion feature, covering all reference types while preserving namespace hierarchy.