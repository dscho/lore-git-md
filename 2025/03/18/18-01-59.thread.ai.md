Updated AI thread summary:

Thread identity: Four-patch series fixing edge cases in Git's date approximation logic ("approxidate"), focusing on special date/time formats combined with explicit day specifications. Series v3 submitted by Tuomas Ahola. References a 2006 bug report by Linus Torvalds, indicating these are long-standing known quirks. The structural change from v2 is significant: the "today" alias has been pulled out into its own standalone patch (now patch 2/4) placed before the core behavioral fix (now patch 3/4), making it independently negotiable. Patch 1/4 (test infrastructure) has now been re-reviewed in detail and is clean. Patches 1/4 and 3/4 address all open items from Junio's and Jeff King's v2 reviews. Patch 4/4 (deferred-mday scheme for "now"/"yesterday") is unchanged in logic from v2 and awaits Junio's first review.

Core problem: Current approxidate handling has multiple issues when processing special date/time strings with explicit day components:
1. Ignores day-of-month with special date strings (e.g., "last week 15th" dropped "15th")
2. Inconsistent tm_mday field setting for "now"/"yesterday" calculations
3. Incorrect day rollover for time-based specials ("noon", "tea") when combined with explicit days -- the "most recent" rollback fires even when a specific day was explicitly provided, producing a contradictory date (e.g., "10 May at tea" on 2026-05-12 11:00 yields 2026-05-09 instead of 2026-05-10)
4. Month/year rollover edge cases when processing dates near boundaries with time-based specials

Current status: v3 submitted. The series is now four patches. Patch 1/4 (test infrastructure) has been revised per Junio's and Jeff King's feedback and is clean with no open issues. Patches 2/4 and 3/4 are ready for further review; patch 4/4 awaits Junio's first review. The series is in re-review.

Key technical details:
- Total delta: similar scope to v2 (~45 insertions, mostly tests; 37 lines changed in date.c spread across three patches)
- Files touched: date.c, t/t0006-date.sh
- Base commit: recent tip from 2026
- Patch 1/4: adds time-of-day offset support to `check_approxidate` test helper. Accepts human-readable offset strings like `+2 days` via an `add_time_offset` helper function using `case` dispatch over `hours`/`days`, addressing Jeff King's v2 interface-design objection to opaque relative integers. Shell logic restructured from a single arithmetic expression into an explicit `if/else` block, with `test` command used instead of `[ ... ]`, and `export` moved to a single declaration -- addressing all three of Junio's v2 coding-style requests. Properly handles the `failure` expectation case when no offset is given. Two new test cases demonstrate the feature: `"January 5th yesterday"` evaluated at different "now" times.  No open issues.
- Patch 2/4: adds "today" as an alias for "now" in the specials table. Same one-liner as v2 patch 2/3, now isolated as its own patch. This is the most contentious item in the series. Junio raised a functional concern about `--since=today` excluding earlier-today commits because `--since` is exclusive ("strictly after"). The author's reply, included as a note in the patch, indicates he may not fully grasp the concern: he describes `--since=today` as effectively a no-op (which is wrong if it excludes all of today's history), and then proposes a fix that would make it behave like `--since=midnight` only when the current time exactly matches the time that was parsed (midnight), which would rarely trigger. That proposed fix would not solve the actual problem. The design discussion is not yet resolved. If dropped, only the alias line and associated test cases are removed; patches 3/4 and 4/4 are unaffected.
- Patch 3/4: core fix -- in `date_time()`, checks whether `tm_mday` has already been set to a non-negative value (meaning a specific day was provided) and if so, skips the "most recent" rollback logic entirely. This is the same three-line change to `date.c` as v2 patch 2/3's core fix, minus the "today" alias. Addresses Junio's v2 requests for clearer commit message with concrete examples and improved comment wording.
- Patch 4/4: introduces deferred-adjustment scheme for "now" and "yesterday" specials, using negative tm_mday values as markers resolved later by `update_tm()`. 25 lines changed in date.c, 4 lines in test file. Logic unchanged from v2 patch 3/3; awaits Junio's first review.

Open questions and loose ends:
- Patch 2/4 ("today" alias): the maintainer's functional concern about `--since=today` excluding earlier-today commits remains unresolved. The author's proposed workaround is insufficient. This needs a substantive response from Tuomas Ahola before the patch can land. This is the most likely patch to be dropped.
- No additional open items on patches 1/4 or 3/4 -- all v2 feedback addressed. Patch 1/4 has been re-reviewed and is clean.
- Patch 4/4 has not yet been reviewed by Junio. It is the only patch whose logic has never been evaluated by the maintainer.
- All remaining open items on patches 3/4 and 4/4 are resolvable. No structural objections to any patch except the "today" alias semantics.

Participant positions:
- Tuomas Ahola (author): submitted v3 with a significant structural improvement (splitting out the alias patch). Addressed all coding-style and interface-design feedback from Junio and Jeff King on patches 1/4 and 3/4. His response to Junio's "today" alias concern suggests a misunderstanding of the semantics -- he proposed an insufficient fix that would rarely trigger. Needs to re-engage on this item.
- Jeff King (reviewer): provided Reviewed-by on the v2 series. His v2 interface-design observations (positional argument awkwardness, opaque relative offsets) were addressed by accepting human-readable offset strings in patch 1/4. Has not reviewed v3 yet.
- Junio C Hamano (maintainer): reviewed patches 1/3 and 2/3 in v2. All three coding-style requests on patch 1/3 have been addressed in patch 1/4. The "today" alias concern (patch 2/4) remains open and the author has not yet grasped the issue. Has not reviewed patch 4/4 (unchanged from v2 patch 3/3) yet.
- Linus Torvalds: original bug reporter (2006), not a current participant.

Related work:
- Old bug report by Linus Torvalds from 2006, indicating these edge cases have been known for two decades.
- Prior v1 and v2 of this series (same subject line, v1 by a different author) received active review from Jeff King and Junio.
- Builds on Git's existing date parsing infrastructure (approxidate in date.c).