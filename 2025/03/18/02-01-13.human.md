This is a technical discussion about the MIDX bitmap implementation, specifically addressing how type-level bitmaps are tracked across layers in the incremental MIDX chain.

Jeff King responds to Taylor Blau's patch that introduces arrays (`commits_all`, `trees_all`, etc.) to track type bitmaps across MIDX layers. He confirms his understanding that these arrays are only valid at the top level of the chain, noting it wouldn't make sense for lower layers to maintain this information.

The main technical discussion centers around the `load_all_type_bitmaps()` function, which allocates and populates these arrays. Jeff suggests a potential optimization for the non-incremental case where the arrays could simply point to the single existing bitmap fields rather than allocating new storage, though he acknowledges this might not be worth the complexity. He also notes that any serious optimization effort would likely focus on reducing pointer-chasing overhead rather than this allocation pattern.

This is a relatively minor point in the broader MIDX bitmap series, showing Jeff's continued careful review of implementation details even as the core functionality appears solid. The tone remains constructive, with Jeff offering suggestions while clearly marking them as optional refinements rather than required changes.