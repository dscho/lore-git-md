Here's the updated AI thread summary:

This is a two-part patch series fixing date parsing behavior in Git's approxidate functionality. The current patch (second in series) addresses how "now"/"today" and "yesterday" specifications are handled, ensuring they always refer to full calendar days rather than being influenced by the current time. The key technical change modifies `date_now` and `date_yesterday` functions in date.c to set `tm->tm_mday = -1`, forcing full-day calculation. This affects cases like "noon today" which previously could resolve to different days depending on execution time. The patch adds "today" as an alias for "now" in the special keywords table and updates tests to verify the fixed behavior. The changes impact any feature using approxidate parsing (e.g., `git log --since`). The implementation appears non-controversial with clear commit message and test coverage. No outstanding questions or objections noted in the thread.