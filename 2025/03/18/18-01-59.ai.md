This is a two-patch series fixing edge cases in Git's date approximation logic ("approxidate"). The changes address inconsistencies in how special date formats like "yesterday" and "now" handle day-of-month calculations.

The first patch makes "specials" respect a fixed day-of-month when one is provided (e.g., "last week 15th"). Currently the day component gets ignored in these cases. The second patch ensures the tm_mday field gets properly set for "now" and "yesterday" calculations, fixing cases where the test time could mask incorrect behavior.

The changes are focused on date.c's approximation logic and come with corresponding test updates in t0006-date.sh. The author notes these were prompted by observing that some tests only passed due to fortunate timing in the test environment. The patches appear to be narrow, targeted fixes rather than broader refactoring, touching just 10 lines total with a 6:4 add/delete ratio.

For future reference in this thread: key changes involve the handling of tm_mday in date.c's approxidate functions, particularly how "specials" like "now" and "yesterday" interact with explicit day specifications. The test changes in t0006-date.sh verify the fixed behavior works correctly regardless of when tests are run.