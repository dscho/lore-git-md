Performance optimization for bundle-uri reference handling, now in its third iteration. This series improves the efficiency of bundle-uri fetches by expanding reference copying from just `refs/heads/` to all `refs/` while preserving hierarchy, which significantly reduces subsequent object downloads (from 32% to 1% in testing). The current version addresses feedback from Derrick Stolee by squashing the first two patches together for better bisectability while keeping the tag handling tests separate.

The main patch now combines both the core functionality change (modifying `bundle-uri.c` to handle all references) and the initial test updates. The second patch remains focused on verifying tag handling behavior. The implementation appears solid with comprehensive test coverage in `t/t5558-clone-bundle-uri.sh` that validates various scenarios including path-based/HTTP bundles, bundle lists, failure cases, and negotiation behavior.

The series has addressed all major technical concerns raised in previous rounds, with Stolee confirming the approach is sound. The remaining open questions about edge cases (like multiple bundle reference collisions) don't appear to block merging. This looks ready for final review, offering measurable performance benefits while maintaining existing fetch behaviors.