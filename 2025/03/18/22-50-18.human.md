This is a refactoring patch that simplifies the refspec API by treating the 'fetch' field as a pure Boolean value rather than using symbolic constants. It's part of a 4-patch series (1/4) that modernizes the refspec handling code.

The patch removes the `REFSPEC_FETCH` and `REFSPEC_PUSH` macros that were previously used to indicate direction, since these were effectively just Boolean values in disguise. The field is explicitly declared as a single-bit unsigned value to make its Boolean nature clear, though this doesn't change the struct's size in practice.

The changes touch several files that initialize or check the fetch field, replacing macro checks with direct Boolean tests. While this makes the code simpler, it introduces some temporary awkwardness in calls like `refspec_item_init_or_die(&spec, refspec, 1)` where the meaning of "1" isn't immediately clear - the commit message notes this will be addressed in follow-up patches.

This appears to be a well-considered cleanup that follows earlier discussion in the thread about simplifying the refspec API. The patch is part of a larger effort to modernize protocol v2 fetch behavior and refspec handling that has already seen extensive review and discussion. The changes are mechanical and focused, with no apparent controversy remaining.