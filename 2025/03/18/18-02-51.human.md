This is a small bugfix/improvement patch targeting `git format-patch`'s notes handling. 

The patch changes how commit notes are formatted when using `git format-patch --notes` with email or mboxrd output formats. Currently, notes get formatted in a way that doesn't work well for these formats, while the raw format would be more appropriate. 

The change is minimal - just a 3-line modification to `log-tree.c` that extends the conditions under which notes are output in raw format to include `CMIT_FMT_EMAIL` and `CMIT_FMT_MBOXRD` cases. The commit message suggests this is a clear improvement, though it doesn't provide specific examples of the problematic formatting being fixed.

The patch appears straightforward and focused, though it might benefit from a more detailed explanation of what exactly was wrong with the current formatting and how the raw format improves things. The change seems uncontroversial and unlikely to break anything, as it's expanding an existing condition rather than changing core behavior.