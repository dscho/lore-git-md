This is the final patch in Christian Couder's promisor-remote bugfix series, addressing case sensitivity in remote name comparisons. The change aligns promisor-remote behavior with Git's standard handling of remote nicknames, which are case-sensitive in configuration (e.g., "origin" vs "Origin" are treated as distinct).

The patch makes two key changes: updating the documentation to explicitly state that name comparisons are case sensitive, and modifying the `remote_nick_find()` function to use `strcmp()` instead of `strcasecmp()`. This matches Git's behavior for remote configuration sections where the nickname portion is case-sensitive while other components are not.

The change is straightforward but important for consistency with Git's configuration parsing rules. The patch builds on the series' earlier improvements to NULL pointer handling and URL validation, completing the hardening of promisor-remote functionality. With this final patch, the series now provides comprehensive protection against edge cases while maintaining consistent behavior with Git's configuration system.