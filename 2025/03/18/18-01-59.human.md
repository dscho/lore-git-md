This is a two-patch series fixing edge cases in Git's date approximation logic ("approxidate"). The changes address inconsistencies in how special date formats like "yesterday" and "now" handle the day-of-month field.

The first patch ensures that fixed day-of-month specifications (like "5th yesterday") are properly respected rather than being overwritten by the special date logic. The second patch fixes cases where the tm_mday field wasn't being properly set for "now" and "yesterday" calculations, which could lead to incorrect dates being generated.

The motivation came from observing that some tests were passing only because of fortunate timing in their test setup, masking these underlying issues. The changes are relatively small (10 lines added/changed across two files) but address subtle correctness issues in date handling. The test updates demonstrate the fixes by making the tests more robust against timing variations.

This appears to be a focused, well-motivated cleanup of edge cases in date parsing. The changes are narrow in scope but improve the reliability of a fundamental Git feature used in log filtering and other date-based operations.