This is a cleanup patch in the `the_repository` removal effort, specifically addressing `update-server-info`. It's the final patch in René Scharfe's 9-part series converting builtin commands to use explicit repository parameters.

The change removes a now-redundant NULL check before calling `repo_config()`, since the function already handles NULL repositories internally. This follows from earlier changes in the series that modified `repo_config()`'s behavior. The patch is mechanically simple - just deleting the conditional wrapper while maintaining the same functionality.

As with the rest of the series, this maintains the high standard of focused, well-justified changes that don't affect observable behavior. The change is small but meaningful, cleaning up code made obsolete by the broader architectural shift away from `the_repository`. The patch includes proper attribution to the mentor who guided the change (Christian Couder) and follows the series' pattern of thorough but minimal modifications.

This concludes the series' systematic conversion of builtin commands to the new repository-handling paradigm, with all patches demonstrating careful attention to maintaining correct behavior while modernizing the codebase.