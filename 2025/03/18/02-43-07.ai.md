This is a technical refinement patch (v4 02/13) in Taylor Blau's incremental MIDX bitmap series, addressing pack-revindex adjustments needed to support layered MIDX operations. Elijah Newren provides minor editorial feedback on the patch.

The changes focus on adapting the reverse index machinery to handle object lookups across MIDX layers, with key modifications:
- Introduces `bitmap_non_extended_bits()` helper to calculate total objects across MIDX layers
- Updates `pack_pos_to_midx()` and `midx_to_pack_pos()` to work with global positions across MIDX layers
- Adjusts `midx_pack_order_cmp()` to account for base layer offsets
- Modifies various bitmap operations to use the new position calculations
- Adds safety checks for layer boundaries and NULL MIDX cases

Elijah's feedback is limited to a single grammatical nit (s/finds/find/) in the commit message, suggesting the technical content is sound. The changes align with the series' goal of enabling efficient bitmap operations across MIDX layers while maintaining the existing architectural patterns. This appears to be a straightforward infrastructure patch that builds on the established design from previous iterations.