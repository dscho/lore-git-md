This is a maintainer response in the reflog enhancement thread, specifically addressing naming and behavior consistency concerns around the new `--single-worktree` flag.

Junio Hamano weighs in on the discussion about the potentially confusing behavior when `--single-worktree` is used without `--all`, which currently silently does nothing (matching the existing `expire` subcommand's behavior). He acknowledges Christian Couder's point that this is unfortunate but suggests the team could either:
1. Accept this as maintaining consistency with `expire`
2. First improve `expire`'s behavior and naming, then align the new `drop` subcommand with those improvements

The email indicates the series is otherwise in good shape, with Junio expressing openness to either approach for handling the worktree flag naming and behavior. This positions the discussion as a relatively minor polish item that doesn't block the current implementation, while leaving the door open for future improvements to both subcommands' interfaces. The thread context shows this comes after all substantive review feedback has been addressed, with only these final UX considerations remaining.