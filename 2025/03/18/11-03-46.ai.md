This is a follow-up discussion in Christian Couder's promisor-remote bugfix series, specifically addressing test isolation improvements for the NULL pointer dereference fixes. The email responds to Junio Hamano's review comments about test cleanup ordering in the test script.

The key points in this response are:
1. Christian acknowledges Junio's observation that `test_when_finished` cleanup commands should be placed before the operations they're meant to clean up (like `git clone`), not after
2. He indicates this change has been implemented in the next version (v6) of the series
3. He's expanding the cleanup approach to all tests in the script for consistency
4. Additional cleanup improvements are being added for URL configuration tests, including:
   - Moving `test_when_finished` before config modifications
   - Adding client directory cleanup even when clone succeeds
   - Applying this to both the "KnownUrl with unset URL" and "KnownUrl with empty URL" test cases

The changes demonstrate careful attention to test robustness, ensuring clean state between tests even in failure scenarios. This builds on the series' existing focus on hardening promisor-remote handling of edge cases, now extending that rigor to the test infrastructure itself. The response shows the iterative refinement process typical of Git development, where review feedback leads to broader improvements beyond the immediate issue.