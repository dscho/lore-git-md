This is the final patch in René Scharfe's 9-part series removing `the_repository` usage, specifically addressing a now-redundant NULL check in `update-server-info`. The change is a straightforward cleanup following earlier work that made `repo_config()` handle NULL repositories safely.

The patch removes an explicit NULL check before calling `repo_config()` since the function now handles NULL repositories internally. This matches the series' pattern of mechanical conversions to use explicit repository parameters. The change is minimal (just 2 lines modified) and maintains the same behavior while simplifying the code.

Key technical details for future reference:
- Modifies `builtin/update-server-info.c` to remove conditional `repo_config()` call
- Builds on prior changes that made `repo_config()` NULL-safe
- Maintains identical functionality while reducing code complexity
- Follows the series' established pattern of mechanical conversions
- Includes Mentored-by tag indicating guidance from Christian Couder

This concludes the series with a small but logical cleanup, demonstrating how earlier architectural work enables simplifications in dependent code. The change is low-risk and well-contained, fitting the series' overall quality standard.