Documentation patch correcting vimdiff mergetool buffer marking syntax. This is version 2 of a straightforward fix that aligns the documentation with the implementation.

The patch corrects a long-standing documentation error where the vimdiff mergetool docs incorrectly stated buffers should be marked with `*` when `MERGED` is absent from layout. In reality, the implementation has always used `@` (arobase) since the feature was introduced in commit 7b5cf8be18.

The author confirms in this follow-up email that while an early version did use asterisks, the documentation was never updated when the implementation switched to using @ symbols. This explains the discrepancy between docs and code.

This is a clean, uncontroversial documentation fix that has already incorporated feedback from the initial review round (including proper sign-off and terminology). The change affects only a single line in `Documentation/mergetools/vimdiff.adoc` and doesn't require any behavior changes since the code already works correctly.

The thread shows good collaboration between the author and reviewers, with the v2 patch addressing all previous concerns. This appears ready for inclusion in the next merge window.