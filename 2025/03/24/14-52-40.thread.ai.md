Thread identity: Bug report regarding `mergetool.vimdiff.layout` behavior, specifically the handling of `@REMOTE` buffer changes during merge conflict resolution.

Core problem: Documentation states that changes made to buffers marked with `@` (including `@REMOTE`) should be reflected in the final merged file, with `@MERGED` as fallback. Actual behavior shows `@LOCAL` and `@MERGED` work as documented, but `@BASE` and `@REMOTE` changes are not propagated to output. D. Ben Knoble's analysis confirms this is an implementation bug in the vimdiff mergetool script.

Current status: Bug confirmed with technical analysis showing the code only checks for `@LOCAL` and `@BASE` explicitly before defaulting to `MERGED`. Original author and current maintainer CC'd for further input. Affects Git 2.48.1 through 2.49.

Key technical details:
- Vimdiff backend of `git mergetool`
- Buffer handling discrepancy between `@LOCAL`/`@MERGED` (working) vs `@BASE`/`@REMOTE` (not working)
- Specific issue location: `mergetools/vimdiff` lines 298-310
- Relevant variables: `LAYOUT`, `FINAL_TARGET`
- Original implementation commit: 0041797449 (2022)
- Clear reproduction steps and video demonstration available

Open questions:
- Will the fix involve updating implementation to handle all buffer types or revising documentation?
- Need maintainer confirmation on intended behavior
- Potential impact on existing user workflows if behavior changes

Participant positions:
- kawarimidoll (reporter): Provided detailed bug report with reproduction case
- D. Ben Knoble: Confirmed bug, identified exact code location, involved relevant maintainers
- Original author and current maintainer CC'd but not yet responded

Related work: None mentioned yet, but may connect to broader `git mergetool` behavior discussions. Implementation dates back to 2022 (0041797449).