Thread identity: Bug report regarding `mergetool.vimdiff.layout` behavior, specifically the handling of `@REMOTE` buffer changes during merge conflict resolution.

Core problem: Documentation states that changes made to buffers marked with `@` (including `@REMOTE`) should be reflected in the final merged file, with `@MERGED` as fallback. Actual behavior shows `@LOCAL` and `@MERGED` work as documented, but `@BASE` and `@REMOTE` changes are not propagated to output.

Current status: Newly reported issue with reproduction case provided. No maintainer response or patch yet. Affects Git 2.48.1 on Darwin/ARM64, likely impacts other platforms.

Key technical details:
- Vimdiff backend of `git mergetool`
- Buffer handling discrepancy between `@LOCAL`/`@MERGED` (working) vs `@BASE`/`@REMOTE` (not working)
- Clear reproduction steps provided
- Video demonstration available in GitHub Gist

Open questions:
- Is this a documentation bug or implementation bug?
- Should all `@`-prefixed buffers propagate changes or is current behavior intentional?
- Need maintainer/reviewer input on expected behavior

Participant positions:
- kawarimidoll (reporter): Provided detailed bug report with reproduction case
- No other participants yet

Related work: None mentioned yet, but may connect to broader `git mergetool` behavior discussions.