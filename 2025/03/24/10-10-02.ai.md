This is a review response in the ongoing pretty-format parsing refactoring series. Patrick Steinhardt agrees with Martin Ågren's approach in patch 4/8 to improve padding placeholder parsing while maintaining the current "print verbatim on failure" behavior.

The key points are:
- Patrick had previously suggested making malformed placeholders (like "%<(10,bad)") trigger a hard error rather than printing them verbatim
- Martin acknowledges this would be better behavior but considers it too risky to change now
- Patrick concurs that this behavioral change can be deferred, allowing the current patch to proceed with its safer approach of only storing successfully parsed data
- The discussion notes this could be revisited later with a "strict mode" option if desired

This email represents consensus on keeping the current patch's scope limited to internal parsing improvements without changing user-facing behavior. The thread context shows this is part of a well-reviewed series that's systematically modularizing pretty.c's parsing logic while maintaining backward compatibility.