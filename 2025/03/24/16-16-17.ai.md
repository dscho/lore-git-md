This is a review comment on patch 11/20 in the ongoing series to make Git's test suite Perl-independent. Phillip Wood notes that the patch's conversion from Perl to sed could be simplified by avoiding unnecessary file redirections.

The patch converts various test scripts that used Perl's substitution operator (`s///`) to use sed instead, removing PERL_TEST_HELPERS prerequisites. Phillip points out that the sed commands could be written more directly as `sed -e ... "$1" | tr ...` rather than using `<"$1"` redirection followed by piping to tr. This observation applies to multiple instances in the patch where sed is used with file input redirection.

The thread context shows this is part of a larger effort to modernize Git's test infrastructure by removing Perl dependencies while maintaining test coverage. Previous patches in the series have addressed similar conversions in other test files, with careful attention to preserving test behavior. The review process has been thorough, with Eric Sunshine providing detailed feedback on output formats and implementation choices.

This particular comment doesn't raise any functional concerns with the patch's approach - it's purely a style suggestion to make the sed usage more idiomatic. The patch itself appears well-motivated as part of the series' goal to eliminate Perl dependencies from the test suite.