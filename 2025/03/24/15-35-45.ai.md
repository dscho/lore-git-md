This is a follow-up discussion in the ongoing bugfix series addressing unexpected default branch advice messages during clone operations. Justin Tobler responds to Phillip Wood's review of v2 patch 2/3, which introduced the `REMOTE_GUESS_HEAD_QUIET` flag to suppress advice messages in `guess_remote_head()`.

The key points are:
1. Phillip identified an additional caller (`wanted_peer_refs()`) that needs advice suppression, which occurs when using `--single-branch` during clone operations
2. Justin confirms this creates another path where the unwanted advice can appear, including a case where bundle clones with `--single-branch` show the advice message twice
3. Justin plans to send an updated version that extends the suppression to `wanted_peer_refs()`

The discussion continues to refine the scope of the fix, now expanding beyond just the bundle clone case to cover all `--single-branch` scenarios. This maintains the thread's focus on comprehensively eliminating unwanted default branch advice during clone operations while respecting the architectural improvements being made to advice handling. The exchange shows the patch series evolving through careful review, with Justin addressing edge cases as they're identified.