This thread documents a small but precise correction to the vimdiff mergetool documentation. The original patch (v2) fixed an inconsistency where the docs incorrectly stated that buffer markers should use `*` when `MERGED` is absent from the layout, when in fact the implementation has always used `@` (arobase) since the feature was introduced. 

The author, Fernando Ramos, followed up to confirm the historical context - while an early development version did use asterisks as originally documented, this was changed to at-signs during implementation without updating the docs. This additional confirmation from the original implementer reinforces that the documentation change correctly aligns with the long-standing behavior. 

With no outstanding objections and the author having addressed prior feedback about terminology and sign-off, this appears settled as a straightforward documentation correction that better matches reality. The change is uncontroversial as it doesn't modify any behavior - it simply updates the docs to reflect what the code has always done.