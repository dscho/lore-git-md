Bugfix for `git blame` porcelain output regarding ignored/unblamable lines. This email thread has progressed from an initial rejected proposal to modify SHA-1 line prefixes to Patrick Steinhardt's accepted approach using new metadata lines. Karthik Nayak now confirms he will implement Patrick's solution, which adds "unblamable" and "ignored" metadata lines while preserving backward compatibility with existing porcelain parsers.

The fix addresses an inconsistency where porcelain mode wasn't showing the documented '?' (ignored) and '*' (unblamable) markers when using `--ignore-rev`/`--ignore-revs-file`. The solution modifies `emit_one_suspect_detail()` in `builtin/blame.c` to output these new metadata lines, following the porcelain format's extensibility rules.

The thread has reached consensus on the technical approach after initial disagreement about interface changes. Junio Hamano established the constraint that the SHA-1 line format must remain unchanged for backward compatibility. Patrick's solution satisfies this while properly exposing the ignored/unblamable status through new metadata lines.

Karthik indicates he will send an updated patch incorporating Patrick's implementation along with modified tests. This appears to be the final resolution to the issue, with the only remaining work being the actual patch submission and potential test coverage additions. The solution maintains consistency with existing porcelain deduplication rules while properly exposing the ignored/unblamable information that was previously missing.