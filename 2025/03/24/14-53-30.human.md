This is a feature/bugfix series improving SMTP error handling in `git-send-email`, part of Zheng Yuting's GSoC project. The v8 iteration continues refining RFC 5321 compliance for SMTP authentication errors, addressing feedback from Junio Hamano and others.

The series remains focused on two key improvements: (1) replacing boolean returns with proper exception handling via eval blocks, and (2) implementing RFC-compliant status code parsing that distinguishes temporary (4xx) from permanent (5xx) failures. The latest version maintains the core approach from v7 while working through final polish issues.

Notable in this submission is the explicit handling of edge cases - undefined results and missing status codes now default to transient failures (returning 1) to maintain robustness. The implementation stays within the `smtp_auth_maybe` subroutine while significantly expanding its capabilities.

The series still faces test suite failures (169/215 in t9001-send-email.sh), which the author will need to classify as either expected behavior changes (due to stricter RFC compliance) or actual regressions. Junio's latest review identified two remaining issues: a suggested code structure improvement (extracting error handling into a helper function) and a warning message inconsistency that needs resolution.

This appears to be a well-motivated change addressing real limitations in SMTP error handling, with the implementation now quite mature after eight iterations. The remaining work seems focused on test compatibility and final code polish rather than fundamental design questions.