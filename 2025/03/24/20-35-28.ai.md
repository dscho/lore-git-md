Bugfix investigation for `mergetool.vimdiff` buffer handling discrepancy. This is a response to a newly reported issue about `@REMOTE` buffer changes not being propagated during merge conflict resolution, contrary to documentation.

D. Ben Knoble analyzes the reported behavior and identifies the likely source of the issue in the vimdiff mergetool script. The code only checks for `@LOCAL` and `@BASE` explicitly before defaulting to `MERGED`, which explains why `@REMOTE` changes aren't being saved. This appears to be an implementation bug rather than a documentation issue, as the current code doesn't match the documented behavior for all `@`-prefixed buffers.

The analysis points to specific lines (298-310) in `mergetools/vimdiff` that determine the final target buffer, showing the logic only handles `@LOCAL` and `@BASE` cases before falling back to `MERGED`. The code dates back to commit 0041797449 from 2022, and Knoble has CC'd both the original author and the recent maintainer of this code for further input.

Key technical details for future reference:
- Affected file: `mergetools/vimdiff`
- Relevant variables: `LAYOUT`, `FINAL_TARGET`
- Current behavior only propagates changes from `@LOCAL` or falls back to `MERGED`
- Documentation suggests all `@`-prefixed buffers (`@REMOTE`, `@BASE`) should work
- Original implementation commit: 0041797449
- Current Git version checked: 683c54c999 (2.49)

This appears to be a genuine bug where the implementation doesn't fully match the documented functionality. The response is technically sound, identifying the exact code location and historical context while appropriately involving relevant maintainers. The next steps would likely involve either fixing the implementation to handle all buffer types or clarifying the documentation if the current behavior is intentional.