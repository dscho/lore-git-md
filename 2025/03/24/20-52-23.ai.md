Documentation patch fixing an incorrect description of vimdiff buffer marking. This corrects the documentation to state that buffers should be marked with an arobase (`@`) rather than an asterisk (`*`), aligning the docs with the actual implementation that has always used `@` since the feature was introduced in commit 7b5cf8be18.

The patch is straightforward - a single-line change to the vimdiff mergetool documentation. The author notes this is version 2 of the patch, now properly signed-off, and references the previous discussion where the terminology "arobase" (the technical name for the `@` symbol) and scope were discussed. The change is uncontroversial and clearly corrects a documentation/implementation mismatch.

Key technical details for future reference: affects `Documentation/mergetools/vimdiff.adoc`, specifically the section about marking buffers when `MERGED` is not present in the layout. The actual code has always looked for `@` despite docs saying `*`. No behavior change - just docs alignment.