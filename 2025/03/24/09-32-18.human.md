This is a follow-up discussion in the thread about suppressing unwanted default branch name advice during clone operations. Phillip Wood points out that while Justin Tobler's patch addresses the main case in `cmd_clone()`, there's another caller of `guess_remote_head()` in `wanted_peer_refs()` that should also suppress the advice message for consistency.

The key observation is that showing the default branch name advice during remote repository cloning isn't helpful to users, as they're not creating a new local repository at that point. Phillip's comment suggests expanding the scope of the fix to cover this additional case, though he presents this as an observation rather than a blocking concern.

This continues the thread's theme of ensuring consistent advice suppression across all clone-related operations, building on Justin's work to fix the regression introduced by earlier refactoring. The discussion remains technical and constructive, with Phillip identifying a related case that wasn't covered in the current patch series.