Security-focused refinement discussion for remote object info feature.

This email continues the security-focused discussion around the remote object info implementation, specifically addressing format string handling and behavior when mixing local and remote object queries. Jeff King (Peff) responds to Peijian Ju's proposal about handling unsupported format placeholders, suggesting alignment with `git for-each-ref`'s behavior of returning empty strings rather than "???" markers. This would maintain consistency with Git's existing patterns and allow for future unification with ref-filter formatting.

The discussion also clarifies the intended behavior when mixing `info` and `remote-object-info` commands in batch mode - local queries will use the full format string while remote ones will only populate supported fields. Peff confirms the proposed strict remote-only behavior for `remote-object-info` commands, where it should always query the remote regardless of local object presence, failing only for purely local objects.

This represents final polishing of edge cases in what appears to be a thoroughly reviewed security-conscious feature. The exchange shows careful attention to both user experience and implementation consistency, with Peff providing experienced guidance to ensure the new functionality aligns with Git's established patterns. The discussion suggests the feature is nearing completion after multiple rounds of security hardening.