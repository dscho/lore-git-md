This is a bugfix proposal for `git blame` porcelain output regarding ignored/unblamable lines, following up on Junio Hamano's review of the initial approach. Patrick Steinhardt suggests an alternative implementation that addresses the backward compatibility concerns by extending the porcelain format's metadata lines rather than modifying the SHA-1 hash lines.

The patch modifies `emit_one_suspect_detail()` in `builtin/blame.c` to add new "unblamable" and "ignored" metadata lines when appropriate, similar to how boundary commits are already handled. This maintains the invariant that record starts with a 40-character SHA-1 while still exposing the markers through the existing extensible metadata mechanism. The changes propagate through several porcelain output functions to pass the blame_entry context needed for the new markers.

Key technical aspects:
- Adds new metadata lines "unblamable" and "ignored" when corresponding flags are set
- Modifies function signatures to pass blame_entry through the porcelain output chain
- Preserves existing porcelain parsing expectations by not altering SHA-1 lines
- Follows the same pattern used for boundary commit markers

This appears to directly address Junio's requirements for maintaining backward compatibility while solving the original visibility issue. The implementation leverages the porcelain format's existing extensibility points rather than breaking its invariants. The changes are focused in the blame porcelain output machinery without touching other subsystems.

The proposal seems well-aligned with the thread's technical constraints and represents a plausible solution path forward. It remains to be seen whether reviewers will request any adjustments to the exact metadata line format or suggest alternative approaches, but the core idea of using additional metadata lines rather than modifying hash lines appears sound.