Bugfix for `git blame` porcelain output regarding ignored/unblamable lines, now implementing the metadata line approach. This email from Karthik Nayak confirms acceptance of Patrick Steinhardt's proposed solution for showing ignored/unblamable status in porcelain mode via new metadata lines rather than modifying the SHA-1 line format.

The key technical aspects are:
- Modifies `emit_one_suspect_detail()` in `builtin/blame.c` to output "unblamable" and "ignored" metadata lines
- Passes the `blame_entry` through the call chain to access per-line status
- Maintains backward compatibility by preserving the strict SHA-1 line format
- Follows porcelain extensibility rules by adding new metadata rather than altering existing formats

This represents the implementation phase after the thread reached consensus on the metadata line solution. Karthik indicates he will incorporate Patrick's patch and update the tests, suggesting this is moving toward finalization. The changes affect porcelain output generation while respecting the interface constraints established earlier in the thread by Junio Hamano.

The patch appears well-considered, building on the existing porcelain format extensibility mechanism and addressing the original bug where porcelain mode wasn't showing the documented markers for ignored/unblamable lines. The technical approach aligns with the thread's resolution to use metadata lines rather than modifying core format elements.