This is a documentation and test maintenance patch in the larger "path-walk" delta compression series. It aligns the `git pack-objects` command's built-in usage message with its documentation synopsis, then removes the command from a test exception list that checks for such mismatches.

The changes are straightforward: the usage message in `builtin/pack-objects.c` is updated to match the exact formatting and option ordering of the documentation, including the new `--path-walk` option introduced in this series. The test script `t0450` is then updated to stop exempting `pack-objects` from its documentation/binary consistency checks.

This is a minor but important housekeeping change that ensures consistency between the command's help text and documentation. The patch is cleanly implemented and fits naturally into the larger series by properly documenting the new `--path-walk` option in both places users would expect to find it. Since it's part of a larger performance-focused series, this patch helps ensure all documentation remains accurate as new features are added.