Test patch for shallow push delta behavior in the path-walk series.

This patch adds two test cases to verify delta compression behavior during shallow pushes, which is relevant to the ongoing path-walk delta compression work. The first test confirms basic object counting works correctly when pushing from a shallow clone. The second, more interesting test creates a scenario where detecting proper delta base usage makes a measurable difference (kilobyte vs bytes in the progress output).

The tests are well-constructed to catch regressions in delta handling, though they don't specifically exercise the new path-walk functionality. They serve as general infrastructure tests that would catch problems with the path-walk approach's interaction with shallow clones. The commit message clearly explains the test methodology and what constitutes success.

This appears to be a solid addition to the test suite that will help maintain delta compression quality as the path-walk feature evolves. The tests are focused and their pass/fail conditions are unambiguous.