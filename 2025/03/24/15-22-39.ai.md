This is a documentation and test maintenance patch in the path-walk delta compression series.

The patch updates the usage documentation for `git pack-objects` to match between the man page and the actual command implementation. The main change is aligning the command's help text in builtin/pack-objects.c to exactly match the SYNOPSIS section in Documentation/git-pack-objects.adoc, including all the same options and formatting. This includes adding the new `--path-walk` option that was introduced earlier in the series.

As part of this alignment, the patch removes 'pack-objects' from the exception list in t0450/adoc-help-mismatches, indicating that the command's help text now properly matches its documentation. The test t0450 verifies that builtin commands' usage matches their documentation, and this change makes pack-objects compliant with that requirement.

The changes are straightforward - the documentation formatting is adjusted to use consistent indentation, and the command's usage message is expanded to include all options in the same order and format as the documentation. This is a minor but important consistency improvement that helps ensure users see the same information whether they read the man page or use the command's help output.

Key technical details:
- Updates both the .adoc file and builtin/pack-objects.c usage strings
- Includes the new `--path-walk` option in both places
- Removes pack-objects from t/t0450 exception list
- Maintains exact option ordering between docs and implementation
- Uses same line-wrapping and indentation in both places

This patch doesn't affect functionality but improves consistency as part of the larger path-walk feature series. It follows good practices for keeping documentation and code in sync, especially when adding new features.