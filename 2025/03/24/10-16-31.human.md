Bugfix follow-up for `git blame` porcelain output regarding ignored/unblamable lines. Patrick Steinhardt responds to Junio Hamano's review with a revised approach that maintains backward compatibility while exposing the missing markers.

The proposed solution extends the existing porcelain metadata mechanism rather than modifying the SHA-1 hash lines, addressing Junio's key concern about parser compatibility. The patch modifies `emit_one_suspect_detail()` to output new "unblamable" and "ignored" metadata lines when appropriate, similar to how boundary commits are already handled. This keeps the invariant 40-character SHA-1 format intact while still surfacing the additional information.

The implementation appears technically sound, passing the blame entry through the call chain to access the unblamable/ignored flags while maintaining the existing porcelain output structure. This aligns with the maintainer's guidance to use extensible metadata lines rather than modifying core format elements. The change is focused and doesn't introduce any obvious new complexity or performance overhead.

This appears to be a thoughtful response to the review that properly balances the need to expose the markers with the requirement to maintain stable porcelain parsing. The approach seems likely to satisfy both the original bug report (missing markers in porcelain mode) and Junio's interface stability concerns.