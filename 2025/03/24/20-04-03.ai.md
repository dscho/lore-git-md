Bugfix discussion for `git blame` porcelain output regarding metadata emission frequency. This email continues the thread about properly showing ignored/unblamable lines in porcelain mode, specifically addressing Toon Claes' question about whether the new metadata lines need to be emitted per-line even when multiple lines blame to the same commit.

Karthik Nayak questions Toon's assumption that the "unblamable" and "ignored" status could differ per line when blaming to the same commit, suggesting these attributes are tied to commits rather than individual lines. This probes an important implementation detail for the metadata line solution that was previously accepted - whether the new metadata needs to be repeated for every line (potentially redundant) or can be optimized when multiple lines share the same commit and status.

The discussion remains focused on the technical implementation within `emit_one_suspect_detail()` and the distinction between `--porcelain` (which deduplicates commit info) and `--line-porcelain` modes. This email doesn't propose new code but seeks clarification on an edge case that could affect the final implementation's efficiency and correctness. The thread appears to be in its final refinement stage before the solution is finalized.