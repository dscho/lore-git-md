Post-merge discussion on batched reference updates implementation.

This email exchange between Karthik Nayak and Patrick Steinhardt addresses two final points in the recently merged batched reference updates feature:

1. **NUL-terminated input/output clarification**: Patrick questions whether the new `--batch-updates` flag supports NUL-terminated mode for both input and output. Karthik confirms it only supports NUL-terminated input (via `-z` flag) and references prior discussion with Phillip Wood that established this behavior should not apply to output formatting. The output format remains LF-terminated as documented in the man page.

2. **Code style nit**: Patrick points out a missing set of curly braces in an `else if` branch in `builtin/update-ref.c`. Karthik acknowledges this and agrees to make the change. The affected code enforces that `--batch-updates` can only be used with `--stdin`.

The exchange shows the typical post-merge refinement process, with minor documentation and style points being addressed after the core functionality has been accepted. Both points relate to the user interface (`-z` behavior) and code quality (braces) rather than the core transaction logic, indicating the main implementation is considered complete.

Key technical details maintained:
- The distinction between input (`-z`) and output formatting
- The `REF_TRANSACTION_ALLOW_FAILURE` flag usage
- The `update_refs_stdin()` function interface
- Error handling for invalid flag combinations