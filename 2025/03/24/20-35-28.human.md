Bugfix investigation for `git mergetool` vimdiff behavior. D. Ben Knoble responds to kawarimidoll's report about `@REMOTE` buffer changes not being saved as documented, confirming the discrepancy between documentation and implementation.

The response identifies the specific code in `mergetools/vimdiff` (lines 298-310) that handles buffer selection logic, showing it only checks for `@LOCAL` and `@BASE` before defaulting to `MERGED`, completely ignoring `@REMOTE`. This explains why changes to `@REMOTE` buffers aren't being saved, despite documentation suggesting they should be.

Knoble notes the behavior appears to date back to the original vimdiff layout implementation in 2022 and has CC'd both the original author and the maintainer who worked on most of the mergetool code, suggesting this might lead to either a documentation clarification or a code fix depending on the intended behavior.

The email provides both inline code snippets and a GitHub link for reference, making it easy for others to examine the issue. While Knoble hasn't reproduced the bug yet, the analysis appears thorough and points clearly to either a documentation inaccuracy or a bug in the buffer selection logic. This moves the thread forward from initial bug report to technical investigation stage.