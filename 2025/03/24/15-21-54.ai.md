Bugfix follow-up discussing advice suppression approach in `guess_remote_head()`. This email continues the thread about suppressing the default branch name advice during clone operations, specifically addressing Phillip Wood's suggestion about simplifying the flags-based approach.

Justin Tobler responds to Phillip's observation that all current `guess_remote_head()` callers that don't set `all` also want to suppress advice, suggesting the flags conversion might be unnecessary. While Justin expresses a mild preference for the explicit configuration approach taken in the patch series, he acknowledges the alternative could work and remains open to changing it in a future version.

The discussion centers on whether to:
1. Keep the current patch's approach of converting the boolean `all` parameter to explicit flags (`REMOTE_GUESS_HEAD_ALL` and `REMOTE_GUESS_HEAD_QUIET`)
2. Simplify by hardcoding quiet behavior when `all` is false, since all such callers want advice suppressed

Key technical points:
- The change affects `guess_remote_head()` in remote.c
- Related to the broader effort to properly suppress default branch advice during clone operations
- Builds on previous architectural improvements to advice handling
- Maintains compatibility with existing clone/fetch/remote callers

This represents a minor refinement discussion in the larger thread, with Justin showing willingness to adjust the implementation based on reviewer feedback while maintaining the core functionality. The email doesn't introduce new test cases or code changes, but rather discusses design philosophy around parameterization versus implicit behavior.