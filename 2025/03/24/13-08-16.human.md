This is a post-merge review comment on the recently merged batched reference updates feature. Patrick Steinhardt points out two small documentation and code style issues in the final patch of the series that added `--batch-updates` support to `git update-ref`.

First, he questions whether the new `--batch-updates` mode properly supports NUL-terminated input (via the `-z` flag), noting this should be documented if implemented. This is a reasonable concern since the feature builds on `--stdin` mode which does support NUL-terminated input.

Second, he notes a minor formatting nit where an `else if` branch in the code should use curly braces for consistency. Both observations are constructive post-merge feedback from someone clearly familiar with the codebase's conventions.

The patch being discussed was already merged as part of a larger series implementing batched reference updates with partial failure support, so these are minor polish items rather than blocking issues. The feedback maintains the thread's technical focus on refining the implementation details of this new reference transaction capability.