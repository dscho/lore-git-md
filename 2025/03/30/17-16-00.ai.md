Documentation modernization converting `git-reset` man page to use AsciiDoc's modern features. This is the first of five patches in a series standardizing documentation format across several Git commands.

The patch mechanically updates `git-reset.adoc` to use proper synopsis blocks that automatically format placeholders in italics and keywords in monospace. It replaces angle-bracket placeholders (`<placeholder>`) with underscored versions (`_<placeholder>_`) in descriptions, and wraps command options and keywords in backticks for consistent rendering. The changes are purely presentational - all content remains identical, only the markup syntax changes to leverage AsciiDoc's built-in formatting capabilities.

Key technical changes include converting the SYNOPSIS section from verse blocks to proper synopsis blocks, updating all command option references to use backticks, and standardizing placeholder notation throughout. The patch touches only documentation files with no behavioral impact on Git itself. This follows the pattern seen in previous documentation standardization efforts, applying consistent mechanical transformations to improve rendering while preserving all informational content.

The commit message clearly explains the scope of changes (synopsis blocks, placeholder formatting, keyword backticking) without attempting to justify the changes, as this appears to be part of an established documentation improvement workflow. The diff shows thorough, systematic application of the new formatting rules across the entire file.