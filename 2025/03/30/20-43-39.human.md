Bugfix for `git blame` porcelain output, addressing a long-standing inconsistency in how ignored/unblamable lines are marked. This is the final version of a patch that has undergone extensive review and iteration.

Karthik Nayak fixes an oversight where the porcelain output modes (`--porcelain` and `--line-porcelain`) didn't indicate which lines were ignored or unblamable, despite the documentation not mentioning this limitation. The patch adds new "ignored" and "unblamable" metadata lines in porcelain output when the corresponding config options are enabled.

The implementation carefully maintains backward compatibility while adding the new metadata lines. The patch includes thorough test coverage that verifies the behavior for both porcelain modes, using `test_cmp` for robust output validation as suggested in earlier reviews. This version also fixes a final formatting issue where `puts()` was adding extra newlines.

This concludes a well-executed thread that addressed all concerns raised during review, with contributions from multiple developers. The patch is now in its final polished form, ready for merging.