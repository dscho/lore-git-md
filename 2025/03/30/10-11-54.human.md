Test adjustment for Windows compatibility in revision walker bugfix. Phillip Wood suggests improvements to Matt Hunter's test case for the `--left-only`/`--right-only` fix with unrelated histories. The original test used Bash process substitution (`<(sort file)`) which doesn't work on Windows, and lacked the helpful debugging output from Git's `test_line_count` helper. Phillip provides a cross-platform alternative that sorts to temporary files first, then uses `comm` and `test_line_count` for clearer test failure diagnostics. This is a minor refinement to the test infrastructure that doesn't affect the core bugfix (which was already verified as correct in the previous discussion). The suggestion follows Git's test suite conventions and maintains the same verification logic while being more portable and debuggable.