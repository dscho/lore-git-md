Documentation modernization completing the series with `git-mv.adoc` conversion. This final patch applies the same formatting improvements to the `git-mv` man page that were previously done for `git-reset` and `git-rm`:

1. Converts the synopsis to a proper synopsis block that will automatically format placeholders in italics and keywords in monospace
2. Standardizes placeholder notation to `_<placeholder>_` throughout
3. Wraps all options and keywords in backticks for consistent rendering
4. Updates submodule documentation to use the new syntax for config keys (`submodule.<name>.path`)

The patch notes one remaining inconsistency in the synopsis regarding ellipsis usage (both for repeated options and commit ranges), which the rendering engine cannot distinguish. This matches the mechanical nature of the series, focusing purely on presentation while preserving all technical content. The changes align with the established pattern from previous patches, completing the trio of command documentation updates.