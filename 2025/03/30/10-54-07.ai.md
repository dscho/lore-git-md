This is a test refinement discussion in the context of a bugfix for `--left-only`/`--right-only` behavior with unrelated histories. The thread has already established the core fix (setting `revs->limited = 1` in revision.c) and is now focused on improving test portability and robustness.

Matt Hunter responds to Phillip Wood's earlier suggestions about test improvements, acknowledging the recommendation to use `test_line_count` instead of process substitution and `wc -l`. The email reveals an interesting testing challenge - Matt initially wanted to verify the exact count of commit IDs returned by `rev-list --X-only` but found it non-trivial to determine the expected count due to interactions with test setup commits. This led to the current approach of checking for zero common commits between the histories, which may actually be a stronger assertion of the desired behavior.

The discussion highlights two key testing considerations: (1) the tradeoff between precise output validation versus broader behavioral assertions, and (2) the learning curve for new contributors working with Git's test infrastructure. The core fix remains unchanged from previous iterations, with this exchange focusing solely on test methodology improvements.