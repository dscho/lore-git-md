Security configuration question about git-daemon ownership checks.

A user running a read-only git-daemon server reports problems after recent security changes that verify repository ownership. They're encountering "dubious ownership" errors when clients try to clone repositories, even after creating a .gitconfig for git-daemon to mark the repository directory as safe. The error has shifted from complaining about an absolute path ('/srv/git/mgsautils.git') to a relative path ('.'), making it unclear what exactly needs to be marked as safe.

The user expresses frustration with the increasing complexity of maintaining the previous behavior, noting they've already had to create .gitconfig files for git-shell users as well. While they acknowledge the security rationale, they're seeking guidance on how to properly configure the system to allow public read-only access without compromising safety.

This appears to be a real-world deployment issue stemming from Git's tightened security defaults. The question is well-framed and shows the user has made reasonable attempts to solve the problem, though they may need more specific guidance about path handling in git-daemon's context. The thread could benefit from input from maintainers familiar with both the security implications and git-daemon's operational requirements.