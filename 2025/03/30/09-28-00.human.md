This is a final polish discussion for the `git blame` porcelain output fix. Phillip Wood points out a subtle issue with the implementation's use of `puts()` in the newly added `emit_porcelain_per_line_details()` function. The function currently adds redundant newlines since `puts()` automatically appends one, while the intended output format should have exactly one newline per metadata line.

Phillip suggests improving the regression test to use `test_cmp` rather than counting lines with `grep`, which would better catch formatting issues like this. This comes after Junio Hamano had already identified the redundant newlines as the last remaining optimization needed before merging.

The thread has otherwise reached consensus on all aspects of the solution, including the core implementation, documentation updates, and test coverage. This final observation about output formatting represents the last bit of polish needed for this long-running effort to properly mark ignored/unblamable lines in `--line-porcelain` mode while maintaining backward compatibility.