This is a final technical clarification in the now-completed `git blame` porcelain output thread. Junio Hamano points out a potential redundancy in Karthik Nayak's v3 patch - the use of `puts()` with strings that already contain newlines. The `puts()` function automatically appends a newline, so having explicit `\n` in the strings would result in double newlines.

The observation comes after all major design decisions have been settled in this long-running thread about displaying ignored/unblamable commit markers in `--line-porcelain` mode. The core solution (using metadata lines while preserving SHA-1 format compatibility), documentation updates, and test improvements are all finalized. This appears to be a last-minute optimization note about output formatting efficiency rather than a substantive change to the approved approach.

The thread has already seen multiple rounds of review addressing porcelain format design, test coverage, and output readability, with this being the only remaining minor point about implementation details. Given the thorough review history and Junio's earlier approval of the overall design, this seems like a straightforward fix that shouldn't require another revision cycle.