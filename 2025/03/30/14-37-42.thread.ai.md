## AI Thread Summary

Feature inquiry from Alexander Shpilkin regarding stash functionality limitations for build system integration. The core problem is the inability to create a stash object that includes untracked files without modifying the working tree, which is needed for a build system that snapshots state before each `make` invocation. Current approaches fall short: `git stash create` excludes untracked files, while `git stash push --include-untracked` modifies the working tree. The author has examined `builtin/stash.c` and identified `do_create_stash()` as the key function, noting it's only called with `include_untracked=0` or in contexts that alter the working tree. The technical workaround using `git update-ref` with `git stash create` doesn't solve the untracked files issue. The inquiry seeks solutions either through existing commands or potential modifications to support this use case, with attention to preserving mtimes for build system correctness. No responses or alternative approaches have been proposed yet in the thread.