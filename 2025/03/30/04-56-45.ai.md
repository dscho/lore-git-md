Final polish on `git blame` porcelain output implementation.

This is the concluding exchange in a long thread about adding metadata markers for ignored/unblamable lines in `git blame` porcelain output. The thread has already established the technical approach (using new metadata lines rather than modifying SHA-1 prefixes), finalized the implementation in `builtin/blame.c`, and addressed all test and documentation requirements.

Junio Hamano points out a potential redundancy in Karthik Nayak's final patch - the use of `puts()` which automatically adds a newline, when the strings being printed (`"unblamable\n"` and `"ignored\n"`) already contain newlines. This would result in double newlines in the output. The observation suggests either switching to `fputs()` without embedded newlines, or removing the newlines from the string literals.

The exchange represents the final quality check before merging, catching a small optimization opportunity in the output generation code. All substantive design and implementation decisions were settled in earlier messages, with this being the last polish on an otherwise complete solution that:
- Maintains backward compatibility
- Uses new metadata lines rather than modifying existing formats
- Properly handles per-line status reporting
- Has comprehensive test coverage
- Includes full documentation updates

The thread has reached full consensus with all feedback addressed, and this final note about output formatting appears to be the last remaining detail before the series can be merged.