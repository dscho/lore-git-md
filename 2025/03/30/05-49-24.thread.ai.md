Here's the updated AI thread summary:

Bugfix for `--left-only`/`--right-only` behavior with unrelated histories, now including Windows test compatibility. The revision walker incorrectly handled these options when comparing commit histories without a common merge base, due to missing UNINTERESTING boundary commits. The fix (2 lines in revision.c) mirrors a prior `--cherry-pick` solution (023756f4eb) by setting `revs->limited = 1` explicitly. The initial test case verification used Bash process substitution that wasn't Windows-compatible, as noted by Johannes Sixt. The test logic remains valid but needs adjustment to use temporary files instead of process substitution for cross-platform compatibility. The core fix for the revision walker behavior is unaffected - this remains a minimal, well-contained correctness fix following established patterns, now with properly portable test verification. The commit message properly references prior related work for context, and the test adjustment maintains the original verification approach while working across all platforms.