Bugfix for `--left-only`/`--right-only` behavior in revision walking, continuing the thread that has evolved through test improvements. This patch addresses the core issue where these options failed to work correctly when comparing commit histories without a common merge base, mirroring a prior solution for `--cherry-pick` (023756f4eb). The fix sets `revs->limited = 1` in revision.c when either option is specified, ensuring proper limiting behavior even without boundary commits.

The test changes show refinement from the initial version, now using sorted temporary files and `test_line_count` rather than process substitution and direct `wc` counting, following feedback about portability and test methodology. The test creates two unrelated histories via rebase and verifies that `--left-only` and `--right-only` produce disjoint commit sets when comparing them.

Key technical points include:
- The revision.c` changes mirror existing patterns for similar options
- Test now uses `sort` and `comm` with temporary files rather than process substitution
- Assertion changed from line counting to verifying zero common commits between outputs
- Follows Phillip Wood's earlier suggestions about test methodology while adding new validation approach

This represents the third iteration of test improvements in the thread, now focusing on semantic validation of the disjointness property rather than just output line counts. The core fix remains stable while the verification evolves toward more robust and meaningful assertions.