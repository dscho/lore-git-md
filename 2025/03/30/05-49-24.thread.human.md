This thread addresses a bug in how `git rev-list` handles the `--left-only` and `--right-only` options when comparing commit histories without a common merge base. The core fix, which mirrors an established pattern from the `--cherry-pick` implementation, has stabilized with broad agreement. The discussion has since focused on refining the test suite to ensure both correctness and portability.

Johannes Sixt, the Git for Windows maintainer, recently pointed out that the current test implementation relies on Bash-specific process substitution (`<(sort head)`), which doesn't work on Windows and isn't guaranteed across all POSIX shells. This continues the thread's pattern of iterative test improvements, following earlier moves from simple output counting to more semantic validation of commit separation. The test evolution has been a collaborative effort, with Phillip Wood contributing methodology improvements and new contributor Matt Hunter helping refine the assertions to better capture the intended behavior.

The thread demonstrates Git's review process at work: starting with a clear algorithmic fix, then progressively improving the verification approach while maintaining cross-platform compatibility. With the core solution settled, the remaining work focuses on ensuring the tests are as robust as the fix itself. No outstanding technical objections remain, and the thread shows how the project balances correctness, portability, and clear verification.