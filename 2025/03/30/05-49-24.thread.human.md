Here's a concise human-readable summary of the thread's current state:

This thread addresses a bug in how `rev-list` handles `--left-only` and `--right-only` options when comparing commit histories without a common ancestor. The core fix modifies the revision walker to properly limit the traversal in these cases, mirroring an established solution used for `--cherry-pick`. 

The discussion has evolved through several phases of refinement. After establishing the technical solution, attention turned to making the tests both portable across platforms (with Johannes Sixt identifying and addressing Windows compatibility issues) and semantically meaningful (with Matt Hunter improving the validation approach). Phillip Wood has been guiding the test methodology, most recently explaining how the test harness manages repository state between test cases - clarifying why direct commit counting wasn't working as expected.

Key participants include the original author, Windows expert Johannes Sixt, test specialist Phillip Wood, and new contributor Matt Hunter. The thread demonstrates Git's characteristic blend of algorithmic precision and cross-platform robustness, with experienced contributors helping orient newcomers to project conventions. The patch appears technically sound, with only minor test implementation details remaining before it's ready for finalization.