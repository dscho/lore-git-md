Here's a concise human-readable summary of the thread's current state:

This thread addresses a bug in Git's revision walker where the `--left-only` and `--right-only` options failed to work correctly when comparing commit histories without a common ancestor. The core fix, which mirrors how `--cherry-pick` handles similar cases, involves setting a flag in revision.c to properly limit the walk.

The discussion has evolved through three phases: first identifying the core issue, then improving test portability (particularly for Windows), and now refining test assertions. Johannes Sixt and Phillip Wood contributed key improvements to make the tests work across platforms and follow Git's testing best practices. Most recently, Matt Hunter has pushed the tests further - instead of just counting output lines, the latest version verifies the more fundamental property that the outputs for left and right commits should have no overlap when histories are unrelated.

All participants have focused on making both the fix and its verification robust, with the algorithmic solution remaining stable while the test methodology becomes increasingly sophisticated. The thread demonstrates Git's characteristic attention to both correctness and cross-platform reliability, with contributors bringing complementary expertise in core functionality, Windows compatibility, and test design. The final patch will need to document not just the code change but also this evolution in testing approach.