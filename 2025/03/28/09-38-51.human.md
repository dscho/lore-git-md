This is a debugging follow-up in the ongoing investigation of platform-specific test failures for the `backfill --min-batch-size` feature on s390x and sparc64 systems.

John Paul Adrian Glaubitz is responding to Patrick Steinhardt's debugging suggestions, requesting clarification on how to apply the proposed test modifications. The core issue remains that the test fails when trying to verify promisor fetch counts in trace2 output on big-endian architectures. 

Patrick had suggested modifying the test infrastructure to use `test_grep` instead of plain `grep` and running with `-ix` flags for more verbose output, which would help reveal what's actually in the trace data when the expected pattern isn't found. Glaubitz is now asking for the exact command line to use these debugging flags, as the standard `make test` invocation doesn't expose them.

This exchange shows the investigation moving into more hands-on debugging, with platform experts now engaged to gather the diagnostic data needed to understand why the batch size verification fails specifically on these architectures. The next steps will likely involve seeing the actual trace2 contents from the failing systems.