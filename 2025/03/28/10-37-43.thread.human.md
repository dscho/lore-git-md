Here's a human-readable summary of the thread:

A user reported a serious issue where `git push --mirror` hangs indefinitely when pushing large repositories to Azure DevOps. The problem occurs after the "Storing index (done)" message appears, leaving the process unresponsive. The reporter provided extensive debugging information including stack traces showing the hang occurs in transport-helper.c during push_refs_with_push, with multiple processes stuck in read() calls.

Despite trying various HTTP configuration tweaks (http.postBuffer, GIT_CURL_TIMEOUT, http.maxRequests) and examining TLS traffic, the issue persists specifically with Azure DevOps. The thorough report includes process dumps and strace outputs, suggesting this is likely an Azure DevOps-specific protocol issue rather than a general Git bug, as the problem doesn't occur with other Git hosts and standard timeout/config adjustments don't resolve it. The reporter used Git 2.48.1 and tested with large repositories like elasticsearch/node to reproduce the issue consistently.