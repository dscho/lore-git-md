This thread addresses Cygwin-specific regressions in gitk and git-gui introduced in Git v2.49.0, where Windows security changes caused stack exhaustion due to recursive interactions between `_which` and wrapped `exec` functions. Mark Levedahl's patch series implements three key fixes: properly scoping exec/open overrides to Windows (preserving security while fixing Cygwin), removing obsolete Cygwin path conversion code, and optimizing PATH searches.

The latest patch tightens PATH search behavior to match POSIX/Tcl standards by only searching PATH for bare executable names, fixing a Windows-specific discrepancy where gitk was incorrectly searching PATH for relative paths. This aligns gitk with git-gui's behavior and completes the series' goals. The change modifies just one line in gitk's Tcl code while maintaining all functionality for Windows users.

Johannes Sixt (Hannes) approved treating Cygwin as non-Windows, and the series has incorporated all feedback with no outstanding objections. The cumulative changes improve correctness, performance, and code simplicity while preserving Windows security protections. With all identified issues now addressed, the series appears ready for merging.