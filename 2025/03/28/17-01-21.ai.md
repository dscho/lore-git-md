This is the second patch in a series enhancing `git version --build-options` to report SHA implementation details, specifically addressing the "unsafe SHA-1" variant naming question raised in the thread.

The patch adds reporting of non-cryptographic SHA-1 implementations (previously added in commit 06c92dafb8) under the "unsafe-SHA-1:" label, showing which backend is in use (OpenSSL, blk, or Apple CommonCrypto). The implementation mirrors the approach from patch 1/2, adding new conditionals in `get_sha_impl()` to detect these unsafe variants via their compile-time defines. Documentation is updated to note that these implementations are faster but not for cryptographic use.

The patch maintains the series' clean approach of adding new output lines while preserving existing behavior when `--build-options` isn't used. The key unresolved issue remains the "unsafe-SHA-1" terminology - while the author uses this label consistently in code and docs, the thread context shows this naming was already questioned as potentially alarming to users, with "fast-SHA-1" suggested as an alternative. This appears to be the primary discussion point awaiting resolution.

Technical details for future reference: The patch touches `help.c`'s SHA detection logic and `git-version.adoc`, adding new output lines with the "unsafe-SHA-1:" prefix when corresponding `SHA1_*_UNSAFE` defines are present. The implementation is straightforward but the naming choice may need revisiting based on list feedback.