This is a test infrastructure fix addressing a performance test script issue. Philippe Blain corrects the `p7821-grep-engines-fixed.sh` performance test to properly declare prerequisites using the `--prereq` flag, which became required after a 2022 change to the perf test framework. Without this fix, the test would fail with an error when run on systems missing the PCRE prerequisite.

The patch is straightforward, adjusting two `test_perf` invocations to move the prerequisite declarations to the proper `--prereq` parameter position. This matches the current test framework expectations and prevents confusing error messages when prerequisites aren't met. The change is minimal (2 lines modified in total) and clearly motivated by actual test failures.

As the first patch in a two-part series focused on performance test fixes, this appears to be a low-risk maintenance change that improves test reliability without affecting core Git functionality. The fix targets a specific, reproducible issue rather than making speculative improvements.