This is a cover letter for a two-patch series fixing performance test issues. The series addresses two distinct problems in Git's performance test suite:

1. The first patch fixes `p7821-grep-engines-fixed.sh` where the test_perf helper was incorrectly invoked when checking prerequisites. The change adjusts the invocation syntax to properly handle test prerequisites.

2. The second patch corrects `p9210-scalar.sh` which was failing when run from a detached HEAD state. The modification ensures the scalar clone test works correctly regardless of the repository's HEAD state.

Both changes are straightforward fixes to test infrastructure rather than core Git functionality. The patches modify only the test scripts themselves with minimal changes (4 lines added, 3 removed total). The issues appear to be genuine problems encountered during actual test runs rather than theoretical edge cases.

The series comes from a known contributor (Philippe Blain) via GitGitGadget and targets the main Git repository. The changes are narrowly scoped to test fixes and unlikely to be controversial, though reviewers may want to verify the test scenarios that exposed these issues.