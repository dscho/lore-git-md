This is a two-patch series fixing issues in Git's performance test suite. The patches address specific test failures the author encountered while running the performance tests.

The first patch fixes `p7821-grep-engines-fixed.sh` where the test_perf invocation for prereqs was incorrect. The second patch corrects `p9210-scalar.sh` which was failing when run from a detached HEAD state. Both changes are small (4 lines changed total) and appear to be straightforward fixes for actual problems encountered during test runs.

The patches modify test scripts in the performance test directory (`t/perf/`), suggesting these are maintenance fixes rather than changes to core functionality. The author, Philippe Blain, provides minimal but sufficient context about the motivation - these were failures noticed during test runs. The changes look focused and uncontroversial, addressing clear technical issues in the test infrastructure.