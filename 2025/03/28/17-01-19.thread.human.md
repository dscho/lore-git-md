This thread has concluded with a successful enhancement to `git version --build-options` that now reports which SHA implementations Git was built with. The two-patch series, now at version 3, adds compile-time detection of both standard and non-collision-detecting SHA-1 backends, displaying them in the version output with appropriate security warnings.

The implementation was led by Patrick Steinhardt with significant contributions from Justin Tobler on documentation wording and Christian Couder on terminology. The final output will show either "SHA-1: <backend>" for standard implementations or "non-collision-detecting-SHA-1: <backend>" for unsafe variants, with the latter line omitted entirely when no unsafe backend is selected. The documentation clearly explains the security implications of using non-collision-detecting SHA-1 variants.

All technical questions were resolved through the thread's discussion, with Junio Hamano approving the final approach. The patches are now ready to be merged, having achieved consensus on both the technical implementation and documentation wording. This completes the feature without any outstanding issues.