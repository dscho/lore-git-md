This is a documentation clarification for Karthik Nayak's batched reference updates feature, which has already been technically approved in the thread.

Jean-Noël Avila points out a formatting issue in the documentation update for `git update-ref`'s new `--batch-updates` flag. The current synopsis incorrectly implies the options are continuations of a single command line rather than alternative invocation patterns. The proper way to document alternative forms is to repeat the full command name on each line.

This is a minor but important documentation fix for an otherwise complete feature. The core functionality (allowing partial failures in batched reference updates) remains unchanged and has already received thorough review and approval. The patch series has been through multiple iterations (v5) with optimizations and style improvements already incorporated.

The thread context shows this is the final patch in an 8-part series that introduces strongly-typed error handling and partial failure support across Git's reference backends. All major technical questions were resolved, with Patrick Steinhardt explicitly approving the final optimizations. The documentation issue noted here is purely about presentation clarity in the manpage.