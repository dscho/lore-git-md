This is a test infrastructure fix addressing a specific edge case in the scalar performance test suite.

The patch modifies p9210-scalar.sh to ensure reliable test execution when run from a detached HEAD state. The issue occurs because 'scalar clone' attempts to determine the remote's default branch when no branch is specified, which fails in detached HEAD state and falls back to using the current branch name. This causes subsequent checkout operations to fail if that branch doesn't exist in the test repository.

The fix adds a branch creation step ('git checkout -B test-branch') during test setup, ensuring consistent behavior regardless of the initial repository state. This change is minimal (2 lines added, 1 removed) and maintains the existing test logic while improving reliability. The commit message clearly explains the failure scenario and solution, with references to relevant code paths in scalar.c.

This is the second patch in a two-part series fixing performance test issues, following a similar pattern of addressing specific test script problems with targeted changes. The change is straightforward and unlikely to be controversial, though reviewers may want to verify the branch name choice ('test-branch') aligns with test suite conventions.