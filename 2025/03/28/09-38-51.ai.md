This is a debugging follow-up in the ongoing investigation of platform-specific test failures for the `backfill --min-batch-size` feature on s390x and sparc64 architectures.

John Paul Adrian Glaubitz requests clarification on how to apply Patrick Steinhardt's proposed debug modifications, specifically asking for the complete command line to run tests with the `-ix` flags. The thread has progressed from initial failure reports to active debugging, with Patrick suggesting replacing `grep` with `test_grep` in the test infrastructure to reveal why the expected trace2 JSON output isn't matching on big-endian systems.

Key technical details in play:
- The test expects to find `"category":"promisor","key":"fetch_count","value":"20"` in trace2 output
- Current test infrastructure silently fails when this match isn't found
- Proposed change would show actual trace content when matches fail
- Need to combine this with `-ix` test flags for verbose output

The exchange highlights a communication gap in the debugging process - while Patrick provided the test infrastructure change, the exact invocation method for non-standard test runs isn't clear to the reporter. This is a common pain point when debugging platform-specific issues where the original developers may not have direct access to the affected hardware.

The next step depends on Patrick providing the complete test command line, after which we should see either:
1) The actual trace2 output that differs from expectations on big-endian systems, or
2) Confirmation that the test passes with modified infrastructure, suggesting the issue was in test observability rather than core functionality