Performance test suite fixes series from Philippe Blain addressing three specific test script issues across three patches, with partial resolution. The series consists of:

1. Fixes for test_perf helper usage in p7821-grep-engines-fixed.sh where prerequisite checking was improperly implemented, now properly using `--prereq` flag in `test_perf` invocations following commit 5dccd9155f's format. The patch makes mechanical changes to prerequisite specification for both single-threaded and threaded grep test cases, ensuring proper test skipping when prerequisites like PCRE support are missing. While this addresses the basic functionality, Patrick Steinhardt's concerns about edge case behavior with empty `$prereq` variables (for regex engine tests covering basic/extended/PCRE, threaded vs non-threaded) remain open for potential follow-up work on prerequisite string construction.

2. Correction for p9210-scalar.sh to work in detached HEAD state by adding explicit branch creation ('git checkout -B test-branch') during setup, preventing failures when 'scalar clone' attempts to determine remote's default branch. This 3-line change has received explicit approval from Patrick Steinhardt as "an easy and pragmatic fix" and is ready to proceed.

3. Documentation fix for p7821-grep-engines-fixed.sh's thread testing instructions, identified during prerequisite handling review. The patch modifies test documentation without changing test logic.

The series comes via GitGitGadget with minimal changes between versions - only adding the third documentation patch in v2. The first two patches remain identical to v1, indicating no substantive changes were needed based on prior review. The scalar test fix is uncontested while the grep test improvements continue refinement, now with both prerequisite handling and documentation clarifications. All patches target test infrastructure with straightforward corrections of invocation syntax, state assumptions, and documentation accuracy.