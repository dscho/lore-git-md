Response to bug report about `git push --mirror` hanging with Azure DevOps. The responder suggests the issue may be server-specific or related to TLS interception rather than a Git bug, noting that the process appears stuck waiting for server responses during the final push phase. They question whether the problem reproduces with other servers and suggest investigating local TLS interception tools (antiviruses, firewalls) that might buffer traffic incorrectly. The response clarifies that `http.postBuffer` is likely irrelevant here as Azure DevOps should handle HTTP/1.1 properly, and analyzes the provided stack traces showing Git blocked in pkt-line reading operations while waiting for server acknowledgments. The technical details focus on transport-layer behavior during the push finalization phase where Git expects periodic server responses that aren't arriving.