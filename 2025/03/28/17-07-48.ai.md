This is a test infrastructure fix addressing incorrect prerequisite handling in a performance test script. The patch corrects `p7821-grep-engines-fixed.sh` to properly declare test prerequisites using the `--prereq` flag in `test_perf` invocations, following the format established in commit 5dccd9155f. Without this fix, the test would fail with an error about missing parameters when run on systems lacking the PCRE prerequisite.

The change is minimal (2 lines modified) and clearly motivated by an actual test failure scenario. It updates two test cases: one for basic grep engine performance and another for threaded grep performance. The fix moves the prerequisite declarations from being positional arguments to using the proper `--prereq` flag syntax.

This appears to be a straightforward correction of an oversight when the test prerequisite mechanism was updated in 2022. The patch maintains the existing test logic while just fixing the invocation syntax. Since this is part of a performance test suite rather than core functionality, the impact is limited to test reliability. The change is unlikely to be controversial given its narrow scope and clear correctness.