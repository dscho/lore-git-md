Git thread summary:

Bug report about `git push --mirror` hanging indefinitely when pushing large repositories to Azure DevOps, occurring after "Storing index (done)" message. Reporter using Git 2.48.1 with large repos like elasticsearch/node. Issue appears Azure DevOps-specific and intermittent, with processes (git, git-remote-https, git send-pack) stuck in transport-helper.c during push_refs_with_push, showing multiple read() operations blocked. Stack traces point to transport-helper.c and pkt-line.c. All attempted HTTP/timeout configurations (http.postBuffer, GIT_CURL_TIMEOUT, http.maxRequests) failed to resolve. TLS connections remain active (TLSv1.3 traffic visible) but unresponsive during finalization phase. Evidence suggests server-side or protocol-specific issue rather than general Git bug, given Azure-specific context and HTTP tweaks' ineffectiveness. Report includes detailed process dumps and strace outputs.