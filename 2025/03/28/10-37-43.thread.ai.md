Updated AI thread summary:

Bug report and ongoing investigation of `git push --mirror` hanging indefinitely with Azure DevOps repositories, occurring after "Storing index (done)" message. Original reporter using Git 2.48.1 with large repos (elasticsearch/node) experiences process freezes in transport-helper.c during push_refs_with_push, with stack traces showing blocked read() operations in transport-helper.c and pkt-line.c. All attempted HTTP/timeout configurations (http.postBuffer, GIT_CURL_TIMEOUT, http.maxRequests) failed to resolve the issue. TLS connections remain active (TLSv1.3 visible) but unresponsive during finalization.

Microsoft support has attributed the issue to rate limiting, though standard timeout configurations (GIT_CURL_TIMEOUT, http.lowSpeedLimit/Time) failed to make Git exit when hung. The investigation now focuses on two key aspects: (1) whether additional timeout mechanisms exist in Git to handle indefinite hangs during push finalization, and (2) whether Azure DevOps might be sending rate-limit headers that Git isn't properly interpreting. The issue remains intermittent but consistently reproducible with large repositories using PAT authentication.

Technical details show the process stuck in pkt-line operations waiting for server responses that never arrive during the final push phase. Evidence continues to point to Azure-specific behavior, with previous analysis suggesting either Azure DevOps peculiarities or local interception tools might be causing the transport layer to wait indefinitely. The core problem remains that Git's HTTP transport appears to wait forever rather than timing out when server responses stop arriving, particularly during the critical finalization phase of mirror pushes. Open questions include whether this represents a gap in Git's timeout handling for HTTP operations or if Azure DevOps is failing to properly signal rate limiting conditions through standard HTTP mechanisms.