This is a debugging follow-up in the ongoing investigation of platform-specific test failures in the backfill feature's `--min-batch-size` option.

Patrick Steinhardt provides concrete debugging instructions to help diagnose why the "do partial clone 2, backfill min batch size" test fails on s390x and sparc64 architectures. The key suggestion is to modify the `test_trace2_data` helper function to use `test_grep` instead of plain `grep`, which will show the actual file contents when the expected trace2 data pattern fails to match. This change, combined with running tests with `-ix` for verbose output, should reveal what data is actually being recorded in the expected "promisor fetch_count" trace2 event.

The email includes the full test output showing where the failure occurs - the `grep` command looking for a specific trace2 event pattern exits with status 1, indicating the expected JSON structure with category="promisor", key="fetch_count", and value="20" was not found. The proposed debug approach aims to surface what data is actually present in the trace file instead, which may point to platform-specific differences in how the batch size handling or trace2 event recording behaves on big-endian systems.

This moves the investigation forward from problem identification to active debugging, though the root cause remains unclear. The thread continues to wait for either test results from the affected platforms or input from the feature author Derrick Stolee, who has been unresponsive to previous reports of this issue. The platform-specific nature of the failure (consistently affecting s390x and sparc64) suggests potential endianness issues in either the batch size handling code or trace2 event recording.