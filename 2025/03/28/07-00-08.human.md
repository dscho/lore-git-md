This is a final polish on the `git blame` porcelain output bugfix thread. Patrick Steinhardt provides two minor code review suggestions for Karthik Nayak's v2 patch, which implements the agreed-upon solution for showing ignored/unblamable markers in porcelain mode.

The first suggestion proposes renaming `emit_per_line_details()` to `emit_porcelain_per_line_details()` to better indicate its purpose within the porcelain output system. The second notes that `puts()` could be used instead of `printf()` for the simple string outputs, though acknowledges this is a minor stylistic point.

Patrick also confirms the test approach makes sense, recognizing that the per-line nature of the unblamable/ignored status means the markers must appear for each affected line rather than being batched per commit. This aligns with the thread's earlier technical consensus that these properties are line-specific rather than commit-wide.

The email represents final fine-tuning of an otherwise complete solution that has already received maintainer approval. The changes discussed are optional refinements rather than substantive issues, indicating the patch series is in its final stages before merging.