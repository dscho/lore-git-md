Here's the updated human-readable thread summary:

This thread began with Philippe Blain's series improving rebase status reporting, focusing on merge commit handling, output formatting, and post-merge guidance. The technical discussion has been refined through reviews from Phillip Wood and Eric Sunshine, with all core changes now confirmed as sound. The patches address specific edge cases in merge commit creation during rebase and improve status message clarity, with test improvements already implemented.

A deeper discussion emerged about an inconsistency in how Git preserves authorship metadata during conflict resolution. Johannes Schindelin pointed out that `git commit` behaves differently after a conflicted cherry-pick (preserving original authorship) versus a conflicted rebase (losing authorship unless using `git rebase --continue`). He argues this is a serious usability issue that should be fixed by making `git commit` aware of rebase's metadata files.

Phillip Wood maintains this divergence is intentional, stemming from 2011 design decisions and rebase's additional features like `--reset-author-date`. He cautions that changing this behavior would introduce complexity and that users should consistently use `git rebase --continue` for conflict resolution. While this discussion doesn't affect the current patch series, it highlights a fundamental design question about command consistency that may need future attention.

The original patch work is now in its final polishing stage, with Phillip Wood's approval pending only minor test reorganization and code style adjustments. Meanwhile, the authorship preservation discussion has opened a separate but important conversation about the long-term design of Git's history-rewriting operations.