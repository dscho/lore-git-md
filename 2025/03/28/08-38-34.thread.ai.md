Updated AI thread summary:

Build system fixes series (v1) collecting four distinct improvements to Git's build infrastructure. Primary author Patrick Steinhardt, with contributions from Eli Schwartz and Thorsten Glaser. Patches address:

1. Meson build: corrects `-Dcurl=auto` detection logic (fixes regression from earlier meson conversion) by properly checking `curl.found()` instead of `get_option('curl').enabled()`, ensuring HTTP-related features build correctly with auto-detection. This one-line change in meson.build prevents silent failures in builds with curl auto-detection.

2. Meson test environment: replaces temporary workaround with proper solution for test execution.

3. Documentation: enforces Perl dependency when building docs (Eli Schwartz's fix for builds with optional Perl disabled). The one-line meson.build modification adds `get_options('docs') != []` to Perl requirement conditions, fixing a real-world failure scenario where `-Ddocs=man -Dperl=disabled -Dtests=false` would incorrectly succeed at configuration time but fail during setup. This ensures documentation builds properly require Perl (for cmd-list.perl) alongside existing requirements for tests, gitweb, and netrc credential helper.

4. Gitweb: fixes JavaScript file generation (Thorsten Glaser's original patch, adapted by Steinhardt) addressing regression from commit 19d8fe7da65 where Makefile rule incorrectly included build script in concatenated output. Solution modifies `gitweb/Makefile` to use `$(filter %.js,$^)` when calling `generate-gitweb-js.sh`, ensuring only JavaScript files are processed while excluding the build script itself.

Technical scope spans meson.build files (including contrib/), gitweb/Makefile, affecting curl auto-detection behavior, test environment setup, documentation build requirements, and gitweb asset pipeline. Each patch carries clear attribution and references prior discussions. The curl detection fix and gitweb JavaScript generation correction are particularly important as they prevent broken functionality in common build scenarios. Series appears ready for merging with no outstanding objections noted, maintaining compatibility with Git's default feature set across different build configurations. The documentation Perl requirement patch completes the set of build dependency fixes, ensuring all components properly declare their prerequisites.