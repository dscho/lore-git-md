Updated AI thread summary:

Build system fixes series (v1) collecting five distinct improvements to Git's build infrastructure. Primary author Patrick Steinhardt, with contributions from Eli Schwartz, Thorsten Glaser, and Sam James. Patches address:

1. Meson build: corrects `-Dcurl=auto` detection logic (fixes regression from earlier meson conversion) by properly checking `curl.found()` instead of `get_option('curl').enabled()`, ensuring HTTP-related features build correctly with auto-detection. This one-line change in meson.build prevents silent failures in builds with curl auto-detection.

2. Meson test environment: replaces temporary workaround with proper solution for test execution.

3. Documentation: enforces Perl dependency when building docs (Eli Schwartz's fix for builds with optional Perl disabled). The one-line meson.build modification adds `get_options('docs') != []` to Perl requirement conditions, fixing a real-world failure scenario where `-Ddocs=man -Dperl=disabled -Dtests=false` would incorrectly succeed at configuration time but fail during setup.

4. Gitweb: fixes JavaScript file generation (Thorsten Glaser's original patch, adapted by Steinhardt) addressing regression from commit 19d8fe7da65 where Makefile rule incorrectly included build script in concatenated output. Solution modifies `gitweb/Makefile` to use `$(filter %.js,$^)` when calling `generate-gitweb-js.sh`.

5. Contrib tests: ensures netrc credential helper and git-subtree tests in contrib/ respect `-Dtests=false` flag (fixes real-world case reported by Sam James). Wraps test definitions in conditional blocks checking `get_option('tests')` in both `contrib/credential/netrc/meson.build` and `contrib/subtree/meson.build`, matching main test suite behavior.

Technical scope spans meson.build files (including contrib/), gitweb/Makefile, affecting:
- curl auto-detection behavior
- test environment setup
- documentation build requirements
- gitweb asset pipeline
- contrib test integration

Each patch carries clear attribution and references prior discussions. The series now comprehensively addresses build system inconsistencies across main and contrib components, with particular attention to:
- Preventing broken functionality in common build scenarios (curl detection)
- Ensuring proper dependency declarations (Perl for docs)
- Maintaining consistent test behavior (main vs. contrib)
- Fixing regression-induced issues (gitweb JS generation)

All patches appear ready for merging with no outstanding objections noted. The series' completion with the contrib tests fix ensures uniform behavior across Git's entire build system when tests are disabled, maintaining compatibility with various build configurations while properly handling optional components.