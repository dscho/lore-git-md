This thread collects a series of build system fixes from Patrick Steinhardt and collaborators, addressing several distinct infrastructure issues. The main focus is on correcting edge cases in Git's Meson build system and related components.

The series includes four key fixes: proper curl auto-detection in Meson (preventing silent build failures), a test environment setup improvement, this Perl dependency enforcement for documentation builds, and a gitweb JavaScript generation fix. The Perl dependency patch (from Eli Schwartz, edited by Steinhardt) closes a gap where documentation builds could be misconfigured to succeed without Perl despite actually requiring it - a one-line change that prevents broken builds when Perl is explicitly disabled but man pages are requested.

All patches appear technically sound and ready for merging, with no outstanding objections raised. The changes span meson.build files and gitweb's Makefile, affecting build-time behavior for curl detection, test execution, documentation pipeline, and web interface assets. The series maintains Git's build compatibility while fixing real-world failure scenarios, particularly the curl detection and Perl dependency cases that could silently produce broken builds.