Git thread summary (AI mode):

This thread addresses a Cygwin-specific regression in gitk introduced in Git v2.49.0, causing stack exhaustion due to a recursive loop between `_which` and wrapped `exec` functions. The issue stems from commit 4cbe9e0e2 implementing Windows-specific protections against executing worktree files, which inadvertently affected Cygwin's path handling. Mark Levedahl reported the regression and proposed two solutions: (1) treating Cygwin as Linux by removing path conversion, or (2) restricting exec/open overrides to Windows only while preserving Cygwin functionality.

Johannes Sixt (Hannes) has now responded, questioning whether the Windows-specific protections need to apply to non-Windows platforms including Cygwin, and specifically asks Johannes Schindelin (Dscho) to confirm any rationale for the current platform-agnostic implementation. The discussion is converging toward restricting the security measures to Windows-only, which would resolve the Cygwin regression while maintaining intended protections where needed. Key technical details remain the interaction between `_which` and `real_exec` functions, PATH handling differences between Windows and Cygwin, and platform-specific behavior in gitk's TCL script. The thread awaits Dscho's input on the security implications before proceeding with a fix.