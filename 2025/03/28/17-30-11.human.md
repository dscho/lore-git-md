This is a follow-up in the Cygwin regression thread about gitk's recursive execution issue. Johannes Sixt confirms Mark Levedahl's analysis that the problematic `exec`/`_which` loop in gitk appears to be Windows-specific and shouldn't affect Cygwin. He questions whether there's any hidden rationale for keeping the current platform-agnostic implementation that's causing the regression.

The email reinforces the technical assessment from earlier in the thread: the security-oriented changes from commit 4cbe9e0e2 were meant to address Windows-specific PATH handling vulnerabilities but inadvertently broke Cygwin compatibility through over-broad implementation. Johannes directly asks Dscho (Johannes Schindelin) to confirm whether there's any unstated reason for maintaining the current behavior across all platforms, implicitly supporting Mark's proposed fix to limit the changes to Windows only.

The exchange shows growing consensus that the regression should be fixed by making the security measures platform-specific, but awaits confirmation from the original patch author before proceeding. The thread remains technically focused on the boundary between Windows security needs and Cygwin compatibility.