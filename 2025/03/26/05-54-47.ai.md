This is a build system tweak in the ongoing comma operator refactoring series.

Patrick Steinhardt suggests modifying the meson build configuration to match the Makefile changes in Johannes Schindelin's patch that adds `-Wcomma` warnings for Clang. The key technical point is that the meson build system can use the same warning flag since its compiler feature detection loop already handles flag compatibility checking, making the Clang-specific conditional unnecessary.

The change would add `-Wcomma` to meson's warning flags when building with developer warnings enabled (warning level 2 or 3). This aligns with the series' goal of systematically detecting comma operator usage across the codebase while maintaining build system consistency. The suggestion represents a minor refinement to the already-reviewed approach rather than introducing new functionality.

This follows the established pattern in the series where build system changes support the code hygiene effort, with Patrick's input continuing his role as a reviewer of the detection methodology. The meson integration would apply the warning uniformly rather than limiting it to Clang 9+, though the practical effect remains the same since the warning is only enabled in developer mode.