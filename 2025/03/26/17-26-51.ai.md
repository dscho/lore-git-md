Bugfix patch completing a two-part series addressing out-of-bounds array access patterns identified by static analysis. This second patch modifies `read-cache.c` to reorder bounds checking in `ce_write_entry()`'s name comparison loop, following the same defensive programming principle as the first patch (which fixed `diff.c`).

The change swaps the order of conditions in a loop that compares cache entry names, ensuring the array bounds check (`common < previous_name->len`) happens before dereferencing `ce->name[common]` and `previous_name->buf[common]`. Like the first patch, this prevents potential out-of-bounds reads when the loop reaches the end of the string. The fix is minimal (2-line change) but important for correctness.

The patch continues the pattern established in the series of addressing CodeQL static analysis findings (`cpp/offset-use-before-range-check` rule). While the first patch handled diff indentation data, this one targets the index handling subsystem's cache entry writing path. The author maintains consistent messaging about the first-principles rationale (bounds checking before access).

No new test cases are included, suggesting these are considered obviously correct defensive changes rather than fixes for observed bugs. The changes appear mechanical and uncontroversial, completing the systematic code quality improvements started in the first patch. The series now covers both diff and index subsystems with similar bounds-checking fixes.