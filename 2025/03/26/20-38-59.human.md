This is a discussion about the implementation approach for the `git-blame-tree` feature, which provides tree-level blame functionality. Taylor Blau responds to Toon Claes' patch series by suggesting an alternative implementation path.

Taylor notes that the current implementation has evolved significantly from its 2011 origins, mentioning a more recent version he worked on with Derrick Stolee that uses changed-path Bloom filters for more efficient traversal. This approach would pass un-blamed paths to parent commits during traversal, potentially offering better performance characteristics than the current pathspec trie-based implementation.

The comment suggests a fundamental reconsideration of the implementation strategy, proposing to start fresh with the Bloom filter approach rather than building upon the submitted series. This represents a significant fork in the road for this long-running effort, as the current series appears production-ready with all core optimizations complete (as noted in the thread summary).

This intervention from an experienced contributor (who has worked on similar functionality) may prompt a reevaluation of the technical direction, though the thread summary indicates the current implementation already shows impressive performance gains (2-21x speedups depending on repository size). The discussion now appears poised between choosing between two mature but distinct technical approaches to solving the same problem.