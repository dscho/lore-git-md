This is a technical discussion about a Windows-specific bugfix in the reftable lockfile handling, now focusing on implementation approach in the v2 patch series.

Johannes Schindelin raises concerns about the proposed solution in patch 2, which uses the undocumented `RtlGetLastNtStatus()` function to detect pending file deletions. He suggests an alternative approach that would handle `EACCES` the same way as `EEXIST` in the lockfile subsystem, avoiding reliance on undocumented Windows APIs. This would be more targeted than the reftable-specific solution proposed in v1, but less invasive than the current v2 approach.

The key technical points are:
- The current v2 patch uses `RtlGetLastNtStatus()` to precisely identify `STATUS_DELETE_PENDING` cases, mapping them to `EEXIST` for proper lockfile retry behavior
- Johannes argues this API is undocumented and thus unsuitable for Git's codebase
- He proposes either:
  1. Treating all `EACCES` errors like `EEXIST` in the lockfile subsystem (narrower than v1 but broader than v2)
  2. Implementing a polling strategy similar to SQLite's approach
- The discussion now centers on balancing correctness (precise error detection) with maintainability (avoiding undocumented APIs)

This email represents a significant development in the thread, as Johannes is both a Windows expert and Git maintainer. His concerns about using undocumented APIs carry substantial technical weight and will likely require a response addressing either the maintainability concerns or proposing an alternative solution that meets both correctness and maintainability requirements. The thread now has three distinct approaches on the table (v1's broad solution, v2's precise but undocumented solution, and Johannes's suggested alternatives).