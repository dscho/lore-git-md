This is a test improvement suggestion for the already-approved `git blame` porcelain output bugfix. Eric Sunshine points out two minor issues in Karthik Nayak's test script that would affect test output readability:

1. The test titles for the `--porcelain`/`--line-porcelain` loop tests currently show a literal `$opt` variable rather than expanding to show which mode is being tested, due to using single quotes. Eric suggests using double quotes to properly interpolate the variable.

2. The second test case has a static title "mark_ignored_lines line_porcelain" that doesn't distinguish between the two porcelain modes being tested, which could make test output less clear. The same double-quote fix would make these more informative.

These are purely cosmetic improvements to the test output - the actual test functionality remains correct. The suggestions maintain the existing test structure while making the output more precise about which porcelain variant is being exercised in each case. The thread context shows this patch has already been through extensive review and approval, with these being final polish items before merging.