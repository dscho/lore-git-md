This is a style discussion in the ongoing refactoring series to eliminate comma operator usage in Git's codebase, specifically focusing on diff-delta.c. Phillip Wood responds to Johannes Schindelin's defense of using `(void)` casts to mark intentional comma operator usage, arguing that approach makes the code unreadable. The exchange centers around two alternative implementations for handling delta encoding in diff-delta.c's delta compression logic.

Key technical details:
- The original code uses comma operators to both write shifted bytes to output and set bits in `i` within single statements
- Johannes proposes keeping the comma operators but marking them with `(void)` casts for clarity, arguing the vertical alignment helps see patterns
- Phillip counters that this is still unreadable and suggests either:
  - Using explicit blocks with braces (as previously suggested by Patrick Steinhardt)
  - Keeping both statements on one line to minimize vertical space impact
- The discussion remains focused on stylistic preferences, as all approaches produce identical behavior in the delta encoding logic

This represents the final unresolved question in the series, with build system integration and other technical aspects already settled. The thread shows developers weighing readability tradeoffs in a performance-sensitive code path, with Phillip advocating for more conventional C style over Johannes's pattern-matching optimization.