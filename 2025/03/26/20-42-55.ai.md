This is a bug report about a platform-specific test failure following a feature addition. The email reports that commit 6840fe9ee2 ("backfill: add --min-batch-size=<n> option") is causing test failures on s390x and sparc64 architectures, specifically in test case "do partial clone 2, backfill min batch size". The reporter provides links to Debian build logs showing the failures and CCs the original author (Derrick Stolee) for awareness.

The commit in question added a new `--min-batch-size` option to the `backfill` command, which allows users to specify minimum batch sizes for delta compression during partial clones. The test failure appears to be architecture-specific, affecting s390x and sparc64 but not necessarily other platforms. The reporter doesn't provide analysis of why the test fails on these architectures, just the observation that it does. This suggests either a platform-specific behavior difference in the new backfill functionality or perhaps an assumption in the test that doesn't hold on these architectures. The issue appears to be raised for investigation rather than proposing a specific fix.