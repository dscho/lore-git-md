The thread continues to investigate why the `backfill --min-batch-size` test fails on s390x and sparc64 architectures. After initial reports of the failure, Patrick Steinhardt provided debugging guidance while John Paul Adrian Glaubitz sought clarification on test invocation. Todd Zullinger has now stepped in with precise instructions: the test should be run with `cd t && ./t5620-backfill.sh -ix` to enable verbose output. This reveals a complication - Patrick's suggestion to replace `grep` with `test_grep` in the test helper doesn't work as-is because `test_grep` requires explicit file input. 

The investigation has identified two parallel paths forward: direct debugging using the clarified test command, and integration work for Fedora's build system to better capture test failures. While the root cause remains unclear - possibly relating to endianness in batch size calculation or trace2 recording differences - the debugging tools are now better specified. The focus has shifted to executing these steps on the affected platforms to gather the missing trace2 output that should reveal why the expected promisor fetch count isn't being recorded correctly on big-endian systems.