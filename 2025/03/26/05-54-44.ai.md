This is a reply in the ongoing refactoring series to mark intentional comma operator usage in Git's codebase. The email specifically addresses patch 7/10 which modifies `wildmatch.c` to explicitly mark a comma operator with a `(void)` cast.

Patrick Steinhardt agrees with Johannes Schindelin's approach in this particular case, stating that introducing curly braces would unnecessarily reduce code brevity. The change in question involves a do-while loop condition in the wildmatch pattern matching code where two operations (assigning `prev_ch` and advancing the pattern pointer) are combined with a comma operator.

This represents a point of consensus in the stylistic debate that emerged earlier in the thread regarding comma operator usage versus curly-braced blocks. While the discussion in `diff-delta.c` remains open, this particular case in `wildmatch.c` is now settled with agreement that the comma operator approach is appropriate here.

The email confirms that at least some cases of comma operator usage can be preserved when they serve to maintain code conciseness, particularly when the operations are closely related (pointer advancement and state tracking in this pattern matching context). This aligns with the series' goal of being selective about comma operator usage rather than eliminating it entirely.