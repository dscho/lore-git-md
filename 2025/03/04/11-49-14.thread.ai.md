RFC proposal for `git init` language-specific `.gitignore` support continues evolving with Oleg Taranenko's new `--only-gitignores` flag suggestion for `git init --template`. This middle-ground approach attempts to reconcile Junio C Hamano's strong neutrality principle (rejecting built-in language awareness) with practical usability by proposing selective copying of just `.gitignore` files from templates while ignoring other components. The maintainer's prior guidance remains the dominant constraint - any solution must work within Git's template mechanism without special-casing language ecosystems in core.

Current technical landscape now includes three distinct approaches: (1) original language-specific built-ins (rejected by maintainer), (2) Randall Becker's regex-based system (facing neutrality concerns), and (3) this new selective template proposal. All must contend with core architectural questions about whether `.gitignore` handling warrants any special treatment beyond current template capabilities. The `--only-gitignores` suggestion introduces new implementation questions around directory structure preservation and conflict resolution that weren't present in earlier proposals.

Key unresolved questions now expand to include: (1) whether selective template copying violates neutrality less than other approaches, (2) if the proposed warning behavior for existing `.gitignore` files sets acceptable precedent, and (3) how this compares to users manually copying ignore files post-init. The thread's trajectory continues suggesting low likelihood of core changes unless proponents can demonstrate their solution truly respects Git's design philosophy while solving real user needs not addressable through existing mechanisms or trivial scripting. Next steps require either concrete patches demonstrating the `--only-gitignores` approach's neutrality preservation or community consensus that the use case doesn't justify core modifications.