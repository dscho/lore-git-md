This is a bugfix patch addressing a memory leak in the reftable infrastructure, specifically in the error paths of `reftable_reader_new()`. The patch builds on prior discussion in the thread about memory management issues in the reftable code.

The key change adds cleanup for the `name` field when initialization fails, preventing a leak that occurs if either `block_source_read_block()` or `parse_footer()` encounters an error. The v2 revision incorporates Jeff King's feedback by adding a NULL check before accessing `r->name`, avoiding a potential NULL dereference in the error path.

The fix takes a minimal approach, maintaining the existing allocation timing while properly cleaning up in failure cases. The patch demonstrates good attention to detail by addressing both the original leak report and the edge case identified during review. The change is well-contained (just 2 lines added) and clearly documented in the commit message with proper attribution to both the reporter and reviewer.

This appears to be the final iteration of the fix for this particular issue, with the thread having converged on this solution after discussing alternative approaches (NULL checks vs label restructuring vs early returns). The remaining documentation inconsistency regarding `reftable_reader_destroy()` mentioned in the thread appears to be intentionally left for future work.