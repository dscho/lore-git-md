This is a test modernization patch, the final version in a series that improves validation in t1403-show-ref.sh. The change replaces a simple file existence check with a more robust helper that also verifies the path is a regular file, following reviewer feedback from Patrick Steinhardt.

The patch simplifies the v3 version by removing redundant checks - where v2/v3 used both `test_path_exists` and `test_path_is_file`, v4 now uses just `test_path_is_file` since that helper already checks for existence. This makes the test both more precise (ensuring we're dealing with a file, not just any filesystem object) and more concise.

The change is narrowly scoped to the `remove_object` helper function in this test script, which deletes objects during test setup. While small, it follows Git's test modernization efforts by using the project's preferred test helpers that provide better diagnostics on failure. The thread shows a clear progression from initial proposal to final optimized solution, with all discussion points now addressed.