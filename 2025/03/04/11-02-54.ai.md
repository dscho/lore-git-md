Test modernization follow-up discussing the `remove_object` helper in t1403-show-ref.sh. Patrick Steinhardt points out that the v2 patch's combined use of `test_path_exists` and `test_path_is_file` is redundant, since the latter already implies the former. This is a technical clarification about test helper usage, noting that the file existence check (`test_path_exists`) can be safely removed while maintaining the same validation since `test_path_is_file` performs both checks. The discussion remains focused on this narrow test infrastructure improvement, with Steinhardt providing implementation guidance to simplify the code while preserving all necessary validation. This appears to be the final piece of feedback before the patch can be considered complete, addressing a small optimization opportunity in the test helper's implementation.