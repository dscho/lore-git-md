Bugfix discussion for `transfer.hideRefs` consistency across ref backends. This email confirms the reftable backend exhibits the same incorrect behavior as packed refs when matching prefix patterns in `ref_is_hidden()`, where it incorrectly hides all refs starting with a prefix rather than requiring a directory separator (like loose refs do). Patrick Steinhardt provides the specific reftable implementation change needed in `should_exclude_current_ref()` to match the correct behavior, requiring either exact matches or proper directory containment (with trailing '/'). Taylor Blau acknowledges the analysis and indicates intent to work on a fix. The thread has now identified the complete technical solution path for all three backends (loose, packed, reftable) to achieve consistent behavior, with implementation work remaining for both packed and reftable backends. The fix will align with the original loose refs behavior where prefix patterns must be directory boundaries to exclude child refs.