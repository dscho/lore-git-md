This is a refinement patch in Taylor Blau's incremental MIDX bitmap series (v4, patch 2/13), responding to Jeff King's feedback about object counting semantics. The key change here is renaming `bitmap_non_extended_bits()` to `bitmap_num_objects_total()` to better distinguish it from `bitmap_num_objects()`, addressing the need to clarify when callers should consider all layers versus just a single layer.

The discussion reveals an important architectural point: some operations (like bitmap extensions and pseudo-merge lookups) need layer-local object numbers, while others (like position calculations) need the total across all layers. Taylor is open to further suggestions about where to document this distinction - either in the commit message or as in-code comments near the renamed function.

This appears to be one of the final polish items before the series could be ready for merging, with the naming clarification being the last substantive change needed in this patch. The exchange shows the careful attention being paid to making the API's layer-handling semantics clear to future developers.