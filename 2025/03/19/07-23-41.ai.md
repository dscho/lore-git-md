This is the final patch in Martin Ågren's 8-part refactoring series for Git's pretty-format parsing code. The series has systematically modularized the complex formatting logic in pretty.c while fixing bugs along the way.

Patch 8/8 focuses on the decoration options parsing (for `%(decorate...)` placeholders), consolidating the handling of the format's colon and parenthesis into `parse_decoration_options()`. The key changes are:
- Moves the ':' and ')' parsing from `format_commit_one()` into `parse_decoration_options()`
- Makes `parse_decoration_options()` handle all error paths including cleanup
- Introduces proper initialization of the `decoration_options` struct
- Maintains identical behavior while making the control flow clearer

The patch continues the series' pattern of breaking down complex formatting logic into more maintainable pieces. Like previous patches, it includes thorough error handling and resource cleanup. The changes are purely internal - no user-visible behavior is altered. This completes the current cycle of refactoring the pretty-format parsing code, having systematically addressed padding, line-wrapping, magic characters, and now decoration options. The series appears well-executed with careful attention to edge cases and resource management.