This is the final patch in a series implementing NUL-delimited output for `git rev-list`, specifically adding support for the `--missing` option. The patch completes the feature by extending the structured NUL-delimited format to missing object reports, using a consistent key-value syntax with `missing=yes` and optional path/type information.

The implementation handles both regular and NUL-delimited output modes, with special care taken to properly format paths containing spaces or special characters. The documentation is updated to include the new format, and tests verify the behavior with edge cases like paths containing spaces. The patch maintains the series' design principle of using structured tokens rather than special prefixes (replacing the '?' prefix with `missing=yes` in NUL-delimited mode).

This appears to be a well-considered addition that completes the NUL-delimited output feature set for `rev-list`. The implementation is thorough, with clear documentation and tests that cover the new functionality. As the final piece of a reviewed series that has already addressed design questions, this patch seems ready for inclusion.