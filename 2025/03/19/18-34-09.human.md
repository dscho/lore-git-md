This is part 4/5 of a feature series adding NUL-delimited output support to `git rev-list`, now focused solely on output formatting after removing stdin handling in v4. The patch implements NUL-delimited output for the `--boundary` option, completing the machine-readable output format coverage.

The change modifies boundary object output to use a key-value format (`<OID> NUL boundary=yes NUL`) rather than prefixing with '-', matching the series' consistent NUL-delimited syntax. The implementation includes documentation updates, code changes to handle the new format, and a new test case verifying the behavior. The patch maintains the series' careful handling of special characters and proper rejection of incompatible options like `--graph` when used with `-z`.

This continues the well-structured approach seen throughout the series, with clear documentation, thorough testing, and attention to edge cases. The change is straightforward given the groundwork laid in earlier patches, and the implementation appears robust. With the design consensus established in previous discussion rounds, this piece fits cleanly into the overall effort to make `rev-list` output more machine-parseable.