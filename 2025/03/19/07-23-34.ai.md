Refactoring patch in a series cleaning up pretty-format parsing. This first patch tightens the function signature of `format_commit_one()` in pretty.c` to take the specific `struct format_commit_context*` type rather than a `void*` that gets immediately cast. The change is straightforward since both callers already have the correct type available. This eliminates unnecessary casting while maintaining identical behavior, serving as a simple cleanup to improve type safety and code clarity. The patch is part of a larger effort to refactor Git's pretty-format parsing logic, with this initial change being a low-risk preparation step. No test changes are needed since this is purely an internal interface adjustment with no observable behavior difference.