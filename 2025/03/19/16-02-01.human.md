This is a minor test fix for the NUL-delimited output feature in `git rev-list`. Justin Tobler responds to Christian Couder's observation about confusing commit ordering in the test case for patch 4/6 of the series.

The test currently assigns `oid1` to HEAD (commit 2) and `oid2` to HEAD~ (commit 1), which is backwards from the chronological commit order. While the test works as written (since it's checking output ordering rather than commit history), Justin acknowledges this could be confusing and agrees to reorder the variable assignments to match the actual commit sequence.

This is a small but thoughtful adjustment to improve test clarity in a series that's otherwise technically complete. The change doesn't affect the functionality of the NUL-delimited output feature itself, which has already been through extensive review and discussion in the thread.