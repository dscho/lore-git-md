This is the first patch in Meet Soni's v4 reftable error handling bugfix series, focusing on propagating specific error codes in `block_writer_add()`. The patch replaces generic -1 return values with defined error codes throughout the reftable implementation, particularly introducing `REFTABLE_ENTRY_TOO_BIG_ERROR` for size-related failures.

The changes span three files (block.c, block.h, and record.c) and systematically update error handling in functions that write reftable records. The key improvement is that callers can now distinguish between a block-full condition (which may trigger a flush and retry) versus other error conditions. The commit message notes this prepares for finer-grained error handling while maintaining the same behavior.

The patch appears well-structured and focused, with clear documentation updates to match the new error handling approach. This continues the series' progression under Patrick Steinhardt's mentorship, addressing prior feedback about error code standardization. The changes are mechanical but important for robust error handling in the reftable backend.