This is a technical refinement in the incremental MIDX bitmap series (patch 7/13), addressing a minor optimization in test infrastructure. Taylor Blau responds to Elijah Newren's observation about redundant zero-initialization in the bitmap test data preparation code.

The issue centers around `bitmap_test_data_prepare()` which first zeroes out the test data, then later uses `CALLOC_ARRAY` (which also zeroes) when preparing base layer data. Newren suggests either removing the initial zeroing or switching to `xmalloc` to avoid the duplicate operation. Blau agrees and opts to replace `CALLOC_ARRAY` with `xmalloc` for the base test data allocation.

This change doesn't affect the core bitmap functionality but improves the test code's efficiency. The discussion shows the series in its final polishing phase, with reviewers catching minor optimizations while the core implementation remains stable. The exchange demonstrates the careful attention to detail in the series' test infrastructure, even for seemingly small memory initialization patterns.