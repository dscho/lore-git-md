Refactoring patch in the NUL-delimited output series for `git rev-list`. This is the second patch in v4 of the series, which consolidates early option parsing for `--missing` and `--exclude-promisor-objects` into a single pass.

The change is mechanical but important groundwork for the subsequent NUL-delimited output feature. It combines two separate option-parsing loops into one, while maintaining the same validation logic (the options remain mutually exclusive). The patch introduces `die_for_incompatible_opt2()` to handle the option conflict check more cleanly.

This refactoring prepares for adding another early-parsed option in later patches (presumably the `-z` output control). The implementation looks straightforward, with the diff showing a clear simplification of the previous code. Since this is part of a well-reviewed series that's already reached consensus on the technical approach, this preparatory change seems uncontroversial.