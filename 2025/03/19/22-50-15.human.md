This is the final patch in a series improving cruft pack handling in `git repack`, introducing the new `--combine-cruft-below-size` option as a clearer alternative to the problematic `--max-cruft-size` behavior. Taylor Blau responds to Elijah Newren's documentation feedback on the patch, making several wording improvements:

1. Changes "prohibits repacking" to "leaves alone" for large cruft packs to better reflect the passive nature of the operation
2. Clarifies that `--max-cruft-size` can be used alongside (not instead of) the new option
3. Declines to document specific size ratios between the options, arguing the explanation would be too verbose and that size limits should generally be avoided anyway

The patch itself (already reviewed earlier in the thread) implements selective cruft pack combination where packs below a size threshold are aggregated (potentially exceeding the threshold) while larger packs remain untouched. This completes the series' transition from the original `--max-cruft-size` semantics to more intentional behavior.

The exchange shows careful attention to documentation clarity while maintaining technical precision. Taylor pushes back slightly on over-specifying usage patterns, wanting to keep the docs focused and discourage unnecessary use of size limits. The series appears ready for finalization with these last documentation tweaks.