This is a documentation refinement in the ongoing cruft pack handling series. Taylor Blau responds to Junio C Hamano's review of the final patch introducing `--combine-cruft-below-size`, agreeing that the documentation should explicitly state the size parameter is in bytes (with k/m/g suffix support) for consistency with related options like `--max-pack-size`. 

The exchange clarifies that while `--max-cruft-size` doesn't redundantly restate the units (since it references `--max-pack-size`'s documentation), the new option should follow the same pattern of explicit unit specification that Git uses for size parameters. This maintains documentation consistency without requiring duplicate explanations.

The response confirms the implementation itself remains unchanged - this is purely a documentation polish addressing Junio's observation about making the units clear. The thread shows the careful attention to detail in both the code and its documentation throughout this series improving cruft pack management in `git repack`.