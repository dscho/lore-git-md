Refactoring and bugfix in Git's pretty-format parsing. This is patch 5/8 in Martin Ågren's series cleaning up pretty.c's formatting code.

The patch addresses lingering state in the `struct padding_args` after processing padding directives ("%<" or "%>"). While the previous commit prevented partial population of this struct, this change ensures all fields (`flush_type`, `truncate`, and `padding`) are fully reset after use. The commit message notes this is defensive programming - no current bug is known to exploit the lingering state, but resetting it makes the code's behavior more predictable and prevents potential future issues.

A new test case verifies that truncation directives only affect the immediately following placeholder, not subsequent ones. The change touches core pretty-formatting logic in `format_and_pad_commit()` while maintaining all existing behavior for valid inputs. This continues the series' pattern of tightening the pretty-format state machine's invariants while preparing for more substantial refactoring.

Key technical details: the patch modifies the padding state machine to clear all three padding-related fields after each use, not just `flush_type`. The test case specifically checks that truncation directives don't leak between placeholders by verifying "%<(4,trunc)%H%H" properly truncates only the first hash. The change is minimal (2-line core change + test) but reinforces the series' goal of making the pretty-format parser more robust.