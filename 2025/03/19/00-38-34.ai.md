This is a technical refinement in Taylor Blau's incremental MIDX bitmap series (v4, patch 11/13), addressing Jeff King's feedback about type bitmap tracking. The patch maintains the core functionality of tracking type-level bitmaps (commits_all, trees_all, blobs_all, tags_all) across MIDX layers, but now includes discussion of potential optimizations for the non-incremental case.

Key technical points in this exchange:
- Jeff King suggests optimizing memory usage for non-incremental cases by pointing the `*_all` arrays directly to the single existing bitmaps rather than allocating new arrays
- Taylor provides a prototype implementation showing how this would work with conditional freeing in `free_bitmap_index()`
- Taylor expresses reservations about coupling the array lifecycle to the `base` pointer NULL check, seeing it as a potential maintenance hazard
- Both agree the optimization is minor and possibly not worth the added complexity
- Jeff notes that deeper pointer-chase optimizations would yield more benefit if micro-optimizations were truly needed

The discussion represents final polishing of an otherwise complete implementation, with Taylor opting for the more conservative approach of consistent allocation patterns rather than introducing conditional allocation logic for a minor optimization. This aligns with the series' current state where all core functionality is implemented and reviewed, with only minor refinements being considered before merging.