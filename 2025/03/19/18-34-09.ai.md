This is a feature patch in the NUL-delimited output series for `git rev-list`, specifically implementing `--boundary` support in NUL-delimited format. The patch extends the NUL-delimited output format to handle boundary objects, replacing the traditional hyphen-prefix with a structured `boundary=yes` token/value pair when `-z` is used. The implementation maintains consistency with the series' key-value syntax while properly rejecting incompatible options like `--graph` and `--left-right` when combined with `-z`.

Key technical details include:
- Updates documentation to include boundary object format in the NUL-delimited output specification
- Modifies `show_commit()` to emit boundary markers in the new format when `line_term` is set (indicating NUL mode)
- Adds test coverage in t6000-rev-list-misc.sh verifying correct output format with boundary commits
- Maintains the series' pattern of using OIDs as record markers (never containing '=') with NUL-delimited metadata
- Preserves the existing behavior of rejecting incompatible options when `-z` is active

The patch follows the series' established design principles and appears mechanically sound, with clear documentation updates and test coverage. This completes the NUL-delimited output implementation for boundary objects, leaving only the `--missing` case (handled in patch 5/5) remaining in the series.