This is a refactoring patch in the NUL-delimited output series for `rev-list`, specifically addressing early option parsing.

The patch consolidates the early parsing of `--missing` and `--exclude-promisor-objects` options into a single pass before `setup_revisions()` is called. This prepares for adding another early-parsed option in a subsequent commit. The main changes are:

1. Moves both option parsers into the same loop through arguments
2. Uses `die_for_incompatible_opt2()` to validate option combinations
3. Simplifies the control flow by removing redundant checks
4. Maintains all existing functionality while making the code more maintainable

The changes are mechanical and preparatory for the NUL-delimited output feature. The patch follows the series' pattern of refactoring before adding new functionality, with the option validation now using Git's standard `die_for_incompatible_opt2()` helper. This maintains consistency with how other commands handle mutually exclusive options.

The implementation appears straightforward and well-contained, touching only the option parsing logic in `builtin/rev-list.c`. The changes align with the series' goal of improving machine-readability of `rev-list` output while maintaining existing behavior for human-readable output.