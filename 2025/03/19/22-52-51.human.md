This is a test cleanup patch in the ongoing cruft pack repacking series. It removes a redundant helper function `write_blob()` that was brought over when tests were moved from another script, replacing its usage with the existing `generate_random_blob()` function which has identical functionality but better cleanup behavior.

The change is straightforward - it updates one test case to use the preferred blob generation method and removes the now-unused helper. This continues the series' pattern of test infrastructure improvements before introducing behavior changes. The patch maintains all existing test coverage while simplifying the test file's maintenance burden by removing duplicate functionality.

This appears to be a minor but sensible cleanup as part of the larger effort to improve cruft pack handling in `git repack`. The change is well-contained and uncontroversial, following the series' careful approach to test preparation.