This is a technical refinement in the incremental MIDX bitmap series, specifically addressing object counting semantics across MIDX layers. Taylor Blau responds to Jeff King's query about the distinction between `bitmap_num_objects()` (single-layer scope) and the new helper `bitmap_non_extended_bits()` (multi-layer scope) by renaming the latter to `bitmap_num_objects_total()` for clarity. The change reflects the ongoing discussion about how to best represent object positions when dealing with layered bitmaps, where some operations need local layer counts while others require cumulative totals across all MIDX layers.

Key technical details:
- The renamed `bitmap_num_objects_total()` now explicitly indicates its role in counting objects across all MIDX layers
- Original `bitmap_num_objects()` remains for single-layer operations like bitmap extension sizing and pseudo-merge validation
- Discussion remains open about whether to add clarifying documentation about the counting semantics

The change appears to resolve Jeff King's concern about function naming while maintaining the architectural separation between layer-local and cross-layer operations. This represents one of the final refinements to the series before potential merging, with only minor documentation questions remaining open.