Refactoring patch in a series adding NUL-delimited output to `git rev-list`. This is the first of five patches in v4 of the series, which has been simplified to focus solely on output formatting (dropping stdin handling from earlier versions).

The patch inlines the single-use `show_object_with_name()` function into `show_object()` and consolidates the OID printing logic that was previously duplicated between named and unnamed object cases. This cleanup prepares for the subsequent NUL-delimited output changes while making the code more straightforward - the diff shows the function being removed from both revision.c and revision.h while its logic is incorporated directly into builtin/rev-list.c.

The change is mechanical and well-contained, touching only the object display path. It maintains the same output format (space-separated OID and name when names are shown, just OID plus newline otherwise) while eliminating an indirection. The commit message clearly explains the motivation and ties it to the larger series goal of adding NUL-delimited output support.