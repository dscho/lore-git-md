This is a refactoring patch in Martin Ågren's series cleaning up Git's pretty-format parsing code. It simplifies the logic for handling color placeholders in pretty-formats by reducing code duplication between the "auto" and default cases.

The change focuses on the `parse_color()` function in pretty.c, which processes color-related format placeholders like `%C(...)`. Previously, the code had separate but identical blocks for handling "auto," and the default case, with "always," as a distinct path. The patch restructures this to make the default case handle both scenarios by opportunistically consuming "auto," when present, while keeping the "always," case separate since it has different behavior (ignoring color settings).

Key technical details: The modification affects color placeholder parsing in the format state machine, specifically around the `skip_prefix()` checks for "auto," and "always," prefixes. The behavior remains identical - "auto" respects `want_color()` while "always" forces color output - but the implementation is now more concise. The change touches core pretty-formatting logic but doesn't require test updates since it's purely an internal refactor with no functional changes.

This continues the series' pattern of small, focused improvements to the pretty-format codebase, with this patch specifically addressing code duplication in color handling while maintaining all existing behavior. The commit message clearly explains the motivation (avoiding careful comparison of duplicate code blocks) and considers alternative implementations before settling on this approach.