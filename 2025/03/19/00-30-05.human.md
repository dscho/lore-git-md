This is a technical refinement in the ongoing MIDX bitmap series, specifically patch 7/13 which teaches `rev-list --test-bitmap` to handle incremental MIDX bitmaps. 

Taylor Blau responds to Elijah Newren's observation about redundant zeroing in the test data preparation code. The issue involves `bitmap_test_data_prepare()` zeroing out memory that was already zeroed by `CALLOC_ARRAY()`. 

Blau agrees with Newren's suggestion to optimize this by switching from `CALLOC_ARRAY()` to `xmalloc()` since the subsequent `bitmap_test_data_prepare()` call will handle the zeroing anyway. This is a minor but sensible cleanup to avoid unnecessary operations in test infrastructure code. 

The exchange shows the series is in its final polishing phase, with reviewers catching small inefficiencies even in test helper code. The change doesn't affect core functionality but demonstrates the careful attention to detail in this mature patch series.