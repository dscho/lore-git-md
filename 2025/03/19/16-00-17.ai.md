This is a refactoring patch in the HTTP TCP keepalive configuration series. It introduces a new helper function `set_long_from_env()` to clean up environment variable parsing for long integers in http.c.

The patch replaces two existing instances of manual getenv()+strtol() handling with calls to the new helper function. The changes are straightforward - removing temporary variables and consolidating the parsing logic into a single reusable function. The commit message explains this prepares for future additions that will need similar environment-to-long parsing.

The implementation maintains the same behavior while reducing code duplication. The diff shows the net effect is positive (+9/-8 lines) with improved readability. This appears to be a well-contained preparatory change that follows the series' pattern of infrastructure improvements before feature implementation.