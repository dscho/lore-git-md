This is a test cleanup patch in the cruft pack repacking series. It removes a redundant helper function and standardizes on the test suite's existing blob generation utility.

The patch eliminates a custom `write_blob()` function that was moved over from another test file during earlier test consolidation. The function duplicates the functionality of `generate_random_blob()` but is slightly worse as it doesn't clean up after itself. The change updates one test case to use the preferred utility and removes the now-unused function.

Key technical details:
- Replaces `write_blob()` calls with `generate_random_blob()` in the "cruft repack with --max-pack-size" test
- Removes the 3-line `write_blob()` helper function definition
- Maintains identical test behavior while using the test suite's standard utilities
- Part of the broader test infrastructure preparation for cruft pack handling changes

This is a straightforward test hygiene improvement that follows naturally from the earlier test reorganization work in the series. The change reduces test code duplication and aligns with the project's test utility conventions.