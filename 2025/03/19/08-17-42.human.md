This is a discussion about naming and behavior of worktree-related flags in the new `git reflog drop` subcommand, following up on the implementation that's otherwise ready for merging.

Christian Couder and Junio Hamano are discussing two approaches to handle the current behavior where `--single-worktree` (which Christian suggests might be better named `--current-worktree`) silently does nothing when used without `--all`. This behavior matches the existing `git reflog expire` command, but both agree it's suboptimal UX.

Junio presents two paths forward: either keep the current behavior for consistency with `expire` and address it later, or first improve `expire`'s behavior before aligning the new `drop` subcommand. Christian expresses openness to either approach, noting these improvements could come in a follow-up series.

The discussion is largely settled, with both agreeing the core functionality is sound and this is a UX polish that doesn't block merging the current series. The thread shows consensus to proceed with the implementation while leaving room for future improvements to the worktree flag behavior across both reflog subcommands.