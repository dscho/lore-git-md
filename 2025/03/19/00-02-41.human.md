This is a technical discussion about the incremental MIDX bitmap implementation, specifically addressing how to handle object counts across MIDX layers. The email is part of a larger, nearly-complete patch series (v4, 13 patches) that introduces efficient bitmap operations for multi-pack indexes.

Taylor Blau responds to Jeff King's questions about the `bitmap_non_extended_bits()` helper function, which calculates object counts differently for MIDX vs single-pack cases. The key points are:

1. Some callers need the total object count including base layers (for bitmap sizing), while others only need the count for their specific layer (for bounds checking and position calculations).

2. There's discussion about whether to rename `bitmap_num_objects()` to better indicate it operates on a single layer, though no firm conclusion is reached.

3. Both authors acknowledge the potential for confusion between local-layer and global-pack positions, but consider type-based solutions in C to be potentially more trouble than they're worth.

This exchange represents fine-tuning of an already well-reviewed implementation, with the only open question being about potential naming improvements to clarify layer-specific operations. The technical approach itself appears settled.