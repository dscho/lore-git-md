This is a follow-up discussion in the HTTP TCP keepalive configuration series, specifically addressing patch 3/3 which implements the new config options. The email from Elijah Newren concurs with Patrick Steinhardt's earlier question about whether Git's default TCP keepalive settings should be adjusted for better out-of-the-box behavior in bandwidth-limited scenarios.

The exchange highlights an important consideration for the feature: while the technical implementation of exposing curl's TCP keepalive parameters (keepAliveIdle, keepAliveInterval, keepAliveCount) through Git config is sound, there's now discussion about whether the default values should be more aggressive than curl's defaults to better handle cases like bandwidth-limited clones. This builds on Taylor Blau's original motivation where he could reliably reproduce connection drops during throttled clones.

The thread context shows this is one of the last open questions before the series can be finalized, with the other being minor implementation cleanups in config parsing. The core functionality and architecture have already been agreed upon, and this discussion represents the final polish phase. The fact that multiple contributors are raising the default values question suggests it may warrant consideration for a follow-up patch or adjustment to the current series.