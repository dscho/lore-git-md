Refactoring patch in Martin Ågren's pretty-format parsing series, focusing on padding-related code organization.

This third patch in the 8-part series restructures how padding placeholders ("%<", "%>") are handled in pretty.c by extracting three related fields from `struct format_commit_context` into a new `struct padding_args`. The fields moved are `flush_type`, `truncate`, and `padding`, which have been used exclusively for padding since their introduction in 2013. The change makes the code's intent clearer by grouping padding-specific state together and modifying `parse_padding_placeholder()` to operate on this new struct rather than the full context.

The diff shows mechanical changes updating all references to these fields throughout pretty.c, including in the padding parsing logic and the formatting functions that consume these values. Key modifications include updating `format_and_pad_commit()` to use the nested struct and adjusting the padding state machine transitions. The patch maintains all existing behavior while making the code's organization more logical - a typical refactoring step that often precedes more substantive changes to the affected subsystems.

For future reference in this thread: the changes center around pretty.c's padding handling state (now in `struct padding_args`), with all padding-related functions updated to use the new structure. The enum types `flush_type` and `trunc_type` remain unchanged but are now exclusively used within the padding context. This appears to be groundwork for subsequent patches that will address padding placeholder parsing bugs mentioned in the series overview.