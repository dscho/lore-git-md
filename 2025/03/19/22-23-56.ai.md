Feature and configuration patch for HTTP TCP keepalive settings.

This final patch in the v2 series introduces user-configurable TCP keepalive parameters for HTTP operations via curl. It adds three new configuration options (`http.keepaliveidle`, `http.keepaliveinterval`, `http.keepalivecount`) with corresponding environment variable overrides (`GIT_HTTP_KEEPALIVE_*`), addressing bandwidth-limited clone failures where connections might timeout prematurely.

The implementation maintains backward compatibility by defaulting to curl's built-in values (-1) and includes version checking for the recently added `CURLOPT_TCP_KEEPCNT` option (curl 8.9.0+). The patch follows the series' pattern of robust type handling, using the new `set_long_from_env()` helper introduced in earlier patches. Documentation is comprehensive, clearly explaining each parameter's purpose and relationship to curl's underlying behavior.

The technical implementation adds static variables for each keepalive parameter, updates the HTTP config callback to parse the new options, and modifies `get_curl_handle()` to apply the settings. Environment variable handling leverages the series' centralized parsing infrastructure. The patch maintains the series' focus on code hygiene while delivering the promised user-facing functionality.

This completes the HTTP keepalive configuration series, which has evolved through constructive feedback to produce a polished implementation with clear documentation. The changes are well-contained to http.c and related files, with no expected controversy given the prior discussion and consensus in the thread.