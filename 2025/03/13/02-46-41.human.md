This is the final patch in a 6-part series converting `git am` from merge-recursive to merge-ort, part of the larger effort to remove merge-recursive entirely. The patch switches `git am`'s three-way merge backend to use the new `merge_ort_generic()` compatibility wrapper.

The change requires a few test adjustments:
1. t4151 now removes an untracked file before merging, as merge-ort (unlike merge-recursive) properly detects and aborts on untracked files in the way
2. t4255 can now pass two previously-failing submodule tests
3. t6427 updates the conflict marker label from "constructed merge base" to "constructed fake ancestor" to better match `git am`'s terminology

The implementation is straightforward - just swapping the include and function call while adding the ancestor label that merge-ort requires callers to specify. The test changes demonstrate merge-ort's improved behavior around untracked files and submodule handling compared to merge-recursive.

This completes the `git am` conversion portion of the merge-ort transition, with all tests now passing. The patch appears well-tested and maintains backward compatibility while fixing some long-standing edge cases.