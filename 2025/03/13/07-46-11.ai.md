This is a follow-up discussion in the ongoing NUL-delimited output format series for `git rev-list`. The thread has already established consensus on implementing comprehensive `-z` support affecting both input and output streams with single NUL delimiters and key-value formatting.

Patrick Steinhardt reinforces the design decision to make `-z` affect both stdin and stdout parsing, citing lessons from `git cat-file` where split input/output controls (`-Z` vs `-z`) proved problematic. He argues for consistently using NUL delimiters in both directions to handle edge cases and guide application developers toward robust parsing practices. This aligns with Junio Hamano's observation about the unified `-z` approach being less surprising than separate controls.

The exchange confirms the architectural direction while addressing Jeff King's earlier compatibility questions. No new technical changes are proposed here - this is primarily a design rationale discussion solidifying the v2 approach that has already addressed all major implementation concerns in the series. The thread shows the maintainers converging on a solution that balances consistency with Git's existing NUL-delimited conventions and forward-looking robustness for machine parsing use cases.