This is the final patch in a series adding NUL-delimited output support to `git rev-list`, specifically implementing it for the `--missing` option. The patch follows the established key-value format used elsewhere in the series, where `missing=yes` replaces the leading '?' character in the output.

The implementation modifies `print_missing_object()` to handle both newline and NUL-delimited output, with the NUL-delimited version using the same format as other object metadata (OID followed by NUL-separated key-value pairs). The patch maintains all existing functionality while adding the new output mode, including proper handling of paths with special characters and type information.

Documentation is updated to include `--missing` in the list of options compatible with `-z`, and new tests verify the NUL-delimited output format works correctly with missing objects, including paths containing spaces. The test creates a repository with a file containing a space in its name, then verifies the output format when that object is missing.

This completes the series' goal of providing comprehensive NUL-delimited I/O support across all major `rev-list` modes. The implementation is consistent with the thread's established design decisions and appears ready for merging.