This is a technical discussion in the ongoing thread about Git bundle reproducibility, with Junio Hamano weighing in on Jeff King's analysis. The email focuses on additional challenges in achieving bit-for-bit identical bundles, particularly around zlib compression variations and performance tradeoffs.

Junio points out a fourth major obstacle beyond those Jeff identified: even with identical Git versions, dynamically linked zlib implementations may produce different compressed outputs. He also notes that the performance penalty Jeff mentions (point 3) may be acceptable for users prioritizing reproducibility over speed.

The email includes Jeff's proposed workaround - generating a stable object ordering via `git rev-list` and `git cat-file`, then hashing the result - which Junio acknowledges as "gross but probably stable." This suggests the core team is exploring alternative verification methods rather than trying to force deterministic pack generation.

The discussion continues to balance technical constraints (zlib behavior, threading models) against the security benefits of reproducible builds, with Junio's input reinforcing that true bit-for-bit matching may be impractical while still leaving room for environment-specific verification approaches.