This is a refactoring patch that's part of a larger series adding NUL-delimited output support to `git rev-list`. 

The patch inlines the `show_object_with_name()` function into its only caller, `show_object()`, and consolidates the common OID printing logic between named and unnamed object cases. This cleanup prepares the code for subsequent changes that will add NUL-delimited output support.

The changes are straightforward: the function being inlined was small and had only one caller, making this a sensible simplification. The diff shows the logic being moved from revision.c to builtin/rev-list.c while maintaining the same behavior. The patch is well-contained, touching only the object display logic without changing any functionality.

This appears to be a preparatory cleanup in a well-structured series that has already gone through several rounds of review. The changes are minimal and focused, with the commit message clearly explaining both the immediate change and how it fits into the larger effort.