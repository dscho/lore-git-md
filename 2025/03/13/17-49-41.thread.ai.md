Here is the dense AI thread summary capturing all key information from this initial proposal:

This thread discusses a proposal to add a config option controlling submodule recursion behavior specifically for clone operations. The author suggests either `clone.recursive` or `clone.recurse-submodules` as possible names for a new configuration that would make `git clone` automatically recurse into submodules, equivalent to passing `--recurse-submodules` on the command line. The motivation stems from workflow needs in GUI tools (particularly IDEs) and multi-level submodule hierarchies where users want full recursive cloning by default while maintaining separate control over other submodule operations. The proposal explicitly aims to leave existing submodule command behaviors unchanged, focusing solely on clone. Key open questions include the config naming (whether to use existing `submodule.*` namespace or new `clone.*` prefix), interaction with `submodule.recurse` if present, and implementation approach in `builtin/clone.c`. The author has not yet provided implementation details, leaving these design decisions for discussion. The use case centers on consistency between CLI and GUI tools and simplifying workflows where recursive cloning is always desired. No reviewers have commented yet on the proposal's technical merits or alternative approaches.