Refactoring patch in the NUL-delimited output series. This is the second patch in v2 of the series, focusing on cleaning up option parsing in `rev-list` before the main feature implementation.

The patch consolidates the early parsing of `--missing` and `--exclude-promisor-objects` options into a single pass, replacing two separate loops with one unified check. It introduces a new helper `die_for_incompatible_opt2()` to handle the mutual exclusion check between these options, making the code more maintainable.

This is a mechanical change that prepares the codebase for the subsequent patches that will add NUL-delimited output support. The change is straightforward and well-contained, touching only `builtin/rev-list.c` with a net reduction of 3 lines. The patch maintains all existing functionality while making the code more robust for future additions.

As part of the larger series that has already reached consensus on design, this refactoring appears uncontroversial and aligns with the project's code quality standards. The commit message clearly explains the motivation (preparing for a new option) and the change's scope.