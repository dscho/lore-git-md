This is part of the ongoing effort to deprecate and remove the `merge-recursive` backend in favor of `merge-ort`. 

Elijah Newren asks Junio to hold off on merging the current version of the `en/merge-ort-prepare-to-remove-recursive` series to 'next', as Patrick Steinhardt and Taylor Blau have requested some changes. A v2 of the patch series is imminent.

The series (currently 3 commits) represents the first steps in removing `merge-recursive`, adding new capabilities to `merge-ort` including verbosity control, rename detection toggling, and a new `merge_ort_generic()` function. The changes appear well-motivated as part of the strategic move to make `merge-ort` the default merge strategy, but the authors want to incorporate feedback before proceeding.

This is a routine request to delay merging pending minor revisions - the core direction of the work seems uncontroversial and aligned with Git's roadmap. The v2 submission will be worth watching for those tracking the merge strategy transition.