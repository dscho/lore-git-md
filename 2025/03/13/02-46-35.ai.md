This is a cover letter for a 6-patch series that's part of the larger effort to remove merge-recursive in favor of merge-ort. The series introduces new features needed for converting callers, fixes a directory rename detection bug, and demonstrates the conversion (git am).

The series builds on prior discussion about API compatibility layers, with key technical points now addressed:
1. The new `merge_ort_generic()` wrapper properly handles ancestor commit labeling differences between callers (git am vs merge-recursive vs stash), fixing a long-standing quirk
2. Rename detection disablement now links to the original public feature request and uses cleaner `!opt->detect_renames` check
3. Verbosity control was relocated to the merge-ort-wrappers layer to minimize exposure of legacy mechanisms
4. Directory rename detection fix (merge.directoryRenames=false) is now properly handled, addressing a bug that affected git am

Notable implementation details for the AI context:
- Adds 72 lines to merge-ort-wrappers.c with new API functions
- Modifies merge-ort.c's rename detection and verbosity handling
- Includes test cases in t4301-merge-tree-write-tree.sh for rename disablement
- Converts builtin/am.c to use the new API in patch 6
- Documents behavioral differences in merge-strategies.adoc

The series shows careful attention to maintaining compatibility while cleaning up technical debt, with all prior review feedback incorporated. The git am conversion in patch 6 serves as a concrete example of how callers will transition to the new API. Test coverage appears thorough, leveraging existing tests where possible while adding new ones for modified behaviors.