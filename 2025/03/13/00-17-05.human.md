This is a feature patch extending the NUL-delimited output support in `git rev-list` to include boundary objects. It's part of a well-developed series (now at patch 5/6) that has already addressed all major design questions and received positive feedback from maintainers.

The patch modifies the `--boundary` output format when `-z` is used, changing from prefixing boundary OIDs with '-' to using a key-value pair `boundary=yes` after the OID. This maintains consistency with the series' approach of using NUL-delimited key-value pairs for metadata. The implementation includes documentation updates, core logic changes in `show_commit()`, and a new test case verifying the behavior.

The changes are cleanly integrated with the existing NUL-delimited output infrastructure, including proper handling of the `line_term` and `info_term` variables introduced earlier in the series. The patch also maintains all existing validation, ensuring `-z` isn't used with incompatible options like `--graph` or `--left-right`.

This appears to be a straightforward extension of the v2 design that has already reached consensus, with clear documentation and test coverage. The implementation looks solid and consistent with the rest of the series, which seems ready for final review and merging.