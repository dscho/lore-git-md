This is a follow-up in the thread investigating non-reproducible Git bundle generation, focusing on security implications and practical reproducibility constraints.

Simon Josefsson confirms the reproducibility issues occur during `git clone` operations rather than within a single repository, corroborating Kyle Lippincott's earlier findings about threading effects on pack file generation. He shares additional unsuccessful attempts to achieve deterministic clones using the dumb protocol and rsync, suggesting these approaches still don't produce identical object ordering.

The email clarifies the security motivation behind pursuing bit-for-bit reproducibility, drawing parallels to established reproducible build efforts for tarballs and gzip. Simon acknowledges that environment-specific factors (Git versions, zlib implementations) would still affect reproducibility, but argues this is an acceptable constraint similar to how binary builds require controlled toolchains. This positions the discussion beyond technical causes toward practical security tradeoffs, suggesting that partial reproducibility (same-environment verification) would still represent progress over the complete non-reproducibility currently observed.

Key technical points for future reference:
- Confirmed failure modes: `git clone` introduces non-determinism that persists in bundles
- Tested alternatives: dumb protocol (`GIT_SMART_HTTP=0`) and rsync clones still vary
- Security context: draws analogy to GNU tar/gzip reproducibility efforts
- Acceptable constraints: environment-specific reproducibility seen as valid intermediate goal
- Open questions:
  * Whether clone-time synchronization could enforce object ordering
  * How to document/specify required environments for verification
  * Security value of partial (same-environment) reproducibility

The email moves the discussion from technical diagnosis toward solution evaluation, emphasizing the security use case while acknowledging practical limitations. The Gnulib bundle verification scenario remains the concrete driver for these reproducibility requirements.