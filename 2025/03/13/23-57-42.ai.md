Refactoring patch in the rev-list NUL-delimited output series (1/6). This preparatory change inlines the single-use `show_object_with_name()` function into `show_object()` and consolidates the OID printing logic that was previously duplicated between named and unnamed object cases. The modification touches three files (builtin/rev-list.c, revision.c, revision.h) by removing the standalone function and its declaration while restructuring the output generation to use a shared OID print statement followed by conditional name handling. This cleanup precedes the main NUL-delimited output implementation in subsequent patches.

Key technical details:
- Removes `show_object_with_name()` function and its header declaration
- Combines OID printing into single `printf()` for both named/unnamed cases
- Maintains same output format (space-delimited OID+name or just OID)
- Preserves newline handling for path names containing newlines
- Part of broader effort to standardize rev-list output formatting
- No behavior change intended - pure refactoring
- Sets stage for NUL-delimited output support in follow-up patches
- Touches core revision walking infrastructure (revision.[ch])
- Follows established pattern of inlining single-use functions

The change appears straightforward and well-contained, with clear motivation stated in the commit message. As the first patch in a technically sound series that's already reached v3, this refactoring has likely been reviewed and appears uncontroversial.