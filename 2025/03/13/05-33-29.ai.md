This is a discussion about the final design details of the NUL-delimited I/O implementation in `git rev-list`, specifically regarding how the `-z` option should interact with stdin parsing and diff output.

Jeff King (Peff) responds to Justin Tobler's explanation about the current implementation where `-z` is parsed early but doesn't affect diff options processing. Peff acknowledges this approach makes sense given the current architecture, though he notes it creates an inconsistency between `git log --stdin -z` and `git rev-list --stdin -z` behavior.

The key technical point is that while the series implements NUL-delimited stdin parsing for `rev-list`, other commands like `log` with `--raw -z` currently only use `-z` for output formatting. Peff raises but ultimately accepts that this inconsistency may be unavoidable given Git's existing behavior patterns, suggesting the team may have addressed this architectural question years ago when designing other `-z` implementations.

This email represents the final resolution of design questions in the series, with Peff (who had previously raised interface concerns) now satisfied with the v2 approach. The thread has effectively reached consensus on all technical points, with this exchange confirming the remaining behavioral edge cases are inherent to Git's existing architecture rather than problems with the current series.