This is a technical clarification in the ongoing boolean syntax standardization documentation effort.

Junio Hamano responds to Taylor Blau's observation about boolean value normalization behavior in Git's configuration system. The discussion centers on whether the documentation should describe the input acceptance policy separately from the output normalization behavior.

Junio provides concrete test cases demonstrating that various truthy inputs ("yes", "1", "2", "on") all get normalized to "true" when retrieved via `git config --type=bool`. This confirms the current implementation behavior but raises the question of whether this normalization should be explicitly documented as part of the canonicalization process.

The exchange reveals an important distinction between:
1. What values Git accepts as boolean inputs (the original focus of the documentation patch)
2. How those values get normalized when read back (which may need separate documentation)

This technical nuance affects how the final documentation should be worded to accurately reflect both the input acceptance policy and the output normalization behavior. The thread shows the patch is nearing consensus but requires this behavioral clarification before finalizing the wording.