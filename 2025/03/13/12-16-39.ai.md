This is a discussion about cruft pack size thresholds, continuing the ongoing thread about Taylor Blau's implementation. The email focuses on Junio Hamano and Elijah Newren debating the interaction between two proposed size parameters for cruft packs: `--max-pack-size` (a hard limit) and `--combine-cruft-below-size` (a soft threshold for combining packs).

Junio points out a scenario where the two thresholds could create an infinite loop: if existing cruft packs are just below the soft threshold (170MB with 180MB threshold) but the next available cruft is too large to fit under the hard limit (40MB would exceed the 200MB max), the repack operation would repeatedly attempt and fail to combine packs. This directly relates to Taylor's original goal of preventing inefficient repacking attempts.

The discussion reveals two key perspectives:
1. Elijah maintains that the soft threshold is merely advisory and not a constraint, useful for initial cruft accumulation
2. Junio argues this doesn't solve the underlying efficiency problem Taylor's series aimed to fix

The exchange shows the conceptual tension between:
- Strict size enforcement (`--max-pack-size`)
- Combination efficiency heuristics (`--combine-cruft-below-size`)
- The original problem of avoiding fruitless repack attempts

This is a late-stage thread discussion that doesn't propose code changes but explores the mathematical and conceptual implications of the dual-threshold approach. It follows Jeff King's earlier intervention questioning whether size limits are the right model altogether, though here the focus is specifically on the interaction between the two proposed parameters.