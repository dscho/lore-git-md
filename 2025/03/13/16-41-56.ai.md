This is a follow-up discussion in the NUL-delimited output format feature series, specifically addressing command behavior consistency concerns raised by Jeff King. The email focuses on the interaction between `-z` flag handling in `git rev-list` versus other commands like `git log`.

Key technical points:
- The v2 implementation uses `nul_delim_stdin` in `setup_revision_opt` to control stdin parsing, keeping the behavior isolated to `git rev-list`
- Justin Tobler acknowledges the current inconsistency where `git log -z --stdin` and `git rev-list -z --stdin` would behave differently
- The discussion reveals a deeper inconsistency in Git's existing handling of `-z` across commands, with some using it only for output while others (like grep) use it for both input and output

The email suggests two perspectives:
1. Practical implementation: The current approach maintains backward compatibility while adding the new feature
2. Philosophical debate: Whether Git should standardize on `-z` affecting both input and output for machine parsing contexts

The thread context shows this is the final open discussion point in an otherwise resolved series, with all technical implementation questions already addressed in v2. The remaining question is about broader Git command consistency rather than specific implementation concerns.