This is a documentation refinement in the NUL-delimited output format series for `git rev-list`. Justin Tobler responds to Patrick Steinhardt's suggestion about improving the structure of the documentation for the `-z` option.

The change moves the description of `--stdin` behavior to appear earlier in the documentation, making it more prominent alongside the initial description of the NUL-delimited format. This addresses Patrick's feedback that the stdin behavior should be mentioned up front rather than at the end of the section.

The patch is part of the v2 series implementing comprehensive NUL-delimited I/O support in `git rev-list`, which has already resolved all major technical questions. This particular change is a minor documentation polish that doesn't affect the implementation, maintaining all the existing functionality including:
- Single NUL delimiters with key-value format ("path=<path>")
- Compatibility with `--objects` output
- Special character handling in paths
- The `nul_delim_stdin` flag behavior

The thread context shows consensus on the overall approach, with this being one of the final refinements before potential merging. The change touches only Documentation/rev-list-options.adoc, keeping the same technical content but improving its organization.