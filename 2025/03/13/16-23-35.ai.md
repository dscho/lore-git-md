This is a technical discussion email in the ongoing cruft pack implementation thread, focusing on the interaction between size thresholds for cruft pack combination.

Elijah Newren responds to Junio Hamano's concerns about the proposed dual-threshold approach for cruft pack combination. The discussion centers around two key thresholds: `--max-pack-size` (a hard constraint) and `--combine-cruft-below-size` (a soft guideline). Newren clarifies that the latter is meant as a heuristic for when to attempt combining cruft packs, not as a strict limit.

The core disagreement revolves around whether the dual-threshold approach actually solves the problem of repeated fruitless repack attempts. Newren argues that even if some rounds of repacking can't combine packs (when the next available object would exceed `--max-pack-size`), future repacks may still make progress when smaller objects become available. He suggests setting `--combine-cruft-below-size` to half of `--max-pack-size` as a reasonable default that would allow consistent progress in combining packs.

This email represents a refinement of the earlier discussion about cruft pack size management, now focusing specifically on the practical implications of threshold settings rather than the fundamental mechanism (which the thread has established is properly handled by `want_cruft_object_mtime()`). The discussion remains technical but has shifted from conceptual validation to implementation tuning.