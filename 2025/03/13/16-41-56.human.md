This is a follow-up discussion in the NUL-delimited output format thread, focusing on command behavior consistency. Justin Tobler responds to Jeff King's concerns about potential inconsistencies in how `-z` affects stdin parsing across different Git commands.

The key point is that `git rev-list --stdin -z` will now parse stdin with NUL delimiters (via the new `nul_delim_stdin` flag in `setup_revision_opt`), while `git log --stdin -z` maintains its current behavior of only affecting output formatting. Justin acknowledges this inconsistency but argues that having `-z` affect both input and output in `rev-list` makes more sense for machine parsing, aligning with Patrick Steinhardt's earlier reasoning.

The discussion reveals an underlying tension between maintaining consistency with existing Git command behavior versus establishing more logical patterns for new features. While the technical implementation is settled (with the v2 patches already addressing all major concerns), this exchange surfaces a philosophical question about when to preserve historical behavior versus when to establish new conventions.

The thread appears to be winding down, with Justin open to further discussion but no immediate changes proposed to the already-reviewed patch series. This seems to be a final clarification of design rationale rather than a blocker to merging.