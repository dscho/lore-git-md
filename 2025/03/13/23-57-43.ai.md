This is a refactoring patch in a series adding NUL-delimited output to `git rev-list`. It's the second of six patches in v3 of the series.

The change consolidates early option handling for `--missing` and `--exclude-promisor-objects` in `builtin/rev-list.c` into a single pass through the arguments. Previously these were handled in separate loops. The refactoring prepares for adding another early-parsed option in a subsequent patch (likely the NUL-delimiter flag).

Key technical details:
- Combines two argument parsing loops into one
- Uses `die_for_incompatible_opt2()` helper to check option conflicts
- Maintains same behavior but with cleaner control flow
- Touches only `builtin/rev-list.c`
- Follows the pattern of the series by doing preparatory refactoring before adding new functionality

The patch appears straightforward and well-contained, with the series context showing this is part of a larger effort that has already received significant review. The change is purely structural with no behavior modification, making it low-risk.