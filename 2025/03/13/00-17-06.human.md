This is a feature patch extending NUL-delimited output support in `git rev-list` to cover the `--missing` option, completing the v2 series' goal of comprehensive machine-parseable I/O. The patch modifies the output format for missing objects to match the key-value style already established for `--objects` and `--boundary`, replacing the leading '?' marker with a `missing=yes` attribute and using NUL delimiters when `-z` is enabled.

The implementation maintains consistency with the series' design principles: OIDs are never escaped, special characters in paths are passed through unmodified, and incompatible options (like `--graph`) are rejected. Documentation and tests are updated to reflect the new format, including a new test case verifying NUL-delimited output with paths containing spaces.

This appears to be the final patch in the series, tying up loose ends after the core infrastructure was established in earlier patches. The changes are well-contained, with clear commit messages and thorough test coverage. Given the resolved design discussions in previous thread iterations, this seems ready for final review.