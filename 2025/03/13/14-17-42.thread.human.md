This thread addresses a Windows-specific race condition in file handling that was causing flakiness in test t0610, particularly affecting the reftable library's lockfile operations. The discussion has progressed through two versions of a patch series, with v2 representing a significant refinement.

The current state focuses on two key changes: First, a Meson build system fix ensures proper inclusion of Windows compatibility sources, correctly distinguishing between MSVC and MinGW toolchains. This straightforward 8-line change aligns Meson with the existing Makefile behavior. Second, and more substantially, the series now uses NtStatus code checking to precisely detect when files are marked for deletion, replacing the simpler error code translation from v1. This 15-line change in mingw.c better preserves legitimate permission errors while specifically handling the delete-pending case that was causing test flakiness.

Patrick Steinhardt, the author, has confirmed the v2 approach resolves the test issues while addressing Johannes Schindelin's earlier concerns about error masking. The series has dropped a previously approved O_CLOEXEC change to stay focused on the core race condition. Discussion now centers on whether the NtStatus checking provides sufficient precision across all error scenarios and what performance impact the additional system calls might have. Johannes Schindelin's review of the refined approach is pending, and the series may need documentation of the Windows-specific behavior before final approval. The changes are relatively small but important for Windows reliability, particularly for reftable operations.