This is a follow-up in the thread about bash/zsh completion fixes for remote names containing slashes. D. Ben Knoble provides verification of the patch's behavior in zsh, confirming it produces the expected completion results for both slashed remote names and regular refs. 

The email includes specific test results showing correct completion behavior in zsh for commands like `git push with/slash :ma<Tab>` (completing to 'main') and `git push with/slash <Tab>` (showing HEAD, main, and with/slash/main). Knoble details their testing methodology, which involved applying the patch to their local zsh completion setup and verifying against David Mandelberg's reproduction steps.

Key technical details confirmed:
- The completion script now properly handles remote names containing slashes
- Both zsh and bash implementations appear consistent in behavior
- The fix works for both the abbreviated (`:ma<Tab>`) and full completion cases

This provides practical confirmation that the patch series successfully addresses the original issue while maintaining expected behavior for standard remote names. The exchange serves as final verification before potential integration, with no new issues identified in the testing.