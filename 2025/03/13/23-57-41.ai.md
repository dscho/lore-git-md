Feature enhancement for `git rev-list` adding comprehensive NUL-delimited I/O support, now at v3 with documentation refinements. This series from Justin Tobler builds on prior discussion (v2 summarized in thread context) to implement machine-readable output and input handling for `rev-list`.

The core change introduces a `-z` flag that makes all output use NUL-delimited `<token>=<value>` pairs instead of newlines, handling special characters in paths and metadata reliably. The format unifies output across `--objects`, `--boundary`, and `--missing` modes while also making `--stdin` input NUL-delimited when `-z` is active. Key implementation details include the `print_object_term()` helper, `nul_delim_stdin` flag, and careful handling of metadata tokens like "boundary=yes" and "missing=yes".

Changes since v2 focus on documentation clarity - moving the `--stdin` behavior note earlier in the docs and minor code style tweaks in the `--missing` output handling. The series remains structured as 6 patches: initial refactors (1-2), stdin support (3), core `-z` implementation (4), then boundary (5) and missing (6) option integrations. Test coverage appears thorough with new cases in t6000, t6017, and t6022.

The thread context shows all technical concerns from v2 were addressed, leaving only the broader philosophical question about `-z` behavior consistency across Git commands (input+output vs output-only). This appears ready for merging with consensus on the implementation, though the stdin behavior may prompt future standardization work.