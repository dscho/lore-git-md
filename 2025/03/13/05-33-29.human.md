This is a discussion about NUL-delimited input/output handling in `git rev-list`, part of an ongoing thread about implementing comprehensive `-z` support.

Jeff King (Peff) responds to Justin Tobler's explanation about how `-z` parsing currently works in the `rev-list` implementation. The key points are:

1. Peff acknowledges that the current approach of early `-z` parsing (before `setup_revisions()`) makes sense given how stdin argument handling works, though it means `-z` doesn't affect diff options parsing.

2. He raises an interesting inconsistency: `git log --stdin -z` and `git rev-list --stdin -z` would behave differently under this implementation, since `log`'s `-z` only affects output while `rev-list`'s would affect input parsing.

3. Peff reflects that while he initially suggested tying `-z` to stdin handling (for consistency with tools like `grep`), Git may have already established a precedent where `-z` only affects output in some commands. He concludes this might be an existing inconsistency we have to live with.

This email doesn't propose any code changes but highlights an important design consideration about command behavior consistency across Git. The discussion appears to be wrapping up, with Peff essentially saying "this might not be ideal, but it's probably the least bad option given existing behavior."