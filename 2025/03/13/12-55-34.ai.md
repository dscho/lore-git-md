This is a documentation refinement suggestion in the ongoing NUL-delimited output format series for `git rev-list`. Patrick Steinhardt proposes improving the documentation structure for the new `-z` option by moving the mention of `--stdin` behavior earlier in the description.

The patch being discussed (v2 4/6) implements NUL-delimited output formatting for `rev-list`, using a key-value format with single NUL delimiters (`<OID> NUL [<token>=<value> NUL]...`). The current documentation places the `--stdin` behavior note at the end, which Patrick suggests should be mentioned upfront since it's a core aspect of the `-z` functionality.

This is a minor but thoughtful improvement to the series, which has otherwise reached consensus on its technical approach. The suggestion aligns with the thread's established direction of making NUL-delimited I/O behavior clear and consistent throughout the documentation. No technical changes are raised - this is purely about improving the user-facing explanation of already-agreed functionality.