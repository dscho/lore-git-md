Bugfix refinement in the promisor-remote subsystem, addressing feedback on the NULL pointer dereference fix. This is version 4 of the patch (though not explicitly labeled as such in the email), responding to Junio Hamano's review of v3.

The core of this iteration focuses on two architectural questions raised in review: whether promisor remotes should follow standard Git remote conventions (using the remote name as a fallback when no URL is configured), and whether empty URL warnings are providing meaningful signal versus noise. Christian Couder defends the current approach, arguing that name fallback makes sense when client, server, and promisor remote share a filesystem, and that empty URL warnings help diagnose configuration issues.

Key changes from v3 to this version (implied by the email but not shown in diff):
1. Removed redundant comment about empty URL handling
2. Changed string comparisons to use `strcasecmp()` for consistency with `remote_nick_find()`
3. Maintained warning system for empty/mismatched URLs as a debugging aid

The thread shows consensus that the immediate NULL dereference fix is sound and ready for inclusion, while leaving open larger questions about promisor remote design philosophy. Christian's responses suggest these may need resolution in future work, particularly around whether promisor behavior should diverge from standard remote conventions when names are meaningful only locally.

This iteration appears to be primarily about responding to review feedback rather than introducing new functionality. The patch remains focused on preventing crashes while the community considers deeper architectural implications.