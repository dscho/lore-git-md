This is a technical discussion in the promisor-remote bugfix thread, where Junio Hamano raises deeper architectural concerns about Christian Couder's proposed solution for handling missing remote URLs. The exchange focuses on whether falling back to remote names (as `git fetch` does) makes sense for promisor remotes, and whether empty URL warnings are over-engineered.

Junio questions the practical validity of Christian's filesystem-sharing use case, suggesting it would require all clients to share the same working directory. He also points out that case-insensitive name comparisons (inherited from `remote_nick_find()`) might prevent having similarly-named remotes differing only in case. The discussion reveals fundamental tensions between promisor-specific needs and general remote handling conventions.

The thread has progressed through four versions of the patch, with the immediate NULL dereference fix now stabilized but these broader design questions remaining open. Junio's latest comments suggest the warning system may be redundant and that promisor remotes might need different handling than standard remotes. While the safety fix appears ready for inclusion, this exchange surfaces important considerations for future API refinements.