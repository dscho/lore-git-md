This is a follow-up discussion in the NUL-delimited rev-list output feature thread, specifically addressing final documentation and code organization nits in the implementation.

Justin Tobler responds to Patrick Steinhardt's review comments on the v2 series. The exchange focuses on two minor refinements: First, they agree to omit a documentation change about metadata tokens since the existing text was intentionally non-exhaustive. Second, Justin accepts Patrick's suggestion to move a variable declaration in `print_missing_object()` closer to its usage point for better code organization.

The thread context shows this is the final polish for a feature that has already gone through substantial review and implementation work. All major technical decisions about the NUL-delimited output format (including handling of missing/boundary objects and stdin behavior) were resolved in earlier iterations. This exchange represents the last remaining cleanups before the series can be merged.

Key technical details referenced include the `print_missing_object()` function's handling of `line_term` and `info_term` variables, and the documentation structure in rev-list-options.adoc that describes the NUL-delimited output format's key-value pairs for metadata. The changes discussed here are purely about presentation and organization, not functionality.