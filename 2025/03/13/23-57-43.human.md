This is a refactoring patch in a series adding NUL-delimited output support to `git rev-list`. It cleans up the early option parsing logic for `--missing` and `--exclude-promisor-objects` to prepare for adding a new early-parsed option in a subsequent patch.

The change consolidates what were previously two passes over the arguments into a single pass, making the code more straightforward. It also properly checks for incompatible options (using the standard `die_for_incompatible_opt2()` helper) rather than having the check spread across the parsing logic.

This is a straightforward preparatory change that doesn't modify behavior - it just reorganizes the code to be more maintainable as the series adds new functionality. The patch is small (17 lines changed) and focused, with the commit message clearly explaining both the current state and why the refactoring is needed.

As part of the larger series (now at v3), this patch helps enable the addition of NUL-delimited I/O support while keeping the code clean. The series as a whole has been through several rounds of review and appears ready for merging.