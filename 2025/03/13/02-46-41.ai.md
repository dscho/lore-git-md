This is the final patch in Elijah Newren's series converting git-am from merge-recursive to merge-ort, completing the behavioral parity demonstration. The patch makes the actual switch in builtin/am.c while addressing several test implications:

1. The core change replaces `merge_recursive_generic()` with `merge_ort_generic()` and adds explicit ancestor labeling ("constructed fake ancestor") that was previously handled internally by merge-recursive. This matches git-apply's terminology more closely.

2. Three test adjustments are required:
   - t4151: Removes an untracked file preemptively since merge-ort properly detects and aborts on such conflicts (unlike merge-recursive which would create confusing ~branch1 files)
   - t4255: Removes a KNOWN_FAILURE marker as merge-ort correctly handles submodule cases
   - t6427: Updates expected conflict marker text to reflect the new ancestor label

The changes are minimal (5 lines net) but demonstrate merge-ort's improved handling of edge cases while maintaining compatibility. The series has now fully validated merge-ort as a drop-in replacement for merge-recursive in git-am, with all identified test failures either fixed or properly explained by merge-ort's stricter conflict detection.