This is the third patch in a series improving Meson build system handling of breaking changes, specifically addressing documentation installation for the `git-pack-redundant` command. The patch completes the series' goal of aligning Meson behavior with the Makefile build system for breaking change scenarios.

The change introduces a new `manpages_breaking_changes` dictionary in meson.build to cleanly separate conditionally-included manpages, specifically moving `git-pack-redundant.adoc` into this new structure. The documentation will now only be installed when breaking changes are disabled, matching the binary's compilation behavior. The patch also updates the lint-docs-meson check to properly handle both manpage sets and adds `git-pack-redundant.adoc` to the exclusion list in the Makefile's manpage comparison logic.

Key technical details include:
- New `manpages_breaking_changes` dictionary for conditional documentation
- Updated manpage sanity checks to handle both manpage sets
- Makefile/meson.build alignment in lint-docs-meson check
- Proper integration with existing `breaking_changes` option logic

The implementation maintains consistency with the series' earlier changes while completing the documentation aspect. The patch builds on Karthik Nayak's prior work, with Patrick Steinhardt now finalizing the documentation handling. The changes are well-contained and properly motivated by the thread's goal of build system alignment, with all modifications focused specifically on the manpage installation logic.