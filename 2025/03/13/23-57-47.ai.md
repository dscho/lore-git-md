Feature implementation completing NUL-delimited output support in `git rev-list`. This is the final patch (6/6) in Justin Tobler's series, adding `-z` support for the `--missing` option to match the NUL-delimited formats already implemented for `--objects` and `--boundary`.

The patch modifies `print_missing_object()` to output missing objects in a key-value format when `-z` is enabled, replacing the human-readable `?<oid>` prefix with a `missing=yes` metadata token. Paths and type information are included unquoted in the NUL-delimited stream, differing from the space-quoted format used in the non-`-z` case. The implementation maintains the same information while making it reliably parseable.

Key technical changes:
- Adds new output format: `<OID> NUL missing=yes NUL [path=<path> NUL] [type=<type> NUL]`
- Updates option validation to allow `-z` with `--missing` while still blocking incompatible combinations
- Includes comprehensive test case verifying handling of paths with spaces and special characters
- Documents the new format in rev-list-options.adoc alongside the existing NUL-delimited formats

The patch follows the series' established pattern of using NUL-delimited key-value pairs for metadata, here applied to missing object reporting. It completes the NUL-delimited I/O implementation by covering the last remaining output mode (`--missing`) that hadn't yet been adapted to the `-z` format.

The implementation appears thorough, with careful handling of both quoted (non-`-z`) and raw (NUL-delimited) output modes. Test coverage verifies correct behavior with problematic paths and includes all metadata fields. Documentation is updated to reflect the new format alongside the other NUL-delimited cases.

This concludes the technical implementation of the feature, with all aspects of NUL-delimited output now supported across `rev-list`'s various modes. The only remaining discussion point from the thread is the broader philosophical question about standardizing `-z` behavior across Git commands, which doesn't affect this specific implementation.