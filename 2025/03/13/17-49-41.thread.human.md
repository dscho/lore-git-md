Here's a human-readable summary of the thread so far:

The thread discusses a proposal to add a configuration option that would make `git clone` automatically recurse into submodules, similar to using the `--recurse-submodules` command-line flag. The author notes that while there are existing config options for submodule behavior in other operations (like checkout), clone currently lacks this capability. The motivation comes from wanting consistent behavior between command-line and GUI tools, particularly in IDEs that already clone submodules automatically, and for workflows involving multi-level submodule hierarchies where users always want full recursion during clone.

Two possible config names are suggested (`clone.recursive` or `clone.recurse-submodules`), though the proposal doesn't yet include implementation details. The author emphasizes they don't want to change behavior for other commands, just add this clone-specific option. Key questions that remain open include the exact config name and namespace, how it should interact with existing submodule.recurse config, and the implementation approach in builtin/clone.c. The proposal is motivated by practical workflow needs but awaits technical details for proper review.