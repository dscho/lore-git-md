This is a technical discussion about optimizing error reporting in the partial reference transaction implementation.

Karthik Nayak responds to Phillip Wood's concern about the O(n) cost of iterating through all reference updates to find rejected ones when most updates succeed. Phillip had suggested storing errors in a separate list of (update-index, error) pairs to make error checking O(number of errors) rather than O(number of updates).

Karthik agrees this is a valid performance consideration and proposes to add an array inside the `ref_transaction` struct specifically to track rejected updates, which would address the O(n) iteration concern while maintaining the current API design where most users don't need to deal with partial transaction complexity unless they opt in.

This exchange represents a refinement of the error reporting mechanism in the partial transaction system, building on the already-established design consensus from earlier in the thread. The change would optimize the implementation while preserving the separation between simple and advanced use cases that Karthik had advocated for in previous messages.

Key technical points:
- Current implementation requires checking all updates to find rejections
- Phillip's suggestion would track only failed updates separately
- Karthik agrees to implement this optimization
- Change maintains the API design philosophy of keeping simple case simple
- Performance improvement for the error reporting path
- No change to the fundamental transaction behavior or user-facing interfaces

This discussion shows the series nearing completion with attention now turning to implementation optimizations rather than fundamental design questions. The thread has progressed from initial design proposals through multiple rounds of review to this final tuning of the error reporting performance characteristics.