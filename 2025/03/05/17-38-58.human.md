Refactoring patch in the files ref backend, part of a series on partially atomic ref updates. This patch removes a redundant duplicate-check in the transaction finish phase, since the same check already happens during prepare. The change makes the code more efficient by eliminating unnecessary work while maintaining the same safety checks.

The patch moves the `ref_update_reject_duplicates()` function from being a public API in refs-internal.h to being static in refs.c, as it's no longer needed outside that file. The diff shows clean removal of the duplicate check in files-backend.c and the corresponding header change.

This appears to be a straightforward cleanup with clear justification - the commit message explains why the check is redundant (the refnames list doesn't change between prepare and finish phases). The change is part of a larger series (3/8) but stands well on its own as a simple optimization.