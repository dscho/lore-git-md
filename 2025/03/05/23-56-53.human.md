This is a response in the ongoing discussion about language-specific `.gitignore` support in `git init`. Junio C Hamano clarifies his earlier comment about the `--template` mechanism, emphasizing that it was primarily designed for `.git/hooks` and `.git/info/` files, not as a general file-copying system. He specifically notes that `.git/info/excludes` was the relevant file he had in mind, not arbitrary `.gitignore` files in the working tree.

The email serves as a gentle pushback against Oleg Taranenko's proposed `--only-gitignores` flag, reinforcing the maintainer's position that Git's template system shouldn't be extended to handle working tree files like `.gitignore`. While not outright rejecting the idea, Hamano's response suggests the proposal may need to better align with Git's existing template philosophy to gain traction. The thread continues to circle around the core tension between user convenience and maintaining Git's language-agnostic design principles.