This is a documentation clarification in the ongoing thread about subject prefix marking for mentoring programs.

Junio confirms Lucas's proposal to update the General Microproject Information documentation rather than implementing new code changes. The solution will recommend using a combined prefix format like "[GSoC PATCH]" instead of the current double-bracket style "[GSoC][PATCH]". This approach leverages Git's existing `--subject-prefix` functionality without requiring any code modifications to `git format-patch`.

The email represents final agreement on the maintainer-approved direction, marking the technical discussion as effectively resolved. All that remains is updating the documentation to reflect this convention. Junio notes this single-bracket style is already a common practice in the wider ecosystem, making it a natural choice for Git's mentoring program participants.

Key technical points settled:
- No new flags needed (`--subject-extra-prefix` and `--patch-prefix` both rejected)
- Existing `--subject-prefix` flag can handle the combined format
- Documentation will be the sole change vector
- Semantic distinction maintained between program identifiers (persistent) and RFC markers (temporary)

The thread has progressed from a 6-patch implementation series to this minimal documentation solution that satisfies all original requirements while keeping Git's interface simpler. The change affects only the General Microproject Information guide, with no code changes to `builtin/log.c` or test updates needed.