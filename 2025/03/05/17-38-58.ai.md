Refactoring patch in the files ref backend, part of a series on partially atomic ref updates (3/8). This patch removes a redundant duplicate refname check in the files backend's transaction finish phase, since an identical check already occurs during prepare. The `ref_update_reject_duplicates()` function is made static as it's now only used in refs.c.

The change is straightforward - removing the duplicate check from files-backend.c while keeping the core implementation in refs.c. The commit message clearly explains the redundancy and why the removal is safe (refnames list remains unchanged between prepare and finish). The patch touches three files but makes clean, focused changes - removing the check from files-backend.c, making the function static in refs.c, and removing its declaration from refs-internal.h.

This appears to be a well-justified cleanup with no behavior change. The patch is part of a larger series (v3) suggesting it's been reviewed before. The commit message could perhaps note whether there were any performance measurements showing the duplicate check was a bottleneck, but the rationale for removal is sound regardless.