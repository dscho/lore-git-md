Refactoring patch in a series about partially atomic ref updates (1/8). This removes redundant checks in the files ref backend's update splitting logic.

The patch simplifies `split_symref_update()` and related functions by removing unnecessary duplicate checks when tracking affected refnames. The code previously had two safety checks: one before insertion (using `string_list_has_string()`) and one after (verifying the new item's `util` field was NULL). The author notes the first check makes the second redundant, since it guarantees no duplicates exist when inserting.

The changes touch three functions in `refs/files-backend.c` that handle ref updates, removing the `util` field manipulation entirely since it was only used for this redundant verification. The diff shows 17 lines removed versus 3 added, with straightforward mechanical changes to the update tracking logic.

This appears to be a clean, well-justified simplification - the commit message clearly explains the redundancy and the BUG checks give confidence the invariants hold. As the first patch in a series about atomic ref updates, it seems to be laying groundwork by cleaning up related infrastructure. The changes are internal to the refs backend and shouldn't affect user-visible behavior.