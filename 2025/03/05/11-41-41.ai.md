This is a response in the ongoing discussion about refactoring Git's global state, specifically regarding Ayush Chandekar's GSOC project inquiry. The email from Jialuo provides feedback on Ayush's proposed approach to removing global state dependencies, particularly around `have_git_dir()` and `local_repo_env[]`.

Jialuo cautions against removing `have_git_dir()` entirely, noting it's tied to `startup_info` removal which is non-trivial. For `local_repo_env[]`, they defer to Patrick Steinhardt's expertise. The response clarifies that the goal isn't to move config settings like `core.*` directly into repo settings, but rather to migrate the global variables that store these parsed values. 

While encouraging Ayush's direction with `core.attributesfile`, Jialuo notes this won't be a small change and suggests focusing on simpler global states first to build understanding. They recommend following call stacks to identify affected functions rather than trying to predict all relevant files upfront. The email confirms Patrick's prior patches ([1], [2]) do represent good examples of the project approach.

Key technical elements referenced include the functions `have_git_dir()` and variable `local_repo_env[]`, along with the configuration parameter `core.attributesfile`. The response maintains a mentoring tone while setting realistic expectations about the complexity involved in this refactoring work.