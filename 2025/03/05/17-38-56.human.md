Refactoring patch removing redundant checks in the files ref backend. This is part 1 of an 8-part series (title suggests the series is about "partially atomic ref updates", though we haven't seen the cover letter yet).

The patch simplifies three functions in `refs/files-backend.c` that were performing duplicate checks when managing ref updates. The key insight is that an earlier `string_list_has_string()` check already prevents duplicates, making subsequent checks of `item->util` redundant. The changes remove these unnecessary checks and the now-unused `item->util` assignments.

The changes appear straightforward and well-justified - the commit message clearly explains why the removed code was redundant. This looks like a clean simplification that shouldn't affect behavior, just making the code more maintainable. The patch touches core ref handling code but only removes checks rather than adding new logic.