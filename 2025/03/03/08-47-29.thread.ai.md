[PATCH 0/12] object: reduce use of `the_repository` in object APIs

Series by René Scharfe completes elimination of global `the_repository` usage across object-related subsystems, now covering all 12 parts with Patrick Steinhardt contributing a compiler warning fix. Final state:

1-9: Previously converted csum-file, core object APIs, pack-writing, big file threshold config, pack verification, pack-revindex, pack-bitmap-write, object format conversion paths, and delta-islands subsystem (43+ files). Each conversion maintained behavior while adding explicit `struct repository *` parameters.

10: Structural refactoring moving hash algorithm implementations from `object-file.c` to new `hash.c` (283 lines moved) including Patrick's `-Wsign-compare` warning fixes for `hash_algo_by_name`, `hash_algo_by_id`, and `hash_algo_by_length`.

11-12: Final conversions completing the effort:
- Part 11: Updates remaining object-file.c cases
- Part 12: Converts `null_oid()` to take explicit `struct git_hash_algo*` parameter instead of `the_hash_algo`, propagating through 48 files (100+ call sites) with special handling for:
  - Subsystems already repository-independent (grep, refs/debug)
  - `diff-no-index` repository-less case
  - Removes final `USE_THE_REPOSITORY_VARIABLE` define from hash.c

Technical progression maintained dual focus throughout:
- Core `the_repository` elimination (87 files total)
- Supporting infrastructure improvements (warning fixes, code organization)
- Consistent methodology of small, reviewable changes

Key technical impacts:
- All object APIs now take explicit repository/hash-algo context
- No behavioral changes in optimized paths (delta-islands, pack-bitmap)
- Hash algorithm handling fully functional without global state
- Clean separation between object storage and hash computation

Review considerations addressed:
- Compiler warning hygiene verified
- Safety of repository passing confirmed across all contexts
- Performance maintained in critical paths
- Clean interaction with ongoing ODB abstraction work
- Improved code organization (hash.c separation)

Series provides complete foundation for pluggable ODB work by removing the last object API dependencies on global state. The warning fixes and final `null_oid()` conversion maintain the pattern of focused, behavior-preserving changes while completing a major architectural improvement.