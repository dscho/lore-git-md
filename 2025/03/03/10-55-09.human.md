This is a technical discussion about the indexing scheme for incremental MIDX bitmap layers, specifically addressing whether `base_nr` should be 0-indexed or 1-indexed.

Patrick Steinhardt follows up on Taylor Blau's patch that adds support for incremental MIDX bitmaps, focusing on the `base_nr` field in the bitmap_index struct. The field currently uses 1-based indexing, which Patrick notes is unusual. Taylor explains this was likely for convenience in iterator initialization, but acknowledges the counterintuitive nature and proposes two alternatives: either renaming to `layers_nr` to make the 1-based nature explicit, or switching to 0-based indexing with callers adjusting as needed. Patrick concurs with the second approach, favoring standard 0-based indexing despite the minor caller adjustment required.

The exchange shows careful attention to API design details in the ongoing MIDX bitmap work, with both developers agreeing on prioritizing intuitive behavior over minor implementation simplifications. This is part of a larger technical thread refining the incremental MIDX bitmap implementation.