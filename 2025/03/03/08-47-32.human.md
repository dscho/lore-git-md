This is part 3/12 of the ongoing series to reduce `the_repository` usage, specifically targeting pack-writing functionality. The patch converts several functions in `pack-write.c` and related files to explicitly take a `struct repository*` parameter instead of implicitly relying on the global `the_repository` and `the_hash_algo`.

The changes are mechanical but widespread, touching pack index writing, reverse index creation, and temporary packfile handling across 7 files including `fast-import`, `index-pack`, and `pack-objects`. Each modified function now takes a repository parameter, with callers updated to pass `the_repository` where they previously relied on the global. The changes maintain all existing behavior while making repository dependencies explicit.

This continues the series' pattern of focused, bisectable conversions. The patch is straightforward with no intended functional changes, just API modernization. It builds on part 2's object API conversions and will be followed by patches addressing other pack-related subsystems. Review should focus on ensuring no subtle behavior changes slipped in during the mechanical conversion.