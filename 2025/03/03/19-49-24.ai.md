This is a performance optimization discussion in the context of the bash completion fixes for remote names with slashes. David Mandelberg responds to SZEDER Gábor's suggestion about modifying `git for-each-ref` to handle remote refname stripping natively.

The key technical points raised:
- David acknowledges the surprising severity of Windows performance issues, where even 4-5 remotes could cause 1+ second delays due to fork/exec overhead
- He questions whether a core `for-each-ref` modification (`%(refname:strip=remote)`) alone solves the problem, noting it would need additional shell-side filtering to handle the `$cur_` pattern matching
- The email raises an important compatibility question about version coupling between core Git features and contrib scripts - whether a completion script can depend on newly-added core features or must maintain backward compatibility

The discussion highlights the tension points in the optimization approach:
1. Shell-side solutions (current approach) are correct but slow on Windows
2. Core modifications would be faster but require careful version coordination
3. Any solution must preserve the existing pattern matching behavior for partial completions

This moves the thread forward by clarifying the constraints around SZEDER's proposed core modification while keeping the Windows performance issues central to the discussion. The version coupling question suggests the solution may need to either:
- Implement a phased rollout with feature detection
- Accept slower performance on older Git versions
- Find a shell-side optimization that works within current constraints