This is a performance optimization discussion in the context of a bugfix series for Git's bash completion of remote names with slashes.

David Mandelberg responds to SZEDER Gábor's concerns about the performance impact of the current implementation on Windows, where fork/exec operations are significantly slower than on Linux. SZEDER had measured a 0.17s delay for just 4-5 remotes, which would make tab completion unpleasantly slow.

The discussion has shifted from shell-level solutions to considering a core Git plumbing enhancement. SZEDER proposed adding a `%(refname:strip=remote)` format specifier to `git for-each-ref` as a more fundamental solution that would avoid shell processing overhead. David acknowledges this approach but notes it would still require additional shell filtering to handle the completion prefix matching.

A key open question emerges about version compatibility: whether a patch series can simultaneously add a core Git feature and use it in the completion script, or if the core feature needs to exist in released versions first. This touches on broader concerns about maintaining backward compatibility while improving performance.

The exchange highlights the careful balancing act required when optimizing Git's shell scripts - weighing Windows performance against solution complexity, version compatibility, and the potential benefits of core Git enhancements versus shell-level workarounds. The thread remains active with participants exploring solutions at multiple levels of the Git system.