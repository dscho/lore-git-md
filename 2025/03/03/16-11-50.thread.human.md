Here's a human-readable summary of the thread's current state:

This thread discusses how to document deprecated Git repository layout features (specifically the `.git/branches` and `.git/remotes` directories) when building with breaking changes enabled. The original patch proposed conditionally including these documentation sections using a new Asciidoc attribute called `without-breaking-changes`, which mirrors existing test infrastructure but creates an awkward double-negative syntax (`ifdef::!without-breaking-changes[]`).

Phillip Wood raised concerns about this naming scheme, and maintainer Junio Hamano has now weighed in agreeing that the double-negative formulation is confusing. Hamano suggests exploring a positive alternative like `!with-breaking-changes` that would maintain consistency with build configurations while being more readable. The technical implementation itself - which modifies the documentation build system (both Makefile and Meson) to conditionally show these legacy features - isn't in dispute, only the naming of the controlling attribute.

The discussion has shifted from the initial implementation to refining the syntax for clarity, with both contributors acknowledging the current approach isn't ideal but needing to balance readability with consistency to existing conventions. This remains part of Git's broader effort to phase out legacy repository layout features while properly documenting the transition.