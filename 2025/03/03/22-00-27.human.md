Feature patch (4/6) implementing the `--subject-extra-prefix` option for `git format-patch`. This patch completes the plumbing work started in earlier patches by actually setting the `subject_extra_prefix` field in `struct rev_info` when the new option is used.

The change is straightforward - it copies the string provided via `--subject-extra-prefix` into the `rev` structure that will be used to generate the patch emails. This follows the pattern established in previous patches and matches how the standard `subject_prefix` is handled. The one-line diff shows the minimal change needed to connect the option parsing (done in earlier patches) with the actual subject generation (coming in later patches).

As part of a well-structured series, this patch builds on the foundation laid by patches 1-3 (which added the CLI interface, option constraints, and data structure) while keeping the way clear for patches 5-6 to handle the actual subject formatting and testing. The implementation appears clean and maintains the incremental approach that makes the series easy to review.