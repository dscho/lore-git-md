Feature patch (5/6) implementing the subject prefix construction for the `--subject-extra-prefix` flag in `git format-patch`. This patch modifies `log-tree.c` to assemble the three-part email subject line when the new flag is used, while maintaining backward compatibility when it isn't.

The change restructures the subject line generation to first add the static "Subject: " prefix, then conditionally inserts the extra prefix in brackets if provided, followed by the standard patch numbering prefix. The implementation handles all three cases: with extra prefix and numbering, with just numbering, and with neither. The diff shows careful attention to maintaining existing behavior while cleanly inserting the new component.

This completes the core functionality plumbing started in earlier patches, now producing the actual output format described in the RFC. The example shows how `--subject-extra-prefix=EXTRA` would produce subjects like "[EXTRA][PATCH v2 1/1]". The change is minimal and focused, modifying only the subject construction logic in `fmt_output_email_subject()` while preserving all other formatting behaviors.

Key technical details for future reference:
- The extra prefix appears before the standard patch prefix when both are present
- Empty/undefined extra prefixes are properly skipped
- All existing subject formatting cases (numbered, unnumbered, empty) remain supported
- The "Subject: " label is now added unconditionally at start rather than in each branch
- Output matches the format shown in the commit message example
- Changes are contained to `log-tree.c` as expected for this stage of the series

The patch appears ready for review, completing the promised functionality while maintaining the tests and documentation for later patches in the series. The implementation follows the established pattern of the series, making focused changes to specific components while maintaining overall consistency.