Here's the updated human-readable thread summary:

This thread reports Windows-specific regressions after upgrading to Git 2.48.1, where two distinct issues appear in a specific repository that worked correctly with Git 2.47.1. The first is spurious lock file errors during `git pull` operations where the reported `.lock` file doesn't actually exist, suggesting either a filesystem race condition or incorrect error reporting. The second is missing status output combined with an unexplained exit code 1, where downgrading restores progress output but the exit code issue persists.

Johannes Schindelin (Git for Windows maintainer) has now joined the discussion, suggesting the lock file issue might share a root cause with a known ReFS drive regression (PR5515). He's requested testing with the latest Git for Windows snapshot to verify this hypothesis. The exit code behavior remains unaddressed in this response, with the focus currently narrowed to the more clearly reproducible lock file problem. Technical context includes Windows 64-bit installation details, potential filesystem cache involvement, and reproduction across both GUI and command-line interfaces. The original reporter provided clear version comparisons and reproduction steps, though some diagnostic details (like exact Windows version and GIT_TRACE output) remain missing. The lock file error appears to be a genuine regression while the exit code issue may relate to the presence of an untracked file in the repository.