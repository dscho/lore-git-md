Feature patch introducing `--subject-extra-prefix` flag for `git format-patch`, part of a series aimed at helping mentoring program participants clearly identify their contributions.

This first patch in the 6-part series adds the core command-line interface for the new `--subject-extra-prefix` option to `git format-patch`. The implementation inserts a new `OPT_STRING` definition in `builtin/log.c` to capture the extra prefix value, which will be used to prepend an additional marker (e.g., "[GSoC]") before the standard patch prefix in email subjects. The change is minimal at this stage - just adding the option parsing - with the actual subject construction logic to follow in later patches.

The commit message explains the target use case: helping new contributors (who might use "[Newbie]") and participants in structured mentoring programs (using tags like "[GSoC]") clearly mark their patch submissions. The implementation appears clean and focused, with the diff showing only the necessary option addition without any unrelated changes.

This initial patch sets up the plumbing for what appears to be a well-motivated feature, though the series may prompt discussion about whether this belongs in core Git versus being handled by wrapper scripts. The change is localized to `builtin/log.c` in this patch, with subsequent patches expected to modify `log-tree.c` and add tests as indicated in the thread summary.