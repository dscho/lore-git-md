Thread summary:

Documentation patch addressing build-time breaking changes for deprecated repository layout features (`.git/branches` and `.git/remotes` directories) when building with `WITH_BREAKING_CHANGES`. The patch introduces conditional documentation sections controlled by a new Asciidoc attribute, with implementation in `Documentation/Makefile`, `Documentation/gitrepository-layout.adoc`, and `Documentation/meson.build`, maintaining consistency with prior test infrastructure (commit c5bc9a7f94a).

The discussion has focused on the naming convention for the new attribute, initially proposed as `without-breaking-changes` leading to double-negative syntax (`ifdef::!without-breaking-changes[]`). Phillip Wood raised concerns about this formulation, and Junio Hamano has now agreed, suggesting an alternative positive formulation (`!with-breaking-changes`) would be clearer while maintaining consistency with build configuration. Both acknowledge the current naming is suboptimal but the technical implementation (conditional blocks wrapping deprecated feature documentation) remains uncontested.

Key technical details:
- Build system changes add attribute when `WITH_BREAKING_CHANGES` isn't set
- Documentation sections for `.git/branches` and `.git/remotes` wrapped in conditional blocks
- Meson build system support added in parallel to Makefile changes

Open questions:
- Final naming convention for the build-time flag (double-negative vs positive formulation)
- Need to balance clarity with consistency to existing test prerequisite naming

Participants:
- Phillip Wood: raised initial naming concerns
- Junio Hamano: agreed with concerns, suggested alternative formulation

The thread remains focused on documentation presentation for deprecated features while refining the conditional syntax's readability, with no objections to the underlying technical approach.