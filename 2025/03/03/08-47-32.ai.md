This is part 3/12 of the `the_repository` removal series, focusing on pack-writing functionality. The patch converts several functions in `pack-write.c` and related files to take explicit `struct repository*` parameters instead of implicitly using `the_repository` and `the_hash_algo` globals. Key changes include:

The patch modifies core pack-writing functions like `write_idx_file()`, `write_rev_file()`, `create_tmp_packfile()`, and `stage_tmp_packfiles()` to accept a repository parameter. This affects pack index creation, reverse index writing, and temporary file handling during pack operations. The changes propagate through 7 files including `fast-import`, `index-pack`, and `pack-objects`, with mechanical conversions that replace hash algorithm access via `the_repository->hash_algo` with direct repository parameter usage.

The modifications maintain existing behavior while making repository dependencies explicit. Each converted function now takes a `struct repository*` where previously it relied on globals, with callers updated to pass `the_repository` (preserving current behavior until further refactoring). The patch follows the established pattern from earlier parts of the series, keeping changes focused on the pack-writing subsystem while remaining bisectable.

Notable technical details for future reference: functions now take `repo` parameter instead of `hash_algo`, affecting index/rev/mtimes file writing; `pack.h` interface changes require updates to all callers; temporary file handling now uses repository-aware variants; WRITE_IDX/REV/MTIMES flags behavior remains unchanged but now operates through repository parameter.