Build system fix for documentation generation, following up on a previous patch that updated file extensions from `.txt` to `.adoc`. Patrick Steinhardt acknowledges Phillip Wood's fix for the overlooked `meson.build` files in the `howto` and `technical` subdirectories, which was causing HTML documentation builds to fail when configured with `-Ddocs=html`. 

The thread context shows this is a straightforward mechanical update to match the main Documentation directory's meson.build configuration, affecting 16 howto docs and 28 technical docs (including API documentation). Patrick raises a broader question about whether to build documentation with Meson by default to catch such issues earlier, but explicitly defers that discussion for later, focusing on landing the immediate fix first. 

This is a low-risk build system adjustment with no content changes, continuing the work started in commit 1f010d6bdf7. The patch maintains consistency in the build system's handling of documentation files after the AsciiDoc extension change, and while it lacks tests (as is typical for build system tweaks), the change is clearly scoped and uncontroversial.