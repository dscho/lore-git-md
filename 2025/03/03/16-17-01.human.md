This is a refactoring patch that lays groundwork for the new `git-diff-pairs` plumbing command. It splits the existing `diff_addremove()` and `diff_change()` functions into two layers - low-level queue operations that return the created `diff_filepair`, and thin compatibility wrappers that maintain the existing behavior.

The change is carefully designed to be a no-op for existing code paths while enabling future functionality. Junio C Hamano notes the patch looks "sane and safe" and approves the approach of keeping the original functions as wrappers to avoid disrupting callback interfaces in `diff_options`.

This is the first of four patches in the final version of the series, establishing the foundation for subsequent changes that will implement batch diff processing capabilities. The thread summary indicates this refactoring is complete with all review feedback addressed, and the overall series is functionally complete pending final documentation polish.