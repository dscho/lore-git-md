This is a minor bugfix patch in the ongoing `the_repository` removal series, addressing compiler warnings in the newly created hash subsystem.

Patrick Steinhardt fixes several "-Wsign-compare" warnings in `hash.c` that were likely surfaced when the hash algorithms were moved to their own file in part 10 of the series. The changes are straightforward type fixes: converting loop counters and length parameters from `int` to `size_t` where appropriate, and removing a now-unneeded `DISABLE_SIGN_COMPARE_WARNINGS` define. The patch touches two files (`hash.c` and `hash.h`) with simple mechanical changes to three hash algorithm lookup functions (`hash_algo_by_name`, `hash_algo_by_id`, and `hash_algo_by_length`).

While this patch doesn't directly advance the `the_repository` removal goal, it maintains code quality in the newly reorganized hash subsystem. The changes are clearly correct and uncontroversial - just cleaning up compiler warnings that would have been caught by CI. The patch follows the series' pattern of small, focused changes, though it's more of a tangential cleanup than core architectural work.