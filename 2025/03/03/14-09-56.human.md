This is a discussion about cross-compilation support in Git's Meson build system, specifically regarding how to handle binaries that need to run on the target system versus those needed during build. Peter Seiderer responds to Patrick Steinhardt's patch that introduced explicit `native: true/false` parameters to distinguish between build-time and target-host binaries.

Peter argues that using Meson's cross-file mechanism for specifying target-host binaries (like `sh`, `python`, and `perl`) is a misuse of the feature, since these files are meant for programs needed during compilation, not for target system paths. He suggests instead adding dedicated configure options for cross-compilation scenarios, with fallbacks to `find_program` for native builds.

The technical discussion centers on whether Meson machine files are the right mechanism for specifying target system paths, with Peter maintaining that this approach is problematic when the target system layout isn't known at compile time. This appears to be an ongoing debate about the proper way to handle cross-compilation configuration in Git's build system.