This is part 1/12 of René Scharfe's series to reduce `the_repository` usage in object APIs, specifically targeting the csum-file subsystem. The patch refactors csum-file.c and its callers to stop depending on the global `the_repository` by passing hash algorithm parameters explicitly. Key changes include modifying `hashfd()`, `hashfd_check()`, and `hashfd_throughput()` to take a `struct git_hash_algo*` parameter, and updating all call sites to pass `the_repository->hash_algo` where needed. The changes are mechanical but widespread, touching 14 files with 56 insertions and 39 deletions. The patch maintains existing functionality while removing implicit dependencies, following the series' goal of making repository dependencies explicit. This is a straightforward conversion that doesn't introduce new behavior but helps pave the way for future repository independence.