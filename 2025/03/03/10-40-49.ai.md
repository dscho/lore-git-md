This is a follow-up in the Git cat-file batch mode filtering enhancement series, specifically addressing the naming convention debate for the new filtering option.

Patrick Steinhardt confirms acceptance of Junio Hamano's authoritative decision to use `--filter=` for the new cat-file filtering option, maintaining consistency with other Git commands like rev-list, pack-objects, repack, and clone. This resolves the earlier debate about whether to use `--objects-filter` or another name to avoid confusion with the existing `--filters` option (which handles smudge/clean filters).

The email serves as an acknowledgment of the naming resolution, with Patrick indicating he will update the implementation accordingly. This marks a key decision point in the series, removing one of the major interface design questions that had been open. The thread now moves forward with clear direction on the command-line interface while continuing to address the technical implementation challenges of integrating the filtering capability with bitmap optimizations.