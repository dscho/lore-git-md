Bugfix addressing NULL pointer handling in promisor-remote URL configuration, v2 of earlier patch.

Christian Couder's revised patch fixes the segfault when promisor-remote encounters missing URLs by pushing empty strings instead of NULL into the strvec. The change maintains strvec invariants while preserving backward compatibility. The patch now also adds explicit warnings when URLs are missing in both client and server configurations during 'KnownUrl' checks, matching existing warning behavior for URL mismatches.

Key technical changes include modifying `promisor_info_vecs()` to handle NULL URLs safely and updating `should_accept_remote()` to warn about missing URLs. The test suite gains two new scenarios: one verifying behavior with missing client-side URLs in 'KnownName' mode, and another testing server-side URL advertisement in 'KnownUrl' mode. The implementation avoids making broader changes to strvec or xstrdup behavior, deferring those discussions.

The patch touches `promisor-remote.c` and its test file, introducing new warning messages and test cases while maintaining the existing error handling patterns. The changes appear focused and well-tested, though Junio Hamano's earlier review questions about strvec API design remain open for future consideration. This version seems to address the immediate NULL dereference while cleanly handling the URL validation edge cases.