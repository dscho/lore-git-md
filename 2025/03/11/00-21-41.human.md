This is a test cleanup patch in Taylor Blau's ongoing cruft pack series. It's part of a larger effort to improve cruft pack handling (v4 2/6), following the test reorganization in the previous patch.

The change removes a redundant `write_blob()` helper function that was moved from another test file, replacing its usage with the more robust `generate_random_blob()` function that already exists in the test infrastructure. The latter is preferred as it automatically cleans up after itself. The patch affects only test code in `t/t7704-repack-cruft.sh`, modifying a test case that creates large blobs to verify cruft pack behavior with size limits.

This is a straightforward quality-of-life improvement for the test suite, eliminating duplicate functionality. The change is mechanical and low-risk, maintaining all existing test coverage while using better tooling. It continues the series' trend of cleaning up test infrastructure before introducing more substantive cruft pack changes in later patches.