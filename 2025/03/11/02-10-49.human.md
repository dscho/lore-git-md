This is a technical discussion thread about index lock behavior in Git commands, focusing on correctness and documentation implications.

Jeff King (Peff) responds to Benjamin Woodruff's explanation of their real-world use case with `git describe --dirty` in Rust tooling, where lockfile conflicts were causing problems. While this wasn't the original motivation for `--no-optional-locks`, Peff agrees it's a valid application of the flag.

The email clarifies some subtle points about index handling: there are two distinct operations (in-memory refresh for correctness vs. on-disk write for performance), and Benjamin's patches correctly handle this distinction. Peff notes an existing race condition in `git describe` where the index is refreshed before taking a lock, which could lead to overwriting concurrent changes.

An interesting discovery is that `git diff`'s refresh behavior is actually optional for correctness due to `diffcore_skip_stat_unmatch()` handling stat-dirty entries, making Benjamin's patch correct despite initial appearances. Peff suggests improving the commit messages to explain these subtleties.

The discussion also touches on libgit2's different default (no background refresh), which Peff agrees makes sense for its library context. The thread continues to focus on getting the technical details and documentation precisely right, with Peff providing experienced guidance on the implementation nuances.