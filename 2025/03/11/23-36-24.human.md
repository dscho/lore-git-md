Junio Hamano concurs with Jeff King's observation about remote URL fallback behavior in this promisor-remote bugfix thread. While the current patch safely handles NULL URLs by converting them to empty strings, Jeff noted that regular Git remotes default to using the remote name when no URL is configured - a behavior that could potentially be mirrored in promisor-remote.

This brief exchange doesn't introduce new code changes but reinforces the architectural discussion about URL handling semantics. Junio's agreement suggests this alternative approach (using remote names as fallbacks) may warrant future consideration, though the thread has already settled on the empty-string solution for the immediate NULL dereference fix. The discussion remains focused on design principles rather than implementation details, with both maintainers showing alignment on the conceptual model while maintaining the practical solution already approved for merging.