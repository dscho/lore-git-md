This is a test infrastructure patch in the ongoing breaking changes cleanup effort, specifically part 3 of a 6-patch series. It introduces a new `WITH_BREAKING_CHANGES` test prerequisite to replace the problematic `WITHOUT_BREAKING_CHANGES` double-negative flag that was originally added in commit c5bc9a7f. The patch makes several key changes:

First, it flips the logic in test scripts (t5323, t5505, t5515, t5516) to use the new positive-form prerequisite. Some tests now use `!WITH_BREAKING_CHANGES` to explicitly test legacy behavior. The patch also adds a mechanism to mark deprecated prerequisites by having them exit with code 125 (matching git-bisect behavior), which triggers a BUG() if used - this catches any future uses of the old `WITHOUT_BREAKING_CHANGES` flag.

The changes demonstrate careful attention to test hygiene, with the new prerequisite being implemented as a lazy prerequisite in test-lib.sh that checks the `$WITH_BREAKING_CHANGES` environment variable. This continues the thread's theme of establishing patterns for handling breaking changes while specifically addressing the name-rev deprecation work. The patch affects multiple test scripts and the core test infrastructure, with all modifications being straightforward prerequisite logic flips or additions.