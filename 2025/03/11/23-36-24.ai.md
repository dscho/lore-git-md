This is a follow-up discussion in the promisor-remote bugfix thread, where Junio Hamano is responding to Jeff King's observation about remote URL fallback behavior. The email continues the technical discussion about how to handle missing URLs in promisor-remote configuration.

Junio agrees with Jeff's point that regular Git remutes default to using the remote name when no URL is configured, suggesting this might be a more natural behavior than the current empty-string fallback approach being implemented in the promisor-remote fix. This introduces a new consideration in the thread about whether the promisor-remote subsystem should mirror this behavior from regular remotes.

The exchange highlights an architectural question that was previously deferred in favor of focusing on the immediate NULL dereference fix. While the core safety fix remains uncontroversial (converting NULL to empty strings to prevent crashes), this discussion opens up the possibility of future changes to make promisor-remote's URL handling more consistent with Git's standard remote behavior.

Key technical details mentioned include:
- Regular remotes use the remote name as a fallback URL when none is configured
- Current promisor-remote implementation uses empty string fallback
- `git remote --get-url` behavior as precedent
- The distinction between configuration validation and URL usage patterns

This email doesn't propose any immediate code changes but rather acknowledges a potential area for future improvement, keeping the discussion alive while maintaining consensus on the immediate fix's readiness for merging. The thread continues to cleanly separate immediate safety fixes from broader API design considerations.