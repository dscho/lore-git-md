Here's the updated human-readable thread summary:

This thread examines a reported edge case in git-rebase where a commit that was reverted on both branches and then reapplied on one branch gets incorrectly skipped during rebase, potentially causing data loss. The original reporter provided a clear reproduction script demonstrating the issue with empty commits and file additions in Git 2.48.1, suggesting this represents a bug in rebase's commit selection logic.

Elijah Newren, Git's merge and rebase expert, has now weighed in with a different perspective. He argues the current behavior is correct and documented - when an equivalent change already exists upstream, rebase should omit the commit. Newren identified potential flaws in the test case's setup regarding commit identity and timing, and suggested more precise rebase command syntax for accurate testing. He also disputed claims about --reapply-cherry-picks creating empty commits unnecessarily, maintaining this is the proper solution for the described scenario.

The discussion has now shifted from a straightforward bug report to a technical debate about whether this is actually expected behavior. Newren's authoritative perspective as the rebase/merge maintainer carries significant weight, though the original reporter may respond with additional test cases or rationale for why the current behavior should be considered incorrect. The thread remains open on whether this represents a bug to fix or documentation to clarify.