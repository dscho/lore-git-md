This is a bugfix for the promisor-remote subsystem, addressing the final review comments before inclusion in Git 2.49.0. The patch fixes a NULL pointer dereference when promisor remotes are configured without URLs, converting NULLs to empty strings while maintaining the existing behavior.

Junio Hamano confirms the patch's approach of focusing narrowly on the promisor-remote.c fix while deferring broader strvec API design questions. He agrees with Christian Couder's decision to handle the immediate crash by pushing empty strings rather than NULLs, though notes that rejecting misconfigured remotes entirely might be another valid approach.

The email highlights that the patch now includes warnings when servers advertise remotes without URLs (in 'KnownUrl' mode) or when clients have name-only configurations for advertised remotes. These warnings match existing behavior for URL mismatches, improving consistency in error reporting.

The discussion makes clear this is a targeted fix that won't change actual fetching behavior - it just makes the error handling more robust. With Junio's approval, this appears ready for merging in the upcoming release while leaving the deeper strvec/xstrdup API questions for future consideration. The patch represents a pragmatic solution that maintains backward compatibility while fixing a clear crash bug.