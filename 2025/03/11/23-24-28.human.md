This is a follow-up discussion in the ongoing feature series adding NUL-delimited output to `git rev-list`. Justin Tobler confirms implementation details for handling boundary objects in the new key-value output format.

The email specifically addresses how boundary markers (used with `--boundary`) will be represented in the structured output format that was recently agreed upon in the thread. The format will use `boundary=yes` attributes following the object ID, maintaining consistency with how missing objects are already being handled. This keeps the implementation aligned with Junio Hamano's proposed unified format where all special cases become key-value attributes.

The discussion shows the series is now focusing on edge cases and final implementation details after reaching consensus on the core design. The approach demonstrates careful attention to maintaining a consistent machine-parseable format across all `rev-list` modes.