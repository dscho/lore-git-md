This is a test reorganization patch in Taylor Blau's ongoing cruft pack series (v4 1/6). It moves several repack-related tests from `t5329-pack-objects-cruft.sh` (which tests low-level pack-objects plumbing) to `t7704-repack-cruft.sh` (which tests user-facing repack behavior). The changes are purely mechanical - 250 lines moved between files with identical content, just in their proper test script homes.

Key test scenarios being relocated include:
- Basic cruft pack generation and validation
- Interaction between cruft packs and geometric repacking
- Edge cases like repacking with no reachable objects
- Size limit handling via `--max-pack-size` and `pack.packSizeLimit`
- Configuration precedence tests for window sizes
- Quiet mode operation

The thread context shows this is part of a larger effort that has already established consensus on core object freshening behavior, now expanding to handle pack size thresholds. This particular patch is straightforward test maintenance, cleaning up the test file organization before introducing new functionality in later patches. The moved tests cover both existing cruft pack behavior and will provide the foundation for verifying the new size threshold features coming in the series.