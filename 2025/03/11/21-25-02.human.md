This is a test infrastructure patch that's part of the ongoing breaking changes infrastructure series. It replaces the double-negative `WITHOUT_BREAKING_CHANGES` test prerequisite with a more intuitive `WITH_BREAKING_CHANGES` version, while maintaining backward compatibility.

The patch updates several test scripts (t5323, t5505, t5515, t5516) to use the new prerequisite, flipping the logic where needed. The key addition is in test-lib.sh where it now marks `WITHOUT_BREAKING_CHANGES` as deprecated by making it exit with code 125 (matching git-bisect behavior for skipped tests), which will trigger a BUG() if used. This helps catch any future uses of the old prerequisite.

The change is part of a larger effort to clean up infrastructure around breaking changes, specifically the removal of the deprecated `--stdin` option in `git name-rev`. The patch demonstrates good attention to backward compatibility while moving toward clearer test semantics. The implementation looks solid, with careful updates to multiple test files and a clear deprecation mechanism for the old prerequisite.