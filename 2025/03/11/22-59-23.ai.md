This is a design discussion email in the ongoing thread about adding NUL-delimited output to `git rev-list`. Justin Tobler responds to D. Ben Knoble's analysis of format tradeoffs, building on the v1 patch series that introduced `-z` support for `--objects` and `--missing`.

The email focuses on the debate between ordered fields versus key-value attribute formats for machine parsing. Justin highlights the parsing challenges posed by `rev-list`'s variable output formats (which change based on options like `--timestamp` and `--parents`), suggesting that a consistent key-value structure (`<oid> NUL [<token>=<value> NUL]...`) might simplify parsing despite increased output size. He notes this could also benefit human readability if exposed through a separate option later.

Key technical points raised:
- Current output formats vary dramatically by option combination
- Ordered fields require context-sensitive parsing
- Key-value pairs would maintain consistent structure across modes
- Potential exists to unify machine and human-readable output paths

The discussion remains philosophical rather than implementation-focused, with no new patches proposed. This positions the thread at a design crossroads between compactness (ordered fields) and parseability (key-value attributes), with Justin leaning toward the latter for its consistency benefits across `rev-list`'s diverse output modes. The thread appears to be converging toward a decision point that will shape the v2 series implementation.