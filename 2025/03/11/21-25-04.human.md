This is a test modernization patch in the ongoing breaking changes infrastructure series. It's the fifth patch in a 6-part series that's removing the deprecated `--stdin` option from `git name-rev` and improving related test infrastructure.

The patch makes some straightforward improvements to the test script t6120-describe.sh:

1. Changes the quoting style to use single quotes for the test body (consistent with most other tests)
2. Uses double quotes for the grep pattern (rather than unquoted ERE syntax)
3. Replaces `grep -E` with the preferred `test_grep` helper

These are minor but worthwhile test hygiene improvements that make the script more consistent with Git's test suite conventions. The changes don't affect test behavior - they just make the code cleaner and more maintainable.

This continues the thread's theme of modernizing test infrastructure while working through the breaking changes process. The patch is small and focused, applying established best practices to one of the test files touched earlier in the series.