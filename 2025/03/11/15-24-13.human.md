Bugfix addressing NULL pointer handling in promisor-remote URL configuration, v2. Christian Couder follows up on earlier discussion about fixing a segfault when promisor-remote URLs are missing from config. The patch now takes a more comprehensive approach than the initial version by:

1. Pushing empty strings instead of NULL into the URL strvec to prevent crashes
2. Adding explicit warnings when URLs are missing in both client config and server advertisements
3. Including new test cases that verify the behavior with missing URLs

The changes touch both the core logic in `promisor-remote.c` and add validation tests in `t5710-promisor-remote-capability.sh`. While the immediate NULL dereference is fixed, the patch intentionally avoids broader questions about strvec API design that were raised in the thread, focusing instead on the specific promisor-remote use case.

This appears to be a well-considered update that addresses the immediate crash while adding proper validation and test coverage. The discussion about strvec API contracts remains open for future work, as noted in the commit message. The new warning messages and test cases make this a more robust solution than the initial version.