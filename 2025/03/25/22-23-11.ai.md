Test patch completing a vimdiff mergetool layout fix by adding verification for REMOTE target support. This is the second patch in a two-part series that enables using "@REMOTE" in vimdiff layout configurations, matching existing support for LOCAL/BASE/MERGED targets.

The patch adds three new test cases (17-19) to the vimdiff unit tests, specifically exercising layouts that target the REMOTE buffer with the "@" marker. Test case 17 verifies REMOTE targeting alongside other buffers, while 18-19 test simpler REMOTE-only targeting scenarios. Each test case includes both the expected vim command sequence and the target buffer specification.

The changes are minimal (10 lines added, 1 modified) and follow the established pattern of the existing test cases. This completes the functional fix from the first patch by ensuring the new REMOTE targeting capability is properly exercised. The test coverage appears thorough for the stated feature, checking both combined and standalone REMOTE targeting scenarios.

This appears to be a clean, focused addition to the test suite that properly validates the series' core functionality. No obvious gaps in test coverage are apparent, and the implementation matches the style of existing tests. The patch maintains the separation of concerns seen in the series, with the first patch implementing the functionality and this one verifying it.