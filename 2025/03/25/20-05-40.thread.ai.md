Here's the updated AI thread summary:

Build system patch addressing Coccinelle integration in Meson builds, specifically preventing automatic Coccinelle enablement when building from tarballs. The change modifies `contrib/coccinelle/meson.build` to require both `spatch` availability and a Git checkout (handling both `.git` files and directories) for Coccinelle support, fixing build failures where `spatch` was installed but Git commands couldn't run in tarball contexts. Uses Meson's `opt.require()` for the combined check with clear error messaging ("coccinelle can only be run from a git checkout" when explicitly enabled). The patch maintains existing detection logic while adding the Git checkout prerequisite, specifically helps distribution packagers who build from tarballs.

Junio C Hamano approved the solution in v2, specifically noting appreciation for handling both `.git` file types, and indicated the patch will be queued for inclusion. Author Eli Schwartz confirmed in a follow-up that the solution also satisfies worktree use cases. Patrick Steinhardt provided a final ack confirming the patch's correctness and thanking the contributor. The thread is now fully resolved with the patch ready for merging, having addressed the original build system issue for tarball-based builds while maintaining functionality for Git checkout and worktree contexts. The implementation remains a focused 6-line change to `contrib/coccinelle/meson.build` with clean error handling - silent skip for auto-detection, explicit error when requested. All key participants (Junio, Patrick) have signed off with no outstanding concerns.