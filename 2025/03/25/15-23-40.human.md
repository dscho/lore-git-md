This is a platform compatibility question about Python version usage in Git.

Randall Becker asks how to ensure Git uses Python3 exclusively on their system, as they're phasing out Python2. They have both Python versions installed in `/usr/bin`, with `python` currently symlinked to `python2`, but plan to change this. They've already tried setting `PYTHON_PATH = /usr/bin/python3` in `config.mak.uname`.

The question appears straightforward but touches on an important transition many systems are making from Python2 to Python3. The response will likely involve either build-time configuration (like their attempted solution) or runtime environment settings. The mention of NonStop and UNIX experience suggests this comes from a production environment where such transitions need careful handling.

This isn't a patch submission but rather a user question that might prompt discussion about Git's Python version handling, particularly for build and test systems that still rely on Python scripts.