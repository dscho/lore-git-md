Refactoring patch in the ongoing comma operator cleanup series, targeting xdiff's Myers diff algorithm implementation.

This is patch 5/10 in Johannes Schindelin's v2 series systematically removing unnecessary comma operators from Git's codebase. The changes focus on `xdiff/xdiffi.c`, which implements core diff algorithm logic. The patch converts two instances where comma operators were used to avoid curly braces in conditional blocks within the `xdl_split()` function - a key routine in the Myers diff algorithm that finds optimal split points between compared files.

The changes replace comma-separated assignments with explicit block statements in two similar code blocks that adjust boundary conditions during the forward and backward passes of the diff algorithm. While functionally identical, the new version improves readability by making the control flow more explicit. This follows the established pattern in the series where comma operators are preserved in performance-critical paths (like diff-delta.c) but removed where they don't serve a clear optimization purpose.

The patch maintains the exact same behavior while making the code's intent clearer - when the diff hits a boundary condition (lim2 < i2 or i2 < off2), it adjusts both i1 and i2 coordinates together. The changes are straightforward mechanical conversions that don't affect the algorithm's operation, consistent with the series' goal of improving code hygiene without functional changes.

This continues the thread's established approach where performance-sensitive diff algorithms receive careful treatment, with comma operators only being replaced when doing so doesn't obscure the algorithm's logic. The xdiff subsystem changes complement earlier patches touching rebase, kwset, and test infrastructure code.