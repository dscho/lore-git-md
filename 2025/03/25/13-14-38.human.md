Final patch in the Perl removal series. This 20th patch converts the remaining Perl dependencies in t5703 (upload-pack ref-in-want tests) to use shell-based alternatives where possible. The changes fall into two categories:

First, it replaces Perl-based capability filtering with `test_grep`, simplifying the test code while maintaining the same verification behavior. This affects the ref-in-want advertisement tests, where Perl was previously used to scan output.

Second, for the CGI script tests that still require Perl (used with HTTPD), the patch adds PERL_TEST_HELPERS prerequisites to individual tests rather than skipping the entire file. This follows the series' pattern of minimizing Perl dependencies while preserving test coverage where complete conversion isn't practical.

The patch maintains all test functionality while reducing the test file's Perl dependency from a file-wide requirement to specific test cases. This completes the series' goal of making Perl optional for all but Gitweb and netrc helper functionality, with the test suite now achieving 98% coverage without Perl. The changes are clean and consistent with the series' established conversion patterns.