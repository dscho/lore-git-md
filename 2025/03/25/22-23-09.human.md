This is a bugfix patch series for the vimdiff mergetool layout configuration. The series addresses a specific issue where the "@" marker in `mergetool.vimdiff.layout` configurations fails to work properly when targeting the MERGED window, while working correctly for LOCAL, BASE, and REMOTE targets.

The first patch fixes the actual implementation bug that prevented the MERGED target from working as expected. The second patch adds new unit tests to verify the fix and prevent future regressions. The changes are focused in the mergetools/vimdiff file with a relatively small diff (13 lines added, 1 removed).

The patch appears well-motivated and focused - it clearly identifies a specific broken case in the vimdiff integration and provides both the fix and corresponding tests. The changes look straightforward and unlikely to be controversial, though vimdiff users who rely on this configuration will want to test the new behavior.