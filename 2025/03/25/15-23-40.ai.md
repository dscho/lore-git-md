This is a platform compatibility question regarding Python version usage in Git.

The email from Randall Becker asks about forcing Git to use Python3 exclusively as his organization phases out Python2. He notes both Python versions exist in /usr/bin, with 'python' currently symlinked to python2, though this may change. He's already attempted to configure this by setting PYTHON_PATH to /usr/bin/python3 in config.mak.uname.

The question appears to be about build-time configuration rather than runtime behavior, suggesting the user is compiling Git from source. The mention of config.mak.uname indicates this is likely for a Unix-like system (though the signature references both UNIX and NonStop platforms).

This touches on Git's Python dependency, which is primarily used for tests and some supporting scripts rather than core functionality. The question doesn't specify which Git components are causing concern, but likely relates to test infrastructure or contrib scripts.

The email provides minimal technical context about the specific problems encountered or which Git version is being used. It's a straightforward inquiry about Python version management during Git builds, coming from an experienced developer (as indicated by the signature) working on a mixed UNIX/NonStop environment.