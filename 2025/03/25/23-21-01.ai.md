This is a build system patch that finalizes the discussion around Coccinelle integration in Meson builds. The patch author (Eli Schwartz) proposed modifying the build configuration to require both `spatch` availability and a Git checkout (detected via `.git` presence) for enabling Coccinelle support, preventing issues when building from tarballs. Junio C Hamano approves the solution, specifically noting appreciation for handling both `.git` files and directories, and indicates the patch will be queued for inclusion.

The change addresses a specific build scenario where distribution packagers working from tarballs might have `spatch` installed but cannot run Git commands. The implementation maintains existing functionality for Git checkout builds while cleanly handling tarball cases - silently skipping Coccinelle when auto-detected but providing clear errors when explicitly requested. This appears to be the final approval in the thread, with no outstanding technical concerns remaining about the solution.