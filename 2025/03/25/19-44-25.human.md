Bugfix refinement for `git blame` porcelain output markers. This email from Junio Hamano responds to Toon Claes' observation about asymmetry in how ignored/unblamable line markers are emitted in porcelain mode. The discussion has reached consensus that these markers should only appear in `--line-porcelain` output, but Claes notes it feels inconsistent to emit just the "ignored" status without other per-line details.

Hamano agrees this is currently the only per-line oddity, but suggests that if more per-line metadata emerges in the future, it would need similar treatment. This reinforces the thread's earlier decision to restrict these markers to `--line-porcelain` mode, as that's the natural home for per-line details. The exchange shows the careful consideration being given to maintaining porcelain output consistency while accommodating new functionality.

The thread appears to be in its final stages, with this being a minor clarification of the design rationale rather than proposing any further changes. The implementation is already adjusting to emit these markers only in `--line-porcelain` mode, preserving backward compatibility while following Git's porcelain extensibility rules.