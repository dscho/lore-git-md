This is a bugfix patch series addressing an issue with the vimdiff mergetool layout configuration. The series consists of two patches that together resolve a long-standing problem where the "@" marker in `mergetool.vimdiff.layout` didn't properly work when targeting the REMOTE window.

The first patch fixes the core issue where the layout configuration failed when REMOTE was specified as the target window for conflict resolution. The second patch adds new unit tests to verify this functionality and prevent future regressions. The changes are focused in the mergetools/vimdiff file with 13 lines added and 1 line modified.

The commit message clearly explains that this was a known limitation where the "@" marker worked for LOCAL, BASE, and MERGED targets but not for REMOTE. The series appears well-structured with a clear separation between the bugfix and test coverage, suggesting careful consideration of maintainability. The small, focused changeset (14 lines total) and explicit test coverage addition make this a low-risk but valuable fix for users relying on vimdiff-based conflict resolution workflows.