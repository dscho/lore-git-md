This short thread presents a straightforward refactoring series from Johannes Schindelin (via GitGitGadget) that cleans up unnecessary uses of the comma operator in Git's codebase. The two-patch series targets isolated instances where the comma operator was used without real need - first in `remote-curl.c`'s RPC service initialization, and now in `builtin/rebase.c`'s detached HEAD setup during rebase. Both changes are purely stylistic, replacing commas with semicolons where expressions didn't actually need to be combined. 

The modifications are small (one-line changes each) and uncontroversial, touching code paths that don't affect functionality or interact with larger ongoing efforts. Johannes motivates the changes by noting the comma operator's obscurity in modern C and potential to confuse readers. No tests are needed since these are syntactic-only changes, and there's been no discussion or objections on the list - the patches appear ready to be picked up as-is. The series follows a consistent pattern of targeting isolated, low-risk instances where the comma operator served no real purpose.