[PATCH 0/2] Replace comma operators with semicolons in two files

Johannes Schindelin (via GitGitGadget) proposes removing unnecessary comma operator usage in Git's codebase, submitting two small refactoring patches. The series targets `remote-curl.c` and `builtin/rebase.c`, replacing comma with semicolon in one location per file where the comma operator's expression-chaining behavior serves no purpose. The changes are purely stylistic with no functional impact, motivated by the comma operator being rare in modern C and potentially confusing. Both patches follow the same pattern: a one-line modification where the comma separated expressions that didn't need to be combined. No objections or discussion have occurred yet; the changes appear uncontroversial as they don't affect behavior or touch isolated code paths. The series doesn't interact with any ongoing large-scale efforts like `the_repository` removal or Rustification.