Johannes Schindelin (via GitGitGadget) initiated a thread about removing unnecessary comma operator usage in Git's codebase, starting with patches for `remote-curl.c` (push_git()) and `builtin/rebase.c` (reset_head_opts). The changes replace commas with semicolons where expression-chaining serves no purpose, with Phillip Wood confirming the rebase.c fix addressed an obvious typo. Patrick Steinhardt and Karthik Nayak supported the changes while discussing detection methods (CodeQL vs. Clang's -Wcomma). Wood later identified a similar issue in `remote-curl.c`'s fetch_git() initialization. Jeff King's new analysis adds nuance, distinguishing between straightforward replacements (like the RPC initializations) and legitimate uses in performance-sensitive code (`wildmatch.c` character iteration, `diff-delta.c` bit manipulation, `clar.c` error handling). He provides concrete diffs showing both simple conversions (kwset.c, xdiff/xdiffi.c) and cases requiring structural changes, suggesting the effort needs case-by-case evaluation rather than blanket replacement. The thread now documents: (1) three confirmed cleanup cases (push_git, rebase.c, fetch_git), (2) legitimate comma operator patterns in loops/error handling, (3) performance-sensitive areas requiring careful restructuring (diff-delta.c), and (4) ongoing discussion of detection methods (CodeQL, -Wcomma) with no false positives reported yet. Participants include key contributors: Schindelin (Windows), Steinhardt (reftable), Wood (rebase), Nayak (refs), and King (fsck/plumbing), with King's analysis shifting focus from simple style fixes to deeper code evaluation. No relation to large-scale efforts like `the_repository` removal, and no tests needed for these non-functional changes. The technical scope has expanded from initial isolated cases to a systematic examination of comma operator usage patterns across Git's codebase.