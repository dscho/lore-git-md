[PATCH 0/2] Replace comma operators with semicolons in two files

Johannes Schindelin (via GitGitGadget) proposes removing unnecessary comma operator usage in Git's codebase through two small refactoring patches. The series targets `remote-curl.c` (push_git() function's RPC service initialization) and `builtin/rebase.c` (reset_head_opts setup for detached HEAD checkout), replacing commas with semicolons where expression-chaining behavior serves no purpose. Both changes are purely stylistic with no functional impact on RPC protocol or rebase behavior, motivated by the comma operator being rare in modern C and potentially confusing. Philip Oakley has inquired about the detection method for these instances, though the question doesn't challenge the patches themselves. The remote-curl.c change touches the RPC layer while the rebase.c change affects sequencer initialization, but neither interacts with ongoing large-scale efforts like `the_repository` removal. No tests are needed as these are syntactic-only changes. The series appears complete with these two uncontroversial patches, with the only discussion so far being Oakley's question about discovery methodology (whether through code review or static analysis). No technical objections or substantive debate have emerged. Key technical details remain: one-line modifications to `push_git()` in remote-curl.c and detached HEAD setup in builtin/rebase.c, replacing commas with semicolons in isolated code paths where expressions didn't need combining.