[PATCH 0/2] Replace comma operators with semicolons in two files

Johannes Schindelin (via GitGitGadget) proposes removing unnecessary comma operator usage in Git's codebase, submitting two small refactoring patches. The series targets `remote-curl.c` (push_git() function's RPC service initialization) and `builtin/rebase.c` (reset_head_opts setup for detached HEAD checkout), replacing commas with semicolons where the comma operator's expression-chaining behavior serves no purpose. Both patches follow the same pattern: one-line modifications of isolated code paths where expressions didn't need combining. The first patch modified remote-curl.c's RPC service initialization (no protocol impact); the second now modifies rebase.c's detached HEAD setup (no rebase behavior change). Changes are purely stylistic with no functional impact, motivated by the comma operator being rare in modern C and potentially confusing. The remote-curl.c change touches the RPC layer while the rebase.c change affects sequencer initialization, but neither interacts with ongoing large-scale efforts like `the_repository` removal or Rustification. No tests are needed as these are syntactic-only modifications. The series appears complete with these two patches, both following the same uncontroversial pattern of isolated, behavior-preserving cleanups. No objections or discussion have occurred yet, suggesting these are likely to generate debate.