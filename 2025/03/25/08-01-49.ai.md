Refactoring patch targeting `remote-curl.c` as part of a two-patch series to remove unnecessary comma operators. This first patch changes a single line in the push_git() function where a comma was used between two statements that didn't need to be combined. The modification replaces the comma with a semicolon after setting rpc.service_name, making the code more conventional while maintaining identical behavior. The commit message explains the change as removing an obscure C feature that could potentially cause confusion, though this particular instance appears harmless. The patch follows the same pattern as the second in the series (which targets builtin/rebase.c) - both are minimal one-line changes focused on code style rather than functionality. No tests are included or needed since this is purely a syntactic change with no runtime impact. The change touches the RPC service initialization path but doesn't affect any protocol behavior or error handling.