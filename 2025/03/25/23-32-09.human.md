Refactoring patch in the ongoing comma operator cleanup series (5/10). This change targets the xdiff subsystem, specifically the Myers diff algorithm implementation in `xdiffi.c`.

The patch replaces two instances of comma operators with explicit curly-bracketed blocks in the `xdl_split()` function, which handles the core diff calculation. The changes are straightforward readability improvements, converting `if (cond) a, b` to `if (cond) { a; b; }` style. Both modifications occur in the diagonal traversal logic that finds the optimal diff path.

This continues Johannes Schindelin's systematic effort to remove unnecessary comma operators from Git's codebase, following the same pattern seen in earlier patches of the series (remote-curl.c, rebase.c, kwset.c). The changes are purely stylistic with no functional impact, and maintain the exact same control flow while making the intent clearer.

The patch fits well with the series' established criteria - it addresses a legitimate readability concern without touching performance-critical paths (unlike some preserved comma operators in diff-delta.c and wildmatch.c that were kept for optimization reasons). Reviewers have consistently approved similar changes throughout the thread.