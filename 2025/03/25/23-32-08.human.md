Refactoring patch in the comma operator cleanup series (4/10). This change modifies the test framework's filesystem helpers to replace comma operators with clearer semicolon-separated statements.

The patch updates two functions in the Clar test framework's filesystem utilities (`fs_copydir_helper` and `fs_rmdir_helper`), converting while loops that used comma operators for directory iteration into more readable for loops with explicit error handling. Each now separately sets `errno = 0` before calling `readdir()` and checks the result in a clearer control flow.

This continues Johannes Schindelin's systematic effort to improve code clarity by removing unnecessary comma operators, following the pattern established in earlier patches of the series. The changes are straightforward readability improvements with no functional impact, consistent with the maintainer-approved approach seen in previous network and porcelain command changes.

The patch maintains the same error checking behavior while making the control flow more explicit - a good example of the series' goal to eliminate confusing comma operators where they don't serve a clear purpose. Like other patches in the series, this appears to be a non-controversial improvement that follows the project's established code hygiene standards.