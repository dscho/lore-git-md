This is a continuation of the thread about removing unnecessary comma operators in Git's codebase, now examining more complex cases. Jeff King responds to Phillip Wood's earlier findings by identifying several legitimate uses of the comma operator that would require more careful consideration before changing.

The email highlights valid comma operator patterns in Git's code, including loop conditions (like in `wildmatch.c` where it helps maintain state between iterations) and conditional expressions (like in `clar` where it combines error handling with a function call). These demonstrate cases where the comma operator serves a real purpose rather than being purely stylistic.

King also shows several straightforward fixes where commas can be replaced with semicolons, including examples from `diff-delta.c`, `kwset.c`, and `xdiff/xdiffi.c`. These changes are similar to previous patches in the thread - simple replacements that improve code clarity without affecting functionality.

The discussion remains technical but non-controversial, with contributors identifying both easy wins and more nuanced cases. This email helps establish boundaries for the cleanup effort by distinguishing between clear-cut improvements and cases where the comma operator serves a legitimate coding purpose.