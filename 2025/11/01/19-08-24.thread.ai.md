Proposal for an Outreachy internship project to refactor Git's global state as part of the ongoing `the_repository` removal effort. Applicant Bello Caleb Olamide (first-time contributor with one merged microproject) plans to migrate repository-scoped globals from `environment.c` into `struct repository` or `struct repo-settings`, building on established patterns like the `core.hooksPath` and `core.sharedRepository` migrations. Key technical challenges include early startup variables (e.g. `have_git_dir()`) and complex cases like `git_attributes_file` requiring subsystem coordination. The proposal demonstrates awareness of prior attempts (e.g. incomplete `is_bare_repository_cfg` work) and includes a structured timeline with code study, implementation phases, and iterative review cycles. The applicant commits to full-time availability and post-internship community involvement.