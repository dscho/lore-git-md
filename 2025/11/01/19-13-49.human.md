This is an updated Outreachy internship proposal (v2) focused on reducing Git's global state by migrating repository-scoped variables from `environment.c` into `struct repository` and `struct repo-settings`. The proposal comes from Bello Olamide, who has already contributed a microproject to Git (a cleanup of strbuf_split*() usage that was merged).

The proposal demonstrates good engagement with mentor feedback by incorporating a detailed analysis of different refactoring strategies, particularly comparing the accessor functions approach (used for `core.hooksPath`) with explicit parameter passing (used for `the_hash_algo`). It identifies two key unfinished works to complete: moving `git_attributes_file` (with special handling due to its relationship with the index subsystem) and `is_bare_repository_cfg`.

The timeline shows a thoughtful approach: initial study phase, community engagement, criteria definition, then implementation from January-February 2026. The proposal is well-grounded in prior work and shows awareness of technical challenges like startup code dependencies and cross-subsystem relationships. A minor note indicates the author will send a corrected v3 due to a formatting issue with git send-email.