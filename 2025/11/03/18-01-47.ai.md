This is a cleanup patch in the merge-ort rename detection series. It removes debugging code that was accidentally left in during development of an earlier commit (a16e8efe5c2b) that fixed directory rename handling when merge.directoryRenames=false. The patch simply deletes an always-true condition (`&& 1`) that the author used for testing different code paths during development. This is a straightforward removal of dead code that doesn't affect functionality, just cleaning up after the earlier fix. The change is minimal (one line removed) and focused on merge-ort.c's rename collection logic. Since this was written by the primary merge-ort author and relates to a known issue in the subsystem, it's likely to be uncontroversial. The patch follows naturally from the first patch in the series which updated test documentation, with the third patch expected to contain the substantive fix for the rename caching issue.