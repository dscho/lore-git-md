This is a technical clarification in the ongoing SHA-1/SHA-256 interoperability work, specifically addressing integer type choices at the Rust/C FFI boundary. The email responds to Collin Funk's question about using `intmax_t`/`uintmax_t` with Rust, confirming these map to fixed-width 64-bit types (`i64`/`u64`) in practice despite their theoretical variable-width definition. The author notes that while Rust supports 128-bit integers (`i128`/`u128`), their ABI compatibility with C's `__int128` is limited to certain compilers and architectures.

The discussion provides important context for the series' earlier decision to use `uint32_t`/`u32` for hash algorithm representation at the FFI boundary (patch 3/14). It reinforces the implementation's careful consideration of cross-language type compatibility, particularly regarding fixed-width requirements and ABI stability. The technical details about compiler behavior (`clang -E` output) and Rust's 128-bit integer support help establish the boundaries of safe type usage between C and Rust components.

This email doesn't introduce new changes to the patch series but solidifies the technical rationale behind existing design choices, particularly relevant for reviewers evaluating the FFI implementation's robustness. The information helps justify why the series avoids more complex integer types that might introduce portability issues across the language boundary.