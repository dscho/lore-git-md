This is a technical clarification in the ongoing SHA-1/SHA-256 interoperability work, specifically addressing type choices at the Rust/C FFI boundary. The email responds to Collin Funk's question about using `intmax_t`/`uintmax_t` with Rust, explaining why these C types (effectively fixed at 64-bit) would map to Rust's `i64`/`u64` rather than larger types. The author notes that while Rust supports 128-bit integers (`i128`/`u128`), their ABI compatibility with C's `__int128` is limited by platform support.

The discussion is part of the broader thread establishing FFI-safe type representations for hash algorithm identifiers, where the series has already settled on using `uint32_t`/`u32` for this purpose (as noted in the thread summary). This email provides useful background about integer type considerations but doesn't introduce new changes to the patch series - it's more of an educational aside about type system interoperability. The tone is technical and informative, typical of implementation detail discussions in this complex Rust integration effort.