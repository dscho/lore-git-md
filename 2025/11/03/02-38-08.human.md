Regression in `git fetch --tags` behavior in bare repositories, where tag updates now fail atomically rather than per-tag. This is a follow-up in an ongoing discussion about whether the new behavior (introduced in Git 2.51.0's reference transaction system) is intentional or a regression.

David Bohman argues the change is problematic for several reasons: it represents a significant behavior change in a mature tool (risking widespread breakage), and produces misleading output where new tags are reported as successfully fetched when they actually aren't stored. The discussion has shifted from diagnosing the regression to debating whether the old per-tag behavior or new atomic behavior is correct.

The thread remains technically focused on tag update mechanics in bare repositories, with implications for how the reference transaction system should handle partial failures. David's perspective adds weight to concerns about the change's impact on real-world usage and error reporting clarity.