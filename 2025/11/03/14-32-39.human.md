Final review comments on the blame diff algorithm patch series.

Phillip Wood provides some last-minute feedback on patch 2/2 of Antonin Delpeuch's series to make the diff algorithm configurable in `git blame`. The patch is nearly ready, with just a few minor suggestions:

1. Notes an edge case in the interaction between `--no-minimal` and `--diff-algorithm` where the current implementation makes `--no-minimal` behave like `--diff-algorithm=myers`. Phillip suggests this is consistent with current behavior but leaves the decision to the author.

2. Points out a small optimization opportunity in the test script where two `sed` commands could be combined into one for better performance, especially on Windows.

3. Recommends marking the `--minimal` option as HIDDEN in the help text to avoid confusion about its interaction with the new `--diff-algorithm` option.

The feedback is minor and non-blocking, indicating the patch series is in good shape after multiple rounds of review. The technical discussion around diff algorithm selection appears settled, and these are final polish items before the maintainer considers merging.