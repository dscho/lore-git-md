Refactoring patch in Patrick Steinhardt's ODB abstraction series (10/13 in v3). This continues the mechanical renaming work to standardize function names for loose object handling in the object database.

The patch renames `has_loose_object()` to `odb_source_loose_has_object()` to match the established naming pattern for functions operating on loose object sources. The change is straightforward and touches three files: builtin/pack-objects.c, object-file.c, and object-file.h. The function's behavior remains identical - it still checks for loose object existence without respecting replace references.

This is part of the broader effort to encapsulate loose object operations within the ODB source abstraction. The patch follows the same naming convention established in earlier patches of the series, where loose object operations are prefixed with `odb_source_loose_`. The changes are mechanical and maintain the existing functionality while improving code organization.

The patch appears well-contained and follows the patterns set by previous patches in the series. It updates all callers of the function, including in pack-objects where the function is used to check for loose objects during packing operations. The commit message is concise but clear about the motivation for the change.