Performance optimization for `git last-modified` command, now ready for merging. This patch series introduces a new algorithm that significantly speeds up finding the most recent commit modifying each path, particularly for rarely-modified files. The implementation uses generation-sorted priority queues, EWAH bitmaps for path activity tracking, and Bloom filter optimizations to skip unnecessary diffs.

Junio C Hamano notes that while he's already merged a fixed-up version to 'next', this v4 includes minor cleanups: using CALLOC_ARRAY() instead of xcalloc() and some formatting tweaks. He observes that while the changes are technically correct, he personally prefers keeping braces around nested if-statements for clarity, and considers the remaining formatting issues minor enough to defer. The performance improvements (2.5x-111.7x faster depending on repository and use case) and correctness fixes have been thoroughly reviewed and validated by multiple contributors, with Taylor Blau providing an Acked-by.

The thread shows consensus that all technical concerns have been addressed, including memory/performance tradeoffs, type safety issues, and edge case handling. The patch is now in its final form, with any remaining stylistic preferences being subjective rather than substantive.