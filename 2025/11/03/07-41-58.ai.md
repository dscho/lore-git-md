This is a refactoring patch in Patrick Steinhardt's ongoing ODB abstraction series (3/13 in v3). It updates function names to match the renamed `struct odb_source` (previously `object_directory`) and aligns them with Git's modern naming conventions where the subject comes before the verb.

The changes are mechanical renames of two functions:
- `free_object_directory()` becomes `odb_source_free()`
- `free_object_directories()` becomes `odb_free_sources()`

The patch updates all call sites in odb.c, including within the object database cleanup path and alternate source handling. This continues the series' pattern of standardizing the ODB interface while maintaining identical behavior. The changes are straightforward and unlikely to be controversial, following the naming pattern established in earlier patches (like the `odb_source_loose` convention previously discussed in the thread).

This patch builds on the foundation laid by a1e2581a1e which did the initial struct rename, and fits into the larger goal of making the ODB interface more consistent and abstracted. The diff shows careful attention to updating all relevant call sites while keeping the surrounding logic unchanged.