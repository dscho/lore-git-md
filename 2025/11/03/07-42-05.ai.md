This is a refactoring patch in Patrick Steinhardt's ongoing ODB abstraction series (v3 10/13). The change renames `has_loose_object()` to `odb_source_loose_has_object()` to align with the established naming convention for loose object operations in the object database source interface.

The patch updates all callers in builtin/pack-objects.c and object-file.c to use the new function name, while maintaining identical behavior. The function's documentation in object-file.h is moved to group it with other loose object source operations, improving header organization. This continues the series' pattern of standardizing names to `odb_source_loose_*` prefix for functions operating on loose object sources.

The change is mechanical and straightforward, following the naming decisions established earlier in the thread (where Karthik Nayak proposed and Junio Hamano approved the `odb_source_loose` pattern). No functional changes are introduced - this purely updates the symbol name and its references across three files (object-file.[ch] and builtin/pack-objects.c).

This patch fits cleanly into the series' progression, building on previous work that moved loose object operations to the ODB source interface (patches 5-9/13). It maintains the thread's focus on encapsulation and naming standardization while preparing for future work on the ODB abstraction. The change appears uncontroversial and well-aligned with the series' established direction.