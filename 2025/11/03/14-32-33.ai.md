This is a review response in the ongoing thread about adding diff algorithm configurability to `git blame`. Phillip Wood is reviewing the first patch in the v4 series, which refactors xdiff internals to cleanly handle algorithm selection.

The patch adds XDF_NEED_MINIMAL to the XDF_DIFF_ALGORITHM_MASK bitmask, eliminating the need for separate clearing of the minimal diff bit when resetting algorithms. Phillip confirms this is a useful improvement that prevents potential errors, noting it makes the DIFF_XDL_CLR macro obsolete (though he suggests keeping some comments about clearing old settings). 

Key technical details:
- Changes xdiff/xdiff.h to include XDF_NEED_MINIMAL in the algorithm bitmask
- Removes redundant DIFF_XDL_CLR calls from diff.c and merge-ort.c
- Maintains same behavior while simplifying the code

Phillip's review indicates the patch is nearly ready, with only minor documentation comments remaining. This follows extensive prior discussion about the proper handling of diff algorithm flags, where consensus was reached that including minimal in the mask was the right approach despite being a technically breaking change for xdiff consumers. The review confirms the implementation matches the agreed-upon solution from earlier technical discussions.