This is a test patch (3/5) in a series addressing submodule `ignore=all` behavior, specifically focusing on test coverage for the new `--include-ignored-submodules` option in `git add`. The test file `t2206-add-submodule-ignored.sh` comprehensively verifies the interaction between submodule ignore settings, pathspecs, and the new command-line option.

The test suite covers several key scenarios: default vs ignore=all submodule configurations, status reporting behavior, and the effect of the new `--include-ignored-submodules` flag. It includes 8 test cases that validate:
1. Basic submodule setup and initial state verification
2. Behavior changes when ignore=all is set in .gitmodules
3. Status output variations with different ignore settings
4. Core functionality of the new --include-ignored-submodules option
5. Pathspec handling (both `sub` and `./sub/` forms)
6. Interaction with `git log --ignore-submodules=none`

The tests use GIT_TRACE for debugging and verify both positive and negative expectations (using `!` patterns). The test the full workflow from initial setup through configuration changes to final verification of commit history. The test file is properly integrated into the build system via meson.build.

This appears to be a thorough test suite that exercises all the functionality introduced in the earlier patches of the series. The test cases are well-organized and cover both the happy path and edge cases, including the trace output verification that was discussed earlier in the thread. The test follows Git's standard test patterns and includes proper cleanup.