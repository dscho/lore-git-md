This is part of a bugfix series addressing submodule `ignore=all` behavior consistency. The patch modifies `add_files_to_cache()` to accept a new `include_ignored_submodules` parameter, which will be used by `git add` to explicitly update ignored submodules when requested.

The change adds plumbing through several callers (`add`, `checkout`, `commit`) while keeping their existing behavior (passing 0 for the new parameter). The core logic in `update_callback()` now checks submodule ignore status and pathspec matches, showing warnings when skipping ignored submodules unless the new flag is set. The implementation includes detailed tracing and maintains the existing warning mechanism for ignored submodules.

This appears to be a well-constructed change that cleanly extends the existing infrastructure. The patch is part of a v2 series that has already incorporated feedback from earlier discussion, particularly around option naming and warning behavior. The changes are focused and maintain backward compatibility while enabling the new `--include-ignored-submodules` functionality that will be exposed in later patches of the series.