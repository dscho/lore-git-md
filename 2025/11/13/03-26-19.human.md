This is a bugfix patch that strengthens Git's protection against creating mixed-hash-algorithm repositories, specifically addressing submodule additions. Jeff King (peff) is reviewing brian m. carlson's implementation that prevents adding submodules with a different hash algorithm than their parent repository.

The review confirms the core approach of validating hash algorithm consistency in `index_path()` makes sense, but identifies two edge cases: (1) a redundant pre-check in `add_to_index()` that could be cleaned up separately, and (2) the `git add -N` (intent-to-add) path bypasses the new validation. Peff suggests both are probably acceptable but worth considering.

The review also offers minor test improvements - using `test_commit -C` to avoid subshells and `test_grep` for more robust output checking - and recommends verifying the error message appears as expected. The discussion remains technical and constructive, with no fundamental objections to the approach.

This continues the thread's consensus that mixed-hash repositories are invalid by design, now moving from theoretical agreement to concrete implementation review. The patch appears well-reasoned with only minor polish needed on the test implementation.