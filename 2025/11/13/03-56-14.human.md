This is a follow-up patch in the ongoing discussion about preventing mixed hash-algorithm repositories. Jeff King builds on brian m. carlson's earlier validation work by cleaning up a redundant submodule HEAD resolution in `add_to_cache()`.

The patch removes duplicate submodule validation that was happening in both `add_to_cache()` and `index_path()`, consolidating it to just `index_path()`. This addresses several issues: it eliminates redundant work, ensures consistent error handling across all code paths that index submodules, and properly surfaces the hash-algorithm validation that brian m. carlson recently added.

Jeff notes this makes the error messages slightly more verbose (now showing both the submodule error and the indexing failure), but argues this is consistent with other error cases. The change maintains all existing functionality while simplifying the code - a clear improvement that doesn't appear controversial. The patch includes a test update to match the new error output format.

This appears to be a straightforward cleanup that complements the main hash-algorithm validation work, addressing an implementation quirk Jeff noticed during his review. It doesn't change the fundamental approach brian m. carlson took, just makes the code more maintainable.