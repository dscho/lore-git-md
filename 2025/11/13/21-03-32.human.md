This is a user experience complaint about the deprecated `git whatchanged` command. Eduard Bloch argues that the removal of this command creates unnecessary friction, as its modern replacement (`git log --raw`) requires more keystrokes to type. The author specifically objects to the deprecation warning not clearly stating the replacement command, forcing them to search online for the alternative. 

The email takes a somewhat humorous tone (mentioning "eating my dogs for breakfast" warnings) but makes a serious point about command-line efficiency for frequent users. The core argument is that the tab-completion advantage of `git whatchanged` (6 keystrokes) outweighs any benefits of removing this legacy alias, even if it remains technically redundant with `git log --raw` (12 keystrokes).

This appears to be reopening a long-settled discussion - `git whatchanged` was deprecated years ago as essentially a less capable version of `git log` functionality. The email doesn't propose a specific solution beyond reverting the deprecation, nor does it address why the original deprecation decision might have been made (like reducing maintenance burden or command namespace clutter). The argument rests entirely on personal workflow preference rather than technical merits.