This is a review response from Junio Hamano on a patch series about submodule `ignore=all` behavior in `git add`. The series (now at v2) aims to make `git add` respect the `ignore=all` setting consistently with other commands like `status` and `diff`.

Junio provides detailed feedback on the cover letter's clarity, noting several places where the description could be more precise about the actual behavior changes rather than using vague terms like "nicely respected". He also points out that the cover letter still refers to `--force` while the implementation has switched to `--include-ignored-submodules` per earlier discussion.

The review highlights the importance of clear, specific commit messages that explain:
1. What commands currently do with `ignore=all` submodules
2. What problematic behavior the patch fixes
3. Exactly how the new behavior differs (e.g. when and why a submodule would be skipped)

Junio doesn't raise any technical objections to the implementation itself, which appears to have incorporated feedback from earlier rounds (including Phillip Wood's suggestions). The series seems technically sound with comprehensive test coverage and documentation updates.

This is likely one of the final reviews before the series could be merged, with the main remaining work being polish on the commit messages and cover letter to better communicate the changes to users.