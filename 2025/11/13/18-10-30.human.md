This is part 2 of a 5-part series implementing consistent `ignore=all` behavior for submodules, focusing on the `git add` command. The patch modifies `read-cache.c` to respect the `ignore=all` setting for submodules during add operations, unless explicitly overridden with the new `--include-ignored-submodules` flag.

The changes ensure that submodules marked with `ignore=all` are skipped during `git add` unless both the flag is used and the submodule path is explicitly specified in the pathspec. When skipping, it shows a helpful message guiding users to use the flag if they really want to add the submodule. The trace output has also been improved for debugging purposes.

The motivation is to better support workflows like submodule branch tracking, making behavior more consistent with how ignored files are handled. The commit message suggests this change will enable more flexible submodule usage patterns similar to the repo tool, allowing looser coupling between superprojects and submodules.

This appears to be a well-considered change that implements the agreed-upon approach from the thread discussion. The patch is focused and maintains backward compatibility while adding the new functionality. It follows through on the technical decisions made in the thread, particularly around warning messages and pathspec handling. The changes are localized to the submodule handling logic in `read-cache.c` with clear trace and user messaging improvements.