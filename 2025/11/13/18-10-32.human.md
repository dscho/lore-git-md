Test maintenance patch in the submodule `ignore=all` series. This is the fourth patch in a five-part series implementing consistent handling of ignored submodules across Git commands, with a focus on `git add`.

The patch updates existing tests that were implicitly relying on the old behavior where `git add` would include changes from submodules marked with `ignore=all`. With the new behavior (introduced in earlier patches of this series), these tests need to explicitly use the new `--include-ignored-submodules` option when they want to add changes from ignored submodules.

Changes are made to:
1. The shared test library `t/lib-submodule-update.sh`, which affects multiple test scripts that use it
2. The status tests in `t7508-status.sh`

The updates are straightforward mechanical changes to add the new flag where needed. This patch follows the series' established pattern of maintaining backward compatibility while introducing the new behavior, ensuring existing tests continue to pass with the modified semantics.

The patch appears well-scoped and correctly implements the test updates needed to support the new submodule handling behavior. It's part of a carefully considered series that has already gone through significant review and discussion.