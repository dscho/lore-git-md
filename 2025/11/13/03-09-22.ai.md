This is a discussion about Patch 4 in Jeff King's ASan fixes series, focusing on parsing safety in the cache-tree code. The patch originally replaced `strtol()` with a simpler parsing approach to avoid undefined behavior when reading non-string buffers, but Patrick Steinhardt raised concerns about the lack of overflow detection and input validation.

Taylor Blau weighs in, noting that while he generally agrees with Patrick's preference for more robust parsing (like the `git_parse_int()` family of functions), those helpers include suffix parsing (k/m/g) that would be inappropriate for this on-disk format parsing context. The core tension is between simplicity (current patch) and defensive programming (Patrick's suggestion), with no clear resolution yet on how to balance these concerns for this specific case.

The discussion remains open-ended, with Taylor not taking a strong position but acknowledging the validity of Patrick's safety concerns while pointing out a technical constraint (suffix handling) that complicates using the existing parsing utilities. This continues the thread's emerging theme about whether to develop more generalized parsing helpers for such cases.