This is part 2 of a 5-patch series implementing consistent `ignore=all` behavior for submodules, specifically focusing on `git add` integration. The patch modifies `read-cache.c` to respect submodule ignore settings while adding files, with several key changes:

The core change makes `git add` skip submodules marked with `ignore=all` unless explicitly overridden by the new `--include-ignored-submodules` flag and the submodule path is specifically targeted. The implementation includes warning messages (similar to how ignored files are handled) that guide users to use the override flag when needed. The diff shows three main modifications: adding submodule-related header includes, updating trace output messages to be more descriptive, and fixing a control flow issue with a missing brace.

The commit message explains this enables better workflow support for submodule branch tracking scenarios, where users want to reference submodules without tracking their exact SHA-1s. The changes align submodule behavior more closely with regular ignored files while maintaining the existing warning system. Trace output is improved to clearly indicate when submodules are skipped due to pathspec mismatches versus ignore settings.

This patch builds on the plumbing changes from patch 1 in the series, implementing the actual behavior changes for `git add` operations. The implementation follows the agreed-upon design from the thread discussion, particularly Phillip Wood's recommendations around explicit option naming and warning preservation. Test coverage for these changes appears in a separate patch in the series.