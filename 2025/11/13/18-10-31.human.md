This is a test patch (part 3/5) in a series addressing submodule `ignore=all` behavior. It adds comprehensive test coverage for the new `--include-ignored-submodules` option in `git add`.

The new test file `t2206-add-submodule-ignored.sh` verifies several key scenarios:
- Default behavior where submodule changes are ignored when `ignore=all` is set
- The new `--include-ignored-submodules` flag properly overrides this behavior
- Interaction between `.gitmodules` config and command-line overrides
- Status and log output behavior in various configurations

The tests follow a logical progression from setup through various edge cases, including path specifications (both `sub` and `./sub/` forms) and verification of trace output. The test appears thorough, covering both the happy path and expected failure modes.

This patch builds on the plumbing changes from earlier in the series, providing the necessary test coverage for the new functionality. The test design aligns with Phillip Wood's suggested approach from earlier in the thread, and the implementation looks clean with appropriate trace debugging included.