This is a follow-up patch in the ongoing discussion about preventing mixed-hash-algorithm submodule additions. Jeff King builds on brian m. carlson's earlier work by cleaning up a redundant submodule HEAD resolution in `add_to_cache()` that was identified during code review.

The patch removes the duplicate `repo_resolve_gitlink_ref()` call from `add_to_cache()` since the same check already happens in `index_path()`, which is the more appropriate location. This consolidation provides several benefits: it eliminates redundant work, ensures consistent error handling across all code paths that reach `index_path()`, and properly centralizes the hash-algorithm validation that was recently added.

Key technical changes include:
- Removing the submodule resolution from `read-cache.c`'s `add_to_cache()`
- Enhancing the error message in `object-file.c`'s `index_path()` to include the "no commit checked out" case
- Updating the test in `t/t3700-add.sh` to expect the new error message format
- Maintaining all existing functionality while simplifying the code

The change makes the code more maintainable by having a single point of truth for submodule validation, though it does make some error messages slightly more verbose as errors now propagate up the call stack. This appears to be a straightforward improvement that addresses the redundancy Jeff noted in his earlier review without introducing new complexity.