This is a refactoring patch that follows up on the earlier submodule hash-algorithm validation work. Jeff King proposes removing a redundant submodule HEAD resolution check from `add_to_cache()` in `read-cache.c`, since the same check already happens in `index_path()` with more comprehensive validation (including the hash-algorithm check that was the focus of brian m. carlson's original patch).

The change consolidates submodule validation logic into `index_path()`, which is the more fundamental operation that `add_to_cache()` calls. This eliminates duplicate work (opening the submodule repository twice) and ensures consistent error handling across all code paths that might index a submodule, not just those going through `add_to_cache()`. The patch maintains the same error message for the "no commit checked out" case, just moving it to `index_path()`.

The implementation removes the `oid` variable and resolution check from `add_to_cache()`, updates `index_path()` to emit the error message directly, and adjusts the test in `t/t3700-add.sh` to expect the slightly modified error output (now including the propagation of the call stack). Junio C Hamano agrees this is a sensible cleanup that improves code quality without changing the core behavior established by the earlier hash-algorithm validation work.

This is a focused technical improvement that doesn't reopen the broader discussion about mixed-hash repositories, but rather refines the implementation of the agreed-upon validation approach. The patch appears well-justified and low-risk, with clear test coverage for the modified error message path.