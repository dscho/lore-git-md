This is a build system patch in the ASan series, specifically addressing the NO_MMAP setting for Meson builds. Junio Hamano is responding to Patrick Steinhardt's suggestion to mirror the Makefile's NO_MMAP change for ASan builds in the Meson build system as well.

The key technical detail is the proposed addition to meson.build that would conditionally set NO_MMAP and include the mmap compatibility code when building with AddressSanitizer, matching the behavior already implemented in the Makefile. The change would only activate for ASan builds, otherwise maintaining the normal mmap check.

Junio is offering to squash Patrick's Meson changes into Jeff King's original Makefile patch (patch 3/9), which would keep the build system changes synchronized in a single commit. This follows the ongoing discussion about build system synchronization policies in the thread, where Patrick has advocated for a pragmatic approach to keeping the two build systems aligned.

The patch appears straightforward and uncontroversial, building on the already validated Makefile changes. It represents a concrete example of the build system synchronization that's been under discussion in the thread.