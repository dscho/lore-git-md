This is a follow-up in the ongoing discussion about submodule `ignore=all` behavior consistency, specifically addressing feedback on the `git add` implementation.

Claus Schneider responds to Phillip Wood's technical review by implementing the suggested `--include-ignored-submodules` option instead of the previously proposed `--force` flag. The email indicates the commit messages have been expanded with additional rationale, though the specific content isn't shown here. Two practical questions are raised about patch submission workflow:
1) Whether to consolidate changes into a single commit or keep them split by component
2) How to properly update the patches via GitGadget PR

The response references key technical points from the thread, including the historical context from commit 5556808690e that originally introduced the divergent behavior between `git add` and `git commands. While the implementation now aligns with Phillip's preference for a dedicated submodule-specific option, the core tension remains between maintaining historical behavior justification and improving user experience consistency.

Notably, the email doesn't address the broader scope question of whether `reset`/`restore` should be included in this change, suggesting focus remains on finalizing the `git add` implementation first. The technical constraint requiring `override_submodule_config=1` for warning generation appears to remain in place despite the behavior change.