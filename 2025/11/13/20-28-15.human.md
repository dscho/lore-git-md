This is a follow-up discussion in the OSX keychain credential helper bugfix thread. Junio Hamano is questioning the patch author's rationale for avoiding Git's standard string buffer utilities (`strbuf.h`) in favor of custom implementations.

Junio makes a technical argument that linking with `libgit.a` (which would include `strbuf.o`) doesn't create meaningful dependency concerns since the credential helper is already part of the Git distribution. He notes that even if there were shared library dependencies, it wouldn't make practical sense to have the credential helper without Git itself.

The exchange highlights a design philosophy question about code reuse versus self-containment in Git's helper programs. Junio's tone suggests he sees this as unnecessary reinvention of existing functionality, though he phrases it as a question rather than outright rejection. This appears to be a review point that the patch submitter will need to address in a revision.

The core bugfix (converting a boolean flag to a string state for accurate credential tracking) isn't being challenged - the discussion has moved to implementation details of the solution. This is typical mid-thread review feedback where maintainers scrutinize architectural choices after agreeing on the general approach.