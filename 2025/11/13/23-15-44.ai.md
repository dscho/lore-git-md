Bugfix follow-up discussing edge cases in submodule hash algorithm validation.

This email from brian m. carlson responds to Jeff King's review of their patch that prevents adding submodules with mismatched hash algorithms. The discussion focuses on two edge cases: the handling of intent-to-add (`git add -N`) operations and a redundant submodule resolution check in `add_to_index()`. 

The key technical points addressed are:
1. The validation in `index_path()` correctly catches regular `git add` operations but intentionally skips intent-to-add cases since they use the empty blob OID (which can't mismatch algorithms). brian agrees this is acceptable since committing with an empty blob OID would already indicate repository corruption.
2. Jeff noted a redundant `repo_resolve_gitlink_ref()` call in `add_to_index()` from commit f937bc2f86 that predates the current validation logic. While orthogonal to the hash algorithm check, this could be cleaned up to improve the error messaging flow.
3. brian plans to send a v2 incorporating Jeff's test suggestions, including verifying stderr output contains the expected error message when `git add` fails on mismatched submodules.
4. The email confirms the patch already covers `git submodule add` cases, which was a deliberate choice to handle the most likely submodule addition paths.

The discussion shows consensus on the technical approach while identifying minor improvements for test coverage and code cleanliness. brian demonstrates responsiveness to review feedback while maintaining the core validation approach that prevents mixed-hash repositories. The thread appears to be reaching its conclusion with these final refinements to the implementation.