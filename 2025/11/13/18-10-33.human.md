This is a documentation patch completing a series about submodule `ignore=all` behavior. It's the final patch (5/5) in v2 of the series that has been through extensive discussion and testing.

The patch adds documentation for the new `--include_ignored_submodules` option to `git add`, which allows staging updates to submodules marked with `ignore=all`. It updates three documentation files:
- `git-add.adoc` to document the new flag
- `submodule.adoc` to clarify the interaction between `ignore=all` and the new option
- `gitmodules.adoc` to explain how `ignore=all` affects various commands

The documentation changes match the implementation from earlier patches in the series, which introduced the new behavior while maintaining backward compatibility. The patch also includes some unrelated Docker configuration files that appear to be development environment setup, though these seem out of place in a documentation-focused patch.

This completes the series by providing user-facing documentation for the new submodule handling behavior, which has already been implemented and tested in previous patches. The documentation is clear and matches the technical implementation we've seen evolve through the thread.