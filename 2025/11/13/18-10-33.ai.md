This is the final documentation patch in a 5-part series addressing submodule `ignore=all` behavior consistency. The patch adds documentation for the new `--include_ignored_submodules` option introduced in earlier patches and updates the descriptions of `ignore=all` behavior in multiple documentation files.

The changes clarify that:
1. `git add` now requires explicit use of `--include_ignored_submodules` to stage updates to submodules marked with `ignore=all`
2. The submodule path must be explicitly specified when using this option
3. The `ignore=all` setting affects multiple commands (`status`, `update-index`, `diff`, `log`) through their underlying diff operations

The documentation updates match the implementation changes from previous patches, particularly the warning system and the new option's behavior. The patch also includes unrelated Dockerfile additions that appear to be development environment setup files, though these seem incidental to the main documentation purpose.

This completes the documentation side of the submodule ignore behavior changes, following through on the implementation decisions made in earlier patches. The documentation maintains consistency with the technical implementation while making the new behavior clear to end users.