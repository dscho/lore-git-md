Follow-up patch in the submodule hash-algorithm validation thread. This is a code cleanup from Jeff King that removes a redundant submodule HEAD resolution check from `add_to_cache()`, since the same check is already done (and now improved) in `index_path()`. 

The change eliminates duplicate work when adding submodules to the index and centralizes the error handling. The patch maintains the same user-visible behavior but with slightly more verbose error messages as the error propagates up the call chain. Jeff notes this is actually an improvement over the original implementation from 2019, which should have put the error handling in `index_path()` to begin with.

The patch is straightforward - it removes the submodule resolution code from `read-cache.c` while enhancing the error reporting in `object-file.c`. Test updates confirm the new error message format works as expected. Junio agrees this is a good cleanup that stands on its own merits, separate from the main hash-algorithm validation work.

This appears to be a well-considered improvement to the codebase that removes redundancy while making the error handling more consistent. It's a small but meaningful cleanup in the context of the larger submodule hash-algorithm discussion.