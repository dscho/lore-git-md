This is a follow-up discussion on the final patch of Junio's completed series introducing incomplete-line whitespace checking.

Junio confirms his intentional exclusion of documentation files from the new incomplete-line checks in response to Phillip Wood's question about extending the checks. He clarifies that the current implementation (which only checks source files via explicit `.gitattributes` rules) was deliberate, noting the series doesn't update `WS_DEFAULT_RULE` to include `WS_INCOMPLETE_LINE`. Junio humorously suggests leaving documentation file checks as future work, implying he wants the current changes to stabilize first.

The exchange shows the series is now considered complete with this intentional scope limitation, though the door remains open for someone to extend the checks to documentation later. This aligns with the thread's overall status where all core functionality is merged, with only non-blocking refinements being discussed post-merge.