This is the final edge case discussion in the submodule gitdir path encoding series, focusing on handling all-lowercase collisions. Aaron Schrab responds to Adrian Ratiu's proposed solutions for when a new all-lowercase submodule ("foo") conflicts with an existing one ("Foo") on case-folding filesystems.

Aaron highlights that the impact is broader than just during `git submodule add` - existing clones would also need manual intervention when pulling the new submodule, which could be problematic for automated environments. He notes that even new clones might require configuration unless submodule setup carefully orders entries, which seems unlikely given common collation rules.

The discussion centers on two resolution approaches: Adrian's suggestion to automatically append a suffix ("foo2" or "foo_") versus documenting this as a known limitation requiring manual `submodule.<name>.gitdir` configuration. Aaron mildly prefers "foo2" as clearer but acknowledges the scenario will be rare. The technical context remains unchanged from the series' established architecture using URL percent-encoding and `submodule.<name>.gitdirpath` as authoritative mapping.

Key technical points for future reference:
- Conflict scenario: all-lowercase submodule addition conflicting with existing case-variant
- Current implementation would require manual `submodule.foo.gitdir` config
- Proposed automatic alternatives: append "2" or "_" suffix
- Impact affects not just `submodule add` but also existing clones
- Collation order concerns with `C` locale putting capitals first
- Maintains existing path resolution via `submodule_name_to_gitdir()`
- Still uses core.ignorecase and validate_submodule_git_dir() for detection

The thread appears to be converging toward implementing automatic suffix solution rather than documenting as a limitation, though no final decision is made in this email. This would represent the last code change needed before the series can be merged.