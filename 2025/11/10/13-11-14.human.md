Bugfix patch in the `git fetch` tag handling series. This is the first of two patches addressing a regression where non-conflicting tags would fail to commit if any conflicts existed in the same batched reference transaction.

Karthik Nayak responds to Patrick Steinhardt's review feedback on the refactoring portion of the fix. The patch extracts reference transaction committing logic into a new `commit_ref_transaction()` helper function. Patrick suggested simplifying the control flow by using a `goto out` pattern rather than nested conditionals, which Karthik agrees makes the code clearer and will incorporate.

The change is purely structural - moving existing logic into a helper function with improved flow - in preparation for the actual bugfix in patch 2/2. The helper centralizes transaction commit handling with parameters for atomicity flag, remote name context, and error reporting. This refactoring follows the common Git practice of separating mechanical changes from functional ones to make review easier.

The thread shows good collaborative refinement, with Karthik being responsive to feedback while maintaining focus on the core regression fix. The series appears well-tested and nearly ready for integration after this final control flow improvement.