This is a follow-up discussion confirming the performance benefits of the recently merged bugfix for `git diff`'s quiet mode behavior with rename/copy detection flags.

Phillip Wood acknowledges D. Ben Knoble's performance measurements showing the fix provides a 3.6x speedup when using `--quiet` with `--find-copies-harder`. The original issue caused incorrect exit codes when these flags were combined with `--cached --no-ext-diff`, which has now been resolved by René Scharfe's patch that disables rename detection entirely in quiet mode.

The email serves as validation of the solution's effectiveness, with Phillip noting that while the performance impact of `--find-copies-harder` was theoretically understood, seeing concrete measurements makes the improvement more apparent. This wraps up the thread with all technical questions answered and the solution's benefits quantitatively confirmed.