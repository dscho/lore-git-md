This is a post-merge review comment on the final patch of Junio C Hamano's whitespace enforcement series, focusing on documentation file coverage. Phillip Wood acknowledges the core change enabling incomplete-line checks for C/header files and shell scripts via `.gitattributes`, then raises a follow-up question about extending the same checks to documentation files.

The key observation is that while the series added `WS_INCOMPLETE_LINE` support throughout Git's diff/apply pipeline, it didn't update `WS_DEFAULT_RULE` to include this check globally. This means documentation files would only get incomplete-line checking if explicitly listed in `.gitattributes`, unlike other whitespace rules that apply by default. Phillip's question suggests considering whether documentation files should receive the same treatment as code files regarding newline enforcement.

The comment comes after the series has already been merged, indicating this is a potential refinement rather than a blocking issue. It continues Phillip Wood's pattern of thorough post-merge review, though more briefly than his previous technical suggestions on test cases and implementation details. The question implies the current implementation may be intentionally conservative in its scope, leaving room for future expansion to other file types if desired.