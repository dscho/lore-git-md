This is a follow-up discussion in the bugfix thread about `git diff` exit status behavior with `--find-copies-harder` and `--quiet`. 

René Scharfe concurs with Phillip Wood's architectural question about whether rename/copy detection should be disabled entirely in quiet mode, as detecting renames shouldn't affect the exit code. This suggests the current fix (disabling just the quick optimization when find_copies_harder is set) might be addressing a symptom rather than the root issue. 

The exchange indicates potential for revisiting the diff machinery's behavior in quiet mode more fundamentally, though no concrete proposal is made yet. Both developers agree there appears to be no good reason for rename detection to influence exit codes in quiet mode, implying the current behavior may be an oversight in Git's design rather than an intentional feature. 

This moves the thread's focus from the immediate bugfix (already implemented) to considering a more comprehensive solution that would change how quiet mode interacts with expensive diff operations. The discussion remains at the conceptual level, with no patches or implementation details yet proposed for the broader change.