New AI thread summary:

This thread introduces a new `--committer` option for `git commit` to provide symmetry with the existing `--author` option. The patch series (currently v1) adds functionality to override the committer identity directly via command line, mirroring how `--author` works today. The implementation in `builtin/commit.c` follows the same pattern as the author handling, adding `determine_committer_info()` parallel to `determine_author_info()`. 

The change addresses an interface inconsistency where committer identity could only be set via environment variables (GIT_COMMITTER_NAME/GIT_COMMITTER_EMAIL) while author had both env vars and a direct option. The patch includes comprehensive test coverage in t/t7509-commit-authorship.sh with 8 new tests verifying basic functionality, pattern matching behavior (looking up committer from existing commits), error handling, and interactions with `--amend` and date overrides. Documentation updates in git-commit.adoc explain the new option's syntax and behavior.

Key technical details:
- New option accepts same formats as `--author`: explicit "Name <email>" or existing commit lookup
- Maintains current precedence: command line > environment > config > automatic
- Handles edge cases like malformed input and invalid commit references
- Preserves existing behavior when not overridden
- No changes to on-disk format or backward compatibility

The patch appears well-scoped and follows established patterns, suggesting it should integrate cleanly. No reviewers have commented yet on this initial version. The change seems motivated by real-world scripting and multi-identity workflows where direct committer control would be useful.