This is a follow-up discussion on a bugfix for `git diff` exit status behavior with specific flag combinations. Phillip Wood questions the underlying design decision while acknowledging René Scharfe's fix.

The thread had previously identified and fixed a bug where `git diff --cached --no-ext-diff --find-copies-harder --quiet` incorrectly returned exit code 0 when changes existed. The fix disabled a quick-exit optimization when `find_copies_harder` is active, ensuring consistent behavior between `--quiet` and `--exit-code` modes.

Phillip raises a new architectural question: why does Git perform rename/copy detection at all when `--quiet` is specified, since those operations shouldn't affect the exit code. This suggests the current solution might be addressing a symptom rather than the root cause. The question implies the optimization might be better handled by skipping rename detection entirely in quiet mode rather than special-casing `find_copies_harder`.

The technical context remains focused on `diff.c`'s quick-exit optimization path and the interaction between `flags.quick`, `flags.has_changes`, and `flags.find_copies_harder`. The discussion now shifts from verifying the specific fix to examining broader design implications of the diff machinery's optimization behaviors.

No new code changes are proposed in this email - it's purely a design discussion probing whether the current solution aligns with Git's architectural principles. The question could lead to revisiting how quiet mode interacts with expensive diff operations more generally.