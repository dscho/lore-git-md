This thread introduces a new `--committer` option for `git commit`, mirroring the existing `--author` functionality. The patch provides a symmetrical way to override the committer identity directly from the command line, rather than requiring environment variables. The implementation carefully follows the same patterns as `--author`, including support for both explicit "Name <email>" format and pattern-based lookup of existing commits.

The change addresses a long-standing inconsistency in the commit command's identity handling. The patch includes comprehensive test coverage (8 new test cases) that verifies basic functionality, error handling, and interactions with related features like `--amend` and environment variables. The implementation appears well-considered, with attention to edge cases and maintaining consistency with existing behavior. This should be particularly useful for scripting and multi-identity workflows where direct command-line control over committer information is needed.