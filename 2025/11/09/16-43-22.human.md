This is a follow-up discussion in the bugfix thread about `git diff` exit codes with `--find-copies-harder` in quiet mode.

René Scharfe concurs with Phillip Wood's architectural question about whether rename/copy detection should be disabled entirely in quiet mode, since these operations shouldn't affect the exit code. The discussion is moving beyond the immediate fix (which correctly handles the reported edge condition) to consider whether the diff machinery's quiet mode should skip expensive rename detection entirely.

The exchange suggests the current solution may be treating a symptom rather than addressing a deeper design question. While the thread started with a concrete bug report about incorrect exit codes, it's now exploring a potential optimization that could simplify the diff code's quiet mode behavior more fundamentally. This could lead to follow-up patches if others agree with Phillip and René's assessment.