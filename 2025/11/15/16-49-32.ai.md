Maintainer response in UTF-8 alignment discussion. Junio C Hamano acknowledges Phillip Wood's concerns about non-UTF-8 encoding support in the table alignment fixes, confirming this is a known limitation affecting multiple Git components like `diff --stat`. The response clarifies this is a systemic issue rather than a new problem introduced by Jiang Xin's patch series.

Junio suggests the traditional approach of using `utf8_strwidth()` for measurement combined with manual `printf()` padding would be preferable to `strbuf_utf8_align()`, though he doesn't outright reject the current implementation. This maintains the thread's technical focus while indicating maintainer preferences for future iterations.

Key technical points carried forward:
- Confirmation that UTF-8 width calculation limitations affect multiple Git components
- Preference expressed for manual padding with `printf()` over `strbuf_utf8_align()`
- Implicit acceptance that non-UTF-8 encoding support remains an open issue

The response moves the discussion toward implementation preferences while acknowledging the broader encoding challenges, setting up potential directions for v3 of the patch series. No new test requirements are introduced in this message, keeping the focus on the core alignment mechanism debate.