Bugfix follow-up in UTF-8 alignment thread. Phillip Wood responds to Jiang Xin's v2 patch fixing table alignment for CJK characters in `builtin/repo.c`, suggesting an alternative approach to using `strbuf_utf8_align()`.

The core observation is that while Jiang's solution works for UTF-8, it doesn't address other character encodings. Phillip proposes keeping `printf()` but manually calculating padding widths using `utf8_strwidth()`, showing a diff that demonstrates this approach. Both methods would require calling `utf8_strwidth()` twice per string, but Phillip notes this overhead is likely negligible.

This is a technical refinement discussion on an already well-reviewed patch series. Phillip's suggestion offers a different tradeoff - slightly more verbose code that might be more encoding-agnostic, versus Jiang's current UTF-8-specific solution. The thread shows active collaboration to find the optimal implementation for character alignment in Git's output tables.