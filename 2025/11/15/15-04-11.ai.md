This is a follow-up discussion on the UTF-8 alignment fix for `builtin/repo.c` output tables. Phillip Wood raises two technical points about Jiang Xin's v2 patch:

1. **Encoding Awareness**: Questions how the solution ensures proper alignment for non-UTF-8 encodings, noting that display width calculation requires knowing the encoding. This suggests the current limitation where the patch assumes UTF-8 without handling other encodings.

2. **Implementation Alternative**: Proposes an alternative approach using `printf()` with manual padding calculations via `utf8_strwidth()`, showing a diff that would maintain the same alignment behavior while potentially being less verbose than the `strbuf_utf8_align()` solution. The tradeoff is calling `utf8_strwidth()` twice per string.

The discussion continues the thread's focus on robust CJK character handling while surfacing new considerations about encoding assumptions and implementation choices. Phillip's feedback doesn't challenge the core alignment fix but explores edge cases and alternative implementations. The thread remains technically focused, with maintainers and contributors collaborating to refine the solution.