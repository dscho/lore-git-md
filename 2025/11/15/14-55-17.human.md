This is a discussion about the legacy `git whatchanged` command and its deprecation. Kristoffer Haugsbakk responds to Chia-Sheng Chang's note about still using the command by explaining that it's being removed since it's essentially redundant with `git log` functionality.

The response provides clear migration guidance, noting that `git whatchanged` can be replaced with `git log --no-merges --raw` to get equivalent output. The email helpfully suggests alternative flags like `--stat` or `--name-only` for human-readable output rather than the machine-oriented `--raw` format.

The exchange highlights an ongoing effort to simplify Git's command set by removing redundant legacy commands, though the tone remains helpful rather than critical of the continued use of `git whatchanged`. The response is practical and solution-oriented, providing immediate value to anyone still using the deprecated command.