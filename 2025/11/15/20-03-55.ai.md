This is a technical discussion thread continuation regarding the xdiff refactoring series' documentation of type mappings between C and Rust, specifically focusing on the semantics of `char` type comparisons.

The email captures Junio Hamano and Ben Knoble's agreement that `-Wsign-compare` warnings about `char` comparisons reflect limitations in the warning system rather than inherent type safety issues. They note that these warnings occur regardless of whether `char` is compared with signed or unsigned integers, supporting Ezekiel Newren's earlier justification (from part 2/10) for using "signless" terminology based on `DEVELOPER=1` warnings.

Key technical details include:
- Confirmation that `char`'s implementation-defined signedness (per C standard 6.2.5, 6.3.1.1) makes warning behavior particularly quirky
- Agreement that the core issue was Ramsay Jones' objection to the non-standard term "signless" rather than the underlying technical approach
- Recognition that the warning behavior occurs consistently regardless of comparison direction (char vs signed/unsigned integers)

This discussion represents final alignment on documentation approach for the type mapping document (unambiguous-types.adoc), with all parties now understanding:
1. The compiler warning behavior is a separate concern from actual type safety
2. The project will document `char`'s implementation-defined nature while maintaining practical warning handling
3. Junio Hamano's position on preserving `char` semantics in `get_indent()` remains unchanged

The thread shows the series reaching consensus on one of its last contentious points, with maintainers and contributors now aligned on how to document and handle `char` type behavior in the context of Rust FFI preparation.