This is a brief maintainer comment on Kiril Ivanov's bash prompt quiet mode series, rather than a substantive review of the patches themselves. Junio Hamano notes that while the patches themselves are fine (they add a quiet mode for upstream indicators in the bash prompt), the test script `t/t9903-bash-prompt.sh` still uses `#!/bin/sh` despite containing bash-specific features. He suggests updating the shebang to `#!/bin/bash` since the entire test is focused on bash-specific prompt behavior anyway.

This is a minor housekeeping observation that doesn't affect the functionality of Kiril's proposed changes. The comment doesn't request any changes to the current patch series, but rather points out a small inconsistency in the test infrastructure that could be addressed separately. The bash prompt series itself appears to be progressing normally with its first version posted and awaiting review feedback.