This is a response in the ongoing thread about adding a `--committer` option to `git commit`, addressing maintainer feedback on the v3 patch. ZheNing Hu responds to Junio Hamano's specific technical critiques while maintaining the argument for interface consistency.

The email focuses on three implementation-level concerns from Junio's review:
1. Documentation phrasing ("override" vs "set/use") - ZheNing notes the current wording matches `--author` and `--date` for consistency but accepts the change
2. The `is_author` boolean parameter - Agrees to change to use the existing `enum want_ident`
3. Author-specific parsing logic in the shared function - Acknowledges this should be separated out

Key technical details for future reference:
- The shared `determine_identity()` function currently handles both author and committer cases
- Author-specific message parsing (from `author_message`) remains entangled in this function
- Variable renaming (`ident` -> `msg_ident`) created unnecessary diff noise
- Proposed solution involves moving author-specific logic to a new `set_author_from_message()` helper

The response shows ZheNing accepting most technical criticisms while maintaining the philosophical position that `--committer` should mirror `--author`'s interface. The discussion remains at an impasse between interface symmetry arguments and maintainer skepticism about the feature's fundamental value. Implementation quality concerns are being addressed, but the core disagreement about whether this functionality belongs in Git persists.