This is a refactoring patch in the final stages of approval for the SHA1/SHA256 submodule validation series. The patch removes duplicate submodule HEAD resolution from `add_to_cache()`, consolidating this logic in `index_path()` where it properly handles all cases including hash algorithm validation. Jeff King originally authored the change to address several issues: redundant work (opening submodule repos twice), missed error cases in some code paths, and confusion around where object-format validation occurs.

The technical change moves error reporting for submodule resolution failures to `index_path()`, which already had most of the necessary validation logic. This creates a single point of truth for submodule indexing while maintaining the same error messages. Junio C Hamano's response focuses on the DCO sign-off process, noting that while brian m. carlson could add their own sign-off as the patch submitter, this appears to be optional per the SubmittingPatches documentation.

Key implementation details:
- Removes `resolve_gitlink_ref()` call from `add_to_cache()`
- Relies on existing validation in `index_path()`
- Preserves original error message about unresolved submodule HEAD
- Handles all submodule resolution cases including hash algorithm mismatch
- The patch is now fully approved and queued for merging
- Completes the technical solution for preventing mixed-hash repositories
- Maintains connection to broader SHA-256 transition work
- Final discussion point about DCO sign-off practices doesn't affect technical approval