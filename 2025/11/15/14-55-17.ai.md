This is a follow-up in the `git whatchanged` discussion thread, providing technical details about the command's replacement. Kristoffer Haugsbakk responds to Chia-Sheng Chang's initial note about still using `git whatchanged` by explaining its deprecation status and equivalent `git log` replacement.

The email clarifies that `git whatchanged` is being removed as it's functionally redundant with `git log --no-merges --raw`, differing only in default output format. It provides specific migration instructions showing how to replicate `git whatchanged` behavior using `git log` flags, and suggests alternative output formats (`--stat` or `--name-only`) that may be more human-readable depending on use case.

Key technical details for future reference:
- The exact replacement syntax: `git log <opts> --no-merges --raw`
- Suggested alternatives for human consumption: `--stat` and `--name-only`
- The fundamental equivalence between the commands' underlying machinery
- The deprecation is framed as long-standing ("a long while ago")

The response appears technically accurate and helpful for users needing to transition from `git whatchanged`, though it doesn't address why the command is being removed now or whether there are any edge cases where the replacement might differ. The thread now has concrete migration guidance that could serve as a reference point for any future discussion about the removal's impact.