This is a bugfix patch in Jeff King's ASan series, addressing memory safety in timestamp parsing within `fsck_ident()`. The patch avoids using `parse_timestamp()` (which internally uses `strtoumax()`) on potentially non-NUL-terminated buffers, working around ASan's strict_string_checks while maintaining overflow detection.

Key technical details:
- The current code relies on a trailing newline to bound parsing, which is fragile and triggers ASan warnings
- The solution copies digits into a NUL-terminated buffer before parsing
- Discussion reveals deeper issues with integer parsing utilities:
  - Existing `git_parse_int()` helpers don't fit this use case due to interface mismatches (strict full-string parsing vs progressive parsing)
  - Fundamental differences in signed/unsigned handling and overflow detection
  - Potential behavior changes if replacing `strtoimax()` with custom parsing

The email pushes back on Patrick Steinhardt's suggestion to generalize parsing utilities, arguing that the problem space is more complex than initially apparent. This continues the thread's broader discussion about parsing patterns while maintaining focus on the immediate ASan fix. The patch itself is validated by Patch 9's enabling of strict string checking.