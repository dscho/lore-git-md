This is a technical discussion in the reftable optimization thread, where Junio Hamano raises concerns about code duplication in the compaction checking logic. The thread has already established that the series implements a new 'is-needed' subcommand for `git maintenance` to check-if-needed functionality for ref optimizations.

Junio questions why the new `reftable_stack_compaction_required()` function in patch 2/5 appears to implement fresh logic rather than reusing existing auto-compaction decision code from `reftable_stack_auto_compact()`. Karthik explains that the new function combines aspects of both `reftable_stack_auto_compact()` (heuristic-based) and `reftable_stack_compact_all()` (full compaction), parameterized via `use_heuristics` to align with `REFS_OPTIMIZE_AUTO` flag usage.

Junio expresses strong concern about maintaining three separate implementations of compaction logic that could drift out of sync, suggesting a refactor to centralize this logic instead of documenting the duplication. This represents the final open discussion point in the otherwise technically resolved series, with Junio pushing for architectural consistency before final acceptance. The exchange highlights the maintainer's focus on long-term maintainability even for approved functionality.