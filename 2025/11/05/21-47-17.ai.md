This is a simple documentation patch fixing a minor formatting issue in error messages. Peter Krefting submits a two-line change that adds a missing space after a single quote in error messages from both the files and reftable ref backends. The messages appear when attempting to write reflog entries with incomplete values (missing old/new values or REF_LOG_ONLY flag). The patch is straightforward and touches only the message formatting - it doesn't affect functionality. Both backend implementations (files and reftable) receive the same correction, maintaining consistency in error reporting. The change is minimal and uncontroversial, just adding proper spacing in user-facing error strings.