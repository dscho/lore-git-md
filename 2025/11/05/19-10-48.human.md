This is the final patch in a series implementing atomic reference updates for `git replay`. The patch adds a `replay.refAction` configuration option that lets users set their preferred default behavior (atomic updates or pipeline output) rather than specifying `--ref-action` each time.

The implementation follows Git's standard configuration patterns, with command-line options overriding config settings. The patch includes thorough tests verifying the config option works as expected, including proper error handling for invalid values. 

The author responds to review feedback by:
- Removing redundant implementation details from the commit message
- Moving some test cleanup fixes to their proper locations in earlier patches
- Keeping the essential documentation and test coverage

This patch represents the last piece of the atomic updates feature, which has gone through multiple rounds of review with all feedback addressed. The series is now merge-ready, with this final patch providing the expected configuration option to complement the command-line control added earlier.