This is a technical discussion about the reftable backend optimization in the ongoing `git maintenance is-needed` series. Junio Hamano questions the implementation approach for checking when reftable compaction is needed, noting that the new function appears to duplicate existing logic from `reftable_stack_auto_compact()` and `reftable_stack_compact_all()` rather than consolidating it.

The core concern is about maintaining consistency - having three separate implementations of similar compaction logic risks subtle behavioral differences. Junio suggests refactoring to have a single shared implementation rather than documenting the current state of duplication in the commit message. This comes after Karthik explained that the new function combines aspects of both existing functions while adding parameterization for heuristic-based vs full compaction modes.

The exchange highlights an important architectural consideration as this series nears completion, with Junio pushing for cleaner code organization before finalizing the implementation. This is a focused discussion about internal implementation details rather than any fundamental issues with the feature's design.