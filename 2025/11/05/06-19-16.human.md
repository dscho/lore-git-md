This is the first of three patches in a series introducing a new 'strip-if-invalid' mode for `git fast-import`'s signed commits functionality. The patch performs preparatory refactoring by extracting commit buffer finalization code into a new `finalize_commit_buffer()` function.

The change moves the logic for adding GPG signatures and commit messages into a dedicated function, making the code more modular. This refactoring will enable the subsequent patches to more easily modify how signatures are handled when verifying commits during import. The patch is purely structural - it doesn't change any behavior yet, just reorganizes the code to make the actual feature implementation cleaner in later patches.

The implementation looks straightforward, moving the signature and message handling code from `parse_new_commit()` into the new function while maintaining all existing functionality. This is a typical preparatory refactoring that makes the actual feature patch easier to review by separating mechanical changes from logical ones. The commit message clearly explains the motivation for the change in the context of the upcoming series.