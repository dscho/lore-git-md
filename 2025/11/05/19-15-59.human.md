Refactoring patch for `git replay` option validation, part of the atomic ref updates series.

This patch prepares for the upcoming `--ref-action` option by standardizing how `git replay` handles incompatible options. It replaces a custom error message with Git's standard `die_for_incompatible_opt2()` helper for validating the mutually exclusive `--advance` and `--contained` options. 

The change makes the code more maintainable by using the same pattern that will be needed for the new `--ref-action` option in subsequent patches. This aligns with Git's established practices for option validation and keeps error messages consistent across commands. The patch is straightforward, touching only a few lines in `builtin/replay.c` to replace the direct `die()` call with the helper function.

As part of the well-reviewed atomic ref updates series that's now in its final version, this change follows the same high standard of code quality seen throughout the series. It's a small but meaningful improvement to the codebase that will make future option additions cleaner.