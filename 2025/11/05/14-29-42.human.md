Refactoring patch in a series adding `--trailer` support to `git rebase`. This is the second of four patches that move trailer processing in-process rather than forking to `interpret-trailers`.

The patch relocates the `process_trailers()` function from `builtin/interpret-trailers.c` to `trailer.c` and exposes it in `trailer.h`. This is a straightforward code movement with no behavior changes, enabling the function to be reused by other parts of Git (specifically by `trailer_process` in upcoming patches). The diff shows a clean cut-and-paste of the function between files.

This continues the series' clean separation of concerns, with each patch building on the last. The change is well-contained (36 lines moved, 36 lines removed) and maintains all existing functionality while setting up for the in-process trailer handling that will follow in patches 3 and 4. The series has already undergone several rounds of review and this particular patch appears uncontroversial.