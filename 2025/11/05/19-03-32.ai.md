This is a final polish patch in the atomic ref updates series for `git replay`, addressing naming consistency and code organization nits from Christian Couder's review. The patch focuses on two main changes: first, making variable naming more consistent by using `ref_action` for string parameters and `ref_mode` for the enum values throughout the code, and second, moving the `parse_ref_action_mode()` helper function to an earlier commit in the series where it's first used for validation.

The changes are straightforward and uncontroversial, aligning with the series' goal of clean, maintainable code. Siddharth Asthana confirms they'll implement both suggestions in v7 of the series - the function relocation and the variable standardization. This leaves the core atomic update functionality unchanged while improving code hygiene, continuing the pattern seen throughout the review process where the maintainers have focused on such polish items after approving the core technical approach.

The patch maintains all existing behavior for the `replay.refAction` config option and `--ref-action` command-line flag, which control whether updates are applied atomically or just printed. With these final naming tweaks, the series appears ready for merging once the reflog message format tests (being handled separately) are completed.