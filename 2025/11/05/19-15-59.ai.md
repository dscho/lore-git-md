This is a refactoring patch in the atomic ref updates for `git replay` series. It prepares for adding the new `--ref-action` option by standardizing the command's option validation to use Git's `die_for_incompatible_opt2()` helper function.

The patch changes how `git replay` reports conflicts between the mutually exclusive `--advance` and `--contained` options, replacing a custom error message with the standard helper that provides consistent formatting across Git commands. This is groundwork for the next patch in the series which will introduce `--ref-action` and need similar validation logic.

The change is mechanical - it replaces three lines of custom validation code with a call to the standard helper while maintaining identical behavior. The commit message explains this is part of aligning `git replay` with Git's standard option handling patterns, particularly in preparation for the upcoming `--ref-action` option that will be introduced in the following commit.

This appears to be a straightforward, uncontroversial change that follows established Git patterns. The patch is small (3 line change) and focused, with a clear explanation of its purpose in the context of the larger series. Since this is part of the v7 submission that has already received maintainer approval, this change has presumably been reviewed and found acceptable as part of that process.