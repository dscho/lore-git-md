This is the third patch in a series adding in-process trailer handling to `git rebase`, eliminating the need to fork to `interpret-trailers`. The patch refactors trailer processing to work directly with buffers rather than files, removing temporary file handling and simplifying error paths.

The key change moves the core trailer processing logic into `amend_strbuf_with_trailers()`, which operates on a memory buffer rather than files. This allows `amend_file_with_trailers()` to read the file once, process it in memory, and write it back, avoiding the previous approach of forking to `interpret-trailers`. The patch also adds a new `write_file_buf_gently()` helper for atomic file writes with proper error handling.

The changes are focused on internal plumbing - there's no user-visible behavior change, but the elimination of process forking should improve performance. The patch maintains all existing functionality while setting up the infrastructure needed for the final rebase integration in the series. Test coverage appears thorough, with the actual tests coming in the final patch.

This continues the series' pattern of careful, incremental refactoring - each patch has built on the last to move trailer processing in-process while maintaining identical behavior. The changes here look well-contained and properly motivated by the series' overall goal.