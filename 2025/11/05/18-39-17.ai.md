This is a preparatory refactoring patch in the atomic ref updates for `git replay` series. The patch converts existing option validation in `builtin/replay.c` to use the standard `die_for_incompatible_opt2()` helper function, replacing a custom error message for mutually exclusive `--advance` and `--contained` options. 

The change is part of patch 1/3 in v6 of the series, which has already received maintainer approval with all technical concerns addressed. This particular patch was split out to make the subsequent introduction of the `--ref-action` option cleaner, as noted in the commit message. The refactoring ensures consistent error handling patterns with other Git commands.

The thread context shows this is a minor cleanup in an otherwise merge-ready series, with Elijah Newren having already approved this change. The patch touches only 3 lines in `builtin/replay.c`, switching from direct `die()` to the standard helper while maintaining identical functionality. This aligns with the series' overall goal of standardizing and improving `git replay`'s implementation patterns.