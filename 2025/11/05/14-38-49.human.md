This is a documentation patch clarifying server behavior in the HTTP protocol. 

Queen Ediri Jessa's patch updates the Git HTTP protocol documentation to explicitly state how servers handle invalid `want` lines from clients. The documentation now specifies that when a server receives a malformed `want` request, it will respond with an error message that includes the problematic object name.

This is version 4 of the patch, which has been simplified from previous versions by removing some explanatory text based on reviewer feedback. The change is straightforward and uncontroversial - it simply documents existing behavior rather than proposing any functional changes.

The patch touches only the `gitprotocol-http.adoc` file, adding two lines to replace a TODO marker. This appears to be a well-scoped improvement to the protocol documentation, making it clearer for both implementers and users of the HTTP protocol.