Refactoring patch in the whitespace error detection series. This is patch 2/12 in Junio Hamano's series introducing "incomplete-line" whitespace handling, focused on making the code style consistent in `diff.c`.

The change is purely stylistic - it standardizes the brace usage in an if/else cascade in the `emit_line_ws_markup()` function. The rule being applied is simple: if any branch of an if/else chain needs braces, then all branches should use braces for consistency. This makes the code more uniform and easier to maintain.

While minor, this cleanup is part of a larger, carefully constructed series that's adding new whitespace error detection capabilities. The author is following good practice by separating mechanical style fixes from behavior changes in these early preparatory commits. The patch does what it claims with no functional impact, and fits cleanly into the series' progression.