Here is the dense AI thread summary:

This thread introduces a new 'strip-if-invalid' mode for `git fast-import`'s `--signed-commits` option, with potential future extensions to signed tags. The series (currently v1) consists of 3 patches that refactor commit buffer handling in fast-import (patch 1), extract and modify commit verification logic from commit.c (patch 2), and implement the new signature stripping mode (patch 3). The changes span fast-import/export machinery, commit verification, and GPG interface code, with test coverage expanded by over 100 lines in t9305.

Technical details include: new `strip_if_invalid` mode for `--signed-commits`, refactored `finalize_commit_buffer()` in fast-import, extracted `verify_commit_buffer()` from commit.c with new stripping capability, new `GPG_VERIFY_STRIP_IF_INVALID` flag in gpg-interface.h. The implementation cleanly separates refactoring from feature introduction.

Open questions: whether this should become default behavior (deferred for future discussion) and potential extension to `--signed-tags`. The changes are motivated by needs of tools like `git filter-repo` that want to automatically strip invalid signatures during import while preserving valid ones. The series does not yet have any Reviewed-by or Acked-by tags.