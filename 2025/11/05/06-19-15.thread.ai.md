Here is the updated dense AI thread summary:

This thread introduces a new 'strip-if-invalid' mode for `git fast-import`'s `--signed-commits` option, with potential future extensions to signed tags. The series (now complete at v1) consists of 3 patches that refactor commit buffer handling in fast-import (patch 1), extract and modify commit verification logic from commit.c (patch 2), and implement the new signature stripping mode (patch 3). The changes span fast-import/export machinery, commit verification, and GPG interface code, with test coverage expanded by over 100 lines in t9305.

Technical details include: new `strip_if_invalid` mode for `--signed-commits`, refactored `finalize_commit_buffer()` in fast-import (now extracted from `parse_new_commit()` with identical behavior), extracted `verify_commit_buffer()` from commit.c (operating directly on commit buffers rather than requiring full commit objects), new `GPG_VERIFY_STRIP_IF_INVALID` flag in gpg-interface.h, and new `SIGN_STRIP_IF_INVALID` enum value. The implementation cleanly separates refactoring (patch 1's buffer handling extraction and patch 2's verification extraction) from feature introduction (patch 3's signature stripping).

The verification refactoring in patch 2 splits the logic into two layers: lower-level `verify_commit_buffer()` handles GPG checking on raw buffers while `check_commit_signature()` becomes a thin wrapper that fetches buffers and delegates. Patch 3 implements the new `handle_strip_if_invalid()` helper in fast-import.c that verifies signatures and conditionally strips them, with comprehensive test coverage for various signature types (OpenPGP, X.509, SSH) and scenarios. Warning messages include commit subject and signer info when invalid signatures are stripped.

Open questions remain about whether this should become default behavior (deferred for future discussion) and potential extension to `--signed-tags`, which is explicitly not implemented in this series. The changes are motivated by needs of tools like `git filter-repo` that want to automatically strip invalid signatures during import while preserving valid ones. The series does not yet have any Reviewed-by or Acked-by tags. Error handling rejects attempts to use the mode where unsupported (tags, fast-export). Documentation is updated to describe the new mode alongside existing signature handling options.