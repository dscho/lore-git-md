Here is the updated dense AI thread summary:

This thread introduces a new 'strip-if-invalid' mode for `git fast-import`'s `--signed-commits` option, with potential future extensions to signed tags. The series (v1) consists of 3 patches refactoring commit buffer handling in fast-import (patch 1), extracting verification logic from commit.c (patch 2), and implementing signature stripping (patch 3). Changes span fast-import/export, commit verification, and GPG interface code, with 100+ new test lines in t9305.

Technical details include: new `strip_if_invalid` mode, refactored `finalize_commit_buffer()` in fast-import, extracted `verify_commit_buffer()` from commit.c (operating on raw buffers), new `GPG_VERIFY_STRIP_IF_INVALID` flag, and `SIGN_STRIP_IF_INVALID` enum. Patch 2 splits verification into buffer-level `verify_commit_buffer()` and object-wrapper `check_commit_signature()`. Patch 3 implements `handle_strip_if_invalid()` with comprehensive test coverage for OpenPGP/X.509/SSH signatures.

Maintainer Junio C Hamano raised fundamental concerns about: (1) definition of "invalid" regarding missing keys/revocations, (2) clock accuracy impacts, and (3) cascading invalidation of subsequent signatures in rewritten history. Elijah Newren's response provides additional motivation from `git filter-repo` use cases where users want to rewrite recent history while preserving older valid signatures. Christian Couder has now clarified in two emails that the validity check uses the same definition as `git verify-commit` (returning 0 for valid), assumes availability of all necessary public keys/revocation data (like verify-commit), and acknowledges but doesn't yet address cascading invalidation concerns.

The discussion with Newren reinforces that signature validation must occur at import time to properly handle cases where validity changes between export and import. Couder agrees with Newren's technical assessment that fast-import is the only point where signature validity can be properly assessed, while pushing back on Newren's alternative proposal of tracking unsigned OIDs in the export/import format. The exchange highlights the tension between cryptographic purity (where any history rewrite invalidates all downstream signatures) and practical use cases (where users want to selectively preserve signatures).

A test infrastructure issue was identified where patch 3's tests fail when merged due to depending on SHA-256 repositories created by another test that now requires Rust support (f6581e23). The signature-stripping functionality itself doesn't need dual-hash interoperability. The maintainer suggests restructuring tests to either make SHA-256 repository creation part of test setup (with prerequisites) or avoid SHA-256 repositories entirely.

Open questions remain about: whether the approach is fundamentally sound given maintainer concerns, potential format changes to fast-export/import to support unsigned OID tracking (as proposed by Newren), default behavior should change, and extension to signed tags. The test dependency issue must be resolved before merging. The series lacks Reviewed-by/Acked-by tags. Error handling rejects unsupported uses (tags, fast-export). Documentation describes the new mode alongside existing options.

Christian Couder plans to include the validation behavior clarifications in v2 while deferring broader documentation improvements (possibly a new gitsignature(7) page) to future work. The discussion continues to weigh technical solutions that could address both the original use case (selective signature preservation) and maintainer concerns about cryptographic integrity, with the test infrastructure issue remaining a practical merge blocker. Naming considerations for alternative approaches have emerged as an additional design factor.