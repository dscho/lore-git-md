Here is the updated dense AI thread summary incorporating the new email:

This thread introduces a 'strip-if-invalid' mode for `git fast-import`'s `--signed-commits` option, with tag support deferred for future work. The v2 series maintains its 3-patch structure: refactoring commit buffer handling in fast-import (patch 1), extracting verification logic from commit.c (patch 2), and implementing signature stripping (patch 3). Changes span fast-import/export, commit verification, and GPG interface code, with 100+ new test lines in t9305.

The first patch (now re-sent) is a mechanical refactoring that extracts commit buffer finalization logic into `finalize_commit_buffer()`, moving signature handling code from `parse_new_commit()`. This prepares for signature verification/stripping while maintaining current SHA-1/SHA-256 signature format support. The change is isolated to `builtin/fast-import.c` and enables code reuse in subsequent patches.

Technical implementation includes new `strip_if_invalid` mode, refactored `finalize_commit_buffer()`, extracted `verify_commit_buffer()` operating on raw buffers, `GPG_VERIFY_STRIP_IF_INVALID` flag, and `SIGN_STRIP_IF_INVALID` enum. Key v2 changes: rebased to avoid translation conflicts, removed dual-signature tests conflicting with Brian Carlson's Rust work, and enhanced documentation clarifying validation matches `git verify-commit` behavior. The commit message now better explains why fast-import must handle validation (with input from Elijah Newren), since neither fast-export nor filter-repo can determine signature validity during rewriting.

Maintainer Junio C Hamano initially raised concerns about: (1) definition of "invalid" regarding missing keys/revocations, (2) clock accuracy impacts, and (3) cascading invalidation of subsequent signatures. He has approved v2's test restructuring approach (avoiding SHA-256 repository dependencies) while theoretical concerns about cascading invalidation remain open. Elijah Newren provided key motivation from `git filter-repo` use cases where users want to rewrite recent history while preserving older valid signatures.

The implementation handles OpenPGP/X.509/SSH signatures with comprehensive tests. While cryptographic purity concerns remain theoretically unresolved, the practical use case is now more clearly articulated. Christian Couder has clarified validity checks use same definition as `git verify-commit` (returning 0 for valid), assumes availability of all necessary public keys/revocation data, and acknowledges but doesn't yet address cascading invalidation edge cases.

Open questions remain about: potential format changes to support unsigned OID tracking (Newren's proposal), default behavior should change, and future tag support. The series still lacks Reviewed-by/Acked-by tags. Error handling rejects unsupported uses (tags, fast-export). Documentation describes new mode alongside existing options while deferring broader improvements (possibly new gitsignature(7) page) to future work.

The technical rationale is sound with thorough test coverage, well-motivated by filter-repo use cases, and carefully avoids breaking changes. Maintainer collaboration has addressed test infrastructure issues, making this likely to progress toward inclusion while leaving some design questions for future consideration. The response to review feedback demonstrates good maintainer collaboration, with cascading invalidation edge cases remaining the primary unresolved theoretical concern.