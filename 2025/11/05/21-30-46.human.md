Refactoring patch in a series improving whitespace handling for incomplete line endings.

This is the sixth patch in Junio Hamano's 12-part series adding detection of missing newlines at file endings as a configurable whitespace error. The change is purely a naming consistency fix - it renames the `ecb` parameter to `ecbdata` in the `emit_rewrite_lines()` function to match how this callback data is named elsewhere in the codebase.

The patch is straightforward and mechanical, changing 6 lines to use the more common naming convention. It follows the series' pattern of careful preparatory cleanups before introducing the core functionality in later patches. While minor, this change helps maintain code readability and consistency as the series builds toward its goal of better handling incomplete line endings in diffs.

This continues the series' focus on meticulous attention to detail, though reviewers may want to confirm the naming change doesn't impact any callers outside the immediate context. The patch maintains all existing functionality while making the codebase more uniform.