This is a refactoring patch in Junio Hamano's series introducing "incomplete-line" whitespace error handling. Patch 06/12 standardizes the parameter name `ecbdata` across all call sites in `diff.c` for consistency, specifically updating the `emit_rewrite_lines()` function which previously used just `ecb`. 

The change is purely mechanical - renaming the parameter and updating its references within the function to match the naming convention used elsewhere in the codebase. The patch maintains all existing functionality while improving code consistency. This continues the series pattern of careful, incremental refactoring to prepare for the new whitespace error class.

Key technical details:
- Updates parameter name in function declaration and all internal references
- Maintains identical line counting logic via `lno_in_preimage`/`lno_in_postimage`
- Preserves `DIFF_SYMBOL_NO_LF_EOF` handling for incomplete lines
- Touches only `diff.c` with straightforward rename operation

This patch follows the series' established approach of cleaning up the diff infrastructure before introducing the new whitespace handling functionality in later patches. It builds on the line number tracking improvements from patches 04-05 while keeping changes minimal and focused.