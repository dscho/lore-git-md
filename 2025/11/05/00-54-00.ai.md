This is a follow-up technical discussion about the `git add` exclude patterns bugfix, focusing on implementation concerns. Junio Hamano expresses discomfort with the current approach in `dir.c` where a custom pathspec matching function (`exclude_matches_pathspec`) is used rather than the standard `match_pathspec` function. He suggests this might be a historical artifact from before pathspec magic existed, dating back to a 2017 rewrite (e1b8c7bd) that replaced older code from 2010.

The email raises an architectural question about whether the fix should instead refactor the code to use the main pathspec` matching infrastructure rather than maintaining a separate implementation. This suggests the current patch might be treating symptoms rather than addressing a deeper design issue. The concern about "unintended fallouts" remains, though testing hasn't revealed problems yet.

Key technical points for future reference:
- Core question: Why does `exclude_matches_pathspec` exist separately from `match_pathspec`?
- Historical context points to e1b8c7bd (2017) and 29209cbe (2010) as potential origins
- Implies the fix might need deeper refactoring of pathspec handling
- Maintains uncertainty about edge cases despite passing tests

This moves the discussion from the specific bugfix to broader questions about Git's pathspec processing architecture, suggesting the thread may pivot toward more fundamental design work.