This is a CI/build system follow-up to the config optional path fixes. Junio C Hamano acknowledges D. Ben Knoble's observation that the new test file `t1311-config-optional.sh` needs to be added to the Meson build system configuration. The change is straightforward - just adding the test filename to the list in `t/meson.build` to ensure the test runs in Meson-based builds like Gentoo.

The patch shows the exact one-line addition needed, inserting the new test between `t1310-config-default.sh` and `t1350-config-hooks-path.sh` in the test list. This completes the integration of the earlier segfault fixes by ensuring their test coverage applies uniformly across both Makefile and Meson build systems. The change is mechanical but important for build system parity, and follows the same careful attention to detail seen in the original functional patches.