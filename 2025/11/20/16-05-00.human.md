This is a bugfix discussion about `git submodule` behavior with `--single-branch` when initializing submodules with branch specifications.

D. Ben Knoble responds to Uwe Kleine-König's bug report about submodule initialization tracking the wrong branch. Knoble points out that the current behavior may be correct according to the documentation - `--single-branch` defaults to tracking HEAD unless `--branch` is explicitly specified. However, he suggests there might be room for improvement by having `--single-branch` automatically respect the branch specified in `.gitmodules`.

The response highlights a potential documentation vs. expected behavior mismatch, where users might reasonably expect the submodule's configured branch to be used by default. This appears to be the first technical response in the thread, moving the discussion from bug report to potential solution space. The suggestion implies this might not be a bug per se, but rather a missing feature or unclear interface.

The exchange surfaces an important design question: should submodule initialization with `--single-branch` automatically honor branch specifications from `.gitmodules`, or is the current explicit-`--branch` behavior the correct approach? This could lead to either a documentation clarification or a behavior change in future Git versions.