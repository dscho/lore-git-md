Refactoring patch in the git-history series, splitting add-patch declarations into a new header.

This patch separates the `add-patch` related declarations from `add-interactive.h` into a new `add-patch.h` header. The change is straightforward - it moves the `add_p_opt` struct, `ADD_P_OPT_INIT` macro, `add_p_mode` enum, and `run_add_p()` function declaration to the new header while keeping interactive-specific declarations in the original file. The patch includes the necessary `#include` adjustments in both header and implementation files.

The refactoring appears clean and well-motivated, making subsystem boundaries clearer. This is part of Patrick Steinhardt's larger git-history series that builds on Git's replay machinery, though this particular change is an independent infrastructure improvement. The patch follows the series' pattern of careful subsystem separation and has no apparent technical issues. Elijah Newren's brief response ("Simple enough") suggests this change is uncontroversial and likely to be accepted.