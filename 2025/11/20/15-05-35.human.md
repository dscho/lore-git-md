This is a refactoring patch in Patrick Steinhardt's `git-history` series, specifically addressing the add-patch subsystem. Phillip Wood provides a brief review of patch 08/11, which removes the dependency between `add-patch` and `add-interactive` by directly storing and initializing interactive configuration in `struct add_p_state` rather than through `add-interactive.h`.

The change is straightforward - it moves configuration handling into the add-patch subsystem where it's actually used, rather than inheriting it indirectly. Phillip Wood approves the change with a simple "Makes sense" comment, indicating this is a clean improvement to the code organization. The patch appears well-contained and focused on its stated goal of reducing unnecessary dependencies between subsystems.

This continues the series' broader effort to refactor interactive add/patch infrastructure to support in-memory index operations for the new `git-history` command. The change aligns with earlier reviews from Phillip Wood and others that have been examining the boundaries between these subsystems.