Refactoring patch in the `git-history` series, splitting add-patch declarations into their own header.

This is the sixth patch in Patrick Steinhardt's v6 series introducing the `git-history` command. It performs a straightforward code organization task: moving declarations related to the add-patch subsystem from add-interactive.h into a new add-patch.h header. The change is clean - declarations are simply relocated with no behavioral modifications.

The patch helps establish clearer boundaries between the add-patch and add-interactive subsystems, which is particularly relevant as the series builds on this infrastructure changes to support in-memory index operations for the new history` subcommands. The implementation looks correct and well-contained, with appropriate include adjustments in both the original and new header files.

This appears to be a preparatory change that enables subsequent patches in the series to work with the add-patch functionality in a more modular way. The change is small (30 lines net) and focused, following the pattern of other infrastructure refactoring patches in this series that have already received positive feedback.