This is a follow-up to a bugfix addressing segfaults in `git config get --path` when handling optional path values marked with `:(optional)`. The original patch fixed the core issue by making `git_config_pathname()` properly clear destination pointers and adjusting return value handling.

D. Ben Knoble points out that while the fix is correct, it needs a small build system update to ensure the new test file `t/t1311-config-optional.sh` is properly included in Meson-based builds. This is a straightforward but important addition since the test verifies the segfault fix works as intended.

The note is brief but highlights a real gap - without this change, distributions using Meson (like Gentoo) would miss the new test coverage. It's a minor but necessary follow-up to complete the original fix. The thread shows good attention to cross-build compatibility details.