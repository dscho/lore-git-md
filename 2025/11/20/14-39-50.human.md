This is a follow-up discussion in the thread about consolidating Git's tempfile creation by replacing `mkstemp()` calls with `git_mkstemp_mode()`. Junio Hamano is responding to Jeff King's suggestion about how to handle the remaining direct `mkstemp()` calls.

Junio agrees with Jeff's approach of either banning `mkstemp()` entirely or using a macro to redirect it to `git_mkstemp_mode()`, noting that since there's only one remaining caller (in `entry.c`), this would be a good change to make. The discussion shows the maintainers converging on a solution that will ensure consistent tempfile handling across all platforms while removing the last bits of platform-specific code.

This email doesn't introduce new changes but rather confirms the direction of the refactoring work, with the maintainers agreeing that the remaining `mkstemp()` usage should be eliminated in favor of Git's unified implementation. The thread demonstrates careful consideration of the tradeoffs between platform-native behavior and cross-platform consistency in Git's internals.