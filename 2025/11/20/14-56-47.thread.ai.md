Git thread summary:

Feature request for a `git cp` command to copy files while preserving history/blame information, analogous to `git mv` for moves. Original author Martin Guy presented a concrete use case of splitting mp3.c into mad.c, lame.c, and twolame.c while maintaining accurate attribution, with clarified need for `git blame` to automatically follow copied lines back to their original source without manual intervention.

Kristoffer Haugsbakk demonstrated Git's existing rename detection (`git log --find-copies-harder`) can track copied content across files, showing example output where a file copied three ways maintains proper source attribution. D. Ben Knoble added historical context by referencing Gentoo's Git patches that implemented similar functionality for ebuild developers, then followed up with key architectural insight: both `git mv` and copy detection work through post-hoc content-based reconstruction rather than explicit commit metadata, citing a 2005 Linus Torvalds email that established this design principle against storing movement metadata in commits. Lucas Seiki Oshiro reinforced this understanding by emphasizing Git's snapshot-based architecture (referencing the Git book) as the foundation for this behavior, noting that rename detection thresholds can be adjusted via `--find-renames` in various commands.

The discussion now encompasses: 1) the original blame-tracking use case for explicit copy recording, 2) demonstration of existing dynamic detection capabilities, 3) evidence of prior implementations, 4) foundational architectural constraints against metadata recording (with multiple confirmations from different contributors), and 5) a clarified technical gap where log-based detection doesn't automatically propagate to blame. Key technical details include Git's copy detection mechanics (`--find-copies-harder` and `--find-renames` flag behavior) versus explicit recording needs for blame, with confirmation that both `git mv` and copy detection use the same post-hoc approach. The Linus reference and subsequent architectural confirmations establish the current behavior as intentional design rather than oversight.

Open questions remain about whether blame could get special treatment for copied content within Git's architectural constraints, and whether this requires new functionality (like `git cp`) or could be addressed within existing features. No maintainer or subsystem expert has yet weighed in on implementation feasibility or desirability in core Git. The thread remains focused on use cases where accurate attribution across file splits is critical for historical tracking, now with multiple independent confirmations of why Git handles movement/copy tracking through content-based detection rather than explicit recording.