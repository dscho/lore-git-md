Performance regression investigation for `git last-modified` on big-endian platforms. Jeff King responds to Anders Kaseorg's report of s390x test failures in the recently merged performance optimization series, noting similar failures on HP NonStop (both big-endian architectures). The key observations:

The failing tests incorrectly attribute file modifications to merge commits (ac29b6e and da1857e) rather than their actual sources. Jeff suggests two potential failure modes - either the algorithm isn't properly propagating blame through merges, or it's failing to propagate blame at all. While the original patch's integer handling appears correct, the big-endian correlation raises suspicions about memory access patterns.

Jeff requests a SANITIZE=address,undefined build to check for endian-specific memory issues, though acknowledges this may be a long shot. The thread now has:
- Confirmed reproduction on two big-endian platforms
- Clear failure patterns showing merge commit misassignment
- Potential endianness angle for investigation
- Next debugging steps proposed

The core algorithm remains validated on little-endian systems, but this platform-specific issue requires resolution before wider deployment. The discussion is now focused on diagnosing whether the problem stems from merge handling logic or deeper architectural assumptions in the bitmap/priority queue implementation.