This is a refactoring patch that makes a function static since it's only used within its own file. The change marks `git_configset_get_pathname()` as file-scoped by adding the `static` keyword in config.c definition and removing its declaration from config.h. The function was previously exposed in the header despite having only one caller within config.c. The patch is a straightforward cleanup with no behavior changes, coming from Junio C Hamano as a minor improvement noticed while working in the area. The diff shows the simple modification to both files, removing the public interface while keeping the implementation. This follows good practice of minimizing API surface area when functionality isn't needed externally.