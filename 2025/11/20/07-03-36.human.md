This is a refactoring patch in Patrick Steinhardt's `git-history` series, specifically addressing the add-patch subsystem. The patch splits out a new `struct interactive_options` to better organize shared configuration between `git add -p` and `git add -i`, which previously had confusing circular dependencies in their option handling.

Elijah Newren provides a brief review, noting the change makes sense and appears reasonable, though he mentions the patch is quite long and might benefit from being split into smaller steps for easier review. He used diff coloring tools to verify the changes, which mostly involved repetitive additions of configuration-related code. While he admits to skimming due to the size, he doesn't raise any technical objections to the approach.

This appears to be a straightforward infrastructure improvement that's part of the larger `git-history` effort, with no controversy or blocking issues raised in the review. The change helps untangle some shared code between interactive add operations, which will be useful for the series' new history editing capabilities.