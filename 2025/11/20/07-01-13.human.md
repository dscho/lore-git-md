This is a refactoring patch in Patrick Steinhardt's v6 series introducing the `git-history` command. The patch extracts commit-picking logic from `git-replay` into a new `replay.c` file to share between `git-replay` and the new `git-history` command, renaming functions with a `replay_` prefix for clarity.

Elijah Newren chimes in to note there are actually two functions being made public (not just one as Phillip Wood's review suggested), but otherwise agrees with Phillip's positive assessment of the change. The refactoring is part of the broader effort to build shared infrastructure for history editing commands.

The patch appears straightforward - it's moving and renaming existing code without changing behavior, with the goal of enabling code reuse. Both reviewers seem satisfied with the approach, suggesting this is a clean preparatory change that helps pave the way for the more substantial functionality coming later in the series.