This is a performance-focused response in the ongoing `git describe` traversal order bugfix thread. Jeff King (peff@peff.net) analyzes Ben Boeckel's proposed topological sorting implementation and identifies a critical performance bottleneck.

The key observation is that the current patch's comparison function performs two full commit traversals (via `repo_is_descendant_of`) for each comparison, explaining the "minutes" of runtime without commit-graphs. Jeff suggests two more efficient alternatives: either using precomputed generation numbers (from commit-graphs when available) or leveraging Git's existing topological sorting infrastructure from the revision traversal code.

The response implies that while the patch correctly implements the desired behavior, its performance characteristics make it unsuitable as-is. Jeff hints that the solution might involve either adapting `git describe` to use the standard revision traversal machinery or applying similar optimization techniques to its custom traversal code.

This moves the thread forward by shifting focus from correctness (already addressed in Ben's patch) to implementation efficiency, while maintaining the goal of proper topological ordering for tag descriptions. The performance considerations are particularly important given this would change default behavior that existing scripts may rely on.