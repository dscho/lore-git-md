This is a bug report about an incorrect error message in `git config set`. The issue occurs when trying to set a value for a config key that already has multiple values - the error message incorrectly suggests using `--add` when it should suggest `--append`.

The reporter provides clear reproduction steps showing how to trigger the misleading message. When attempting to overwrite a multi-valued config entry, Git warns about multiple values and suggests using `--add`, but this option doesn't exist (`--append` is the correct one). The reporter includes their Git version (2.48.1) and system information to help diagnose the issue.

The bug appears in the error message text itself, specifically in the config.c file where these validation messages are generated. This seems like a straightforward documentation/error message fix that shouldn't require deep architectural changes, though the patch would need to verify there aren't other references to the non-existent `--add` option elsewhere in the codebase.

The report is well-formatted with clear steps to reproduce and includes all relevant system information. The issue appears legitimate and the fix would improve user experience when dealing with multi-valued config entries.