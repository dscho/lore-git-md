This is a follow-up to the bugfix thread addressing segfaults in `git config get --path` with optional path values. The original patch fixed the core issue in `git_config_pathname()` and `format_config()` handling, but D. Ben Knoble points out that the new test file `t/t1311-config-optional.sh` needs to be added to the Meson build system's test list (`t/meson.build`) for proper integration in Meson-based distributions like Gentoo. 

The note is brief but important - while the functional fix is complete, the build system integration was overlooked. This is a common oversight when adding new test files, and the reminder will ensure the test runs in all build configurations. The change would be trivial (just adding the test filename to the list) but necessary for full compatibility.