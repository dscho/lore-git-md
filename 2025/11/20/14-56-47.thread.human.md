Here's a human-readable summary of the thread's current state:

The discussion began with Martin Guy proposing a `git cp` command to explicitly track file copies while preserving history, similar to how `git mv` handles moves. Contributors demonstrated that Git already has content-based copy detection through commands like `git log --find-copies-harder`, though this doesn't automatically apply to `git blame`. Key insights emerged about Git's architecture - notably from D. Ben Knoble and Lucas Seiki Oshiro - explaining that Git intentionally avoids storing movement metadata, instead reconstructing file relationships through post-hoc content analysis, a design principle dating back to Linus Torvalds' early decisions.

The conversation took an interesting detour when Randall Becker raised a theoretical question about whether Git's content-matching could detect SHA-1 collisions during copy operations. Martin Guy later clarified that since Git's rename/copy detection works by comparing content after the fact, the same collision considerations would apply as elsewhere in Git. While this doesn't directly affect the `git cp` proposal, it deepened the discussion of Git's underlying mechanics.

Martin also shared a personal observation about potentially wanting word-level rather than line-level diffing for more semantic change tracking, though this remains a side note rather than a concrete proposal. The core question remains open: whether Git needs explicit copy tracking (perhaps via `git cp`) or if the existing content-based detection could be enhanced to better serve blame use cases, all while respecting Git's snapshot-based architecture. No maintainers have yet weighed in on feasibility or desirability of changes in this area.