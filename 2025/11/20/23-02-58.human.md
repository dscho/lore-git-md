This is a follow-up in the ongoing SHA-1/SHA-256 interoperability series, specifically addressing Windows/MSVC build issues that were identified as pre-existing problems. The email contains a proposed fix attempt for the CI failures, though the author notes it doesn't fully resolve the issue.

brian m. carlson shares a WIP patch that modifies both the Makefile and cargo-meson.sh to properly distinguish between MSVC and GNU toolchains on Windows, rather than treating all Windows builds the same. The changes adjust the library names (`.lib` vs `.a` files) based on whether the MSVC toolchain is detected. This follows Ezekiel Newren's earlier diagnosis that the root cause was a Windows/MSVC vs Windows/GNU naming convention mismatch.

The patch represents an attempt to fix the build system behavior that was originally introduced in Patrick Steinhardt's initial Rust work. While the changes appear technically correct for handling MSVC specifically, the author notes they don't yet fully resolve the CI failures, pointing to a test run that still isn't passing. The thread remains in a holding pattern while awaiting more diagnostic information from the enhanced CI logging that was recently deployed.

This exchange shows the careful debugging process around Git's first major Rust integration, particularly around cross-platform build system nuances. The discussion remains focused on isolating the pre-existing Windows/MSVC issue rather than any problems with the core SHA-1/SHA-256 interoperability functionality.