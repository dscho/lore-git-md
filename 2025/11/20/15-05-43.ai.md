This is a technical review discussion about infrastructure refactoring in the `git-history` series, specifically addressing the add-patch subsystem changes.

The email continues the conversation about patch 09/11 in the series, which introduces `run_add_p_index()` for in-memory index operations. Phillip Wood responds to Elijah Newren's question about why the implementation uses a copy-paste-modify approach for `run_add_p_index()` rather than having `run_add_p()` call it with `r->index`. 

Key technical points:
- The discussion centers on whether the current approach of having parallel `run_add_p()` and `run_add_p_index()` functions risks future divergence
- Phillip Wood acknowledges the concern about potential for accidental use of `s->r->index` instead of the intended `s->index` in the in-memory variant
- The exchange suggests there may be room for further consolidation between the two functions, though no concrete solution is proposed in this message

This follows up on earlier reviews where Phillip Wood had examined the add-patch/add-interactive separation and Elijah Newren had been reviewing the split subcommand implementation. The discussion remains focused on implementation details rather than fundamental objections to the approach.