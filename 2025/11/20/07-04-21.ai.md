This is a technical review follow-up in the ongoing `git-history` series, focusing on the add-patch subsystem refactoring. Elijah Newren raises a code organization question about the newly introduced `run_add_p_index()` function in patch 09/11, which provides in-memory index patching support.

The core concern is about potential code duplication between `run_add_p_index()` and the existing `run_add_p()` function. Newren questions whether the new function was created via copy-paste-modify and suggests exploring whether `run_add_p()` could instead call `run_add_p_index()` with `r->index` to avoid divergence. He acknowledges his limited familiarity with the add-patch subsystem while flagging this as a maintenance risk.

The review doesn't challenge the functionality itself but probes the architectural decision to have parallel implementations. This follows the pattern of earlier reviews in this thread where Newren has been examining implementation details of the infrastructure changes supporting the history editing features. The question remains open-ended, inviting either a technical justification for the current structure or consideration of unification.

Key technical elements mentioned:
- `run_add_p_index()` (new in-memory operation)
- `run_add_p()` (existing implementation)
- `r->index` access pattern
- Potential code duplication concerns
- Maintenance implications of parallel implementations

This continues the thread's pattern of substantive technical review focused on code organization and maintainability in the series' infrastructure changes. The question doesn't block progress but seeks to improve the long-term codebase health of the add-patch subsystem decoupling work.