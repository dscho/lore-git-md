This is a CI/build system follow-up to the config optional path fixes. Junio Hamano provides the trivial but necessary addition of the new `t1311-config-optional.sh` test to the Meson build system's test list, as pointed out by D. Ben Knoble. 

The change is straightforward - just adding the test filename to the existing list in `t/meson.build`. This ensures the new test for optional path handling will run in Meson-based builds like Gentoo, completing the test coverage across all build systems. 

This is the final piece needed after the two main patches that fixed segfaults in `git config get --path` with optional values. The original changes were already comprehensive in their code and test coverage, and this small build system tweak ensures that coverage is fully realized in practice.