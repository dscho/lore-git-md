Bugfix for `git config set` error message in multi-value cases. This is a follow-up to Paul Wintz's bug report about misleading error messages when attempting to set multi-valued configurations. The patch updates builtin/config.c to recommend the correct flags (`--value=<pattern>`, `--append`, or `--all`) instead of the previously suggested but non-existent `--add` and incorrect `--replace-all` options.

The change is minimal (a single line modification) but important for user experience, as it from suggesting invalid commands to providing accurate guidance. The fix maintains all existing error detection logic while correcting the help text shown to users. The patch properly attributes the original reporter and keeps all other behavior unchanged.

Key technical details for future reference:
- Modifies the error message in cmd_config_set() function
- Replaces incorrect flag suggestions with valid alternatives
- Maintains same error condition (CONFIG_NOTHING_SET)
- Only affects the help text, not the underlying config handling
- References the specific case of multi-value configuration entries

The patch appears straightforward and well-justified, addressing a clear documentation bug that could confuse users following the error message's advice. No test changes are included, but the fix is simple enough that additional testing might not be necessary.