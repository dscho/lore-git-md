This is a technical discussion email in the ongoing `git-history` command RFC thread, focusing on branch handling strategy and commit metadata preservation.

Phillip Wood responds to Elijah Newren's concerns about the current single-branch approach in the `git-history` implementation. The key technical points center around:

1. **Branch handling**: Phillip acknowledges Elijah's proposal to use `git replay --onto` with `--path` and `--branches` flags to automatically rewrite all affected branches rather than just HEAD. He notes this would avoid the current need for extra revision walks to detect multi-branch cases, though raises concerns about edge cases when rewriting branch tips and potential conflicts with concurrent operations (similar to issues `git rebase --update-refs` handles).

2. **Commit metadata**: The discussion clarifies that while most extended headers should be preserved during rewording, the encoding header presents special cases since message editing triggers re-encoding. For splits, Phillip agrees both new commits should prompt the user with the original message as a starting point rather than automatically assigning it to either commit.

The email shows Phillip coming to understand Elijah's technical arguments better, particularly regarding the branch handling complexity tradeoffs. He acknowledges these are important considerations that should be documented, even if the immediate implementation path remains undecided. The exchange demonstrates growing consensus around some of Elijah's technical points while leaving open questions about the exact branch handling strategy to adopt.