This is a question about Git's clean/smudge filter mechanism, specifically regarding secret redaction workflows. Kache Hit is implementing a Python filter to redact secrets when committing files while preserving local secret values during checkout, but encounters an issue where the smudge filter cannot access the working copy file on disk.

The email explains the current behavior is documented - smudge filters should not try to access the working file since it may not exist or have different contents. Kache provides a complete Python implementation that demonstrates the desired functionality (redacting secrets on clean, restoring local secrets on smudge) and includes thorough tests, but notes the smudge operation fails with FileNotFoundError when trying to read the working file.

The core question is whether there's a workaround to access the working file from smudge, or an alternative approach to achieve the secret-preserving workflow. Kache also asks for the rationale behind Git's current design that prevents smudge from reading the working file.

The implementation looks well-crafted with good test coverage, suggesting this is a serious attempt to solve a real workflow problem. The question touches on Git's internals and security considerations around filter processes, making it likely to interest developers working on similar secret management solutions.