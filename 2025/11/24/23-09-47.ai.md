This is a technical discussion about cache-tree parsing safety within Jeff King's ASan hardening series, specifically addressing Patch 4/9. Junio Hamano responds to Jeff's query about what makes the current parsing approach "unfinished," identifying two specific concerns: (1) unsigned integer parsing with wraparound detection and (2) signed integer range validation after negation. 

Junio considers three potential solutions: (1) improving the existing validation logic to handle these edge cases more cleanly, (2) copying data into a NUL-terminated buffer for strtol() parsing (which Jeff noted might feel "dirty" but performant), or (3) verifying newline termination while keeping the current strtol() usage (incompatible with ASan's strict_string_checks). 

Significantly, Junio proposes a fourth option: introducing a cache-tree version 2 format that would avoid text parsing entirely by using a binary format (network byte order or varint). While acknowledging there aren't currently major deficiencies in the cache-tree format beyond minor pathname sorting regrets, he suggests this could be a more forward-looking solution if combined with other potential improvements. The discussion remains focused on balancing robustness, performance, and compatibility with ASan's strict checking requirements, with Junio leaning toward a more fundamental format change rather than incremental parsing fixes.