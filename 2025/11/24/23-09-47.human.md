This is a technical discussion about cache-tree parsing safety, part of Jeff King's ASan hardening series. The conversation focuses on three potential approaches to handling strtol() parsing in cache-tree.c, with Junio Hamano weighing in on the tradeoffs.

Junio expresses dissatisfaction with two aspects of the current parsing implementation: handling unsigned integer wraparound when parsing absolute values, and checking if the parsed value fits in a signed int when negated. He suggests these could be implemented more cleanly, though doesn't propose specific code changes.

The discussion then turns to alternative approaches: (1) copying data into a NUL-terminated buffer for strtol(), which Jeff notes would be performant but feels "dirty"; (2) verifying newline termination but keeping existing strtol() usage (incompatible with ASan's strict_string_checks); or (3) Junio's new suggestion of introducing a cache-tree-version-2 index extension to avoid text parsing entirely. Junio notes this would be a more substantial change but could address other minor cache-tree format issues, though he doesn't see any pressing needs beyond the current parsing problem.

This email continues the thread's pattern of careful consideration of memory safety tradeoffs, with Junio now proposing a more architectural solution that could eliminate the text parsing problem at its root rather than working around it. The discussion remains technical and collaborative, with no clear resolution yet on which path to take.