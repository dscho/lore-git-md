Thread summary:

Bugfix correcting misleading help text for `git config unset` flags in `builtin/config.c`. The `--all` flag's description incorrectly suggested it would "replace" rather than unset values in multi-valued configurations, while `--value` was described as showing configs rather than unsetting them. The patch updates these two help strings to accurately reflect their functionality as selection criteria for what to unset. Behavior remains unchanged - this is purely a documentation fix for the command-line interface. Changes are minimal (2 lines modified) and focused on improving user experience through accurate flag descriptions. The patch is well-scoped and uncontroversial, following standard formatting with a clear commit message explaining the rationale.