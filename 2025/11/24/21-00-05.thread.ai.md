Thread summary:

Bugfix correcting misleading help text for `git config unset` flags in `builtin/config.c`. The `--all` flag's description incorrectly suggested it would "replace" rather than unset values in multi-valued configurations, while `--value` was described as showing configs rather than unsetting them. René Scharfe's patch updates these two help strings to accurately reflect their functionality as selection criteria for what to unset. The change is minimal (2 lines modified in `builtin/config.c`) and focused on improving user experience through accurate flag descriptions. Junio C Hamano has reviewed and approved the patch ("Will queue"), confirming it correctly addresses the documentation issue without changing behavior. The fix is uncontroversial and will be included in the next integration cycle, requiring no further discussion.