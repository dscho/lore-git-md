This is a response in the ongoing discussion about `git clone` config parsing regression, focusing on the documentation and expected behavior of `-c` and `--config` options.

Junio C Hamano weighs in on the reported regression where whitespace-trimming behavior was removed in Git v2.52.0. He points out that the documentation for `git clone` clearly specifies that `-c`/`--config` should take `<key>=<value>` as a separate argument from the option itself, suggesting that accepting them as a single quoted argument (like `'-c KEY=VALUE'`) may have been a bug all along. He notes the documentation doesn't even mention the `--config=K=V` form, unlike other long options which are explicitly documented with `=` syntax.

Hamano makes two key observations:
1. The current behavior where `git clone '-c KEY=VALUE'` works (pre-v2.52.0) might be incorrect based on the documentation
2. Trimming whitespace around keys (like `' KEY=VALUE'` becoming `'KEY=VALUE'`) would violate Git's config key format rules

He acknowledges the documentation is incomplete regarding `--config=K=V` form, noting this is inconsistent with how other long options are documented. The response suggests the reported "regression" might actually be correcting undocumented/buggy behavior, though Hamano leaves the final assessment open ("I dunno"). This moves the discussion from being purely about a regression to questioning what the correct behavior should be based on documentation and config parsing rules.