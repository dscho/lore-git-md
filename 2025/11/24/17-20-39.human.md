Hook subsystem refactoring (part 6/10): This patch adds flexibility to the hook execution API by making the "ungroup" behavior configurable rather than hardcoded. The change allows callers to disable ungrouping when needed, particularly for sideband output processing which requires grouped output.

The patch modifies `run_hooks_opt()` to respect a new `ungroup` flag in the options struct while maintaining the existing ungrouped behavior as the default for all current hook callers (commit hooks, `hook run` command, and the simple `run_hooks()` wrapper). The author notes this preserves backward compatibility while solving the immediate need for sideband processing in the larger hook modernization effort.

This appears to be a straightforward change that completes an important piece of the parallel hook execution infrastructure. The implementation is clean separation of concerns, with clear documentation of the new field and explicit comments about the ungrouping requirements of existing callers. As part of the well-reviewed v3 series, this change seems ready for inclusion.