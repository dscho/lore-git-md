Hook subsystem refactoring (part 3/10): This patch converts the post-rewrite hook in sequencer.c to use the new hook API, continuing the series that modernizes Git's hook handling infrastructure.

The change replaces manual child process management with the simpler `hook_run_opt()` interface, which automatically handles SIGPIPE toggling and hook discovery. The implementation introduces a new `pipe_from_strbuf` callback that safely streams hook input from a strbuf through the new feed_pipe mechanism. This matches the previous patches' pattern of moving away from direct process management while maintaining the same functionality.

The conversion demonstrates the series' benefits clearly - the new version is more concise (25 lines added vs 17 removed) while handling the same core task of piping commit hashes to the post-rewrite hook. The patch maintains all existing behavior including error handling for EPIPE cases, now managed by the shared hook infrastructure rather than custom code.

As part of the larger series that has already received design approval, this change appears well-executed and follows the established pattern. It's one of several conversions needed to fully modernize Git's hook handling while preparing for future parallel execution capabilities.