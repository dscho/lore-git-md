This is part 5/10 of the hook subsystem refactoring series, converting the reference-transaction hook to use the new hook API. The patch replaces direct `run-command` usage with the structured `run_hooks_opt` approach, eliminating manual child process setup and signal handling.

The key change introduces a `transaction_hook_feed_stdin` callback that processes one ref update per invocation, writing the update details to the hook's stdin. While the current implementation processes updates one-by-one, the commit message notes this could be batched for better throughput if needed. The conversion maintains all existing functionality while being more maintainable and consistent with the series' goal of standardizing hook execution.

The patch appears well-constructed, with careful handling of edge cases (EPIPE, REF_LOG_ONLY updates) and proper memory management (strbuf initialization/release). It follows the pattern established in earlier parts of the series for converting hooks to use the new stdin feeding mechanism. The change is focused and doesn't introduce any observable behavior changes - it's an internal refactoring that makes the code more maintainable while preparing for future parallel execution capabilities.