This is a technical discussion about the `GIT_REF_URI` implementation for reference backend selection, specifically addressing feedback on the patch. The email focuses on two key points of clarification:

1. Documentation improvement - Karthik agrees to clarify in the docs that the path should point to the reference directory (like `.git/refs`), not the repository root, to match user expectations from `GIT_OBJECT_DIRECTORY`.

2. Repository format tracking - Justin raises whether `r->ref_storage_format` should be updated when using `GIT_REF_URI`, but Karthik explains this isn't feasible due to lazy evaluation and argues it shouldn't modify the repository state since the environment variable is meant as a temporary override rather than configuration change.

The discussion shows the implementation is being refined based on review feedback, though the broader design questions about the URI approach (raised in Junio Hamano's earlier comments) remain unresolved. This exchange focuses narrowly on making the current implementation more user-friendly and technically sound while avoiding deeper architectural changes.