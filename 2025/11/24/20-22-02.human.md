Bugfix for misleading error message in `git config set` when dealing with multi-valued config entries. This follows up on Paul Wintz's report about the command incorrectly suggesting a non-existent `--add` flag instead of the proper `--append` option.

René Scharfe provides a patch that updates the error message in builtin/config.c to recommend the correct modern flags: `--value=<pattern>`, `--append`, or `--all`. The change is straightforward - just a one-line modification to the error string. The patch maintains the same warning about not being able to overwrite multiple values with a single value, but now points users to the right solutions.

The fix appears well-targeted and uncontroversial, directly addressing the reported issue without introducing any behavioral changes beyond the corrected messaging. The patch includes proper attribution to the original reporter and follows Git's standard contribution format. This should be a quick win for the next release cycle.