This is the final patch in Jeff King's ASan hardening series, enabling strict string checking in the test suite. The patch activates ASan's `strict_string_checks` option by default, which verifies NUL-termination for string operations. While this initially produced some false positives (like in `fsck_ident()` where newlines safely terminate operations), it also caught a real issue in `cache-tree.c`'s `strtol()` parsing that would have been difficult to detect otherwise.

The patch follows the series' pattern of making Git's code more robust against memory safety issues, though here the focus shifts to test infrastructure rather than core functionality. The change is minimal (a single line added to `t/test-lib.sh`) but builds on the previous work that fixed all identified issues. The commit message makes a strong case that the benefits outweigh the occasional false positives, as the stricter checking makes the codebase more maintainable and could catch subtle bugs in the future.

Key technical details for future reference:
- Enables ASan's `strict_string_checks=1` globally in test runs
- Follows up on fixes made earlier in the series (particularly patch 5's `fsck_ident()` changes)
- Specifically mentions two cases that motivated the change: `cache-tree.c`'s `strtol()` parsing (real bug) and `fsck_ident()` (false positive but led to cleaner code)
- Maintains the series' approach of fixing all identified issues before enabling stricter checking

This appears to be the culmination of the ASan hardening effort, with all identified issues addressed and the test suite now clean under the stricter checking regime. The patch maintains the series' high quality with clear motivation and thorough testing.