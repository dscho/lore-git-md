This is a follow-up discussion about the Windows platform compatibility patch that removes comma operator usage in error handling code. Jeff King (peff) agrees with Junio Hamano's approval of the changes, noting they improve readability while maintaining functionality.

The email points out one remaining case where Git still uses the comma operator - in the `error()` macro wrapper. Peff observes this doesn't trigger compiler warnings since it doesn't involve assignments, and suggests leaving it alone since it's not causing problems. This isn't proposing any code changes, just noting an exception to the general cleanup pattern.

The discussion confirms the original patch is sound and ready to merge, with this minor observation about the `error()` macro being the one acceptable holdout case. The thread appears settled with no further action needed on the Windows-specific changes.