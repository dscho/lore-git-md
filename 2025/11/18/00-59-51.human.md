This is a Windows platform compatibility patch that fixes error handling in the pthread emulation layer. The change makes `pthread_cond_wait()` properly report failures from the underlying Windows `SleepConditionVariableCS()` API rather than silently ignoring them.

The patch moves the implementation of `pthread_cond_wait()` from a macro in the header to a proper function in the .c file, where it can properly check the return value and convert Windows error codes to POSIX-style error numbers. This matches how other pthread functions in the compatibility layer behave.

The change is straightforward and appears correct - it's fixing what looks like an oversight in the original implementation where error cases weren't properly handled. The patch is small (9 lines total) and focused, touching only the Windows pthread compatibility code. There's no test included, but the change is simple enough that this seems reasonable for a low-level synchronization primitive.