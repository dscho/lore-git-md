This is a follow-up discussion about the Windows compatibility patch that refactors comma operator usage in error handling. Jeff King (peff) agrees with Junio Hamano's approval of the changes, noting they improve readability by avoiding the confusing comma operator pattern where `errno` assignment and return values were combined.

The email points out one remaining case where Git still uses the comma operator - in the `error()` macro wrapper. This appears to be a special case that doesn't trigger compiler warnings since it doesn't involve assignments. Peff suggests this can remain as-is since it's not causing issues, though acknowledges it's not ideal from a code clarity perspective.

The discussion confirms the original patch is sound and ready for integration, while noting this one exception where the comma operator pattern persists in the codebase. No new changes are proposed - this is an acknowledgement that the broader cleanup has some natural limits in the macro system. The thread appears settled with consensus on both the patch's approach and the pragmatic exception for the error macro.