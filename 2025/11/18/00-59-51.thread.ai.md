Thread identity: Platform compatibility patch for Windows thread synchronization in the pthread emulation layer.

Core problem: The Windows pthread emulation layer was silently ignoring failures in `SleepConditionVariableCS()` by unconditionally returning success (0) via `return_0()` macro, masking potential system errors in thread synchronization paths.

Current status: Single patch version (v1) that moves `pthread_cond_wait` from a macro to a proper function in pthread.c to enable proper error handling.

Key technical details:
- Changes affect Windows-specific compatibility code in pthread.c
- Converts Windows error codes to POSIX-style using existing `err_win_to_posix()`
- Updates header declaration to match new function implementation
- Follows existing error handling patterns in the codebase

Open questions:
- Commit message could be expanded to document specific failure scenarios
- No review feedback yet on the implementation approach

Participant positions:
- Original author identified the silent error swallowing issue
- No reviewers have commented yet

Related work:
- Builds on existing Windows pthread emulation layer
- Uses established `err_win_to_posix()` error conversion pattern