Bugfix follow-up regarding interactive rebase merge commit handling. Phillip Wood responds to Bailey Muckel's Windows-specific report about a BUG message when trying to drop merge commits during `git rebase -i --rebase-merges`. 

The key technical detail is that this was already fixed in commit 4d491ade8f6 ("rebase -i: permit 'drop' of a merge commit") which shipped in Git 2.51.1. The bug manifested when changing a merge commit's command from "merge -C" to "drop" in the interactive rebase todo list, triggering an "unexpected todo_command" error in sequencer.c. 

This appears to be a straightforward case of pointing the reporter to an existing fix in a newer Git version (2.49.0.windows.1 -> 2.51.1). No new investigation or patch is needed since the fix is already in the release stream. The thread can likely be closed unless the reporter has follow-up questions about upgrading or verifying the fix. 

The response is concise and directly addresses the reported issue by identifying both the specific fix commit and the version where it became available. This helps the reporter understand they're encountering a known issue with a clear upgrade path. The original bug report's detailed reproduction steps and system info remain useful context for anyone investigating similar issues in older versions.