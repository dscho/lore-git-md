This is part 7/10 of Ezekiel Newren's xdiff refactoring series, continuing the systematic modernization of xdiff's core data structures for Rust FFI compatibility. The patch converts the `nrec` field in `xdfile_t` from `long` to `size_t`, as it represents the count of records in the `recs` array and elements in the `changed` array.

The change follows the series' established pattern of using size-appropriate types, with `size_t` being the natural choice for array element counts. The patch updates all comparisons and arithmetic operations involving `nrec` across xdiff's implementation files (xdiffi.c, xemit.c, xmerge.c, xpatience.c, xprepare.c) to explicitly cast when interfacing with code still using `long`. This maintains backward compatibility while progressing the type modernization effort.

Key technical points:
- Updates the `xdfile_t` struct definition in xtypes.h
- Modifies 26 lines across 6 files to handle the type change
- Preserves existing behavior through explicit casts at interface boundaries
- Touches core diff algorithms (patience, histogram) and merge machinery
- Follows the series' established approach of methodical type updates

The change is straightforward and consistent with the series' goals, though it does introduce numerous casts that could be cleaned up in future work as more code migrates to size_t. This continues the incremental progress toward fully specified type mappings between C and Rust.