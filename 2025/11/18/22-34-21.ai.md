This is part 9/10 of Ezekiel Newren's xdiff refactoring series, continuing the systematic modernization of core xdiff structures for Rust FFI compatibility. The patch converts the `rindex` field in `xdfile_t` from `long` to `size_t`, following the same pattern as previous array size/offset conversions in the series.

The change is straightforward since `rindex` is only used for array indexing, with no cascading impact. This matches the series' established pattern of updating array-related fields to `size_t` (following patches 7/10 for `nrec` and 8/10 for `nreff`). The diff shows a simple one-line change in `xtypes.h` updating the pointer type declaration.

This continues the series' methodical approach to type safety - each patch tackles a specific field or set of related fields, with clear documentation of the scope and impact. The commit message notes the limited refactoring impact, consistent with the series' careful consideration of backward compatibility. As with previous patches, this change builds on the foundational type mapping standards established earlier in the series.