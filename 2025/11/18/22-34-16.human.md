This is part 4/10 of Ezekiel Newren's xdiff refactoring series, continuing the modernization of core xdiff structures for Rust FFI compatibility. The patch focuses on type consistency by changing `xrecord_t.size` from `long` to `size_t`, which is the more appropriate type for memory sizes.

The change is mechanical but widespread, touching 5 files with 19 line changes. The patch updates all uses of `rec->size` to use `size_t`, adding explicit casts where needed to maintain compatibility with existing interfaces that expect `long`. Key changes include loop counters in diff generation and merge handling, plus updates to the core `xrecord_t` struct definition.

This follows the series' established pattern of methodical type updates, building on the foundational type mapping documentation from earlier patches. The changes maintain backward compatibility through careful casting while progressing toward the goal of type-correct FFI interfaces. No new discussion points are raised - this appears to be a straightforward application of the series' established type modernization principles.