This is a follow-up discussion in the SHA-1/SHA-256 interoperability thread, specifically addressing Windows/Meson build issues that surfaced during CI testing. Junio C Hamano reports that the Windows+Meson build job fails when the SHA-256 interop series is present in 'seen', but passes without it. Brian M. Carlson responds that these Windows build failures predate his changes and have been occurring for over a month.

The key technical detail is that an empty CI-related commit (672cb7c62e) fails the Windows/Meson build when on top of the interop series, but passes when on the previous merge base (950efaac03). This suggests the interop series may be exposing or interacting with pre-existing Windows build issues rather than introducing new ones.

The discussion remains unresolved, with Carlson noting they can produce a v3 if needed but maintaining that the Windows issues are longstanding. This ties back to the thread's open question about Windows/Meson status confirmation, with Patrick Steinhardt potentially available to assist with Windows-specific build problems. The exchange highlights remaining platform compatibility concerns before this major Rust infrastructure change can be merged.