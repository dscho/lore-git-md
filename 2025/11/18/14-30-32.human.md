This is a technical review response in Jeff King's ASan hardening series, focusing on the cache-tree parsing improvements. Phillip Wood provides feedback on the `parse_int()` helper function introduced in patch, noting several areas where the implementation could be made more robust:

1. **Integer overflow handling**: Points out that the current signed integer parsing has undefined behavior on overflow, unlike the original strtol() usage which was well-defined. Suggests handling INT_MIN properly since the code already handles negative numbers.

2. **Sign parsing**: Notes the function accepts multiple '-' signs where strtol() only allows one, suggesting this is unnecessarily permissive for Git's needs.

3. **Input validation**: Observes the function accepts bare '-' as valid input without requiring subsequent digits, which could be tightened.

4. **Error handling**: Highlights that negative subtree_nr values could lead to problematic memory allocation before hitting the existing error check, suggesting failing earlier.

The review is constructive and focused, identifying real issues while acknowledging the overall direction of the patch. These are the kind of detailed observations that help refine safety-critical code changes. The feedback doesn't challenge the patch's core approach but offers specific improvements to make the parsing more robust and consistent with standard behavior.