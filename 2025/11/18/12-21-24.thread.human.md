This thread addresses a minor but proactive cleanup in the test-mktemp helper program, fixing memory and file descriptor leaks that were identified by static analysis. The leaks weren't causing immediate issues since the program exits quickly and existing tests only exercise failure cases where the resources would be abandoned anyway. The patch modifies the cmd__mktemp() function to properly track and release the duplicated template string and temp file descriptor before exit.

The changes are straightforward - introducing variables to hold these resources so they can be freed/closed - and the author notes this isn't urgent but worth fixing since the analysis work was already done. The fix is contained to the test helper code and doesn't affect any production functionality. While current test coverage in t0070 doesn't exercise the success path where these leaks would matter, the patch future-proofs the code against any expanded usage.