This is a bugfix patch in Jeff King's ASan series, addressing memory safety issues in cache-tree parsing. The patch replaces `strtol()` with a custom parser when reading cache-tree extension entries from the index file, avoiding potential buffer overreads that could occur with malformed input.

The key issue is that the existing code uses `strtol()` on non-NUL-terminated mmap'd buffers, which could theoretically read past the buffer end if the index file is malformed in specific ways. While the security implications are limited (since the index is local), the change makes the code more robust and satisfies ASan's strict string checking requirements.

The new `parse_int()` helper implements similar parsing logic but with explicit bounds checking. It introduces some minor behavior changes (like rejecting leading whitespace and requiring strict newline termination) that align better with the index format specification. The patch includes instructions for reproducing the issue with ASan's strict string mode.

This appears to be a well-considered fix - the commit message thoroughly explains the problem space, the solution's tradeoffs, and how it fits into the broader ASan hardening effort. The change is focused and maintains backward compatibility with valid index files while being more defensive against malformed ones.