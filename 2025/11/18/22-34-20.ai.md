This is part of Ezekiel Newren's ongoing xdiff refactoring series to modernize core xdiff structures for Rust FFI compatibility, specifically focusing on type standardization. Patch 8/10 converts the `nreff` field in `xdfile_t` from `long` to `size_t` since it tracks array element counts in `rindex`.

The change follows the series' established pattern of using `size_t` for array sizes and element counts, maintaining consistency with previous patches that updated similar fields like `nrec`. The implementation removes temporary `nreff` variables from `xdl_cleanup_records()` and directly updates the struct field during iteration, simplifying the control flow while preserving the same logic.

This continues the systematic type modernization documented in the series' foundational `unambiguous-types.adoc`, with all major technical points already resolved in earlier reviews. The patch touches core diff preparation code but maintains backward compatibility through the natural conversion between `long` and `size_t` in the affected arithmetic.