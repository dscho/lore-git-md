This thread addresses improvements to Windows test output visibility when using the Meson build system in CI. The two-patch series aims to make test failures easier to debug by properly surfacing output, matching the existing behavior of other CI jobs.

The first patch prepares the ground by modifying the unit test runner to ignore the `--no-chain-lint` option, following an established pattern. The second patch, now under discussion, introduces a new wrapper script specifically for Meson tests on Windows. This script properly handles test options and integrates with the CI's failure reporting infrastructure, including printing detailed output and uploading artifacts when tests fail. The author considered sharing code with the existing test runner but opted for a separate solution due to differences in how Meson and Make handle test arguments.

The changes are focused on CI infrastructure rather than core Git functionality, totaling just 26 lines across both patches. The approach appears well-considered, maintaining existing parallel test slicing while solving the visibility problem that made Windows Meson test failures hard to debug. No reviews have been posted yet, but the small, targeted nature of the changes and clear problem statement suggest this could be ready for merging once any feedback is addressed.