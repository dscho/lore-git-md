Refactoring patch in Ezekiel Newren's xdiff modernization series, converting `xrecord_t.size` from `long` to `size_t` for memory size representation. This is patch 4/10 in v5 of the series, applying the type mapping standards established earlier.

The change systematically updates the `size` field in `xrecord_t` and all related arithmetic across xdiff's core files (xdiffi.c, xemit.c, xmerge.c, xprepare.c). The patch maintains backward compatibility by adding explicit casts where needed at interface boundaries, particularly where functions still expect `long` parameters (like `xdl_blankline()` and `xdl_recmatch()`). 

Key technical points:
- Changes the fundamental `xrecord_t` struct in xtypes.h to use `size_t` for size
- Updates loop variables and local counters to `size_t` where appropriate
- Preserves existing behavior through careful casting at API boundaries
- Touches core diff generation, merge handling, and preparation logic
- Follows the series' established pattern of mechanical type updates after documentation consensus

This continues the series' methodical approach to type modernization, building on the prior documentation and maintainer-approved standards. The changes are consistent with the thread's established technical direction and don't introduce new discussion points.