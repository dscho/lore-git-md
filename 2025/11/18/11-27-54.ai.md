This is a bugfix series addressing regressions in `git fetch`'s batched reference updates, now expanded to three patches. The core issue remains fixing non-conflicting tags not being committed when other conflicts exist, but v6 adds handling for post-fetch operations that were also being skipped on batched update failures.

The series extracts transaction committing logic into a new `commit_ref_transaction()` helper (patch 1), fixes the tag commitment issue (patch 2), and now adds a third patch ensuring `FETCH_HEAD` updates, upstream tracking, and remote HEAD setting still occur even when batched updates fail. Karthik notes these were pre-existing issues but now properly handled for non-atomic fetches.

Key technical details in v6:
- New patch 3/3 adds recovery of three post-fetch operations previously skipped on batched update failures
- Maintains the core fix where non-conflicting tags now commit properly despite other failures
- Continues using the extracted `commit_ref_transaction()` helper for cleanup
- Adds comprehensive tests covering all fixed scenarios
- Confirms pruning behavior remains unchanged (executes before transaction creation)

The series has incorporated all prior feedback and appears ready for merging, with Karthik planning follow-up work for additional pre-existing issues after 2.52 releases. The control flow questions from earlier discussion are resolved by keeping the transaction commit in cleanup while ensuring proper operation sequencing.