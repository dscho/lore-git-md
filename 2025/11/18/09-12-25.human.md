This is a bugfix patch in Jeff King's ongoing series addressing ASan findings, specifically hardening the fsck_ident() function. The patch removes a redundant timestamp validation check that became unnecessary after earlier hardening work in commit 8e4309038f.

The change simplifies the date parsing logic in fsck_ident() by eliminating a check for empty input (p == end) that can no longer occur due to previous validation. This makes the code slightly more maintainable while keeping all the same error detection capabilities. The patch maintains the important checks for integer overflow and proper date format.

This appears to be a straightforward cleanup that follows logically from the earlier hardening work. The change is minimal (just one line removed) and well-explained in the commit message, connecting it clearly to the previous changes in this area. As part of the larger ASan hardening series, this contributes to making Git's parsing code more robust against memory safety issues.