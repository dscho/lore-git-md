This is part of Ezekiel Newren's ongoing xdiff refactoring series to modernize core types for Rust FFI compatibility. Patch 9/10 continues the systematic type updates by converting the `rindex` field in `xdfile_t` from `long` to `size_t`, as it's used for array indexing. 

The change is straightforward since `rindex` only interacts with other arrays and doesn't require cascading changes. This follows earlier patches that updated related fields like `nrec` and `nreff` to use proper size types. The series has already established clear type mapping standards and received maintainer approval, with this being one of the final mechanical updates to complete the core structure modernization.

The patch is small (just a one-line change) and low-risk, building on the solid foundation of type safety work done in previous patches. It demonstrates the careful, incremental approach taken throughout this series to update types while maintaining backward compatibility.