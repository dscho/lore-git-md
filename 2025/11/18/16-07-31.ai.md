This is a bugfix patch series addressing output formatting issues in `git worktree list`. The series consists of two patches that work together to improve the display of worktree information, particularly when paths contain special characters.

The first patch fixes column alignment problems that occur when worktree paths contain multibyte characters, which currently causes excess padding between columns. The second patch adds proper path quoting to prevent control characters from disrupting the output formatting. Together these changes make the output more reliable and readable in edge cases.

The changes touch both the worktree command implementation and its tests, with a roughly even split between code modifications (41 lines changed) and test updates (37 lines changed). The test changes suggest thorough validation of the new behavior, including cases with multibyte characters and control sequences.

The patch appears well-motivated and focused, addressing a clear usability issue without introducing broader architectural changes. The commit messages are clear about the problems being solved, and the changes are appropriately scoped to the worktree subsystem. The inclusion of test updates indicates good attention to maintaining test coverage for the fixed behaviors.