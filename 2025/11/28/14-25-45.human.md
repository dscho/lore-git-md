This is a documentation patch fixing a command syntax typo in the git-pull man page. Stefan Rieche (submitting via GitGitGadget) corrects "git --rebase abort" to the proper "git rebase --abort" in the CONFLICT RESOLUTION section, matching the parallel "git merge --abort" example.

The patch is a straightforward one-line fix with no behavioral impact. This is version 2 of the patch, where Stefan has updated the commit message per Kristoffer Haugsbakk's earlier feedback - changing the prefix from "docs" to "doc" and removing redundant "man page" wording. The sign-off still shows the "snowdroppe" handle rather than the author's full name, which was noted in the thread as a minor deviation from project conventions.

The change itself is clearly correct and uncontroversial, though the discussion around it has highlighted some of Git's documentation formatting norms. The patch appears ready for inclusion pending any final sign-off identity alignment.