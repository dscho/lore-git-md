This thread documents a straightforward improvement to Git's advice messages, replacing references to `man` with `git help` for accessing manual pages. Kristoffer Haugsbakk's initial patch updated branch-related error messages in `branch.c` and `builtin/branch.c`, along with corresponding test updates in `t/t3200-branch.sh`.

Junio C Hamano suggested strengthening the justification beyond just platform compatibility (some systems lacking `man`) to also highlight the benefits of `git help`'s flexibility, including HTML output via `git help -w` and support for customized `help.format` settings. Kristoffer agreed and updated the rationale accordingly while keeping the minimal code changes intact - just 5 lines across 3 files that don't affect behavior beyond the wording improvements.

The discussion has reached clear consensus, with no outstanding objections. The patch is essentially ready for acceptance, representing a small but meaningful step in Git's ongoing documentation modernization efforts. The changes align with making Git's interface more consistent and platform-agnostic while preserving the advice system's plain text constraints.