This thread introduces a small but thoughtful documentation improvement that makes Git's advice messages more consistent by preferring `git help` over `man` references. The patch updates two branch-related error messages to suggest `git help check-ref-format` instead of the external `man` command, following Git's conventional help system. 

The changes touch `branch.c` and `builtin/branch.c` for the advice updates, plus a corresponding test modernization in `t/t3200-branch.sh` that replaces literal single quotes with the `${SQ}` test helper. The patch builds on earlier work from March 2024 (commit 8fbd903e) that first added these advice messages. 

With only 5 lines changed across 3 files, this is a focused improvement to both user-facing documentation consistency and test suite maintainability. The changes are purely mechanical with no functional impact beyond the wording of advice messages. The patch appears ready for merging as a straightforward documentation/refactoring improvement.