This is a translation (l10n) follow-up confirming a German localization issue in `git rev-parse --verify` error messages. Native German speaker Sören Krecker responds to brian m. carlson's earlier report, agreeing that "Revision" would be a more accurate translation than the current "Commit" for the error message. The confirmation addresses the core issue raised in the thread - that the German translation incorrectly implies the command only works with commit objects when it actually handles all Git object types (blobs, trees, tags, commits). Krecker's input as a native speaker provides the linguistic validation that was explicitly requested in the original report.

The message is brief but significant as it resolves the open question about whether "Revision" would be an appropriate German equivalent for the generic "revision" concept in this context. This aligns with the existing German translations of related terms like "Revisionsgang" (revision walk) and "Revisionsliste" (revision list) that were noted in the original report.

No technical details beyond the translation issue are discussed, as this is purely a localization matter. The thread now appears ready for someone to submit a patch updating the German translation in `builtin/rev-parse.c` to use "Revision" instead of "Commit" in the error message. The confirmation from a native speaker removes the uncertainty about the correct terminology that was present in the initial report.