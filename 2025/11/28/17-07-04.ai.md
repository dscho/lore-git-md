This is a technical discussion about the `git replay --revert` feature, focusing on the handling of disconnected commit ranges. Elijah Newren responds to Junio Hamano's proposal for future support of non-contiguous commit ranges in the revert operation.

Elijah raises important questions about how to properly parse complex revision ranges when supporting disconnected commits. He notes that Junio's example of `topic~5 topic~3..topic` presents parsing challenges that differ from other range patterns like `^topic~7 topic~5` or ancestry-path queries. The core issue is distinguishing between different types of range specifications when they appear together in the same command.

The email suggests that some new flag might still be needed (though not `--no-walk`) to properly handle these cases, and questions whether this flag should apply consistently across Git commands like `git log`. This implies the discussion has broader implications beyond just the `replay` command.

Key technical points raised:
- The need to distinguish between different range specification patterns
- Potential requirement for a new flag to properly handle disconnected ranges
- Question about whether such functionality should be consistent across Git commands
- Implicit concern about maintaining consistent parsing behavior between commands

This continues the thread's ongoing discussion about the boundaries of the current implementation versus future enhancements, with Elijah providing critical analysis of the parsing challenges involved in supporting truly disconnected commits. The exchange shows the maintainers carefully considering both the immediate feature implementation and its long-term architectural implications for Git's revision parsing system.