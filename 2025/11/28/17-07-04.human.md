This is a technical discussion about the design of Git's `--revert` functionality, specifically around handling disconnected commit ranges. Elijah Newren responds to Junio Hamano's proposal for handling non-contiguous commit ranges in `git replay --revert`.

The discussion focuses on parsing complex revision ranges where commits aren't necessarily connected (like selecting specific commits from different points in history). While agreeing that the current patch series shouldn't tackle this yet, Elijah raises important questions about how such functionality would work across Git commands.

Key points raised:
1. The challenge of distinguishing commit ranges in different contexts (e.g., `topic~5` could mean different things depending on surrounding syntax)
2. Whether a new flag would be needed (different from `--no-walk`) to properly handle these cases
3. The potential to extend this functionality to other commands like `git log`

Elijah's questions suggest this is a deeper architectural consideration that would affect multiple Git commands, not just `replay`. The discussion remains technical but productive, with both contributors clearly thinking through edge cases and consistency across the Git command interface. This appears to be laying groundwork for future enhancements rather than blocking the current `--revert` implementation.