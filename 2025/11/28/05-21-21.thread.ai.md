New AI thread summary:

This thread introduces a new environment variable `GIT_REF_URI` to configure Git's reference backend system, allowing specification of both backend type and storage location via URI syntax (e.g. `files:/path/to/refs` or `reftable:/path/to/refs`). The patch modifies core refs handling in `refs.c`, `refs.h`, and `refs/files-backend.c` to parse the URI format and initialize the appropriate backend. This change is part of the ongoing effort to make reference storage configurable, building on previous work by Patrick Steinhardt and Karthik Nayak on reftable integration.

Key technical details include: URI parsing logic that splits on the first colon, validation of supported backend types (currently files and reftable), and path resolution relative to repository directories. The implementation handles error cases like malformed URIs or unsupported backends. Open questions include URI syntax design tradeoffs versus separate config options, integration with existing `GIT_DIR`/`GIT_COMMON_DIR` mechanisms, and security implications of environment-variable configuration. Missing test coverage and documentation updates are noted as follow-up work needed. The change would enable explicit backend selection for testing and specialized workflows, advancing the refs backend abstraction effort.