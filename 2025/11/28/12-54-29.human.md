This is a documentation/consistency patch that updates branch-related error messages to use Git's built-in help system rather than the external `man` command. 

The patch changes two instances where users are advised to check branch naming rules - instead of suggesting `man git check-ref-format`, it now suggests `git help check-ref-format`. This aligns better with Git's standard help system and avoids relying on external tools. 

As part of the change, the author also standardizes on single quotes for command references (which is more conventional in Git) and updates the corresponding test to use the `{SQ}` test helper for better readability and maintainability. 

The changes are straightforward and clearly motivated - this appears to be a small but thoughtful improvement to make Git's error messages more consistent and self-contained. The patch touches three files with simple, focused changes and includes test updates to match.