This is a technical discussion about edge case handling in the `git replay --revert` feature, specifically around disconnected commit support. Junio Hamano responds to Siddharth Asthana's agreement to defer `--no-walk` functionality, arguing that even when implemented, `--no-walk` wouldn't properly address the core use case for selective reverts of non-contiguous commits.

Junio outlines a more sophisticated approach that would:
1. Leverage `setup_revisions()` parsing but bypass its walking behavior
2. Process commit ranges and individual commits as independent "islands"
3. Handle three distinct input types (singletons, ranges, and deduplicated sets)
4. Preserve the ability to specify complex combinations like `topic~5 topic~3..topic`

The email reinforces that the current series should focus on basic `--revert` functionality while clearly delineating this more advanced handling as future work. Junio's technical prescription suggests the eventual solution will need deeper integration with revision walking internals rather than just adding a `--no-walk` flag. This maintains the thread's trajectory of incremental development while providing clear architectural guidance for future disconnected commit support.