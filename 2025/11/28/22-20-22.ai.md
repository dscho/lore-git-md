This is a follow-up discussion in the `git-last-modified` bitmap initialization thread, focusing on code style improvements beyond the immediate bugfix.

Anders Kaseorg responds to Jeff King's suggestion about using `sizeof(lm->scratch->words)` for the bitmap clearing operation, pointing out that since `words` is a pointer, the correct syntax would be either `sizeof *lm->scratch->words` or `sizeof lm->scratch->words[0]`. This is a technical correction to King's proposed alternative to the original fix (which used `sizeof(eword_t)`).

The exchange shows the careful attention to detail in the Git mailing list's code review process, where small style improvements are discussed even after consensus has been reached on the core fix. This particular point relates to the broader discussion about making the code more maintainable and less error-prone, though it doesn't change the already-agreed-upon solution for the bitmap initialization bug.

The thread continues to focus on the bitmap handling in `builtin/last-modified.c`, with this email providing a precise technical correction to one of the suggested alternative implementations. The discussion remains constructive and focused on code quality, though no new changes to the patch are being proposed at this stage.