This is a technical clarification in the ongoing `git replay --revert` feature development. Siddharth Asthana acknowledges a misunderstanding in how the command handles commit ranges, correcting his earlier claim that users could already specify disconnected commits for selective reverts.

Elijah Newren pointed out that the current implementation actually processes all ancestors of any specified commits (like `git log` does), rather than just the explicit commits. The command fails when attempting true disconnected commits, either with an ordering error or a segfault. While the syntax `git replay --revert <target> <commit1> <commit4>` exists, it doesn't yet provide the intended selective functionality.

Siddharth agrees to defer implementing proper `--no-walk` support (which would enable true disconnected commit handling) to a future series, keeping the current submission focused on core `--revert` functionality. This maintains the thread's trajectory toward merging the basic feature while identifying a clear path for follow-up work. The exchange demonstrates careful technical review catching an important behavioral nuance before it could become a documentation inaccuracy.