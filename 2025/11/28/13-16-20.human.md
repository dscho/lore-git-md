This is a straightforward documentation patch fixing a typo in the `git-pull` man page. Stefan Rieche corrects the command syntax from the incorrect `git --rebase abort` to the proper `git rebase --abort` in the section describing how to abort a conflicted rebase operation.

The change is minimal (a single character moved from before to after "rebase") but technically important since the incorrect form wouldn't work as a Git command. The patch follows standard formatting and includes a clear commit message with sign-off.

This appears to be a clean, uncontroversial fix that aligns the documentation with actual Git command syntax. No discussion or controversy is expected, and the patch should be quickly mergeable.