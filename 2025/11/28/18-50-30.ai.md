This is a follow-up discussion in the `git-last-modified` documentation series, specifically addressing patch 1's implementation of NUL termination handling. Toon Claes responds to Junio C Hamano's technical review suggesting architectural improvements.

Junio had proposed simplifying the NUL termination implementation by:
- Replacing the `line_termination` int field with a boolean `null_termination`
- Eliminating the need for `LAST_MODIFIED_INIT` macro
- Using `OPT_BOOL` instead of `OPT_SET_INT` for cleaner option parsing
- Noting the current implementation hardcodes `\n`/`\0` despite the configurable termination design

Toon acknowledges the boolean approach is better but explains the current implementation followed existing `line_termination` patterns seen elsewhere in the codebase. He agrees to adapt the patch to use a boolean flag as suggested, which will:
- Simplify the initialization
- Make the option parsing more straightforward
- Better match the actual binary (NUL vs non-NUL) behavior being implemented

The exchange shows constructive technical discussion about proper API design for termination handling, with the author receptive to the suggested improvements. This addresses one of the key open questions from the thread summary about optimal NUL handling implementation. The resolution leans toward Junio's simpler boolean approach rather than the more general but unused termination character flexibility in the original patch.