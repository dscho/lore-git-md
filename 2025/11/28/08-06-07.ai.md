This is a technical follow-up in the ongoing discussion about dropping empty commits in `git replay`. Elijah Newren responds to Phillip Wood's patch, agreeing with the core approach but offering implementation feedback and test simplification suggestions.

Elijah confirms the behavior change makes sense, noting he had a similar local implementation. He agrees with dropping commits that become empty while preserving originally-empty commits, matching `git rebase`/`cherry-pick` behavior. The key technical discussion revolves around Phillip's question about the `result->tree` assignment - Elijah doesn't recall the original rationale but approves Phillip's change to use a temporary variable instead.

For the test changes, Elijah suggests simplifying the complex sed-based verification by:
1. Actually updating branches temporarily (with cleanup)
2. Using `--contained` instead of `--branches --ancestry-path` for clarity
3. Directly checking branch states rather than reconstructing them from print output

The test would then more straightforwardly verify that:
- Commit F (from topic1) is properly dropped when replayed since its changes exist upstream
- The topic1 branch correctly updates to point to commit C after F is dropped
- Originally-empty commits remain intact

This maintains all the verification goals while being more maintainable. The thread shows technical alignment on the behavior change itself, with only implementation polish remaining.