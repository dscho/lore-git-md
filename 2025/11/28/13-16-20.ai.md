Documentation patch fixing a command syntax typo in the git-pull man page.

Stefan Rieche submits a small documentation correction that fixes the syntax of the rebase --abort command in git-pull's man page. The patch changes "git --rebase abort" to the correct form "git rebase --abort" in the section describing how to abort a conflicted pull operation. This matches the actual command syntax used by Git and aligns with the parallel "git merge --abort" example shown in the same paragraph.

The change is straightforward and clearly correct - it's a simple typo fix in documentation with no behavioral implications. The patch touches only one line in Documentation/git-pull.adoc, changing the command syntax while preserving all surrounding context. The commit message is minimal but sufficient for this type of trivial documentation fix.

This appears to be a standalone patch with no relation to any ongoing series or discussion. The fix targets the asciidoc source for git-pull's man page, specifically the command syntax in the "CONFLICT RESOLUTION" section. No tests or other files are affected.