Bugfix follow-up addressing a minor technical correction in the `git-last-modified` bitmap initialization discussion. 

This is a quick reply from Anders Kaseorg to Jeff King's suggestion about using `sizeof(lm->scratch->words)` in the bitmap clearing code. Kaseorg points out that since `words` is a pointer, the correct syntax would need to dereference it (either `sizeof *lm->scratch->words` or `sizeof lm->scratch->words[0]`). 

The exchange shows careful attention to detail in the ongoing discussion about proper memory initialization in the `git-last-modified` command. While the core bug was already fixed in the previous patch, this note ensures the surrounding discussion maintains technical accuracy. The tone remains constructive, with Kaseorg providing the correction matter-of-factly. 

The thread continues to demonstrate the project's attention to code correctness, even in peripheral discussion points about potential alternative implementations. No new changes to the code are proposed here - this is purely a clarification of the earlier technical discussion.