Here's a human-readable summary of the thread so far:

The thread discusses a limitation in Git's submodule behavior when working with Gerrit's review references. Alon Bar-Lev explains that while Gerrit stores review changes under `refs/changes/` (rather than the standard `refs/heads/` branches), Git submodules currently require a branch name for checkout operations. This creates friction in workflows that rely on Gerrit's review system.

The core issue is that submodule configurations mandate branch tracking even for simple checkouts where any reference would suffice. Alon questions whether submodules could be made more flexible to either work without branches or search alternative ref namespaces like Gerrit's `refs/changes/`. The discussion touches on how this interacts with existing submodule configuration options like `submodule.<name>.branch`.

This appears to be a genuine workflow limitation that affects teams using Git submodules with Gerrit for code review. While the email clearly identifies the pain point, it doesn't yet propose a specific solution or patch to address the behavior. The topic may require consideration of how such a modification would interact with existing submodule commands and configuration options.