This is a bugfix for the `--shallow-since` functionality in Git's shallow clone feature. The patch addresses an edge case where border commits in the shallow clone list might incorrectly include descendant commits when they should be excluded.

The issue occurs during shallow cloning when some border commits in the list are direct descendants of others. The current implementation can incorrectly mark these descendant commits as borders when they should instead be expanded to include additional parent commits. The patch modifies the commit flagging logic in `shallow.c` to properly handle these cases.

The changes introduce a more sophisticated approach to flag management:
1. It tracks whether commits must remain non-shallow (due to parent relationships)
2. Properly propagates shallow/non-shallow flags to parent commits
3. Only includes appropriate commits in the final border list

The commit message provides a clear 4-step explanation of the new logic, and the code changes appear focused and well-contained to the specific issue. This seems like a solid fix for a subtle edge case in shallow cloning behavior.