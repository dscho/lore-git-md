Documentation clarification in config file handling thread.

Delilah Ashley Wu acknowledges Kristoffer Haugsbakk's observation that the inconsistency in `git config list --global` output (showing only `$HOME/.gitconfig` when both that and `$XDG_CONFIG_HOME/git/config` exist) is specifically a problem when both config files are present. This confirms the scope of the issue being addressed in Wu's patch series to align Git's config listing behavior with its actual reading behavior.

Wu indicates this clarification will be incorporated into the v2 revision of the series, suggesting the author is responsive to feedback and attentive to precise documentation of edge cases. The exchange shows collaborative refinement of the patch series' context, though no technical changes to the implementation are discussed here.