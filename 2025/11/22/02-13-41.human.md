This is a question about submodule behavior with non-standard references, specifically in the context of Gerrit code review.

Alon Bar-Lev describes a workflow issue when trying to review changes in Gerrit that span both a parent repository and its submodule. The problem arises because Gerrit stores changes under `refs/changes/` rather than `refs/heads/`, and Git's submodule checkout mechanism appears to find references only under `refs/heads/` by default.

The email asks whether it's possible to either:
1. Configure a submodule without requiring a branch (just using a direct reference)
2. Make Git search for references in `refs/changes/` during checkout

The question seems well-motivated by a real workflow pain point in Gerrit-based development, though it's unclear from the email whether this is a limitation of Git's submodule implementation or a missing configuration option. The tone suggests genuine confusion about why branch information is required during checkout when a specific reference is already provided.

This appears to be a niche but legitimate use case that might interest Git developers working on submodule functionality or those familiar with Gerrit integration scenarios. The question could spark discussion about submodule reference resolution flexibility or Gerrit-specific workflow improvements.