This is part 10/10 of Ezekiel Newren's xdiff refactoring series, focusing on type safety improvements for Rust FFI compatibility. The patch renames the `rindex` field in `xdfile_t` to `reference_index` to better reflect its purpose as a mapping between compacted array indices and original file lines. This change is purely a rename with no functional impact, completing the structural updates to `xdfile_t` that began in earlier parts of the series.

The patch updates all references to `rindex` across xdiff's core files (xdiffi.c, xprepare.c) and the type definition in xtypes.h. The new name `reference_index` more clearly describes the field's role in maintaining the relationship between processed diff records and their original file positions. This follows the series' pattern of improving code clarity while maintaining backward compatibility.

As with previous parts, the change is mechanical and behavior-preserving, touching only the field name while keeping all type conversions (to `size_t` in this case) consistent with part 9's work. The patch completes the `xdfile_t` modernization that included converting `nrec`, `nreff`, and now `reference_index` to `size_t` for proper unsigned array indexing.

The series has maintained strong reviewer engagement throughout, with Patrick Steinhardt and Phillip Wood providing FFI-specific expertise and Junio Hamano overseeing the technical direction. This final patch in the series demonstrates the same careful attention to detail seen in previous parts, with thorough updates to all affected code paths and no outstanding review concerns.