This is part 9/10 of Ezekiel Newren's xdiff refactoring series to modernize type usage for Rust FFI compatibility. The patch changes the `rindex` field in `xdfile_t` from `long` to `size_t`, continuing the systematic type safety improvements across the xdiff subsystem.

The change is straightforward - `rindex` is used as an array index, so `size_t` is the more appropriate type. The commit message notes this won't require cascading changes since the field is only used for direct array indexing. This follows the pattern established in earlier parts of the series where array indices and sizes are being converted to `size_t` for correctness and consistency.

The patch appears mechanically sound, with a clear single-purpose change that fits into the larger series' goals. It maintains the series' approach of making incremental, behavior-preserving type changes while improving type safety at interface boundaries. The change has already been reviewed by FFI experts Patrick Steinhardt and Phillip Wood, with Junio Hamano confirming the technical direction.

This continues the series' steady progress toward completing the xdiff modernization while preserving backward compatibility. The remaining work appears to be focused on completing type consistency across the subsystem.