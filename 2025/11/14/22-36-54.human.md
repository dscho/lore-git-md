This is part 8 of Ezekiel Newren's ongoing xdiff refactoring series to modernize core xdiff structures for Rust FFI compatibility. The patch converts the `nreff` field in `xdfile_t` from `long` to `size_t`, continuing the systematic type safety improvements across the xdiff subsystem.

The change makes `nreff` (which tracks the number of elements in the `rindex` array) use the more appropriate unsigned `size_t` type for counting memory elements. The implementation updates both the type declaration in `xtypes.h` and removes temporary `nreff` variables in `xprepare.c` in favor of directly using the struct field. This matches the pattern established in earlier parts of the series for handling array sizes.

The patch is a straightforward mechanical change that follows the series' established approach of incremental, behavior-preserving updates. It builds on previous work that standardized other xdiff fields to more appropriate types. The commit message is brief but clear about the rationale for using `size_t` in this context.

This continues the series' steady progress toward type safety at interface boundaries while maintaining backward compatibility. The change appears well-contained and follows the patterns reviewers have already approved in earlier parts of the series.