This is part 8 of Ezekiel Newren's xdiff refactoring series, focusing on type safety improvements for Rust FFI compatibility. The patch converts the `nreff` field in `xdfile_t` from `long` to `size_t`, following the established pattern of using unsigned types for counts and indices. The change affects both the struct definition in `xtypes.h` and its usage in `xprepare.c`.

The modification simplifies the code by eliminating temporary `nreff` variables in `xdl_cleanup_records()`, instead updating the struct field directly during iteration. This matches the field's purpose of tracking the number of elements in the `rindex` array. The change is behavior-preserving and maintains consistency with previous parts of the series that standardized other size-related fields to `size_t`.

This continues the systematic modernization of xdiff's core data structures, with particular attention to interface boundaries where type mismatches could cause issues in cross-language interoperability. The patch follows the series' incremental approach, focusing on one logical change per commit while ensuring all related algorithms are updated to handle the new type correctly.