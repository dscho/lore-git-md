This is part 9 of Ezekiel Newren's xdiff refactoring series, continuing the type safety improvements for Rust FFI compatibility. The patch changes the `rindex` field in `xdfile_t` from `long` to `size_t`, following the established pattern of converting array indices to unsigned types.

The change is straightforward since `rindex` is only used for array indexing and doesn't require cascading changes elsewhere. This maintains consistency with previous parts of the series that standardized similar fields (`nrec`, `nreff`, etc.) to `size_t`. The commit message notes this change has no refactoring impact beyond the direct type conversion.

This continues the series' systematic approach to type modernization, with each part focusing on specific fields while maintaining backward compatibility. The patch follows the same review and implementation patterns seen in earlier parts, with clear justification for the type change and minimal diff impact.