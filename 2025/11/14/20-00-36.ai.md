This is a CI/build system issue affecting Git's test infrastructure. Phillip Wood reports that Ubuntu-based CI jobs are failing because the system's `sudo` command (apparently a Rust implementation) no longer supports the `--preserve-env` flag used in Git's test scripts. The error manifests in GitHub Actions runs, with a specific failing job linked in the message.

The immediate problem centers around environment variable preservation during test execution, a critical requirement for Git's test suite. Phillip suggests two potential solutions: either ensuring the installation of the traditional C-based `sudo` implementation, or switching to `runuser` as an alternative command. The message seeks input from others who might know how to control which `sudo` implementation gets installed in Ubuntu environments.

This appears to be an urgent but straightforward infrastructure issue that needs resolution to unblock CI testing. The proposed solutions suggest the problem is well-understood, though the best path forward may depend on Ubuntu package management specifics. The thread is just starting, so no consensus or implementation has been reached yet.