This is a test patch adding UTF-8 width calculation verification, specifically for CJK (Chinese/Japanese/Korean) characters, as part of a series addressing display alignment issues in Git's repository output.

The patch introduces a new unit test file `t/unit-tests/u-utf8-width.c` that thoroughly exercises Git's UTF-8 width calculation functions. It tests both `utf8_strnwidth` and `utf8_strwidth` with various CJK character scenarios including: basic Chinese strings, mixed ASCII/CJK text, Japanese and Korean characters, ANSI escape sequences, empty strings, length-limited cases, and invalid/truncated UTF-8 sequences. The tests verify that CJK characters (which typically occupy 2 display columns) are measured correctly alongside ASCII (1 column).

The test suite follows the Clar framework conventions and includes build system updates (Makefile and meson.build) to incorporate the new tests. The author transparently notes AI tool assistance (Claude Code) in development with proper Co-developed-by attribution. This appears to be a well-scoped foundational test for the subsequent patch that will fix the actual display alignment issues in `builtin/repo.c`.

Key technical details for future reference:
- Tests validate width calculations for CJK characters in `utf8.[ch]` functions
- Includes edge cases for invalid/partial UTF-8 sequences
- Build system hooks added in both Makefile and meson.build
- Follows existing unit test patterns and naming conventions
- Prepares for fixing CJK alignment in repository structure display