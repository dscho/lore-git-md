This is part 3 of Ezekiel Newren's xdiff type safety refactoring series (part 4 overall), focusing on making the xrecord_t.ptr field explicitly uint8_t instead of char. The change reflects that this field represents raw bytes in memory rather than text characters.

The patch updates the ptr field declaration in xtypes.h and propagates this change through all xdiff algorithm implementations (xdiffi.c, xemit.c, xmerge.c, xpatience.c, xprepare.c, xutils.c). Most changes are mechanical casts at interface boundaries where char* is still expected, with one substantive change in get_indent() where a local char variable becomes uint8_t. The commit notes 7 specific locations where casting was unnecessary but left for consistency.

This continues the series' pattern of explicit type safety improvements while maintaining backward compatibility through interface casting. The change is behavior-preserving and follows the established approach from earlier parts of the series. The patch appears well-scoped and mechanically sound, with all xdiff algorithms updated consistently.