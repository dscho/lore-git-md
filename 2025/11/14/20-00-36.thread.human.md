This CI infrastructure thread began by addressing two issues - a resolved `sudo` incompatibility on Ubuntu and ongoing macOS Perforce test reliability concerns. The discussion has now narrowed to focus entirely on the Perforce testing setup.

After initial confusion about version compatibility, Koji Nakamaru confirmed that Perforce r23.2's native arm64 binaries (in the `bin.macosx12arm64` directory) work correctly on macOS without requiring Rosetta emulation. This validation came after Junio Hamano questioned the need for upgrading to r24.2, with Nakamaru clarifying that both versions contain the necessary arm64 support, making the version bump unnecessary.

The technical path forward is now clear: the team can either use these confirmed-working native arm64 binaries or configure x86_64 runners. With the version compatibility question settled, the native arm64 approach appears technically viable. The thread awaits Junio's evaluation of these remaining options to finalize the implementation decision for improving macOS CI reliability.