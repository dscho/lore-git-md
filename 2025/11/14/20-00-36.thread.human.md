The thread began by addressing a resolved issue with Ubuntu's Rust-based `sudo` (fixed in maint-2.51) before shifting focus to macOS CI reliability, particularly Perforce test flakiness caused by running x86_64 binaries through Rosetta on arm64 machines. Koji Nakamaru initially proposed using arm64-native Perforce r24.2 binaries as a solution, which Junio Hamano questioned regarding version selection and alternative runner configurations.

Nakamaru's latest email clarifies that the original r23.2 version actually do contain functional arm64 binaries (contrary to his initial assumption), demonstrating successful test runs. This means the architecture mismatch can be resolved without a version bump, maintaining compatibility with existing Linux (x86_64) setups. While this addresses the version selection question, the broader discussion about optimal runner configurations (native arm64 vs x86_64) remains open, with Junio's original concerns about runtime costs still pending evaluation. The thread has effectively narrowed to determining whether the simpler arm64 binary fix suffices or if deeper infrastructure changes are warranted.