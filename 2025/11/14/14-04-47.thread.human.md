Kristoffer Haugsbakk sent a documentation patch improving cross-references in the `git-commit` manpage, specifically for the `--dry-run` format options. The patch standardizes references to `git-status(1)` where status concepts are mentioned, addressing inconsistencies between different parts of the documentation. Changes include adding proper links for the `--branch` option (to match existing `--short` docs), clarifying that `--long` matches `git-status`'s default output, and fixing a bare "status" mention for `--null`.

Junio C Hamano has now marked this patch for inclusion in the 'next' branch, indicating it's ready to graduate to the main codebase. The changes are purely documentation improvements with no behavioral impact, maintaining consistency between the shared internals of `commit --dry-run` and `status` commands. The patch cites historical inconsistencies (between commits 7c9f7038 and f3f47a1e) as motivation for these clarifications.