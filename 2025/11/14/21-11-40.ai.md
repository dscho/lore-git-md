This is a follow-up in the SHA-1/SHA-256 interoperability patch series, specifically addressing CI warnings in the Rust implementation. Junio C Hamano reports that recent CI changes (commit 4b44c464) have exposed Clippy warnings in patches 12-13 of the series. He provides a temporary workaround that makes several Rust code adjustments to satisfy the linter, though notes some changes may need deeper review.

The changes include:
- Replacing `assert_eq!(h.is_safe(), true)` with `assert!(h.is_safe())` in `src/hash.rs`
- Adding a type alias `TwoHashesTestVectorEntry` in `src/loose.rs` to clarify test data structure
- Changing various `assert_eq!(x, false)` to `assert!(!x)`
- Removing unnecessary borrows in `map_object` and `map_oid` calls

Junio flags the first hunk in `src/loose.rs` as potentially needing more expert review, while approving other changes as reasonable improvements. The email shows the maintainer actively shepherding the Rust code quality while the series undergoes final refinements before what will be Git's first production Rust code. The parallel Rust infrastructure reorganization (moving to Cargo workspace) remains a future consideration that may affect file locations but doesn't impact these immediate CI fixes.