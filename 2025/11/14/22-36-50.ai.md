Refactoring patch in the ongoing xdiff modernization series, specifically updating the `xrecord_t.size` field from `long` to `size_t` for proper memory size representation. This is part 4 of 10's systematic type safety improvements for Rust FFI compatibility.

The patch mechanically updates all uses of `rec->size` across xdiff/ (xdiffi.c, xemit.c, xmerge.c, xprepare.c) to handle the type change, adding explicit casts where needed to maintain compatibility with existing interfaces that expect `long`. The core change is straightforward - `size_t` is indeed the correct type for memory sizes - but requires careful auditing of all call sites since xdiff's algorithms frequently pass these values through multiple layers.

Notable implementation details:
- Preserves all existing behavior through explicit casting at interface boundaries
- Updates loop variables to use `size_t` where iterating over `rec->size`
- Maintains backward compatibility with functions like `xdl_blankline()` that still take `long` parameters
- Includes the expected mechanical updates to xdiff's core files (xdiffi.c, xemit.c, etc.)
- Follows the established pattern from earlier parts of the series for type safety improvements

This continues the incremental, behavior-preserving approach that has received maintainer approval in previous rounds. The change is low-risk but touches many call sites, requiring careful review of the casting decisions. Part of the broader effort to establish clear type contracts for cross-language interoperability.