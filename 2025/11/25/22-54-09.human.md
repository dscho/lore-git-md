This is a build system discussion about artifact handling in Git's Makefile and build systems.

Junio Hamano follows up on Johannes Schindelin's earlier suggestion about improving how the `strip` target handles binaries in the Makefile. While acknowledging that the original patch to include `scalar` in the strip target is technically correct, Junio sees an opportunity for a more comprehensive solution that would properly categorize different types of build artifacts.

The discussion has revealed that meson likely handles stripping automatically for all executables, while CMake's stripping support appears to be lacking. Junio decides to queue the original minimal patch for now, leaving the door open for more substantial Makefile restructuring that would properly separate programs from other artifacts like DLLs.

This email represents a checkpoint in an ongoing conversation about Git's build system architecture, where a simple one-line fix has sparked broader questions about artifact categorization and build system strategy. The immediate change is uncontroversial, but the thread hints at more significant build system decisions ahead regarding CMake's role and the relationship between Makefile, meson, and CMake builds.