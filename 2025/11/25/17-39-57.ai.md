This is a reply in the ongoing thread about Windows/Meson CI test output visibility fixes. The email provides feedback on Jeff King's patch that introduced a Meson-specific test runner script for Windows CI.

Johannes Schindelin acknowledges the patch as a good fix but expresses skepticism about the value of using Meson on Windows given its low adoption in the MSYS2 ecosystem compared to make and CMake. He notes this represents yet another build system requiring duplicate infrastructure, referencing an unspecified historical pattern of repetition.

The reply also addresses Jeff's observation about path handling differences between PowerShell and bash, explaining that Windows-style path conversion occurs when using PowerShell which isn't fully reverted when bash is called, causing test failures. This confirms the need to keep using `pwsh` as the shell for these tests.

The technical discussion remains focused on the Windows CI infrastructure, specifically around:
- Path conversion behavior between shells
- The tradeoffs of supporting multiple build systems
- The specific test output handling being fixed in this series

No new technical changes are proposed - this is primarily review commentary on the existing approach. The thread appears to be converging toward acceptance of the solution despite reservations about the broader Meson adoption context.