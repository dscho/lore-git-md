Code organization refinement in the ongoing `git replay --revert` feature development. Junio Hamano provides concrete feedback on the message header generation refactoring, suggesting a cleaner approach to eliminate code between `do_pick_commit()` and the new revert functionality. His proposed diff shows extracting the revert message formatting into a new `sequencer_format_revert_header()` helper function.

The email notes this change currently conflicts with Patrick Steinhardt's parallel "history" series refactoring work, recommending delaying integration until that lands. This continues the thread's focus on implementation quality improvements, specifically around code deduplication and maintainability concerns raised in earlier reviews. The core functionality remains unchanged while the internal architecture evolves toward cleaner separation of concerns.

Key technical points for future reference:
- New helper function `sequencer_format_revert_header()` consolidates revert message generation
- Still handles both "Revert" and "Reapply" cases from original subject lines
- Conflicts with ongoing sequencer.c refactoring in Patrick's series
- Maintains same behavior for `--commit-use-reference` case

This represents incremental refinement rather than a major direction change, with the maintainer guiding the implementation toward better code organization while coordinating with parallel development efforts.