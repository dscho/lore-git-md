This is a follow-up in the ongoing `git replay --revert` feature discussion, where Junio Hamano reports test failures when applying the patch to current master. The patch adds server-side revert capability to `git replay`, but appears to have test issues that surface even in normal (non-sanitizer) builds.

Junio notes the patch currently conflicts with Patrick Steinhardt's parallel "history" refactoring work, suggesting it may need to wait for that series to stabilize before being rebased. More immediately concerning, several tests fail - notably test 23 which checks basic revert functionality. The test expects one line of output from `git replay --revert` but gets empty output instead, indicating the revert operation isn't producing the expected branch update.

While this doesn't invalidate the feature's design (which has received positive feedback from Johannes Schindelin and others), it does highlight implementation bugs that need fixing before merging. The test failures appear reproducible in normal test runs, not just under sanitizers, suggesting they're genuine issues rather than environment quirks. This email serves as a quality control checkpoint, flagging problems that the author will need to address in a v2 submission.