This is a code review follow-up in the `git replay --revert` feature thread. Junio Hamano provides concrete implementation feedback on the revert message generation logic, suggesting a refactoring to eliminate code duplication between the new `sequencer_format_revert_header()` function and existing code in `do_pick_commit()`.

The review focuses on maintainability improvements, proposing to extract the shared revert message formatting logic into a common helper function. Junio provides a sample diff showing how the `do_pick_commit()` implementation could be simplified to call the new helper function, reducing duplicate code while maintaining identical behavior. This addresses one of the key concerns raised in earlier thread discussion about code organization.

The suggested change would modify `sequencer.c` to consolidate the revert header generation logic in one place, making the codebase more maintainable. This follows the thread's broader theme of refining implementation quality while preserving the core functionality for server-side reverts. The review maintains technical alignment with the feature's goals while pushing for cleaner internal architecture.