Here's a human-readable summary of the thread's current state:

This thread is discussing the addition of a `--revert` capability to Git's `replay` command, primarily to support GitLab's Gitaly service. The feature is now in its second version, with the core functionality implemented in the latest patch. The implementation treats reverts as a distinct operation mode (separate from existing `--onto` and `--advance` modes) that creates new commits undoing changes from a specified range, following the same commit-by-commit approach as `git revert`.

Key technical decisions have been settled through discussion with maintainers: reverts will use swapped tree arguments to reverse diffs, maintain `git revert`-style commit messages, and preserve atomic ref updates. The implementation reuses existing merge infrastructure while adding new tests for revert-specific cases. There's ongoing debate about command syntax naming (with Junio Hamano suggesting alternatives like `--rebase`/`--cherry-pick`), but the current technically-precise names appear likely to stay.

The thread has reached architectural consensus on several important points: revert is a standalone mode, complex revision ranges are supported through Git's standard parsing (though truly disconnected commits would require future work), and the implementation maintains consistency with existing `replay` behavior. The latest patch addresses all technical feedback, including proper handling of revision ranges and integration with the existing infrastructure.

Remaining open questions center on final test coverage, documentation of edge cases, and how this feature might eventually handle disconnected commits. The implementation appears mature and well-tested, with the discussion now focused on final polishing before integration.