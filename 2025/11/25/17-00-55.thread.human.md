The thread discusses adding `--revert` capability to `git replay`, primarily for GitLab's server-side operations. The initial implementation allows reverting commits directly on bare repos by reusing cherry-pick infrastructure, but testing revealed failures in basic revert operations (tests 23-25, 27, 31) where expected commits weren't being produced.

Reviewers have raised significant concerns about the design. Phillip Wood questioned the practical utility, noting the tests demonstrate unrealistic scenarios producing empty commits, and suggested making `--revert` a distinct mode with reverse commit ordering. Johannes Schindelin had earlier proposed an alternative bulk-revert approach using `git merge-tree`. 

Elijah Newren has now weighed in, agreeing with Phillip's concerns and framing the issues more broadly. He suggests `--revert` should be a primary operation mode (alongside `--onto` and `--advance`) rather than a flag, as the current implementation doesn't match user mental models of version control. He also notes the confusing terminology when combining `--revert` with `--advance`.

The discussion has shifted from implementation details to fundamental design questions. Key open issues include whether to:
1. Fix the current implementation's test failures
2. Restructure `--revert` as a primary mode incompatible with other operations
3. Consider Johannes' bulk-revert approach instead
4. Completely overhaul the test suite to better model real-world usage

Junio Hamano has been guiding the implementation details but notes this conflicts with Patrick Steinhardt's parallel "history" series work. The thread now awaits either a redesign proposal or resolution of whether to proceed with fixes to the current approach.