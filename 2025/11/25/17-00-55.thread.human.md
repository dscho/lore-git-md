Here's a human-readable summary of the thread's current state:

The discussion continues around adding revert capability to `git replay`, with the feature now in its final polishing stages. The core design has solidified, establishing `--revert` as a distinct operation mode that preserves commit-by-commit reversal with individual commit messages. The implementation handles complex revision ranges through Git's standard parsing, though limited to connected commit sets.

Junio Hamano recently clarified important technical boundaries around revision range handling. While expressions like `master..topic1 topic2` work for selecting commits across branches, they still operate within Git's connected commit model. True support for disconnected commits would require new syntax or flags beyond current capabilities. This maintains consistency with `git log` behavior while leaving room for future enhancements.

Key participants have reached consensus on several fronts:
- The interface requires explicit target branch + commit range specification
- The implementation preserves Git's standard revision parsing behavior
- Complex cases like non-contiguous commit reversal work within connected histories
- Documentation will clearly state current limitations regarding disconnected commits

Open questions remain about finalizing test coverage and documenting tradeoffs, but the architectural direction is now clear. The thread shows the project's careful balance between implementing useful new functionality while maintaining Git's consistent parsing model. Junio continues guiding the effort with attention to code organization, documentation precision, and long-term architectural implications.