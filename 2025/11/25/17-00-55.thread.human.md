This thread centers on extending `git replay` to support reverting commits, particularly for GitLab's Gitaly service. The feature has evolved through extensive discussion with key contributors like Elijah Newren, Phillip Wood, and Junio Hamano, reaching consensus on design choices. The implementation now handles reverts as a distinct operation mode, preserving commit-by-commit reversal with individual messages while supporting complex revision ranges through Git's standard parsing.

A recent technical clarification revealed that while the command accepts syntax for selecting specific commits (like `<commit1> <commit4>`), it actually reverts all ancestors of those commits—matching Git's standard revision behavior rather than operating on truly disconnected commits. Siddharth Asthana, the patch author, acknowledges this limitation and plans to focus the current series on core functionality, deferring true disconnected commit support (which would require `--no-walk` implementation) as a future enhancement.

The thread shows the feature nearing completion with only final polishing remaining. Siddharth has addressed code quality concerns, improved documentation accuracy, and expanded test coverage while incorporating feedback from maintainers. While a naming debate continues (Junio prefers `--rebase`/`--cherry-pick` while Elijah defends the current `--onto`/`--advance` terminology), the technical implementation has solidified. The discussion remains productive, with clear alignment on delivering a working core implementation first before tackling edge cases like disconnected commits.