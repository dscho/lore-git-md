Final polish for a submodule bugfix.

This is a brief exchange where Junio Hamano accepts Elijah Newren's suggested wording improvements to the commit message for a submodule bugfix. The patch in question fixes a segfault in `git submodule add` that occurs when processing incomplete .gitmodules entries (those missing path definitions). The thread summary indicates this is the final cleanup before applying the straightforward fix.

The changes are purely editorial - adjusting verb tense ("accessing" becomes "tries to access") and fixing a possessive form ("caller's" becomes "caller"). The substantive content of the patch remains unchanged. This appears to be the final step before the fix gets merged, with the thread wrapping up after addressing these minor wording nits.

The patch itself (not shown in this email) has already been reviewed and includes both the NULL check fix and test coverage, with a related issue in update_submodule() marked for future work. This exchange shows the Git maintainer process in its final polishing stage before applying a well-vetted fix.