This is a follow-up review comment on the `git replay --revert` feature addition. Junio Hamano provides two main points:

First, he shares a concrete code suggestion for refactoring the revert message generation logic into a shared helper function (`sequencer_format_revert_header()`), extracting it from `do_pick_commit()`. This addresses earlier concerns about code duplication in the patch series.

Second, he notes a timing consideration - the patch currently conflicts with Patrick Steinhardt's ongoing "history" series refactoring work. He suggests waiting for that series to stabilize before proceeding with version 2 of this patch, allowing the changes to build on top of the refactored codebase.

The email shows the patch is progressing through review, with both implementation improvements being discussed (the message generation refactoring) and broader integration considerations (coordination with other in-flight work). The core functionality appears accepted, with attention now on code organization and timing of the changes.