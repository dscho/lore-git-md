This is a build system discussion continuing the thread about artifact handling in Makefile targets, now touching on CMake/meson considerations.

Johannes Schindelin responds to Junio Hamano's proposal for restructuring Makefile artifact handling, agreeing with the approach but raising concerns about build system consistency. The reply clarifies that meson appears to handle stripping automatically (though Johannes notes unfamiliarity with meson specifics), while CMake lacks stripping support entirely as it was primarily designed for Windows/Visual Studio development workflows.

A significant new development is Johannes' suggestion to potentially abandon CMake support entirely, citing meson's adoption as the modern build system and CMake's failure to meet Visual Studio debugging needs. This would represent a notable shift in build system strategy if pursued.

The technical discussion remains focused on:
- Makefile variables `OTHER_PROGRAMS` vs new `OTHER_ARTIFACTS`
- `strip` and `artifacts-tar` target behaviors
- Windows-specific `INCLUDE_DLLS_IN_ARTIFACTS` handling

The thread now has two parallel tracks:
1. The immediate Makefile restructuring (where consensus seems to be forming)
2. The broader build system strategy question (CMake's future, meson adoption implications)

This email introduces new uncertainty about long-term build system direction while moving the immediate Makefile changes forward. The CMake abandonment suggestion in particular may spark debate given its implications for Windows developers.