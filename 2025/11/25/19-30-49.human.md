This is a code review follow-up in the `git replay --revert` feature thread. Junio Hamano provides specific implementation feedback on the revert message generation logic, noting that the new `sequencer_format_revert_header()` function duplicates code from `do_pick_commit()`. He suggests refactoring this as a preliminary patch to eliminate the duplication before the main feature patch.

The review includes a concrete diff showing how to modify `do_pick_commit()` to call the new helper function, which would reduce the code by 13 lines while maintaining identical behavior. This is presented as a starting point for the author to consider, not as a mandatory change. The feedback continues the thread's focus on code organization and maintainability improvements for this server-side revert feature.