This is a technical discussion about Git filter limitations for secret management workflows, continuing the thread about clean/smudge filter constraints. Kache Hit responds to Junio C Hamano's clarification about smudge filter limitations by proposing a simplified "line-wise ignore" model analogous to gitignore behavior.

The email makes several key points: First, it acknowledges Junio's technical explanation about why smudge filters can't reliably access working tree files during checkout operations. Second, it reframes the original proposal as a line-based ignore mechanism that would preserve local changes to ignored lines during checkout, similar to how gitignore preserves entire ignored files. Third, it identifies a new edge case where unused secrets from previous working copies could be lost during checkout, analogous to Git's handling of local changes during branch switches.

Kache maintains that while Git's current filter system supports several transformation patterns (idempotent operations, perfect mappings, and info addition/removal), it cannot support the desired "remove info -> restore info" workflow needed for secret redaction. The discussion highlights a fundamental architectural constraint in Git's filter system while continuing to explore alternative conceptual models for the original use case.

The thread remains focused on finding a solution that respects Git's technical constraints while addressing the secret management and selective line-ignoring requirements, particularly for mixed-content configuration files. Kache's latest response suggests the need for either new Git functionality or a different approach that doesn't rely on smudge filters accessing working tree state.