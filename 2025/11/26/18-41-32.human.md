This is a design discussion email in the ongoing thread about adding revert capability to `git replay`. Junio Hamano is responding to Elijah Newren's critique of the current `--revert` flag implementation.

The key development is that Elijah has framed revert as a fundamentally distinct operation mode (like `--onto` for rebasing or `--advance` for cherry-picking) rather than just a flag. This addresses core concerns about confusing interactions between flags and better matches Git's existing sequencer model. Junio agrees this clarifies the design problem and answers his own questions about flag incompatibility.

The email reinforces the thread's growing consensus that the current implementation needs significant redesign rather than incremental fixes. While technical details like test failures remain, the focus has shifted to higher-level architecture - specifically whether to restructure revert as a primary operation mode incompatible with other modes like `--onto`.

This moves the discussion forward by validating the need for deeper changes while narrowing the design space toward Elijah's proposed approach. The exchange suggests the patch author will likely need to rework the implementation substantially to align with this clarified vision.