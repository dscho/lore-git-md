This thread documents and improves Scalar's configuration settings, building on a September discussion about better config documentation. The series now has five patches: the first implements "# set by scalar" annotations as suggested by Patrick Steinhardt, the second fixes a performance regression by correcting the `index.skipHash` setting, the third removes five stale config values, the fourth reorganizes config handling alphabetically, and the fifth adds comprehensive documentation for all remaining settings.

Junio Hamano has now reviewed the first patch and suggests introducing a helper function (`scalar_config_set()`) to reduce duplication in the annotation implementation, though he confirms the current approach works as-is. The series appears on track, with only minor implementation refinement suggested. Remaining open questions include coordinating with Git for Windows maintainers about the `core.fscache` removal and verifying Windows-specific behavior changes with users. The documentation may still receive final wording feedback, but the technical content appears settled.