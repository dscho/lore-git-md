This is a follow-up in the ongoing `git replay` revert feature thread, addressing test failures identified by Junio Hamano. The core issue stems from a mismatch between the test expectations and the default behavior of atomic ref updates (REF_ACTION_UPDATE) which produces no stdout output, while the tests were written assuming --ref-action=print behavior.

Siddharth explains the test failures occur because the tests expect output parsing that only works with explicit print mode. The fix involves modifying the tests to either explicitly request print output mode when needed or verify ref states directly rather than parsing stdout. This addresses the empty output failures in tests 23-25, 27, and 31 from t3650-replay-basics.sh.

The response confirms these are behavioral rather than sanitizer-specific issues, as Junio observed the same failures in regular test runs. This clarification helps narrow the debugging scope from potential memory issues to test/behavior alignment. The fix appears straightforward and focused on test expectations rather than core functionality, suggesting the revert implementation itself remains sound.

This exchange shows progress in test suite validation while the broader design discussions about operation modes and series restructuring continue separately. The test adjustments align with the planned v2 submission that will wait for prerequisite series to stabilize.