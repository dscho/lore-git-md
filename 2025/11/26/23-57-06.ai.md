This is a continuation of the interface design discussion for the `git replay` command's operation modes, specifically addressing the naming and argument ordering debate between Elijah Newren and Junio Hamano.

Elijah Newren responds to Junio's proposal about argument ordering by highlighting technical constraints and design principles. He notes that complex revision ranges (like `^main feature1 feature2` or `^$COMMIT --ancestry-path --branches`) would be problematic to parse if they were arguments to a mode flag. The current implementation handles these through Git's standard revision range parsing, which expects ranges to appear after options like `git log` does.

Newren defends the current design where `--onto` and `--advance` take the base as an argument, arguing this maintains consistency with `git log`'s option/range ordering and avoids the usability problems of `git rebase`'s multiple positional arguments. He expresses concern that making both base and range positional would recreate the memorization challenges of `git rebase`'s interface.

The email also reveals an important implementation detail: the command was specifically designed to handle complex revision ranges that `git rebase` cannot process, suggesting the interface needs to preserve this capability. Newren questions whether sacrificing the `git log`-like interface pattern is worth it just to enable different mode names (`--rebase`/`--cherry-pick`), implying the current names may be less confusing when considering the full range of supported operations.

Key technical considerations mentioned:
- Current flag design (`--onto <base>`) versus proposed alternatives
- Revision range parsing complexity with different argument orderings
- Consistency with `git log`'s option/range ordering
- Comparison to `git rebase`'s problematic positional arguments
- Support for advanced range specifications beyond simple commit ranges

The discussion remains focused on interface ergonomics rather than core functionality, with Newren providing concrete examples of how the proposed changes would impact real-world usage patterns and the command's capability to handle complex cases. This represents a deeper technical examination of the naming debate's implications for the command's architecture.