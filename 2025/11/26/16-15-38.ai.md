This is a bug report continuation regarding test failures in `git last-modified` during merges, specifically on the NonStop platform. Randall Becker follows up on Kristoffer Haugsbakk's suggestion to examine Jeff King's analysis about potential tag resolution issues, but finds no problematic casts in the code. He instead suggests a new angle: possible platform-specific issues with enum vs. int bitwise operations (like `BOUNDARY` flag checks) that could behave differently on big-endian systems. The email also notes that the `SANITIZE` build option generates invalid CFLAGS for their compiler. This moves the investigation toward platform-specific behavior in flag handling while maintaining focus on the original test failures where `last-modified` shows commit hashes instead of expected tags. The thread remains centered on determining whether Git 2.52.0 can ship with these known failures on NonStop.