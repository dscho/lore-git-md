This thread began as a documentation effort for the experimental `git-last-modified` command but has evolved into a deeper discussion about its fundamental behavior. The original 3-patch series aimed to document existing features like the `-z` flag and `--max-depth` parameter, but has now uncovered that the command's current non-recursive default behavior was an implementation artifact rather than a deliberate design choice.

Key participants have weighed in: Junio C Hamano and Karthik Nayak provided early feedback on documentation details, while Patrick Steinhardt has been driving the UX design discussion. Author Toon Claes revealed that the non-recursive default stems from historical constraints (the command initially lacked depth controls) and is now proposing to make recursive behavior the default while the command remains experimental.

In the latest development, Patrick has suggested concrete approaches for depth handling - either defaulting to infinite recursion or combining recursive-by-default with max-depth=0 for non-recursive cases. He's also considering whether max-depth=1 might serve as a better non-recursive mode. The discussion highlights GitHub's `git-blame-tree` as a precedent for recursive-by-default behavior.

The thread now faces a key decision point: whether to proceed with documenting the current behavior or to first implement behavior changes while the command is still marked EXPERIMENTAL. The author appears open to redesign but is awaiting broader feedback before proceeding. This discussion will likely shape not just the documentation but the fundamental UX of this emerging command.