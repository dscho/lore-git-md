Here's the updated human-readable thread summary:

This thread documents a 3-patch series adding comprehensive documentation for the experimental `git-last-modified` command's features including NUL-termination, depth control, and traversal behavior. What began as straightforward documentation improvements has evolved into a deeper discussion about the command's design.

The series initially documented existing behavior: patch 1 covered the `-z` flag (simplified from initial implementation per Junio's feedback), patch 2 explained `--max-depth` (with Karthik Nayak improving cross-references), and patch 3 described depth calculation (with Eric Sunshine catching typos). However, Patrick Steinhardt raised fundamental questions about whether the current non-recursive default for nested paths ("sub/file") provides good UX, given the now-available depth controls.

Author Toon Claes responded by revealing the historical context: the command was first implemented without depth controls, explaining the current behavior. With `--max-depth` now available (via separate work), Claes suggests making recursion the default, especially since the command is still marked EXPERIMENTAL. He cites GitHub's internal `git-blame-tree` as precedent for recursive-by-default behavior.

The discussion has shifted from documenting current behavior to reconsidering what that behavior should be. While the documentation patches are technically ready, there's now consensus that the underlying implementation might need changes first. Key participants await Junio's perspective on whether to: 1) proceed with documenting current behavior as-is, or 2) first modify the command to be recursive by default and then document that. The experimental status makes this a good time for such design changes if they're going to happen.