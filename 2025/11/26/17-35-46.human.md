This is a discussion about the design of the `--revert` option for `git replay`, following up on Phillip Wood's critique of the current implementation. Elijah Newren weighs in with additional concerns about the command's usability and conceptual clarity.

Elijah agrees with Phillip that the current `--revert` design seems problematic, noting that it awkwardly combines with existing modes (`--onto` and `--advance`) in ways that may confuse users. He suggests making `--revert` a distinct operation mode rather than an option that modifies other modes, similar to how the sequencer handles rebasing, cherry-picking, and reverting as separate operations.

The discussion reveals deeper questions about the feature's practical utility - while Johannes Schindelin had suggested a bulk-revert approach for Merge Requests, the current implementation focuses on reverting individual commits in sequence. Elijah notes this may have limited use cases, especially since server-side workflows often prefer single atomic reverts.

The email also highlights interface design concerns, pointing out that combining `--advance` (forward motion) with `--revert` (backward motion) creates cognitive dissonance for users. Elijah clarifies the proper way to handle mode incompatibilities, correcting an earlier implementation detail about `--contained` checks.

This exchange moves the thread from specific test failures to broader design reconsideration, with multiple core contributors now questioning whether the current approach aligns with real-world needs and Git's established command patterns. The patch author will likely need to either significantly rework the implementation or better justify the current design's tradeoffs.