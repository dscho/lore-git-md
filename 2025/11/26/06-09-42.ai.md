This is a documentation patch series (3 parts) improving the `git-last-modified` command's manual page and help text. The series focuses on adding missing documentation for two important options (`-z` for NUL termination and `--max-depth` for traversal limits) while also clarifying how depth handling works in general.

The changes are straightforward - each patch adds explanations to both the man page and the command's `-h` output. The first patch documents NUL termination behavior, the second adds the missing `--max-depth` option, and the third provides more detail about depth calculation. The modifications touch both the documentation file and the builtin's source code where the help text is defined.

The series appears well-scoped and clearly motivated - the commit message notes these are "pretty crucial" options that were previously undocumented. The changes are focused purely on documentation improvements without modifying any actual functionality. The patch count seems appropriate with each commit handling a distinct documentation gap. This looks like a solid quality-of-life improvement for users of `git-last-modified`.