This is a refactoring patch that prepares for future work with alternate reference directories. Karthik Nayak extracts the core logic from `get_main_ref_store()` into a new helper function `get_ref_store_for_dir()`, which can create a ref_store for any given directory path rather than just the main repository's $GIT_DIR.

The change is straightforward - it moves the ref_store initialization code into a reusable function while maintaining the existing behavior for the main ref_store case. The commit message indicates this is groundwork for upcoming patches that will need to work with reference directories in arbitrary locations, though those use cases aren't shown here yet.

The patch is cleanly implemented with minimal changes (10 lines added, 3 removed) and maintains the same functionality for the existing `get_main_ref_store()` callers. This appears to be a well-justified preparatory refactoring that follows Git's established patterns for the refs subsystem. The commit message clearly explains both the immediate change and its purpose in the larger context.