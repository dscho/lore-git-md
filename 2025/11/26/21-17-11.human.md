This is a design discussion about the naming of operation modes in the experimental `git replay` command, specifically regarding the `--revert` option being added in this series. Junio Hamano responds to Elijah Newren's observation that the command's modes (`--onto`, `--advance`, and the new `--revert`) parallel the three main sequencer operations (rebase, cherry-pick, and revert).

Junio suggests that the existing `--onto` and `--advance` options might be misnamed - they could more clearly be called `--rebase` and `--cherry-pick` respectively. He notes that if such a renaming is desired, it should happen while `git replay` is still marked as experimental. This appears to be a late-stage design consideration rather than a blocking issue, as the thread has largely converged on the implementation approach.

The email doesn't propose concrete changes but rather raises a philosophical question about command interface design. It's notable that this comes from Junio, suggesting the naming issue may warrant consideration even at this advanced stage of the patch series. The discussion remains focused on improving the command's usability rather than challenging its core functionality.