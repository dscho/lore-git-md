This is a design discussion email in the ongoing `git replay` revert capability thread. Junio Hamano responds to Elijah Newren's critique of the current `--revert` flag implementation, agreeing with the core concerns about operation mode confusion.

The key points center around clarifying that revert should be treated as a distinct operation mode rather than a flag that can combine with other modes like `--onto` or `--advance`. Elijah's analysis frames the three logical operations (rebase, cherry-pick, revert) as fundamentally incompatible modes that shouldn't be mixed, similar to how the sequencer handles them separately.

Junio explicitly endorses this perspective, noting it resolves his own questions about flag interactions and validates Siddharth's earlier observation about `--contained` being specific to `--onto` mode. The email reinforces the growing consensus that the current implementation needs architectural changes to properly separate these operation types rather than trying to combine them through flags.

This moves the discussion toward a potential redesign where `--revert` would become a primary mode incompatible with other operation flags, though no specific implementation path is proposed yet. The exchange helps crystallize one of the major open questions in the thread about whether to fix the current approach or restructure the command's operation model.