This is a follow-up discussion on a performance bugfix in the Scalar configuration series. Junio Hamano notes an interesting implementation choice in the test changes for the `index.skipHash` correction.

The patch being discussed fixes a performance regression by changing Scalar's `index.skipHash` setting from false to true. In the test updates, the author explicitly unsets `GIT_TEST_SPLIT_INDEX` by exporting an empty value rather than using the more common `sane_unset` helper. Junio points out this unusual approach, suggesting the simpler alternative would have been preferred.

The exchange shows careful attention to test implementation details, though the core performance fix itself (correcting the `index.skipHash` value) appears uncontroversial. This is part of a larger series improving Scalar's configuration handling, where earlier discussion established the pattern of adding "# set by scalar" annotations to modified configs.