This is a discussion about whether to fix a regression in `git clone` config parsing behavior regarding whitespace around keys in `-c` arguments.

Junio C Hamano is responding to Jeff King's analysis of the historical behavior and current state of config parsing. The thread has revealed that the original whitespace-trimming behavior was more extensive than initially thought, affecting both `git clone` and environment variable parsing, and that similar behavior was already broken in `git -c` since 2021 without reports.

Junio acknowledges Jeff's point that the regression might not be worth fixing, especially since the original behavior violated Git's config key format rules. He admits he can't muster enough concern to push for a fix, effectively agreeing with Jeff's position that this should be treated as an intentional bugfix rather than a regression needing correction.

The discussion appears to be winding down with consensus forming that the stricter parsing behavior should remain, leaving affected third-party tools to adapt rather than maintaining backward compatibility for what was essentially an accidental behavior. This represents a shift from earlier proposals to fix the issue, with both maintainers now leaning toward accepting the change as an improvement.