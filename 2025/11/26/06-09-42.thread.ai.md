Here is the updated dense AI thread summary incorporating the new email:

This thread documents a 3-patch series for the experimental `git-last-modified` command, evolving from documentation to deeper design discussions. The series covers `-z` NUL-termination (patch 1), `--max-depth` (patch 2), and depth calculation behavior (patch 3).

Implementation history and current state:
- Originally implemented without depth controls (a1dfa5448d)
- Later gained `--max-depth` via separate series
- Current non-recursive default stems from initial limited scope
- Uses diff machinery's depth control internally
- GitHub's `git-blame-tree` implementation defaults to recursive behavior

Patch 1 documented `-z` flag with:
- `line_termination` field simplified to boolean `null_termination`
- Incorporated Junio's and Karthik's feedback
- Patrick's rationale for help text visibility

Patch 2's `--max-depth` documentation now includes:
- Rejection of linking to `diff-options.adoc` (many irrelevant options)
- Technical limitations in diffopt flag handling via `setup_revision()`
- Inherits all diffopts including irrelevant ones like `--no-prefix`
- Shows `--max-depth` implies `--recursive` when positive
- Junio's help text formatting improvements remain

Patch 3's documentation vs implementation tension:
- Currently documents root-level-only default behavior
- Patrick questioned whether this is optimal UX
- Author Toon revealed historical reason (missing `--max-depth`)
- Considering recursive default while still EXPERIMENTAL

Key technical considerations:
- Depth inference from pathspecs may need work
- Implementation changes may be needed for intuitive path handling
- Current behavior reflects incremental development path
- EXPERIMENTAL status allows behavior changes
- Patrick proposes specific depth control combinations:
  - Option 1: Infinite max-depth with recursive default
  - Option 2: Recursive default with max-depth=0 for non-recursive
  - Less certain about max-depth=1 for non-recursive
- Architectural questions about diffopt flag handling surfaced
- `setup_revision()` inheritance may need refactoring for proper option isolation

Open questions and decisions needed:
- Should patch 3 document current behavior or drive redesign?
- Is recursive-by-default the right UX direction?
- Which depth control combination is most intuitive?
- What implementation changes would be required for proper option handling?
- How to handle pathspec interpretation?
- Does this affect series structure (new patches needed)?
- Should diffopt flag inheritance be addressed now or later?

Key participants and positions:
- Toon Claes (author) - open to recursive default, revealed implementation constraints
- Patrick Steinhardt - driving UX design discussion, proposing depth control options
- Junio C Hamano - previously guided NUL termination simplification
- Karthik Nayak - focused on documentation consistency, suggested option linking
- Eric Sunshine - caught typographical issues

The discussion has expanded from behavior design to include architectural questions about how the experimental command interacts with Git's diff machinery, particularly around option handling. While the immediate focus remains on documenting current behavior, the thread suggests potential future refactoring work to better isolate the command's option processing.