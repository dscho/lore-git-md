Refactoring patch for the refs subsystem, part of a 2-patch series. This first patch extracts the ref_store creation logic from `get_main_ref_store()` into a new helper function `get_ref_store_for_dir()`. The new function takes a repository, directory path, and storage format as parameters, making it reusable for creating ref_stores for arbitrary directories rather than just the main repository's $GIT_DIR.

The change is straightforward - it enables future patches (likely the second in this series) to create ref_stores for alternate reference directories. The implementation maintains all existing behavior for the main ref_store case while generalizing the underlying mechanism. The patch is cleanly separated with minimal changes (10 lines added, 3 removed) and preserves the debug wrapper functionality.

Key technical details for future reference:
- Introduces `get_ref_store_for_dir()` with parameters (repository*, char* dir, enum ref_storage_format)
- Keeps same initialization flags (REF_STORE_ALL_CAPS) and debug wrapping behavior
- Affects both files and reftable backends since it operates at the abstract ref_store level
- Follows the pattern of lazy initialization seen in the original code
- No behavior change for existing `get_main_ref_store()` callers

This appears to be infrastructure work supporting some upcoming feature involving alternate reference directories, though the specific use case isn't stated in this patch. The change is well-contained and seems unlikely to be controversial.