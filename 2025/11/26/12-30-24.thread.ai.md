HTTP 429 (rate limiting) retry support for Git's HTTP client. Series introduces three new config options: http.maxRetries (default 0, opt-in), http.retryAfter (fallback delay without Retry-After header), and http.maxRetryTime (300s delay ceiling). Implements RFC-compliant handling of both delay-seconds and HTTP-date Retry-After formats. Core retry logic in Patch 1 with struct slot_results gaining retry_after field, new HTTP_RATE_LIMITED (7) status code in http.h, configuration parsing in http_options(), and retry logic in http_request_reauth(). Extensive test coverage in new t5584-http-429-retry.sh.

Taylor Blau reviewed Patch 1/3, suggesting maintainability improvements: simplify commit message by referencing git-config(1), mark error messages for translation, refactor header parsing into separate functions, make sleep_for_retry() non-blocking to allow concurrent requests, extract rate limit handling from http_request_reauth(), and improve test structure by moving scripts to lib-httpd.sh. Vaidas Pilkauskas responded agreeing to implement all suggested changes in next version while keeping core functionality intact.

Patch 2 fixes pre-existing memory leak in `show_http_message()`. Taylor Blau suggested alternative approach: rename to `show_http_message_fatal`, handle cleanup directly before die(), take die() format string/args as parameters, and release all buffers. Discussion focused on implementation details without challenging core functionality.

Patch 3 adds trace2 instrumentation for monitoring rate limiting: retry attempt counters, Retry-After header values, sleep durations, error conditions, and retry source (header vs config). Uses trace2_data_intmax() and trace2_data_string() with trace2_region_enter/leave around sleep operations. All instrumentation scoped to the_repository.

Implementation provides functional retry support with comprehensive observability while maintaining backward compatibility (opt-in via http.maxRetries=0). Documentation covers all config options. Test coverage remains at 429 lines in t5584-http-429-retry.sh with LeakSanitizer verification. Taylor Blau's reviews suggest maintainability refinements but do not block merging. No outstanding open questions - series components are complete with core functionality (Patch 1), bug fix (Patch 2), and instrumentation (Patch 3). Next version will address review feedback on code organization.