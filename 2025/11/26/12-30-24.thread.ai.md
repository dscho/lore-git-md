HTTP 429 (rate limiting) retry support for Git's HTTP client. Series introduces three new config options: http.maxRetries (default 0, opt-in), http.retryAfter (fallback delay without Retry-After header), and http.maxRetryTime (300s delay ceiling). Implements RFC-compliant handling of both delay-seconds and HTTP-date Retry-After formats. Core retry logic in Patch 1 with struct slot_results gaining retry_after field, new HTTP_RATE_LIMITED (7) status code in http.h, configuration parsing in http_options(), and retry logic in http_request_reauth(). Extensive test coverage in new t5584-http-429-retry.sh verifying retry behavior, header formats, config combinations, and edge cases using mock HTTP server.

Patch 2 fixes pre-existing memory leak in `show_http_message()` that became more visible with retry testing. The function was modifying caller's strbuf in place via `strbuf_reencode()`, causing issues when called before `die()`. Fix introduces local `msgbuf` copy for isolated processing, maintaining same functionality while properly cleaning up. Affects all HTTP error paths (HTTP_MISSING_TARGET, HTTP_NOAUTH etc.), not just new HTTP_RATE_LIMITED case. Change is 10 lines added, 4 removed in remote-curl.c with verification via existing t5584 tests under LeakSanitizer.

Remaining work: Patch 3 will add trace2 instrumentation for monitoring retry operations. Open question remains whether to include 50x status code retries from previous attempt. Backward-compatible opt-in design addresses real-world rate limiting while maintaining current behavior by default. Documentation updates cover new config options. Implementation shows attention to both functionality (core retry logic, memory leak fix) and debuggability (planned trace2 integration). Tests cover major scenarios including invalid headers, environment overrides, and timing validations with 429 lines of test script.