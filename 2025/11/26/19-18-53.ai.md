Feature addition to `git replay` command with discussion of revert approaches. Siddharth Asthana responds to Johannes Schindelin's review of the `--revert` patch, addressing two distinct use cases for reverting commits in GitLab's Gitaly service. The email confirms the need for both commit-by-commit reverts (for auditability and selective undo) and bulk reverts via `merge-tree` (for efficiency in whole-MR rollbacks). 

Key technical points include the plan to document `merge-tree` as an alternative for bulk reverts, and test suite consolidation to reduce redundancy while maintaining coverage of core functionality (basic revert, conflict handling, and reapply behavior). The response acknowledges Schindelin's concern about test suite bloat and CI runtime impacts.

This exchange occurs against the backdrop of broader thread consensus that revert should be implemented as a distinct operation mode rather than a flag combination. Siddharth's response doesn't directly address the architectural concerns raised by other reviewers, focusing instead on the immediate test suite improvements and documenting alternative approaches. The email suggests the next version will be a refinement of the current implementation rather than a structural redesign.