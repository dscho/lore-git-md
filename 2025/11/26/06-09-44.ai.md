Documentation patch for `git-last-modified`'s `--max-depth` option, part 2 of a 3-patch series. This patch adds documentation for the traversal depth control option that was previously implemented but not documented. The changes include:

The patch updates both the man page and command-line help to describe `--max-depth`, which limits directory recursion depth when scanning for modified files. The documentation explains that a negative value means no limit, positive values imply `--recursive`, and the option cannot be used with wildcard pathspecs. The implementation side adds initialization of `max_depth` in the `LAST_MODIFIED_INIT` macro and hooks it to the underlying diff machinery's depth control, which was added in commit a1dfa5448d.

The changes are straightforward and maintain consistency with how depth limits work elsewhere in Git. The patch follows the same pattern as the first in the series by documenting an existing but previously undocumented feature, with corresponding implementation tweaks to ensure the documentation matches reality. Like the rest of the series, this appears uncontroversial as it only surfaces existing functionality rather than changing behavior.