This is a maintainer response in the ongoing thread about signature handling in `git replay`. Junio C Hamano weighs in on Phillip Wood's patch that adds "gpgsig-sha256" to the list of excluded headers when copying commit metadata during replay operations, matching the behavior of `git commit --amend`.

The response is brief but decisive - Hamano indicates that bugfixes should take precedence over new development work, implicitly approving the patch and suggesting it should be applied even though it conflicts with Patrick Steinhardt's ps/history branch that moves this code. This resolves the open question from the previous email about whether to backport the fix or wait for the code reorganization.

The technical context remains unchanged from the thread summary: this is a 1-line change to builtin/replay.c that ensures both GPG signature headers are excluded when replaying commits to maintain signature validity. The broader question of centralizing this exclusion list (currently duplicated in three files) remains open but is not addressed in this response. The key development is the maintainer's clear direction to prioritize this fix over conflicting development work.