Feature patch extending bash completion for Git's short options. This is version 2 of a previously reviewed patch that adds tab completion for common single-letter Git options when typing `git -<TAB>`.

The patch modifies `__git_main()` in `git-completion.bash` to handle `-*` patterns, completing six short options: `-C`, `-P`, `-c`, `-h`, `-p`, and `-v`. The implementation follows Junio Hamano's suggestion from v1 review to make the subject line more specific about completing `git -<TAB>`. The diff shows the change is minimal - replacing a `--*` pattern match with `-*` and adding the short options to the completion list while preserving all existing long option completions.

Key technical details: The completion remains separate from long option completion (which still requires `--<TAB>`), though the thread context notes this might be unified in future work. The patch touches only the bash completion script with no other dependencies. The six supported options cover common needs like configuration (`-c`), help (`-h`), pagination (`-p`), and version (`-v`).

The patch appears well-scoped and low-risk, addressing a clear gap in the completion system. The v2 revision shows responsiveness to review feedback by improving the commit message clarity while maintaining the same technical approach that was already deemed acceptable. No new concerns or objections have been raised in the thread since the initial review.