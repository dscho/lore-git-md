Thread identity: Feature patch introducing `GIT_REF_URI` environment variable to specify reference backends and their storage locations, addressing asymmetry with `GIT_OBJECT_DIRECTORY`. Authored by an unspecified contributor (name not provided in input).

Core problem: Git currently lacks equivalent environment variable support for reference backends compared to object storage (`GIT_OBJECT_DIRECTORY`). This patch adds `GIT_REF_URI` following URI format `<ref_backend>://<URI-for-resource>` to specify both backend type (files or reftable) and storage location.

Current status: Initial version implementing core functionality with comprehensive test coverage. No version iteration or review feedback mentioned yet.

Key technical details:
- URI parsing in `get_ref_store_from_uri()` splits on ":" and validates format
- Backend initialization delayed until `get_main_ref_store()` when hash algorithm is known
- Test suite `t1423-ref-backend.sh` covers error cases (empty URI, invalid format, unknown backend) and cross-backend operations
- Maintains worktree compatibility by applying specified directory to all worktree operations
- Documentation updates explain new variable usage and format

Open questions:
- Potential debate about URI format vs separate variables for backend/path
- No review feedback or maintainer input yet on design choices

Implementation considerations:
- Careful edge case handling
- Maintains existing behavior when variable unset
- Well-structured with thorough test coverage

Related work:
- Builds on existing ref backend work (files/reftable)
- Complements `GIT_OBJECT_DIRECTORY` functionality
- May interact with ongoing ref backend abstraction efforts