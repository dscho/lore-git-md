Bugfix discussion regarding `git clone -c` whitespace handling regression.

Jeff King (peff) responds to Junio Hamano's consideration of restoring whitespace-trimming behavior for `git clone -c` arguments, arguing against preserving what he views as an accidental feature. He notes the behavior was inconsistent (working only in `clone` but not `git -c`) and likely stemmed from buggy caller code that happened to work rather than intentional usage patterns.

Key technical observations:
- The whitespace-trimming was Git-specific and not general option behavior
- Original report appears to be about buggy caller code (incorrect argument splitting) rather than intentional usage
- Similar whitespace-swallowing may exist elsewhere in option parsing (mentions `strtol()` as potential example)

Peff's position is that while a conservative deprecation path is possible, the effort isn't justified given the behavior's accidental nature and limited legitimate use cases. He's open to others implementing a fix but personally favors leaving the regression as a bugfix" rather than restoring compatibility behavior.

This continues the thread's central tension between preserving historical behavior versus cleaning up what participants increasingly view as an accidental feature. Peff's message reinforces the view that the original behavior was more bug than feature, while acknowledging the practical considerations of breaking existing (if likely buggy) usage.