This is a follow-up discussion in the ongoing `git replay --revert` feature thread, where Siddharth Asthana responds to Johannes Schindelin's review. The patch aims to add revert capability to `git replay` for GitLab's Gitaly service.

Siddharth acknowledges Johannes's suggestion to use `merge-tree` for bulk reverts of entire Merge Requests, noting it would be more efficient for that case. However, he explains GitLab also needs commit-by-commit reverts for auditability when only specific commits need reversal. He plans to document both approaches.

In response to test suite feedback, Siddharth agrees to consolidate tests in the next version, focusing on essential coverage of basic revert functionality, conflict handling, and reapply behavior while reducing redundancy. This addresses concerns about test suite bloat while maintaining core verification.

The exchange highlights the patch's evolution toward supporting both bulk and granular revert operations, with Siddharth demonstrating responsiveness to design feedback while maintaining the original commit-by-commit use case that motivated the feature. The technical discussion remains focused on practical implementation tradeoffs rather than revisiting the now-settled architectural questions about operation modes.