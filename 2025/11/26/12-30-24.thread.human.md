Here's a human-readable summary of the current state of these threads:

Vaidas Pilkauskas has been working on two related but distinct improvements to Git's codebase. The first adds proper handling of HTTP 429 "Too Many Requests" responses with Retry-After headers to Git's HTTP client, while the second fixes several instances of strbuf_attach usage to ensure proper NUL termination.

The HTTP 429 implementation is now in its fifth iteration and has received thorough review from Jeff King and Taylor Blau. It includes comprehensive support for rate limit handling with configurable retry behavior, new error codes, and test infrastructure. Junio Hamano has now weighed in, approving the first three patches (which include the strbuf fixes and a helper function) but suggesting the HTTP transport experts should give the final HTTP 429 implementation one more review before merging.

The strbuf_attach fixes address five call sites across the codebase to ensure buffer allocations properly account for NUL termination. These changes maintain the performance benefits of buffer reuse while fixing the allocation sizes. Junio has approved these changes, noting they could potentially be merged independently if the HTTP work needs more time.

The series appears largely complete, with only final review from HTTP transport experts needed before potential inclusion. The conservative defaults (disabling retries by default) align with Git's fail-fast philosophy while giving users the option to enable more robust rate limit handling when needed.