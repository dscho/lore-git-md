This is a follow-up in the performance optimization thread for `git last-modified`, which has uncovered platform-specific test failures on s390x architecture. Kristoffer Haugsbakk asks for more diagnostic information from the failing tests, specifically suggesting running them with `--verbose` to get additional output that might help debug why the new algorithm is incorrectly attributing file modifications across merges on s390x.

The context is that the optimization was already merged to 'next' after thorough review, but now faces a platform-specific correctness regression. The issue appears limited to s390x and affects merge boundary cases not caught in prior testing. The core algorithm (which uses generation-sorted priority queues and EWAH bitmaps) seems sound but may have platform-specific assumptions about merge handling or bitmap processing that need investigation.

This email doesn't contain new technical analysis but rather seeks more debugging information to help diagnose why the behavior differs on s390x compared to other architectures. The thread has active participation from the patch author (Toon Claes) and other Git contributors who previously reviewed the optimization.