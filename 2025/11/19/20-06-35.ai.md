Platform-specific bug investigation for `git last-modified` performance series. This is a follow-up in the ongoing thread about s390x test failures in the recently merged performance optimization series.

Anders Kaseorg provides the concrete test output showing the s390x-specific failures that were previously discussed. The diffs demonstrate two key misattribution cases: in test 16, `m1.t` is incorrectly assigned to the merge commit rather than its actual author `m1`, and in test 19, both test files are wrongly attributed to the merge commit SHA `da1857e0` instead of their expected authors `b5` and `b2`.

The output confirms the earlier bisection pointing to commit 2a04e8c293 ("last-modified: implement faster algorithm") as the regression source. This provides concrete reproduction data for the investigation into whether the issue stems from endianness handling in EWAH bitmaps, merge commit parent ordering assumptions, or other platform-specific factors.

The email doesn't propose solutions but serves to document the exact failure modes, which will help Toon Claes and others analyze whether the core algorithm needs modification or if platform-specific workarounds might suffice. The test output shows the previously theorized symptoms of incorrect attribution across merges, validating the need for further platform-specific investigation.

Key technical details from the test failures:
- Merge commit attribution errors (SHA `da1857e0` appearing where individual authors should)
- Specific test cases 16 and 19 failing consistently
- Original bisection confirmed by concrete output
- No new theories proposed - just providing requested diagnostic data
- Thread remains active with platform-specific investigation ongoing