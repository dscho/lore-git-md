Here's the updated human-readable thread summary:

Randall Becker reported test failures in t8020.16 and t8020.19 on the NonStop platform involving the `git last-modified` command during merge operations. The tests expect to see tag names (m1.t, m2.t) in the output but instead get commit hashes. This appeared in Git 2.52.0's final release after passing in rc0, suggesting a regression.

Kristoffer Haugsbakk linked a potentially related report about tag resolution issues, though it's unclear if it's the same underlying problem. Becker confirms the failures are consistently reproducible on NonStop but suspects this might not be platform-specific. The issue involves how `git last-modified` (which uses `git name-rev --tags` internally) handles tag names across merge boundaries.

So far, the discussion has established the problem exists but hasn't yet determined whether this is specific to NonStop or a more general issue with tag resolution. The connection to another report suggests this might be part of a broader pattern, but no new technical analysis has been shared yet. The next steps would likely involve someone investigating why the behavior differs between platforms and between rc0 and the final 2.52.0 release.