This is a refactoring patch in the submodule gitdir path encoding series, moving URL encoding functionality to a more appropriate location for reuse. The patch relocates `is_rfc3986_unreserved()` from `builtin/credential-store.c` to `url.[ch]` as a public function, reversing a previous move that made it static when it was only used for credentials. The change enables the function to be called by submodule.c in the next patch for URL-encoding submodule names to prevent filesystem conflicts.

The implementation preserves the exact same functionality while changing the function's scope and location. The commit message clearly explains the motivation - the upcoming submodule encoding work needs this helper for percent-encoding submodule names when constructing gitdir paths. The diff shows the straightforward movement of code with minimal changes: removing the static implementation from credential-store.c and adding it to url.c with its header declaration.

This appears to be a well-justified architectural change that properly groups related URL functionality together while enabling reuse. The patch is cleanly separated from the actual submodule encoding implementation that will follow, keeping the series' changes incremental and reviewable. The commit message references both the historical context (f89854362c) and the immediate need in the next patch, providing good traceability.