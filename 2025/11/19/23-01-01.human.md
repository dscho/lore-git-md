This is a follow-up in the `GIT_REF_URI` feature thread, addressing review feedback on the test suite implementation. Karthik Nayak responds to Eric Sunshine's comments about two test suite issues in the second patch of the series.

The first issue concerns using bare `!` for error checking instead of Git's preferred `test_must_fail` helper. Karthik acknowledges this was an oversight from an earlier implementation detail and agrees to fix it.

The second issue points out that the nested test loops for backend formats (`files` and `reftable`) don't actually use their loop variables, instead hardcoding values in the test bodies. Karthik admits this was hasty implementation and promises to correct it, either by properly using the loop variables or making the format combinations explicit in test titles.

These appear to be straightforward test suite polish issues rather than fundamental problems with the feature implementation. The exchange shows the patch is moving through normal review refinement, with the author responsive to feedback about following Git's testing conventions. The core functionality enabling reference storage location control via URIs remains unchanged by this discussion.