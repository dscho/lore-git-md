This is a refactoring patch in Patrick Steinhardt's ongoing series to centralize object database management. While most of the series focuses on ODB handling, this patch takes a brief detour to add a missing utility function for the chdir-notify system.

The patch adds `chdir_notify_unregister()` to complement the existing `chdir_notify_register()` function, allowing cleanup of directory change listeners. The implementation carefully matches listeners by name, callback function, and data pointer, with proper handling of NULL names. The change is straightforward and mechanically sound, though the commit message is quite terse - it only states this will be needed by a subsequent commit without explaining the broader context.

This appears to be preparatory work for later changes in the series, likely related to ODB cleanup during repository teardown. The patch itself is small (20 lines added) and focused, touching only the chdir-notify implementation and header files. While not directly related to the ODB work, it seems to be a necessary building block for the series' goals.