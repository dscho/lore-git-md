Refactoring patch in Patrick Steinhardt's ongoing ODB backend abstraction series. This is patch 8/18, continuing the work to eliminate union-based coupling in object streaming infrastructure by introducing dedicated structures for each stream type.

The patch creates `struct odb_packed_read_stream` for packed object streams, following the same approach used for loose objects in patch 7/18. Key changes:
- Moves packed stream fields (`pack` and `pos`) from the union in `odb_read_stream` into the new structure
- Updates all packed stream functions (`read_istream_pack_non_delta`, `close_istream_pack_non_delta`, `open_istream_pack_non_delta`) to handle the new type
- Maintains existing zlib initialization and error handling paths while changing the allocation pattern
- Uses `CALLOC_ARRAY` for allocation and proper type conversion between base and derived stream types

The changes are purely internal plumbing - no user-visible behavior changes. This continues the series' systematic preparation for pluggable ODB backends by:
- Removing implicit coupling between stream implementations
- Allowing backend structures to be defined separately
- Preserving all existing functionality while changing internal organization
- The diff shows careful attention to error paths and memory management
- Builds directly on the loose object stream changes from previous patch
- Next logical step would be handling delta-packed objects similarly

This continues the high-quality, incremental refactoring approach seen throughout the series, with clear commit messages and thorough mechanical changes. The patch appears ready for review with no obvious issues.