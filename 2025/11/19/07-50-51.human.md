Refactoring patch in the ODB abstraction series. This is patch 3/13 in Patrick Steinhardt's series to reorganize object database management. The change moves the logic for closing an object database from packfile.c to odb.c, renaming the function from `close_object_store()` to `odb_close()`.

The patch notes that while some of the cleanup logic relates to packfiles, other parts (like handling object sources and commit graphs) are more general ODB responsibilities. The change centralizes this functionality in odb.c while maintaining identical behavior. The implementation shows careful attention to detail - all existing callers are updated, and the new function handles the same resources (packfiles, midx, commit graphs) as before.

This appears to be a straightforward mechanical change that makes logical sense as part of the larger ODB abstraction effort. The patch touches several builtin commands (clone, gc, repack) and infrastructure files, but the changes are all simple call site updates. The commit message clearly explains the motivation and the diff shows no behavior changes beyond the relocation.