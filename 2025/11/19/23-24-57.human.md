This is a status update in the ongoing SHA-1/SHA-256 interoperability series, which introduces Git's first major Rust infrastructure. The thread has been investigating Windows/Meson CI failures that appear when the interop series is present in the 'seen' branch.

Junio Hamano reports that an empty CI commit (672cb7c62e) fails on Windows/Meson when the interop series is present, but passes on the base commit (950efaac03). Brian Carlson confirms the failures predate his changes by over a month and suspects the build.rs file may be involved, but lacks Windows systems to investigate. 

The key development is that today's push includes a fix (jk/ci-windows-meson-test-fix) that should make the Windows/Meson failure logs more readable, which may help diagnose the root cause. This remains the final integration hurdle for this major Rust infrastructure change, as the technical implementation is otherwise complete. The thread is awaiting Windows expertise (potentially from Patrick Steinhardt) to help resolve the build issues.