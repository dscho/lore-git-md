This is part 11/18 of Patrick Steinhardt's series refactoring Git's object streaming infrastructure for pluggable ODB backends. The patch focuses on packed object handling.

The change extracts packed object info reading logic from `do_oid_object_info_extended()` into a new standalone function `packfile_store_read_object_info()`. This continues the series' pattern of moving functionality into backend-specific implementations. The new function provides a cleaner interface for reading object metadata from packfiles while hiding the internal `find_pack_entry()` helper (which is now made static).

The patch maintains all existing behavior while restructuring the code to better support future pluggable backends. It touches core packfile handling code but doesn't introduce any user-facing changes. The diff shows careful attention to error handling paths and maintains all existing functionality around freshening objects and handling corrupt packs.

This is a technical refactoring that builds on the previous stream type separation work, continuing the methodical preparation of plumbing for pluggable backends. The changes are well-contained and follow the established pattern of the series.