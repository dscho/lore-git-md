This is part 10 of Patrick Steinhardt's 13-part series refactoring object database (ODB) source management, continuing the effort to centralize ODB initialization and handling.

The patch consolidates ODB initialization logic that was previously split between `initialize_repository()` and `repo_set_gitdir()`. The key change moves source setup into `odb_new()`, ensuring new object databases are fully initialized upon creation. The function now takes parameters for primary and alternate sources, with sensible defaults when not specified.

The changes maintain existing behavior while making the initialization path more straightforward. The commit message clearly explains the motivation - to make ODB setup self-contained and easier to modify in future. This is a logical next step in the series' progression toward proper ODB abstraction.

The implementation touches core repository setup code but appears mechanically sound, with careful handling of path expansion and memory management. As noted in the commit message, one remaining inconsistency (where `repo_set_gitdir()` still reaches into sources) will be addressed in the following patch. The changes align well with the series' systematic approach to ODB management.