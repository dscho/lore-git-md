This is a refactoring patch that adds unregistration capability to Git's chdir-notify system, as part of Patrick Steinhardt's larger ODB source management series (11/13).

The patch introduces `chdir_notify_unregister()` to complement the existing `chdir_notify_register()` function, allowing cleanup of registered directory change callbacks. The implementation carefully matches callbacks by comparing all three registration components (name string, callback function pointer, and user data pointer) before removing entries from the notification list. The change is minimal but complete - adding the new function declaration in chdir-notify.h and its implementation in chdir-notify.c with proper list traversal and cleanup.

While the commit message is brief, it clearly states this is preparatory work needed for a subsequent change in the series. The implementation appears straightforward and mechanically sound, using the same list management patterns seen elsewhere in Git's codebase. The patch maintains the series' focus on proper resource management, though it's not yet clear from this commit alone how the unregister functionality will be used in the ODB refactoring work.