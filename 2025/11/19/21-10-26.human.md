This is a refactoring patch in the submodule gitdir path encoding series, consolidating validation logic for submodule gitdir paths. Adrian Ratiu moves scattered validation checks into the central `submodule_name_to_gitdir()` function, which now always validates paths before returning them.

The change simplifies the API by making validation mandatory rather than leaving it to callers, reducing the risk of missed checks. It removes duplicate validation from several locations including clone operations and submodule moves, while maintaining the same safety guarantees against nested submodule gitdirs. The implementation shows careful attention to parallel operation safety by keeping validation at the unified entry point.

This appears to be a well-considered cleanup that follows the series' architectural direction of centralizing path handling through `submodule_name_to_gitdir()`. The patch maintains all existing functionality while making the codebase more maintainable. As part of the mature v5 series, it builds on established agreement about the core approach to submodule path encoding.