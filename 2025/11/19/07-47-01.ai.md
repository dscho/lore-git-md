This is the first patch in Patrick Steinhardt's major refactoring series to make Git's object streaming infrastructure pluggable. The patch performs a straightforward but wide-ranging rename of the core streaming structure from `git_istream` to `odb_read_stream`, aligning it with the existing `odb_write_stream` naming pattern. 

The change affects 7 files across the codebase, updating all the streaming interface functions and their callers in commands like `archive-tar`, `archive-zip`, `index-pack`, and `pack-objects`. The rename is purely mechanical - no behavior changes are introduced, just preparation for the deeper architectural changes coming in subsequent patches. 

Key technical details for future reference: the patch establishes `odb_read_stream` as the new canonical name for the object streaming interface, with all related function signatures (open/close/read operations) updated to use this type. The change maintains backward compatibility while setting up for the planned separation of streaming logic into backend-specific implementations. The diff shows extensive but straightforward substitutions across the streaming subsystem's core files and its consumers.