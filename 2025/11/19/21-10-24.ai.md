This is the first patch in v5 of the submodule gitdir path encoding series, focusing on a refactoring change in `submodule--helper.c`. The patch standardizes gitdir path handling by replacing hardcoded path construction with the existing `submodule_name_to_gitdir()` helper function, which is central to the series' path encoding mechanism.

The change affects the `add_submodule()` function, which previously used direct string formatting (`.git/modules/%s`) to construct paths. The new version properly routes through the encoding-aware path resolution system, ensuring consistent behavior with the rest of the submodule infrastructure. The patch also cleans up variable scoping by moving the `submod_gitdir_path` declaration into the first branch where it's actually used.

Key technical details:
- Switches from `xstrfmt()` to `submodule_name_to_gitdir()` for path resolution
- Replaces `free()` of char* with `strbuf_release()` for the new STRBUF-based path handling
- Maintains identical error handling and user messaging flows
- Tightens variable scope as a side benefit of the refactoring

This appears to be a straightforward preparatory change that aligns the submodule addition code with the series' architectural approach before introducing the new encoding functionality. The patch is small (13 lines changed) and focused, with no behavior changes beyond adopting the centralized path resolution mechanism. It passes existing tests, though the commit message notes it was discovered through testing the encoding feature.