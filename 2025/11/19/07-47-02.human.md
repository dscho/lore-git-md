This is part 2/18 of Patrick Steinhardt's major refactoring series to move Git's object streaming infrastructure into pluggable backends. The patch simplifies the stream initialization process by eliminating an intermediate `open()` callback step that was creating awkward initialization patterns.

The change removes the `open_istream_fn` callback from the `odb_read_stream` structure, instead having the `istream_source()` function immediately open the appropriate stream type (loose, packed, or in-memory) based on where the object is stored. This makes the initialization flow more straightforward and prepares for future patches that will move stream creation fully into backend-specific code.

The diff shows a net reduction of 6 lines while restructuring the control flow to be more direct. This appears to be a clean, mechanical change that removes an unnecessary layer of indirection in the streaming interface. As part of a larger architectural shift, this patch helps pave the way for more substantial backend-specific streaming implementations while maintaining the same external behavior.