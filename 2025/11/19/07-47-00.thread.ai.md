Patrick Steinhardt's 18-patch series refactoring Git's object streaming infrastructure for pluggable ODB backends continues with patch 14/18, reviewed by Karthik Nayak. The series systematically prepares for pluggable backends by standardizing interfaces and isolating backend-specific logic, with previous changes (1-13/18) restructuring type handling, eliminating global state, relocating code to proper subsystems, and simplifying object stream creation.

Patch 14/18 makes `struct odb_read_stream` public by moving its definition from streaming.c to streaming.h, enabling backend implementations to allocate and initialize streams directly. The 26-line diff (11-/15+) relocates the struct and its function pointer typedefs without functional changes. Karthik Nayak's review identifies an API inconsistency: since the struct already includes object type and size fields, `open_istream()`'s separate output parameters for these values may be redundant, suggesting a future cleanup opportunity.

The series continues its architectural progression:
- Previously (12/18) simplified stream creation to try sources sequentially
- Now exposes core stream structure to support backend implementations
- Maintains all existing behavior while enabling pluggability
- Karthik has provided thorough reviews on patches 2/18, 5/18, 6/18, 11/18, 12/18, and now 14/18
- Total series impact ~1500/+1700 lines across object streaming infrastructure

Open questions include:
- Whether to refactor `open_istream()` to use stream struct fields exclusively
- How delta-packed objects will integrate with the pluggable backend model
- Finalization of the ODB backend interface standardization (with `odb_` prefix)

The Patrick-Karthik review dynamic remains productive, with Karthik consistently catching interface design considerations early in the process. The series continues to methodically prepare the object streaming subsystem for pluggable backends while maintaining existing functionality.