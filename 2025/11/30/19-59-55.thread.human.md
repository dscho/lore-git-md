This thread discusses adding a `-z` flag to `git repo structure` for null-terminated output, maintaining consistency with other Git commands. The implementation reuses existing infrastructure from `git-repo-info` to handle the format parsing.

Junio Hamano initially reviewed the patch, requesting clearer documentation grouping for the `--format`/`-z` options and corrections to the test cases. Patrick Steinhardt and Lucas Seiki Oshiro defended the current approach as consistent with `git repo info` precedent, while Justin Tobler identified some final documentation and test polish needed. The discussion reached consensus on maintaining parity with `git repo info`, with only minor documentation formatting to address.

Junio's latest email provides the needed clarification about Git's standard behavior (last option wins) and the proper way to document mutually exclusive options, referencing established conventions. This appears to be the final piece needed before the patch can be finalized, as the technical implementation has already been agreed upon. The changes are limited to option handling and tests, building on recently merged infrastructure from the `lo/repo-info-all` branch.