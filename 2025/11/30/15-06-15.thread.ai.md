UI improvement for `git add -p` hunk selection feedback. Patch adds visual state indicators (`[selected]` or `[deselected]`) to hunk prompts when navigating with K/J keys, addressing Junio C Hamano's request for better feedback. Implementation in `add-patch.c` checks `hunk->use` flag (USE_HUNK/SKIP_HUNK) to determine state, modifying only hunk-level prompts (PROMPT_HUNK) while preserving other prompt types. Changes involve: 1) Adding format placeholder to hunk prompt string 2) Conditional logic in `patch_update_file()` to inject state 3) Maintaining existing non-hunk prompt behavior. Focused change to interactive staging interface.