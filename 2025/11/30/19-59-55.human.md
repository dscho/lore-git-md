Feature patch adding `-z` as a null-terminated output option for `git repo structure`.

Lucas Seiki Oshiro proposes adding the `-z` flag as an alias for `--format=nul` in `git repo structure`, bringing it in line with similar options in other Git commands like `git-config` and `git-status`. The change is straightforward, copying the existing `-z` implementation from `git repo info` since both commands share the same format parser.

The patch updates documentation, adds the option to the command-line parser, and includes a test verifying the new behavior matches the existing `--format=nul` output. The implementation looks clean and follows established patterns, with proper test coverage. This appears to be a minor but useful improvement for scriptability, making the command's interface more consistent with other Git plumbing commands.