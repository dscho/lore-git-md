Performance optimization follow-up for `diff-index` with `--find-copies-harder`. Junio Hamano acknowledges René Scharfe's patch that improves performance by avoiding redundant work on unchanged file pairs, noting the 1.33x speedup demonstrated in the Linux repository benchmarks.

Hamano asks whether the same optimization could apply to `diff-tree` (likely yes) and `diff-files` (possibly not, due to working tree blob handling differences). This suggests the optimization might be extendable to other diff commands, though the current patch focuses specifically on `diff-index`.

The patch is accepted for queuing, indicating it meets Git's quality standards. The discussion remains technical and constructive, focused on potential broader applications of the optimization technique rather than any issues with the current implementation.