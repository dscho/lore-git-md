This thread introduces a UI improvement for `git add -p`'s interactive hunk selection. The patch adds visual feedback showing whether each hunk is currently selected or deselected when navigating with K/J keys, addressing a usability issue noted by Junio Hamano.

The change modifies the hunk prompt in `add-patch.c` to display `[selected]` or `[deselected]` based on the `hunk->use` flag, while leaving other prompt types unchanged. The implementation is focused and maintains existing behavior for non-hunk operations. This should make the interactive staging workflow more intuitive by providing clear state feedback during navigation.

The patch appears ready for merging, with Junio's prior request serving as implicit approval. No outstanding concerns or requests for changes have been raised in the discussion.