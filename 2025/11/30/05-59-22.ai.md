This is a test fix in the ongoing Windows symlink compatibility series, specifically addressing patch 4/10 which handles `git diff --no-index` behavior in t0001-init.sh.

Junio Hamano responds to Johannes Schindelin's patch that skips symlink comparison when `GIT_TEST_CMP` uses `--no-index` on Windows. While agreeing the test needs fixing, Junio questions whether this is truly a test bug versus a `test_cmp` implementation issue. He suggests an alternative approach that would use `cmp` directly when `--no-index` is detected, rather than skipping the comparison entirely.

The discussion highlights a subtle interaction between Windows path handling and Git's test infrastructure. The core issue stems from `git diff --no-index` not resolving symlinks (showing targets instead) when the test expects direct file content comparison. Junio's proposed solution would maintain test coverage while working around the Windows-specific behavior, though it introduces platform-specific logic into the test script.

This exchange shows the careful consideration needed when modifying test behavior for platform compatibility - the fix must balance correctness, coverage, and maintainability while preserving the original test intent. The thread context indicates this is part of a broader effort to prepare Git's test suite for MSYS2's planned symlink support enablement.