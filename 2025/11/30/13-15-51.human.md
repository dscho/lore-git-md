This is a refactoring patch in Jeff King's ASan findings series, converting cache-tree integer parsing to use the new `parse_int_from_buf()` utility introduced earlier in the series. The patch replaces a hand-rolled integer parser in cache-tree.c with a call to the more robust buffer-safe parsing function. The original parser didn't properly handle overflow cases, matching the behavior of the strtol() it replaced, while the new version inherits proper bounds checking from the shared utility.

The change is straightforward - the 23-line custom parser is replaced with a 5-line wrapper around `parse_int_from_buf()`, maintaining the same interface for callers. The wrapper still handles pointer/length management specific to the cache-tree format. This continues the series' theme of centralizing parsing logic while fixing memory safety issues found by ASan.

The patch appears well-contained and low-risk, building on infrastructure already reviewed in the series. It demonstrates the new parsing utilities in action while simplifying the cache-tree code. Since this is part of an approved series, it's likely to proceed smoothly.