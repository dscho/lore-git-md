Thread summary:

Feature patch adding `-z` flag to `git repo structure` command for null-terminated output consistency with other Git commands. The change introduces `-z` as an alias for `--format=nul`, matching behavior seen in `git-config` and `git-status`. Implementation copies existing `OPT_CALLBACK_F` pattern from `git-repo-info` since both commands share the same format parser. Changes include: `builtin/repo.c` modification for option handling, documentation update in git-repo.adoc, and new test case in t1901-repo-structure.sh. The patch builds on recent work (master + lo/repo-info-all) and maintains consistency with Git's established conventions for null-terminated output. No open questions or objections noted in this initial submission.