Junio C Hamano confirms the discussion about the `git clone -c` whitespace parsing regression is effectively closed. He clarifies that Git LFS is not actually affected (contrary to earlier concerns) and dismisses the need to accommodate Atlassian Bamboo's non-compliant usage. The thread concludes with agreement to treat the behavior change as an intentional bugfix rather than a regression, letting affected tools adapt to Git's stricter config parsing rules.

This marks the end of the discussion, with no further action planned from the Git project side. The change stands as a correction of what was effectively broken behavior, despite its historical usage. Junio humorously frames this as "writing it off as an inadvertent bugfix," reinforcing the maintainers' position that tools should follow Git's documented config syntax rather than relying on legacy quirks.