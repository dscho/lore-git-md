Here's a human-readable summary of the thread so far:

The thread discusses a proposal to introduce an optional versioning system to Git that would exist alongside tags and branches. The author suggests this could provide a more robust way to track version numbers that persists through operations like rebase and cherry-pick, while remaining completely optional to maintain backward compatibility.

The proposal outlines three possible technical approaches for implementation (commit headers, special refs, or new object types) and sketches some new commands for version management. Potential benefits mentioned include better automation support, reproducible builds, and reducing the need for custom versioning solutions. The RFC specifically seeks community feedback on whether there's interest in such a feature before moving forward with implementation.

While the proposal is well-structured and carefully distinguishes itself from existing Git features, it currently lacks concrete examples of problems with current versioning approaches that this would solve. There are also open questions about how this would interact with Git's history-sensitive operations like bisect and blame, as well as potential implications for Git's object model and interoperability with other tools. The author acknowledges the philosophical questions around introducing mutable metadata into Git's typically immutable data model.