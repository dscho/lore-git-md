Final polish version of the `git blame` diff algorithm configurability series, addressing the last two minor style issues from v5. This is a feature patch series that has already received thorough review and approval, now in its sixth iteration with all technical aspects resolved.

The changes in v6 are purely cosmetic:
1. Restores the "clear out previous settings" comments that were removed in v5
2. Fixes whitespace around redirection operators in the test script as noted by Junio

The series remains split into two patches:
1. Xdiff refactoring to properly include XDF_NEED_MINIMAL in XDF_DIFF_ALGORITHM_MASK
2. User-facing `git blame` changes adding `--diff-algorithm` option and config support

Key technical aspects already addressed in previous versions:
- Comprehensive test coverage in t8015-blame-diff-algorithm.sh
- Proper handling of algorithm selection precedence (CLI over config)
- OPT_HIDDEN for legacy `--minimal` flag
- Cleaned up flag handling in xdiff interface
- Fixed missed cases in merge-ort.c and builtin/merge-file.c

The series has received final approval from both Phillip Wood (confirming all technical requirements met) and Junio (noting only these trivial style fixes remained). With these last changes, the series is fully ready for merging into 'next'.