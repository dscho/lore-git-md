This is a documentation refinement in the ongoing xdiff refactoring series for Rust FFI compatibility, specifically addressing the technical description of `char` type behavior in the type mapping document.

Junio Hamano proposes rewording the problematic paragraph about `char` signedness in `unambiguous-types.adoc`, moving away from the non-standard 'signless' terminology that Ramsay Jones objected to. The suggested revision focuses on two key points: the C standard's definition of `char` as implementation-defined (per section 6.2.5), and the practical impact of `-Wsign-compare` warnings in Git's developer builds. The new wording avoids making claims about `char` being neither signed nor unsigned, instead framing the issue as one of compiler warning behavior rather than inherent type properties.

This follows the thread's earlier technical discussion where Ben Knoble noted the `-Wsign-compare` warnings occur regardless of whether `char` is compared with signed or unsigned integers, and Ramsay Jones emphasized the C standard's precise definition. The proposed text aligns with the maintainer consensus that documentation should accurately reflect the language standard while acknowledging the practical warning behavior developers encounter.

The change represents the final resolution of one of the last contentious points in the xdiff type modernization series, specifically around how to document `char` type handling in the context of cross-language compatibility. It preserves the technical accuracy demanded by the reviewers while maintaining the practical orientation of the original documentation. The series has now addressed all major feedback points across its 10 parts, with this being a minor but important clarification to the type mapping guidelines.