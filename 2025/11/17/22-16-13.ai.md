This is part of the ongoing SHA-1/SHA-256 interoperability work introducing Rust infrastructure (patch 7/15 in the v2 series). The patch focuses on enhancing the `ObjectID` struct with better safety and usability features.

The key additions are:
1. A new constructor `ObjectID::new()` that properly validates hash lengths and zero-pads storage
2. An accessor method `algo()` to safely retrieve the hash algorithm
3. Proper `Debug` and `Display` trait implementations for formatted output
4. Comprehensive test coverage for the new functionality

The changes enforce stricter invariants around `ObjectID` usage, preventing invalid algorithm values and malformed hashes from propagating through the system. The new formatting capabilities will be particularly useful for debugging and logging. The tests verify correct behavior for both SHA-1 and SHA-256 cases, including special cases like null OIDs and empty tree/blob hashes.

This builds on the series' foundation of type-safe Rust abstractions while addressing practical needs for object ID manipulation. The patch appears well-structured with clear documentation and thorough test coverage, though reviewers may want to verify the panic behavior in the constructor matches Git's error handling conventions.