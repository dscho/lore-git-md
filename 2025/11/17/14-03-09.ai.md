This is a follow-up clarification in the Git for Windows GUI credential helpers porting thread. Johannes Schindelin responds to Johannes Sixt's earlier question about the use of `gitexec` versus `file join` syntax in the patch that sets GIT_ASKPASS to `git-gui--askpass` when unset.

The email provides historical context tracing the code's evolution from msysGit (2010) through Git for Windows (2025), explaining how the original `gitexec` usage became inconsistent with surrounding `file join` calls after context changes. Schindelin confirms this was an unintentional artifact of the code's history rather than a deliberate design choice, noting that Johannes Sixt had already corrected it during integration.

Key technical points:
- The inconsistency stemmed from the code's migration path from msysGit to Git for Windows
- The final version in git.git (commit aeaabebc1a10) correctly uses `file join` like surrounding code
- No functional impact since the corrected version is already merged

This resolves one of the open questions from the thread summary regarding path resolution style, confirming the `file join` approach is correct. The exchange demonstrates the careful attention to detail in maintaining cross-platform compatibility for the GUI helpers, particularly around path handling conventions. The remaining action items from the thread summary (commit message terminology, Tk implementation specifics) are unaffected by this clarification.