This is the first patch in a 3 series introducing signature stripping for `git fast-import`. It's a refactoring change that extracts commit buffer finalization logic into a new `finalize_commit_buffer()` function, preparing for signature verification and optional stripping in subsequent patches.

The patch moves the commit buffer assembly code (adding GPG signatures and commit message) from `parse_new_commit()` into a new helper function. This includes handling both SHA-1 and SHA-256 signature formats. The refactoring enables reuse of this logic when the series later adds signature verification capabilities.

The change is mechanical and well-contained, touching only `builtin/fast-import.c`. It maintains the existing behavior while making the code more modular. The commit message clearly states the motivation - enabling signature handling in future commits without code duplication.

This foundational change is part of a larger effort to add `--strip-if-invalid` functionality to `git fast-import`, motivated by `git filter-repo` use cases where users want to rewrite history while preserving valid signatures. The thread context shows this series has already undergone significant review, with maintainer concerns addressed in v2.