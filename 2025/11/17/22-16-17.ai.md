Rust build infrastructure patch in the SHA-1/SHA-256 interoperability series. This is patch 11/15 in v2, adding a `build.rs` script to handle linking requirements for test binaries in Git's Rust code.

The patch introduces a Cargo build script (`build.rs`) that specifies linking against `libgit.a` and `libz` for test binaries. This prepares for future test functionality that will need to call into core Git code. The Makefile is updated to ensure the Rust library depends on `libgit.a`, preventing race conditions during parallel builds.

Key technical details:
- New `build.rs` script sets up link paths and library dependencies (`libgit` and `libz`)
- Makefile modified to make `$(RUST_LIB)` depend on `$(LIB_FILE)` (the Git library)
- Maintains GPLv2 licensing header in new file
- Part of broader Rust infrastructure work for hash algorithm interoperability

This is a straightforward build system change that enables future test integration with Git's C code. The patch appears well-contained and properly motivated as preparation for upcoming test functionality that will need the Git library. It follows established patterns for Rust-C interop while maintaining the project's build system conventions.