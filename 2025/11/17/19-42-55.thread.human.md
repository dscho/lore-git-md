This thread began with a refactoring that merged `xmkstemp()` into `xmkstemp_mode()` to simplify tempfile creation code, eliminating duplicate logic while maintaining the same behavior. Jeff King provided historical context showing the original complexity was no longer necessary given Git's current tempfile handling guarantees, though he noted one remaining direct `mkstemp()` call in `entry.c` as potential future work.

René Scharfe has now followed up with a patch that completes this cleanup by removing the last uses of system `mkstemp(3)` entirely. His change replaces the Windows-specific implementation with a macro that routes all `mkstemp()` calls through Git's internal `git_mkstemp_mode()` at 0600 permissions. This ensures consistent behavior across platforms while eliminating compatibility code, addressing the remaining case in `entry.c` that was previously noted. The change reflects the consensus that Git's tempfile handling is now robust enough to make the system call redundant, building on the earlier work to provide a unified interface.