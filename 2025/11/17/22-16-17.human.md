This is a build system patch in the ongoing Rust integration series, specifically adding infrastructure for test binaries. Patch 11/15 in the v2 series introduces a `build.rs` script to handle linking requirements for Rust test binaries that will need to use Git's C library (`libgit.a`).

The change does two main things: First, it adds a new `build.rs` file that tells Cargo how to link the test binaries against `libgit.a` and zlib. Second, it updates the Makefile to ensure the Rust library build depends on `libgit.a` being available first, preventing race conditions during parallel builds.

The patch is straightforward and well-motivated - it's preparing for future test functionality that will need to call into Git's C code. The implementation looks correct, with proper license headers and clean build system integration. This is a typical infrastructure piece needed when mixing Rust and C codebases, and it fits logically in the series' progression toward Rust/C interoperability.