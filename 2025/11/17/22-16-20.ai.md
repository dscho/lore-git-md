This is part of the SHA-1/SHA-256 interoperability work introducing Rust code to Git's core. The patch adds a Rust wrapper around Git's existing hashfile functionality, enabling the new binary object map code to write to files while maintaining the same checksumming behavior as the C implementation.

The patch introduces `src/csum_file.rs` with a `HashFile` struct that implements Rust's `std::io::Write` trait, bridging between Rust's I/O model and Git's C hashfile API. The wrapper handles chunked writing (splitting large writes to avoid u32 overflow) and provides a type-safe interface around the unsafe C calls. The implementation includes methods for creating hashfiles, writing data, flushing, and finalizing with a checksum.

Key technical points:
- Wraps C functions `hashfd`, `hashwrite`, `hashflush`, and `finalize_hashfile`
- Maintains compatibility with Git's existing checksumming behavior
- Integrates with the series' existing `HashAlgorithm` type
- Handles chunked writes automatically to prevent u32 overflow
- Preserves the component and fsync flags from the C implementation
- Adds the new module to both Makefile and meson build systems

This is a straightforward FFI wrapper that enables the object map code (from patch 13/15) to write to real files while maintaining testability through Rust's Write trait. The implementation appears solid, with proper error handling and memory safety considerations. It builds on the series' existing hash infrastructure while keeping the unsafe C interactions contained.