This is a follow-up discussion in the ongoing thread about adding a `--committer` option to `git commit`. The conversation has shifted from technical implementation to practical workflow considerations.

ZheNing Hu responds to Matej Dujava's suggestion about using `includeIf` patterns in Git config to manage multiple identities. While acknowledging this as a good practice, Hu notes that many users may not be aware of it. The discussion then focuses on correcting misconfigured identities in commits.

Matej demonstrates that `git commit --amend --reset-author` can update both committer and author information when the config is fixed, though it resets dates. For rebasing multiple commits, he suggests using `git rebase -x` with `git commit --amend --no-edit --reset-author` as a workaround.

The exchange shows the practical challenges of identity management in Git, with Hu maintaining that a dedicated `--committer` option would provide a more user-friendly solution than current workarounds. However, the thread appears to be winding down with no new technical arguments being introduced, just refinements of existing workflow examples. The fundamental philosophical objection from maintainers about maintaining author/committer asymmetry remains unaddressed in this exchange.