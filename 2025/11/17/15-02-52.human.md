This is the second patch in a 2-part series adding the `--all` flag to the experimental `git-repo-info` command. The series has gone through several rounds of review, with this version incorporating all feedback from previous iterations.

The patch adds a `--all` flag that outputs all available repository metadata keys and values, eliminating the need for users to manually specify which keys they want. The implementation includes:
- A new `print_all_fields()` function that iterates through all defined fields
- Documentation updates explaining the new flag
- Tests verifying the `--all` behavior and its interaction with other options

The changes are cleanly separated from the first patch's refactoring work, maintaining good commit discipline. Test coverage appears thorough, checking both successful usage and error cases (like invalid combinations with key arguments). The implementation follows the optimization patterns established in the first patch, using direct output and avoiding unnecessary allocations.

This appears to be the final version of the feature, having addressed all technical feedback from mentors and reviewers. The patch series now seems ready for potential merging as an experimental feature.