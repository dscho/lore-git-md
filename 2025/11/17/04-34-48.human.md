This is the first patch in a 3-part series introducing signature validation and optional stripping in `git fast-import`. It's a refactoring change that prepares for the new functionality by extracting commit buffer finalization logic into a dedicated `finalize_commit_buffer()` function.

The patch cleanly moves the commit message and signature assembly code from `parse_new_commit()` into a new helper function. This creates a clear separation of concerns and will allow the subsequent patches to more easily modify the signature handling behavior. The change is straightforward - it just moves code without altering functionality, maintaining the same buffer construction logic that adds both SHA-1 and SHA-256 signatures.

As part of a larger effort to add signature validation capabilities to fast-import, this foundational change appears well-executed. The commit message clearly explains the refactoring's purpose in service of the upcoming signature handling features. The diff shows careful attention to preserving existing behavior while making the code more modular.

This patch doesn't introduce any user-facing changes on its own, but sets up the infrastructure needed for the signature validation features coming in patches 2 and 3 of the series. The clean separation of concerns suggests good architectural thinking about how to implement the new functionality.