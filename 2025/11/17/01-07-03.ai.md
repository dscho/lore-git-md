This is a documentation patch improving cross-references in the `git-commit` manpage, now being marked for inclusion in the 'next' branch by Junio C Hamano. The patch standardizes references to `git-status(1)` for various format options in `git commit --dry-run` output, addressing inconsistencies in the documentation.

The changes include: adding a `git-status(1)` reference for `--branch` to match existing `--short` documentation, explicitly stating that `--long` format matches `git-status` default output, and replacing a bare "status" mention with a proper `linkgit` reference for `--null`. The patch maintains consistency with the shared internals between `commit --dry-run` and `status` commands.

Key technical details for future reference: the changes affect `Documentation/git-commit.adoc`, modifying documentation for `--branch`, `--long`, and `--null` options. The patch cites historical inconsistencies between commits 7c9f7038 (which included proper references) and f3f47a1e (which did not) as motivation for these changes. The changes are purely documentation improvements with no behavioral impact.