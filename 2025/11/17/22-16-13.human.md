This is part of the ongoing SHA-1/SHA-256 interoperability work introducing Rust infrastructure to Git. Patch 7/15 in the series focuses on improving the `ObjectID` struct's safety and usability in Rust.

The patch adds several key features to the `ObjectID` struct that represents Git object hashes:
- A proper constructor (`new()`) that validates hash lengths and properly initializes storage
- An accessor method (`algo()`) to safely retrieve the hash algorithm
- `Display` and `Debug` trait implementations for proper string representation
- Comprehensive tests verifying all functionality

The changes enforce proper invariants (like correct hash lengths) that were previously possible to violate by directly accessing struct fields. The new `Debug` implementation is particularly useful as it includes both the hash and algorithm name in a format that matches Git's conventions.

This appears to be a well-constructed patch with thorough test coverage. It builds on previous work in the series (the `HashAlgorithm` enum and FFI foundations) while providing necessary infrastructure for future patches that will need to work with object IDs. The commit message clearly explains the motivation and the changes are focused and cleanly implemented.