Build system patch adding Scalar to the `make strip` target.

This is a straightforward Makefile fix that adds the `scalar` binary to the `strip` target, which was overlooked when Scalar was integrated into Git's main build system in 2022. The change aligns with the original integration commit 7b5c93c6c68 and appears to be motivated by Windows/MSYS2 compatibility work. The patch is minimal (a single line change) and correctly follows the existing pattern for other binaries in the Makefile.

For future reference: The modification affects the build system's stripping behavior for the `scalar` binary, adding it alongside other Git programs in the `strip` target. The change is in `Makefile` line 2565, adding `scalar$X` to the dependencies. No behavioral changes to Git itself - this is purely a build system adjustment. The patch seems uncontroversial and well-justified by the commit message.