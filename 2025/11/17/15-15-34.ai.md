This is ZheNing Hu's response in the ongoing discussion about adding a `--committer` option to `git commit`, now focusing on practical workflow solutions for identity management. The email acknowledges Matej Dujava's configuration approach using `includeIf` patterns as a best practice, but notes that many users may not be aware of or adopt this method. 

Key technical points raised:
- Confirms that `git commit --amend --reset-author` can correct both committer and author identities in one step (though it resets dates)
- Validates Matej's rebase workflow suggestion using `git rebase -x 'git commit --amend --no-edit --reset-author'` as an effective alternative
- Notes that environment variables don't work for author identity during rebase due to rebase's own variable handling

The discussion has shifted from technical implementation to practical solutions, with ZheNing now framing the original proposal as primarily addressing user experience issues when correcting misconfigured identities rather than enabling multiple-identity workflows. The thread appears to be converging on existing solutions (like `includeIf` and `--reset-author`) rather than introducing new flags, though the fundamental disagreement about author/committer symmetry remains unresolved.