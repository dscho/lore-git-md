This is a follow-up in the ongoing thread about `git submodule` behavior with `--single-branch` and branch specifications in `.gitmodules`. Uwe Kleine-König responds to D. Ben Knoble's explanation that the current behavior is documented, while arguing the documentation and behavior should be improved.

The key new points are:
1. Uwe clarifies that the commit being checked out (b33a8abe) is not actually part of the remote's HEAD branch, showing the current behavior works correctly for checkout but misconfigures tracking
2. He argues that since `.gitmodules` specifies a branch, this should implicitly serve as the `--branch` parameter for `--single-branch` operations
3. He notes that while the behavior matches documentation, both should be changed to be more intuitive for submodule workflows
4. The issue specifically manifests in the remote tracking configuration written to `.git/modules/yocto_poky/config`

This reinforces the earlier discussion about whether to modify `--single-branch` behavior for submodules to respect `.gitmodules` branch by default. Uwe's technical details about the actual commit not being in HEAD provide additional justification for why the current behavior, while documented, may be surprising and worth changing. The response keeps the focus on submodule-specific behavior rather than general `--single-branch` semantics.