This is patch 16/19 in Patrick Steinhardt's object database streaming interface refactoring series, focusing on moving packed object stream handling into the packfile backend. The patch continues the series' pattern of relocating implementation details to their logical subsystems while maintaining the same external behavior.

The key technical changes move the packed object streaming logic from streaming.c to packfile.c, introducing a new public interface `packfile_store_read_object_stream()`. The implementation remains largely unchanged but is now properly colocated with other packfile handling code. The patch:

1. Moves the `odb_packed_read_stream` struct and its associated read/close functions to packfile.c
2. Exposes the stream creation via new packfile.h declaration
3. Updates streaming.c to use the new backend interface
4. Maintains all existing functionality including zlib stream state handling and error conditions
5. Preserves the non-delta object limitation (only handling commits, trees, blobs and tags)

The changes touch 3 files with 134 insertions and 135 deletions, showing this is primarily a code relocation rather than functional modification. This follows the series' incremental approach where each patch moves specific streaming responsibilities to their natural subsystems while keeping the high-level interface stable.

The patch appears well-contained and mechanically sound, continuing the established pattern from previous patches in the series. It sets up for the final parameter cleanup in patch 19/19 while completing another step in isolating streaming implementation details to their respective backends.