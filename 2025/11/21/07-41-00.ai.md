This is patch 15/19 in Patrick Steinhardt's ongoing refactoring of Git's object database streaming interface, continuing the work to move streaming logic into backend-specific implementations.

The patch relocates loose object stream reading functionality from the generic `streaming.c` into `object-file.c`, making several previously public functions private in the process. Key changes include:

- Moves the entire `odb_loose_read_stream` implementation into `object-file.c`
- Makes `odb_source_loose_map_object()`, `unpack_loose_header()`, and `parse_loose_header()` static since they're now only used internally
- Adds a new public interface `odb_source_loose_read_object_stream()` for creating loose object streams
- Updates the callers in `streaming.c` to use the new backend-specific function
- Preserves all existing functionality while better encapsulating the loose object handling

The changes follow the series' pattern of incrementally moving responsibility into backend implementations while maintaining the same external behavior. This prepares for future work where backends may implement their own streaming variants. The patch touches 3 files with 164 insertions and 178 deletions, showing a net reduction in code while improving organization.

This continues the systematic refactoring approach seen throughout the series, with each patch building on the last to gradually shift control to backend implementations. The changes are mechanical and well-contained, focusing solely on loose object streaming without introducing new concepts or behaviors.