This is the final patch (19/19) in Patrick Steinhardt's object database streaming interface refactoring series, completing the cleanup of redundant parameters from the streaming API. The patch removes the now-unnecessary `type` and `size` out-parameters from `odb_read_object_stream()`, since these values are already available in the public `struct odb_read_stream` that was exposed in earlier patches.

The change touches 7 files, updating callers to access the type and size information directly from the stream structure rather than through the out-parameters. The modifications are straightforward mechanical updates, with callers now reading `st->type` and `st->size` where they previously used the out-parameters. The patch maintains all existing functionality while simplifying the interface.

This completes the series' technical goals by:
1. Removing the last redundant interface elements now that the stream structure is public
2. Further simplifying the streaming API surface
3. Making the data flow more explicit (type/size come from the stream structure rather than being passed separately)

The changes are consistent with the series' direction and address the interface redundancy that Karthik Nayak had previously noted. With this patch, the streaming interface refactoring appears complete, having achieved its goals of better encapsulation, explicit repository dependencies, and cleaner code organization.