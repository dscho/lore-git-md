Refactoring patch in Patrick Steinhardt's object database streaming interface series (12/19). This change reorganizes how object streams are created to be more efficient and better support pluggable backends.

The patch shifts from a two-step process (first finding object info, then creating stream) to having object sources directly create streams. This eliminates redundant iteration through object sources and moves toward a cleaner interface where the streaming subsystem doesn't need backend-specific knowledge. The change particularly benefits loose objects by avoiding double iteration through sources.

While the packfile store still operates at the database level (not per-source yet), this is groundwork for future changes. The implementation maintains all existing behavior while simplifying the control flow. The diff shows significant restructuring of the streaming logic, with 41 lines removed and 24 added, primarily in `streaming.c`.

This continues the series' pattern of incremental improvements to the ODB streaming interface, with each patch building toward the goal of better isolation and pluggability. The change appears well-contained and consistent with the architectural direction established in earlier patches.