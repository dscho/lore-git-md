This is part 11/19 of Patrick Steinhardt's ongoing refactoring series to improve Git's object database streaming interface. The patch focuses on packed object handling.

The patch extracts packed object info reading logic from `do_oid_object_info_extended()` into a new standalone function `packfile_store_read_object_info()`. This continues the series' pattern of moving functionality into backend-specific operations. Key changes include:

1. Makes `find_pack_entry()` internal-only by moving it from packfile.h to packfile.c
2. Introduces new public API `packfile_store_read_object_info()` that handles all packed object info reading
3. Reorganizes code to maintain proper function ordering dependencies
4. Preserves all existing behavior while cleaning up the interface

The change affects three files with 69 lines added and 43 removed. The new function will be used in subsequent commits, continuing the series' progression toward properly isolated streaming backends. This is a well-contained change that fits cleanly into the architectural direction established by earlier patches in the series.

The patch maintains the series' high quality standards with thorough implementation and clear commit message. It builds on previous work to further detangle packed object handling while keeping external behavior identical.