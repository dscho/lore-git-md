This is patch 15/19 in Patrick Steinhardt's ongoing refactoring of Git's object database streaming interface. The series is systematically moving streaming logic into backend-specific implementations as part of making the ODB subsystem more modular.

The current patch relocates the loose object streaming implementation from the generic `streaming.c` into `object-file.c`, which handles loose object operations. This involves:

1. Moving the `odb_loose_read_stream` struct and its associated read/close functions
2. Making several previously public loose object parsing functions static since they're now only used internally
3. Adding a new `odb_source_loose_read_object_stream()` function as the public interface for loose object streaming

The changes are mechanical - no behavior is introduced or modified. The patch follows the series' pattern of incrementally moving functionality into backend-specific locations while maintaining the same external interfaces. This continues the effort to make the streaming subsystem more modular and properly encapsulate implementation details.

The diff shows 164 lines added and 178 removed across 3 files, with most changes being straightforward code movement. This is another step toward the series' goal of having each ODB backend fully manage its streaming implementation.