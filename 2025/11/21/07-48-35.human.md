This is a bugfix patch in Patrick Steinhardt's ongoing series refactoring object database management. The patch addresses a specific issue with `index-pack --fsck-objects` when run outside a repository context.

The discussion confirms that the solution (using a new `fsck_has_queued_checks()` helper) provides best-effort validation for packfiles. It works reliably for Git-generated packs (which put trees before blobs) but may fail gracefully for non-Git implementations that might structure packs differently. Junio Hamano agrees this is an acceptable tradeoff until more comprehensive validation can be implemented with future ODB improvements.

The patch appears technically sound, with both author and maintainer aligned on the approach. It's part of a well-reviewed series that's nearing completion, with only minor documentation tweaks remaining in other patches. The fix prevents crashes while maintaining reasonable validation coverage for the common case.