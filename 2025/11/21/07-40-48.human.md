This is a refactoring patch in Patrick Steinhardt's ongoing series to make Git's object database (ODB) backend pluggable. It focuses on cleaning up how object types are handled in the streaming interface.

The patch simplifies the object type propagation by moving responsibility for setting the type entirely to the format-specific callbacks (loose, packed, and in-core object handlers) rather than having it spread across multiple layers. Instead of passing around a pointer to be filled in, the type is now tracked in the stream structure itself alongside the size field, which makes the data flow more obvious.

This change is part of a larger effort to clean up the streaming interface in preparation for making the ODB backend pluggable. The commit message explains that this will allow removing an extra object info lookup in a subsequent patch. The diff shows straightforward mechanical changes to move the type handling into the stream structure and update the various open functions accordingly.

The patch appears well-contained and builds on prior discussion in the thread, with the changes focused on internal implementation details rather than user-visible behavior. It's a logical step in the series' progression toward a cleaner streaming interface.