Refactoring patch in Patrick Steinhardt's ODB streaming interface series. This is patch 8/19 in the series, introducing a type-safe container for packed object streams, following the same pattern used for in-core and loose object streams in previous patches.

The patch creates a new `struct odb_packed_read_stream` that embeds the base `odb_read_stream` structure while adding packed-object-specific fields (pack pointer and position). This continues the series' goal of eliminating the union in `odb_read_stream` by moving format-specific data into dedicated structures. The changes are mechanical but thorough - all packed object streaming functions are updated to use the new structure while maintaining identical external behavior. 

Like earlier patches in the series, this is a well-contained change (75 lines modified) that maintains the architectural direction toward pluggable backends. The implementation follows the same proven pattern from patches 6/19 (in-core streams) and 7/19 (loose object streams), suggesting this approach is becoming the established convention for the series.

The patch-specific changes are straightforward and unlikely to be controversial, building on prior consensus in the thread. The series continues its incremental approach, with each change moving toward the ultimate goal of a cleaner, more maintainable ODB streaming interface.