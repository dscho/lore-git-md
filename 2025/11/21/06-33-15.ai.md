This is a follow-up discussion in Patrick Steinhardt's ongoing refactoring series for pluggable ODB backends, specifically addressing patch 14/18 which makes the `odb_read_stream` struct public. The exchange focuses on interface redundancy in `open_istream()` that was previously noted in the thread summary.

Karthik Nayak points out that since `open_istream()` returns a `struct odb_read_stream` containing the object type and size, the function's separate output parameters for these values are now redundant. Patrick acknowledges this and agrees to add a follow-up patch to remove the redundant parameters, though he notes this will require another tree-wide change.

The discussion confirms the transitional nature of the current interface design, where some redundancy remains from earlier iterations. This aligns with the series' incremental approach to refactoring, where temporary interfaces are maintained during the transition phases. The resolution demonstrates the ongoing refinement of the API as the series progresses toward its goal of standardized streaming interfaces for pluggable backends.

Key technical points:
- `struct odb_read_stream` now publicly exposes its fields (close callback, read callback, type, size)
- `open_istream()` currently maintains redundant output parameters that mirror the stream struct fields
- Patrick will add a new patch to remove this redundancy, though it requires widespread changes
- The exchange shows the series continuing to clean up interface artifacts as the architecture solidifies