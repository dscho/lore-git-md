This is part 7/19 of Patrick Steinhardt's ongoing refactoring of Git's object database streaming interface, continuing the effort to enable pluggable backends. The patch introduces a dedicated `struct odb_loose_read_stream` for handling loose object streams, following the same pattern as previous patches that created type-safe containers for other stream types.

The change moves the loose object stream implementation details out of the union in `struct odb_read_stream` and into its own properly typed structure. This maintains the series' architectural direction while making the code more type-safe. The patch touches only the streaming.c file, modifying 44 lines while preserving all existing functionality.

The implementation follows the established pattern seen in earlier patches of the series: creating a new stream type structure that embeds the base `odb_read_stream`, then updating all related functions to use the proper type. The change is well-contained and appears mechanically sound, continuing the incremental improvements that have been acked by maintainers in previous patches.

This is another step toward the larger goal of cleaning up the streaming interface to support pluggable backends, with each patch building on prior consensus while keeping the codebase functional throughout the transition.