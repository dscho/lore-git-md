Refactoring patch in Patrick Steinhardt's object database streaming interface series. This is patch 18/19, completing the physical reorganization of the streaming code by moving it into the odb/ subdirectory. The change renames streaming.{c,h} to odb/streaming.{c,h} and updates all include paths across 15 files to reflect the new location. 

The patch is purely organizational - it doesn't modify any functionality but makes the codebase structure better reflect the logical architecture established by earlier patches in the series. The commit message notes that while "streaming" was a generic term, these files are specifically part of the object database subsystem, so moving them makes their purpose clearer.

This is the final structural change in the series before the concluding patch (19/19) which will clean up redundant parameters. The change touches build systems (Makefile and meson.build) and updates includes in various components that use the streaming interface including archive formats, pack operations, and checkout code. All modifications are straightforward path updates with no behavioral impact.

The patch continues the series' pattern of making the object database subsystem's boundaries more explicit and self-contained. It follows naturally from the earlier work that moved streaming implementations into backend-specific files and established the ODB-centric interface naming.