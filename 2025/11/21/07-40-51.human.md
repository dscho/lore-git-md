This is part 6/19 of Patrick Steinhardt's refactoring series for Git's object database streaming interface, continuing the effort to enable pluggable backends. The patch focuses on restructuring how in-core object streams are handled.

The code moves the in-core stream implementation out of the union in `struct odb_read_stream` and into its own dedicated `struct odb_incore_read_stream`. This new structure contains the base stream fields plus the in-core specific data (buffer pointer). The change makes the code more maintainable by giving each stream type its own clearly defined structure rather than sharing a union.

The patch maintains the same functionality while improving the architecture - all existing tests continue to pass. It's a well-contained change (44 lines modified) that follows logically from the series' earlier work to separate stream concerns. The commit message clearly explains the motivation as part of the larger goal to eliminate the stream-type union.

This continues the series' pattern of incremental, clearly motivated changes that move toward the pluggable backend architecture while keeping the code working at each step. The change looks straightforward and uncontroversial, building on prior consensus in the thread.