This is a refactoring patch in a 5-patch series focused on `:(optional)` path handling and related code quality improvements. The patch converts an integer flag (`is_optional`) to a boolean type in `parse-options.c`, following Phillip Wood's suggestion.

The change is straightforward - it modifies the `do_get_value` function to use `bool` for the `is_optional` flag tracking whether a filename has the `:(optional)` prefix. The patch also updates the initialization of this flag from `0` to `false` for consistency. This is part of a broader effort in the series to improve type correctness, with similar boolean conversions appearing in other patches.

The modification is small (2 lines changed) and non-controversial, aligning with Git's code style guidelines for using boolean types where appropriate. The change doesn't affect behavior - it's purely a type system improvement that makes the code's intent clearer. The patch builds on previous work in the series that fixed `:(optional)` path handling behavior, though this particular change is independent of that functional fix.