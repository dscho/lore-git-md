This is a small refactoring patch in a 5-part series focused on improving path handling and code quality. It converts the `is_optional` flag in `git_config_pathname()` from an `int` to a `bool` type, following the same boolean type cleanup done in earlier patches of the series.

The change is straightforward and aligns with the series' theme of type safety improvements. As noted in the thread context, this continues the work suggested by Phillip Wood to use proper boolean types where appropriate. The patch modifies just one line in config.c, changing the variable declaration without altering any behavior.

This appears to be a mechanical cleanup with no expected controversy, similar to the other boolean conversions in the series. The change is small but contributes to the overall code quality improvements being made across parse-options.c and config.c in this patch set.