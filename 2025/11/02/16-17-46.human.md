This is a small refactoring patch in a series about `:(optional)` path handling. It changes the type of the `is_optional` flag in `parse-options.c` from `int` to `bool`, making the code more type-correct and self-documenting. The change is straightforward and uncontroversial, following a suggestion from Phillip Wood. 

The patch is part of a 5-patch series that fixes inconsistencies in path handling and improves related code quality. This particular change doesn't affect behavior - it just makes the code clearer by using the proper boolean type for a flag variable. The diff shows two simple changes: the type declaration and initializing the flag to `false` rather than `0`.

As with the rest of the series, this is a small but meaningful improvement to the codebase, showing attention to detail in type safety and code clarity. The change is well-justified and unlikely to need much discussion.