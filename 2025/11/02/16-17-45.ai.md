This is a documentation patch in a 5-part series focused on `:(optional)` path handling improvements. The patch clarifies the equivalence between using `:(optional)` on a non-existent file and the default command behavior in gitcli.adoc.

The change is minimal but meaningful - it expands a terse comment to explicitly state that the `:(optional)` form is equivalent to running the command without arguments when the file doesn't exist. This makes the documentation more readable while maintaining technical accuracy. The modification follows up on a suggestion from Phillip Wood and aligns with the series' goal of improving clarity around path handling behavior.

Key technical details:
- Updates gitcli.adoc documentation
- Specifically documents the equivalence case for `:(optional)` paths
- Small, focused change (1 line modified)
- Part of larger series addressing path handling consistency

The patch appears straightforward and non-controversial, serving as a documentation improvement to complement the behavioral fixes in the rest of the series.