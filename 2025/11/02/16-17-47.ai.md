This is a small refactoring patch in a series about `:(optional)` path handling and code quality improvements. It converts the `is_optional` flag in `git_config_pathname()` from `int` to `bool` type, continuing the type safety work started in patch 3 of the series.

The change is straightforward - just a one-line modification in config.c` to use the more appropriate boolean type for a flag variable. The patch follows Phillip Wood's earlier suggestion (as noted in the Suggested-by tag) and maintains consistency with the series' focus on type correctness. 

This is a mechanical change with no behavior impact, aligning with the series' pattern of small, focused improvements. The patch touches the same config.c` file modified in earlier patches, showing the author's systematic approach to cleaning up related code. As with previous patches, the justification is minimal since the improvement is self-evident from the change itself.