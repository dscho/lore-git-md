Technical refactoring patch in Ezekiel Newren's xdiff modernization series, changing `xrecord_t.ptr` from `char*` to `uint8_t*` for clearer byte-oriented semantics. This is part 3 of 10 in the series that systematically updates xdiff's types for Rust FFI compatibility.

The patch converts the pointer type while maintaining behavior through explicit casts where needed, particularly in functions like `xdl_blankline()` and `xdl_recmatch()` that expect `char*` arguments. Changes span xdiffi.c, xemit.c, xmerge.c, xpatience.c, xprepare.c, and xutils.c, with corresponding header updates in xtypes.h. Each modification preserves existing functionality while making the byte-oriented nature of the pointer explicit.

This continues the series' pattern of replacing ambiguous C types with precise alternatives, following earlier changes to `ha` field splitting and `dstart`/`dend` type updates. The commit notes all existing usage was audited to ensure correct casting, though the temporary casts indicate this is an incremental step toward broader xdiff refactoring.