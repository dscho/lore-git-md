This is a technical refinement to patch 3/4 in Kristoffer Haugsbakk's series on in-process trailer handling. Phillip Wood notes that the newly introduced `amend_strbuf_with_trailers()` helper function can never fail (it always returns 0) and thus should have a `void` return type rather than `int`. This observation came while reviewing the subsequent patch 4/4, with Phillip indicating he'll provide a more complete review of that patch soon.

The change affects the trailer processing internals where the series is moving from forking `interpret-trailers` to direct buffer manipulation. The function in question handles the core trailer insertion logic into a strbuf, with error checking happening at the higher `amend_file_with_trailers()` level for file I/O operations. This aligns with the series' goal of making trailer processing more efficient by avoiding process overhead.

The thread context shows this patch is part of ongoing architectural refinements to the series, with Junio Hamano having requested clearer patch organization and Phillip Wood providing detailed technical review. Patch 1/4 is already finalized, while patches 2-4 continue to evolve based on reviewer feedback. This particular note about the return type appears to be a straightforward cleanup that doesn't affect the overall design direction.