This is part 4/10 of Ezekiel Newren's xdiff refactoring series, focusing on type safety improvements for Rust FFI compatibility. The patch changes `xrecord_t.size` from `long` to `size_t` to better represent memory sizes, following the series' goal of using appropriate fixed-width types.

Key technical changes include:
- Updates `xrecord_t` struct definition in xtypes.h to use `size_t` for the size field
- Modifies all xdiff subsystem files (xdiffi.c, xemit.c, xmerge.c, xprepare.c) to handle the type change
- Adds explicit casts to `long` where needed for backward compatibility with existing functions like `xdl_blankline()` and `xdl_recmatch()`
- Updates loop variables and local size calculations to use `size_t` where appropriate

The changes maintain behavior while improving type correctness, with careful attention to preserving existing function signatures that expect `long` parameters. This continues the series' pattern of incremental type safety improvements while keeping the diff machinery functional. The patch follows the established approach of adding explicit casts at interface boundaries rather than changing function signatures, minimizing disruption to existing code.