This is part 3 of Ezekiel Newren's xdiff refactoring series, which modernizes core xdiff structures for Rust FFI compatibility. The patch changes the `xrecord_t.ptr` field from `char*` to `uint8_t*` to better represent that it points to raw bytes in memory.

The change is straightforward but requires casting in several places where the pointer is used with functions expecting `char*`. The patch updates all call sites across xdiff's implementation files (xdiffi.c, xemit.c, xmerge.c, etc.) to maintain compatibility while making the type more precise. This is part of the series' broader effort to replace platform-dependent types with unambiguous alternatives.

As with other patches in this series, the change is mechanical and well-contained, touching only the type declaration and its usage sites. The commit message notes that all existing usage was inspected to ensure proper casting, suggesting thorough review. This patch follows the established pattern of the series in making targeted type safety improvements while maintaining backward compatibility.