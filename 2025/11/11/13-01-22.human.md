Feature proposal for `git commit --committer` option, continuing the ongoing discussion about command-line identity overrides.

ZheNing Hu responds to Phillip Wood's question about use cases for multiple committer identities in a single repository. The key motivation is simplifying workflow when needing to correct misconfigured committer information - currently requiring manual environment variable setup (`GIT_COMMITTER_NAME`/`GIT_COMMITTER_EMAIL`) when only `--author` is available. The proposed `--committer` option would mirror `--author`'s behavior and validation.

The technical implementation (shown in the diff) follows the same pattern as the existing `--author` handling, with parallel functions for committer identity resolution. The patch includes comprehensive tests covering:
- Basic override functionality
- Pattern matching behavior
- Error handling for malformed input
- Interaction with `--amend`
- Precedence over environment variables
- Date handling consistency

This response strengthens the scripting/automation case by highlighting the cognitive overhead of current workarounds. The discussion remains balanced between those seeing value in CLI symmetry (especially for error correction) and maintainers questioning whether existing configuration methods suffice. The technical merits appear sound, though the broader AI contribution questions in this thread remain unresolved.