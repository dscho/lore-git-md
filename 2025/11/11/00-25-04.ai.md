This is a resubmission of the Meson build system enhancement for HTML documentation path configurability, addressing the minor style feedback from the previous discussion. The patch adds a `htmldir` option to Meson builds, allowing customization of HTML documentation installation paths to match distribution-specific layouts like Gentoo's versioned doc directories.

The technical implementation remains unchanged from the previous version:
- Introduces string-type `htmldir` Meson option defaulting to `datadir/doc/git-doc`
- Modifies 7 build files to replace hardcoded paths with the new variable
- Maintains backward compatibility with existing installations
- Propagates the configured path to `git --html-path` output
- The only change from v1 is reordering of options in meson_options.txt to be lexicographical, as suggested by Patrick Steinhardt
- The patch maintains all previous functionality and testing coverage
- Builds on Git's ongoing Meson integration work
- Provides feature parity with Makefile's existing HTML doc path configuration

The thread context indicates this patch is technically sound and ready for inclusion, with the only outstanding question being whether to accept it as-is or wait for a post-release v2 submission with the reordered options. Since this version now includes the requested ordering change, it appears ready for final review and potential merging.