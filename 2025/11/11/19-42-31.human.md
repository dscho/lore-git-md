This is part 9/10 of Ezekiel Newren's xdiff refactoring series, focusing on type safety improvements for Rust FFI compatibility. The patch changes the `rindex` field in `xdfile_t` from `long` to `size_t` to properly represent its role as an array index.

The change is straightforward - `rindex` is used to index other arrays, so `size_t` is the more appropriate type. The commit message notes this won't trigger cascading changes since `rindex` is only used for direct array access. This continues the series' pattern of updating xdiff's internal structures with more precise types while maintaining behavior.

The patch appears well-contained and follows the established approach of the series. It's a mechanical change that aligns with the overall goal of improving type safety in xdiff subsystem, particularly for future Rust interoperability. Since this is part of a larger, already-reviewed series that's been progressing steadily, it likely requires minimal additional scrutiny.