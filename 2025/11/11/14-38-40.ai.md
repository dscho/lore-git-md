This is a feature proposal for `git commit` to add a `--committer` option mirroring the existing `--author` functionality. Phillip Wood questions the practical need for this option in response to ZheNing Hu's corporate workflow example where users occasionally misconfigure their committer identity.

Phillip's key technical challenge focuses on the timing of when users would need this option: if correcting misconfigured commits via `--amend`, the committer would already be correct from the fixed config; if committing before realizing the misconfiguration, the user wouldn't know to use `--committer`. This narrows the discussion to whether there's a real gap between these scenarios that justifies a new option.

The exchange highlights a tension between:
1. ZheNing's argument for symmetry with `--author` and convenience over environment variables
2. Phillip's skepticism about actual workflow gaps not covered by existing methods (config, env vars, separate clones)
3. The thread's broader context showing other contributors have proposed additional use cases (corporate identity switching, legal requirements)

This reply doesn't introduce new technical elements but sharpens the debate by questioning the claimed workflow benefits. It represents maintainer pushback against feature creep while acknowledging the discussion has moved beyond simple symmetry arguments to concrete workflow analyses. The response maintains the thread's technical focus now that AI authorship concerns are resolved.