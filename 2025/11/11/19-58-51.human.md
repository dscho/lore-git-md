Documentation patch standardizing terminology in the notes merge documentation. This is part 1 of a 3-part series aligning documentation with Git's glossary definitions.

The patch updates the documentation for `git notes merge` to use "worktree" (one word) instead of "work tree" when referring to the `NOTES_MERGE_WORKTREE` directory. This matches the glossary definition where "worktree" refers to a working tree plus repository metadata, while "working tree" (two words) refers specifically to checked-out files.

The change is mechanical and straightforward, affecting just two lines in the git-notes.adoc file. The author (Junio C Hamano) notes that `NOTES_MERGE_WORKTREE` is indeed the "worktree" type (with repository metadata) rather than just a "working tree", making this change technically accurate.

This appears to be a well-justified documentation improvement that brings consistency with established Git terminology. The patch is small and focused, with no code changes - just documentation alignment. It's part of a larger effort to standardize terminology across Git's documentation.