This is a refactoring patch from Junio C Hamano's series on whitespace handling, specifically part 6 of 12. The change standardizes the variable name used for the emit_callback data structure from `ecb` to `ecbdata` in the `emit_rewrite_lines()` function, matching the naming convention used elsewhere in the codebase.

The patch is purely a naming consistency improvement - it doesn't change any functionality. The diff shows the straightforward renaming of the parameter and all its uses within the function, including updates to line counting variables and emit functions. This is part of the larger effort to clean up and standardize the whitespace handling infrastructure in Git's diff/apply pipeline.

As with the rest of the series, this is a well-crafted change with a clear, focused purpose. It follows the pattern of incremental improvements seen throughout the thread, though this particular patch is more about code style than functional changes. The patch has already been merged as part of the completed series.