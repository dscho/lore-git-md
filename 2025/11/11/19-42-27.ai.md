This is part 5 of Ezekiel Newren's xdiff refactoring series, focusing on type safety improvements for Rust FFI compatibility. The patch updates `xdl_hash_record()` and related functions to use unambiguous fixed-width integers throughout the xdiff hashing subsystem.

Key technical changes include converting `char*` to `uint8_t*` for explicit byte semantics in memory operations, changing hash outputs from `unsigned long` to `uint64_t` for platform-independent results, and updating the `flags` parameter from `long` to `uint64_t` to prevent high-bit truncation on 32-bit systems. The changes affect multiple xdiff files (`xutils.c`, `xprepare.c`, `xutils.h`, and `xdiff-interface.c`) while maintaining the same hashing behavior through careful type transitions.

The patch continues the series' pattern of explicit type safety at interface boundaries, with particular attention to the hash calculation logic that underpins Git's diff functionality. The changes are consistent with the thread's established approach of maintaining backward compatibility while preparing for future Rust integration.