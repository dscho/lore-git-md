CI / build system patch that finalizes the HTML documentation path configurability in Meson builds. This is the v2 submission that has already addressed all previous feedback and is now in its final form.

The email confirms that the current implementation correctly handles the HTML directory path configuration in Meson, using an explicit empty-check block rather than recursive option definition. Karthik Nayak had wondered if Meson supported recursive option dependencies, but Ben Knoble confirms this isn't possible based on Meson's documentation and existing patterns in the codebase (like perllibdir).

The patch is technically sound and ready for merging, with all substantive discussion complete. It maintains feature parity with the Makefile build system's HTML doc path configuration while addressing Gentoo's packaging requirements. The minor style concern about option ordering was already addressed in v2, and there are no remaining objections or requested changes.

This appears to be the final piece needed before the feature can be merged, with all reviewers satisfied and the implementation validated against real-world distribution requirements. The thread has reached consensus that the patch is correct and complete as-is.