This is part 8/10 of Ezekiel Newren's xdiff refactoring series for Rust FFI compatibility, specifically focusing on type safety improvements in the xdiff subsystem. The patch changes the `nreff` field in `xdfile_t` from `long` to `size_t` to better represent its role as an array size counter for the `rindex` array. The implementation updates both the type declaration in `xtypes.h` and removes temporary `nreff` variables in `xprepare.c`, instead directly using the struct field with proper initialization.

The changes follow the series' pattern of using more precise types while maintaining behavior - here eliminating a potential overflow risk for large diffs by using `size_t` for array indexing. The patch simplifies the code by removing temporary variables and directly tracking the count in the struct field. This continues the systematic modernization of xdiff's type system that has been reviewed throughout the thread, with previous patches having addressed similar type safety issues in other parts of the diff machinery.