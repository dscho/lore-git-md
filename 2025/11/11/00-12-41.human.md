This is a coordination message about Rust infrastructure work that intersects with the ongoing SHA-1/SHA-256 interoperability series. Ezekiel Newren announces plans to restructure Git's Rust code organization by converting the single Cargo crate into a Cargo workspace, which would move the source tree from `/src` to `/gitcore/src`. 

The message notes this change is planned for after the v2.52.0 release and references prior discussions about Rust tooling, including Patrick Steinhardt's work on cbindgen (a tool for generating C bindings from Rust code). The thread context shows this relates to the larger Rust infrastructure work happening alongside brian m. carlson's interoperability patches, though it's a separate effort focused on build system organization rather than the object storage format itself.

The message serves as a heads-up about upcoming changes to the Rust code structure that may affect the interoperability work, particularly since both efforts involve Rust/C FFI boundaries. The coordination appears smooth, with Patrick having stepped back to let Ezekiel to lead this particular infrastructure change.