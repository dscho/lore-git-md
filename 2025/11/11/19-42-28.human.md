This is part 6 of Ezekiel Newren's xdiff refactoring series, focusing on type safety improvements for Rust FFI compatibility. The patch splits the `ha` field in the `xrecord_t` struct into two distinct fields with clearer purposes: `line_hash` (a simple hash of the line content) and `minimal_perfect_hash` (a unique identifier for line comparison).

The change makes the code's intent more explicit by separating the dual-purpose `ha` field that previously served both hashing and line comparison roles. The new `line_hash` uses `uint64_t` for consistent hashing, while `minimal_perfect_hash` uses `size_t` for array indexing, which will simplify Rust interop by avoiding casts. The patch updates all affected code paths across xdiff's various algorithms (histogram, patience, etc.) to use the new fields appropriately.

This is a well-motivated change that improves code clarity while maintaining the existing behavior. The commit message clearly explains the rationale and the technical distinction between the two hash types. As part of the larger type safety effort, this patch follows the series' pattern of careful, incremental changes with thorough updates to all affected code paths.