This is a refactoring patch from Junio C Hamano's completed 12-part series on whitespace handling, specifically addressing naming consistency in the diff output code. The patch standardizes the parameter name from `ecb` to `ecbdata` in the `emit_rewrite_lines()` function to match usage elsewhere in the codebase.

The change is purely mechanical - it updates all references to the `emit_callback` parameter within this function to use the more descriptive `ecbdata` name that appears consistently in other call sites. The patch touches 6 lines in diff.c, modifying parameter declarations and all internal references to the callback data structure. This includes updates to line counter increments (`lno_in_preimage`/`lno_in_postimage`) and calls to helper functions like `emit_del_line()`, `emit_add_line()`, and `emit_diff_symbol()`.

As part of the larger whitespace handling series, this change maintains consistency with the established pattern of incremental code quality improvements. The patch doesn't alter any functionality - it simply makes the code more uniform by using the same variable naming convention throughout the diff output pipeline. This follows the series' broader theme of cleaning up and standardizing the whitespace-related infrastructure.