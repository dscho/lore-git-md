This is part 8/10 of Ezekiel Newren's xdiff refactoring series, continuing the work to modernize xdiff's type system for Rust FFI compatibility. The patch focuses on changing the `nreff` field in `xdfile_t` from `long` to `size_t`, as it tracks array size for `rindex`.

The change is straightforward but meaningful - `size_t` is the proper type for memory size tracking, especially for array indices. The patch updates both the type declaration in `xtypes.h` and removes temporary `nreff` variables in `xprepare.c`, instead using the struct field directly during array population. This maintains the same behavior while being more type-correct.

This continues the series' pattern of type safety improvements while preserving existing functionality. The changes are mechanical but important for the larger goal of Rust interoperability. The patch appears well-contained and follows the established approach of the series, with clean diffs and a clear commit message.