This is a minor correction to the ref-filter performance optimization series, specifically addressing patch 14/14 which implements lazy object parsing. Jeff King points out an incorrect comment in the `get_object()` function regarding the `eaten` flag behavior. The comment claims `parse_object_buffer()` sets `eaten` to 1 when freeing is needed, but the actual code shows the opposite logic - `eaten` is 1 when no free is needed, and the `free()` call is guarded by `!eaten`. This appears to be a simple documentation error in the patch that doesn't affect functionality but could confuse future readers.

The thread context shows this is part of a well-reviewed series that's ready for merging, with this being a last-minute nit. The series as a whole introduces significant performance improvements (13% speedup in ref-filter operations) through lazy parsing optimizations while maintaining proper object verification. This particular email doesn't raise any concerns about the core functionality - it's just ensuring the code comments accurately reflect the implementation details.