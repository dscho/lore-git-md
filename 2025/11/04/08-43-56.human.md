This is the first patch in a 5-part series introducing a new 'is-needed' subcommand for `git maintenance` to check whether maintenance tasks are required. The series focuses on ref backend optimization checks.

The patch refactors reftable internals to better support the new functionality. It modifies `stack_table_sizes_for_compaction()` to directly return compaction segments rather than just table sizes, eliminating an intermediate step. This change makes the code more reusable for the upcoming maintenance check feature while maintaining the same behavior for existing auto-compaction.

The refactoring is clean and straightforward, with the commit message clearly explaining both the immediate benefit (removing an unnecessary intermediate step) and the future use case (supporting maintenance checks). The patch has already been through review with no outstanding concerns noted in the thread summary.

This is a preparatory change that lays groundwork for the more user-visible functionality coming in the series. The implementation looks solid and focused, with proper error handling preserved through the refactoring.