This is a naming refinement in the ongoing SHA-1/SHA-256 interoperability work, specifically addressing API design at the Rust/C FFI boundary in patch 6/14 of the series. Junio C Hamano responds to brian m. carlson's defense of the `hash_algo_ptr_by_offset()` function name by proposing two concrete naming improvements that better distinguish between hash algorithm numbers and instances.

The key technical points are:
1. The current function returns a pointer to a `git_hash_algo` struct given a numeric identifier (`uint32_t algo`), with naming debate around whether "offset" accurately reflects the semantic meaning
2. Junio suggests `hash_algo_ptr_by_algo_number()` as more semantically clear than "by_offset", while keeping the pointer return type distinction
3. He also notes the parallel function `hash_algo_by_name()` returns a numeric ID, creating potential confusion with the struct pointer return, and suggests renaming it to `hash_algo_num_by_name()` for clarity
4. This continues the thread's focus on FFI boundary ergonomics, now moving from type safety to precise naming that conveys semantics across language boundaries

The exchange shows progress from abstract naming concerns to concrete alternatives, with Junio providing specific rename suggestions that maintain the technical distinction between numeric IDs and struct pointers while improving clarity. This refinement doesn't affect the underlying implementation but could influence future API usage patterns in both Rust and C code. The discussion remains focused on this narrow naming issue while the broader series continues its technical implementation work.