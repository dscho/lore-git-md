Thread identity: Bugfix patch addressing a regression in ref-filter's object parsing logic, introduced by commit 054f5f457e ("ref-filter: parse objects on demand"). Originating author not specified in this summary.

Core problem: Commit 054f5f457e added lazy object parsing via `maybe_object` in `struct expand_data`, but failed to reset this field between calls, causing stale object data to persist across operations. This particularly affects version sorting of annotated tags when ref data is cached in packed-refs.

Current status: Proposed fix resets `maybe_object` to NULL in `get_object()` before processing each new object. Includes new test case in t/t7004-tag.sh verifying correct behavior with cached refs. Builds on ps/ref-peeled-tags branch at 054f5f457e.

Key technical details:
- Fix targets ref-filter.c's object parsing infrastructure
- Regression manifests when using get_or_parse_object() function
- Test case specifically exercises version sorting of annotated tags with packed-refs caching
- Impact limited to code paths using the affected lazy-parsing functionality

Open questions: None raised yet - appears to be a straightforward bugfix with test coverage. May need review from original author of 054f5f457e (Patrick Steinhardt) or ref-filter experts like Jeff King.

Participant positions: Junio Hamano identified the regression. Patch builds on prior work from Jeff King. No other positions stated yet.

Related work: Directly addresses regression from ps/ref-peeled-tags branch (054f5f457e). Part of ongoing ref-filter improvements around object parsing efficiency.