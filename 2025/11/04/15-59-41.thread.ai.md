Thread identity: Bug report and fix for `git add` behavior with exclude patterns (`:(exclude)`) interacting with `.gitignore` rules, reported by René Rahn, with proposed fix from Junio Hamano evolving into architectural discussion.

Core problem: `git add` incorrectly warns about ignored files when explicitly excluded via pathspec magic (`:(exclude)`). The current implementation treats exclude patterns as positive matches, requiring `-f` workaround. Deeper issue identified: separate `exclude_matches_pathspec` function in `dir.c` may represent architectural debt rather than being the optimal solution.

Current status: Initial fix proposed by Junio modified `dir.c` to properly handle exclude patterns with new test coverage. New discussion questions whether this should instead refactor to use main `match_pathspec` infrastructure, suggesting current approach might be treating symptoms. All tests pass with initial fix but architectural concerns remain.

Key technical details:
- Fix targets pathspec processing in `dir.c`, specifically exclude pattern interaction with ignore rules
- Historical context: potential origins in e1b8c7bd (2017) and 29209cbe (2010) changes
- Current implementation uses custom `exclude_matches_pathspec` rather than standard `match_pathspec`
- New test in `t/t2204-add-ignored.sh` verifies desired behavior
- Core architectural question: whether to unify pathspec matching logic

Open questions:
- Whether to pursue deeper refactoring of pathspec infrastructure
- Edge cases in other commands using similar magic (e.g. `git log`, `git diff`)
- Historical rationale for separate exclude matching implementation
- Potential performance implications of unification

Participant positions:
- René Rahn provided original clear bug report
- Junio Hamano proposed initial fix but now questions architectural approach
- No other contributors have weighed in yet on the deeper design question

Related work:
- General pathspec processing architecture in Git
- Historical changes to dir.c (e1b8c7bd, 29209cbe)
- Pathspec magic implementations across other commands
- `.gitignore` pattern matching evolution

The thread has expanded from a specific bugfix to potentially significant architectural reconsideration of pathspec handling, though no concrete alternative proposal has emerged yet. The passing tests with current approach may reduce urgency for deeper changes.