Thread summary:

Bugfix for Meson build system's handling of the `git-contacts` Perl script in the contrib directory, where the script was being installed without executable permissions (causing "not a git command" errors). D. Ben Knoble's patch modifies `generate-perl.sh` to explicitly mark `git-contacts` as executable by adding `|*git-contacts` to the permission-setting case statement. Patrick Steinhardt reviewed the solution, confirming its technical correctness while noting an alternative approach (renaming to `git-contacts.perl`) would avoid needing a special case. The current solution is deemed sufficient despite the alternative's theoretical cleanliness. Technical details unchanged: the fix ensures proper permissions when building with `-Dcontrib=contacts` in Meson. The thread shows consensus on the approach, with Patrick's review validating the solution while acknowledging trade-offs with the alternative naming scheme. No outstanding objections or follow-ups required.