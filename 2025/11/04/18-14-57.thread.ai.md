Bugfix for Meson build system's handling of the `git-contacts` Perl script in contrib directory, where installation lacked executable permissions (causing "not a git command" errors). D. Ben Knoble's solution modifies `generate-perl.sh` to explicitly mark `git-contacts` as executable via `|*git-contacts` in the permission case statement, addressing the discrepancy with `generate-script.sh`'s behavior. Patrick Steinhardt reviewed the approach, confirming technical correctness while noting an alternative (renaming to `git-contacts.perl`) would avoid special casing. Maintainer Junio C Hamano concludes the discussion, agreeing with the technical resolution and acknowledging Ben's v2 patch that fixed a trivial typo ("install" -> "installing") in the commit message during the pre-release freeze. Junio reinforces Patrick's point about `.perl` suffix consistency being theoretically cleaner, while humorously noting contrib directory's tolerance for such "sloppiness". The thread shows clear consensus with no outstanding objections - the typo correction is deemed not critical enough to require further patch revision, and the technical solution (special-casing permissions in `generate-perl.sh`) is accepted as sufficient despite the alternative approach's theoretical advantages. All aspects are closed regarding executable permission handling for this contrib script in Meson builds, with maintainer confirmation that the current solution stands. The v2 patch maintains the same technical content as v1 while fixing the minor documentation typo, demonstrating the solution's stability through multiple iterations.