This is a refactoring patch in Junio Hamano's series introducing "incomplete-line" whitespace error detection. It's the sixth patch in a 12-part series.

The change standardizes the parameter name used in `emit_rewrite_lines()` from `ecb` to `ecbdata`, matching the naming convention used elsewhere in the codebase. This is a straightforward consistency improvement - the function's behavior remains exactly the same, it just uses the same variable naming as other similar functions in diff.c.

The patch is small (6 lines changed) and focused, continuing the series' pattern of cleaning up surrounding code before introducing the new incomplete-line functionality. Like previous patches in the series, this demonstrates careful attention to codebase consistency and maintainability. The change touches the same diff emission machinery that will be used by the upcoming incomplete-line feature, though this particular patch doesn't yet introduce any of that new functionality.