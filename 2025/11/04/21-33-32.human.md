This is a follow-up discussion on a bugfix patch for ref-filter's object parsing logic. The patch addresses a regression where stale object data was persisting between calls, particularly affecting version sorting of annotated tags when ref data is cached in packed-refs.

Jeff King (peff@peff.net) reviews Patrick Steinhardt's fix and test case, confirming the solution makes sense. He suggests an alternative test approach using `git for-each-ref` that more directly exposes the bug. The discussion reveals an interesting edge case where uninitialized data could be shown when the first parsed object is a non-tag, though this isn't a security concern as the type checking remains sound.

The review confirms the fix properly handles all cases while maintaining the performance benefits of the original lazy-parsing optimization. No new issues are identified, and the solution appears robust against potential edge cases. The thread shows consensus on both the problem and solution, with the fix already queued for merging.