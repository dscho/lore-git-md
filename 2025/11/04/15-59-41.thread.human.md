Here's a human-readable summary of the thread:

This thread reports a bug in `git add` where exclude patterns (`:(exclude)`) don't properly interact with `.gitignore` rules. René Rahn demonstrates that when trying to add files while explicitly excluding one that's also ignored, Git incorrectly warns about the file being ignored, even though the exclude pattern should make that check unnecessary. The current behavior forces users to either see spurious warnings or use `-f` (force) which could accidentally include other ignored files.

The core issue appears to be in how Git processes pathspec exclusions versus ignore rules - the exclude pattern should logically take precedence, making the ignore check redundant for explicitly excluded files. The bug affects pathspec handling in `git add` and likely involves the interaction between `pathspec.c` and `dir.c`. René's report provides clear reproduction steps and makes a compelling case that this is an inconsistency worth fixing to make the behavior more intuitive.