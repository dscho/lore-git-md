This is a clarification in the performance optimization and refactoring series, specifically addressing patch 14/14 which introduces lazy object parsing in ref-filter. Jeff King confirms that while the comment about the `eaten` flag in `get_object()` was incorrectly inverted in the patch, the underlying logic remains correct. The flag's behavior (where 1 means the buffer doesn't need freeing) is unchanged, though the initialization now defaults to 0 to handle cases where `parse_object_buffer()` isn't called. 

Key technical points:
- The `eaten` flag initialization changed from 1 to 0 to properly handle lazy parsing paths
- `parse_object_buffer()` still sets `eaten=1` when the buffer doesn't need freeing
- The logic checking `if (!eaten) free(oi->content)` remains correct
- A NULL `contentp` case would safely no-op on free() rather than error

This resolves Junio Hamano's question about whether the logic was inverted along with the comment. The series remains in good shape for merging with this documentation correction, as the code behavior was always correct despite the misleading comment. This maintains the 13% performance improvement from lazy parsing while ensuring proper resource cleanup.