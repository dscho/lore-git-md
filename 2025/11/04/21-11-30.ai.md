This is a technical discussion about the ref-filter object parsing fix, continuing the thread about the regression introduced by commit 054f5f457e.

Jeff King (peff@peff.net) responds to Junio Hamano's question about whether the "on demand" parsing optimization needs new benchmarks. He confirms that the %(raw) case (the original optimization target) remains unaffected by the fix, as it never used the problematic maybe_object field. The discussion then explores the scope of the optimization - it specifically benefits formats needing object contents without parsing, with %(raw) being the primary example.

The email notes that more significant performance gains could come from progressive parsing (stopping after needed fields), similar to what pretty.c implements, but acknowledges this would be more complex. The parser unification between ref-filter and pretty.c is mentioned as a potential future direction, though not directly related to the current fix.

Key technical points for future reference:
- The fix maintains performance for %(raw) use cases
- Current optimization only helps formats needing object contents without parsing
- Progressive parsing (like in pretty.c) could provide broader benefits
- Potential future work to unify ref-filter and pretty.c parsers

The discussion confirms the fix is correct while exploring the boundaries of the optimization strategy, but doesn't raise any new issues with the patch itself.