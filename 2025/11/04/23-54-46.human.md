This is a documentation correction in the final patch of a performance optimization series for the ref-filter subsystem. Jeff King points out that while the code logic in Patrick Steinhardt's patch is correct, the comment about the `eaten` flag behavior was backwards - it should indicate that `eaten=1` means free() is *not* needed (the code already implements this correctly). 

The patch is part of a larger series that introduces lazy object parsing optimizations showing 13% speedups in ref-filter operations. Junio Hamano had questioned whether the logic itself might be inverted, but Jeff confirms the implementation is sound - the comment just needed fixing. This was the last remaining issue before merging the series, which has now been resolved.

The series represents significant architectural improvements to Git's ref handling, completing the separation between transport and core ref structures while adding performance optimizations. With this documentation clarification addressed, the path is clear for merging into 'next'. The change is minor but important for maintainability, ensuring the code's behavior matches its documentation.