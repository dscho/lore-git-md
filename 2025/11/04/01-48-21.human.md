This is a technical discussion about naming and type safety in the SHA-1/SHA-256 interoperability patch series, specifically focusing on how to expose algorithm lookup functions across the C/Rust FFI.

The email continues a discussion about patch 6/14 in the series, which introduces functions for looking up hash algorithm information. Junio Hamano had raised concerns about the naming of `hash_algo_ptr_by_offset()`, suggesting the "by_offset" suffix reveals too much implementation detail. Brian Carlson responds that while he's open to better names, alternatives like "by_id" or "by_index" may not be clearly better, and invites suggestions.

A second point clarifies the distinction between two similarly-named functions: `hash_algo_by_name()` returns an algorithm number, while `hash_algo_ptr_by_offset()` returns a pointer to the algorithm structure. Brian notes the pointer return is explicitly marked in the name to avoid confusion.

The discussion also touches on why the hash algorithm structure is passed as a void pointer rather than being fully exposed in Rust like `ObjectID` was. Brian explains this is a temporary measure pending better Rust/C interop tooling, and that the compiler can optimize better with the current approach for functions like `raw_len()` where the algorithm is known at compile time.

This exchange shows careful attention to API design at the C/Rust boundary, with both parties working to find clear, unambiguous names that properly convey the semantics while maintaining type safety. The discussion remains technical and collaborative, focused on making the right long-term design choices for this foundational interoperability work.