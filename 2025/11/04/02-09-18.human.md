Refactoring patch in a series introducing new whitespace handling for incomplete lines. This is patch 2/12 from Junio Hamano's series to detect and handle missing newlines at end of files as a configurable whitespace error class.

The patch makes a simple but consistent style change to `emit_line_ws_markup()` in diff.c, standardizing the use of braces in an if/else cascade. The function now uses braces for all conditional arms, where previously some were omitted. This matches Git's coding guidelines that recommend consistent brace usage when any arm of a conditional requires them.

No behavior changes are introduced - this is purely a code style improvement that makes the control flow more visually consistent. The change is small (4 lines modified) and straightforward, part of the preparatory cleanups that precede the actual feature implementation in later patches.

As with the rest of the early series, this demonstrates careful attention to code quality and style consistency before introducing new functionality. The patch follows the established pattern of the series by isolating mechanical improvements from behavior changes.