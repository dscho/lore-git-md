This thread addresses a straightforward bug in the Meson build system where the `git-contacts` Perl script in the contrib directory was being installed without executable permissions, causing "not a git command" errors. D. Ben Knoble proposed a fix by modifying `generate-perl.sh` to explicitly mark `git-contacts` as executable, adding it to the permission-setting case statement.

Patrick Steinhardt reviewed the solution, confirming its technical validity while noting that renaming the script to `git-contacts.perl` would be a cleaner alternative, though not strictly necessary. In a subsequent email, Ben acknowledged a minor typo Patrick caught in the commit message but decided against sending a revised version unless specifically requested, as the technical solution itself remains sound.

The thread has reached clear consensus on the current approach, with no outstanding objections or follow-ups required. The fix ensures proper permissions when building with `-Dcontrib=contacts` in Meson, and while there was discussion of alternative implementations, the chosen solution is considered sufficient by all participants.