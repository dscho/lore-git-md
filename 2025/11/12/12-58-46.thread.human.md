This thread began with Martin Wilck reporting that `git submodule update --depth=1` fails for SHA1 submodules in SHA256 superprojects. The discussion quickly established that this is working as designed - Git deliberately does not support mixing hash algorithms within a repository, as it would constitute repository corruption. brian m. carlson implemented validation in `index_path()` to prevent creating such invalid states through `git add` and `git submodule add`, with Jeff King following up to clean up redundant submodule resolution code.

The latest exchange addresses some edge cases in the implementation. brian confirms that the validation correctly handles intent-to-add operations (`git add -N`) by design, since these use the empty blob OID which can't mismatch algorithms. Jeff noted a redundant submodule resolution check that could be cleaned up, and brian plans to incorporate Jeff's test suggestions in a v2 patch. The discussion shows consensus on the technical approach while identifying minor improvements for test coverage.

Martin Wilck, who originally reported the issue from an openSUSE deployment perspective, has now acknowledged the historical context and technical constraints. The thread has effectively concluded with mutual understanding that mixed-hash repositories remain invalid by design, and practical recommendations to use tarballs instead of submodules in Martin's deployment scenario. The final patches will provide robust validation while maintaining Git's existing design constraints around hash algorithm interoperability.