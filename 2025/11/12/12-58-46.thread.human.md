This thread began with Martin Wilck reporting a submodule checkout failure in SHA256 repositories when using `--depth=1`, which revealed a deliberate design choice: Git does not support mixing SHA1 and SHA256 repositories. brian m. carlson implemented validation in `index_path()` to prevent creating such invalid states, with Jeff King following up to clean up duplicate submodule resolution code. Junio C Hamano has approved Jeff's cleanup patch, and brian now confirms they'll incorporate it into their upcoming v2 series along with the original validation logic. Testing is underway for this combined approach.

The discussion has expanded to practical deployment solutions, with Marc Branchaud proposing maintaining SHA256 mirrors of SHA1 upstreams as a workaround, drawing parallels to early SVN-to-Git transition patterns. While the technical resolution is settled - mixed-hash repositories remain invalid by design - open questions remain about mirror implementation details and remote configuration compatibility. brian's v2 patch series, expected soon, will include improved test coverage and the merged improvements from Jeff's cleanup work. The thread shows the project's typical pattern of rigorous technical review followed by practical deployment considerations, with multiple contributors collaborating to both enforce design invariants and support real-world migration paths.