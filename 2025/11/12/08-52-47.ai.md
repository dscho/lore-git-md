This is a follow-up discussion in the bugfix thread addressing `git fetch`'s tag handling regression. Karthik Nayak responds to Patrick Steinhardt's review about the interaction between the fix and `prune_refs()` behavior.

Key points in this email:
- Karthik acknowledges Patrick's concern about potential behavior changes in `prune_refs()` when references are locked (particularly with the files backend)
- After attempting to write a test case, Karthik realizes the transaction isn't actually created during pruning, meaning there's no behavior change to test
- The realization corrects Karthik's earlier assumption that partial deletions might succeed during pruning
- Karthik plans to amend and send a new version of the patch series to clarify this point

This email resolves one of the two remaining verification points Patrick had identified (regarding `prune_refs()` behavior), leaving only the need for edge case testing with locked references across different backends as an open consideration. The discussion confirms the core fix remains sound while adjusting the scope of its impact on pruning functionality.