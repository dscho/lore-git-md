Here's the updated AI thread summary:

Bugfix for HTTP authentication in remote-curl when handling large requests with probe_rpc(). The issue occurs during filtered clones (`--filter=blob:none`) where initial authentication succeeds for commit history but subsequent blob requests fail with 401 errors due to missing auth headers in probe_rpc() calls. The patch adds `http_append_auth_header()` to probe_rpc() in remote-curl.c to ensure credentials are included, addressing the immediate issue of token reuse while leaving HTTP_REAUTH cases for future work. The change is minimal (2-line addition) and specifically targets the Bearer token flow, though not limited to it. The author provided a detailed reproduction scenario using git-credential-msal to demonstrate the real-world impact. The solution follows existing auth handling patterns in the HTTP transport code.