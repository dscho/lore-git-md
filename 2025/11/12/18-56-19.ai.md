This is a maintainer response to a feature proposal for adding `--committer` flag symmetry to `git commit`. Junio Hamano provides detailed technical feedback on the v3 implementation while reinforcing skepticism about the feature's necessity.

The email focuses on code quality concerns in the current patch:
1. Questions the verb choice ("override" vs "set/use") in documentation
2. Critiques the `is_author` boolean parameter in favor of using the existing `enum want_ident`
3. Notes unsatisfactory conditional logic for author-specific handling
4. Suggests extracting message-parsing into a helper function to reduce patch noise

Key technical points for future reference:
- The shared `determine_identity()` function handles both author/committer cases
- Environment variable handling (`GIT_COMMITTER_*` vs `GIT_AUTHOR_*`)
- Message header parsing remains author-specific
- Maintainer prefers cleaner separation of concerns in the refactoring

The response continues the thread's trajectory toward likely rejection, with maintainer concerns now including both philosophical objections and implementation cleanliness. The technical feedback suggests that even if the feature were accepted, significant restructuring would be needed to meet code quality standards. The discussion remains focused on whether the problem being solved justifies the interface expansion, with Junio's latest comments reinforcing that position through close code examination.