This is a refactoring patch in Junio C Hamano's 12-part series introducing comprehensive whitespace handling for missing newlines. The patch standardizes the parameter name `ecbdata` across all calls to `emit_rewrite_lines()` in diff.c, bringing it in line with the naming convention used elsewhere in the codebase.

The change is purely mechanical, renaming the `ecb` parameter to `ecbdata` in both the function declaration and all its call sites. This affects line counting operations during diff output generation, where `lno_in_preimage` and `lno_in_postimage` are incremented, as well as the emission of diff symbols for lines without newlines (DIFF_SYMBOL_NO_LF_EOF).

The patch maintains all existing functionality while improving code consistency. It's a small but meaningful change in the context of the larger whitespace handling series, particularly as it touches the same line counting infrastructure that was discussed earlier in the thread regarding the `#leftoverbits` item about line counting behavior. The modification is straightforward with 6 lines changed (all parameter name updates) and no behavioral impact.