This is the final discussion point for the v7 documentation patch series introducing `gitdatamodel.adoc`. The email focuses on the presentation of file type information in the tree object section of the new documentation.

Junio Hamano approves the current approach of moving the Unix-mode-inspired numeric representations to a side note, finding it makes the main text less cluttered while still providing the technical details. The patch now clearly lists the five supported file types (regular, executable, symlink, directory, gitlink) in the main text with their semantic meanings, while the numeric representations appear in a NOTE section with explicit mapping to Unix file modes.

The example tree object display shows the concrete format Git uses (mode/type/oid/name) with real SHA-1 hashes, matching `git ls-files --stage` output. The documentation maintains technical accuracy while improving readability by separating the conceptual file types from their implementation-specific numeric representations.

This appears to be the last outstanding discussion point before the series can be merged, with all other technical aspects having been resolved in previous iterations. The email suggests consensus is forming around this balanced approach to presenting file type information in the data model documentation.