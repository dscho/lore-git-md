This is a technical follow-up in the ongoing discussion about committer identity control in `git commit`. ZheNing Hu responds to Junio Hamano's suggestion about using `-c` config overrides, pointing out that this approach still requires explicit `--author` specification and lacks symmetry between author and committer controls.

The email reinforces the need for a more streamlined interface compared to the current workarounds involving multiple config overrides. While not introducing new code changes, it continues advocating for a unified solution that would handle both author and committer identities consistently, maintaining the thread's focus on improving command-line ergonomics for single-commit identity modifications.

Key technical points referenced:
- Current workflow requires both config overrides (`-c user.name`/`-c user.email`) and explicit `--author` flag
- The asymmetry between author and committer specification
- The thread's prior discussion of `--committer-is-author` and `--user` alternatives

This response keeps the technical discussion active while the thread appears to be converging toward some form of unified identity control interface, with participants still working through the exact parameter naming and behavior details.