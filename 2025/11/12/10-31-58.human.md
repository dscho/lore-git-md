This is a technical discussion about Patch 3 in Jeff King's ASan series, which proposes using NO_MMAP with ASan builds to improve memory safety detection. The email responds to Collin Funk's findings about modern compilers detecting mmap bounds violations.

Jeff King provides detailed test cases showing that ASan's mmap bounds checking is inconsistent - it sometimes detects page-sized overflows but misses smaller ones, and behaves unpredictably even when it does catch issues. His tests demonstrate that heap allocations (via malloc) give more reliable ASan detection than mmap, supporting the original patch's approach.

The discussion reveals nuances in ASan's mmap handling - it may round up to page sizes, only catch some violations, and produce less useful error messages compared to heap allocations. Jeff maintains that NO_MMAP remains valuable for catching one-byte overruns that mmap+ASan might miss, though he acknowledges the compiler landscape is evolving.

This exchange highlights the careful tradeoffs in memory safety tooling - while modern compilers are improving, the patch's conservative approach still appears justified for thorough detection of all buffer overruns, not just page-sized ones. The technical depth suggests this patch warrants close review from those familiar with ASan's internals.