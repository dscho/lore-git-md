This is a follow-up discussion in the `git-history` feature thread, where Sergey Organov points to an existing third-party tool (`git-revise`) that offers similar functionality to what Patrick Steinhardt's series is implementing. The email suggests looking at `git-revise`'s interactive message editing feature (`--interactive --edit`) which handles bulk commit message changes via a single file interface, similar to a todo list. The tool also reportedly has commit splitting capabilities, though the author hasn't personally used that feature. This appears to be an informational note rather than a critique - Sergey explicitly states they haven't closely followed prior discussions and may be unaware if these points were already addressed. The mention serves to highlight alternative approaches in the ecosystem that could potentially inform the design of the new `git-history` command. No direct changes to the current patch series are requested.