This is a follow-up discussion in the `git-history` command thread, where Sergey Organov points to an existing third-party tool (`git-revise`) that offers similar history editing capabilities. 

Sergey notes that he hasn't closely followed the full discussion but suggests looking at `git-revise`'s interactive message editing feature, which handles bulk commit message changes via a single file interface. He also mentions it has commit splitting functionality, though he hasn't personally used that part. The comment appears to cross-pollinate ideas between the proposed `git-history` command and this external tool's approach.

The suggestion comes late in the thread (after v6 of the patch series) and doesn't directly critique the current implementation. Rather, it offers an alternative implementation pattern to consider, particularly for the message editing workflow. Since this is the first mention of `git-revise` in the thread, it may spark discussion about whether to incorporate any of its approaches or whether the current `git-history` design sufficiently covers these use cases.