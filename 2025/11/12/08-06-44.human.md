This is part 7 of a 9-patch series addressing ASan (AddressSanitizer) findings, focusing on memory safety fixes in Git's codebase. The current patch is a small cleanup in the fsck subsystem's date validation logic.

The patch removes a redundant check in `fsck_ident()` that verified whether a timestamp parsing operation found any digits. The author explains this check became unnecessary after a previous hardening change (8e4309038f) that already ensures we bail early if no digits are present. The change simplifies the code slightly while maintaining all existing validation behavior.

This appears to be a straightforward cleanup that follows logically from earlier hardening work in the series. The change is minimal (just one line removed) and well-explained, with a clear reference to the prior commit that made this check redundant. As part of the larger ASan-focused series, it contributes to making the codebase more robust against memory safety issues.