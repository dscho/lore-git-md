Thread identity: Bug report turned design discussion about submodule checkout failure in SHA256 repositories when using --depth=1, originally reported by Martin Wilck, now with concrete implementation from brian m. carlson following input from Junio C Hamano.

Core problem: The thread began with a report of submodule checkout issues in mixed SHA1/SHA256 repositories but established this is working as designed - such mixing constitutes repository corruption. brian m. carlson has now implemented code changes to prevent creation of invalid mixed-hash states via `git add` and `git submodule add`.

Current status: brian m. carlson's patch implements the discussed validation in `index_path()` to actively detect and reject mixed-hash scenarios, hardening against corruption. The patch includes comprehensive tests verifying rejection in both hash algorithm directions. The original submodule case remains a documented limitation with no planned changes beyond this prevention mechanism.

Key technical details:
- Original issue: `git submodule update --depth 1` fails for SHA1 submodules in SHA256 superprojects
- Root cause: Deliberate design choice to not support mixed hash-algorithm repositories
- Implementation adds validation in `object-file.c`'s `index_path()` comparing `oid->algo` with parent repo's `hash_algo`
- Tests in `t/t3700-add.sh` and `t/t7400-submodule-basic.sh` cover both `git add` and `git submodule add` paths
- Tests verify both SHA1-in-SHA256 and SHA256-in-SHA1 rejection scenarios
- Explicit error message when hash algorithms mismatch
- Maintains position that mixed-hash repos are invalid by design
- Workaround (manual fetching) remains only option for existing mixed cases

Open questions:
- Whether documentation should be updated to explain these constraints more prominently
- Potential edge cases in the validation logic during real-world usage
- Participant positions:
- Martin Wilck provided initial detailed bug report
- Junio C Hamano authoritatively confirmed design intent
- brian m. carlson implemented the validation patch maintaining original constraints
- No dissenting opinions on the fundamental limitations

Related work:
- Directly connects to brian m. carlson's SHA-1/SHA-256 interoperability work
- Relevant to ongoing hash algorithm transition discussions
- May inform future documentation about repository compatibility
- First concrete code change resulting from this thread
- Implementation avoids fsck false positives noted in commit message