Bugfix patch in the ASan findings series, focusing on fsck timestamp validation cleanup.

This is patch 7/9 in Jeff King's series addressing AddressSanitizer findings. The change removes a redundant timestamp validation check in `fsck_ident()` that became unnecessary after commit 8e4309038f added stricter buffer parsing. The patch simplifies the code by eliminating a `p == end` check that's now guaranteed to be false due to prior validation.

The change is small (1 line removed) but meaningful - it removes a redundant condition while maintaining all existing error checking behavior. The commit message clearly explains why the check is no longer needed, referencing the earlier commit that made it redundant. This appears to be a straightforward cleanup that should be uncontroversial, though it's part of a larger series that's still awaiting review.

Key technical details:
- Modifies `fsck.c`'s timestamp parsing logic
- Removes check for empty parse result (`end == p`)
- Keeps all other validation (overflow, timezone format)
- References prior commit 8e4309038f that made this check redundant

This patch continues the series' theme of hardening fsck's string handling while simplifying the code. It builds on the earlier fsck_ident() changes from patches 5-6 in the series, though it's a more isolated change focused on cleaning up redundant checks rather than adding new safety measures.