This is a technical follow-up in the ongoing discussion about adding committer identity control to `git commit`. ZheNing Hu responds to Junio Hamano's skepticism about the workflow benefits of a `--committer` flag versus using environment variables or rebase operations.

The key new points in this response:
1. Acknowledges that bulk committer changes might better belong in `git rebase` (suggesting future work there)
2. Concedes that environment variables (`GIT_COMMITTER_*`) are indeed cumbersome to use
3. Implicitly maintains the case for a `--committer` flag by noting the awkwardness of current workarounds

The email doesn't introduce new technical arguments but reinforces the usability pain points that motivated the original proposal. It shows the discussion narrowing to specific implementation questions:
- Leaving bulk operations to rebase
- Keeping the simple case (single commit correction) for `git commit` interface
- Future potential for symmetry between commit and rebase identity controls

The response tactfully addresses Junio Hamano's concerns while keeping the door open for the flag-based approach in simple correction scenarios. No new code changes are proposed here - this is purely a discussion email advancing the conversation about appropriate interfaces for committer identity control.