Technical refinement in the `git commit` identity control discussion. ZheNing Hu responds to Jeff King's consideration of using `-c` config overrides instead of a new `--committer` option, arguing for the latter's advantages. 

The email highlights three key benefits of a dedicated `--committer` flag over the `-c` approach: symmetry with the existing `--author` flag, requiring only a single option rather than multiple config settings, and the ability to leverage Git's existing commit history lookup functionality (though this was not the original motivation). 

This continues the thread's technical debate about the best interface for controlling committer identity during single-commit operations, with participants now weighing the tradeoffs between config overrides and dedicated command-line flags. The discussion remains focused on ergonomic improvements for the simple correction case, maintaining the established boundary from bulk operations that would be handled by future `git rebase` work.