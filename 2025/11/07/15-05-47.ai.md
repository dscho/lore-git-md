This is the final patch in the submodule gitdir path encoding series, implementing case-folding collision detection and resolution. The patch adds a new check in `validate_submodule_git_dir()` that detects potential case-folding filesystem collisions and triggers a stricter URI encoding scheme when needed, percent-encoding uppercase characters (e.g., "Foo" becomes "%46oo").

The implementation introduces a new `is_casefolding_rfc3986_unreserved()` function in url.[ch] that treats uppercase letters as reserved characters requiring encoding. The collision detection logic first checks for case-insensitive filesystems via `core.ignorecase` config, then verifies potential collisions by comparing normalized paths. The encoding is only applied when necessary, leaving case-sensitive filesystems unaffected.

Test coverage in t7425-submodule-encoding.sh demonstrates the behavior by creating submodules with case-variant names ("folding" vs "FoldinG") and verifying they get distinct encoded gitdir paths. The patch maintains the series' architectural principles while addressing the final edge case of case-folding collisions, completing the comprehensive solution for submodule path encoding.

Key technical details for future reference:
- New `is_casefolding_rfc3986_unreserved()` predicate in url.[ch]
- Case-folding detection via `core.ignorecase` config and filesystem checks
- Path normalization using `strbuf_normalize_path()`
- Two-stage encoding fallback in `submodule_name_to_gitdir()`
- Test case demonstrating case-variant submodule handling
- Maintains all prior series constraints while adding this final safety check