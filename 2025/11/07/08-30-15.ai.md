This is a refinement discussion in the `git diff --raw-extended` attribute reporting thread, focusing on output format details. Torsten Bögershausen suggests using TAB as a separator before the filename in the proposed single-line format, drawing from their experience with `git ls-files --eol` output. The suggestion aims to improve both human readability and machine parseability by clearly delineating the attribute section from the filename.

The email references Justin Tobler's latest format proposal (`:100644 100644 a1961526 e231acb1 binary=yy M foo`) and Junio Hamano's historical influence on using TAB separators in similar contexts. Torsten demonstrates with a hex dump how TAB separation worked in `ls-files --eol`, where all metadata appears before the TAB and filenames after, enabling two-stage parsing if needed.

This positions the discussion at a finer granularity than the core format debate, now addressing field separation within the already-agreed single-line structure. The TAB proposal maintains backward compatibility while potentially offering clearer parsing boundaries than space separation. The suggestion appears technically sound but awaits feedback from Justin (as the current patch author) and Junio (as the maintainer with strong parsing requirements) on whether this refinement would improve the design.