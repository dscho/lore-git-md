Bug investigation update on `git cherry-pick` vs `git apply` behavior discrepancy. Bhavik Bavishi reports that when using `git apply --3way --verbose` with the test patch, all files including `mango/utils/apple_utils.cc` apply cleanly with exit code 0, matching the behavior of `git cherry-pick`. This confirms Johannes Sixt's hypothesis that the three-way merge mode explains the difference from regular `git apply`'s failure.

The key finding is that both `cherry-pick` and `apply --3way` produce identical file contents for the problematic `apple_utils.cc` case where regular `apply` would fail. This moves the discussion toward understanding whether the silent success in three-way mode is expected behavior (perhaps due to merge conflict resolution heuristics) or still represents a bug in error reporting. The test case involves multiple files in a mango codebase, with the original issue centered around conditional logic in apple-related utilities.

This reply provides important empirical data showing the behavior aligns between the two three-way merge operations, though the thread remains open on whether this should be considered correct behavior or if `cherry-pick` should still surface some notification when it resorts to three-way resolution. The investigation now focuses on the implications of three-way merging rather than a direct bug in `cherry-pick`'s implementation.