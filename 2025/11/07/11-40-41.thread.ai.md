Thread identity: Discussion about potential removal of `git whatchanged` command, initiated by user feedback from Franz Brauße, with Kristoffer Haugsbakk providing the deprecation rationale.

Core problem or goal: Evaluating the planned removal of `git whatchanged` (which shows file modifications per commit) due to its functional overlap with `git log --no-merges --raw` and other flags. The thread balances established user workflows against maintenance costs and command-line consistency.

Current status: Kristoffer Haugsbakk has clarified this is an already-decided technical migration rather than an open debate, citing equivalent `git log` functionality (`--no-merges --raw` for exact behavior, with `--stat` or `--name-only` suggested for human-readable alternatives). Franz Brauße maintains the command remains useful in their workflow despite the alternatives.

Key technical details: `git whatchanged` (builtin/whatchanged.c) shares machinery with `git log` but presents output differently. The deprecation follows Git's BreakingChanges policy, with specific migration paths documented. The exact flags needed to replicate behavior are now explicitly stated in the thread: `--no-merges --raw` matches the default `whatchanged` output, while `--stat` provides more human-readable formatting.

Open questions and loose ends: Whether the migration documentation adequately supports users like Franz Brauße who rely on the existing command. No response yet from Franz regarding the suggested alternatives or whether they meet their needs. The maintenance cost rationale mentioned in earlier messages remains unaddressed in the technical response.

Participant positions: Franz Brauße advocates keeping the command based on established workflow value. Kristoffer Haugsbakk represents the deprecation position, framing it as an already-decided technical migration with clear alternatives. The tone suggests this may not be negotiable despite user feedback.

Related work: Part of broader efforts to streamline Git's command set by removing redundant functionality. The thread references Git's BreakingChanges documentation as governing policy for such decisions. Previous discussions likely informed the current deprecation path.