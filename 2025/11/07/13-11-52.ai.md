This is a discussion about the potential removal of `git whatchanged` in the context of user feedback. Kristoffer Haugsbakk responds to Franz Brauße's defense of keeping the command by clarifying the rationale for its deprecation while providing migration guidance.

The email explains that `git whatchanged` is being removed because its functionality is fully covered by `git log` with appropriate flags (`--no-merges --raw`), noting these commands share the same underlying machinery. The response offers specific migration advice, suggesting alternative flags like `--stat` or `--name-only` for human-readable output versus machine parsing.

Key technical details include the equivalent `git log` flags that replicate `git whatchanged`'s behavior, and a reference to Git's BreakingChanges documentation that presumably outlines the deprecation policy. The response maintains a neutral tone while correcting the perception that this was up for a vote, positioning it instead as an already-decided technical migration.

The thread context shows this is part of an ongoing evaluation of older Git commands, with Franz Brauße representing established users who find value in the current implementation. Kristoffer's response doesn't address the maintenance cost arguments hinted at in the thread summary, focusing instead on the technical migration path. This suggests the discussion may continue around whether the migration documentation and transition period adequately address real-world usage patterns like Franz Brauße's workflow.