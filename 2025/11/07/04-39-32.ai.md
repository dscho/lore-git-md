Bug report about `git cherry-pick` silently ignoring patch application errors that `git apply` would catch. The reporter demonstrates a case where `git cherry-pick` completes successfully but fails to apply a hunk that `git apply --verbose` correctly identifies as problematic in the file `mango/utils/apple_utils.cc`. The issue appears in both Git 2.34.1 and 2.51.2, and also affects JGit (via Gerrit UI).

The key technical details: The failing hunk attempts to modify code around line 658 in apple_utils.cc, where the context shows a function `IsAppleBoxPackEnabled()` with complex conditional logic. The patch fails to apply because the expected surrounding context doesn't match, though the exact mismatch isn't shown. The reporter suggests adding a `--verbose` option to `git cherry-pick` to surface such issues.

This appears to be a genuine discrepancy in error handling between two related Git commands, with `cherry-pick` being less strict about reporting patch application failures. The report is well-formatted with clear reproduction steps and version information, though it would benefit from the exact `cherry-pick` command used and the complete original+patched file context. The issue could affect users relying on `cherry-pick` for accurate patch application.