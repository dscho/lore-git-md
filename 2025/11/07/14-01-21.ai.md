Bugfix follow-up addressing error handling in `git fetch` tag transactions.

Karthik Nayak responds to Justin Tobler's review of the `commit_ref_transaction()` helper introduced to fix tag handling in batched reference updates. Justin identified a control flow issue where the return value from the new helper wasn't being checked in `do_fetch()`, potentially allowing execution to continue after failed operations where the original code would have aborted. Karthik acknowledges the oversight and confirms the fix will be added.

This is a focused response to a specific review comment in the ongoing thread about transaction handling in `git fetch`. The core regression fix (non-conflicting tags failing when other conflicts exist) remains sound, but this follow-up ensures proper error handling matches the original behavior where failed transactions should abort the operation. The exchange demonstrates careful review catching an edge case in the control flow that wasn't covered by the initial test cases.

Key technical points:
- Missing return value check for `commit_ref_transaction()` in `do_fetch()`
- Original behavior aborted on transaction failures via `goto cleanup`
- New helper maintains same error semantics when properly checked
- Fix preserves atomic/non-atomic fetch modes' error handling
- Transaction cleanup patterns remain consistent with NULL assignment to prevent double-free

The thread continues to refine the solution while maintaining all previously established requirements around batched updates, backfill behavior, and transaction management. This iteration addresses one of the architectural concerns raised earlier about distributed responsibility for transaction commitment.