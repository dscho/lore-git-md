This is a discussion about the potential removal of `git whatchanged`, with Kristoffer Haugsbakk responding to Franz Brauße's defense of the command. 

Kristoffer clarifies that `git whatchanged` is being removed because its functionality has long been available through `git log` with different defaults. They provide a direct translation guide: `git whatchanged <opts>` can be replaced with `git log <opts> --no-merges --raw`, and suggest alternatives like `--stat` or `--name-only` for better human readability.

The response tactfully addresses Franz's "vote" comment by noting it wasn't intended as a formal voting mechanism, while acknowledging how it could be interpreted that way. Kristoffer points to Git's BreakingChanges documentation as context for such decisions.

This continues the thread's core tension between maintaining legacy commands for established workflows versus simplifying Git's interface by removing redundant functionality. The exchange remains constructive, with Kristoffer offering practical migration advice rather than just asserting the removal.