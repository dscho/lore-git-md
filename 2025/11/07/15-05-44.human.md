This is a refactoring patch in the submodule gitdir path encoding series, which is now in its final stages with broad consensus among key contributors. The patch updates `submodule--helper` to use the centralized `submodule_name_to_gitdir()` helper function rather than hardcoding the modules path, bringing it in line with other parts of the codebase that handle submodule gitdirs.

The change fixes test failures that arose from the hardcoded path not accounting for the new encoding scheme. The patch also tightens variable scoping by moving the `submod_gitdir_path` declaration inside the conditional block where it's actually used, and switches to using a `strbuf` in the else branch for better memory management.

This appears to be a straightforward cleanup that properly integrates the helper into one of the last remaining places that wasn't using it. The change is small (13 lines modified) and focused, with no apparent controversy given the established consensus around the encoding approach. As part of the final v4 series, it represents a minor but necessary adjustment to complete the implementation.