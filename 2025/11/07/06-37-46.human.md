This is a follow-up in the ongoing bug report about `git cherry-pick` silently ignoring patch application errors that `git apply` would catch. Johannes Sixt asks a clarifying question to help diagnose the issue: whether using `git apply --3way` produces the same behavior as `cherry-pick` (silent success) or fails like `apply --verbose`.

The question suggests that the difference in behavior might relate to whether the commands use three-way merging when applying patches. This is a useful diagnostic step since `cherry-pick` internally uses three-way merge machinery, while plain `git apply` does not unless explicitly requested with `--3way`.

The response is brief and technical, focused on narrowing down where exactly the discrepancy occurs in the patch application pipeline. It doesn't propose any solutions yet but moves the investigation forward by testing a specific flag that might explain the behavioral difference. The thread remains in diagnostic mode, with the original issue still unaddressed but better understood.