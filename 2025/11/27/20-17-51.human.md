This is a feature addition to the `git replay` command, specifically addressing the ability to perform selective reverts of non-contiguous commits. The email confirms that the current implementation already supports reverting specific commits (like commit1 and commit4 from a merged topic) by leveraging Git's standard revision range syntax, similar to how `git log` works.

The discussion highlights that this capability was somewhat unexpected but welcome, as it satisfies a key workflow need: the ability to selectively undo problematic commits from a merged branch without having to revert the entire range. This maintains the tool's usefulness for both bulk operations and precise, audit-friendly reverts.

The technical approach uses Git's existing rev-list machinery, allowing users to specify disconnected commits directly in the command line (e.g., `git replay --revert <target> <commit1> <commit4>`). This builds on the established design principle of maintaining consistency with Git's standard parsing conventions.

The email shows the patch series is in its final polishing phase, with Junio Hamano confirming the implementation's flexibility while acknowledging the technical soundness of the approach. This appears to be a well-considered addition that addresses real-world use cases while staying true to Git's existing patterns.