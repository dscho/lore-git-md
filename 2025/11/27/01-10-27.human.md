This is a minor housekeeping patch in Ezekiel Newren's ongoing Rust infrastructure refactoring series. It's patch 5/13 in the effort to reorganize Git's Rust code into a Cargo workspace structure.

The patch makes two simple changes to support the new build system setup:
1. Adds `/generated/` to `.gitignore` to prevent generated files from being tracked
2. Updates the `rust-clean` target in the Makefile to also remove the `generated/` directory

This appears to be a straightforward cleanup change that follows from the workspace restructuring introduced in earlier patches. The changes are minimal and focused on keeping the build system tidy, though the commit message could be more explicit about why the `generated/` directory needs to be ignored (presumably it will contain build artifacts from the new workspace setup).

As with the rest of the series so far this is infrastructure-focused rather than touching any actual Rust code. The patch seems uncontroversial and well-contained, though it's unclear if there might be more generated directories that should also be ignored.