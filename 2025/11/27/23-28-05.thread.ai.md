Thread identity: Translation issue report about German localization inaccuracy in `git rev-parse --verify` error messages.

Core problem: The German translation of `git rev-parse --verify`'s error message incorrectly uses "Commit" which is too specific, while the command works with all object types (commits, blobs, trees, tags). Other languages (Spanish, French) correctly use generic terms equivalent to "revision".

Current status: Native German speaker Sören Krecker has confirmed that "Revision" is the correct translation, resolving the open question from the initial report. The thread is ready for a patch updating the German translation in `builtin/rev-parse.c`.

Key technical details: The error message appears when object verification fails. The correction changes the German text from "Commit" to "Revision" to properly reflect that the command handles all Git object types, not just commits. The message appears in `builtin/rev-parse.c`.

Open questions: None remaining - the linguistic validation requested in the original report has been provided. The thread awaits a patch implementing the confirmed change.

Participant positions: brian m. carlson originally reported the issue with cross-language analysis. Sören Krecker (native German speaker) confirmed "Revision" is correct, providing the needed linguistic validation. No patch has been submitted yet.

Related work: The confirmation aligns with existing German translations of related terms ("Revisionsgang", "Revisionsliste") and matches how Spanish/French handle this case. The thread can now proceed to implementation.