This is a small refactoring patch in Ezekiel Newren's Rust infrastructure series, changing the type `unsigned char` to `uint8_t` in varint.h's function declarations. The change appears to be part of the broader Rust integration work, likely preparing the C headers for better compatibility with Rust's type system where `uint8_t` is the standard fixed-width type rather than `unsigned char`.

The patch is minimal (just 2 lines changed) and straightforward, modifying the parameter types in the varint encoding/decoding interface. This type change is consistent with Rust's FFI best practices and may help avoid potential portability issues across language boundaries. The commit message is very brief, not explaining the motivation beyond the technical change itself.

As part of the larger series, this seems to be a preparatory change for the Rust FFI work, though its direct connection to the workspace/crate restructuring isn't immediately clear from the commit message. The change is simple enough that it's unlikely to be controversial, though reviewers might appreciate a brief note about why this change is being made now as part of the Rust effort.