This is Junio Hamano's response confirming that Siddharth Asthana's implementation of `git replay --revert` already supports selective, non-contiguous commit reversal through Git's standard revision range syntax. The key technical point is that the implementation leverages `setup_revisions()` parsing before `prepare_revilimit_list()` processes the range, allowing users to specify individual commits (like `<commit1> <commit4>`) rather than requiring contiguous ranges. This satisfies Junio's workflow requirement #2 from earlier discussion - the ability to revert specific faulty commits from a merged topic branch. The exchange demonstrates the design's flexibility in handling complex use cases while maintaining consistency with Git's existing revision parsing conventions. No new functionality is proposed here; this is confirmation that the current implementation meets previously identified needs.