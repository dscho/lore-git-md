CI / build system patch in Ezekiel Newren's Rust workspace refactoring series (11/13). This small but important change standardizes the Rust build flag names between Make and Meson build systems in GitHub CI workflows. The patch changes `ci/run-build-and-tests.sh` to use `-Dwith_rust=enabled` (matching Meson convention) instead of `-Drust=enabled`, and `WITH_RUST=true` (standard boolean) instead of `WITH_RUST=YesPlease` (Git legacy style). 

This appears to be a cleanup step in preparation for the multi-crate Rust build system, ensuring consistent parameter naming across build systems. The change is mechanical and focused on CI configuration, though it doesn't yet address the known GitHub workflow breakages mentioned in earlier thread context. The patch follows the series' pattern of incremental infrastructure changes, though it's unclear if this fully resolves any of the open CI issues or just aligns naming conventions.