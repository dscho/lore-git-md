SZEDER Gábor raises concerns about the patch adding short option completion to Git's bash completion script. While the technical implementation (adding `-C`, `-P`, `-c`, `-h`, `-p`, `-v` to `__git_main()`) is straightforward, SZEDER questions the fundamental value of completing short options at all. 

The critique focuses on two points: first, that short option completion doesn't meaningfully save keystrokes (since you'd still need to type the `-<letter>` prefix), and second, that short options lack discoverability value because their single-letter forms don't indicate their purpose (SZEDER notes they didn't even know `-p` and `-P` existed before seeing this patch). 

This pushes back against Junio's earlier position that the patch was ready to queue pending only the open question about whether `-<TAB>` should complete both short and long options. SZEDER suggests the patch needs stronger justification for why this feature is worth adding at all, marking the first substantive objection in the thread. The discussion now appears to be shifting from implementation details to whether the feature itself is desirable.