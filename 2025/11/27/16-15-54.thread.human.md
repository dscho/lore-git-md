This thread has reached a clear resolution regarding changes to the experimental `git replay` command's handling of empty commits. The discussion started with Phillip Wood proposing to make `git replay` drop commits that become empty during replay (when their changes already exist in the target branch), matching the behavior of `git rebase` and `git cherry-pick` with similar options. The implementation compares tree OIDs to detect empty commits while preserving originally-empty commits.

After thorough technical review from Junio Hamano and Elijah Newren, the v3 patch now incorporates all feedback, including improved commit message punctuation and test case refinements. The core behavior - dropping commits that become empty during replay - has received maintainer approval, with the implementation details (tree comparison logic and branch pointer updates) confirmed as correct. While the command remains experimental, this change aligns it more closely with established Git behaviors.

The only remaining consideration is whether to eventually add an option to retain empty commits, but this is deferred as the current behavior matches other Git commands. With all technical questions resolved and the patch polished, this change appears ready for inclusion in Phillip Wood's `ps/history` branch. The thread demonstrates how even experimental features benefit from the Git community's rigorous review process before stabilization.