This thread focuses on refining the behavior of the experimental `git replay` command to drop commits that become empty during replay operations, matching the behavior of similar commands like `git rebase` and `git cherry-pick` with their `--empty=drop` options. The key change involves modifying `replay.c` to detect when a commit's changes are already present in the target branch by comparing tree OIDs, while preserving commits that were originally empty.

The discussion has reached technical consensus, with maintainer Junio Hamano endorsing the approach as sensible and Elijah Newren confirming the implementation matches his own work. Phillip Wood, the patch author, has incorporated all feedback suggestions, including test simplifications from Elijah and commit message improvements. The tests in t3650 now directly verify branch states rather than using sed reconstructions, and properly isolate test cases with `test_when_finished`.

While the patch remains in RFC status, there are no outstanding technical objections. The only remaining question is whether the next version should be based on `master` or Phillip's `ps/history` branch. The implementation details are fully resolved, including the tree comparison logic, branch pointer updates for dropped commits, and test coverage. The change is considered justified given `git replay`'s experimental status, though the commit message leaves room for potentially adding an option to retain empty commits in the future if user feedback indicates it's needed.