Feature addition to `git replay` command, specifically addressing the revert operation's interface design. Phillip Wood responds to Siddharth Asthana's example usage of `--revert` with a more general case requirement for the command's interface. The key point is that reverting arbitrary commit ranges (not just linear sequences) necessitates specifying both a target branch to update and the commit range to reverse, as shown in Phillip's example `git replay --revert main main..feature`.

This email advances the interface discussion by:
- Clarifying that `--revert` needs dual specification (target branch + commit range)
- Providing concrete syntax for reverting non-linear commit ranges from merged branches
- Implicitly reinforcing the earlier consensus that revert is a distinct operation mode

The example demonstrates how the command would handle reverting all commits introduced by a feature branch onto main, which aligns with the thread's established design principles while expanding the practical use case coverage. This input will likely influence final documentation and test case development for the feature.