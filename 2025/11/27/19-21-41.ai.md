This is a feature addition to the `git replay` command's revert capability, addressing selective commit reversal workflows. Siddharth Asthana confirms that the current implementation already supports reverting non-contiguous commits by leveraging Git's standard revision range parsing, similar to `git log`. He demonstrates this with the example syntax `git replay --revert <target> <commit1> <commit4>` for selectively reverting specific commits from a merged branch.

The email highlights two key points about the implementation:
1. The command inherits `git log`'s flexibility in handling disconnected commits, avoiding the need for multiple single-commit operations
2. Siddharth plans to add tests and documentation to verify and publicize this capability

This response directly addresses Junio Hamano's concern about supporting selective commit reverts (workflow #2 from previous discussion) while maintaining the design's consistency with Git's existing revision parsing behavior. The implementation appears technically sound, building on established patterns from `git log` while extending `git replay`'s functionality for audit-focused revert scenarios. The planned test addition will help validate edge cases in non-linear history handling.