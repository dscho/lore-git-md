This is a design refinement in the ongoing `git replay` revert capability discussion. Phillip Wood clarifies the intended interface for reverting arbitrary commit ranges, suggesting that `--revert` should take both a target branch name and a commit range. He provides the example `git replay --revert main main..feature` to show how users would specify which branch to update (main) and which commits to reverse (those in feature but not in main).

The note builds on earlier consensus that revert is a distinct operation mode, but adds the practical consideration that users need to explicitly name both the destination branch and the source range. This aligns with the thread's focus on making the interface both powerful and unambiguous, though it introduces a new syntax question about how to cleanly specify these two pieces of information.

Phillip's input continues the pattern of experienced contributors helping shape the final design through concrete use case examples, though the email doesn't propose specific implementation changes. The suggestion appears consistent with the technical direction established in earlier discussion, focusing on making the revert operation flexible enough for real-world scenarios like undoing merged feature branches.