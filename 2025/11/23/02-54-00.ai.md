This is a discussion about branch handling strategy in the `git-history` feature series, specifically addressing a potential pitfall in the current implementation.

Elijah Newren clarifies that his earlier feedback was not about feature requests but about addressing bugs and design limitations in the current single-branch approach. The key issue is that the implementation doesn't verify whether a commit being modified exists on multiple branches, which could silently cause branch divergence. He argues this is a bug that should be addressed before merging, not a deferred enhancement.

Three solutions are proposed: (1) document the limitation with warnings, (2) detect multi-branch cases and error out (with an optional override flag), or (3) automatically rewrite all affected branches. Elijah strongly advocates for option 3, noting it would actually simplify the code (replacing ~12 lines while removing more complex logic) and better serve users. He offers to implement this if others agree, though he's willing to help with alternative solutions if the maintainers prefer a more conservative approach.

The email highlights a tension between Junio Hamano's preference for minimal experimental merging and Elijah's concern about merging code with known behavioral pitfalls. Elijah emphasizes this isn't about feature completeness but about avoiding a situation where problematic behavior becomes entrenched (drawing parallels to the `git-switch`/`git-restore` history). The discussion remains focused on how to handle commits that exist in multiple branch histories, with Elijah providing concrete implementation alternatives to address the issue.