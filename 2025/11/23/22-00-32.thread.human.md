Here's a human-readable thread summary:

The thread discusses the recent deprecation of `git whatchanged`, which now requires users to explicitly opt-in with a `--i-still-use-this` flag to continue using the command. Daniel Hammer has raised concerns about this approach, arguing it disrupts his daily workflow unnecessarily. He suggests the command should remain available with updated documentation marking it as legacy, rather than forcing users through an opt-in mechanism. The email characterizes this as a developer experience regression.

The discussion touches on the command's utility as a familiar shorthand (functionally similar to certain `git log` invocations) and the friction introduced by the new requirement. While not proposing specific technical alternatives, the message clearly opposes the current deprecation strategy and may prompt reconsideration of how Git handles backward compatibility for legacy commands. The tone indicates this change has affected real-world workflows, suggesting the thread could explore balancing modernization with user expectations.