This is patch 18/19 in Patrick Steinhardt's object database streaming interface refactoring series, focusing on code organization. The patch moves the streaming implementation files (`streaming.{c,h}`) into a new `odb/` subdirectory to better reflect their subsystem affiliation.

The change is purely organizational - renaming and relocating files while updating all include paths across 15 files (Makefile, meson.build, and various source files that use the streaming interface). The diff shows straightforward path updates with no functional changes, just mechanical adjustments to point to the new location.

This continues the architectural cleanup from previous patches by giving the streaming code a more logical home within the object database subsystem. The move follows the pattern established earlier in the series where streaming functionality was reorganized around ODB concepts rather than being a standalone feature. All callers are updated consistently, maintaining the same API contracts established in patch 17/19's renaming of core functions to `odb_read_stream_*`.

Key technical details for future reference:
- Files moved: `streaming.c` -> `odb/streaming.c`, `streaming.h` -> `odb/streaming.h`
- Build system updates: Makefile and meson.build adjusted to reflect new paths
- 14 include directives updated across archive, builtin, and core object handling code
- No API or behavior changes - purely a code organization improvement
- Maintains all type-safe stream containers and backend-specific implementations introduced earlier in series