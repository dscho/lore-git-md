This is a user feedback email regarding the deprecation of `git whatchanged`. The author Daniel Hammer objects to the current deprecation approach that requires users to explicitly opt-in with a `--i-still-use-this` flag to continue using the command.

The email notes that `git whatchanged` is part of the author's daily workflow, and argues that simply updating the documentation to clarify its status as a legacy command would be preferable to the current forced opt-in mechanism. The author characterizes the current approach as a "DevEx regression" (developer experience regression) that should be reversed.

The email references the command's documentation, suggesting it serves as shorthand for another Git operation (likely referring to its similarity to `git log` with certain default flags). However, the author finds the alternative command "counter-intuitive" compared to `whatchanged`.

This appears to be pushback against a recently implemented deprecation warning system for `git whatchanged`. The tone suggests frustration with the breaking change to a familiar workflow, though the email doesn't propose specific technical solutions beyond reverting the opt-in requirement. The message could spark discussion about deprecation strategies and backward compatibility in Git.