Thread identity: Discussion about deprecation strategy for `git whatchanged`, a legacy command that shows commit history with diff output.

Core problem: Recent change introduced a `--i-still-use-this` opt-in flag requirement for `git whatchanged`, making it harder to use. Daniel Hammer argues this creates unnecessary friction for users who rely on this command in their daily workflow.

Current status: The command is marked as deprecated in documentation but remains functional if users explicitly opt-in with the new flag. No version changes mentioned in this email.

Key technical details:
- `git whatchanged` provides similar functionality to `git log -p` with certain default flags
- Current implementation requires `--i-still-use-this` flag to bypass deprecation warning
- Documentation already notes its legacy status

Open questions:
- Should the deprecation strategy be reverted to just documentation warnings?
- Is the forced opt-in mechanism too aggressive for this case?
- What is the right balance between cleaning up legacy commands and maintaining user workflows?

Participant positions:
- Daniel Hammer: Strongly objects to current approach, calls it a "DevEx regression", prefers documentation-only deprecation

Related work:
- Original deprecation discussion/implementation not referenced in this email
- Similar discussions may exist around other legacy commands like `git pull --rebase` warnings