This is a user complaint about the deprecation process for `git whatchanged`. Daniel Hammer objects to the current approach of requiring users to explicitly opt-in with `--i-still-use-this` to continue using the command, calling it "unnecessarily heavy handed" compared to simply documenting the command's deprecated status.

The email notes that the author uses `git whatchanged` frequently (multiple times daily) and views it as a useful shorthand, though they acknowledge it's essentially just an alias for another Git operation. The tone suggests frustration with what they perceive as a developer experience regression, requesting the deprecation be reversed.

The message doesn't propose specific technical changes but rather voices opposition to the current deprecation strategy. It appears to be responding to an existing thread about removing `git whatchanged`, though no prior thread context was provided in the input. The stance is clearly against removal, though the argument focuses more on process (the opt-in requirement) than technical merits.