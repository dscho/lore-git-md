This is a technical discussion about the behavior of three-way merges in Git, specifically around `git cherry-pick` and `git apply --3way` versus direct patch application.

Johannes Sixt responds to Bhavik Bavishi's confirmation that both `git cherry-pick` and `git apply --3way` produce identical (and seemingly correct) results for a problematic case where direct `git apply` fails. The discussion has moved from identifying the behavior difference to understanding why it occurs.

Sixt explains that three-way merge operations may omit changes when the merge strategy determines the change was already applied independently, using a concrete example with repeated text blocks where context matching can be ambiguous. This suggests the current behavior may be intentional merge strategy logic rather than a bug.

The thread remains open on whether this silent success in three-way mode is the correct behavior or if there should be some notification when the merge resorts to three-way resolution. No maintainer input has been provided yet on the design intent behind this behavior. The discussion is technically detailed but remains exploratory rather than conclusive at this stage.