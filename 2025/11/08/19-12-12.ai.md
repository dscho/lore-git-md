This is a technical follow-up in the ongoing bug investigation about `git diff` exit codes with specific flag combinations. D. Ben Knoble adds new observations about potential contributing factors to the previously identified issue where `--quiet` incorrectly reports no changes when used with `--cached`, `--no-ext-diff`, and `--find-copies-harder`.

The email highlights two key technical points: First, the interaction between `diff_setup_done` and `diff_from_contents` when external diffs are disabled (`--no-ext-diff`), suggesting this might be another code path affecting the bug's behavior. Second, the observation that this involves two of the three problematic flags (quiet and ext-diff), though the exact mechanism remains unclear.

This builds on the thread's established understanding that the bug stems from how the diff queue processing diverges between `--quiet` and `--exit-code` modes when rename detection is active. The new information suggests the external diff configuration path may also play a role in the incorrect behavior, though the author hasn't yet traced the full interaction with all three flags. The technical nature of the analysis indicates this is a subtle interaction in the diff machinery's internals that will require careful debugging to resolve properly.