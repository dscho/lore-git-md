This is a discussion response in the thread about adding a 'strip-if-invalid' mode to `git fast-import`'s `--signed-commits` option. Elijah Newren is addressing Junio C Hamano's concerns about the fundamental assumptions of signature validation in the context of history rewriting.

The response provides additional motivation for the feature, explaining that `git filter-repo` users often want to rewrite only recent history while preserving older signatures, but current behavior forces full rewriting when signatures are involved. Newren outlines the technical constraints in the export/import pipeline that make signature validation challenging, with fast-import being the only point where validity can be determined.

Newren acknowledges the maintainer's concerns about key availability and verification accuracy, suggesting that requiring all public keys might be a limitation but questioning whether there are viable alternatives. He proposes one potential solution - modifying fast-export to include unsigned commit OIDs for comparison - while asking if other approaches exist. The response maintains a constructive tone while defending the feature's utility for common use cases.

This email advances the discussion by providing concrete use cases and technical context, but doesn't yet resolve the fundamental concerns about signature validity in rewritten history. The proposed alternative (unsigned OID tracking) would require format changes to fast-export/import and may not address all verification scenarios. The thread remains open on whether the current approach can be salvaged or needs redesign.