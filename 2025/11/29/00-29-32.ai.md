Bugfix investigation for ARM64-specific `git fetch` SIGSEGV during submodule operations. Justin Su provides additional reproduction details, isolating the crash to two specific config options: `fetch.writeCommitGraph=true` and `submodule.recurse=true`. The crash occurs even with a fresh clone on a new EC2 instance, suggesting this is not related to repository corruption (contradicting earlier findings from Brian m. carlson's analysis of the diagnostic snapshot).

This email advances the investigation by clearly identifying the triggering configuration, which points toward a potential race condition or memory corruption in the interaction between commit-graph writing and submodule recursion during fetch operations. The fresh clone reproduction eliminates repository corruption as the root cause, shifting focus to platform-specific behavior (ARM64) or a timing-sensitive bug in the commit-graph cleanup path. The consistent reproduction on new infrastructure strengthens the case that this is a genuine bug rather than a one-off corruption issue.

Key technical details include the narrowed configuration (`fetch.writeCommitGraph` combined with `submodule.recurse`) and the use of fresh clones to eliminate corruption variables. This moves the discussion toward potential race conditions in commit-graph lock handling during submodule operations, particularly on ARM64 architecture. The thread remains unresolved but now has a clear reproduction path for further debugging.