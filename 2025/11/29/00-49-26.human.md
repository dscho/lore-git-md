Bugfix follow-up: SIGSEGV in `git fetch` with submodules and commit-graph writing appears resolved in Git 2.52.0.

Justin Su reports that after upgrading to Git 2.52.0 (from the Ubuntu PPA), they can no longer reproduce the segmentation fault that previously affected ARM64 systems when running `git fetch` with both `fetch.writeCommitGraph=true` and `submodule.recurse=true` enabled. This suggests the issue was fixed between versions 2.43.0 and 2.52.0, though the specific fix isn't identified in this brief message.

The original bug involved a platform-specific crash that occurred during commit-graph cleanup after submodule operations, which had been narrowed down to a reproducible test case involving fresh clones with those two config options. While this email doesn't provide analysis of what changed in 2.52.0 to fix the issue, it serves as valuable confirmation that whatever underlying race condition or memory handling problem existed has been addressed in the newer version.

This appears to close the investigation thread, as the reporter confirms the original problematic behavior is no longer present with the updated Git version. The resolution suggests the bug was likely fixed as part of general commit-graph or submodule handling improvements in the intervening releases, though the specific commit that addressed it remains unidentified in this correspondence.