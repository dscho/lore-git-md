Documentation patch fixing inaccuracies in the `git-replay` manual page regarding revision ranges. 

Elijah Newren corrects several issues in the `git-replay` documentation that incorrectly suggested the command could accept multiple revision ranges. The patch clarifies that `git-replay` follows the standard Git convention of taking a single revision range (with exceptions clearly noted when they exist). The changes remove misleading references to "Commit Limiting options" that don't exist on the page and clean up the description of how revision ranges work with the `--advance` option.

The commit message provides useful historical background, noting when these inaccuracies were introduced in previous patch series revisions and linking to relevant discussion threads. It also acknowledges that this documents current behavior rather than ideal behavior, referencing Junio Hamano's past suggestions for potential improvements to revision walking in `git-replay`.

The patch appears straightforward and well-justified, with clear explanations of each change. It's marked as conflicting with another pending documentation patch from Phillip Wood, with Newren offering to resubmit after that patch lands if preferred. The changes are limited to documentation and one corresponding usage message update in the code.