This is part 2/10 of the hook subsystem refactoring series, introducing stdin handling via callback for Git hooks. The patch adds a `feed_pipe_fn` callback mechanism to `struct run_hooks_opt` as an alternative to the existing file-based stdin feeding (`path_to_stdin`), with strict BUG() assertions ensuring the two methods remain mutually exclusive.

Key technical aspects:
- New `feed_pipe` callback and associated context pointers (`feed_pipe_ctx` and `feed_pipe_cb_data`) in `hook.h` for streaming stdin without filesystem overhead
- Pipe initialization handled by `start_command()` while maintaining the existing hook execution flow
- Documentation comments explain the callback's purpose for large data streams and internal access patterns
- Author notes a minor comment inconsistency to fix in v4 and hints at potential future API simplification based on parallel execution work

The change builds on the parallel stdin infrastructure from part 1, with the implementation showing careful attention to memory safety and API boundaries. The patch appears well-integrated with the series' overall goal of standardizing hook execution while enabling future optimizations. No technical concerns are raised beyond the noted documentation fix, and the change has already received sign-offs from multiple contributors including Ævar Arnfjörð Bjarmason.