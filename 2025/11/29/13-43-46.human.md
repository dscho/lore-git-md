This is a bugfix for the `git last-modified` command's behavior in sparse checkouts. The patch addresses an issue where the command couldn't properly handle paths outside the sparse checkout area, even though the documentation suggests this should work using the `--` separator syntax.

The fix is straightforward - it adds the `PARSE_OPT_KEEP_DASHDASH` flag to the option parsing in `builtin/last-modified.c`, which ensures the command properly handles the `--` separator. A new test case is added to verify the behavior works correctly in sparse checkouts.

The change is small (just one line of code changed) but meaningful for users working with sparse checkouts who need to query modification times of paths outside their working directory. The patch appears well-constructed with a clear commit message linking to the original bug report and includes appropriate test coverage. This seems like a low-risk, high-value fix that aligns with documented behavior.