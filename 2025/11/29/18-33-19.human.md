This is a documentation patch adding missing config option documentation, part of a series improving `scalar` configuration visibility.

The patch adds documentation for the `gui.GCWarning` config option, which controls whether `git-gui` prompts users about garbage collection when many loose objects are detected. The default is "true". The author noticed this was undocumented while investigating `scalar`'s configuration behavior.

This is the second patch in a v2 series that primarily focuses on making `scalar`'s configuration more transparent. While this particular change is small (5 lines added to `gui.adoc`), it completes the series' goal by documenting a previously missing option that `scalar` interacts with. The implementation is straightforward and uncontroversial, matching the style of surrounding documentation.

The patch appears well-formed with a clear commit message and proper sign-off. As with the rest of the series, this seems like a clear improvement to Git's documentation with no apparent downsides. Readers interested in `scalar` or Git's configuration system would benefit from reviewing the full series.