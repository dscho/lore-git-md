This is a technical clarification in the ongoing `git-last-modified` bitmap initialization bugfix thread. Jeff King acknowledges Anders Kaseorg's correction about proper pointer size calculation syntax in C. King had previously suggested using `sizeof(lm->scratch->words)` for the bitmap clearing operation, but Kaseorg pointed out this would give the pointer size rather than the element size. 

King agrees with Kaseorg's suggested alternatives (`sizeof *lm->scratch->words` or `sizeof lm->scratch->words[0]`) that correctly calculate the element size. He notes his general preference for the pointer-dereference style (`sizeof *var`) as it automatically stays correct if the variable's type changes, though acknowledges the potential for forgetting the dereference operator. This is a minor stylistic discussion that doesn't affect the already-approved patch, but clarifies best practices for similar cases. The exchange shows the thread moving from bugfix implementation to broader code hygiene considerations, with all technical points now resolved.