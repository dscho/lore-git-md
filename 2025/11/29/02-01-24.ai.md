Bugfix for `git-last-modified` bitmap initialization, now marked for backport to maint. This is a follow-up to the earlier discussion about incorrect memory initialization in the bitmap handling code of `builtin/last-modified.c`. Junio C Hamano confirms the fix (changing `memset` to account for `eword_t` size) is correct and notes the bug dates back to v2.52.0, making it suitable for the maint branch. The patch remains unchanged from Toon Claes' original submission, which was already reviewed by Jeff King and confirmed to resolve the s390x test failures (though the issue was not endian-specific, just more visible on big-endian systems). No new technical discussion is added - this is effectively the maintainer's approval stamp on the already-agreed solution. The thread appears resolved with this acknowledgement.