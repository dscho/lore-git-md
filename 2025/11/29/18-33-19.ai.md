Documentation patch adding missing config option documentation for `gui.GCWarning`. This is the second patch in a two-part series focused on improving `scalar` configuration visibility, though this particular change documents a `git-gui` setting rather than a `scalar` one.

The patch adds documentation for the `gui.GCWarning` config option, which controls whether `git-gui` should prompt users about garbage collection when many loose objects are detected. The default value is noted as "true". The change is minimal (5 lines added to `Documentation/config/gui.adoc`) and appears straightforward, though it's worth noting this is somewhat tangential to the series' stated focus on `scalar` configuration.

Key details for future thread context:
- Documents previously undocumented `gui.GCWarning` config setting
- Affects `Documentation/config/gui.adoc`
- Default value is "true"
- Controls GC warning behavior in `git-gui`
- Patch motivation comes from author's `scalar` config investigation
- No behavioral changes - purely documentation

The patch seems technically sound but stands out as slightly off-topic for a series otherwise focused on `scalar`. The connection to the series' theme could be clearer in the commit message.