This is a technical discussion about the `git replay --revert` feature's handling of commit ranges, specifically addressing how disconnected commit sets should be documented and supported.

Junio Hamano responds to Elijah Newren's point about the current `git replay` documentation stating it accepts "any range expression." Junio clarifies that by definition, Git's revision walker (`prepare_revision_walk()`) only handles connected commit sets, though it can process useful expressions like `master..topic1 topic2` or `^master topic1 topic2` that don't require disjoint sets. 

He notes that while Git has survived without native disjoint set support in `git log` (except via `--no-walk` for individual commits), the documentation should be updated if they later extend the syntax to handle truly disconnected commits. The discussion reflects ongoing careful consideration of how to balance current limitations with future extensibility in Git's revision parsing architecture.

This email continues the thread's pattern of thoughtful technical debate about edge cases and documentation precision, with Junio providing his characteristic maintainer's perspective on how to properly frame the command's current capabilities while leaving room for future enhancements.