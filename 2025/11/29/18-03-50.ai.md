This is a brief reply in the ongoing documentation patch series about `scalar` configuration discoverability. Kristoffer Haugsbakk points out that both patches in Matthew Hughes and Derrick Stolee's v2 series are missing the required `Signed-off-by:` trailer in the commit messages. This is a standard contributor requirement for Git project patches, so the oversight would need to be corrected before the series could be merged.

The reply doesn't challenge the technical content of the patches (which document `gui.GCWarning` and add config change comments in `scalar.c`), but flags this procedural issue. Since this is the first review comment on the thread, it may prompt the authors to resend with the signoffs added. The thread context shows this is the second iteration of the series, so the missing signoffs appear to be an oversight in the v2 submission rather than a new issue.