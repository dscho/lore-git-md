This is a technical discussion about the output format for Git's proposed `--raw-extended` diff attribute reporting feature. Justin Tobler responds to Ben Knoble's feedback about whether to use single-line or multi-line output for reporting file metadata like binary status and CRLF attributes.

The email shows Justin reconsidering his earlier multi-line proposal and now leaning toward a single-line format where attributes appear as key=value pairs appended to the existing `--raw` output (e.g., `binary=yy,crlf=nn`). He acknowledges Ben's point about line-oriented tools while maintaining that parsers should handle either approach similarly. The example demonstrates how the feature would look when reporting both binary` and `crlf` attributes.

This continues the thread's progression toward a concrete implementation that balances human readability with machine parseability. Justin's position appears flexible as he incorporates feedback, though the discussion remains open on whether key=value pairs or positional codes would be better for scripting. The email doesn't introduce major design changes but shows incremental refinement of the output format based on peer input.