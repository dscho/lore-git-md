Maintainer guidance on diff algorithm interface design. This email from Junio C Hamano provides final direction on how to handle the `--minimal`/`--no-minimal` flags in the context of the new `--diff-algorithm` option for `git blame`. 

Junio agrees with Phillip Wood's observation about the current behavior where `--no-minimal` acts like `--diff-algorithm=myers`, noting this is consistent with the existing two-algorithm world. He clarifies that while deprecating `--no-minimal` makes sense long-term (as boolean toggles don't work well with multiple algorithms), that's separate from the current patch series. 

For the immediate implementation, Junio confirms:
1. The current `--no-minimal` behavior is acceptable
2. Phillip's suggestion to mark `--minimal` as OPT_HIDDEN is good
3. The focus should remain on the new `--diff-algorithm` interface

This provides the final maintainer approval needed for the series, with only minor implementation polish remaining (OPT_HIDDEN flag and test script optimizations). The email also establishes design principles for future algorithm selection interfaces, favoring explicit algorithm names over boolean toggles. The thread has now reached full consensus on both technical implementation and interface design direction.