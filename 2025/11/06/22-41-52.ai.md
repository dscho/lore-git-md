Feature patch series finalizing configurable diff algorithm support for `git blame`, now at version 5 with all review feedback implemented. This two-patch series (1) refactors xdiff internals to properly handle algorithm selection flags, and (2) exposes the functionality through `--diff-algorithm` option and `diff.algorithm` config in `git blame`.

The v5 changes address the last remaining polish items from maintainer review:
- Marks `--minimal` as OPT_HIDDEN to avoid confusion with the new algorithm selection interface
- Simplifies test script by combining sed operations to reduce process count
- Removes redundant XDF_NEED_MINIMAL from bit operations since it's now part of XDF_DIFF_ALGORITHM_MASK
- Maintains all existing test coverage while optimizing implementation

Key technical details:
- Patch 1 (xdiff refactoring) adds XDF_NEED_MINIMAL to XDF_DIFF_ALGORITHM_MASK, making DIFF_XDL_CLR macro redundant but keeping documentation
- Patch 2 (blame feature) implements the user-facing interface with:
  * `--diff-algorithm` option supporting myers/minimal/patience/histogram
  * Config precedence matching git-diff behavior
  * Hidden `--minimal` flag for backward compatibility
  * Comprehensive tests in t8015-blame-diff-algorithm.sh covering:
    - Default behavior
    - Config/CLI precedence
    - Edge cases
    - Cross-algorithm comparisons

The series has completed all technical review cycles with consensus on implementation and maintainer approval. Junio's final guidance establishes `--diff-algorithm=<name>` as the canonical interface while accepting the current `--minimal` behavior as transitional. The xdiff changes are confirmed correct by Phillip Wood, and the blame implementation provides clean algorithm selection with thorough test coverage. This version addresses all final polish items and is ready for merge.