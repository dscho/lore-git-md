This is a maintainer guidance email in the ongoing thread about making the diff algorithm configurable in `git blame`. Junio C Hamano weighs in on the final design decisions regarding the `--minimal`/`--no-minimal` flag interactions with the new `--diff-algorithm` option.

Junio agrees with Phillip Wood's observation that `--no-minimal` currently behaves like `--diff-algorithm=myers` (which maintains backward compatibility) but suggests that in the long term, boolean flags like `--no-minimal` don't make sense in a multi-algorithm context. He clarifies this is future-looking guidance rather than a requirement for the current patch series.

For the immediate implementation, Junio is fine with keeping the current behavior where `--no-minimal` defaults to Myers algorithm, and supports Phillip's suggestion to mark `--minimal` as OPT_HIDDEN to avoid user confusion. The email confirms the technical approach is sound while providing maintainer perspective on the direction for diff algorithm interfaces.

This represents the final maintainer sign-off on the design decisions, leaving only minor implementation polish (like the OPT_HIDDEN change) before the feature can be merged. The thread has reached consensus on all major technical points after multiple review iterations.