This is a discussion about improving line removal tracking in `git blame --reverse`. Ben Knoble responds to Simon Richter's feature proposal with a practical workaround using `git log -S` (the pickaxe search).

The thread began with Simon identifying a gap in `git blame --reverse` - it shows when a line last appeared but not when/why it was removed, particularly problematic when the last appearance is a merge commit. Ben suggests using pickaxe search (`git log -S`) as an existing solution, demonstrating how to search for when a specific string disappeared from a file's history.

Ben's approach provides immediate utility while the more comprehensive blame enhancement is being discussed. The email shows there are already some tools available for this use case, though they require more manual effort than Simon's proposed integrated solution would. This keeps the conversation grounded in practical solutions while the feature proposal develops.