This is a bugfix for gitk's external diff functionality when handling renamed files, now at v8. The patch addresses Johannes Sixt's review comments from v7 by making three key improvements: (1) fixing rename detection to work when either the original or renamed file is selected (matching the commit message's claims), (2) replacing `string match` with more precise `string equal -length` checks for "rename from/to" lines, and (3) using `ne` instead of `!=` for string comparisons as suggested.

The implementation continues to parse rename information from gitk's diff text panel (via `difffilestart` and `ctext`), but now checks both the original and renamed filenames when determining matches. The `check_for_renames_in_diff` function scans for "rename from/to" lines 2-3 lines after each diff header, extracting paths only when the selected filename appears in either the source or destination. This maintains the architectural approach from v7 while fixing its key limitation.

The 40-line change (38 additions, 2 modifications) remains focused on gitk's Tcl script with no broader impact on Git core. The patch appears well-tested through eight iterations, with Tobias Boesch having addressed all technical feedback from the maintainer. The only outstanding item from previous discussions is the deferred non-ASCII filename handling, which isn't blocking for this change. The solution now robustly handles the core rename detection case while using more idiomatic Tcl comparisons.