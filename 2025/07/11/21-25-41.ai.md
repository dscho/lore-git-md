Maintainer ack for rebase state cleanup fix. Junio C Hamano responds to Øystein Walle's v2 patch addressing invalid `rebase.instructionFormat` handling during interactive rebase. The patch reorders operations in `do_interactive_rebase()` to validate the format via `sequencer_make_script()` before initializing rebase state, preventing repository from being left in a half-initialized state when format validation fails.

Hamano acknowledges the patch as a step forward despite being less comprehensive than Phillip Wood's proposed solution involving broader error handling changes. The maintainer notes this focused fix doesn't block future improvements and thanks Walle for the contribution. The technical approach remains unchanged from v1 - moving state initialization after format validation - with only sign-off and commit message fixes added in this revision.

Key technical details preserved: the operation reordering ensures clean failure before state initialization, and the test in t3415-rebase-autosquash.sh confirms proper cleanup of .git/rebase-merge on failure. The thread shows consensus on accepting this immediate fix while leaving Wood's more comprehensive error handling changes as future work. The test case remains valuable for regression testing regardless of implementation approach.