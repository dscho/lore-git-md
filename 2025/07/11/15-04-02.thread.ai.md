Bug report and discussion about path expansion and command parsing in Git's GPG configuration, focusing on `gpg.program` behavior. Original report showed inconsistent tilde expansion between `gpg.program` and other path settings. The thread progressed through a v2 series that implements tilde expansion via `git_config_pathname()` while maintaining the current limitation of not parsing command-line parameters.

Current status is a merged two-patch series plus this follow-up test addition:
1. Modified `gpg-interface.c` to use `git_config_pathname()` for `gpg.program` expansion
2. Added initial test coverage for relative paths and home-directory expansion
3. New follow-up patch adds comprehensive test cases verifying:
   - Relative path resolution (PATH lookup)
   - Home directory expansion (~ syntax)
   - Environment variable handling (HOME override)

Key technical details:
- Implementation in `gpg-interface.c` handles tilde expansion consistently via `git_config_pathname()`
- Documentation in `gpg.adoc` updated to specify pathname semantics
- Test suite now covers all scenarios: absolute paths, home-directory expansion (with HOME override), and PATH lookup (with modified PATH)
- Current behavior still treats entire value as single path (no argument parsing)
- Test cases carefully control environment variables and follow established patterns
- Follow-up tests acknowledge limitation of using HOME manipulation rather than true user creation

Thread evolution:
1. Initial bug showing tilde expansion inconsistency
2. Junio Hamano initially resisted change due to command-line argument concerns
3. Empirical demonstration showed current behavior fails both as path and command
4. v1 patch implemented path expansion while documenting limitations
5. v2 refines approach with better documentation and initial test coverage
6. Now merged with follow-up test addition completing verification

Open questions remain about future command-line argument handling, but the path expansion work is now fully implemented and verified. The test additions provide comprehensive coverage while maintaining the project's testing standards. The thread may continue to explore argument handling approaches separately from this now-complete path expansion work.