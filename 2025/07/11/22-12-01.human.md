This is a technical discussion follow-up in the HTTP protocol compliance thread about language tag handling. Carlo Arenas responds to Brian Carlson's examples supporting the minimal filtering approach for Accept-Language headers, agreeing that filtering just "C" and "POSIX" locales is sufficient despite seeming somewhat ad-hoc. 

He notes that while the solution might appear simplistic, it effectively prevents non-compliant locale values from being sent while avoiding over-engineering. Carlo also points out that consolidating the filtering logic (already present in the NO_GETTEXT code path) into a helper function presents opportunities for code modernization, suggesting potential use of strvec or hashtables for processing candidate values.

The email reinforces the thread's consensus toward practical solutions over comprehensive locale parsing, while hinting at possible implementation improvements in the upcoming patch revision. Carlo also CCs another contributor (Yi EungJun) to bring them into the discussion.