This is the final patch in a refactoring series that moves sparse-checkout configuration from global variables to the `repo_settings` struct, part of the ongoing `the_repository` removal effort. The email is a follow-up clarification to Junio Hamano about config validation behavior.

The author, Ayush Chandekar, corrects a previous misunderstanding about how `prepare_repo_settings()` handles invalid configuration values. Contrary to initial assumptions, the function does maintain Git's traditional fail-fast behavior for invalid configs rather than silently falling back to defaults. This addresses Junio's concern about potentially losing validation feedback for misconfigured sparse-checkout settings.

The exchange confirms that the series maintains existing error handling behavior while successfully eliminating the last global variable (`sparse_expect_files_outside_of_patterns`) from the sparse-checkout subsystem. With this clarification, the v5 series appears technically complete, having addressed all prior review feedback about initialization safety, performance, and now config validation behavior.

This wraps up a clean migration of sparse-checkout configuration into the repository-specific settings structure, removing another set of globals as part of the broader architectural effort. The discussion also helps establish patterns for handling similar configuration migrations in future `the_repository` removal work.