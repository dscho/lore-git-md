This is a refactoring patch that removes unused code as part of the `git for-each-ref` pagination series. The patch eliminates the `find_ref_entry()` function from the ref-cache implementation, which is no longer needed after the refactoring work in this series.

The change is straightforward - it removes both the function implementation from `refs/ref-cache.c` and its declaration from `refs/ref-cache.h`. The commit message notes this was identified as dead code during the pagination feature development.

This appears to be a clean removal with no impact on functionality, as confirmed by the test suite. The change helps maintain code hygiene by removing unused components that could otherwise confuse future maintainers. As part 2 of 4 in the series, it follows the foundational API exposure work and precedes the actual pagination implementation.