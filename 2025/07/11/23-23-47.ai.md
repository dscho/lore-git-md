Bugfix targeting GPG configuration path handling. This patch addresses the inconsistency where `gpg.program` didn't expand `~` to the user's home directory, unlike other Git path configurations. The change is minimal but impactful, switching from `git_config_string()` to `git_config_pathname()` in `gpg-interface.c` to enable tilde expansion. The documentation is updated to clarify that the setting expects a pathname rather than just a program name.

The patch directly responds to the reported issue where users couldn't specify GPG programs in their home directory using tilde shorthand. The fix maintains backward compatibility while adding the expected path expansion behavior. The change is localized to the GPG interface configuration parsing, with no apparent impact on other subsystems.

Key technical details: the modified `git_gpg_config()` function now uses pathname handling for the format program pointer, and the documentation drops the mention of `$PATH` since the setting now explicitly handles pathnames. The patch appears well-scoped and correctly implements the requested functionality without introducing obvious side effects. This seems likely to be non-controversial given the clear inconsistency it fixes.