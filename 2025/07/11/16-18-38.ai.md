This is a refactoring patch that removes unused code as part of the `git for-each-ref` pagination series. It eliminates the `find_ref_entry()` function from the ref-cache implementation, which was previously used to look up references in the files backend but is now obsolete.

The patch removes both the function implementation in `refs/ref-cache.c` and its declaration in `refs/ref-cache.h`, totaling 21 lines deleted. The removal is straightforward with no behavior changes, as the function was already unused in the codebase. This cleanup aligns with the series' broader refactoring of reference iteration logic, where the new pagination support (`--start-after`) uses more direct iterator-based approaches rather than the older lookup pattern.

Key technical details:
- Removes unused lookup function from files backend implementation
- Part of larger refactoring for pagination support
- No impact on functionality since function was already unused
- Clean removal of both declaration and header documentation
- Files touched: `refs/ref-cache.c`, `refs/ref-cache.h`
- No test changes needed since functionality was already unused

This is a straightforward cleanup that was reviewed and approved as part of the larger pagination series, with no outstanding discussion points or concerns noted in the thread.