This is a continuation of the discussion about deprecating `core.commentChar=auto`, now focusing on the practical implications of configuration scope handling in the deprecation warnings.

Phillip Wood responds to Junio C Hamano's questions about how the deprecation advice will handle different configuration scopes (system-wide vs user vs local). The key points are:

1. The deprecation warnings will recommend updating `commentChar` in the same scope where `auto` is currently set (system, user, or local config). Phillip raises a practical concern about write permissions - when `auto` is set in `/etc/gitconfig` but the user can't modify it, should the advice suggest overriding in `~/.gitconfig` instead?

2. Phillip confirms the plan to make Git die with an error when encountering `auto` post-3.0, rejecting Junio's earlier suggestion to silently fall back to `#`. This avoids potential confusion if commit messages contain `#` lines.

The discussion shows the implementation is nearly ready, with only minor policy decisions remaining about handling system config files where users lack write access. Phillip indicates he'll send an updated patch series soon. This email doesn't introduce new technical changes but clarifies the remaining open questions around the deprecation's user experience.