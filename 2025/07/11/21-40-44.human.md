Junio Hamano responds to Justin Tobler's concerns about the lightweight tag idempotency patch, marking the discussion as resolved. This is a brief follow-up in the ongoing thread about duplicate tag handling.

Junio addresses Justin's example workflow of `git tag T O && git push --tags`, noting it's not a scalable approach for real-world use since it doesn't properly handle multiple remotes or partial failures. He suggests `push --follow-tags` as the better alternative that automatically pushes relevant tags when their referenced commits are pushed.

While acknowledging Justin's point about losing feedback on whether a tag was actually created, Junio indicates he doesn't feel strongly about preserving this distinction. He concludes by marking the topic for the "trash bin" (likely meaning he considers the discussion complete and the patch acceptable as-is).

The exchange shows the maintainer's perspective that the automation use case Justin raised isn't compelling enough to block the simple idempotency change, especially given better alternatives like `--follow-tags`. This appears to be the thread's conclusion, with no further action expected on the feedback visibility concern.