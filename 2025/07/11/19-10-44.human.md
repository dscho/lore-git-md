Bugfix patch reintroducing lightweight tag idempotency, following up on the withdrawn proposal in the ongoing duplicate tags discussion. Junio Hamano proposes allowing `git tag` without `--force` when recreating a lightweight tag pointing to the same commit, making this case a true no-op. The change is implemented with a simple check in `builtin/tag.c` that exits early when the existing tag already points to the requested object.

The patch maintains the existing behavior for annotated tags (still requiring `--force`) and when tags point to different commits. Test updates verify the new behavior for lightweight tags while confirming restrictions remain for other cases. This aligns with the technical rationale Junio previously outlined in the thread, preserving safety for dangerous overwrites while accommodating Randall Becker's workflow concerns.

The implementation is minimal, touching only the core check in `create_tag()` and updating related tests. While this doesn't address the original error message improvements, it provides a clean solution for the specific pain point of lightweight tag recreation. The patch appears well-contained with clear test coverage of both allowed and restricted scenarios.