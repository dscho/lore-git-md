This is a discussion about output format design for the new `git repo` command series. Justin Tobler responds to Patrick Steinhardt's suggestion about using `--format=key-value|nul` instead of a `-z` flag for null-terminated output. While agreeing with the general approach of format extensibility, Justin proposes keeping `-z` as shorthand for `--format=nul` to maintain consistency with other Git commands. He also questions the motivation for potentially adding JSON output in the future, noting that earlier discussions didn't surface a strong need for it.

The exchange highlights an ongoing tension in the design - between following existing Git conventions (Justin's position favoring `-z`) and building for future extensibility (Patrick's `--format` proposal). This is part of a larger thread where the command's output formatting is being refined, with previous consensus leaning toward simple key-value pairs as the default human-readable format. The discussion remains constructive, with both participants engaging on technical merits rather than personal preferences.