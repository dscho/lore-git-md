This is a fixup patch for the `git last-modified` series, addressing upcoming Bloom filter API changes. The patch updates three function calls in `builtin/last-modified.c` to match the new Bloom filter naming convention: `clear_bloom_key` becomes `bloom_key_clear` and `fill_bloom_key` becomes `bloom_key_fill`. These changes align with an independent optimization effort for Bloom filters with multiple pathspec elements.

The changes are mechanical and straightforward replacements that maintain the same functionality while keeping compatibility with the evolving Bloom filter API. This was the last remaining item noted in the thread summary, and the series now appears technically complete with all review feedback addressed. The patch follows the established pattern of the `last-modified` command's implementation which uses Bloom filters for performance optimization when available.