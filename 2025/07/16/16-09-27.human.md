This is a design discussion about the output format for the new `git last-modified --extended` command, part of the feature series implementing tree-level path modification tracking.

Junio C Hamano provides detailed feedback on the proposed extended output format documentation, pointing out several potential issues. The main concerns are around how message termination is handled in both regular and `-z` modes, and how the format might evolve to accommodate future fields. He notes that the current description doesn't clearly specify how a reader should detect the end of the commit message portion, especially when new fields might be added later.

The feedback suggests the format may need more explicit tagging of message content (like "message SP <message>") rather than relying on indentation or position. Junio also mentions plans to modify `pretty_print_commit()`'s handling of empty lines, which would affect the current indentation-based approach. The discussion highlights important considerations for making the output format both unambiguous and extensible.

This email represents the final design consideration before the series can be integrated, continuing the thread's focus on ensuring the extended format's output fidelity. The feedback is thorough and comes from Git's maintainer, indicating this will likely require a revision to the patch before final acceptance.