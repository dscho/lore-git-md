This is a review response in the config subsystem refactoring thread. Jeff King (peff) examines Phillip Wood's patch that eliminates the `path` field from config parsing structures while fixing a use-after-free issue. The review confirms the technical soundness of using `origin_type` and `filename` fields instead of the redundant `path`, noting the BUG() check ensures filename presence for file-origin configs. Key points include verification that filename is reliably set (via strintern() that would crash if NULL), analysis of the main config file caller passing identical name/path values historically, and approval of the simplified relative include path handling logic. The review provides additional validation of the patch's safety by tracing data flow through kvi_from_source() and do_config_from_file(), concluding the changes maintain behavior while improving code hygiene. This moves the thread toward resolution with an experienced maintainer's endorsement of the technical approach.