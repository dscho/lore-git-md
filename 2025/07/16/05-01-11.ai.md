This is a follow-up in the byte-swapping optimization series, now in its final review stage. Sebastian Andrzej Siewior confirms to Junio C Hamano that v3 contains only typo fixes to commit messages from v2, with no code changes to the patches themselves. The series standardizes ntohl/ntohll handling across platforms while leveraging modern compiler optimizations.

Key points from this email:
- The actual patch content remains identical to v2, addressing prior feedback about typos in commit messages
- Discussion continues around the need for Git's custom implementations versus relying on system libraries, with musl being the main holdout still using function calls rather than optimized macros
- ntohll() remains a special case as it's not universally provided by system libraries (only Windows' winsock.h was noted)
- The email suggests treating further optimizations (like endian(3) usage) as future work after this series lands

The thread context shows this is likely the final iteration before merge, having addressed all substantive feedback from previous rounds. The changes focus on compat/bswap.h, removing platform-specific assembly in favor of compiler optimizations while maintaining behavior. MSVC remains the only compiler showing suboptimal performance with the put/get_be*() operations, which this series doesn't attempt to address.