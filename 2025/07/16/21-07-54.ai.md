This is a bugfix patch for the CI system, specifically addressing an issue in GitHub Actions test failure reporting. The patch corrects a shell syntax error introduced in commit eab5dbab ("ci: wire up Meson builds") where a variable substitution in ci/print-test-failures.sh used incorrect syntax (`${TEST_OUTPUT_DIRECTORY:t}` instead of the proper `${TEST_OUTPUT_DIRECTORY:-t}`). This typo caused "Bad substitution" errors during test runs.

The fix is straightforward - it changes the parameter expansion to use the correct `:-` syntax for providing a default value when TEST_OUTPUT_DIRECTORY is unset. The patch only modifies this one line in ci/print-test-failures.sh, which handles artifact collection for failed tests in GitHub Actions CI. The change ensures proper directory path construction for storing test artifacts when tests fail.

This appears to be a simple but important correction to restore proper test failure reporting functionality in the CI system. The patch comes from the Git maintainer and addresses a clear regression, so it's likely to be uncontroversial and quickly merged. The fix is narrowly scoped to the GitHub Actions CI path and doesn't affect other CI providers or test execution logic.