This is a discussion about output format design for the new `git repo` command, specifically regarding the `references.format` field implementation. The email continues the debate between Justin Tobler and Patrick Steinhardt about how to handle different output formats. Patrick proposes using `--format=key-value` and `--format=nul` instead of Justin's suggested `-z` flag, arguing this provides more flexibility for future formats like JSON. Justin agrees with the general approach but still advocates for including `-z` as shorthand for null-terminated output, citing its common use in other Git commands. He questions the motivation for supporting JSON output, noting previous discussions didn't indicate a strong need for it.

Key technical points:
- The discussion centers around patch 2/4 which adds `references.format` field
- Two format mechanisms are now in play: `-z` flag vs `--format=nul` option
- JSON format is mentioned as a potential future consideration
- The debate touches on Git CLI conventions (`-z` being common) vs future extensibility
- Justin references previous conversations with Lucas Seiki Oshiro about JSON needs

This email advances the output format discussion (#12 in the open questions) while introducing new considerations about format extensibility and the practical need for JSON support. The thread shows Patrick and Justin finding common ground on the general approach while maintaining their distinct positions on implementation details.