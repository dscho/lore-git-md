This is a technical follow-up discussion about the meson build system's PCRE2 dependency handling on macOS, building on the recently merged solution. Carlo Arenas clarifies a subtle point in the implementation regarding dependency fallback behavior when the system PCRE2 installation is broken.

The exchange focuses on how meson's `dependency()` function behaves with different configurations. Eli Schwartz had noted that the error message path could be simplified by relying on meson's default `required: true` behavior. Carlo confirms this understanding but points out an edge case: the fallback will still work as intended even with `--wrap-mode=nofallback`, since they're explicitly using `method: 'builtin'` rather than the default auto-detection.

This is a minor refinement to the already-working solution that detects broken macOS system PCRE2 installations via header verification. The discussion shows careful attention to meson's nuanced behavior, though it doesn't change the core functionality that was recently merged. The thread remains focused on polishing implementation details rather than introducing new changes.