This is a refactoring patch that adds a new `no_indent` field to `struct pretty_print_context` to control commit message indentation in pretty-printed output. The change is part of the `git last-modified` command series (v5 4/6), though it modifies core pretty-printing infrastructure rather than the command itself.

The patch adds a simple boolean flag that, when set, disables the standard 4-space indentation applied to commit messages during pretty-printing. The implementation is minimal - just a one-line change in `pretty.c` to check the new flag, plus the struct definition in `pretty.h`. The indentation behavior remains unchanged by default (maintaining backward compatibility) but can now be controlled by callers.

This appears to be infrastructure work supporting the `--extended` format added in v5 of the series, likely needed for the forge-style tree views mentioned in the thread summary. The change is straightforward and well-contained, touching only the pretty-printing subsystem with no apparent impact on other functionality. The patch follows Git's standard style and includes the necessary documentation via the struct field comment.

The thread context shows this is part of a mature series that has undergone significant review, with all major technical decisions resolved. This particular change seems uncontroversial - a small but useful extension of the pretty-printing API to support the new output format requirements of the `last-modified` command.