This is a late-arriving refinement to the meson build system's PCRE2 handling for macOS. The thread had already reached resolution with the main solution (using `compiler.has_header()` verification for broken system installations) being merged to 'next'. Carlo Arenas Belón's new observation identifies a specific macOS issue where the system's pkgconf module incorrectly points to a non-existent header in /usr/local/include.

Junio Hamano notes the timing issue - the refinement arrived just after the main solution was merged. He initially asks if this could be an incremental update, then quickly retracts that request (likely realizing it's too minor to warrant disrupting the already-merged series). The technical content suggests this would adjust header path checking for macOS's incorrect pkgconf configuration if submitted as follow-up.

The thread context indicates this is a minor polish to an otherwise settled solution. The core functionality (detecting broken macOS system PCRE2 via header verification) remains valid, with this refinement potentially improving detection accuracy for a specific misconfiguration case. The exchange shows the Git maintainer's typical workflow - balancing thoroughness with merge timing, where small late refinements may be deferred unless critical.