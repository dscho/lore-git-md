Documentation patch formalizing the use of C99 `bool` type in Git's codebase, concluding an 18-month experimental period that began with commit 8277dbe987 in December 2023. This is the first patch in a 3-part series that has already received extensive review and final acks from core maintainers.

The change updates CodingGuidelines to explicitly permit using `bool` from `<stdbool.h>` for predicate functions, marking the successful conclusion of a gradual adoption process. The patch adds a single section to the documentation noting this is now allowed, alongside other permitted C99 features like `for` loop variable declarations. The commit message references the original experimental commit and notes the 18-month period without complaints as justification for formalizing the practice.

This patch represents the policy/documentation side of a coordinated change that includes mechanical updates to function signatures in subsequent patches. The thread context shows the series has already undergone thorough technical review covering platform compatibility (verified by Randall S. Becker), style guidance from Brian m. carlson, and semantic rationale from Junio C Hamano. All feedback including typo corrections (notably Jeff King's "Guildlines" -> "Guidelines" fix) has been addressed in previous iterations.

The change aligns with the project's conservative approach to C99 adoption, adding `bool` to the list of selectively permitted modern C features after extended real-world use. The documentation update serves as the formal record of this decision, while the actual code changes follow in patches 2-3 of the series.