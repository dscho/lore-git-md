This is a build system fixup for René Scharfe's performance optimization series that converts commit traversal to use priority queues. Junio Hamano is adding the new performance test script `p1501-rev-parse-oneline.sh` to the meson build configuration, which was accidentally omitted from the original patch series. The test script measures the impact of the priority queue conversion on `rev-parse` operations with pathological histories, showing a 92% speedup in worst-case scenarios while maintaining near-identical performance for normal cases.

The fix is straightforward - just adding one line to `t/meson.build` to include the new performance test. This ensures the test will run as part of the test suite when built with meson. The change is marked as a fixup for the first patch in Scharfe's series, which introduced the priority queue conversion for `pop_most_recent_commit()`.

Key technical details:
- Adds `perf/p1501-rev-parse-oneline.sh` to meson.build
- Test measures performance of priority queue conversion for commit traversal
- Original series showed 92% speedup for pathological cases (many merges/refs)
- Maintains 1% regression for normal traversal cases
- Build system change only - no functional impact
- Follows up on Patch 1/3 of Scharfe's series

This is a minor administrative fix with no technical controversy, ensuring the performance tests for the optimization are properly included in the build. The thread has already established consensus on the technical approach, with this being the final housekeeping item before integration.