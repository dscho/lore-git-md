Bugfix and build system patch addressing macOS-specific PCRE2 dependency issues in the meson build system. This is the final implementation in a series that has resolved platform-specific PCRE2 handling problems across macOS and Windows.

The patch specifically handles macOS's broken system PCRE2 installation, which provides a pkgconf module pointing to a non-existent header. The solution implements Eli Schwartz's suggested approach of using `compiler.has_header()` verification when using system PCRE2 on Darwin. It provides three distinct behaviors: explicit enable falls back to built-in if possible; auto mode disables with warning; disabled takes no action. The implementation maintains static linking preference via `default_library=static` and aligns with Windows handling by adding a special rule to re-enable PCRE2 there.

Key technical details include the header verification check, fallback logic paths, and the meson_options.txt change from 'enabled' to 'auto' default. The patch incorporates feedback from Eric Sunshine (wording improvements) and builds on Carlo Arenas's earlier verification of the solution. The changes affect meson.build (core detection logic) and meson_options.txt (default value).

This appears to be the final queued version of the solution, having addressed all identified issues in the thread. The implementation is now focused solely on the immediate build system fix, while broader questions about meson upstream improvements (as proposed by Schwartz) remain as separate future work. The patch maintains consistency with the Windows cmake build system while solving the macOS-specific problem.