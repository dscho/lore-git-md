This is a technical follow-up to the meson build system changes for PCRE2 dependency handling, specifically addressing error messaging in the macOS case. Eli Schwartz provides additional meson build system expertise to refine the error handling when falling back from a broken system PCRE2 installation on macOS.

The email focuses on two specific code snippets from Carlo Arenas' implementation that handle the fallback case when macOS's system PCRE2 is broken. Schwartz suggests two alternative approaches to improve the error messaging: either using meson's built-in `not_found_message` parameter in the `dependency()` call, or explicitly setting `required: false` and handling the failure manually (with the latter requiring an additional `allow_fallback: true` parameter as previously discussed).

This is a minor refinement to the already-merged solution that added header verification for macOS system PCRE2 installations. The core functionality remains unchanged - the system still detects broken installations via `compiler.has_header()` and falls back appropriately - but the error messaging to users could be made more precise. The suggestion maintains the existing behavior while potentially providing clearer feedback when the fallback fails.

The email references previous technical discussion about fallback conditions (specifically the need for `allow_fallback: true`) and demonstrates familiarity with meson's documentation. This appears to be a polish suggestion rather than a fundamental change to the approach, coming from someone deeply familiar with meson's internals. The change would affect only the error path messaging in `meson.build` when both system and fallback PCRE2 installations fail on macOS.