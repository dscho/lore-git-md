This is a fixup patch for the `git last-modified` series, specifically updating the Bloom filter API calls to match recent naming changes in the Git codebase. The patch mechanically replaces `clear_bloom_key` with `bloom_key_clear` and `fill_bloom_key` with `bloom_key_fill` in three locations within `builtin/last-modified.c`. These changes align with parallel Bloom filter optimizations for multiple pathspec elements while maintaining all existing functionality.

The modifications are straightforward and maintain the same parameter order and behavior, just using the new function names. The patch affects the path tracking logic in three key areas: when releasing memory for tracked paths (`last_modified_release`), when adding new paths from diffs (`add_path_from_diff`), and when marking paths as changed (`mark_path`).

This appears to be the final patch in the series, addressing a naming consistency issue that arose from concurrent development of the Bloom filter API. The changes are minimal and focused, with no behavioral impact - they simply keep the `last-modified` command in sync with the evolving Bloom filter implementation. The patch follows the standard fixup format and includes a reference to the related Bloom filter optimization work.