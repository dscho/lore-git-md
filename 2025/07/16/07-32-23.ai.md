Git-gui feature patch series adding SHA-256 repository support. This is version 3 of a 2-patch set that builds on previous discussion to enable the graphical interface to work with SHA-256 hash repositories. The series comes with preparatory cleanup and the main feature implementation.

The first patch is a refactoring that replaces direct usage of `$null_sha1` with `$nullid` throughout the codebase, standardizing on a more generic variable name that won't imply SHA-1 specifically. This affects multiple UI components including blame display, checkout operations, repository selection, commit interface, and branch deletion.

The main patch adds SHA-256 repository support to git-gui by properly handling the different hash lengths throughout the interface. The changes touch the same core components as the first patch, ensuring they work correctly with either hash algorithm. The diff shows careful attention to hash length handling in various UI operations.

The changes appear focused and well-scoped, touching only the necessary components to add SHA-256 compatibility. The author notes this version includes corrections and updates from previous feedback, suggesting the patches have been through some review iteration already. The series represents an important step in Git's broader SHA-256 transition effort by bringing the graphical interface up to parity with the command-line tools.