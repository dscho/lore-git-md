This is a brief follow-up in the ongoing meson build system changes for PCRE2 dependency handling. Junio Hamano notes that Carlo Arenas' late-arriving refinement for macOS system library detection (which properly identifies broken installations by checking for missing headers in /usr/local/include) arrived just after the previous solution was merged to 'next'. 

The thread had already established working solutions for both macOS (using `compiler.has_header()` verification) and Windows (subproject fallback behavior), with the implementation being queued for inclusion. This particular email simply acknowledges the timing mismatch for this final refinement.

The core technical work appears complete and merged, with this being a minor potential follow-up that would adjust header path checking for macOS's incorrect pkgconf module configuration if submitted separately. The thread shows good collaboration with multiple contributors validating solutions across platforms.