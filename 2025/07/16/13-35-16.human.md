This is a refactoring patch in the `git last-modified` series that adds a small but useful formatting control. The change allows callers to disable the default 4-space indentation of commit messages by setting a new `no_indent` flag in the `pretty_print_context` struct. This appears to be a supporting change for the series' extended format output, though the commit message doesn't explicitly state the connection.

The implementation is straightforward - just a one-line change in `pretty.c` to respect the new flag, plus the struct field addition in `pretty.h`. While simple, this gives more control over output formatting, which could be particularly useful for machine parsing or integration with tools like forges that might want unindented text.

The patch is cleanly implemented with no obvious issues, though it's worth noting this is a plumbing-level change that doesn't affect end-user behavior unless other parts of the system start using the new flag. As part of the larger `last-modified` series that's nearing completion, this seems like a minor but reasonable addition to the formatting capabilities.