This is a refactoring patch for git-gui, part of a 2-patch series (position 1/2) preparing for SHA-256 support. The change standardizes on using the `$nullid` variable instead of the equivalent `$null_sha1` throughout the GUI codebase.

The patch touches two files (`git-gui.sh` and `lib/checkout_op.tcl`), removing the redundant `null_sha1` definition and updating all references to use `nullid` instead. The commit message explains this is preliminary work for SHA-256 repository support, though the immediate change is purely about variable naming consistency rather than hash algorithm changes.

The changes are straightforward mechanical replacements with no behavior modifications. The patch appears well-scoped and correctly implemented, with all references to the old variable name updated. Since this is part of a series, the actual SHA-256 support work will presumably come in the next patch. The commit message could be slightly more explicit about why this standardization helps with SHA-256 preparation, but the technical content looks sound.