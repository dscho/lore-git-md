This is a follow-up to the meson build system changes for PCRE2 dependency handling, specifically addressing a late-arriving fix for macOS system library detection. The thread had already converged on solutions for both macOS (using `compiler.has_header()` verification) and Windows (subproject fallback behavior), with the implementation merged to 'next'.

Junio Hamano notes that Carlo Arenas' additional fix for macOS - which properly identifies the broken system PCRE2 installation by detecting the missing header location - arrived just after the previous round was merged. The fix would need to be submitted as an incremental update if still needed. This appears to be a minor refinement to the already-working macOS solution that was confirmed by Arenas in earlier testing.

The core technical solution remains unchanged: the build system verifies system PCRE2 installations on macOS by checking for the presence of headers, with three behavior modes (explicit enable with fallback; auto with warning; disabled). The late fix would presumably adjust the header path check to account for macOS's incorrect pkgconf module configuration pointing to /usr/local/include. Since the thread has largely concluded with working solutions for all platforms, this would be a minor follow-up patch if deemed necessary.