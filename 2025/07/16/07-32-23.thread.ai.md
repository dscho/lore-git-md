Thread identity: git-gui SHA-256 repository support patch series (v3), 2 patches, by original author. Type: feature implementation building on prior discussion.

Core problem: Enable git-gui to work with SHA-256 hash repositories, bringing graphical interface parity with command-line SHA-256 support. Requires handling variable hash lengths throughout UI components.

Current status: Version 3 of series. First patch refactors to use `$nullid` instead of SHA-1-specific `$null_sha1`. Second patch implements SHA-256 support across affected UI components with dynamic hash length detection via `git rev-parse --show-object-format`, generating `$nullid` and `$nullid2` appropriately.

Key technical details:
- Changes span multiple UI areas: blame display, checkout operations, repository selection, commit interface, branch deletion
- Introduces global `$hashlength` variable (40 or 64) determined at startup
- Replaces hardcoded SHA-1 lengths with `$hashlength` using Tcl's `string map`
- Special-cases repository picker dialog which runs before hash length is known
- Updates blame interface, commit validation, branch deletion, and repository cloning
- Maintains backward compatibility with SHA-1 repositories
- v3 improvements include better documentation, fixed null ID generation, and improved abbreviated ID handling in blame

Open questions: None explicitly mentioned in this version, suggesting prior feedback has been addressed. Reviewers should verify complete coverage of hash-length-sensitive operations.

Participant positions: Author has incorporated feedback from previous versions (implied by version increment and changelog references). No new review tags or objections in this submission.

Related work: Part of Git's broader SHA-256 transition effort, complementing command-line support already implemented in core. Builds on brian m. carlson's foundational SHA-256 work.