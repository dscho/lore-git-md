This is a technical clarification in an ongoing discussion about `git diff` behavior between `--ignore-matching-lines` and `--name-only` options. Lidong Yan responds to a suggestion that `--name-only` could be considered a formatting variant of full diff output by explaining Git's actual implementation approach.

The email reinforces the core design principle established earlier in the thread: metadata-focused outputs like `--name-only` work by comparing SHA values rather than examining content, making them fundamentally different from diff formatting variants. Yan emphasizes this performance optimization where Git avoids generating full diff content when only file names are needed.

Key technical points maintained from previous discussion:
- SHA comparison as the basis for `--name-only` output
- Content examination remains separate from metadata generation
- Performance rationale for avoiding diff generation in name-only mode

This response doesn't introduce new technical arguments but solidifies understanding of the current implementation's design choices. The thread remains in a state where the maintainer's position is clear but some participants continue exploring alternative interpretations of the options' relationship.