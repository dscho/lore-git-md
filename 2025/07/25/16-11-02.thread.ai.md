Here is the dense AI thread summary for this new bug report:

Bug report from Russell King about git-daemon regression in Fedora 40 (Git 2.49.0) where user directory paths with tildes (`~user`) are no longer properly expanded when serving repositories via `--user-path=public_git`. Configuration includes `/etc/gitconfig` safe directories listing `/home/rmk/public_git/*` and daemon runs with `--export-all --user-path=public_git` as user 'nobody'. The daemon correctly identifies user paths and prepends "public_git" but fails to expand `~rmk` to `/home/rmk`, rejecting paths like "'~rmk/public_git/linux-arm.git': not in directory list". This worked in previous versions and now impacts production infrastructure during kernel merge window. No root cause identified yet - possible issues with tilde expansion or user path resolution under 'nobody' user context.