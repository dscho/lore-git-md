Here is the updated dense AI thread summary combining all messages:

Bug report and resolution regarding git-daemon regression in Fedora 40 (Git 2.49.0) where user directory paths with tildes (`~user`) fail to expand when serving repositories via `--user-path=public_git`. Original report from Russell King described production impact during kernel merge window with configuration including `/etc/gitconfig` safe directories listing `/home/rmk/public_git/*` and daemon running as 'nobody' with `--export-all --user-path=public_git`. Daemon correctly prepended "public_git" but rejected paths like "'~rmk/public_git/linux-arm.git': not in directory list" without expanding `~rmk` to `/home/rmk`, behavior that worked in previous versions.

Jeff King's initial investigation found tilde expansion working in test environments, suggesting the issue might relate to path validation in `daemon.c:path_ok()` rather than expansion itself. Todd Zullinger identified SELinux as the root cause in Fedora 40, providing a complete solution path involving: 1) setting proper directory permissions, 2) correcting SELinux context types to `git_user_content_t`, and 3) enabling the `git_system_enable_homedirs` boolean. This revealed the issue as Fedora-specific due to tightened SELinux policies.

Jeff King's follow-up acknowledges Todd's SELinux findings explain the environment-specific behavior but notes remaining questions about the error message sequence - specifically why `enter_repo()` appears to succeed while `ok_paths` validation fails, suggesting a subtle interaction between path expansion and permission checking in `daemon.c`. This technical detail may warrant future investigation but doesn't block the immediate SELinux-based solution. The thread concludes with the issue understood as Fedora-specific policy tightening rather than a Git regression, though it highlighted documentation gaps in Fedora's git-daemon package regarding SELinux configuration.