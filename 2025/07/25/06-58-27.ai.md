This is a refactoring patch in Patrick Steinhardt's reflog migration series, specifically addressing type safety in identity parsing. The change modifies `split_ident_line()` to use `size_t` for its length parameter instead of `int`, since all callers pass positive values from string length operations.

The patch is straightforward - it updates both the declaration in ident.h and implementation in ident.c to use the more appropriate unsigned type for string lengths. This aligns with common C practices where `size_t` is the standard type for representing sizes and lengths. The commit message clearly explains the rationale: callers already pass values from `strlen()`, `strbuf::len`, or pointer differences, all of which are naturally unsigned.

This appears to be a preparatory change for the reflog migration work, ensuring type consistency in identity parsing functions that will be used when processing reflog entries. The change is minimal and low-risk, touching only the type signature without modifying any logic. It follows the pattern of previous refinements in the series where small technical improvements are made to support the larger reflog infrastructure changes.

Key technical details for future reference:
- Function modified: `split_ident_line()` in ident.c/ident.h
- Parameter changed: `int len` -> `size_t len`
- Related to identity parsing used in reflog handling
- Part of broader reflog migration infrastructure work
- No behavior change, only type safety improvement