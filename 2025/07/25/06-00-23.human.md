This is a follow-up discussion about the interaction between `git diff --ignore-matching-lines` and `--name-only` options. The thread had previously established that this behavior is intentional - metadata-focused options like `--name-only` deliberately avoid examining content to maintain consistent answers about file modifications regardless of ignore patterns.

The email questions whether this design makes sense, arguing that `--name-only` could be seen as just a formatting change that should still respect content filtering. The author suggests the current behavior is surprising since files filtered by `--ignore-matching-lines` disappear from normal diff output but reappear with `--name-only`, requiring internal implementation knowledge to understand.

This appears to be reopening the settled discussion with a different perspective on what `--name-only` option should represent. The maintainer had previously explained the performance and consistency rationale for the current behavior, but this email suggests some users might expect content filtering to propagate through all output formats. The brief reference to implementation details ("how it is implemented") hints this may be coming from someone who has looked at the code.