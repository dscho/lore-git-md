This is a technical clarification in an ongoing discussion about `git diff` behavior between `--ignore-matching-lines` and `--name-only` options. 

Lidong Yan responds to a suggestion that `--name-only` could be considered just a formatting variant of full diff output by explaining Git's actual implementation approach. The key point is that `--name-only` operates by comparing SHA values of files rather than examining diff content, making it fundamentally different from content-filtering options like `--ignore-matching-lines`. 

The email reinforces Junio Hamano's earlier design principle that metadata-focused outputs deliberately avoid content examination for performance reasons. Yan's explanation helps bridge the conceptual gap between the user's mental model (seeing `--name-only` as output formatting) and Git's internal optimization (using SHA comparisons to determine modified files).

This continues the thread's technical discussion without introducing new proposals, instead providing implementation context to support the maintainer's position about why content-ignoring options shouldn't affect metadata outputs. The exchange remains focused on understanding Git's architectural decisions rather than pushing for behavior changes.