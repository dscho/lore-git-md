Thread identity: Bug report and fix for unexpected alias behavior with `git grep -h`, reported by Kevin Brodsky and fixed by Jeff King (Peff), with design discussions involving Junio Hamano, Peff, and D. Ben Knoble about edge cases and implementation philosophy.

Core problem: Git's alias handling doesn't properly distinguish between `-h` as a help request versus command-specific flag usage, affecting both built-in and external commands in aliases. The initial fix addressed `grep -h` but revealed deeper issues in the alias expansion pipeline regarding argument preservation and help mode detection, now extending to philosophical debate about Git's alias model.

Current status: Jeff King's initial fix (modifying `handle_alias()` for `-h` as sole argument) remains in place. The discussion has progressed from technical edge cases to fundamental questions about Git's alias implementation, with Junio Hamano now strongly advocating for a simple, consistent approach that always shows alias expansion for `-h` without execution, rejecting Peff's more complex proposals that would handle built-in vs shell command aliases differently.

Key technical details: The problem stems from alias expansion occurring before command-specific parsing. Junio's position clarifies two key requirements: 1) For all alias types (built-in with injected args or shell commands), `-h` should show the alias without execution, 2) This behavior should be implemented without special argument stripping or case-specific logic, maintaining Git's textual substitution model. The solution path now focuses on modifying `handle_alias()` to consistently intercept `-h` before any command execution.

Open questions and loose ends: While Junio's response moves the thread toward resolution, remaining considerations include: 1) Final implementation details for consistent `-h` handling across all alias types, 2) Documentation updates needed to clarify this behavior, 3) Whether any edge cases (like combined `-h` with other flags) require special handling despite the simplified approach. The philosophical tension between practical UX and Git's design principles remains present but now has clear guidance from the maintainer.

Participant positions: Kevin Brodsky reported the original issue. Jeff King provided the initial fix and identified additional edge cases, proposing targeted improvements that would modify argument handling. D. Ben Knoble challenged these proposals on philosophical grounds. Junio Hamano has now decisively weighed in favoring simplicity and consistency over sophisticated handling, effectively resolving the philosophical debate in favor of preserving Git's textual substitution model while still fixing the core issue.

Related work: Connects to broader discussions about alias behavior and command dispatch, with relevance to D. Ben Knoble's `--help-all` work and fundamental design of Git's command processing pipeline. Junio's position suggests this may become a reference case for how to balance bugfixes against Git's core design principles.