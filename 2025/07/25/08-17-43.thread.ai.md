Thread identity: Bug report and fix for unexpected alias behavior with `git grep -h`, reported by Kevin Brodsky and fixed by Jeff King (Peff), with ongoing design discussions involving Junio Hamano, Peff, and now D. Ben Knoble about edge cases and implementation philosophy.

Core problem: Git's alias handling doesn't properly distinguish between `-h` as a help request versus command-specific flag usage, affecting both built-in and external commands in aliases. The initial fix addressed `grep -h` but revealed deeper issues in the alias expansion pipeline regarding argument preservation and help mode detection.

Current status: Jeff King's initial fix (modifying `handle_alias()` for `-h` as sole argument) remains in place. New analysis shows additional edge cases where alias-injected arguments (like `--cached`) interfere with help mode detection. Peff proposes two enhancements: 1) stripping alias-added arguments during `-h` processing, and 2) preventing shell command aliases from executing with `-h`. D. Ben Knoble raises concerns about these proposals diverging from Git's textual substitution model and creating false positives for third-party commands.

Key technical details: The problem stems from alias expansion occurring before command-specific parsing. Current issues include: 1) Argument loss when combining `-h` with alias-injected flags, 2) Shell command aliases executing despite `-h` presence, 3) Inconsistency between built-in vs. external command handling. Peff demonstrates concrete failure modes where argument injection bypasses help mode. Knoble's response questions whether modifying argument handling for `-h` cases breaks Git's textual alias model and whether `--help-all` behavior should be considered in the same context.

Open questions and loose ends: Should Git: 1) Modify argument handling to preserve `-h` semantics when aliases inject flags, potentially breaking textual substitution expectations? 2) Block shell alias execution for `-h` requests despite possible false positives with third-party commands? 3) Extend the solution to handle `--help-all` cases? The discussion now balances implementation philosophy (textual vs. semantic alias handling) against practical correctness.

Participant positions: Kevin Brodsky reported the original issue. Jeff King provided the initial fix and identified additional edge cases, proposing targeted improvements. D. Ben Knoble questions the philosophical implications of Peff's proposals, advocating to preserve Git's textual substitution model where possible. Junio Hamano's earlier concerns about external command handling remain relevant but now have more concrete examples to inform next steps.

Related work: Connects to broader discussions about alias behavior and command dispatch, with new relevance to D. Ben Knoble's ongoing work on `--help-all` improvements. The thread demonstrates how alias expansion design decisions can have subtle consequences across Git's command processing pipeline, now including philosophical considerations about the nature of alias substitution.