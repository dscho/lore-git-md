This is a discussion about the potential Rustification of Git's xdiff library and its implications for downstream projects. The email is part of an ongoing thread about introducing Rust as a hard dependency in Git, specifically focusing on how this would affect other projects that use xdiff (like libgit2 and Vim).

Ben Knoble responds to Patrick Steinhardt's earlier message about the divergence between Git's xdiff and libgit2's fork. Patrick suggested that libgit2 could maintain its own C-only xdiff fork if needed, as the library is mostly in maintenance mode. Ben raises concerns about Vim potentially needing a C-only version and suggests a possible compromise: allowing Vim to supply its own xdiff library or helping Vim support a Rust-based xdiff directly.

The thread highlights the broader challenges of introducing Rust into Git's core, particularly around platform support (Gentoo, HPPA, Alpha, m68k) and downstream compatibility (libgit2, Vim). The discussion remains open-ended, with no clear resolution yet on whether Rust will become a hard dependency or how downstream projects will adapt.