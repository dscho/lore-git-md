This is a discussion about the behavior of `git diff --name-only` with content-ignoring options like `--ignore-matching-lines`. The email comes from Arne, who is pushing back against the established design principle that metadata-focused flags like `--name-only` should avoid examining file contents for performance reasons.

Arne makes a UX argument rather than a technical one - they claim the current behavior is surprising because `--name-only` can cause files to appear in the output that wouldn't appear in a full diff (when changes are ignored by other flags). They argue this violates the principle of least surprise since neither the documentation nor the flag name suggests this behavior, and that implementation details (content examination avoidance) shouldn't dictate user-facing behavior.

The key distinction from previous discussion is framing this as a documentation/UX issue rather than a technical optimization question. Arne suggests making `--name-only` behave more like other diff output modes where ignored changes would suppress file listing, seeing the current content-examining optimization as an implementation detail that shouldn't affect user expectations.

This keeps open the thread's core tension between performance optimizations and consistent behavior, but shifts focus from technical feasibility to user experience and documentation clarity. The response doesn't propose specific code changes but continues advocating for behavior that would be more intuitive to users, separate from the implementation constraints.