This is a continuation of the discussion about content-ignoring options (`--ignore-matching-lines`, `-w`) and their interaction with metadata output formats (`--name-only`, `--raw`). Junio Hamano responds to Jeff King's concerns about the current behavior being unintuitive, while acknowledging potential backward compatibility risks.

The email clarifies that `--raw` output must remain stable for machine parsing, and `--checkdiff` has specific semantic reasons to ignore whitespace changes. This leaves only `--name-status` and `--name-only` as candidates for potential behavior changes to respect content-ignoring options. However, Junio expresses caution about modifying these longstanding behaviors due to possible workflow disruptions.

Key technical points include:
- The distinction between machine-readable `--raw` output (which must remain stable) and human-oriented formats
- Specific semantic reasons why `--checkdiff` should ignore whitespace changes
- Recognition that only `--name-status` and `--name-only` might reasonably be changed
- Strong emphasis on backward compatibility concerns

The email maintains the technical position that content examination should remain separate from metadata generation for performance reasons, while acknowledging the UX arguments. Junio indicates openness to reviewing proposed changes but won't initiate them himself, suggesting the discussion remains at an impasse between technical design and user expectations.