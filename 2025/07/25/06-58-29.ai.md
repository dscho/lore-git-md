This is a refactoring patch in Patrick Steinhardt's reflog migration series, specifically addressing the behavior of the `REF_HAVE_OLD` flag during log-only updates. The change preserves the flag's value while still preventing old-OID verification for reflog entries, preparing for future use of this signal in the patch modifies flag handling across multiple backend implementations (files and reftable) to maintain `REF_HAVE_OLD` while adding explicit `REF_LOG_ONLY` checks where old-OID verification should be skipped. The core change moves from implicitly clearing `REF_HAVE_OLD` for reflog updates to explicitly checking `REF_LOG_ONLY` when deciding whether to verify old object IDs. This maintains the existing behavior while preserving the flag's state for future use.

Key technical details include:
- Modified files: `refs.c`, `refs/files-backend.c`, `refs/refs-internal.h`, `refs/reftable-backend.c`
- Flag behavior changes:
  - `REF_HAVE_OLD` is no longer cleared for log-only updates
  - New explicit checks for `REF_LOG_ONLY` where old-OID verification should be skipped
- Preserved invariants:
  - Log-only updates still don't verify old object IDs
  - Symbolic reference handling remains unchanged
- Backend consistency:
  - Both files and reftable backends updated to match new behavior
  - Error messages and BUG checks adjusted accordingly
- Documentation updated in `refs-internal.h` to clarify the behavior

The patch appears well-constructed with thorough updates across all affected components. It maintains backward compatibility while enabling future functionality, and includes appropriate documentation updates. The changes are focused and clearly motivated by the series' progression toward more robust reflog handling.