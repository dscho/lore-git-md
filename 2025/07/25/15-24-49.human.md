This is a technical discussion about implementing warnings for skipped commits during rebase conflict resolution, specifically focusing on the implementation details in `sequencer.c`.

The email shows Junio Hamano responding to Phillip Wood's suggestion about when to issue warnings about skipped commits during rebase operations. Phillip had proposed checking for `rebase_path_message()` existence before printing warnings, and suggested reading `REBASE_HEAD` to make the messages more informative. He also noted this would affect both `--continue` and `--skip` operations.

Junio questions whether `--skip` would even reach the code path being discussed (`commit_staged_changes()`), admitting he's navigating the sequencer logic largely through educated guesses about function names rather than deep familiarity with the implementation. The exchange shows careful consideration of edge cases in the rebase state machine, with both contributors trying to ensure warnings only appear in appropriate contexts.

This continues the thread's technical discussion about how to reliably detect and warn about skipped commits, now focusing on the precise conditions under which warnings should trigger. The core challenge remains distinguishing legitimate empty-step skips from other rebase states while maintaining clear user feedback.