This is a discussion about the behavior of `git diff --ignore-matching-lines` with `--name-only`. Arne Schmidt responds to the established design principle (that metadata-focused flags like `--name-only` deliberately avoid content examination for performance) by arguing from a user experience perspective rather than implementation details.

The key point is that Arne finds the current behavior surprising and inconsistent with `git diff`'s normal operation, since `--name-only` can affect which files appear in the output while the documentation doesn't mention this interaction. They contend that implementation optimizations shouldn't override user expectations, and that the behavior should align with the rest of `git diff` even if it means sacrificing some performance.

This keeps the thread's technical discussion alive by framing the issue as a documentation/UX problem rather than challenging the core performance optimization. The response doesn't propose specific code modifications but suggests the current behavior might warrant either documentation updates or reconsideration of the design tradeoffs. The thread remains open on whether metadata outputs should conceptually consider ignored content.