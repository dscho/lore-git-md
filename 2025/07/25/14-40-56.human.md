This is a follow-up discussion in the ongoing thread about improving `git rebase`'s handling of skipped commits during conflict resolution. Junio C Hamano responds to Cameron Steffen's suggestion to reuse the existing "skipped previously applied commit" warning message format.

The exchange focuses on implementation details, specifically the challenge of reliably detecting when a commit was skipped because its changes were already applied (versus other reasons a commit might become empty during rebase). Hamano agrees with the message format approach but raises a key technical concern about accurately determining the skip reason, particularly after user intervention during conflict resolution.

The discussion shows the maintainer engaging with the proposed solution while identifying a critical edge case that needs addressing - distinguishing legitimate skips from other empty-commit scenarios that can occur when users manually resolve conflicts. This moves the thread forward by narrowing the focus to a specific technical challenge in the implementation.