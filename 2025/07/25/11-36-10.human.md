This is a technical discussion about ref transaction behavior, specifically around the `REF_HAVE_OLD` flag and log-only updates. Jeff King (peff) is responding to Patrick Steinhardt's patch that stops unsetting `REF_HAVE_OLD` for log-only updates, which will be used as a signal in a subsequent commit.

Peff generally likes the direction but raises an interesting observation: while Patrick's patch removes the `REF_HAVE_OLD` unsetting in `split_symref_update()`, the similar `split_head_update()` function doesn't strip `REF_HAVE_OLD`. This means log-only HEAD updates currently do an extra `check_old_oid()` that would be skipped after Patrick's patch.

The core concern is whether this could lead to non-atomic behavior in transactions - specifically if a log-only update could succeed while the actual ref update fails. Peff notes that ref transactions are meant to be all-or-nothing but recalls recent discussions about non-atomic transactions, wondering if this could be problematic.

Peff also shares context from his own recent work where he encountered related behavior with dangling symrefs and HEAD updates, noting that his initial solution (clearing `HAVE_OLD` like `split_symref_update()` does) would conflict with Patrick's approach. He suggests checking for `LOG_ONLY` in `check_old_oid` (as Patrick's patch does) might be the better solution.

This is a nuanced discussion between two experienced contributors about subtle edge cases in ref transaction handling, showing careful consideration of how changes might interact with other parts of the system. The exchange demonstrates the kind of deep technical discussion that happens on the list when modifying core refs infrastructure.