Follow-up discussion on `git diff --ignore-matching-lines` behavior with `--name-only`. The email questions Junio Hamano's assertion that content-ignoring options shouldn't affect metadata outputs, arguing that `--name-only` could be seen as just a formatting change from the full diff output. The author suggests that since files with all changes don't appear in regular diff output when using `--ignore-matching-lines`, they shouldn't appear in `--name-only` output either, comparing this to piping the diff through grep and cut operations.

Key technical points raised:
- Challenges the design principle that metadata outputs should be content-independent
- Proposes viewing `--name-only` as a formatting variant rather than a fundamentally different operation
- Notes the current behavior requires internal implementation knowledge to understand
- Describes the current behavior as surprising from a user perspective

The email maintains the thread's focus on the interaction between content-filtering options and metadata outputs, but introduces a new perspective questioning the core design assumption. The comparison to shell pipeline operations suggests an alternative mental model for how `--name-only` could work with content filters. No new technical details are added beyond what was previously discussed in the thread.