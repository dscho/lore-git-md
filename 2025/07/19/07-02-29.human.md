This is a design discussion about the internals of the priority queue implementation in Git's commit traversal code. Jeff King (Peff) is responding to René Scharfe's proposal about maintaining heap properties in the prio_queue data structure.

The core question is whether it's acceptable to temporarily violate the heap property (where the root item should be the minimum) between operations, as long as the queue's size and array contents remain consistent. Scharfe argues this would be sufficient for current use cases like commit enumeration, while King expresses some discomfort with exposing an inconsistent state.

King considers alternatives like making the array private (with performance tradeoffs) or using a foreach macro, but remains undecided. The discussion highlights the tension between implementation cleanliness and practical performance considerations in Git's low-level data structures.

This email represents the final design discussion point in a performance optimization series that is otherwise ready for merging. The exchange provides valuable insight into the thought process behind Git's data structure design decisions, though it doesn't block the series' progress.