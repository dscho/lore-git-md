This is a technical clarification in the ongoing `git last-modified` feature series, specifically addressing a Bloom filter integration detail.

Jeff King (Peff) responds to Taylor Blau's question about a NULL oid check in the `mark_path()` function. The discussion stems from comparing the current implementation with Taylor's original `blame-tree` version from GitHub's fork. Peff explains that while Taylor's version needed the NULL check due to its caching system, Toon Claes' current implementation doesn't require it since `mark_path()` is always called with a valid oid pointer.

This is a minor but important clarification that helps maintainers understand why the current implementation differs from the original GitHub version. The exchange shows careful attention to detail in reviewing the Bloom filter integration, though it doesn't impact the core functionality of the feature.