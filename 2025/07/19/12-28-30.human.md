Test modernization patch converting grep to test_grep in interactive patch tests.

This is the first patch in Leon Michalak's v4 series implementing diff context configuration for interactive patch commands. It's a straightforward test modernization change, converting all uses of `grep` to the more robust `test_grep` helper in two test files (t3701-add-interactive.sh and t4055-diff-context.sh). 

The change provides better debugging output when tests fail, as `test_grep` includes the pattern and file being searched in failure messages. The patch is mechanical and low-risk, touching only test assertions without modifying any production code. It follows Git's ongoing effort to modernize test scripts by using the test helper utilities.

This appears to be a well-executed preparatory cleanup before the main functional changes in the series. The commit message is clear about the purpose, and the diff shows careful attention to converting all relevant grep calls while maintaining the same test semantics. Since this is part of a larger series that has already undergone several rounds of review, we can expect the changes to be solid.