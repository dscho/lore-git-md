Here is the dense AI thread summary for this patch:

The thread concerns a proposal to remove Git's `--early-output` feature from the revision machinery. This feature was added in 2007 (commit 23c17d4a6a) to support progressive rendering in gitk/QGit but was never documented or widely adopted. The patch from Jeff King (Peff) removes all related code (154 lines across `builtin/log.c`, `revision.c`, `revision.h`) including: the `early_output` field in `struct rev_info`, the `show_early_output_fn_t` function pointer type and its volatile global instance, timer-based output logic with `estimate_commit_count()`, `SIGALRM` signal handling, and implicit `--topo-order` enabling. The feature has been broken multiple times (2011-2017 and currently with commit-graphs) without reports, indicating no active users. The removal simplifies the revision code for future work. Peff considered but rejected keeping the option as a no-op, arguing complete removal is justified given the feature's unused state. The patch is a clean-up with no expected user impact.