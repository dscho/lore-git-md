Here is the updated dense AI thread summary:

The thread concerns Jeff King's (Peff) proposal to remove Git's `--early-output` feature from the revision machinery, originally added in 2007 (commit 23c17d4a6a) for gitk/QGit but never documented or widely adopted. The patch removes all related code (154 lines across `builtin/log.c`, `revision.c`, `revision.h`) including: the `early_output` field in `struct rev_info`, `show_early_output_fn_t` function pointer type and its volatile global instance, timer-based output with `estimate_commit_count()`, `SIGALRM` signal handling, and implicit `--topo-order` enabling. The feature had been broken multiple times (2011-2017 and currently with commit-graphs) without user reports. Junio C Hamano has now acked the patch, confirming the feature has no active users and recalling the unmerged gitk RFC from 2007. He agrees with direct removal rather than keeping the option as a no-op, noting the same justification (lack of usage) applies to both approaches. The patch is queued for inclusion, eliminating untested, undocumented code that had become a maintenance burden in the revision machinery. No objections were raised during discussion. This clean-up simplifies the codebase for future work with no expected user impact.