Test modernization patch converting grep calls to test_grep in t3701 and t4055.

This is the first patch in the v4 series implementing diff context configuration for interactive patch commands. It performs a preparatory cleanup by replacing direct `grep` calls with the `test_grep` test helper in two test files (t3701-add-interactive.sh and t4055-diff-context.sh). The change provides better debugging information when tests fail by including the failed pattern and output in the test output.

The patch makes 38 mechanical substitutions across both files, converting patterns like:
- Simple pattern matches (`grep "pattern"` -> `test_grep "pattern"`)
- Negative matches (`! grep` -> `test_grep !`)
- Pattern matching with options (`grep -E` -> `test_grep -E`)
- Color code matching (handling escape sequences)

This change follows established test modernization practices in the Git project and doesn't modify any test logic - it only improves failure reporting. The changes are straightforward and unlikely to be controversial as they align with the project's testing conventions. The patch is properly signed-off and appears to be a clean application of the test_grep conversion pattern seen elsewhere in the codebase.