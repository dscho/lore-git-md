This is a refactoring patch series (v6) completing the migration of sparse-checkout configuration from global variables to `repo_settings`, part of the `the_repository` removal effort. The series finalizes the work started in v5 by addressing remaining concerns about `prepare_repo_settings()` call placement and config validation timing.

The key changes in v6 are:
1. Optimized calls to `prepare_repo_settings()` to avoid redundant invocations, particularly in `sparse-index.c` where calls are now consolidated in `convert_to_sparse()` rather than `is_sparse_index_allowed()`
2. Restored proper config validation behavior for `sparse.expectfilesoutsideofpatterns` by moving it into `repo_settings` rather than localizing it, ensuring errors are caught early
3. Fixed commit message inaccuracies and clarified the reasoning behind call placement changes

The series touches 15 files, removing global declarations while maintaining all existing functionality. Changes are concentrated in sparse-checkout handling code (`builtin/sparse-checkout.c`, `sparse-index.c`, `dir.c`) and config plumbing (`repo-settings.[ch]`). The author has addressed all technical feedback from v5, including Junio's concerns about excessive `prepare_repo_settings()` calls and Phillip's validation timing issue.

This appears to be the final version of the series, with all architectural questions resolved and no outstanding technical objections. The changes are well-contained and follow established patterns for migrating globals to repository-specific storage. The patch descriptions clearly explain each change and its rationale, with good attention to edge cases like config validation timing.