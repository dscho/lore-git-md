Refactoring patch (part 1/3) in the v6 series moving sparse-checkout configuration from global variables to `repo_settings`, as part of the `the_repository` removal effort.

This patch specifically migrates the `core.sparsecheckout` setting from the global `core_apply_sparse_checkout` variable to `repo_settings.sparse_checkout`. It carefully manages initialization by adding `prepare_repo_settings()` calls where needed (in `builtin/backfill.c`, `builtin/clone.c`, `builtin/mv.c`, `dir.c`, `wt-status.c`) and removing redundant calls (in `sparse-index.c` and `builtin/worktree.c`). The change also removes the now-unneeded `USE_THE_REPOSITORY_VARIABLE` macro from `backfill.c`.

Key technical details:
- Adds `sparse_checkout` field to `struct repo_settings`
- Moves config parsing from `config.c` to `repo-settings.c`
- Updates 15 files to use the new location, with special attention to initialization timing
- Maintains all existing behavior while eliminating global variable access
- Follows established patterns from prior configuration migrations

The patch appears technically sound, having addressed all feedback from previous iterations. The changes are focused and well-contained, touching only the necessary files to complete this piece of the larger refactoring effort. The commit message clearly explains each modification and its impact on initialization safety.