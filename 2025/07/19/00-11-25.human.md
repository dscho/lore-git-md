This is the sixth version of a refactoring patch series that completes the migration of sparse-checkout configuration from global variables to the `repo_settings` struct, as part of the ongoing `the_repository` removal effort.

The series now addresses all technical concerns raised in previous reviews, particularly around the placement of `prepare_repo_settings()` calls and config validation timing. The three patches systematically move each remaining sparse-checkout-related global variable (`core_apply_sparse_checkout`, `core_sparse_checkout_cone`, and `sparse_expect_files_outside_of_patterns`) into `repo_settings` while maintaining proper initialization and error handling.

Key improvements in v6 include:
- Optimized calls to `prepare_repo_settings()` to avoid redundant invocations
- Fixed a user-facing issue where config validation could happen too late in the call chain
- Corrected documentation and commit messages
- Confirmed that all config validation behavior matches the original through `prepare_repo_settings()`

The changes touch core sparse-checkout functionality across multiple commands (clone, grep, mv, worktree) and subsystems (dir, unpack-trees, wt-status). The series appears technically complete with thorough consideration of edge cases and performance implications. With all reviewer concerns addressed, this looks ready for final maintainer consideration.