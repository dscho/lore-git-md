This is a follow-up discussion in the ongoing thread about removing experimental markers from `git-switch` and `git-restore`, specifically addressing the contested removal of the `-m` short flag. The email shows Justin Tobler responding to Junio Hamano's objections about the proposed changes.

Key technical points in this response:
- Justin acknowledges Junio's workflow reliance on `git checkout -m` and concedes the short flag should be kept if the operation is indeed common
- Proposes an alternative approach for the `--conflict` flag implementation that would keep it in the shared `add_common_options()` function while allowing per-command control over short flag availability
- Suggests adding a parameter to toggle short flag usage while maintaining the unified option parsing

The discussion reveals two potential paths forward:
1. Dropping the `-m` removal entirely while keeping the documentation updates (Junio's preference)
2. A compromise implementation that maintains shared option handling while allowing command-specific short flag control (Justin's suggestion)

This email moves the thread toward resolution by showing willingness to adapt the approach based on real-world usage patterns while still attempting to maintain some of the original goals around code organization. The core disagreement about whether shared option implementations should enforce identical short flags remains unresolved.