Feature implementation with test feedback. This is the final patch in a series adding diff context configuration support to interactive patch commands (add/checkout/commit/reset/restore/stash with --patch). The patch introduces command-line overrides (-U/--unified and --inter-hunk-context) that complement the config support added earlier.

Phillip Wood provides detailed test feedback, noting that while the new tests for command-line overrides look good, the patch incorrectly removes existing tests for config-based diff.context/diff.interHunkContext behavior that were added in previous commits. He also questions the value of some additional tests in t4055 that duplicate coverage of -U option behavior across commands.

The thread context shows this series has undergone extensive review, with all major design questions resolved (including acceptance of the OPT_DIFF_* macros based on their six existing users). Junio Hamano had previously indicated the v4 changes looked good for merging, pending addressing remaining review comments. This email highlights one such remaining issue - the test coverage regression - that Leon Michalak will need to address before final merge.

Key technical details:
- Adds -U/--unified and --inter-hunk-context options to all interactive patch commands
- Options override diff.context/diff.interHunkContext config values
- Includes validation rejecting negative values
- Test coverage needs to preserve both config and command-line override cases
- Series builds on prior work modernizing tests (45 grep/config call conversions)
- Final outstanding issue is test organization/coverage balance

The implementation itself appears sound, with the main remaining work being test cleanup to ensure comprehensive coverage of both config and command-line paths without redundancy. Phillip's feedback provides clear guidance on what needs adjustment before merge.