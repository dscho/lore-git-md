This is a follow-up in the thread about removing experimental status markers from `git-switch` and `git-restore`. Junio C Hamano responds to Justin Tobler's patch, focusing on the documentation changes while expressing reservations about the companion patch that would remove the `-m` short flag.

Junio suggests rewording the commit message to better reflect that the commands' experimental status has become outdated through widespread adoption rather than lack of changes. He approves the straightforward documentation updates removing the "EXPERIMENTAL" warnings from both man pages, but maintains his objection to the first patch in the series that would remove the `-m` flag.

The response indicates a split in the proposed changes - while the documentation updates are uncontroversial, the interface changes remain disputed. Junio hints that if the flag removal were implemented, it would require another long stabilization period before removing the experimental markers, suggesting he views these as separate concerns rather than a single package.

Key technical details carried forward:
- Documentation changes to git-restore.adoc and git-switch.adoc remain on track
- The fate of `-m` flag removal (patch 1/2) remains unresolved
- Junio's workflow concerns about `-m` usage persist
- The thread now effectively splits the original proposal into two independent decisions

This email doesn't introduce new technical elements but clarifies Junio's position on sequencing - he's willing to proceed with the documentation updates separately from (and potentially before) any interface changes. The response maintains the thread's focus on balancing interface stability against implementation cleanliness.