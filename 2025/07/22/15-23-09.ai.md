This is a test fix patch in the ongoing merge-ort rename detection bugfix series. It corrects staging issues in testcases 12i, 12j, and 12k that were introduced in an earlier commit (806f83287f8d). The patch fixes forgotten `git add` operations for modified files in these test setups, which had caused the tests to run with unstaged changes unrelated to their actual purpose.

The changes normalize the test state by properly staging the 'baz' file modifications in each test scenario, making the test output cleaner (changing " M" to "M " in status output) and removing a distraction from the actual rename-to-self behavior being tested. The patch also includes an unrelated fix to testcase 12n's expected line count (from 2 to 3) that appears to be included here for convenience.

The commit message explains that these staging issues were masking a deeper bug in merge-ort's handling of directory renames that will be addressed in a subsequent patch. This fix prepares the test infrastructure to properly expose that issue. The changes are straightforward and mechanical, focused on test setup correctness rather than core merge behavior.