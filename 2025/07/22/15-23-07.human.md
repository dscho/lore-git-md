Refactoring patch in the merge-ort rename detection series. This is patch 2/6 in a bugfix series addressing complex edge cases in directory rename handling during merges.

The patch simplifies the `check_for_directory_rename()` function in merge-ort.c by replacing a verbose pattern of checking for a key in a strmap with a temporary variable) with a direct call to `strmap_contains()`. The change is purely code cleanup - the behavior remains identical, but the implementation becomes more straightforward.

The commit message explains that the original code had a "weirdly coded check" that's now replaced with a more natural approach. The diff shows this removes 4 lines while adding 2, with the key functional check now being a single line. The accompanying comment is also updated to remove reference to the now-removed variable.

This appears to be a straightforward improvement from the merge-ort maintainer (Elijah Newren) himself, suggesting it's a clear win for code readability. As part of a larger series fixing subtle rename detection issues, this patch stands out as a simple cleanup that makes the more complex changes in other patches easier to review.