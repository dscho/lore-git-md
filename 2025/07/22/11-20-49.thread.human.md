Patrick Steinhardt's reflog migration series continues to evolve with detailed technical discussion about the handling of the `REF_HAVE_OLD` flag. The thread began with Patrick introducing infrastructure for migrating reflogs between storage formats (files and reftable), including a new `git reflog write` subcommand. The series has progressed through multiple versions, with recent focus on fixing old OID migration issues via a new `REF_LOG_USE_PROVIDED_OIDS` flag and refining flag behavior during log-only updates.

In the latest exchange, Jeff King noted an inconsistency in how `split_head_update()` and `split_symref_update()` handle the `REF_HAVE_OLD` flag. Patrick explains this is intentional - the flag needs to remain set for reflog-only updates to support upcoming changes, suggesting instead that `split_head_update()` should resolve the old OID when the flag isn't set. This ties into broader concerns about atomicity in transactions mixing log-only and ref updates, connecting to Jeff's recent work on dangling symrefs.

The discussion remains technical and focused, with Patrick, Jeff, and Junio Hamano working through edge cases in the ref transaction machinery. The series has been rebased onto v2.50.1 with object store API updates, and continues to address subtle behaviors in reflog migration as part of the larger reftable transition effort.