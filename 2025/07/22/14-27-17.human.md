This is a brief administrative follow-up in the performance optimization series converting commit traversal to use priority queues.

Junio Hamano acknowledges SZEDER Gábor's discovery of a patch ordering issue in the series - patch 3 (which adds the `prio_queue_replace()` function) needs to come before patch 2 (which uses that function) to avoid compilation errors. The issue is purely mechanical, with no technical changes needed to the patches themselves. 

The email also notes an amusing formatting artifact where a stray MIME-Version header appears in one of the commit subjects. This comes after the series had already received full technical approval from reviewers, with all performance characteristics and implementation details thoroughly vetted. The only remaining step is reordering the patches to resolve the build system dependency before merging.