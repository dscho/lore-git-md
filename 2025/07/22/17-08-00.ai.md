This is a performance optimization discussion in the ongoing `git blame` thread. Junio Hamano responds to Patrick Steinhardt's request for more justification of the proposed optimization, agreeing that concrete performance measurements and clearer documentation of the call site requirements would strengthen the patch. 

Junio raises an interesting counterpoint - if the optimization doesn't provide measurable benefits, we should consider removing the partial-parse capability entirely rather than maintaining code paths that don't meaningfully improve performance. This suggests the optimization's value proposition needs clearer validation before proceeding either way.

The discussion remains focused on the two key requirements Patrick identified: 1) benchmark data showing real-world speedups (particularly for large repos), and 2) explicit documentation of why `emit_other()` and `find_alignment()` don't need full commit details. Junio's response indicates these are reasonable asks that the original author should address in a v2 submission.

The thread continues to center around Git's performance optimization culture where even small changes require empirical validation. The partial parsing capability (from 2006's cee7f245d) appears underutilized, but whether to expand its use or remove it remains an open question pending better data.