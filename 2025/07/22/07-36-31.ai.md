This is a bugfix patch addressing a resource leak in the test-truncate helper. It's the first of four patches in a series fixing similar file descriptor leaks across test helpers and builtin commands.

The patch adds a missing `close(fd)` call in `t/helper/test-truncate.c` after the file truncation operation completes. Currently, the file descriptor remains open after the `ftruncate()` call, which could lead to resource exhaustion in test scenarios where the helper is called repeatedly. The fix is straightforward - just three lines added to ensure proper cleanup.

The change follows Git's standard pattern for resource cleanup, though the commit message doesn't specify whether this was found through code review, testing, or other means. The fix appears uncontroversial and well-contained, affecting only this test helper. No behavioral changes are introduced beyond the proper resource cleanup.

This is part of a larger series addressing similar leaks in other components (builtin/archive and test-delta), but each patch stands alone. The changes are minimal and focused, with no apparent impact on functionality beyond fixing the resource leaks.