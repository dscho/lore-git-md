This is a reply in an ongoing thread about file descriptor leak fixes, with Jeff King weighing in on the proposed patch series. 

King agrees with the uncontested fixes for test helpers (`test-truncate` and `test-delta`), noting they follow good practice by explicitly closing file descriptors even though the OS would reclaim them. He reiterates his opposition to the `archive.c` changes from earlier discussion, maintaining they should be dropped since cleanup before `die()` is unnecessary in Git's error handling philosophy.

The reply includes some technical aside about potential pitfalls when closing descriptors in error paths (errno clobbering), though King confirms these particular fixes avoid that issue. He also asks whether the leaks were found via static analysis tools, suggesting interest in the detection methodology. 

This continues the thread's division between uncontroversial test helper improvements and the debated `archive.c` changes, with King's position remaining consistent. The test helper portions could likely proceed independently if the author chooses to split the series.