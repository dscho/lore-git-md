Feature refinement for IMAP sent-mail archiving in `git send-email`

Aditya Garg responds to naming concerns about the IMAP folder feature, clarifying that the current implementation is intentionally flexible. The `sendemail.imapfolder` config and `--imap-folder` option can target any IMAP folder (Sent, Outbox, Drafts), not just sent-mail storage. This addresses Junio Hamano's concern about future extensibility by demonstrating the existing support for varied use cases.

Garg acknowledges the possibility of a future "IMAP-only" send mode but suggests this would require a separate boolean flag rather than complicating the folder parameter. While defending the current naming as sufficiently general, he remains open to alternative suggestions that might better capture the feature's purpose without limiting future IMAP-related enhancements.

This exchange represents the final naming discussion before potential merge, with no remaining technical objections. The thread has progressed from initial implementation through documentation fixes to this last semantic consideration. The patch appears ready for integration once this naming question is resolved, offering users a flexible way to archive sent emails via IMAP while maintaining SMTP delivery.