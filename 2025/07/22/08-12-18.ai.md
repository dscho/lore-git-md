Bugfix patch addressing a file descriptor leak in the test-delta helper. This is the third in a series fixing similar resource leaks across test helpers and builtins, following earlier fixes for test-truncate and archive.c.

The patch adds a missing `close(fd)` call in the error path of `cmd__delta()` when `fstat()` fails after a successful `open()`. The change follows the same minimal, style-preserving approach seen in previous patches - adding just the necessary cleanup while maintaining existing error reporting via `perror()`. The file descriptor in question is opened with `O_RDONLY` to read delta test input.

This continues the series' pattern of thorough error path auditing, now covering the delta test helper which is used in the test suite for exercising Git's delta compression routines. The change prevents potential file descriptor leaks that could occur during test failures, though in practice these would likely be caught quickly since they occur in test code. The patch maintains the existing error handling flow while properly closing the descriptor before returning.

Key technical details include the specific location of the fix (the `fstat` error path in `cmd__delta()`), the unchanged error reporting mechanism (`perror`), and the file access mode (`O_RDONLY`). The change is consistent with the series' focus on resource cleanup without modifying successful code paths or behavior.