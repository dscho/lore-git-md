This thread proposes removing the experimental status markers from `git-switch` and `git-restore`, which were introduced in 2019 but have seen stable use for six years. The two-patch series comes from [author name], with the first patch removing the `-m` short flag for `--merge` (while keeping the long form) and the second updating documentation to remove the experimental warnings.

The changes are straightforward: the implementation removes warnings from builtin/checkout.c and Documentation/*.adoc files, while preserving all other flag behavior including the commonly used `-c`/`--create`. The author argues the interface has stabilized sufficiently, noting minimal changes since introduction. The only open question is whether reviewers agree the commands are ready for finalization, particularly regarding the removal of the little-used `-m` short flag. 

No behavioral changes are proposed beyond removing the experimental markers and the `-m` flag. The series builds on commit 4e43b7ff that originally introduced these commands, representing a formalization of their now-mature status in Git's interface. Review feedback is still pending as this is the initial submission.