This is a performance optimization discussion for `git blame`, following up on Han Young's patch to skip unnecessary commit parsing. Patrick Steinhardt requests two key improvements to the patch's explanation: 

First, he asks for concrete performance measurements showing the speedup in realistic scenarios, particularly for large repositories where this optimization would matter most. Second, he suggests the commit message should explicitly explain why the two modified call sites (`emit_other()` and `find_alignment()`) don't need full commit details - while reviewers could deduce this, making it explicit would help evaluation.

The thread shows this is a straightforward optimization building on 2006 work (cee7f245d) that appears technically sound but could benefit from stronger justification. The changes are minimal (two call site modifications) and low-risk since they only affect code paths that already don't use the skipped information. This looks like a good candidate for inclusion once the author addresses the documentation requests.