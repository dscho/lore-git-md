This is part 2 of a 5-patch series introducing a new `git repo info` subcommand, focusing on adding reference format detection. The patch implements the `references.format` field to report whether a repository uses files or reftable for reference storage, leveraging `git rev-parse --show-ref-format` internally.

The implementation introduces a callback-based architecture where each field (currently just `references.format`) provides its value through a dedicated function. The code handles duplicate field requests gracefully and includes tests verifying correct output for both files and reftable formats. Documentation is updated to describe the new field.

The patch appears well-structured with clear commit message, thorough test coverage, and appropriate helper/mentor sign-offs. It builds on the scaffolding from patch 1/5 while maintaining the series' focus on read-only repository metadata queries. The implementation choice to use direct printing (rather than buffering) aligns with the architectural decisions established in the thread.