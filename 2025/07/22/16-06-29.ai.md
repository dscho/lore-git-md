This is a follow-up discussion about SHA-256 compatibility in the `git last-modified` feature series, specifically regarding the `--extended` format implementation.

Toon Claes responds to Junio Hamano's concerns about SHA-256 compatibility in the test suite for the `--extended` format feature. Junio had pointed out that the tests would fail with SHA-256 repositories due to hardcoded assumptions about hash lengths. While Toon acknowledges this oversight, he indicates this particular commit will likely be excluded from future rerolls of the series, implying the SHA-256 fixes will be handled differently in subsequent versions.

The exchange highlights ongoing attention to cross-hash-algorithm compatibility in the test infrastructure, though the specific technical discussion about the test implementation is deferred since this commit is being dropped. This interaction shows the maintainers' focus on ensuring the feature works correctly across different repository configurations, even as implementation details continue to evolve.