This is a follow-up discussion in the ongoing thread about removing experimental markers from `git-switch` and `git-restore`, specifically addressing the controversial proposal to remove the `-m` short flag.

Justin Tobler responds to Junio Hamano's objections about removing the `-m` flag from `git switch` and `git restore`. Junio had argued that `-m` is frequently used in his workflow and removing it would be disruptive. Justin acknowledges this feedback, admitting he had incorrectly assumed `--merge` was an uncommon operation. He suggests dropping the patch to remove the short flag if there are indeed common workflows relying on it.

The discussion also touches on the handling of the related `--conflict` flag. Junio had expressed concerns about splitting the option definitions between commands, arguing that keeping them in the common implementation makes the code clearer. Justin proposes a compromise where the option could remain in the common code but with a parameter to control short flag availability.

This exchange shows the author backing away from the more controversial aspects of the original proposal while still exploring ways to improve the code organization. The thread appears to be converging toward keeping the existing interface while focusing on the less contentious documentation updates.