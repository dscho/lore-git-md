This thread addresses a series of bugfixes for file descriptor leaks in Git's test helpers (`test-truncate`, `test-delta`) and the `archive` builtin. The first patch focuses on `test-truncate`, adding a missing `close(fd)` call after file truncation operations. While this helper is only used in test environments, the fix prevents potential resource exhaustion in long-running test suites.

The series appears straightforward, with minimal changes that preserve existing code style while ensuring proper resource cleanup. The patches target specific leak scenarios without introducing behavioral changes beyond fixing the resource management. Given the focused nature of these fixes and their limited impact to test environments, the series seems likely to generate much discussion unless reviewers spot additional cleanup opportunities. The remaining patches will presumably address similar issues in `test-delta` and the `archive` builtin.