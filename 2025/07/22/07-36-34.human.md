This is the fourth and final patch in a bugfix series addressing resource leaks in test helpers and builtin commands. The patch specifically fixes a file descriptor leak in the test-delta helper when `fstat()` fails after opening the third argument file. The fix is minimal - just two lines to ensure the file descriptor is properly closed on the error path.

The change follows the same pattern as patch 3/4 in the series, which fixed a similar leak in the same helper for a different error case. Both are straightforward correctness fixes that prevent potential resource exhaustion. The patch adds proper cleanup without changing any functional behavior.

This concludes Hoyoung Lee's systematic cleanup of file descriptor leaks found through code review. The entire series is quite small (about ten lines total across four files) and appears uncontroversial, with each patch standing alone to fix a specific leak. The changes demonstrate careful attention to resource management in error paths across different parts of the codebase.