Refactoring patch in the merge-ort rename detection series, specifically optimizing directory rename checks.

This patch simplifies the logic in `check_for_directory_rename()` by replacing a verbose key-check pattern with a direct `strmap_contains()` call. The change removes an unnecessary temporary variable (`otherinfo`) and streamlines the check for directory rename exclusions. The commit message explains this was a "weirdly coded" check that can be expressed more naturally with the available API.

The modification maintains all existing functionality while making the code more straightforward. The comment block above the changed code is also updated to remove reference to the now-removed variable while preserving the important explanation about directory rename exclusion logic and its relation to test cases 9c/9d in t6423.

This appears to be a straightforward cleanup with no behavior change, coming from the merge-ort maintainer (Elijah Newren) as part of a larger series addressing directory rename edge cases. The patch follows the series' first documentation update that was already reviewed and signed off by the same maintainer.