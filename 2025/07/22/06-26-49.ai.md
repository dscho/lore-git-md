This is a build system / patch series ordering issue report from SZEDER Gábor regarding René Scharfe's performance optimization series that converts commit traversal to use priority queues. The series had already completed technical review and was approved for merging, but Gábor points out a critical problem with the patch application order that causes build failures.

The key issue is that the patches were applied in reverse order of their dependencies: the third patch (which adds the new `prio_queue_replace()` function) needs to come before the second patch (which uses that function). This leads to an implicit function declaration error during compilation. Gábor also notes an unrelated formatting issue with a stray MIME-Version header appearing in one commit's subject line.

This email doesn't propose any changes to the technical content of the patches themselves - the optimizations and their performance characteristics remain as previously analyzed and approved. It's purely an administrative note about the mechanical process of applying the series. The thread context shows this was the final approved version of the patches, so this ordering issue would need to be corrected before merging, likely by either reordering the patches or having the maintainer adjust the application process.