Bugfix completing a series addressing file descriptor leaks in test helpers and builtins. This fourth patch fixes a leak in `test-delta.c` where a file descriptor opened for reading (`O_RDONLY`) would remain open if the subsequent `fstat()` call failed. The fix adds a `close(fd)` in the error path while maintaining the existing `perror()` error reporting.

The patch also includes a minor style improvement in `builtin/archive.c` from the same series, making the error handling for `dup2()` more symmetric by moving the `close(output_fd)` into the conditional block. Both changes follow the series' consistent approach of minimal, targeted fixes that preserve existing behavior while plugging resource leaks.

This concludes the series that has systematically addressed file descriptor leaks across test helpers (`test-truncate`, `test-delta`) and the `archive` builtin. The changes are straightforward and well-contained, demonstrating careful attention to resource cleanup in error paths without introducing any functional changes to successful execution paths.