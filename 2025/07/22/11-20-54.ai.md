This is a refactoring patch in Patrick Steinhardt's reftable migration series, though it addresses a general ident parsing issue rather than being reflog-specific. The change modifies `split_ident_line()` to use `size_t` for its length parameter instead of `int`, since all callers pass positive values (from `strlen()`, `strbuf::len`, or pointer diffs) and the signed type was misleading. The change is mechanical, touching both the declaration in ident.h and implementation in ident.c.

While not directly related to the reftable work, this cleanup likely surfaced during the reflog migration investigation since reflog entries use ident parsing. The patch follows good practice by updating both header and implementation simultaneously. The change is low-risk as it doesn't alter behavior - it just makes the interface more accurately reflect its actual usage patterns.

This appears to be a preparatory cleanup before the upcoming reftable-specific migration fixes in patches 6-8. The minimal diff suggests it was caught by code inspection rather than being bug-driven, though the type mismatch could theoretically cause problems with extremely large inputs. No test updates are included since this is purely a type signature change with no functional impact.