AI thread summary:

Performance optimization for `git blame` to reduce unnecessary commit parsing by skipping full commit information when only basic author data is needed. The patch modifies `builtin/blame.c` to pass `0` (basic info) instead of `1` (full details) to `get_commit_info()` at two call sites (`emit_other()` and `find_alignment()`), building on a 2006 optimization (cee7f245d) that wasn't fully utilized. Patrick Steinhardt requested two additions: 1) concrete performance measurements showing speedup impact for large repositories, and 2) explicit documentation of why these call sites don't need full commit details. Junio Hamano has now weighed in, supporting Patrick's requests while raising a broader architectural question - if the optimization doesn't provide measurable benefits, we should consider removing the partial-parse capability entirely rather than maintaining unused code paths. The discussion highlights Git's performance-sensitive culture where optimizations require empirical validation. Open questions remain: 1) actual performance impact measurements needed to justify either keeping and expanding the optimization or removing it, 2) clear documentation of why these specific call sites only need basic commit info, and 3) whether this partial parsing capability is worth maintaining at all if unused. The thread demonstrates how even small optimizations undergo rigorous scrutiny regarding both their immediate impact and broader architectural implications.