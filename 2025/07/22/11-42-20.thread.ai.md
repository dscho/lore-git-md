AI thread summary:

Performance optimization for `git blame` to reduce unnecessary commit parsing. The patch modifies `builtin/blame.c` to skip detailed commit information when only basic author data is needed, building on a 2006 optimization (cee7f245d) that wasn't fully utilized. Changes affect two call sites of `get_commit_info()`:
1. `emit_other()` for blame output
2. `find_alignment()` calculations
Both now pass `0` (basic info) instead of `1` (full details) since they only require author metadata. The change is low-risk as it targets code paths that explicitly don't need the skipped information. No performance measurements are provided, but the optimization should benefit large repositories. The patch appears ready for review with straightforward modifications to two call sites and no other changes needed.