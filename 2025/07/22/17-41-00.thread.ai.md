Test helper resource leak fixes (v3) - complete series

Jeff King's three-patch series modernizing test helper resource management is now complete with the addition of the final patch explicitly closing the output file descriptor in `test-delta`. The series addresses multiple issues across `test-truncate` and `test-delta` helpers: file descriptor leaks, memory leaks under SANITIZE=leak, integer truncation risks, and race conditions in file reading.

Key technical changes include:
- Conversion to `strbuf` APIs in `test-delta` (replacing manual buffer management)
- Standardized error handling via `die_errno()`
- Explicit file descriptor cleanup
- Removal of 53 lines of code while fixing subtle issues

The thread established consensus that test helpers can rely on process termination for cleanup while still properly handling memory leaks. Junio C Hamano and Eric Sunshine endorsed this approach, with Sunshine noting the original cleanup code was superficially correct but flawed. The final patch maintains consistency of the series by adding the missing `close()` with error checking, completing the modernization of `test-delta.c`.

All technical discussion has concluded with clear agreement on the approach. The series demonstrates improved resource management patterns that could serve as examples for other test helpers, while maintaining the practical balance appropriate for short-lived test programs.