This is the final patch in a series implementing diff context configuration support for interactive patch commands (add/checkout/commit/reset/restore/stash with --patch). The patch adds command-line overrides (-U/--unified and --inter-hunk-context) to complement the existing config-based diff.context and diff.interHunkContext support.

The implementation uses new OPT_DIFF_UNIFIED and OPT_DIFF_INTERHUNK_CONTEXT macros (justified by their six existing users) to handle command-line options consistently across commands. The patch extends the add_p_opt struct and adds validation in init_add_i_state() to match the config validation behavior. Documentation updates centralize the information in diff-context-options.adoc.

The thread has reached consensus on the technical approach, with Phillip Wood approving the implementation and Junio C Hamano indicating readiness to merge pending two test adjustments: restoring accidentally removed config-based behavior tests ('add -p respects diff.context' etc.) and removing redundant -U option coverage in t4055. Leon Michalak confirms understanding of these final changes needed before merge.

Key technical details include: options override config values, negative values are rejected consistently, and the implementation touches multiple builtins while maintaining consistency with other diff-generating commands. The series has modernized test infrastructure (45 grep/config call conversions) and addressed all major design concerns, with only these final test adjustments remaining before merge.