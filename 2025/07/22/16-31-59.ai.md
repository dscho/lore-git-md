This is a follow-up discussion in the IMAP sent-folder archiving feature thread, where Aditya Garg responds to Junio Hamano's defense of the feature's value proposition. The exchange focuses on whether the IMAP archiving capability justifies its implementation given existing alternatives.

Garg questions whether the feature's scope aligns with Git's needs, noting that users who can use IMAP could alternatively use SMTP directly. This introduces a new skepticism about the feature's necessity that wasn't previously voiced in the thread. The discussion shifts from implementation details (which were largely settled in v3) to a more fundamental question of whether the functionality belongs in Git at all.

Key technical context remains unchanged from the thread summary: the implementation uses `sendemail.imapSentFolder` config and preserves Bcc headers when archiving via existing `git imap-send` infrastructure. However, Garg's latest comment suggests reconsidering the premise that drove the previous three patch versions - whether the archiving-after-SMTP workflow provides enough unique benefit to warrant core Git support versus manual pipe alternatives.

This represents a potential regression in consensus, as the feature appeared merge-ready after naming and technical concerns were resolved in v3. The thread now faces a new philosophical debate about feature scope that could impact whether the series proceeds to merge. Junio had previously argued that `send-email`'s advanced features (CC management, cover letters) make archive-after-send preferable to manual pipe alternatives, but Garg now questions whether those advantages justify the implementation.