This is a response in the ongoing discussion about removing experimental markers from `git-switch` and `git-restore`. Junio C Hamano pushes back against two specific changes proposed in the patch series: removing the `-m` short flag for `--merge` and splitting the `--conflict` flag handling between commands.

Junio argues that removing `-m` would be disruptive to daily workflows, citing his own frequent use of `git checkout -m` (which would presumably translate to `git switch -m`). He questions the wisdom of removing a commonly used short flag to reserve it for a hypothetical future feature. 

On the technical side, Junio disagrees with restructuring the option handling to make `--conflict` command-specific, pointing out that the underlying implementation shares the same merge logic across `checkout`, `switch`, and `restore`. He suggests keeping the common option handling would better reflect the shared code paths and make the implementation more maintainable.

The response indicates these changes may be more controversial than initially anticipated, particularly the removal of `-m` which appears to have real-world usage despite being labeled as "uncommon" in the original patch. Junio's comments suggest he views these as unnecessary deviations from established behavior rather than improvements.

This email likely marks a turning point in the discussion where the original proposal will need to be revised or better justified to address these workflow and implementation concerns. The thread now has clear technical disagreements to resolve before proceeding with the experimental marker removal.