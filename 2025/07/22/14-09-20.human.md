This is a test patch that complements the ongoing reflog memory leak fix series. Jeff King adds a test case for per-ref reflog expiration configuration, a feature that has existed since 2008 but was never properly tested. The test verifies that `gc.refs/heads/root2/*.reflogExpire` correctly overrides the base `gc.reflogexpire=never` setting for matching branches.

The test creates four branches (two under `root1/` and two under `root2/`), sets different expiration policies, and confirms only the `root2/` branches have their reflogs expired. This would have caught the memory leak being fixed in the main patch series, as noted in the commit message. Junio Hamano provides a minor typo fix suggestion in the commit message ("individual" -> "individual refs").

This is a straightforward test addition that fills a long-standing gap in test coverage. It's particularly valuable as it exercises the same code paths where the memory leak was discovered, helping prevent future regressions. The test appears well-constructed with clear setup, execution, and verification steps.