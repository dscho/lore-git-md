This is the final patch in a series implementing diff context configuration support for interactive patch commands (like `git add -p`). The patch adds command-line overrides (`-U`/`--unified` and `--inter-hunk-context`) to control the diff context shown during interactive patching.

The thread has seen extensive review over multiple versions, with all major technical questions now resolved. The implementation is merge-ready pending two final test adjustments: restoring some accidentally removed config-based behavior tests and removing redundant test coverage. The patch builds on previous work that made interactive commands respect `diff.context` and `diff.interHunkContext` config settings.

Leon Michalak acknowledges the remaining test issues and plans to address them. The changes follow established patterns, with new OPT_DIFF_* macros handling the command-line options consistently across six different commands. The series represents a complete solution after incorporating feedback from Phillip Wood and Junio Hamano, who have both approved the approach.

This concludes a well-executed effort to provide unified control over diff context in interactive operations, with both config and command-line options available. The implementation is thorough, with modernized test infrastructure (45 grep/config call conversions) and comprehensive documentation.