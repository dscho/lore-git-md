This is a technical discussion about Bloom filter integration in the `git last-modified` command, focusing on implementation details and performance test refinement.

The email continues the thread about optimizing `git last-modified` with Bloom filters, specifically addressing Taylor Blau's feedback on the v5 implementation. Toon Claes responds to several points:

1. **Performance test methodology**: Agrees with Taylor's observation that the subdirectory test case (8020.3) may be skewed by using the lexicographically first subdirectory (".github") and suggests making this selection more representative.

2. **Memory management**: Confirms the safety of `clear_bloom_key()` calls in both `last_modified_release()` and `mark_path()`, noting they won't cause double-frees due to `FREE_AND_NULL()` usage.

3. **Commit-graph preparation**: Discusses the current hack of using `generation_numbers_enabled()` to prepare commit-graphs and agrees a proper `prepare_commit_graph()` function would be cleaner.

4. **Generation number check**: Questions the purpose of checking `commit_graph_generation()` in `maybe_changed_path()`, noting this check exists in the original blame.c implementation but its rationale isn't clear from git history.

The discussion remains focused on final polishing of the Bloom filter integration, with particular attention to test accuracy and internal API usage. Toon demonstrates careful consideration of edge cases while maintaining the 50.6% speedup for top-level checks and 36.4% for subdirectory cases mentioned in previous performance results.