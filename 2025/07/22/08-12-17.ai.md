This is a bugfix patch addressing a file descriptor leak in the `archive` builtin. It's the second in a series of four patches fixing similar resource leaks across test helpers and builtins.

The patch fixes a case in `builtin/archive.c` where a file descriptor opened with `xopen()` could leak if the subsequent `dup2()` call fails. The change adds a missing `close(output_fd)` call in the error path, maintaining the existing error handling pattern that calls `die_errno()`. The fix is minimal and focused - just one line added to properly clean up resources in the failure case.

This follows the same pattern as the first patch in the series (which fixed test-truncate), showing consistent attention to resource cleanup in error paths. The change is straightforward and unlikely to be controversial, though reviewers might want to verify there are no other similar leaks in nearby code. The patch preserves all existing behavior while preventing potential exhaustion of file descriptors in edge cases.

Key technical details for future reference: the fix touches `builtin/archive.c`'s `create_output_file()` function, specifically the error path after `dup2()` fails. The file descriptor in question is opened with mode 0666 and O_CREAT|O_WRONLY|O_TRUNC flags. The patch maintains the existing error reporting via `die_errno()`.