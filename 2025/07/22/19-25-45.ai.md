This is a discussion about the implementation approach for the `git repo info` subcommand's field system, specifically focusing on patch that adds the `references.format` field. The email comes from Justin Tobler, a contributor who has previously helped with the series.

The discussion centers on two key implementation choices in the patch:

1. The callback-per-field architecture, where each key has a dedicated function to retrieve its value. Justin notes this might become inflexible for future cases where a single operation could generate multiple related fields (like reference statistics), though acknowledges it may be fine for the current `info` subcommand's simpler needs.

2. The behavior when no keys are specified - currently the command silently does nothing. Justin questions whether this is the best default, suggesting alternatives like erroring out or implicitly showing all available fields.

The technical details show the current implementation uses:
- A `struct field` mapping keys to callback functions
- Binary search (via `bsearch`) for key lookup
- Lexicographical ordering of both the internal field table and output
- Error handling for unknown keys

This is part of a larger series (v5) implementing the `git repo` command hierarchy, with this patch specifically adding the first concrete field (`references.format`) to demonstrate the field system. The thread context shows the series has since expanded to include three fields and two output formats, with ongoing discussion about the machine-readable format's separator handling.

For future AI context: The discussion highlights potential architectural tension between the current simple callback-per-field approach and future needs for batch field generation, particularly relevant for the planned `repo stats` subcommand. The silent no-keys behavior also remains an open question.