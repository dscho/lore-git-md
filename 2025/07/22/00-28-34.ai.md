This is the third implemented field in the `git repo info` series, adding shallow repository detection. The patch introduces `layout.shallow` which reports whether the repository is shallow via `is_repository_shallow()`, following the same pattern as the previous `layout.bare` field.

The implementation reuses the established field system architecture, adding a new callback `get_layout_shallow` that returns "true"/"false" strings. The field is inserted in lexicographical order in the fields array alongside `layout.bare` and `references.format`. Test coverage includes both shallow and non-shallow cases, plus verification that multiple fields can be queried together.

The change demonstrates the field system's flexibility to handle different types of repository metadata while maintaining the series' consistent approach. Documentation is included for the new field in git-repo.adoc. The implementation appears clean and follows the project's style, with comprehensive tests covering the new functionality.

Key technical details:
- Uses `is_repository_shallow()` from shallow.h
- Returns static strings like other boolean fields
- Maintains sorted field array ordering
- Tests include clone --depth scenario for shallow=true
- Verifies multi-field output format
- Updates documentation with new field

This continues the steady progress of the series' field-by-field implementation approach, with each patch demonstrating another working piece while maintaining architectural consistency. The change is straightforward and well-tested, following the patterns established in earlier patches.