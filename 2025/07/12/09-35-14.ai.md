This is a refactoring patch in the bloom filter optimization series, specifically focused on function naming conventions. The patch renames two key bloom filter functions to follow Git's code style guidelines where functions operating on a struct should be named `structname_verb()`. The changes are mechanical but widespread, touching multiple files that use these bloom filter APIs:

1. `fill_bloom_key()` becomes `bloom_key_fill()`
2. `clear_bloom_key()` becomes `bloom_key_clear()`

The changes propagate through bloom.c/h, revision.c, blame.c, line-log.c, and test-bloom.c, updating all call sites. The function signatures remain identical - only the names change to match Git's conventions. This is part of the larger bloom filter optimization work that introduces `struct bloom_keyvec` for handling multiple pathspecs, but this particular patch is purely about naming consistency rather than functional changes.

The patch maintains all existing behavior while making the API more consistent with Git's style. It's a straightforward change that has already been reviewed in previous iterations of the series, with no outstanding technical concerns. The changes are mechanical enough that they're unlikely to introduce new bugs, though they do touch several components that use bloom filters.