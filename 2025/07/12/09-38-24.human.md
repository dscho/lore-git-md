This is a technical follow-up in the ongoing discussion about `gpg.program` path expansion behavior. Andreas Schwab responds to Junio Hamano's argument that `gpg.program` should accept full command lines rather than just paths, demonstrating that Git's current implementation doesn't actually support command-line arguments in this configuration value.

The email shows a concrete test case where setting `gpg.program` to "echo --compat=gnupg" fails because Git tries to execute the entire string as a single command path rather than parsing it as a command with arguments. This contradicts Hamano's earlier suggestion that the configuration could be used for full command specifications.

The exchange highlights a deeper implementation issue: while Hamano argued the configuration should conceptually support command lines, the actual code doesn't handle argument parsing at all. This moves the discussion from a philosophical debate about configuration types to a concrete technical limitation in the current implementation. The thread remains unresolved but now has clearer technical grounding about what the code actually does versus what participants think it should do.