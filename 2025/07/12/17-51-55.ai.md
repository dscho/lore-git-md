This is a build system discussion about PCRE2 dependency handling in the meson configuration. The thread began with a patch to disable PCRE2 by default in meson builds to match Makefile behavior and address macOS-specific issues with system PCRE2 libraries. Brian m. carlson now questions whether the change should be macOS-specific rather than global, noting that Linux systems typically have working system PCRE2 libraries and users expect PCRE support by default there.

The core technical point revolves around platform differences in PCRE2 availability and quality. macOS ships with a broken PCRE2 implementation (missing headers despite passing initial checks), while Linux systems generally provide a working version. The original patch proposed a blanket disable, but this new response suggests a more targeted approach that would maintain PCRE2-by-default behavior on Linux while only disabling it on problematic platforms like macOS.

No code changes are proposed in this email - it's purely a discussion point about the scope of the configuration change. The thread now has two clear positions: the original "disable everywhere" approach for consistency with Makefile/cmake, versus this new "platform-specific defaults" suggestion that would better match user expectations on Linux. The discussion may need input from macOS users about their typical PCRE2 installation practices, and from Linux users about whether they'd notice or care about the difference in default builds.