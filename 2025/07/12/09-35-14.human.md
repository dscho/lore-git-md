This is a refactoring patch in Lidong Yan's v6 series optimizing pathspec bloom filters. It renames two bloom filter functions to follow Git's naming conventions: `fill_bloom_key()` becomes `bloom_key_fill()` and `clear_bloom_key()` becomes `bloom_key_clear()`. The changes are mechanical but touch several files where these functions are called, including bloom.c/h, blame.c, line-log.c, and revision.c.

The patch is straightforward and uncontroversial, aligning with the project's established style guidelines. It's part of a larger performance optimization effort that has already received Reviewed-by tags from key maintainers. The changeset shows good attention to detail by updating all call sites consistently, including test helper code. This is a minor but necessary step in the series' goal of improving bloom filter handling for multiple pathspecs.