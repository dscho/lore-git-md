Thread subject: meson build system change for PCRE2 dependency handling. Patch series modifies meson build configuration to address PCRE2 dependency issues across platforms, with distinct solutions for macOS (broken system installations) and Windows (subproject fallback behavior). Current implementation uses `compiler.has_header()` verification for active detection.

Core problem spans immediate build system solutions and broader regex architecture questions. Technical details: meson.build now properly detects broken system PCRE2 installations via header verification (macOS solution). Eli Schwartz's clarification corrects previous misunderstanding about Windows fallback - meson's `dependency()` function only falls back for *required` dependencies by default, with specific conditions (system found OR wrap with internet/local download available). This provides the missing piece for Windows-specific handling of 'auto' default, suggesting built-in fallback mechanism may suffice without requiring `-Dpcre2=enabled`.

Implementation maintains static linking preference via `default_library=static`. Platform-specific behaviors: error when PCRE2 required but broken (macOS), submodule fallback when possible (Windows using corrected understanding of meson's fallback logic), or graceful disable with warning in auto mode. Default in meson_options.txt changed from 'enabled' to 'auto' with now-understood Windows implications.

Open questions resolved regarding Windows fallback behavior - solution path now clear using meson mechanisms. Original architectural questions (modular isolation vs overhaul, long-term regex strategy) remain as separate future work. The solution may still inspire meson `dependency()` function enhancements for header validation.

Participant positions: author implemented core solution; Patrick Steinhardt and Eli Schwartz's technical approach adopted; Carlo Arenas provided both macOS verification and Windows regression discovery (now corrected by Schwartz); Junio's architectural vision noted for future work. Related work: broader build system consistency efforts, historical PCRE2 adoption discussions. Thread has completed primary meson configuration resolution with Windows fallback behavior now properly understood.