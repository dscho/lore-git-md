This is a platform compatibility response regarding Git builds on RHEL 6. Ramsay Jones weighs in on the ongoing thread about compilation failures, clarifying Git's official support policy and providing a workaround for the `getrandom()` linking error.

The email makes two key points: First, it notes that RHEL 6 is likely too old to be officially supported (with RHEL 8 being the current baseline). Second, it provides a concrete solution for the immediate `getrandom()` issue - setting `CSPRNG_METHOD=` during the build process to avoid the unsupported function call.

The response is helpful but firm, discouraging the practice of copying newer system headers to older systems. While the thread has progressed through several technical layers (header issues, macro syntax, now system call compatibility), this reply focuses narrowly on the cryptographic random number generation problem while setting expectations about platform support. The workaround suggests this might be treated as a best-effort fix rather than something that would be merged upstream.