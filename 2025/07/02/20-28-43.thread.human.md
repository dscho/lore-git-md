This thread began with Eric Salem reporting unexpected behavior in `git log --diff-filter`, where lowercase exclusion filters (like `d` to exclude deletions) weren't working as expected with minimal output formats. Initial responses suggested this was expected behavior, with diff filters only applying when Git computes diffs. However, Jeff King's investigation revealed this was actually a regression from a 2022 change (commit 75408ca949), where exclusion filters previously worked independently of output format.

The discussion has now clarified that exclusion filters like `--diff-filter=d` should omit commits containing deletions from the output entirely, not just filter the diff output. This contradicts earlier assumptions and confirms a behavioral regression. Jeff maintains this affects all exclusion filters (a-z) with minimal output formats, not just deletions. The thread has shifted from documentation clarification to identifying a concrete bug, with the regression traceable to the 2022 change. Workarounds exist (using `--name-only` with `grep` or `--stat` with complex filtering), but the core issue likely requires code changes. The discussion now awaits input from the original author of the suspect commit to determine whether to revert or amend it. The interaction between output formats and diff computation is proving more nuanced than initially understood.