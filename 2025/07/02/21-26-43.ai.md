This is a bugfix series addressing `git apply --intent-to-add` behavior, now reduced to 4 patches after removing the initial error-handling patch. The series fixes incorrect index handling that made `-N` (intent-to-add) non-functional except in empty repositories.

The v2 series makes several key changes from v1: drops the first patch about erroring outside gitdir (now silently ignored), expands commit messages with more technical rationale, and fixes a formatting issue in the test patch. The core fixes remain:
1. Proper index reading in ITA mode via `read_apply_cache()` (patch 1)
2. Precise conditions for writing ITA markers (patch 2)
3. Expanded test coverage (patch 3)
4. Documentation clarifications (patch 4)

Technical focus remains on patch 2's index update conditions, where Raymond clarifies the relationship between `state->apply`, `check_index`, and `ita_only` flags. The tests now verify more edge cases including interaction with existing index entries and complex patches. Documentation updates make clear that `--intent-to-add` is repository-bound and interacts with other index-affecting flags.

The series appears ready for final review pending resolution of the remaining discussion points from v1, particularly around the index update conditions in patch 2. Test coverage is thorough (31 new lines in t4140) and the documentation improvements are straightforward. The removal of the error-handling patch suggests the author has accepted Junio's preference for silent ignore behavior outside gitdir.