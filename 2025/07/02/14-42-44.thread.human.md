This thread addressed a bug in `git gc --auto`'s memory detection on BSD systems, where improper handling of `sysctl()` calls could lead to incorrect memory calculations. Carlo Marcelo Arenas Belón's patch series went through three iterations to resolve the issue, with key input from Patrick Steinhardt and Junio Hamano.

The v3 patch introduced robust fixes including proper initialization of `physical_memory`, length verification, and removal of problematic `HW_MEMSIZE` usage that caused FreeBSD regressions. It also changed the variable type from `int64_t` to `uint64_t` to prevent sign extension issues and added explicit handling for partial `sysctl()` returns. In a follow-up discussion, Carlo clarified some technical points for Junio regarding 32-bit system behavior and compiler warnings, confirming these edge cases were properly considered in the final solution.

The changes maintain compatibility across BSD variants while fixing the specific FreeBSD issues, with all review feedback addressed. The implementation in `builtin/gc.c` now handles edge cases robustly while keeping the code simple and safe. Junio had previously signaled this was ready for inclusion, and the final clarifications confirm there are no outstanding issues. The thread demonstrates how careful iteration and review produced a complete solution for accurate memory detection across BSD platforms.