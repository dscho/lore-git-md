This is a follow-up discussion in the performance optimization series for pathspec bloom filters, specifically addressing case sensitivity handling and patch organization.

Junio Hamano responds to Lidong Yan's agreement that PATHSPEC_ICASE shouldn't use bloom filter optimizations, suggesting a deeper consideration of how case-insensitive systems might still benefit from bloom filters if paths are normalized during filter creation. He notes this would require checking the current implementation's handling of case sensitivity based on core.ignorecase.

The email also addresses patch maintenance questions from the v3 series. Hamano recommends fixing the known parameter alignment issues in a reroll, but advises keeping the nowildcard_len optimization as a separate future commit rather than expanding the current series. This maintains focus on the core bloom keyvec changes while allowing the additional optimization to be developed independently after the current patches stabilize.

Key technical points mentioned:
- Potential case sensitivity handling through path normalization during bloom filter creation
- Current repository-wide case sensitivity determined by core.ignorecase
- Two unaligned parameters in the v3 implementation that should be fixed
- The nowildcard_len optimization as a distinct future change

The discussion continues the series' focus on bloom filter optimizations while addressing edge cases (PATHSPEC_ICASE) and practical patch series management considerations. The technical exchange shows the patch series is nearing completion with only minor organizational questions remaining.