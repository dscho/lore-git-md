This is a follow-up discussion in the BSD platform compatibility thread about memory calculation fixes for `git gc --auto`. Carlo Marcelo Arenas Belón clarifies his reasoning for initializing the `physical_memory` variable in the BSD-specific sysctl() handling.

The exchange focuses on a technical nuance from the v2 patch series - whether zero-initializing the variable is truly necessary given that the value is only used when sysctl() succeeds. Carlo explains his defensive programming approach: even if sysctl() returns success (0), there could be partial writes due to type size mismatches on some platforms, and initialization ensures predictable behavior on little-endian systems.

This reply doesn't introduce new code changes but provides important context about the safety considerations behind the existing patch. The discussion remains focused on edge case handling in the core BSD memory detection fix remains uncontested. The technical back-and-forth demonstrates careful attention to platform-specific behavior details, though the practical impact of this particular initialization question appears minor.