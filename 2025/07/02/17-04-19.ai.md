This is a technical follow-up in the BSD platform compatibility thread. Carlo Marcelo Arenas Belón clarifies the reasoning behind initializing the `physical_memory` variable in the BSD `sysctl()` memory detection code. The key point is that while the value is only used when `sysctl()` succeeds (returns 0), there could still be partial writes due to type size mismatches - particularly on little-endian systems where uninitialized portions of the variable might be read. The initialization ensures predictable behavior in these edge cases.

The email doesn't introduce new code changes but provides important context about the v2 patch's design decisions. It confirms the initialization is primarily about handling potential partial writes from `sysctl()` rather than guarding against failure cases, addressing Patrick Steinhardt's earlier question about the safety justification. The discussion remains focused on BSD-specific memory detection edge cases, with no new technical objections raised to the current approach.