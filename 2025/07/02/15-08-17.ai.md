This is a discussion thread continuation regarding the SHA-256 transition, specifically about how commands should behave when operating outside a repository. The email focuses on patch 3/10 from the series, which changes such commands to use GIT_HASH_DEFAULT rather than hardcoding SHA-1.

Patrick Steinhardt responds to brian m. carlson's position that commands like `index-pack` and `ls-remote` should use the new default hash algorithm (SHA-256 when built with WITH_BREAKING_CHANGES) rather than falling back to SHA-1. While Patrick acknowledges the initial inconvenience of requiring explicit format specification during transition, he concedes that brian's approach will become more useful over time as SHA-256 adoption grows.

Key technical points:
- The debate centers on whether external commands should use GIT_HASH_DEFAULT (brian's position) or GIT_HASH_SHA1_LEGACY (Patrick's original suggestion)
- Commands affected include repository-object operations like `index-pack` and `ls-remote`
- Patrick now accepts brian's argument about long-term usability outweighing short-term transition pain

This email marks a partial resolution to one of the thread's open questions, with Patrick softening his position on external command behavior while not fully retracting his original concerns. The discussion remains technically approved but leaves the external command debate as the last significant point of contention in the series.