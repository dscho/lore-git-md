This is a follow-up discussion about the BSD platform compatibility fix for `git gc --auto` memory calculations. Patrick Steinhardt questions some technical details in Carlo Marcelo Arenas Belón's patch that corrects the `sysctl()` handling for BSD systems.

The key points raised are:
1. The patch claims to make `physical_memory` safe to use if `sysctl()` fails, but Patrick notes the code only uses the value when `sysctl()` succeeds (returns 0), making the initialization redundant for safety.
2. Patrick questions whether `sysctl()` could ever return 0 without writing to the output parameter, though notes the initialization doesn't hurt.
3. He acknowledges the added `uint64_t` cast is consistent with other branches, though not strictly necessary.

This is probing the technical justification for parts of the BSD memory detection fix rather than proposing changes. The discussion focuses on implementation details and documentation accuracy rather than the core fix's validity. The thread shows careful review of edge cases in system call behavior.