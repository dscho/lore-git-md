This is a documentation patch that concludes a series fixing `git apply --intent-to-add` behavior. It clarifies the documentation for the `--intent-to-add` option, removing a double negative and making the distinction between `--index` and `--cached` modes clearer.

The patch simplifies the wording to state that `--intent-to-add` is ignored when either `--index` or `--cached` are used, rather than the previous phrasing about what it's "not ignored" under. This matches the technical fixes in the earlier patches of the series, which properly handle these interaction cases.

As the final patch in a v2 series that has already addressed core technical issues with index handling, this appears to be a straightforward documentation improvement that aligns with the code changes. The series as a whole has been refined based on feedback, with this patch completing the picture by ensuring the docs match the actual behavior.