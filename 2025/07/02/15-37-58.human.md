This is a technical discussion about Unicode normalization handling in Git's filesystem operations, particularly relevant to the ongoing `git pull --prune` case-sensitivity issues.

Junio C Hamano clarifies how Git currently handles Unicode normalization on macOS systems, explaining that paths from both the command line (via `argv[]`) and filesystem operations (via `opendir`/`readdir`) are normalized through Git's compatibility layer before reaching core code. This normalization happens in `compat/precompose_utf8.c`, converting between NFD (UTF-8-MAC) and NFC forms.

The email responds to Chris Torek's concern about potential issues with German umlaut variations in refnames, explaining that Git's infrastructure already handles such normalization consistently. This provides important context for the broader discussion about case-insensitive filesystem handling, showing that Unicode normalization is already addressed separately from the current case-folding problems being discussed in the thread.

The note serves to prevent potential scope creep in the `--prune` discussion by showing that Unicode normalization is already handled at a different layer of the system, though it doesn't directly advance the core case-sensitivity solution under debate.