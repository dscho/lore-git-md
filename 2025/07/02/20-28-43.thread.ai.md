Here is the updated AI thread summary:

Bug report from Eric Salem about unexpected behavior in `git log --diff-filter` where lowercase exclusion filters (like `d` to exclude deletions) fail to work with default output formats. Jeff King (peff) has established this as a regression from commit 75408ca949 (2022), contradicting K Jayatheerth's initial assessment that the behavior was expected. 

Key technical details: exclusion filters (a-z) should affect commit selection regardless of output format, not just filter diff output. The regression causes these filters to only work when Git computes diffs, breaking the expected behavior where `--diff-filter=d` would omit commits containing deletions entirely. This affects all exclusion filters with minimal output formats. Jeff's analysis shows this worked correctly in Git 2.50.0 and earlier.

Current status: The thread has moved from documentation clarification to identifying a concrete regression. Two workarounds exist (using `--name-only` with `grep` or `--stat` with complex filtering), but the core issue requires code changes. The discussion now awaits input from the original author of commit 75408ca949 to determine whether to revert or amend it.

Open questions: The interaction between output formats and diff computation is more nuanced than initially understood. The regression affects the fundamental behavior of exclusion filters, which should operate at commit selection level according to Jeff's analysis. The thread needs resolution on whether to restore pre-75408ca949 behavior or document the current behavior as intentional (though Jeff argues against this).