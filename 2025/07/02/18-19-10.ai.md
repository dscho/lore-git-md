This is a follow-up discussion about clang-format's impact on recently merged code, specifically the `--skip-until` feature for `git for-each-ref`. The thread has shifted from the original pagination feature to code formatting concerns.

Karthik Nayak responds to Junio Hamano's observations about problematic clang-format changes to the merged code. The key points are:
1. The formatting tool produces undesirable changes to array initializers and commented-out code in refs.h
2. The "RemoveBracesLLVM" style rule creates non-standard formatting for if/else blocks that doesn't match Git's style
3. Karthik proposes removing problematic rules to reduce false positives before enforcing clang-format

The discussion includes specific examples of bad formatting in:
- The `for_each_ref_usage` array in builtin/for-each-ref.c
- Option definitions in the same file
- Ref iterator code in refs.c
- Function declarations in refs.h

Karthik suggests stripping out problematic rules to get to zero false positives before considering enforcement. This represents a tactical retreat from earlier formatting ambitions to focus on establishing a usable baseline. The exchange shows the practical challenges of applying automated formatting to Git's codebase while maintaining readability and style consistency.