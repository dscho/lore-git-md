This is a follow-up discussion about clang-format's behavior on recently merged code, specifically touching the `for-each-ref` changes that added `--skip-until` functionality. Karthik Nayak responds to Junio Hamano's observations about problematic clang-format output, agreeing that some of the formatting changes are undesirable.

The email focuses on clang-format's handling of:
1. Array initialization (unnecessarily reformatting the entire `opts[]` array in `for-each-ref.c`)
2. Commented-out code in `refs.h`
3. Brace placement (notably the `RemoveBracesLLVM` rule producing non-standard formatting for `if/else` blocks)

Karthik proposes removing the problematic `RemoveBracesLLVM` rule entirely from the clang-format configuration to reduce false positives, with the goal of eventually enforcing clang-format more widely. The attached diff shows examples of both the undesirable formatting and Karthik's suggested configuration changes.

This is a maintenance discussion rather than a feature or bugfix - it's about improving the tooling workflow around code formatting, specifically for the recently merged pagination feature in `git for-each-ref`. The exchange shows careful attention to code style consistency even after a feature has been merged.