This is a refactoring patch that cleans up the interface for `refs_warn_dangling_symrefs()` as part of the ongoing performance optimization series addressing O(N^2) behavior in `git fetch --prune`. The patch moves format strings inside the function rather than passing them as parameters, making the interface more robust by eliminating potential printf-format mismatches. It replaces the fragile format-string parameter with a simpler `indent` string and `dry_run` flag, which are used to construct the appropriate warning message internally.

The changes affect four files: builtin/fetch.c, builtin/remote.c, refs.c, and refs.h. The key modifications include removing duplicate format strings from the callers (fetch.c and remote.c) and consolidating the message generation logic within refs.c. The warning messages now consistently include proper newlines and maintain the same semantic content while being more safely constructed. The patch maintains backward compatibility in warning output format while making the code more maintainable.

This cleanup follows the earlier performance optimizations in the series, showing attention to both algorithmic efficiency and API design quality. The changes are focused and mechanical, with no behavior changes beyond the internal implementation details of warning message generation. The patch demonstrates good awareness of the codebase's evolution, removing now-unnecessary parameters that were made obsolete by earlier changes in the series.