This is a follow-up discussion about the `git last-modified` command's path handling behavior, specifically regarding directory vs. file output in non-recursive mode. The thread context shows this is part of the final polishing phase for the new plumbing command that tracks file modification history.

Toon Claes confirms an inconsistency between `git ls-tree` and `git last-modified` behavior when given directory paths in non-recursive mode. While `git ls-tree` shows the directory entry itself (tree object), `git last-modified` currently shows modification hashes for the directory's contents instead. This matches Kristoffer Haugsbakk's earlier observation that revealed the unexpected behavior.

The email includes concrete test case comparisons showing the commands' outputs for mixed file/directory pathspecs. Toon acknowledges this as a bug to address in the next version, aligning with Junio Hamano's earlier confirmation that the command should follow `git ls-tree` conventions for path handling.

This discussion resolves one of the remaining interface design questions in the thread, specifically confirming that directory paths in non-recursive mode should show the directory entry rather than its contents. The fix would complete the path handling behavior to match established Git plumbing conventions.