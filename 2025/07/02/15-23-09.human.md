This is a follow-up discussion about a BSD platform compatibility fix for `git gc --auto` memory calculations. Patrick Steinhardt questions some details of Carlo Marcelo Arenas Belón's patch that corrects type handling in BSD `sysctl()` calls.

The main point of contention is whether zero-initializing the `physical_memory` variable is actually necessary, since the code only uses the value when `sysctl()` returns success (which is indicated by zero). Patrick finds the justification in the commit message puzzling, though acknowledges the initialization doesn't hurt. He also notes the added type cast is consistent with other code paths but not strictly required.

The exchange highlights a minor technical clarification about BSD system call behavior rather than any fundamental issue with the patch. The discussion remains focused on implementation details, with Patrick providing a careful reading of both the code and its explanation. No new changes to the patch are suggested at this point.