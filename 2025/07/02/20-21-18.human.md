Bugfix for BSD system memory detection in `git gc --auto`. This is the third version of a patch series addressing incorrect RAM calculations on BSD systems when determining whether to automatically run garbage collection.

Carlo Marcelo Arenas Belón's patch fixes edge cases in the BSD-specific `sysctl()` calls used to detect total system memory. The key changes in v3 add proper handling for cases where the system returns fewer bytes than expected (32-bit values on 64-bit systems). The patch ensures unused bytes are properly discarded through bit-shifting operations, making the calculation robust across different BSD variants.

The thread history shows this has been carefully reviewed, with v2 already receiving positive feedback from both Patrick Steinhardt and Junio Hamano. This version appears to be a minor refinement addressing a remaining technical edge case. The changes remain focused on the BSD-specific path in `builtin/gc.c`'s `total_ram()` function.

Given the thorough review process and the narrow, platform-specific nature of the fix, this appears ready for inclusion. The patch demonstrates attention to cross-platform compatibility details that matter for Git's portable codebase.