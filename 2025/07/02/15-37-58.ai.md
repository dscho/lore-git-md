This is a technical discussion about Unicode normalization in Git's handling of case-insensitive filesystems, continuing the thread about `git pull --prune` failures. Junio C Hamano responds to Chris Torek's concerns about Unicode normalization on macOS by explaining Git's existing precompose_utf8.c compatibility layer. The system normalizes paths from both argv[] and filesystem operations (opendir/readdir) before Git's core code processes them, ensuring consistent handling of decomposed vs precomposed Unicode characters. This affects how Git records paths in the index and tree objects tree objects. The exchange clarifies that while the thread's initial focus was case-folding, Unicode normalization presents additional edge cases that Git's filesystem interaction layer already handles in a standardized way. This provides important context for the ongoing discussion about reference handling strategies, though it doesn't directly impact the current reftable-focused solution path.