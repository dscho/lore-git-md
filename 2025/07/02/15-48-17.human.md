This is a performance optimization thread discussing bloom filter handling for multiple pathspecs. Junio Hamano responds to Lidong Yan's v3 patch series, focusing on two key points:

First, they clarify that PATHSPEC_ICASE (case-insensitive matching) should not use bloom filter optimizations, though there's a question about whether case-normalized bloom filters could help on case-insensitive filesystems. Junio notes this would need investigation into how bloom filters are currently built on such systems.

Second, regarding the patch series itself, Junio advises fixing the known parameter alignment issues in the current patch but suggests holding off on the `nowildcard_len` optimization changes until after the current series is finalized. This keeps the scope focused while allowing incremental improvements.

The exchange shows the patch series is nearing completion, with only minor cleanups and organizational decisions remaining. The technical approach appears sound, with the main outstanding question being about case sensitivity handling in bloom filters.