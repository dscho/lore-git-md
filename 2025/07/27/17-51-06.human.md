This is the foundational patch for a new feature introducing the `git repo` command hierarchy, part of a GSoC project to better organize repository metadata access currently scattered in `git rev-parse`. The patch establishes the basic command structure with an initial `info` subcommand that will eventually house repository metadata queries.

The implementation creates all the boilerplate needed for a new Git command: documentation stub, build system integration, and a minimal implementation skeleton in `builtin/repo.c`. The documentation clearly marks this as experimental while outlining the planned purpose - retrieving repository-level information through key-based queries. The patch has multiple sign-offs from mentors and contributors, indicating it's been through prior review.

This is patch 1/5 in v5 of the series (though the thread context shows it's now at v6 with additional refinements). The series aims to create a more logical home for repository inspection functionality while maintaining backward compatibility. The implementation approach appears methodical, starting with this structural foundation before adding actual query capabilities in subsequent patches.