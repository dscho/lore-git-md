This is a follow-up in the git-daemon regression thread, focusing on debugging the Fedora-specific behavior where tilde expansion fails for user paths.

Jeff King responds to Todd Zullinger's SELinux hypothesis, noting it explains why the issue wasn't reproducible on his Debian system. He remains puzzled by the specific error message Russell King reported ("not in directory list" rather than a repository-not-found error), suggesting the path validation in `daemon.c:path_ok()` is passing but the safe-directory check is failing. 

The key technical detail is Jeff's observation about the error message sequence implying `enter_repo()` succeeds while `ok_paths` validation fails, which may indicate a subtle interaction between path expansion and permission checking. He invites further debugging but acknowledges Todd's SELinux findings likely provide the practical solution.

This exchange highlights the environment-specific nature of the bug (Fedora's SELinux policies vs Debian's defaults) while keeping open a small question about Git's internal path validation logic that might warrant future investigation, though not blocking the immediate fix.