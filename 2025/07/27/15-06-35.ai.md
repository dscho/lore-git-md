This is a follow-up to the bash completion feature patch introducing configurable ref sorting. The author Nelson Benítez León provides a minor correction to the original submission, noting that the example "git checkout " in the cover letter should have shown tab completion (as "git checkout <TAB>") which was apparently stripped by GitHub's email processing.

The core functionality remains unchanged from the initial proposal: adding the `GIT_COMPLETION_REFS_SORT_BY_FIELDNAME` configuration variable to customize ref sorting in bash completions, with alphabetical (`refname`) remaining the default. The implementation validates input against a regex pattern and modifies 8 relevant call sites in git-completion.bash while preventing interference from bash's default sorting.

No substantive changes to the patch are indicated in this follow-up - it appears to be purely a clarification of the example usage case that was mangled in transmission. The thread context shows this remains a standalone feature patch without test coverage, with the author previously noting unfamiliarity with the test framework.