This is a performance optimization patch that refactors xdiff's string hashing implementation, building on Phillip Wood's prior work. The patch splits `xdl_hash_record()` into two variants: `xdl_hash_record_verbatim` (for normal hashing) and `xdl_hash_record_with_whitespace` (for whitespace-aware hashing), with an inline wrapper to select between them. The key change moves the whitespace flag check outside the hot loop, yielding an 8% speedup in `git log` benchmarks.

The implementation maintains the existing djb2 hash algorithm (starting with seed 5381) while restructuring the code to allow better optimization. The whitespace-handling variant remains similar to the original implementation but is now explicitly separated. Benchmark results show measurable improvement (1.67s vs 1.80s for a `git log` range scan), though the actual impact will vary by workload.

This appears to be a well-motivated change that follows established optimization patterns in the codebase. The patch is part of a series (1/2) that continues with further optimizations to the hashing implementation. The changes are focused in xdiff/xutils.{c,h} with minimal diff noise (17 lines net added). The approach is conservative, maintaining hash stability while improving performance through better code structure.