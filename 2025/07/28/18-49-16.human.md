This is a response in the ongoing discussion about the `git reflog write` subcommand implementation, specifically addressing refname resolution behavior. Junio Hamano is responding to Kristoffer Haugsbakk's observation that `git reflog drop` accepts shorthand branch names (like "branch") while the new `git reflog write` requires full ref paths ("refs/heads/branch").

Junio takes a strong position, calling the shorthand behavior in `reflog drop` a bug rather than a feature to emulate. This suggests the `write` subcommand's stricter behavior may be intentional, though the thread shows this is still under discussion. The exchange highlights an inconsistency in user experience between reflog subcommands that needs resolution - either by making `write` more permissive or `drop` more strict.

The response is brief but significant as it comes from the Git maintainer, indicating this design question may need to be settled before the feature can be finalized. The thread has already addressed several technical aspects of reflog migration between storage backends, but this user-facing behavior issue emerged late in the review process.