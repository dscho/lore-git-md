Refactoring patch for `git blame` internals. This removes an unused parameter from `get_commit_info()` that was meant to allow early termination of commit parsing. The author notes that none of the callers actually use this feature, and that performance testing showed no meaningful benefit from the early-exit capability. The change simplifies the function signature and removes dead code paths while maintaining identical behavior.

The patch is straightforward - it removes the `detailed` parameter and its associated conditional block, then updates all callers to use the simpler interface. The diff shows four call sites being adjusted, all in `builtin/blame.c`. This appears to be a clean, low-risk cleanup of an unused feature that makes the code slightly simpler to understand.

While not fixing any bug or adding functionality, this is the kind of small but clear improvement that helps maintain code quality. The commit message could perhaps be more detailed about how the performance testing was conducted, but the change itself looks correct and well-contained.