This is a discussion about output format quoting in the new `git repo` command series, specifically regarding how to handle special characters in key-value output. 

Junio Hamano responds to Lucas Seiki Oshiro's consideration of following `git config --list`'s unquoted output format. Hamano makes the key technical point that while `git config --list` doesn't quote its output, this design causes parsing problems when keys contain "=" characters, even though config keys technically forbid newlines and null bytes. He argues against repeating this "old mistake" in the new command's design.

The exchange reveals an important design decision point for the new command's output format handling. While the thread summary shows the series has implemented both human-readable (`keyvalue`) and machine-parsable (`nul`) formats, this email surfaces a previously unaddressed edge case about how to properly handle special characters in the human-readable output. Hamano's position suggests the need for proper quoting in the non-null format to avoid parsing ambiguities, even if this differs from `git config --list`'s behavior.

This discussion connects to several open questions from the thread summary, particularly around output format robustness and quoting strategy. It represents a new consideration that wasn't fully explored in previous iterations of the patch series, despite the extensive review of the format implementation. The exchange may lead to revisions in how the command handles special characters in its output formats.