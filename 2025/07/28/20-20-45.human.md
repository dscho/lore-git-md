This is a cleanup and documentation follow-up series to Karthik Nayak's earlier work adding the `--start-after` flag to `git for-each-ref`. The series collects small improvements from code review feedback into five focused patches.

The changes include type safety fixes (converting an `int` to `size_t` in ref-cache string handling), documentation improvements (fixing argument ordering and rewording `--start-after` explanations), test coverage (adding a test for `--start-after` with `--exclude`), and code quality (replacing a magic number with a named constant). 

The patches appear well-scoped and address specific review comments, though as Karthik notes, they are relatively minor individually. The most substantive changes are the type safety fix in the ref-cache code and the new test case verifying flag interactions. The documentation updates align with Git's evolving standards, including converting to the newer `[synopsis]` markup style.

This represents final polish on the `--start-after` feature after its main implementation was merged, with all changes being straightforward improvements rather than controversial modifications. The series is based on the already-merged code and maintains all existing functionality while improving code quality and docs.