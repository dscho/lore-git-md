This is a discussion about refname resolution behavior in the `git reflog` subcommands, specifically regarding the `write` vs `drop` commands. The email continues the thread about Patrick Steinhardt's reflog migration infrastructure series.

Junio Hamano responds to Kristoffer Haugsbakk's observation that `git reflog drop` accepts shorthand branch names (like `branch`) while `git reflog write` requires full refnames (`refs/heads/branch`). Hamano takes a strong position that this behavior in `drop` is actually a bug, implying the stricter `write` behavior may be correct.

The exchange highlights an inconsistency in user experience between reflog subcommands that was previously identified by Haugsbakk. This touches on core questions about whether the commands should behave uniformly (resolving refnames) or if there are valid technical reasons for the divergence. The discussion is moving from technical implementation details toward user interface design decisions.

Key technical points at play:
- Refname resolution behavior differs between `write` and `drop` subcommands
- Potential risk of creating reflog entries at wrong refs with automatic resolution
- Whether documentation should warn about or standardize this behavior

This appears to be a pivot point where the thread may shift focus from internal flag handling (`REF_HAVE_OLD`, `REF_LOG_USE_PROVIDED_OIDS`) to user-facing command behavior and documentation needs. Hamano's characterization of the current `drop` behavior as buggy suggests the resolution may favor stricter, more explicit ref specifications.