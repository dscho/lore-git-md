This is a technical discussion about refname resolution behavior in reflog subcommands, part of the ongoing reflog migration infrastructure work. Karthik Nayak responds to Junio Hamano's assertion that `git reflog drop` accepting shorthand branch names is a bug, not a feature.

The email explains the current implementation details: `drop`, `delete`, and `expire` subcommands use `repo_dwim_log()` with a set of resolution rules that try common ref prefixes (like `refs/heads/`) when given a shorthand name. The function includes ambiguity checking and warnings when multiple matches exist.

This discussion represents a key design decision point in the series - whether to make all reflog operations require full refnames (as Junio suggests) or maintain the current shorthand behavior for some subcommands. The technical details provided show the underlying mechanism that would need to change if the resolution behavior were to be unified across subcommands.

The exchange highlights an important inconsistency in Git's user interface that has persisted until now, with different subcommands handling refname resolution differently. The thread suggests this may be headed toward a more consistent (but potentially more verbose) approach requiring explicit full refnames.