This thread discusses a small but focused refactoring of Git's blame internals. The patch removes an unused `detailed` parameter from `get_commit_info()` in `builtin/blame.c`, simplifying the function's interface. The parameter was effectively dead code since all callers were passing the same value (true) to request full commit metadata.

The change touches several blame output functions that display commit information, but maintains their existing functionality - they continue showing full commit details as before. This appears to be a straightforward cleanup of a vestigial feature rather than a behavior change. The patch removes 11 lines while adding 4, with all call sites updated to use the simpler interface.

While the commit message could provide more historical context about why the parameter became unused, the change itself is low-risk and follows Git's pattern of removing dead code paths. No test modifications are needed since this internal refactoring doesn't affect observable behavior. The patch seems ready for inclusion as a clean-up improvement to the blame implementation.