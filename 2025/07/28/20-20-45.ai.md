This is a cleanup and documentation follow-up series to Karthik Nayak's earlier work adding the `--start-after` flag to `git for-each-ref`. The series collects small fixes and improvements identified during review of the original feature implementation.

The patches include:
1. A type safety fix in `refs/ref-cache.c` changing `int` to `size_t` for string length handling, addressing potential issues from pointer arithmetic.
2. Documentation fixes for `git-for-each-ref` including argument ordering corrections and rewording the `--start-after` description.
3. A new test case verifying interaction between `--start-after` and `--exclude` options.
4. Code quality improvement replacing a `1` with the named constant `REF_ITERATOR_SEEK_SET_PREFIX`.

The changes are minor but collectively improve code quality and documentation clarity. The series builds on the already-merged `--start-after` functionality, with v2 addressing a whitespace issue and converting documentation to use the newer `synopsis` block style. Karthik notes these could potentially be combined or dropped if deemed too minor, but they represent final polish on the earlier work.

Key technical details:
- The ref-cache change affects string comparison in `cache_ref_iterator_seek`
- Documentation updates clarify option interactions and fix formatting
- New test case in t6302 verifies combined `--start-after` and `--exclude` behavior
- Constant replacement in `ref-filter.c` improves code readability

This appears to be the final cleanup before closing out the `--start-after` feature work, with all substantive technical issues already resolved in earlier rounds.