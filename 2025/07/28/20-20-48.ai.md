This is a documentation and code cleanup patch in Karthik Nayak's follow-up series to improve the `git-for-each-ref` documentation and related code. The patch focuses on two specific improvements:

1. It rewrites the documentation for `--start-after` to be more precise about its incompatibilities, replacing the vague "general pattern matching" phrase with explicit mentions of `--sort`, `--stdin`, and pattern arguments. This makes the limitation clearer for users.

2. It fixes a lingering typo in the `OPT_STRING` macro call in `builtin/for-each-ref.c` where the help text incorrectly used "start-start" instead of "start-after". This was a carry-over from earlier versions that Junio Hamano had already fixed in the merged documentation.

The changes are straightforward and uncontroversial, being part of the final polish for the `--start-after` feature that was already merged. The patch maintains consistency with the thread's overall goal of documentation standardization and code cleanup following the initial feature implementation.