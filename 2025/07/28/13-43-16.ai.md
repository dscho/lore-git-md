This is a continuation of the thread about `-h` handling in Git aliases, with Junio Hamano weighing in on Jeff King's proposals. The discussion has shifted from technical implementation details to broader philosophical questions about Git's alias model.

Junio strongly agrees with the core patch (showing alias info only for lone `-h`) but pushes back against Peff's more complex proposal to strip alias-injected arguments during `-h` processing. He argues this would introduce "voodoo magic" into Git's straightforward textual substitution model. Instead, Junio advocates for a simpler approach: always stopping after showing the alias expansion when `-h` is present, regardless of whether the alias is a simple command or shell command.

Key technical points in this response: Junio identifies two distinct cases - built-in command aliases with injected arguments (like `grep --cached`) and shell command aliases (starting with `!`). For both, he prefers consistent behavior where `-h` shows the alias without executing anything. This contrasts with Peff's more nuanced approach that would handle these cases differently.

The email reveals a clear philosophical divide: Junio prioritizes conceptual simplicity and predictability in Git's alias model, while Peff's earlier proposals aimed for more sophisticated behavior that might better serve users but at the cost of added complexity. Junio's position suggests he sees this as a fundamental design question about Git's behavior rather than just a bugfix opportunity.

This response moves the thread closer to resolution by strongly endorsing the simplest possible fix while rejecting more complex alternatives, though it leaves open how to handle the tension between practical user experience and Git's design philosophy. The discussion now appears to be converging toward a solution that would modify `handle_alias()` to consistently show alias expansions for `-h` without any command execution.