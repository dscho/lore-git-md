Documentation patch improving clarity of `--start-after` flag restrictions in `git-for-each-ref`. This is part 3 of Karthik Nayak's 5-patch follow-up series cleaning up documentation and type safety issues from the earlier `--start-after` feature implementation.

The patch makes two specific improvements:
1. Replaces the vague "general pattern matching" phrasing in the `--start-after` documentation with explicit mentions of the incompatible options (`--sort`, `--stdin`, and pattern arguments)
2. Fixes a lingering typo in the `OPT_STRING` macro where "start-start" was incorrectly used instead of "start-after"

The changes are straightforward and build on prior review feedback, with Junio Hamano providing a Helped-by. This continues the series' trend of polishing the documentation and implementation details of the `--start-after` feature. The patch appears well-considered and uncontroversial, addressing specific points raised in earlier discussion.