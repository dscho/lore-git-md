This is a refactoring patch that modifies the reflog callback interface to pass the refname explicitly rather than requiring callers to pass it through callback data. The change affects 17 files across the codebase, primarily updating callback signatures and their implementations.

The patch standardizes the callback signature for `each_reflog_ent_fn` to include the refname as the first parameter, eliminating awkward patterns where callers had to track the current refname separately. The commit message notes this will be particularly useful for upcoming refactoring of reference migration logic, though that work isn't shown here.

Key technical changes include:
- Modifies `each_reflog_ent_fn` typedef in `refs.h` to include `refname` parameter
- Updates all callback implementations to accept the refname parameter (many marked UNUSED for now)
- Removes now-redundant refname tracking from several structs (`read_ref_at_cb`, `reflog_migration_data`)
- Changes call sites to pass NULL instead of refname in callback data where appropriate

The changes are mechanical but widespread, touching core refs handling, builtins, and test infrastructure. This appears to be preparatory work for Patrick Steinhardt's performance optimization series mentioned in the thread context, though the direct connection isn't explained in this commit. The patch is well-contained and follows the established pattern of similar callback interface changes in the codebase.