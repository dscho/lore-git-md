This is a refactoring patch for the blame subsystem. Han Young removes an unused `detailed` parameter from the `get_commit_info()` function in `builtin/blame.c`. The parameter was previously used to skip some commit information processing when not needed, but all callers were passing `1` (true) anyway, making the conditional logic dead code.

The change simplifies the function signature and removes 11 lines of code while adding 4, for a net reduction. The patch appears straightforward and mechanical - it doesn't change any functionality, just cleans up an unused parameter. All call sites are updated to match the new signature.

This seems like a good cleanup - the commit message could be more detailed about why the parameter was no longer needed, but the change itself looks correct and improves code clarity by removing dead code paths. The patch touches only the blame command's implementation and shouldn't affect users.