Documentation patch finalizing the removal of experimental status markers from `git-switch` and `git-restore`.

Junio Hamano accepts Justin Tobler's patch to remove the "THIS COMMAND IS EXPERIMENTAL" warnings from both commands' documentation. This concludes the uncontested part of the thread, which had separated into documentation updates (now approved) and interface design discussions (still stalled). The patch simply deletes the experimental markers that were added in 2019 (4e43b7ff), acknowledging that after six years of stable use, breaking changes are no longer feasible. 

This change reflects the commands' de facto stability, though it avoids any celebratory language about their maturity. The technical discussion about command-line interface details (particularly around the `-m` flag) remains unresolved, but this documentation update stands on its own as a clear improvement. The patch is minimal and focused, touching only the two documentation files to remove the outdated warnings.