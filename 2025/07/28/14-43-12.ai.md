This is a technical follow-up in the reflog migration infrastructure series, specifically addressing the `REF_HAVE_OLD` flag behavior during log-only updates. The discussion focuses on an inconsistency between how `split_symref_update()` and `split_head_update()` handle the flag.

Patrick Steinhardt responds to Jeff King's observation that `split_head_update()` doesn't clear `REF_HAVE_OLD` like `split_symref_update()` does. Patrick argues this is actually correct behavior, as the next commit in the series relies on `REF_HAVE_OLD` being set for reflog-only updates. He suggests the real issue is that `split_head_update()` should resolve the old OID when `REF_HAVE_OLD` isn't set.

The thread context shows this is part of a larger effort to improve reflog migration between storage formats, with recent additions including the `REF_LOG_USE_PROVIDED_OIDS` flag and fixes for old OID migration. The discussion touches on atomicity concerns for transactions with mixed log-only and ref updates, connecting to Jeff King's recent work on dangling symref handling.

Key technical points:
- The `REF_HAVE_OLD` flag signals whether an old OID was provided
- Current inconsistency between HEAD and symref update splitting
- Upcoming changes depend on preserving `REF_HAVE_OLD` for reflog updates
- Related to new `REF_LOG_USE_PROVIDED_OIDS` flag functionality

This email advances the technical discussion about flag handling consistency while preparing for subsequent changes in the series. The exchange demonstrates careful attention to edge cases in the ref transaction machinery.