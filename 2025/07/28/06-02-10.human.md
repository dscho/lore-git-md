This is a refactoring patch in the blame subsystem, following up on Han Young's earlier removal of an unused `detailed` parameter from `get_commit_info()`. Patrick Steinhardt confirms the parameter has been effectively dead code since its introduction in 2006, with all callers always passing `1` (true) for this flag. 

The change is straightforward cleanup with no behavior impact, simply removing unused code paths. Patrick notes the historical context - the parameter was part of the original blame implementation but never actually used for its intended early-termination purpose. He gives the patch his approval, calling it "obviously correct."

This appears to be a non-controversial, mechanical cleanup that advances the ongoing effort to simplify the blame codebase by removing dead code. The patch seems ready for inclusion based on the positive review.