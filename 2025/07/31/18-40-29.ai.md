This is the final review response for the `git last-modified` feature series (v6), focusing on the Bloom filter integration and architectural decisions.

Junio C Hamano acknowledges Toon Claes' v6 changes, which include:
- Dropping the controversial `--format` option implementation
- Exporting `prepare_commit_graph()` for Bloom filter integration
- Consolidating the callback architecture into a direct `last_modified_emit()` function
- Adapting to recent Bloom filter API changes (`bloom_key_fill()`/`clear()`)

The key approval comes with Junio noting the series is ready to be queued, despite some API churn from the Bloom filter refactoring. This represents the final green light after six iterations of the feature, which has undergone significant refinement in path handling, output formats, performance testing methodology, and architectural organization.

The series now stands with:
- Core tree-level path tracking
- Bloom filter optimization (50.6% speedup for top-level checks)
- Comprehensive test coverage (19 scenarios)
- Performance benchmarks
- Simplified monolithic builtin implementation
- Documentation updates

The only remaining open question (about `prepare_commit_graph()` export) appears resolved by Junio's acceptance of the current approach, though Patrick Steinhardt had proposed an alternative `bloom_filters_enabled()` API. The series demonstrates mature handling of merge commits, SHA-256 compatibility, and performance characteristics across different usage patterns.