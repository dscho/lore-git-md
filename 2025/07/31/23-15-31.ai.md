This is a self-review follow-up in the ongoing "repo info" command implementation series. Lucas Seiki Oshiro notes they missed including the `--format` flag in the command's usage string in patch 5/5 of v5. The flag is a key part of the command's output formatting system, which supports both human-readable (`keyvalue`) and machine-parsable (`nul`) formats. The thread context shows this was later implemented with `quote_c_style` for the keyvalue format and a planned `-z` short option for the NUL format.

The oversight is minor since the flag was properly implemented in the code - this is just a documentation correction for the usage string. The thread shows the format system is now complete with robust error handling and test coverage. This kind of self-review follow-up is typical in the final polishing stages of a patch series, especially one mentored through GSoC where attention to detail is being emphasized. The command's architecture is otherwise solid with multiple production fields implemented and migration from `git rev-parse` functionality progressing.