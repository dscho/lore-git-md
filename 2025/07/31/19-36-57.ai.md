This is a review comment on patch 2/5 of Junio Hamano's string-list API refactoring series. Eric Sunshine catches a typo in the commit message where "stirng" should be "string". The patch aligns `string_list_split()` with its `_in_place()` counterpart by allowing multiple delimiter characters, preparing for future feature additions in the series.

The thread context shows this is part of a systematic cleanup unifying string-list functionality, with patch 2 specifically standardizing delimiter handling between the split variants. The typo fix is minor but important for documentation accuracy. No code changes are needed - this is purely a commit message correction for an otherwise uncontroversial technical improvement that helps unify the API surface.