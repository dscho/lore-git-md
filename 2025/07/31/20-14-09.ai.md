This is a minor correction to patch 6/9 in the ongoing string handling refactoring series. Eric Sunshine points out a typo in Junio Hamano's commit message where "trimming" should be "trim" in the explanation about `string_list_split*()`'s whitespace handling capabilities. 

The correction doesn't affect the actual code changes in the patch, which had already converted `builtin/notes.c`'s stdin parsing from `strbuf_split()` to `string_list_split()` with the `STRING_LIST_SPLIT_TRIM` flag. The original patch maintained the existing behavior of requiring at least two tokens per line while ignoring extra tokens, explicitly noting this behavior was outside the scope of the refactoring series.

This appears to be a straightforward editorial fix to the commit message, not requiring any code changes. The thread context shows this is part of a larger, well-received series modernizing string handling across multiple Git subsystems by replacing `strbuf_split*()` with more appropriate alternatives. The series has already addressed all substantive feedback and appears ready for merging.