Documentation fix with formatting correction. Junio Hamano responds to Knut Harald Ryager's patch that aimed to fix a mismatched bracket in the `git-clone` synopsis. While agreeing there's a formatting issue, Junio points out the original patch removed the wrong bracket - the extra `]` should be removed after `--filter=<filter>` rather than after `--also-filter-submodules`. He also provides guidance on proper commit message style, noting that Git prefers imperative voice ("Remove the extra bracket") rather than descriptive ("This patch removes...").

The correction maintains the intended documentation structure showing that `--also-filter-submodules` is dependent on `--filter`. This is a minor but precise documentation fix that requires an updated patch submission. Junio's response demonstrates the project's attention to detail even in documentation formatting and commit message conventions.