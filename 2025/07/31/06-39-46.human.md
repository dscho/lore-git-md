This is the second patch in a 5-part refactoring series for Git's string-list API, focusing on aligning the behavior of `string_list_split()` with its `_in_place` counterpart. The patch makes `string_list_split()` accept multiple delimiter characters, matching the existing capability of `string_list_split_in_place()`. This is a straightforward mechanical change that updates all callers across 19 files to pass delimiter strings (like `","`) rather than single characters (like `','`). The change appears well-executed with comprehensive updates to both implementation and tests, though the commit message doesn't explain why the inconsistency existed originally. As part of a larger series to unify and enhance string-list functions, this patch lays groundwork for future additions while fixing a minor API asymmetry.