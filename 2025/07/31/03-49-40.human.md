Test fix correcting path verification in security test for submodule checkout behavior. This is a follow-up to earlier feedback on the patch's presentation quality.

The patch updates test t7450-bad-git-dotfiles.sh to verify the correct path where a malicious post-checkout hook would create files during submodule cloning. The test checks a security fix from commit 05e9cd64 that prevents repositories from executing arbitrary hooks during recursive submodule clones. The key change replaces "$PWD/foo" with "$PWD/bad-clone/sub/foo" to match the actual directory structure where the hook would operate.

The v2 revision improves upon the initial submission with a clearer commit message explaining the security context and test purpose, addressing feedback from Junio C Hamano. The technical content remains the same - this is purely a test correction to properly verify the security behavior. Justin Tobler, the original test author, has validated the technical correctness of the path adjustment.

The patch appears ready for inclusion, cleanly addressing both the technical test fix and presentation concerns raised in the thread. It maintains the test's dual purpose of verifying both the security fix (hook doesn't run) and the original vulnerability behavior (preventing checkout into arbitrary locations).