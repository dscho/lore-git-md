This is a minor editorial correction to patch 7/9 in the strbuf-to-string_list refactoring series. Eric Sunshine points out two typos in Junio Hamano's commit message for the config.c conversion: "spliting" should be "splitting" and "leafes" should be "leaves". The typos are in the explanation of why string_list_split() is preferable to strbuf_split() for parsing GIT_CONFIG_PARAMETERS.

The actual code changes in this patch were already reviewed and deemed correct - it replaces strbuf_split() with string_list_split() to avoid manual delimiter trimming while maintaining identical parsing behavior of the environment variable. The patch also comments on the awkward API design of string_list_split()'s max parameter, which requires passing n-1 for n desired splits.

This follows the series' pattern of replacing strbuf-based string splitting with more appropriate utilities while preserving behavior. The correction is purely textual and doesn't affect the substantive changes, which had already converted the config parameter parsing to use string_list_split() with proper whitespace handling. No code changes are needed in response to this email - it's just a documentation fix for the commit message.