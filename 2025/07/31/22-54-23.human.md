This is the first patch in an 11-part refactoring series that replaces `strbuf_split*()` usage with more appropriate string handling alternatives. The patch focuses on `wt-status.c`'s `abbrev_oid_in_line()` function, which processes rebase todo list lines like "pick <oid> title".

The change converts the function from using `strbuf_split_max()` to `string_list_split()`, which better matches the actual usage pattern. The original code split lines into an array of strbufs but only used them as read-only strings before reassembling. The new version:
- Uses `string_list` for simpler string handling
- Maintains the same behavior for abbreviating object IDs
- Reduces code by 11 lines while keeping the same functionality
- Follows the series' theme of replacing manual strbuf management with more appropriate string utilities

This appears to be a straightforward, well-motivated refactoring that simplifies the code without changing behavior. As the first patch in the series, it establishes the pattern that subsequent patches will follow for similar conversions across other parts of the codebase.