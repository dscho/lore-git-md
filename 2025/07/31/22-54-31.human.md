This is patch 9/11 in a refactoring series that replaces `strbuf_split*()` usage with more appropriate string handling utilities. The patch focuses on sub-process status reading in `sub-process.c`.

The change simplifies the code that reads status packets from subprocesses by replacing an over-engineered `strbuf_split_str()` approach with direct `skip_prefix()` matching. Instead of splitting each line into key-value pairs and allocating temporary strbufs, the new version simply checks for the "status=" prefix and captures the remaining value directly. This eliminates memory management overhead while maintaining the same "last status wins" behavior.

The diff shows a clear reduction in complexity - 9 lines removed vs 7 added, with the removal of temporary buffer allocations and cleanup. This follows the series' pattern of replacing manual string handling with simpler, more direct alternatives when possible. The change is straightforward and appears to be a clear improvement over the original implementation.

As noted in the thread context, Christian Couder observed that including `string-list.h` is no longer needed after this change, though that was already addressed in a previous version of the patch. This appears to be a clean, non-controversial refactoring that improves both code clarity and efficiency.