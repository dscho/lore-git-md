This is a small follow-up in a refactoring series that replaces `strbuf_split*()` with more modern string handling utilities. Eric Sunshine points out a minor typo in Junio Hamano's commit message for patch 6/9, which converts `builtin/notes.c` to use `string_list_split*()` with trimming instead of `strbuf_split()`.

The correction is trivial - fixing "trimming" to "trim" in the commit message explanation. The patch itself, which modernizes the notes command's stdin parsing while maintaining its existing behavior (including its lenient handling of extra tokens), remains unchanged. This appears to be a straightforward typo fix that doesn't affect the substance of the ongoing string handling refactoring work.