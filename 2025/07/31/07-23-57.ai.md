This is the final patch in a 5-part series extending the promisor-remote protocol to support configurable validation of remote attributes. The patch focuses on code hygiene by introducing string constants for the mandatory 'name' and 'url' fields, completing the pattern established in the series for other fields.

The change replaces hardcoded string literals "name" and "url" with constants `promisor_field_name` and `promisor_field_url`, mirroring the approach already taken for "partialCloneFilter" and "token" fields. This improves maintainability by centralizing field names and reducing duplication. The patch touches the protocol capability string construction in `promisor_remote_info()` and the parsing logic in `parse_one_advertised_remote()`.

As a clean-up patch in a mature series, it doesn't introduce new functionality but completes the architectural pattern consistently across all fields. The implementation is straightforward, replacing string literals with the new constants while maintaining identical behavior. The series as a whole has already addressed all substantive feedback, with this patch representing final polish before potential merge.

Key technical details include the new string constants`promisor_field_name` and `promisor_field_url` being added alongside existing field constants, with updates to both the string construction and parsing paths to use them. The changes maintain the existing percent-encoding behavior and protocol structure while improving code organization.