Test patch for the new `git refs list` command, part of the RFC series introducing this feature. This is the final patch in v4 of the series, adding comprehensive testing by leveraging the shared test infrastructure created in the previous patch.

The test script `t1461-refs-list.sh` is intentionally minimal - it simply sets the `git_for_each_ref` variable to use the new `git refs list` command and sources the shared test library. This ensures the new command is tested against the full suite of `for-each-ref` tests, verifying it behaves as a proper drop-in replacement.

The approach follows established patterns seen in other Git commands like `git-blame` and `git-annotate` that share test infrastructure. The patch also updates the meson build file to include the new test script. With this addition, the series now has complete test coverage for the new functionality while avoiding duplication of test logic.

This appears to be a well-constructed test addition that builds on the architectural work done earlier in the series. The implementation is clean separation of concerns - the test driver is minimal while getting full coverage through the shared library. The patch maintains the series' high quality standard with proper attribution to mentors and follows project conventions.