This thread addressed a straightforward documentation fix for the `git-clone` man page, correcting the nested bracket syntax around the `--filter` and `--also-filter-submodules` options. The original error showed these as independent options (`[--filter=<filter>] [--also-filter-submodules]`), when in fact the submodule filtering depends on the main filter being specified, so the correct syntax groups them together (`[--filter=<filter> [--also-filter-submodules]]`).

Knut Ryager submitted the initial fix, which Junio Hamano reviewed, leading to several iterations focused on improving the commit message's clarity. The final version, now approved by Junio, includes historical context tracing the error's introduction and a clear explanation of why the grouping matters. The patch itself remains a simple one-line correction to `Documentation/git-clone.adoc`.

With Junio's sign-off, this uncontroversial documentation fix is ready for inclusion. The discussion wrapped up after ensuring the commit message properly documented both the technical correction and its rationale.