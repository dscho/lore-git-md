The thread concerns a straightforward documentation fix for the `git-clone` man page, correcting the nested bracket syntax for the `--filter` and `--also-filter-submodules` options. Knut Ryager initially submitted a patch to fix an extra closing bracket that made the syntax incorrect, showing these as separate options when `--also-filter-submodules` should be nested within `--filter`'s brackets since it depends on the main filter being active.

Junio Hamano reviewed the initial version, clarifying the proper nesting relationship and suggesting commit message improvements (removing "this patch" phrasing and ensuring author/sign-off consistency). The v2 patch incorporates these suggestions while keeping the same doc change—removing the misplaced bracket to correctly display the option relationship as `[--filter=<filter> [--also-filter-submodules]]`. 

This is a purely cosmetic fix with no functional impact, addressing a minor but clear documentation error in the command synopsis. The change is uncontroversial and targets Documentation/git-clone.adoc, specifically a line introduced in f05da2b4 and later reformatted in 76880f05. The patch is submitted via GitGitGadget (PR #2023) and appears ready for merging.