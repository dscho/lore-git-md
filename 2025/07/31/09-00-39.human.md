This is a test refactoring patch from Meet Soni, part of the RFC v4 series introducing `git refs list`. The patch refactors the test suite for `git for-each-ref` to make it shareable with the new `git refs list` command.

The key change moves the core test logic from `t6300-for-each-ref.sh` into a new `for-each-ref-tests.sh` file, replacing hardcoded `git for-each-ref` calls with a `$git_for_each_ref` variable. The original test file becomes a simple driver that sets this variable and sources the test library. This follows the pattern used for sharing tests between `git-blame` and `git-annotate`.

The patch is well-structured and clearly motivated, with the commit message explaining the changes and their purpose. It builds on prior work in the series to enable testing the new `refs list` command while maintaining existing test coverage. The diff shows a straightforward mechanical transformation of the test code to make it more reusable.

This appears to be a solid preparatory step in the larger effort to introduce `git refs list` as a thin wrapper around `git for-each-ref`. The changes are focused and maintainable, following established patterns in the codebase.