Thread identity: Build system patch addressing Git/Meson version compatibility for `ls-files --deduplicate` support (introduced in Git 2.31).

Core problem: Fix build failures when running Meson builds with Git versions older than 2.31 that lack `ls-files --deduplicate` support. The patch handles version detection differently based on Meson's own version capabilities.

Current status: Single patch version (v1) proposing to modify `meson.build` to:
1. For Meson 0.62+: Check Git version and only use `--deduplicate` if Git >=2.31
2. For Meson <0.62: Assume Git is new enough (no version check possible)

Key technical details:
- Modifies header file detection logic in build system
- Uses Meson's `version()` comparison for Meson >=0.62
- Falls back to unconditional `--deduplicate` for older Meson versions
- Affects build-time behavior only, no runtime impact

Open questions:
- No review feedback yet
- Potential silent failure case with Meson <0.62 + Git <2.31 remains unaddressed (acknowledged as limitation by author)

Participant positions:
- Single author submission, no reviewers yet

Related work:
- Build system modernization effort (Meson/CMake alongside traditional Makefile)
- `ls-files --deduplicate` was introduced in Git 2.31 (2021-03-15)