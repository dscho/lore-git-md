This is the implementation patch for the new `git refs list` subcommand, part of the ongoing refs command consolidation effort. It's the third patch in a 5-part series that introduces a thin wrapper around `git for-each-ref` functionality under the `git refs` umbrella.

The patch adds the new `list` subcommand by having it call the shared `for_each_ref_core()` helper function that was extracted from `git-for-each-ref` in a previous commit. The implementation is kept minimal and consistent by reusing the common infrastructure - the command shares the same usage string macro (`COMMON_USAGE_FOR_EACH_REF`) and leverages the newly created shared options documentation.

The documentation additions show this is a full-featured alias for `git-for-each-ref`, supporting all the same filtering, formatting and sorting options through the included `for-each-ref-options.adoc` file. The implementation appears clean and well-integrated with the architectural foundation established in earlier patches, following the maintainer-approved pattern of thin command wrappers around shared core functionality.

This patch represents the main user-facing addition of the series, providing the actual `git refs list` command that was the original goal of the project. The implementation looks solid, building on the preparatory work done in previous patches to ensure consistency and maintainability.