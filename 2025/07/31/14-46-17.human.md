This is a documentation patch fixing a minor formatting issue in the `git-clone` man page. 

Knut Harald Ryager noticed and corrected a stray closing bracket in the command synopsis section of `git-clone.adoc`. The line describing the `--also-filter-submodules` option had an extra ']' character, making it appear as `--also-filter-submodules]]` instead of the correct `--also-filter-submodules]`.

The change is straightforward and non-controversial - it simply aligns the documentation with the actual command syntax. The patch touches only one line in the documentation file and includes a clear commit message explaining the fix. 

This appears to be a clean, focused documentation improvement that should be uncontroversial to merge. The author followed the standard patch submission process via GitGitGadget.