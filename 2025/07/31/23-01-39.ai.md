This is a follow-up discussion in the ongoing series introducing a new `git repo info` command with format options. The email addresses two specific review points from Eric Sunshine regarding the `--format` flag implementation.

The first point concerns adding a `-z` short option for consistency with other Git commands that handle NUL-terminated output. Lucas confirms this is planned for a future patch, indicating the option naming is still being finalized.

The second point improves test methodology for the NUL format verification. Eric suggests using `printf` with explicit newlines and null bytes combined with `test_cmp_bin` for more robust testing. Lucas accepts this approach, noting it aligns with previous test improvements in the series.

The exchange shows the series maturing through review feedback, with both functional improvements (the upcoming `-z` option) and testing best practices being incorporated. The discussion occurs in the context of already-established technical decisions about output formats (`keyvalue` and `nul`) and field handling that were solidified in earlier versions of the patch series.