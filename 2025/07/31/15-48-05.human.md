This is a review comment from Junio Hamano on the v7 iteration of the promisor-remote protocol extension series. The email focuses on a specific parsing improvement in the code, where Christian Couder replaced `strbuf_split()` with `string_list_split_in_place()` to handle comma-separated remote attributes more cleanly.

Junio highlights the technical advantage of this change - it eliminates the need for manual cleanup of trailing commas that was previously required. He notes that while `strbuf_split()` could only handle single-byte delimiters and left artifacts requiring post-processing, the new approach using `string_list_split_in_place()` provides cleaner parsing behavior by design.

The comment comes as part of the final polishing phase for this series, which has already addressed all major implementation feedback and is now in the home stretch. Junio's approval of this particular change suggests the technical implementation is sound, with only minor terminology decisions remaining before the series can be merged. The email reinforces that this is a well-executed extension to the promisor-remote protocol that improves both functionality and code quality.