This is a refactoring patch discussion about header includes in the sub-process string handling series.

Christian Couder questions whether including `string-list.h` is necessary in the final patch of the series, since the actual refactoring replaced `strbuf_split_str()` with `skip_prefix()` rather than using any string-list functionality. The patch being discussed (v3 part 9) had already simplified the sub-process status reading by eliminating temporary strbuf allocations in favor of direct prefix matching.

The thread context shows this is the culmination of a systematic series converting `strbuf_split*()` usage across multiple subsystems, with this particular change reducing code while maintaining the last-status-wins behavior. The question focuses on a minor implementation detail - whether an apparently unused header should remain - rather than challenging the core refactoring approach. This suggests the series is otherwise uncontroversial and nearing completion, with only small polish items remaining.