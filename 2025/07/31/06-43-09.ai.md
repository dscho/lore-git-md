This is a review response in the `git last-modified` feature series, specifically addressing the Bloom filter integration in patch 4/4. Patrick Steinhardt questions the architectural approach of directly exporting `prepare_commit_graph()` and suggests an alternative higher-level API.

The review focuses on two technical points:
1. The `maybe_changed_path()` helper function, while similar to blame.c's implementation, lacks the generation number check present in the blame version. This discrepancy is noted but not flagged as critical.
2. The core architectural concern is the direct use of `prepare_commit_graph()` to enable Bloom filters. Patrick proposes exposing a higher-level `bloom_filters_enabled()` function instead, mirroring the pattern used for generation numbers and corrected commit dates.

This represents the last major architectural discussion point in the series, with Patrick providing a concrete alternative to the current implementation approach. The review maintains the positive tone established in previous exchanges while focusing on this specific technical refinement needed before merging.