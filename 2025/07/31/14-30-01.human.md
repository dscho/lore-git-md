Refactoring patch in the string handling cleanup series. This is the final patch (9/9) in Junio Hamano's series replacing `strbuf_split*()` usage with more appropriate alternatives. 

The patch simplifies sub-process status reading by replacing an over-engineered `strbuf_split_str()` call with direct `skip_prefix()` matching, eliminating temporary strbuf allocations while maintaining the same last-status-wins behavior. Christian Couder correctly noted the now-unnecessary inclusion of `string-list.h`, which Junio acknowledges was a leftover from an earlier approach and will be removed.

This continues the series' pattern of replacing string splitting with simpler direct operations where possible, resulting in a net reduction of 2 lines (-9/+7). The change is straightforward and uncontroversial, with the only discussion being the minor header cleanup. The series appears ready for merging after this small adjustment.