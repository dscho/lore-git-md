This thread discusses a 9-patch series refactoring Git's string handling by replacing `strbuf_split*()` calls with more appropriate alternatives like `string_list_split*()` and `skip_prefix()`. The changes span multiple subsystems including status handling, config parsing, merge-tree, and sub-process communication.

The series has been well-received as a clean modernization effort that reduces code complexity while maintaining behavior. Christian Couder raised the only substantive question about whether a `string-list.h` include was needed in the final patch (which refactors sub-process status reading). Junio Hamano confirmed this include could be removed since the patch ultimately used `skip_prefix()` rather than string-list functionality.

With that minor clarification resolved, the series appears ready to proceed. The changes demonstrate consistent improvements across the codebase:
- Replacing manual memory management with appropriate utilities
- Preserving input validation while reducing code
- Converting array access patterns
- Achieving net line reductions (-137/+125 overall)

The discussion highlights Git's careful review process even for straightforward refactorings, with maintainers paying close attention to implementation details like header inclusions. This series represents the kind of incremental but valuable cleanup that helps maintain the codebase's long-term health.