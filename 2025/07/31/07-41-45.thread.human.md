This thread discusses a 9-patch series that systematically refactors Git's string handling by replacing `strbuf_split*()` calls with more appropriate alternatives like `string_list_split*()` and `skip_prefix()`. The changes span multiple subsystems including `wt-status.c`, `clean.c`, `merge-tree.c`, and `sub-process.c`, consistently reducing code while maintaining behavior.

The series has seen three iterations (v1-v3) with no major objections to the core approach. The latest discussion focuses on a minor implementation detail in the final patch (v3 part 9) that refactors sub-process status reading. Christian Couder questioned whether including `string-list.h` is necessary since the patch ultimately used `skip_prefix()` rather than string-list functionality. This suggests the series is in good shape with only small polish items remaining before potential merging.

The work represents focused, non-controversial refactoring that aligns with Git's ongoing string handling modernization efforts. The series demonstrates clear patterns of replacing manual memory management with appropriate utilities while preserving input validation, achieving a net reduction of 12 lines (-137/+125) across the codebase. With this final discussion about header includes being the only open question, the series appears ready for maintainer consideration.