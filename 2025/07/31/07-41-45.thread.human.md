This thread presents a 9-patch series refactoring Git's string handling by systematically replacing `strbuf_split*()` with more appropriate alternatives like `string_list_split*()`, particularly where whitespace trimming is needed. The series touches multiple subsystems including `wt-status.c`, `clean.c`, and `config.c`, demonstrating consistent patterns of reducing code complexity while maintaining behavior.

Junio Hamano and Christian Couder reviewed the series, with Junio confirming the final patch correctly removed an unnecessary `string-list.h` inclusion. The changes show net reductions in code (-137/+125 overall) through careful conversions that preserve input validation while modernizing string handling patterns.

Eric Sunshine recently noted a minor typo in patch 6/9's commit message ("trimming" should be "trim"), which doesn't affect the actual code changes. The patch had already correctly implemented the conversion of `builtin/notes.c`'s stdin parsing to use `string_list_split()` with proper whitespace handling.

With all substantive feedback addressed and only this small documentation correction remaining, the series appears ready for merging as a clean refactoring that improves Git's string handling internals.