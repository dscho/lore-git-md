This is a code hygiene patch in the promisor-remote protocol series, specifically addressing string constant usage. It's the final patch (5/5) in v7 of the series that extends promisor-remote capabilities.

The change replaces hardcoded "name" and "url" string literals with named constants (`promisor_field_name` and `promisor_field_url`), matching the approach already taken for "partialCloneFilter" and "token" fields in previous patches. This improves consistency across the codebase and reduces the risk of typos when referencing these protocol field names.

The patch is straightforward and mechanical, touching only the promisor-remote.c file with 10 lines added and 4 changed. It maintains all existing functionality while making the code more maintainable. As the final patch in a well-reviewed series that has already addressed all substantive feedback, this appears to be a clean finishing touch before potential merge.