This is a documentation patch review in an ongoing thread about fixing a formatting issue in the git-clone man page. Junio Hamano responds to Knut Ryager's initial patch that removed what appeared to be a stray closing bracket after `--also-filter-submodules`.

Hamano points out that the original patch targeted the wrong bracket - the issue stems from a documentation reformatting change that incorrectly made `--filter` and `--also-filter-submodules` appear as separate optional arguments when they should be nested (the submodule filtering only makes sense when regular filtering is active). He provides historical context, referencing the original commits that introduced the option (f05da2b4) and later reformatted it (76880f05).

The review also includes style guidance on commit message wording (avoiding "this patch" phrasing) and suggests the author name should match the sign-off. The actual change remains a one-character documentation fix, but Hamano indicates it should remove the bracket after `--filter=` rather than the one after `--also-filter-submodules`, maintaining the intended nesting relationship between these options.

Key technical details: The fix affects Documentation/git-clone.adoc, specifically the synopsis section showing command-line options. The correct syntax should show `--also-filter-submodules` as being nested within the `--filter` option's brackets. This is a follow-up to PR #2023 submitted via GitGitGadget.