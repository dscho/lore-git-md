This is a follow-up discussion about the final patch in a refactoring series that replaces `strbuf_split*()` usage across Git's codebase. Christian Couder questions whether including `string-list.h` is necessary in the `sub-process.c` changes, since the patch actually uses `skip_prefix()` rather than any string list functions.

The patch in question (part 9/9) simplifies sub-process status reading by replacing an over-engineered `strbuf_split_str()` approach with direct `skip_prefix()` matching. While the change itself looks correct and improves the code, Christian points out that the added `#include "string-list.h"` seems unrelated to the actual changes made. This appears to be a minor oversight in an otherwise clean refactoring that reduces code while maintaining behavior.

The thread shows this series has been consistently well-received, with this being the only nitpick in the final patch. The discussion is focused and technical, typical of late-stage review of a mature patch series.