This is a review response in the `git last-modified` feature series, focusing on the Bloom filter integration in patch 4/4. Patrick Steinhardt questions the architectural approach of directly exporting `prepare_commit_graph()` for Bloom filter access, suggesting instead a higher-level `bloom_filters_enabled()` function similar to other commit-graph helper functions.

The review acknowledges the impressive performance improvements shown in the benchmarks (50.6% speedup for top-level checks) but raises concerns about proper abstraction boundaries. The proposed alternative would maintain consistency with how other commit-graph features are exposed while still enabling the optimization. This represents the last major architectural discussion point for the series, which otherwise appears well-tested and refined through multiple iterations.

Patrick also observes that the Bloom filter checking logic closely mirrors existing code in `blame.c`, though with one notable difference - the missing generation number check present in the blame implementation. The review maintains a constructive tone while clearly identifying what may be a more idiomatic solution to the commit-graph preparation question.