This is the v4 cover letter for a feature series introducing `git refs list` as a consolidated interface for ref operations, building on previous feedback to improve the architecture. The series now implements the maintainer-recommended approach of extracting shared logic into helper functions rather than direct command forwarding.

The key changes in v4 address Junio Hamano's architectural concerns from previous rounds:
- Core `for-each-ref` logic moved to a shared helper function in new `for-each-ref.h`
- Both commands now act as thin wrappers around this shared implementation
- Usage strings unified via shared macros in the new header
- Series reorganized with preparatory refactoring commits first

The implementation maintains strict backward compatibility while fixing a pre-existing inconsistency between `for-each-ref`'s --help output and man page. The test infrastructure remains robust, with shared test logic between the old and new commands.

The cover letter reiterates the long-term vision of consolidating ref-related commands under `git refs`, though this series remains focused on establishing the pattern with `list` as a drop-in replacement for `for-each-ref`. The author invites discussion about future refinements to the command's interface now that the architectural foundation is in place.

This appears to be a well-executed iteration that addresses all substantive feedback from previous rounds. The changes demonstrate thoughtful engagement with maintainer guidance while keeping the scope focused. The series seems ready for final review, with the broader questions about command consolidation left for future discussion.