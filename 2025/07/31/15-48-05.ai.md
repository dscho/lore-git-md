This is a review response from Junio Hamano on the v7 series extending promisor-remote protocol support. The email focuses on implementation details of the parsing improvements in patch 3, specifically the switch from `strbuf_split()` to `string_list_split_in_place()`.

Key technical points noted:
- The change eliminates the need for manual `strbuf_strip_suffix()` calls that were previously required when using `strbuf_split()`
- `string_list_split_in_place()` is praised for its ability to handle multiple delimiters natively, making the code cleaner
- The parsing refactoring is part of the series' broader effort to improve type safety and memory handling through the new `struct promisor_info`

The email indicates approval of these technical changes while the thread's remaining open questions (terminology choice and error handling strategy) are not addressed here. This suggests the parsing implementation is now considered settled while other aspects of the series remain under discussion.