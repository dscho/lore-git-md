This is a review comment on patch 2/5 of Junio Hamano's string-list API refactoring series. Eric Sunshine catches a typo in the commit message where "stirng" should be "string". The patch itself aligns `string_list_split()` with its `_in_place()` counterpart by allowing multiple delimiter characters, part of the broader effort to unify these functions through a shared helper.

The review is straightforward - just a minor typo fix with no substantive changes needed to the code. This continues the pattern of clean, uncontroversial improvements to the string-list API that has characterized the series so far. The typo correction is the only new development since the thread summary was written.