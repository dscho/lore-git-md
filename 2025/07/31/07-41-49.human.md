This is part 4/9 in a refactoring series converting Git's string handling from `strbuf_split*()` to `string_list_split*()`. The patch focuses on `builtin/clean.c`, specifically the interactive pattern filtering in `filter_by_patterns_cmd()`.

The change replaces `strbuf_split()` with `string_list_split_in_place_f()` for parsing user-provided exclude patterns, leveraging the string_list API's new trimming capability. The key improvements are eliminating temporary strbuf management and simplifying the code path while maintaining identical functionality. The patch reduces line count and improves memory safety by avoiding strbuf reallocation risks.

This continues the series' theme of mechanical but meaningful conversions where string_list offers better ergonomics. The change is straightforward and well-contained, building on previous safety improvements in the same file. The commit message clearly explains the motivation and technical approach.