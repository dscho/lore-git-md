This is a refactoring series that continues the effort to modernize Git's string handling by replacing `strbuf_split*()` usage with more appropriate alternatives. The 11-patch series builds on previous work to extend `string_list` APIs, focusing on cases where `string_list_split*()` provides better ergonomics for whitespace trimming and delimiter handling.

The series converts usage across several subsystems including `wt-status.c`, `clean.c`, `merge-tree.c`, `notes.c`, `config.c`, `environment.c`, `sub-process.c`, and `trace2`. Key technical aspects include:
- Replacing manual memory management with `string_list` utilities
- Preserving input validation while reducing code
- Maintaining behavior while simplifying array access patterns
- Addressing edge cases like the awkward `max` parameter in `string_list_split()`
- Adding explicit comments about API design issues

Notable changes in this revision:
- Patch 6/11 (notes.c) clarifies the commit message about stdin parsing
- Patch 7/11 (config.c) fixes two typos in the commit message
- Patch 9/11 (sub-process.c) removes an unnecessary `string-list.h` include
- Adds two new patches (10-11/11) for trace2 subsystem cleanup

The series demonstrates consistent patterns of behavior-preserving conversions with net code reduction (-137/+125 overall). It appears ready for merging as a clean refactoring that modernizes string handling while reducing complexity. The work represents focused, non-controversial improvements aligned with ongoing string handling modernization efforts.