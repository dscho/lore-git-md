Test patch completing the `git refs list` series by adding comprehensive test coverage. This is the final patch in the RFC v4 series introducing a new `git refs list` subcommand as a thin wrapper around `for-each-ref` functionality.

The patch adds `t/t1461-refs-list.sh` as a minimal test driver that reuses the shared test infrastructure from the previous patch (4/5). By setting `git_for_each_ref='git refs list'` and sourcing the shared `for-each-ref-tests.sh` library, it ensures the new command undergoes identical testing to `git for-each-ref`. This verifies behavioral parity while avoiding test duplication.

Key technical elements:
- Simple 8-line test script that acts as an adapter
- Leverages shared test library created in patch 4/5
- Follows established pattern seen in `git-blame`/`git-annotate` tests
- Added to meson.build test suite integration
- Maintains same test coverage as `for-each-ref`

The implementation appears clean and well-integrated with the series' architectural approach. By building on the shared test infrastructure established earlier, it demonstrates the maintainability benefits of the refactoring work done in prior patches. The test strategy aligns with the series' goal of creating `git refs list` as a true drop-in replacement for the corresponding `for-each-ref` functionality.