This is a follow-up discussion about the final patch in a refactoring series that replaces `strbuf_split*()` usage across Git's codebase. The specific patch in question (9/9) refactors sub-process status reading in `sub-process.c` by replacing `strbuf_split_str()` with direct `skip_prefix()` matching.

Junio Hamano confirms Christian Couder's observation that including `string-list.h` is unnecessary in this case, as the patch ultimately didn't use any string-list functionality despite initially considering it. The patch had already simplified the status parsing by:
- Eliminating the temporary strbuf array allocation
- Using direct prefix matching instead of token splitting
- Maintaining the existing last-status-wins behavior
- Reducing code size (-9/+7 lines)

This clarification resolves the only outstanding question about the series, which has otherwise been well-received as a clean refactoring that modernizes string handling while reducing code complexity. The series as a whole has demonstrated consistent patterns of replacing manual memory management with more appropriate string utilities while preserving behavior across multiple subsystems.

The discussion indicates this patch (and likely the entire series) is ready to proceed, with only this minor header inclusion cleanup needed before finalization. The exchange highlights the careful review process even for straightforward refactorings in the Git project.