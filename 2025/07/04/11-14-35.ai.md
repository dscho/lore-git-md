This is a refactoring patch in the bloom filter optimization series, specifically addressing API naming consistency. The patch renames two functions that operate on `struct bloom_key` to follow Git's naming convention where functions operating on a struct S should be named S_verb.

The changes rename `fill_bloom_key()` to `bloom_key_fill()` and `clear_bloom_key()` to `bloom_key_clear()`. This affects multiple files including bloom.c/h, revision.c, blame.c, line-log.c, and the test helper code. The changes are mechanical and straightforward, updating all call sites to use the new names.

This patch is part of the v4 iteration of the bloom filter optimization series that has been through multiple review rounds. The current focus is on final API polish, with this change specifically addressing Junio Hamano's earlier concerns about API naming inconsistencies. The patch maintains all existing functionality while bringing the bloom filter API into better alignment with Git's coding conventions.

The changes to test-bloom.c confirm that the test infrastructure continues to work with the renamed functions. The patch demonstrates good attention to detail by updating all call sites across the codebase, including less obvious locations like the blame and line-log subsystems that also use bloom filters.