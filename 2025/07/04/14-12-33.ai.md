This is the final version of a 2-patch series completing the removal of `the_repository` usage from `builtin/prune.c`, part of the broader `the_repository` removal effort. The series has gone through multiple iterations since its initial submission on June 8, with all review feedback now addressed.

The first patch moves the `repository_format_precious_objects` global variable into `struct repository`, updating all affected code paths. The second patch completes the conversion of `builtin/prune.c` to use repository instances instead of `the_repository`, including proper initialization sequence handling. A key addition in this version is the test case in `t/t1517-outside-repo.sh` verifying that `git prune -h` works without repository initialization, following the pattern established by similar tests like `update-server-info -h`.

Technical changes in this version include formatting improvements to the commit message of patch 1, moving the `repo_init_revisions()` call after `parse_options()` in patch 2 for better command lifecycle handling, and adding the aforementioned test case. The solution demonstrates proper handling of repository-specific configuration flags during `the_repository` removal, with the test pattern potentially serving as a template for other commands undergoing similar conversions.

All technical and stylistic feedback has been incorporated, with the test addition being the last verification requirement before merge. The series is now ready for final review, with the test location approved by Junio Hamano and the technical approach previously approved by Patrick Steinhardt. This represents a clean reference implementation for handling repository-specific configuration flags in the ongoing `the_repository` removal effort.