Documentation patch clarifying the long-standing behavior of empty template parameters in `git init`. 

Todd Zullinger provides additional historical background on the feature, pointing to the original 2008 commit (d65d2b2fb4) that implemented the behavior where `--template=` skips template copying. He notes this has been stable for 17 years and suggests documenting it in the TEMPLATE DIRECTORY section of git-init(1). 

The email helpfully suggests checking and documenting the behavior consistency across all template specification methods (`--template`, `init.templatedir`, and `GIT_TEMPLATE_DIR`), proposing a concise way to cover all cases if they behave identically. This builds on the thread's earlier confirmation that the feature is actively used by package managers and other tools.

The patch discussion remains non-controversial, with clear consensus that documenting this existing behavior would be beneficial. Todd's technical suggestions provide useful guidance for anyone preparing the documentation update.