This is the first patch of a 4-part series introducing `--skip-until` pagination for `git for-each-ref`. The patch refactors the ref iterator interface by moving it from an internal header to the public `refs.h` header.

The change exposes the `ref_iterator` struct and related functions that were previously internal to the refs subsystem. This allows advanced users to directly use the iterator interface rather than being limited to the wrapper functions. The commit explains that as functionality grows, wrapper functions become unwieldy, so exposing the underlying iterator provides more flexibility.

The patch is a straightforward code movement - it takes the iterator documentation and implementation from `refs-internal.h` and copies it to `refs.h` while cleaning up the now-redundant internal version. The functionality remains identical, just more accessible. This foundational change enables the subsequent patches that will implement the new pagination feature using these exposed interfaces.

The implementation looks clean and well-documented, with comprehensive comments explaining the iterator usage patterns. As the first step in a larger series, it does exactly what's needed - making the necessary infrastructure available without introducing new behavior. The commit message clearly explains the motivation and the thread context shows this is part of a well-planned feature addition.