This is a refactoring patch in the `--skip-until` pagination series for `git for-each-ref`. It removes the unused `find_ref_entry()` function from the ref-cache implementation, cleaning up both the .c and .h files. The function was previously used to look up references by name in the files backend's cache structure but is no longer needed in the current codebase.

The patch is straightforward - it simply deletes the function definition and its header declaration, along with the associated documentation comment. This is patch 2/4 in the v2 series, following the ref iterator interface exposure in patch 1. The removal appears safe as the function wasn't being called anywhere, and the change aligns with the series' broader goal of refactoring the ref iteration infrastructure to support efficient pagination.

The commit message is minimal but sufficient for this type of cleanup change. The diff shows the exact lines being removed, which is helpful for reviewers to verify there are no unintended side effects. Since this is part of a larger series, the impact should be considered in context with the other changes, particularly the new ref iteration patterns being introduced in the surrounding patches.