This is a bugfix patch that resolves the regression in `git log --diff-filter` behavior where lowercase exclusion filters weren't working with default output formats. The patch builds on Jeff King's earlier analysis that identified commit 75408ca949 as introducing the regression by splitting negative filter bits into a separate `filter_not` field without updating `setup_revisions()` to check this field.

The technical fix is minimal - a one-line change in `revision.c` that makes `setup_revisions()` consider both `filter` and `filter_not` fields when determining if diffs are needed. The patch includes a new test case in `t/t4202-log.sh` that verifies the corrected behavior where exclusion filters now work regardless of output format.

Karthik Nayak's email serves as a review/acknowledgment of Jeff King's fix, confirming the solution is correct and well-explained. The thread appears fully resolved with this patch, which maintains backward compatibility while restoring the expected functionality where exclusion filters should operate at the commit selection level. The fix is ready for merging through normal processes, with no outstanding questions or concerns remaining in the discussion.