This thread addressed a build system issue affecting FreeBSD, where recent changes to `sysinfo()` detection conflicted with the platform's preferred `sysctl()` approach. Ramsay Jones proposed a solution that reorders build system checks in `configure.ac` and `meson.build` to prioritize BSD's native `sysctl()` (HAVE_BSD_SYSCTL) over `sysinfo()` compatibility (HAVE_SYSINFO).

Eric Sunshine reviewed the patch, noting that while modifying preprocessor conditionals in `builtin/gc.c` would provide a more comprehensive fix for edge cases, the current solution is adequate and well-documented. Renato Botelho verified the fix works on FreeBSD. The change preserves behavior on non-BSD systems while resolving the platform-specific build failure, touching only build system files with clear conditional logic.

Junio Hamano has now acknowledged the solution, confirming consensus and effectively merging the change. The fix is straightforward and uncontroversial, with no open questions remaining. It resolves the build issue without requiring changes to core Git functionality or the Makefile build system.