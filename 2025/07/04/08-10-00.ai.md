This is a proposal in the ongoing discussion about remote naming ambiguity in Git, specifically addressing the issue of forward slashes in remote names causing refname collisions. 

Lidong Yan responds to Patrick Steinhardt's suggestion about conflict detection by proposing an alternative approach: prohibiting remotes where one name is a prefix of another (like "origin" and "origin/chat"). This would prevent the ambiguity at the remote definition level rather than detecting conflicts during refname resolution.

The suggestion builds on the thread's technical context about refname parsing in `remote.c` and the trade-offs between strict validation and backward compatibility. Yan's proposal appears to be a stricter variant of Steinhardt's conflict detection idea, potentially preventing the problematic cases entirely rather than just detecting them.

Key technical aspects remain the same as in previous discussion: remote handling in `remote.c`, refname parsing logic, and the balance between preventing ambiguity and maintaining compatibility with existing repositories. The proposal doesn't address implementation details or migration paths for existing repositories with conflicting remote names.

This continues the pattern of contributors offering alternative solutions to the core problem while maintaining the thread's focus on finding a solution that is both correct and minimally disruptive. The discussion remains focused on the technical implementation in Git's remote and ref handling subsystems.