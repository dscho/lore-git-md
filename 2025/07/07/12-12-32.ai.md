This is the third patch in a bugfix series addressing `git apply --intent-to-add` behavior. The patch adds comprehensive test coverage for the fixed functionality, specifically verifying interactions between intent-to-add and existing index entries.

The test file `t4140-apply-ita.sh` is expanded with two new scenarios: applying a creation patch to an existing index (verifying it doesn't modify tracked files while correctly adding ITA entries) and applying a complex patch with both modifications and new files (ensuring ITA state is properly set for new files while existing file changes are handled normally). The tests use `git ls-files --stage` to verify index states and `git diff` to check working tree changes.

The test setup now includes a committed file alongside the ITA test file, allowing verification of mixed scenarios. This builds on the previous patches' fixes for index reading/writing logic, providing concrete validation that the core fixes work as intended in realistic usage. The test cases appear thorough, covering both simple and complex interactions that were previously broken.

This patch completes the technical implementation of the series, with the final patch being a documentation update. The tests specifically validate the key behavior changes from patches 1-2, including the `check_index || (ita_only && is_new)` condition for ITA entry creation.