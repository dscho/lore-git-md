Git for Windows regression report and diagnostic follow-up. Fabio Frumento originally reported Git for Windows 2.50.0 failing to launch on Windows 11 Pro 64-bit (terminal window closes immediately, fixed by downgrading to 2.49.0. Brian M. Carlson confirmed this as a Git for Windows-specific issue. Maintainer Johannes Schindelin provided diagnostic instructions: test with v2.50.1 and run bash directly in PowerShell with `& "C:\Program Files\Git\bin\bash.exe" -li` to capture error output. Fabio now reports bash works with `--norc` but fails with default `--login -i -v` options, narrowing the issue to shell initialization. A screenshot shows brief window appearance before closure, though text content isn't described. Current evidence points to rc file processing or login shell initialization as the failure point rather than fundamental incompatibility. Maintainer now has two key data points: working `--norc` case versus failing login shell case, plus visual crash timing evidence. Next steps involve analyzing the screenshot for error messages and comparing initialization paths between working and broken configurations. Thread remains in diagnostic phase with maintainer actively engaged, though root cause still unknown - likely a Windows-specific packaging or environment interaction affecting certain configurations during shell startup.