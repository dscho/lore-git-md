Updated AI thread summary:

Git for Windows regression report and diagnostic follow-up. Fabio Frumento originally reported Git for Windows 2.50.0 failing to launch on Windows 11 Pro 64-bit (terminal window closes immediately), fixed by downgrading to 2.49.0. Brian M. Carlson confirmed this as a Git for Windows-specific issue. Maintainer Johannes Schindelin provided diagnostic instructions leading to discovery that bash works with `--norc` but fails with default `--login -i -v` options, narrowing to shell initialization. Further investigation by Fabio isolates the crash to command substitution (`$(...)`) in initialization files, specifically `/etc/bash.bashrc` (line 57 with `declare -p PS1`) and `/etc/profile.d/git-prompt.sh` (line 22 with `git --exec-path`). Any command substitution triggers silent failure, indicating a fundamental issue with command execution in Windows bash environment. The reporter questions whether this is local or widespread, though multiple users confirm similar symptoms. Maintainer now has complete diagnostic picture: working `--norc` case, failing login shell case, exact failure triggers (command substitution), and specific files/lines involved. Root cause remains unknown but narrowed to command execution during shell initialization in Windows-specific environment. Thread shows active maintainer engagement with clear reproduction steps and failure patterns established, moving investigation from general crash reporting to focused command execution analysis. Next steps likely involve debugging bash's command substitution handling on Windows and comparing behavior between 2.49.0 (working) and 2.50.0 (broken) versions.