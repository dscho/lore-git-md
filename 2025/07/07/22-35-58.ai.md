This is a final review request for the promisor-remote capability extension series, now at v5. Junio Hamano is checking if there are any remaining loose ends before merging the series into 'next'. The changes since v4 are minimal: patch 1 got expanded commit message explanations about the `struct strvec` design choice and a fixed typo, plus an improved BUG() message. Patch 2 added some code comments in `fields_from_config()`. 

The series has seen thorough review from multiple contributors (Patrick Steinhardt, Karthik Nayak, Justin Tobler) with all feedback addressed. Christian Couder confirms CI is passing and considers this version final. The only outstanding discussions are minor documentation formatting questions (BNF vs synopsis format, backtick consistency) and one philosophical question about NULL checks in `should_accept_remote()`. 

This represents the final administrative step before merging a series that extends the promisor-remote protocol to support configurable validation of remote attributes like `partialCloneFilter` and `token`, with both server-side advertisement and client-side verification. The implementation is complete and tested, with documentation now undergoing final polish from Jean-Noël Avila. The changes maintain backward compatibility while adding security through strict field handling between servers and clients.