Junio Hamano responds to Carlo Marcelo Arenas Belón's v4 patch fixing BSD system memory detection in `git gc --auto`. This is part of the ongoing bugfix series addressing incorrect memory calculations from `sysctl()` calls.

Junio raises two specific technical concerns about the current implementation:
1. Questions the use of `unsigned` for the 4-byte fallback case, suggesting `uint32_t` would be more appropriate if available (which it should be in Git's codebase)
2. Challenges the endianness-specific conditional compilation, arguing the fallback path should work universally since this isn't performance-critical code

The response indicates Junio is still evaluating the complexity tradeoffs in the v4 approach, particularly around the endian-aware handling of partial sysctl returns. His comments suggest a preference for simpler, more maintainable code over comprehensive but complex solutions, continuing the thread's central tension between correctness and simplicity.

This moves the discussion toward potentially:
- Standardizing on fixed-width types (`uint32_t`) for the fallback path
- Removing endian-specific conditions to simplify the code
- Maintaining the core functionality improvements from earlier versions (proper type handling, length verification) while reducing complexity

The exchange shows the careful balancing act in systems-level Git code between platform compatibility, type safety, and maintainability.