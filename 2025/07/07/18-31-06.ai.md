This is the final patch in the `for-each-ref` pagination series, addressing Junio C Hamano's review feedback on the error message wording for the patch implements the `--skip-until` option (now renamed to `--start-after` in subsequent versions) for paginating ref listings.

The key exchange here resolves the last remaining issue before merging: Junio notes a missing "with" in the error message when combining pagination with sorting options (`cannot use --skip-until with custom sort options`). While acknowledging there might be theoretical use cases for combining pagination and sorting, both agree the current design decision to keep them mutually exclusive is sound for usability, with potential to revisit if concrete needs emerge.

This completes all technical and UX aspects of the pagination feature, which has evolved through naming discussions (`--skip-until` -> `--start-with` -> `--start-after`) and implementation refinements across three versions. The series now has:
- Consistent O(log N) seeking across all ref backends
- Clear documentation of the name-based pagination behavior
- Robust handling of edge cases and concurrent modifications
- Finalized error messaging

With this last wording fix, the series is fully reviewed and ready for merging, representing a complete solution for efficient ref listing pagination in Git.