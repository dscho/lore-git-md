This is a bugfix patch in the SSH signing subsystem, addressing a tempfile cleanup issue while refining the test implementation. It's the third version of a fix for a memory leak where detaching a filename strbuf from a tempfile structure prevented proper cleanup.

The technical changes include:
- Replacing `strbuf_detach()` with `xstrdup()` in `sign_buffer_ssh()` to maintain symmetry with the other code path that uses `interpolate_path()`
- Updating the test to verify tempfile cleanup while maintaining isolation via subshell TMPDIR
- Switching to `test_must_be_empty` helper for verification
- Merging the test case with existing SSH-agent tests as suggested in previous discussion

The patch implements the test refinements discussed in the thread, including Patrick Steinhardt's subshell isolation approach and Phillip Wood's preference for combined tests. It maintains the core fix while addressing the test suite concerns raised in previous rounds. The changes show responsiveness to feedback while keeping the original bugfix intact.

Key implementation details for future reference:
- File modified: `gpg-interface.c` (SSH signing path)
- Test file: `t/t7528-signed-commit-ssh.sh`
- New test pattern: subshell with exported TMPDIR + `test_must_be_empty` verification
- Maintains symmetry between key file handling paths
- Uses `test_unconfig` for cleanup outside subshell

The patch appears ready for final review, having incorporated feedback from multiple contributors (brian m. carlson, Patrick Steinhardt, Phillip Wood) while resolving the original memory leak issue. The test refinements address both isolation concerns and diagnostic clarity through the combined test approach.