Bugfix for test script following Perl-to-shell conversion. This is the second patch in a series addressing minor issues that surfaced after removing Perl dependencies from test scripts. The patch fixes a missing terminator in a `sed` command in t5333-pseudo-merge-bitmaps.sh that was silently causing the test to pass for the wrong reasons. Patrick Steinhardt explains how the broken `sed` command led to a cascade of failures that coincidentally produced the expected test outcome, while SZEDER Gábor originally reported the issue.

The fix adds the missing pipe character to properly terminate the `sed` substitution command and makes the test more robust by avoiding a pipe that masked the `sed` failure. The change is minimal (2 lines modified) and clearly explained, with the commit message detailing both the problem and solution. This continues the thread's theme of cleaning up test scripts converted from Perl to shell utilities, following up on work from commit 6aec8d38fdd. The patch appears straightforward and well-justified, addressing a subtle but real issue in the pseudo-merge bitmap test infrastructure.