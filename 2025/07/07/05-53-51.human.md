This is a design discussion about metadata preservation in Git's email workflow, specifically regarding Junio Hamano's counter-proposal to the change-ID tracking patch series.

Junio strongly advocates for using commit trailers rather than email headers for change-IDs, but proposes a more generic solution for preserving arbitrary commit metadata through email workflows. His approach would add a `--extra-headers` option to `git format-patch` that would encode all non-standard commit headers (like `X-Change-ID`) as `X-git-extra-commit-header` email headers, with proper encoding handling.

The key distinction is that Junio sees this as a general mechanism that could handle both well-designed metadata (like properly standardized change-IDs) and poorly-placed metadata from third-party tools. He emphasizes that while trailers would be preferable for change-IDs specifically, the generic solution would provide a robust way to preserve any commit header metadata through email workflows.

This represents a significant shift in the discussion from the original patch's specific change-ID implementation to a broader design question about metadata preservation. The proposal is still conceptual - no implementation is provided - but it suggests a path forward that could satisfy both the immediate change-ID use case and future needs. The thread remains unresolved, with the fundamental question being whether to pursue this generic approach or stick with more targeted solutions.