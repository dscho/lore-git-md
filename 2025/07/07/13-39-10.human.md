This is a technical discussion about memory detection fixes for `git gc --auto` on BSD systems, continuing the thread about proper handling of `sysctl()` calls. Junio Hamano responds to Carlo Marcelo Arenas Belón's v4 patch with two specific concerns about the implementation details.

First, he questions the use of plain `unsigned` for the fallback 4-byte memory read, suggesting `uint32_t` would be more appropriate if available (which Git should ensure). Second, he challenges the logic of limiting the 4-byte fallback to little-endian systems, noting that since this isn't performance-critical code, the fallback could work universally.

The exchange shows Junio continuing to push for simplification of the solution, building on his earlier feedback that the v4 approach may be over-engineered. This follows the thread's pattern where each iteration has addressed prior concerns but introduced new complexity, with Junio now questioning whether some of the endianness handling is truly necessary. The discussion remains focused on finding the right balance between correctness across platforms and code maintainability.