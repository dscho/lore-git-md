This is the documentation patch in a 4-part series fixing `git apply --intent-to-add` behavior. The patch clarifies the documentation for the `--intent-to-add` option, removing a double negative and making the distinction between `--index` and `--cached` modes clearer. The changes specify that the option is ignored when either `--index` or `--cached` are used (rather than just `--index`), and maintains the note about repository requirements.

The documentation update is straightforward and aligns with the implementation changes made in earlier patches of the series. It matches the technical decisions made in patches 1-3 regarding when intent-to-add behavior should be active (only for working tree operations within a repository). The wording changes improve clarity without altering the documented behavior.

This patch completes the series by ensuring the documentation accurately reflects the fixed implementation. The series as a whole appears technically sound, with thorough test coverage and clear documentation updates. The changes are ready for integration.