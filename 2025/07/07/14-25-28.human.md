This is a bugfix thread about SSH signing tempfile cleanup, now discussing test implementation details. The technical fix for the memory leak (using `xstrdup()` in `sign_buffer_ssh()`) has already been approved, and the conversation has shifted to refining the test coverage.

Redoste responds to two suggestions from Phillip Wood about the test implementation: (1) using a subshell to contain the TMPDIR variable (acknowledging Patrick Steinhardt's earlier observation about environment leakage), and (2) adopting the more idiomatic `test_file_is_empty` helper instead of spawning `wc`. 

The more substantive discussion is about whether to modify an existing SSH-agent test to include tempfile checks (Phillip's preference for test suite speed) versus keeping a separate test (Redoste's concern about test clarity and single responsibility). Redoste notes they had previously separated these concerns based on brian m. carlson's advice in an earlier round.

The thread shows good technical alignment on the core fix, with only minor test implementation details being worked out. The parallel discussion about pseudonym policy documentation (mentioned in the thread context) does not appear in this particular email. The patch appears ready for merging once these last test refinements are settled.