This is a final documentation and code consistency patch in the promisor-remote validation series. It completes the string constant standardization work started in earlier patches by adding dedicated constants for the mandatory "name" and "url" fields, matching the approach already taken for "partialCloneFilter" and "token".

The change replaces hardcoded string literals with the new `promisor_field_name` and `promisor_field_url` constants in three locations: the protocol capability documentation comment, the server-side advertisement string construction in `promisor_remote_info()`, and the client-side parsing logic in `parse_one_advertised_remote()`. This maintains consistency with the field handling pattern established for the optional fields in previous patches.

As the final patch in the v6 series, this represents a minor but logical cleanup that brings the mandatory fields into alignment with the optional field handling approach. The changes are straightforward string substitutions with no behavioral impact, and the patch follows the same style and patterns used elsewhere in the series. With this applied, all promisor-remote field handling now follows the same constant-based pattern, reducing magic strings and improving maintainability.