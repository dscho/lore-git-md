This is a code review response in the promisor-remote protocol extension series, specifically addressing patch 3/5 which refactors the parsing of advertised remote fields. Junio Hamano provides detailed feedback on the implementation of `parse_one_advertised_remote()`, focusing on API usage and string handling.

Key technical points raised:
1. Suggests renaming `elems` to singular `element` for better readability when accessing array indices
2. Recommends replacing `strbuf_split()` with `string_list_split_in_place()` as a more appropriate API choice, noting that `strbuf` arrays are generally not ideal for parallel string editing
3. Points out that the current implementation leaks memory when discarding unneeded decoded values
4. Questions whether `url_percent_decode()` should be enhanced to detect malformed input, or alternatively suggests considering syntax changes if the current helper can't be improved
5. Notes that the error handling for missing name/URL fields is correctly implemented

The review maintains the thread's focus on implementation quality while avoiding rehashing the ongoing terminology debate about "fields" vs "configuration variables". It provides specific, actionable feedback about memory management and API choices in the parsing logic. The suggestions align with Git's code quality standards and would make the parsing more robust while potentially simplifying memory management.