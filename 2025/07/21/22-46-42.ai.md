This is a refactoring patch that updates a NEEDSWORK comment in `builtin/rev-list.c` about broken option parsing behavior. The patch comes from Junio Hamano as a follow-up to earlier discussion about relocating `struct rev_list_info` and clarifying comments about rev-list's option handling.

The patch rewrites a verbose and unclear NEEDSWORK comment into a more focused explanation of the core issue: the current loop attempts to detect option usage without properly understanding which options take arguments, leading to incorrect parsing (e.g., failing to recognize `--exclude-promisor-objects` when preceded by `--grep`). The new comment more clearly states the fundamental limitation and proposes the same solution - having `setup_revisions()` implement proper early option parsing with command-specific allow/disallow rules.

The changes are purely comment edits with no code modifications. The thread context shows this concludes the discussion about comment phrasing that began with Jeff King questioning an earlier edit. The core refactoring (moving `struct rev_list_info`) remains uncontested, while this final iteration on the NEEDSWORK comment provides clearer documentation of the known parsing issue for future contributors.