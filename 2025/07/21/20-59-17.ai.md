This is a review response in the promisor-remote protocol extension series, focusing on the client-side field validation implementation (patch 4/5). Junio Hamano provides several technical suggestions for improvement while acknowledging the overall approach.

Key technical feedback includes:
1. Style nit: Reminds that static ints don't need explicit zero initialization (BSS handles it) and suggests documenting this in CodingGuidelines
2. Code formatting: Points out the asterisk in pointer declarations should stick to the variable name (`string_list *fields`) not the type
3. Design question: Questions whether case-insensitive field comparison is truly necessary, suggesting case-sensitive matching would simplify both this loop and `match_field_against_config()` by allowing direct string lookups rather than iteration

The review indicates the core validation logic (three-way matching of advertised vs configured fields) is functionally sound, but could be made more minimal and consistent with Git's typical case-sensitive string handling. No behavioral changes are requested beyond these implementation refinements.

This continues the thread's pattern of Junio pushing for simpler, more minimal interfaces while Christian Couder maintains the user-facing design choices. The exchange highlights ongoing tension between user convenience (case-insensitive matching) and code simplicity (direct lookups with case sensitivity).