This is a code review in the promisor-remote protocol extension series, focusing on parsing refactoring. Junio Hamano provides detailed technical feedback on patch 3/5, which introduces `parse_one_advertised_remote()` to centralize remote attribute parsing.

The review suggests several improvements to the string handling implementation. Junio recommends using `string_list_split_in_place()` instead of `strbuf_split()` for better memory management, notes potential issues with silent error handling in `url_percent_decode()`, and points out that the variable naming could be more precise (using singular form for array elements). He also confirms the proper memory management of allocated strings in the current implementation.

While the review identifies areas for improvement, it acknowledges the core functionality is sound, particularly praising the validation of mandatory name/URL fields. This continues the thread's technical refinement phase, with all major architectural decisions already settled in earlier rounds. The feedback focuses on implementation polish rather than fundamental changes.