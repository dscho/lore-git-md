This is a discussion about output format design for the new `git repo` command series, specifically focusing on the `info` subcommand. The email continues the conversation between Lucas Seiki Oshiro and Justin Tobler about how to structure the command's output formats.

Justin responds to Lucas's suggestions about format variants, agreeing that `porcelain` (default), `keyvalue`, and `null` formats would work, with `-z` as an alias for the null format. He expresses uncertainty about whether the default "user-friendly" format should differ from the key-value format for the `info` subcommand, suggesting they could start with just porcelain and null formats and add more later if needed.

The email also discusses Lucas's idea of automatically switching to null-terminated output when piping (similar to `git diff`'s color behavior), with Justin leaning toward making format selection explicit rather than automatic. This reflects the ongoing refinement of output format decisions in the series, where core design choices are largely settled but some interface details remain open for discussion.

Key technical points:
- Format types under discussion: `porcelain` (default), `keyvalue`, and `null`/`nul`
- `-z` would alias `--format=null`
- Question about whether default format should differ from keyvalue for `info`
- Potential future feature: automatic format switching when piped (like git diff colors)
- Output format stability guarantees (machine-readable formats remain stable while default may evolve)

The discussion shows the series is in its final polishing phase, with participants working through remaining interface decisions while maintaining the established architectural foundation of three format types with different stability guarantees.