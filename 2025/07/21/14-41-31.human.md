This is a discussion email in the ongoing `git repo` command implementation thread, focusing on output format design decisions.

Justin Tobler responds to Lucas Seiki Oshiro's suggestions about output formats for the new `git repo` command. The discussion centers around three proposed format types: a default format (currently called "porcelain" or "user"), a stable "keyvalue" format, and a null-delimited format (with `-z` as an alias). 

Justin agrees with Lucas's naming suggestion of "porcelain" for the default format and "null" for the null-delimited version. He expresses some uncertainty about whether the default format should differ from the key-value format for the `info` subcommand, but is open to adding a separate human-readable format later if needed. 

The email also touches on a potential future feature where the command could automatically switch to null-delimited output when piped (similar to `git diff`'s color behavior), though Justin leans toward keeping format selection explicit rather than automatic. This continues the thread's pattern of refining implementation details while maintaining the core architectural decisions about output formats.