This is a refactoring patch that concludes the promisor-remote protocol extension series. It introduces string constants for the mandatory "name" and "url" fields in the promisor-remote protocol, bringing them in line with the already-constantized "partialCloneFilter" and "token" fields from earlier in the series.

The change eliminates repeated string literals in the code, making future maintenance easier and reducing the chance of typos. The patch touches the same areas modified earlier in the series - the protocol capability handling and remote parsing logic - but focuses purely on this internal consistency improvement rather than adding new functionality.

As the final patch in a well-reviewed series (now at v6), it represents a straightforward cleanup that maintains the established patterns while completing the project's goal of properly constantizing all protocol field names. The changes are minimal and focused, with no behavior alterations - just replacing literal strings with their constant equivalents in three locations.