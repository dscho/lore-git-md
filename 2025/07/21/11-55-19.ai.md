Test reorganization patch completing a 2-patch series to finalize the help output verification strategy. This second patch demonstrates the repository-dependent test movement principle by relocating the `update-server-info -h` test from t1517-outside-repo.sh to t5200-update-server-info.sh, where it logically belongs with other tests for that command.

The change is straightforward - removing the test from t1517 and adding it to t5200 with identical content. This follows the architectural direction established in the thread to keep repository-dependent tests with their command-specific test files while t1517 focuses exclusively on outside-repo testing. The patch maintains the same verification pattern (test_grep + exit code 129) used throughout the help testing framework.

This completes the demonstration of both parts of the test strategy: automated outside-repo testing via `--list-cmds` (handled in patch 1) and proper organization of repository-dependent tests (shown here). The changes are minimal (5 lines moved) but important for long-term test maintainability. The patch carries suggestions/acks from both Patrick Steinhardt (who proposed the original consolidation) and Junio Hamano (who provided the architectural direction), indicating consensus on the approach.

Key technical details: The moved test verifies basic help output behavior for `update-server-info` when run with `-h` flag, checking both the usage pattern output and expected exit code. Its new location in t5200 groups it with other tests for the same command, including tests for midx behavior and pack entry handling. The test case itself remains unchanged, just relocated to its proper logical home.