This is a follow-up discussion in the ongoing thread about `git grep -h` alias behavior, focusing on the design philosophy of how `-h` should interact with aliases.

Jeff King (Peff) responds to Junio Hamano's preference for a simpler approach where `git foo -h` would only show the alias expansion without executing the underlying command. Peff had previously suggested more complex solutions involving argument dropping and shell command handling, but now concedes that Junio's simpler approach is likely better.

The key technical point is the resolution to treat `-h` consistently across all alias types (both built-in and shell commands) by showing only the alias expansion. This avoids edge cases that would arise from trying to execute the underlying command while maintaining Git's textual substitution model for normal operation. Peff acknowledges that automatically showing the sub-command's output (his original suggestion) may not be as valuable as he initially thought.

The discussion has reached consensus on Junio's proposed behavior, marking a philosophical resolution to treat `-h` as a special case where Git should show alias information without execution. This represents a clean solution that avoids "voodoo magic" (Junio's term) while providing consistent user experience. The thread appears to be winding down with this design direction settled, though documentation updates may still be needed to formalize the behavior.