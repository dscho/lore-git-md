This is a test modernization patch in the ongoing diff context configuration series. It converts grep calls to test_grep in t3701-add-interactive.sh and t4055-diff-context.sh as a preparatory step for the main feature implementation.

The patch makes 38 grep-to-test_grep conversions across both test files, following the test infrastructure modernization that was discussed earlier in the thread. The changes are mechanical replacements that maintain the same test assertions while providing better debugging output when tests fail. The conversions include both positive matches (test_grep "pattern") and negative assertions (test_grep ! "pattern").

This change is part of the broader effort to standardize test practices in Git's test suite, where test_grep is preferred over raw grep for its improved failure diagnostics. The patch doesn't introduce new functionality but prepares the test infrastructure for the subsequent patches in the series that will implement diff context configuration support for interactive patch commands.

The thread context shows this test modernization was identified as a prerequisite early in the series (v3) and has been consistently maintained through all iterations, with the final v5 version now including all 45 test_grep/test_config conversions (this patch covers the grep portion). The changes align with the series' goal of comprehensive test coverage and modernization.