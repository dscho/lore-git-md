This is a test infrastructure refinement in the context of the `repo info` command series. Eric Sunshine corrects his own recommendation about null-byte handling in test scripts, adding missing quotes around a variable expansion for safety. The correction changes `printf "%s\0" $expected_value` to `printf "%s\0" "$expected_value"` when writing test expectations.

This micro-fix aligns with the thread's consensus around robust test handling, where proper quoting prevents potential issues with whitespace or special characters in test values. The discussion had previously settled on using `printf "%s\0"` as the preferred method for null-byte handling in tests, with Ben Knoble initially advocating for the quoted form that Eric now endorses.

The email doesn't introduce new functionality but reinforces the careful approach to test scripting that has been a theme throughout the review. It demonstrates the attention to detail in the series' test infrastructure, particularly around the machine-parsable `nul` format output testing. The correction maintains consistency with Git's test conventions while addressing a subtle safety consideration in shell scripting.