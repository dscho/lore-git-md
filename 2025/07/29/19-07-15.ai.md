This is a technical clarification in the ongoing `the_repository` removal thread regarding merge log configuration behavior. Phillip Wood points out a subtle but important behavioral change in how `merge.log` and `merge.summary` config values interact in the new implementation.

The key observation is that while the old code processed config values in order (last one wins), the new `adjust_shortlog_len()` function always prefers `merge.log` over `merge.summary` regardless of their order in config files. Phillip suggests this breaks Git's standard config precedence rules and that preserving the original behavior would require keeping the callback-based approach that processes values sequentially.

This adds a new dimension to the existing discussion about config validation behavior, highlighting that the refactoring affects not just error reporting but also config precedence semantics. The comment implies the current patch may need adjustment to maintain consistent config processing behavior while still achieving its `the_repository` removal goal. The issue touches on core Git configuration philosophy regarding how multiple values for related settings should interact.