This is a follow-up discussion in the ongoing thread about Git's alias handling with `-h` flags. Jeff King (Peff) responds to D. Ben Knoble's concerns about modifying alias behavior, clarifying the proposed approach.

The exchange digs into philosophical questions about whether aliases should be treated as pure textual substitutions or if Git should apply special logic for help flags. Peff argues that treating `-h` as always meaning help (when used alone) is reasonable, even if it means dropping alias-provided arguments in some cases. He notes this behavior is already enforced for Git's built-in commands.

The discussion also touches on related work around `--help-all` flags, with Peff suggesting that while `-h` deserves special handling, `--help-all` doesn't need the same treatment since its meaning is unambiguous. The thread shows alignment with Junio Hamano's earlier position favoring consistency in alias handling, while still working through edge cases and implementation details.

This email doesn't introduce new patches but helps clarify the design thinking behind the proposed changes. The conversation remains technical but productive, with participants working toward a solution that balances Git's design principles with practical user expectations around help flag behavior.