René Scharfe is working on removing `the_repository` global variable usage from Git, with this thread focusing on changes to the `fmt-merge-msg` command. The 2-patch series moves merge log configuration away from global state while maintaining functionality, but has uncovered some subtle behavioral questions.

The first patch changed how invalid `merge.summary` config values are handled - they now fail silently when overridden by command-line `--log`, whereas previously they would trigger errors during config parsing. Junio Hamano and Ayush Chandekar are discussing whether this change is acceptable. Junio jokingly suggests defaulting to considering any behavior change a regression, but makes clear this isn't a final judgment. The question remains open whether to document this as intentional, restore the original validation behavior, or keep the new silent handling.

The second patch removes `the_repository` usage through mechanical conversions, but Junio noted it incorrectly reorders config parsing to happen after command-line option processing, contrary to Git's standard pattern. This needs to be fixed by either restoring the original order or justifying the change.

The discussion shows how even straightforward refactoring work requires careful attention to Git's configuration semantics and established patterns. The changes touch several files including the merge message formatting code and tests, with the new `adjust_shortlog_len()` function being a key addition. The thread remains active with these two open questions about configuration behavior and processing order.