This is a patch series addressing reflog migration issues between files and reftable backends, with a focus on fixing identity handling and old-oid propagation. The series also introduces a new `git reflog write` subcommand and includes race condition fixes for HEAD updates.

The v3 series builds on previous versions with several key changes: requiring fully-qualified refnames for the new `write` subcommand, adding a new commit to address race conditions in HEAD reflog generation for the files backend, and being based on v2.50.1 for potential backporting. The series now consists of 9 patches that touch documentation, builtin commands, refs infrastructure, and test coverage.

Technical highlights include:
- Fixes for incorrect committer identity in migrated reflog entries
- Correction of all-zero old-oid values during reflog migration
- New `ref_transaction_update_reflog()` export for reflog manipulation
- Type safety improvements in ident handling (changing string length to size_t)
- Race condition mitigation in files backend for HEAD reflog updates
- Changes to `REF_HAVE_OLD` flag behavior for log-only updates
- Comprehensive test coverage including new t1421-reflog-write.sh

The series appears well-structured with clear commit messages and thorough test coverage. The addition of race condition handling for HEAD updates addresses previous concerns raised in the thread, though some fundamental race scenarios may remain unfixable. The changes are motivated by concrete issues found during libgit2's reftable implementation, giving them practical validation.