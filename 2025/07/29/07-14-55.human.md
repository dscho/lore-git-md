Bugfix and refactoring patch in the reflog migration infrastructure series.

Jeff King (Peff) responds to Patrick Steinhardt's earlier patch about `REF_HAVE_OLD` flag handling in reflog updates. The discussion centers around whether log-only updates (those that only write to the reflog without updating the ref itself) should maintain the `REF_HAVE_OLD` flag.

Peff agrees with Patrick's approach of keeping `REF_HAVE_OLD` set for reflog-only updates, noting this is needed for subsequent patches in the series. However, he points out this creates a subtle behavior change for split HEAD updates - they'll no longer perform the extra old-oid check that they used to, while other symref log-only updates maintain their existing behavior.

The email appears to be truncated mid-thought, with Peff starting to explain context for readers unfamiliar with the split_head_update() function but not completing the explanation. This continues the thread's technical discussion about flag handling should work consistently across different types of reflog updates in Git's refs backend.

The patch appears to be a well-considered refinement of the reflog infrastructure, though the truncation leaves some questions about the full context Peff intended to provide. The change seems reasonable but warrants close review given it affects HEAD update behavior.