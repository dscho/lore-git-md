Regular status update from the Git maintainer. This email corrects a previous CI failure report, noting that the `lo/repo-info` topic (which adds a new `git repo` subcommand for querying repository characteristics) and all other in-flight topics except `tc/last-modified` are now passing CI tests in the 'seen' integration branch. The `lo/repo-info` series, which introduces format options to report repository layout and reference format details, had been temporarily ejected from 'seen' due to CI failures but those appear to have been resolved or misreported. The maintainer indicates this topic still needs a reroll before final consideration. This update maintains the project's standard practice of tracking topic branches through integration branches (maint/master/next/seen) with clear status indicators.