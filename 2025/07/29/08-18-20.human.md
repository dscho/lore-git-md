This is a bugfix patch addressing inconsistent behavior of `git diff -I<regex>` across different output formats. Lidong Yan proposes a solution to make `--name-only` and `--name-status` respect ignore patterns the same way `--stat` and `-p` do, while explicitly preserving the current behavior for `--raw` and `--check` outputs.

The patch introduces a new `diffcore-ignore.c` module that implements regex-based change filtering early in the diff pipeline. The implementation carefully handles edge cases like binary files, unmerged changes, and textconv filters. Extensive test coverage demonstrates the fixed behavior across formats and verifies that `--raw` and `--check` remain unaffected.

This directly addresses the thread's core tension between performance optimization and behavioral consistency. The solution strikes a compromise by changing only the human-oriented output formats while maintaining stability for machine-readable ones. The implementation appears thorough, with careful attention to edge cases and clear test coverage. This could resolve the UX surprise while respecting Junio's concerns about backward compatibility for automated tools.