This is a discussion email in the ongoing refactoring series to move sparse-checkout configuration from global variables to repository-specific storage. The email captures a key architectural debate about where these settings should live (`struct repository` vs `repo_settings`) and how they should be initialized.

Derrick Stolee strongly advocates for moving sparse-checkout settings directly into `struct repository` (option 1), arguing they are core repository functionality rather than optional feature flags. He rejects the alternative approach of treating `repo_settings` as core behavior storage (option 2), calling the sharing argument weak. Ayush Chandekar had previously implemented the `repo_settings` approach but now agrees to rework the series per this direction, asking whether related settings like `index.sparse` should also move.

The email surfaces two important technical details: 1) The current `repo_settings` placement was based on a misunderstanding of its purpose as core config storage, and 2) The discussion has clarified that `struct repository` is the more natural home for these settings, aligning with Junio Hamano's earlier concerns about scattered `prepare_repo_settings()` calls. This represents a significant pivot in the series' architecture that will require reworking the patches. The late config parsing issue raised by Phillip Wood remains unresolved but is now secondary to the storage location decision.