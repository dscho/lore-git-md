This is a type safety refactoring patch in Patrick Steinhardt's reflog migration series, specifically addressing the `split_ident_line()` function signature. The change switches the length parameter from `int` to `size_t` to better match its actual usage, as callers typically pass values from `strlen()`, `strbuf::len`, or pointer differences which are naturally unsigned.

The patch (5/9 in v3) modifies both the declaration in `ident.h` and implementation in `ident.c` to use `size_t` for the line length parameter. This aligns with the function's contract where negative lengths wouldn't make sense, as the input comes from string length operations. The change is straightforward with no behavior modification - it simply makes the types more accurate.

This appears to be preparatory work for the larger reflog infrastructure changes, ensuring type safety in identity parsing which is relevant for reflog entries. The patch follows the series' pattern of careful refactoring with clear justification, though it's a minor change compared to the core reflog functionality being introduced elsewhere in the series.