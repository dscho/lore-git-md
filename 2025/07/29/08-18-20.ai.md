This is a bugfix patch addressing the inconsistency between `git diff`'s `-I<regex>` option behavior across different output formats. The patch introduces a new `diffcore_ignore()` function to handle ignore-regex filtering at the diffcore level rather than just in `builtin_diff()`, making the filtering apply consistently to `--name-only`, `--name-status`, and `--stat` outputs while maintaining the current behavior for `--raw` and `--check` formats.

The technical implementation creates a new `diffcore-ignore.c` file with logic to:
1. Check diff hunks against ignore patterns using xdiff infrastructure
2. Handle textconv and binary file cases appropriately
3. Skip filtering for formats where content examination is intentionally avoided (`--raw`, `--check`)
4. Add comprehensive test cases demonstrating the fixed behavior

Key changes include:
- New `diffcore_ignore()` function called from `diffcore_std()`
- `ignore_match()` helper to determine if a filepair should be filtered
- `diff_ignore()` performing the actual regex matching against diff hunks
- Test coverage for various format combinations with `-I<regex>`

The patch directly addresses the thread's core issue by implementing the behavior Arne and Jeff King advocated for (consistent filtering across formats) while respecting Junio Hamano's constraints about maintaining stable `--raw` output. It represents a concrete proposal to resolve the UX inconsistency while preserving performance optimizations for machine-readable outputs.

The implementation appears thorough, with careful attention to edge cases (binary files, textconv, error handling) and includes extensive test coverage. The patch maintains backward compatibility where required while fixing the specific inconsistency that triggered the original bug report.