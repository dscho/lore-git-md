This is a preparatory refactoring patch in a performance optimization series for remote ref operations. Karthik Nayak is reviewing Patrick Steinhardt's first patch which standardizes the reflog callback interface by adding a `refname` parameter to `each_reflog_ent_fn`. This change affects 17 files and will be used by subsequent patches to simplify ref migration logic.

The patch modifies the callback signature to include the reference name being processed, eliminating the need for callers to track this separately. Karthik confirms the change addresses a pain point he encountered in the migration code and approves the implementation. He provides minor formatting suggestions from clang-format for the `refs.c` and `refs.h` changes, though these appear to be purely stylistic adjustments to line wrapping and parameter alignment.

This foundational change enables cleaner handling of reflog entries in bulk operations, particularly for the ref migration work that follows in the series. The thread context shows this patch was already reviewed positively by Justin Tobler and acked by Junio Hamano, with Karthik's review further validating the approach. The change is mechanical but important infrastructure work that supports the more significant performance improvements in later patches.