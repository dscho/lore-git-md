This is a discussion about quoting strategy in the ongoing "repo info" command series. Patrick Steinhardt weighs in on Eric Sunshine's argument for implementing `quote_c_style` output formatting from the start rather than deferring it.

The exchange centers on whether to implement proper quoting in the human-readable key=value output format now (v6) or later. Eric makes a strong case for doing it immediately to establish the convention early and prevent future compatibility issues when new fields with special characters are added. Patrick agrees, noting that while the NUL-terminated machine format should be preferred by tools, the quoting change is simple enough to implement now rather than deferring.

This discussion represents one of the few remaining open questions in the otherwise mature v6 series - the quoting strategy for the keyvalue output format. The thread shows consensus forming around implementing proper quoting now, with Patrick supporting Eric's position against the initial deferral approach mentioned in Lucas's v6 cover letter.

Key technical points:
- `quote_c_style` would handle special characters in output values
- Primarily affects the human-readable `keyvalue` format (not the `nul` format)
- Would establish convention before more fields (including path fields) are added
- Simple change but has long-term compatibility implications

The exchange suggests this may be one of the final issues to resolve before the series can be merged, with both mentors now aligned on implementing quoting immediately.