Refactoring patch in Patrick Steinhardt's reflog migration series (v3 3/9). This change exports the previously internal `ref_transaction_update_reflog()` function to make it available for upcoming reflog infrastructure work, while simplifying its interface by removing the `flags` parameter since all current callers use the same set of flags (REF_HAVE_OLD|REF_HAVE_NEW|REF_LOG_ONLY|REF_FORCE_CREATE_REFLOG|REF_NO_DEREF).

The patch moves the function declaration from refs.c to refs.h with an expanded docstring, maintaining the same core functionality but now enforcing consistent flag usage. The migration code in `migrate_one_reflog_entry()` is updated to use the new signature. This appears to be a straightforward preparatory change for the larger reflog work, with no behavior changes beyond the API exposure.

Key technical details for future reference:
- Function now always uses fixed flags including REF_HAVE_OLD/NEW
- Maintains same parameters otherwise (transaction, refname, oids, committer_info, msg, index, err)
- Docstring clarifies index=0 means ordering by refname
- Changes synchronized with ongoing reflog migration work
- Part of broader effort to support reftable backend transitions