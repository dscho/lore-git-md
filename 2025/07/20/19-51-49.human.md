This is a patch in the ongoing `git repo` command implementation series, specifically addressing error handling for missing subcommands. Lucas Seiki Oshiro points out that the current code has redundant error checking since the parse-options infrastructure (added in commit fa83cc834d) already handles both missing and invalid subcommands by showing appropriate error messages. The patch will simplify the code by removing the now-unnecessary manual error handling, since parse_options() will abort execution for invalid cases.

The change is small but improves code cleanliness by removing redundant logic. This is part of the larger effort to implement a new `git repo` command hierarchy with subcommands, where the implementation is now in the polishing phase with most architectural decisions settled. The thread shows careful attention to edge cases and proper use of Git's existing infrastructure.