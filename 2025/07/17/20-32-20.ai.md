This is part 3/7 of the RFC series proposing Rust as a hard dependency with xdiff optimizations, focusing on refactoring the xdiff implementation. The patch removes three unused fields from xdiff's core data structures: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. These fields were initialized but never referenced in the codebase, indicating they were likely remnants from earlier implementations or planned features that never materialized.

The changes touch key xdiff files (`xprepare.c` and `xtypes.h`), simplifying the code by eliminating dead storage and associated allocations. The modifications include removing hash-related parameters from `xdl_classify_record()`, cleaning up memory management in `xdl_prepare_ctx()`, and updating structure definitions. All behavior remains unchanged - this is purely a cleanup operation.

This refactoring appears well-justified and low-risk, with the commit message clearly stating the rationale. The changes align with the series' broader goal of preparing xdiff for Rust integration by removing unnecessary complexity before the cross-language transition. The patch maintains the series' pattern of incremental, behavior-preserving changes to xdiff internals.