This is a follow-up in the ongoing discussion about the `pull.autoStash` configuration option implementation. Lidong Yan responds to Junio Hamano's query about whether `pull.autoStash` should also apply to merge operations (when `pull.rebase=false`), not just rebase operations.

The key points in this response:
- Clarification that the current implementation does handle `pull.rebase=false` cases (contrary to Junio's initial reading)
- Acknowledgment of Junio's earlier message about command interactions with autostash
- Concern raised about potential user confusion if checking `merge.autoStash` instead of `rebase.autoStash` for merge operations

The discussion continues to center on the scope question of whether the autostash behavior should be unified across rebase and merge operations or remain operation-specific. Lidong expresses hesitation about changing the current behavior where users might rely on `rebase.autoStash` affecting merge operations, suggesting this could be a backward compatibility consideration.

This email doesn't introduce new code changes but rather continues the design discussion about the configuration's behavior scope. The thread remains active with both high-level design questions (operation scope) and implementation details being discussed in parallel.