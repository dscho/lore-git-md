This is part 11 of Patrick Steinhardt's ongoing series to remove implicit `the_repository` usage in config handling code. The patch removes the `git_config_get_bool()` wrapper function, converting all its callers to use `repo_config_get_bool(the_repository, ...)` instead.

The change follows the established pattern of the series - making implicit global state explicit while maintaining identical behavior. The patch touches 21 files across various subsystems including archive, am, checkout, gc, grep, and transport, updating calls that check boolean config values like `uploadarchive.allowunreachable`, `am.threeway`, and `transfer.bundleuri`.

The conversion is mechanical and doesn't change functionality, though the commit message notes some call sites could be further optimized in future by using available repository handles rather than `the_repository`. This continues the incremental progress toward the larger goal of removing `the_repository` global from config handling.

The patch is well-contained with a clear commit message explaining the change, and follows the same uncontroversial pattern as previous conversions in the series. It removes 5 lines of wrapper code while maintaining all existing behavior.