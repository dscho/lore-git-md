Refactoring patch in the Rust dependency series, cleaning up unused xdiff internals. This is part 3/7 of a larger effort to prepare xdiff for Rust optimizations, following the structural reorganization in part 2.

The patch removes three unused fields from xdiff's core data structures: `xrecord_t.next`, `xdfile_t.hbits`, and `xdfile_t.rhash`. These were initialized but never actually used in the diffing algorithm. The changes are straightforward deletions with no behavior changes, simplifying the code by removing dead weight. The diff shows the net removal of 24 lines across `xprepare.c` and `xtypes.h`.

This continues the preparatory work for upcoming Rust optimizations by cleaning up the xdiff internals. The changes appear mechanical and low-risk, though they do touch core diff data structures. The patch maintains the series' focus on structural improvements before introducing Rust code in later parts.