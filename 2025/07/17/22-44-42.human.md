Windows-specific bug report with diagnostic update. Fabio Frumento follows up on the Git for Windows 2.50.x launch failure issue, reporting that `bash.exe` works when launched with `--norc` but fails with Git Bash's default `--login -i -v` arguments. This narrows the problem to something in the shell initialization process rather than the binary itself. The reporter has captured a screenshot showing the brief error flash before the terminal closes, though the text content isn't visible in the description. This moves the investigation forward by confirming the failure occurs during shell initialization, likely in `.bashrc` or related startup files. The maintainers now have two concrete leads: the screenshot (if it captured the error text) and the `--norc` workaround, which suggests the regression involves some initialization sequence that changed between 2.49.0 and 2.50.0. The report is helpful but still needs the actual error text from PowerShell as previously requested to pinpoint the exact failure.