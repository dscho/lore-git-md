This is a follow-up discussion about the `--extended` output format implementation in the `git last-modified` feature series. Junio C Hamano points out several issues with the current test implementation:

1. **Maintainability concerns**: The test hardcodes all output values including commit hashes, timestamps, and message content, making it fragile and difficult to update.

2. **SHA-256 compatibility**: The test will break when run with SHA-256 repositories (the default in future Git versions) since it assumes SHA-1 hashes.

3. **Inconsistent output format**: The test shows tree objects with full hashes but omits commit hashes, which appears inconsistent in the output format.

The discussion continues the thread's focus on finalizing the `--extended` format implementation, with Junio identifying concrete technical issues that need addressing. This follows previous concerns about using `pretty_print_commit()` for the extended format and the need for clearer documentation around format specifications. The test case in question demonstrates the current implementation's output format but reveals underlying problems that would affect long-term maintainability and compatibility.

Key technical details mentioned:
- The test shows the extended format output structure with path headers followed by commit metadata
- Current output includes tree hashes but not commit hashes
- All values are hardcoded rather than dynamically generated
- The format needs to work with both SHA-1 and SHA-256 repositories

This feedback suggests the test needs restructuring to be more robust, likely by:
- Using dynamically generated expected values rather than hardcoded ones
- Ensuring SHA-256 compatibility
- Making the output format more consistent
- Potentially revisiting the extended format design itself

The discussion continues to focus on finalizing this last major piece before the feature can be considered complete.