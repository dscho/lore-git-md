This is a discussion about output format design for the new `git repo` command hierarchy, specifically focusing on the `size` subcommand. Patrick Steinhardt responds to Justin Tobler's suggestion about supporting multiple output formats.

Patrick agrees with the need for multiple output formats but advocates for a more flexible `--format=<type>` approach rather than just a `-z` flag, though he's open to including `-z` as a shorthand for null-terminated output. His key concern is maintaining flexibility to evolve the default "user friendly" format for `git repo size` to potentially match git-sizer's rich output in the future.

The discussion reveals an important design tension: Patrick wants the default format to be human-friendly and evolvable (especially for `size`), while the thread has been converging on simple key-value pairs as the default. This suggests the output format discussion isn't fully settled despite previous consensus forming around key-value defaults.

The exchange highlights how subcommand-specific needs (like `size` vs general command principles are being balanced in this design process. Patrick's perspective as a project maintainer brings long-term maintainability considerations to what had been more implementation-focused discussions.