This is a discussion email in the ongoing thread about the new `git repo` command hierarchy, specifically about output format design. Justin Tobler responds to Patrick Steinhardt's position on format handling, showing alignment on key decisions while raising some implementation considerations.

The email marks significant convergence in the format discussion, with Justin now agreeing that `git repo` should serve as a unified command providing both plumbing-style and user-friendly output. He accepts Patrick's proposal for three distinct format types (`user`, `keyvalue`, and `nul`), with `user` as the default that can evolve over time while the other formats remain stable. 

Key technical points confirmed:
- `--format=<mode>` will be the primary interface with three options
- The `user` format is explicitly acknowledged as mutable default output
- `keyvalue` and `nul` formats will provide stable parsing interfaces
- The `size` subcommand will prioritize human-readable output to compete with git-sizer

Justin raises one open question about how the `user` format might differ from `keyvalue` for the `info` subcommand, suggesting this may not need immediate resolution. The email shows the output format design is now largely settled, with only minor implementation details remaining to be worked out. This moves the thread closer to finalizing the command's interface before focusing on remaining implementation polish.