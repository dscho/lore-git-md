This is part of the ongoing `git sparse-checkout clean` feature series, specifically addressing safety controls by adopting behavior from `git clean`. The patch adds `--force`/`--dry-run` requirements to prevent accidental deletions, mirroring `git clean`'s safety model.

The patch introduces three key safety mechanisms:
1. Requires `--force` or `--dry-run` by default (configurable via `clean.requireForce`)
2. Adds `--dry-run` support that shows what would be removed without deleting
3. Preserves the existing behavior of skipping directories with staged changes

The implementation is thorough, with comprehensive test coverage that verifies:
- The force/dry-run requirement enforcement
- Config option behavior (`clean.requireForce`)
- Interaction with staged changes (continues to skip those directories)
- New merge conflict scenarios (fails cleanly when index can't be converted to sparse)

This appears to be a well-considered addition to the sparse-checkout clean feature, addressing safety concerns that came up in earlier discussion. The patch maintains consistency with Git's existing safety patterns while preserving the command's specialized focus on sparse directories. The tests demonstrate careful consideration of edge cases, particularly around merge conflicts and staged changes.

As part of the larger series, this helps solidify the command's behavior before final documentation and integration. The changes are focused and clearly motivated by prior feedback about preventing accidental data loss.