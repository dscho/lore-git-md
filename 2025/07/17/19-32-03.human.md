This is a discussion about configuration behavior for `git pull`'s auto-stashing functionality. Ben Knoble weighs in on the ongoing thread about whether `pull` should respect `rebase.autostash` and `merge.autostash` settings.

The email argues that `git pull` should maintain consistency with its conceptual model as either "fetch+rebase" or "fetch+merge", and thus should respect the corresponding operation's autostash configuration. Knoble specifically supports honoring `rebase.autostash` for rebase-style pulls, and suggests `merge.autostash` should also be respected for consistency, though they remain neutral on the separate `pull.autostash` configuration.

This contributes to the thread's central debate about configuration precedence and backward compatibility, with Knoble taking a position that prioritizes conceptual consistency in Git's operations over strict backward compatibility concerns. The email doesn't propose new code but rather adds perspective to the design discussion, noting the tension between intuitive behavior and historical implementation choices.