Feature patch introducing `git refs list` as a wrapper for `git for-each-ref`, part of a GSoC project to consolidate ref-related commands. This v2 iteration addresses maintainer feedback by implementing the new command as a thin wrapper that forwards all processing to the existing `for-each-ref` implementation, avoiding code duplication.

The patch moves shared documentation into a new `refs-list-options.adoc` file included by both commands' man pages, ensuring documentation stays synchronized. The implementation adds minimal new code in `builtin/refs.c` that constructs and forwards arguments to `cmd_for_each_ref`. Existing tests for `for-each-ref` are reused via command substitution.

This appears to be a well-considered implementation that achieves the goal of command consolidation without introducing maintenance overhead. The approach preserves all existing functionality (including the `--include-root-refs` option) while establishing the `git refs` namespace for future ref-related commands. The mentoring team includes experienced Git contributors, and the patch follows established patterns for command wrappers.