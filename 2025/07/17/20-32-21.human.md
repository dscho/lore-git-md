Refactoring patch in the Rust integration series, focusing on xdiff type compatibility. This fourth patch in the 7-part series modifies the `xrecord_t` struct and related code to use Rust-compatible types (`u8`, `usize`, `u64`) instead of C-native types, requiring explicit casts in several places. The changes span across xdiff's core files (xdiffi.c, xemit.c, etc.) to handle pointer types consistently and update struct field definitions in xtypes.h.

The patch appears mechanically sound, adding necessary type casts where C pointers interact with the new Rust-style types. While this doesn't change functionality, it's a prerequisite for the planned Rust optimizations mentioned in earlier patches. The changes are widespread but focused - touching pointer handling in diff calculations while maintaining the existing algorithms.

This continues the series' pattern of incremental, compatibility-focused changes that pave the way for Rust integration. The explicit casts suggest some friction in the C/Rust boundary that future patches may need to address more elegantly. As with previous patches, the actual performance benefits remain to be demonstrated in later parts of the series.