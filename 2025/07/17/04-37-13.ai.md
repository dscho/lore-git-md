Junio Hamano raises an important technical consideration about the scope of the proposed `pull.autoStash` configuration. This is a follow-up questioning the design decision to limit the new option's effect to rebase operations only, suggesting it should also apply to merge operations.

The key point is whether `pull.autoStash` should trigger stashing behavior for both rebase and merge operations during `git pull`, rather than just rebase as currently implemented. Junio references a previous email where he discussed the interaction between pull, rebase, and merge commands regarding autostash behavior, though he doesn't restate those arguments here.

This email indicates the discussion is moving from implementation details (documentation wording, test coverage) back to fundamental design questions about the feature's scope. The maintainer is questioning whether the current rebase-only limitation is the right approach, which could require significant changes to the patch if addressed.

The reference to prior discussion suggests this isn't a new concern but rather a continuation of earlier architectural debate about how configuration should flow between related commands. This may reopen questions about the patch series had attempted to settle with its current rebase-focused implementation.