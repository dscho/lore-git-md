This is a follow-up discussion in the `git last-modified` series, specifically addressing the pretty-printing implementation for the `--extended` output format. The thread has been focused on finalizing the output format design, with Junio C Hamano raising concerns about the current approach of using `pretty_print_commit()` with indentation control.

Toon Claes responds to Junio's critique of a previous version that accidentally included a merge conflict marker in the commit message. More substantively, Junio points out that using `pretty_print_commit()` for the `-z --extended` format may be inappropriate since that function does more transformations than just indentation (like rewrapping and formatting commit messages). Toon acknowledges this concern and suggests they might need to bypass `pretty_print_commit()` entirely for this use case.

This exchange continues the discussion about how to implement the extended format's message output, with two approaches now on the table: either extend the pretty-printing controls further (as attempted in this patch with the `no_indent` flag) or avoid `pretty_print_commit()` completely for this output mode. The latter approach appears to be gaining favor as the more correct solution.

Key technical points:
- The patch attempted to add a `no_indent` flag to `struct pretty_print_context`
- The discussion reveals this may be the wrong approach for the `--extended` format's needs
- The extended format requires raw message output without any transformations
- Current implementation may need to bypass `pretty_print_commit()` entirely

This is part of the final design decisions needed before the `last-modified` feature can be finalized, with the output format being the last major unresolved question.