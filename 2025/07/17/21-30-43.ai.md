This is a follow-up discussion in the Rust integration series, focusing on build system details. brian m. carlson objects to including `Cargo.lock` in the repository, arguing it creates maintenance problems across Rust toolchain versions. They explain that since Git aims to support Debian's stable Rust version plus one year, version-locking dependencies via `Cargo.lock` could conflict with distros needing to backport security fixes to older dependency versions while other users want newer versions. The concern stems from Rust's rapid release cycle and the practical realities of distribution packaging.

The email references an earlier discussion ("as I mentioned downthread") about version support policy, reinforcing their position that the project should avoid pinning dependencies to maintain flexibility for both distro maintainers and users on newer toolchains. This continues the thread's theme of balancing performance gains with practical deployment considerations, now focusing on dependency management rather than the earlier CI configuration questions.

Key technical points: `Cargo.lock` would be excluded from version control, letting each build environment resolve dependencies appropriately for its Rust version. The concern applies specifically to the Rust interop and xdiff crates shown in the diff. This positions the discussion as part of the broader challenge of integrating a fast-moving ecosystem (Rust) with Git's stability requirements.