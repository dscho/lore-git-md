This is part 5 of the RFC series proposing Rust as a hard dependency with xdiff optimizations. The patch focuses on refactoring xdiff's line processing to separate parsing from hashing, a preparatory step for switching to the xxhash algorithm.

The change splits `xdl_prepare_ctx()` into two distinct operations: `xdl_parse_lines()` now handles the line-by-line parsing, while the original function focuses on hashing and classification. This separation makes the code more modular and easier to modify - particularly important since the next steps will replace the current DJB2a hashing with xxhash for performance gains.

The implementation appears careful, maintaining all existing functionality while restructuring the code. The diff shows 44 lines added and 31 removed, with most changes being straightforward movement of logic into the new parsing function. This continues the series' methodical approach of incremental preparation before introducing the Rust optimizations.

As with previous patches, this is groundwork rather than the performance-critical changes that would validate the Rust dependency. The commit message clearly explains the change's purpose (easier hash function swapping) but doesn't quantify the expected benefits yet. The series remains controversial due to the mandatory Rust dependency, though this particular patch is purely C-side refactoring.