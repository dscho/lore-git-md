This is a discussion about output format design for the new `git repo` command hierarchy, specifically focusing on the `size` subcommand. Patrick Steinhardt responds to Justin Tobler's suggestion about supporting multiple output formats, expanding on the rationale for a flexible format system.

The key points are:
1. Patrick agrees with supporting multiple output formats but prefers `--format=<type>` over just `-z` for future extensibility, though he's fine with `-z` as an alias for `--format=nul`.
2. He emphasizes the importance of keeping the default format user-friendly and mutable, particularly for `git repo size` which aims to compete with git-sizer's human-readable output.
3. The discussion reveals three distinct format types under consideration: human-friendly (default), key-value pairs, and NUL-terminated key-value pairs.
4. Patrick explicitly states he doesn't care about JSON output despite earlier mentions, focusing instead on maintaining flexibility for future format improvements.

This email advances the output format discussion by clarifying the architectural priorities (future flexibility and user-friendly defaults) while accommodating existing Git conventions (`-z` flag). The thread shows convergence on format mechanisms while leaving room for further refinement of the human-readable output design.