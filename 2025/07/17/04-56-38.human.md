This is a refactoring patch in Patrick Steinhardt's ongoing series to remove `the_repository` dependencies from object-file.c. 

Patch 12/16 cleans up an internal implementation detail by making `for_each_file_in_obj_subdir()` static, since it's only used within object-file.c. The function's declaration is removed from object-file.h while its definition is updated with the `static` keyword. This is a straightforward change that reduces the public API surface without affecting functionality.

The patch fits into the series' broader goal of making object-file.c's internals more self-contained and less exposed to the rest of Git. While minor, such cleanup helps prevent future misuse of internal functions and makes the codebase easier to reason about. The change is mechanical and unlikely to be controversial, though reviewers may want to confirm no external callers exist beyond what the author has checked.