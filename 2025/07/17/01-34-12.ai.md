This is a documentation patch that updates the sparse index expansion advice message to reference the new `git sparse-checkout clean` command. The change is part of the ongoing effort to improve sparse-checkout usability and performance.

The patch modifies the advice text in `sparse-index.c` to suggest using `git sparse-checkout clean` instead of the more generic `git clean` when users encounter sparse index expansion. This makes sense given the thread context showing that the new clean command is specifically designed to handle tracked-but-sparse directories that cause performance issues, while preserving important files like those with staged changes.

The change is straightforward and uncontroversial, simply updating documentation to reflect new functionality. It builds on the previous work in the thread where the clean command was developed with various safety controls (`--force`, `--dry-run`, `--verbose`) and integrated with the sparse index infrastructure. The patch maintains the existing advice structure while making it more specific to the sparse-checkout use case.

Key technical details:
- Updates the `SPARSE_INDEX_EXPAND_*` advice string
- Keeps existing guidance about checking sparse-checkout patterns
- Replaces generic `git clean` suggestion with `git sparse-checkout clean`
- Maintains same file location and context (sparse-index.c)
- Follows from the thread's discussion of clean command being the proper solution for sparse index optimization

This appears to be a simple but useful documentation improvement that helps users discover the new specialized tool for their sparse-checkout maintenance needs. The change is minimal (3 lines) and well-contained, with no apparent controversy or open questions remaining.