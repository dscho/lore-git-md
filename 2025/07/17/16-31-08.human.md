This is a follow-up discussion in the `git last-modified` series about the pretty-printing implementation details. The patch in question (v5 4/6) had added a `no_indent` flag to suppress indentation in commit message output, but Junio C Hamano points out this may be the wrong approach for the `--extended` format's needs.

The core issue is that `pretty_print_commit()` does more transformations than just indentation (like rewrapping and formatting), which may not be desirable for the machine-readable `--extended` output format. Toon Claes acknowledges this concern and suggests they may need to bypass `pretty_print_commit()` entirely rather than trying to modify its behavior.

This exchange shows the series is in its final polishing stages, with careful attention being paid to output format correctness. The discussion has moved from core functionality to these subtle interface design questions, indicating the technical implementation is otherwise solid. The thread continues to involve multiple experienced contributors reviewing the design decisions.