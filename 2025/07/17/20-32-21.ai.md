This is part 4/7 of the RFC series introducing Rust as a hard dependency with xdiff optimizations. The patch focuses on making xdiff's `xrecord_t` structure more Rust-friendly by adjusting type definitions and adding necessary casts.

The changes modify `xrecord_t` in `xtypes.h` to use Rust-compatible types (`u8 const*` for pointers, `usize` for size, `u64` for hash values) and propagate these changes throughout xdiff's implementation files. The patch adds explicit casts in various xdiff functions where pointer types need conversion between C and Rust representations, particularly around string operations in `xdiffi.c`, `xemit.c`, `xmerge.c`, and other components.

The modifications maintain existing functionality while preparing for Rust integration, continuing the series' pattern of incremental changes that don't alter behavior. The casts introduced are a temporary measure until the Rust implementation can handle these types natively. This patch follows logically from part 3's dead code removal, further simplifying xdiff's internals for the planned Rust optimizations.

Key technical details for future reference:
- `xrecord_t` now uses Rust types in its definition
- Pointer casts added in 7 files (21 changes total)
- String operations now explicitly cast between `char*` and `u8 const*`
- Changes touch core diff algorithms but don't modify logic
- Builds on part 1's Rust type definitions in git-compat-util.h

The patch appears mechanically sound but doesn't yet demonstrate the performance benefits that would justify the Rust dependency - those are presumably coming in later parts of the series. The changes are consistent with the series' incremental approach to preparing xdiff for Rust integration.