This is a feature proposal for consolidating ref-related commands under `git refs`, specifically introducing `git refs list` as a wrapper for `git for-each-ref`. The v2 series responds to maintainer feedback by significantly simplifying the implementation - rather than duplicating logic, it now directly calls `for-each-ref` internally while maintaining identical behavior.

The key changes in v2 are architectural: the command is now a thin wrapper to avoid code duplication, and documentation has been refactored to share option descriptions between both commands via a new `refs-list-options.adoc` file. The test suite runs existing `for-each-ref` tests through the new command to verify compatibility.

The proposal remains part of a larger vision to group scattered ref commands (`show-ref`, `update-ref`, etc.) under the `git refs` umbrella for better discoverability and future evolution. While this initial step is conservative (just adding the wrapper), the cover letter invites discussion about potentially streamlining options in future iterations.

The implementation looks clean and well-considered, with thorough documentation and testing. The maintainer's preference for wrapper-style implementation has been fully addressed. Performance considerations raised in the thread (particularly around default format overhead) remain relevant but aren't tackled in this compatibility-focused version.