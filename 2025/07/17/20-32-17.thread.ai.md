RFC series introducing Rust infrastructure for xdiff (now v3 12/15) continues systematic preparation with cleanup of redundant field after memory management refactoring. The series maintains its incremental approach while progressing toward core functionality changes, now removing the nrec field after replacing chastore with ivec in v3 11/15.

**Technical implementation**:
- Standardizes xdiff core types for Rust compatibility (from v3 9/15):
  - `xrecord_t` with Rust-aligned primitives (`u8 const*`, `usize`, `u64`)
  - Aligned `xdlclass_t::ha` with `xrecord_t::ha`
- Completed transition from chastore to ivec (v3 11/15 + v3 12/15):
  - Removes dual data structure (chastore + recs array) in xdfile_t
  - Eliminates redundant `nrec` field (now using `ivec.length`)
  - Updates all array bounds checks and loops across xdiff:
    - xdiffi.c (core diff algorithm)
    - xemit.c (diff output generation)
    - xmerge.c (merge conflict handling)
    - xpatience.c (patience diff variant)
    - xprepare.c (initialization/cleanup)
  - Maintains existing type casts (long-based array indexing)
- Preserves all prior infrastructure:
  - `ivec` FFI-compatible vector type
  - Type-safe C wrappers via `DEFINE_IVEC_TYPE`
  - Dual memory management (Git allocator/Rust allocator)
- Build system (`INTEROP_OBJS`, `INTEROP_LIB`) and CI unchanged:
  - GitHub Actions with `Cargo.lock` tracking
  - Windows/MSVC, Windows/Meson, Linux matrices
  - Static analysis coverage

**Platform support**:
- All existing platform handling maintained:
  - Windows/MSVC target compatibility
  - Cygwin exclusion (Ramsay Jones tracking)
  - musl compatibility (Rust 1.72.0+)
  - 32-bit Linux target triple
- No new platform requirements from changes

**Policy dimensions**:
- Rust requirement and version constraints unchanged
- Documentation updates from earlier patches still apply
- **Open items**:
  - All architectural items remain (future refactoring)
  - Windows/MSVC target handling ongoing
  - Linux linker flags pending
  - musl CI validation needed
  - Long-term `userenv` solution for Windows SDK
  - Future `Cargo.lock` management
  - `ivec` integration testing
  - Additional xdiff refactoring opportunities
  - Verification of prior dead code removal completeness
  - Evaluation of type safety impact from explicit casts (v3 9/15)
  - New: Potential follow-up cleanups in other xdiff files using similar patterns
  - Closed: Removal of temporary recs array (completed in v3 12/15)

**Participant positions**:
- Ezekiel Newren continues driving implementation with memory management refactoring
- Johannes Schindelen previously contributed alignment suggestion for hash fields
- Ramsay Jones remains engaged on Cygwin considerations
- Maintainer alignment on preparatory approach continues

Series progresses methodically, now completing the ivec transition by removing redundant bookkeeping while maintaining all existing behavior and platform support. The patch demonstrates continued attention to code organization as the infrastructure work moves toward eventual Rust component integration, with v3 12/15 building cleanly on prior type standardization and memory management unification work.