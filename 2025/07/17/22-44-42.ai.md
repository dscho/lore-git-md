This is a diagnostic follow-up in the Git for Windows regression investigation. Fabio Frumento reports that the bash shell itself works when launched with `--norc`, but fails when started with Git Bash's default `--login -i -v` options. This narrows the problem to something in the shell initialization process rather than a complete failure to launch. The reporter has captured a screenshot showing the brief window appearance before closure, though the text content isn't described. This suggests the issue may involve rc file processing or login shell initialization rather than a fundamental incompatibility. The maintainer now has two concrete data points to investigate: the working `--norc` case versus the failing login shell case, plus visual evidence of the crash timing. Next steps would likely involve analyzing the screenshot for error messages and comparing initialization paths between the working and broken configurations. The thread remains focused on diagnosis, with this email providing useful incremental information about the failure mode's boundary conditions.