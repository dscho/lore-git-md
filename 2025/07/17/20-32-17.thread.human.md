This RFC thread on introducing Rust infrastructure for Git's xdiff` remains at an impasse, with all technical paths for Rust adoption now exhausted. The discussion has centered on a custom `ivec` type designed for C/Rust interoperability, with Ben Knoble initially questioning whether Rust's native `Vec` could be used instead via the `Allocator` trait. Ezekiel Newren provided detailed technical explanations about why `Vec`'s memory layout cannot be stabilized for FFI use across Rust versions, justifying the need for the dual C/Rust `ivec` implementation.

In the latest exchange, Knoble acknowledges Newren's explanations and confirms understanding of the design constraints, effectively resolving the remaining technical question about `ivec`. This leaves the implementation technically sound but still blocked by policy concerns - particularly around platform support, as confirmed compiler limitations (mrustc being bootstrap-only, gccrs too immature) eliminate alternatives for platforms like NonStop that cannot use standard Rust toolchains. Junio Hamano has been reviewing the build system changes while maintaining a neutral stance on the larger policy questions, though he did settle a minor style question by confirming the project's preference for quote-style header includes.

The thread now awaits either a policy resolution addressing platform exclusion concerns or a decision to abandon the Rust xdiff effort. The technical work appears complete, with all substantive review feedback addressed, leaving the debate squarely in the realm of project governance rather than implementation.