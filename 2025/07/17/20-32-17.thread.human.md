The thread continues to discuss the technical implementation of Rust infrastructure for xdiff, now in its third version. The core issue remains the policy debate around platform support, with all technical alternatives for Rust compilation on NonStop and other platforms now exhausted after Haelwenn Monnier confirmed mrustc is only for bootstrapping and gccrs is too immature.

On the technical side, the `ivec` type design—a custom vector implementation that works across Rust and C—has been thoroughly vetted. Ezekiel Newren provided detailed explanations about why Rust's native `Vec` can't be used for FFI due to version instability and layout concerns. Ben Knoble initially questioned this but now fully accepts the design. Brian Carlson's latest email reinforces this consensus, sharing practical experience that custom types like `ivec` are the right choice when C code needs direct access, while boxed native Rust types work better for Rust-only components.

Junio Hamano has weighed in on style questions, confirming the project's preference for quote-style (`""`) header includes over angle brackets (`<>`) based on existing convention. Patrick Steinhardt has offered MSYS2-based CI patches for Cygwin/MinGW testing, though this is peripheral to the core platform support issues.

The thread remains blocked on policy questions about whether the benefits justify excluding platforms without Rust support, with Randall Becker's position strengthened by the compiler confirmations. Technical implementation questions about the `ivec` design and build system are now resolved, leaving the platform support debate as the primary open issue.