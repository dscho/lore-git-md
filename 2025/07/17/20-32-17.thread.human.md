Here's a human-readable summary of the thread's current state:

The RFC proposing Rust as a hard dependency with xdiff optimizations (claiming 5-19% performance gains) has evolved through seven versions, now including CI integration. The series refactors xdiff internals and introduces Rust build infrastructure. Performance analysis shows most gains come from hash algorithm choice (XXH3) rather than Rust itself, with comparable speedups achievable in C.

Platform support remains a key sticking point. The discussion has surfaced concrete constraints for Windows, Gentoo, and non-LLVM architectures, with GCC-based Rust compilation emerging as a potential long-term solution (though requiring 1+ year for maturity). Gentoo maintainers emphasize practical bootstrapping challenges rather than ideological opposition.

The latest exchange focuses on `Cargo.lock` file management - a tension between Rust's reproducibility norms and Git's security maintenance realities. After Brian Carlson's warnings about vulnerability report noise from Git LFS experience, Ezekiel Newren proposes a compromise: ignore `Cargo.lock` in the main repository but preserve it in CI builds, with documentation guiding issue reporters to include their local lockfiles. This attempts to balance auditability needs with practical security considerations.

Open questions span technical (Windows targets, type consistency), ecosystem (licensing, dependency management), and policy dimensions (version support, platform compatibility). The discussion remains active but increasingly focused on concrete implementation tradeoffs rather than fundamental objections.