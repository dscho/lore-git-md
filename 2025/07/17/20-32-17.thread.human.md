This RFC thread on introducing Rust infrastructure for xdiff remains stuck on fundamental questions of platform support and policy, though the technical implementation has reached a mature state. The series, now at v3, has exhausted all technical paths for Rust compiler alternatives after Haelwenn Monnier confirmed mrustc is bootstrap-only and gccrs too immature. The core `ivec` interoperability type has been finalized with both C and Rust implementations to avoid Rust versioning issues, though Ben Knoble continues probing whether Rust's native `Vec` with `Allocator` could serve instead.

The latest exchange focuses on a minor style question - whether to use quotes or angle brackets for including `git-compat-util.h` in the `ivec` code. Junio Hamano acknowledges that while angle brackets might be theoretically cleaner given the `-I.` include path, the project's overwhelming convention (361 of 362 instances) uses quotes, making consistency more important than theoretical purity here. This stylistic discussion occurs against the backdrop of larger unresolved issues: NonStop platform's lack of Rust support, whether SHA-256 transition precedents apply given different platform impacts, and whether the build system can accommodate NonStop's C11/C17 constraints. While Patrick Steinhardt has offered MSYS2 CI patches and Ezekiel Newren continues polishing technical details, the fundamental policy deadlock remains unchanged.