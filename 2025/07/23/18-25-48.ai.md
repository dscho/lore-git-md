This is a refactoring patch from Justin Tobler in the completed reflog migration series. The email provides a minor correction to Patrick Steinhardt's original patch that exported `ref_transaction_update_reflog()` from refs.c to refs.h. 

The key change in the original patch was making this previously static function available to other callers while simplifying its interface by removing the `flags` parameter, since all callers were expected to use the same set of flags (REF_HAVE_OLD|REF_HAVE_NEW|REF_LOG_ONLY|REF_FORCE_CREATE_REFLOG|REF_NO_DEREF). Justin points out a typo fix ("functino" -> "function") and confirms the rationale for removing the flags parameter makes sense given there's currently only one caller (in the reflog migration code).

The patch touches refs.c and refs.h, moving the function declaration and updating its documentation. This was part of the infrastructure work needed for the larger reflog migration effort between storage formats. The change is straightforward and uncontroversial, with Justin's reply indicating agreement with the approach. Since this is part of a completed series that has already incorporated all feedback, no further action is needed on this particular patch.