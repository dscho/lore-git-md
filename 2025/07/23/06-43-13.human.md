Test infrastructure patch for the new `git refs list` command. This is the final patch in a series introducing a thin wrapper around `git for-each-ref` as part of a GSoC project.

The patch adds a minimal test driver (`t1461-refs-list.sh`) that reuses the comprehensive test suite from `git for-each-ref` by setting a shell variable to invoke the new command instead. This ensures `git refs list` behaves identically to its underlying implementation. The approach follows maintainer feedback to avoid test duplication and environment variable concerns from earlier versions.

The implementation is straightforward - just 8 lines of shell script that source the shared test library after configuring which command to test. The patch also updates the meson build file to include the new test. This completes the test infrastructure restructuring that was a key focus of the v3 redesign.

With all prior technical concerns addressed through mentoring and maintainer feedback, this appears ready for final review. The shared test approach provides thorough coverage while maintaining clean separation between the commands' implementations.