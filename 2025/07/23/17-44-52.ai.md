This is a technical discussion about string handling in the `git repo info` command implementation, part of the ongoing GSoC project. The email is a response to Phillip Wood's concern about the current `get_value_fn` callback returning `const char*`, which prevents dynamic string allocation.

Lucas Seiki Oshiro acknowledges the limitation, noting it hasn't caused problems yet but will become an issue when implementing future `path.*` fields. He initially kept the simpler `const char*` approach for v5 but now agrees with Phillip Wood's suggestion to pass a `struct strbuf` to callbacks instead, planning to make this architectural change in v6.

The exchange highlights a key implementation decision point in the field system architecture, where the current design (callback-per-key with `const char*` returns) needs to evolve to support more complex field types. This connects to the thread's open question #1 about string handling alternatives, with Lucas now leaning toward the `struct strbuf` approach over function pointer emission.

Technical context for future reference:
- Current limitation: `get_value_fn` typedef forces static strings
- Proposed solution: Pass `struct strbuf` to callbacks for dynamic content
- Impacted future work: `path.*` field implementation
- Related to thread discussions about field system architecture and output formats
- Maintains alignment with prior decisions about LF-separated output format