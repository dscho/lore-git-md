Thread identity: Bug report about unexpected warning "ignoring unknown core.fsync component 'true'" during clone/fetch operations, reported across Git versions 2.25.1 and 2.34.1.

Core problem: Remote repository has malformed `core.fsync` configuration where 'true' is incorrectly used as a component name, triggering parser warnings during clone/fetch. The warning occurs when remote has `core.fsync=true` (boolean syntax) instead of proper component-based syntax (e.g., `core.fsync=loose-object`). Patrick Steinhardt confirms this is purely a server-side configuration issue.

Current status: Issue diagnosed - remote admin needs to correct configuration from `core.fsync=true` to proper syntax like `core.fsync=all`. No client-side changes required since malformed config isn't transferred locally. The warning serves its intended function of reporting invalid configuration.

Key technical details: `core.fsync` expects component names like 'loose-object' or 'pack'. The string 'true' is incorrectly interpreted as component name during remote operations. Warning appears with "remote:" prefix confirming server-side origin. Correct syntax for enabling fsync globally is `core.fsync=all`.

Open questions: None remaining - issue fully diagnosed with clear resolution path (server config correction). No evidence this affects operations beyond clone/fetch.

Participant positions: Original reporter described behavior. Patrick Steinhardt provided authoritative diagnosis confirming server-side origin and correct syntax, moving thread to resolution. No further action needed unless reporter cannot get server config updated.

Related work: This appears to be an isolated configuration error rather than part of any broader fsync-related work. The warning mechanism is working as designed to surface invalid configurations.