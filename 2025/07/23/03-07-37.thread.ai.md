Thread identity: Bug report about unexpected warning "ignoring unknown core.fsync component 'true'" during clone/fetch operations, reported across Git versions 2.25.1 and 2.34.1.

Core problem: Remote repository appears to have malformed `core.fsync` configuration where 'true' is incorrectly used as a component name, triggering parser warnings during clone/fetch. The warning suggests someone set `core.fsync=true` (boolean syntax) instead of proper component-based syntax (e.g., `core.fsync=loose-object`).

Current status: Issue reported but not yet diagnosed or fixed. Behavior consistent across multiple Git versions, indicating configuration syntax misunderstanding rather than version-specific bug.

Key technical details: `core.fsync` expects component names like 'loose-object' or 'pack' to specify which repository components should be fsync'd. The string 'true' is being incorrectly interpreted as a component name rather than boolean value. Warning appears during remote operations but doesn't persist locally since invalid config isn't transferred.

Open questions: Exact remote repository configuration causing this hasn't been verified. Whether this affects other operations beyond clone/fetch is unclear. No proposed fix yet for handling such malformed configurations more gracefully.

Participant positions: Single reporter so far describing the behavior, no maintainer or other contributors have responded yet.