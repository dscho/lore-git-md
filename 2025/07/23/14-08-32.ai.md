This is part 11 of Patrick Steinhardt's ongoing series to remove `the_repository` global by converting config-related wrapper functions to explicit variants. The patch removes the `git_config_get_bool()` wrapper in favor of direct `repo_config_get_bool(the_repository, ...)` calls, following the established pattern from previous conversions in the series.

The change affects 21 files across various subsystems, updating boolean config lookups for settings like `uploadarchive.allowunreachable`, `am.threeway`, `gc.autodetach`, `http.getanyfile`, and many more. The conversion is mechanical - each call site simply replaces the wrapper with the explicit repository variant while still passing `the_repository`, maintaining identical behavior. The patch notes this is an intermediate step; future work should optimize by passing actual repository instances where available rather than always using the global.

The changes are consistent with prior patches in the series (referencing commit 036876a1067) and follow the same disciplined approach of making repository dependencies explicit without yet optimizing them. The patch removes the wrapper declaration from config.h while keeping the `git_config_get_bool_or_int()` variant for now. This continues the systematic elimination of config wrappers as part of the broader `the_repository` removal effort.