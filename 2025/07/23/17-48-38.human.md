This is a design discussion in the ongoing `git repo info` implementation thread. Junio C Hamano follows up on Lucas Seiki Oshiro's response about LF handling in the output format, confirming that the current approach of restricting field names (keys) to not contain LFs is reasonable. 

The exchange resolves one of the open questions from the thread about LF separator robustness in the `--format=null` output. Junio notes this aligns with similar restrictions in `git config`, where LF and NUL are forbidden in configuration variable names. This validation from the Git maintainer suggests the design decision is sound and unlikely to face further challenges.

The discussion remains focused on technical implementation rather than introducing new concerns, indicating steady progress on this GSoC project. With this clarification, the planned move to `struct strbuf` in v6 appears to be the remaining major implementation hurdle before this feature can mature.