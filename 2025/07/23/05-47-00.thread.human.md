Here's the updated human-readable thread summary:

The thread began with Lidong Yan reporting unexpected behavior where `git diff --ignore-matching-lines` had no effect when combined with `--name-only` in Git 2.49.0. Junio Hamano explained this was by design - metadata-focused outputs like `--name-only` deliberately avoid examining file contents for performance reasons. Arne and Jeff King (peff) countered with UX arguments, showing how this creates confusing inconsistencies where files appear in `--name-only` output but not in full diffs when changes.

The discussion reached an impasse between performance considerations and behavioral consistency, with Junio emphasizing backward compatibility concerns, particularly for machine-readable formats like `--raw`. Now, a new patch has arrived that resolves the tension. It introduces a `diffcore_ignore()` function to handle ignore-regex filtering at a lower level, making the behavior consistent across `--name-only`, `--name-status`, and `--stat` outputs while preserving the current optimized behavior for `--raw` and `--check` formats.

The implementation is thorough, with careful handling of edge cases (binary files, textconv) and comprehensive test coverage. This appears to be the solution the thread was waiting for - it implements the consistency improvements Arne and peff advocated while respecting Junio's technical constraints. The patch directly addresses the original bug report while maintaining backward compatibility where needed. If accepted, this would resolve the core issue without breaking existing workflows that depend on the current `--raw` behavior.