Here's the updated human-readable summary:

This thread examines an unexpected interaction between `git diff`'s `--ignore-matching-lines` and `--name-only` options in Git 2.49.0. Lidong Yan originally reported that content-ignoring options have no effect when combined with metadata outputs like `--name-only`. Junio Hamano explained this is by design - metadata outputs avoid examining file contents for performance reasons, making content filters irrelevant for these modes.

The discussion has evolved into a debate between technical design principles and user experience. Arne and now Jeff King argue the current behavior violates the principle of least surprise, as `--name-only` can list files that wouldn't appear in the actual diff output when changes are ignored. Jeff provided a concrete example showing this inconsistency with whitespace changes.

While Junio maintains the performance rationale for separating content examination from metadata generation, the thread now acknowledges valid UX concerns. Eric Sunshine contributed documentation suggestions, including using `git log --format=reference` for citations. The core tension remains between:
1) Preserving the performance optimization of avoiding content examination
2) Making behavior more consistent across diff output formats
3) Potential backward compatibility risks of changing the behavior

No implementation changes have been proposed yet, but the discussion has shifted toward whether better documentation could mitigate the surprise factor, or if the behavior should be modified to prioritize consistent output. The thread remains open with active participation from both technical and UX perspectives.