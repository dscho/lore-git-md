Here is the dense AI thread summary:

Bug report regarding unexpected interaction between `git diff --ignore-matching-lines` and `--name-only` options in Git 2.49.0. When both flags are used together, `--ignore-matching-lines` has no effect - all changed files are listed regardless of the ignore pattern. Issue is reproducible with `--no-ext-diff`, indicating it is not related to external diff tools. A separate but possibly related issue notes that the external diff helper difftastic ignores `--ignore-matching-lines` entirely, though it is unclear whether this is a difftastic bug or an issue with Git's external diff helper interface. Core command affected is `git diff` with these two options combined. The interaction appears undocumented, suggesting an implementation oversight. No proposed fix included yet, but the report clearly documents the unexpected behavior. The issue touches both internal diff machinery and the external diff helper interface.