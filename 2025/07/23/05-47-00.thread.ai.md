Here is the updated dense AI thread summary:

Bug report and analysis of unexpected interaction between `git diff --ignore-matching-lines` and `--name-only` in Git 2.49.0. When combined, `--ignore-matching-lines` has no effect - all changes are listed regardless of pattern. Lidong Yan confirms this occurs even with `--no-ext-diff`, ruling out external diff tools like difftastic as the cause. Technical analysis traces the issue to `diffcore_std()` not applying ignore_regex filtering to file pairs, with filtering only occurring in `builtin_diff()`. This explains the failure in `--name-only` mode which bypasses builtin rendering. The behavior appears undocumented and likely an implementation oversight in the diff filtering pipeline. While no fix is proposed yet, the analysis clearly identifies where in the diff machinery (diffcore_std() vs builtin_diff()) the filtering stages diverge. The issue affects both internal diff processing and potentially the external diff helper interface, though the current focus is on core diff behavior. The thread now contains both the original bug report and technical analysis of the underlying cause, providing clear direction for anyone investigating a solution.