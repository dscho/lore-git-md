This is part of Patrick Steinhardt's ongoing series to remove `the_repository` global variable usage, specifically targeting config-related functions. Patch 3/21 mechanically converts the `git_config_get()` wrapper to direct `repo_config_get(the_repository, ...)` calls.

The change follows the pattern established in commit 036876a1067, removing another config wrapper function that hid repository dependencies. The patch updates 4 call sites across `builtin/gc.c`, `builtin/submodule--helper.c`, and `t/helper/test-config.c`, plus removes the wrapper declaration from `config.h`. The conversion maintains identical behavior since it's just making the repository dependency explicit at each call site.

Key technical details for future reference:
- Removes `git_config_get()` wrapper in favor of direct `repo_config_get(the_repository, ...)` calls
- Updates maintenance, submodule, and test code to use new form
- Keeps all existing functionality while making repository dependencies visible
- Part of systematic removal of config wrappers hiding `the_repository` usage

The change is purely mechanical and low-risk, following the established pattern from earlier in the series. It continues the effort to make repository dependencies explicit in the codebase while maintaining current behavior.