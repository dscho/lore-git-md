Documentation polish for the `git refs list` wrapper command. This is a follow-up to Eric Sunshine's review of the shared options documentation in the RFC patch series introducing `git refs list` as a thin wrapper around `git for-each-ref`.

Meet Soni acknowledges Eric Sunshine's suggestion to remove the explicit "Shared options for..." comment from the documentation file, agreeing that such comments can become stale and that `grep` is a better way to track which commands share the documentation. The change is straightforward and will be included in the next version of the patch series.

This is a minor documentation cleanup in the context of the larger effort to introduce `git refs list` while maintaining consistency with `git for-each-ref`. The exchange shows the patch author responding to maintainer feedback, though the more substantive discussion about test infrastructure design raised by Junio Hamano remains to be addressed in future revisions.