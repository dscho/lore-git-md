This is a test refactoring patch in the `git refs list` series. It moves the test logic from `t6300-for-each-ref.sh` into a new shared file `for-each-ref-tests.sh` to prepare for testing the new `git refs list` command. The key changes are:

- Creates new `for-each-ref-tests.sh` with 2141 lines of test logic
- Reduces `t6300-for-each-ref.sh` to a minimal driver that sources the shared tests
- Replaces hardcoded `git for-each-ref` calls with `$git_for_each_ref` variable
- Follows the pattern used for sharing tests between `git-blame` and `git-annotate`

The patch is part of the v3 redesign of the `git refs list` command that addresses prior maintainer concerns. It restructures the test infrastructure while preserving test coverage, using shell variable substitution and shared files rather than environment variables as suggested in earlier reviews.

Key technical elements:
- New test driver pattern with `$git_for_each_ref` variable
- Shared test file `for-each-ref-tests.sh` contains all test cases
- Original test file becomes minimal wrapper that sets up the variable
- Maintains all existing test coverage while enabling reuse

This change enables the subsequent patch to add tests for `git refs list` by sourcing the same shared test file. The approach has been reviewed and approved by mentors and maintainers as part of the overall series.