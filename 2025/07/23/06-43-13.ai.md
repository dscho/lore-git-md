Test infrastructure patch for the `git refs list` command, completing the v3 series. This adds `t1461-refs-list.sh` as a minimal test driver that sources the shared test library from the previous patch (`for-each-ref-tests.sh`), overriding the command variable to test `git refs list` instead of `git for-each-ref`. The approach follows the maintainer-approved pattern of shared test infrastructure, addressing earlier concerns about test organization.

Key technical elements:
- New test file simply sets `git_for_each_ref='git refs list'` before sourcing the shared test library
- Added to meson.build for test suite inclusion
- Verifies identical behavior between `refs list` and `for-each-ref` by running identical test cases
- Follows the git-blame/annotate shared test pattern established earlier in the series

This patch represents the final piece of the test restructuring work, providing comprehensive coverage for the new command while maintaining DRY principles. The shared test approach means any future test additions or modifications to `for-each-ref` will automatically apply to `refs list` as well, ensuring long-term compatibility between the commands.