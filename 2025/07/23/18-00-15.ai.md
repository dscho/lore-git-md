This is a follow-up in the `git imap-send` message marking thread, addressing the final implementation details. Aditya Garg seeks clarification on whether to maintain the current backend inconsistency (OpenSSL marking messages unread vs curl marking them read) or align OpenSSL behavior with curl's read-by-default approach. The question arises after Junio C Hamano rejected integrating this with `send-email` due to curl version constraints.

Key technical points in this email:
- OpenSSL backend currently doesn't mark messages as read by default behavior
- The inconsistency stems from OpenSSL not requiring new APIs to implement read marking
- The decision point is whether to standardize behavior across backends now that broader integration plans are off the table

The thread has effectively concluded the design phase, with this being one of the last implementation details to resolve. The email shows the author working through the practical implications of earlier decisions to keep the change minimal and avoid curl version complications. No new files or config options are introduced here - this is purely about finalizing the behavior in `builtin/imap-send.c`.