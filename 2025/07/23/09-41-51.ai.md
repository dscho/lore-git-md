This is a test fixup for the interactive patch commands diff context configuration series. Phillip Wood provides a patch to restore accidentally removed test coverage and eliminate redundant tests in the final patch of the series.

The changes:
1. Restores three key config-based behavior tests in t3701 that were accidentally dropped in v4:
   - Verifying `add -p` respects `diff.context`
   - Verifying `add -p` respects `diff.interHunkContext` 
   - Verifying `add -p` rejects negative `diff.context` values
2. Removes 6 redundant tests from t4055 that duplicated coverage of command-line override behavior already tested in t3071
3. Maintains all other test modernization (45 grep/config conversions) from the series

The fix addresses the final outstanding issue identified in review - ensuring comprehensive test coverage while avoiding duplication. This aligns with Junio's merge requirements to restore the config tests and remove redundant -U tests before merging to 'next'. The patch is minimal and focused, directly addressing the test gaps without introducing new changes to the core implementation.

This represents the last piece needed to finalize the series, which has already resolved all major design questions around the OPT_DIFF_* macros, validation behavior, and documentation approach. The implementation provides consistent diff context configuration support across all interactive patch commands through both config and command-line overrides.