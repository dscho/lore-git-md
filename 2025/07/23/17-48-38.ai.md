This is a discussion thread continuation about the `git repo info` command implementation, specifically addressing output format parsing concerns. Junio C Hamano responds to Lucas Seiki Oshiro's explanation about LF handling in the `--format=null` output.

The exchange confirms the design decision to use LF as a field separator in the machine-readable output format, with the first LF marking the boundary between key and value. This matches `git config -z`'s approach of restricting LF in keys while allowing it in values. Junio notes this is a reasonable choice, effectively resolving one of the open questions from the thread about LF separator robustness.

The discussion also implicitly validates the callback architecture's current string handling limitations (using `const char*` returns) while acknowledging the planned v6 migration to `struct strbuf` for more complex field types. This maintains forward progress on the GSoC project while addressing reviewer concerns about future extensibility.

Key technical points confirmed:
- LF separator design matches existing Git conventions
- Key names will be restricted from containing LFs (like git config subsections)
- Values may contain LFs (parsing stops at first LF after key)
- The solution is considered robust enough for the current field types

This email marks resolution of one architectural question while the broader implementation continues toward v6 with string handling improvements planned.