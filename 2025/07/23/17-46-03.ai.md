This is a response in the `git imap-send` message marking thread, addressing a CI compatibility issue with the proposed patch. Junio Hamano notes that the GitHub Actions workflows are failing because the patch's use of `CURLOPT_UPLOAD_FLAGS` requires libcurl 8.13.0 (recently released), while CI uses older versions. He suggests shelving the patch until libcurl requirements can be raised in the future, explicitly rejecting the addition of version-checking conditional compilation as a solution. This effectively puts the feature on hold, maintaining the status quo where the OpenSSL and curl backends continue to behave differently regarding message marking. The thread's main unresolved question - how to handle the libcurl version dependency - appears settled for now with a decision to wait rather than implement workarounds.