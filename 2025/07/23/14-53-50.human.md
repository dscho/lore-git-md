This is a design discussion about the field system implementation in the `git repo info` command series. Phillip Wood responds to Justin Tobler's concerns about the callback-per-key architecture by pointing out an additional limitation: the current `get_value_fn` returns a `const char*`, preventing allocation of strings. He suggests two alternatives: passing a `struct strbuf` to callbacks for building values incrementally, or having callbacks invoke a function pointer to produce output directly.

The discussion highlights ongoing architectural questions in this GSoC project, particularly around how the field system will handle more complex cases like batch operations or allocated strings. Phillip's feedback builds on Justin's earlier concerns about scalability while focusing on a concrete implementation constraint. The thread shows active mentoring engagement, with multiple contributors helping shape the design before it solidifies.