This is a refactoring patch that explores using C99 compound literals to simplify initialization patterns in the Git codebase. Phillip Wood proposes using this feature to make struct initialization more readable and concise, demonstrating how it could clean up common patterns like `strbuf_init()` and complex function calls with struct parameters.

The patch serves as a "test balloon" - a minimal change to `strbuf_init()` that replaces a `memcpy` from a blank struct with direct assignment from a compound literal. This tests compiler support while demonstrating the technique's benefits: reduced boilerplate and more readable initialization at call sites.

The commit message provides clear examples showing how compound literals could improve code in `builtin/rebase.c` by allowing struct initialization directly in function calls, making the relationship between values and their use more obvious. It also shows how they could simplify common initialization functions throughout the codebase.

The change itself is small and low-risk, modifying only `strbuf.c` to use the new pattern. As a test balloon, it seems well-positioned to start a discussion about wider adoption of this C99 feature if compiler support proves reliable. The patch appears carefully considered with a thoughtful commit message explaining both the immediate change and its broader implications.