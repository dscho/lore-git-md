Refactoring patch to formalize `bool` type usage in Git's codebase. This is the first of a 3-part series standardizing predicate functions to return C99 `bool` instead of `int`, following an 18-month experimental period that began with commit 8277dbe987 in December 2023.

The patch updates Documentation/CodingGuidelines to explicitly permit using `stdbool.h`'s `bool` type, alongside other already-allowed C99 features like `for` loop variable declarations. The change is presented as a natural progression after the long experimental period showed no issues in production use.

Technical scope is minimal - just 3 lines added to CodingGuidelines documenting the policy change. The patch references the original test commit and positions this as formalizing existing practice rather than introducing new behavior. As the first in the series, it establishes the foundation for subsequent mechanical conversions of string utility and strbuf functions to use `bool` returns.

This appears to be a straightforward documentation update with low risk, building on established precedent. The 18-month incident-free trial period provides strong evidence for the change's stability. The patch follows Git's incremental adoption pattern for C99 features, similar to how loop variable declarations were previously standardized.