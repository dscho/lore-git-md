This is a review response in the SHA-256 support for git-gui thread. Johannes Sixt provides detailed feedback on Takashi Iwai's v2 patch, focusing on code quality and edge cases while generally approving the approach.

Key technical points raised:
- Notes the patch raises minimum version requirement to Git 2.25.0 due to `--show-object-format` usage
- Suggests using `$nullid` variable instead of `string repeat 0 $hashlength` in multiple places
- Points out a missed SHA-1 length assumption in git-gui.sh line 3217 (`{1,39}` regex)
- Questions whether `hashlength` is available during repository picker dialog execution
- Confirms the commit_committree validation fix is correct
- Comments on the `string map` regex substitution pattern being idiomatic but underdocumented

The review is thorough but notes several opportunities for polish, particularly around variable usage and edge cases. It suggests the commit message could be more precise about the scope of changes. The technical feedback indicates the patch is fundamentally sound but could benefit from addressing these observations before final submission.