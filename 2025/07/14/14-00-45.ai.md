This is a discussion about the meson build system's PCRE2 dependency handling, specifically regarding platform-specific defaults. The email continues the thread about whether to disable PCRE2 by default everywhere (as proposed) or just on macOS (as suggested by brian m. carlson).

Carlo Arenas responds to the suggestion of macOS-only disabling with several concerns: (1) it would create inconsistency with builds from other systems (Makefile, cmake), (2) the root issue (broken macOS system PCRE2) should ideally be fixed by Apple or meson itself, and (3) questions whether PCRE2 should be enabled by default on Linux where system libraries work well. He also questions the broader architectural direction - whether PCRE2 should remain tied to just `git grep` or expand to replace other regex engines, noting previous attempts at broader adoption didn't materialize.

The discussion reveals deeper questions about Git's regex strategy while focusing on the immediate build system fix. The thread shows alignment on needing to handle macOS's broken system PCRE2, but disagreement on whether the solution should be platform-specific (current v3 approach) or global. Technical details mentioned include the meson wrap fallback mechanism and packaging practices across different distributions. The exchange highlights tensions between platform-specific workarounds and build system consistency.