This is the final administrative closure for the Git daemon signal handling series. Junio Hamano responds to Phillip Wood's note about Windows signal handling behavior, agreeing that setting errno on SIGCHLD in the Windows-compatibility layer (patch 1) would be preferable despite the current lack of callers checking it. 

The exchange confirms two key points: (1) the current implementation's decision to not set errno was based on speculative concerns about breaking existing code, and (2) both maintainers agree this conservative approach can be revisited later when actual callers emerge. Junio explicitly approves queuing the v4 series as-is while leaving the errno behavior as a follow-up opportunity.

The thread concludes with all technical decisions finalized and the patches ready for integration. The only remaining consideration is Phillip Wood's observation about errno handling in the signal compatibility layer, which is noted as a potential future improvement rather than a blocking issue. This maintains the separation between the current signal handling improvements and any future work on pipe-based process tracking or errno refinements.