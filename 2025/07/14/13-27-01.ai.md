Refactoring patch converting string predicate functions in `git-compat-util.h` from `int` to `bool` return types, part of a series standardizing C99 `bool` usage for predicates. This second patch focuses on the case-insensitive string prefix functions `skip_iprefix()` and `skip_iprefix_mem()`, aligning them with their case-sensitive counterparts that were converted in an earlier experimental commit (8277dbe987).

The changes are mechanical type signature updates with no behavioral modifications - all `1`/`0` returns become `true`/`false`. The commit message positions this as formalizing an 18-month-old practice after successful production use, noting the `bool` return type better communicates the functions' predicate nature. 

Technical scope is limited to `git-compat-util.h`, touching 6 lines each for the two functions. The patch follows the series' established pattern of incremental C99 adoption, building on prior work converting similar string utilities. As with the rest of the series, this appears to be a low-risk change given the extended testing period and constrained impact.