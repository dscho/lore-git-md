This is a discussion about Git's regex strategy and potential PCRE2 integration beyond just `git grep`. Junio C Hamano responds to Carlo Arenas' suggestion about isolating PCRE2 code, exploring what it would take to use PCRE2 more widely across Git's regex operations.

The email outlines the technical requirements for broader PCRE2 adoption: exposing the `grep_pat` struct and `compile_regexp()` function from grep.c, plus rewriting all direct `regcomp()`/`regexec()` calls to use this interface. Junio notes this would naturally enable PCRE patterns in commands like `git log --grep` and `git blame -L` if implemented.

The exchange reveals deeper architectural questions about whether Git should maintain multiple regex engines or consolidate on PCRE2. While both agree PCRE2 is widely available on Linux systems, the discussion suggests this would be a significant refactoring with implications across the codebase. The thread context shows this ties into ongoing build system work around PCRE2 dependency management, particularly on macOS compatibility issues.

This appears to be a speculative exploration rather than concrete proposal - Junio is seeking clarification on Carlo's vision for PCRE2 isolation while outlining the scope of changes required. The technical depth suggests this could become a substantial project if pursued, but currently remains in the discussion phase.