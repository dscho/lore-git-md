This is a cover letter for a refactoring series that concludes an experiment with C99's `bool` type in Git's codebase. The series follows up on a test change from December 2023 that converted some string utility functions to return `bool` instead of `int`. After 18 months without issues, Phillip Wood proposes to standardize this approach by updating the CodingGuidelines to permit `bool` usage and converting additional string predicate functions to return `bool` consistently.

The series touches core utility functions in `git-compat-util.h` and string buffer operations in `strbuf.[ch]`, changing their return types from `int` to `bool` where they function as predicates. The changes are mechanical but widespread, affecting 4 files with 29 insertions and 26 deletions. The cover letter references a base commit and provides GitHub links for viewing the changes, suggesting this is a well-prepared submission.

For AI context: This continues work started in commit 8277dbe987, converting predicate functions like `skip_prefix()` and `strbuf` methods to use `bool` returns. The changes are focused on type signatures in headers and implementations, with no behavioral modifications. The CodingGuidelines update in patch 1/3 would formally bless this style, while patches 2-3 apply it systematically to remaining string utilities. The lack of reported issues with the initial bool experiment suggests this is low-risk refactoring.