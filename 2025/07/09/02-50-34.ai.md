This is a review comment on patch 1/2 of the submodule configuration improvement series, which has already been approved by Junio C Hamano. The patch prevents `.gitmodules` overwrites when adding new submodules at paths that previously hosted other submodules.

D. Ben Knoble provides user experience feedback, noting that the `--force` behavior (which auto-increments the submodule name when conflicts occur) might be unexpected. They suggest documenting this behavior in `git help submodule` to clarify what happens when force is used. The reviewer also expresses mild disappointment that the patch doesn't implement a "clever trick" to allow reuse of the same name, though they acknowledge the test case properly demonstrates the failure case.

The feedback doesn't raise any technical concerns about the implementation in `builtin/submodule--helper.c` or the test coverage in t7400-submodule-basic.sh. The core functionality remains unchanged from the approved version, with Knoble's comments focusing solely on documentation clarity and user expectations around the force behavior. Since this is patch 1/2 and already approved, these comments appear to be minor suggestions rather than blocking issues.