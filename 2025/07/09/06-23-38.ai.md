This is a refactoring patch in Patrick Steinhardt's Meson build system improvement series (v3 4/8). It cleans up the build infrastructure by removing unused variables in the documentation generation logic.

The patch simplifies the manpage generation in Documentation/meson.build by eliminating two variables: `manpage_target` which was unused, and `manpage_path` which was only used once. The latter is inlined directly into the `custom_target()` call that generates the manpages. This follows the series' theme of streamlining the Meson build configuration.

The changes are straightforward and mechanical cleanup of the build system, removing unnecessary indirection. The patch maintains identical functionality while making the code more direct. This continues the series' pattern of addressing technical debt in the Meson transition, though this particular change is more about code hygiene than any functional improvement.

Key technical details:
- Removes `manpage_target` variable (unused)
- Inlines `manpage_path` into `custom_target()` output parameter
- Maintains same manpage generation behavior
- Touches only Documentation/meson.build
- Follows series' pattern of build system simplification

This appears to be a low-risk cleanup that was likely identified during review of earlier versions of the series, as it aligns with the overall goal of making the Meson build configuration more maintainable.