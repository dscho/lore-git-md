This is the seventh patch in an 8-part refactoring series moving MIDX tracking from global state to per-source storage, part of the larger effort to enable pluggable object database backends.

The patch simplifies `get_all_packs()` by removing its dependency on the global MIDX linked list, instead iterating through the per-source MIDX pointers that were introduced earlier in the series. The key insight is that `prepare_packed_git()` already handles the necessary preparation of both packfiles and MIDX files, making the previous explicit MIDX preparation redundant.

The change is straightforward - it replaces the global MIDX list traversal with iteration through the object sources, checking each source's MIDX pointer. This maintains the same behavior while better matching the series' goal of localizing MIDX handling to individual object sources. The diff shows a clean switch from the old global list approach to the new per-source model with minimal code churn.

As with previous patches in the series, this appears to be a well-considered step in the incremental refactoring, carefully maintaining existing functionality while moving toward the new architecture. The commit message clearly explains why the change is safe (relying on `prepare_packed_git()`'s existing behavior) and the diff shows focused, surgical changes to the core packfile handling code.