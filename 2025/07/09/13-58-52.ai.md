This is a review comment in the ongoing parse-options integer type safety refactoring series. Patrick Steinhardt suggests a naming improvement for the helper functions introduced in patch 2/7 (PARSE_OPT_CMDMODE handling) and later patches.

The current implementation introduces `do_get_int_value()` as a low-level helper that performs raw casting with precision checking, while reserving `get_int_value()` for a higher-level wrapper that will die on errors. Patrick proposes renaming these to `get_int_value()` and `get_int_value_or_die()` respectively, which would make their behaviors more immediately obvious to readers.

This suggestion comes after the series has already seen significant review and refinement, with Patrick having provided consistent technical feedback throughout. The naming change would affect multiple patches in the series (particularly patches 2-7) that work with these integer handling helpers. The proposal maintains the same functionality while improving code clarity, aligning with the series' goal of making the integer option handling more robust and maintainable.

The thread context shows this is part of a comprehensive effort to add type safety across all parse-options integer variants (CMDMODE, SET_INT, BIT, NEGBIT, BITOP, and COUNTUP), with René Scharfe implementing the changes and Patrick Steinhardt providing review. The series has already addressed several technical considerations around integer handling, bounds checking, and backward compatibility.