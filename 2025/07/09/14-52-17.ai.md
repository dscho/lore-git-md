This is a test improvement suggestion for the ongoing submodule configuration management series.

Junio C Hamano responds to D. Ben Knoble's query about test assertions in patch 2/2 of K Jayatheerth's series. The discussion focuses on a specific test case that verifies submodule add operations skip redundant `.active` config entries when the path matches an existing pattern. Knoble questions whether the test should use `test_must_fail` rather than a bare `!` operator to check for the absence of a configuration value.

Junio confirms this would be an improvement, noting that `test_must_fail` provides better error detection since it would catch abnormal failures (like segfaults) that a simple negation operator would miss. This is a minor but meaningful test hygiene point that doesn't affect the core functionality being tested.

The exchange occurs late in the review cycle for a patch that has already undergone extensive discussion and testing. The suggestion doesn't introduce new technical considerations but would make the test more robust. This level of attention to test quality indicates the series is in its final polishing phase before potential merging.