This is part 11 of Patrick Steinhardt's ongoing series to refactor object-file.c for pluggable loose object backends, continuing the `the_repository` removal effort. The patch is a straightforward cleanup that inlines the `for_each_loose_file_in_objdir_buf()` function into its only caller, `for_each_loose_file_in_objdir()`, since the function wasn't being used elsewhere.

The change removes 28 lines of code (including the header declaration) while maintaining identical functionality. This is a mechanical refactoring that doesn't affect behavior - it simply eliminates an unnecessary layer of indirection. The patch follows the series' pattern of simplifying the object-file interface while working toward the larger goal of removing global state dependencies.

While small, this change helps reduce the surface area that will need to be modified in future patches as the series progresses toward its goal of making loose object handling backend-agnostic. The patch appears well-contained and low-risk, with no obvious downsides to the change.