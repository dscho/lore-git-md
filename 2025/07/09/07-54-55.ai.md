This is the seventh patch in an 8-part refactoring series moving MIDX tracking from global state to per-source storage. The series is part of the larger effort to prepare for pluggable object database backends by restructuring how multi-pack indices are managed.

The patch converts `get_all_packs()` to use the new per-source MIDX storage rather than the global linked list. The key change is iterating over `r->objects->sources` to find MIDX file references instead of walking the old `multi_pack_index` linked list. The commit notes this is safe because `prepare_packed_git()` already handles initializing the necessary data structures, eliminating the need for explicit alternate preparation or direct `get_multi_pack_index()` calls.

The diff shows the straightforward mechanical conversion - replacing the MIDX list iteration with source iteration while maintaining the same core logic for preparing MIDX packs. The change touches only `packfile.c` with minimal line modifications (6 insertions, 5 deletions). This continues the series' pattern of focused, incremental changes that maintain backward compatibility while moving toward the new architecture.

This patch follows the established approach of the series, building on previous conversions of MIDX-related functions to use per-source storage. It represents another step in eliminating global MIDX state while preserving existing functionality. The change appears well-contained and consistent with the series' overall goal of better aligning MIDX handling with the object database's source-based structure.