This is a documentation bugfix for git-subtree, continuing the thread about an invalid example command. Marc Branchaud confirms Alexander Krupp's report that the `git subtree split` example fails with "fatal: '0a8f4f0^..' does not refer to a commit" when run verbatim from the docs. The issue stems from the commit range syntax `0a8f4f0^..` being rejected by the internal `rev-parse` check in the subtree script.

Marc notes that removing the `..` suffix allows the command to run but produces empty results, suggesting the example may need more substantial revision than just syntax tweaking. He has CC'd the subtree author (Avery) and David (who added the commit verification check in commit 10a49587fab) for their input. The thread remains open awaiting subtree maintainer input on how to properly fix the documentation example - whether to adjust the commit range syntax or provide a different working example entirely.