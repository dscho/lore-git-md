This is a technical discussion about Windows signal handling compatibility in the context of the ongoing git-daemon child process tracking redesign. Carlo Marcelo Arenas Belón and Phillip Wood are debating how the Windows `sigaction(SIGCHLD)` implementation should behave when replacing existing `signal()` calls.

Key technical points:
- Current Windows `signal(SIGCHLD)` doesn't set errno despite documentation saying it should, possibly due to Git's custom signal number (17)
- Phillip advocates making `sigaction()` behave like standard POSIX, setting errno=EINVAL for consistency
- Carlo prefers initially matching Windows' current `signal()` behavior (no errno set) to avoid potential regressions, with plans to standardize later
- Testing shows using signal number 20 (instead of 17) makes Windows CRT behave as documented
- The discussion reveals platform-specific quirks in Windows signal handling that may require Johannes Schindelin's input
- Underlying tension between immediate backward compatibility (Carlo) and long-term POSIX consistency (Phillip)

This email continues the thread's focus on Windows-specific signal handling details that must be resolved before finalizing either the documentation or pipe-based tracking solutions. The debate now centers on whether to:
1. Make Windows `sigaction(SIGCHLD)` immediately POSIX-compliant (Phillip's position)
2. First match current Windows `signal()` behavior then standardize later (Carlo's preference)

The discussion remains technical but productive, with both sides providing concrete observations about Windows behavior. Resolution may require input from Windows experts like Johannes Schindelin to determine the best path forward for Git's Windows compatibility layer.