This is a maintenance note about an upcoming API change affecting the `git last-modified` series. Junio C Hamano points out that another in-flight topic will rename Bloom filter API functions that the `last-modified` command currently uses (`clear_bloom_key` and `fill_bloom_key` to `bloom_key_clear` and `bloom_key_fill` respectively). 

The email includes a small patch (3-line change) that updates the function calls in `builtin/last-modified.c` to match the new names. Junio suggests keeping the topics independent for now since the renaming work isn't close to being merged, but notes this trivial conflict resolution may be needed if the `last-modified` series requires another reroll after the Bloom filter changes land.

This is a straightforward heads-up about an impending but not yet urgent API change that affects the already-reviewed `last-modified` implementation. The proposed fix is minimal and mechanical, just function name updates in three places where Bloom filter keys are manipulated. The thread context shows the `last-modified` series is otherwise complete with all major design issues resolved.