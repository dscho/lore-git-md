This is a minor test improvement suggestion for the ongoing submodule configuration management series.

Junio C Hamano responds to D. Ben Knoble's query about test assertions in patch 2/2 of the series, which aims to eliminate redundant submodule active entries. The discussion focuses on a specific test case that verifies Git skips writing redundant active entries when a submodule path is already covered by an existing pattern.

The key point is a technical suggestion about test hygiene: Junio agrees that using `test_must_fail` would be better than a bare `!` operator in the test assertion, since `test_must_fail` would properly catch segmentation faults or other abnormal failures that a simple negation would miss. This doesn't affect the core functionality of the patch but improves the robustness of its test coverage.

The exchange shows the patch is in its final review stages, with maintainers scrutinizing even small details like test assertion style. The core functionality remains approved, with this being one of the last minor refinements being discussed before potential merging.