This is a follow-up in the ongoing discussion about SIGCHLD handling in `git daemon`. Phillip Wood is requesting a re-roll of Carlo Marcelo Arenas Belón's patch series that replaces `signal()` with `sigaction()` for more reliable child process handling across platforms.

The original series (currently stalled) aims to fix undefined behavior around signal handling during `poll()` operations, particularly addresses platform-specific quirks in Windows/Cygwin environments. Carlo notes the changes resolve two unspecified behaviors: whether SA_RESTART is enabled by `signal()`, and whether `poll()` can be interrupted when it is enabled.

Phillip references a previous email (message ID provided) where he presumably provided feedback needing incorporation. The discussion remains active but cautious, with related work on self-pipe notification enhancements being held back until this foundational change stabilizes. The series appears technically sound but needs revision before progressing further.