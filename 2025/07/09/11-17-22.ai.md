This is part 12 of Patrick Steinhardt's ongoing series to refactor object-file.c for pluggable loose object backends, continuing the `the_repository` removal effort. The patch focuses on cleaning up an unused public interface.

The change removes the declaration of `for_each_file_in_obj_subdir()` from object-file.h since the function is only used internally within object-file.c. The function itself is marked static to make its file-local scope explicit. This follows the pattern from part 11 which simplified the loose object iteration path, further reducing the public interface surface area.

The modification is straightforward - it converts the function to static and removes its header declaration. The diff shows 14 lines removed (the header declaration and related whitespace) and 7 lines modified (adding static to the function definition). This continues the series' incremental approach of mechanical refactoring while maintaining behavior.

This cleanup is a natural progression after part 11 inlined `for_each_loose_file_in_objdir_buf()`, as it removes another piece of unnecessary public interface. The change doesn't affect functionality but helps prepare for future backend work by reducing the API surface that would need to be maintained across different object storage implementations.

Key technical details for future reference:
- Function name: `for_each_file_in_obj_subdir()`
- File: object-file.c (implementation) and object-file.h (removed declaration)
- Parameters remain unchanged (subdir_nr, path, algop, obj_cb, cruft_cb, subdir_cb, data)
- Still uses `the_hash_algo` parameter (not yet converted to repository-specific)
- Part of loose object iteration path cleanup
- Follows same pattern as part 11's interface reduction