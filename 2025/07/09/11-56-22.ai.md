This is a follow-up discussion about the remote naming collision detection patch that has already reached technical approval. Raymond Pasco provides user perspective on the practical implications of the change, particularly regarding remote names containing slashes.

The email focuses on real-world usage patterns, noting that slash-containing remote names are common (e.g. for GitHub-style username/reponame conventions) but that the specific collision case being prevented (like having both "gregkh" and "gregkh/scsi" remotes) is rare in practice. Pasco confirms the change makes conceptual sense by analogy to existing branch naming rules, and raises a practical question about handling pre-existing conflicting remote configurations.

Key technical points for future reference:
- The validation only applies to new remote additions via `git remote add`, not existing configs
- The discussion confirms the patch's approach aligns with user expectations around hierarchical naming
- No new technical concerns are raised about the implementation
- The thread context shows this is post-consensus feedback rather than a blocking concern

This appears to be final confirmation from a user perspective that the change is reasonable, with the only open question being about legacy config handling (which the patch explicitly doesn't address). The discussion remains focused on the porcelain-level behavior rather than the underlying implementation details that were settled earlier in the thread.