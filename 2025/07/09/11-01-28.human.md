Documentation bug report for `git subtree`. Alexander Krupp reports that the example command in the documentation for `git subtree split` fails with a fatal error. The command attempts to use a revision range `0a8f4f0^..` which Git rejects as invalid. The reporter expected the documented example to work as written, but lacks the subtree expertise to propose a specific fix.

This appears to be a straightforward documentation issue where an example command doesn't actually function as shown. The error suggests the revision range syntax in the example may need adjustment, though the exact correct form isn't immediately obvious from the report. The bug is well-documented with clear reproduction steps and version information, making it easy for maintainers to investigate. Since subtree is a contrib command with less frequent maintenance, such documentation drift isn't entirely surprising.