Refactoring patch in the `the_repository` removal effort, part 4/19. This continues Patrick Steinhardt's series preparing object-file.c for pluggable loose object backends by eliminating global state dependencies.

The patch inlines three helper functions (`check_and_freshen_odb()`, `check_and_freshen_local()`, and `check_and_freshen_nonlocal()`) into their sole remaining caller `freshen_loose_object()`, while renaming `check_and_freshen_odb()` to `check_and_freshen_source()` to better reflect its scope. The change removes 28 lines of code while maintaining the same functionality, with the freshening operation now explicitly iterating through object sources rather than using the previous layered function calls.

Key technical details:
- The renamed `check_and_freshen_source()` still uses the static `strbuf path` buffer for path construction
- `freshen_loose_object()` now directly handles both local and non-local object sources via explicit iteration
- The change still temporarily uses `the_repository->objects` for source iteration, as the series hasn't yet fully eliminated this global
- Behavior remains identical for object freshening operations
- This is a straightforward cleanup that follows logically from part 3's introduction of source-specific object handling
- No functional changes or new interfaces introduced
- Maintains the series' pattern of incremental, behavior-preserving refactors
- Touches core object freshness checking used during garbage collection and object writing
- Next steps in the series will likely continue removing `the_repository` usage in surrounding code