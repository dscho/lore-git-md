This is a review comment on patch 2/2 of K Jayatheerth's submodule configuration series, focusing on eliminating redundant `submodule.<name>.active` entries. D. Ben Knoble provides feedback from a user perspective while noting limited familiarity with submodule internals.

The review raises several points about the patch's clarity and implementation. Knoble requests more concrete examples showing the configuration clutter the patch aims to address, noting their own submodule setup doesn't exhibit this issue. They identify a documentation concern where the replacement comment ("force-enable") is less informative than the original about the active flag behavior. A test suggestion questions whether `test_must_fail` would be more appropriate than bare `!` for the config check.

The review doesn't challenge the technical approach but focuses on improving communication and test robustness. It comes from an actual submodule user perspective, validating that the change doesn't disruptive existing simple configurations (like those using just `submodule.active .`). The feedback suggests minor refinements rather than fundamental changes to the wildcard-based pattern matching logic that's already been extensively discussed in previous rounds.

Key technical details referenced include: the `configure_added_submodule()` function changes, `wildmatch()` usage for path pattern checking, the test case in t7413-submodule-is-active.sh that verifies no redundant active entry is created when a submodule path matches an existing pattern, and the handling of missing submodule.active config. The thread context shows this is the final review stage for a technically sound implementation that's awaiting maintainer approval.