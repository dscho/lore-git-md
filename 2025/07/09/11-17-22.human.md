This is part 12 of Patrick Steinhardt's ongoing series to refactor object-file.c for pluggable loose object backends, continuing the `the_repository` removal effort. The patch focuses on cleaning up an unused function declaration.

The change removes the public declaration of `for_each_file_in_obj_subdir()` from object-file.h and makes the function static in object-file.c, since it's only used internally within that file. This follows the pattern from part 11 which simplified the loose object iteration interface.

The modification is straightforward - it converts the function to static scope and removes its header declaration. This reduces the public API surface area while maintaining all existing functionality. The diff shows 14 lines removed (the declaration) and 7 lines changed (adding static to the definition).

This continues the series' incremental approach of mechanical refactoring, in this case cleaning up an interface that was unnecessarily exposed. Like previous patches, it doesn't change behavior but prepares for future work by reducing global dependencies and cleaning up the codebase.