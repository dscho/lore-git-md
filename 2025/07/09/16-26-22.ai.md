This is a status update in the ongoing discussion about Carlo Marcelo Arenas Belón's SIGCHLD handling series for `git daemon`. The thread involves platform-specific signal handling complexities, particularly around Windows compatibility.

Junio C Hamano clarifies that the series is waiting on reviews (especially for patch 2) rather than author inaction, which he considers a form of "stalling". Carlo responds with technical rationale for the changes, noting they address two unspecified behaviors in current signal handling: the undefined SA_RESTART status when using `signal()`, and the undefined interruptibility of `poll()`. The series would make these by using `sigaction()` instead, with SA_RESTART behavior becoming explicit (potentially making patch 4 unnecessary).

Key technical points raised:
- The changes would standardize behavior currently left to implementation specifics
- Windows compatibility remains a concern, with SIGCHLD signal number differences noted (17 vs 22)
- The discussion references prior feedback from Johannes Sixt about Windows signal handling limitations

The email shows the series remains in limbo state - technically sound but awaiting review consensus, particularly patch 2's implementation, with Windows-specific considerations still needing resolution. The core value proposition (standardizing signal behavior) is clear, but platform-specific edge cases continue to complicate the path forward.