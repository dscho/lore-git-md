This is a maintainer note about an upcoming API change affecting the `git last-modified` series. Junio C Hamano points out that another in-flight patch series will rename some Bloom filter API functions that `git last-modified` currently uses (`clear_bloom_key` and `fill_bloom_key` to `bloom_key_clear` and `bloom_key_fill` respectively). 

He provides a small patch showing the necessary changes to `builtin/last-modified.c` but suggests holding off on applying it until the other series lands in `master`, as the two topics should remain independent for now. This is a typical coordination note about managing parallel development branches, with Junio volunteering to handle the eventual rebase when needed.

The email confirms the `git last-modified` series is otherwise complete and doesn't require any immediate action from the authors. It's a heads-up about a minor technical conflict that will need resolution before final merging, but one that the maintainer plans to handle himself when the time comes.