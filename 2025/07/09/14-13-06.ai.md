This is a technical discussion about Windows signal handling in the ongoing git-daemon signal handling redesign. Phillip Wood and Carlo Marcelo Arenas Belón are debating how the Windows compatibility layer should implement `sigaction(SIGCHLD)` when replacing the current `signal()` calls.

The key points under discussion:
1. **Behavior consistency**: Carlo initially proposed making Windows' `sigaction(SIGCHLD)` match Windows' `signal()` behavior (returning -1 without setting errno), while Phillip argues it should follow standard `sigaction()` semantics (setting errno=EINVAL).
2. **Regression concerns**: Carlo wants to maintain current behavior to avoid potential regressions, while Phillip believes the risk is higher from platform inconsistency.
3. **Implementation details**: They discuss Windows' actual behavior - Carlo notes that while Microsoft's docs say `signal()` should set errno, their testing shows it doesn't for SIGCHLD (using Git's custom signal number 17), but does for standard signals (like 20).

The exchange reveals:
- Phillip is pushing for platform-consistent POSIX behavior
- Carlo is prioritizing backward compatibility in the transition
- Both are investigating Windows' actual behavior through testing
- The discussion remains unresolved, with Carlo suggesting involving Johannes Schindelin (Windows expert) for input.

This is a narrow technical debate within the larger signal handling redesign, focused specifically on Windows implementation details for SIGCHLD handling. The thread context shows this is now a secondary consideration after the main SA_RESTART decision was settled.