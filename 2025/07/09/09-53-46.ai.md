This is the final review discussion for the `--start-after` pagination feature in `git for-each-ref`. The patch implements efficient O(log N) reference seeking across all ref backends, completing the series that exposed the ref iterator API and added seek capabilities.

Key points in this email:
1. Junio C Hamano suggests clarifying the pagination workflow in the commit message (though Karthik Nayak believes this belongs in the commit message rather than user docs)
2. Junio corrects the documentation format for `--start-after` to show it takes a `<marker>` parameter
3. They refine the wording around non-existent markers to be more precise about the always-lexicographic behavior
4. Junio indicates the changes look good overall and will be queued

Technical details:
- The implementation uses the new `ref_iterator_seek()` infrastructure
- Behavior handles concurrent modifications between pagination calls
- Output always starts from lexicographically next ref after marker
- Documentation covers edge cases and usage patterns

The thread shows all technical aspects are settled with:
- Final naming (`--start-after`)
- Precise documentation wording
  - Marker parameter syntax
  - Lexicographic ordering guarantees
  - Concurrent modification behavior
- Junio's approval for merging

This concludes the development cycle for reference pagination, with all review feedback addressed and consensus achieved on the implementation.