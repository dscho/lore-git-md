This is a review comment on the second patch of K Jayatheerth's submodule configuration series. The patch aims to avoid writing redundant `submodule.<name>.active` entries when a submodule's path is already covered by an existing pattern.

D. Ben Knoble provides feedback from a user perspective, noting they find the patch's motivation unclear in practice since their own submodule-heavy repository doesn't exhibit the described configuration clutter. They suggest adding concrete examples showing the problematic configuration patterns the patch addresses.

The review also includes several technical suggestions:
1. A typo fix (change -> Change)
2. A question about whether removing the original comment loses useful context
3. A query about whether `test_must_fail` would be more appropriate than `!` in the test case

These are relatively minor points that don't challenge the core approach, which has already been vetted through multiple rounds of review. The patch appears technically sound but could benefit from clearer documentation of the problem it solves. The reviewer's request for examples suggests the commit message might need expansion to better motivate the change for end users.