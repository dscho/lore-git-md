This is part 4/11 of the SHA-256 default hash algorithm transition series, focusing on consistently using the new GIT_HASH_SHA1_LEGACY constant for legacy protocol and format cases. The patch updates 9 files to replace direct references to GIT_HASH_SHA1 with GIT_HASH_SHA1_LEGACY in situations where SHA-1 is being used as a fallback for older formats rather than an explicit choice. Key changes include protocol handling in receive-pack, bundle operations, remote connections (connect.c, fetch-pack), and repository setup.

The changes affect several important subsystems: receive-pack's protocol parsing, bundle creation/reading, remote transport operations, and repository initialization. The patch systematically replaces hardcoded SHA-1 assumptions with the legacy constant, making it clearer which parts of the code are handling backward compatibility cases versus making explicit hash algorithm choices. This is a mechanical but important change that helps document the distinction between intentional SHA-1 use and legacy fallback behavior.

The patch maintains the same behavior while improving code clarity around hash algorithm selection. It builds on the constant definitions introduced in earlier patches of the series and touches many of Git's network protocol and repository format handling components. The changes are straightforward replacements with no functional changes, though they do make the legacy fallback cases more visible in the code.