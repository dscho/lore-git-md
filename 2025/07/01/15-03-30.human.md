Feature patch completing a series to add pagination support to `git for-each-ref`. This final patch introduces the user-facing `--skip-until` option, building on the ref iterator infrastructure added in earlier patches.

The new option allows starting iteration from a specific reference rather than always beginning from the start, enabling efficient pagination workflows in repositories with millions of refs. The implementation uses the `ref_iterator_seek()` functionality added in patch 3, with careful handling of edge cases like partial matches and directory prefixes. The patch includes extensive tests (180 new lines) covering various skip scenarios and validation that the option can't be combined with general pattern matching.

This appears to be a well-constructed conclusion to the series, with thorough test coverage and clear documentation. The implementation leverages the preparatory work done in earlier patches while keeping the user interface simple. The feature should be particularly useful for tools that need to process large numbers of references in chunks.