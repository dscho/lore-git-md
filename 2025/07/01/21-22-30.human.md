This is part 4 of an 11-patch series transitioning Git to use SHA-256 as the default hash algorithm when built with WITH_BREAKING_CHANGES. The patch focuses on updating legacy protocol and format handling to explicitly use the new GIT_HASH_SHA1_LEGACY constant rather than hardcoded SHA-1 references.

The changes affect several key areas of Git's networking and repository handling code - receive-pack, bundle operations, remote connections, and transport setup. Each case now uses GIT_HASH_SHA1_LEGACY where previously it would directly reference SHA-1, making it clear these are fallback cases rather than intentional SHA-1 selections. The patch is mechanical but important for maintaining clarity as the codebase transitions to SHA-256 by default.

This continues the series' theme of carefully distinguishing between explicit SHA-1 usage (GIT_HASH_SHA1) and legacy fallback cases (GIT_HASH_SHA1_LEGACY). The changes are straightforward replacements with no behavior changes, just improved documentation through constant usage. The patch appears well-tested as part of the larger series, touching 9 files with simple but widespread changes to protocol handling code.