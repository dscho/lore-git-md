This is a CI/build system patch updating the FreeBSD testing environment. The change moves from FreeBSD 13.4 (which is no longer supported) to 14.3, which will be the oldest supported release for the next 10 months. The patch also makes several related CI improvements: it moves some variables into the environment section, adds a test skip for `t7815.12` which assumes glibc regex behavior, and simplifies the test script by removing redundant target specifications.

The changes are straightforward and well-motivated by the FreeBSD release cycle. The patch touches only the `.cirrus.yml` configuration file, updating the FreeBSD image family and adjusting several CI-related environment variables. The commit message clearly explains the rationale for moving to FreeBSD 14.3 and notes the additional CI improvements made while updating the configuration. This appears to be a routine maintenance update to keep the CI system current with supported FreeBSD releases.