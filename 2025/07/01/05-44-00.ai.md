This is a technical discussion about patch 3/5 in the `git apply --intent-to-add` bugfix series, focusing on the conditions for writing intent-to-add index entries. Raymond Pasco responds to Junio Hamano's query about the relationship between the `ita_only` and `update_index` flags in the index update logic.

The core debate centers on whether checking `ita_only` should be explicitly coupled with `update_index` when determining whether to write an index entry. Raymond explains that the code path is already guarded by `state->apply`, which implies one aspect of `update_index` (applying the patch), while the decision to touch the index depends on either `check_index` or `ita_only`. He maintains that the current condition `(state->check_index || (state->ita_only && patch->is_new > 0))` correctly handles the different scenarios where the index should be updated.

This email advances the thread by providing technical rationale for the current implementation while acknowledging Junio's concern about flag coupling. The discussion remains focused on ensuring proper behavior when applying patches with `--intent-to-add`, particularly around the interaction between index updates and new file creation. The exchange demonstrates careful consideration of edge cases in the index update logic while maintaining the series' goal of fixing broken intent-to-add behavior.