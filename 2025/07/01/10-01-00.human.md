This is a brief follow-up in the git-config documentation update thread. Patrick Steinhardt acknowledges Kristoffer Haugsbakk's 5-patch series that updated the git-config(1) man page to document new command-line options (`--show-names`, `--value`, `--url`) and standardize terminology (replacing `value-pattern` with `--value=<pattern>`). 

Patrick gives a general approval of the changes, noting they look "sensible" aside from the previously identified issues (the test inconsistency in t0450.142 where documentation uses `--value=<pattern>` while the implementation shows `--value=<value>`, plus some formatting nits). This email doesn't introduce new technical content but serves as maintainer feedback confirming the documentation improvements are on the right track, pending resolution of the known discrepancies. The thread remains open with the key outstanding issue being whether to update the implementation's help text or revert the documentation to match the existing `--value=<value>` wording.