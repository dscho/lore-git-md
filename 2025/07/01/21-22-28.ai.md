This is part of the SHA-256 default hash algorithm transition series, specifically introducing a new constant to distinguish legacy SHA-1 usage from explicit SHA-1 selection.

The patch adds `GIT_HASH_SHA1_LEGACY` as a constant in hash.h, defined to be the same as `GIT_HASH_SHA1` but with distinct semantic meaning. The key distinction is that this constant represents SHA-1 usage required by older formats (like bundle v1/v2) that don't specify a hash algorithm, rather than cases where SHA-1 is explicitly chosen. This builds on the thread's goal of clearly separating these concepts in the codebase.

The change is purely documentary at this stage - the constant's value is identical to `GIT_HASH_SHA1` - but establishes important conceptual infrastructure for the larger transition. The commit message explains the motivation well, citing bundle formats as a concrete example where the legacy designation applies. This patch follows the v2 series' naming resolution where `GIT_HASH_SHA1_LEGACY` was settled on after discussion (previously called `GIT_HASH_ORIGINAL` in v1).

The change is minimal (just 2 lines added to hash.h) but foundational for the series' approach to handling legacy compatibility. It doesn't yet modify any actual usage sites - those come in later patches - but provides the vocabulary needed to make those changes clear. The patch appears well-contained and properly explained, with no open questions specific to this change.