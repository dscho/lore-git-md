This is a technical clarification in the parse-options refactoring thread, specifically addressing review feedback on patch 1/6 (PARSE_OPT_CMDMODE precision handling). René Scharfe responds to Patrick Steinhardt's question about pointer comparison changes in build_cmdmode_list().

The key clarification is about whether comparing `opts->value` pointers (void*) directly is safe versus the previous approach of comparing through an intermediate `value_ptr`. René confirms they are still comparing pointers, just doing it directly through the struct option's value field rather than through an intermediate variable. This maintains the same pointer comparison semantics while simplifying the code.

René also provides additional context about two other changes in the patch:
1. Why get_value() now receives the full struct option (to properly handle precision when dereferencing void* values)
2. The rationale for using optbug() followed by BUG() for precision validation (wanting to report all bugs but still abort)

The response maintains the series' focus on type safety while clarifying this specific implementation detail. This addresses one of the four open questions noted in the thread summary (the cmdmodes pointer comparison issue). The exchange remains technical and constructive, with René providing clear explanations for the design choices.