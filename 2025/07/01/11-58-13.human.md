Performance discussion about reftable block sizes in network operations. Patrick Steinhardt follows up on Brian M. Carlson's confirmation that `reftable.blockSize` is configurable, adding context about why different defaults exist (4KB for most filesystems vs. Google's 64KB for Spanner storage). He reiterates the performance tradeoff between write efficiency (larger blocks) and read performance (smaller blocks), noting the optimal size depends on both filesystem characteristics and use case.

Steinhardt expresses skepticism that reftable writes would be the primary bottleneck during clones, since reftables require explicit opt-in. This suggests the original performance investigation might need to verify whether reftables are actually in use before optimizing their configuration. The exchange remains technical and constructive, with multiple experts weighing in on implementation details while maintaining focus on practical tuning advice.