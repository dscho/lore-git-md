Bugfix follow-up discussing error reporting improvements in `git stash` index operations.

Justin Tobler responds to Han Young's patch about improving index lock error messages in `git stash`. The core change passes `LOCK_REPORT_ON_ERROR` to `repo_hold_locked_index()` to show specific lock failure reasons rather than generic "could not write index" messages. Justin agrees with the approach but raises a testing concern: while the new tests verify the lock-specific error message, they no longer check for the original generic error message that should still appear in the output. The response suggests maintaining both checks to ensure backward compatibility.

The discussion focuses on test coverage details rather than the core change itself, indicating the patch is technically sound but may need minor test adjustments. Justin's questions imply the change might need verification that the original error message isn't lost in the transition to more detailed reporting. This follows the thread's established direction of improving debugging visibility for index operations while maintaining existing behavior guarantees.