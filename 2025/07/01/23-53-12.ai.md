This is a refactoring patch in the final version (v5) of a series moving sparse-checkout configuration away from global variables, specifically addressing Phillip Wood's concern about config validation timing for `sparse.expectfilesoutsideofpatterns`.

The key issue is that moving this config read from `git_default_config()` (where invalid values cause immediate command failure) to `clear_skip_worktree_from_present_files()` (where invalid values would only be detected mid-command) changes user experience. Ayush acknowledges this but notes the series follows the established pattern in `repo_settings` where invalid config values typically get default values rather than causing errors.

The email confirms the behavioral difference is intentional, aligning with how other migrated settings handle invalid configs via `prepare_repo_settings()`. This represents the final open question in the series - whether to prioritize architectural consistency (treating all sparse-checkout configs uniformly) or user experience (preserving early validation for this specific setting). The thread shows the series has otherwise addressed all technical concerns around initialization safety and performance, with this being the last design tradeoff to resolve.