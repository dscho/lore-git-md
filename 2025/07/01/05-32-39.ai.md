This is a technical discussion about patch 2/5 in the `git apply --intent-to-add` bugfix series, focusing on the index reading behavior. Raymond Pasco responds to Junio Hamano's concern about the current implementation writing a new index with only intent-to-add (ITA) entries, which Junio suggests may be conceptually wrong.

Raymond clarifies that the bug causes the index to think all existing files are deleted when using `--intent-to-add`, because it creates a new index with only ITA entries rather than properly reading and updating the existing index. The root cause is that `read_apply_cache()` wasn't being called in intent-to-add mode, leading to an empty starting state.

The exchange reveals a key technical detail: the fix involves modifying `read_apply_cache()` to properly handle intent-to-add mode by reading the existing index rather than starting fresh. This maintains Junio's original vision for `add -N` behavior while fixing the broken implementation in `apply -N`. The discussion highlights the subtle interaction between index reading/writing and intent-to-add markers, with Raymond providing the technical rationale for why reading the existing index is the correct approach.