This is the first patch in a series introducing a new `--skip-until` pagination feature for `git for-each-ref`. The patch focuses on refactoring the reference iteration internals by exposing the `ref_iterator` interface in `refs.h`. 

The change moves the iterator API documentation and related flags from the internal header to the public `refs.h`, making these implementation details available to advanced users who need more control over reference iteration. The commit explains that the current wrapper functions (`refs_for_each...`) are becoming unwieldy as new functionality combinations are needed, and exposing the iterator directly allows more flexible usage.

The implementation appears thorough, with comprehensive documentation moved verbatim to the public header. This foundational change enables the subsequent patches that will implement the actual pagination functionality across different ref backends. The commit message clearly explains the motivation without delving into the pagination feature itself, keeping the change focused.