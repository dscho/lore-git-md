This thread discusses CI and build system updates for FreeBSD testing support. The initial patch moves the CI environment from FreeBSD 13.4 (now unsupported) to 14.3, which will be the oldest supported release for nearly a year. Alongside this version bump, the change includes several CI improvements: reorganizing environment variables, adding a test skip for `t7815.12` which assumes glibc-specific regex behavior, and simplifying the test script by removing redundant target specifications.

The changes are focused entirely on the `.cirrus.yml` configuration file and appear uncontroversial - they're motivated by FreeBSD's release cycle and represent routine maintenance to keep the CI system current. The patch author has taken the opportunity to clean up related CI configuration while making the necessary version update. There are no outstanding questions or objections noted in the thread.