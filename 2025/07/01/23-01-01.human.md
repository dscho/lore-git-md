This is a final polish patch for the new `git last-modified` plumbing command, addressing a small but important usability issue. The patch series (now in its third revision) introduces a command to track file modification history by walking commits backwards, with Bloom filter optimizations and comprehensive test coverage already in place.

Junio C Hamano points out that the command's `-h` help output doesn't match its documentation, which would cause the standard Git test suite (`t0450`) to fail. This is a straightforward fix needed before the command can be merged. The rest of the series appears solid, with path handling behavior confirmed and performance improvements validated through benchmarks showing 36-50% speedups.

The patch series is in its final polishing stage, with this being one of the last remaining issues to address. The command remains marked as EXPERIMENTAL in the documentation while these final technical items are resolved. The core functionality and optimizations (particularly the Bloom filter integration) appear well-tested and ready for inclusion.