This is part 8/11 of the SHA-256 default hash algorithm series, focusing on test updates for operations outside repositories. The patch modifies t4042-diff-textconv-caching.sh to use the new test_oid infrastructure with --hash=builtin rather than hardcoding SHA-1 values, preparing for when the default hash algorithm changes to SHA-256.

The changes introduce test_oid cache entries for both SHA-1 and SHA-256 variants of the test hashes, then uses test_oid --hash=builtin to select the appropriate one at runtime. This maintains test correctness regardless of the default hash algorithm while keeping the same test logic. The diff output format is updated to use the variable references rather than hardcoded SHA-1 values.

This follows the pattern established in earlier patches (particularly patch 7/11) for transitioning tests to be hash-agnostic. The approach is consistent with the series' goal of making SHA-256 the default when WITH_BREAKING_CHANGES is set, while maintaining backward compatibility. The patch is narrowly focused on test modernization without changing behavior, aligning with the series' incremental approach to the hash transition.

Key technical details:
- Adds test_oid_cache entries for both SHA-1 and SHA-256 variants
- Uses test_oid --hash=builtin to select the correct hash at runtime
- Updates diff output expectations to use variable references
- Maintains same test logic while removing SHA-1 assumptions
- Follows pattern from patch 7/11 for test transitions

The patch appears well-contained and mechanically sound, continuing the series' thorough test coverage approach. It doesn't address the ongoing debate about external command behavior (GIT_HASH_DEFAULT vs GIT_HASH_SHA1_LEGACY) but implements the author's chosen approach consistently within the test framework.