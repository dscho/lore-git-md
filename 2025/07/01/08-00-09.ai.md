Documentation patch addressing inconsistencies in `git-config` option terminology, part of an ongoing thread about post-merge cleanup of the config subcommands series.

Kristoffer Haugsbakk follows up on their earlier patch that standardized `--value=<pattern>` terminology in the git-config documentation, now noting that this change causes test t0450.142 to fail. The test verifies consistency between documentation and command-line help output, and the failure reveals that while the documentation was updated to use `--value=<pattern>`, the actual `git config -h` help text still shows `--value=<value>`. 

This email serves as both an acknowledgement of the test failure and an implicit request for guidance on how to proceed - whether to update the help text to match the documentation's terminology or vice versa. The thread context shows this stems from an earlier testing gap where t0450 wasn't run during the documentation update process, allowing the inconsistency to slip through.

Key technical details for future reference: the test failure specifically involves the `adoc_to_synopsis` vs `help_to_synopsis` comparison in t0450.142, with the documentation now using `<pattern>` while the implementation still shows `<value>`. The original patch series (fe3ccc7aab) introduced this dual terminology, and while the documentation update aimed to standardize on `<pattern>`, the implementation side remains to be addressed. This leaves the thread with an open question about which direction to standardize - documentation to implementation or vice versa.