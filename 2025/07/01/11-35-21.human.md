This is a follow-up discussion in the ongoing thread about Git's SHA-256 transition, specifically addressing the distinction between explicit SHA-1 usage and legacy SHA-1 fallback behavior.

Patrick Steinhardt responds to brian m. carlson's explanation about the new hash algorithm constants (GIT_HASH_SHA1 vs GIT_HASH_ORIGINAL/LEGACY_SHA1). He agrees with the semantic distinction, noting it helps clarify whether SHA-1 usage is intentional (like in test helpers or reftable format v2) versus legacy behavior (like in bundle formats or reftable v1 that implicitly use SHA-1).

The discussion highlights how the constants make the code's intent clearer - GIT_HASH_SHA1 means "use SHA-1 specifically" while GIT_HASH_ORIGINAL means "use whatever the legacy format requires (which happens to be SHA-1)". Patrick provides a concrete example with reftable formats to illustrate when each constant would apply.

This is a technical refinement to the larger SHA-256 transition series, showing the careful consideration being given to backward compatibility and clear semantics. The exchange demonstrates consensus building around the approach, with Patrick offering supportive feedback while also verifying his understanding of the distinctions.