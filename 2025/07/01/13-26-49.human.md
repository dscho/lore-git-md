This is a CI/build system patch providing a minor improvement to the meson build target for git-clang-format that was recently added in this series. Toon Claes points out two small technical issues in Karthik Nayak's implementation: the `find_program()` call should include `native: true` to ensure it finds the host system's git-clang-format (not a cross-compiled version), and the run target should use the found program variable rather than hardcoding 'git clang-format'.

The changes are straightforward and align with meson best practices. Since this is the final patch in an already-approved series that adds meson support matching the existing Makefile functionality, these appear to be minor polish items rather than fundamental issues. The thread context shows the series has already addressed the major technical concerns around git-clang-format detection and target naming.