This is a review follow-up in the `git stash` index lock error reporting thread. Junio C Hamano weighs in on Justin Tobler's observation about test coverage for backward compatibility in error messages.

The discussion focuses on whether the test changes should verify both the new detailed lock error message ("Unable to create 'index.lock'") and the original generic error ("could not write index"). Junio confirms Justin's concern that the test patch removed the check for the original message while only adding the new one, suggesting this was likely unintentional.

The exchange indicates consensus that both error messages should be present in the output for backward compatibility, though neither participant has yet proposed specific test modifications. This moves the thread forward by clarifying an implementation detail that wasn't fully addressed in the initial patch - the need to maintain existing error output while adding new diagnostic information.

The technical context remains unchanged from the thread summary: the core change adds `LOCK_REPORT_ON_ERROR` to index operations in `git stash`, but the testing implications are still being worked out. This email doesn't introduce new technical elements but reinforces the importance of comprehensive error message testing that preserves backward compatibility.