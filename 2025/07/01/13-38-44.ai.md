This is a technical discussion email in the ongoing git-daemon signal handling redesign thread, focusing on implementation details of the pipe-based child tracking approach. Phillip Wood provides several concrete suggestions and concerns about Carlo's proposed pipe implementation.

Key technical points raised:
- Recommends renaming `poll_pipe` to `signal_pipe` for clarity
- Suggests moving the `signal(SIGCHLD)` call from the handler to the event loop to prevent AIX recursion issues
- Argues for proper EINTR handling in pipe writes, noting future maintainability concerns
- Questions the "optional" nature of pipe creation, advocating for `die()` on failure rather than silent fallback
- Notes SIGPIPE handling considerations when using sigaction

The email continues the debate about signal handler placement (beginning vs end) and whether the pipe should be mandatory. Phillip maintains a focus on robustness, preferring explicit error handling over silent fallbacks. He also clarifies his earlier suggestion about AIX recursion prevention by removing signal() from the handler entirely.

This represents incremental refinement of the pipe approach, with Phillip pushing for stronger guarantees and clearer code while Carlo appears more willing to accept fallback paths. The discussion remains technical and constructive, with both parties engaging on specific implementation choices rather than fundamental design disagreements.