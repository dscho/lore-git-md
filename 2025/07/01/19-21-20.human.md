Bugfix improving error reporting in `git stash` when index operations fail. This is a follow-up to Han Young's patch that makes `git stash` show more specific error messages when it can't acquire the index lock, rather than the generic "could not write index" message.

Junio C Hamano chimes in on Justin Tobler's review, agreeing that while the new test properly checks for the detailed lock error message ("Unable to create 'index.lock'"), it should also verify that the original generic error ("could not write index") still appears for backward compatibility. This maintains consistency with how other Git commands handle similar lock failures.

The discussion remains focused on test coverage rather than the core change itself, which appears uncontroversial. The one-line code change in read-cache.c that enables detailed lock reporting seems sound, but the test suite needs to properly validate both old and new behaviors. This is typical Git maintenance work - small but meaningful improvements to error reporting, with careful attention to backward compatibility.