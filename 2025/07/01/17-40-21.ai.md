This is a follow-up to the memory leak fix in `send-pack`'s `extra_have` oid array handling. Junio C Hamano points out that the original patch missed cleaning up resources when `match_push_refs()` fails early in the function. He proposes an additional change to ensure proper cleanup in all exit paths by introducing a `cleanup` label and consolidating the resource release code there. The modification ensures the `extra_have` array (along with other resources like refs, refspecs, and shallow arrays) gets cleared even when the push operation fails during the ref matching phase. This builds on the earlier uncontroversial leak fix while maintaining the same cleanup pattern for all error paths. The change is straightforward and aligns with established error handling practices in the codebase. No new test coverage is mentioned for this particular refinement since it addresses an internal resource management issue rather than changing observable behavior.