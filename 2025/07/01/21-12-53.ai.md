This is a follow-up to the memory leak fix in `send-pack`'s `extra_have` oid array handling. Junio C Hamano acknowledges Jacob Keller's original fix but points out it missed cleanup during early exit paths when `match_push_refs()` fails. His refinement introduces a `cleanup` label to consolidate resource release, ensuring the `extra_have` array (along with refs, refspecs, and shallow arrays) gets properly cleared in all exit paths. The change doesn't affect observable behavior but makes the cleanup more robust. Jacob Keller confirms the refinement is sound, effectively concluding this thread about comprehensive resource management in `cmd_send_pack()`. The only remaining gap (test coverage for alternate-store push scenarios) was previously acknowledged as out of scope for this fix.