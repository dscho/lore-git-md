This is a follow-up to a documentation patch in the git-config man page update series, addressing a terminology inconsistency in the thread has already established that the documentation uses `--value=<pattern>` while the implementation's help text shows `--value=<value>`, creating a test failure in t0450.142.

Kristoffer Haugsbakk confirms their original patch missed some instances of `--value=<value>` in the documentation, though they don't specify exactly where these remaining instances are located. This serves as additional confirmation of the documentation/implementation mismatch that was previously identified in the thread, rather than proposing new changes.

The email reinforces that the inconsistency stems from the original series (merged in fe3ccc7aab) where both `--value=<value>` and `--value=<pattern>` forms were used, with the documentation patch attempting to standardize on `<pattern>` terminology since it better describes the option's actual behavior for regex pattern matching. The thread has already noted this creates a mismatch with the help text implementation.

Key elements for future reference: this confirms additional undocumented instances of the terminology inconsistency beyond what was caught in t0450.142, though the specific locations aren't provided. The thread now contains both the original documentation patch (PATCH 2/5) and this follow-up confirmation of remaining inconsistencies, documenting the full scope of the issue that needs resolution.