This is a documentation and code alignment patch series updating git-config(1) man page and implementation to match recent subcommand changes. The v2 series addresses feedback from the initial submission, particularly fixing test failures in t0450.142 that revealed inconsistencies between documentation and implementation.

The 5-patch series standardizes terminology around the `--value=<pattern>` option (replacing both `value-pattern` and inconsistent `--value=<value>` usage), documents the new `--[no-]show-names` option, and adds `--url` to the synopsis. Key technical changes include updates to both Documentation/git-config.adoc and builtin/config.c to ensure alignment, with the interdiff showing the systematic replacement of `--value=<value>` with `--value=<pattern>` throughout the command usage strings.

Notable for future reference: the series now carries Acked-by tags from Patrick Steinhardt for all patches, indicating maintainer approval of the technical changes. The v2 changes specifically address the test failure by ensuring documentation and implementation match in their use of `--value=<pattern>`, though the thread notes this leaves a remaining inconsistency between the documentation's "pattern" terminology and the implementation's help text still showing "value".

The series builds on the ps/config-subcommands merge (fe3ccc7aab) and references to specific commits like 4e513890008 that introduced the original functionality. While the documentation update is now technically complete, the thread has surfaced an underlying issue about whether the implementation's help text should also be updated to say "pattern" instead of "value", which may need follow-up discussion.