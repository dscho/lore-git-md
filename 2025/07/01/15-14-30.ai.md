Documentation patch completing the git-config man page updates for the `--value`/`--no-value` options. This is part of the v2 series that standardizes terminology and documentation following the ps/config-subcommands merge.

The patch adds documentation for `--value=<pattern>` and `--no-value` options to git-config.adoc, noting they work with get/set/unset subcommands to match against patterns (as extended regexes unless `--fixed-value` is used). The commit message references the original implementation in fe3ccc7aab and follows Junio Hamano's suggested documentation style. The patch carries Patrick Steinhardt's Acked-by, indicating maintainer approval.

Key technical details for future reference:
- Documents behavior that was implemented but previously undocumented
- Standardizes on `--value=<pattern>` terminology (vs original `value-pattern`)
- Notes regex behavior and interaction with `--fixed-value`
- Covers all three affected subcommands (get/set/unset)
- Follows same style as recently documented `--[no-]show-names` option

This completes one of the remaining documentation gaps in the series, with the only outstanding question being whether to update the in-code help text to match the new `--value=<pattern>` terminology. The patch appears straightforward and well-justified, with clear commit message linking to prior discussion.