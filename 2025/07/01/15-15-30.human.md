This is a technical clarification in the parse-options refactoring series, specifically addressing review feedback on patch 1/6 which introduced precision handling for PARSE_OPT_CMDMODE options.

René Scharfe responds to Patrick Steinhardt's question about a pointer comparison in build_cmdmode_list(). The original patch changed from comparing direct value pointers to comparing through a struct option pointer, and Patrick raised a concern about whether distinct options might share the same value pointer. René confirms they are still comparing pointers (the void* value fields), just accessed through the struct option rather than directly.

The exchange also clarifies two other points from the original patch: the purpose of get_value() checking all PARSE_OPT_CMDMODE variables (to detect conflicting changes), and the rationale for using optbug() followed by BUG() for precision validation (to report all issues while still enforcing termination).

This is a minor but important clarification in an otherwise solid refactoring series. The discussion remains technical and focused, with René providing clear explanations of the implementation choices. The thread shows careful attention to detail from both author and reviewer, typical of Git's development culture.