This is part of the ongoing SHA-256 transition series, specifically introducing a new constant to clarify legacy hash algorithm usage. The patch adds `GIT_HASH_SHA1_LEGACY` to distinguish cases where SHA-1 is used due to older formats (like bundle v1/v2) that implicitly require it, versus other situations where SHA-1 might be explicitly chosen. 

The change is purely documentary at this stage - the new macro expands to the same `GIT_HASH_SHA1` value, but its introduction helps code readers understand when SHA-1 is being used for backward compatibility versus other reasons. This is the second patch in an 11-part series that aims to make SHA-256 the default hash algorithm when Git is built with `WITH_BREAKING_CHANGES`.

The patch is straightforward and well-justified, with a clear commit message explaining the motivation. It builds on previous discussion where the constant naming was debated and finalized as `GIT_HASH_SHA1_LEGACY`. This kind of careful distinction between different SHA-1 usage contexts will be important as the project transitions to SHA-256 by default.