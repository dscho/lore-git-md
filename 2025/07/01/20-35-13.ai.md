This is a discussion about the user interface design of the new `git last-modified` command, specifically around its path handling behavior. Kristoffer Haugsbakk raises several points about the command's current behavior with paths and directories, comparing it to other Git commands' patterns.

The key observations focus on the command's non-recursive default behavior and its handling of pathspecs. Currently, without `-r`, the command shows directories rather than individual files in subdirectories, which differs from how most Git commands treat pathspecs. Kristoffer demonstrates this with examples showing how `git last-modified` outputs directory names when given subdirectory files, unless `-r` is used to force recursion.

The email also notes some edge cases: the command accepts pathspec magic (like `:^` exclusions) but produces potentially confusing output when given patterns like `t/*` (showing just the directory). There's also a minor UX note about error messages for unknown options appearing after the fatal error.

This discussion comes as part of the final polishing phase for the `git last-modified` series, which has already gone through significant technical refinement (Bloom filter optimizations, memory management) and is now focusing on user-facing aspects. The thread context shows this is part of an ongoing conversation about the command's experimental status and interface design, with multiple contributors involved in shaping its behavior.