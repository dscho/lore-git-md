Refactoring patch in Patrick Steinhardt's series to eliminate `the_repository` global state. This is patch 3/17 in the v6 series, following the structural renaming of `raw_object_store` to `object_database` in previous patches.

The patch renames the core object storage files from `object-store.{c,h}` to `odb.{c,h}` to better reflect their new role in the object database subsystem. This is a mechanical change that updates include paths across 126 files, with no functional changes. The commit message notes this rename aligns with the earlier structural renaming `struct raw_object_store` to `struct object_database`.

The changes are straightforward and consistent with the series' goal of establishing clear naming conventions for the ODB subsystem. The patch has no direct dependencies beyond the earlier structural renames in the series, and its impact is limited to include directives and build system references. This appears to be a well-contained preparatory change for the larger architectural refactoring.