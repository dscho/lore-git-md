This is a follow-up to a documentation patch series that standardized terminology in the git-config man page. The original series aligned the documentation to use `--value=<pattern>` consistently, but Kristoffer Haugsbakk notes that some instances of `--value=<value>` were missed in the initial pass.

The thread context reveals this is part of a larger effort to clean up inconsistencies following the ps/config-subcommands merge. A test failure in t0450.142 exposed that the synopsis (showing `--value=<pattern>`) didn't match the `-h` help output (showing `--value=<value>`), which went unnoticed because the t0450 tests weren't run during the documentation updates.

This email serves as confirmation of remaining inconsistencies rather than proposing new changes. The author acknowledges the oversight, reinforcing that the test verification gap was the root cause. The thread documents both the intended behavior (using `--value=<pattern>`) and the process issue that allowed the inconsistency to slip through.

The patch appears mechanically sound - it's a straightforward documentation fix - but the discussion highlights the importance of comprehensive test coverage even for documentation changes. The remaining work to align the implementation's help text with the documentation is noted as an open issue.