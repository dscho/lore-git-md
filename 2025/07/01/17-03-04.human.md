This is a follow-up clarification in the Git for Windows credential manager thread. Johannes Schindelin corrects his earlier syntax suggestion for overriding the default credential helper, noting that the proper flag is `--append` rather than `--add` when using `git config set`. The email serves as a minor correction to the previously validated solution, confirming that Aditya Garg had already found the correct approach by manually editing the config file. The core solution remains unchanged - setting an empty-string helper followed by the desired helper successfully overrides Git for Windows' default credential manager. This exchange highlights the importance of precise syntax in Git configuration commands, though the practical outcome is the same as previously established.