This is a follow-up discussion in the clang-format rules refinement thread, specifically addressing formatting details in the implementation. Toon Claes responds to Karthik Nayak's patch series about reducing clang-format false positives, noting an unexpected formatting change in pathspec.h where spaces were added around the `<<` operator in a bit-shift operation.

The email highlights a specific formatting case that wasn't covered by the proposed rules, showing how `git clang-format` modified `(1<<7)` to `(1 << 7)` in pathspec.h. Toon expresses surprise at not finding a configuration setting to control this particular spacing behavior, suggesting this might be an area needing further attention or documentation.

This touches on the series' goal of reducing false positives while maintaining that some automatic formatting changes (like this operator spacing) may still be desirable or need explicit configuration. The thread has already resolved major technical points about line wrapping and brace removal, with this being a minor follow-up observation about edge cases in the formatting rules.