This is a follow-up clarification in the Git for Windows credential manager thread. Johannes Schindelin corrects his earlier syntax suggestion for overriding the credential helper, pointing out that the correct flag is `--append` rather than `--add` when using `git config set`. The email serves as a minor correction to the previously validated solution, noting that the documentation specifies `--append` for adding new values without altering existing ones. Aditya had already found the correct approach by manually editing the config file, confirming the solution works. The thread remains effectively resolved, with this email providing documentation accuracy for future reference. The core technical solution (empty-string helper followed by desired helper) remains unchanged and validated.