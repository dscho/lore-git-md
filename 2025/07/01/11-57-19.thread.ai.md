[PATCH v2] git stash: improve index lock error reporting

Thread identity: Patch series from Han Young addressing error reporting in `git stash` when index operations fail due to lock contention, now in second version with all feedback incorporated and ready for merging.

Core problem: `git stash` commands show generic "could not write index" messages when failing to acquire the index lock. The change exposes specific lock failure reasons by passing `LOCK_REPORT_ON_ERROR` to `repo_hold_locked_index()` while maintaining backward compatibility with existing error output.

Current status: Version 2 has completed review with all feedback addressed. Junio C Hamano has taken the patch for final integration after handling a minor 80-column line wrap locally. The implementation verifies both error message formats work as intended through comprehensive test coverage in t/t3903-stash.sh.

Technical details: Maintains the one-line core change in read-cache.c to propagate lock errors with `LOCK_REPORT_ON_ERROR`. Test updates use `test_grep` to check both detailed lock error messages (e.g. "Unable to create 'index.lock': File exists") and the generic "could not write index" output pattern.

Participant positions: Justin Tobler identified testing gaps which were fully addressed. Junio C Hamano approved the technical content and is handling final formatting. Han Young has responded to all feedback with comprehensive test coverage while keeping the implementation minimal.

Related work: Builds on Git's lock reporting infrastructure (`LOCK_REPORT_ON_ERROR` flag). The thread is now resolved with the patch ready for integration into the main codebase.