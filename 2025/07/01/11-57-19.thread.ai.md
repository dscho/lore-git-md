[PATCH v2] git stash: improve index lock error reporting

Thread identity: Patch series from Han Young addressing error reporting in `git stash` when index operations fail due to lock contention, now in second version with review feedback incorporated from Justin Tobler and Junio C Hamano.

Core problem: `git stash` commands show generic "could not write index" messages when failing to acquire the index lock. The change exposes specific lock failure reasons by passing `LOCK_REPORT_ON_ERROR` to `repo_hold_locked_index()` while maintaining backward compatibility with existing error output.

Current status: Version 2 adds comprehensive test coverage addressing prior review feedback. The implementation now verifies both error message formats work as intended: the new detailed lock error messages (e.g. "Unable to create 'index.lock': File exists") and the original "could not write index" output. Test cases in t/t3903-stash.sh use `test_grep` to check both patterns, replacing static expected output files for better maintainability.

Technical details: Maintains the one-line core change in read-cache.c to propagate lock errors with `LOCK_REPORT_ON_ERROR`. The test updates demonstrate the dual output behavior works correctly - scripts parsing the old messages won't break while human users get more detailed diagnostics. The changes remain focused on `git stash` without expanding scope to other commands.

Open questions: Whether to document the enhanced error reporting in release notes remains unresolved. Potential applicability to other commands using similar index writing patterns is noted but not pursued in this series.

Participant positions: Justin Tobler's initial testing gap identification has been fully addressed in v2. Junio C Hamano's requirement for backward compatibility is verified by the new tests. Han Young has responded to feedback by implementing comprehensive test coverage while keeping the implementation minimal.

Related work: Continues building on Git's lock reporting infrastructure (`LOCK_REPORT_ON_ERROR` flag). Follows patterns from other commands showing detailed lock errors. The thread has progressed from implementation to verification, with all identified review concerns now addressed in v2.