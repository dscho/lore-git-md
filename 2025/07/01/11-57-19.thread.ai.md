[PATCH] git stash: improve index lock error reporting

Thread identity: Patch from "Some Author" addressing error reporting in `git stash` when index operations fail due to lock contention.

Core problem: Currently, `git stash` commands show a generic "could not write index" message when failing to acquire the index lock, hiding the underlying cause. The change exposes the specific lock failure reason by passing `LOCK_REPORT_ON_ERROR` to `repo_hold_locked_index()` in non-gentle mode.

Technical details: Modifies `repo_refresh_and_write_index()` in read-cache.c to propagate lock errors. Updates t/t3903-stash.sh tests to expect detailed lock messages (e.g. "Unable to create 'index.lock': File exists") instead of generic failures. The change is minimal (one-line core modification) but improves debugging visibility.

Status: Initial version proposing the improvement. No reviews or acks yet. Tests demonstrate the new behavior but may need verification on edge cases like permission errors.

Open questions: Whether the change should apply to other commands beyond `git stash` that use similar index writing patterns. Potential need to document the more verbose error reporting in release notes.

Related work: Builds on Git's existing lock reporting infrastructure (`LOCK_REPORT_ON_ERROR` flag). Follows established patterns from other commands that show detailed lock errors.