[PATCH v2] git stash: improve index lock error reporting

Thread identity: Patch series from Han Young addressing error reporting in `git stash` when index operations fail due to lock contention, now in second version with all review feedback incorporated.

Core problem: `git stash` commands show generic "could not write index" messages when failing to acquire the index lock. The change exposes specific lock failure reasons by passing `LOCK_REPORT_ON_ERROR` to `repo_hold_locked_index()` while maintaining backward compatibility with existing error output.

Current status: Version 2 adds comprehensive test coverage addressing prior review feedback from Justin Tobler and Junio C Hamano. The implementation verifies both error message formats work as intended: new detailed lock error messages (e.g. "Unable to create 'index.lock': File exists") and the generic "could not write index" output. Junio has indicated the change is ready for merging, with only a minor 80-column line wrap needed which he will handle locally.

Technical details: Maintains the one-line core change in read-cache.c to propagate lock errors with `LOCK_REPORT_ON_ERROR`. Test updates in t/t3903-stash.sh use `test_grep` to check both message patterns, replacing static expected files for better maintainability. The changes remain focused on `git stash` without expanding scope to other commands.

Open questions: None remaining - all prior concerns have been addressed. Documentation of the enhanced error reporting in release notes could be a future follow-up but is not blocking this change.

Participant positions: Justin Tobler's testing gap identification has been fully addressed. Junio C Hamano has approved the technical content and will handle final formatting. Han Young has responded to all feedback with comprehensive test coverage while keeping the implementation minimal.

Related work: Builds on Git's lock reporting infrastructure (`LOCK_REPORT_ON_ERROR` flag). Follows patterns from other commands showing detailed lock errors. The thread has progressed through review to final approval, with all identified concerns now resolved.