[PATCH] git stash: improve index lock error reporting

Thread identity: Patch series from Han Young addressing error reporting in `git stash` when index operations fail due to lock contention, with review feedback from Justin Tobler.

Core problem: `git stash` commands show generic "could not write index" messages when failing to acquire the index lock, obscuring the underlying cause. The change exposes specific lock failure reasons by passing `LOCK_REPORT_ON_ERROR` to `repo_hold_locked_index()` in non-gentle mode.

Current status: Initial version proposing the improvement, now under review. Justin Tobler agrees with the technical approach but identifies a testing gap - while new tests verify lock-specific error output, they should also confirm the original generic error message still appears for backward compatibility.

Technical details: Modifies `repo_refresh_and_write_index()` in read-cache.c to propagate lock errors with `LOCK_REPORT_ON_ERROR`. Updates t/t3903-stash.sh tests to expect detailed lock messages (e.g. "Unable to create 'index.lock': File exists"). The change remains minimal (one-line core modification) but improves debugging visibility. Testing needs to verify both the new detailed messages and preservation of the original generic error.

Open questions: Whether test adjustments should explicitly check for both old and new error messages. Potential need to document the more verbose error reporting in release notes. The thread hasn't yet addressed whether the change should apply to other commands using similar index writing patterns.

Participant positions: Justin Tobler supports the technical direction but requests enhanced test coverage to maintain backward compatibility guarantees. Han Young has not yet responded to the testing feedback.

Related work: Builds on Git's existing lock reporting infrastructure (`LOCK_REPORT_ON_ERROR` flag). Follows established patterns from other commands that show detailed lock errors. The discussion remains focused on `git stash` without yet expanding scope to other commands.