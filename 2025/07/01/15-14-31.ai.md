This is a documentation patch completing the terminology standardization in the git-config man page, part of a v2 series that has already addressed technical issues and received maintainer approval.

The patch updates remaining instances of `value-pattern` in the documentation to use the preferred `--value=<pattern>` syntax, aligning with the implementation introduced in fe3ccc7aab. Changes affect both option descriptions and examples, including the `--replace-all`, `--fixed-value`, and multi-line configuration sections. The deprecated `value-pattern` terminology is now properly confined to the "Deprecated Modes" section as intended.

The patch carries an Acked-by from Patrick Steinhardt, confirming the changes match the implementation's behavior. This completes one of the final documentation alignment tasks from the ps/config-subcommands series merge, with the only remaining inconsistency being the help text terminology (which remains `--value=<value>`) - a non-blocking issue noted for potential future follow-up.

Key technical details for future reference:
- Affects git-config.adoc documentation only
- Standardizes on `--value=<pattern>` terminology throughout
- Maintains consistency with `--fixed-value` option description
- Preserves regex behavior documentation
- Leaves help text (`--value=<value>`) as known inconsistency
- Part of comprehensive doc update for config subcommands functionality