This is a test infrastructure patch addressing an issue with `GIT_TEST_INSTALLED`. The change ensures the test suite properly respects this environment variable when determining the default hash algorithm, fixing a case where tests would fail because the hash was incorrectly detected as empty.

The patch modifies `t/test-lib.sh` to consistently use the same logic for locating the Git binary in two places: the initial build check and the hash detection. Previously, the hash detection code would always use `$GIT_BUILD_DIR/git`, ignoring `GIT_TEST_INSTALLED`. The fix introduces a `GIT_BINARY` variable that incorporates both possible locations (build dir or installed path) plus the platform-specific `$X` suffix, though the suffix is noted to be unnecessary for execution on Windows.

The change appears straightforward and well-motivated by real-world test failures in the author's environment. The commit message clearly explains the problem and solution, though it doesn't mention whether this affects any particular test files beyond the initialization sequence. The patch is small, focused, and touches only test infrastructure code, making it unlikely to introduce regressions in core functionality.