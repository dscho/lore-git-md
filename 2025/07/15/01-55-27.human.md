This is a build system patch addressing PCRE2 dependency handling in the meson build configuration, specifically for macOS. Eli Schwartz responds to Carlo Marcelo Arenas Belón's v3 patch with strong disagreement about the approach to handling broken system PCRE2 installations on macOS.

Eli argues the current solution (which defaults to 'auto' and adds a macOS-specific workaround option) is fundamentally flawed because it still allows builds to fail at compile time rather than detecting and handling the broken dependency early. He proposes an alternative implementation that would actively verify PCRE2 header availability on macOS and fail fast with clear error messages when the feature is explicitly enabled but broken, while Carlo's patch makes the feature opt-in via a workaround flag, Eli wants the build system to automatically detect and handle broken installations without requiring user intervention. The technical discussion centers around whether to use meson's `disable_auto_if()` (current approach) versus direct header verification (proposed alternative).

This email deepens the technical debate about how to properly handle platform-specific dependency issues in the meson build, with Eli taking a stronger stance on build system correctness and user experience. The disagreement reflects broader tensions in the thread between pragmatic workarounds and robust architectural solutions for dependency management.