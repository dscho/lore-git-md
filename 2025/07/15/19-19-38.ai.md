This is a technical discussion thread about SHA-256 support in git-gui, specifically addressing edge cases in the v2 patch series. Johannes Sixt responds to Takashi Iwai's proposed solutions for several initialization timing issues where the hash length isn't yet available.

Key technical points in this response:
- Confirms the regex at line 3217 should handle abbreviated hashes, though the exact need for 39-char checks remains unclear
- Agrees to use a range check (40,64) for the repository picker dialog case where `hashlength` isn't initialized yet
- Suggests an alternative string manipulation approach for parsing FETCH_HEAD that avoids regex entirely
- Approves Iwai's idea to use `git rev-parse HEAD` output length for the post-checkout hook case
- Notes the opportunity to use existing `$nullid` variable where possible

The discussion shows progress on resolving the open issues from the v2 review, with both parties converging on practical solutions that maintain compatibility with both SHA-1 and SHA-256 repositories. The technical focus remains on proper hash length handling before full repository initialization.