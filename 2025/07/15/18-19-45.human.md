This is a discussion about the proposed `git repo` command implementation, specifically focusing on the output format specification in the documentation.

Justin Tobler responds to Lucas Seiki Oshiro's patch introducing the new `git repo` command hierarchy. The main concern raised is about documenting the output format (nul-terminated key-value pairs) before the actual implementation exists. Justin suggests either deferring this documentation until the implementation is complete or considering making the nul-terminated format optional via a `-z` flag rather than the default, following Git's established conventions.

The discussion occurs within the broader context of an ongoing effort to create a new command for repository information that would absorb some functionality currently in `git rev-parse`. The thread shows active mentoring and review from multiple Git contributors, with particular attention to command design decisions and documentation practices.

This email highlights an important implementation detail that needs resolution before the feature can be finalized - whether to make the machine-readable format the default or optional. The feedback comes from an experienced contributor (Justin Tobler) and reflects Git's established patterns for similar commands.