This is a technical discussion about the meson build system's PCRE2 dependency management, specifically addressing macOS-specific issues. Eli Schwartz responds to Carlo Arenas Belón's v3 patch with strong disagreement about the approach to handling broken system PCRE2 installations on macOS.

Eli argues the current solution (defaulting to 'auto' with a macOS-specific disable and an opt-in workaround flag) is fundamentally flawed because it still allows builds to fail at compile time rather than detecting and handling the broken dependency early. He proposes an alternative implementation that would actively verify PCRE2 header availability on macOS and either fail clearly (if PCRE2 is required) or gracefully disable the feature (if auto-detected as broken).

Key technical points in Eli's response:
1. The patch doesn't actually solve the macOS PCRE2 problem - it just moves when the build fails
2. Suggests using `compiler.has_header()` to properly detect broken installations
3. Provides concrete code for early failure with clear error messages
4. Agrees with changing the default from 'enabled' to 'auto' globally
5. Strongly objects to the macOS-specific workaround flag approach

This email deepens the technical debate about how to handle broken system dependencies, with Eli advocating for more robust detection rather than platform-specific toggles. The discussion remains focused on the immediate build system issue while the broader regex architecture questions (raised earlier in the thread by Junio) remain unaddressed in this response.