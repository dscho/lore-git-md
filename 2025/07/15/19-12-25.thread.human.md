This thread has been working through optimizations for byte-swapping operations in Git's codebase, particularly focusing on standardizing the handling of `ntohl()` and related functions across different platforms and compilers. The series, originally authored by Carlo Marcelo Arenas Belón with contributions from Sebastian Andrzej Siewior, is now in its third iteration and being circulated by Junio C Hamano for final review.

The key changes in v3 are limited to typo fixes in commit messages from v2, with no substantive code changes, suggesting this is likely the final version. The series modernizes the implementation by removing x86-specific assembly in favor of compiler optimizations, adds support for MSVC via defined endianness constants, and leverages `__builtin_bswap` where available while maintaining fallback implementations. The changes are confined to `compat/bswap.h` and maintain all existing behavior while improving code structure.

Sebastian Andrzej Siewior has now confirmed that v3 contains only these minor documentation fixes, with the code itself remaining unchanged from v2. Discussion continues around some edge cases, particularly regarding musl's use of function calls rather than optimized macros and Windows' handling of `ntohll()`. MSVC still shows some suboptimal performance with certain operations, but these are being deferred as future work. With all substantive feedback addressed and no outstanding objections, this series appears ready for merging.