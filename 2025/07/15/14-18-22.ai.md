This is a follow-up in the SHA-256 support for git-gui thread, addressing review feedback on the v2 patch. Takashi Iwai responds to Johannes Sixt's technical suggestions about the SHA-256 implementation details in git-gui.

Key points from this response:
- Acknowledges the need to fix the regex at line 3217 that checks for partial hash IDs (currently {1,39} instead of proper length)
- Questions why the original code checked up to 39 chars rather than 40 for partial hashes
- Will prepare a preliminary cleanup patch for the duplicate $null_sha1/$nullid variables
- Identifies two places where $hashlength can't be used (repository picker dialog and post-checkout hook) since it's not yet initialized
- Proposes alternative approaches for these cases: using length ranges (40,64) or git rev-parse output length
- Confirms the Tcl string map approach for regex patterns is acceptable despite being somewhat obscure

The email shows the author working through edge cases in the SHA-256 conversion, particularly focusing on initialization timing issues and variable usage patterns. Several technical questions remain open about the best way to handle hash length checks in early initialization code paths.