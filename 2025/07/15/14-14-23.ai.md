This is a technical follow-up in the meson build system thread about PCRE2 dependency handling, specifically addressing Windows fallback behavior. Eli Schwartz clarifies a key point about meson's dependency resolution that was previously misunderstood in the thread.

The email corrects Carlo Arenas' mistaken assumption that 'auto' mode would fall back to the subproject wrapper, explaining meson's actual fallback logic through the `dependency()` function's `allow_fallback` parameter. The technical detail provided shows that fallback only occurs for *required* dependencies by default, and outlines the precise conditions under which automatic fallback would work (system found OR wrap with internet/local download available).

This input directly addresses the open question from the thread summary about Windows-specific handling of the 'auto' default, providing the missing piece to properly implement the desired behavior. The explanation suggests the solution may lie in using meson's built-in fallback mechanism rather than requiring explicit `-Dpcre2=enabled` on Windows.

The email maintains the thread's technical focus while narrowing the discussion to the specific Windows fallback case, offering concrete path forward for the build system configuration. It doesn't revisit the broader architectural questions about regex handling that were noted as separate future work in the thread summary.