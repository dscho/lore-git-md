This thread addresses a test infrastructure issue where the `GIT_TEST_INSTALLED` environment variable wasn't being respected during hash algorithm detection in the test suite. The patch fixes this by ensuring consistent handling of the binary path between the initial build check and hash detection phases, introducing a `GIT_BINARY` variable to unify the logic.

The change is small and focused, modifying only `t/test-lib.sh` to prevent test failures that occurred when the hash algorithm was incorrectly detected as empty. While the commit doesn't specify which particular tests were affected, the fix appears well-motivated by real-world testing scenarios. The patch touches only test infrastructure code, making it low-risk for core functionality.