This is a follow-up discussion in the meson build system thread about PCRE2 dependency handling, specifically addressing macOS verification and potential meson improvements.

Eli Schwartz responds to Carlo Arenas' confirmation that the `compiler.has_header()` check successfully detects broken PCRE2 installations on macOS. The exchange reveals two key technical points: first, that the current solution works for the immediate problem, and second, that it exposes a potential enhancement opportunity for meson itself. Schwartz suggests two possible meson-side improvements - either adding a `has_header` parameter to `dependency()` similar to `find_library()`, or implementing a platform-specific quirk in meson build system code that would automatically verify headers for PCRE2 on Darwin systems.

The email includes a concrete code snippet showing how such a quirk could be implemented in meson's internal dependency handling code, specifically proposing a modification to the `PCRE2PkgConfigDependency` class that would perform header verification on Darwin platforms. This demonstrates the solution could be pushed upstream to meson rather than requiring Git-specific workarounds.

The thread context shows this discussion follows resolution of the Windows fallback behavior, with macOS verification being the remaining platform-specific issue. Schwartz acknowledges Arenas' v4 patch incorporating these changes while maintaining the wrap fallback capability. The tone suggests the technical solution is now clear, though the broader question of whether to implement fixes in Git's build system or contribute them upstream to meson remains open.