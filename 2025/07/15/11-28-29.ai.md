Code cleanup in the ref-filter subsystem. This is part of the ongoing pagination for `git for-each-ref` series (v5 patch 4/5).

The patch removes an unnecessary `else` clause in `do_filter_refs()` since the preceding `if` clause calls `die()`, making the `else` redundant. This is a code style improvement suggested by Christian Couder in earlier reviews, now implemented by Karthik Nayak. The change reduces indentation levels and simplifies control flow while maintaining identical behavior.

The modification affects the ref filtering logic in `ref-filter.c`, specifically around how different ref types (branches, remotes, tags) are processed. The core functionality remains unchanged - this is purely a readability improvement. The patch follows the established pattern of the series where final polish is being applied to the codebase after the main pagination feature implementation.

Key technical details for future reference:
- Function modified: `do_filter_refs()` in `ref-filter.c`
- Removed redundant control flow structure after `die()` call
- Maintains all existing ref type handling (FILTER_REFS_BRANCHES, FILTER_REFS_REMOTES, etc.)
- Preserves HEAD ref special casing logic
- No behavioral changes, just code structure improvement

This represents one of the final cleanups in the series, with the thread showing consensus that the implementation is complete and well-reviewed.