This is a build system patch addressing PCRE2 dependency handling on macOS, marking the final resolution of the meson build system's PCRE2 detection issues. The patch implements the active detection approach suggested earlier in the thread, specifically handling for macOS's broken system PCRE2 installation where headers are missing despite the library being present.

The changes modify meson.build to verify header availability when PCRE2 is detected, adding a fallback to the built-in submodule when possible. The patch introduces three distinct behaviors: erroring out when PCRE2 is explicitly required but broken, falling back to the submodule when possible, or gracefully disabling PCRE2 with a warning in auto mode. The default setting in meson_options.txt is also changed from 'enabled' to 'auto' to match this new behavior.

Key technical details include the use of `compiler.has_header()` for verification, the three-way handling of enabled/auto/disabled states, and the preservation of static linking preferences. The solution maintains compatibility while providing clear user feedback about broken installations. This appears to be the final, consensus solution to the immediate build system issues discussed in the thread, with all technical points now addressed per the earlier discussion.