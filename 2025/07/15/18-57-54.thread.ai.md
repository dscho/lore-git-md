Thread identity: Test infrastructure patch addressing `GIT_TEST_INSTALLED` behavior regarding hash algorithm detection.

Core problem: The test suite was inconsistently handling `GIT_TEST_INSTALLED` when determining the default hash algorithm, causing test failures when the environment variable was set. The issue stemmed from different code paths using different methods to locate the Git binary - some respected `GIT_TEST_INSTALLED` while others always used `$GIT_BUILD_DIR/git`.

Technical details: The fix introduces a `GIT_BINARY` variable in `t/test-lib.sh` that properly accounts for both `GIT_TEST_INSTALLED` and build directory locations, plus platform-specific `$X` suffix (noted as unnecessary on Windows). This ensures consistent binary path usage between the initial build check and hash detection phases.

Status: Initial version of patch submitted. No reviews or discussion yet recorded. The change is limited to test infrastructure and doesn't affect core functionality. The commit message clearly explains the motivation from real-world test failures but doesn't specify which particular tests were affected.