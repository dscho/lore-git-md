Thread identity: Test infrastructure patch addressing `GIT_TEST_INSTALLED` behavior regarding hash algorithm detection, now with maintainer approval and secondary review confirmation.

Core problem: The test suite was inconsistently handling `GIT_TEST_INSTALLED` when determining the default hash algorithm, particularly problematic when building with `WITH_BREAKING_CHANGES` (which changes default hash from SHA1 to SHA256). Different code paths used different methods to locate the Git binary - some respected `GIT_TEST_INSTALLED` while others always used `$GIT_BUILD_DIR/git`, causing test failures.

Current status: Junio C Hamano has approved and queued the patch for inclusion. Justin Tobler's follow-up review confirms the technical correctness of the solution, noting it properly handles the hash detection case that originally revealed the issue. The change introduces a `GIT_BINARY` variable in `t/test-lib.sh` that properly accounts for both `GIT_TEST_INSTALLED` and build directory locations, ensuring consistent binary path usage between the initial build check and hash detection phases.

Technical details: The solution handles platform-specific `$X` suffix (noted as unnecessary on Windows) while maintaining test environment consistency. The patch specifically fixes cases where `git version --build-options` would query the build directory binary rather than the installed one specified by `GIT_TEST_INSTALLED`. The change is limited to test infrastructure in `t/test-lib.sh` and doesn't affect core functionality.

Participant positions: Junio C Hamano has reviewed and approved the approach, indicating no further discussion or modifications are needed. Justin Tobler provided additional technical validation of the solution, confirming it correctly handles the hash detection case and maintains consistency, with final approval ("Looks good to me"). The patch remains queued for the next integration cycle.

Related work: The issue became particularly visible when testing builds with `WITH_BREAKING_CHANGES` (SHA-256 as default hash), though the underlying inconsistency affected all test runs using `GIT_TEST_INSTALLED`.