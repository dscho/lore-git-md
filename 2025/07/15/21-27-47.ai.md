Maintainer ack for test infrastructure fix addressing hash algorithm detection consistency. This is a follow-up to the initial patch that fixed `GIT_TEST_INSTALLED` handling in the test suite.

Junio C Hamano approves the change to use the newly introduced `GIT_BINARY` variable consistently throughout `t/test-lib.sh`, particularly for detecting the default hash algorithm. The patch now ensures the same Git binary path (accounting for both `GIT_TEST_INSTALLED` and build directory locations) is used for both the initial binary check and hash detection phases.

The maintainer specifically praises the reuse of the previously detected binary path rather than hardcoding `$GIT_BUILD_DIR/git` for hash detection. This maintains consistency with the test environment setup while solving the original issue where different code paths used different methods to locate the Git binary.

The change is being queued for inclusion, indicating it has passed initial review and will be part of the next integration cycle. No further discussion or modifications appear necessary at this stage. The patch remains focused on test infrastructure without affecting core Git functionality.