This is a discussion about C99 compound literals in Git's codebase, continuing the thread started by Phillip Wood's test balloon patch. Patrick Steinhardt points out two existing uses of compound literals in the reftable subsystem (`REFTABLE_FLOCK_INIT` and `REFTABLE_TMPFILE_INIT` macros) that were added in Git 2.50. These serve as de facto test balloons, though they weren't explicitly marked as such.

The email raises the question of whether these existing uses should be retroactively designated as test balloons rather than introducing new ones, given they already demonstrate the pattern in production code. This adds a new dimension to the ongoing discussion about compiler compatibility and maintainability concerns raised by Junio Hamano earlier in the thread.

Key technical details include the specific structs involved (`reftable_flock` and `reftable_tmpfile` with their `fd` members initialized to -1) and their usage patterns in `reftable/stack.c`. The discussion now has concrete examples to evaluate regarding resource management concerns, as these structs do contain file descriptors that require cleanup.

This moves the conversation forward by providing real-world examples that both demonstrate the pattern's utility and potentially validate Junio's concerns about resource management. The thread remains open on whether to formalize guidelines around compound literal usage or maintain the status quo with these existing examples.