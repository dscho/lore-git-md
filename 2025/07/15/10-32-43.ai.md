This is a follow-up in the resolved meson build system discussion about PCRE2 dependency handling, specifically confirming the final behavior of the fallback mechanism.

Patrick Steinhardt confirms Carlo Arenas' understanding that the subprojects wrapper fallback remains active when PCRE2 is set to 'auto' in the meson configuration. This was the last remaining question about the implementation after the thread had already converged on using Eli Schwartz's active detection approach (checking headers via `compiler.has_header()`) to properly handle broken system PCRE2 installations on macOS.

The key technical point here is that with the default changed from 'enabled' to 'auto', the build system can now gracefully fall back when encountering broken PCRE2 installations while still maintaining the wrapper as a safety net. This confirmation closes the last minor uncertainty about the solution's behavior, leaving only the separate architectural questions about Git's regex handling (previously partitioned as future work) still open.

The thread has now fully resolved the immediate build system issue with all technical concerns addressed, including this final verification of fallback behavior. The changes are focused in meson.build (detection logic) and meson_options.txt (default value change), with the solution providing clear error messages when PCRE2 is explicitly enabled but unavailable while allowing graceful degradation in auto mode.