This is the first patch in a v5 series exposing the `ref_iterator` API to enable advanced ref iteration patterns, particularly for implementing pagination in `git for-each-ref`. The patch moves the `ref_iterator` struct and related functions from `refs-internal.h` to the public `refs.h` header, along with comprehensive documentation explaining iterator usage patterns.

Key technical details for future reference:
- Introduces public API for `ref_iterator` with functions like `ref_iterator_advance()`, `ref_iterator_seek()`, and `ref_iterator_peel()`
- Documents iterator lifecycle (creation -> advance/free) and proper usage patterns
- Preserves all existing flags (`DO_FOR_EACH_INCLUDE_BROKEN`, `DO_FOR_EACH_PER_WORKTREE_ONLY`, etc.) while making them public
- Includes detailed comments explaining seek behavior (prefix matching, iterator reset semantics)
- Maintains backward compatibility by keeping identical function signatures
- Sets up infrastructure for subsequent pagination patches by exposing seek functionality

The patch represents a significant API surface expansion, enabling callers to implement custom iteration patterns without requiring new wrapper functions for each combination of options. This is particularly important for the pagination feature being developed in later patches, which needs precise control over iteration state.

The implementation appears thorough, with careful documentation and no behavioral changes - just moving and exposing existing well-tested functionality. The extensive comments suggest this API is meant to be long-term stable, with clear guidance on proper usage patterns.