This is a technical discussion about the meson build system's PCRE2 dependency handling, specifically addressing a Windows regression concern. 

Eli Schwartz clarifies a point about meson's fallback behavior when PCRE2 is set to "auto" mode, correcting Carlo Arenas' earlier misunderstanding about wrap fallbacks. The key insight is that meson will only fall back to the wrap (subproject) for *required* dependencies by default, not for optional "auto" mode dependencies. 

Eli points to meson's `dependency_allow_fallback` documentation, suggesting it could be used to implement the desired behavior where "auto" would attempt to enable PCRE2 if either system detection succeeds or if a wrap fallback is available (with internet access or local download). This provides a potential solution path for the Windows regression concern raised earlier in the thread.

The email advances the discussion by offering concrete technical details about meson's behavior while keeping focus on the immediate build system issue, separate from the broader regex architecture questions noted in the thread summary.