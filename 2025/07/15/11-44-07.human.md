This is a build system patch addressing PCRE2 dependency handling on macOS, marking the final resolution of a thread about broken system PCRE2 installations. The patch implements the active detection approach suggested earlier in the thread, now in its fourth iteration.

The change specifically targets macOS systems where the base PCRE2 library is present but unusable due to missing headers. The new logic first checks for the problematic macOS case by verifying header availability, then provides two fallback paths: if PCRE2 was explicitly enabled, it attempts to use a built-in submodule; if in auto mode (now the default), it gracefully disables the feature with a warning. The solution maintains static linking preferences while improving error messaging.

This appears to be a well-considered solution to a platform-specific build issue, with clear handling of different configuration scenarios (enabled/auto modes) and proper error propagation. The patch follows through on the technical approach agreed upon in the thread, incorporating Eli Schwartz's suggestion for active detection. With this change, the immediate build system issues around PCRE2 appear fully resolved, leaving broader architectural questions about Git's regex handling for future work.