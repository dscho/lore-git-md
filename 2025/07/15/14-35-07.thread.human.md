René Scharfe has proposed a performance optimization that refactors Git's commit traversal to use priority queues, significantly improving worst-case performance (92% faster for pathological histories) while maintaining normal-case speeds. The 3-patch series converts the commit list handling to use a priority queue for O(log n) operations, adds a helper function to optimize common get+put pattern, and recovers the original best-case behavior.

The series has undergone thorough review from key contributors. Junio Hamano validated the performance measurements, Justin Tobler confirmed the technical approach, and Jeff King (Peff) provided detailed analysis showing the changes align with his own unpublished work in this area. Peff's latest contribution precisely characterized the optimization's impact and confirmed it maintains proper ordering semantics for commits with identical timestamps (using insertion order as a tie-breaker, matching the original behavior).

Scharfe's follow-up email confirms this analysis and suggests adding unit tests to explicitly verify the stable ordering property. With all technical questions now resolved, the series appears ready for merging, pending potential test additions to codify the stability guarantees. The optimization particularly benefits operations like rev-parse with ":/" patterns and fetch's mark_recent_complete_commits() when dealing with repositories containing many merges or references.