René Scharfe has proposed a series optimizing Git's commit traversal by converting from linked lists to priority queues (prio_queue), significantly improving worst-case performance for pathological histories (92% faster) while maintaining normal-case speeds. The three-part series first introduces the priority queue approach, then adds a helper to optimize common operations, and finally applies this to recover initial performance in typical cases.

Key reviewers Jeff King (Peff) and Justin Tobler validated the technical approach, with King confirming his own unpublished work followed similar lines. Junio Hamano oversaw the discussion and verified the performance measurements. The optimization particularly benefits operations like `:/` rev-parse and fetch operations with many references, where the change from quadratic to log-linear complexity matters most.

The final technical question - about how the implementation handles commits with identical timestamps - has now been resolved. King confirmed the prio_queue maintains insertion order as a tie-breaker, matching Git's existing behavior. With this settled, the series appears technically complete. Scharfe may add some additional unit tests to codify the stability guarantees, but there are no remaining open questions about the core implementation. The changes span several core components including commit handling and fetch operations, with comprehensive performance tests showing the improvements. This looks ready to be picked up for integration.