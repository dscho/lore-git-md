This is a platform compatibility patch that adds support for the `__BYTE_ORDER__` macro in Git's byte-swapping utilities. It's part of a 5-patch series (position 1/5) though no cover letter context is provided.

The change makes `compat/bswap.h` recognize compiler-provided endianness macros (`__BYTE_ORDER__` and related defines) as a fallback when the traditional libc macros (`BYTE_ORDER` etc.) aren't available. This specifically helps on platforms like mingw where the libc macros may be missing. The patch notes these macros have been available in major compilers (gcc since ~4.6, clang since ~3.2, icc since ~16.0.3).

The implementation adds a new conditional block that maps the compiler's endianness macros to Git's internal `GIT_*` versions, maintaining the same interface while expanding platform support. The change is straightforward and appears well-scoped, touching only the compatibility header with 6 lines added.

This seems like a reasonable platform compatibility improvement, though the commit message could benefit from mentioning any specific issues encountered on mingw or other platforms that motivated the change. The patch follows Git's existing pattern for endianness handling and doesn't appear to introduce any behavior changes for platforms that already worked.