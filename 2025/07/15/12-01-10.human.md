This is a follow-up in the completed meson build system thread about PCRE2 dependency handling on macOS. Carlo Arenas confirms that Eli Schwartz's proposed solution using `compiler.has_header()` works correctly to detect broken system PCRE2 installations, marking the final verification needed for this fix. 

Arenas notes the solution is simpler than expected, suggesting future improvements to meson's `dependency()` function could incorporate similar header validation logic internally. He mentions posting a v4 patch that incorporates Schwartz's code while maintaining the fallback-to-submodule behavior when possible.

The exchange serves as final confirmation that the technical solution is sound, wrapping up this thread's immediate concerns about PCRE2 detection. The broader discussion about Git's regex handling remains as future work, but the specific macOS build issue is now resolved with consensus.