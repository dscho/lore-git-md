This is a follow-up clarification to patch 3/8 in the MIDX refactoring series, responding to Justin Tobler's review feedback about the `close_midx()` behavior changes. The original patch transitioned MIDX cleanup from global linked-list traversal to per-source handling, eliminating recursive cleanup via `m->next` in favor of direct source iteration.

Justin pointed out two areas needing clarification in the commit message: (1) that `close_midx()` itself no longer iterates through sources (the responsibility now lies with callers), and (2) that existing `close_midx()` callsites not handling global cleanup remain unchanged but are safe since they don't require global MIDX closure. Patrick Steinhardt acknowledges both points and agrees to add explanatory notes to the commit message.

This exchange confirms the technical correctness of the per-source iteration pattern while improving documentation of the behavioral change. The response maintains the series' careful attention to transitioning global state handling while preserving existing functionality where needed. No new technical concerns are raised - this is purely about improving the explanation of already-implemented behavior changes.