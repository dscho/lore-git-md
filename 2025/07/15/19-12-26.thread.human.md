This thread introduces a 5-patch series to improve platform compatibility in Git's byte-swapping utilities. The first patch, from an unnamed contributor, adds support for compiler-provided `__BYTE_ORDER__` macros as a fallback when traditional libc endianness definitions are unavailable. This change specifically helps on platforms like mingw where these macros may be missing, while maintaining the same interface for all other platforms.

The implementation is clean and focused, adding just 6 lines to `compat/bswap.h` to map compiler macros to Git's internal definitions. The change appears safe since it only expands support without altering behavior on platforms. While the commit message could be strengthened by citing specific platform issues that motivated the change, the technical approach follows Git's established patterns for endianness handling.

As this is the first patch in the series, we can expect subsequent patches to build on this foundation with related platform compatibility improvements. The thread is just starting, so we'll need to see the remaining patches and any review feedback to understand the full scope of the changes.