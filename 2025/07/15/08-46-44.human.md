Build system patch discussion on PCRE2 dependency handling. Patrick Steinhardt responds to Eli Schwartz's technical proposal for improving PCRE2 detection on macOS in the meson build system. The key points:

Eli suggested replacing the current platform-specific disable logic with active verification of PCRE2 headers using `compiler.has_header()`, which would properly detect broken system installations on macOS. Patrick agrees this is a better approach than the current workaround, noting it makes sense to error out when PCRE2 is explicitly required but broken, while gracefully falling back with a warning otherwise.

The discussion also touches on the default setting for PCRE2 - they agree it should be "auto" rather than "enabled" since PCRE2 cannot be expected to be universally available. Patrick acknowledges his initial "enabled" default was likely influenced by the presence of a bundled PCRE2 wrapper in subprojects/.

This email advances the technical discussion by accepting Eli's proposed solution for proper dependency verification while maintaining alignment on the broader configuration approach. The exchange demonstrates progress toward a more robust solution for the macOS PCRE2 issue, moving beyond platform-specific workarounds to active capability testing.