This is a follow-up discussion on Øystein Walle's bugfix for interactive rebase state cleanup when the instruction format is invalid. Phillip Wood acknowledges the patch's correctness but points out it doesn't address the parallel issue when `--autostash` is used, since that option creates the state directory earlier in the process. The technical observation is that while the current patch moves state initialization after format validation, fixing the autostash case would require a different approach and would make this specific change redundant.

The discussion confirms the patch is still mergeable as-is (already acked by Junio Hamano) since it improves the non-autostash case without blocking future work. Phillip notes the solution doesn't make future fixes harder, though solving the autostash case would supersede it. The thread shows consensus on merging this incremental improvement while leaving the autostash edge case for future work.

Key technical details for future reference: the fix reorders operations in `do_interactive_rebase()` to validate formats via `sequencer_make_script()` before `init_basic_state()`, with test coverage in t3415. The autostash path remains problematic because it initializes state earlier in `cmd_rebase()`. No changes to the patch are expected before merging.