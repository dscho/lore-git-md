This is a code review in the ongoing `git repo info` implementation series, focusing on the field callback architecture. Patrick Steinhardt provides feedback on patch 2/5 which adds the `references.format` field.

The review identifies two key architectural limitations in the current implementation: memory management for allocated strings and error handling. Patrick suggests changing the callback signature to use a `struct strbuf` parameter instead of returning a `const char*`, which would allow proper memory management and error returns. He provides a concrete example of how the `get_references_format` function would look with this change.

Additionally, Patrick notes a minor style nit about using `sizeof(*repo_info_fields)` instead of explicitly naming the struct type in the bsearch call, which makes the code more maintainable.

This feedback aligns with the thread's earlier discussions about evolving the field system architecture, particularly the planned move to `struct strbuf` in v6. The review doesn't raise any fundamental objections to the patch's functionality but helps guide the implementation toward being more robust and future-proof.