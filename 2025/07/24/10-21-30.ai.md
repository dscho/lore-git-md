Refinement of reflog migration infrastructure focusing on REF_HAVE_OLD flag behavior.

Karthik Nayak reviews Patrick Steinhardt's patch that stops unsetting REF_HAVE_OLD for log-only updates in the reflog migration code. The key change preserves the REF_HAVE_OLD flag during reflog operations, ensuring proper old_oid propagation. The review confirms the technical correctness while suggesting minor readability improvements to `ref_update_expects_existing_old_ref()` and noting a documentation opportunity.

The patch modifies several key locations:
1. Removes the REF_HAVE_OLD unsetting in `ref_transaction_update_reflog()`
2. Updates the `ref_update_expects_existing_old_ref()` logic to account for REF_LOG_ONLY
3. Adjusts flag checks in files-backend.c and reftable-backend.c to properly handle the combined REF_LOG_ONLY and REF_HAVE_OLD cases

Nayak proposes an alternative implementation for better readability but approves the current version. He also identifies a stale comment about symbolic ref updates that could be simplified and catches a documentation gap regarding how the function interacts with reflog-only entries. The review confirms the technical soundness of the approach while highlighting opportunities for improved code clarity and documentation.