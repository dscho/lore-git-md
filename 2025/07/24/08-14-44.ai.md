Documentation patch clarifying `--start-after` restrictions in `git-for-each-ref`. This is part 3 of Karthik Nayak's 5-patch cleanup series following their earlier `--start-after` feature addition.

The patch improves the documentation for the `--start-after` option by replacing the vague "general pattern matching" phrase with specific incompatibilities: the option cannot be used with `--sort`, `--stdin`, or pattern arguments. This makes the limitation clearer for users. The change also fixes a typo in the option string description in the code (correcting "start-start" to "start-after").

The patch builds on the thread's established context of refining the `--start-after` implementation and documentation. It addresses a specific nit from prior review by making the documentation more precise about usage constraints. The changes are straightforward and uncontroversial, with Junio C Hamano having already provided a Helped-by tag indicating review approval.