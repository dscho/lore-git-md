This is a test patch in Karthik Nayak's series cleaning up the `--start-after` functionality in `git for-each-ref`. While the documentation already notes incompatibilities for this new flag, this patch proactively adds a test combining `--start-after` with `--exclude` to ensure they work together properly. 

The test creates a scenario where references are filtered to start after `refs/odd/spot` while also excluding tags matching `refs/tags/foo`, verifying the expected output. This is a straightforward quality-of-life addition that helps prevent future regressions in these interacting features.

The patch is small and focused, following the pattern of the series which has been receiving positive review feedback. It doesn't introduce any new functionality but strengthens the test coverage around the recently added `--start-after` option.