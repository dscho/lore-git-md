Test modernization: conversion of reftable readwrite tests to Clar framework. This is part 2/5 in Seyi Kuforiji's series converting reftable unit tests from custom test-lib.h assertions to the Clar testing framework, following Patrick Steinhardt's broader effort to standardize reftable testing.

The patch mechanically converts t-reftable-readwrite.c to u-reftable-readwrite.c, replacing test-lib.h assertions with Clar equivalents while preserving all test logic. The changes include updating Makefile and meson.build to remove the old test file and add the new one. The diff shows 68% similarity in test logic despite the framework change, with line count reductions from Clar's more concise syntax.

Key technical details: The conversion touches test infrastructure files and updates build systems to reflect the transition. Test helpers like cl_reftable_write_to_buf replace their test-lib.h counterparts while maintaining reftable-specific details. The tests cover buffer operations, log compression, overflow handling, write limits, sequential reads, seek operations, index behavior, and corruption detection. Build system changes correctly place test file additions in their logical commits, addressing earlier feedback about commit organization.

This appears to be a straightforward mechanical conversion with no behavior changes, following the established pattern from previous conversions in the series. The patch maintains all existing test coverage while modernizing the infrastructure. The changes are non-controversial and align with the project's testing direction.