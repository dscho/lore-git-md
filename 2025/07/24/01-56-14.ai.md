This is a clarification in the ongoing discussion about `git diff --ignore-matching-lines` behavior with metadata outputs. Lidong Yan responds to Junio Hamano's explanation that content-ignoring options like `--ignore-matching-lines` are intentionally excluded from affecting metadata-focused outputs (`--name-only`, `--raw`, etc.) for performance and consistency reasons.

The email confirms understanding of the design principle: `--ignore-matching-lines` suppresses diff output for matching changes but doesn't affect whether a file is considered modified at the metadata level. This maintains the invariant that answers to "was this path modified?" don't depend on content-ignoring patterns.

Key technical points reinforced:
- Content-ignoring options only affect diff output presentation, not modification status
- Metadata outputs deliberately avoid content examination for performance
- The behavior is intentional design, not a bug

The thread appears to be reaching consensus that documentation should clarify this interaction rather than changing behavior. Lidong's response suggests acceptance of the design rationale while seeking confirmation of their understanding. No new technical arguments or proposals are introduced in this message.