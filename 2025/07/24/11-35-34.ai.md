This is a follow-up discussion in Patrick Steinhardt's reflog migration infrastructure series, specifically addressing the behavior of REF_HAVE_OLD flag during log-only updates. The thread has already established technical consensus on preserving REF_HAVE_OLD for log-only operations to maintain correct old_oid propagation while preventing races.

Karthik Nayak suggested two documentation improvements: updating the function comment for `ref_update_expects_existing_old_ref()` to clarify its behavior with reflog-only entries, and proposed an alternative implementation that separates the REF_LOG_ONLY check into a distinct conditional for better readability. Patrick Steinhardt agrees with both suggestions, particularly favoring the more straightforward conditional structure proposed by Nayak.

The changes discussed would affect the refs.c file, specifically around the `ref_update_expects_existing_old_ref()` function which handles reference update expectations. The key technical point is maintaining the invariant that reflog-only entries (marked with REF_LOG_ONLY) shouldn't expect the reference to exist, while properly preserving the REF_HAVE_OLD flag's race prevention semantics.

This email represents final polish work on an otherwise resolved technical issue, with both parties agreeing on the remaining documentation and code clarity improvements needed before the series can be considered complete. The discussion remains focused on non-controversial refinements to an already-approved approach.