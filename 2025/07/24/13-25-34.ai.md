This is a discussion email in the ongoing refactoring series to move sparse-checkout configuration from globals to `repo_settings`, part of the `the_repository` removal effort.

Derrick Stolee weighs in on Junio Hamano's architectural concerns about `prepare_repo_settings()` call placement, proposing two alternative approaches. He suggests either (1) moving sparse-checkout configs directly into `struct repository` (his preferred solution) or (2) changing `repo_settings` to be initialized earlier as core repo behavior rather than optional flags. Stolee considers the current shared-settings use case for `repo_settings` to be weak justification for approach (2).

The email echoes Junio's earlier question about why these settings belong in `repo_settings` rather than `struct repository`, reinforcing that this remains an open design decision. Stolee's intervention elevates the discussion by framing the choice as between fundamentally different architectural approaches rather than just call placement optimization.

Key technical points raised:
- Questions whether `repo_settings` was meant for core functionality vs optional flags
- Suggests moving configs to `struct repository` would avoid scattered `prepare_repo_settings()` calls
- Notes this would require significant mechanical changes to both current and follow-up series
- Implicitly rejects performance concerns as primary motivation for current approach

This represents a substantive new perspective in the thread that could significantly redirect the series' approach if adopted. The core tension remains between maintaining the current `repo_settings` pattern versus treating sparse-checkout as more fundamental repository state.