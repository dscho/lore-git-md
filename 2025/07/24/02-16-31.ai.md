This is a documentation-style follow-up in the ongoing thread about `git diff --ignore-matching-lines` behavior with `--name-only`. Eric Sunshine provides a minor correction to Lidong Yan's commit reference format, suggesting a simpler `git log` invocation using the built-in `--format=reference` option rather than manually constructing the output format.

The email doesn't introduce new technical discussion about the core issue, which has already been resolved with consensus that the current behavior is intentional. Instead, it focuses on proper commit citation style, demonstrating the `reference` format which produces standardized output like "commit 296d4a94e7 (diff: add --ignore-matching-lines option, 2020-10-20)".

Key technical detail for future reference is the `--format=reference` log option, which provides a compact, standardized way to cite commits in documentation and discussions. The thread context shows this is purely a style suggestion unrelated to the original technical discussion about diff behavior.