This is a technical refinement in Patrick Steinhardt's reflog migration series, specifically addressing the `REF_HAVE_OLD` flag behavior during log-only updates. The patch clarifies that while unsetting of `REF_HAVE_OLD` was originally sensible to avoid OID verification during reflog writes, keeping it provides useful signaling about caller-provided OIDs. 

The discussion reveals this patch serves primarily as groundwork for a subsequent safety check that will require both `REF_HAVE_OLD` and `REF_HAVE_NEW` when `REF_LOG_USE_PROVIDED_OIDS` is set. Patrick confirms the current version isn't actually sufficient to record old OIDs as initially suggested in the commit message, which he plans to rewrite.

A notable side discussion clarifies the actual flag combinations used during reflog migration: `REF_LOG_ONLY` (to skip ref updates) works alongside several other flags including the new `REF_LOG_USE_PROVIDED_OIDS`. Patrick acknowledges the flag system has grown complex but found no simpler alternative without side effects. The thread shows consensus on the technical approach, with only documentation polish remaining before this series can conclude.