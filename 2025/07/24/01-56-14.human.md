This is a clarification in the ongoing discussion about `git diff`'s `--ignore-matching-lines` behavior with metadata outputs like `--name-only`. Lidong Yan responds to Junio Hamano's explanation that this is intentional design rather than a bug.

The email confirms understanding of the core principle: content-ignoring options like `--ignore-matching-lines` are meant to suppress diff output while still considering files changed for metadata purposes. Yan acknowledges this distinction - the option hides matching changes from the diff display but doesn't affect whether a file is reported as modified in `--name-only` output.

The exchange helps solidify the thread's consensus that the current behavior is correct, with Yan now clearly understanding the separation between content examination (affected by ignore patterns) and metadata reporting (unaffected by them). This moves the discussion toward potential documentation improvements rather than behavior changes.