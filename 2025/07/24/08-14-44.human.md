Documentation patch clarifying `--start-after` behavior in `git for-each-ref`. This is part of Karthik Nayak's ongoing cleanup series following the addition of the `--start-after` flag.

The patch improves the documentation by replacing the vague phrase "general pattern matching" with specific details about incompatible options: `--sort=<key>`, `--stdin`, and the `<pattern>` arguments. This makes it clearer to users exactly when they can and cannot use `--start-after`. The change was suggested by Junio C Hamano.

Additionally, the patch fixes a typo in the option string for `--start-after` in the code, correcting "start-start" to "start-after". This is a straightforward documentation improvement that makes the command's behavior more precise and understandable.