This is a documentation clarification in the ongoing reflog migration infrastructure series. Toon Claes points out an ambiguity in the comment for `ref_transaction_update_reflog()` regarding how the `index` field is handled when ordering updates. The current comment states updates are ordered by refname when the index is "not used", but Toon notes this is specifically when the index is zero, as seen in `transaction_update_cmp()` in the reftable backend.

The suggestion is straightforward - replace "not used" with "zero" in the function's documentation comment to be more precise about the index field's behavior. This is a minor polish item in the larger reflog migration work, which has already addressed more substantial issues around flag handling and identity management. The thread shows consensus on the technical approach, with participants now focusing on final documentation nits like this one.