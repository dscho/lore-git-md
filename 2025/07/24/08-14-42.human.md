Refactoring patch fixing a type mismatch in ref-cache.c. This is the first of five patches in a series cleaning up small issues in the ref-filter code following the recent `--start-after` feature addition.

The patch changes an `int` variable used for string length comparison to `size_t`, which is the proper type for this purpose. The original code was potentially problematic as it could truncate large string lengths when casting from `size_t` (returned by `strlen`) to `int`. The change is straightforward and clearly motivated by both type correctness and avoiding potential integer overflow issues.

The patch builds on commit 090eb5336c) which added seek functionality to the ref-cache iterator. While this is a small change, it's part of a thoughtful cleanup series addressing specific nits from prior review. The diff shows the simple but important type correction along with the removal of an unnecessary cast.