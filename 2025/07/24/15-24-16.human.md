This is a patch series addressing submodule handling, with both bugfix and documentation components. The changes focus on preventing accidental overwrites of `.gitmodules` and optimizing submodule activation logic.

The first patch prevents `.gitmodules` from being overwritten when reusing paths, which could lead to data loss. The second patch skips redundant submodule activations when a pathspec already covers the module, improving efficiency. Both changes come with corresponding test updates to verify the behavior.

The author has iterated on the implementation based on feedback, cleaning up whitespace, improving test assertions (using `test_must_fail` instead of `! git config`), and maintaining consistency with existing force logic. Documentation updates accompany the behavioral changes to clarify the `--force` option's implications.

The patches touch core submodule functionality in `builtin/submodule--helper.c` and include tests across multiple test files, suggesting careful consideration of edge cases. The changes appear focused and well-motivated, addressing specific pain points in submodule management.