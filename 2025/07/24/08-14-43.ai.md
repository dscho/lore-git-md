Documentation patch improving `git-for-each-ref` synopsis formatting. This is part of Karthik Nayak's ongoing series cleaning up minor issues around the recently added `--start-after` functionality.

The patch makes two documentation improvements to the command's synopsis section. First, it adds parentheses around `--exclude=<pattern>` to properly indicate this option can be repeated, matching the actual implementation's support for multiple exclude patterns. Second, it moves the `--stdin | <pattern>...` alternative to the end of the option list, since patterns are positional arguments that should follow all flags.

These changes don't affect functionality but improve the documentation by more accurately representing the command's actual syntax rules. The modifications align with standard documentation practices for showing repeatable options and proper argument ordering. As with other patches in this series, this addresses a minor nit from prior review feedback.