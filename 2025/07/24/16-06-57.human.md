This is a discussion thread about the output format design for the new `git repo info` command, specifically focusing on field separators and quoting.

Junio Hamano and Patrick Steinhardt are discussing the tradeoffs between LF-separated and NUL-terminated output formats for the new command. Patrick explains that while NUL is better for scripts, LF separation is more human-readable for ad-hoc inspection. The conversation reveals that the thread had previously focused more on machine parsing than human usability.

The key point under debate is whether to implement quoting for LF-separated output when field values might contain newlines. Patrick suggests it wouldn't be hard to add `quote_c_style()` handling when needed, but the exact approach isn't settled. Junio seeks clarification on what "the right thing" would be - whether to use LF with quoting when necessary, or some other solution.

This exchange shows the command's output format design is still being refined, with attention now turning to making it both script-friendly and human-friendly. The discussion builds on previous decisions to rename the NUL format and implement proper string handling via strbuf.