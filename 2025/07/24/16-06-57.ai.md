This is a discussion thread about output format design for the new `git repo` command's info subcommand, specifically focusing on field separators and quoting mechanisms.

The email captures Junio Hamano's follow-up questions to Patrick Steinhardt's earlier review comments about the human-readable output format. The key points under discussion are:

1. **Human vs machine consumption**: Patrick argues that while scripts should use NUL-delimited format (`--format=nul`), human readers benefit from LF-separated output for ad-hoc inspection. Junio questions whether this justifies maintaining two formats given potential edge cases.

2. **Quoting requirements**: Patrick suggests simple `quote_c_style()` handling for LF-separated output when newlines appear in field values, acknowledging this is primarily for human readability rather than script robustness. Junio seeks clarification on whether this should be the standard approach.

3. **Format robustness**: The discussion touches on whether LF-separated output needs full quoting support given its human-oriented purpose, versus the stricter requirements for machine-readable NUL-delimited output.

This exchange occurs after the thread has already settled on renaming the machine format from `null` to `nul` and adopting `struct strbuf` for field callbacks. The conversation now focuses on finalizing the human-readable output design before implementation proceeds to v6. The technical direction appears stable, with only minor syntax details remaining to be resolved.