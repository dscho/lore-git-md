Here's the updated human-readable summary:

This thread discusses improving `git rebase`'s feedback when commits are silently skipped during conflict resolution because all changes were removed. The original proposal highlighted how this silent behavior can lead to unintended history modifications going unnoticed.

Junio Hamano has now responded with a concrete but untested implementation approach - adding a two-line warning message ("omitting a step that has become empty") in sequencer.c when such skips occur. While this addresses the core issue, several open questions remain about the right verbosity level and whether the warning might trigger in inappropriate contexts. Hamano is specifically seeking input from recent contributors to sequencer.c to help refine the approach.

The discussion has progressed from identifying the UX problem to exploring specific implementation details, with the shared goal of making rebase behavior more transparent when users resolve conflicts by removing all changes. The next steps will likely involve testing the proposed warning and gathering feedback on its wording and triggering conditions.