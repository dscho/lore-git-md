This thread discusses a proposed enhancement to `git rebase` that would warn users when commits are silently skipped during conflict resolution because they become empty after changes. The core challenge is distinguishing legitimate skips (like those from `--skip` or `break` commands) from cases where commits disappear unexpectedly.

The discussion has reached consensus on the warning format - it will reuse Git's existing "skipped previously applied commit" message pattern. Phillip Wood contributed a key guard condition to prevent false positives by checking for `rebase_path_message()`. However, a new debate has surfaced between Junio Hamano's preference for strict detection (only warning on confirmed already-applied commits) and Cameron Steffen's broader approach (warning on any skip during conflict resolution with no staged changes).

The latest email from Junio raises important questions about whether the proposed warning location in `commit_staged_changes()` would actually be reached during `--skip` operations, suggesting the implementation needs careful validation of the sequencer's control flow. While Phillip Wood's earlier technical suggestions are acknowledged as sound, Junio notes he's reasoning about unfamiliar code paths, indicating this area may need deeper review from rebase experts.

The thread shows steady progress on the warning mechanism's design while surfacing important implementation challenges around the sequencer's state machine. The participants - Cameron Steffen (proposer), Junio Hamano, and Phillip Wood - continue to refine the approach, with the current focus being on ensuring reliable behavior across all rebase operation pathways before finalizing the change.