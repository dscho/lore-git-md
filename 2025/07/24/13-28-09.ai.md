Test modernization: conversion of reftable table tests to Clar framework. This is part 1/5 in Seyi Kuforiji's v5 series that completes the reftable test migration from custom test-lib.h to Clar. The patch converts t-reftable-table.c to u-reftable-table.c, updating Makefile and meson.build to reflect the change. 

The conversion is mechanical, replacing test-lib.h assertions with Clar equivalents (check() becomes cl_assert(), check_int() becomes cl_assert_equal_i(), etc.) while preserving all test logic. The patch maintains three key test scenarios: table seek operations, repeated reseeks, and block iteration. Build system changes remove the old test file from UNIT_TEST_PROGRAMS and add the new one to CLAR_TEST_SUITES. 

Technical details: The conversion touches reftable table operations including iterator initialization, seeking, and block traversal. Test helpers like cl_reftable_write_to_buf replace their test-lib.h counterparts. The diff shows 75% similarity in the test logic despite the framework change, with line count reductions from Clar's more concise syntax. Build system updates are minimal but necessary for the transition. 

This appears to be a straightforward mechanical conversion with no behavior changes, following the established pattern from earlier patches in the series. The patch correctly handles reftable-specific details like record comparison with REFTABLE_HASH_SIZE_SHA1 and proper resource cleanup. The only minor issue is a missing newline at EOF in the Makefile diff, which appears to be a carryover from previous iterations.