This is a discussion thread about refactoring MIDX tracking to use per-source storage rather than global state, part of Patrick Steinhardt's ongoing series to prepare for pluggable backends. Taylor Blau raises several questions about the first patch in the series, which introduces per-source MIDX tracking.

Taylor seeks clarification on the distinction between "object database" and "source" in the context of alternates, wondering whether alternates should be considered separate sources or part of a single database. He also questions whether the `local` parameter in `prepare_multi_pack_index_one()` should be a property of `struct odb_source` rather than passed as a parameter.

The email notes some stylistic concerns about mixing functional changes with style cleanups (like adding braces to if/else blocks) and suggests the field name `multi_pack_index` might be better abbreviated as `midx`. Taylor also points out that some header file changes include unrelated declaration reordering that makes the actual changes harder to spot.

These are thoughtful review comments on an architectural change, showing engagement with the series while seeking to understand the design choices before the refactoring proceeds further. The questions indicate areas where the commit message or code comments might benefit from additional explanation about the object database/source relationship.