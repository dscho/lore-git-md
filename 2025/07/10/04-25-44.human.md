This is a platform compatibility discussion about Windows signal handling in the context of Carlo Marcelo Arenas Belón's stalled SIGCHLD handling series.

The email continues a technical discussion about Windows signal number behavior, specifically around SIGCHLD. Carlo responds to Johannes Sixt's skepticism about the relevance of signal numbers by pointing to historical context from commit d28250654f, which changed SIGCHLD's value from 0 to 17 on Windows. He observes that using 17 causes silent failures in `signal()` calls, while using 20 (as Cygwin/macOS do) would properly set errno to EINVAL.

The exchange highlights a key sticking point in Carlo's series to replace `signal()` with `sigaction()` in `git daemon`. While Johannes maintains that signals are largely irrelevant in native Windows outside Cygwin, Carlo probes whether changing the SIGCHLD number to 20 would be acceptable as it would at least provide consistent error behavior. This represents an attempt to unblock the stalled series by addressing one of the Windows-specific concerns raised in reviews.