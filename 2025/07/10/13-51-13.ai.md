Performance optimization patch in the bloom filter pathspec series, specifically refactoring the pathspec-to-bloom-key conversion logic. This is a refinement patch in Derrick Stolee's follow-up to Lidong Yan's main series.

The patch extracts the pathspec-to-bloom-key conversion code from `prepare_to_use_bloom_filter()` into a new helper function `convert_pathspec_to_filter()`. This change:
1. Isolates the path normalization logic (handling trailing slashes) and bloom key creation
2. Simplifies the control flow in the main function by moving error handling into the helper
3. Prepares for the subsequent change that will handle multiple pathspecs by making the single-pathspec case cleaner
4. Maintains all existing functionality while making the code more modular

The refactoring touches only revision.c, focusing on the bloom filter preparation code. This is a pure code organization change with no behavior modification - the same pathspec processing and bloom key creation happens, just in a more structured way. The patch follows the series' established conventions and appears to be a straightforward improvement to the code structure before the multiple-pathspexec optimization is implemented.

Key technical details:
- New `convert_pathspec_to_filter()` helper takes pathspec item, bloom_keyvec pointer, and settings
- Handles path normalization (trailing slash removal) and empty path checking
- Returns 0 on success, -1 on failure (empty path)
- Caller remains responsible for memory cleanup of the bloom_keyvec array
- Preserves all existing error handling and filter disablement paths

This appears to be a clean, focused refactoring that improves code organization without introducing new complexity. It's part of the final polish for the bloom filter optimization series that has already undergone extensive review.