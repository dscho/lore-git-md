This is a follow-up to the `git last-modified` command series, specifically addressing an upcoming Bloom filter API change that will affect the implementation. Junio C Hamano notes that while the current series is technically complete and doesn't need immediate changes, there's an in-flight topic that will rename Bloom filter API functions from `clear_bloom_key`/`fill_bloom_key` to `bloom_key_clear`/`bloom_key_fill` and change their parameter order. 

The email includes a 3-line diff that shows how the `builtin/last-modified.c` implementation would need to be updated when rebased on top of the upcoming changes. The modifications are straightforward - adjusting function calls to match the new names and parameter ordering (moving the key structure to be the first parameter). 

Junio suggests keeping the topics independent for now since the API changes aren't urgent, but provides the necessary adjustment as a reference for when the rebase eventually happens. This maintains the series' current state while documenting the trivial conflict resolution that will be needed in the future. The changes don't substantive - they're purely mechanical updates to match the evolving Bloom filter interface.