Refactoring patch in a performance optimization series for pathspec bloom filters. This is patch 2/4 in Lidong Yan's v5 series addressing code style consistency for bloom key operations.

The patch renames two functions to follow Git's naming style where functions operating on a struct should be named `structname_verb()`. Specifically:
- `fill_bloom_key()` becomes `bloom_key_fill()`
- `clear_bloom_key()` becomes `bloom_key_clear()`

The changes are mechanical but touch several files (bloom.c/h, blame.c, line-log.c, revision.c, and test-bloom.c) where these functions are used. The patch maintains all existing functionality while making the API naming more consistent with Git conventions.

This appears to be a straightforward cleanup that was likely suggested during review of the larger bloom filter optimization series. The changes are well-contained and the patch follows the standard Git contribution process with a clear commit message explaining the rationale.