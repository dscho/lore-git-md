Git commit verbose scissor-line bug report with VS Code editor.

Bug report describes unexpected behavior in `git commit --verbose` when VS Code as editor (`core.editor = code --wait`), where text below scissor-line marker (`# ------------------------ >8 ------------------------`) incorrectly appears in final commit message. Original reproduction case used Git 2.50.1 on macOS ARM64 with custom VS Code configuration.

Junio C Hamano verified core Git functionality works correctly with standard editors, confirming proper scissor-line handling in test methodology using `git checkout --detach HEAD` and `git reset --soft HEAD~23`. Eric Sunshine extended investigation by testing with default VS Code settings on Linux VM, also unable to reproduce the issue. This suggests the problem is specific to either:
1. The original reporter's VS Code configuration (non-default settings)
2. macOS ARM64 platform behavior interacting with VS Code
3. Some combination of environment factors

Current status indicates Git's scissor-line processing works as designed when tested with:
- Standard non-VS Code editors (Junio)
- Default-configured VS Code (Eric)
Only fails in original reporter's specific setup. Key open questions:
- What VS Code setting(s) might affect temporary commit message file handling?
- Is there a macOS-specific interaction with VS Code's file processing?
- Does ARM64 architecture play any role?

No proposed fixes yet as root cause remains unisolated. Next steps would likely involve original reporter sharing their exact VS Code configuration and environment details to identify the triggering factors. The thread has effectively ruled out a core Git bug, narrowing focus to VS Code environment-specific behavior.