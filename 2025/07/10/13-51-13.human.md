Performance optimization patch in the bloom filter pathspec series. This is a refactoring change that extracts the pathspec-to-bloom-key conversion logic into a separate helper function (`convert_pathspec_to_filter()`) to simplify the main bloom filter preparation code. 

Derrick Stolee proposes this as an incremental improvement to Lidong Yan's ongoing work optimizing pathspec handling with bloom filters. The change doesn't modify behavior but makes the code more maintainable by:
1. Isolating the pathspec processing logic
2. Reducing local variable scope in the main function
3. Making the next change (handling multiple pathspecs) cleaner

The patch is marked as optional/nit-picky, suggesting it's a nice-to-have refinement rather than a critical fix. It builds on the broader performance work that has already been through multiple review rounds, with the key optimization being the new `bloom_keyvec` abstraction for handling multiple pathspecs efficiently.

This appears to be a straightforward, uncontroversial change that follows the established patterns in the series. The implementation looks clean and the commit message clearly explains the motivation. Since this is part of a well-reviewed effort, it seems likely to be accepted as a final polish before merging the larger optimization.