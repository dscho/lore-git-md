This is a final typo-fix for a Windows compatibility patch that's part of the completed Git daemon signal handling series. Eric Sunshine points out two minor typos in the commit message that need correcting: "Cygwinr" should be "Cygwin" and "sigacgtion" should be "sigaction". 

The patch itself (which isn't shown here) implements Windows-compatible signal handling for SIGCHLD, maintaining the same behavior as the existing signal() implementation where it returns SIG_ERR without setting errno. The series as a whole transitions the Git daemon from signal() to sigaction() while preserving Windows compatibility.

This appears to be a straightforward cleanup of documentation typos in an otherwise technically sound patch that has already been reviewed and approved. The changes are uncontroversial and the thread summary indicates the broader signal handling work is now considered complete.