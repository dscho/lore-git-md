This is a follow-up to the `git last-modified` command series, which is now technically complete with maintainer approval. Junio C Hamano notes two pending API changes from an unrelated Bloom filter topic that will eventually require minor updates to the `last-modified` implementation. The changes involve renaming `clear_bloom_key` to `bloom_key_clear` and `fill_bloom_key` to `bloom_key_fill`, along with parameter reordering. 

Junio provides the exact 3-line diff that would be needed if the series has to be rebased later, but emphasizes this isn't urgent and can be handled when the other topic lands. The message is primarily informational - no action is required from the `last-modified` author at this stage. This maintains the separation between the two independent development efforts while documenting the trivial future integration work needed.