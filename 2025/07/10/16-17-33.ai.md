This is a performance optimization patch in the bloom filter pathspec series, specifically addressing code cleanup after refactoring. The email from Junio Hamano points out two now-unused variables in `prepare_to_use_bloom_filter()` that resulted from moving bloom keyvec creation logic into a helper function (`convert_pathspec_to_filter()`).

The unused variables are:
1. `struct bloom_keyvec *bloom_keyvec` - no longer needed since keyvec creation is handled directly in the helper's return value
2. `const char *p` - previously used in path processing that's now encapsulated in the helper

This is a minor cleanup following the major refactoring that introduced the `bloom_keyvec` abstraction for handling multiple pathspecs. The changes maintain the optimization benefits while removing dead code artifacts from the transition. The thread context shows this series has already undergone extensive review and is nearing finalization, with this being a polish step addressing implementation details rather than introducing new functionality.