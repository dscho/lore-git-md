This is a discussion about a refactoring patch in the MIDX (multi-pack index) series that moves tracking from global to per-source storage. The conversation focuses on patch 4/8, which introduces a `multi_pack_index_loaded` flag to track MIDX loading state per source.

Taylor Blau and Justin Tobler are questioning whether this new flag is actually necessary, since `packed_git_initialized` already serves a similar purpose at the repository level. Taylor speculates the flag might be preparatory for future removal of the global `packed_git_initialized`, but notes this isn't clearly stated in the commit message. Both reviewers seem to think the patch could potentially be dropped if the flag isn't serving a clear purpose.

The discussion highlights an open question about the transitional architecture - whether this flag is temporary scaffolding or has a long-term role. The thread context shows this is part of a larger, well-reviewed series that's nearly complete, with most technical questions already resolved in earlier iterations. This particular patch appears to be one of the few remaining points needing clarification before the series can proceed.