This is a follow-up in the stalled SIGCHLD handling series for `git daemon`, specifically addressing Windows compatibility concerns. Carlo Marcelo Arenas Belón is responding to Johannes Sixt's feedback about signal number behavior in MinGW.

The key technical points:
- SIGCHLD's numeric value differs between platforms (17 in MinGW vs 20 in Cygwin/macOS)
- Current MinGW behavior returns error from `signal(SIGCHLD)` without setting errno
- Carlo proposes changing MinGW's SIGCHLD value to 20 to match Cygwin/macOS behavior
- Johannes Sixt notes Windows doesn't properly support signals outside Cygwin

This continues the thread's unresolved question about whether the SIGCHLD series can safely proceed given Windows platform quirks. The discussion has shifted from SA_RESTART concerns to more fundamental signal handling differences in MinGW. Carlo appears to be seeking consensus on changing MinGW's SIGCHLD definition as a path forward, though Johannes's response suggests deeper Windows signal handling limitations may make this approach problematic.