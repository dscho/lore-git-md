This is a build system fix for git-gui, part of a larger series modernizing macOS support. The patch corrects a missing dependency in the Makefile that was overlooked when the script generation logic was moved to a separate file in March 2025. Without this fix, changes to the generator script wouldn't trigger a rebuild of the main git-gui script when they should.

The change is straightforward - adding `generate-git-gui.sh` to the dependency list for `$(GITGUI_MAIN)`. This ensures the main script gets regenerated whenever either the template (`git-gui.sh`) or the generator logic changes. The patch is small (one line changed) and clearly motivated by the kind of build system oversight that often surfaces during refactoring.

While technically separate from the series' main macOS focus, this fix aligns with the overall cleanup theme. It's a minor but correct improvement to the build system that should prevent subtle issues for developers working on git-gui. The change appears uncontroversial and well-justified by the commit message.