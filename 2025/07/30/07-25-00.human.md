Test fix correcting path assertion in t7450-bad-git-dotfiles.sh. 

This is a straightforward test correction from 陈建虎 that fixes an assertion in t7450-bad-git-dotfiles.sh. The test was checking for the existence of a file at "$PWD/foo" when it should have been checking "$PWD/bad-clone/sub/foo" instead. 

The change appears to be a simple fix for a test assertion that didn't match the actual file location being tested. The test verifies behavior when submodules check out into directories with problematic names (specifically testing post-checkout hook behavior with special characters in paths).

The patch is minimal (just a one-line change) and correctly updates the test to match the expected file location. The commit message could be slightly more detailed about why this path correction is needed, but the change itself looks correct and focused.