This is a technical discussion email in the ongoing sparse-checkout configuration refactoring thread, focusing on late config parsing issues.

Phillip Wood responds to Derrick Stolee's preference for moving sparse-checkout configs directly into `struct repository` (option 1) rather than expanding `repo_settings`'s option 2). He suggests an alternative approach: modifying `git_default_config()` to accept a repository pointer as callback data, which would maintain the "last one wins" config behavior while avoiding global variables. However, he acknowledges this would be invasive, requiring changes to all callbacks that eventually invoke `git_default_config()`.

The email references a prior discussion with Patrick Steinhardt about calling `prepare_repo_settings()` from `repo_read_config()`, which proved unworkable. Phillip's proposal attempts to address both the global variable elimination goal and the late config parsing concerns raised earlier in the thread, though it would represent a more fundamental change to Git's config handling infrastructure.

This continues the thread's exploration of architectural tradeoffs while maintaining focus on the core problem of eliminating sparse-checkout globals. The discussion remains technical, with Phillip offering a third path between the two options Derrick outlined, though one that would require significant refactoring of config callback chains.