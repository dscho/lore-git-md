This is a follow-up discussion in the completed test automation thread, focusing on exit code inconsistencies discovered during post-merge review.

The email confirms and narrows down an earlier point about command exit codes, specifically identifying exit code 129 as the common deviation when testing `git subcmd -h` behavior outside a repository. D. Ben Knoble clarifies that this affects primarily script-based commands (those with `.sh` or `.perl` suffixes) and agrees with Usman Akinyemi's observation that these cases should be tracked as #leftoverbits for future cleanup.

The discussion maintains the established boundary that exit code standardization is out of scope for the already-merged test automation series, while acknowledging it as a legitimate follow-up task. This aligns with the thread's post-merge pattern of incrementally addressing non-blocking issues, with exit code work already identified as a priority in the #leftoverbits tracking.

Technical context for future reference: The affected commands appear in the exception list of t1517's automated testing (including p4.py, pickaxe, quiltimport, etc.), where their non-129 exit codes currently represent expected-but-inconsistent behavior rather than test failures. The confirmation of 129 as the primary deviation pattern provides a clear target for future standardization efforts.