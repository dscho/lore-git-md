This thread has evolved into a deeper discussion about git-gui's handling of Tcl/Tk dependencies on macOS, particularly around whether the system-provided version should be treated as immutable. The original 9-patch series aimed to modernize macOS support by removing deprecated components and fixing build issues, but Junio Hamano raised a fundamental question about the architectural premise - whether Tcl/Tk should be considered a system component or a regular dependency.

Carlo Arenas has now provided crucial context about macOS's Tcl/Tk integration, explaining that while users can upgrade Tcl/Tk (it's not strictly immutable), the system version (8.5) is outdated and deprecated with warnings about future removal. The core issue appears to be git-gui's Makefile behavior that preferentially uses the system version, creating compatibility problems, rather than the upgrade capability itself.

The discussion is now at a crossroads between:
1) The original approach of removing macOS-specific workarounds for the deprecated system Tcl/Tk
2) Potentially reworking how git-gui detects and uses Tcl/Tk across platforms
3) Addressing Johannes Sixt's outstanding technical concerns about implementation details

Key unresolved questions remain about how to best handle the immediate macOS issues while considering the broader implications for cross-platform Tcl/Tk dependency management in git-gui. The series' next iteration will need to either strengthen its justification for the current approach or propose an alternative solution that addresses both the technical debt and the architectural concerns raised.