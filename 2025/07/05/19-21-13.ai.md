This is a follow-up discussion on the SSH signing tempfile leak bugfix. Jeff King confirms the original issue (temporary key files not being cleaned up due to `strbuf_detach()` usage) and provides a reproduction case. He agrees with the fix's approach while suggesting two potential improvements that could be made separately: avoiding the `xstrdup()` by using the `key_file` pointer directly (though notes this would complicate cleanup logic), and simplifying the tempfile deletion code to handle NULL entries more cleanly.

The email also raises a contributor process issue regarding the sign-off from "redoste" without a legal name, referencing Git's DCO requirements. Jeff notes the patch may be trivial enough to bypass this concern but CCs Brian for guidance on name/privacy policies. The technical discussion remains focused on the resource management fix while acknowledging the original patch's correctness.