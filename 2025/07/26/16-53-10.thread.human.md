This thread concerns a patch series expanding `-h` and `--help-all` support across Git commands, making them work outside repository contexts and allowing `-h` to coexist with other arguments while preserving special cases in `ls-remote` and `grep`. The series is now at version 1, with all planned changes implemented: refactoring parse-options internals, extending `--help-all` support, and modifying help request handling in `git.c`.

The author has incorporated test fixes from D. Ben Knoble, addressing minor issues in the test infrastructure. These include removing `quiltimport` and `request-pull` from an expected failure list (since they now properly handle help requests) and cleaning up syntax in another test file. Both the author and Knoble agree these fixes should be squashed into the existing commits rather than kept separate.

While the core functionality appears complete, the author acknowledges that the final patch allowing `-h` with additional arguments might be controversial and has offered to drop it if problematic. The series maintains backward compatibility and preserves special cases while improving help system behavior. No reviews have been posted yet, but the test fixes show collaborative polish on the supporting infrastructure.