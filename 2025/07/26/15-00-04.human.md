This is a technical discussion about improving `git rebase`'s handling of skipped commits during conflict resolution. Cameron Steffen is responding to Johannes Sixt's concerns about accurately detecting when commits are truly skipped versus becoming empty through other means.

Cameron argues that Git can detect most cases where a commit is manually replaced (via `git commit` without auto-populated messages) or when a hard reset occurs. He suggests these scenarios could still trigger the warning about skipped commits safely. This addresses some of Johannes' workflow examples where the original detection approach might produce false positives.

The exchange highlights the core challenge: distinguishing legitimate skips from other rebase states where commits become empty through user actions. Cameron's response shows he's refining the proposal to handle more edge cases, though the fundamental tension between detection coverage and accuracy remains unresolved. The discussion remains technical but productive, with both sides working toward a solution that won't mislead users about their rebase state.