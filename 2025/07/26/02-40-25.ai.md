This is a technical discussion in the ongoing `git rebase` warning feature thread, focusing on the precise conditions for triggering commit-skipped warnings during conflict resolution.

Cameron Steffen responds to Junio Hamano's concerns about the warning message's accuracy. The core tension is between Steffen's original approach (warning whenever a commit is skipped during rebase due to becoming empty) versus Hamano's stricter requirement that warnings should only fire when we can definitively say the commit was skipped because it was previously applied.

Steffen acknowledges Hamano's point that reusing the existing "skipped previously applied commit" message would be misleading if the warning fires for other types of empty-commit skips. This reveals a fundamental design question: should the warning system have broad coverage (potentially some false positives) or strict accuracy (possibly missing some cases).

The discussion has shifted from implementation details to the conceptual model of what constitutes a "skipped commit" worth warning about. This email marks a key moment where the proposal's scope may need narrowing to maintain message accuracy, unless the state machine can be enhanced to reliably classify different skip reasons. The thread remains active with core participants debating the tradeoffs between completeness and precision in the warning system.