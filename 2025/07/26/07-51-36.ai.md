Bug investigation response regarding git-daemon's tilde expansion behavior. Jeff King attempts to reproduce Russell King's reported regression where git-daemon fails to properly expand `~user` paths when serving repositories via `--user-path=public_git` in Fedora 40 (Git 2.49.0).

Key technical details from the response:
- Jeff demonstrates successful tilde expansion in his test environment using similar configuration
- The error message "not in directory list" suggests the issue may be related to path validation in `daemon.c:path_ok()`
- Systemd service configuration is examined as a possible source of unexpected arguments
- Multiple test scenarios are attempted (direct invocation, proxy script, systemd service) but the issue cannot be reproduced

The response focuses on troubleshooting the environment rather than identifying a Git bug, suggesting the problem may be specific to the Fedora 40 deployment. Jeff notes the daemon appears to be looking in the correct expanded paths (`/home/user/public_git/`) despite showing the tilde form in logs, and hypothesizes about possible systemd argument injection. The investigation remains inconclusive with no clear root cause identified yet.