Here's a human-readable summary of the thread:

The thread started with a bug report about inconsistent behavior between `git ls-files` and `git grep` when using the `:^:` pathspec syntax to exclude files. While `git ls-files` correctly interprets this as an exclude pattern, `git grep` fails with an "ambiguous argument" error, suggesting it's incorrectly trying to parse it as a revision instead.

Junio Hamano, the Git maintainer, has now responded. He confirms the inconsistency but notes the `:^:` syntax is intentionally designed to be somewhat confusing (as it resembles revision syntax). He recommends using the more explicit `:(exclude)` form as a workaround. Junio pinpointed where in the code this happens - specifically in `setup.c` where filename verification occurs - and explained that while it might be possible to fix the shorthand syntax handling, any changes would need careful consideration to avoid unintended consequences in other parts of Git that use the same parsing logic.

The discussion reveals this is a known edge case in Git's parsing rules, where the same syntax can be interpreted differently depending on context. While not a fundamental limitation, fixing it would require caution due to potential ripple effects. For now, users encountering this issue should use the more explicit `:(exclude)` syntax when working with `git grep`.