This is a follow-up discussion about the recently merged test reorganization series for Git command help output testing. The email focuses on a technical detail regarding exit codes in the automated help output tests.

Usman Akinyemi responds to D. Ben Knoble's observation about command exit codes in the help output tests, specifically asking for clarification about whether the issue involves exit code 129. The context is the automated testing framework in t1517 that verifies help output for all main Git commands outside a repository, where certain commands were excluded via an exception list.

The discussion touches on an implementation detail where some commands that were excluded from testing (like `quiltimport` and `request-pull`) might actually work with `-h` outside a repository but fail due to their exit code behavior. This adds to the growing list of post-merge cleanup items being tracked as `#leftoverbits`, which already includes various minor improvements to the test infrastructure.

Key technical points include the use of `git --list-cmds=main` to dynamically test commands, the command exception list that handles special cases, and now the observation about exit code patterns (specifically code 129) potentially being the real reason some commands were excluded rather than actual help output failures. The email suggests this exit code behavior might warrant future standardization work, though it's explicitly noted as out of scope for the already-merged series.