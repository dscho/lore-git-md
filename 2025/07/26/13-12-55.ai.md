This is a discussion about edge cases in Git's alias handling with `-h` flags, continuing the thread about help mode detection. D. Ben Knoble responds to Jeff King's analysis of problematic cases where alias-injected arguments interfere with `-h` help requests.

The email focuses on three key aspects of the ongoing discussion: First, it questions Jeff King's proposal to make alias handling non-textual by dropping alias-added arguments during `-h` processing, noting this would diverge from Git's current mostly-textual substitution model. Second, it raises concerns about false positives if Git assumes `-h` always means help for third-party commands. Third, it suggests considering `--help-all` behavior in the same context, as Knoble has been working on patches to improve that flag's functionality.

Knoble's response highlights the tension between making `-h` behavior robust while maintaining Git's simple alias substitution model. The discussion remains technical, focusing on implementation tradeoffs rather than proposing concrete solutions. This continues the thread's examination of how deeply Git should interpret user intent versus preserving predictable textual substitution behavior.