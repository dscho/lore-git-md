This thread began as a feature proposal from Alon Bar-Lev about improving `git clean`'s handling of ignored files in nested repositories, where the current `-x` flag (for cleaning ignored files) doesn't work recursively with `-ff` (force in nested repos). The discussion has now uncovered a more fundamental issue: `git clean -dxff` fails to remove *any* untracked files (not just ignored ones) from submodules, contrary to both documentation and expected behavior.

The latest email provides a concrete reproduction case showing `module1/junk.txt` remains after the command when it should be removed. This appears to be either a longstanding bug or a regression in the interaction between the `-d` (directories), `-x` (ignored files), and `-ff` (force in nested repos) flags. The complexity of the `dir.h` implementation suggests fixing this will require careful work.

What started as a discussion about potentially adding new functionality (recursive ignored-file cleaning via `-xx` or modified `-x`) has revealed a more pressing correctness issue with the existing flags. The next steps likely involve diagnosing whether this is an implementation bug, documentation bug, or intentional-but-poorly-documented limitation, with the reproduction case providing a clear starting point for investigation.