This is a technical discussion about implementing warnings for skipped commits during rebase conflict resolution.

Junio Hamano points out a logical inconsistency in Cameron Steffen's proposed approach to warning about skipped commits during rebase. The core issue revolves around how to accurately detect when a commit is being skipped because it became empty during conflict resolution versus other reasons.

Hamano notes that Steffen's suggested warning message ("skipped previously applied commit <hash>") would be misleading if used in all cases where a commit is skipped due to becoming empty, since the message implies the commit was already applied rather than becoming empty. This reveals a need to either:
1. Develop a new warning message specific to empty-commit skips
2. Find a more precise way to detect when skips occur due to commits becoming empty

The discussion continues to focus on the sequencer's state tracking capabilities, with Hamano maintaining his position that warnings should only trigger when we can definitively determine the skip reason. This builds on earlier thread consensus about using Phillip Wood's `rebase_path_message()` check as a guard condition, while exposing new nuances in message semantics.

The exchange highlights ongoing challenges in designing reliable state tracking for rebase operations, particularly around distinguishing between different types of skips during conflict resolution. The technical depth suggests this will require careful implementation to avoid false positives in warning messages.