Refactoring patch in a series improving Git's help system functionality. This second patch focuses on making the parse-options internals more readable by replacing magic numbers with named enums for usage style and output destination flags.

The change introduces two new enums in parse-options.c: `usage_style` (normal/full) and `usage_output` (stdout/stderr). These replace the previous boolean parameters in `usage_with_options_internal()` and related functions, making call sites more self-documenting. The patch does this through:

1. Adding the enum definitions at file scope
2. Updating the function prototype
3. Modifying all call sites to use the new enum values
4. Adjusting the internal logic to check the enum values instead of booleans

The changes are mechanical but improve code clarity, particularly for the frequently seen "0, 0" and "0, 1" parameter patterns in help system calls. This is part of a larger series (4 patches) that aims to expand `-h` and `--help-all` functionality across Git commands, with this patch serving as internal cleanup to support those changes.

Key technical details:
- New enums: `usage_style` and `usage_output`
- Modified function: `usage_with_options_internal()`
- Updated callers: `parse_options_step()`, `usage_with_options()`, `show_usage_with_options_if_asked()`
- No behavior changes - purely a readability improvement
- Part of series building on test branch ua/t1517-short-help-tests

The patch appears well-contained and focused, with clear commit message explaining the motivation. It touches only parse-options.c (32 lines modified) and maintains all existing functionality while improving maintainability.