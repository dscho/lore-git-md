This is a post-merge review comment in the completed test organization series. D. Ben Knoble follows up on the merged patch that automated help output testing outside repositories, noting an additional observation about exit code behavior.

The email points out that while the automated test correctly identifies commands that fail the `-usage` test outside a repository, the underlying issue appears to be primarily about exit codes rather than fundamental incompatibility. Knoble suggests this as potential future cleanup work (`#leftoverbits`), though explicitly marks it as out of scope for the already-merged series.

The comment builds on the established test framework that dynamically checks `-h` behavior for all main commands via `git --list-cmds=main`, with exceptions handled through an explicit list. The observation doesn't require immediate action but identifies a subtle pattern in the test failures that could inform future test improvements or command behavior standardization.

This fits naturally with the thread's trajectory of incremental test improvements, where the core architecture (automated outside-repo testing via command listing) is now in place, leaving room for polishing edge cases it surfaces. The tone is collaborative, treating the observation as a natural next step rather than a critique of the merged work.