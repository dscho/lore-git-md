Thread summary:

Feature proposal for `git clean` behavior in nested repositories, initiated by Alon Bar-Lev. Current `git clean -dxff` removes ignored files (`-x` flag) only in top-level repo, requiring separate `git submodule foreach` for submodules. Proposal suggests either modifying `-x` to work recursively with `-ff` or adding new `-xx` flag for explicit recursive ignored-file cleaning. Core questions: is current behavior intentional limitation or oversight? Should recursive ignored-file cleaning be default with `-x` or require separate flag given destructive potential? No patch yet; seeking conceptual feedback on approach. Touches submodule handling and `clean` command's ignored-file behavior. Key technical detail: interaction between `-x` (ignored files), `-f` (force), and `-ff` (force in nested repos) flags. Open question: which solution path (modify `-x` vs new `-xx`) would be more intuitive and maintainable.