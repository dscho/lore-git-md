Thread continuation in the `commit.requireIgnoreFirst` discussion, now with Junio C Hamano weighing in on the fundamental limitations of the proposed approach. This moves beyond Torsten's reproducibility challenge to question the core concept's effectiveness.

Junio argues that merely requiring `.gitignore` first doesn't sufficiently address the real problem - incorrect ignore rules can still cause issues later. He shares a concrete example where a broad `*.txt` ignore rule caused build tool templates to be missed during `git add`, requiring history rewriting. The key insight is that the proposed config wouldn't have prevented this class of problems.

The email shifts focus from the original narrow proposal to the broader challenge of detecting when `git add` operations might be missing files due to over-broad ignore rules. Junio suggests what's really needed is a way to flag cases where added pathspecs unintentionally exclude files that would match historically. This implies the need for more sophisticated pattern analysis rather than a simple commit-order check.

Technical context expands to include the interaction between pathspec matching and ignore rules, with Junio's example showing how historical repository patterns (what files were previously tracked) could inform warnings about potentially problematic additions. The discussion now appears to be moving toward whether Git could/should implement more intelligent add-time validation rather than the original pre-commit check.

This intervention from a core maintainer significantly raises the stakes - the thread is no longer just about validating the original problem case, but about whether the entire approach addresses the right problem. Future discussion will likely need to either pivot toward Junio's suggested direction or make a strong case for why the simpler check still provides value despite its limitations.