This is a test fixup patch, part 1 of a 4 expanding `-h` and `--help-all` option availability. It addresses minor issues in the test infrastructure for the feature series, specifically in t1517-outside-repo.sh and t5200-update-server-info.sh.

The changes clean up test artifacts and correct test expectations. In t1517-outside-repo.sh, it removes a spurious "Hello" message that was accidentally left in the test and adjusts the list of commands being tested (removing quiltimport which was incorrectly marked as failing). The t5200-update-server-info.sh fix corrects a syntax error in the test assertion.

These are preparatory fixes for the main feature work, ensuring the test suite is clean before implementing the help system changes. The patch is straightforward with clear, focused changes to test files. The author expects these to be squashed into the upstream branch rather than standing as separate commits.