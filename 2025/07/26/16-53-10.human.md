This is a feature patchset that improves the behavior of `-h` and `--help-all` options across Git commands. The series makes these help flags work in more scenarios while maintaining backward compatibility.

The patchset builds on an existing test branch (ua/t1517-short-help-tests) and starts with a fix for that dependency. The main changes allow `--help-all` to work outside repository contexts and enable `-h` to be used alongside other arguments without breaking existing special cases in `ls-remote` and `grep`.

The implementation involves some preparatory refactoring to clean up internal option parsing code, followed by the core changes to the help system behavior. The changes touch several builtin commands and the option parsing infrastructure, with corresponding test updates to verify the new behavior.

The series appears well-structured with clear commit messages explaining each step. The author has taken care to maintain backward compatibility with existing usage patterns while expanding the help system's flexibility. The changes are relatively contained but could affect many commands, so reviewers will likely want to verify the behavior across different command combinations.