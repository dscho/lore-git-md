This is a feature patch series (4 parts) that expands the availability of `-h` and `--help-all` options in Git commands. The series builds on an existing test branch (ua/t1517-short-help-tests) and includes preparatory fixes before implementing the main changes.

The core changes allow `--help-all` to work outside repository contexts and enable `-h` to be used alongside other arguments without breaking existing special cases in `ls-remote` and `grep`. The implementation involves refactoring internal parse-options machinery to make the usage helper's arguments clearer, then modifying how builtins handle help requests.

The series touches several key areas: parse-options.c gets the most significant changes (41 lines modified) with adjustments to the help system internals. Multiple builtin commands and test files are updated to accommodate the new behavior. The test/t1517-outside-repo.sh tests are particularly relevant as they verify the new out-of-repo help functionality.

This appears to be a well-structured series with clear preparatory steps leading to the main feature. The changes are focused on improving user experience while maintaining backward compatibility with existing command-line patterns. The dependency on another test branch suggests careful attention to testing infrastructure.