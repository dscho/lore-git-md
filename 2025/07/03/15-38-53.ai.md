This is a follow-up discussion in the memory leak fix thread for `send-pack`'s `extra_have` oid array handling. Jeff King (peff@peff.net) responds to Jacob Keller's original patch, confirming the fix looks good and providing context about why the leak wasn't caught by existing tests.

The email acknowledges that while the current test suite (t5539) doesn't expose this particular leak scenario involving the `extra_have` array, the fix is still worthwhile. Jeff King notes this was one of several leaks he had previously addressed in commit c8009635785e, but this particular case slipped through because the tests didn't exercise the code path where alternate object stores populate the `extra_have` array during push operations.

Jeff King suggests that while expanded test coverage for such edge cases would be ideal, he's comfortable proceeding with just the fix itself. This aligns with the thread's earlier conclusion that while test coverage gaps exist for alternate-store push scenarios, they're out of scope for this particular fix. The response confirms the technical solution is sound and follows established patterns, marking another step toward finalizing this uncontroversial memory leak fix.