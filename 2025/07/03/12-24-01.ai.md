This is a technical discussion about the reftable backend implementation, specifically addressing where to set the default reference storage format in the codebase. The exchange builds on Patrick Steinhardt's v2 patch series to make reftable Git 3.0's default ref storage backend.

Karthik Nayak responds to Steinhardt's proposed change in setup.c, initially questioning whether the default format should be set in REPOSITORY_FORMAT_INIT macro instead. Steinhardt had opted to modify repository_format_configure() instead, citing concerns about the macro's widespread use and improved code clarity with the explicit else branch. Nayak now agrees with this approach after reviewing the code flow, noting the current implementation makes it hard to trace where the default '_FILES' format is set.

The discussion introduces a potential future direction - changing REPOSITORY_FORMAT_INIT to use _UNKNOWN for both ref storage format and hash algorithm, which would make initialization behavior more consistent and explicit. Nayak leaves it to Steinhardt to decide whether to include this change in the current series. The exchange demonstrates consensus on the core approach while identifying opportunities for further codebase improvements.

Key technical points include the specific location of the change (setup.c's repository_format_configure()), the current default behavior (REF_STORAGE_FORMAT_DEFAULT resolving to '_FILES'), and the proposed future state (REPOSITORY_FORMAT_INIT using _UNKNOWN). The discussion remains focused on implementation details rather than architectural concerns, with both participants showing alignment on the technical merits of the approach.