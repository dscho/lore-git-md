This is a refactoring patch in Patrick Steinhardt's Meson build system cleanup series. It removes two unnecessary variables from the documentation build configuration in `Documentation/meson.build`.

The patch eliminates `manpage_target` which was defined but never referenced, and inlines the single-use `manpage_path` variable directly into the `custom_target()` call where it was used. The change simplifies the build configuration by removing redundant variable declarations while maintaining identical functionality - the manpages will still be generated and installed to the same locations.

This is a straightforward cleanup that follows the series' pattern of tightening up the Meson build configuration. The change affects only the build system internals with no impact on the generated documentation or runtime behavior. The patch is small (3 lines removed, 2 added) and focused, with a clear explanation of what's being removed and why.