This is a follow-up discussion about the `git blame` ignoreRevsFile bugfix. Kristoffer Haugsbakk points to a relevant 2021 thread (Noah Pendleton's original implementation of ignoreRevsFile) as additional context for Francesco Nicoletta Puzzillo's patch. The current patch modifies `builtin/blame.c` to gracefully handle missing ignoreRevsFile when configured globally, while the linked thread shows the original feature introduction where this edge case wasn't considered.

The reference suggests this isn't just a simple bugfix but touches on a design question about how optional configuration files should behave - whether they should fail loudly or silently skip when specified but missing. The 2021 thread might contain relevant discussion about the original design intent that could inform whether the current fix aligns with that vision. No new code changes are proposed in this email; it's purely providing historical context that could be useful for reviewers evaluating the correctness of the proposed solution.