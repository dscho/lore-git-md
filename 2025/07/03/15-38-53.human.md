This is a follow-up discussion about a memory leak fix in `send-pack`'s `extra_have` oid array handling. The thread has already established that the leak occurs during push operations when processing remote refs from alternate object stores, and that both the main success path and error cases have been addressed through cleanup refinements.

Jeff King (peff@peff.net) responds to Jacob Keller's original patch, acknowledging that while the tests in t5539 don't currently trigger this leak (explaining why it wasn't caught earlier), the fix is valid. He notes that many similar "slightly-exotic" command invocations likely still leak, suggesting that while expanded test coverage would be nice, fixing the leaks directly is also reasonable.

The discussion appears to be winding down, with all technical aspects resolved and only the test coverage gap remaining as an acknowledged but out-of-scope item. The exchange maintains the friendly, collaborative nature typical of Git list discussions, with Jeff providing context about why the leak was missed initially while supporting the fix.