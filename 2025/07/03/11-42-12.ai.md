Refactoring patch in the reftable-as-default series, addressing initialization location feedback. Patrick Steinhardt responds to Karthik Nayak's suggestion about where to set the default ref storage format, explaining his preference for modifying `setup.c`'s `repository_format_configure()` rather than the `REPOSITORY_FORMAT_INIT` macro. 

Steinhardt argues the current change makes the default value flow more explicit in the code path where it's actually used, while avoiding touching the widely-used initialization macro. He further suggests that `REPOSITORY_FORMAT_INIT` could potentially be changed to use `_UNKNOWN` for both ref storage format and hash algorithm, indicating this might be a direction for future cleanup.

The exchange represents ongoing refinement of the implementation details for the reftable default transition, with Steinhardt maintaining his architectural approach while acknowledging the review feedback. The discussion remains focused on code organization clarity rather than challenging the fundamental versioned-defaults strategy established in earlier reviews.