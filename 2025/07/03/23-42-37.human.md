This is a confirmation that Jeff King's bugfix for the `git log --diff-filter` regression works as intended. Eric Salem, who originally reported the issue, verifies that the patch successfully restores the expected behavior where lowercase exclusion filters (like `d` to exclude deletions) work regardless of output format.

The fix addresses a 2022 regression where these filters only worked when Git computed diffs. Jeff's solution modifies `setup_revisions()` to properly consider both filter fields, with the change being minimal (just one line in `revision.c`) but effective. The patch includes a new test case to prevent future regressions.

With Eric's confirmation, this thread appears resolved. The fix maintains backward compatibility while restoring the documented behavior, and no further discussion seems needed unless maintainers have questions before merging.