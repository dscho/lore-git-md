This is a build system cleanup patch in Patrick Steinhardt's ongoing series to improve Git's Meson infrastructure. The patch fixes a minor issue where the 'https' backend option was being listed twice in the build summary output - once in the auto-detected features section and again in the backends section. The change simply removes the redundant entry from the features summary while keeping the backend listing intact.

The fix is straightforward - a one-line deletion from meson.build that doesn't affect functionality, just cleans up the build summary display. This continues the series' theme of polishing the Meson build configuration, following the first patch's Python version detection changes. The patch appears well-scoped and uncontroversial, addressing a small but noticeable inconsistency in the build output.