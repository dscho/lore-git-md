This is a bugfix for `git blame`'s handling of the `ignoreRevsFile` configuration. Currently, if a user sets `blame.ignoreRevsFile` globally but the specified file doesn't exist in some repositories, `git blame` fails. The patch makes the command more robust by first checking if the file exists before attempting to parse it.

The change is straightforward - it adds a `stat()` check before calling `oidset_parse_file_carefully()`. The patch includes a new test case that verifies the fix by setting a global `ignoreRevsFile` configuration and ensuring `git blame` still works in a repo where that file doesn't exist.

This appears to be a well-contained fix for a real usability issue. The patch is small (3 lines changed in the implementation plus a test) and follows Git's coding style. The test case directly exercises the scenario being fixed. The change seems unlikely to be controversial as it's clearly making the command more robust without changing any intentional behavior.