This is a bugfix discussion regarding `git log --diff-filter` behavior with exclusion filters. Jeff King (peff) corrects K Jayatheerth's earlier assertion that diff filters only apply when diffs are computed, pointing out that exclusion filters should affect commit selection regardless of output format. 

The key technical point is that `--diff-filter=d` (excluding deletions) should omit commits containing deletions from the output entirely, not just filter diff output. This contradicts the previous understanding that filters only apply when computing diffs for display. The behavior change appears to stem from commit 75408ca949 in 2022, making this a regression from earlier Git behavior.

The discussion has moved beyond documentation clarification to identifying a concrete behavioral regression. Jeff maintains that exclusion filters should work at the commit selection level, not just during diff output generation. This affects all exclusion filters (a-z) when used with minimal output formats, not just the deletion case.

The thread now awaits input from the original author of the suspect commit to determine whether to revert or amend it. The core issue involves the interaction between output formats and diff computation, which is more complex than initially thought. This email reinforces the regression hypothesis and clarifies the expected behavior for exclusion filters.