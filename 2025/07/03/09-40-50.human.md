This is a refactoring patch in the ongoing `--skip-until` pagination series, addressing interface clarity in the ref iterator's seek functionality. Karthik Nayak responds to Patrick Steinhardt's feedback on making the `ref_iterator_seek()` behavior more intuitive.

The patch originally proposed adding a `set_prefix` boolean to distinguish between seeking to a reference (like a cursor) versus setting a new iteration prefix. Patrick suggests replacing this with a flags field for better readability, which Karthik agrees to implement. The discussion also touches on improving the function's documentation to clearly separate the two behaviors.

This is a follow-up refinement to the pagination infrastructure, ensuring the seek interface is both functional and self-documenting. The changes maintain all existing behavior while making the API more intuitive for future use. The exchange shows the core pagination work is largely settled, with attention now on polishing the implementation details.