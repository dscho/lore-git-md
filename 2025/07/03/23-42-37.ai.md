This is a confirmation that Jeff King's bugfix for the `git log --diff-filter` regression works as intended. Eric Salem, who originally reported the issue, verifies that the patch successfully restores the expected behavior where lowercase exclusion filters (like `d` to exclude deletions) work regardless of output format.

The fix addressed a regression introduced in 2022 that caused exclusion filters to only work when Git computed diffs, breaking the expected commit selection behavior. Jeff's solution modified `setup_revisions()` to properly consider both `filter` and `filter_not` fields, with the change being minimal (one line in `revision.c`) but effective.

With this confirmation from the original reporter, the thread appears fully resolved. The patch can now proceed through the normal merge process, having been both technically validated by its author and functionally verified by the user who encountered the issue. No further discussion appears necessary.