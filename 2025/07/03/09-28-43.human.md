This is a build system cleanup patch in Patrick Steinhardt's Meson improvement series. It removes redundant Python version discovery in the build process, fixing an incorrect check for Python availability that was looking at the build host rather than the target host. The change simplifies the Meson configuration by eliminating unnecessary native Python detection while maintaining proper functionality for git-p4 integration. The patch appears focused and well-justified, addressing a clear technical inconsistency in the build system without introducing new complexity.