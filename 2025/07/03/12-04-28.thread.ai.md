Updated AI thread summary:

SHA-256 support for git-gui (v2). Patch series modifies git-gui to handle SHA-256 repositories by replacing SHA-1 length assumptions with dynamic hash length checks. Changes span main script, blame viewer, repository chooser, commit interface, and remote branch deletion. Key technical details: adds global `hashlength` variable set via `git rev-parse --show-object-format` (requires Git >=2.25.0), replaces hardcoded "40" with `$hashlength` in regexes/string ops, handles null SHA with `string repeat 0 $hashlength` (could use existing `$nullid` variable), fixes commit_committree validation in v2. Current status: author Takashi Iwai addressing review feedback from Johannes Sixt about edge cases and initialization timing.

Open issues:
- Regex at line 3217 still uses {1,39} instead of proper length check
- Need to resolve why original code checked up to 39 chars for partial hashes
- Two initialization timing problems where `hashlength` isn't available (repository picker dialog and post-checkout hook)
- Duplicate $null_sha1/$nullid variables need cleanup

Proposed solutions under discussion:
- Using length ranges (40,64) for early initialization cases
- Direct git rev-parse output length measurement
- Tcl string map approach for regex patterns (confirmed acceptable)

Technical questions remaining:
- Best approach for hash length checks before `hashlength` is initialized
- Whether to standardize on $nullid or $null_sha1 variable
- Need to verify behavior across all GUI components with both SHA-1 and SHA-256 repos

Review status: Johannes Sixt confirmed fundamental approach is sound but identified several edge cases needing polish. No tests included yet but changes follow established SHA-256 patterns in Git. Commit message scope needs clarification in next version.