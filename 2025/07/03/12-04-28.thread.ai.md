SHA-256 support for git-gui (v2). Patch modifies git-gui to handle SHA-256 repositories by replacing SHA-1 length assumptions with dynamic hash length checks. Changes span main script, blame viewer, repository chooser, commit interface, and remote branch deletion. Key technical details: adds global `hashlength` variable set via `git rev-parse --show-object-format` (requires Git >=2.25.0), replaces hardcoded "40" with `$hashlength` in regexes/string ops, handles null SHA with `string repeat 0 $hashlength` (could use existing `$nullid` variable), fixes commit_committree validation in v2. Open issues: missed SHA-1 length assumption in git-gui.sh line 3217 (`{1,39}` regex), need to verify `hashlength` availability during repository picker dialog execution. Review from Johannes Sixt confirms approach is fundamentally sound but suggests polishing variable usage and edge cases. No tests included but changes follow established SHA-256 patterns in Git. Commit message could be more precise about scope.