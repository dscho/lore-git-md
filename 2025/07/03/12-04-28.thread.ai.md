SHA-256 support for git-gui (v2). Patch series modifies git-gui to handle SHA-256 repositories by replacing SHA-1 length assumptions with dynamic hash length checks. Changes span main script, blame viewer, repository chooser, commit interface, and remote branch deletion. Key technical details: adds global `hashlength` variable set via `git rev-parse --show-object-format` (requires Git >=2.25.0), replaces hardcoded "40" with `$hashlength` in regexes/string ops, handles null SHA with `string repeat 0 $hashlength` (could use existing `$nullid` variable), fixes commit_committree validation in v2.

Current status: author Takashi Iwai addressing review feedback from Johannes Sixt about edge cases and initialization timing. Progress made on resolving open issues: regex at line 3217 confirmed to need abbreviated hash support (though 39-char check rationale still unclear), range check (40,64) agreed for repository picker dialog case, `git rev-parse HEAD` length measurement approved for post-checkout hook case, existing `$nullid` variable preferred where possible. Alternative string manipulation approach suggested for FETCH_HEAD parsing to avoid regex.

Open issues remaining:
- Finalize solution for regex patterns needing hash length checks before initialization
- Standardize on `$nullid` variable usage
- Verify behavior across all GUI components with both SHA-1 and SHA-256 repos
- Need to clarify commit message scope in next version

Review status: Johannes Sixt confirms fundamental approach remains sound and approves specific solutions for initialization timing problems. Technical discussion shows convergence on practical solutions maintaining compatibility with both hash algorithms. No tests included but changes follow established SHA-256 patterns in Git.