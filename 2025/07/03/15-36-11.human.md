Bugfix discussion regarding `git log --diff-filter` behavior regression. Jeff King pushes back on the claim that the current behavior is expected, arguing that exclusion filters (like `d` to omit deletions) should work even when not showing full diffs. He points out that `--diff-filter=D` (showing only deletions) works as expected by selecting commits based on diff options, so the exclusion case should similarly filter commits before output formatting.

This continues the thread investigating a regression introduced in Git 2.50.0 where lowercase exclusion filters stopped working with minimal output formats. Jeff's analysis showed this wasn't always the case, and now he provides a concrete example of the expected behavior where commit selection should happen before formatting. The discussion appears to be moving toward recognizing this as a genuine bug rather than expected behavior, with potential implications for how diff computation interacts with output filtering.