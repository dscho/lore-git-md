This is a discussion follow-up in the `for-each-ref --skip-until` pagination thread, focusing on race condition behavior. The email clarifies two key points about the name-based skipping approach:

1. The skipped reference (if it exists) will be included in the current page's output, but subsequent pages correctly exclude it regardless of whether it still exists. This maintains pagination integrity even if the marker ref is deleted between requests.

2. New references inserted before the skip marker in sort order are properly excluded from subsequent pages, preserving the expected pagination behavior. This contrasts with numeric offset approaches where inserted refs could disrupt the pagination sequence.

The discussion confirms the implementation's handling of concurrent modifications aligns with intuitive expectations for cursor-based pagination. No code changes are proposed - this serves as documentation of the design's behavior under race conditions, building on earlier analysis of the O(log N) performance characteristics and interface refinements.

Key technical details for future reference:
- The `--skip-until` functionality uses sort-order positioning rather than ref existence checks
- Behavior remains correct when marker refs are deleted or new refs are inserted
- Contrasts with numeric offset approach where insertions would break pagination
- Implementation lives in `ref_iterator_seek()` interface and backend adaptations