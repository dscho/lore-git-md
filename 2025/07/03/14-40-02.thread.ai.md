Here's the updated AI thread summary:

Documentation patch proposing to clarify that passing an empty path to `git init --template=` (e.g. `--template=`) skips template copying, a behavior that has existed since Git 1.5.6.4 (2008) where it was implemented to prevent incorrectly linking root directory files. The empty string check has moved from `copy_templates()` to `get_template_dir()` in newer versions. Han Young initially confirmed this undocumented feature is widely used by package managers and other tools that create temporary repositories just for fetching remote data where template files would be unnecessary overhead. Boris Kolpackov has now reinforced this with additional confirmation of real-world usage, noting the feature's obscurity doesn't diminish its legitimate use by tools. The thread remains focused on documenting this long-standing behavior in the TEMPLATE DIRECTORY section of git-init(1), affecting `builtin/init-db.c` template handling logic. With two independent confirmations of widespread usage and no objections raised, the case for documenting the existing implementation is strengthened. The patch remains non-controversial as it only clarifies rather than changes behavior, with consensus forming around making this feature more visible in the documentation.