Bugfix follow-up: Eric Salem confirms the workaround (using `--dirstat` with blank line filtering) successfully addresses their use case for `git log --diff-filter` exclusion filters. This comes after Jeff King identified the behavior as a regression from 2022 that broke lowercase filter functionality with minimal output formats.

The email serves as a positive resolution note in the thread, though it doesn't address the underlying regression. Eric has found a practical solution for their immediate needs using `--dirstat` output combined with post-processing. This workaround aligns with one of the two previously suggested approaches in the thread (the other being `--name-only` with `grep`).

While this provides closure for Eric's specific case, the thread remains open for the core issue - whether to fix the regression that causes exclusion filters to only work when diffs are computed. The exchange demonstrates how users can adapt to the current behavior, but doesn't resolve whether Git should restore the pre-2022 functionality where filters worked at the commit selection level.