This is a follow-up discussion about a bugfix for `git blame`'s handling of missing ignoreRevsFile configurations. The original patch from Francesco Nicoletta Puzzillo addressed an issue where setting `blame.ignoreRevsFile` globally (e.g., in ~/.gitconfig) would cause `git blame` to fail in repositories that didn't have the specified file, even though this feature is meant to be optional.

Kristoffer Haugsbakk's email points to a relevant historical thread from 2021 that appears to discuss similar functionality or related issues with `git blame`'s ignoreRevsFile feature. The reference suggests this might provide additional context about the feature's design or prior discussions about its behavior.

The fix itself (already summarized in the thread context) is straightforward - it modifies `builtin/blame.c` to check file existence before parsing, making the feature properly optional as intended. The patch includes test coverage and maintains backward compatibility. While no reviews are mentioned yet, the change appears uncontroversial as it fixes broken behavior without introducing new complexity.

The reference to the 2021 thread might be useful background for reviewers considering whether this fix aligns with the original design intentions for the ignoreRevsFile feature.