This is a follow-up discussion in the reftable-as-default series, specifically addressing the implementation details of where to set the default ref storage format. The email confirms the decision to keep the default setting in `repository_format_configure()` rather than moving it to `REPOSITORY_FORMAT_INIT`, as previously agreed in the thread.

Patrick Steinhardt declines Karthik Nayak's suggestion to also make `REPOSITORY_FORMAT_INIT` initialize to `_UNKNOWN` for both ref storage and hash algorithm in this series, citing the complexity of the repository format code in setup.c. He prefers to keep the current focused change (adding the explicit else branch in `repository_format_configure()`) and defer broader initialization changes to avoid opening what he describes as a "can of worms".

The exchange shows consensus on the immediate technical approach while identifying a potential future cleanup (making `REPOSITORY_FORMAT_INIT` use `_UNKNOWN` consistently) that could be tackled separately. This maintains the series' focus on preparing reftable as the default backend while acknowledging technical debt in the repository initialization code that may warrant future attention.