This thread discusses a straightforward code cleanup patch that moves the `struct rev_list_info` from bisect.h to builtin/rev-list.c, since the structure is only used by the rev-list implementation. The commit clarifies this was never actually related to bisect functionality despite its previous location.

The change is purely organizational - the struct definition is moved verbatim while being removed from the bisect header. The patch includes two minor incidental fixes: correcting a comment typo in rev-list.c and updating the Git version in the signature. The author describes this as part of ongoing "#leftoverbits" cleanup work.

The structure itself contains pointers to rev_info and various display-related flags, but no functionality changes are made. A NEEDSWORK comment about option parsing remains untouched. This appears to be a simple, uncontroversial improvement to code organization.