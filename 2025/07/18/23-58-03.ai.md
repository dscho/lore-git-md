Refactoring patch moving a data structure to its sole user.

This patch relocates the `struct rev_list_info` from bisect.h to builtin/rev-list.c, since it's only used by the rev-list implementation. The commit message notes this structure was never actually related to bisect functionality. The change is straightforward - the struct definition is moved verbatim while removing it from the bisect header file.

The diff shows two minor incidental changes: fixing a comment typo in rev-list.c ("the options they are" instead of "that the options they are") and updating the Git version in the signature. The patch appears to be part of ongoing codebase cleanup work, referenced as a "#leftoverbits tangent" from a previous discussion.

For future reference: The struct contains pointers to rev_info and various display-related flags (show_timestamp, hdr_termination, header_prefix). This change doesn't modify any functionality - it just better localizes the scope of this implementation detail to its single consumer. The NEEDSWORK comment about option parsing remains unchanged.