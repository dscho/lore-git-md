This is a follow-up in the performance optimization thread about improving `pop_most_recent_commit()` using a priority queue.

René Scharfe responds to Junio's "What's cooking" report regarding his series that converts `pop_most_recent_commit()` to use a priority queue for better worst-case performance. The series currently consists of 4 commits (including one fixup) and is slated for merging to 'next'. 

Scharfe indicates he'll send a v2 soon to incorporate feedback received so far. He's considering whether to make the queue replacement optimization implicit (removing the need for the new `prio_queue_replace()` function), but suggests this might warrant a separate follow-up series rather than being included in the current optimization work.

The exchange shows this is a straightforward performance improvement that's progressing smoothly through review, with the author being responsive to feedback while considering additional cleanups. The priority queue approach appears to be the right direction for this performance-sensitive code path.