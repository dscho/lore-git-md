This thread concerns refactoring to move `struct rev_list_info` (containing rev_info pointers and display flags) from bisect.h to builtin/rev-list.c, as it's only used by rev-list. The original patch from Junio Hamano included two incidental changes: fixing a Git version signature and editing a comment about rev-list's option parsing.

Jeff King initially questioned the comment edit, arguing the original "broken options" phrasing might be more accurate than Junio's edit about problematic patterns. René Scharfe clarified the core issue is parsing loops failing to distinguish options from arguments, supporting Junio's removal of "that" while noting this is tangential to the struct relocation. Peff later acknowledged this, agreeing the edit was correct though awkwardly phrased.

Junio now sends a final iteration updating the NEEDSWORK comment to clearly explain the parsing limitation: the current loop attempts to detect option usage without properly understanding which options take arguments (e.g., failing to recognize `--exclude-promisor-objects` when preceded by `--grep`). The new comment precisely documents the issue and proposes having `setup_revisions()` implement proper early option parsing with command-specific rules as the solution. This concludes the discussion about comment phrasing that began with Peff's initial query.

The core refactoring - moving `struct rev_list_info` to builtin/rev-list.c - remains uncontested throughout the thread. All participants (Junio Hamano, Jeff King, René Scharfe) agree the struct relocation is correct. The discussion has refined the documentation of rev-list's option parsing behavior while maintaining focus on the primary cleanup work. The final NEEDSWORK comment provides clear guidance for future contributors about the known parsing limitation and its potential solution.