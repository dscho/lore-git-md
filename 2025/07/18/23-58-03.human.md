This is a refactoring patch that moves a data structure`rev_list_info` from `bisect.h` to `builtin/rev-list.c`, where it's actually used. The change is straightforward - the structure was incorrectly placed in the bisect header despite being exclusively used by the rev-list command implementation.

Junio Hamano notes this was a leftover from previous work, and the patch simply relocates the structure to be private to its only user. The diff shows the structure being removed from `bisect.h` and added to `rev-list.c` with no functional changes, along with a minor comment typo fix.

The change appears clean and well-justified - the structure clearly doesn't belong in bisect.h if it's only used by rev-list. This is the kind of small but sensible cleanup that helps maintain code organization. The patch includes a signed-off-by and references the previous discussion where this was identified.