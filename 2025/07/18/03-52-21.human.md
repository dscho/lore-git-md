This is a feature patch introducing a new `pull.autoStash` configuration option to complement the existing `rebase.autoStash` and `merge.autoStash` settings. The patch comes from an ongoing thread about configuration inheritance in Git's pull command.

Lidong Yan proposes adding `pull.autoStash` as a higher-level override that takes precedence over the operation-specific settings. The implementation follows the emerging consensus from the thread, where `pull.autoStash` serves as an explicit override when set, while falling back to `rebase.autoStash` or `merge.autoStash` when unset, depending on whether the pull uses rebase or merge.

The patch includes thorough documentation explaining the configuration hierarchy and adds comprehensive test coverage for all combinations of the new option with existing settings. The tests verify behavior with both rebase and merge operations, ensuring the precedence rules work as intended.

This appears to be a well-constructed patch that addresses the thread's goal of clarifying configuration inheritance while maintaining backward compatibility. The implementation is clean, the commit message is clear, and the extensive test coverage gives confidence in the behavior. The patch moves the discussion forward by implementing the agreed-upon design while leaving room for any final adjustments to the documentation or behavior.