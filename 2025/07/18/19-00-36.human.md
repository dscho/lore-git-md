Junio Hamano responds to the Rust-based xxhash optimization patch (6/7 in the series) with a focus on practical integration concerns. While praising the technical demonstration, he notes the current implementation only works when whitespace flags are disabled, creating a functional gap compared to the existing C implementation. He suggests the eventual goal should be a Rust xxhash variant that handles whitespace processing, which could then fully supersede `xdl_hash_record()`.

The email highlights an important ecosystem consideration: third-party consumers like libgit2 and Vim may need to maintain parallel C implementations even if Git moves to Rust, since they embed xdiff as a standalone library. This reinforces the thread's ongoing tension between Git's internal optimization goals and external library compatibility. Junio's comments position this patch as a stepping stone rather than a final solution, with clear technical milestones remaining before the Rust implementation could become the default.