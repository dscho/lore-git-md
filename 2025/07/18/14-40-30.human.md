This is a follow-up in the thread about removing the `path` field from config parsing structures. Phillip Wood acknowledges Junio C Hamano's merge conflict resolution for `builtin/commit.c` where the deprecated `core.commentChar=auto` work was still checking `kvi->path`. The resolution updates four instances to use `origin_type` and `filename` fields instead, maintaining the KEY_SEEN_ONCE/TWICE flagging system.

Phillip confirms the resolution looks good and notes this code will be moved but not substantially changed in his upcoming re-roll of the `core.commentChar` deprecation work, which he plans to send next week. The exchange shows smooth coordination between ongoing refactoring (removing `path`) and feature work (comment char deprecation), with both changes touching similar config callback machinery.