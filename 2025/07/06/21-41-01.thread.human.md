This thread completes a 7-patch series that consolidates scattered tests for command help behavior (the `-h` flag) into a single test file, `t1517-outside-repo.sh`. The effort implements a suggestion from Patrick Steinhardt to unify these tests, which previously lived in various context-specific test files (GPG, reftable, transport operations, etc.).

The series demonstrates that help behavior testing follows a consistent pattern across all command types (plumbing, porcelain, verification, and now transport commands): each test verifies the command exits with code 129 and outputs a usage message, both inside and outside a repository context. The final patch moves the `send-pack -h test from t5400-send-pack.sh, completing the proof that help tests are independent of their original contexts - in this case showing transport command help works the same way regardless of repository state.

All seven command tests now follow the same template established by the initial `checkout-index` move: using `test_expect_code 129` and case-insensitive `test_grep` for the usage string. The series makes no behavior changes - it's purely an organizational improvement that will make these tests easier to maintain. With the `send-pack` test now relocated, the series is complete, having successfully unified help tests for commands across all major categories.