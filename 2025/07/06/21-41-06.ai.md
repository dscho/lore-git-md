Test reorganization series (5/7): Moving `send-pack -h` test to dedicated outside-repo test file.

This patch continues the established pattern of relocating command help tests (-h flag behavior) to t1517-outside-repo.sh, now addressing `git send-pack`. The test verifies the command exits with code 129 and shows usage information in both repository and non-repository contexts, matching the series' consistent approach of case-insensitive pattern matching via test_grep.

The test moves from t5400-send-pack.sh where it was somewhat isolated from other help tests. Like previous patches in the series, this is purely an organizational change with no behavior impact. The implementation follows the same template seen in earlier patches (checkout-index, for-each-ref, ls-files, pack-refs), maintaining uniformity across the test suite.

Key technical details for future reference: The test checks exit code 129 and case-insensitive usage pattern for `git send-pack` in both git and nongit contexts. The original test in t5400 had identical verification logic, confirming this is a straightforward relocation. The patch leaves two remaining commands (verify-commit, verify-tag) to complete the 7-command consolidation effort.