This is a test reorganization patch series, part of the ongoing effort to consolidate command help tests that should run outside a repository context. The series moves `-h` flag tests for several Git commands (`checkout-index`, `for-each-ref`, `ls-files`, `pack-refs`, `send-pack`, `verify-commit`, `verify-tag`) from their individual test files to the dedicated `t1517-outside-repo.sh` test script.

The motivation comes from Patrick Steinhardt's suggestion that help tests checking command behavior outside a repository belong in this centralized location, following the pattern established by 43a8391977 which moved similar tests for `verify-tag`. The changes are straightforward - each patch in the 7-part series removes a test from its original file and adds it to t1517 with identical content. The series shows good attention to detail, with v2 specifically noting typo fixes from the initial submission.

For future thread reference: The changes touch test files across multiple subsystems (ref handling, index operations, remote operations, commit/tag verification) but all follow the same mechanical pattern. No behavior changes are introduced - this is purely a test file reorganization. The series appears uncontroversial and aligns with existing test organization principles, though reviewers may want to verify the moved tests don't have any hidden repository dependencies.