Feature discussion for `git grep` behavior modification.

Phillip Wood points out that the existing `--max-count` option in `git grep` already provides functionality similar to what Markus Elfring was proposing - the ability to limit matches per file. This suggests the desired behavior might be achievable without adding new flags, though it's not yet clear if `--max-count=1` would be an exact substitute for Markus's use case of seeing just the first matching line per file.

The thread continues exploring whether existing options can satisfy the need before considering new functionality. Phillip's observation moves the discussion toward practical implementation details rather than abstract use cases. This may help focus the conversation on whether `--max-count` meets the requirements or if there are subtle differences that would still warrant a dedicated flag.

Key technical detail is the `--max-count` option's behavior when set to 1, which would show only the first match per file while still displaying the matching content (unlike `-l` which shows only filenames). The thread now has concrete implementation alternatives to evaluate rather than hypothetical solutions.