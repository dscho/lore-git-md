This is a test reorganization patch, part of a completed 7-patch series consolidating command help tests into t1517-outside-repo.sh. The patch moves the `ls-files -h` test from t3004-ls-files-basic.sh to the dedicated outside-repo test file, following the established pattern seen in previous patches of the series.

The test verifies that `git ls-files -h` exits with code 129 and shows usage information, both inside and outside a repository. The move is justified by the test's focus on command behavior rather than ls-files-specific functionality. The patch maintains the original test logic while relocating it to be with similar help tests for other commands.

This follows the same template seen in earlier patches: dual-context testing (git and nongit), exit code verification, and case-insensitive usage pattern matching via test_grep. The series has already established this as the standard approach for help tests, with this patch confirming the pattern works for ls-files just as it did for the six previously moved commands (checkout-index, for-each-ref, pack-refs, send-pack, verify-commit, and verify-tag).

The patch is cleanly implemented with symmetric file changes - removing the test from its original location while adding it to the new file with identical verification logic. This completes the ls-files portion of the broader test consolidation effort, leaving no loose ends specific to this command.