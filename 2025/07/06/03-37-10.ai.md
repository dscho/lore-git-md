This is a reply in the ongoing thread about adding X-Change-ID headers for commit emails. Jeff King responds to Drew DeVault's concern about testing the change-id functionality by providing a technical workaround for creating test commits with change-ids in Git's test suite.

The email offers a detailed shell snippet demonstrating how to programmatically create a commit with a change-id line by manipulating the commit object directly through `git cat-file` and `git hash-object`. This bypasses the need for third-party tools that would normally generate these identifiers. Jeff notes this could be useful for testing even if the feature itself remains controversial.

Key technical details include the use of `perl` to inject the change-id line and the full object-hash workflow with `git hash-object -w` and `git update-ref`. The response is neutral on the feature's merits, explicitly stating no position on whether change-ids should use commit headers versus trailer lines, while acknowledging that discussion exists around this design choice.

The reply maintains the thread's open questions about testing strategy and header format while providing concrete technical guidance should the patch proceed. It does not address the other outstanding questions from the thread summary about X- prefix convention or core-vs-external placement.