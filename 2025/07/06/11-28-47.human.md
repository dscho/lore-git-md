This is a feature proposal for `git grep`. Markus Elfring suggests adding functionality to limit search results to just the first match per file, rather than showing all matches as the command currently does. The email is quite brief and doesn't provide implementation details or use cases - it simply poses the question of whether there would be interest in such a feature. 

The proposal seems straightforward but lacks context about why this would be useful or how it would differ from existing options like `-m` (which limits total matches) or post-processing with `head -1`. Without more technical details or motivating examples, it's hard to assess how compelling this feature would be or whether it warrants changing core Git functionality versus being handled by wrapper scripts. The email reads more like an initial inquiry than a fully-formed proposal.