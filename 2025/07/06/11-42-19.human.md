This is a brief response in an exploratory thread about modifying `git grep` behavior. Andreas Schwab points out that the existing `-l`/`--files-with-matches` option already provides similar functionality to what Markus Elfring was asking about - it shows which files contain matches rather than all individual matches. The reply suggests the desired behavior may already be available rather than needing new functionality.

The exchange is still at an early stage of determining whether there's actually a need for new behavior beyond what `git grep -l` provides. No technical changes are proposed in this email - it's simply pointing to existing functionality that may satisfy the use case. The thread may develop further with clarifications about whether `-l` fully meets the need or if additional behavior is still desired.