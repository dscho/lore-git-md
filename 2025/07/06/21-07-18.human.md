This is a test reorganization patch series, moving builtin command help tests to a more appropriate location. The series relocates `-h` flag tests for several commands (`checkout-index`, `for-each-ref`, `ls-files`, etc.) from their individual test files to the shared `t1517-outside-repo.sh` test script.

The motivation comes from Patrick Steinhardt's earlier suggestion that tests verifying command behavior outside a repository belong together in this dedicated test file. The changes are mechanical - each patch removes identical help test blocks from various test files and adds them to t1517. The series touches 8 test files, with 49 lines added and 49 removed in total.

This appears to be a straightforward cleanup following established patterns. The changes are focused and well-scoped, with each commit handling one command's test migration. The series builds on prior work to consolidate repository-agnostic tests, though the cover letter doesn't specify whether this completes the effort or if more commands remain to be moved.