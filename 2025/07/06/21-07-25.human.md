This is the final patch in a test refactoring series that consolidates builtin command help tests into a shared script. The patch moves the `verify-tag -h` test from `t7030-verify-tag.sh` to `t1517-outside-repo.sh`, completing the series' goal of grouping tests for command behavior outside repositories.

The change follows the same pattern as the previous six patches in the series, relocating a test that verifies a command's `-h` flag produces exit code 129 and proper usage output, both inside and outside a repository. The test content remains identical, just moved to a more logical location where similar tests live.

With this patch, the series achieves its aim of cleaning up test organization by moving seven identical test patterns from individual command-specific test files to a centralized location. The `verify-tag` test file can now focus more narrowly on actual tag verification functionality rather than basic command interface testing. The change is mechanical and low-risk, maintaining all existing test coverage while improving maintainability.