This is the final patch in a test refactoring series, moving the `verify-tag -h` test to consolidate builtin command help tests in `t1517-outside-repo.sh`. The patch follows the established pattern of the series, relocating a 7-line test block that verifies exit code 129 and usage message output for the `verify-tag` command both inside and outside a repository.

The test moves from `t7030-verify-tag.sh` (which focuses on GPG/signing functionality) to the more general-purpose `t1517` script, keeping the same test description and assertions. This leaves `t7030` more focused on actual tag verification behavior while grouping all similar help-text tests together. The change is purely organizational - the test content remains identical, just in a new location that better matches its purpose.

With this patch, the series completes its goal of relocating seven identical help-text test blocks for various commands (`checkout-index`, `for-each-ref`, `ls-files`, `pack-refs`, `send-pack`, `verify-commit`, and now `verify-tag`).

Key details:
- Maintains same test conditions (in-repo and `nongit` cases)
- Preserves GPG test prerequisite from original location
- Keeps identical `test_grep` pattern for usage message verification
- Follows same diff structure as previous patches (7 lines removed, 7 added)
- Completes the series' coverage of builtin commands with identical help tests

The change is straightforward and low-risk, being the last in a series of mechanical relocations that have already established the approach. Reviewers need only verify the test description makes sense in its new context, as the test logic itself is unchanged.