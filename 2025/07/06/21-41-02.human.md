Test reorganization patch moving a `checkout-index -h` test to the dedicated outside-repo test file. This is part of a v2 series consolidating help tests that should run outside repository context into t1517-outside-repo.sh, following Patrick Steinhardt's suggestion.

The patch moves the test verifying that `git checkout-index -h` shows usage information both inside and outside a repository from t2006-checkout-index-basic.sh to t1517-outside-repo.sh. The test content remains identical - this is purely an organizational change to group similar tests together. The series appears well-motivated and follows an established pattern from a previous similar test move. Each patch in the series handles one command's help test, making for straightforward review. The v2 iteration suggests the author has addressed initial feedback (likely typo fixes mentioned in the thread context).