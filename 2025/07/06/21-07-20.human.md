This is the second patch in a test refactoring series that consolidates help-text tests for various Git commands into a shared test file. The patch moves the `for-each-ref -h` test from its original location in `t6300-for-each-ref.sh` to the more appropriate `t1517-outside-repo.sh`, which specializes in testing command behavior outside repositories.

The change is purely organizational - the test itself remains identical, still verifying that `git for-each-ref -h` exits with code 129 and shows the usage message both inside and outside a repository. This follows the pattern established in the first patch of the series (which moved `checkout-index -h`) and continues the effort to group similar tests together for better maintainability.

The patch appears well-executed, with clean diffs showing the test being removed from one file and added to another without modifications. As with the rest of the series, this is a straightforward mechanical change that improves test organization while maintaining all existing coverage.