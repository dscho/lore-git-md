Updated AI thread summary:

Thread summary:

Bug report and documentation discussion regarding `git stash create`/`store` message handling discrepancies. José Miguel Armajo Fidalgo initially reported (with Darren Rambaud confirming) that custom messages in `git stash create` appear in commit objects but not in reflog entries, which show generic "Created via git stash store" messages. Jeff King's technical analysis identified the root cause in `stash.c`'s split handling between `create_stash()` (sets commit message) and `do_store_stash()` (overwrites reflog message).

Junio C Hamano provided historical context through a documentation patch clarifying which commands accept custom messages (`push`, `save`, `store`), explaining `git stash list` behavior, and noting `git stash create` is primarily for internal script use. Jeff King initially suggested improving `git stash store` ergonomics for script writers by having it pull default messages from commits, but later expressed reservations about this change given the command's limited real-world usage over 12 years, particularly outside `rebase.autostash` contexts.

José Miguel Armijo Fidalgo's follow-up confirms they were exploring functionality without a specific use case, addressing Jeff's request for motivation. They highlight key documentation gaps: (1) failure to explain `git stash create` produces regular commit objects usable beyond stash operations (for branching, cherry-picking etc.); (2) suggest the commands' obscurity may stem from poor documentation creating a "chicken-egg" problem where unclear docs discourage usage.

Current technical aspects remain unchanged in `stash.c`: the separation between `create_stash()` (commit messages) and `do_store_stash()` (reflog messages). The discussion has shifted from potential behavior changes to pure documentation improvements, with consensus forming around updating `Documentation/git-stash.adoc` to better explain:
- Command purposes and capabilities
- Commit object reusability from `create`
- Current message handling behavior
- Primary use cases vs. scripting interfaces

No code changes to message handling are planned without stronger use cases. The thread appears resolved with documentation as the agreed solution, though the door remains open for future ergonomic improvements if concrete scripting needs emerge. Key participants' positions: Junio (documentation focus), Jeff King (skeptical of behavior changes without use cases), original reporter (now satisfied with doc solution).