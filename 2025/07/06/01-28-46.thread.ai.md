Thread summary:

Bug report and analysis of `git stash create`/`store` message handling discrepancy, with discussion converging on documentation versus implementation solutions. José Miguel Armajo Fidalgo initially reported and Darren Rambaud confirmed that custom messages in `git stash create` appear in commit objects but not in reflog entries, which show a generic "Created via git stash store" message. Jeff King's technical analysis identified the root cause in `stash.c`'s split handling between `create_stash()` (sets commit message) and `do_store_stash()` (overwrites reflog message).

José now proposes documenting this behavior as an explicit resolution path, reinforcing one of Jeff's two suggested solutions (documentation clarification vs implementation change). The thread has established clear technical details: commit messages persist correctly while reflog messages get overwritten, with reproduction steps demonstrating the distinction via `git log` versus `git stash list`. The discussion now centers on whether to accept this as expected behavior (requiring clearer documentation) or modify `stash store` to preserve commit messages when no explicit reflog message is given.

Key technical aspects remain the `stash.c` implementation's separation of commit and reflog message handling, with the current behavior creating a potential pitfall for users expecting message consistency. The conservative documentation approach would maintain backward compatibility while making the behavior more discoverable, whereas a code change would align the behaviors but risk impacting existing workflows. No new technical objections or alternative solutions have emerged since Jeff's initial analysis, suggesting the thread may be nearing resolution with either a documentation patch or a small behavioral tweak to `do_store_stash()`.