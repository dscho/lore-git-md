Test refactoring continuing the series to consolidate `-h` flag tests. This is patch 4/7, moving the `pack-refs` command's help test from `t0610-reftable-basics.sh` to the shared `t1517-outside-repo.sh` location. The test verifies `pack-refs -h` shows usage information and exits with code 129 both inside and outside a repository.

The change follows the established pattern: removing a 7-line test block from its original file (here, the reftable test suite) and adding it to `t1517` with identical assertions. The test description "pack-refs does not crash with -h" is preserved. This maintains coverage while better organizing tests by behavior rather than implementation details.

Technical details:
- Moves test from reftable-specific file to general behavior test file
- Keeps same exit code (129) and usage message verification
- Maintains both in-repo and out-of-repo test cases
- Follows same format as previous patches in series
- Next commands to relocate: `send-pack`, `verify-commit`, `verify-tag`

The patch continues the mechanical, uncontroversial relocation work with no functional changes. Reviewers only need to confirm the test works as before in its new location. The reftable test file becomes slightly more focused on reftable-specific behavior by removing this generic test.