Test refactoring continuing the consolidation of builtin command help tests into t1517-outside-repo.sh. This is patch 6/7 in the series, relocating the `verify-commit -h` test from t7510-signed-commit.sh to the shared location.

The patch moves the standard 7-line test block that verifies `verify-commit` exits with code 129 and shows usage text when given the `-h` flag, both inside and outside a repository. The test is identical to its previous version, just moved to better align with Patrick Steinhardt's suggestion for grouping tests of command behavior outside repositories. The test description ("verify-commit does not crash with -h") is preserved during the move.

This follows the same pattern as the previous 5 patches in the series, maintaining consistency in the test relocation approach. The test file t7510-signed-commit.sh becomes more focused on actual commit signing/verification functionality after this removal. One command (`verify-tag`) remains to be moved in the final patch of the series.

Key technical details:
- Moved from: t7510-signed-commit.sh (GPG/signing test file)
- Moved to: t1517-outside-repo.sh (shared behavior file)
- Test verifies: exit code 129 and usage message pattern match
- Maintains both in-repo and nongit test cases
- Uses same test_grep pattern for usage message verification
- Follows established refactoring pattern from 43a8391977
- Series progress: 6/7 commands relocated (verify-tag remains)