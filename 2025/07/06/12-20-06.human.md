This is a follow-up in the thread about modifying `git grep` to show only the first match per file. Markus Elfring responds to Andreas Schwab's suggestion that the existing `-l/--files-with-matches` flag already provides similar functionality by showing just filenames with matches.

Markus acknowledges the existing option but suggests there may still be value in seeing the actual matching line content (rather than just the filename) when only wanting the first match per file. This keeps the discussion open about whether there are use cases where displaying the first match's content would provide more context than `-l` alone.

The exchange remains exploratory - no concrete implementation is proposed yet, but it surfaces the key question of whether the additional context of seeing the first match (versus just knowing a file contains matches) would be useful enough to warrant new functionality. The thread appears to be in the early stages of evaluating whether there's a gap in current grep capabilities.