This is a test refactoring patch series that moves builtin command help tests to a more appropriate location. The series relocates `-h` flag tests for several commands (`checkout-index`, `for-each-ref`, `ls-files`, `pack-refs`, `send-pack`, `verify-commit`, `verify-tag`) from their individual test files to the shared `t1517-outside-repo.sh` test script.

The motivation comes from Patrick's suggestion that tests verifying command behavior outside a repository belong together in the dedicated `t1517` file. The changes are mechanical - each patch removes identical 7-line test blocks from various test files and adds them to `t1517` with appropriate descriptions. The series maintains test coverage while improving test organization.

Key technical details:
- Affects 8 test files (7 removals, 1 addition)
- Each moved test verifies basic `-h` flag functionality for its command
- No behavior changes - purely moving existing tests
- Follows precedent set by commit 43a8391977 which did similar moves for other commands

The series appears well-structured with each commit handling one command's test move. The changes are straightforward and unlikely to be controversial, though reviewers may want to verify the test descriptions in the new location are sufficiently clear.