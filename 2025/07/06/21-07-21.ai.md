Test refactoring continuing the consolidation of builtin command help tests into t1517-outside-repo.sh. This is patch 3/7 in the series, moving the `ls-files -h` test from t3004-ls-files-basic.sh to the shared location.

The patch follows the established pattern from previous patches in the series, mechanically relocating the identical 7-line test block that verifies `ls-files -h` behavior both inside and outside a repository. The test checks for exit code 129 and proper usage message output in both scenarios, matching the verification pattern used for `checkout-index` and `for-each-ref` in earlier patches.

Key technical details:
- Moves test from t3004-ls-files-basic.sh to t1517-outside-repo.sh
- Maintains identical test conditions and assertions
- Preserves the test description "ls-files does not crash with -h"
- Follows same diff structure as previous patches (7 lines removed, 7 identical lines added)
- Continues the systematic relocation approach suggested by Patrick Steinhardt

The change is straightforward and uncontroversial, being the third in a series of mechanical test relocations. The remaining patches will follow the same pattern for `pack-refs`, `send-pack`, `verify-commit`, and `verify-tag` commands. Reviewers need only verify the test description clarity in the new location, as the test content itself remains unchanged.