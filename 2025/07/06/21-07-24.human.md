This is the sixth patch in a test refactoring series, moving the `verify-commit -h` test to a more appropriate location. The patch relocates a standard help-text verification block from `t7510-signed-commit.sh` to the shared `t1517-outside-repo.sh` file, which collects tests for command behavior outside repositories.

The change follows the same pattern as previous patches in the series: a 7-line test block that checks both in-repo and out-of-repo behavior of the `-h` flag (exit code 129 and usage message) is moved to the centralized location. The test description remains identical ("verify-commit does not crash with -h") and the test conditions are preserved exactly.

This continues the systematic cleanup started by Patrick Steinhardt's suggestion, with `verify-commit` being the sixth of seven commands getting this treatment. The move makes sense as the help behavior is not specific to signed commits, and the test file can now focus more narrowly on GPG-related functionality. One command (`verify-tag`) remains to be moved in the final patch of the series.

The patch appears mechanically sound - it's a straightforward file move with no behavior changes, just better test organization. The commit message clearly explains the motivation and maintains the series' consistent approach.