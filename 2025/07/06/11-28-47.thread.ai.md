Thread proposing modification to `git grep` to show only first match per file rather than all matches. Original proposal by Markus Elfring sought behavior between current `git grep` (all matches) and `git grep -l` (just filenames). Discussion has explored two existing alternatives: `-l` (shows only filenames) and `--max-count=1` (shows first matching line per file). 

Current status has shifted to examining programmatic access considerations. Markus Elfring's latest email probes whether the same information from `-l` can be obtained through Git's internal C functions and whether file details can be transferred through other APIs. This suggests evaluation of existing options now includes API accessibility, not just command-line behavior. No implementation has been proposed yet.

Key technical details remain: `--max-count=1` limits matches per file while showing content (unlike `-l`), making it a candidate solution. New dimension is API accessibility of these features. Open questions now include: (1) whether existing options satisfy programmatic use cases, (2) if internal C functions expose equivalent functionality to `-l`, and (3) whether API limitations might still warrant a dedicated flag despite command-line alternatives existing.

Key participants: Markus Elfring (original proposer, now focusing on API aspects), Andreas Schwab (noted `-l` similarity), Phillip Wood (identified `--max-count` alternative). Discussion remains focused on evaluating solutions rather than proposing new functionality.