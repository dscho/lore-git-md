Thread proposing modification to `git grep` to show only first match per file rather than all matches. Original proposal by Markus Elfring sought behavior between current `git grep` (all matches) and `git grep -l` (just filenames). Andreas Schwab noted `-l` provides similar functionality but without showing matching content. Phillip Wood pointed out that `--max-count=1` may already provide the desired behavior of showing first matching line per file while still displaying content.

Current status has shifted from abstract discussion to evaluating existing options, with `--max-count=1` emerging as potential solution. Open questions: whether `--max-count=1` exactly matches Markus's use case requirements, and if any subtle behavioral differences would still warrant a dedicated flag. Technical detail: `--max-count` limits matches per file while still showing content (unlike `-l`), making it a candidate solution. Discussion now focused on practical evaluation of existing options rather than hypothetical new functionality.

Key participants: Markus Elfring (original proposer), Andreas Schwab (noted `-l` similarity), Phillip Wood (identified `--max-count` alternative). No implementation proposed yet as discussion explores whether existing options suffice.