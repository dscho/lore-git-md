This is the first patch in a 7-part test refactoring series that consolidates help tests (-h flag behavior) into a dedicated test script. The patch moves the `checkout-index -h` test from t2006-checkout-index-basic.sh to t1517-outside-repo.sh, where it better fits with other tests of command behavior outside a repository.

The test verifies that `git checkout-index -h` shows usage information and exits with code 129, both inside and outside a Git working tree. The patch follows a consistent pattern seen throughout the series - maintaining the same test logic while relocating it to a more appropriate file. The test uses `test_grep` with case-insensitive matching to check the usage output, matching the approach used for other commands in the series.

This appears to be a straightforward organizational improvement with no behavior changes. The patch is cleanly implemented, removing the test from its original location while preserving all verification logic. As the first patch in the series, it establishes the pattern that will be followed for the remaining six command tests. The change was suggested by Patrick Steinhardt as part of broader test consolidation efforts.