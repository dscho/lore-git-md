This is the final patch in a completed 7-part test refactoring series that consolidates command help tests (-h flag behavior) into a dedicated test file. The patch moves the verify-tag help test from t7030-verify-tag.sh to t1517-outside-repo.sh, following the established pattern seen in previous patches.

The change is straightforward - it relocates the test verifying that `git verify-tag -h` shows usage information (with exit code 129) both inside and outside a repository. The test follows the same template as others in the series: checking exit code, case-insensitive usage pattern matching, and testing in both git and nongit contexts.

This completes the series' goal of unifying scattered help tests into a single location, demonstrating that the pattern works consistently across different command types (including this GPG-related one). The patch maintains all original test behavior while improving test organization. The series has already addressed feedback in v2, and this final patch cleanly wraps up the consolidation effort.