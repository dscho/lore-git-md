Test refactoring continuing the consolidation of builtin command help tests. This is patch 3/7 in a series moving identical 7-line test blocks that verify `-h` flag behavior (exit code 129 and usage message) from individual test files to the shared `t1517-outside-repo.sh`.

This patch relocates the `ls-files -h` test from `t3004-ls-files-basic.sh`, maintaining the same assertions about command behavior both inside and outside a repository. The change is purely organizational - the test content remains identical, just moved to a more logical location following Patrick Steinhardt's suggestion.

The series is progressing systematically, having already moved tests for `checkout-index` and `for-each-ref`. The mechanical nature of these changes makes them straightforward to review, with the main consideration being whether the consolidated test file remains well-organized. This patch follows the same uncontroversial pattern as the previous ones in the series.