This is a test reorganization patch, part of a 7-patch series consolidating command help tests into t1517-outside-repo.sh. The patch moves the `for-each-ref -h` test from t6300-for-each-ref.sh to the new consolidated location, following the established pattern of verifying exit code 129 and case-insensitive usage message matching in both repository and non-repository contexts.

The test being moved checks that `git for-each-ref -h` shows usage information and exits cleanly (with status 129) regardless of whether it's run inside or outside a Git repository. The test logic remains identical during the move - it's simply being relocated to join similar tests for other commands. The patch removes the test from its original location in t6300-for-each-ref.sh where it was surrounded by format-string related tests, as the help behavior is independent of the ref-filter formatting logic tested there.

This follows the same template demonstrated in the first patch of the series (which moved checkout-index tests) and maintains the dual-context approach (git vs nongit) and verification pattern (exit code + case-insensitive usage matching) that unifies all seven command tests in the series. The move is straightforward with no functional changes, just file relocation.