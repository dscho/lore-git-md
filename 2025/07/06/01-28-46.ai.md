Bugfix for `git stash create` message argument behavior. This report identifies a discrepancy where the message argument passed to `git stash create` is ignored when the created stash is later stored and listed, instead showing a generic "Created via git stash store" message. The reporter (José Miguel Armijo Fidalgo) demonstrates this with a clear reproduction case showing the expected vs actual behavior mismatch.

The issue appears to be either a documentation gap (where the argument's purpose isn't explained) or a bug in the implementation where the message should be preserved through the store operation. The reporter suggests two possible resolutions: either remove the unused argument or document its actual behavior. The bug report includes full system information showing the issue was observed on macOS with Git 2.50.0.

This appears to be a straightforward documentation/behavior alignment issue in the stash subsystem, where the `create` subcommand's interface doesn't match its actual behavior when combined with `store`. The report is well-formed with clear reproduction steps and system context, making it easy to verify and address. The core question is whether this represents an undocumented limitation that should be explained, or a bug where the message should propagate through the stash storage process.