Test refactoring continuing the consolidation of builtin command help tests. This is patch 5/7 in a series moving identical `-h` flag verification tests from individual test files to the shared `t1517-outside-repo.sh` script.

Usman Akinyemi moves the `send-pack` command's help test from `t5400-send-pack.sh` to the consolidated location, following the same pattern as previous patches in the series. The test verifies that `git send-pack -h` exits with code 129 and shows usage information, both inside and outside a repository. The change is purely organizational - the test content remains identical, just relocated to better match Patrick Steinhardt's suggestion for grouping tests of command behavior outside repositories.

This continues the mechanical, uncontroversial series that has already relocated similar tests for `checkout-index`, `for-each-ref`, `ls-files`, and `pack-refs`. Two commands (`verify-commit` and `verify-tag`) remain to be moved in subsequent patches. Reviewers need only confirm the test description makes sense in its new location, as the test logic itself is unchanged.