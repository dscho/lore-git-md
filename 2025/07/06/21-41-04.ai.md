Test reorganization moving `ls-files -h` help test to consolidated outside-repo test file. This is the third patch in a 7-part series consolidating command help tests that should run outside repository context into t1517-outside-repo.sh, following Patrick Steinhardt's suggestion.

The patch moves the 'ls-files does not crash with -h' test from t3004-ls-files-basic.sh to t1517-outside-repo.sh. The test verifies that `git ls-files -h` exits with code 129 (standard for usage errors) and shows the usage message, both inside and outside a repository context. The test uses case-insensitive pattern matching (`[Uu]sage`) to accommodate variations in output formatting.

The change is purely organizational - the test behavior remains identical, just relocated to be with similar tests. The patch follows the same pattern as previous moves in the series (checkout-index and for-each-ref tests), maintaining consistency in verifying both repository and non-repository contexts despite the "outside-repo" filename. The test removal from t3004-ls-files-basic.sh leaves that file focused on core ls-files functionality tests.

This continues the series' clean approach to test reorganization with no apparent technical concerns. The patch appears ready for review, with the main consideration being whether reviewers agree this test properly belongs in the outside-repo category given it tests both contexts. The series maintains good momentum toward its goal of consolidating scattered help tests.