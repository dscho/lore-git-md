Git thread summary:

This thread introduces a test refactoring series that relocates builtin command help tests (`-h` flag verification) for multiple commands to the shared `t1517-outside-repo.sh` script, following Patrick Steinhardt's suggestion for consolidating tests of command behavior outside repositories. The series consists of 7 mechanical moves of identical 7-line test blocks from individual test files to `t1517`, maintaining coverage while improving organization.

The first patch (1/7) moved the `checkout-index -h` test from `t2006-checkout-index-basic.sh` to `t1517`, preserving the established test pattern that verifies exit code 129 and usage message content both inside and outside a repository. The second patch (2/7) now moves the `for-each-ref -h` test from `t6300-for-each-ref.sh` to `t1517` using the same approach - a direct relocation of the 7-line test block with identical assertions, only changing the file location.

Technical details:
- Affects 8 test files (7 removals, 1 addition)
- Each moved test verifies basic `-h` flag functionality (exit code 129 + usage message)
- Maintains identical test conditions and assertions
- Follows established refactoring approach from 43a8391977
- Current progress: 2/7 commands relocated (`checkout-index`, `for-each-ref`)

The series continues systematically with straightforward mechanical moves that improve test organization without behavior changes. Remaining patches will relocate tests for `ls-files`, `pack-refs`, `send-pack`, `verify-commit`, and `verify-tag` in the same manner. The changes remain uncontroversial, with reviewers only needing to verify the new test descriptions in `t1517` are sufficiently clear.