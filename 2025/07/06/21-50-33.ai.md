Test reorganization patch moving `checkout-index -h` verification from t2006 to t1517, part of a completed series consolidating command help tests. This first patch relocates the checkout-index help test to the dedicated outside-repo test file, following Patrick Steinhardt's suggestion to unify these tests. The test verifies exit code 129 and case-insensitive usage message matching in both repository and non-repository contexts, matching the pattern established across all seven commands in the series.

The patch removes the test from t2006-checkout-index-basic.sh where it was originally placed, though the commit message notes this was already testing behavior both inside and outside a repository. The new location in t1517-outside-repo.sh groups it with similar tests for other commands, improving maintainability. The test logic remains identical - it still checks the same exit code and usage pattern matching via test_grep.

This initial move demonstrates the series' consistent approach: each patch relocates a command's help test while preserving its verification of the standardized behavior (exit code 129, usage message presence). The checkout-index test was a straightforward first candidate since its dual-context testing already matched the target pattern, requiring no behavioral changes beyond the file location.