This is a bugfix patch in the ongoing SSH signing tempfile cleanup thread. It addresses a memory management issue in the SSH signing code where detaching a strbuf from a tempfile structure could prevent proper cleanup.

The patch makes two key changes:
1. Replaces `strbuf_detach()` with `xstrdup()` in `sign_buffer_ssh()` to maintain symmetry with the other code path that uses `interpolate_path()`, while ensuring the tempfile gets properly cleaned up
2. Adds a new test case in t7528 that verifies no temporary files remain after commit signing operations, using a dedicated tmpdir and `find` for robust checking (addressing prior feedback about test methodology)

The v2 iteration includes both the technical fix and improved test coverage, with brian m. carlson providing a Helped-by. The change is small but important for resource cleanup in line with the thread's original tempfile leak fix. The test addition follows the collaborative refinement seen throughout this thread, now using proper isolation and verification techniques.

Key technical details for future reference:
- Modified file: gpg-interface.c (ssh signing path)
- Test file: t/t7528-signed-commit-ssh.sh
- Tempfile verification now uses `find` in isolated `tmpdir`
- Maintains symmetry between key file handling paths
- Follows from prior fix of tempfile leak in same function