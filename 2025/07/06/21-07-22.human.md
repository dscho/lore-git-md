This is the fourth patch in a test refactoring series consolidating builtin command help tests. The patch moves the `pack-refs -h` test from `t0610-reftable-basics.sh` to the shared `t1517-outside-repo.sh` script, following the same pattern as previous patches in the series.

The test verifies that `git pack-refs -h` shows usage information and exits with code 129, both inside and outside a repository. The relocation is mechanical - the test content remains identical, just moved to a more logical location with other similar tests of command behavior outside repositories. This continues the systematic cleanup started in earlier patches, with `pack-refs` being the fourth of seven commands being relocated.

The change is straightforward and low-risk, matching the established refactoring approach. Three commands remain to be moved in subsequent patches: `send-pack`, `verify-commit`, and `verify-tag`. The series appears well-organized and maintains test coverage while improving code organization.