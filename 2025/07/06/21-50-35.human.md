Test reorganization patch moving the `ls-files -h` help test to a consolidated location. This is part of a completed 7-patch series that relocates scattered help tests (-h flag behavior) into a dedicated t1517-outside-repo.sh script, following Patrick Steinhardt's suggestion.

The patch moves the test verifying `ls-files -h` behavior from t3004-ls-files-basic.sh to t1517-outside-repo.sh, where it joins similar tests for other commands. The test checks that `ls-files` shows usage information and exits with code 129 both inside and outside a repository, matching the now-standard pattern for help tests. The original test is removed from its old location with no functional changes.

This continues the series' clean pattern of test consolidation - each patch moves one command's help test while preserving its verification logic. The ls-files test fits naturally with the other outside-repo behavior checks in t1517, despite originally living in a basic functionality test file. The series as a whole demonstrates that help behavior is independent of command context (GPG, reftable, etc), making this consolidation both possible and beneficial for maintenance.