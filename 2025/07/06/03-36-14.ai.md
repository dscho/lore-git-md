Bugfix discussion for `git stash` message handling discrepancy. Darren Rambaud provides additional technical details confirming the current behavior where custom messages from `git stash create` appear in the commit object's message (visible via `git log` when checked out) but not in the reflog entry shown by `git stash list`. The demonstration shows the commit properly retains "On main: changes to readme" while the reflog shows the generic "Created via git stash store" message.

This email reinforces the technical distinction between commit messages and reflog messages that was previously identified in the thread. The reproduction steps specifically show how to observe both message locations, confirming that the issue isn't about message loss but rather about where each message appears. The example uses a detached HEAD state to inspect the commit object directly, providing a clear verification method for the underlying behavior.

Key technical points: The commit SHA created by `stash create` contains the custom message in its object metadata, while `stash store` writes a fixed reflog message regardless of the commit's content. This aligns with Jeff King's earlier analysis but provides concrete command-line evidence of the current behavior. The email doesn't propose a solution but serves as a data point confirming the need for either documentation clarification or behavioral change as previously discussed.