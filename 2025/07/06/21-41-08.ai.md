Test reorganization series completion moving verify-tag help test to dedicated outside-repo script.

This is the final patch in a 7-part series consolidating command help tests (-h flag behavior) into t1517-outside-repo.sh. It moves the verify-tag test from t7030-verify-tag.sh, completing the series' goal of gathering these tests in one place. The test verifies that `git verify-tag -h` shows usage information and exits with code 129, both inside and outside a repository.

The patch follows the established pattern seen in previous patches: removing the test from its original location (here, a GPG/signing context test file) and adding it to t1517 with identical verification logic. The test maintains the dual-context checking (git and nongit) and case-insensitive usage pattern matching that has been consistent throughout the series.

Key technical details include the removal of the GPG test prerequisite that was present in the original location, confirming this test doesn't actually depend on GPG functionality despite coming from a GPG-focused test file. The diff shows the straightforward movement of the test with no behavioral changes, just like the previous six patches in the series.

This completes the consolidation effort that Patrick Steinhardt suggested, having now moved tests for checkout-index, for-each-ref, ls-files, pack-refs, send-pack, verify-commit, and finally verify-tag. The series demonstrates a clean organizational improvement with no functional impact, maintaining all existing verification while making the tests more maintainable through consolidation.