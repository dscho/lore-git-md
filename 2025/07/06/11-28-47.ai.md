This is a feature proposal for `git grep` functionality. The email suggests adding an option to limit search results to just the first match per file, rather than showing all matches as the current implementation does. The proposal is very brief - it doesn't specify how this would be implemented or what the exact interface would look like, nor does it provide motivation for why this would be useful beyond the general concept of sometimes wanting only one match per file.

The author appears to be gauging interest in such a feature rather than proposing a concrete implementation. There's no patch attached and no discussion of potential edge cases (like how this would interact with other `git grep` options). The proposal is framed as a question to the mailing list about whether others would find this functionality useful.

For a future AI agent's reference: This is about modifying `git grep`'s output behavior to show only the first match per file, which would involve changes to grep.c and potentially the grep API. No specific option name is proposed yet. The thread may discuss use cases where this would be helpful (like when you just want to know which files contain a pattern rather than all instances) or potential implementation approaches.