This is a test reorganization patch moving a `pack-refs -h` help test as part of the ongoing consolidation of command help tests into t1517-outside-repo.sh. The patch relocates the test from t0610-reftable-basics.sh where it was originally placed, following the established pattern seen in previous patches of this series.

The test verifies that `git pack-refs -h` shows usage information and exits with code 129, both inside and outside a repository context. The move demonstrates that this help test is independent of the reftable context where it was previously located, confirming the series' thesis that command help behavior tests are context-agnostic.

The patch follows the exact template established earlier in the series: identical exit code verification (129), case-insensitive usage pattern matching via test_grep, and dual-context testing (both repository and non-repository environments). The test logic remains unchanged - only its location is modified to group it with similar help behavior tests.

This is the fourth of seven patches in the series, with the pack-refs test relocation showing the pattern works for a command that was previously tested in a storage-format-specific context (reftable). The clean separation from reftable-specific testing confirms the help behavior is indeed format-independent. The patch maintains the high quality and consistency seen throughout the series, with no behavior changes - purely an organizational improvement.