This is the second patch in a test reorganization series moving command help tests to a dedicated outside-repository test file. It relocates the `for-each-ref -h` test from t6300-for-each-ref.sh to t1517-outside-repo.sh, following the same pattern established in the first patch for checkout-index.

The test verifies that `git for-each-ref -h` shows usage information and exits with code 129 (standard help exit code) in both repository and non-repository contexts. The implementation uses test_grep to check the usage message pattern and test_expect_code to verify the exit status. The patch makes no functional changes - it's purely moving the test to a more logical location alongside similar help tests.

This continues the series' goal of consolidating scattered help tests into t1517-outside-repo.sh. The test being moved here was previously isolated in t6300-for-each-ref.sh, which primarily tests ref iteration functionality rather than basic command behavior. The new location groups it with other command help tests that need to work outside a repository context.

Key technical details: The test checks both `git for-each-ref -h` and `nongit git for-each-ref -h` to verify behavior in both contexts. The test_grep pattern `[Uu]sage:` accounts for case variations in the output. The test remains identical to its previous implementation, just in a new file location.

This appears to be a straightforward, well-motivated test reorganization following an established pattern. The patch is cleanly implemented with no functional changes, just file movement. It advances the series' goal of better test organization by grouping similar tests together.