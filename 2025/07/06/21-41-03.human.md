This is the second patch in a test reorganization series moving command help tests to a dedicated script. The patch relocates the `for-each-ref -h` test from t6300-for-each-ref.sh to t1517-outside-repo.sh, following the same pattern as the previous patch that moved the checkout-index test.

The test verifies that `git for-each-ref -h` shows usage information and exits with code 129, both inside and outside a repository context. The move is purely organizational - the test behavior remains unchanged, just consolidated with similar tests in the new location. The change follows Patrick Steinhardt's suggestion to group these help tests together.

This appears to be a straightforward, well-executed test reorganization. The patch follows the established pattern from the first patch in the series and maintains the same test coverage while improving test file organization. The commit message clearly explains the motivation and change.