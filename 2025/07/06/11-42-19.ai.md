This is a brief response in an exploratory feature discussion about modifying `git grep` behavior. Andreas Schwab points out that the existing `-l` (--files-with-matches) option already provides similar functionality to what Markus Elfring was asking about - it shows only filenames containing matches rather than all matching lines. This suggests the requested behavior may already be available through existing flags, though it doesn't show the actual matching line like the original proposal might have intended. The exchange indicates this may be a feature that doesn't require new code if `-l` meets users' needs, though the thread could continue with use cases where seeing just the first match (rather than just the filename) would be valuable.