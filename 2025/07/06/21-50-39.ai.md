Test refactoring completing a 7-patch series consolidating command help tests into t1517-outside-repo.sh. This final patch moves the verify-tag -h test from t7030-verify-tag.sh to the dedicated help test file, following the same pattern established by previous patches in the series.

The test verifies that `git verify-tag -h` shows usage information and exits with code 129, both inside and outside a repository context. Like the other relocated tests, it uses test_grep for case-insensitive matching of the usage pattern. The move demonstrates that help tests are independent of the GPG context where verify-tag normally operates, just as earlier patches showed independence from transport (send-pack) and other specialized contexts.

This completes the series' goal of unifying scattered help tests into a single location, with all seven commands now following the identical verification pattern: exit code check, usage pattern matching, and dual-context testing. The patch removes the test from its original GPG-specific location while preserving all verification logic. No behavior changes are introduced - this is purely an organizational improvement to make the test suite more maintainable.