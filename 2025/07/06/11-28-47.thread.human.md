This thread explores whether `git grep` needs a new mode to show only the first match per file, rather than all matches (current behavior) or just filenames (`-l` flag). The discussion has moved from the abstract idea to evaluating existing options, with Phillip Wood noting that `--max-count=1` may already provide the desired behavior by showing the first matching line per file while still displaying content.

Markus Elfring, the original proposer, has now raised questions about programmatic access to this functionality, asking whether the same information can be obtained through Git's C APIs and how results might flow through other interfaces. This suggests his use case may involve scripting or tooling integration where command-line output alone might not suffice. Andreas Schwab had earlier pointed out the similarity to `-l`, while Phillip Wood's `--max-count` observation currently stands as the most promising existing solution.

The thread remains in an exploratory phase, with no patches proposed yet. The key open question is whether `--max-count=1` fully satisfies Markus's needs, particularly for programmatic use cases, or if there are subtle gaps that would still warrant a dedicated flag. Participants are now examining the existing options' technical boundaries rather than proposing new functionality.