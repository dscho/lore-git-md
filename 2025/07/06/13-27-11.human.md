This is a reply in the ongoing discussion about modifying `git grep` to show only the first match per file.

Phillip Wood points out that the existing `--max-count` option (when set to 1) already provides the requested functionality of showing just the first match per file. This suggests the desired behavior may not require a new flag after all.

The reply is brief but potentially significant - if `--max-count=1` fully satisfies the use case Markus described, it could resolve the thread by showing the functionality already exists. However, it remains to be seen whether Markus finds this solution satisfactory compared to his original proposal for a dedicated flag with slightly different output formatting.

This moves the discussion from exploring new functionality to evaluating whether existing options adequately cover the use case. The ball is now back in Markus's court to either accept this solution or explain why `--max-count=1` doesn't fully meet his needs.