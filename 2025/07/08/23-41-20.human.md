This is a status update from Junio Hamano about the `git sparse-checkout clean` feature and its interaction with another in-flight topic. The patch series, which adds a new command to clean up tracked-but-sparse directories in cone mode, is currently blocked from merging into the 'seen' branch due to conflicts with Derrick Stolee's work on removing the `sparse_expect_files_outside_of_patterns` global variable.

Junio notes that while he likes the feature, there are test failures when both changes are merged together. For now, he's holding the sparse-checkout clean series out of 'seen' to avoid blocking other CI-tested changes, but may revisit the merge order later - either by merging this first and having the other topic rebase, or vice versa.

The update doesn't introduce new technical discussion but signals that this useful but currently blocked feature (which has already gone through several rounds of review addressing safety concerns and edge cases) will need some coordination with related refactoring work before it can proceed. The temporary hold suggests the feature is still on track but needs this dependency resolved first.