This is a follow-up in the ongoing discussion about `git stash` message handling discrepancies. José Miguel Armijo Fidalgo suggests that if the current behavior (where custom messages from `git stash create` don't appear in `git stash list`) is intentional, it should be documented as such. This positions the issue as potentially a documentation bug rather than a behavioral one.

The email is brief but reinforces the core observation from earlier in the thread: there's a disconnect between where stash messages appear (commit object vs reflog). While previous messages explored technical solutions, this shifts focus to whether clarifying the documentation would be sufficient. The thread has already established that the behavior stems from `stash.c`'s implementation, so this suggestion offers a lightweight alternative to code changes.