This is the first patch in a 4-part series adding pagination support to `git for-each-ref`. It's a refactoring that moves the `ref_iterator` interface from an internal header to the public `refs.h`, exposing it for external use.

The patch relocates the `ref_iterator` struct and all related functions from `refs/refs-internal.h` to `refs.h`, making them available to advanced users who need more flexible reference iteration than the standard `for_each_ref` wrappers provide. The commit message explains that exposing this lower-level interface allows callers to mix and match iteration options without requiring new wrapper functions for every combination.

The implementation looks clean and well-motivated, with comprehensive documentation moved along with the code. This foundational change enables the pagination feature being built in subsequent patches, where the exposed iterator interface will be used to implement the new `--start-after` option. The thread context shows this series has undergone thorough review and naming discussion, with all technical aspects now settled.