Here's a human-readable summary of the thread's current state:

The discussion centers on Carlo Marcelo Arenas Belón's series to improve `git daemon`'s signal handling by replacing `signal()` calls with `sigaction()`. This would make behavior more predictable across platforms, particularly regarding whether system calls restart after signals (SA_RESTART) and how `poll()` handles interruptions. While the technical merits are clear, Windows compatibility has emerged as a sticking point.

Junio clarified the hold-up isn't from the author - the series is technically complete and awaiting review, especially for patch 2. Carlo explained the changes would resolve two currently undefined behaviors in the signal handling. However, Johannes Sixt previously noted that Windows' limited signal support (where true POSIX signals don't exist outside Cygwin) complicates matters, making some proposed changes irrelevant or problematic for MinGW builds.

The thread shows a pattern common in cross-platform work: a technically sound change that runs into edge cases on niche platforms. While the standardization benefits are clear, the Windows implications need careful consideration before proceeding. The series remains in limbo until reviewers reach consensus on how to handle these platform-specific concerns.