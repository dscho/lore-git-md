Here's the updated human-readable thread summary:

The thread continues discussion of Carlo Marcelo Arenas Belón's stalled series to improve `git daemon` signal handling by replacing `signal()` with `sigaction()`. This effort has encountered platform-specific complications, particularly around Windows signal handling limitations and discrepancies in SIGCHLD values between MinGW (17) and Cygwin/macOS (20).

In the latest exchange, Johannes Sixt responds to Carlo's proposal to standardize on SIGCHLD=20 across platforms. While not objecting to the change, Johannes notes he's curious why the different signal numbers produce different error patterns (EINVAL with 20 versus silent failure with 17) but is unable to investigate this himself currently. This continues the technical discussion that has been blocking progress on the series.

The broader context shows this as one of the few stalled items in Junio's "What's cooking" report for Git 3.0, where major infrastructure changes like SHA-256-by-default and reftable adoption are progressing well. The SIGCHLD series remains in limbo as the Windows-specific signal handling constraints may require architectural reconsideration of the approach. Johannes' latest response keeps the conversation alive but doesn't resolve the fundamental platform compatibility questions that have held up the series.