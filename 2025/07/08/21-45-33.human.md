This is a small documentation update that ties into the ongoing `git sparse-checkout clean` feature development. Derrick Stolee proposes modifying the sparse index expansion advice message to recommend the new `git sparse-checkout clean` command instead of the more generic `git clean`. The change is minimal - just a one-line message update in `sparse-index.c`.

Elijah Newren acknowledges the change makes sense, but his brief reply ("once we work out any wrinkles") references the open questions about the clean command's behavior that were raised in earlier thread discussion. This suggests the patch is conceptually sound but may need to wait until those design questions are resolved before being merged.

The change itself is straightforward and uncontroversial, serving as a natural follow-on to the new clean` command's introduction. It helps users discover the more specialized tool for their sparse-checkout maintenance needs. The thread context shows this is part of a larger effort combining performance improvements (the clean command) with architectural cleanup (the_repository refactoring).