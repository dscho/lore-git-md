Documentation patch correcting an inaccuracy in gitglossary(7) regarding glob pathspec behavior. This is a follow-up to the initial patch in the thread, maintaining focus on the same documentation issue.

The patch removes the incorrect claim that `**/foo` is equivalent to `foo` in glob pathspec contexts from glossary-content.adoc. The author explains that while `**/foo` matches "foo" anywhere in the directory structure, plain `foo` does not have this same behavior - it only matches at the top level. The change is minimal (2 insertions, 3 deletions) and precisely targeted at this specific documentation error.

The diff shows the exact modification: removing the phrase "the same as pattern 'foo'" while keeping the rest of the surrounding context about glob pathspec syntax intact. The commit message provides clear justification for the change by explaining the actual behavioral difference between these patterns in pathspec matching.

This appears to be a straightforward, well-justified documentation correction. The patch maintains the existing documentation structure while fixing an inaccuracy, and the change aligns with known pathspec behavior. No code changes are needed since this was purely a documentation error. The patch follows standard formatting and includes a proper Signed-off-by line.