This is a refactoring patch in Patrick Steinhardt's Meson build system cleanup series (v2 4/8). It simplifies the documentation build configuration by removing unused and underutilized variables in `Documentation/meson.build`.

The patch eliminates the `manpage_target` variable which wasn't referenced anywhere, and inlines the single-use `manpage_path` variable directly into the `custom_target()` call that generates manpages. The change maintains identical functionality while reducing variable clutter in the build configuration.

This is a straightforward cleanup that follows the pattern of the series - removing unnecessary indirection in the Meson build files. The change touches only the manpage generation logic in the documentation build system, specifically modifying how the output filename is constructed for each manpage target. The patch is small (3 lines removed, 2 added) and focused, with no behavioral impact on the build process.