This is a refactoring patch in Patrick Steinhardt's Meson build system cleanup series (part 4/8). It simplifies the documentation build configuration by removing unused and underutilized variables in `Documentation/meson.build`.

The patch eliminates the unused `manpage_target` variable and inlines the `manpage_path` variable that was only referenced once. This makes the build configuration more straightforward by reducing unnecessary indirection. The change is purely internal - it doesn't affect the generated documentation or build behavior, just makes the code cleaner and easier to maintain.

As with the rest of the series, this is a well-justified change that follows through on the goal of cleaning up the Meson build configuration. The patch is small and focused, with a clear explanation of what it does and why. Since this is part of an already-reviewed series that's ready for merging, this change likely won't need additional scrutiny.