Refactoring patch removing unused code in the ref-cache subsystem.

This is the second patch in a 4-part series adding pagination support to `git for-each-ref`. While the main focus of the series is implementing the new `--start-after` option, this patch is a straightforward cleanup of the ref-cache backend. It removes the `find_ref_entry()` function and its declaration, which are no longer used anywhere in the codebase.

The change is mechanical and low-risk, deleting 21 lines of unused code (14 from the implementation and 7 from the header). The removal was likely enabled by earlier refactoring work, though the commit message doesn't specify what made this function obsolete. As part of a well-reviewed series that has already gone through multiple iterations, this cleanup can be safely ignored by most readers unless they're specifically working on the ref-cache internals.