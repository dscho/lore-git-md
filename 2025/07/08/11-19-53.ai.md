Documentation update for the new `git sparse-checkout clean` feature. This is the third patch in a series introducing the command to handle leftover sparse directories in cone mode.

The patch modifies the sparse index expansion advice message to recommend the new `git sparse-checkout clean` command instead of the more generic `git clean`. This change reflects the series' goal of providing a more precise tool for cleaning up sparse directories that cause unnecessary index expansions. The advice text now suggests comparing the sparse-checkout definition with working directory contents and using the new specialized command for cleanup.

The implementation is minimal - a one-line change to the advice message in `sparse-index.c` that replaces the `git clean` suggestion with `git sparse-checkout clean`. This aligns with the series' focus on improving the sparse-checkout experience by directing users to the new, more appropriate tool for this specific use case.

The change is straightforward and uncontroversial, serving as a documentation update that completes the feature introduction by ensuring users encountering sparse index expansion issues will be guided toward the new solution. It maintains consistency with the series' goal of addressing performance problems caused by unwanted full index expansions due to leftover sparse directories.