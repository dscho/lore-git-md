This is a follow-up in the `git stash create`/`store` message handling thread, suggesting a potential documentation solution. José Miguel Armijo Fidalgo responds to the earlier technical analysis by proposing that if the current behavior is intentional, the documentation should be clarified to explicitly state that custom messages provided to `git stash create` won't appear in the reflog output from `git stash list`.

The email reinforces the core issue where commit messages and reflog messages are handled differently in the stash implementation, but shifts focus from potential code changes to documentation updates as a resolution path. This aligns with one of the two options previously identified by Jeff King (documenting the current behavior versus changing the implementation).

Key technical context remains the split between commit object messages (set by `create_stash()`) and reflog messages (overwritten by `do_store_stash()`). The suggestion implies accepting the current implementation's behavior and making it more discoverable to users through documentation, which would be the more conservative approach compared to modifying the plumbing behavior. This positions the thread at a decision point between behavioral change and documentation clarification.