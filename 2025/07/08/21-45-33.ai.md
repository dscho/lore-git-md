This is a small documentation update that ties into the ongoing `git sparse-checkout clean` feature development. The patch modifies the sparse index expansion advice message to recommend the new `git sparse-checkout clean` command instead of the more generic `git clean` when users encounter sparse index expansion scenarios.

The change is straightforward - it updates a single advice string in `sparse-index.c` to reference the new specialized command. The commit message explains that this addresses the common case where sparse index expansion occurs due to leftover files in tracked-but-now-sparse directories, which is exactly what `sparse-checkout clean` is designed to handle.

The patch appears uncontroversial and well-motivated, though it depends on the `sparse-checkout clean` feature being finalized (as noted in Elijah Newren's reply). The thread context shows there are still some open questions about edge case handling for the clean command, but this documentation change would naturally follow once those are resolved. The modification is minimal (3 lines changed) and focused purely on improving user guidance.