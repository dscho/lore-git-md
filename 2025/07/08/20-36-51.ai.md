This is a discussion email in the ongoing sparse-checkout 'clean' command series, where Elijah Newren seeks clarification on the command's scope and behavior. The email focuses on understanding exactly what types of files the new `git sparse-checkout clean` will handle, probing edge cases beyond the primary untracked/ignored files use case.

Newren methodically examines several categories of files that might exist in sparse directories: untracked, ignored, tracked-but-unmodified, tracked-and-modified, and tracked-and-conflicted. He notes the ambiguity around "tracked" in sparse-checkout contexts, distinguishing between sparse index and fully expanded index states. The email questions whether users might still need to run `git sparse-checkout reapply` alongside the new command in some scenarios.

The discussion evaluates the design choice of a separate 'clean' subcommand versus rejected alternatives like augmenting `reapply` or adding a `--sparse` flag to `git clean`. Newren appears to favor the current approach but seeks implementation details about error handling for modified files and behavior in non-cone-mode or non-sparse-index scenarios. The email maintains the thread's technical depth while focusing on user experience implications of the proposed solution.