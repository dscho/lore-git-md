This is a test fix addressing a TAP format violation in the Git test suite. The patch corrects an issue where a `grep` command outside a test case was producing output that broke the TAP format when running tests under Meson, causing annoying (though non-fatal) error messages.

The problem was introduced in a recent commit (9fd38038b9c) that added a `grep` check to determine whether a subsequent test should expect success or failure. While the logic is sound, the `grep` was printing matching lines directly to stdout rather than operating quietly. The fix simply adds the `-q` flag to suppress output.

The patch is straightforward and well-justified - it solves a clear nuisance in test output while maintaining the intended test logic. The commit message provides good context about the issue's symptoms and root cause, including the exact error message seen in Meson. The change is minimal (one character added) and focused on the test infrastructure rather than core Git functionality.