Performance optimization and refactoring patch in Patrick Steinhardt's series modernizing Git's refs subsystem. This is patch 8/14 in v4 of the series, converting `git show-ref` to use the new `reference_get_peeled_oid()` interface.

The patch focuses on unifying how `git show-ref` handles reference display across its different modes (listing references, verifying existence). Previously, the verification mode couldn't use the new peeling interface because it didn't have access to a `struct reference`. The solution constructs a minimal `struct reference` when needed, allowing both code paths to use the same peeling interface.

This change is part of the broader effort to separate transport-related `struct ref` from core ref `struct reference` and to enable lazy object parsing optimizations. The patch maintains all existing functionality while preparing for future performance improvements. It's a straightforward refactoring that follows the architectural direction established earlier in the series.

The diff shows 19 lines added and 13 removed, mostly mechanical changes to pass around the `struct reference` instead of separate name/oid pairs. The patch appears well-contained and builds on previous work in the series without introducing new complexity.