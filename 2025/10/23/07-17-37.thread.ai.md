Thread identity: Cover letter for a 3-patch series introducing cbindgen support to Git's Rust infrastructure, authored by an unspecified contributor. The series aims to facilitate Rust-C integration through automated header generation, starting with varint implementation verification.

Core problem/goal: Enable Rust-C interoperability by generating C headers from Rust's `extern "C"` blocks using cbindgen. The series progresses through three steps: CI preparation (switching 32-bit CI from Ubuntu to Debian), build system standardization (renaming Rust library target), and core functionality implementation (cbindgen integration with varint verification).

Current status: v1 series with three patches now fully described:
1. CI preparation: Switches 32-bit CI from Ubuntu to Debian, updating .github/workflows/main.yml, .gitlab-ci.yml, and related scripts. Justified by Ubuntu's EOL status and package availability for cbindgen work.
2. Build system: Renames Rust library from `git_rs` to `rust` in meson.build for consistency with test target naming, enabling more intuitive `meson compile rust` command.
3. Core functionality: Adds cbindgen configuration with varint implementation as first use case, generating `c-bindings.h` that enforces type consistency between Rust and C implementations.

Key technical details:
- CI changes: Updates image references (ubuntu-20.04 -> debian:oldstable), package installation (language-pack-is -> locales-all), adds cbindgen tool installation
- Build systems modified: Makefile and meson.build (target rename and new cbindgen rules)
- New configuration: cbindgen.toml specifying C-style includes and struct formatting
- Implementation strategy: Header included in `varint.c` rather than `varint.h` to avoid unnecessary recompilation
- Verification mechanism: Compile-time type checking between Rust's `decode_varint` and C implementation
- Infrastructure: Proper dependency tracking and clean rules for generated headers

Open questions/loose ends: None yet raised. Future work implied for broader Rust-C integration beyond initial varint verification, with the infrastructure now in place to support it.

Participant positions: No reviews or responses yet as this is the series introduction.

Related work: Connects to ongoing Rustification effort and builds upon prior Rust infrastructure work. CI changes address platform-specific concerns similar to those raised in other Rust-related threads. The target renaming follows existing conventions for Rust components in the build system. The cbindgen approach provides a systematic alternative to manual header maintenance as seen in other mixed-language projects.