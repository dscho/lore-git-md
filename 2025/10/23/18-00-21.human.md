This is a discussion about implementation details of the Rust-C interop patch series, specifically focusing on the cbindgen configuration and generated header placement.

Ezekiel Newren responds to Patrick Steinhardt's patch with several technical suggestions. First, he proposes moving generated headers to an `interop/` directory rather than the root, both for cleanliness and to make `.gitignore` management easier. He raises a practical concern about non-Rust platforms needing these generated files for C compilation.

Newren shares his own `cbindgen.toml` configuration as a reference point, noting key differences like including `git-compat-util.h` explicitly and setting `usize_is_size_t` for better type alignment. He points out cbindgen's limitation of only supporting spaces (not tabs) for indentation.

Regarding CI changes, Newren suggests treating cbindgen as a build dependency in `Cargo.toml` rather than a system package, and proposes moving to a Cargo workspace structure to support per-crate header generation instead of a single monolithic file. This would provide better modularity as the Rust codebase grows.

The exchange shows active refinement of the technical approach to Rust-C interop, with Newren bringing practical experience with cbindgen to bear on the implementation details. The suggestions appear well-considered and aligned with Git's code organization principles.