This is a follow-up discussion on the Rust cbindgen integration series, specifically addressing build system details in the third patch. Junio Hamano raises a concern about the `make clean` behavior with respect to the generated `c-bindings.h` file.

The key technical point is a proposed change to the Makefile that would make the `c-bindings.h` file always considered for cleanup, rather than only when `WITH_RUST` is set. The current patch moves the `C_BINDINGS` variable definition outside the `WITH_RUST` conditional block, ensuring the generated header is cleaned regardless of Rust build configuration.

Junio's hesitation stems from a general principle that `make clean` should thoroughly remove build artifacts without being overly sensitive to the original build configuration. The change appears minor (3-line diff) but touches on an important build system philosophy point. This continues the thread's theme of balancing Rust integration needs with Git's established build practices.

The discussion remains focused on implementation details rather than the broader Rust interoperability goals established in earlier messages. No new technical alternatives are proposed here - it's primarily Junio evaluating whether this specific Makefile adjustment aligns with project norms.