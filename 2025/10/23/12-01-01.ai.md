This is a technical discussion about fixing the `git diff --quiet` regression, focusing on output redirection implementation details.

Jeff King responds to Junio Hamano's proposed fix, identifying a flaw in the current approach where redirection to `/dev/null` happens once at the start of the loop rather than per-pair. He demonstrates how this causes `git show -w --name-status` to incorrectly suppress all output, as the redirection persists through both the quiet check and actual output phases.

The key technical points are:
- The current implementation in `jc/diff-from-contents-fix` incorrectly applies redirection globally for the entire diff operation
- Jeff proposes moving the redirection inside the loop, reusing a single `/dev/null` filehandle for efficiency
- The fix involves saving/restoring the file handle around each `diff_flush_patch_quietly()` call
- This explains why Lidong Yan's t4013 test was failing - it should show three `--raw` lines that were being incorrectly suppressed
- Jeff suggests the filehandle could potentially be cached in the diff_options struct for cleaner access

The email advances the technical discussion by:
1) Identifying a concrete bug in the current implementation
2) Providing a specific test case that fails (`git show -w --name-status`)
3) Offering a more precise fix that handles redirection per diff pair
4) Connecting this to the ongoing test failures in the thread

The discussion remains focused on implementation mechanics rather than design philosophy, with Jeff proposing a pragmatic solution that maintains the immediate fix while leaving room for future optimization (like caching the `/dev/null` handle). This moves the thread forward by resolving Junio's uncertainty about when file handle restoration matters.