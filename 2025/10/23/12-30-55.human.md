This is a follow-up discussion in an ongoing bugfix thread about diff output behavior in dry-run mode. Jeff King points out an unrelated but long-standing issue with external diff output not respecting the `--output` file redirection, which has existed since the feature was introduced.

The main observation is that when using `GIT_EXTERNAL_DIFF` with `--output`, the external diff output incorrectly goes to stdout instead of the specified file. Jeff provides a simple reproduction case and suggests a potential fix involving redirecting the external diff's output to match the main diff output file.

This is presented as an aside to the main dry-run mode fixes - Jeff explicitly notes this isn't related to the immediate regression being fixed, but rather an independent bug that was discovered while reviewing the diff output code paths. The proposed solution involves flushing the output file and setting the external diff's output file descriptor to match the main diff output.

The email maintains the technical depth expected in Git development discussions while keeping focused on the specific output redirection issue, separate from the thread's primary dry-run behavior topic.