Reviewer ack for OpenSSH test fix. Jeff King (peff) gives his approval to Patrick Steinhardt's solution for the OpenSSH 10.1+ socket path length issue in the `t7528-signed-commit-ssh.sh` test. The patch uses `ssh-agent -T` to create sockets in `/tmp` rather than the default `~/.ssh/agent` location that can hit Unix domain socket path limits. 

While the thread previously identified two broader test fragility issues (shell evaluation problems with spaces in paths and potential SSH key exposure), peff agrees these are pre-existing concerns that don't block the immediate fix. The approval comes with the understanding that these can be addressed separately in future work.

 

This marks the final step before merging, as the technical discussion has settled on the minimal `-T` approach being the right solution for the OpenSSH compatibility problem. The exchange shows the Git community's typical pattern of identifying immediate fixes while noting areas for future improvement.