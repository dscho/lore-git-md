This is the first patch in Patrick Steinhardt's v4 series of 14 patches, focused on refactoring the refs subsystem. The patch introduces a wrapper struct for the `each_ref_fn` callback function type to make future interface changes less disruptive.

The key change replaces direct callback parameters with a `struct reference` containing:
- The refname
- The target (for symrefs)
- The OID
- Flags

This affects 49 files with 392 insertions and 462 deletions, showing the wide impact of the refactoring. The motivation is to avoid the extensive churn seen in previous changes (like e8207717f1 which touched 48 files to add one parameter) by encapsulating callback data in a struct.

The patch maintains all existing functionality while making the interface more flexible for future changes. It's a foundational change for the series that enables subsequent optimizations around tag peeling and lazy object parsing. The implementation looks thorough, updating all callback implementations across the codebase to use the new struct.

This is a significant but mechanical refactoring that doesn't change behavior - it just provides better infrastructure for the performance improvements coming later in the series. The patch appears well-constructed with a clear commit message explaining both the benefits and tradeoffs of the approach.