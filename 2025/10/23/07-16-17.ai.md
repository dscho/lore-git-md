This is a refactoring patch in Patrick Steinhardt's performance optimization series, specifically converting `builtin/show-ref.c` to use the new `reference_get_peeled_oid()` interface introduced earlier in the series. The change is part of the broader effort to separate `struct ref` and `struct reference` while optimizing tag peeling behavior.

The patch modifies `show-ref` to consistently use `struct reference` throughout its code paths, even in cases where it wasn't using an iterator before. The key change is in the verification mode, which previously called `refs_read_ref()` directly and now manually constructs a `struct reference` to maintain interface consistency. This allows replacing `peel_iterated_oid()` with `reference_get_peeled_oid()` everywhere in the command.

The implementation handles three main cases: showing all references matching a pattern, listing exact matches, and checking reference existence. The verification path now creates a temporary `struct reference` on the stack to maintain the uniform interface. The changes are mechanical - the patch doesn't alter functionality but ensures all code paths use the new peeling interface consistently.

This is a straightforward conversion that builds on the series' earlier architectural changes. It demonstrates how the new peeling interface can be adopted across different parts of the codebase while maintaining backward compatibility. The patch is small (32 lines changed) and focused, with no apparent behavioral changes or new edge cases introduced.