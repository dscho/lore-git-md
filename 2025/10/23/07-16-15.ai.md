Performance optimization and refactoring patch in the refs subsystem modernization series, specifically targeting upload-pack's tag peeling behavior.

This patch modifies upload-pack to consistently use the new `reference_get_peeled_oid()` interface introduced in patch 5, eliminating the previous reliance on `peel_iterated_oid()`'s global state. The change affects two code paths in `write_v0_ref()`: the normal ref advertisement case (which already had access to a `struct reference`) and the capabilities announcement case (which previously constructed ref information manually). The patch unifies these by creating a temporary `struct reference` for the capabilities case, allowing both paths to use the new peeling interface.

Key technical changes include:
- Modifies `write_v0_ref()` to take a `struct reference` instead of separate refname/OID parameters
- Updates the capabilities announcement case to construct a minimal `struct reference` with hardcoded "capabilities^{}" name and null OID
- Replaces `peel_iterated_oid()` with `reference_get_peeled_oid()` for both code paths
- Maintains all existing functionality while removing global state dependency
- Touches 22 lines in upload-pack.c (13 insertions, 9 deletions)
- Follows the series' architectural direction of moving away from global state
- Builds on patch 5's introduction of `reference_get_peeled_oid()`
- Prepares for the final performance optimizations in patch 14

The change appears straightforward and well-contained, with no new test coverage added since it's a refactoring that maintains existing behavior. The patch fits cleanly into the series' progression toward eliminating global state in preparation for lazy object parsing optimizations.