This is a refactoring patch in Patrick Steinhardt's performance optimization series for the refs subsystem, specifically focusing on tag peeling behavior. The patch modifies the ref-filter subsystem to propagate peeled object IDs through the ref array structure rather than discarding and re-parsing them. Key changes include:

1. Adds a `peeled_oid` field to `struct ref_array_item` to store peeled tag information
2. Updates `new_ref_array_item()` and `ref_array_push()` to accept and store peeled OIDs
3. Modifies `populate_value()` to use the stored peeled OID when available, falling back to manual peeling
4. Updates callers in ls-remote.c, tag.c, and verify-tag.c to pass NULL for peeled OIDs (maintaining current behavior)
5. Simplifies `apply_ref_filter()` to take a `struct reference` rather than separate parameters

The changes allow tag peeling information to be preserved through the ref-filter pipeline, avoiding redundant object parsing. This builds on the series' architectural goal of separating transport-related ref handling from core ref operations while preparing for the final performance optimization patches that will implement lazy object parsing.

The patch touches 5 files with 66 insertions and 32 deletions, primarily focused on the ref-filter.c implementation. It maintains backward compatibility while enabling future optimizations, though some callers still pass NULL for peeled OIDs. The changes are consistent with the series' progression toward eliminating global state and improving tag handling performance.