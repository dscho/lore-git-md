Thread addresses OpenSSH compatibility in `t7528-signed-commit-ssh.sh` test for SSH commit signing, specifically OpenSSH 10.1+ moving agent sockets to `~/.ssh/agent` hitting Unix domain socket path length limits. Patrick Steinhardt's solution uses `ssh-agent -T` to force `/tmp` socket creation, maintaining backward compatibility. Jeff King (peff) reviewed and approved the approach while identifying two pre-existing test fragility issues: (1) shell evaluation with spaces in Git's "trash directory" paths, and (2) potential exposure of real SSH keys via SSH_AUTH_SOCK fallback. These are acknowledged as separate concerns not introduced by the patch. Alternatives like relative paths were rejected due to working directory risks. Consensus confirms the minimal `-T` fix resolves the immediate issue while leaving broader test improvements for future work. The patch is now ready for merging, having received explicit approval from Jeff King. Key participants are Patrick Steinhardt (author) and Jeff King (reviewer), with brian m. carlson involved in earlier discussion. Technical scope remains focused on OpenSSH 10.1+ socket path limits and test isolation, with no core functionality changes. Follow-up email from Jeff King explicitly confirms the solution and readiness for merge, marking the thread as resolved pending maintainer action.