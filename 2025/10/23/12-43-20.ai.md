This is a follow-up discussion about the OpenSSH compatibility fix for the `t7528-signed-commit-ssh.sh` test. Jeff King (peff) responds to Patrick Steinhardt's proposed solution, agreeing with the `ssh-agent -T` approach while noting some additional edge cases and potential pitfalls.

The email highlights two key concerns beyond the original socket path length issue: First, the space in Git's "trash directory" path could break shell evaluation even if the path were shortened. Second, the test's fallback to using the user's real SSH_AUTH_SOCK when ssh-agent fails could inadvertently expose real keys during testing. While these aren't new problems introduced by the patch, they represent existing test fragility.

Jeff considers but rejects alternative approaches like using relative paths ("./sshsock") due to potential gotchas with working directory changes. The discussion reinforces that the `-T` flag solution, while not perfect, is the most practical fix for the immediate issue. The tone suggests this will likely be the final approach, with other test infrastructure improvements possibly addressed separately.

For future reference, the key technical details are: the `t7528` test's interaction with OpenSSH 10.1+, the `ssh-agent -T` workaround, the 108-character Unix socket path limit, and the "trash directory" path evaluation quirk. The thread shows consensus forming around this minimal fix while acknowledging broader test infrastructure issues that may need attention later.