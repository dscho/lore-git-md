Junio Hamano responds to Patrick Steinhardt's patch about Rust-C bindings generation, focusing on build system considerations. This is part of the ongoing series introducing cbindgen support for Git's Rust infrastructure.

Junio expresses mild concern about the patch's approach to handling generated bindings in the Makefile. While he previously found the core implementation reasonable, he now questions whether the conditional handling of `c-bindings.h` in the clean target might be too build-option-specific. The proposed change moves the `C_BINDINGS` definition outside the `WITH_RUST` conditional, making it always present but only actually generated when Rust is enabled.

This appears to be a minor refinement discussion about build system hygiene rather than a fundamental objection to the approach. Junio's feedback suggests he's thinking about edge cases where build options change between invocations. The exchange shows careful attention to detail in how the Rust integration affects the broader build system.