This is part 2 of a 14-part refactoring and performance optimization series focused on the refs subsystem. The patch continues the work to separate `struct ref` and `struct reference` by introducing a `.ref` field in the base iterator structure.

The change replaces direct fields in the iterator (`refname`, `oid`, `flags`, `referent`) with a single `struct reference` member. This simplifies the iterator interface by allowing implementations to populate and pass the reference structure directly rather than managing individual fields. The patch also updates `should_pack_ref()` to take the reference structure directly.

The changes are mechanical but widespread, touching 8 files with 75 insertions and 100 deletions. The patch appears well-executed, maintaining consistency across both files and packed ref backends while simplifying the iterator interface. This is a foundational change that enables later optimizations in the series, particularly around lazy object parsing.

As part of an established series that has gone through multiple iterations, this patch follows the established pattern of clean refactoring with no behavior changes. The changes are straightforward and unlikely to be controversial, though reviewers will want to verify the mechanical transformations are correct.