Performance optimization discussion for `git last-modified` implementation.

Taylor Blau follows up on Toon Claes's benchmark results comparing different bitmap implementations for tracking path modifications. The thread has previously established EWAH bitmaps as the preferred approach due to their memory efficiency and performance characteristics. Claes's latest benchmarks show that using bitmap pointers (783.7ms) outperforms using bitmap values (856.7ms) by about 9%, likely due to better cache locality from smaller slab footprint.

Blau confirms the technical understanding that the "bitmap-ewah" implementation uses standard struct bitmaps from the EWAH codebase rather than compressed EWAH bitmaps. He agrees with the pointer-based approach despite initial surprise at the performance advantage, noting the pointer indirection is offset by cache benefits from smaller data size.

This exchange resolves two open questions from the thread: (1) confirms EWAH bitmaps (as pointers) are the right technical choice over character arrays, and (2) validates the performance characteristics of pointer-based bitmap storage in the commit slab. The discussion also mentions making `bitmap_grow()` public for benchmarking purposes, though this appears to be a temporary testing measure rather than a proposed API change.

The technical objections or concerns raised in this message - it primarily serves to validate and confirm the implementation choices based on empirical data. The thread continues to show productive collaboration between the contributors, with careful attention to both performance characteristics and memory efficiency in the `last-modified` optimization work.