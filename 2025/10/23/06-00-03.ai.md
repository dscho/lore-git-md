This is the final performance optimization in Patrick Steinhardt's refactoring series, implementing lazy object parsing in `ref-filter`. The patch adds a `maybe_object` field to `struct expand_data` and introduces a `get_or_parse_object()` helper to defer parsing until needed, showing a 13% speedup in `git for-each-ref --format='%(raw)'` operations.

Junio Hamano suggested improving the helper's calling convention to directly return the object pointer rather than using an output parameter, noting the repeated pattern of checking return values and casting in callers. Patrick acknowledges this as a good suggestion and plans to send an updated version with this ergonomic improvement.

The change builds on the series' architectural work separating `struct ref` from `struct reference` and the tag verification improvements. While the current implementation is approved, this final tweak will simplify the three call sites that need to handle the parsed object. The series as a whole is now complete and ready for integration after this minor interface refinement.