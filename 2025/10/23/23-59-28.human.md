This is a performance optimization discussion for the `git last-modified` command, specifically focusing on bitmap implementation choices. Taylor Blau follows up on Toon Claes's benchmark results comparing different bitmap approaches.

The key findings from the benchmarks show that using EWAH bitmaps as pointers (rather than values) provides both speed and memory efficiency advantages - about 9% faster than using bitmaps as values, and slightly faster than character arrays while using significantly less memory. Taylor agrees with Toon's conclusion that the pointer-based EWAH bitmap approach is the right choice, theorizing that the smaller slab footprint improves cache locality despite the extra indirection.

This exchange represents a refinement phase of the optimization work, where the core algorithm has consensus and the discussion has moved to implementation details. The benchmarks confirm earlier design decisions and help settle remaining questions about bitmap storage strategies. The thread shows productive collaboration between the contributors as they validate performance characteristics of different approaches.