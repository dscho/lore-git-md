Bugfix discussion for `git diff --quiet` regression with ongoing architectural refinements.

Jeff King responds to Junio Hamano's proposed fix for the `git diff --quiet` regression, identifying a subtle bug in the current approach where output redirection affects both the quiet check and subsequent name-status display. The core issue involves managing file handle redirections during diff processing, where the current implementation incorrectly persists `/dev/null` redirection through the entire diff operation.

Key technical points:
- The fix needs to toggle redirection states per diff pair rather than once at the start
- Jeff proposes reusing a single `/dev/null` file handle rather than reopening it repeatedly
- The discussion reveals the current implementation in jc/diff-from-contents-fix still has the same output-squelching bug
- Test case demonstrates the problem using `git show -w --name-status`

The email advances the thread by:
1. Pinpointing why Junio's current approach still fails for name-status output
2. Offering a more precise implementation that handles redirection per-pair
3. Suggesting potential optimizations (caching the /dev/null handle)
4. Connecting this to Lidong Yan's earlier test failures

This continues the careful balancing act between fixing the regression while maintaining correct behavior for all diff output modes. The discussion remains focused on implementation details rather than revisiting the fundamental approach, suggesting the architectural direction is settling.