This is part 6 of a 14-part performance optimization and refactoring series focused on Git's refs subsystem, specifically addressing tag peeling behavior in `upload-pack`. The patch converts `upload-pack` to use the new `reference_get_peeled_oid()` interface introduced in patch 5, eliminating reliance on global state for tag peeling.

The change modifies `write_v0_ref()` to consistently use `struct reference` throughout, including for the special case of capability advertisement. Previously, capability announcements had to use the older `peel_iterated_oid()` because they didn't have a proper reference object. The patch solves this by constructing a temporary reference struct when announcing capabilities.

This is a clean refactoring that maintains the series' architectural goal of removing global state while keeping all functionality intact. The change is relatively small (13 lines added, 9 removed) and focused on `upload-pack.c`. It follows naturally from the infrastructure changes in earlier patches and helps pave the way for the lazy parsing optimizations coming later in the series.

The patch appears well-constructed with a thorough commit message explaining both the problem and solution. As part of an already-reviewed series that's nearing completion, this change seems like a straightforward improvement that shouldn't introduce controversy.