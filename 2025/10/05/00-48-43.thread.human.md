This thread addressed an inconsistency in how `git status --short -z` handled color output, where modified markers (`M`) remained colored while untracked markers (`??`) did not. The discussion clarified that while `-z` typically indicates machine-readable output, the combination with `--short` explicitly requests human-readable (colored) output with NUL separators.

The fix, which moves color application in `wt_shortstatus_other()` before the null-termination check, has been reviewed and validated. Jeff King provided authoritative guidance on the expected behavior, and Langbart confirmed the solution works for their real-world use case of piping colored output to `fzf --read0` (with the minor note that `--ansi` is needed for optimal fzf display). The patch includes new test coverage and maintains the documented behavior where `--short` keeps colors while `--porcelain` suppresses them.

With the technical solution agreed upon and practical validation complete, this thread has reached a clear resolution. The patch appears ready for merging, addressing both the implementation inconsistency and demonstrating real-world utility for workflows that need colored, NUL-separated status output.