This thread addressed a subtle inconsistency in how `git status --short -z` handles colored output, where modified markers (`M`) were colored but untracked markers (`??`) were not. The original reporter identified and fixed the issue by adjusting when colors are applied in the status output code, ensuring consistent behavior. Jeff King provided key technical guidance, clarifying that while `-z` typically indicates machine-readable output, the `--short` flag specifically requests human-readable (colored) output with NUL separators.

The solution was validated in a real-world scenario using the `fzf` fuzzy-finder tool, where Langbart confirmed the fixed behavior works well when piping colored status output to `fzf --read0 --ansi`. The discussion also revealed a useful configuration tip - setting `FZF_DEFAULT_OPTS` to include `--ansi` avoids needing to specify it each time. With the patch having been submitted and reviewed, and its practical utility confirmed, the thread reached a clear resolution with no outstanding issues. The fix maintains Git's documented behavior while addressing an implementation inconsistency that affected users wanting to combine colored output with NUL-separated formatting for tools like fzf.