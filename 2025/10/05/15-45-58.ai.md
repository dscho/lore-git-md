This is a patch series addressing navigation behavior in `git add --interactive`, building on previous discussion about hunk selection regressions. The v2 series expands the scope from just fixing the immediate bug to improving the consistency of hunk navigation commands (j/J/k/K/y/n/e) when rolling over between hunks.

The key technical changes in this version:
1. Makes all hunk navigation commands (j/J/k/K/y/n/e) consistently roll over to the next undecided hunk
2. Splits out the roll-over behavior for 'j' into its own patch
3. Adds roll-over support for 'k' and 'K' commands to match 'j' and 'J'
4. Resets the `permitted` bitmask at loop start to prevent state leakage
5. Improves documentation of the roll-over behavior
6. Expands test coverage with new scenarios

The changes touch `add-patch.c`'s core hunk processing logic, modifying how `patch_update_file()` handles state transitions and navigation commands. The series addresses several of the interface design questions raised in the thread, particularly around navigation symmetry between forward/backward movement and consistent treatment of decided vs undecided hunks.

Notable technical details:
- The `permitted` bitmask reset (patch 5) fixes potential state leakage between hunk operations
- New test cases verify wrap-around behavior for all navigation commands
- Documentation updates clarify the distinction between 'j' (next undecided) and 'J' (next hunk) behaviors
- The changes maintain backward compatibility while making the interface more predictable

This appears to be a well-considered response to the thread's technical discussion, addressing both the immediate regression and several of the deeper interface questions raised. The expanded test coverage and documentation updates suggest careful attention to both implementation correctness and user experience.