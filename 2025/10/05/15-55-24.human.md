This is the second patch in a series improving `git add --interactive` navigation behavior. It formally documents and tests the roll-over behavior of the 'J' key (next hunk) when used at the last hunk, wrapping around to the first hunk. 

The patch makes three key changes: updating the documentation to explicitly mention the roll-over behavior, modifying the help text in the code to match, and adding a new test case that 'J' properly wraps around. The change also fixes a subtle issue where the 'permitted' bitmask wasn't being reset between hunks, which was actually useful for this roll-over case.

The patch appears well-constructed with clear documentation update, code change, and test coverage. It builds on Junio Hamano's suggestion to make this existing-but-undocumented behavior official. This continues the series' theme of making hunk navigation more consistent and predictable in the interactive add interface.