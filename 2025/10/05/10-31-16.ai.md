This is a discussion about the deprecation of `git whatchanged` and its potential replacement. Kristoffer Haugsbakk responds to Jeffery Carr's suggestions about handling the command's removal, clarifying that Git doesn't have built-in aliases in the way Carr proposed. The email explains that `git whatchanged` is being removed because it has been functionally replaced by `git log` for over 12 years, with both commands using the same underlying machinery but with different defaults.

Haugsbakk provides specific instructions for replacing `git whatchanged` with `git log`, suggesting the equivalent command would be `git log <opts> --no-merges --raw`. They also note that users might prefer `--stat` or `--name-only` instead of `--raw` for better readability when viewing output directly. The email confirms that future versions will include a message suggesting the alternative command to run instead, addressing Carr's suggestion about improving the deprecation warning.

The discussion continues to focus on the practical aspects of deprecation rather than revisiting the decision to remove the command. The technical details provided about the command's replacement suggest this is moving toward implementation rather than debate about whether to remove it. The exchange indicates the removal is proceeding but with attention to helping users transition smoothly.