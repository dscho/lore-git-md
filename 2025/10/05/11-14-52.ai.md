Performance issue with untracked files cache behavior in `git status` commands. Devste Devste confirms the cache behavior inconsistency reported earlier, showing that setting `status.showUntrackedFiles=all` does enable caching as described in e6a653554b, but reveals a new wrinkle: certain combinations of status flags (particularly when used together with porcelain mode and other options common in IDE/GUI usage) appear to prevent cache updates. The email includes timing data showing 3.4s vs 0.8s differences between initial and subsequent status checks, demonstrating the performance impact.

Key observations: 1) Cache behavior becomes "sticky" once established - if the cache was created with `status.showUntrackedFiles=all`, subsequent commands with `--untracked-files=all` will use the cache regardless of current config, and 2) The combination of `--porcelain`, `--no-optional-locks`, `--ignore-submodules=dirty`, and `--no-ahead-behind` (common in tooling) may interfere with cache updates, though the exact culprit isn't isolated yet. This suggests the caching logic has additional undocumented interactions beyond just the showUntrackedFiles symmetry requirement.

The findings complicate the original understanding of the issue, showing that while the config/flag symmetry is one factor, there are other command-line combinations that affect cache behavior in ways that could impact real-world tool performance. The email doesn't propose a solution but provides concrete reproduction steps showing the cache behaving differently based on command history and option combinations.