Cover letter for a refactoring series cleaning up refs optimization code.

This 3-patch series follows up on recent work to add `git refs optimize` functionality (db0babf9b2). It consolidates duplicate code paths by removing the older `pack_refs` functions in favor of the newer, more generic `optimize` variants across the refs subsystem. The changes are mechanical renames and deletions with no behavior changes.

Key technical details:
- Removes `pack_refs_fn` and `pack_refs` from `ref_storage_be` struct
- Renames `pack_refs_opts` to `refs_optimize_opts` with corresponding flag renames (PACK_REFS_* -> REFS_OPTIMIZE_*)
- Updates all backend implementations (files, packed, reftable) to use only the optimize interface
- Maintains identical functionality while simplifying the codebase
- Includes test updates to reflect the naming changes

The series builds on master (143f58ef75) with 'ps/ref-peeled-tags' merged in. V2 squashes some commits and improves variable naming consistency. The changes are straightforward and unlikely to be controversial, being purely internal cleanup of now-redundant code paths.