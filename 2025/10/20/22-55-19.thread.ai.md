Thread identity: Patch series refactoring strbuf usage in gpg-interface.c, completing work started by Junio Hamano to replace unnecessary strbuf_split*() calls with simpler string parsing throughout the file.

Core problem: Eliminate overhead of strbuf operations in gpg-interface.c where strings are split but results aren't modified, making strbuf API unnecessary. Two specific cases addressed: `get_ssh_key_fingerprint()` and `get_default_ssh_signing_key()` now both use direct string manipulation (strchr() + xmemdupz()) instead of strbuf_split_max().

Current status: v4 series complete with both patches submitted. First patch modified `get_ssh_key_fingerprint()`; second patch now addresses `get_default_ssh_signing_key()` with same pattern. Both maintain security checks via is_literal_ssh_key() while simplifying memory management. Series appears ready for final review.

Key technical details: Both patches replace strbuf_split_max() with direct string extraction (between spaces for fingerprints, at newline for signing key). Error checking and security preserved while eliminating temporary strbufs. Follows pattern from wt-status.c. First patch uses space delimiter, second uses newline. Error messages adjusted to match new control flow but behavior identical. Memory management simplified with xmemdupz() replacing strbuf_list_free().

Open questions: None remaining - both cases identified in original optimization suggestion now addressed. Series appears complete.

Participant positions: Junio Hamano originally identified optimization opportunity. Christian Couder provided input on earlier versions. Author iterated through multiple approaches (string_list_split_in_place() in v3, current simpler solution in v4). Current approach well-vetted through discussion.

Related work: Completes strbuf cleanup started by Junio Hamano. Follows patterns from wt-status.c changes. Tested via GitHub CI. No further strbuf optimizations currently identified in gpg-interface.c.