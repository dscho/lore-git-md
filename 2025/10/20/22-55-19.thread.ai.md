Thread identity: Patch series refactoring strbuf usage in gpg-interface.c, completing work started by Junio Hamano to replace unnecessary strbuf_split*() calls with simpler string parsing throughout the file.

Core problem: Eliminate overhead of strbuf operations in gpg-interface.c where strings are split but results aren't modified, making strbuf API unnecessary. Two specific cases addressed: `get_ssh_key_fingerprint()` and `get_default_ssh_signing_key()` now both use direct string manipulation (strchr() + xmemdupz()) instead of strbuf_split_max().

Current status: v4 series complete with both patches submitted. First patch modified `get_ssh_key_fingerprint()`; second patch addresses `get_default_ssh_signing_key()` with same pattern. Christian Couder's reviews of both patches confirm core approach is sound while identifying edge cases: missing newline handling in second patch, memory management sequence in first patch. Both maintain security checks via is_literal_ssh_key() while simplifying memory management. Documentation fixes needed for both patches (Helped-by cleanup, name spelling). Christian's latest review focuses on submission mechanics and cover letter polish.

Key technical details: Both patches replace strbuf_split_max() with direct string extraction (between spaces for fingerprints, at newline for signing key). First patch uses space delimiter, second uses newline. Christian's reviews identified: unused variable `begin` in first patch, potential overuse of die_errno(), need to keep `fingerprint_ret` for proper cleanup, and now missing newline check in second patch. Error checking and security preserved while eliminating temporary strbufs. Follows pattern from wt-status.c. Memory management simplified with xmemdupz() replacing strbuf_list_free().

Open questions: Second patch needs newline check added per Christian's review. Both patches need documentation cleanup (Helped-by trailer fixes, Kristoffer Haugsbakk name spelling correction). Cover letter requires updates: remove references to superseded string_list_split*() approach, fix example commit reference, standardize patch subject formatting ("[PATCH v4 1/2]" style). Submission should use `--in-reply-to` for proper threading. No remaining technical objections to approach.

Participant positions: Junio Hamano originally identified optimization opportunity. Christian Couder provided detailed reviews of both patches in v4, confirming approach while catching edge cases and documentation issues, now also advising on submission mechanics. Author Olamide Caleb Bello has iterated through multiple approaches (string_list_split_in_place() in v3, current simpler solution in v4). Christian's reviews focus on polish and completeness rather than fundamental changes.

Related work: Completes strbuf cleanup started by Junio Hamano. Follows patterns from wt-status.c changes. Tested via GitHub CI. No further strbuf optimizations currently identified in gpg-interface.c.