Thread identity: Patch series refactoring strbuf usage in gpg-interface.c, continuing work started by Junio Hamano to replace unnecessary strbuf_split*() calls with simpler string parsing.

Core problem: Eliminate overhead of strbuf operations in gpg-interface.c where strings are split but the results aren't modified, making strbuf API unnecessary. Two specific cases identified where direct string manipulation (strchr() + xmemdupz()) can replace strbuf_split_max().

Current status: v4 series represents significant change from v3's string_list_split_in_place() approach to even simpler direct parsing. Both patches in series follow same pattern of replacing strbuf operations with lightweight string extraction when results are only used as simple strings.

Key technical details: Both patches modify gpg-interface.c to replace strbuf_split_max() with strchr()/xmemdupz() pattern while maintaining same security checks via is_literal_ssh_key(). Properly handles memory allocation and error cases. Follows established cleanup pattern previously used in wt-status.c.

Participant positions: Junio Hamano originally identified this pattern as suboptimal. Christian Couder provided input on earlier versions. Author (name not provided in summary) has iterated through multiple versions (v3 used string_list_split_in_place(), v4 simplifies further).

Related work: Continues strbuf cleanup work started by Junio Hamano. Follows pattern established in prior wt-status.c changes. Tested via GitHub CI.