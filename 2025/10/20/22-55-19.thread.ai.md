Thread identity: Final iteration (v5) of patch series refactoring strbuf usage in gpg-interface.c, completing work started by Junio Hamano to replace unnecessary strbuf_split*() with direct string parsing. Two patches address `get_ssh_key_fingerprint()` and `get_default_ssh_signing_key()`, now in final form with all technical and documentation issues resolved.

Core problem: Eliminate strbuf overhead in SSH key handling where strings are split but not modified. Both functions now use direct string manipulation (strchr() + xmemdupz()) instead of strbuf_split_max(), maintaining security checks via is_literal_ssh_key() while simplifying memory management. Final edge cases (missing newline in signing key input, space delimiters in fingerprints) explicitly handled with null terminator fallback and proper boundary checking.

Current status: v5 fully implemented with all technical changes complete, including corrected buffer pointer usage in strchr() calls and adjusted error reporting from die_errno() to die() where appropriate. Both patches now properly reference sub-process.c (d6fd08bd) as the canonical example of direct string parsing approach, per Junio Hamano's suggestion. Series is ready for integration with all CI tests passing.

Key technical details: Both patches replace strbuf_split_max() with direct string extraction - space delimiter for fingerprints via two strchr() calls, newline/null terminator for signing key via single strchr(). Error checking preserved while eliminating temporary strbufs. Memory management uses xmemdupz() returns. Security checks via is_literal_ssh_key() remain unchanged. v5 fixes include proper buffer handling in strchr() calls and error reporting adjustments. The `get_default_ssh_signing_key()` implementation specifically uses strchr() to locate line boundaries and xmemdupz() for direct string copying.

Open questions: None. All technical and documentation issues resolved across five iterations.

Participant positions: Junio Hamano identified original optimization opportunity, provided maintainer guidance on attribution, and suggested final commit message reference update to sub-process.c. Christian Couder and Kristoffer Haugsbakk provided thorough v5 reviews, catching edge cases and documentation issues. Author Bello Olamide implemented all requested changes across five iterations and confirmed the final reference update.

Related work: Completes strbuf cleanup started by Junio Hamano. References sub-process.c changes (d6fd08bd) as primary analogy. Tested via GitHub CI. No further strbuf optimizations currently identified in gpg-interface.c.