This is a refactoring patch that prepares for adding `--all` flag functionality to `git repo info` by extracting field printing logic into a dedicated function.

The patch moves the field printing code from `print_fields()` into a new `print_field()` function in builtin/repo.c. The new function takes the same parameters (output format, key name, value/quote buffers) and maintains identical behavior for both KEYVALUE and NUL_TERMINATED output formats. The refactoring reduces code duplication and makes the logic reusable for the upcoming `--all` flag implementation in patch 2/2.

Key technical details preserved:
- Maintains existing format handling with same BUG() check for invalid formats
- Keeps same buffer management pattern with strbuf_reset() calls
- Preserves exact output formatting including quote_c_style() usage
- The change is purely structural - no behavior modifications
- Test coverage remains unchanged as this is preparatory work
- Follows the series' goal of enabling `--all` flag functionality

The patch appears well-contained and mechanically sound, with no obvious issues in the straightforward code movement. The commit message clearly states the purpose as enabling future functionality.