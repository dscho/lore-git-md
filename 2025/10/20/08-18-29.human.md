Refactoring patch consolidating refs optimization code paths. This is the first of a 3-part series cleaning up redundant `pack_refs` interface now that the more generic `optimize` functionality can handle all use cases.

Karthik Nayak's patch removes the legacy `pack_refs` function from the refs backend interface, keeping only the `optimize` variant which serves the same purpose. The changes touch all ref backends (files, packed, reftable) and their supporting infrastructure, but maintain identical functionality while simplifying the codebase. The commit message notes this is purely internal cleanup with no behavior changes, and all callers already use the `optimize` interface.

The changes appear mechanical and well-contained, removing 44 lines while adding 12. This looks like straightforward technical debt reduction as part of the ongoing ref backend abstraction effort, building on previous work that introduced the `optimize` functionality. The patch series seems ready for review as a non-controversial cleanup.