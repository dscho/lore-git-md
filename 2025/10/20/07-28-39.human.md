This is a discussion about making `git add` respect the `ignore=all` submodule configuration consistently with other commands, part of an ongoing patch series. Claus Schneider responds to Phillip Wood's technical concerns about the proposed changes.

Claus acknowledges the historical context Phillip raised about why `git add` and `git reset` were designed to override submodule config, but argues from an end-user perspective that the current behavior is confusing. He outlines what he believes would be a more logical behavior for various commands (`status`, `diff`, `add`, `reset`, `clean`) when dealing with `ignore=all` submodules, suggesting they should all respect the ignore setting unless explicitly overridden.

The email reveals some gaps in the current documentation that make it hard for non-developers to understand submodule behavior, particularly around `reset`. Claus proposes that either the current `ignore=all` behavior should be changed to match his more consistent vision, or perhaps a new option value like "always" could be introduced to provide this behavior while maintaining backward compatibility.

This continues the thread's key debate about whether to change `git add`'s behavior and what mechanism (`--force` vs a new option) would be most appropriate. Claus's perspective as an end-user highlights the usability concerns driving his patch series, though the technical objections raised earlier in the thread remain unresolved.