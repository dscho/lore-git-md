This is the second patch in a 3-part refactoring series to consolidate refs optimization code paths. The patch focuses on renaming the remaining `pack_refs` terminology to use the newer `optimize` naming scheme introduced in the first patch.

The key changes include:
- Renames `struct pack_refs_opts` to `struct refs_optimize_opts` across the codebase
- Updates all related flag names from `PACK_REFS_*` to `REFS_OPTIMIZE_*`
- Modifies documentation strings to reflect the new terminology
- Updates all backend implementations (files, packed, reftable) to use the new struct and flag names
- Maintains identical functionality while making the naming consistent with the broader refactoring effort

The changes are mechanical and straightforward, touching 8 files with 30 line changes total (equal insertions and deletions). This continues the series' goal of removing legacy `pack_refs` terminology now that the more generic `optimize` interface can handle all use cases. The patch appears well-scoped and properly implements the naming changes throughout the codebase without introducing any functional changes.