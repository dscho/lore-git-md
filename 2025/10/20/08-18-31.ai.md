Test infrastructure patch completing a refactoring series about refs optimization. This is the third and final patch in Patrick Steinhardt's series to consolidate refs optimization code paths, following the earlier removal of `pack_refs` interfaces and renaming to `optimize` terminology.

The change moves the `test_done` directive out of the shared test script `t/pack-refs-tests.sh` and into the individual test files that include it (`t0601-reffiles-pack-refs.sh` and `t1463-refs-optimize.sh`). This fixes an issue introduced in ac0bad0af4 where having `test_done` in the shared script prevented adding additional tests to the including scripts. The change maintains all existing test functionality while allowing future test additions.

The patch is small (4 lines added, 2 removed across 3 files) and mechanical in nature, aligning with the series' goal of cleaning up refs optimization infrastructure. It follows logically from the previous patches' renaming work, as the test files being modified are those that exercise the refs optimization functionality. The commit message clearly explains the rationale and references the prior change that introduced the issue being fixed.