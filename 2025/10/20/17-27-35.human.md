This is a follow-up to a bugfix patch improving bash completion for `git log` and `git shortlog`. The original patch from KIYOTA Fumiya fixed two completion issues: adding the missing `--exclude=` option and properly handling the different behaviors of `--committer` between the two commands.

In this email, KIYOTA acknowledges Junio Hamano's review feedback about the commit message style and confirms they will update it to match Git's preferred wording ("H" instead of "I'll"). The discussion also surfaces an underlying inconsistency in how `--committer` works differently in `git log` (where it filters commits) versus `git shortlog` (where it groups output without accepting values). While Hamano notes this as a potential issue, KIYOTA opts to keep the completion behavior aligned with the current implementation rather than trying to fix the deeper inconsistency.

The patch appears ready to merge with the minor wording tweak, and while it doesn't resolve the underlying command interface inconsistency, it properly handles the current behavior in the completion system. The changes are focused and well-justified, addressing real completion gaps while leaving room for future discussion about command consistency.