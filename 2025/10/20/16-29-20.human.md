This is a documentation patch that serves as the final polish for the `git sparse-checkout clean` feature series. Junio Hamano is applying Derrick Stolee's supplemental patch (8/8) that improves the advice message shown when Git needs to expand a sparse index. The new wording explicitly tells users to resolve merge conflicts and staged changes before running either `clean` or `reapply` commands, addressing Elijah Newren's earlier feedback about making the workflow clearer.

The patch follows through on the maintainer-approved approach of handling these final documentation tweaks via a supplemental patch rather than a full reroll. While Hamano notes some minor grammatical awkwardness in the commit message, he confirms the technical correctness and merges the change. This completes the series with all documentation now properly aligned between error messages and general advice, including the sparse index advice text that now comprehensively lists all recommended user actions.