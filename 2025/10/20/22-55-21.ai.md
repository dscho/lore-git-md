Refactoring patch completing the strbuf cleanup in gpg-interface.c, following up on Junio Hamano's optimization suggestion. This second patch in the v4 series addresses the remaining case where `get_default_ssh_signing_key()` unnecessarily used strbuf_split_max() to extract the first line of output.

The patch replaces the strbuf splitting with direct string manipulation using strchr() to find the newline and xmemdupz() to duplicate the first line. This matches the pattern established in the first patch of the series and follows similar simplifications done in wt-status.c. The change maintains all existing security checks via is_literal_ssh_key() and preserves the same error handling behavior, while eliminating the memory management overhead of temporary strbufs.

Key technical changes include replacing the strbuf** keys array with direct char* pointers (begin, new_line, first_line), removing the need for strbuf_list_free(), and slightly adjusting the control flow to free the first_line buffer when validation fails. The patch keeps the same warning messages but updates them to work with the new string-based approach.

This appears to be a well-considered change that completes the strbuf optimization work started earlier in the thread. The patch follows established patterns, maintains all safety checks, and simplifies the code as intended. With both patches in the series now submitted, the strbuf cleanup in gpg-interface.c appears complete.