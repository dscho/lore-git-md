Here's the human-readable summary of the thread's current state:

The thread discusses adding an `--all` flag to the experimental `git repo info` command to show all repository metadata keys. The original two-patch series implemented this by first refactoring the field printing logic and then adding `--all` support that could be combined with specific key arguments. The implementation maintained backward compatibility while adding test coverage for various edge cases.

Patrick Steinhardt has now raised concerns about the design choice to allow combining `--all` with specific keys. He points out inconsistencies in the output ordering and a mismatch with the documentation's implied behavior. While Junio Hamano had previously approved the combined approach, Patrick argues that making `--all` mutually exclusive with key arguments would create a cleaner, more predictable interface. The discussion remains open on whether to adjust the behavior before the feature is finalized, with Patrick's objections focusing on interface clarity rather than technical implementation.