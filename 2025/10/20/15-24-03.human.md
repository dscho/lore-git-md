This is a discussion about potential changes to `git grep`'s around binary file handling, continuing the thread about attribute-based filtering.

Thomas Braun responds to Brian m. carlson's concerns about backward compatibility if `git grep` were to ignore binary files by default. While understanding Brian's use cases (like searching PDFs with embedded text or XMP metadata), Thomas maintains that environment-controlled config settings shouldn't break scripts that don't account for them. He notes that users opting into binary exclusion should be aware of the implications.

The email highlights the tension between user convenience (Thomas's desire to avoid repeatedly specifying binary exclusion) and script reliability (Brian's examples where binary searching is necessary). Thomas also observes that while the revived 2012 discussion about attribute-based filtering is interesting, implementing such a system now would face even greater compatibility challenges than it would have over a decade ago.

The exchange shows the complexity of changing default behaviors in widely-used tools, even when the change seems obviously beneficial to some users. The thread continues to explore whether attribute-based filtering could provide a path forward that satisfies both convenience and compatibility needs.