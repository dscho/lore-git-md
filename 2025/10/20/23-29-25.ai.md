This is part 5 of a 9-part refactoring series modernizing xdiff's data structures for Rust FFI compatibility, specifically focusing on splitting the dual-purpose `ha` field in `xrecord_t` into two distinct fields.

The patch separates the previously overloaded `ha` field into `line_hash` (a simple hash of the line content) and `minimal_perfect_hash` (a unique identifier assigned by the classifier). This makes the code's intent clearer by distinguishing between the two different hash purposes that were previously conflated. The change improves readability by making it obvious that line comparisons use both the hash and direct content comparison via `xdl_recmatch()` to handle collisions.

The author notes the lack of feedback on this particular patch, expressing mild surprise that no one questioned either the field name length or the splitting rationale. The patch appears to be a straightforward improvement that fits within the series' broader goal of clarifying xdiff's internals while maintaining behavior.

This change continues the series' pattern of incremental, focused improvements to xdiff's core data structures. The separation of concerns here supports both the immediate goal of code clarity and the longer-term Rust compatibility work by making the hash handling semantics more explicit. With no objections raised during review, the author considers this patch ready to proceed.