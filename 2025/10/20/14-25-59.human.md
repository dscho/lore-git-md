This is a final documentation polish for the `git sparse-checkout clean` series, which has already been approved for merging. The series combines a new `clean` command (to remove tracked-but-sparse directories) with refactoring to remove `the_repository` usage in the sparse-checkout code.

Derrick Stolee follows up on Elijah Newren's review by pointing to a previously sent patch (now patch 8 in the thread) that updates error and advice messages. The changes address Newren's feedback about making the sparse index advice more comprehensive by explicitly mentioning both the `clean` and `reapply` commands. Stolee notes this could be squashed but was sent separately to avoid rerolling the 'next' branch.

The thread shows all major technical issues resolved, with this being the final documentation tweak before merging. The implementation has thorough test coverage and clear user guidance, with only some deferred work around SKIP_WORKTREE bit management acknowledged as future work. Maintainers have approved the series in its current form.