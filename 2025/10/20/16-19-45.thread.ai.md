Here is the updated dense, loss-free AI mode summary incorporating the new discussion:

This thread introduces a new `--all` flag for the experimental `git repo info` command to show all available repository metadata keys. The two-patch series from [author] implements this functionality through: 1) preparatory refactoring extracting field printing logic into `print_field()`, and 2) main implementation adding `--all` flag support.

Patch 1/2 refactors builtin/repo.c by moving field printing code from `print_fields()` into a new `print_field()` function, maintaining identical behavior for KEYVALUE and NUL_TERMINATED output formats. The refactoring preserves all existing format handling (including BUG() check for invalid formats), buffer management with strbuf_reset(), and output formatting with quote_c_style().

Patch 2/2 currently implements `--all` functionality via `print_all_fields()` that allows combining `--all` with specific key arguments, but after discussion between Patrick Steinhardt and Junio Hamano, the design has shifted to enforce mutual exclusivity between `--all` and key arguments. This resolves the interface inconsistency where the documentation synopsis implied mutual exclusivity while the code allowed combination, which created awkward output ordering (all fields first, then command-line keys) without clear use cases for duplicate key output.

The implementation will need modification to either enforce mutual exclusivity at parse time or update the synopsis, with Junio now agreeing with Patrick's position that mutual exclusivity is the better design. Test coverage in t1900-repo.sh currently tests all-keys output, duplicate key handling, invalid key warning behavior while preserving valid output, and backward compatibility - these will need updates to reflect the new interface constraints.

The series remains mechanically sound but requires changes to patch 2/2 to implement the now-agreed-upon mutual exclusivity design. Key technical details remain unchanged: the command handles repository metadata fields (layout.bare, layout.shallow, object.format, references.format) using the refactored `print_field()` infrastructure from patch 1/2. The discussion has reached consensus on the interface design question that was previously open.