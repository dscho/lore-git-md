Here is the updated dense, loss-free AI mode summary incorporating the new discussion:

This thread introduces a new `--all` flag for the experimental `git repo info` command to show all available repository metadata keys. The two-patch series from [author] implements this functionality through: 1) preparatory refactoring extracting field printing logic into `print_field()`, and 2) main implementation adding `--all` flag support.

Patch 1/2 refactors builtin/repo.c by moving field printing code from `print_fields()` into a new `print_field()` function, maintaining identical behavior for KEYVALUE and NUL_TERMINATED output formats. The refactoring preserves all existing format handling (including BUG() check for invalid formats), buffer management with strbuf_reset(), and output formatting with quote_c_style().

Patch 2/2 implements the `--all` functionality via new `print_all_fields()` that iterates through all defined repository metadata fields (layout.bare, layout.shallow, object.format, references.format) using the refactored `print_field()` infrastructure. The current implementation allows combining `--all` with specific key arguments, where `--all` outputs all fields first followed by any additional specified keys (including duplicates).

Patrick Steinhardt has raised design concerns about this implementation, noting: 1) the documentation synopsis implies mutual exclusivity between `--all` and key arguments while the code allows combination, 2) the output ordering becomes inconsistent (all fields first, then command-line keys), and 3) there appears to be no strong use case for duplicate key output. He suggests making `--all` and key arguments truly mutually exclusive for a cleaner interface, despite Junio Hamano's previous preference for the combined approach.

Test coverage in t1900-repo.sh currently tests: all-keys output, duplicate key handling, invalid key warning behavior while preserving valid output, and backward compatibility. The discussion has reopened the design question of whether to maintain the current behavior or modify it to enforce mutual exclusivity between `--all` and key arguments. The series remains mechanically sound but now has an open design question about interface consistency versus flexibility.