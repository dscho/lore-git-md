This is a discussion about making submodule `ignore=all` behavior consistent across Git commands, particularly focusing on the interaction between `git add` and `git reset`.

Claus Schneider responds to Phillip Wood's technical concerns about the patch series by providing an end-user perspective on how `ignore=all` should logically behave across different Git commands. He argues that from a user standpoint, `ignore=all` should consistently suppress submodule changes in status/diff views and require explicit `--force` to modify them in action commands like `add`, `reset`, and `restore`. He acknowledges the documentation creates confusion about `reset`'s behavior and suggests the current implementation may need clearer semantics.

Key points in this response:
- Documents the user's confusion about `reset` behavior with `ignore=all`, noting the documentation is unclear
- Proposes a comprehensive vision for how `ignore=all` should work across Git's command categories (pre-action, action, post-action)
- Suggests potentially introducing new configuration values like "always" or "logical" to clarify the behavior
- Maintains that the current patch's approach aligns submodule handling with how ignored files work
- Acknowledges Phillip's point about `reset` needing similar treatment if `add` is changed

The email continues the thread's central debate about whether to:
1) Make `add` respect `ignore=all` (current patch approach)
2) Keep `add` and `reset` behaving differently from `diff` (historical behavior)
3) Find a third way with new options/values to clarify the semantics

This response shifts the discussion toward user experience considerations while still needing to address Phillip's technical concerns about implementation approach and backward compatibility. The submodule behavior design questions remain unresolved.