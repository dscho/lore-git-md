This is a technical review response in the ongoing SHA-1/SHA-256 interoperability patch series, specifically addressing patch 5/14 which introduces Rust hash algorithm abstractions. Junio C Hamano raises two key points about the implementation details.

First, he questions the handling of unknown hash algorithms in the `ObjectID` slice accessors, noting that silently returning the full hash buffer for unrecognized algorithms (rather than erroring) seems surprising. This relates to the FFI-safe `ObjectID` struct design that was previously agreed upon, where the `algo` field uses `u32` to match C's `uint32_t`.

Second, Junio observes that the Rust implementation appears to be a near-complete rewrite of the C's `hash.h` functionality, including even the relatively obscure `block_size` method that's only used for push certificate computation in `receive-pack.c`. This suggests the Rust abstraction may be more comprehensive than strictly necessary for the current interoperability work.

The exchange continues the thread's pattern of careful FFI boundary scrutiny, with Junio focusing on behavioral edge cases and implementation scope rather than the core design decisions already settled in earlier patches. No new technical disagreements emerge, but the review highlights areas where the Rust implementation choices might benefit from additional justification or potential simplification.