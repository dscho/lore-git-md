This is a small refactoring patch in the gpg-interface code, part of a larger series combining cleanups and translation work. The patch simplifies SSH fingerprint parsing by removing a redundant `strstr()` call that was computing the same string search twice. 

Junio C Hamano reviews the change and agrees it's an obvious optimization, though he notes the underlying validation logic (looking for "key " anywhere in the line) might deserve scrutiny separately. The patch doesn't change that behavior - it just makes the existing code more efficient by reusing the already-computed result.

This is patch 1/5 in Christian Couder's series, which also includes translation marking for error messages in fast-export, fast-import, and gpg-interface. The change is straightforward and low-risk, focusing on a clear code quality improvement without altering functionality. Junio's review suggests this is ready to proceed as-is, with any deeper validation questions being a separate matter.