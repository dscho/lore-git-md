This is a small refactoring patch in the GPG/SSH verification code. Christian Couder cleans up a redundant string search in the SSH fingerprint parsing logic within `gpg-interface.c`. The patch eliminates a duplicate `strstr()` call by reusing a previously computed pointer, making the code slightly more efficient and easier to follow.

The change is straightforward and low-risk - it doesn't modify any behavior, just removes an unnecessary computation. This was apparently part of some earlier cleanup work that got dropped, and now being resurrected as part of a larger series that also includes internationalization improvements elsewhere in the codebase.

As the first of five patches in this series, this serves as a warm-up - a simple cleanup that stands on its own but sets the stage for more substantial translation work in subsequent patches. The change looks correct and well-contained, though not particularly impactful on its own.