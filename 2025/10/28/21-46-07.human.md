This is a preparatory refactoring patch in the atomic ref updates series for `git replay`. It standardizes the command's option validation by switching from a custom error message to Git's standard `die_for_incompatible_opt2()` helper function.

The change replaces a manual check for conflicting `--advance` and `--contained` options with the more maintainable helper function. This paves the way for cleanly adding validation of the new `--ref-action` option in subsequent patches while keeping error message formatting consistent with other Git commands.

The patch is small (3-line change) and mechanical, following established Git patterns. It's part of the final v5 series that has already received thorough review and maintainer approval, with all technical concerns addressed. The change improves maintainability without affecting functionality, keeping the codebase consistent with how other commands handle option conflicts.