This is a preparatory refactoring patch for the atomic ref updates in `git replay` series. It changes the option validation in `builtin/replay.c` to use the standard `die_for_incompatible_opt2()` helper function instead of a custom error message. 

The patch modernizes the validation of mutually exclusive options `--advance` and `--contained` to match Git's standard pattern for option conflict handling. This change is made in anticipation of adding the new `--ref-action` option in the next patch, which will also need to validate option incompatibilities. The standardized helper provides consistent error messages and makes the code more maintainable.

The implementation is straightforward, replacing a direct `die()` call with the helper function while maintaining the same validation logic. This aligns with the series' goal of following Git's established patterns, as noted in the thread summary. The change is minimal (3 lines modified) and focused purely on the validation mechanism, not affecting any actual functionality.

This patch has already been reviewed as part of the v5 series which received maintainer approval, indicating the technical approach is sound. The change demonstrates good code hygiene by standardizing on Git's conventional patterns for option handling.