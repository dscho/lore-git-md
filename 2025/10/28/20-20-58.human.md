This is a technical discussion in the ongoing SHA-1/SHA-256 interoperability patch series, specifically about type safety at the FFI boundary between C and Rust code.

Junio C Hamano clarifies his earlier suggestion about using a typedef for hash algorithm identifiers, emphasizing that he was referring to a C-side enum/typedef approach rather than suggesting passing Rust enums across the FFI. He acknowledges Ezekiel Newren's concern about Rust enum ABI stability but notes the current implementation already uses `u32` on the Rust side (in the `ObjectID` struct) which avoids this issue.

The exchange highlights the careful balancing act in this series between type safety, documentation clarity, and FFI stability requirements. While the core approach of using `uint32_t`/`u32` appears settled, there's still discussion about whether additional C-side type annotations would improve code clarity without compromising safety. This patch remains part of the foundational work for Git's first major Rust integration.