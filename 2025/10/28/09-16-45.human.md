This is a technical review comment on the first patch of the SHA-1/SHA-256 interoperability series introducing Rust infrastructure. Patrick Steinhardt responds to brian m. carlson's patch that makes Rust support mandatory for the interoperability features.

The core of the patch requires Rust for the new loose object format implementation, marking it as experimental since the current interoperability code is considered non-functional anyway. Patrick agrees with the approach but suggests a minor code improvement - using the `MAYBE_UNUSED` attribute instead of a void cast for the unused `repo` parameter when Rust support is disabled.

The exchange shows early technical review of this foundational Rust integration patch, with Patrick offering a small but clear suggestion while generally approving the direction. This appears to be routine patch refinement rather than any substantive disagreement about the series' goals. The suggestion aligns with modern C practices while maintaining the patch's safety guarantees.