Final patch in the atomic ref updates series for `git replay`. This patch adds the `replay.refAction` config option, completing the implementation of configurable atomic updates that was previously only available via command-line flags.

The author responds to Junio Hamano's feedback about the code structure, agreeing to restructure the config parsing to follow Git's conventional pattern of reading config before parsing command-line options. This will use a shared `parse_ref_action_mode()` helper function as suggested by Christian Couder, but with the more standard Git architecture where config is processed first and command-line options override it.

The patch also addresses Phillip Wood's nit about using `test_grep` instead of raw `grep` in tests for better error reporting, which Junio strongly endorsed. With these final adjustments, the series is fully ready for merging, having addressed all technical feedback from multiple reviewers.

This concludes the transition of atomic reference updates from RFC to production-ready status in `git replay`, with config support now matching the command-line interface and following Git's standard patterns.