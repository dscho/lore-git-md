Bug report with reproduction case for Git's merge behavior losing multi-line Java formatting changes during merges where one branch didn't modify the location. Guo Tingsheng provides a public repository (github.com/CoriWork/bug-replay) with detailed reproduction steps showing how a specific Java test file modification (collapsing a multi-line method call into one line) gets dropped during merge when the other branch made no changes to that location.

The key technical detail is that this occurs when a patch is split into two disjoint hunk sets applied to separate branches, then merged back. The actual merged result sometimes reverts the formatting change from branch B despite branch A not touching that code section, producing incorrect output that differs from the original commit's content. The reproduction shows the issue happening deterministically under these specific conditions.

This advances the thread by providing the concrete reproduction materials Elijah Newren requested, moving the investigation from hypothesis to testable case. The repository allows examining the exact merge base and commit history that triggers the issue. The report maintains focus on the core problem - loss of whitespace/multi-line changes during merge when one side is unmodified - while now providing the complete context needed for debugging.