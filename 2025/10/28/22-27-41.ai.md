Documentation patch updating `git bisect` usage strings and man page alignment.

This patch synchronizes the usage strings in `builtin/bisect.c` with the documentation in `Documentation/git-bisect.adoc`. The changes include moving the subcommand synopsis from the DESCRIPTION section to the SYNOPSIS section where it belongs, which also fixes a test (`t0450-txt-doc-vs-help.sh`) that checks for consistency between command-line help and documentation. The patch also documents the previously undocumented `git bisect next` subcommand, which allows explicitly requesting the next bisection step after an interruption.

Key technical details:
- Updates usage strings in `builtin/bisect.c` to match documentation format
- Restructures `git-bisect.adoc` to properly place subcommand synopsis
- Adds documentation for `bisect next` with usage example
- Removes `bisect` from `t0450/adoc-help-mismatches` as it no longer has mismatches
- Follows up on prior discussion referenced in lore.kernel.org link
- Changes are straightforward documentation alignment with no behavior impact

The patch appears well-scoped and correctly implements its stated goals of documentation consistency. The addition of `bisect next` documentation fills a clear gap, and the commit message provides adequate context for the changes.