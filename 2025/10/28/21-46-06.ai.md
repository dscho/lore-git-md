This is the final v5 cover letter for the atomic ref updates in `git replay` feature series, now fully ready for merging after addressing all review feedback. The series transitions atomic reference updates from RFC to production-ready status, making them the default behavior with configurable output modes.

The v5 iteration incorporates final refinements from maintainer Junio Hamano and reviewers Christian Couder and Phillip Wood. Key changes include:
- Added trailing comma to enum definition for future extensibility
- Standardized error message formatting with single-quoted ref names
- Extracted config parsing into helper functions (`parse_ref_action_mode`, `get_ref_action_mode`)
- Improved test suite hygiene with `test_grep`, `test_config`, and better isolation
- Documentation terminology fixes ("ergonomic", "configuration variable")
- Reverted incidental whitespace style change to match Git conventions

The implementation uses Git's ref transaction API (`ref_store_transaction_begin`, `ref_transaction_update`, `ref_transaction_commit`) for atomic behavior. The series includes comprehensive tests in t3650 verifying the new atomic behavior, config precedence, and backward compatibility.

All technical reviewers have signed off, and the series is now fully merge-ready after addressing the final round of minor documentation and formatting nits. The cover letter provides detailed change summaries and range-diffs against v4 patch. The implementation follows Git's standard patterns for config parsing and error handling, modeled after similar features in `builtin/am.c` and `builtin/column.c`.