This is a technical discussion in the ongoing SHA-1/SHA-256 interoperability patch series, specifically addressing the FFI boundary design for hash algorithm representation. The email clarifies a point about type safety in the C/Rust interface. Junio C Hamano responds to Ezekiel Newren's concerns about using Rust enums across the FFI boundary, explaining that his earlier suggestion about a C-side `hash_algo_type` typedef was purely about C code organization and documentation value, not about exposing Rust enums to C. He confirms understanding that the Rust implementation already represents the algorithm as a `u32` in the `ObjectID` struct, which aligns with the thread's earlier consensus on using FFI-safe primitive types rather than Rust enums at the boundary.

Key technical points:
- The discussion centers on patch 3/14 which establishes the hash algorithm representation
- Junio clarifies his position was about C-side typedefs for code clarity, not Rust enum usage
- Confirms the current Rust implementation uses `u32` for algorithm field in `ObjectID`
- Reiterates that Rust enums are unsuitable for FFI due to ABI instability
- Shows alignment on using primitive types (`uint32_t`/`u32`) at the FFI boundary

This email resolves a minor misunderstanding in the earlier discussion while reinforcing the technical approach already taken in the series. The core design decision (using primitive types at the FFI boundary) remains unchanged, with Junio's clarification providing additional context about his earlier suggestions regarding C-side code organization.