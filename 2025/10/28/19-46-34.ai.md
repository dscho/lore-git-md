This is a feature patch in the final polishing stage of the atomic ref updates series for `git replay`. The email contains test improvements from Siddharth Asthana in response to Phillip Wood's review of the config option tests in patch 3/3.

Key technical details addressed:
- Replaces direct `git rev-parse` with existing tag references (`topic1`) for test setup consistency
- Switches from manual config cleanup to `test_config` helper
- Replaces `grep`/`test_line_count` with `test_cmp` and `test_grep` for better test hygiene
- Maintains test coverage for config precedence (`--ref-action` overrides `replay.refAction`)
- Keeps invalid config value error handling tests

The changes are straightforward test improvements responding to specific review feedback, with no new functionality or design changes. The patch remains focused on the config option implementation for the atomic ref update behavior, now with more robust test patterns matching Git's testing conventions.

This represents the final round of minor test improvements before the series is ready for merging, as all substantive technical and design issues were resolved in earlier rounds. The changes align with Phillip Wood's test hygiene recommendations and maintain the existing test coverage for the config option behavior.