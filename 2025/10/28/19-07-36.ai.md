This is a technical discussion about Rust FFI implementation details in the SHA-1/SHA-256 interoperability series (patch 4/14). Ezekiel Newren responds to Patrick Steinhardt's questions about the `ObjectID` struct design and file licensing.

The email clarifies why the current `ObjectID` struct design (using a `u32` algo field with fixed-size hash array) is necessary for FFI compatibility, explaining that Rust enums would be unsafe across the C boundary due to potential discriminant mismatches and undefined behavior. Newren notes this matches the existing C struct layout precisely.

On licensing headers, Newren expresses skepticism about including per-file GPL notices in Rust code despite the cover letter's rationale, preferring a single license file approach like Git's C code uses. This continues an ongoing discussion about Rust/C differences in the project's first major Rust integration.

The exchange shows careful attention to FFI safety constraints while maintaining alignment with Git's existing object ID handling. The technical depth suggests this patch is progressing toward consensus, though the licensing question remains open.