This is a code cleanup patch in the gpg-interface subsystem, part of a series combining cleanups with translation work. The patch simplifies SSH fingerprint parsing by removing a redundant `strstr()` call in `parse_ssh_output()`. 

The change is straightforward - it reuses the already-computed `key` pointer rather than recomputing the same string search. Junio C Hamano notes this is clearly an improvement, though he observes the underlying assumption about finding 'key ' anywhere in the line may deserve scrutiny (but that's pre-existing behavior, not introduced here).

This is patch 1/6 in Christian Couder's series that resurrects some previously dropped patches while adding translation markings. The series has already been reviewed with Junio providing feedback on patch 3/6, and this first patch appears uncontroversial. The change is small (1 line modified) but eliminates unnecessary work in the SSH signature verification path.

Key technical details for future reference:
- Modifies `gpg-interface.c`'s `parse_ssh_output()` function
- Changes how `sigc->fingerprint` is populated from SSH keygen output
- Maintains identical behavior while being more efficient
- Part of broader effort touching fast-export, fast-import and gpg-interface
- Builds on prior cleanup work where this optimization was originally