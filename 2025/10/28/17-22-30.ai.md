This is a technical discussion about type safety in the SHA-1/SHA-256 interoperability work, specifically regarding the `hashwrite` function's parameter types.

Ezekiel Newren responds to brian m. carlson's patch that changes `hashwrite`'s count parameter from `unsigned int` to `uint32_t` for Rust compatibility. The change ensures consistent type sizes across the C/Rust FFI boundary. Newren expresses general support for using unambiguous types, aligning with the thread's broader focus on robust cross-language interoperability.

The exchange occurs in patch 07/14 of the series, which introduces Rust infrastructure for handling loose objects across hash algorithms. While this particular message is brief, it represents ongoing attention to FFI boundary details in the larger effort. The discussion shows technical alignment on type safety principles, with Newren's response indicating no objections to this specific change while maintaining the thread's pattern of careful review.