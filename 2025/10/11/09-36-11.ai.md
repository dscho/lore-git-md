This is a documentation patch from an Outreachy participant, clarifying an in-code comment in `commit.c`. The patch improves the explanation around commit re-parsing behavior in `parse_commit_buffer()`.

The original comment suggested the parent list cleanup was for handling leftovers from a failed parse, which could imply an error condition. The new wording makes it clearer that this is a deliberate reset to ensure a clean state before re-parsing, regardless of whether the previous parse failed. The change is small (3 lines modified) but improves the code's maintainability by making the intent more explicit.

The patch appears well-crafted for its purpose - it identifies a real documentation issue and provides a clear, focused improvement. As an Outreachy contribution, it demonstrates good attention to detail in reading and improving code comments. The commit message explains the motivation clearly and the diff itself is clean and minimal.