This is a follow-up in the CI/build system thread about Ubuntu 25.10's sudo-rs compatibility issues. Patrick Steinhardt acknowledges Brian m. carlson's upstream feature request to sudo-rs (issue #1299) for adding `--preserve-env` support. The brief exchange confirms awareness of the current workaround (falling back to C-based sudo) being potentially temporary if sudo-rs implements the needed flag by Ubuntu 26.04. 

The thread continues to track both the immediate CI fix (already implemented in previous patches) and the longer-term solution path through upstream sudo-rs development. No new technical changes are proposed in this message - it primarily serves to coordinate awareness of the Git project's workaround and the parallel upstream effort. The maintainers appear aligned on monitoring sudo-rs development while keeping the current detection-and-fallback logic in place for CI stability.