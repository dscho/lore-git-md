This is a documentation patch from an Outreachy participant, clarifying an in-code comment in `commit.c`. The patch improves the explanation around why `parse_commit_buffer()` clears the parent list before re-parsing a commit.

The original comment suggested the parent list cleanup was for handling leftovers from a failed parse, which could imply an error-handling scenario. The new wording makes it clearer that this is simply ensuring a clean slate before re-parsing, regardless of whether the previous parse attempt failed or not.

The change is small (3 lines of comment text modified) but improves the code's maintainability by making the intent more explicit. This appears to be a straightforward improvement with no behavioral changes - just better documentation for future developers working in this area. The patch follows Git's standard formatting and includes a clear commit message explaining the rationale.