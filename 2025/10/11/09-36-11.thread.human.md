This thread begins with a documentation patch from an Outreachy participant, focusing on improving clarity in `commit.c`. The change updates a comment in `parse_commit_buffer()` to better explain the purpose of resetting the parent list before re-parsing a commit. The original wording could be misinterpreted as handling error cases, while the new version makes it clear this is a deliberate clean-state reset regardless of prior parse status.

The patch is small (just 3 lines modified) but thoughtfully addresses a real documentation gap. As an Outreachy contribution, it shows good attention to detail in reading and improving code comments. The commit message clearly explains the motivation, and the change itself is well-scoped for its purpose. This appears to be a straightforward improvement that makes the code's intent more explicit for future maintainers.