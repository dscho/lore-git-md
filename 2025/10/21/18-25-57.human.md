This is a refactoring patch that exports a helper function as part of the `git repo structure` series. The patch makes `ref_kind_from_refname()` publicly available by moving its declaration from `ref-filter.c` to `ref-filter.h`. This small but strategic change enables the function to be reused in later commits for counting references by type.

The change is straightforward - just removing the `static` modifier and adding the function to the header file with a brief docstring. This follows the pattern seen earlier in the series where internal functions are exposed when needed for broader use. The commit message clearly explains the motivation: this prepares for reference counting functionality coming in subsequent patches.

As part of the larger effort to build repository analysis tooling, this is one of several foundational changes that make internal utilities available to new features while maintaining clean separation of concerns. The patch is minimal and focused, with no behavior changes beyond the intended API exposure.