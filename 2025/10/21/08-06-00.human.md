This is a minor documentation suggestion in the submodule gitdir path encoding series. Patrick Steinhardt notes that the newly exposed `is_rfc3986_unreserved()` function in strbuf.h could benefit from a brief explanatory note, since not all readers will immediately recognize the RFC3986 reference (which defines URL percent-encoding). The function is part of the submodule path encoding implementation that uses URL-style percent-encoding for filesystem safety.

The thread context shows this series is in its final stages with all major architectural decisions settled, so this is purely a documentation polish point. The suggestion is straightforward and uncontroversial - just adding a clarifying comment about the RFC's relevance to the path encoding scheme. Since the series has reached consensus on all technical points, this type of minor improvement is exactly what you'd expect to see in the final review rounds.