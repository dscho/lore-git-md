This is a feature patch implementing the `reword` subcommand for the proposed `git-history` command, part of Patrick Steinhardt's v4 series. The patch focuses on the core functionality for rewriting commit messages while maintaining ancestry relationships.

The implementation includes several key components: commit collection logic that verifies the target commit is reachable from HEAD, message editing infrastructure that supports both direct message input and interactive editor sessions, and commit recreation with the new message while preserving the original tree and parents. The patch also handles various error cases like invalid commit references and parse failures.

In response to Karthik Nayak's review, Patrick addresses several minor issues: fixing header includes, correcting documentation wording ("kept" vs "removed"), aligning code formatting, and removing redundant commit parsing. The discussion reveals thoughtful design choices, like centralizing ancestry checks in `collect_commits()` for reuse across subcommands.

The patch appears technically sound with clear error handling and follows Git's sequencer-style approach. While part of a larger series, this specific implementation of `reword` seems ready for integration, with only minor style and documentation tweaks remaining from the review discussion.