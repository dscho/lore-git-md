Thread identity: Documentation and test updates for `git add -p`'s 'P' (pager) option, improving visibility of an existing feature.

Core problem or goal: Make the 'P' option (print hunk with pager) in interactive add (`git add -p`) more discoverable by properly documenting it alongside the existing 'p' (print without pager) option and updating test expectations to reflect both variants.

Current status: Initial patch submitted, updating documentation in `git-add.adoc` and test expectations in t/t3701-add-interactive.sh. The changes are purely documentation and test updates; no functional changes to the actual 'P' option behavior.

Key technical details: Documentation now shows 'P' as distinct from 'p' in the interactive prompt options. Test cases updated to expect both 'p' and 'P' in prompt strings across various scenarios (hunk navigation, color output, error cases). The patch touches add-patch.c's help text to split the combined "p/P" line into two distinct options.

Participant positions: Single-author patch with no reviews yet. The changes are mechanical documentation and test updates that should be uncontroversial.

Related work: Builds on existing interactive add functionality maintained primarily by Phillip Wood, who has deep expertise in this area. The patch follows the established pattern of improving discoverability of existing features through documentation.