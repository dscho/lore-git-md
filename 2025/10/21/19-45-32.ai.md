This is a discussion about enabling `rerere.enabled` by default, part of the ongoing thread considering whether to make this change for Git 3.0. Taylor Blau supports the general idea of enabling rerere by default but questions whether waiting for a major version bump (3.0) is necessary, suggesting it could potentially be done in the current 2.x series (specifically 2.52).

The email introduces a new consideration in the thread - the timing of the change. While Junio had proposed tying the change to the symbolic milestone of Git 3.0, Taylor argues from personal experience that rerere is already battle-tested and wonders about making the change sooner. This shifts the discussion from "should we enable it by default?" to "when should we enable it by default?" while keeping the technical details of rerere's maturity and usability concerns previously raised in the thread.

Key technical context remains: `rerere` (reuse recorded resolution) would become active by default if implemented, with known usability issues around its output messages and documentation that were previously identified in the thread. The open question now includes whether the version number constraint is meaningful or could be relaxed.