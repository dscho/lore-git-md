This is a follow-up discussion in the ongoing xdiff refactoring series aimed at making the code more Rust FFI compatible. Phillip Wood raises concerns about type casting in the C code after the modernization changes.

Phillip notes that despite converting all `long` struct members to `size_t`, the series introduces many more `(long)` casts in the xdiff code (38 vs 4 in master). He questions why these casts are needed given the type changes, and suggests cleaning up the C code to avoid casting between `long` and `size_t`, especially since Rust will remain optional for some time.

The email includes before/after comparisons of the key xdiff structures (xrecord_t and xdfile_t) showing how types were changed to be more explicit (uint8_t, size_t, uint64_t) and fields were renamed/reorganized. The 9-part series had systematically updated these structures for type safety while maintaining behavior.

This appears to be a quality-focused follow-up to an already merged technical refactoring, with Phillip advocating for cleaner C code during the transition period before Rust integration. The discussion remains constructive, focusing on practical maintainability concerns rather than challenging the overall approach.