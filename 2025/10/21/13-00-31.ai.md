This is a feature patch introducing geometric repacking as a maintenance task, building on the ongoing geometric repack strategy work in the thread. Patrick Steinhardt responds to Taylor Blau's review feedback on the implementation details.

The patch adds a new `maintenance.geometric-repack.auto` config option (default 100 loose objects) to control when the geometric repack task runs automatically. The implementation uses a hybrid approach - performing geometric repacks normally but switching to full repacks with cruft packs when packs would be completely merged, allowing unreachable object expiration.

Key technical points from the discussion:
- Taylor confirms the hybrid approach mirrors GitHub's production setup
- Suggests future optimization potential for `pack-objects --stdin-packs` to mark reachability without full walks
- Proposes making the geometric split factor (currently hardcoded to 2) configurable via `repack.geometricSplitFactor`
- Notes redundant pack geometry calculations between auto-condition and task execution that could be shared
- Discusses test improvements around verifying pack creation counts and loose object checks

Patrick agrees to:
- Add a follow-up patch to make the split factor configurable
- Keep the current test approach (verifying commands via trace2 rather than full pack structure validation)
- Address minor wording and formatting suggestions

The exchange shows productive technical discussion around the implementation details, with both parties aligned on the core design while identifying clear paths for future optimization. The patch appears well-tested (220+ lines of tests in t7900-maintenance.sh) and integrates cleanly with the existing maintenance infrastructure.