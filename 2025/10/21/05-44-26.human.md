This is a discussion about the behavior of the new `--all` flag for `git repo info` in the ongoing feature implementation. Patrick Steinhardt is responding to Lucas Seiki Oshiro's patch that added the `--all` functionality.

Patrick raises two main concerns about the current implementation where `--all` flag can be combined with specific key arguments. First, he points out that the documentation synopsis doesn't match the actual behavior - it suggests `--all` and keys are mutually exclusive when they actually work together. Second, he questions the usefulness of allowing both together, noting it creates inconsistent output ordering where some keys come from the fixed `--all` list while others follow command-line order.

The discussion reveals a design decision point: whether to make `--all` truly mutually exclusive with key arguments (Patrick's preference) or keep the current behavior where they combine (as Junio apparently suggested earlier). Patrick argues the exclusive approach is cleaner since duplicating keys seems unnecessary and the mixed ordering could be inconsistent. He also notes the test cases don't demonstrate a compelling use case for the combined behavior.

This is a follow-up in a thread that previously introduced the `--all` feature through two patches - one refactoring the output code and another implementing the new flag. The current discussion focuses on refining the user interface behavior before finalizing the feature.