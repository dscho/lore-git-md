This is a technical discussion about fixing a regression in `git diff --quiet` behavior, specifically addressing output suppression mechanisms.

Jeff King (peff@peff.net) responds to Johannes Schindelin's questions about the fix for the `--quiet` regression. The discussion centers on whether additional output suppression measures are needed beyond redirecting to /dev/null, particularly regarding the `color_moved` setting that was included in the fix without explicit justification.

Peff explains that his patch was intentionally a minimal revert of the problematic hunk from commit b55e6d36eb, preserving the original `color_moved = 0` line that came with the color-moved feature. He speculates this was originally added to avoid unnecessary computation (since the output goes to /dev/null anyway) rather than for color suppression. The exchange reveals differing approaches to regression fixes - Peff prefers conservative reverts for stability while Johannes advocates for more comprehensive analysis using tools like CodeQL.

The conversation touches on broader questions about the diff subsystem's output control architecture, particularly the relationship between the `/dev/null` safety mechanism and the `dry_run` optimization. Peff acknowledges these are valid concerns but maintains his focus was specifically on fixing the regression in the safest way possible for the maintenance branch. The discussion remains technical and collaborative, with both contributors working toward a robust solution while differing slightly on implementation philosophy.