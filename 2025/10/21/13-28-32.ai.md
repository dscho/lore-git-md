This is a technical discussion about type safety in the xdiff refactoring series, specifically focusing on the proliferation of `(long)` casts after converting fields to `size_t`. Phillip Wood raises concerns that the series introduces more `(long)` casts (38 vs 4 in master) despite converting all `long` members to `size_t`, suggesting this undermines the type safety goals. He argues that since Rust will remain optional for some time, the C code should be cleaned up to avoid these casts between `long` and `size_t`.

The email references the before/after struct definitions showing the type changes (e.g., `long` to `size_t` for `nrec`, `nreff`, and `reference_index`, and `char*` to `uint8_t*` for `ptr`). Phillip's critique focuses on the practical maintainability impact rather than the FFI preparation rationale, noting the tension between type purity and clean C code during the transition period. This continues the thread's ongoing examination of type choices, adding a new concern about cast proliferation that wasn't previously discussed. The issue appears to stem from the series converting struct fields to `size_t` while some surrounding code still operates on `long` values, requiring explicit casts. Phillip suggests this should be addressed to keep the C code clean while Rust integration remains optional.