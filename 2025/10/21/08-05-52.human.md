This is a technical discussion about the submodule gitdir path encoding implementation, specifically addressing config override behavior. Patrick Steinhardt provides feedback on Adrian Ratiu's patch from the v3 series, focusing on two main points:

First, he suggests a code improvement in `submodule.c` to use `repo_config_get_string_tmp()` for cleaner memory management when reading the gitdir path override. This is a straightforward optimization that avoids explicit freeing of temporary strings.

Second, Patrick questions the test implementation's handling of the `submodule.<name>.gitdirpath` config without requiring the `extensions.submoduleEncoding` extension to be enabled. He argues this seems inconsistent with the series' overall design where the extension is meant to gate the new path handling behavior. This raises an important architectural question about when the config override should take effect.

The discussion occurs late in the thread's lifecycle (v5 is already implementing the final design), suggesting this may be catching a subtle edge case in the config handling that wasn't fully considered earlier. Patrick's feedback appears thoughtful and grounded in the series' established design principles, though it's unclear from this email alone whether his concern represents an actual oversight or an intentional design choice.