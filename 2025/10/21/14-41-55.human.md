This is a follow-up in the thread about `protocol.file.allow` behavior in submodules. The original reporter, having accepted the workaround of using `-c` flag, now raises two documentation-related questions: 

1. Whether protocol.allow settings would be the right kind of config to inherit if submodules ever did use local config (a hypothetical question about design principles)
2. Whether the git-submodule manpage should explicitly state that local config is generally ignored, since it currently only mentions specific cases where local config is used (like default remote settings)

The email notes that the current documentation's selective mention of local config usage without the broader context of it being generally ignored created confusion. This suggests the reporter sees a potential documentation gap, though they frame it as a question rather than asserting it's definitely a bug.

The thread context established that the core behavior (not inheriting local config) is intentional due to architectural constraints, so this follow-up shifts focus to improving user understanding through documentation rather than changing the implementation.