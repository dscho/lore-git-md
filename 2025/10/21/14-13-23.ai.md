This is a refactoring patch that's part of a larger series introducing geometric repacking strategies for Git maintenance. It removes the global `repack` variable in builtin/gc.c that was used to accumulate arguments for `git repack`, replacing it with a stack-allocated `strvec` that gets passed through function calls.

The key technical changes involve:
- Removing the static `repack` strvec and passing repack arguments explicitly through functions like `add_repack_all_option()` and `need_to_gc()`
- Creating a new `gc_condition()` wrapper that handles the temporary strvec for maintenance tasks
- Updating all call sites to use the new parameterized approach
- Maintaining identical behavior while making the code more reusable for future patches

The patch is well-motivated as part of the geometric repacking series, with clear goal of making the code more modular for subsequent changes. It touches core gc functionality but keeps the changes localized to argument passing mechanics. The commit message thoroughly explains the rationale and approach, particularly around the maintenance task integration.

This appears to be a clean, focused refactoring that sets up infrastructure needed later in the series while improving code structure. The changes are mechanical but important for enabling the geometric repacking functionality to be cleanly integrated.