This is a performance optimization follow-up in the `git last-modified` series, focusing on bitmap implementation details. Toon Claes responds to Taylor Blau's questions about using EWAH bitmaps versus character arrays and whether to store bitmaps as pointers or values.

Key technical points:
- Benchmark results show EWAH bitmaps (793ms) are slightly faster than character arrays (809ms) while being more memory efficient
- Additional benchmarks reveal storing bitmaps as pointers (784ms) outperforms storing them as values (857ms), contrary to initial expectations
- The response includes a patch making `bitmap_grow()` public to enable the benchmark comparison
- Implementation changes involve commit slab usage (`commit_bitmaps`) and bitmap lifecycle management

The email advances the thread by providing concrete performance data to resolve open questions about bitmap storage strategies. It demonstrates the ongoing refinement of the implementation while maintaining the core algorithm's performance gains. The benchmarks help validate design decisions around memory/performance tradeoffs that were previously theoretical in the discussion.