Performance optimization follow-up for `git last-modified` command. This email from Toon Claes responds to Taylor Blau's questions about the bitmap implementation choices in the ongoing performance work. 

The key findings are benchmark results showing that using EWAH bitmaps (1) performs slightly better than character arrays (1.02x faster) while being more memory efficient, and (2) works better as pointers than values (1.09x faster). Toon provides concrete patch showing how they exposed `bitmap_grow()` to enable the pointer-based approach.

The discussion remains technical and productive, with Toon providing clear data to support implementation decisions. This continues the thread's pattern of methodically evaluating tradeoffs in the algorithm redesign. The benchmarks help validate the current direction while leaving room for future optimization of the bitmap handling.