This is a final review comment in the strbuf refactoring series for gpg-interface.c, specifically addressing the choice of example commit to reference in the commit message. Junio Hamano suggests that the current reference to wt-status.c changes (commit 2efe707054) is no longer the best parallel, since the final version of the patches no longer uses string_list functions. Instead, he points to d6fd08bd (sub-process.c changes) as a more relevant comparison, since both cases avoid strbuf splitting in favor of direct in-place parsing.

The exchange shows the careful attention to detail in this series' review process, with Christian Couder having previously flagged the example as potentially misleading. With this clarification from Hamano, the author (Bello Olamide) can now update the commit message to reference the more analogous sub-process.c changes. This marks the last minor polish needed before the technically complete v4 series can be submitted.

Key technical context for future reference: the final implementation uses direct string manipulation (strchr() + xmemdupz()) rather than strbuf_split_max(), following patterns seen in d6fd08bd. The security checks via is_literal_ssh_key() remain unchanged throughout all iterations. All edge cases (including missing newline handling) were addressed in v4 per earlier reviews.