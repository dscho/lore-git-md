This is a technical review comment in the ongoing hook subsystem refactoring series. Patrick Steinhardt questions the implementation approach for the post-rewrite hook conversion in sequencer.c, specifically regarding how callback data is accessed.

The patch being reviewed (v2 03/10) converts the post-rewrite hook to use the new hook API, introducing a `pipe_from_strbuf` callback function. Patrick notes that the current implementation reaches into the `hook_cb->options->feed_pipe_ctx` struct to access the data to pipe to the hook, and suggests an alternative approach: using `opt.feed_pipe_cb_data` which would make the context available via `pp_task_cb` parameter instead.

This appears to be a style/implementation detail question rather than a fundamental design issue. The comment doesn't indicate any functional problems with the current approach, but proposes what might be a cleaner way to handle the callback data flow. The suggestion aligns with the series' broader goal of standardizing hook API usage patterns.

The thread context shows this is part of a larger effort to convert all hooks to a callback-based API, with previous reviews having addressed similar implementation details in other hook conversions. The series has established core patterns for hook data handling, and this comment appears to be refining one of those patterns for consistency.