This is part 4 of a series making `git add` respect `ignore=all` submodule configurations consistently with other commands. The patch updates existing tests that assumed `git add` would always stage submodule changes, now requiring `--force` to do so when submodules are marked `ignore=all`.

The changes modify three test files to add `--force` where needed:
- Updates `t/lib-submodule-update.sh` to use `--force` when adding submodules in test setup
- Adjusts `t7508-status.sh` to use `--force` in a test checking staged-but-ignored submodules

These test updates maintain existing test coverage while accounting for the new behavior where `git add` skips `ignore=all` submodules by default. The changes are straightforward mechanical updates to match the new expected behavior introduced in earlier patches of the series.

The patch appears well-scoped, focusing only on updating tests that would otherwise fail under the new submodule handling rules. It maintains the pattern established in patch 3 of adding comprehensive test coverage for the new behavior.