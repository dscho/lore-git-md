This is a test update as part of the series making `git add` respect `ignore=all` submodule configurations. The patch updates existing tests that assumed `git add` would always stage submodule changes, now requiring explicit `--force` to match the new behavior.

The changes modify three test files to add `--force` where submodules are being added:
1. `t/lib-submodule-update.sh` - Updates multiple test scenarios where submodules are added during test setup
2. `t7508-status.sh` - Adjusts a test verifying staged-but-ignored submodule behavior

The updates are mechanical additions of `--force` flags to maintain test validity under the new submodule handling rules. This follows logically from the series' goal of making `add` respect `ignore=all` like other commands. The changes appear straightforward and maintain existing test coverage while adapting to the new behavior.

This is the fourth patch in a five-part series, building on the earlier infrastructure changes that introduced the `--force` requirement for ignored submodules. The test updates confirm the core functionality works as intended across different submodule operations.