This is a test patch in the series making `git add` respect submodule `ignore=all` configuration. It adds comprehensive test coverage for the new behavior introduced in patches 1-2.

The new test script `t2206-add-submodule-ignored.sh` verifies that:
1. Submodules with `ignore=all` are properly skipped by default in `git add` operations
2. The `--force` flag correctly overrides the ignore setting when explicitly requested
3. Behavior matches `git status` and `git log` when showing/hiding ignored submodule changes
4. Both direct submodule`) and path-based (`./sub/`) references work with `--force`
5. The ignore setting works whether configured in `.gitmodules` or via command line

The test follows a logical progression from setup through various combinations of ignore settings and add operations, with trace logging to verify the internal handling. It checks both positive cases (where changes should be ignored) and negative cases (where `--force` should override). The test includes verification of status and log output to ensure consistency across commands.

This appears to be thorough test coverage for the new functionality, with careful attention to edge cases like path handling and the interaction between different submodule ignore mechanisms. The tests should help prevent regressions as the series progresses.