Updated AI thread summary:

This thread introduces and documents a patch series to make `git add` respect the `ignore=all` submodule configuration consistently with `status` and `diff` commands. The series modifies core read-cache functionality to treat `ignore=all` submodules like regular ignored files - they won't appear in diffs or be added without `--force`.

Key technical implementation:
- Patch 1 added `ignored_too` parameter to `add_files_to_cache()` and propagated through callers in `builtin/add.c`, `builtin/checkout.c`, `builtin/commit.c`
- Patch 2 implemented core logic in `update_callback()` to skip `ignore=all` submodules unless forced with `--force`
- Patch 3 added comprehensive coverage in `t2206-add-submodule-ignored.sh`
- Patch 4 updated existing tests in `t/lib-submodule-update.sh` and `t7508-status.sh` to add `--force` where needed
- Patch 5 (new) documents the behavior in `git-add` and `gitmodules` man pages, clarifying `--force` requirement and listing all affected commands (`status`, `update-index`, `diff`, `log`)

Test coverage includes:
- Default skipping of `ignore=all` submodules in `git add`
- `--force` override behavior
- Consistency checks with other commands
- Path handling for direct/relative references
- Configuration via `.gitmodules` and command line
- Updated test infrastructure

Documentation now explicitly states:
- `git add --force` requirement for `ignore=all` submodules
- Complete list of commands respecting `ignore=all`
- Relationship between the setting and forced operations

The changes enable workflows where submodules can be loosely tracked without constant SHA-1 updates while maintaining backward compatibility. The series appears complete with both implementation and documentation now covering the new behavior. The test suite provides robust verification across scenarios, and documentation matches the technical changes. No outstanding technical issues remain beyond potential minor wording polish in the documentation.