Updated AI thread summary:

This thread discusses making submodule `ignore=all` behavior consistent across Git commands, with initial patches focusing on `git add` and subsequent discussion expanding to `git reset`. The core tension is between historical implementation choices and user expectations for consistent behavior.

Technical implementation status:
- Original patch set modified `add_files_to_cache()` to respect `ignore=all` via new `ignored_too` parameter
- Added `--force` override capability in `update_callback()`
- Comprehensive test coverage in `t2206-add-submodule-ignored.sh`
- Documentation updates for `git-add` and `gitmodules`

Key discussion points:
1) Phillip Wood's technical critique:
   - Questions changing `add` but not `reset` (historical divergence intentional per 5556808690e)
   - Suggests simpler implementation by removing `override_submodule_config` flag
   - Concerns about `--force` conflating ignore concepts
   - Proposes dedicated option if breaking changes acceptable

2) Claus Schneider's user perspective:
   - Advocates for consistent `ignore=all` behavior across all commands
   - Documents real user confusion about `reset` behavior
   - Proposes new config values ("always", "logical") for clearer semantics
   - Suggests treating submodules like ignored files throughout workflow

Open design questions:
- Scope: Should consistency include `reset`/`restore` or just `add`?
- Mechanism: `--force` vs new dedicated option vs config values
- Backward compatibility: How to handle existing workflows
- Documentation: Need to clarify expected behaviors and command categories

Implementation alternatives under consideration:
1) Current approach (add-only with `--force`)
2) Broader change covering `add`/`reset`/`restore`
3) New configuration values to specify behavior granularity
4) Simpler implementation removing `override_submodule_config`

The thread has evolved from a focused implementation to a design discussion about submodule ignore semantics. Next steps require resolving:
- Whether to expand scope beyond `git add`
- Final decision on implementation approach
- Documentation updates to reflect chosen semantics
- Potential follow-up patches for related commands if scope expands

Test coverage remains robust but may need expansion if scope changes. Both technical and user experience perspectives are now represented in the discussion.