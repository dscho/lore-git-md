Updated AI thread summary:

This thread introduces and documents a patch series to make `git add` respect the `ignore=all` submodule configuration consistently with `status` and `diff` commands. The series modifies core read-cache functionality to treat `ignore=all` submodules like regular ignored files - they won't appear in diffs or be added without `--force`.

Key technical implementation:
- Patch 1 added `ignored_too` parameter to `add_files_to_cache()` and propagated through callers in `builtin/add.c`, `builtin/checkout.c`, `builtin/commit.c`
- Patch 2 implemented core logic in `update_callback()` to skip `ignore=all` submodules unless forced with `--force`
- Patch 3 added comprehensive coverage in `t2206-add-submodule-ignored.sh`
- Patch 4 updated existing tests in `t/lib-submodule-update.sh` and `t7508-status.sh` to add `--force` where needed
- Patch 5 documents the behavior in `git-add` and `gitmodules` man pages

Phillip Wood's review raises important technical concerns:
1) Historical context - commit 5556808690e deliberately made `git add` and `git reset` ignore submodule config, unlike `diff`
2) Questions why change `git add` but not `git reset` if making consistent
3) Suggests simpler implementation by removing `override_submodule_config` flag rather than adding `--force` logic
4) Expresses skepticism about using `--force` to override `ignore=all`, suggesting it conflates two different ignore concepts
5) Proposes a dedicated option might be cleaner if breaking behavior is acceptable

Open questions needing resolution:
- Justification for changing `git add` but not `git reset`
- Whether `--force` is the right mechanism versus a new option
- Potential simpler implementation approaches
- Backward compatibility considerations

Test coverage remains robust (default behavior, `--force` override, path handling, config methods), but design decisions now require reevaluation based on Phillip's feedback. The documentation matches current implementation but may need updates depending on design changes. Phillip's review, while noting his limited submodule experience, provides substantive technical critique that requires addressing before the series can proceed.