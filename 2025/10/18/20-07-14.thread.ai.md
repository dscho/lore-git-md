Updated AI thread summary:

This thread introduces a patch series to make `git add` respect the `ignore=all` submodule configuration consistently with `status` and `diff` commands. Currently `git add` ignores this setting, creating inconsistency and potential for accidental submodule updates. The series modifies core read-cache functionality to treat `ignore=all` submodules like regular ignored files - they won't appear in diffs or be added without `--force`.

Key technical implementation:
- Patch 1 added `ignored_too` parameter to `add_files_to_cache()` and propagated through callers in `builtin/add.c`, `builtin/checkout.c`, `builtin/commit.c`
- Patch 2 implemented core logic in `update_callback()` to skip `ignore=all` submodules unless forced with `--force`
- Patch 3 added comprehensive test coverage in `t2206-add-submodule-ignored.sh`
- Patch 4 (new) updates existing tests in `t/lib-submodule-update.sh` and `t7508-status.sh` to add `--force` where submodules are being added, maintaining test validity under new behavior

Test coverage now includes:
- Default skipping of `ignore=all` submodules in `git add`
- `--force` override behavior verification
- Consistency checks with `git status` and `git log`
- Path handling for direct/relative submodule references
- Configuration via `.gitmodules` and command line
- Updated test infrastructure to use `--force` where needed

Open technical considerations:
- Trace messages may need final polish
- Pathspec matching logic sharing potential with other submodule code
- Behavior now fully aligned with `git status` but differs from historical `git add`
- Test coverage appears thorough but may need expansion for additional edge cases

The changes enable workflows where submodules can be loosely without constant SHA-1 updates. Implementation maintains backward compatibility while fixing the inconsistency between `add` and other commands regarding `ignore=all`. The test suite provides robust verification across scenarios, with patch 4 ensuring existing tests properly account for the new behavior.