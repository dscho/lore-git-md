Updated AI thread summary:

This thread examines submodule `ignore=all` behavior consistency across Git commands, with technical implementation and design discussions progressing through multiple iterations. The conversation has evolved from initial `git add` patches to broader considerations of command behavior alignment.

Technical implementation status:
- Current patch set modifies `add_files_to_cache()` with `ignored_too` parameter
- Retains `override_submodule_config=1` internally for diff generation in warnings
- Includes `--force` override capability in `update_callback()`
- Comprehensive tests in `t2206-add-submodule-ignored.sh`
- Documentation updates for `git-add` and `gitmodules`

Key technical positions:
1) Phillip Wood's maintained arguments:
   - Strong preference for dedicated `--include-ignored-submodules` over `--force`
   - Documents historical divergence rationale (commit 5556808690e)
   - Notes unavoidable internal use of `override_submodule_config` for warnings
   - Suggests careful framing of UX improvements in commit messages

2) Claus Schneider's user experience focus:
   - Advocates unified ignore behavior across commands
   - Proposes config values ("always", "logical") for clearer semantics
   - Highlights real-world user confusion with current `reset` behavior

Implementation constraints revealed:
- Must preserve some `override_submodule_config` usage for warning generation
- Option naming (`--force` vs dedicated flag) impacts conceptual clarity
- Historical behavior changes require careful documentation

Open design questions:
1) Scope expansion:
   - Should `reset`/`restore` be included now or deferred?
   - Potential need for follow-up series if scope changes

2) Interface design:
   - `--force` vs new dedicated option tradeoffs
   - Config value alternatives ("always"/"logical")

3) Documentation requirements:
   - Historical context for behavior changes
   - Clear explanation of new semantics
   - Command categorization if behavior differs

The discussion has reached a technical impasse between:
- Pure separation of ignore concepts (Phillip's position)
- Unified user experience (Claus's position)

Critical next steps:
1) Resolution on option naming (`--force` vs dedicated flag)
2) Final decision on historical behavior justification approach
3) Potential patch revisions addressing:
   - Internal `override_submodule_config` usage documentation
   - Warning message implementation details
4) Awaiting input from submodule subsystem experts

Test coverage remains comprehensive but may require expansion if scope extends to additional commands. The thread now combines deep technical analysis with user workflow considerations, requiring careful balance in any resolution.