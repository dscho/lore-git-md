Updated AI thread summary:

This thread examines submodule `ignore=all` behavior consistency across Git commands, with technical implementation and design discussions progressing through multiple iterations. The conversation has evolved from initial `git add` patches to broader considerations of command behavior alignment.

Technical implementation status:
- Current patch set modifies `add_files_to_cache()` with `ignored_too` parameter
- Now implements `--include-ignored-submodules` option (per Phillip Wood's suggestion)
- Retains `override_submodule_config=1` internally for diff generation in warnings
- Comprehensive tests in `t2206-add-submodule-ignored.sh`
- Documentation updates for `git-add` and `gitmodules` in progress

Key technical positions:
1) Phillip Wood's maintained arguments (now implemented):
   - Dedicated `--include-ignored-submodules` option accepted over `--force`
   - Documents historical divergence rationale (commit 5556808690e)
   - Notes unavoidable internal use of `override_submodule_config` for warnings
   - Suggests careful framing of UX improvements in commit messages

2) Claus Schneider's implementation response:
   - Adopts dedicated option approach while maintaining core functionality
   - Expands commit messages with additional historical context
   - Raises practical workflow questions about patch consolidation
   - Leaves broader command scope (`reset`/`restore`) for future consideration

Implementation constraints confirmed:
- Must preserve `override_submodule_config` usage for warning generation
- Historical behavior changes require careful documentation
- Patch submission workflow questions remain open (consolidation vs split)

Open design questions:
1) Submission mechanics:
   - Single commit vs component-split organization
   - GitGadget PR update procedure

2) Future scope:
   - Deferred reset/restore` command alignment
   - Potential follow-up series needed

3) Documentation completeness:
   - Final historical context wording
   - Warning implementation details

The discussion has resolved the immediate technical impasse on option naming while maintaining the core tension between historical behavior and UX consistency. Test coverage remains comprehensive for current scope. Next steps focus on finalizing:
1) Patch organization and submission
2) Documentation details
3) Potential follow-up work for other commands

Notably absent from this update is any change to the fundamental technical constraint requiring `override_submodule_config` for warnings, nor resolution on whether to expand scope beyond `git add`. The thread now shifts toward practical implementation finalization while leaving broader architectural questions open.