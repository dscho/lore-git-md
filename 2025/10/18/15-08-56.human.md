This is a follow-up discussion in the thread about fixing color consistency in `git status --short -z` output. The original patch has already been reviewed and accepted, resolving an inconsistency where modified markers (`M`) were colored while untracked markers (`??`) were not in this specific output mode.

Langbart confirms the usefulness of the fix for their workflow with `fzf`, noting they've configured `fzf` to handle ANSI colors by default via the `FZF_DEFAULT_OPTS` environment variable. This serves as real-world validation of the patch's value, showing it enables colored status output to be properly displayed when piped to tools like `fzf` with null-terminated input.

The exchange is brief and positive, with no new technical discussion - just appreciation for the fix and confirmation that the solution works as intended in practice. The thread appears to be concluding with this confirmation of the patch's utility in actual usage scenarios.