This thread has been working through the implementation details of making `git add` properly respect the `ignore=all` setting for submodules, bringing its behavior in line with other Git commands. The discussion has progressed through multiple iterations, with Claus Schneider leading the implementation and Phillip Wood providing key design input.

The current patch series (v2) implements the agreed-upon approach across five patches that cover plumbing changes, core behavior modifications, test coverage, and documentation. The technical implementation is largely settled, with Phillip Wood's suggested `--include-ignored-submodules` option already implemented in the code, though Claus expresses some lingering preference for the simpler `--force` naming in this latest email.

Junio Hamano has reviewed the series and confirmed the technical approach aligns with Phillip's recommendations, while requesting some mechanical improvements to the first patch (converting a parameter to a flag bit, fixing style issues, and extracting a helper function). The documentation patch has been updated to clearly explain the new option's behavior and requirements.

While the core implementation is complete, there remain a few final refinements:
- Cleaning up the cover letter to remove outdated `--force` references and improve behavior descriptions
- Implementing Junio's suggested code improvements to patch 1
- Resolving the minor ongoing discussion about option naming (though the implementation already uses Phillip's suggested name)

The thread shows healthy technical debate about CLI design principles, with the participants working through the tradeoffs between descriptive option names (`--include-ignored-submodules`) and simpler, more established patterns (`--force`). With test coverage complete and documentation updated, the series appears nearly ready for final submission once these last items are addressed.