This thread continues the discussion about making `git add` behave consistently with other commands when submodules are set to `ignore=all`. The latest update shows Claus Schneider incorporating Phillip Wood's feedback by replacing the proposed `--force` flag with a more specific `--include-ignored-submodules` option for `git add`, while maintaining the internal use of `override_submodule_config` for warning generation.

Phillip has consistently advocated for keeping submodule ignore behavior conceptually separate from force operations, pointing to historical reasons for the current divergence. Claus, focusing on user experience, has worked to unify behavior while addressing technical constraints. The implementation now includes comprehensive tests and documentation updates, with particular attention to explaining the historical context behind these changes.

While the core `git add` changes appear close to final, two workflow questions remain about commit organization and patch submission. The broader question of whether to extend these changes to `reset` and `restore` commands has been deferred for now. The thread shows productive collaboration between Phillip's technical expertise and Claus's user-centered approach, with the latest iteration finding a middle ground through the dedicated submodule option.