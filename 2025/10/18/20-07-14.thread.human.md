Here's a human-readable summary of the thread:

This thread has been working through changes to make `git add` respect the `ignore=all` setting for submodules consistently with other Git commands. The series, now in its second version, implements a new `--include-ignored-submodules` option (suggested by Phillip Wood) that allows explicitly overriding this behavior when needed.

The implementation is now fully documented, with the final patch adding clear explanations of the new option and updating relevant man pages. The changes clarify that submodules marked with `ignore=all` will now be skipped by `git add` unless the new flag is used, and that the path must be explicitly specified when overriding. The documentation maintains consistency with the technical implementation from earlier patches, which included core plumbing changes, behavior modifications in `read-cache.c`, and comprehensive test coverage.

The series appears ready for merging, having addressed all technical feedback and now completing the documentation side. While broader questions about submodule ignore behavior in other commands like `reset` and `restore` remain, those have been deliberately deferred to keep this series focused on the `git add` use case. The implementation preserves the existing warning system while adding new trace diagnostics for debugging purposes.