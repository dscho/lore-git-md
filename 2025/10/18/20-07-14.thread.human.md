This thread has finalized the implementation of consistent `ignore=all` behavior for submodules, particularly ensuring `git add` aligns with `status` and `diff` by requiring a `--force` flag to modify ignored submodules. The v4 patch series, now in `next`, completed all technical work and documentation, with only minor wording fixes remaining.

Claus Schneider, the series author, has addressed all substantive feedback, including Phillip Wood's implementation suggestions and Ben Knoble's documentation phrasing improvements. Kristoffer Haugsbakk caught two final documentation nits—a missing space after "`log`" and "underlaying" versus "underlying"—which Claus is now corrected. Junio Hamano has already confirmed the series is ready to merge, so these are purely polish changes to the manual pages explaining the new `--force` requirement.

The implementation preserves backward compatibility, sparse checkout behavior, and adds comprehensive test coverage. Claus is now deciding whether to submit the doc fixes as a follow-up patch or amendment, though the core functionality is complete and approved. This marks the final step before the feature graduates from `next` to `master`.