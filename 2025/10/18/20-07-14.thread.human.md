This thread continues the discussion about making submodule `ignore=all` behavior consistent across Git commands, with Phillip Wood and Claus Schneider as the primary participants. The core tension remains between technical implementation concerns and user experience expectations.

Phillip has doubled down on his technical critique, maintaining that the current approach using `--force` conflates distinct concepts and that a dedicated option like `--include-ignored-submodules` would be cleaner. He emphasizes the historical context behind the current divergence between `git add` and `git reset` behavior, suggesting this rationale should be properly documented if the behavior is changed. The discussion has revealed an implementation constraint - the patch still needs internal overrides to generate warning messages, even while trying to respect ignore settings.

While Phillip acknowledges the user experience argument (the current disconnect between `git status` and `git add` output), he's pushing for a more technically sound solution. The thread is now at an impasse between these competing priorities, with Phillip's latest comments suggesting the patch may need revision around option naming and historical justification before gaining his approval. The path forward likely requires either a revised implementation that addresses these concerns or input from other submodule experts to break the deadlock.