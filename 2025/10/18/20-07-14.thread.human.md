This thread is working to align how Git commands handle submodules configured with `ignore=all`, particularly ensuring `git add` behaves consistently with `git status` and `git diff`. The discussion has progressed through three versions of the patch series, with the latest iteration settling on using a `--force` flag approach.

The implementation is now quite mature, with the plumbing changes in place (patch 1) and core behavior implemented through a new `skip_submodule()` helper (patch 2). The test coverage (patch 3) is thorough, verifying all aspects of the new behavior across 8 test cases. The remaining patches update existing tests and documentation to match the new approach.

Key participants have reached consensus on the technical direction. Phillip Wood proposed the `--force` naming that was ultimately adopted, while Claus Schneider implemented the comprehensive solution. Junio Hamano has reviewed the series and requested only minor style fixes, indicating the technical approach is sound. The implementation preserves the warning system and adds debugging support while maintaining compatibility with existing submodule handling.

With the test coverage now complete in this latest patch, the series appears ready for final review pending resolution of the remaining style nits. The work has been carefully structured to focus first on `git add` while leaving potential follow-ups for other commands like `reset` and `restore` for future consideration.