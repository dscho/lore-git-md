Here's the updated human-readable thread summary:

This thread focuses on improving consistency in how Git commands handle submodules configured with `ignore=all`. The discussion began with patches to modify `git add` behavior and has evolved into a broader consideration of command alignment, with active participation from Claus Schneider (implementing) and Phillip Wood (reviewing).

The core change introduces a new `--include-ignored-submodules` option for `git add`, addressing cases where developers unintentionally track changes in ignored submodules. This matches how `git status` and `git diff` already handle such submodules. The implementation carefully preserves the internal use of `override_submodule_config` for warning generation while providing a cleaner user experience.

Phillip Wood's review feedback has been instrumental in shaping the solution - particularly the move from a generic `--force` option to the more specific `--include-ignored-submodules` naming. The current patch set includes comprehensive tests and documentation updates, with Claus addressing all major technical concerns raised in review.

While the immediate `git add` changes are nearly ready, the thread acknowledges this is part of a larger effort. Commands like `reset` and `restore` may need similar adjustments in future work. The discussion now focuses on finalizing documentation details and deciding whether to submit the changes as a single commit or split into components.