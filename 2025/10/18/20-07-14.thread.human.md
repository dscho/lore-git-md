This thread is about making `git add` consistently respect the `ignore=all` submodule configuration, bringing it in line with how `status` and `diff` already handle ignored submodules. The series introduces infrastructure changes to treat `ignore=all` submodules like regular ignored files - they won't appear in diffs or be added without explicit `--force`.

The first three patches laid the foundation: modifying core read-cache functionality to support forced adds of ignored submodules, implementing the actual skipping logic, and adding comprehensive test coverage. The latest patch updates existing tests that assumed the old behavior, mechanically adding `--force` flags where needed in test scripts like `t/lib-submodule-update.sh` and `t7508-status.sh`. These changes verify the new behavior works as intended across different submodule operations.

The series nearly complete (this is patch 4 of 5), it addresses a long-standing inconsistency where `git add` would stage submodule changes despite `ignore=all` settings. The changes enable workflows where submodules can be loosely tracked without constant SHA-1 updates, similar to repo tool's approach, while maintaining backward compatibility. Test coverage appears thorough, verifying both the new core behavior and its integration with existing submodule operations.