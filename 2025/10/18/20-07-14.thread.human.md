This thread has been refining the behavior of `git add` when dealing with submodules marked with `ignore=all`, ensuring it aligns with how `git status` and `git diff` handle such cases. After three iterations of the patch series, the technical implementation is settled: the `--force` flag is now required to add ignored submodules, with comprehensive test coverage and clear documentation explaining the change.

The latest email focuses on final polish. Kristoffer Haugsbakk pointed out minor grammatical fixes needed in the commit message for patch 2/5, suggesting "similar" instead of "simular," "more reasons" instead of "more reason," and "handling" instead of "handle." These are straightforward edits that don't affect the technical substance. The series has already addressed all major design questions, with Phillip Wood's `--force` approach implemented, Claus Schneider's helper functions integrated, and Junio C Hamano's style requests incorporated. Ben Knoble previously improved the documentation phrasing, and now Kristoffer is helping with the final commit-message tweaks.

With all substantive issues resolved and only minor wording refinements remaining, this series appears ready for merging once these last edits are applied. The only open question is when Junio will pick it up for inclusion. Future work on aligning `git reset` and `git restore` with this behavior has been noted but deferred.