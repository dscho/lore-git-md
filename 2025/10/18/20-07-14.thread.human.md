This thread addresses an inconsistency in how Git handles submodules marked with `ignore=all`. The original series modified `git add` to respect this setting, bringing it in line with `status` and `diff` commands. The submodules would now be skipped by default unless explicitly forced with `--force`. The implementation touched core read-cache functionality and added comprehensive test coverage across various scenarios.

The latest contribution is a documentation patch that completes the series by updating the `git-add` and `gitmodules` man pages. It clarifies that `--force` is now required to update `ignore=all` submodules, and lists all commands where this setting takes effect. The changes ensure users understand the new behavior where these submodules are treated consistently across commands while still allowing forced updates when needed. With this documentation update, the series appears complete, having addressed both the technical implementation and user-facing documentation.