This thread has addressed the behavior of `git add` when working with submodules configured with `ignore=all`. The discussion has progressed through multiple iterations, with the final implementation settling on requiring `--force` to stage changes to such submodules, bringing `git add` into alignment with how `git status` and `git diff` handle ignored submodules.

The series has reached its final form with comprehensive test coverage and matching documentation. Key technical points include:
- A new `skip_submodule()` helper function that centralizes the logic for handling ignored submodules
- Preservation of warning messages when users attempt to add ignored submodules without `--force`
- Updates to several test files to account for the new behavior while maintaining all existing assertions
- Clear documentation explaining the `--force` requirement and its interaction with submodule ignore settings

The implementation has been reviewed by Phillip Wood and Junio C Hamano, with no outstanding technical objections. The changes maintain compatibility with existing features like pathspecs and sparse checkouts while providing a consistent user experience across Git commands. With this final documentation patch, the series is now complete and ready for merging. Future work may address similar behavior alignment in `git reset` and `git restore`, but those changes have been deliberately deferred to keep this series focused.