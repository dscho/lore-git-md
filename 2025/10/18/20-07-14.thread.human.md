Here's a human-readable summary of the thread's current state:

This thread has been working through the details of making `git add` handle ignored submodules consistently with `git status` and `git diff`. The proposed solution requires using `--force` when staging changes to submodules marked with `ignore=all`, which helps avoid accidental updates while still allowing intentional ones when needed (like for branch tracking workflows).

The patch series has gone through three iterations, with v3 appearing complete - it includes all the plumbing changes, core behavior implementation, comprehensive tests, and updated documentation. Key contributors like Phillip Wood, Claus Schneider, and Ben Knoble have refined the approach, addressing everything from the warning system to documentation phrasing.

However, there's currently some version confusion. While the implementation seems ready, Junio noted that recent emails reference v2 while the "What's cooking" report shows the series (as "cs/add-skip-submodule-ignore-all") awaiting a reroll based on two remaining review comments. The technical work appears done, but the final submission needs to clearly mark itself as v3 and confirm all feedback is addressed before Junio can merge it.

The core idea remains solid: requiring `--force` creates a sensible default while preserving workflow flexibility, with all the supporting tests and docs now in place. Just needs that final version clarity to cross the finish line.