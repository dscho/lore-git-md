This thread addresses making `git add` behavior consistent with `git status` and `git diff` when dealing with submodules configured with `ignore=all`. The series, now at version 4, introduces a `--force` flag requirement to stage changes to ignored submodules, where previously they would be silently skipped.

Claus Schneider has led the implementation, which carefully maintains backward compatibility while adding the new behavior. The changes span plumbing internals (adding an `ignored_too` parameter), core behavior (a comprehensive `skip_submodule()` helper), and extensive test coverage (including a new dedicated test file with 8 cases). The current patch completes the documentation updates across three man pages to clearly explain the new `--force` requirement.

The technical work appears complete, with all feedback from reviewers addressed. Phillip Wood had some earlier review comments that have since been incorporated, and Ben Knoble contributed improved documentation phrasing. Junio Hamano just needs confirmation about the version numbering (expecting v3 but receiving v4) before merging. The series lands in a good state - it provides consistent behavior while preserving workflow flexibility, with clear user guidance when submodules are skipped.