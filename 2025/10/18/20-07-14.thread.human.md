This thread has finalized the alignment of `git add` behavior with other Git commands when submodules are configured with `ignore=all`. After three iterations of patches and discussion, the series has settled on requiring `--force` to stage submodule updates in this case, matching how `git status` and `git diff` handle ignored submodules.

The implementation, spread across five patches, includes core plumbing changes, a new `skip_submodule()` helper function, comprehensive test coverage in a dedicated test file (t2206), and now complete documentation updates. Phillip Wood's design was implemented with the `--force` naming, while Claus Schneider contributed the implementation details including the warning system and debug tracing. Junio C Hamano reviewed the approach and requested only minor style fixes.

The final documentation patch updates three files to clearly explain the new behavior: `git-add.adoc` now states that `--force` overrides `ignore=all`, while the submodule configuration files document which commands respect the ignore setting. Ben Knoble noted some minor English improvements that could be made to the phrasing, but the technical content is complete and accurate.

With all patches reviewed and the documentation now matching the implementation, this series appears ready for merging. The only remaining open item is future work to align `git reset` and `git restore` behavior, which has been deferred for now. The changes maintain compatibility with pathspecs, sparse checkouts, and existing configuration overrides while providing clear guidance to users about the `--force` requirement.