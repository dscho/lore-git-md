This thread has been working through the details of making `git add` behave consistently with other commands (`status`, `diff`, etc.) when dealing with submodules marked with `ignore=all`. The current implementation requires using `--force` to override the ignore setting, which helps maintain branch tracking in submodules while reducing unnecessary SHA-1 updates.

The series has gone through four iterations, with v4 being the current version that splits changes into logical patches: plumbing modifications, core behavior, new tests, test updates, and documentation. The implementation is thorough, preserving backward compatibility while adding debugging support and user guidance. Test coverage is comprehensive with a new test file plus updates to existing tests.

Junio Hamano has indicated the series appears technically complete and ready for merging, with all prior feedback addressed. The most recent email from Kristoffer Haugsbakk points out two minor documentation fixes (a missing space and a typo) in the final documentation patch. These are polish items that don't affect the technical implementation.

Key participants include Claus Schneider (the author), Junio Hamano (who has approved the approach), Phillip Wood (who provided earlier reviews), Ben Knoble (who improved documentation phrasing), and Kristoffer Haugsbakk (with final grammatical fixes). The series is now in `next` and likely to be merged soon, providing consistent submodule handling across Git commands while maintaining the ability to explicitly override ignore settings when needed.