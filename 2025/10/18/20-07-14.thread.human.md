This thread has been discussing how to make `git add` handle ignored submodules consistently with other Git commands like `status` and `diff`. The proposed solution adds a `--force` flag to override the default behavior of skipping ignored submodules. 

The implementation is now complete after several iterations (currently at v4), with all technical aspects addressed: the plumbing changes to support the new flag, comprehensive test coverage, updated documentation, and preservation of backward compatibility. Key contributors like Claus Schneider (the implementer), Ben Knoble (documentation improvements), and Phillip Wood (review feedback) have shaped the final version.

Junio Hamano, the Git maintainer, has just noted a minor formatting consistency in the sign-off lines between versions - this doesn't affect the technical content. The series appears ready for merging once any remaining administrative points are cleared. The change will provide more consistent submodule handling while maintaining the ability to explicitly override the behavior when needed through the new `--force` option.