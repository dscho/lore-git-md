This is the final patch in a series implementing atomic reference updates for `git replay`, now in its merge-ready state with maintainer approval. The patch adds a `replay.refAction` config option to complement the existing `--ref-action` command-line flag, allowing users to configure the default behavior (atomic updates vs. just printing ref updates).

The email captures final polish discussions between Christian Couder and Junio Hamano. Christian suggests consolidating the string-to-enum conversion logic into a helper function, while Junio prefers the more conventional pattern of handling config first then command-line overrides. They also agree on improving test hygiene by using `test_grep` instead of raw `grep` for better error reporting.

This represents the last style and test-cleanup discussions before merging. All technical aspects of the atomic update functionality have been reviewed and approved by multiple contributors, with only minor implementation details like this remaining. The series is essentially ready to go once these final nits are addressed.