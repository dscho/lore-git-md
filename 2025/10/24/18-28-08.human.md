Bugfix follow-up discussing optimization tradeoffs in Git's pattern matching. This email from Sruteesh Kumar continues the thread about fixing `**` pattern matching in `.gitignore` files, responding to Junio Hamano's review of Jeff King's proposed solution.

Sruteesh questions whether the current prefix-stripping optimization in `match_pathname()` provides any real performance benefit, noting that both the pattern and path must still be compared to the end of the prefix whether done by `match_pathname()` or `fnmatch()`. They ask for clarification on why this optimization exists if it doesn't actually save work.

The email also strongly rejects Junio's suggestion of a more targeted fix that would only adjust the prefix when encountering `**/` patterns, calling this approach "literally bypassing this single scenario in the code which is not a good practice." This suggests a preference for Jeff's more general solution despite its "disgusting hack" characterization.

The discussion remains focused on finding the right balance between correctness (fixing the `foo**/bar` matching issue) and maintaining performance optimizations, with Sruteesh now questioning whether the prefix-stripping optimization is worth keeping at all. The thread continues to explore implementation details while maintaining consensus that the core bug needs fixing.