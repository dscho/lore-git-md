Performance optimization in the commit-reach code. René Scharfe replaces quadratic-time `commit_list_insert_by_date()` calls with a more efficient append-then-sort pattern in several merge-base related functions. The change shows dramatic improvements in pathological cases - a new perf test demonstrates a 94.5% speedup when dealing with 16,384 merge bases. The patch includes thorough test case demonstrating the optimization's impact on a complex merge history.

The change is straightforward - instead of maintaining sorted order during insertion (which requires scanning the list each time), it collects commits in arbitrary order and sorts once at the end. The commit message notes this won't affect normal repositories where merge bases are few, but protects against worst-case scenarios. The patch includes a new performance test that builds a deep merge history to stress the algorithm.

This appears to be a well-considered optimization from an experienced contributor. The commit message clearly explains the motivation and impact, and the new test provides concrete performance measurements. The change touches core commit-walking logic but is focused and unlikely to introduce regressions in common cases.