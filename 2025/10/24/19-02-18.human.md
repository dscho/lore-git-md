This is a bugfix discussion thread investigating a platform-specific issue with `git notes` on NonStop systems.

Randall Becker provides additional debugging details in response to Jeff King's questions about the exact command execution path. The key new information is a gdb session showing that `git notes show HEAD` converts to `git show 1aa950256...` (a notes blob hash) when passed to execvp, which fails immediately on NonStop with error -12. When Randall manually runs this same `git show` command in bash, it reports "no note found" (exit code 1), confirming the notes subsystem is constructing an incorrect command.

The discussion is narrowing in on why the notes subsystem generates this particular blob hash (1aa950256...) instead of using HEAD's commit hash (3fc1917e0...). Jeff King had questioned whether Randall might have accidentally run `git notes show` instead of `git show` in manual testing, but Randall's gdb output proves the exact command being executed. This helps confirm the bug is in the notes-to-show command conversion logic rather than in process execution or pager handling.

The thread remains focused on understanding why the notes subsystem produces an invalid blob hash during command conversion, with platform-specific behavior on NonStop helping to surface the issue. The debugging is methodical, with both participants carefully verifying each other's observations to isolate the root cause.