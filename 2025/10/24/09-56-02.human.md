This is a refactoring patch in Patrick Steinhardt's ongoing object database abstraction series (3/13). It updates function names to match the recent rename from `struct object_directory` to `struct odb_source`, while also aligning with Git's modern naming conventions.

The patch renames two functions: `free_object_directory()` becomes `odb_source_free()` (for freeing a single source) and `free_object_directories()` becomes `odb_free_sources()` (for freeing all sources in a database). The changes are purely mechanical - updating the function names and their call sites in odb.c to match the new naming scheme.

This is a straightforward cleanup that follows logically from the earlier structural changes in the series. The patch maintains the same functionality while making the codebase more consistent. As with the previous patches in the series, this appears to be a well-crafted change with a clear motivation stated in the commit message.