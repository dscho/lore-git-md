This is a reply in the ongoing dry-run refactoring thread, specifically discussing the architectural change in patch 3/4 that replaces the `dry_run` boolean with NULL file pointer checks. Junio C Hamano concurs with Jeff King's explanation of the approach, which converts silent output bugs into segmentation faults to make programming errors immediately visible.

The reply reinforces the technical rationale behind the change: since all dry-run output should go nowhere, using NULL file pointers (rather than a separate boolean flag) ensures any accidental output attempts will crash rather than produce incorrect output. This builds on earlier fixes that revealed the boolean flag approach was prone to missed spots where output could still occur.

Key technical points confirmed in this exchange:
- The change affects core diff machinery in `diff.c`/`diff.h`
- NULL file pointers now serve as the dry-run indicator
- This makes the system fail-fast rather than silently produce wrong output
- References specific commits (b55e6d36eb, 3ed5d8bd73) that demonstrate the problems with the boolean flag approach

The reply indicates maintainer agreement with the architectural direction, though no formal Ack has been given yet. This positions patch 3 as a conceptually sound change that completes the preparatory work from patches 1-2, with patch 4 then cleaning up now-redundant code. The thread shows steady progress toward making dry-run behavior more robust through this NULL pointer approach.