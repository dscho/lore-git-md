This is a test hygiene refinement for the atomic ref updates in `git replay` series.

Phillip Wood follows up on his earlier suggestion about improving test patterns in the series, specifically addressing how to verify the output of `git replay` when using the `--ref-action=print` mode. He initially proposed replacing `test_line_count` plus `grep` with `test_cmp`, but realizes that since the exact ref value isn't known in advance, the original approach using `test_line_count` with `test_grep` (rather than raw `grep`) is actually more appropriate.

This email represents a minor but important test suite polish for the already-approved patch series. It demonstrates the careful attention to test robustness that has characterized the review process, with Phillip Wood catching subtle issues in verification patterns. The change doesn't affect functionality but improves maintainability by using Git's test helper functions properly. This aligns with the series' overall quality focus, where multiple reviewers have contributed to refining the test coverage and verification methods.

The thread context shows this is the final patch in a series that's essentially ready for merging, with only minor documentation and test pattern nits remaining. This particular suggestion relates to Patch 3's test cases for the `replay.refAction` configuration option, where output verification needs to be both precise about the format but flexible about the exact ref values. The use of `test_grep` rather than `grep` follows Git's test suite best practices for portable and reliable output checking.