This is a v2 patch series introducing cbindgen support to Git's Rust infrastructure, building on the previous v1 discussion. The series now consists of 5 patches (up from 3 in v1) and addresses several CI and configuration issues raised in the initial review.

Key changes in v2 include:
- Added two new CI-related patches to reorder GitLab CI jobs and backfill missing Linux jobs
- Incorporated some cbindgen.toml configuration suggestions from Ezekiel Newren while holding off on `usize_is_size_t` due to Ubuntu 20.04 compatibility
- Fixed AlmaLinux 8 CI failures by handling cbindgen unavailability on that platform
- Added autogeneration warnings and tab width configuration to cbindgen.toml
- Split package installation logic in CI to handle platform-specific Rust dependencies

The series maintains its core purpose of enabling Rust-C interoperability through automated header generation, currently focused on verifying varint implementation consistency. The v2 changes show responsiveness to review feedback while maintaining compatibility constraints.

Technical details worth noting for future reference:
- cbindgen.toml now includes autogeneration warnings and 8-space indentation
- CI handles AlmaLinux 8's lack of cbindgen via conditional package installation
- Rust library target renamed from `git_rs` to `rust` in meson.build
- Generated headers remain in root directory for now (vs proposed `interop/`)
- Verification mechanism for Rust/C varint implementations remains core use case
- Makefile properly handles `c-bindings.h` cleanup
- GitLab CI jobs now better aligned with GitHub's order and coverage

The series appears to be progressing well through review, with most major technical points from v1 addressed. Remaining open questions from v1 around header location and workspace structure appear deferred for now. The CI improvements in v2 suggest the series is nearing readiness for integration.