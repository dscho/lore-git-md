This is a bugfix for the debug ref backend that addresses a segmentation fault during ref migration with tracing enabled.

The patch adds a missing `remove_on_disk` implementation to the debug ref backend (`refs_be_debug`). The debug backend wraps another ref backend while adding debug logging, but was missing this callback which caused crashes when running `GIT_TRACE_REFS=1 git refs migrate --ref-format=reftable`. The new implementation follows the same pattern as other debug backend functions - it delegates to the underlying backend while adding trace output.

The change is straightforward and clearly motivated by fixing a concrete crash scenario. The implementation matches the style of existing debug backend functions, calling through to the wrapped backend while adding a trace_printf_key() call. The patch touches only refs/debug.c, adding 9 lines to implement the missing callback and register it in the backend struct.

This appears to be a clear bugfix with no obvious controversy - the debug backend should implement all backend operations to properly wrap the underlying implementation. The patch provides the minimal change needed to fix the crash while maintaining consistency with the debug backend's logging behavior.