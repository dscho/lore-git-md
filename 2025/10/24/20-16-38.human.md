This is a bugfix discussion thread investigating a platform-specific issue with Git's notes functionality on NonStop systems. Jeff King (peff) is analyzing why `git notes show HEAD` fails with an unusual exit code (-12) on NonStop, while manually running the same command through bash works correctly.

The key insight from this exchange is that the debugger shows `git notes show` is correctly constructing a `git show` command with the notes blob hash (1aa95025...), but there's confusion about why manually running this command in bash produces a "no note found" error. Peff questions whether Randall might have accidentally run `git notes show` instead of `git show` when testing manually, as the error message suggests notes functionality is being invoked when it shouldn't be.

The thread has narrowed the issue to notes.c's command construction logic, with platform-specific behavior on NonStop making the bug more visible. The discussion remains focused on understanding the discrepancy between automatic and manual command execution paths.