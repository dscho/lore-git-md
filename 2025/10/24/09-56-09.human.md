Refactoring patch in Patrick Steinhardt's ODB abstraction series (10/13). This patch renames `has_loose_object()` to `odb_loose_source_has_object()` to better reflect its role in the new object database source interface. The change is purely mechanical, updating the function name and its callers in `builtin/pack-objects.c` and `object-file.c` while maintaining identical behavior.

The new name follows the established pattern in this series for functions operating on loose object sources. The patch also moves the function's documentation in `object-file.h` to group it with other loose source operations. This continues the series' systematic approach to renaming and reorganizing loose object handling code as part of the broader ODB abstraction effort.

The changes are straightforward call site updates with no functional impact. Like previous patches in the series, this prepares the codebase for more substantial architectural changes by establishing consistent naming conventions for the new ODB source interface.