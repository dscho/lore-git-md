This is a bugfix patch in the geometric repacking series that improves error handling for invalid maintenance strategies.

The patch addresses a silent failure case where Git would ignore unknown maintenance strategy configurations and silently fall back to a no-op strategy. Patrick Steinhardt changes this behavior to instead fail explicitly with an error message when encountering an invalid strategy. The change helps users discover configuration mistakes rather than having maintenance silently do nothing.

The implementation extracts the strategy parsing logic into a new `parse_maintenance_strategy()` function that will be reused in future commits. The patch includes a new test case that verifies the error message appears when an invalid strategy is configured. This is part of the larger effort to implement configurable maintenance strategies, with this particular change ensuring users get clear feedback about configuration problems.

The change is straightforward and well-tested, continuing the series' pattern of careful error handling improvements. It follows naturally from earlier discussions in the thread about making maintenance behavior more transparent and configurable.