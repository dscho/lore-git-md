This is a technical discussion about submodule `ignore=all` behavior, specifically addressing implementation concerns raised in earlier patches.

Phillip Wood responds to Claus Schneider's defense of making `git add` respect `ignore=all` consistently with other commands. He maintains several key technical positions from his earlier critique: the `--force` approach conflates distinct ignore concepts, and a dedicated option like `--include-ignored-submodules` would be cleaner. Phillip emphasizes the historical context (commit 5556808690e) that intentionally diverged `add`/`reset` behavior from `diff`, suggesting this rationale should be documented when changing it.

The exchange reveals implementation constraints - the current patch still needs `override_submodule_config=1` internally to generate diffs for warning messages, even while trying to respect ignore settings. Phillip acknowledges the user experience argument (disconnect between `status` and `add`) has merit but should be framed carefully in commit messages.

This moves the discussion forward by:
1. Clarifying the technical necessity of keeping some override behavior
2. Reiterating the case for a dedicated option over `--force`
3. Highlighting documentation needs for behavior changes
4. Passing the baton to submodule experts for deeper code review

The thread remains at an impasse between technical purity (separate options for separate concepts) and user experience (unified ignore handling). Phillip's comments suggest the patch may need revision before gaining his approval, particularly around option naming and historical justification.