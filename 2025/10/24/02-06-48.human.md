This is a follow-up discussion about the default behavior for signature handling in `git fast-import` and `git fast-export`. Elijah Newren is raising a concern about the current default (`verbatim`) for the new `--signed-tags` option in Christian Couder's patch series, suggesting it should be changed to `abort` before the 2.52 release.

The series has already undergone extensive review and is otherwise complete, with robust test coverage and documentation updates. The core implementation reuses the shared `parse_signed_buffer()` function and properly handles all signature formats (PGP, X.509, SSH) consistently across both commands. 

Newren's concern focuses specifically on forward compatibility - he worries that defaulting to `verbatim` now might limit future improvements to signature validation. This echoes similar concerns he raised about the recently merged `--signed-commits` option. The discussion represents the final decision point before this otherwise production-ready series can be merged.

The email also contains a minor correction about the thread history (the v3 announcement was accidentally sent as a reply to v1 rather than v2), but this doesn't affect the technical content. The substantive discussion is about whether to change the default behavior from `verbatim` to `abort` for better future flexibility.