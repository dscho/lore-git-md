This is a minor test improvement suggestion for the atomic ref updates in `git replay` series.

Phillip Wood follows up on his earlier review of the test cases in patch 3/3, which adds the `replay.refAction` config option. He initially suggested replacing the combination of `test_line_count` and `grep` with `test_cmp`, but then realizes that since the test doesn't know the exact ref value beforehand, the original approach using `test_line_count` plus `test_grep` (rather than raw `grep`) is actually more appropriate.

This is a small but important test hygiene point - using Git's test helper functions (`test_grep`) rather than direct shell commands (`grep`) makes the tests more robust and consistent with Git's test suite conventions. The exchange shows the careful attention to detail in the final review phase of this otherwise-approved series.

The thread context shows this patch has already been through extensive review and is in its final approval state, with this being one of the last minor refinements before merging. Phillip Wood's suggestion aligns with the overall effort to improve test patterns that was noted in the thread summary.