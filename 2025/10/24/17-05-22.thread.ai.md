This thread presents a 4+1 patch series refactoring Git's dry-run behavior, initiated by Lidong with a follow-up bugfix from Jeff King. The series progresses from technical debt cleanup to architectural changes in how dry-run mode is implemented across Git's diff machinery, culminating in a post-series simplification.

The first four patches established the new architecture: replacing the explicit `dry_run` boolean in `diff_options` with NULL file pointer checks, making programming errors fail-fast via segmentation faults rather than silently producing wrong output. Key changes affected `emit_diff_symbol_from_struct`, `builtin_diff`, `run_external_diff`, and `diff_flush_patch_quietly`. Junio C Hamano and Jeff King endorsed this approach, noting it prevents accidental dry-run output by design. The series touched `diff.c` and `diff.h` with test coverage in `t/t4020-diff-external.sh`, building on ly/diff-name-only-with-diff-from-content.

Jeff King's follow-up patch simplifies `run_external_diff()` by leveraging the new architecture's invariants - now only two entry paths exist (from `diff_flush_patch_quietly` for dry-runs or `diff_flush_patch_all_file_pairs` with DIFF_FORMAT_PATCH set). This removes redundant condition checks, handling all file pointer cases (NULL, stdout, other) in adjacent code. The change completes the simplification of dry-run logic by eliminating now-unnecessary conditionals made redundant by the NULL pointer approach.

The series demonstrates a clear progression: preparatory fixes (patches 1-2), architectural change (patch 3), cleanup (patch 4), and post-series optimization (Jeff King's patch). All focus on making dry-run behavior robust through the NULL pointer approach. The maintainer's agreement with the technical direction suggests the series is conceptually sound and likely to be merged, with Jeff King's addition further refining the implementation.