This is a refactoring patch in the cbindgen/Rust integration series, specifically renaming the Rust library target in meson.build. 

The patch changes the target name from `git_rs` to `rust` in meson build configuration, making it more intuitive to compile (`meson compile rust`) and matching the existing test target naming convention (`meson test rust`). The change is straightforward - a single line in src/meson.build updates the custom_target name while keeping all other build logic identical.

This appears to be a minor but logical improvement to the build system interface, aligning with the project's naming patterns. The change is part of the larger Rust infrastructure work but is self-contained and non-controversial, simply improving developer ergonomics when working with the Rust components. The patch follows the series' pattern of incremental refinements to the build system integration.