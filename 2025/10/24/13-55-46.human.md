This is a technical discussion about submodule `ignore=all` behavior, specifically addressing implementation choices for `git add` and `reset`. Phillip Wood responds to Claus Schneider's earlier email, continuing the debate about how to handle ignored submodule changes consistently.

Phillip emphasizes the importance of documenting the historical context (commit 5556808690e) when changing behavior, agreeing that the current divergence between `status` and `add` is unintuitive from a user perspective. However, he strongly cautions against using `--force` to override ignored submodules, arguing this conflates two distinct concepts (ignoring changes vs ignoring untracked files). Instead, he proposes a new dedicated option like `--include-ignored-submodules` to avoid accidental staging of ignored files.

The email reveals an implementation constraint - the need to keep `override_submodule_config=1` to generate the diff list for warnings, even while trying to respect ignore settings. Phillip defers to more experienced submodule contributors for final review, suggesting the discussion needs broader input before proceeding.

This exchange highlights the tension between historical implementation choices and user expectations, with both technical and UX considerations in play. The thread appears to be moving toward needing either a new dedicated option or clearer configuration semantics rather than overloading `--force`.