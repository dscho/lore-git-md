Thread identity: Documentation request for `git log` pretty formats, specifically regarding the "oneline" format string equivalent and undocumented placeholder terminology like "title-line" and "full-commit-message".

Core problem: The `git log` manpage describes built-in formats behaviorally but doesn't expose their exact format string implementations or make all descriptive terms consistently searchable/documentable. This creates discoverability issues for users trying to customize log output, particularly when incorporating notes (%N).

Key technical details:
- Built-in formats like "oneline" have undocumented equivalent format strings (now revealed by René Scharfe as `%h %s` with optional `%d` when log.decorate is set)
- Placeholder terminology in descriptions (e.g. "title-line") corresponds to `%s`/`%b`/`%B` but wasn't explicitly documented as such
- Notes (%N) formatting remains limited by having only a single placeholder without subject/body separation and no newline stripping capability
- The `--oneline --notes` combo provides a practical workaround for notes display without format string construction
- Format language unification between `git log` and `git for-each-ref` remains unimplemented despite past discussion
- Historical email-based terminology origins explain some current documentation gaps

Current status:
- Ben Knoble suggested `--oneline --notes` as a simpler alternative to manual format string construction
- Junio Hamano confirmed this workaround's validity while explaining technical limitations
- René Scharfe has now answered the exact format string questions authoritatively
- The core documentation requests about format string equivalences now have concrete answers
- Format language unification is confirmed inactive despite being a past mentoring topic

Open questions:
- Whether to implement named formats as actual format strings (René's suggestion for deeper improvement)
- Best way to present format string equivalences in documentation (now with concrete examples available)
- Whether to document notes formatting limitations more explicitly
- Potential future for format language unification to address current limitations

Participant positions:
- David Moberg (author) identified concrete documentation gaps and suggested improvements
- Ben Knoble contributed a practical workaround using existing flags
- Junio Hamano provided authoritative context about current limitations
- René Scharfe has now provided the missing format string details and suggested potential implementation changes

Related work: This connects to ongoing documentation improvements around pretty formats and the general effort to improve manpage clarity. Also relates to inactive efforts around format language unification between `git log` and `git for-each-ref`. René's suggestion about implementing named formats as actual format strings could represent a new direction for this work.