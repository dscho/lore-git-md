This is a bugfix discussion thread about a platform-specific issue with Git's command conversion in the notes subsystem on NonStop systems.

Jeff King (peff) is clarifying a point in the ongoing investigation of why `git notes show HEAD` fails on NonStop. The key issue is around how the notes command gets converted to a `git show` of an incorrect blob hash (1aa950256... instead of the actual HEAD's 3fc1917e0...). 

Peff questions whether Randall Becker might have accidentally run `git notes show` instead of `git show` when testing the direct invocation, since the "no note found" error comes from the notes subsystem, not `git show`. He's trying to confirm the exact test case that produced the error message, as this affects understanding of the failure path.

The discussion remains focused on narrowing down the exact point where the notes-to-show command conversion goes wrong, with particular attention to how the blob hash gets incorrectly substituted. This appears to be a portable code issue that manifests differently on NonStop due to its execvp() behavior with invalid paths.