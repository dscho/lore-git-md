This is a discussion about default behavior for signature handling in `git fast-import` and `git fast-export`, part of Christian Couder's completed series unifying signature processing. The thread has reached consensus on technical implementation but debates whether the default mode for `--signed-tags` should be `verbatim` (current behavior) or `abort` (Elijah Newren's proposal).

Christian Couder responds to Newren's concern about locking in `verbatim` as the default by noting this was already the longstanding behavior even before these patches, and that the recently merged `--signed-commits` also inherited this default. He argues changing defaults now would disrupt existing users, especially since he plans to soon implement new signature verification modes that may warrant another default change. While open to patches changing the default, he prefers maintaining consistency until the verification work completes.

Key technical context:
- Current implementation uses `verbatim` default in both import/export paths
- Series has already been merged to 'next' (`--signed-tags`) and 'master' (`--signed-commits`)
- Future work planned for signature validation modes that may influence default choice
- The discussion centers on forward compatibility vs. backward compatibility tradeoffs
- No technical objections to the implementation itself, only policy question about defaults
- This represents the final decision point before the series ships in Git 2.52

The exchange clarifies the historical context behind the current defaults while leaving the door open for Newren to propose specific changes if he feels strongly about switching to `abort` before release.