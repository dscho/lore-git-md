This is a bugfix investigation thread focusing on a platform-specific issue with Git's notes functionality on NonStop systems.

Randall Becker provides crucial debugging details showing how `git notes show HEAD` incorrectly converts to a `git show` command with an invalid blob hash (1aa950256...) rather than the actual HEAD commit (3fc1917e0...). The debugger output confirms the exact command being passed to execvp() fails immediately on NonStop with error -12, while manual execution of the same command in bash produces a "no note found" error.

Key technical details:
- The notes subsystem is generating a malformed command by using a notes blob hash instead of the commit hash
- NonStop's execvp() fails immediately with -12 when given invalid input
- Manual testing shows the command should produce "no note found" (exit 1) when run correctly
- The debug session captures the exact argv array being passed to execvp()

This email advances the investigation by:
1. Confirming the exact incorrect hash being generated by the notes subsystem
2. Demonstrating the platform-specific execvp() behavior through debugger output
3. Showing the discrepancy between automatic and manual command execution

The core issue remains in the command conversion logic within builtin/notes.c, where the wrong object hash is being used. The discussion is now focused on understanding why the manual test produces different results than the automatic conversion path.