This is a discussion about default behavior for signature handling in `git fast-import` and `git fast-export`, part of Christian Couder's completed series unifying signature processing.

Christian responds to Elijah Newren's concern about the default `verbatim` mode for the new `--signed-tags` option (mirroring the recently merged `--signed-commits` behavior). He notes that `verbatim` has been the longstanding default for tag and commit signature handling even before these changes, meaning users may already rely on this behavior. While acknowledging Elijah's forward-looking argument about future validation improvements, Christian suggests keeping the current default since he plans to implement new signature-checking modes soon, which would likely become the preferred default later.

The exchange highlights the tension between maintaining backward compatibility and enabling future improvements. Christian leaves the door open for Elijah to propose default changes via separate patches if desired, but indicates his own preference to wait until the new validation modes are ready before revisiting defaults. This appears to be the final discussion point before this well-reviewed series can be fully settled.