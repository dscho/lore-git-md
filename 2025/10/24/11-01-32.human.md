This is a code review follow-up in the atomic ref updates series for `git replay`. Christian Couder suggests two small improvements to the final patch that adds the `replay.refAction` config option.

First, he proposes extracting the ref action mode logic into a dedicated function for better organization and clarity. His suggested `get_ref_action_mode()` would handle both command-line and config values in one place with proper type safety via the existing `REF_ACTION_UPDATE`/`REF_ACTION_PRINT` enum.

Second, he notes that the tests should use `test_grep` instead of raw `grep` for better error reporting when checking the output format. This is a minor but useful consistency improvement for Git's test suite.

The feedback comes as the series is in its final review stage, with all major technical aspects already approved. These are polish suggestions rather than blocking issues - the implementation itself is sound and ready to merge after addressing these last nits. The thread context shows this patch has already been reviewed by multiple maintainers and is effectively merge-ready.