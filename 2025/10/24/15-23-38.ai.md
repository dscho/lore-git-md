This is a style discussion in the final review phase of the atomic ref updates series for `git replay`. Junio Hamano responds to Christian Couder's comment about a whitespace change in the `replay_usage` array declaration, pushing back against what appears to be an unnecessary reformatting.

The key points are that (1) the preimage style (with spaces around the `*` in `const char * const`) matches the prevalent codebase convention, (2) such style-only changes should not be mixed into feature patches, and (3) the justification about CI tests seems questionable since the original would presumably fail the same tests. This is a minor nit in an otherwise approved series, showing the maintainer's attention to codebase consistency even in final review rounds.

The discussion doesn't affect the technical implementation of atomic ref updates (handled via `ref_store_transaction_begin()` and related APIs) or the test coverage (using `test_grep` for verification), which remain approved. This is purely about code presentation style in one declaration, with Junio advocating for maintaining existing patterns unless there's a compelling reason to change.