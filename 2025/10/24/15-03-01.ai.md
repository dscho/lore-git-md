This is a discussion about the default behavior for signature handling in `git fast-import` and `git fast-export`. The thread has reached consensus on the technical implementation but is debating whether the default mode for `--signed-tags` should be `verbatim` (current behavior) or `abort` (proposed by Elijah Newren).

Junio Hamano responds to Newren's concern about forward compatibility by pointing out that the series is actually improving the status quo - previously there was no choice but verbatim copying of signatures. He argues that changing the default now would break existing users who rely on the current behavior, and that the series' main value is making the behavior configurable rather than changing defaults. This maintains backward compatibility while allowing future work to introduce verification capabilities that could justify changing defaults later.

Key technical details:
- The `--signed-tags` option offers five modes: `verbatim`, `warn-verbatim`, `warn-strip`, `strip`, and `abort`
- The parallel `--signed-commits` option shares the same semantics
- Current implementation defaults to `verbatim` to match historical behavior
- Newren wants `abort` as default to prevent future compatibility issues
- Hamano counters that verbatim was the only option before this series

The discussion reveals a tension between backward compatibility (maintaining existing behavior) and forward-looking security (wanting to eventually verify signatures). Hamano's position is that making the behavior configurable is sufficient for now, and default changes should wait until proper verification is implemented. The exchange doesn't resolve the disagreement but clarifies the historical context and constraints.