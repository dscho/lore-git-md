Maintainer style nit on atomic ref updates series.

Junio Hamano responds to Christian Couder's comment about a minor code style change in the atomic ref updates patch series. The discussion focuses on whether a whitespace adjustment in `replay_usage[]` declaration is necessary or appropriate in this context.

Junio notes that the existing style (with spaces around the asterisk) is more prevalent in the codebase and questions whether this change was motivated by CI requirements. He suggests avoiding such incidental formatting changes in feature/bugfix patches unless they're part of a dedicated style cleanup. The comment implies this doesn't affect the series' approval status but serves as guidance for future contributions.

This is a minor stylistic point in an otherwise approved series, showing the maintainer's attention to codebase consistency even in small details. The core functionality of making atomic ref updates default in `git replay` remains uncontested.