This is a refactoring patch in the ongoing series adding `--trailer` support to `git rebase`. The change simplifies trailer handling by removing redundant `--trailer` prefix processing in the `amend_strbuf_with_trailers()` helper function. 

The patch modifies three files to make callers pass plain trailer text directly rather than having the helper strip the `--trailer=` prefix. This eliminates some string processing and makes the code more straightforward. The changes are mechanical but meaningful - they clean up the interface between the command-line parsing and the trailer processing internals.

This appears to be a straightforward improvement that builds on the earlier refactoring work in the series. The patch is small and focused, touching only the necessary parts of `builtin/commit.c`, `builtin/tag.c`, and `trailer.c` to achieve its goal. The commit message is clear about what's being changed and why.

As part of a larger series that has already seen multiple iterations, this patch shows attention to detail in refining the implementation based on earlier feedback. It doesn't introduce new functionality but makes the existing code cleaner and more maintainable.