This is a test refinement patch in the ongoing `--trailer` support for `git rebase` series. It improves error message verification in the test suite by making the assertion more specific.

The change modifies t3440-rebase-trailer.sh to check that the error message from `git rebase --apply --trailer` explicitly mentions the `--trailer` option when rejecting the apply backend. Previously, the test only verified that the error referenced the merge backend requirement. The new assertion ensures users get clear feedback about which option triggered the backend restriction.

The patch is minimal (1 line changed) and focused, maintaining all existing test coverage while making the verification more precise. It follows the series' pattern of incremental test improvements, in this case ensuring error messages properly identify the problematic option. The change doesn't affect runtime behavior - it just makes the test's expectations more explicit.