This is a test refinement patch in the ongoing `--trailer` support for `git rebase` series. It improves error message verification in the test suite by making the test explicitly check that the error message for using `--trailer` with the apply backend includes the option name in the output. The change is minimal (just one line modified in t3440-rebase-trailer.sh) but makes the test more precise by ensuring the error message clearly references the problematic `--trailer` option when rejecting the apply backend.

The patch maintains all existing test coverage while adding this additional verification. It's a straightforward improvement that follows the pattern of making error messages more explicit and actionable, which is particularly useful when users encounter this backend limitation. The change is well-contained and clearly motivated as part of the larger series' focus on robust error handling.