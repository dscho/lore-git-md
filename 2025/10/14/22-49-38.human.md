This is a technical discussion about patch-id hash algorithm compatibility, continuing the thread about SHA-1 versus SHA-256 usage in patch IDs.

brian m. carlson responds to Junio Hamano's argument that SHA-1 should be preserved for patch IDs due to its non-security role in content identification. They counter that the ship has already sailed on SHA-256 support, pointing out that Git has used SHA-256 in SHA-256 repositories since version 2.29 (2020), and users now expect this behavior. 

The email highlights two key concerns: first, that some distributions may need to drop SHA-1 entirely for compliance reasons, and second, that the current implementation already supports SHA-256 in certain contexts. brian m. carlson proposes a compromise solution - adding an option to explicitly choose the hash algorithm, which would allow users in SHA-256 repositories to still request SHA-1 if needed for backward compatibility.

This continues the thread's central tension between long-term identifier stability and future-proofing, with brian m. carlson emphasizing the practical realities of SHA-1 deprecation timelines and existing SHA-256 usage. The suggestion of a configurable algorithm represents a potential middle ground that could satisfy both stability and flexibility requirements.