This is a technical discussion about patch-id hash algorithm selection, continuing the thread about SHA-1 versus SHA-256 usage in patch IDs. brian m. carlson responds to Junio C Hamano's position that SHA-1 should be preserved for patch IDs due to its non-security content identification role.

The email makes several key points about the current state and future needs: First, it notes that SHA-256 patch IDs are already in use in SHA-256 repositories since Git 2.29, creating user expectation that must be maintained. Second, it emphasizes regulatory pressures that will eventually require SHA-1 removal from some Git distributions, making algorithm flexibility necessary. 

brian m. carlson proposes a compromise solution: adding configuration to explicitly select the hash algorithm, allowing SHA-1 use in SHA-256 repositories when needed for historical comparison while still supporting SHA-256 as the default. This addresses Junio's use case while maintaining forward compatibility with SHA-1 deprecation requirements.

The discussion highlights two competing technical constraints: the need for stable long-term patch identifiers versus compliance with cryptographic standards. The proposed configuration option would provide an escape hatch for both requirements, though it would add complexity to the patch-id implementation. This builds on the earlier discussion by moving from theoretical concerns to concrete implementation suggestions while acknowledging the reality of existing SHA-256 usage.