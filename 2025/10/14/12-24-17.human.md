Refactoring patch in the `--trailer` support series for `git rebase`. This is a minor optimization in the trailer processing internals that moves the initialization of two linked list heads (`config_head` and `arg_head`) into the conditional block where they are actually used. The change reduces unnecessary memory allocation when `opts->only_input` is true, though the commit message notes there is no functional change.

This appears to be a straightforward cleanup that follows naturally from the larger refactoring work in earlier patches of the series. The change is small (3 lines moved) and focused, with a clear explanation of its purpose. Since it's part of a larger, well-reviewed series that's already on its 4th iteration, this patch seems uncontroversial and likely to be accepted.