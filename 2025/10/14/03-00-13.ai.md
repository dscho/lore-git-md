This is a response to a documentation and code clarification patch about patch-id SHA1 usage, from Junio C Hamano to the original author Okhuomon Ajayi.

Junio provides a more nuanced technical review of the patch's approach to enforcing SHA1 for patch IDs. He points out that the original NEEDSWORK comment was actually suggesting a broader change - converting all patch ID computation code to use SHA1, not just the immediate context. The current patch only handles the case where no hash algorithm is set, leaving the case where it's already set unaddressed.

The response highlights potential pitfalls in blindly setting the_hash_algo to SHA1, noting that other parts of the code may still need to access repository objects using the repository's configured hash algorithm. Junio speculates that the patch-id code might be safe since it appears to work only on external text files without accessing repository objects, but this would need verification.

Key technical points raised:
- The original NEEDSWORK comment had a broader scope than addressed in the patch
- Simply setting the_hash_algo to SHA1 could cause problems if other code needs the repository's actual hash algorithm
- The patch-id code might be special-cased to avoid these issues since it works on external files
- A more complete solution would need to carefully separate SHA1 usage for patch IDs from the repository's hash algorithm for other operations

This moves the discussion from a simple documentation alignment to deeper technical considerations about hash algorithm handling in the patch-id subsystem. The response suggests the original patch may be incomplete or potentially problematic in edge cases.