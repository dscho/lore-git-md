Refactoring patch simplifying `git cat-file` submodule handling. This is a follow-up to Taylor Blau's earlier patch that removed redundant OID-to-hex conversion in submodule cases.

Jeff King confirms the change is correct and considers whether further simplification is possible by removing the OID parameter from `report_object_status()` entirely. However, testing reveals this would break error handling for corrupted packs, so he concludes the current patch is the right approach. The change is minimal (just passing NULL instead of an OID conversion) and behavior-preserving.

The patch has received an informal ack from King ("Your patch looks good to me"), suggesting it's likely to be merged. This appears to be a straightforward cleanup with no controversy, building on previous work to streamline submodule handling in batch mode.