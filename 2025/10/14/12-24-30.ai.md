This is a test cleanup patch in the ongoing `--trailer` rebase series. It refactors the test setup in t3440-rebase-trailer.sh to create expected trailer files at their point of use rather than upfront in the shared history setup.

The change moves the `create_expect` calls from the initial test setup block into the individual test cases that actually need them. This:
- Removes unused fixture creation for tests that don't verify trailers
- Makes it clearer which tests depend on which expected outputs
- Reduces the shared state between tests
- Maintains all existing test coverage while simplifying the test file structure

The patch is a straightforward improvement to test organization that follows the test-at-point-of-use pattern seen elsewhere in Git's test suite. It's a small but worthwhile cleanup that makes the test file more maintainable without changing any functionality. The change is well-contained (8 lines removed, 4 added) and clearly motivated in the commit message.