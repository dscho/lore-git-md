This is a test modernization patch that updates path verification checks in the worktree pruning tests to use dedicated test helpers rather than direct shell commands. The patch converts 17 instances of `test -f`/`test -d` to their more readable `test_path_is_file`/`test_path_is_dir` equivalents in t/t2401-worktree-prune.sh.

The changes are mechanical and maintain the exact same test semantics while improving code clarity. The test file exercises various worktree pruning scenarios including invalid directories, unreadable gitdir files, locked checkouts, duplicate entries, and proper worktree preservation. All test assertions remain unchanged - only the verification method is updated to use Git's standard test helpers.

The patch appears well-scoped and correctly implemented, touching only the intended test file with straightforward substitutions. Since it's a pure test modernization with no behavior changes, it should be uncontroversial. The commit message is brief but adequately explains the change's purpose. No test coverage is added or removed - this is purely a readability improvement for existing tests.