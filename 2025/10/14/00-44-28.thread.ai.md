[PATCH] refactor gpg-interface.c: replace strbuf_split with string_list_split

This thread explores replacing strbuf_split_max() usage in gpg-interface.c's SSH key handling functions (get_ssh_key_fingerprint and get_default_ssh_signing_key) with string_list_split(). The proposal comes from an Outreachy applicant as a potential microproject, building on prior discussions about modernizing string splitting patterns in the codebase.

The current state is exploratory - no patch yet, just identification of two target functions in the GPG/SSH interface code. The technical merits remain unanalyzed: no comparison of edge case handling, performance implications, or whether string_list_split() is actually better suited than the current strbuf_split_max() approach. The author references but doesn't summarize conclusions from a previous similar thread, leaving unclear if this case was already considered there.

This appears to be an early contribution attempt from a new participant, focused on identifying refactoring opportunities rather than presenting a complete solution. The value at this stage is primarily in surfacing these specific functions as candidates for string splitting modernization, potentially as part of broader code hygiene efforts around string processing utilities.