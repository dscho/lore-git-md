This is a documentation follow-up in the ongoing thread about clarifying `git blame --incremental` output format. Philip Patsch responds to Junio Hamano's earlier typo fix ("stating" -> "starting") by raising a new documentation gap - the manpage currently lacks any definition of what a plain `<rev>` argument means in the context of `git blame`.

The email notes that while `--reverse` is documented in the options section, neither `<rev>` nor `--since` appear there (`--since` is only referenced later in the "SPECIFYING RANGES" section). This suggests the documentation may need to better explain the basic command-line syntax and revision specification for `git blame`, separate from the incremental output format discussion that has been the thread's focus so far.

The exchange continues the pattern of careful attention to documentation precision, now potentially expanding the scope beyond just the incremental output fields to include more fundamental usage explanations. The thread remains focused on improving user-facing documentation without any proposed code changes.