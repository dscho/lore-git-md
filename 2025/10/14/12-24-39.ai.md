This is a test modernization patch in the ongoing `git rebase --trailer` series. It updates the test suite to use the standard `example.com` domain instead of the potentially real `ex.com` in test email addresses, following Git's convention for test data.

The change is mechanical, replacing all instances of `a@ex.com` and `b@ex.com` with their `example.com` counterparts in t3440-rebase-trailer.sh. The test cases being modified verify that multiple `Signed-off-by` trailers are preserved during rebase operations, with the domain change being purely cosmetic and not affecting test behavior.

This patch appears late in the series (26/29) and represents a minor cleanup of test data rather than a functional change. The modification is straightforward and uncontroversial, aligning with Git's standard practices for test data while maintaining all existing test assertions. The patch preserves the test logic exactly while using more appropriate example domain names.