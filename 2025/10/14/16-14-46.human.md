This is a test modernization patch that updates path checks in the worktree pruning tests to use dedicated test helpers. 

Solly's patch converts 17 instances of direct shell `test -f` and `test -d` checks into calls to the more readable `test_path_is_file` and `test_path_is_dir` helpers in t/t2401-worktree-prune.sh. The changes are purely mechanical - the test logic remains identical, just expressed through Git's test framework helpers rather than raw shell commands.

The patch follows an established pattern in the Git test suite of migrating older tests to use the framework's helper functions, which produce clearer output on failure. The changes touch various test cases for pruning worktrees under different conditions (invalid gitdir files, locked checkouts, duplicate entries, etc.), but don't modify any of the test behaviors or assertions.

This is a straightforward, uncontroversial cleanup that improves test maintainability. The patch is correctly focused (only touching path checks in one test script) and the commit message clearly explains the change's scope and purpose.