This is a refactoring patch in the ongoing packfile store API cleanup series. Justin Tobler is reviewing Patrick Steinhardt's change to convert object-name.c from using `packfile_store_get_packs()` to `packfile_store_get_all_packs()`. The change affects two functions in object-name.c that handle object disambiguation and abbreviation length calculation.

The key technical detail is that this patch continues the series' goal of eliminating the stateful behavior between the two pack-getting functions. The reviewer confirms the change is safe since these call sites already handle MIDX'd packs appropriately, despite the old function theoretically excluding them. The diff shows straightforward replacements in `find_short_packed_object()` and `find_abbrev_len_packed()`, with identical iteration patterns just using the more predictable `get_all_packs()` variant.

This appears to be a well-justified change that follows the series' established pattern of first converting callers before later removing the deprecated API. The review is positive, with Justin Tobler noting the change looks good after verifying the MIDX handling implications match the commit message's claims. This patch represents one of the final steps in standardizing pack iteration across the codebase before the series can remove the confusing dual-API entirely.