This is a small but important refactoring patch in the `git-history` series, specifically addressing the `replay.c` infrastructure. The change ensures commits are properly parsed before being dereferenced in `replay_pick_regular_commit()`, making the function more robust for external callers.

Karthik Nayak confirms the technical correctness of Patrick Steinhardt's change, noting that while it might appear to duplicate parsing work, the internal `repo_parse_commit_internal()` function already checks the `object.parsed` flag to avoid redundant work. The patch adds a safety check that will be particularly valuable as this function becomes part of the shared infrastructure for history editing operations.

The change is minimal (just 3 lines added) but demonstrates good defensive programming practice as the `replay.c` code evolves to support more use cases. This aligns with the series' focus on building robust, reusable components while maintaining existing behavior. The review interaction shows careful attention to implementation details from both author and reviewer.