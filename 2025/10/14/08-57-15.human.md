This is a small but important bugfix in the `git-history` infrastructure series, specifically addressing commit parsing in the replay functionality. Karthik Nayak confirms that Patrick Steinhardt's change to explicitly parse commits in `replay_pick_regular_commit()` is correct and won't cause duplicate parsing due to the existing parsed-check in the internal implementation.

The patch ensures that when the replay functionality is called from other contexts (as part of the broader history editing work), commits will be properly parsed before their fields are accessed. Karthik notes this makes the function more robust while avoiding performance penalty from redundant parsing. This is part of the ongoing effort to make the replay infrastructure more reliable for use by the new history editing commands.