This is a test modernization patch updating t/t2401-worktree-prune.sh to use Git's standard test helpers for path verification. The patch mechanically converts 17 instances of direct shell `test -f`/`test -d` commands to the more readable `test_path_is_file`/`test_path_is_dir` helpers while preserving all test semantics. The changes are purely stylistic - the test file continues to exercise worktree pruning scenarios (invalid dirs, locked checkouts, duplicate entries, etc.) with the same assertions, just expressed via the project's preferred test idioms. The patch is well-scoped, touching only this one test file with straightforward substitutions that improve code clarity without altering behavior. As a mechanical test modernization with no functional impact, it should be uncontroversial.