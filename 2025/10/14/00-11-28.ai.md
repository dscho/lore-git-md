Refactoring patch for `git cat-file`'s submodule handling, with review feedback.

Jeff King responds to Taylor Blau's patch that simplifies OID handling in `git cat-file --batch-check` for submodules. The original patch removed redundant OID-to-hex conversion by passing NULL to `report_object_status()` when processing submodules (S_IFGITLINK mode). 

Jeff confirms the change is correct and safe, noting it's a clear simplification. He explores whether further simplification is possible by removing the OID parameter from `report_object_status()` entirely, but finds this breaks a case with corrupted packs in t5313 tests. The test failure demonstrates the OID parameter is still needed as a fallback for error paths.

Key technical details:
- The discussion centers around `report_object_status()` in builtin/cat-file.c
- Current patch modifies `batch_object_write()` to pass NULL for submodules
- Jeff's exploration shows the OID parameter remains necessary for error cases
- Test t5313 would fail with more aggressive simplification

The thread shows consensus that the original patch is good as-is, with Jeff providing an Ack-by-proxy through his positive review. No further changes appear needed beyond what Taylor proposed.