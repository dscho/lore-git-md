This is a test refinement patch in the ongoing `git rebase --trailer` series. It updates an existing test case to verify that trailer configuration mapping works correctly when using both the `trailer.review.key` config and the `--trailer=` CLI syntax with an equals separator.

The change modifies the `rebase --root --trailer` test to use `-c trailer.review.key=Reviewed-by` and `--trailer=review="Dev <dev@example.com>"` instead of directly specifying the `Reviewed-by` trailer. This exercises the configuration mapping feature while still ensuring the same `Reviewed-by` trailer gets added to every commit. The test continues to verify trailer addition to both the first commit and initial empty commit using the existing `expect_trailer_msg` helper.

This is a straightforward test improvement that builds on the series' earlier work while adding coverage for config-based trailer mapping. The change is small (2 lines modified) but meaningful, demonstrating how the feature handles configured trailer mappings in combination with CLI arguments.