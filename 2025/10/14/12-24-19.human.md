This is a small refactoring patch in the larger series adding `--trailer` support to `git rebase`. The change simplifies the non-in-place output path in `interpret_trailers()` by using `strbuf_write()` directly on the output buffer rather than manually calling `fwrite()`. 

The patch is straightforward - it replaces one line of code with a more idiomatic Git API call that achieves the same result. This continues the series' pattern of internal cleanups and follows the project's preference for using `strbuf` helper functions where available. The change doesn't affect functionality but makes the code slightly more maintainable.

As part of the broader effort to move trailer handling into Git's core, this minor optimization helps streamline the I/O path while maintaining all existing behavior. The patch is well-contained (affecting just one file) and follows the established pattern of the series.