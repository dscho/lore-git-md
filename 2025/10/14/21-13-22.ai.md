This is a bugfix for the `git replay` feature series, addressing a documentation inconsistency between the command-line help (`-h`) and full help text (`--help`). Junio Hamano reports that when the atomic ref updates series is merged to 'seen', it breaks t0450 which verifies consistency between these help outputs. The issue stems from a line break difference in the usage string in `builtin/replay.c` - the `-h` output shows the options on one line while `--help` shows them split across two lines.

The proposed fix modifies the usage string in `builtin/replay.c` to match the single-line format used in the documentation, either by squashing this change into the existing series or adjusting the documentation to match. This is a straightforward alignment of help text formats, with the minimal diff showing exactly where the newline character needs to be removed to make the outputs consistent.

The thread context shows this is part of the v3 series making atomic ref updates the default in `git replay`, with all major technical aspects already reviewed and approved. This late discovery of a help text inconsistency is typical of final polish before integration, and the maintainer provides clear guidance on how to resolve it while maintaining the series' overall goals.