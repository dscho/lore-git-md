This is a refactoring patch in a larger series adding `--trailer` support to `git rebase`. It restores a helper function that was previously removed, consolidating duplicate trailer processing logic from `cmd_interpret_trailers()` into a single `interpret_trailers()` function.

The change factors out the common read/process/write sequence used for both file and stdin input paths, maintaining the same error handling and output behavior. The helper handles both in-place file editing and stdout output modes, while the main command function retains responsibility for option parsing and safety checks.

This appears to be a straightforward cleanup that improves code organization without changing functionality, part of the broader effort to move trailer processing into core Git. The patch is small (50 lines net change) and focused, with no obvious issues in the implementation.