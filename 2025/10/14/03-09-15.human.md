This is a follow-up in the thread about Git's `**` pattern matching behavior in `.gitignore` files. Jeff King acknowledges his earlier patch (which fixed the incorrect matching of patterns like `foo**/bar`) should have been marked as RFC, as he had concerns about the implementation approach being a "disgusting hack" despite correctly solving the core issue.

The email itself doesn't introduce new technical content - it's mainly Jeff noting he forgot to properly flag his previous patch as RFC. The substantive discussion remains around whether the current fix (which preserves one character of prefix context in `match_pathname()`) is the right approach, or if a more thorough solution should be developed to handle edge cases while maintaining performance. The thread continues to weigh the tradeoffs between correctness and implementation elegance for this pattern matching corner case.