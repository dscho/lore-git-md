This is a test refinement patch in the ongoing `--trailer` support for `git rebase` series. It modifies the `--root` test case in t3440-rebase-trailer.sh to verify that trailer configuration mappings work correctly during rebase operations.

The change replaces the direct `--trailer` argument with a combination of `-c trailer.review.key=Reviewed-by` config and `--trailer=review="Dev <dev@example.com>"` CLI syntax. This exercises two key features: the ability to map configured keys (where "review" maps to "Reviewed-by") and the equals-separator form of trailer specification. The test continues to verify that trailers are added to both the root commit and subsequent commits, maintaining all existing assertions through the `expect_trailer_msg` helper.

This is a focused test improvement that builds on the series' comprehensive trailer handling verification while adding coverage for config-based key mapping. The change is minimal (4 lines modified) and maintains the test's core purpose of validating `--root` behavior with trailers. It follows the series' pattern of iterative test refinement while introducing new configuration scenarios.