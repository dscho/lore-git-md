This is a minor refactoring patch in the ongoing `--trailer` support series for `git rebase`. The change converts the `had_trailer_before` variable in `trailer.c` from `int` to `bool` to better reflect its logical purpose as a boolean flag. This is a straightforward type cleanup with no behavior change, following the pattern of modernizing Git's codebase to use proper boolean types where appropriate.

The patch is part of a larger effort to refactor trailer handling internals, but this particular change stands alone as a simple improvement to type safety and code clarity. It touches only one line in trailer.c, changing the declaration of a variable used to track whether a commit already had trailers before processing. The change aligns with C best practices for boolean usage and maintains all existing functionality.

This appears to be a clean, uncontroversial improvement that fits well with the series' broader goal of refining the trailer processing implementation. The commit message is concise but complete, clearly stating the nature and scope of the change. As with other patches in this series, it demonstrates attention to code quality details while making minimal, focused changes.