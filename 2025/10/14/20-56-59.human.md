This is a minor CI optimization patch in Patrick Steinhardt's Rust CI infrastructure series. Justin Tobler points out that the patch correctly removes redundant `apt-get update` calls in the CI setup, since the distro-specific step already handles this. 

The observation is straightforward - the current setup makes these subsequent calls unnecessary. Justin also notes a potential fragility in the assumption that all jobs use Aptitude-based distros, though he explicitly states this doesn't need to be addressed in this particular change.

This appears to be a clean, uncontroversial optimization that fits within the broader Rust CI work. The thread context shows this series is in its final stages with major decisions already settled, and this patch seems to be a small but sensible improvement to the implementation.