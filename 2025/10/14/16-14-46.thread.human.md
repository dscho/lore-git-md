This thread introduces a straightforward test modernization patch that updates worktree pruning tests to use Git's dedicated test helpers. The patch mechanically converts 17 instances of direct shell commands (`test -f` and `test -d`) to their more readable equivalents (`test_path_is_file` and `test_path_is_dir`) in t/t2401-worktree-prune.sh.

The changes maintain identical test semantics while improving code clarity. The test file covers various worktree pruning scenarios including invalid directories, locked checkouts, and duplicate entries. Since this is a pure readability improvement with no behavior changes or test coverage modifications, the patch should be uncontroversial. The implementation appears correct and well-scoped, touching only the intended test file with simple substitutions.