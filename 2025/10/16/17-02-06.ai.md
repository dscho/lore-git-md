This is a documentation thread discussing potential breaking changes to Git's diff and checkout syntax, specifically around the `X..Y` and `X...Y` notations. Kristoffer Haugsbakk joins the discussion to report that `git checkout X...` works for them, contrary to Martin von Zweigbergk's earlier report of it failing. This observation adds a new data point to the ongoing debate about the current behavior and future of these syntax forms.

The email highlights a discrepancy in behavior between different users' environments, with Kristoffer successfully using the triple-dot syntax with `git checkout` while Martin could not. This reinforces Junio C Hamano's earlier point about the syntax being used across multiple commands, though it also introduces uncertainty about the current implementation's consistency.

The thread continues to grapple with several key technical challenges: the cross-command dependencies of the syntax (particularly for fork-point operations in `git checkout`), shell escaping constraints for any proposed alternatives, and the need for coordinated changes if modifications are made. Junio's position remains that syntax meanings should remain stable once accepted, while Martin and Justin Tobler advocate for removing the potentially confusing notations, and brian m. carlson maintains strong compatibility objections.

This new observation about `git checkout` behavior adds complexity to the discussion, as it suggests either platform-specific behavior or user error in testing, and could impact decisions about whether and how to modify these syntax forms. The thread remains unresolved with fundamental disagreements about whether Git should make such breaking changes and how to handle the cross-command implications.