This is a continuation of the discussion about removing potentially confusing `git diff X..Y` and `X...Y` syntax in Git 3.0. Martin von Zweigbergk responds to Junio C Hamano's technical concerns about cross-command syntax dependencies, particularly regarding the triple-dot notation's role in commands like `git checkout`.

Key technical points in this response:
- Martin confirms he couldn't reproduce Junio's example of `git checkout main...` working (it fails with "invalid reference")
- Acknowledges Junio's broader point about triple-dot syntax being used beyond just `git diff`
- Agrees that any change to triple-dot syntax would need to be coordinated across commands
- Notes the need for a merge-base revision syntax alternative if triple-dot is removed

The email reinforces the technical constraints raised in the thread while maintaining support for the original proposal's direction. It surfaces an implementation detail (the failing `git checkout` example) that may need investigation if the syntax change proceeds. The discussion remains focused on the practical challenges of changing established syntax that spans multiple Git commands.