This thread continues the optimization work for `git last-modified`, a command that efficiently finds the most recent commit modifying each path. The current implementation, building on prior work from GitHub and substantially reworked by Toon Claes, introduces performance improvements through generation-sorted commits, path activity bitmaps, and Bloom filter optimizations.

The discussion has now turned to a NULL oid segfault issue in the path-marking logic. Taylor Blau provides historical context, explaining this wasn't an error from GitHub's original battle-tested version but rather a check added later in their "--go-faster" mode that wasn't properly carried over. He clarifies that the original implementation handled TREESAME cases differently without bitmap optimizations.

Jeff King and Taylor Blau are carefully analyzing the memory/performance tradeoffs between bitmaps and character arrays, with Taylor suggesting a concrete optimization for TREESAME cases where unchanged bitmaps could be directly OR'ed to parents. Throughout these technical refinements, the thread maintains its focus on ensuring the new implementation matches `git log --max-count=1` behavior as the correctness benchmark.

The exchange demonstrates the project's attention to historical context and implementation details, with contributors cross-referencing multiple code versions (GitHub's original, Taylor's fork, and Toon's current patches). A minor double-free issue in Taylor's blame-tree.c was also noted, stemming from rebase conflicts with recent diff queue management changes. The core algorithm has consensus, and the discussion continues to refine the implementation while preserving its substantial performance improvements.