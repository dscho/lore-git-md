Here's the human-readable summary of the thread:

The thread concerns a performance optimization for `git last-modified` that was recently merged after positive reviews, showing significant speedups (2.5x-111.7x) on most platforms. The optimization introduced generation-sorted priority queues and EWAH bitmaps for tracking file modifications. However, test failures have now been confirmed on big-endian architectures (s390x and HP NonStop), where the command incorrectly attributes file changes to merge commits rather than their actual sources.

Jeff King has joined the investigation, noting the correlation with big-endian systems and proposing two possible failure modes: either the algorithm isn't properly propagating blame through merges, or it's failing to propagate blame entirely. While the core integer handling appears correct, the platform-specific nature of the issue suggests memory access patterns might be involved. Jeff has suggested building with SANITIZE=address,undefined to check for endian-specific memory issues.

The optimization remains valid for little-endian systems, but the team needs to resolve this platform-specific issue before wider deployment. Key participants include Toon Claes (the original author), Jeff King (investigating the endianness angle), and Anders Kaseorg (who provided the initial s390x failure reports). The discussion is now focused on determining whether the problem lies in the merge handling logic or deeper architectural assumptions in the bitmap implementation.