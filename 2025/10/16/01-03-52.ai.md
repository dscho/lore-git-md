This is a refactoring patch series from an Outreachy participant, continuing work started by Junio Hamano to replace `strbuf_split_max()` with `string_list_split()` in cases where the split strings are only read and not modified. The series focuses on `gpg-interface.c` where GPG signature parsing occurs.

The cover letter references prior work (commit 2efe707054) that identified `string_list_split()` as more appropriate than `strbuf_split*()` when the strings are only being stored/read. The v2 adds context by linking to Junio's original patch series that established this pattern.

The two patches in this series make mechanical conversions in `gpg-interface.c`:
1. First patch replaces one `strbuf_split_max()` call with `string_list_split()`
2. Second patch does the same for another instance in the same file

The changes are straightforward - swapping one string splitting function for another while maintaining the same behavior. The diffstat shows 20 lines changed (10 additions, 10 deletions) across the file, suggesting simple 1:1 replacements rather than deeper restructuring. As this follows an established refactoring pattern and touches GPG signature handling code (which is security-sensitive), reviewers will likely check carefully that the behavior remains identical.