Bugfix for GPG signature handling with CR/LF line endings. Okhuomon Ajayi addresses an issue where the `remove_cr_after()` function in gpg-interface.c was stripping all CR characters, despite its comment suggesting it should only remove CRs before LF line endings. The patch renames the function to `trim_cr_before_lf()` to better reflect its actual behavior and updates the comment to clearly document the intended functionality of preserving standalone CR characters while only converting CRLF sequences to LF.

The changes appear straightforward and well-motivated, fixing a potential source of confusion between the code's behavior and its documentation. The patch maintains the same core logic but improves the function's name and documentation to match its actual purpose. This is a relatively small but clear improvement to the GPG interface code.