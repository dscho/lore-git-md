This is a small but important type-safety change in the ongoing xdiff refactoring series preparing the codebase for Rust FFI compatibility. The patch changes the `xrecord_t.ptr` field from `char` to `uint8_t` to better reflect its actual usage (as raw bytes rather than text characters) and align with Rust's `u8` type.

The change is straightforward and well-justified - the field is used to point to memory contents being diffed, not character data, so using an unsigned byte type is more semantically correct. This matches Rust's memory model where `u8` is the standard type for raw bytes.

As part of the larger 9-part series modernizing xdiff's data structures, this patch follows the established pattern of making targeted, behavior-preserving type changes to improve type safety and FFI compatibility. The commit message clearly explains the Rust interoperability motivation while keeping the change focused and reviewable.

No functional changes are expected from this conversion since `char` and `uint8_t` have the same size and representation in practice. The patch appears cleanly implemented with no obvious issues, continuing the high quality seen throughout this refactoring effort.