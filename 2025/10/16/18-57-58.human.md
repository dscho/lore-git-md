This is the test coverage patch (3/3) for the Message-ID tracking series. It adds verification that `git am` correctly stores Message-IDs in commit headers, completing the feature proposal to link commits with their mailing list discussions.

The patch modifies the existing `t4150-am.sh` test file to handle the new behavior. It splits the test case for applying patches into two parts: one that verifies basic patch application without Message-IDs (using a new test file `patch1-nomsgid.eml`), and a new test that specifically checks that Message-IDs are properly added to commit headers when present in the input. The test confirms the Message-ID appears in the raw commit output while maintaining the expected parent-child relationships.

This final patch addresses the test coverage gap noted in the earlier parts of the series. The implementation appears straightforward and well-integrated with the existing test framework, though reviewers may want to verify the specific Message-ID value being checked matches the expected from the test setup. The series as a whole now provides both the implementation and verification for tracking Message-IDs in commits.