Junio Hamano's response to the Message-ID tracking proposal raises concerns about the technical implementation approach. This is a feature proposal discussion about commit metadata storage.

The email focuses on two key objections to storing Message-IDs in commit headers rather than trailers. First, Hamano argues that commit headers create maintenance headaches for operations like rebase and cherry-pick, where the extra header would need special handling. Second, he questions whether the implementation in patch 2/3 might break the common "format-patch | am" workflow by requiring msg_id to exist during mail parsing, since format-patch output wouldn't naturally contain these headers.

The response maintains Hamano's characteristic direct style while acknowledging he hasn't fully reviewed patch 2/3 yet. His concerns suggest the current implementation may need adjustment to preserve existing workflows and avoid introducing fragile metadata handling. The critique focuses specifically on the commit header storage mechanism rather than the overall concept of Message-ID tracking.

This moves the thread into a more critical phase where the implementation details are being scrutinized, particularly around backward compatibility and the practical implications of commit metadata storage choices. The response doesn't address the earlier consensus about the email-specific use case being distinct from Change-ID systems.