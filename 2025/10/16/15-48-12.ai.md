CI / build system update completing a series of GitHub Actions version bumps, this time for `actions/github-script` from v7 to v8. This final patch in the 4-part series affects only one workflow file (`main.yml`), updating the version used in a step that checks whether commits have already been tested to avoid redundant CI runs. 

The change follows the same pattern as previous patches in the series - a semver-major version bump with no described functional impact on Git's CI behavior. Johannes Schindelin preserves the original Dependabot authorship while adding his sign-off, maintaining consistent attribution across the series. The modification is minimal, touching just one line in the workflow file to update the version reference.

This completes the mechanical updates to GitHub Actions dependencies that began with `actions/download-artifact` and `actions/checkout` in earlier patches. The `github-script` action appears to be used in a relatively narrow context compared to the more widely-used `checkout` action updated earlier, suggesting lower potential for cross-cutting impact. As with the rest of the series, this appears to be a low-risk maintenance update following established patterns for CI dependency management.