This is a follow-up in the ongoing discussion about removing potentially confusing `X..Y` and `X...Y` syntax from `git diff`. Martin von Zweigbergk responds to Kristoffer Haugsbakk's observation that `git checkout X...` works, clarifying that his earlier test failure was due to testing with an ambiguous merge base case rather than a syntax issue. 

The email provides important technical context about edge case behavior - when multiple merge bases exist, `git checkout X...` fails with different error messages than the simple syntax error Martin initially reported. This demonstrates that the syntax is valid but has special case handling that can produce confusing errors in certain repository states.

Martin maintains his position that the triple-dot syntax should be considered alongside the double-dot syntax for removal, while acknowledging Junio C Hamano's point about the notation being used consistently across multiple commands. The exchange highlights the complexity of changing syntax that has cross-command dependencies and subtle edge case behaviors.

Key technical details for future reference:
- `git checkout X...` works when merge base is unambiguous
- Fails with "pathspec did not match" or "invalid reference" when merge base is ambiguous
- Confirms Junio's point about syntax needing to be consistent across commands
- Shows real-world testing can produce different results than expected due to repository state

The thread continues to grapple with balancing clarity improvements against backward compatibility and cross-command consistency, with this email adding concrete data about actual behavior in edge cases.