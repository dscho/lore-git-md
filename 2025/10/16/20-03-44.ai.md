This is a bugfix patch addressing line ending handling in GPG signature verification. The patch focuses on `gpg-interface.c` and specifically the function that processes CR/LF line endings in signed content.

The current implementation has a function called `remove_cr_after()` that strips all CR characters, which contradicts its own comment suggesting it should only remove CRs before LF (Windows-style line endings). The patch renames this to `trim_cr_before_lf()` and updates both the implementation and comment to match the intended behavior - preserving lone CR characters while only converting CRLF sequences to LF.

The changes include:
1. Renaming the function to better reflect its purpose
2. Updating the comment to clearly state the CR-before-LF trimming behavior
3. Code formatting improvements (indentation, spacing)
4. Removing a duplicate function definition that appears in the diff (likely an artifact of the patch creation process)

This appears to be a straightforward correctness fix that aligns the code with its documented intent. The patch is small and focused, though the duplicate function in the diff suggests the patch might need a quick review for accidental inclusions. The change could affect GPG signature verification on Windows systems where CRLF line endings are common.