This is a continuation of the discussion about removing potentially confusing `git diff X..Y` and `X...Y` syntax in Git 3.0. Martin von Zweigbergk responds to Junio C Hamano's technical concerns about the broader implications of changing the triple-dot syntax (`X...Y`), which is used beyond just `git diff` (notably in `git checkout` for fork points).

The exchange highlights the complexity of making this change - while there's agreement that the current syntax is confusing (with `X..Y` showing a two-dot diff while looking like range syntax, and `X...Y` having merge-base behavior), removing it requires coordinated changes across multiple commands. Martin notes he couldn't reproduce Junio's example of `git checkout main...` working, but acknowledges the general point about cross-command syntax dependencies.

The discussion remains at an impasse between the desire to clean up confusing syntax and the practical challenges of maintaining compatibility, particularly around the triple-dot notation which has established uses elsewhere in Git. No clear path forward emerges in this message, though Martin does agree that some syntax for merge-base operations would be useful if the change proceeds.