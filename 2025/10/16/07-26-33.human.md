This is a refactoring patch in the geometric maintenance strategy series, making the loose object threshold check more flexible. The change modifies `too_many_loose_objects()` to accept a direct integer threshold parameter rather than requiring the full `gc_config` struct, in preparation for future callers that may want to use different threshold values.

The patch is straightforward - it simply replaces the struct parameter with the threshold value it was extracting anyway, updating all four call sites to pass the new parameter. The behavior remains identical, but the function becomes more reusable. This is part of the groundwork for the geometric repacking strategy where different maintenance operations may need to check the loose object count with different thresholds.

The change is cleanly implemented with thorough updates to all callers, suggesting careful attention to maintain correctness. As the second patch in an 8-part series, it continues the preparatory refactoring started in patch 1, keeping the codebase flexible for the upcoming geometric maintenance functionality.