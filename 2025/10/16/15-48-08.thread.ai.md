CI dependency update series by Johannes Schindelin adapting GitHub Actions version bumps from Git for Windows' Dependabot suggestions, now completed for the main Git repository. The four-patch series updates standard GitHub-maintained Actions across multiple workflow files:

1. `actions/download-artifact` v4->v5 in `.github/workflows/main.yml` (affecting Windows SDK setup and meson build steps)
2. `actions/checkout` v4->v5 across four workflow files (`check-style.yml`, `check-whitespace.yml`, `coverity.yml`, `main.yml`), appearing in 10 distinct steps including Windows vcpkg initialization
3. `actions/setup-python` v5->v6 (not yet detailed in thread)
4. `actions/github-script` v7->v8 in `main.yml`, updating a step that checks for previously tested commits

All changes are mechanical semver-major version number updates with no described functional impact on Git's build or test behavior. Schindelin has preserved Dependabot's original authorship while adding his sign-off throughout the series. The `actions/checkout` update was the most substantial due to its widespread use across workflows, while the `github-script` change affects only one narrow use case. The series follows established patterns for CI maintenance updates, with all four patches now submitted, the thread appears complete pending any final review comments. No objections or requests for changes have been raised during the thread's progression.