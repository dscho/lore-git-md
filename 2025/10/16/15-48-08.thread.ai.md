CI dependency update series by Johannes Schindelin adapting GitHub Actions version bumps from Git for Windows' Dependabot suggestions, now merged into Git's integration queue. The four-patch series updates standard GitHub-maintained Actions across multiple workflow files:

1. `actions/download-artifact` v4->v5 in `.github/workflows/main.yml` (affecting Windows SDK setup and meson build steps)
2. `actions/checkout` v4->v5 across four workflow files (`check-style.yml`, `check-whitespace.yml`, `coverity.yml`, `main.yml`), appearing in 10 distinct steps including Windows vcpkg initialization
3. `actions/setup-python` v5->v6
4. `actions/github-script` v7->v8 in `main.yml`, updating a step that checks for previously tested commits

All changes are mechanical semver-major version number updates with no described functional impact on Git's build or test behavior. Maintainer Junio Hamano has accepted the series into Git's integration queue, confirming no outstanding issues. Schindelin preserved Dependabot's original authorship while adding his sign-off throughout the series. The `actions/checkout` update was the most substantial due to its widespread use across workflows, while the `github-script` change affects only one narrow use case. The thread is now complete with the series merged, concluding the maintenance effort that originated from Git for Windows' Dependabot suggestions. No objections or requests for changes were raised during the thread's progression.