This is a refactoring patch in the geometric maintenance strategy series, preparing the `too_many_loose_objects()` function for reuse with different threshold values. The change modifies the function to accept a simple integer limit parameter rather than requiring the full `gc_config` struct, making it more flexible for future callers that may want to use different thresholds.

The patch updates four call sites to pass just the threshold value from the config struct. The change is straightforward - the function's core logic remains identical, it just accesses the threshold parameter directly rather than through the config struct. This appears to be a clean preparatory change with no behavior modifications, setting up for subsequent patches in the series that will introduce new callers with different threshold needs.

The modification touches only builtin/gc.c, with simple mechanical changes to the function signature and its callers. This continues the pattern from the first patch in the series of making gc.c internals more modular and reusable in preparation for the new geometric maintenance functionality. The change maintains all existing behavior while improving the function's flexibility.