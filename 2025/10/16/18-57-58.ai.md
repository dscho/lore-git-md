Test patch completing the Message-ID tracking series. This is the third and final patch in James Bottomley's series to store email Message-IDs in commit headers, adding test coverage for the functionality introduced in patches 1 and 2.

The test modifies t4150-am.sh to verify two aspects: first, it adjusts an existing test case to use a new patch file without Message-ID headers to maintain equivalence with older commits, and second, it adds a new test case that explicitly checks that `git am` correctly adds the Message-ID header to commits. The test verifies both the commit content (showing the commits differ only by the Message-ID) and that the header appears in the raw commit output.

The test implementation is straightforward and follows Git's test conventions, using `test_expect_success` blocks and standard test helpers like `git diff --exit-code` and `git show --pretty=raw`. The test data includes both Message-ID and non-Message-ID variants of the same patch to enable comparison.

This completes the test coverage that was noted as missing in the earlier patches of the series. The test appears well-constructed to verify the core functionality while maintaining backward compatibility with existing test cases. The series now appears complete with both implementation and verification components.