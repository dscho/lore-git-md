This is the final patch in Ramsay Jones' v3 documentation formatting series, addressing `large-object-promisors.adoc`. The patch adds the file to both Makefile and meson build systems after it was previously omitted despite having an `.adoc` extension. The changes fix two sets of asciidoc/asciidoctor warnings:

1. Section header level inconsistencies (using `+` underlines changed to `^` to match expected hierarchy)
2. Mixed numeral styles in headings (removed numerals from "Non goals" and converted roman numeral list items to plain headings)

The patch follows the same pattern as previous patches in the series, making mechanical formatting changes while preserving content. The modifications standardize the document's structure to match asciidoc best practices, particularly around section levels and list formatting. The changes are verified to be presentation-only, with no semantic alterations to the technical content about large object promisors.

This completes the v3 series' work on bringing technical documentation into compliance with asciidoc formatting standards, following earlier patches for `commit-graph.adoc`, `sparse-checkout.adoc`, and `remembering-renames.adoc`. The build system changes ensure the document will now be properly formatted in future documentation builds.