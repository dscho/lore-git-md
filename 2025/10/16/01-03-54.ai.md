Refactoring patch converting string splitting in GPG verification code from `strbuf_split_max()` to `string_list_split()`, part 2 of 2 in series. This second patch focuses on SSH signing key handling in `get_default_ssh_signing_key()`.

The change replaces `strbuf_split_max()` with `string_list_split()` for parsing SSH key output, maintaining the same split behavior (newline-delimited tokens) while using the more appropriate API for read-only string handling. The key technical difference is the parameter change from requesting 2 tokens to performing 1 split, which produces equivalent results. The patch includes careful validation that exactly 2 tokens are produced, matching the original behavior.

Security implications are noted since this touches SSH key parsing, though the change is strictly mechanical with no intended functional changes. The v2 improvements include clearer commit message explanations and explicit token count verification in the test. This follows the pattern established by Junio Hamano's prior work on similar conversions in the codebase.

The patch demonstrates attention to detail in maintaining behavior while modernizing the code, though reviewers will likely scrutinize the security-sensitive context. The series appears well-contained (10 lines changed across both patches) and focused on a clear refactoring goal.