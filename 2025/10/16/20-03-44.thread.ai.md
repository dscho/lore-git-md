Thread identity: Bugfix patch addressing CR/LF line ending handling in GPG signature verification, touching `gpg-interface.c`.

Core problem: The current `remove_cr_after()` function incorrectly strips all CR characters during GPG signature verification, contrary to its documented purpose of only removing CRs before LF (CRLF conversion). This could affect signature validation on Windows systems.

Key technical changes:
- Renames `remove_cr_after()` to `trim_cr_before_lf()` to accurately reflect behavior
- Updates function implementation to preserve lone CRs while converting CRLF to LF
- Improves code formatting and removes accidental duplicate function definition
- Adjusts comments to clearly specify CR-before-LF trimming behavior

Current status: Initial version of the fix submitted, awaiting review. The change is small and focused but needs verification for correctness in GPG signature handling, particularly on Windows platforms.

Open questions: Whether the patch might introduce any edge cases in signature verification, especially with non-standard line endings or malformed input. The accidental duplicate function in the diff suggests the patch may need cleanup before final acceptance.

Participant positions: No reviews or responses yet (this is the initial patch submission). The change appears straightforward but will need verification from GPG/Windows experts.