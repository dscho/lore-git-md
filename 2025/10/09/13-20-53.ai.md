This is a documentation patch discussion in Julia Evans' ongoing series introducing a new `gitdatamodel.adoc` man page. The current exchange focuses on how to best document reference access patterns in a backend-agnostic way, specifically comparing direct filesystem access (like `cat .git/refs/heads/main`) with plumbing commands (`git for-each-ref`).

Key technical points in this message:
- Julia explores using `git for-each-ref` with specific formatting (`--format="%(refname) %(if)%(symref)%(then)%(symref)%(else)%(objectname:short)%(end)"`) as a backend-neutral alternative to filesystem inspection
- The discussion acknowledges `git for-each-ref` as the canonical tool for examining local references, paralleling how `git ls-remote` handles remote references
- The exchange highlights the pedagogical challenge of showing reference inspection without assuming filesystem storage (particularly relevant for reftable backend compatibility)

This continues the thread's theme of balancing technical accuracy with teaching effectiveness, now focusing on reference access patterns. The discussion may influence how the documentation presents reference inspection examples, potentially favoring plumbing commands over filesystem operations to maintain backend neutrality.