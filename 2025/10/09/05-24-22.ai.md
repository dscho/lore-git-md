Security proposal discussion on preventing arbitrary code execution in untrusted repositories, focusing on implementation strategy.

Jeff King (Peff) enters the thread with a structured approach to the security problem, separating the discussion into two distinct phases. First, he emphasizes the need to define what "unsafe" behavior looks like in Git - specifically suggesting skipping repo-level config and disabling hooks in untrusted contexts. He references a prior discussion where he proposed such behavior, stressing maintainability as the key concern to prevent future security regressions.

Second, he addresses the safety marking mechanism (Michael's token proposal) but suggests simpler alternatives like environment variables (GIT_ASSUME_UNSAFE) or config-based safe directories (similar to safe.directories). Notably, he proposes these as opt-in features initially, with commands like git-prompt using --assume-unsafe flags, allowing gradual adoption before considering default behavior changes.

Key technical points raised:
1. Clear separation between unsafe behavior definition and safety marking mechanism
2. Preference for existing patterns (GIT_ALLOW_PROTOCOL, safe.directories) over new token system
3. Suggestion to implement opt-in unsafe mode first via CLI/env vars before changing defaults
4. Emphasis on practical experience before committing to any particular marking scheme

This moves the discussion toward incremental implementation while maintaining the core security goals. The email doesn't reject the token approach outright but positions it as a potential future option after establishing the unsafe behavior baseline. It introduces the concept of phased rollout and real-world testing before settling on defaults - a pragmatic approach that could help resolve the current token-vs-config debate by deferring that decision.