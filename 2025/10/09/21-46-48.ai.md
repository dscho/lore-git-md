This is a follow-up to a const correctness bugfix in the patch-id comparison logic. Kristoffer Haugsbakk notes that while Okhuomon Ajayi's patch fixed the const correctness issue in `patch_id_neq()` by making the local `opt` variable const, it left behind the NEEDSWORK comment that originally flagged this issue. The comment is now obsolete since the const correctness problem has been addressed.

The thread context shows this is a simple, uncontroversial improvement from an Outreachy participant - the original patch was a one-line change to fix a known const correctness violation in patch-id handling. This follow-up suggests a minor documentation cleanup to remove the now-irrelevant NEEDSWORK comment that previously called attention to the const issue.

Key technical details remain unchanged from the original patch: this affects the patch ID comparison callback used in operations like cherry-pick, with no behavior change - just type safety and documentation improvements. The follow-up maintains the same focused scope, suggesting one additional documentation tweak to complete the const correctness fix.