The thread addressed a clang compiler warning in `refs/files-backend.c` where `-Wunreachable` incorrectly flagged code guarded by `!create_ref_symlink()` on platforms without symlink support. Johannes Schindelin proposed applying the `NOT_CONSTANT()` workaround at the callsite, while Patrick Steinhardt suggested an alternative approach modifying the macro definition. After discussion, consensus formed around Johannes's original implementation, with Junio Hamano noting the solution works and further iterations weren't worth the churn.

Patrick Steinhardt has now confirmed he accepts this resolution, explicitly stating no objections to placing the warning suppression at the callsite. This provides clear closure on the implementation choice. The 7-line change preserves current behavior while the community agrees to deprecate the related `core.preferSymlinkRefs` configuration in Git 3.0. With all participants aligned and no outstanding questions, the thread is now resolved.