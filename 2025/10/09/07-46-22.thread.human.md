This thread addressed a clang compiler warning in the refs backend code, where `-Wunreachable` incorrectly flagged code guarded by `!create_ref_symlink()` as unreachable on platforms without symlink support. Johannes Schindelin proposed applying the `NOT_CONSTANT()` workaround at the callsite, while Patrick Steinhardt suggested moving it into the macro definition. After discussion, Johannes decided to proceed with the original callsite approach, as both solutions produce identical compiled output and the change is localized to this single callsite.

Junio Hamano has now weighed in to confirm the matter is settled, appreciating Patrick's review while agreeing that further iterations aren't necessary. The community consensus maintains the current implementation while keeping the long-term plan to deprecate the `core.preferSymlinkRefs` configuration option in Git 3.0. With no outstanding technical questions, this concludes the thread with a straightforward 7-line fix that silences the warning without changing behavior.