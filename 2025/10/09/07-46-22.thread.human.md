Johannes Schindelin started this thread with a patch addressing a clang compiler warning in `refs/files-backend.c`, where `-Wunreachable` was incorrectly flagging code guarded by `!create_ref_symlink()` on `NO_SYMLINK_HEAD` platforms. Two equivalent solutions emerged: Johannes's original approach applying `NOT_CONSTANT()` at the callsite, and Patrick Steinhardt's alternative applying it at the function definition.

Junio Hamano approved the warning-suppression approach as a reasonable interim solution while proposing to deprecate the `core.preferSymlinkRefs` configuration in Git 3.0. The latest email from Patrick confirms consensus on both the immediate fix and this long-term direction, noting that symbolic ref writing functionality could be safely removed. While the implementation choice between the two warning-suppression approaches remains pending Johannes's response, all parties agree on the core solution's validity. The thread is now effectively waiting for Johannes to indicate his preference between the two equivalent implementations before proceeding.