This is a follow-up to a const correctness bugfix in the patch-id comparison logic. Kristoffer Haugsbakk points out that while the original patch fixed the const correctness issue in `patch_id_neq()`, it left behind a "NEEDSWORK" comment about const correctness that is now obsolete. The comment was originally there to flag the very issue that was just fixed by adding the `const` qualifier to the `opt` variable declaration.

The observation is correct - since the commit message explicitly states it's fixing the const correctness issue, the comment no longer serves any purpose and should be removed. This would be a trivial one-line follow-up change to the original patch.

The thread shows good attention to detail from both participants - first Okhuomon Ajayi fixing the actual const correctness violation, and now Kristoffer catching the leftover comment. The change is small but improves code hygiene by removing now-irrelevant documentation.