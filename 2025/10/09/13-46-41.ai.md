This is a follow-up discussion about the documentation patch warning against `--committer-date-is-author-date` in `git-am` and `git-rebase`. Phillip Wood responds to Kristoffer Haugsbakk's initial patch, agreeing with the warning but raising additional concerns about the option's applicability to rebase`.

Key technical points from Phillip's response:
- Confirms the original `git-am` use case (preserving SHA-1s when reapplying patches) is valid
- Questions the option's rationale in `git-rebase`, suggesting it may have been mistakenly ported from `git-am`
- Proposes considering deprecation for `git-rebase` since rebasing typically moves commits forward in time
- Requests several documentation tweaks:
  - Clarify reference to clock skew in `git-rev-list`
  - Replace "lie" with "override" in the warning text
  - Adjust wording from "applying commits" to "rebasing commits" for the rebase documentation
  - Add warning about using the option with interactive rebase (`-i`)

The discussion reveals deeper concerns about timestamp ordering violations that weren't fully addressed in the initial patch. Phillip's comments suggest the documentation may need to distinguish more clearly between valid `git-am` use cases and potentially problematic `git-rebase` scenarios. The thread continues to explore the tension between technical correctness (timestamp ordering) and user needs (metadata preservation), with this email focusing on refining the warning's precision and scope.