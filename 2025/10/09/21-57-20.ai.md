Follow-up const correctness cleanup in patch-id comparison logic. This v2 patch from Okhuomon Ajayi addresses Kristoffer Haugsbakk's observation that the NEEDSWORK comment about const correctness in `patch_id_neq()` should be removed now that the underlying issue is fixed. The change is minimal - just deleting the obsolete comment while keeping the const-correct variable declaration from v1. 

The patch maintains the same technical approach as before: the callback function in patch-ids.c now properly respects const correctness by declaring its internal `opt` variable as const to match the function's contract, eliminating compiler warnings. This is part of the diff machinery used by commands like `git cherry-pick` for comparing patch IDs, though no behavior changes are involved - purely type safety improvements.

With the NEEDSWORK comment removed, this completes the cleanup of the const correctness issue originally noted in the code. The change remains focused to patch-ids.c with a trivial diff (2 lines modified). Given the straightforward nature and lack of controversy in the thread, this appears ready for inclusion as a quality improvement.