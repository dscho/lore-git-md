This is a platform compatibility patch addressing a clang compiler warning in the refs backend. The issue stems from code that becomes technically unreachable when `NO_SYMLINK_HEAD` is defined, triggering clang's `-Wunreachable` warning despite being intentional platform-specific behavior. The solution adopts an existing pattern from the codebase (`NOT_CONSTANT` macro) to suppress the false positive while preserving the warning's usefulness for genuinely unreachable code.

The patch makes a minimal change (7 lines added, 1 deleted) to `refs/files-backend.c`, wrapping the symlink check with the macro and adding an explanatory comment. Junio Hamano's response notes this is a reasonable fix, though he suggests the broader `core.preferSymlinkRefs` functionality might be worth deprecating in favor of simpler text files for HEAD references.

The change appears well-contained and low-risk, focusing purely on build system compatibility without affecting runtime behavior. It follows established patterns in the codebase and includes clear documentation of the workaround's purpose. While the warning suppression is platform-specific (affecting only clang builds with `NO_SYMLINK_HEAD`), the solution maintains the code's correctness across all platforms.