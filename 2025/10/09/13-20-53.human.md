This is a documentation discussion in the ongoing thread about Julia Evans' `gitdatamodel.adoc` man page series, now at v2. The conversation focuses on how to best explain Git references in a way that's backend-agnostic (not assuming `.git/refs` structure) and pedagogically useful.

Julia responds to D. Ben Knoble's suggestion to use `git for-each-ref` as a teaching tool by sharing a complex format string she developed to simulate the simple `cat .git/<refname>` output while working across ref backends. She notes the challenge of finding a straightforward plumbing command for reference inspection that's as simple as `git cat-file` is for objects.

The exchange highlights the practical tension in documentation between showing simple-but-fragile examples (like direct filesystem inspection) versus robust-but-complex plumbing commands. Julia's experimental format string demonstrates the kind of implementation detail that might need to be either simplified for the manual or explained thoroughly. This discussion directly informs the reference section of her data model documentation.