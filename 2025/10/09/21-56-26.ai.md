This is a test infrastructure patch in the SHA-1/SHA-256 interoperability series, specifically handling broken object tests in compatibility mode.

The patch modifies `t1010-mktree.sh` to properly use the `BROKEN_OBJECTS` prerequisite that was introduced earlier in the series. The key change is that tests involving broken objects (which cannot be properly mapped between hash algorithms) are now skipped when running in compatibility mode. The test that writes broken objects (`git update-index --add --info-only`) is now conditional on the prerequisite, and the test for `--missing` handling is explicitly marked with `BROKEN_OBJECTS`.

The changes ensure the test suite remains reliable when operating in hash compatibility mode, where certain operations with broken objects would fail or produce incorrect results. This matches the series' broader goal of maintaining consistent behavior across hash algorithms while properly handling edge cases. The implementation follows the pattern established by other tests in the series that need to be skipped in compatibility scenarios.

Technical details include:
- Wrapping broken object creation in a `test_have_prereq BROKEN_OBJECTS` conditional
- Adding `BROKEN_OBJECTS` prerequisite to the test_expect_success line for the `--missing` test
- Maintaining all existing test cases while making them conditional on compatibility mode support
- Preserving the test's original functionality when not in compatibility mode

This is a straightforward final patch in the series that properly integrates the mktree tests with the new compatibility mode infrastructure. It follows the pattern established by earlier patches for handling hash-algorithm-specific test cases.