This thread addresses a const correctness issue in Git's patch-id comparison logic. The original patch from Outreachy participant Okhuomon Ajayi fixed a violation in `patch_id_neq()` where a const parameter was incorrectly cast to non-const internally. The one-line change made the local variable const to match the function's contract, resolving a longstanding issue that had been marked with a NEEDSWORK comment.

Kristoffer Haugsbakk followed up noting that while the code change addressed the const correctness problem, the original NEEDSWORK comment remained in place despite now being obsolete. The thread remains focused and uncontroversial, with both changes being straightforward improvements - first fixing the type safety issue in the comparison callback used by operations like cherry-pick, then cleaning up the now-redundant documentation marker. No behavior changes are involved, just code hygiene improvements to this narrow piece of the patch-id subsystem.