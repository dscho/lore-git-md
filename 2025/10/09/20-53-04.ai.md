Feature deprecation notice response. This is a user reporting continued use of `git whatchanged` in response to its deprecation warning. The command now requires `--i-still-use-this` flag to operate, with users instructed to email the list if they still depend on it. 

The email serves as a simple usage report from Justin Sherrill, who encountered the warning while using `git whatchanged` in what appears to be a configuration management context (referencing an AWS-related YAML file). No technical details about the command's usage patterns or justification for keeping it are provided - just a straightforward confirmation of active use.

This represents the feedback-gathering phase of the deprecation process for `git whatchanged`, which was previously marked for removal. The thread may help determine whether the command has enough active users to warrant maintenance or if the deprecation should proceed. The original deprecation notice (not shown here) presumably explained why the nearly identical `git log` functionality makes `whatchanged` redundant.