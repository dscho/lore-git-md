Refactoring patch in the packfile store API cleanup series. This is the fourth patch in v2 of the 6-part series, removing the now-unused `packfile_store_get_packs()` function after all callers have been converted to other interfaces in previous patches.

The change is straightforward - deleting both the function implementation in packfile.c and its declaration in packfile.h, along with the associated documentation comment. The removal follows the series' progression where earlier patches systematically migrated callers away from this function, which had confusing stateful behavior regarding MIDX'd pack inclusion. The patch maintains the series' focus on simplifying pack iteration patterns by eliminating deprecated interfaces after ensuring safe migration paths.

This continues the technical approach seen throughout the thread: first introducing safer alternatives (`repo_for_each_pack` macro, direct `packfile_store_prepare` calls), then removing the old confusing API once unused. The change is behavior-preserving and was pre-approved in principle by Taylor Blau during earlier iterations, with Patrick Steinhardt now executing the final cleanup step. The patch touches core packfile infrastructure but has no user-facing impact, purely improving internal code quality by removing dead code after the refactoring is complete.