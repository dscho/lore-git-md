This is part of the packfile store API refactoring series, specifically converting object-name.c to use `packfile_store_get_all_packs()` instead of the problematic `packfile_store_get_packs()`. The patch focuses on two functions in object-name.c that handle object ID disambiguation and abbreviation length calculation.

The change addresses the confusing statefulness between `get_packs()` and `get_all_packs()` where the former's behavior depended on whether the latter had been called. The patch notes that both `find_abbrev_len_packed()` and `find_short_packed_object()` already properly handle MIDX'd packs, making this a safe conversion. The diff shows straightforward replacements of the function calls with no other changes needed.

This is the first patch in a v2 series that continues the systematic conversion of pack iteration patterns across the codebase. The change is behavior-preserving but improves internal API consistency by eliminating a known confusing interaction between the two pack-getting functions. The commit message provides clear technical rationale for why this particular conversion is safe and beneficial.