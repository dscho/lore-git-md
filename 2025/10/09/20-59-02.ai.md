This is a follow-up in the ongoing discussion about deprecating `git whatchanged`. Kristoffer Haugsbakk responds to Justin Sherrill's usage report with specific migration advice, suggesting equivalent `git log` invocations to replace `git whatchanged` functionality.

The email provides concrete translation rules between the commands, noting that `git whatchanged <opts>` can be replaced with `git log <opts> --no-merges --raw`. It further suggests alternative output formats (`--stat` or `--name-only`) that may be more human-readable than `--raw` for some use cases, while acknowledging `--raw` might still be needed for programmatic consumption.

This response advances the thread by moving from simple usage reporting to practical migration guidance, though it doesn't address whether the command should ultimately be kept or removed. The technical details confirm that both commands share the same underlying machinery, differing only in default output formatting. No new objections to deprecation are raised - the focus is purely on helping existing users transition.

Key technical points for future reference:
- Exact replacement syntax: `git log --no-merges --raw`
- Suggested alternatives: `--stat` and `--name-only` for human-readable output
- Confirmation that both commands use same backend machinery
- No new arguments for/against deprecation, just migration advice

The email maintains a helpful tone while reinforcing the deprecation path, suggesting the project is proceeding with removal unless stronger objections emerge. No new usage patterns or unique needs for `git whatchanged` are identified beyond what was already reported.