Thread identity: Feature patch introducing `commitGraph.changedPaths` config option for commit-graph changed-path Bloom filters, authored by Taylor Blau.

Core problem or goal: Add persistent configuration for writing changed-path Bloom filters during `git commit-graph write`, which currently require explicit `--changed-paths` flag. The filters accelerate file history computations in large repositories.

Current status: Initial version of patch implementing the config option with three states (true/false/unset), maintaining backward compatibility with existing behavior. Command-line flags (`--changed-paths`/`--no-changed-paths`) override the config. Scalar integration recommends enabling for large repos.

Key technical details: New `enable_changed_paths` field in `struct commit_graph_opts` (-1/0/1). Config handling in commit-graph.c with precedence rules. Scalar sets as optional recommended config. Test coverage in t5318-commit-graph.sh verifies: CLI override behavior, config enabling filters, persistence of existing filters across config changes.

Open questions: None explicitly raised in this initial submission. Potential areas for review could include: interaction with other commit-graph features, performance impact measurements, edge cases in config precedence.

Participant positions: Only author Taylor Blau so far, proposing the change based on proven stability and performance benefits in large monorepos. Scalar integration suggests Microsoft/GitHub use case validation.

Related work: Builds on existing changed-path Bloom filter implementation. Connects to Scalar's large-repo optimizations. Relevant to monorepo performance work by Derrick Stolee and others.