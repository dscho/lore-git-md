Thread identity: Feature patch series introducing `commitGraph.changedPaths` config option for commit-graph changed-path Bloom filters, with contributions from Taylor Blau and Emily Yang (Microsoft Office monorepo team), reviewed by Junio C Hamano and Derrick Stolee.

Core problem or goal: Establish persistent configuration for writing changed-path Bloom filters during `git commit-graph write`, resolving tension between config symmetry and non-destructive behavior while supporting large monorepo workflows. The design distinguishes command-line flags (`--[no-]changed-paths`) from config settings, with different semantics for filter removal versus preservation.

Current status: Final version queued by Junio Hamano for 'next' after Derrick Stolee's Reviewed-by on v2 implementation. The three-state config (true/false/unset) with Scalar integration is now documented to clearly differentiate command-line behavior (where `--no-changed-paths` removes filters) from config behavior (where `false` preserves existing filters). Microsoft Office monorepo use case remains the primary validation environment.

Key technical details: `enable_changed_paths` field in `struct commit_graph_opts` uses -1/0/1 values. Command-line flags take precedence over config. Test coverage in t5318-commit-graph.sh verifies: (1) config/flag precedence rules, (2) preservation of existing filters when config is set to `false`, and (3) interaction with maintenance commands. Documentation explicitly states config precedence changes don't trigger filter removal.

Open questions: Real-world performance validation in Microsoft Office monorepo remains ongoing. The config/flag pattern may serve as a model for similar features. Edge case monitoring in maintenance command interactions will continue post-merge.

Participant positions: Taylor Blau initiated design. Emily Yang implemented with Microsoft-specific integration. Junio approved documentation clarifications before queueing. Derrick Stolee provided Reviewed-by, confirming technical consensus. Scalar team's needs directly shaped the solution.

Related work: Builds on changed-path Bloom filters and connects to Scalar/monorepo optimizations. Microsoft's Office monorepo serves as primary validation environment, with potential implications for other large-scale Git deployments. The config behavior pattern may inform similar features elsewhere in Git.