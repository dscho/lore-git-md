Thread identity: Feature patch introducing `commitGraph.changedPaths` config option for commit-graph changed-path Bloom filters, authored by Taylor Blau, with review feedback from Junio C Hamano.

Core problem or goal: Add persistent configuration for writing changed-path Bloom filters during `git commit-graph write`, which currently require explicit `--changed-paths` flag. The filters accelerate file history computations in large repositories. Junio's review focuses on ensuring the config option's design is conceptually clean and follows Git's configuration conventions.

Current status: Initial version implements the config option with three states (true/false/unset), maintaining backward compatibility. Junio suggests making the false case symmetric (implying `--no-changed-paths`) for future-proofing, even though current defaults make this moot. He also questions the -1/0/1 implementation approach, preferring standard boolean 0/1.

Key technical details: New `enable_changed_paths` field in `struct commit_graph_opts` currently uses -1/0/1 values. Junio outlines the standard configuration processing flow (default -> config -> env -> command line) and suggests the implementation should better follow this pattern. Scalar integration recommends enabling for large repos. Test coverage in t5318-commit-graph.sh exists but may need expansion for revised behavior.

Open questions: Implementation needs revision to handle false case symmetrically and align with standard config processing patterns. Should the internal representation change from -1/0/1 to 0/1? Are there edge cases in config precedence that need explicit testing?

Participant positions: Taylor Blau proposed the change based on Scalar's large-repo use cases. Junio provides authoritative review feedback emphasizing design symmetry and conventional configuration handling patterns. His suggestions carry significant weight as maintainer.

Related work: Builds on existing changed-path Bloom filter implementation. Connects to Scalar's large-repo optimizations and monorepo performance work by Derrick Stolee. The config option would complement other commit-graph features like generation numbers.