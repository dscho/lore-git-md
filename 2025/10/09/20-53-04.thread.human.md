The thread continues to evaluate the potential deprecation of `git whatchanged`, a legacy command that duplicates `git log` functionality with different default output. After Justin Sherrill reported using the command in AWS YAML file management (the only usage report so far), Kristoffer Haugsbakk has now offered detailed migration guidance. 

Kristoffer provided exact equivalents for `git whatchanged` using `git log --no-merges --raw`, along with suggestions for more human-readable alternatives like `--stat` or `--name-only` output formats. This confirms the commands share the same backend machinery, differing only in presentation. While helpful for users who may need to transition, the response doesn't present new reasons to keep `git whatchanged` - it simply makes the deprecation path clearer.

The discussion remains open for additional usage reports, but so far only one straightforward case has emerged, and the project appears to be proceeding toward removal unless stronger justifications surface. The deprecation warning mechanism (requiring `--i-still-use-this`) continues to serve its purpose of identifying active users before final decisions are made.