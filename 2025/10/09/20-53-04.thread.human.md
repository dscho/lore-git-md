This thread discusses the potential deprecation of `git whatchanged`, a legacy command that duplicates `git log` functionality but with different default output. The command currently requires a `--i-still-use-this` flag as a deprecation warning measure while the project evaluates whether there are enough active users to justify continued support.

Justin Sherrill initially reported using `git whatchanged` in an AWS YAML configuration management context, providing the first concrete usage example. Kristoffer Haugsbakk then contributed detailed migration guidance showing how to replace `git whatchanged` with equivalent `git log` invocations, including `--no-merges --raw` for an exact functional match, plus more readable alternatives like `--stat` or `--name-only`. 

In the latest update, Justin Sherrill confirms the migration guidance was helpful and expresses satisfaction with the replacement options. This positive feedback suggests the transition path is working as intended, with users understanding how to adapt their workflows. No new technical objections or unique use cases have emerged beyond the initial report.

The discussion remains open to gather any additional user reports, but the current evidence suggests the deprecation process is proceeding smoothly. The command shares backend machinery with `git log` and differs only in default formatting, making this a relatively straightforward migration compared to some other legacy command removals. The project's approach of providing clear documentation alongside the deprecation warning appears effective in facilitating this transition.