This is part 5/6 of the packfile store API refactoring series, introducing a new macro to simplify pack iteration patterns. The patch adds a `repo_for_each_pack()` macro that replaces repetitive boilerplate code for iterating through all packs in a repository. 

The macro is implemented in packfile.h and used to replace 20 instances of manual pack iteration across various files including builtins (cat-file, count-objects, fsck, gc, pack-objects), object lookup paths, and test helpers. Each conversion follows the same straightforward pattern - replacing explicit `packfile_store_get_all_packs()` calls with the new macro while maintaining the same iteration logic. The changes are purely mechanical refactoring with no behavior changes.

Key technical details include the macro definition in packfile.h that hides the packfile store access behind a cleaner interface, and the systematic conversion of call sites that previously had to explicitly access the repository's packfile store. The patch continues the series' focus on simplifying pack iteration patterns by providing a more ergonomic API while maintaining the same underlying behavior. This builds on previous patches that established the new iteration patterns and removed confusing stateful variants of pack access.

The change is well-contained and follows the established pattern of the series - first introducing safer alternatives, then converting callers, and now providing syntactic sugar to make the new patterns more maintainable. It touches core pack operations but remains an internal refactoring with no user-facing impact.