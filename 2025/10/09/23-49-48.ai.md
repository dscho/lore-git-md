Follow-up review email in const correctness patch thread.

This is a review response to Okhuomon Ajayi's v2 patch fixing const correctness in `patch_id_neq()`. Agatha Isabelle provides three submission process suggestions: 1) The patch subject should include `[PATCH v2]` prefix, 2) The commit message is missing a `Signed-off-by` trailer, and 3) The version changelog ("Changes in v2") should appear in the email body rather than the commit message. The review is positive ("Nice patch!") and includes links to relevant documentation about Git contribution conventions.

The technical content of the patch itself remains unchanged from the thread summary - it removes a NEEDSWORK comment and properly types the diff_options parameter as const in patch-ids.c. This email focuses purely on submission mechanics rather than the code changes, indicating the patch is technically sound but needs minor formatting adjustments before final submission. The reviewer appears to be helping guide a new contributor (an Outreachy participant) through Git's patch submission workflow.