Thread identity: Deprecation feedback for `git whatchanged`, a legacy porcelain command that duplicates `git log` functionality but with different default output format. Current status is that the command requires `--i-still-use-this` flag as a deprecation warning measure.

Core problem: Evaluating whether `git whatchanged` has enough active users to justify continued support given its functional overlap with `git log`. The command was previously marked for removal but the project is gathering usage data before final decision.

Current status: Two usage reports received - initial from Justin Sherrill showing active use in configuration management context (AWS YAML file reference), followed by his confirmation that Kristoffer Haugsbakk's migration guidance (`git log --no-merges --raw` for exact match, with `--stat` or `--name-only` as alternatives) provides satisfactory replacement path. The deprecation warning mechanism continues to surface users while enabling transition.

Key technical details: `git whatchanged` shows commit history with diff output by default (equivalent to `git log -p`), sharing backend machinery with `git log` but differing in default formatting. No behavioral changes proposed - discussion remains focused on removal viability given demonstrated migration path.

Open questions: Whether additional users will report dependencies beyond the confirmed case. Current evidence suggests migration documentation successfully addresses the known use case, lowering barrier to removal. No technical objections or unique value propositions for `git whatchanged` have emerged.

Participant positions: Justin Sherrill reported usage and confirmed migration path works. Kristoffer Haugsbakk provided comprehensive replacement syntax without opposing deprecation. Interaction pattern shows users accepting transition when given clear alternatives.

Related work: Part of ongoing legacy command pruning (cf. `git repo-config` -> `git config` migration). Current thread follows established deprecation playbook: document exact replacement syntax, gather usage reports, then remove if no blockers emerge. The `--i-still-use-this` flag effectively surfaces active users while nudging them toward modern equivalents.