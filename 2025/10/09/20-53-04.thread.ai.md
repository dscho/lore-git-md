Thread identity: Deprecation feedback for `git whatchanged`, a legacy porcelain command that duplicates `git log` functionality but with different default output format. Current status is that the command requires `--i-still-use-this` flag as a deprecation warning measure.

Core problem: Evaluating whether `git whatchanged` has enough active users to justify continued support given its functional overlap with `git log`. The command was previously marked for removal but the project is gathering usage data before final decision.

Current status: One usage report received from Justin Sherrill showing active use in configuration management context (AWS YAML file reference). Kristoffer Haugsbakk has provided detailed migration guidance showing how to replace `git whatchanged` with equivalent `git log` invocations (`--no-merges --raw` for exact match, with `--stat` or `--name-only` suggested as more readable alternatives). The deprecation warning mechanism (`--i-still-use-this` flag) continues to surface active users while enabling their transition.

Key technical details: `git whatchanged` shows commit history with diff output by default (equivalent to `git log -p`), originally created before `git log` gained diff capabilities. Kristoffer confirms both commands share the same backend machinery, differing only in default output formatting. No behavioral or interface changes proposed - discussion remains focused on removal vs. continued support.

Open questions: Whether additional users will report dependencies on the command beyond the single known case, and whether any reported use cases demonstrate unique value not served by `git log`. The provided migration path appears technically sound, lowering the barrier to removal.

Participant positions: Justin Sherrill reported basic usage. Kristoffer Haugsbakk contributed migration guidance without taking a position on deprecation itself. No technical objections or alternatives proposed yet beyond the initial usage report.

Related work: Part of ongoing effort to prune rarely-used legacy commands that duplicate functionality of more modern equivalents. Similar discussions have occurred around `git repo-config` (replaced by `git config`) and other historical aliases. The technical response pattern mirrors prior migrations where exact replacement syntax was documented before removal.