Feature patch (part 4/5) implementing submodule gitdir path encoding to prevent filesystem conflicts. This patch introduces the `extensions.submoduleEncoding` config which enables URL-style encoding of submodule paths in `.git/modules/`, with special handling for case-insensitive filesystems (A -> _a, B -> _b etc.). The implementation preserves backward compatibility by continuing to recognize legacy unencoded paths while applying encoding to new submodules. The patch includes comprehensive tests in `t7425-submodule-encoding.sh` covering mixed legacy/encoded environments, cloning scenarios, and nested submodule cases.

This is a well-constructed patch that clearly builds on prior work in the series (parts 1-3 established the infrastructure). The commit message honestly notes remaining Windows reserved name issues while delivering a robust solution for the immediate problem. Test coverage appears thorough, including edge cases like parallel cloning and nested submodules. The documentation updates clearly explain the new extension and its interaction with existing configs.