This is a follow-up discussion on a CI/build system patch that removes an obsolete Windows-specific workaround for Python certificate store issues. The original patch (3/5 in the series) deleted 9 lines from `.gitlab-ci.yml` that previously imported cURL's certificates into Python's store via OpenSSL - a hack that had become problematic when Chocolatey updated OpenSSL packages.

Toon Claes confirms the removal makes sense, pointing to a GitLab runner image change that likely fixed the underlying certificate store issue. The exchange validates the cleanup is safe and timely, with no objections to removing what was clearly marked as an "ugly workaround" in the original commit message. This appears to be a straightforward housekeeping improvement to the CI configuration, with the discussion serving as a quick sanity check before merging.