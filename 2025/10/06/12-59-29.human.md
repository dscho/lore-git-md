This is a documentation patch that adds clarity to the interaction between the new `stash.index` config and `--autostash` operations. It's the final piece of a now-merged series that made stash operations respect staged changes by default when configured.

The patch explicitly documents that the `stash.index` setting (which controls whether `git stash apply/pop` restores staged changes) also affects operations that use stash implicitly via `--autostash`, such as merge, rebase, and pull. This connection wasn't previously mentioned in the docs, though the behavior was implemented in the earlier patches.

The change is straightforward - just four lines added to the config documentation that clearly state the relationship. This follows through on the series' goal of making stash behavior more consistent and configurable, while ensuring users understand all the ways the new setting affects their workflow. The patch builds on already-merged work and appears ready to go as a final documentation polish.