This is a small refactoring patch in Karthik Nayak's reftable fsck validation series (v5). It alphabetically reorders the `fsck_msg_type` enum in `fsck.h` to maintain consistent organization. The changes are purely cosmetic - no behavior is altered, just the ordering of error/warning/info message types within their respective sections.

The patch is straightforward and uncontroversial, fixing some minor inconsistencies in the existing ordering. It moves related message types (like the gitmodules and gitattributes groups) together and ensures alphabetical ordering within each severity category (ERROR, WARN, INFO). 

As part of the final polishing for this series (which has already received maintainer approval), this change improves code maintainability by making the message types easier to scan and verify. The diff shows the before/after ordering clearly, with no functional impact on the fsck system.