This is part 1 of a 5-patch series implementing submodule gitdir path encoding to prevent filesystem conflicts. The series is now in its third iteration with significant refinements based on reviewer feedback.

The patch updates `submodule--helper` to use the centralized `submodule_name_to_gitdir()` function rather than hardcoding path construction. This change ensures consistent handling of submodule paths across the codebase, particularly important as the series introduces new path encoding behavior. The modification is straightforward, replacing direct string formatting with the helper function while maintaining the same logical flow.

The change fixes test failures that occurred when the hardcoded paths didn't account for the new encoding scheme. This appears to be a mechanical but necessary update to align with the series' broader goal of making submodule paths more robust. The patch is small (13 lines changed) and focused, with no apparent functional changes beyond the path handling unification.

As the first patch in the series, it sets up infrastructure that subsequent patches will build upon. The clean separation between this mechanical update and the more substantive encoding changes in the series suggests thoughtful patch organization.