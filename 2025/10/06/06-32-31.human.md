This is the second patch in a 4-part series refactoring the string-list API to improve type safety and prevent integer overflow issues. The patch focuses on `string_list_find_insert_index()`, which previously used a negative index encoding (-1 - index) to indicate exact matches. The new version replaces this with a cleaner `bool* exact_match` parameter and changes the index return type to `size_t`.

The changes make the API more intuitive by separating the index position from the exact-match flag, while also addressing potential overflow issues with large string lists. The patch updates all existing callers in `add-interactive.c`, `mailmap.c`, and `refs.c` to use the new interface. Notably, callers that don't need the exact-match information can now pass NULL for the parameter.

This is a well-motivated change that follows through on the series' goal of eliminating sign comparison warnings while improving API clarity. The implementation appears thorough, with all call sites updated consistently. The patch builds on previous discussion in the thread about balancing type safety with practical considerations, and represents a clear improvement over the previous encoding scheme.