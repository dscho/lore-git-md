Git submodule path encoding cleanup (part of v5 series). This is a small but important cleanup patch from Junio Hamano addressing a remaining hardcoded path in `add_submodule()` that wasn't using the new `submodule_name_to_gitdir()` helper. The change ensures consistent path handling across all submodule operations as part of the larger path encoding effort.

The patch replaces direct path construction with the centralized helper function, which properly handles the new encoding scheme. Junio notes the commit message could be clearer about this being a conversion to an existing helper rather than introducing new functionality. He also observes the patch incidentally narrows a variable's scope as a side effect of the change.

This appears to be a straightforward improvement that brings `add_submodule()` in line with the rest of the codebase's handling of submodule paths. The change is well-contained and maintains existing resource management patterns. As part of the mature v5 series that has addressed all major technical concerns, this patch looks ready to go in.