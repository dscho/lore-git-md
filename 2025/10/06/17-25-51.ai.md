Performance investigation into untracked files cache behavior, focusing on `--no-optional-locks` interaction. Matthew Hughes isolates `--no-optional-locks` as the specific flag causing cache update failures when combined with `--untracked-files=all`, building on Devste Devste's earlier findings about flag interactions. The key discovery is that `repo_update_index_if_able()` in `cmd_status` only executes when `use_optional_locks` returns true, creating a direct link between the flag and cache update behavior.

The email provides concrete timing data showing the performance impact: commands with `--no-optional-locks` take >1s while subsequent runs without it drop to ~300ms. This confirms the cache update mechanism depends on optional locks being enabled, though the exact relationship between lock behavior and cache updates remains to be fully explained. The reproduction case demonstrates how the issue manifests in practice, using a clean config environment via `GIT_CONFIG_GLOBAL=/dev/null`.

This moves the thread forward by identifying a specific flag among the previously suspected group (`--porcelain`, `--no-optional-locks`, `--ignore-submodules=dirty`, `--no-ahead-behind`) that definitively affects cache updates. The analysis suggests the "sticky cache" behavior observed earlier may stem from this lock-related update mechanism failing. The findings provide a concrete starting point for potential fixes while raising new questions about whether this lock dependency is intentional design or an oversight.