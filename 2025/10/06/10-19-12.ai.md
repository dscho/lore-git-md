This is a follow-up discussion on a CI/build system patch that removes an obsolete Windows-specific workaround for Python certificate store issues. The original patch (3/5 in the series) deleted 9 lines from `.gitlab-ci.yml` that previously imported cURL's certificates into Python's store via OpenSSL - a solution that had become problematic when Chocolatey updated OpenSSL packages.

Toon Claes confirms the removal is appropriate, linking to a GitLab merge request that appears to have fixed the underlying certificate store issue in Windows container images. The exchange serves as validation of the patch's technical basis, with no objections raised to the cleanup. The discussion remains focused on this specific CI configuration change without expanding scope.

The thread context shows this was part of a broader series improving GitLab CI reliability, with this particular patch addressing both technical debt of an outdated workaround and the practical issue of intermittent OpenSSL installation failures during package updates. The follow-up provides additional technical context (the GitLab runner image change) that wasn't in the original commit message but supports the change's rationale.