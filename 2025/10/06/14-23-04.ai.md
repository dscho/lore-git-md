This is part 6/7 of the reftable fsck validation series, implementing the core consistency checking infrastructure for the reftable backend. The patch introduces `reftable/fsck.[ch]` which provides a callback-based API (`reftable_fsck_check()`) for running fsck checks on reftable stacks while maintaining separation from Git internals.

The main addition is table name validation through `table_check_name()`, which enforces the expected hex-hex-hex.ref format pattern. The implementation uses a flexible architecture with `table_check_fns[]` array to allow future checks to be added easily. The validation includes detailed error reporting via `reftable_fsck_info` struct and supports verbose progress reporting.

Key technical details:
- New files: `reftable/fsck.c` and `reftable/reftable-fsck.h`
- Core function: `reftable_fsck_check()` takes a stack and callback functions for reporting issues/progress
- Current check: `table_has_valid_name()` validates the xxx-xxx-xxx.ref pattern using `strtoull`/`strtoul`
- Error handling: Uses `REFTABLE_FSCK_ERROR_TABLE_NAME` for invalid names
- Build system: Updates Makefile and meson.build to include new files
- The patch maintains reftable's independence from Git core while providing integration points
- Follows the series' pattern of strict validation and careful error reporting
- Prepares ground for future checks via the callback architecture

This is a well-structured addition to the reftable fsck series, implementing the promised validation infrastructure with clean separation of concerns. The code matches the quality seen in previous iterations, with thorough error handling and clear interfaces. The patch completes the technical foundation for reftable fsck that was outlined in earlier series discussions.