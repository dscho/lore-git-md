This is part 1/5 of the submodule gitdir path encoding series (v3), focusing on a bugfix in the submodule--helper's `add_submodule` function. The patch converts hardcoded path construction to use the centralized `submodule_name_to_gitdir()` helper, which properly handles the new path encoding scheme when the `extensions.submoduleEncoding` config is enabled.

The change ensures consistent path handling between submodule operations, fixing test failures that occurred when the new encoding was active. The modification affects the path checking logic when adding existing submodules, replacing direct string formatting with the helper function that respects the encoding rules established in the series.

Key technical details:
- Replaces `xstrfmt(".git/modules/%s", name)` with `submodule_name_to_gitdir()`
- Uses `strbuf` for path storage rather than raw char pointers
- Maintains same error handling flow but with encoded paths when enabled
- Touches only `builtin/submodule--helper.c`

This is a straightforward mechanical change that aligns the helper with the series' goal of consistent path handling. The patch appears well-contained and properly tested as part of the larger series' test suite in `t7425-submodule-encoding.sh`.