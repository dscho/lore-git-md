This is a refactoring patch in Patrick Steinhardt's ongoing ODB abstraction series, specifically addressing the alternate object database handling logic. Karthik Nayak provides a review of patch 1/13, which simplifies the `alt_odb_usable()` function by separating the path usability check from the path storage operation.

The key change moves the path storage from `alt_odb_usable()` to `link_alt_odb_entry()`, making the ownership relationship clearer - paths are now stored only after the source object is created, using the source's path pointer directly. This eliminates the subtlety where paths were temporarily stored in a map before being properly owned by the source object.

Karthik approves the change, noting it improves the logical flow by performing the path addition after source creation rather than during the usability check. The review highlights how this makes the code more intuitive while maintaining the same functionality. The patch touches `odb.c` with 17 insertions and 13 deletions, primarily restructuring the path handling logic without changing behavior.

This continues the series' pattern of positive reviews with minor suggestions, in this case a nit about sentence structure in the commit message. The change aligns with the series' goal of making the ODB abstraction cleaner and more maintainable while preserving existing behavior.