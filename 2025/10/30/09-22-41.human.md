This is a technical discussion about final polish items in the `git blame` diff algorithm configurability series. Antonin Delpeuch responds to Phillip Wood's review feedback about handling the `XDF_NEED_MINIMAL` flag in the diff algorithm selection.

The key issue is that the current code doesn't properly clear the `XDF_NEED_MINIMAL` flag when resetting the diff algorithm, which could cause incorrect behavior with certain config file configurations. Phillip suggested introducing a new macro to handle this cleanup, but Antonin explores whether it might be better to instead include `XDF_NEED_MINIMAL` in the existing `XDF_DIFF_ALGORITHM_MASK`.

Antonin notes this change could simplify the code by making some existing flag-clearing operations redundant, but recognizes it might be too invasive since xdiff is used as a library by other projects. The discussion shows careful consideration of both the technical solution and its broader implications, typical of the final polishing phase of a well-reviewed patch series.