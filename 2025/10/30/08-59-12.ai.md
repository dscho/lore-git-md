This is a follow-up discussion about the pack-objects caching optimization in patch 5/8 of the packfile store abstraction series. Patrick Steinhardt responds to Toon Claes' question about the rationale behind the `last_found` cache in the `has_sha1_pack_kept_or_nonlocal()` function.

Key points:
- The function checks if an object exists in kept/non-local packs, with a cache (`last_found`) to optimize repeated lookups
- Toon questioned the cache's effectiveness since it's global and called with arbitrary object IDs
- Patrick acknowledges it's likely a micro-optimization but chose not to investigate its origins deeply
- The focus was on making the caching logic more maintainable rather than questioning its existence
- This relates to the earlier simplification where Patrick replaced manual pack iteration with `repo_for_each_pack()`

Context from thread:
- Taylor Blau had previously confirmed the `(void *)1` sentinel check could be safely removed in this patch
- The caching optimization was preserved while making the code more readable
- Patrick indicates openness to future optimizations (like using kept_cache) but prioritized maintainability for now

The discussion shows the patch is settling into its final form with all major questions addressed, though there remains potential for future performance work on the caching strategy.