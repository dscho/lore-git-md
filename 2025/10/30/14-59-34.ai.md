Platform compatibility follow-up regarding NonStop shell behavior. Randall Becker confirms that Jeff King's proposed `TEST_SHELL_PATH=/bin/bash` workaround successfully addresses the immediate test failures in t7900 by ensuring bash is used consistently. He notes this resolves the divergence between Make's recipe processing (using bash) and test script execution (previously defaulting to ksh via `/bin/sh` shebang).

The email indicates this was not an isolated issue - other ksh-related problems exist with the `SHELL` setting in Make, though Randall will report those separately as they surface in specific test cases. The discussion remains focused on practical solutions that minimize manual intervention for NonStop maintainers, with the `TEST_SHELL_PATH` approach proving effective for the immediate test_subcommand assertion failures.

Key technical details include the interaction between Make 4.1.2's shell handling, Git's `SHELL_PATH` configuration, and the platform-specific `/bin/sh`->ksh mapping on NonStop systems. The thread continues evaluating whether additional test infrastructure changes might be needed to handle other shell-related issues that may emerge, balancing portability concerns with maintainer workflow impacts.