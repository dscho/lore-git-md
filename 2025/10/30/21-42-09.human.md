This is a brief follow-up in the ongoing SHA-1/SHA-256 interoperability series, specifically addressing a build system implementation detail in the Rust integration work.

The email resolves an open question from the thread about memory allocation strategy at the FFI boundary between C and Rust code. Junio C Hamano had noted that the current implementation uses plain `malloc()` where Git traditionally uses `xmalloc()` (which aborts on failure). The author confirms they will switch to `xmalloc` for consistency with Git's conventions, while noting that Rust's own allocation handling (which panics on OOM) means they won't need to handle allocation failures explicitly in the Rust code.

This is a minor but important clarification in the larger context of safely integrating Rust into Git's codebase. The change aligns with the project's memory management practices while respecting Rust's different approach to allocation failures. The response is straightforward and uncontroversial, moving one of the thread's open questions toward resolution.