This is a technical discussion about platform-specific test failures on NonStop systems, continuing the thread about shell selection issues during Git test execution.

Jeff King responds to Randall Becker's report about test failures occurring when the test scripts' `#!/bin/sh` shebang causes them to run under ksh instead of bash on NonStop systems. While Randall had identified that manually running tests with bash resolves the failures, Jeff suggests trying `make TEST_SHELL_PATH=/bin/bash` as a potential solution. He explains the distinction between Git's `SHELL_PATH` (used for #! lines and C program invocations) and Make's `SHELL` (used for recipe processing), noting that setting `SHELL_PATH` might help if `/bin/sh` isn't fully functional.

The discussion remains focused on diagnosing and resolving the shell selection behavior that affects test execution, particularly for maintenance tests verifying `git gc` invocations. This appears to be a platform-specific interaction between NonStop's shell configuration and GNU Make, with Jeff offering a Git-specific configuration option (`TEST_SHELL_PATH`) as a potential workaround beyond the basic Make `SHELL` variable that Randall had already tried. The response maintains the technical focus of the thread while introducing a new variable to consider in the solution space.