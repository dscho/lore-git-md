This is a small follow-up discussion in the packfile refactoring series, specifically about patch 5/8 which simplified object lookup in `builtin/pack-objects.c`. 

Toon Claes points out a minor code organization issue in the patch - suggesting that a particular check (which skips re-checking the `last_found` pack) should be moved to the beginning of the loop rather than its current position. This isn't about the `(void *)1` sentinel check that was discussed earlier in the thread, but rather about logical flow within the function.

The comment shows this is part of ongoing fine-tuning of the pack-objects caching logic, where the maintainers are carefully reviewing even small implementation details. Since this is a relatively minor point in an otherwise well-reviewed patch series, it's the kind of optimization that might get addressed in a follow-up or during integration rather than requiring a full re-roll.