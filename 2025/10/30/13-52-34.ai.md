This is a platform compatibility discussion about NonStop shell behavior in Git's test suite. D. Ben Knoble follows up on Randall Becker's report of test failures when `t7900` maintenance tests run under ksh instead of bash on NonStop systems. The core issue involves a mismatch between Make's use of `/bin/bash` for recipe processing versus test scripts' `#!/bin/sh` shebang invoking ksh, causing `test_subcommand` assertions to fail.

Knoble seeks clarification on whether this is the only ksh-related failure and probes whether the root cause is a test library limitation or non-portable shell behavior. He expresses a preference for solutions that make the setup "just work" without violating portable standards, suggesting openness to modifying the test infrastructure if the issue stems from shell portability rather than platform quirks.

The email continues the thread's exploration of workarounds versus test harness fixes, with Jeff King's earlier suggestion of `TEST_SHELL_PATH` still under consideration. Key technical details include the specific test file (`t7900`), the problematic assertion pattern (`test_subcommand !`), and the shell selection mechanics involving Make 4.1.2's handling of `SHELL` versus Git's `SHELL_PATH`. The discussion remains focused on balancing platform-specific needs with maintainability concerns.