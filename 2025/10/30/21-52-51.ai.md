This is a follow-up discussion in the SHA-1/SHA-256 interoperability patch series, specifically addressing memory allocation at the FFI boundary between C and Rust code.

The email resolves an earlier question about whether to use `malloc` or Git's customary `xmalloc` for allocating hash contexts at the FFI boundary. Brian M. Carlson confirms he will change the implementation to use `xmalloc` for consistency with Git's conventions, noting that while Rust handles its own allocations (panicking on OOM), the FFI boundary should maintain Git's standard practices. Junio C. Hamano acknowledges this and reflects on the need to think beyond "C vs Rust" boundaries, emphasizing that allocations should seamlessly cross between language domains.

This discussion finalizes a technical detail in patch 9/14 of the series, which exposes hash context functions to Rust. The resolution aligns with the thread's focus on careful FFI design while maintaining Git's existing conventions where appropriate. The change affects the `git_hash_alloc()` function implementation but doesn't require broader architectural modifications. This email marks the conclusion of the memory allocation strategy discussion that was previously noted as an open question in the thread summary.