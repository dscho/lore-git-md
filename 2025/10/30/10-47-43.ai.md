This is a technical follow-up in the `git blame` diff algorithm configurability series, focusing on implementation details of flag handling.

Phillip Wood responds to Antonin Delpeuch's consideration of adding XDF_NEED_MINIMAL to XDF_DIFF_ALGORITHM_MASK for consistent algorithm flag handling. Phillip agrees this is a good solution, noting that "minimal" should be treated as a distinct algorithm rather than a variant of Myers. He acknowledges this would be a breaking change for xdiff library users but considers it justified given the conceptual model.

The discussion reveals two viable approaches for fixing the flag handling issue: either expand XDF_DIFF_ALGORITHM_MASK (Antonin's suggestion) or use a CLEAR_DIFF_ALGORITHM macro (Phillip's original suggestion). Phillip had initially proposed the macro approach due to concerns about reviewing all existing uses of XDF_DIFF_ALGORITHM_MASK, but now sees the mask expansion as conceptually cleaner.

This email moves the technical discussion forward by establishing consensus on the preferred solution (mask expansion) while acknowledging the breaking change implications for xdiff. The exchange demonstrates both contributors' careful consideration of implementation impacts beyond just the immediate use case. The thread remains focused on finalizing this technical detail before the otherwise-ready series can be merged.