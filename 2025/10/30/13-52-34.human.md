This is a follow-up in the ongoing discussion about NonStop platform test failures related to shell selection in Git's test suite. D. Ben Knoble responds to Randall Becker's frustration about `/bin/sh` defaulting to ksh on NonStop systems, causing `test_subcommand` assertions to fail in t7900 maintenance tests.

Knoble seeks clarification on whether this is the only ksh-related failure and asks about the root cause - whether it's a test library issue or a shell portability problem. The email suggests a preference for finding a solution that makes the setup "just work" without requiring manual intervention, assuming there's no clear violation by ksh.

The context shows this is part of a broader effort to resolve platform-specific test failures while minimizing maintenance burden. Previous suggestions included using `make TEST_SHELL_PATH=/bin/bash` as a workaround, but the discussion continues to explore more sustainable solutions. The tone is collaborative, focusing on understanding the technical constraints before proposing fixes.