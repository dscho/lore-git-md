This is a refactoring patch in the atomic ref updates series for `git replay`. It prepares for adding the new `--ref-action` option by standardizing the command's option validation.

The patch replaces a custom error message for mutually exclusive options (`--advance` and `--contained`) with Git's standard `die_for_incompatible_opt2()` helper. This change:
- Provides consistent error messaging that matches other Git commands
- Makes the code more maintainable by using established patterns
- Sets up the infrastructure for cleanly adding validation of the upcoming `--ref-action` option in subsequent patches

The implementation is straightforward, changing just 3 lines in `builtin/replay.c` to use the helper function while maintaining the same validation logic. This follows the series' pattern of aligning with Git's standard practices, as noted in previous reviews. The change is well-motivated by the need to maintain consistency when adding the new option in the next patch.

This patch represents final polish on the series' implementation details after addressing all prior review feedback. It demonstrates good attention to codebase conventions and maintainability, though as a pure refactoring with no behavior change, its impact is limited to internal code quality.