This is a refactoring patch in the atomic ref updates series for `git replay`. It's the first of three patches in v6 of the series, which has already received extensive review and is now in its final polish phase.

The patch standardizes option validation in `git replay` by switching from a custom error message to Git's standard `die_for_incompatible_opt2()` helper. This prepares for the upcoming `--ref-action` option in the next patch by establishing consistent error handling patterns. The change is mechanical, replacing a direct `die()` call with the helper function for the existing `--advance`/`--contained` option conflict check.

The patch is small (just 3 lines changed) and clearly motivated as part of aligning with Git's standard patterns. Since this series has already gone through multiple review rounds with all technical concerns addressed, this appears to be a straightforward preparatory change that maintains the codebase's consistency. The commit message clearly explains the rationale and connects it to the larger series' goals.