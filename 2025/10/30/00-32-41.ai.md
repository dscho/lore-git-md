This is a technical discussion about the Rust `ObjectID` struct implementation in the SHA-1/SHA-256 interoperability series. The email responds to concerns about Rust idioms versus performance tradeoffs in FFI code.

The author (brian m. carlson) acknowledges Patrick Steinhardt's desire for more idiomatic Rust patterns but explains the deliberate design choice to minimize conversions in the `ObjectID` implementation. The key justification is performance - this code handles hot paths in `index-pack` and protocol operations where bulk object ID conversions occur during repository cloning/format conversion and ref advertisement (with some deployments having 500,000+ refs). The current approach prioritizes speed over Rust idioms at the FFI boundary, though the author notes plans for future patches to improve the Rust-side interface.

This exchange highlights an ongoing tension in the Rust integration - balancing FFI performance constraints against establishing good practices for Git's first major Rust code. The discussion suggests the current implementation is a pragmatic choice for critical paths, with room for refinement in future iterations. The thread context shows this is part of broader design discussions about type safety, memory management, and build system integration in the Rust/C interop layer.