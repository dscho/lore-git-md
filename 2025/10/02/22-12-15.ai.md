Documentation patch fixing asciidoc formatting issues in `sparse-checkout.adoc`, part 3/4 in a series addressing build warnings across technical docs. This patch specifically targets two classes of asciidoc/asciidoctor warnings in the sparse-checkout documentation: numbered lists incorrectly starting at 0 (should start at 1) and section heading level mismatches (using `===` where `==` was expected). The changes renumber one list and adjust heading levels throughout while preserving content.

The patch also addresses several other formatting issues: fixes nested unordered list syntax (adding appropriate `*` levels), improves literal block handling for consistent rendering, converts some sections to labeled lists for better structure, and removes a problematic trailing space in a `git grep` example. The diff shows extensive changes (704 lines modified) but these are primarily syntax adjustments rather than content changes.

This continues the series' goal of bringing Makefile-built documentation parity with meson builds by eliminating asciidoc warnings. The changes appear thorough and well-justified, though as noted in the thread context, some monospace and URL formatting issues remain unresolved across the documentation effort. The patch maintains the technical content while making the source more compliant with asciidoc standards.