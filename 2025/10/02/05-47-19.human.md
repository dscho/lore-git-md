This is a follow-up discussion on the build system refactoring series aimed at Rust integration. Patrick Steinhardt raises two specific points about the second patch, which removes the XDIFF_LIB static library in favor of including xdiff objects directly in LIB_OBJS.

First, he notes that the patch removes the `xdiff-objs` phony target without explanation in the commit message. While he personally doesn't object to its removal, he suggests either keeping it or justifying its removal. Second, he questions whether the XDIFF_OBJS variable is still needed at all if the objects are being folded into LIB_OBJS, suggesting they could be added directly.

The questions focus on implementation details rather than the overall approach, which Junio Hamano has already approved. The thread context shows this is part of a non-controversial series to unify static library handling between Makefile and meson builds, primarily to support future Rust integration. Patrick's comments appear to be seeking clarification rather than raising objections, suggesting the patch may need minor adjustments to its commit message or implementation.