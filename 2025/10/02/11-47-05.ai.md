Security-focused follow-up in the symbolic reference validation thread. Patrick Steinhardt agrees with Karthik Nayak's analysis that the validation logic should be unified between `git fsck` and `git refs verify`, noting that `refs_resolve_ref_unsafe()` currently provides the needed validation in fsck but is missing from the refs verification path.

The key technical point is that reference validation should ultimately live in `git refs verify`, with fsck delegating to it rather than implementing its own checks. This would create a migration path for the current fsck-specific validation of symbolic references (like detecting malformed HEAD via `refs_resolve_ref_unsafe()`) to become part of the core refs verification infrastructure.

The discussion continues to refine the implementation approach, now focusing on how to properly structure the validation hierarchy between these components rather than just fixing the immediate files-backend.c issue. This represents progress toward the thread's goal of consistent reference validation across Git's subsystems, though the specific email doesn't propose new code changes.