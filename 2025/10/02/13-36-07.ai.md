This is a technical discussion about unifying reference validation logic between `git fsck` and `git refs verify` in the context of a security fix for symbolic reference handling.

Junio C Hamano reinforces the architectural direction proposed by Patrick Steinhardt and Karthik Nayak, agreeing that reference validation should be centralized in `git refs verify` rather than having duplicate logic in `git fsck`. He confirms that while `git fsck` currently catches invalid symbolic references (like "refs/../HEAD") through `refs_resolve_ref_unsafe()`, this validation properly belongs in the refs verification subsystem.

The discussion clarifies that `git refs verify` was created as a focused ref-checking tool separate from `git fsck`'s broader repository validation, though the exact original motivation for this separation isn't detailed here. Junio emphasizes that `git fsck` should maintain its current behavior of detecting invalid references, whether through native checks or by delegating to `git refs verify`.

This email moves the thread forward by solidifying consensus on the solution architecture - the validation hierarchy restructuring that will make `git refs verify` the canonical source for reference validation. The immediate files-backend.c fix for symbolic reference validation will need to align with this broader change, though implementation details remain to be worked out. The security scope question (protecting against manual edits) appears settled as an acceptable hardening measure.