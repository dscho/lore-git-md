This is a CI/build system patch series addressing Windows-specific test failures. The cover letter introduces 5 patches that collectively improve reliability of GitLab CI runs on Windows platforms.

The series tackles two categories of issues: intermittent failures in the Windows CI environment itself, and a persistent test failure in the `git-last-modified` tests. The changes include cleanup of duplicate CI configuration, making the system more resilient to monitoring service failures, removing an obsolete Python certificate workaround, improving test log reporting, and fixing a test that was failing due to non-deterministic tag sorting.

The patches touch both CI configuration (`.gitlab-ci.yml`) and test scripts (`t/t8020-last-modified.sh`), with the latter being the more substantial change at 34 lines modified. The author reports successful test runs in their GitLab merge request pipeline, suggesting these changes have been validated in practice.

Key technical details for future reference: the series removes Windows-specific workarounds (Python cert store handling, realtime monitoring), standardizes on JUnit reporting for Meson tests, and fixes test 8020's tag sorting assumption. The changes appear focused and well-scoped to the stated Windows CI issues.