Interactive add navigation behavior fixes and improvements (v3 series):

This thread addresses a regression in `git add --interactive` hunk selection and improves interface consistency across all navigation commands. The v3 series from René Scharfe builds on previous work to unify circular roll-over behavior, now extending it to all navigation commands ('y', 'n', 'j', 'J', 'k', 'K', 'a', 'd', 'e') while refining error messages, test coverage, and documentation.

Core changes in v3:
1. Complete circular roll-over implementation for all navigation operations with new helper functions `get_first_undecided()`, `inc_mod()`, and `dec_mod()`.
2. Unified error messaging with direction-neutral "No other [undecided] hunk" phrasing across all commands.
3. Comprehensive test coverage in `t/t3701-add-interactive.sh` for all navigation scenarios including new test cases for command permission reset behavior.
4. Documentation improvements in `git-add.adoc` and in-program help clarifying command behaviors, with unnecessary status references removed from help text.
5. Implementation refinements including proper reset of `permitted` bitmask for each hunk (fixing command availability carry-over issue).

Technical implementation details:
- Built-in/add-interactive.c modifications for consistent navigation logic and state management.
- `patch_update_file()` updated to handle command availability per-hunk and use `get_first_undecided()`.
- New test case verifies command permission reset behavior by checking invalid 's' command rejection.
- Maintained all previous improvements from v3 series including circular roll-over and error message unification.

Key participants:
- René Scharfe (author) - driving the systematic interface unification and documentation refinements.
- Junio Hamano - provided feedback on implementation symmetry, documentation scope, and suggested considering future dynamic help text. Now indicates the series is ready for merging, with only branch selection ('next' vs 'master') remaining.
- Phillip Wood - reviewed technical approach.

Documentation status:
- Current patch properly eliminates unnecessary status references rather than adding new documentation.
- Dynamic help text (adapting phrasing based on whether current hunk is last undecided one) identified as potential future enhancement but deferred for now.

Status update:
- Series has maintainer approval and will be queued for inclusion in Git's development branches.
- All major feedback incorporated, documentation and test coverage complete.
- Implementation unifies behavior across all navigation commands with robust state management.
- Only minor technical constraints remain (implementation asymmetry between 'J' and 'K' commands, potential redundancy in overflow checking).

Future considerations:
- Dynamic help text possibility for y/n commands (deferred but noted as desirable).
- The changes will appear in an upcoming Git release after integration into 'next' or 'master'.