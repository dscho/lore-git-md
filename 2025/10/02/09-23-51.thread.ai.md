Interactive add navigation behavior fixes and improvements (v3 series) - final status with all feedback:

This thread documents René Scharfe's completed work standardizing `git add --interactive` hunk navigation through a v3 series that has received maintainer approval and thorough technical review. The implementation introduces circular roll-over behavior across all navigation commands ('y', 'n', 'j', 'J', 'k', 'K', 'a', 'd', 'e') via helper functions `get_first_undecided()`, `inc_mod()`, and `dec_mod()` in `builtin/add-interactive.c`, with comprehensive test coverage in `t/t3701-add-interactive.sh`.

Technical implementation includes:
1. Unified navigation logic with direction-neutral error messaging ("No other [undecided] hunk")
2. Command availability handling via `permitted` bitmask (using an anonymous enum inside loop)
3. Verified edge cases through new test scenarios
4. Maintainer Junio Hamano's approval for integration
5. Phillip Wood's confirmed review validating hunk_index safety and test coverage completeness

Post-approval feedback consists of:
- Ulrich Windl's documentation wording preference (original vs current clarified text) regarding navigation key descriptions in `Documentation/git-add.adoc` and `add-patch.c` help text
- Windl's new stylistic suggestion to move the anonymous `permitted` enum declaration outside the loop or use a named typedef, though acknowledging equivalent optimization by GCC

Files affected:
- builtin/add-interactive.c (navigation logic and enum declaration)
- t/t3701-add-interactive.sh (test coverage)
- Documentation/git-add.adoc (navigation documentation)
- add-patch.c (help text)

Key functions:
- get_first_undecided() - finds next actionable hunk
- inc_mod()/dec_mod() - circular navigation
- patch_update_file() - handles command availability

The series remains integration-ready with two minor outstanding style/documentation preferences that would require new patches if adopted. The core technical implementation, having received maintainer approval and thorough review, is considered final. Windl's comments represent the only open discussion points on this otherwise concluded thread.