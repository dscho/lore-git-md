This is a discussion about the design philosophy for `git replay`'s atomic reference updates feature, which is now in its final implementation stage. Kristoffer Haugsbakk draws a parallel to `git-for-each-ref`'s focused, composable design, suggesting that `git replay` could similarly output a clean mapping of old-to-new commits that users could then process as needed.

The email raises an interesting point about client-side use cases, particularly around hook integration, where a format-based output might offer more flexibility than the current atomic-by-default approach. This comes as a thoughtful aside in a thread that has otherwise largely settled on the server-side focused implementation.

While not proposing concrete changes to the current patch series (which has already addressed major technical concerns), the message highlights how different workflows might influence future evolution of the command. The discussion remains theoretical at this stage, with no immediate impact on the merge-ready atomic updates feature.