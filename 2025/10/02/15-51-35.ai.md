This is the second documentation patch in a series improving `git worktree` guidance, specifically adding a recommended example for side-by-side branch checkouts. The patch follows the thread's consensus to enhance worktree documentation rather than modify behavior.

The patch adds a concrete example showing how to set up sibling worktree directories from a bare repository clone, complementing the first patch's warnings against nested worktrees. The example demonstrates:
1. Creating a new directory for the repository
2. Cloning as a bare repo into the `.git` subdirectory
3. Adding two worktrees for different branches in parallel

This addresses one of the open questions from the thread by providing clear usage patterns while maintaining the worktree-as-peer-entities design philosophy. The example uses the `--git-dir` option explicitly, which may help users understand the underlying mechanics.

The patch continues the documentation-focused approach approved in earlier discussion, touching the same `git-worktree.adoc` file as the first patch. It leaves the BUGS section unchanged, correctly focusing only on usage examples rather than experimental feature caveats.

Technical details for future reference:
- Modifies `Documentation/git-worktree.adoc`
- Adds new example section after existing examples
- Shows bare repository setup pattern
- Demonstrates parallel branch worktrees
- Uses explicit `--git-dir` syntax
- Maintains existing file structure
- Does not yet address bare repository documentation theory

This appears ready for review alongside the first patch, completing the immediate documentation improvements while leaving room for future enhancements to the EXAMPLES section or cross-references to related commands.