This is the second documentation patch in a series improving `git worktree` guidance, following up on earlier discussion about worktree placement best practices. While the first patch focused on warning against nested worktrees, this one adds a concrete example showing the recommended "side by side" branch checkout pattern.

The patch demonstrates how to set up multiple worktrees for different branches in sibling directories, which avoids the pitfalls of nesting while maintaining clean separation. The example shows the full workflow from repository creation (using a bare clone) through adding two worktrees. This complements the earlier warning by showing what to do instead of nested worktrees.

The change is straightforward - just adding one new example to the EXAMPLES section of git-worktree.txt. It directly addresses the documentation gap identified in the thread about showing proper worktree layout patterns. The example uses realistic commands (including the `--bare` clone and explicit `--git-dir` specification) that match real-world usage.

As with the first patch, this maintains Git's existing behavior while improving user guidance. It represents the second half of the agreed-upon documentation improvements from the thread, focusing on the positive example" counterpart to the earlier "don't do this" warning. The patch appears well-formed and directly implements the discussed solution.