This is a technical follow-up in Adrian Ratiu's hook subsystem refactoring series, specifically addressing review feedback on patch 1's parallel process stdin handling. Junio Hamano acknowledges Patrick Steinhardt's detailed review points about type consistency (`size_t` iteration), comment style (preferring `/*` over `/**` for non-interface comments), and file descriptor handling (questioning the use of `0` versus `-1` for closed pipes). 

The discussion highlights two key implementation details needing attention in v2: the mismatch between documented callback return value semantics (`ret > 0` in docs vs `ret == 1` check in code) and proper signaling of EOF conditions. Junio agrees with Patrick's observations but leaves open the specific choice between using `-1` or another sentinel value for closed file descriptors, noting the importance of avoiding confusion with valid descriptors.

This email moves the thread forward by confirming several technical improvements needed for patch 1 while leaving one minor design question (FD sentinel value) for the author to resolve. The exchange demonstrates careful attention to low-level details in the parallel process infrastructure that underpins the larger hook refactoring effort.