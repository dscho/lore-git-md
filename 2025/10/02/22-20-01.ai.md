This is a feature patch finalizing atomic reference updates for `git replay`, addressing feedback from the mailing list discussion. The email focuses on correcting technical inaccuracies and implementing reviewer suggestions.

Key technical corrections:
- Acknowledges that `git update-ref --stdin` already provides atomicity, contrary to the original commit message claim
- Clarifies the actual benefits are pipeline elimination and better ergonomics rather than new atomic guarantees
- Plans to rewrite commit message per Elijah Newren's suggested structure

Implementation changes proposed:
- Dropping the `--allow-partial` option entirely due to lack of concrete use cases
- Simplifying to just `--output-commands` flag for the traditional pipeline behavior
- Extracting a helper function to handle ref updates/printing) to reduce code repetition
- Fixing long lines and deep indentation issues in the ref update logic

The patch maintains the core functionality where atomic ref updates are now the default, with `--output-commands` providing backward compatibility. The discussion reveals consensus has formed around this design, with remaining work being documentation and code organization improvements rather than fundamental changes.

For future AI reference: The changes affect `builtin/replay.c`'s ref handling, introducing new helper functions and transaction management. Test coverage in t3650 verifies both atomic and output-command behaviors, including bare repository scenarios. Documentation updates in `git-replay.adoc` reflect the new defaults and option changes.