Regression fix discussion for `git diff --quiet` behavior. Jeff King responds to Jake Zimmerman's report about `git diff --quiet HEAD` incorrectly producing output for staged files in v2.51.1, confirming it as a serious regression.

Jeff notes the test suite's gap in catching this issue, observing that while t4035 tests similar scenarios with `git-diff-tree`, it doesn't cover `git-diff` behavior. He references a patch from the original author (b55e6d36eb) that might coincidentally fix the issue, but expresses concern about whether it comprehensively addresses all dry_run cases.

The key technical discussion revolves around diff.c's handling of the `--quiet` flag, particularly the `diff_flush` function's interaction with `DIFF_FORMAT_NO_OUTPUT` and `diff_from_contents`. Jeff proposes a conservative solution: reverting a specific hunk from the original commit that would force output to /dev/null when in quiet mode, ensuring no spurious output while potentially sacrificing some optimization opportunities.

This moves the thread from problem identification to solution discussion, with Jeff weighing the tradeoffs between a targeted fix versus a more comprehensive approach that might catch other edge cases. The response indicates maintainer engagement with the issue and suggests the regression will be addressed, though the exact solution path remains open for discussion.