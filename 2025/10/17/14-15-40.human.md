Hook subsystem refactoring (part 6/10): This patch introduces configurability for child process output grouping in hooks, addressing a specific interaction issue with sideband reading. The change makes the previously hardcoded `ungroup=1` behavior optional through a new `ungroup` flag in `run_hooks_opt`, while maintaining the default ungrouped output for existing callers like `run_hooks()` and the `hook run` command.

The motivation stems from the discovery that sideband reading collated output from child hooks doesn't work with ungrouped output. The patch preserves backward compatibility by keeping ungrouped output as the default for all current hook invocations, adding comments noting this could be made configurable if future use cases require it.

Implementation touches the hook API surface (`hook.h`), core hook runner (`hook.c`), and two key call sites (`builtin/hook.c` and `commit.c`). The changes are straightforward - adding the flag definition and plumbing it through while documenting the existing behavior. This appears to be a well-scoped change that fits cleanly into the larger hook refactoring series, addressing a real technical constraint while maintaining the established patterns.