Documentation patch completing Julia Evans' series to clarify `git-reset` man page, specifically focusing on the pathspec-based reset behavior. This final patch addresses user confusion around terminology and workflow for resetting specific files.

The changes simplify and clarify the description of pathspec resets by: replacing "tree-ish" with "commit or tree", changing "hunks" to "changes" for interactive mode, and making it clearer that `git reset` only affects the index. The patch removes the indirect suggestion to chain `git reset` with `git restore`, instead directly recommending `git restore` when working directory updates are needed. Terminology is made more consistent with the rest of the series (11 insertions, 16 deletions).

This completes the series' goal of making `git reset` documentation more approachable while maintaining technical accuracy. The changes build on prior patches that reorganized content and clarified mode behaviors, now finalizing the pathspec-specific sections. The series appears well-executed, with clear user-driven motivation and careful attention to both technical correctness and readability.