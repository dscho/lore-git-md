This thread began as a bugfix series addressing inconsistent diff output in dry-run mode, following an incomplete fix in commit b55e6d36eb. The discussion settled on a two-phase solution: an urgent regression fix (already applied to 'maint') and a comprehensive dry-run behavior improvement (now in its fourth iteration and queued in 'seen'). The series has been thoroughly reviewed by Johannes Schindelin, Jeff King, and Junio Hamano, with all technical requirements met except for one final design question about whether metadata should be shown when using `-I".*"` in dry-run mode.

In a new development, Jeff King has identified an unrelated but long-standing issue where external diffs (via `GIT_EXTERNAL_DIFF`) don't properly respect the `--output` redirection, sending output to stdout instead of the specified file. This predates the current dry-run fixes and stems from `run_external_diff()` not handling the output file descriptor correctly. Jeff has proposed a straightforward fix that makes external diff behavior consistent with internal diff output redirection. While independent of the main thread's focus, this is a clear bug that should be addressed separately. The dry-run fixes can proceed without waiting for this, but both issues touch similar areas of the diff machinery.