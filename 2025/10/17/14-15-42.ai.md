Hook subsystem refactoring (parallel output control). This patch (v2 8/10) extends the hook API to support capturing output via a new `consume_sideband` callback in `struct run_hooks_opt`, enabling server-side hooks to relay output through sideband channels rather than writing directly to stderr. The change builds on the parallel process infrastructure from earlier in the series, exposing the existing `run_parallel_processes()` callback mechanism through the hook interface.

The implementation adds a single line to `hook.c` wiring the callback through to the parallel execution engine, plus documentation in `hook.h` explaining the new field's purpose and referencing test-run-command.c for implementation examples. This maintains consistency with the series' pattern of thin wrapper APIs over core run-command functionality.

Key technical points: The change enables output redirection without breaking existing hook behavior (stderr remains default when no callback is set), aligns with the series' streaming architecture, and supports the server-side use case mentioned in the commit message. The patch appears straightforward with no visible edge cases or compatibility concerns beyond those already noted in the thread regarding parallel execution model tradeoffs.

This continues the series' progression toward complete output handling flexibility, following earlier patches that established the parallel execution model (7/10) and output grouping controls (6/10). Reviewers have previously approved similar callback patterns in the infrastructure, suggesting this incremental change is likely uncontroversial. The patch maintains the series' focus on backward compatibility while expanding capability for server-side hook implementations.