Thread identity: Regression report for `git diff --quiet` behavior in v2.51.1, originating from Jake Zimmerman. Thread type: bug report.

Core problem: `git diff --quiet HEAD` incorrectly produces output for newly staged files in v2.51.1 when it should remain silent as documented. This violates the `--quiet` flag's explicit promise to "Disable all output of the program."

Current status: Regression confirmed via bisect pointing to commit b55e6d36eb ("diff: ensure consistent diff behavior with ignore options") as the root cause. No fix proposed yet, but the report clearly demonstrates the version-specific behavior change (silent in v2.51.0, verbose in v2.51.1).

Key technical details:
- Affects `builtin/diff.c` handling of `--quiet` flag
- Specifically manifests when comparing HEAD to index with newly staged files
- Expected silent behavior is documented in man pages
- Regression introduced in v2.51.1 by b55e6d36eb

Open questions: None yet raised - this is a fresh report. The next expected steps would be either:
1. Maintainer confirmation of the regression
2. A proposed fix from the reporter or another contributor
3. Discussion of whether the change should be considered intended behavior (though the report makes a strong case it is not)

Participant positions: Only Jake Zimmerman so far, who has provided thorough technical analysis including bisect results and version comparisons. The quality of the report suggests they are familiar with Git's behavior and debugging practices.

Related work: The offending commit b55e6d36eb was part of a series to make diff behavior more consistent with ignore options, suggesting this may have been an unintended side effect of those changes.