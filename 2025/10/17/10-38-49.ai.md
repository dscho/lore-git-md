Performance optimization for `git last-modified` command, continuing the thread about improving path-modification tracking efficiency. Toon Claes responds to Justin Tobler's code review questions about the bitmap-based approach, clarifying several implementation details:

1. Explains the core algorithm: paths are only passed to treesame parents via bitmaps, with merge commits properly handling path inheritance. Provides a concrete example showing how bitmaps track which paths need examination per commit.

2. Defends the dual `paths`/`all_paths` storage by noting the hashmap enables efficient reverse lookups from paths to their bitmap indices, while `all_paths` maintains stable ordering.

3. Justifies keeping bitmap initialization in `last_modified_run()` rather than init/release functions due to cleanup sequencing requirements.

4. Clarifies the PARENT1/PARENT2 flag usage (replacing SEEN/BOTTOM) and their role in commit processing, acknowledging the naming could be improved.

5. Confirms a segfault bug reported by Taylor Blau, referencing the proposed fix involving NULL oid handling in `mark_path()`.

The response focuses on architectural decisions rather than introducing new changes, suggesting the implementation is stabilizing. Key unresolved points from earlier reviews (bitmap lifecycle, flag usage clarity) remain unaddressed in this reply. The exchange demonstrates growing consensus on the bitmap approach's validity while fine-tuning details.