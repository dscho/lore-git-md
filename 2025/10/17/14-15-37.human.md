Hook subsystem refactoring (part 3/10): This patch modernizes the post-rewrite hook implementation in `sequencer.c` to use the new hook API rather than direct `run-command` calls. The change simplifies the code by eliminating manual child process setup and SIGPIPE handling, which are now managed internally by `hook_run_opt()`.

The patch introduces a `pipe_from_strbuf` callback that streams hook input via the new parallel processing infrastructure from earlier patches. This maintains the same functionality - piping old/new object IDs to the hook - but with cleaner ownership semantics and error handling. The conversion demonstrates the series' goal of standardizing hook implementations while reducing boilerplate.

This appears to be a straightforward mechanical conversion that follows the pattern established in previous patches. The change is well-contained to `sequencer.c` and the commit message clearly explains both the technical change and its benefits. As part of a larger refactoring effort that's already received positive feedback, this patch seems uncontroversial and likely to be accepted.