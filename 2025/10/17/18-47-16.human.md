Refactoring follow-up in the `strbuf_split_max()` to `string_list_split()` conversion series. Junio Hamano responds to Christian Couder's analysis of SSH key fingerprint parsing, pushing back against tightening validation rules in this patch.

The discussion centers on whether to strictly require exactly 3 tokens (key size, fingerprint hash, and comment) when parsing SSH key fingerprints, as Christian suggested might be reasonable given typical `ssh-keygen` output. Junio argues this would be an inappropriate behavior change for a refactoring patch, maintaining that the original lenient check (`> 1`) should be preserved since the code only needs the second element (the fingerprint hash).

Junio emphasizes this should remain purely a mechanical API conversion, with any validation tightening deferred to a separate follow-up patch. This maintains the series' focus on replacing `strbuf_split_max()` with `string_list_split()` while preserving existing behavior. The exchange shows careful consideration being given to behavior preservation in security-sensitive code, even during seemingly straightforward API migrations.

The response indicates the v3 patches will likely keep the original validation logic, though Bello has committed to better documenting the rationale in the commit message. This thread demonstrates how even simple refactorings in security-adjacent code receive thorough scrutiny about behavior preservation.