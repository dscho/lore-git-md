This is a feature proposal for `git grep` behavior modification. The email suggests making binary file ignoring the default behavior for `git grep`, with a configuration option to control it. 

The proposal comes from Eloy, who notes that while `git grep -I` exists to ignore binary files, there's currently no way to make this the default behavior. The suggested implementation would:
1. Add a `grep.ignoreBinary` config option defaulting to false (maintaining current behavior)
2. Potentially add a `--include-binary` flag (though notes `-a/--text` might already serve this purpose)
3. Suggests making ignore-binary the default in Git 3.0 as a "better default"

The email is relatively brief and doesn't provide detailed rationale beyond personal preference ("it works great"). There's no discussion of potential backward compatibility concerns or analysis of how often users might want to search binary files. The author offers to implement the change if the idea is accepted.

Key technical points for future reference:
- Would modify `grep.c` to handle new config/flag
- Default behavior change would be significant for Git 3.0
- Relationship to existing `-I` and `-a` flags needs clarification
- No test changes or implementation details provided yet

The proposal seems straightforward but may need more justification about why the default should change, especially since binary file handling can be important for some workflows (e.g., searching minified JS or compiled binaries). The version number bump suggestion (3.0) implies this might be considered a breaking change.