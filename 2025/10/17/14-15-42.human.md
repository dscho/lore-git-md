This is part 8/10 of the hook subsystem refactoring series, focusing on output capture for server-side hooks. The patch adds a `consume_sideband` callback to the `run_hooks_opt` struct, allowing hook output to be captured and relayed (e.g., over a network connection) rather than always printing to stderr. This is particularly useful for server-side hooks where output needs to be transmitted to clients.

The change is minimal (just 9 lines added across two files) but enables significant functionality. The implementation builds on the parallel process infrastructure from earlier in the series, passing the callback through to `run_parallel_processes()`. The commit message notes this is needed for sideband communication, though the actual use case isn't detailed beyond that.

This appears to be a straightforward plumbing change that enables more flexible hook output handling. It follows the pattern established in previous patches of the series, maintaining consistency with the callback-based API approach. The change is well-contained and includes documentation in the header file pointing to test examples. As part of the larger hook refactoring effort, this helps move toward the goal of more robust and flexible hook handling.