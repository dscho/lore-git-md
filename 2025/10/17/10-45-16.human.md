This is a follow-up discussion in the performance optimization thread for `git last-modified`, addressing edge cases in the command's behavior. Toon Claes responds to D. Ben Knoble's observation about the current implementation producing inconsistent results compared to `git log --max-count=1` for certain maintenance branch merges.

The key point here clarifies a specific correctness issue where the old implementation showed different commit hashes for `pkt-line.h` when run with versus without path arguments (15df15fe vs 5b49c1af). While the exact reason for this discrepancy isn't fully understood, Claes confirms the new algorithm in the patch resolves this inconsistency as a beneficial side effect.

The exchange highlights how the thread has evolved from pure performance optimization to also ensuring behavioral correctness, with the new implementation now matching `git log`'s output as the reference standard. This response moves the discussion forward by acknowledging the edge case while showing the patch's broader improvements beyond just speed.