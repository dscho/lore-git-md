Documentation patch continuing Julia Evans' series to improve `git-reset` man page clarity, focusing on the introductory DESCRIPTION section.

This second installment addresses specific user-reported confusion points by rewriting the opening description to be more approachable while maintaining technical accuracy. The changes focus on three key areas: terminology simplification, action clarity, and use case orientation. The patch removes potentially confusing terms like "tree-ish" and "entries" from the intro, instead explaining the command's behavior in terms of what HEAD points to. It also splits the explanation into two clear use cases - changing HEAD to undo operations versus updating staged files - which helps users understand when each form applies.

The technical changes are minimal (8 insertions, 5 deletions) but strategically targeted. The new text explicitly calls out `git reset`'s dual nature as either a commit-pointer updater or a staging-area modifier based on arguments, which was previously inferred from the command forms. This matches the series' goal of making common patterns more discoverable while keeping all technical details available later in the document. The patch maintains the same structure as the first in the series, continuing the documentation reorganization without adding new content sections.

Key terms introduced/modified: replaces "Reset current HEAD" with "Set HEAD to point at", clarifies "mode" parameter context, explicitly mentions undo operations (commit/merge/rebase/pull) as primary use case. The changes align with the series' reference to prior `git checkout` documentation improvements, applying similar user-centric principles to `git reset`.