Performance optimization follow-up for `git last-modified` algorithm. Taylor Blau responds to Jeff King's analysis of a NULL oid segfault in the path-marking logic, providing historical context from GitHub's original implementation. The discussion reveals this wasn't an extraction error from the battle-tested version but rather a check added later in GitHub's "--go-faster" mode that wasn't properly carried over.

Key technical points clarified:
- The NULL oid check was introduced in GitHub's version specifically for the go-faster mode
- Original blame-tree.c implementation handled TREESAME cases differently without bitmap optimizations
- Memory/performance tradeoffs between bitmaps and char arrays are revisited
- Potential optimization identified for passing unchanged bitmaps to parents in TREESAME cases

Taylor provides a concrete diff suggestion for optimizing the TREESAME case by directly OR'ing bitmaps when no diffs are queued. The thread maintains focus on matching `git log --max-count=1` behavior as the correctness benchmark while exploring these implementation refinements.

The exchange demonstrates careful attention to historical context and implementation details, with both contributors cross-referencing multiple versions of the code (GitHub's original, Taylor's fork, and Toon's current patches). A minor double-free issue in Taylor's blame-tree.c is also noted, stemming from rebase conflicts with recent diff queue management changes.