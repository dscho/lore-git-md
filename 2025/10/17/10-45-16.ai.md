Performance optimization for `git last-modified` command, addressing correctness issues with maintenance branch merges.

This email continues the discussion about the `git last-modified` performance improvements, specifically focusing on a correctness issue raised by D. Ben Knoble. The original implementation produced different results between `git last-modified -- pkt-line.h` and `git last-modified | grep pkt-line.h`, with the latter incorrectly showing an older maintenance branch merge (5b49c1af) instead of the actual most recent change (15df15fe).

Toon Claes acknowledges the observation about the maintenance branch merge case, noting that while the topological ordering considers it earlier, the commit date is actually more recent. The new implementation in the patch series happens to fix this discrepancy as a side effect, though the exact mechanism isn't explained here. The thread shows consensus that matching `git log --max-count=1` behavior should be the correctness benchmark.

The email also contains a minor wording discussion about describing the improvements as "more correct" versus "more accurate", showing the careful attention to technical communication in the thread. This comes amidst broader technical discussions about bitmap implementations, flag usage, and performance tradeoffs covered in the thread summary.