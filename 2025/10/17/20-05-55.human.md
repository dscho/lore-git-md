This is the first patch in a documentation series improving the `git-reset` man page, focusing on reorganizing content for better usability.

Julia Evans reorders the command forms in the SYNOPSIS and DESCRIPTION sections to prioritize the most commonly used `git reset [mode]` pattern (like `--hard`/`--soft`), moving it from last to first position. The change is motivated by user feedback showing this is the primary way people interact with `git reset`, with one user noting they've never used other forms. The patch maintains all technical content but restructures it to surface the important patterns first, while noting that path-based resets can also be done via `git restore`.

The 58-line diff shows significant reorganization (29 lines added/removed) while preserving all information. This appears to be a well-considered change that directly addresses real documentation pain points, following a similar approach previously used for `git checkout` improvements. The patch is cleanly focused on reordering without introducing new content - subsequent patches in the series will presumably build on this foundation.