Refactoring patch in the hook subsystem series (6/10) that introduces configurable process output grouping behavior. This change allows callers to override the previously hardcoded ungroup=1 setting in `run_hooks_opt()`, addressing a compatibility issue with sideband reading which requires grouped output.

The patch adds a new `ungroup` bitfield to `struct run_hooks_opt` while maintaining the default ungrouped behavior for existing APIs (`run_hooks()` and `hook run` command). The implementation includes explicit comments in `builtin/hook.c`, `commit.c`, and `hook.c` noting that their current use cases require ungrouped output, with suggestions for future modification if needed.

Key technical changes:
- New `ungroup` flag in `hook.h`'s `run_hooks_opt` struct
- Modified `run_hooks_opt()` to respect the option
- Preserved existing behavior via explicit `opt.ungroup = 1` in legacy paths
- No test changes included (continues to rely on existing hook tests)

This appears to be a straightforward extension of the hook API to support additional use cases while maintaining backward compatibility. The patch follows the series' pattern of incremental improvements to the hook infrastructure, though reviewers may want to verify the sideband reading use case that motivated the change.