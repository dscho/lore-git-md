Updated AI thread summary:

Bug report and documentation discussion regarding `protocol.file.allow` configuration behavior in submodule operations, with focus on inheritance limitations between local and global scopes. Original issue identified that setting `protocol.file.allow=always` locally fails to enable file protocol access for `git submodule add` and `git submodule update`, while working globally. Jeff King confirmed this as a known limitation where `git-clone` doesn't inherit parent repository's local config during submodule creation, providing workarounds (global config or `-c` flag). The follow-up from fence.borrowing375@passmail.net extends the discussion to documentation gaps, noting the git-submodule manpage fails to clearly state local config is generally ignored for most submodule purposes, despite mentioning some exceptions (like default remote settings). The email raises two key technical questions: whether protocol-related config keys would be appropriate candidates for inheritance if implemented, and whether the documentation should be updated to explicitly warn about this limitation. The thread maintains its focus on the architectural tension between user expectations of config inheritance and Git's current implementation constraints, with the latest contribution shifting emphasis toward improving documentation clarity even if the technical behavior remains unchanged. Technical details confirm the behavior stems from `git-clone` not seeing parent repo local config during submodule creation, with no immediate plans to change this architecture. The discussion now includes both the original workarounds (global config, `-c` flag) and potential documentation improvements to manage user expectations.