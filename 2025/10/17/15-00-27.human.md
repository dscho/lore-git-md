This is a feature proposal to change `git grep`'s default behavior regarding binary files. El_Hoy suggests making `git grep` ignore binary files by default (matching the current `-I` flag behavior), with configuration options to control this.

The proposal outlines a straightforward implementation path: adding a `grep.ignoreBinary` config option (defaulting to false initially for compatibility) and considering whether to reuse the existing `-a/--text` flag or add a new `--include-binary` flag to override the default. The author also floats the idea of changing the default behavior in Git 3.0.

The email is well-structured and clearly explains the motivation (avoiding binary file matches by default) while considering backward compatibility. The author offers to implement the change if the idea is accepted. This seems like a reasonable quality-of-life improvement, though the suggestion to change the default in Git 3.0 might warrant discussion about compatibility expectations.