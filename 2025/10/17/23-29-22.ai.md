This is a follow-up in the rejected `git grep` binary file handling discussion. Thomas Braun responds to Junio C Hamano's opposition to Eloy's proposal for a `grep.ignoreBinary` config option by suggesting a compromise: keep the default behavior unchanged but still add the config option for user convenience. 

Braun shares his personal workflow pain point - frequently needing to manually specify `-I` when grepping, despite having an alias - and questions the actual use cases for grepping binary files. The email doesn't introduce new technical implementation details but rather presents a user experience perspective on why the config option might still be valuable even without changing defaults.

The response doesn't directly address Junio's core objections about script compatibility and GNU grep precedent, instead focusing on individual user convenience. This keeps the thread technically alive but doesn't appear to shift the fundamental disagreement - the maintainer had rejected the very premise of adding configuration for this behavior rather than just the default change aspect.

Key elements for future reference: the proposed `grep.ignoreBinary` config name remains central, the `-I` vs `-a` flag distinction is still relevant, and the discussion now includes personal workflow arguments alongside the original technical/ecosystem concerns. No new implementation details or patches are offered in this message.