This is a technical discussion in the ongoing refactoring series converting `strbuf_split_max()` to `string_list_split()` in GPG/SSH key handling code. Christian Couder responds to Junio Hamano's question about whether the patch's stricter validation (requiring exactly 3 split tokens) matches the original code's more lenient behavior (accepting 2-3 tokens).

Christian analyzes the expected `ssh-keygen -l` output format (key size, fingerprint hash, optional comment) and concludes both approaches are reasonable: either maintain the original lenient check (`> 1`) or enforce the stricter 3-token requirement, since even keys without comments should produce 3 tokens. He notes the commit message should better explain this validation logic.

The discussion highlights a key decision point in this security-adjacent code - whether to tighten input validation during the API conversion. Christian provides useful context about SSH key format while remaining neutral on which approach to take, leaving that to the original author to address in a v3. This keeps the thread focused on the technical merits of validation strictness during API modernization.