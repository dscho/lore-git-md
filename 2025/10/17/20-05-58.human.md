Documentation patch completing Julia Evans' series to clarify `git-reset` man page. This final patch focuses on the path-specific reset behavior, addressing user confusion points from direct feedback.

The changes simplify terminology, replacing "tree-ish" with "commit or tree" and "hunks" with "changes" for better readability. It makes clearer that `git reset <pathspec>` only affects the index (not working tree) and removes the indirect suggestion to chain it with `git restore`, instead recommending `git restore` directly when working tree updates are needed. The interactive mode section (`-p/--patch`) gets similar simplification while maintaining all technical accuracy.

This concludes the 4-part series that systematically improved the reset documentation's organization and clarity. The changes appear well-considered, with each modification directly addressing specific user-reported pain points while preserving the command's technical precision. The series continues Evans' pattern of successful documentation improvements (following her prior work on `git checkout` docs) by making complex concepts more accessible without sacrificing correctness.