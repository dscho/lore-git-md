Here's a human-readable summary of the thread so far:

Eloy has proposed changing `git grep`'s default behavior to ignore binary files, similar to how `git grep -I` works today. Currently, users must explicitly pass `-I` to skip binary files, but the proposal suggests making this the default while adding a `grep.ignoreBinary` configuration option to control the behavior. The change would maintain backward compatibility by defaulting the option to false initially, with a suggestion to flip the default in Git 3.0.

The proposal is light on implementation details but mentions the change would involve modifying `grep.c` and potentially adding a `--include-binary` flag (though it notes `-a/--text` might already serve this purpose). No test changes or deeper analysis of the impact are provided yet. The rationale given is primarily that the author finds the behavior "works great" for their use case, without addressing potential drawbacks or other workflows that might rely on the current behavior.

As this is the first message in the thread, there are no responses yet evaluating the idea or questioning the implications of changing such a long-standing default. The suggestion to tie the default flip to Git 3.0 implies this might be considered a breaking change worth bundling with other significant updates.