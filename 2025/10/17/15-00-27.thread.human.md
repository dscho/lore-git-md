Here's the human-readable thread summary:

The thread discusses a proposal to make `git grep` ignore binary files by default, with Eloy suggesting a `grep.ignoreBinary` config option that could eventually default to true in Git 3.0. The initial proposal noted this would require modifying `grep.c` while keeping the existing `-I` flag, but lacked analysis of backward compatibility impacts.

Junio C Hamano, the Git maintainer, has now responded with strong objections to the core idea. He argues that the current behavior (with explicit `-I` and `-a` flags) has worked well for nearly two decades, matches GNU grep's approach, and that changing defaults would break existing scripts. His response focuses on preserving compatibility rather than debating implementation details, effectively rejecting the proposal unless significant new justification emerges.

Given Junio's maintainer role and the weight of his arguments about backward compatibility, the proposal appears unlikely to move forward in its current form. Unless Eloy or others can present compelling evidence of widespread need for this change or address the compatibility concerns, this discussion may conclude with the status quo remaining unchanged.