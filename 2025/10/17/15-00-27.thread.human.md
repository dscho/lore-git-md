The thread continues to discuss whether `git grep` should ignore binary files by default, with the conversation now shifting toward broader file filtering mechanisms. The original proposal by Eloy suggested a `grep.ignoreBinary` config option, but Junio C Hamano and others raised concerns about script breakage and compatibility with GNU grep's behavior. Jeff King (Peff) then proposed a more flexible attribute-based exclusion system that could handle binary files as one of many possible filters, though this would require more implementation work.

Recent contributions from Brian m. carlson and now Randall S. Becker have reinforced the case against changing defaults, citing real-world use cases where searching binary or "quasi-binary" files (like SQL dumps, PDFs, PostScript, and XMP metadata) is necessary. Becker's examples from SQL environments add weight to the argument that the current behavior serves legitimate needs. While the discussion remains unresolved, the momentum appears to be moving away from a binary-specific default change and toward either maintaining the status quo or exploring Peff's more general attribute-based filtering approach. The core tension remains between user experience (skipping noisy binary matches) and backward compatibility (preserving existing workflows).