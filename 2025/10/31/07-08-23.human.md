This is a final polish suggestion in the atomic ref updates series for `git replay`. Christian Couder provides two small naming consistency improvements for the `get_ref_action_mode()` function parameters, suggesting `ref_action` instead of `ref_action_str` to match the surrounding codebase conventions. He also notes that the `parse_ref_action_mode()` helper could have been introduced in the previous commit for better incremental development.

These are minor style tweaks to an otherwise complete and reviewed series that has already addressed all technical feedback. The suggestions align with Git's codebase conventions and don't affect functionality. The thread context shows this is the final round of review before merging, with all substantive issues already resolved.