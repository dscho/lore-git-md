This is the final patch in a series implementing atomic reference updates for `git replay`. It adds a `replay.refAction` config option to control the default behavior, complementing the existing `--ref-action` command-line flag. The config accepts "update" (default atomic behavior) or "print" (pipeline output mode), following Git's standard precedence where command-line options override config settings.

Elijah Newren provides final review feedback, noting that the commit message's implementation details and examples sections don't add much value beyond restating what's obvious from the code. He also points out that some test improvements (like better state cleanup) should have been in earlier patches. The patch itself is technically sound and follows Git conventions, with comprehensive tests covering config parsing, defaults, overrides, and error handling.

The series has already received thorough review from multiple maintainers and is merge-ready. This final patch completes the interface by adding persistent configuration support while maintaining the command-line override capability. The implementation uses standard Git patterns for config handling and error reporting.