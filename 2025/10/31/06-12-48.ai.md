This is a refactoring patch in Patrick Steinhardt's ongoing ODB abstraction series (12/13). The change renames `write_object_file()` to `odb_loose_source_write_object()` to align with the established naming convention for functions operating on loose object sources. The patch updates the function declaration in object-file.h, its definition in object-file.c, and its single caller in odb.c.

The rename follows the pattern seen in previous patches of this series, where functions are being moved from generic object-file operations to explicitly ODB-aware implementations. The function maintains identical behavior while getting a more precise name that reflects its role in the new architecture. This continues the series' consistent approach to naming functions after their specific ODB source type (`odb_loose_source_*` for loose objects).

The change is straightforward with no functional modifications, just the rename and corresponding updates to the single caller. This appears to be part of the final cleanup phase of the series, where remaining functions are being brought into alignment with the new naming scheme before the ODB interface standardization is complete.