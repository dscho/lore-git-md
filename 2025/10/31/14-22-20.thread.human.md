This thread introduces a new 'is-needed' subcommand for `git maintenance` that checks whether tasks are required without performing them. Karthik Nayak's 5-patch series has evolved through review and is now technically complete, with the last open question resolved. The series refactors reftable stack compaction logic, introduces infrastructure for checking when optimization is needed, and integrates this with the maintenance command.

The implementation provides both strict checking and heuristic-based approaches for determining when ref compaction is required. Patrick Steinhardt and Justin Tobler have reviewed the series, with all major technical points addressed. The final discussion centered on naming a parameter in the reftable API - Junio suggested using "geometric" rather than the more generic "heuristics" since the current implementation only uses geometric progression for compaction decisions. Patrick agreed with the more precise terminology, resolving the last open question. With this naming clarification, the series is ready for integration into the next release window. The work builds on in-flight topics that should be resolved before merging.