This is a small refactoring patch in the final stages of the atomic ref updates series for `git replay`. 

The patch changes how `git replay` validates incompatible options (`--advance` and `--contained`) to use the standard `die_for_incompatible_opt2()` helper function instead of a custom error message. This brings the command's option validation in line with Git's established patterns and prepares for adding the new `--ref-action` option in a subsequent commit.

Elijah Newren, one of the series' key reviewers, approves the change with a simple "looks good" acknowledgement. The patch is straightforward and uncontroversial, representing a minor cleanup as part of the larger effort to make `git replay`'s reference updates atomic by default.

Since this is part of a fully reviewed and approved series that has already addressed all technical feedback, this change can be considered merge-ready along with the rest of the patches. The thread context shows this is one of the final refinements before the series is ready for inclusion.