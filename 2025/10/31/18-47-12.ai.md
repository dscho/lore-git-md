This is a preparatory refactoring patch in the atomic ref updates series for `git replay`. The patch standardizes option validation in `builtin/replay.c` by replacing a custom error check with the standard `die_for_incompatible_opt2()` helper function. This change makes the code more consistent with Git's established patterns for handling mutually exclusive options.

The patch modifies the validation for the existing `--advance` and `--contained` options, which cannot be used together. The new implementation uses Git's standard helper function that provides consistent error messaging across commands. This refactoring is done in preparation for adding the new `--ref-action` option in subsequent patches, which will also need to perform similar option validation.

The change is minimal (3 lines modified) and focused solely on the validation logic, keeping the actual behavior identical while improving maintainability. The commit message clearly explains the motivation and how this fits into the larger series. This appears to be a straightforward, well-justified refactoring that follows Git's established patterns.