Bugfix for debug ref backend's OID handling in `read_raw_ref`.

Patrick Steinhardt responds to Xinyu Ruan's patch fixing an issue in the debug ref backend where `read_raw_ref` was incorrectly clearing OIDs before delegation. While acknowledging the problem, Patrick questions two aspects of the explanation: (1) why clearing the OID matters if the underlying `read_raw_ref` would overwrite it anyway, and (2) what the original rationale was for clearing the OID in the first place, suggesting this belongs in the commit message.

Patrick also identifies a testing gap, noting the lack of any test coverage for `GIT_TRACE_REFS` functionality. He proposes creating a new test file `t0620-ref-debug.sh` to exercise the debug backend's callbacks, suggesting this bugfix could serve as the impetus for establishing that test infrastructure.

The response keeps focus on the debug ref backend implementation in `refs/debug.c` while raising important questions about both the immediate fix and broader test coverage needs. This continues the thread's technical discussion while potentially expanding its scope to include test development. The core issue remains properly propagating reference data through the debug wrapper without premature OID clearing.