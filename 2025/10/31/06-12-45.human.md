Refactoring patch in Patrick Steinhardt's ongoing ODB abstraction series. This is patch 9/13 in v2, continuing the effort to move loose object handling into the object database layer.

The patch refactors how loose objects are read, shifting responsibility from repository-level functions to the ODB layer. Key changes:
- Replaces `loose_object_info()` and `map_loose_object()` with new ODB-source-specific variants
- Moves loose object lookup logic into the ODB source structure
- Simplifies the code by removing repository-level iteration (now handled at ODB level)
- Keeps `open_istream_loose()` at repository level temporarily (marked for future cleanup)

The changes maintain existing behavior while better encapsulating loose object handling within the ODB abstraction. This continues the series' pattern of focused, incremental improvements to the object storage architecture. The patch appears well-constructed with thorough updates across multiple files to maintain consistency.