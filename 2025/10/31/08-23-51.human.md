This is a lighthearted but informative response in the ongoing discussion about deprecating `git whatchanged`. Kristoffer Haugsbakk acknowledges the deprecation proposal while providing a practical migration path for users who still rely on the command.

The email explains that `git whatchanged` is redundant with `git log` (using the same backend but with different defaults) and offers specific replacement syntax. The suggested equivalent is `git log <opts> --no-merges --raw`, with additional recommendations to use `--stat` or `--name-only` for human-readable output rather than machine parsing.

While the thread began with a humorous "fatal:" joke about a fictional `--i-still-use-this` option, this response takes a more practical approach by addressing the actual technical transition. The email serves as both a confirmation that some users still use `git whatchanged` and a helpful guide for migrating away from it, suggesting the author sees value in making the deprecation as smooth as possible for existing users.