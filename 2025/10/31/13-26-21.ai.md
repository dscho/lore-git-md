This is a bugfix patch for the Git-SVN bridge, specifically addressing path handling in branch operations. The patch modifies `perl/Git/SVN/Fetcher.pm` to properly strip branch location prefixes from paths before checking them with ignore/include regexps. Currently, when testing paths against these regexps, the full path relative to the SVN repository root is used, which includes unwanted branch location prefixes. The fix adds logic to strip these prefixes using the existing `path_strip` field and removes any leading slashes from the resulting path.

The change is small (4 lines added) and focused, modifying the `is_path_ignored` subroutine to handle path normalization before regexp matching. This appears to be a straightforward correction of a clear edge case in path processing, though the commit message could benefit from a more detailed explanation of the problematic scenario (what exactly goes wrong without this fix) and perhaps a test case demonstrating the issue. The patch targets a specific component of the SVN bridge's Perl implementation, suggesting the author has domain knowledge of this subsystem.