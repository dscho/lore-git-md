Thread identity: RFC patch series introducing Rust infrastructure to Git, part 4/14, authored by Ezekiel Newren. Focus is on adding foundational Rust types that mirror Git's C internals.

Core problem/goal: Establish basic Rust equivalents of Git's core data structures, starting with object identifiers (`struct object_id` in C). This patch introduces `ObjectID` in Rust as the first building block for future Rust-Git interoperability.

Current status: Series at v1 (based on message numbering). This patch adds `src/hash.rs` with the `ObjectID` struct definition and associated constants. Build system updated to compile the new module. No reviews or feedback on this specific patch yet.

Key technical details:
- New `ObjectID` struct in `src/hash.rs` marked with `#[repr(C)]` for C compatibility
- Contains two fields: `hash: [u8; 32]` (matching `GIT_MAX_RAWSZ`) and `algo: u32`
- Derives common traits: Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash
- Defines `GIT_MAX_RAWSZ` constant (32) for hash storage
- Proper licensing headers and module organization

Open questions/loose ends:
- No direct feedback yet on this specific patch
- Future patches will need to build on this foundation
- Platform compatibility concerns (particularly NonStop) may surface later

Participant positions:
- Ezekiel Newren driving the Rustification effort
- No other participants have commented on this patch yet

Related work:
- Part of larger 14-part series introducing Rust infrastructure
- Builds toward eventual Rust-Git interoperability
- Separate from gitoxide (Sebastian Thiel's independent Rust implementation)