This is the foundational patch in Patrick Steinhardt's v6 series introducing a new `git history` command, which aims to provide more intuitive history editing operations compared to interactive rebase. The patch establishes the basic command infrastructure without yet implementing any subcommands - those come in later patches.

The commit adds the new `builtin/history.c` with a minimal implementation that just handles basic argument parsing, plus all the necessary build system plumbing (Makefile, meson.build, command-list.txt updates). It also includes initial documentation in `git-history.adoc` that frames the command as an experimental alternative to `git rebase` for common history editing tasks like reordering, splitting, dropping, and squashing commits. The documentation clearly marks this as experimental and notes current limitations around merge commits.

A simple test script verifies the basic command behavior - that it runs without arguments and rejects unknown ones. The implementation looks clean and follows Git's established patterns for new commands. This appears to be a solid foundation for the more complex history editing operations that will be built on top in subsequent patches.

As the first patch in a series that has already undergone several rounds of review, this primarily serves to establish the command framework before adding functionality. The thread context shows this is part of a well-considered effort to improve Git's history editing capabilities, with Junio Hamano and others already engaged in reviewing the approach.