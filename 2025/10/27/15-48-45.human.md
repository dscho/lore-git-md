This is a performance optimization follow-up in the `git last-modified` series. Toon Claes responds to Taylor Blau's feedback about potential alternative implementations for path storage in the new algorithm. 

The discussion centers on tradeoffs between different data structures - hashmap vs strvec vs string_list - for tracking paths during the modified-file detection process. Toon had considered using a hashmap-per-commit approach that would avoid duplicating path storage, but benchmarked it as 30% slower than the current v3 implementation. He also notes this approach would complicate memory management.

The thread has already settled on the core algorithm using generation-sorted commits and EWAH bitmaps, with v3 addressing all prior technical concerns. This email confirms the current implementation's design choices after exploring alternatives, suggesting the patch series is nearing final form with performance characteristics well-understood. The exchange demonstrates careful optimization work where even small structural changes get measured before being accepted or rejected.