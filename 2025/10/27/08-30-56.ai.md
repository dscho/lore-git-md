This is a refactoring patch in the geometric repacking maintenance task series, specifically improving code readability of maintenance strategy definitions. The patch reorganizes the strategy initialization in `builtin/gc.c` to use nested designated initializers, making it clearer which fields belong to which maintenance tasks.

The change transforms flat initializations like `[TASK_COMMIT_GRAPH].enabled = 1` into nested blocks with explicit task scoping. This improves visual parsing of the code by grouping related configuration together under each task identifier. The behavior remains identical - this is purely a code organization improvement suggested by Taylor Blau during earlier review.

Key technical points:
- Modifies strategy definitions for `none_strategy`, `default_strategy`, and `incremental_strategy`
- Maintains all existing task enablement and scheduling (hourly commit-graph/prefetch, daily incremental-repack/loose-objects, weekly pack-refs)
- Preserves the same underlying `struct maintenance_strategy` layout
- No functional changes to the maintenance task execution logic
- Part of broader series refactoring maintenance infrastructure

The patch follows the series' pattern of incremental improvements while maintaining all existing functionality. It addresses a specific code organization concern raised during review without introducing new behavior or configuration options.