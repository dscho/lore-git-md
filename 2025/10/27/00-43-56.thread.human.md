This thread continues the Rust integration effort in Git, focusing on hash algorithm interoperability between C and Rust code. The latest patch introduces a helper function `hash_algo_ptr_by_offset()` to allow Rust code to safely access C's `git_hash_algo` structs when needed for FFI calls. The implementation adds the C function to hash.c/hash.h and provides a Rust wrapper method `hash_algo_ptr()` on the `HashAlgorithm` enum.

The change maintains the existing hash algorithm numbering (SHA1=0, SHA256=1) and follows established patterns for Rust-C interop in the codebase. While touching core hash infrastructure, the patch is carefully scoped to avoid impacting existing behavior. The implementation appears straightforward, with clear motivation explained in the commit message about Rust's need to pass hash algorithm information to C functions. This represents another incremental step in the broader Rustification effort.