Git v2.51.2 maintenance release announcement and subsequent NonStop platform bug reports. Fixes included: `git diff --quiet -w` behavior (Jeff King), `--no-index` trailing slash handling (Jacob Keller), `git status --short -z` coloring consistency, Windows CI fixes (Johannes Schindelin), OpenSSH test workarounds (Patrick Steinhardt), Unicode 17 updates, and GitHub Actions refreshes.

Randall Becker reported test failures on NonStop systems with OpenSSL 3.5, specifically in t7900 maintenance tests involving `test_subcommand !` assertions. The root cause was a divergence between Make's `/bin/bash` usage and test scripts' ksh invocation via `#!/bin/sh` on NonStop. Jeff King's `TEST_SHELL_PATH=/bin/bash` workaround resolved this by ensuring consistent bash usage, confirmed effective by Randall. This addressed the immediate issue but left open questions about other potential ksh-related problems in Make's `SHELL` settings.

Kristoffer Haugsbakk proposed introducing release candidates for maintenance releases to catch issues like the NonStop shell divergence earlier. Randall Becker countered this suggestion, arguing maintenance releases should focus strictly on fixes without introducing new functionality or dependencies that might break existing behavior, except for critical cases like CVEs requiring breaking changes. Junio Hamano has now weighed in, questioning the value of maintenance release candidates since 'master' already serves as a continuous test bed where changes must first pass through 'next'. He notes that NonStop-specific issues should ideally be caught during normal development on 'master' or 'next', and expresses skepticism that those not testing 'master' would test maintenance candidates either.

Technical context includes the t7900 test file, problematic `test_subcommand !` assertions, and shell selection mechanics in Make/Git's test harness. The thread has evolved from the resolved technical issue (NonStop shell divergence) to broader release engineering practices, with Randall advocating for stability in maintenance releases and Junio questioning the effectiveness of additional pre-release steps. Open questions remain about whether additional test infrastructure changes will be needed for other shell-related issues on NonStop systems, balancing portability concerns with maintainer workflow impacts. The discussion now includes both the specific resolved issue and the ongoing debate about maintenance release processes, with Junio's response providing the maintainer perspective on the proposed process change.