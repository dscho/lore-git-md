Git v2.51.2 maintenance release announcement and subsequent NonStop platform bug report. Fixes regressions and improves CI: `git diff --quiet -w` now correctly suppresses output for additions/deletions/conflicts (Jeff King), `--no-index` handles trailing slashes properly (Jacob Keller), and `git status --short -z` has consistent coloring. Windows CI disables interfering real-time monitoring (Johannes Schindelin), OpenSSH tests work around socket path length limits (Patrick Steinhardt). Includes Unicode 17 width table updates and GitHub Actions dependency refreshes. Contributions from core maintainers addressing distinct issues, typical for a maintenance release targeting specific regressions and test stability.

Randall Becker reports test failures on NonStop systems when building with OpenSSL 3.5, specifically in t7900 (maintenance tests) related to `test_subcommand` assertions. Failures occur when testing `git gc` invocations, with one case due to a missing argument in the test harness when checking for absence of an auto-gc command. The error suggests the test infrastructure may not properly handle negated assertions (`test_subcommand !`) on this platform. These issues were not present in v2.51.1, implying a possible regression, though the OpenSSL version change may be coincidental. The failure mode points to platform-specific shell behavior differences in bash 5.0.18 regarding test command parsing. This appears to be a test suite rather than core functionality issue, but one that could mask real problems if left unfixed. The report highlights ongoing platform compatibility challenges, particularly for NonStop systems where Randall Becker is the primary maintainer. No resolution or proposed fix yet; the thread awaits response from test infrastructure experts.