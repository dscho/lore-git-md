Git v2.51.2 maintenance release announcement and subsequent NonStop platform bug reports. Fixes regressions and improves CI: `git diff --quiet -w` now correctly suppresses output for additions/deletions/conflicts (Jeff King), `--no-index` handles trailing slashes properly (Jacob Keller), and `git status --short -z` has consistent coloring. Windows CI disables interfering real-time monitoring (Johannes Schindelin), OpenSSH tests work around socket path length limits (Patrick Steinhardt). Includes Unicode 17 width table updates and GitHub Actions dependency refreshes.

Randall Becker reports test failures on NonStop systems when building with OpenSSL 3.5, specifically in t7900 maintenance tests related to `test_subcommand` assertions. The issue manifests when testing `git gc` invocations, particularly with negated assertions (`test_subcommand !`), producing "test: argument expected" errors. SZEDER Gábor and Randall Becker are investigating unusual instrumentation output that shows file descriptor redirections without expected command traces, suggesting platform-specific behavior in how bash 5.0.18 processes test assertions on NonStop systems.

SZEDER Gábor provides detailed bash -x trace examples showing normal `test_subcommand` output patterns (with nested subshell indicators and command traces), contrasting these with Randall's output which lacks expected traces. The technical disagreement centers on whether the problematic behavior originates from bash or the test infrastructure - Randall maintains it comes from their NonStop bash 5.0.18 environment, while SZEDER's analysis suggests the output patterns don't match bash's typical format. The issue appears isolated to test suite behavior rather than core Git functionality, but could mask regressions if failures go undetected.

This highlights ongoing platform compatibility challenges for NonStop systems where Randall Becker is the primary maintainer. The discussion remains focused on diagnosing instrumentation differences, with SZEDER providing concrete examples of expected bash output to help identify the divergence point. No resolution yet proposed; the thread awaits further debugging information to determine whether changes are needed in the test harness or if this represents a platform-specific limitation in bash/test interaction.