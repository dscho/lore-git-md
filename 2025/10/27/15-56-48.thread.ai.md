Git v2.51.2 maintenance release announcement and subsequent NonStop platform bug reports. Fixes regressions and improves CI: `git diff --quiet -w` now correctly suppresses output for additions/deletions/conflicts (Jeff King), `--no-index` handles trailing slashes properly (Jacob Keller), and `git status --short -z` has consistent coloring. Windows CI disables interfering real-time monitoring (Johannes Schindelin), OpenSSH tests work around socket path length limits (Patrick Steinhardt). Includes Unicode 17 width table updates and GitHub Actions dependency refreshes.

Randall Becker reports test failures on NonStop systems when building with OpenSSL 3.5, specifically in t7900 maintenance tests related to `test_subcommand` assertions. The issue manifests when testing `git gc` invocations, particularly with negated assertions (`test_subcommand !`), producing "test: argument expected" errors. SZEDER Gábor and Randall Becker are investigating unusual instrumentation output that shows file descriptor redirections without expected command traces, suggesting platform-specific differences in how bash 5.0.18 processes test assertions on NonStop systems.

The disagreement centers on whether the instrumentation behavior originates from bash or the test infrastructure. Randall maintains the error comes from their NonStop bash 5.0.18 environment, while the output patterns suggest deeper interaction issues between the test harness and platform shell. The problem appears isolated to test suite behavior rather than core Git functionality, but could mask real regressions if failures go undetected. No resolution yet proposed; the discussion remains focused on diagnosing the instrumentation differences, with Randall offering additional debugging information. This highlights ongoing platform compatibility challenges, particularly for NonStop systems where Randall Becker is the primary maintainer. The thread awaits further input from test infrastructure experts to determine whether changes are needed in the test harness or if this represents a platform-specific limitation.