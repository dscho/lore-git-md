This thread introduces a Rust interoperability change that standardizes the `count` parameter type in the `hashwrite` function from `unsigned int` to `uint32_t`. The modification, which affects both the function declaration and implementation, is motivated by the need for consistent type sizes when calling from FFI contexts. The author notes this is a safe change since `unsigned int` is already 32 bits on all target platforms.

The patch appears to be a straightforward, mechanical change that maintains existing behavior while making the interface more precisely specified for cross-language use. As part of a larger Rust integration effort, this modification doesn't require test changes since it has no runtime impact on existing C callers. The change is minimal, touching just two lines in the header and implementation files to update the type signature.