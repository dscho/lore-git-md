This is part of the ongoing effort to expose Git's internals to Rust, specifically focusing on hash functionality. The patch moves several inline hash-related functions from `hash.h` to `hash.c` and makes them non-inline, while adding new allocation and initialization helpers. This allows Rust code to properly link against these functions while avoiding manual memory management of hash contexts.

The changes include new `git_hash_alloc` and `git_hash_free` functions for context lifecycle management, plus `git_hash_init` for initialization. The existing hash operations (`clone`, `update`, `final`, `final_oid`) are moved from inline to regular functions. This maintains the same functionality while making the code more accessible to Rust.

The patch appears well-motivated and cleanly implemented, with straightforward changes to both header and implementation files. It's part of a larger series (position 9/14) working on Rust interoperability, though the exact scope of the full series isn't visible from this single patch. The changes are focused and don't appear controversial, just exposing existing functionality in a more Rust-friendly way.