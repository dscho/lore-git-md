This is a follow-up discussion in the bugfix thread about Git's pattern matching behavior in `.gitignore` and `.gitattributes` files. Jeff King (peff) responds to Junio Hamano's concerns about potential correctness issues with the proposed "minusone" solution that preserves one character of context during pattern matching.

The key points in this email:
- Jeff addresses Junio's concern that stepping back one byte might cause incorrect matches, arguing that providing more context to `fnmatch()` should never make things worse
- He notes this is somewhat speculative ("mostly hand-waving") but believes the approach is sound
- Jeff mentions an unrelated backslash handling bug from the original report that isn't addressed by this fix
- The tone suggests the technical discussion is winding down, with no major objections remaining

This appears to be the final exchange before the patch series (which fixes the `foo**/bar` matching issue while preserving optimization benefits) would be ready for merging. The thread shows careful consideration of both performance and correctness tradeoffs, with benchmarking data helping validate the solution.