This is a refactoring patch that's part of a larger series introducing Rust interoperability. The change standardizes the `count` parameter type in the `hashwrite` function from `unsigned int` to `uint32_t` across both the declaration and implementation.

The motivation is explicitly stated as enabling easier calling from FFI (Foreign Function Interface) contexts by ensuring consistent type sizes. The author notes this is safe because `unsigned int` is already 32 bits on all target platforms. The change is minimal - just two lines in the header and implementation files to update the type signature.

This appears to be a straightforward, mechanical change with clear motivation tied to the series' Rust integration goals. The patch maintains the existing behavior while making the interface more precisely specified for cross-language use. No test changes are included since this is purely a type declaration change with no runtime impact on existing C callers.