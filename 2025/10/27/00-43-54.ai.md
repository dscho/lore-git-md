This is part 4 of a 14-part series introducing Rust infrastructure to Git. The patch adds a basic `ObjectID` struct to the Rust codebase, mirroring the C `struct object_id` functionality.

The new `ObjectID` struct in `src/hash.rs` contains two fields: a 32-byte array for the hash and a `algo` u32 field to track the hash algorithm. The struct is marked with `#[repr(C)]` for C compatibility and derives common traits (Debug, Clone, etc.). The patch also includes the necessary build system updates to compile the new module.

This appears to be foundational work for Rust-Git interoperability, establishing the basic object identifier type that will be needed for subsequent Rust implementations. The implementation is straightforward and matches the C counterpart exactly as described in the commit message. The patch is cleanly implemented with proper licensing headers and module organization.

For future reference in this thread: this introduces `ObjectID` in the `hash` module with fields `hash: [u8; 32]` and `algo: u32`, matching C's `object_id`. The series seems to be building up Rust infrastructure incrementally, with this being a core primitive type. The `GIT_MAX_RAWSZ` constant (32) is also defined here for hash storage.