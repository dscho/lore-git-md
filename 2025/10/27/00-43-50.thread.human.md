This thread continues the discussion of brian m. carlson's series introducing Rust code to enable SHA-1/SHA-256 repository interoperability. The latest email provides technical clarification about integer type choices at the Rust/C interface, confirming that while Rust supports 128-bit integers, their compatibility with C varies by compiler. This reinforces the series' earlier decision to use fixed-width 32-bit types (`uint32_t`/`u32`) for hash algorithm representation at the FFI boundary, a choice that ensures portability across architectures.

The thread has seen significant progress since the v2 series was posted, with key decisions now settled. Junio Hamano clarified that the build system's single-library approach was misrepresented as a Rust requirement when it was actually about general simplification. While philosophically disagreeing with this consolidation, Junio acknowledged it's too late to change course. The memory allocation strategy has also been resolved to use Git's `xmalloc` at the FFI boundary for consistency.

Open questions remain about documenting the object map format, finalizing the `Hasher` naming, and addressing platform support concerns (particularly for NonStop). The technical discussion has been thorough, with multiple contributors weighing in on FFI design and type safety. While no Reviewed-by tags have appeared yet, the series appears to be converging toward merge readiness as these final details are ironed out.