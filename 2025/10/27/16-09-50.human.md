Follow-up discussion on `git-repo-info` field printing optimization.

Eric Sunshine responds to Junio C Hamano's suggestion for optimizing the field printing code in the `git-repo-info` command. The discussion focuses on the first patch of the v3 series, which refactors field printing into a dedicated function.

Junio had proposed eliminating the temporary `strbuf` allocation entirely by using `quote_c_style()` with direct `FILE*` output, building on Eric's earlier suggestion to scope the `strbuf` more tightly. Eric agrees this is an improvement, noting it makes the code cleaner and more readable while avoiding allocation overhead.

This exchange represents the final refinement of the field printing implementation approach, with both reviewers now aligned on using direct output rather than intermediate buffers. The solution maintains the command's functionality while improving efficiency, in line with Git's performance-conscious coding standards.

The discussion of this technical detail suggests the patch series is nearing final approval, with reviewers focusing on small but meaningful optimizations rather than fundamental design issues.