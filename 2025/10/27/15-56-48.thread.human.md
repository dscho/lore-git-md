This thread discusses test failures on NonStop systems related to Git's maintenance release v2.51.2. The core issue involves `test_subcommand` assertions in the t7900 maintenance tests failing on NonStop platforms when using bash 5.0.18, particularly with negated assertions (`test_subcommand !`) that produce "test: argument expected" errors.

Randall Becker, the primary NonStop maintainer, initially reported the issue, noting unusual instrumentation output showing file descriptor redirections without the expected command traces. SZEDER Gábor has since joined the investigation, providing detailed bash -x trace examples to contrast normal behavior with Randall's output. The key technical disagreement centers on whether the problematic behavior originates from bash itself or Git's test infrastructure.

The discussion remains focused on diagnosing the platform-specific differences in how bash processes test assertions. While the issue appears limited to test suite behavior rather than core Git functionality, it could potentially mask real regressions if test failures go undetected. The thread currently awaits further analysis to determine whether changes are needed in Git's test harness or if this represents a NonStop-specific limitation. This case highlights the ongoing challenges of maintaining compatibility across Git's supported platforms, particularly for less common systems like NonStop where Randall Becker provides primary support.