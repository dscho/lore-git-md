This is a technical refinement to the `git history reword` subcommand implementation in Patrick Steinhardt's v5 patch series. The email responds to Junio Hamano's review of patch 6/12, addressing several key points:

1. **Documentation cleanup**: Patrick agrees to remove the command-less form from the synopsis (since it serves no useful purpose) and reorder the `--message` option before the commit argument in the documentation.

2. **Option deferral**: The `-m`/`--message` option will be dropped entirely from this iteration while the design around message handling is still being worked out.

3. **Implementation simplification**: The most significant change is recognizing that the current use of replay/pick machinery is unnecessarily complex for the reword operation. Patrick acknowledges this was a leftover from earlier iterations and commits to simplifying the implementation to directly create new commits with updated messages rather than going through the pick infrastructure.

The changes respond directly to Junio's technical feedback and represent a meaningful simplification of the implementation. This continues the pattern of the series maturing through review, with Patrick being responsive to both documentation and implementation concerns. The reword subcommand remains focused on its core purpose of safely rewriting commit messages without merge complications.