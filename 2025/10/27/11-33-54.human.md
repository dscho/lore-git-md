This is a refactoring patch in Patrick Steinhardt's `git-history` series, splitting the add-patch declarations into their own header file. 

The patch moves declarations related to `add-patch.c` from `add-interactive.h` into a new `add-patch.h` header, cleaning up the interface between these subsystems. The changes are straightforward - structs and functions specific to the patch functionality are relocated while maintaining all existing behavior. This follows good software engineering practice by creating clearer boundaries between components.

As part of the larger `git-history` effort, this refactoring helps prepare the infrastructure needed for the history editing commands. The patch appears well-executed, with proper header includes added and no functionality changes. It builds on previous work in the series while keeping the scope focused on code organization.

The change is relatively small (30 lines net) but meaningful for maintainability, making it easier to work with the patch-related code independently from the interactive add functionality. This kind of preparatory refactoring is typical in larger feature series like `git-history`.