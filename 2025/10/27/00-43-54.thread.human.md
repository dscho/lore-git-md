This thread introduces a 14-part series adding Rust infrastructure to Git, with this being part 4. The current patch establishes a foundational `ObjectID` struct in Rust that mirrors C's `struct object_id`, containing a 32-byte hash array and algorithm identifier. The implementation in `src/hash.rs` is carefully designed for C compatibility (using `#[repr(C)]`) while including standard Rust traits. 

The work appears methodical, with this core primitive type serving as building block for future Rust-Git interoperability. The patch includes all necessary build system updates and follows project conventions, suggesting it's part of a well-planned incremental approach to introducing Rust to the codebase. The `GIT_MAX_RAWSZ` constant (32) is also carried over from C here for consistent hash storage sizing.