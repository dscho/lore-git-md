This is a refactoring patch in the geometric repacking maintenance series (part 2/10). It makes the `too_many_loose_objects()` helper function more flexible by having it accept a simple integer parameter rather than requiring the full `gc_config` struct. 

The change is straightforward - the function only needed the `gc_auto_threshold` value from the config struct, and this modification allows future callers to pass their own threshold values directly. The patch updates all existing callers to pass the threshold value from their config structs.

This appears to be a clean, mechanical change that improves the function's reusability while maintaining identical behavior. It's part of the larger effort to make maintenance tasks more configurable, with this particular change likely paving the way for custom loose object thresholds in different maintenance contexts. The patch follows the series' pattern of careful refactoring to support the geometric repacking strategy work.