This is a code review discussion about implementation details in the `git-repo-info` refactoring patch. Junio C Hamano responds to Eric Sunshine's earlier suggestion about scoping a `strbuf` variable more tightly in the field printing logic.

The key technical points are:
- The discussion focuses on the `print_field()` function's handling of quoted output in `FORMAT_KEYVALUE` mode
- Eric had suggested limiting the `strbuf` scope to just the case branch that needs it
- Junio proposes an alternative approach using `quote_c_style()` with `FILE*` output to avoid allocation entirely
- The suggestion would eliminate the temporary `strbuf` by writing quoted output directly to stdout
- This would simplify the code while maintaining the same functionality
- The change would affect the first patch in the series (the refactoring commit)

The exchange shows the maintainer's preference for simpler, more direct solutions when possible, especially for straightforward output formatting cases. This continues the thread's pattern of refining implementation details while keeping the overall design intact.