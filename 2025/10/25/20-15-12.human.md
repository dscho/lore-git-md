This is a build system patch adding installation support for credential helper binaries. Thomas Uhle follows up on previous cleanup work by adding standard `install` targets to the Makefiles for both the libsecret` and `osxkeychain` credential helpers in the contrib directory.

The changes mirror the installation approach used in other contrib components, creating the target directory and installing the built binaries with proper permissions. This appears to be a straightforward quality-of-life improvement for packagers and system administrators who want to install these optional components.

The patch is small (12 lines added, 2 removed across two files) and clearly motivated as completing the standardization of these Makefiles after prior cleanup work. The changes look technically sound, following the pattern established in other parts of the contrib directory. This seems like a low-risk, maintainability-focused improvement that aligns with Git's build system conventions.