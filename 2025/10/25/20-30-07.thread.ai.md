Thread summary:

Patch series adding standard installation support for contrib/credential helpers (libsecret and osxkeychain). Version 2 addresses missing Signed-off-by in v1. Changes follow established contrib/ patterns:
- Adds `install` targets to both credential helper Makefiles
- Defines `INSTALL` variables matching core Git conventions
- Creates target directories with correct permissions
- Updates `.PHONY` declarations
- Builds on prior Makefile cleanup work (requested as separate patch)

Implementation appears correct and consistent with other contrib components. No outstanding technical concerns raised in thread. Ready for maintainer consideration.