Documentation clarification on `git log` format strings and placeholders. This is a reply in an ongoing thread about improving the documentation of `git log`'s pretty formats.

René Scharfe provides concrete answers to David Moberg's questions about format string equivalents and placeholders. The key technical details revealed are:
- The exact format strings for different `--oneline` variants (showing differences between terminal vs non-terminal output and `--oneline` vs `--format=oneline`)
- Clarification that `%s`, `%b`, and `%B` correspond to subject, body, and full commit message respectively
- Confirmation that inline notes display with newline stripping isn't currently possible

The email endorses David's documentation improvement suggestions, particularly:
- Showing format string equivalents for built-in formats
- Making descriptive terms like "title-line" more searchable
René also suggests an implementation approach where named formats would use the documented format strings rather than hand-coded implementations.

This moves the thread forward by providing authoritative technical details while keeping the documentation improvement requests open. The reply maintains the thread's focus on making format strings more discoverable and better documented, though it doesn't propose specific wording for the manpage yet.