Thread identity: Build system patch adding installation support for credential helpers (libsecret and osxkeychain), following up on prior Makefile cleanup work.

Core problem: The credential helper Makefiles in contrib/ lacked proper installation support, making deployment more manual than necessary. This completes their modernization to match standard Git build conventions.

Current status: This is a follow-up to prior Makefile cleanup work, implementing the installation target addition as a separate change per reviewer request. The patch is in its first version ([PATCH]) and has not yet been reviewed.

Key technical details:
- Adds standard `install` targets to contrib/credential/libsecret/Makefile and contrib/credential/osxkeychain/Makefile
- Uses `$(DESTDIR)` and `$(gitexecdir)` variables consistently
- Maintains existing build characteristics (pkg-config for libsecret, framework linkages for osxkeychain)
- Follows same installation pattern used in other contrib/ components
- Updates `.PHONY` lists appropriately

Open questions: None raised yet - this appears to complete the requested changes from prior review.

Participant positions: No reviews or acks yet - this is the initial submission of the installation support follow-up.

Related work: Builds on prior credential helper Makefile cleanup work in the same area.