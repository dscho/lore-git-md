Junio C Hamano replies to René Scharfe's refactoring patch for `git add -p`'s quit behavior. This is part of a two-patch series simplifying exit handling in the interactive patch interface.

Hamano acknowledges Scharfe's recent focus on improving `add -p` and expresses surprise that the redundant quit behavior (which unnecessarily marked undecided hunks as skipped) remained in the code for so long. He notes he originally wrote this code before its C port and approves the change, stating he will queue the patch.

The technical change separates the 'q' (quit) command handling from 'd' (skip) command handling in `add-patch.c`, making quit operations immediately exit without processing remaining hunks. This is a clean internal improvement that doesn't affect user-visible behavior. Hamano's response indicates this is a clear improvement that should be uncontroversial to merge.