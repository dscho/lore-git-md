This is a documentation formatting fix in the ongoing thread about Asciidoc rendering issues. Jean-Noël Avila responds to Jeff King's analysis of an indentation problem in the `refStorage` documentation, where a "Note that..." warning paragraph was being misaligned differently between Asciidoctor and asciidoc.py processors.

King had proposed a solution wrapping the included `ref-storage-format.adoc` content in explicit block markers (`--`) to maintain consistent indentation across both processors. Avila acknowledges the complexity, noting the difficulty of automated testing for such rendering edge cases. The change is narrowly focused on preserving the semantic hierarchy where the warning applies to the overall `refStorage` setting rather than individual bullet points.

This represents the final refinement in a thread that has already seen consensus on the core formatting fixes, with all key reviewers (Avila, King, and Junio C Hamano) aligned on the approach. The solution maintains documentation accuracy while working around processor-specific rendering quirks.