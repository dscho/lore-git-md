Thread identity: Patch simplifying submodule handling in `git cat-file --batch-check`, originating from b0b910e052 (submodule handling in batch mode) and 6a951937ae (OID-to-hex conversion in `report_object_status()`).

Core problem/goal: Remove redundant OID-to-hex conversion when processing submodules in `git cat-file --batch-check` by leveraging existing NULL-handling in `report_object_status()`.

Current status: Single patch version (no series), no reviews or acks yet. Change is minimal (1 line) and behavior-preserving.

Key technical details: Modifies `batch_object_write()` in `builtin/cat-file.c` to pass NULL instead of explicit hex OID when mode is S_IFGITLINK (submodule case). `report_object_status()` already handles NULL by converting the OID internally.

Open questions: None raised yet - straightforward cleanup with no apparent controversy.

Participant positions: No reviews or discussion yet. Patch author found this while merging v2.50.1 into GitHub's fork.

Related work: Builds on b0b910e052 (submodule handling) and 6a951937ae (NULL handling in OID conversion). No other in-flight topics mentioned.