This is a follow-up discussion about a const-correctness fix for the patch-id subsystem, where Junio Hamano identifies a problem with the proposed solution.

The original patch from Okhuomon Ajayi attempted to fix a const-correctness violation in `patch_id_neq()` by making the `diff_options` parameter const, but Junio points out this creates a new issue. The underlying problem is that while the hashmap API requires the `cmpfn_data` parameter to be const, the actual diff processing needs to modify the options structure (via `commit_patch_id()` -> `diffcore_std()`).

Junio notes two specific issues with the patch:
1. It introduces trailing whitespace that should be removed
2. More importantly, the const change breaks the actual functionality since the diff processing needs to modify the options structure

The discussion reveals this is more complex than initially thought - the const-correctness issue stems from a tension between the hashmap API's requirements and the diff subsystem's needs. Junio suggests this is what the original NEEDSWORK comment was referring to, implying the solution might require changes to the hashmap API rather than just the patch-id code.

This appears to be a case where a seemingly simple const-correctness fix has deeper architectural implications. The thread is now at a point where it needs input from someone familiar with both the hashmap API and diffcore implementation to determine the right path forward.