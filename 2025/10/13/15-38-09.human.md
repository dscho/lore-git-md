This is a follow-up in a thread about fixing a test regression in t1016-compatObjectFormat.sh related to GPG configuration persistence. The core issue was a subtle change from persistent `git config` to ephemeral `test_config` for gpg.program setup that broke test dependencies.

Junio Hamano responds to Andrew Kreimer's apology for the oversight, emphasizing that review oversights are a collective responsibility rather than solely the patch author's fault. This wraps up the main technical discussion after Junio's earlier fix (reverting to `git config`) was confirmed to resolve the test failures.

The exchange shows good collaborative spirit in addressing what turned out to be a tricky test infrastructure issue. While the immediate configuration persistence problem is solved, the thread context indicates there may still be unrelated timing-related flakes in GitHub CI that need separate investigation.