This is a test infrastructure patch in Christian Couder's v3 series on unified signature handling. It addresses a fragility in t9350-fast-export.sh where tests were using hardcoded counts of annotated tags, which could break if new tests added more tags. The solution introduces an `ANNOTATED_TAG_COUNT` variable that increments with each tag creation, making the tests dynamically track the expected count rather than relying on magic numbers.

The change affects four test cases that verify tag handling during fast-export/import operations. While the author acknowledges the solution is "a bit ugly," it explicitly surfaces test interdependencies that alternative approaches would hide. The patch maintains all existing test assertions while making them more robust against future test additions, particularly important as the signature handling series adds new tag-related tests.

This is a straightforward improvement to test reliability that doesn't modify any production code or affect the core signature handling functionality being developed in the series. The change builds on prior test infrastructure work in the thread while remaining focused on the fast-export test suite's needs.