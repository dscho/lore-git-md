This thread discusses a small but clear refactoring to simplify submodule handling in `git cat-file --batch-check`. The patch removes redundant OID-to-hex conversion code by leveraging existing behavior in `report_object_status()`, which already handles NULL input by converting the OID internally. The change affects only the submodule case (S_IFGITLINK) and maintains identical behavior while making the code slightly cleaner. The patch was discovered during routine maintenance while merging Git v2.50.1 into GitHub's fork. While not fixing any specific bug, it's a straightforward improvement that removes unnecessary code.