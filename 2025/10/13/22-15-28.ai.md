This is a follow-up discussion about the legacy `git whatchanged` command, continuing a thread where Samer Abdallah noted they still use it. Kristoffer Haugsbakk provides a direct translation of `git whatchanged` invocations to equivalent `git log` commands, suggesting `git log --no-merges --raw` as the base replacement while noting `--stat` or `--name-only` may be more human-readable alternatives when not processing the output programmatically.

The email offers practical migration advice without challenging the original poster's usage, focusing purely on command equivalency. It assumes familiarity with both commands' output formats and makes no broader commentary about `git whatchanged`'s deprecated status beyond the technical translation. The response is narrowly scoped to the original observation, providing actionable information for those who might want to transition away from `git whatchanged` while maintaining similar functionality.