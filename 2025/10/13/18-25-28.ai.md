This is a refactoring patch that's part of the `git replay` atomic updates series (v3). It prepares for upcoming changes by standardizing option validation in `builtin/replay.c`.

The patch replaces a custom error check for mutually exclusive `--advance` and `--contained` options with Git's standard `die_for_incompatible_opt2()` helper. This change brings three benefits: (1) it aligns with Git's established pattern for option conflict handling, (2) provides consistent error messaging, and (3) sets up infrastructure for cleanly adding validation of the new `--update-refs` option in subsequent patches.

The implementation is straightforward - a direct replacement of the manual `die()` call with the helper function while maintaining the same logic. The commit message clearly explains the motivation and connects this preparatory work to the series' broader goal of improving `git replay`'s reference update behavior. This appears to be a well-scoped, uncontroversial change that follows project conventions.