This is a follow-up in the const-correctness discussion for the patch-id subsystem, continuing the thread about hashmap API design tensions.

Okhuomon Ajayi acknowledges Junio Hamano's earlier feedback about their initial const-correctness fix being incorrect, as it would break functionality since `commit_patch_id()` and related functions need to modify the `diff_options` structure. The email confirms the root issue is the mismatch between the hashmap API's requirement for `const void*` in `cmpfn_data` and the diff subsystem's need to modify options during processing.

Ajayi states they will revert their local changes and consider safer alternatives, recognizing that simply adding const was the wrong approach. They leave open the possibility of either finding an alternative solution or keeping the existing NEEDSWORK comment if modifying the hashmap API isn't feasible. The email shows the contributor has understood the architectural implications of the issue and is stepping back to reconsider the solution space rather than pushing forward with an incorrect fix.

Key technical points:
- Confirms `diff_options` modification in `commit_patch_id()` makes simple const addition unworkable
- Identifies hashmap API's `const void*` requirement for `cmpfn_data` as core tension
- References existing NEEDSWORK comment about this architectural mismatch
- Leaves open whether solution requires hashmap API changes or alternative approach

This moves the thread forward by clearly establishing the initial approach was wrong and setting up for deeper consideration of API-level solutions rather than superficial const-correctness fixes. The contributor demonstrates good responsiveness to feedback while recognizing the complexity of the underlying issue.