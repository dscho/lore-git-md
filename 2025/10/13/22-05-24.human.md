This is the second patch in a 3-part series implementing atomic reference updates for `git replay`, now in its final review stage before merging. The patch makes atomic ref updates the default behavior while preserving the traditional pipeline workflow via a new `--update-refs=<mode>` option (with "yes" for atomic updates and "print" for pipeline output).

Junio Hamano provides final review feedback focusing on interface clarity. He suggests the enum-based `--update-refs` option might be better named `--ref-action` since "print" doesn't logically answer the implied "update?" question. The review also includes detailed suggestions for code improvements - converting the mode string to an enum early, making the ref update handler more future-proof, and clarifying documentation about how `--advance` interacts with the update modes.

The patch appears technically sound and addresses all prior feedback from the thread, with only minor naming and documentation polish remaining. Junio's suggestions aim to make the interface more intuitive and the implementation more robust, but don't raise any blocking concerns. This represents the final refinement pass before the atomic ref update functionality can be merged.