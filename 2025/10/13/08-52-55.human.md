This is a refactoring patch in a series introducing a `--required` flag for reference optimization operations. It's the third patch in a 9-part series primarily aimed at supporting GitLab's Gitaly service performance optimizations.

The patch renames `pack_refs_opts` to `optimize_refs_opts` and updates related flag names (`PACK_REFS_*` to `OPTIMIZE_REFS_*`) to maintain consistency after previous refactoring work. Patrick Steinhardt suggests further naming improvements in his review: the struct should be `refs_optimize_opts` to match the function name pattern, and flags should use `REFS_OPTIMIZE_*` prefix. Karthik Nayak acknowledges these suggestions and will implement them in the next revision.

This is a straightforward cleanup patch that follows through on naming changes started in earlier patches. The changes are mechanical and uncontroversial, with the review focusing on naming conventions rather than functionality. The patch helps maintain consistency as the series moves reference optimization operations to a unified interface.