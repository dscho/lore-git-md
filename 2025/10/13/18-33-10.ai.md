This is the second patch in a 3-part series implementing atomic reference updates for `git replay`, transitioning the command from its previous pipeline-based output to direct atomic ref updates. The patch makes atomic updates the default behavior while maintaining backward compatibility through a new `--update-refs=<mode>` option.

The implementation introduces a `handle_ref_update()` helper function that centralizes ref update logic, supporting both the new atomic mode (using Git's ref transaction API with `ref_store_transaction_begin()`, `ref_transaction_update()`, and `ref_transaction_commit()`) and the legacy print mode. The default mode is now "yes" for atomic updates, with "print" available for pipeline workflows.

Key technical aspects include:
- New enum-based `--update-refs` option with "yes" (default) and "print" modes
- Comprehensive test coverage verifying atomic behavior, including all-or-nothing guarantees using lock files
- Test isolation improvements using `test_when_finished`
- Documentation updates reflecting the new defaults and modes
- Bare repository support for server-side workflows
- Validation of atomic vs. pipeline equivalence in test suite

The patch builds on previous thread consensus around using enum-based design for forward compatibility and addresses all outstanding technical concerns from earlier discussions. It maintains the experimental status of `git replay` while providing a clear path for server-side atomic operations.