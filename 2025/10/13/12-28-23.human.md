This is a test modernization patch, part of an ongoing effort to improve test readability by replacing old-style shell path checks with modern test helpers.

The patch updates `t/t2401-worktree-prune.sh` to use `test_path_is_file` and `test_path_is_dir` helpers instead of direct shell conditionals. This is a straightforward conversion that replaces 17 lines of manual path checking with the more readable test helper equivalents. The changes are mechanical and don't affect test behavior - they just make the test intentions clearer.

The commit message is brief but sufficient for this type of change, explaining the motivation as improved readability. The patch appears well-scoped, touching only one test file with simple substitutions. This follows a pattern we've seen in other test modernization efforts on the list.

While not a critical fix, these kinds of cleanups are generally welcome as they make tests easier to maintain. The patch looks like a solid, uncontroversial improvement that could be merged as-is.