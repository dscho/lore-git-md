This is a preparatory refactoring patch for the atomic reference updates in `git replay`. It's part of a 3-patch series that has already seen extensive discussion and is now in its final form.

The patch standardizes option validation in `git replay` by switching from a custom error message to using the shared `die_for_incompatible_opt2()` helper function. This change makes the code more maintainable and consistent with other Git commands, setting up for the introduction of the new `--update-refs` option in the next patch.

The actual change is minimal - just converting one option conflict check to use the standard helper. This is clearly a well-considered change that follows established Git patterns, and it's part of a larger effort that has already been thoroughly reviewed and agreed upon by multiple contributors. The patch appears straightforward and low-risk, serving as groundwork for the more substantial changes that follow in the series.