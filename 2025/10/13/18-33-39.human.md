This is a follow-up in the const-correctness discussion around the patch-id subsystem. Junio Hamano responds to Okhuomon Ajayi's removal of a NEEDSWORK comment in `builtin/patch-id.c`, expressing concern that simply removing the comment without deeper analysis of the hashmap API's const usage is insufficient.

The thread had previously concluded that the current implementation - which involves a const-cast in `patch_id_neq()` - is actually correct given the architectural constraints between the hashmap and diff subsystems. While Okhuomon's removal of the comment aligned with Junio's earlier suggestion to document rather than change the behavior, Junio now clarifies that the comment removal should have been accompanied by a more thorough investigation into whether the hashmap API's const requirements are appropriate more broadly.

This highlights an ongoing tension in the codebase between strict const-correctness and practical API design. The exchange serves as a useful case study in how even seemingly simple documentation changes can require careful consideration of wider architectural implications.