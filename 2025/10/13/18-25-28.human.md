This is a refactoring patch that's part of a series improving `git replay`'s option handling. The change standardizes how `git replay` reports incompatible options by switching from a custom error message to using Git's standard `die_for_incompatible_opt2()` helper function. 

The patch prepares for future work (coming in the next patch) that will add a new `--update-refs` option. By making this change now, the author ensures consistent error reporting when that new option conflicts with existing ones. The change is mechanical - it replaces a direct `die()` call with the helper function while maintaining the same validation logic.

This is a straightforward cleanup that follows Git's established patterns for option validation. The patch is well-contained (just 3 lines changed) and clearly motivated by upcoming changes in the series. It doesn't change any user-visible behavior, just makes the code more maintainable and consistent with other commands.