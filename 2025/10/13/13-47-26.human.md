**User report of active `git whatchanged` usage**

This is a user response to Git's deprecation warning for `git whatchanged`, following the standard procedure to report continued usage of the command. Kristoffer Haugsbakk confirms they still use `git whatchanged` in response to Grzegorz Koperwas's initial report, while also providing helpful migration advice.

The email notes that `git whatchanged` is redundant with `git log --no-merges --raw` (which uses the same underlying machinery) and suggests alternative output formats like `--stat` or `--name-only` for human-readable output. This appears to be a helpful, good-faith response that both acknowledges the deprecation process while providing useful information for other users who might be affected.

The response is technically accurate and constructive, though it doesn't present any new arguments for keeping the command beyond confirming active usage. This follows the expected pattern for such deprecation feedback, where users are simply asked to report usage rather than argue for the command's retention. The thread now awaits maintainer response to assess whether this (and any future) usage reports warrant keeping the command.