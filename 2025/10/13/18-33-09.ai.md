This is a preparatory refactoring patch for the atomic reference updates in `git replay` series. It standardizes the option validation logic in preparation for adding the new `--update-refs` option in subsequent patches.

The patch replaces a custom error message for conflicting `--advance` and `--contained` options with a call to `die_for_incompatible_opt2()`, which provides standardized error messages for mutually exclusive options. This change aligns `git-replay`'s option handling with other Git commands and sets up a consistent pattern that will be used when validating the new `--update-refs` option against other incompatible options.

The implementation is straightforward - it simply replaces the direct `die()` call with the helper function while maintaining the same logical conditions. This is part of the broader effort to finalize the atomic reference updates feature, where consistent error handling will be important for the new `--update-refs` option that will be introduced in the next patch.

The change is minimal (3 lines modified) and focused purely on the validation infrastructure, with no behavior changes beyond the error message formatting. This appears to be a clean preparatory step that follows established Git conventions for option validation.