This is a follow-up in the ongoing CI/build system thread about Ubuntu 25.10's sudo-rs compatibility issues. Junio C Hamano acknowledges Brian m. carlson's upstream feature request to sudo-rs (issue #1299) for implementing the missing `--preserve-env` functionality. The email confirms alignment with the current two-pronged approach: maintaining the immediate CI workaround (falling back to C-based sudo) while supporting the upstream effort for a proper fix in sudo-rs itself. 

The thread has reached consensus on the technical solution implemented in `ci/install-dependencies.sh` and now shifts focus to tracking the upstream development. Junio's brief response indicates the Git maintainers view the upstream solution as preferable but are content with the temporary workaround until sudo-rs implements the feature. No further action is required from the Git side unless the upstream situation changes. The discussion remains focused on ensuring CI stability while keeping open the possibility of future simplification if sudo-rs adds the needed functionality.