This appears to be a user question about Jenkins Git plugin behavior rather than a patch submission. The user is encountering an error when Jenkins tries to use `git whatchanged` to retrieve change history for shared libraries, as the command is being deprecated in Git.

The error shows Jenkins failing because `git whatchanged` now requires explicit `--i-still-use-this` flag to run, which isn't being passed by the plugin. The user wants to restore the previous behavior of seeing change history in their builds.

This isn't a Git development discussion but rather a usage question that would normally be directed to Jenkins plugin maintainers. The core issue stems from Git's deprecation of `git whatchanged` in favor of `git log` (which provides similar functionality with more modern options). The Jenkins plugin would need to be updated to use `git log` instead or explicitly pass the `--i-still-use-this` flag if continuing to use `whatchanged`.

For Git mailing list purposes, this would likely be noted as an example of real-world impact from the `whatchanged` deprecation, but doesn't represent a patch or proposal for Git itself. The user would probably be better served by contacting Jenkins plugin maintainers or consulting Jenkins documentation about Git integration.