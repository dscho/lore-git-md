This is a user support response regarding Jenkins integration with Git's deprecated `git whatchanged` command. Kristoffer Haugsbakk points to an existing fix in the Jenkins issue tracker (JENKINS-76017) that addresses the problem where Jenkins builds were failing due to the deprecation of `git whatchanged`. The thread had previously established that users were encountering errors when Jenkins attempted to use this deprecated command without the required `--i-still-use-this` flag on Windows systems.

The response indicates this is not a Git-side issue but rather a Jenkins plugin problem that has already been resolved upstream. The fix presumably either adds the opt-in flag for backward compatibility or migrates to the modern `git log` equivalent. This appears to close the immediate support question by directing the original poster to the existing solution in the Jenkins ecosystem.

Key technical details carried forward:
- The underlying issue stems from Git's deprecation policy for `git whatchanged`
- Windows systems were particularly affected due to path specifics (D:\git\bin\git.exe)
- The resolution exists in Jenkins' issue tracker rather than requiring Git changes

This email effectively concludes the thread by providing the solution path, though it doesn't specify whether the fix implements the flag workaround or full migration to `git log`. The thread can likely be considered resolved unless follow-up questions emerge about implementing the Jenkins-side fix.