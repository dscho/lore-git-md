This is a final review response in the `--signed-tags` implementation thread for `git fast-import`. Patrick Steinhardt confirms Christian Couder's explanations about two key points in the patch series.

First, regarding documentation ordering, Patrick accepts Christian's justification for placing `--signed-tags` before `--signed-commits` based on historical precedent in `git fast-export` and Junio Hamano's earlier input, despite the non-alphabetical ordering.

Second, Patrick seeks clarification on the signature stripping implementation in `fast-import.c`. Christian explains that while users can manually insert signatures anywhere in tag messages, Git's own signatures are always appended to the end, making the current truncation approach safe. This confirms the technical correctness of the signature handling logic using `strbuf_setlen()` to remove signatures from the end of tag messages.

The exchange resolves the last remaining nits in the series, with Patrick indicating satisfaction with Christian's explanations. This clears the way for the final version of the patch series implementing signature handling between `fast-export` and `fast-import`. The discussion confirms the technical approach is sound and documentation ordering follows established conventions.