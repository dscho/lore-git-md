Feature refinement for `git replay` atomic updates. Phillip Wood responds to Siddharth Asthana's v2 patch making atomic ref updates the default behavior in `git replay`. The discussion focuses on two key interface design points:

1. Phillip suggests consolidating the `--output-commands` and `--allow-partial` options into a single `--update-refs=<mode>` parameter for better future extensibility, even as Siddharth considers dropping the less-used `--allow-partial` option entirely.

2. Phillip notes code duplication in the ref update handling and recommends extracting a helper function to manage both atomic updates and command output generation, which Siddharth agrees would improve readability.

This exchange represents final polishing of the atomic update feature that has already gained consensus from core maintainers. The thread shows experienced contributors refining the interface while maintaining the technical approach approved in earlier reviews. Phillip's suggestions aim to future-proof the command's design without disrupting the agreed-upon atomic-by-default behavior.