This is patch 8/14 in a refactoring and bugfix series modernizing Git's refs subsystem, specifically focusing on tag peeling behavior and iterator interfaces. The patch converts `builtin/show-ref.c` to use the new `reference_get_peeled_oid()` API instead of the older `peel_iterated_oid()`.

The change makes `git show-ref` consistently use the new `struct reference` interface across all its modes, even when verifying individual refs (which previously didn't have access to the full reference structure). The implementation manually constructs a minimal `struct reference` when needed, maintaining the same behavior while aligning with the series' goal of eliminating global state and standardizing the refs API.

This is a straightforward mechanical conversion that follows the architectural changes introduced earlier in the series. It demonstrates how the new reference handling works in practice while maintaining all existing functionality. The patch appears well-contained and properly integrated with the broader series' goals.