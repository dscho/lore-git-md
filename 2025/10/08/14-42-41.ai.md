This is a reply in an ongoing thread about the potential removal of `git whatchanged`. Kristoffer Haugsbakk responds to Filippo Falezza's request to preserve the command by explaining its technical redundancy with `git log`.

The reply provides concrete migration advice, noting that `git whatchanged` has been functionally equivalent to `git log --no-merges --raw` for over 12 years, with both commands sharing the same underlying implementation. The email suggests alternative output formats (`--stat` or `--name-only`) that might be more readable for human consumption while maintaining similar functionality.

Key technical details include:
- The explicit mapping between `whatchanged` and equivalent `log` invocations
- The 12-year timeline since `log` subsumed `whatchanged`'s functionality
- Output format considerations (`--raw` vs `--stat` vs `--name-only`) for different use cases

The response implies that removal is justified by this long-standing redundancy, though it doesn't address whether there are active plans to remove the command. The tone suggests this is more about educating users on alternatives rather than debating removal policy. No new technical arguments for or against preservation are introduced beyond the established functional equivalence.