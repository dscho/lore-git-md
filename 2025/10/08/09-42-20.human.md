This is a test infrastructure patch in the ongoing `--signed-tags` series. Christian Couder responds to Patrick Steinhardt's question about why the GPG test prereq manually creates its home directory, while the GPGSM prereq does not. 

The discussion reveals an inconsistency in how different cryptographic backends handle directory creation - GPGSSH uses `mkdir -p` while GPGSM appears to let the tool create the directory. Christian acknowledges the observation and commits to investigating whether `gpg --homedir` behavior varies by version or differs from gpgsm's behavior.

This is a minor but thoughtful refinement to the test setup that surfaced during the broader signature-handling work. The exchange shows careful attention to test reliability details, though the actual impact appears limited to test environment setup rather than core functionality.