Documentation clarification regarding `git bundle` clone behavior. Andrew Harmon provides a script demonstrating the discrepancy between his expectations (that `git clone` from a bundle would mirror cloning from a remote) and the actual behavior (which follows Git's conservative remote clone semantics). The script shows that while `git bundle --all` includes all refs, a subsequent clone only fetches local branches by default, requiring explicit fetch commands to get remote-tracking branches.

This email reinforces the need for better documentation around bundle cloning behavior, particularly to explain: (1) why remote-tracking branches aren't fetched by default (matching network clone behavior), (2) how to perform complete transfers using either explicit fetch syntax or mirror cloning, and (3) the original design intent of bundles as network-less clones. Andrew's concrete example would serve well as a documentation case study showing both the default behavior and the workarounds.

Key technical details include the reproduction script showing bundle creation with `--all`, verification of included refs, and the two methods to fetch all refs (`git fetch origin 'refs/remotes/origin/*:refs/remotes/origin/*'` and mirror cloning). The thread has established this as expected behavior, leaving documentation improvements as the primary remaining action.