**CI infrastructure patch with style discussion**

This is a reply in Patrick Steinhardt's ongoing series adding Rust CI infrastructure to Git, specifically discussing the formatting check implementation from patch 2/6. SZEDER Gábor points out a potential inconsistency in the script's error handling - while most Git CI scripts use `set -e` to automatically fail on errors, this Rust formatting check manually checks the exit code. The observation is technical rather than stylistic, noting the script either needs alignment with existing conventions or an explanation for the deviation.

The thread context shows this patch sits within broader discussions about Rust formatting standards (80 vs 100 character line lengths) and CI infrastructure design. While earlier messages debated style policy at length, this contribution focuses on implementation details, suggesting the author either standardize the error handling pattern or document the rationale for doing it differently. The comment doesn't take a position on the line length debate, instead reinforcing the importance of consistent CI script behavior.