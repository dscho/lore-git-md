This is a compatibility discussion regarding the `git whatchanged` command, continuing a thread started by Filippo Falezza who advocated for preserving the command. Kristoffer Haugsbakk responds with technical details about its deprecation status and equivalent alternatives.

The reply clarifies that `git whatchanged` has been functionally redundant with `git log` for over 12 years, using the same underlying machinery but with different defaults. It provides specific conversion instructions: replacing `git whatchanged` with `git log --no-merges --raw`, while suggesting `--stat` or `--name-only` might be more readable alternatives for human consumption.

The response appears technically sound and helpful, offering clear migration paths while confirming the command's deprecated status. It doesn't address whether there are active removal plans from the codebase, focusing instead on practical alternatives. The tone is matter-of-fact rather than confrontational, though it doesn't engage with the original poster's specific workflow needs beyond the general replacement suggestion.