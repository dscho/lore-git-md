Post-merge naming discussion in refs modernization series, with new question about tag verification error handling.

Jialuo raises a follow-up question about Patrick Steinhardt's patch 12/14 in the recently merged refs series. While the original patch addressed tag verification by skipping storage of invalid peeled object IDs, Jialuo questions whether this is the right approach for handling corruption. The key technical point is whether Git should fail loudly (die) or continue silently when encountering tag objects with type mismatches, given that `git fsck` would flag such corruption anyway. This touches on the series' broader theme of making tag verification more rigorous.

The discussion remains focused on design philosophy rather than implementation - whether silent degradation is preferable to hard failure when the refdb contains corrupted tag objects. No code changes are proposed, just probing the chosen approach. The thread context shows this is a post-merge refinement discussion, with the core technical work already completed and approved. The question extends the series' examination of how strictly Git should enforce object validity during ref operations.