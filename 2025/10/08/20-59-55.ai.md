This is a feature patch finalizing the interface design for atomic reference updates in `git replay`. The email captures a key design decision point in the ongoing discussion about how to expose the atomic ref update functionality.

Elijah Newren weighs in on the interface debate between Siddharth Asthana and Phillip Wood regarding command-line options for controlling ref updates. The discussion centers on whether to use:
1. A simple boolean `--[no-]update-refs` flag (Elijah's original suggestion)
2. An extensible `--update-refs=<mode>` approach (Phillip's proposal)

Elijah now expresses preference for Phillip's more extensible `--update-refs=<mode>` design, reversing his earlier position. This suggests the interface may shift from a simple boolean to an enum-based approach to better accommodate future expansion (like potential partial update modes).

The thread context shows this is the last major unresolved design question before the series can be merged, with all technical implementation already complete. The discussion has moved from implementation details to long-term interface philosophy, with maintainers now reconsidering earlier consensus on boolean config options in light of Junio Hamano's extensibility concerns.

Key technical points at stake:
- The enum approach would allow future additions like partial updates without breaking changes
- The boolean approach matches existing `rebase.*` config conventions
- No changes to the underlying atomic update implementation are needed either way

This email represents a significant shift in the design discussion, with a core contributor changing their position based on maintainer feedback about future-proofing the interface. The decision will affect both the command-line flags and corresponding config option (`replay.updateRefs`).