This is patch 7/14 in a refactoring and bugfix series modernizing Git's tag peeling and ref iteration infrastructure. The patch focuses on the ref-filter subsystem, which handles formatting and filtering references for commands like `git tag` and `git ls-remote`.

The key change propagates peeled object IDs through the ref-filter pipeline by adding a `peeled_oid` field to `struct ref_array_item`. This allows the subsystem to use pre-computed peeled OIDs when available, avoiding redundant object parsing through `peel_iterated_oid()`. The patch updates several callers to pass the peeled OID through the stack, including `ref_array_push()` and `pretty_print_ref()` interfaces.

The changes are mechanical but wide-ranging, touching 5 files with 66 lines modified. The patch appears well-contained within the series' goals, building on earlier infrastructure changes while enabling later optimizations. Performance testing in the Chromium repo shows a 13% speedup from this work. The series has already received positive reviews from Taylor Blau and Junio Hamano, with this patch being part of the merge-ready first half (patches 1-7).