Patrick Steinhardt continues his series improving CI infrastructure for Rust support, now focusing on static analysis with Clippy integration (patch 4/6). This follows earlier patches that optimized CI setup and added Rust formatting checks. The new change is straightforward but important - it installs Clippy alongside rustfmt in the CI/install-dependencies.sh script and runs it with strict warning settings (-Dwarnings) via the existing run-rust-checks.sh framework.

The series maintains its consistent approach of following Rust ecosystem conventions while integrating with Git's existing CI infrastructure. This patch builds on the foundation laid by previous work, adding another layer of quality control without modifying production code. The changes are minimal but meaningful, treating all Clippy lints as errors to match Git's standards for static analysis. With this addition, the Rust CI pipeline now covers both formatting (via rustfmt) and common code quality issues (via Clippy), addressing two key aspects of Rust development workflow. The series continues to progress methodically, with Windows build support and minimum Rust version verification still to come in the remaining patches.