This is a review response in the ongoing `--signed-tags` implementation series for `git fast-export`/`fast-import`. Patrick Steinhardt provides feedback on Christian Couder's patch that extends signature handling to support all signature types (PGP, X.509, SSH) consistently.

The key technical change being reviewed replaces a PGP-specific signature detection with `parse_signed_buffer()`, which properly handles all signature formats. Patrick approves of this change, noting it both removes special-casing and centralizes signature detection logic. The implementation correctly updates the message size calculation when stripping signatures.

Patrick also notes a minor test style issue - modern Git test conventions omit blank lines at test case boundaries - but this doesn't affect functionality. The review confirms the technical approach while maintaining the series' goal of unified signature handling across Git's fast-import/export operations. This patch represents the fast-export side of the signature handling parity, with corresponding fast-import changes covered elsewhere in the series.