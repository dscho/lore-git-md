This is a code review response in the ongoing series adding `--signed-tags` support to `git fast-import`. Patrick Steinhardt is reviewing Christian Couder's patch that makes `fast-export` handle all signature types (PGP, X.509, SSH) consistently by switching to the centralized `parse_signed_buffer()` function.

Patrick approves the technical approach, noting it properly removes the old PGP-specific check in favor of the more general signature detection. He points out one minor style issue in the test cases (unnecessary empty lines at test boundaries) but otherwise finds the implementation sound. The review is positive overall, with Patrick explicitly calling out the improvement over the previous PGP-specific code.

This patch is part of the broader effort to achieve signature handling parity between import and export operations, with previous discussion having covered documentation updates and test infrastructure improvements. The review doesn't raise any blocking concerns, just a small test formatting nit.