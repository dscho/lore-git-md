This is part 3 of Adrian Ratiu's hook subsystem refactoring series, focusing on converting the post-rewrite hook to use the new `hook.h` infrastructure. The patch addresses several review comments from Patrick Steinhardt and others.

Key technical elements:
- Introduces `pipe_from_strbuf` callback for feeding stdin to hooks
- Maintains existing deadlock behavior (same as pre-refactor code)
- Includes optimizations for pipe throughput with batched writes
- Contains some unrelated cleanup (like `child`->`notes_cp` rename) that will be dropped in v2

The discussion clarifies that while deadlock potential exists (as before), it's considered a bug condition. The patch also confirms the importance of batching writes for performance with large inputs like post-receive hooks. Review feedback has led to planned simplifications in v2:
- Removing unnecessary `strbuf_reset()`
- Fixing style issues (EPIPE handling braces)
- Dropping the unrelated variable rename

The core functionality remains unchanged from the original implementation while adopting the new hook infrastructure. This patch appears mechanically sound but still needs resolution on the deadlock documentation question raised in earlier reviews. The changes are focused and well-explained, showing good responsiveness to reviewer feedback.