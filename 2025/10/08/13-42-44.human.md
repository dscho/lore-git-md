This is a refactoring patch that's part of a larger series modernizing Git's refs subsystem. The patch introduces a wrapper struct for the `each_ref_fn` callback to make future interface changes less disruptive. While the initial refactoring requires widespread changes (touching 48 files), it pays off by making subsequent modifications easier.

The discussion here addresses minor follow-up points from Justin Tobler's review. Patrick Steinhardt agrees to update a comment about callback memory guarantees to reflect the new struct-based approach, and acknowledges that the struct is populated in a few additional spots beyond the ref backends and iterator code. These are small refinements to an already well-reviewed change that's part of a performance-oriented series nearing merge.