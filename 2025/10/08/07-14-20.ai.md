This is a test infrastructure patch in the ongoing `--signed-tags` series. Patrick Steinhardt responds to Christian Couder's change to `t/lib-gpg.sh` that makes the GPG test prereq create its home directory with `mkdir -p` rather than plain `mkdir`. 

Patrick questions whether manual directory creation is needed at all, noting that the GPGSM prereq doesn't do this and that `gpgsm` handles directory creation automatically. He speculates that `gpg --import` might similarly create its own directory, implying the change might be unnecessary. The comment suggests this could be a minor cleanup opportunity in the test setup, though it doesn't block the overall series.

The exchange shows careful attention to test infrastructure details in the signature-handling work, with Patrick identifying a potential inconsistency between how GPG and GPGSM tests manage their environments. This relates back to the series' goal of consistent signature handling across all formats.