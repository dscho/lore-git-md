This is a test infrastructure patch in the ongoing `--signed-tags` series, specifically addressing GPG test setup. Christian Couder responds to Patrick Steinhardt's question about why the test prereq for GPG manually creates the GNUPGHOME directory while GPGSM does not.

The discussion focuses on whether `gpg --homedir` automatically creates the home directory like `gpgsm` appears to do. Christian notes the GPGSSH prereq already uses `mkdir -p` but isn't sure if this is version-dependent behavior or a difference between gpg and gpgsm. He plans to investigate further.

This is a minor technical clarification in the test setup that doesn't affect the core functionality of the signature handling series. The exchange shows careful attention to test reliability across different cryptographic backends, though the actual impact appears limited to test robustness rather than user-facing behavior. The thread context indicates this is part of broader test infrastructure improvements supporting the signature handling unification work.