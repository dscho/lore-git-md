This is a follow-up discussion in the atomic reference updates patch series for `git replay`. Elijah Newren clarifies that the `--format` suggestion for the command came from Kristoffer Haugsbakk, not himself, and argues against adding such an option at this stage. 

Newren emphasizes that `git replay` is still highly experimental, with fundamental behaviors like conflict handling still under discussion. He compares this to past discussions about `git merge-tree`, where format options were deemed premature when core functionality wasn't yet stabilized. The email reinforces the current consensus to keep the implementation simple and focused on atomic ref updates without adding potentially distracting features like output formatting.

The exchange shows the maintainers being cautious about scope creep while the command is still evolving, preferring to solidify core atomic update behavior first. This aligns with the thread's overall trajectory toward a minimal, stable implementation of atomic updates as the default behavior.