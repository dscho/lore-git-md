Filippo Falezza started this thread by advocating for preserving the `git whatchanged` command, which they rely on for their workflow, though without specifying what aspects they find uniquely valuable compared to `git log`. The command has been considered deprecated in favor of `git log` but remains available in current Git versions.

Kristoffer Haugsbakk responded by explaining that `git whatchanged` has been functionally equivalent to `git log --no-merges --raw` for over 12 years, with both commands sharing the same underlying implementation. He provided concrete migration advice, suggesting alternative output formats like `--stat` or `--name-only` that might offer more readable output while maintaining similar functionality. The reply implies that removal would be justified by this long-standing redundancy, though it doesn't confirm any active plans to deprecate the command.

The discussion now centers on whether there are technical or workflow reasons to preserve `whatchanged` given its functional overlap with `log`, and whether users like Filippo have specific needs that aren't met by the suggested alternatives. No maintainers have yet weighed in on potential removal plans.