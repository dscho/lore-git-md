This is a user support question about Git integration with Jenkins, specifically regarding the deprecation of `git whatchanged`.

The email comes from a Jenkins user encountering an error when their build system tries to use `git whatchanged` to retrieve change history for shared libraries. The command fails with a message indicating `whatchanged` is being removed unless explicitly opted into with `--i-still-use-this`. The error occurs in a Windows environment (D:\git\bin\git.exe) and prevents the Jenkins Git plugin from retrieving the changeset information needed for build reporting.

The core issue stems from Git's deprecation of `whatchanged` in favor of `git log` (which provides similar functionality with more modern formatting options). The error message clearly states the workaround - adding `--i-still-use-this` - but the user appears unaware of how to modify the Jenkins Git plugin's behavior to include this flag.

This isn't a patch submission but rather a request for help, likely from someone unfamiliar with Git's deprecation policies. The thread context suggests this may be part of an ongoing discussion about `whatchanged` removal, though no prior thread summary was provided. The solution would involve either configuring Jenkins to use `git log` instead, or modifying its Git plugin to pass the required opt-in flag when calling `whatchanged`.