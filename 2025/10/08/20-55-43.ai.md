This is a technical refinement to the Rust CI infrastructure series, specifically addressing the formatting check implementation in `ci/run-rust-checks.sh`. SZEDER Gábor points out a deviation from Git's shell script conventions - the script lacks `set -e` error handling that's standard in other CI scripts. 

The observation is narrowly focused on script consistency rather than the broader formatting policy debate. The critique suggests either aligning with existing error handling patterns or documenting the rationale for divergence. This comes as the series has already established technical consensus on Rust formatting verification via rustfmt`, with the current discussion focused on style policy decisions.

Key technical detail: The script currently uses explicit `RET=1` assignment on failure rather than relying on `set -e` termination. This matches the pattern used in some other Git shell scripts that need to accumulate multiple checks before exiting, but differs from the simpler `set -e` approach more common in the CI scripts. The comment implies this distinction should be either normalized or justified.

This feedback occurs against the backdrop of the ongoing formatting standards debate, but doesn't engage with those higher-level questions - it's strictly about implementation consistency in the CI infrastructure. The series maintainer will need to decide whether to standardize error handling patterns or document the current approach as intentional.