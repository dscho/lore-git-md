This is a discussion about the potential removal of the legacy `git whatchanged` command. Kristoffer Haugsbakk responds to Nick Giampietro's request to keep the command by explaining that `git log` has effectively replaced it for over a decade, using the same underlying code but with different defaults.

The email provides a clear migration path for users: replacing `git whatchanged` with `git log --no-merges --raw` (or `--stat`/`--name-only` for human-readable output). This suggests the removal is being considered as a cleanup of redundant functionality rather than a feature deprecation.

The response is technical and matter-of-fact, focusing on the functional equivalence between the commands rather than engaging with the workflow concerns raised in the original request. No maintainer perspective is included in this exchange, leaving the future of the command still unresolved. The thread appears to be in early stages, with users stating their positions but no clear decision-making yet.