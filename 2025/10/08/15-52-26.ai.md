This is a follow-up question in the completed refactoring series about tag peeling behavior. The thread summary shows this was a major 14-patch series that has already been merged, modernizing Git's refs iterator interface and tag verification infrastructure.

Jialuo asks a specific naming question about the `struct reference` introduced in the first patch of the series, wondering why the symbolic ref target field is named `target` rather than `referent`. They note that since the struct is already called `reference`, using `referent` might be redundant. This appears to be a minor stylistic point raised after the series was completed, rather than a substantive issue with the implementation.

The question focuses on a detail in patch 1/13, which introduced the wrapper struct that encapsulates refname, target, oid, and flags - a key part of the series' infrastructure changes. The thread context indicates this patch was part of the core infrastructure changes (patches 1-7) that were already merged with approval from maintainers. The discussion here is purely about naming preference in already-accepted code.