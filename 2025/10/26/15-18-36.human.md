Performance analysis of the `**` pattern matching optimization in `dir.c`.

Jeff King provides detailed benchmark results comparing four approaches to handling prefix matching in `.gitignore` patterns: the current implementation (`git.orig`), removing prefix matching entirely (`git.none`), only keeping the full-match" optimization (`git.fullonly`), and his proposed "keep one character" solution (`git.minusone`). The tests show that prefix matching provides real performance benefits (1.46x faster for exact matches), with Jeff's solution performing comparably to the current code while fixing the `**` matching bug.

The benchmarks reveal that `fnmatch()` is significantly slower than direct string comparison, validating the optimization's value. Jeff notes that while the "full match" case is likely more common, the prefix optimization helps in both exact and partial match scenarios. He concludes that keeping the optimization (via his "one character" approach) is worthwhile given its performance benefits and relatively small code footprint, provided no further edge cases emerge that break the solution.

This email advances the technical discussion by providing concrete data to support keeping the prefix optimization, addressing earlier questions about its value. The benchmarks suggest Jeff's solution strikes a reasonable balance between correctness and performance, though the thread remains open for discussion of edge cases and alternative implementations.