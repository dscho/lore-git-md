Final patch series fixing `**` pattern matching in `.gitignore` and `.gitattributes`. This two-part series addresses the bug where patterns like `foo**/bar` incorrectly matched `foobar` by preserving directory boundary context during path matching optimizations.

Jeff King (Peff) has restructured the solution based on Junio Hamano's feedback, splitting it into two clear steps. The initial patch reorganizes the code to check for complete matches before attempting prefix stripping, improving readability. The second patch implements the "minusone" solution that preserves one character of context during optimization, fixing the `**` matching while maintaining performance benefits.

The series comes with test coverage in t0008-ignores.sh and shows minimal code changes (23 lines added, 6 removed across dir.c and the test file). Benchmarks previously shared in the thread demonstrated this approach maintains a 1.46x speedup for exact matches compared to unoptimized matching, validating the optimization's preservation.

With consensus reached on the approach and no outstanding technical objections, this appears ready for merging. The clean split into logical steps makes the changes easier to review, and the thread discussion has thoroughly vetted both the correctness and performance implications.