This is a refactoring patch in the `git-repo-info` series, with Eric Sunshine providing implementation feedback on the field printing API. The review focuses on making the `print_field()` function cleaner and more self-contained.

Eric suggests two improvements to the field printing implementation from the first patch in the series. First, he recommends changing the `valbuf` parameter from a `struct strbuf *` to a `const char *` to better indicate its read-only nature. Second, he proposes making the `quotbuf` buffer local to the function rather than passing it in as a parameter, eliminating an unnecessary optimization for this non-critical path.

The reviewer provides two alternative implementations - one making `quotbuf` function-scoped, and an even better version scoping it just to the `FORMAT_KEYVALUE` case branch where it's actually needed. This keeps the memory management tight and makes the code's intent clearer. The feedback maintains the separation between refactoring and feature work while improving the internal API design.

This continues the thread's pattern of focused implementation review following earlier design consensus. The changes suggested would make the code more maintainable without affecting the external behavior or the planned `--all` flag functionality in the subsequent patch.