This is a refactoring patch that's part of the `git-repo-info` feature development. It's the first of two patches in v3 of the series, which has been progressing through design discussions on the mailing list.

The patch extracts the field printing logic from `print_fields()` into a new standalone `print_field()` function. This is a clean preparatory change that will make it easier to implement the `--all` flag functionality in the next patch. The refactoring maintains all existing behavior while making the code more modular - the diff shows the printing logic being moved to the new function while the calling code becomes simpler.

The change is straightforward and well-contained, touching only `builtin/repo.c` to move the printing logic. This follows good Git development patterns by separating refactoring from feature changes in distinct commits. The patch builds on the thread's earlier design discussions and appears ready for inclusion.