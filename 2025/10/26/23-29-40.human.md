This is a follow-up discussion in the bugfix thread about Git's pattern matching behavior in `.gitignore` and `.gitattributes` files. Junio Hamano responds to Jeff King's earlier performance measurements and rationale for the "minusone" solution that preserves one character of context during prefix stripping.

Junio clarifies that his suggestion to limit the fix to known bad cases (like `**/` patterns) was motivated by correctness concerns rather than performance - he wanted to avoid potential false matches from the one-byte adjustment. However, he acknowledges Jeff's benchmark data showing the strncmp() optimization remains significantly faster than byte-by-byte comparison, validating the approach taken in the patches.

The exchange confirms technical alignment on the solution, with Junio's correctness concerns addressed and Jeff's performance measurements accepted. This appears to be the final discussion point before the series can be merged, as all substantive questions about the approach have now been resolved. The thread demonstrates Git maintainers' careful attention to both correctness and performance when fixing subtle matching behavior.