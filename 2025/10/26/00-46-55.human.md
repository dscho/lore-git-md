This is a follow-up to the `git add -p` refactoring series, specifically addressing test improvements for the EOF handling changes. Junio C Hamano suggests squashing in a test update from René Scharfe that replaces plain `grep` calls with Git's `test_grep` helper in the EOF quit test case. The change is purely about test hygiene - using the project's preferred test assertion style while verifying the same behavior (that EOF causes immediate exit after showing the first file's diff but before processing the second file). 

The update maintains all the test's existing verification points, just expressed through Git's test infrastructure rather than raw shell commands. This is a minor but proper finishing touch to the already-approved patch series, bringing the new test in line with project conventions. The thread context shows this completes the work on consistent exit handling for `git add -p`, with all changes now ready for merging.