This is a code review response in the ongoing `git-repo-info` implementation thread. Eric Sunshine provides implementation feedback on the first patch of the series, which refactors field printing into a dedicated function.

The review suggests two improvements to the `print_field()` function: first, making the `quotbuf` parameter local to the function rather than passing it in, and second, scoping the `strbuf` just to the `FORMAT_KEYVALUE` case branch where it's actually needed. The suggestions aim to make the API cleaner and more self-contained while maintaining the same functionality.

This continues the thread's focus on refining implementation details while preserving the already-settled design decisions around the `git-repo-info` command's interface. The feedback is narrowly focused on code quality rather than architectural concerns, indicating the overall approach is sound. The author will likely incorporate these suggestions in a future revision.