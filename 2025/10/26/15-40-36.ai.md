This is the final version of the bugfix series addressing incorrect `**` pattern matching in `.gitignore` and `.gitattributes` files. Jeff King (Peff) presents a two-patch solution incorporating feedback from the thread discussion.

The first patch reorganizes the code to check for complete matches before attempting prefix stripping, improving clarity over the original "== 1" check. The second patch implements the "minusone" solution that preserves one character of context during prefix stripping, fixing the `foo**/bar` matching `foobar` bug while maintaining optimization benefits.

The series demonstrates the resolution of the technical debate, keeping the performance optimization (which benchmarks showed provides a 1.46x speedup for exact matches) while correctly handling the `**` pattern edge case. The changes are minimal (23 lines added/changed) and include new test coverage in t0008-ignores.sh.

Key technical points carried forward from the thread:
- Prefix-stripping optimization remains valuable (fnmatch() is substantially slower than strcmp())
- One-character context preservation correctly handles `**` patterns
- Code organization now checks complete matches first
- Benchmark results will be documented in the commit message

This appears ready for merging into `next` with no outstanding objections, showing Git's careful approach to balancing correctness with performance optimizations. The thread history demonstrates how benchmark data helped validate keeping the optimization while fixing its edge case behavior.