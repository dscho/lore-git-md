This is Junio Hamano's response to Jeff King's patch series fixing the `**` pattern matching issue in `dir.c`. The email provides final review comments before the series can be merged.

Junio clarifies that his earlier suggestion to limit the fix to `**/` patterns was motivated by correctness concerns rather than performance - he wanted to ensure the one-byte context preservation wouldn't accidentally cause new false matches. He acknowledges Jeff's benchmarking data showing the strncmp() optimization remains significantly faster than byte-by-byte comparison, validating the approach taken in the patches.

The exchange confirms the technical consensus reached in the thread: the "minusone" solution with one character of context preservation correctly handles the `**` patterns while maintaining performance benefits. Junio's concerns about potential side effects appear resolved, and he thanks Jeff for the performance measurements that helped validate the approach.

This email effectively serves as the final approval for the patch series, with no outstanding objections remaining. The thread shows how careful benchmarking and iterative review led to a solution that preserves optimization benefits while fixing the documented behavior.