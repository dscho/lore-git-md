This thread addresses an inconsistency in Git's config file handling where `git config list --global` didn't properly reflect that Git actually reads both `$HOME/.gitconfig` and `$XDG_CONFIG_HOME/git/config` for global configuration. The patch series, reviewed by Johannes Schindelin, aligns the listing behavior with the actual application behavior.

The core implementation modified `config_sequence()` to properly handle global scope by reading both config files when `--global` is specified. The series initially introduced a temporary regression where the command would incorrectly succeed when both global config files were unreadable, but this final patch fixes that edge case. It adds tracking of successful reads and properly fails when neither file is accessible, matching Git's documented behavior.

The changes have been thoroughly tested across platforms, with Johannes Schindelin verifying the Windows-specific aspects. The series maintains backward compatibility while making the listing behavior more accurate. With this final fix, the implementation appears complete and ready for merging, having addressed all identified issues in a technically sound manner.