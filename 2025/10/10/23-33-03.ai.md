This is a refactoring patch that cleans up an outdated comment in `commit.c`. The change comes from an Outreachy participant, suggesting it may be part of their internship work.

The patch modifies a comment in `parse_commit_buffer()` that describes why the function clears the parent list before re-parsing a commit. The old comment suggested this was a workaround for failed parses, while the new version presents it as a deliberate cleanup step to ensure a fresh parsing state. The actual code change is minimal - just the comment text is updated while the surrounding logic remains identical.

The new comment provides a more positive framing of the operation, describing it as a proactive measure rather than a leftover from error handling. This appears to be a straightforward documentation improvement with no behavioral implications. The patch is cleanly formatted and follows Git project conventions, though it doesn't include any test updates since it's purely a comment change.

Key technical details for future reference: The change affects the parsing logic in `commit.c`, specifically around how commit parent lists are handled during re-parsing. The function `parse_commit_buffer()` is a core part of Git's object parsing infrastructure. The modified comment now better reflects the intentional design rather than implying it's working around past issues.