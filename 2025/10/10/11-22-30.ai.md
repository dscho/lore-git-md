This is a minor test cleanup patch in the refs optimization series. Patrick Steinhardt points out a cosmetic issue in patch 4/9 where removing the `test_done` call from the shared test script left a trailing empty line. The change is part of reorganizing test infrastructure for the larger refs optimization work, where test helpers are being moved to callee scripts.

The patch itself is straightforward - it removes the `test_done` that was previously at the end of the shared test script, since that responsibility is now handled by the individual test scripts that source this file. The only remaining issue is the cosmetic empty line, which Patrick notes should be cleaned up for consistency. This doesn't affect functionality but maintains code hygiene expected in the test suite.

The change touches only `t/pack-refs-tests.sh` and is part of the preparatory work for the refs optimization series. While trivial, it shows attention to detail in the test reorganization that supports the larger feature. The thread context indicates this is one of several preparatory patches standardizing interfaces before introducing the new `--required` flag functionality.