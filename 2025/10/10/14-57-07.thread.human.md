This thread addressed a subtle bug in Git's handling of `**` patterns in `.gitignore` and `.gitattributes` files, where patterns like `foo**/bar` incorrectly matched paths like `foobar` despite documentation requiring slashes around `**` for special meaning. Jeff King (Peff) diagnosed the issue in `dir.c`'s `match_pathname()` function, where an optimization that strips matching prefixes before calling `fnmatch()` was inadvertently losing directory boundary context.

After benchmarking alternatives, Jeff proposed a "minusone" solution that preserves one character of context during prefix stripping. His measurements showed this maintained a 1.46x speedup for exact matches while fixing the bug. Junio Hamano initially suggested special-casing `**/` patterns but ultimately agreed with Jeff's more general approach after reviewing the performance data and implementation.

The resolution comes as a clean two-patch series: first reorganizing the code to check complete matches earlier (improving clarity), then implementing the one-character preservation fix. New test coverage verifies the corrected behavior. Junio's latest email confirms the technical consensus, addressing his earlier concerns about potential side effects, and effectively approves the series for merging. This demonstrates Git's careful approach to balancing optimization with correctness, where benchmarking and iterative review led to a solution that preserves performance while fixing the documented behavior.