This is a follow-up in the CI/build system thread about Ubuntu 25.10's sudo-rs compatibility issues. Brian m. carlson provides additional context by noting they've filed an upstream feature request with sudo-rs to implement the missing `--preserve-env` functionality (issue #1299). This suggests the current workaround using the C-based sudo may only be needed temporarily if the Rust implementation adds this capability by Ubuntu 26.04. The email doesn't propose any code changes but connects the Git CI issue to broader ecosystem concerns, indicating the problem affects other projects as well. The tone suggests this is more an informational update than a technical contribution, keeping others aware of parallel efforts to resolve the root cause rather than just working around it in Git's CI scripts.