This is a refactoring patch that's part of a larger series introducing a `--required` flag for `git refs optimize`. It's the first of nine patches in the series.

The patch standardizes how reference backends handle optimization operations by consolidating around the `.optimize` function in `struct ref_storage_be`. Previously, some backends used `.pack_refs` while others used `.optimize` for similar functionality. The change removes the redundant `.pack_refs` function pointer from all backends (files, packed, reftable, and debug) and updates their implementations to use only `.optimize`.

The changes are mechanical and don't affect behavior - each backend's optimization logic remains the same, just accessed through a consistent interface. The patch notes this is preparation for later changes in the series that will build on this unified interface. The commit message explains that all existing callers already use `.optimize`, so no caller-side changes are needed.

The diff shows straightforward changes across four backend implementations, removing duplicate code paths and updating function names. The changes appear to be well-contained and properly documented, with clear commit message explaining the change's role in the larger series. This is a typical preparatory refactoring that simplifies the codebase before adding new functionality.