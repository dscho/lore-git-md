Platform compatibility follow-up regarding clang warning suppression in refs/files-backend.c.

Junio Hamano responds to Patrick Steinhardt's alternative implementation suggestion for the clang `-Wunreachable` warning fix. While acknowledging Patrick's macro-based approach would be cleaner by localizing the `NOT_CONSTANT()` workaround to the `NO_SYMLINK_HEAD` case, Junio considers the matter settled with Johannes Schindelin's original callsite solution. The rationale is that the current implementation works correctly and further iterations aren't worth the churn, especially since this is the only callsite of `create_ref_symlink()`.

This email confirms consensus on the technical approach while appreciating Patrick's sharp code review. The discussion reinforces that both solutions produce identical compiled output, with the choice between them being primarily about code organization philosophy rather than functional differences. The thread appears concluded with no outstanding technical questions, maintaining the path toward eventual `core.preferSymlinkRefs` deprecation in Git 3.0.