This is a feature patch series introducing a new `--required` flag for `git refs optimize`, aimed at improving performance in GitLab's Gitaly service. The series comes from a clear production need where the ability to check if reference optimization is needed (without actually running it) would allow more efficient use of read snapshots in their transaction manager.

The first four patches are preparatory cleanups around the existing reference optimization code, with the author noting some could potentially be dropped. Patch 5 updates documentation tests to properly handle subcommands. The core functionality is built in patches 6-8, which add infrastructure to check optimization requirements across different ref backends (files, packed, reftable). The final patch 9 introduces the user-facing `--required` flag.

The changeset is substantial (21 files modified) but appears well-structured and motivated by a concrete use case. The GitLab context suggests this will see real-world usage, though the implementation touches multiple ref backends which may warrant careful review. The series builds on recent `git refs optimize` work and includes test updates, though the documentation additions are minimal (just flag mentions).