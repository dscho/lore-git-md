This is a test infrastructure discussion in the context of the ongoing signature handling work.

Patrick Steinhardt responds to Christian Couder's patch that modifies the GPG test prereq setup in t/lib-gpg.sh. The change adds a NEEDSWORK comment explaining why `prepare_gnupghome()` isn't be called in the GPG2 prereq check - doing so would expose a hidden bug in t1016-compatObjectFormat.sh where some tests incorrectly run instead of being skipped.

Steinhardt confirms his understanding that the current behavior (where GPG commands fail due to missing home directory) is acceptable for now, though not ideal. He notes this doesn't need to block the current signature handling series. The response indicates the remaining patches in the series have addressed his previous feedback.

This is a minor technical discussion about test infrastructure quirks that surfaced during the broader signature handling work. The main takeaway is that reviewers are comfortable proceeding with the known-but-unrelated test issue marked for future resolution.