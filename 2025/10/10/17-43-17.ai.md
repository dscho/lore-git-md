This is a documentation patch in Jean-Noël Avila's ongoing series converting Git man pages to Asciidoc synopsis style. The email discusses a specific formatting edge case that emerged after the git-stash.adoc changes were merged into the 'next' branch.

The thread had previously noted that command references in conditional Asciidoc blocks (like `ifndef::git-stash[]`) need special handling - they should use backticks (`show`) rather than single quotes ('show') for proper verbatim formatting when included elsewhere. Jean-Noël acknowledges this Asciidoc processing behavior but explains that after investigation, no clean solution works across both asciidoc.py and asciidoctor toolchains.

The key technical detail is the treatment of the 'show' command reference in the stash.adoc config file's conditional attribute. While the current merged version uses single quotes, Jean-Noël confirms the ideal formatting would use backticks for verbatim rendering, but practical toolchain limitations prevent a cross-compatible solution. The decision is to leave the current formatting as-is rather than attempt a problematic reroll.

This exchange demonstrates the attention to detail in the documentation standardization effort and the practical considerations when dealing with multiple Asciidoc processing tools. The patch remains technically correct despite the minor formatting compromise, and no further action is planned on this specific issue.