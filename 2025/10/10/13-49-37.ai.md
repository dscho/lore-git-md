Follow-up in platform compatibility thread regarding clang warning suppression in symlink ref handling. Johannes Schindelin responds to Patrick Steinhardt's alternative implementation suggestion, advocating to keep the `NOT_CONSTANT()` workaround at the callsite in `create_symref_locked()` rather than moving it to the `create_ref_symlink()` macro definition.

The technical debate centers on code organization philosophy - Johannes prefers compiler workarounds near the warning symptom (the `if` condition checking the symlink creation result) while Patrick's approach would centralize it in the macro. Both solutions produce identical compiled output and runtime behavior, as previously established in the thread. This email represents the final implementation decision point before applying the fix.

The exchange confirms the thread's prior consensus on using the `NOT_CONSTANT()` pattern from commit 82e79c63642c, with only placement remaining as the open question. Johannes's position maintains the original patch's structure while acknowledging Patrick's technically equivalent alternative. No new technical arguments are introduced - this is a maintainer preference decision about warning suppression location in the codebase.

Context from earlier in the thread remains relevant: the fix addresses a clang `-Wunreachable` false positive on `NO_SYMLINK_HEAD` platforms, with broader agreement that the `core.preferSymlinkRefs` functionality should be deprecated in Git 3.0. The immediate fix preserves current behavior while marking the code for future removal.