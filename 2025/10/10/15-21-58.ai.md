Documentation patch clarifying `git blame` output format. This is a simple but useful improvement to the manpage that explains the meaning of "sourceline" and "resultline" in `git blame --incremental` output, which was previously unclear.

The patch adds explicit documentation for these terms based on a Stack Overflow question that revealed the confusion. The explanation distinguishes that "sourceline" refers to the line number in the original commit being blamed, while "resultline" is the line number in the final output. This matches the implementation behavior but wasn't previously documented.

The change is straightforward - just 7 lines added to `git-blame.adoc` with 2 lines modified. While the author notes they couldn't locally verify the manpage build due to docbook toolchain issues, the change is simple enough that this seems low-risk. The patch fills a genuine documentation gap that had caused real user confusion, as evidenced by the Stack Overflow reference.

For future thread context: this touches only documentation, specifically the `git-blame.adoc` file, adding clarification about the `--incremental` output format's line number fields. No behavior changes or code modifications are involved. The Stack Overflow link provides additional context about the real-world confusion this addresses.