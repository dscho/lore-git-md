Here's the updated human-readable summary:

The thread addresses a CI/build system compatibility issue with Ubuntu 25.10's switch to sudo-rs (a Rust-based sudo replacement). Patrick Steinhardt initially reported that the missing `--preserve-env` flag support breaks Git's CI pipelines. The solution involves modifying `ci/install-dependencies.sh` to detect Ubuntu 25.10+ and fall back to the traditional C-based sudo via the alternatives system.

Junio C Hamano refined the approach by suggesting checking for the binary's existence rather than just the alternatives config, and narrowing the scope to Ubuntu specifically. Patrick implemented these changes, noting that checking `/etc/alternatives/sudo` works for detection while remaining open to additional checks if needed. Brian m. carlson provided context about filing an upstream feature request with sudo-rs (issue #1299) to implement the missing functionality.

The thread has now reached consensus on maintaining a two-pronged approach: keeping the immediate CI workaround (falling back to C-based sudo) while supporting the upstream effort for a proper fix in sudo-rs itself. Junio's latest response confirms the maintainers view the upstream solution as preferable but are content with the temporary workaround until sudo-rs implements the feature. No further Git-side changes are needed unless the upstream situation evolves. The discussion remains focused on ensuring CI stability while leaving room for future simplification if sudo-rs adds the needed functionality.