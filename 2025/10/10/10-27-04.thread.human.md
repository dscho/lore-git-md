This thread introduces a `--required` flag for `git refs optimize`, primarily to support performance optimizations in GitLab's Gitaly service where read operations are cheaper than writes. The series begins with preparatory cleanups, with the first patch consolidating reference backend operations around a unified `.optimize` interface. The latest patch continues this cleanup by removing the now-redundant `pack_refs` function pointer and associated wrapper, since all backends now use the `optimize` interface exclusively.

The changes are well-contained refactorings that maintain existing behavior while simplifying the codebase, with updates to documentation and tests. The series appears to be following a clear progression: first establishing a cleaner foundation through these mechanical changes, before introducing the new `--required` flag functionality in later patches. The work builds on recent ref optimization improvements and requires coordinated changes across multiple ref backend implementations, but so far the changes seem uncontroversial and well-motivated by GitLab's specific performance needs in Gitaly.