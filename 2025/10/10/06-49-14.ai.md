This is a test infrastructure discussion in the context of the signature handling series.

Patrick Steinhardt responds to Christian Couder's patch modifying the GPG2 test prereq in t/lib-gpg.sh. The patch adds a NEEDSWORK comment explaining why prepare_gnupghome() isn't being called in the GPG2 prereq check - doing so would expose a pre-existing bug in t1016-compatObjectFormat.sh where some tests incorrectly run instead of being skipped when the home directory doesn't exist.

Steinhardt confirms his understanding that the current GPG commands fail when the home directory is missing, causing the prereq to be disabled. He agrees this issue doesn't need to block the current signature handling series, and gives his approval for the remaining patches in the series that address his previous feedback.

The discussion highlights an existing test infrastructure quirk that will need future investigation (marked with NEEDSWORK), but doesn't require changes to the core signature handling functionality being implemented in this series. This is a pattern seen throughout the thread where peripheral test issues are noted but separated from the main cryptographic signature work.