Here is the updated AI thread summary:

CI / build system fix for Ubuntu 25.10 compatibility. Patrick Steinhardt reports that Ubuntu 25.10's switch to sudo-rs (Rust-based sudo) breaks Git's CI pipelines due to missing `--preserve-env` flag support. The initial patch modifies `ci/install-dependencies.sh` to detect Ubuntu 25.10+ and switch back to traditional C-based implementation (`sudo.ws`) via the alternatives system when available. Junio C Hamano suggests refinements: (1) making the fallback conditional on binary existence rather than just alternatives config, and (2) narrowing scope to Ubuntu systems specifically rather than all Debian-based distros. The discussion focuses on technical implementation details while maintaining the core approach of using alternatives to select working sudo implementation. The fix remains targeted at resolving active CI breakage with minimal risk, though may need future adjustment if Ubuntu phases out C-based sudo entirely. The thread shows progress toward a robust solution with maintainer input on making the detection more precise.