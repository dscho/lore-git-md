Here is the updated AI thread summary:

CI / build system fix for Ubuntu 25.10 compatibility. Patrick Steinhardt reports that Ubuntu 25.10's switch to sudo-rs (Rust-based sudo) breaks Git's CI pipelines due to missing `--preserve-env` flag support. The patch modifies `ci/install-dependencies.sh` to detect Ubuntu 25.10+ and switch back to traditional C-based implementation (`sudo.ws`) via the alternatives system when available. The change checks for alternatives configuration and uses `update-alternatives` to select working implementation, affecting both GitHub and GitLab CI. The fix is targeted to resolve immediate CI breakage with minimal risk, though may need future adjustment if Ubuntu phases out C-based implementation entirely. The patch appears ready for fast-tracking given active CI impact.