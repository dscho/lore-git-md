Here is the updated AI thread summary:

CI / build system fix for Ubuntu 25.10 compatibility. Patrick Steinhardt reports that Ubuntu 25.10's switch to sudo-rs (Rust-based sudo) breaks Git's CI pipelines due to missing `--preserve-env` flag support. The initial patch modifies `ci/install-dependencies.sh` to detect Ubuntu 25.10+ and switch back to traditional C-based implementation (`sudo.ws`) via the alternatives system when available. Junio C Hamano suggests refinements: (1) making the fallback conditional on binary existence rather than just alternatives config, and (2) narrowing scope to Ubuntu systems specifically rather than all Debian-based distros. Brian m. carlson adds context about filing an upstream feature request with sudo-rs (issue #1299) to implement the missing functionality, suggesting the workaround may be temporary if sudo-rs adds `--preserve-env` support by Ubuntu 26.04. The discussion now includes both the immediate solution (conditional fallback to C-based sudo) and longer-term tracking of the upstream fix. The thread shows progress toward a robust solution with maintainer input on precise detection and awareness of parallel ecosystem efforts to resolve the root cause. The technical focus remains on the CI script modifications while acknowledging this may evolve based on sudo-rs development.