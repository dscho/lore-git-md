Here is the updated AI thread summary:

CI / build system fix for Ubuntu 25.10 compatibility. Patrick Steinhardt reports that Ubuntu 25.10's switch to sudo-rs (Rust-based sudo) breaks Git's CI pipelines due to missing `--preserve-env` flag support. The initial solution modifies `ci/install-dependencies.sh` to detect Ubuntu 25.10+ and fall back to traditional C-based sudo (`sudo.ws`) via the alternatives system.

Junio C Hamano suggested refinements: (1) making the fallback conditional on binary existence rather than just alternatives config, and (2) narrowing scope to Ubuntu specifically. Patrick Steinhardt implemented these changes, confirming that checking `/etc/alternatives/sudo` is currently sufficient for detection while remaining open to additional checks if needed. He notes Ubuntu has dropped 32-bit support, making the i386/ubuntu case irrelevant, with plans to migrate 32-bit CI to i386/debian.

Brian m. carlson provided context about filing an upstream feature request with sudo-rs (issue #1299) to implement `--preserve-env`. Junio C Hamano's latest response acknowledges this upstream effort and confirms alignment with the two-pronged approach: maintaining the immediate CI workaround (fallback to C-based sudo) while supporting the upstream effort for a proper fix in sudo-rs itself. The thread has reached consensus on the technical solution implemented in `ci/install-dependencies.sh` and now shifts focus to tracking upstream development. 

The maintainers view the upstream solution as preferable but are content with the temporary workaround until sudo-rs implements the feature. No further action is required from the Git side unless the upstream situation changes. The discussion remains focused on ensuring CI stability while keeping open the possibility of future simplification if sudo-rs adds the needed functionality. Patrick Steinhardt's initial implementation with Junio's refinements stands as the current solution, with Brian m. carlson's upstream engagement providing a potential future resolution path.