This is a refactoring patch in Taylor Blau's ongoing series to modularize `builtin/repack.c`. The patch moves the `existing_packs_remove_redundant` function to its own compilation unit, passing the `packdir` parameter explicitly rather than using a static variable.

The key technical point is that while the current approach works (passing `packdir` as a parameter), both Taylor and reviewer Jeff King recognize it creates a brittle API since `packdir` could theoretically be derived from the repository object already available in the struct. However, Taylor prefers to keep this minimal change for now as part of the code movement, with plans to improve the API in follow-up work.

This continues the pattern seen in earlier patches where Taylor makes conservative changes to enable the refactoring first, deferring API improvements to later. The discussion shows consensus that this is a reasonable approach for the series' goals, though the brittleness is noted as something to address eventually. The patch appears uncontroversial and aligns with the series' incremental refactoring strategy.