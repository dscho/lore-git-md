This is a follow-up to a const correctness bugfix in the patch-id comparison logic, now addressing submission format issues.

Okhuomon Ajayi's v2 patch fixes a const correctness violation in `patch_id_neq()` while removing an obsolete NEEDSWORK comment. The technical content remains sound - it properly types the callback data parameter as const while maintaining identical runtime functionality. The change is minimal (2 lines modified) and focused solely on type safety improvements in patch-ids.c.

Agatha Isabelle's review confirms the technical correctness but notes several submission process issues that need addressing: the patch lacks a proper `[PATCH v2]` prefix in the subject, is missing a `Signed-off-by` trailer, and places the version changelog incorrectly within the commit message rather than the email body. These are standard requirements for Git project contributions.

The thread shows a typical new-contributor workflow where technical improvements are validated first, followed by submission process guidance. The patch itself remains uncontroversial - a straightforward cleanup that eliminates a const-cast warning while improving code hygiene. Once the submission format issues are addressed, this appears ready for inclusion.