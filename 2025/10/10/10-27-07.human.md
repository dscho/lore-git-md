Refactoring patch in a series introducing `--required` flag for `git refs optimize`. This is the third patch in a 9-part series that aims to support performance optimizations for GitLab's Gitaly service.

The patch continues the cleanup from previous commits by renaming `pack_refs_opts` to `optimize_refs_opts` and updating all related flag names (e.g., `PACK_REFS_PRUNE` becomes `OPTIMIZE_REFS_PRUNE`). This change affects multiple files across the refs subsystem, including backend implementations and header files, to maintain naming consistency after the removal of `pack_refs()` functionality.

The changes are mechanical and thorough, updating all relevant function signatures, struct definitions, and flag names. The patch appears to be a straightforward continuation of the series' refactoring work, with no behavior changes - just aligning names with the new optimization-focused terminology.