Junio Hamano's follow-up in the commit-graph changed-paths config discussion. This email focuses on clarifying the behavior of the `--no-changed-paths` option versus the proposed config setting, with particular attention to documentation needs.

Junio acknowledges Derrick Stolee's explanation of the tri-state behavior (write/remove/preserve) for the command-line flags, noting that `--no-changed-paths` acts more like a "strip existing" operation than a simple negation. He highlights a potentially confusing case where a repo-specific `false` config overrides a global `true` setting but doesn't remove existing filters, contrary to what users might expect from the `--no-` flag behavior.

The key technical point is the distinction between the command-line interface's destructive `--no-changed-paths` and the config's non-destructive `false` value. Junio suggests the documentation should explicitly state that `false` in config only prevents new filter writes without removing existing ones, unlike the command-line flag. He proposes clearer wording that directly compares the config settings to their command-line equivalents (`true` = `--changed-paths`, `false` = no new writes but preserves existing).

This email moves the discussion toward documentation requirements while implicitly accepting the asymmetric design rationale Stolee presented earlier. The focus on edge cases in config precedence (global vs. repo-specific) and behavioral expectations sets up important context for Emily Yang's upcoming monorepo-focused contributions that will test these design decisions at scale.