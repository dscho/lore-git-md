This thread begins with a straightforward documentation improvement to `commit.c`, submitted by an Outreachy participant. The patch updates a comment in `parse_commit_buffer()` to better explain why the function clears the parent list before re-parsing a commit. Rather than framing this as a workaround for failed parses (as the old comment did), the new version presents it as a deliberate cleanup step to ensure a fresh parsing state. The change is purely textual - the surrounding logic remains unchanged - and serves to clarify the intentional design of this core object-parsing function.

The patch touches an important but subtle aspect of Git's commit parsing infrastructure, though the change itself is minor and uncontroversial. As a documentation-only change from a new contributor, this appears to be a clean, low-risk improvement that helps future readers understand the code's intent.