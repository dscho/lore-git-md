This is a refactoring patch that's part of a larger series introducing a `--required` flag for reference optimization operations. The patch focuses on standardizing the backend interface by moving from the files-backend-specific `pack_refs` function to the more generic `optimize` callback that works across all reference backends.

Patrick Steinhardt provides a code review of Karthik Nayak's patch, noting that the commit message could be clearer about the transition being made. He points out that the `pack_refs_opts` struct name should ideally be updated to match the new terminology (which the later patches in the series do address). The change itself is straightforward - renaming the callback function while keeping the same underlying behavior.

This is preparatory work for the series' main goal of adding optimization checking functionality. The patch maintains existing behavior while cleaning up the interface, making it easier to add the new `--required` flag in subsequent commits. The review is constructive, focusing on naming consistency which is important for the series' goal of unifying the reference optimization interfaces.