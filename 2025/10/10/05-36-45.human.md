This is a platform compatibility patch addressing a clang warning in the refs backend. The thread has already established that the warning suppression approach is reasonable, with two equivalent implementation options on the table (one from Johannes Schindelin modifying the callsite, another from Patrick Steinhardt modifying the function definition). 

Patrick's email here is a brief agreement with Junio Hamano's suggestion to eventually deprecate the `core.preferSymlinkRefs` configuration option, noting that symbolic ref writing could be phased out while keeping read support temporarily. The technical discussion about the warning suppression appears settled, with no new implementation concerns raised in this message.

The patch itself (not shown in this email but referenced from the thread) is a straightforward 7-line change using Git's existing `NOT_CONSTANT()` pattern to silence a clang warning about unreachable code on platforms without symlink support. All participants agree the change is correct, with the remaining discussion being about the long-term plan for symlink ref handling rather than the immediate fix.