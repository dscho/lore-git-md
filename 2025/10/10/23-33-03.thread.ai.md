Thread identity: Comment cleanup in `commit.c` regarding commit parsing behavior, from an Outreachy participant.

Core problem/goal: Improve accuracy of comment in `parse_commit_buffer()` to better reflect the intentional design of clearing parent lists during commit re-parsing, rather than implying it's a workaround for past issues.

Current status: Initial version of comment-only patch submitted, no behavioral changes. No version history yet as this is the first submission.

Key technical details: Modifies comment in `commit.c`'s `parse_commit_buffer()` function about why parent list is cleared before re-parsing commits. Old comment framed this as error handling workaround, new version presents it as deliberate cleanup to ensure fresh parsing state. Core parsing logic remains unchanged - only comment text updated.

Participant positions: Outreachy participant submitted patch, no reviews or responses yet. Patch follows Git conventions but lacks test updates (not strictly needed for comment-only change).

Related work: Part of Outreachy internship contributions, though not tied to any larger ongoing technical effort. Similar documentation cleanups occur regularly in the codebase.