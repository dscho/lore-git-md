Thread identity: Test fix for t1016-compatObjectFormat.sh regression in GPG configuration persistence across test cases.

Core problem: Commit c348192a (2024-10-22) inadvertently changed a persistent `git config` call to ephemeral `test_config` when setting gpg.program, breaking test case dependencies where later tests rely on configuration from earlier ones.

Current status: Junio Hamano identified this as a review oversight. The fix reverts to using `git config` for gpg.program setup, restoring the intended test behavior where configuration persists across test cases.

Key technical details: The change affects only test infrastructure in t1016-compatObjectFormat.sh, specifically how GPG program configuration is established. No production code changes are involved. The fix is minimal - a one-line change from `test_config` back to `git config` for the gpg.program setting.

Participant positions: Junio Hamano noted this was a review oversight that should have been caught earlier. The fix appears uncontroversial as it restores clearly intended behavior.