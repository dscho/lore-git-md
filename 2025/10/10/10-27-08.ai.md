This is a test refactoring patch in the ongoing refs optimize series. It fixes an issue with test organization where the `test_done` directive was incorrectly placed in the shared `pack-refs-tests.sh` script after a previous refactoring. The patch moves this directive back to the individual test scripts (`t0601-reffiles-pack-refs.sh` and `t1463-refs-optimize.sh`) that include the shared test suite, allowing additional tests to be added to either script while maintaining proper test flow control.

The change is straightforward - removing `test_done` from the shared file and adding it to both including scripts. This follows good test organization practices by keeping test flow control in the main test scripts rather than shared libraries. The patch references commit ac0bad0af4 which introduced the current structure, and explains why this change is needed for future test additions.

This is a preparatory change in the larger series (patch 4/9) that's cleaning up test infrastructure before adding the new `--required` flag functionality. The modification affects three test files but doesn't change any actual test logic - it just fixes the test script structure. The change appears well-contained and correctly motivated by the need to enable future test additions.