**Documentation issue regarding pattern matching behavior**

Sruteesh Kumar reports a discrepancy between Git's actual behavior and its documented behavior regarding double-asterisk (`**`) pattern matching in `.gitignore` files. The issue was originally raised in the Eclipse JGit project (issue #217) where Git matches `foobar` against the pattern `foo**/bar`, while the documentation states this should not match.

This appears to be either a documentation bug (if the current behavior is correct but undocumented) or a code bug (if the implementation doesn't match the spec). The email doesn't take a position on which is wrong, just points out the inconsistency. The reference to the Git documentation's "double asterisk" section suggests this involves the recursive directory matching rules.

The email is brief and doesn't include any analysis of why the behavior exists or whether it might be intentional, so this would need investigation by someone familiar with Git's pattern matching implementation. The issue could affect interoperability between Git and JGit implementations.