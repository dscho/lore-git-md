This is a small refactoring patch from an Outreachy participant that cleans up an outdated comment in `commit.c`. 

The patch updates the comment above `parse_commit_buffer()` to better explain why the parent list is being freed before re-parsing a commit. The old comment suggested this was handling leftover state from a failed parse, while the new version more accurately describes it as ensuring a clean slate for re-parsing.

The change is straightforward - just a comment update with no functional changes. The patch appears to be part of the contributor's onboarding work, focusing on code clarity rather than fixing any actual bug. The new wording is more precise about the function's behavior, though the practical impact is minimal since this is just documentation.