Refactoring patch in the xdiff subsystem cleanup series. This is the first of nine patches preparing xdiff data structures for better Rust FFI compatibility by improving type safety and field organization.

The patch changes the type of dstart and dend fields in xdfile_t from long to ssize_t, which is more appropriate for pointer-relative offsets that can be negative. It also moves these fields to the end of the struct, noting this will minimize disruption when they are moved to a different struct in a future patch. The change is minimal (2-line diff) and focused on xtypes.h.

This continues the thread's established pattern of precise type changes to xdiff's core data structures. The modification appears behavior-preserving while improving type correctness. The field reordering is explicitly motivated by upcoming changes in the series, showing thoughtful sequencing of the refactoring steps. Reviewers will likely check for any subtle alignment or padding implications from the field movement, though the change seems straightforward.