This is part 11/49 of Taylor Blau's refactoring series to modularize `builtin/repack.c`, focusing on splitting out functionality into dedicated compilation units. The patch moves the `pack_objects_args` struct and its cleanup function into new `repack.[ch]` files, marking the start of breaking up the monolithic repack implementation.

The change is straightforward - it extracts the struct definition and `pack_objects_args_release()` function from `builtin/repack.c` into new header and implementation files, updating the build systems accordingly. This is a mechanical first step in the larger effort to split up the repack functionality, which has grown to handle many features (pack generation, geometric repacking, cruft packs, MIDX writing) in one large file.

The patch follows the series' pattern of careful, incremental changes - this first extraction is simple and non-controversial, establishing the new compilation unit that will presumably house more repack-related functionality in future patches. The commit message clearly explains the motivation (making the code easier to work with) and notes this is just the beginning of the split.