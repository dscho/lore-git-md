This is a technical discussion about the ongoing repack refactoring series (tb/incremental-midx-part-3), specifically addressing patch 41/49 which deals with cruft pack writing.

Jeff King (Peff) is responding to Taylor Blau's changes that refactored `write_cruft_pack()` to use a `write_pack_opts` struct rather than individual parameters. Peff notes that under his earlier suggestion (to move `pack_prefix` calculation into called functions), they'd need to call `find_pack_prefix()` within `write_cruft_pack()` itself. However, he observes this would be offset by removing the prefix calculation from the caller (`cmd_repack`), making it a neutral change in terms of line count.

The discussion shows careful consideration of interface design tradeoffs in the repack refactoring. Peff's comment suggests this is a blocking point that Taylor will need to address in the next version of the series. The exchange demonstrates the thorough review process this large refactoring is undergoing, with multiple reviewers scrutinizing interface details.