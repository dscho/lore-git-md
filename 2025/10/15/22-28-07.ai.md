This is patch 16/49 in Taylor Blau's incremental MIDX repack refactoring series (tb/incremental-midx-part-3), focusing on modularizing repack functionality. The patch moves the `remove_redundant_pack()` function from `builtin/repack.c` to the new `repack.[ch]` compilation unit, renaming it to `repack_remove_redundant_pack()` to follow the subsystem's naming conventions.

The function handles cleanup of redundant packfiles during repacking, including MIDX-related cleanup when packs are removed. The move is part of the series' systematic effort to extract repack-specific logic from the builtin into dedicated modules while maintaining behavior. Two call sites are updated to use the new location - one in the pack geometry code (which will eventually move to its own module) and another in the main repack logic.

The patch demonstrates the series' pattern of mechanical conversions: moving code while preserving functionality, establishing clear subsystem boundaries, and applying consistent naming (`repack_` prefix for repack-specific functions). This continues the architectural restructuring while keeping the focus on incremental, reviewable changes. The change is straightforward with no apparent controversy, following the established pattern from previous patches in the series.