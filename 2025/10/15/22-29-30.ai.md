This is a refactoring patch in Taylor Blau's ongoing repack modularization series (tb/incremental-midx-part-3), specifically moving `finish_pack_objects_cmd()` out of the builtin.

The patch relocates the `finish_pack_objects_cmd()` function from `builtin/repack.c` to `repack.c` and adds its declaration to `repack.h`. This continues the series' pattern of extracting repack functionality into dedicated compilation units. The function processes output from `pack-objects`, validating object IDs and populating pack names, with special handling for local vs. non-local packs.

Key technical details:
- Function signature remains unchanged: takes `git_hash_algo`, `write_pack_opts`, `child_process`, and `string_list` parameters
- Uses the recently introduced `write_pack_opts_is_local()` helper (from patch 43/49)
- Maintains all existing behavior including pack name population via `generated_pack_populate()`
- Follows the series' pattern of moving functions once they have no builtin-specific dependencies
- Prepares for future extraction of `write_cruft_pack()` and `write_filtered_pack()`

The change is straightforward with no behavior modifications, continuing the series' systematic approach to modularization. It builds on previous infrastructure like the `write_pack_opts` struct and follows established patterns from earlier patches in the series. The patch appears well-contained and maintains the series' focus on incremental, behavior-preserving refactoring.