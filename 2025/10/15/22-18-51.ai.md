This is a refactoring patch in a series standardizing refs optimization terminology, specifically renaming `pack_refs_opts` to `refs_optimize_opts` and related flags (`PACK_REFS_*` to `REFS_OPTIMIZE_*`). The changes are mechanical and consistent with the series' goal of moving from legacy `pack_refs` terminology to the newer `optimize` vocabulary.

The patch updates 8 files where these types and flags are referenced, including core refs files (`refs.c`, `refs.h`), backend implementations (files, packed, reftable, debug), and the `pack-refs.c` command interface. Justin Tobler notes the changes look good overall, while suggesting two minor follow-up cleanups that could be considered: (1) renaming the local `pack_refs_opts` variable in `pack-refs.c` to match the new type name, and (2) renaming the unused `pack_opts` parameter in `packed-backend.c` to simply `opts`. These are presented as optional suggestions rather than required changes.

The changes maintain identical behavior while improving codebase consistency, continuing the series' work to eliminate technical debt from the transition to the `optimize` callback interface. The patch follows naturally from the previous removal of `pack_refs()` references, with no objections or concerns raised about the core approach. Justin's feedback focuses on potential additional naming polish rather than substantive issues.