This is part 25/49 of Taylor Blau's repack refactoring series, continuing the systematic elimination of static variables in favor of explicit parameters. The patch modifies `repack_promisor_objects()` to accept a `packtmp` parameter instead of relying on a static variable, following the pattern established in previous patches.

The change is mechanical and straightforward - the function signature is updated to include the new parameter, and the single call site in `cmd_repack()` is updated to pass its local `packtmp` variable. This maintains identical behavior while improving modularity, preparing for the function's eventual move to repack.[ch] in a future patch.

The patch characteristics align with the series' established patterns: type-safe changes (using `const char*` for the new parameter), no behavior modifications, and clear mechanical steps toward the larger goal of modularizing repack functionality. This continues the series' careful balance between interface improvements and maintaining working code, with each change being small and reviewable.

Key technical details:
- Adds `packtmp` parameter to `repack_promisor_objects()`
- Updates call site in `cmd_repack()` to pass local `packtmp`
- Maintains all existing functionality
- Prepares for future code movement to repack.[ch]
- Follows series pattern of replacing static variables with parameters
- Part of systematic elimination of implicit state in repack machinery