This is part of Taylor Blau's ongoing refactoring series to remove `the_hash_algo` global usage in the repack machinery (patch 9/49 in v2). The change modifies `finish_pack_objects_cmd()` to accept a `git_hash_algo` parameter instead of relying on the global, continuing the systematic pattern seen in earlier patches of the series.

The patch updates all callers of `finish_pack_objects_cmd()` to pass the hash algorithm from their available context (typically via `existing->repo->hash_algo`). The key functional change is replacing `the_hash_algo->hexsz` with `algop->hexsz` in the object ID length validation check. This maintains identical behavior while removing global state dependencies.

The changes are mechanical but span multiple call sites, making the diff appear noisier than previous patches in the series. Each modification follows the established pattern of threading the algorithm through from repository structures rather than using the global. The patch demonstrates careful attention to detail by ensuring all callers (including those in `write_filtered_pack()`, `write_cruft_pack()`, and `cmd_repack()`) properly propagate the hash algorithm parameter.

This continues the series' broader goal of modularizing repack functionality while eliminating global variables, with particular focus on both `the_repository` and `the_hash_algo`. The change is low-risk as it doesn't alter behavior - it simply relocates where the algorithm information comes from. The patch fits cleanly into the incremental refactoring approach that has characterized the series so far.