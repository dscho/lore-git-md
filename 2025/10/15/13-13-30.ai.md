Documentation patch (3/4 in series) removing a potentially confusing example from `git-pull` documentation.

This patch deletes a historical example from `git-pull.adoc` that showed a merge scenario, as user feedback indicated it was misleading about the default behavior (which is actually `--ff-only`). Julia Evans explains that while they could replace it with a fast-forward example, such an example might not add much value since fast-forward merges are conceptually simpler. The removal helps streamline the documentation while avoiding potential misinterpretations.

The change follows the series' pattern of modernizing and clarifying the pull documentation, building on previous patches that restructured parameter documentation and clarified integration options. This deletion aligns with the thread's trajectory toward more precise documentation, though it does raise the question of whether some minimal example might still be pedagogically useful (a point not addressed in this patch).

The diff shows the straightforward removal of 26 lines containing the example and its explanatory text, leaving the surrounding content about integration options and conflict resolution intact. This maintains continuity with patch 2/4's clear listing of the four integration approaches while eliminating outdated material that could confuse users about default behaviors.

Technical context for future thread summaries:
- Removed example implied `git merge` was default when `--ff-only` is actually default
- Example deletion follows series' focus on accuracy and clarity
- No replacement example for fast-forward case, though the possibility is noted
- Maintains link to `git-merge` for conflict handling details
- Part of broader documentation restructuring in v3 series