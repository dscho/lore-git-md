Refactoring patch in Taylor Blau's repack modularization series (48/49). This moves the cruft pack writing functionality out of `builtin/repack.c` into a new `repack-cruft.c` compilation unit, following the same pattern as previous patches in the series.

The patch relocates `write_cruft_pack()` and its helper `combine_small_cruft_packs()` to the new file while maintaining identical functionality. The function is exposed through `repack.h` with the same signature, taking a `write_pack_opts` struct, cruft expiration parameters, and pack lists. The implementation handles pack prefix calculation, `pack_objects` command setup with `--cruft` and expiration flags, and pack list filtering logic.

Build system changes add the new compilation unit to both Makefile and meson.build. This continues the series' systematic approach of moving repack components into dedicated modules while maintaining the existing behavior. The change is part of the broader effort to eliminate global variables and modularize the repack machinery, with `write_cruft_pack()` now properly integrated into the repack API rather than being buried in the builtin implementation.

Key technical details preserved:
- Cruft pack expiration handling via `--cruft-expiration`
- Small pack combining logic via `combine_cruft_below_size`
- Pack list management through `existing_packs` and `string_list` parameters
- Integration with `prepare_pack_objects()` and `finish_pack_objects_cmd()`
- Pack prefix derivation from `write_pack_opts`

The patch follows established patterns from previous refactorings in the series and appears ready for review, though as with other patches in this series, careful examination of the moved code's new context would be warranted.