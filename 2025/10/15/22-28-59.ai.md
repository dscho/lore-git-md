This is patch 34/49 in Taylor Blau's incremental MIDX repack refactoring series, focusing on further modularizing the repack machinery. The patch moves MIDX pack name tracking into the `existing_packs` struct rather than maintaining it as a separate parameter passed between functions.

The change adds a `midx_packs` string_list to `struct existing_packs` and populates it during `existing_packs_collect()`. This eliminates the need for separate MIDX pack name collection and passing, reducing parameter passing between functions. The implementation requires careful ordering of checks in `existing_packs_collect()` to maintain the existing behavior of including non-local packs in the MIDX tracking.

Key technical details:
- Adds `midx_packs` field to `struct existing_packs`
- Updates `existing_packs_collect()` to populate it when `p->multi_pack_index` is true
- Removes standalone `midx_pack_names` string_list and related collection code from `cmd_repack()`
- Simplifies `midx_has_unknown_packs()` and `midx_included_packs()` by removing `midx_pack_names` parameter
- Maintains existing behavior of tracking non-local MIDX packs
- Follows the series' pattern of consolidating related data into structured fields

This continues the series' systematic approach to reducing ad-hoc parameter passing and global state by moving related data into appropriate structs. The change is relatively straightforward but helps further clean up the repack code's organization.