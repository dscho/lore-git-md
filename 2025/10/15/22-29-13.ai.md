Refactoring patch in Taylor Blau's incremental MIDX series (patch 39/49), moving MIDX-specific repack functionality out of the builtin. This patch relocates the `write_midx_included_packs()` function and its supporting code from `builtin/repack.c` to the new `repack-midx.c` compilation unit, along with the `repack_write_midx_opts` struct definition to `repack.h`.

The change is a pure code movement with no behavioral modifications, continuing the series' systematic modularization of repack functionality. Key elements moved include:
- The core `write_midx_included_packs()` function that handles MIDX pack selection logic
- Supporting functions `midx_has_unknown_packs()`, `midx_included_packs()`, and `remove_redundant_bitmaps()`
- The `repack_write_midx_opts` configuration struct that parameterizes MIDX writing

This patch follows the established pattern of the series by separating MIDX-specific concerns from the main repack implementation, while maintaining all existing behavior. The movement helps isolate MIDX functionality in preparation for future incremental MIDX improvements, and is part of the broader effort to eliminate global variables like `the_repository` from the codebase.

The patch appears well-contained and mechanically sound, with 305 lines removed from the builtin and 295 added to the new location (plus 12 lines of header additions). It builds on previous patches in the series that established the `repack-midx.c` unit and related infrastructure.