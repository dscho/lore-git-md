This is a user-facing documentation/UX suggestion regarding the deprecated `git whatchanged` command. Greg Couch proposes improving the deprecation warning message to be more helpful by directly recommending `git log` as the replacement, rather than suggesting users email the mailing list with `--i-still-use-this`.

The current behavior, which requires users to explicitly opt-in to using the deprecated command, is described as "painful" and "unappreciated." The patch appears to be a straightforward request to make the deprecation message more user-friendly by providing clear migration guidance rather than what might be perceived as an obstruction.

While the email doesn't include a formal patch, it raises a valid point about improving the user experience when dealing with deprecated commands. The suggestion seems reasonable and aligned with Git's general approach to providing helpful migration paths for deprecated functionality.