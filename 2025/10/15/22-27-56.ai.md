This is part 12/49 of Taylor Blau's refactoring series for the repack machinery, continuing the modularization effort and removal of global variables. The patch introduces a new `struct repack_config_ctx` to pass both cruft and non-cruft `pack_objects_args` to the `repack_config` callback, preparing for future removal of the `delta_base_offset` static variable.

The change is mechanical but strategic - it creates the plumbing needed to eventually move `delta_base_offset` into `pack_objects_args` in a subsequent patch. The new context struct cleanly bundles the two pack_objects_args pointers that the config function needs to access. The implementation maintains the existing behavior while enabling the next step in the refactoring.

This continues the series' pattern of incremental, behavior-preserving changes that methodically eliminate global state. The patch touches the config callback machinery and repack initialization code, but doesn't yet modify the actual handling of `delta_base_offset`. The change is straightforward with clear testability since it just rearranges how existing parameters are passed through the system.

The patch follows the established style of the series, using memset for initialization and maintaining the separation between cruft and non-cruft pack arguments. It's a typical example of the careful, systematic approach seen throughout this large refactoring effort.