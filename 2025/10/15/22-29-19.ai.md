This is patch 41/49 in Taylor Blau's refactoring series to modularize `builtin/repack.c` for MIDX-based repacking, continuing the effort to eliminate global variables and improve code organization.

The patch extends the use of the new `write_pack_opts` struct (introduced in patch 40/49) to the `write_cruft_pack()` function. This consolidates several parameters (previously passed individually) into a single structured parameter, reducing the function's parameter count from 5 to 2. The changes follow the series' pattern of moving toward more structured parameter passing while maintaining existing behavior.

Key technical changes include:
- Replacing individual parameters (`args`, `destination`, `pack_prefix`) with a single `write_pack_opts` struct
- Updating all internal references to use the struct fields (`opts->destination`, `opts->pack_prefix`, etc.)
- Modifying the call sites in `cmd_repack()` to construct and pass the struct
- Maintaining the same functionality for both regular cruft pack writing and expiration cases

The patch continues the series' systematic approach to interface cleanup while preserving all existing behavior. It builds on the foundation established in the previous patch (40/49) by applying the same parameter consolidation pattern to another key repacking function. Like other patches in the series, this change helps move toward the goal of eliminating global variables while improving code organization and maintainability.

The changes are mechanical in nature and follow the established pattern from earlier in the series, suggesting they should integrate cleanly with the ongoing refactoring effort. The patch maintains the series' careful balance between ideal interfaces and practical backward compatibility requirements.