This is a refactoring series cleaning up code around the recently added `git refs optimize` functionality. It's a follow-up to earlier work that introduced reference optimization, now focusing on unifying the code paths between the older `pack-refs` and newer `optimize` operations.

The 4-patch series:
1. Shifts all callers to use the `.optimize` functions instead of `.pack_refs`
2. Cleans up surrounding optimization code
3. Renames the options struct to match the new terminology
4. Moves test cleanup to individual test scripts

The changes are mechanical renames and consolidations, touching multiple refs backend implementations (files, packed, reftable) and their test suites. The author notes these changes were extracted from a larger abandoned series, but stand on their own as cleanup work.

The patch appears well-scoped and non-controversial, simply aligning naming with the newer optimization terminology. The diffstat shows a net reduction of 66 lines across 11 files, suggesting this is a genuine simplification rather than just churn.