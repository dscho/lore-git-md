This is the second patch in a refactoring series converting strbuf_split usage to string_list_split in gpg-interface.c, focusing on the SSH key fingerprint parsing path. The change affects get_default_ssh_signing_key() which retrieves and processes SSH signing keys.

The patch replaces strbuf_split_max with string_list_split when parsing the output of the ssh-keygen command. The key function processes command output to extract the first valid key, and the change simplifies the code by using string_list's more straightforward API for this read-only parsing case. The logic remains identical - it still checks for valid SSH keys via is_literal_ssh_key() and handles errors the same way - but with cleaner memory management through string_list API.

Technical details show careful handling of the API transition: the string_list_split parameter (1 split producing 2 tokens) matches the previous strbuf_split_max behavior, and the xstrdup replaces strbuf_detach for the key copying. The patch reduces the line count by 4 while maintaining all functionality, including warning messages for error cases.

This follows the same pattern as the first patch in the series (which handled GPG verification paths), demonstrating consistent approach to similar problems in different parts of the file. The change appears well-contained and low-risk, though reviewers may want to verify edge cases in SSH key parsing behavior remains unchanged. The commit message clearly explains the motivation (simplifying code that doesn't need strbuf's mutability features) without overstating benefits.