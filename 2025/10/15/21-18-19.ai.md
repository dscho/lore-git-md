Refactoring patch in a series preparing xdiff for Rust FFI compatibility, converting xdfile_t.nreff from long to size_t.

This seventh patch in the 9-part series continues the type modernization effort by changing the nreff field in xdfile_t from long to size_t, as it tracks the number of elements in the rindex array. The change follows the pattern established in earlier patches of using size_t for array element counts (previously applied to nrec in patch 6). 

The implementation removes temporary nreff variables from xdl_cleanup_records() and instead directly updates the xdf->nreff field during iteration. This simplifies the control flow while maintaining the same behavior. The change affects xdiff's record cleanup logic, which determines which records should be kept or discarded during diff processing.

Like previous patches in the series, this maintains backward compatibility while improving type safety. The modification is localized to xprepare.c and xtypes.h, with 8 lines changed across 2 files. The change aligns with Rust's usize type for array indexing and continues the incremental preparation of xdiff data structures for better FFI interoperability.

Review considerations include verifying the direct field updates don't introduce any subtle behavioral changes in the correctness of array bounds handling with size_t, and ensuring no performance regressions in the record cleanup path. The patch follows the established pattern of the series, making a focused type change with minimal disruption.