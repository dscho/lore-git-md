This is patch 20/49 in Taylor Blau's incremental MIDX repack refactoring series, focusing on naming improvements for better code organization.

The patch renames `struct generated_pack_data` to the simpler `struct generated_pack`, along with related functions to follow Git's naming conventions. The change makes the code more natural to read by removing redundant "data" from names and establishing clearer relationships between the struct and its operations. Functions now use `generated_pack_` prefix consistently, like `generated_pack_populate()` and `generated_pack_has_ext()`.

This is a mechanical refactoring that maintains all existing behavior while improving naming consistency. The changes affect several functions in `builtin/repack.c` that handle temporary pack files and their extensions. The patch follows the series' pattern of methodical, incremental improvements to repack internals, with this change specifically addressing naming clarity as part of the broader effort to modularize repack functionality.

Key technical details:
- Renames the main struct and its related functions
- Updates all call sites to use the new names
- Maintains identical functionality
- Follows Git's naming conventions where functions operating on a struct are prefixed with the struct name
- Part of the systematic cleanup of repack.c as it's being split into modular components

The change is straightforward and unlikely to be controversial, continuing the series' pattern of careful mechanical improvements. It builds on previous patches that established naming conventions (like patch 15/49) while preparing for future work to further modularize the repack code.