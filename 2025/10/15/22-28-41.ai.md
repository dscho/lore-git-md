This is part 28/49 of Taylor Blau's incremental MIDX repack refactoring series, continuing the systematic elimination of static variables in favor of explicit parameter passing. The patch focuses on the `pack_geometry_init()` function in `builtin/repack.c`.

The change removes the implicit dependency on the static `pack_kept_objects` variable by making it an explicit parameter to `pack_geometry_init()`. This is part of the broader effort to modularize the repack code by removing global state and making dependencies clear at the API boundary. The modification is straightforward - the function signature is updated to accept the new parameter, and the single call site is updated to pass the variable.

This follows the established pattern in the series of:
1. Identifying static variables that create implicit dependencies
2. Making them explicit parameters to functions that need them
3. Preparing for future moves of functionality to dedicated compilation units

The patch maintains all existing behavior while improving the code's modularity. It's a small but meaningful step in the larger refactoring effort to create clean interfaces between repack components. The change is consistent with the series' quality standards and doesn't introduce any new technical debt - in fact it reduces existing debt by removing a hidden dependency.