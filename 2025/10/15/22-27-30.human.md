This is part 5/49 of Taylor Blau's ongoing refactoring series to eliminate `the_repository` usage in the repack machinery, specifically focusing on promisor pack handling. The patch modifies `repack_promisor_objects()` to accept a `struct repository` parameter instead of relying on the global `the_repository`.

The change is mechanical but important - it updates the function signature and all call sites to pass the repository pointer explicitly. The core behavior remains unchanged, including the promisor object enumeration logic that uses `for_each_packed_object()` with the `FOR_EACH_OBJECT_PROMISOR_ONLY` flag. The patch follows the established pattern seen in earlier parts of the series, systematically removing global state dependencies while maintaining existing functionality.

This continues the series' high-quality approach with careful attention to detail - the commit message is clear, the change is focused, and it fits logically into the broader effort to modernize the repack codebase. Reviewers have already given positive feedback on similar patches in the series, suggesting this should be uncontroversial.