Refactoring patch in Taylor Blau's incremental MIDX repack series (patch 27/49). This continues the systematic renaming and modularization effort by standardizing function names related to pack geometry operations in `builtin/repack.c`.

The patch renames all functions working with `struct pack_geometry` to use a consistent `pack_geometry_` prefix, including:
- `geometry_pack_weight` -> `pack_geometry_weight`
- `geometry_cmp` -> `pack_geometry_cmp`
- `init_pack_geometry` -> `pack_geometry_init`
- `split_pack_geometry` -> `pack_geometry_split`
- `get_preferred_pack` -> `pack_geometry_preferred_pack`
- `geometry_remove_redundant_packs` -> `pack_geometry_remove_redundant`
- `free_pack_geometry` -> `pack_geometry_release` (to match Git's naming conventions)

The changes are purely mechanical renames with no behavior modifications, maintaining all existing functionality while improving code organization and consistency. This follows the series' pattern of establishing clear module boundaries and consistent naming schemes as functionality is moved out of `builtin/repack.c`. The patch updates all call sites to use the new names, demonstrating the series' careful attention to detail in maintaining correctness during refactoring.