This is the third patch in a refactoring series cleaning up reference optimization code in Git's refs subsystem. The patch continues the standardization of terminology by renaming `pack_refs_opts` to `refs_optimize_opts` and updating all related flag names (`PACK_REFS_PRUNE` -> `REFS_OPTIMIZE_PRUNE`, `PACK_REFS_AUTO` -> `REFS_OPTIMIZE_AUTO`). The changeset is mechanical but wide-ranging, touching 8 files across the refs backend implementations (files, packed, reftable, debug) and core refs infrastructure (refs.[ch], refs-internal.h).

The patch maintains the same behavior while aligning the codebase with the newer `optimize` terminology introduced in the prior `git refs optimize` functionality. Key technical details include updating the struct name in pack-refs.c where options are initialized, modifying function signatures in all backend implementations, and adjusting documentation in refs.h. The changes are consistent with the series' goal of removing legacy `pack_refs` terminology in favor of the unified `optimize` interface.