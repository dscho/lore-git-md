This is a refactoring patch that's part of the `git repo structure` series. It's a simple rename of the `repo_info()` function to `cmd_repo_info()` in builtin/repo.c to follow Git's convention of prefixing subcommand entry points with `cmd_`. The change is minimal (just 3 lines modified) and straightforward, aligning with established patterns in the codebase.

This patch was already merged in earlier versions of the series, so this appears to be a rebased version being included for completeness in the v5 submission. The thread context shows this is part of a larger effort to create a new `git repo structure` subcommand for repository analysis, but this particular patch is purely about internal naming consistency.