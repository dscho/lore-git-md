This is part 29/49 of Taylor Blau's incremental MIDX repack refactoring series, continuing the systematic elimination of static variables in favor of explicit parameter passing. The patch modifies `pack_geometry_remove_redundant()` to take a `packdir` parameter directly rather than relying on implicit access, following the series' pattern of making dependencies explicit at API boundaries.

The change is straightforward - the function signature is updated to include the `packdir` parameter, and the single call site is modified to pass it through. This continues the series' architectural work of moving repack functionality into dedicated compilation units by removing implicit dependencies. The patch maintains the series' focus on type safety and clear module boundaries while preserving existing behavior.

This follows the same pattern seen in patch 28/49 (which made `pack_kept_objects` explicit) and earlier patches in the series that have systematically removed static variables and global dependencies. The change is small but contributes to the larger goal of creating clean, modular repack infrastructure that can be more easily maintained and extended in the future.