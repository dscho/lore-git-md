This is part 22/49 of Taylor Blau's repack refactoring series, continuing the modularization of `builtin/repack.c` with a focus on eliminating static variables. The patch modifies `generated_pack_populate()` to take an explicit `packtmp` parameter instead of accessing the static variable directly.

The change is mechanical but important for the series' goal of making the repack code more modular and relocatable. The function now receives the temporary pack prefix as a parameter rather than relying on the static `packtmp` variable, which prepares it to be moved out of `builtin/repack.c` in future patches. Three call sites are updated to pass the `packtmp` value they already have access to.

This follows the established pattern in the series of gradually removing implicit dependencies on static/global state. The patch maintains all existing behavior while improving the code's structure. Like previous patches in the series, this is a focused change that moves incrementally toward the larger goal of modular repack infrastructure.

The modification is straightforward and unlikely to be controversial, as it follows the same approach that has been working well throughout the series. It continues the careful, systematic refactoring that has characterized patches 1-21, with each change being small and clearly motivated by the overall architectural goals.