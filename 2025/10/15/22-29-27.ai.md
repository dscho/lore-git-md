This is patch 44/49 in Taylor Blau's repack refactoring series (tb/incremental-midx-part-3), continuing the modularization of `builtin/repack.c` with a focus on eliminating global variables and improving interface design.

The patch modifies `finish_pack_objects_cmd()` to take a `write_pack_opts` struct parameter instead of individual fields, continuing the series' pattern of consolidating parameters into structured types. Key changes include:
- Replacing the explicit `local` parameter with a call to the new `write_pack_opts_is_local()` helper from patch 43/49
- Accessing `packtmp` through the `write_pack_opts` struct rather than the static variable
- Updating all three call sites to pass the struct (two transparently pass existing opts, while `cmd_repack()` initializes a new one)

The changes are straightforward mechanical refactoring with no behavior changes, though they do touch the critical pack generation path. The patch follows the series' established pattern of methodically replacing direct variable access with structured parameter passing, moving toward the goal of making `finish_pack_objects_cmd()` callable from the new repack API. 

This continues the series' careful balance between ideal interfaces and practical backwards compatibility, with the temporary initialization of `write_pack_opts` in `cmd_repack()` showing the transitional nature of this refactoring phase. The changes are consistent with Jeff King's earlier feedback on API design tradeoffs in this area.