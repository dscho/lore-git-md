This is patch 24/49 in Taylor Blau's ongoing refactoring series to modularize Git's repack machinery and eliminate global variables. The patch moves the "generated_pack" API out of `builtin/repack.c` and into the new `repack.c` module, continuing the series' systematic relocation of repack functionality.

The change is purely mechanical - the code is moved verbatim with no behavior changes. The patch:
1. Removes 83 lines from `builtin/repack.c` that handled pack file extensions (.pack, .idx, .rev etc.) and pack installation
2. Adds those same 83 lines to `repack.c` along with corresponding declarations in `repack.h`
3. Updates loop counters from `int` to `size_t` for consistency with earlier patches in the series

This continues the series' pattern of:
- Moving repack-related code to dedicated compilation units
- Establishing clear module boundaries
- Maintaining identical behavior while improving code organization
- Using consistent type safety practices (like `size_t` for array indices)

The patch is part of the series' middle section that is establishing the new repack module's architecture. It follows the same high-quality approach seen throughout the series - careful, incremental changes with clear commit messages. Reviewers have already approved the overall approach, and this patch appears uncontroversial as it simply relocates already-refactored code.