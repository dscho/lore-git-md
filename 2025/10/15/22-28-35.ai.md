This is patch 26/49 in Taylor Blau's repack refactoring series, continuing the modularization of `builtin/repack.c` with a focus on promisor object handling. The patch moves the `repack_promisor_objects()` function and its supporting code out of the builtin into a new `repack-promisor.c` file, while keeping the declaration in `repack.h`.

Key technical details:
- The function handles repacking of promisor objects (part of Git's partial clone functionality)
- Moves 95 lines from `builtin/repack.c` to new `repack-promisor.c`
- Maintains identical behavior while improving code organization
- Follows the series' pattern of separating specialized functionality into dedicated files
- Keeps the `NEEDSWORK` comments about potential delta optimization improvements and promisor file handling
- Updates build systems (Makefile and meson.build) to include the new source file
- Preserves all error handling and object feeding logic unchanged

The change continues the series' systematic approach to:
1. Removing code from the monolithic builtin
2. Grouping related functionality together
3. Maintaining clear API boundaries via repack.h
4. Preparing for future improvements (noted in NEEDSWORK comments)

The patch appears mechanically sound, following the established patterns from earlier patches in the series. It doesn't introduce new functionality or behavior changes, focusing purely on code organization as part of the larger repack modularization effort.