This is part 47/49 of Taylor Blau's refactoring series (tb/incremental-midx-part-3), continuing the modularization of `builtin/repack.c` by moving `write_filtered_pack()` into its own compilation unit.

The patch relocates `write_filtered_pack()` from `builtin/repack.c` to a new `repack-filtered.c` file while maintaining identical functionality. The function is added to the `repack.h` API as it represents a distinct component of the repacking operations. The change follows the series' pattern of systematically extracting repack functionality into dedicated compilation units.

Key technical details:
- The function takes a `write_pack_opts` struct, `existing_packs` struct, and `string_list` of pack names as parameters
- It handles pack filtering logic using `--stdin-packs` and `--keep-pack` options with `git pack-objects`
- The implementation remains unchanged from its previous location, just moved to the new file
- Build system files (Makefile and meson.build) are updated to include the new compilation unit

This continues the series' systematic approach to:
1. Removing code from `builtin/repack.c`
2. Creating well-defined module boundaries
3. Maintaining existing behavior while improving code organization
4. Progressing toward the goal of eliminating global variables and creating clean interfaces

The change is straightforward and follows the established pattern of previous patches in the series, with no behavior modifications. It represents another incremental step in the larger refactoring effort to modularize the repack machinery.