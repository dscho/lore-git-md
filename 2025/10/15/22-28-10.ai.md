This is patch 17/49 in Taylor Blau's incremental MIDX repack refactoring series, continuing the modularization of `builtin/repack.c`. The patch focuses on preparing the `existing_packs_remove_redundant` function for eventual relocation to its own compilation unit by making it accept the `packdir` parameter explicitly rather than relying on a static variable.

The change modifies two functions in the call chain - `existing_packs_remove_redundant()` and its helper `remove_redundant_packs_1()` - to take the pack directory path as a parameter. This eliminates their dependency on the static `packdir` variable, which is used both for writing new packs and removing redundant ones. The patch updates the single call site in `cmd_repack()` to pass this parameter through.

This is a mechanical refactoring that maintains identical behavior while improving modularity, following the pattern established in earlier patches of the series. The change is straightforward and well-contained, touching only the pack removal logic while leaving other aspects of the repack machinery unchanged. It continues the series' systematic approach to removing implicit dependencies and preparing code for relocation to dedicated modules.