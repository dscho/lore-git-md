This is part 36/49 of Taylor Blau's ongoing refactoring series to modularize Git's repack machinery, with a focus on MIDX-based repacking and removing global variables.

The patch inlines the `remove_redundant_bitmaps()` function directly into `write_midx_included_packs()`, eliminating the need for callers to track which packs were included in the MIDX. This is a preparatory step for future changes where the caller won't need to know about the included packs at all.

The change is mechanical and maintains the same functionality - it still removes bitmaps for packs that were written into the MIDX, handling ENOENT gracefully. The diff shows the straightforward relocation of the bitmap cleanup code into the MIDX writing function, with some minor flow control adjustments to accommodate the change.

This continues the series' pattern of incrementally improving the repack architecture while maintaining behavior. The change is well-contained and follows the established approach of the series, which has already received positive review feedback. It's one of the final steps before the series can be submitted for merging.