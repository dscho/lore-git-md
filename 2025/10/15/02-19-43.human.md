This is the second patch in a refactoring series from an Outreachy participant, converting `strbuf_split` usage to `string_list_split` in `gpg-interface.c`. The patch focuses on the SSH key signing path, specifically the `get_default_ssh_signing_key()` function.

The change replaces `strbuf_split_max` with `string_list_split` when parsing the output of the SSH key command. The key improvement is simplification - since the code only needs to read the split tokens (not modify them), the string_list API is more appropriate. The patch reduces line count while maintaining the same functionality, including the validation check via `is_literal_ssh_key()`.

This follows the same pattern as the first patch in the series (which handled GPG signature verification), showing consistent application of the refactoring approach. The changes appear well-contained and the commit message clearly explains the motivation. As with the first patch, this is a code quality improvement rather than a functional change, and reviewers will likely focus on verifying the string_list behavior matches all edge cases in SSH key parsing.