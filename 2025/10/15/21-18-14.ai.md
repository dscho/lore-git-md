Refactoring patch in the xdiff type cleanup series, specifically changing xrecord_t.ptr from char* to uint8_t* for Rust FFI compatibility. This is the second patch in a 9-part series preparing xdiff data structures for better Rust interoperability.

The change converts the pointer type in xrecord_t to explicitly indicate binary data handling rather than text, matching Rust's u8 type for byte buffers. The patch updates all xdiff subsystem files (xdiffi.c, xemit.c, xmerge.c, etc.) to handle the new type, adding casts where needed to maintain compatibility with functions expecting char* (like xdl_blankline() and regex handling). The changes are mechanical but widespread, touching 7 files with 22 lines added/removed.

This continues the series' pattern of precise type changes for FFI safety while preserving behavior. The patch maintains backward compatibility through explicit casts at API boundaries where char* is still expected. Reviewers will want to verify these casts are safe and that no platform-specific character handling assumptions are broken by the uint8_t change. The change is well-motivated by Rust interop needs and fits with the series' incremental approach to type modernization.