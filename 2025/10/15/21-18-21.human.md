Refactoring patch in a series preparing xdiff for Rust FFI compatibility.

This is the final patch in a 9-part series modernizing xdiff's data structures. It renames the `rindex` field in `xdfile_t` to the more descriptive `reference_index`, completing the type safety and clarity improvements started earlier. The change is straightforward - just a rename with no behavioral impact - updating all references across xdiff's core files (xdiffi.c, xprepare.c, and xtypes.h).

The commit message explains that this array maps between compacted diff lines and their original file positions, making the new name more self-documenting. With this change, the series has now systematically updated xdiff's core types (from hash values to array indices) to be more precise and Rust-compatible while maintaining identical behavior.

This appears to be a well-executed conclusion to the refactoring effort, with each patch building carefully on the last. The changes are focused and mechanical, with the rename being a logical final step after the earlier type modernization work.