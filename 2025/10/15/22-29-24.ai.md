Refactoring patch in Taylor Blau's repack modularization series (tb/incremental-midx-part-3), addressing feedback from Jeff King. This patch (43/49) extracts duplicate local-pack detection logic into a new helper function.

The patch introduces `write_pack_opts_is_local()` to consolidate identical code from `write_cruft_pack()` and `write_filtered_pack()` that checks if a pack destination is local by comparing paths. The new function operates on the `write_pack_opts` struct (containing both `destination` and `packdir` fields) and returns a bool, replacing the previous `skip_prefix()` approach that required a dummy scratch variable. This change follows the series' pattern of moving functionality into dedicated compilation units while improving type safety and reducing redundancy.

Key technical details:
- New function `write_pack_opts_is_local()` added to repack.c and exposed via repack.h
- Replaces duplicate `skip_prefix()` calls with `starts_with()` as suggested in review
- Changes return type from `int` to `bool` to match modern conventions
- Eliminates need for temporary `scratch` variable in callers
- Part of systematic elimination of code duplication in pack-writing interface
- Follows pattern from patch 42/49's `write_pack_opts_pack_prefix()`
- Maintains behavior while simplifying call sites
- Leaves remaining duplication (function calls) for follow-up patch

The change addresses Jeff King's feedback on API design while maintaining the series' incremental improvement approach. It demonstrates the ongoing refinement of the repack module interfaces as the series nears completion.