This thread documents a completed 9-part refactoring series modernizing xdiff subsystem data structures for Rust FFI compatibility, building on two previously merged cleanup efforts. The work updates xdiff's core structures (xrecord_t and xdfile_t) with unambiguous types and clearer naming while maintaining behavior. Key technical changes include type safety updates (ssize_t/ptrdiff_t for offsets, uint8_t* for byte pointers), field separation (line_hash vs minimal_perfect_hash), and naming improvements (rindex -> reference_index).

Recent discussion with Ezekiel Newren provides deeper technical rationale for the char* to uint8_t* conversion in core structures, addressing Phillip Wood's concerns about cast proliferation. Newren explains this is specifically for FFI tooling compatibility (cbindgen/bindgen handle uint8_t better than char) and to avoid signedness issues in developer mode builds. The change is deliberately scoped to xdiff's fundamental data-passing structures to minimize broader impact. In response to Patrick Steinhardt's questions, Newren confirms the limited scope was verified through CLion searches and DEVELOPER=1 compiler warnings, with all necessary casts added where the field is used with differing types. He agrees to document these implementation choices in the commit message, particularly the rationale for keeping function signatures unchanged while updating the core structure field type.

Newren's earlier email addressed the `dstart`/`dend` field type debate (ssize_t vs ptrdiff_t) by proposing to revive his previously proposed `compat/rust_types.h` file that would formally map C types to Rust equivalents (ptrdiff_t -> isize). This structural solution aims to prevent recurring debates about type choices in FFI contexts by institutionalizing the mappings rather than leaving them as implicit review knowledge. The email links to the original proposal that was previously rejected but now gains relevance given the ongoing Rust integration work.

Open technical questions now include:
1. Final resolution on dstart`/`dend` type choice (ssize_t vs ptrdiff_t) considering both POSIX alignment and FFI needs
2. Whether to adopt a formal type mapping system (via rust_types.h or documentation) for future FFI work
3. uint8_t* vs char* tradeoffs for byte-oriented pointers, with specific toolchain impacts documented
4. Cast proliferation management while maintaining type safety
5. Documentation needs around type choices, now with concrete FFI tooling examples and potential rust_types.h integration
6. Maintainer perspective on transitional complexity vs long-term type safety
7. Scope boundaries for Rust-aligned types (currently limited to core xdiff structures)

The thread has evolved from implementation details to broader Rust integration strategy, with Newren's contributions introducing both specific solutions for xdiff and systematic approaches to type mapping that could influence future FFI-related changes. All architectural decisions are settled, with remaining discussion focused on documentation clarity, type system conventions, and transitional tradeoffs. Key participants (Newren, Steinhardt, Wood, Hamano) are aligned on the technical direction but continue examining structural solutions for recurring type system challenges at the C/Rust boundary, with Newren providing concrete verification methods and documentation commitments for the uint8_t* conversion approach.