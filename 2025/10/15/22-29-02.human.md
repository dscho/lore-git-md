This is a refactoring patch in Taylor Blau's ongoing series to modularize Git's repack machinery (patch 35/49 in v2 series). The change is straightforward - it reorders two functions in `builtin/repack.c` to eliminate a forward declaration, preparing for a future patch that will inline `remove_redundant_bitmaps()` into `write_midx_included_packs()`.

The patch moves the `remove_redundant_bitmaps()` function definition above `write_midx_included_packs()` where it will soon be used. The function itself remains unchanged - it still handles removing bitmap files for packs that are now included in the MIDX. This is a small preparatory step in the larger refactoring effort to improve the code organization of the repack functionality.

As part of the systematic repack refactoring, this change follows the series' pattern of making incremental improvements to code structure while maintaining behavior. The patch appears well-contained and low-risk, with no functional changes - just moving code blocks to enable cleaner future modifications.