This is the third patch in a 9-part refactoring series modernizing xdiff's data structures for better Rust FFI compatibility. The change converts the `size` field in `xrecord_t` from `long` to `size_t`, which is more appropriate for representing memory sizes. The patch affects multiple xdiff files (xdiffi.c, xemit.c, xmerge.c, xprepare.c) and the xtypes.h header.

The key technical change is straightforward - replacing `long` with `size_t` for the size field in xrecord_t. However, this requires adding explicit casts at API boundaries where the size is passed to functions expecting `long` parameters (like `xdl_blankline`, `xdl_recmatch`). The changes are mechanical but widespread, touching 5 files with 19 insertions and 20 deletions.

This continues the series' pattern of precise type changes that maintain behavior while improving type safety. The conversion to `size_t` is particularly relevant for Rust FFI since Rust's usize type maps naturally to size_t. The patch includes all necessary casts to maintain compatibility with existing code that expects long parameters, showing careful attention to backward compatibility.

The changes affect core diff operations (recording matching, blank line detection, merge conflict resolution) but should not alter behavior. Review considerations include verifying the safety of the size_t to long casts (ensuring no truncation on platforms where size_t is larger than long) and confirming no performance regressions in hot paths like recmatch(). The patch follows the established pattern of the series - focused, behavior-preserving type changes that prepare the xdiff subsystem for Rust integration.