This is a brief discussion about finalizing the naming convention for the atomic reference updates feature in `git replay`. The thread has already established technical consensus on the implementation, with this being the last remaining naming decision before merge.

Christian Couder responds to Siddharth Asthana's proposal about aligning the configuration and command-line interface naming for reference update behavior in `git replay`. The discussion focuses on two naming options for the config/CLI pairing: either using `replay.refAction` with `--ref-action=(update|print)` (Option 1) or `replay.updateRefs` with `--update-refs=(yes|print)` (Option 2).

Christian expresses clear preference for Option 1, citing better clarity in conveying the action's purpose and direct mirroring between config and CLI names. This aligns with earlier maintainer feedback favoring enum-based design and logical consistency in naming. The response is concise and confirms the naming direction that was already emerging from previous technical discussions, effectively closing the last open question in the series.