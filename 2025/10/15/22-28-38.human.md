Refactoring patch in Taylor Blau's incremental MIDX repack series (27/49). This continues the systematic renaming effort to standardize function names working with `struct pack_geometry` by prefixing them with "pack_geometry_". The most notable change is converting `free_pack_geometry()` to `pack_geometry_release()` to better match Git's naming conventions (where "release" indicates freeing struct contents rather than the struct itself).

The changes are mechanical and maintain all existing behavior, following the series' focus on improving code organization and naming consistency. This patch fits into the broader pattern of the series where earlier patches moved functionality into dedicated files, and now the remaining functions are being given more consistent names. The changes touch only `builtin/repack.c`, with 26 lines changed (all simple renames).

This is a straightforward cleanup that reviewers have generally approved in principle throughout the series, though it depends on the earlier architectural changes in patches 1-26. The patch maintains the series' high quality standards with clear commit messages and thorough mechanical changes.