This is the second patch in a refactoring series preparing xdiff's data structures for better Rust FFI compatibility. It changes the `xrecord_t.ptr` field from `char*` to `uint8_t*` to better represent that it points to binary data rather than text characters, matching Rust's `u8` type for byte buffers.

The change is straightforward but touches many files (7 total) since the pointer type is widely used across xdiff's core operations. Each modification includes a cast when the pointer is passed to functions expecting `char*`, preserving existing behavior while making the binary nature of the data more explicit in the type system. The patch continues the series' pattern of precise, behavior-preserving type changes that improve cross-language compatibility without altering functionality.

As with the rest of the series, this appears mechanically sound and well-contained. Reviewers will want to verify the casts don't mask any type safety issues, though the changes follow established patterns from previous xdiff cleanups. The patch fits cleanly into the broader effort to make xdiff's internals more robust for both C and Rust consumers.