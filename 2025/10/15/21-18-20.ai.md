This is the eighth patch in a 9-part refactoring series preparing xdiff for Rust FFI compatibility, focusing on type modernization. The patch converts the `rindex` field in `xdfile_t` from `long` to `size_t`, continuing the series' pattern of using more appropriate types for memory indexing operations. The change aligns with Rust's `usize` type and follows previous conversions of similar fields like `nrec` and `nreff`.

The 2-line change modifies `xtypes.h` to update the pointer type while maintaining the same memory layout, as the field already pointed to array indices. The commit message notes this is part of a broader cleanup that will eventually remove the adjacent `dstart`/`dend` fields (which were moved to the struct's end in patch 1 to minimize future conflicts). 

This change follows the series' established approach of precise, behavior-preserving type updates that:
1. Match the semantic purpose of the field (memory indexing)
2. Maintain compatibility with Rust's type system
3. Require minimal code changes due to the field's existing pointer nature
4. Build on prior structural preparations (field reordering)

The patch appears straightforward with low risk, as it continues the series' consistent pattern of type modernization while explicitly noting its relationship to upcoming work (dstart/dend removal). Review considerations would focus on verifying the pointer arithmetic remains correct with the new type and confirming no platform-specific issues arise from the size_t conversion.