This is the seventh patch in a 9-part refactoring series modernizing xdiff's data types for better Rust FFI compatibility. The change converts the `nreff` field in `xdfile_t` from `long` to `size_t`, as it tracks array element counts for the `rindex` array.

The patch follows the established pattern of the series by selecting the most appropriate type for the data's purpose - `size_t` is the conventional type for array sizes in C. The change is straightforward, removing temporary `nreff` variables and updating the code to directly use the struct field. The diff shows 8 lines changed across two files, with the bulk being mechanical updates to the cleanup logic in `xprepare.c`.

This continues the series' careful approach to type safety while maintaining behavior. The change is well-contained and appears to be a logical progression from the previous patch that converted the related `nrec` field to `size_t`. The commit message could benefit from mentioning this connection to the prior work, but the technical content itself looks solid.