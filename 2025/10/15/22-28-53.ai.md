This is patch 32/49 in Taylor Blau's incremental MIDX repack refactoring series, continuing the modularization of `builtin/repack.c`. The patch focuses on the `write_midx_included_packs()` function, which handles MIDX creation during repacking.

The key change introduces a `repack_write_midx_opts` struct to bundle the function's parameters, replacing six individual arguments. This follows the series' pattern of reducing parameter lists and making dependencies more explicit. The struct includes both existing parameters (include list, geometry, names, refs snapshot) and newly exposed ones (packdir, midx_must_contain_cruft) that were previously static variables.

The patch maintains all existing functionality while improving the interface design. It touches the MIDX writing logic that handles:
- Progress display control
- Bitmap generation
- Preferred pack selection
- Reference snapshot handling
- Cruft pack inclusion requirements

This change enables future extraction of MIDX-related functionality into a separate compilation unit (likely coming in subsequent patches) by providing a cleaner interface boundary. The patch aligns with the series' systematic approach to eliminating static variables and global dependencies while maintaining behavior.

Technical details to note for future reference:
- New struct `repack_write_midx_opts` in `builtin/repack.c`
- Updated call sites in `cmd_repack()` to use the new struct
- Preserved logic for preferred pack selection and bitmap handling
- Maintained error handling and cleanup paths
- Follows series convention of explicit parameter passing rather than static variable access