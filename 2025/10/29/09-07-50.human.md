This is a technical discussion about the Rust implementation of Git's hashing functionality, part of the larger SHA-1/SHA-256 interoperability work.

Patrick Steinhardt follows up on two points from Brian Carlson's patch introducing Rust hashing infrastructure. First, he confirms understanding of the "safe" vs "unsafe" hashing distinction (where "safe" uses the collision-resistant SHA-1-DC variant while "unsafe" could use faster algorithms for trusted data). Brian had offered to defer this distinction until needed, which Patrick accepts.

Second, they discuss the API design for hash initialization. Patrick had suggested combining the separate `alloc()` and `init()` calls into a single `git_hash_new()`, but Brian explains the current split allows for potential future reset functionality. Patrick acknowledges this rationale and doesn't strongly object to either approach.

The exchange shows productive technical review of implementation details in this foundational Rust code, with both maintainers demonstrating flexibility on API design choices while ensuring the solution meets Git's security and performance requirements. This patch is part of the critical path for Git's Rust integration and hash algorithm interoperability.