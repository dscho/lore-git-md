This is part 8/10 of Ezekiel Newren's refactoring series to modernize xdiff data structures for Rust FFI compatibility. The patch changes `xdfile_t.nreff` from `long` to `size_t` since it tracks the number of elements in the `rindex` array, making it more semantically appropriate for a memory-related count.

The change requires some straightforward code flow adjustments in `xdl_cleanup_records()` where the temporary `nreff` variable is eliminated in favor of directly updating the struct field. This maintains the same behavior while using more precise types. The change aligns with the series' goal of establishing clear type boundaries between C and Rust code.

This continues the pattern seen in earlier patches of the series, where similar changes were made to `nrec` and other fields. The patch appears well-contained and follows the established approach, with no obvious issues in the implementation. Given the prior consensus on the series' direction and the straightforward nature of this change, it seems likely to be uncontroversial.