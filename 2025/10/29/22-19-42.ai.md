This is part 4/10 of the xdiff refactoring series for Rust FFI compatibility, focusing on type safety updates. The patch changes the `xrecord_t.size` field from `long` to `size_t` to better represent memory sizes, requiring explicit casting at 20 usage sites across xdiff's core operations (line comparison, diff output generation, merge conflict detection).

The changes propagate through xdiff's core algorithms:
- Line comparison functions (`xdl_recmatch`, `xdl_blankline`) now cast size_t to long for compatibility
- Memory operations (`memcpy`, line buffer handling) use the native size_t type
- Diff output generation maintains backward compatibility through casting
- Merge conflict detection preserves existing behavior while using size_t internally

The patch follows the series' pattern of introducing type safety while maintaining algorithmic behavior, with explicit casting at FFI boundaries. It builds on the earlier uint8_t* change for xrecord_t.ptr (patch 3/10) and aligns with the project's documented type mapping conventions for Rust interoperability. The casting strategy mirrors the approach used in other parts of the series, maintaining compatibility with existing xdiff interfaces while preparing for FFI use.