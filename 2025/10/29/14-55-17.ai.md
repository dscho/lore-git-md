This is a technical follow-up in the packfile refactoring series, specifically addressing patch 5/8 which simplifies object lookup in `builtin/pack-objects.c`. Toon Claes provides focused feedback on the implementation details of the caching mechanism in `has_sha1_pack_kept_or_nonlocal()`.

The key points from Toon's email:
1. He acknowledges the original function was hard to understand and appreciates Patrick's simplification.
2. Raises a question about the purpose of the `last_found` cache, noting it's a global variable tracking the last successful pack lookup regardless of object ID.
3. Suggests improving the code comment to better explain the `(void *)1` sentinel value check used to initialize `last_found`.

The discussion remains technical and constructive, focused on implementation clarity rather than challenging the overall approach. Toon's suggestion about the comment would make the sentinel value's purpose more explicit to future readers. This follows the thread's technical depth while maintaining the series' forward momentum.

The changes affect `builtin/pack-objects.c` specifically around the `has_sha1_pack_kept_or_nonlocal()` function, which checks for objects exist in kept/non-local packs. The caching optimization remains but is now more clearly separated from the pack iteration logic.