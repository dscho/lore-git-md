This is a discussion about licensing and FFI design in the ongoing SHA-1/SHA-256 interoperability work. The email is part of a 14-patch series introducing Rust code to Git's object storage system.

Brian m. carlson responds to Ezekiel Newren's concerns about licensing headers in Rust files, explaining they want explicit GPLv2 notices in each file to ensure proper attribution when code is used by LLMs. They note this approach already exists in Git's reftable code.

The exchange also touches on FFI design decisions for the `ObjectID` struct, with both agreeing that matching C's struct layout is necessary at the FFI boundary rather than using Rust enums, due to undefined behavior risks. This continues the thread's technical discussion about how to safely represent hash algorithms across the Rust/C boundary.

The response reinforces the series' careful approach to FFI safety while showing differing opinions persist about licensing style. The technical discussion appears settled on using C-compatible structs rather than Rust enums at the boundary.