This is a build system compatibility issue in the SHA-1/SHA-256 interoperability patch series, specifically around Rust version requirements. Junio C Hamano reports a build failure when testing the series with `WITH_RUST=YesPlease`, where the build script uses a newer Cargo syntax (`cargo::`) that isn't supported by the declared minimum Rust version (1.49.0). The error suggests either downgrading the build script syntax to use single-colon format (`cargo:`) or raising the minimum Rust version to 1.77.0, though the latter may be problematic for older distributions like Debian oldstable.

The issue highlights a practical deployment consideration for the Rust infrastructure being introduced - the tension between using modern Rust features and maintaining compatibility with older Rust toolchains that might be present in real-world build environments. This comes as part of the broader discussion about build system integration (patch 10/14 in the series) where library organization and linking strategies are already under review. The specific technical detail involves the build script output syntax in `Cargo.toml` and the implications for minimum supported Rust version, which connects to ongoing platform support concerns noted in the thread's open questions.