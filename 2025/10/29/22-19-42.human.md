This is part 4/10 of a refactoring series to modernize xdiff's data structures for Rust FFI compatibility. The patch changes the `xrecord_t.size` field from `long` to `size_t` to better represent memory sizes, requiring explicit casting at 20 usage sites across xdiff's line comparison, merge, and diff generation functions.

The change is straightforward - `size_t` is the more appropriate type for memory sizes, and the patch updates all callers that expected a `long` with explicit casts. This maintains compatibility with existing xdiff functions while making the memory size semantics clearer for FFI purposes.

The patch follows the series' pattern of type modernization with minimal behavior change. Like previous patches, it includes careful casting at boundaries to ensure no functional regression. The changes are mechanical but thorough, touching all relevant parts of the xdiff codebase.

This continues the series' goal of establishing clear type boundaries between C and Rust, with `size_t` being the natural counterpart to Rust's `usize` for memory sizes. The patch appears well-executed with comprehensive coverage of all affected code paths.