This is a follow-up discussion in the Rust CI infrastructure series, specifically about the formatting check script's error handling behavior.

SZEDER Gábor responds to Patrick Steinhardt's explanation about why the Rust formatting check script doesn't use `set -e`. While Patrick had explained that running all checks (even if some fail) helps developers see all issues at once, SZEDER counters that it can be frustrating when error messages aren't at the bottom of CI logs.

The exchange reveals a tension between two valid CI workflow preferences: seeing all failures at once versus having the most critical error appear last. This is a minor but practical consideration in the now-accepted Rust CI infrastructure, where the formatting check is just one component of a comprehensive system that includes rustfmt, Clippy, and MSRV validation.

The discussion doesn't propose any code changes at this point - it's more of a post-merge reflection on CI usability tradeoffs that might inform future adjustments to the workflow.