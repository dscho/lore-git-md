This is a follow-up in the NonStop platform bug thread, focusing on test suite failures in `t7900` (maintenance tests) when using OpenSSL 3.5. Randall Becker responds to SZEDER Gábor's analysis of the test output, maintaining that the "argument expected" error is indeed coming from bash 5.0.18 when processing `test_subcommand !` assertions.

The core issue appears to be platform-specific behavior in how the test harness processes negated assertions (`test_subcommand !`), with disagreement about whether the error originates from bash or the test infrastructure. Randall offers to provide more debugging information but notes the failure pattern is consistent across similar test cases.

This remains an unresolved platform-specific test suite issue rather than a core Git bug, but one that could mask real regressions if test failures go undetected on NonStop systems. The exchange highlights the challenges of maintaining cross-platform test consistency, particularly for less common platforms like NonStop where shell behavior may differ.