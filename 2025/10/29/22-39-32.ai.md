This is a technical discussion email in the ongoing packfile store abstraction thread, specifically addressing patch 2/8 which moves the MRU packfile list into the packfile store. Taylor Blau raises several implementation questions after reviewing Patrick Steinhardt's changes.

The email focuses on two main concerns about the series' approach to handling packfiles across object sources. First, Taylor questions how object lookups will work with alternates under the new model, noting that `find_pack_entry()` doesn't yet show the expected changes to iterate across multiple object sources. Patrick's clarification that this series is preparatory (with per-source lists coming later) resolves this concern.

Second, Taylor probes the rationale for introducing a new `struct packfile_list` rather than reusing the existing MRU list infrastructure. He suggests the change might be preventing misuse of global lists, but seeks confirmation on whether this is the primary motivation or if there are other technical reasons driving the new type.

The discussion remains focused on implementation details rather than proposing changes, with Taylor is trying to understand the architectural direction before the more invasive changes in later patches. This email doesn't introduce new patches but helps clarify the technical reasoning behind the series' design choices, particularly relevant as the changes touch core packfile handling that will affect object lookup performance and correctness.