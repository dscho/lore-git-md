This is a follow-up discussion in the Rust CI infrastructure series, specifically about the Rust formatting check script's error handling behavior. SZEDER Gábor responds to Patrick Steinhardt's justification for not using `set -e` in the Rust checks script.

The key technical point revolves around whether the script should use `set -e` (which would stop at first error) versus the current approach that continues through all checks even if some fail. Patrick had explained this was intentional to surface all potential issues at once rather than requiring iterative fixes. SZEDER counters that this makes error messages harder to find in CI logs since they may not appear at the end.

This touches on CI script conventions (where `set -e` is typical) versus practical developer experience considerations. The discussion occurs after the series has already been accepted for merging, suggesting this is a minor refinement point rather than a blocking issue. The exchange shows the project's attention to both consistency and usability in CI feedback.