Documentation patch continuing the effort to align `git bisect` usage strings with man page content. This is a follow-up to Ruoyu Zhong's earlier patch, with Ben Knoble providing feedback.

The changes focus on:
1. Further refining the usage strings in `builtin/bisect.c` to precisely match the man page format
2. Adding explicit documentation for the `git bisect help` subcommand that was previously missing
3. Confirming the removal of `bisect` from the test mismatch list since the documentation is now properly aligned

The patch appears to be a straightforward continuation of the documentation cleanup work, maintaining the same careful attention to detail as the original. No behavior changes are introduced - this remains purely a documentation synchronization effort. The changes look correct and well-scoped, building on the previous work to complete the alignment between code and docs.