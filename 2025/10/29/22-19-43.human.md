This is part 5/10 of a refactoring series modernizing xdiff's data structures for Rust FFI compatibility. The patch updates `xdl_hash_record()` and related functions to use unambiguous types: `char*` becomes `uint8_t*` for byte-oriented memory handling, and `unsigned long` becomes `uint64_t` to ensure consistent hash values across platforms. The `long` type for flags is also upgraded to `uint64_t` to prevent bit truncation on 32-bit systems.

The changes maintain the same hash computation behavior while making the code more portable and Rust-FFI-friendly. The patch includes updates to function signatures, variable declarations, and explicit casts where needed. This continues the series' pattern of type safety improvements while keeping the core diff algorithm unchanged.

The changes are mechanical but thorough, touching 4 files with 21 line modifications. The commit message clearly explains the rationale for each type change, and the implementation includes careful handling of whitespace and verbatim hashing modes. This patch follows the established pattern from earlier in the series and appears ready for merging.