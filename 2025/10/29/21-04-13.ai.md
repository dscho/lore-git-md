This is a follow-up discussion in the thread about the `commitGraph.changedPaths` config option for commit-graph Bloom filters. SZEDER Gábor raises two substantive concerns about the proposed implementation:

First, they question the behavior where setting `commitGraph.changedPaths=false` doesn't actually remove existing Bloom filters, only prevents writing new ones. They argue this wastes resources by continuing to compute filters the user explicitly tried to disable, calling this behavior "not sensible."

Second, they point out that the test coverage doesn't include split commit-graph scenarios, noting there's prior history of bugs in this area. This suggests the test suite should be expanded to verify the config option works correctly with split graphs.

The email doesn't propose specific solutions but highlights these as potential design flaws and testing gaps in the otherwise-ready patch series. These concerns touch on both the logical consistency of the config behavior and the robustness of the implementation - particularly important given the feature's use in large monorepos where split graphs are common.

The thread context shows this is coming late in the review cycle (after Junio queued the patches for 'next'), suggesting these may be last-minute considerations before final merge. The concerns are technical rather than conceptual, focusing on edge cases rather than opposing the feature itself.