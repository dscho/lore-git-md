This is the first patch in a 10-part series establishing type safety conventions for Rust FFI interoperability in Git's xdiff subsystem. The patch introduces comprehensive documentation (`Documentation/technical/unambiguous-types.adoc`) defining precise type mappings between C and Rust, with special attention to edge cases and platform-specific behaviors.

The document covers boolean, integer, floating-point, size, and character types, explaining the nuances of each mapping. It particularly highlights problematic areas like C's ambiguous `char` type (which should map to Rust's `u8` for memory operations) and warns against using Rust's `std::ffi::c_*` types due to their platform-dependent behavior. The documentation also includes detailed examples of version-specific Rust behavior and potential pitfalls with enum types across the FFI boundary.

As the foundation for the entire series, this patch establishes the type safety principles that subsequent patches will implement in the xdiff code. The documentation is thorough and well-structured, addressing both immediate needs (Rust FFI) while leaving room for future language mappings. Given the thread context shows this series has already reached consensus, this appears to be a well-vetted contribution that sets important standards for Git's cross-language compatibility.