This is part 9/10 of Ezekiel Newren's xdiff refactoring series for Rust FFI compatibility, specifically converting the `rindex` field in `xdfile_t` from `long` to `size_t`. The change aligns with the series' goal of using unambiguous types for cross-language interoperability.

The patch focuses on `rindex`, which tracks array indices in xdiff's core data structures. The commit message explains this is safe because `rindex` is only used for array indexing, where `size_t` is the more appropriate type. The change follows the same pattern as previous conversions in the series (like `nrec` in patch 7/10 and `nreff` in patch 8/10) and maintains consistency with the `reference_index` array's type.

The diff shows a straightforward one-line change in `xdiff/xtypes.h`, updating the type declaration without any behavioral modifications. This continues the series' pattern of type safety improvements that don't require cascading refactors, as noted in the commit message. The change builds on the thread's established consensus about using `size_t` for memory-related indices and array sizes.

This patch fits cleanly into the series' documented approach for FFI compatibility, where all array indexing and memory-related quantities use `size_t` rather than platform-dependent types like `long`. The thread context shows this change was already discussed and approved as part of the broader type safety effort.