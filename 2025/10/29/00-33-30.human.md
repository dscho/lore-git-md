This is a technical clarification in the ongoing SHA-1/SHA-256 interoperability patch series, specifically addressing FFI design concerns in patch 3/14. The discussion focuses on how to represent hash algorithms at the Rust/C boundary.

Brian m. carlson responds to Patrick Steinhardt's suggestion to use Rust enums for hash algorithm representation, explaining why this approach wouldn't work. The key issue is that Git's current code sometimes zero-initializes object IDs, which would be undefined behavior with Rust enums (since they require valid values). This reinforces the series' current approach of using `uint32_t`/`u32` for FFI safety.

The exchange highlights an important design constraint in the Rust/C interop work - the need to maintain compatibility with existing Git memory initialization patterns while ensuring Rust safety guarantees. This is part of the broader challenge of introducing Rust code that must interoperate with Git's existing C codebase.

The response is brief but directly addresses a core technical concern in the series, showing the careful consideration being given to FFI boundary design. It also hints at future patches in the series that will demonstrate the practical consequences of these design choices.