This is a build system discussion within the SHA-1/SHA-256 interoperability patch series, specifically addressing library linking strategy for Rust test binaries.

The email continues the technical discussion about how to handle library dependencies for the Rust test infrastructure. Junio C Hamano is responding to Ezekiel Newren's comments about library organization, noting apparent confusion around the timeline of changes to `XDIFF_LIB` and `REFTABLE_LIB` in the Makefile. The core question is whether maintaining separate libraries (as currently implemented in the series) provides a better developer experience compared to Ezekiel's earlier proposal of consolidating everything into `libgit.a`.

Key technical points under discussion:
- The current patch links test binaries against multiple libraries (`libgit.a`, `libreftable.a`, `libxdiff.a`, `libz`)
- Ezekiel's earlier series had removed `XDIFF_LIB` and `REFTABLE_LIB` from the Makefile by consolidating them into `libgit.a`
- Junio questions whether the multi-library approach is problematic or if it represents a viable alternative to consolidation

The discussion remains unresolved, with Junio expressing continued confusion about the relative merits of each approach and whether the current implementation's linking strategy works effectively. This ties back to the broader build system integration work in patch 10/14 of the series, where Rust test infrastructure needs to properly link against Git's C libraries.