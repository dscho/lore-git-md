This is a technical discussion about naming and safety semantics in the Rust hashing implementation, part of the SHA-1/SHA-256 interoperability series. Ben Knoble responds to an exchange between brian m. carlson and Ezekiel Newren regarding the `Hasher` naming and safety distinction in the Rust code.

The discussion centers on two key aspects of patch 11/14: the naming of the `Hasher` type (which could conflict with `std::hash::Hasher`) and the meaning of "safe" in the context of hash algorithms (which refers to collision detection capabilities rather than Rust memory safety). brian m. carlson explains that the safety distinction exists to handle different SHA-1 implementations - some with collision detection (safe for untrusted data) and some optimized for performance (unsafe for untrusted data).

Ben Knoble suggests adding clarifying documentation to distinguish between Rust's memory safety and the cryptographic safety of the hashing algorithms. This would help prevent confusion between the two concepts, particularly since the term "safe" has different meanings in these contexts. The note would point to existing documentation about the FFI type for further details.

The exchange shows ongoing refinement of the Rust implementation details, with participants working to establish clear naming and documentation patterns that will serve both immediate needs and long-term codebase maintainability. The discussion remains technical and collaborative, focused on making the implementation as clear and correct as possible.