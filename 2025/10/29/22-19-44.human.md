This is part 6/10 in a refactoring series to modernize xdiff's data structures for Rust FFI compatibility. The patch splits the `ha` field in `xrecord_t` into two distinct fields: `line_hash` (a simple hash of the line content) and `minimal_perfect_hash` (a unique identifier assigned during classification). 

The change clarifies the previously confusing use of a single field for two different purposes - one for quick line comparisons and another for unique identification during diff processing. The patch updates all affected code paths across xdiff's algorithms (histogram, patience, and Myers diff) to use the appropriate hash field. 

This is a well-motivated change that improves code clarity while maintaining the exact same diff behavior. The patch is thorough, updating all relevant call sites consistently. It builds on the type safety work from earlier patches in the series (converting hash values to specific types like `uint64_t` and `size_t`) and helps establish clearer data model needed for Rust interoperability. The changes are mechanical but meaningful, touching core diff logic while being careful to preserve all existing behavior.