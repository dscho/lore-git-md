Final config polish for atomic ref updates in `git replay`.

This email from Christian Couder provides final review feedback on the last patch in the series implementing configurable atomic reference updates for `git replay`. The patch adds the `replay.refAction` configuration option with `update` (default) and `print` modes, complementing the existing `--ref-action` command-line option.

Couder points out two minor consistency issues in the implementation:
1. A naming inconsistency where `mode_str` parameter name in `parse_ref_action_mode()` differs from the `ref_action_str` naming used elsewhere in the code
2. A test hygiene observation that `test_config` could be used instead of raw `git config` commands in one test case, matching the style used in other tests

The feedback comes after the series has already received maintainer approval and addresses only final polish items. The core functionality - atomic updates via config option with proper precedence rules - remains unchanged from the approved implementation. The suggested changes would improve internal naming and test consistency without affecting behavior.

This represents the final round of review feedback before merging, focusing on small consistency improvements rather than substantive changes to the already-approved implementation. All major technical aspects (atomic transaction handling, config parsing, error cases) were validated in earlier rounds.