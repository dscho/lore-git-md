This is a technical discussion about the Rust implementation of `ObjectID` in the SHA-1/SHA-256 interoperability work (patch 4/14). The email continues the thread's examination of FFI boundary design decisions, specifically around type representation between C and Rust.

Patrick Steinhardt acknowledges brian m. carlson's explanation for using a fixed-size array with u32 algo field rather than a Rust enum - the need for FFI compatibility with existing C code. While recognizing the current approach is necessary for interoperability, Patrick raises concerns about setting good precedents for future Rust code in Git, suggesting an ideal separation between low-level FFI primitives and higher-level idiomatic Rust interfaces.

The discussion highlights the tension between immediate technical constraints (C compatibility requirements) and long-term codebase maintainability (establishing good Rust patterns). Patrick adopts a pragmatic stance, accepting the current design while noting this will likely require iteration as the project gains more Rust experience. The exchange confirms consensus on the `ObjectID` representation while leaving open the possibility of future refactoring once C dependencies are removed.

Key technical points:
- Current `ObjectID` uses `u32` algo field with fixed-size hash array (`[u8; GIT_MAX_RAWSZ]`)
- Rust enums would be preferable for type safety but incompatible with C FFI requirements
- Discussion reflects broader concerns about establishing Rust coding patterns in Git
- FFI boundary design remains a focal point for the series

This email doesn't introduce new technical proposals but reinforces the careful consideration being given to Rust/C interface design decisions in this foundational work. The tone suggests reviewers are converging on acceptable solutions while maintaining awareness of future maintainability concerns.