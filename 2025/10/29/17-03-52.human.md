This is a technical discussion about the object mapping format in the ongoing SHA-1/SHA-256 interoperability work, specifically patch 12/14 in the series introducing Rust infrastructure.

Junio C Hamano raises two key points about the proposed loose object mapping format. First, he notes initial confusion about the terminology - the "loose object format" being discussed is actually a mapping mechanism between primary and compatibility hash algorithms, not the traditional zlib-compressed object storage. Second, he questions whether the legacy mapping file format (`loose-object-idx`) should continue to be supported for reading, suggesting it may be worth dropping unless there's no maintenance cost.

The discussion occurs in the context of a larger effort to enable SHA-1/SHA-256 repository interoperability through new Rust infrastructure. This particular patch introduces a binary format for efficiently mapping between object IDs in different hash algorithms, addressing performance limitations of the current approach where object ID lists aren't sorted for quick lookup.

Junio's comments indicate careful review of the technical details while maintaining focus on keeping the implementation clean and maintainable. The thread shows active engagement from multiple core contributors working through design decisions for Git's first major Rust integration.