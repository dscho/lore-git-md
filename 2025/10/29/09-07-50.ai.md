This is a technical discussion about the Rust hashing implementation in the SHA-1/SHA-256 interoperability series, specifically addressing patch 11/14 which introduces object hashing functionality.

The email clarifies two key points from earlier discussion: First, the "safe" hashing distinction refers to using SHA-1-DC (a collision-resistant variant) versus potentially faster unsafe variants when hashing trusted data, matching existing C functionality. Second, the separate `alloc()` and `init()` calls (rather than a combined `git_hash_new()`) are maintained to allow potential future state reset capability, though the author is open to changing this if reviewers prefer.

The discussion shows consensus forming around these implementation details, with Patrick Steinhardt accepting the explanations about the safety distinction and allocation pattern after initial questions. This represents incremental progress in finalizing the Rust hashing infrastructure that will support the new object map format.

Key technical details confirmed:
- Safety distinction maps to SHA-1-DC vs hardware-accelerated variants
- Separate allocation/initialization preserved for future flexibility
- Maintains compatibility with existing C hash API patterns (`git_hash_alloc`, `git_hash_init`)

The thread continues to refine the Rust/C FFI boundary design while building toward the series' goal of enabling hash-algorithm interoperability through Rust infrastructure. This email resolves two open questions from earlier review while keeping other aspects (like Hasher naming) still under discussion.