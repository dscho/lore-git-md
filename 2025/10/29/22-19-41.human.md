This is part 3 of a 10-part refactoring series to modernize xdiff's data structures for better Rust FFI compatibility. The patch changes the `xrecord_t.ptr` field from `char*` to `uint8_t*` to align with Rust's byte-oriented memory model, since the field refers to raw bytes rather than text characters.

The change requires adding explicit casts to `char*` at all usage sites to maintain compatibility with existing xdiff functions that expect character pointers. The commit message notes that this casting approach was chosen to avoid a larger refactoring of xdiff code, though it does touch 7 files with 22 line modifications.

This is a straightforward technical change that follows the series' goal of establishing unambiguous type mappings between C and Rust. The thread context shows this series has already reached consensus and is ready for merging, with this patch being one of the simpler mechanical conversions in the overall effort. The change is well-contained and maintains behavior while improving type safety for cross-language interoperability.