This is a technical discussion about the Rust `ObjectID` implementation in the SHA-1/SHA-256 interoperability series, specifically addressing the handling of unknown hash algorithms in the `as_slice()` and `as_mut_slice()` methods.

The discussion focuses on the behavior when an unknown hash algorithm is encountered (represented by `None` in the match statement). Junio C Hamano expresses surprise that the current implementation returns the full hash buffer when an unknown algorithm is detected, rather than failing or returning an error. He notes this is particularly questionable for the read-only `as_slice()` case, though acknowledges the mutable version might be justified for initialization purposes.

Key technical details under discussion:
- The `ObjectID` struct's slice accessor methods currently return either:
  - A properly-sized slice when the algorithm is known (using `algo.raw_len()`)
  - The full `[u8; GIT_MAX_RAWSZ]` buffer when the algorithm is unknown
- Junio suggests this behavior should be more strict, especially for read operations
- The discussion builds on prior agreement about using `u32` for algorithm representation at the FFI boundary

This exchange represents ongoing refinement of the Rust/C interface design, specifically around error handling patterns when dealing with potentially invalid algorithm identifiers. The conversation suggests the current implementation may need adjustment to be more conservative in its handling of unknown algorithms, particularly for read operations.