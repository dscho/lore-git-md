This is a follow-up discussion on the `commitGraph.changedPaths` config option feature, with SZEDER Gábor raising concerns about the design and test coverage.

The email questions the behavior where setting `commitGraph.changedPaths=false` doesn't actually remove existing Bloom filters, only prevents new ones from being written. SZEDER argues this wastes resources by continuing to compute filters the user explicitly tried to disable. This appears to be a deliberate design choice in the patch series, but the objection suggests it may be counterintuitive to users.

SZEDER also requests additional test coverage for split commit-graph scenarios, noting this area has been historically problematic for changed-path Bloom filters. The current test suite in t5318-commit-graph.sh verifies basic config behavior but doesn't exercise the split graph case.

The discussion occurs after the patch series has already received Reviewed-by tags and been queued for 'next', indicating this is late-stage review feedback. The concerns touch on both user experience (the config behavior) and technical robustness (test coverage gaps), suggesting these points may need addressing before final merge.