This is a final polish patch in the atomic ref updates series for `git replay`, addressing Christian Couder's review feedback on naming consistency and test hygiene. The patch responds to two specific issues in the config option implementation: an inconsistent parameter name (`mode_str` vs `ref_action_str`) in the config parsing function, and use of raw `git config` instead of the preferred `test_config` helper in one test case.

The author agrees with both suggestions, noting that:
1. The naming inconsistency makes the code harder to follow, and similar functions in the codebase use simpler parameter names like `arg`
2. `test_config` is indeed the preferred approach even for multiple config operations, as it handles cleanup more robustly than manual `test_when_finished` with `|| true`

The changes are straightforward and will be incorporated in v6. This concludes all outstanding review feedback on the series, which has already received maintainer approval after addressing all technical concerns from multiple reviewers (performance, atomicity, interface design, and test coverage).

Key technical details referenced:
- `parse_ref_action_mode()` function interface
- `test_config` vs manual config cleanup in tests
- Similar patterns in `gpg-interface.c`
- Test cleanup robustness considerations

The thread context shows this is the final patch in a fully-reviewed series that has addressed all feedback, with these being the last minor consistency nits. The implementation follows established Git patterns and has consensus from all technical reviewers.