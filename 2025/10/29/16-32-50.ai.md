This is a technical discussion about memory allocation practices in the Rust/C FFI boundary of the SHA-1/SHA-256 interoperability work.

Junio C Hamano raises a question about memory allocation strategy in patch 9/14 of the series, which exposes hash context functions to Rust. He notes that `git_hash_alloc()` uses plain `malloc()` rather than Git's customary `xmalloc()`, which aborts on out-of-memory (OOM) conditions. The question probes whether this represents an intentional shift toward explicit OOM handling in the Rust portions of the codebase.

The query touches on an important design decision for the FFI boundary: whether to maintain Git's traditional OOM behavior (aborting via `xmalloc`) or adopt Rust's more explicit error handling patterns. This comes at a critical point in the series where C/Rust memory management practices intersect. The answer could influence both immediate implementation choices and long-term patterns as more Rust code is introduced.

The discussion occurs against the backdrop of ongoing work to establish robust FFI patterns, with previous emails having addressed type safety, algorithm representation, and error handling approaches. This particular question about allocation strategy remains open, with no immediate response yet from the patch author. The resolution may affect other FFI-boundary functions in the series and set precedent for future Rust/C interactions in the codebase.