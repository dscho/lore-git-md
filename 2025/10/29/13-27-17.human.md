This is a technical discussion about the Rust implementation of `ObjectID` in the ongoing SHA-1/SHA-256 interoperability work. The conversation focuses on how to handle unrecognized hash algorithms when converting an `ObjectID` to a byte slice.

Junio Hamano responds to Ezekiel Newren's earlier comment about error handling, clarifying that his surprise wasn't about using `Option` vs `Result`, but rather about the behavior when an unknown algorithm is encountered. The current implementation returns the full hash buffer for unknown algorithms, which Hamano finds questionable - he'd expect it to fail instead. He notes this might make sense for the mutable case (where you'd want to write the full buffer), but not for the read-only case.

This discussion represents a refinement of the Rust/C FFI boundary design, specifically around error handling and safety when dealing with hash algorithms. The thread shows active engagement from maintainers in shaping Git's first major Rust code, with attention to both practical interoperability needs and long-term code quality.