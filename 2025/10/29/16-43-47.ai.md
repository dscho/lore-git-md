This is a build system discussion in the context of the ongoing SHA-1/SHA-256 interoperability work introducing Rust code (patch 10/14 of the series). The email focuses on linking dependencies for Rust test binaries.

Junio C Hamano responds to brian m. carlson's patch adding a `build.rs` script that explicitly links against `libgit.a`, `libreftable.a`, `libxdiff.a`, and `libz` when building Rust test binaries. Junio recalls Ezekiel Newren previously advocating for rolling reftable and xdiff into `libgit.a` to simplify linking, but notes that if the current approach only requires these simple link directives, the complexity may be acceptable. He expresses confusion about the current state since `XDIFF_LIB` and `REFTABLE_LIB` variables no longer exist in the main Makefile, suggesting this might warrant revisiting earlier changes.

The discussion touches on an important build system design question about library organization - whether to maintain separate static libraries or consolidate them into `libgit.a`. This affects both the test build process shown here and potentially future Rust integration patterns. The thread context shows this is part of broader FFI boundary design considerations in the series, though this particular email focuses narrowly on build-time linking.