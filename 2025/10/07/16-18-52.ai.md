This is a review response in the ongoing refactoring series about tag peeling behavior in Git's refs subsystem. Karthik Nayak is examining patch 5/13, which converts `upload-pack.c` to use the new `reference_get_peeled_oid()` API introduced earlier in the series.

The patch focuses on `write_v0_ref()`, a callback function that handles both capability advertisement and reference propagation in upload-pack. Karthik confirms the technical approach of manually constructing a `struct reference` for the capability advertisement case, allowing consistent use of the new peeling interface. He notes this as a reasonable interim solution while observing that the dual-purpose nature of `write_v0_ref()` suggests these operations should ideally be separated into distinct functions.

Karthik's review validates the correctness of the changes while identifying potential future cleanup work. The response indicates no blocking concerns with this specific patch, though it surfaces architectural considerations about the upload-pack code structure that could be addressed separately from the current refactoring effort. The review maintains the series' momentum toward eliminating global state in the refs subsystem while preserving all existing functionality.