CI infrastructure patch removing redundant `apt-get update` calls in the Rust CI setup.

This first patch in Patrick Steinhardt's Rust CI series eliminates duplicate `apt-get update` commands from the dependency installation script. The change recognizes that the initial distro-specific setup already updates package sources, making subsequent updates in job-specific sections unnecessary. The modification removes four redundant update calls while keeping all actual package installations intact.

The patch affects `ci/install-dependencies.sh`, specifically targeting sections for ClangFormat, StaticAnalysis, sparse, and Documentation jobs. This appears to be a straightforward cleanup with no behavior change beyond eliminating redundant network operations during CI setup. The change aligns with the series' goal of streamlining Rust-related CI infrastructure.

Technical details show the patch simply deletes the update commands while preserving the actual package installation lines. This follows established patterns for CI optimization where redundant operations are removed to speed up builds. The change is low-risk since it doesn't modify any package installation logic or dependencies.