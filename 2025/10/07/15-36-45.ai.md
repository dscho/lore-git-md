This is a design discussion email in the submodule gitdir path encoding series, where Junio C Hamano responds to Adrian Ratiu's implementation approach for handling existing submodule gitdirs when enabling the new encoding feature.

Junio suggests a more robust transition strategy that would treat `submodule.<name>.gitdirpath` as the authoritative mapping mechanism rather than just an override. The key proposal is to register all existing submodule gitdirs under their original names in this mapping when enabling the extension, then use the encoding scheme only for new submodules added afterward. This would make the system more maintainable by having a single source of truth for the mapping.

The email outlines a potential user-facing explanation that would describe the encoding's purpose (filesystem compatibility) and nature (irreversible transformation) without exposing implementation details that might tempt users to bypass the official mapping. Junio's approach would simplify the system by eliminating the need to maintain parallel paths - one for encoded new submodules and another for unencoded legacy ones.

This continues the thread's progression toward finalizing the migration strategy, with Junio providing concrete suggestions for how to handle the encoding transition while maintaining backward compatibility. The proposal aligns with the previously settled technical decisions about using percent-encoding while addressing the practical concerns of the transition period.