Refactoring patch in the submodule gitdir path encoding series, addressing Junio's review comments on patch v3. This change updates `add_submodule()` in submodule--helper to use the centralized `submodule_name_to_gitdir()` function rather than hardcoding path construction, bringing it in line with other submodule handling code. The commit message will be reworded per Junio's suggestion to clarify this is adopting an existing helper rather than introducing a new one.

The patch also incidentally narrows the scope of `submod_gitdir_path` variable since it's now only used in one code path. Junio notes this secondary effect could be mentioned in the commit message for completeness. This appears to be a straightforward mechanical change with no behavior impact, just aligning with the project's established patterns for submodule path handling.

Key technical details:
- Modifies `builtin/submodule--helper.c`
- Uses existing `submodule_name_to_gitdir()` helper from submodule.c
- Follows pattern established by commit ce125d431a
- No test changes needed since this maintains existing behavior

The change is uncontroversial and addresses Junio's clarity concerns about the commit message wording. This appears to be the final polish needed before this patch can proceed in the series.