Packfile store API refactoring series (v6) from Taylor Blau and Patrick Steinhardt, concluding the cleanup of pack iteration patterns. The series has progressed through removing the deprecated `packfile_store_get_packs()`, introducing `packfile_store_for_each_pack()` macro, and now simplifying the remaining API by renaming `packfile_store_get_all_packs()` to `packfile_store_get_packs()` in the final patch.

Core work remains focused on internal pack subsystem improvements with no user-facing impact, systematically converting callers across 20+ files to use standardized iteration patterns. Taylor Blau confirms and approves Patrick Steinhardt's changes to `object-name.c`, specifically converting `find_short_packed_object` and `find_abbrev_len_packed` to use `get_all_packs()`, noting these are safe since both functions already properly filter out MIDX'd packs through `unique_in_pack` discarding packs with `multi_pack_index` bit set.

Technical details show the changes touch core pack operations (pack-bitmap, pack-objects), maintenance commands (gc, repack), and object lookup paths, eliminating confusing statefulness between `get_packs()` and `get_all_packs()` where callers couldn't reliably predict MIDX'd pack inclusion. The final patch remains purely mechanical, updating fast-import, pack-objects, and packfile internals to use the simplified function name now that the deprecated variant is gone. The series demonstrates careful focus on pack subsystem internals independent of larger efforts like ODB abstraction, with all patches being behavior-preserving improvements that follow the incremental strategy - first ensuring safe migration paths, then introducing quality-of-life macros, and finally cleaning up naming. Both authors maintain technical alignment throughout, with no remaining open questions as the series appears complete.