Packfile store API refactoring series (v1) from Taylor Blau, building on prior packfile store introduction work. Focuses on eliminating confusion between `packfile_store_get_packs()` and `packfile_store_get_all_packs()` functions in the packfile subsystem. Core issue: behavior difference between these functions is non-obvious to callers, with results depending on whether `get_all_packs()` was called first.

Changes systematically convert all callers to use `get_all_packs()`, then remove `get_packs()` entirely. Introduces new `FOR_EACH_PACKFILE_IN_STORE()` macro for iteration and renames remaining function to more descriptive name. Touches 22 files across core pack operations (`packfile.c`, `midx.c`), builtin commands (`pack-objects.c`, `repack.c`), and test helpers (`t/helper/test-packfile.c`).

Technical details: follows recent midx and repository work, maintains all existing functionality while simplifying interface. No user-facing behavior changes - purely internal API improvement. Each commit handles specific component conversions before final removal. Related to ongoing packfile store abstraction work but independent of other large efforts like ODB abstraction or reftable.

Open questions: none raised yet in this initial submission. Potential areas for review attention include macro safety and whether any edge cases exist in the mechanical conversions. Taylor Blau's involvement signals deep pack subsystem expertise; changes align with his established work in this area.