Packfile store API refactoring series (v5) from Taylor Blau and Patrick Steinhardt, continuing the cleanup after removing the deprecated `packfile_store_get_packs()` in part 4/6. The series has systematically converted callers to `get_all_packs()` and now introduces a new `packfile_store_for_each_pack()` macro in part 5/6 to standardize pack iteration patterns.

Core work remains focused on internal pack subsystem cleanup with no user-facing impact. The new macro replaces repetitive `for (p = packfile_store_get_all_packs(); p; p = p->next)` loops with a simpler `packfile_store_for_each_pack(store, p)` construct across 20+ files, reducing boilerplate while maintaining identical iteration semantics. This follows the series' methodical approach of incremental improvements after ensuring safe migration paths.

Technical details show the macro wraps `get_all_packs()` in packfile.h and updates callers in core pack operations (pack-bitmap, pack-objects), maintenance commands (gc, repack), and object lookup paths. The changes are purely mechanical with no behavior modifications. The series has now touched 23+ files across the codebase, demonstrating careful focus on pack subsystem internals independent of larger efforts like ODB abstraction. No new open questions emerge from this quality-of-life improvement, which validates the series' incremental strategy while preparing for the final patch in the sequence. Both authors maintain their technical alignment with pack subsystem expertise throughout.