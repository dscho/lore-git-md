This is a follow-up discussion in the submodule gitdir path encoding series, specifically addressing the placement of the `is_rfc3986_unreserved()` helper function. The thread context shows this was previously debated, with Junio C Hamano objecting to its proposed location in strbuf.[ch] since strbuf doesn't use URL functionality.

Adrian Ratiu acknowledges Junio's point about the strbuf placement being inappropriate, explaining they only chose that location due to historical precedent. They now propose moving it to url.[ch] instead, while remaining open to other suggestions. This email represents the final step in resolving the helper function location debate that was a key open question in the series.

The technical context involves this function being needed for URL-encoding submodule names to prevent filesystem conflicts, with the series introducing an `extensions.submoduleEncoding` config option. The helper was previously moved to credential-store.c when that was its only consumer, but now needs to be shared with submodule.c for the new encoding functionality.

This discussion is now effectively closed with consensus forming around placing the helper in either url.[ch] or submodule.c, with Junio having previously expressed strong preference for submodule.c as the current primary consumer. The email shows the author's willingness to accommodate the project's architectural preferences now that the strbuf location has been definitively ruled out.