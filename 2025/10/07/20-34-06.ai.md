Bugfix for color consistency in `git status --short -z` output.

This patch finalizes the fix for inconsistent color handling in `git status --short -z` by ensuring untracked file markers (`??`) are colored consistently with modified file markers (`M`). Jeff King provides the authoritative resolution to the thread, confirming that while `-z` typically indicates machine-readable output, the combination with `--short` explicitly requests human-readable (colored) output with NUL separators.

The technical change moves the color application in `wt_shortstatus_other()` (in `wt-status.c`) before the null-termination check, aligning with the pattern used in `wt_shortstatus_unmerged()`. The patch includes a new test case that verifies color preservation in `-z --short` mode by converting NULs to newlines for test comparison. This maintains the documented behavior where `--short` keeps colors while `--porcelain` suppresses them, resolving the inconsistency without changing the fundamental (if unusual) `--short -z` combination behavior.

The commit message provides thorough rationale about the expected behavior and use cases, and the implementation appears clean with proper test coverage. This concludes the thread with a clear resolution to the identified inconsistency.