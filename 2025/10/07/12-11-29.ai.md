This is a code hygiene patch in the reftable fsck validation series, specifically addressing enum ordering in `fsck.h`. The patch alphabetizes the `fsck_msg_type` enum entries, which had some minor ordering inconsistencies. The changes are purely organizational - no behavior is modified.

The patch moves several error and warning message types into proper alphabetical sequence, including:
- Reordering `BAD_REF*` entries to group related items
- Moving all `GITMODULES_*` and `GITATTRIBUTES_*` entries together in their respective sections
- Sorting warning and info-level messages consistently

This appears to be final polish work on the fsck message infrastructure that was introduced earlier in the series. The changes are straightforward and mechanical, with no functional impact. The patch maintains the existing fsck message categorization (ERROR/WARN/INFO) while just improving code organization.

This is part of the v6 iteration of the reftable fsck series that has already received maintainer approval and been merged to 'next'. The change follows through on the code hygiene improvements that were part of the series' final polishing phase, similar to the earlier header cleanup patch (1/7) that removed unused includes.