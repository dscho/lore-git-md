This is part 4/6 of the packfile store API refactoring series, now removing the deprecated `packfile_store_get_packs()` function after converting all callers in previous patches. The change is straightforward - deleting the function definition and its header declaration since it's no longer used anywhere in the codebase.

The removal marks a milestone in the series' goal to eliminate the confusing dual API where `get_packs()` and `get_all_packs()` behaved differently based on call order. With all callers now using the more predictable `get_all_packs()` variant, this cleanup patch safely removes the old interface without any behavior changes.

The patch follows the series' careful, incremental approach - each step first migrates callers before removing the old API. This particular change is low-risk since it touches only internal implementation files (packfile.c/h) and comes after thorough verification that no code still depends on the removed function. The diff shows a clean deletion of the function and its documentation, leaving only the preferred `get_all_packs()` variant.

This continues the series' pattern of focused, mechanical changes to improve the pack subsystem's internal APIs while maintaining stability. The removal of `get_packs()` represents concrete progress toward the series' goal of simplifying pack store interactions.