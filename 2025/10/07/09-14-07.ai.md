Performance investigation into untracked files cache behavior, focusing on real-world tooling impacts. Devste Devste confirms the "sticky cache" phenomenon observed in SourceTree usage, where the cache state persists based on initial conditions and cannot be updated due to tooling-imposed constraints. The email highlights a practical workaround (running `git status --untracked-files` manually) while acknowledging the inability to modify SourceTree's command-line flags.

The discussion builds on Matthew Hughes' identification of `--no-optional-locks` as the specific flag preventing cache updates via `repo_update_index_if_able()`. Devste's observation about SourceTree's fixed command-line arguments demonstrates how this issue affects real-world GUI tools that may combine `--no-optional-locks` with untracked file operations. The "sticky cache" behavior means tools inheriting cache state from other operations may permanently suffer performance degradation if they use these flag combinations.

Key technical points: the cache update mechanism's dependency on optional locks creates a situation where GUI tools (like SourceTree) cannot refresh the cache due to their default flag usage. The workaround suggests users must manually run cache-updating commands outside their normal workflow. This highlights a tension between the cache's design assumptions and real-world tooling constraints, where performance optimization features become ineffective for common usage patterns.