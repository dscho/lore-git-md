This is a submodule bugfix patch that's part of the ongoing submodule gitdir path encoding series. Adrian Ratiu responds to Junio C Hamano's review of a previous version, agreeing to clarify the commit message and add details about a variable scope change.

The patch fixes test failures in `submodule--helper` by making it use the centralized `submodule_name_to_gitdir()` helper function rather than hardcoding paths, bringing it in line with other parts of the codebase. Junio had initially misunderstood the change as introducing a new helper rather than using an existing one, so Adrian agrees to reword the commit message for clarity.

The change also incidentally narrows the scope of a variable that's no longer needed outside a specific block, which Junio notes should be mentioned in the commit message. This appears to be a straightforward cleanup patch that addresses a consistency issue in the submodule path handling code. The discussion is focused on commit message clarity rather than any technical concerns with the change itself.