This is a technical clarification in the ongoing submodule gitdir path encoding series. Junio C Hamano responds to Adrian Ratiu's point about handling existing submodule gitdir names when the new encoding extension is enabled.

Junio seeks confirmation that the proposed `submodule.<name>.gitdirpath` configuration will properly handle edge cases where existing gitdir names might coincidentally contain percent-encoded sequences (like `%41`). He suggests that explicitly recording the literal path in this config variable would prevent any accidental decoding of pre-existing paths that happen to look encoded.

The exchange shows the maintainer validating a subtle but important detail in the migration strategy - ensuring backward compatibility isn't broken by the new encoding scheme. This appears to be one of the final verification steps before the series can be merged, as the core design decisions have already been settled in previous discussion.