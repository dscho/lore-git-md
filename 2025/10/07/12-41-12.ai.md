This is the final patch in a refactoring series focused on the packfile store API. The patch simplifies the naming of `packfile_store_get_all_packs()` to `packfile_store_get_packs()` now that the deprecated `packfile_store_get_packs()` function (with different semantics) has been removed in earlier parts of the series. The change is purely mechanical, updating callers in fast-import, pack-objects, and packfile internals to use the new name.

The patch continues the series' pattern of incremental, careful improvements to the pack subsystem internals. It touches the same key areas as previous patches (pack operations and object lookup paths) but focuses solely on the naming cleanup. The diff shows straightforward replacements across 4 files, with no behavior changes - just removal of the now-redundant "all" infix from the function name.

This concludes the series that has systematically modernized pack iteration patterns, introducing the new `packfile_store_for_each_pack()` macro in the previous patch and now finalizing the API naming. The changes demonstrate consistent attention to detail and maintain the series' focus on internal quality-of-life improvements for pack handling code.