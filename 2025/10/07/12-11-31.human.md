This is the final patch in Karthik Nayak's reftable fsck validation series, implementing table name validation for the reftable backend. The patch adds a check that enforces the reftable specification's recommended naming convention (`${min_update_index}-${max_update_index}-${random}.ref`) and reports non-conformant names as warnings through Git's fsck infrastructure.

The implementation introduces a new fsck warning ID (`badReftableTableName`) and maps reftable library errors to Git's fsck system. It includes thorough test coverage in `t0614-reftable-fsck.sh` that verifies both valid repositories and various invalid table name patterns. The patch also maintains code hygiene by keeping header includes in lexicographical order.

As the last piece of this well-reviewed series (now merged to 'next'), this completes the table-level validation work while leaving reference-level checks as identified future work. The implementation is clean and follows the callback-based design established earlier in the series, with careful error handling and progress reporting.