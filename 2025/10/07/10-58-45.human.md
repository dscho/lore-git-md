This is the 8th patch in Patrick Steinhardt's 13-part series refactoring Git's refs subsystem, focusing on tag peeling behavior and iterator interfaces. The patch removes the `current_ref_iter` global variable and its associated `peel_iterated_oid()` function, which were previously used as a performance optimization hack for tag peeling during ref iteration.

The change builds on earlier patches that introduced a cleaner approach - storing peeled object IDs directly in the new `struct reference`. With this infrastructure in place, the old global state can be eliminated entirely, making the code more robust and easier to understand. The patch removes the variable declaration and all related code paths across three files (`refs.c`, `refs/iterator.c`, and `refs/refs-internal.h`).

This is a straightforward cleanup that follows naturally from the architectural improvements in previous patches. It demonstrates good attention to removing technical debt once better alternatives are available. The change appears well-contained and low-risk, touching only internal refs implementation details rather than affecting user-facing behavior.