This is a follow-up discussion in the submodule gitdir path encoding series, focusing on implementation details of the path encoding scheme. The thread has already reached consensus on using URL percent-encoding and config-based mapping via `submodule.<name>.gitdirpath`.

Junio pushes back against over-encoding of uppercase letters, arguing they should only be encoded when necessary to prevent filesystem conflicts (case collisions or path overlaps). He reiterates the importance of maintaining a single stable encoding scheme rather than leaving room for future encoding variants, though notes the config-based approach does allow for internal algorithm improvements without breaking existing mappings.

Key technical points:
- Encoding should be minimal - only applied when needed for filesystem safety
- `submodule.<name>.gitdirpath` serves as the authoritative mapping, insulating users from encoding algorithm changes
- No need to expose encoding details to end users in documentation
- The discussion confirms the series is in final polishing stages with all major design decisions settled

The exchange shows the maintainer and contributor aligning on final implementation details after the broader architecture was agreed upon in earlier rounds. This represents the fine-tuning phase of a mature patch series where only minor edge cases remain before merging.