Thread identity: Documentation-focused continuation of discussion about `git bundle --all` behavior regarding remote refs during cloning, originally reported by Andrew Harmon and confirmed by Junio C Hamano as expected behavior matching Git's standard remote cloning semantics.

Core problem: Persistent documentation gap around the philosophical and technical alignment between bundle cloning and remote repository cloning behaviors, particularly regarding default ref fetching. Andrew Harmon's latest email provides concrete reproduction demonstrating that while `git bundle --all` includes all refs, subsequent operations follow conservative fetch semantics identical to network clones.

Current status: Maintainer consensus firmly established that behavior is correct but underdocumented. Andrew's script now serves as canonical demonstration of: (1) bundle creation with `--all` including all refs (verifiable via `git bundle verify`), (2) default clone fetching only `refs/heads/*`, (3) two established solutions - explicit fetch syntax (`git fetch origin 'refs/remotes/origin/*:refs/remotes/origin/*'`) and mirror cloning (`git fetch repo.bndl "refs/*:refs/*"`).

Key technical details: The reproduction script conclusively shows bundle mechanics - all refs are properly included in bundle file but follow standard fetch rules during application. This mirrors Git's network clone behavior where remote-tracking branches represent the clone's view of origin, not the original repository's remote state. Junio's earlier explanations confirm this preserves `git bundle`'s design as network-less clone mechanism.

Open questions: Documentation requirements now clearly specified: (1) philosophical explanation of why remote-tracking branches aren't fetched by default (security/conservative design), (2) side-by-side examples showing default clone vs complete transfer methods, (3) technical note about bundle contents versus fetch semantics using Andrew's script as reference case. No remaining technical questions - only documentation work needed.

Participant positions: Junio C Hamano's maintainer position remains unchanged - behavior is correct. Andrew Harmon has transitioned from bug reporter to documentation contributor by providing ideal teaching example. Thread shows consensus that documentation should: explain design rationale, demonstrate workarounds, and use Andrew's case as illustrative example.

Impact: High-value documentation opportunity identified. Required updates would help users understand: the intentional parallel between bundle and remote clone behaviors, the security-conscious design behind default fetch rules, and the explicit syntax needed for complete repository transfer. Andrew's script provides ready-made example for documentation.