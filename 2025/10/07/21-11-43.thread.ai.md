Updated AI summary:

Thread identity: Discussion about `git bundle --all` behavior regarding remote refs during cloning, originally reported as a bug but now clarified as expected behavior by maintainer.

Core problem: Initial report highlighted inconsistency where `git bundle create --all` includes all refs (local and remote-tracking) but cloning from such bundles only fetches local branch refs (`refs/heads/*`). Maintainer Junio C Hamano explains this matches Git's standard remote cloning behavior - remote-tracking branches from the original repository (`refs/remotes/origin/*`) are not automatically fetched, as these represent the bundle creator's remote state rather than content meant for the clone.

Current status: Behavior confirmed as intentional design rather than bug. Two proper solutions provided: (1) explicit fetch of remote refs using `git fetch origin 'refs/remotes/origin/*:refs/remotes/origin/*'` syntax, or (2) mirror clone approach via `git fetch repo.bndl "refs/*:refs/*"`. The thread is shifting toward documentation improvement rather than code change.

Key technical details: Bundle creation (`--all`) and clone operations correctly implement Git's ref handling semantics - bundles include all requested refs, while clones follow the same conservative ref import policy as remote cloning. The disconnect stems from different expectations about what "all refs" should mean during clone versus bundle creation.

Open questions: Whether and how to document this behavior more clearly in `git bundle` man pages. Potential additions could explain: (1) the distinction between bundle contents and clone behavior, (2) recommended patterns for complete repository transfer, and (3) analogy to regular remote clone operations.

Participant positions: Junio C Hamano (maintainer) has authoritatively established this as expected behavior, not a bug. Original reporter's concern about offline workflow limitations remains valid but now framed as a documentation/clarification issue rather than code defect.

Impact: While not changing core functionality, this clarification affects how users should approach complete repository transfers via bundles. The explicit fetch patterns and mirror clone alternatives provide workable solutions once properly documented.