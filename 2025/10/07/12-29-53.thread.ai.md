Updated AI thread summary:

This thread implements a `--signed-tags=<mode>` option for `git fast-import` to achieve signature handling parity with `git fast-export`, supporting all signature types (PGP, X.509, SSH) consistently. Christian Couder's series progresses through documentation updates, test improvements, and implementation across both commands, with Patrick Steinhardt providing key technical review.

The series modernizes git-tag documentation to reflect all signing methods (using "cryptographic signature" terminology per Patrick's suggestion), improves test infrastructure in t/lib-gpg.sh and t9350-fast-export.sh, and implements core functionality in fast-import.c with new signed_tag_mode variable and handle_tag_signature() function. The implementation uses parse_signed_buffer() for universal format support, centralizing signature logic while correctly handling message size calculations. The final patch adds `--signed-tags=<mode>` to git fast-import with the same modes as fast-export (verbatim, warn-verbatim, warn-strip, strip, abort) but defaulting to verbatim instead of abort.

Christian addresses test fragility in t9350-fast-export.sh by introducing a dynamic tag count mechanism (ANNOTATED_TAG_COUNT), responding to Patrick's review of patch 3/5. While Patrick suggested isolating tests, Christian prefers the counter approach despite its ugliness as it surfaces technical debt. In response to Patrick's latest review, Christian agrees to remove empty lines at test case boundaries in the X.509 signed tag verification test, maintaining the counter-based approach while conforming to modern test script formatting conventions.

The documentation discussion regarding signature format listing in git-tag(1) has been resolved with Patrick Steinhardt agreeing to Christian Couder's approach of simply referencing the `gpg.format` configuration variable rather than enumerating all supported formats (`openpgp`, `x509`, `ssh`). This avoids documentation duplication while maintaining discoverability of the configuration point, aligning with Git's documentation strategy of centralizing detailed format specifications in git-config(1).

The implementation introduces a new handle_tag_signature() function in builtin/fast-import.c that uses parse_signed_buffer() to universally handle PGP, X.509, and SSH signatures. Christian addresses Patrick's review comments about documentation ordering (justifying the non-alphabetical placement based on historical precedent and fast-export's ordering) and promises to fix minor issues in V2: marking error messages for translation, fixing comment indentation, and addressing test formatting. A technical discussion clarifies that while users can manually add signatures anywhere, Git's own signatures are always appended to the end, making the current truncation approach safe.

All open questions have been resolved, with only minor documentation and style nits remaining to be addressed in V2. Patrick has approved the technical approach, and the discussion has settled on implementation details after earlier rounds of review focused on test infrastructure, signature handling behavior, and documentation strategy. The patch series is now in its final refinement stage pending resolution of these final nits.