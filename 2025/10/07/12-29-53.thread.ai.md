Updated AI thread summary:

This thread introduces a new `--signed-tags=<mode>` option for `git fast-import` to handle tag signatures similarly to its existing `--signed-commits` functionality, bringing parity with `git fast-export`'s behavior. The series from Christian Couder builds on prior signature handling work, aiming to help repository filtering tools like `git-filter-repo` manage signatures during operations.

The series begins with documentation update (patch 1) modernizing git-tag's man page to be inclusive of non-GPG signing methods (GPG, X509, SSH) via `gpg.format` configuration. Implementation work follows with preparatory groundwork (patches 2-6) covering test infrastructure improvements. Patch 2 modifies t/lib-gpg.sh to use `mkdir -p` when creating GNUPGHOME. Patch 6 improves t9350-fast-export.sh by replacing hardcoded tag counts with a dynamic `ANNOTATED_TAG_COUNT` variable.

The main feature implementation (patch 7) adds the `--signed-tags` option to fast-import.c with modes ("ignore", "verbatim") matching fast-export's capabilities. A new patch (8) modifies `builtin/fast-export.c` to use `parse_signed_buffer()` instead of hardcoded PGP detection, enabling proper handling of all Git-supported signature types (PGP, X.509, SSH). Test coverage is expanded in t9350-fast-export.sh with new cases for X.509 and SSH signatures, verifying:
- Signature preservation in `verbatim` mode
- Signature stripping in `strip` mode
- Backward compatibility

The implementation maintains consistency with Git's signing infrastructure and integrates with the preparatory work through use of the `ANNOTATED_TAG_COUNT` variable. The series shows clear progression from documentation through test improvements to implementation, addressing an oversight where only PGP signatures were properly detected in fast-export. Windows CI failures appear unrelated to the actual changes.