This thread implements a `--signed-tags=<mode>` option for `git fast-import` to achieve signature handling parity with `git fast-export`, supporting all signature types (PGP, X.509, SSH) consistently. Christian Couder's series progresses through documentation updates, test improvements, and implementation across both commands, with Patrick Steinhardt providing key technical review.

The series modernizes git-tag documentation to reflect all signing methods (using "cryptographic signature" terminology per Patrick's suggestion), improves test infrastructure in t/lib-gpg.sh and t9350-fast-export.sh (including GPGSM directory creation discussions), and implements core functionality in fast-import.c with new signed_tag_mode variable and handle_tag_signature() function. The implementation uses parse_signed_buffer() for universal format support, centralizing signature logic while correctly handling message size calculations.

The final patch adds `--signed-tags=<mode>` to git fast-import with the same modes as fast-export (verbatim, warn-verbatim, warn-strip, strip, abort) but defaulting to verbatim instead of abort. Patrick's review identifies minor issues: documentation ordering (should follow `--signed-commits` alphabetically), untranslated abort message, misindented fallthrough comments, and questions whether signatures always appear at tag message end (a key implementation assumption). Test coverage in t9306-fast-import-signed-tags.sh verifies behavior across all signature types and modes.

Open questions include Patrick's inquiry about signature positioning in tag messages and whether the test isolation approach in t9350-fast-export.sh might need refinement. The series otherwise achieves unified signature management for repository filtering workflows while modernizing documentation and infrastructure, with Patrick approving the technical approach pending resolution of minor documentation and style issues.