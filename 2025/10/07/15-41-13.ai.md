This is a follow-up discussion in the submodule gitdir path encoding series, specifically addressing the config override mechanism for submodule gitdir paths. Junio C Hamano responds to Adrian Ratiu's patch that introduced the ability to override gitdir paths via config files.

Junio makes two key points about the implementation:
1. He now accepts the existence of this configuration setting but suggests it should serve as the authoritative source of truth for all submodule gitdir paths, not just an override mechanism. This aligns with his broader proposal in the thread to use these config entries as the primary mapping system during the transition to encoded paths.
2. He points out a discrepancy between the patch's documentation (which refers to `submodule.<name>.gitdir`) and the actual implementation (which uses `submodule.<name>.gitdirpath`).

The discussion continues to refine the transition strategy for existing submodules when enabling the encoding extension, with Junio advocating for a more comprehensive approach where all submodule gitdir paths would be registered in the config mapping. This email represents final polishing of the implementation details after the major architectural decisions about encoding scheme and migration behavior have been settled in previous thread discussions.