This is a follow-up discussion in the submodule gitdir path encoding series, which has now reached consensus on all major design points. Junio Hamano provides some final guidance on implementation details before the series is merged.

The discussion clarifies that while percent-encoding will be used for path encoding, it should only be applied when necessary to prevent actual filesystem conflicts (like case collisions or path overlaps), not universally for all uppercase letters. Junio emphasizes that the encoding algorithm can be improved over time since the authoritative mapping is stored in `submodule.<name>.gitdirpath` config entries, making the actual encoding scheme an implementation detail.

The email confirms that Adrian Ratiu has agreed to drop earlier ideas about supporting multiple encoding schemes in the future, avoiding potential versioning headaches. Junio notes this flexibility means the exact encoding algorithm doesn't need to be documented to end users, as the config entries serve as the source of truth.

This represents final polishing of the implementation details in what appears to be a mature and well-considered patch series, with all major technical decisions now settled between the key contributors. The thread shows the maintainer and contributor aligning on last subtle points before merging.