This is a review comment on a Rust infrastructure patch in Patrick Steinhardt's series adding comprehensive CI support for Git's Rust code.

Karthik Nayak provides a minor but useful suggestion for patch 2/6, which introduces Rust code formatting checks via rustfmt. The original patch explains the rationale for using rustfmt's default style (consistency across the Rust ecosystem) and notes the edition is automatically derived from Cargo.toml. Karthik points out the commit message could clarify that `cargo fmt` is just a wrapper around rustfmt that handles file discovery - a small but helpful implementation detail for readers.

The exchange shows careful review of the Rust CI series, though this particular comment is purely additive rather than identifying any problems. The thread context indicates this is part of a well-structured effort to establish robust Rust tooling, with this patch focusing specifically on formatting standards enforcement.