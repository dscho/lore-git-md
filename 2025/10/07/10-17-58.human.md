This is a discussion about handling long path names in the submodule gitdir encoding series. Adrian Ratiu is responding to Junio C Hamano's feedback about whether to error out when encoded submodule names exceed filesystem length limits.

The key points:
- The current patch adds explicit length checking, but Junio questions whether this is necessary if the OS would fail anyway
- Adrian is open to dropping this commit entirely in the next version
- They discuss alternative approaches like using hashed names (e.g., `.git/modules/<hash>_<name>`) to avoid length issues
- The conversation reveals the series has evolved from having separate encoded/unencoded paths to now encoding all names (with legacy exceptions)
- Adrian notes the prior consensus was to avoid maintaining a separate name mapping database due to complexity risks

This appears to be one of the final design decisions needed before the series can be finalized. The discussion is technical but collegial, with Adrian showing flexibility about implementation choices while Junio probes the architectural implications. The outcome will affect how Git handles edge cases with very long submodule names when the new encoding scheme is enabled.