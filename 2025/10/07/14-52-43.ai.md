This is a review comment on patch 4/13 of Patrick Steinhardt's refs iterator refactoring series, focusing on the peeled object ID exposure changes. Karthik Nayak provides feedback on the implementation details while generally approving the approach.

The patch introduces a new `peeled_oid` field in `struct reference` to avoid relying on global state for tag peeling, replacing the previous `peel_iterated_oid()` mechanism. Karthik confirms the technical correctness of the implementation across the files, packed, and reftable backends, noting the proper handling of field resets during iterator advancement.

Key technical points from the review:
- Clarifies that only packed-refs (not all files backend refs) store peeled values
- Approves the new `reference_get_peeled_oid()` function as a cleaner alternative to the global-state-dependent `peel_iterated_oid()`
- Validates the packed-backend's handling of `peeled_oid` NULL assignments
- Confirms the reftable backend's proper population of the new `peeled_oid` field
- Reiterates his earlier concern about field reset safety during iteration, but sees it properly addressed here

The review maintains the thread's technical focus on iterator safety and peeled object handling while moving the discussion forward with specific implementation validation. Karthik's approval suggests this patch is on track for inclusion, pending any responses to his earlier naming convention suggestion from patch 3/13.