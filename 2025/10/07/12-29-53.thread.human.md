This thread implements a `--signed-tags=<mode>` option for `git fast-import` to bring signature handling parity with `git fast-export`, supporting all signature types (PGP, X.509, SSH) consistently. Christian Couder's series modernizes documentation, improves test infrastructure, and implements core functionality with Patrick Steinhardt providing key technical review.

The implementation introduces a new `signed_tag_mode` variable and `handle_tag_signature()` function in fast-import.c, using `parse_signed_buffer()` for universal format support. The series has progressed through several iterations addressing documentation terminology ("signature" vs "cryptographic signature"), test improvements, and minor implementation details like untranslated abort messages and signature positioning in tags.

In the latest exchange, Christian responds to Patrick's feedback about fragile test assertions in t9350-fast-export.sh by introducing a dynamic tag counter rather than reorganizing tests, arguing this better exposes test dependencies. While Patrick suggested isolating affected tests, Christian maintains the counter approach makes the technical debt visible while fixing the immediate issue. The discussion reflects broader differences in test philosophy between making dependencies explicit versus isolating components.

The series is nearing completion with only minor documentation and style issues remaining open. Patrick has approved the technical approach, and the resolution of this test fragility discussion clears one of the last substantive hurdles before the series can be merged. The work achieves unified signature management for repository filtering workflows while modernizing both documentation and test infrastructure.