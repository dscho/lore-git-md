Christian Couder's series to add `--signed-tags` support to `git fast-import` is nearing completion, having progressed through multiple rounds of review with Patrick Steinhardt. The work brings signature handling parity between fast-import and fast-export, supporting PGP, X.509, and SSH signatures consistently. The implementation introduces a new `handle_tag_signature()` function in fast-import.c and updates test infrastructure to properly verify all signature types.

The latest discussion focused on documentation, where Patrick and Christian agreed to keep the git-tag manpage concise by simply referencing the `gpg.format` configuration rather than duplicating format details. This follows the project's preference for centralized documentation where possible. Earlier technical concerns about test fragility and signature handling have been resolved, with Christian adopting a counter-based approach for tag tests while addressing formatting nits.

With only minor documentation formatting adjustments remaining, the series appears ready for final approval. Patrick has signed off on the technical approach, and the remaining work is largely polish. This represents a significant step forward in consistent signature handling across Git's fast-import/export commands.