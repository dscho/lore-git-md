This thread has implemented a new `--signed-tags=<mode>` option for `git fast-import` to match the signature handling capabilities of `git fast-export`, supporting all signature types (PGP, X.509, SSH) consistently. Christian Couder has led the implementation with thorough technical review from Patrick Steinhardt.

The series modernizes documentation, improves test infrastructure, and implements core functionality in fast-import.c with a new signed_tag_mode variable and handle_tag_signature() function. The implementation uses parse_signed_buffer() for universal format support while correctly handling message size calculations. The final patch adds the new option with the same modes as fast-export (verbatim, warn-verbatim, warn-strip, strip, abort), defaulting to verbatim.

The discussion has now reached consensus on all technical points. Patrick has accepted Christian's explanations about documentation ordering (following fast-export's precedent) and confirmed the safety of the signature stripping implementation, which relies on Git's convention of always appending signatures. With these final clarifications, the series appears ready for merging, having resolved all outstanding questions about test infrastructure, signature handling behavior, and documentation strategy.