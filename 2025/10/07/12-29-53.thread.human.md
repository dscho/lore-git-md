This thread centers on Christian Couder's work to implement consistent signature handling (`--signed-tags=<mode>`) across `git fast-import` and `git fast-export`, supporting PGP, X.509, and SSH signatures. The series has progressed through multiple iterations with input from key reviewers like Patrick Steinhardt and test infrastructure contributions from Collin Funk and Todd Zullinger.

The latest patch improves test reliability in `t9350-fast-export.sh` by replacing hardcoded tag counts with a dynamic `ANNOTATED_TAG_COUNT` variable. While Christian acknowledges this solution is somewhat inelegant, it explicitly tracks tag additions across test cases—preventing fragility when new tests are inserted. This addresses Patrick's earlier concerns about test interdependencies.

The series has already resolved several technical challenges: modernizing documentation to cover all signature types (not just GPG), centralizing signature handling logic with proper message size calculations, and fixing test infrastructure issues around GPG home directory creation. Patrick has signed off on the technical approach, including the signature stripping implementation using `strbuf_setlen()` and the documentation structure following `fast-export`'s precedent.

With all review feedback addressed—from test infrastructure improvements to cryptographic behavior—the series appears ready for final submission. The changes maintain backward compatibility while bringing `fast-import` to parity with `fast-export`'s signature support, completing a longstanding gap in Git's import/export capabilities.