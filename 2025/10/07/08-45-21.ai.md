This is a follow-up discussion in the reftable fsck validation series, specifically addressing test coverage gaps identified by Jeff King in patch 7/7. Karthik Nayak acknowledges two issues with the test case:

1. The test incorrectly uses root refs (`branch-$i`) instead of properly namespaced refs (`refs/heads/branch-$i`), which violates Git's naming conventions. Karthik notes this currently works only because reference-level checks haven't been implemented in reftables yet.

2. Jeff points out that `git refs verify` should ideally detect these malformed ref names, suggesting this as future work once reference-level checks are implemented. Karthik agrees this would be the expected behavior but notes it's not yet implemented in the current series.

Karthik indicates willingness to send a quick re-roll to fix the test case, though the current implementation technically passes. The exchange highlights that while the table-level validation is complete (as merged to 'next'), there remains future work to implement reference-level validation in the reftable backend. This discussion serves as a bridge between the completed table validation work and potential future ref name validation enhancements.