This is a follow-up in the submodule gitdir path encoding series, now focused on finalizing the transition strategy. Adrian Ratiu agrees with Junio C Hamano's proposal to make `submodule.<name>.gitdirpath` the authoritative mapping mechanism for encoded submodule paths, rather than just an override. 

The key insight is that since we need to maintain a mapping between submodule names and their encoded paths anyway, we might as well make this mapping the single source of truth. This simplifies the design by centralizing the path resolution logic. Adrian confirms he will implement this approach in v4 of the patch series.

The discussion has now converged on all major architectural points: using percent-encoding for path names, handling long paths via truncation and uniquification, and this new config-based mapping strategy for the transition period. What remains is implementing these agreed-upon designs, which Adrian indicates he will do in the next revision.

This email represents the final conceptual alignment between the patch author and maintainer on how to handle the transition period when the encoding feature is enabled in existing repositories. The implementation work can now proceed with clear direction.