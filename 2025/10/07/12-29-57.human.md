This is a bugfix and feature enhancement for `git fast-export`'s signature handling, part of a series improving tag signature support across Git's fast-import/export components.

The patch fixes `fast-export` to properly recognize and handle all Git-supported tag signature formats (GPG, X.509, and SSH) by using the existing `parse_signed_buffer()` function instead of hardcoding a check for PGP signatures. This brings `fast-export` in line with Git's broader signature handling capabilities and matches the functionality being added to `fast-import` elsewhere in the series.

The change is well-motivated and clearly explained, with comprehensive test coverage added for both X.509 and SSH signatures. Tests verify that signatures are correctly handled in both "verbatim" (preserve) and "strip" modes. The patch builds on earlier preparatory work in the series (noted in the thread context) and maintains the existing test infrastructure improvements like the `ANNOTATED_TAG_COUNT` variable.

This appears to be a solid, focused improvement that closes a real gap in signature handling while maintaining backward compatibility. The changes are minimal (just 7 lines of core logic changed) but impactful, with 48 lines of new tests providing thorough verification.