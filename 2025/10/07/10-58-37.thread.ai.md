Thread identity: Concluded architectural discussion about refs subsystem naming conventions following Patrick Steinhardt's merged 14-patch series refactoring tag peeling behavior. Primary participants were Patrick Steinhardt, Jeff King, Toon Claes, and Junio Hamano, with Junio's final email closing the debate and Patrick's follow-up providing technical rationale.

Core problem: The merged series introduced `struct reference` while keeping `struct ref`, creating naming tension. Final resolution accepts this duality after Junio's technical probing confirmed the impracticality of unification due to `peer_ref` linkage complications and API stability constraints. Patrick's follow-up elaborates the distinct use cases: `struct ref` for transport operations (push/fetch) versus `struct reference` for core ref operations.

Current status: Series remains merged with all performance improvements (13% gain). Consensus solidified to:
1. Maintain both `struct ref` (transport-specific) and `struct reference` (core abstraction) as distinct types
2. `struct ref` retains transport fields like `new_oid` and force-update flags
3. `struct reference` enables new capabilities like standalone peeled ref operations
4. Future API growth will use `struct reference` for core operations
5. Documentation reflects intentional technical debt of dual types

Key technical details:
- `struct reference` confirmed as stable name for new APIs with added capabilities:
  - `reference_get_peeled_oid()` without iteration
  - Potential for higher-level queries like `reference_is_branch()`
- `struct ref` remains unchanged with transport-specific fields
- Clear semantic boundary established between types
- Performance optimizations preserved without naming changes
- Junio validated three scenarios showing unification impractical
- Patrick demonstrated concrete benefits of type separation

Open questions resolved:
1. Community acceptance: Jeff King and Junio Hamano endorsed the compromise
2. API direction: Clear path established with `struct reference` for new development
3. Technical constraints: `peer_ref` complications acknowledged as permanent
4. Documentation: Can proceed with architectural rationale now fully articulated
5. Maintainer position: Junio's conclusion and Patrick's follow-up complete the picture

Participant positions:
- Patrick Steinhardt's phased approach validated by maintainer, with follow-up technical justification
- Jeff King's stability-first position confirmed
- Junio Hamano closed discussion with technical ratification
- Consensus reached that technical debt is preferable to breaking changes

Related work: Builds on master branch. Connects to broader refs modernization (reftable, ODB abstraction). Documentation proceeds under Jean-Noël Avila's synopsis-style conversion. Performance work preserved while closing naming discussion. `peer_ref` complexities now formally acknowledged with complete technical rationale. Patrick's follow-up serves as post-hoc design documentation, aligning with Junio's practical resolution approach.