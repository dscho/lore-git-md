Refactoring patch in a series optimizing ref iteration and tag peeling. This is patch 2/13 from Patrick Steinhardt's series, which introduces a new `struct reference` to encapsulate ref metadata (name, target, oid, flags) in the iterator interface.

The change replaces scattered fields in the base iterator with a single `struct reference` member, avoiding the need to construct new structs when passing refs to callbacks. This affects all ref iterator implementations (files, packed, reftable backends) and their callers, with mechanical updates to access the fields through the new struct.

The patch is straightforward and mechanical, touching 65 files across the refs subsystem. It builds on patch 1/13's introduction of the wrapper struct and appears to be a clean preparatory change for later optimizations in the series. The diff shows careful attention to detail in updating all affected code paths consistently.