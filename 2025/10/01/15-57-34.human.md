This is a refactoring patch in Patrick Steinhardt's `git-history` series, specifically part 8/12 of v4. The patch focuses on cleaning up the interactive add/patch infrastructure that will be used by the new history editing commands.

The main change splits out a new `struct interactive_options` to hold common configuration used by both `git add -i` and `git add -p`, replacing the previous `struct add_p_opt`. This helps clarify which options apply to which parts of the interactive code. The patch moves color handling and other configuration into a new `interactive_config` struct.

The changes are mechanical but wide-ranging, touching many files that use the interactive add/patch functionality. The patch maintains all existing behavior while making the code organization clearer. This appears to be preparatory work for the larger history editing functionality being developed in the series.

The refactoring looks thorough and well-motivated, with clear commit messages explaining the changes. As part of a larger series that has already seen several iterations, this seems like a solid incremental improvement to the codebase.