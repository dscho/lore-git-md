Thread identity: Bugfix patch addressing symbolic reference validation security issue in the files backend, now in discussion between Han Young, Junio C Hamano, Karthik Nayak, and Patrick Steinhardt.

Core problem: Malformed symbolic references (e.g. "refs/../HEAD") could bypass validation checks when manually editing `.git/HEAD`, allowing reference and lock files to be written outside the intended refs/ directory. Requires circumventing Git CLI protections since `git update-ref` already validates refnames properly via `check_refname_format()`.

Current status: Technical direction refined - validation should be unified between `git fsck` and `git refs verify`, with `refs_resolve_ref_unsafe()`'s current fsck validation moving into core refs verification. Patrick Steinhardt confirms Karthik Nayak's analysis that fsck catches invalid HEAD while `git refs verify` does not, proposing fsck should delegate to refs verification rather than implement its own checks.

Key technical details: Fix targets `files-backend.c` in `split_symref_update()`. Solution must incorporate both `refname_is_safe()` and fsck's reference checking via `refs_resolve_ref_unsafe()`. New architectural requirement emerges to restructure validation hierarchy - moving fsck's symbolic reference checks into `git refs verify` as the canonical validation point. The vulnerability remains limited to manual repository edits since CLI commands already prevent malformed refs.

Open questions: Security justification still debated - whether hardening against manual edits is within scope. Implementation work now requires: (1) restructuring validation hierarchy to make `git refs verify` the canonical source, (2) aligning files backend with this unified validation, and (3) adding test coverage demonstrating both fixes. The architectural shift to centralize validation in refs verify represents a more comprehensive solution than the initial files-backend.c fix.

Participant positions: Han Young submitted initial patch. Junio C Hamano's fsck alignment request now addressed technically by Karthik Nayak's analysis. Patrick Steinhardt reinforces the validation hierarchy restructuring approach. Karthik continues questioning security scope while not opposing the technical improvements. Thread now shifts to implementation with both the immediate fix and broader architectural changes needed.

Related work: Connects to broader reference validation consistency across Git tools. The fsck/refs-verify alignment may set precedent for how different components handle reference parsing. Discussion continues to probe Git's boundaries for repository corruption protection versus only securing its own operations, with the validation hierarchy restructuring representing a significant architectural decision.