This is a refactoring patch in Patrick Steinhardt's `git-history` series, specifically part 4 of 12 in v4. The patch focuses on improving the robustness of shared replay infrastructure.

The change ensures that commits are properly parsed before being accessed in the `replay_pick_regular_commit()` function, which was recently extracted to be used by multiple callers. While existing call paths already parsed commits implicitly, this explicit check makes the function more self-contained and safer for new users of the API.

The modification is small (just 3 lines added) but meaningful - it follows good API design practices by making the function handle its own preconditions rather than relying on callers to do so. This is particularly important as the replay functionality is being shared between different commands in the `git-history` effort.

The patch appears well-considered and aligns with the series' broader goal of building reliable shared infrastructure. It's a straightforward improvement that doesn't introduce new functionality but makes the existing code more robust.