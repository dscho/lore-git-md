Documentation clarification with test expansion for URL prefix matching in Git credentials.

This is a reply in an ongoing thread about documenting URL prefix matching behavior in Git credential configuration. The original patch clarified that path components must match as prefixes (so `https://example.com/bar` matches `https://example.com/bar/baz.git` but not `https://example.com/barry/repo.git`), correcting inaccuracies from commit 9a121b0d226 (2020).

Brian m. carlson reviews the changes positively, noting the improved description and expanded test coverage. The test changes in t/t0300-credentials.sh now include more comprehensive cases for both valid prefix matches (various forms of `example.com/org/repo.git`) and negative cases (partial segments like `example.com/o`). The tests use a credential helper that echoes a password to confirm matches.

The changes remain documentation and test-only, as the implementation already behaved as now documented. The review confirms the patch's quality, with clear documentation improvements and thorough test coverage that verifies the prefix matching behavior through both positive and negative test cases.