This is a refactoring patch that splits out header declarations from `add-interactive.h` into a new `add-patch.h` file. It's part of Patrick Steinhardt's v4 series introducing the `git-history` command, though this particular patch focuses on cleaning up the interactive add/patch infrastructure.

The change moves all `add-patch.c` related declarations (structs, enums, and function prototypes) into the new header, while keeping the interactive add-specific declarations in `add-interactive.h`. This creates clearer boundaries between the two subsystems and makes it easier to locate relevant code. The patch is straightforward - it creates the new header, moves the declarations, and updates the includes accordingly.

The implementation looks clean and well-contained, with no behavior changes. This appears to be preparatory work for the larger `git-history` series, helping to organize the codebase before adding new functionality. The patch follows Git's typical refactoring patterns and maintains all existing functionality while improving code organization.