This is a refactoring patch that splits header declarations from `add-interactive.h` into a new `add-patch.h` file. It's part of Patrick Steinhardt's v4 series introducing the `git-history` command, though this particular patch focuses on cleaning up the add-patch implementation details rather than directly contributing to the new command's functionality.

The patch moves all `add-patch.c`-related declarations (struct `add_p_opt`, enum `add_p_mode`, and the `run_add_p()` function) from `add-interactive.h` to the new `add-patch.h`. The motivation is to better separate concerns between the two subsystems and make code boundaries clearer. The patch maintains all existing functionality while improving code organization.

Key technical details:
- Creates new `add-patch.h` header with 26 lines of declarations
- Removes 20 lines from `add-interactive.h` while keeping its core interactive state management
- Updates `add-patch.c` to include the new header
- Preserves all existing functionality and initialization macros
- Maintains compatibility with the pathspec and repository struct dependencies

This appears to be a straightforward code organization improvement that doesn't affect runtime behavior. The change helps prepare the codebase for the larger `git-history` work by keeping header responsibilities cleanly separated. The patch follows Git's typical refactoring patterns and includes no functional changes.