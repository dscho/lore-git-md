This is a follow-up discussion on a security-focused bugfix patch for the files backend's symbolic reference validation. Junio C Hamano responds to Han Young's initial patch that prevents creation of symbolic references with paths that could escape the refs directory.

The maintainer raises two key points about the proposed fix: First, he questions whether the validation logic should align with what `git fsck` would consider invalid, suggesting the check should be consistent across both validation paths. Second, he requests test coverage to verify the new validation works as intended.

The discussion builds on the original patch's security focus, which added validation in `split_symref_update()` using the existing `refname_is_safe()` function. The maintainer's response indicates the patch is moving toward acceptance but needs these two refinements - consistency with fsck behavior and test coverage - before being ready to merge.

Technical details remain focused on `refs/files-backend.c` and the validation of symbolic reference paths. The thread shows no disagreement about the security issue's severity or the general approach, but highlights Git's principle that similar validations should share logic where possible, and that new protections need accompanying tests.