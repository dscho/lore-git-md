This is a follow-up in the ongoing discussion about Git's push optimization when the server already has most objects. Sainan reports a test case where a push succeeded with partial object reuse (34 of 46 objects reused), though the results show some inefficiency with 40 objects still being transferred when fewer might have been possible.

The email provides concrete data showing Git's current behavior in practice - while some objects are being reused (34 delta-reused), there's still room for improvement as the pack-reused count is zero. This continues the thread's investigation into when and why Git fails to maximize object reuse during pushes, particularly in cases where the server should already have most needed objects.

The discussion remains focused on understanding the practical implications of Git's push optimization heuristics, with participants examining real-world push/fetch outcomes to identify optimization opportunities. This particular data point suggests the current behavior is working but not yet optimal, keeping the door open for further investigation into the edge cases where reuse could be better.