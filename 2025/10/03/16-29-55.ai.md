Documentation formatting fix for refStorage config rendering. This is a follow-up to the ongoing thread about Asciidoctor/asciidoc.py compatibility issues in Git's documentation.

The patch addresses a specific interaction between nested lists and included content in `Documentation/config/extensions.adoc`. Jeff King proposes wrapping the `ref-storage-format.adoc` include in explicit block markers (`--`) to ensure consistent rendering across both documentation processors. This solution maintains the correct indentation level (level 2) for the "Note that..." warning paragraph while fixing visual regressions introduced by earlier changes to handle nested bullet lists.

Junio C Hamano concurs with the approach, indicating the thread is reaching resolution. The change is narrowly scoped to preserve documentation semantics while working around processor differences - asciidoc.py implicitly handles includes differently from Asciidoctor. The solution is somewhat fragile as it requires awareness of the included file's content structure, but appears to be the only way to achieve consistent rendering.

This marks the final refinement in a series addressing multiple documentation formatting issues, with all key reviewers now satisfied. The changes affect how configuration options like `refStorage` and related format specifications are displayed in the rendered HTML documentation.