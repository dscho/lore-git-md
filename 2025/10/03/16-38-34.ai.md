This is a follow-up in the `git whatchanged` deprecation thread, confirming the migration path for shell alias users. Stephen Harding acknowledges Junio Hamano's earlier explanation that the `--i-still-use-this` flag serves informational rather than gatekeeping purposes, and that the oh-my-zsh `gwch` alias can be directly replaced with equivalent `git log` flags (`--no-merges -p --abbrev-commit`). The email reinforces that `--pretty=medium` is redundant as it matches `git log`'s default formatting.

The exchange demonstrates successful identification and resolution of one real-world usage pattern (shell aliases) during the deprecation process. No new technical concerns are raised - the thread now has a verified migration path for this common case. The remaining open question is whether other unanticipated use cases will emerge before final removal. The tone suggests acceptance of the deprecation plan now that the specific alias case has been addressed with clear migration instructions.