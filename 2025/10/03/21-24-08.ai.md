Documentation patch with test coverage, already merged to 'next' branch.

This is a follow-up to the documentation and test changes clarifying URL prefix matching behavior in Git credential configuration. The original patch was already merged to the 'next' integration branch, so Junio Hamano questions why a replacement patch (v2) is being submitted rather than an incremental update. The v2 patch contains minor wording improvements to the documentation about prefix matching requirements.

The thread context shows this is purely a documentation and test clarification - the implementation already behaved correctly. The documentation now accurately describes that credential URL patterns must include protocol and host to match as prefixes (e.g. `https://example.com/bar` matches `https://example.com/bar/baz.git` but not `https://example.com/barry/repo.git`). The extensive test coverage in t/t0300-credentials.sh verifies this through both positive and negative cases.

Since the changes are already in 'next', the maintainer suggests the author should instead submit any further improvements as incremental updates rather than replacement patches. No technical issues remain open - this is purely about patch submission workflow.