Bugfix follow-up for interactive add hunk selection.

Phillip Wood responds to René Scharfe's fix for the `git add --interactive` hunk selection regression. The original patch modified `patch_update_file()` in `add-patch.c` to properly skip already-decided hunks when using the "J" (join) command, but Phillip raises two technical concerns about the implementation.

First, he questions whether the code properly handles the edge case where there might be no undecided hunks left, noting the current logic could lead to an out-of-bounds array access if `hunk_index` isn't reset properly. This suggests the fix might need additional bounds checking or invariant enforcement.

Second, Phillip critiques the test coverage, suggesting the first test case might be redundant since the second test already verifies the core functionality of finding the first undecided hunk. This implies the test suite could potentially be simplified while still maintaining adequate coverage.

The exchange shows maintainer scrutiny of both the implementation safety and test design, though Phillip's tone remains constructive. No new code is proposed in this email - it serves as a review pointing out potential improvements to the existing fix. The thread appears to be moving toward resolution with this maintainer feedback.