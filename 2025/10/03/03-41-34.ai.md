This is a documentation formatting fix addressing an orthogonal issue in the refStorage documentation indentation that emerged during review of Jean-Noël Avila's patch series. Jeff King corrects his earlier misdiagnosis about asciidoc.py behavior and proposes a more robust solution that works across both asciidoc and asciidoctor processors.

The core problem involves proper indentation of a "Note that..." warning paragraph in the `refStorage` configuration section. King's analysis shows three possible indentation levels for this paragraph, with the correct position being as a continuation of the main `refStorage` definition (level 2). His solution wraps the included `ref-storage-format.adoc` content in explicit AsciiDoc block markers (`--`) to ensure consistent rendering, though notes this is somewhat brittle as it requires awareness of the included file's structure.

The patch maintains semantic correctness while fixing the visual indentation regression Avila's changes introduced for asciidoctor. This represents a final refinement to the documentation formatting issues discussed in the thread, with King's solution preserving the intended hierarchy where the warning applies to the overall `refStorage` setting rather than any specific bullet point. The change is narrowly scoped to `Documentation/config/extensions.adoc` and its interaction with the included `ref-storage-format.adoc` content.