Thread examining performance impact of untracked files cache behavior in `git status`, focusing on interactions between `--untracked-files=all` flag and `status.showUntrackedFiles=all` config. Original report (Windows) and Matthew Hughes (Linux) showed 3x slowdown when cache is bypassed due to flag/config mismatch (e6a653554b). Devste Devste confirms core issue but reveals additional complexity: 1) Cache becomes "sticky" once established - subsequent commands with --untracked-files=all use cache regardless of current config if initially created with showUntrackedFiles=all, and 2) Common tooling flags (`--porcelain`, `--no-optional-locks`, `--ignore-submodules=dirty`, `--no-ahead-behind`) may interfere with cache updates, though exact mechanism unclear. New timing data shows 3.4s vs 0.8s differences, demonstrating performance impact extends beyond original config/flag symmetry issue. Current understanding: cache activation depends on both initial conditions (config matching runtime flags) and undocumented interactions with other status options, creating inconsistent behavior particularly problematic for IDEs/GUIs that combine these flags. No proposed solution yet, but reproduction steps now capture both the original config-dependent behavior and newly discovered flag interaction effects. Windows and Linux timings confirm cross-platform relevance. Open questions: which specific flag combinations break cache updates, and whether the "sticky" cache behavior is intentional or a bug.