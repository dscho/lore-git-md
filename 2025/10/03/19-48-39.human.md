Feature refinement for `git replay`'s atomic reference updates.

Elijah Newren provides final review comments on Siddharth Asthana's upcoming v3 patch series for making atomic ref updates the default in `git replay`. The discussion focuses on three key refinements:

First, they settle on naming for the new config option, preferring the boolean `replay.updateRefs` over a string-based alternative. Elijah suggests the command-line flag could be `--[no-]update-refs` to match, though notes the exact naming remains open for discussion. This maintains consistency with Git's existing config patterns while providing clear override capability.

Second, Elijah confirms the removal of the `--allow-partial` option is the right choice, agreeing with Siddharth's simplified two-mode design (atomic updates by default with `--output-commands` for the old behavior). This resolves earlier concerns about unnecessary complexity.

Third, they discuss patch organization, with Elijah recommending separating the `die_for_incompatible_opt2` indentation fix into its own commit since it's orthogonal to the main changes. He also agrees with dropping the special empty-range handling from this series, suggesting it could be addressed later if real use cases emerge.

The exchange shows the technical consensus holding strong as the series moves toward final submission, with only minor organizational and naming decisions remaining. The changes reflect experienced guidance about Git's conventions while preserving the core atomic update functionality that received broad approval earlier in the thread.