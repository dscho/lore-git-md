This is a discussion about future extensibility of `git replay`'s output format, specifically considering Kristoffer Haugsbakk's proposal for a `git-for-each-ref`-style `--format` option. The exchange occurs in the context of the already-approved atomic ref updates series that is preparing for v3 submission.

Kristoffer expands on their earlier suggestion for a more flexible output format, motivated by two concrete use cases: better integration with the `post-rewrite` hook (currently requiring manual OID parsing) and client-side reflog customization needs. They contrast these with server-side workflows that may prioritize simplicity. The proposal suggests `git replay` could offer both a default format matching `git update-ref --stdin` requirements and customizable formats through atoms like `%(old)` and `%(new)`.

Siddharth Asthana's response acknowledges the merit of this approach for future client-side features (particularly interactive rebase functionality) but maintains focus on the current atomic update implementation. The discussion reveals a tension between immediate needs (atomic ref updates with simple output) and long-term flexibility, with Kristoffer providing specific examples where format customization would eliminate manual parsing work for hook integration.

Key technical points mentioned include:
- Current output format is tightly coupled with `git update-ref --stdin` requirements
- Potential format atoms could mirror `git-for-each-ref`'s design (`%(old)`, `%(new)`)
- Client-side use cases (hook integration, reflog customization) differ from server-side priorities
- The discussion doesn't block the current series but may influence future `git replay` evolution

The exchange shows thoughtful consideration of how experimental commands like `git replay` might balance immediate utility with extensibility, while maintaining focus on the already-approved core functionality. Kristoffer's concrete examples help ground the abstract discussion of output formats in real-world usage patterns.