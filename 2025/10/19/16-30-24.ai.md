Bugfix patch (v4) addressing remaining dry-run mode output inconsistencies, following up on the v3 series that was already deemed complete. This appears to be a minor refinement to the comprehensive solution for ensuring silent operation in dry-run mode across all diff output paths.

The patch makes three specific changes:
1. Early return in `emit_diff_symbol_from_struct()` when in dry-run mode, catching previously missed cases for file deletions and mode changes
2. Extends quiet mode handling in `run_external_diff()` to include dry-run conditions
3. Suppresses unmerged path notifications in `run_diff_cmd()` during dry-run

Test coverage is expanded in t4013-diff-various.sh with a comprehensive test case that verifies silent operation across --raw, --name-only, and --name-status formats, while still maintaining the expected file listing behavior. The test creates a scenario with deleted files, mode changes, and unmerged paths to exercise all the fixed code paths.

This appears to be a final polish to the dry-run behavior fixes, addressing any remaining edge cases that might produce output. The changes are minimal and focused, building on the already-agreed technical approach from v3. The test case is thorough and should prevent future regressions. Given the previous consensus that v3 was complete, this v4 likely represents minor improvements based on final testing rather than any major design changes.