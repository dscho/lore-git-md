This is a review response discussing a patch series about `git add` and `ignore=all` submodule behavior. Phillip Wood raises several technical and design concerns about the proposed changes.

The email identifies a key historical context - commit 5556808690e deliberately made `git add` and `git reset` ignore submodule ignore settings, unlike `git diff`. Phillip questions why the series changes `git add` but not `git reset`, and suggests the change could be implemented more simply by removing the `override_submodule_config` flag rather than adding new `--force` logic.

Phillip expresses skepticism about using `--force` to override `ignore=all`, arguing this conflates two different concepts: ignoring changes to tracked paths (submodule behavior) versus ignoring untracked paths (gitignore behavior). He suggests a new dedicated option might be cleaner if breaking existing behavior is acceptable.

The review notes Phillip lacks submodule experience to judge the behavioral change's merit, but provides important technical critique about the implementation approach and historical context. This raises potential issues the patch author will need to address regarding:
1) Justification for changing `git add` but not `git reset`
2) Whether `--force` is the right mechanism
3) Simpler alternative implementations
4) Backward compatibility considerations

The response doesn't reject the series outright but identifies several areas needing clarification or reconsideration in the design. This appears to be a substantive technical review from an experienced contributor, though not a submodule expert.