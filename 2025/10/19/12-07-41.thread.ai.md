Thread identity: Two-part refactoring series by Olamide Caleb Bello converting SSH key handling in `gpg-interface.c` from `strbuf_split*()` to `string_list_split*()`, now complete at v3.

Core problem: Modernize functions handling SSH keys by replacing `strbuf_split_max()` with `string_list_split_in_place()` where appropriate, as the latter is better suited for read-only string splitting. Part 1 addressed `get_ssh_key_fingerprint()` while part 2 handles `get_default_ssh_signing_key()`.

Current status: Complete v3 series incorporating feedback from Junio Hamano and Christian Couder. Key changes across both parts:
- Using `STRING_LIST_INIT_NODUP` consistently since strings are borrowed
- Adopting `_in_place` variant for both functions
- Adjusting split counting logic (1 split yields 2 tokens, 2 splits yield 3 tokens)
- Maintaining safety checks while adapting to new API semantics

Key technical details:
- Part 1 (`get_ssh_key_fingerprint`):
  - Now expects >=2 splits (3 tokens) to locate fingerprint at index 1
- Part 2 (`get_default_ssh_signing_key`):
  - Uses >0 splits check instead of array indexing
  - Returns first token via `items[0].string` instead of `keys[0]->buf`
- Both functions:
  - Preserve existing behavior while using more appropriate API
  - Handle read-only strings without duplication
  - Include clear commit messages explaining API semantic differences

Participant positions:
- Junio Hamano originally identified `strbuf_split*()` as overkill for this use case
- Christian Couder provided review feedback that shaped v3 changes
- Olamide Caleb Bello implemented both parts with clear rationale and proper API usage

Related work: Part of broader effort to replace `strbuf_split*()` with more suitable string splitting APIs throughout the codebase. Follows similar conversions elsewhere while focusing specifically on SSH key handling in gpg-interface.c.