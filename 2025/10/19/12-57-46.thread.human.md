Here's a concise human-readable summary of the thread:

This thread introduces improvements to the bash completion script for `git log` and `git shortlog`. The patch adds proper tab-completion support for the `--exclude=` option that was previously missing from both commands, despite being valid syntax. 

More significantly, it fixes a subtle difference in how `--committer` works between the two commands. While `git log` accepts a pattern (like `--committer=john`), `git shortlog` explicitly rejects any value for this option. The patch correctly separates these cases in the script, preventing invalid completions that would cause errors when used with `git shortlog`.

The changes are cleanly implemented in `contrib/completion/git-completion.bash` and maintain the existing pattern of sharing common options while handling command-specific cases appropriately. The commit message clearly explains both the problem and solution.