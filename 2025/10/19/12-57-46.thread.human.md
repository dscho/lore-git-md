KIYOTA Fumiya's patch to improve bash completion for `git log` and `git shortlog` is progressing well. The changes fix two specific issues: adding the missing `--exclude=` option to both commands' tab completion, and properly separating the `--committer` handling between the commands (keeping pattern-based completion for `log` while making it valueless for `shortlog` to match actual behavior).

Junio C Hamano reviewed the patch, suggesting a minor wording tweak to the commit message ("I'll" -> "Handle"), which the author has agreed to implement. The discussion revealed an underlying inconsistency in how `--committer` works between the two commands - as a filter in `log` versus a grouping flag in `shortlog` - but both parties recognize this as a separate interface design matter that shouldn't block the current completion improvements.

The patch remains focused on the completion script (`git-completion.bash`), making targeted fixes without changing command semantics. With the commit message adjustment, it appears ready for merging, though it surfaces a potential area for future discussion about unifying command interfaces. The author has been responsive to feedback while appropriately scoping the changes to completion behavior only.