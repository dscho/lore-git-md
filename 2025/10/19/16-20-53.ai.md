This is a bugfix patch (v3) that completes the dry-run mode implementation for Git's diff machinery, following up on the incomplete fix in b55e6d36eb. The patch addresses remaining cases where dry-run mode was still producing output for file deletions, mode changes, and unmerged paths.

The key technical changes are:
1. Adds early return in `emit_diff_symbol_from_struct()` when in dry-run mode
2. Extends quiet mode handling in `run_external_diff()` to include dry-run cases
3. Suppresses unmerged path notifications in `run_diff_cmd()` during dry-run
4. Adds comprehensive test coverage in t4013-diff-various.sh that verifies no content changes are shown while still listing affected files

This patch represents the "comprehensive solution" phase of the two-part fix agreed upon in the thread, where Junio Hamano will separately handle the immediate regression fix via /dev/null redirection. The implementation follows the technical approach previously discussed, with all output paths now properly respecting the dry-run flag.

The test cases specifically verify behavior with --raw, --name-only, and --name-status output formats, ensuring the fix works across different diff modes. The patch appears well-tested and aligns with the thread's resolution to properly implement dry-run behavior rather than relying on the /dev/null workaround long-term.