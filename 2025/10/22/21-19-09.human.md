Implementation refinement for atomic ref updates in `git replay`.

This is a maintainer review of the second patch in the 3-part series finalizing atomic reference updates for `git replay`. Junio Hamano provides two specific style suggestions for the implementation:

1. **Enum formatting**: Recommends adding a trailing comma after the last enum value (`REF_ACTION_PRINT`) to minimize patch noise when adding future values, unless this is intentionally omitted as a sentinel value.

2. **Error message style**: Points out that the error message for failed ref updates should follow Git's coding guidelines more precisely - specifically noting that the ref name should be enclosed in single quotes and the message should not be capitalized.

The review is narrowly focused on these two style points, indicating the core technical implementation (which was previously reviewed) is otherwise sound. This suggests the patch is in final polishing stages before merging, with only minor documentation and style tweaks remaining. The thread context shows this series has already addressed all major technical concerns and achieved naming consensus, so these appear to be among the last remaining items before the feature can be merged.