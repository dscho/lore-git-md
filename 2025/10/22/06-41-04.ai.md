Refactoring patch in a performance optimization series, focusing on reference status flags modernization.

This patch (4/14 in v3 series) converts the reference status flags from preprocessor defines to an enum and changes the storage type from `int` to `unsigned`. The flags track reference properties like being symbolic (`REF_ISSYMREF`), packed (`REF_ISPACKED`), broken (`REF_ISBROKEN`), or having invalid names (`REF_BAD_NAME`). 

The change aligns with Git's modern coding practices by using enums for flags and unsigned types for bitfields. While mechanical, this refactoring improves type safety and maintainability. The patch is part of a larger effort to clean up the refs subsystem interface while maintaining the separation between transport-related `struct ref` and core `struct reference`. 

The diff shows straightforward conversion of the flag definitions in `refs.h`, with no behavior changes. This prepares the codebase for subsequent patches that build on these interfaces. The change affects 41 lines (21 insertions, 20 deletions) in a single header file, with minimal risk of introducing new issues.