This is a small refactoring patch in the ongoing trailer API series, converting an `int` flag to `bool` for clearer type semantics. 

The change is straightforward: the `had_trailer_before` variable in `trailer.c` is switched from `int` to `bool` to better reflect its logical purpose as a true/false flag. As noted in the commit message, there's no functional change - this is purely a code hygiene improvement.

This patch is part of the larger 29-patch series refactoring Git's trailer handling infrastructure, now in its 5th iteration. The series has been through extensive review, with recent patches focusing on API cleanup and optimizations. While this particular change is minor, it contributes to the overall goal of modernizing and streamlining the trailer processing code.

The patch appears well-scoped and uncontroversial, following the pattern of similar boolean conversions seen elsewhere in the codebase. It maintains the series' focus on internal improvements while keeping the public API stable.