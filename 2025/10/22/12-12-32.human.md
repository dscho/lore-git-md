This is a style discussion about pointer alignment in the ongoing `git-history` command series. Karthik Nayak points out that the current usage string declaration (`const char * const`) doesn't match Git's documented pointer alignment style (right-aligned asterisks). Junio Hamano asks for clarification on what clang-format would suggest for this edge case, noting that the style guide doesn't explicitly cover const-qualified pointer declarations.

Karthik investigates and reports that clang-format's `PointerAlignment: Right` setting would produce `const char *const usage` (no space between `*` and `const`), which doesn't match Git's prevalent style of having spaces on both sides of the asterisk. The discussion reveals a gap between the coding guidelines and tooling capabilities for this specific case.

This is a minor but precise style discussion that emerged during final polish of the `git-history` series. It doesn't affect functionality but shows the project's attention to consistency even in edge cases. The exchange suggests the team may need to either adjust expectations or document this exception to the pointer alignment rule.