Test modernization in the ongoing `--trailer` rebase series. This patch refactors test assertions in t3440-rebase-trailer.sh to use `test_commit_message` for verifying trailer behavior, replacing fragile pipeline-based checks. The changes make the tests more robust by:

1. Converting the "duplicate trailer replacement" test to explicitly check for the expected commit message format with only the last trailer preserved
2. Updating the "multiple Signed-off-by trailers" test to verify both trailers appear in the expected order rather than counting occurrences
3. Maintaining the same test coverage while using Git's built-in test helpers for more reliable failure detection

The patch is part of the larger series introducing `--trailer` support to `git rebase`, specifically focusing on test suite improvements. It follows earlier test modernization work in the series by making the assertions more precise and less dependent on implementation details like grep behavior. The changes align with the series' overall goal of robust trailer handling while keeping the test suite maintainable.