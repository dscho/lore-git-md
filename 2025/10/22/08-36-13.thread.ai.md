Thread identity: bugfix for `git bisect` subcommand handling, addressing regressions from the 2022 builtin conversion, originally reported by Ruoyu Zhong with patch v1, now approved by Junio C Hamano for inclusion.

Core problem: `git bisect` incorrectly processes invalid subcommands and fails to show help text as documented, due to all unrecognized inputs being routed through `bisect_state()` and potentially triggering an interactive autostart.

Current status: patch approved for merging with final confirmation of validation logic. Fixes both issues - restoring `git bisect help` behavior and properly rejecting invalid commands while maintaining backward compatibility with configured term aliases. Maintainer and author agree solution is correct and ready for merging as-is.

Key technical details:
- Fix modifies `cmd_bisect__helper()` in `builtin/bisect.c`
- Adds explicit `help` subcommand handling before state machine
- Validates unrecognized commands against state terms (good/bad/new/old) using `one_of()` combined with `check_and_set_terms()`
- Confirmed to correctly handle both invalid commands and custom term aliases (e.g. "newe/olde")
- Shows proper error for invalid commands instead of processing through state machine
- Maintains backward compatibility with alternate terms
- Review suggestions about `OPT_SUBCOMMAND` and usage text updates remain deferred

Open questions: none blocking merge - all parties confirm current approach correctly handles edge cases while maintaining compatibility. Broader subcommand improvements remain possible future work.

Participant positions:
- Ruoyu Zhong: original author, confirms validation logic correctness
- Junio C Hamano: maintains approval, verifies combined term handling works as intended
- Ben Knoble: earlier suggestions acknowledged as non-blocking future possibilities

Related work: regression stems from 2022 builtin conversion. Current solution deliberately avoids broader subcommand infrastructure changes to preserve backward compatibility, leaving potential `OPT_SUBCOMMAND` integration for separate follow-up if desired.