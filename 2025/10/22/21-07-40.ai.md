This is a technical discussion about type choices in the xdiff refactoring for Rust FFI compatibility, specifically addressing the `dstart`/`dend` field types in `xdfile_t`. 

Ezekiel Newren responds to Junio Hamano's summary of Phillip Wood's concern about using `ssize_t` versus `ptrdiff_t` for pointer-relative offsets. Newren reveals he previously proposed a `compat/rust_types.h` file that mapped C types to Rust equivalents, using `ptrdiff_t` for Rust's `isize` rather than `ssize_t`. He suggests reviving this file (or creating equivalent documentation) to provide clear type mapping guidance, especially for FFI-related changes. The email includes a link to the original proposal which was previously rejected.

The discussion highlights an ongoing tension between POSIX-aligned types (`ssize_t`) and C/Rust FFI considerations (`ptrdiff_t`/`isize`). Newren's proposal would institutionalize type mappings rather than leaving them as implicit review knowledge. This connects to the thread's broader theme of establishing clear conventions for Rust interoperability while maintaining type safety. The email doesn't resolve the immediate `dstart`/`dend` question but offers a structural solution that could prevent similar debates in future FFI work.