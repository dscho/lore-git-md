This is a small cleanup patch in the ongoing `--trailer` option series for `git rebase`. It removes redundant code that was stripping the `--trailer=` prefix from arguments, since the `OPT_STRVEC` option parser already handles this. The change simplifies the argument validation logic in `validate_trailer_args_after_config()` by working directly with the pre-processed values.

The patch is part of a larger effort (now at v5) to integrate trailer functionality directly into `git rebase`, moving away from external `interpret-trailers` calls. While this particular change is minor (just 5 lines removed), it follows the series' pattern of streamlining the implementation. The validation behavior remains exactly the same - it still checks for empty trailers and proper formatting - just with less intermediate string manipulation.

This appears to be a straightforward improvement that was likely identified during code review of the larger series. The change is small, focused, and maintains all existing functionality while making the code slightly more direct.