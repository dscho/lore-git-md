This is the first patch in a 29-part series refactoring Git's trailer handling infrastructure to enable in-process trailer manipulation, eliminating the need for fork/exec to `git interpret-trailers`. The change is a foundational refactor that moves trailer processing logic from `builtin/interpret-trailers.c` into a new `trailer_process()` function in `trailer.c`, with `amend_strbuf_with_trailers()` providing the core in-memory processing capability.

Key technical changes include:
- Removes tempfile handling and fork/exec overhead from trailer operations
- Introduces `trailer_process()` as the central API for all trailer manipulation
- Maintains identical behavior through careful preservation of edge cases (empty messages, missing newlines, etc.)
- Updates `amend_file_with_trailers()` to use the new in-process path
- Keeps the command-line interface unchanged while rewriting its internals

The patch touches three main files:
- `builtin/interpret-trailers.c` loses 116 lines of tempfile/fork handling code
- `trailer.c` gains 125 lines with the new processing logic
- `trailer.h` gets new declarations for the in-process APIs

This is a well-structured refactor that shows careful attention to maintaining existing behavior while cleaning up the implementation. The commit message clearly explains the motivation (removing fork overhead) and connects it to the larger series goal (enabling `rebase --trailer`). The diff shows substantial but focused changes, with good separation between the refactor and future functionality.