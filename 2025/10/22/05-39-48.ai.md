Test patch exercising trailer configuration mapping in rebase.

This is the 28th patch in a 29-part series introducing `--trailer` support to `git rebase`. The patch modifies an existing test case in t3440-rebase-trailer.sh to verify that trailer configuration works properly with both the `-c` config option and `--trailer=` CLI syntax. The test now uses `trailer.review.key=Reviewed-by` config to map the "review" key to the standard "Reviewed-by" trailer, while passing the trailer value via `--trailer=review="Dev <dev@example.com>"`.

The change demonstrates that the rebase trailer functionality properly handles config-based trailer key mapping while maintaining the expected behavior of adding trailers to every commit during a root rebase. This builds on the series' earlier work standardizing test domains to example.com` and using the `expect_trailer_msg` helper for verification. The test continues to verify trailer addition works across the commit history by checking both the HEAD and HEAD^ commits.

This appears to be a straightforward test enhancement that exercises an important configuration integration point for the new trailer functionality. It follows the series' pattern of comprehensive test coverage while keeping the test changes minimal and focused. The patch maintains all existing test assertions while adding the config mapping verification.