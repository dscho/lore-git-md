This is a follow-up in the performance optimization thread for `git last-modified`, addressing signed/unsigned comparison issues in the patch series. Taylor Blau points out a minor oversight in their previous suggestion - the `path` parameter in the newly proposed `last_modified_mark_non_treesame()` function should be marked as `const` since it's not modified. This builds on Junio Hamano's earlier fixes for compiler warnings related to type safety in the path-to-index mapping code.

The change represents a refinement of the solution that inlines path lookup and bitmap operations while avoiding the signed/unsigned comparison warnings that were triggering compiler errors. This maintains the significant performance improvements (7.9x-111.7x speedups on linux.git) achieved by the new algorithm while improving code quality.

The email is brief but touches on an important implementation detail in the ongoing effort to optimize the `git last-modified` command's path-tracking performance using EWAH bitmaps and generation-sorted commits. The thread has seen productive collaboration between multiple contributors to refine both the algorithm and its type safety aspects.