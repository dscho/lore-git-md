This is a final review comment on a bugfix/performance patch series that refactors strbuf usage in gpg-interface.c. Christian Couder provides a minor nitpick on the commit message formatting for the first patch in the v5 series, which replaces strbuf_split_max() with direct string parsing in get_ssh_finger_print().

The reviewer suggests making two sentences in the commit message clearly part of the same paragraph for better readability, but explicitly states the patch itself looks fine. This appears to be the last bit of polish needed before the series can be merged, as the thread summary indicates all technical issues have been resolved across five iterations.

The change is part of an effort to eliminate unnecessary strbuf overhead in SSH key handling, with both patches in the series now using direct string manipulation (strchr() + xmemdupz()) while maintaining security checks. The v5 iteration has already addressed all previous feedback, including proper buffer handling and error reporting adjustments.