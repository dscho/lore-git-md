This is a test modernization patch in the ongoing `--trailer` option for `git rebase` series (v5 24/29). It refactors the test suite to use a new `expect_trailer_msg()` helper that wraps `test_commit_message` and deduplicates trailer verification logic. The changes eliminate temporary files and simplify assertions by using a shared `REVIEWED_BY_TRAILER` constant.

Key technical changes include:
- Introduces `REVIEWED_BY_TRAILER` constant to standardize trailer text
- Replaces `create_expect` file-based checks with direct `expect_trailer_msg` calls
- Updates conflict resolution test to check HEAD directly rather than HEAD~2
- Modifies both `--apply` rejection and `--root` tests to use the new helper
- Maintains all existing test coverage while reducing boilerplate

The patch continues the series' pattern of test suite improvements, now focusing on DRY principles by centralizing trailer verification. It touches only t3440-rebase-trailer.sh, modifying 23 lines with a net reduction of 12 lines. The changes align with previous test modernizations in this series while preserving all behavioral assertions.