This is a refactoring patch in the large trailer/rebase series, specifically part 3 of 29 in the fifth iteration. The change simplifies how trailer text is passed to the `amend_strbuf_with_trailers()` helper by removing redundant `--trailer=` prefix handling. Instead, callers now pass the plain trailer text directly.

The patch modifies three files: `builtin/commit.c` and `builtin/tag.c` switch from `OPT_PASSTHRU_ARGV` to `OPT_CALLBACK_F` for the trailer option, while `trailer.c` drops the prefix-skipping logic. This is a clean internal improvement that reduces code duplication and makes the API more straightforward.

As part of a well-reviewed series that's been through multiple iterations, this change appears solid and focused. It maintains backward compatibility while cleaning up the implementation. The patch follows the series' pattern of moving trailer processing into core Git functionality with cleaner interfaces.