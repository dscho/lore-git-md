Refactoring patch converting `git show-ref` to use the new `reference_get_peeled_oid()` interface as part of the larger refs subsystem modernization effort.

This is patch 8/14 in Patrick Steinhardt's series that builds on the merged `struct ref`/`struct reference` separation work. The patch focuses on unifying the peeling behavior in `builtin/show-ref.c` by ensuring both code paths (pattern matching and exact reference checking) use the new `struct reference` wrapper consistently.

The key change is modifying the `show_one()` function to take a `struct reference` parameter instead of separate `refname` and `oid` parameters. This allows using `reference_get_peeled_oid()` for both iterator-based and direct reference cases. For the direct reference case (when checking exact matches), the patch constructs a minimal `struct reference` on the stack since the peeled OID wouldn't be available anyway in this context.

The changes are mechanical but important for consistency - they eliminate the last remaining use of `peel_iterated_oid()` in this command while maintaining the same behavior. The patch touches 32 lines across `builtin/show-ref.c`, mostly parameter passing and error handling adjustments to accommodate the new interface.

This fits into the series' broader goal of cleaning up the refs interface by:
1. Using the new `struct reference` wrapper consistently
2. Eliminating direct use of the old `peel_iterated_oid()` function
3. Preparing for later optimizations that rely on pre-peeled OIDs

The change is low-risk since it doesn't modify the actual peeling logic or behavior - it's purely an interface update to match the new refs subsystem architecture. The patch maintains all existing functionality while making the code more consistent with the direction established in earlier patches of this series.