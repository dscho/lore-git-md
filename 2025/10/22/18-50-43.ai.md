Refactoring patch for `git replay` option validation.

This is the first patch in a 3-part series finalizing atomic reference updates for `git replay`. It prepares for the upcoming `--ref-action` option by standardizing the command's option validation to use `die_for_incompatible_opt2()`. The change replaces a custom error message for conflicting `--advance` and `--contained` options with the standard Git helper function.

The patch is purely mechanical refactoring with no behavior change, converting the existing validation check to use the more maintainable shared infrastructure. This establishes a pattern that will be used when adding validation for the new `--ref-action` option in subsequent patches. The change is minimal (3 lines modified) and focused on the `builtin/replay.c` file.

This preparatory work follows established Git conventions for option handling and helps maintain consistency across commands. The commit message clearly explains the motivation as enabling clean addition of future option validation, particularly for the atomic ref updates feature being introduced in this series. The patch appears straightforward and uncontroversial, with the series already having reached consensus on technical approach and naming.