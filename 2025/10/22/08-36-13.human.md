This is a bugfix for the `git bisect` command's handling of subcommands. The patch addresses two related issues that were introduced when `git bisect` was converted to a full builtin in 2022:

1. `git bisect help` currently fails silently (if in a bisect session) or triggers an interactive autostart prompt, rather than showing usage information as documented.
2. Invalid subcommands like `git bisect foobar` exhibit the same incorrect behavior instead of properly failing with an error message.

The fix adds explicit handling for the `help` subcommand to show usage, validates unrecognized commands before processing them as state commands, and ensures invalid commands fail cleanly with proper error messages. The changes are minimal (6 lines modified) and focused on the subcommand validation logic in `builtin/bisect.c`.

This appears to be a straightforward correction of clearly unintended behavior, with a clean implementation that maintains backward compatibility for valid state commands while fixing the documented `help` behavior. The commit message provides clear context about when and how the bugs were introduced.