This is a technical discussion about type choices in the xdiff refactoring series, specifically addressing the `char*` to `uint8_t*` conversion for FFI compatibility. Ezekiel Newren responds to Phillip Wood's concerns about cast proliferation and type choices in the context of Rust FFI preparation.

The email clarifies the scope of the type changes, explaining they're intentionally limited to xdiff's core structures (`xrecord_t` and `xdfile_t`) to maintain cross-language transparency while avoiding broader refactoring. Newren provides specific technical rationale for preferring `uint8_t` over `char` - FFI tooling (cbindgen/bindgen) doesn't properly handle `char`/`u8` conversions - Signedness differences between `char` and `uint8_t` cause compilation issues in developer mode - The change is focused on structures fundamental to xdiff's data passing

The response references prior discussion about Rust FFI motivation (Junio's suggestion to be explicit about this) and addresses Phillip's concern about cast proliferation by emphasizing the limited scope. It also links to a previous email detailing problems with C types in FFI contexts, suggesting this is part of a broader interoperability challenge.

Key technical details for future reference: - Structures affected: `xrecord_t.ptr`, `xdfile_t` members - FFI tools mentioned: cbindgen, bindgen - Specific type conflict: `std::ffi::c_char` vs `u8` in Rust - Build impact: `DEVELOPER=1` compilation failures due to signedness

The discussion remains focused on balancing FFI requirements against C code cleanliness, with Newren advocating for type safety at the boundary while acknowledging the transitional complexity. This directly continues the thread's examination of how aggressively to pursue type alignment between C and Rust implementations.