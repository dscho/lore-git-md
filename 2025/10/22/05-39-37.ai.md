This is a test modernization patch in the ongoing `--trailer` rebase series (v5 17/29). It refactors the test setup in t3440-rebase-trailer.sh to create expected trailer files at their point of use rather than upfront in the shared history setup.

The change moves the `create_expect` calls from the initial repository setup block into the individual test cases that actually need them (`rebase -m --trailer` and `rebase --root --trailer`). This eliminates unused fixture creation for tests that don't need these expectations, making the test file's dependencies clearer and reducing unnecessary setup overhead.

The patch is a straightforward cleanup that follows good test hygiene principles - creating test data only when needed rather than upfront. It maintains all existing test coverage while making the test file's structure more maintainable. This appears to be a mechanical improvement that doesn't affect the actual functionality being tested, just the test organization.