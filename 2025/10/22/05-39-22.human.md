Refactoring patch in a series adding `--trailer` support to `git rebase`.

This is the second patch in a 29-part series that moves trailer handling from an external helper into Git's core. The change reintroduces an `interpret_trailers()` helper function that was previously removed, consolidating the duplicate read/process/write logic that was spread across both file and stdin handling paths. The helper maintains all existing error handling and output behavior while reducing code duplication.

The patch is part of a larger effort to enable `git rebase` to manipulate commit message trailers without forking an external process. This foundational work shows careful attention to preserving existing behavior while cleaning up the implementation. The changes are focused on `builtin/interpret-trailers.c` and maintain backward compatibility with all current uses of the command.

As part of a well-structured series that has undergone multiple rounds of review, this patch appears to be a straightforward refactoring that sets up subsequent integration with the rebase machinery. The commit message clearly explains the change and its context within the larger effort.