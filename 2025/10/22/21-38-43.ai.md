This is a discussion about type mapping strategy for Rust FFI compatibility in the ongoing xdiff refactoring series. Junio Hamano responds to Ezekiel Newren's suggestion about reviving a `rust_types.h` file for C-to-Rust type mappings, proposing an alternative approach where each language uses its native type names with clear conversion at the FFI boundary.

The key technical points are:
1. Hamano suggests using conceptually equivalent types in each language (e.g. `i64` in C for Rust's 64-bit signed integer) rather than trying to match type names exactly
2. He agrees with the need for clear documentation of type mappings but questions whether a header file is the right approach
3. The discussion highlights the need to handle cases where types don't have direct equivalents across languages
4. This continues the thread's theme of establishing systematic type handling for FFI while maintaining type safety

This email represents maintainer feedback on one of the open questions from the thread summary - specifically the adoption strategy for type mapping (rust_types.h vs documentation). Hamano's response suggests a preference for documentation over header files while still supporting the goal of clear type mapping standards. The exchange shows the series moving from implementation details toward establishing project-wide conventions for Rust interoperability.