Bugfix follow-up regarding Git's `**` pattern matching behavior in `.gitignore` files. This is a brief reply from Sruteesh Kumar to Jeff King's proposed fix for the `**` pattern matching issue in `dir.c`. The email acknowledges Jeff's patch as a hack that solves the problem without major code impact, while expressing concern about potential regression issues. Sruteesh requests review from the Git community but does not provide additional technical analysis or alternative solutions. The message serves primarily to bump the thread and solicit wider feedback on Jeff's RFC patch, which modifies `match_pathname()` to preserve one character of prefix context when passing paths to `fnmatch()`. The core technical issue remains as described in the thread summary: ensuring `foo**/bar` doesn't incorrectly match `foobar` while maintaining optimization benefits of prefix stripping. No new test cases or implementation concerns are raised in this brief response.