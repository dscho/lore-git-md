Refactoring patch in a performance optimization series. This is patch 4/14 in Patrick Steinhardt's series that builds on the refs subsystem modernization work. The patch converts reference status flags from preprocessor defines to an enum and changes the flags field from `int` to `unsigned int` to follow modern Git coding conventions.

The change is mechanical - it replaces the existing `#define` constants with an enum and updates the type of the `flags` field in `struct reference`. The actual flag values and their meanings remain the same (symbolic refs, packed refs, broken refs, and malformed names). This is part of a larger effort to clean up and standardize the refs subsystem interfaces while maintaining the separation between transport-related and core ref handling.

The patch appears straightforward and well-contained, touching only refs.h with 21 lines added and 20 removed. It follows the series' pattern of incremental improvements to the refs infrastructure while preserving existing behavior. The change aligns with Git's current coding standards and should make the code more maintainable going forward.