This is a refactoring patch series that has completed its review cycle, cleaning up redundant code paths in the refs subsystem. The series removes the legacy `pack_refs` interface in favor of the newer `optimize` functionality, which was added earlier as part of the ref backend abstraction effort.

The author, Karthik Nayak, has addressed all review feedback from Patrick Steinhardt (the ref backend maintainer) in version 2 of the series. Changes include squashing commits, renaming variables, and improving commit messages. The series also includes a small but important test infrastructure fix that moves `test_done` out of a shared script into individual test files, enabling future test extensions.

With Patrick's ack, this appears ready for merging. The changes are purely internal with no behavior modifications, continuing Git's ongoing effort to modernize and simplify the refs backend code.