This is a discussion about the `rerere.enabled` default flip proposal, focusing on identifying and addressing usability issues before implementation. Junio Hamano responds to Ben Knoble's request to compile a list of known sharp edges in the rerere functionality that need polishing before enabling it by default.

The email highlights a specific technical limitation: delete/modify conflicts are currently not recorded and replayed due to safety concerns about discarding modifications that might need different resolutions in future conflicts. However, Junio notes that in practice, users often want to resolve such conflicts by deleting the path. This reveals a mismatch between the conservative safety design and common user expectations.

The footnote explains the technical rationale - modifications that are safe to discard in one context might require different resolutions if the underlying conflict changes. This shows the tension between safety mechanisms and practical workflow needs in the rerere subsystem.

The discussion continues the case that while rerere is technically mature, several user-facing aspects need attention before flipping the default. This email specifically adds delete/modify conflict handling to the growing list of usability concerns that includes unclear messaging and suboptimal workflows for handling incorrect cached resolutions. The thread appears to be moving toward compiling a comprehensive list of these issues to address before the default change.