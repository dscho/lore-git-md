This is the final patch in a series implementing atomic reference updates for `git replay`, adding configuration support for the new behavior. The patch introduces a `replay.refAction` config option that lets users set their preferred default behavior (atomic updates or pipeline output) while still allowing per-command overrides with `--ref-action`.

The implementation follows Git's standard configuration precedence - command-line options override config values. The config accepts the same values as the CLI (`update` for atomic ref updates, `print` for pipeline output) and provides clear error messages for invalid values. The patch includes thorough tests verifying config-driven behavior, CLI override precedence, and error handling.

This completes the series' transition from RFC to merge-ready state, with all design questions resolved and naming finalized as `--ref-action`/`replay.refAction` per maintainer feedback. The implementation is clean separation of concerns, with config handling added after CLI option processing but before the default fallback. Documentation is comprehensive, explaining both the config option and its relationship to the command-line flag.

The patch appears well-crafted with clear commit message, appropriate test coverage, and attention to edge cases. It represents the culmination of several rounds of review addressing performance, interface design, and naming concerns from multiple contributors. With this config support in place, the atomic reference update feature is now fully usable in both interactive and scripted contexts.