This is a refactoring patch in the ongoing trailer infrastructure series, specifically converting a variable from `int` to `bool` to better match its logical usage.

The patch changes the type of `had_trailer_before` in `trailer.c` from `int` to `bool`, as the variable is used as a boolean flag indicating whether trailers existed in the input message before processing. This is a straightforward modernization that aligns with Git's codebase conventions for boolean values, though it doesn't affect functionality.

This change is part of the larger trailer refactoring effort that's now in its 5th iteration, where earlier patches have focused on API cleanup and memory optimizations. The patch appears mechanically sound and uncontroversial, being a simple type change with no behavioral impact. It follows the series' pattern of incremental improvements to the trailer processing internals.