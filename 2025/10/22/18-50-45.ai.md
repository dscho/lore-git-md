This is the final patch in a 3-part series implementing atomic reference updates for `git replay`, now adding configurable default behavior via `replay.refAction`. The patch introduces configuration support matching the CLI's `--ref-action` option, with the same `update`/`print` values and clear precedence rules (CLI overrides config).

Key technical details:
- Adds new `replay.refAction` config option in Documentation/config/replay.adoc
- Implements config parsing in builtin/replay.c with early validation
- Maintains same enum-based type safety as CLI option
- Tests cover config-driven behavior, CLI override precedence, and error handling
- Follows Git's standard config precedence model
- Documents default fallback to `update` mode when unset

The implementation matches the series' design consensus, with config values mirroring CLI options exactly (`update`/`print`) as previously agreed. Test coverage verifies all specified behaviors including error cases. This completes the atomic ref update feature by making the behavior configurable at both global (config) and per-invocation (CLI) levels while maintaining the same safety guarantees.