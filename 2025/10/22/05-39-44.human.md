This is a test modernization patch in the ongoing `--trailer` rebase feature series (v5 24/29). It refactors the test suite for the new rebase trailer functionality by introducing a helper function `expect_trailer_msg()` to simplify trailer verification in tests.

The patch replaces manual test file creation with a standardized helper that wraps `test_commit_message` and uses a constant `REVIEWED_BY_TRAILER` definition. This eliminates temporary files and makes the test assertions more direct. The changes include:
- Updating conflict resolution tests to check HEAD directly rather than HEAD~2
- Applying the helper to both --apply rejection and --root test cases
- Maintaining the same test coverage while reducing boilerplate

This appears to be a straightforward cleanup that follows the series' pattern of test modernization, building on previous infrastructure changes to the trailer handling system. The patch is focused and well-contained, with clear commit message explaining the rationale for each change.