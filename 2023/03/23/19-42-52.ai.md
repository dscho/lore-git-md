This is a follow-up discussion about optimizing `git branch --contains` performance through exclusion patterns.

Junio C Hamano responds to Oswald Buddenhagen's feature request by suggesting an existing workaround using `git branch --list --contains` with a glob pattern (`\??/\*`) to limit the search to topic branches while excluding integration branches like 'next' and 'seen'. This approach leverages Git's existing branch listing capabilities rather than introducing new exclusion syntax.

The maintainer's response indicates that while the performance concern is valid, there may already be sufficient tooling to address the use case without modifying the `--contains` implementation. The suggested pattern specifically targets local work-in-progress branches (matching Oswald's stated need) while avoiding the overhead of scanning all branches.

This moves the discussion toward evaluating whether existing functionality adequately solves the problem versus requiring new exclusion pattern support. The thread remains in the exploration phase, with Junio providing practical guidance rather than rejecting the idea outright.