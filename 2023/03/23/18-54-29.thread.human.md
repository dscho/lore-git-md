Here is the updated human-readable thread summary:

This thread began with Oswald Buddenhagen reporting a performance issue where `git branch --contains` took 1.5 minutes to complete when replace refs were present. Jeff King (Peff) diagnosed the root cause: replace refs invalidate commit-graph optimizations, causing Git to fall back to slower algorithms. He provided an immediate workaround (`-c core.useReplaceRefs=false`) that reduced runtime to 30ms.

The discussion then turned to Git's design choices. Peff explained why the implementation conservatively disables optimizations when replace refs exist - maintaining correct behavior was prioritized over performance in this edge case. Oswald followed up with questions about why users aren't warned when optimizations are disabled, given that commit graphs are now built during routine operations like GC.

In the latest message, Oswald acknowledges the challenge of warning users without being overly noisy. He suggests two approaches: allowing users to manually suppress warnings (though this risks them missing future fixes), or making warnings advisory and explicitly configurable. While the core performance issue is solved, the thread continues to explore how Git might better handle this edge case from a user experience perspective.

The exchange has provided valuable insights into Git's commit-graph optimization boundaries and surfaced clear opportunities to improve handling of similar cases in future versions. Peff's technical explanations and Oswald's user-focused follow-ups have created a comprehensive record of both the immediate solution and the broader design considerations.