This is a follow-up in the discussion about optimizing `git branch --contains` performance. Oswald Buddenhagen clarifies that Junio's suggested workaround (`git branch --list --contains` with glob patterns) doesn't fully address the core issue - the command's fundamental inefficiency in large repositories like the Linux kernel tree.

The key problem remains: the current implementation performs expensive sequential revision walks for each branch, which becomes painfully slow (30+ seconds in Oswald's case) when searching local work branches. He suggests a potential optimization path - performing a simultaneous descent across all branches (similar to how `merge-base` operates) - but notes this would still waste cycles when the commit isn't found on any local branch.

The email highlights that the performance bottleneck isn't just about filtering results (which glob patterns help with), but the underlying algorithm's inefficiency when dealing with deep histories and many branches. Oswald's technical explanation suggests he's analyzed the actual behavior and identified where the time is being spent.

This moves the discussion from workarounds to potential algorithmic improvements in the branch containment checking mechanism itself. The thread remains constructive, with Oswald providing concrete performance data and technical rationale for why a deeper solution might be needed.