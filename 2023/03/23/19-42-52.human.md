This is a follow-up discussion about optimizing `git branch --contains` performance by limiting search scope.

Junio C Hamano responds to Oswald Buddenhagen's feature request for exclusion patterns in `git branch --contains` by suggesting an existing workaround: using `--list` with a glob pattern (`\??/\*`) to limit the search to topic branches while excluding integration branches like `next` and `seen`. This achieves similar results to the proposed exclusion syntax without requiring new functionality.

The maintainer's response indicates the requested behavior may already be possible through existing mechanisms, though the glob approach is less precise than explicit exclusions would be. This suggests the feature request might not need implementation if the current workaround proves sufficient for most use cases.

The thread remains in exploratory mode, with Junio seeking to understand whether the existing solution adequately addresses Oswald's performance concerns with scanning all branches. No technical objections to the proposal have been raised yet, but the maintainer appears to be probing whether the change is truly necessary.