Here is the updated dense AI thread summary incorporating the new email:

Thread subject: feature request for `git branch --contains` to support exclusion patterns (e.g. `^ref` syntax). Originating author: Oswald Buddenhagen. Thread type: feature request exploring performance optimization in branch listing machinery.

Core problem: `git branch --contains` performance issues, resolved to be caused by replace refs disabling commit-graph optimizations. Discussion has progressed from diagnosis to examining Git's design choices around this case and potential UX improvements.

Current status: Jeff King (Peff) confirmed the workaround (`-c core.useReplaceRefs=false`) solves Oswald's immediate issue (1m40s->30ms). Oswald has now responded to Peff's design rationale, proposing UX improvements for the optimization-disabled case.

Key technical details:
- Replace refs invalidate commit-graph invariants about commit generations/ancestry
- Current implementation conservatively disables optimizations when replace refs exist
- Workaround remains effective: `-c core.useReplaceRefs=false` bypasses the restriction
- Historical context: initial implementation chose simple disable over complex versioning
- Automatic graph building (GC/fetch) complicates warning implementation
- Core components involved: commit-graph loading heuristics, replace ref handling

Open questions: While performance issue is resolved, design questions remain:
- Whether to implement versioned commit-graphs tracking replace ref state (unimplemented)
- How to warn about disabled optimizations without spamming users (Oswald suggests configurable warning levels)
- Whether the repository's replace ref is needed for daily work

Participant positions: Jeff King provided root cause analysis, workaround, and design rationale. Oswald Buddenhagen confirmed solution, raised architectural questions, and now proposes UX approaches for optimization warnings. Peff's technical investigation remains authoritative while Oswald focuses on improving user-facing aspects.

Related work: Connects to Git's commit-graph loading heuristics, replace refs behavior, and performance optimization edge cases. The exchange provides concrete data about optimization impacts and surfaces clear opportunities to improve Git's handling of similar cases in future versions, both technically and from a UX perspective.