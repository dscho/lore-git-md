Here is the updated dense AI thread summary incorporating the new email:

Thread subject: feature request for `git branch --contains` to support exclusion patterns (e.g. `^ref` syntax). Originating author: Oswald Buddenhagen. Thread type: feature request exploring performance optimization in branch listing machinery.

Core problem: `git branch --contains` performance issues, with two distinct optimization paths identified: 1) replace refs disabling commit-graph optimizations (original issue), and 2) untapped potential of cached DFS algorithm (`contains_tag_algo`) for branch operations when generation numbers are available.

Current status: Jeff King (Peff) confirmed the workaround (`-c core.useReplaceRefs=false`) solves Oswald's immediate issue (1m40s->30ms). Kristofer Karlsson now demonstrates additional 3.7-16x speedups from enabling `contains_tag_algo` for branch operations when commit-graph exists, with benchmarks from Spotify's large monorepo.

Key technical details:
- Replace refs invalidate commit-graph invariants about commit generations/ancestry
- Current implementation conservatively disables optimizations when replace refs exist
- Workaround remains effective: `-c core.useReplaceRefs=false` bypasses the restriction
- New optimization opportunity: `contains_tag_algo` provides 3.7-16x speedups for branch operations when generation numbers exist
- Benchmark data shows crossover point around 3-6 refs where both algorithms perform similarly
- Core components involved: commit-graph loading heuristics, replace ref handling, ref-filter internals

Open questions:
- Whether to implement versioned commit-graphs tracking replace ref state (unimplemented)
- How to warn about disabled optimizations without spamming users (Oswald suggests configurable warning levels)
- Whether the repository's replace ref is needed for daily work
- Implementation path for enabling `contains_tag_algo` in branch operations (Kristofer's proposal)

Participant positions: Jeff King provided root cause analysis and workaround for replace refs issue. Oswald Buddenhagen confirmed solution and raised architectural questions. Kristofer Karlsson now contributes performance data and concrete optimization proposal for branch operations. Peff's technical investigation remains authoritative reference point while new data expands optimization opportunities.

Related work: Connects to Git's commit-graph loading heuristics, replace refs behavior, and performance optimization edge cases. The thread now encompasses both immediate workarounds and broader optimization opportunities, with concrete data from production-scale repositories validating the proposed changes. Kristofer's benchmarks provide clear evidence for enabling `contains_tag_algo` in branch operations when generation numbers are available.