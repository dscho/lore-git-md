This is a technical follow-up in the discussion about optimizing `git branch --contains` performance. Oswald Buddenhagen clarifies that Junio's suggested glob pattern workaround (`\??/\*`) doesn't address the core performance issue - the command still performs expensive sequential revision walks when searching for commit containment across branches.

The key insight is that the current implementation checks branches one at a time rather than walking commit graphs simultaneously, which becomes particularly wasteful when the target commit isn't found in any local branch (forcing full walks to root commits). Oswald suggests a potential optimization path where the revision walk could be performed across all candidate branches concurrently, similar to how `merge-base` operations work.

The email highlights an important distinction between the original feature request (adding exclusion patterns) and the underlying performance problem (inefficient revision walking strategy). While the thread began exploring syntactic solutions, this response shifts focus to algorithmic inefficiencies in the branch containment checking implementation itself. The discussion remains technical and constructive, with Oswald providing concrete details about the performance characteristics observed in the Linux kernel repository.