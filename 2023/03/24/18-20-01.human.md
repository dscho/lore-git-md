This is a technical discussion about optimizing `git branch --contains` performance in large repositories. Derrick Stolee responds to Oswald Buddenhagen's finding that commit-graph optimizations don't sufficiently address the inefficiency when searching for a small number of commits in massive histories (1M+ commits).

Stolee acknowledges the generation number optimization should theoretically provide orders-of-magnitude improvement unless dealing with extremely old commits. He then suggests two alternative approaches that might better address the original request for branch filtering: using `git for-each-ref` with explicit branch lists or ref patterns to limit the search scope, rather than checking all branches.

The exchange highlights the technical tension between data structure optimizations (commit-graph) and algorithmic approaches (targeted branch searches). Stolee's suggestions aim to work within existing infrastructure while still providing performance benefits, though it's unclear if they fully address Oswald's concern about wasted computation when no branches contain the target commits. The thread continues exploring this performance optimization space for large repositories.