This is a technical deep-dive into the performance characteristics of `git branch --contains` and related commands, with Jeff King (Peff) analyzing the commit-graph interaction and reachability algorithms. The email follows up on Derrick Stolee's suggestion about commit-graph usage, but Peff identifies that the current implementation still performs separate walks per reference even with generation numbers enabled.

Peff proposes a patch to enable the tag algorithm (which maintains a cache across walks) for all cases where generation numbers are available, showing modest improvements (30ms to 23ms in his linux.git test). However, he notes the bigger win comes simply from having the commit-graph at all (1m40s to 30ms). The email raises open questions about whether Git could use more sophisticated reachability filtering infrastructure that already exists for `--merged` operations.

The discussion remains focused on technical optimization paths rather than the original feature request about exclusion patterns, highlighting a disconnect between the empirical performance observations and the theoretical expectations about commit-graph benefits. Peff's analysis suggests the current implementation may not be fully leveraging available optimizations.