This is a performance investigation thread about `git branch --contains` behavior with replace refs. Jeff King (Peff) identifies the root cause of Oswald Buddenhagen's slow performance - the presence of a replace ref in the repository is disabling commit-graph optimizations entirely. 

Peff explains that replace refs make commit-graph data unreliable since they can modify commit ancestry relationships. He suggests two solutions: temporarily disabling replace refs with `-c core.useReplaceRefs=false` for faster operations, or configuring it off for daily use. The email also invites further benchmarking to see if additional optimizations would still provide meaningful speedups.

The thread has progressed from initial performance questions to concrete diagnosis (via strace) and now a clear explanation of the technical constraint. This appears to resolve the mystery while providing practical workarounds. The discussion remains open for further optimization analysis if Oswald wants to pursue it.