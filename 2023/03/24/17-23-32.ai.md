This is a technical follow-up in the ongoing discussion about optimizing `git branch --contains` performance. Derrick Stolee suggests checking whether the commit-graph is being used effectively, as its presence should enable a single reachability walk from all starting points rather than separate walks for each branch. He specifically points to `repo_is_descendant_of()` in commit-reach.c as the implementation detail that changes behavior based on commit-graph availability.

The email provides a concrete suggestion to test performance after running `git commit-graph write --reachable`, which would ensure the commit-graph contains all reachable commits. This could potentially address Oswald's observed performance issues without requiring algorithmic changes to the branch listing machinery itself. The response stays focused on the technical aspects while offering a specific diagnostic step that might reveal whether the current implementation is performing as optimally as possible given the available data structures.