This is a technical follow-up in the ongoing discussion about optimizing `git branch --contains` performance. Derrick Stolee suggests checking whether the commit-graph is being used effectively, noting that with `git commit-graph write --reachable`, the command should perform a single reachability walk from all starting points rather than separate walks per branch. He points to `repo_is_descendant_of()` in commit-reach.c as the relevant implementation.

The email provides a concrete performance optimization suggestion that addresses the core issue Oswald identified - the inefficiency of sequential revision walks. Stolee's recommendation focuses on ensuring proper use of existing infrastructure (the commit-graph) rather than proposing new algorithms or syntax changes. This positions the discussion toward practical solutions path that could provide immediate benefits without requiring major code changes.

The response maintains the thread's technical depth while shifting focus from theoretical optimizations to a specific, actionable check that could significantly improve performance in real-world Linux kernel repository case Oswald mentioned. It also helpfully points to the relevant implementation details for further investigation.