This is a technical follow-up in the `git branch --contains` performance optimization thread. Oswald Buddenhagen responds to Derrick Stolee's suggestion about using the commit-graph to improve performance, reporting that running `git commit-graph write --reachable` did not resolve the fundamental inefficiency he's observing.

The key point is that even with commit-graph optimizations, the operation remains wasteful when searching for a small number of commits (tens) in a very large repository (over a million commits). This reinforces Oswald's original position that algorithmic changes are needed rather than just better utilization of existing data structures. The response maintains focus on the core performance issue while providing concrete feedback on the suggested diagnostic step.

The exchange highlights the tension between Derrick's data-structure-aware optimization approach and Oswald's push for deeper algorithmic changes to handle the large-repository case more efficiently. The thread continues to explore whether the solution lies in better utilizing existing infrastructure or requires more fundamental changes to the branch-listing machinery.