The thread continues to explore how to fix `git describe`'s incorrect commit traversal order, which currently follows commit-date rather than proper ancestry relationships. The core issue causes wrong tag descriptions in repositories with complex merge histories, and the discussion has evolved to balance correctness against performance impacts.

Jeff King (Peff) has weighed in with technical analysis of Ben Boeckel's proposed topological sorting solution, identifying significant performance concerns. The current patch would be prohibitively slow for large repositories without commit-graphs, as it uses `repo_is_descendant_of()` which requires two full traversals per comparison. Peff suggests more efficient alternatives: leveraging generation numbers (from commit-graphs when available) or adapting existing topological sorting infrastructure from Git's revision traversal code.

The discussion now centers on implementation strategy - whether to modify `git describe`'s custom traversal to incorporate these optimizations or to refactor it to use the revision machinery's topological sorting directly. Performance remains the key constraint, particularly for repositories without commit-graphs where the current approach could take minutes rather than seconds. The thread remains focused on finding a solution that maintains the corrected behavior while keeping performance acceptable across different repository sizes and commit-graph availability.