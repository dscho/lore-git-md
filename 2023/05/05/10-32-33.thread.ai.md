Thread identity: Documentation patch series (v2) updating git-restore.txt to remove outdated information about `git restore --patch` pathspec limitations.

Core problem: The documentation incorrectly stated that `git restore --patch` couldn't handle pathspecs, a limitation that was true with the old Perl implementation of `add -i` but became obsolete when the C implementation (supporting pathspecs) was introduced in Git 2.35 (commit 0527ccb1b5).

Current status: Version 2 of the patch, with Johannes Schindelin (Dscho) reviewing and confirming the change is correct. The revision improved commit message formatting without altering technical content. Ready for inclusion.

Key technical details: Removes three lines from git-restore.txt's `--patch` section. The underlying behavior change came from the `add -i` rewrite from Perl to C in 2021, which added pathspec support to interactive modes.

Participant positions:
- Patch author identified and fixed the documentation inaccuracy through bisection
- Johannes Schindelin reviewed, verified the technical basis, and indicated readiness for inclusion

Related work: Connects to the broader `add -i` rewrite effort that replaced Perl implementations with C built-ins.