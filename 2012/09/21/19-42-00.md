# Re: [PATCH v2 14/14] Add git-check-ignore sub-command

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | Adam Spiers <git@adamspiers.org> |
| **Date** | 2012-09-21T12:42:00-07:00 |
| **Message-ID** | [7v392b5fjr.fsf@alter.siamese.dyndns.org](https://lore.kernel.org/git/7v392b5fjr.fsf@alter.siamese.dyndns.org) |

---

**Thread**: [thread](./../02/00-12-02.thread.md#:~:text=2012/09/21/19-42-00)

Adam Spiers <git@adamspiers.org> writes:

> ```diff
> +expect_in () {
> +	dest="$HOME/expected-$1" text="$2"
> +	if test -z "$text"
> +	then
> +		>"$dest" # avoid newline
> +	else
> +		echo -e "$text" >"$dest"
> ```
>


This breaks when your shell is not "bash".

> ```diff
> +test_check_ignore () {
> +	args="$1" expect_code="${2:-0}" global_args="$3"
> +
> +	init_vars &&
> +	rm -f "$HOME/stdout" "$HOME/stderr" "$HOME/cmd" &&
> +	echo $(which git) $global_args check-ignore $quiet_opt $verbose_opt $args \
> +		>"$HOME/cmd" &&
> ```
>


Don't use which in scripts.  Is this just leftover cruft from debugging?


