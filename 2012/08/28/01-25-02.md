# Re: [PATCH 5/5] (BROKEN) get_merge_bases_many(): walk from many tips in parallel

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | git@vger.kernel.org |
| **Date** | 2012-08-27T18:25:02-07:00 |
| **Message-ID** | [7vipc3q041.fsf@alter.siamese.dyndns.org](https://lore.kernel.org/git/7vipc3q041.fsf@alter.siamese.dyndns.org) |

---

**Thread**: [thread](./../27/23-11-58.thread.md#:~:text=2012/08/28/01-25-02)

Junio C Hamano <gitster@pobox.com> writes:

> ```diff
>  	for (i = 0; i < cnt; i++) {
> -		if (rslt[i])
> +		/*
> +		 * Is rslt[i] an ancestor of any of the others?
> +		 * then it is not interesting to us.
> +		 */
> +		for (j = 0; j < i; j++)
> +			others[j] = rslt[j];
> +		for (j = 1 + 1; j < cnt; j++)
> ```
>


s/1 + 1/i + 1/;

With that, all tests seem to pass ;-)

> ```diff
> +			others[j - 1] = rslt[j];
> +		list = merge_bases_many(rslt[i], cnt - 1, others);
> +		clear_commit_marks(rslt[i], all_flags);
> +		for (j = 0; j < cnt - 1; j++)
> +			clear_commit_marks(others[j], all_flags);
> +		while (list) {
> +			if (rslt[i] == list->item)
> +				break;
> +			list = list->next;
> +		}
> +		if (!list)
>  			commit_list_insert_by_date(rslt[i], &result);
> +		free_commit_list(list);
>  	}
>  	free(rslt);
> +	free(others);
>  	return result;
>  }
> ```
>



