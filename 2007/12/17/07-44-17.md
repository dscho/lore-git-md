# Re: [PATCH v2] threaded pack-objects: Use condition variables for thread communication.

| Header | Value |
|--------|-------|
| **From** | Johannes Sixt <j.sixt@viscovery.net> |
| **To** | Junio C Hamano <gitster@pobox.com> |
| **Date** | 2007-12-17T08:44:17+01:00 |
| **Message-ID** | [476628D1.4020300@viscovery.net](https://lore.kernel.org/git/476628D1.4020300@viscovery.net) |

---

**Thread**: [thread](./../15/23-18-53.thread.md#:~:text=2007/12/17/07-44-17)

I just discovered a theoretical resource leakage:

> ```diff
> +	/* Partition the work amongst work threads. */
>  	for (i = 0; i < delta_search_threads; i++) {
> ```
>

...
> ```diff
> +		pthread_mutex_init(&p[i].mutex, NULL);
> +		pthread_cond_init(&p[i].cond, NULL);
> ```
>


These two initializations...

> ```diff
>  	}
>  
> +	/* Start work threads. */
> +	for (i = 0; i < delta_search_threads; i++) {
> +		if (!p[i].list_size)
> +			continue;
> ```
>


... should go here because...

> ```diff
> +		ret = pthread_create(&p[i].thread, NULL,
> +				     threaded_find_deltas, &p[i]);
> +		if (ret)
> +			die("unable to create thread: %s", strerror(ret));
> +		active_threads++;
> +	}
> ```
>

...
> +	while (active_threads) {

...
> ```diff
>  		if (!sub_size) {
>  			pthread_join(target->thread, NULL);
> +			pthread_cond_destroy(&target->cond);
> +			pthread_mutex_destroy(&target->mutex);
> ```
>


... we tear down only those for which we actually started a thread.

> ```diff
>  			active_threads--;
>  		}
> +	}
> ```
>


Will send a patch this evening.

-- Hannes


