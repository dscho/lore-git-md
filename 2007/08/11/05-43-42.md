# Re: [PATCH resend] git-apply: apply submodule changes

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | skimo@liacs.nl |
| **Date** | 2007-08-10T22:43:42-07:00 |
| **Message-ID** | [7vd4xupqwh.fsf@assigned-by-dhcp.cox.net](https://lore.kernel.org/git/7vd4xupqwh.fsf@assigned-by-dhcp.cox.net) |

---

**Thread**: [thread](./../10/09-30-49.thread.md#:~:text=2007/08/11/05-43-42)

Sven Verdoolaege <skimo@kotnet.org> writes:

> ```diff
> @@ -1994,20 +2014,18 @@ static int apply_data(struct patch *patch, struct stat *st, struct cache_entry *
>  	alloc = 0;
>  	buf = NULL;
>  	if (cached) {
> -		if (ce) {
> -			enum object_type type;
> -			buf = read_sha1_file(ce->sha1, &type, &size);
> -			if (!buf)
> -				return error("read of %s failed",
> -					     patch->old_name);
> -			alloc = size;
> -		}
> +		if (read_file_or_gitlink(ce, &buf, &size))
> +			return error("read of %s failed", patch->old_name);
> +		alloc = size;
>  	}
>  	else if (patch->old_name) {
>  		size = xsize_t(st->st_size);
>  		alloc = size + 8192;
>  		buf = xmalloc(alloc);
> -		if (read_old_data(st, patch->old_name, &buf, &alloc, &size))
> +		if (S_ISGITLINK(patch->old_mode))
> +			size = snprintf(buf, alloc,
> +				"Subproject commit %s\n", sha1_to_hex(ce->sha1));
> +		else if (read_old_data(st, patch->old_name, &buf, &alloc, &size))
> ```
>


Who guarantees that ce is given to apply_data() in this codepath?


