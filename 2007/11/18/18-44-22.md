# Re: [PATCH] builtin-commit: fix partial-commit support

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | git@vger.kernel.org |
| **Date** | 2007-11-18T10:44:22-08:00 |
| **Message-ID** | [7vve7zv1fd.fsf@gitster.siamese.dyndns.org](https://lore.kernel.org/git/7vve7zv1fd.fsf@gitster.siamese.dyndns.org) |

---

**Thread**: [thread](./10-21-24.thread.md#:~:text=2007/11/18/18-44-22)

Junio C Hamano <gitster@pobox.com> writes:

> ```diff
> diff --git a/builtin-commit.c b/builtin-commit.c
> index 3e7d281..e487bc0 100644
> --- a/builtin-commit.c
> +++ b/builtin-commit.c
> ```
>


There was another...

> ```diff
> +static int list_paths(struct path_list *list, const char *with_tree,
> +		      const char *prefix, const char **pattern)
> +{
> +	struct dir_struct dir;
> +	int i;
> +	char *m;
> +
> +	for (i = 0; pattern[i]; i++)
> +		;
> +	m = xcalloc(1, i);
> +
> +	memset(&dir, 0, sizeof(dir));
> +	if (with_tree)
> +		overlay_tree_on_cache(with_tree, prefix);
> +
> +	for (i = 0; i < active_nr; i++) {
> +		struct cache_entry *ce = active_cache[i];
> +		if (ce->ce_flags & htons(CE_UPDATE))
> +			continue;
> +		if (!pathspec_match(pattern, m, ce->name, 0))
> +			continue;
> +		if (excluded(&dir, ce->name))
> +			continue;
> +		path_list_insert(ce->name, list);
> +	}
> ```
>


This "excluded", and the whole "struct dir" business is
unneeded, as we are walking on the index and finding the set of
paths the user cares about.


