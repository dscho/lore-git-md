# Re: [PATCH] Add "--expire <time>" option to 'git prune'

| Header | Value |
|--------|-------|
| **From** | Johannes Sixt <j.sixt@viscovery.net> |
| **To** | Johannes Schindelin <Johannes.Schindelin@gmx.de> |
| **Date** | 2007-11-29T15:35:22+01:00 |
| **Message-ID** | [474ECE2A.9050700@viscovery.net](https://lore.kernel.org/git/474ECE2A.9050700@viscovery.net) |

---

**Thread**: [thread](./../18/11-25-52.thread.md#:~:text=2007/11/29/14-35-22)

Johannes Schindelin schrieb:
> ```diff
> +test_expect_success 'prune --expire' '
> +
> +	BLOB=$(echo aleph | git hash-object -w --stdin) &&
> +	BLOB_FILE=.git/objects/$(echo $BLOB | sed "s/^../&\//") &&
> +	test 20 = $(git count-objects | sed "s/ .*//") &&
> +	test -f $BLOB_FILE &&
> +	git reset --hard &&
> ```
>


Here you could throw in:

```
	git prune --expire=1.hour.ago &&
	test 20 = $(git count-objects | sed "s/ .*//") &&
	test -f $BLOB_FILE &&

```

to test that the object is not pruned (and the alternate --expire syntax).

> ```diff
> +	if touch -d "Jan 1 1970" $BLOB_FILE
> +	then
> +		git prune --expire 1.day &&
> +		test 19 = $(git count-objects | sed "s/ .*//") &&
> +		! test -f $BLOB_FILE
> +	else
> +		say "Skipping test due to non-working touch -d"
> +	fi
> ```
>


-- Hannes


