# [PATCH] Do _not_ call unlink on a directory

| Header | Value |
|--------|-------|
| **From** | Thomas Glanzmann <sithglan@stud.uni-erlangen.de> |
| **To** | git@vger.kernel.org, gitster@pobox.com |
| **Date** | 2007-07-16T19:12:52+02:00 |
| **Message-ID** | [11846059721204-git-send-email-sithglan@stud.uni-erlangen.de](https://lore.kernel.org/git/11846059721204-git-send-email-sithglan@stud.uni-erlangen.de) |

---

**Thread**: [thread](./10-08-03.thread.md#:~:text=2007/07/16/17-12-52)

Calling unlink on a directory on a Solaris UFS filesystem as root makes it
inconsistent. Thanks to Johannes Sixt for the obvious fix.
---
```
 entry.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

```

```diff
diff --git a/entry.c b/entry.c
index 82bf725..1f2e34d 100644
--- a/entry.c
+++ b/entry.c
@@ -14,10 +14,10 @@ static void create_directories(const char *path, const struct checkout *state)
 		if (mkdir(buf, 0777)) {
 			if (errno == EEXIST) {
 				struct stat st;
-				if (len > state->base_dir_len && state->force && !unlink(buf) && !mkdir(buf, 0777))
-					continue;
 				if (!stat(buf, &st) && S_ISDIR(st.st_mode))
 					continue; /* ok */
+				if (len > state->base_dir_len && state->force && !unlink(buf) && !mkdir(buf, 0777))
+					continue;
 			}
 			die("cannot create directory at %s", buf);
 		}
-- 
```

1.5.2.1


