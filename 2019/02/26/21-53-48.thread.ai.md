Thread identity: bugfix for `git describe` regarding `--contains --all` with pattern matching options.

Core problem: `git describe --contains --all` silently ignores `--match` and `--exclude` patterns when they should be rejected as invalid. The issue persists in current Git, with independent confirmation from Tuomas Ahola showing both `--match=bogus` and `--exclude="*"` having no effect when combined with `--contains --all`.

Current status: Jacob Keller's initial untested patch to reject this combination remains technically correct. Eric Sunshine provided surface-level review suggesting commit message wording improvement ("bail of" -> "bail if") and optional code style change (nested if/else to else-if). No substantive technical objections were raised.

Key technical details: The fix adds validation to die() when both `--contains --all` and pattern options are present, with an error message explaining the incompatibility. The implementation path remains unchanged from Jacob's original solution.

Open questions: The patch still needs tests to prevent regression. The author may want to address the minor commit message typo and consider the optional style change in a v2, though neither is blocking for correctness.

Participant positions: Jacob Keller identified and fixed the issue. Eric Sunshine reviewed with minor wording/style suggestions, implying technical acceptance. Tuomas Ahola independently validated the bug's existence in current Git, demonstrating real-world impact.

Related work: This addresses a gap in the describe command's pattern matching behavior, which has seen other fixes around tag matching in recent releases. The independent confirmation suggests this case was overlooked in prior work.