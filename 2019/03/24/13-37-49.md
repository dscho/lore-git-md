# Re: [PATCH 1/3] rebase: teach rebase --keep-base

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | Denton Liu <liu.denton@gmail.com> |
| **Date** | 2019-03-24T22:37:49+09:00 |
| **Message-ID** | [xmqqpnqgo0k2.fsf@gitster-ct.c.googlers.com](https://lore.kernel.org/git/xmqqpnqgo0k2.fsf@gitster-ct.c.googlers.com) |

---

**Thread**: [thread](./../23/15-25-22.thread.md#:~:text=2019/03/24/13-37-49)

Denton Liu <liu.denton@gmail.com> writes:

> ```diff
>  	if (strstr(options.onto_name, "...")) {
>  		if (get_oid_mb(options.onto_name, &merge_base) < 0)
> +		    if (keep_base)
> +			die(_("'%s': need exactly one merge base with branch"),
> +				options.upstream_name);
> +		    else
>  			die(_("'%s': need exactly one merge base"),
>  			    options.onto_name);
> ```
>


If you turn a single statement body into if/else, have {} around the
body of the outer if, i.e.

```
	if (get_oid_mb(...) < 0) {
		if (keep_base)
			die(_("'%s': need exactly one merge base with branch"),
			    options.upstream_name);
		else
			die(_("'%s': need exactly one merge base"),
			    options.onto_name);
	}

```

Otherwise -Werror=danglng-else will stop compilation.

Thanks.


