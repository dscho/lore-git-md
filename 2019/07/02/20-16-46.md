# Re: [PATCH] transport-helper: enforce atomic in push_refs_with_push

| Header | Value |
|--------|-------|
| **From** | Junio C Hamano <gitster@pobox.com> |
| **To** | Emily Shaffer <emilyshaffer@google.com> |
| **Date** | 2019-07-02T13:16:46-07:00 |
| **Message-ID** | [xmqqk1d02nv5.fsf@gitster-ct.c.googlers.com](https://lore.kernel.org/git/xmqqk1d02nv5.fsf@gitster-ct.c.googlers.com) |

---

**Thread**: [thread](./00-53-40.thread.md#:~:text=2019/07/02/20-16-46)

Emily Shaffer <emilyshaffer@google.com> writes:

> ```diff
> +		if ((flags & TRANSPORT_PUSH_ATOMIC) && err) {
> +			for (struct ref *it = remote_refs; it; it = it->next)
> +				switch (it->status) {
> +				case REF_STATUS_NONE:
> +				case REF_STATUS_UPTODATE:
> +				case REF_STATUS_OK:
> +					it->status = REF_STATUS_ATOMIC_PUSH_FAILED;
> +				default:
> +					continue;
> +				}
> +		}
> ```
>



Let's write this more like so

```
		if ((flags & TRANSPORT_PUSH_ATOMIC) && err) {
			for (struct ref *it = remote_refs; it; it = it->next)
				switch (it->status) {
				case REF_STATUS_NONE:
				case REF_STATUS_UPTODATE:
				case REF_STATUS_OK:
					it->status = REF_STATUS_ATOMIC_PUSH_FAILED;
					break;
				default:
					break;
				}
		}

```

to prevent compilers from giving "implicit fallthru" warnings.


